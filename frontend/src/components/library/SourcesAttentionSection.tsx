/**
 * Phase 235 plan 11 — RED STUB. Deliberately renders nothing, so the suite beside it fails
 * by NAMING each behaviour rather than by failing to resolve an import. Replaced in the same
 * plan's GREEN commit.
 */
export interface SourcesAttentionSectionProps {
  onGoToSource?: (watchId: string) => void
}

export function SourcesAttentionSection(_props: SourcesAttentionSectionProps) {
  return null
}
