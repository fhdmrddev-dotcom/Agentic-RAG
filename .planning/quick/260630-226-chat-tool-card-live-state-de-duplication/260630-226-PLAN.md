---
phase: quick-260630-226
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/ToolCallPanel.tsx
  - frontend/src/components/chat/StatusPill.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/__tests__/components/ToolCallPanel.test.tsx
  - frontend/src/__tests__/components/MessageItem.sticky.test.tsx
autonomous: true
requirements: [SEED-098]

must_haves:
  truths:
    - "An active (preparing/running) tool rests as the SAME one-line essence shape as a finished tool — no auto-expanded heavy body, no show→collapse flicker"
    - "A running tool's right-side pill is the Variant B merged chip: state word + live ticking duration in ONE chip (`● RUNNING · 3.2s`); preparing shows `● PREPARING` (or `● PREPARING · X.X KB` when argsBytesStreamed > 0)"
    - "Clicking an active essence row expands it to the live ToolArgsLivePanel / ExecuteCodeBody, exactly like clicking a finished row reveals its result; default is COLLAPSED"
    - "The loose `→ updated todos · see panel` write_todos/workspace_write pointers below the run card no longer render (todos live only in the right Workspace panel); the ask_user PausedRunCue is preserved"
    - "The loose `Skill activated: docx` line below the run card no longer renders (the in-card `Loading skill` SkillRow already covers it)"
    - "The bottom italic `Preparing code…/Analyzing document…` echo no longer renders during a tool-active run (the RunCard header strip already carries the live verb + timer); terminal-state messages (timed_out/stopped) and the no-tools-yet thinking indicator are preserved"
    - "`message.activatedSkill` is no longer read in the render path; the FIELD stays in the type + StreamsProvider (DB-loaded-message compat) untouched (SEED-098 Change 3)"
  artifacts:
    - path: "frontend/src/components/chat/ToolCallPanel.tsx"
      provides: "Active tools collapse to the unified essence line; merged-pill active states behind the existing expand/collapse machinery"
    - path: "frontend/src/components/chat/StatusPill.tsx"
      provides: "Optional in-pill live ticking duration for the running merged pill (Variant B)"
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "Loose duplicate lines removed; ask_user cue + terminal/thinking indicators preserved"
  key_links:
    - from: "ToolCallPanel ToolEssenceLine"
      to: "StatusPill liveStartedAt"
      via: "running essence row renders the merged live-duration pill"
      pattern: "liveStartedAt"
    - from: "ToolCallPanel main map"
      to: "expandedSteps Set"
      via: "active tool collapses to essence unless its stepKey is expanded"
      pattern: "expandedSteps\\.has\\(stepKey\\)"
---

<objective>
UI-only de-duplication of the chat live-execution surface (SEED-098). Make an
ACTIVE tool render in the SAME calm one-line essence shape as a finished tool
(killing the show→collapse→change flicker), with its live body one click behind
the chevron, and delete three loose duplicate lines below the run card.

The design is ALREADY LOCKED in SEED-098 + the operator-approved sketch
(Variant B = the active-row look). This plan implements only — do NOT
re-litigate the design.

Purpose: One unified tool-card shape live and finished; remove the noise the
operator recorded (todos in 3 places, skill activation in 2, live verb in 2,
plus the auto-expanded heavy body flicker).
Output: Surgical, additive edits to two G-5-satisfied hot files + StatusPill,
with test reconciliation.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/seeds/SEED-098-chat-tool-card-deduplication-unified-essence-line.md
@.planning/sketches/098-tool-card-live-essence/index.html
@frontend/src/components/chat/ToolCallPanel.tsx
@frontend/src/components/chat/StatusPill.tsx
@frontend/src/components/chat/MessageItem.tsx

<interfaces>
<!-- Contracts the executor needs — already in the codebase, no exploration required. -->

ToolCallPanel.tsx (current shape):
- ToolEssenceLine({ tc, onExpand }) — the ONE-line resting shape, currently
  rendered ONLY for finished tools (`isFinishedCollapsed`). Renders:
  icon + toolLabel + ` → {result}` + StatusPill(done) + ChevronRight.
  data-testid="tool-result-summary".
- Per-tool render branch (inside displayItems.map, ~line 716): chooses between
  `isFinishedCollapsed ? <ToolEssenceLine/>` : `execute_code ? <TOOL_BODIES.execute_code/>`
  : `<generic heavy body>`. ACTIVE tools currently fall into the heavy-body
  branches (auto-expand) — this is the flicker source.
- expandedSteps: Set<string> + expandStep(key)/collapseStep(key) — the
  per-step expand machinery, keyed on stepKeyOf(tc, i). PRESERVE verbatim.
- nodeStateOf(tc): running/preparing → "active". StepRow renders the rail node.
- ElapsedTimer({ startedAt }) — existing live ticker (separate chip today).
- pillStatus(tc.status): preparing|running|interrupted|done → ToolStatus.

StatusPill.tsx (current props):
  { status: ToolStatus; duration?: number; runningLabel?: string; doneLabel?: string }
  - done/failed/interrupted → `{base} · {formatDuration(duration)}`.
  - preparing/running → just the verb word (NO live duration today).

MessageItem.tsx (current render sites to delete — file:line as of read):
  - 417-426: `.filter(seamKindFor !== null && name !== "ask_user").map(SeamPointer)`
    → the loose `see panel` todo pointers. DELETE.
  - 427: `hasPendingAsk(...) && <PausedRunCue/>` → KEEP (load-bearing, not duplicated).
  - 430-435: `message.activatedSkill && (<Zap/> Skill activated: ...)` → DELETE.
  - 557-576: the `hasAnyTools ? (<span>{stickyBottomLabel}…</span>)` bottom echo
    arm of the content ternary → DELETE (collapse the arm to `: null`).
  - 348-365: `stickyLabelRef` + `computedLabel` + `stickyBottomLabel` — orphaned
    after 557-576 goes (stickyBottomLabel is its only consumer). DELETE.
  - PRESERVE: 546-556 (no-tools thinking first-call), 585-592 (stopped/timed_out
    Square block), 593-606 (active-tool-alongside-content + planning indicators).
  - seamKindFor / type SeamKind / requestOpenPanel stay imported (still used by
    the reload-mode SeamCard path ~636-645). `SeamPointer` import becomes UNUSED
    → drop it (keep `type SeamKind` on the same line). `Zap` import becomes
    UNUSED → drop it. `activeTool`/`activeToolLabel`/`isMessageStreaming`/
    `outerBannerLabel`/`useRef` stay used elsewhere → KEEP.

StreamsProvider.tsx onSkillActivated (~645-660): sets BOTH `activatedSkill`
(legacy) and `activatedSkills` (array). DO NOT TOUCH — Change 3 only confirms
the legacy field is no longer READ in render.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify the active-tool shape into the essence line + merged live pill</name>
  <files>frontend/src/components/chat/ToolCallPanel.tsx, frontend/src/components/chat/StatusPill.tsx, frontend/src/__tests__/components/ToolCallPanel.test.tsx</files>
  <behavior>
    - Active running tool renders ONE essence line by default (data-testid="status-pill" with data-status="running"), NOT the heavy ToolArgsLivePanel/ExecuteCodeBody body.
    - The running pill is the Variant B merged chip: state word + a live ticking duration inside the SAME chip (mirrors `● DONE · 1.5s`). No separate ElapsedTimer chip on the active essence row.
    - A preparing tool's essence pill shows `preparing` (or `preparing · X.X KB` when tc.argsBytesStreamed > 0).
    - Clicking an active essence row adds its stepKey to expandedSteps and reveals the existing live body (ExecuteCodeBody for execute_code, ToolArgsLivePanel/generic head+body otherwise); default is collapsed.
    - Finished-tool essence + finished-card expand behavior is UNCHANGED (all existing ToolCallPanel.test cases still pass).
    - The active tool still renders its StepRow rail node in the "active" state (step-node data-node-state="active").
  </behavior>
  <action>
Implement SEED-098 Change 1. Two surgical edits + one shared-component additive prop + new tests.

(1) StatusPill.tsx — add an optional `liveStartedAt?: number` prop. When
`status === "running"` AND `liveStartedAt != null`, render the label as
`running · {formatDuration(elapsed)}` where `elapsed` ticks live via an internal
`useState`+`setInterval(250ms)` (mirror the existing ElapsedTimer cadence in
ToolCallPanel; clean up the interval on unmount). When `liveStartedAt` is absent,
behavior is byte-identical to today (existing RunCard/StatusPill tests must stay
green). Do NOT change any per-status colors — the Variant B "merged pill"
requirement is satisfied by combining word+duration into ONE chip; preserving the
shared component's existing color contract avoids cross-surface visual regressions
(D: discretion — sketch shows amber preparing, but recoloring the shared StatusPill
would blast RunCard and every other consumer; out of this surgical scope).

(2) ToolCallPanel.tsx — flip the DEFAULT visibility for active tools so they rest
collapsed to the essence line:
  - In the per-tool render branch, replace the `isFinished`/`isFinishedCollapsed`
    gate with a broader collapse predicate that also covers active tools:
    `const isActive = tc.status === "running" || tc.status === "preparing"` and
    `const isCollapsedToEssence = (isFinished || isActive) && !expandedSteps.has(stepKey)`.
    Use `isCollapsedToEssence` where `isFinishedCollapsed` was used. The active tool
    sits AT activeIndex (not `< activeIndex`), so it reaches the StepRow render and
    now hits the essence branch by default. When its stepKey IS in expandedSteps it
    falls through UNCHANGED to the `execute_code ? <TOOL_BODIES.execute_code/>` /
    generic heavy-body branch (the live streaming body — preserved verbatim).
  - Extend `ToolEssenceLine` to render the active states (branch on tc.status):
      • running → text `Running {toolLabel(tc.name)}` (primary, mirror the existing
        running verb styling) + optional ` "{summary}"`; right pill =
        `<StatusPill status="running" liveStartedAt={tc.startedAt} />` (the merged
        live chip — no separate ElapsedTimer here).
      • preparing → text `Preparing {toolLabel(tc.name)}…` + the existing
        `preparingDescription(tc)` em-dash suffix when present; right pill =
        `<StatusPill status="preparing" runningLabel={tc.argsBytesStreamed > 0 ? \`preparing · ${(tc.argsBytesStreamed/1024).toFixed(1)} KB\` : undefined} />`.
      • done/interrupted → UNCHANGED (` → {result}` + StatusPill done with duration).
    Keep the row a button with the trailing ChevronRight and `onExpand`. For the
    active essence keep data-testid="status-pill" reachable (it comes from StatusPill);
    the finished branch keeps data-testid="tool-result-summary".
  - PRESERVE all existing machinery untouched: expandedSteps Set, panelExpanded
    Record, lastPreparingIndex, ToolArgsLivePanel, ExecuteCodeBody/ExecuteCodeEditorInset,
    tc-active-wrap + tc-bottom-shimmer, the `i < activeIndex` step-summary-row branch,
    iteration dividers, dedup. Only the DEFAULT visibility of the active body flips.

(3) ToolCallPanel.test.tsx — the existing suites (per-step expand, rail/Round-N,
clientKey dedup) must still pass; re-run and confirm. ADD a focused describe block:
  - "active running tool rests as one essence line by default" — feed a single
    running tool; assert a status-pill with data-status="running" is present, the
    heavy body is NOT (`tc-editor` absent for an execute_code running tool at rest;
    `ToolArgsLivePanel` body absent), and clicking the essence row (its chevron
    button) reveals the live body.
  - "preparing tool shows the merged preparing pill (with KB when argsBytesStreamed > 0)".
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/ToolCallPanel.test.tsx src/components/chat/StatusPill.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>Active tools rest as the unified essence line with the Variant B merged live pill; the heavy body is collapsed by default and reveals on click; all prior ToolCallPanel + StatusPill tests pass; new active-row tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the three loose duplicate lines below the run card + reconcile tests</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/__tests__/components/MessageItem.sticky.test.tsx</files>
  <action>
Implement SEED-098 Change 2 + Change 3 (verification). Surgical deletions only —
this is a G-5-satisfied hot file; no refactor, no provider/wire/types changes.

(a) write_todos/workspace_write loose pointers — in the
`isMessageStreaming && message.tool_calls.length > 0` block (~415-429), DELETE the
`.filter(...).map(<SeamPointer/>)` (the loose `see panel` todo pointers). KEEP the
`hasPendingAsk(...) && <PausedRunCue/>` line — the ask_user paused cue is
load-bearing and not duplicated. The wrapping `<div className="mt-1 flex flex-col
gap-0.5">` stays (now hosting only the PausedRunCue). After removal, `SeamPointer`
becomes an unused import → drop it from the line-29 import, keeping `type SeamKind`
(still used by the reload-mode SeamCard path ~636-645). `seamKindFor`,
`requestOpenPanel`, and `SeamCard` stay used → keep them.

(b) activatedSkill loose line — DELETE the `message.activatedSkill && (<div>…Skill
activated: {message.activatedSkill}</div>)` block (~430-435). The in-card
`Loading skill "docx"` SkillRow (rendered by ToolCallPanel from `activatedSkills`)
already covers it. After removal, `Zap` becomes an unused import → drop it from the
line-2 lucide import.

(c) bottom italic echo — in the content ternary, DELETE the `: hasAnyTools ? (
<span>…{stickyBottomLabel}…</span> )` arm (~557-576) and collapse it so the chain
ends `: isStreaming && !hasAnyTools ? (thinking) : null`. The RunCard header strip
(RunStatusStrip — live verb + timer) already carries this. Then DELETE the now-orphaned
`stickyLabelRef` / `computedLabel` / `stickyBottomLabel` computation (~348-365) —
`stickyBottomLabel` was its only consumer. PRESERVE: the no-tools-yet thinking
first-call indicator (~546-556), the stopped/timed_out Square block (~585-592, this
is where "Agent reached time limit" / "Response stopped" still render), and the
active-tool-alongside-content + between-round planning indicators (~593-606). Keep
`activeTool`/`activeToolLabel`/`isMessageStreaming`/`outerBannerLabel`/`useRef` —
all still used elsewhere (verify with a grep before deleting any).

Change 3 (verification only): after (b), confirm `message.activatedSkill` is no
longer referenced anywhere in MessageItem's render path
(`grep -n "activatedSkill" frontend/src/components/chat/MessageItem.tsx` returns
nothing). Do NOT touch the field in `frontend/src/types` or StreamsProvider —
it stays for DB-loaded-message compat.

Test reconciliation — `MessageItem.sticky.test.tsx` is coupled to the deleted
`stickyBottomLabel` (its `stickyLabelText()` helper targets the italic-span-in-span
position we removed). The "Running code…" / "Synthesizing answer…" assertions
(working-run echo) are now obsolete and the terminal "Agent reached time limit" /
"Response stopped" assertions now resolve ONLY from the Square `<div>` block. Rewrite
the suite to: (i) assert NO tool-active bottom italic echo renders during a
streaming run with tools (the duplicate is gone), and (ii) assert the terminal
messages still render in the Square block (`message.runStatus === "timed_out"` →
"Agent reached time limit"; cancelled/stopped → "Response stopped"). Before editing,
run the suite on baseline to separate real breakage from the ~14-17 pre-existing
vitest rot (SEED-056) — only reconcile assertions our change actually broke.
  </action>
  <verify>
    <automated>cd frontend && grep -n "activatedSkill" src/components/chat/MessageItem.tsx | grep -v "^#" | wc -l | tr -d ' ' | grep -qx 0 && echo "activatedSkill-not-read OK" && npx vitest run src/__tests__/components/MessageItem.sticky.test.tsx src/__tests__/components/MessageItem.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <done>The three loose duplicate lines are gone; PausedRunCue + terminal/thinking indicators preserved; `message.activatedSkill` no longer read in render (field untouched in types/provider); unused `SeamPointer`/`Zap` imports dropped; sticky test rewritten to the new contract; no NEW tsc-b unused-local errors introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side render change. No new input crosses any boundary; no network, storage, auth, or backend surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-01 | Information disclosure | ToolEssenceLine active-state text (preparingDescription / summary) | accept | Renders the same `tc` data the existing live body already showed; no new field surfaced. React text children are auto-escaped (no innerHTML). |
| T-226-02 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — zero new dependencies. |
</threat_model>

<verification>
- `cd frontend && npx tsc -b 2>&1 | tail -40` — no NEW errors beyond the ~29 SEED-056 baseline (capture baseline on HEAD first if unsure; do NOT fix baseline errors).
- `cd frontend && npx vitest run src/__tests__/components/ToolCallPanel.test.tsx src/__tests__/components/MessageItem.sticky.test.tsx src/__tests__/components/MessageItem.test.tsx src/components/chat/StatusPill.test.tsx` — pass (modulo pre-existing rot, which must be distinguished from real breakage).
- Atomic commits: one per task (T1 ToolCallPanel+StatusPill, T2 MessageItem).
</verification>

<success_criteria>
- An active tool rests as the unified one-line essence shape with the Variant B merged live pill; the heavy streaming body is collapsed by default and one click behind the chevron (no show→collapse flicker).
- The three loose duplicate lines (write_todos/workspace_write pointers, `Skill activated`, bottom `Preparing code…` echo) no longer render; the ask_user PausedRunCue and terminal/thinking indicators are preserved.
- `message.activatedSkill` is no longer read in the render path; the field stays intact in types + StreamsProvider.
- TypeScript compiles with no NEW errors; affected vitest suites pass (real breakage reconciled, baseline rot left as-is).

## Final acceptance gate (ORCHESTRATOR — post-execution, not a task)
G-4 lived-experience UAT is REQUIRED before this is done: watch a real
cross-provider live run end-to-end (per SEED-098, DeepSeek + one native big-4)
via Chrome MCP / operator-driven, confirming (1) the active row never shows the
auto-expanded body / flicker, (2) clicking an active row reveals the live
streaming body, (3) the loose lines are gone, (4) todos appear only in the right
Workspace panel. Wire format + screenshots alone are insufficient.
</success_criteria>

<output>
Create `.planning/quick/260630-226-chat-tool-card-live-state-de-duplication/260630-226-SUMMARY.md` when done.
</output>
