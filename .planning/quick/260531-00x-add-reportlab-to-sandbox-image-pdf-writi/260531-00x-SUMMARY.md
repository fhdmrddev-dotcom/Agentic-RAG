---
quick_id: 260531-00x
status: complete
completed: 2026-05-31
commit: (see code commit before docs commit)
---

# Summary: Add reportlab to sandbox image (260531-00x)

## What changed
- `backend/Dockerfile.sandbox` — added `reportlab==4.2.5` to the pip-install block (PDF-writing engine; pypdf only reads).
- `CLAUDE.md` — package list now includes reportlab; recommended image tag bumped `075.1 → 075.1.1`.

## Why
A "generate a 1-page PDF report" prompt failed because the image had no PDF-writing library. reportlab is part of the Claude.ai analysis-tool set the image targets.

## Operator action REQUIRED (Docker — not done by this task)
```
docker build -f backend/Dockerfile.sandbox -t agentic-rag-sandbox:075.1.1 backend/
```
then `SANDBOX_IMAGE=agentic-rag-sandbox:075.1.1` in `backend/.env`. New chats only (sessions cached per thread_id until ~30-min idle eviction).

## Follow-up (seeded)
Deeper gap — extensible/managed sandbox package set + hardening the agent's "pip install on ModuleNotFoundError, do NOT give up" fallback (the operator report shows a provider gave up instead of installing) — captured as a new sandbox-package-management seed, paired with SEED-025 (telemetry should surface missing-package events).
