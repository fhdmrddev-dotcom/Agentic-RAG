---
id: BUG-260718-04
title: Chat model selection resets to default on navigate/refresh — not remembered per thread
reported: 2026-07-18
surface: Agentic-RAG
severity: major
status: closed
affected_areas: [frontend/chat, model-selection]
folded_into: "196"
verified_closed_by: "196"
owed_before_close: "NOTHING OWED — DISCHARGED 2026-08-18 BY SPLITTING THE REPORT, not by waiving anything. This report carried a TWO-HALF close condition ('restores its last-used model across navigate AND refresh'). ✅ THE NAVIGATE HALF IS SHIPPED AND VERIFIED LIVE: G-4 row U-C1 was driven with Chrome DevTools MCP on 2026-08-18 and thread 'Weekly Report Generation' showed the composer at Ollama / qwen3-30b-a3b-instruct-2507@q4_k_xl, BYTE-MATCHING that thread's most recent `runs` row — plan 196-07's derivation doing exactly what it claims, on top of 17 hook cases and 2 integration cases. ⚠ THE REFRESH HALF WAS MEASURED UNREACHABLE AND WAS NEVER A MODEL-SELECTION DEFECT: on F5 the THREAD ITSELF does not survive (no per-thread URL — location stays bare `/`; localStorage holds only `chat_history_collapsed`; sessionStorage empty), so the restore never runs and showing the global default in a BRAND-NEW thread is CORRECT. That half is a navigation / session-restore capability with a different root cause and a different owner file, and it now lives in SEED-178 with a mechanical discharge check. Closing on the navigate half rather than parking a verified, shipped fix behind an unrelated missing capability indefinitely. ⚠ ORIGINAL FIELD PRESERVED VERBATIM IN GIT HISTORY AND IN THE BODY BELOW; it is superseded, not deleted."
related_seeds: [SEED-135, SEED-178]
re_open_trigger: "CLOSED 2026-08-18 ON THE NAVIGATE HALF; the refresh half moved to SEED-178. RE-OPEN THIS REPORT only if the MODEL restore itself misbehaves — i.e. a thread resolves to a model that is no longer enabled in the registry (resolveRestoreTarget's disabled refusal is implemented but was never observed live), or a thread whose last message PREDATES the feature is left with no sensible default (option (a)'s known blind spot). ⚠ Do NOT re-open this report for the refresh case — that is SEED-178 and is not a model-selection defect. ⚠ CLOSE CONDITION MEASURED UNREACHABLE AS WRITTEN — U-C1 DRIVEN 2026-08-18 (Chrome MCP, live app): restore-on-NAVIGATE VERIFIED (thread 'Weekly Report Generation' showed Ollama / qwen3-30b-a3b-instruct-2507@q4_k_xl, byte-matching its last `runs` row), but on REFRESH THE THREAD ITSELF DOES NOT SURVIVE — no per-thread URL (location stays bare `/`), nothing in localStorage beyond `chat_history_collapsed`, nothing in sessionStorage; the app returns to a NEW chat where the global default is CORRECT. So the restore is never given the chance to run, and 'REFRESH and see the model still selected' CANNOT PASS until THREAD-SELECTION PERSISTENCE exists — a capability Phase 196 never scoped and could not have fixed. ⚠ THIS REPORT NEEDS ITS CLOSE CONDITION RESTATED OR SPLIT; do not simply re-drive U-C1. STILL FOLDED at Phase 196's close (2026-08-18) — see `owed_before_close` above for the
  one artifact that stands between this and `closed`. ⚠ The report's OWN named blind spots are also
  still live and were NOT designed out: RE-OPEN if the restore reads a model that is no longer enabled
  (option (a)'s registry-staleness arm — mitigated by resolveRestoreTarget's disabled refusal but not
  observed live), or if a thread whose last message predates the feature is left with no sensible
  default. ⚠ ORIGINAL FOLDING NOTE PRESERVED VERBATIM BELOW.
  >>>> ORIGINAL (2026-08-17), verbatim: FOLDED at /gsd:discuss-phase 196 (2026-08-17), on an EXPLICIT operator decision that
  deliberately widens that phase's canvas-only scope fence — recorded as 196-CONTEXT.md D-18. Direction
  chosen: THIS REPORT'S OWN OPTION (a) — derive the thread's model from its most recent message (each
  message already persists the model it used, ChatArea.tsx:320), so NO new storage, no schema change,
  no migration. Option (b) (explicit per-thread persistence) was considered and NOT taken: it adds
  storage for something already derivable. The fallback clause this report itself names — 'fall back to
  the global default only for brand-new threads or if the stored model is no longer enabled in the
  registry' — is honoured by reusing 196's D-07 disabled-model rule rather than inventing a second one.
  Flip to `closed` only when a thread demonstrably restores its last-used model across navigate AND
  refresh. RE-OPEN if the restore reads a model that is no longer enabled, or if a thread whose last
  message predates the feature is left with no sensible default (the known blind spot of option (a))."
reproduces_on:
  branch: develop
  commit: 14056e4b
  date: 2026-07-18
---

# BUG-260718-04: Chat model selection resets to default on navigate/refresh — not remembered per thread

## RESOLUTION 2026-08-18 — CLOSED by SPLIT, on live evidence

This report was closed by **splitting its two-half close condition**, not by waiving either half.

| Half | Verdict |
|---|---|
| **navigate** — restore the last-used model when returning to a thread | ✅ **SHIPPED AND VERIFIED LIVE** |
| **refresh** — the same across F5 | ➜ **MOVED TO `SEED-178`** — measured unreachable, and never a model-selection defect |

**The live evidence for the navigate half** (G-4 row U-C1, driven with Chrome DevTools MCP,
2026-08-18, against `localhost:5173` + backend `:8000`): opening thread *Weekly Report Generation*
showed the composer at **`Ollama` / `qwen3-30b-a3b-instruct-2507@q4_k_xl`**, **byte-matching that
thread's most recent `runs` row**. That is option (a) — this report's own proposal — working end to end.

**Why the refresh half moved out.** On F5 **the thread itself does not survive**: no per-thread URL
(`location.href` stays bare `/` with a thread open), `localStorage` holds only
`chat_history_collapsed`, `sessionStorage` is empty. Re-checked at 3.5 s and 9.5 s. So the restore is
never given the chance to run, and showing the global default in a **brand-new** thread is *correct*.
⚠ **That is "the app forgets which conversation I was in" — a navigation/session-restore concern with a
different root cause and a different owner file.** It is now `SEED-178`, with a mechanical discharge
check. Keeping this report open against a condition it **structurally cannot meet** would have parked a
verified, shipped fix behind an unrelated missing capability indefinitely.

⚠ **The original section below is PRESERVED VERBATIM and is now SUPERSEDED** — it was written before
U-C1 was driven, when the refresh half was believed merely *undriven* rather than *unreachable*.

---

## Status at Phase 196's close (2026-08-18) — HALF DISCHARGED, deliberately still `folded` — ⚠ SUPERSEDED, see RESOLUTION above

**What plan `196-07` shipped:** the restore, via option (a) exactly as this report proposed — derived
from the thread's own run-backed message history, **no new storage, no schema change, no migration**.
It lives in `frontend/src/hooks/useComposerModel.ts` (17 cases) with 2 integration cases on the
composer control itself, and `ChatArea.tsx` came out of the change **five `useState` and one
`useEffect` lighter** than it went in.

| The close condition's two halves | Status |
|---|---|
| restores across **navigate** | ✅ covered — the per-thread re-restore case |
| restores across **refresh** | ⛔ **NOT PROVEN, and not provable from a unit suite** |

⚠ **jsdom has no page reload.** A hook test exercises a **derivation**; it never exercises a
**lifecycle**. Both suites prove *which model a thread resolves to* — they do not prove *that it
survives F5*. **That claim is G-4 UAT row `U-C1`, ratified and not yet driven.**

**So the status stays `folded`.** This project has a recorded lesson that a bug's `status:` frontmatter
**IS** the routing index — prose inside a `folded` record is invisible to the cross-check scan, and
that once hid a live bug for two months. The owed row is therefore recorded in the **frontmatter**
(`owed_before_close`), not only here.

**Two things worth knowing about the shipped restore, because both are non-obvious:**

1. ⚠ **The restore is a SEED with a CLOSING WINDOW, and the window closes on a DELIBERATE CHOICE as
   well as on a restore.** *"Restore once per thread"* alone is not sufficient: a thread with nothing
   to restore from never marks itself settled (correctly — messages arrive asynchronously). The
   operator picks a model and sends; the reply lands carrying a different one; the effect re-evaluates,
   now finds a target, and **overwrites the pick the operator just made** — a fresh instance of the
   exact defect class this report is about. Both `setSelectedModel` and `handleProviderChange`
   therefore close the window.
2. **A disabled model is refused as a RESTORE TARGET, reusing D-07's rule by name** — via the new
   `disabled_models` field on `GET /settings/providers` — and separately refused if no provider offers
   it, so the composer cannot render a selection its `<select>` has no option for. ⚠ **The composer's
   OFFERED list is deliberately unfiltered by this work**; that belongs to the app-wide model-registry
   sweep, not here.

---

## What we observed

Operator picks a specific model for a chat. After the chat finishes, OR after navigating away, OR after refreshing the page and returning to the same thread, the model picker has **reverted to the default model** — it does not remember the model that thread was using. Desired: a thread remembers its **last-used model** (the operator may switch models mid-thread; keep the latest one used in that thread).

## Why it matters

Real workflow friction on a core control: the operator deliberately chooses a model per conversation, but the app silently reverts to the default, so a follow-up message in an existing thread can go to the wrong model unless they re-select every time. Higher than cosmetic — it affects which model actually answers. Severity: major.

## Hypothesized cause (read-only check)

`selectedModel` is ephemeral component state seeded from the GLOBAL default, never restored per-thread:
- `frontend/src/components/chat/ChatArea.tsx:63` — `const [selectedModel, setSelectedModel] = useState<string>("")`.
- `ChatArea.tsx:154-156` — on load it seeds `preferred = active_model ?? activeProvider.models[0]` (the global default), and `:201` resets to `p.models[0]` on provider change.
- `ChatArea.tsx:320` — the model IS sent per message (`sendMessage(..., selectedModel || undefined, ...)`), so each stored message already carries the model it used.
- On thread switch / remount / refresh, `selectedModel` re-initializes to the default; there is no per-thread restore.

So the data to restore from likely already exists (each message stores its model). The gap is purely the restore-on-load.

## Surface classification

`Agentic-RAG` — chat model control. Routing candidate.

## Suggested routing

- **Fold into in-flight phase:** n/a (deferred)
- **Defer to future phase / milestone:** post-v3.3. Options: (a) derive the thread's last-used model from its most recent assistant/user message's model on load and seed `selectedModel` from it (no new storage — the model is already persisted per message); (b) persist the per-thread selection explicitly (thread metadata field or localStorage keyed by thread_id); fall back to the global default only for brand-new threads or if the stored model is no longer enabled in the registry. Prefer (a) if the message model is reliably available client-side.
- **Plant as seed:** candidate if not scheduled next milestone.
- **External — note only:** no

## Workarounds

Re-select the model each time you return to a thread.

## Reference / evidence links

- Read-only check 2026-07-18. `ChatArea.tsx` (model state) + `MessageInput.tsx` (picker). Registry-enabled-model check ties into Phase 149/159 (a remembered model that was later disabled must fall back gracefully).
