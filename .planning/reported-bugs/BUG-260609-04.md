---
id: BUG-260609-04
title: Phase card shows the placeholder slug "phase-0" instead of the real phase slug (reconcile-floor clobber)
reported: 2026-06-10
surface: Agentic-RAG
severity: minor
status: closed
affected_areas: [frontend/panel, harness/run-honesty]
folded_into: "188"
verified_closed_by: "188"
related_seeds: []
re_open_trigger: "CLOSED 2026-08-05 by live observation, the condition this report itself set. Launched readonly_refusal_098uat (the exact fixture the defect was photographed on) and read the workspace panel: `Phase 1 of 1, readonly_probe, complete` / `readonly_probe - done` / `Complete`. A regex sweep for /phase-\d+/g over the whole document returned [] both live and after re-opening the run, and the DB cross-check at the same moment read [(0, 'readonly_probe', 'completed')]. The screen agrees with the database. Evidence: 188-UAT.md Row 17 + uat-row17-run-surface.png. Re-open if any positional placeholder is seen again on a live run. PRIOR: FOLDED at /gsd:discuss-phase 188 (2026-08-05) — D-188-22. Two independent closures: (a) the canvas is immune BY CONSTRUCTION because Phase 188 joins live phase rows to definition nodes by phase_index, never by slug (D-188-01), so a placeholder slug can never mislabel a node; (b) the panel is fixed at its ROOT — reconcilePhases' LIVE branch overlays the real slugs from wf.phases (the backend already returns them for the ACTIVE anchor, threads.py resolves phases_source_run_id = active_workflow_run_id first, so real slugs ARE available mid-run) onto the positional skeleton by phase_index, keeping the total_phases floor for rows the harness has not inserted yet. This is fix option 1 below, and Phase 188 is already editing reconcilePhases for the `?? \"done\"` fail-open fix. Flip to closed only when the phase-0 render is verified gone on a LIVE single-phase run. UPDATED 2026-08-05 — THE CODE-LEVEL CLOSURE HAS LANDED, in plan 188-04: reconcilePhases' LIVE branch now builds a Map<phase_index, WorkflowPhaseState> from wf.phases and overlays IDENTITY ONLY (slug + phase_type) onto the positional skeleton, joined by phase_index and never by slug — the value that can BE a placeholder cannot also be the key that repairs it. That is fix option 1 below, as folded. Three corrections to the folded note: (a) the overlay is COMPLETE, not partial — RESEARCH Open Question 4 measured that create_workflow_run (backend/app/db/workflows.py:206-214) inserts EVERY workflow_phases row at run creation in one transaction, and is the only INSERT INTO workflow_phases in backend/app, so the hedge about keeping the floor 'for rows the harness has not inserted yet' describes a case that cannot occur; total_phases survives as defence in depth only. (b) A SECOND, PREVIOUSLY UNRECORDED HALF of the same bug is closed by the same edit — the live branch also flattened phaseType to unknown on every row (the value that drives the step's icon and its type sentence); the report had only ever named the slug. (c) STATUS is deliberately NOT overlaid: the positional derivation is a forward-only floor and is often MORE advanced than the DB rows (a row flips only at complete_phase), so overlaying it would drag PhaseTimeline.tsx:115-127's shipped counter BACKWARD. The ternary is byte-unchanged (it appears on no +/- line of the diff) and is fenced by a floor guard in panel/__tests__/PhaseReconcile.test.tsx. The defect was observed RED before the fix — received 'phase-0' where the server had sent 'collect-inputs', 'phase-2' where it had sent 'check-the-numbers', and 'unknown' for all four phaseTypes; raw vitest output in 188-04-SUMMARY.md. The canvas half remains closed BY CONSTRUCTION (D-188-01). REMAINING CONDITION FOR closed, UNCHANGED: the LIVE single-phase run in 188-UAT.md must be observed rendering the real step name with no phase-N anywhere on screen — the operator gate (CORRECTION 2026-08-05: this clause said 'Plan 12's operator gate'; the operator gate is plan **188-13**, not 188-12, which measured the phase gates and pinned the count gate). Do NOT flip on the unit tests alone: they prove the reducer, not the render. GATE OPENED 2026-08-05, PLAN 188-13 — STATUS DELIBERATELY NOT FLIPPED, AND THE OBSERVATION IS STILL OWED. The gate ran with the operator away, so row 17 of 188-UAT.md ('A live single-phase run shows the real step name, never phase-0') was NOT driven and remains [pending]. Nothing about the code changed at this gate; only the record did. This report therefore stays `folded` with `verified_closed_by: null`, exactly as this trigger requires — flipping it on the strength of 188-04's reducer tests is the one move the clause above forbids. THE ROW TO RUN, PRECISELY: launch the published single-phase workflow **`readonly_refusal_098uat`** (1 phase, DB slug `readonly_probe`) — measured present and published on 2026-08-05 — and watch the developer workspace panel WHILE IT IS LIVE, not after it finishes. This is the exact fixture the defect was originally photographed on ('Read-only refusal (098 UAT)', screenshots of 2026-06-10 showing 'PHASE 1 · Step phase-0' against DB slug 'readonly_probe'), so a pass on it is evidence about the reported case and not about a neighbouring one. PASS = the real step name renders and `phase-0` (or any `phase-N` placeholder) appears NOWHERE on screen, live and after a mid-run refresh. FAIL = any positional placeholder visible at any moment. Three other published single-phase fixtures can corroborate: `multitool_scope_098uat` (slug `analyze`), `skill_compose_099uat` (`assess`), `fresh-pause-102uat-20a89243` (`brief`). PRIOR: Reviewed at /gsd:discuss-phase 183 (2026-07-25) — left OPEN, NOT folded: 183 projects a STATIC WorkflowDefinition and reads phase.slug directly from it, so the reconcile-floor clobber cannot manifest on the canvas. Re-check at Phase 188 (Non-Technical Run Observability), where CanvasRunView reads the SAME live usePhases(threadId) slice the buggy workspace-panel phase card reads — that is the phase that can actually close this. Prior review at /gsd:discuss-phase 124 (2026-06-26) also left it open: live-run reconcile-floor slug clobber, not the static glyph-dot phase spine Phase 124 builds."
reproduces_on:
  branch: v2.5-dev
  commit: 9c60f718
  date: 2026-06-10
---

# BUG-260609-04: Phase card shows placeholder slug "phase-0" instead of the real slug

## What we observed

Running the Test-6 fixture "Read-only refusal (098 UAT)" (gpt-5.4-mini), the workspace WORKFLOW panel rendered the phase as **"PHASE 1 · Step phase-0"** — both while Running and when Complete. The DB has the REAL slug **"readonly_probe"** for that phase (`workflow_phases.slug`). So the UI showed the positional placeholder, not the real phase name. This is the "why do we show Phase 0?" the operator flagged earlier (Test A / Doc Q&A discussion). Evidence: `screenshots/Screenshot 2026-06-10 010423.png` (running, "phase-0") + `Screenshot 2026-06-10 010515.png` (complete, "phase-0"). DB: `workflow_phases` for run 852b1c66 = `[(0, 'readonly_probe', 'completed')]`.

## Why it matters

Minor run-honesty/labeling defect: the phase node shows a synthetic positional name (`phase-0`) instead of the authored step slug, so the operator can't tell which step is which. Cosmetic (no scope/correctness impact) but confusing on every workflow where it triggers.

## Hypothesized cause (high confidence; same reconcile-floor family as 01/02/03)

`reconcilePhases` (StreamsProvider.tsx ~2587) seeds a SKELETON whose non-current rows carry positional placeholder slugs `phase-${i}`, and the current row uses `current_phase_slug ?? 'phase-${i}'`. `replacePhasesForThread` OVERWRITES `phasesByThread` with that skeleton. When the reconcile fires AFTER the live `phase_started` (which had already appended the REAL slug) — e.g. a fresh kickoff in a NEW thread triggers a `[threadId]`-change reconcile — the placeholder skeleton CLOBBERS the real-slug row. If `current_phase_slug` is also null at that fetch instant (current_phase_id not yet set), even the current row seeds `phase-0`. The `appendPhaseForThread` placeholder-replacement (slug `phase-N` → real slug on a later `phase_started`) only fires if another `phase_started` arrives after the clobber; for a single-phase run none does, so `phase-0` sticks. (This is also the slug-mismatch path that makes a missed `phase_completed` strand a phase — BUG-260609-01 — though that symptom is now covered by the index-based sweep.)

## Surface classification

`Agentic-RAG` — harness/workflow panel render. Routing candidate.

## Suggested routing / fix options

- Sibling to BUG-260609-01/02/03 (098-UAT harness-UI run-honesty cluster). Fix options (additive, frontend):
  1. In `reconcilePhases`, prefer the real per-phase slug array (`wf.phases`, already added in the BUG-260609-01 fix-B) for the LIVE skeleton too — not just terminal runs — so seeded slugs are real, never `phase-N`.
  2. Make `replacePhasesForThread`/the reconcile merge NOT clobber a row whose slug is already real with a `phase-N` placeholder (preserve the live real slug).

## Gate status — 2026-08-05, plan 188-13 (the operator gate)

**Still `folded`. NOT closed. The closing observation has not been made.**

| | |
|---|---|
| Code-level closure | ✅ landed in 188-04 — `reconcilePhases`' live branch overlays identity (slug + `phase_type`) from `wf.phases` by `phase_index`, never by slug |
| Reducer-level falsification | ✅ observed RED before the fix, then green — `panel/__tests__/PhaseReconcile.test.tsx`, raw output in `188-04-SUMMARY.md` |
| Canvas half | ✅ closed BY CONSTRUCTION (D-188-01 — the canvas joins by `phase_index`) |
| **The live render observation** | ⬜ **OWED** — row 17 of `188-UAT.md`, `[pending]`, not driven |

The gate opened with the operator away. **Unit tests were deliberately not accepted as
the closing evidence**, because this report's own `re_open_trigger` says in as many words
that they prove the reducer and not the render — and a phase whose entire subject is a
surface that must not claim what did not happen cannot close its own bug on an inference.

**To close it:** run row 17 on **`readonly_refusal_098uat`** — the fixture this defect was
originally photographed on — watching the workspace panel *live*, then refreshing mid-run.
The real step name must render with no `phase-N` anywhere on screen at any moment. On a
pass, set `status: closed` and `verified_closed_by` to the observing plan or session.

## Reference / evidence links

- `screenshots/Screenshot 2026-06-10 010423.png`, `Screenshot 2026-06-10 010515.png`
- DB: workflow_phases run 852b1c66 → slug 'readonly_probe' (UI showed 'phase-0')
- `frontend/src/providers/StreamsProvider.tsx` reconcilePhases skeleton (placeholder slugs) + appendPhaseForThread placeholder-replacement
