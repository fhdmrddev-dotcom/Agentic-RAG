---
phase: 063
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/integration/test_063_post_contract.py
  - backend/tests/integration/test_063_post_then_subscribe.py
  - backend/tests/integration/test_063_legacy_path_deleted.py
  - e2e/tests/063-refresh-mid-stream.spec.ts
  - e2e/tests/063-resume-failed.spec.ts
  - .planning/phases/063-frontend-stream-decoupling/063-LEGACY-TEST-AUDIT.md
autonomous: true
requirements:
  - STREAM-04
  - STREAM-02b
tags:
  - frontend
  - sse
  - testing
  - tdd
  - phase-063
must_haves:
  truths:
    - "Five Wave-0 stub test files exist and import the new D-063-01 contract (POST returns JSON {message_id, run_id})"
    - "Stub tests RED-fail with a recognizable contract error (NOT collection error) — proving they bind to real symbols"
    - "Legacy POST-SSE test audit document lists every test_058_*/test_059_*/test_061_*.py file that POSTs to /threads/{tid}/messages and reads SSE off the response, with disposition (rewrite | delete | inherit existing exclusion)"
    - "Audit document is committed to .planning/ so Plan 05 has a checkable input"
  artifacts:
    - path: "backend/tests/integration/test_063_post_contract.py"
      provides: "RED stub asserting POST returns 201 + JSON content-type + {message_id, run_id} body"
      min_lines: 30
    - path: "backend/tests/integration/test_063_post_then_subscribe.py"
      provides: "RED stub asserting POST→GET-stream roundtrip drains terminal-typed events"
      min_lines: 40
    - path: "backend/tests/integration/test_063_legacy_path_deleted.py"
      provides: "RED stub asserting event_consumer is not importable AND send_message source contains JSONResponse not EventSourceResponse"
      min_lines: 25
    - path: "e2e/tests/063-refresh-mid-stream.spec.ts"
      provides: "RED Playwright stub for F5-mid-stream → active-runs reattach"
      min_lines: 30
    - path: "e2e/tests/063-resume-failed.spec.ts"
      provides: "RED Playwright stub for failed-run Resume button visibility"
      min_lines: 30
    - path: ".planning/phases/063-frontend-stream-decoupling/063-LEGACY-TEST-AUDIT.md"
      provides: "Catalog of every legacy POST-SSE test with rewrite/delete disposition for Plan 05"
      min_lines: 40
  key_links:
    - from: "backend/tests/integration/test_063_legacy_path_deleted.py"
      to: "app.api.threads"
      via: "import inspection + getsource"
      pattern: "hasattr|getsource"
    - from: ".planning/phases/063-frontend-stream-decoupling/063-LEGACY-TEST-AUDIT.md"
      to: "backend/tests/integration/test_058_*.py / test_059_*.py / test_061_*.py"
      via: "grep listing each match"
      pattern: "EventSourceResponse|streamMessage|POST.*messages"
---

<objective>
Land the five Wave-0 stub test files (3 backend + 2 e2e) AND the legacy-test audit document so Plans 02–05 have a binding contract surface and a known list of legacy tests to deal with in Plan 05.

Purpose: Per `<deep_work_rules>`, every code-producing task in later plans MUST have an `<automated>` verify command that exists at task start. This plan creates those test files as RED stubs (per Nyquist Rule) AND inventories legacy POST-SSE tests so Plan 05's rewrites/deletions are not invented mid-execution.

Output: Five test files (RED-failing for the right reason — contract mismatch, NOT import/collection error) and one audit markdown document.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Vibe Apps/Agentic RAG/CLAUDE.md
@C:/Vibe Apps/Agentic RAG/.planning/PROJECT.md
@C:/Vibe Apps/Agentic RAG/.planning/ROADMAP.md
@C:/Vibe Apps/Agentic RAG/.planning/STATE.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/063-frontend-stream-decoupling/063-CONTEXT.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/063-frontend-stream-decoupling/063-PATTERNS.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/063-frontend-stream-decoupling/063-VALIDATION.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/061-run-backed-streaming-backend/061-CONTEXT.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/062-replay-tail-api/062-CONTEXT.md

<interfaces>
<!-- Backend symbols Wave 0 stubs must reference verbatim. Extracted from threads.py (read 2026-05-03). -->

From backend/app/api/threads.py (existing — Wave 0 binds to these symbol names):
- `event_consumer` (module-level async generator at threads.py:331-426) — Plan 02 deletes this
- `send_message` (POST handler at threads.py:727-2244) — Plan 02 changes the return shape
- `RUN_TASKS: dict[uuid.UUID, asyncio.Task]` (threads.py:78)
- `TERMINAL_TYPES = frozenset({"done", "error", "cancelled"})` (threads.py:82)

From backend/app/api/runs.py (Phase 062 — already shipped, Wave 0 binds against these endpoints):
- `GET /runs/{run_id}/stream?since={offset}` (replay-tail consumer)
- `DELETE /runs/{run_id}` (cancel verb)
- `GET /threads/{thread_id}/active-runs` (lives in threads.py:449-490)

From backend/tests/integration/_run_helpers.py:
- `_build_mock_supabase()`, `_make_result()`, `USER_ID`

From backend/tests/integration/test_062_stream_replay.py:36-51:
- `_reset_redis_singleton` autouse fixture (copy verbatim into new files that touch real Redis)

From backend/tests/integration/test_059_disconnect.py:
- `_reset_sse_starlette_app_status` (import via `# noqa: F401, E402`)

New contract under test (D-063-01):
```python
# POST /threads/{thread_id}/messages
# Returns: HTTP 201 application/json
# Body: {"message_id": "<user_message_uuid>", "run_id": "<run_uuid>"}
# (NOT EventSourceResponse, NOT text/event-stream)
```

E2E auth helpers (from e2e/tests/060-thread-race.spec.ts):
- `signIn(page)`, `TEST_EMAIL`, `TEST_PASSWORD`, `LONG_STREAM_PROMPT`, `sendMessageInActiveThread(page, text)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three backend Wave-0 test stubs (RED for the right reason)</name>
  <files>backend/tests/integration/test_063_post_contract.py, backend/tests/integration/test_063_post_then_subscribe.py, backend/tests/integration/test_063_legacy_path_deleted.py</files>
  <read_first>
    - C:/Vibe Apps/Agentic RAG/backend/tests/integration/test_062_active_runs.py — mock-supabase pattern + httpx.AsyncClient + `_reset_sse_starlette_app_status` import (063-PATTERNS.md "test_063_post_contract.py" section)
    - C:/Vibe Apps/Agentic RAG/backend/tests/integration/test_062_stream_replay.py — `_reset_redis_singleton` fixture lines 36-51 + producer-drive-via-POST + GET stream pattern
    - C:/Vibe Apps/Agentic RAG/backend/tests/integration/_run_helpers.py — `_build_mock_supabase`, `_make_result`, `USER_ID`
    - C:/Vibe Apps/Agentic RAG/.planning/phases/063-frontend-stream-decoupling/063-VALIDATION.md "Wave 0 Requirements" — exact filenames + intent
    - C:/Vibe Apps/Agentic RAG/.planning/phases/063-frontend-stream-decoupling/063-PATTERNS.md "test_063_*" three sections — full code skeletons to copy
  </read_first>
  <action>
Create three RED-stub test files implementing the D-063-01 contract per 063-PATTERNS.md "test_063_*" sections (verbatim code blocks):

(1) `backend/tests/integration/test_063_post_contract.py` — mock-supabase only (no Redis fixture). Contains exactly TWO test functions:
   - `test_post_returns_message_and_run_ids()` — asserts `resp.status_code == 201`, content-type starts with `application/json`, body has both `message_id` and `run_id` keys (UUID strings). Use the mock pattern from `test_062_active_runs.py:13-66` (per 063-PATTERNS.md). Includes anti-false-RED guard: `assert resp.headers.get("content-type", "").startswith("application/json"), f"Expected JSON content-type, got {resp.headers.get('content-type')!r}"`.
   - `test_post_returns_before_producer_first_xadd()` — RED-stub asserting that the response returns BEFORE the producer's first XADD lands in Redis. Use `test_062_stream_replay.py` Redis fixture pattern. Stub may simply assert `resp.status_code == 201` plus a TODO comment for the timing guard so it RED-fails because POST currently returns SSE, not JSON. Plan 02 fills the timing assertion.
   Imports MUST include the line `from tests.integration.test_059_disconnect import _reset_sse_starlette_app_status  # noqa: F401, E402` per 063-PATTERNS.md "Shared Patterns" section. Use `@pytest.mark.asyncio` and `@pytest.mark.timeout(15)` on every test.

(2) `backend/tests/integration/test_063_post_then_subscribe.py` — uses real Redis (`redis_client` fixture from 061 conftest) + the `_reset_redis_singleton` autouse fixture copied verbatim from `test_062_stream_replay.py:36-51` (single fixture per file — D-062-14 / Phase 062 Plan 02 deviation Rule 3). Exactly ONE test function `test_post_then_get_stream_renders_full_response(redis_client)`: per 063-PATTERNS.md "test_063_post_then_subscribe.py" section. Steps: (a) `resp = await ac.post("/threads/{THREAD_A}/messages", json={...})`; (b) `assert resp.status_code == 201`; (c) `run_id = resp.json()["run_id"]`; (d) `async with ac.stream("GET", f"/runs/{run_id}/stream?since=0", ...) as stream_resp:` — drain `aiter_lines()` collecting events with `data: ` prefix until any event's `type` is in TERMINAL_TYPES; (e) assert `any(e.get("type") == "delta" for e in events)` AND `events[-1].get("type") in TERMINAL_TYPES`. Patch `app.api.threads.create_adaptive_streaming_chat`, `app.services.suggestion_service.generate_suggestions`, `app.api.threads.generate_thread_title` exactly as 062-PATTERNS.md shows.

(3) `backend/tests/integration/test_063_legacy_path_deleted.py` — pure Python static-check, no fixtures. TWO test functions per 063-PATTERNS.md:
   - `test_event_consumer_not_importable()` — `from app.api import threads as threads_module; assert not hasattr(threads_module, "event_consumer"), "event_consumer is still defined in app.api.threads — D-063-01 hard cutover not complete"`
   - `test_post_does_not_return_eventsourceresponse()` — `import inspect; from app.api.threads import send_message; src = inspect.getsource(send_message); assert "EventSourceResponse" not in src, "send_message still references EventSourceResponse — D-063-01 not complete"; assert "JSONResponse" in src or "201" in src, "send_message must return JSONResponse with status 201"`

All three files must RED-fail when executed against current master (because event_consumer DOES exist, send_message DOES return EventSourceResponse, POST DOES return SSE not JSON). The RED reason must be the contract mismatch, NOT a collection error or import error.

Per `<scope_reduction_prohibition>`: Do NOT skip any of the assertions above. Do NOT mark tests xfail. Do NOT add `pytest.skip(...)`. The tests MUST be RED at this commit and become GREEN only after Plan 02.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -m pytest tests/integration/test_063_post_contract.py tests/integration/test_063_post_then_subscribe.py tests/integration/test_063_legacy_path_deleted.py --collect-only -q 2>&amp;1 | grep -v '^#' | grep -E '5 tests collected|test_063_'</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist at the paths in `<files>`
    - `pytest --collect-only -q tests/integration/test_063_post_contract.py tests/integration/test_063_post_then_subscribe.py tests/integration/test_063_legacy_path_deleted.py` reports exactly 5 collected tests with no collection errors (`grep -cE '^tests/integration/test_063_.*::test_' <output>` is 5)
    - Running the suite (`pytest -x tests/integration/test_063_*.py`) RED-fails with assertion errors (NOT ImportError, NOT ModuleNotFoundError, NOT SyntaxError) — `grep -cE 'AssertionError|ContractError' <pytest_output>` is &gt;= 1
    - `grep -c 'pytest.skip\|pytest.xfail\|@pytest.mark.skip\|@pytest.mark.xfail' tests/integration/test_063_post_contract.py tests/integration/test_063_post_then_subscribe.py tests/integration/test_063_legacy_path_deleted.py` is exactly 0
    - `grep -cE 'def test_post_returns_message_and_run_ids|def test_post_returns_before_producer_first_xadd' tests/integration/test_063_post_contract.py` is 2
    - `grep -cE 'def test_post_then_get_stream_renders_full_response' tests/integration/test_063_post_then_subscribe.py` is 1
    - `grep -cE 'def test_event_consumer_not_importable|def test_post_does_not_return_eventsourceresponse' tests/integration/test_063_legacy_path_deleted.py` is 2
    - `grep -c '_reset_sse_starlette_app_status' tests/integration/test_063_post_contract.py tests/integration/test_063_post_then_subscribe.py` is 2 (one per file)
    - `grep -c '_reset_redis_singleton' tests/integration/test_063_post_then_subscribe.py` is &gt;= 2 (fixture defined + autouse decorator)
  </acceptance_criteria>
  <done>Three backend test files committed with the exact functions named above, RED-failing for contract reasons, no skips/xfails, anti-false-RED content-type guard present in test_post_contract.</done>
</task>

<task type="auto">
  <name>Task 2: Create two e2e Wave-0 Playwright stubs + legacy-test audit document</name>
  <files>e2e/tests/063-refresh-mid-stream.spec.ts, e2e/tests/063-resume-failed.spec.ts, .planning/phases/063-frontend-stream-decoupling/063-LEGACY-TEST-AUDIT.md</files>
  <read_first>
    - C:/Vibe Apps/Agentic RAG/e2e/tests/060-thread-race.spec.ts — auth helper, LONG_STREAM_PROMPT, sendMessageInActiveThread, request listener pattern (063-PATTERNS.md "e2e/tests/063-*" section)
    - C:/Vibe Apps/Agentic RAG/.planning/phases/063-frontend-stream-decoupling/063-RESEARCH.md "Phase Requirements → Test Map" lines 928-944
    - C:/Vibe Apps/Agentic RAG/.planning/phases/063-frontend-stream-decoupling/063-RESEARCH.md "Open Questions" #4 (audit task scope) and "Runtime State Inventory" row "Backend integration test files referencing old POST-stream contract"
    - C:/Vibe Apps/Agentic RAG/.planning/phases/061.1-run-backed-streaming-cleanup/deferred-items.md (DEF-061.1-01/02 — pre-existing exclusions to inherit)
  </read_first>
  <action>
Create two e2e Playwright stubs and one audit document.

(1) `e2e/tests/063-refresh-mid-stream.spec.ts` — Playwright stub per 063-PATTERNS.md "e2e/tests/063-refresh-mid-stream.spec.ts" section. Imports `signIn`, `TEST_EMAIL`, `TEST_PASSWORD` (or whatever the existing 060 spec exports) from a shared helper or copy verbatim. Single test `test("F5 mid-stream: assistant message reattaches via active-runs", async ({ page }) =&gt; { ... })`:
   - signIn(page); start a new chat; `sendMessageInActiveThread(page, LONG_STREAM_PROMPT)`
   - register `page.on("request")` listener that pushes any `GET /threads/.+/active-runs` and `GET /runs/.+/stream` matches into a `streamActivity` array
   - `await page.waitForTimeout(3_000)`; `await page.reload()`
   - `await page.waitForRequest((req) =&gt; req.method() === "GET" &amp;&amp; /\/threads\/[^/]+\/active-runs$/.test(req.url()), { timeout: 10_000 })`
   - assert `streamActivity.some(a =&gt; /\/runs\/[^/]+\/stream/.test(a.url))` AND assistant bubble eventually has &gt;0 character growth post-reload (read DOM text)

(2) `e2e/tests/063-resume-failed.spec.ts` — Playwright stub per 063-PATTERNS.md "e2e/tests/063-resume-failed.spec.ts" line. Imports same auth helpers. Single test `test("Resume button visible only on failed runs", async ({ page }) =&gt; { ... })`:
   - signIn; create/select a thread known to have a failed run (mock or fixture per Phase 062 redis_down test pattern; for Wave 0, leave a `// TODO: inject failed run via fixture in Plan 04` comment and assert presence of a button matching `getByRole("button", { name: /resume/i })` is `toHaveCount(0)` against current master — RED because the button doesn't exist yet)

Both specs MUST RED-fail when executed against current master:
- 063-refresh-mid-stream: fails because POST currently streams SSE on the same response (no separate `/runs/{rid}/stream` GET happens)
- 063-resume-failed: fails because Resume button is not in the DOM (Plan 04 adds it)

Per `<scope_reduction_prohibition>`: Do NOT use `test.skip` or `test.fixme`. RED-failing is the correct state at this commit.

(3) `.planning/phases/063-frontend-stream-decoupling/063-LEGACY-TEST-AUDIT.md` — Markdown document with the following structure:

```
# Phase 063 — Legacy POST-SSE Test Audit

**Audit run:** {today's date}
**Audit commands:**
- `cd backend &amp;&amp; grep -rln 'EventSourceResponse\|/threads/[^/]*/messages.*ac.stream\|client.stream.*messages' tests/integration/`
- `cd backend &amp;&amp; grep -rln 'streamMessage' tests/`

## Inherited Exclusions (from 062-VERIFICATION.md `inherited_exclusions`)

| Test | Source | Disposition |
|------|--------|-------------|
| `test_normal_stream_unchanged` | DEF-061.1-01 | Inherit exclusion (out of 063 scope) |
| `test_failed_run_expires_60s` | DEF-061.1-02 | Inherit exclusion (out of 063 scope) |
| `test_120s_timeout_fires_full_finally` | DEF-061.1-02 | Inherit exclusion |
| `test_producer_continues_after_consumer_disconnect` | DEF-061.1-02 | Inherit exclusion |

## New 063 Audit Findings

| Test File | Test Name(s) | POSTs to /threads/{tid}/messages? | Reads SSE off POST response? | Disposition (Plan 05) |
|-----------|--------------|-----------------------------------|------------------------------|----------------------|
| (one row per match — fill in by running the audit grep commands and inspecting each match) |

## Disposition Codes

- **rewrite-to-get-stream**: test still has value; rewrite to POST→assert-201→GET-stream
- **delete-redundant-with-062**: test logic is fully covered by an existing test_062_*.py case; delete
- **delete-bound-to-removed-code**: test exercises the deleted event_consumer or its closure; delete
- **inherit-existing-exclusion**: already excluded per DEF-061.1 — leave alone
- **keep-as-is**: test does NOT actually use the legacy SSE-on-POST shape (false positive in grep)
```

Run the two grep commands cited and fill the table with EVERY match. For each row, classify the disposition based on direct reading of the test (do not guess). Tests under `test_062_*.py` may match `client.stream.*messages` — those are GET-stream tests against `/runs/{rid}/stream`, NOT POST — classify as `keep-as-is`.

Per `<scope_reduction_prohibition>`: The audit MUST be exhaustive. If a row has a disposition you are unsure about, mark it `INVESTIGATE` and record what you read; Plan 05 will adjudicate. Do NOT pre-defer rows. Do NOT mark them all `keep-as-is` without inspection.
  </action>
  <verify>
    <automated>cd e2e &amp;&amp; npx playwright test --list 063-refresh-mid-stream.spec.ts 063-resume-failed.spec.ts 2>&amp;1 | grep -E '063-refresh-mid-stream|063-resume-failed'</automated>
  </verify>
  <acceptance_criteria>
    - Both `e2e/tests/063-refresh-mid-stream.spec.ts` and `e2e/tests/063-resume-failed.spec.ts` exist
    - `npx playwright test --list e2e/tests/063-*.spec.ts` lists exactly 2 test entries (one per file) with no parse errors
    - `grep -c 'test.skip\|test.fixme' e2e/tests/063-refresh-mid-stream.spec.ts e2e/tests/063-resume-failed.spec.ts` is exactly 0
    - `grep -cE 'getByRole|page.reload|waitForRequest' e2e/tests/063-refresh-mid-stream.spec.ts` is &gt;= 2 (proves it actually drives the browser)
    - `grep -cE 'getByRole.*resume' e2e/tests/063-resume-failed.spec.ts` is &gt;= 1
    - `.planning/phases/063-frontend-stream-decoupling/063-LEGACY-TEST-AUDIT.md` exists
    - The audit MD contains BOTH the "Inherited Exclusions" table AND the "New 063 Audit Findings" table
    - The audit table has &gt;= 1 data row (proven by `grep -cE '^\| (test_058_|test_059_|test_061_|backend/tests).*\|' 063-LEGACY-TEST-AUDIT.md` &gt;= 1) — even if only to record "no matches found, table empty" the planner may NOT skip the audit; if zero matches, the audit must explicitly state `**No remaining 063-relevant matches.**` outside the table
    - Audit document references the FOUR inherited exclusions by exact name (`grep -c 'test_normal_stream_unchanged\|test_failed_run_expires_60s\|test_120s_timeout_fires_full_finally\|test_producer_continues_after_consumer_disconnect' 063-LEGACY-TEST-AUDIT.md` is 4)
  </acceptance_criteria>
  <done>Two e2e specs exist and parse, audit document committed with the actual grep-derived table, no skips/fixmes, inherited exclusions explicitly enumerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → backend | Mock supabase + real Redis fixtures must not leak credentials or shared state across tests (per Phase 061 _reset_redis_singleton pattern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-063-01-01 | I | test_063_post_then_subscribe.py shared Redis singleton | mitigate | Per-file `_reset_redis_singleton` autouse fixture copied verbatim from test_062_stream_replay.py:36-51 (D-062-14 Plan 02 deviation Rule 3) — pytest-asyncio function-scoped loop binding trap |
| T-063-01-02 | T | test_063_legacy_path_deleted.py introspects production code via `inspect.getsource` | accept | Read-only AST inspection; cannot mutate state; same pattern used by static-import checks elsewhere in the project |

</threat_model>

<verification>
- All five test files exist and parse cleanly
- Backend stubs RED-fail with assertion errors (NOT collection errors)
- E2E stubs parse and list via `npx playwright test --list`
- Legacy-test audit document committed with at least the inherited-exclusion table populated
- No `pytest.skip`, `pytest.xfail`, `test.skip`, or `test.fixme` markers anywhere in the new files
</verification>

<success_criteria>
- `pytest --collect-only -q tests/integration/test_063_*.py` reports 5 tests, 0 errors
- `pytest -x tests/integration/test_063_*.py` exits non-zero with assertion failures (proves RED for the right reason)
- `npx playwright test --list e2e/tests/063-*.spec.ts` lists 2 tests with 0 parse errors
- `.planning/phases/063-frontend-stream-decoupling/063-LEGACY-TEST-AUDIT.md` exists, contains both required tables, references all 4 inherited exclusions by name
- `git ls-files | grep -cE '^backend/tests/integration/test_063_.*\.py$'` returns 3
- `git ls-files | grep -cE '^e2e/tests/063-.*\.spec\.ts$'` returns 2
</success_criteria>

<output>
After completion, create `.planning/phases/063-frontend-stream-decoupling/063-01-SUMMARY.md` documenting: file paths created, exact test counts, audit findings count by disposition code, RED-reason confirmation for each backend test (assertion text observed).
</output>
