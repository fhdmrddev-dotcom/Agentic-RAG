---
phase: 189-governed-external-action-node-model
plan: 08
type: execute
wave: 3
depends_on: ["189-06"]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/phaseState.ts
  - frontend/src/lib/phaseState.test.ts
  - frontend/src/components/panel/PhaseCard.tsx
  - frontend/src/components/panel/PhaseTimeline.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "D-07 half one: the recorded_not_sent slug resolves to its OWN client status and its OWN canvas reading — it is never resolved to done, and never left absorbed by the unknown default."
    - "There is ONE derivation and TWO vocabularies. lib/phaseState.ts gains the map key, the union member and the switch arm; the panel keeps its own harness words. No second derivation of the status rule is added anywhere."
    - "The fail-closed baseline is not regressed: before this plan a recorded_not_sent row rendered as State unknown, never as Complete. After it, the row renders as its own word, and an unrecognised status STILL renders State unknown."
    - "The developer panel states the new terminal honestly with an INHERITED glyph, and the PHASE_TYPE_LABEL table is DECLINED with the declination recorded — the shipped table already declines llm_emit and degrades an unmapped type to Step."
    - "S2 binds: every test-count change lands in the SAME COMMIT as its vitest-count-gate.cjs BASELINE pin. A pin edit without the test leaves the gate blind; a test change without the pin leaves HEAD red."
    - "The count gate's failed column is NOT read as a regression backstop (D-188.2-DEF-01). The COUNT columns are the backstop."
  artifacts:
    - path: "frontend/src/lib/phaseState.ts"
      provides: "DB_PHASE_STATUS key, the 8th CanvasReading member, and the canvasReading switch arm"
      contains: "recorded-not-sent"
    - path: "frontend/src/components/panel/PhaseCard.tsx"
      provides: "the typecheck-FORCED STATUS_META row for the new status"
      contains: "recorded-not-sent"
  key_links:
    - from: "frontend/src/lib/phaseState.ts DB_PHASE_STATUS"
      to: "the workflow_phases.status slug written by the engine"
      via: "an own-property-guarded total lookup"
      pattern: "recorded_not_sent"
    - from: "frontend/src/lib/phaseState.ts CanvasReading"
      to: "the four exhaustive Record tables downstream"
      via: "widening the union, which makes each table a typecheck error until filled"
      pattern: "CanvasReading"
---

<objective>
Widen the client's ONE status derivation to carry the sixth database slug, and give the developer panel
its own honest word for it.

Purpose: D-07 requires the not-sent state to be distinct from passed and done at every surface it
renders. This plan does the DERIVATION half plus the panel surface; plan 189-10 does the canvas
vocabulary half. Splitting them this way is deliberate: widening `CanvasReading` here is what makes the
four exhaustive `Record<CanvasReading, ...>` tables in 189-10 a TYPECHECK ERROR rather than a silent
gap. The compiler is the reminder, and this plan arms it.

⚠ **The fail-closed baseline is a genuine wave-ordering freedom and must not be lost.** Before this
plan, `phaseStatusFromDb("recorded_not_sent")` misses the own-property guard, resolves to `unknown`,
and renders as "State unknown" — never as "Complete". That is why migration 115 could land first
without the UI ever lying. Re-derive that baseline before changing anything, and keep the unknown floor
intact afterwards.

Output: one type member, three edits to the one derivation module, two panel edits, and the gate pin.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md
@.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md
@CLAUDE.md

RE-DERIVE EVERY ANCHOR by searching for the identifier.

MEASURED BASELINES, re-derived at plan time on 2026-08-07 and to be re-derived again before you start:
`cd frontend; npx tsc --noEmit -p tsconfig.app.json` = **33 errors** (a bare `--noEmit` checks ZERO
files) · `node scripts/vitest-count-gate.cjs` = **total 2508, 45/45 pinned files**.

⚠ The count gate has TWO knobs: `TARGETS` selects which files RUN, `BASELINE` is the pinned count. This
plan adds no new suite, so only `BASELINE` moves. ⚠ Do NOT read the gate's `failed 0` as "no
regression" — that column is rot on this machine (it varied 1 to 21 to 50 to 10 to 0 to 0 across eight
runs at an identical total). The COUNT columns are the backstop.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the ONE derivation — the status union, the DB map, the reading union, the switch arm</name>
  <files>frontend/src/types/index.ts, frontend/src/lib/phaseState.ts, frontend/src/lib/phaseState.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/lib/phaseState.ts` — read the WHOLE file (about 169 lines). Its docblock states the
      rule this plan must not break: this module holds the DERIVATION, never the VOCABULARY; two
      vocabularies are correct (the developer panel has harness words, the canvas has business words);
      TWO DERIVATIONS ARE NOT. Then find `DB_PHASE_STATUS`, `phaseStatusFromDb` (total and own-guarded),
      the `CanvasReading` union, and the `canvasReading` switch with its `default:` floor.
      ⚠ The `DB_PHASE_STATUS` docblock claims its five keys are EXACTLY
      `workflow_phases_status_check` — migration 115 falsified that. Correct it in this commit.
    - `frontend/src/types/index.ts` — search for the `Phase` interface's `status` union.
    - `frontend/src/lib/phaseState.test.ts` — the existing case style.
    - `supabase/migrations/115_workflow_phases_recorded_not_sent.sql` — confirm the slug you are mapping
      is byte-identical to the one in the constraint.
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` section 3a — the
      slug-to-status-to-reading-to-word table, and the note that the STATUS member and the READING
      member are deliberately the SAME STRING.
  </read_first>
  <action>
FIRST, re-derive and record the fail-closed baseline: in the test suite, assert TODAY that
`phaseStatusFromDb("recorded_not_sent")` returns `unknown`. Record that observation in the SUMMARY, then
make the change that supersedes it. This is the guard that must not regress into a `done`.

Four edits, in this order (each one is what forces the next):

1. `frontend/src/types/index.ts` — add `"recorded-not-sent"` to the `Phase["status"]` union.
   ⚠ This is the SOURCE of the forcing; `STATUS_META` in the panel becomes a typecheck error the moment
   it lands, which is Task 2.
2. `frontend/src/lib/phaseState.ts` `DB_PHASE_STATUS` — add the key `recorded_not_sent` mapping to
   `"recorded-not-sent"`. The KEY is the database slug (snake_case, D-17); the VALUE is the client
   status (kebab). Correct the docblock sentence that claimed the five keys are exactly the CHECK
   constraint, naming migration 115 and Phase 189 as what changed it.
3. `frontend/src/lib/phaseState.ts` `CanvasReading` — add the 8th member `"recorded-not-sent"`.
   Use the SAME STRING as the status member, exactly as `failed` and `skipped` already do. That makes
   the switch arm a one-line identity and removes a place for the two unions to drift.
4. `frontend/src/lib/phaseState.ts` `canvasReading` — add the switch arm. **Keep the `default:` floor
   returning `unknown`** — it is the fail-closed catch-all and an unrecognised state must never be a
   pass.

⚠ `phaseStatusFromDb` needs NO change: it reads the map behind an own-property guard and is already
total. Do not add a second lookup, a second map, or a local status rule anywhere — the acceptance for
the one-derivation requirement was a grep proving zero local re-derivations, and this plan must not be
the one that adds one.

Extend `frontend/src/lib/phaseState.test.ts`:
- `phaseStatusFromDb("recorded_not_sent")` is NOT `"done"` and NOT `"unknown"` — it is
  `"recorded-not-sent"`. **Assert both negatives explicitly**; asserting only the positive would still
  pass if the value were also aliased to done somewhere.
- `phaseStatusFromDb("a_status_nobody_ships")` STILL returns `"unknown"` — the floor survives.
- `canvasReading` for a phase carrying the new status returns the new reading, and a phase with
  `pendingAsk` set still returns the waiting reading (the precedence above the switch is unchanged).
- A prototype-pollution probe: `phaseStatusFromDb("constructor")` returns `"unknown"`. The own-property
  guard is already there; this pins it while the map grows.

Update `scripts/vitest-count-gate.cjs`'s `BASELINE` entry for `phaseState.test.ts` in the SAME COMMIT.
  </action>
  <verify>
    <automated>cd frontend; npx vitest run src/lib/phaseState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new cases PASS, including both negatives (`!== "done"` and `!== "unknown"`) and the
      `"constructor"` probe returning `"unknown"`.
    - The SUMMARY records the pre-change observation that `phaseStatusFromDb("recorded_not_sent")`
      returned `"unknown"`, and quotes the assertion that superseded it.
    - `canvasReading`'s `default:` arm still returns `unknown` — verify by reading the diff, and by the
      unrecognised-status test.
    - `grep -rn "recorded_not_sent" frontend/src --include=*.ts --include=*.tsx` shows the SNAKE_CASE
      slug in exactly ONE place outside tests: the `DB_PHASE_STATUS` key. Any second occurrence is a
      second derivation.
    - The `DB_PHASE_STATUS` docblock no longer claims its keys are exactly the CHECK constraint.
    - `node scripts/vitest-count-gate.cjs` reports OK with the `phaseState.test.ts` pin moved to its new
      count in the same commit, and no per-file DECREASE anywhere.
  </acceptance_criteria>
  <done>One derivation carries the sixth slug; the unknown floor and the WR-04 guard both survive; the gate pin moved with the tests.</done>
</task>

<task type="auto">
  <name>Task 2: The developer panel's own honest word — STATUS_META (forced) and the announcer (silent)</name>
  <files>frontend/src/components/panel/PhaseCard.tsx, frontend/src/components/panel/PhaseTimeline.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/panel/PhaseCard.tsx` — search `STATUS_META`. It is an EXHAUSTIVE
      `Record<Phase["status"], StatusMeta>` and is therefore a TYPECHECK ERROR right now. Read its
      `unknown` row and the comment above it: it documents WHY the compiler demanded that row and notes
      the glyph was INHERITED rather than invented. Copy both habits.
    - `frontend/src/components/panel/PhaseCard.tsx` — search `PHASE_TYPE_LABEL`. ⚠ **BAD ANALOG. DO NOT
      EXTEND IT.** It has only five entries (`llm_emit` was already declined) and degrades an unmapped
      type to `UNKNOWN_PHASE_META` = "Step".
    - `frontend/src/components/panel/PhaseTimeline.tsx` — search `milestoneFor`. It is a switch with a
      `default:` arm, so it is SILENT to the new member — only this list finds it.
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` sections 3a and 3b — the
      panel row (`Not sent`, glyph `↛`, muted text class) and the announcer sentence, plus the C10
      reasoning that `↛` is measured UNUSED in `frontend/src` while `⊘` already means cancelled and
      reusing it would give one glyph two meanings.
  </read_first>
  <action>
A — `STATUS_META` (typecheck-FORCED). Add ONE row for `"recorded-not-sent"`:
glyph `↛`, text `Not sent`, and the muted panel text class the `unknown` and `skipped` rows already use.
ADD ONLY — nothing above changes. Write the comment in the shipped `unknown` row's voice: state that the
row exists because the compiler demanded it, name Phase 189 / CONN-01 / D-07 as the reason
`Phase["status"]` grew, and record that `↛` was chosen because it is measured unused in `frontend/src`
whereas `⊘` already means CANCELLED — one glyph, one meaning, per the icon convention.

⚠ The panel keeps its OWN vocabulary. `Not sent` here and `Not sent — recorded` on the canvas are
CORRECT as two words for one state: the panel has harness words, the canvas has business words, and only
the derivation is shared. Do NOT re-word the panel's shipped six.

B — `milestoneFor` (SILENT). Add one arm producing the announcer sentence in the shipped pattern —
ordinal, slug, then the state, ending in `not sent`. Keep the `default:` arm.

C — `PHASE_TYPE_LABEL` is **DECLINED, and the declination is RECORDED**. Add a short comment at that
table stating that Phase 189 deliberately did not add a seventh entry: the table already declines
`llm_emit`, degrades an unmapped type honestly to "Step", and inventing a panel vocabulary for a type
the panel never gained one for would be a slot spent for nothing. Declining a slot is a decision, and it
is recorded here rather than discovered later. ⚠ `STATUS_META` is a DIFFERENT table and is NOT
declinable — it is typecheck-forced.

Extend the panel suites with: the new `STATUS_META` row rendering its text and glyph; the announcer
sentence for the new status; and a guard that `PHASE_TYPE_LABEL` still has exactly its shipped number of
entries so the declination is pinned rather than merely commented.

Update `scripts/vitest-count-gate.cjs` `BASELINE` for every panel test file whose count changed, in the
SAME COMMIT.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.app.json` reads exactly **33** errors — the measured baseline. Any
      other number is explained line by line in the SUMMARY. (Between Task 1 and Task 2 it will read
      MORE than 33 because `STATUS_META` is forced; record that intermediate number as proof the forcing
      fired.)
    - `npx vitest run src/components/panel` passes, including the new rows.
    - `STATUS_META` contains the `recorded-not-sent` row with glyph `↛` and text `Not sent`, and
      `grep -c "↛" frontend/src` shows it appears in the panel row and nowhere it would collide.
    - `grep -c "⊘" frontend/src/components/panel/PhaseCard.tsx` is unchanged from HEAD — `⊘` was not
      reused.
    - `PHASE_TYPE_LABEL`'s entry count is UNCHANGED and is pinned by a test; the declination comment
      names Phase 189.
    - `git diff` on `STATUS_META` shows only an ADDED row; no shipped row's text, glyph or class changed.
    - `node scripts/vitest-count-gate.cjs` reports OK with every changed pin moved in the same commit.
  </acceptance_criteria>
  <done>The developer panel names the new terminal in its own harness vocabulary with an inherited glyph, and the declined table is pinned rather than merely commented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `workflow_phases.status` (untyped `str` on the wire) to the client status union | an arbitrary server string enters client type-space here; the guard is a total, own-property-checked lookup |
| an object-literal lookup table to a user-influenceable key | `constructor`, `toString` and `__proto__` are INHERITED, are never nullish, and a `?? fallback` does not fire for them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-23 | Spoofing | an unrecognised `workflow_phases.status` read as success | mitigate | `canvasReading`'s `default:` floor and `phaseStatusFromDb`'s own-property guard both stay. Tests assert an unshipped status still returns `unknown`, and assert the new status is NEITHER `done` NOR `unknown`. Verdict surfaces fail CLOSED — an unrecognised state is never a pass. |
| T-189-24 | Tampering | prototype pollution through `DB_PHASE_STATUS[raw]` (**WR-04**) | mitigate | `phaseStatusFromDb` already uses `Object.prototype.hasOwnProperty.call`. This plan pins it with a `"constructor"` probe while the map grows, because a bare `TABLE[key] ?? fallback` on an object literal returns `[Function Object]` and hard-crashes the render — measured in this codebase. |
| T-189-25 | Repudiation | a second, local derivation of the status rule drifting from the shared one | mitigate | An acceptance criterion greps for the snake_case slug and requires exactly ONE non-test occurrence. Two vocabularies are correct; two derivations are not. |
| — | Information Disclosure / Denial of Service / Elevation of Privilege | — | n/a | **No new attack surface.** This plan adds one string to a union, one key to a lookup, one switch arm and two presentational rows. It opens no route, reads no credential, makes no request and gains no privilege. Stated with its reason rather than omitted. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs nothing — no `npm install`, no new dependency. No install task exists in this phase, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `cd frontend; npx vitest run src/lib/phaseState.test.ts src/components/panel`
- `cd frontend; npx tsc --noEmit -p tsconfig.app.json` reads exactly 33.
- `node scripts/vitest-count-gate.cjs` reports OK, 45/45 files, no per-file decrease, with every moved
  pin in the same commit as the tests that moved it. ⚠ The `failed` column is NOT evidence — read the
  COUNT columns.
- `git diff --stat` lists exactly the six declared files. No canvas file, no `runVocabulary.ts`, no
  fenced card-subtree module is touched by this plan.
</verification>

<success_criteria>
- The sixth slug has a client status, a canvas reading and a panel word, derived in one place.
- The unknown floor and the own-property guard both survive, pinned by tests.
- `tsc` is back to 33 and the count gate is OK with pins moved in-commit.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-08-SUMMARY.md` when done.
Record: the pre-change fail-closed observation, the intermediate `tsc` error count that proved
`STATUS_META` was forced, the final count of 33, and every count-gate pin moved.
</output>
