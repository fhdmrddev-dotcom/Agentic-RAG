---
status: complete
phase: 189-governed-external-action-node-model
source:
  - 189-01..15-SUMMARY.md
  - 189-VALIDATION.md (the driven sessions — 2026-08-07 and 2026-08-08)
started: 2026-08-08
updated: 2026-08-08
---

<!--
⚠ READ THIS BEFORE TREATING ANY ROW AS UNVERIFIED.

This board is RECONCILED, not freshly interrogated. Phase 189's live UAT was already driven
end to end across two Chrome-MCP sessions (189-16 Task 3, commits `267df91d` / `00afaa55`),
and the rows below carry that session's evidence rather than a re-ask. Re-driving twenty
already-measured rows conversationally would produce weaker evidence than the DOM/geometry
reads and live DB/Redis queries already recorded in `189-VALIDATION.md`.

Two rows were re-verified INDEPENDENTLY by this workflow against the live database at
:54322 rather than inherited (see rows 6 and 17). Everything else cites the driven session.

The only rows presented to the operator as live checkpoints are the ones that need a
DECISION, not an observation — they are at the bottom, numbered D1/D2.
-->

## Current Test

[testing complete]

## Operator decisions at this gate — 2026-08-08

### D1. `BUG-260808-01` (test 22) → NOT a 189 gap; rolls forward as a filed bug

Rationale recorded so it is not re-litigated: the slug is not user-authorable (`D-184-11` —
the UI has no slug field; the caller derives it), so the row was driven from a seeded fixture
and no real user can reach it. It is not a 189 regression — the sink predates the phase. And
G-7's own logic applies: a closure round here would be fixing code the previous round touched,
while every ROADMAP success criterion is verified.

⚠ **The systemic answer is named rather than deferred silently.** `/gsd:secure-phase 189`
identified the root enabler while judging T-189-24: `slug` is unconstrained end to end —
`backend/app/models/harness.py:202` is a bare `slug: str` and
`supabase/migrations/058_workflow_phases.sql:18` is `text NOT NULL`. Guarding slug-keyed sinks
one at a time has now produced **three** reports (`BUG-260806-01` → `BUG-260807-01` →
`BUG-260808-01`). The next visit should constrain the slug, not add an eighth guard.

### D2. The armed checkpoint's approve label → FIXED NOW (G-3), not deferred to 190

"Approve and run this step" → **"Approve this step"**, commit `249c3c83`.

The step's own recorded output reads *"No email was sent. Nothing left this workflow. This is
a record of an intention, not a receipt."* while the button offered to *run* it — the one
user-visible sentence contradicting SC#4.

**Bigger than a one-string fix, and stated rather than smoothed:** the literal is BOTH the
presented label AND the consent token, asserted at **16 sites across 4 test files**. It was
changed as ONE literal for every armed type rather than made phase-type-conditional, because
the gate at `_resolve_failure_with_ask_user` (`harness_engine.py:~1365`) is an EXACT-MATCH
fail-closed compare — a conditional label would force its consent allow-list to accept two
strings, widening the consent set on an irreversible action, which is precisely the surface
Phase 190 makes dangerous. Verified before editing that no live paused run existed (the three
`active` `workflow_runs` are stale zombies from June/July), and a run paused under the old
label fails CLOSED on resume rather than silently approving.

Gates: pytest **47 passed** · vitest **27 passed** · `tsc -p tsconfig.app.json` = **33**
(unmoved) · count gate **OK, 48/48 pinned, 0 failing**.

## Tests

### 1. Place the governed external-action step on the canvas
expected: The step-type picker offers a 7th type ("Sends an email"); choosing it places a card on the spine.
result: pass
evidence: Placed BY KEYBOARD (`End` then `Enter`) on `weekly-status-report-wfzqel` after BUG-260807-02 shipped. `End` scrolled the panel and row 7 became genuinely hit-testable (`elementFromPoint` reached it) before `Enter` chose it. Driven 2026-08-08.

### 2. The picker row previews the card that actually lands (WR-03)
expected: The picker row's two lines match the placed card's two lines.
result: pass
evidence: Row 7 reads "Sends an email / Stops for your approval before it acts outside"; the placed card reads the same two lines plus the `Not connected` badge.

### 3. The 7th glyph is visible on Deep Midnight, not ~4x dimmer (U1)
expected: The new mark is not a repeat of the `llm_batch_agents` luminance-34.5 defect.
result: pass
evidence: Marks are IDENTICAL, not merely in-band — all seven picker rows fill `rgb(243,245,252)`, opacity 1, 32x32; on canvas luminance 91.39 at 48x48. Only the tint WELL differs (`rgba(242,95,217,0.38)`, luminance 32.07 — mid-range against the other six).

### 4. The "Not connected" badge occludes nothing and is occluded by nothing (U2)
expected: `elementFromPoint` resolves the badge at its own centre and each neighbour at theirs.
result: pass
evidence: Badge rect `747,460 142x33`; probes at the badge centre, the ⛨ seal, the verdict-mark edge and both badge corners all resolve as expected. FALSIFICATION CONTROL OBSERVED SWINGING BOTH WAYS — a planted `zIndex 99999` div drove `badgeReachable` true → false; removal drove it false → true.

### 5. Adding the badge does not break the card height budget (U4)
expected: Neighbours do not reflow; the spine's top baseline does not move.
result: pass
evidence: All three cards share `y=302`, spaced uniformly 478 px. `act` is 222 h vs 172 h for both neighbours — 50 px taller, growing DOWNWARD ONLY, the documented acceptance. ⚠ The without-badge half is unmeasurable on the same card (the badge is state-conditional on `notConnected`, and nothing can connect while zero MCP code exists); the neighbour-reflow half — what the row protects — is fully measured.

### 6. An external-action workflow can be published
expected: The publish gauntlet accepts a definition containing an `external_action` step.
result: pass
evidence: **INDEPENDENTLY RE-VERIFIED BY THIS WORKFLOW** against the live DB at :54322, not inherited — `weekly-status-report-wfzqel` v1 `status=published`, phases `retrieve · act · emit`, `act.config.phase_type = external_action`, `capability = send_email`, at phase 2 of 3 (deliberately not last, the only arrangement in which the CR-02 mid-run sweep can fire). `business_requirement` present.

### 7. SC#4 — the step records intent and sends NOTHING
expected: The phase output reads as a record of intention, with no receipt language and no send.
result: pass
evidence: Read straight from `workflow_phases.output`: "NOT SENT — recorded only." / "What this step would have done" / "No email was sent. Nothing left this workflow. This is a record of an intention, not a receipt." with `recorded_intent.capability = "send_email"`.

### 8. WR-02 — the user's chat question is not swept into the record
expected: `recorded_intent.inputs` carries only the action's own inputs.
result: pass
evidence: Keys are `['content']`; `kickoff_prompt` is ABSENT. Before the fix the user's chat question was swept in.

### 9. A LIVE run pauses at the armed checkpoint and refuses to continue (D-04 / 189-14)
expected: The run stops at the external-action step and waits for a human.
result: pass
evidence: Live run paused at `act`: "This step is marked as needing your approval first. The run is waiting here and will not continue until you answer." ⚠ Stronger than recorded: `action_risk_armed` is ABSENT from the stored JSONB (verified in the live DB by this workflow), so the pause proves the D-04 model_validate COERCION arms it on the read path — which is exactly the "all six write paths converge" property, not a persisted flag.

### 10. A GOLDEN run auto-continues past the armed checkpoint (D-19)
expected: Publish-time golden runs do not hang at the checkpoint.
result: pass
evidence: Two full runs went straight through — `[0s] run=active` → `[69s] act=recorded_not_sent` → `[150s] run=completed`. The pre-fix behaviour died at 7200 s.

### 11. The arming switch renders ON and visibly REFUSES
expected: The switch shows armed, and clicking it does not disarm.
result: pass
evidence: `role="switch"`, `aria-checked="true"`, `aria-disabled="true"`, copy "This step reaches outside your workspace, so it always stops and asks you first. That cannot be switched off." A real click left it `true` — the Phase-185 visible-refusal shape.

### 12. CR-02 — the step reads "Not sent" mid-run and never flashes "Complete"
expected: Through BOTH finalize sweeps, `act` holds its honest terminal.
result: pass
evidence: Proven on the WIRE (event 5 `phase_recorded_not_sent` arrives before event 7 `phase_started emit`, the sweep trigger) AND on the PAINT (after `emit` started and after the run completed — firing `finalizeEarlierPhasesForThread` AND `finalizeAllPhasesForThread` — the spine still read `retrieve ✓ Complete ▸ act ↛ Not sent ▸ emit ✓ Complete`). `act` never flashed "✓ Complete".

### 13. The "Not sent" ring is distinguishable by SHAPE ALONE in greyscale (U3)
expected: The 8th reading is separable without colour.
result: pass
evidence: On the LIVE rendered canvas of a real run — `stroke-dasharray "32.044 21.363" x4` (FOUR arcs), `stroke-dashoffset 16.022`, muted stroke, `animationName: none`. Every number 189-10 predicted from a synthetic DOM confirmed on a real one, including the 16.022. Byte-identical under `filter: grayscale(1)`. It is the ONLY ring on the canvas — both `Complete` cards carry zero rings.

### 14. Live-run readings stay distinguishable; the running arc spins; a no-reading card is still (U5 — the 188.2 debt)
expected: Four readings, four distinct shapes, on one run.
result: pass
evidence: `Running` → one arc, `"55.543 158.085"`, offset 0, `animating: true` (genuinely spins). `Not started`/`Locked` → no ring, `animating: false`. `Complete` → no ring. `Not sent` → four arcs, still. Discharges `D-188.2-DEF-07` row A2.

### 15. One glance at a Builder card reads correctly (U6 — the 188.2 debt)
expected: Type name, subtitle and badge in the intended hierarchy.
result: pass
evidence: The ⛨ "Must prove it" seal sits top-right on `retrieve`/`emit` and is correctly ABSENT on `act`. No layout break at any of three window sizes. Discharges `D-188.2-DEF-08` row A1's visual half.

### 16. The run band's RUN-level verdict reads correctly above a phase-level "Not sent" (D-25)
expected: A completed run containing an honestly-unsent step does not read as a contradiction.
result: pass
evidence: RENDERED half, not just the wire — the band reads "Phase 3 of 3, emit, complete" while the spine reads `retrieve ✓ Complete ▸ act ↛ Not sent ▸ emit ✓ Complete`.

### 17. Migration 115 is applied to the live local DB (U7)
expected: `workflow_phases` accepts the `recorded_not_sent` literal in production use.
result: pass
evidence: **INDEPENDENTLY RE-VERIFIED BY THIS WORKFLOW** — `pg_get_constraintdef` on `workflow_phases_status_check` reads `ARRAY['pending','active','completed','failed','skipped','recorded_not_sent']`, and **4 real phase rows** are persisted at `recorded_not_sent`. Applied via the SQL editor path; no `db push`, no `db reset`.

### 18. The capability picker behaves like the radiogroup it declares (WR-04)
expected: Arrow keys move focus and selection together.
result: pass
evidence: Roving tabindex `0,-1,-1`; a real `ArrowDown` moved focus 0 → 1 with `aria-checked` following (`false,true,false`) and the roving tabindex moving with it (`-1,0,-1`); `ArrowUp` restored. Correct APG behaviour.

### 19. No focusable control entered the card (the 188.2 invariant)
expected: The card holds one tab stop per node; ✕ and ＋ live on the lane.
result: pass
evidence: `act.querySelectorAll('button,a[href],input,select,textarea,[tabindex]:not([tabindex="-1"])')` → **0**.

### 20. The AI seed places `external_action` unprompted from plain language
expected: A plain-language requirement mentioning an email yields the governed node, with a seed receipt.
result: pass
evidence: Given "…then email that answer to the requester", the NL generator produced `Phase 4: Email the cited answer to the requester (external_action)` with `capability: send_email`, and its seed receipt explained the grounding it chose ("1 step reads your documents, so I set it to must prove it"). 189-09 holds on a real request.

### 21. The own-vs-Open-Platform decision is recorded (SC#3)
expected: The milestone carries the connector-architecture verdict as a repository document.
result: pass
evidence: `docs/CONNECTOR-ARCHITECTURE.md` (MCP-first, first-party-thin, Open-Platform-sequenced) + pointer entry `D-v3.6-01` in `.planning/prd-reset/DECISIONS.md` + one added CLAUDE.md discoverability line. Verified during `/gsd:secure-phase` to name no credential, endpoint, API key, hostname or IP (T-189-08).

### 22. A phase slugged `constructor` renders in its own lane
expected: The card lands in its correct lane, not stacked at the origin.
result: issue
reported: "Driven row FAILED. `constructor`-slugged phase renders with NO transform at all — card stacked on phase 1 at rect 58,176, identical to `retrieve`. Control swung both ways: the same fixture with slug `ordinaryslug` renders `translate(320px, 0px)`."
severity: minor
evidence: The `own()` guard shipped for `verticalOffsetFor` HOLDS (`transformHasNaN: false` everywhere) — this is a DIFFERENT, seventh slug-keyed sink resolving the inherited `Object.prototype.constructor` (a function, never nullish, so every `!== undefined` / `?? fallback` passes). Filed as `BUG-260808-01`. ⚠ The UI cannot author this slug at all (`D-184-11` — there is no slug field; the caller derives it), so the row was driven from a seeded fixture, since deleted.

## Summary

total: 22
passed: 21
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

```yaml
- truth: "A phase slugged `constructor` renders in its own lane rather than stacked at the origin"
  status: failed
  reason: "Driven row FAILED — a seventh slug-keyed lookup resolves Object.prototype.constructor and writes no transform. Distinct sink from the one BUG-260807-01's own() guard fixed; that guard holds."
  severity: minor
  test: 22
  artifacts: [".planning/reported-bugs/BUG-260808-01-node-position-lookup-prototype-slug.md"]
  missing: []
  reachability: "NOT user-reachable — D-184-11: the UI has no slug field; the caller derives the slug. Driven from a seeded fixture."
```

## Routed elsewhere — recorded so they are not lost

Captured by the operator at the 189-16 UAT and explicitly **NOT folded into 189** (commit `5a4f4228`):

| Item | Where it went |
|---|---|
| No way to stop a workflow mid-run | `SEED-140` |
| The approval pause hands the user off to chat (raised a 2nd time) | `BUG-260808-02` |
| All seven phase types are AI steps — investigate deterministic utility nodes | `SEED-141` |
| ⑂ Tweak fails with a silent 409 (console only, no toast) | recorded in `189-VALIDATION.md` |
| The LOOSE "Describe & run" door never persists `business_requirement`, so nothing it creates can ever be published | recorded in `189-VALIDATION.md` |

The operator's note: BUG-260808-02, SEED-140 and SEED-139 converge on one question — what the run surface owns versus chat — so they want ONE phase, not three visits.
