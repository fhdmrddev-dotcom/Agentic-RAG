---
phase: 189-governed-external-action-node-model
plan: 15
type: execute
wave: 8
depends_on: ["189-13", "189-14"]
files_modified:
  - frontend/src/components/workflows/PhaseNode.tsx
  - frontend/src/components/workflows/canvasModel.ts
  - frontend/src/components/workflows/PhaseNodeCard.tsx
  - frontend/src/components/workflows/phaseNodeCardContract.ts
  - frontend/src/components/workflows/NodeCornerMarks.tsx
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.test.tsx
  - frontend/src/components/workflows/canvasModel.test.ts
  - frontend/src/components/workflows/PhaseNode.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "D-12 and D-18: badge slot 1 — the LAST free word-badge on the card — is spent on the state-conditional label 'Not connected', constructed in the ONE place that builds the badge tuple, and the card component itself is not edited."
    - "The badge is FIRST in the tuple. The tuple is ORDERED and the shipped waits-for-you badge is documented as slot 2 in three separate places."
    - "The tuple is built with EXPLICIT BRANCHES, never a spread. A spread widens the tuple type to an array and silently retires the max-two typecheck guard that a control test drove RED."
    - "The three card invariants hold and are re-observed rather than assumed: a third badge is a TYPECHECK ERROR (the control swings), no focusable control lives inside the card (the badge is a span), and the top-right corner remains the governance seal's."
    - "Six shipped prose claims flip from 'slot 1 is reserved' to 'slot 1 is spent', and the two test guards are REWRITTEN rather than deleted — each with a positive control."
    - "D-21: the shipped docblock naming Phase 189 as the claimant of the unarmed edge state is a PROSE CORRECTION ONLY. D-04 makes that state unreachable forever and the projection already cannot produce it. No ghost-detour edge is built."
    - "Any edit inside the fenced six-file card subtree is COMMENT-ONLY, the subtree path list stays at six, and its length pin does not move."
  artifacts:
    - path: "frontend/src/components/workflows/PhaseNode.tsx"
      provides: "the Not connected badge object and the explicit-branch tuple expression"
      contains: "Not connected"
    - path: "frontend/src/components/workflows/canvasModel.ts"
      provides: "the corrected slot-1 prose and the corrected unarmed-edge docblock (D-21)"
  key_links:
    - from: "canvasModel notConnected"
      to: "PhaseNode badge tuple"
      via: "a projection-time boolean read by the adapter"
      pattern: "notConnected"
    - from: "the badge tuple"
      to: "PhaseNodeCard badges prop"
      via: "the existing prop — the card is NOT edited"
      pattern: "badges"
---

<objective>
Spend badge slot 1 — the last free word-badge on the card — on the one fact the card cannot otherwise
carry: this step reaches outside and is not wired to anything yet.

Purpose: D-12 chose the TRANSIENT fact over the INVARIANT one deliberately. The armed-for-approval fact
is invariant for the type, and a badge that is always present on a type is really part of the type; Phase
185 set that precedent by spending NO badge at all on governance and using the corner seal instead.
Because this badge is conditional on STATE, it retires cleanly when Phase 190 connects the node — by
DATA, not by an edit.

⚠ **This is the plan most likely to break something quietly**, for three measured reasons: the badge
tuple is ORDERED and the shipped badge owns slot 2; a spread would silently retire a typecheck guard;
and SIX shipped prose claims plus TWO live test guards assert the slot is still reserved.

Output: one predicate read, one badge object, one branch expression, six prose corrections, two rewritten
guards — and zero code changes inside the fenced card subtree.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md
@.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@CLAUDE.md

**LOAD THE PROJECT DESIGN SKILL** — `Skill("sketch-findings-agentic-rag")` — and read its
canvas-frame-and-node-anatomy reference (the node geometry and the badge budget) and
`references/icon-convention.md` section 4 before touching the card's face. ⚠ The card is a SIX-FILE
subtree now, and the skill's pointers are per-fact across it; a blanket "read the card component" is as
wrong as a blanket "read the new modules".

RE-DERIVE EVERY ANCHOR by searching for the identifier.
BASELINES: `npx tsc --noEmit -p tsconfig.app.json` = **33** · count gate **total 2508 / 46 pinned files
after plan 189-14**. Re-derive both before starting.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The badge — one object, one explicit-branch tuple, zero card edits</name>
  <files>frontend/src/components/workflows/PhaseNode.tsx, frontend/src/components/workflows/PhaseNode.test.tsx, frontend/src/components/workflows/canvasModel.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseNode.tsx` — search for the shipped badge object and the
      expression that builds the badge tuple. **This is the ONLY place in the tree that constructs one.**
      Note the badge object's four fields and their conventions: a kebab test id with the canvas prefix,
      a tone from the shared three-tone vocabulary, a label, and a single kebab data attribute valued
      "true".
    - `frontend/src/components/workflows/phaseNodeCardContract.ts` — search `BadgeSlots` and read the
      docblock above it: it is a MAX-TWO TUPLE UNION and a third badge is a TYPECHECK ERROR, not a review
      comment. Note that a control test drove that guard RED. Also search for the statement that the
      top-right corner is claimed for governance and that this phase may not take it.
    - `frontend/src/components/workflows/PhaseNodeCard.tsx` — search for how the `badges` prop is
      rendered and for the shared status-chip primitive. ⚠ **You are reading this to confirm NO CODE
      CHANGE IS NEEDED HERE.** The card already renders the tuple.
    - `frontend/src/components/workflows/canvasModel.ts` — search `buildPhaseData` and confirm the
      `notConnected` field added in plan 189-13 is present and populated.
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — search for the leaf walk asserting the
      card contains NO focusable control of any kind. It was driven RED against a planted button.
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` sections 2a, 2c and 2d —
      the tuple construction shape, the tone-and-typography table against the shipped slot-2 badge, and
      what happens at Phase 190.
  </read_first>
  <action>
A — THE BADGE OBJECT. In `PhaseNode.tsx`, add a second badge object mirroring the shipped one's four
fields: a kebab test id in the canvas convention, the MUTED tone, the label `Not connected`, and a single
kebab data attribute valued "true".

Why muted and not the primary tone: primary is the live/waiting indigo and is spoken for by slot 2; a
success tone would read as good news; the strong tokens are banked for run status. "Not connected" is a
calm design-time fact, not an alarm. The WORD carries the meaning either way — tone is decoration.
⚠ The badge carries NO GLYPH. The icon convention is explicit that a word-badge carries no glyph.

B — THE TUPLE. Replace the single-condition tuple expression with EXPLICIT BRANCHES covering all four
combinations of the two booleans, with the new badge FIRST in every branch that includes it.

⚠ **NEVER A SPREAD.** A spread widens the tuple type to a plain array and silently retires the max-two
typecheck guard. Explicit branches only.
⚠ **ORDER IS A TUPLE POSITION, NOT A PREFERENCE.** The shipped badge is documented as slot 2 in three
separate places; the new one is slot 1 and therefore first.
⚠ The two-badge branch is UNREACHABLE from real data in this phase — the two badges belong to different
phase types — and it must STILL BE WRITTEN, because the tuple type demands it be representable and the
positive control for the max-two guard needs it. Say that in a comment rather than leaving the next
reader to wonder.

C — ZERO CARD EDITS. `PhaseNodeCard.tsx` renders the tuple through its existing prop, so no code change
is needed there or in the other four fenced modules. That is exactly what the 188.2 refactor cut the
contract out for. If you find yourself editing card code, stop and re-read the trace.

D — RE-OBSERVE THE THIRD-BADGE CONTROL. The max-two guard is backed by a deliberate compile-error control
that was observed RED at one error count and green at another. **Re-run that control and confirm it still
swings both ways** now that slot 1 is spent. A guard nobody has re-observed after changing its subject is
a guard on trust.

E — TESTS.
- `PhaseNode.test.tsx` / `canvasModel.test.ts`: the badge is PRESENT on the new type and ABSENT on all
  six shipped types, asserted individually — that negative branch is the falsifiable half available
  today, since nothing in the app can be connected to anything until Phase 190. Say so in the comment.
- The badge is FIRST when both are present — assert the order, not just the membership.
- The badge element is a span with no role, no tabindex and no handler; re-run the card's
  no-focusable-control leaf walk and confirm it still passes.
- Both strings coexist on one card without reading as a duplication: assert that the badge's word and
  the run-time status word BOTH render for a recorded-not-sent external-action node, and that they are
  NOT equal. ⚠ Use exact-match assertions — the badge word and one shipped run word share a leading
  token, so a substring assertion would be ambiguous.

Update the gate pins in the SAME COMMIT.
  </action>
  <verify>
    <automated>cd frontend; npx vitest run src/components/workflows/PhaseNode.test.tsx src/components/workflows/canvasModel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The badge renders on the new type and is ABSENT on all six shipped types, asserted individually.
    - When both badges are present the new one is FIRST — asserted on order.
    - `grep -c "\.\.\." ` over the tuple expression returns 0 — no spread.
    - `cd frontend; npx tsc --noEmit -p tsconfig.app.json` reads exactly 33, and the SUMMARY records the
      re-observed third-badge control swinging to its error count and back.
    - The card's no-focusable-control leaf walk still PASSES.
    - The badge word and the run word both render for one node and are asserted NOT equal, using
      exact-match assertions.
    - `git diff --stat` shows ZERO code changes in `PhaseNodeCard.tsx`, `phaseNodeCardContract.ts`,
      `NodeCornerMarks.tsx`, `NodeIconWell.tsx`, `NodeRunOverlay.tsx` and `ownProperty.ts` from THIS task
      (Task 2's comment-only edits are separate and are checked there).
    - The count gate reports OK with pins moved in-commit.
  </acceptance_criteria>
  <done>Slot 1 is spent on a state-conditional badge, ordered correctly, built without a spread, with the max-two guard re-observed.</done>
</task>

<task type="auto">
  <name>Task 2: Flip six reserved-slot prose claims, rewrite two guards with positive controls, and correct the D-21 docblock</name>
  <files>frontend/src/components/workflows/canvasModel.ts, frontend/src/components/workflows/PhaseNodeCard.tsx, frontend/src/components/workflows/phaseNodeCardContract.ts, frontend/src/components/workflows/NodeCornerMarks.tsx, frontend/src/components/workflows/PhaseNodeCard.test.tsx, frontend/src/components/workflows/WorkflowCanvas.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - Re-derive the SIX prose sites yourself:
      `grep -rn "slot 1\|badge slot\|189" frontend/src/components/workflows/` and read each hit. Measured
      on 2026-08-07 they were in the card component, the projection module, the corner-marks module, the
      adapter (twice) and the slot contract — **six FILES, and one of them states it at three separate
      places.** Verify rather than trust.
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — find the guard whose comment says slot
      1 is still empty and still reserved. ⚠ **Measured: its ASSERTION stays valid** — it passes an
      explicit badge list and asserts one chip, which is the card's own budget test. **Only the COMMENT
      is falsified.** Read it before deciding what to change.
    - `frontend/src/components/workflows/WorkflowCanvas.test.tsx` — find the guard whose title says no
      phase node carries a chip because slot 1 is empty and reserved. ⚠ **Measured: it asserts the
      absence of the RETIRED GROUNDING chip specifically, not the absence of any badge**, so it does NOT
      mechanically break. Only its WORDING misleads.
    - `frontend/src/components/workflows/canvasModel.ts` — find the docblock about the edge's armed
      states. It says verbatim that the `false` value is this phase's state, an external-action step whose
      checkpoint the author turned off. ⚠ **D-04 makes that unreachable FOREVER**, and the projection's
      checkpoint helper already cannot produce it.
    - `frontend/src/components/workflows/FlowEdge.tsx` — search the ghost-marks branch. **Read it only to
      confirm you are NOT editing it.**
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` section 2e (the six sites
      and the two guards, with the measured corrections) and its CONFLICTS item 6 (D-21).
  </read_first>
  <action>
A — THE SIX PROSE SITES. Flip each from "slot 1 is reserved for Phase 189" to a record of what 189 SPENT
it on and WHY it retires at Phase 190. Keep each site's surrounding argument intact — in particular the
corner-marks module's argument that governance spends no badge must SURVIVE the edit, and the slot
contract's sentence is the RECORD of why the 188.2 refactor cut where it did and must not be deleted,
only put into past tense.

⚠ **Three of these files are inside the FENCED six-file card subtree. These edits must be COMMENT-ONLY.**
Every changed line in those three files must begin with comment syntax. Verify it in the diff, line by
line. The subtree path list stays at SIX entries and its length pin does not move.

B — THE TWO GUARDS, REWRITTEN NOT DELETED.
- The card-suite guard: rewrite the falsified COMMENT, and ADD a two-badge render case as the POSITIVE
  CONTROL. Its assertion about the card's badge budget stays.
- The canvas-suite guard: rewrite the misleading title and description, and ADD the real 189 guard beside
  it — the not-connected data attribute is PRESENT on an external-action node and ABSENT on all six other
  types. Keep the original grounding-chip absence assertion; it still guards something real.

C — D-21, THE PROSE CORRECTION THAT IS NOT A FEATURE. Delete the sentence naming this phase as the
claimant of the unarmed edge state, and record WHY: D-04 makes that state unreachable forever, and the
projection's checkpoint helper already returns only the armed value or nothing.

⚠ **BUILD NOTHING HERE.** The edge module's ghost-marks branch stays shipped and stays unreachable. A
plan that budgets work for a ghost-detour edge has misread this. Confirm by an acceptance criterion that
the edge module's diff is EMPTY.

Update the gate pins in the SAME COMMIT for any suite whose test COUNT changed.
  </action>
  <verify>
    <automated>cd frontend; npx vitest run src/components/workflows/PhaseNodeCard.test.tsx src/components/workflows/WorkflowCanvas.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "still empty\|still reserved\|reserved for" frontend/src/components/workflows/` returns
      NO hit that still claims slot 1 is reserved.
    - All six re-derived prose sites are updated, and the SUMMARY lists each file with its before and
      after sentence.
    - For the three fenced-subtree files, `git diff` shows COMMENT-ONLY hunks — every changed line begins
      with comment syntax. State this explicitly in the SUMMARY.
    - The card-subtree path list still has SIX entries and its length pin is UNMOVED.
    - The card-suite guard has a two-badge positive control and still PASSES.
    - The canvas-suite guard asserts the not-connected attribute present on the new type and absent on all
      six others, and RETAINS its original grounding-chip absence assertion.
    - The unarmed-edge docblock no longer names this phase, and `git diff --stat` on the edge module is
      EMPTY.
    - `cd frontend; npx tsc --noEmit -p tsconfig.app.json` reads exactly 33.
    - The count gate reports OK with pins moved in-commit.
  </acceptance_criteria>
  <done>No claim in the tree still says slot 1 is reserved, both guards assert the new truth with controls, and the ghost edge stayed unbuilt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the projection's node data to the rendered card face | what the canvas asserts about a step must be true of that step |
| the fenced six-file card subtree to the rest of the workflows directory | seventeen negative source fences plus four haystacks read a six-path list; a file added without being listed leaves them covering less |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-44 | Tampering | a spread silently retiring the max-two badge typecheck guard | mitigate | Explicit branches only; an acceptance criterion greps for a spread in the tuple expression; the deliberate compile-error control is RE-OBSERVED swinging both ways after slot 1 is spent. |
| T-189-45 | Elevation of Privilege | a focusable control entering the card, breaking the one-tab-stop-per-node contract | mitigate | The badge is a span with no role, no tabindex and no handler. The card suite's leaf walk — driven RED against a planted button in a real destination module — must still pass. |
| T-189-30 | Tampering | the fenced card subtree losing coverage | mitigate | Every edit inside those files is COMMENT-ONLY, verified line by line in the diff; the path list stays at six and its length pin does not move; no file is added to that directory. |
| T-189-46 | Spoofing | a badge asserting something untrue of the step it sits on | mitigate | The badge is driven ABSENT on all six shipped types, asserted individually. The predicate keeps its type test and state test on separate lines so Phase 190 retires it by DATA. |
| T-189-38 | Repudiation | six shipped claims and two test titles asserting a reservation that no longer exists | mitigate | All six are flipped with before/after recorded, and both guards are REWRITTEN with positive controls rather than deleted. A deleted guard is coverage nobody notices losing. |
| T-189-47 | Tampering | building an unreachable ghost-detour edge from a stale docblock (**D-21**) | mitigate | The docblock is corrected and the edge module's diff is required to be EMPTY. The state is unreachable by construction and the projection already cannot produce it. |
| — | Information Disclosure / Denial of Service | — | n/a | **No new attack surface.** This plan adds a presentational label and corrects comments. No route, no credential, no request. Stated with its reason. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs nothing. No install task exists in this phase, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `cd frontend; npx vitest run src/components/workflows` — no NEW failures relative to the HEAD baseline;
  any pre-existing failure is named in the SUMMARY.
- `cd frontend; npx tsc --noEmit -p tsconfig.app.json` reads exactly 33, with the third-badge control
  re-observed swinging.
- `node scripts/vitest-count-gate.cjs` reports OK, no per-file decrease, pins moved in-commit.
  ⚠ COUNT columns, not the `failed` column.
- `git diff --stat` over the six fenced card-subtree files shows COMMENT-ONLY changes in exactly three of
  them and nothing in the rest.
- ⚠ **jsdom proves none of the visual claims here.** Whether the badge occludes or is occluded by its
  neighbours, and whether it pushes the card past its height budget, are U2 and U4 — driven Chrome MCP
  rows in plan 189-16.
</verification>

<success_criteria>
- The last free badge slot is spent on the transient fact, ordered first, and retires by data.
- The three card invariants hold and two of them were re-observed rather than assumed.
- Six prose claims and two guards now describe the tree as it is.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-15-SUMMARY.md` when done.
Record: the six prose sites with before and after, the re-observed third-badge control's two error
counts, proof the fenced-file edits were comment-only, and confirmation the edge module's diff is empty.
</output>
