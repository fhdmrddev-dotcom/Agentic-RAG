---
phase: 189-governed-external-action-node-model
plan: 10
type: execute
wave: 4
depends_on: ["189-08"]
files_modified:
  - frontend/src/components/workflows/runVocabulary.ts
  - frontend/src/components/workflows/NodeRunOverlay.tsx
  - frontend/src/components/workflows/runVocabulary.test.ts
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "D-16: the canvas word for the new reading is exactly 'Not sent — recorded', and it lives in RUN_READING_WORD — the canvas vocabulary layer, 400 lines and one language away from the database slug (D-17)."
    - "D-07: the new word collides with none of the seven shipped readings, and specifically not with Running and not with the waiting word. It also does not read as success, as failure, or as skipped."
    - "The eighth ring reading is identifiable BY SHAPE ALONE IN GREYSCALE — it is the only reading drawn in FOUR arcs, and a test counts the dash/gap pairs rather than eyeballing them. That is the shipped BUILD CRITERION, not a preference."
    - "The eighth ring does NOT animate. Only the running reading moves, and that is running's own uniqueness property — so the separation from running is by arc COUNT, which survives prefers-reduced-motion."
    - "Colour REINFORCES and never carries: the stroke is the calm muted token that skipped and unknown already share, because success, destructive, warning and primary would each make a false claim."
    - "RUN_READING_BORDER is deliberately left UNSET for the new reading — only the three loud readings claim the card border — and the absence is RECORDED in the table's docblock rather than left to be discovered."
    - "The whole ring table tiles the circle exactly (repeats times dash-plus-gap equals 1) — true of all seven shipped rows and asserted NOWHERE until this plan asserts it over the whole table."
  artifacts:
    - path: "frontend/src/components/workflows/runVocabulary.ts"
      provides: "RUN_READING_WORD (D-16), STATIC_CLAUSE, and the 8th RING_GEOMETRY row"
      contains: "recorded-not-sent"
    - path: "frontend/src/components/workflows/NodeRunOverlay.tsx"
      provides: "the RING_STROKE row — the ONE typecheck-forced edit inside the fenced card subtree"
      contains: "recorded-not-sent"
  key_links:
    - from: "frontend/src/lib/phaseState.ts CanvasReading"
      to: "the four exhaustive Record tables"
      via: "the widened union making each a typecheck error until filled"
      pattern: "CanvasReading"
    - from: "RING_GEOMETRY recorded-not-sent"
      to: "the rendered stroke-dasharray"
      via: "ringDash computing four dash/gap pairs"
      pattern: "stroke-dasharray"
---

<objective>
Give the new terminal its canvas word and its ring shape.

Purpose: plan 189-08 widened `CanvasReading`, which made four exhaustive `Record<CanvasReading, ...>`
tables a TYPECHECK ERROR. Three of them are a one-line fill. The fourth, `RING_GEOMETRY`, is NOT a table
fill: its docblock states a BUILD CRITERION — *the arc geometry IS the state; colour only ever
reinforces it* — and enumerates the seven uniqueness properties that make every reading identifiable
with colour switched off. An eighth reading owes an eighth distinguishable shape, and there is no
shipped precedent for one because the obvious design space is already occupied.

⚠ **This phase is G-2-EXEMPT** — the ROADMAP names 189 as "the design/decision phase" — so there is no
sketch to defer to. The shape is a PLAN DECISION, already taken in `189-UI-SPEC.md` section 4b with its
reasoning, and it owes a DRIVEN greyscale UAT row (U3, in plan 189-16). Implement the specified shape;
do not re-derive it, and do not substitute a "simpler" one.

Output: three forced table rows, one forced stroke row, one deliberate omission, and a whole-table
invariant nobody has asserted before.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md
@.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md
@CLAUDE.md

RE-DERIVE EVERY ANCHOR by searching for the identifier.

BASELINES to re-derive before you start: `cd frontend; npx tsc --noEmit -p tsconfig.app.json` = **33**
(a bare `--noEmit` checks ZERO files) · `node scripts/vitest-count-gate.cjs` = **total 2508, 45/45**.
⚠ Do NOT read the gate's `failed` column as a regression backstop — it is rot on this machine. The
COUNT columns are the backstop.

⚠ **`NodeRunOverlay.tsx` IS INSIDE THE FENCED SIX-FILE CARD SUBTREE.** It is the ONE exception this
phase makes, and only for the typecheck-forced `RING_STROKE` row. `CARD_SUBTREE_PATHS` must STAY at six
entries and its length pin must not move. If you find yourself adding a file to that directory, stop.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The D-16 word and the D-07 non-collision — RUN_READING_WORD and STATIC_CLAUSE</name>
  <files>frontend/src/components/workflows/runVocabulary.ts, frontend/src/components/workflows/runVocabulary.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/runVocabulary.ts` — search `RUN_READING_WORD` and read the
      seven shipped words. Then search `STATIC_CLAUSE` and read its docblock: it is declared as an
      exhaustive `Record` rather than a switch with a `default:` arm DELIBERATELY, so that an eighth
      reading becomes a TYPECHECK ERROR here where a `default:` would have silently absorbed it. **189
      is literally the "eighth reading added later" that sentence was written for.**
    - `frontend/src/components/workflows/runVocabulary.ts` — read the docblock near `RUN_READING_WORD`
      that states a FENCE DISCIPLINE: the badge's own words are deliberately NOT quoted in this file
      because an acceptance grep asserts their absence, and a docblock that spelled them would make that
      check vacuous. ⚠ **Therefore "Not connected" (D-18) must NOT be written into `runVocabulary.ts`
      prose either.**
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` sections 3a, 3c and 3d —
      the word, the reason the clause is `null`, and the full non-collision table.
    - `frontend/src/pages/WorkflowRunPage.tsx` — search `readBand` and read the run-band words, so you
      can see (and NOT change) the accepted divergence: the band reads a RUN status while the node reads
      a PHASE status.
  </read_first>
  <action>
Fill the two forced word tables.

A — `RUN_READING_WORD` gains the row for the new reading with the value **exactly**
`Not sent — recorded` (em-dash, D-16, the operator's locked wording). Outcome first, consolation
second: it cannot be misread as success, and "recorded" says where the detail went.

B — `STATIC_CLAUSE` gains the row with the value `null`, and the reason goes in the table's comment:
`STATIC_CLAUSE`'s own rule is that a clause is carried by the readings a person cannot act on from the
word alone. There is nothing for the person to do here — the approval already happened and the run
continued — and the D-16 word already contains its own clause after an em-dash, so appending another
would produce a two-em-dash sentence restating what the badge and the output body both carry.

C — `RUN_READING_BORDER` is a `Partial<Record<...>>` and is **deliberately left UNSET** for the new
reading. Only the three loud readings (running, waiting-for-you, failed) claim the card border; the
quiet ones stay quiet so the loud two keep pulling the eye. **RECORD the absence in the table's
docblock beside the existing ones** — it is an editorial decision, not an omission, and an unrecorded
omission is indistinguishable from a bug.

Extend the vocabulary suite with the D-07 NON-COLLISION assertions, and get the assertion style right
because it is a measured trap:

- The new word is NOT equal to `Complete`, NOT equal to `Running`, and NOT equal to the waiting word.
  Those three are D-07's binding constraint (running and waiting-for-you may never share a word, and
  the new word must collide with neither).
- The new word is NOT equal to `Failed` and NOT equal to `Skipped` — nothing failed, and the step DID
  run with a human approving, so reading as skipped would lie in a different direction.
- ⚠ **USE EXACT-MATCH ASSERTIONS.** The new word SHARES THE PREFIX "Not " with the shipped
  `Not started`. A test asserting `.toContain("Not")` becomes ambiguous and would pass while proving
  nothing. Assert equality and inequality on whole strings.
- All eight `RUN_READING_WORD` values are pairwise DISTINCT — assert it as a set-size property over the
  whole table rather than as seven comparisons, so a ninth reading inherits the guard.

Update `scripts/vitest-count-gate.cjs` `BASELINE` for every changed suite in the SAME COMMIT.
  </action>
  <verify>
    <automated>cd frontend; npx vitest run src/components/workflows/runVocabulary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `RUN_READING_WORD` for the new reading is byte-exactly `Not sent — recorded`, em-dash included.
    - `STATIC_CLAUSE` for the new reading is `null`, with the reason in the table's comment.
    - `RUN_READING_BORDER` has NO entry for the new reading, and its docblock records the deliberate
      absence.
    - The pairwise-distinct assertion over all eight words PASSES.
    - `grep -c "Not connected" frontend/src/components/workflows/runVocabulary.ts` returns 0 — the
      badge's words stay out of this file so the shipped acceptance grep does not go vacuous.
    - No `.toContain("Not")`-style assertion exists in the new tests — verify by grep.
    - `git diff` on `RUN_READING_WORD` shows only an ADDED row; no shipped word changed.
    - The count gate reports OK with pins moved in the same commit.
  </acceptance_criteria>
  <done>The canvas has one new honest word that cannot be confused with the seven it joins, asserted exactly rather than by substring.</done>
</task>

<task type="auto">
  <name>Task 2: The eighth ring — four arcs, still, muted, and a whole-table tiling invariant</name>
  <files>frontend/src/components/workflows/runVocabulary.ts, frontend/src/components/workflows/NodeRunOverlay.tsx, frontend/src/components/workflows/runVocabulary.test.ts, frontend/src/components/workflows/PhaseNodeCard.test.tsx, frontend/src/pages/WorkflowRunPage.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/runVocabulary.ts` — search `RING_GEOMETRY` and read its FULL
      docblock. It states the BUILD CRITERION verbatim and enumerates all seven uniqueness properties:
      not-started is the only one with NO arc; running is the only one that MOVES and statically the
      only short single arc; done is the only CLOSED unbroken ring; failed is the only ring snapped into
      TWO arcs; skipped is evenly dashed all the way round and COARSE; waiting-for-you is the only ring
      with ONE wide gap at 12 o'clock; unknown is the only DOTTED ring, fine and sparse.
    - `frontend/src/components/workflows/runVocabulary.ts` — search `ringDash` (or the function that
      computes `stroke-dasharray` / `stroke-dashoffset` from a `RingSpec`) and `ringSpecFor` (the TOTAL
      lookup that falls back to the unknown ring, own-guarded).
    - `frontend/src/components/workflows/NodeRunOverlay.tsx` — search `RING_STROKE`. This is the ONE
      edit this phase makes inside the fenced card subtree.
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — search `CARD_SUBTREE_PATHS` and the
      assertion pinning its LENGTH at six. ⚠ Its location was mis-cited by RESEARCH by roughly 160
      lines; PATTERNS corrected it. **Search for the identifier.** The list must stay at six.
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` section 4 in full —
      4a (the seven shipped shapes with their rendered numbers), 4b (the eighth shape, its computed
      dasharray and dashoffset, and its separation from each of the seven), 4c (the stroke), 4d (the
      border omission).
  </read_first>
  <action>
A — `RING_GEOMETRY` gains ONE row for the new reading, with EXACTLY the specified shape: a `fraction`
arc with `dash` 0.15, `gap` 0.10, `repeats` **4**, `gapCentre` **0.125**, and `spinning` **false**.

Do not substitute a different shape. The reasoning is already recorded and each clause of it is load-
bearing:
- **Its assertable unique property is that it is the ONLY reading drawn in FOUR arcs.** Every other
  fraction row names one or two dash/gap pairs, solid emits none, and the two length rows emit an
  unbounded texture. A test counts the pairs — a machine check, not an eyeball.
- **`repeats: 4` and not 3** because every shipped row tiles the circle EXACTLY, so no seam appears at
  the path start; three repeats need dash-plus-gap of one third, which is not expressible in the
  two-decimal style the table uses.
- **`gapCentre: 0.125`** because it is unclaimed (null, 0.375 and 0.75 are taken), it puts the four gaps
  on the 45-degree diagonals so none sits on a cardinal point, and it keeps the computed dashoffset
  POSITIVE like every shipped row.
- **`spinning: false`** because the run is over for this phase, and movement is `running`'s own
  uniqueness property. This is also why the separation from `running` is by arc COUNT — that
  distinction survives `prefers-reduced-motion`, when the spin is off and running's property is
  unavailable.
- **Not a near-closed ring with one small notch**, which was rejected: it maximises confusability with
  `done`, the one reading this must be most distinct from. Forty percent of the ring is missing here, in
  four visible gaps.

Write the row's comment in the docblock's own voice, adding the eighth bullet to the enumerated
uniqueness list so the criterion stays complete rather than becoming seven-of-eight.

B — `NodeRunOverlay.tsx` `RING_STROKE` gains ONE row: the calm muted foreground token that `skipped` and
`unknown` already share. Record the reason in a comment: colour REINFORCES, it never carries. Success
would claim success — the confusion this whole phase exists to prevent. Destructive would claim failure,
and nothing failed. Warning means needs-you and would say the person is still required when the person
has already answered. Primary means live. A duplicate token is established precedent in this table
because the SHAPE carries the distinction.

⚠ **This is the ONLY edit this phase makes inside the fenced card subtree.** Nothing else in
`NodeRunOverlay.tsx` changes, and no file is added to that directory.

C — THE WHOLE-TABLE TILING INVARIANT. Add a test asserting that for EVERY `fraction` row,
`repeats * (dash + gap)` equals 1. This is currently true of all seven shipped rows and is asserted
NOWHERE. Assert it over the table, not over the new row — a property nobody stated is a property the
next reading can silently break.

D — THE ARC-COUNT UNIQUENESS TEST. For all eight readings, compute the rendered `stroke-dasharray` and
assert the new reading is the ONLY one producing four dash/gap pairs. Then assert the eight readings'
rendered geometry strings are pairwise DISTINCT. ⚠ **jsdom cannot prove greyscale distinguishability** —
it applies no CSS and paints nothing. This test proves the ATTRIBUTE-level distinction; the visual half
is U3, a driven Chrome MCP row in plan 189-16. Say that in the test's comment so nobody mistakes this
green for the visual proof.

E — Confirm `ringSpecFor` still falls back to the unknown ring for an unowned reading, and add a
`"constructor"` probe asserting the own-guarded lookup does not return an inherited member.

Update `scripts/vitest-count-gate.cjs` `BASELINE` for every changed suite in the SAME COMMIT.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.app.json` reads exactly **33** — the measured baseline. Record the
      intermediate count seen after 189-08 and before this plan filled the tables, as proof the four
      exhaustive Records actually forced.
    - `RING_GEOMETRY`'s new row has `dash` 0.15, `gap` 0.10, `repeats` 4, `gapCentre` 0.125,
      `spinning` false — verify each value by reading the diff.
    - The tiling invariant test PASSES over ALL eight rows.
    - The arc-count test PASSES and proves FOUR pairs is unique to the new reading.
    - The pairwise-distinct geometry assertion PASSES for all eight.
    - The `"constructor"` probe on `ringSpecFor` returns the unknown ring.
    - `RING_STROKE` gains exactly one row and `git diff --stat frontend/src/components/workflows/NodeRunOverlay.tsx`
      shows a diff of fewer than 8 changed lines.
    - `CARD_SUBTREE_PATHS` still has SIX entries and its length pin is UNMOVED — verify by grep and by
      `git diff` showing no change to that list.
    - `git diff --name-only` shows NO other file inside the fenced card subtree changed:
      `PhaseNodeCard.tsx`, `phaseNodeCardContract.ts`, `ownProperty.ts`, `NodeCornerMarks.tsx` and
      `NodeIconWell.tsx` are untouched by this plan.
    - `node scripts/vitest-count-gate.cjs` reports OK with pins moved in the same commit.
  </acceptance_criteria>
  <done>The eighth reading has a shape that is unique in a property a test can assert, does not move, spends no new colour, and did not cost the fenced subtree a seventh file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a server-supplied phase status to a client lookup table | `ringSpecFor` and the word tables are keyed by a value that originates server-side |
| an object-literal table to an inherited property name | `constructor`, `toString` and `__proto__` are inherited, are never nullish, and a `?? fallback` does not fire for them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-23 | Spoofing | a run state that reads as success when it is not | mitigate | The new word is asserted NOT equal to `Complete`, and the new ring is asserted distinct from `done` by arc count. `ringSpecFor` still falls back to the UNKNOWN ring — never to the closed circle — for an unowned reading. Verdict surfaces fail closed. |
| T-189-24 | Tampering | prototype pollution through `RING_GEOMETRY[reading]` (**WR-04**) | mitigate | `ringSpecFor` is already own-guarded; this plan pins it with a `"constructor"` probe while the table grows. ⚠ `runVocabulary.ts` keeps its own module-private guard with a different second-parameter type, constrained by that file's own fences — **do not merge it with `ownProperty.ts`'s `own()`**; that boundary is explicitly recorded as untouched. |
| T-189-29 | Repudiation | a shipped BUILD CRITERION silently reduced to seven-of-eight | mitigate | The eighth uniqueness bullet is added to the docblock's enumerated list, and the tiling invariant plus the arc-count uniqueness are asserted over the WHOLE table rather than over the new row. |
| T-189-30 | Tampering | the fenced card subtree gaining a seventh file or losing fence coverage | mitigate | `CARD_SUBTREE_PATHS` must stay at six with its length pin unmoved, and an acceptance criterion requires the other five fenced modules to be untouched. Seventeen negative fences read that path list; a file added without being listed would leave them covering less. |
| — | Information Disclosure / Denial of Service / Elevation of Privilege | — | n/a | **No new attack surface.** This plan adds presentational table rows. It opens no route, reads no credential, makes no request and gains no privilege. Stated with its reason rather than omitted. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs nothing — no npm package, no shadcn block, no registry fetch. No install task exists in this phase, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `cd frontend; npx vitest run src/components/workflows/runVocabulary.test.ts src/components/workflows/PhaseNodeCard.test.tsx src/pages/WorkflowRunPage.test.tsx`
- `cd frontend; npx tsc --noEmit -p tsconfig.app.json` reads exactly 33.
- `node scripts/vitest-count-gate.cjs` reports OK, 45/45, no per-file decrease, pins moved in-commit.
  ⚠ Read the COUNT columns; the `failed` column is not evidence.
- `git diff --name-only` confirms only `NodeRunOverlay.tsx` was touched inside the fenced subtree.
</verification>

<success_criteria>
- The new reading has an exact word, a null clause, no border claim, a four-arc shape and a muted stroke.
- Three whole-table invariants now exist that did not before: pairwise-distinct words, exact tiling,
  and pairwise-distinct rendered geometry.
- `tsc` back to 33; the fenced subtree still six files.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-10-SUMMARY.md` when done.
Record: the intermediate `tsc` count proving the four Records forced, the rendered `stroke-dasharray`
for all eight readings, and confirmation that `CARD_SUBTREE_PATHS` is unmoved at six.
</output>
