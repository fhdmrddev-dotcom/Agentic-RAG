---
phase: 189-governed-external-action-node-model
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CONNECTOR-ARCHITECTURE.md
  - .planning/prd-reset/DECISIONS.md
  - CLAUDE.md
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "SC#3 is satisfied: the milestone records the own-framework-vs-Open-Platform decision DURABLY — a doc under docs/ that survives milestone archiving, plus a citable D-entry in the register CLAUDE.md points at."
    - "D-10: the D-entry POINTS AT the doc and does NOT restate it — it copies D-v3.4-01's pointer shape, never D-PRD-15's full-ADR shape, and its closing sentence names the doc as the home of the full text."
    - "D-11: the EXISTING verdict is RECORDED, not re-validated — MCP-first, first-party-thin, broad catalog sequenced with Open Platform (SEED-013/014) — and it carries a DATED re-open trigger naming what would overturn it."
    - "The doc is discoverable rather than orphaned: all five shipped docs/ files are named from CLAUDE.md and the sixth is too, in the same commit."
    - "SC#3 is independent of every line of 189 code, so a late code problem cannot cost the phase a success criterion."
  artifacts:
    - path: "docs/CONNECTOR-ARCHITECTURE.md"
      provides: "the own-framework-vs-Open-Platform verdict, its three-source basis, and the dated re-open trigger"
      contains: "MCP-first"
    - path: ".planning/prd-reset/DECISIONS.md"
      provides: "D-v3.6-01 — a POINTER entry to the doc"
      contains: "D-v3.6-01"
    - path: "CLAUDE.md"
      provides: "the pointer line that makes the sixth docs/ file discoverable"
      contains: "CONNECTOR-ARCHITECTURE"
  key_links:
    - from: ".planning/prd-reset/DECISIONS.md"
      to: "docs/CONNECTOR-ARCHITECTURE.md"
      via: "the closing 'this is a pointer; the full text lives in the doc' sentence"
      pattern: "CONNECTOR-ARCHITECTURE"
    - from: "CLAUDE.md"
      to: "docs/CONNECTOR-ARCHITECTURE.md"
      via: "a named reference beside the other five docs/ files"
      pattern: "CONNECTOR-ARCHITECTURE"
---

<objective>
Ship SC#3 — "the milestone records the own-framework-vs-Open-Platform decision durably" — as a
`docs/` architecture doc plus a pointer entry in the D-numbered register.

Purpose: SC#3 is genuinely independent of all the code in this phase, and it sits in wave 1
deliberately so that a late problem in the engine, the migration or the canvas cannot cost the phase a
success criterion.

This plan RECORDS a verdict. It does not RE-VALIDATE one (D-11). The 2026-07-24 competitor crawl plus
SEED-013 / SEED-014 already reached the answer, and that research is on file. Re-running it is scope
this phase did not buy.

Output: one new doc, one new register entry, one CLAUDE.md pointer line.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@CLAUDE.md

RE-DERIVE EVERY ANCHOR. `.planning/prd-reset/DECISIONS.md` measured 1424 lines on 2026-08-07.
Find `D-v3.4-01` and `## Cross-references` by SEARCHING for those strings, never by line number.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/CONNECTOR-ARCHITECTURE.md — the verdict, its basis, and a dated re-open trigger</name>
  <files>docs/CONNECTOR-ARCHITECTURE.md</files>
  <read_first>
    - `docs/SANDBOX-PACKAGES.md` — the exact convention model. Its shape: SCREAMING-KEBAB filename,
      NO YAML front matter, a single H1, `##` sections, and an explicit "Why this doc exists" blockquote
      naming a dated incident.
    - `docs/DEPLOYMENT-WORKFLOW.md` — confirm the conventions hold across the set (measured: all five
      files in `docs/` share them).
    - `.planning/seeds/SEED-013-external-integrations-api-mcp.md` — the three consumer modes (REST API;
      MCP where WE are the SERVER and Claude Desktop / Cursor / Cline are the clients; webhooks), and
      its own v2.5 audit line recording that no MCP code exists anywhere in the repo — the `.mcp.json`
      servers are consumed by Claude Code, not served by this app.
    - `.planning/seeds/SEED-014-automations-routines.md` — the binding sequencing note ("do not plan
      automations as a separate primitive — plan them as a runtime mode for skills") and its
      relates_to link to SEED-013.
    - `.planning/research/deep-dive/` — list it. Measured: exactly three files, BEAM.md, GLEAN.md, N8N.md.
    - `.planning/REQUIREMENTS.md` — CONN-01 verbatim, and CONN-02 / CONN-03 for what Phase 190 owns.
  </read_first>
  <action>
Create `docs/CONNECTOR-ARCHITECTURE.md` matching the `docs/` conventions exactly: SCREAMING-KEBAB
filename, no YAML front matter, one H1, `##` sections.

Required content:

- H1 plus a one-paragraph "what this is" — the canonical, human-readable home of the
  own-framework-vs-Open-Platform verdict for external integrations.
- A "Why this doc exists" blockquote in the SANDBOX-PACKAGES.md shape: the decision was reached by the
  2026-07-24 competitor crawl and was living only in seeds and research notes, where a decision buried
  in a seed reads as still-pending. That is D-10's rejected alternative, stated as the reason this file
  exists.
- A "The verdict" section — MCP-first, first-party-thin, broad catalog sequenced with Open Platform
  (SEED-013/014). Spell out each clause: MCP-first means building against the Model Context Protocol
  rather than hand-rolling a bespoke connector framework; first-party-thin means a deliberately small
  first-party set (Phase 190's email / JIRA / Slack slice), not a catalog; broad catalog sequenced with
  Open Platform means breadth defers to the Open Platform milestone (SEED-013 and its sibling
  SEED-014), not forked into v3.6.
- A "What this decision is based on" section — name the sources by path: the three deep-dive files,
  plus SEED-013 and SEED-014, plus the crawl date. Record explicitly that SEED-031 is the LLM-provider
  seed, NOT connectors — both CONTEXT and the ROADMAP say so, and citing it would be a category error
  the next reader inherits.
- A "What it does NOT decide" section — the credential model, org scoping, who may extend the
  capability set, and the SSRF / egress allow-list are all CONN-03 / Phase 190. 189 ships zero egress.
- A "The re-open trigger" section, DATED and CONCRETE (D-11). At minimum: (a) MCP specification churn
  that breaks the client contract we would build against; (b) a real connector need that no MCP server
  covers, forcing a first-party integration outside the thin set; (c) the Open Platform milestone
  slipping past the point where breadth is commercially needed. Give each a one-line OBSERVABLE so a
  future reader can tell whether it has fired, and date the trigger to this plan's execution date.
- A "Where the governed node model lives" section — a short pointer to Phase 189's `external_action`
  phase type and the fact that its capabilities record intent and send nothing; live egress is 190.

Write the verdict as a RECORD, not as an argument. Do not re-crawl a competitor, do not add a
comparison matrix, do not justify from first principles. If you are arguing, you have exceeded D-11.
  </action>
  <verify>
    <automated>grep -c "MCP-first" docs/CONNECTOR-ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/CONNECTOR-ARCHITECTURE.md` exists; its FIRST line begins with a single `#` and is NOT `---`
      (no YAML front matter — verified with `head -1`).
    - `grep -c "MCP-first"` returns at least 1; `grep -c "SEED-013"` at least 1; `grep -c "SEED-014"`
      at least 1; `grep -ci "re-open trigger"` at least 1.
    - The three deep-dive filenames BEAM.md, GLEAN.md and N8N.md each appear at least once.
    - The doc contains a sentence recording that SEED-031 is the LLM-provider seed and NOT connectors.
    - The re-open-trigger section carries an explicit ISO date and at least three named triggers, each
      with an observable.
    - The doc contains no competitor scoring table — D-11 forbids re-validation.
  </acceptance_criteria>
  <done>The verdict has a durable, discoverable, dated home under `docs/`, written as a record.</done>
</task>

<task type="auto">
  <name>Task 2: Add D-v3.6-01 as a POINTER entry, plus the CLAUDE.md pointer line, in one commit</name>
  <files>.planning/prd-reset/DECISIONS.md, CLAUDE.md</files>
  <read_first>
    - `.planning/prd-reset/DECISIONS.md` — read the ID-convention paragraph near the top (search
      `D-PRD-NN`), then read `D-v3.4-01` in full (search `D-v3.4-01`). It is itself a POINTER entry that
      does not restate its doc, and it is the exact model D-10 requires. Also read the
      `## Cross-references` heading (search that string) — the new entry goes immediately BEFORE it,
      and a bullet is added INSIDE it.
    - `.planning/prd-reset/DECISIONS.md` — skim `D-PRD-15` (search `D-PRD-15`) so you can RECOGNISE the
      full-ADR shape (Context / Decision / Consequences subheadings) and deliberately NOT copy it.
    - `CLAUDE.md` — find the lines naming the five shipped `docs/` files (search `SANDBOX-PACKAGES`,
      `DEPLOYMENT-WORKFLOW`, `OPERATOR.md`) to see how a doc is pointed at.
  </read_first>
  <action>
A — THE REGISTER ENTRY. Insert a new section headed `## D-v3.6-01` into
`.planning/prd-reset/DECISIONS.md`, placed AFTER the `D-v3.4-01` section and BEFORE the
`## Cross-references` heading.

Copy `D-v3.4-01`'s POINTER shape exactly: the heading with a short title; a
`**Status:** Ratified <ISO date>` and `**Type:** Per-milestone architectural decision (v3.6).` header
pair; two or three short paragraphs saying WHAT is locked (MCP-first, first-party-thin,
Open-Platform-sequenced) and which requirement it discharges (CONN-01, SC#3); and a closing sentence
in D-v3.4-01's own form stating that this is a pointer and that the full text lives in
`docs/CONNECTOR-ARCHITECTURE.md`.

D-10 forbids restating the doc. Do NOT reproduce the basis, the alternatives or the re-open trigger
here — name the doc and stop. `D-v3.6-01` is the correct id: the convention is that per-milestone
architectural decisions keep a `D-vX.Y-NN` prefix tied to the milestone they were locked in, and
`D-v3.4-01` is currently the only such entry in the file. VERIFY no other `D-v3.6-` entry already
exists before writing one.

Then add a bullet to the existing `## Cross-references` list pointing at the new entry.

B — THE CLAUDE.md POINTER LINE, IN THE SAME COMMIT. All five shipped `docs/` files are named from
`CLAUDE.md`; a sixth that is not is an orphan. Add ONE short pointer line naming
`docs/CONNECTOR-ARCHITECTURE.md` and what it holds, placed with the other doc references, following
the SANDBOX-PACKAGES.md precedent (one clause, not a paragraph).

Do not restructure `CLAUDE.md`. One added line. It is a binding project-rules file and a reflowed
section is a diff nobody can review.
  </action>
  <verify>
    <automated>grep -c "D-v3.6-01" .planning/prd-reset/DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-v3.6-01" .planning/prd-reset/DECISIONS.md` returns at least 2 (the heading plus the
      Cross-references bullet).
    - The new section appears AFTER the `D-v3.4-01` section and BEFORE `## Cross-references` — verify
      with `grep -n` on all three strings and compare the line numbers.
    - The new section contains the substring `CONNECTOR-ARCHITECTURE.md` and a closing sentence naming
      the doc as the home of the full text.
    - The new section contains NO `### Context`, `### Decision` or `### Consequences` subheadings — that
      is `D-PRD-15`'s full-ADR shape and D-10 forbids it here.
    - `grep -c "CONNECTOR-ARCHITECTURE" CLAUDE.md` returns at least 1.
    - `git diff --stat CLAUDE.md` shows a small edit — 1 to 3 changed lines, not a reflow.
    - `git diff --stat` for this task lists exactly `CLAUDE.md` and `.planning/prd-reset/DECISIONS.md`.
  </acceptance_criteria>
  <done>The verdict is citable by D-number, the pointer does not restate the doc, and the doc is discoverable from CLAUDE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan writes three documentation files. It adds no route, no input parser, no data path and no executable code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-08 | Information Disclosure | `docs/CONNECTOR-ARCHITECTURE.md` is a repository-visible file | accept | The doc records an architecture verdict already present in `.planning/` seeds and research. It must name NO credential, NO endpoint, NO API key and NO internal hostname — CONN-03's credential model is explicitly out of scope and is Phase 190's. Acceptance criteria forbid a competitor scoring table; a reviewer should also confirm no secret-shaped string appears. |
| T-189-09 | Tampering | `CLAUDE.md` is a binding project-rules file read by every agent | mitigate | The change is capped at ONE added pointer line and the acceptance criterion is a 1-to-3-line `git diff --stat`. A reflow of this file would silently alter rules that gate migrations, deploys and guardrails. |
| — | S/R/D/E | — | n/a | **No new attack surface.** 189 as a whole ships no outbound egress (SC#4), and this plan in particular ships no code at all. That is stated with its reason rather than omitted. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs nothing (`189-RESEARCH.md`, Package Legitimacy Audit: no new dependency anywhere in the phase). No install task exists, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `head -1 docs/CONNECTOR-ARCHITECTURE.md` starts with a single `#`, not `---`.
- `grep -n "D-v3.4-01" .planning/prd-reset/DECISIONS.md` and `grep -n "D-v3.6-01" ...` and
  `grep -n "## Cross-references" ...` show the new entry sits strictly between them.
- `git diff --stat` for the whole plan lists exactly three files and no source file.
- No test suite is affected: the frontend count-gate total stays at its measured baseline of 2508 and
  `npx tsc --noEmit -p tsconfig.app.json` stays at 33. Neither needs re-running for this plan, but
  neither may move because of it.
</verification>

<success_criteria>
- SC#3 is satisfied and is verifiable by three greps.
- D-10 is honoured mechanically: the register entry names the doc and carries none of its content.
- D-11 is honoured: nothing was re-validated, and the re-open trigger is dated and observable.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-03-SUMMARY.md` when done.
Record: the doc's section headings, the exact `D-v3.6-01` heading text, and the one CLAUDE.md line added.
</output>
