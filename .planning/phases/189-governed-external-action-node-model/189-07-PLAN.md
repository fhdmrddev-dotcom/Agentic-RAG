---
phase: 189-governed-external-action-node-model
plan: 07
type: execute
wave: 3
depends_on: ["189-01", "189-04"]
files_modified:
  - backend/app/models/harness.py
  - backend/tests/unit/test_189_external_action_model.py
  - backend/tests/unit/test_harness_models.py
  - backend/tests/unit/test_185_detection.py
  - frontend/src/components/workflows/phaseVocabulary.ts
  - frontend/src/components/workflows/GovernanceSection.tsx
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "D-01: external_action is the SEVENTH member of the PhaseConfig discriminated union, appended additively. The discriminator, extra='forbid' and the union structure are untouched, and every pre-189 stored JSONB row still model_validate()s."
    - "D-15: the capability is a Literal of exactly three — send_email, create_ticket, post_message — and a fourth name raises. The Literal's members are fenced EQUAL to grounding.EXTERNAL_ACTION_CAPABILITIES by a test, because two spellings of one closed set is the drift shape this project keeps getting bitten by."
    - "D-03: the capability IS an entry in available_tools, derived from the capability by the model itself so the two can never disagree — one fact, one derivation, no drift."
    - "D-04: action_risk_armed is STRUCTURALLY TRUE on this type and is not disarmable. A model_validator on PhaseSpec COERCES it to True, catching all six write paths at once because every one goes through WorkflowDefinition.model_validate."
    - "D-04 COERCES rather than RAISES, and that departure from the two shipped validators is stated in the docblock — a new row written with armed:false by a stale client must not 422 and brick the workflow. Coercion is fail-CLOSED and never destroys a row."
    - "D-26: the two shipped docblocks justifying the grounding gate with 'available_tools exists only on LlmAgentPhaseConfig and LlmBatchAgentsPhaseConfig' are CORRECTED IN THE SAME COMMIT that falsifies them. The CONSTANT stays; only its stated REASON changes."
    - "D-03 verified: the three capability names are DISJOINT from KB_TOOLS, so grounding_cause returns None and an external_action step is free to think by construction, carrying no governance seal. That is correct, not a bug."
  artifacts:
    - path: "backend/app/models/harness.py"
      provides: "ExternalActionPhaseConfig, the 7th union member, and the PhaseSpec model_validator pinning action_risk_armed"
      contains: "external_action"
  key_links:
    - from: "backend/app/models/harness.py ExternalActionPhaseConfig.capability"
      to: "backend/app/services/harness/grounding.py EXTERNAL_ACTION_CAPABILITIES"
      via: "a test asserting the Literal's args equal the frozenset exactly"
      pattern: "EXTERNAL_ACTION_CAPABILITIES"
    - from: "backend/app/models/harness.py PhaseSpec model_validator"
      to: "action_risk_armed"
      via: "coercion to True on an external_action phase, on every write path"
      pattern: "action_risk_armed"
---

<objective>
Add the seventh `phase_type` and make its two governance properties STRUCTURAL rather than
conventional.

Purpose: D-01 says this is the trodden path, not an invention — the module docblock records that the
`phase_type` Literal set has grown additively from 5 to 6 and always has, and the sixth member's own
docblock calls itself "the standard extension". What is NOT routine is D-04: `action_risk_armed` must
be true on this type in a way an author, a stale client, or a hand-edited JSONB row cannot undo. SC#2
says the node "cannot be wired around a gate", and the hard-won 185 rule is that a gate which can be
loosened away is not a gate.

Output: one new config class, one union member, one `model_validator`, one cross-module agreement
fence, and two same-commit prose corrections that D-03 falsifies the moment it lands.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md
@.planning/phases/189-governed-external-action-node-model/189-01-SUMMARY.md
@CLAUDE.md

RE-DERIVE EVERY ANCHOR by searching for the identifier. Interpreter: `backend/venv/Scripts/python.exe`.

This plan turns plan 189-01's RED cases GREEN. Read `189-01-SUMMARY.md` for the verbatim REDs first —
a green whose RED was never seen proves nothing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExternalActionPhaseConfig — the 7th union member, with available_tools derived from the capability</name>
  <files>backend/app/models/harness.py, backend/tests/unit/test_189_external_action_model.py, backend/tests/unit/test_harness_models.py</files>
  <read_first>
    - `backend/app/models/harness.py` — read the module docblock (the additive-growth contract: the two
      Literal sets grow additively; growth is safe because an old row never names the new member; what
      must NOT change is an existing member's spelling, nor the discriminator, `extra="forbid"` or the
      union structure). Then read `LlmEmitPhaseConfig` in full — it is the 6th member and its docblock
      calls itself "the standard extension (the 5 existing members were added this way)". Then find the
      `PhaseConfig` union (search `Field(discriminator=`).
    - `.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md` section 1 — the
      verbatim `LlmEmitPhaseConfig` excerpt and the union excerpt.
    - `backend/app/services/harness/grounding.py` — search `EXTERNAL_ACTION_CAPABILITIES` (landed in
      plan 189-04) and `KB_TOOLS`.
    - `backend/app/api/runs.py` — search `resolve_phase_available_tools`, the server-side re-read at run
      time. And `backend/app/services/harness/phase_types.py` — search `_build_phase_tool_context` and
      `phase_whitelist`, which is where the whitelist is actually built for the main run path.
    - `backend/app/services/tool_dispatcher.py` — search `def dispatch_tool` and `phase_whitelist`; this
      is the ENFORCEMENT point D-03 rides, and the plan should name it rather than only naming
      `resolve_phase_available_tools`.
  </read_first>
  <action>
Add `ExternalActionPhaseConfig` to `backend/app/models/harness.py` as a `_StrictBase`, and append it to
the `PhaseConfig` union as the SEVENTH discriminated member. Append only — never reorder, never rename
an existing member (renaming one orphans every stored row that uses it), and do not touch the
discriminator, `extra="forbid"` or the union structure. Those are LOCKED mechanism.

Fields:
- `phase_type: Literal["external_action"]`
- `capability: Literal["send_email", "create_ticket", "post_message"]` — D-15, the operator's locked
  set. This is the closed vocabulary; a fourth name is a `ValidationError`, never a dynamic lookup.
- `available_tools: list[str]` — D-03. The capability IS an entry here, so it flows through the
  existing per-phase whitelist exactly like every other tool and SC#1's claim becomes literally true:
  the guard that already exists is the guard.
- Any optional shape-symmetry fields the family shares (mirror `LlmEmitPhaseConfig`'s treatment) — keep
  the family uniform, add nothing speculative.

Add a `model_validator(mode="after")` ON THE CONFIG that DERIVES `available_tools` from `capability`
so the two can never disagree: after validation `available_tools` must contain the chosen capability.
One fact, one derivation, no drift — the same one-definition rule D-03 cites. An author who submits an
emptied `available_tools` gets a phase that still declares its capability; an author who submits a
DIFFERENT tool alongside it is a decision you must make explicitly and record in the docblock (the
minimal reading of D-03 is that `available_tools` equals exactly `[capability]`).

Write the class docblock in `LlmEmitPhaseConfig`'s voice, and make it claim the same thing for the 7th:
appended as the 7th discriminated member, the standard extension; `_StrictBase` rejects unknown keys;
old JSONB phase rows without `external_action` still `model_validate()`. Add two facts that member does
not carry: (1) the capability is a STEP THE EXECUTOR PERFORMS, not a tool the LLM may call (D-22 — the
`render_template` precedent, registered for the whitelist but never advertised in `get_tools()`), and
(2) the three names are disjoint from `KB_TOOLS` by construction, so this step reads no knowledge base
and carries no governance seal.

⚠ **NO new `ValidatorSpec.kind` is needed.** D-04 reuses the shipped `action_risk_approval` kind, which
is already a member. Do not grow the second Literal set.

⚠ **NO `_TOOL_REGISTRY` entry and NO `get_tools()` schema.** Both are Phase 190's, and the second opens
the D-20 leak. `backend/app/services/openai_service.py` and `backend/app/services/tool_dispatcher.py`
must be untouched by this plan.

Then add the CROSS-MODULE AGREEMENT FENCE to `backend/tests/unit/test_189_external_action_model.py`:
read the `capability` Literal's members with `typing.get_args` and assert the resulting set EQUALS
`grounding.EXTERNAL_ACTION_CAPABILITIES` exactly. Two spellings of one closed set are unavoidable here
(Pydantic needs a literal; the frozenset is the runtime home), so the agreement must be MECHANICAL.
Observe it RED first by temporarily planting a fourth name in one spelling, and record that observation.
  </action>
  <verify>
    <automated>cd backend; venv/Scripts/python.exe -m pytest tests/unit/test_189_external_action_model.py tests/unit/test_harness_models.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - Plan 189-01's cases 1, 2, 3, 6, 7 and 8 now PASS; quote each RED-to-GREEN pair in the SUMMARY.
    - The `PhaseConfig` union names SEVEN members and `external_action` is LAST. `git diff` shows no
      reordering and no renaming of the six shipped members.
    - `grep -c "extra=" backend/app/models/harness.py` and the `_StrictBase` definition are unchanged
      by this diff.
    - The agreement fence PASSES, and the SUMMARY records the observed RED from the planted fourth name.
    - `grep -c "send_email" backend/app/services/openai_service.py` returns 0 and
      `grep -c "send_email" backend/app/services/tool_dispatcher.py` returns 0 — D-22 holds.
    - `grep -c "action_risk_approval" backend/app/models/harness.py` shows the validator-kind Literal
      set is UNCHANGED in length — no new kind was added.
    - A definition whose phases use only the six shipped types still validates (case 3 stays green).
  </acceptance_criteria>
  <done>The 7th member exists, its capability set is closed and mechanically agreed with its runtime twin, and the whitelist entry is derived rather than duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: D-04 — pin action_risk_armed structurally, and prove it cannot be stored false</name>
  <files>backend/app/models/harness.py, backend/tests/unit/test_189_external_action_model.py, backend/tests/unit/test_185_detection.py</files>
  <read_first>
    - `backend/app/models/harness.py` — search `action_risk_armed` on `PhaseSpec` (it is a plain
      `bool = False` SHARED BY ALL SEVEN TYPES, which is why a `Literal[True]` field cannot work), and
      read the `PhaseSpec` docblock passage arguing that a derived-at-read-time property makes a
      requirement true BY CONSTRUCTION rather than by code audit, so a stale or hand-edited JSONB row
      cannot lie about it. That is the argument D-04 reuses.
    - `backend/app/models/harness.py` — read BOTH shipped `@model_validator(mode="after")` methods on
      `WorkflowDefinition` (search `_folder_scope_requires_project`). ⚠ **Both RAISE. This one must
      COERCE**, and that departure must be stated in the docblock — copying a shape while inverting its
      disposition is exactly the silent divergence a reviewer cannot see.
    - `backend/app/services/harness_engine.py` — search `action_risk_armed` (the run-time pause reader)
      and `_is_armed_action_risk` (the boot-time resume sweep, deliberately INDEPENDENT and pinned by
      `test_the_two_resume_predicates_are_independent`). Neither may be coupled or edited here.
    - `frontend/src/components/workflows/phaseVocabulary.ts` — search `actionRiskArmed`, the third
      reader (the canvas edge state). Do not edit it in this task.
    - `.planning/phases/189-governed-external-action-node-model/189-01-SUMMARY.md` — the verbatim V06 RED.
  </read_first>
  <action>
Add a `model_validator(mode="after")` on `PhaseSpec` that PINS `action_risk_armed` to `True` whenever
`self.config.phase_type == "external_action"`.

**COERCE, do not RAISE.** The reasoning must go in the docblock, because it is a deliberate departure
from both shipped validators: an old row never names the new member, so growth is safe — but a NEW row
that a client wrote with `armed: false` before the client shipped its own pin would 422 the whole
definition and BRICK the workflow. Coercing to `True` is fail-CLOSED (it arms) and never destroys a
row. Log the coercion so it is observable rather than silent.

Why a `PhaseSpec` validator and not the alternatives, also for the docblock:
- A `Literal[True]` FIELD cannot work: `action_risk_armed` lives on `PhaseSpec`, shared by all seven
  types. Moving it onto the config would create a second home for one fact — the exact drift D-03 rejects.
- Enforcement at RESOLVE time (in the engine) protects the RUN but not the STORED ROW. A row could then
  read `armed: false` while the engine arms anyway, and the canvas — which reads the stored boolean —
  would paint an unarmed edge on an armed step. A lie on screen.
- A `PhaseSpec` validator catches ALL SIX write paths at once (the field default, a draft save, a
  canvas/panel edit, a direct JSONB hand-edit, the publish gauntlet, the NL generator) because every one
  of them goes through `WorkflowDefinition.model_validate`.

Take V06 (a definition with `armed: false` round-trips to `True`) and V09 to GREEN, and confirm the
NEGATIVE CONTROL from plan 189-01 still passes: an `llm_single` phase with `action_risk_armed: false`
still parses as `False`. Without that control, this pin could pass by arming everything, which would be
a far worse defect than the one it fixes.

Add one more case to `backend/tests/unit/test_185_detection.py`: assert `grounding_cause` returns None
for an `external_action` phase whose `available_tools` carries a capability — the disjointness from
`KB_TOOLS` means the grounding dial is NOT silently armed, and the missing governance seal is correct
rather than a bug. Assert on the SET of capabilities, not one name.
  </action>
  <verify>
    <automated>cd backend; venv/Scripts/python.exe -m pytest tests/unit/test_189_external_action_model.py tests/unit/test_185_detection.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - V06 PASSES for BOTH inputs (`armed: false` present, and the key absent). Quote the 189-01 RED and
      the new PASS side by side.
    - The negative control (an `llm_single` phase stays unarmed) STILL PASSES.
    - V09 PASSES.
    - The new `test_185_detection.py` case PASSES and asserts on the whole capability SET.
    - The validator's docblock contains the words COERCE and the reason it does not RAISE, and names all
      three rejected alternatives.
    - `git diff` shows NO hunk in `harness_engine.py` — neither the run-time reader nor
      `_is_armed_action_risk` was touched — and
      `cd backend; venv/Scripts/python.exe -m pytest tests/test_harness_engine.py -q` is unchanged from
      HEAD, including `test_the_two_resume_predicates_are_independent`.
    - The coercion emits a log line; the SUMMARY quotes it.
  </acceptance_criteria>
  <done>action_risk_armed is not a representable false on this type, on any write path, and the proof is a round-trip through the model rather than an audit of the engine.</done>
</task>

<task type="auto">
  <name>Task 3: D-26 — correct the two shipped docblocks that D-03 falsifies, in the same commit</name>
  <files>frontend/src/components/workflows/phaseVocabulary.ts, frontend/src/components/workflows/GovernanceSection.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/phaseVocabulary.ts` — search `GROUNDING_DIAL_TYPES` and read the
      docblock immediately above it. It justifies the two-type gate with the claim that `available_tools`
      exists ONLY on `LlmAgentPhaseConfig` and `LlmBatchAgentsPhaseConfig`.
    - `frontend/src/components/workflows/GovernanceSection.tsx` — search `DIAL_TYPES` and read its
      docblock. It re-types the same sentence.
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` — its CONFLICTS section
      item 4, which is where these two sites were FOUND. Neither CONTEXT nor RESEARCH found them.
  </read_first>
  <action>
D-03 puts `available_tools` on `ExternalActionPhaseConfig` too, which makes both docblocks FALSE the
moment Task 1 lands. Correct them here, in the same commit as the model change, because a stale reason
is how the next phase inherits a false claim — the drift class 188.2 found four times and PATTERNS
found three more times.

⚠ **THE CONSTANT MUST NOT CHANGE.** `GROUNDING_DIAL_TYPES` is READ and NEVER edited — a D-185-15 red
line — and the gate is still CORRECT: a third type carrying `available_tools` does not make it
grounding-capable, because its capabilities are disjoint from `KB_TOOLS`. **Do not add `external_action`
to `GROUNDING_DIAL_TYPES` or to `DIAL_TYPES`.**

Only the stated REASON changes. Replace "the only types with `available_tools`" with the true reason:
these are the only types whose tools can ever INTERSECT `KB_TOOLS`, which is what `grounding_cause` can
report `detected` for. Name Phase 189 and D-03 as what falsified the old wording, so the next reader can
see the history rather than guess at it.

Edit ONLY the two comment blocks. No code change, no constant change, no test change.
  </action>
  <verify>
    <automated>npx --yes --package=typescript@5 tsc --noEmit -p frontend/tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - Both docblocks no longer claim `available_tools` exists only on the two agent config types, and
      both name the true reason (intersection with `KB_TOOLS`) plus D-03 / Phase 189.
    - `git diff` on both files shows COMMENT-ONLY hunks — zero changed lines of executable code. Verify
      by inspecting the diff; every changed line begins with comment syntax.
    - `GROUNDING_DIAL_TYPES` and `DIAL_TYPES` are byte-identical to HEAD.
    - The frontend typecheck reads exactly 33 errors, the measured baseline (re-derived at plan time on
      2026-08-07). Use `-p tsconfig.app.json` — a bare `--noEmit` checks ZERO files.
    - The frontend count gate is not run for this task (no test count changes); if it is run it must
      still read total 2508 across 45 files.
  </acceptance_criteria>
  <done>No shipped docblock in the tree claims something D-03 has made false.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-supplied definition JSONB to `WorkflowDefinition.model_validate` | the ONLY chokepoint every write path shares; untrusted `capability`, `available_tools` and `action_risk_armed` values cross here |
| `config.available_tools` to `ToolContext.phase_whitelist` to `dispatch_tool` | the run-time enforcement point D-03 rides |
| the capability Literal to `grounding.EXTERNAL_ACTION_CAPABILITIES` | two spellings of one closed set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-01 | Elevation of Privilege | `PhaseSpec.action_risk_armed` on an `external_action` phase (**T2 — the D-04 disarm paths**) | mitigate | A `model_validator(mode="after")` on `PhaseSpec` COERCES the value to True. It catches the field default, draft save, panel edit, direct JSONB hand-edit, publish gauntlet and NL generator at once, because all six go through `model_validate`. Falsified by V06 round-tripping `armed: false` and asserting True, with an `llm_single` negative control so the pin cannot pass by arming everything. |
| T-189-02 | Tampering | dynamic resolution of an author-supplied `capability` | mitigate | `capability` is a `Literal` of exactly three; a fourth is a `ValidationError` at parse time, before the engine consumes it. `_StrictBase` with `extra="forbid"` is the codebase's own answer to the injected-key threat. |
| T-189-11 | Tampering | the Literal and the frozenset drifting apart | mitigate | A cross-module agreement test asserts `get_args` of the Literal EQUALS `EXTERNAL_ACTION_CAPABILITIES`, observed RED against a planted fourth name. |
| T-189-05 | Elevation of Privilege | a capability reaching `get_tools()` or `_TOOL_REGISTRY` (**T1 — the D-20 governance hole**) | mitigate | Acceptance criteria require `openai_service.py` and `tool_dispatcher.py` to contain zero occurrences of the capability names. D-22: the capability is a step the executor performs, and its precedent is `render_template` — honoured by the whitelist, invisible to the model. |
| T-189-12 | Spoofing | a capability silently arming the grounding dial | mitigate | A new `test_185_detection.py` case asserts `grounding_cause` returns None for an `external_action` phase, over the whole capability SET. `GROUNDING_DIAL_TYPES` is untouched (a D-185-15 red line). |
| T-189-22 | Denial of Service | a strict RAISE bricking a stored workflow | mitigate | The validator COERCES rather than raises, which is fail-closed (it arms) and never destroys a row. The reasoning is recorded in the docblock so a later reviewer does not "tighten" it into a brick. |
| T-189-13 | Repudiation | two shipped docblocks stating a reason D-03 falsified | mitigate | Task 3 corrects both in the SAME commit, comment-only, with the constant untouched. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs nothing. No install task exists in this phase, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `cd backend; venv/Scripts/python.exe -m pytest tests/unit/test_189_external_action_model.py tests/unit/test_harness_models.py tests/unit/test_185_detection.py tests/test_harness_engine.py -q --no-header`
- The nine-file 189-scope backend command from `189-RESEARCH.md` section D15: only the two named
  pre-existing `pool is closing` failures remain.
- `cd frontend; npx tsc --noEmit -p tsconfig.app.json` reads exactly 33 errors.
- `git diff --stat` lists the six declared files and nothing else. `openai_service.py`,
  `tool_dispatcher.py`, `harness_engine.py` and `threads.py` are all untouched.
</verification>

<success_criteria>
- The union has seven members and every pre-189 row still validates.
- `action_risk_armed: false` is not a representable stored value on this type, proven by round-trip.
- The capability set has one runtime home and one Literal, mechanically fenced equal.
- No docblock in the tree states a reason D-03 has falsified.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-07-SUMMARY.md` when done.
Record: every RED-to-GREEN pair from 189-01, the observed RED of the agreement fence against a planted
fourth name, the coercion log line, and the before/after of both corrected docblocks.
</output>
