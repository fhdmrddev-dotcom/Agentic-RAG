---
phase: 188-non-technical-run-observability
plan: 12
type: execute
wave: 10
depends_on: ["188-10"]
files_modified:
  - scripts/vitest-count-gate.cjs
  - .planning/phases/188-non-technical-run-observability/188-VALIDATION.md
autonomous: true
requirements: [RUNVIZ-01, RUNVIZ-02, RUNVIZ-03]

must_haves:
  truths:
    - "Every net-new suite this phase authored is PINNED, not merely run — TARGETS and BASELINE are two knobs and a suite in only the first can be gutted with the gate green"
    - "Every pin value is read from the script's own printed actual column across two agreeing runs, never hand-counted"
    - "At least one new pin is observed producing a [count-decrease] on a genuinely deleted it( block, then restored"
    - "The WorkflowCanvas.tsx diff cap is measured over the WHOLE PHASE, not just one plan's commit, and recorded"
    - "Zero migration files were added by this phase"
    - "The backend full suite is run once and non-attribution is proved by DIFFING the failure set against the Plan-01 baseline, never by a rollback run"
    - "The per-task verification map in 188-VALIDATION.md is filled in from measurement, not from assumption"
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "BASELINE entries for every suite this phase added or grew"
    - path: ".planning/phases/188-non-technical-run-observability/188-VALIDATION.md"
      provides: "The filled per-task map, the phase-gate results, and the recorded diffs"
  key_links:
    - from: "scripts/vitest-count-gate.cjs"
      to: "phaseState.test.ts / WorkflowRunPage.test.tsx / ChatLayout.launch.test.tsx"
      via: "BASELINE entries pinning what the phase added"
      pattern: "phaseState.test.ts"
---

<objective>
Pin what this phase built, and prove the gates rather than assert them.

Purpose: `TARGETS` and `BASELINE` are two knobs. Every earlier plan added its suite to `TARGETS` so it
RUNS; none pinned it, because a pin must be read from the script's printed `actual` column once the file
is final. Without this plan, every guard this phase authored could be deleted with the gate green —
verbatim the round-5 "verification truth 14" failure Phase 187 shipped. This plan also runs the phase
gates and records them where verification will read them.

Output: the final pins, an observed `[count-decrease]`, and a filled-in `188-VALIDATION.md`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/188-non-technical-run-observability/188-SPEC.md
@.planning/phases/188-non-technical-run-observability/188-VALIDATION.md
@.planning/phases/188-non-technical-run-observability/188-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin every suite this phase added or grew, and prove one pin bites</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` — the whole file, including the header's rule that pins come from the printed `actual` column and never from hand-counting `it(` literals, and the per-file rationale-comment house style.
    - The suites this phase authored or grew: `src/lib/phaseState.test.ts`, `src/pages/WorkflowRunPage.test.tsx`, `src/components/layout/ChatLayout.launch.test.tsx`, the panel seam suite chosen in Plan 10, plus the grown `src/components/workflows/PhaseNode.test.tsx`, `PhaseNodeCard.test.tsx`, `WorkflowCanvas.test.tsx` and `src/components/panel/__tests__/PhaseReconcile.test.tsx`.
  </read_first>
  <action>
Run `node scripts/vitest-count-gate.cjs` **twice**. Confirm the two runs agree on every per-file `actual`.
If they disagree, stop and record the flake by file — a pin taken from a flaky count is a pin that
cannot fire.

Add or raise `BASELINE` entries for every suite this phase authored or grew, using the printed `actual`
values. Where a suite already had a pin from Plan 01, raise it to the new measured value — never lower
it, and never leave a suite whose real count grew sitting on its old pin, because the slack is exactly
the room a future deletion hides in. (`WorkflowCanvas.test.tsx` began this phase pinned at 31 while
running 35; close that slack here.)

Give each new entry a one-line rationale comment in the house style naming the phase, the plan and what
the suite guards, so a future reader knows what a decrease would mean.

Leave `BASELINE_TOTAL` as the computed reduce.

Then **prove a new pin bites**: delete one whole `it(...)` block from `src/lib/phaseState.test.ts`, run
the gate, confirm it exits non-zero with `[count-decrease]` naming that file, paste the raw output into
the plan summary, restore with `git checkout --`, and confirm the gate is green again.
  </action>
  <acceptance_criteria>
    - `node scripts/vitest-count-gate.cjs` exits 0 twice with identical per-file `actual` values
    - `grep -c '"phaseState.test.ts"' scripts/vitest-count-gate.cjs` returns 1; the same for `"WorkflowRunPage.test.tsx"` and `"ChatLayout.launch.test.tsx"`
    - `"WorkflowCanvas.test.tsx"`'s pin equals its printed `actual` — no remaining slack
    - The printed `N/N pinned files present` count equals the number of `BASELINE` entries
    - The plan summary contains the raw `[count-decrease]` output naming `phaseState.test.ts`, and `git status --porcelain frontend/src/lib/phaseState.test.ts` is empty afterwards
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Every guard this phase wrote is pinned at its measured count, and one pin was watched failing.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase gates and record every result</name>
  <files>.planning/phases/188-non-technical-run-observability/188-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/188-non-technical-run-observability/188-VALIDATION.md` — the `## Measured baselines` section recorded by Plan 01 and the per-task verification map seeded by requirement.
    - `.planning/phases/188-non-technical-run-observability/188-SPEC.md` § "Acceptance Criteria" — the 17 pass/fail criteria this record must be readable against.
  </read_first>
  <action>
Run and record, in a new `## Phase gates — measured at plan 188-12` section:

1. `node scripts/vitest-count-gate.cjs` — total, failed, pinned total, pinned-files count. Must be
   exit 0 / `failed` 0.
2. `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — must equal the Plan-01 baseline (expected
   33). Record the number and, if it moved, every new error with its file.
3. `cd frontend && npx vite build` — exit code. (`vite` skips `tsc`, which is why (2) is separate and
   why "the build passed" is never a typecheck claim in this project.)
4. `cd backend && ./venv/Scripts/python.exe -m pytest tests/test_188_workflow_run_read.py tests/test_182_canvas_gate.py tests/test_revert_byte_identical.py -q` — must be exit 0.
5. `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q -p no:randomly` — record failed/passed
   and **diff the failure set against the Plan-01 baseline**. Record the diff explicitly: which tests
   are newly failing (must be none attributable to this phase) and which are unchanged. **Non-attribution
   is proved by diffing the failure set, never by a rollback run.** Confirm the pre-existing
   `test_thread_workflow_endpoint.py` red is still exactly the one Plan 01 recorded.
6. `git diff --numstat <phase-base-commit>..HEAD -- frontend/src/components/workflows/WorkflowCanvas.tsx`
   — the WHOLE-PHASE diff, not one plan's commit. Must be ≤ 15 insertions and ≤ 4 deletions. Record the
   raw line. If any later plan touched the file, this is where the cap is actually enforced.
7. `git diff --name-only <phase-base-commit>..HEAD -- supabase/migrations/` — must be empty. Record the
   command and its empty output. Zero migrations is SPEC failure condition #10.
8. `bash scripts/check-deploy-drift.sh` — exit 0. This phase adds no env var, no seed migration, no
   bundled service and no sandbox tag change, so there is nothing to reconcile; record that it was
   confirmed rather than assumed.
9. `node scripts/check-gap-closure-rounds.cjs 188` — record the output. Zero rounds have run, so G-7 is
   clear; recording it now means the number at the first `gaps_found` is derived rather than eyeballed.

Then fill in the **per-task verification map** at the top of the file: for each seeded row, set `Task ID`,
`Plan` and `Wave` from the plans as executed, and set `Status` from the measurement — ✅ only where the
named command was actually run and passed. Rows whose evidence is a manual observation stay ⬜ and point
at `188-UAT.md`.

Finally set `nyquist_compliant: true` in the frontmatter **only if** every row is ✅ or is an explicitly
manual row with a home in `188-UAT.md`. If any row is unproved, leave it `false` and say which row.
  </action>
  <acceptance_criteria>
    - `188-VALIDATION.md` contains a `## Phase gates` section with nine labelled command/result pairs, each showing the literal command
    - The backend line shows an explicit failure-set DIFF against the Plan-01 baseline, not just a total
    - The `WorkflowCanvas.tsx` line shows a whole-phase `--numstat` within ≤ 15 / ≤ 4
    - The migrations line shows the command and its empty output
    - Every row of the per-task verification map has a non-TBD `Plan` and a `Status` that is ✅ or ⬜-with-a-UAT-pointer
    - `nyquist_compliant` is `true` only if the previous criterion holds with no unproved row
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs && cd frontend && npx tsc --noEmit -p tsconfig.app.json; cd .. && git diff --name-only HEAD -- supabase/migrations/</automated>
  </verify>
  <done>Nine gates measured and recorded; the per-task map reflects measurement rather than intention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the recorded phase record ↔ what actually ran | A verification record that claims more than was measured is the failure mode this plan exists to prevent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-12-01 | Tampering | the phase's test estate | mitigate | Every suite authored this phase is pinned at its measured count, closing the `TARGETS`-only gap that would let a guard be deleted with the gate green. One pin is observed producing `[count-decrease]`. |
| T-188-12-02 | Repudiation | non-attribution of pre-existing backend reds | mitigate | The failure set is DIFFED against the Plan-01 baseline. A rollback run is explicitly not accepted as evidence. |
| T-188-12-03 | Tampering | the G-5 diff cap | mitigate | The cap is measured over the whole phase range, not one plan's commit, so a later plan cannot quietly widen it. |
| T-188-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed across the phase; `scripts/check-deploy-drift.sh` is run and recorded as confirmation. |
</threat_model>

<verification>
- `node scripts/vitest-count-gate.cjs` exits 0, `failed` 0, every pin present
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` equals the Plan-01 baseline
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q -p no:randomly` failure set diffs clean against the Plan-01 baseline
- `git diff --numstat <phase-base>..HEAD -- frontend/src/components/workflows/WorkflowCanvas.tsx` ≤ 15 / ≤ 4
- `git diff --name-only <phase-base>..HEAD -- supabase/migrations/` is empty
- `bash scripts/check-deploy-drift.sh` exits 0
</verification>

<success_criteria>
- Every suite this phase wrote is pinned, and one pin was watched failing
- Nine phase gates measured and recorded with their literal commands
- `WorkflowCanvas.tsx` whole-phase diff within the pinned cap; zero migrations
- `nyquist_compliant` set from measurement, never from intention
</success_criteria>

<output>
Create `.planning/phases/188-non-technical-run-observability/188-12-SUMMARY.md` when done, including the
raw `[count-decrease]` output and the raw whole-phase `--numstat` line.

⚠ Do NOT call `requirements.mark-complete`, `state.advance-plan` or `roadmap.update-plan-progress` —
those SDK verbs write false completion records in this project.
</output>
