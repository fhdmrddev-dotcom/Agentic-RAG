---
phase: 188-non-technical-run-observability
plan: 05
type: execute
wave: 3
depends_on: ["188-02", "188-04"]
files_modified:
  - frontend/src/lib/phaseState.ts
  - frontend/src/lib/phaseState.test.ts
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/components/panel/PhaseTimeline.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [RUNVIZ-01, RUNVIZ-02]

must_haves:
  truths:
    - "One derivation, two views — the phase-state logic lives in frontend/src/lib/phaseState.ts and both the developer panel and the canvas consume it (D-188-02, SPEC Req 8)"
    - "The module holds the DERIVATION, never the VOCABULARY — the panel keeps STATUS_META and its harness words verbatim; the canvas gets its own business-word table in a different file"
    - "phaseStatusFromDb is TOTAL over every string and its fallback is unknown, never done"
    - "canvasReading collapses retrying to running in ONE named place (D-188-03), so the Req-4 subset property is a property of one function"
    - "canvasReading(undefined) is not-started, NOT unknown — a definition node with no row is a KNOWN state; unknown is reserved for a row that exists with an unrecognised status"
    - "The set of readings the canvas can paint is a subset of those a reconcile can produce — retrying is not a CanvasReading value at all (SPEC Req 4)"
    - "A source fence proves zero local re-derivations, with a POSITIVE CONTROL on every absence assertion"
    - "phaseState.ts imports ONLY @/types — no lib module in this tree imports a component, and this one does not become the first"
    - "TERMINAL_RUN_STATUSES carries timed_out forward UNCHANGED (D-188-21), with the RESEARCH measurement that it is dead recorded in the docblock so a later phase can retire it in one line"
  artifacts:
    - path: "frontend/src/lib/phaseState.ts"
      provides: "DB_PHASE_STATUS, phaseStatusFromDb, CanvasReading, canvasReading, TERMINAL_RUN_STATUSES"
      contains: "export function canvasReading"
      min_lines: 60
    - path: "frontend/src/lib/phaseState.test.ts"
      provides: "Totality, the Req-4 subset property, the parity assertion and the zero-re-derivation source fence"
  key_links:
    - from: "frontend/src/providers/StreamsProvider.tsx"
      to: "frontend/src/lib/phaseState.ts"
      via: "import phaseStatusFromDb; the local DB_PHASE_STATUS is deleted"
      pattern: "phaseStatusFromDb"
    - from: "frontend/src/components/panel/PhaseTimeline.tsx"
      to: "frontend/src/lib/phaseState.ts"
      via: "import TERMINAL_RUN_STATUSES; the local Set is deleted"
      pattern: "TERMINAL_RUN_STATUSES"
---

<objective>
Extract the phase-state derivation into one module, so the developer timeline and the business canvas
cannot drift.

Purpose: SPEC Req 8 and the milestone's own G-5 note both name this extraction. Today the derivation
lives inside `StreamsProvider.tsx` (`DB_PHASE_STATUS`) and `PhaseTimeline.tsx`
(`TERMINAL_RUN_STATUSES`), and the canvas would be their second consumer. Two vocabularies are correct
here — the panel says "Locked", the canvas says "Not started" — but **two derivations are not**, and
Req 8's acceptance is a grep proving zero local re-derivations, not that the two views print the same
strings (D-188-02).

Output: `frontend/src/lib/phaseState.ts`, its rewired consumers, and a suite that fences the totality,
the subset property and the single-derivation claim.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/188-non-technical-run-observability/188-SPEC.md
@.planning/phases/188-non-technical-run-observability/188-CONTEXT.md
@.planning/phases/188-non-technical-run-observability/188-RESEARCH.md
@.planning/phases/188-non-technical-run-observability/188-PATTERNS.md
</context>

<interfaces>
- `Phase` (`frontend/src/types/index.ts:1010-1025`), with `status` widened by Plan 02 to
  `"pending" | "running" | "done" | "failed" | "retrying" | "skipped" | "unknown"`, plus `pendingAsk: string | null`.
- `DB_PHASE_STATUS` currently at `frontend/src/providers/StreamsProvider.tsx:3292` —
  `{ pending: "pending", active: "running", completed: "done", failed: "failed", skipped: "skipped" }`.
  Its single consumer is the terminal branch of `reconcilePhases` at `:3337`.
- `TERMINAL_RUN_STATUSES` currently at `frontend/src/components/panel/PhaseTimeline.tsx:40` —
  `new Set(["completed", "failed", "cancelled", "timed_out"])`. Its single consumer is `:129`
  (`frame?.run_status != null && TERMINAL_RUN_STATUSES.has(frame.run_status)`).
- `workflow_phases_status_check` = `pending | active | completed | failed | skipped` — the five values a
  reconcile can return, which is why the Req-4 subset property is provable.
- The seven readings, LOCKED by D-188-04:
  `"not-started" | "running" | "done" | "failed" | "skipped" | "waiting-for-you" | "unknown"`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend/src/lib/phaseState.ts — the one derivation</name>
  <files>frontend/src/lib/phaseState.ts</files>
  <read_first>
    - `frontend/src/lib/workspacePanel.ts` — the whole file. It is the exact analog: a pure `lib/*` module with an exported lookup `Record`, an exported `Set` constant, and a TOTAL mapping function whose last arm is `default:`. Copy its docblock voice (`:1-30`), its `Record` shape (`:156-167`), its `Set` shape (`:43-59`) and its total-function shape (`:67-88`).
    - `frontend/src/lib/stepCount.ts` — the secondary docblock-voice analog.
    - `frontend/src/providers/StreamsProvider.tsx:3290-3341` — the current home of `DB_PHASE_STATUS` and the terminal branch that consumes it.
    - `frontend/src/components/panel/PhaseTimeline.tsx:34-40, 129` — the current home of `TERMINAL_RUN_STATUSES` and its consumer.
    - `.planning/phases/188-non-technical-run-observability/188-RESEARCH.md` § "Code Examples 1 and 2" and § "Open Question 5" (the `timed_out` measurement) and § "Open Question 6" (why `lib/` and not `components/workflows/`).
  </read_first>
  <action>
Create `frontend/src/lib/phaseState.ts`. It imports **only** `@/types` — nothing else. No `lib/*` module
in this tree imports a component today, and this one must not become the first; that is what makes a
cycle impossible by construction and keeps `PhaseNodeCard`'s provider-free-render promise intact.

Export exactly five things:

1. `DB_PHASE_STATUS: Record<string, Phase["status"]>` — moved VERBATIM from `StreamsProvider.tsx:3292`,
   keeping its Phase-098-UAT provenance comment and appending the 188 move note.
2. `phaseStatusFromDb(raw: string): Phase["status"]` — `DB_PHASE_STATUS[raw] ?? "unknown"`. TOTAL over
   every string. Its docblock states the whole mapping in prose (the `workspacePanel.mapStatus` house
   style) and names the fallback as the honest catch-all, never as success, citing SPEC Req 3 and the
   publish gauntlet's `findIndex → -1` precedent.
3. `type CanvasReading` — the seven-member union, exactly as D-188-04 locks it:
   `"not-started" | "running" | "done" | "failed" | "skipped" | "waiting-for-you" | "unknown"`.
4. `canvasReading(phase: Phase | undefined): CanvasReading` — the collapse rule. Order matters:
   `undefined` returns `"not-started"` FIRST; then `phase.pendingAsk != null` returns
   `"waiting-for-you"`; then a `switch` over `phase.status` mapping `pending → not-started`,
   `running` and `retrying` → `running` (D-188-03, the ONE named place `retrying` collapses),
   `done → done`, `failed → failed`, `skipped → skipped`, and `default: "unknown"`.
   Docblock must state why `undefined` is `not-started` and not `unknown`: a definition node with no
   matching row is a step the harness has not reached, which is a KNOWN state; `unknown` is reserved for
   a row that EXISTS and carries an unrecognised status. Conflating them loses SPEC Req 3's
   "visually distinct from both done and not started" requirement quietly.
5. `TERMINAL_RUN_STATUSES` — moved from `PhaseTimeline.tsx:40`, carrying `timed_out` **unchanged**
   (D-188-21). Its docblock records the RESEARCH measurement so the deferral's re-open trigger is
   satisfied: `run_status` is assigned in exactly ONE place in `threads.py` (`run_status = wf_row["status"]`
   from `workflow_runs.status`), whose CHECK constraint is `active | paused | cap_paused | completed |
   failed | cancelled`; the Deep producer `runs` row's status feeds only a local `producer_terminal`
   boolean and never reaches `run_status`. So `timed_out` is dead — carried forward here by decision, and
   retirable by a later phase in one line, with the evidence now on record rather than assumed.

**This module holds NO vocabulary.** No user-facing strings, no glyphs, no colours, no Tailwind classes.
State that in the header docblock — it is the sentence Req 8's fence depends on.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/phaseState.ts` exists and exports `DB_PHASE_STATUS`, `phaseStatusFromDb`, `CanvasReading`, `canvasReading`, `TERMINAL_RUN_STATUSES`
    - The only `import` statement in the file is from `@/types` — verify `grep -c '^import' frontend/src/lib/phaseState.ts` returns 1
    - `grep -Ec '"Locked"|"Running"|"Complete"|"Not started"|"Paused for your answer"|text-|hsl\(' frontend/src/lib/phaseState.ts` returns 0 (no vocabulary, no styling)
    - `grep -c 'timed_out' frontend/src/lib/phaseState.ts` returns at least 1 and the surrounding docblock names `workflow_runs_status_check`
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>The one derivation exists as a pure leaf module with a single import and no vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the two existing consumers to the module</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/components/panel/PhaseTimeline.tsx</files>
  <read_first>
    - `frontend/src/providers/StreamsProvider.tsx:3290-3341` — the local `DB_PHASE_STATUS` declaration and the terminal branch's `?? "unknown"` call site (post Plan 02).
    - `frontend/src/components/panel/PhaseTimeline.tsx:34-40` and `:129` — the local `TERMINAL_RUN_STATUSES` and its single consumer.
    - `frontend/src/lib/phaseState.ts` — the module written in Task 1.
  </read_first>
  <action>
In `StreamsProvider.tsx`: delete the local `const DB_PHASE_STATUS` declaration and its now-relocated
comment, import `phaseStatusFromDb` from `@/lib/phaseState`, and replace the terminal branch's
`DB_PHASE_STATUS[r.status] ?? "unknown"` with `phaseStatusFromDb(r.status)`. The fallback now lives
inside the total function, which is the point — there is one place a fallback can be got wrong.

In `PhaseTimeline.tsx`: delete the local `const TERMINAL_RUN_STATUSES` and import it from
`@/lib/phaseState`. The consumer at `:129` is unchanged. **Change nothing else in this file** — the SPEC
puts re-homing or re-styling the developer timeline out of scope, and this is a move, not a redesign.

After the rewire, re-run the Plan-02 and Plan-04 falsification suites. They must be green and their
assertions unchanged: an extraction that requires a test edit is not an extraction.
  </action>
  <acceptance_criteria>
    - `grep -c 'const DB_PHASE_STATUS' frontend/src/providers/StreamsProvider.tsx` returns 0
    - `grep -c 'const TERMINAL_RUN_STATUSES' frontend/src/components/panel/PhaseTimeline.tsx` returns 0
    - `grep -c '@/lib/phaseState' frontend/src/providers/StreamsProvider.tsx` returns 1 and the same for `PhaseTimeline.tsx`
    - `cd frontend && npx vitest run src/components/panel/__tests__/` exits 0 with NO assertion edited — verify `git diff frontend/src/components/panel/__tests__/` is empty for this task
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
    - `node scripts/vitest-count-gate.cjs` exits 0 with `failed` 0 and no per-file decrease
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/ && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>Both shipped consumers import the shared module; their local declarations are gone; no test assertion moved.</done>
</task>

<task type="auto">
  <name>Task 3: The module's suite — totality, the subset property, and the zero-re-derivation fence</name>
  <files>frontend/src/lib/phaseState.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/lib/workspacePanel.test.ts:1-30` — the siting, the factory shape and the fixture discipline for a pure-module suite.
    - `frontend/src/components/workflows/phaseVocabulary.test.ts:28-29` (the `?raw` import line), `:846-859` (count-the-declarations with its POSITIVE CONTROL), `:862-882` (the purity block).
    - `frontend/src/components/workflows/DescribeKbPicker.test.tsx:53-66` and `:393-408` — the `FORBIDDEN_SYMBOLS` declaration and the source-side loop, plus the needle-assembly rule (`["list","Folders"].join("")`) that keeps a fence from being satisfied by its own source.
    - `scripts/vitest-count-gate.cjs:92-211` — both knobs.
    - `.planning/phases/188-non-technical-run-observability/188-RESEARCH.md` § "Code Examples 2" — the Req-4 subset property written as a real assertion.
  </read_first>
  <action>
Create `frontend/src/lib/phaseState.test.ts`. Read the module's own source with Vite's `?raw` loader
(`import phaseStateSource from "./phaseState?raw"`) — **never** `fs.readFileSync`; `tsconfig.app.json`
gives tests no `node` types and there are zero `readFileSync` source fences in `frontend/src`.

Cover five groups:

1. **Totality.** For every one of the five `workflow_phases_status_check` values, `phaseStatusFromDb`
   returns the mapped client status. For an unmapped string — assembled from parts, never spelled inline —
   it returns `"unknown"`. Assert explicitly that it does NOT return `"done"`, naming SPEC Req 3.

2. **The Req-4 subset property.** Build `RECONCILABLE` as `[...Object.values(DB_PHASE_STATUS), "unknown"]`
   and `PAINTABLE` as the seven `CanvasReading` values. Assert that for every reconcilable status
   `canvasReading` lands inside `PAINTABLE`, AND that `"retrying"` is not a member of `PAINTABLE` — the
   canvas can never paint a state a reconcile cannot restore. Add the pair that makes it meaningful:
   a `Phase` with `status: "retrying"` reads as `"running"`, and a `Phase` with `status: "running"`
   reads identically, so the two are indistinguishable on the canvas by construction (D-188-03).

3. **Precedence.** `canvasReading(undefined)` is `"not-started"`, not `"unknown"`.
   `pendingAsk != null` wins over every status, including `done` and `failed` — assert both.

4. **Parity with the developer view (SPEC Req 1).** Assert that for every reconcilable status, the value
   `canvasReading` reads and the value the panel's `STATUS_META` is keyed by come from the SAME source
   value — i.e. both are functions of `Phase["status"]` and there is no second mapping between them.
   Do this as a table over the five DB values: `phaseStatusFromDb(dbValue)` produces a `Phase["status"]`,
   that value is a valid `STATUS_META` key, and `canvasReading` of a phase carrying it produces the
   expected reading. Import `STATUS_META`'s key set indirectly if it is not exported — if it is not, assert
   instead over the `Phase["status"]` union enumerated in the test, and record in a comment that the
   panel's exhaustive `Record<Phase["status"], StatusMeta>` is the compiler's own parity guarantee.

5. **The source fence (SPEC Req 8 acceptance).** Declare a `FORBIDDEN_SYMBOLS` list and loop it, in the
   `DescribeKbPicker` shape:
   - `phaseState.ts` imports nothing from `@/lib/api`, nothing from `@/providers/*`, nothing from
     `@/components/*`;
   - it declares `canvasReading` exactly once and `phaseStatusFromDb` exactly once;
   - it contains no vocabulary — none of the locked reading words, no `text-` class, no `hsl(`.
   **Every absence assertion carries a POSITIVE CONTROL** proving the needle can match: assert the regex
   matches a literal sample string built in the test, so its absence in the module is a measurement and
   not a tautology. Assemble every needle from parts so this test's own source can never satisfy a grep
   run over it (the 187-24 lesson).

Then add `"src/lib/phaseState.test.ts"` to `TARGETS` in `scripts/vitest-count-gate.cjs` — a file-level
entry, not the bare `src/lib` directory, which holds ten unmeasured suites the green gate has never run.
Add a comment naming the two-knob distinction. Leave `BASELINE` alone; Plan 11 pins the final count from
the script's printed `actual` column.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/phaseState.test.ts` exits 0 with at least 20 tests
    - The suite contains an assertion that `phaseStatusFromDb(<unmapped>)` is not `"done"`
    - The suite contains an assertion that `"retrying"` is not a member of the paintable set
    - The suite contains an assertion that `canvasReading(undefined) === "not-started"`
    - Every `not.toMatch` / `not.toContain` assertion in the source-fence block is accompanied by a positive control in the same `it(` block
    - Every needle literal is assembled with `.join("")` — verify no forbidden word appears as a bare inline string literal in the test source
    - `grep -c 'readFileSync' frontend/src/lib/phaseState.test.ts` returns 0
    - `grep -c 'src/lib/phaseState.test.ts' scripts/vitest-count-gate.cjs` returns 1
    - `node scripts/vitest-count-gate.cjs` exits 0, `failed` 0, `total` increased
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/phaseState.test.ts</automated>
  </verify>
  <done>The derivation's totality, subset property, precedence and single-declaration claim are all mechanically fenced, and the suite runs inside the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server phase status strings → the one client derivation | After this extraction, a single function decides what every view says about a step. A defect here is a defect everywhere at once — which is the trade Req 8 deliberately makes in exchange for the drift it removes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-05-01 | Spoofing (of outcome) | `phaseStatusFromDb` | mitigate | Totality is asserted directly, including the explicit "is not done" assertion for an unmapped value. The single fallback is now in one place rather than at each call site. |
| T-188-05-02 | Tampering | a second derivation appearing later | mitigate | The `?raw` source fence asserts exactly one declaration of each function and forbids the module importing a provider or component; every absence assertion carries a positive control, so the fence is a measurement rather than a gesture. |
| T-188-05-03 | Information disclosure | none introduced | accept | The module renders nothing and holds no vocabulary; it is pure data-to-data. Enforced by the no-vocabulary grep. |
| T-188-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed. Verified in `188-RESEARCH.md § Package Legitimacy Audit`. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/phaseState.test.ts src/components/panel/__tests__/` exits 0
- `node scripts/vitest-count-gate.cjs` exits 0, `failed` 0, no per-file decrease
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
- `grep -rn "DB_PHASE_STATUS" frontend/src --include=*.ts --include=*.tsx` shows declarations ONLY in `lib/phaseState.ts`
- `git diff --name-only HEAD -- supabase/migrations/` is empty
</verification>

<success_criteria>
- One derivation module consumed by both shipped views; zero local re-derivations, proved by a fenced grep with positive controls (SPEC Req 8)
- `retrying` collapses in exactly one named place (D-188-03) and is not paintable (SPEC Req 4)
- `timed_out` carried forward unchanged with the measurement recorded (D-188-21)
- The new suite RUNS inside the count gate
</success_criteria>

<output>
Create `.planning/phases/188-non-technical-run-observability/188-05-SUMMARY.md` when done.

⚠ Do NOT call `requirements.mark-complete`, `state.advance-plan` or `roadmap.update-plan-progress` —
those SDK verbs write false completion records in this project.
</output>
