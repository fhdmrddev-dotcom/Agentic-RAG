---
phase: 188-non-technical-run-observability
plan: 04
type: execute
wave: 3
depends_on: ["188-02"]
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx
  - .planning/reported-bugs/BUG-260609-04.md
  - .planning/reported-bugs/BUG-260609-02.md
autonomous: true
requirements: [RUNVIZ-01, RUNVIZ-02]

must_haves:
  truths:
    - "BUG-260609-04 is closed at its root: reconcilePhases' LIVE branch stops labelling non-current phases with the placeholder slug phase-N when the server already returned the real slugs (D-188-22)"
    - "The overlay carries IDENTITY ONLY — slug and phaseType. The positional status derivation is untouched, so the shipped forward-only floor cannot move backward"
    - "The defect is observed RED before the fix, using a fixture where wf.phases carries real slugs and the live branch is taken"
    - "phaseType stops resolving to \"unknown\" on the live branch — the second, previously unrecorded half of the same bug"
    - "BUG-260609-02 is recorded as reviewed and left OPEN (D-188-23) — 188 touches neither subAgents nor the sub-results render"
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "The live-branch identity overlay from wf.phases, joined by phase_index"
    - path: "frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx"
      provides: "The RED-first slug-clobber test plus a forward-only-floor guard"
    - path: ".planning/reported-bugs/BUG-260609-04.md"
      provides: "Updated re_open_trigger recording the code-level closure and the remaining live-UAT condition"
  key_links:
    - from: "frontend/src/providers/StreamsProvider.tsx"
      to: "ThreadWorkflowState.phases"
      via: "an identity overlay onto the positional skeleton, keyed by phase_index"
      pattern: "phase_index"
---

<objective>
Close BUG-260609-04 at its root, in the one function this phase is already inside.

Purpose: `reconcilePhases`' LIVE branch builds a positional skeleton whose non-current rows carry
placeholder slugs `phase-${i}` and `phaseType: "unknown"`, because it was written when only
`current_phase_slug` was believed to be known mid-run. RESEARCH measured that premise FALSE:
`workflow_phases` is fully populated at run creation in one transaction, and
`GET /threads/{id}/workflow` returns real `slug` + `phase_index` + `status` + `phase_type` for EVERY row
of a LIVE run. So the real values are already on the wire and the skeleton is discarding them. The
canvas is immune by construction (it joins by `phase_index` and labels from the definition — D-188-01),
but the developer panel is not, and this is the phase whose `re_open_trigger` named itself.

Output: an identity overlay, observed RED first, that leaves the forward-only status floor untouched.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/188-non-technical-run-observability/188-CONTEXT.md
@.planning/phases/188-non-technical-run-observability/188-RESEARCH.md
@.planning/phases/188-non-technical-run-observability/188-PATTERNS.md
@.planning/reported-bugs/BUG-260609-04.md
</context>

<interfaces>
- `frontend/src/lib/api.ts:1250-1300` — `ThreadWorkflowState` carries `mode`, `lock_is_stale`,
  `total_phases`, `current_phase_index`, `current_phase_slug`, `active_workflow_run_id`, and
  `phases: WorkflowPhaseState[]`. `WorkflowPhaseState` is `{ slug, phase_index, status, phase_type? }`.
- `frontend/src/providers/StreamsProvider.tsx:3300-3324` — the LIVE branch: `Array.from({length: total})`
  producing `slug: i === current ? (wf.current_phase_slug ?? \`phase-${i}\`) : \`phase-${i}\``,
  `phaseType: "unknown"`, and the positional status derivation
  `i < current ? "done" : i === current ? "running" : "pending"`.
- `frontend/src/components/panel/PhaseTimeline.tsx:115-127` — the shipped forward-only step counter that
  the positional status derivation feeds. It must not be able to move backward.
- Measured (RESEARCH Open Question 4): `create_workflow_run` (`backend/app/db/workflows.py:206-214`)
  inserts one `workflow_phases` row per `PhaseSpec` at run creation, ALL `status='pending'`, in one
  transaction. It is the only `INSERT INTO workflow_phases` in `backend/app`. Therefore there is no
  "rows the harness has not inserted yet" case, and the overlay is COMPLETE rather than partial.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Observe the slug clobber and the phaseType loss RED on the live branch</name>
  <files>frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx</files>
  <read_first>
    - `frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx` — including the two blocks Plan 02 added; reuse the same `getThreadWorkflow` mock harness.
    - `frontend/src/providers/StreamsProvider.tsx:3300-3324` — the LIVE branch verbatim. Note the condition that selects it: `wf.mode === "harness" && !wf.lock_is_stale`.
    - `.planning/reported-bugs/BUG-260609-04.md` — the report body and its fix options; option 1 is what this plan implements.
    - `.planning/phases/188-non-technical-run-observability/188-RESEARCH.md` § "Open Question 4" — the four consequences, especially caution (4): overlay identity only.
  </read_first>
  <action>
Add one `describe` block driving the **LIVE** branch: mock `getThreadWorkflow` to return
`mode: "harness"`, `lock_is_stale: false`, `total_phases: 4`, `current_phase_index: 1`,
`current_phase_slug: "draft-the-letter"`, and a `phases` array carrying four REAL rows with real slugs
(e.g. `collect-inputs`, `draft-the-letter`, `check-the-numbers`, `produce-the-report`), real
`phase_index` values 0..3, and real `phase_type` values.

Assert, against unmodified production source:
- the reconciled phase at index 0 has `slug === "collect-inputs"` — on HEAD it is `phase-0`;
- the reconciled phase at index 2 has `slug === "check-the-numbers"` — on HEAD it is `phase-2`;
- every reconciled phase has a `phaseType` matching the server row — on HEAD they are all `"unknown"`.

Add the **forward-only-floor guard in the same block**, asserting the property the fix must NOT break:
give the server rows statuses that are BEHIND the positional derivation (e.g. the row at index 0 is
still `pending` in the DB even though `current_phase_index` is 1, which happens because the DB row only
flips at `complete_phase`), and assert the reconciled status at index 0 is `done` — i.e. the positional
floor still wins for STATUS. This guard is green before the fix and must stay green after; it is what
stops a well-meant "overlay everything" from moving the shipped counter backward.

Record the observed RED values verbatim in a comment at the top of the block. Modify no production
source in this task. Paste the raw failing output into the plan summary.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/panel/__tests__/PhaseReconcile.test.tsx` exits non-zero, with failures showing received `"phase-0"` / `"phase-2"` and received `"unknown"` for `phaseType`
    - The forward-only-floor assertion PASSES in the same run (it is a guard, not a falsification)
    - The block contains a comment recording each observed RED value verbatim
    - `git diff --name-only` for this task lists ONLY `PhaseReconcile.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/PhaseReconcile.test.tsx</automated>
  </verify>
  <done>The placeholder-slug clobber and the `phaseType` loss are both observed failing on unmodified source; the floor guard is green.</done>
</task>

<task type="auto">
  <name>Task 2: Overlay identity only, from wf.phases, by phase_index</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - `frontend/src/providers/StreamsProvider.tsx:3300-3324` — the LIVE branch and its existing comment explaining why placeholders exist.
    - `frontend/src/providers/StreamsProvider.tsx:2828-2839` — the BUG-260609-01 sibling comment; it explains its by-INDEX choice and enumerates what it never touches. Write this amendment in that voice.
    - `.planning/phases/188-non-technical-run-observability/188-PATTERNS.md` § "(a) `DB_PHASE_STATUS` + the `?? "done"` fail-open" — the live branch's current bytes.
  </read_first>
  <action>
Inside the LIVE branch, build a `Map<number, WorkflowPhaseState>` from `wf.phases ?? []` keyed by
`phase_index`, then in the `Array.from` callback overlay **identity only**:
- `slug`: the mapped row's `slug` when present, else the existing expression (`current_phase_slug` for
  the current index, `phase-${i}` otherwise) as the unchanged fallback;
- `phaseType`: the mapped row's `phase_type` when present, else `"unknown"` as today.

**Do not overlay `status`.** Leave `i < current ? "done" : i === current ? "running" : "pending"`
byte-identical. RESEARCH caution (4): the derived statuses are a forward-only floor and are in some
cases MORE advanced than the DB rows, because a DB row only flips at `complete_phase`; a naive full
overlay would move `PhaseTimeline`'s shipped counter backward.

Replace the branch's existing comment rather than appending a second one, and state four things:
that `wf.phases` is fully populated from t=0 (measured — `create_workflow_run` inserts every row in one
transaction and is the only INSERT in `backend/app`); that the overlay is therefore COMPLETE, not
partial, so the "rows the harness has not inserted yet" clause describes a case that cannot occur; that
the join key is `phase_index` because a live slug can be a placeholder and an index cannot; and that
STATUS is deliberately NOT overlaid, naming the forward-only floor at `PhaseTimeline.tsx:115-127`.

Keep the `total_phases` floor as the array length. It may remain as defence in depth; do not present it
as load-bearing.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/panel/__tests__/PhaseReconcile.test.tsx` exits 0 — the clobber tests, the `phaseType` tests, the forward-only-floor guard, and both Plan-02 falsifications
    - The positional status expression `i < current ? "done" : i === current ? "running" : "pending"` is byte-unchanged — verify with `git diff frontend/src/providers/StreamsProvider.tsx` that no line containing that ternary is modified
    - `grep -c 'phase_index' frontend/src/providers/StreamsProvider.tsx` shows the new join key is present in the live branch
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
    - `node scripts/vitest-count-gate.cjs` exits 0 with `failed` 0 and no per-file decrease
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/PhaseReconcile.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>Mid-run phase cards carry real slugs and real phase types; the forward-only status floor is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Record the reported-bugs routing outcome</name>
  <files>.planning/reported-bugs/BUG-260609-04.md, .planning/reported-bugs/BUG-260609-02.md</files>
  <read_first>
    - `.planning/reported-bugs/BUG-260609-04.md` — frontmatter (`status: folded`, `folded_into: "188"`) and its `re_open_trigger`, which already says to flip to `closed` only when the `phase-0` render is verified gone on a LIVE single-phase run.
    - `.planning/reported-bugs/BUG-260609-02.md` — frontmatter and `re_open_trigger`.
    - `.planning/reported-bugs/TEMPLATE.md` — the frontmatter contract.
    - `CLAUDE.md` § "Reported bugs cross-check" — the status lifecycle and the `/gsd:plan-phase` obligation.
  </read_first>
  <action>
`BUG-260609-04`: keep `status: folded` and `folded_into: "188"`. Do **not** flip to `closed` — closure
requires the live-UAT observation, which is Plan 12's operator gate. Append to `re_open_trigger` a dated
2026-08-05 note recording: the code-level closure landed in plan `188-04` as an identity overlay in
`reconcilePhases`' live branch keyed by `phase_index`; the canvas half is closed by construction via
D-188-01 (labels come from the definition, never from a live slug); the previously unrecorded second half
(`phaseType` resolving to `"unknown"` mid-run) is closed by the same edit; and the remaining condition
for `closed` is the live single-phase run in `188-UAT.md`. Add `verified_closed_by: null` if absent.

`BUG-260609-02`: leave `status: open` and change no other field. Append to `re_open_trigger` a dated
2026-08-05 note recording the D-188-23 review — Phase 188 touches neither `subAgents` nor the
sub-results render, so it is reviewed and NOT folded.

Change no other report. Every other open `surface: Agentic-RAG` report is a chat-surface / provider /
harness-emit item on the SEED-045 track or the harness backlog and is not a routing candidate for this
phase.
  </action>
  <acceptance_criteria>
    - `grep -c 'status: folded' .planning/reported-bugs/BUG-260609-04.md` returns 1 and `grep -c 'status: closed' .planning/reported-bugs/BUG-260609-04.md` returns 0
    - `BUG-260609-04.md`'s `re_open_trigger` contains the string `188-04` and names the live single-phase run as the remaining condition
    - `grep -c 'status: open' .planning/reported-bugs/BUG-260609-02.md` returns 1
    - `BUG-260609-02.md`'s `re_open_trigger` contains `D-188-23`
    - `git diff --name-only` for this task lists exactly those two files
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'status: folded' .planning/reported-bugs/BUG-260609-04.md && grep -q '188-04' .planning/reported-bugs/BUG-260609-04.md && grep -q 'D-188-23' .planning/reported-bugs/BUG-260609-02.md && echo OK</automated>
  </verify>
  <done>Both reports' frontmatter reflects the actual routing; neither claims a closure that has not been observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server `ThreadWorkflowState.phases` → client reducer | Server-authored slugs and phase types are rendered as labels in the developer panel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-04-01 | Tampering (of meaning) | `reconcilePhases` live branch | mitigate | The overlay is IDENTITY ONLY. Overlaying status would let a lagging DB row move the shipped forward-only counter backward — a regression dressed as a fix. The Task-1 floor guard is the mechanical fence. |
| T-188-04-02 | Information disclosure / XSS | overlaid `slug` and `phase_type` rendering in `PhaseCard` | accept | Both render as plain React text children through the shipped `PhaseCard`; no `dangerouslySetInnerHTML` is introduced and no new render site is created. These are the same authored strings the terminal branch already surfaced. |
| T-188-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed. Verified in `188-RESEARCH.md § Package Legitimacy Audit`. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/panel/__tests__/` exits 0
- `node scripts/vitest-count-gate.cjs` exits 0, `failed` 0, no per-file decrease
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
- `git diff --name-only HEAD -- supabase/migrations/` is empty
</verification>

<success_criteria>
- The live-run panel shows real step names and real step types (D-188-22)
- The shipped forward-only status floor is byte-unchanged and mechanically guarded
- The defect was observed RED before the fix, with raw output in the summary
- Both reported bugs' frontmatter records the true routing outcome (D-188-22, D-188-23)
</success_criteria>

<output>
Create `.planning/phases/188-non-technical-run-observability/188-04-SUMMARY.md` when done, including the
raw RED output.

⚠ Do NOT call `requirements.mark-complete`, `state.advance-plan` or `roadmap.update-plan-progress` —
those SDK verbs write false completion records in this project.
</output>
