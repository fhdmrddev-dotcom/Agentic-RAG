---
phase: 188-non-technical-run-observability
plan: 07
type: execute
wave: 5
depends_on: ["188-06"]
files_modified:
  - frontend/src/components/workflows/runVocabulary.ts
  - frontend/src/components/workflows/PhaseNode.tsx
  - frontend/src/components/workflows/PhaseNode.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.tsx
  - frontend/src/components/workflows/WorkflowCanvas.test.tsx
autonomous: true
requirements: [RUNVIZ-01]

must_haves:
  truths:
    - "WorkflowCanvas.tsx's diff stays within a HARD cap of 15 insertions / 4 deletions, measured with git diff --numstat and recorded as an acceptance criterion (SPEC Req 8, G-5)"
    - "The canvas is a PASS-THROUGH: it derives no run state, reads no phase_index, and imports no vocabulary table — runState arrives already worded from the page"
    - "run state is merged inside the settledNodes memo, which excludes dragOverlay — the memo split is load-bearing for the anti-blink fix and must not be collapsed"
    - "The run reading joins the node's accessible name, so a screen-reader user tabbing the spine hears the state"
    - "The visible run line and the accessible-name suffix come from ONE function (runReadingLabel), so they cannot disagree"
    - "The canvas run-state render path contains zero raw DB status literals and zero phase_index (SPEC Req 2)"
    - "The run-time waiting string is not equal to the shipped 'Waits for you' badge literal and does not differ from it only by verb tense (SPEC Req 5)"
    - "188 spends ZERO badge slots — slot 1 stays empty and reserved for Phase 189; the max-2 BadgeSlots tuple makes a third badge a typecheck error"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseNode.tsx"
      provides: "Reads data.run, passes status + emitFailure to the card; the slot-contract comment corrected"
    - path: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      provides: "The capped runState prop mirror and the ariaLabel append"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      to: "PhaseNode"
      via: "node.data.run, merged in the settledNodes memo exactly as verdict is"
      pattern: "runState\\?\\."
    - from: "frontend/src/components/workflows/PhaseNode.tsx"
      to: "PhaseNodeCard"
      via: "status + emitFailure props"
      pattern: "status="
---

<objective>
Thread run state from the page to the card without letting the canvas learn anything.

Purpose: G-5 fires on `WorkflowCanvas.tsx` — 9 plans across 3 phases, 1,582 lines. It is honoured here
by SCOPE, not by silence: this phase performs the extraction the ledger's own 188 row names (done in
Plan 05) and caps this file's diff as a **pinned plan acceptance criterion**, exactly as `187-08` pinned
≤7/≤2 and Phase 185 honoured G-5 on `PhaseFormPanel.tsx`. The `PlaneEditingLayer` / `EDIT_AFFORDANCE`
lift named by `185-10` is deliberately NOT attempted — it serves *editing*, which run-viz never touches,
and lifting it risks the live ESM cycle for zero run-viz payoff. That debt stays OWED.

Output: a `runState` prop that mirrors the shipped `marks` prop, a `PhaseNode` adapter that reads one
`data` field, and the measured `git diff --numstat` recorded.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/188-non-technical-run-observability/188-SPEC.md
@.planning/phases/188-non-technical-run-observability/188-UI-SPEC.md
@.planning/phases/188-non-technical-run-observability/188-RESEARCH.md
@.planning/phases/188-non-technical-run-observability/188-PATTERNS.md
</context>

<interfaces>
- `frontend/src/components/workflows/canvasModel.ts:121-152` — `PhaseNodeData` carries
  `slug, phaseIndex, phaseType, title, technicalTitle, subtitle, grounded, armed, waitsForYou` and an
  index signature `[k: string]: unknown`. **Because of that index signature, `data.run` needs no
  `canvasModel.ts` edit** — `verdict` and `technical` already arrive the same way.
- `frontend/src/components/workflows/canvasModel.ts:379-388` — `toCanvas` sets each node's top-level
  `ariaLabel: ariaLabelFor(phase, data.title)`, producing e.g. `Phase 4: Draft the renewal letter (llm_agent)`.
  It is a node property, not a `data` field.
- `frontend/src/components/workflows/WorkflowCanvas.tsx:801-807` — the `marks?:` prop, measured at
  **5 comment lines + 1 declaration = 6**. This is the precedent the new prop mirrors line for line.
- `frontend/src/components/workflows/WorkflowCanvas.tsx:865-881` — the destructure list.
- `frontend/src/components/workflows/WorkflowCanvas.tsx:926-956` — the `settledNodes` memo; the `data`
  merge is at `:952` and the dependency array at `:955`.
- `frontend/src/components/workflows/WorkflowCanvas.tsx:918-925` and `:958-973` — the docblock explaining
  why `settledNodes` is split from the thin drag overlay, and the overlay itself. Collapsing the split
  reintroduces the ~60fps card flicker during a drag.
- `frontend/src/components/workflows/PhaseNode.tsx:142-157` — how `technical` and `verdict` are read off
  `data` with a cast; `:160-186` — the badge assembly, the `Waits for you` literal, and the
  slot-contract comment naming `status`, `technicalLine` and `stepNumber` as deliberately not passed;
  `:209-223` — the `<PhaseNodeCard .../>` call; `:257` — the `⤳` on-fail branch-edge marker.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The NodeRunState shape and the PhaseNode adapter</name>
  <files>frontend/src/components/workflows/runVocabulary.ts, frontend/src/components/workflows/PhaseNode.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/runVocabulary.ts` — the module written in Plan 06.
    - `frontend/src/components/workflows/PhaseNode.tsx` — the whole file, especially `:140-160` (the `technical` and `verdict` reads with their casts and their docblocks), `:160-186` (badges + the slot-contract comment), `:209-223` (the card call), `:290-310` (the memo pairing docblock).
    - `frontend/src/components/workflows/PhaseNodeCard.tsx` — the `status` and `emitFailure` props as narrowed in Plan 06.
    - `.planning/phases/188-non-technical-run-observability/188-UI-SPEC.md` § "Two Facts on One Node" and § "The node's accessible name".
  </read_first>
  <action>
Add ONE exported type to `runVocabulary.ts`:
`NodeRunState = { reading: CanvasReading; label: string; emitFailure?: EmitFailure | null }`.
`label` is `runReadingLabel(reading, emitFailure)` computed by the PAGE. It exists as a field so that
`WorkflowCanvas` can append it to the accessible name **without importing a vocabulary table** — the
canvas must stay a pass-through that derives nothing. Say that in the type's docblock.

In `PhaseNode.tsx`:
- Read `const run = data.run as NodeRunState | undefined`, using the same cast-with-docblock shape
  `verdict` uses at `:157`. State that the shell merges it, that the page owns the derivation, and that
  `PhaseNode` therefore cannot grow a second source of truth for a value only the run stream owns.
- Pass `status={run?.reading}` and `emitFailure={run?.emitFailure}` to `<PhaseNodeCard>`. Nothing else in
  the card call changes.
- **Do not touch `badges`.** `BadgeSlots` is a max-2 tuple, slot 2 is `Waits for you`, slot 1 stays empty
  and reserved for Phase 189, and a third badge is a typecheck error rather than a review comment.
- Correct the `:182-186` slot-contract comment, which is a maintenance obligation and not decoration:
  remove `status` from the "still deliberately NOT passed" list, state that Phase 188 now fills it, and
  state that `technicalLine` is **DECLINED** by 188 (UI-SPEC card-budget rule 2) and `stepNumber` remains
  unspent, both still free for Phase 189. Keep the existing sentence about `verdict` (184-10) and
  `grounded` (185-09) — the list is corrected each time rather than left, because a comment that still
  names a slot the component now fills is the same defect as a false docblock.
- Change nothing about the `⤳` branch-edge marker at `:257`. It is the on-fail `skip_to_phase` edge, and
  reusing it for the *skipped* reading is exactly the collision D-188-06 forbids.
  </action>
  <acceptance_criteria>
    - `grep -c 'NodeRunState' frontend/src/components/workflows/runVocabulary.ts` returns at least 1
    - `grep -c 'data.run' frontend/src/components/workflows/PhaseNode.tsx` returns 1
    - `grep -c 'status={' frontend/src/components/workflows/PhaseNode.tsx` returns 1
    - `git diff frontend/src/components/workflows/PhaseNode.tsx` shows the `badges` assembly and the `Waits for you` literal byte-unchanged
    - The `:182-186` comment no longer lists `status` as unpassed and now names `technicalLine` as DECLINED
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json && npx vitest run src/components/workflows/PhaseNode.test.tsx</automated>
  </verify>
  <done>`PhaseNode` reads one `data` field and forwards two props; zero badge slots are spent; the slot-contract comment is true again.</done>
</task>

<task type="auto">
  <name>Task 2: The capped WorkflowCanvas pass-through — ≤ 15 insertions / ≤ 4 deletions</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:795-810` — the `marks?:` declaration and its 5-line docblock, the shape to mirror.
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:865-881` — the destructure list.
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:918-973` — the `settledNodes` memo, its dependency array, the split docblock and the drag overlay.
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:20-40` — the module-scope `edgeTypes = { flow: FlowEdge }` VALUE import. The ESM-cycle constraint is real; nothing extracted may import back into this file. `runVocabulary` is a leaf and does not.
    - `.planning/phases/188-non-technical-run-observability/188-RESEARCH.md` § "Open Question 7" — the itemised 10-insertion/2-deletion minimum against the `marks` precedent, and the explicit list of what must NOT be inside the cap.
    - `.planning/phases/188-non-technical-run-observability/188-UI-SPEC.md` § "The node's accessible name" — the one-line `ariaLabel` append.
  </read_first>
  <action>
Exactly five edits, and no others:

1. Fold `import type { NodeRunState } from "./runVocabulary"` into an existing import block — one line.
2. Declare `runState?: (slug: string) => NodeRunState | undefined` beside `marks?:`, with a docblock in
   the `marks` voice: the page owns the `phase_index` join (D-188-01), **this canvas derives no run
   state and reads no `phase_index`**, and the prop is OPTIONAL and inert when absent so every shipped
   Builder caller renders unchanged.
3. Add `runState` to the destructure list — one identifier.
4. Rewrite the `data` merge line at `:952` to append `run: runState?.(node.id)`; add `runState` to the
   memo dependency array at `:955`. Two lines rewritten.
5. Add the accessible-name append inside the same `settledNodes` return object:
   `ariaLabel` becomes the node's own label with ` — ` and `run.label` appended when a run reading exists,
   and the node's own label otherwise. One line. `run.label` is already worded by the page, so this file
   imports no vocabulary and derives nothing.

⚠ **The memo split is load-bearing.** `runState` belongs in `settledNodes`, which excludes `dragOverlay`.
Collapsing the split reintroduces the ~60fps card flicker the docblock at `:918-925` describes.

⚠ **What must NOT be inside this file, and is what makes the cap honest rather than decorative:** the
`phase_index → slug` join (the page), the reading vocabulary (`runVocabulary.ts`), the ring SVG
(`PhaseNodeCard.tsx`), and the `usePhases` subscription (the page — also required by the 067.5 Branch-D3
guard that keeps the canvas out of the stream path).

Then measure and record:
`git diff --numstat -- frontend/src/components/workflows/WorkflowCanvas.tsx`.
The measured minimum is 11 insertions / 2 deletions. **The cap is ≤ 15 insertions / ≤ 4 deletions.**
If the measurement exceeds it, do not widen the cap — move the excess out of this file. Paste the raw
`--numstat` line into the plan summary.
  </action>
  <acceptance_criteria>
    - `git diff --numstat -- frontend/src/components/workflows/WorkflowCanvas.tsx` reports insertions ≤ 15 AND deletions ≤ 4
    - The raw `--numstat` line is pasted into the plan summary
    - `grep -c 'phase_index\|phaseIndex' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0
    - `grep -Ec 'Not started|Running|Complete|Failed|Skipped|Paused for your answer|State unknown' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0 — no vocabulary in this file
    - `grep -c 'runState' frontend/src/components/workflows/WorkflowCanvas.tsx` returns exactly 5 (import type usage, declaration, destructure, merge, dep array) — a sixth occurrence means logic leaked in
    - `git diff frontend/src/components/workflows/WorkflowCanvas.tsx` shows NO change inside the `dragOverlay` memo
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
  </acceptance_criteria>
  <verify>
    <automated>git diff --numstat -- frontend/src/components/workflows/WorkflowCanvas.tsx && cd frontend && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>The canvas gained one optional pass-through prop and one aria append, inside a measured ≤15/≤4 diff; every shipped Builder caller renders unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: The Req-2 vocabulary fence, the Req-5 string separation, and the prop-mirror guard</name>
  <files>frontend/src/components/workflows/PhaseNode.test.tsx, frontend/src/components/workflows/WorkflowCanvas.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseNode.test.tsx` — the whole file (13 cases at HEAD, pinned by Plan 01). Reuse its render harness.
    - `frontend/src/components/workflows/WorkflowCanvas.test.tsx` — the whole file (31 pinned, 35 running; 4 cases sit in slack).
    - `frontend/src/components/workflows/DescribeKbPicker.test.tsx:53-66, 365-408` — the `FORBIDDEN_SYMBOLS` declaration, the needle-assembly rule, and the positive-control discipline.
    - `.planning/phases/188-non-technical-run-observability/188-UI-SPEC.md` § "Two Facts on One Node" — the four structural separations and Req 5's mechanical acceptance.
  </read_first>
  <action>
In `PhaseNode.test.tsx`:

1. **Req 2 — no harness vocabulary in the run-state render path.** A `?raw` fence over `PhaseNode.tsx`
   AND `runVocabulary.ts` asserting zero occurrences of the raw DB status literals (`pending`, `active`,
   `completed`, `failed`, `skipped` as DB values) and zero `phase_index` in the render path. Assemble
   every needle from parts (`["comp","leted"].join("")`) so this test's own source cannot satisfy the
   grep — the 187-24 lesson. Give every absence assertion a positive control.
2. **Req 2 — no slug on the face with the reveal off.** Render a node at each of the seven readings with
   `technical` false, and assert the phase slug string does not appear in the rendered text.
3. **Req 5 — two facts on one node.** Render an `llm_human_input` node that is BOTH `waitsForYou` (the
   shipped design-time badge) and in the `waiting-for-you` run reading. Assert: both strings are present;
   they are not equal; and they do not differ only by verb tense — implement that as an explicit
   assertion that the two strings differ in more than a trailing/leading verb inflection, e.g. that the
   run string contains a word absent from the badge string AND the badge string contains a word absent
   from the run string. Add a grep assertion that the run-time literal is not the badge's literal.
4. **The label is derived once.** Assert the rendered run-line text is exactly the substring the node's
   accessible-name suffix carries — i.e. the visible sentence and the assistive sentence come from the
   same `runReadingLabel` call and cannot drift.

In `WorkflowCanvas.test.tsx`:

5. **The prop mirror.** Render the canvas with a `runState` returning a reading for one slug and
   `undefined` for the others; assert the returned value reaches that node's `data.run` and that the
   other nodes carry `undefined`. Assert `runState` absent renders exactly as before (the shipped
   read-only Builder path).
6. **The canvas derives nothing.** A `?raw` fence over `WorkflowCanvas.tsx` asserting zero `phase_index`,
   zero reading words, and zero import of `@/lib/phaseState`'s runtime exports — with positive controls.
7. **The aria append.** Assert the node's accessible name ends with the run label when a reading is
   supplied and is unchanged when it is not.

Note in a comment on the `WorkflowCanvas.test.tsx` additions that this file is pinned at 31 while it runs
35, so four cases sat in slack; the new cases raise the real count and Plan 11 re-pins from the script's
printed `actual` column.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/PhaseNode.test.tsx` exits 0 with a case count strictly greater than 13
    - `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx` exits 0 with a case count strictly greater than 35
    - Every `not.toMatch` / `not.toContain` in both fence blocks has a positive control in the same `it(`
    - The Req-5 test asserts BOTH strings render simultaneously AND that they are unequal AND that neither is a tense variant of the other
    - The label-derived-once test compares the run-line text against the accessible-name suffix
    - `node scripts/vitest-count-gate.cjs` exits 0, `failed` 0, no per-file decrease
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/</automated>
  </verify>
  <done>Req 2's grep, Req 5's string separation and the canvas's derives-nothing property are all mechanically fenced with positive controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page-derived run state → canvas → node leaf | The canvas is a projection. If it acquires the ability to derive run state, it becomes a second source of truth — the D-14 red line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-07-01 | Tampering | `WorkflowCanvas.tsx` scope creep | mitigate | A pinned `git diff --numstat` cap of ≤15/≤4, plus a `?raw` fence asserting zero `phase_index` and zero reading words in the file, plus an exact occurrence count on `runState`. The cap is enforced as an acceptance criterion, not as advice. |
| T-188-07-02 | Information disclosure | harness identifiers on a business surface | mitigate | The Req-2 fence asserts zero raw DB status literals and zero `phase_index` in the render path, and a render assertion proves no slug text appears with the ⌥ reveal off. Every needle is assembled from parts so the fence cannot be satisfied by its own source. |
| T-188-07-03 | Tampering / XSS | authored strings reaching the aria label | accept | `ariaLabel` is set as a string property on a node object and rendered by React Flow as an attribute; no HTML is constructed. `run.label` is a closed set of fixed strings, not user input. |
| T-188-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed. Verified in `188-RESEARCH.md § Package Legitimacy Audit`. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/` exits 0
- `git diff --numstat -- frontend/src/components/workflows/WorkflowCanvas.tsx` reports ≤ 15 insertions and ≤ 4 deletions
- `node scripts/vitest-count-gate.cjs` exits 0, `failed` 0, no per-file decrease
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
- `git diff --name-only HEAD -- supabase/migrations/` is empty
</verification>

<success_criteria>
- The `WorkflowCanvas.tsx` diff is measured and within the ≤15/≤4 cap, with the raw `--numstat` in the summary (SPEC Req 8, G-5)
- The canvas derives no run state, reads no `phase_index`, and imports no vocabulary
- The visible run line and the accessible name come from one function
- Zero badge slots spent; `technicalLine` and `stepNumber` remain unspent
</success_criteria>

<output>
Create `.planning/phases/188-non-technical-run-observability/188-07-SUMMARY.md` when done, including the
raw `git diff --numstat` line for `WorkflowCanvas.tsx`.

⚠ Do NOT call `requirements.mark-complete`, `state.advance-plan` or `roadmap.update-plan-progress` —
those SDK verbs write false completion records in this project.

⚠ Record in the summary that the `PlaneEditingLayer` / `EDIT_AFFORDANCE` extraction remains OWED and is
due at the next `WorkflowCanvas.tsx` feature touch — deferring it is a decision, not a closure.
</output>
