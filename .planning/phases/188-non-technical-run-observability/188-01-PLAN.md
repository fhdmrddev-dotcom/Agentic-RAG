---
phase: 188-non-technical-run-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/vitest-count-gate.cjs
  - .planning/phases/188-non-technical-run-observability/188-VALIDATION.md
autonomous: true
requirements: [RUNVIZ-01, RUNVIZ-02, RUNVIZ-03]

must_haves:
  truths:
    - "The panel reconcile suites this phase's falsification tests live in are INSIDE the count gate's TARGETS, so a suite that does not run cannot silently prove nothing"
    - "PhaseNode.test.tsx and PhaseNodeCard.test.tsx — which RUN today but are PINNED by nothing — are pinned, so 188's node-level guards cannot be deleted with the gate green"
    - "Every pin value is read from the script's own printed actual column across two agreeing runs, never hand-counted"
    - "At least one pin is observed producing a [count-decrease] on a genuinely deleted it( block, then restored"
    - "The measured pre-existing baselines are recorded BEFORE any production edit, so nothing this phase touches can be blamed for them"
    - "The pre-existing 1-of-7 RED in test_thread_workflow_endpoint.py is recorded by name and assertion, since it fences the endpoint the run surface reads"
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "TARGETS extended to run the panel reconcile suites; BASELINE extended to pin the two unpinned node suites"
      contains: "PhaseReconcile.test.tsx"
    - path: ".planning/phases/188-non-technical-run-observability/188-VALIDATION.md"
      provides: "The recorded pre-edit baselines every later gate is diffed against"
      contains: "Measured baselines"
  key_links:
    - from: "scripts/vitest-count-gate.cjs"
      to: "frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx"
      via: "a TARGETS entry — what RUNS"
      pattern: "components/panel/__tests__"
    - from: "scripts/vitest-count-gate.cjs"
      to: "PhaseNodeCard.test.tsx"
      via: "a BASELINE entry — what is PINNED"
      pattern: "PhaseNodeCard.test.tsx"
---

<objective>
Make this phase's test estate actually run and actually pin, and record the pre-edit baselines, before
a single production line changes.

Purpose: `scripts/vitest-count-gate.cjs` has **two knobs** — `TARGETS` decides what RUNS, `BASELINE`
decides what is PINNED — and `src/components/panel/__tests__/`, `src/lib/` and `src/pages/` land
outside **both** by default. Every falsification test this phase is about to write would therefore be
silently never executed, and a suite that does not run cannot fail. That is verbatim the round-5
"verification truth 14" failure Phase 187 shipped and had to fix afterwards. This plan fixes it first.

Output: an extended gate (both knobs), and the measured baseline block recorded in `188-VALIDATION.md`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/188-non-technical-run-observability/188-SPEC.md
@.planning/phases/188-non-technical-run-observability/188-VALIDATION.md
@.planning/phases/188-non-technical-run-observability/188-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TARGETS so the panel reconcile suites RUN</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` — the whole file. `BASELINE` is at `:92-190`, `TARGETS` at `:192-211`, `BASELINE_TOTAL` is a computed reduce at `:~190`. The header explains why pins are read from the printed `actual` column and never hand-counted.
    - `.planning/phases/188-non-technical-run-observability/188-PATTERNS.md` § "`scripts/vitest-count-gate.cjs` — ⚠ LOAD-BEARING, two knobs" — both blocks quoted verbatim at HEAD, plus the per-file consequence table.
    - `frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx` — the suite Plan 02 and Plan 04 write their falsification tests into.
    - `frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx` — the developer-view suite the Req-1 parity assertion leans on.
  </read_first>
  <action>
Add file-level entries to the `TARGETS` array for `src/components/panel/__tests__/PhaseReconcile.test.tsx`
and `src/components/panel/__tests__/PhaseTimeline.test.tsx`.

Use **file-level** entries, not the bare directory `src/components/panel/__tests__`. The directory holds
fourteen suites (`CsvTablePreview`, `FailReason`, `FilePreview`, `FilesSection`, `PendingAskCard`,
`Seam`, `TodosSection`, `VersionDiff`, `WorkspacePanel.derived`, `WorkspacePanel`, plus fixtures and
`replayHarness`), and the gate requires **0 failing**. Pulling unmeasured suites into a green gate is
how a gate acquires someone else's rot. Before choosing, run the directory form once under plain vitest
and record its failing count in the commit message; if it is 0 you MAY use the directory entry instead
and must say so with the measured number. If it is non-zero, keep the two file entries and record the
failing suites by name so a later phase can adopt them deliberately.

Write the entry comment in the house voice already used for the `WorkflowBuilderPage.describe.test.tsx`
entry: name the phase, name the two-knob distinction, and state what would have gone unguarded. Do NOT
delete or reword the existing entries or their comments.

Do NOT add `src/lib/phaseState.test.ts` or `src/pages/WorkflowRunPage.test.tsx` here — neither file
exists yet, and a `TARGETS` entry for a missing path makes the gate error rather than fail. Plan 05 and
Plan 08 each add their own entry in the same task that creates the file.
  </action>
  <acceptance_criteria>
    - `grep -c 'PhaseReconcile.test.tsx' scripts/vitest-count-gate.cjs` returns at least 1
    - `node scripts/vitest-count-gate.cjs` exits 0 and its printed `failed` is 0
    - The printed `total` is strictly greater than the 2196 recorded at `db086240` (the two panel suites now execute)
    - The existing 8 TARGETS entries are byte-unchanged: `git diff scripts/vitest-count-gate.cjs` shows insertions only inside the TARGETS array, and `git diff --numstat -- scripts/vitest-count-gate.cjs` shows 0 deletions for this task
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The panel reconcile and timeline suites execute inside the gate; `failed` is 0; total exceeds 2196.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the two running-but-unpinned node suites, and prove a pin bites</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs:92-190` — the `BASELINE` object and the computed `BASELINE_TOTAL` reduce. Note the per-file rationale comments; they are the house style for every new pin.
    - `.planning/phases/188-non-technical-run-observability/188-VALIDATION.md` § "Measured baselines" — `PhaseNode.test.tsx` (13) and `PhaseNodeCard.test.tsx` (68) RUN today and are pinned by nothing.
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — the file that will carry 188's seal-at-seven guard and the ring geometry assertions.
    - `frontend/src/components/workflows/PhaseNode.test.tsx` — the file that will carry Req 2's no-harness-vocabulary fence and Req 5's string-inequality guard.
  </read_first>
  <action>
Add `BASELINE` entries for `PhaseNode.test.tsx`, `PhaseNodeCard.test.tsx`, `PhaseReconcile.test.tsx` and
`PhaseTimeline.test.tsx` (the last two only if Task 1 brought them into `TARGETS`).

**Read every value from the script's own printed `actual` column, across two agreeing runs.** Never
hand-count `it(` literals — the script's header explains why (`definitionOps.test.ts` declares ~122
literals and runs 232 cases under `it.each`, so a hand-counted pin below the real count can never fire).
The 13 and 68 figures in VALIDATION are the *expected* results; if the printed column disagrees, the
printed column wins and the discrepancy is recorded in the commit message.

Leave `BASELINE_TOTAL` as the computed reduce. Do not hand-write the sum.

Then **observe the pin biting**: delete one whole `it(...)` block from `PhaseNodeCard.test.tsx`, run the
gate, confirm it exits non-zero with `[count-decrease]` naming that file, paste the raw output into the
plan summary, then restore the deleted block with `git checkout -- frontend/src/components/workflows/PhaseNodeCard.test.tsx`
and confirm the gate is green again. A pin nobody has watched fail is a gesture.
  </action>
  <acceptance_criteria>
    - `grep -c '"PhaseNodeCard.test.tsx"' scripts/vitest-count-gate.cjs` returns 1 and `grep -c '"PhaseNode.test.tsx"' scripts/vitest-count-gate.cjs` returns 1
    - `node scripts/vitest-count-gate.cjs` exits 0; the printed `pinned total` is strictly greater than 946
    - The printed `N/N pinned files present` count has grown by exactly the number of entries added
    - The plan summary contains the raw `[count-decrease]` output from the deliberately deleted `it(` block, naming `PhaseNodeCard.test.tsx`
    - After restore, `git status --porcelain frontend/src/components/workflows/PhaseNodeCard.test.tsx` is empty
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Four suites pinned from measured values; a `[count-decrease]` was observed and the deletion reverted; gate exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Record the pre-edit baselines in 188-VALIDATION.md</name>
  <files>.planning/phases/188-non-technical-run-observability/188-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/188-non-technical-run-observability/188-VALIDATION.md` — the whole file; § "Test Infrastructure" already carries the `db086240` figures. This task re-measures them at the CURRENT working tree and records both, rather than assuming the old numbers still hold.
    - `.planning/phases/188-non-technical-run-observability/188-RESEARCH.md` § "Open Questions — 10. Backend test-suite reality" — the 211-failed / 3296-passed figure, measured twice, and the four canvas/gate suites' individual states.
  </read_first>
  <action>
Measure and record, in a new `## Measured baselines — recorded at plan 188-01, before any production edit`
section appended below § "Test Infrastructure":

1. `node scripts/vitest-count-gate.cjs` — record `total`, `failed`, `pinned total`, and the pinned-files
   count, POST-Task-1/2.
2. `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — record the error count. The expectation is
   **33**, not 0; the root `tsconfig.json` is `{"files": [], "references": [...]}` and checks ZERO files,
   so any later gate must read *"still 33"*.
3. `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q -p no:randomly` — record failed/passed
   /skipped/xfailed/xpassed/errors and the run duration. Record it as THE baseline the phase-close
   non-attribution diff is taken against. Note explicitly that session memory's "~62-red" figure is
   REFUTED and must not be re-inherited.
4. `cd backend && ./venv/Scripts/python.exe -m pytest tests/test_revert_byte_identical.py tests/test_182_canvas_gate.py -q`
   — expected **12 passed**. This is the honest gate for the SPEC's byte-identity criterion.
5. `cd backend && ./venv/Scripts/python.exe -m pytest tests/test_thread_workflow_endpoint.py -q` — record
   the pre-existing RED **by test name and failing assertion** (RESEARCH measured
   `test_thread_workflow_state_shape` asserting `body["locked"] is True` and getting `False`). This suite
   fences `GET /threads/{id}/workflow`, which the run surface reads, so it must be provably
   non-attributable before this phase touches anything.
6. `cd backend && ./venv/Scripts/python.exe -m pytest tests/test_181_flip_on.py tests/test_182_grounding_bundle.py -q`
   — record the pre-existing reds (RESEARCH measured 1 and 2 respectively).

For each figure, record the command verbatim and the observed number. Where a number differs from the
`db086240` figure already in the file, record BOTH and say which is current — do not silently overwrite
a measured figure. Set the frontmatter `wave_0_complete: true`.
  </action>
  <acceptance_criteria>
    - `188-VALIDATION.md` contains a `## Measured baselines` section with six labelled command/result pairs, each showing the literal command
    - The backend full-suite line records a failed count and states the "~62-red" memory figure is REFUTED
    - The `test_thread_workflow_endpoint.py` line names the failing test and its assertion
    - The tsc line states the expected gate is "still 33", not 0, and names `-p tsconfig.app.json`
    - Frontmatter `wave_0_complete: true`
    - `git diff --name-only` for this task lists only `188-VALIDATION.md`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs && cd frontend && npx tsc --noEmit -p tsconfig.app.json; true</automated>
  </verify>
  <done>Six baselines measured at the current tree and recorded verbatim; `wave_0_complete: true`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate ↔ repository | The count gate is the only mechanical guard that a shipped fence still exists. Weakening it silently weakens every downstream security assertion in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-01-01 | Tampering | `scripts/vitest-count-gate.cjs` | mitigate | A test file outside `TARGETS` never runs, so a guard can be deleted with the gate green. Task 1 brings the phase's falsification suites inside `TARGETS`; Task 2 pins them; the `[count-decrease]` observation proves the pin bites rather than asserting it. |
| T-188-01-02 | Repudiation | phase baselines | mitigate | Task 3 records the pre-edit failure sets so a later red cannot be attributed to, or hidden behind, this phase. Non-attribution is proved by diffing the recorded failure set, never by a rollback run. |
| T-188-SC | Tampering | npm/pip/cargo installs | accept | This phase installs **zero** packages — `188-RESEARCH.md § Package Legitimacy Audit` records the gate as not applicable and `package.json` / `pyproject.toml` are untouched. If any later plan introduces a dependency, the Package Legitimacy Gate applies to it before the install lands. |
</threat_model>

<verification>
- `node scripts/vitest-count-gate.cjs` exits 0, `failed` 0, `total` > 2196, `pinned total` > 946
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
- `git diff --name-only HEAD -- supabase/migrations/` is empty
- No file under `frontend/src` or `backend/app` is modified by this plan
</verification>

<success_criteria>
- Both gate knobs extended; every pin read from the printed `actual` column across two agreeing runs
- One `[count-decrease]` observed and its raw output pasted into the summary, then reverted
- Six pre-edit baselines measured at the current tree and recorded in `188-VALIDATION.md`
- `wave_0_complete: true`
</success_criteria>

<output>
Create `.planning/phases/188-non-technical-run-observability/188-01-SUMMARY.md` when done.

⚠ Do NOT call `requirements.mark-complete`, `state.advance-plan` or `roadmap.update-plan-progress` —
those SDK verbs write false completion records in this project. The orchestrator owns STATE/ROADMAP writes.
</output>
