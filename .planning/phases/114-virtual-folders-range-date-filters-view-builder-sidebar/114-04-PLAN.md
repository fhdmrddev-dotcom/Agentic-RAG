---
phase: 114-virtual-folders-range-date-filters-view-builder-sidebar
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ingestion/NavRow.tsx
  - frontend/src/components/ingestion/NavRow.test.tsx
  - frontend/src/components/ingestion/FolderNode.tsx
  - frontend/src/components/ingestion/FolderTree.tsx
  - frontend/src/components/ingestion/DocumentList.tsx
autonomous: true
requirements: [VIEW-03, UX-01]

must_haves:
  truths:
    - "A single shared NavRow primitive renders the row (icon slot · name · count · reachable hover/keyboard menu); both Folders and Views build from it"
    - "Folders still render correctly after the refactor: selection, inline rename, delete-confirm, New-subfolder, global pill — no regression"
    - "The folder-tree debt is fixed: per-folder count badges everywhere, tooltip-labeled G pill, keyboard/touch-reachable actions, one soft indent guide + ~3-level indent cap"
    - "A 'Move to folder' document-row action reuses MoveToFolderDialog (no drag-drop is built)"
  artifacts:
    - path: "frontend/src/components/ingestion/NavRow.tsx"
      provides: "Shared row primitive (icon/name/count/inline-rename/hover-menu) — built ONCE, not cloned"
      min_lines: 60
    - path: "frontend/src/components/ingestion/FolderNode.tsx"
      provides: "Folder row refactored onto NavRow (recursion preserved, debt fixed)"
      contains: "NavRow"
    - path: "frontend/src/components/ingestion/DocumentList.tsx"
      provides: "Move-to-folder row action reusing MoveToFolderDialog"
      contains: "MoveToFolderDialog"
  key_links:
    - from: "frontend/src/components/ingestion/FolderNode.tsx"
      to: "frontend/src/components/ingestion/NavRow.tsx"
      via: "FolderNode delegates its row body to NavRow"
      pattern: "NavRow"
    - from: "frontend/src/components/ingestion/DocumentList.tsx"
      to: "frontend/src/components/health/MoveToFolderDialog.tsx"
      via: "the Move-to-folder action mounts the existing dialog"
      pattern: "MoveToFolderDialog"
---

<objective>
Extract ONE shared `NavRow` primitive from `FolderNode`, refactor Folders onto it (fixing the verified tree debt while extracting), and add a "Move to folder" document-row action reusing the existing `MoveToFolderDialog` — so Plan 06 can build the Views group from the FIXED row, never a clone of the flawed one (D-114-13).

Purpose: This is the frontend foundation that unblocks the Views sidebar group (Plan 06). It runs parallel to the backend (no file overlap) and carries no backend dependency. Per the design contract (sketch 033-A), Folders and Views must read as peers built from the same row.
Output: `NavRow.tsx` (+ test), `FolderNode`/`FolderTree` refactored onto it with the debt fixed, and the Move-to-folder action on the Documents-page document row.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-CONTEXT.md
@.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-PATTERNS.md
@./.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- The row shape to extract + the reuse targets. Extracted from the codebase. -->

From frontend/src/components/ingestion/FolderNode.tsx (the row body to extract — :110-256):
- Row container + selected styling (:110-120): bg-primary/10 when selected, hover:bg-accent/60 otherwise
- Icon slot (:142-148): amber FolderIcon (text-amber-500/70) / FolderOpen when expanded+selected
- Inline rename (:151-170): editValue state + editInputRef; Enter saves, Esc cancels, onBlur commits
- G pill (:183-187): opaque single letter today → wrap in Tooltip "Global — shared with everyone"
- Hover action menu (:189-255): DropdownMenu + MoreHorizontal; today opacity-0 group-hover:opacity-100 (TOUCH-INVISIBLE — fix)
- Recursion (:296-322) stays in FolderNode; only the row body delegates to NavRow

From frontend/src/components/ingestion/FolderTree.tsx:
- Group header (:115-132): uppercase "Folders" label + New-folder button
- Root node (:135-150): the ONLY row showing a count today (:147-149) → add counts everywhere

From frontend/src/components/health/MoveToFolderDialog.tsx + HealthDocumentRow.tsx (the reuse pattern, PATTERNS.md §"Move-to-folder document-row action"):
```tsx
const [moveOpen, setMoveOpen] = useState(false)
// trigger → onClick={() => setMoveOpen(true)}, tooltip "Move to folder"
<MoveToFolderDialog open={moveOpen} documentId={doc.id} documentName={doc.filename}
  onClose={() => setMoveOpen(false)} onMoved={() => setMoveOpen(false)} />
// calls PATCH /documents/{id}/move — zero net-new backend
```

Design contract: SKILL.md §"Folder-tree polish = ONE shared NavRow (033-A)" + §"Saved Views ... Move to folder row action" + references/saved-views-and-left-nav.md. Terminology: "saved filters," never "query." Differentiator = icon + count + no New-subfolder action (NOT drop rejection).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared NavRow primitive (fix the debt while extracting)</name>
  <read_first>
    - frontend/src/components/ingestion/FolderNode.tsx (the file being refactored — the full row body :110-256 to extract; the touch-invisible action menu at :191; the opaque G pill at :183; the recursion at :296-322 to preserve)
    - 114-PATTERNS.md §"NavRow.tsx (NEW)" + §"FolderNode.tsx + FolderTree.tsx (MODIFY)" (the extraction map + debt-fix checklist)
    - ./.claude/skills/sketch-findings-agentic-rag/SKILL.md §"Folder-tree polish = ONE shared NavRow (033-A)" (the design contract: counts everywhere, labeled G, keyboard/touch-reachable actions, one soft indent guide, ~3-level indent cap)
  </read_first>
  <action>
    Create `frontend/src/components/ingestion/NavRow.tsx` — a single shared row primitive with prop slots: `icon` (a lucide component + color — Folders pass amber FolderIcon, Views will pass a funnel), `name` (with inline-rename support — the editValue/editInputRef idiom from FolderNode:151-170, Enter saves / Esc cancels / blur commits), `count` (a `<span className="text-xs text-muted-foreground ml-auto">{count}</span>` rendered on EVERY row, D-114-13/8), `isSelected` styling (bg-primary/10 selected vs hover:bg-accent/60), an optional `isGlobal` G pill wrapped in a Tooltip labeled "Global — shared with everyone" (FIX the opaque pill at FolderNode:183), and an `actions` slot rendering a DropdownMenu + MoreHorizontal that is keyboard/touch-reachable (FIX the `opacity-0 group-hover:opacity-100` touch-invisibility at FolderNode:191 — keep a hover affordance but ensure the trigger is focusable + reachable on touch, e.g. always-rendered at reduced opacity or revealed on focus-within). Keep the indent/tree-guide as a SINGLE soft indent guide (replace the dense hand-drawn branch lines at FolderNode:90-108) with a ~3-level indent cap (deeper nesting leans on the existing FolderBreadcrumb — do not render indent beyond the cap). NavRow holds NO recursion and NO folder-specific business logic — it is a pure presentational row. Write `frontend/src/components/ingestion/NavRow.test.tsx` (vitest + Testing Library): renders icon/name/count; selected styling applied when isSelected; inline rename commits on Enter and cancels on Esc; the G pill tooltip renders when isGlobal; the action menu trigger is reachable (present in the DOM, focusable — not opacity-0-only).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- NavRow</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- NavRow` is GREEN
    - `NavRow.tsx` exists and is ≥ 60 lines; it renders a count slot on every row (not just Root)
    - The G pill is wrapped in a Tooltip with the "Global — shared with everyone" label (grep confirms the tooltip label text)
    - The action-menu trigger is NOT gated behind `opacity-0 group-hover:opacity-100` alone — the test asserts the trigger is in the DOM and focusable (keyboard/touch-reachable, D-114-13)
    - NavRow contains no `FolderNode` recursion and no folder CRUD logic (pure row primitive)
  </acceptance_criteria>
  <done>NavRow is a shared, debt-fixed row primitive (counts everywhere, labeled G pill, reachable actions, one soft indent guide) ready for both Folders and Views.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor FolderNode/FolderTree onto NavRow (no regression) + add per-folder counts</name>
  <read_first>
    - frontend/src/components/ingestion/FolderNode.tsx (the file being refactored — delegate the row body to NavRow, keep the recursion :296-322, the delete-confirm :258-283, the subfolder-create input :285-292)
    - frontend/src/components/ingestion/FolderTree.tsx (the file being refactored — the group header :115-132, the Root node :135-150 which already shows a count, the FolderNode mapping :170-194)
    - frontend/src/components/ingestion/NavRow.tsx (the primitive from Task 1)
    - 114-PATTERNS.md §"FolderNode.tsx + FolderTree.tsx (MODIFY)"
  </read_first>
  <action>
    Refactor `FolderNode.tsx` so its visible row body delegates to `NavRow` (passing the amber folder icon, the folder name + inline-rename handlers, a per-folder document count, the is_global flag, and the New-subfolder + Rename + Delete actions in the menu) — KEEP the recursion (:296-322), the inline delete-confirmation (:258-283), and the subfolder-create input (:285-292) in FolderNode. Refactor `FolderTree.tsx` so the Root node also renders through NavRow (it already has a count) and the group header stays. Add per-folder document counts (D-114-13 — parity with the mandated View count): pass each folder's document count into NavRow (source the count from the existing folder data the tree already receives, or a count map threaded from the page — do NOT add a per-folder resolve call that would degrade the sidebar at ~10k docs; if no count is readily available, render 0/empty gracefully). Apply the ~3-level indent cap + single soft indent guide via NavRow's indent slot. Do NOT change folder CRUD endpoints or the selection contract — this is a pure presentational refactor. Re-run the existing FolderNode/FolderTree tests and confirm no regression (selection, rename, delete-confirm, New-subfolder, global pill all still work).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- FolderNode FolderTree NavRow</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- FolderNode FolderTree NavRow` is GREEN (existing folder tests pass post-refactor — no regression)
    - `grep -c 'NavRow' frontend/src/components/ingestion/FolderNode.tsx` ≥ 1 (FolderNode delegates to NavRow)
    - FolderNode recursion (`FolderNode` self-reference for children) is preserved; the inline delete-confirm and subfolder-create still render
    - Folders render a per-folder count badge (not only Root) — the dense hand-drawn branch lines (old :90-108) are replaced by NavRow's single soft indent guide
    - `cd frontend && npx tsc --noEmit` reports no new type errors in the touched files
  </acceptance_criteria>
  <done>Folders render through the shared NavRow with per-folder counts and the fixed indent/actions, with the existing folder tests green (no regression).</done>
</task>

<task type="auto">
  <name>Task 3: Add the "Move to folder" document-row action (reuse MoveToFolderDialog)</name>
  <read_first>
    - frontend/src/components/ingestion/DocumentList.tsx (the file being modified — the action-button cluster :334-367, the row-click→panel pattern :301-326, the static 7-col table header :266-276)
    - frontend/src/components/health/MoveToFolderDialog.tsx (the dialog to reuse — calls PATCH /documents/{id}/move)
    - frontend/src/components/health/HealthDocumentRow.tsx (the exact reuse pattern — moveOpen state :35, trigger :146-158, dialog mount :190-196)
    - 114-PATTERNS.md §"Move-to-folder document-row action (D-114-14)"
  </read_first>
  <action>
    Add a "Move to folder" action to the Documents-page document row in `DocumentList.tsx`, reusing the EXACT `HealthDocumentRow` pattern: a `moveOpen` state per row (or a single dialog keyed to the active row's document id), a trigger button in the existing action cluster (lucide `FolderInput`, tooltip "Move to folder"), and the `MoveToFolderDialog` mounted with the row's `documentId` + `documentName`, `onMoved` closing the dialog (and refreshing the list if the page provides a refetch callback). This reuses `MoveToFolderDialog` + `PATCH /documents/{id}/move` — ZERO net-new backend. Build NO drag-drop (the original "rejects drag-drop" criterion is struck — D-114-14; no file drag-drop exists on the Documents page today). Keep the existing row-click→detail-panel behavior intact. Add or extend a DocumentList test asserting the Move-to-folder trigger renders and opens the dialog.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- DocumentList</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- DocumentList` is GREEN
    - `grep -c 'MoveToFolderDialog' frontend/src/components/ingestion/DocumentList.tsx` ≥ 1 (the existing dialog is reused, not re-implemented)
    - The Move-to-folder trigger renders in the document-row action cluster with a "Move to folder" tooltip; clicking it opens the dialog (test asserts)
    - No drag-drop handlers are added (no `onDragStart`/`onDrop` introduced — `grep -c 'onDrop\|onDragStart' DocumentList.tsx` unchanged from base)
    - The existing row-click→detail-panel behavior is preserved (no regression in DocumentList tests)
  </acceptance_criteria>
  <done>The Documents-page document row exposes a "Move to folder" action reusing MoveToFolderDialog + PATCH /documents/{id}/move, with no drag-drop built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document row → PATCH /documents/{id}/move | The Move-to-folder action triggers an owner-scoped server-side move; the row only supplies the document id it already renders for the caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-04-01 | Elevation of Privilege | Move-to-folder action | mitigate | The action reuses the EXISTING `MoveToFolderDialog` + `PATCH /documents/{id}/move` — an already-shipped owner-scoped endpoint (Health page). No new backend, no new authz surface; the row only passes a document id the caller is already authorized to render. |
| T-114-04-02 | Information Disclosure | per-folder count rendering | accept | Folder counts render from data the tree already holds for the caller; no new query that could surface another user's totals. Low risk — counts are caller-scoped by the existing folder fetch. |
| T-114-04-SC | Tampering | npm/pip installs | accept | This plan installs NO packages (composes existing shadcn/lucide/MoveToFolderDialog primitives). No supply-chain surface. |
</threat_model>

<verification>
- `cd frontend && npm run test -- NavRow FolderNode FolderTree DocumentList` — all GREEN (folder refactor regression-free; NavRow + Move-to-folder pass).
- `cd frontend && npx tsc --noEmit` — no new type errors in the touched files.
- NavRow is the single shared row; Folders build from it; the Move-to-folder action reuses the existing dialog with no drag-drop.
</verification>

<success_criteria>
- A single shared `NavRow` primitive exists (counts everywhere, labeled G pill, reachable actions, one soft indent guide + ~3-level cap).
- Folders render through NavRow with no regression; per-folder counts added.
- The Documents row exposes a Move-to-folder action reusing MoveToFolderDialog (no drag-drop).
</success_criteria>

<output>
Create `.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-04-SUMMARY.md` when done.
</output>
