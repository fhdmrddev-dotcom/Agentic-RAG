---
phase: 162-personal-org-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/105_personal_org_backfill.sql
autonomous: true
requirements: [MIG-01]

must_haves:
  truths:
    - "MIG-01 / D-01: every existing auth.users row gets exactly ONE personal org + default 'General' dept + org-admin membership, created via create_org_with_default_dept() behind a NOT-EXISTS(org_members) idempotency gate"
    - "MIG-01 / D-02 / D-03: the extended handle_new_user DEFINER trigger auto-provisions a personal org for FUTURE signups and can NEVER abort the auth.users insert (defensive EXCEPTION WHEN OTHERS swallow)"
    - "MIG-01 / D-07 / D-09: org_id is backfilled across all 35 targets by ONE reusable batched PROCEDURE with per-batch COMMIT — 30 via own user_id/created_by, 4 owner-less children via parent FK, in 3 ordered waves"
    - "MIG-01 / D-08 / D-11: a RAISE-EXCEPTION zero-NULL guard precedes EVERY SET NOT NULL flip; operator_audit_log is EXCLUDED from backfill and stays nullable (no owning auth.users)"
    - "MIG-01 / D-04: the file is HANDS-OFF is_global / is_system — it never reads, renames, or writes those columns, so no shared resource can lose visibility"
    - "MIG-01 / D-06 / D-10: migration 105 is idempotent / re-paste-safe and targets exactly the 35 user-facing org_id tables (document_chunks + skill_embeddings deferred to 163)"
  artifacts:
    - path: "supabase/migrations/105_personal_org_backfill.sql"
      provides: "The complete idempotent personal-org backfill migration (provisioning + trigger + batched backfill + self-guarded NOT-NULL flips)"
      contains: "CREATE OR REPLACE PROCEDURE public._mig105_backfill"
  key_links:
    - from: "supabase/migrations/105_personal_org_backfill.sql"
      to: "public.create_org_with_default_dept"
      via: "personal-org creation loop + handle_new_user trigger"
      pattern: "create_org_with_default_dept"
    - from: "supabase/migrations/105_personal_org_backfill.sql"
      to: "public.org_members"
      via: "membership insert + backfill resolver join"
      pattern: "org_members"
---

<objective>
Author migration **105_personal_org_backfill.sql** — the complete, idempotent, re-paste-safe SQL data migration that (1) creates one personal org + default `'General'` department + `org-admin` membership for every existing `auth.users` row, (2) extends the `handle_new_user` DEFINER trigger so future signups auto-provision the same, (3) backfills `org_id` non-NULL across all **35** user-facing target tables via a batched procedure with per-batch `COMMIT`, and (4) flips those columns `NOT NULL` behind self-verifying zero-NULL guards.

Purpose: populate the `org_id` substrate + memberships that Phase 163's RLS crux consumes — with zero user action and zero data loss (Deep Mode stays byte-identical; RLS is still service-role-bypassed until 163).

Output: the single migration file `supabase/migrations/105_personal_org_backfill.sql`. **This plan AUTHORS only — it does NOT apply the migration and does NOT commit the SQL** (D-06 requires 105 + the regenerated `full-schema.sql` in ONE commit, which Plan 02 performs post-apply; this mirrors the Phase-161 / mig-104 author-then-apply split).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-personal-org-backfill/162-CONTEXT.md
@.planning/phases/162-personal-org-backfill/162-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Live-verified against the PG 17.6 catalog @ :54322 at plan time. -->
<!-- Use these directly — no codebase exploration needed. -->

From supabase/migrations/104_org_dept_role_schema.sql (the reusable substrate — MIRROR its idioms):
  create_org_with_default_dept(p_name text, p_subscription_tier text DEFAULT NULL,
                               p_default_dept_name text DEFAULT 'General') RETURNS uuid
    -- service_role/owner-only SECDEF; creates 1 org + 1 default dept; does NOT create membership.
  org_members_org_user_unique UNIQUE (org_id, user_id)          -- for ON CONFLICT
  org_members.role CHECK: 'super-admin' | 'org-admin' | 'dept-admin' | 'member'
    -- super-admin is service-role-only (CR-02); 162 seeds 'org-admin' only.

From the live catalog (VERIFIED at plan time — do NOT re-query, do NOT assume stale columns):
  -- profiles PK = id  -> the trigger's profiles insert can safely ON CONFLICT (id) DO NOTHING
  -- handle_new_user() TODAY: SECURITY DEFINER, SET search_path TO 'public', inserts ONLY a profiles row.
  --   trigger on_auth_user_created AFTER INSERT ON auth.users already exists — only the FUNCTION body changes.
  -- organizations cols = id, name, slug, subscription_tier, add_ons, settings, created_at, updated_at -> NO is_personal.
  -- org_members cols = id, org_id, user_id, role, created_at, updated_at -> NO joined_via.
  --   The idempotency gate MUST NOT reference is_personal / joined_via / slug (they do not exist — Pitfall 4).
  -- Pre-162 state: auth.users=8, organizations=0, org_members=0, departments=0, 0 NULL emails.

  -- The 35 backfill targets + owner columns (VERIFIED live):
  --   28 direct user_id (NOT NULL): audit_log, classification_rules, code_executions, document_images,
  --     document_relationships, document_tables, document_views, documents, eval_ratings, eval_results,
  --     eval_runs, folders, harness_audit, message_feedback, messages, pdf_extraction_runs, runs,
  --     sandbox_files, skill_files, skill_proposals, skill_publish_overrides, skill_test_cases,
  --     skill_versions, skills, threads, tuner_runs, user_memory, user_settings
  --   2 direct created_by (NOT NULL): workflow_definitions, workspace_files
  --   1 nullable user_id (0 NULL live -> resolve via user_id): metadata_field_definitions
  --   4 owner-less / parent-FK children: workflow_runs, todos, workspace_file_versions, workflow_phases
  --   EXCLUDED (NOT targets): operator_audit_log (no owning auth.users — stays nullable),
  --     document_chunks + skill_embeddings (-> Phase 163), operator_users/profiles/app_settings/
  --     model_capabilities_overrides (no org_id), org-infra departments/dept_members/org_invitations/
  --     org_members/sso_configs (already NOT NULL, written by the §A provisioning loop).

  -- Verified parent-FK edges (for the 4 children):
  --   todos.thread_id -> threads.id
  --   workflow_runs.thread_id -> threads.id
  --   workspace_file_versions.workspace_file_id -> workspace_files.id
  --   workflow_phases.workflow_run_id -> workflow_runs.id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author §A personal-org creation loop + §D handle_new_user trigger extension</name>
  <files>supabase/migrations/105_personal_org_backfill.sql</files>
  <read_first>
    - .planning/phases/162-personal-org-backfill/162-RESEARCH.md — §Personal-Org Creation & Idempotency (the exact gated DO loop), §handle_new_user Extension (the exact defensive body), §create_org_with_default_dept (signature + "the caller MUST insert membership separately")
    - .planning/phases/162-personal-org-backfill/162-CONTEXT.md — D-01, D-02, D-03
    - supabase/migrations/104_org_dept_role_schema.sql — the helper + the re-paste-safe idiom style (header discipline, ON CONFLICT DO NOTHING, CREATE OR REPLACE) to MIRROR
    - CLAUDE.md — numbered-migration rules (digits-only filename, apply via SQL editor NOT db push, same-commit, cloud-parity)
  </read_first>
  <action>
    Create `supabase/migrations/105_personal_org_backfill.sql`. Open with a header comment block mirroring mig 104's: idempotent / re-paste-safe; apply by pasting into the LOCAL Supabase SQL editor with psycopg2 `autocommit=True` @ :54322 OR per-`CALL` execution — NEVER `db push`/`db reset`; 105 joins the pending-cloud set (099–104 + `SECRETS_ENCRYPTION_KEY`) applied only at the next operator-gated prod push; regenerate `full-schema.sql` + commit both same-commit (D-06); digits-only filename (a `105b` suffix is silently skipped).

    §A — Personal-org creation loop: a NORMAL transactional `DO` block (small, bounded by user count — NO per-iteration COMMIT) iterating `auth.users au` filtered by the idempotency gate `NOT EXISTS (SELECT 1 FROM public.org_members m WHERE m.user_id = au.id)` (Option B, membership-existence; do NOT reference `is_personal`/`joined_via`/`slug` — they do not exist, D-01 / Pitfall 4). For each user: `v_org_id := public.create_org_with_default_dept(u.email || '''s Organization', NULL, 'General')` then `INSERT INTO public.org_members (org_id, user_id, role) VALUES (v_org_id, u.id, 'org-admin') ON CONFLICT (org_id, user_id) DO NOTHING`. Org name literal `"{email}'s Organization"`, tier `NULL`, dept `'General'`, role `'org-admin'` per D-01.

    §D — Extend `handle_new_user`: `CREATE OR REPLACE FUNCTION public.handle_new_user()` KEEPING `SECURITY DEFINER` + `SET search_path TO 'public'` (do NOT drop either — the EoP mitigation). Harden the existing profiles insert with `ON CONFLICT (id) DO NOTHING` (profiles PK = `id`, verified). Then a DEFENSIVE inner `BEGIN … EXCEPTION WHEN OTHERS THEN RAISE WARNING … END` sub-block wrapping the SAME NOT-EXISTS-gated org creation + `org-admin` membership insert, using `COALESCE(new.email, new.id::text) || '''s Organization'` for the name. The swallow guarantees org-creation failure can never abort the `auth.users` INSERT (D-03 / Pitfall 5). Do NOT create the `on_auth_user_created` trigger — it already exists; only the function body changes. Reuse the exact §A gate + membership shape so the two creation paths are identical logic.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && f=supabase/migrations/105_personal_org_backfill.sql && grep -q "CREATE OR REPLACE FUNCTION public.handle_new_user" $f && grep -q "EXCEPTION WHEN OTHERS" $f && grep -q "SET search_path TO 'public'" $f && grep -q "create_org_with_default_dept" $f && grep -q "ON CONFLICT (org_id, user_id) DO NOTHING" $f && grep -q "NOT EXISTS" $f && ! grep -Eq "is_personal|joined_via|'personal-" $f && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/105_personal_org_backfill.sql` exists with a mig-104-style header (idempotent; apply-via-SQL-editor-NOT-db-push; cloud-parity 105-joins-pending; D-06 same-commit).
    - Contains `CREATE OR REPLACE FUNCTION public.handle_new_user`, `SECURITY DEFINER`, `SET search_path TO 'public'`, and an `EXCEPTION WHEN OTHERS` swallow around the org-creation block.
    - The profiles insert uses `ON CONFLICT (id) DO NOTHING`; the membership insert uses `ON CONFLICT (org_id, user_id) DO NOTHING`.
    - The §A loop iterates `auth.users` and is gated by `NOT EXISTS (... org_members ... WHERE ... user_id ...)`.
    - Does NOT contain any of `is_personal`, `joined_via`, or a `'personal-` slug literal (Pitfall 4).
    - Does NOT contain `CREATE TRIGGER on_auth_user_created` (the trigger already exists; only the function body is replaced).
  </acceptance_criteria>
  <done>§A personal-org loop + §D defensive trigger authored; both idempotent (NOT-EXISTS gate + ON CONFLICT); the trigger keeps SECDEF + pinned search_path and can never abort signup.</done>
</task>

<task type="auto">
  <name>Task 2: Author §B batched-backfill procedure + the 35-table resolver CALLs (3 ordered waves)</name>
  <files>supabase/migrations/105_personal_org_backfill.sql</files>
  <read_first>
    - .planning/phases/162-personal-org-backfill/162-RESEARCH.md — §Batched Backfill (the reusable procedure shape + the "invalid transaction termination" finding), §Parent-FK Resolution Graph (the Group-A resolver + the 4-child JOINs + wave order), §Backfill-Target Table Set (the 35-table decomposition + the 3 discrepancy flags)
    - .planning/phases/162-personal-org-backfill/162-CONTEXT.md — D-07, D-09, D-10
    - supabase/migrations/104_org_dept_role_schema.sql — §Section 4 (the 23-table sweep = part of the target list)
    - supabase/migrations/096_org_id_stub_sweep.sql — the "children inherit org via parent FK" precedent
    - The `<interfaces>` block above (the authoritative 35-target list + owner columns + parent-FK edges)
  </read_first>
  <action>
    Append §B to the migration. First `CREATE OR REPLACE PROCEDURE public._mig105_backfill(p_sql text, p_batch int DEFAULT 10000)` that LOOPs: `EXECUTE p_sql USING p_batch;` -> `GET DIAGNOSTICS v_rows = ROW_COUNT;` -> `RAISE NOTICE 'batch % -> % rows', ...;` -> `COMMIT;` -> `EXIT WHEN v_rows = 0;` (the exact shape in RESEARCH §Batched Backfill — the per-batch `COMMIT` is the lock-storm mitigation). Do NOT author a bare `DO $$` backfill block — the batching MUST be the PROCEDURE (the §A `DO` block is the ONLY DO block, and it does no backfill).

    Then `CALL public._mig105_backfill($SQL$…$SQL$)` once per target with the resolver SQL, in THREE ordered waves (children resolve from the parent's ALREADY-backfilled org_id — order is load-bearing):

    Wave 1 — the 30 direct-owner targets via own `user_id` (or `created_by` for `workflow_definitions` + `workspace_files`) PLUS `metadata_field_definitions` via `user_id` (nullable but 0 NULL live): the Group-A resolver shape `UPDATE public.<t> t SET org_id = pm.org_id FROM public.org_members pm WHERE pm.user_id = t.<owner_col> AND t.org_id IS NULL AND t.id IN (SELECT id FROM public.<t> WHERE org_id IS NULL LIMIT $1)`. Name EACH of the 31 tables from the `<interfaces>` list explicitly — do not abbreviate the set.

    Wave 2 — the 3 parent-FK children/nullable-owner via their parent's org_id: `workflow_runs` via `thread_id -> threads.org_id`; `todos` via `thread_id -> threads.org_id`; `workspace_file_versions` via `workspace_file_id -> workspace_files.org_id`. Resolver shape `UPDATE public.<child> c SET org_id = p.org_id FROM public.<parent> p WHERE p.id = c.<fk> AND c.org_id IS NULL AND p.org_id IS NOT NULL AND c.id IN (SELECT id FROM public.<child> WHERE org_id IS NULL LIMIT $1)`.

    Wave 3 — `workflow_phases` via `workflow_run_id -> workflow_runs.org_id` (MUST come after Wave 2's `workflow_runs` CALL).

    Every UPDATE keeps `WHERE org_id IS NULL` (idempotent re-run). EXCLUDE `operator_audit_log`, `document_chunks`, `skill_embeddings` — they are NOT backfill targets. Do NOT touch `is_global`/`is_system` (D-04 — resolution is purely via `user_id`/`created_by`/parent org_id).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && f=supabase/migrations/105_personal_org_backfill.sql && grep -q "CREATE OR REPLACE PROCEDURE public._mig105_backfill" $f && grep -qi "COMMIT" $f && for t in audit_log classification_rules code_executions document_images document_relationships document_tables document_views documents eval_ratings eval_results eval_runs folders harness_audit message_feedback messages metadata_field_definitions pdf_extraction_runs runs sandbox_files skill_files skill_proposals skill_publish_overrides skill_test_cases skill_versions skills threads tuner_runs user_memory user_settings workflow_definitions workspace_files workflow_runs todos workspace_file_versions workflow_phases; do grep -q "public.$t " $f || { echo "MISSING $t"; exit 1; }; done && ! grep -Eq "operator_audit_log|document_chunks|skill_embeddings" $f && awk '/public.workflow_runs /{r=NR} /public.workflow_phases /{p=NR} END{exit !(r && p && r<p)}' $f && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Contains `CREATE OR REPLACE PROCEDURE public._mig105_backfill` with an internal `COMMIT` inside the loop and `EXIT WHEN` at 0 rows.
    - All 35 targets appear in a resolver/CALL (the 31 Wave-1 direct + `workflow_runs`, `todos`, `workspace_file_versions`, `workflow_phases`).
    - `operator_audit_log`, `document_chunks`, `skill_embeddings` do NOT appear as backfill targets.
    - The `workflow_phases` CALL appears AFTER the `workflow_runs` CALL (Wave 3 after Wave 2).
    - Every backfill UPDATE carries `WHERE org_id IS NULL` and a `LIMIT $1` batch bound; no bare `DO $$` backfill loop exists (the only `DO` block is §A).
    - No reference to `is_global`/`is_system` in any resolver (D-04 hands-off).
  </acceptance_criteria>
  <done>The reusable batched procedure + all 35 resolver CALLs authored in correct 3-wave order; batching releases locks via per-batch COMMIT; re-run-safe via WHERE org_id IS NULL.</done>
</task>

<task type="auto">
  <name>Task 3: Author §C pre-flip NULL census + self-guarded NOT-NULL flips + DROP the procedure</name>
  <files>supabase/migrations/105_personal_org_backfill.sql</files>
  <read_first>
    - .planning/phases/162-personal-org-backfill/162-RESEARCH.md — §NOT-NULL Flip (the proven RAISE-guard + per-table decision table + the "ALL CALLs COMMIT before ANY flip" ordering), §is_global / is_system HANDS-OFF (0 orphan shared rows), Assumptions A1 (metadata_field_definitions cloud caveat)
    - .planning/phases/162-personal-org-backfill/162-CONTEXT.md — D-08, D-11
    - .planning/phases/162-personal-org-backfill/162-VALIDATION.md — SC#2 (the post-flip nullable-set expectation)
  </read_first>
  <action>
    Append §C, which runs AFTER all §B CALLs have COMMITted (never flip first — the load-bearing ordering).

    First, an operator-visibility pre-flip NULL census: a single `SELECT … UNION ALL …` over ALL 35 targets returning `(table_name, count(*) FILTER (WHERE org_id IS NULL))` ordered by nulls desc (RESEARCH §NOT-NULL Flip census shape) — every count must read 0 before the flips.

    Then, for EACH of the 35 targets, a self-guarded flip: a `DO $$ BEGIN IF EXISTS (SELECT 1 FROM public.<t> WHERE org_id IS NULL) THEN RAISE EXCEPTION 'MIG105 <t>: % rows still NULL — NOT-NULL flip aborted', (SELECT count(*) …); END IF; END $$;` immediately followed by `ALTER TABLE public.<t> ALTER COLUMN org_id SET NOT NULL;`. The guard IS the "verified zero-NULL check" (D-08) — the DB, not a human, enforces it. `metadata_field_definitions` gets the SAME guarded flip (NOT an unconditional flip): locally 0 NULL-owner rows so it flips; if cloud holds NULL-owner global system field-defs the guard FIRES and the operator decides at apply time (D-11 fallback = keep nullable) — flag this in a comment (Assumption A1). `operator_audit_log` gets NO flip statement at all — it stays NULLABLE (D-11: no owning auth.users; add a comment that Phase 163's RLS predicate on `operator_audit_log` MUST handle NULL org_id explicitly).

    Finally `DROP PROCEDURE public._mig105_backfill(text, int);`. Add a closing comment: is_global/is_system were never touched (D-04); the is_global -> is_org_shared RENAME stays in Phase 165 (D-05 handoff).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && f=supabase/migrations/105_personal_org_backfill.sql && flips=$(grep -c "ALTER COLUMN org_id SET NOT NULL" $f) && guards=$(grep -c "RAISE EXCEPTION" $f) && test "$flips" -eq 35 && test "$guards" -ge 35 && ! grep -q "public.operator_audit_log ALTER COLUMN org_id SET NOT NULL" $f && grep -q "DROP PROCEDURE public._mig105_backfill" $f && grep -qi "metadata_field_definitions" $f && echo "PASS flips=$flips guards=$guards"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 35 `ALTER TABLE public.<t> ALTER COLUMN org_id SET NOT NULL` statements — one per backfill target.
    - A `RAISE EXCEPTION` zero-NULL guard immediately precedes EVERY `SET NOT NULL` (guard count >= 35).
    - `operator_audit_log` has NO `SET NOT NULL` (stays nullable) and a comment notes 163 must handle its NULL org_id.
    - `metadata_field_definitions`'s flip is guarded (not unconditional), with a comment on the cloud NULL-owner caveat (A1).
    - A pre-flip census `SELECT` covering the 35 targets exists before the flips.
    - `DROP PROCEDURE public._mig105_backfill(text, int)` present; a closing comment records is_global HANDS-OFF (D-04) + the 165 rename handoff (D-05).
  </acceptance_criteria>
  <done>Self-guarded flips for all 35 targets authored (operator_audit_log excluded, metadata_field_definitions guarded-conditional); the DB enforces zero-NULL; the batching procedure is dropped; the whole file is re-paste-safe end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL-editor/psycopg2 session -> DB (owner/service_role) | The migration runs as owner and BYPASSES RLS by design (RLS enforcement is Phase 163). Correctness — not RLS — is the guard here. |
| `create_org_with_default_dept` / `handle_new_user` (SECDEF) | Run as their owner `postgres` with a pinned `search_path`; a privilege boundary crossed by definer rights. |
| `auth.users` INSERT (signup) -> `handle_new_user` trigger | An uncaught trigger exception aborts the triggering INSERT (breaks signup) — an availability boundary. |
| `_mig105_backfill(p_sql)` dynamic SQL | `p_sql` is migration-authored ONLY (never user input) — no untrusted-input boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-01 | Tampering / integrity | §A personal-org loop + §D trigger re-run | mitigate | `NOT EXISTS (org_members WHERE user_id=…)` gate wraps every create; membership insert `ON CONFLICT (org_id,user_id) DO NOTHING`. Idempotent by construction (Task 1). |
| T-162-02 | Denial of Service | §B large-table backfill UPDATE | mitigate | One `_mig105_backfill` PROCEDURE loops `UPDATE … LIMIT $1 (~10k)` with a per-batch `COMMIT` that releases the lock window (proven on PG 17.6) (Task 2). |
| T-162-03 | Availability / integrity | §C NOT-NULL flip on incomplete data | mitigate | A `RAISE EXCEPTION` zero-NULL guard precedes EVERY `SET NOT NULL`; ALL §B CALLs COMMIT before ANY flip. The flip is structurally impossible on dirty data (Task 3). |
| T-162-04 | Information disclosure / integrity | is_global / is_system sharing lost | mitigate | HANDS-OFF is_global/is_system (D-04) — resolution is purely via user_id/created_by/parent org_id; 0 orphan shared rows verified (1 global folder, 1 global skill, 1 system skill-creator, 15 global workflow_defs all resolve to their owner). No flip can orphan a shared resource (Tasks 2+3). |
| T-162-05 | Denial of Service | §D trigger aborts signup | mitigate | Inner `BEGIN … EXCEPTION WHEN OTHERS THEN RAISE WARNING … END` swallows any org-creation failure so the `auth.users` INSERT always succeeds (Task 1). |
| T-162-06 | Elevation of Privilege | SECDEF search_path hijack | mitigate | `SET search_path TO 'public'` KEPT on the trigger (verify preserved via `CREATE OR REPLACE`); `create_org_with_default_dept` retains its own pinned search_path + service_role/owner grant (CR-01) (Task 1). |
| T-162-SC | Supply chain (Tampering) | package installs | accept (N/A) | This phase installs ZERO packages (no npm/pip/cargo) — no Package Legitimacy Gate applies; uses only Postgres 17.6 built-ins + the mig-104 helper. |

Block-on severity = HIGH: no HIGH threat is left unmitigated — T-162-01…06 all carry a `mitigate` disposition traced to a concrete task above.
</threat_model>

<verification>
This plan is authoring-only; the DB-state proof (SC#1–4) runs in Plan 02 after apply. At authoring time, verify the file's STRUCTURE:
- Personal-org loop + defensive trigger present, idempotent, no stale columns (Task 1 automated grep).
- Batched procedure + all 35 resolver CALLs in correct 3-wave order; exclusions honored (Task 2 automated grep).
- 35 guarded NOT-NULL flips; operator_audit_log excluded; procedure dropped (Task 3 automated grep).
- Whole file is re-paste-safe (gate-on-no-personal-org, WHERE org_id IS NULL, ON CONFLICT DO NOTHING, CREATE OR REPLACE, IF EXISTS guards).
</verification>

<success_criteria>
- `supabase/migrations/105_personal_org_backfill.sql` exists and is complete: §A provisioning loop, §B batched procedure + 35 CALLs, §C self-guarded flips + DROP, §D defensive trigger.
- All three tasks' automated grep gates return PASS.
- The file references `create_org_with_default_dept` and `org_members`, references NO stale columns (`is_personal`/`joined_via`/`slug`), and is HANDS-OFF `is_global`/`is_system`.
- The migration is NOT applied and NOT committed here — Plan 02 owns apply + regenerate + same-commit.
</success_criteria>

<output>
Create `.planning/phases/162-personal-org-backfill/162-01-SUMMARY.md` when done. Record: the final target-table count authored (must be 35), any executor discretion taken (batch size, procedure naming, the metadata_field_definitions guard note), and an explicit note that the SQL file is authored-but-uncommitted (Plan 02 applies + commits it same-commit with the regenerated full-schema.sql).
</output>
