---
phase: 261-an-expert-you-can-author
plan: 03
type: execute
wave: 2
depends_on: [261-01]
files_modified:
  - backend/app/services/tool_dispatcher.py
  - backend/app/services/run_producer.py
  - backend/app/services/agent_loop.py
  - backend/tests/unit/test_261_expert_runtime_scoping.py
autonomous: true
requirements: [PACK-02, PACK-07]
mutates_local_db: false

must_haves:
  truths:
    - "Deliverable-producing tools (execute_code, workspace_write, render_template, ask_user) are preserved in effective_tools as an additive floor (D-v4.3-02, SEED-303 S6)."
    - "Knowledge composition defaults to Union (scope_mode='biased', S4): thread folder + expert folders are combined so existing chat context is not wiped out (D-v4.3-01)."
    - "Strict isolation (scope_mode='restricted', S5) restricts retrieval exclusively to expert folders and states the cost upfront."
    - "scoped_folder_path injected into the system prompt is synchronized with effective folders, eliminating prompt/retrieval desync (BUG-260920-01)."
    - "agent_loop.py contains strictly zero 'if expert:' branches; all scoping is handed to the loop via RunContext data."
  artifacts:
    - path: "backend/app/services/run_producer.py"
      provides: "Union scope merging, scope_mode resolution, and synchronized scoped_folder_path generation"
    - path: "backend/tests/unit/test_261_expert_runtime_scoping.py"
      provides: "Tests verifying Union scope retrieval, additive tool floor retention, and prompt synchronization"
---

<objective>
Fix runtime scoping desync, implement Union scope composition by default (`scope_mode='biased'`), and establish the additive tool floor preserving deliverable tools (`execute_code`, `workspace_write`, `render_template`, `ask_user`), closing `BUG-260920-01` and fulfilling ratified decisions `D-v4.3-01` and `D-v4.3-02`.

Requirements: PACK-02 (accurate thread scoping), BUG-260920-01 closure, and SEED-303 (S4, S5, S6).
Output: Updated `tool_dispatcher.py`, `run_producer.py`, and passing tests in `test_261_expert_runtime_scoping.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task id="261-03-01" name="Preserve Deliverable Tools in Additive Tool Floor (D-v4.3-02 / S6)">
1. In `backend/app/services/tool_dispatcher.py`:
   - Extend `EXPERT_CORE_TOOLS` (or define `EXPERT_DELIVERABLE_TOOLS`) to include `execute_code`, `workspace_write`, `render_template`, and `ask_user`.
   - Update module-level subset assert against `_TOOL_REGISTRY`.
2. In `backend/app/services/run_producer.py`:
   - Ensure `_resolve_thread_scoping` preserves deliverable tools so experts can write files, calculate formulas, fill templates, and ask clarifying questions.
</task>

<task id="261-03-02" name="Implement Union Scope Composition & Fix Prompt Desync (D-v4.3-01 / S4)">
1. In `backend/app/services/run_producer.py`:
   - Inspect thread's existing `folder_id` in addition to `active_expert_id`.
   - If `resolved.scope_mode == 'biased'` (Union Scope, default):
     - `effective_folder_ids = tuple(sorted(set(thread_folder_ids).union(expert_folder_ids)))`
   - If `resolved.scope_mode == 'restricted'` (Strict Isolation):
     - `effective_folder_ids = tuple(sorted(expert_folder_ids))`
   - Synchronize `scoped_folder_path`: resolve folder path string corresponding to effective folders and pass on `RunContext`.
2. In `backend/app/services/agent_loop.py`:
   - Use `ctx.scoped_folder_path` when provided, ensuring prompt instructions match retrieval scope exactly.
   - Maintain zero `if expert:` branches.
</task>

<task id="261-03-03" name="Author Scoping & Tool Floor Tests in test_261_expert_runtime_scoping.py">
1. Test that when `scope_mode == 'biased'` and both thread folder and expert folder exist, both are searched together (S4).
2. Test that when `scope_mode == 'restricted'`, only expert folder is searched (S5).
3. Test that `execute_code`, `workspace_write`, `render_template`, and `ask_user` remain available in `effective_tools` (S6).
4. Test that `scoped_folder_path` matches the actual search scope without desync.
</task>
</tasks>
