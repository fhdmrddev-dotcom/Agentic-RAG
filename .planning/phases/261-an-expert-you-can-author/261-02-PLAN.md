---
phase: 261-an-expert-you-can-author
plan: 02
type: execute
wave: 2
depends_on: [261-01]
files_modified:
  - backend/app/services/expert_authoring.py
  - backend/app/api/experts.py
  - backend/tests/unit/test_261_expert_authoring.py
  - backend/tests/unit/test_261_closed_core_inventory.py
autonomous: true
requirements: [PACK-09]
mutates_local_db: false

must_haves:
  truths:
    - "AI-assisted drafting service in backend/app/services/expert_authoring.py uses forced_emit substrate to generate a candidate ExpertDraftOutput (PACK-09)."
    - "Endpoint POST /experts/draft accepts user description and ephemeral file uploads, extracting text in-memory."
    - "Brainstorm files are processed ephemerally in-memory and are strictly NEVER ingested into public.documents or public.chunks."
    - "AI drafting produces a DRAFT ROW returned to the client that a human must review, edit, and manually save (never auto-publishes)."
    - "Closed-core inventory fence in test_261_closed_core_inventory.py mechanically asserts 7 phase types, 1 emitter, 29 tools, and EXPERT_CORE_TOOLS contains 10 base tools."
    - "Non-ingestion test in test_261_expert_authoring.py verifies that uploading files to /experts/draft results in zero database document additions."
  artifacts:
    - path: "backend/app/services/expert_authoring.py"
      provides: "AI drafting service using forced_emit for expert bundle synthesis"
      exports: ["generate_expert_draft", "ExpertDraftOutput"]
    - path: "backend/tests/unit/test_261_expert_authoring.py"
      provides: "Unit tests for AI draft synthesis and non-ingestion guarantee verification"
    - path: "backend/tests/unit/test_261_closed_core_inventory.py"
      provides: "AST inventory fence asserting closed-core counts are strictly maintained"
---

<objective>
Implement AI-assisted Expert drafting with ephemeral brainstorm file support and explicit non-ingestion guarantees, preserving the closed-core inventory invariant.

Requirements: PACK-09 (AI drafting emitting editable draft row, ephemeral file upload, non-ingestion guarantee, closed-core inventory fence).
Output: `expert_authoring.py` service, `POST /experts/draft` endpoint, and passing tests in `test_261_expert_authoring.py` and `test_261_closed_core_inventory.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task id="261-02-01" name="Author AI Drafting Service in backend/app/services/expert_authoring.py">
1. Define `ExpertDraftOutput` Pydantic model:
   - `name: str`
   - `slug: str`
   - `icon: str` (Lucide-style vector glyph name, e.g. 'chart', 'scale', 'shield', 'briefcase', 'truck', 'terminal')
   - `category: str`
   - `when_to_use: str`
   - `description: str`
   - `scope_mode: Literal['biased', 'restricted']` (defaults to 'biased' per D-v4.3-01)
   - `prompt_suggestions: list[PromptSuggestion]` (3 Action Tiles)
   - `member_skills: list[str]`
   - `knowledge_folder_ids: list[UUID]`
   - `required_connections: list[str]`
2. Implement `generate_expert_draft(description: str, brainstorm_text: str | None, available_folders, available_skills, available_connections)`:
   - Reuses `forced_emit` substrate with `ExpertDraftOutput`.
   - Grounds selections against caller org's accessible folders, skills, and connections.
   - Emits draft row data for human review.
</task>

<task id="261-02-02" name="Add Draft Endpoint to backend/app/api/experts.py">
1. Implement `POST /experts/draft`:
   - Accepts multipart `description: str = Form(...)` and `files: list[UploadFile] = File(None)`.
   - Gated by `require_expert_manage` (PACK-08).
   - Extracts plain text from uploaded files in-memory (ephemeral).
   - Discards files immediately without saving to disk or database.
   - Returns `ExpertDraftOutput`.
</task>

<task id="261-02-03" name="Author Non-Ingestion & Closed-Core Tests">
1. Create `backend/tests/unit/test_261_expert_authoring.py`:
   - Tests that calling `generate_expert_draft` produces valid `ExpertDraftOutput`.
   - Tests that uploading sample files to `POST /experts/draft` results in **zero** records added to `public.documents` or `public.chunks` (non-ingestion guarantee).
2. Create `backend/tests/unit/test_261_closed_core_inventory.py`:
   - Counts `PHASE_TYPE_REGISTRY_ENTRIES` (7).
   - Counts `EMITTER_REGISTRY` (1).
   - Counts `_TOOL_REGISTRY` (29).
   - Mechanically proves zero new agent loops or executors were added.
</task>
</tasks>
