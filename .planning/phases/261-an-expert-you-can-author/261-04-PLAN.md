---
phase: 261-an-expert-you-can-author
plan: 04
type: execute
wave: 3
depends_on: [261-01, 261-02]
files_modified:
  - frontend/src/lib/api/experts.ts
  - frontend/src/components/org/OrgAdminShell.tsx
  - frontend/src/components/org/OrgExpertsTab.tsx
  - frontend/src/components/experts/ExpertAuthoringStudio.tsx
  - frontend/src/components/experts/__tests__/OrgExpertsTab.test.tsx
  - frontend/src/components/experts/__tests__/ExpertAuthoringStudio.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [PACK-07, PACK-10]
mutates_local_db: false

must_haves:
  truths:
    - "OrgAdminShell.tsx hosts a live 'experts' tab rendered for org-admins."
    - "OrgExpertsTab.tsx lists accessible experts, displays role/user grant badges, and provides an '+ Author New Expert' CTA (PACK-07)."
    - "ExpertAuthoringStudio.tsx provides AI brainstorming with file upload and explicit non-ingestion guarantee badge (PACK-09)."
    - "ExpertAuthoringStudio.tsx renders a live reactive preview of the shared 5-element Expert Card on the right, updating synchronously on form changes."
    - "Form includes Title, Slug, Vector Icon Picker, Category, When-to-use, Description, Scope Mode radio (+ Union Scope vs Strict Isolation), Tool Floor toggle, Resource pickers, 3 Action Tiles, and Access Grants (Org-Wide vs Role-Gated vs Named Users)."
    - "Saving or updating calls existing POST /experts or PATCH /experts/{id} endpoints (PACK-07)."
    - "New test suites are pinned in scripts/vitest-count-gate.cjs and pass cleanly."
  artifacts:
    - path: "frontend/src/lib/api/experts.ts"
      provides: "Frontend API client with expert CRUD, AI draft, and grant management methods"
      exports: ["createExpert", "updateExpert", "deleteExpert", "draftExpert", "getExpertGrants", "setExpertGrants"]
    - path: "frontend/src/components/org/OrgExpertsTab.tsx"
      provides: "Org Admin Expert listing and management tab"
      exports: ["OrgExpertsTab"]
    - path: "frontend/src/components/experts/ExpertAuthoringStudio.tsx"
      provides: "Comprehensive authoring form with live 5-element reactive card preview and AI brainstorming"
      exports: ["ExpertAuthoringStudio"]
---

<objective>
Implement frontend Expert authoring studio and management surface inside `OrgAdminShell.tsx`, with live reactive 5-element card preview, AI brainstorming upload dropzone, and granular access grants configuration.

Requirements: PACK-07 (in-app CRUD over existing endpoints) and PACK-10 (grant configuration UI).
Output: Updated `experts.ts` API client, `OrgExpertsTab.tsx`, `ExpertAuthoringStudio.tsx`, and passing Vitest tests pinned in count-gate.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task id="261-04-01" name="Extend Frontend API Client in frontend/src/lib/api/experts.ts">
1. Add `createExpert(payload: ExpertBundleCreate)` calling `POST /experts`.
2. Add `updateExpert(id: string, payload: ExpertBundleUpdate)` calling `PATCH /experts/{id}`.
3. Add `deleteExpert(id: string)` calling `DELETE /experts/{id}`.
4. Add `draftExpert(description: string, files?: File[])` calling `POST /experts/draft` (multipart).
5. Add `getExpertGrants(id: string)` and `setExpertGrants(id: string, grants: ExpertGrantCreate[])`.
</task>

<task id="261-04-02" name="Author OrgExpertsTab.tsx & Mount in OrgAdminShell.tsx">
1. In `frontend/src/components/org/OrgAdminShell.tsx`:
   - Add `{ id: "experts", label: "Experts", locked: false }` to `TABS`.
   - Render `<OrgExpertsTab />` when active tab is `"experts"`.
2. In `frontend/src/components/org/OrgExpertsTab.tsx`:
   - Lists org experts and system templates.
   - Shows badges: Category, Scope Mode (`+ Union Scope` / `🔒 Strict Isolation`), Grants (`👥 Org-Wide`, `🛡️ Role-Gated: HR`, `👤 Named Users`).
   - Actions: Edit, Disable, Delete.
   - Prominent button: `+ Author New Expert`.
</task>

<task id="261-04-03" name="Author ExpertAuthoringStudio.tsx with Live 5-Element Card Preview">
1. Create `frontend/src/components/experts/ExpertAuthoringStudio.tsx`:
   - **AI Brainstorming Dropzone**:
     - Natural language description prompt.
     - Ephemeral file upload (PDF/DOCX/TXT).
     - Prominent non-ingestion badge: *"🛡️ Uploaded brainstorm files are analyzed ephemerally in a sandbox to synthesize this draft row. They are NEVER silently ingested into your permanent knowledge library."*
     - "Generate Draft" button calling `draftExpert`.
   - **Form Configurator**:
     - Name, Slug, Icon Picker (Lucide vector icons), Category tag, When-to-use description, Detailed description.
     - Knowledge Scope Mode: Radio between `+ Union Scope (Default)` and `🔒 Strict Isolation (Opt-in)`.
     - Tool Floor: Checkbox preserving `execute_code`, `workspace_write`, `render_template`, `ask_user`.
     - Multi-pickers for Folders, Skills, and Connections.
     - 3 Action Tiles editor (icon, title, prompt).
     - Access Grants editor: `Org-Wide` vs `Role-Gated` (role selector) vs `Named Users` (user selector).
   - **Live Reactive Card Preview**:
     - Renders the shared 5-element Expert Card on the right side, updating synchronously in real time as the user types.
   - Save / Update button triggering `createExpert` or `updateExpert`.
</task>

<task id="261-04-04" name="Author Vitest Tests & Update Count Gate">
1. Author `OrgExpertsTab.test.tsx`: tests listing, grant badges, and author CTA click.
2. Author `ExpertAuthoringStudio.test.tsx`: tests form inputs, live preview reactivity, AI draft population, and submit dispatch.
3. Update `scripts/vitest-count-gate.cjs` TARGETS and BASELINE with the new suites.
4. Verify count gate passes with 0 failures.
</task>
</tasks>
