---
phase: 056.1
plan: 03
subsystem: frontend/ui
tags: [react, typescript, ToolCallPanel, ElapsedTimer, tool-calling, preparing-state, streaming]
dependency_graph:
  requires:
    - phase: 056.1-02
      provides: preparing-toolcall-state — ToolCall.status includes "preparing", onToolPreparing wired in api.ts + useMessages
  provides:
    - ElapsedTimer component with 250ms tick + clearInterval cleanup
    - preparing-state row render in ToolCallPanel (icon opacity, italic label, pulsing dot, gradient bar)
    - conditional ElapsedTimer vs TimeBadge based on status
    - activeTool includes preparing entries so header shows tool name not Thinking
    - ExecuteCodeBlock guarded with tc.status !== "preparing"
  affects: [frontend/src/components/chat/ToolCallPanel.tsx]
tech-stack:
  added: []
  patterns: [conditional-timer-pattern, preparing-state-render-guard, status-conditional-badge]
key-files:
  created: []
  modified:
    - frontend/src/components/chat/ToolCallPanel.tsx
key-decisions:
  - "D-03 (Phase 56.1): ElapsedTimer uses setInterval(250ms) with useEffect cleanup — clearInterval called on unmount to prevent timer leak (T-56.1-07)"
  - "D-04 (Phase 56.1): Duration badge is conditional — ElapsedTimer when running+startedAt set, TimeBadge when done; preparing shows neither (no timestamps available)"
  - "D-05 (Phase 56.1): ExecuteCodeBlock guarded with tc.status !== 'preparing' — prevents render with empty args:{} which would crash ExecuteCodeBlock (T-56.1-08)"
patterns-established:
  - "Status-conditional badge: check status before choosing live vs static timer component"
  - "Preparing row: opacity-50 icon + italic label + pulsing dot + gradient bar — visual system for in-between state"
requirements-completed: []

duration: ~10min
completed: 2026-04-29
---

# Phase 056.1 Plan 03: ToolCallPanel preparing-state render + ElapsedTimer Summary

**ElapsedTimer component (250ms tick, clearInterval cleanup) added to ToolCallPanel with full preparing-state row render: pulsing dot, opacity-50 icon, italic label, gradient bar, and live timer for running tools replacing the static badge.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-29T16:00:00Z
- **Completed:** 2026-04-29T16:10:00Z
- **Tasks:** 3 (2 code tasks + 1 smoke test)
- **Files modified:** 1

## Accomplishments

- `ElapsedTimer` component defined: `useState(Date.now() - startedAt)`, `useEffect` with `setInterval(250ms)` and `clearInterval` cleanup on unmount — addresses timer leak T-56.1-07
- Preparing-state row render: icon wrapper at `opacity-50`, italic "Preparing {Tool Name}…" label, pulsing dot status indicator, pulsing gradient bar (`from-primary/30 to-primary/10`)
- Duration badge made conditional: `ElapsedTimer` when `tc.status === "running" && tc.startedAt != null`, `TimeBadge` otherwise (done shows static badge; preparing shows nothing — no timestamps)
- `ExecuteCodeBlock` guard: `tc.name === "execute_code" && tc.status !== "preparing"` — prevents render with `args: {}` (T-56.1-08)
- `activeTool` updated to include `status === "preparing"` — panel header shows tool phase label during preparing, not "Thinking…"
- `isSynthesizing` correctly stays false during preparing (activeTool is truthy) — no unintended "Synthesizing answer" flash
- All Phase 56 features verified intact: `isSynthesizing`, `SkillRow`, `taskPhaseLabel`, step counter

## Task Commits

Each task was committed atomically:

1. **Task 1: Add ElapsedTimer component** - `d4c0613` (feat)
2. **Task 2: preparing-state render + conditional ElapsedTimer** - `5f3aa21` (feat)
3. **Task 3: smoke test** - (no code changes; `tsc --noEmit` exits 0, verified)

## Files Created/Modified

- `frontend/src/components/chat/ToolCallPanel.tsx` — `useEffect` import, `ElapsedTimer` component, preparing-state row render (icon opacity, label, status dot, gradient bar), conditional ElapsedTimer vs TimeBadge, `activeTool` includes preparing, `ExecuteCodeBlock` guard

## Decisions Made

- D-03: `setInterval(250ms)` cleanup via `useEffect` return — satisfies T-56.1-07 timer leak threat
- D-04: Conditional badge pattern: `ElapsedTimer` only when `running && startedAt != null`; preparing shows nothing (no startedAt yet)
- D-05: `tc.status !== "preparing"` guard on `ExecuteCodeBlock` — satisfies T-56.1-08 tampering threat (empty args would crash ExecuteCodeBlock)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. TypeScript type-checked cleanly (`npx tsc --noEmit` exits 0) after all changes.

## Known Stubs

None. All changes are fully wired — `ElapsedTimer` renders from `tc.startedAt` provided by Plan 02's upgrade logic; preparing row renders from `tc.status === "preparing"` entries created by Plan 02's `onToolPreparing` callback.

## Threat Flags

No new security surface. All mitigations from plan threat model applied:
- T-56.1-07: `clearInterval(t)` in `useEffect` return — implemented in `ElapsedTimer`
- T-56.1-08: `tc.status !== "preparing"` guard on `ExecuteCodeBlock` — implemented
- T-56.1-09: Accepted — tool name already visible at same trust boundary

## Next Phase Readiness

Phase 056.1 Plans 01-03 are complete. The silence-window fix is end-to-end:
- Plan 01: Backend emits `tool_preparing` SSE event in all 3 LLM paths
- Plan 02: Frontend wires `tool_preparing` into `toolCalls` with `status: "preparing"` placeholder
- Plan 03: ToolCallPanel renders preparing entries with visual feedback + live timer for running tools

Ready for Plan 04 (system prompt hardening) if applicable.

## Self-Check: PASSED

- `frontend/src/components/chat/ToolCallPanel.tsx` modified: confirmed
- `ElapsedTimer` in file: 2 occurrences (definition + usage)
- `useEffect` in file: 2 occurrences (import + usage)
- `clearInterval` in file: 1 occurrence (cleanup)
- `tc.status === "preparing"` in file: 5 occurrences
- `tc.status !== "preparing"` in file: 1 occurrence (ExecuteCodeBlock guard)
- `from-primary` in file: 2 occurrences (gradient bar)
- `activeTool` includes `"preparing"`: confirmed (line 540)
- `npx tsc --noEmit` exits 0: confirmed
- Commits d4c0613, 5f3aa21 present in git log: confirmed

---
*Phase: 056.1-agent-feedback-gaps*
*Completed: 2026-04-29*
