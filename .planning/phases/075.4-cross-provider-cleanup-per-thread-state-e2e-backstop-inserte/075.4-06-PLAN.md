---
phase: 075.4
plan: 06
type: execute
wave: 3
depends_on: ["075.4-01", "075.4-02", "075.4-03", "075.4-04", "075.4-05"]
files_modified:
  - frontend/tests/e2e/scenario-01-parallel-composers.spec.ts
  - frontend/tests/e2e/scenario-02-gemini-thought-signature.spec.ts
  - frontend/tests/e2e/scenario-03-openrouter-final-pptx.spec.ts
  - frontend/tests/e2e/scenario-04-done-latency.spec.ts
  - frontend/tests/e2e/scenario-05-unknown-model-inference.spec.ts
  - frontend/tests/e2e/scenario-06-iteration-parity.spec.ts
  - .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md
  - backend/tests/test_provider_router.py
  - backend/tests/integration/test_066_langsmith_clean.py
autonomous: false
success_criteria_addressed: [SC#8, SC#9]

must_haves:
  truths:
    - "6 E2E scenarios exist, each mapping 1:1 to a regression class + each asserts a LangSmith trace"
    - "DB-state teardown asserts no orphaned runs.status='streaming' for test user; localhost hard-gate (inherited from Plan 05 fixture)"
    - "075.4-TEST-TRIAGE.md categorizes all 95 pre-existing backend test failures; sums to 95"
    - "FK-violation cluster (~6 failures) fixed via fk_aware_runs_factory from Plan 05 Task 4"
    - "Post-fix pytest count ≤ 89 (95 - 6 closed)"
  artifacts:
    - path: "frontend/tests/e2e/scenario-01-parallel-composers.spec.ts"
      provides: "BUG-260523-01 regression"
      min_lines: 40
    - path: "frontend/tests/e2e/scenario-06-iteration-parity.spec.ts"
      provides: "BUG-260523-04 measurement (NOT root-cause fix)"
      min_lines: 50
    - path: ".planning/phases/075.4-.../075.4-TEST-TRIAGE.md"
      provides: "95-failure categorization (revised from stale 44)"
      min_lines: 120
  key_links:
    - from: "frontend/tests/e2e/scenario-*.spec.ts"
      to: "frontend/tests/e2e/fixtures/auth.fixture.ts (Plan 05 Task 2)"
      via: "import test from '../fixtures/auth.fixture'"
      pattern: "from.*fixtures/auth\\.fixture"
    - from: "frontend/tests/e2e/scenario-*.spec.ts"
      to: "frontend/tests/e2e/fixtures/db-teardown.fixture.ts (Plan 05 Task 2)"
      via: "teardownTestUserData + assertNoOrphanedStreamingRuns"
      pattern: "teardownTestUserData|assertNoOrphanedStreamingRuns"
    - from: "frontend/tests/e2e/scenario-*.spec.ts"
      to: "frontend/tests/e2e/fixtures/langsmith.fixture.ts (Plan 05 Task 2)"
      via: "assertLangSmithTraceExists"
      pattern: "assertLangSmithTraceExists"
    - from: "backend/tests/test_provider_router.py (Task 3 here)"
      to: "backend/tests/conftest.py fk_aware_runs_factory (Plan 05 Task 4)"
      via: "fixture import + parameter injection"
      pattern: "fk_aware_runs_factory"
---

<objective>
**Wave 3 — Finalize 075.4** (depends on Plans 01-04 committed + Plan 05 Wave 0 bootstrap).

This plan was split from Plan 05 at plan-phase time (per plan-checker W-1 + SDK wave-grouping mismatch). Plan 05 ships the test infrastructure substrate (CLAUDE.md UAT rule + Playwright config + 3 fixtures + restart scripts + /health endpoint + FK-aware fixture + frontend-tests.yml + bug-report frontmatter updates). Plan 06 ships the actual E2E scenarios + test-failure triage + FK fix cluster — these all require Plans 01-04 commits AND Plan 05's bootstrap to be in place.

Tasks:
1. 6 Playwright scenarios mapping 1:1 to regression classes (was Plan 05 Task 6).
2. 075.4-TEST-TRIAGE.md categorizing all 95 pre-existing backend test failures (was Plan 05 Task 7).
3. Fix the FK-violation cluster (~6 failures) using fk_aware_runs_factory from Plan 05 Task 4 (was Plan 05 Task 8).
4. Operator UAT scoreboard sweep + CI dry-run + final SUMMARY.md (was Plan 05 Task 9 checkpoint).

Output: 6 frontend test files + 1 triage artifact + 2 backend test file updates.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-CONTEXT.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-RESEARCH.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-PATTERNS.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-VALIDATION.md

@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-01-SUMMARY.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-02-SUMMARY.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-03-SUMMARY.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-04-SUMMARY.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-05-SUMMARY.md

@CLAUDE.md

<interfaces>
6 E2E scenarios per RESEARCH §Plan 05 + AUDIT (verbatim):
1. scenario-01-parallel-composers — Thread A streaming + Thread B composer accepts input (BUG-260523-01)
2. scenario-02-gemini-thought-signature — gemini-3-flash-preview 2+ tool rounds, no 400 (BUG-260523-02)
3. scenario-03-openrouter-final-pptx — OpenRouter pptx-with-charts, exactly 1 final pptx (BUG-260523-03a)
4. scenario-04-done-latency — SSE done → UI settled < 500ms (BUG-260523-03b)
5. scenario-05-unknown-model-inference — gpt-99-mega under OpenAI, inference fallback (Phase 075.3 Plan 02 guard)
6. scenario-06-iteration-parity — same prompt × 4 providers, counts within ~50% (BUG-260523-04 measurement only)

Backend pytest baseline (RESEARCH §Plan 05, 2026-05-23): 95 failed / 625 passed / 5 skipped of 728 collected.
FK-violation cluster: 4 in test_provider_router.py + 2 in test_066_langsmith_clean.py = 6 fixable here.
</interfaces>
</context>

<tasks>

<task type="auto" wave="3">
  <name>Task 1: 6 Playwright scenarios with LangSmith trace assertion + DB-state teardown</name>
  <files>frontend/tests/e2e/scenario-01-parallel-composers.spec.ts, frontend/tests/e2e/scenario-02-gemini-thought-signature.spec.ts, frontend/tests/e2e/scenario-03-openrouter-final-pptx.spec.ts, frontend/tests/e2e/scenario-04-done-latency.spec.ts, frontend/tests/e2e/scenario-05-unknown-model-inference.spec.ts, frontend/tests/e2e/scenario-06-iteration-parity.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/fixtures/auth.fixture.ts (from Plan 05 Task 2 — re-use authedPage)
    - frontend/tests/e2e/fixtures/db-teardown.fixture.ts (Plan 05 Task 2)
    - frontend/tests/e2e/fixtures/langsmith.fixture.ts (Plan 05 Task 2)
    - .planning/phases/075.4-.../075.4-RESEARCH.md §`The 6 E2E scenarios` (verbatim assertions)
    - .planning/AUDIT-2026-05-23-cross-cutting-cleanup.md lines 127-132 (regression class table)
    - Plans 01-04 SUMMARYs (read what behaviors actually shipped to assert against)
  </read_first>
  <action>
    Create 6 Playwright spec files following this template per scenario (≥40 lines each except scenario-04 ≥30 + scenario-06 ≥50):

    ```typescript
    import { test, expect } from '../fixtures/auth.fixture'   // re-use authedPage
    import { teardownTestUserData, assertNoOrphanedStreamingRuns } from '../fixtures/db-teardown.fixture'
    import { assertLangSmithTraceExists } from '../fixtures/langsmith.fixture'

    const TEST_USER_EMAIL = process.env.E2E_USER_EMAIL || 'fhdmrd@gmail.com'

    test.describe('@075.4 scenario-NN — <description>', () => {
      test.beforeEach(async () => { await teardownTestUserData(TEST_USER_EMAIL) })
      test.afterEach(async () => { await assertNoOrphanedStreamingRuns(TEST_USER_EMAIL) })

      test('<assertion>', async ({ authedPage: page }) => {
        // ... scenario steps ...
      })
    })
    ```

    **scenario-01-parallel-composers.spec.ts** (BUG-260523-01):
    1. Open New Chat → enter long Anthropic prompt → click Send → wait for streaming to start.
    2. Open second New Chat (Thread B) in same tab via UI nav (or new tab if app supports it; likely sidebar New Chat button).
    3. Assert composer textarea on Thread B is NOT `disabled`.
    4. Type a prompt in Thread B → click Send → assert second run starts (loading spinner appears, /threads/{id}/runs POST returns 201).
    5. Capture run_id from SSE → `assertLangSmithTraceExists(runId)`.

    **scenario-02-gemini-thought-signature.spec.ts** (BUG-260523-02):
    1. Set Settings → AI Model → google + gemini-3-flash-preview (or whichever Gemini 3 model is available).
    2. Send a prompt that triggers 2+ tool rounds (e.g., "Search docs for 'X' then run python to plot results").
    3. Assert no 400 INVALID_ARGUMENT in network responses.
    4. Assert run completes (terminal SSE arrives).
    5. `assertLangSmithTraceExists(runId, 'google')` — provider tag check.

    **scenario-03-openrouter-final-pptx.spec.ts** (BUG-260523-03a):
    1. Set Settings → OpenRouter + minimax-m2.7 (or any OpenRouter model).
    2. Send "Make a 3-slide pptx with sample charts about climate change".
    3. Wait for streaming complete + final_output_files SSE.
    4. Assert exactly ONE .pptx is shown in the final pinned panel (`expect(pptxCards).toHaveCount(1)`).
    5. Assert the visible pptx is the LATEST version (no stale intermediate; if any "Replaces:" subline visible, it points at a prior filename).
    6. `assertLangSmithTraceExists(runId, 'openrouter')`.

    **scenario-04-done-latency.spec.ts** (BUG-260523-03b):
    1. Same OpenRouter run as scenario-03 OR fresh chat with same model.
    2. Send a short prompt that streams.
    3. Capture timestamp when SSE `done`/terminal event arrives (via page.evaluate or via inspecting CDP network events).
    4. Capture timestamp when UI shows "complete" state (loading spinner gone, message marked done).
    5. Assert delta < 500ms.

    **scenario-05-unknown-model-inference.spec.ts** (Phase 075.3 Plan 02 regression-guard):
    1. Settings → OpenAI provider → models field: add `gpt-99-mega` (unregistered).
    2. Set llm_model to `gpt-99-mega` → Save.
    3. Send any short prompt.
    4. Assert backend logs show `model_capability_unknown` warning ONCE (poll backend logs OR check network response headers — or assert via a `/admin/log-tail` endpoint if available; otherwise this assertion lives in backend logging which the operator manually verifies — annotate test with manual-step note if not autodetectable).
    5. Assert run completes (inference fallback applies safe defaults).
    6. Assert NO UnknownProviderError (that's for unknown PROVIDER, not unknown MODEL — verify distinction).
    7. `assertLangSmithTraceExists(runId)`.

    **scenario-06-iteration-parity.spec.ts** (BUG-260523-04 MEASUREMENT, not fix):
    1. Define a parameterized test running the SAME prompt ("Build a simple analysis: load a CSV, compute means, plot a bar chart") against 4 providers in sequence.
    2. For each provider (OpenAI gpt-5.x, Anthropic claude-4.x, Google gemini-3-pro, OpenRouter minimax-m2.7), capture the final iteration count (from `runs.metadata.iteration_count` or by counting tool_preparing SSE events).
    3. Assert all 4 complete.
    4. Assert max(counts) / min(counts) <= 2 (within ~50% spread). If Anthropic blows this gate, the test FAILS — this is the BUG-260523-04 measurement. The fix is deferred per CONTEXT.md carry-forward; the failing test stays in the suite as a regression measurement.
    5. Document expected behavior in test comments: "Per CONTEXT.md routing, BUG-260523-04 root-cause fix is deferred. This test FAILS today; it stays as a measurement gate that the future focused-fix phase will close."

    Each scenario MUST be tagged `@075.4` (via test.describe title) so `npx playwright test --grep @075.4` selects them all.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --list 2>&1 | grep -c "@075.4"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 scenario files exist (`test -f` for each)
    - `wc -l frontend/tests/e2e/scenario-01-parallel-composers.spec.ts` >= 40
    - `wc -l frontend/tests/e2e/scenario-02-gemini-thought-signature.spec.ts` >= 40
    - `wc -l frontend/tests/e2e/scenario-03-openrouter-final-pptx.spec.ts` >= 40
    - `wc -l frontend/tests/e2e/scenario-04-done-latency.spec.ts` >= 30
    - `wc -l frontend/tests/e2e/scenario-05-unknown-model-inference.spec.ts` >= 40
    - `wc -l frontend/tests/e2e/scenario-06-iteration-parity.spec.ts` >= 50
    - `cd frontend && npx playwright test --list 2>&1 | grep -c "@075.4"` returns 6 (one per scenario file when each has a single test)
    - Each scenario imports `assertLangSmithTraceExists`: `grep -l "assertLangSmithTraceExists" frontend/tests/e2e/scenario-*.spec.ts | wc -l` returns 6
    - Each scenario uses `teardownTestUserData`: `grep -l "teardownTestUserData" frontend/tests/e2e/scenario-*.spec.ts | wc -l` returns 6
    - Each scenario asserts no orphaned streaming runs: `grep -l "assertNoOrphanedStreamingRuns" frontend/tests/e2e/scenario-*.spec.ts | wc -l` returns 6
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "tests/e2e/scenario-"` returns 0
  </acceptance_criteria>
  <done>6 Playwright scenarios authored mapping 1:1 to regression classes; each asserts LangSmith trace + DB-state teardown; scenario-06 stays failing as BUG-260523-04 measurement gate.</done>
</task>

<task type="auto" wave="3">
  <name>Task 2: 075.4-TEST-TRIAGE.md — categorize all 95 pre-existing backend test failures</name>
  <files>.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md</files>
  <read_first>
    - .planning/phases/075.4-.../075.4-RESEARCH.md §Plan 05 Current pytest baseline (95 failures, top files listed)
    - backend/pytest.ini (config)
    - backend/tests/conftest.py (FK-aware fixture from Plan 05 Task 4 — available for "FIX with fixture" recommendations)
    - Live failure output: run `cd backend && venv/Scripts/python -m pytest tests/ --no-header -q 2>&1 | grep -E "^FAILED" | sort > /tmp/failures.txt && wc -l /tmp/failures.txt`
  </read_first>
  <action>
    Create `075.4-TEST-TRIAGE.md` (≥120 lines). Structure:

    ```markdown
    # Phase 075.4 — Backend Test Triage (95 failures on v2.5-dev HEAD)

    **Baseline:** 95 failed / 625 passed / 5 skipped / 3 xfailed of 728 collected (verified 2026-05-23 per RESEARCH §Plan 05).
    **Audit cited 44.** RESEARCH measured 95. Triage budget revised from 3-4h to 6-8h.

    ## Bucket Strategy (D-075.4-G3)

    | Bucket | Approx count | Strategy |
    |--------|-------------|---------|
    | (a) Tests referencing deleted/moved code | ~5 | DELETE the test (or @pytest.mark.skip with deferred-items.md ref) |
    | (b) Real regressions — test correct, code wrong | ~5-10 | FIX code (out-of-scope unless trivial; document in Plan 06 deferral list) |
    | (c) Mock drift / sync→async / fixture drift | ~80 | UPDATE tests (use FK-aware fixture from conftest.py where applicable) |

    ## Per-File Triage (sums to 95)

    | File | Fail count | Bucket | Action | Owner |
    |------|-----------|--------|--------|-------|
    | tests/unit/test_retrieval_service.py | 15 | (c) | RuntimeWarning: coroutine 'search_documents' was never awaited — bulk async-mock update | Plan 06 Task 3 OR deferred |
    | tests/unit/test_sql_service.py | 12 | (c) | DID NOT RAISE ValueError — sync→async migration; bulk update | deferred-to-076 |
    | tests/integration/test_threads_skills.py | 11 | (c) | Need full inspection; likely real-Supabase / fixture drift | deferred |
    | tests/unit/test_explorer_agent.py | 6 | (c) | mock drift | deferred |
    | tests/unit/test_multimodal_query.py | 5 | (c) | TypeError / AssertionError — schema or signature drift | deferred |
    | tests/test_provider_router.py | 4 | (c) | FK violations — USE `fk_aware_runs_factory` from Plan 05 Task 4 | Plan 06 Task 3 |
    | tests/integration/test_075_code_stdout_progressive.py | 4 | (c) | Phase 075 fixture drift | deferred-to-076 |
    | tests/integration/test_066_per_call_timer.py | 4 | (c) | Per-call-timer infra changes | deferred |
    | tests/integration/test_threads.py | 3 | (c) | mock or real-Supabase drift | deferred |
    | tests/unit/test_extraction_service.py | 2 | (c) | Phase 071.3 demoted Docling; legacy goldens stale | deferred-to-076 |
    | tests/integration/test_066_langsmith_clean.py | 2 | (c) | FK violations — same fix as provider_router | Plan 06 Task 3 |
    | <... remaining ~27 across smaller failing files ...> | <fill via live pytest run output> | <bucket> | <action> | <owner> |
    | **Total** | **95** | | | |

    ## Plan 06 In-Scope Fixes (Task 3)

    Within Plan 06's 6-8h budget, fix the FK-violation cluster ONLY:
    - 4 fails in test_provider_router.py
    - 2 fails in test_066_langsmith_clean.py
    - (potentially others surfaced as same FK pattern when greppable)

    **Expected delta: 6 of 95 closed → 89 remaining.** Plan 06 ships at 89 failures (or whatever the actual post-fix count is). The remaining 89 are documented here with explicit deferral.

    ## Deferred to Future Phases

    Pattern-grouped bulk updates that exceed Plan 06's budget:
    - 15 retrieval_service async-mock drift → defer to Phase 076 cleanup task
    - 12 sql_service sync→async migration → defer to Phase 076
    - 11 threads_skills fixture drift → defer to Phase 076 OR closer to v2.7 Skill Studio
    - 6 explorer_agent mock drift → defer (low priority; unblock at next touch)
    - 5 multimodal_query drift → defer to Phase 076 (Confidence Recalibration is the natural touch point for multimodal)
    - 4 test_075_code_stdout_progressive → defer to Phase 076 (sandbox infra touch)
    - 4 test_066_per_call_timer → defer to Phase 077 (multi-worker validation harness)

    ## Bucket (a) — Delete Candidates

    Run `cd backend && pytest tests/ --co 2>&1 | grep "ImportError"` to find tests with stale imports. Likely 0-5 files. Document them here when found.

    ## Bucket (b) — Real Regressions

    If ANY of the 95 fails turn out to be Bucket (b) (test correct, code wrong), document them here with the specific regression. These MAY be in-scope if they're trivially fixable; otherwise file as bug reports and defer.

    ## Sign-off

    - [ ] All 95 failures categorized (bucket + action + owner)
    - [ ] Categories sum to 95
    - [ ] In-scope fixes shipped in Task 3 (FK-violation cluster)
    - [ ] Deferred items rolled forward to specific phases (076 / 077 / etc.)
    ```

    To populate the per-file table fully: run `cd backend && venv/Scripts/python -m pytest tests/ -q --no-header 2>&1 | grep -E "^FAILED" | awk -F'::' '{print $1}' | sort | uniq -c | sort -rn`. This produces "<count> <file>" lines. Paste each into the table. Sum should equal 95.
  </action>
  <verify>
    <automated>test -f .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md && wc -l .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md`
    - `wc -l .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 120
    - `grep -c "95" .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 3 (baseline, total, sum check)
    - `grep -cE "Bucket \(a\)|Bucket \(b\)|Bucket \(c\)" .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 3
    - `grep -c "fk_aware_runs_factory" .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 1 (refs Plan 05 Task 4 fixture)
    - The per-file table sums to 95: extract counts in the table column and `awk` to sum (operator-verifiable)
  </acceptance_criteria>
  <done>95-failure triage artifact landed; each file mapped to bucket+action+owner; counts sum to 95; in-scope fixes (FK cluster) identified; deferrals rolled forward to specific phases.</done>
</task>

<task type="auto" wave="3">
  <name>Task 3: Fix the FK-violation cluster (~6 failures using fk_aware_runs_factory)</name>
  <files>backend/tests/test_provider_router.py, backend/tests/integration/test_066_langsmith_clean.py</files>
  <read_first>
    - backend/tests/test_provider_router.py (find the 4 failing tests; identify which tests INSERT into runs without parent threads row)
    - backend/tests/integration/test_066_langsmith_clean.py (2 failing tests, same pattern)
    - backend/tests/conftest.py (Plan 05 Task 4 fixture `fk_aware_runs_factory`)
  </read_first>
  <action>
    Run `cd backend && venv/Scripts/python -m pytest tests/test_provider_router.py -v 2>&1 | grep "FAILED"` to identify the 4 failing tests by name.

    For each failing test:
    1. Read the test body. Identify where `runs` table INSERT happens.
    2. Inject the `fk_aware_runs_factory` fixture parameter to the test function.
    3. Replace direct `_supabase.table("runs").insert(...)` calls with `await fk_aware_runs_factory(user_id=..., **other_fields)`.
    4. Re-run the test; if green, move to next.
    5. If still failing for a non-FK reason, ROLL BACK the change and document the test in 075.4-TEST-TRIAGE.md as Bucket (b) (real regression — beyond this task's scope).

    Repeat for the 2 failing tests in `test_066_langsmith_clean.py`.

    Cap this task at the 6 tests. If MORE FK-violation tests surface during pytest output review (e.g., other files with `runs_thread_id_fkey` errors), document them in the triage artifact and defer.

    Update 075.4-TEST-TRIAGE.md after this task — flip the affected rows from `Plan 06 Task 3` → `Fixed in Plan 06 Task 3 — green`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_provider_router.py tests/integration/test_066_langsmith_clean.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/test_provider_router.py -q 2>&1 | grep -cE "passed|failed"` shows passes increased (delta ≥ 3, since 4 were failing — accept 3-4 fix as success; 1 may remain as bucket-b)
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_066_langsmith_clean.py -q 2>&1 | grep -cE "passed|failed"` shows the 2 fails closed (or documented as bucket-b in triage if not FK-driven)
    - `grep -c "fk_aware_runs_factory" backend/tests/test_provider_router.py` returns >= 1
    - `grep -c "fk_aware_runs_factory" backend/tests/integration/test_066_langsmith_clean.py` returns >= 1
    - 075.4-TEST-TRIAGE.md updated: `grep -cE "Fixed in Plan 06 Task 3|→ green" .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 2 (provider_router + 066_langsmith_clean rows updated)
  </acceptance_criteria>
  <done>FK-violation cluster fixes shipped (~6 failures closed); fk_aware_runs_factory consumed by both files; triage artifact updated with green flips.</done>
</task>

<task type="checkpoint:human-verify" wave="3" gate="blocking">
  <name>Task 4: Run Playwright E2E + UAT scoreboard sweep + verify integration with real provider keys</name>
  <files>(verification-only — no code changes)</files>
  <read_first>
    - All 6 scenario files from Task 1
    - 075.4-TEST-TRIAGE.md (Task 2) — confirm green flips from Task 3
    - VALIDATION.md §Manual-Only Verifications (operator-led 4-axis sweep)
  </read_first>
  <what-built>
    Operator runs the full E2E + UAT sweep:
    1. `cd frontend && npx playwright test --grep @075.4` against the live dev stack (backend + frontend on localhost with real provider keys configured in `backend/.env`). Capture per-scenario pass/fail.
    2. Manually execute the 4-axis UAT scoreboard (cross-provider × multi-tool × parallel-thread × long-message) for each touched plan's UAT row — record results in each plan's `<verification>` UAT scoreboard table.
    3. `cd backend && venv/Scripts/python -m pytest tests/ -q --no-header 2>&1 | tail -3` — confirm post-Task-3 failure count is 95 - (6 fixed) = 89 or less.
    4. CI dry-run: push a no-op commit on a test branch to verify `.github/workflows/frontend-tests.yml` runs and GitHub Actions auto-redacts `secrets.LANGSMITH_API_KEY`.

    Scenario-06 (BUG-260523-04 measurement) is EXPECTED to fail today — that's the regression-detection gate. Other 5 scenarios must pass.
  </what-built>
  <how-to-verify>
    1. Set env vars in `backend/.env`: real `LANGSMITH_API_KEY`, real provider keys (OPENAI_API_KEY, ANTHROPIC_API_KEY, GOOGLE_API_KEY, OPENROUTER_API_KEY).
    2. Boot stack: `bash scripts/restart-backend.sh` (or PowerShell equivalent) + `cd frontend && npm run dev`. Verify `curl http://localhost:8000/health` returns 200.
    3. Run Playwright: `cd frontend && npx playwright test --grep @075.4 --reporter=list`. Expect scenarios 1-5 GREEN, scenario-6 RED with iteration-spread > 2 (BUG-260523-04 measurement).
    4. For each plan (01-04), fill in the UAT scoreboard's "Actual" + "Pass" columns by manually running the rows against the live dev stack with all 4 providers configured.
    5. Confirm post-fix pytest count: `cd backend && venv/Scripts/python -m pytest tests/ -q --no-header 2>&1 | tail -3` shows ≤ 89 failures.
    6. (Optional) Push a branch + open a draft PR to verify CI fires and `.github/workflows/frontend-tests.yml` runs. Inspect log to confirm `LANGSMITH_API_KEY` value is redacted as `***`.
    7. Document final results in `075.4-06-SUMMARY.md` (created at end-of-plan): scenarios 1-5 pass count + scenario 6 measurement value + pytest count delta + CI dry-run status.
  </how-to-verify>
  <action>
    See <what-built> above. Operator-driven full E2E sweep + UAT scoreboard fill + pytest re-count + optional CI dry-run.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --grep @075.4 --reporter=list 2>&1 | tail -15</automated>
  </verify>
  <done>5+ Playwright scenarios pass (1-5); scenario-6 documented as expected-fail (BUG-260523-04 measurement gate); post-fix pytest count ≤ 89; UAT scoreboards filled in Plans 01-04; SUMMARY.md authored.</done>
  <acceptance_criteria>
    - `cd frontend && npx playwright test --grep @075.4 2>&1 | tail -10` shows 5+ passes (scenarios 1-5), 1 expected fail (scenario 6 measurement)
    - `cd backend && venv/Scripts/python -m pytest tests/ -q --no-header 2>&1 | tail -3` shows failures count ≤ 89 (95 - 6 FK fixes) — confirms Task 3 effective
    - UAT scoreboards in 075.4-01-PLAN.md, 075.4-02-PLAN.md, 075.4-03-PLAN.md, 075.4-04-PLAN.md `<verification>` blocks have Actual + Pass columns filled (operator-recorded — verified by reading each PLAN's UAT block has non-empty cells)
    - 075.4-06-SUMMARY.md (Task 4 output) exists and documents: scenario pass/fail count + iteration-spread measurement + post-fix pytest count + CI dry-run status
    - GitHub Actions log (if CI dry-run executed) shows `secrets.LANGSMITH_API_KEY` redacted as `***` in step output (verify by inspection — no actual key string visible)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the SUMMARY.md path + scenario pass count + post-fix pytest count, OR describe blocking issues (e.g., "scenario-02 fails on Gemini-3 — needs Plan 02 fix re-verify" → orchestrator routes back).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright scenario specs | Run against live dev stack with real provider keys; could expose keys if logged. |
| LangSmith trace polling | Uses `LANGSMITH_API_KEY` from env. |
| FK-violation test fix | Mutates test files in backend/tests/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075.4-07 (inherited from Plan 05) | I (Information disclosure) — test-user data leak in CI logs | Playwright scenario specs | mitigate | Creds loaded from `process.env.E2E_USER_EMAIL` / `E2E_USER_PASSWORD` (NOT committed; reused from Plan 05 fixtures). |
| T-075.4-08 (inherited from Plan 05) | I (Information disclosure) — LangSmith API key exposure in CI logs | scenario specs invoke fixture | mitigate | `langsmith.fixture.ts` from Plan 05 already redacts; scenarios just call `assertLangSmithTraceExists()`. |
| T-075.4-EXTRA | T (Tampering) — wrong test mutated in FK-fix task | Task 3 file edits | mitigate | Verify diff: `git diff backend/tests/test_provider_router.py` shows ONLY `fk_aware_runs_factory` injection + INSERT-call replacement, no body rewrites. Roll back if other changes leaked. |
</threat_model>

<verification>
## Plan-Level Checks
1. `cd frontend && npx playwright test --list 2>&1 | grep -c "@075.4"` returns 6 (6 scenarios discoverable).
2. `wc -l .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 120; per-file counts sum to 95.
3. Post-Task-3 pytest failures count ≤ 89 (was 95).

## UAT Scoreboard — Plan 06 is operator-led full E2E sweep across all touched plans + CI dry-run.
</verification>

<success_criteria>
- 6 Playwright scenarios + LangSmith trace assertions + DB-state teardown; scenarios 1-5 pass against live dev stack; scenario-6 stays failing as BUG-260523-04 measurement gate
- 075.4-TEST-TRIAGE.md categorizes all 95 pre-existing backend test failures into a/b/c buckets summing to 95
- Plan 06 Task 3 ships ~6 in-scope FK-violation fixes; remaining ~89 deferred to specific future phases per triage
- Operator UAT sweep + CI dry-run completed; SUMMARY.md documents the closing state
</success_criteria>

<forward_references>
- → Phase 076 / 077 (and others per triage): the deferred ~89 backend test failures are rolled forward to specific phases per 075.4-TEST-TRIAGE.md `Deferred to Future Phases` section.
- → Post-075.4 focused phase: BUG-260523-04 root-cause fix (E2E #6 measures it; not closed in 075.4 per CONTEXT.md carry-forward).
- → Phase 082 (cross-cutting verification): Plan 06's Playwright scenarios + langsmith.fixture + db-teardown.fixture (from Plan 05) are the test substrate 082 rides on.
</forward_references>

<output>
After completion, create `.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-06-SUMMARY.md`
</output>
