---
phase: 075.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/stores/streamsStore.ts
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/chat/MessageList.tsx
  - frontend/src/hooks/useMessages.ts
  - frontend/src/__tests__/integration/streamsStore_per_thread.test.ts
  - frontend/src/__tests__/providers/streamsProvider_067_5_regression.test.tsx
autonomous: true
requirements: []  # 075.4 is a cleanup phase; SC#1 from ROADMAP §Phase 075.4 is the de-facto requirement (no v2.6 REQ-ID maps here per RESEARCH §1)
success_criteria_addressed: [SC#1]
bugs_folded: [BUG-260523-01]

must_haves:
  truths:
    - "Two threads can stream in parallel; Thread A streaming does NOT disable Thread B's composer"
    - "Per-thread selectors return values for the queried thread only (no cross-thread bleed)"
    - "067.5 Branch D-3 clearMessages guard at StreamsProvider.tsx:603-619 is preserved verbatim"
    - "Refresh during Thread A streaming preserves Thread A's bucket (067.5 regression test added)"
    - "The 5 lifted globals (streamingThreads, loadingThreads, reconcileErrors, fallbackNotices, subscriptionsByThread) all key by threadId"
  artifacts:
    - path: "frontend/src/stores/streamsStore.ts"
      provides: "Per-thread state shape (Map/Set keyed by threadId for all 5 lifted globals)"
      contains: "streamingThreads: Set<string>"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "Per-thread state writes + new thread-scoped selectors"
      exports: ["useStreamingForThread", "useLoadingForThread", "useReconcileErrorForThread", "useFallbackNoticeForThread"]
    - path: "frontend/src/__tests__/integration/streamsStore_per_thread.test.ts"
      provides: "Cross-thread bleed-through regression coverage"
      min_lines: 80
    - path: "frontend/src/__tests__/providers/streamsProvider_067_5_regression.test.tsx"
      provides: "067.5 empty-thread-until-refresh regression test (multi-thread parallel)"
      min_lines: 50
  key_links:
    - from: "frontend/src/components/chat/ChatArea.tsx"
      to: "useStreamingForThread"
      via: "selector call with thread?.id"
      pattern: "useStreamingForThread\\(thread\\?\\.id"
    - from: "frontend/src/providers/StreamsProvider.tsx:603-619"
      to: "streamingThreadIdRef"
      via: "067.5 Branch D-3 guard predicate (PRESERVED VERBATIM)"
      pattern: "tid !== streamingThreadIdRef\\.current"
---

<objective>
Lift the 5 cross-thread global state pollutions in `frontend/src/stores/streamsStore.ts` to per-thread keys. Closes BUG-260523-01 (composer locked globally during any stream) by ensuring Thread A's streaming state does not gate Thread B's composer. Preserves the Phase 067.5 Branch D-3 `clearThreadBucket` guard verbatim. Adds an explicit 067.5 regression test (per D-075.4-A3) that Phase 082 inherits.

Purpose: A second concurrent stream surface must render without state collision (foundation for v2.7 multi-thread workspace). Cross-thread bleed eliminated.
Output: streamsStore.ts shape refactor + StreamsProvider.tsx writes/selectors + ChatArea.tsx consumer rewrites + 2 new test files.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-CONTEXT.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-RESEARCH.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-PATTERNS.md

@frontend/src/stores/streamsStore.ts
@frontend/src/providers/StreamsProvider.tsx
@frontend/src/components/chat/ChatArea.tsx
@frontend/src/hooks/useMessages.ts
@frontend/src/lib/streamsCache.ts

<interfaces>
<!-- Current shape of the 5 globals being lifted (streamsStore.ts:43-100) -->

```typescript
// BEFORE (lines 43-56) — what we're changing:
export interface StreamsState {
  bucketsBySurface: Map<SurfaceId, Map<string, Message[]>>  // UNTOUCHED (already per-thread)
  viewedThreadId: string | null                             // UNTOUCHED
  isStreaming: boolean                                      // LIFT → streamingThreads: Set<string>
  fallbackNotice: string | null                             // LIFT → fallbackNotices: Map<string, string>
  reconcileError: { threadId: string; error: Error } | null // LIFT → reconcileErrors: Map<string, Error>
  loadingThreadId: string | null                            // LIFT → loadingThreads: Set<string>
  subscriptionsByRunId: Set<string>                         // LIFT → subscriptionsByThread: Map<string, Set<string>>
  actions: { ... }
}

// AFTER (target shape per D-075.4-A1):
export interface StreamsState {
  bucketsBySurface: Map<SurfaceId, Map<string, Message[]>>
  viewedThreadId: string | null
  streamingThreads: Set<string>                             // NEW
  fallbackNotices: Map<string, string>                      // NEW
  reconcileErrors: Map<string, Error>                       // NEW
  loadingThreads: Set<string>                               // NEW
  subscriptionsByThread: Map<string, Set<string>>           // NEW
  actions: { ... }
}
```

```typescript
// Current selectors (StreamsProvider.tsx:1375-1381) — what we're extending:
export const useIsStreaming = (): boolean =>
  useStreamsStore((state) => state.isStreaming)
export const useStreamSubscriptions = (runId: string): boolean =>
  useStreamsStore((state) => state.subscriptionsByRunId.has(runId))
```

```typescript
// 067.5 Branch D-3 guard (StreamsProvider.tsx:603-619) — MUST PRESERVE VERBATIM (predicate unchanged):
clearThreadBucket: (surface) => {
  const tid = activeThreadIdRef.current
  if (tid && tid !== streamingThreadIdRef.current) {     // ← THE GUARD (preserve)
    useStreamsStore.setState((state) => {
      // ... delete tid's bucket from the surface map ...
      return {
        bucketsBySurface: nextBuckets,
        isStreaming: false,   // ← DROP this write (Plan 01); streaming-end SSE handler owns Set removal
      }
    })
  }
},
```

```typescript
// streamsCache.ts:130-165 — verified independent (D-075.4-A2); reader returns only Map<SurfaceId, Map<string, Message[]>>:
// No references to isStreaming/loadingThreadId/reconcileError/fallbackNotice/subscriptionsByRunId.
// → NO cache migration required; STREAMS_CACHE_VERSION stays at current value.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift 5 globals to per-thread Map/Set in streamsStore.ts + add thread-scoped selectors</name>
  <files>frontend/src/stores/streamsStore.ts</files>
  <read_first>
    - frontend/src/stores/streamsStore.ts (full file — current `StreamsState` interface lines 43-81 + initial defaults lines 89-100)
    - frontend/src/lib/streamsCache.ts (lines 100-165 — verify cache shape independence per D-075.4-A2)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`frontend/src/stores/streamsStore.ts` (Pattern S4 — per-thread Map/Set keyed by threadId — analog at line 44)
    - .planning/phases/075.4-.../075.4-RESEARCH.md §Plan 01 Implementation Surface (D-075.4-A1 + A2)
  </read_first>
  <behavior>
    - Test 1: `streamsStore.ts` `StreamsState` interface contains `streamingThreads: Set<string>`, `fallbackNotices: Map<string, string>`, `reconcileErrors: Map<string, Error>`, `loadingThreads: Set<string>`, `subscriptionsByThread: Map<string, Set<string>>` and DOES NOT contain `isStreaming`, `fallbackNotice`, `reconcileError`, `loadingThreadId`, `subscriptionsByRunId` as type fields.
    - Test 2: Initial state defaults (the `create<StreamsState>()(...)` literal) initialize the 5 new fields to `new Set<string>()` / `new Map<string, ...>()` respectively.
    - Test 3: `STREAMS_CACHE_VERSION` value unchanged (D-075.4-A2: no localStorage migration).
  </behavior>
  <action>
    Refactor `frontend/src/stores/streamsStore.ts` per D-075.4-A1. Rename the 5 globals exactly as listed (no `legacy` aliases — clean cut):
    1. `isStreaming: boolean` → `streamingThreads: Set<string>`
    2. `fallbackNotice: string | null` → `fallbackNotices: Map<string, string>`
    3. `reconcileError: { threadId; error } | null` → `reconcileErrors: Map<string, Error>`
    4. `loadingThreadId: string | null` → `loadingThreads: Set<string>`
    5. `subscriptionsByRunId: Set<string>` → `subscriptionsByThread: Map<string, Set<string>>`
    Update the `StreamsState` interface (lines 43-81) and the initial-state defaults inside `create<StreamsState>()(subscribeWithSelector(() => ({ ... })))` (lines 89-100). Initialize each new field to a fresh empty `Set<string>` or `Map<string, T>`.

    Do NOT touch `bucketsBySurface`, `viewedThreadId`, `STREAMS_CACHE_VERSION`, or `readSnapshotSyncOrEmpty()`. Cache shape is structurally independent per D-075.4-A2 — `streamsCache.ts:130-165` only reads `parsed.surfaces`.

    Comment block above the new fields citing Plan 075.4-01 + D-075.4-A1 + PATTERNS.md S4 (analog: `bucketsBySurface` at line 44).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -c "streamsStore.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "streamingThreads: Set<string>" frontend/src/stores/streamsStore.ts` returns >= 2 (interface declaration + initial-state default)
    - `grep -c "fallbackNotices: Map<string, string>" frontend/src/stores/streamsStore.ts` returns >= 2
    - `grep -c "reconcileErrors: Map<string, Error>" frontend/src/stores/streamsStore.ts` returns >= 2
    - `grep -c "loadingThreads: Set<string>" frontend/src/stores/streamsStore.ts` returns >= 2
    - `grep -c "subscriptionsByThread: Map<string, Set<string>>" frontend/src/stores/streamsStore.ts` returns >= 2
    - `grep -cE "^\s+isStreaming:|^\s+fallbackNotice:|^\s+reconcileError:|^\s+loadingThreadId:|^\s+subscriptionsByRunId:" frontend/src/stores/streamsStore.ts` returns 0
    - `grep "STREAMS_CACHE_VERSION" frontend/src/stores/streamsStore.ts` returns same value as on HEAD (no version bump per D-075.4-A2)
    - `cd frontend && npx tsc --noEmit` MAY report errors in StreamsProvider.tsx/ChatArea.tsx because they still consume old fields — those are fixed in Tasks 2-3; only assert no errors in streamsStore.ts itself
  </acceptance_criteria>
  <done>StreamsState interface lifted to per-thread Map/Set for all 5 globals; initial state defaults match; cache version unchanged; downstream TS errors are localized to consumer files (fixed in Tasks 2-3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update StreamsProvider.tsx writes + selectors + PRESERVE 067.5 Branch D-3 guard verbatim</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/hooks/useMessages.ts</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx (lines 540-650 for `clearThreadBucket` guard + 700-1280 for all 19 write sites + 1370-1382 for selectors)
    - frontend/src/hooks/useMessages.ts (full 89 lines — thin reader)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`frontend/src/providers/StreamsProvider.tsx — clearThreadBucket guard` (analog at lines 599-619 — PRESERVE PREDICATE VERBATIM)
    - .planning/phases/067.5-frontend-reconcile-fix-empty-thread-until-refresh/067.5-01-SUMMARY.md (Branch D-3 context — protects against regression)
  </read_first>
  <behavior>
    - Test 1: `clearThreadBucket` predicate `tid !== streamingThreadIdRef.current` remains byte-identical (verified via grep).
    - Test 2: All 19 write sites in StreamsProvider.tsx that previously wrote to `isStreaming`/`fallbackNotice`/`reconcileError`/`loadingThreadId`/`subscriptionsByRunId` now write to the per-thread fields via copy-then-mutate (`new Set(prev).add(tid)` / `new Map(prev).set(tid, ...)`).
    - Test 3: 4 new selectors exported: `useStreamingForThread`, `useLoadingForThread`, `useReconcileErrorForThread`, `useFallbackNoticeForThread`. Existing `useIsStreaming()` rewritten to `useStreamsStore((s) => s.streamingThreads.size > 0)`. `useStreamSubscriptions(runId)` rewritten to scan `subscriptionsByThread.values()`.
    - Test 4: `useMessages.ts` `useIsStreaming` re-export rewritten to internal `useStreamingForThread(viewedThreadId)` call; ChatArea public destructure shape preserved.
  </behavior>
  <action>
    Refactor `frontend/src/providers/StreamsProvider.tsx` per D-075.4-A1 + PATTERNS.md S4.

    **Step 1 — Preserve the 067.5 Branch D-3 guard verbatim (lines 603-619).** The predicate `tid !== streamingThreadIdRef.current` MUST stay unchanged. INSIDE the `setState` callback at line 615, DROP the `isStreaming: false` return key (the streaming-end SSE handler at line 1057 owns Set removal now). Keep `bucketsBySurface: nextBuckets` write. Add a comment citing 067.5 Branch D-3 + Plan 075.4-01 D-075.4-A1.

    **Step 2 — Rewrite all 19 write sites per the table in PATTERNS.md (Plan 01 row):**
    - L:543 `useStreamsStore.setState({ fallbackNotice: ... })` + 4s clear → `useStreamsStore.setState((s) => ({ fallbackNotices: new Map(s.fallbackNotices).set(threadId, msg) }))`; the 4s clear timeout deletes that thread's entry via copy-then-mutate.
    - L:742, 785, 929-932, 986 (subscription add) → `subscriptionsByThread: copy of Map, inner Set gets new runId added via new Set(prev).add(runId)`.
    - L:820-822, 854-856, 1021-1023, 1064-1066 (subscription remove) → mirror: inner Set copy-and-delete; if empty, delete the threadId key entirely.
    - L:912 `isStreaming: true` → `streamingThreads: new Set(prev).add(threadId)`.
    - L:1057 `isStreaming: false` → `streamingThreads: copy-then-delete(threadId)` (`const next = new Set(prev); next.delete(threadId); return { streamingThreads: next }`).
    - L:1179 `loadingThreadId: threadId` → `loadingThreads: new Set(prev).add(threadId)`.
    - L:1215-1216 conditional `reconcileError: null` → `reconcileErrors: copy-then-delete(threadId)` (predicate no longer needed since per-thread key naturally drops the global cross-talk).
    - L:1248 `reconcileError: { threadId, error }` → `reconcileErrors: new Map(prev).set(threadId, error)`.
    - L:1262 `loadingThreadId: null` → `loadingThreads: copy-then-delete(threadId)`.

    **Step 3 — Add 4 new thread-scoped selectors at lines 1375-1381 (after existing `useIsStreaming` / `useStreamSubscriptions`).** Verbatim from PATTERNS.md:
    ```typescript
    export const useStreamingForThread = (threadId: string | null): boolean =>
      useStreamsStore((s) => threadId ? s.streamingThreads.has(threadId) : false)
    export const useLoadingForThread = (threadId: string | null): boolean =>
      useStreamsStore((s) => threadId ? s.loadingThreads.has(threadId) : false)
    export const useReconcileErrorForThread = (threadId: string | null): Error | null =>
      useStreamsStore((s) => threadId ? s.reconcileErrors.get(threadId) ?? null : null)
    export const useFallbackNoticeForThread = (threadId: string | null): string | null =>
      useStreamsStore((s) => threadId ? s.fallbackNotices.get(threadId) ?? null : null)
    ```

    **Step 4 — Rewrite existing selectors:** `useIsStreaming()` → `useStreamsStore((s) => s.streamingThreads.size > 0)` (back-compat: any-thread-streaming). `useStreamSubscriptions(runId)` → scan all `subscriptionsByThread.values()` for the runId.

    **Step 5 — Update `frontend/src/hooks/useMessages.ts`:** rewrite the `useIsStreaming` re-export to call `useStreamingForThread(viewedThreadId)` internally. The public destructure shape that ChatArea consumes stays byte-identical (`{ isStreaming, fallbackNotice, ... }` destructure pattern; values become thread-scoped under the hood). For fallbackNotice expose `useFallbackNoticeForThread(viewedThreadId)`.

    Add a `// Plan 075.4-01 D-075.4-A1: per-thread state lift; 067.5 Branch D-3 guard preserved at line 603-619` comment block at file top.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -cE "StreamsProvider\.tsx|useMessages\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tid !== streamingThreadIdRef\.current" frontend/src/providers/StreamsProvider.tsx` returns 1 (Branch D-3 guard preserved)
    - `grep -cE "isStreaming: (true|false)" frontend/src/providers/StreamsProvider.tsx` returns 0 (all writes lifted)
    - `grep -cE "loadingThreadId: (threadId|null)" frontend/src/providers/StreamsProvider.tsx` returns 0
    - `grep -c "fallbackNotice:" frontend/src/providers/StreamsProvider.tsx` returns 0 (writes lifted; only `fallbackNotices` map writes remain)
    - `grep -c "reconcileError:" frontend/src/providers/StreamsProvider.tsx` returns 0 (writes lifted)
    - `grep -c "subscriptionsByRunId:" frontend/src/providers/StreamsProvider.tsx` returns 0 (writes lifted)
    - `grep -c "export const useStreamingForThread" frontend/src/providers/StreamsProvider.tsx` returns 1
    - `grep -c "export const useLoadingForThread" frontend/src/providers/StreamsProvider.tsx` returns 1
    - `grep -c "export const useReconcileErrorForThread" frontend/src/providers/StreamsProvider.tsx` returns 1
    - `grep -c "export const useFallbackNoticeForThread" frontend/src/providers/StreamsProvider.tsx` returns 1
    - `grep -c "useStreamingForThread(viewedThreadId)" frontend/src/hooks/useMessages.ts` returns 1 OR `useStreamingForThread` is referenced from useMessages.ts
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "StreamsProvider\.tsx|useMessages\.ts"` returns 0
  </acceptance_criteria>
  <done>All 19 StreamsProvider write sites lifted; 4 new selectors exported; useMessages.ts re-exports rewritten; 067.5 Branch D-3 guard predicate byte-identical; TypeScript clean for StreamsProvider.tsx and useMessages.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite ChatArea + MessageList consumers to use thread-scoped selectors (closes BUG-260523-01)</name>
  <files>frontend/src/components/chat/ChatArea.tsx, frontend/src/components/chat/MessageList.tsx</files>
  <read_first>
    - frontend/src/components/chat/ChatArea.tsx (full file — 7 lift sites at lines 30-31, 52, 56-57, 59, 65, 221, 308-310, 318, 348)
    - frontend/src/components/chat/MessageList.tsx (lines 8, 15, 76, 79, 83, 95 — prop drilling)
    - .planning/phases/075.4-.../075.4-RESEARCH.md §Plan 01 Implementation Surface (ChatArea.tsx 7-hit table)
    - .planning/reported-bugs/composer-locked-globally-during-any-stream.md (BUG-260523-01 — closes via per-thread `disabled={useStreamingForThread(thread?.id)}` at L:221)
  </read_first>
  <behavior>
    - Test 1: ChatArea.tsx imports `useStreamingForThread`, `useLoadingForThread`, `useReconcileErrorForThread`, `useFallbackNoticeForThread` from `@/providers/StreamsProvider`.
    - Test 2: `disabled={isStreaming}` at L:221 → `disabled={useStreamingForThread(thread?.id ?? null)}` — closes BUG-260523-01.
    - Test 3: ChatArea no longer destructures `reconcileError.threadId` predicate (selector returns thread-scoped value naturally).
    - Test 4: MessageList prop-drill unchanged — receives thread-scoped `isStreaming` boolean from ChatArea (no store touch in MessageList).
  </behavior>
  <action>
    Rewrite `frontend/src/components/chat/ChatArea.tsx` per the 7-hit refactor table in RESEARCH §Plan 01:

    1. L:30-31 — destructure shape from `useMessages()` stays `{ isStreaming, fallbackNotice, ... }` because useMessages.ts now exposes thread-scoped values internally (Task 2 wired this). If destructure needs adjustment to read from thread-scoped selectors directly instead, prefer the direct-selector path.
    2. L:52 — replace `const reconcileError = useStreamsStore((s) => s.reconcileError)` with `const reconcileError = useReconcileErrorForThread(thread?.id ?? null)`. Add import at file top.
    3. L:56-57 — replace `loadingThreadId === thread.id` derivation with `const isLoadingThisThread = useLoadingForThread(thread?.id ?? null)`. Use `isLoadingThisThread` directly downstream.
    4. L:59, 65 — replace `setState({ reconcileError: null })` with new `actions.clearReconcileError(thread.id)` call OR (simpler) replace with `useStreamsStore.setState((s) => { const next = new Map(s.reconcileErrors); next.delete(thread.id); return { reconcileErrors: next } })`. Pick whichever pattern is consistent with StreamsProvider.tsx Task 2 — if Task 2 added a `clearReconcileError` action, use it; otherwise inline the copy-then-delete.
    5. L:221 — `disabled={isStreaming}` becomes `disabled={useStreamingForThread(thread?.id ?? null)}`. This is the BUG-260523-01 close-out: now Thread B's composer is NOT disabled by Thread A's streaming.
    6. L:308-310 — `{fallbackNotice && ...}` consumes the thread-scoped value already (since useMessages.ts internally calls `useFallbackNoticeForThread(viewedThreadId)`). If a direct read is cleaner, call `useFallbackNoticeForThread(thread?.id ?? null)` here.
    7. L:318 — `reconcileError && reconcileError.threadId === thread?.id` simplifies to just `reconcileError &&` since the selector returns null for any other thread by construction.
    8. L:348 — `<MessageList isStreaming={isStreaming} />` — thread-scoped value passes through (the prop is now the per-thread boolean from the thread-scoped selector).

    For `frontend/src/components/chat/MessageList.tsx`: NO code change required (prop drilling stays the same; the boolean it receives is now thread-scoped). Verify by reading lines 8, 15, 76, 79, 83, 95 — confirm no direct store reads.

    Add a `// Plan 075.4-01 D-075.4-A1: thread-scoped composer enablement; closes BUG-260523-01 at L:221` comment block above the disabled-prop line.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -cE "ChatArea\.tsx|MessageList\.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useStreamingForThread" frontend/src/components/chat/ChatArea.tsx` returns >= 1 (either for `disabled` prop or destructured from useMessages)
    - `grep -c "useReconcileErrorForThread" frontend/src/components/chat/ChatArea.tsx` returns >= 1
    - `grep -c "useLoadingForThread" frontend/src/components/chat/ChatArea.tsx` returns >= 1
    - `grep -c "reconcileError\.threadId" frontend/src/components/chat/ChatArea.tsx` returns 0 (predicate no longer needed; selector returns thread-scoped naturally)
    - `grep -c "useStreamsStore((s) => s.reconcileError)" frontend/src/components/chat/ChatArea.tsx` returns 0
    - `grep -c "loadingThreadId" frontend/src/components/chat/ChatArea.tsx` returns 0
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "ChatArea\.tsx|MessageList\.tsx"` returns 0
    - The `disabled` prop on the composer textarea (around L:221) MUST resolve via `useStreamingForThread(thread?.id ?? null)` (BUG-260523-01 close)
  </acceptance_criteria>
  <done>ChatArea consumes 4 new thread-scoped selectors; BUG-260523-01 closed at composer `disabled` prop; MessageList prop-drill preserved; TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Per-thread bleed-through regression test + 067.5 empty-thread-until-refresh regression test</name>
  <files>frontend/src/__tests__/integration/streamsStore_per_thread.test.ts, frontend/src/__tests__/providers/streamsProvider_067_5_regression.test.tsx</files>
  <read_first>
    - frontend/src/__tests__/providers/streamsProvider.test.tsx (analog — Phase 068 + 068.5 + 067.5 regression coverage shape; specifically the `describe "Phase 068 — L-068-01 Branch D-3 ..."` block ~line 235)
    - frontend/src/__tests__/hooks/useMessages.test.ts (lines 403-431 — existing Phase 067.5 Row 11 regression test; new test EXTENDS not duplicates)
    - .planning/phases/075.4-.../075.4-RESEARCH.md §Plan 01 — the 067.5 regression test must exercise (1) Thread A streaming → `streamingThreads.has(A) === true`; (2) flip activeThreadIdRef to B; A still in set; (3) ChatArea would call `clearMessages()` (now no-op since B has no bucket) — A's bucket preserved; (4) refresh → reconcile from cache → A's messages appear.
  </read_first>
  <behavior>
    - Test file 1 (`streamsStore_per_thread.test.ts`):
      - Test 1: Two threads streaming in parallel → `streamingThreads.has("thread-a") && streamingThreads.has("thread-b")` both true; `useStreamingForThread("thread-a")` and `useStreamingForThread("thread-b")` both true; `useStreamingForThread("thread-c")` false.
      - Test 2: Composer disabled-state derived per-thread (`useStreamingForThread("thread-a") === true` does NOT imply `useStreamingForThread("thread-b") === true`).
      - Test 3: `subscriptionsByThread.get("thread-a")?.has("run-1") === true` after subscribe; `subscriptionsByThread.get("thread-b")?.has("run-1") === false`.
      - Test 4: `reconcileErrors.get("thread-a")` returns Error for A; `reconcileErrors.get("thread-b")` returns null.
      - Test 5: GC — when streaming ends on thread-a, `streamingThreads.has("thread-a") === false` AND `subscriptionsByThread.get("thread-a") === undefined` (if Set was emptied via inner-empty-then-delete-key path).
    - Test file 2 (`streamsProvider_067_5_regression.test.tsx`):
      - Test 1: Thread A starts streaming → `useStreamingForThread("thread-a") === true`.
      - Test 2: User navigates to Thread B; `activeThreadIdRef.current = "thread-b"`; `streamingThreadIdRef.current = "thread-a"` (still A is streaming).
      - Test 3: `clearThreadBucket("messages")` is invoked → predicate `tid !== streamingThreadIdRef.current` evaluates `"thread-b" !== "thread-a" === true` → bucket deletion runs for thread-b BUT thread-b has no bucket (no-op); thread-a's bucket is PRESERVED.
      - Test 4: Multi-thread parallel: Thread A and Thread B both streaming; switch back from B to A; A's bucket still present.
  </behavior>
  <action>
    Create `frontend/src/__tests__/integration/streamsStore_per_thread.test.ts` mirroring the test conventions of `frontend/src/__tests__/providers/streamsProvider.test.tsx`. Use vitest + @testing-library/react + the existing `useStreamsStore` import + Zustand's `getState()`/`setState()` direct manipulation for test setup. Cover the 5 behaviors above.

    Create `frontend/src/__tests__/providers/streamsProvider_067_5_regression.test.tsx` extending the existing Phase 068 L-068-01 describe block. Specifically add a NEW `describe("Phase 075.4 D-075.4-A3 — per-thread streaming preserves cross-thread bucket")` containing the 4 sub-tests above. Reuse `streamingThreadIdRef` + `activeThreadIdRef` simulation pattern from the existing test (mock refs via `vi.fn()` + ref-current shape).

    The new file is in addition to (not a replacement for) the existing `streamsProvider.test.tsx` — leaves backward-compat coverage intact.

    Include in each test file's top docstring: "Phase 075.4 Plan 01 D-075.4-A3 — per-thread state regression coverage; inherited by Phase 082 per FORWARD-REF #2."
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/integration/streamsStore_per_thread.test.ts src/__tests__/providers/streamsProvider_067_5_regression.test.tsx 2>&1 | grep -E "(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/__tests__/integration/streamsStore_per_thread.test.ts` (file exists)
    - `test -f frontend/src/__tests__/providers/streamsProvider_067_5_regression.test.tsx` (file exists)
    - `wc -l frontend/src/__tests__/integration/streamsStore_per_thread.test.ts` returns >= 80 lines
    - `wc -l frontend/src/__tests__/providers/streamsProvider_067_5_regression.test.tsx` returns >= 50 lines
    - `grep -c "useStreamingForThread" frontend/src/__tests__/integration/streamsStore_per_thread.test.ts` returns >= 3
    - `grep -c "streamingThreads.has" frontend/src/__tests__/integration/streamsStore_per_thread.test.ts` returns >= 2
    - `grep -c "067.5\|Branch D-3\|clearThreadBucket\|streamingThreadIdRef" frontend/src/__tests__/providers/streamsProvider_067_5_regression.test.tsx` returns >= 2
    - `cd frontend && npx vitest run src/__tests__/integration/streamsStore_per_thread.test.ts src/__tests__/providers/streamsProvider_067_5_regression.test.tsx` exits 0 (all tests green)
    - Existing `frontend/src/__tests__/providers/streamsProvider.test.tsx` AND `frontend/src/__tests__/hooks/useMessages.test.ts` still green: `cd frontend && npx vitest run src/__tests__/providers/streamsProvider.test.tsx src/__tests__/hooks/useMessages.test.ts` exits 0
  </acceptance_criteria>
  <done>Two new test files exercising per-thread state correctness + 067.5 Branch D-3 multi-thread parallel preservation; all green; existing 068/067.5 coverage unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Zustand store | Client-only state; no untrusted input crosses the store boundary directly. |
| Cross-thread state | Each thread's stream metadata MUST be isolated to that thread's key; cross-thread bleed is the threat surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075.4-01 | I (Information disclosure) — cross-thread state confusion | `streamsStore.ts` per-thread Map/Set lift | mitigate | All 5 globals key by threadId; thread-scoped selectors (`useStreamingForThread(threadId)`) gate every consumer read. Regression test `streamsStore_per_thread.test.ts` asserts `streamingThreads.has(threadA)` does NOT imply `streamingThreads.has(threadB)`. RLS on thread ownership is enforced at DB layer (out of scope for client store); threadId-to-userId binding stays at Supabase row-level — no client-side cross-user leak path introduced by this refactor. |
</threat_model>

<verification>
## Plan-Level Checks
1. `cd frontend && npx tsc --noEmit` exits 0 (no TS errors anywhere in the codebase after all 4 tasks land).
2. `cd frontend && npx vitest run src/__tests__/integration/streamsStore_per_thread.test.ts src/__tests__/providers/streamsProvider_067_5_regression.test.tsx src/__tests__/providers/streamsProvider.test.tsx src/__tests__/hooks/useMessages.test.ts` exits 0.
3. `grep -c "isStreaming: boolean\|fallbackNotice: string\|reconcileError: {\|loadingThreadId: string\|subscriptionsByRunId: Set" frontend/src/stores/streamsStore.ts` returns 0 (all 5 globals lifted).
4. `grep -c "tid !== streamingThreadIdRef\.current" frontend/src/providers/StreamsProvider.tsx` returns 1 (067.5 Branch D-3 guard predicate preserved verbatim).
5. `grep -c "STREAMS_CACHE_VERSION" frontend/src/stores/streamsStore.ts` returns same value as HEAD (no version bump — D-075.4-A2).

## UAT Scoreboard (under new CLAUDE.md §UAT scoreboard recipe rule — Plan 05 Wave 0 lands the rule)

| Provider | Tools | Threads | Msg len | Expected | Actual | Pass |
|---|---|---|---|---|---|---|
| OpenAI gpt-5.x | 0 (chat-only) | 2 parallel | short | Thread B composer accepts input while Thread A streams | | |
| Anthropic claude-4.x | 2 (sandbox + skill) | 2 parallel | short | No cross-thread tool-card bleed; both threads' composers independent | | |
| Google gemini-3-pro | 3 (multi-tool) | 2 parallel | short | Thread B composer not gated by A's streaming | | |
| OpenRouter | 1 | 2 parallel + page nav A→B→A | long (≥50 messages) | A's bucket persists across A→B→A nav cycle; 067.5 regression closed | | |
</verification>

<success_criteria>
- All 5 globals in `streamsStore.ts` lifted to per-thread keys (Map/Set keyed by threadId)
- All 19 StreamsProvider.tsx write sites refactored to copy-then-mutate per-thread fields
- 4 new thread-scoped selectors exported: `useStreamingForThread`, `useLoadingForThread`, `useReconcileErrorForThread`, `useFallbackNoticeForThread`
- 067.5 Branch D-3 guard predicate at StreamsProvider.tsx:603-619 preserved byte-identical
- ChatArea composer `disabled` prop now per-thread (closes BUG-260523-01)
- 2 new regression tests landed (per-thread bleed-through + 067.5 multi-thread parallel)
- Cache shape unchanged; STREAMS_CACHE_VERSION not bumped (D-075.4-A2)
- TypeScript clean across all touched files
- Existing 068 + 068.5 + 067.5 vitest coverage stays green
</success_criteria>

<forward_references>
- → Phase 082 (cross-cutting verification): the 067.5 regression test added here is INHERITED — Phase 082 must keep this test in its substrate.
- → Phase 077/079: this plan introduces no Redis-backed state; per-thread Map/Set is client-only and survives single-worker → multi-worker promotion unchanged.
</forward_references>

<output>
After completion, create `.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-01-SUMMARY.md`
</output>
