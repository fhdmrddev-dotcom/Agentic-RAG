---
phase: 075.4
plan: 05
type: execute
wave: 0
depends_on: ["075.4-01", "075.4-02", "075.4-03", "075.4-04"]  # only Wave 3 tasks need this
files_modified:
  - CLAUDE.md  # Wave 0
  - frontend/playwright.config.ts  # Wave 0
  - frontend/tests/e2e/fixtures/auth.fixture.ts  # Wave 0
  - frontend/tests/e2e/fixtures/db-teardown.fixture.ts  # Wave 0
  - frontend/tests/e2e/fixtures/langsmith.fixture.ts  # Wave 0
  - frontend/tests/e2e/scenario-01-parallel-composers.spec.ts  # Wave 3
  - frontend/tests/e2e/scenario-02-gemini-thought-signature.spec.ts  # Wave 3
  - frontend/tests/e2e/scenario-03-openrouter-final-pptx.spec.ts  # Wave 3
  - frontend/tests/e2e/scenario-04-done-latency.spec.ts  # Wave 3
  - frontend/tests/e2e/scenario-05-unknown-model-inference.spec.ts  # Wave 3
  - frontend/tests/e2e/scenario-06-iteration-parity.spec.ts  # Wave 3
  - scripts/restart-backend.sh  # Wave 0
  - scripts/restart-backend.ps1  # Wave 0
  - .github/workflows/frontend-tests.yml  # Wave 0
  - backend/tests/conftest.py  # Wave 0
  - backend/app/api/health.py  # Wave 0
  - backend/app/main.py  # Wave 0 — router mount
  - frontend/package.json  # Wave 0 — @playwright/test devDep
  - .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md  # Wave 3
  - .planning/reported-bugs/composer-locked-globally-during-any-stream.md  # Wave 0 — folded_into: 075.4
  - .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md  # Wave 0 — folded
  - .planning/reported-bugs/openrouter-duplicate-output-and-delayed-finish.md  # Wave 0 — folded
  - .planning/reported-bugs/empty-response-fallback-misleading-iter-count.md  # Wave 0 — folded
  - .planning/reported-bugs/final-outputs-backend-omits-url-in-event-payload.md  # Wave 0 — folded
  - .planning/reported-bugs/final-outputs-pinned-panel-no-download-link.md  # Wave 0 — re_open_trigger noted
  - .planning/reported-bugs/anthropic-excessive-tool-iterations-on-multi-step-tasks.md  # Wave 0 — status: deferred
autonomous: false
success_criteria_addressed: [SC#8, SC#9, SC#10]

must_haves:
  truths:
    - "CLAUDE.md has new ## UAT scoreboard recipe (MANDATORY) section enforcing 4-axis bandwidth"
    - "Playwright installed with config + 3 fixtures (auth env-driven, db-teardown localhost-gated, langsmith poller)"
    - "6 E2E scenarios exist, each mapping 1:1 to a regression class + each asserts a LangSmith trace"
    - "DB-state teardown asserts no orphaned runs.status='streaming' for test user; localhost hard-gate"
    - "restart-backend.{ps1,sh} kills+relaunches uvicorn; Windows orphan-worker aware"
    - "/health endpoint returns 200 with {status: 'ok'}"
    - "frontend-tests.yml runs vitest + Playwright on PR; uses secrets.LANGSMITH_API_KEY"
    - "075.4-TEST-TRIAGE.md categorizes all 95 pre-existing backend test failures; sums to 95"
    - "5 bug reports flipped to folded_into: 075.4; BUG-260523-04 stays deferred; BUG-260521-02 frontmatter notes auto-close"
  artifacts:
    - path: "CLAUDE.md"
      provides: "## UAT scoreboard recipe (MANDATORY)"
      contains: "cross-provider × multi-tool × parallel-thread × long-message"
    - path: "frontend/playwright.config.ts"
      provides: "Playwright config"
      min_lines: 20
    - path: "frontend/tests/e2e/scenario-01-parallel-composers.spec.ts"
      provides: "BUG-260523-01 regression"
      min_lines: 40
    - path: "frontend/tests/e2e/scenario-06-iteration-parity.spec.ts"
      provides: "BUG-260523-04 measurement (NOT root-cause fix)"
      min_lines: 50
    - path: ".github/workflows/frontend-tests.yml"
      provides: "vitest + Playwright CI"
      min_lines: 50
    - path: ".planning/phases/075.4-.../075.4-TEST-TRIAGE.md"
      provides: "95-failure categorization (revised from stale 44)"
      min_lines: 120
    - path: "scripts/restart-backend.ps1"
      provides: "Windows uvicorn restart"
      min_lines: 15
    - path: "backend/app/api/health.py"
      provides: "/health endpoint"
      contains: '"/health"'
  key_links:
    - from: "frontend/tests/e2e/fixtures/db-teardown.fixture.ts"
      to: "process.env.SUPABASE_URL contains 'localhost' or '127.0.0.1'"
      via: "hard-gate at fixture entry"
      pattern: "(localhost|127\\.0\\.0\\.1)"
    - from: ".github/workflows/frontend-tests.yml"
      to: "secrets.LANGSMITH_API_KEY"
      via: "GitHub Actions secret + auto-redaction"
      pattern: "secrets\\.LANGSMITH_API_KEY"
---

<objective>
**Wave 0** — Test infra bootstrap so Plans 01-04 author UAT scoreboards under the new CLAUDE.md normative rule:
1. CLAUDE.md normative UAT recipe rule (SC#10).
2. Playwright config + 3 fixtures (auth, db-teardown, langsmith).
3. restart-backend.{ps1,sh} scripts.
4. .github/workflows/frontend-tests.yml CI.
5. /health endpoint.
6. backend/tests/conftest.py FK-aware runs fixture.
7. Bug-report frontmatter updates (5 folded + 1 deferred + 1 auto-close).

**Wave 3** — After Plans 01-04 commit:
1. 6 Playwright scenarios mapping 1:1 to regression classes.
2. Per-scenario LangSmith trace assertions.
3. DB-state teardown.
4. 075.4-TEST-TRIAGE.md categorizing ALL 95 pre-existing backend test failures (NOT 44 per stale audit).
5. SANDBOX_IMAGE docs coordination with Plan 04 (avoid double-write).

Purpose: After 075.4 ships, Phases 076 → 082.5 proceed without insert-phases — the test backstop + normative UAT rule catch what phase-internal UATs have been missing.
Output: 9 frontend test files + 2 scripts + 1 CI workflow + 1 backend route + 1 conftest fixture + 1 triage artifact + 7 bug-report frontmatter updates.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-CONTEXT.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-RESEARCH.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-PATTERNS.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-VALIDATION.md

@CLAUDE.md
@.github/workflows/backend-tests.yml
@scripts/regenerate-full-schema.sh

<interfaces>
SC#10 verbatim from ROADMAP:
"Any phase touching streaming, agent loop, provider routing, or UI state MUST include UAT rows for cross-provider × multi-tool × parallel-thread × long-message scenarios."

6 E2E scenarios per RESEARCH §Plan 05 + AUDIT:
1. scenario-01-parallel-composers — Thread A streaming + Thread B composer accepts input (BUG-260523-01)
2. scenario-02-gemini-thought-signature — gemini-3-flash-preview 2+ tool rounds, no 400 (BUG-260523-02)
3. scenario-03-openrouter-final-pptx — OpenRouter pptx-with-charts, exactly 1 final pptx (BUG-260523-03a)
4. scenario-04-done-latency — SSE done → UI settled < 500ms (BUG-260523-03b)
5. scenario-05-unknown-model-inference — gpt-99-mega under OpenAI, inference fallback (Phase 075.3 Plan 02 guard)
6. scenario-06-iteration-parity — same prompt × 4 providers, counts within ~50% (BUG-260523-04 measurement only)

Backend pytest baseline (RESEARCH §Plan 05, 2026-05-23): 95 failed / 625 passed / 5 skipped of 728 collected.
Top failing files: test_retrieval_service.py (15), test_sql_service.py (12), test_threads_skills.py (11), test_explorer_agent.py (6), test_multimodal_query.py (5), test_provider_router.py (4 FK), test_075_code_stdout_progressive.py (4), test_066_per_call_timer.py (4).

Bug routing table (CONTEXT.md §Reported Bug Routing):
- BUG-260523-01..03 + BUG-260522-01..02 → folded_into: "075.4", status: folded
- BUG-260523-04 → status: deferred (E2E #6 measures only; root-cause fix post-075.4)
- BUG-260521-02 → status: partial-closed (current); re_open_trigger field added
</interfaces>
</context>

<tasks>

<task type="auto" wave="0">
  <name>Task 1 (Wave 0): CLAUDE.md normative UAT recipe rule</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (locate `## Reported bugs cross-check (MANDATORY)` heading for insertion below)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`CLAUDE.md (modify — Wave 0 normative UAT recipe rule, ~30 LOC)` (verbatim shape)
  </read_first>
  <action>
    Insert a NEW `## UAT scoreboard recipe (MANDATORY)` section directly BELOW the existing `## Reported bugs cross-check (MANDATORY)` section. Verbatim shape from PATTERNS.md (sized 25-35 lines). Must contain (a) blockquote of SC#10 verbatim, (b) 4-axis bandwidth table (Cross-provider / Multi-tool / Parallel-thread / Long-message), (c) closing rule that UAT rows MUST be in VALIDATION.md not PLAN.md.
  </action>
  <verify>
    <automated>grep -c "## UAT scoreboard recipe (MANDATORY)" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## UAT scoreboard recipe (MANDATORY)" CLAUDE.md` returns 1
    - `grep -c "cross-provider × multi-tool × parallel-thread × long-message" CLAUDE.md` returns >= 1
    - `grep -c "ROADMAP SC#10" CLAUDE.md` returns >= 1
    - `grep -A 50 "## UAT scoreboard recipe (MANDATORY)" CLAUDE.md | grep -c "OpenAI, Anthropic, Google, OpenRouter"` returns >= 1
    - Placed AFTER `## Reported bugs cross-check (MANDATORY)`: `awk '/## Reported bugs cross-check/{found=1} found && /## UAT scoreboard recipe/{print "OK"; exit}' CLAUDE.md` prints `OK`
  </acceptance_criteria>
  <done>CLAUDE.md has a new MANDATORY section enforcing the 4-axis UAT recipe; placed below the bugs cross-check rule.</done>
</task>

<task type="auto" wave="0">
  <name>Task 2 (Wave 0): Playwright bootstrap — config + 3 fixtures + package.json devDep</name>
  <files>frontend/playwright.config.ts, frontend/tests/e2e/fixtures/auth.fixture.ts, frontend/tests/e2e/fixtures/db-teardown.fixture.ts, frontend/tests/e2e/fixtures/langsmith.fixture.ts, frontend/package.json</files>
  <read_first>
    - frontend/package.json (verify `@playwright/test` is NOT yet devDep)
    - frontend/vitest.config.ts (ensure `tests/e2e/**/*.spec.ts` does NOT overlap vitest's glob)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`frontend/playwright.config.ts + frontend/tests/e2e/*.spec.ts (create)` + §Security Threat Surfaces Plan 05
    - reference_local_dev_app.md memory (test login fhdmrd@gmail.com / 123456)
  </read_first>
  <action>
    **Step 1:** `cd frontend && npm install --save-dev @playwright/test && npx playwright install --with-deps chromium`. Add `"e2e": "playwright test"` to scripts in package.json.

    **Step 2 — playwright.config.ts (≥20 lines):**
    ```typescript
    import { defineConfig } from '@playwright/test'
    export default defineConfig({
      testDir: './tests/e2e',
      testMatch: /scenario-\d+-.+\.spec\.ts$/,
      fullyParallel: false,  // DB-teardown shared state
      forbidOnly: !!process.env.CI,
      retries: process.env.CI ? 2 : 0,
      workers: 1,
      reporter: process.env.CI ? 'github' : 'list',
      use: {
        baseURL: process.env.E2E_BASE_URL || 'http://localhost:5173',
        trace: 'on-first-retry',
        screenshot: 'only-on-failure',
      },
      projects: [{ name: 'chromium', use: { browserName: 'chromium' } }],
    })
    ```

    **Step 3 — auth.fixture.ts:** env-driven creds (NOT committed) — `process.env.E2E_USER_EMAIL` / `E2E_USER_PASSWORD` with `fhdmrd@gmail.com`/`123456` defaults; Playwright `test.extend` pattern yielding `authedPage` that logs in via `/login` form fill+submit, waits for redirect away from `/login`.

    **Step 4 — db-teardown.fixture.ts:** export `teardownTestUserData(email)` + `assertNoOrphanedStreamingRuns(email)`. CRITICAL hard-gate at top of both functions: if `process.env.SUPABASE_URL` does NOT contain `'localhost'` or `'127.0.0.1'`, throw with message `"db-teardown refusing to run: SUPABASE_URL must contain 'localhost' or '127.0.0.1'"`. Use `@supabase/supabase-js` admin client with `SUPABASE_SERVICE_ROLE_KEY`; find user via `supabase.auth.admin.listUsers()`; delete runs → messages → threads in FK-safe order. `assertNoOrphanedStreamingRuns` queries runs.status='streaming' for the user and throws if any rows.

    **Step 5 — langsmith.fixture.ts:** export `pollLangSmithTrace(runId, opts)` + `assertLangSmithTraceExists(runId, providerExpected?)`. Polls `https://api.smith.langchain.com/runs/{run_id}` with `X-API-Key: $LANGSMITH_API_KEY`. Exponential backoff: `[1000, 2000, 4000, 4000, 4000]ms` (5 attempts, ~15s ceiling). If `process.env.LANGSMITH_API_KEY` unset, returns null + console.warn (soft-skip in dev; CI sets secret). On 200 response, validates `trace.run_type === "llm"` AND if `providerExpected` passed, validates `trace.metadata.provider || trace.metadata.ls_provider` matches case-insensitive.
  </action>
  <verify>
    <automated>cd frontend && cat package.json | grep -c "@playwright/test" && test -f playwright.config.ts && test -f tests/e2e/fixtures/auth.fixture.ts && test -f tests/e2e/fixtures/db-teardown.fixture.ts && test -f tests/e2e/fixtures/langsmith.fixture.ts && echo "all-present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@playwright/test"' frontend/package.json` returns >= 1
    - `test -f frontend/playwright.config.ts`
    - `test -f frontend/tests/e2e/fixtures/auth.fixture.ts`
    - `test -f frontend/tests/e2e/fixtures/db-teardown.fixture.ts`
    - `test -f frontend/tests/e2e/fixtures/langsmith.fixture.ts`
    - `wc -l frontend/playwright.config.ts` returns >= 20
    - `grep -c "process.env.E2E_USER_EMAIL\|process.env.E2E_USER_PASSWORD" frontend/tests/e2e/fixtures/auth.fixture.ts` returns >= 2
    - `grep -c "localhost\|127.0.0.1" frontend/tests/e2e/fixtures/db-teardown.fixture.ts` returns >= 2
    - `grep -c "process.env.LANGSMITH_API_KEY" frontend/tests/e2e/fixtures/langsmith.fixture.ts` returns >= 1
    - `grep -cE "1000.*2000.*4000|backoff|delays" frontend/tests/e2e/fixtures/langsmith.fixture.ts` returns >= 1
    - `cd frontend && npx playwright --version 2>&1` succeeds
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "playwright\.config|tests/e2e/fixtures"` returns 0
  </acceptance_criteria>
  <done>Playwright installed; config + 3 fixtures landed; creds env-driven; localhost hard-gate in db-teardown; LangSmith poller exponential backoff; vitest glob unchanged.</done>
</task>

<task type="auto" wave="0">
  <name>Task 3 (Wave 0): restart-backend.{ps1,sh} + /health endpoint</name>
  <files>scripts/restart-backend.sh, scripts/restart-backend.ps1, backend/app/api/health.py, backend/app/main.py</files>
  <read_first>
    - scripts/regenerate-full-schema.sh (header convention)
    - backend/app/main.py (find router-mount pattern; grep for existing /health endpoint)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`scripts/restart-backend.sh (create)` + §`scripts/restart-backend.ps1 (create)` (verbatim shapes)
  </read_first>
  <action>
    **Step 1 — scripts/restart-backend.sh** per PATTERNS.md verbatim: shebang + `set -euo pipefail` + `pkill -f 'uvicorn.*app.main:app'` + `sleep 2` + relaunch via `source venv/bin/activate; nohup uvicorn ...`. Header comment cites FORWARD-REF #2 + Phase 079. `chmod +x` after creation.

    **Step 2 — scripts/restart-backend.ps1** per PATTERNS.md verbatim: `Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Where-Object { $_.CommandLine -match 'uvicorn.*app\.main:app' } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }`; Start-Sleep 2; Start-Process relaunch with venv activate. Header cites FORWARD-REF #2.

    **Step 3 — /health endpoint.** First grep `backend/app/api/` for existing `/health` route. If absent, create `backend/app/api/health.py`:
    ```python
    from fastapi import APIRouter
    router = APIRouter(tags=["health"])

    @router.get("/health")
    async def health() -> dict:
        """Plan 075.4-05 Wave 0 — minimal health probe."""
        return {"status": "ok"}
    ```
    In `backend/app/main.py` find existing `app.include_router(...)` calls; add `from .api import health as health_router; app.include_router(health_router.router)`. If /health already exists, ensure response includes `status` key.
  </action>
  <verify>
    <automated>test -f scripts/restart-backend.sh && test -f scripts/restart-backend.ps1 && cd backend && venv/Scripts/python -c "from fastapi.testclient import TestClient; from app.main import app; c = TestClient(app); r = c.get('/health'); assert r.status_code == 200 and 'status' in r.json(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/restart-backend.sh` AND `test -f scripts/restart-backend.ps1`
    - `wc -l scripts/restart-backend.sh` returns >= 10; `wc -l scripts/restart-backend.ps1` returns >= 15
    - `grep -c "FORWARD-REF #2\|Phase 079" scripts/restart-backend.sh` returns >= 1
    - `grep -c "FORWARD-REF #2\|Phase 079" scripts/restart-backend.ps1` returns >= 1
    - `grep -c "pkill -f 'uvicorn" scripts/restart-backend.sh` returns 1
    - `grep -c "Get-CimInstance Win32_Process" scripts/restart-backend.ps1` returns 1
    - `cd backend && venv/Scripts/python -c "from fastapi.testclient import TestClient; from app.main import app; c = TestClient(app); r = c.get('/health'); assert r.status_code == 200 and 'status' in r.json(); print('OK')"` prints `OK`
  </acceptance_criteria>
  <done>POSIX + PowerShell restart scripts ship with FORWARD-REF #2 header; /health endpoint returns 200 with {"status": "ok"}.</done>
</task>

<task type="auto" wave="0">
  <name>Task 4 (Wave 0): FK-aware runs fixture in backend/tests/conftest.py</name>
  <files>backend/tests/conftest.py</files>
  <read_first>
    - backend/tests/conftest.py (current state — may not exist at top-level; only tests/integration/conftest.py)
    - backend/tests/test_provider_router.py (failure pattern: ForeignKeyViolationError runs_thread_id_fkey)
    - backend/tests/integration/test_066_langsmith_clean.py (same FK pattern, 2 fails)
    - PROJECT.md Phase 073-04 Rule-1 deviation (FK-safe seed: auth.users → threads → runs)
  </read_first>
  <action>
    Read existing `backend/tests/conftest.py` (may be empty or absent). Add an FK-aware factory fixture for `runs` rows that auto-creates parent `threads` row + seeds `auth.users` row. Pattern from PROJECT.md Phase 073-04 Rule-1 deviation:

    ```python
    import pytest_asyncio
    import uuid

    @pytest_asyncio.fixture
    async def fk_aware_runs_factory(_supabase):  # adapt to your existing supabase fixture name (check tests/integration/conftest.py)
        """Plan 075.4-05 Wave 0 — closes ~5 of 95 pre-existing failures (FK violations: runs without threads).
        Yields make_run(user_id, **overrides). FK-safe cleanup in reverse order on teardown.
        """
        created = {"users": [], "threads": [], "runs": []}
        async def make_run(user_id=None, **overrides):
            uid = user_id or str(uuid.uuid4())
            try:
                _supabase.table("auth.users").insert({"id": uid}).execute()
                created["users"].append(uid)
            except Exception: pass
            tid = str(uuid.uuid4())
            _supabase.table("threads").insert({"id": tid, "user_id": uid, "title": "test"}).execute()
            created["threads"].append(tid)
            rid = overrides.pop("id", str(uuid.uuid4()))
            _supabase.table("runs").insert({"id": rid, "user_id": uid, "thread_id": tid, "status": "queued", **overrides}).execute()
            created["runs"].append(rid)
            return {"run_id": rid, "thread_id": tid, "user_id": uid}
        yield make_run
        for rid in created["runs"]:
            try: _supabase.table("runs").delete().eq("id", rid).execute()
            except Exception: pass
        for tid in created["threads"]:
            try: _supabase.table("threads").delete().eq("id", tid).execute()
            except Exception: pass
        for uid in created["users"]:
            try: _supabase.table("auth.users").delete().eq("id", uid).execute()
            except Exception: pass
    ```

    The fixture name `_supabase` may need adjustment to match the existing supabase fixture in tests/integration/conftest.py. Read that conftest first to confirm the exact fixture name. Comment block: `# Plan 075.4-05 Wave 0 — FK-safe factory; pattern from PROJECT.md Phase 073-04 Rule-1 deviation. Used by Task 9 triage to fix the 4 test_provider_router.py + 2 test_066_langsmith_clean.py FK-violation failures.`
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import importlib.util; spec = importlib.util.spec_from_file_location('conftest', 'tests/conftest.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert hasattr(m, 'fk_aware_runs_factory'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/tests/conftest.py`
    - `grep -c "fk_aware_runs_factory" backend/tests/conftest.py` returns >= 1
    - `grep -c "Plan 075.4-05" backend/tests/conftest.py` returns >= 1
    - `grep -cE "FK-safe|FK violation|ForeignKey" backend/tests/conftest.py` returns >= 1
    - `cd backend && venv/Scripts/python -m pytest --collect-only tests/test_provider_router.py 2>&1 | tail -3` runs without ImportError
  </acceptance_criteria>
  <done>FK-aware factory in top-level conftest.py; available via pytest discovery; commented hand-off to Task 9 triage.</done>
</task>

<task type="auto" wave="0">
  <name>Task 5 (Wave 0): frontend-tests.yml CI workflow + 7 bug-report frontmatter updates</name>
  <files>.github/workflows/frontend-tests.yml, .planning/reported-bugs/composer-locked-globally-during-any-stream.md, .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md, .planning/reported-bugs/openrouter-duplicate-output-and-delayed-finish.md, .planning/reported-bugs/empty-response-fallback-misleading-iter-count.md, .planning/reported-bugs/final-outputs-backend-omits-url-in-event-payload.md, .planning/reported-bugs/final-outputs-pinned-panel-no-download-link.md, .planning/reported-bugs/anthropic-excessive-tool-iterations-on-multi-step-tasks.md</files>
  <read_first>
    - .github/workflows/backend-tests.yml (full file — mirror verbatim)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`.github/workflows/frontend-tests.yml (create)` (verbatim shape)
    - All 7 reported-bug .md files — read frontmatter to preserve all fields not being changed
    - .planning/reported-bugs/TEMPLATE.md (frontmatter shape)
    - CONTEXT.md §Reported Bug Routing (5 folded + 1 deferred + 1 auto-close)
  </read_first>
  <action>
    **Step 1 — Create `.github/workflows/frontend-tests.yml`** per PATTERNS.md verbatim shape (50+ lines). Two jobs: `vitest` (npm ci + npm test) + `playwright` (needs vitest; boots Redis from docker-compose.dev.yml + backend uvicorn + waits for /health then runs `npx playwright test` with `secrets.LANGSMITH_API_KEY` in env). Path filters: `frontend/**` + `.github/workflows/frontend-tests.yml`. Branches: master, main, v2.5-dev. After playwright job, add documentation step:
    ```yaml
    - name: LangSmith key redaction note
      if: always()
      run: echo "GitHub Actions auto-redacts secrets.* references in step outputs."
    ```

    **Step 2 — Update bug-report frontmatter (use Edit tool, NOT sed). For 5 reports flip `status: open` → `status: folded` + add `folded_into: "075.4"`:**
    - `composer-locked-globally-during-any-stream.md`
    - `gemini-3-thought-signature-missing-on-tool-rounds.md`
    - `openrouter-duplicate-output-and-delayed-finish.md`
    - `empty-response-fallback-misleading-iter-count.md`
    - `final-outputs-backend-omits-url-in-event-payload.md`

    **Step 3 — `final-outputs-pinned-panel-no-download-link.md`:** Preserve existing status (`partial-closed`). ADD field `re_open_trigger: "If pinned-panel download link still missing after 075.4 ships (BUG-260522-02 final_output_files now includes url+size); else closes naturally."`

    **Step 4 — `anthropic-excessive-tool-iterations-on-multi-step-tasks.md` (BUG-260523-04):** Flip `status` → `deferred`. ADD `re_open_trigger: "After 075.4 ships — Plan 05 E2E #6 measures iteration parity. Focused root-cause investigation phase to follow."`. Do NOT add `folded_into` (per CONTEXT.md: stays open/deferred, NOT folded).

    Preserve all body text and other frontmatter fields. Only modify the listed fields.
  </action>
  <verify>
    <automated>test -f .github/workflows/frontend-tests.yml && grep -c "secrets.LANGSMITH_API_KEY" .github/workflows/frontend-tests.yml && grep -l 'folded_into:.*075.4\|folded_into: "075.4"' .planning/reported-bugs/composer-locked-globally-during-any-stream.md .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md .planning/reported-bugs/openrouter-duplicate-output-and-delayed-finish.md .planning/reported-bugs/empty-response-fallback-misleading-iter-count.md .planning/reported-bugs/final-outputs-backend-omits-url-in-event-payload.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/frontend-tests.yml`
    - `wc -l .github/workflows/frontend-tests.yml` returns >= 50
    - `grep -c "secrets.LANGSMITH_API_KEY" .github/workflows/frontend-tests.yml` returns >= 1
    - `grep -c "npx playwright test" .github/workflows/frontend-tests.yml` returns >= 1
    - `grep -c "npm test" .github/workflows/frontend-tests.yml` returns >= 1
    - `grep -c "docker compose -f docker-compose.dev.yml" .github/workflows/frontend-tests.yml` returns >= 1
    - For each of the 5 folded bugs: `grep -c 'folded_into:.*075.4' <file>` returns >= 1 AND `grep -c '^status: folded\|status: "folded"' <file>` returns >= 1
    - For `final-outputs-pinned-panel-no-download-link.md`: `grep -c "re_open_trigger" <file>` returns >= 1 AND existing status preserved
    - For `anthropic-excessive-tool-iterations-on-multi-step-tasks.md`: `grep -c 'status:.*deferred' <file>` returns >= 1 AND `grep -c "re_open_trigger" <file>` returns >= 1 AND NO `folded_into` field
  </acceptance_criteria>
  <done>frontend-tests.yml CI workflow lands; 7 bug-report frontmatter updates applied per CONTEXT.md routing.</done>
</task>

<task type="auto" wave="3">
  <name>Task 6 (Wave 3): 6 Playwright scenarios with LangSmith trace assertion + DB-state teardown</name>
  <files>frontend/tests/e2e/scenario-01-parallel-composers.spec.ts, frontend/tests/e2e/scenario-02-gemini-thought-signature.spec.ts, frontend/tests/e2e/scenario-03-openrouter-final-pptx.spec.ts, frontend/tests/e2e/scenario-04-done-latency.spec.ts, frontend/tests/e2e/scenario-05-unknown-model-inference.spec.ts, frontend/tests/e2e/scenario-06-iteration-parity.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/fixtures/auth.fixture.ts (from Task 2 — re-use authedPage)
    - frontend/tests/e2e/fixtures/db-teardown.fixture.ts (Task 2)
    - frontend/tests/e2e/fixtures/langsmith.fixture.ts (Task 2)
    - .planning/phases/075.4-.../075.4-RESEARCH.md §`The 6 E2E scenarios` (verbatim assertions)
    - .planning/AUDIT-2026-05-23-cross-cutting-cleanup.md lines 127-132 (regression class table)
    - Plans 01-04 SUMMARYs (read what behaviors actually shipped to assert against)
  </read_first>
  <action>
    Create 6 Playwright spec files following this template per scenario (≥40 lines each except scenario-04 ≥30 + scenario-06 ≥50):

    ```typescript
    import { test, expect } from '../fixtures/auth.fixture'   // re-use authedPage
    import { teardownTestUserData, assertNoOrphanedStreamingRuns } from '../fixtures/db-teardown.fixture'
    import { assertLangSmithTraceExists } from '../fixtures/langsmith.fixture'

    const TEST_USER_EMAIL = process.env.E2E_USER_EMAIL || 'fhdmrd@gmail.com'

    test.describe('@075.4 scenario-NN — <description>', () => {
      test.beforeEach(async () => { await teardownTestUserData(TEST_USER_EMAIL) })
      test.afterEach(async () => { await assertNoOrphanedStreamingRuns(TEST_USER_EMAIL) })

      test('<assertion>', async ({ authedPage: page }) => {
        // ... scenario steps ...
      })
    })
    ```

    **scenario-01-parallel-composers.spec.ts** (BUG-260523-01):
    1. Open New Chat → enter long Anthropic prompt → click Send → wait for streaming to start.
    2. Open second New Chat (Thread B) in same tab via UI nav (or new tab if app supports it; likely sidebar New Chat button).
    3. Assert composer textarea on Thread B is NOT `disabled`.
    4. Type a prompt in Thread B → click Send → assert second run starts (loading spinner appears, /threads/{id}/runs POST returns 201).
    5. Capture run_id from SSE → `assertLangSmithTraceExists(runId)`.

    **scenario-02-gemini-thought-signature.spec.ts** (BUG-260523-02):
    1. Set Settings → AI Model → google + gemini-3-flash-preview (or whichever Gemini 3 model is available).
    2. Send a prompt that triggers 2+ tool rounds (e.g., "Search docs for 'X' then run python to plot results").
    3. Assert no 400 INVALID_ARGUMENT in network responses.
    4. Assert run completes (terminal SSE arrives).
    5. `assertLangSmithTraceExists(runId, 'google')` — provider tag check.

    **scenario-03-openrouter-final-pptx.spec.ts** (BUG-260523-03a):
    1. Set Settings → OpenRouter + minimax-m2.7 (or any OpenRouter model).
    2. Send "Make a 3-slide pptx with sample charts about climate change".
    3. Wait for streaming complete + final_output_files SSE.
    4. Assert exactly ONE .pptx is shown in the final pinned panel (`expect(pptxCards).toHaveCount(1)`).
    5. Assert the visible pptx is the LATEST version (no stale intermediate; if any "Replaces:" subline visible, it points at a prior filename).
    6. `assertLangSmithTraceExists(runId, 'openrouter')`.

    **scenario-04-done-latency.spec.ts** (BUG-260523-03b):
    1. Same OpenRouter run as scenario-03 OR fresh chat with same model.
    2. Send a short prompt that streams.
    3. Capture timestamp when SSE `done`/terminal event arrives (via page.evaluate or via inspecting CDP network events).
    4. Capture timestamp when UI shows "complete" state (loading spinner gone, message marked done).
    5. Assert delta < 500ms.

    **scenario-05-unknown-model-inference.spec.ts** (Phase 075.3 Plan 02 regression-guard):
    1. Settings → OpenAI provider → models field: add `gpt-99-mega` (unregistered).
    2. Set llm_model to `gpt-99-mega` → Save.
    3. Send any short prompt.
    4. Assert backend logs show `model_capability_unknown` warning ONCE (poll backend logs OR check network response headers — or assert via a `/admin/log-tail` endpoint if available; otherwise this assertion lives in backend logging which the operator manually verifies — annotate test with manual-step note if not autodetectable).
    5. Assert run completes (inference fallback applies safe defaults).
    6. Assert NO UnknownProviderError (that's for unknown PROVIDER, not unknown MODEL — verify distinction).
    7. `assertLangSmithTraceExists(runId)`.

    **scenario-06-iteration-parity.spec.ts** (BUG-260523-04 MEASUREMENT, not fix):
    1. Define a parameterized test running the SAME prompt ("Build a simple analysis: load a CSV, compute means, plot a bar chart") against 4 providers in sequence.
    2. For each provider (OpenAI gpt-5.x, Anthropic claude-4.x, Google gemini-3-pro, OpenRouter minimax-m2.7), capture the final iteration count (from `runs.metadata.iteration_count` or by counting tool_preparing SSE events).
    3. Assert all 4 complete.
    4. Assert max(counts) / min(counts) <= 2 (within ~50% spread). If Anthropic blows this gate, the test FAILS — this is the BUG-260523-04 measurement. The fix is deferred per CONTEXT.md carry-forward; the failing test stays in the suite as a regression measurement.
    5. Document expected behavior in test comments: "Per CONTEXT.md routing, BUG-260523-04 root-cause fix is deferred. This test FAILS today; it stays as a measurement gate that the future focused-fix phase will close."

    Each scenario MUST be tagged `@075.4` (via test.describe title) so `npx playwright test --grep @075.4` selects them all.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --list 2>&1 | grep -c "@075.4"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 scenario files exist (`test -f` for each)
    - `wc -l frontend/tests/e2e/scenario-01-parallel-composers.spec.ts` >= 40
    - `wc -l frontend/tests/e2e/scenario-02-gemini-thought-signature.spec.ts` >= 40
    - `wc -l frontend/tests/e2e/scenario-03-openrouter-final-pptx.spec.ts` >= 40
    - `wc -l frontend/tests/e2e/scenario-04-done-latency.spec.ts` >= 30
    - `wc -l frontend/tests/e2e/scenario-05-unknown-model-inference.spec.ts` >= 40
    - `wc -l frontend/tests/e2e/scenario-06-iteration-parity.spec.ts` >= 50
    - `cd frontend && npx playwright test --list 2>&1 | grep -c "@075.4"` returns 6 (one per scenario file when each has a single test)
    - Each scenario imports `assertLangSmithTraceExists`: `grep -l "assertLangSmithTraceExists" frontend/tests/e2e/scenario-*.spec.ts | wc -l` returns 6
    - Each scenario uses `teardownTestUserData`: `grep -l "teardownTestUserData" frontend/tests/e2e/scenario-*.spec.ts | wc -l` returns 6
    - Each scenario asserts no orphaned streaming runs: `grep -l "assertNoOrphanedStreamingRuns" frontend/tests/e2e/scenario-*.spec.ts | wc -l` returns 6
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "tests/e2e/scenario-"` returns 0
  </acceptance_criteria>
  <done>6 Playwright scenarios authored mapping 1:1 to regression classes; each asserts LangSmith trace + DB-state teardown; scenario-06 stays failing as BUG-260523-04 measurement gate.</done>
</task>

<task type="auto" wave="3">
  <name>Task 7 (Wave 3): 075.4-TEST-TRIAGE.md — categorize all 95 pre-existing backend test failures</name>
  <files>.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md</files>
  <read_first>
    - .planning/phases/075.4-.../075.4-RESEARCH.md §Plan 05 Current pytest baseline (95 failures, top files listed)
    - backend/pytest.ini (config)
    - backend/tests/conftest.py (FK-aware fixture from Task 4 — available for "FIX with fixture" recommendations)
    - Live failure output: run `cd backend && venv/Scripts/python -m pytest tests/ --no-header -q 2>&1 | grep -E "^FAILED" | sort > /tmp/failures.txt && wc -l /tmp/failures.txt`
  </read_first>
  <action>
    Create `075.4-TEST-TRIAGE.md` (≥120 lines). Structure:

    ```markdown
    # Phase 075.4 — Backend Test Triage (95 failures on v2.5-dev HEAD)

    **Baseline:** 95 failed / 625 passed / 5 skipped / 3 xfailed of 728 collected (verified 2026-05-23 per RESEARCH §Plan 05).
    **Audit cited 44.** RESEARCH measured 95. Triage budget revised from 3-4h to 6-8h.

    ## Bucket Strategy (D-075.4-G3)

    | Bucket | Approx count | Strategy |
    |--------|-------------|---------|
    | (a) Tests referencing deleted/moved code | ~5 | DELETE the test (or @pytest.mark.skip with deferred-items.md ref) |
    | (b) Real regressions — test correct, code wrong | ~5-10 | FIX code (out-of-scope unless trivial; document in Plan 05 deferral list) |
    | (c) Mock drift / sync→async / fixture drift | ~80 | UPDATE tests (use FK-aware fixture from conftest.py where applicable) |

    ## Per-File Triage (sums to 95)

    | File | Fail count | Bucket | Action | Owner |
    |------|-----------|--------|--------|-------|
    | tests/unit/test_retrieval_service.py | 15 | (c) | RuntimeWarning: coroutine 'search_documents' was never awaited — bulk async-mock update | Plan 05 Task 8 OR deferred |
    | tests/unit/test_sql_service.py | 12 | (c) | DID NOT RAISE ValueError — sync→async migration; bulk update | deferred-to-076 |
    | tests/integration/test_threads_skills.py | 11 | (c) | Need full inspection; likely real-Supabase / fixture drift | deferred |
    | tests/unit/test_explorer_agent.py | 6 | (c) | mock drift | deferred |
    | tests/unit/test_multimodal_query.py | 5 | (c) | TypeError / AssertionError — schema or signature drift | deferred |
    | tests/test_provider_router.py | 4 | (c) | FK violations — USE `fk_aware_runs_factory` from Plan 05 Task 4 | Plan 05 Task 8 |
    | tests/integration/test_075_code_stdout_progressive.py | 4 | (c) | Phase 075 fixture drift | deferred-to-076 |
    | tests/integration/test_066_per_call_timer.py | 4 | (c) | Per-call-timer infra changes | deferred |
    | tests/integration/test_threads.py | 3 | (c) | mock or real-Supabase drift | deferred |
    | tests/unit/test_extraction_service.py | 2 | (c) | Phase 071.3 demoted Docling; legacy goldens stale | deferred-to-076 |
    | tests/integration/test_066_langsmith_clean.py | 2 | (c) | FK violations — same fix as provider_router | Plan 05 Task 8 |
    | <... remaining ~27 across smaller failing files ...> | <fill via live pytest run output> | <bucket> | <action> | <owner> |
    | **Total** | **95** | | | |

    ## Plan 05 In-Scope Fixes (Wave 3 Task 8)

    Within Plan 05's 6-8h budget, fix the FK-violation cluster ONLY:
    - 4 fails in test_provider_router.py
    - 2 fails in test_066_langsmith_clean.py
    - (potentially others surfaced as same FK pattern when greppable)

    **Expected delta: 6 of 95 closed → 89 remaining.** Plan 05 ships at 89 failures (or whatever the actual post-fix count is). The remaining 89 are documented here with explicit deferral.

    ## Deferred to Future Phases

    Pattern-grouped bulk updates that exceed Plan 05's budget:
    - 15 retrieval_service async-mock drift → defer to Phase 076 cleanup task
    - 12 sql_service sync→async migration → defer to Phase 076
    - 11 threads_skills fixture drift → defer to Phase 076 OR closer to v2.7 Skill Studio
    - 6 explorer_agent mock drift → defer (low priority; unblock at next touch)
    - 5 multimodal_query drift → defer to Phase 076 (Confidence Recalibration is the natural touch point for multimodal)
    - 4 test_075_code_stdout_progressive → defer to Phase 076 (sandbox infra touch)
    - 4 test_066_per_call_timer → defer to Phase 077 (multi-worker validation harness)

    ## Bucket (a) — Delete Candidates

    Run `cd backend && pytest tests/ --co 2>&1 | grep "ImportError"` to find tests with stale imports. Likely 0-5 files. Document them here when found.

    ## Bucket (b) — Real Regressions

    If ANY of the 95 fails turn out to be Bucket (b) (test correct, code wrong), document them here with the specific regression. These MAY be in-scope if they're trivially fixable; otherwise file as bug reports and defer.

    ## Sign-off

    - [ ] All 95 failures categorized (bucket + action + owner)
    - [ ] Categories sum to 95
    - [ ] In-scope fixes shipped in Task 8 (FK-violation cluster)
    - [ ] Deferred items rolled forward to specific phases (076 / 077 / etc.)
    ```

    To populate the per-file table fully: run `cd backend && venv/Scripts/python -m pytest tests/ -q --no-header 2>&1 | grep -E "^FAILED" | awk -F'::' '{print $1}' | sort | uniq -c | sort -rn`. This produces "<count> <file>" lines. Paste each into the table. Sum should equal 95.
  </action>
  <verify>
    <automated>test -f .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md && wc -l .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md`
    - `wc -l .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 120
    - `grep -c "95" .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 3 (baseline, total, sum check)
    - `grep -cE "Bucket \(a\)|Bucket \(b\)|Bucket \(c\)" .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 3
    - `grep -c "fk_aware_runs_factory" .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 1 (refs Plan 05 Task 4 fixture)
    - The per-file table sums to 95: extract counts in the table column and `awk` to sum (operator-verifiable)
  </acceptance_criteria>
  <done>95-failure triage artifact landed; each file mapped to bucket+action+owner; counts sum to 95; in-scope fixes (FK cluster) identified; deferrals rolled forward to specific phases.</done>
</task>

<task type="auto" wave="3">
  <name>Task 8 (Wave 3): Fix the FK-violation cluster (~6 failures using fk_aware_runs_factory)</name>
  <files>backend/tests/test_provider_router.py, backend/tests/integration/test_066_langsmith_clean.py</files>
  <read_first>
    - backend/tests/test_provider_router.py (find the 4 failing tests; identify which tests INSERT into runs without parent threads row)
    - backend/tests/integration/test_066_langsmith_clean.py (2 failing tests, same pattern)
    - backend/tests/conftest.py (Task 4 fixture `fk_aware_runs_factory`)
  </read_first>
  <action>
    Run `cd backend && venv/Scripts/python -m pytest tests/test_provider_router.py -v 2>&1 | grep "FAILED"` to identify the 4 failing tests by name.

    For each failing test:
    1. Read the test body. Identify where `runs` table INSERT happens.
    2. Inject the `fk_aware_runs_factory` fixture parameter to the test function.
    3. Replace direct `_supabase.table("runs").insert(...)` calls with `await fk_aware_runs_factory(user_id=..., **other_fields)`.
    4. Re-run the test; if green, move to next.
    5. If still failing for a non-FK reason, ROLL BACK the change and document the test in 075.4-TEST-TRIAGE.md as Bucket (b) (real regression — beyond this task's scope).

    Repeat for the 2 failing tests in `test_066_langsmith_clean.py`.

    Cap this task at the 6 tests. If MORE FK-violation tests surface during pytest output review (e.g., other files with `runs_thread_id_fkey` errors), document them in the triage artifact and defer.

    Update 075.4-TEST-TRIAGE.md after this task — flip the affected rows from `Plan 05 Task 8` → `Fixed in Plan 05 Task 8 — green`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_provider_router.py tests/integration/test_066_langsmith_clean.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/test_provider_router.py -q 2>&1 | grep -cE "passed|failed"` shows passes increased (delta ≥ 3, since 4 were failing — accept 3-4 fix as success; 1 may remain as bucket-b)
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_066_langsmith_clean.py -q 2>&1 | grep -cE "passed|failed"` shows the 2 fails closed (or documented as bucket-b in triage if not FK-driven)
    - `grep -c "fk_aware_runs_factory" backend/tests/test_provider_router.py` returns >= 1
    - `grep -c "fk_aware_runs_factory" backend/tests/integration/test_066_langsmith_clean.py` returns >= 1
    - 075.4-TEST-TRIAGE.md updated: `grep -cE "Fixed in Plan 05 Task 8|→ green" .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 2 (provider_router + 066_langsmith_clean rows updated)
  </acceptance_criteria>
  <done>FK-violation cluster fixes shipped (~6 failures closed); fk_aware_runs_factory consumed by both files; triage artifact updated with green flips.</done>
</task>

<task type="checkpoint:human-verify" wave="3" gate="blocking">
  <name>Task 9 (Wave 3): Run Playwright E2E + UAT scoreboard sweep + verify integration with real provider keys</name>
  <files>(verification-only — no code changes)</files>
  <read_first>
    - All 6 scenario files from Task 6
    - 075.4-TEST-TRIAGE.md (Task 7) — confirm green flips from Task 8
    - VALIDATION.md §Manual-Only Verifications (operator-led 4-axis sweep)
  </read_first>
  <what-built>
    Operator runs the full E2E + UAT sweep:
    1. `cd frontend && npx playwright test --grep @075.4` against the live dev stack (backend + frontend on localhost with real provider keys configured in `backend/.env`). Capture per-scenario pass/fail.
    2. Manually execute the 4-axis UAT scoreboard (cross-provider × multi-tool × parallel-thread × long-message) for each touched plan's UAT row — record results in each plan's `<verification>` UAT scoreboard table.
    3. `cd backend && venv/Scripts/python -m pytest tests/ -q --no-header 2>&1 | tail -3` — confirm post-Task-8 failure count is 95 - (6 fixed) = 89 or less.
    4. CI dry-run: push a no-op commit on a test branch to verify `.github/workflows/frontend-tests.yml` runs and GitHub Actions auto-redacts `secrets.LANGSMITH_API_KEY`.

    Scenario-06 (BUG-260523-04 measurement) is EXPECTED to fail today — that's the regression-detection gate. Other 5 scenarios must pass.
  </what-built>
  <how-to-verify>
    1. Set env vars in `backend/.env`: real `LANGSMITH_API_KEY`, real provider keys (OPENAI_API_KEY, ANTHROPIC_API_KEY, GOOGLE_API_KEY, OPENROUTER_API_KEY).
    2. Boot stack: `bash scripts/restart-backend.sh` (or PowerShell equivalent) + `cd frontend && npm run dev`. Verify `curl http://localhost:8000/health` returns 200.
    3. Run Playwright: `cd frontend && npx playwright test --grep @075.4 --reporter=list`. Expect scenarios 1-5 GREEN, scenario-6 RED with iteration-spread > 2 (BUG-260523-04 measurement).
    4. For each plan (01-04), fill in the UAT scoreboard's "Actual" + "Pass" columns by manually running the rows against the live dev stack with all 4 providers configured.
    5. Confirm post-fix pytest count: `cd backend && venv/Scripts/python -m pytest tests/ -q --no-header 2>&1 | tail -3` shows ≤ 89 failures.
    6. (Optional) Push a branch + open a draft PR to verify CI fires and `.github/workflows/frontend-tests.yml` runs. Inspect log to confirm `LANGSMITH_API_KEY` value is redacted as `***`.
    7. Document final results in `075.4-05-SUMMARY.md` (created at end-of-plan): scenarios 1-5 pass count + scenario 6 measurement value + pytest count delta + CI dry-run status.
  </how-to-verify>
  <action>
    See <what-built> above. Operator-driven full E2E sweep + UAT scoreboard fill + pytest re-count + optional CI dry-run.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --grep @075.4 --reporter=list 2>&1 | tail -15</automated>
  </verify>
  <done>5+ Playwright scenarios pass (1-5); scenario-6 documented as expected-fail (BUG-260523-04 measurement gate); post-fix pytest count ≤ 89; UAT scoreboards filled in Plans 01-04; SUMMARY.md authored.</done>
  <acceptance_criteria>
    - `cd frontend && npx playwright test --grep @075.4 2>&1 | tail -10` shows 5+ passes (scenarios 1-5), 1 expected fail (scenario 6 measurement)
    - `cd backend && venv/Scripts/python -m pytest tests/ -q --no-header 2>&1 | tail -3` shows failures count ≤ 89 (95 - 6 FK fixes) — confirms Task 8 effective
    - UAT scoreboards in 075.4-01-PLAN.md, 075.4-02-PLAN.md, 075.4-03-PLAN.md, 075.4-04-PLAN.md `<verification>` blocks have Actual + Pass columns filled (operator-recorded — verified by reading each PLAN's UAT block has non-empty cells)
    - 075.4-05-SUMMARY.md (Task 9 output) exists and documents: scenario pass/fail count + iteration-spread measurement + post-fix pytest count + CI dry-run status
    - GitHub Actions log (if CI dry-run executed) shows `secrets.LANGSMITH_API_KEY` redacted as `***` in step output (verify by inspection — no actual key string visible)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the SUMMARY.md path + scenario pass count + post-fix pytest count, OR describe blocking issues (e.g., "scenario-02 fails on Gemini-3 — needs Plan 02 fix re-verify" → orchestrator routes back).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test user credentials | `fhdmrd@gmail.com / 123456` is the documented dev login; reused for local Playwright. |
| LangSmith API key in CI | GitHub Actions secret; flows through workflow step env. |
| DB teardown (TRUNCATE / DELETE) | If pointed at prod, catastrophic data loss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075.4-07 | I (Information disclosure) — test-user data leak in CI logs | Playwright fixtures + scenario specs | mitigate | Creds loaded from `process.env.E2E_USER_EMAIL` / `E2E_USER_PASSWORD` (NOT committed). CI uses GitHub Actions secrets (auto-redacted). Local dev: defaults to known fhdmrd@gmail.com / 123456 (already in repo memory; acceptable for local-only fixture per D-075.4-G1). Dedicated test user deferred to v3.0 multi-tenancy work. |
| T-075.4-08 | I (Information disclosure) — LangSmith API key exposure in CI logs | `.github/workflows/frontend-tests.yml` | mitigate | Workflow uses `${{ secrets.LANGSMITH_API_KEY }}` reference; GitHub Actions auto-redacts secret values in step output. Documented in workflow comment + explicit redaction-verification step. Local dev: key lives only in `backend/.env` (gitignored). |
| T-075.4-EXTRA | T (Tampering) — accidental prod data wipe via teardown | `db-teardown.fixture.ts` | mitigate | Hard-gate at fixture entry: throw if `SUPABASE_URL` does NOT contain `'localhost'` or `'127.0.0.1'`. Prevents accidental TRUNCATE against prod. Confirmed by Security Threat Surface Plan 05 row. |
</threat_model>

<verification>
## Plan-Level Checks
1. `grep -c "## UAT scoreboard recipe (MANDATORY)" CLAUDE.md` returns 1 (Wave 0 SC#10 landed).
2. `cd frontend && npx playwright test --list 2>&1 | grep -c "@075.4"` returns 6 (6 scenarios discoverable).
3. `test -f .github/workflows/frontend-tests.yml` AND `grep -c "secrets.LANGSMITH_API_KEY" .github/workflows/frontend-tests.yml` returns >= 1.
4. `test -f scripts/restart-backend.sh` AND `test -f scripts/restart-backend.ps1`.
5. `curl -fsS http://localhost:8000/health` returns 200 with `{"status": "ok"}` (when backend running).
6. `wc -l .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-TEST-TRIAGE.md` returns >= 120; per-file counts sum to 95.
7. 5 folded bug reports have `folded_into: "075.4"` AND `status: folded`; BUG-260523-04 has `status: deferred`; BUG-260521-02 has `re_open_trigger:` field added.
8. Post-Task-8 pytest failures count ≤ 89 (was 95).

## UAT Scoreboard — Plan 05 is the substrate that the OTHER plans' UAT scoreboards run on. Plan 05's own UAT is the operator-led 4-axis sweep across all touched plans + the CI dry-run.
</verification>

<success_criteria>
- CLAUDE.md ## UAT scoreboard recipe (MANDATORY) section landed (Wave 0; SC#10)
- Playwright config + 3 fixtures + 6 scenario files; scenarios 1-5 pass against live dev stack; scenario-6 stays failing as BUG-260523-04 measurement gate
- LangSmith trace assertion fixture (exponential backoff poller) + per-scenario assertions
- DB-state teardown with localhost hard-gate; no orphaned `runs.status='streaming'` post-test
- restart-backend.{ps1,sh} scripts ship with FORWARD-REF #2 multi-worker update path header
- /health endpoint returns 200 with `{"status": "ok"}`
- backend/tests/conftest.py fk_aware_runs_factory landed; consumed by Task 8 to close ~6 FK-violation failures
- .github/workflows/frontend-tests.yml CI workflow runs vitest + Playwright on PR; uses secrets.LANGSMITH_API_KEY
- 075.4-TEST-TRIAGE.md categorizes all 95 pre-existing backend test failures into a/b/c buckets summing to 95
- Plan 05 Task 8 ships ~6 in-scope FK-violation fixes; remaining ~89 deferred to specific future phases per triage
- 7 bug-report frontmatter updates applied: 5 folded → 075.4, 1 deferred (BUG-260523-04), 1 auto-close noted (BUG-260521-02)
</success_criteria>

<forward_references>
- → Phase 077/079 (multi-worker enable): `restart-backend.ps1` + `restart-backend.sh` headers cite FORWARD-REF #2/#3 — update for `--workers N` when Phase 079 lands. Document update path inline.
- → Phase 082 (cross-cutting verification): Plan 05's Playwright infrastructure is the test substrate 082 rides on. The 6 scenarios + langsmith.fixture + db-teardown.fixture inherited.
- → Phase 076 / 077 (and others per triage): the deferred ~89 backend test failures are rolled forward to specific phases per 075.4-TEST-TRIAGE.md `Deferred to Future Phases` section.
- → Post-075.4 focused phase: BUG-260523-04 root-cause fix (E2E #6 measures it; not closed in 075.4 per CONTEXT.md carry-forward).
</forward_references>

<output>
After completion, create `.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-05-SUMMARY.md`
</output>
