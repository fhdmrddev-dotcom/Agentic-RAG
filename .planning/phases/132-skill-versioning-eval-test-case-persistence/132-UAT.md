---
phase: 132
slug: skill-versioning-eval-test-case-persistence
type: uat
status: passed
date: 2026-06-30
requirements: [VER-01, EVAL-01]
sc10_applicable: false
sc10_reason: "Phase does not touch streaming, agent loop, provider routing, or chat UI state — no cross-provider × multi-tool × parallel-thread × long-message matrix required (per 132-VALIDATION.md)."
---

# Phase 132 — UAT (Skill Versioning + Eval Test-Case Persistence)

Conversational UAT validating the user-facing behaviors of VER-01 + EVAL-01.
All scenarios were exercised **live this session (2026-06-30)** by the operator
(browser click-through, G-4 lived-experience gate) AND independently re-proven by
the orchestrator against the running system (live HTTP API + live DB trigger,
rolled back — no dev data mutated). App: backend `:8000`, frontend `:5173`,
local Supabase `:54322`.

## Result: PASSED (6/6)

| # | Scenario (user's perspective) | Requirement | Result | Evidence |
|---|---|---|---|---|
| 1 | "Eval test cases" section appears in a saved skill's detail panel | EVAL-01 | ✅ PASS | Operator confirmed in UI |
| 2 | Add a test case (prompt + expected behavior) → persists across page reload | EVAL-01 | ✅ PASS | Operator reload check + live API `POST /skills/{id}/test-cases` → `201`, re-list shows it |
| 3 | Edit a test case persists; delete removes it (across reload) | EVAL-01 | ✅ PASS | Operator + live API `PATCH /test-cases/{id}` → `200`, `DELETE` → `204`, list back to 0 |
| 4 | Version history list is visible (shows `v1 · backfill`) | VER-01 | ✅ PASS | Operator + live `GET /skills/{id}/versions` → `200` returns `v1·backfill` |
| 5 | Editing skill instructions adds a NEW version; toggling enabled/global adds NONE | VER-01 / D-02 | ✅ PASS | Operator + live DB trigger: instructions change → `v2·manual` (n+1); is_enabled/is_global toggles → no new row (rolled back) |
| 6 | Save gives a clear success signal ("Saved ✓") | EVAL-01 (G-4 polish) | ✅ PASS | Post-UAT fix `b529af46`; operator confirmed "good" |

## Notes
- **Append-only immutability** independently confirmed: attempting to UPDATE an existing
  version row is blocked with SQLSTATE `23514` (live DB).
- **Owner-scoping** enforced server-side (`.eq("user_id", current_user["id"])` on all 5
  routes) — token used was the test user's own; cross-user isolation covered by
  `test_owner_scope_isolation` (Plan 02).
- The **"Weak trigger description"** hint the operator observed is the pre-existing
  Phase 123 skill-triggering lint feature — unrelated to Phase 132, no action.
- Automated coverage backing these scenarios: 11/11 integration tests pass
  (`test_132_skill_versions.py` 6/6 + `test_132_test_cases.py` 5/5).

## Disposition
No gaps found. No fix plans required. Phase 132 user-facing behavior validated end-to-end.
Goal-backward verification also passed 4/4 (`132-VERIFICATION.md`).
