---
phase: 168-sso-saml-2-0-core
plan: 05
type: execute
wave: 4
depends_on: ["168-04"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useOrgPermissionsProbe.ts
  - frontend/src/providers/OrgProvider.tsx
  - frontend/src/hooks/useAuth.ts
autonomous: true
requirements: [SSO-01]

must_haves:
  truths:
    - "api.ts exposes the SSO client fns (getSsoRoute, listSsoConfigs, createSsoProvider, updateSsoProvider, deleteSsoProvider, provisionSso) + OrgPermissions.can_manage_sso"
    - "canManageSso is fail-CLOSED render-only across the probe + OrgProvider (a blip never flashes the SSO tab to a non-manager, T-166-07 lineage)"
    - "signInWithSSO({ domain }) redirects the browser to data.url MANUALLY — it does not auto-navigate (D-168-02)"
    - "signInWithPassword is UNTOUCHED — the password fallback path is preserved (SC#3)"
    - "On SIGNED_IN for an SSO session, the app calls provisionSso() then re-probes /org/me so the join-additive org resolves (D-168-04)"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "SSO client fns + OrgPermissions.can_manage_sso + SsoConfig type"
      contains: "can_manage_sso"
    - path: "frontend/src/hooks/useOrgPermissionsProbe.ts"
      provides: "canManageSso fail-closed field"
      contains: "canManageSso"
    - path: "frontend/src/providers/OrgProvider.tsx"
      provides: "canManageSso in OrgValue + SSO-session JIT provision-then-reprobe wiring"
      contains: "canManageSso"
    - path: "frontend/src/hooks/useAuth.ts"
      provides: "signInWithSSO"
      contains: "signInWithSSO"
  key_links:
    - from: "frontend/src/providers/OrgProvider.tsx"
      to: "frontend/src/lib/api.ts::provisionSso"
      via: "on SIGNED_IN SSO session → provisionSso() → re-probe /org/me"
      pattern: "provisionSso"
    - from: "frontend/src/hooks/useOrgPermissionsProbe.ts"
      to: "GET /org/me can_manage_sso"
      via: "getOrgPermissions() payload → canManageSso"
      pattern: "can_manage_sso"
---

<objective>
Build the frontend data + auth layer the SSO surfaces (Plan 06) render on: the api-client SSO functions + the `can_manage_sso` render flag threaded through the probe and `OrgProvider` in lockstep with the existing `canManage`/`canAuditView`, the `signInWithSSO` hook method, and the SIGNED_IN → JIT-provision → re-probe wiring so a first-time SSO user lands in the org switcher.

Purpose: Keep the presentational plan (06) pure by landing all contracts + async plumbing here. The `can_manage_sso` flag is fail-closed render-only (the server `require_sso_manage` is the wall); the JIT provision fires silently on the SSO callback.
Output: `api.ts` SSO fns + type, `useOrgPermissionsProbe` + `OrgProvider` `canManageSso`, `useAuth.signInWithSSO`, and the provision-on-SIGNED_IN effect.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/168-sso-saml-2-0-core/168-RESEARCH.md
@.planning/phases/168-sso-saml-2-0-core/168-PATTERNS.md
@.planning/phases/168-sso-saml-2-0-core/168-CONTEXT.md
@.planning/phases/168-sso-saml-2-0-core/168-UI-SPEC.md

<interfaces>
<!-- The exact analogs to extend (getOrgMembers / OrgPermissions / useOrgPermissionsProbe / OrgProvider / useAuth). -->

From frontend/src/lib/api.ts:4599-4702 (OrgPermissions + getOrgPermissions — add can_manage_sso in lockstep):
  interface OrgPermissions { org_id: string|null; role: string; can_manage: boolean; can_audit_view: boolean; memberships: OrgMembership[] }
  getOrgPermissions(): Promise<OrgPermissions>   // defensive `?? fallback` unwrap — add can_manage: body.can_manage_sso ?? false pattern

From frontend/src/lib/api.ts:4707+ (getOrgMembers — the client-fn shape to clone: getAuthHeaders() auto-injects X-Org-Id, ApiError on non-OK, defensive envelope):
  getAuthHeaders()  // auto-injects X-Org-Id; getSsoRoute is PRE-auth (login page) so it may not have X-Org-Id — do not require it

From frontend/src/hooks/useOrgPermissionsProbe.ts (the CLOSED fail-closed default + the return map — add can_manage_sso:false + canManageSso):
  const CLOSED: OrgPermissions = { org_id:null, role:"member", can_manage:false, can_audit_view:false, memberships:[] }
  returns { orgId, canManage, canAuditView, role, memberships, loading }

From frontend/src/providers/OrgProvider.tsx (OrgValue interface 41-58 + probe destructure 99-100 + useMemo 124-126 — add canManageSso at all three):
  interface OrgValue { ...; canManage: boolean; canAuditView: boolean; ... }

From frontend/src/hooks/useAuth.ts:37-61 (onAuthStateChange SIGNED_IN + signIn/signUp — add signInWithSSO sibling; keep signInWithPassword untouched):
  supabase.auth.onAuthStateChange((_event, sess) => {...})   // SIGNED_IN fires after the SSO callback
  signIn = async (email,password) => { await supabase.auth.signInWithPassword({email,password}) }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts SSO client fns + OrgPermissions.can_manage_sso</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (lines 4580-4702 the org client block + OrgPermissions + getOrgPermissions; lines 4707-4760 getOrgMembers/getOrgAudit — the fetch-fn shape: getAuthHeaders, ApiError, defensive `?? fallback` unwrap)
    - .planning/phases/168-sso-saml-2-0-core/168-PATTERNS.md (api.ts section lines 282-296)
    - .planning/phases/168-sso-saml-2-0-core/168-UI-SPEC.md (the SSO tab data needs: metadata URL + email domain in; connection row = domain + provider_id + status)
  </read_first>
  <action>
    Extend `OrgPermissions` with `can_manage_sso: boolean` and update `getOrgPermissions` to unwrap `can_manage_sso: body.can_manage_sso ?? false` (defensive, matching the existing `?? false` pattern). Add an `SsoConfig` type `{ id: string; email_domain: string; provider_id: string | null; status: "pending_approval"|"active"|"disabled"; approved_at: string | null }`. Add the SSO client fns, each mirroring `getOrgMembers` (getAuthHeaders auto-injects X-Org-Id, ApiError on non-OK, defensive unwrap):
    - `getSsoRoute(domain: string): Promise<{ sso: boolean }>` — GET `/org/sso/route?domain=` ; called PRE-auth from the login page, so build headers WITHOUT requiring X-Org-Id (a bare fetch is fine — the endpoint is membership-free); defensive `{ sso: body.sso ?? false }`.
    - `listSsoConfigs(): Promise<SsoConfig[]>` — GET `/org/sso/providers`.
    - `createSsoProvider(metadataUrl: string, emailDomain: string): Promise<SsoConfig>` — POST `/org/sso/providers`.
    - `updateSsoProvider(id, {...}): Promise<SsoConfig>` — PUT `/org/sso/providers/{id}`.
    - `deleteSsoProvider(id: string): Promise<void>` — DELETE `/org/sso/providers/{id}`.
    - `provisionSso(): Promise<{ org_id: string|null; role: string|null; joined: boolean }>` — POST `/org/sso/provision`.
    Surface the four create-error messages from the UI-SPEC copywriting contract by letting `ApiError` carry the server `detail` (the SsoTab renders the message). NEVER return or store the management token / any secret.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc -b --noEmit && grep -q "can_manage_sso" src/lib/api.ts && grep -q "getSsoRoute" src/lib/api.ts && grep -q "provisionSso" src/lib/api.ts && echo API_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` (frontend) exits 0 with the new fns + type.
    - `OrgPermissions` has `can_manage_sso: boolean` and `getOrgPermissions` unwraps it defensively.
    - `getSsoRoute` does NOT require an X-Org-Id header (callable pre-auth from the login page).
    - All six SSO fns exist and use `getAuthHeaders`/`ApiError` per the `getOrgMembers` shape (except `getSsoRoute`'s pre-auth exception).
  </acceptance_criteria>
  <done>The api-client SSO surface + can_manage_sso contract exist and typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: canManageSso through probe + OrgProvider, signInWithSSO, and the SSO-session JIT provision-then-reprobe</name>
  <files>frontend/src/hooks/useOrgPermissionsProbe.ts, frontend/src/providers/OrgProvider.tsx, frontend/src/hooks/useAuth.ts</files>
  <read_first>
    - frontend/src/hooks/useOrgPermissionsProbe.ts (the CLOSED default lines 22-31 + the interface line 9-13 + the return map 100-107 — add can_manage_sso/canManageSso at each in lockstep; the fail-closed polarity is load-bearing)
    - frontend/src/providers/OrgProvider.tsx (OrgValue 41-58, the probe destructure 98-100, the useMemo value+deps 124-126, the SECURITY NOTE 20-23; the WR-01 self-heal effect 109-113 — the re-probe trigger to model the SSO provision on)
    - frontend/src/hooks/useAuth.ts (onAuthStateChange 37-41 SIGNED_IN, signIn 53-56, the rehydrate bind idiom)
    - .planning/phases/168-sso-saml-2-0-core/168-RESEARCH.md (signInWithSSO behavior lines 219-230; the JIT fires on SIGNED_IN lines 274; A1 callback note)
    - .planning/phases/168-sso-saml-2-0-core/168-PATTERNS.md (useAuth section lines 194-211; OrgProvider section 266-273; probe section 276-278)
  </read_first>
  <action>
    (a) `useOrgPermissionsProbe.ts`: add `canManageSso: boolean` to the `UseOrgPermissionsProbe` interface, add `can_manage_sso: false` to the `CLOSED` fail-closed default, and add `canManageSso: perms.can_manage_sso` to the return map. Fail-closed polarity is load-bearing — a probe blip must never flash the SSO tab to a non-manager.
    (b) `OrgProvider.tsx`: add `canManageSso: boolean` to the `OrgValue` interface, pull `canManageSso` from the probe destructure, and add it to the `useMemo` value + deps array (in lockstep with `canManage`/`canAuditView`). The SECURITY NOTE applies verbatim — render-only; `require_sso_manage` is the wall.
    (c) `OrgProvider.tsx` JIT wiring: add an effect that, on an SSO session (detect via the session's provider/amr indicating an `sso:` provider — read from the `session`/user the provider already has access to, or accept the user id + a passed session flag), calls `provisionSso()` exactly once then triggers a `/org/me` re-probe (mirror the WR-01 self-heal that adopts the resolved org; the provision must precede org resolution because `/org/me` returns empty memberships until JIT runs). Guard it so it fires at most once per SSO sign-in and never for a plain password session (no-op safety: `provisionSso` returns joined=false for a non-SSO caller anyway). Keep it idempotent (safe to call on every SIGNED_IN).
    (d) `useAuth.ts`: add a `signInWithSSO = async (domain: string) => { const { data, error } = await supabase.auth.signInWithSSO({ domain }); if (error) throw error; if (data?.url) window.location.href = data.url }` sibling of `signIn`, and expose it on the `UseAuth` interface + return. It MUST redirect manually (`window.location.href = data.url`) — supabase-js does not auto-navigate. Leave `signInWithPassword`/`signUp` UNTOUCHED (the password fallback, SC#3).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc -b --noEmit && grep -q "canManageSso" src/hooks/useOrgPermissionsProbe.ts && grep -q "canManageSso" src/providers/OrgProvider.tsx && grep -q "signInWithSSO" src/hooks/useAuth.ts && grep -q "provisionSso" src/providers/OrgProvider.tsx && echo FLAG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` (frontend) exits 0.
    - `canManageSso` appears in the probe interface, the CLOSED default has `can_manage_sso: false`, and the probe return map + OrgProvider OrgValue/useMemo all carry it (lockstep with canManage).
    - `useAuth.signInWithSSO` calls `supabase.auth.signInWithSSO({ domain })` and does `window.location.href = data.url` (manual redirect).
    - `signInWithPassword` and `signUp` are byte-unchanged (the password fallback is preserved) — `git diff` shows no edits to those two methods' bodies.
    - The SSO-session provision effect calls `provisionSso()` at most once per sign-in and re-probes `/org/me` after.
  </acceptance_criteria>
  <done>canManageSso is a fail-closed render flag end to end; signInWithSSO redirects manually; the SSO callback silently provisions membership then resolves the org.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser render flag → SSO tab visibility | canManageSso decides RENDERING ONLY; the server require_sso_manage is the authority |
| browser → SSO redirect | signInWithSSO hands a GoTrue-issued URL; the browser only follows it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-06 | Info Disclosure/Tampering | canManageSso render flag | mitigate | fail-CLOSED default (`can_manage_sso: false`); render-only (the SECURITY NOTE) — a forged flag reaches no data; require_sso_manage + mig-104 RLS is the wall |
| T-168-07 | Denial of Service | login path | accept | `signInWithPassword` untouched — the password fallback is retained; nobody is locked out (SC#3) |
| T-168-03 | Elevation | JIT provision from client | mitigate | `provisionSso()` sends NO role/org — the server resolves org from the authenticated provider and hardcodes member (Plans 03/04) |
</threat_model>

<verification>
- `npx tsc -b` (frontend) is the type gate for both tasks.
- grep gates confirm the flag + fns are wired; the byte-unchanged password path is confirmed via git diff.
</verification>

<success_criteria>
- The api-client SSO surface + can_manage_sso contract exist and typecheck.
- canManageSso is fail-closed render-only across the probe + provider.
- signInWithSSO redirects manually; the password path is untouched.
- The SSO callback silently provisions membership and re-probes /org/me.
</success_criteria>

<output>
Create `.planning/phases/168-sso-saml-2-0-core/168-05-SUMMARY.md` when done.
</output>
