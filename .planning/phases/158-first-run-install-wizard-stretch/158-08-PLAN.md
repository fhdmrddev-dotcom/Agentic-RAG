---
phase: 158-first-run-install-wizard-stretch
plan: 08
type: execute
wave: 4
depends_on: ["158-01", "158-06"]
files_modified:
  - frontend/src/lib/supabase.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/setupApi.ts
  - frontend/src/lib/__tests__/supabase.test.ts
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "D-07: `frontend/src/lib/supabase.ts` never throws at import on a placeholder `VITE_SUPABASE_URL` (defensive init), and `hydrateSupabaseFromRuntime(apiBase)` overlays runtime creds from `GET /public-config`, keeping baked `VITE_*` as fallback — login works without a frontend rebuild"
    - "D-15: `frontend/src/lib/setupApi.ts` is a SEPARATE unauthenticated helper carrying `X-Setup-Token` — it NEVER routes through `getAuthHeaders` (which throws 'Not authenticated' pre-auth)"
    - "`getSetupStatus()` + `getPublicConfig()` are public unauth GETs beside `getMaintenanceStatus`, resolving safely on failure"
    - "The setup client mirrors the 158-06 endpoint contract exactly (status/detect/validate/schema-bootstrap/operator/provider-key/smoke/finalize)"
  artifacts:
    - path: "frontend/src/lib/supabase.ts"
      provides: "defensive createClient + hydrateSupabaseFromRuntime + reassignable export"
      contains: "hydrateSupabaseFromRuntime"
    - path: "frontend/src/lib/setupApi.ts"
      provides: "X-Setup-Token unauth fetch helpers for /setup/*"
      contains: "X-Setup-Token"
  key_links:
    - from: "frontend/src/lib/supabase.ts hydrateSupabaseFromRuntime"
      to: "GET /public-config"
      via: "runtime creds overlay baked VITE_*"
      pattern: "public-config"
    - from: "frontend/src/lib/setupApi.ts"
      to: "X-Setup-Token header (NOT getAuthHeaders)"
      via: "pre-auth unauthenticated writes"
      pattern: "X-Setup-Token"
---

<objective>
Build the frontend runtime-config shim and the setup API client: harden `supabase.ts` so a placeholder `VITE_*` can never white-screen the app (including `/setup`) and let a wizard-entered Supabase URL reach the browser at runtime via `GET /public-config`; add the public `getSetupStatus`/`getPublicConfig` GETs; and add `setupApi.ts` — the unauthenticated `X-Setup-Token` client for the `/setup/*` writes.

Purpose: D-07 (the SC#3 honesty hinge — "no hand-editing files" is only true for the browser tier with this shim), D-15 (pre-auth writes never hit `getAuthHeaders`). Consumes the 158-06 endpoint contract.
Output: `supabase.ts` (rewrite defensive) + `api.ts` (public GETs) + `setupApi.ts` (NEW). Flips `supabase.test.ts` green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-first-run-install-wizard-stretch/158-RESEARCH.md
@.planning/phases/158-first-run-install-wizard-stretch/158-PATTERNS.md
@.planning/phases/158-first-run-install-wizard-stretch/158-06-PLAN.md

<interfaces>
Current fragile init to harden — supabase.ts:1-6 (PATTERNS.md §lib/supabase.ts):
  export const supabase = createClient(import.meta.env.VITE_SUPABASE_URL, import.meta.env.VITE_SUPABASE_ANON_KEY)
  → Pitfall 4: a `<project-ref>` placeholder URL throws at import (new URL()) → white-screens the SPA including /setup.
  RESEARCH Pattern 8 gives the exact defensive rewrite: safeUrl swap + `export let supabase` + `hydrateSupabaseFromRuntime(apiBase)`.

Anti-analog to DIVERGE from — api.ts:75-83 getAuthHeaders (throws "Not authenticated" if no session) — setupApi MUST NOT use it.
Public unauth sibling — api.ts getMaintenanceStatus (resolves false on any failure) — model getSetupStatus/getPublicConfig on it. API_BASE = api.ts:13.

Endpoint contract (from 158-06): GET /setup/status, GET /public-config (open); POST /setup/{detect,validate,schema-bootstrap,operator,provider-key,smoke,finalize} (X-Setup-Token).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: supabase.ts defensive init + runtime hydrate (D-07)</name>
  <files>frontend/src/lib/supabase.ts, frontend/src/lib/__tests__/supabase.test.ts</files>
  <behavior>
    - `createClient` never throws at import: a placeholder `VITE_SUPABASE_URL` (`<...>` or empty) is swapped for a harmless `http://localhost:54321` default
    - `hydrateSupabaseFromRuntime(apiBase)` fetches `/public-config`; if it returns real creds AND (baked is placeholder OR runtime !== baked), it reassigns `supabase = createClient(runtimeUrl, runtimeKey)`; on any fetch error it keeps the baked client
    - a real baked URL identical to the runtime URL is NOT re-created
  </behavior>
  <read_first>
    - frontend/src/lib/supabase.ts (the whole 6-line file) + frontend/src/hooks/useAuth.ts:20-25 (calls `supabase.auth.getSession()` at mount — why the client MUST construct without throwing)
    - 158-RESEARCH.md Pattern 8 (the exact defensive rewrite) + Pitfall 4 ; frontend/src/lib/__tests__/supabase.test.ts (the 158-01 todos to realize)
  </read_first>
  <action>
    Rewrite `supabase.ts` per RESEARCH Pattern 8: read `BAKED_URL`/`BAKED_KEY` from `import.meta.env.VITE_*`; `isPlaceholder(v) = !v || (v.includes("<") && v.includes(">"))`; `const safeUrl = isPlaceholder(BAKED_URL) ? "http://localhost:54321" : BAKED_URL`; `export let supabase = createClient(safeUrl, BAKED_KEY || "placeholder-anon-key")` (note `let`, reassignable — NOT `const`). Add `export async function hydrateSupabaseFromRuntime(apiBase: string): Promise<void>` that fetches `${apiBase}/public-config`, and if `cfg.supabase_url && cfg.supabase_anon_key && (isPlaceholder(BAKED_URL) || cfg.supabase_url !== BAKED_URL)` reassigns `supabase = createClient(cfg.supabase_url, cfg.supabase_anon_key)`; wrap in try/catch that keeps the baked client (the `/setup` page never calls Supabase, so it renders regardless). Preserve every existing named export and behavior for the configured path.
    Realize the supabase.test.ts todos as real tests: placeholder URL → no throw at import; hydrate overlays runtime creds (mock fetch); identical baked URL → no re-create.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/__tests__/supabase.test.ts` → all pass (no todos remain)
    - `grep -c "export let supabase" frontend/src/lib/supabase.ts` == 1 (reassignable)
    - `grep -c "hydrateSupabaseFromRuntime" frontend/src/lib/supabase.ts` == 1
    - `cd frontend && npx tsc -b` → 0 net-new errors vs the SEED-056/049 baseline
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: public GETs (getSetupStatus / getPublicConfig) + setupApi.ts (X-Setup-Token client)</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/setupApi.ts</files>
  <read_first>
    - frontend/src/lib/api.ts:13 (`API_BASE`) + :75-83 (`getAuthHeaders` — the anti-analog to AVOID) + `getMaintenanceStatus` (the public unauth sibling to model)
    - 158-06-PLAN.md interfaces (the exact endpoint shapes) + 158-RESEARCH.md Anti-Patterns (routing setup writes through getAuthHeaders throws)
  </read_first>
  <action>
    In `api.ts`, add `getSetupStatus()` → `GET ${API_BASE}/api/setup/status` returning `{needs_setup, finalized, has_token}`, resolving to a safe `{needs_setup:false}` on any failure (mirror getMaintenanceStatus's fail-safe), and `getPublicConfig()` → `GET ${API_BASE}/api/public-config` returning `{supabase_url, supabase_anon_key}` (safe-null on failure). Both are UNAUTH — do NOT call `getAuthHeaders`.
    Create `frontend/src/lib/setupApi.ts`: a `const API_BASE` reuse + a private `setupHeaders(token)` returning `{ "Content-Type": "application/json", "X-Setup-Token": token }` (NEVER `getAuthHeaders` — that throws "Not authenticated" pre-auth). Export typed helpers matching the 158-06 contract: `postDetect`, `postValidate(body)`, `postSchemaBootstrap(body)`, `postOperator(body)`, `postProviderKey(body)`, `postSmoke(body)`, `postFinalize(body)` — each POSTs to `/api/setup/<name>` with the token header, parses the JSON, and surfaces a non-2xx status + the server message to the caller (so the wizard can render the plain-language error). Add types for the request/response shapes.
  </action>
  <acceptance_criteria>
    - `grep -c "X-Setup-Token" frontend/src/lib/setupApi.ts` >= 1 ; `grep -c "getAuthHeaders" frontend/src/lib/setupApi.ts` == 0
    - `grep -c "getSetupStatus\|getPublicConfig" frontend/src/lib/api.ts` >= 2
    - `cd frontend && npx tsc -b` → 0 net-new errors ; `cd frontend && npx vite build` exits 0 (the new modules resolve)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → /public-config | Runtime fetch of the two public Supabase values (never a secret) |
| pre-auth wizard → /setup/* | Writes carry the setup token, never a Supabase session |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-04 | Information Disclosure | public-config consumption | mitigate | The frontend consumes ONLY `supabase_url` + `supabase_anon_key` from `/public-config` (both public by design); no secret ever reaches the browser. |
| T-158-01 | Spoofing | setup writes | mitigate | `setupApi.ts` attaches the operator-supplied `X-Setup-Token` (from `docker compose logs backend`); it never routes through `getAuthHeaders`, so a pre-auth write is token-gated, not session-gated. |
| T-158-whitescreen | DoS | placeholder VITE_* | mitigate | Defensive `createClient` guarantees the SPA (and `/setup`) mounts even with placeholder creds, so the wizard is always reachable (Pitfall 4). |
| T-158-SC | Tampering | npm installs | accept | Zero new packages — no `react-router`, no `cmdk`; only the already-vendored `@supabase/supabase-js`. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/__tests__/supabase.test.ts` → green
- `grep -c "getAuthHeaders" frontend/src/lib/setupApi.ts` == 0 ; `grep -c "X-Setup-Token" frontend/src/lib/setupApi.ts` >= 1
- `cd frontend && npx tsc -b && npx vite build` → 0 net-new errors, build exits 0
</verification>

<success_criteria>
supabase.ts never throws on placeholder creds and hydrates runtime creds from /public-config; the public getSetupStatus/getPublicConfig GETs exist; setupApi.ts posts the /setup/* writes with X-Setup-Token and never getAuthHeaders; supabase.test.ts is green and the build is clean.
</success_criteria>

<output>
Create `.planning/phases/158-first-run-install-wizard-stretch/158-08-SUMMARY.md` when done.
</output>
