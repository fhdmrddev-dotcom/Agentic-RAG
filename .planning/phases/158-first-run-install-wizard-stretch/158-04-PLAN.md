---
phase: 158-first-run-install-wizard-stretch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-deploy-drift.sh
  - docker-compose.prod.yml
  - docs/OPERATOR.md
  - CLAUDE.md
  - .github/workflows/deploy-artifacts.yml
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "D-16: `scripts/check-deploy-drift.sh` (set -euo pipefail, non-zero exit on drift) checks the 4 invariants: preset-keys (with an OMITTED_FROM_ONEBOX allowlist), OPERATOR.md seed-list vs supabase/migrations/, sandbox-tag consistency, and `docker compose -f docker-compose.prod.yml config` parses"
    - "D-16: a same-commit CLAUDE.md sync rule requires any env-var / seed-migration / bundled-service / sandbox-tag change to update the Phase-157 artifacts in the same commit"
    - "D-02: `docker-compose.prod.yml` adds a named `setup_data:/data` volume on the `backend` service (0600 store home) — check #4 verifies it parses"
    - "The naive onebox-vs-backend.env diff yields 41 false positives; check #1 seeds OMITTED_FROM_ONEBOX so only NEW unclassified drift trips it"
  artifacts:
    - path: "scripts/check-deploy-drift.sh"
      provides: "the 4-check deployment-artifact drift gate"
      min_lines: 60
      contains: "OMITTED_FROM_ONEBOX"
    - path: "docker-compose.prod.yml"
      provides: "setup_data volume on backend"
      contains: "setup_data"
    - path: ".github/workflows/deploy-artifacts.yml"
      provides: "CI job running the drift-check"
      contains: "check-deploy-drift"
  key_links:
    - from: "scripts/check-deploy-drift.sh"
      to: "deploy/onebox.env.example + backend/.env.example + docs/OPERATOR.md + docker-compose.prod.yml"
      via: "git-tree invariant checks"
      pattern: "check-deploy-drift"
---

<objective>
Ship the folded deployment-artifact drift-check (D-16) and its runtime dependency, the `setup_data` volume (D-02 compose half): a `set -euo pipefail` bash script that makes 157-artifact drift visible, wired into CI, plus the same-commit CLAUDE.md sync rule and the OPERATOR.md reflection of the new volume + wizard path.

Purpose: the 157 artifacts are a static snapshot that drifts as future phases add env vars / migrations / services; this keeps them honest. Independent of the wizard code — runs in Wave 1.
Output: `scripts/check-deploy-drift.sh` (NEW), the `setup_data` volume, a CI job, the CLAUDE.md rule, and the OPERATOR.md updates.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-first-run-install-wizard-stretch/158-RESEARCH.md
@.planning/phases/158-first-run-install-wizard-stretch/158-PATTERNS.md
@scripts/pending-cloud-migrations.sh
@docker-compose.prod.yml
@deploy/onebox.env.example
@docs/OPERATOR.md

<interfaces>
Sibling script (copy header + `set -euo pipefail` + non-zero-exit-on-drift style) — PATTERNS.md §check-deploy-drift.sh:
  scripts/pending-cloud-migrations.sh  (set -euo pipefail:26 ; TARGET_REF="${1:-HEAD}" ; git diff --name-only --diff-filter=A ...)

The 4 checks (RESEARCH Pattern 9):
  1. preset keys — keys in backend/.env.example NOT in deploy/onebox.env.example AND NOT in OMITTED_FROM_ONEBOX (seed with the 41 known-optional keys: extra providers, S3, LangSmith, Supabase-CLI convenience vars). A naive diff = 41 false positives.
  2. seed-migration list — every filename in the OPERATOR.md Step-3 seed table exists under supabase/migrations/ (9 files today) ; a new INSERT/UPDATE-bearing migration above the highest listed → WARN.
  3. sandbox tag — SANDBOX_IMAGE in onebox == the tag in CLAUDE.md == the Dockerfile.sandbox -t intent (today 101.1).
  4. `docker compose -f docker-compose.prod.yml config` parses AND reflects the new setup_data volume.

Exact CLAUDE.md rule wording — RESEARCH Pattern 9 (Proposed CLAUDE.md same-commit sync rule).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.prod.yml — add the setup_data volume (D-02)</name>
  <files>docker-compose.prod.yml</files>
  <read_first>
    - docker-compose.prod.yml (the `backend` service + its `env_file: ./.env` + the top-level `volumes:` block — read this pass; PATTERNS.md §docker-compose.prod.yml)
    - CLAUDE.md → "## Local dev infrastructure" (the run-buffer/volume conventions) for tone
  </read_first>
  <action>
    Add a named volume `setup_data` to the top-level `volumes:` block and mount it on the `backend` service as `setup_data:/data` (the 0600 setup-store home the wizard writes to at `/data/setup.json`). Add an inline comment: "Phase 158 (DEPLOY-02) — first-run install-wizard setup-store; holds infra config + finalize marker + setup token (0600). `env_file: ./.env` is read ONCE at container create, so a wizard-configured box persists its infra tier here, overlaid onto config at boot." Optionally surface a `SETUP_STORE_PATH=/data/setup.json` default in the backend environment (matches the code default). Do NOT change `env_file:` or any existing service wiring.
  </action>
  <acceptance_criteria>
    - `grep -c "setup_data" docker-compose.prod.yml` >= 2 (volume declaration + backend mount)
    - `docker compose -f docker-compose.prod.yml config >/dev/null 2>&1 && echo PARSES` prints PARSES (if docker is unavailable in-session, a YAML lint `python -c "import yaml,sys; yaml.safe_load(open('docker-compose.prod.yml'))"` must succeed and this must be re-run at wave gate)
    - the `backend` service mounts `setup_data:/data`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: check-deploy-drift.sh + CI job (D-16)</name>
  <files>scripts/check-deploy-drift.sh, .github/workflows/deploy-artifacts.yml</files>
  <read_first>
    - scripts/pending-cloud-migrations.sh (the `set -euo pipefail` + header-comment discipline + git-diff structure — copy the style)
    - deploy/onebox.env.example + backend/.env.example (run `comm -23 <(grep -oE '^[A-Z_]+=' backend/.env.example | sort -u) <(grep -oE '^[A-Z_]+=' deploy/onebox.env.example | sort -u)` to derive the 41-key OMITTED_FROM_ONEBOX seed list) ; docs/OPERATOR.md Step-3 seed table ; CLAUDE.md sandbox-tag line (`SANDBOX_IMAGE=agentic-rag-sandbox:101.1`)
    - .github/workflows/frontend-tests.yml (the existing vitest/playwright job shape — mirror `runs-on: ubuntu-latest` + a single `run:` step; RESEARCH Pattern 9 CI wiring)
  </read_first>
  <action>
    Create `scripts/check-deploy-drift.sh` with `set -euo pipefail`, a why/usage/notes header, and the 4 checks. Check 1 (preset keys): compute keys in `backend/.env.example` absent from `deploy/onebox.env.example`, subtract the curated `OMITTED_FROM_ONEBOX` array (seed it with the ~41 known-optional keys derived above), fail non-zero listing any NEW unclassified key. Check 2 (seed list): assert every filename in the OPERATOR.md Step-3 seed table exists under `supabase/migrations/`; WARN (not fail) on a new INSERT/UPDATE-bearing migration above the highest listed number. Check 3 (sandbox tag): assert `SANDBOX_IMAGE=` in onebox equals the CLAUDE.md tag equals the `Dockerfile.sandbox -t` intent (today `101.1`). Check 4: `docker compose -f docker-compose.prod.yml config >/dev/null` parses (guard with a "docker not installed → skip with a loud WARN" branch so CI without docker still runs the other three). Print a clean PASS summary + exit 0 when all pass; exit 1 with the drift named on any failure.
    Create `.github/workflows/deploy-artifacts.yml`: a lightweight `deploy-drift` job (`runs-on: ubuntu-latest`, checkout, `run: bash scripts/check-deploy-drift.sh`) — no new secrets/actions. Reference it as a GSD gate in the script header.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-deploy-drift.sh` exits 0 on the clean tree (prints a PASS summary)
    - fixture-drift assertion: temporarily append a fake required key to `backend/.env.example`, re-run → exit non-zero naming the key; revert the fixture (leave the tree clean)
    - `grep -c "set -euo pipefail" scripts/check-deploy-drift.sh` == 1 ; `grep -c "OMITTED_FROM_ONEBOX" scripts/check-deploy-drift.sh` >= 1
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-artifacts.yml'))"` succeeds and the job runs the script
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md same-commit rule + OPERATOR.md volume/wizard reflection</name>
  <files>CLAUDE.md, docs/OPERATOR.md</files>
  <read_first>
    - CLAUDE.md → "## Deployment (cloud) — operator-gated" + the sandbox-tag sync-rule siblings (where the new same-commit rule joins) — PATTERNS.md §CLAUDE.md
    - docs/OPERATOR.md → the Home-B Step-3 (schema + 9 seeds + global row) + the compose-up steps (reflect the volume + the wizard path) — PATTERNS.md §OPERATOR.md
    - 158-RESEARCH.md → Pattern 9 "Proposed CLAUDE.md same-commit sync rule (exact wording)"
  </read_first>
  <action>
    In CLAUDE.md, add the same-commit "Deployment-artifact parity" rule verbatim from RESEARCH Pattern 9: any change to an env var the app reads, a seed-bearing migration, a bundled service, or the sandbox image tag MUST update the Phase-157 artifacts (`deploy/onebox.env.example`, `docs/OPERATOR.md` Step-3 seed list, `docker-compose.prod.yml`, and the `SANDBOX_IMAGE` tag) in the same commit; `scripts/check-deploy-drift.sh` enforces it in CI; a new intentionally-omitted var is registered in the script's `OMITTED_FROM_ONEBOX` list. Place it beside the existing sandbox-tag / cloud-parity sync siblings.
    In OPERATOR.md, add a short note that the `backend` service now carries a `setup_data:/data` volume (the install-wizard's setup-store, 0600), and cross-reference the Phase-158 first-run wizard as the browser alternative to the Home-B Steps 2-5 manual flow ("open the browser → `/setup` → the wizard walks the same steps; read the setup token from `docker compose logs backend`"). Keep the manual runbook intact (the wizard is additive; the docs remain the fallback + the drift-check's seed-list source of truth). Append migration 102 to the cloud-parity pending list if OPERATOR.md tracks it.
  </action>
  <acceptance_criteria>
    - `grep -ci "same-commit\|Deployment-artifact parity" CLAUDE.md` >= 1
    - `grep -c "setup_data" docs/OPERATOR.md` >= 1 ; `grep -ci "/setup\|install wizard\|setup token" docs/OPERATOR.md` >= 1
    - `bash scripts/check-deploy-drift.sh` still exits 0 after the OPERATOR.md edit (seed-list check consistent)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| repo tree → deployment artifacts | Static invariants; the CI drift-check is the enforcement, no runtime surface |
| mounted volume → host | The setup_data volume persists a 0600 secret file on the host |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-09 | Information Disclosure | setup_data volume | mitigate | The volume holds the 0600 `/data/setup.json` (infra secrets + token). The app writes it 0600 (158-03); the compose comment documents its sensitivity; it never enters the image or logs. |
| T-158-drift | Tampering | 157 artifacts drift | mitigate | `check-deploy-drift.sh` fails CI when a new required env var / seed migration / sandbox tag is not reflected in the onebox preset + runbook; the CLAUDE.md same-commit rule is the process control. |
| T-158-SC | Tampering | CI actions | accept | No new GitHub Actions or packages — a single `bash` run on `ubuntu-latest`. |
</threat_model>

<verification>
- `bash scripts/check-deploy-drift.sh` → exit 0 on the clean tree; exit non-zero on injected drift (fixture reverted)
- `grep -c "setup_data" docker-compose.prod.yml` >= 2 and the file parses
- `grep -ci "Deployment-artifact parity\|same-commit" CLAUDE.md` >= 1
</verification>

<success_criteria>
The drift-check exists with the 4 checks + OMITTED_FROM_ONEBOX allowlist and passes clean / fails on injected drift; the `setup_data` volume is added and the compose file parses; the CLAUDE.md same-commit rule and OPERATOR.md volume/wizard notes are in place; a CI job runs the script.
</success_criteria>

<output>
Create `.planning/phases/158-first-run-install-wizard-stretch/158-04-SUMMARY.md` when done.
</output>
