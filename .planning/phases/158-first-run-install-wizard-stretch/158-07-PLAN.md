---
phase: 158-first-run-install-wizard-stretch
plan: 07
type: execute
wave: 4
depends_on: ["158-01", "158-03", "158-06"]
files_modified:
  - backend/app/main.py
  - backend/tests/test_setup_boot_tolerant.py
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "D-03: the lifespan is setup-mode-tolerant — in setup mode it does NOT call `assert_action_types_synced(await get_pg_pool())` (main.py:356-357, the ONE un-guarded DB hard-fail) and does NOT spawn the four background reconcilers; a configured box still runs them all (byte-identical when finalized)"
    - "D-04: `SetupMiddleware` is registered alongside `MaintenanceMiddleware`, BEFORE CORS (CORS stays outermost so a 503 carries CORS headers)"
    - "D-15: on an unfinalized boot the lifespan announces the setup token to stdout (`docker compose logs backend`) exactly once"
    - "The setup router + /public-config are included in the app; a configured box's boot path is unchanged (setup mode gated on the file marker, no DB read)"
  artifacts:
    - path: "backend/app/main.py"
      provides: "setup-mode-tolerant lifespan guard + SetupMiddleware registration + setup router include + token announce"
      contains: "SetupMiddleware"
  key_links:
    - from: "backend/app/main.py lifespan"
      to: "setup_store.setup_finalized() → _setup_mode guard on assert_action_types_synced + reconciler spawns"
      via: "if not _setup_mode:"
      pattern: "_setup_mode|setup_finalized"
    - from: "backend/app/main.py"
      to: "app.add_middleware(SetupMiddleware) + app.include_router(setup.router)"
      via: "registered before CORS; router included"
      pattern: "SetupMiddleware|setup.router"
---

<objective>
Wire the setup mechanism into the app boot: make the lifespan degrade (not crash) on an unbound box by guarding the single un-wrapped DB hard-fail (`main.py:357`) + skipping the four reconciler spawns in setup mode, register `SetupMiddleware` before CORS, include the setup router + `/public-config`, and announce the setup token once on an unfinalized boot.

Purpose: D-03 (boot-tolerant — the operator reaches `/setup` instead of a crash-loop), D-04 (gate registration), D-15 (token in logs). This is the integration seam; a configured box is byte-identical.
Output: `main.py` edits. Flips `test_setup_boot_tolerant.py` green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-first-run-install-wizard-stretch/158-RESEARCH.md
@.planning/phases/158-first-run-install-wizard-stretch/158-PATTERNS.md

<interfaces>
The ONE crash path to guard — main.py:356-357 (read this pass; every OTHER startup step is already try/except):
  from app.services.audit_service import assert_action_types_synced
  await assert_action_types_synced(await get_pg_pool())   # UN-wrapped — awaits an unreachable DB in setup mode

The four reconciler spawns to skip in setup mode — main.py:376, :399, :421, :453 (asyncio.create_task(_resume_stranded / _reconcile_orphans / _sweep_expired_templates / _reconcile_orphans_periodic))
House style for a deliberately un-wrapped hard-fail (re-enable it post-finalize the same way) — main.py:324 (_validate_and_report_cipher, un-wrapped by design)

Middleware registration — main.py:559 (app.add_middleware(MaintenanceMiddleware), BEFORE CORS at :567). Router include block — main.py:602-628.

_setup_mode = not setup_store.setup_finalized()   # cheap file read, no DB (RESEARCH Pattern 3)
announce — setup_store.announce_token_if_unfinalized()  (logs the token once; RESEARCH Code Examples)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setup-mode-tolerant lifespan + token announce</name>
  <files>backend/app/main.py, backend/tests/test_setup_boot_tolerant.py</files>
  <behavior>
    - in setup mode (`not setup_finalized()`): the lifespan does NOT call `assert_action_types_synced` and does NOT spawn the 4 reconcilers; it announces the setup token once
    - in configured mode: `assert_action_types_synced` runs AND all 4 reconcilers spawn (byte-identical to today)
  </behavior>
  <read_first>
    - backend/app/main.py:286-357 (the lifespan up to the un-guarded audit-enum drift guard) + :364-453 (the four `asyncio.create_task` reconciler spawns) — read this pass
    - 158-RESEARCH.md Pattern 3 (the line-by-line audit: 357 is the ONLY crash path) + Pitfall 3 (crash-loop hides the token)
    - backend/tests/test_setup_boot_tolerant.py (158-01 contract — asserts assert_action_types_synced is NOT called in setup mode; the bare-TestClient-no-lifespan trick from test_147_maintenance_mw.py:189-211)
  </read_first>
  <action>
    Near the top of the `lifespan` (after the AnyIO limiter bump), compute `_setup_mode = not setup_store.setup_finalized()` (a cheap file read — no DB). Guard the audit-enum drift guard: wrap the `from app.services.audit_service import assert_action_types_synced` + `await assert_action_types_synced(await get_pg_pool())` block (main.py:356-357) in `if not _setup_mode:` — in setup mode, `logger.warning("SETUP MODE — DB unbound; deferring audit-enum drift guard until finalize")` instead (keep the loud hard-fail for configured boxes). Also gate the four `asyncio.create_task(...)` reconciler spawns (:376/:399/:421/:453) behind `if not _setup_mode:` (skip spawning to keep the log clean on a fresh box — they are already best-effort, but a fresh box has no DB to reconcile). When `_setup_mode`, call `setup_store.announce_token_if_unfinalized()` so the operator can read the token from `docker compose logs backend`. Do NOT touch the already-guarded best-effort blocks (Redis PING, settings migration, cipher sweep, operator seed) — they log-and-continue as-is.
    Fill test_setup_boot_tolerant.py green: monkeypatch `setup_finalized()` → False and assert (via a spy/monkeypatch on `assert_action_types_synced`) it is NOT awaited during lifespan; monkeypatch → True and assert it IS.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_setup_boot_tolerant.py -x -q` → all pass (not-called in setup mode; called in configured mode)
    - `grep -c "_setup_mode" backend/app/main.py` >= 5 (the guard + the 4 reconciler gates)
    - `grep -c "announce_token_if_unfinalized" backend/app/main.py` == 1
    - `cd backend && python -c "import app.main"` imports clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: register SetupMiddleware (before CORS) + include the setup router</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py:549-574 (the MaintenanceMiddleware registration BEFORE CORS + the CORS block) + :602-628 (the `app.include_router(...)` block) — read this pass
    - 158-RESEARCH.md Pattern 4 (register before CORS so a 503 carries CORS headers) + PATTERNS.md §main.py
  </read_first>
  <action>
    Register `SetupMiddleware` alongside `MaintenanceMiddleware` in the same neighborhood (main.py:559), BEFORE the CORS middleware, with a comment mirroring the MaintenanceMiddleware rationale (CORS outermost so a 503 setup-required response still carries CORS headers; pure-ASGI so SSE is never buffered; the file-marker latch means a configured box is a single-bool no-op). Import `SetupMiddleware` from `app.middleware.setup`. Add `from app.api import setup as setup_api` to the router-imports line and `app.include_router(setup_api.router)` in the include block, plus ensure the open `GET /public-config` route is mounted at the top level (either a route on the setup router with an absolute path or a small `@app.get("/public-config")` beside `/health` — pick one; the SetupMiddleware allowlist already exempts `/public-config`). Do NOT reorder or alter existing middleware/router registration.
  </action>
  <acceptance_criteria>
    - `grep -c "SetupMiddleware" backend/app/main.py` >= 1 and it appears BEFORE the `CORSMiddleware` add (setup registered before CORS)
    - `grep -c "setup_api.router\|setup.router" backend/app/main.py` >= 1 ; `grep -c "public-config" backend/app/main.py` >= 1
    - `cd backend && python -c "import app.main; print('ok')"` prints ok (app constructs with both middlewares + the router)
    - `cd backend && python -m pytest tests/test_setup_*.py -q` → the full 12-file setup suite green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| container boot → setup mode | On an unbound box the boot must reach setup mode + print the token, not crash-loop |
| middleware order | SetupMiddleware must sit inside CORS so a 503 is browser-readable |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-07 | DoS | boot crash-loop hides the token | mitigate | Guard the sole un-wrapped DB hard-fail (`assert_action_types_synced`, main.py:357) + skip the 4 reconciler spawns in setup mode, so a fresh box boots into setup mode and PRINTS the token instead of crash-looping (Pitfall 3). |
| T-158-05 | DoS | gate never bounces live users | mitigate | `_setup_mode` is derived from the file-marker latch (`setup_finalized()`), never a DB read — a configured box always boots fully (audit guard + reconcilers), byte-identical. |
| T-158-01 | Spoofing | token discoverability | mitigate | `announce_token_if_unfinalized` prints the token ONLY to stdout → `docker compose logs backend` (host/log access = the legitimate operator). |
| T-158-SC | Tampering | pip installs | accept | Zero new packages. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_setup_boot_tolerant.py tests/test_setup_gate.py -q` → green
- `cd backend && python -c "import app.main"` imports clean with both middlewares + the setup router
- `grep -c "_setup_mode" backend/app/main.py` >= 5 (audit guard + 4 reconciler gates)
</verification>

<success_criteria>
The lifespan degrades gracefully in setup mode (no audit-guard crash, no reconciler spawn) and announces the token once; `SetupMiddleware` is registered before CORS; the setup router + `/public-config` are included; a configured box boots byte-identically; the full setup suite is green.
</success_criteria>

<output>
Create `.planning/phases/158-first-run-install-wizard-stretch/158-07-SUMMARY.md` when done.
</output>
