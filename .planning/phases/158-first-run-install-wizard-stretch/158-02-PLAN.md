---
phase: 158-first-run-install-wizard-stretch
plan: 02
type: execute
wave: 1
depends_on: ["158-01"]
files_modified:
  - supabase/migrations/102_setup_complete.sql
  - backend/app/models/user_settings.py
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "D-05: the auditable DB half of the finalize marker exists — `app_settings.setup_complete boolean NOT NULL DEFAULT false` (migration 102) with a `setup_complete()` reader beside `maintenance_mode()`"
    - "D-05: the DB flag is the AUDITABLE signal only, NOT the gate authority — `setup_complete()` fails soft to False on a cold cache / DB blip (mirrors maintenance_mode)"
    - "Migration 102 uses the exact 097 apply-header discipline (paste into the Supabase SQL editor; keep the `INSERT ... ('global') ON CONFLICT DO NOTHING` A6 row) — the live apply + full-schema regen is 158-12 (operator-gated)"
  artifacts:
    - path: "supabase/migrations/102_setup_complete.sql"
      provides: "app_settings.setup_complete column + global-row guarantee"
      contains: "setup_complete boolean"
    - path: "backend/app/models/user_settings.py"
      provides: "setup_complete() auditable reader"
      contains: "def setup_complete"
  key_links:
    - from: "backend/app/models/user_settings.py setup_complete()"
      to: "load_app_settings().setup_complete"
      via: "TTL-cached settings read, fail-soft"
      pattern: "setup_complete"
---

<objective>
Author migration 102 (`app_settings.setup_complete`) and the `setup_complete()` reader helper — the AUDITABLE DB half of the D-05 dual finalize marker. The live apply + full-schema regen is deliberately deferred to operator-gated plan 158-12; this plan only writes the SQL file and the code that reads the column (fail-soft until the column exists).

Purpose: give the finalize endpoint (158-06) a DB flag to write and an auditable read path, without a live-DB operation blocking the autonomous code chain.
Output: `supabase/migrations/102_setup_complete.sql` + `setup_complete()` in `user_settings.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-first-run-install-wizard-stretch/158-PATTERNS.md
@.planning/phases/158-first-run-install-wizard-stretch/158-RESEARCH.md

<interfaces>
Analog migration (copy the apply-header discipline verbatim) — from PATTERNS.md §102 migration:
  supabase/migrations/097_operator_flags.sql
    ALTER TABLE public.app_settings ADD COLUMN IF NOT EXISTS maintenance_mode boolean DEFAULT false;
    INSERT INTO public.app_settings (id) VALUES ('global') ON CONFLICT DO NOTHING;

Analog reader (exact sibling) — user_settings.py:910 (read this pass, verbatim in PATTERNS.md §setup_complete):
  def maintenance_mode() -> bool:
      try:
          return load_app_settings().maintenance_mode
      except Exception:   # default-OPEN False on cold cache / read failure
          return False
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: write migration 102 (analog 097)</name>
  <files>supabase/migrations/102_setup_complete.sql</files>
  <read_first>
    - supabase/migrations/097_operator_flags.sql (copy the header-comment + apply-note discipline verbatim; PATTERNS.md §`102_setup_complete.sql`)
    - CLAUDE.md → the migration rules ("<digits>_name.sql"; apply via the Supabase SQL editor, never `db push`/`db reset`; regen full-schema after)
  </read_first>
  <action>
    Create `supabase/migrations/102_setup_complete.sql` with the 097 header discipline. Body is one column add plus the A6 global-row guarantee: `ALTER TABLE public.app_settings ADD COLUMN IF NOT EXISTS setup_complete boolean NOT NULL DEFAULT false;` then `INSERT INTO public.app_settings (id) VALUES ('global') ON CONFLICT DO NOTHING;`. In the header comment, state: apply by pasting into the LOCAL Supabase SQL editor (or psycopg2 to 127.0.0.1:54322) — NEVER `supabase db push`/`db reset`; then run `bash scripts/regenerate-full-schema.sh` (no `--reset`); commit both files. Note CLOUD PARITY still pending (append 102 to the migs 099/100/101 + `SECRETS_ENCRYPTION_KEY` cloud-parity list). Do NOT hand-edit `supabase/full-schema.sql` — that regen is plan 158-12.
    Filename MUST match `102_setup_complete.sql` exactly (digits-underscore; no letter suffix — the Supabase CLI silently skips `102b`-style names).
  </action>
  <acceptance_criteria>
    - `test -f supabase/migrations/102_setup_complete.sql` succeeds
    - `grep -c "setup_complete boolean" supabase/migrations/102_setup_complete.sql` == 1
    - `grep -c "ON CONFLICT DO NOTHING" supabase/migrations/102_setup_complete.sql` >= 1 (the A6 global-row guarantee)
    - `grep -Eiq "db push|db reset" supabase/migrations/102_setup_complete.sql` → matches ONLY inside a "never" warning line (no directive to run them)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: add the setup_complete() reader beside maintenance_mode()</name>
  <files>backend/app/models/user_settings.py</files>
  <read_first>
    - backend/app/models/user_settings.py:910 (`maintenance_mode()` — the exact sibling; add directly beside it) + the `AppSettings` model / `load_app_settings()` it reads (so the `setup_complete` attribute resolves)
    - 158-RESEARCH.md → Code Examples `setup_complete()` helper; PATTERNS.md §`user_settings.py`
  </read_first>
  <action>
    Add `def setup_complete() -> bool:` immediately beside `maintenance_mode()` (user_settings.py:910). Body mirrors maintenance_mode exactly: `try: return load_app_settings().setup_complete` / `except Exception: return False` — default False on cold cache / DB blip / column-absent. Docstring states this is the AUDITABLE/app-facing signal only — NOT the gate authority (the file marker in `setup_store.py` is, per D-05). Ensure the settings model that `load_app_settings()` builds exposes a `setup_complete` attribute (add the field with a `False` default to the `AppSettings` dataclass/model and to `_build_settings_from_row` if that function enumerates columns, so a fresh row without the column reads False rather than raising). Do NOT fork `save_app_settings` — the finalize DB write in 158-06 reuses it.
  </action>
  <acceptance_criteria>
    - `grep -c "def setup_complete" backend/app/models/user_settings.py` == 1
    - `cd backend && python -c "from app.models.user_settings import setup_complete; print(setup_complete())"` prints `False` (fail-soft, no raise, even with the column not yet applied)
    - `cd backend && python -c "import app.models.user_settings"` imports clean (no syntax/attribute error)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DDL → app_settings | An additive boolean column; no data exposure, no cross-user read path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-mig | Tampering | migration 102 apply | mitigate | Applied only via the Supabase SQL editor / psycopg2 to :54322 (no `db push`/`db reset`) in the operator-gated 158-12; `ADD COLUMN IF NOT EXISTS` is idempotent; the `('global') ON CONFLICT DO NOTHING` row prevents the A6 silent-no-op (Pitfall 5). |
| T-158-05 | Tampering | setup_complete() as gate | accept→mitigate | The DB flag is AUDITABLE-only; the middleware gate authority is the file marker (158-03), so a DB blip can never bounce live users into the wizard. `setup_complete()` fails soft to False. |
| T-158-SC | Tampering | pip installs | accept | Zero new packages (158-RESEARCH). |
</threat_model>

<verification>
- `grep -c "setup_complete boolean" supabase/migrations/102_setup_complete.sql` == 1
- `cd backend && python -c "from app.models.user_settings import setup_complete; setup_complete()"` returns False without raising
- No live-DB operation performed in this plan (apply + regen is 158-12)
</verification>

<success_criteria>
Migration 102 authored with the 097 discipline + A6 global row; `setup_complete()` reader added fail-soft beside `maintenance_mode()`; both import/parse clean; live apply deferred to 158-12.
</success_criteria>

<output>
Create `.planning/phases/158-first-run-install-wizard-stretch/158-02-SUMMARY.md` when done.
</output>
