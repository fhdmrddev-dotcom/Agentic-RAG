---
phase: 122-cross-provider-trust-honesty-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/services/openai_service.py
  - backend/tests/unit/test_config_registry.py
  - backend/tests/unit/test_gateway_forcing.py
autonomous: true
requirements: [MP-02]
must_haves:
  truths:
    - "Every MODEL_CAPABILITIES row carries an explicit emit_tier in {force_strict, force, coerce} (D-122-04, SC#2)"
    - "Exactly 14 rows are force_strict and all 14 are provider==openai; 0 non-OpenAI force_strict rows (D-122-04, SC#2)"
    - "The 2 DeepSeek rows are emit_tier=force (demoted — strict is inert without /beta base_url) (D-122-04, Pitfall 3, SC#2)"
    - "GLM/Zhipu rows resolve to emit_tier=force (kept, intentional, non-strict) (D-122-04, SC#2)"
    - "An un-doc-verified / registry-miss model resolves to emit_tier=coerce (default-SAFE, D-122-05)"
    - "The hardcoded provider=='openai' strict gate (openai_service.py:1555) is removed — json_schema response_format is requested on the resolved tier, never a provider-name check (D-122-04, SC#2)"
    - "The inert DeepSeek function-level strict block (openai_service.py:1548) is removed (D-122-04, Pitfall 3, SC#2)"
    - "Removing the function-level strict block does NOT regress OpenAI force_strict (A4) — json_schema response_format still requested for force_strict shots"
  artifacts:
    - path: "backend/app/config.py"
      provides: "emit_tier Literal field on ModelCapability + 55-row migration"
      contains: "emit_tier"
    - path: "backend/app/services/openai_service.py"
      provides: "forcing branch with the provider-name gate + inert strict removed"
    - path: "backend/tests/unit/test_config_registry.py"
      provides: "registry invariant: every row has emit_tier; force_strict subset of OpenAI"
    - path: "backend/tests/unit/test_gateway_forcing.py"
      provides: "no-provider-gate + deepseek-force gateway-request-capture assertions"
  key_links:
    - from: "backend/app/config.py emit_tier"
      to: "get_model_capability default-SAFE coerce"
      via: "cap.get('emit_tier', 'coerce')"
      pattern: "emit_tier.*coerce"
---

<objective>
MP-02: replace the implicit two-bool forcing guess (`forced_emission` + `strict_json_schema`) AND the hardcoded `provider=="openai"` strict gate with a single explicit, doc-verified `emit_tier` enum (`force_strict | force | coerce`) on every `MODEL_CAPABILITIES` row. Drop the inert DeepSeek function-level `strict`; keep GLM forcing (resolves to non-strict `force`).

This is the foundation Wave for the phase: the `emit_tier` field + migration is what the MP-01 ladder (Plan 02) reads to build its rung list, and the gate removal is what makes `force_strict` mean "OpenAI by measurement, not by name."

Purpose: One explicit declared value, no provider-name special-casing, default-SAFE `coerce` (D-122-04/05). Closes the residual guesswork that left DeepSeek wrongly tagged `force_strict` with an unreachable strict path (Pitfall 3).
Output: Migrated `config.py` registry, cleaned `openai_service.py` forcing branch, two registry/gateway invariant tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-CONTEXT.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-RESEARCH.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-PATTERNS.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-VALIDATION.md

<interfaces>
<!-- The exact live seams this plan edits (verified 2026-06-23). -->

ModelCapability TypedDict — backend/app/config.py:136-182 (total=False):
  fields: native_tools, provider, llm_call_timeout_seconds, max_output_tokens,
          capability_source: Literal["registry","inferred"],
          uses_max_completion_tokens, supports_parallel_tools, max_tools,
          forced_emission: bool,  # default-SAFE absent => coerce
          strict_json_schema: bool  # default-SAFE absent => no strict
  ADD: emit_tier: Literal["force_strict", "force", "coerce"]  # Phase 122 D-122-04

MODEL_CAPABILITIES registry — backend/app/config.py:207-337 (55 rows).
  Live invariant (grep-confirmed): 50 rows carry forced_emission:True; 16 carry
  strict_json_schema:True (= 14 OpenAI + 2 DeepSeek).

openai_service.py forcing branch — backend/app/services/openai_service.py:1534-1581:
  :1536-1549 — function-level strict block (INERT for DeepSeek) → REMOVE
  :1555     — `if strict_response_format and provider == "openai":` gate → REMOVE the `and provider == "openai"`
  DO NOT TOUCH :1521-1526 (DeepSeek thinking-off) or :1449-1456 (111.1 local-provider routing — Pitfall 5)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the emit_tier field + migrate all 55 registry rows + registry invariant test</name>
  <read_first>
    - backend/app/config.py:136-182 (ModelCapability TypedDict — the field-add template; emit_tier lands beside capability_source/strict_json_schema)
    - backend/app/config.py:207-337 (the 55 MODEL_CAPABILITIES rows — the migration target)
    - backend/app/config.py:464-486 (get_model_capability — default-SAFE resolution; mirror its .get(..., default) shape for emit_tier)
    - 122-PATTERNS.md §"backend/app/config.py" (the 55-row migration table with live line ranges + the row examples)
    - 122-RESEARCH.md §"Pattern 2" + §"Pitfall 3" (DeepSeek demotion rationale — strict needs /beta base_url we never set)
    - backend/tests/unit/test_gateway_forcing.py:1-33 (module-top import style for the NEW test_config_registry.py — registry symbols exist today)
  </read_first>
  <behavior>
    - test_every_row_has_emit_tier: every MODEL_CAPABILITIES row has emit_tier in {force_strict, force, coerce}
    - test_force_strict_is_openai_only: every emit_tier==force_strict row has provider==openai; assert exactly 14 such rows
    - test_deepseek_is_force: every provider==deepseek row has emit_tier==force (never force_strict — strict is inert)
    - test_no_non_openai_force_strict: 0 rows with emit_tier==force_strict and provider!=openai
    - test_glm_zhipu_is_force: provider==zhipu rows resolve to emit_tier==force (kept, non-strict)
  </behavior>
  <action>
    Add `emit_tier: Literal["force_strict", "force", "coerce"]` to the `ModelCapability` TypedDict (next to `capability_source`/`strict_json_schema`), with a Phase-122 D-122-04 comment naming it the single source of truth, default-SAFE `coerce`.

    Migrate all 55 `MODEL_CAPABILITIES` rows per the locked mapping (122-PATTERNS.md table): 14 OpenAI rows → `force_strict`; the 2 DeepSeek rows (`deepseek-v4-flash`/`deepseek-v4-pro`) → `force` (DEMOTED — strict inert, no /beta base_url, per Pitfall 3); 34 forced-no-strict rows (anthropic/google/minimax/zhipu + conditional openrouter) → `force`; 5 Kimi/Moonshot rows (`forced_emission` absent) → `coerce`. KEEP GLM/Zhipu forcing as `force` (D-122-04 locked — verify live against the registry). Add an inline `# DEMOTED — strict inert` comment on each DeepSeek row.

    Per RESEARCH Open Q1 / D-122-04: this plan ADDS `emit_tier` as an explicit literal and LEAVES the two old bools (`forced_emission`/`strict_json_schema`) in place but deprecated-unread for one phase (safer rollback). Do NOT add a derived view that re-reads `strict_json_schema` (re-introduces the DeepSeek guess — RESEARCH anti-pattern). Plan 02 reads `emit_tier` directly; this plan does not remove the old bools.

    Create NEW `backend/tests/unit/test_config_registry.py` with the registry invariants above (module-top `from app.config import MODEL_CAPABILITIES`). Assert the exact count: 14 `force_strict` rows.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_config_registry.py -k emit_tier -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/unit/test_config_registry.py -k emit_tier -x` passes
    - Source: every row in `MODEL_CAPABILITIES` has an `emit_tier` key (no KeyError in the invariant loop)
    - Source: exactly 14 rows are `force_strict`, all provider==openai; 0 non-OpenAI `force_strict`
    - Source: both deepseek rows are `force` (grep `config.py` for the two deepseek rows shows `"emit_tier": "force"`)
    - Behavior: `get_model_capability` for a registry-miss model returns a dict whose `.get("emit_tier", "coerce")` yields `coerce` (default-SAFE — assert in test)
  </acceptance_criteria>
  <done>emit_tier field added; all 55 rows migrated (14/2/34/5); DeepSeek demoted to force; registry invariant test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the provider-name strict gate + inert DeepSeek strict in the forcing branch + gateway-capture test</name>
  <read_first>
    - backend/app/services/openai_service.py:1534-1581 (the force_tool_name branch — the two removal sites)
    - backend/app/services/openai_service.py:1521-1526 (DeepSeek thinking-off — DO NOT TOUCH, separate concern)
    - backend/app/services/openai_service.py:1449-1456 (111.1 local-provider routing — Pitfall 5, DO NOT TOUCH)
    - 122-PATTERNS.md §"backend/app/services/openai_service.py" (the two removal sites + target state + A4 OpenAI-force_strict-must-not-regress note)
    - 122-RESEARCH.md §"Code Examples → MP-02 deletions" + §"Pitfall 5" (the 111.1 gateway-seam adjacency)
    - backend/tests/unit/test_gateway_forcing.py (the file to extend — the GatewayRequest capture style)
    - backend/tests/unit/test_103_forced_emit_strict.py:117-185 (the request.strict_schema capture pattern to mirror)
  </read_first>
  <behavior>
    - test_no_provider_gate: a force_strict-tier shot for a non-"openai" provider-name context still requests the json_schema response_format (the gate is tier-driven, not name-driven)
    - test_deepseek_force: a deepseek force-tier shot carries NO function-level strict flag on the forced tool def
    - test_openai_force_strict_preserved (A4): an OpenAI force_strict shot still requests the json_schema response_format (no regression from removing the function-level strict block)
  </behavior>
  <action>
    In the `force_tool_name` branch (`openai_service.py:1534-1581`): REMOVE the function-level strict block (`:1536-1549` — the `_fn["strict"] = True` loop; inert for DeepSeek, and OpenAI's guarantee comes from the `response_format` json_schema, not this flag — A4). REMOVE the `and provider == "openai"` name check from the gate at `:1555` so the `response_format` json_schema is built whenever `strict_response_format` is true. Per D-122-04 the caller sets `strict_response_format` ONLY for `emit_tier=="force_strict"` shots (post-migration: OpenAI-only by measurement) — so the gate becomes tier-driven, no provider-name special-case. Keep the json_schema-building loop (`:1567-1581`) intact.

    DO NOT touch the DeepSeek thinking-off block (`:1521-1526`) or the 111.1 local-provider routing (`:1449-1456`) — Pitfall 5. Read BUG-260616-01's seam note before editing.

    Extend `backend/tests/unit/test_gateway_forcing.py` with the three captures above using the `GatewayRequest`/kwargs-capture pattern from `test_103_forced_emit_strict.py` (assert what reaches the gateway: `response_format` present/absent, function-level `strict` absent). The A4 OpenAI-force_strict test is load-bearing — it proves removing the function-level strict block did not weaken OpenAI.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_gateway_forcing.py -k "no_provider_gate or deepseek_force or openai_force_strict" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/unit/test_gateway_forcing.py -k "no_provider_gate or deepseek_force or openai_force_strict" -x` passes
    - Source: `openai_service.py` no longer contains `provider == "openai"` inside the forcing branch (grep the branch range — the name check is gone)
    - Source: `openai_service.py` no longer contains the `_fn["strict"] = True` function-level loop in the forcing branch
    - Source: the `:1449-1456` local-provider routing and `:1521-1526` thinking-off blocks are unchanged (diff shows no edit there)
    - Behavior: A4 — an OpenAI force_strict shot still emits the json_schema response_format (test green)
  </acceptance_criteria>
  <done>Provider-name gate removed; inert DeepSeek strict removed; OpenAI force_strict json_schema preserved (A4); 111.1 seams untouched; gateway-capture test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → forced_emit/gateway | the forced-shot request crosses into provider adapters; the resolved `emit_tier` decides what forcing/strict reaches the wire |
| forced shot → non-active provider | a cross-provider shot resolves the TARGET provider's key/base_url (existing injection block — 111.1's seam, NOT touched here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01-01 | Information Disclosure | cross-provider key/base_url injection (`forced_emit.py:251-291`) | accept | This plan does NOT touch the injection block (Pitfall 5 / 111.1's seam); the gate-removal edits ONLY the forcing-translation lines (`openai_service.py:1534-1581`). Diff-verified no change to `:1449-1456`. |
| T-122-01-02 | Tampering | inert DeepSeek strict removal | mitigate | A4 — removing the function-level `strict` flag does NOT weaken OpenAI (OpenAI's guarantee is the `response_format` json_schema, requested whenever `strict_response_format` is true). The explicit OpenAI force_strict test (Task 2) proves it live-in-test. |
| T-122-01-03 | Spoofing | registry `emit_tier` integrity (a wrong tier could over-claim forcing) | mitigate | Default-SAFE `coerce` on a registry miss (D-122-05); the registry invariant test asserts `force_strict ⊆ OpenAI` so no non-OpenAI row can claim token-level strict it cannot reach. |
| T-122-01-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan — pure first-party edits to `config.py`/`openai_service.py`. Package Legitimacy Gate N/A (RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `pytest backend/tests/unit/test_config_registry.py -x -q` green (registry invariant)
- `pytest backend/tests/unit/test_gateway_forcing.py -x -q` green (no-provider-gate + deepseek-force + OpenAI-force_strict A4)
- Full backend unit suite no net-new failures: `backend/venv/Scripts/python.exe -m pytest backend/tests/ -q`
- Diff confirms `forced_emit.py:251-291` and `openai_service.py:1449-1456`/`:1521-1526` untouched (Pitfall 5)
</verification>

<success_criteria>
- emit_tier field on ModelCapability + all 55 rows migrated (14 force_strict / 2 deepseek-force / 34 force / 5 coerce)
- provider=='openai' gate removed (tier-driven); inert DeepSeek function-level strict removed
- OpenAI force_strict json_schema response_format preserved (A4, no regression)
- Registry invariant + gateway-capture tests green; 111.1 local-provider seams untouched
</success_criteria>

<output>
Create `.planning/phases/122-cross-provider-trust-honesty-parity/122-01-SUMMARY.md` when done
</output>
