---
phase: 177-v3-4-org-surface-polish
plan: 02
type: execute
wave: 2
depends_on: [177-01]
files_modified:
  - frontend/src/components/org/OrgBand.tsx
  - frontend/src/components/layout/ProfileMenu.tsx
  - frontend/src/components/layout/ProfileMenu.test.tsx
autonomous: true
requirements: [ORGUX-01]
must_haves:
  truths:
    - "D-04: the ◆ Org-admin / Member badge renders via the ONE shared RoleBadge in OrgBand AND ProfileMenu — the inline isOrgAdminRole copies are gone (one element, not look-alikes)"
    - "D-05: the role badge reflects the ACTIVE org's role (OrgProvider re-derives role per active org via the activeOrgId-keyed probe) — audited, no new switching behavior added"
    - "D-06: the org-admin shield + the 'Organization admin' entry + the switcher stay honest-ABSENT (canManage / orgs.length >= 2) — never rendered disabled"
    - "D-07: the org zone stays indigo and the operator zone stays amber — amber never appears in any org surface"
  artifacts:
    - path: "frontend/src/components/org/OrgBand.tsx"
      provides: "Org band consuming RoleBadge (Shield + ORG ADMIN chip + recording marker + ⌥ preserved)"
      contains: "RoleBadge"
    - path: "frontend/src/components/layout/ProfileMenu.tsx"
      provides: "Identity anchor consuming RoleBadge; switcher honest-absent at >= 2 orgs"
      contains: "RoleBadge"
  key_links:
    - from: "OrgBand.tsx"
      to: "OrgIdentity.RoleBadge"
      via: "import + <RoleBadge role={role} />"
      pattern: "RoleBadge"
    - from: "ProfileMenu.tsx"
      to: "OrgProvider.role (per active org)"
      via: "useOrgOptional().role → RoleBadge"
      pattern: "role"
---

<objective>
Make the org-identity language read as ONE built-together element across the shell band and the
rail-footer identity anchor (ORGUX-01, sketch 131-C). Swap the two inline `◆ Org-admin / Member`
badge copies in OrgBand + ProfileMenu for the shared `RoleBadge` (177-01), then audit-and-lock the
honest-state matrix: per-org role (D-05), honest-absent affordances (D-06), and indigo/amber zone
separation (D-07). Behavior is byte-identical — this is display cohesion + an invariant lock, NOT new
switching behavior.

Purpose: 131-C's cohesion thesis is that the badge is one component reused everywhere, and its
honesty (member here / org-admin there; the shield VANISHES for a member) holds across the matrix.
Output: OrgBand + ProfileMenu rendering RoleBadge; a ProfileMenu switcher-visibility test; grep-locked
honest-absent + zone invariants.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/177-v3-4-org-surface-polish/177-CONTEXT.md
@.planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md
@.planning/sketches/131-org-state-matrix/README.md
@.planning/phases/177-v3-4-org-surface-polish/177-01-SUMMARY.md

<interfaces>
<!-- Consumed from 177-01 (frontend/src/components/org/OrgIdentity.tsx) -->
RoleBadge({ role: string })            // renders ◆ Org-admin / Dept-admin / Member via roleBadgeMeta
roleBadgeMeta(role): { label, admin }  // the reconciled 4-tier mapping
OrgAvatar({ initial, pending? })       // the gradient initial-circle atom

<!-- Per-org role truth (audit target) — frontend/src/providers/OrgProvider.tsx -->
OrgValue.role: string       // "the caller's role in the ACTIVE org" — drives the badge
OrgValue.canManage: boolean // org:manage — gates the shell + rail shield (render-only)
// The probe (useOrgPermissionsProbe) is keyed on activeOrgId + fail-closed → a switch re-derives role.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap OrgBand + ProfileMenu inline role badges → shared RoleBadge (D-04)</name>
  <files>frontend/src/components/org/OrgBand.tsx, frontend/src/components/layout/ProfileMenu.tsx</files>
  <read_first>
    - frontend/src/components/org/OrgIdentity.tsx (the RoleBadge API from 177-01)
    - frontend/src/components/org/OrgBand.tsx (whole file — swap :46-49 isOrgAdminRole + :78-87 the two badge spans)
    - frontend/src/components/layout/ProfileMenu.tsx (whole file — swap :56-60 isOrgAdminRole + :133-142 the two badge spans)
    - .planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md §"OrgIdentity + RoleBadge" (the 4-site consolidation)
  </read_first>
  <action>
    In `OrgBand.tsx`: delete the local `isOrgAdminRole` helper (:47-49) and the two inline badge
    `<span>`s (the admin ◆ pill + the muted Member pill, :78-87); import `RoleBadge` from
    `@/components/org/OrgIdentity` and render `<RoleBadge role={role} />` in that slot. Keep EVERYTHING
    else verbatim — the `Shield` glyph, the `ORG ADMIN` mono chip, the `TechnicalNamesToggle`, the
    "every action recorded" recording marker (062-A honesty beat), the `recordingPulse` prop, the
    "Back to app" button. The band stays `border-primary/25` indigo (D-07) — do not touch its colors.
    In `ProfileMenu.tsx`: delete the local `isOrgAdminRole` (:58-60) and the two inline badge spans in
    the identity header (:133-142); render `<RoleBadge role={role} />` there (role already resolves via
    `const role = org?.role ?? "member"`, :75 — the per-org value, D-05). Keep the identity header
    name/email, the `showSwitcher = orgs.length >= 2` gate + the whole switcher section, the theme item,
    and Sign out verbatim. Do NOT move any gating into RoleBadge. No new imports beyond RoleBadge; the
    `Shield`/`isOrgAdminRole` removal must not orphan an unused import.
  </action>
  <acceptance_criteria>
    - `grep -c "isOrgAdminRole" frontend/src/components/org/OrgBand.tsx frontend/src/components/layout/ProfileMenu.tsx` → 0 in both (the local helpers removed)
    - both files `grep -q "RoleBadge"` (import + render)
    - `grep -c "◆" frontend/src/components/org/OrgBand.tsx frontend/src/components/layout/ProfileMenu.tsx` → 0 (the inline ◆ spans gone; the glyph now lives only inside RoleBadge)
    - OrgBand still contains "ORG ADMIN" and "every action recorded" (grep both — the honesty beats preserved)
    - ProfileMenu still contains `orgs.length >= 2` (the D-06 switcher honest-absent gate)
    - `npx tsc -b` passes (no orphaned imports)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/org/OrgAdminShell.test.tsx --reporter=dot</automated>
  </verify>
  <done>OrgBand + ProfileMenu render the shared RoleBadge; the ORG ADMIN chip, recording marker, ⌥, and switcher gate are preserved; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Audit-and-lock the honest-state matrix (D-05 / D-06 / D-07) + ProfileMenu switcher test</name>
  <files>frontend/src/components/layout/ProfileMenu.test.tsx</files>
  <read_first>
    - frontend/src/providers/OrgProvider.tsx:43-63,107-131 (role/canManage per active org; probe keyed on activeOrgId)
    - frontend/src/hooks/useOrgPermissionsProbe.ts (re-keyed on activeOrgId, fail-closed — confirm no new wiring needed)
    - frontend/src/components/layout/NavPanel.tsx:113-116,190-222 (org shield `canManage &&` indigo · operator shield `isOperator &&` amber)
    - frontend/src/components/org/OrgAdminShell.tsx (the non-manager guard renders a message, never chrome)
    - .planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md §"Shared Patterns" (D-05 / honest-absent / zone separation)
    - frontend/src/components/org/InvitationsTab.test.tsx (a mocked-provider render test to mirror for ProfileMenu)
  </read_first>
  <action>
    AUDIT (D-05): confirm OrgProvider.role + canManage are re-derived per active org — the probe
    (useOrgPermissionsProbe) is keyed on `activeOrgId` and fail-closed, so a switch re-probes and the
    badge/shield follow the active org with NO new code. Record the verdict in the SUMMARY: D-05
    collapses to a display-consistency audit (every badge site reads OrgProvider.role, threaded from
    the shell) — do NOT add role-derivation logic. If (and only if) the audit finds a site reading a
    stale/local role copy, fix that ONE read in place; otherwise no source change here.
    LOCK (D-06 / D-07) via a net-new `ProfileMenu.test.tsx`: mock `@/hooks/useAuth` and
    `@/providers/OrgProvider` (`useOrgOptional`) and assert — (1) with a 1-org membership the switcher
    is ABSENT (no "Switch organization" label; a solo user sees just the identity), (2) with a 2-org
    membership the switcher renders both orgs, (3) role="member" renders the Member badge (no ◆
    Org-admin) and role="org-admin" renders the ◆ Org-admin badge — proving the badge follows the
    active org's role (D-05 display). Follow the InvitationsTab.test mocked-render shape. This is the
    honest-state matrix locked as a regression.
  </action>
  <acceptance_criteria>
    - SUMMARY records the D-05 audit verdict (per-org role re-derives via the activeOrgId-keyed probe; no new switching behavior added)
    - `grep -n "canManage &&" frontend/src/components/layout/NavPanel.tsx` shows the org shield honest-absent gate intact; `grep -n "isOperator &&"` shows the operator shield gate intact (D-06)
    - `grep -c "amber" frontend/src/components/org/OrgBand.tsx frontend/src/components/org/OrgMembersTab.tsx frontend/src/components/org/InvitationsTab.tsx frontend/src/components/org/SsoTab.tsx` → 0 (amber never in an org surface, D-07); NavPanel org shield uses `indigo-400`, operator shield uses `amber-400` (grep both present, distinct)
    - `ProfileMenu.test.tsx` exists and asserts switcher-absent-at-1-org, switcher-present-at-2-orgs, and member-vs-org-admin badge
    - `npx vitest run src/components/layout/ProfileMenu.test.tsx` passes 100%
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/ProfileMenu.test.tsx --reporter=dot</automated>
  </verify>
  <done>The per-org-role audit is recorded (no new wiring); the honest-absent + zone invariants are grep-locked; the ProfileMenu switcher/badge matrix is a passing regression test.</done>
</task>

</tasks>

<verification>
Security posture: NO threat model (D-01/D-03) — display polish + an invariant lock over already-secured
166 surfaces; no authz gate touched. RED LINES as guards:

- RED LINE (D-02): `git diff --name-only` shows `frontend/src/providers/StreamsProvider.tsx` UNTOUCHED (and `OrgProvider.tsx` unchanged unless the D-05 audit found a stale read).
- RED LINE (authz byte-identical): `canManage`/`canManageSso`/`canInvite` polarity + call sites unchanged — `git diff` shows no gate condition edited.
- `cd frontend && npx tsc -b` passes.
- Differential (SEED-056): `npx vitest run src/components/org src/components/layout --reporter=dot` shows OrgBand/ProfileMenu/shell tests green + the new ProfileMenu test green + 0 net-new failures vs baseline `6d145860`.
</verification>

<success_criteria>
The ◆ Org-admin / Member badge is one shared element in OrgBand + ProfileMenu; the per-org-role,
honest-absent, and indigo/amber-zone invariants are audited + regression-locked; no authz, no
StreamsProvider, no server contract touched.
</success_criteria>

<output>
Create `.planning/phases/177-v3-4-org-surface-polish/177-02-SUMMARY.md` when done — record the D-05
audit verdict and any drift-fix.
</output>
