---
phase: 177-v3-4-org-surface-polish
plan: 04
type: execute
wave: 2
depends_on: [177-01]
files_modified:
  - frontend/src/components/org/OrgMembersTab.tsx
  - frontend/src/components/org/InviteMemberDialog.tsx
autonomous: true
requirements: [ORGUX-01, ORGUX-02]
must_haves:
  truths:
    - "D-04: the OrgMembersTab roster renders the ◆ Org-admin / Member badge via the shared RoleBadge — the inline roleBadge copy is gone"
    - "D-08: the roster's Active/Pending adoption chip renders via the shared StatusChip — the inline adoption spans are gone"
    - "D-09: the invite dialog's role-picker + labels snap to the shared 4px grid + org-zone micro-label style"
    - "D-10: the invite dialog's link-first success (copyable invite link) is preserved verbatim"
    - "The roster stays a pure read leaf — RoleBadge/StatusChip render as <span>, so the roster renders ZERO <button> (its test asserts this)"
  artifacts:
    - path: "frontend/src/components/org/OrgMembersTab.tsx"
      provides: "Members roster on shared RoleBadge + StatusChip + OrgAvatar"
      contains: "RoleBadge"
    - path: "frontend/src/components/org/InviteMemberDialog.tsx"
      provides: "Invite dialog on the shared 4px grid; link-first preserved"
      contains: "invite-link"
  key_links:
    - from: "OrgMembersTab.tsx"
      to: "StatusChip + RoleBadge"
      via: "RowChips renders shared primitives"
      pattern: "StatusChip|RoleBadge"
---

<objective>
Fold the members roster and the invite dialog into the same family language (sketch 133-B / 131-C):
the roster's role badge → shared `RoleBadge`, its adoption chip → shared `StatusChip`, its avatar →
shared `OrgAvatar` (177-01); the invite dialog's role-picker + labels snap to the shared 4px grid. The
roster's pure-read-leaf invariant (zero buttons) and the dialog's link-first success are preserved
byte-identical (D-01/D-10). This is the "C's admin-vs-member view is really B's read-only behavior"
fold — no split view, just the shared primitives.

Purpose: OrgMembersTab is the fourth RoleBadge site and a StatusChip site; InviteMemberDialog is the
grid-cohesion target. Output: both on the shared vocab/grid, honest behaviors intact.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/177-v3-4-org-surface-polish/177-CONTEXT.md
@.planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md
@.planning/sketches/133-org-management-surfaces/README.md
@.planning/phases/177-v3-4-org-surface-polish/177-01-SUMMARY.md

<interfaces>
<!-- Consumed from 177-01 -->
StatusChip({ tone, children, testId? })          // org/StatusChip.tsx
statusChipMeta(status): { label, tone }          // shared union mapper (covers "active"/"pending")
RoleBadge({ role })                              // org/OrgIdentity.tsx (4-tier)
OrgAvatar({ initial, pending? })                 // org/OrgIdentity.tsx — pending → dashed indigo variant

<!-- OrgMembersTab green contract (OrgMembersTab.test.tsx) — MUST stay green -->
// getByText "Org-admin"/"Member"; getAllByText("Active").length === 3; getByText("Pending");
// queryByRole("button") === null  (roster is a pure read leaf — chips MUST be spans, never buttons)

<!-- InviteMemberDialog green contract (via InvitationsTab.test.tsx role-picker block) -->
// Member + Org-admin buttons ENABLED, Dept-admin DISABLED "(soon)"; data-testid="invite-link"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrgMembersTab roster → shared RoleBadge + StatusChip + OrgAvatar (D-04/D-08/D-09)</name>
  <files>frontend/src/components/org/OrgMembersTab.tsx</files>
  <read_first>
    - frontend/src/components/org/OrgMembersTab.tsx (whole file — :47-53 roleBadge, :55-62 adoptionChip, :147-181 RowChips inline spans, :193-198 avatar, :225-228 dashed pending avatar)
    - frontend/src/components/org/OrgMembersTab.test.tsx (the green contract — esp. `queryByRole("button") === null`)
    - frontend/src/components/org/StatusChip.tsx + OrgIdentity.tsx (177-01 APIs)
    - .planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md §"OrgIdentity" (RoleBadge is a <span>, gates NOTHING)
  </read_first>
  <action>
    In `RowChips` (:148-181): delete the local `roleBadge` mapper (:49-53) and render the role slot as
    `<RoleBadge role={role} />`; keep the local `adoptionChip` mapper (:58-62 — pending→primary/"Pending",
    active→muted/"Active") but render the adoption slot as `<StatusChip tone={adoption.tone}
    testId="adoption-chip">{adoption.label}</StatusChip>` (drop the two inline ternary spans :169-179).
    Replace the `MemberRow` avatar (:193-198) with `<OrgAvatar initial={initial} />` and the
    `PendingRow` dashed avatar (:225-228) with `<OrgAvatar initial={initial} pending />`. Keep the row
    anatomy, the header + search, the empty/loading states, and the "no coming-soon banner" verbatim.
    CRITICAL: RoleBadge + StatusChip render as `<span>` — the roster must still render ZERO `<button>`
    (its test asserts `queryByRole("button") === null`). Do not introduce any interactive element.
    No inline `◆` spans remain; the adoption chip is the shared component.
  </action>
  <acceptance_criteria>
    - `grep -c "◆" frontend/src/components/org/OrgMembersTab.tsx` → 0; file imports `RoleBadge`, `StatusChip`, `OrgAvatar`
    - `grep -c "border-primary/30 bg-primary/10 text-primary" frontend/src/components/org/OrgMembersTab.tsx` → 0 (the inline chip token strings gone; they live only in the primitives now)
    - `npx vitest run src/components/org/OrgMembersTab.test.tsx` passes 100% — incl. `queryByRole("button") === null`, getAllByText("Active").length===3, getByText("Pending"), Org-admin/Member
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/org/OrgMembersTab.test.tsx --reporter=dot</automated>
  </verify>
  <done>The roster renders shared RoleBadge + StatusChip + OrgAvatar; inline maps/spans gone; the pure-read-leaf (zero buttons) invariant holds; the test suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: InviteMemberDialog → shared 4px grid + org-zone micro-label style (D-09/D-10)</name>
  <files>frontend/src/components/org/InviteMemberDialog.tsx</files>
  <read_first>
    - frontend/src/components/org/InviteMemberDialog.tsx (whole file — :146-188 link-first success, :191-259 the form, :218-247 the role-picker segmented chips, :193-215 the micro-labels)
    - frontend/src/components/org/InvitationsTab.test.tsx §"invite modal role picker" (the green contract)
    - .planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md §"Row-anatomy + 4px-grid" and §"Link-first"
    - frontend/src/components/org/SsoTab.tsx:211-215 (the org-zone micro-label style reference: `text-[11px] uppercase tracking-wide text-muted-foreground`)
  </read_first>
  <action>
    Align the dialog to the shared org-zone rhythm WITHOUT changing behavior. The role-picker chips
    (:222-244) are INTERACTIVE selectable buttons (aria-pressed / disabled Dept-admin) — do NOT swap
    them for `RoleBadge` (that's the display badge; these are controls). Only snap their spacing to the
    4px grid (`px-3 py-1` chips are already grid; align the group `gap-1.5` + label spacing to the
    shared micro-label style so the two field labels ("Email address", "Role") read on the same grid as
    the sibling tabs). Reconcile the micro-label tokens (:195, :215 use `text-[0.7rem]
    tracking-[0.06em]`) toward the org-zone convention `text-[11px] uppercase tracking-wide
    text-muted-foreground` used by the sibling surfaces (SsoTab :211-215) for family cohesion. PRESERVE
    verbatim: the link-first success panel (:146-188, incl. `data-testid="invite-link"` + Copy link),
    the ROLE_OPTIONS (Member/Org-admin enabled, Dept-admin disabled "(soon)" — D-167-03), the 422/400
    error discrimination, the reset-on-open, and the `sendInvitation` call. No server contract, no new
    role option.
  </action>
  <acceptance_criteria>
    - the role-picker stays interactive: `grep -q "aria-pressed" frontend/src/components/org/InviteMemberDialog.tsx` and Dept-admin still `disabled` (NOT converted to RoleBadge)
    - link-first preserved: `grep -q "invite-link" frontend/src/components/org/InviteMemberDialog.tsx`
    - the two field micro-labels read on the shared org-zone style (grep the reconciled label class)
    - `npx vitest run src/components/org/InvitationsTab.test.tsx` passes — the role-picker block (Member/Org-admin enabled, Dept-admin disabled) stays green
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/org/InvitationsTab.test.tsx --reporter=dot</automated>
  </verify>
  <done>The invite dialog snaps to the shared 4px grid + micro-label style; the role-picker stays interactive (not RoleBadge); link-first + role options + error handling are byte-identical.</done>
</task>

</tasks>

<verification>
Security posture: NO threat model (D-01/D-03) — visual cohesion over the already-secured 166/167
surfaces; no gate, no server contract (`sendInvitation`) touched. RED LINES as guards:

- RED LINE (D-02): `git diff --name-only` shows `frontend/src/providers/StreamsProvider.tsx` UNTOUCHED.
- RED LINE (roster invariant): OrgMembersTab still renders zero buttons (its test's `queryByRole("button") === null` stays green).
- RED LINE (D-10): the dialog's link-first `invite-link` success is unchanged.
- `cd frontend && npx tsc -b` passes.
- Differential (SEED-056): `npx vitest run src/components/org --reporter=dot` — OrgMembersTab + InvitationsTab suites green + 0 net-new failures vs baseline `6d145860`.
</verification>

<success_criteria>
The roster and invite dialog share the family's RoleBadge / StatusChip / grid; the roster stays a pure
read leaf (zero buttons); link-first + role options are byte-identical; no authz / StreamsProvider /
server contract touched.
</success_criteria>

<output>
Create `.planning/phases/177-v3-4-org-surface-polish/177-04-SUMMARY.md` when done.
</output>
