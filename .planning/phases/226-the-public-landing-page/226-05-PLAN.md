---
phase: 226-the-public-landing-page
plan: 05
type: execute
wave: 4
depends_on:
  - 226-04
files_modified:
  - docs/LANDING.md
  - scripts/check-landing-drift.cjs
  - scripts/check-deploy-drift.sh
autonomous: true
requirements:
  - D-226-05
  - SC#3
  - SC#4
  - SC#6
  - SC#7
  - SEED-241
user_setup: []

must_haves:
  truths:
    - "docs/LANDING.md documents the landing page architecture, fact mappings, B2B routing, and the landing-worthy closeout touchpoint"
    - "scripts/check-landing-drift.cjs is driven RED by a simulated source divergence, confirms exit 1, and returns to exit 0 on clean tree"
    - "scripts/check-deploy-drift.sh passes clean on phase-226"
    - "The production landing JS bundle is under 400 KB gzipped"
    - "Chrome UAT script and steps are documented for Claude reviewer post-phase signoff"
  artifacts:
    - docs/LANDING.md
  key_links:
    - "docs/LANDING.md provides the narrative and maintenance standard for future phase builders"
---

<objective>
Document the landing surface, demonstrate the drift guard firing RED, verify deployment consistency and bundle metrics, and prepare the UAT verification pack for Claude's review:
1. Write `docs/LANDING.md` describing the landing page purpose, section catalog, source-of-truth mappings, B2B routing rationale, continuous loop behavior, and the `landing-worthy?` closeout checklist.
2. Execute the mandatory RED-first validation for `scripts/check-landing-drift.cjs` (temporarily alter a source value, observe exit code 1, restore to identical state, observe exit code 0).
3. Run `bash scripts/check-deploy-drift.sh` to confirm environment variables and configuration files remain in lockstep.
4. Verify production bundle metrics (`npm run build`), confirming the landing bundle gzip size is under 400 KB.
5. Provide detailed G-4 UAT instructions for the reviewer (scene hover/playback, navigation anchors, 390px mobile viewport verification).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-the-public-landing-page/226-PROPOSAL.md
@.planning/phases/226-the-public-landing-page/226-CONTEXT.md
@.planning/seeds/SEED-241-living-landing-page.md
@frontend/src/landing/facts.ts
@scripts/check-landing-drift.cjs
@scripts/check-deploy-drift.sh
</context>

<threat_model>
ASVS L1 Threat Assessment:
- Threat: Unobserved security or configuration drift during deployment.
  Mitigation: `check-deploy-drift.sh` asserts that all new environment variables (`VITE_DEMO_URL`, `VITE_APP_URL`) are present in example files and documentation. `check-landing-drift.cjs` ensures product claims match reality.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive Documentation (docs/LANDING.md)</name>
  <files>docs/LANDING.md</files>
  <read_first>.planning/phases/226-the-public-landing-page/226-PROPOSAL.md, .planning/seeds/SEED-241-living-landing-page.md</read_first>
  <action>
    Create `docs/LANDING.md` documenting:
    1. Purpose: Public B2B marketing surface at `/` isolated from the app SPA.
    2. Architecture: Separate Vite entry (`landing.html`), zero app bundle, negative import fence.
    3. Section map: Description of all 16 sections from Hero to Footer.
    4. Living Facts & Sources of Truth: Exact mapping between `facts.ts` and code files (`MODEL_CAPABILITIES`, `acceptedFormats.ts`, `PublishGauntlet.tsx`, `_TOOL_REGISTRY`, `servicesCatalog.ts`, and tab definitions).
    5. Anti-Drift Guard: Explanation of `scripts/check-landing-drift.cjs`, CI integration, and hook execution.
    6. B2B Routing & Placeholders: `VITE_DEMO_URL` and `VITE_APP_URL` semantics.
    7. Continuous Loops: Why scenes loop continuously in production with reduced-motion freeze.
    8. Phase Closeout Touchpoint: The mandatory `landing-worthy?` question for `/gsd:complete-milestone` and phase summaries.
  </action>
  <verify>
    Verify `docs/LANDING.md` exists and markdown renders cleanly.
  </verify>
  <acceptance_criteria>
    - `docs/LANDING.md` exists and covers all specified operational topics.
  </acceptance_criteria>
  <done>Landing documentation completed.</done>
</task>

<task type="auto">
  <name>Task 2: Red-Drift Guard Demonstration and Deploy Drift Verification</name>
  <files>scripts/check-landing-drift.cjs, scripts/check-deploy-drift.sh</files>
  <read_first>scripts/check-landing-drift.cjs, scripts/check-deploy-drift.sh</read_first>
  <action>
    1. Run `node scripts/check-landing-drift.cjs` and verify clean exit 0.
    2. Demonstrate RED failure:
       - Temporarily modify a fact or source file (e.g. change a label in `facts.ts` or `_TOOL_REGISTRY`).
       - Run `node scripts/check-landing-drift.cjs` and assert exit code 1 with clear diagnostic output.
       - Revert the modification and assert exit code 0 again.
    3. Run `bash scripts/check-deploy-drift.sh` (or via Git bash) to confirm zero deployment drift.
  </action>
  <verify>
    Run drift check commands and verify exit codes (1 on simulated drift, 0 on clean tree).
  </verify>
  <acceptance_criteria>
    - Drift guard is verified to actively catch mismatches (proven RED before GREEN).
    - `check-deploy-drift.sh` exits 0.
  </acceptance_criteria>
  <done>Drift guard proven functional and deploy drift checks passing.</done>
</task>

<task type="auto">
  <name>Task 3: Production Bundle Size Verification and Reviewer UAT Pack</name>
  <files>frontend/dist/assets/landing-*.js</files>
  <read_first>frontend/vite.config.ts</read_first>
  <action>
    1. Run production build: `npm --prefix frontend run build`.
    2. Measure the gzipped size of the landing entry chunk (`dist/assets/landing-*.js` or equivalent).
    3. Confirm gzip size is strictly below 400 KB.
    4. Run full test suite for landing:
       - `npx vitest run src/landing` with `GSD_VITEST_MAX_WORKERS=2`.
    5. Document UAT verification checklist for Claude reviewer:
       - Chrome visit to `/`
       - Visual check of 3D tilt hero and 3D models orbit
       - Inspection of all 8 tour scene tabs and their playback
       - Viewport resize to 390px width checking no horizontal overflow
       - CTA links validation
  </action>
  <verify>
    Confirm build output exists, chunk size is < 400 KB gz, and vitest runs green.
  </verify>
  <acceptance_criteria>
    - Landing production bundle is < 400 KB gzipped.
    - All vitest tests in `src/landing` pass.
  </acceptance_criteria>
  <done>Production bundle verified and UAT preparation complete.</done>
</task>

</tasks>
