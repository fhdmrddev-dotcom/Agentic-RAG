---
phase: 226-the-public-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/landing/facts.ts
  - scripts/check-landing-drift.cjs
  - .claude/hooks/landing-drift-guard.js
  - .claude/settings.json
  - frontend/src/landing/__tests__/facts.test.ts
autonomous: true
requirements:
  - D-226-04
  - D-226-05
  - D-226-06
  - SC#3
  - SEED-241
user_setup: []

must_haves:
  truths:
    - "frontend/src/landing/facts.ts is the typed single source of truth for all quantitative claims, tabs, lists, and quotes"
    - "scripts/check-landing-drift.cjs extracts ground truth directly from backend and frontend sources without hardcoded workarounds"
    - "scripts/check-landing-drift.cjs uses house exit codes: 0 clear, 1 drift, 2 harness error (e.g. missing source file)"
    - "scripts/check-landing-drift.cjs verifies verbatim quotes against stepIdentityVocabulary.ts, doorVocabulary.ts, and PublishGauntlet.tsx"
    - ".claude/hooks/landing-drift-guard.js is registered in .claude/settings.json under PostToolUse, silent when clean"
    - "frontend/src/landing/__tests__/facts.test.ts validates the structure of facts.ts and scans JSX text nodes for un-facted claim numbers"
  artifacts:
    - frontend/src/landing/facts.ts
    - scripts/check-landing-drift.cjs
    - .claude/hooks/landing-drift-guard.js
    - frontend/src/landing/__tests__/facts.test.ts
  key_links:
    - "check-landing-drift.cjs prevents marketing rot by binding facts.ts directly to authoritative source files"
---

<objective>
Implement the living claims mechanism (SEED-241) aligned with authoritative code measured at HEAD (F-2, F-5):
1. Author `frontend/src/landing/facts.ts` exporting strongly typed data structures:
   - `providers`: 8 model providers from `MODEL_CAPABILITIES` (`openai`, `anthropic`, `google`, `deepseek`, `moonshot`, `minimax`, `zhipu`, `openrouter`).
   - `localRuntimes`: 2 local runtimes (`ollama`, `lmstudio`) from `_PROVIDER_BASE_URLS` minus the 8 model providers.
   - `toolCount`: 29 built-in tools from `_TOOL_REGISTRY` in `tool_dispatcher.py`.
   - `ingestFormats`: 11 formats (8 dropzone formats from `acceptedFormats.ts` + 3 server-side override extensions `.msg`, `.eml`, `.dxf` from `documents.py`).
   - `connectorCatalog`: 13 catalog entries from `CATALOG_SERVICES` in `servicesCatalog.ts` (including `smtp` and `custom_mcp`).
   - `gauntletStages`: 10 stages from `PublishGauntlet.tsx` `STAGES`.
   - `surfaceTabs`: Derived tabs for Library, Settings (admin view, noting dynamic retrieval label), Control Room, and Org Admin.
   - `verbatimQuotes`: 6 verified quotes from `stepIdentityVocabulary.ts`, `doorVocabulary.ts`, and `PublishGauntlet.tsx`.
   - `heroFacts`: Derived claims (10 publish checks, 29 built-in tools, 8 model providers + 2 local runtimes, 100% citation-gated workflows).
2. Build `scripts/check-landing-drift.cjs` extracting facts directly from the authoritative files, with house exit codes (0 clear, 1 drift, 2 harness error) and zero hardcoded provider patches.
3. Register `.claude/hooks/landing-drift-guard.js` in `.claude/settings.json` under `hooks.PostToolUse`, running silently on exit 0.
4. Author `frontend/src/landing/__tests__/facts.test.ts` scanning JSX text nodes for un-facted claim numbers.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-the-public-landing-page/226-PROPOSAL.md
@.planning/phases/226-the-public-landing-page/226-PREFLIGHT.md
@.planning/phases/226-the-public-landing-page/226-CONTEXT.md
@.planning/seeds/SEED-241-living-landing-page.md
@backend/app/config.py
@backend/app/services/tool_dispatcher.py
@frontend/src/components/ingestion/acceptedFormats.ts
@frontend/src/components/workflows/PublishGauntlet.tsx
@frontend/src/components/settings/servicesCatalog.ts
@frontend/src/pages/LibraryPage.tsx
@frontend/src/pages/SettingsPage.tsx
@frontend/src/components/admin/ControlRoomPage.tsx
@frontend/src/components/org/OrgAdminShell.tsx
@frontend/src/components/workflows/stepIdentityVocabulary.ts
@frontend/src/components/workflows/doorVocabulary.ts
</context>

<threat_model>
ASVS L1 Threat Assessment:
- Threat: False marketing claims or compliance misrepresentation (e.g. claiming security or architectural features that do not match current backend/frontend capability).
  Mitigation: Automated static drift guard (`check-landing-drift.cjs`) directly parses AST / regex patterns of authoritative code and halts CI/tool hooks if `facts.ts` diverges. Missing source files exit with code 2 (harness error), never masked as pass.
- Threat: Tampering with drift verification.
  Mitigation: Test suite verifies the drift script against both valid and invalid states, ensuring failure on mismatch.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author Typed Fact Manifest (facts.ts)</name>
  <files>frontend/src/landing/facts.ts</files>
  <read_first>backend/app/config.py, frontend/src/components/ingestion/acceptedFormats.ts, frontend/src/components/workflows/PublishGauntlet.tsx, backend/app/services/tool_dispatcher.py, frontend/src/components/settings/servicesCatalog.ts, frontend/src/components/workflows/stepIdentityVocabulary.ts, frontend/src/components/workflows/doorVocabulary.ts</read_first>
  <action>
    Create `frontend/src/landing/facts.ts` exporting strongly typed, frozen data structures matching HEAD:
    1. `providers`: 8 model providers extracted from `MODEL_CAPABILITIES` in `backend/app/config.py`:
       `openai`, `anthropic`, `google`, `deepseek`, `moonshot`, `minimax`, `zhipu`, `openrouter`.
    2. `localRuntimes`: 2 local runtime backends: `ollama`, `lmstudio` (derived from `_PROVIDER_BASE_URLS` minus the 8 model providers).
    3. `ingestFormats`: 11 formats: 8 dropzone formats from `ACCEPTED_FORMATS.displayLabels` (`PDF`, `DOCX`, `PPTX`, `XLSX`, `CSV`, `TXT`, `MD`, `EPUB`) + 3 server-side override extensions (`MSG`, `EML`, `DXF` from `backend/app/api/documents.py:128-130`).
    4. `extractCapabilities`: `["Text", "Tables → data", "Figures & images", "Metadata + confidence", "Email headers", "Attachments", "Relationships"]`.
    5. `produceFormats`: `["XLSX", "DOCX", "PPTX", "PDF", "CSV", "PNG charts", "Markdown", "HTML", "Filled templates"]`.
    6. `gauntletStages`: 10 items in exact pipeline order matching `PublishGauntlet.tsx:196-207` `STAGES`:
       Owner, Valid, Goal, Structure, Pause, Grounding, Golden run, Citations, Judge, Commit.
    7. `toolCount`: 29 built-in tools from `_TOOL_REGISTRY` in `tool_dispatcher.py`.
    8. `connectorCatalog`: 13 catalog entries matching `CATALOG_SERVICES` in `servicesCatalog.ts` (slack, github, notion, google, microsoft, jira, smtp, custom_mcp, figma, linear, sentry, intercom, miro). Google entry notes supported apps: Gmail, Calendar, Drive.
    9. `surfaceTabs`:
       - `library`: 5 tabs (`Documents`, `Views`, `Ingestion`, `Indexing`, `Health`).
       - `settings`: 5 tabs (`AI Model`, `Search & Retrieval`, `Integrations`, `Memory`, `Audit Log` — noting retrieval label is dynamic).
       - `controlRoom`: 5 tabs (`Control Plane`, `Users & Access`, `Model Registry`, `Secrets`, `Audit log`).
       - `orgAdmin`: 7 tabs (`Members`, `Audit`, `Settings`, `Invitations & Roles`, `SSO`, `Subscription`, `Retention`).
    10. `verbatimQuotes`: 6 verified product quotes:
       - `"Paused — waiting for you."` (`stepIdentityVocabulary.ts:97` `ASK_PAUSED`)
       - `"Nothing has been sent yet."` (`stepIdentityVocabulary.ts`)
       - `"Describe the recurring work in plain language. The AI writes the steps, sets how strict it is, and asks you about anything it had to guess."` (`doorVocabulary.ts`)
       - `"Golden run — a REAL harness run against the project KB."` (`PublishGauntlet.tsx:203`)
       - `"Independent judge — an independent model grades the deliverable."` (`PublishGauntlet.tsx:205`)
       - `"Failure reason not captured by the backend — surfaced explicitly so the run is never shown as an empty success."` (`stepIdentityVocabulary.ts`)
    11. `heroFacts`:
       - 10 publish checks (derived from `gauntletStages.length`)
       - 29 built-in tools (derived from `toolCount`)
       - 8 model providers + 2 local runtimes (derived from `providers.length` and `localRuntimes.length`)
       - 100% citation-gated workflows (derived from `gauntletStages` `structural_gate` rule)
    12. `comparisonData`: Category comparison (8 rows) and full comparison (30 rows, 5 columns: Agentic RAG, General AI chat assistant, Enterprise search / knowledge tool, Workflow automation platform, In-house build) scored with full/partial/none.
  </action>
  <verify>
    Run `npx tsc -p frontend/tsconfig.app.json` and ensure no type errors.
  </verify>
  <acceptance_criteria>
    - `frontend/src/landing/facts.ts` exists and reflects measured facts at HEAD.
    - Exported arrays and objects use `readonly` and `as const`.
  </acceptance_criteria>
  <done>Typed fact manifest created representing single source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Build Drift Guard Script and Register Claude Hook</name>
  <files>scripts/check-landing-drift.cjs, .claude/hooks/landing-drift-guard.js, .claude/settings.json</files>
  <read_first>frontend/src/landing/facts.ts, backend/app/config.py, frontend/src/components/ingestion/acceptedFormats.ts, frontend/src/components/workflows/PublishGauntlet.tsx, backend/app/services/tool_dispatcher.py, frontend/src/components/settings/servicesCatalog.ts, .claude/settings.json</read_first>
  <action>
    1. Create `scripts/check-landing-drift.cjs`:
       - Use Node `fs` and `path` to load files relative to repo root.
       - If any source file does not exist, print error and exit with code 2 (`harness error`).
       - Parse `backend/app/config.py`:
         - Extract unique `"provider": "..."` values from `MODEL_CAPABILITIES` (asserts 8 providers: openai, anthropic, google, deepseek, moonshot, minimax, zhipu, openrouter). Compare against `facts.ts` `providers`.
         - Extract `_PROVIDER_BASE_URLS` keys minus the 8 providers (asserts 2 local runtimes: ollama, lmstudio). Compare against `facts.ts` `localRuntimes`.
       - Parse `frontend/src/components/ingestion/acceptedFormats.ts`:
         - Extract `displayLabels`. Combine with `.msg`, `.eml`, `.dxf` server overrides from `documents.py`. Compare against `facts.ts` `ingestFormats`.
       - Parse `frontend/src/components/workflows/PublishGauntlet.tsx`:
         - Extract `STAGES` labels and count (10). Compare against `facts.ts` `gauntletStages`.
       - Parse `backend/app/services/tool_dispatcher.py`:
         - Extract keys of `_TOOL_REGISTRY` (29). Compare count against `facts.ts` `toolCount`.
       - Parse `frontend/src/components/settings/servicesCatalog.ts`:
         - Extract service IDs in `CATALOG_SERVICES` (13). Compare against `facts.ts` `connectorCatalog`.
       - Parse tabs from `LibraryPage.tsx`, `SettingsPage.tsx`, `ControlRoomPage.tsx`, `OrgAdminShell.tsx`. Compare against `facts.ts` `surfaceTabs`.
       - Assert all `verbatimQuotes` appear verbatim in their respective source files (`stepIdentityVocabulary.ts`, `doorVocabulary.ts`, `PublishGauntlet.tsx`).
       - If any mismatch: print detailed diagnostic table showing expected vs actual, and exit with code 1 (`drift`).
       - If all match: print `✓ Landing facts match application code (zero drift)` and exit with code 0 (`clear`).
    2. Create `.claude/hooks/landing-drift-guard.js`:
       - Run `node scripts/check-landing-drift.cjs`.
       - If exit code is 0, stay completely silent (no stdout).
       - If exit code is non-zero, pass through error output and exit with that code.
    3. In `.claude/settings.json`:
       - Register the new hook in `hooks.PostToolUse` following the pattern of entry 4 (`claude-md-size-guard.js`):
         `{ "matcher": "Write|Edit", "command": "node .claude/hooks/landing-drift-guard.js", "timeout": 5 }`.
  </action>
  <verify>
    Run `node scripts/check-landing-drift.cjs` from workspace root and verify exit code 0.
    Run `node .claude/hooks/landing-drift-guard.js` and verify it is completely silent and exits 0.
  </verify>
  <acceptance_criteria>
    - `scripts/check-landing-drift.cjs` exits 0 on matching facts and 2 if a file is missing.
    - Hook is registered in `.claude/settings.json` and silent on green.
  </acceptance_criteria>
  <done>Drift guard and silent post-tool-use hook operational.</done>
</task>

<task type="auto">
  <name>Task 3: Facts Validation and JSX Text-Node Fence Test</name>
  <files>frontend/src/landing/__tests__/facts.test.ts</files>
  <read_first>frontend/src/landing/facts.ts</read_first>
  <action>
    Create `frontend/src/landing/__tests__/facts.test.ts`:
    1. Assert that `facts.ts` contains exactly 8 providers, 2 local runtimes, 10 gauntlet stages, 29 tools, 13 connector catalog items, 11 ingest formats.
    2. Assert that each verbatim quote matches its source file.
    3. Add a JSX text-node scanner test (Advisory A-2):
       - Recursively read all `.tsx` files in `frontend/src/landing/components/` and `scenes/`.
       - Extract text nodes appearing between `>` and `<` (excluding comments and JSX tags).
       - Test that no hardcoded product claim numbers (such as `8`, `9`, `10`, `11`, `13`, `29`) appear as bare literal text nodes in the UI.
       - Require all numbers in UI copy to be imported from `facts.ts`.
  </action>
  <verify>
    Run `npx vitest run src/landing/__tests__/facts.test.ts` with `GSD_VITEST_MAX_WORKERS=2` and verify it passes.
  </verify>
  <acceptance_criteria>
    - `facts.test.ts` verifies all counts and guards against un-facted numerals in landing JSX text nodes without false positives on SVG viewBox or CSS.
  </acceptance_criteria>
  <done>Facts suite established and enforcing that JSX renders claims from facts.ts only.</done>
</task>

</tasks>
