---
phase: 226-the-public-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/landing/facts.ts
  - scripts/check-landing-drift.cjs
  - frontend/src/landing/__tests__/facts.test.ts
  - .claude/hooks/post-tool-use.sh
autonomous: true
requirements:
  - D-226-04
  - D-226-05
  - D-226-06
  - SC#3
  - SEED-241
user_setup: []

must_haves:
  truths:
    - "frontend/src/landing/facts.ts is the typed single source of truth for all quantitative claims and lists on the landing page"
    - "scripts/check-landing-drift.cjs parses application source code (backend and frontend) and asserts exact equality with facts.ts"
    - "scripts/check-landing-drift.cjs exits 0 on matching facts and exits 1 with detailed failure diffs on any divergence"
    - "frontend/src/landing/__tests__/facts.test.ts validates the structure of facts.ts and verifies that no un-facted numerals appear in landing JSX"
    - "The drift guard is integrated into the post-tool-use hook pipeline"
  artifacts:
    - frontend/src/landing/facts.ts
    - scripts/check-landing-drift.cjs
    - frontend/src/landing/__tests__/facts.test.ts
  key_links:
    - "check-landing-drift.cjs prevents marketing rot by binding facts.ts directly to authoritative source files"
---

<objective>
Implement the living claims mechanism (SEED-241):
1. Author `frontend/src/landing/facts.ts` exporting fully typed data structures for all facts, numbers, lists, tabs, and quotes.
2. Build `scripts/check-landing-drift.cjs` to extract ground truth from source files (`MODEL_CAPABILITIES`, `acceptedFormats.ts`, `PublishGauntlet.tsx`, `_TOOL_REGISTRY`, `servicesCatalog.ts`, and tab definitions) and compare against `facts.ts`.
3. Author `frontend/src/landing/__tests__/facts.test.ts` to assert fact integrity and scan landing JSX components for prohibited literal magic numbers.
4. Integrate `check-landing-drift.cjs` into the developer hook pipeline so any source edit immediately flags drift.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-the-public-landing-page/226-PROPOSAL.md
@.planning/phases/226-the-public-landing-page/226-CONTEXT.md
@.planning/seeds/SEED-241-living-landing-page.md
@backend/app/config.py
@backend/app/services/tool_dispatcher.py
@frontend/src/components/ingestion/acceptedFormats.ts
@frontend/src/components/workflows/PublishGauntlet.tsx
@frontend/src/components/settings/servicesCatalog.ts
@frontend/src/pages/LibraryPage.tsx
@frontend/src/pages/SettingsPage.tsx
@frontend/src/components/admin/ControlRoomPage.tsx
@frontend/src/components/org/OrgAdminShell.tsx
</context>

<threat_model>
ASVS L1 Threat Assessment:
- Threat: False marketing claims or compliance misrepresentation (e.g. claiming security or architectural features that do not match current backend/frontend capability).
  Mitigation: Automated static drift guard (`check-landing-drift.cjs`) directly parses AST / regex patterns of authoritative code and halts CI/tool hooks if `facts.ts` is not updated synchronously.
- Threat: Tampering with drift verification.
  Mitigation: Test suite verifies the drift script against both valid and invalid states, ensuring failure on mismatch.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author Typed Fact Manifest (facts.ts)</name>
  <files>frontend/src/landing/facts.ts</files>
  <read_first>backend/app/config.py, frontend/src/components/ingestion/acceptedFormats.ts, frontend/src/components/workflows/PublishGauntlet.tsx, backend/app/services/tool_dispatcher.py, frontend/src/components/settings/servicesCatalog.ts</read_first>
  <action>
    Create `frontend/src/landing/facts.ts` exporting strongly typed, frozen data structures:
    1. `providers`: 9 items (`openai`, `anthropic`, `google`, `deepseek`, `moonshot`, `minimax`, `zhipu`, `openrouter`, `lmstudio`) with display labels, logo symbol/component keys, and capability summary.
    2. `ingestFormats`: 8 dropzone formats (`PDF`, `DOCX`, `PPTX`, `XLSX`, `CSV`, `TXT`, `MD`, `EPUB`) + 3 server-side (`MSG`, `EML`, `DXF`). Total = 11 formats.
    3. `extractCapabilities`: `["Text", "Tables → data", "Figures & images", "Metadata + confidence", "Email headers", "Attachments", "Relationships"]`.
    4. `produceFormats`: `["XLSX", "DOCX", "PPTX", "PDF", "CSV", "PNG charts", "Markdown", "HTML", "Filled templates"]`.
    5. `gauntletStages`: 10 items in exact pipeline order:
       - 1. Owner ("Owner check — RLS-resolve + you own it")
       - 2. Valid ("Definition valid — re-validates as a WorkflowDefinition")
       - 3. Goal ("business_requirement — exactly one must be declared")
       - 4. Structure ("Structural lint — reachable · terminal · inputs satisfied · no orphans")
       - 5. Pause ("Interactive-phase check — human-pause phases can't validate synchronously")
       - 6. Grounding ("Grounding check — the folders, tools and skills this workflow points at must all resolve")
       - 7. Golden run ("Golden run — a REAL harness run against the project KB")
       - 8. Citations ("Structural gate — citations / integrity checked during the run")
       - 9. Judge ("Independent judge — an independent model grades the deliverable")
       - 10. Commit ("Publish commit — the draft must not have changed while we were checking it")
    6. `toolCount`: 29, with category breakdown (KB navigation, document retrieval, memory, skills, workspace file ops, interactive tasks, templates, graph queries).
    7. `connectorCatalog`: 15 entries (Google Workspace, Gmail, Google Calendar, Google Drive, Slack, GitHub, Notion, Microsoft 365, Jira, Figma, Linear, Sentry, Intercom, Miro, Custom MCP) with descriptions, mark keys, and category tags.
    8. `surfaceTabs`:
       - `library`: `["Documents", "Views", "Ingestion", "Indexing", "Health"]` (5)
       - `settings`: `["AI Model", "Knowledge & Retrieval", "Integrations", "Memory", "Audit Log"]` (5)
       - `controlRoom`: `["Control Plane", "Users & Access", "Model Registry", "Secrets", "Audit log"]` (5)
       - `orgAdmin`: `["Members", "Audit", "Settings", "Invitations & Roles", "SSO"]` (5)
    9. `verbatimQuotes`: 6 product quotes:
       - "Paused — waiting for you." / "Nothing has been sent yet." (Approval card)
       - "Describe the recurring work in plain language. The AI writes the steps, sets how strict it is, and asks you about anything it had to guess." (Describe door)
       - "Golden run — a REAL harness run against the project KB." / "Independent judge — an independent model grades the deliverable." (Publish gauntlet)
       - "Only what you tick can appear in the draft." (Workflow services)
       - "Failure reason not captured by the backend — surfaced explicitly so the run is never shown as an empty success." (Run step failure)
       - "Shown here only. The record keeps what ran, not what it said." (Run record privacy)
    10. `comparisonData`: Category comparison (8 rows) and full comparison (30 rows, 5 columns: Agentic RAG, General AI chat assistant, Enterprise search / knowledge tool, Workflow automation platform, In-house build) scored with full/partial/none.
    11. `heroFacts`: 4 headline metrics derived from counts (100% cited, 10 publish checks, 29 tools & actions, 9 model providers).
  </action>
  <verify>
    Run `npx tsc -p frontend/tsconfig.app.json` and ensure no type errors.
  </verify>
  <acceptance_criteria>
    - `frontend/src/landing/facts.ts` exists and exports all specified typed fact objects.
    - Types are strictly defined with `as const` / `readonly`.
  </acceptance_criteria>
  <done>Typed fact manifest created representing single source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Build Drift Guard Script (scripts/check-landing-drift.cjs)</name>
  <files>scripts/check-landing-drift.cjs, .claude/hooks/post-tool-use.sh</files>
  <read_first>frontend/src/landing/facts.ts, backend/app/config.py, frontend/src/components/ingestion/acceptedFormats.ts, frontend/src/components/workflows/PublishGauntlet.tsx, backend/app/services/tool_dispatcher.py, frontend/src/components/settings/servicesCatalog.ts</read_first>
  <action>
    1. Create `scripts/check-landing-drift.cjs`:
       - Read and parse `backend/app/config.py`: extract all unique `provider` values in `MODEL_CAPABILITIES` + add `lmstudio`. Compare against `facts.ts` `providers`.
       - Read and parse `frontend/src/components/ingestion/acceptedFormats.ts`: extract `ACCEPTED_FORMATS.displayLabels` + check server-side `.msg`, `.eml`, `.dxf` in `backend/app/api/documents.py`. Compare against `facts.ts` `ingestFormats`.
       - Read and parse `frontend/src/components/workflows/PublishGauntlet.tsx`: extract `STAGES` length and labels. Compare against `facts.ts` `gauntletStages`.
       - Read and parse `backend/app/services/tool_dispatcher.py`: extract keys of `_TOOL_REGISTRY`. Compare count against `facts.ts` `toolCount`.
       - Read and parse `frontend/src/components/settings/servicesCatalog.ts`: check catalog entries against `facts.ts` `connectorCatalog`.
       - Read and parse tabs in `LibraryPage.tsx`, `SettingsPage.tsx`, `ControlRoomPage.tsx`, and `OrgAdminShell.tsx`. Compare against `facts.ts` `surfaceTabs`.
       - If any divergence is detected: print clear red diagnostic table showing expected vs actual, and exit with code 1.
       - If all match: print green confirmation `✓ Landing facts match application code (zero drift)` and exit 0.
    2. In `.claude/hooks/post-tool-use.sh` (if present or applicable):
       - Wire `node scripts/check-landing-drift.cjs` into the verification checks.
  </action>
  <verify>
    Run `node scripts/check-landing-drift.cjs` from the workspace root and verify it exits 0.
  </verify>
  <acceptance_criteria>
    - `scripts/check-landing-drift.cjs` exits 0 when `facts.ts` matches codebase.
    - Script reports specific mismatches when files differ.
  </acceptance_criteria>
  <done>Drift guard script implemented and verified against codebase.</done>
</task>

<task type="auto">
  <name>Task 3: Facts Validation and Literal-Number Fence Test</name>
  <files>frontend/src/landing/__tests__/facts.test.ts</files>
  <read_first>frontend/src/landing/facts.ts</read_first>
  <action>
    Create `frontend/src/landing/__tests__/facts.test.ts`:
    1. Assert that `facts.ts` contains exactly 9 providers, 10 gauntlet stages, 29 tools, 15 connector catalog items, 11 ingest formats.
    2. Add a JSX scanner test:
       - Recursively read all `.tsx` files in `frontend/src/landing/` (excluding `facts.ts` and test files).
       - Regex check for bare hardcoded product claim numbers outside quotes/props (e.g. `\b(9|10|29|30|15)\b` as raw text nodes).
       - Require all numbers in UI copy to be imported from `facts.ts`.
  </action>
  <verify>
    Run `npx vitest run src/landing/__tests__/facts.test.ts` with `GSD_VITEST_MAX_WORKERS=2` and verify it passes.
  </verify>
  <acceptance_criteria>
    - `facts.test.ts` verifies all counts and guards against un-facted numerals in landing JSX.
  </acceptance_criteria>
  <done>Facts suite established and enforcing that JSX renders claims from facts.ts only.</done>
</task>

</tasks>
