---
context: phase
phase: 226-the-public-landing-page
task: 0
total_tasks: 0
status: paused_before_planning
last_updated: 2026-09-02T20:45:45.800Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Non-greedy regex across repeated blocks | A `<div class="ucviz">[\s\S]*?<h3>Team</h3>` swap matched from the FIRST strip to each later heading and deleted five of six business-case cards; shipped once before the operator noticed ("I only see IT & data") | advisory | Replace repeated blocks by index (`lastIndexOf` from the anchor) or with a bounded lookahead; count the blocks before and after and refuse to save on a decrease |
| Verifying by intuition instead of looking | Three rounds of overlap fixes were reasoned about; one Chrome screenshot showed the real cause (scene rendered in the narrow column) | advisory | For any visual claim, drive it in the browser before handing over; the design skill permits looking at your own seeded canvas |
| Claims copied instead of derived | "Eight-stage gauntlet" was written from memory; `PublishGauntlet.tsx` has TEN | advisory | Every number on the landing reads `facts.ts`; `check-landing-drift.cjs` ships in the same phase (D-226-04/05) |

<current_state>
Phase 226 is PROPOSED and registered, not planned. All design work is finished and approved by the operator on the canvas. The operator directed: build after Phase 224 / 225 close; Gemini plans + executes, Claude pre-flights and reviews. Nothing in 226 has code yet.
</current_state>

<completed_work>

- Design canvas *Agentic RAG Landing* — six iteration rounds with the operator; final structure: hero (3D frame, mouse tilt) · facts · how it works · 12 features · Tour (8 tabs, 8 scenario scenes) · Files · 6 illustrated business cases · verbatim quotes · Studio + gauntlet · 2 category-level comparisons · Models ring (real logos) · Works with (15 logos) · Security · CTA (Book a demo / Sign in).
- Canvas source committed at `.planning/design/landing-canvas/`.
- `SEED-241` written and folded into 226. `226-PROPOSAL.md` written (8 fixed decisions, fact→source table, success + G-6 criteria, out-of-scope, deploy half).
- ROADMAP.md phase table / detail / progress rows; STATE.md queued note; `BUS-068` to Gemini. Commit `f58d2a345` on `develop`.
</completed_work>

<remaining_work>

- Operator closes 224 / 225 (their working-tree changes to ChatArea / MessageItem / ChatToolApprovalCard / threads.ts / types are NOT this session's — leave them).
- Gemini: `/gsd:plan-phase 226` from the proposal.
- Claude: `226-PREFLIGHT.md` before wave 1 (see Pre-Execution Critique below), then post-review + G-4 UAT.
- Operator: choose `VITE_DEMO_URL` and `VITE_APP_URL`; say which catalog apps should read "coming".
- After milestone close: operator-triggered promotion; no migrations.
</remaining_work>

<decisions_made>

- Separate Vite entry, zero app bundle (D-226-01) — a public page must not mount auth or the API client.
- B2B: Book a demo primary, Sign in quiet (D-226-03) — operator: not self-serve.
- `facts.ts` + drift guard in the same phase (D-226-04/05) — hand-edited claims rot; proven during design.
- Category-level comparisons only (D-226-06); icons from the packages (D-226-08); scenes as components running continuous loops in the build (D-226-07).
</decisions_made>

<blockers>
- Sequencing only: 226 waits for 224 / 225 by operator direction.
</blockers>

## Required Reading (in order)
1. `.planning/phases/226-the-public-landing-page/226-PROPOSAL.md` — the brief; decisions are fixed, do not re-open at plan time.
2. `.planning/seeds/SEED-241-living-landing-page.md` — the mechanism the phase must ship.
3. `.planning/design/landing-canvas/README.md` then `Main.dc.html` — the acceptance bar (the live canvas: https://claude.ai/code/artifact/d33a1829-8e5c-498a-8f65-dcbda8cbc448).
4. `feedback_gemini_plans_claude_reviews_pipeline` (memory) — pre-flight audits cross-plan SEAMS; the last two escapes were seams.
5. `.planning/METHODOLOGY.md` (if it exists).

## Critical Anti-Patterns (do NOT repeat these)
- Bulk regex edits over repeated markup → index-based replacement + count check.
- Visual claims without a screenshot → drive it in Chrome first.
- Numbers from memory → `facts.ts` only.

## Infrastructure State
- Canvas artifact published and watched; saving enabled (`self` + `downloads`). Preview loads slowly (20–60 s) — that is the platform, not the design.
- Chrome MCP: extension connects; clicks land ~17 px left of screenshot x in the canvas editor; page ignores input for ~30 s after first paint.
- No local services touched this session.

## Pre-Execution Critique Required
- Design artifact: the canvas + `226-PROPOSAL.md`.
- Critique focus for `226-PREFLIGHT.md`: (1) the import fence — landing chunk must not contain `@/lib/api*`, providers, `@supabase/*`; (2) D-226-02 — how `ChatLayout` treats the bare root for a logged-in user BEFORE the Vercel rewrite is chosen; (3) the drift guard must be driven RED in the plan, not merely written; (4) env vars land in `deploy/onebox.env.example` + `check-deploy-drift.sh` in the same commit; (5) scenes as components, not PNGs.
- Gate: do NOT begin execution until the pre-flight is written and any gap is folded back into the plans.

<context>
The operator wanted a landing page that shows the whole product, is honest, and stays current. The design is done; the build is deliberately deferred behind the current phases. The proposal encodes everything that took two days to settle so the build does not re-litigate it.
</context>

<next_action>
Start with: after 224/225 close, Gemini runs `/gsd:plan-phase 226`; Claude reads the plans and writes `226-PREFLIGHT.md` before wave 1.
</next_action>
