---
id: 067.4-05
plan_id: 067.4-05
phase: 067.4
plan: 05
type: execute
wave: 3
depends_on:
  - 067.4-01
  - 067.4-02
  - 067.4-03
  - 067.4-04
files_modified:
  - .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md
  - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md
  - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md
  - .planning/STATE.md
autonomous: false
requirements:
  - STREAM-04-correctness-round3
requirements_addressed:
  - STREAM-04-correctness-round3
must_haves:
  truths:
    - "067.4-HUMAN-UAT.md scoreboard exists with rows for R-3, R-4, R-5, NR-067.2-11, NR-067.2-12, plus 067.2-6 mirror, plus 067.3-NR no-regression spot-checks (R-1, R-2, R-2-IDOR, 067.2-3/4/5, N-01)."
    - "Claude drives Chrome MCP authenticated session (`fhdmrd@gmail.com` / `123456` at http://localhost:5173/) per D-067.4-WAVE-02; user reviews scoreboard and replies `approved` or `gaps`."
    - "If 12/12 GREEN: mirror back into 067.2-HUMAN-UAT.md (Rows 6, 11, 12 flipped) AND 067.3-HUMAN-UAT.md (Rows R-3, 067.2-6 mirror flipped) AND flip frontmatter `status: closed` on ALL THREE phase UAT files. STATE.md gets 3 new entries in the `Recent Completed Phases` section in ONE commit (per D-067.4-CLOSURE-01)."
    - "If any non-`n/a`-eligible row RED: append `## Phase 067.4 verdict — N/M GREEN (gate not met)` sections to all three UAT files; STATE.md status reflects actual outcome; escalate to Phase 067.5."
    - "Plan 05 UAT MUST execute the R-1 cross-thread switch protocol (Bread Thread A + Physicists Thread B mid-stream switch) AS WELL AS the R-4 active-thread tool-stage protocol (per D-067.4-QUALITY-01) — both pass OR neither closes."
  artifacts:
    - path: ".planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md"
      provides: "Scoreboard with 12 rows + frontmatter (gate_met, green_count, red_count, deferred_count, escalation)"
      contains: "STREAM-04-correctness-round3"
    - path: ".planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md"
      provides: "Cross-phase mirror — Phase 067.4 verdict section appended; Rows 6/11/12 checkboxes flipped (if gate met); frontmatter status flipped to closed (if gate met)"
      contains: "Phase 067.4 verdict"
    - path: ".planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md"
      provides: "Cross-phase mirror — Phase 067.4 verdict section appended; R-3 / 067.2-6 mirror checkboxes flipped (if gate met); frontmatter status flipped to closed (if gate met)"
      contains: "Phase 067.4 verdict"
    - path: ".planning/STATE.md"
      provides: "3 new entries in `Recent Completed Phases` section (067.2, 067.3, 067.4) — single commit"
      contains: "Phase 067.4: Streaming Suggestions, Tool-Stage Render & Code-Execution Stream (Complete"
  key_links:
    - from: "067.4-HUMAN-UAT.md scoreboard"
      to: "067.2-HUMAN-UAT.md + 067.3-HUMAN-UAT.md mirror sections"
      via: "Plan 05 Task 4 (mirror) + Task 5 (STATE.md)"
      pattern: "Phase 067\\.4 verdict"
    - from: "Chrome MCP authenticated browser session"
      to: "067.4-HUMAN-UAT.md evidence rows"
      via: "Plan 05 Task 3 (drive UAT, capture run_ids + screenshots)"
      pattern: "fhdmrd@gmail.com"
---

<objective>
Plan 05 closing UAT: close all three open phases (067.2, 067.3, 067.4) when the strict 12/12 GREEN gate is met (per D-067.4-CLOSURE-01).

**Mode:** `autonomous: false` — Claude-drives-Chrome-MCP, user-confirms (per D-067.4-WAVE-02). Mirrors the 067.3 Plan 05 protocol. After Claude drives all rows and posts the scoreboard, the user reviews and replies `approved` (12/12 GREEN → close all three phases) or `gaps` (any RED → escalate to 067.5).

**Five tasks (with one human-verify checkpoint between Task 2 author and Task 3 execute):**

1. **Task 1 (auto)** — Author `067.4-HUMAN-UAT.md` with frontmatter + 12-row scoreboard schema mirroring 067.3-HUMAN-UAT.md format. Rows: R-3 + 067.2-6 mirror, R-4, R-5, NR-067.2-11, NR-067.2-12, R-1 cross-thread protection (regression spot-check), R-2 sandbox download (regression spot-check), R-2-IDOR (regression spot-check), 067.2-5 re-sign architecture (regression spot-check), N-01 router (regression spot-check), 067.3-NR no-regression suite (Bread + Physicists + pgvector + Photosynthesis), and the multi-step pipeline cross-provider re-verification of 067.2 row 9 symmetric evidence. (Final row count: 12 user-actionable checkboxes per D-067.4-CLOSURE-01.)

2. **Task 2 (auto, prep)** — Pre-flight: read `067.4-04-PROTOCOL.md` for OR_MODEL_A + OR_MODEL_B + key presence verdict. Verify backend dev server + frontend dev server are running.

3. **Task 3 (checkpoint:human-verify)** — Drive UAT via Chrome MCP. Each row gets exact reproduction steps (no DOM-observer proxies — lived experience required per D-067.4-QUALITY-01). Capture run_ids, screenshots, and SSE replay assertions for each row. Spot-check R-1 (cross-thread switch) AS WELL AS R-4 (active-thread tool-stage) — both pass OR neither closes. After all 12 rows have proposed verdicts, present the scoreboard to the user via the checkpoint and wait for `approved` (12/12 GREEN) or `gaps` (any RED).

4. **Task 4 (auto, gate-conditional)** — Cross-phase mirror.
   - **GREEN PATH (gate met):** flip checkboxes in 067.2-HUMAN-UAT.md (Rows 6, 11, 12) and 067.3-HUMAN-UAT.md (Rows R-3, 067.2-6 mirror) from `[ ]` to `[x]`; update each phase's UAT frontmatter `status:` to `closed`; append a `## Phase 067.4 verdict — 12/12 GREEN (gate met)` section to both 067.2 and 067.3 UAT files.
   - **RED PATH (any non-`n/a`-eligible row RED):** append `## Phase 067.4 verdict — N/M GREEN (gate not met)` sections to all three UAT files; do NOT flip checkboxes; do NOT change frontmatter `status:`. Capture gap details for Phase 067.5 escalation.

5. **Task 5 (auto, gate-conditional)** — STATE.md update.
   - **GREEN PATH:** add three new entries to the `Recent Completed Phases` section (Phase 067.2, Phase 067.3, Phase 067.4) in YYYY-MM-DD format. Single commit ties all three.
   - **RED PATH:** STATE.md status reflects gap-blocking outcome; escalation Phase 067.5 noted; `Recent Completed Phases` does NOT yet include 067.2/067.3/067.4.

Purpose: Cross-phase closure of three open phases via single user-confirmed UAT. The strict 12/12 GREEN gate enforces D-067.4-QUALITY-01 (quality > closure speed; no compromise on shipped functionality).

Output: closed phase artifacts (3 UAT files + STATE.md) OR gap-escalation artifacts (3 UAT verdict sections + Phase 067.5 escalation note in STATE.md).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-CONTEXT.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-PATTERNS.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-VALIDATION.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-01-SUMMARY.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-02-SUMMARY.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-03-SUMMARY.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-SUMMARY.md
@.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md

<interfaces>
<!-- 067.4-HUMAN-UAT.md frontmatter schema (verbatim from RESEARCH.md § 2.5). -->

```yaml
---
phase: 067.4
plan: 05
status: <closed|gaps_blocking>
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
closure_outcome: <closed|gate_blocked>
green_count: <N>
red_count: <N>
deferred_count: <N>
new_findings_count: <N>
gate: 12/12 user-ticked GREEN required for phase close
gate_met: <true|false>
escalation: <none|067.5>
---
```

Cross-phase mirror diff plan (from RESEARCH.md § 2.5):
- 067.2-HUMAN-UAT.md: append `## Phase 067.4 verdict — N/M GREEN (gate met)` after existing `## Phase 067.3 verdict — 9/12 GREEN (gate not met)`. Update frontmatter `status: blocked → closed` (only if gate met). Flip Rows 3, 4, 5, 6, 11, 12 checkboxes `[ ]` → `[x]` (only if gate met).
- 067.3-HUMAN-UAT.md: append same verdict pattern. Frontmatter `status: gaps_blocking → closed`. Flip R-3 + 067.2-6 mirror checkboxes (only if gate met).

STATE.md `Recent Completed Phases` section format (existing pattern — verbatim from STATE.md):
```markdown
### Phase 067.2: Streaming Render & Storage Fixes (Complete YYYY-MM-DD)

- (one-line summary)

### Phase 067.3: Streaming Render & Storage Fixes — Round 2 (Complete YYYY-MM-DD)

- (one-line summary)

### Phase 067.4: Streaming Suggestions, Tool-Stage Render & Code-Execution Stream (Complete YYYY-MM-DD)

- (one-line summary)
```

Chrome MCP authenticated session (per MEMORY `reference_local_dev_app.md`):
- URL: http://localhost:5173/
- Credentials: `fhdmrd@gmail.com` / `123456`

R-1 cross-thread switch protocol (per D-067.4-QUALITY-01 + 067.3 Plan 05):
- Open Bread thread (existing or new); submit a multi-step prompt that streams text.
- WHILE Thread A is mid-stream, click Physicists thread; submit a different multi-step prompt.
- Click back to Bread thread; assert Bread thread's content is preserved (no truncation, no Physicists content leaked).
- Click Physicists thread; assert content is preserved.

R-4 active-thread tool-stage protocol (per CONTEXT.md):
- Submit `"Use the code execution tool to print numbers 1 to 5"` on a fresh thread.
- Watch the spinner. Within 5 seconds, do NOT navigate threads.
- Note whether the spinner clears AND output renders WITHOUT manual refresh OR thread navigation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 067.4-HUMAN-UAT.md scoreboard with 12 rows</name>
  <files>.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md (full file — schema and row patterns to mirror);
    .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md (cross-phase mirror reference);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-CONTEXT.md (D-067.4-CLOSURE-01 mechanics);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md (§ 2.5 closing UAT mechanics);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md (read OR_MODEL_A + OR_MODEL_B from frontmatter);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-VALIDATION.md (Plan 05 row in Per-Task Verification Map)
  </read_first>
  <action>
    Create `.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` with the schema from RESEARCH.md § 2.5.

    **Frontmatter (initial state — `gate_met: false` until UAT runs):**
    ```yaml
    ---
    phase: 067.4
    plan: 05
    status: pending
    created: <today YYYY-MM-DD>
    updated: <today YYYY-MM-DD>
    closure_outcome: pending
    green_count: 0
    red_count: 0
    deferred_count: 0
    new_findings_count: 0
    gate: 12/12 user-ticked GREEN required for phase close
    gate_met: false
    escalation: none
    ---
    ```

    **Body — 12 scoreboard rows.** Each row format mirrors 067.3-HUMAN-UAT.md:
    ```markdown
    ### Row N: <ID> — <description>

    - [ ] **GREEN** ⬜ **RED** ⬜ **n/a — <reason>**

    **How to test:** <verbatim Chrome MCP / DB / SSE-replay steps>
    **Pass:** <verbatim acceptance criterion>
    **Evidence:** _<placeholder for run_id, screenshot ref, DB output>_
    **Verdict:** _pending_
    ```

    **The 12 rows:**

    1. **R-3** — Suggestion pills render after Fahed-Mrad search-only prompt; SSE contains `suggestions` event with non-empty `questions` (or empty list if LLM still empty post-bump).
       - How: Submit `"Search for any document about Fahed Mrad and summarize."`; observe message render with both confidence indicator AND suggestion pills; replay `/runs/{id}/stream?since=0`; confirm exactly one `suggestions` event present.
       - Pass: At least one suggestion pill visible AND `suggestions` event present (with questions list — empty or non-empty per D-067.4-R3-02).
       - **MIRROR row** (cross-phase): 067.2 Row 6 + 067.3 Row R-3 — both flip GREEN if this passes.

    2. **R-4** — Active-thread tool-stage spinner-to-result transition WITHOUT thread navigation.
       - How: Submit `"Use the code execution tool to print numbers 1 to 5"`; do NOT navigate threads; wait up to 30s.
       - Pass: Spinner clears AND tool output renders WITHOUT thread switch or refresh.
       - If Plan 02 took Branch B (non-reproducible): record as GREEN with note "intermittent — under observation; regression test in place."

    3. **R-5** — Code-execution shows live elapsed counter during execution; lines render in Output panel on completion.
       - How: Submit prompt with `import time; for i in range(5): print(i); time.sleep(1)`; confirm `Executing… N.Ns` counter ticks during execution (heartbeat); confirm post-completion lines populate Output panel.
       - Pass: Elapsed counter visible during run AND increments AND post-completion lines rendered. SSE replay shows `code_executing` events at ~1s cadence + final `code_stdout` events.

    4. **NR-067.2-11** — OpenRouter Model A clean cancellation format.
       - How: Follow `067.4-04-PROTOCOL.md` "Protocol — Model A" steps verbatim with model `<OR_MODEL_A from protocol frontmatter>`.
       - Pass: `runs.error` matches `^timed_out: \d+s per-call deadline exceeded at iteration \d+ \(model=.+\)$` (NOT `GeneratorExit`).
       - `n/a` allowed: `OPENROUTER_API_KEY` missing per `067.4-04-PROTOCOL.md` `or_api_key_present: false`.

    5. **NR-067.2-12** — OpenRouter Model B clean cancellation format.
       - How: Same protocol; substitute `<OR_MODEL_B from protocol frontmatter>`.
       - Pass: Same regex match; distinct model_id appears in `error`.
       - `n/a` allowed: same as Row 4.

    6. **R-1 cross-thread switch protection** (regression spot-check).
       - How: Open Bread thread; submit multi-step prompt streaming text. Mid-stream, switch to Physicists thread; submit a different multi-step prompt. Switch back to Bread; verify content preserved.
       - Pass: Both threads' renders preserved post-switch (no truncation, no cross-thread leak).

    7. **R-2 sandbox download via JS-blob fetch** (regression spot-check).
       - How: From a prior sandbox-output thread (e.g., the pgvector docx run from 067.3), click the Download button. Confirm fresh signed URL + 200 response (no 401).
       - Pass: File downloads successfully; no 401 in network panel.

    8. **R-2-IDOR existence-leak prevention** (regression spot-check).
       - How: Attempt to access another user's sandbox output by URL manipulation (technical equivalent — same JWT + foreign user_id).
       - Pass: 404 returned (no existence leak).

    9. **067.2-5 re-sign architecture** (regression spot-check).
       - How: Click any historical sandbox download — re-sign endpoint hit; fresh URL with new `iat`.
       - Pass: New signed URL produced on every click; 200 response.

    10. **N-01 model→provider router** (regression spot-check).
        - How: Switch model picker to Anthropic claude-sonnet-4-6; submit a streaming prompt; confirm Anthropic provider used (not 404 from OpenAI SDK).
        - Pass: Stream completes via correct provider; no 404 cross-routing.

    11. **067.3-NR no-regression suite** — 4 streaming threads (Bread, Physicists, pgvector, Photosynthesis) all complete cleanly; auto-titles generate; no cross-talk.
        - Pass: 4/4 threads complete with non-`New Chat` titles; no streaming corruption.

    12. **Multi-step pipeline cross-provider re-verification** (067.2 row 9 symmetric evidence).
        - How: Submit a clear multi-step intent (e.g., "search → report → charts → docx") with each available provider — gpt-5.4-mini (already verified in 067.2), Anthropic claude-sonnet-4-6, OpenRouter <OR_MODEL_A>. For each, confirm pipeline executes WITHOUT intermediate confirmation prompts.
        - Pass: All available providers execute pipeline end-to-end. `n/a` allowed for unavailable providers per multi-provider availability rule.

    Add a closing section:
    ```markdown
    ## Closure rules (D-067.4-CLOSURE-01)

    - **12/12 GREEN (or `n/a` for unreachable providers)** → User replies `approved`. Plan 05 Task 4 mirrors back to 067.2-HUMAN-UAT.md + 067.3-HUMAN-UAT.md; flips frontmatter `status: closed` on all three; STATE.md adds 3 entries.
    - **Any non-`n/a`-eligible RED** → User replies `gaps`. Plan 05 Task 4 appends `## Phase 067.4 verdict — N/M GREEN (gate not met)` to all three UAT files. Phase 067.5 escalated.

    ## R-1 + R-4 dual-pass requirement (D-067.4-QUALITY-01)

    Both Row 2 (R-4) AND Row 6 (R-1) must pass. If R-1 regresses while R-4 passes, the phase does NOT close.
    ```

    **Commit message:** `docs(067.4-05): author HUMAN-UAT.md scoreboard with 12 rows for closing UAT`
  </action>
  <verify>
    <automated>test -f .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md && grep -cE "^### Row [0-9]+:" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
    - Frontmatter contains all 11 required keys:
      - `grep -nE "^(phase|plan|status|created|updated|closure_outcome|green_count|red_count|deferred_count|gate|gate_met|escalation):" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md | wc -l` returns >= 11
    - 12 scoreboard rows:
      - `grep -cE "^### Row [0-9]+:" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` returns `12`
    - All 12 rows have unticked checkbox triplet:
      - `grep -cE "\[ \] \*\*GREEN\*\*" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` returns `12`
    - Required row IDs present:
      - `grep -nF "R-3" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
      - `grep -nF "R-4" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
      - `grep -nF "R-5" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
      - `grep -nF "NR-067.2-11" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
      - `grep -nF "NR-067.2-12" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
    - Closure rules section present:
      - `grep -nF "Closure rules (D-067.4-CLOSURE-01)" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
    - R-1 + R-4 dual-pass requirement section present:
      - `grep -nF "R-1 + R-4 dual-pass requirement (D-067.4-QUALITY-01)" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
    - Commit: `git log -1 --pretty=%s` matches `docs(067.4-05): author HUMAN-UAT.md scoreboard with 12 rows for closing UAT`
  </acceptance_criteria>
  <done>
    Scoreboard authored; ready for Task 3 (Chrome MCP execution).
  </done>
</task>

<task type="auto">
  <name>Task 2: Pre-flight — verify dev servers + read 067.4-04-PROTOCOL.md</name>
  <files></files>
  <read_first>
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md (full file — confirm OR_MODEL_A + OR_MODEL_B + or_api_key_present);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md (rows authored in Task 1);
    backend/.env (NAME-ONLY probe — re-verify OPENROUTER_API_KEY presence)
  </read_first>
  <action>
    1. **Re-read** `067.4-04-PROTOCOL.md`; capture `or_model_a`, `or_model_b`, `or_api_key_present` from frontmatter.
    2. **Verify backend dev server is running** at http://localhost:8000/. Use a Bash health check:
       ```bash
       curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8000/health 2>&1 || echo "BACKEND_DOWN"
       ```
       Expected: `200` or a similar healthy code.
    3. **Verify frontend dev server is running** at http://localhost:5173/. Use:
       ```bash
       curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:5173/ 2>&1 || echo "FRONTEND_DOWN"
       ```
       Expected: `200`.
    4. **If either is down**, prompt the user to start them (do NOT auto-start unless explicitly authorized — dev environments vary).
    5. Record verdicts in a short pre-flight note in the Plan 05 SUMMARY draft.

    No git commit for this task — pre-flight is operational only.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w "BACKEND=%{http_code}\n" http://localhost:8000/health; curl -sS -o /dev/null -w "FRONTEND=%{http_code}\n" http://localhost:5173/</automated>
  </verify>
  <acceptance_criteria>
    - Backend responds to `http://localhost:8000/health` with HTTP 200 (or other 2xx)
    - Frontend responds to `http://localhost:5173/` with HTTP 200 (or 3xx redirect)
    - Plan 05 SUMMARY draft has pre-flight note section with both server statuses + protocol model IDs
    - If either is down: user prompted to start; do NOT proceed to Task 3 until both green
  </acceptance_criteria>
  <done>
    Both dev servers running; protocol model IDs read; ready for Task 3 Chrome MCP UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Drive 12-row UAT via Chrome MCP — user reviews scoreboard</name>
  <what-built>
    Plan 01 R-3 emit fix + threadpool wrap + budget bump + narrowed exception (single commit).
    Plan 02 R-4 vitest test + (Branch A) optional surgical patch OR (Branch B) regression test alone.
    Plan 03 R-5 code-execution heartbeat (backend ~1s tick + frontend elapsed counter).
    Plan 04 OpenRouter synthetic-timeout protocol document.
  </what-built>
  <how-to-verify>
    Claude drives Chrome MCP authenticated session at http://localhost:5173/ (login `fhdmrd@gmail.com` / `123456`). For each of the 12 rows in `067.4-HUMAN-UAT.md`:

    1. Execute the `How to test` steps verbatim.
    2. Capture evidence: run_id (from network panel or DB), screenshot of UI state, SSE replay JSON output for backend assertions, DB query output for `runs.error` for NR rows.
    3. Compare actual vs `Pass` criterion; mark row GREEN, RED, or `n/a — <reason>`.
    4. Update `067.4-HUMAN-UAT.md` with proposed verdicts inline.

    **Critical dual-pass requirement (D-067.4-QUALITY-01):**
    - Row 2 (R-4 active-thread tool-stage protocol) MUST pass.
    - Row 6 (R-1 cross-thread switch protocol) MUST pass.
    - Both pass OR neither closes.

    **For NR rows (4 + 5):** if `or_api_key_present: false` from `067.4-04-PROTOCOL.md`, mark as `n/a — provider unconfigured` per multi-provider availability rule.

    **For Row 12 (multi-step cross-provider):** test gpt-5.4-mini, claude-sonnet-4-6, and `<OR_MODEL_A>`. Mark unavailable providers as `n/a`.

    **After all 12 rows posted:** Claude updates frontmatter:
    - `green_count`: count of GREEN rows
    - `red_count`: count of non-`n/a` RED rows
    - `deferred_count`: count of `n/a` rows
    - `gate_met`: `true` ONLY IF `red_count == 0` AND (Row 2 GREEN AND Row 6 GREEN) — both R-4 and R-1 spot-check must pass
    - `closure_outcome`: `closed` if `gate_met == true`, else `gate_blocked`
    - `escalation`: `none` if gate met, else `067.5`
    - `status`: `closed` if gate met, else `gaps_blocking`

    **Commit the populated scoreboard** with message: `docs(067.4-05): closing UAT — N/12 GREEN (gate <met|not met>)`.

    **Then PRESENT the scoreboard to the user** with a short summary table:
    | Row | ID | Verdict |
    |-----|-----|---------|
    | 1 | R-3 + 067.2-6 mirror | <verdict> |
    | 2 | R-4 active-thread | <verdict> |
    | ... | ... | ... |
    Plus the gate verdict (`MET` or `NOT MET`) and the resume signal options below.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` — confirms 12/12 GREEN gate met; proceed to Task 4 GREEN PATH (cross-phase mirror + STATE.md update + close 067.2 + 067.3 + 067.4 together).
    - `gaps` — any RED row(s) require Phase 067.5 escalation; proceed to Task 4 RED PATH.
    - `revise: <specific row issue>` — request re-execution of one or more rows with specific instructions.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Cross-phase mirror — gate-conditional</name>
  <files>.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md, .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md, .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md (Task 3 verdicts);
    .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md (rows 6, 11, 12 + frontmatter status);
    .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md (R-3 + 067.2-6 mirror rows + frontmatter status)
  </read_first>
  <action>
    Branch on Task 3 user response.

    **GREEN PATH (`approved`):**

    1. **067.2-HUMAN-UAT.md mirror:**
       - Append a new section AFTER the existing `## Phase 067.3 verdict — 9/12 GREEN (gate not met)` section:
         ```markdown
         ## Phase 067.4 verdict — 12/12 GREEN (gate met)

         Closing UAT executed via Plan 067.4-05; cross-phase closure per D-067.4-CLOSURE-01.

         - Row 6 (suggestion pills) → GREEN via Plan 01 R-3 fix (run_id `<from 067.4 evidence>`)
         - Row 11 (OpenRouter Model A timeout) → <GREEN|n/a> per `067.4-04-PROTOCOL.md`
         - Row 12 (OpenRouter Model B timeout) → <GREEN|n/a> per `067.4-04-PROTOCOL.md`

         Phase closed alongside 067.4 + 067.3.
         ```
       - Flip checkboxes for Rows 6, 11, 12 from `[ ]` to `[x]` (locate them in the existing scoreboard; preserve all other content).
       - Update frontmatter `status:` from `blocked` (or current value) to `closed`.

    2. **067.3-HUMAN-UAT.md mirror:**
       - Append same `## Phase 067.4 verdict — 12/12 GREEN (gate met)` section.
       - Flip checkboxes for R-3 + 067.2-6 mirror rows from `[ ]` to `[x]`.
       - Update frontmatter `status:` from `gaps_blocking` to `closed`.

    3. **067.4-HUMAN-UAT.md:** Already updated in Task 3; verify `gate_met: true` and `status: closed` in frontmatter.

    4. **Commit:** Single commit including all three UAT file changes with message:
       ```
       docs(067.4-05): cross-phase closure — 067.2 + 067.3 + 067.4 GREEN (12/12 gate met)
       ```

    **RED PATH (`gaps`):**

    1. **067.2-HUMAN-UAT.md:** Append `## Phase 067.4 verdict — N/M GREEN (gate not met)` section listing failing rows + escalation note. Do NOT flip checkboxes; do NOT change frontmatter `status`.

    2. **067.3-HUMAN-UAT.md:** Append same section. Same constraints.

    3. **067.4-HUMAN-UAT.md:** Already updated in Task 3 with `gate_met: false`, `status: gaps_blocking`, `escalation: 067.5`.

    4. **Commit:** Single commit with message:
       ```
       docs(067.4-05): closing UAT N/12 — gate not met; escalating to Phase 067.5
       ```

    **REVISE PATH (`revise: ...`):** Loop back to Task 3 for the named row(s); re-execute; update verdict; come back here.
  </action>
  <verify>
    <automated>grep -cE "^## Phase 067\.4 verdict" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - **Both branches:**
      - `grep -nF "Phase 067.4 verdict" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` exits 0
      - `grep -nF "Phase 067.4 verdict" .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md` exits 0
      - Single commit covers all three UAT files: `git diff HEAD~1 --name-only | grep -c "HUMAN-UAT.md"` returns `3`
    - **GREEN PATH only:**
      - `grep -nE "^status:\s+closed" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
      - `grep -nE "^status:\s+closed" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` exits 0
      - `grep -nE "^status:\s+closed" .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md` exits 0
      - Commit message: `git log -1 --pretty=%s` matches `docs(067.4-05): cross-phase closure`
      - Rows 6, 11, 12 in 067.2-HUMAN-UAT.md show `[x]` (verify via `grep -cE "^- \[x\]" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` returns >= 3 more than pre-edit baseline)
    - **RED PATH only:**
      - `grep -nE "^status:\s+gaps_blocking" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
      - `grep -nE "^escalation:\s+067\.5" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` exits 0
      - 067.2 + 067.3 frontmatter `status:` UNCHANGED from pre-edit values (verify via `git diff HEAD~1 -- ... | grep -E "^[+-]status:"` shows zero changes outside of 067.4 file)
  </acceptance_criteria>
  <done>
    Cross-phase mirror committed in single commit. All three UAT files reflect Plan 05 verdict consistently.
  </done>
</task>

<task type="auto">
  <name>Task 5: STATE.md update — gate-conditional, single commit</name>
  <files>.planning/STATE.md</files>
  <read_first>
    .planning/STATE.md (full file — particularly the `Recent Completed Phases` section and `Current Position` block);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md (final verdicts);
    .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md (post-mirror state);
    .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md (post-mirror state)
  </read_first>
  <action>
    Branch on Task 4 outcome.

    **GREEN PATH (gate met):**

    1. **Update `Current Position` block** at the top of STATE.md:
       - `Phase:` → blank or "v2.5 milestone — phases 067.2/067.3/067.4 closed; next planned phase TBD"
       - `Status:` → "Phase 067.4 closed (cross-phase: 067.2 + 067.3 + 067.4 all closed YYYY-MM-DD)"
       - `Last activity:` → "<today YYYY-MM-DD> — Phase 067.4 Plan 05 closing UAT 12/12 GREEN; cross-phase closure of 067.2 + 067.3 + 067.4"

    2. **Add three entries to `Recent Completed Phases` section** in date order (existing pattern):
       ```markdown
       ### Phase 067.2: Streaming Render & Storage Fixes (Complete <YYYY-MM-DD>)

       - 6 lived-experience gaps closed via 067.3 + 067.4 R-3 fix; cross-phase closure of all six gaps.

       ### Phase 067.3: Streaming Render & Storage Fixes — Round 2 (Complete <YYYY-MM-DD>)

       - R-1 per-thread message store; R-2 sandbox download; R-2-IDOR existence-leak prevention; N-01 model→provider router. R-3 closed via 067.4.

       ### Phase 067.4: Streaming Suggestions, Tool-Stage Render & Code-Execution Stream (Complete <YYYY-MM-DD>)

       - R-3 suggestion-emit fix (max_completion_tokens=2000 + always-emit-empty + run_in_threadpool wrap + narrowed exception); R-4 active-thread tool-stage regression test; R-5 code-execution heartbeat; NR-067.2-11/12 OpenRouter clean-cancellation verified.
       ```

    3. **Update `Blockers` section:** clear the four R-3/R-4/R-5/NR escalation items now that they are resolved.

    4. **Update `Phases OPEN`:** remove 067.2, 067.3, 067.4 from the open list. Note that v2.5 milestone is at 100% phase completion (or update milestone progress accordingly).

    5. **Single commit covering STATE.md + (if not yet committed) any final SUMMARY artifacts:**
       ```
       docs(state): close phases 067.2 + 067.3 + 067.4 — STREAM-04-correctness-round3 shipped
       ```

    **RED PATH (gate not met):**

    1. **Update `Current Position` block:**
       - `Status:` → "Phase 067.4 OPEN — UAT N/12 GREEN; gate not met; escalating to Phase 067.5"
       - `Last activity:` → "<today YYYY-MM-DD> — Phase 067.4 Plan 05 closing UAT N/12; gap-blocking escalation to 067.5"

    2. **Update `Blockers` section:** keep the failing items (R-3 / R-4 / R-5 / NR specific to which row failed) and add a new top-level note that Phase 067.5 is the next escalation target.

    3. **Do NOT add 067.2 / 067.3 / 067.4 to `Recent Completed Phases`.**

    4. **Single commit:**
       ```
       docs(state): Phase 067.4 closing UAT N/12 — escalating to Phase 067.5
       ```
  </action>
  <verify>
    <automated>grep -nE "^### Phase 067\.[234]:" .planning/STATE.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - **GREEN PATH:**
      - `grep -nE "^### Phase 067\.2:" .planning/STATE.md` exits 0
      - `grep -nE "^### Phase 067\.3:" .planning/STATE.md` exits 0
      - `grep -nE "^### Phase 067\.4:" .planning/STATE.md` exits 0
      - `grep -nF "(Complete " .planning/STATE.md | grep -c "067.[234]"` returns >= 3
      - Commit: `git log -1 --pretty=%s` matches `docs(state): close phases 067.2 + 067.3 + 067.4`
    - **RED PATH:**
      - `grep -nE "Phase 067\.5" .planning/STATE.md` exits 0 (escalation noted)
      - `grep -nF "Recent Completed Phases" .planning/STATE.md` shows the section but does NOT contain a Phase 067.4 entry: `grep -nA 30 "Recent Completed Phases" .planning/STATE.md | grep -F "Phase 067.4:"` exits 1
      - Commit: `git log -1 --pretty=%s` matches `docs(state): Phase 067.4 closing UAT`
    - Both paths: STATE.md frontmatter `last_updated` reflects today's timestamp; STATE.md `last_activity` reflects today's date.
  </acceptance_criteria>
  <done>
    STATE.md reflects final outcome; single commit covers cross-phase closure summary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chrome MCP browser session → backend | Authenticated POST /messages + GET /runs/{id}/stream during UAT execution |
| UAT executor → 3 phase UAT files (mutate frontmatter + checkboxes) | Plan 05 Task 4 mutates 067.2 + 067.3 + 067.4 HUMAN-UAT.md; git-tracked, reversible |
| UAT executor → STATE.md | Plan 05 Task 5 mutates STATE.md `Recent Completed Phases` section; git-tracked, reversible |
| User → Plan 05 Task 3 checkpoint | User reviews scoreboard and replies `approved` / `gaps` / `revise:` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067.4-05-01 | T (Tampering) | Cross-phase frontmatter mutations (3 files in single commit) | mitigate | Single-commit-per-task discipline (Tasks 4 + 5 each one commit). Git diff before commit shows exact mutations; user can revert. Acceptance criteria gate on specific frontmatter regex (`^status:\s+closed`). |
| T-067.4-05-02 | I (Information disclosure) | run_ids, screenshots, DB output captured in HUMAN-UAT.md evidence | accept | Evidence is git-tracked artifact for audit trail; run_ids are UUIDs (not PII); screenshots are user-driven and reviewed by the user before commit. |
| T-067.4-05-03 | R (Repudiation) | "Cross-phase closure" without per-phase commit could obscure history | mitigate | Each task has a distinct commit message that names the phases it touches. Task 4: `docs(067.4-05): cross-phase closure — 067.2 + 067.3 + 067.4`. Task 5: `docs(state): close phases 067.2 + 067.3 + 067.4`. Audit trail intact. |
| T-067.4-05-04 | E (Elevation of privilege) | UAT executor authenticates as `fhdmrd@gmail.com` (test user) | accept | Test user is single-user dev environment per MEMORY `reference_local_dev_app.md`. No org/multi-tenant boundary involved. |
| T-067.4-05-05 | D (Denial of service) | UAT may take 30-60 minutes of Chrome MCP driving | accept | Plan 05 is `autonomous: false` — user-confirmed checkpoint. Time investment is the closing UAT contract. Plan 05 may run multiple iterations if `revise:` returns. |
| T-067.4-05-06 | T (Tampering) | OpenRouter `LLM_CALL_TIMEOUT_OVERRIDES` env-var change persisting after UAT | mitigate | `067.4-04-PROTOCOL.md` "Cleanup" section requires removing the override line + final uvicorn restart. Plan 05 SUMMARY must record cleanup completion. |
| T-067.4-05-07 | I (Information disclosure) | Chrome MCP screenshots may capture content from prior threads | mitigate | UAT executor reviews each screenshot before pasting reference into HUMAN-UAT.md. Test data only (Bread / Physicists / pgvector / Photosynthesis test threads). |

**Disposition tally:** 4 mitigate, 3 accept. No `high`-severity threats. Block-on-high gate: PASS.
</threat_model>

<verification>
1. `067.4-HUMAN-UAT.md` exists with valid 12-row scoreboard.
2. Pre-flight green (both dev servers running; protocol model IDs read).
3. Task 3 user-verify checkpoint reached and resolved (`approved` / `gaps` / `revise:`).
4. Task 4 cross-phase mirror committed (3 UAT files modified in single commit).
5. Task 5 STATE.md update committed (single commit).
6. **GREEN PATH only:** all three phase UAT files frontmatter `status: closed`; STATE.md `Recent Completed Phases` has 3 new entries.
7. **RED PATH only:** verdict sections appended; STATE.md notes 067.5 escalation.
</verification>

<success_criteria>
- ✅ `067.4-HUMAN-UAT.md` authored with 12 rows + closure rules + R-1/R-4 dual-pass requirement
- ✅ Pre-flight verified (both dev servers up; protocol read)
- ✅ Chrome MCP UAT executed for all 12 rows; verdicts captured with run_ids + screenshots + DB outputs
- ✅ User reviews scoreboard and replies `approved` / `gaps` / `revise:`
- ✅ Cross-phase mirror committed in single commit (3 UAT files)
- ✅ STATE.md update committed in single commit
- ✅ **GREEN PATH:** all three phase UAT files closed; STATE.md `Recent Completed Phases` has 3 new entries; v2.5 milestone reflects closure of 067.2/067.3/067.4
- ✅ **RED PATH:** Phase 067.5 escalation noted; UAT verdict sections preserve audit trail
- ✅ R-1 + R-4 dual-pass requirement honored (D-067.4-QUALITY-01)
- ✅ NR-067.2-11/12 either GREEN OR `n/a — provider unconfigured` (not blocking phase closure)
- ✅ No `--watch` flags anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-05-SUMMARY.md` documenting:
- Final scoreboard verdict (N/12 GREEN; gate met or not met)
- Per-row verdict table with run_ids + evidence references
- Cross-phase mirror diff hunks (the 3 UAT files modified)
- STATE.md diff hunk (3 new completed phase entries OR escalation note)
- User response to Task 3 checkpoint (approved / gaps / revise sequence)
- If GREEN: closure attestation — "Phases 067.2 + 067.3 + 067.4 closed YYYY-MM-DD via cross-phase D-067.4-CLOSURE-01."
- If RED: explicit gap list + Phase 067.5 escalation outline (which rows failed, what evidence pointed to root cause, what 067.5 must address).
</output>
