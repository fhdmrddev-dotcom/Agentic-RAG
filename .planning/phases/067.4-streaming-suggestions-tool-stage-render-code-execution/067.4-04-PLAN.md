---
id: 067.4-04
plan_id: 067.4-04
phase: 067.4
plan: 04
type: execute
wave: 2
depends_on:
  - 067.4-01
  - 067.4-02
  - 067.4-03
files_modified: []
autonomous: true
requirements:
  - STREAM-04-correctness-round3
requirements_addressed:
  - STREAM-04-correctness-round3
must_haves:
  truths:
    - "Two distinct OpenRouter models are selected from the `MODEL_CAPABILITIES` registry (deterministic — per orchestrator-resolved decision #5; NOT live `/models` API)."
    - "Synthetic-timeout protocol is documented end-to-end for both models including: env var setting, uvicorn restart, prompt submission, run_id capture, DB query for `runs.status` + `runs.error`."
    - "If `OPENROUTER_API_KEY` is configured, both runs produce `runs.error` matching `^timed_out: 10s per-call deadline exceeded at iteration \\d+ \\(model=.+\\)$` (clean cancellation format, NOT `GeneratorExit`) — closing UAT in Plan 05 verifies."
    - "If `OPENROUTER_API_KEY` is missing or rotated at execute time, capture `n/a — provider unconfigured` per multi-provider availability rule from Phase 067.2 D-067.2-WAVE-03 (do NOT block phase closure)."
    - "No code changes — protocol is documentation + env-var manipulation; Plan 05 UAT exercises the runs."
  artifacts:
    - path: ".planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md"
      provides: "Step-by-step OpenRouter synthetic-timeout protocol for two distinct models; model IDs read from MODEL_CAPABILITIES registry"
      contains: "LLM_CALL_TIMEOUT_OVERRIDES"
    - path: "backend/.env (config probe — read-only verification)"
      provides: "Confirmation that OPENROUTER_API_KEY is present (or absent — `n/a` branch)"
      contains: "OPENROUTER_API_KEY"
  key_links:
    - from: "backend/app/config.py:120-128 (MODEL_CAPABILITIES registry)"
      to: ".planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md"
      via: "Read OpenRouter entries; pick two distinct model_ids"
      pattern: "openrouter"
    - from: ".planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md"
      to: "Plan 05 UAT scoreboard rows NR-067.2-11, NR-067.2-12"
      via: "Plan 05 executes the protocol per row, captures run_ids + DB error strings as evidence"
      pattern: "NR-067.2-1[12]"
---

<objective>
Close the carry-forward NR-067.2-11/12 OpenRouter timeout regression rows.

Plan 04 is **documentation-only** + **env-var/registry probe**. NO code changes. Per orchestrator-resolved decision #5, model selection uses the **`MODEL_CAPABILITIES` registry** (deterministic, 9 OpenRouter entries) — NOT the live `/models` API.

Per CONTEXT.md D-067.4-NR-01 + research finding 2.4:
- The `_parse_llm_call_timeout_overrides()` parser at `backend/app/config.py:186-227` handles arbitrary OpenRouter model names (slashes, dots, `:free` suffixes) via `rsplit("=", 1)` — confirmed safe.
- The OpenRouter / OpenAI-compat path goes through `_drain_stream_with_close_on_cancel` at `threads.py:158-262` (Phase 067.1 Plan 01 Track A — GeneratorExit-safe).
- The terminal error format at `threads.py:2538-2542` is `f"timed_out: {_last_per_call_budget}s per-call deadline exceeded at iteration {_last_iteration} (model={_last_model_id})"`.
- Anthropic-native sonnet test in 067.2 already produced this clean format — OpenRouter goes through the SAME OpenAI-compat path that gpt-5.4 already verified. Confidence on Track A symmetry is HIGH.

This plan ships:
1. **`067.4-04-PROTOCOL.md`** (NEW) — a step-by-step runbook documenting the synthetic-timeout protocol for two distinct OpenRouter models. Read by Plan 05 closing UAT.
2. **Config probe** — verify `OPENROUTER_API_KEY` is present in `backend/.env` (name-only check; do NOT read or paste the value, per `feedback_env_secrets_handling.md` MEMORY).
3. **Registry pick** — read two distinct OpenRouter model IDs from `MODEL_CAPABILITIES` in `backend/app/config.py`; record them in the protocol doc with the actual current values (model list rotates as upstream releases — capture today's values for audit trail per D-067.4-NR-01 representative-not-literal rule).

**Plan 04 does NOT execute the synthetic-timeout runs themselves** — Plan 05 closing UAT is the row that exercises them via Chrome MCP. Plan 04 just produces the runbook so Plan 05 has a deterministic protocol to follow.

Purpose: Make NR-067.2-11/12 closure deterministic. Plan 05 reads `067.4-04-PROTOCOL.md` and executes; either both pass (GREEN) or `OPENROUTER_API_KEY` is missing (`n/a` per multi-provider availability rule). No ambiguity.

Output: One protocol document; one config probe verdict (key present / absent); two distinct OpenRouter model IDs locked for Plan 05.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-CONTEXT.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-PATTERNS.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-VALIDATION.md
@backend/app/config.py
@backend/.env.example

<interfaces>
<!-- Verbatim contracts from RESEARCH.md and CONTEXT.md. -->

From `backend/app/config.py:186-227` (parser handles arbitrary OpenRouter model IDs — verified):
```python
def _parse_llm_call_timeout_overrides(raw: str) -> dict[str, int]:
    """Syntax mirrors MODEL_CONTEXT_LIMITS / MODEL_OUTPUT_LIMITS:
    model-id=seconds,model-id=seconds (uses '=' so model ids containing
    ':' like 'minimax/minimax-m2.5:free' are unambiguous)..."""
    for entry in raw.split(","):
        if "=" not in entry:
            continue
        model_id, val_str = entry.rsplit("=", 1)
        ...
```

From `backend/app/api/threads.py:2538-2542` (terminal error format — invariant):
```python
_terminal_error = (
    f"timed_out: {_last_per_call_budget}s per-call deadline "
    f"exceeded at iteration {_last_iteration} "
    f"(model={_last_model_id})"
)
```

From `backend/app/config.py:120-128` (`MODEL_CAPABILITIES` registry — read OpenRouter entries):
- Current OpenRouter entries (per RESEARCH.md § 2.4): `deepseek/deepseek-chat`, `deepseek/deepseek-reasoner`, `deepseek/deepseek-r1`, `z-ai/glm-5.1`, `moonshotai/kimi-k2.5`, `moonshotai/kimi-k2.6`, `minimax/minimax-01`, `minimax/minimax-m2.7`, `minimax/minimax-m2.5:free` (9 entries — verify at execute time)

From CONTEXT.md D-067.4-NR-01 (representative-not-literal rule — verbatim):
> The model identifiers in legacy row labels (`moonshotai/kimi-k2.5`, `minimax/minimax-m2.7`) are **representative**, not literal — at execute time, substitute whichever OpenRouter models the user has currently configured in the model picker (provider routing rotates as upstream releases). What we're testing is the **OpenRouter provider-class** timeout-cancellation contract, not specific pinned models.

User MEMORY note (per `feedback_env_secrets_handling.md`):
> Never ask user to paste full .env; always use name-only extraction. User leaked live keys 2026-05-02; all rotated.

PowerShell name-only env probe pattern:
```powershell
Select-String -Path "backend/.env" -Pattern "^OPENROUTER_API_KEY=" -SimpleMatch | ForEach-Object { ($_.Line -split "=", 2)[0] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read MODEL_CAPABILITIES registry, probe OPENROUTER_API_KEY name presence, write 067.4-04-PROTOCOL.md</name>
  <files>.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md</files>
  <read_first>
    backend/app/config.py (specifically the `MODEL_CAPABILITIES` block at ~lines 120-128 — extract OpenRouter entries verbatim);
    backend/.env.example (canonical layout);
    backend/.env (NAME-ONLY probe — do NOT read or paste secret values; confirm only that the key `OPENROUTER_API_KEY` is present);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-CONTEXT.md (D-067.4-NR-01 — representative-not-literal rule);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md (§ 2.4 — full protocol shape with regex, env-var pattern);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-VALIDATION.md (Plan 04 + Plan 05 manual-only verifications)
  </read_first>
  <action>
    **Step 1 — Read `MODEL_CAPABILITIES` registry from `backend/app/config.py`.** Extract the current OpenRouter entries (those with `provider == "openrouter"` or string-matching the openrouter slug). Pick TWO distinct entries — preferably the same shape as legacy rows (one moonshotai/kimi-* and one minimax/minimax-* — but if those are no longer in the registry, pick any two distinct OpenRouter model_ids).

    Record the two chosen IDs in the protocol document with the verbatim string. Note the date of selection (2026-05-09 or whenever Plan 04 runs) so the audit trail survives upstream model rotations.

    **Step 2 — Probe `OPENROUTER_API_KEY` name presence in `backend/.env`** (name-only — do NOT read or paste the value).

    On Windows PowerShell:
    ```powershell
    Select-String -Path "backend/.env" -Pattern "^OPENROUTER_API_KEY=" -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count
    ```
    Or via Bash:
    ```bash
    grep -c "^OPENROUTER_API_KEY=" backend/.env
    ```

    Record the result in the protocol document as PRESENT (count >= 1) or MISSING (count == 0). Do NOT paste the actual value.

    **Step 3 — Author `.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md`** with the following structure:

    ```markdown
    ---
    phase: 067.4
    plan: 04
    type: protocol
    created: <date>
    or_model_a: <verbatim model ID, e.g., moonshotai/kimi-k2.5>
    or_model_b: <verbatim distinct model ID, e.g., minimax/minimax-m2.7>
    or_api_key_present: <true|false>
    selection_method: MODEL_CAPABILITIES registry (per D-067.4-NR-01 + orchestrator-resolved decision #5)
    representative_not_literal: true
    ---

    # Phase 067.4 Plan 04 — OpenRouter Synthetic-Timeout Protocol

    ## Selected models
    - **OR_MODEL_A:** `<verbatim>` — sourced from MODEL_CAPABILITIES registry @ <date>
    - **OR_MODEL_B:** `<verbatim, distinct>` — sourced from MODEL_CAPABILITIES registry @ <date>

    Per D-067.4-NR-01 these are **representative of the OpenRouter provider class**, not literal pinned models. The contract under test is "OpenRouter timeout cancellation produces clean format", invariant across model rotations.

    ## Pre-flight check
    - `OPENROUTER_API_KEY` presence in `backend/.env`: **<PRESENT|MISSING>** (name-only probe, no value disclosed)
    - If MISSING → Plan 05 UAT marks NR-067.2-11 and NR-067.2-12 as `n/a — provider unconfigured` per multi-provider availability rule (Phase 067.2 D-067.2-WAVE-03). Do NOT block phase closure.

    ## Protocol — Model A
    1. Append to `backend/.env`:
       ```
       LLM_CALL_TIMEOUT_OVERRIDES=<OR_MODEL_A>=10
       ```
    2. Restart uvicorn (Ctrl+C, then re-run dev command).
    3. Open http://localhost:5173/ ; login `fhdmrd@gmail.com` / `123456` (Chrome MCP authenticated session).
    4. Switch model picker to `<OR_MODEL_A>`.
    5. Submit a long-form prompt that will exceed 10s of LLM time, e.g., `"Write a 10-page detailed technical spec for a real-time chat application with all components, sections, examples, and code samples."`
    6. Wait for cancellation (~10s into the run).
    7. Capture `run_id` from the network panel (or via `cancelRun` POST trace).
    8. Run DB query against the `runs` table:
       ```sql
       SELECT run_id, status, error
       FROM public.runs
       WHERE run_id = '<captured run_id>';
       ```
    9. **Acceptance:**
       - `status = 'timed_out'`
       - `error` matches regex `^timed_out: \d+s per-call deadline exceeded at iteration \d+ \(model=.+\)$`
       - GREEN if both match; capture verbatim `error` string in Plan 05 evidence row.
       - **NOT** `GeneratorExit` format — that would indicate Track A regression.

    ## Protocol — Model B
    1. Replace the `LLM_CALL_TIMEOUT_OVERRIDES` line in `backend/.env`:
       ```
       LLM_CALL_TIMEOUT_OVERRIDES=<OR_MODEL_B>=10
       ```
    2. Restart uvicorn.
    3. Repeat steps 3-9 from Model A protocol with `<OR_MODEL_B>`.
    4. Acceptance: same — distinct model_id appears in `error` string.

    ## Cleanup
    1. After both models tested, REMOVE the `LLM_CALL_TIMEOUT_OVERRIDES` line from `backend/.env` (or set to empty: `LLM_CALL_TIMEOUT_OVERRIDES=`).
    2. Restart uvicorn one final time so dev session returns to normal timeout policy.

    ## Plan 05 evidence rows
    - **NR-067.2-11**: `<OR_MODEL_A>` — capture run_id + verbatim `runs.error` string.
    - **NR-067.2-12**: `<OR_MODEL_B>` — capture run_id + verbatim `runs.error` string (distinct from Model A).

    ## Closure paths
    - **GREEN both:** mirror back to 067.2-HUMAN-UAT.md Rows 11 + 12 + 067.4-HUMAN-UAT.md Rows NR-067.2-11 + NR-067.2-12.
    - **`n/a` (key absent):** record reason in evidence; honor multi-provider availability rule; phase closure not blocked.
    - **RED on either:** capture verbatim error string; escalate to Phase 067.5 with explicit Track A regression note.
    ```

    Replace placeholders verbatim with actual model IDs read from registry.

    **Commit message:** `docs(067.4-04): protocol for NR-067.2-11/12 OpenRouter synthetic-timeout regression`
  </action>
  <verify>
    <automated>test -f .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md && grep -cE "^or_model_[ab]:" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md` exits 0
    - Frontmatter contains both model IDs:
      - `grep -nE "^or_model_a:\s+\S" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md` exits 0
      - `grep -nE "^or_model_b:\s+\S" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md` exits 0
    - Frontmatter contains key probe result:
      - `grep -nE "^or_api_key_present:\s+(true|false)" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md` exits 0
    - Frontmatter contains selection method line:
      - `grep -nF "MODEL_CAPABILITIES registry" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md` exits 0
    - The two model IDs are DISTINCT (not the same string twice):
      - `python -c "import re,sys; t=open('.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md').read(); a=re.search(r'^or_model_a:\s+(.+)$',t,re.M).group(1).strip(); b=re.search(r'^or_model_b:\s+(.+)$',t,re.M).group(1).strip(); sys.exit(0 if a!=b and a and b else 1)"` exits 0
    - Protocol body includes the SQL query + the regex acceptance:
      - `grep -nF "SELECT run_id, status, error" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md` exits 0
      - `grep -nF "timed_out: \\d+s per-call deadline exceeded" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md` exits 0 OR the literal regex appears with proper escaping
    - Cleanup section present:
      - `grep -nF "REMOVE the \`LLM_CALL_TIMEOUT_OVERRIDES" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md` exits 0
    - **Negative gate (no secret leak):**
      - `grep -E "OPENROUTER_API_KEY=[^P-]" .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md | wc -l` returns `0` (no actual key value pasted; only `OPENROUTER_API_KEY` as a name)
    - **No code changes:**
      - `git diff HEAD -- backend/ frontend/ | wc -l` returns `0` (Plan 04 is documentation-only)
    - Commit message: `git log -1 --pretty=%s` matches `docs(067.4-04): protocol for NR-067.2-11/12 OpenRouter synthetic-timeout regression`
  </acceptance_criteria>
  <done>
    Protocol document created with two distinct OpenRouter model IDs (read from `MODEL_CAPABILITIES` registry per orchestrator-resolved decision #5), `OPENROUTER_API_KEY` name presence recorded (no secret leakage), full step-by-step UAT runbook including pre-flight check, model A protocol, model B protocol, cleanup, and Plan 05 evidence row schema. Single commit: `docs(067.4-04): protocol for NR-067.2-11/12 OpenRouter synthetic-timeout regression`. Wave 3 (Plan 05) cleared to begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → `backend/.env` | Manual edits by Plan 05 UAT executor add then remove `LLM_CALL_TIMEOUT_OVERRIDES=<model>=10` |
| Operator → uvicorn process | Manual restart between protocol steps |
| Operator → Chrome MCP browser | Submits long-form prompts to trigger synthetic timeout |
| Plan 04 reader → `backend/.env` | NAME-ONLY probe (`grep -c "^OPENROUTER_API_KEY="`); secret value never read or written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067.4-04-01 | I (Information disclosure) | `backend/.env` secret values | mitigate | NAME-ONLY probe specified in action: `grep -c "^OPENROUTER_API_KEY="`. Acceptance criterion `grep -E "OPENROUTER_API_KEY=[^P-]" ... wc -l == 0` confirms no value paste. MEMORY `feedback_env_secrets_handling.md` enforced. |
| T-067.4-04-02 | T (Tampering) | `LLM_CALL_TIMEOUT_OVERRIDES` env-var change persisting after UAT | mitigate | Cleanup section in protocol explicitly requires removing the override line + final uvicorn restart. Plan 05 SUMMARY must record cleanup completion. |
| T-067.4-04-03 | I (Information disclosure) | `runs.error` string captured in evidence | accept | The error string contains `model=<id>` only — no PII. Format verified by RESEARCH.md § 2.4 + threads.py:2538-2542. |
| T-067.4-04-04 | S (Spoofing) | Selecting wrong OpenRouter models could test wrong provider class | mitigate | Frontmatter records selection method (`MODEL_CAPABILITIES registry`) + dates so audit trail can verify the picks were valid OpenRouter entries at execute time. |
| T-067.4-04-05 | E (Elevation of privilege) | None | accept | Plan 04 has no auth/RLS/route-level surface; protocol is documentation. |
| T-067.4-04-06 | R (Repudiation) | run_ids captured by Plan 05 may not be persisted | mitigate | Plan 05 evidence rows in HUMAN-UAT.md require run_id capture; git-tracked artifact preserves audit trail. |

**Disposition tally:** 4 mitigate, 2 accept. No `high`-severity threats. Block-on-high gate: PASS.
</threat_model>

<verification>
1. **Protocol document exists** with frontmatter recording both model IDs + key presence.
2. **Distinct models verified** via the python regex check.
3. **No secret leak** verified by negative grep gate.
4. **No code changes** verified by `git diff HEAD -- backend/ frontend/ | wc -l == 0`.
5. **Plan 05 UAT executor has a deterministic runbook to follow** for NR-067.2-11/12 rows.

Manual UAT (deferred to Plan 05): Plan 05 follows this protocol verbatim.
</verification>

<success_criteria>
- ✅ `067.4-04-PROTOCOL.md` exists with valid frontmatter (model IDs + key presence + selection method)
- ✅ Two DISTINCT OpenRouter model IDs recorded (registry-sourced)
- ✅ `OPENROUTER_API_KEY` presence recorded (no value disclosed)
- ✅ Full step-by-step protocol body (pre-flight, Model A, Model B, cleanup, Plan 05 evidence rows)
- ✅ Negative gate passes — no secret value paste
- ✅ Zero production code changes (`git diff HEAD -- backend/ frontend/ | wc -l == 0`)
- ✅ Commit message: `docs(067.4-04): protocol for NR-067.2-11/12 OpenRouter synthetic-timeout regression`
- ✅ Wave 3 (Plan 05) cleared to begin
</success_criteria>

<output>
After completion, create `.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-SUMMARY.md` documenting:
- Final two OpenRouter model IDs selected (with date of selection)
- `OPENROUTER_API_KEY` presence verdict (PRESENT or MISSING)
- Hand-off note to Plan 05: "Plan 05 UAT executes the protocol per `067.4-04-PROTOCOL.md`; if MISSING, mark NR rows as `n/a — provider unconfigured`."
- No code changes — documentation-only commit hash
</output>
