---
phase: 067.4
title: Streaming Suggestions, Tool-Stage Render & Code-Execution Stream
created: 2026-05-09
status: discussed
locked: true
related_phases:
  - 067 (frontend streaming-UX baseline; closed)
  - 067.1 (agent streaming polish; closed)
  - 067.2 (streaming render & storage fixes; OPEN — Rows 6, 11, 12 carry-forward)
  - 067.3 (round-2 fixes; OPEN — R-3 RED + 067.2-6 mirror RED + UAT-discovered R-4/R-5)
inherits_from:
  - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-CONTEXT.md
  - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-05-SUMMARY.md (UAT verdicts + 067.4 scope recommendation)
  - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-INVESTIGATION-NOTES.md (Plan 04 No-op variant + branch-routing protocol)
---

# Phase 067.4 — Streaming Suggestions, Tool-Stage Render & Code-Execution Stream — CONTEXT

<domain>
**Phase 067.4 closes the UAT gate for three open phases simultaneously: 067.2, 067.3, and 067.4.** Five scoped issues, ordered by dependency:

- **R-3** (gate-blocking, mirrors 067.2 Row 6): suggestion pills not emitted to SSE for Fahed-Mrad search-only prompts. Plan 04 instrumentation at `backend/app/api/threads.py:2487-2526` is live; first action is uvicorn-stdout capture to read which log line fires (`logger.warning("suggestion generation failed", exc_info=True)` exception-swallow path vs `logger.info("suggestions empty for run X")` empty-LLM-output path). Fix branches by evidence — DO NOT re-explore the disconfirmed `max_tokens` 200→800 hypothesis (retest run_id `00dcf270-3773-44e2-8181-6ee36169dc03` SSE still missing `suggestions` event).

- **R-4** (UAT-discovered in 067.3 Plan 05): active-thread UI freezes at tool-execution stage; refetch on thread-switch repairs. Likely a Plan 03 R-1 refactor regression — when `useMessages.ts` was refactored to `messagesByThread: Map<string, Message[]>`, one or more SSE handlers (`tool_end`, `tool_preparing`, post-`done` finalizer) likely missed the update to write to the per-thread bucket on the active thread. Fix posture is **surgical** — patch each missed handler in place, do NOT broadly refactor the SSE dispatcher.

- **R-5** (UAT-discovered in 067.3 Plan 05, additive UX): code-execution stdout streams line-by-line via a new `code_output_line` SSE event. Strictly additive — backend hooks `llm-sandbox` stdout in `backend/app/api/threads.py` near the existing `tool_start`/`tool_end` block; frontend handler in `useMessages.ts` writes each line into the per-thread bucket parallel to `delta`; `ExecuteCodeBlock.tsx` Output panel renders incrementally. NO changes to `OutputFileCard` (R-2), `messagesByThread` shape (R-1), `MODEL_CAPABILITIES` router (N-01), `suggestion_service` (R-3) surfaces.

- **NR-067.2-11/12** (carry-forward from 067.2): OpenRouter **provider-class** synthetic-timeout regression. The model names in the row labels (kimi-k2.5, minimax-m2.7) are **representative of the OpenRouter provider class**, not literal pinned models — at execute time, Plan 04 uses whichever current OpenRouter models the user has configured (the model list rotates as providers release new versions). Same protocol applies regardless: `OPENROUTER_API_KEY` is currently present in `backend/.env`; run the standard synthetic-timeout protocol on two distinct OpenRouter models; confirm clean cancellation format (NOT `GeneratorExit`).

- **Plan 05 closing UAT**: `autonomous: false`. Cross-phase scoreboard mirrors verdicts back into BOTH 067.2-HUMAN-UAT.md AND 067.3-HUMAN-UAT.md AND 067.4's own 067.4-HUMAN-UAT.md — closes all three phases together when the strict 12/12 GREEN gate is met.
</domain>

<decisions>

### D-067.4-R3-01 — R-3 investigation strategy (LOCKED)

**Decision:** Plan 01 starts with **uvicorn stdout capture** during a Fahed-Mrad search-only run. Read which Plan 04 log line fires:
- `logger.warning("suggestion generation failed for run X — main response unaffected", exc_info=True)` → exception-swallow path; the broad `except Exception:` at `threads.py:2509` is hiding a non-`NotFoundError` exception. Fix: narrow the exception class OR re-raise in a controlled way OR fix the underlying exception.
- `logger.info("suggestions empty for run X — generate_suggestions returned [] (no emit; not an error)")` → empty-LLM-output path; the LLM is returning `content=""`. Likely candidate causes: (a) reasoning-token starvation on `gpt-5.4-mini` (max_tokens budget consumed by chain-of-thought; the `max_tokens=200` config at `suggestion_service.py:75/92` is suspect — but **DO NOT re-explore the 200→800 budget bump alone**, which was disconfirmed live during 067.3 Plan 05); (b) JSON output parser miscount (function-call-style response wrapping); (c) prompt insufficiency (system prompt at `suggestion_service.py:15-20` may need tightening for reasoning-capable models).

**Fallback if uvicorn stdout is unavailable:** Plan 01 writes a focused integration test that calls `generate_suggestions(...)` directly with the captured 067.3 prompts/responses (`user_message="Search for any document about Fahed Mrad and summarize."`, `assistant_response=<the actual completed response from run_id 36b509e2-…>`). Assert non-empty return. If that test fails locally with the same `gpt-5.4-mini` model, the bug is fully in `suggestion_service.py` and can be fixed without SSE-pipeline involvement.

**Why this serves "systematic + clean close + flexibility":** Plan 04 already invested in this exact instrumentation specifically for this UAT cycle. Reading what we shipped is the cheapest evidence-first move. Adding more instrumentation is a sunk-cost trap. The fallback test gives us a deterministic local repro for whichever fix path we pick.

**Disconfirmed-hypothesis logged** (do NOT re-explore in 067.4): `max_tokens` budget bump from 200 to 800 in `backend/app/services/suggestion_service.py:75/92`. Retest run_id `00dcf270-3773-44e2-8181-6ee36169dc03` after the bump SSE still missing `suggestions` event. Root cause is elsewhere.

### D-067.4-R3-02 — Empty `[]` emit guarantee (LOCKED)

**Decision:** When `generate_suggestions(...)` returns `[]` (no exception, just empty), the backend should **still emit a `suggestions` event with `questions=[]`** so the frontend can distinguish "we tried and there are no suggestions" from "we never ran the suggestion service." Today at `threads.py:2497-2498`, the emit is gated by `if questions:` — change to always emit, even when empty.

**Frontend behavior on empty `suggestions`:** Render nothing visible (no empty pill row, no "no suggestions" placeholder), but the message record carries the truthful state. Useful for telemetry + future UX work that may want to show a "Why no suggestions?" affordance.

**Why this serves "systematic + clean close":** The current code path makes the SSE stream's "missing event" ambiguous between three states (emit happened with empty, emit didn't happen, exception swallowed). Always emitting when we know for sure the suggestion service ran cleanly removes one ambiguity dimension permanently.

### D-067.4-R4-01 — R-4 fix posture (LOCKED)

**Decision:** Plan 02 takes a **surgical handler-by-handler patch** approach to `frontend/src/hooks/useMessages.ts`. NO broad refactor of the SSE event dispatcher.

**Plan 02 execution order:**

1. `git diff worktree-agent-aef92f6902853490f...HEAD -- frontend/src/hooks/useMessages.ts` (or equivalent — diff against the pre-Plan-03 baseline) to identify which SSE event handlers were updated to write to `messagesByThread[threadId]` and which were NOT updated.
2. For each missed handler (likely candidates: `tool_end`, `tool_preparing`, `tool_start`, post-`done` finalizer), apply the same per-thread-bucket-write pattern that the `delta` and `confidence` handlers use.
3. Add a regression test (vitest IF available; else a documented manual repro protocol) that:
   - Submits a sandbox-invoking prompt
   - Asserts the spinner-to-result transition fires WITHOUT thread navigation
4. Spot-check R-1 (cross-thread switch protocol from 067.3 R-1) alongside R-4 to confirm no regression.

**Why this serves "systematic + clean close + flexibility":** A dispatcher refactor in 067.4 would touch the same module Plan 03 R-1 just shipped, risking a second-wave regression. Surgical keeps blast radius minimal and reversible. The added test guards future refactors. Flexibility for the future: if R-4's pattern recurs (e.g., new SSE event types added in 067.5+), THAT phase can introduce a centralized dispatcher with proper test coverage.

### D-067.4-R5-01 — R-5 streaming granularity (LOCKED)

**Decision:** Plan 03 emits `code_output_line` SSE events **line-by-line** — every `\n` in sandbox stdout triggers one SSE emit.

**Backend (additive only):**
- Hook the `llm-sandbox` stdout stream in `backend/app/api/threads.py` near the existing `tool_start`/`tool_end` block.
- Emit one SSE event per stdout line: `await _emit(redis, run_id, 'code_output_line', tool_index=N, line=<the line>)`.

**Frontend (additive only):**
- Add a handler in `useMessages.ts` for `code_output_line` events.
- Write each line into the same `messagesByThread[threadId]` bucket the `delta` handler uses, structured under `tool_calls[N].output_lines: string[]` (parallel to existing `tool_calls[N].args`).
- `frontend/src/components/chat/ExecuteCodeBlock.tsx` Output panel renders the array incrementally.

**Strictly additive constraint** — NO changes to:
- `OutputFileCard` (R-2 surface)
- `messagesByThread` shape (R-1 surface) — only new keys nested under `tool_calls`
- `MODEL_CAPABILITIES` router (N-01 surface)
- `suggestion_service` or its emit path (R-3 surface)

**Why this serves "systematic + clean close + flexibility":** Line-by-line matches the existing `delta` event cadence (per-token streaming) — reuses the per-thread bucket append pattern already validated in Plan 03 R-1 and what Plan 02 R-4 will lock down. No buffer-flush logic to forget at end-of-execution (a class of bug that bit 067.2 Plan 03's `tool_end` finalizer). Sandbox stdout volume is far lower than LLM token deltas, so the chattier network cost is negligible. **Flexibility:** if observability later shows perf issues at high stdout volumes, time-buffering is a one-line change behind the same SSE event name — backward-compatible with the frontend handler.

### D-067.4-NR-01 — NR-067.2-11/12 OpenRouter provider-class timeout regression protocol (LOCKED)

**Decision:** Plan 04 runs the standard synthetic-timeout protocol for **two distinct OpenRouter models**. The model identifiers in legacy row labels (`moonshotai/kimi-k2.5`, `minimax/minimax-m2.7`) are **representative**, not literal — at execute time, substitute whichever OpenRouter models the user has currently configured in the model picker (provider routing rotates as upstream releases). What we're testing is the **OpenRouter provider-class** timeout-cancellation contract, not specific pinned models.

```bash
# Pick TWO distinct OpenRouter models from the live model picker
# (do NOT hard-code model names; read from the same source the chat composer uses).
OR_MODEL_A="<first OpenRouter model from current picker>"
OR_MODEL_B="<second distinct OpenRouter model from current picker>"

# Run the protocol for each:
LLM_CALL_TIMEOUT_OVERRIDES="${OR_MODEL_A}=10"
# restart uvicorn; submit long-form prompt with model=${OR_MODEL_A}
# expect: runs.status='timed_out', runs.error='timed_out: 10s per-call deadline exceeded at iteration N (model=${OR_MODEL_A})'
# (NOT GeneratorExit format)

LLM_CALL_TIMEOUT_OVERRIDES="${OR_MODEL_B}=10"
# (same protocol; substitute model)
```

**Acceptance:** Either of:
- ✅ GREEN: `runs.error` shows clean cancellation format (NOT `GeneratorExit`) for **both** OpenRouter models. Provider-class regression check passes.
- ⬜ n/a — provider unconfigured: if `OPENROUTER_API_KEY` rotated/missing at execute time. Honor the multi-provider availability rule from Phase 067.2 D-067.2-WAVE-03 — do NOT block phase closure on creds rotation.

**Capture:** Both run_ids in 067.4-HUMAN-UAT.md Evidence fields for future audit. Record the actual model IDs used (not just "row 11" / "row 12") so the audit trail survives upstream model rotations.

**Why "representative-not-literal":** OpenRouter is intentionally the flexible provider in the registry — the model list rotates as upstream releases. The contract we're locking is "OpenRouter timeout cancellation produces clean format", which is invariant across model rotations. A literal kimi-k2.5/minimax-m2.7 lock would silently rot when those models retire. Provider-class testing is the right granularity.

### D-067.4-WAVE-01 — Plan ordering (LOCKED)

**Decision:** 5 plans in 3 waves:

- **Wave 1 (parallel-safe):**
  - Plan 01 — R-3 disambiguation+fix (`backend/app/services/suggestion_service.py` + `backend/app/api/threads.py:2487-2526` emit gate change)
  - Plan 02 — R-4 SSE handler audit+patch (`frontend/src/hooks/useMessages.ts` — files DO NOT overlap with Plan 01)
  - Plan 03 — R-5 code-execution streaming (`backend/app/api/threads.py` near tool_start/tool_end + `frontend/src/hooks/useMessages.ts` + `frontend/src/components/chat/ExecuteCodeBlock.tsx`) — **Plan 03 has frontend file overlap with Plan 02 (`useMessages.ts`)**, so Plans 02 and 03 must run **sequentially** within Wave 1 OR be merged into a single plan; Plan 01 backend-only is fully parallel-safe with both.

  **Wave-1 dispatch policy:** Plan 01 runs in parallel with the Plan 02→Plan 03 sequential pair. The parallel pair (Plan 01 + Plan 02) starts together; Plan 03 starts after Plan 02 commits its `useMessages.ts` changes back. This avoids `useMessages.ts` add/add merge conflicts at the post-merge gate.

- **Wave 2 (sequential, after Wave 1 lands):**
  - Plan 04 — NR-067.2-11/12 OpenRouter timeout regression (`backend/.env` config probe + manual run protocol; no code changes; depends on Wave 1 stability so the OpenRouter test doesn't get confused with R-3/R-4 symptoms)

- **Wave 3 (sequential, autonomous: false):**
  - Plan 05 — Closing UAT (`autonomous: false`, scoreboard mirroring back into 067.2 + 067.3 + 067.4 HUMAN-UAT.md files)

### D-067.4-WAVE-02 — Closing UAT mode (LOCKED — user choice)

**Decision:** Plan 05 closing UAT is **Claude-drives-Chrome-MCP, user-confirms**, mirroring the 067.3 Plan 05 protocol.

**Rationale:** 067.3's Claude-driven UAT pass was efficient and produced strong evidence (run_ids captured, screenshots taken, network requests logged). The original Phase 067.2 Plan 06 reliability lesson ("DOM-observer proxies are NOT acceptance") still holds — Claude is using a **real** browser via Chrome MCP, not a DOM observer, so the lived-experience constraint is honored.

**User-confirms checkpoint:** Plan 05 is `autonomous: false`. After Claude drives all rows and posts the scoreboard, the user reviews and replies `approved` (12/12 GREEN → close all three phases) or `gaps` (any RED → escalate to 067.5).

### D-067.4-CLOSURE-01 — Cross-phase closure (LOCKED)

**Decision:** Phase 067.4 Plan 05 closing UAT closes **all three** open phases (067.2, 067.3, 067.4) when the strict 12/12 GREEN gate is met.

**Mechanics:**
- Plan 05 Task 1: author 067.4-HUMAN-UAT.md scoreboard with rows for R-3, R-4, R-5, NR-067.2-11/12, plus 067.2-6 mirror, plus 067.3-NR no-regression.
- Plan 05 Task 2: USER UAT (Claude drives Chrome MCP) — capture verdicts.
- Plan 05 Task 3: mirror verdicts back into BOTH 067.2-HUMAN-UAT.md AND 067.3-HUMAN-UAT.md. Flip checkboxes from `[ ]` to `[x]` for the mirrored rows AND update each phase's frontmatter `status:` to `closed`.
- Plan 05 Task 4: STATE.md update — flip 067.2 and 067.3 entries from "OPEN" to "Complete YYYY-MM-DD" in the `Recent Completed Phases` section, alongside the 067.4 entry. Single commit.

**If gate not met (any non-`n/a`-eligible row RED):** Same gap-escalation pattern as 067.3 Plan 05 — append `## Phase 067.4 verdict — N/M GREEN (gate not met)` sections to all three phases' UAT files; STATE.md status reflects the actual outcome; escalate to Phase 067.5.

</decisions>

<canonical_refs>

**Required reads for downstream agents (researcher + planner):**

- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-CONTEXT.md` — prior CONTEXT decisions (R-1/R-2/R-3/N-01 design forks; carry-forward boundaries).
- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-05-SUMMARY.md` — full UAT verdict counts, hypothesis-disconfirmation log, R-4/R-5 discovery details.
- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-INVESTIGATION-NOTES.md` — Plan 04 No-op variant evidence + branch-routing protocol.
- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-03-SUMMARY.md` — R-1 per-thread store implementation (the substrate R-4 patches; **must read** before Plan 02).
- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-SUMMARY.md` — Plan 04 observability landed; the log lines R-3 disambiguation reads.
- `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` — closing-UAT mirror target (Rows 6, 11, 12).
- `.planning/STATE.md` — Blockers (escalated to Phase 067.4) section with full disambiguation hypotheses.
- `.planning/ROADMAP.md` § Phase 067.4 — full Goal + Success Criteria + Risks/pitfalls block.
- `backend/app/api/threads.py:2487-2526` — Plan 04 instrumented suggestion-emit block (R-3 disambiguation start point).
- `backend/app/services/suggestion_service.py` — R-3 fix surface (75/92 token budget; 15-20 system prompt; 65-92 model resolution + LLM call).
- `frontend/src/hooks/useMessages.ts` — R-4 audit surface (Plan 03 `messagesByThread` Map + SSE handlers).
- `frontend/src/components/chat/ExecuteCodeBlock.tsx` — R-5 render surface (Output panel for `code_output_line` lines).
- `backend/.env` — `OPENROUTER_API_KEY` presence check for NR-067.2-11/12.

**Project-level refs (already known via PROJECT.md / CLAUDE.md, listed here for completeness):**

- `CLAUDE.md` — project rules (no LangChain/LangGraph; raw SDK calls; Pydantic for structured outputs; SSE streaming; stateless chat; venv for Python; Realtime is hint-only; `run_in_threadpool` for blocking I/O).
- `.planning/PROJECT.md` — milestone v2.5 Deployment Strategy; STREAM-04 requirement chain.

</canonical_refs>

<code_context>

**Reusable assets and patterns from prior phases (do not re-invent):**

- **Per-thread bucket write pattern** (Plan 03 R-1): `messagesByThread.set(threadId, [...messagesByThread.get(threadId), newMessage])` or the equivalent immutable update via the existing setter. R-5's `code_output_line` handler uses this pattern; R-4's surgical patches restore this pattern to the missed handlers.
- **Plan 04 log-line format** (instrumentation already shipped): `logger.warning("suggestion generation failed for run %s — main response unaffected", run_id, exc_info=True)` and `logger.info("suggestions empty for run %s — generate_suggestions returned [] (no emit; not an error)", run_id)`. Plan 01 reads these from uvicorn stdout.
- **SSE replay endpoint**: `/runs/{run_id}/stream?since=0` — replays all events for a run; useful for assertion in regression tests (R-4 + R-5).
- **Model resolution chain** (`suggestion_service.py:46-60`): `user_settings.sub_agent_model` → `settings.sub_agent_model` → `_SUB_AGENT_MODEL_DEFAULTS[provider]` → `user_settings.llm_model` → `settings.llm_model`. Plan 01 may need to swap `_SUB_AGENT_MODEL_DEFAULTS["openai"]` from `gpt-5.4-mini` to a non-reasoning model if branch (b) — empty-output reasoning-token starvation — turns out to be the root cause.
- **Synthetic-timeout protocol** (Phase 067.2 D-067.2-06-01): `LLM_CALL_TIMEOUT_OVERRIDES=<model>=<seconds>`. NR-067.2-11/12 reuses this verbatim.

**Modified surfaces that R-3/R-4/R-5 plans interact with:**

- `backend/app/api/threads.py` — Plan 01 modifies `:2487-2526` (R-3 emit gate); Plan 03 adds emit hook near `tool_start`/`tool_end` (R-5).
- `backend/app/services/suggestion_service.py` — Plan 01 fix surface; lines 65-92 LLM call + content extraction.
- `frontend/src/hooks/useMessages.ts` — Plan 02 surgical patches (R-4); Plan 03 adds `code_output_line` handler (R-5). **Sequential within Wave 1 to avoid merge conflicts.**
- `frontend/src/components/chat/ExecuteCodeBlock.tsx` — Plan 03 Output panel renders `code_output_line` array (R-5).

</code_context>

<deferred>

Out-of-067.4-scope (preserve carry-forward boundaries):

- **N-02** (Phase 067.2 carry-forward): multi-step pipeline self-QA = 8 of 14 tool_calls on opus pptx run. UX/cost concern. Cap via SYSTEM_PROMPT or MAX_SELF_QA_ITERATIONS in a future UX-tuning phase. NOT in 067.4.
- **D-066-11**: langsmith trace gap for Anthropic-native streams — resolved as `runs.error` is canonical-clean-cancellation per Phase 067.2 D-067.2-06-01. NOT in 067.4.
- **Historical message URL migration**: legacy 1h-expiry URLs continue decaying; re-sign endpoint is for new outputs only (Phase 067.2 D-067.2-03 boundary). NOT in 067.4.
- **Centralized SSE dispatcher**: deferred to a future phase (067.5+ or later) IF R-4's surgical-patch approach proves to recur with new event types. Capture in deferred ideas; do NOT pre-emptively refactor in 067.4.
- **Suggestion service prompt tuning**: only in scope for 067.4 IF Plan 01's branch evidence points there. Default Plan 01 stance is fix in-place; broader prompt redesign deferred to a content-quality phase.
- **Model registry curation for known providers (OpenAI / Anthropic / Google)**: today the model picker exposes the full provider model list (OpenAI shows `gpt-4.1`, `gpt-4.1-mini`, `gpt-4o`, `gpt-5`, `gpt-5.4`, `gpt-5.2`, `gpt-5.4-mini`, `gpt-5.4-nano`, `gpt-4o-mini`, `gpt-4.1-nano`, `gpt-5.5`, `o1`, `o3`, `o4` — 14 models; similar surface for Anthropic and Google). The intended posture going forward is to **curate** these lists down to a "more suitable" subset (cost-effective, currently stable, fits the agent's tool-use + RAG patterns) rather than expose every model the provider publishes. **Re-open trigger:** any of {a user complains the picker is too noisy, b cost telemetry shows a deprecated model is still being selected, c we ship a new known-provider integration and want to start curated rather than retrofit}. Contrast: **OpenRouter and Ollama remain intentionally flexible** — they're the "bring your own model" providers and full lists are correct there. Out-of-067.4-scope; capture for a future model-registry / picker-UX phase.

</deferred>

<success_signals>

Phase 067.4 closes cleanly when:

1. R-3 fixed and 067.2 Row 6 mirror flipped GREEN — suggestion pills render after Fahed-Mrad search-only prompts; SSE stream contains a `suggestions` event.
2. R-4 fixed — sandbox-invoking prompts show spinner-to-result transition without thread navigation; R-1 cross-thread switch still GREEN (no regression).
3. R-5 shipped — code-execution stdout streams line-by-line in the Output panel.
4. NR-067.2-11/12 closed — clean cancellation format OR explicitly accepted as `n/a — provider unconfigured`.
5. Plan 05 closing UAT 12/12 GREEN — all three phases (067.2, 067.3, 067.4) close together; STATE.md `Recent Completed Phases` section adds entries for all three in one commit.

</success_signals>
