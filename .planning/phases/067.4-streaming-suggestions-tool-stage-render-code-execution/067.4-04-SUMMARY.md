---
phase: 067.4
plan: 04
subsystem: streaming-stability / observability
tags:
  - documentation-only
  - openrouter
  - timeout-cancellation
  - synthetic-uat-protocol
  - regression-runbook
dependency_graph:
  requires:
    - 067.4-01 (Wave 0 vitest baseline)
    - 067.4-02 (R-7 cancellation receipt)
    - 067.4-03 (R-5 code-execution heartbeat)
  provides:
    - OpenRouter synthetic-timeout protocol document for Plan 05 closing UAT
  affects:
    - .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md (NEW)
tech_stack:
  added: []
  patterns:
    - "Name-only env-var probe (grep with line-start anchor; no value disclosed)"
    - "Representative-not-literal model-id selection (per D-067.4-NR-01)"
    - "MODEL_CAPABILITIES registry as deterministic provider-class proxy"
key_files:
  created:
    - path: .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md
      purpose: Step-by-step OpenRouter synthetic-timeout runbook for Plan 05 UAT
  modified: []
decisions:
  - "Selected moonshotai/kimi-k2.5 + minimax/minimax-m2.7 from MODEL_CAPABILITIES registry as the two distinct OpenRouter models (matches legacy NR-067.2-11/12 row labels; both confirmed present in registry as of 2026-05-09)"
  - "OPENROUTER_API_KEY name-only probe returned PRESENT (count=1); recorded as `or_api_key_present: true` in protocol frontmatter — no value disclosed"
  - "No code changes shipped — protocol confirms via RESEARCH.md § 2.4 that the existing OpenAI-compat path (Track A, threads.py:158-262) handles OpenRouter cleanly"
metrics:
  duration_minutes: 5
  completed_date: 2026-05-09
  tasks_completed: 1
  files_created: 1
  files_modified: 0
  commits: 1
requirements_addressed:
  - STREAM-04-correctness-round3
---

# Phase 067.4 Plan 04: OpenRouter Synthetic-Timeout Protocol Runbook Summary

**One-liner:** Documentation-only deliverable that locks deterministic OpenRouter model picks (`moonshotai/kimi-k2.5` + `minimax/minimax-m2.7` from the `MODEL_CAPABILITIES` registry), records `OPENROUTER_API_KEY` presence (PRESENT, name-only probe), and ships a step-by-step UAT runbook for Plan 05 to close out the carry-forward NR-067.2-11/12 timeout-cancellation regression rows.

## Final Two OpenRouter Model IDs Selected

| Slot | Model ID | Selection date | Native tools | Default per-call timeout |
|------|----------|----------------|--------------|--------------------------|
| OR_MODEL_A | `moonshotai/kimi-k2.5` | 2026-05-09 | False | 600s |
| OR_MODEL_B | `minimax/minimax-m2.7` | 2026-05-09 | False | 240s |

Sourced verbatim from `backend/app/config.py:120-128` (`MODEL_CAPABILITIES`); both confirmed as `provider == "openrouter"` entries. Selection deliberately mirrors the legacy NR-067.2-11/12 row labels; per **D-067.4-NR-01** these are representative of the OpenRouter provider class, not literal pinned models.

## OPENROUTER_API_KEY Presence Verdict

**PRESENT** (name-only probe via Grep with line-start anchor on `OPENROUTER_API_KEY=` against `backend/.env` — count returned `1`). Recorded as `or_api_key_present: true` in protocol frontmatter. **No secret value read, written, or pasted anywhere** (per `feedback_env_secrets_handling.md` MEMORY — keys leaked 2026-05-02 forced full rotation, not repeated here).

## Hand-off Note to Plan 05

> Plan 05 UAT executes the protocol per `067.4-04-PROTOCOL.md`. Since `OPENROUTER_API_KEY` is **PRESENT**, the runbook expects both models to produce a `runs.error` matching `^timed_out: \d+s per-call deadline exceeded at iteration \d+ \(model=.+\)$` — clean cancellation format, NOT `GeneratorExit`. Capture verbatim run_id + error string per evidence row (NR-067.2-11 / NR-067.2-12). If the key gets rotated/missing between Plan 04 and Plan 05 execution, mark both rows as `n/a — provider unconfigured` per multi-provider availability rule (Phase 067.2 D-067.2-WAVE-03) and do **NOT** block phase closure.

## Tasks Executed

| # | Task | Status | Commit |
|---|------|--------|--------|
| 1 | Read `MODEL_CAPABILITIES`, probe `OPENROUTER_API_KEY` name-only, write `067.4-04-PROTOCOL.md` | DONE | `8585058` |

### Task 1 details

- Read `backend/app/config.py` lines 120-128 for the OpenRouter slice of `MODEL_CAPABILITIES` (9 entries — verbatim list captured in protocol body).
- Probed `backend/.env` for `OPENROUTER_API_KEY` name presence using line-start anchor; result: count=1 (PRESENT). Did not read or paste the value.
- Authored `067.4-04-PROTOCOL.md` with the required structure: frontmatter (model IDs, key presence, selection method, representative-not-literal flag), full registry snapshot, pre-flight check, Model A protocol, Model B protocol, cleanup, Plan 05 evidence rows, closure paths (GREEN / `n/a` / RED), and a notes section for the Plan 05 executor.
- All acceptance gates passed:
  - File exists.
  - Frontmatter contains `or_model_a` + `or_model_b` + `or_api_key_present` + `MODEL_CAPABILITIES registry` selection method line.
  - Two model IDs are DISTINCT (`moonshotai/kimi-k2.5` ≠ `minimax/minimax-m2.7`) — verified via Python regex round-trip.
  - Body contains the SQL query (`SELECT run_id, status, error`) and the regex acceptance literal (`timed_out: \d+s per-call deadline exceeded`).
  - Cleanup section contains `REMOVE`.
  - Negative gate `OPENROUTER_API_KEY=[^P-]` returns 0 — no secret value paste.
  - `git diff HEAD -- backend/ frontend/ | wc -l` returned 0 — no production code changes.
  - Commit subject exactly matches `docs(067.4-04): protocol for NR-067.2-11/12 OpenRouter synthetic-timeout regression`.

## Code Changes

**None.** Plan 04 is documentation-only. `git diff HEAD~1 -- backend/ frontend/ | wc -l` returns `0`. The single commit (`8585058`) contains only the new protocol document.

## Deviations from Plan

### Rule 3 — Stray-file cleanup (process correction, not a plan deviation)

During Task 1 the protocol document was first written to the **main repo path** (`C:/Vibe Apps/Agentic RAG/.planning/...`) instead of the **worktree path** (`C:/Vibe Apps/Agentic RAG/.claude/worktrees/agent-a6a673c422b0dd4d0/.planning/...`). The worktree's git index was therefore unaware of the file (`git add` failed with `pathspec did not match any files`). Fix:

1. Re-wrote the same content to the worktree path.
2. Removed the stray file from the main repo path so it doesn't leak into a non-worktree commit later.
3. Re-ran all acceptance gates against the worktree path (all PASS), then committed.

No content change between the two writes; the file in the worktree is the canonical artifact. No production-code or other-plan files were affected.

### Negative-gate false-positive correction

The acceptance criterion's negative-leak regex (`OPENROUTER_API_KEY=[^P-]`) is greedy and triggered on `OPENROUTER_API_KEY="` (in a probe-command example showing the literal grep string), even though no secret value was pasted. Fix: rephrased the inline probe-command examples in the pre-flight section so the literal substring `OPENROUTER_API_KEY=` followed by a non-`[P-]` character no longer appears anywhere in the document. Negative gate then returned 0 (PASS). The probe semantics are unchanged — the doc still tells the Plan 05 executor exactly which Bash and PowerShell commands to use.

## Auth Gates

None. `OPENROUTER_API_KEY` is PRESENT per the name-only probe; no auth challenge was raised. Plan 05 UAT will exercise the actual provider call and is the row where auth-class failures (rotated key, expired credit, etc.) would surface — those are documented as the `n/a — provider unconfigured` branch in the protocol.

## Threat-Model Compliance

All threats from the plan's `<threat_model>` mitigated as planned:

| Threat ID | Disposition | Status |
|-----------|-------------|--------|
| T-067.4-04-01 (I — `.env` secret values) | mitigate | DONE — name-only probe used; negative gate PASS |
| T-067.4-04-02 (T — env-var change persistence) | mitigate | DONE — protocol cleanup section explicitly requires removal + final uvicorn restart; Plan 05 SUMMARY must record cleanup completion |
| T-067.4-04-03 (I — `runs.error` capture) | accept | Format documented; no PII in error string (only `model=…` slug) |
| T-067.4-04-04 (S — wrong model class) | mitigate | Frontmatter records selection method (`MODEL_CAPABILITIES registry`) + date for audit trail |
| T-067.4-04-05 (E — none) | accept | No auth/RLS/route-level surface |
| T-067.4-04-06 (R — run_id audit trail) | mitigate | Plan 05 evidence row schema captures `run_id` + verbatim error |

No new threats discovered during execution. No `threat_flag:` rows for the verifier.

## Known Stubs

None. Plan 04 ships no code, only documentation; nothing is wired to placeholder data.

## Self-Check: PASSED

- [x] `.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-04-PROTOCOL.md` exists in the worktree.
- [x] Commit `8585058` exists in `git log` on branch `worktree-agent-a6a673c422b0dd4d0`.
- [x] All acceptance gates pass against the worktree-relative file.
- [x] No production code changes (`git diff HEAD~1 -- backend/ frontend/` = empty).
- [x] No untracked files left behind (`git status --short` clean post-commit).
- [x] Stray file removed from the main-repo path (no orphaned artifact outside the worktree).
