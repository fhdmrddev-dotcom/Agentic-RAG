---
phase: 141-template-input-resolver-run-scope-stretch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/092_workspace_files_run_claim.sql
  - backend/app/services/template_asset_service.py
  - backend/tests/test_141_run_scope.py
autonomous: true
requirements: [COLL-02]

must_haves:
  truths:
    - "COLL-02: migration 092 adds a NULLABLE text `run_claim` column to workspace_files with NO default and NO backfill (D-141-02, D-141-04)"
    - "COLL-02: pure helper `claim_visible(row_claim, own_claim)` returns True iff `row_claim is None or row_claim == own_claim` — the 5-direction truth table (D-141-01)"
    - "COLL-02: pure helper `own_claim_for_ctx(ctx)` returns `str(ctx.workflow_run_id)` when set else the `'deep'` sentinel (D-141-02)"
    - "COLL-02: the faithful fail-before/pass-after repro test file exists covering workflow→Deep, Deep→workflow, W1→W2 blocked plus Deep→Deep and same-workflow-run reuse allowed (D-141-06)"
  artifacts:
    - path: "supabase/migrations/092_workspace_files_run_claim.sql"
      provides: "Additive nullable run_claim column, no default, no backfill, no new RLS policy"
      contains: "ADD COLUMN IF NOT EXISTS run_claim text"
    - path: "backend/app/services/template_asset_service.py"
      provides: "claim_visible + own_claim_for_ctx pure helpers"
      contains: "def claim_visible"
    - path: "backend/tests/test_141_run_scope.py"
      provides: "COLL-02 behavior→test map (RED backstop for Plan 02)"
      contains: "def test_workflow_to_deep_blocked"
  key_links:
    - from: "backend/tests/test_141_run_scope.py"
      to: "backend/app/services/template_asset_service.py"
      via: "import claim_visible / own_claim_for_ctx"
      pattern: "claim_visible"
---

<objective>
Lay the claim contract for run-scoped template resolution: the migration 092 column, the
two pure helpers the resolver + callers key off, and the full fail-before/pass-after test
file (the RED backstop Plans 02/03 flip GREEN).

Purpose: COLL-02 — a `template_input` template claimed by one run's context must not be
resolvable by a foreign run's context. This plan builds the schema + logic contract and the
executable proof; it does NOT yet wire the resolver WHERE clause or the callers (Plan 02) or
apply the migration to the live DB (Plan 03).

Output: `supabase/migrations/092_workspace_files_run_claim.sql`, two pure helpers in
`template_asset_service.py`, and `backend/tests/test_141_run_scope.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-CONTEXT.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-RESEARCH.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-VALIDATION.md

# The resolver whose helpers this plan defines (Branch 2 edits land in Plan 02)
@backend/app/services/template_asset_service.py
# Migration + test precedents to mirror 1:1
@supabase/migrations/076_messages_origin.sql
@backend/tests/test_120_origin_filter.py

<interfaces>
<!-- Contracts downstream tasks/plans consume. Extracted from the live codebase. -->

Helper contract (this plan creates these in template_asset_service.py):
- claim_visible(row_claim: str | None, own_claim: str) -> bool
    returns (row_claim is None or row_claim == own_claim)   # pure, no I/O
- own_claim_for_ctx(ctx) -> str
    wf = getattr(ctx, "workflow_run_id", None)
    returns str(wf) if wf is not None else "deep"            # keyed ONLY off workflow_run_id

workspace_files (supabase/full-schema.sql:1457-1473) — existing columns:
  id, thread_id, path, mime_type, content_inline, content_storage_path,
  created_by, created_at, kind ('template_input'|'agent'), expires_at.
  Migration 092 adds: run_claim text (nullable, no default).

mock_asyncpg_pool recorder API (backend/tests/conftest.py:455-513):
  set_fetchrow_result(v) / set_fetchrow_results([...]) / set_execute_result("UPDATE 1")
  .calls -> list[(sql, args)] in execution order.

Migration static-contract test precedent (backend/tests/test_workspace_template.py:400-418):
  reads the .sql text; asserts "ADD COLUMN IF NOT EXISTS ..."; asserts
  `"NOT NULL" not in sql.replace("IS NOT NULL", "")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 092 — nullable run_claim column (no default, no backfill)</name>
  <files>supabase/migrations/092_workspace_files_run_claim.sql</files>
  <read_first>
    - supabase/migrations/076_messages_origin.sql (the isolation-column precedent — but note the DIVERGENCE below)
    - supabase/full-schema.sql:1457-1473 (workspace_files current shape)
    - backend/tests/test_workspace_template.py:400-418 (the static-contract assertions this file must satisfy)
    - .planning/phases/141-template-input-resolver-run-scope-stretch/141-RESEARCH.md (Pitfall 3 — why NOT NULL/DEFAULT is forbidden here)
  </read_first>
  <action>
    Create `supabase/migrations/092_workspace_files_run_claim.sql`. Add ONE column to
    `public.workspace_files`: `run_claim` of type `text`, NULLABLE, NO DEFAULT, using
    `ADD COLUMN IF NOT EXISTS run_claim text` (idempotent re-apply). Per D-141-02 the column
    holds either a `str(workflow_run_id)` OR the literal `'deep'` sentinel — a server-set
    value only, so add NO CHECK constraint and NO FK (a uuid FK cannot hold the sentinel).
    Per D-141-04 add NO backfill and NO `UPDATE` — pre-migration rows stay `run_claim IS NULL`
    (unclaimed) and get claimed on first resolve. Add NO new RLS policy — the column inherits
    workspace_files' existing thread-owner RLS (mirror migration 076/050). CRITICAL DIVERGENCE
    from 076: do NOT copy `NOT NULL DEFAULT 'deep'` — that would permanently deep-claim every
    legacy row and block cross-context reuse forever (Pitfall 3). Head-comment the file with:
    the D-141-02/04 rationale, the "apply by hand (SQL editor / psycopg2 :54322), never
    `db push`/`db reset`, then `bash scripts/regenerate-full-schema.sh` (no --reset)" process,
    and a note that Plan 03 (autonomous:false / BLOCKING) applies it — this plan ONLY authors
    the file and does NOT touch supabase/full-schema.sql.
  </action>
  <acceptance_criteria>
    - `test_migration_092_additive_nullable` (Task 3) passes: file contains
      `ADD COLUMN IF NOT EXISTS run_claim text`, and `"NOT NULL" not in sql.replace("IS NOT NULL","")`,
      and contains no `DEFAULT` on run_claim, no `db push`, no `db reset`.
    - Source assertion: the file adds exactly one column and no data-migration `UPDATE`/backfill.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_141_run_scope.py::test_migration_092_additive_nullable -x -q</automated>
  </verify>
  <done>Migration 092 exists as a nullable, no-default, no-backfill additive column; static-contract test green; full-schema.sql untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add pure helpers claim_visible + own_claim_for_ctx to template_asset_service.py</name>
  <files>backend/app/services/template_asset_service.py</files>
  <read_first>
    - backend/app/services/template_asset_service.py (module top — add helpers above resolve_template_source; do NOT edit Branch 2 here, that is Plan 02)
    - backend/app/services/tool_dispatcher.py:77-142 (ToolContext.workflow_run_id at :125 — the field own_claim_for_ctx reads)
    - .planning/phases/141-template-input-resolver-run-scope-stretch/141-RESEARCH.md (§"Sub-agent lineage … RESOLVED" — why the claim MUST key off workflow_run_id, never run_id/parent_run_id)
  </read_first>
  <action>
    In `template_asset_service.py` add two module-level pure functions (no I/O, importable by
    tests and by the Plan-02 callers). `claim_visible(row_claim: str | None, own_claim: str) -> bool`
    returns `row_claim is None or row_claim == own_claim` — this is the single source of the
    eligibility truth (the WHERE predicate in Plan 02 mirrors it). `own_claim_for_ctx(ctx) -> str`
    reads `getattr(ctx, "workflow_run_id", None)` and returns `str(wf)` when non-None else the
    literal `"deep"` sentinel. Key off `workflow_run_id` ONLY: keying off `run_id` breaks Deep→Deep
    reuse (each Deep turn has a new run_id) and keying off `parent_run_id` is null on top-level
    runs; `workflow_run_id` is None on Deep and inherited by sub-agents (task_service.py:625), which
    gives correct sub-agent inheritance with zero plumbing. Define a shared sentinel constant
    (e.g. `DEEP_CLAIM = "deep"`) and use it in both the helper and Plan 02. Do NOT modify
    `resolve_template_source` in this task.
  </action>
  <acceptance_criteria>
    - `test_unclaimed_visible`, `test_deep_to_deep_reuse`, `test_same_workflow_run_reuse`,
      `test_workflow_to_deep_blocked`, `test_deep_to_workflow_blocked`,
      `test_cross_workflow_run_blocked` all pass (the 5-direction truth table over claim_visible).
    - `own_claim_for_ctx` returns `"deep"` for a ctx with `workflow_run_id=None` and `str(W)`
      for `workflow_run_id=W` (asserted in the test file).
    - Source assertion: `claim_visible` and `own_claim_for_ctx` are importable from
      `app.services.template_asset_service`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_141_run_scope.py -x -q -k "claim_visible or unclaimed or reuse or blocked or own_claim"</automated>
  </verify>
  <done>Both pure helpers exist, key off workflow_run_id, and pass the full truth-table + derivation tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author test_141_run_scope.py — the full behavior→test map (RED backstop)</name>
  <files>backend/tests/test_141_run_scope.py</files>
  <behavior>
    - Truth table (GREEN now via Task 2 helpers): unclaimed visible; Deep→Deep reuse; same-workflow-run reuse; workflow→Deep blocked; Deep→workflow blocked; W1→W2 blocked.
    - own_claim_for_ctx derivation: None→"deep", W→str(W).
    - Migration static contract (GREEN now via Task 1): nullable, no default, no backfill, IF NOT EXISTS, no db push/reset.
    - RED-pending Plan 02 (mark xfail(strict=False)): resolver stamps a NULL-claim row + returns bytes; resolver returns the "belongs to another run" honest error (not bytes) for a foreign-claimed non-expired row; happy-path (own-upload + Branch 1 library) unchanged; claim-aware WHERE still carries created_by + thread_id; both Branch-2 resolve sites pass own_claim.
    - RED-pending Plan 02 Task 3 (xfail): _ProducerStreamCtx yields workflow_run_id == str(W) so a workflow emit render claims str(W), never "deep".
  </behavior>
  <read_first>
    - backend/tests/test_120_origin_filter.py (the faithful-repro discipline + source-assertion style to mirror)
    - backend/tests/conftest.py:455-513 (mock_asyncpg_pool recorder API — set_fetchrow_result/results, .calls, set_execute_result)
    - backend/app/services/template_asset_service.py:136-210 (Branch 2 SELECT + two-query probe the new tests assert against)
    - .planning/phases/141-template-input-resolver-run-scope-stretch/141-VALIDATION.md (Per-Task Verification Map — use the EXACT test names)
    - backend/tests/test_workspace_template.py:400-418 (migration static-contract pattern)
  </read_first>
  <action>
    Create `backend/tests/test_141_run_scope.py` implementing every row of the VALIDATION.md
    Per-Task Verification Map using the EXACT test names: `test_unclaimed_visible`,
    `test_deep_to_deep_reuse`, `test_same_workflow_run_reuse`, `test_workflow_to_deep_blocked`,
    `test_deep_to_workflow_blocked`, `test_cross_workflow_run_blocked`,
    `test_resolver_stamps_unclaimed`, `test_resolver_foreign_claim_honest_error`,
    `test_emit_ctx_carries_workflow_lineage`, `test_in_scope_render_unchanged`,
    `test_where_preserves_user_and_thread_scope`, `test_migration_092_additive_nullable`. Also add
    `test_own_claim_deep_vs_workflow` and `test_both_branch2_resolve_sites_pass_own_claim` (source
    assertions over tool_dispatcher.py + phase_types.py). Truth-table tests call `claim_visible`
    directly. Resolver tests seed rows via `mock_asyncpg_pool.set_fetchrow_result(...)` /
    `set_fetchrow_results([...])` (the recorder returns canned dicts; it does NOT evaluate the
    WHERE — so pair row seeds with the pure helper for the truth proof) and assert the stamp lands
    in `pool.calls` as a conditional `UPDATE ... SET run_claim = $... WHERE ... AND run_claim IS NULL`
    with the own-claim arg, and that NO stamp fires for an already-own-claim row. The foreign-claim
    test encodes the CURRENT wrong behavior explicitly (pre-fix, a foreign-claimed row is returned
    as bytes because there is no claim dimension in the WHERE) so it is RED before the fix and GREEN
    after — a faithful repro, not a proxy. The emit-ctx test wraps a `SimpleNamespace(run_id=W, ...)`
    in `_ProducerStreamCtx` and asserts `own_claim_for_ctx(_render_ctx) == str(W)`. Mark every
    behavior not yet implemented by Plan 02 with `pytest.mark.xfail(strict=False, reason="Plan 141-02")`;
    Plan 02 drops those markers as it wires each behavior. NEVER use watch-mode flags. Feedback
    latency target < 4s for the quick file.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_141_run_scope.py -q` exits 0 (truth-table + own_claim + migration tests
      pass; Plan-02 behaviors are xfail — no hard failures).
    - Test file collects all 14 named tests (verify with `--collect-only`).
    - The foreign-claim and stamp tests assert against `mock_asyncpg_pool.calls` (real recorder),
      not a mocked helper.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_141_run_scope.py -q</automated>
  </verify>
  <done>The test file exists, collects all named tests, is green (helper/migration tests pass, Plan-02 behaviors xfail-pending), and is the RED backstop Plan 02 flips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author → live DB schema | A tampered/copy-pasted migration (NOT NULL/DEFAULT) permanently alters access-control behavior for every legacy row |
| test harness → resolver contract | The offline test set is the executable authorization spec; a weak/proxy assertion would let a real leak ship green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-06 | Tampering / DoS | migration 092 run_claim column | mitigate | `test_migration_092_additive_nullable` asserts nullable, no default, no backfill, `IF NOT EXISTS`; forbids `NOT NULL`/`DEFAULT` on run_claim (Pitfall 3) — prevents permanent cross-context block |
| T-141-04 | Information Disclosure | claim_visible truth table | mitigate | Truth-table tests pin all 5 directions (3 blocked, 2 allowed) so a widened/weakened predicate is caught before ship |
| T-141-SC | Tampering (supply chain) | package installs | accept | No npm/pip/cargo installs in this phase — pure backend + SQL migration; nothing to slopcheck |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_141_run_scope.py -q` — green (helpers + migration pass; Plan-02 behaviors xfail).
- Migration file present under `supabase/migrations/092_*.sql`; `supabase/full-schema.sql` NOT modified in this plan.
- No package installs; no watch-mode flags in the test file.
</verification>

<success_criteria>
- Migration 092 authored: nullable text `run_claim`, no default, no backfill, no new RLS policy (D-141-02, D-141-04).
- `claim_visible` + `own_claim_for_ctx` helpers exist, keyed off `workflow_run_id` (D-141-01).
- `test_141_run_scope.py` collects all named tests and is the executable fail-before/pass-after repro (D-141-06).
</success_criteria>

<output>
Create `.planning/phases/141-template-input-resolver-run-scope-stretch/141-01-SUMMARY.md` when done.
</output>
