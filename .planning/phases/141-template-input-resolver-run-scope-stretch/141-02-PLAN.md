---
phase: 141-template-input-resolver-run-scope-stretch
plan: 02
type: execute
wave: 2
depends_on: [141-01]
files_modified:
  - backend/app/services/template_asset_service.py
  - backend/app/services/tool_dispatcher.py
  - backend/app/services/harness/phase_types.py
autonomous: true
requirements: [COLL-02]

must_haves:
  truths:
    - "COLL-02: resolve_template_source Branch 2 filters ephemeral rows on the run-lineage claim (`run_claim IS NULL OR run_claim = own`) and stamps a NULL-claim row with the caller's own-claim on first resolve (D-141-03)"
    - "COLL-02: a foreign-claimed non-expired template row yields the honest 'belongs to a different run' relay string — never bytes, never the other run's id/filename (D-141-05)"
    - "COLL-02: same-mode reuse is preserved — Deep→Deep and same-workflow_run-across-phases still resolve; only cross-context (workflow→Deep, Deep→workflow, W1→W2) is blocked (D-141-01)"
    - "COLL-02: BOTH Branch-2 resolve callers derive and pass the same own-claim, and the harness emit `_ProducerStreamCtx` carries `workflow_run_id` so a workflow emit render claims `str(W)`, never 'deep' (Landmine 2)"
  artifacts:
    - path: "backend/app/services/template_asset_service.py"
      provides: "Branch 2 claim WHERE predicate + conditional stamp + foreign-claim honest error"
      contains: "run_claim IS NULL"
    - path: "backend/app/services/tool_dispatcher.py"
      provides: "own_claim derivation passed into resolve_template_source at the render handler"
      contains: "own_claim"
    - path: "backend/app/services/harness/phase_types.py"
      provides: "emit-path own_claim + _ProducerStreamCtx workflow_run_id stamp"
      contains: "workflow_run_id ="
  key_links:
    - from: "backend/app/services/tool_dispatcher.py"
      to: "backend/app/services/template_asset_service.py"
      via: "own_claim=own_claim_for_ctx(ctx) kwarg on resolve_template_source"
      pattern: "own_claim"
    - from: "backend/app/services/harness/phase_types.py"
      to: "backend/app/services/tool_dispatcher.py"
      via: "_ProducerStreamCtx.workflow_run_id stamp read by _handle_render_template"
      pattern: "workflow_run_id ="
---

<objective>
Wire the run-lineage claim end-to-end: teach `resolve_template_source` Branch 2 to filter,
stamp, and honestly reject on the claim, and thread the own-claim through BOTH Branch-2
callers — including the harness emit `_ProducerStreamCtx` (Landmine 2) so a workflow emit
render claims `str(workflow_run_id)`, never the `'deep'` sentinel.

Purpose: COLL-02 — close the cross-context template leak (workflow↔Deep, W1↔W2) while
preserving same-mode reuse (Deep→Deep, same-workflow-run across phases). This plan flips the
RED tests authored in Plan 01 to GREEN.

Output: claim-aware Branch 2 in `template_asset_service.py`; own-claim wiring in
`tool_dispatcher.py` and `harness/phase_types.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-CONTEXT.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-RESEARCH.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-VALIDATION.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-01-SUMMARY.md

@backend/app/services/template_asset_service.py
@backend/app/services/harness/emitters.py

<interfaces>
<!-- Contracts this plan consumes (from Plan 01) and the three Branch-2 seams it edits. -->

From Plan 01 (backend/app/services/template_asset_service.py):
  claim_visible(row_claim: str | None, own_claim: str) -> bool
  own_claim_for_ctx(ctx) -> str          # str(ctx.workflow_run_id) or "deep"
  DEEP_CLAIM = "deep"

resolve_template_source signature TODAY (:81-98) — add `own_claim: str | None = None`:
  async def resolve_template_source(*, pool, supabase, thread_id, user_id,
                                    asset_ref=None) -> dict
  Branch 1 (asset_ref is not None, :99-134) — library path, UNCHANGED (ignore own_claim).
  Branch 2 (:136-210) — ephemeral SELECT (:141-155) + two-query expired/never probe (:157-186).

Caller 1 — tool_dispatcher.py:2051 (_handle_render_template):
  src = await resolve_template_source(pool=ctx.pool, supabase=ctx.supabase,
        thread_id=ctx.thread_id, user_id=ctx.current_user["id"], asset_ref=asset_ref)
  ctx here is a ToolContext (Deep: workflow_run_id=None; sub-agent: inherited W;
  emit re-dispatch: a _ProducerStreamCtx — see Landmine 2).

Caller 2 — phase_types.py:1105 (_exec_llm_emit, direct pre-resolve):
  ctx is the RAW harness bag → ctx.run_id IS workflow_runs.id (verified :354).
  The bag has NO workflow_run_id attr → own_claim MUST be str(ctx.run_id) here.

Landmine 2 — phase_types.py:1026-1049 (_ProducerStreamCtx) + :1398 (construction):
  _render_ctx = _ProducerStreamCtx(ctx, producer_id)  # overrides run_id to producer id,
  __getattr__ delegates all else to the bag (which has no workflow_run_id) → naive
  getattr(_render_ctx, "workflow_run_id", None) returns None → mis-claims 'deep'.

Caller 3 (do NOT touch) — workflow_authoring.py:240 — Branch 1 only (asset_ref always set,
  thread_id=""); the optional own_claim default leaves it untouched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Claim-aware Branch 2 — WHERE filter + conditional stamp + honest foreign-claim error</name>
  <files>backend/app/services/template_asset_service.py</files>
  <behavior>
    - Resolver called with own_claim="deep" and a NULL-claim row → returns bytes AND stamps run_claim="deep" via a conditional UPDATE (own-claim recorded in pool.calls).
    - Called with own_claim=str(W) and a row already run_claim=str(W) → returns bytes, NO stamp fires.
    - Called with own_claim="deep" and the only non-expired row is run_claim=str(W) (foreign) → returns the "belongs to a different run" honest error string, bytes=None, no id/filename of the foreign run leaked.
    - Claim-aware WHERE still carries created_by ($2) and thread_id ($1) — scope never widened.
    - own_claim=None (Branch-1-only / legacy test callers) → pre-141 behavior, no claim predicate, no stamp (existing test_workspace_template.py Branch-2 tests stay green).
  </behavior>
  <read_first>
    - backend/app/services/template_asset_service.py:81-210 (the whole resolver — Branch 2 SELECT :141-155, two-query probe :157-186, envelope)
    - .planning/phases/141-template-input-resolver-run-scope-stretch/141-RESEARCH.md (Pitfall 2 race, Pitfall 4 newest-ELIGIBLE-wins probe ordering, Security Domain)
    - backend/tests/test_141_run_scope.py (the RED tests this task flips: test_resolver_stamps_unclaimed, test_resolver_foreign_claim_honest_error, test_in_scope_render_unchanged, test_where_preserves_user_and_thread_scope)
  </read_first>
  <action>
    Add `own_claim: str | None = None` to `resolve_template_source`. Leave Branch 1 (:99-134)
    untouched — it ignores own_claim. In Branch 2: (a) add `, run_claim` to the SELECT projection
    (:143-148); (b) add the claim predicate to the WHERE as `AND (run_claim IS NULL OR run_claim = $3 OR $3 IS NULL)`
    binding `own_claim` as `$3` — the `OR $3 IS NULL` arm makes own_claim=None a no-op (pre-141
    behavior, single static query, no dynamic SQL). This mirrors the pure `claim_visible` helper.
    When a row is returned and `own_claim is not None` and `row["run_claim"] is None`, stamp it
    with a CONDITIONAL update: `await pool.execute("UPDATE workspace_files SET run_claim = $1 WHERE id = $2 AND run_claim IS NULL", own_claim, row_id)`
    and inspect the returned status. On `UPDATE 0` (race — Pitfall 2), re-SELECT that row's
    run_claim: if it now equals own_claim proceed; if it is foreign, fall through to the
    foreign-claim error path (do NOT return bytes). Extend the row-not-found probe chain (:157-186)
    with a NEW probe ordered eligible→FOREIGN→expired→never (Pitfall 4): when `own_claim is not None`
    and the eligible SELECT returned nothing, run a probe for a non-expired row whose
    `run_claim IS NOT NULL AND run_claim <> $3`; if one exists, return the envelope with
    `provenance="template_input"`, `bytes=None`, and `error="This template belongs to a different run or context. Upload it again for this run."`
    (D-141-05 — name the condition ONLY; never include the foreign run's id, filename, or bytes).
    Preserve the existing expired-vs-never distinction after the foreign probe. Do NOT add
    `run_in_threadpool` — the stamp is a non-blocking asyncpg `pool.execute` (CLAUDE.md D-v2.5-01
    satisfied). Keep `created_by`/`thread_id` scopes in every probe query (V4 — never widen).
  </action>
  <acceptance_criteria>
    - `test_resolver_stamps_unclaimed` passes: bytes returned + conditional
      `UPDATE ... SET run_claim ... WHERE ... AND run_claim IS NULL` with own_claim in `pool.calls`;
      no stamp for an already-own-claim row.
    - `test_resolver_foreign_claim_honest_error` passes: foreign-claimed non-expired row →
      error string (not bytes); assert the error contains no foreign id/filename.
    - `test_in_scope_render_unchanged` passes: own-upload (Branch 2) + library (Branch 1) resolve
      byte-identically to pre-141.
    - `test_where_preserves_user_and_thread_scope` passes (source assertion: Branch 2 SELECT still
      binds thread_id + created_by and now carries the run_claim predicate).
    - `test_resolver_claim_race_falls_through` passes (T-141-03 mitigation): on a losing stamp
      (`UPDATE 0`) the resolver re-SELECTs and, when the row is now foreign-claimed, returns the
      honest "belongs to another run" error — never bytes.
    - Full quick file green: `pytest tests/test_141_run_scope.py -x -q` (drop the xfail markers for
      the five tests above).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_141_run_scope.py -x -q -k "resolver or in_scope or preserves_user"</automated>
  </verify>
  <done>Branch 2 filters + stamps + honestly rejects on the claim; same-mode reuse preserved; scopes intact; four resolver tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread own-claim through the Deep/sub-agent render handler (tool_dispatcher.py:2051)</name>
  <files>backend/app/services/tool_dispatcher.py</files>
  <behavior>
    - _handle_render_template derives own_claim via own_claim_for_ctx(ctx) and passes it to resolve_template_source.
    - Deep ctx (workflow_run_id=None) → own_claim="deep"; sub-agent ctx (workflow_run_id=W inherited) → str(W); emit re-dispatch ctx (_ProducerStreamCtx with stamped workflow_run_id, from Task 3) → str(W).
  </behavior>
  <read_first>
    - backend/app/services/tool_dispatcher.py:1925-2074 (_handle_render_template; the resolve call at :2051; the lazy import at :1964)
    - backend/app/services/tool_dispatcher.py:77-142 (ToolContext.workflow_run_id :125)
    - backend/app/services/template_asset_service.py (own_claim_for_ctx — Plan 01)
    - backend/tests/test_141_run_scope.py (test_both_branch2_resolve_sites_pass_own_claim)
  </read_first>
  <action>
    In `_handle_render_template`, extend the lazy import at :1964 to also import
    `own_claim_for_ctx` from `app.services.template_asset_service`. Immediately before the resolve
    call at :2051 derive `own_claim = own_claim_for_ctx(ctx)` and add `own_claim=own_claim` to the
    `resolve_template_source(...)` kwargs. Because `own_claim_for_ctx` keys off
    `ctx.workflow_run_id`, this one line is correct for all three ctx shapes that reach this
    handler: Deep (None→"deep"), workflow llm_agent sub-agent (inherited W→str(W)), and the emit
    re-dispatch (a `_ProducerStreamCtx` whose `workflow_run_id` is stamped in Task 3). Do NOT read
    tool args for lineage (D-141 / Tampering — the claim is server-derived only). No other change
    to the handler.
  </action>
  <acceptance_criteria>
    - `test_both_branch2_resolve_sites_pass_own_claim` passes (source assertion: the :2051 resolve
      call site includes `own_claim=`).
    - Source assertion: `own_claim` is derived from `ctx` (via own_claim_for_ctx), never from
      `args`/tool input.
    - Quick file stays green.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_141_run_scope.py -x -q -k "resolve_sites or own_claim"</automated>
  </verify>
  <done>The Deep/sub-agent render handler derives and passes own_claim from ctx.workflow_run_id; source-assertion test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit-path own-claim + _ProducerStreamCtx workflow_run_id stamp (Landmine 2)</name>
  <files>backend/app/services/harness/phase_types.py</files>
  <behavior>
    - _exec_llm_emit's direct resolve (:1105) passes own_claim=str(ctx.run_id) (the raw harness bag's run_id IS workflow_runs.id) — always a workflow lineage, never "deep".
    - _render_ctx (_ProducerStreamCtx, :1398) carries workflow_run_id set from the raw bag's run_id, so the re-dispatch's own_claim_for_ctx(_render_ctx) yields str(W).
    - test_emit_ctx_carries_workflow_lineage: a _ProducerStreamCtx wrapping SimpleNamespace(run_id=W) yields own_claim_for_ctx == str(W), never "deep".
  </behavior>
  <read_first>
    - backend/app/services/harness/phase_types.py:59 (the module-top `from app.services.template_asset_service import resolve_template_source` import to extend)
    - backend/app/services/harness/phase_types.py:1026-1049 (_ProducerStreamCtx proxy)
    - backend/app/services/harness/phase_types.py:1090-1125 (_exec_llm_emit direct resolve at :1105; run_id at :1093)
    - backend/app/services/harness/phase_types.py:1360-1401 (_render_ctx construction at :1398; the post_processor re-dispatch)
    - backend/app/services/harness/phase_types.py:340-359 (_build_phase_tool_context — proof ctx.run_id IS workflow_runs.id :354)
    - backend/app/services/harness/emitters.py:98-176 (_render_template_post — the re-dispatch into _handle_render_template)
    - .planning/phases/141-template-input-resolver-run-scope-stretch/141-RESEARCH.md (Landmine 1 & 2, Pitfall 1)
  </read_first>
  <action>
    Two edits in `phase_types.py`, both in the emit path. (1) At the direct pre-resolve (:1105),
    extend the module-top import at line 59 to also import `own_claim_for_ctx`, but do NOT call
    `own_claim_for_ctx(ctx)` here — the raw harness bag has NO `workflow_run_id` attr (Landmine
    1/2), so derive `own_claim = str(getattr(ctx, "run_id", None))` (the bag's run_id IS
    workflow_runs.id, always a workflow phase on this path, never "deep") and add
    `own_claim=own_claim` to the `resolve_template_source(...)` kwargs. Guard the None edge: if
    run_id is unexpectedly None (minimal/unit ctx), pass own_claim=None (Branch 1 bound-asset path
    ignores it anyway). (2) At `_render_ctx` construction (:1398), after building the
    `_ProducerStreamCtx`, stamp `_render_ctx.workflow_run_id = getattr(ctx, "run_id", None)` from
    the RAW bag (its run_id = workflow_runs.id). A locally-set attribute short-circuits the proxy's
    `__getattr__`, so `own_claim_for_ctx(_render_ctx)` inside the re-dispatched
    `_handle_render_template` (Task 2) now yields `str(W)`, matching the direct-resolve own_claim —
    both emit-path resolves stamp/filter with the SAME lineage (Landmine 1 consistency). Do NOT
    touch `threads.py`, `pin_templates_for_run`, or `template_service.py` (G-5). Do NOT stamp
    `run_id` for lineage — it is the producer id on the proxy; use the bag's `run_id` explicitly.
  </action>
  <acceptance_criteria>
    - `test_emit_ctx_carries_workflow_lineage` passes: `_ProducerStreamCtx(SimpleNamespace(run_id=W))`
      yields `own_claim_for_ctx(...) == str(W)`, never "deep" (the single most likely green-but-broken
      failure — Pitfall 1).
    - Source assertion: the emit direct resolve (:1105) passes `own_claim=` derived from `ctx.run_id`;
      `_render_ctx.workflow_run_id` is assigned from the raw bag's run_id at :1398.
    - Full quick file green (all xfail markers dropped): `pytest tests/test_141_run_scope.py -x -q`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_141_run_scope.py -x -q</automated>
  </verify>
  <done>Both emit-path resolves derive str(W); _ProducerStreamCtx carries workflow_run_id; the Landmine-2 guard test is green and the whole quick file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → render_template tool args | Untrusted model input crosses here; the claim MUST NOT be derived from tool args |
| run-context → resolver | The new authorization boundary: workflow-run W vs Deep vs W1/W2 — a foreign lineage must be invisible |
| resolver → agent context (error relay) | The failure string crosses back to the model/user; it must not carry foreign bytes/ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Information Disclosure | resolve_template_source Branch 2 WHERE | mitigate | Claim predicate `run_claim IS NULL OR run_claim = own`, symmetric via the `'deep'` sentinel — foreign-lineage rows invisible (Task 1) |
| T-141-02 | Tampering | own-claim derivation (all 3 callers) | mitigate | own-claim derived from `ctx.workflow_run_id` / raw-bag `ctx.run_id` (server substrate) — never from tool args (Tasks 2, 3) |
| T-141-03 | Elevation / Info Disclosure | claim-stamp race | mitigate | Conditional `UPDATE ... WHERE run_claim IS NULL` + rowcount re-check; on UPDATE 0 re-SELECT and fall to foreign-claim error (Task 1, Pitfall 2) |
| T-141-04 | Information Disclosure | scope-widen regression | mitigate | `created_by` + `thread_id` remain in every Branch-2 probe query; `test_where_preserves_user_and_thread_scope` backstop (Task 1) |
| T-141-05 | Information Disclosure | honest foreign-claim error | mitigate | D-141-05 message names the condition only — never the other run's id/filename/bytes (Task 1) |
| T-141-08 | Information Disclosure | Landmine 2 — emit render mis-claims 'deep' | mitigate | `_ProducerStreamCtx.workflow_run_id` stamped from raw-bag run_id; guard test asserts str(W) (Task 3) |
| T-141-SC | Tampering (supply chain) | package installs | accept | No npm/pip/cargo installs — pure code edits; nothing to slopcheck |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_141_run_scope.py -x -q` — fully green (all xfail markers dropped).
- Wave-merge blast-radius: `cd backend && venv/Scripts/python -m pytest tests/test_141_run_scope.py tests/test_workspace_template.py tests/unit/test_citation_policy.py tests/unit/test_llm_emit_executor.py -q` — green (no resolver-neighbor regression).
- Source: `threads.py`, `template_service.py`, `pin_templates_for_run` untouched (G-5); Branch 1 unchanged.
</verification>

<success_criteria>
- Branch 2 filters + stamps + honestly rejects on the run-lineage claim; Deep→Deep and same-workflow reuse preserved (D-141-01, D-141-03, D-141-05).
- Both Branch-2 callers derive the same own-claim; the emit `_ProducerStreamCtx` carries workflow_run_id so a workflow emit render claims str(W) (Landmine 1 & 2).
- All twelve+ VALIDATION.md offline tests green; no G-5 hot-file growth.
</success_criteria>

<output>
Create `.planning/phases/141-template-input-resolver-run-scope-stretch/141-02-SUMMARY.md` when done.
</output>
