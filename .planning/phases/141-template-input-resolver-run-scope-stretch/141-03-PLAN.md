---
phase: 141-template-input-resolver-run-scope-stretch
plan: 03
type: execute
wave: 3
depends_on: [141-01, 141-02]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [COLL-02]

must_haves:
  truths:
    - "COLL-02: migration 092 is applied to the live LOCAL Supabase DB by hand (psycopg2 :54322 / SQL editor), never db push / db reset (D-141-02)"
    - "COLL-02: supabase/full-schema.sql is regenerated (no --reset) and committed together with migration 092"
    - "COLL-02: the live workspace_files.run_claim column exists as nullable text with no default, so the resolver's claim filter + stamp run against the real schema (D-141-02, D-141-04)"
    - "COLL-02: after the live apply, the cross-provider render SMOKE (ONE representative model, not the SC#10 4-axis) renders an in-scope template with no regression (D-141-07)"
    - "COLL-02: cloud-parity is recorded — migration 092 must be applied to cloud Supabase by hand at deploy (scripts/pending-cloud-migrations.sh lists it)"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "Regenerated single-file deploy artifact including workspace_files.run_claim"
      contains: "run_claim"
  key_links:
    - from: "supabase/migrations/092_workspace_files_run_claim.sql"
      to: "supabase/full-schema.sql"
      via: "regenerate-full-schema.sh (live-DB dump, no reset) after by-hand apply"
      pattern: "run_claim"
---

<objective>
Apply migration 092 to the live LOCAL Supabase DB by hand, regenerate the full-schema
deploy artifact (no reset), commit both together, and record the cloud-parity follow-up.

Purpose: build/type checks pass WITHOUT the live column (nothing type-checks against a live
column and the offline tests use a mock pool), so phase verification would false-positive
until 092 is actually applied. This BLOCKING plan makes the run-scope claim real in the DB
the resolver queries at runtime, mirroring the Phase 120 (migration 076) / Phase 140
(migration 091) author→apply split.

Output: applied migration on `:54322`, regenerated `supabase/full-schema.sql`, one commit of
migration + schema, and a cloud-parity note.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-CONTEXT.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-RESEARCH.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-VALIDATION.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-01-SUMMARY.md
@.planning/phases/141-template-input-resolver-run-scope-stretch/141-02-SUMMARY.md

@supabase/migrations/092_workspace_files_run_claim.sql

<notes>
- CLAUDE.md migration process: apply BY HAND (paste into Supabase SQL editor OR psycopg2 to
  local :54322), NEVER `supabase db push` / `db reset` (that wipes dev data). Then
  `bash scripts/regenerate-full-schema.sh` (default = live-DB dump, NO reset). NEVER hand-edit
  full-schema.sql. Commit migration + regenerated schema together.
- MEMORY (Phase 140): migration 091 was applied via psycopg2 on :54322; the evidence-tool
  convention is psycopg2 to the local Supabase Postgres.
- Cloud parity: migration 092 joins the 079–086 pending-cloud set; `scripts/pending-cloud-migrations.sh`
  lists migrations not yet on origin/production. This is a NOTE for deploy time, not a task to run now.
- Python phase execution landmine (MEMORY project_140_executed): run worktrees-OFF / sequential
  main-tree — the venv lives only in the main checkout.
- Cross-provider render SMOKE (D-141-07, one representative model, manual UAT) lives in
  141-VALIDATION.md "Manual-Only Verifications" — it becomes runnable once this apply lands.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration 092 to live LOCAL DB + regenerate full-schema.sql</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - supabase/migrations/092_workspace_files_run_claim.sql (the file to apply — authored in Plan 01)
    - scripts/regenerate-full-schema.sh (the no-reset live-DB dump regenerator)
    - .planning/phases/141-template-input-resolver-run-scope-stretch/141-RESEARCH.md (Environment Availability — :54322 psycopg2 apply)
  </read_first>
  <action>
    Apply `supabase/migrations/092_workspace_files_run_claim.sql` to the live LOCAL Supabase
    Postgres on port 54322 via psycopg2 (the project's established apply/evidence tool — mirror
    the Phase 140 migration 091 apply). Do NOT use `supabase db push` or `supabase db reset`
    (either wipes dev data). After apply, verify the column exists by querying
    `information_schema.columns` for `table_name='workspace_files' AND column_name='run_claim'` and
    confirm `is_nullable='YES'`, `data_type='text'`, and `column_default IS NULL`. Then run
    `bash scripts/regenerate-full-schema.sh` (NO `--reset`) to rebuild `supabase/full-schema.sql`
    from the live DB. Confirm the regenerated `full-schema.sql` now contains a `run_claim` column
    on `workspace_files`. Do NOT hand-edit `full-schema.sql`.
  </action>
  <acceptance_criteria>
    - Live-DB assertion: `information_schema.columns` shows `workspace_files.run_claim` as
      nullable text with NULL default (psycopg2 query output).
    - `supabase/full-schema.sql` contains `run_claim` under the workspace_files table definition.
    - No `db push` / `db reset` was invoked (dev data intact — existing thread/message rows still present).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import psycopg2; c=psycopg2.connect(host='127.0.0.1',port=54322,dbname='postgres',user='postgres',password='postgres'); cur=c.cursor(); cur.execute(\"select is_nullable,data_type,column_default from information_schema.columns where table_name='workspace_files' and column_name='run_claim'\"); r=cur.fetchone(); assert r and r[0]=='YES' and r[1]=='text' and r[2] is None, r; print('run_claim OK', r)"</automated>
  </verify>
  <done>Migration 092 applied live (nullable text run_claim, NULL default), full-schema.sql regenerated with the column, dev data preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator confirms the live apply + approves the migration/schema commit</name>
  <files>supabase/full-schema.sql</files>
  <action>
    Pause for the operator to confirm the live apply and approve the commit. Present the psycopg2
    verify output (`run_claim OK ('YES','text',None)`) and `git diff --stat` showing exactly the
    new `supabase/migrations/092_workspace_files_run_claim.sql` (from Plan 01) and the regenerated
    `supabase/full-schema.sql`. On approval, commit both together with message
    `feat(141): migration 092 run_claim + regen full-schema (COLL-02)`, and record the cloud-parity
    follow-up (092 must be applied to cloud Supabase by hand at deploy — `scripts/pending-cloud-migrations.sh`
    lists it). Do NOT push to master/production.
  </action>
  <what-built>
    Migration 092 applied to the live LOCAL DB via psycopg2 (:54322) and `supabase/full-schema.sql`
    regenerated (no reset). The resolver's claim filter + stamp now run against the real
    workspace_files.run_claim column.
  </what-built>
  <how-to-verify>
    1. Confirm the psycopg2 verify (Task 1) printed `run_claim OK ('YES','text',None)`.
    2. `git diff --stat` shows exactly `supabase/migrations/092_workspace_files_run_claim.sql` (new,
       from Plan 01) and `supabase/full-schema.sql` (regenerated) — no hand-edits elsewhere.
    3. Approve the commit of the migration + regenerated full-schema together.
    4. Acknowledge the cloud-parity follow-up (deploy-time by-hand apply to cloud Supabase).
    5. Optional lived-glance: run the D-141-07 cross-provider render SMOKE (one representative model
       uploads a .docx in a Deep turn and renders it) — deliverable produced, in-scope, byte-correct.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the psycopg2 column check + `git diff --stat` scope, then approves the commit.</human-check>
  </verify>
  <resume-signal>Type "approved" to commit, or describe issues.</resume-signal>
  <done>Operator-approved commit of migration 092 + regenerated full-schema.sql; cloud-parity follow-up recorded; no push to deploy branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → live LOCAL DB | A wrong apply mechanism (`db push`/`db reset`) would wipe dev data; the by-hand psycopg2/editor path is the only safe route |
| local schema → cloud schema | Cloud drifts from local — 092 must be applied to cloud by hand at deploy or the resolver leaks on prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-07 | Denial of Service / Tampering | migration apply mechanism | mitigate | Apply by hand via psycopg2 :54322 (never `db push`/`db reset`); blocking operator checkpoint confirms column + intact dev data before commit |
| T-141-09 | Information Disclosure | cloud schema drift | mitigate | Cloud-parity note recorded; 092 added to the pending-cloud set for by-hand deploy apply (prod resolver otherwise queries a missing column) |
| T-141-SC | Tampering (supply chain) | package installs | accept | No installs — SQL apply + schema dump only |
</threat_model>

<verification>
- psycopg2 live-DB check: `workspace_files.run_claim` is nullable text, NULL default.
- `supabase/full-schema.sql` regenerated (no reset) and contains run_claim; committed with migration 092.
- Cloud-parity follow-up recorded (deploy-time by-hand apply).
</verification>

<success_criteria>
- Migration 092 live on :54322, dev data intact, full-schema regenerated + committed together (D-141-02, D-141-04).
- Blocking operator checkpoint approved the apply + commit.
- Cross-provider render SMOKE (D-141-07, one representative model, manual UAT in 141-VALIDATION.md) is now runnable against the real column.
</success_criteria>

<output>
Create `.planning/phases/141-template-input-resolver-run-scope-stretch/141-03-SUMMARY.md` when done.
</output>
