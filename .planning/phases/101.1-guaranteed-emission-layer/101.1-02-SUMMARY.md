---
phase: 101.1-guaranteed-emission-layer
plan: 02
subsystem: api
tags: [provider-gateway, forced-emission, cross-provider, tool-choice, structured-output, anthropic, google, openai, deepseek, moonshot, native-recovery]

# Dependency graph
requires:
  - phase: 101.1-guaranteed-emission-layer (Plan 01)
    provides: flat EmitFieldMap + emit_field_map_to_legacy + EMITTER_REGISTRY + LlmEmitPhaseConfig + the two default-SAFE D-15 MODEL_CAPABILITIES flags (forced_emission / strict_json_schema) + the Wave-0 RED test stubs
  - phase: 092.5-provider-gateway-extraction
    provides: GatewayRequest envelope (carried tool_choice) + open_stream dispatcher + the 3 native adapters (anthropic/google/openai_compat) — the D-14 forcing-seam home
provides:
  - GatewayRequest.force_tool_name + GatewayRequest.strict_schema (additive, default-SAFE — the auto path is byte-identical)
  - Per-provider forcing translation in all 3 native adapters (anthropic tool_choice={type:tool,name}; google tool_config(mode=ANY); openai_compat named tool_choice + strict response_format)
  - stream_anthropic / stream_google / create_adaptive_streaming_chat each gain ONE additive forcing param (G-5 additive seam, NOT an extraction)
  - forced_emit.py — the sealed single forced-shot substrate (D-08 layers 1-4) + recover_narrated_emission (D-06 NATIVE narrated-JSON recovery)
  - is_truncated now accepts stop_reason=/finish_reason= kwargs alongside the legacy positional meta dict (call-shape reconcile)
  - The D-14 shared-path guard test GREEN (no per-provider forcing branch in the shared chunk/SSE path; Deep byte-identical)
affects: [101.1-03 llm_emit executor, 101.1-04 gate/driver parity, 101.1-05 operator apply+seed, 102 llm_judge, 103 NL-authoring, 104 flagship templates, 107 receipt VIEW]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Capability-tiered forcing at the gateway boundary ONLY (D-05) — the forcing decision rides GatewayRequest.force_tool_name; each native adapter translates it to its provider shape; the shared chunk/SSE path is NEVER branched (D-14)"
    - "Additive forcing param per raw-SDK stream constructor (stream_anthropic tool_choice / stream_google tool_config / create_adaptive_streaming_chat force_tool_name+strict_response_format) — default None/False preserves the byte-identical auto path (G-5 additive seam, not an extraction)"
    - "Sealed single forced shot (forced_emit drives open_stream ONCE, never the open agent loop) — the D-01/Pitfall-5 isolation that fixes GAP-A"
    - "D-06 parse-back-or-fail-honestly: a still-narrated NATIVE emission is recovered (fenced/tool-wrapped EmitFieldMap → model_validate) or fails honestly — never a silent drop, never prose-as-artifact (GAP-D fix)"
    - "Default-SAFE tier resolution via get_model_capability(id).get('forced_emission', False) — a case-sensitivity registry miss coerces, never wrongly forces"

key-files:
  created:
    - backend/app/services/forced_emit.py
  modified:
    - backend/app/services/provider_gateway/dispatcher.py
    - backend/app/services/provider_gateway/anthropic.py
    - backend/app/services/provider_gateway/google.py
    - backend/app/services/provider_gateway/openai_compat.py
    - backend/app/services/anthropic_service.py
    - backend/app/services/google_service.py
    - backend/app/services/openai_service.py
    - backend/app/services/template_render_service.py
    - backend/tests/unit/test_gateway_forcing.py
    - backend/tests/unit/test_forced_emit.py

key-decisions:
  - "D-14 routing HONORED: forcing landed as a tightly-scoped ADDITIVE gateway seam, NOT an anthropic_service.py extraction. anthropic_service.py grew by exactly ONE param (tool_choice); the G-5 hot-file debt is satisfied in spirit (the adapter boundary holds the forcing logic) without a risky 4-file extraction. The D-14 guard test ships GREEN."
  - "Deviation (Rule 3): google_service.stream_google + openai_service.create_adaptive_streaming_chat each gained ONE additive forcing param (symmetric to the anthropic tool_choice param) — required so the adapter can inject forcing without branching the shared chunk handler. Both default to the byte-identical auto path."
  - "Deviation (Rule 3): is_truncated reconciled to accept stop_reason=/finish_reason= kwargs alongside the legacy positional meta dict — the Wave-0 test_truncation_rejected stub asserts the kwargs form and Plan 01 SUMMARY flagged this for THIS plan to resolve. Backward-compatible (the existing dict-positional test_template_render call still passes)."
  - "recover_narrated_emission handles BOTH the tool-wrapped narration (via the existing parse_structured_tool_calls helper — D-06 reuse) AND a bare fenced EmitFieldMap object (the reasoning-native GAP-D shape). Either path MUST model_validate as EmitFieldMap to be accepted; else a clean honest None."

patterns-established:
  - "Pattern: forcing translation lives ONLY in the 3 gateway adapters; the consumer threads the decision via GatewayRequest and never branches per-provider for forcing (the D-14 guard test enforces this structurally)"
  - "Pattern: the emit step is a SEALED single shot (forced_emit) — never run_task_sub_agent / run_agent_loop (the open loop is the GAP-A root cause)"

requirements-completed: []  # TMPL-02 / TMPL-03 stay OPEN — mark complete at phase verification (live cross-provider UAT), the 099/WFSKILL-01 multi-plan convention

# Metrics
duration: ~11min
completed: 2026-06-11
---

# Phase 101.1 Plan 02: Cross-Provider Forcing Core Summary

**The capability-tiered forcing seam at the provider_gateway boundary (force_tool_name + strict_schema on GatewayRequest, translated per-provider in all 3 native adapters with Anthropic thinking-OFF / Gemini mode=ANY / OpenAI named tool_choice + strict response_format) plus the `forced_emit` sealed-single-shot substrate with D-06 NATIVE narrated-JSON recovery and a D-08 truncation guard — the seam that turns the GAP-A/GAP-D 0-docx failure into either a forced emission or an honest failure, with the D-14 shared-path guard proving Deep stays byte-identical and no per-provider forcing branch leaks into the shared chunk/SSE path.**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-06-11T07:55:46Z
- **Completed:** 2026-06-11T08:06:46Z
- **Tasks:** 2 (each TDD: RED test gate → GREEN feat)
- **Files modified:** 10 (1 created: forced_emit.py; 9 modified)

## Accomplishments

- **D-05 / D-14 — the forcing seam at the gateway boundary:** `GatewayRequest` gained `force_tool_name: str | None = None` + `strict_schema: bool = False` (additive — the SAFE defaults preserve the byte-identical `tool_choice="auto"` path). Each native adapter translates `force_tool_name` to its provider's native named-tool-forcing shape:
  - **anthropic** (`provider_gateway/anthropic.py`): `tool_choice={"type":"tool","name":<emitter>}` passed to `stream_anthropic`; thinking stays OFF (TIER-FORCE-NOTHINK — forcing ERRORS under extended reasoning). Unset → `tool_choice=None` → the :177 force_no_tools/auto resolution is byte-identical.
  - **google** (`provider_gateway/google.py`): `tool_config=types.ToolConfig(function_calling_config=types.FunctionCallingConfig(mode="ANY", allowed_function_names=[<emitter>]))` injected into `config_kwargs`; `automatic_function_calling` stays disabled (ANY is independent).
  - **openai_compat** (`provider_gateway/openai_compat.py`): named `tool_choice={"type":"function","function":{"name":<emitter>}}` + a strict `response_format` json_schema (built from the forced tool's `parameters`, which are already `additionalProperties:false` + all-required-with-null from D-09's EmitFieldMap) when `strict_schema`.
- **G-5 additive seam (NOT an extraction):** `anthropic_service.stream_anthropic` grew by EXACTLY one param (`tool_choice: dict | None = None`; default preserves :177). `google_service.stream_google` (one `tool_config` param) and `openai_service.create_adaptive_streaming_chat` (`force_tool_name` + `strict_response_format`) each gained additive params, all defaulting to the byte-identical auto path. The forced branch in `create_adaptive_streaming_chat` slots BESIDE the `:1352` auto branch (only reached when `force_tool_name is not None`).
- **D-01 / D-06 / D-08 — the `forced_emit` substrate** (`backend/app/services/forced_emit.py`, NEW): `forced_emit(...)` resolves the tier registry-first (default-SAFE), builds a `GatewayRequest` with `force_tool_name=<emitter>` for TIER-FORCE (or `tool_choice="auto"` + an explicit directive for TIER-COERCE — Kimi/Moonshot, never a code fallback), drives `open_stream` ONCE (sealed single shot — never the open loop), drains, applies the D-08 layer-4 truncation guard (`is_truncated`), and on the NATIVE path recovers a still-narrated emission via `recover_narrated_emission` (D-06) — else an honest `model_failed_to_emit`. Returns a structured `{emitted, tier, provider, forced, recovered_from_narration, truncated, failure}` the Plan-03 executor consumes for layers 5-6.
- **D-14 guard GREEN + Deep byte-identical:** `test_no_provider_branch_in_shared_path` asserts no `if provider ==`/`active_provider_name ==` forcing branch exists in the shared `agent_loop` consumer (forcing must thread through `open_stream`); `test_deep_request_byte_identical` proves a Deep `tool_choice="auto"` request carries `force_tool_name=None` and is unchanged by the existence of a forced request. `agent_loop.py` is byte-identical (`git diff --stat` empty).

## Task Commits

Each task was TDD (RED test gate → GREEN feat):

1. **Task 1 (RED): gateway-forcing tests** — `ab306330` (test)
2. **Task 1 (GREEN): capability-tiered forcing seam at the gateway boundary** — `6672626e` (feat)
3. **Task 2 (RED): forced-emit substrate tests + is_truncated reconcile** — `47cbe58b` (test)
4. **Task 2 (GREEN): forced_emit substrate** — `183ae315` (feat)

_TDD note: each task is a RED test commit (the failing contract) then a GREEN feat commit (the implementation that un-marks the Wave-0 stubs). No REFACTOR commit needed._

## Files Created/Modified

- `backend/app/services/forced_emit.py` (NEW) — the sealed single forced-shot substrate (D-08 layers 1-4) + `recover_narrated_emission` (D-06). Imports only the gateway + tool parser + config registry + the deterministic template-render core; NEVER imports `agent_loop`/`threads.py`/`task_service` (the open loop).
- `backend/app/services/provider_gateway/dispatcher.py` — `GatewayRequest` gained `force_tool_name` + `strict_schema` (additive, default-SAFE).
- `backend/app/services/provider_gateway/anthropic.py` — translates `force_tool_name` → `tool_choice={type:tool,name}`, thinking OFF.
- `backend/app/services/provider_gateway/google.py` — translates `force_tool_name` → `tool_config(function_calling_config(mode=ANY, allowed_function_names))`; imports `types` from `google.genai`.
- `backend/app/services/provider_gateway/openai_compat.py` — passes `force_tool_name` + `strict_schema` through to `create_adaptive_streaming_chat`.
- `backend/app/services/anthropic_service.py` — ONE additive param (`tool_choice: dict | None = None`); the :177 line now honors a caller-supplied tool_choice (G-5 additive seam).
- `backend/app/services/google_service.py` — ONE additive param (`tool_config`); injected into `config_kwargs` only when forced.
- `backend/app/services/openai_service.py` — `create_adaptive_streaming_chat` gained `force_tool_name` + `strict_response_format`; a forced branch beside the `:1352` auto branch.
- `backend/app/services/template_render_service.py` — `is_truncated` reconciled to accept kwargs alongside the legacy positional meta dict (backward-compatible).
- `backend/tests/unit/test_gateway_forcing.py` — 9 tests: forcing-field defaults, the D-14 shared-path guard, Deep byte-identical, per-adapter translation (anthropic/google/openai_compat), the anthropic_service additive-param shape, and default-SAFE-on-registry-miss.
- `backend/tests/unit/test_forced_emit.py` — 8 tests: D-06 recovery / honest-fail, truncation reject, the forced happy path, NATIVE narration recovered, unrecoverable narration honest-fail, truncation rejected, TIER-COERCE no-force, and the no-open-loop guard.

## Decisions Made

All implementation followed the LOCKED decisions D-01..D-15 and the plan's explicit D-14 routing decision (the additive gateway seam). The substantive judgement calls:

- **D-14 routing honored exactly as the plan prescribed:** the forcing seam is a tightly-scoped additive gateway seam, not an anthropic_service.py extraction. `anthropic_service.py` grew by exactly one param (proven by `git diff --stat` = 1 file, +12/-2). The G-5 hot-file debt is satisfied in spirit by confirming the adapter boundary holds the forcing logic + shipping the D-14 guard test.
- **recover_narrated_emission dual-strategy:** the test's narrated case is a BARE fenced `EmitFieldMap` (not a `{"tool","arguments"}` wrapper), so the recovery tries the tool-wrapped narration first (via `parse_structured_tool_calls` — the D-06 helper-reuse the acceptance criteria mandate) then a bare fenced object; either must `model_validate` as `EmitFieldMap`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added one additive forcing param to `google_service.stream_google` and two to `openai_service.create_adaptive_streaming_chat`**
- **Found during:** Task 1 (gateway forcing seam)
- **Issue:** The plan's `files_modified` listed only the 4 gateway-package files + `anthropic_service.py`. But `stream_google` builds its `config_kwargs` internally and `create_adaptive_streaming_chat` only handled `tool_choice == "auto"` — neither could receive a forcing decision from its adapter without an additive param. Injecting forcing any other way would have required branching the shared chunk handler (a D-14 violation).
- **Fix:** Added `tool_config: types.ToolConfig | None = None` to `stream_google` (symmetric to the anthropic `tool_choice` param) and `force_tool_name`/`strict_response_format` to `create_adaptive_streaming_chat` (a forced branch beside the `:1352` auto branch). All default to the byte-identical auto path; the openai forced branch is only reached when `force_tool_name is not None`.
- **Files modified:** `backend/app/services/google_service.py`, `backend/app/services/openai_service.py`
- **Verification:** `test_google_forcing_translation_mode_any`, `test_openai_compat_forcing_translation`, `test_openai_compat_forced_tool_choice_kwargs` GREEN; the 32 anthropic/google-service regression tests + the 17 gateway-seam/chunk-handler tests stay GREEN.
- **Committed in:** `6672626e` (Task 1 GREEN commit)

**2. [Rule 3 - Blocking] Reconciled `is_truncated` to accept `stop_reason=`/`finish_reason=` kwargs**
- **Found during:** Task 2 (forced_emit substrate)
- **Issue:** The shipped Wave-0 `test_truncation_rejected` stub calls `is_truncated(stop_reason="max_tokens")` (kwargs), but the production signature was `is_truncated(meta: dict)` (positional dict). Plan 01's SUMMARY "Issues Encountered" explicitly flagged this for THIS plan to resolve.
- **Fix:** Made `is_truncated` accept EITHER a positional `meta` dict (the legacy spike/citation/integrity callers) OR `stop_reason=`/`finish_reason=` kwargs (the new `forced_emit` caller, which has the two reasons directly after the drain). Backward-compatible.
- **Files modified:** `backend/app/services/template_render_service.py`
- **Verification:** the existing `test_template_render.py` (dict-positional) still passes (20/20); `test_truncation_rejected` (kwargs) now GREEN.
- **Committed in:** `47cbe58b` (Task 2 RED commit — the call-shape reconcile lands with the test that asserts it)

---

**Total deviations:** 2 auto-fixed (both Rule 3 — blocking). Both were the only way to inject forcing / satisfy the shipped test contract without violating the D-14 RED LINE or breaking back-compat. No scope creep — both are tiny additive, default-SAFE changes that keep every existing caller byte-identical.

**Impact on plan:** None on scope. The two source files touched beyond the plan's `files_modified` (`google_service.py`, `openai_service.py`) are the symmetric siblings of the anthropic seam the plan already prescribed — the plan's interface notes (`google_service.py:406 config_kwargs`, `openai_service.py:1352 auto branch`) anticipated exactly these injection points.

## How the D-14 guard proves Deep byte-identity + no shared-path branching

- **`test_no_provider_branch_in_shared_path`** greps the live `agent_loop` source: if it references `force_tool_name` at all it must thread it through `open_stream` (it does not reference it — forcing lives entirely in the gateway), and NO `if provider ==`/`active_provider_name ==` line mentions forcing. The shared chunk handler (`_ClosableEventStream._normalize` in `openai_compat.py`) is untouched — its only per-provider branches are the pre-existing `<think>`/usage ones.
- **`test_deep_request_byte_identical`** constructs a Deep `GatewayRequest(tool_choice="auto")` and asserts `force_tool_name is None` + `strict_schema is False`; it then constructs a forced anthropic request and re-asserts the Deep one is unchanged (each GatewayRequest is self-contained — forcing provider X never mutates provider Y).
- **`git diff --stat backend/app/services/agent_loop.py` is EMPTY** — the Deep loop is byte-identical. The forced branches in `create_adaptive_streaming_chat` / the adapters are only reached when `force_tool_name is not None`, which no Deep / pre-101.1 caller ever sets.

## Net-New Failure Proof (SEED-056 baseline)

- **Target modules GREEN:** `test_gateway_forcing.py` (9) + `test_forced_emit.py` (8) = **17 passed**. The wider target set (`+ test_provider_gateway_seam` + `test_chunk_handler_provider_aware` + `test_template_render` + `test_template_integrity` + `test_emit_field_map`) = **60 passed, 0 failed**.
- **Wave slice** (`-k "emit or render or template or gateway or harness or capabilit or config or forcing or anthropic or google or openai"`): `4 failed, 200 passed, 2 xfailed, 1 xpassed`. All 4 failures proven PRE-EXISTING SEED-056 rot via **base-checkout** (checked out `backend/app` + the 3 failing test files at the pre-plan base commit `b256a813` and ran them — all 4 failed IDENTICALLY, then restored the working tree to HEAD):
  - `test_get_model_capability_inference.py::test_infer_openai_from_gpt_prefix` — touches `config.py`, which Plan 02 did NOT modify (`git diff --stat b256a813 HEAD -- config.py` = untouched); the same `assert 300 == 90` drift Plan 01 documented.
  - `test_075_4_final_output_files_payload.py` (2) + `test_phase56_iteration_start.py` (1) — threads.py/output-files-payload test-vs-source drift; grep-confirmed NONE import any module Plan 02 changed (`forced_emit` / the gateway adapters / `stream_anthropic` / `stream_google` / `create_adaptive_streaming_chat`); all 3 test files are byte-identical to the base.
- **Net-new failures = 0.**

## Issues Encountered

- The `parse_structured_tool_calls` helper validates tool names against a `known_tools` set and expects a `{"tool","arguments"}` wrapper — but the narrated GAP-D case is a BARE `EmitFieldMap` fenced object. Resolved by making `recover_narrated_emission` a two-strategy parse (tool-wrapped first via the helper, then a bare fenced object), each validated against `EmitFieldMap`. The acceptance-criteria `parse_structured_tool_calls` grep (>=1) is satisfied (used for strategy 1).
- The `run_task_sub_agent`/`run_agent_loop` grep-guard initially tripped on the module DOCSTRING (which named the forbidden symbols to explain the D-01 isolation). Rephrased the docstring to avoid the literal tokens; the substrate genuinely never references the open loop (ast-guard + grep both 0).

## User Setup Required

None for this plan. The live cross-provider forced-emit behavior is exercised by Plan 05's operator UAT (apply migration 069, re-seed the D-13 fixture, run the 8 live-UAT rows + the SC#10 4-axis scoreboard). The sandbox image rebuild/bump to `101.1` is also a Plan-05 operator step.

## Next Phase Readiness

- The cross-provider forcing core is complete and unit-proven: the emit step can now be FORCED on the 6/8 TIER-FORCE providers, runs thinking-OFF on Anthropic, and hard-validates/recovers/honest-fails on the rest.
- **Plan 03 (`_exec_llm_emit` executor):** the 6th harness executor calls `forced_emit` (NOT the open loop), runs the D-08 layers 5-6 (bounded retry via `_retry_suffix` / honest-fail via `_surface_failure_message`) on a `failure="model_failed_to_emit"` result, wires the `EmitterEntry.post_processor` render-dispatch, injects the bound AssetRef (GAP-B / D-10), and emits the GAP-C sub-events + the D-12 audit receipt.
- **Plan 04 (gate/driver parity):** re-touches `_iter_leaves`/`build_context` for the flat shape; `forced_emit`'s `emitted` is an `EmitFieldMap` that `emit_field_map_to_legacy` (Plan 01) maps to the legacy shape the gate/driver consume.
- **Plan 05 (operator):** applies migration 069, regenerates `full-schema.sql`, re-seeds D-13, runs the live cross-provider UAT.

## Self-Check: PASSED

- Created/key files exist: `forced_emit.py`, `dispatcher.py`, `test_gateway_forcing.py`, `test_forced_emit.py`, `101.1-02-SUMMARY.md` — all FOUND.
- Task commits exist: `ab306330` (test), `6672626e` (feat), `47cbe58b` (test), `183ae315` (feat) — all FOUND.
- Target tests GREEN (17/17 in the two target files; 60/60 in the wider target set); net-new failures = 0 (proven via base-checkout); `agent_loop.py` byte-identical; `anthropic_service.py` grew by exactly one param.

---
*Phase: 101.1-guaranteed-emission-layer*
*Completed: 2026-06-11*
