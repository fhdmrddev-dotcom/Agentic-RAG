---
phase: 101.1-guaranteed-emission-layer
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - supabase/full-schema.sql
  - backend/tests/fixtures/seed_llm_emit_fixture.py
  - backend/tests/fixtures/uat_fixture_ids.json
autonomous: false
requirements: [TMPL-02, TMPL-03]
user_setup: []

must_haves:
  truths:
    - "Migration 069 (the harness_audit event_type ALTER) is applied to the live local DB and full-schema.sql is regenerated to match"
    - "write_audit can write the 7 emit kinds against the live DB without a CHECK violation"
    - "The seeded live UAT WorkflowDefinition is re-seeded with a prior llm_agent planning phase feeding an llm_emit fill phase (the D-13 two-step), bound to the library template AssetRef"
    - "The seeded definition validates (LlmEmitPhaseConfig parses; required fields present) and is published idempotently without tripping the immutability trigger"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated bootstrap artifact reflecting the 069 ALTER (live-DB dump, no reset)"
      contains: "emit_validated"
    - path: "backend/tests/fixtures/seed_llm_emit_fixture.py"
      provides: "idempotent DELETE-then-INSERT seeder for the D-13 two-step llm_emit UAT definition"
      contains: "llm_emit"
  key_links:
    - from: "backend/tests/fixtures/seed_llm_emit_fixture.py"
      to: "supabase/migrations/069_harness_audit_emit_kinds.sql"
      via: "the emit phase writes audit rows that require the 069 CHECK to be live first"
      pattern: "llm_emit"
    - from: "backend/tests/fixtures/seed_llm_emit_fixture.py"
      to: "backend/tests/fixtures/uat_fixture_ids.json"
      via: "definition_id + asset_id emitted for the live UAT rows"
      pattern: "definition_id"
---

<objective>
Finalize the phase for live cross-provider UAT: apply migration 069 to the live local DB (operator-run, SQL-editor convention — never `db push`/`db reset`), regenerate `full-schema.sql`, and re-seed the live UAT `WorkflowDefinition` `00000000-...-0000000101a0` from the GAP-A open-loop `llm_agent` fill phase to the D-13 two-step shape: a prior `llm_agent` planning/retrieval phase feeding an `llm_emit` fill phase bound to the library template AssetRef. After this plan, the operator can run the 8 `101-HUMAN-UAT.md` rows + the SC#10 4-axis scoreboard against a real `llm_emit` workflow.

Purpose: the code is inert without the live substrate. The audit receipt CHECK must be live before any emit run, and the seeded definition must exercise `llm_emit` (not the old open loop) to reproduce — then close — the GAP-A..D live failure. Implements D-12 (live apply) + D-13 (two-step authoring shape, baked into the UAT fixture).
Output: 069 applied live + `full-schema.sql` regenerated + committed; an idempotent D-13 two-step seeder + the re-seeded live definition + emitted fixture ids.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-CONTEXT.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-VALIDATION.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-03-SUMMARY.md
@.planning/phases/101-template-fill-integrity-validation/101-HUMAN-UAT.md

<interfaces>
<!-- Contracts + conventions. Use directly. -->

CLAUDE.md migration convention (MANDATORY): apply by pasting supabase/migrations/069_*.sql into the Supabase SQL editor (or psycopg2 to local :54322 per the 100/099 precedent) — NEVER `supabase db push` / `db reset` (preserves dev data). Then `bash scripts/regenerate-full-schema.sh` (default = live-DB dump, NO reset). Commit BOTH. Never hand-edit full-schema.sql.

Prior seeder precedent (101-06, STATE.md): the seeded UAT WorkflowDefinition id `00000000-0000-0000-0000-0000000101a0` is published; the 056/067 immutability trigger is BEFORE-UPDATE only => a DELETE-then-INSERT seeder is idempotent for a stale published row. assets[] AssetRef points at a real workspace-files Storage object at {user_id}/_library/risk-register-101uat.docx. Existing ids in backend/tests/fixtures/uat_fixture_ids.json (definition_id + asset_id + storage_path).

The D-13 two-step shape (CONTEXT D-13): a PRIOR llm_agent/llm_single planning+retrieval phase (search_documents over the bound folder_scope) whose text feeds the llm_emit phase; the emit call itself is non-thinking + forced. The llm_emit phase config: phase_type="llm_emit", prompt=<derive the cited field-map from the prior phase's evidence>, emitter="render_template", available_tools omitted/empty (the emit tool is injected server-side, not model-selected).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migration 069 to the live local DB + regenerate full-schema.sql (operator-run)</name>
  <files>supabase/full-schema.sql</files>
  <what-built>
    Plan 01 authored `supabase/migrations/069_harness_audit_emit_kinds.sql` (the ALTER extending the harness_audit event_type CHECK with the 7 emit kinds) and extended `_AUDIT_EVENT_TYPES` in lockstep. The migration is NOT yet applied to the live DB — Claude cannot run `db push`/`db reset` (forbidden) and the SQL-editor apply is an operator step.
  </what-built>
  <read_first>
    - supabase/migrations/069_harness_audit_emit_kinds.sql (the exact ALTER to paste)
    - CLAUDE.md migration convention (apply via SQL editor; regen no-reset; commit both)
    - feedback_apply_migrations_via_sql_editor.md + feedback_regen_full_schema_no_reset.md (memory)
  </read_first>
  <action>
    Operator-run (Claude cannot `db push`/`db reset`). Paste `supabase/migrations/069_harness_audit_emit_kinds.sql` into the local Supabase SQL editor (or psycopg2 to local :54322 per the 100/099 precedent) and run it; confirm the new `harness_audit_event_type_check` def includes the emit kinds; run `bash scripts/regenerate-full-schema.sh` (no-reset live dump); commit the migration + the regenerated `full-schema.sql`. Follow the numbered steps in `<how-to-verify>`. Never run `supabase db push`/`db reset`.
  </action>
  <how-to-verify>
    1. Open the Supabase SQL editor for the local stack (or use psycopg2 to local :54322 per the 100/099 precedent).
    2. Paste the full contents of `supabase/migrations/069_harness_audit_emit_kinds.sql` and run it. Expected: the two ALTER statements succeed (DROP then ADD the CHECK); no error.
    3. Confirm the new CHECK is live — run: `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='harness_audit_event_type_check';` and verify `emit_validated`/`emit_failed`/`emit_integrity_failed` appear in the returned definition.
    4. Regenerate the bootstrap artifact (NO reset): `bash scripts/regenerate-full-schema.sh` — verify it dumps the live DB without resetting (your dev data is preserved). Confirm `grep -c emit_validated supabase/full-schema.sql` >= 1.
    5. Do NOT run `supabase db push` or `supabase db reset` at any point.
    6. Confirm the audit write works live (optional but recommended): from the backend venv, write a throwaway `harness_audit` row with `event_type='emit_validated'` via `write_audit` (psycopg2 / the pool) — it must INSERT without a CHECK violation; delete the throwaway row after.
  </how-to-verify>
  <verify>Operator confirms `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='harness_audit_event_type_check';` returns a def containing `emit_validated`; `grep -c emit_validated supabase/full-schema.sql` >= 1.</verify>
  <done>Migration 069 applied live (the CHECK includes the 7 emit kinds); full-schema.sql regenerated (no reset) + committed; a live `write_audit('emit_validated', ...)` succeeds without a CHECK violation.</done>
  <resume-signal>Type "applied" once the 069 CHECK is live (step 3 confirms emit kinds) and full-schema.sql is regenerated (step 4), or describe the error.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Idempotent D-13 two-step llm_emit UAT seeder + re-seed the live definition</name>
  <files>backend/tests/fixtures/seed_llm_emit_fixture.py, backend/tests/fixtures/uat_fixture_ids.json</files>
  <read_first>
    - backend/tests/fixtures/uat_fixture_ids.json (the existing definition_id + asset_id + storage_path from 101-01/101-06)
    - the prior 101-06 seeder (STATE.md: DELETE-then-INSERT idempotent, published-in-one-statement, never trips the immutability trigger) — clone its shape
    - backend/app/models/harness.py — WorkflowDefinition / PhaseConfig union / LlmEmitPhaseConfig (Plan 01) / AssetRef — the seeded JSON must model_validate()
    - CONTEXT D-13 (the two-step shape) + 101-HUMAN-UAT.md (the 8 rows the fixture must enable)
  </read_first>
  <behavior>
    - The seeder DELETE-then-INSERTs the published WorkflowDefinition id 00000000-...-0000000101a0 with a TWO-phase definition (D-13): phase 1 llm_agent (search_documents over the bound folder_scope, produces the evidence text) -> phase 2 llm_emit (phase_type="llm_emit", emitter="render_template", prompt derives the cited field-map from phase 1's output; the emit tool is injected server-side, available_tools empty)
    - assets[] carries the library template AssetRef (kind="template") pointing at the existing {user_id}/_library/risk-register-101uat.docx Storage object (GAP-B source)
    - The seeded JSON model_validate()s against the Plan 01 schema (LlmEmitPhaseConfig is the 6th union member; required fields present)
    - The seeder is idempotent (a clean 2nd run does not error; DELETE permitted because the immutability trigger is BEFORE-UPDATE only)
    - definition_id + asset_id are (re)written to uat_fixture_ids.json
  </behavior>
  <action>
    Create `backend/tests/fixtures/seed_llm_emit_fixture.py` cloning the 101-06 DELETE-then-INSERT idempotent seeder. Build the WorkflowDefinition JSON with the D-13 two-step phases:
    - Phase 1 (`llm_agent`, slug e.g. `gather`): `available_tools: ["search_documents"]`, `folder_scope: [<the bound risk folder id from the existing fixture>]`, `prompt`: retrieve + summarize the cited risk evidence from the project KB.
    - Phase 2 (`llm_emit`, slug `fill`): `phase_type: "llm_emit"`, `emitter: "render_template"`, `prompt`: derive a cited flat field-map from the prior phase's evidence (one risk per row, every value carrying its source chunk), `model: null` (D-07 provider-feature-fit — emit-critical phases default to a forceable provider; the operator selects a forceable model per cross-provider axis run, Kimi/Moonshot treated best-effort).
    - `assets`: `[{kind: "template", asset_id: <existing asset_id from uat_fixture_ids.json>, ...}]`.
    Validate the JSON via `WorkflowDefinition.model_validate(...)` IN the seeder before insert (fail fast if the schema drifted). Publish idempotently (INSERT-published-in-one-statement; DELETE the stale row first). Re-write `definition_id`/`asset_id` to `uat_fixture_ids.json`. Run the seeder against the live local stack (the stack is up; the seed is re-runnable). Do NOT touch `backend/app/**` (fixture/data only). This task is `auto` — Claude runs the seeder via psycopg2 to local :54322 (no `db push`).
  </action>
  <verify>
    <automated>cd backend && python tests/fixtures/seed_llm_emit_fixture.py && python -c "import json; d=json.load(open('tests/fixtures/uat_fixture_ids.json')); print('definition_id' in d and 'asset_id' in d)"</automated>
  </verify>
  <acceptance_criteria>
    - `python tests/fixtures/seed_llm_emit_fixture.py` exits 0 on a first AND a second run (idempotent)
    - `grep -c "llm_emit" backend/tests/fixtures/seed_llm_emit_fixture.py` >= 1
    - The seeder calls `WorkflowDefinition.model_validate(...)` before insert (schema drift fails fast)
    - A psycopg2 read-back of definition `00000000-...-0000000101a0` shows a `fill` phase with `phase_type: "llm_emit"` and `emitter: "render_template"`, and a prior `llm_agent` phase with `search_documents`
    - The definition's `assets[]` carries the template AssetRef (kind="template")
    - No `backend/app/**` file modified; no `supabase db push`/`db reset` invoked
  </acceptance_criteria>
  <done>The live UAT definition is re-seeded to the D-13 two-step llm_emit shape, validates, is bound to the library template, and the seeder is idempotent; fixture ids emitted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sanity check — one forced-emit run produces an openable .docx (the GAP-A close confirmation)</name>
  <files>backend/tests/fixtures/uat_fixture_ids.json</files>
  <what-built>
    The full layer is live: 069 applied, the D-13 two-step definition seeded, the gateway forcing + forced_emit substrate + _exec_llm_emit executor + GAP-C sub-events all shipped. This checkpoint confirms the headline behavior end-to-end before the full SC#10 4-axis scoreboard (which is authored in VALIDATION.md and run as part of /gsd:verify-work).
  </what-built>
  <read_first>
    - .planning/phases/101.1-guaranteed-emission-layer/101.1-VALIDATION.md (the 4-axis scoreboard + the named G-6 failure rows — this checkpoint is the smoke test before that full sweep)
    - .planning/phases/101-template-fill-integrity-validation/101-HUMAN-UAT.md (row 1 — cross-provider field-map emission)
    - CLAUDE.md sandbox-image note (rebuild/bump only if the package set changed — it did NOT for 101.1; use a FRESH chat so the bumped image is picked up)
    - reference_local_dev_app.md (dev app http://localhost:5173/, login fhdmrd@gmail.com / 123456) + feedback_user_starts_backend.md (operator starts uvicorn)
  </read_first>
  <action>
    Operator-run smoke test (no code change). With the backend + local Supabase + Redis up and `SANDBOX_IMAGE=agentic-rag-sandbox:101.1` set, run the seeded `00000000-...-0000000101a0` workflow on ONE forceable provider from a FRESH chat; confirm the emit sub-steps render, a real openable cited `.docx` is produced, an `emit_validated` audit row exists, and an induced failure surfaces an honest state (never a silent 0-docx). Follow the numbered steps in `<how-to-verify>`. The full 8-row + 4-axis sweep runs at `/gsd:verify-work`.
  </action>
  <how-to-verify>
    1. Ensure the backend (uvicorn) and the local Supabase + Redis stacks are running; the sandbox image `agentic-rag-sandbox:101.1` is set (it already carries docxtpl). Use a FRESH chat (cached sessions keep the old image ~30 min).
    2. From the dev app (http://localhost:5173/), enter workflow mode and run the seeded `00000000-...-0000000101a0` workflow on ONE forceable provider (e.g. an OpenAI or Anthropic model — Anthropic runs thinking-OFF for the emit).
    3. Watch the run-honesty rail: you should see the emit sub-steps `forcing -> emitting -> validating -> rendering -> validated` (NOT a static "Step 0 · working…" box), and the prior `gather` phase's retrieval.
    4. Confirm a real openable `.docx` is produced (the hero output file). Open it — no "needs repair" banner; the risk table is filled with cited rows.
    5. Confirm an `emit_validated` `harness_audit` row exists for the run (psycopg2 read of `harness_audit` filtered by the run_id; the receipt carries the field-map + gate verdict + integrity verdict + output SHA-256).
    6. Negative sanity: a provider that cannot land (or an induced citation-gate reject) shows an HONEST failure state on the rail (one of the 5) + a receipt — NEVER a silent 0-docx and NEVER a Markdown stand-in.
  </how-to-verify>
  <verify>Operator confirms an openable cited .docx + visible emit sub-steps + an `emit_validated` harness_audit row for the run; the negative case shows an honest failure state, not a silent 0-docx.</verify>
  <done>One forceable provider produces an openable cited .docx with visible emit sub-events + an immutable audit receipt; the GAP-A..D live failure is closed for at least one forceable provider; the phase is ready for the full /gsd:verify-work sweep.</done>
  <resume-signal>Type "approved" once one forceable provider produces an openable cited .docx with visible emit sub-steps + an emit_validated audit row, OR describe what failed (the full 8-row + 4-axis sweep runs at /gsd:verify-work).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL → live DB | a mis-applied ALTER could weaken the audit immutability or block valid kinds |
| seeded definition JSON → published row | a malformed definition could fail validation or trip the immutability trigger |
| seeded AssetRef → Storage object | the bound template must point at the user's own library object |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101.1-05-01 | Repudiation | the ALTER accidentally dropping the INSERT-only RLS (audit no longer immutable) | mitigate | 069 is an event_type-CHECK ALTER only; it does NOT touch RLS policies; the operator confirms the constraint def + leaves policies untouched; `db push`/`db reset` forbidden (preserves the immutable receipt history) |
| T-101.1-05-02 | Tampering | a stale/malformed seeded definition slipping past validation | mitigate | the seeder calls `WorkflowDefinition.model_validate(...)` before insert (fail fast); `_StrictBase` rejects unknown keys; DELETE-then-INSERT respects the BEFORE-UPDATE-only immutability trigger |
| T-101.1-05-03 | Information Disclosure | the seeded AssetRef pointing at another user's Storage object | mitigate | the AssetRef reuses the existing user-scoped `{user_id}/_library/...` object from uat_fixture_ids.json; resolve_template_source is user-scoped on created_by at run time (404-not-403) |
| T-101.1-05-04 | Tampering | full-schema.sql hand-edited / regenerated with a reset (data loss) | accept | convention enforced: regen is the no-reset live dump; never hand-edit; operator confirms data preserved — accepted as a process control |
</threat_model>

<verification>
- Migration 069 is live (the CHECK def includes the emit kinds); full-schema.sql regenerated (no reset) + committed
- `write_audit('emit_validated', ...)` succeeds against the live DB (no CHECK violation)
- The seeded definition reads back as a D-13 two-step llm_emit workflow bound to the library template; the seeder is idempotent
- The operator sanity run produces an openable cited .docx with visible emit sub-steps + an emit_validated audit row (GAP-A closed for at least one forceable provider)
- The full SC#10 4-axis scoreboard + 8 UAT rows run at /gsd:verify-work (authored in VALIDATION.md, not here)
</verification>

<success_criteria>
- 069 applied via the SQL-editor convention (no `db push`/`db reset`); full-schema.sql regenerated + committed (D-12 live)
- The live UAT WorkflowDefinition is re-seeded to the D-13 two-step shape (llm_agent gather -> llm_emit fill), validated + idempotent (D-13)
- One forceable provider produces a real openable cited .docx with visible emit sub-events + an immutable audit receipt — the GAP-A..D live close, confirmed
- The phase is ready for /gsd:verify-work (full cross-provider 4-axis scoreboard) + /gsd:secure-phase
</success_criteria>

<output>
After completion, create `.planning/phases/101.1-guaranteed-emission-layer/101.1-05-SUMMARY.md`
</output>
