---
phase: 101.1-guaranteed-emission-layer
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: false
requirements: [TMPL-02, TMPL-03]
files_modified:
  - backend/app/api/workspace.py
  - frontend/src/lib/api.ts
  - frontend/src/components/panel/FilePreview.tsx
  - frontend/src/stores/streamsStore.ts
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/lib/api.test.ts
  - frontend/src/components/panel/FilePreview.test.tsx
  - frontend/src/providers/StreamsProvider.test.tsx

must_haves:
  truths:
    - "The operator can DOWNLOAD a produced workspace deliverable from the panel — the Download control fetches the real bytes (Bearer-authed) and saves the file"
    - "During a live fill the PhaseCard rail shows the discrete emit sub-steps (forcing → emitting → [recovering] → validating → rendering → validated) and failed-as-failed states — populated from the phase_substep wire event"
    - "On a workflow terminal event the panel refetches workspace files so a finished emit's file + phase status appear without a manual refresh"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "downloadWorkspaceFile helper (067.3 blob pattern) + onPhaseSubstep callback + phase_substep demux branch"
      contains: "downloadWorkspaceFile"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "phase_substep demux → Phase.emitSubStep/emitFailure (panel-only) + terminal refetch"
      contains: "onPhaseSubstep"
    - path: "backend/app/api/workspace.py"
      provides: "raw-bytes download route for an inline binary workspace file (if needed)"
      contains: "raw"
  key_links:
    - from: "frontend/src/components/panel/FilePreview.tsx"
      to: "downloadWorkspaceFile"
      via: "Fallback onDownload prop passed at all three call sites"
      pattern: "onDownload="
    - from: "frontend/src/providers/StreamsProvider.tsx"
      to: "streamsStore phasesByThread"
      via: "onPhaseSubstep → setPhaseEmitSubstepForThread (closure threadId, panel-only)"
      pattern: "setPhaseEmitSubstepForThread"
---

<objective>
Close gap 3 (the download dead text — a produced deliverable is unreachable from the workflow surface, UAT Test 2 screenshot 203002), gap 6 (GAP-C demux unwired — the backend emits all 12 phase_substep events but nothing populates Phase.emitSubStep, UAT Test 13), and the frontend half of gap 4 (panel staleness — FILES shows "No files yet" / "fill — Running" after completion until F5). After this plan, the operator can download a deliverable, watch the discrete emit sub-steps live on the rail, and see a finished emit reflected without a manual refresh.

Purpose: workflow mode delivers ONLY into the workspace panel, so an unreachable Download breaks the last mile of TMPL-02. The phase CONTEXT (GAP-C / D-11) promised visible emit sub-steps IN-SCOPE; Plan 04 shipped the render contract but deferred the StreamsProvider demux (G-5 hot file). This plan wires a TIGHTLY-SCOPED, ADDITIVE, PANEL-ONLY demux (phase_substep → Phase.emitSubStep/emitFailure) that writes phasesByThread exclusively — zero chat-surface change, Deep byte-identical.

CHECKPOINT NOTE FOR THE OPERATOR (gap 6 scope discrepancy): the UAT flagged that Plan 04 deferred this demux as a G-5 concern. This plan implements the additive demux now (the CONTEXT promise) — but it is PANEL-ONLY and additive, so it does NOT pile onto the chat-surface hot files (it writes phasesByThread like the existing 094 phase-lifecycle demux, never bucketsBySurface). If at execution the operator prefers to instead RATIFY a re-scope of GAP-C's visible half to the dedicated run-legibility phase, the checkpoint task surfaces that choice. Default = wire it.

Output: a working Download; a live emit sub-step rail; a self-healing panel.

RED LINE: the demux writes phasesByThread ONLY (never bucketsBySurface → zero chat re-renders, the PANEL-09 discipline); Deep chat behavior byte-identical; no provider branching (one shared phase_substep event for every provider); XSS rule held (fixed text children, no dangerouslySetInnerHTML).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101.1-guaranteed-emission-layer/101.1-UAT.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-CONTEXT.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-04-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase — use directly, no exploration needed. -->

The PROVEN blob-download pattern — api.ts downloadSandboxOutput (:1227-1286): getAuthToken() → fetch with `Authorization: Bearer` → follow the 302 → res.blob() → programmatic <a download> click → revoke. DownloadError class (:1218) with status (number | "network"). Reuse this shape verbatim for workspace files.

The workspace content endpoint — workspace.py get_workspace_file_content (:249-327): for INLINE files it returns `{storage_type:"inline", content: <base64-DECODED TEXT via _decode_inline_content>}` — this CORRUPTS binary docx (it str-decodes the bytes). For BUCKET files it returns `{storage_type:"bucket", signed_url}`. A 37 KB docx is stored INLINE (DEFAULT_INLINE_THRESHOLD=256KB), so its bytes are NOT downloadable via the existing content route. The existing GET routes are mounted under the workspace router (prefix includes /threads/{thread_id}/workspace; routes: POST /files, GET /files, GET /files/{file_id}/content, .../versions, .../diff). _verify_thread_ownership(thread_id, current_user, supabase) is the 404-on-non-owner guard; the row SELECT is expiry-gated (`.or_("expires_at.is.null,expires_at.gt." + _now_iso())`).

FilePreview Fallback (:94-116) ALREADY accepts `onDownload?: () => void` and renders a real Download button when passed. The three call sites that omit it: :230 (error/no-content), :246 (bucket non-image), :272 (inline fallback default). getWorkspaceFileContent(threadId, fileId, signal) is imported (:28). The file row carries `file.id` / `file.path` / resolved.id (useResolvedFileId).

The 094 phase-lifecycle demux pattern — StreamsProvider.tsx:856-908: each callback closes over the factory's threadId (Pitfall 6 — a background run never corrupts the viewed thread), writes phasesByThread ONLY (never bucketsBySurface), provider-agnostic. onPhaseStarted appends a phase; onPhaseCompleted/onGateFailed/onRunFailed patch status via setPhaseStatusForThread(threadId, slug, status, patch?). onWorkspaceFileWritten (:817) → setWorkspaceFileForThread.

The api.ts demux branches — api.ts:749-768: `else if (t === "phase_started" && callbacks.onPhaseStarted) ...`, phase_completed, phase_transition, gate_failed, run_failed — each reads the FLAT producer payload (parsed.phase / parsed.phase_index / parsed.status / parsed.failure) and carries NO return (cursor-advance still fires). The backend already emits `phase_substep` with flat fields {phase, phase_index, status?, failure?} via _emit_phase_substep (phase_types.py:939, harness_engine._emit one-XADD).

The Phase type ALREADY has the fields (types/index.ts:402-417): `emitSubStep?: EmitSubStep` (forcing|emitting|recovering|validating|rendering|validated) and `emitFailure?: EmitFailure` (model_failed_to_emit|citation_gate_rejected|render_failed|integrity_failed|no_template_bound). PhaseCard.tsx ALREADY renders SUBSTEP_META + the EmitFailure taxonomy when Phase.emitSubStep/emitFailure is present (Plan 04). The ONLY missing link is populating those fields from the wire.

streamsStore actions (streamsStore.ts): setPhaseStatusForThread(threadId, slug, status, patch?) (:245) and appendPhaseForThread (:242) exist; the demux needs a sibling action to patch emitSubStep/emitFailure by phase slug+index.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: downloadWorkspaceFile helper + raw-bytes route + wire the Download button (gap 3)</name>
  <files>backend/app/api/workspace.py, frontend/src/lib/api.ts, frontend/src/components/panel/FilePreview.tsx, frontend/src/lib/api.test.ts, frontend/src/components/panel/FilePreview.test.tsx</files>
  <behavior>
    - Test 1 (api.ts): downloadWorkspaceFile(threadId, fileId, filename) injects the Bearer token, fetches the raw-bytes route, on 200 creates a blob + triggers an <a download> click; on 401 throws DownloadError(401), on 404 throws DownloadError(404) (mirror downloadSandboxOutput's contract). Use the existing test patterns for downloadSandboxOutput.
    - Test 2 (FilePreview): the Fallback rendered at the inline-default + bucket-non-image + error call sites now receives an onDownload prop that calls downloadWorkspaceFile with the resolved file id + filename. Assert the Download button is present and clicking it invokes the helper.
    - Test 3 (backend, if route added): GET the raw-bytes route for an owned inline binary file returns the exact bytes with a binary content-type + Content-Disposition attachment; a non-owner gets 404; an expired template row gets 404.
  </behavior>
  <action>
    (A) BACKEND — the existing /content route base64-DECODES inline content to text (corrupts binary docx) and only mints a signed URL for BUCKET files. A 37 KB docx is stored INLINE, so add a raw-bytes download route on the workspace router: `GET /files/{file_id}/raw` returning a fastapi Response (or StreamingResponse) of the EXACT bytes. For inline: return `Response(content=row["content_inline"] decoded back to bytes, media_type=row["mime_type"], headers={"Content-Disposition": f'attachment; filename="..."'})`. For bucket: download the bytes from Storage (reuse the _read_from_storage / signed-URL approach) OR 302 to the signed URL — pick the simplest that delivers exact bytes; inline binaries are the load-bearing case here. Apply the SAME guards as /content: `_verify_thread_ownership` (404 non-owner) + the expiry-gated row SELECT (404 on expired/missing — collapse missing-and-IDOR to 404, the existence-leak rule). Note: content_inline is stored as base64/JSONB by write_file's inline path — reuse the same decode the persist used (check how _decode_inline_content / content_inline round-trips and return the RAW bytes, not the str). If inline content cannot be reconstructed to exact bytes, escalate by lowering nothing — instead store the rendered docx to the bucket (out of scope here) — but FIRST verify the inline round-trip preserves bytes; if it does, no fallback needed.
    (B) FRONTEND api.ts — add `downloadWorkspaceFile(threadId: string, fileId: string, filename: string): Promise<void>` mirroring downloadSandboxOutput (:1227-1286) verbatim: getAuthToken → fetch `${API_BASE}/threads/${threadId}/workspace/files/${fileId}/raw` with Bearer → 401/404/5xx → DownloadError → res.blob() → <a download> click → revoke. Reuse the existing DownloadError class (do not add a second).
    (C) FilePreview.tsx — pass `onDownload` to the Fallback at all three call sites (:230, :246, :272). The handler calls `downloadWorkspaceFile(threadId, resolved.id, deriveFilename(file.path))` where the filename is the basename of file.path. For the bucket-image case keep the inline <img> (no change). Import downloadWorkspaceFile. Show a graceful inline error toast/text if the download throws (reuse whatever the panel already uses for errors, or a minimal local state — do not add a new toast system).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run api.test FilePreview.test</automated>
  </verify>
  <done>The panel Download control fetches the real bytes (Bearer-authed) and saves the deliverable; non-owner/expired → 404; the bucket-image preview is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: phase_substep demux → Phase.emitSubStep/emitFailure (gap 6, panel-only additive)</name>
  <files>frontend/src/lib/api.ts, frontend/src/stores/streamsStore.ts, frontend/src/providers/StreamsProvider.tsx, frontend/src/providers/StreamsProvider.test.tsx</files>
  <behavior>
    - Test 1 (api.ts): a `phase_substep` SSE with {phase, phase_index, status:"validating"} invokes a new onPhaseSubstep callback with {phase, phaseIndex, status:"validating"}; one with {phase, phase_index, failure:"citation_gate_rejected"} invokes it with the failure field. The branch carries NO return (cursor-advance still fires — mirror onPhaseStarted).
    - Test 2 (store): a new action setPhaseEmitSubstepForThread(threadId, slug, phaseIndex, {emitSubStep?/emitFailure?}) patches the matching phasesByThread row's emitSubStep/emitFailure and touches NOTHING else (no bucketsBySurface write).
    - Test 3 (StreamsProvider): onPhaseSubstep closes over the factory threadId, calls the store action for the OWNING thread only, and a background thread's phase_substep never patches the viewed thread. Deep dispatch (delta/tool_* etc.) byte-identical.
  </behavior>
  <action>
    (A) api.ts — add `onPhaseSubstep?: (p: { phase: string; phaseIndex: number; status?: EmitSubStep; failure?: EmitFailure }) => void` to StreamCallbacks (near onPhaseStarted, :385). Add an else-if branch in the harness phase-lifecycle block (after phase_transition / gate_failed, ~:762) reading the FLAT payload verbatim: `else if (t === "phase_substep" && callbacks.onPhaseSubstep) callbacks.onPhaseSubstep({ phase: parsed.phase as string, phaseIndex: parsed.phase_index as number, status: parsed.status as EmitSubStep | undefined, failure: parsed.failure as EmitFailure | undefined })` — NO return (cursor-advance fires). Import EmitSubStep/EmitFailure types if not already in scope.
    (B) streamsStore.ts — add `setPhaseEmitSubstepForThread(threadId, slug, phaseIndex, patch: { emitSubStep?: EmitSubStep; emitFailure?: EmitFailure })` to the actions interface + body. Body: find the phasesByThread[threadId] row matching slug (or phaseIndex when slug is a placeholder) and merge emitSubStep/emitFailure; write phasesByThread ONLY (mirror setPhaseStatusForThread's immutable update). Add the no-op default in the default-store stub.
    (C) StreamsProvider.tsx — add an `onPhaseSubstep` demux callback alongside the 094 lifecycle block (:856-908): `onPhaseSubstep: (s) => useStreamsStore.getState().actions.setPhaseEmitSubstepForThread(threadId, s.phase, s.phaseIndex, { emitSubStep: s.status, emitFailure: s.failure })`. Closure threadId (Pitfall 6), phasesByThread only (PANEL-09 — zero chat re-renders). Comment: "101.1-09 (gap 6 / GAP-C / D-11): the phase_substep demux Plan 04 deferred — ADDITIVE + PANEL-ONLY (writes phasesByThread like the 094 lifecycle demux, never bucketsBySurface; the chat selector reads bucketsBySurface exclusively → Deep byte-identical, no chat re-render). PhaseCard already renders emitSubStep/emitFailure (Plan 04) — this populates them from the wire."
    Do NOT touch the Deep dispatch branches, bucketsBySurface, or any provider-specific handling. The phase_substep event is one shared wire event for every provider.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run StreamsProvider.test</automated>
  </verify>
  <done>A live phase_substep event populates Phase.emitSubStep/emitFailure on the owning thread's rail (PhaseCard renders the discrete sub-steps + failed-as-failed states); the chat surface is byte-identical; background threads are isolated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Panel refetch on workflow terminal (gap 4 frontend half)</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/providers/StreamsProvider.test.tsx</files>
  <behavior>
    - Test 1: on a workflow terminal event (onRunCompleted "completed" / onDone for a harness run), the provider triggers a workspace-files refetch for the owning thread so a just-persisted deliverable appears without F5. Assert the refetch (the existing list/getWorkspaceFiles seam or the snapshot/getThreadWorkflow reconcile) is invoked for that threadId.
    - Test 2: the refetch is scoped to the owning threadId (a background terminal never refetches the viewed thread's files incorrectly).
    - Test 3: a Deep (non-harness) completion does NOT trigger the workspace refetch (no behavior change for Deep).
  </behavior>
  <action>
    In StreamsProvider's harness terminal handler (onRunCompleted at :909 and/or onDone for a harness run), add a workspace-files refetch for the owning threadId so the FILES section and phase status self-heal on completion (the UAT log shows FILES fetched 2 min BEFORE the row existed and never refetched). Reuse the EXISTING reconcile seam — prefer the getThreadWorkflow / workspace files reconcile the panel already uses (look for setWorkspaceFilesForThread / a reconcile action / the getSnapshot path) rather than adding a new fetch. If a list-workspace-files reconcile action exists, call it; if the snapshot reconcile already refetches files, ensure it runs on the harness terminal. Closure threadId (PANEL-09); do NOT trigger on Deep completions (guard on the workflow-lock/mode being harness). This may fold into the gap-6 demux wiring if the snapshot reconcile already pulls phases+files — verify and avoid a duplicate fetch. Comment naming gap 4 (frontend): the terminal refetch closes the "FILES stale after completion" half; the backend snapshot degrade (Plan 08 Task 3) ensures that refetch doesn't 503.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run StreamsProvider.test</automated>
  </verify>
  <done>A harness run's terminal triggers a scoped workspace refetch (file + phase status appear without F5); Deep completions are unaffected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Ratify the gap-6 scope (ship the panel-only demux, or re-scope GAP-C's visible half)</name>
  <what-built>The download helper + raw-bytes route (gap 3), the panel-only phase_substep demux populating the emit sub-step rail (gap 6), and the terminal refetch (gap 4 frontend). All additive, panel-only, Deep byte-identical.</what-built>
  <how-to-verify>
    This checkpoint exists ONLY to ratify the gap-6 scope decision flagged in the UAT (Plan 04 deferred this demux as a G-5 concern; the phase CONTEXT promised it in-scope). The default path (already implemented above) wires the additive panel-only demux — confirm you want it shipped in 101.1, OR direct a re-scope of GAP-C's visible half to the dedicated run-legibility phase (in which case revert Task 2 and note the re-scope in the SUMMARY + UAT).
    1. Confirm the demux is panel-only (writes phasesByThread, never bucketsBySurface) — the diff shows no chat-surface file touched beyond the additive callback.
    2. Decide: SHIP the demux in 101.1 (default) — or RE-SCOPE to the run-legibility phase.
  </how-to-verify>
  <resume-signal>Type "ship demux" to keep the wiring (default), or "re-scope gap-6" to defer GAP-C's visible half (revert Task 2 + record the re-scope).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → raw-bytes route | an authed user requests workspace file bytes; cross-user / expired access must 404 |
| producer SSE → panel store | a phase_substep event from a background run must not corrupt the viewed thread's rail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101.1-09-01 | Information disclosure | the raw-bytes download route | mitigate | _verify_thread_ownership + expiry-gated row SELECT + missing-and-IDOR collapsed to 404 (Task 1) — same guards as /content; no existence leak |
| T-101.1-09-02 | Tampering | the viewed thread's phase rail | mitigate | the demux closes over the factory threadId (Pitfall 6) + writes phasesByThread only — a background run's phase_substep cannot touch the viewed thread (Task 2) |
| T-101.1-09-03 | Tampering | the Deep chat surface | mitigate | the demux never writes bucketsBySurface; the Deep dispatch branches are byte-identical — a guard/regression test asserts no chat re-render (Task 2) |
| T-101.1-09-04 | Elevation of privilege | the download Bearer flow | accept | the helper injects the same Bearer token the rest of api.ts uses; right-click "Save link as" → 401 is the accepted 067.3 trade-off |
</threat_model>

<verification>
- The Download control fetches real bytes and saves the deliverable (Task 1); non-owner/expired → 404.
- A live phase_substep populates the rail; chat byte-identical; background isolation (Task 2).
- A harness terminal self-heals FILES without F5 (Task 3).
- The operator ratifies the gap-6 scope (checkpoint).
- Net-new vitest failures = 0 vs the SEED-056 frontend rot baseline (baseline-anchored at the pre-plan base).
</verification>

<success_criteria>
- A produced deliverable is downloadable from the panel (gap 3 closed).
- The emit sub-step rail is populated live (gap 6 closed, pending checkpoint ratification).
- The panel self-heals on a harness terminal (gap 4 frontend closed).
- Deep byte-identical; panel-only; net-new failures = 0.
</success_criteria>

<output>
After completion, create `.planning/phases/101.1-guaranteed-emission-layer/101.1-09-SUMMARY.md`.
</output>
