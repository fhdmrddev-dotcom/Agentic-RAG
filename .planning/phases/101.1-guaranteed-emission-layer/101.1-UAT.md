---
status: re-verified
phase: 101.1-guaranteed-emission-layer
source: [101.1-01-SUMMARY.md, 101.1-02-SUMMARY.md, 101.1-03-SUMMARY.md, 101.1-04-SUMMARY.md, 101.1-05-SUMMARY.md, 101.1-06-SUMMARY.md, 101.1-07-SUMMARY.md, 101.1-08-SUMMARY.md, 101.1-09-SUMMARY.md, 101.1-10-SUMMARY.md, 101.1-VALIDATION.md, 101-HUMAN-UAT.md]
started: 2026-06-11T15:19:15Z
updated: 2026-06-12T01:05:00Z
---

## Current Test

[re-verification complete (Plan 10, live, 2026-06-11) — all 6 diagnosed gaps PROVEN CLOSED; the 3 blocked-on-fixes tests now reach honest verdicts. 15 pass / 0 issue / 0 blocked. 3 minor run-honesty findings routed forward (non-blocking).]

## Tests

### 1. Setup & Cold-Start Smoke (live prerequisites)
expected: Migration 069 applied live (harness_audit CHECK = 7 emit kinds); sandbox image rebuilt + bumped to `agentic-rag-sandbox:101.1` in `backend/.env`; live D-13 two-step fixture re-seeded; backend boots clean; a FRESH chat kicks off the seeded workflow with no `sandbox_image_stale` error.
result: pass
evidence: "Live DB verify (psycopg2 :54322) — migration 069 CHECK carries all 7 emit kinds; definition 00000000-…-0000000101a0 confirmed D-13 two-step; SANDBOX_IMAGE=agentic-rag-sandbox:101.1 in backend/.env; operator confirmed docker image 101.1 (ID 68ad48fc4d13, In Use) + clean boot. harness_audit already held emit_rendered:1/emit_validated:4/emit_rejected:4/emit_integrity_failed:2/emit_failed:1."

### 2. Cross-provider forced emission — full native-7 + OpenRouter (TMPL-02, D-14, SC#10 cross-provider)
expected: Each provider produces an openable `.docx` AND a cited flat field-map — OR a visible honest failure naming the gate. Shared path never branches; Deep byte-identical.
result: pass
evidence: "RE-VERIFIED LIVE (Plan 10, 2026-06-11) — the DeepSeek silent-crash DEFECT is CLOSED. deepseek-v4-pro run fbec07cd: no 400, no silent crash; emit_forced receipt has thinking:false (FORCE-NOTHINK proven live); honest no-commit failure emit_forced->emit_failed, fill phase flipped 'failed', EXACTLY ONE message (gap 2 dup CLOSED). Weak-model render column FILLED: gemini-3.5-flash (a7878fd6, emit_failed 'model never committed'), claude-sonnet-4-6 (60bee474, citation reject x3), MiniMax-M3 (dd8e0314, citation reject x3) all VISIBLE honest failures naming the gate, 1 message each. Pre-fix 3 PROVEN openable .docx (gpt-5.4-mini 37,636B / nemotron-3-ultra 37,916B / GLM glm-5.1 38,049B 100% cited) stand. G-6 acceptance bar MET on every provider: openable cited .docx OR a visible honest failure naming the gate. Live pill mislabel found (Finding 1, minor, routed forward)."

### 3. GLM/MiniMax — file, not narrated prose (D-06 / GAP-D, TMPL-02)
expected: GLM/MiniMax produce a FILE or fail honestly — never a text dump masquerading as the artifact.
result: pass
evidence: "GLM glm-5.1 run 8531a6fc: clean forced emission -> openable .docx, 100% cited. MiniMax-M3 run f808cab5: real structured tool calls (2 structured rejects) then honest emit_failed — never prose-as-artifact. GAP-D held."

### 4. docx run-split via ephemeral arbitrary-upload path (TMPL-03, 101-UAT #3)
expected: Upload an ad-hoc `.docx` with a `{{token}}` fragmented across `<w:r>` runs; the ephemeral run_replace path fills it; residual scan `[]`.
result: pass
evidence: "RE-VERIFIED LIVE (Plan 10, 2026-06-11) — gpt-5.5 run a5705707 on the run-split fixture uat-files/status-memo-runsplit-101uat.docx ({{top_risk}} fractured across runs): full success ladder emit_forced->emit_validated->emit_rendered. /deliverable-filled.docx (37,859 B) PERSISTED to a DISTINCT path (binary version-delta skip held, the uploaded template NOT overwritten — gap 5 CLOSED). File appeared in FILES WITHOUT an F5 (workspace files + snapshot refetch on the terminal event — gap 4 frontend CLOSED; snapshot 200 on the finished run — gap 4 backend CLOSED). FilePreview opens; REAL Download button; /raw route 200 content-disposition attachment; the downloaded bytes re-open via python-docx, residual scan [], the fractured {{top_risk}} token coalesced+filled ('Risk M-02 (Integration dependency)') — gap 3 CLOSED."

### 5. Won't-open integrity failure → honest error + field-map fallback (TMPL-03, D-08, state d)
expected: Forced integrity failure → specific error naming the gate; file NOT delivered; cited field-map preserved as fallback.
result: pass
evidence: "Folded from live 101.1-06 onion runs 454e30c9 + 7fa36d2a (gpt-5.4-mini): trail emit_forced -> emit_validated -> emit_integrity_failed, file NOT persisted (no workspace row), run ended honestly (completed, not crashed), receipt names the integrity verdict. Fallback-surface message proven by the haiku state-b message ('the cited field-map is preserved below'). Operator accepted the fold."

### 6. pptx variable-row table limit — documented honest limit (TMPL-03, 101-UAT #5)
expected: pptx fill produces an openable file; scalars fill; verdict carries `documented_limit: "pptx cannot grow tables (python-pptx ≥1.0.0)"`.
result: pass
evidence: "RE-VERIFIED LIVE (Plan 10, 2026-06-11) — gpt-5.5 run cc7a4098 (ephemeral pptx table-deck): the persist crash that blocked this is FIXED; the run now REACHES its honest verdict emit_forced->emit_validated->emit_integrity_failed, 'integrity re-open failed' named on the rail, 1 message, phase 'failed' (pre-fix: silent crash at persist). FINDING 3 (minor, routed forward): the office-format integrity gate fails the re-open instead of delivering with documented_limit 'pptx cannot grow tables' semantics — honest + observable, but the documented-limit delivery path never emits a file for pptx in the 101.1 emitter gate (the documented_limit lives in template_render_service.assert_integrity; the emitter gate must honor it). No silent fall-through; no stuck-active row."

### 7. xlsx chart strip — documented honest limit (TMPL-03, 101-UAT #6)
expected: Chart-bearing xlsx fills scalars; verdict carries `documented_limit: "openpyxl drops charts on save"`.
result: pass
evidence: "RE-VERIFIED LIVE (Plan 10, 2026-06-11) — gpt-5.5 run 9b0e8f5f (ephemeral xlsx chart-book): reaches the honest integrity-failure ladder emit_forced->emit_validated->emit_integrity_failed (pre-fix: persist crash). GAP 6 PROVEN LIVE during this run — the DOM sampler captured the emit sub-steps ticking on the PhaseCard rail: 'Emitting the field-map' (19:21:49) -> 'Rendering the deliverable' (19:21:57) -> the named failure state (19:22:22). FINDING 3 applies (xlsx documented_limit 'openpyxl drops charts' not honored by the emitter gate — minor, routed forward). 1 message, phase 'failed'."

### 8. xlsx merged-cell mis-write (TMPL-03, 101-UAT #7)
expected: Merged-cell anchor fills correctly OR verdict documents the limit; integrity re-open passes.
result: pass
evidence: "RE-VERIFIED LIVE (Plan 10, 2026-06-11) — gpt-5.5 run 23d5eb22 (ephemeral xlsx merged-cell): same honest integrity-failure ladder emit_forced->emit_validated->emit_integrity_failed (pre-fix: persist crash). 1 message, phase 'failed'. FINDING 3 applies (office-format documented_limit not honored by the emitter integrity gate — minor, routed forward). No silent crash; no stuck-active row."

### 9. Sandbox isolation proof (TMPL-02, 101-UAT #8)
expected: NEW chat against the bumped image renders; an OLD pre-bump cached session raises ModuleNotFoundError — render is sandbox-isolated.
result: pass
evidence: "Positive half proven live repeatedly: every render ran inside the agentic-rag-sandbox:101.1 container (fresh threads); docxtpl is ABSENT from backend host code (Pitfall 4 — oracle uses stdlib zip parse; suite greps enforce no docxtpl import in backend/app). Negative half (pre-bump session ModuleNotFoundError) untestable now — no pre-bump cached sessions remain (sessions evicted; image bumped this morning); isolation is structurally proven by the host-side absence of the render libs."

### 10. Multi-tool — D-13 two-step retrieval feeding emit (SC#10 multi-tool)
expected: gather (`search_documents`) feeds fill (`llm_emit`); emitted citations resolve to chunks gather actually retrieved; 2+ tools in one run.
result: pass
evidence: "Every live run IS the D-13 two-step. GLM run 8531a6fc: gather retrieved over the bound folder, emit cited 100% against the SERVER-SIDE valid set unioned from gather's source_refs (_emit_evidence one-walk). The a12ee906 trail proves the converse: prose-only evidence -> all citations invented -> rejected. Retrieval+emit exercised in one run on 8 providers."

### 11. Parallel-thread — concurrent fill + new prompt (SC#10 parallel-thread)
expected: Thread A streaming a fill while Thread B accepts a new prompt; no blocking, no cross-contamination; receipts keyed to the right run_id.
result: pass
evidence: "Operator launched 7 provider runs across 7 threads within a 6-minute window (15:33-15:39 UTC), overlapping execution. Each run's receipts keyed to its own run_id; each thread's deliverable/message matches its own model (GLM file on the GLM thread, nemotron file on the nemotron thread, haiku reject on the haiku thread); no cross-contamination observed in receipts, files, or messages."

### 12. Long-message — ≥50-row chunking OR ≥5 KB prior output (SC#10 long-message)
expected: A ≥50-row register chunks across multiple forced emits OR a ≥5 KB prior planning output feeds the emit and the fill still produces a correct openable cited deliverable.
result: pass
evidence: "Variant 2 proven live ABUNDANTLY: gather outputs feeding the emit ranged 8.8KB-64.8KB across the 9 runs (all >= 5KB). GLM produced its perfect 100%-cited openable docx from a 61,743-char (~60KB) prior-phase input; deepseek-v4-pro's gather fed 64.8KB. Variant 1 (chunking) NOT exercised — D-09's 'chunk very large maps into multiple forced emits' is UNIMPLEMENTED (grep: no chunking code in forced_emit.py/phase_types.py); a >50-row register would hit the truncation guard's honest fail. Logged as deferred-scope note below (the authored row passes via OR)."

### 13. Emit-moment run-honesty on the rail (GAP-C / D-11, G-4 visual)
expected: During a live fill the PhaseCard rail shows `forcing → emitting → [recovering] → validating → rendering → validated`; failures render failed-as-failed via the closed taxonomy.
result: pass
evidence: "RE-VERIFIED LIVE (Plan 10, 2026-06-11) — the Plan 09 phase_substep demux (RATIFIED 'ship demux', additive + panel-only) populates Phase.emitSubStep from the wire. Run 9b0e8f5f: the DOM sampler captured the discrete sub-steps ticking on the PhaseCard rail — 'Emitting the field-map' (19:21:49) -> 'Rendering the deliverable' (19:21:57) -> a named failure state (19:22:22); also observed live mid-run on run dd8e0314 ('Emitting the field-map'). The static 'Running' pill is replaced by the discrete forcing->emitting->validating->rendering->validated sub-steps + failed-as-failed. FINDING 1 (minor, routed forward): the LIVE terminal pill still shows green 'Complete' on an honestly-failed fill (the failure box + named state render correctly below it; the pill flips to red 'Failed' on reload) — run-honesty polish, the honest failure IS visible."

### 14. Uncited / invented citation rejected BEFORE render (G-6, state b)
expected: An invented citation is rejected at the gate BEFORE render; server-side valid set (KB-content cannot whitelist its own ids); bounded retry with named leaves; honest fail when ungroundable.
result: pass
evidence: "Proven live across 3 independent trails: a12ee906 (24/24 invented -> emit_rejected, 0% coverage, BEFORE render); 3596174c (rejected x2 with named-leaf feedback then CONVERGED on attempt 3 — the retry ladder works); claude-haiku 257bf70d (rejected x3 with covers_template=false then honest final fail). Valid set computed server-side from gather source_refs. Unit tests cover the injection case."

### 15. No-template-bound honest failure (G-6, state e)
expected: No `assets[]` template AND no ephemeral upload → honest state-e error; never a silent fall-through; never 0-files-no-error.
result: pass
evidence: "Orchestrator-driven live (run 3438f9f8, gpt-5.4-mini, fresh thread, NO upload, ephemeral definition 101a1): run COMPLETED honestly (not crashed), emit_failed receipt written, 0 workspace files, phases flipped correctly, surfaced message names the exact remedy: 'No template is bound to this workflow phase and no usable template was found: No template uploaded to this thread. Upload a .docx/.pptx/.xlsx first.' (Duplicate-message minor reproduced — gap 3.)"

### 16. DeepSeek forced fill post-WR-05 strict fix (review-fix verification)
expected: WR-05 moved strictness onto the forced function definition and gated `response_format: json_schema` to OpenAI only — a DeepSeek forced fill must run (no 400), write the full receipt enrichment, and reach a docx or visible honest failure.
result: pass
evidence: "Orchestrator-driven live (run b67a5064, deepseek-v4-pro, library workflow): no 400, forced shot ran, honest no-commit failure (emit_forced→emit_failed), fill phase flipped 'failed', EXACTLY ONE message. emit_failed receipt enrichment byte-identical to pre-fix shape (tier TIER-FORCE, provider deepseek, forced true, thinking false) — no receipt regression."

### 17. WR-01/F1 — honest-fail fill card renders FAILED live (review-fix verification)
expected: The new `phase_failed` SSE flips the live card to a red "Failed" pill the moment an honest emit failure lands — no green "✓ Complete" over the failure alert, without reload.
result: pass
evidence: "Observed live on run b67a5064 (same session, no reload): card reads 'PHASE 2 · Step fill Failed', a11y live-region announces 'Phase 2 of 2, fill, failed', run header 'Run failed during fill.', failure alert + named state below. F1 mislabel CLOSED. (Minor pre-existing: SUB-RESULTS pill said 'Sub-task Running' briefly after terminal — BUG-260609-02 family, run-honesty polish, pre-existing.)"

### 18. Moonshot/Kimi TIER-COERCE no longer structurally dead (WR-04 verification)
expected: Pre-fix the coerce tier never showed the schema to STRUCTURED-mode models (structurally dead). Post-fix a Kimi fill must reach receipts and a docx or honest failure.
result: pass
evidence: "Orchestrator-driven live (run 47b6b086, kimi-k2.6, library workflow): PRODUCED THE DELIVERABLE — /risk-register.docx 37,948 B, emit_forced→emit_validated→emit_rendered, receipts tier TIER-COERCE provider moonshot forced:false (honestly coerced, not forced), citation coverage 100%, covers_template true. Bytes re-open clean via python-docx, residual [], 7 grounded table rows. The previously-unforceable provider now renders — first coerce-tier deliverable ever."

### 19. OpenAI regression guard post-WR-05 (function-level strict)
expected: The strict-mode change must not regress the proven OpenAI path — a library fill still renders an openable cited docx.
result: pass
evidence: "Orchestrator-driven live (run eb022543, gpt-5.5, library workflow): /risk-register.docx 37.0 KB, High confidence, 6 sources, full success ladder emit_forced→emit_validated→emit_rendered. No regression."

## Summary

total: 19
passed: 19
issues: 0
pending: 0
skipped: 0
blocked: 0

re-verified: 2026-06-11 (Plan 10, live) + 2026-06-12 (post-review-fix, live — tests 16-19)
note: "All 6 diagnosed gaps PROVEN CLOSED live (gaps 1-6); the 3 previously-blocked-on-fixes tests (6-8) now reach honest verdicts. 2026-06-12 post-review-fix live verification (tests 16-19): WR-01 red-card live PROVEN (F1 CLOSED), WR-05 DeepSeek strict fix proven (no 400, receipts intact), WR-04 coerce tier ALIVE — Kimi rendered its first openable 100%-cited docx, OpenAI regression-free. Remaining routed-forward findings (non-blocking): F2 reload failure-reason degrade, F3 office-format documented_limit not honored by the emitter integrity gate (→ SEED-082 policy family). See 101.1-10-SUMMARY.md + 101.1-REVIEW-FIX.md."

## Gaps

- truth: "Every provider produces an openable .docx OR a visible honest failure naming the gate — NEVER a run with 0 files and no surfaced error (G-6 silent-0-docx); a terminal run never leaves 'Running' state behind"
  status: closed
  re_verified: "CLOSED LIVE (Plan 10, 2026-06-11). deepseek-v4-pro run fbec07cd: no 400, no silent crash, emit_forced thinking:false (FORCE-NOTHINK live), honest emit_forced->emit_failed, fill phase flipped 'failed', 1 message. Weak-model render column filled (gemini-3.5-flash a7878fd6 / claude-sonnet-4-6 60bee474 / MiniMax-M3 dd8e0314 — all visible honest failures naming the gate). G-6 bar met on every provider. Global: zero new dirty rows across 8 runs; the only stuck-active rows are the 2 out-of-scope Phase-092 confirm rows (DI-101.1-10-A)."
  reason: "DeepSeek (v4-flash 575e7345 AND v4-pro a7f415ad): silent run-crash — no emit_failed receipt, no assistant message, empty avatar, panel stuck 'Running' FOREVER. Reproduced a third time by the ephemeral persist crash (4ea9bc56). DB proof: all 3 crashed runs have workflow_runs.status='failed' but workflow_phases fill status='active' permanently."
  severity: major
  test: 2
  root_cause: "TWO stacked defects, both traceback-confirmed (backend.39368.log:4184-4232, backend.47720.log:137-183). (1) DEEPSEEK PROVIDER-SHAPE: 400 'Thinking mode does not support this tool_choice' — openai_service.py:1352-1360 enables DeepSeek v4 thinking unconditionally; the forced branch sends named tool_choice without disabling it. DeepSeek shares Anthropic's no-force-under-thinking constraint -> needs the FORCE-NOTHINK treatment (D-13 two-step already moves reasoning to the gather phase). (2) MISSING D-08 LAYER-6 BACKSTOP: forced_emit() does not wrap open_stream/_drain (forced_emit.py:213-215) and _exec_llm_emit does not wrap forced_emit or the post_processor render dispatch (phase_types.py:1112/:1245) — ANY raised exception escapes the whole honest-failure ladder to threads.py agent_runner:1438, which marks the run failed but writes NO receipt, surfaces NO message, emits NO terminal UI event, and never flips the active workflow_phases row."
  artifacts:
    - path: "backend/app/services/openai_service.py"
      issue: "lines 1352-1360: DeepSeek thinking enabled unconditionally — disable when force_tool_name is set"
    - path: "backend/app/services/forced_emit.py"
      issue: "open_stream/_drain (213-215) unwrapped — return _failure(provider_error) on raise instead of propagating"
    - path: "backend/app/services/harness/phase_types.py"
      issue: "_exec_llm_emit: no catch-all around the attempt loop (:1112) or the render dispatch (:1245) — the 5-state ladder misses raised exceptions"
    - path: "backend/app/api/threads.py"
      issue: "agent_runner:1438 catch marks the run failed but never flips active workflow_phases rows (3 dirty rows live: 575e7345/a7f415ad/4ea9bc56) — engine-level cleanup + one-off repair needed"
  missing:
    - "DeepSeek FORCE-NOTHINK: disable the thinking block on forced emit calls (provider-scoped, service boundary)"
    - "forced_emit try/except -> honest _failure('provider_error') result"
    - "_exec_llm_emit catch-all -> emit_failed receipt + phase_substep failure + _surface_failure_message + honest output (covers provider AND render/persist raises)"
    - "Engine/agent_runner cleanup: flip active phase rows to failed on terminal failure + one-off repair of the 3 dirty rows"
    - "D-15: live-validate forced_emission flags against this finding (deepseek keeps forced=true + nothink rider)"
  debug_session: "backend/logs/backend.39368.log:115-164,4184-4232 + backend/logs/backend.47720.log:137-183"

- truth: "An honest emit failure surfaces EXACTLY ONE assistant message"
  status: closed
  re_verified: "CLOSED LIVE (Plan 10, 2026-06-11). EXACTLY ONE assistant failure message persisted on every honest-failure run across all 8 re-verification runs (fbec07cd / a7878fd6 / 60bee474 / dd8e0314 / cc7a4098 / 9b0e8f5f / 23d5eb22). Single-owner persist (Plan 07: _surfaced flag → engine skips the second persist) holds live."
  reason: "The honest-failure message is written TWICE on every failing thread (gemini, kimi, haiku, and the orchestrator-driven test-15 run). Joins the run-honesty dup family (BUG-260610-01)."
  severity: minor
  test: 2
  root_cause: "Double-persist: _exec_llm_emit calls _surface_failure_message directly (all 5 state returns) AND returns the same text as the phase output, which the engine's completion path persists again as the final answer (run completes 'successfully' with the failure text as its output — test-15 phases read 'completed'). Two writers, one message. Fix: single owner — either the executor stops surfacing directly (engine's RC-4/final-answer persist owns it) or the failure output is flagged so the engine skips the second persist."
  artifacts:
    - path: "backend/app/services/harness/phase_types.py"
      issue: "_exec_llm_emit surfaces directly at 6 sites AND returns the text — double-persist with the engine's final-answer path"
    - path: "backend/app/services/harness_engine.py"
      issue: "completion path persists the final phase output unconditionally (:918 RC-4 site is for FAILED outcomes; the emit failure returns as a NORMAL output)"
  missing:
    - "Pick one owner for the failure persist (recommend: executor returns flagged output; engine persists once)"
  debug_session: ""

- truth: "The operator can DOWNLOAD the produced deliverable from the workflow surface (the last mile of TMPL-02)"
  status: closed
  re_verified: "CLOSED LIVE (Plan 10, 2026-06-11). Run a5705707: FilePreview shows a REAL Download button; the /raw route returns 200 with a content-disposition attachment; the downloaded bytes re-open via python-docx (residual scan [], the fractured {{top_risk}} token filled). downloadWorkspaceFile + the /workspace/files/{id}/raw route (Plan 09) deliver the byte-exact docx."
  reason: "User reported + screenshot 203002: file appears in workspace FILES (after F5) but 'Download' does nothing."
  severity: major
  test: 2
  root_cause: "FilePreview.tsx Fallback renders a real Download button ONLY when an onDownload prop is passed — ALL THREE call sites (:230/:246/:272) omit it, so the visible 'Download' is part of a static STRING. No workspace-file download helper exists in api.ts. The proven blob pattern exists at api.ts:1209-1286 (067.3 sandbox-outputs downloader) but was never wired to workspace files. Workflow mode delivers ONLY into the workspace panel -> deliverable produced+persisted but unreachable."
  artifacts:
    - path: "frontend/src/components/panel/FilePreview.tsx"
      issue: "Fallback called without onDownload at :230/:246/:272"
    - path: "frontend/src/lib/api.ts"
      issue: "no downloadWorkspaceFile helper; binary inline needs a raw-bytes route (the JSON content endpoint is preview-shaped)"
  missing:
    - "downloadWorkspaceFile(threadId, fileId, filename) reusing the 067.3 blob pattern (+ raw-bytes backend route if needed)"
    - "Pass onDownload at all three FilePreview call sites"
  debug_session: ""

- truth: "The workspace panel reflects a finished emit without manual refresh (file visible, phase flips)"
  status: closed
  re_verified: "CLOSED LIVE (Plan 10, 2026-06-11). Run a5705707: the deliverable appeared in FILES WITHOUT an F5 (workspace files + snapshot refetch observed in the network log on the terminal event — gap 4 frontend, Plan 09 self-heal); GET /snapshot returned 200 on the finished run (gap 4 backend, Plan 08 no-such-key degrade). The fill phase status flipped correctly with no manual refresh."
  reason: "FILES showed 'No files yet' and 'fill — Running' AFTER completion; F5 healed SUCCESS runs (screenshots 202955/203002). Log: panel fetched workspace_files 2 min BEFORE the file row existed and never refetched; GET /snapshot crashed 'no such key' (mislabeled Redis unreachable)."
  severity: minor
  test: 2
  root_cause: "Plan 04 deliberately deferred the demux wiring through api.ts/StreamsProvider (G-5) to the run-legibility phase — no live event/refetch updates FILES or phase status on emit completion. Additionally threads.py get_snapshot raises on a missing run buffer key instead of degrading to DB reconcile (D-v2.5-03)."
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      issue: "no refetch/demux on workflow terminal events (deferred scope)"
    - path: "backend/app/api/threads.py"
      issue: "get_snapshot ResponseError('no such key') -> 500 instead of DB-reconcile degrade (log 39368:1611)"
  missing:
    - "Minimal refetch-on-terminal (or fold into the gap-13 demux wiring); snapshot no-such-key degrade"
  debug_session: "backend/logs/backend.39368.log:1611-1652"

- truth: "An ephemeral-upload fill persists its rendered deliverable (TMPL-03 last mile)"
  status: closed
  re_verified: "CLOSED LIVE (Plan 10, 2026-06-11). Run a5705707 (gpt-5.5, run-split fixture): emit_forced->emit_validated->emit_rendered; /deliverable-filled.docx (37,859 B) PERSISTED to a DISTINCT path (binary version-delta skip held — gap 5a; the uploaded template NOT overwritten — gap 5b). No \\u0000 JSONB crash. The downloaded bytes re-open clean, residual []. Plan 08 (skip binary delta + distinct ephemeral output filename) holds live."
  reason: "Run 4ea9bc56: every gate passed; persist crashed — binary docx to the SAME path as the uploaded template -> overwrite -> version 2 -> _compute_delta_from_prev embeds binary in delta_from_prev JSONB -> Postgres rejects \\u0000."
  severity: major
  test: 4
  root_cause: "CONFIRMED traceback backend.47720.log:137-183: _handle_render_template (tool_dispatcher.py:1853) -> write_file (workspace_service.py:284) -> insert_version (db/workspace.py:77) binds a \\u0000-bearing JSONB delta. Contributing: (1) write_file:279-282 computes delta_from_prev for version>1 regardless of mime — binary must skip the delta; (2) the ephemeral output path COLLIDES with the uploaded template_input path (filled output overwrites the user's template — also wrong UX; bound path writes a distinct /risk-register.docx). Bound runs never hit it (fresh path -> v1 -> delta=None)."
  artifacts:
    - path: "backend/app/services/workspace_service.py"
      issue: "write_file:279-282 — delta for binary content must be skipped"
    - path: "backend/app/services/tool_dispatcher.py"
      issue: "_handle_render_template ephemeral branch output path collides with the uploaded template path"
  missing:
    - "Skip delta_from_prev for binary mimes"
    - "Distinct output filename on the ephemeral path (never overwrite the user's template)"
  debug_session: "backend/logs/backend.47720.log:137-183"

- truth: "The emit moment is honestly visible live: discrete forcing→emitting→validating→rendering→validated sub-steps on the PhaseCard rail (GAP-C/D-11 — the phase-promised user-visible deliverable)"
  status: closed
  re_verified: "CLOSED LIVE (Plan 10, 2026-06-11). The Plan 09 phase_substep demux (RATIFIED 'ship demux', additive + panel-only) populates Phase.emitSubStep from the wire. Run 9b0e8f5f: DOM sampler captured the discrete sub-steps ticking on the rail — 'Emitting the field-map' (19:21:49) -> 'Rendering the deliverable' (19:21:57) -> a named failure state (19:22:22); also seen mid-run on dd8e0314. The static 'Running' pill is replaced by the discrete forcing->emitting->validating->rendering->validated + failed-as-failed. RESIDUAL (Finding 1, minor): the live terminal pill still shows green 'Complete' on an honestly-failed fill (failure box + named state render correctly below; pill flips red on reload) — run-honesty polish, routed forward."
  reason: "Operator watched multiple live fills: only the static 'Running' pill — no sub-steps ever render. The backend emits all 12 phase_substep events; PhaseCard can render them; NOTHING populates Phase.emitSubStep from the wire."
  severity: major
  test: 13
  root_cause: "Plan 04 shipped the render contract but deliberately deferred the StreamsProvider/api.ts demux (G-5 hot file) to the run-legibility phase. The phase CONTEXT (GAP-C/D-11) promised the sub-events 'replacing the static box' IN-SCOPE — scope discrepancy between the plan deferral and the phase promise. Decision needed at fix planning: wire a tightly-scoped additive demux now (emitSubStep/emitFailure only) vs formally re-scope GAP-C's visible half to the run-legibility phase (operator must ratify — it changes what 101.1 claims to close)."
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      issue: "no phase_substep demux (emitSubStep/emitFailure never populated)"
    - path: "frontend/src/lib/api.ts"
      issue: "SSE event vocabulary lacks the phase_substep mapping"
  missing:
    - "Additive demux: map phase_substep wire events -> Phase.emitSubStep/emitFailure (panel-only, no chat-surface change) OR ratified re-scope"
  debug_session: ""

# MINOR RE-VERIFICATION FINDINGS (Plan 10, 2026-06-11 — non-blocking, routed forward; NOT new gap rows)
# - F1 LIVE phase-pill mislabel: a fill phase that fails honestly shows green 'Complete' on the
#   live card (the failure box + named state render correctly below it); after reload the pill
#   correctly shows red 'Failed'. The live terminal event maps to Complete regardless of phase
#   failure. The honest failure IS visible — run-honesty polish. Route to the run-legibility phase.
# - F2 RELOAD failure-reason degrade: after reload the rich failure reason is replaced by
#   'Failure reason not captured by the backend — error field was empty' — the reason lives in
#   harness_audit + the chat message but NOT in the workflow_phases row's error field, so the DB
#   reconcile can't render it. Persist the failure reason to the phase error field to close.
#   Route to the run-legibility phase.
# - F3 office-format integrity-gate calibration: all three office-format ephemeral fills
#   (Tests 6-8) validated citations then FAILED integrity re-open instead of delivering with
#   documented_limit semantics (pptx table-growth / xlsx chart-strip allowances from the
#   Phase-101 sandbox path are not honored by the 101.1 emitter integrity gate). HONEST, not
#   silent — but the documented-limit 'pass OR documented' delivery path never delivers a file
#   for these formats. (documented_limit lives in template_render_service.assert_integrity; the
#   emitter gate must honor it.) Route to a follow-up.
#
# DEFERRED-SCOPE NOTES (not failing rows)
# - D-09 chunking unimplemented: 'chunk very large maps into multiple forced emits' (locked
#   D-09) has no implementation — a >50-row register hits the truncation guard's honest fail.
#   Test 12 passed via its OR variant (>=5KB prior output, proven to 64.8KB). Re-open trigger:
#   Phase 104 flagship templates with large registers, or the first real >50-row corpus.
# - Weak-model render column: RESOLVED at re-verify (Plan 10) — gemini-3.5-flash / claude-sonnet-4-6
#   / MiniMax-M3 / deepseek-v4-pro all end in VISIBLE honest failures naming the gate (no docx);
#   the G-6 acceptance bar (openable docx OR visible honest failure) is MET for all. The openable
#   weak-model docx itself stays unfilled BY DESIGN (weak models honestly decline) — feeds the
#   Phase 103/104 per-phase model-selection design gap (badge the picker by forced_emission).
# - Tests 6-8 (pptx/xlsx fixtures): UNBLOCKED at re-verify (Plan 10) — the persist crash is fixed;
#   each now reaches its honest integrity verdict (see F3 for the documented_limit follow-up).

# CAPTURED ENHANCEMENT (operator, 2026-06-11): template-requirements story must become
# (a) user-visible docs, (b) application documentation, (c) UI upload-time guidance/validation
# ({{placeholder}} convention, pptx/xlsx documented limits, KB-grounding nulls, parsed
# placeholder count shown on upload via parse_docx_template_variables). Route at fix planning.

# CAPTURED DESIGN GAP (operator, 2026-06-11): the Workflow builder (094 sketches + NL-authoring
# vision) never designed WHERE the model choice lives. llm_emit phases carry model:null -> runs
# inherit the chat composer's model; a user on an unforceable/weak model silently gets the
# best-effort path. D-07/D-15 are the substrate; the builder needs per-workflow/per-phase model
# selection with the picker filtered/badged by forced_emission. Route to Phase 103/104.
