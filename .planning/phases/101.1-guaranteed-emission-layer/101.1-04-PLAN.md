---
phase: 101.1-guaranteed-emission-layer
plan: 04
type: execute
wave: 4
depends_on: [01, 03]
files_modified:
  - backend/app/services/harness/phase_types.py
  - backend/app/services/template_render_service.py
  - frontend/src/components/panel/PhaseCard.tsx
  - backend/tests/unit/test_template_render.py
  - backend/tests/unit/test_template_integrity.py
  - frontend/src/components/panel/PhaseCard.test.tsx
autonomous: true
requirements: [TMPL-02, TMPL-03]
user_setup: []

must_haves:
  truths:
    - "The emit moment emits discrete phase_substep events (forcing -> emitting -> recovering -> validating -> rendering -> validated) on the existing run-honesty rail"
    - "The 5 distinguishable failure states (model_failed_to_emit / citation_gate_rejected / render_failed / integrity_failed / no_template_bound) render failed-as-failed, never an empty 'done' card"
    - "A flat EmitFieldMap maps to the SAME docxtpl context (and the same rendered bytes) as the equivalent nested map — the two shapes cannot drift"
    - "The citation gate (check_coverage) accepts the flat shape via the third _iter_leaves branch with unchanged uncited/invented semantics"
    - "Old 097/101 nested artifacts still validate + render unchanged (back-compat held)"
  artifacts:
    - path: "backend/app/services/harness/phase_types.py"
      provides: "phase_substep _emit calls at each emit transition (GAP-C wire vocabulary)"
      contains: "phase_substep"
    - path: "backend/app/services/template_render_service.py"
      provides: "third _iter_leaves/build_context branch for the flat EmitFieldMap shape"
      contains: "_iter_leaves"
    - path: "frontend/src/components/panel/PhaseCard.tsx"
      provides: "STATUS_META sub-step states + FAILURE TAXONOMY 5 emit failure values"
      contains: "model_failed_to_emit"
  key_links:
    - from: "backend/app/services/harness/phase_types.py"
      to: "backend/app/services/harness_engine.py"
      via: "_emit(redis, run_id, 'phase_substep', status=...) one canonical XADD"
      pattern: "phase_substep"
    - from: "frontend/src/components/panel/PhaseCard.tsx"
      to: "backend/app/services/harness/phase_types.py"
      via: "SSE phase_substep status/failure values map to status-node states"
      pattern: "phase_substep"
    - from: "backend/app/services/template_render_service.py"
      to: "backend/app/services/template_render_service.py"
      via: "emit_field_map_to_legacy + the third _iter_leaves branch feed the SAME build_context the docxtpl driver consumes"
      pattern: "build_context"
---

<objective>
Close GAP-C (emit-moment run honesty) and the D-09 scoped re-touch. Two concerns: (1) emit discrete `phase_substep` sub-events + the 5 distinguishable failure states, rendered on the EXISTING sketch-findings run-honesty rail (PhaseCard STATUS_META + FAILURE TAXONOMY — no new UI invented); (2) re-touch the citation gate (`check_coverage` / `_iter_leaves`) and the docxtpl driver mapping (`build_context`) so the flat `EmitFieldMap` (Plan 01) renders the SAME bytes as the equivalent nested map — the WR-04 "two copies cannot drift" discipline — with its own parity tests.

Purpose: the GAP-C complaint is the ABSENCE of emit sub-step events behind a static "Step 0 · working…" box; the surface to render them already exists. And the flat field-map must produce byte-identical output to the proven nested path. Implements D-09 (the scoped gate/driver re-touch with tests) + D-11 (emit sub-events + 5 failure states).
Output: `phase_substep` emit calls at each transition; the third `_iter_leaves`/`build_context` branch; PhaseCard sub-step + failure rendering; flat-map render-parity + back-compat tests GREEN.

## G-2 note (sketch-before-plan does NOT fire)
Per RESEARCH §5 / A5: GAP-C renders the EXISTING wire vocabulary on the ALREADY-SKETCHED phase-timeline / status-node rail (sketch 008/010/014/015). This is rendering existing sub-events on the existing rail — NOT a new visual affordance. If the executor finds it needs a genuinely new affordance beyond the rail, STOP and flag G-2 (do not invent UI inline). The acceptance bar is the validated sketch-findings design language.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-CONTEXT.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-PATTERNS.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-03-SUMMARY.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- Contracts extracted from the codebase + Plan 01/03. Use directly. -->

From backend/app/services/harness_engine.py (:130 — the one canonical XADD; reuse for every phase_substep):
```python
async def _emit(redis, run_id: UUID, type: str, **fields) -> None:
    await redis.xadd(f"run:{run_id}", {"data": json.dumps({"type": type, **fields})},
                     maxlen=10000, approximate=True)
```

From backend/app/services/template_render_service.py (:148 — the dual-shape walk to add a THIRD branch to; :182 / :304):
```python
def _iter_leaves(fm_dict: dict):
    scalars = fm_dict.get("scalars"); collections = fm_dict.get("collections")
    if scalars is not None or collections is not None:    # branch 1 (generic envelope)
        ...; return
    # branch 2 (flat spike-style {key: cited} + list-of-rows)
    ...
def check_coverage(...): ...     # :182 — uncited/invented semantics MUST stay unchanged
def build_context(...): ...      # :304 — maps field-map -> docxtpl context; driver UNCHANGED downstream
def emit_field_map_to_legacy(fm: EmitFieldMap) -> dict: ...   # Plan 01 — flat -> existing flat-dict shape
```

From frontend/src/components/panel/PhaseCard.tsx (:65-90 — extend STATUS_META + the closed FAILURE TAXONOMY; XSS rule :25-27 plain text children):
```typescript
const STATUS_META: Record<Phase["status"], StatusMeta> = {
  pending: {...}, running: {...}, done: {...}, failed: {...}, retrying: {...}, skipped: {...},
}
type FailureKind = "max_steps" | "gate_failed" | "wall_clock_timeout" | "reason_unknown"
```

RESEARCH §5 emit sub-event vocabulary (status field): forcing | emitting | recovering | validating | rendering | validated
RESEARCH §5 the 5 failure values (failure field): model_failed_to_emit | citation_gate_rejected | render_failed | integrity_failed | no_template_bound
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flat EmitFieldMap render parity + citation-gate re-touch (D-09 scoped sub-task) with its own tests</name>
  <files>backend/app/services/template_render_service.py, backend/tests/unit/test_template_render.py, backend/tests/unit/test_template_integrity.py</files>
  <read_first>
    - backend/app/services/template_render_service.py:148-179 (_iter_leaves dual-shape) + :182 (check_coverage) + :304 (build_context) — RESEARCH §3 "Map to docxtpl driver + citation gate"
    - backend/app/services/template_render_service.py — the Plan 01 EmitFieldMap + emit_field_map_to_legacy normalizer
    - backend/tests/unit/test_template_render.py — the existing render tests + the WR-04 "two copies cannot drift" discipline (101-06 history)
    - .planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md §3 (back-compat is structural) + PATTERNS.md "EmitFieldMap flat model"
  </read_first>
  <behavior>
    - A flat EmitFieldMap, normalized via emit_field_map_to_legacy, passes through _iter_leaves/check_coverage with the SAME uncited/invented/null-rate semantics as the equivalent nested GenericFieldMap
    - build_context turns the normalized flat shape back into the keyed dict the docxtpl template expects ({key: _cell(...)} scalars + {collection: [{col: _cell(...)}]} rows) — producing the SAME context as the nested path
    - The docxtpl driver (render_docx_template / _RENDER_DRIVER_SRC) is UNCHANGED downstream of build_context
    - Old 097/101 nested artifacts still validate + render byte-identically (back-compat)
  </behavior>
  <action>
    Add a THIRD branch to `_iter_leaves` (or route `EmitFieldMap` through `emit_field_map_to_legacy` BEFORE `_iter_leaves`/`check_coverage`/`build_context` — RESEARCH §3 offers either; prefer the normalizer route to keep `_iter_leaves` minimal, but if the flat-list shape reaches `_iter_leaves` directly, add the branch). Ensure `check_coverage` walks `(value, source_chunk_id)` per leaf on the flat shape with `invented_citation_count`/`uncited_value_count`/`null_rate` semantics UNCHANGED. Re-touch `build_context` (:304) so the normalized flat lists become the keyed dict the docxtpl template consumes — the driver stays UNCHANGED downstream. Do NOT modify `render_docx_template` / `_RENDER_DRIVER_SRC` / `run_replace`.

    Add tests (D-09 scoped sub-task — the mandated parity + back-compat coverage):
    - `test_flat_map_renders_identical` (in test_template_render.py): a flat `EmitFieldMap` and its equivalent nested `GenericFieldMap` produce the SAME `build_context` output (and, where the test tier can render, the SAME bytes) — the WR-04 discipline.
    - `test_flat_map_citation_gate_parity` (test_template_render.py): `check_coverage` returns identical verdicts (uncited/invented/null-rate) for the flat vs nested shape over the same data, including an invented-citation case (rejected).
    - `test_legacy_nested_still_renders` (test_template_render.py): a 097/101-shaped nested map still flows through build_context unchanged.
    - Extend test_template_integrity.py: `test_is_truncated_rejects` already exists — confirm it covers the flat path; add a flat-shape truncation row if missing.
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_template_render.py tests/unit/test_template_integrity.py tests/unit/test_emit_field_map.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_template_render.py tests/unit/test_template_integrity.py -x -q` passes
    - `grep -c "def test_flat_map_renders_identical" backend/tests/unit/test_template_render.py` == 1
    - `grep -c "def test_flat_map_citation_gate_parity" backend/tests/unit/test_template_render.py` == 1
    - `git diff backend/app/services/template_render_service.py` shows NO change to `render_docx_template`/`_RENDER_DRIVER_SRC`/`run_replace` bodies (driver unchanged downstream of build_context)
    - `grep -c "class GenericFieldMap" backend/app/services/template_render_service.py` == 1 (back-compat preserved)
  </acceptance_criteria>
  <done>The flat EmitFieldMap renders the same context/bytes as the nested map; check_coverage parity holds; the docxtpl driver is unchanged downstream; nested artifacts still render; parity + back-compat tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-C emit sub-events (backend phase_substep) + 5 failure states on the existing PhaseCard rail (frontend)</name>
  <files>backend/app/services/harness/phase_types.py, frontend/src/components/panel/PhaseCard.tsx, frontend/src/components/panel/PhaseCard.test.tsx</files>
  <read_first>
    - backend/app/services/harness_engine.py:130 (_emit one canonical XADD) — PATTERNS.md "One canonical XADD for run-honesty events"
    - backend/app/services/harness/phase_types.py — the _exec_llm_emit transitions from Plan 03 (each transition is where a phase_substep emits)
    - frontend/src/components/panel/PhaseCard.tsx:25-27 (XSS plain-text rule) + :65-90 (STATUS_META + FAILURE TAXONOMY) — PATTERNS.md "PhaseCard.tsx emit sub-events render"
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (run-honesty / status-node rail / closed failure taxonomy + reason_unknown fallback — sketch 008/010/014/015); references/harness-phase-timeline.md + references/chat-tool-card-unification.md
    - .planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md §5 (the vocabulary + the 5 failure-state mapping table)
  </read_first>
  <behavior>
    - _exec_llm_emit emits one phase_substep event per transition via the EXISTING _emit: status in {forcing, emitting, recovering, validating, rendering, validated}; recovering only fires when D-06 NATIVE recovery runs (amber tint = degraded-but-honest)
    - Each terminal failure emits a phase_substep with a failure field in {model_failed_to_emit, citation_gate_rejected, render_failed, integrity_failed, no_template_bound}
    - PhaseCard renders each status as a sub-row / status-node state on the existing rail; each failure renders failed-as-failed (closed taxonomy + reason_unknown fallback), never an empty 'done' card (RC-4)
    - Every agent-supplied string is a plain React text child (XSS rule); no new visual affordance beyond the existing rail (A5 / G-2 does not fire)
  </behavior>
  <action>
    Backend: in `_exec_llm_emit` (Plan 03), add an `await _emit(ctx.redis, ctx.run_id, "phase_substep", phase_slug=..., status=<vocab>)` at each transition (`forcing` when building the forced request, `emitting` before the forced shot, `recovering` only when forced_emit reports `recovered_from_narration`, `validating` around check_coverage, `rendering` before the post_processor, `validated` on integrity-pass). On each terminal failure, emit `phase_substep` with the matching `failure=<one of the 5>` (mirror the receipt event_type from Plan 03). Reuse the EXISTING `_emit` — one event vocabulary, no new wire path. Do NOT branch the shared SSE emitter.

    Frontend: in PhaseCard.tsx, extend `STATUS_META` with the sub-step states (map `forcing/emitting/recovering/validating/rendering` to the active/pulsing-primary node state per sketch 014; `recovering` gets the amber/degraded tint per sketch 010; `validated` -> the done/green node). Extend the closed `FailureKind` taxonomy with the 5 emit failure values; render each failed-as-failed with a human label (closed taxonomy + the EXISTING `reason_unknown` fallback). Render the sub-steps as sub-rows under the `fill` phase's RunCard on the existing status-node rail — do NOT add a new container/affordance. Preserve the XSS rule (every agent string is a plain text child, no dangerouslySetInnerHTML). Add `PhaseCard.test.tsx` cases: each sub-step status renders the expected node state; each of the 5 failures renders failed-with-a-reason (never a 'done' card); an unknown sub-step status falls back to reason_unknown (never crashes).
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_llm_emit_executor.py -x -q && cd ../frontend && npx vitest run src/components/panel/PhaseCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "phase_substep" backend/app/services/harness/phase_types.py` >= 6 (one per transition + failures)
    - The backend phase_substep emits reuse `_emit` (no new XADD path): `grep -c "_emit(" backend/app/services/harness/phase_types.py` increased; no new `xadd(` call added in phase_types.py
    - `grep -c "model_failed_to_emit\|citation_gate_rejected\|render_failed\|integrity_failed\|no_template_bound" frontend/src/components/panel/PhaseCard.tsx` == 5 (all 5 failure values present)
    - `npx vitest run src/components/panel/PhaseCard.test.tsx` passes
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/panel/PhaseCard.tsx` == 0 (XSS rule held)
    - No new container component file added (renders on the existing rail — A5)
  </acceptance_criteria>
  <done>The emit moment streams discrete phase_substep sub-events + 5 distinguishable failure states on the existing PhaseCard rail (no new UI); failures render failed-as-failed; XSS rule held; tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent/run text → PhaseCard render | run-honesty event strings cross into the DOM |
| flat field-map → docxtpl context | the normalized flat shape feeds the render command |
| emit transition → SSE wire | sub-events must reuse the shared one-XADD path, never a new branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101.1-04-01 | Tampering | XSS via a run-honesty event string rendered in PhaseCard | mitigate | every agent-supplied string is a plain React text child (PhaseCard XSS rule :25-27); no `dangerouslySetInnerHTML`; the closed taxonomy maps to fixed labels |
| T-101.1-04-02 | Spoofing | an empty 'done' card hiding a failure (RC-4 regression) | mitigate | the 5 failure states render failed-as-failed via the closed taxonomy + `reason_unknown` fallback; an unknown sub-step never renders as 'done' |
| T-101.1-04-03 | Tampering | the flat-map re-touch drifting from the nested render path (silent corruption) | mitigate | the WR-04 "two copies cannot drift" parity test asserts identical build_context output; the docxtpl driver is unchanged downstream of build_context |
| T-101.1-04-04 | Tampering | a forcing/provider branch leaking into the shared SSE emitter via the new sub-events | mitigate | phase_substep reuses the EXISTING `_emit` one-XADD path; no new wire branch; the D-14 guard (Plan 02) still holds |
</threat_model>

<verification>
- `pytest tests/unit/test_template_render.py tests/unit/test_template_integrity.py tests/unit/test_emit_field_map.py -x -q` passes
- `npx vitest run src/components/panel/PhaseCard.test.tsx` passes
- Flat-map render parity proven (same build_context / bytes as nested); nested back-compat held
- All 6 sub-step statuses + 5 failure states render on the existing rail; no new UI affordance (A5)
- phase_substep reuses the one-XADD `_emit`; shared SSE path not branched
- Net-new failures = 0 against the SEED-056 baseline (baseline checkout, not raw count)
</verification>

<success_criteria>
- The emit moment streams discrete `forcing -> emitting -> [recovering] -> validating -> rendering -> validated` sub-events (D-11)
- The 5 distinguishable failure states render failed-as-failed on the existing PhaseCard rail (D-11 / RC-4) — never an empty 'done'
- The flat `EmitFieldMap` renders the SAME context/bytes as the nested map; check_coverage parity holds; the driver is unchanged downstream (D-09)
- Old nested artifacts still validate + render (back-compat)
- No new UI invented (G-2 does not fire); XSS rule held
</success_criteria>

<output>
After completion, create `.planning/phases/101.1-guaranteed-emission-layer/101.1-04-SUMMARY.md`
</output>
