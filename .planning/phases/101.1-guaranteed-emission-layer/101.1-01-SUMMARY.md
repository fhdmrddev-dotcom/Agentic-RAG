---
phase: 101.1-guaranteed-emission-layer
plan: 01
subsystem: api
tags: [pydantic, harness, emitter-registry, model-capabilities, supabase-migration, forced-emission, structured-output]

# Dependency graph
requires:
  - phase: 101-template-fill-integrity-validation
    provides: GenericFieldMap/Cited + build_field_map_tool_schema + check_coverage/build_context deterministic core; harness_audit table (059) + write_audit helper; PhaseConfig discriminated union (5 members)
  - phase: 092.5-provider-gateway-extraction
    provides: GatewayRequest envelope (carries tool_choice) + open_stream dispatcher (the D-14 forcing seam home, consumed by downstream plans)
provides:
  - Flat strict-friendly EmitFieldMap (FlatScalar/FlatRow) + emit_field_map_to_legacy normalizer (D-09 — additive, GenericFieldMap untouched)
  - Closed EMITTER_REGISTRY + register_emitter + resolve_emitter (D-04 — render_template the v1 entry)
  - LlmEmitPhaseConfig — the 6th discriminated PhaseConfig member (additive, zero-migration)
  - forced_emission / strict_json_schema MODEL_CAPABILITIES flags, default-SAFE (D-15)
  - Migration 069 (ALTER harness_audit event_type CHECK to 16 kinds) + _AUDIT_EVENT_TYPES extension (D-12)
  - 5 Wave 0 test files (the Nyquist contract for the whole phase)
affects: [101.1-02 gateway-forcing, 101.1-03 llm_emit executor, 101.1-04 gate/driver parity, 101.1-05 operator apply+seed, 102 llm_judge, 107 receipt VIEW]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Closed-registry dispatch (EMITTER_REGISTRY mirrors PROGRAMMATIC_PHASE_REGISTRY — name not present RAISES, never eval'd)"
    - "Flat strict-friendly cited shape (citation as SIBLING fields, extra=forbid everywhere, bounded depth 3, no minLength/maxLength) for cross-provider forced emission"
    - "Additive normalizer seam (emit_field_map_to_legacy) keeps a new model wire-compatible with the existing gate/driver without re-touching them"
    - "Default-SAFE capability flags read via get_model_capability(id).get('forced_emission', False) so a case-sensitivity registry miss coerces, never wrongly forces"
    - "ALTER-only audit migration + in-lockstep frozenset (069 CHECK ↔ _AUDIT_EVENT_TYPES)"

key-files:
  created:
    - backend/app/services/harness/emitters.py
    - supabase/migrations/069_harness_audit_emit_kinds.sql
    - backend/tests/unit/test_emit_field_map.py
    - backend/tests/unit/test_forced_emit.py
    - backend/tests/unit/test_gateway_forcing.py
    - backend/tests/unit/test_llm_emit_executor.py
    - backend/tests/unit/test_harness_audit_emit.py
  modified:
    - backend/app/services/template_render_service.py
    - backend/app/models/harness.py
    - backend/app/config.py
    - backend/app/db/workflows.py

key-decisions:
  - "D-09 flat EmitFieldMap is ADDITIVE — GenericFieldMap/Cited byte-untouched (097/101 nested artifacts still model_validate); the normalizer is the seam so the gate/driver are NOT re-touched in this plan (Plan 04 does, with its own parity tests)"
  - "D-04 EMITTER_REGISTRY is a closed dataclass dict; resolve_emitter raises on unknown (T-101.1-01-02 — never eval/getattr/import)"
  - "D-15 forced_emission default-SAFE: a registry/case miss reads False (TIER-COERCE) — proven for the minimax-m3 lowercase case-miss"
  - "D-12 migration 069 is an ALTER (table exists since 059), not a CREATE; does NOT touch INSERT-only RLS; receipt fields ride metadata jsonb (no new columns)"

patterns-established:
  - "Pattern: register a future deliverable = one EMITTER_REGISTRY entry (schema + driver), never new engine code (D-04)"
  - "Pattern: audit event_type kinds live in BOTH the SQL CHECK and the _AUDIT_EVENT_TYPES frozenset, extended in lockstep"

requirements-completed: []  # TMPL-02 stays OPEN — marks complete at phase verification (live cross-provider UAT), the 099/WFSKILL-01 multi-plan convention

# Metrics
duration: ~40min
completed: 2026-06-11
---

# Phase 101.1 Plan 01: Guaranteed-Emission Substrate Summary

**The additive schema + registry + config + migration foundation every downstream emission plan stands on: a flat strict-friendly `EmitFieldMap` (citation-as-sibling, depth-3, additionalProperties:false everywhere) + its legacy normalizer, a closed `EMITTER_REGISTRY`, the `LlmEmitPhaseConfig` 6th union member, two default-SAFE `MODEL_CAPABILITIES` forcing flags, migration 069 (ALTER the `harness_audit` CHECK to 16 kinds), and the 5 Wave 0 RED test stubs (the Nyquist contract for the phase).**

## Performance

- **Duration:** ~40 min
- **Started:** 2026-06-11
- **Completed:** 2026-06-11
- **Tasks:** 3
- **Files modified:** 12 (5 created tests + emitters.py + migration 069 + 4 source files)

## Accomplishments

- **D-09 flat model:** `FlatScalar`/`FlatRow`/`EmitFieldMap` in `template_render_service.py` — `extra="forbid"` at every level (→ `additionalProperties:false`), citation as SIBLING fields (not a nested `Cited{}` wrapper), bounded nesting depth 3, no `minLength`/`maxLength`/`pattern`. Plus `emit_field_map_to_legacy(fm) -> dict` mapping the flat lists back to the legacy flat-dict shape the existing `_iter_leaves`/`check_coverage`/`build_context` consume. `GenericFieldMap`/`Cited` are byte-untouched (back-compat proven by `test_legacy_nested_still_validates`).
- **D-04 closed EMITTER_REGISTRY:** new `harness/emitters.py` mirroring `programmatic.py` — a closed `dict[str, EmitterEntry]` + `register_emitter` decorator + `resolve_emitter` that RAISES on an unknown name (T-101.1-01-02; never eval/getattr/import). `render_template` is the v1 entry (`schema_builder=build_field_map_tool_schema`, `post_processor=None` until the executor plan wires the render-dispatch).
- **D-04 LlmEmitPhaseConfig:** the 6th discriminated `PhaseConfig` member in `models/harness.py` (`phase_type:Literal["llm_emit"]`, `prompt`, `emitter="render_template"`, `model`, + shape-symmetry optionals). Additive / zero-migration; `_StrictBase` `extra="forbid"` rejects an injected key (T-101.1-01-01); old JSONB rows still `model_validate()`.
- **D-15 forcing flags:** `forced_emission` / `strict_json_schema` added to `ModelCapability(TypedDict, total=False)` and populated default-SAFE — `forced_emission:True` on the TIER-FORCE rows (OpenAI/Anthropic/Google/DeepSeek-v4/GLM/MiniMax/forceable-OpenRouter), ABSENT on Kimi/Moonshot/Ollama (→ TIER-COERCE). `strict_json_schema:True` only on verified rows (OpenAI + DeepSeek-v4); MiniMax left ABSENT (A1, live-verify). A registry/case miss SAFELY reads False (proven for `minimax-m3` lowercase).
- **D-12 audit substrate:** migration `069_harness_audit_emit_kinds.sql` ALTERs the existing CHECK (table exists since 059) to add 7 emit kinds; `_AUDIT_EVENT_TYPES` extended in lockstep (9 → 16); `write_audit` reused unchanged. Migration authored only — NOT applied (Plan 05 operator step).
- **Wave 0 RED stubs:** 5 test files matching the VALIDATION.md Per-Task map; the 3 model-back-compat tests are real GREEN against this plan's own code, the rest are `xfail(strict=False)` un-marked by their owning downstream plans.

## Task Commits

1. **Task 1: Wave 0 RED test stubs** — `e4f93251` (test)
2. **Task 2: flat EmitFieldMap + normalizer + EMITTER_REGISTRY + LlmEmitPhaseConfig + D-15 flags** — `23375e20` (feat, TDD GREEN — un-marked the 3 model tests)
3. **Task 3: migration 069 + _AUDIT_EVENT_TYPES extension** — `d2905b2f` (feat — un-marked the 2 audit tests)

_TDD note: Task 1 is the RED gate (test commit); Tasks 2 and 3 are the GREEN gates (feat commits that un-mark their owned stubs). No REFACTOR commit needed._

## Files Created/Modified

- `backend/app/services/template_render_service.py` — added `FlatScalar`/`FlatRow`/`EmitFieldMap` + `emit_field_map_to_legacy`; imported `ConfigDict`. `GenericFieldMap`/`Cited` untouched; no `docxtpl` at module top (Pitfall 4 intact).
- `backend/app/services/harness/emitters.py` (NEW) — closed `EMITTER_REGISTRY` + `register_emitter` + `resolve_emitter` + `EmitterEntry` dataclass; `render_template` v1 entry.
- `backend/app/models/harness.py` — `LlmEmitPhaseConfig` + appended to the `PhaseConfig` Union (6th member).
- `backend/app/config.py` — `forced_emission`/`strict_json_schema` TypedDict keys + populated rows (default-SAFE).
- `backend/app/db/workflows.py` — `_AUDIT_EVENT_TYPES` 9 → 16 in lockstep with 069; `write_audit` docstring/error 9 → 16.
- `supabase/migrations/069_harness_audit_emit_kinds.sql` (NEW) — ALTER the event_type CHECK (NOT applied here).
- `backend/tests/unit/test_emit_field_map.py` / `test_forced_emit.py` / `test_gateway_forcing.py` / `test_llm_emit_executor.py` / `test_harness_audit_emit.py` (NEW) — the 5 Wave 0 stub files.

## Decisions Made

None beyond honoring the LOCKED D-01..D-15 decisions and the 3 RESEARCH drift corrections (harness_audit already exists → ALTER not CREATE; forcing lands at the 092.5 gateway boundary off `GatewayRequest.tool_choice`; `PHASE_TYPE_REGISTRY` at `harness_engine.py:111`). All implementation followed the plan's per-task actions and the RESEARCH §3 model verbatim.

## Deviations from Plan

None - plan executed exactly as written.

The one nuance worth recording (NOT a deviation — explicit plan/TDD ordering): the 3 model-back-compat tests (`test_flat_model_strict_schema`, `test_legacy_nested_still_validates`, `test_emit_field_map_to_legacy_roundtrip`) were authored `xfail(strict=False)` in the Task 1 RED commit (so the suite exits 0 BEFORE the model exists) and un-marked to real GREEN in the Task 2 feat commit. This is the project's TDD un-mark-on-landing convention (098/099/100/101), exactly what the plan's Task 1 action prescribes ("write them as REAL GREEN behavioral tests … they should already be GREEN from Task 1" — reconciled to RED-in-Task-1-then-GREEN-in-Task-2 because Task 2 is where the target model lands).

## Known Stubs

| Stub | File | Line | Reason / resolving plan |
|------|------|------|-------------------------|
| `EmitterEntry(post_processor=None)` for `render_template` | `backend/app/services/harness/emitters.py` | v1 entry registration | INTENTIONAL per D-04 — the deterministic render-dispatch is wired by the downstream `_exec_llm_emit` executor plan (reusing the hardened `_handle_render_template`, one render code path). The schema_builder half is live now. |
| 9 `xfail(strict=False)` Wave 0 tests | `test_forced_emit.py`, `test_gateway_forcing.py`, `test_llm_emit_executor.py` | per-test | RED-by-design (the Nyquist contract) — un-marked to GREEN by each owning downstream plan (gateway-forcing / executor). This is the VALIDATION.md Wave 0 mandate, not an incomplete stub. |

These stubs are the planned interface-first seams every downstream plan implements against; they do not block this plan's goal (the substrate contract is fully defined + the 5 GREEN tests prove the model/registry/config/audit halves work).

## Net-New Failure Proof (SEED-056 baseline)

- **Target modules GREEN:** the 5 Wave 0 files = `5 passed, 3 skipped, 4 xfailed, 2 xpassed` (suite exits 0). The 3 model tests + 2 audit tests are real GREEN.
- **Wave slice** (`-k "emit or render or template or gateway or harness or capabilit or config"`): `11 failed, 244 passed`. All 11 failures proven PRE-EXISTING SEED-056 rot:
  - `test_get_model_capability_inference.py::test_infer_openai_from_gpt_prefix` (`assert 300 == 90`) — I touched `config.py`, so proven by **stash-at-base**: checked out `config.py` at the pre-plan commit `f1fd0716`, the test failed IDENTICALLY (`assert 300 == 90`), then restored to HEAD. The failure is a test-vs-code drift on `_INFERRED_DEFAULT_TIMEOUT_S` (300, not 90) — untouched by my `forced_emission`/`strict_json_schema` additions.
  - The other 10 (`test_063_post_then_subscribe`, `test_067_4_suggestion_emit`, `test_075_code_stdout_progressive`, `test_threads_skills`, `test_harness_gates::test_bounded_retry_reaches_failed_after_3_attempts` [the documented `KeyError:'tool_call_id'`], `test_075_4_final_output_files_payload`, `test_phase56_iteration_start`) — grep-confirmed NONE import any module I changed (`template_render_service` / `harness.emitters` / `LlmEmitPhaseConfig` / `EmitFieldMap` / `emit_field_map_to_legacy` / `forced_emission` / `strict_json_schema`); the failing harness test has 0 references to `_AUDIT_EVENT_TYPES`/`write_audit`/emit kinds.
- **Net-new failures = 0.**

## Self-Check: PASSED

(see appended self-check section below)

## Issues Encountered

None. The `is_truncated(meta: dict)` signature in `template_render_service.py` takes a dict (not kwargs), which the `test_truncation_rejected` stub in `test_forced_emit.py` calls with kwargs — but that test is `xfail(strict=False)` and its owning forced-emit plan reconciles the call shape; collection stays green.

## User Setup Required

None for this plan. Migration 069 is authored only — Plan 05 (operator, `autonomous:false`) applies it via the Supabase SQL editor + regenerates `full-schema.sql` + re-seeds the D-13 fixture + runs the live cross-provider UAT. The sandbox image rebuild/bump to `101.1` (for live render) is also a Plan-05 operator step.

## Next Phase Readiness

- The substrate contract is fully defined — every downstream plan (gateway forcing, `_exec_llm_emit`, gate/driver parity, operator apply+seed) imports these symbols and implements against the Wave 0 stubs.
- **Plan 02 (gateway forcing / D-14):** threads forcing through `GatewayRequest` + translates per-adapter; un-marks `test_gateway_forcing.py` (esp. `test_no_provider_branch_in_shared_path` — the RED LINE guard).
- **Plan 03 (`_exec_llm_emit` executor):** the 6th harness executor; un-marks `test_llm_emit_executor.py` + wires the `EmitterEntry.post_processor` render-dispatch.
- **Plan 04 (gate/driver parity):** re-touches `_iter_leaves`/`build_context` for the flat shape WITH parity tests (the normalizer seam this plan left additive).
- **Plan 05 (operator):** applies migration 069, regenerates `full-schema.sql`, re-seeds D-13, runs live UAT.

---
*Phase: 101.1-guaranteed-emission-layer*
*Completed: 2026-06-11*
