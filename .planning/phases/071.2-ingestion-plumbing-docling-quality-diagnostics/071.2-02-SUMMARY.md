---
phase: 071.2-ingestion-plumbing-docling-quality-diagnostics
plan: 02
subsystem: ui
tags: [frontend, status-badge, realtime, ingestion-step, react, supabase-realtime, uat]

requires:
  - phase: 071.2-ingestion-plumbing-docling-quality-diagnostics
    plan: 01
    provides: "Instant-201 BackgroundTask refactor + INSERT documents row with status='pending' BEFORE BackgroundTask fires (Supabase Realtime broadcasts new row immediately)"
  - phase: 071.2-ingestion-plumbing-docling-quality-diagnostics
    plan: 04
    provides: "Byte-identical preservation of the two `ingestion_step` UPDATE writes at documents.py:1158 (extracting_tables) and :1169 (extracting_images) — the writes that feed Plan 02's new badge labels (D-071.2-11 preservation guarantee)"
provides:
  - "DocumentStatusBadge.tsx::ingestionStepLabel renders friendly text for 6 ingestion_step values: extracting, extracting_tables, extracting_images, chunking, embedding, metadata"
  - "Closes D-071.2-11 label-side: the two `extracting_tables` / `extracting_images` cases that the backend already writes but the frontend previously fell through to the generic 'processing' fallback for"
affects:
  - 072 (RAG-MM-LIFT) — multimodal lift will continue writing `extracting_tables` / `extracting_images` ingestion_step values; badge already labels them
  - Future ingestion_step additions — single-file pattern is clear (one `if (step === "X") return "Label"` line per new value)

tech-stack:
  added: []
  patterns:
    - "Switch-on-string ingestionStepLabel function: one `if (step === \"X\") return \"Label\"` line per ingestion_step value backend writes; fallthrough to 'processing' for unknown values — keeps the function additive across phases"

key-files:
  created:
    - ".planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-02-SUMMARY.md"
  modified:
    - "frontend/src/components/ingestion/DocumentStatusBadge.tsx (added 2 switch cases in ingestionStepLabel — 2 lines added, 0 removed)"

key-decisions:
  - "D-071.2-11 closure (label side): added `if (step === \"extracting_tables\") return \"Extracting tables\"` and `if (step === \"extracting_images\") return \"Extracting images\"` to ingestionStepLabel. Insertion point: AFTER the existing `if (step === \"extracting\")` case and BEFORE the `if (step === \"chunking\")` case — matches the natural backend-write ordering (extract → tables → images → chunk → embed → metadata)."
  - "Did NOT touch `useDocuments.ts` Realtime subscription — Plan 02's PATTERNS.md §Plan 02 explicitly classified the Realtime wiring as verify-only. The optimistic-add + INSERT/UPDATE handler logic already correctly drives `pending → processing → completed` transitions from Supabase postgres_changes events."
  - "Task 2 (Chrome MCP UAT) returned as `checkpoint:human-verify` — sequential executor lacks `mcp__chrome-devtools__*` tools in this session (chrome-devtools MCP not installed in agent's tool list) AND network probes (curl / Invoke-WebRequest) denied by sandbox, so neither automation path nor a manual probe of the dev stack is reachable from here. Orchestrator must drive the live UAT against http://localhost:5173/ with test login fhdmrd@gmail.com / 123456."

patterns-established:
  - "Pattern: 'ingestion_step label add' — when backend introduces a new `ingestion_step` value, add a single `if (step === \"value\") return \"Friendly Label\"` line to DocumentStatusBadge.tsx::ingestionStepLabel. Keep ordering aligned with the backend write sequence so the visual cycle matches the code path."

requirements-completed: [RAG-DOCLING-01]

duration: ~4min
completed: 2026-05-15
---

# Phase 071.2 Plan 02: Frontend status badge label additions + Realtime lifecycle UAT Summary

**Two new ingestion_step label cases (`extracting_tables`, `extracting_images`) in DocumentStatusBadge.tsx — closes D-071.2-11 label-side; live Realtime-lifecycle UAT (Task 2) returned as `checkpoint:human-verify` for orchestrator to drive.**

## Performance

- **Duration:** ~4 min for Task 1 autonomous implementation (Task 2 deferred — see Checkpoint Status section)
- **Started:** 2026-05-15T20:00Z (approx — executor agent spin-up)
- **Completed (Task 1):** 2026-05-15T20:04Z
- **Tasks:** 1/2 autonomous executed (Task 2 is `checkpoint:human-verify`)
- **Files modified:** 1 (DocumentStatusBadge.tsx — 2 lines added)
- **Commits:** 1 (Task 1 GREEN — no RED/REFACTOR because the plan is not `tdd="true"`; Task 1's `<verify>` block is a single `tsc --noEmit` gate + grep)

## Accomplishments

- `frontend/src/components/ingestion/DocumentStatusBadge.tsx::ingestionStepLabel` widened from 4 to 6 backend-aligned cases:
  - `extracting` → "Extracting" (unchanged)
  - **`extracting_tables` → "Extracting tables"** (NEW — closes D-071.2-11)
  - **`extracting_images` → "Extracting images"** (NEW — closes D-071.2-11)
  - `chunking` → "Chunking" (unchanged)
  - `embedding` → "Embedding" (unchanged)
  - `metadata` → "Extracting metadata" (unchanged)
- Insertion point: AFTER the existing `extracting` case and BEFORE the `chunking` case — matches the backend ingestion_step write ordering in `documents.py::ingest_document` (extract → tables → images → chunk → embed → metadata) so the visible label cycle and the backend code path march in lockstep.
- Total diff: 2 lines added, 0 removed. TypeScript compilation (`cd frontend && npx tsc --noEmit`) exits 0.
- `useDocuments.ts` Realtime wiring untouched per plan's verify-only directive. The existing `supabase.channel("documents-changes").on("postgres_changes", ...)` handler at lines 37-65 already routes INSERT → optimistic add, UPDATE → in-place mutation, DELETE → filter; combined with Plan 01's `status='pending'` INSERT-before-BackgroundTask refactor, the badge will drive `pending → processing (with the new labels) → completed` without manual refresh once the orchestrator runs Task 2's live UAT.

## Task Commits

1. **Task 1 GREEN:** `e24ce04` — `feat(071.2-02): add extracting_tables + extracting_images badge labels`

No RED/REFACTOR commits — the plan declares `type=execute` (not `type=tdd`); Task 1's `<verify>` block is a single `tsc --noEmit` + grep gate, not a behavior test. Auto-mode is disabled (`workflow.auto_advance=false`).

**Plan metadata (final summary commit):** to be created after this SUMMARY is written + STATE/ROADMAP updates land.

## Files Created/Modified

- `frontend/src/components/ingestion/DocumentStatusBadge.tsx` — 2-line addition inside `ingestionStepLabel` function body (lines 18-19). Style classes (lines 9-14), component props (Props interface at lines 3-7), component body (lines 24-39) all untouched.
- `.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-02-SUMMARY.md` — this file.

## Decisions Made

- **Insertion ordering follows backend write order, not alphabetical.** The plan's PATTERNS.md §Plan 02 specified "insert AFTER `extracting` AND BEFORE `chunking`" — this matches the natural ingestion pipeline sequence (extract → tables → images → chunk → embed → metadata). Alphabetical would have put `extracting_images` before `extracting_tables` and broken the visual march; the chosen order makes the label cycle match the user's mental model of the pipeline.
- **No touch on `useDocuments.ts`.** Plan explicitly classified the Realtime + optimistic-add wiring as verify-only. Modifying the subscription handler would be scope creep AND would invalidate the existing 067.x/Phase 071.2-01 test coverage that proves the BackgroundTask seam works end-to-end.
- **Task 2 returned as checkpoint, not skipped.** Two compounding blockers from the sequential executor: (a) the `mcp__chrome-devtools__*` tools are not in this session's accessible tool list — only Read/Write/Edit/Bash/Grep/Glob were exposed; (b) the sandbox denies network probes (`curl`, PowerShell `Invoke-WebRequest`), so I cannot even confirm whether the dev stack (backend uvicorn on :8000, frontend Vite on :5173, Supabase local on :54321/:54323) is currently running. The plan acknowledges this exact path in its `<how-to-verify>` block ("If Chrome MCP cannot find the tools this session, fall back to a manual browser run + Supabase Studio for row inspection"). Returning the checkpoint cleanly to the orchestrator lets the user (who can drive a real browser) run the SC#1/SC#2/SC#3 lifecycle verification.

## Task 2 — Checkpoint Status

**Task 2 is `checkpoint:human-verify` — NOT executed by this sequential executor.** Two compounding blockers:

1. **Chrome DevTools MCP unavailable in this session.** The agent's exposed tools are limited to `Read`, `Write`, `Edit`, `Bash`, `Grep`, `Glob`. No `mcp__chrome-devtools__*` callables. The plan anticipates this fallback ("If Chrome MCP cannot find the tools this session, fall back to a manual browser run + Supabase Studio for row inspection") but the executor cannot drive a manual browser.
2. **Network probes denied by sandbox.** Both `curl` and PowerShell `Invoke-WebRequest` calls were rejected by the Bash permission gate, so the executor cannot verify dev-stack health (`/health`, `localhost:5173`, `supabase status`) pre-UAT.

### What the orchestrator/user must run for Task 2

Per the plan's `<how-to-verify>` block — verbatim:

1. **Confirm dev stack is running:**
   - Backend: `curl -s http://localhost:8000/health` returns 200 in <500ms
   - Frontend: `curl -sI http://localhost:5173/ | head -1` returns `HTTP/1.1 200 OK`
   - Supabase local: `supabase status` shows running
   - If any are down, start them per `backend/README.md` and `frontend/README.md`. **DO NOT start uvicorn from this executor — return-back instead.**

2. **Login + observe:** Navigate to http://localhost:5173/, log in with `fhdmrd@gmail.com` / `123456`, open the Documents / Knowledge page. Open DevTools Network + Console panels.

3. **Upload thesis PDF** (the same fixture exercised in Phase 071.1 — or any ~1MB PDF if not at hand).

4. **Within 1.5s of the upload click, verify:**
   - DevTools Network: /upload Status 201, Time <1500ms (SC#1)
   - New row in documents list with `pending` badge visible (SC#3 part A — Realtime INSERT)
   - Supabase Studio: `SELECT status, ingestion_step FROM documents ORDER BY created_at DESC LIMIT 1;` shows `status='pending'`, `ingestion_step IS NULL` initially

5. **Watch badge transitions WITHOUT refresh** (SC#3 part B):
   - `pending` → `processing` with label cycling through `Extracting` → `Extracting tables` → `Extracting images` → `Chunking` → `Embedding` → `Extracting metadata` → final `completed`
   - **Critical:** confirm the two NEW labels ("Extracting tables" + "Extracting images") render — if they show "processing" instead, EITHER Task 1's grep missed something OR Plan 04 accidentally removed the backend `ingestion_step` UPDATE writes at documents.py:1158/1169. Re-check `grep -n "ingestion_step.*extracting_tables" backend/app/api/documents.py` — must return ≥ 1 hit (Plan 04 verified this — line 1158).

6. **Concurrent /health probe** (SC#2 — proves Plan 01's threadpool sweep doesn't regress under in-flight extract):
   ```bash
   while true; do printf '%s ' $(date +%H:%M:%S); time curl -s http://localhost:8000/health > /dev/null; sleep 1; done
   ```
   Every sample during the extract should complete <2s real time.

7. **Record result** in `.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-HUMAN-UAT.md` (create if absent; mirror the 071.1 HUMAN-UAT.md scoreboard shape).

### Task 2 UAT Results (to be filled by orchestrator/user)

| Check | Evidence | Verdict |
|---|---|---|
| SC#1: /upload 201 latency <1.5s | TBD — DevTools Network panel screenshot or response-time number | TBD |
| SC#2: /health <2s under concurrent extract | TBD — `time curl` sample outputs from the loop | TBD |
| SC#3 part A: Row visible with `pending` badge within 1s of 201 | TBD — DOM snapshot or Chrome screenshot at t≈1s | TBD |
| SC#3 part B: Badge cycles through 6 labels including `Extracting tables` AND `Extracting images` | TBD — DOM snapshots or screen recording showing both new labels | TBD |
| SC#3 part C: No manual page refresh required to reach `completed` | TBD — Chrome session continuity confirmation | TBD |

### Failure modes worth flagging

- **Label says "processing" during tables/images phase** → root cause is one of: (a) Task 1's grep missed an actual edit failure (unlikely — verified `git diff` shows 2 lines added; tsc clean); (b) Plan 04 accidentally removed the `ingestion_step` UPDATE writes at documents.py:1158/1169 (Plan 04's SUMMARY claims byte-identical preservation, verified via grep at lines 1158 + 1169 — see 071.2-04-SUMMARY.md acceptance criteria #9 + #10).
- **Row doesn't appear until refresh** → Realtime not firing. Check Supabase Studio → Realtime → channels for `documents-changes` activity. D-v2.5-03 says Realtime is best-effort; `useDocuments.ts` reconciles via `loadDocuments()` on mount, so worst case the refresh fallback works — but SC#3 requires the no-refresh path.
- **/health exceeds 2s at any point during extract** → Plan 01's threadpool sweep missed a call site. Re-grep `documents.py` for bare `supabase.table(...).execute()` chains outside `run_in_threadpool` windows.

## Deviations from Plan

### Auto-fixed Issues

None — Task 1 followed the plan verbatim:
- Insertion point AFTER `extracting`, BEFORE `chunking` — matches PATTERNS.md §Plan 02 directive
- Label strings exactly `"Extracting tables"` and `"Extracting images"` — matches plan's verbatim code block
- 2-space indentation + double-quote string style — matches existing surrounding lines
- No touch on style classes, component props, or JSX block — matches plan's "Do NOT touch" list
- No touch on `useDocuments.ts` — matches plan's verify-only directive

**Total deviations:** 0 auto-fixed.
**Impact on plan:** None — implementation matched the planned shape verbatim.

## Issues Encountered

- **Chrome DevTools MCP unavailable + network probes denied by sandbox** — see "Task 2 — Checkpoint Status" above. Not a Plan 02 implementation issue; an executor-environment limitation that triggers the plan's intended checkpoint-return path.

## Verification

Plan verify commands (from `<verify>` blocks):

```bash
cd frontend && npx tsc --noEmit
```

Result: exits 0 (no output) — TypeScript compiles cleanly across the entire frontend codebase including the modified `DocumentStatusBadge.tsx`.

### Acceptance criteria

| # | Criterion | Result |
|---|-----------|--------|
| 1 | `DocumentStatusBadge.tsx` contains literal `step === "extracting_tables"` | PASS (line 18, 1 match) |
| 2 | `DocumentStatusBadge.tsx` contains literal `step === "extracting_images"` | PASS (line 19, 1 match) |
| 3 | `DocumentStatusBadge.tsx` contains literal `"Extracting tables"` | PASS (line 18, 1 match) |
| 4 | `DocumentStatusBadge.tsx` contains literal `"Extracting images"` | PASS (line 19, 1 match) |
| 5 | `DocumentStatusBadge.tsx` still contains existing 4 cases (`extracting`, `chunking`, `embedding`, `metadata`) — no accidental deletion | PASS (lines 17, 20, 21, 22) |
| 6 | `cd frontend && npx tsc --noEmit` exits 0 | PASS (no errors, no output) |
| 7 | `git diff` shows ≤ 4 lines changed (2 inserted, 0 removed) | PASS (2 inserted, 0 removed) |
| 8 | Task 2 SC#1 + SC#2 + SC#3 lifecycle UAT (live thesis-PDF upload, Realtime badge cycle, concurrent /health) | DEFERRED — `checkpoint:human-verify` returned to orchestrator; requires live dev stack + browser |

## User Setup Required

None — no new env vars, no migrations, no service registration. Pure-frontend additive change; no schema or backend surface touched.

## Next Phase Readiness

- **Plan 05 (per-aspect extraction dispatcher)** is the last open plan in Phase 071.2. Independent of Plan 02 — touches `backend/app/services/extractors/aspects/`, `multimodal_service.py`, and a new migration 045. Plan 02's label additions are stable across Plan 05's dispatcher refactor because the dispatcher does NOT change which `ingestion_step` values the backend writes; it only changes which extractor produces the data inside the `extracting_*` step.
- **Phase 072 (RAG-MM-LIFT)** inherits a label-complete badge. When 072 raises `_MAX_VISION_CALLS` from 20 → 100 and adds the DOCX `related_parts` walk, the badge cycle stays unchanged (the `extracting_images` step write is preserved through 071.2 Plan 04 + this plan).
- **Task 2 UAT remains parked** for orchestrator/user. SC#1 (latency) + SC#2 (concurrent /health) + SC#3 (Realtime lifecycle with new labels) are the gate. Once GREEN, mark `- [x] 071.2-02-PLAN.md` in ROADMAP.md (orchestrator action — left `[ ]` by this executor per sequential_execution directive).

## Self-Check: PASSED

**Files claimed created/modified exist:**
- `frontend/src/components/ingestion/DocumentStatusBadge.tsx` — FOUND (modified; 2 new cases at lines 18-19 confirmed via grep)
- `.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-02-SUMMARY.md` — FOUND (this file)

**Commits exist:**
- `e24ce04` — to be verified post-write via `git log --oneline | grep e24ce04` (commit made before this summary was written)

**TDD gate compliance:**
- N/A — plan is `type=execute` (not `type=tdd`). Plan's `<verify>` is a single `tsc --noEmit` + grep gate. No RED commit required; no GREEN commit pair required. Single `feat(...)` commit (`e24ce04`) is the canonical artifact.

---
*Phase: 071.2-ingestion-plumbing-docling-quality-diagnostics — Plan 02*
*Completed: 2026-05-15 (Task 1 GREEN autonomous; Task 2 `checkpoint:human-verify` returned to orchestrator)*
