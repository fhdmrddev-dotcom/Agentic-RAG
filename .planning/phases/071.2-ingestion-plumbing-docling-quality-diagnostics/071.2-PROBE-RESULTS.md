# Phase 071.2 Plan 03 — Probe Results (Thesis Pair)

> **Status:** PENDING HUMAN VERIFICATION (Task 3 checkpoint)
>
> Plan 03 Tasks 1 + 2 shipped clean (commits `934def8` + `a4346cc`). Task 3
> requires the user (or orchestrator with live uvicorn + Supabase access) to
> restart the backend (so `do_formula_enrichment=True` takes effect after the
> `_CONVERTER` cache invalidates) and run the extended probe against the
> thesis PDF + DOCX siblings.

## How to run

```
# 1. Restart uvicorn (Pitfall 4 — _CONVERTER cache freezes pipeline options per process)
cd backend
venv/Scripts/python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 2. PDF probe — thesis document_id
cd backend
venv/Scripts/python.exe scripts/probe_docling_timeout.py 23cc112a-92c2-440f-83c9-13aa8bf3d53d

# 3. DOCX probe — sibling document_id (substitute the user's DOCX document_id)
venv/Scripts/python.exe scripts/probe_docling_timeout.py <docx-document-id>
```

## Expected PDF output (per RESEARCH-COMPLETENESS Part E predictions)

```
[probe] len(text)= ~8,000  len(full_markdown)= ~40,000+
[probe] chunks(text)= ~19  chunks(full_markdown)= ~400+
[probe] PDF: pdfplumber.images=2  fitz.get_images(full=True)=20+
[probe] PDF: pdfplumber.tables=4  docling.tables=25+
[VERDICT] DETECTION CEILING HIT: pdfplumber.images=2 << fitz.get_images(full=True)=...
[VERDICT] D-071.2-13 GATE GREEN: chunks(full_markdown)=... >= 3x chunks(text)=...
```

## Expected DOCX output

```
[probe] DOCX: inline_shapes=0  zip a:blip=10+  v:imagedata=0  word/media/=10+
[VERDICT] DETECTION CEILING HIT: inline_shapes=0 but ZIP a:blip=10+. Plan 05 zip_xpath_docx required.
```

## Actual PDF output (paste here)

```
TBD — paste full stdout from probe run above
```

## Actual DOCX output (paste here)

```
TBD — paste full stdout from probe run above
```

## D-071.2-13 Gate Decision

- [ ] **GREEN:** `chunks(full_markdown) >= 3 x chunks(text)` AND `len(full_markdown) >= len(text)` → Plan 05 cleared to ship per-aspect dispatcher with new defaults
- [ ] **RED:** gate failed → escalate per D-071.2-12 (Marker GPL-fenced opt-in via SEED-018 promotion); revise Plan 03 scope before Plan 05 commits

### Plan 05 detection-ceiling inputs (also captured by this probe)

- [ ] PDF: `fitz_images >> pdfplumber_images` → justifies `pymupdf_full` default for PDF image dispatcher
- [ ] PDF: `docling_tables >> pdfplumber_tables` → justifies `docling_tf` default for PDF table dispatcher
- [ ] DOCX: `zip a:blip > 0` AND `inline_shapes == 0` → justifies `zip_xpath` default + RAG-MM-LIFT-02 closure

---

**Verdict block:** (operator to type below after running probe)

> _PENDING — type "approved — gate GREEN, Plan 05 cleared" or "RED — escalate" after running probe._
