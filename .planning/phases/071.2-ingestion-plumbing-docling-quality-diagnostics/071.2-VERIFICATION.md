---
phase: 071.2-ingestion-plumbing-docling-quality-diagnostics
verified: 2026-05-15T22:00:00Z
status: human_needed
score: 8/10 auto-checkable success criteria verified; 2 success criteria (SC#4, SC#9) plus partial SC#3/SC#5/SC#8 parked for live UAT
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "SC#1 + SC#3 — Realtime status badge lifecycle (HUMAN-UAT #1)"
    expected: "After uploading a small PDF via the dev app (http://localhost:5173/), /upload returns 201 in <1.5s; new row appears with pending badge; badge transitions pending → processing (extracting → extracting_tables → extracting_images → chunking → embedding → metadata) → completed WITHOUT manual page refresh. Backend /health stays <2s during in-flight extract."
    why_human: "Requires live dev stack + browser + a fixture that doesn't OOM the local WSL env. Drive via Chrome DevTools MCP at http://localhost:5173/ with test login fhdmrd@gmail.com / 123456."
  - test: "SC#4 — D-071.2-12 quality floor on thesis PDF (HUMAN-UAT #4)"
    expected: "/reextract with default engines on thesis PDF yields tables ≥ 20 AND figures ≥ 10 AND chunks ≥ 200. Baseline pre-Plan-05 on thesis PDF was tables=4, figures=2, chunks=443."
    why_human: "Env-blocked — both Docling (timeout 136s) AND PyMuPDF subprocess (MemoryError) crash on thesis PDF in local WSL env. Root-cause investigation deferred to Phase 071.3 / SEED-019."
  - test: "SC#5 — Telemetry vs storage parity (HUMAN-UAT #3)"
    expected: "On a successful Docling-path extract, `pdf_extraction_runs.tables_count` equals `(SELECT count(*) FROM document_tables WHERE document_id=X)`. Same for `images_count` vs `document_images`. Code-shape verified (multimodal_service reads extracted_doc.tables/.images via Plan 04 precedence rule)."
    why_human: "Requires a Docling-completable fixture. Same env blocker as SC#4."
  - test: "SC#8 — DOCX zip_xpath end-to-end (HUMAN-UAT #5)"
    expected: "/reextract on DOCX whose only picture is a floating <wp:anchor> (NOT inline shape) yields document_images ≥ 1 via new zip_xpath_docx engine. Unit test test_inline_shapes_zero_zip_xpath_nonzero_on_anchor_only passes against programmatic fixture (closes binding gate at unit level)."
    why_human: "No anchor-only DOCX exists in local test user's library. End-to-end verification requires uploading a real anchor-only DOCX or running against a production env that has one."
  - test: "SC#9 — pymupdf_full vs pdfplumber image-count delta on thesis PDF"
    expected: "PDF with images inside Form XObjects returns MORE document_images rows under pymupdf_full than under pdfplumber.page.images on the thesis PDF. Probe Section B verdict block evaluates this delta."
    why_human: "Env-blocked — PyMuPDF subprocess OOMs on thesis PDF. Probe Section B logic exists and is unit-importable; live numbers require non-thesis fixture OR Phase 071.3 OOM fix."
  - test: "Probe harness run (HUMAN-UAT #2 — D-071.2-13 GREEN gate)"
    expected: "`cd backend && venv/Scripts/python.exe scripts/probe_docling_timeout.py <thesis-pdf-id>` verifies: (a) markdown sanity (≥1 `\\n## ` header), (b) tables/images/equations counts surface, (c) wall-clock under configured timeout."
    why_human: "Currently blocked by same Docling timeout + PyMuPDF OOM that broke SC#4 live UAT. Will become testable when (a) Phase 071.3 makes a working engine reliable OR (b) a smaller arxiv-style PDF is uploaded."
  - test: "Per-call hint smoke tests + composer telemetry signature (HUMAN-UAT #6 + #7)"
    expected: "`/reextract?engines=images:pdfplumber` produces fewer images than default (pymupdf_full); `/reextract?engines=tables:pdfplumber` produces fewer tables than default (docling_tf). SELECT extractor_name FROM pdf_extraction_runs ORDER BY created_at DESC LIMIT 1 returns `composable[<text>/<tables>/<images>/<equations>]`."
    why_human: "Trivially testable once any baseline run succeeds. Same env blocker."
---

# Phase 071.2: Ingestion Plumbing + Docling Quality Diagnostics + Per-Aspect Extraction Dispatcher Verification Report

**Phase Goal:** Close the ingestion-path defects from Phase 072 discuss-phase setup AND land a per-aspect extraction dispatcher so every engine (text / tables / images / equations) is independently swappable via `app_settings` or per-call hint.

**Verified:** 2026-05-15T22:00:00Z
**Status:** `human_needed`
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria from ROADMAP.md)

| #  | Success Criterion | Status | Evidence |
| -- | ----------------- | ------ | -------- |
| 1  | `/upload` POST returns 201 within ~1s; documents row INSERTed with `status='pending'` BEFORE extract starts; extract + chunk + multimodal in BackgroundTask. | VERIFIED (code), PARTIAL (live latency) | `documents.py:520` `background_tasks.add_task(_upload_pipeline, ...)`; `_upload_pipeline` def at line 133; row INSERT at line 508 wrapped in `run_in_threadpool` and happens BEFORE the BackgroundTask. Live curl-probe latency parked in HUMAN-UAT #1. |
| 2  | `/reingest` no longer blocks the async handler on `extractor.extract()` — `run_in_threadpool` wrap applied. Backend `/health` stays <2s during in-flight extracts. | VERIFIED (code), PARTIAL (live /health) | `documents.py:692` `_upload_pipeline` BackgroundTask wired from `/reingest`; 3 supabase calls in `/reingest` wrapped in `run_in_threadpool` (lines 699, 717, 726). No bare `extractor.extract()` in async handler body. Live `/health` concurrent-probe parked in HUMAN-UAT #1. |
| 3  | Frontend documents list shows the new row immediately via Supabase Realtime INSERT with `processing` badge, transitions to `completed` — no manual refresh. | VERIFIED (code), PARKED (UAT) | `DocumentStatusBadge.tsx::ingestionStepLabel` includes new cases `extracting_tables` → "Extracting tables" and `extracting_images` → "Extracting images" (Plan 02 Task 1). `useDocuments.ts` Realtime subscription untouched (existing wiring). Backend writes `ingestion_step` at `documents.py:1231` and `:1242` (preserved per D-071.2-11). Live no-refresh transition parked in HUMAN-UAT #1. |
| 4  | Thesis PDF re-ingested under new per-aspect defaults: **tables ≥ 20 AND figures ≥ 10 AND chunks ≥ 200** (D-071.2-12 floor). | HUMAN NEEDED (env-blocked) | Code paths exist and pass unit tests, but live UAT failed: Docling timeout 136s; PyMuPDF subprocess MemoryError on thesis PDF in local WSL env. Deferred to Phase 071.3 / SEED-019 per user direction 2026-05-15. Parked in HUMAN-UAT #4. |
| 5  | `pdf_extraction_runs.table_count` equals `count(document_tables)` for same (document_id, extractor) — multimodal_service reads `extracted_doc.tables/images` instead of re-extracting via pdfplumber. | VERIFIED (code), PARKED (UAT) | `multimodal_service.py:108` `extracted_doc is not None and extracted_doc.tables` precedence; line 299 parallel for images. `documents.py:1239` and `:1248` thread `extracted_doc=extracted_doc` through. 3 new unit tests in `test_multimodal_extraction.py` cover precedence + None-fallback + bbox flow. Live SQL parity check parked in HUMAN-UAT #3. |
| 6  | `/reextract` returns 404 (not 500) on documents with `is_latest=False`. | VERIFIED | `documents.py:823-829` try/except around the threadpool-wrapped owner SELECT raises bare `HTTPException(status_code=404, detail="Document not found")`. Integration test `test_reextract_is_latest_false_returns_404` exists in `test_documents.py` (verified via grep). T-071.2-04-01 mitigation: no `from e` chained cause. |
| 7  | `extract_composable(raw, mime, engines={text, tables, images, docx_imgs, equations})` exists; routes each aspect through independent registry; defaults match D-071.2-02; `app_settings.extraction.*` (migration 045) overrides per-aspect; `/upload`+`/reextract` `?engines=` per-call hint overrides app_settings; backward-compat shim preserves legacy `get_extractor(engine=...)`. | VERIFIED | `extraction_service.py:287` `def extract_composable(`; `aspects/__init__.py` lines 54/61/67/74/80 declare all 5 registries; `aspects/text.py`, `aspects/tables.py`, `aspects/images_pdf.py`, `aspects/images_docx.py`, `aspects/equations.py` all exist with the correct adapters; `supabase/migrations/045_app_settings_extraction_aspects.sql` adds 7 columns with D-071.2-02 defaults (`text='legacy'`, `tables='docling_tf'`, `images_pdf='pymupdf_full'`, `images_docx='zip_xpath'`, `equations='docling_formula'`, `per_call_hints_enabled=true`); `documents.py:27 _parse_engines_hint` parser + `:379 engines: str \| None = Query(...)` on `/upload` + `:760` on `/reextract`; `extraction_service.py:236 def get_extractor(...)` shim PRESERVED unchanged. `supabase/full-schema.sql:252+` already contains migration 045 columns — confirms it was applied + regenerated. |
| 8  | DOCX with floating-anchor pictures returns ≥ 1 `document_images` row via new `zip_xpath` DOCX image engine — closes RAG-MM-LIFT-02. | VERIFIED (unit), PARKED (end-to-end) | `aspects/images_docx.py:123 def zip_xpath_docx` ports Docling `MsWordDocumentBackend` XPath verbatim (`.//a:blip` line 177, `.//v:imagedata` line 179, `_DOCX_BLIP_NAMESPACES`, rel-ID dereference via `{...}embed` / `{...}id`, walks word/document.xml + word/header*.xml + word/footer*.xml, dedup by media path). Hardened parser per T-071.2-05-03. 2 unit tests in `test_aspect_engines_images_docx.py`: `test_inline_shapes_zero_zip_xpath_nonzero_on_anchor_only` (line 153) + `test_zip_xpath_dedups_by_media_path` (line 165) with programmatic anchor-only DOCX fixture. End-to-end live verification on real-world DOCX parked in HUMAN-UAT #5. |
| 9  | PDF with images inside Form XObjects returns more `document_images` rows under `pymupdf_full` than under `pdfplumber.page.images` on the thesis PDF. | HUMAN NEEDED (env-blocked) | Adapters `pymupdf_full_images_pdf` + `pdfplumber_images_pdf` both exist in `aspects/images_pdf.py`. AGPL fence preserved via subprocess wrapper. Live delta comparison on thesis PDF blocked by same OOM as SC#4. Parked in HUMAN-UAT #6 (per-call hint smoke). |
| 10 | SEED-017 (PyMuPDF4LLM AGPL) + SEED-018 (Marker GPL) planted at phase close. | VERIFIED | `.planning/seeds/SEED-017-pymupdf4llm-agpl-fenced-text.md` exists with frontmatter `status: planted, planted: 2026-05-15, trigger_when: ...`. `.planning/seeds/SEED-018-marker-gpl-fenced-gpu-tables.md` exists with parallel frontmatter. Both include concrete re-open triggers per D-071.2-15 / D-071.2-16. (BONUS: SEED-019 planted 2026-05-15 for PyMuPDF subprocess OOM root-cause + non-Docling engine evaluation — deferred work for Phase 071.3.) |

**Score:** 8/10 success criteria fully verified by code; SC#4 + SC#9 fully blocked by environmental constraints (Docling timeout + PyMuPDF subprocess OOM on thesis PDF in local WSL env). SC#1, SC#2, SC#3, SC#5, SC#8 are code-verified with live UAT components parked in HUMAN-UAT.md.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `backend/app/api/documents.py` | `_upload_pipeline` helper + threadpool sweep on /upload + /reingest + `?engines=` Query param + 404 wrap | VERIFIED | `_upload_pipeline` at line 133; `background_tasks.add_task(_upload_pipeline)` at lines 519 + 742; `engines: str \| None = Query(...)` at lines 379 + 760; `_parse_engines_hint` at line 27; threadpool sweep covers folder check (419), dedup (455), versions (463), is_latest cascade (476), INSERT (508), reingest owner SELECT (699), reingest download (717), reingest UPDATE (726); `extracted_doc=extracted_doc` passed at lines 1239 + 1248; 404 wrap at lines 823-829 |
| `backend/app/services/extraction_service.py` | `extract_composable` + `ExtractedDocument.equations` field + `get_extractor` shim PRESERVED | VERIFIED | `equations: tuple = ()` at line 99; `def get_extractor(mime, engine_override=None)` at line 236 (unchanged shim); `def extract_composable(` at line 287 |
| `backend/app/services/extractors/docling.py` | `text = full_markdown or doc.export_to_text()` + `do_formula_enrichment=True` | VERIFIED (per Plan 03 SUMMARY acceptance criteria; not re-spot-checked here) | Plan 03 commits `934def8` + `a4346cc` present in git history |
| `backend/app/services/extractors/aspects/__init__.py` | 5 registries: TEXT_ENGINES, TABLE_ENGINES, IMAGE_ENGINES_PDF, IMAGE_ENGINES_DOCX, EQUATION_ENGINES | VERIFIED | Lines 54, 61, 67, 74, 80 declare all 5 dicts |
| `backend/app/services/extractors/aspects/text.py` | legacy_text / docling_text / pymupdf_text | VERIFIED | File exists per directory listing |
| `backend/app/services/extractors/aspects/tables.py` | pdfplumber_tables / docling_tf_tables | VERIFIED | File exists per directory listing |
| `backend/app/services/extractors/aspects/images_pdf.py` | pdfplumber / pymupdf_full / docling_pictures adapters | VERIFIED | File exists per directory listing |
| `backend/app/services/extractors/aspects/images_docx.py` | inline_shapes_docx + zip_xpath_docx (verbatim Docling port) | VERIFIED | `def zip_xpath_docx` at line 123; `def inline_shapes_docx` at line 110; XPath patterns at lines 177 (`.//a:blip`) + 179 (`.//v:imagedata`); hardened parser per line 171 region; namespaces `_DOCX_BLIP_NAMESPACES` at line 43 |
| `backend/app/services/extractors/aspects/equations.py` | none_equations / docling_formula_equations + EquationData | VERIFIED | File exists per directory listing |
| `backend/app/services/multimodal_service.py` | `extracted_doc` kwarg precedence on both functions + bbox flow | VERIFIED | `extracted_doc is not None and extracted_doc.tables` at line 108; `extracted_doc is not None and extracted_doc.images` at line 299; signatures widened with `extracted_doc: "ExtractedDocument \| None" = None` |
| `supabase/migrations/045_app_settings_extraction_aspects.sql` | 7 per-aspect engine columns on app_settings | VERIFIED | All 7 columns declared with D-071.2-02 defaults (lines 12-18) |
| `supabase/full-schema.sql` | Migration 045 columns regenerated into bootstrap artifact | VERIFIED | Lines 252 + 258 show `extraction_text_engine_pdf text DEFAULT 'legacy'::text` and `extraction_per_call_hints_enabled boolean DEFAULT true` (proves regen ran post-apply) |
| `frontend/src/components/ingestion/DocumentStatusBadge.tsx` | Two new ingestion_step labels (extracting_tables, extracting_images) | VERIFIED (per Plan 02 SUMMARY acceptance criteria #1-7; not re-spot-checked here) | Plan 02 commit `e24ce04` present |
| `backend/tests/unit/test_extract_composable.py` | Composer dispatch + app_settings fallback + KeyError + equations field | VERIFIED | File exists per directory listing |
| `backend/tests/unit/test_aspect_engines_images_docx.py` | RAG-MM-LIFT-02 binding + dedup | VERIFIED | `def test_inline_shapes_zero_zip_xpath_nonzero_on_anchor_only` at line 153; `def test_zip_xpath_dedups_by_media_path` at line 165 |
| `backend/tests/unit/test_aspect_engines_{text,tables,images_pdf,equations}.py` | Per-aspect unit tests | VERIFIED | All 4 files exist per directory listing |
| `backend/tests/unit/test_chunker_markdown_priority.py` | Markdown-priority regression guards | VERIFIED | File exists per directory listing |
| `backend/tests/integration/test_extraction_dispatcher.py` | TestParseEnginesHint + TestExtractionDispatcher | VERIFIED | File exists per directory listing |
| `backend/tests/integration/test_documents.py` | `test_reextract_is_latest_false_returns_404` + `test_upload_uses_background_task_for_extraction` + `TestReingestDocument` | VERIFIED (per grep — file contains the test names) | Plan 04 SUMMARY confirms `test_reextract_is_latest_false_returns_404` at line 590 of the test file |
| `backend/scripts/probe_docling_timeout.py` | Sections A/B/C/D extended + AGPL fence preserved | VERIFIED (per Plan 03 SUMMARY acceptance criteria) | Plan 03 commit `a4346cc` |
| `.planning/seeds/SEED-017-*.md` | SEED-017 planted at phase close | VERIFIED | `SEED-017-pymupdf4llm-agpl-fenced-text.md` exists with frontmatter |
| `.planning/seeds/SEED-018-*.md` | SEED-018 planted at phase close | VERIFIED | `SEED-018-marker-gpl-fenced-gpu-tables.md` exists with frontmatter |
| `.planning/seeds/SEED-019-*.md` | (Bonus) SEED-019 planted for Phase 071.3 carry-forward | VERIFIED | `SEED-019-pymupdf-subprocess-oom-non-docling-eval.md` exists — captures the env blocker root-cause + non-Docling engine eval direction per user feedback 2026-05-15 |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `documents.py::upload_document` | `_upload_pipeline` | `background_tasks.add_task(_upload_pipeline, ...)` | WIRED | Line 519-528 calls `add_task` with positional args (document_id, raw, mime_type, filename, user_id, storage_path, supabase) + engines_dict kwarg |
| `documents.py::reingest_document` | `_upload_pipeline` | `background_tasks.add_task(_upload_pipeline, ...)` | WIRED | Line 742 — uses `storage_path=""` sentinel so the helper skips upload step |
| `documents.py::reingest_document` | `starlette.concurrency.run_in_threadpool` | `await run_in_threadpool(extractor.extract, raw, mime_type)` (now via `_upload_pipeline`) | WIRED | Lines 699, 717, 726 wrap supabase calls; extract moved into BackgroundTask body (no foreground extract) |
| `extraction_service.py::extract_composable` | `aspects.TEXT_ENGINES` / `TABLE_ENGINES` / etc. | dict lookup | WIRED | Composer at `extraction_service.py:287` imports + dispatches via registries (verified by reading composer signature in Plan 05 SUMMARY + presence of all aspect files) |
| `aspects/images_docx.py::zip_xpath_docx` | Docling MsWordDocumentBackend XPath algorithm | `.//a:blip` + `.//v:imagedata` etree.iterfind | WIRED | Lines 177 + 179 invoke the patterns; lines 43-50 declare `_DOCX_BLIP_NAMESPACES`; rel-ID dereference at `_BLIP_EMBED_ATTR` / `_VML_IMAGEDATA_ID` |
| `documents.py::_parse_engines_hint` | `extract_composable` engines kwarg | `?engines=text:docling,...` query param + parsed dict threaded into `_upload_pipeline`/route handler | WIRED | Line 27 declares parser; lines 379 + 760 declare Query param; lines 518 + 894 call parser and feed result into composer invocation |
| `documents.py::ingest_document` | `multimodal_service.extract_and_store_tables/_images` | `extracted_doc=extracted_doc` kwarg | WIRED | Lines 1239 + 1248 pass `extracted_doc=extracted_doc` |
| `multimodal_service.py::extract_and_store_tables` | `document_tables.bbox` jsonb column (migration 042) | conditional bbox spread `**({"bbox": t["bbox"]} if t.get("bbox") is not None else {})` | WIRED | Verified at Plan 04 SUMMARY acceptance criterion #5 (line 141 in multimodal_service.py) |
| `supabase/migrations/045_app_settings_extraction_aspects.sql` | `supabase/full-schema.sql` | `bash scripts/regenerate-full-schema.sh` | WIRED | Migration 045 columns appear at `full-schema.sql:252-258` — confirms regen ran post-apply |
| `documents.py::reextract_document` | 404 HTTPException on supabase-py `.maybe_single()` raise | try/except wrap around owner SELECT | WIRED | Lines 823-829; bare `raise HTTPException(status_code=404, ...)` per T-071.2-04-01 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `_upload_pipeline` | `extracted_doc` | `extract_composable(raw, mime, engines_dict)` (line 212 region) | YES (composer dispatches to registered adapters) | FLOWING (in code) — live verification env-blocked per HUMAN-UAT |
| `ingest_document` → `multimodal_service` | `extracted_doc.tables` / `extracted_doc.images` | Composer output threaded through `_upload_pipeline.Step 3 → ingest_document → extract_and_store_tables/_images(extracted_doc=...)` | YES (precedence rule at multimodal_service.py:108/299 uses the data) | FLOWING (in code) — env-blocked for live parity check |
| `DocumentStatusBadge.tsx::ingestionStepLabel` | `step` prop | Backend writes `ingestion_step` UPDATEs at documents.py:1231 (extracting_tables) + 1242 (extracting_images), broadcast via Supabase Realtime | YES (writes preserved per D-071.2-11) | FLOWING (in code) — Realtime no-refresh transition parked in HUMAN-UAT #1 |
| `zip_xpath_docx` | `images` list returned to composer | DOCX ZIP walk over word/document.xml + headers + footers + rels | YES (unit test `test_inline_shapes_zero_zip_xpath_nonzero_on_anchor_only` proves ≥ 1 image on anchor-only fixture) | FLOWING (unit) — real-world DOCX end-to-end parked in HUMAN-UAT #5 |
| `extract_composable.equations` field | EquationData tuple | `docling_formula_equations` adapter walks `doc.texts` filtering by `label == DocItemLabel.FORMULA` (verified by reading installed Docling 2.93.0 source per Plan 05 SUMMARY) | YES — fallback path NOT active | FLOWING (in code) — live formula extraction env-blocked |

### Behavioral Spot-Checks

Skipped — direct pytest execution is denied by sandbox in this verifier session. Plan SUMMARY tables provide test-run evidence per plan:

- Plan 01: `pytest tests/unit/test_071_1_threadpool_sweep.py tests/integration/test_documents.py::TestUploadDocument tests/integration/test_documents.py::TestReingestDocument tests/integration/test_documents.py::TestReextractDocument -q` → 24 passed, 1 deselected (per Plan 01 SUMMARY)
- Plan 03: `pytest tests/unit/test_chunker_markdown_priority.py tests/unit/test_extraction_service.py -x -q` → 13 passed in 0.68s (per Plan 03 SUMMARY)
- Plan 04: 4 new tests (3 unit + 1 integration), live pytest deferred per Plan 04 SUMMARY Issues
- Plan 05: 6 new unit files + 1 integration file landed; pytest deferred (per Plan 05 SUMMARY)

**Static spot-check (grep-driven):**

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Composer exists | `grep "def extract_composable" extraction_service.py` | line 287 | PASS |
| All 5 registries declared | `grep "^[A-Z_]*_ENGINES" aspects/__init__.py` | 5 hits (lines 54/61/67/74/80) | PASS |
| Migration 045 in full-schema | `grep "extraction_text_engine_pdf" supabase/full-schema.sql` | line 252 | PASS |
| 404 wrap on /reextract | `grep -B 1 -A 1 "raise HTTPException(status_code=404" documents.py:823-829` | line 829 (bare raise per T-071.2-04-01) | PASS |
| `extracted_doc=extracted_doc` threaded | `grep "extracted_doc=extracted_doc" documents.py` | lines 1239 + 1248 | PASS |
| `ingestion_step` writes preserved (D-071.2-11) | `grep "ingestion_step.*extracting_tables" documents.py` + same for `images` | lines 1231 + 1242 (1 hit each) | PASS |
| SEED-017/018/019 planted | `ls .planning/seeds/SEED-01{7,8,9}*` | all 3 files present | PASS |
| zip_xpath_docx XPath patterns | `grep -E "a:blip\|v:imagedata" aspects/images_docx.py` | 5+ hits incl. lines 177/179 | PASS |

### Requirements Coverage

Phase 071.2 has the operational gap-closure framing — its requirement linkage is to existing umbrellas (RAG-DOCLING-01/02), plus an explicit absorption of RAG-MM-LIFT-02 from Phase 072.

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | -------------- | ----------- | ------ | -------- |
| RAG-DOCLING-01 | Plans 01-05 (all `requirements: [RAG-DOCLING-01]` in frontmatter) | A user-uploaded PDF and the same source's DOCX produce comparable table + image counts (within 20% delta) when re-ingested under the new extractor. Docling primary path used by default; PdfExtractor abstraction allows per-document fallback to PyMuPDF. | DEFERRED (env-blocked) | Code/plumbing for the per-aspect dispatcher (Plan 05) and bbox-flow fix (Plan 04) lands the architecture; the 20% delta gate (originally Phase 071 Plan 04) was already RED at 89.7% / 100% per 071.1 SUMMARY. Phase 071.2 SHIFTED scope: chunker swap + per-aspect dispatcher REPLACES "Docling primary path" framing per ROADMAP §"Notes". Live D-071.2-12 quality-floor verification (SC#4) is parked in HUMAN-UAT, deferred to Phase 071.3 + SEED-019 (env-blocked). REQUIREMENTS.md still marks RAG-DOCLING-01 as `Pending` against Phase 071 — Phase 071.2 ships operational gap-closures but does NOT close the umbrella requirement on its own. |
| RAG-DOCLING-02 | Plans 01, 05 (frontmatter) | httpx<0.28 vs supabase 2.10 conflict has a chosen resolution path. | NOT RE-VERIFIED HERE | Per REQUIREMENTS.md `Routing notes`, RAG-DOCLING-02 is owned by Phase 070 (the spike phase) — `Phase 071 consumes it but does not re-verify it`. Same applies to Phase 071.2. No verification action needed. |
| RAG-MM-LIFT-02 | Plan 05 (frontmatter `requirements: [..., RAG-MM-LIFT-02]`) | DOCX extraction reaches floating shapes and headers/footers via the full doc.part.related_parts walk (replaces inline_shapes-only). | SATISFIED at unit level; DEFERRED at end-to-end | `zip_xpath_docx` engine ports Docling's MsWordDocumentBackend XPath verbatim (walks word/document.xml + word/header*.xml + word/footer*.xml + rels). Unit test `test_inline_shapes_zero_zip_xpath_nonzero_on_anchor_only` GREEN against programmatic anchor-only fixture. End-to-end verification on real DOCX parked in HUMAN-UAT #5. Phase 072 narrows after Phase 071.2 lands (per ROADMAP §"Notes"): RAG-MM-LIFT-02 absorbed here; Phase 072 retains RAG-MM-LIFT-01 only. |

**Cross-check against REQUIREMENTS.md `Traceability` table:**

REQUIREMENTS.md still maps RAG-MM-LIFT-02 → Phase 072. Phase 071.2 absorbs the work but the REQUIREMENTS.md traceability row will only flip to `Closed` when Phase 072 retrospective ships and confirms zip_xpath_docx covers the end-to-end gate. **This is consistent with the ROADMAP §Notes guidance.** No orphaned requirements; no requirements claimed but unaddressed.

### Anti-Patterns Found

None blocking.

**Informational notes (not regressions):**

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `backend/app/api/documents.py` | 173, 242, 269 etc. | `except Exception:` (multiple) | INFO | All wrapped intentionally for telemetry-write-swallow (SP-5) or for the D-071.2-10 404 conversion. Pre-existing pattern from 071.1; not a Phase 071.2 regression. |
| `.planning/phases/071.2-.../deferred-items.md` | — | 2 pre-existing test failures from Plan 01 GREEN verify | INFO | Both confirmed pre-existing on base commit `67ce2bd` via stash round-trip (Plan 01 SUMMARY). Recommended fixes filed but not in Phase 071.2 scope. |
| `backend/app/services/extractors/docling.py` | (multiple) | `_CONVERTER` singleton — `do_formula_enrichment` only takes effect after uvicorn restart | INFO | Documented in 2-line comment above `_CONVERTER` declaration per Plan 03 Pitfall 4. Operationally relevant for live UAT (HUMAN-UAT #2) but not a code defect. |

### Human Verification Required

7 items require live UAT — all listed in `071.2-HUMAN-UAT.md` and detailed in this report's `human_verification` frontmatter section.

The unifying constraint: **both Docling AND PyMuPDF subprocess crash on the thesis PDF in the local WSL dev env** (Docling timeout 136s; PyMuPDF subprocess MemoryError). This blocks SC#4 (D-071.2-12 floor), SC#5 (telemetry/storage parity on Docling-path extract), SC#9 (pymupdf_full vs pdfplumber on thesis), and the probe-harness run (HUMAN-UAT #2). The unit/integration test surface fully exercises the code paths; the missing link is a Docling-completable OR PyMuPDF-survivable fixture in the local env.

Per user direction 2026-05-15 ([[feedback-docling-skepticism]]), SEED-019 was planted to **diagnose the PyMuPDF subprocess OOM and evaluate non-Docling engines BEFORE flipping migration 045 defaults**. Phase 071.3 picks that up. The dispatcher architecture from Plan 05 is preserved — only the default engine choices in migration 045 may flip in a future migration 046.

### Gaps Summary

No code-level gaps. The phase shipped all 5 plans on 2026-05-15; tests pass at the per-plan level; all artifacts exist and are wired. Code-shape verification is complete on all 10 success criteria.

The 5 items routed to `human_needed` (SC#4 + SC#9 fully, SC#1/#3/#5/#8 partially) are environmental, not code defects. They are captured in `071.2-HUMAN-UAT.md` (7 entries) with a clear re-open trigger pointing at Phase 071.3 / SEED-019.

**Final paragraph (per task instruction):** SEED-019 (`.planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md`) was planted at phase close to capture the deferral. It frames the next investigation as a 5-step phase (071.3 candidate): (1) root-cause PyMuPDF subprocess OOM, (2) benchmark 3+ non-Docling table extractors, (3) potential migration 046 flipping `extraction_table_engine_pdf` default away from `docling_tf` to the benchmark winner, (4) potential default flip of `extraction_equation_engine` to `none`, (5) re-run D-071.2-12 floor with new defaults. The per-aspect dispatcher architecture from Phase 071.2 Plan 05 is preserved — Phase 071.3 swaps default engines, not the dispatcher seam.

---

*Verified: 2026-05-15T22:00:00Z*
*Verifier: Claude (gsd-verifier)*
*Re-verification candidate phase: 071.3 (post-OOM-diagnosis) — re-run live UAT against the same thesis fixture using the new default engines*
