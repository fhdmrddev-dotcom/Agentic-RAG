---
phase: 217-the-library-one-home-for-documents
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/IngestionPage.tsx
  - frontend/src/pages/LibraryPage.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/lib/nav-items.ts
  - frontend/src/__tests__/components/IngestionPage.test.tsx
  - frontend/src/pages/__tests__/LibraryPage.test.tsx
  - frontend/src/__tests__/library/renameFence.test.ts
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/sketches/218-the-library-and-its-tabs/drive.cjs
  - .planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md
autonomous: true
requirements: [LIB-01]
decisions: [D-217-01, D-217-02, D-217-03, D-217-11]

must_haves:
  truths:
    - "The surface is called Library in the nav and on the page"
    - "The page file is LibraryPage.tsx and the rename lands in the same commit as the label"
    - "ActiveView still contains \"documents\" — the key is never printed to a user"
    - "ChatLayout's trailing else is still <KnowledgeHealthPage /> — the app still has a default view"
    - "The sketch's live-tree assertions still pass against the moved tree"
  artifacts:
    - path: "frontend/src/pages/LibraryPage.tsx"
      provides: "the renamed page, exporting LibraryPage, with the h1 Library"
      contains: "export function LibraryPage"
    - path: "frontend/src/__tests__/library/renameFence.test.ts"
      provides: "?raw fences on ActiveView and ChatLayout's trailing else"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "the IngestionPage section moved to LibraryPage, content preserved"
      contains: "frontend/src/pages/LibraryPage.tsx"
  key_links:
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "frontend/src/pages/LibraryPage.tsx"
      via: "the single import at :8 and the activeView === \"documents\" branch at :757-758"
      pattern: "LibraryPage"
    - from: "CLAUDE.md hot-file table"
      to: "docs/HOT-FILE-LEDGER.md anchor"
      via: "the same-commit sync rule — a row without a section is drift"
      pattern: "frontendsrcpageslibrarypagetsx"
---

<objective>
SC#1: the document space stops being a stale filename. `Documents` becomes `Library` in the nav and
on the page, `IngestionPage.tsx` becomes `LibraryPage.tsx`, and the two registers that carry the old
path move with it in the same commit.

Purpose: the ledger's own finding on this file is that *"it is already named `Documents` everywhere
the user can see — only the FILE NAME is stale"*. This plan makes the file honest and the label
correct, and it does the paperwork the same-commit sync rule requires instead of leaving drift.

Output: a renamed page, one import site updated, one nav label, the ledger section and its CLAUDE.md
row moved, a `?raw` fence protecting the two things that must NOT change, and a regenerated sketch
contract.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-the-library-one-home-for-documents/217-CONTEXT.md
@.planning/phases/217-the-library-one-home-for-documents/217-PATTERNS.md
@.planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md

<interfaces>
The BUILD-CONTRACT pins the new words — do not invent copy:

| | value |
|---|---|
| page title | `Library` |
| page subtitle | `What the agent can read, and how well it reads it.` |
| shipped title being replaced | `Documents` |
| shipped subtitle being replaced | `Upload documents to give the AI context for your conversations.` |

⛔ Must NOT change, both fenced by this plan's own test:
- `frontend/src/App.tsx:102` — `ActiveView` keeps `"documents"`; the comment at `:96-101` explains
  that a union member with no branch silently renders Knowledge Health.
- `frontend/src/components/layout/ChatLayout.tsx:879` — `) : ( <KnowledgeHealthPage /> )`, the
  app's positional fallback. **Phase 218 owns replacing it.** Sketch fence `A8` reads this exact
  shape from the live tree.

⛔ Must NOT be renamed: `SIDEBAR_PIN_KEY = "documents.sidebar.pinnedExpanded"` (`IngestionPage.tsx:47`,
read/written `:125`, `:129`). It is stored state; renaming silently resets every user's pin.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv the page, rename its export and its words, update the one import site and the nav label</name>
  <files>frontend/src/pages/IngestionPage.tsx, frontend/src/pages/LibraryPage.tsx, frontend/src/components/layout/ChatLayout.tsx, frontend/src/lib/nav-items.ts, frontend/src/__tests__/components/IngestionPage.test.tsx, frontend/src/pages/__tests__/LibraryPage.test.tsx</files>
  <read_first>
    - `frontend/src/pages/IngestionPage.tsx:1-50` (the import block and `SIDEBAR_PIN_KEY`), `:75` (the export), `:338-341` (the h1 and subtitle), `:440-460` (the column-shedding rule that must survive untouched)
    - `frontend/src/components/layout/ChatLayout.tsx:1-12`, `:750-765`, and `:872-885`
    - `frontend/src/lib/nav-items.ts:30-55`
    - `frontend/src/__tests__/components/IngestionPage.test.tsx` (all 158 lines — its `vi.hoisted` + `vi.mock` block at `:14-35` and the `TooltipProvider` wrapper at `:11`)
    - `docs/HOT-FILE-LEDGER.md` § `frontend/src/pages/IngestionPage.tsx` (line ~6458) — D-217-01 requires this before touching the file
    - `.planning/phases/217-the-library-one-home-for-documents/217-RESEARCH.md` § *SC#1 — the rename's true blast radius* (the enumerated 15 sites; do NOT re-derive them)
  </read_first>
  <action>
    `git mv frontend/src/pages/IngestionPage.tsx frontend/src/pages/LibraryPage.tsx`, then:
    rename the export at `:75` `IngestionPage` -> `LibraryPage`; change the `<h1>` at `:338` from
    `Documents` to `Library`; replace the subtitle at `:339-341` with
    `What the agent can read, and how well it reads it.`

    `frontend/src/components/layout/ChatLayout.tsx`: update the single import at `:8` to
    `import { LibraryPage } from "@/pages/LibraryPage"` and the element at `:758` to
    `<LibraryPage onNavigate={onNavigate} />`. ⛔ Touch `:757-758` ONLY. Line `:879`'s
    `<KnowledgeHealthPage />` trailing else is the app's default view and Phase 218 owns replacing
    it — changing it here removes the default view and breaks sketch fence `A8`.

    `frontend/src/lib/nav-items.ts:37`: `label: "Documents"` -> `label: "Library"`. ⛔ `view: "documents"`
    is UNCHANGED — it is a key, never printed. The `library-health` (`:42`) and `governance` (`:43`)
    entries stay; Phase 218 retires them.

    `git mv frontend/src/__tests__/components/IngestionPage.test.tsx frontend/src/pages/__tests__/LibraryPage.test.tsx`
    (the destination directory already exists with 6 suites) and update its import, its `describe`
    name and the seven assertion sites RESEARCH enumerates (`:2`, `:106`, `:124-125`, `:130-131`,
    `:135`, `:144-145`, `:151-152`). Add two cases: the rendered h1 reads `Library`, and the
    rendered subtitle is the contract's sentence.

    ⛔ `SIDEBAR_PIN_KEY = "documents.sidebar.pinnedExpanded"` is NOT renamed. Add a one-line comment
    above it saying it is stored state and a rename resets every user's pin.

    Comment-only references (`App.tsx:288,290`, `ClassificationRulesPage.tsx:29`,
    `FilterBar.test.tsx:127`, `DocumentDetailPanel.tsx:307`, `RelationshipsSection.tsx:9`,
    `citationNav.tsx:11,56,129`, `GovernancePage.tsx:95`, `types/index.ts:328`) are OUT of this
    plan's `files_modified` — later plans own several of those files and a same-wave edit would
    collide. Record them in the summary as a known, harmless residue for a later sweep.

    ⚠ **G-5 / D-217-01 statement, required in the summary:** `IngestionPage.tsx` measures
    `26 / 9 / 601` and FIRES. Its named seam is *"the sidebar, the filter bar and the grid are three
    independent concerns in one component — with a tab shell arriving, extract the shell first and
    let each tab own its body, otherwise the tab bar becomes the tenth conditional branch."*
    **This plan does not take that seam — plan 09 does, and it is the plan that adds the shell.**
    This plan is a pure rename: zero behaviour change, zero new branch. Also state the gate side:
    the file is in NEITHER count-gate knob today, and its suite has never been executed by the gate;
    plan 12 adopts it.
  </action>
  <acceptance_criteria>
    - `git log --follow --oneline -1 -- frontend/src/pages/LibraryPage.tsx` shows the file's history survived (a `git mv`, not a delete+add)
    - `grep -rn "IngestionPage" frontend/src --include=*.tsx --include=*.ts | grep -v "^.*://" ` returns only comment lines — no import, no JSX element, no export
    - `grep -n "export function LibraryPage" frontend/src/pages/LibraryPage.tsx` returns one line
    - `grep -n ">Library</h1>" frontend/src/pages/LibraryPage.tsx` returns one line and `grep -n ">Documents</h1>"` returns none
    - `grep -n 'label: "Library"' frontend/src/lib/nav-items.ts` returns one line; `grep -n 'view: "documents"' frontend/src/lib/nav-items.ts` still returns one line
    - `grep -n "documents.sidebar.pinnedExpanded" frontend/src/pages/LibraryPage.tsx` returns the unchanged literal
    - `grep -n "<KnowledgeHealthPage />" frontend/src/components/layout/ChatLayout.tsx` is unchanged (`git diff` shows no hunk near line 879)
    - `npx vitest run src/pages/__tests__/LibraryPage.test.tsx --maxWorkers=2` passes with at least the original 158-line suite's case count plus 2
    - `npx tsc --noEmit -p tsconfig.app.json` reports no NEW errors versus the recorded baseline (re-derive the baseline first; `tsc --noEmit` alone checks ZERO files)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/LibraryPage.test.tsx --maxWorkers=2</automated>
  </verify>
  <done>The page, its export, its words, its one importer, its suite and the nav label all say Library; `ActiveView`, the fallback and the pin key are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Move the ledger section and its CLAUDE.md row in the SAME commit</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - `docs/HOT-FILE-LEDGER.md` § `frontend/src/pages/IngestionPage.tsx` in full (its named seam, its binding invariants, the ⭐ "already named Documents everywhere the user can see" finding, and the same-commit obligation it states about its own rename)
    - `CLAUDE.md` § *Workflow guardrails* — the hot-file ledger table, the row whose link target is `#frontendsrcpageslibrarypagetsx`'s predecessor `#frontendsrcpagesingestionpagetsx`, and the SAME-COMMIT SYNC RULE paragraph directly above the table
    - `CLAUDE.md` § *CLAUDE.md context budget* — the 200-char disposition cap and the gate that enforces it
  </read_first>
  <action>
    In `docs/HOT-FILE-LEDGER.md`: change the section heading to
    `### \`frontend/src/pages/LibraryPage.tsx\`` and PRESERVE the section's content verbatim — the
    named seam, the invariants (the `minmax(0,1fr) 430px` push/split grid, the 288px pinnable
    Folders+Views rail, the positional column-shedding rule) and the ⭐ finding. Append one dated
    line recording that the file was renamed at Phase 217 and that its git history follows the move,
    so a future re-derive with `git log --oneline -- <file>` is not read as a reset triple.

    In `CLAUDE.md`: update the corresponding table row's path text AND its link anchor from
    `#frontendsrcpagesingestionpagetsx` to `#frontendsrcpageslibrarypagetsx`. Re-derive the triple
    with the recipe in that section (`git log --oneline -- <file> | wc -l`, the phase-bucket sed
    pipeline, `wc -l`) — do not copy the existing `26 / 9 / 601`. ⛔ Keep the disposition cell under
    **200 characters**; the reasons belong in the ledger section, not in the cell.

    A row without a section, or a section without a row, is drift — both edits land in the same
    commit as the rename.
  </action>
  <acceptance_criteria>
    - `grep -n "frontend/src/pages/LibraryPage.tsx" docs/HOT-FILE-LEDGER.md` returns the section heading
    - `grep -n "frontendsrcpagesingestionpagetsx" CLAUDE.md docs/HOT-FILE-LEDGER.md` returns nothing
    - `grep -n "frontendsrcpageslibrarypagetsx" CLAUDE.md` returns the row's link target
    - `node scripts/check-claude-md-size.cjs` exits 0 (no `[disposition-too-long]`, no `[duplicate-row]`, no `[malformed-row]`)
    - The ledger section still contains the strings `430px` and `nth-child` — the invariants survived the move
    - `git show --stat HEAD` for this commit includes `frontend/src/pages/LibraryPage.tsx`, `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md` together
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <done>The ledger row, its section and its anchor all point at the new path, with content preserved and the cell under the cap.</done>
</task>

<task type="auto">
  <name>Task 3: The rename fence, and the sketch's live-tree assertions re-pointed and re-emitted</name>
  <files>frontend/src/__tests__/library/renameFence.test.ts, .planning/sketches/218-the-library-and-its-tabs/drive.cjs, .planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md</files>
  <read_first>
    - `.planning/sketches/218-the-library-and-its-tabs/drive.cjs:48-51` (`src()`), `:73-99` (the positive controls and the `src()` constants), `:111-112` (`A2`), `:190-201` (`A7`..`A7e`), `:214-220` (`A9`/`A9c` — the RENAME-MAP precedent), `:805-900` (the `--emit` path)
    - `frontend/src/components/workflows/argumentModel.test.ts:24-80` — the `?raw` fence idiom, its non-vacuity control and its CRLF rule
    - `frontend/src/components/files/FileRow.sweep.test.ts:32-41` and `:100-105` — the three-way fence guard and the LINE-ANCHORED comment stripper (the unanchored variant eats live code and turns every `not.toContain` into a pass)
  </read_first>
  <action>
    Create `frontend/src/__tests__/library/renameFence.test.ts` with `?raw` imports of
    `frontend/src/App.tsx` and `frontend/src/components/layout/ChatLayout.tsx`. Assert:
    (a) `ActiveView` still carries `"documents"`;
    (b) `ChatLayout`'s trailing else is still `) : ( <KnowledgeHealthPage /> )` — the app's default
    view, Phase 218's to replace;
    (c) the nav label reads `Library` while `view: "documents"` is unchanged (a third `?raw` over
    `nav-items.ts`);
    (d) `SIDEBAR_PIN_KEY`'s literal is still `"documents.sidebar.pinnedExpanded"`.
    Guard the fence three ways per the `FileRow.sweep.test.ts` precedent: a LENGTH assertion per
    `?raw` import, an IDENTITY symbol only that file contains, and a stripper non-vacuity pair.
    Every pattern tolerates CRLF (`\r?\n`, `[\s\S]`) — source files check out with Windows line
    endings on this box and a terminator spelled `\n\n` silently never matches.

    In `drive.cjs`, four edits (D-217-11 — and these are named so they are not discovered at
    execution time):
    1. `:79` control `src("frontend/src/pages/IngestionPage.tsx")` -> `LibraryPage.tsx`;
    2. `:85` `const PAGE = src(...)` -> the new path. ⚠ If this is missed, `PAGE` becomes `""` and
       every `PAGE.*` assertion silently passes-or-fails on an empty string;
    3. `:111` `A2` — keep the assertion, update its NAME to say `LibraryPage`;
    4. `:197-201` `A7b` / `A7c` — these are SHIPPED-STATE fences that now read `Library`. Rewrite
       them as a RENAME MAP on the `A9c` precedent (*a verbatim check cannot tell a RENAME from a
       LOSS*): assert the old word is gone AND the new word is present AND `COPY.SHIPPED_PAGE_TITLE`
       still records what it replaced. ⛔ Do not delete them — a deleted fence is a lost guarantee.
    ⛔ Do NOT touch `A3b` (plan 06 owns it), and do NOT touch `A5*` / `COPY.STAGES` / `index.html`
    (plan 08 owns the stage order).

    Then run `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs --emit` from the repo
    root and commit the regenerated `BUILD-CONTRACT.generated.md`. ⚠ It is GENERATED — never
    hand-edited. ⚠ It is **already stale at HEAD before this phase changes anything**: it claims 190
    assertions where `node drive.cjs` measures **193** (re-confirmed 2026-08-29). Re-derive; quote
    the measured number in the summary, never the file's own header.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/library/renameFence.test.ts --maxWorkers=2` passes with at least 8 assertions
    - The fence file contains a length assertion per `?raw` import and at least one identity assertion; deleting the `ActiveView` arm makes the suite RED (state that this was observed)
    - `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs` prints `0 failed` and an assertion count that is re-derived, not copied
    - `grep -n "IngestionPage" .planning/sketches/218-the-library-and-its-tabs/drive.cjs` returns nothing
    - `git diff .planning/sketches/218-the-library-and-its-tabs/drive.cjs` shows no hunk touching `A3b`, `A5b`, `A5e` or `COPY.STAGES`
    - `BUILD-CONTRACT.generated.md` is regenerated in this same commit and its header assertion count matches the drive's printed count
  </acceptance_criteria>
  <verify>
    <automated>node .planning/sketches/218-the-library-and-its-tabs/drive.cjs && cd frontend && npx vitest run src/__tests__/library/renameFence.test.ts --maxWorkers=2</automated>
  </verify>
  <done>The two things that must not change are fenced from source; the sketch reads the moved tree and its contract is regenerated, not transcribed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan adds no route, no input parsing and no data path. Its risk is availability of a shipped surface, not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-15 | Denial of service (availability) | `ChatLayout.tsx:879` | mitigate | The trailing `else` renders `KnowledgeHealthPage` and is the app's DEFAULT VIEW; disturbing it turns an unmatched `ActiveView` into a blank screen. Fenced from source by task 3, not by care |
| T-217-16 | Tampering | `SIDEBAR_PIN_KEY` | mitigate | It is a persisted localStorage key containing the word `documents`; renaming it silently resets every user's pinned sidebar. Fenced from source by task 3 |
| T-217-17 | Repudiation | ledger drift | mitigate | A hot file missing from the CLAUDE.md scan list is permanently invisible to its own guardrail. The row, the section and the anchor move in the same commit; `check-claude-md-size.cjs` is the mechanical check |
| T-217-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed by this plan |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/__tests__/LibraryPage.test.tsx src/__tests__/library/renameFence.test.ts --maxWorkers=2` -> passes
- `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs` -> `0 failed`
- `node scripts/check-claude-md-size.cjs` -> exit 0
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` -> no new errors versus the re-derived baseline
</verification>

<success_criteria>
- The nav and the page say `Library`; the file is `LibraryPage.tsx` with its git history intact
- `ActiveView`, `ChatLayout.tsx:879` and `SIDEBAR_PIN_KEY` are provably unchanged
- The ledger row, its section and its anchor moved in the same commit
- The sketch contract is regenerated from a green drive
</success_criteria>

<output>
Create `.planning/phases/217-the-library-one-home-for-documents/217-04-SUMMARY.md` when done.
Record: the re-derived `LibraryPage.tsx` ledger triple, the drive's measured assertion count, and
the residue list of comment-only `IngestionPage` references left for a later sweep.
</output>
