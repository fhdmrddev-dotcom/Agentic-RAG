---
phase: 217-the-library-one-home-for-documents
plan: 10
type: execute
wave: 4
depends_on: ["217-01", "217-02", "217-03", "217-08"]
files_modified:
  - frontend/src/lib/api/documents.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/__tests__/apiBarrel.test.ts
  - frontend/src/components/metadata/DocumentContentSection.tsx
  - frontend/src/components/metadata/DocumentChunksSection.tsx
  - frontend/src/components/metadata/DocumentDetailPanel.tsx
  - frontend/src/components/metadata/__tests__/DetailSections.lazy.test.tsx
autonomous: true
requirements: [LIB-04]
decisions: [D-217-01, D-217-04, D-217-05, D-217-08]

must_haves:
  truths:
    - "A person can read the parsed text of their own document, paged by line range, with no line numbers glued to it"
    - "A person can read the chunks the agent actually searches, with the embedding model each was written with"
    - "Opening a document fires exactly ONE section request — the shipped Relationships section, which fetched on mount before this phase and still does. Each of the FIVE NEW sections fetches on EXPAND, adding zero. ⚠ D-217-25c: the count is ONE, never zero — DocumentDetailPanel.tsx:265 omits defaultOpen and RelationshipsSection.tsx:96 fetches in a bare useEffect. Changing that is a props edit this plan's own fence forbids."
    - "Every new client function is reachable from the @/lib/api barrel, and that is now guarded by a test"
  artifacts:
    - path: "frontend/src/lib/api/documents.ts"
      provides: "the five client functions for the five new routes"
      contains: "getDocumentContent"
    - path: "frontend/src/lib/api.ts"
      provides: "the barrel re-export of all five"
    - path: "frontend/src/lib/__tests__/apiBarrel.test.ts"
      provides: "barrel completeness extended from connectors.ts to documents.ts"
    - path: "frontend/src/components/metadata/DocumentDetailPanel.tsx"
      provides: "two new lazily-mounted PanelSections"
      contains: "defaultOpen={false}"
  key_links:
    - from: "frontend/src/components/metadata/DocumentContentSection.tsx"
      to: "GET /documents/{id}/content"
      via: "the barrel import @/lib/api, fetched on expand"
      pattern: "getDocumentContent"
    - from: "frontend/src/components/metadata/DocumentDetailPanel.tsx"
      to: "PanelSection defaultOpen={false}"
      via: "PanelSection.tsx:94 mounts children only when open — the lazy mechanism is free"
      pattern: "defaultOpen=\\{false\\}"
---

<objective>
LIB-04, first half: the parsed text and the chunks reach the screen, and the five client functions
that plan 11 also needs land in one place with the barrel guarded.

Purpose: `document_chunks.content` has been stored since migration `002:24` and `full_markdown`
since the pipeline's terminal write — neither has ever been reachable by the browser. The ledger's
own note on this panel already names what belongs here: *"a Retrieval section, a read-only Chunks
list, tables rendered as tables, image descriptions — none of which needs schema."*

⚠ And the way this goes wrong is measured: `PanelSection` defaults `defaultOpen = true` and the three
shipped mounts rely on that default, so copying the shipped Relationships mount verbatim would fire
five requests — one of them potentially megabytes — the instant a document is opened.

Output: five client functions, a barrel re-export that is now tested, two lazily-mounted sections,
and a suite that asserts ZERO fetches before the accordion is clicked.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-the-library-one-home-for-documents/217-CONTEXT.md
@.planning/phases/217-the-library-one-home-for-documents/217-PATTERNS.md
@.planning/phases/217-the-library-one-home-for-documents/217-VALIDATION.md

<interfaces>
Landed by plans 01/02/03 — the five routes:

```
GET /documents/{id}/content?start_line=&end_line=  -> DocumentContentResponse
GET /documents/{id}/chunks                          -> DocumentChunkRow[]
GET /documents/{id}/tables                          -> DocumentTableRow[]
GET /documents/{id}/images                          -> DocumentImageRow[]
GET /documents/{id}/queries                         -> DocumentQueryRow[]
```

Landed by plan 08 — the five matching TypeScript types in `frontend/src/types/index.ts`.

Shipped — `frontend/src/lib/api/documents.ts:13-20`, the exact client shape to copy:

```ts
import type { Document, Folder, WorkspaceFile } from "../../types"
import { API_BASE, getAuthHeaders, getAuthToken } from "./_core"

export async function listDocuments(): Promise<Document[]> {
  const headers = await getAuthHeaders()
  const res = await fetch(`${API_BASE}/documents`, { headers })
  if (!res.ok) throw new Error("Failed to list documents")
  return res.json() as Promise<Document[]>
}
```

Shipped — the barrel block already exists at `frontend/src/lib/api.ts:74-90`
(`export { listDocuments, uploadDocument, … } from "./api/documents"`). ⚠ Every consumer imports
from `@/lib/api`, and `196-08` measured **249 red tests** from one mis-handled export. ⚠ The barrel
completeness test covers **`connectors.ts` only** today, so a missing `documents.ts` re-export is
caught by NOTHING.

Shipped — `PanelSection.tsx`: props at `:23-30` = `{title, count?, warn?, defaultOpen?, children}`
(**no `onOpenChange`, no `onToggle`**), `defaultOpen = true` at `:46`, and ⭐ the free lazy-mount at
`:94` (`217-PATTERNS.md` CORRECTS RESEARCH's `:93`):

```tsx
      {open && (
        <div id={bodyId} role="region" aria-labelledby={headId} className="pb-3">
          {children}
        </div>
      )}
```

Shipped — `RelationshipsSection.tsx`, the section-owns-its-fetch pattern: `type LoadState =
"loading" | "ready" | "error"` (`:57`), the keyed `load` in a `useCallback` on `[docId,
onTotalChange]` (`:74-99`), the `onTotalChange` lift that populates the `PanelSection` count badge
(`:48-55`), and FOUR honest arms at `:151` / `:160` / `:175` — `loading` (`role="status"
aria-live="polite"`), `ready & rows>0`, `ready & rows===0` (a WORDED empty arm), `error`
(`role="alert"` + a *Try again* button). Every meaningful string uses
`text-panel-muted-foreground[-dim]`, never the global muted (its docblock `:28-29` records the
3.59:1 measurement).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five client functions, the barrel re-export, and the barrel test extended to cover them</name>
  <files>frontend/src/lib/api/documents.ts, frontend/src/lib/api.ts, frontend/src/lib/__tests__/apiBarrel.test.ts</files>
  <read_first>
    - `frontend/src/lib/api/documents.ts:1-60` (the imports at `:13-14` and `listDocuments` at `:15-20`)
    - `frontend/src/lib/api.ts:70-95` (the documents re-export block)
    - `frontend/src/lib/__tests__/apiBarrel.test.ts` (all 34 lines — the loop, and its `expect(connectorExports.length).toBeGreaterThan(0)` non-vacuity control)
    - `docs/HOT-FILE-LEDGER.md` § `frontend/src/lib/api.ts` — ⚠ its row is the **BARREL**, not the domain modules; `frontend/src/lib/api/documents.ts` has **no ledger row of its own**, which is the finding to record rather than repeat
  </read_first>
  <action>
    Add five exported functions to `frontend/src/lib/api/documents.ts`, each following
    `listDocuments`' exact shape (`getAuthHeaders()` -> `fetch` -> `if (!res.ok) throw` ->
    `res.json() as Promise<T>`), typed against the types plan 08 added:
    `getDocumentContent(id, opts?: { startLine?: number; endLine?: number }): Promise<DocumentContentResponse>`
    (serialising `start_line` / `end_line` query params — ⚠ those are the parameter names the backend
    takes, NOT `from` / `to`), `listDocumentChunks(id): Promise<DocumentChunkRow[]>`,
    `listDocumentTables(id): Promise<DocumentTableRow[]>`,
    `listDocumentImages(id): Promise<DocumentImageRow[]>`,
    `listDocumentQueries(id): Promise<DocumentQueryRow[]>`.
    Each throws a distinct message so an error arm can say which section failed.

    Re-export all five from the existing documents block in `frontend/src/lib/api.ts`. ⚠ This is
    mandatory, not stylistic: every consumer imports from `@/lib/api` (see
    `RelationshipsSection.tsx:33`) and a symbol exported from a domain module but forgotten in the
    barrel typechecks perfectly and is invisible to every consumer — that is `D-207-06`, which has
    had no guard.

    Close that hole: extend `frontend/src/lib/__tests__/apiBarrel.test.ts` with a second
    `import * as DocumentsDomain from "../api/documents"` and a second loop asserting every runtime
    export of that module is present on the barrel and is the SAME reference. Keep the shipped
    non-vacuity control shape (`expect(documentsExports.length).toBeGreaterThan(0)`) — a loop over an
    empty export set passes for free.

    ⚠ This file is pinned in BASELINE at **3** and is in TARGETS. Extending it RAISES its count;
    plan 12 re-pins from the gate's own printed `— N new` column. Record the new case count in the
    summary so that arithmetic is attributable.
  </action>
  <acceptance_criteria>
    - `grep -n "export async function getDocumentContent\|listDocumentChunks\|listDocumentTables\|listDocumentImages\|listDocumentQueries" frontend/src/lib/api/documents.ts` returns five lines
    - `grep -n "getDocumentContent" frontend/src/lib/api.ts` returns one line inside the documents re-export block
    - `getDocumentContent` builds `start_line` / `end_line` query params — `grep -n "start_line" frontend/src/lib/api/documents.ts` returns a line; `grep -n "[?&]from=" ` returns nothing
    - `npx vitest run src/lib/__tests__/apiBarrel.test.ts --maxWorkers=2` passes and its case count is greater than 3 (record the exact number)
    - Deleting one of the five from `frontend/src/lib/api.ts` makes the barrel test RED — state that this was observed
    - `npx tsc --noEmit -p tsconfig.app.json` -> no new errors
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/apiBarrel.test.ts --maxWorkers=2</automated>
  </verify>
  <done>Five client functions exist, all are re-exported, and the barrel is now guarded for `documents.ts` as well as `connectors.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: The Content and Chunks sections — lazy by construction, four honest arms each</name>
  <files>frontend/src/components/metadata/DocumentContentSection.tsx, frontend/src/components/metadata/DocumentChunksSection.tsx, frontend/src/components/metadata/DocumentDetailPanel.tsx</files>
  <read_first>
    - `frontend/src/components/relationships/RelationshipsSection.tsx:1-175` in full — the import block at `:31-37` (⭐ it imports from `@/lib/api`, THE BARREL, never `lib/api/*`), the props at `:48-55`, `LoadState` at `:57`, the keyed load at `:74-99`, and the four arms at `:151` / `:160` / `:175`
    - `frontend/src/components/panel/PanelSection.tsx` (all ~101 lines — `defaultOpen = true` at `:46`, the conditional body at `:94`, `PanelSectionCount` at `:21` and `renderCount` at `:32-40`)
    - `frontend/src/components/metadata/DocumentDetailPanel.tsx:230-300` — the three shipped mounts at `:242`, `:265-274`, `:279`, which all rely on the eager default
    - `docs/HOT-FILE-LEDGER.md` § `frontend/src/components/metadata/DocumentDetailPanel.tsx` — D-217-01 requires it; ⚠ its binding invariant is that this is a **CROSS-SURFACE shell** reusing `WorkspacePanel`'s sheet, which `ChatLayout` mounts, so a change here is judged against the chat surface too; and its 430px track is set by the HOST grid, not by this file
    - `.claude/skills/sketch-findings-agentic-rag/SKILL.md` — the document detail panel section it covers
  </read_first>
  <action>
    Create `frontend/src/components/metadata/DocumentContentSection.tsx` and
    `DocumentChunksSection.tsx`, both on the `RelationshipsSection` pattern: the section owns its own
    fetch, keyed on `docId` in the `useCallback` deps, lifting its total via `onTotalChange` so the
    `PanelSection` badge shows a count. Import the client functions from `@/lib/api` — the BARREL,
    never `@/lib/api/documents`.

    Each renders FOUR distinct states and no fewer: `loading` (`role="status" aria-live="polite"`
    with an `sr-only` label), `ready` with rows, `ready` with none (a WORDED empty arm), and `error`
    (`role="alert"` plus a *Try again* button). ⚠ An error must be visually distinct from an empty
    result — D-117-10. Use `text-panel-muted-foreground[-dim]` for every meaningful string, never the
    global muted; the panel's own docblock records the 3.59:1 measurement behind that rule.

    **Content section** — renders the document's parsed text for the current line range, with a
    *Load more* control gated on the envelope's `has_more`, and the document's `extractor` shown as
    the provenance of that text (D-217-08 — it is already on the row the panel holds, so no extra
    request). ⛔ The text arrives UNNUMBERED from the route; do not add numbering.

    **Chunks section** — a read-only list of chunks in `chunk_index` order, each showing its content
    and its `embedding_model` (D-217-08: the per-chunk variation mid-re-embed is the whole point, so
    a chunk written with an older model must be visibly distinguishable from one written with the
    current model).

    ⚠ **Both are untrusted document content.** Render every string as a React text child; ⛔ no
    `dangerouslySetInnerHTML` anywhere in either file.

    Mount both in `DocumentDetailPanel.tsx` as `<PanelSection … defaultOpen={false}>`. ⛔
    `defaultOpen={false}` is the ENTIRE lazy mechanism (`PanelSection.tsx:94` renders children only
    when open) and `PanelSection` has **no `onOpenChange` / `onToggle` prop** — so a section that
    fetches in a bare `useEffect` while mounted eagerly is exactly the trap the shipped
    Relationships mount falls into. Copy the pattern, not that default.

    ⚠ State in the summary, per D-217-01: this panel measures `6 / 5 / 405` and FIRES G-5; it is a
    cross-surface shell, so this change is judged against the chat surface too; and its gate side is
    PARTIAL — `DocumentDetailPanel.images.test.tsx` is pinned at 3 in both knobs while
    `DocumentDetailPanel.a11y.test.tsx` is in neither.
  </action>
  <acceptance_criteria>
    - `grep -c "defaultOpen={false}" frontend/src/components/metadata/DocumentDetailPanel.tsx` returns at least `2` after this plan
    - `grep -n "@/lib/api/documents" frontend/src/components/metadata/DocumentContentSection.tsx frontend/src/components/metadata/DocumentChunksSection.tsx` returns nothing — both import the barrel
    - `grep -n "dangerouslySetInnerHTML" frontend/src/components/metadata/` returns nothing
    - Each section file contains `role="status"`, `role="alert"` and a distinct worded empty string — four arms, greppable
    - The chunks section renders `embedding_model` per chunk — assert by rendering two chunks with different models and finding both strings
    - The content section renders text with no `^\d+: ` prefix on any line
    - `npx vitest run src/components/metadata/__tests__/DetailSections.lazy.test.tsx --maxWorkers=2` passes
    - The three shipped `PanelSection` mounts at `:242`, `:265`, `:279` are UNCHANGED (`git diff` shows no hunk altering their props). ⚠ **D-217-25a: this fence is about PROPS, not POSITION.** The two new sections are INSERTED between Details and Relationships, so Relationships and Classification MOVE DOWN. That is required, not a violation — a relocation alters no prop
    - **D-217-25b — the titles are exactly `Text` and `Chunks`.** ⛔ `grep -n 'title="Details"' frontend/src/components/metadata/DocumentDetailPanel.tsx` returns NOTHING: the shipped metadata section's plain label is already `Details` (`termMap.ts:90`), and a second `Details` would put the same word on the panel twice over two unrelated surfaces
    - **D-217-25a order fence, asserted from the rendered DOM not the source:** in `DetailSections.lazy.test.tsx`, query all `PanelSection` headings and assert the sequence begins `Details, Text, Chunks` and ends `Relationships, Classification`. ⚠ **Non-vacuity: the assertion must be observed RED** against a build that appends the two sections after Classification instead
  </acceptance_criteria>
  <verify>
    <!-- ⚠ NOT `DetailSections.lazy.test.tsx` — TASK 3 of this plan creates it, so naming it here
         errors file-not-found at execute-plan's HARD GATE and sends the executor into a repair cycle
         over a plan that was correct. This arm runs the SHIPPED panel suite (⭐ the only fully-gated
         doc-space suite — it renders `DocumentDetailPanel`, so mounting two new children and
         breaking the shell shows up here) plus a source gate over this task's own three files. The
         lazy suite command lives on task 3, which creates it. -->
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json && npx vitest run src/components/metadata/DocumentDetailPanel.images.test.tsx --maxWorkers=2 && node -e 'const fs=require("fs");const p=fs.readFileSync("src/components/metadata/DocumentDetailPanel.tsx","utf8");const n=(p.match(/defaultOpen=\{false\}/g)||[]).length;if(n<2)throw new Error("expected at least 2 lazy mounts, found "+n);for(const f of ["DocumentContentSection","DocumentChunksSection"]){const t=fs.readFileSync("src/components/metadata/"+f+".tsx","utf8");if(/@\/lib\/api\/documents/.test(t))throw new Error(f+" imports the domain module instead of the barrel");if(/dangerouslySetInnerHTML/.test(t))throw new Error(f+" renders raw HTML");for(const a of ["role=\"status\"","role=\"alert\"","Try again"])if(!t.includes(a))throw new Error(f+" is missing its "+a+" arm")}console.log("task 2 source gate OK")'</automated>
  </verify>
  <done>Two sections fetch on expand, render four honest arms, escape all document content, and the shipped eager mounts are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: The lazy suite — ZERO requests before the accordion is clicked</name>
  <files>frontend/src/components/metadata/__tests__/DetailSections.lazy.test.tsx</files>
  <read_first>
    - `frontend/src/components/relationships/RelationshipsSection.test.tsx` and `RelationshipsSection.a11y.test.tsx` (both shipped, both currently in NEITHER count-gate knob) — the mocking style and the a11y assertions to model on
    - `frontend/src/components/metadata/DocumentDetailPanel.images.test.tsx` — ⭐ the ONLY fully-guarded doc-space suite (TARGETS + BASELINE at 3); match its harness rather than inventing a second one
    - `.planning/phases/217-the-library-one-home-for-documents/217-VALIDATION.md` — the LIB-04 / D-217-04 rows
  </read_first>
  <action>
    Create `frontend/src/components/metadata/__tests__/DetailSections.lazy.test.tsx`, mocking
    `@/lib/api` **by the barrel path** (that is how the components import it — mocking
    `@/lib/api/documents` would leave the real barrel in place and the mock would never be reached).

    Cases:
    1. ⭐ **The lazy criterion** — render `DocumentDetailPanel` for a document and assert the content
       and chunks client functions were called **zero** times. Then click the section's accordion
       button and assert the corresponding function was called exactly once with the document's id.
       Add an explicit NON-VACUITY control: a mocked function that IS expected to be called on mount
       (or an assertion that the accordion button was found), so "zero calls" cannot be satisfied by
       the panel failing to render at all.
    2. **Four arms per section** — a pending promise renders the `role="status"` arm; a rejection
       renders the `role="alert"` arm WITH a *Try again* control; an empty array renders the worded
       empty arm; rows render the list. Assert the empty and error renderings are DIFFERENT strings.
    3. **Escaping** — a chunk whose content is `<img src=x onerror=alert(1)>` renders as literal
       text and produces no `img` element in the DOM.
    4. **Re-key on document change** — switching `docId` re-fetches for the OPEN section and does not
       show the previous document's rows.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/metadata/__tests__/DetailSections.lazy.test.tsx --maxWorkers=2` passes with at least 10 cases
    - Case 1 has a non-vacuity control; removing `defaultOpen={false}` from either mount makes it RED — state that this was observed
    - `grep -n "vi.mock(\"@/lib/api\")" frontend/src/components/metadata/__tests__/DetailSections.lazy.test.tsx` returns a line (the barrel path, not the domain path)
    - Case 3 asserts `container.querySelector("img")` is `null`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/metadata/__tests__/DetailSections.lazy.test.tsx --maxWorkers=2</automated>
  </verify>
  <done>Zero fetches on open is asserted with a control that cannot pass vacuously, and untrusted content is proven to render as text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> panel DOM | `document_chunks.content` and `full_markdown` are UNTRUSTED document content rendered into the page |
| panel -> five read routes | the open document's id parameterises five requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-37 | Injection (XSS) | rendering chunk text and parsed markdown | mitigate | Every cell/paragraph renders via React text children, which auto-escape — the discipline `CsvTablePreview.tsx:15-17` records as T-087-04. ⛔ No `dangerouslySetInnerHTML` in `components/metadata/`, asserted by grep AND by a render case that a payload produces no `img` element |
| T-217-38 | Denial of service | fetching megabytes on document open | mitigate | `defaultOpen={false}` on both new sections, plus line-range paging on `/content`. Asserted as ZERO calls before the accordion click, with a non-vacuity control |
| T-217-39 | Information disclosure | the panel is a CROSS-SURFACE shell | mitigate | It reuses `WorkspacePanel`'s sheet, which `ChatLayout` mounts — a change here lands on the chat surface too. The new sections are additive `PanelSection` children with no change to the shell's shape or to the three shipped mounts, asserted by `git diff` |
| T-217-40 | Elevation of privilege | section requests | mitigate | Every request goes to a route that applies RLS plus an owner check server-side (plans 02/03); the client passes only the open document's id and authorizes nothing |
| T-217-41 | Repudiation | a missing barrel export | mitigate | `D-207-06` has had no guard: a symbol exported from a domain module and forgotten in the barrel typechecks and is invisible. Task 1 extends `apiBarrel.test.ts` to close it for `documents.ts` |
| T-217-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed by this plan |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/metadata/__tests__/DetailSections.lazy.test.tsx src/lib/__tests__/apiBarrel.test.ts --maxWorkers=2` -> passes
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` -> no new errors
- ⚠ Manual row **M-5** (the empty arms are honest — *"No tables"* / *"No tables were extracted"* / *"This file type has no tables"* are three different claims) stays OWED and is read against real documents of each kind.
</verification>

<success_criteria>
- Five client functions exist, are re-exported, and the barrel is guarded for this module
- The parsed text and the chunks are readable, unnumbered, with per-chunk embedding model
- Opening a document fires zero section requests
- No `dangerouslySetInnerHTML`; document content renders as text
</success_criteria>

<output>
Create `.planning/phases/217-the-library-one-home-for-documents/217-10-SUMMARY.md` when done.
Record the new `apiBarrel.test.ts` case count and the observed REDs (barrel deletion, `defaultOpen`
removal), so plan 12's BASELINE arithmetic is attributable.
</output>
