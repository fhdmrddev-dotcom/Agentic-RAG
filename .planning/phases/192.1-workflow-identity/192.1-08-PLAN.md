---
phase: 192.1-workflow-identity
plan: 08
type: execute
wave: 7
depends_on: ["192.1-07"]
files_modified:
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/phases/192.1-workflow-identity/192.1-UAT.md
  - frontend/src/components/workflows/library/WorkflowCard.tsx
autonomous: false
requirements: [LIB-05]
gap_closure: false

must_haves:
  truths:
    - "The next phase that touches WorkflowsPage.tsx or WorkflowCard.tsx inherits a MEASURED hotness count instead of re-deriving one"
    - "The ROADMAP no longer states something about the hot-file ledger that is false"
    - "Nine lived-experience UAT rows exist, none of which locates its target by id"
    - "The phase's own residual — that some rows cannot be told apart by any recorded fact — is reported in writing, not discovered at UAT"
  artifacts:
    - path: "CLAUDE.md"
      provides: "the WorkflowsPage.tsx ledger row re-measured, and a NEW WorkflowCard.tsx row at its measured young count"
      contains: "WorkflowCard.tsx"
    - path: ".planning/phases/192.1-workflow-identity/192.1-UAT.md"
      provides: "the nine G-4 rows, each driven by looking"
      min_lines: 60
      contains: "U9"
  key_links:
    - from: "CLAUDE.md"
      to: "frontend/src/components/workflows/library/WorkflowCard.tsx"
      via: "the new ledger row, and the card's own docblock carrying the same measured subtree figure"
      pattern: "SUBTREE"
---

<objective>
Close the phase honestly: **re-measure and publish both hot-file ledger rows** (D-02), **correct the
ROADMAP flag line that is factually wrong** (D-02), **correct the stale figures in the card's own
docblock on measurement** (S-10), and **author the nine G-4 UAT rows** (D-27), including the two rows
that exist to report this phase's own residuals rather than let them be discovered.

Purpose: *a guardrail cannot see what is absent from its list.* `WorkflowsPage.tsx` escaped G-5 for
ten phases for exactly that reason. And *a figure written at a phase's close goes stale on that
phase's own fix commit* — so the figures published here carry their derivation, not just their value.
Output: two ledger rows, one ROADMAP correction, one docblock correction, `192.1-UAT.md`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**NEVER `rm -rf` a worktree** — `bash scripts/teardown-worktree.sh <path>`.
⚠ Assert the dispatched base SHA and record it. ⚠ `GSD_VITEST_MAX_WORKERS=4` on every vitest run.

⚠ This plan contains a **blocking human checkpoint** and is therefore `autonomous: false`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192.1-workflow-identity/192.1-CONTEXT.md
@.planning/phases/192.1-workflow-identity/192.1-RESEARCH.md
@.planning/phases/192.1-workflow-identity/192.1-PATTERNS.md
@.planning/phases/192.1-workflow-identity/192.1-VALIDATION.md
@.planning/phases/192.1-workflow-identity/192.1-BASELINE.md
@.planning/sketches/163-the-assembled-card/README.md

<interfaces>
<!-- What must be corrected, verbatim as it reads today. -->

.planning/ROADMAP.md — Phase 192.1 **Flags** line, WRONG and corrected here rather than quietly followed:
  "G-5: `WorkflowCard.tsx` and `WorkflowsPage.tsx` are both on the hot-file ledger — check it before planning."
  MEASURED: WorkflowCard.tsx is NOT on the ledger at all. Its only occurrence in CLAUDE.md sits inside
  the WorkflowsPage.tsx row's PROSE ("`WorkflowCard.tsx` 545 L is a REWRITE"), which is a sentence, not
  a row. And on the numbers it would not fire anyway: 4 commits, ONE phase (192-09/12/13), 567 L at the
  pre-phase reading — the G-5 threshold is ≥ 3 PHASES.

frontend/src/components/workflows/library/WorkflowCard.tsx:119-120 — STALE at HEAD:
  *   PAGE      `WorkflowsPage.tsx`  1407 → 1007  (−28.4 %)   CODE 1021 → 479  (−53.1 %)
  *   SUBTREE   page + these 7 modules  1407 → **3182  (+126.2 %)**   CODE 1021 → 1499 (+46.8 %)
  RESEARCH re-measured at its own HEAD: page 1180, seven modules 2258, subtree 3438.

The ledger rows this phase must join — CLAUDE.md § Workflow guardrails, hot-file ledger:
  `frontend/src/pages/WorkflowsPage.tsx`  — 30 commits / 11 phases / 1407 → 1180 L, "satisfied (192)"
  `frontend/src/components/workflows/PhaseNodeCard.tsx` — the model for a row that states its own
  derivation commands and its own subtree-growth figure rather than asserting them.

The nine G-4 rows to author — 192.1-VALIDATION.md § Manual-Only Verifications (U1…U9).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure and publish both ledger rows, and correct the ROADMAP flag line (D-02, S-10)</name>
  <files>CLAUDE.md, .planning/ROADMAP.md, frontend/src/components/workflows/library/WorkflowCard.tsx</files>
  <read_first>
    - `CLAUDE.md` § "Workflow guardrails (MANDATORY)" — the whole hot-file ledger table, especially the `WorkflowsPage.tsx` row (the one being re-measured) and the `PhaseNodeCard.tsx` row (the model for stating derivations)
    - `.planning/ROADMAP.md` `#### Phase 192.1` — the **Flags** line
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` lines 105-145 (the docblock carrying the stale figures and the derivation template)
    - `.planning/phases/192.1-workflow-identity/192.1-BASELINE.md` (the PRE-phase classification — the "before" column)
    - `192.1-CONTEXT.md` D-01, D-02 · `192.1-PATTERNS.md` § S-10
  </read_first>
  <action>
**(a) Re-derive, do not look up.** Run and record every command:
```
git log --oneline -- frontend/src/pages/WorkflowsPage.tsx | wc -l
git log --oneline -- frontend/src/components/workflows/library/WorkflowCard.tsx | wc -l
git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | sort -u
wc -l frontend/src/pages/WorkflowsPage.tsx
wc -l frontend/src/components/workflows/library/*.ts frontend/src/components/workflows/library/*.tsx
```
(exclude `*.test.*` from the subtree sum, as 188.2 and Phase 192 both did — and say so).

Re-run Plan 02's VALIDATED line classifier over the post-phase page and all `library/` source modules
to produce the "after" column. **If any figure you inherit from a prior SUMMARY disagrees with your
measurement, publish YOURS with the derivation and say the earlier one was stale** — that is the habit
the two existing ledger rows already keep, and the `WorkflowsPage.tsx` row records a case where its own
number went stale on its own phase's fix commit.

**(b) Update the `WorkflowsPage.tsx` ledger row** with the new commit count, the new phase list
(192.1 joins it), the new line count, and the G-5 status. **State the growth rather than smoothing
it** — 188.2 measured +67.1 % and Phase 192 measured +126.2 % against its own +5…+16 % estimate. Record
whether the D-01 extraction made the page SMALLER (it should: ~225 L moved out) and whether the SUBTREE
grew (it will: five new modules with their own headers, imports and props types).

Record the G-5 verdict for the next phase in the row's own words: **G-5 fired on this file, the
recommendation was produced as the first option, and it was TAKEN in 192.1** — with the seam that
remains named, so the next phase inherits a measured obligation instead of re-deriving one. Name what
is still on the page (the Builder host, the publish-gauntlet mount, the `WorkflowDoorSwitch`, the
post-publish Run CTA banner, the three-feed fetch orchestration, the `onOpenDraft` navigation) and say
that a phase adding a genuinely SECOND concern there still owes a refactor recommendation FIRST.

**(c) ADD the `WorkflowCard.tsx` row at its measured young count (D-02).** G-5 does NOT fire on it —
state that plainly with the numbers — and add the row anyway. *The standing lesson from Phase 192 is
that a guardrail cannot see what is absent from its list; `WorkflowsPage.tsx` escaped G-5 for ten
phases for exactly that reason.* A young file put on the ledger now inherits a measured count instead
of forcing a re-derivation in 194. Include its derivation commands in the cell, like the two rows
above it.

**(d) Correct the ROADMAP Flags line (D-02).** It says `WorkflowCard.tsx` is on the ledger; measured,
it was not. **Correct it in place and leave the correction VISIBLE** — say what it said, what is true,
and how that was measured. This project's habit is to leave the wrong claim readable beside its
correction, not to delete it.

**(e) Correct `WorkflowCard.tsx:119-120` on measurement (S-10).** Replace the stale
`PAGE 1407 → 1007` / `SUBTREE 1407 → 3182` with the measured post-phase figures, and add the phase's
own delta beneath them using the same classifier and the same known-good citation
(`95a4c915:PhaseNodeCard.tsx = 797/518/249/30`). ⚠ This is a comment-only edit to a source file — no
behaviour, no string, no testid changes.
  </action>
  <acceptance_criteria>
    - The `WorkflowsPage.tsx` ledger row states a commit count, a phase list including 192.1, a line count, and a subtree figure — each with the command that produced it, present in the cell
    - A NEW `frontend/src/components/workflows/library/WorkflowCard.tsx` row exists in the ledger table, stating that G-5 does NOT fire and giving the measured commit/phase/line counts with their commands
    - `grep -c "WorkflowCard.tsx" CLAUDE.md` is now ≥ 2, and at least one occurrence is a table ROW (a line beginning with `| \`frontend/src/components/workflows/library/WorkflowCard.tsx\``)
    - The ROADMAP Phase 192.1 Flags line no longer asserts that `WorkflowCard.tsx` is on the ledger, and the correction states both the old claim and the measurement
    - `WorkflowCard.tsx:119-120`'s figures match the measured post-phase reading, cite `95a4c915` and are the only changed lines in that file's diff (comment-only)
    - `git diff frontend/src/components/workflows/library/WorkflowCard.tsx` contains ONLY comment lines
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/WorkflowCard.test.tsx` is green at its pinned count
  </acceptance_criteria>
  <verify>
    <automated>grep -c "frontend/src/components/workflows/library/WorkflowCard.tsx" CLAUDE.md && cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/</automated>
  </verify>
  <done>Both ledger rows carry re-derived numbers with their commands, the ROADMAP's false flag claim is corrected visibly, and the card's stale figures are replaced on measurement.</done>
</task>

<task type="auto">
  <name>Task 2: Author 192.1-UAT.md — nine rows, none located by id (D-27, D-28, D-07, D-31)</name>
  <files>.planning/phases/192.1-workflow-identity/192.1-UAT.md</files>
  <read_first>
    - `192.1-VALIDATION.md` § "Manual-Only Verifications — G-4 lived-experience UAT (FIRES)" (rows U1…U9 and the rule above them)
    - `.planning/ROADMAP.md` Phase 192.1 § "How we'd know this failed (G-6)" and the paragraph beginning *"And the verification of the fix was itself too weak"*
    - `.planning/sketches/163-the-assembled-card/README.md` § "What this still cannot catch, said plainly"
    - `192.1-CONTEXT.md` D-07, D-13, D-27, D-28, D-30, D-31 · `192.1-RESEARCH.md` § 5b, § P-3
    - `.planning/phases/192-workflow-library-ia/192-UAT.md` if present (the house UAT-table format)
  </read_first>
  <action>
Create `.planning/phases/192.1-workflow-identity/192.1-UAT.md` carrying VALIDATION's nine rows U1…U9
verbatim in scope, each expanded into: **what the operator does**, **what they should see**, **what
counts as a failure**, and a **result** column left blank for the drive.

**⚠ THE RULE THAT MAKES THIS FILE NECESSARY, and it must appear at the top in these words:**
*A machine check that bypasses the human's task does not verify the human's task.* Phase 192's post-fix
re-drive located rows with `getElementById` on a known UUID — it proved the code and could not prove the
row was FINDABLE, and the operator, who had to find it, could not. **NO row in this file may locate its
target by id.** Search for it, read the line, point at it. Name
`.planning/sketches/163-the-assembled-card/index.html` as the visual reference for the pixel-level
comparison no automated check covers, and say plainly what none of the automated checks cover: spacing,
Tailwind class choices, wrap, truncation, and hover/focus states.

The nine rows, each with its criterion:
- **U1** (SC#1) — pick one specific *Compliance Gap Report* by READING the screen and open it; verify it
  is the one intended. This is the row Phase 192 failed.
- **U2** (SC#2) — a row says whose it is and what it came from: read `Yours`/`Shared` + lineage on a
  fork, an original and a starter.
- **U3** (SC#3) — tell which workflow changed most recently, by reading `changed <rel>` across the list.
- **U4** (D-07 + D-31) — **two questions in one row, both of which this phase owes an answer to.**
  (a) Does `1 of 43` read as an INDEX rather than a count? If yes, it is a one-line change in
  `libraryVocabulary.ts` by construction. (b) **The D-31 residual, stated honestly:** on rows identical
  in every recorded fact the line CANNOT disambiguate, `1 of N` is then a true warning, and the
  product's answer is the rename (162-B). Confirm the line does not PRETEND to disambiguate.
  ⚠ **Write the residual here rather than letting it be discovered.**
- **U5** (D-19/D-20) — fork a starter, type a colliding name, observe the warning, PROCEED ANYWAY, and
  confirm the primary button says `Create my copy`.
- **U6** (D-23) — fork a published row that already has a draft: **no prompt**, the existing draft
  opens. Regressing this re-opens the U5 blocker.
- **U7** (D-28) — block `/workflows/drafts`, reload, and read a colliding row's line AND the
  `source-failed` banner together. Confirm a collapsed `1 of N` under the banner does not mislead.
  Record that this was DECIDED (the banner is the disclosure, spent once rather than 106 times), not
  defaulted.
- **U8** — compare the identity line against sketch 163 at the duplicate shape: spacing, wrap,
  truncation on a long lineage phrase; **check 375 px**.
- **U9** (D-05/D-34) — type and delete rapidly in search and watch ONE row: the line must not flicker,
  re-order, or disagree with itself, and `1 of N` must not change.

**⚠ Add a tenth, non-numbered NOTE that will otherwise read as a bug (D-13 / D-30 / C-3):**
`compliance-gap-report` — the base slug of the 43-row family — is itself an ORPHAN under the copy-fork
regex (`report` is six legal lowercase alphanumerics), so **the family's TRUE ORIGINAL renders NO
lineage segment while its 43 copies say "Copy of Compliance Gap Report."** That is CORRECT under D-13:
silence beats a fabricated claim, and the measured trade is **57 resolve · 12 go quiet · 0 lie.** It is
written here so it is reported rather than discovered.

**Cross-provider roster: state that it DOES NOT APPLY** — no streaming, no agent loop, no provider
routing, no provider-dependent UI state. **Say it, do not omit it.**
  </action>
  <acceptance_criteria>
    - `.planning/phases/192.1-workflow-identity/192.1-UAT.md` exists with nine rows U1…U9, each carrying what-to-do / what-to-see / what-counts-as-failure / result
    - `grep -ci "getElementById\|by id" .planning/phases/192.1-workflow-identity/192.1-UAT.md` shows the phrase used ONLY inside the prohibition, never as an instruction
    - The prohibition sentence *"A machine check that bypasses the human's task does not verify the human's task"* appears verbatim
    - U4 covers BOTH the `1 of N` wording question and the D-31 residual
    - The `compliance-gap-report` orphan note is present with the `57 · 12 · 0` trade
    - The cross-provider roster's non-applicability is STATED
    - `index.html` of sketch 163 is named as the visual reference
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/192.1-workflow-identity/192.1-UAT.md && grep -q "does not verify the human's task" .planning/phases/192.1-workflow-identity/192.1-UAT.md && grep -q "compliance-gap-report" .planning/phases/192.1-workflow-identity/192.1-UAT.md && grep -q "U9" .planning/phases/192.1-workflow-identity/192.1-UAT.md</automated>
  </verify>
  <done>Nine lived-experience rows exist, none locating its target by id, with the phase's two residuals reported in writing and the sketch named as the visual bar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator drives the G-4 rows against the real library</name>
  <read_first>
    - `.planning/phases/192.1-workflow-identity/192.1-UAT.md` (the nine rows, as authored in Task 2)
    - `.planning/sketches/163-the-assembled-card/index.html` (the visual bar)
  </read_first>
  <what-built>
    Everything above: `updated_at` end to end; the fork concern extracted out of `WorkflowsPage.tsx`;
    the identity resolver with its four-state lineage and D-31 ranker; the nine-band relative-time
    formatter; the 14th atom on every card; and the fork name prompt. All gates are green —
    `tsc -p tsconfig.app.json` at its baseline, eslint 0, `npm run lint:a11y` 0, the eight-suite scoped
    run at 0 failed, and `node scripts/vitest-count-gate.cjs` exit 0 — and the numbers are quoted in the
    SUMMARY.
  </what-built>
  <how-to-verify>
    1. Start the app (`http://localhost:5173/`) and open the Workflows page against your REAL library.
    2. Work through `192.1-UAT.md` rows **U1 → U9 in order**. **Drive them the way a person does — by
       looking.** Search for a row, read its identity line, click it. **Do not locate anything by id:**
       that is precisely the check Phase 192 ran, and it proved the code while failing to prove the row
       was findable.
    3. **U1 first** — it is SC#1 and it is the row Phase 192 failed: pick one specific *Compliance Gap
       Report* by reading the screen and open the one you meant.
    4. **U8 needs the sketch open side by side** at `.planning/sketches/163-the-assembled-card/index.html`,
       and needs a 375 px viewport check.
    5. **U7 needs the drafts feed blocked** (DevTools request blocking on `/workflows/drafts`, then
       reload) — read the colliding row's line AND the banner together.
    6. Record PASS / FAIL / BLOCKED in each row's result column, with what you saw. **A blocked row is
       recorded as ⛔ with its reason — never dropped from the table.**
    7. ⚠ **U4 asks you two design questions, and both are real:** does `1 of 43` read as an index rather
       than a count (a one-line fix in one file if so), and does the line honestly present itself as a
       warning on rows it cannot disambiguate rather than pretending to?
  </how-to-verify>
  <acceptance_criteria>
    - Every one of U1…U9 carries a recorded PASS, FAIL or ⛔-with-reason — none is blank and none is omitted
    - U1's result names the specific workflow opened and confirms it was the intended one
    - U4's two questions each have a written answer
    - Any FAIL is sized before routing: ≤ 1 file / ≤ 10 lines with no schema or API surface is `/gsd:fast` under G-3, never a gap round
  </acceptance_criteria>
  <action>
Hand the nine rows to the operator and WAIT. Do not drive them yourself, do not substitute a headless
or Chrome-MCP drive for the human's reading, and do not mark a row PASS from another row's evidence.
Transcribe the operator's verbatim answers into each row's result column in
`.planning/phases/192.1-workflow-identity/192.1-UAT.md`, including ⛔ with its reason for any blocked
row, then commit that file.
  </action>
  <resume-signal>Type "approved" with the nine results, or describe what you saw fail.</resume-signal>
  <verify>
    <human-check>All nine rows recorded in `192.1-UAT.md` with results; the operator has confirmed or rejected U4's two questions.</human-check>
  </verify>
  <done>The nine G-4 rows are driven by looking and their results are recorded in `192.1-UAT.md`, including any ⛔ with its reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future phases | A wrong number in `CLAUDE.md` or a docblock survives every gate, because nothing typechecks prose (Phase 189's banked lesson). |
| automated gates → the human's actual task | The G-6 failure condition for this phase is precisely a fix "verified by a driver that located rows by id rather than by reading the screen." |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192.1-23 | Repudiation | A ledger figure asserted rather than derived | mitigate | Every number in both rows carries the command that produced it, in the cell, following the `PhaseNodeCard.tsx` row's model. Any disagreement with an inherited figure is published with both readings. |
| T-192.1-24 | Spoofing (of verification) | A UAT drive that bypasses the human's task | mitigate | D-27 is written into the file's own header, and no row may locate its target by id. The blocking checkpoint requires a recorded result per row, with ⛔ rather than omission for blocked rows. |
| T-192.1-25 | Information Disclosure | — | n/a | This plan changes no runtime code path; the only source edit is comment-only. |
| T-192.1-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** |
</threat_model>

<verification>
- `grep` proves a `WorkflowCard.tsx` ROW exists in the CLAUDE.md ledger table
- The ROADMAP Flags line no longer contains the false claim, and the correction is visible
- `git diff frontend/src/components/workflows/library/WorkflowCard.tsx` is comment-only
- `192.1-UAT.md` exists with nine rows and the D-27 prohibition verbatim
- Final phase gate: `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` → **33**; `npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` → 0; `npm run lint:a11y` → 0; `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` → exit 0; `bash scripts/check-deploy-drift.sh` → silent
- The operator has recorded a result for each of U1…U9
</verification>

<success_criteria>
- Both hot-file ledger rows carry re-derived, command-backed figures, and `WorkflowCard.tsx` is ON the ledger for the first time
- The ROADMAP's false G-5 claim is corrected with the correction left visible
- The card's stale growth figures are replaced on measurement
- Nine G-4 rows exist, none by id, with the phase's two residuals reported rather than discovered
- The operator has driven them and recorded the results
</success_criteria>

<output>
Create `.planning/phases/192.1-workflow-identity/192.1-08-SUMMARY.md` when done.
Record: the base SHA; every re-derived ledger figure with its command and any disagreement with an
inherited reading; the final gate numbers; the nine UAT results; and — if any row FAILED — the size of
each fix, so the orchestrator can route it under G-3 rather than opening a round.
</output>
