---
phase: 192.1-workflow-identity
plan: 02
subsystem: planning-artifact
tags: [baseline, characterization, measurement, D-01, LIB-05]
requires: []
provides:
  - "192.1-BASELINE.md — the pre-move stick Plans 03-07 are measured against"
  - "WORKFLOWS_PAGE_DIGEST = 84e51f199b4b8992 (65266 chars, 1180 lines)"
  - "A line classifier validated against 188.2's published known-good"
  - "The 13-case fork-arc characterization set, recorded green pre-move"
affects:
  - ".planning/phases/192.1-workflow-identity/192.1-BASELINE.md"
tech-stack:
  added: []
  patterns:
    - "Validate a measuring tool against a published known-good BEFORE trusting its output"
key-files:
  created:
    - ".planning/phases/192.1-workflow-identity/192.1-BASELINE.md"
  modified: []
decisions:
  - "The fork-arc characterization set is THIRTEEN cases, not the twelve every upstream document says"
  - "The a11y CI gate (npm run lint:a11y) is RED on a clean tree with 4 pre-existing out-of-scope errors; the binding 192.1 lint baseline is the SCOPED eslint command, which is exit 0"
  - "FOUR suites sit above their count-gate pins, not two; the four deltas sum to exactly the +24 total slack"
metrics:
  duration: "~35 min"
  completed: 2026-08-12
  tasks: 2
  commits: 2
  production_files_changed: 0
---

# Phase 192.1 Plan 02: Pre-move baseline capture Summary

Captured every stick the D-01 extraction will be judged against on the UNMOVED tree — eight suite
counts, both typecheck knobs, both lint scopes, two count-gate runs, the 13-case fork-arc
characterization set, an FNV-1a page digest, and a known-good-validated blank/comment/code
classification of the page and the seven-module library subtree — into one dated artifact, and
found six upstream figures that measurement contradicts.

## Base SHA — asserted, and it needed correcting

**`8fc9bd7450470f461cfce0aae1ecbb38ba16a73b`** — verified and recorded inside `192.1-BASELINE.md`
itself, not only here.

⚠ **The assertion fired.** `git merge-base HEAD 8fc9bd74…` initially resolved to **`3781a3fe`** —
this worktree had forked from the wrong base, the same fault measured on **12 of 12 Phase 192
worktrees**. Corrected with `git reset --hard 8fc9bd74…` and re-asserted **before any measurement
ran**. Had it not been caught, every number in the artifact would have been timestamped to the wrong
tree — and Plan 03 would have trusted it. For a plan whose entire product is baselines, this guard
is the difference between an artifact and a liability.

## What was measured

| Stick | Value |
|---|---|
| Scoped 8-suite run | **8 files · 303 tests · 0 failed** (48/39/64/36/36/32/16/32) |
| `tsc -p tsconfig.app.json --noEmit` | **33** errors over **1129** files |
| `tsc --noEmit` (bare) | **0** errors over **0** files — it checks nothing |
| `eslint` library + page | **exit 0**, no output |
| `npm run lint:a11y` | **exit 1**, 4 pre-existing errors, none in scope |
| Count gate ×2 | `total 3188 · failed 0 · pinned total 3164`, 60/60, exit 0 — **both runs identical** |
| Fork-arc characterization | **13 / 13 green** |
| `WORKFLOWS_PAGE_DIGEST` | `84e51f199b4b8992` · 65266 chars · 1180 lines |
| Classifier validation | `95a4c915:PhaseNodeCard.tsx` → **797 / 518 / 249 / 30** ✅ exact, first attempt |
| Page classification | **1180** = 614 comment + 518 code + 48 blank |
| Subtree classification | **3438** = 1725 comment + 1547 code + 166 blank |

## Figures that DISAGREED with RESEARCH / VALIDATION / the plan

Six, each with both numbers. Recording a correction is the point of this plan, not a criticism of
the document corrected.

| # | Upstream claim | Measured |
|---|---|---|
| **B-1** | Plan Task 2(c): validate the classifier against `95a4c915:frontend/src/components/workflows/library/WorkflowCard.tsx` | **That path does not exist at `95a4c915`** — `library/` was created in Phase 192. The known-good is `95a4c915:…/PhaseNodeCard.tsx`, which RESEARCH §2d and the CLAUDE.md ledger both actually name. Validated against that instead. |
| **B-2** | RESEARCH §2c, VALIDATION and the plan all say the fork arc is **12 cases** | **13.** The `:886` describe holds **four** `it(`s; RESEARCH's table lists three and omits **`:945`** (*"the card SAID so before the click…"*). 3+4+4+2 = 13. |
| **B-3** | VALIDATION: **two** suites report above their pins | **Four** — also `WorkflowBuilderPage.canvas.test.tsx` (+5) and `builderStore.test.ts` (+6). The four sum to exactly the **+24** total slack, so it is fully attributed with no unpinned residual. |
| **B-4** | RESEARCH §6d: *"Both must be green"* | **`npm run lint:a11y` exits 1** on a clean tree — 4 pre-existing errors, none in the library subtree or on the page. |
| **B-5** | `vitest-count-gate.cjs:77`: *"`BASELINE_TOTAL` now EQUALS the measured total… zero slack"* | **Stale** — 3164 pinned vs 3188 measured, **24 cases of slack**. This header is the origin of the C-5 conflation. |
| **B-6** | `WorkflowCard.tsx:119-120`: `PAGE 1407 → 1007`, `SUBTREE 1407 → 3182` | Stale, **and so are both CODE columns beside them**: page CODE `479` → measured **518**; subtree CODE `1499` → measured **1547**. Totals **1180** / **3438**. Plan 07 owes all four. |

**Reproduced exactly** (no correction needed): all eight suite pin counts and their line numbers,
the 223 five-suite subtotal, the 303 total, tsc 33, scoped eslint 0, the gate's `3188/0/3164` line,
RESEARCH §2d's `3438` subtree and `1180` page, and the ledger's `30 commits / 11 phases / 1180 L`
and `4 commits / 1 phase / 567 L`.

## Classifier validation result

**Exact on the first attempt — no fix was required.** `git show 95a4c915:…/PhaseNodeCard.tsx |
classify` → **797 total / 518 comment / 249 code / 30 blank**, matching 188.2's published
known-good digit for digit (and `wc -l` → 797). Only after that was any 192.1 figure trusted. The
classifier's full source is embedded in the artifact so a reader can re-run it.

The **digest transcription was validated the same way**, which the plan did not ask for but the
same logic demands: run against `frontend/src/lib/threadGroups.tsx` it reproduces the shipped
`THREAD_GROUPS_DIGEST = "4723f9885190e84a"` and `THREAD_GROUPS_LENGTH = 7144` exactly. A
hand-transcribed hash that has never reproduced a known value is not evidence either.

## Deviations from Plan

**One, and it was forced by measurement rather than chosen.**

**1. [Rule 3 - Blocking] The plan's classifier known-good path does not exist**
- **Found during:** Task 2(c)
- **Issue:** Task 2(c) instructs `git show 95a4c915:frontend/src/components/workflows/library/WorkflowCard.tsx`. `git ls-tree 95a4c915 --name-only frontend/src/components/workflows/library/` returns **empty** — the `library/` directory was created in Phase 192, well after this 188.2-era commit. The instruction as written cannot execute.
- **Fix:** Validated against `95a4c915:frontend/src/components/workflows/PhaseNodeCard.tsx`, which is the known-good that RESEARCH §2d line 383, the CLAUDE.md 188.2 ledger row and `WorkflowCard.tsx:116` all actually name, and which is what the `797/518/249/30` tuple describes. The plan's success criterion (`= 797/518/249/30`) is met unchanged.
- **Files modified:** none (planning artifact only)
- **Commit:** `bb3d6344`

No architectural decisions were needed; no packages were installed; no source file was touched.

## Verification

- `test -f .planning/phases/192.1-workflow-identity/192.1-BASELINE.md` → **EXISTS** (383 lines, min 60)
- `grep -c "pinned total"` → **3** · `grep -c "95a4c915"` → **5** · `grep -c "797"` → **4** · `grep -c "WORKFLOWS_PAGE_DIGEST"` → **3**
- `git status --porcelain -- frontend/ backend/ scripts/` → **empty**, after both tasks
- `git status --porcelain` (whole tree) → **empty** after each commit
- No STATE.md or ROADMAP.md modification

## Notes for later plans

- **The `failed` line is not the backstop on this machine.** Both gate runs read `failed 0`, but
  `GSD_VITEST_MAX_WORKERS=4` only *reduces* the flake (D-192-DEF-01 measured `0/0/1` across three
  capped runs). **The COUNT columns are the regression signal.** Two agreeing runs do not retire it.
- **Four above-pin suites are pre-existing slack.** Do not "fix" them and do not read them as a
  192.1 regression.
- **`npm run lint:a11y` is already RED.** The binding 192.1 lint baseline is the *scoped* eslint
  command (exit 0). Keep later plans' lint commands scoped identically.
- **`frontend/src/lib/api.workflows.test.ts` is outside BOTH gate knobs** and references
  `createWorkflowDraft`. D-22 forbids touching that throw site; if a plan does, that suite is
  ungated and it must be said out loud.
- **The digest is NOT a fence.** The page must change. It exists so Plan 03's SUMMARY can state the
  exact delta rather than estimate it.
- **No growth number is predicted anywhere in the artifact, deliberately.** 188.2 measured +67.1 %
  and Phase 192 measured +126.2 % against a +5…+16 % estimate — an ~8× miss. Measure at the end.

## Known Stubs

None. This plan writes no production code by design.

## Threat Flags

None. No trust boundary is crossed — the plan reads the repository and writes one planning
document. Zero packages installed; the digest and classifier scripts are throwaway Node builtins
run outside the app tsconfig, so neither adds a dependency nor a 34th type error.

## Self-Check: PASSED

- `FOUND: .planning/phases/192.1-workflow-identity/192.1-BASELINE.md`
- `FOUND: e3b31985` — docs(192.1-02): capture the pre-move suite/tsc/lint/gate baselines
- `FOUND: bb3d6344` — docs(192.1-02): the fork-arc set, the page digest, the validated classification
