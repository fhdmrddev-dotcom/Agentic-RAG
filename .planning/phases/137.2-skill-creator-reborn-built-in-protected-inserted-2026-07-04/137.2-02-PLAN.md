---
phase: 137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/087_skill_creator_reborn.sql
  - docs/DEPLOYMENT-LESSONS.md
  - docs/DEPLOYMENT-WORKFLOW.md
autonomous: true
requirements: [CREATE-01]
must_haves:
  truths:
    - "D-02: migration 087 is idempotent (re-apply converges) and SUPERSEDES migration 018's stale/absent skill-creator row `…0010` — same system user `…0001`, `is_system=true`, `is_global=true`, rewritten platform-native instructions"
    - "D-02 refresh nuance: the SKILL row uses `ON CONFLICT (id) DO UPDATE` (not `DO NOTHING`) so a stale 018 row on ANY environment is actually refreshed; the `auth.users` insert keeps `DO NOTHING` (never mutate the user) — RESEARCH Pitfall 1"
    - "D-03: the reborn instructions propose 2-3 eval cases as chat text and DIRECT the user to Skill Studio → Evals tab (existing CaseEditor) — NO new agent tool; the prose names only real tools (`search_documents`/`save_skill`/`load_skill`/`read_skill_file`/Python `execute_code`) and never claims subagents/browser/`claude -p`/non-Python execution (SEED-096 honesty)"
    - "The `is_system` column is added additively (`ADD COLUMN IF NOT EXISTS is_system boolean NOT NULL DEFAULT false`) so every pre-087 row is valid unchanged and the badge value is write-locked to this migration"
    - "CREATE-01 SC#1 deploy parity: because data seeds are NOT in schema-only `full-schema.sql`, migration 087 is documented as data-carrying in BOTH DEPLOYMENT-LESSONS.md A6 and DEPLOYMENT-WORKFLOW.md §5 so an operator applies it to cloud rather than assuming the schema dump covered it"
  artifacts:
    - path: "supabase/migrations/087_skill_creator_reborn.sql"
      provides: "is_system column ALTER + idempotent superseding upsert of the built-in skill-creator (id …0010, owner …0001) with reborn 7-step platform-native instructions"
      contains: "ADD COLUMN IF NOT EXISTS is_system boolean NOT NULL DEFAULT false"
    - path: "docs/DEPLOYMENT-LESSONS.md"
      provides: "A6 seed-migration list bumped 6->7, appends `087 skill_creator_reborn (supersedes 018's seed)`"
      contains: "087 skill_creator_reborn"
    - path: "docs/DEPLOYMENT-WORKFLOW.md"
      provides: "§5 parity row names 087's cloud seed-verification SELECT"
      contains: "087"
  key_links:
    - from: "supabase/migrations/087_skill_creator_reborn.sql"
      to: "public.skills row …0010 (owner …0001, is_global=true, is_enabled=true, is_system=true)"
      via: "INSERT … ON CONFLICT (id) DO UPDATE"
      pattern: "ON CONFLICT \\(id\\) DO UPDATE"
    - from: "the seeded row (is_global=true + is_enabled=true)"
      to: "agent_loop.py catalog injection (1197-1225)"
      via: "existing enabled-global-skill read — NO code change (additive, D-14-safe)"
      pattern: "skill-creator"
---

<objective>
Author migration 087 — the additive `is_system` column plus the idempotent,
superseding seed of the reborn `skill-creator` built-in — and record its cloud
deploy-parity in the two deployment docs. This is the data heart of the phase: one
column, one row, and the platform-native instructions that teach the agent to run
OUR skill lifecycle.

Purpose: migration 018's thin 3-question seed is gone from the live DB and was never
deploy-safe. 087 supersedes it with a 7-step interview -> RAG -> save -> test ->
improve -> publish loop grounded in the real toolset (SEED-101 scope + SEED-096
honesty), owned by the system user so it is read-only for everyone. Because data seeds
are excluded from `full-schema.sql`, the docs are the only signal telling the operator
to apply 087 to cloud.

Output: `supabase/migrations/087_skill_creator_reborn.sql` (new) + two doc edits. No
code, no endpoint, no runtime change. Live application of this file is Plan 04.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-RESEARCH.md
@.planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-PATTERNS.md
@.planning/seeds/SEED-101-skill-creator-native-builtin-protected.md

<content_brief>
The instructions prose is AUTHORED CONTENT (no code analog). Its shape is locked; wording is Claude's Discretion. Two authoritative sources:
- RESEARCH.md Code Example #7 — a strong, near-complete 7-step draft in imperative form with the honesty box. Use it as the starting draft.
- SEED-101 lines 119-167 — "What the reborn skill-creator IS" (the 7-step scope) + the Red lines (never claim capabilities the runtime lacks; seeded content is versioned via migration, not hand-edited).

The 7 steps: (1) Interview/intent-capture -> (2) Research with `search_documents` (our RAG differentiator) -> (3) Draft + `save_skill` with writing craft (imperative form, explain-the-why, pushy-but-honest description, generalize-don't-overfit, progressive disclosure = instructions + attached skill_files read via `read_skill_file`, Python-only scripts note) -> (4) Propose 2-3 eval cases as chat text and DIRECT the user to Skill Studio -> Evals tab / CaseEditor (D-03: no new tool) -> (5) Explain the with/without eval arms + honest verdict -> (6) Improve via the proposals loop (SI-01) + Trigger Tuner -> (7) Publish gate (GATE-01) is the finish line. End with a "What this runtime can and cannot do" honesty box. OMIT sub-agent / `claude -p` / browser / non-Python-execution language entirely (RESEARCH Pitfall 4).
</content_brief>

<migration_facts>
Verified live 2026-07-05 (PATTERNS.md + migrations read this session).
- Migration number is 087 (live `supabase/migrations/` tops out at 086_app_settings_harness_judge_model.sql).
- System user UUID: 00000000-0000-0000-0000-000000000001 (already in auth.users from 018). Skill id to REUSE: 00000000-0000-0000-0000-000000000010 (so 087 supersedes, never duplicates).
- Copy the `auth.users` idempotent insert VERBATIM from 018 (lines 9-32), keep `ON CONFLICT (id) DO NOTHING`.
- Header convention (086): lead with a comment block explaining the WHY, then the DDL. `ADD COLUMN IF NOT EXISTS … NOT NULL DEFAULT` shape (086:13-14).
- Use PostgreSQL dollar-quoting for the instructions body so the prose can contain raw apostrophes (018 had to double every ''). Biggest authoring-friction reducer.
- The skill INSERT column list: id, user_id, name, description, instructions, is_enabled, is_global, is_system. All three booleans true.
</migration_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 087 — is_system column + superseding skill-creator seed with reborn instructions</name>
  <files>supabase/migrations/087_skill_creator_reborn.sql</files>
  <read_first>
    - supabase/migrations/018_skill_creator_seed.sql (the seed pattern to reuse — auth.users insert lines 9-32; skills insert lines 34-64; same UUIDs)
    - supabase/migrations/086_app_settings_harness_judge_model.sql (the recent header/ALTER convention — comment-block-first + ADD COLUMN IF NOT EXISTS)
    - .planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-RESEARCH.md (Code Examples #1 = migration shape; Code Example #7 = instructions draft; Pitfall 1 = DO UPDATE vs DO NOTHING)
    - .planning/seeds/SEED-101-skill-creator-native-builtin-protected.md (lines 119-167 — the 7-step scope + red lines)
  </read_first>
  <action>
    Create `supabase/migrations/087_skill_creator_reborn.sql` with a leading comment
    block (why: supersedes 018's stale/absent seed; CREATE-01/SEED-101; idempotent;
    data-carrying — NOT captured by schema-only full-schema.sql; apply via SQL editor /
    psycopg2, never db push/reset), then three statements:
    (1) An additive ALTER: `ALTER TABLE public.skills ADD COLUMN IF NOT EXISTS is_system
    boolean NOT NULL DEFAULT false;`
    (2) The `auth.users` idempotent insert copied verbatim from 018 (same UUID `…0001`),
    keeping `ON CONFLICT (id) DO NOTHING`.
    (3) `INSERT INTO public.skills (id, user_id, name, description, instructions,
    is_enabled, is_global, is_system) VALUES (…0010, …0001, 'skill-creator', <a
    one-sentence pushy-but-honest description>, dollar-quoted instructions body, true,
    true, true)` with `ON CONFLICT (id) DO UPDATE SET user_id=EXCLUDED.user_id,
    name=EXCLUDED.name, description=EXCLUDED.description, instructions=EXCLUDED.instructions,
    is_enabled=EXCLUDED.is_enabled, is_global=EXCLUDED.is_global, is_system=EXCLUDED.is_system`.
    Use `DO UPDATE` (NOT 018's `DO NOTHING`) for the skill row so a stale `…0010` is truly
    refreshed on any env that still holds it (RESEARCH Pitfall 1); keep `DO NOTHING` for
    the auth.users row. Author the dollar-quoted instructions body as the reborn 7-step
    platform-native prose per the content_brief — imperative form, the writing-craft
    guidance, the D-03 hand-off to Skill Studio -> Evals (no new tool), and the honesty
    box naming only real tools. Reuse skill id `…0010` (do NOT mint a fresh UUID). Do NOT
    include the operator's private-copy rename here (that is Plan 04, dev-only). Do NOT
    touch full-schema.sql (Plan 04 regenerates it after applying).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && f=supabase/migrations/087_skill_creator_reborn.sql && test -f "$f" && grep -cF 'ADD COLUMN IF NOT EXISTS is_system boolean NOT NULL DEFAULT false' "$f" && grep -cF 'ON CONFLICT (id) DO UPDATE' "$f" && grep -cF 'ON CONFLICT (id) DO NOTHING' "$f" && grep -cF '00000000-0000-0000-0000-000000000010' "$f" && grep -cF 'search_documents' "$f" && grep -cF 'save_skill' "$f"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/087_skill_creator_reborn.sql` exists.
    - Contains `ADD COLUMN IF NOT EXISTS is_system boolean NOT NULL DEFAULT false` (grep -cF ≥ 1).
    - The skill INSERT uses `ON CONFLICT (id) DO UPDATE` (≥ 1) AND the auth.users insert keeps `ON CONFLICT (id) DO NOTHING` (≥ 1).
    - Reuses skill id `00000000-0000-0000-0000-000000000010` and owner `00000000-0000-0000-0000-000000000001`.
    - Instructions body is dollar-quoted and names the real tools `search_documents` and `save_skill` (grep -cF ≥ 1 each).
    - Manual source assertion at review (honesty): the prose contains a "What this runtime can and cannot do" honesty section, routes eval-case creation to Skill Studio -> Evals (D-03), and contains NO claim of sub-agent delegation, browser automation, `claude -p`, or non-Python script execution.
  </acceptance_criteria>
  <done>Migration 087 exists with the additive column and the superseding, honest, dollar-quoted seed.</done>
</task>

<task type="auto">
  <name>Task 2: Record migration 087 in the deploy-parity docs (CREATE-01 SC#1)</name>
  <files>docs/DEPLOYMENT-LESSONS.md, docs/DEPLOYMENT-WORKFLOW.md</files>
  <read_first>
    - docs/DEPLOYMENT-LESSONS.md (entry A6, lines 49-59 — the "6 seed migrations" list)
    - docs/DEPLOYMENT-WORKFLOW.md (§5 parity checklist, line 111 — the "A new app setting / seed row" row)
    - .planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-RESEARCH.md (Deploy Parity section — the exact two edits)
  </read_first>
  <action>
    Two surgical doc edits so the operator knows 087 is data-carrying (not covered by
    schema-only `full-schema.sql`):
    (1) DEPLOYMENT-LESSONS.md A6 (line ~57): change "**6 seed migrations**" -> "**7 seed
    migrations**" and append `087 skill_creator_reborn (supersedes 018's seed)` to the
    parenthetical list (010 app_settings, 018 skill_creator_seed, 053, 056
    workflow_definitions, 061 harness_seed_templates, 066 eval_coverage_seed), matching the
    existing comma-separated `NNN name` format exactly.
    (2) DEPLOYMENT-WORKFLOW.md §5, the "A new app setting / seed row" table row (line
    ~111): extend the right cell with an explicit named check for 087 — after deploy, verify
    `SELECT count(*) FROM public.skills WHERE id='00000000-0000-0000-0000-000000000010'
    AND is_system=true` returns 1 in cloud; if 0 (env bootstrapped from full-schema.sql),
    apply migration 087 via the SQL editor (idempotent — safe to re-run). Keep the table-cell
    markdown format; do not restructure the table.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -cF '7 seed migrations' docs/DEPLOYMENT-LESSONS.md && grep -cF '087 skill_creator_reborn' docs/DEPLOYMENT-LESSONS.md && grep -cF '087' docs/DEPLOYMENT-WORKFLOW.md</automated>
  </verify>
  <acceptance_criteria>
    - DEPLOYMENT-LESSONS.md A6 now reads "7 seed migrations" and lists `087 skill_creator_reborn` (grep -cF ≥ 1 each).
    - DEPLOYMENT-WORKFLOW.md §5 names migration 087 with the cloud verification SELECT on `id='…0010' AND is_system=true` (grep -cF '087' ≥ 1; manual read confirms the SELECT is present in the seed-row cell).
    - Neither doc's surrounding structure is otherwise changed.
  </acceptance_criteria>
  <done>Both deployment docs name migration 087 as a data-carrying seed with a cloud verification step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> DB | Migration 087 is the ONLY writer of `is_system` and of the seeded instructions. Trusted, versioned, reviewed. |
| seeded instructions -> agent context | The seed's `instructions` become agent system-prompt content via the existing catalog injection. This is authored static content, not user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.2-03 | Tampering (prompt) | seeded `instructions` body | mitigate | Instructions are static, authored content versioned via migration (SEED-101 red line: seeded content is versioned via migration, not hand-edited in the DB). The "What this runtime can and cannot do" honesty box prevents the agent from being taught to claim/attempt browser/CLI/non-Python capabilities the sandbox lacks (SEED-096). |
| T-137.2-02 | Spoofing / Elevation-of-trust | `is_system` value | mitigate | `is_system=true` is set EXCLUSIVELY by this migration (the additive column defaults false; only the seed row flips it). No route writes it — the badge cannot be spoofed. |
| T-137.2-01 | Tampering / Denial | seeded row ownership | mitigate | Owner is the system UUID `…0001` (from 018, unchanged) — never equals a real user, so the built-in is read-only/undeletable for everyone via the existing owner-scoping (proven in Plan 01 + verified live in Plan 04). |

_No package installs in this plan → no supply-chain (T-…-SC) threat; slopcheck N/A._
</threat_model>

<verification>
- `grep -cF 'ADD COLUMN IF NOT EXISTS is_system boolean NOT NULL DEFAULT false' supabase/migrations/087_skill_creator_reborn.sql` ≥ 1.
- `grep -cF 'ON CONFLICT (id) DO UPDATE' supabase/migrations/087_skill_creator_reborn.sql` ≥ 1 AND `grep -cF 'ON CONFLICT (id) DO NOTHING' …087….sql` ≥ 1.
- `grep -cF '087 skill_creator_reborn' docs/DEPLOYMENT-LESSONS.md` ≥ 1.
- Manual review of the instructions prose against the honesty red line (no subagent/browser/`claude -p`/non-Python claims; routes eval cases to Skill Studio -> Evals).
- Live application + row-content verification is Plan 04 (this plan only authors the file).
</verification>

<success_criteria>
- Migration 087 exists: additive `is_system` column + superseding `DO UPDATE` seed of the reborn skill-creator (D-02).
- The instructions are platform-native, honest, and hand eval-case creation to the Studio (D-03).
- Both deployment docs name 087 as data-carrying with a cloud verification step (CREATE-01 SC#1).
</success_criteria>

<output>
Create `.planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-02-SUMMARY.md` when done.
</output>
