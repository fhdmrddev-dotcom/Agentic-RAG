---
phase: 137.2
slug: skill-creator-reborn-built-in-protected-inserted-2026-07-04
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-05
---

# Phase 137.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Low-code, high-precision seed-and-signal phase (one migration, one output field,
> one query `.order()` change, one badge, one one-off rename, two doc edits) — not a
> feature build. SC#4's lived conversational walkthrough is the true acceptance bar;
> automated tests are a backstop for the mechanical edits around it.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Backend framework** | pytest (`asyncio_mode=auto`) — `backend/pytest.ini` (`testpaths = tests`) |
| **Frontend framework** | vitest — `frontend/vitest.config.ts` |
| **Backend skills tests** | `backend/tests/integration/test_skills.py` (mocked Supabase via `conftest._supabase`), `test_skill_catalog_note.py`, `test_agent_loop_catalog_override.py` |
| **Frontend skills tests** | `SkillFormDialog.test.tsx`, `SkillEvalSection.test.tsx` (no `SkillCard.test.tsx` today) |
| **Backend quick run** | `cd backend && python -m pytest tests/integration/test_skills.py -x` |
| **Backend full suite** | `cd backend && python -m pytest tests -q` |
| **Frontend full suite** | `cd frontend && npx vitest run` |
| **Estimated runtime** | backend quick ~5-10s · full suite ~60s · frontend ~20-40s (existing suite, not separately timed this phase) |

---

## Sampling Rate

- **After every task commit:** `cd backend && python -m pytest tests/integration/test_skills.py -x`
- **After every plan wave:** Backend full suite (`python -m pytest tests -q`) + `npx vitest run` (skills components).
- **Before `/gsd:verify-work`:** Full suites green + the SC#4 live conversational walkthrough.
- **Max feedback latency:** < 60 seconds.

---

## Per-Task Verification Map

> Task ID / Plan / Wave are TBD — assigned by the planner. Rows below are seeded from
> RESEARCH.md's Phase Requirements → Test Map; the planner maps each to a concrete task.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | CREATE-01 (seed) | T-137.2-01 | Built-in row present, `is_system=true`, `is_global=true`, owner `…0001`, non-thin (`DO UPDATE` refreshed) instructions | manual (live DB) | psycopg2 `:54322`: `SELECT is_system,is_global,user_id,length(instructions) FROM skills WHERE id='00000000-0000-0000-0000-000000000010'` | ❌ manual/UAT | ⬜ pending |
| TBD | TBD | TBD | CREATE-01 (response) | T-137.2-02 | `GET /skills` JSON carries `is_system`; never accepted on `SkillCreate`/`SkillUpdate` | unit | `pytest tests/integration/test_skills.py -k list -x` (assert key present) | ⚠️ extend (W0) | ⬜ pending |
| TBD | TBD | TBD | D-05 (order) | — | `list_skills` returns `is_system` rows first, then alphabetical | unit | `pytest tests/integration/test_skills.py -k order -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-01 (protection) | T-137.2-01 | Non-owner `update`/`delete`/`toggle` the built-in → 404/403 | unit | `pytest tests/integration/test_skills.py -k "owner or forbidden" -x` | ⚠️ extend (W0) | ⬜ pending |
| TBD | TBD | TBD | CREATE-01 (badge) | — | `SkillCard` renders "Built-in" when `is_system`; hides owner actions for system-owned skill | component | `npx vitest run SkillCard` | ❌ W0 (new file; optional — see note) | ⬜ pending |
| TBD | TBD | TBD | D-04 (rename / collision) | T-137.2-02 | Operator's private `skill-creator` copy renamed so `load_skill("skill-creator")` resolves to the built-in on the dev account | manual (live DB) | psycopg2 `:54322`: `SELECT name FROM skills WHERE id='7f504819-2ab6-4b12-8692-50e29696db54'` | ❌ manual/UAT | ⬜ pending |
| TBD | TBD | TBD | SC#4 (lived walkthrough) | — | "help me create a skill for X" reaches a saved, eval-tested skill using only the instructions; never claims subagent/browser/`claude -p`/non-Python execution | manual UAT (G-4) | Chrome MCP or operator-driven chat session | ❌ manual only | ⬜ pending |

*Status: ⬜ pending · ✅ green · ◻ manual (operator/live-key checkpoint) · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/tests/integration/test_skills.py` — add: (a) `list_skills` returns `is_system`-first ordering; (b) `SkillResponse` serializes `is_system`; (c) a non-owner mutation of the system-owned row returns 404/403.
- [ ] `frontend/src/components/skills/SkillCard.test.tsx` — NEW: "Built-in" pill renders on `is_system`; owner actions hidden when `user_id !== currentUserId`. *(Optional — the G-4 live walkthrough is the real acceptance bar; the planner may judge this component test low-value given the phase's small surface.)*
- [ ] No framework install needed (pytest + vitest already present).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Built-in skill row present in live DB with refreshed (not stale-018) instructions | CREATE-01 | Migration data can't be asserted by mocked unit tests | psycopg2 `:54322`: confirm row `…0010` has `is_system=true`, `is_global=true`, `user_id='00000000-0000-0000-0000-000000000001'`, and instructions length reflects the new 7-step content (not 018's thin 3-question text) |
| Operator's manual copy renamed | D-04 | Live data mutation on the dev DB, not code | psycopg2 `:54322`: confirm skill `7f504819-2ab6-4b12-8692-50e29696db54` name is no longer `skill-creator` |
| Cloud deploy parity documented | CREATE-01 SC#1 | Doc content, not testable code | Confirm `docs/DEPLOYMENT-LESSONS.md` A6 list includes migration 087; confirm `docs/DEPLOYMENT-WORKFLOW.md` §5 names the 087 seed's cloud verification step |
| **G-4 — lived conversational walkthrough** | SC#4 | Inherently a lived-experience, multi-turn conversational test — not automatable | Start a fresh chat, say "help me create a skill for X." Confirm the agent: (1) interviews before drafting, (2) calls `search_documents` to ground the draft, (3) calls `save_skill` and confirms it's saved, (4) proposes 2-3 eval cases as chat text and directs you to Skill Studio → Evals tab, (5) never claims sub-agent delegation, browser automation, `claude -p`, or non-Python script execution. Do this on at least one representative tool-capable provider (OpenAI/Anthropic/Google) — light cross-provider sanity glance recommended since the instructions are consumed by every provider's agent loop, though this phase does not itself touch streaming/agent-loop/provider-routing code (SC#10's full 4-axis matrix is not triggered by this change). |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
