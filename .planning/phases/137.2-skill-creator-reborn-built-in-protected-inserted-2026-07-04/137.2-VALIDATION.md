---
phase: 137.2
slug: skill-creator-reborn-built-in-protected-inserted-2026-07-04
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-05
updated: 2026-07-05
---

# Phase 137.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Low-code, high-precision seed-and-signal phase (one migration, one output field,
> one query `.order()` change, one badge, one one-off rename, two doc edits) — not a
> feature build. SC#4's lived conversational walkthrough is the true acceptance bar;
> automated tests are a backstop for the mechanical edits around it.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Backend framework** | pytest (`asyncio_mode=auto`) — `backend/pytest.ini` (`testpaths = tests`) |
| **Frontend framework** | vitest — `frontend/vitest.config.ts` |
| **Backend skills tests** | `backend/tests/integration/test_skills.py` (mocked Supabase via `conftest._supabase`), `test_skill_catalog_note.py`, `test_agent_loop_catalog_override.py` |
| **Frontend skills tests** | `SkillFormDialog.test.tsx`, `SkillEvalSection.test.tsx`, `SkillCard.test.tsx` (NEW — Plan 03) |
| **Backend quick run** | `cd backend && python -m pytest tests/integration/test_skills.py -x` |
| **Backend full suite** | `cd backend && python -m pytest tests -q` |
| **Frontend full suite** | `cd frontend && npx vitest run` |
| **Estimated runtime** | backend quick ~5-10s · full suite ~60s · frontend ~20-40s (existing suite, not separately timed this phase) |

---

## Sampling Rate

- **After every task commit:** `cd backend && python -m pytest tests/integration/test_skills.py -x`
- **After every plan wave:** Backend full suite (`python -m pytest tests -q`) + `npx vitest run` (skills components).
- **Before `/gsd:verify-work`:** Full suites green + the SC#4 live conversational walkthrough.
- **Max feedback latency:** < 60 seconds.

---

## Per-Task Verification Map

> Task ID / Plan / Wave assigned by the planner (2026-07-05). Each RESEARCH Phase
> Requirements → Test Map row is now mapped to a concrete plan task. SC#4 stays a
> manual UAT row (G-4) — NOT an automated plan task (CLAUDE.md UAT-scoreboard mandate).

| Threat Ref | Plan / Task | Wave | Requirement | Secure Behavior | Test Type | Automated Command | Status |
|------------|-------------|------|-------------|-----------------|-----------|-------------------|--------|
| T-137.2-01 | 137.2-04 / T1 (apply) | 2 | CREATE-01 (seed) | Built-in row present, `is_system=true`, `is_global=true`, owner `…0001`, non-thin (`DO UPDATE` refreshed) instructions | manual (live DB, checkpoint) | psycopg2 `:54322`: `SELECT is_system,is_global,user_id,length(instructions) FROM skills WHERE id='00000000-0000-0000-0000-000000000010'` (assert is_system/is_global true, owner …0001, len > 1500) | ⬜ pending |
| T-137.2-02 | 137.2-01 / T1 (field) + T3 (test) | 1 | CREATE-01 (response) | `GET /skills` JSON carries `is_system`; never accepted on `SkillCreate`/`SkillUpdate` | unit | `pytest tests/integration/test_skills.py -x` (assert key present; input models lack it) | ⬜ pending |
| — | 137.2-01 / T2 (order) + T3 (test) | 1 | D-05 (order) | `list_skills` returns `is_system` rows first, then alphabetical | unit | `pytest tests/integration/test_skills.py -x` (`order.assert_any_call("is_system", desc=True)`) | ⬜ pending |
| T-137.2-01 | 137.2-01 / T3 (test) | 1 | D-01 (protection) | Non-owner `update`/`delete`/`toggle` the built-in → 404/403 (empty-result miss) | unit | `pytest tests/integration/test_skills.py -x` | ⬜ pending |
| — | 137.2-03 / T2 (pill) + T3 (test) | 1 | CREATE-01 (badge) | `SkillCard` renders "Built-in" (xor "Global") when `is_system`; hides owner actions for system-owned skill | component | `npx vitest run src/components/skills/SkillCard.test.tsx` | ⬜ pending |
| T-137.2-04 | 137.2-04 / T3 (rename) | 2 | D-04 (rename / collision) | Operator's private `skill-creator` copy renamed so `load_skill("skill-creator")` resolves to the built-in on the dev account | manual (live DB) | psycopg2 `:54322`: `SELECT name FROM skills WHERE id='7f504819-2ab6-4b12-8692-50e29696db54'` == `skill-creator (imported reference)`; dev user owns 0 skills named `skill-creator` | ⬜ pending |
| T-137.2-03 | 137.2-02 / T1 (prose) | 1 | CREATE-01 (honesty) | Instructions name only real tools; no subagent/browser/`claude -p`/non-Python claims; route eval cases to Skill Studio → Evals (D-03) | source assertion + SC#4 | `grep -cF search_documents/save_skill …087….sql` ≥ 1 + manual prose review | ⬜ pending |
| — | manual UAT (post-phase) | — | SC#4 (lived walkthrough) | "help me create a skill for X" reaches a saved, eval-tested skill using only the instructions; never claims subagent/browser/`claude -p`/non-Python execution | manual UAT (G-4) | Chrome MCP or operator-driven chat session (see Manual-Only Verifications) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ◻ manual (operator/live-key checkpoint) · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> No separate Wave-0 scaffolding wave is needed: the test additions are authored WITH
> their production code inside the Wave-1 plans (mocked unit/component tests, not
> TDD-gated). Tracked here for completeness.

- [ ] `backend/tests/integration/test_skills.py` — **Plan 137.2-01 / Task 3** adds: (a) `list_skills` `.order("is_system", desc=True)` call assertion; (b) `SkillResponse` serializes `is_system`; (c) a non-owner mutation of the system-owned row returns 404/403; plus the `is_system=False` kwarg on `_skill_row`.
- [ ] `frontend/src/components/skills/SkillCard.test.tsx` — **Plan 137.2-03 / Task 3** (NEW): "Built-in" pill renders on `is_system` (and "Global" on a non-system global skill); owner actions hidden when `user_id !== currentUserId`. Authored fresh (PublishGateDialog.test.tsx style) — not leaning on the known-rotted vitest siblings.
- [ ] No framework install needed (pytest + vitest already present).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Built-in skill row present in live DB with refreshed (not stale-018) instructions | CREATE-01 | Migration data can't be asserted by mocked unit tests | **Plan 137.2-04 / Task 1** (blocking checkpoint). psycopg2 `:54322`: confirm row `…0010` has `is_system=true`, `is_global=true`, `user_id='00000000-0000-0000-0000-000000000001'`, and instructions length reflects the new 7-step content (> 1500, not 018's thin ~900-char text) |
| Operator's manual copy renamed | D-04 | Live data mutation on the dev DB, not code | **Plan 137.2-04 / Task 3**. psycopg2 `:54322`: confirm skill `7f504819-2ab6-4b12-8692-50e29696db54` name is `skill-creator (imported reference)` and the dev user owns no skill named `skill-creator` |
| Cloud deploy parity documented | CREATE-01 SC#1 | Doc content, not testable code | **Plan 137.2-02 / Task 2**. Confirm `docs/DEPLOYMENT-LESSONS.md` A6 list says "7 seed migrations" and includes `087 skill_creator_reborn`; confirm `docs/DEPLOYMENT-WORKFLOW.md` §5 names the 087 seed's cloud verification step |
| **G-4 — lived conversational walkthrough** | SC#4 | Inherently a lived-experience, multi-turn conversational test — not automatable | Runs AFTER Plan 04 (built-in live + operator copy renamed). Start a fresh chat, say "help me create a skill for X." Confirm the agent: (1) interviews before drafting, (2) calls `search_documents` to ground the draft, (3) calls `save_skill` and confirms it's saved, (4) proposes 2-3 eval cases as chat text and directs you to Skill Studio → Evals tab, (5) never claims sub-agent delegation, browser automation, `claude -p`, or non-Python script execution. Do this on at least one representative tool-capable provider (OpenAI/Anthropic/Google) — light cross-provider sanity glance recommended since the instructions are consumed by every provider's agent loop, though this phase does not itself touch streaming/agent-loop/provider-routing code (SC#10's full 4-axis matrix is not triggered by this change). |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (SC#4 is the one documented manual-only UAT, G-4)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (folded into Plan 01 T3 + Plan 03 T3)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved (planner, 2026-07-05) — every RESEARCH test-map row mapped to a concrete plan task; SC#4 retained as manual UAT per the CLAUDE.md UAT-scoreboard mandate.
