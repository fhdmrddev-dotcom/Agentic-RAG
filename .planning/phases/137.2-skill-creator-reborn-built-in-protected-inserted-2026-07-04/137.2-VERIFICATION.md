---
phase: 137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04
verified: 2026-07-05T06:08:02Z
status: human_needed
score: 8/9 must-haves verified (1 is an inherently-manual UAT item, SC#4)
overrides_applied: 0
human_verification:
  - test: "Live conversational walkthrough — 'help me create a skill for X' (SC#4 / G-4)"
    expected: "Agent (1) interviews before drafting, (2) calls search_documents to ground the draft, (3) calls save_skill and confirms it's saved, (4) proposes 2-3 eval cases as chat text and directs the user to Skill Studio -> Evals tab, (5) never claims sub-agent delegation, browser automation, `claude -p`, or non-Python script execution."
    why_human: "Inherently a lived, multi-turn conversational experience test — not automatable. Explicitly documented as manual-only in 137.2-VALIDATION.md (G-4); has not yet been run. The dev-account name collision that would have made this test the WRONG skill is already resolved (verified live), so the walkthrough is unblocked and ready to run."
---

# Phase 137.2: Skill Creator Reborn — Built-in + Protected — Verification Report

**Phase Goal:** Every user (local + cloud) has a read-only, undeletable, "Built-in"-badged skill-creator whose platform-native instructions run our full loop: interview → RAG research → save_skill → eval cases → eval → proposals/tuner → publish gate
**Verified:** 2026-07-05T06:08:02Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Method

This report is built from goal-backward, adversarial verification: every claim below was checked directly against the current codebase and the live local Supabase DB, not read off SUMMARY.md. Concretely, I:
- Read all 4 PLAN.md / SUMMARY.md files, the REVIEW.md, VALIDATION.md, CONTEXT.md, RESEARCH.md, ROADMAP.md phase entry, and REQUIREMENTS.md.
- Read the actual current source of every touched file (`skill.py`, `skills.py`, `test_skills.py`, migration 087, `types/index.ts`, `SkillCard.tsx`, `SkillCard.test.tsx`, `full-schema.sql`, `tool_dispatcher.py`).
- Ran `backend/tests/integration/test_skills.py` myself (16/16 passed) and a Pydantic isolation probe (confirmed `is_system` cannot be smuggled through `SkillUpdate`).
- Queried the live local Supabase DB directly via psycopg2 (independent of the SUMMARY's claimed query results) to confirm the seeded row, the ordering behavior, and the operator-copy rename.
- Diffed `39de3eed` (the commit immediately before this phase's execution) against `HEAD` to get the authoritative, complete list of files this phase touched (22 files) — used to verify the "additive-only / shared-path-untouched" claim (SC#5) directly, not by trusting the SUMMARY.
- Temporarily reverted `skill.py`/`skills.py` to their pre-phase (`39de3eed`) state and re-ran the two backend test files that show failures, to prove those 13 failures are pre-existing rot and NOT a regression introduced by this phase (then restored the working tree exactly, verified byte-identical via `git diff`, and dropped the temporary stash).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | **[SC#1]** An idempotent seed migration installs the `skill-creator` skill (system-user-owned, `is_global=true`), superseding migration 018's stale content; the seed reaches cloud via a documented deploy-parity entry | VERIFIED | `supabase/migrations/087_skill_creator_reborn.sql` exists: additive `ADD COLUMN IF NOT EXISTS is_system boolean NOT NULL DEFAULT false`; `auth.users` insert keeps `ON CONFLICT (id) DO NOTHING`; skill row uses `ON CONFLICT (id) DO UPDATE SET ...` (all 7 EXCLUDED columns) so a stale row is truly refreshed, not skipped. `docs/DEPLOYMENT-LESSONS.md` A6 now reads "7 seed migrations" and lists `087 skill_creator_reborn (supersedes 018's seed)` (grep-confirmed). `docs/DEPLOYMENT-WORKFLOW.md` §5 names migration 087 with an explicit cloud-verification SELECT on `id='…0010' AND is_system=true` (grep-confirmed). |
| 2 | **[SC#2a]** The built-in skill is read-only/undeletable through all app mutation paths for non-owners | VERIFIED | Read every mutation route (`update_skill`, `delete_skill`, `toggle_enabled`, `toggle_global`) — all filter `.eq("user_id", current_user["id"])`; the system owner `…0001` never matches a real user. `delete_skill` was HARDENED in this phase (WR-01 from code review): it now checks `if not result.data: raise HTTPException(404, ...)` instead of unconditionally returning 204 — closing a false-success gap the review found. 4 dedicated `TestSystemSkillProtection` tests (update→404, toggle-enabled→403, toggle-global→403, delete→404) all pass; I ran the full `test_skills.py` myself: **16/16 passed**. |
| 3 | **[SC#2b]** The built-in renders a "Built-in" badge in the Skills UI, and the badge is unspoofable (backend-controlled, output-only) | VERIFIED | `SkillResponse.is_system: bool = False` exists ONLY on the response model (`backend/app/models/skill.py:33`); confirmed via direct Pydantic probe that `SkillCreate`/`SkillUpdate` lack the field and silently drop it if smuggled in a request body. `frontend/src/types/index.ts:483` adds `is_system` to `Skill` only (not `SkillCreate`/`SkillUpdate`). `SkillCard.tsx:106-114` renders a ternary — "Built-in" (trust-tint) when `is_system`, else "Global" when `is_global`, never both. Full data-flow traced: `GET /skills` → `listSkills()` (raw `res.json()`, no stripping) → `useSkills` state → `SkillsPage.tsx:149` `<SkillCard skill={skill}>` — no hollow/hardcoded prop anywhere in the chain. |
| 4 | **[D-05]** `list_skills` pins `is_system` rows to the top (is_system DESC, then name ASC) | VERIFIED | Source: `.order("is_system", desc=True).order("name")` (`skills.py:151`, the old bare `.order("name")` is gone, not duplicated). Test asserts the actual PostgREST call. **Independently confirmed live** via psycopg2: `SELECT name, is_system FROM skills ORDER BY is_system DESC, name ASC` returns `('skill-creator', True)` first, then the rest alphabetically. |
| 5 | **[SC#3]** Instructions are platform-native per SEED-101's locked 7-step scope and never claim capabilities the runtime lacks | VERIFIED | Read the live migration's dollar-quoted instructions body in full: all 7 steps present (interview → `search_documents` RAG → `save_skill` draft with imperative/explain-why/generalize/pushy-honest-description/progressive-disclosure craft → propose 2-3 eval cases + explicit hand-off to "Skill Studio → Evals" with "There is no separate tool for this" (D-03, no new tool) → honest with/without-skill judge explanation → improve via proposals loop + Trigger Tuner → publish gate). Ends with a "What this runtime can and cannot do" honesty box naming only real tools (`search_documents`, `save_skill`, `load_skill`, `read_skill_file`, `execute_code`) and explicitly states "You do NOT have a browser-automation tool… cannot shell out to external CLIs, and do not orchestrate separate eval 'agents.'" No mention of sub-agents/`claude -p` anywhere in the seeded text. |
| 6 | **[SC#4]** A live conversational walkthrough reaches a saved, eval-tested skill using only what the instructions teach | ⚠ UNCERTAIN — human verification required | Explicitly scoped as manual-only in `137.2-VALIDATION.md` (G-4) — not a plan task, not automatable. Per the additional context provided for this verification, this walkthrough has not yet been run with the user. The blocking prerequisite (dev-account name collision) IS resolved (see Truth 9), so the walkthrough is unblocked and ready. See Human Verification Required below. |
| 7 | **[SC#5 / D-14]** The shipped 132-137 surfaces and the shared agent-loop/provider path are untouched (additive-only) | VERIFIED | `git diff 39de3eed..HEAD --stat` shows exactly 22 files changed, ALL inside `.planning/`, `backend/app/api/skills.py`, `backend/app/models/skill.py`, `backend/tests/integration/test_skills.py`, `docs/DEPLOYMENT-*.md`, `frontend/src/{types/index.ts, components/skills/*}`, and `supabase/{migrations/087_*, full-schema.sql}`. Zero touches to `agent_loop.py`, `openai_service.py`, `anthropic_service.py`, `google_service.py`, `threads.py`, `tool_dispatcher.py`, `StreamsProvider.tsx`, or any chat/panel component. Independently ran the 7 catalog-injection tests (`test_skill_catalog_note.py`, `test_agent_loop_catalog_override.py`, `test_115_catalog.py`) — all 7 pass. |
| 8 | **[Plan 04 / CREATE-01 SC#1]** Migration 087 is actually APPLIED to the live local DB, and the `DO UPDATE` refresh genuinely took effect (not a silent no-op against 018's thin content) | VERIFIED | Independently queried the live local Supabase DB myself (not trusting the SUMMARY's numbers): `is_system=True, is_global=True, is_enabled=True, user_id='00000000-0000-0000-0000-000000000001', length(instructions)=4376, name='skill-creator'` for row `…0010`. 4376 chars is unambiguously the reborn 7-step content, not 018's ~900-char seed. `information_schema.columns` confirms `is_system boolean NOT NULL DEFAULT false`. `supabase/full-schema.sql:1083` contains the column; `grep -c '00000000-0000-0000-0000-000000000010' full-schema.sql` = 0 (seed row correctly absent from the schema-only dump, as designed). |
| 9 | **[D-04]** The operator's private manual copy is renamed (not deleted); no dev-account name collision remains | VERIFIED | Independently queried live: skill `7f504819-2ab6-4b12-8692-50e29696db54` now has `name='skill-creator (imported reference)'`, still owned by `d8a54002-…`, still `is_global=false` (not deleted, still private). `SELECT count(*) FROM skills WHERE name='skill-creator' AND user_id='d8a54002-…'` = 0. `SELECT ... WHERE name='skill-creator'` returns exactly ONE row total (the built-in, `…0010`). |

**Score:** 8/9 truths verified programmatically; 1/9 (SC#4) is an explicitly-manual, not-yet-run acceptance test.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/app/models/skill.py` | `SkillResponse.is_system: bool = False`; `SkillCreate`/`SkillUpdate` unchanged | ✓ VERIFIED | Line 33, with load-bearing default + explanatory comment. Confirmed via direct Pydantic model-field introspection (not just grep). |
| `backend/app/api/skills.py` | `list_skills` two-column order; owner-scoped mutation routes unchanged except the WR-01 delete fix | ✓ VERIFIED | Line 151 order chain; `delete_skill` (374-383) now raises 404 on empty result — the ONLY other change in the file (confirmed via full diff against pre-phase commit). |
| `backend/tests/integration/test_skills.py` | Wave 0 tests (badge serialize, order-call, non-owner protection) + `is_system` kwarg on `_skill_row` | ✓ VERIFIED | 334 lines, 16 tests total (was 12 pre-phase + 4 new: serialize, order-first, +2 `TestSystemSkillProtection` from Plan 01, +2 more from the WR-01 fix commit). Ran the suite myself: **16 passed**. |
| `supabase/migrations/087_skill_creator_reborn.sql` | Additive column + superseding upsert seed with reborn instructions | ✓ VERIFIED | 130 lines. Content matches every plan/research claim exactly (read in full). Applied live (see Truth 8). |
| `docs/DEPLOYMENT-LESSONS.md` | A6 bumped 6→7 seed migrations, appends `087 skill_creator_reborn` | ✓ VERIFIED | Confirmed by direct read of A6. |
| `docs/DEPLOYMENT-WORKFLOW.md` | §5 parity row names 087's cloud-verification SELECT | ✓ VERIFIED | Confirmed by direct read of §5's seed-row table cell. |
| `frontend/src/types/index.ts` | `Skill.is_system: boolean`; `SkillCreate`/`SkillUpdate` unchanged | ✓ VERIFIED | Line 483, with explanatory comment; `SkillCreate`(496-501)/`SkillUpdate`(503-507) confirmed to lack the field. |
| `frontend/src/components/skills/SkillCard.tsx` | Built-in/Global ternary pill | ✓ VERIFIED | Lines 106-114; `isOwner` (line 46) and the owner-action block (198-271) are byte-unchanged — no new `is_system`-aware guard logic added, confirmed via diff. |
| `frontend/src/components/skills/SkillCard.test.tsx` | NEW: Built-in pill + owner-action hiding tests | ✓ VERIFIED (static trace) — execution not independently reproduced, see note below | 124 lines, 4 `it()` blocks with real `render`/`screen`/`expect` assertions (not stubs). I traced each assertion against the actual `SkillCard.tsx` conditional logic by hand and they match exactly. Could NOT execute `vitest` myself in this session — see "Frontend Execution Note" below. |
| `supabase/full-schema.sql` | Regenerated, captures `is_system` column, NOT the seed row | ✓ VERIFIED | Line 1083 `is_system boolean DEFAULT false NOT NULL`; seed row id count = 0 (grep). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `list_skills` | `SkillResponse.is_system` | `response_model=list[SkillResponse]` serialization | WIRED | Confirmed end-to-end: DB column → `select("*")` → Pydantic model → JSON. |
| `test_skills.py` | `list_skills` `.order()` call | `mock_builder.order.assert_any_call` | WIRED | Test passes; live psycopg2 `ORDER BY is_system DESC, name ASC` independently confirms real sort behavior. |
| Migration 087 | `public.skills` row `…0010` | `INSERT … ON CONFLICT (id) DO UPDATE` | WIRED | Live row confirmed with refreshed content (length 4376). |
| Seeded row (`is_global`+`is_enabled`) | `agent_loop.py` catalog injection | Existing enabled-global-skill read, zero code change | WIRED | `agent_loop.py` appears nowhere in the phase's diff; 7 catalog-injection tests pass independently. |
| `SkillCard.tsx` | `Skill.is_system` (from `GET /skills`) | `skill.is_system ? Built-in : is_global && Global` | WIRED | Full chain traced: `api.ts:listSkills` → `useSkills.ts` (raw state, no transform) → `SkillsPage.tsx:149` (`skill={skill}`, no field stripping). |
| `load_skill("skill-creator")` (dev account) | the built-in row `…0010` (not the renamed private copy) | Name uniqueness after the D-04 rename | WIRED (for the one account exercised) | Live-confirmed: dev user owns 0 skills named `skill-creator`; exactly 1 row named `skill-creator` exists platform-wide. **General-case caveat:** `tool_dispatcher.py:665-677` (`_handle_load_skill`) still resolves name ties with `.order("is_global")` ascending (owned-before-global) — UNCHANGED by this phase. Any OTHER user who creates/saves/imports a skill literally named `skill-creator` in the future would silently shadow the built-in for themselves at `load_skill` time. This is a real, open, but self-contained (single-user-blast-radius) gap — filed as `SEED-102` with a concrete re-open trigger, and explicitly classified by the phase's own code review as a Warning, not a Blocker. See Anti-Patterns below. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `SkillCard.tsx` Built-in pill | `skill.is_system` (prop) | `GET /skills` → `SkillResponse` → `listSkills()` → `useSkills` state → `SkillsPage.tsx` map, unmodified | Yes — live DB value, not hardcoded/hollow | ✓ FLOWING |
| `list_skills` ordering | `is_system` column read server-side | Live Postgres `skills` table via PostgREST `.order()` chain | Yes — independently confirmed via direct SQL query returning the built-in first | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Backend skills suite is green | `pytest tests/integration/test_skills.py -q` (run by me) | `16 passed` | ✓ PASS |
| `is_system` is genuinely output-only (Pydantic-level) | `python -c "...SkillResponse/SkillCreate/SkillUpdate.model_fields..."` (run by me) | `OK: is_system output-only field confirmed` | ✓ PASS |
| A smuggled `is_system=True` in a PATCH body is dropped | `SkillUpdate.model_validate({'name':'x','is_system':True})` (run by me) | `model_dump: {'name': 'x'}`, `hasattr is_system` = False | ✓ PASS |
| Live built-in row state | psycopg2 SELECT on `…0010` (run by me) | `(True, True, True, '…0001', 4376, 'skill-creator')` | ✓ PASS |
| Live rename + collision check | psycopg2 SELECT on `7f504819…` + count query (run by me) | renamed correctly; 0 owned collisions; exactly 1 global row | ✓ PASS |
| Live is_system-first ordering | `SELECT name, is_system FROM skills ORDER BY is_system DESC, name ASC` (run by me) | `skill-creator` (True) first, rest alphabetical | ✓ PASS |
| No regression in adjacent skill tests | Reverted `skill.py`/`skills.py` to pre-phase commit `39de3eed`, re-ran `test_threads_skills.py` + `test_skills_lint.py` (run by me), then restored working tree exactly | Same 13 failures at baseline AND at HEAD — confirmed pre-existing, not a regression (matches REVIEW.md's claim, independently reproduced) | ✓ PASS |
| Catalog-injection tests unaffected | `pytest tests/unit/test_skill_catalog_note.py tests/test_agent_loop_catalog_override.py tests/integration/test_115_catalog.py -q` (run by me) | `7 passed` | ✓ PASS |
| Frontend type-check / component test | `tsc -b` / `vitest run SkillCard.test.tsx` | Could not execute — see note below | ? SKIP (environment) |

**Frontend Execution Note:** This verification session's `frontend/node_modules` is missing entire package trees (`@radix-ui/*` — 0 entries; `node_modules/.bin` — 0 entries), producing ~100 unrelated `Cannot find module` / pre-existing type errors when `tsc -b` is run directly via `node node_modules/typescript/bin/tsc -b` (the `.bin/tsc` shim itself doesn't exist). None of those errors touch `SkillCard.tsx`, `SkillCard.test.tsx`, or `types/index.ts`. This matches the code review's own disclosed caveat ("this environment's `frontend/node_modules` has a broken native binding… I could not actually execute `vitest`") and Plan 03's SUMMARY (the executor needed a directory-junction workaround in its worktree to get a working `node_modules`). This is a pre-existing, environment-level limitation, not a defect introduced by this phase — I verified the phase's frontend logic correctness by direct, careful source-reading instead (see Truth 3 and the SkillCard.test.tsx artifact row).

### Probe Execution

Step 7c: SKIPPED (no `scripts/*/tests/probe-*.sh` convention exists in this project, and neither the plans nor SUMMARYs declare any probe scripts for this phase).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|--------------|--------|----------|
| CREATE-01 | 137.2-01, 137.2-02, 137.2-03, 137.2-04 (all 4 plans declare it) | Built-in `skill-creator` skill for every user — seeded, read-only/undeletable, "Built-in" badged, deploy-safe, platform-native instructions, never claims capabilities the runtime lacks | ✓ SATISFIED | Truths 1-9 above collectively satisfy every clause of CREATE-01's text in `REQUIREMENTS.md` line 39, except the always-manual SC#4 walkthrough (pending human execution, not a satisfaction gap). `REQUIREMENTS.md`'s checkbox and Traceability-table "Pending" status are administrative bookkeeping the orchestrator updates post-verification — not evidence of a gap. |

No orphaned requirements: `REQUIREMENTS.md` line 85 maps only `CREATE-01` to Phase 137.2, and all 4 plans declare exactly that ID in frontmatter.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `backend/app/services/tool_dispatcher.py` | 665-677 (`_handle_load_skill`) | Name-collision tie-break (`.order("is_global")` ascending, owned-before-global) is UNCHANGED by this phase — the general case (any future user creating/saving/importing a skill literally named `skill-creator`) can still silently shadow the built-in for themselves at `load_skill` runtime | ⚠ WARNING | Self-contained blast radius (a user can only shadow the built-in for their own sessions, per the code review's own analysis). The ONE known instance (the dev/test account) IS fixed and independently verified (Truth 9). The general case is tracked as `SEED-102` with a concrete re-open trigger and two named candidate fixes. This was explicitly classified by the phase's own code review (137.2-REVIEW.md, WR-02) as a Warning, not a Blocker, and correctly scoped out of this phase (the fix touches `tool_dispatcher.py`/`skills.py` beyond the phase's declared file list). Not treated as a phase gap — it is a deliberately deferred, already-tracked residual risk. |
| `.planning/phases/137.2-.../137.2-VALIDATION.md` | frontmatter `wave_0_complete: false` | Stale relative to the document's own Sign-Off checklist, which marks Wave 0 items as covered (folded into Plan 01/03 Task 3) | ℹ INFO | Cosmetic documentation inconsistency only — the actual Wave 0 tests exist and pass (independently confirmed). No functional impact. |

No debt markers (`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`) found in any file this phase touched (grep-swept `skill.py`, `skills.py`, migration 087, `types/index.ts`, `SkillCard.tsx`, `SkillCard.test.tsx`, `test_skills.py` — all clean).

### Human Verification Required

#### 1. Live conversational walkthrough (SC#4 / G-4)

**Test:** Start a fresh chat on a tool-capable provider (OpenAI, Anthropic, or Google — pick one to start; a light second-provider glance is recommended since the instructions are consumed by every provider's agent loop). Say something like "help me create a skill for summarizing project briefs" (or any real task).

**Expected:** The agent (1) interviews before drafting (asks about the task, inputs/outputs, "good" criteria, trigger condition) rather than jumping straight to a draft; (2) calls `search_documents` at some point to ground the draft in the user's actual knowledge base; (3) calls `save_skill` and confirms to the user that it saved; (4) proposes 2-3 concrete eval cases as chat text and explicitly directs the user to open **Skill Studio → Evals** to add them (never claims to create an eval case itself via a tool); (5) at no point claims it spawned a sub-agent, used a browser, shelled out to `claude -p`, or ran a non-Python script.

**Why human:** Inherently a lived, multi-turn conversational-quality test — not mechanically verifiable by grep or a unit test. This is `137.2-VALIDATION.md`'s own designated G-4 manual-only row, and per this task's provided context, it has not yet been run. The one concrete blocker that would have made this test exercise the WRONG content (the dev account's private-copy name collision) is confirmed resolved live, so the walkthrough is unblocked and ready to run now.

### Gaps Summary

No must-have FAILED. All 8 programmatically-checkable truths (seed migration + idempotency + deploy-parity docs, mutation-path protection incl. the in-phase WR-01 hardening, the unspoofable Built-in badge with full data-flow wiring, the is_system-first ordering, the platform-native honest instructions, the additive/shared-path-untouched claim, the live-applied migration with a confirmed real refresh, and the D-04 rename with a live-confirmed resolved collision) are backed by direct, independently-reproduced evidence — source reads, a live-run test suite, live psycopg2 queries against the actual local DB, and a git-diff-based regression check — not by trusting SUMMARY.md's narrative.

The 9th truth (SC#4, the lived conversational walkthrough) is not a gap in execution — it was always scoped as the phase's manual acceptance bar (G-4) and simply has not been run yet. That is the only reason this report resolves to `human_needed` rather than `passed`.

One residual, already-tracked risk is worth carrying forward explicitly: `tool_dispatcher.py`'s `_handle_load_skill` name-collision tie-break still favors an owned skill over the built-in on a name tie, in the general case (beyond the one dev account this phase fixed). This is filed as `SEED-102`, was surfaced and classified (Warning, not Blocker) by the phase's own code review, and is not a blocker to shipping this phase — but it should stay visible rather than quietly disappear once this phase's planning folder is archived.

---

*Verified: 2026-07-05T06:08:02Z*
*Verifier: Claude (gsd-verifier)*
