---
phase: 137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04
plan: 04
type: execute
wave: 2
depends_on: ["137.2-02"]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [CREATE-01]
must_haves:
  truths:
    - "CREATE-01 SC#1: migration 087 is APPLIED to the live local Supabase DB (via SQL editor paste / psycopg2 :54322, never db push/reset), so the `is_system` column and the seeded built-in row `…0010` actually exist live"
    - "The seed truly landed and the `DO UPDATE` refresh took effect (not a silent `DO NOTHING` no-op): `SELECT is_system,is_global,user_id,length(instructions) FROM skills WHERE id='…0010'` returns is_system=true, is_global=true, user_id='…0001', and a non-trivial instructions length (the reborn 7-step content, not 018's thin ~900-char text)"
    - "`full-schema.sql` is regenerated (no --reset) so it captures the new `is_system` COLUMN — and by design does NOT contain the seed ROW (schema-only dump); that data-parity gap is what Plan 02's docs cover"
    - "D-04: the operator's private manual copy `7f504819-2ab6-4b12-8692-50e29696db54` (owner `d8a54002-…`) is RENAMED (not deleted) to `skill-creator (imported reference)` so `load_skill(\"skill-creator\")` resolves to the built-in on the dev account (name-collision that would otherwise shadow the built-in — RESEARCH Pitfall 3), before the SC#4 walkthrough"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated schema-only dump capturing the is_system column (NOT the seed row — expected)"
      contains: "is_system"
  key_links:
    - from: "migration 087 (authored in Plan 02)"
      to: "live public.skills (column + row …0010)"
      via: "SQL editor paste / psycopg2 :54322 apply"
      pattern: "is_system"
    - from: "load_skill(\"skill-creator\") on the dev account"
      to: "the reborn built-in row …0010 (not the renamed private copy)"
      via: "name uniqueness after the D-04 rename (tool_dispatcher.py:665-677 prefers owned-over-global on a tie)"
      pattern: "skill-creator"
---

<objective>
Make the built-in real: apply migration 087 to the live local Supabase DB, regenerate
`full-schema.sql`, and perform the one-off D-04 rename of the operator's private
skill-creator copy so it can't shadow the built-in. This is the gated Wave-2 step that
turns the authored migration (Plan 02) into live state.

Purpose: type/build checks alone can't prove a migration was applied — they pass even
if the live DB never got the column or the seed row. This plan closes CREATE-01 SC#1
(the seed exists live + the `DO UPDATE` refresh actually took effect) and removes the
dev-account name collision that would otherwise make the SC#4 walkthrough test the
wrong (runtime-inert) instructions.

Output: migration 087 applied live, `full-schema.sql` regenerated + committed, and the
operator's manual copy renamed. This unblocks the SC#4 lived walkthrough (manual UAT in
VALIDATION.md — NOT a task here).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-RESEARCH.md
@.planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-PATTERNS.md
@supabase/migrations/087_skill_creator_reborn.sql

<apply_facts>
PROJECT-MANDATED migration discipline (CLAUDE.md — this OVERRIDES generic conventions):
- Apply the migration by pasting the SQL into the local Supabase Studio SQL editor OR by executing it via psycopg2 against localhost:54322. NEVER `supabase db push` / `supabase db reset` (both wipe/diverge preserved local dev data).
- After applying, regenerate the artifact: `bash scripts/regenerate-full-schema.sh` (DEFAULT no-reset mode — dumps the live DB schema only, no data loss). Do NOT pass `--reset`.
- Commit both the migration file (already committed by Plan 02) and the regenerated `full-schema.sql`.

Local Postgres connection for psycopg2 verification/queries (MEMORY reference_evidence_tools_inventory): host `localhost`, port `54322`, db `postgres`, user `postgres`, password `postgres`. Run any psycopg2 snippet from the scratchpad dir — NEVER write scratch `.py` into `backend/` (uvicorn --reload watches it — MEMORY feedback_no_scratch_in_watched_tree).

Live target rows (verified live 2026-07-05): built-in skill id `00000000-0000-0000-0000-000000000010`, system owner `00000000-0000-0000-0000-000000000001`; operator's private copy id `7f504819-2ab6-4b12-8692-50e29696db54`, owner `d8a54002-6a29-4b88-b918-cff2aa4a06d5` (private, is_global=false).
</apply_facts>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration 087 to the live local Supabase DB</name>
  <files>supabase/migrations/087_skill_creator_reborn.sql (applied, not modified)</files>
  <read_first>
    - supabase/migrations/087_skill_creator_reborn.sql (the exact SQL to apply — read it in full before applying)
    - CLAUDE.md (the migration-discipline rule — SQL editor / psycopg2 only, never db push/reset)
  </read_first>
  <what-built>
    Migration 087 (authored in Plan 02) adds the additive `is_system` column and upserts
    the reborn skill-creator built-in (id `…0010`, owner `…0001`, is_global/is_enabled/
    is_system=true). It is NOT yet applied to the live DB — this step applies it.
  </what-built>
  <action>
    Apply migration 087 to the LIVE local Supabase DB using the project-mandated path.
    Preferred (per CLAUDE.md): the operator pastes the full contents of
    `supabase/migrations/087_skill_creator_reborn.sql` into the local Supabase Studio SQL
    editor and runs it. Acceptable alternative the operator may authorize: execute the same
    file via psycopg2 against `localhost:54322` (postgres/postgres) from the scratchpad.
    Do NOT use `supabase db push` or `supabase db reset` (forbidden — would wipe/diverge
    local dev data). The migration is idempotent, so a re-run is safe.
  </action>
  <how-to-verify>
    After applying, Claude runs this psycopg2 SELECT against localhost:54322 (from the
    scratchpad, not backend/):
    `SELECT is_system, is_global, user_id, length(instructions) FROM skills WHERE id='00000000-0000-0000-0000-000000000010';`
    Expected exactly one row: `is_system=true`, `is_global=true`,
    `user_id='00000000-0000-0000-0000-000000000001'`, and `length(instructions)` non-trivial
    (> 1500 — proves the reborn 7-step content landed and the `DO UPDATE` took effect, not a
    silent `DO NOTHING` no-op against 018's ~900-char text).
  </how-to-verify>
  <verify>
    <automated>python -c "import psycopg2; c=psycopg2.connect(host='localhost',port=54322,dbname='postgres',user='postgres',password='postgres'); cur=c.cursor(); cur.execute(\"SELECT is_system,is_global,user_id,length(instructions) FROM skills WHERE id='00000000-0000-0000-0000-000000000010'\"); r=cur.fetchone(); assert r and r[0] is True and r[1] is True and str(r[2])=='00000000-0000-0000-0000-000000000001' and r[3] and r[3]>1500, r; print('OK',r)"</automated>
  </verify>
  <acceptance_criteria>
    - The `is_system` column exists on `public.skills` live (the SELECT does not error with "column does not exist").
    - Exactly one row `…0010` with `is_system=true`, `is_global=true`, `user_id='…0001'`, `length(instructions) > 1500`.
    - The apply was done via SQL editor / psycopg2 — NOT `db push`/`db reset` (operator confirms).
  </acceptance_criteria>
  <resume-signal>Type "applied" once 087 is run and the SELECT returns the expected row (or describe any error).</resume-signal>
  <done>Migration 087 is live; the seeded built-in exists with refreshed instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate full-schema.sql (no --reset) + commit</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - CLAUDE.md (the regenerate-full-schema.sh discipline — default no-reset, commit both)
    - scripts/regenerate-full-schema.sh (confirm the default no-reset behavior before running)
  </read_first>
  <action>
    Run `bash scripts/regenerate-full-schema.sh` (DEFAULT mode — do NOT pass `--reset`; a
    reset would wipe local dev data). This dumps the live schema, capturing the new
    `is_system` column. Confirm the regenerated `supabase/full-schema.sql` now contains the
    `is_system` column on the skills table, and confirm it does NOT contain the seed row
    (no `INSERT … 00000000-0000-0000-0000-000000000010` — the schema-only dump omits data
    by design; that gap is documented by Plan 02's deploy-parity edits). Commit the
    regenerated `full-schema.sql` (migration 087 itself was committed by Plan 02).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && bash scripts/regenerate-full-schema.sh && grep -c 'is_system' supabase/full-schema.sql && test "$(grep -c '00000000-0000-0000-0000-000000000010' supabase/full-schema.sql)" -eq 0 && echo "SCHEMA-ONLY-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/full-schema.sql` contains the `is_system` column (grep ≥ 1).
    - `supabase/full-schema.sql` does NOT contain the seed row id `…0010` (grep count == 0 — proves schema-only, the documented parity gap).
    - `full-schema.sql` is committed.
  </acceptance_criteria>
  <done>full-schema.sql captures the is_system column (not the seed row) and is committed.</done>
</task>

<task type="auto">
  <name>Task 3: One-off rename of the operator's manual copy (D-04, dev DB only — NOT a migration)</name>
  <files>(live dev DB only — no committed file)</files>
  <read_first>
    - .planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-RESEARCH.md (Code Example #6 = the one-off UPDATE; Pitfall 3 = why the rename is load-bearing for SC#4)
    - .planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-CONTEXT.md (D-04 — rename not delete; it stays as the operator's private reference copy)
  </read_first>
  <action>
    Per D-04, rename (NOT delete) the operator's private manual skill-creator copy so it
    can no longer shadow the built-in on the dev account. Run a single one-off UPDATE via
    psycopg2 against localhost:54322 (or the Supabase SQL editor), scoped by BOTH id and
    owner for safety: set `name = 'skill-creator (imported reference)'` where
    `id='7f504819-2ab6-4b12-8692-50e29696db54' AND user_id='d8a54002-6a29-4b88-b918-cff2aa4a06d5'`.
    This is a dev-only private row — do NOT fold it into migration 087 or commit it
    anywhere (embedding a specific dev user's id in a shared migration leaks it and matches
    0 rows on every other env — RESEARCH anti-pattern). It is a rename, fully reversible;
    the operator keeps their data. This must happen before the SC#4 walkthrough so
    `load_skill("skill-creator")` resolves uniquely to the built-in (the dev user then owns
    no skill named `skill-creator`; tool_dispatcher.py:665-677 prefers owned-over-global on
    a name tie, which is exactly the collision this removes).
  </action>
  <verify>
    <automated>python -c "import psycopg2; c=psycopg2.connect(host='localhost',port=54322,dbname='postgres',user='postgres',password='postgres'); cur=c.cursor(); cur.execute(\"SELECT name FROM skills WHERE id='7f504819-2ab6-4b12-8692-50e29696db54'\"); r=cur.fetchone(); assert r and r[0]=='skill-creator (imported reference)', r; cur.execute(\"SELECT count(*) FROM skills WHERE name='skill-creator' AND user_id='d8a54002-6a29-4b88-b918-cff2aa4a06d5'\"); assert cur.fetchone()[0]==0; print('OK renamed, no owned collision')"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT name FROM skills WHERE id='7f504819-…'` returns `skill-creator (imported reference)` (no longer `skill-creator`).
    - The dev user `d8a54002-…` now owns NO skill named `skill-creator` (count == 0) — the collision is gone.
    - The row still exists (renamed, not deleted); nothing was committed to the repo for this rename.
  </acceptance_criteria>
  <done>The operator's copy is renamed; load_skill("skill-creator") now resolves to the built-in on the dev account.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live DB | The migration apply + rename mutate live dev data. Gated (human-action checkpoint) + scoped by id/owner. |
| built-in row -> every user's catalog | Once live, the seeded is_global+is_enabled row reaches every user via existing catalog injection — read-only for all (owner `…0001`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.2-01 | Tampering / Denial | live built-in row (post-apply) | mitigate | Verified LIVE: owner is `…0001`, which never equals a real `current_user["id"]` → the built-in is read-only/undeletable for every real user through the existing owner-scoped mutation routes (Plan 01 proves the code path; this plan proves the live owner). |
| T-137.2-04 | Denial / data loss | operator's private copy `7f504819-…` | mitigate | D-04 is a RENAME, not a delete — the owner keeps full control of their private row; no cascade, no file orphaned. The UPDATE is scoped by BOTH id AND user_id so it can touch only that one row. Reversible. |
| T-137.2-05 | Tampering (destructive apply) | live local dev DB | mitigate | Apply is restricted to SQL-editor paste / psycopg2 :54322; `supabase db push`/`db reset` explicitly forbidden (they would wipe preserved dev data). `regenerate-full-schema.sh` runs in default no-reset mode. |

_No package installs in this plan → no supply-chain (T-…-SC) threat; slopcheck N/A._
</threat_model>

<verification>
- psycopg2 SELECT on `…0010` returns is_system=true, is_global=true, owner `…0001`, length(instructions) > 1500.
- `supabase/full-schema.sql` contains `is_system` and NOT the seed row id `…0010`.
- psycopg2 SELECT on `7f504819-…` returns `skill-creator (imported reference)`; the dev user owns no skill named `skill-creator`.
- Post-plan (manual UAT, VALIDATION.md — NOT a task here): the SC#4 lived conversational walkthrough on a tool-capable provider.
</verification>

<success_criteria>
- Migration 087 is applied live; the built-in exists with refreshed instructions (CREATE-01 SC#1).
- full-schema.sql captures the column (not the seed row) and is committed.
- The operator's manual copy is renamed (D-04); no dev-account name collision remains.
</success_criteria>

<output>
Create `.planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-04-SUMMARY.md` when done.
</output>
