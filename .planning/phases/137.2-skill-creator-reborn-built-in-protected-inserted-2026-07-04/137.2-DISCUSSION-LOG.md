# Phase 137.2: Skill Creator Reborn — Built-in + Protected (INSERTED) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-05
**Phase:** 137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04
**Areas discussed:** Protection mechanism, Eval-case creation, Manual copy disposition, Badge + list ordering

---

## Protection mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Add `is_system` column (Recommended) | New boolean on `skills`, set true only by the seed migration. Badge condition = `skill.is_system`. Clean, extensible, no magic UUID string in frontend code. Read-only/undeletable still comes free from existing owner-scoping — this column is only for the badge/UI signal. | ✓ |
| Ownership alone, no new column | Badge condition = `skill.user_id === '00000000-0000-0000-0000-000000000001'` hardcoded wherever needed. Zero migration surface beyond the seed row, but the magic UUID has to be duplicated. | |

**User's choice:** Add `is_system` column.
**Notes:** Confirmed protection itself is already free via existing owner-scoped mutation routes (verified live in `skills.py`); the column is purely the UI/badge signal. No further questions on this area.

---

## Eval-case creation

| Option | Description | Selected |
|--------|-------------|----------|
| Hand off to Studio (Recommended) | Agent proposes cases in chat text, directs user to the skill's Skill Studio → Evals tab (existing CaseEditor UI). Zero new tool, zero new cross-provider whitelist/testing surface. | ✓ |
| New `add_test_case` tool | Agent persists proposed cases directly into `skill_test_cases` mid-conversation. More seamless but new tool-registry surface — whitelisting + cross-provider verification + tests. | |

**User's choice:** Hand off to Studio.
**Notes:** No further questions on this area.

---

## Manual copy disposition

| Option | Description | Selected |
|--------|-------------|----------|
| Rename it now | e.g. "skill-creator (manual/legacy)" — keeps it for reference, visually distinct immediately. | ✓ (after clarification) |
| Delete it now | Remove it since the built-in supersedes it and its instructions assume capabilities the runtime doesn't have. | |
| Leave it, decide later | Don't touch it this phase. | |

**User's choice:** Initially answered via free text (not one of the 3 listed options) — expressed uncertainty about whether the scripts/HTML files inside the original zip had been assessed, and explicitly delegated the final call ("you decide based on best benefit of the app"). Claude verified live via psycopg2 (`:54322`) that the operator's copy (skill id `7f504819-2ab6-4b12-8692-50e29696db54`) carries only 3 of the zip's 18 files (`agents/analyzer.md`, `agents/comparator.md`, `agents/grader.md`) — the scripts/HTML were never imported — and confirmed all 18 zip files WERE assessed in SEED-101's capability matrix. Claude recommended **rename** (not delete) as the best-benefit call; presented as a follow-up confirm question with an explicit "Actually, delete it instead" escape hatch. User confirmed rename via "Next area (rename confirmed)".
**Notes:** This is the user's own private, owned data — rename chosen over delete specifically because ROADMAP says "operator decision, never auto-deleted," and rename is reversible while still resolving the visual-confusion risk.

---

## Badge + list ordering

| Option | Description | Selected |
|--------|-------------|----------|
| Pin to top (Recommended) | `is_system` rows sort before everything else, then alphabetical within each group. Most discoverable. | ✓ |
| Natural alphabetical order | No special-case sort — "skill-creator" sits wherever it falls alphabetically. Simpler query. | |

**User's choice:** Pin to top.
**Notes:** No further questions on this area.

---

## Claude's Discretion

- Exact migration number (next in sequence, likely 087).
- Whether `is_system` warrants an index (likely not — tiny table, low cardinality).
- Exact "Built-in" badge copy/styling (follow the existing "Global" pill's visual language unless a distinct treatment is clearly better).
- Full instructions prose for the reborn skill (content brief locked by SEED-101; wording is planner/researcher's job).
- Exact mechanism for renaming the operator's manual copy (one-off UPDATE vs. folded into the same migration).
- Whether the existing "hide enabled-toggle for global skills you don't own" UI rule needs adjustment for `is_system` skills (likely not, not explicitly discussed).

## Deferred Ideas

None — all 4 selected gray areas resolved without scope creep. The `spike-nl-workflow-authoring` todo was reviewed (score 0.6) but not folded — same disposition as Phases 134/135/136/137.1 (keyword-only match, already satisfied by Phase 103).
