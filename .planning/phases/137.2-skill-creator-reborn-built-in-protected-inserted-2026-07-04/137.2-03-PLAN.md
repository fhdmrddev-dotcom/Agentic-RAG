---
phase: 137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/components/skills/SkillCard.tsx
  - frontend/src/components/skills/SkillCard.test.tsx
autonomous: true
requirements: [CREATE-01]
must_haves:
  truths:
    - "D-01: SkillCard renders a 'Built-in' pill when `skill.is_system` is true — the visible trust badge that tells a user the skill-creator is a platform built-in, not a user skill"
    - "D-01 (badge redundancy): the header shows ONE pill via a ternary — 'Built-in' when `is_system`, else 'Global' when `is_global` (a built-in is inherently global, so two pills is redundant)"
    - "The frontend cannot spoof `is_system`: the field is added to the `Skill` read type ONLY, never to `SkillCreate`/`SkillUpdate`; SkillCard reflects the backend value, it does not set it"
    - "Owner-only actions (edit/delete/share/enabled-toggle) are already hidden for the system-owned built-in with ZERO new conditional logic — `isOwner = skill.user_id === currentUserId` is already false for owner `…0001`"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "Skill.is_system boolean (output/wire shape); SkillCreate/SkillUpdate deliberately unchanged"
      contains: "is_system: boolean"
    - path: "frontend/src/components/skills/SkillCard.tsx"
      provides: "Built-in/Global ternary pill in the header row"
      contains: "is_system"
    - path: "frontend/src/components/skills/SkillCard.test.tsx"
      provides: "NEW component test: Built-in pill renders on is_system; owner actions hidden when not owner"
      contains: "Built-in"
  key_links:
    - from: "frontend/src/components/skills/SkillCard.tsx"
      to: "Skill.is_system (from GET /skills SkillResponse)"
      via: "skill.is_system ? Built-in : is_global && Global"
      pattern: "is_system"
---

<objective>
Add the frontend half of the "Built-in" trust badge: the `is_system` wire-type field
and a single "Built-in" pill in `SkillCard` (shown instead of "Global" for system
skills), plus a fresh component test. This is the visible signal that the seeded
skill-creator (Plan 02) is a protected platform built-in.

Purpose: the badge is a *display of a backend-controlled value*. The client reads
`is_system` from `GET /skills`; it can never set it. The read-only reflection
(hidden owner actions) is already correct because `isOwner` is false for the
system-owned skill — so this plan adds a pill and a type field, nothing more.

Output: `Skill.is_system` type field, the `SkillCard` ternary pill, and
`SkillCard.test.tsx` (new). No change to SkillsPage/useSkills (backend order is
authoritative — do not add a client sort).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-RESEARCH.md
@.planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-PATTERNS.md

<interfaces>
CURRENT Skill type (frontend/src/types/index.ts, lines 475-493) — is_system inserts AFTER `is_global` (line 482); do NOT add it to SkillCreate (495-500) or SkillUpdate (502-506):
```typescript
export interface Skill {
  id: string
  user_id: string
  name: string
  description: string
  instructions: string
  is_enabled: boolean
  is_global: boolean        // ← INSERT `is_system: boolean` AFTER this line
  created_at: string
  updated_at: string
  lint_warnings?: SkillLintWarning[]
}
```

CURRENT SkillCard header pill (frontend/src/components/skills/SkillCard.tsx, lines 97-106) — the `{skill.is_global && (...)}` block becomes a Built-in/Global ternary:
```tsx
<div className="flex items-start gap-2">
  <Zap className="h-4 w-4 mt-0.5 text-primary shrink-0" />
  <span className="font-semibold text-sm text-foreground truncate flex-1 min-w-0">{skill.name}</span>
  {skill.is_global && (
    <span className="text-[10px] text-muted-foreground bg-muted px-2 py-1 rounded-full shrink-0">
      Global
    </span>
  )}
</div>
```

Already-correct reflection (no change needed): `isOwner = skill.user_id === currentUserId` (SkillCard:46) is false for the `…0001`-owned built-in → edit/delete/share (owner block :189-262) already hidden; the enabled-toggle is also hidden for global skills the user doesn't own (:157).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_system to the Skill wire type (output-only)</name>
  <files>frontend/src/types/index.ts</files>
  <read_first>
    - frontend/src/types/index.ts (the `Skill` interface lines 475-493; `SkillCreate` 495-500; `SkillUpdate` 502-506)
    - .planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-PATTERNS.md (types/index.ts section)
  </read_first>
  <action>
    Insert `is_system: boolean` into the `Skill` interface immediately after `is_global:
    boolean` (line 482), with a short `// Phase 137.2 / CREATE-01 — "Built-in" pill`
    comment per house style. Per D-01 this mirrors the output-only backend field: do NOT
    add `is_system` to `SkillCreate` (495-500) or `SkillUpdate` (502-506) — the client
    reads it, never sends it (badge-spoofing mitigation, T-137.2-02). No other type change.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && awk '/export interface Skill \{/,/^\}/' src/types/index.ts | grep -q 'is_system: boolean' && ! awk '/export interface SkillCreate \{/,/^\}/' src/types/index.ts | grep -q 'is_system' && ! awk '/export interface SkillUpdate \{/,/^\}/' src/types/index.ts | grep -q 'is_system' && echo 'OK: is_system on Skill only'</automated>
  </verify>
  <acceptance_criteria>
    - `is_system: boolean` is present in the `Skill` interface span AND absent from the `SkillCreate`/`SkillUpdate` interface spans (the verify greps each interface body separately, not the whole file).
    - Defense-in-depth only: the real badge-spoofing boundary is the backend (Plan 01 keeps `is_system` off the `SkillCreate`/`SkillUpdate` input models); this scoped check is a test-quality guard, not a security fix.
    - `npx tsc --noEmit` exits 0 (no type errors introduced).
  </acceptance_criteria>
  <done>Skill carries is_system on the wire; input types are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Render the "Built-in" pill in SkillCard (Built-in xor Global ternary)</name>
  <files>frontend/src/components/skills/SkillCard.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillCard.tsx (header row lines 97-106; `isOwner` line 46; owner block 189-262; toggle-hidden line 157 — confirm no other change is needed)
    - .planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-RESEARCH.md (Code Example #5 — the ternary + the tint recommendation)
  </read_first>
  <action>
    Convert the header-row `{skill.is_global && (...)}` pill (lines 97-106) into a ternary
    that shows "Built-in" when `skill.is_system`, else the existing "Global" pill when
    `skill.is_global` — ONE pill, not both (a built-in is inherently global; two pills is
    redundant clutter). Use a distinguishing trust-badge tint for "Built-in"
    (`text-[10px] text-primary bg-primary/10 px-2 py-1 rounded-full shrink-0`) while
    keeping the muted classes for "Global"; the XOR (one pill) is the key call regardless
    of tint (CONTEXT.md discretion). Change NOTHING else in this file — `isOwner` already
    hides owner actions for the system-owned skill, and the enabled-toggle is already
    hidden for unowned global skills. Do NOT add any `is_system`-aware button/guard logic.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -c 'skill.is_system' src/components/skills/SkillCard.tsx && grep -cF 'Built-in' src/components/skills/SkillCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The header pill is a ternary keyed on `skill.is_system` that renders the literal text `Built-in` for system skills and falls back to the existing `Global` pill otherwise (grep ≥ 1 for both `skill.is_system` and `Built-in`).
    - No new owner-action/guard conditionals were added (source assertion: `isOwner` block and toggle line unchanged).
    - `npx tsc --noEmit` exits 0.
    - Behavioral proof is covered by Task 3.
  </acceptance_criteria>
  <done>System skills show a single "Built-in" pill; all other cards are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: New SkillCard.test.tsx — Built-in pill renders + owner actions hidden</name>
  <files>frontend/src/components/skills/SkillCard.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/PublishGateDialog.test.tsx (freshly-authored sibling render-test conventions — imports, vi.mock of @/lib/supabase and @/lib/api, userEvent.setup pointerEventsCheck:0, afterEach cleanup)
    - frontend/src/components/skills/SkillFormDialog.test.tsx (the `mkSkill(overrides)` factory, lines 67-80 — copy it and ADD `is_system: false` to the defaults)
    - frontend/src/components/skills/SkillCard.tsx (the props it takes — `skill`, `currentUserId`, and any handlers — so the render call is correct)
    - .planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-PATTERNS.md (SkillCard.test.tsx section — the two assertions + why to author fresh, not lean on the rotted vitest siblings)
  </read_first>
  <action>
    Create a NEW `frontend/src/components/skills/SkillCard.test.tsx` (none exists today).
    Author it fresh in the PublishGateDialog.test.tsx style (do NOT lean on the known-rotted
    vitest siblings — MEMORY project_frontend_vitest_rot). Copy `mkSkill(overrides)` from
    SkillFormDialog.test.tsx and add `is_system: false` to its defaults so
    `mkSkill({ is_system: true })` works. Defensively `vi.mock('@/lib/supabase', …)` and
    `vi.mock('@/lib/api', () => ({ getPublishGate: vi.fn() }))` because SkillCard embeds a
    (closed-by-default) PublishGateDialog. Two assertions:
    (a) Built-in pill renders — render `<SkillCard skill={mkSkill({ is_system: true })} …/>`
    and assert `screen.getByText(/built-in/i)` is present; also assert a NON-system global
    skill (`mkSkill({ is_global: true })`) shows "Global" not "Built-in".
    (b) Owner actions hidden when not owner — render with `currentUserId="someone-else"`
    (so `isOwner` is false) and assert the edit/delete/share controls are absent
    (`screen.queryBy…` returns null). Supply whatever required props/handlers SkillCard
    needs (read the component signature). Keep it a plain render test — no need to open the
    Radix dialog unless an assertion requires it.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/SkillCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/skills/SkillCard.test.tsx` exists with ≥ 2 passing tests: Built-in pill on `is_system`, and owner actions hidden when `user_id !== currentUserId`.
    - `npx vitest run src/components/skills/SkillCard.test.tsx` exits 0 (all green).
    - The test proves a non-system global skill still shows "Global" (the ternary's else branch).
  </acceptance_criteria>
  <done>SkillCard's badge + read-only reflection are behaviorally covered by a fresh, green test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> client | `is_system` arrives on the read `Skill` shape only. The client renders it; it never round-trips it back as a settable field. |
| client -> API (request body) | `SkillCreate`/`SkillUpdate` must not carry `is_system` — the frontend has no path to set the badge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.2-02 | Spoofing / Elevation-of-trust | `Skill` type + SkillCard | mitigate | Add `is_system` to the `Skill` READ type ONLY (Task 1); never to `SkillCreate`/`SkillUpdate`. SkillCard reflects the backend value in a pill — there is no input control that sets it. The badge cannot be spoofed from the client. |
| T-137.2-01 | Tampering / Denial | SkillCard owner-action reflection | mitigate | Reflect-not-enforce: `isOwner` is already false for the `…0001`-owned built-in, so edit/delete/share/toggle are already hidden with zero new logic. The API (Plan 01) is the actual boundary — the UI merely mirrors it. |

_No package installs in this plan → no supply-chain (T-…-SC) threat; slopcheck N/A._
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — exit 0.
- `cd frontend && npx vitest run src/components/skills/SkillCard.test.tsx` — all green.
- Skills components at wave merge: `cd frontend && npx vitest run` (skills suite green; pre-existing rot per MEMORY is out of scope).
- Live badge + top-of-list ordering are verified in the Plan 04 / SC#4 walkthrough (manual UAT, VALIDATION.md).
</verification>

<success_criteria>
- `Skill.is_system` exists on the read type; input types cannot set it (D-01 / T-137.2-02).
- SkillCard shows a single "Built-in" pill for system skills, "Global" otherwise (D-01).
- Owner actions remain hidden for the system-owned built-in with no new logic (T-137.2-01).
</success_criteria>

<output>
Create `.planning/phases/137.2-skill-creator-reborn-built-in-protected-inserted-2026-07-04/137.2-03-SUMMARY.md` when done.
</output>
