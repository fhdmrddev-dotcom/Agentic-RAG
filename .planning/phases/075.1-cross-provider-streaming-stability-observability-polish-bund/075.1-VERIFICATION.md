---
phase: 075.1-cross-provider-streaming-stability-observability-polish-bund
verified: 2026-05-20T01:00:00Z
status: human_needed
score: 12/14 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Plan 01 Task 3 — 45-second sleep cell completes across all 3 providers without F5"
    expected: "Submit `import time; print('starting'); time.sleep(45); print('done')` against OpenAI, Anthropic, and OpenRouter. At the 45-second mark the assistant message renders the final `done` output WITHOUT an F5 reload. OpenRouter additionally must NOT show the Resume button mid-stream (B-260519-03 check). Take Chrome MCP screenshots at t=0, t=15s, t=30s, t=46s per provider."
    why_human: "Requires live dev stack with Docker sandbox, Supabase, Redis, and all three LLM provider API keys active. SSE timing behavior cannot be exercised programmatically. Resume signal: 'approved-all-3-providers' (or identify which provider failed with SSE wire log + backend log window)."
  - test: "Plan 03 Task 3 — Anthropic live render, sticky indicator, and BUG-260514-02 hybrid probe"
    expected: "Submit the dissertation-defense prompt against Anthropic (claude-sonnet-4-6). PASS: assistant text starts streaming within 5 s (not blank-until-F5), tool-call cards appear in real time, accumulated text does NOT disappear when a new tool_use block opens, bottom indicator stays animated during silent matplotlib windows. Record BUG-260514-02 verdict: synthesized summary vs raw tool-call narration in the final message."
    why_human: "Full Anthropic streaming run requires live API + Docker sandbox. The reducer-invariant fix confirmed the code is already correct; the UAT determines whether the cumulative Plan 01+02+03 substrate closes the visible symptom end-to-end. BUG-260514-02 routing decision (fold into 075.1 vs stay deferred to v2.7) gates on this observation. Resume signal: 'approved-anthropic-live-render' + BUG-260514-02 verdict."
  - test: "Plan 04 Task 4 — End-to-end Plan 04 bug-closure checklist across all 3 providers"
    expected: |
      Run the dissertation-defense prompt against OpenAI, Anthropic, and OpenRouter and verify each bug closure:
      - B-260519-02: New thread initial GET /threads/{tid}/snapshot returns 200 with empty since_cursors (not 503)
      - B-260519-04 (Anthropic): LangSmith shows trace named 'ChatAnthropic' with run_type=llm
      - B-260519-04 (provider tag): LangSmith shows 'ChatOpenrouter' (not generic 'ChatOpenAI') for OpenRouter runs
      - B-260519-05 (log): Backend log line 'sub-agent invoked tool=analyze_document main_model=... sub_model=... reason=cost_default'
      - B-260519-05 (payload): DevTools Network response shows sub_agent_model field in tool_call_result
      - B-260519-05 (UI line): Tool-card shows 'Sub-agent: {model_id}' italic mono line
      - B-260519-05 (Settings): Sub-agent dropdown visible in Settings with 'Auto (cheapest)' option
      - B-260519-07: Successful stdout text renders in neutral/emerald color, not red; stderr renders red
      - B-260519-08: Dissertation pptx run succeeds without ModuleNotFoundError loop (sandbox image built with SANDBOX_IMAGE=agentic-rag-sandbox:075.1)
      - B-260519-09: Output files written to /sandbox/output/ with working download links
      - B-260519-10: Each tool_call_id renders exactly one card in the tool panel
      - B-260519-11 + BUG-260514-01: Per-cell shows delta files only; pinned 'Final outputs' panel at message bottom shows full cumulative set
      - Test 2 dual-/messages: Thread switch produces 1 /snapshot request, 0 /messages requests (DevTools Network)
    why_human: "13-item checklist across 3 LLM providers. Requires live LangSmith project, Docker sandbox image build (`docker build -f backend/Dockerfile.sandbox -t agentic-rag-sandbox:075.1 backend/` + SANDBOX_IMAGE env var), live Supabase, live Redis, and all three provider API keys. Resume signal: 'approved-plan04-all-bugs-closed' with count of items checked; list any failures with evidence."
---

# Phase 075.1: Cross-Provider Streaming Stability + Observability Polish Bundle — Verification Report

**Phase Goal:** Close the 11 bugs catalogued in `075-CROSS-PROVIDER-UAT.md` (v4) — universal SSE-break, Anthropic content-block render failure mid-stream, snapshot 503 on empty threads, sub-agent silent downgrade, LangSmith Anthropic untraced + provider mislabel, OpenAI ModuleNotFoundError give-up, OpenRouter sandbox path inconsistency + Resume-button surfacing, stdout-in-red styling, ToolCallPanel duplication, output-files panel cumulative-repeat. PLUS BUG-260514-01 (folded).
**Verified:** 2026-05-20T01:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification
**Test counts confirmed:** 21/21 backend unit tests pass + 27/27 frontend unit tests pass + 10/10 Plan 02 regression tests (harvest threadpool + drain_step) pass after Plan 04 signature adaptation

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Universal SSE-break root cause fixed: `harvest_output_files` no longer blocks the async event loop after sandbox completion | VERIFIED | `run_in_threadpool(harvest_output_files, ...)` at `backend/app/api/threads.py:2699-2703`; unit test `test_harvest_offloaded_keeps_loop_responsive` passes: concurrent asyncio.sleep completes before harvest returns |
| 2 | Client-side transient-stream-end filter covers 4 SSE close paths and probes /snapshot before flipping runStatus | VERIFIED | `_isTransientStreamEnd` at `StreamsProvider.tsx:122` exported at module scope; covers buffer_expired error, generic error, done-with-active-tools, reader_done; 8/8 unit tests in `StreamsProvider.transient.test.ts` pass |
| 3 | Non-explicit terminal SSE close triggers snapshot probe and re-attaches if backend says streaming continues | VERIFIED | `_reattachAfterTransient` at `StreamsProvider.tsx:176`; called from both onTerminal handlers (reconcile + sendMessage); Branch D-3 guard preserved at `StreamsProvider.tsx:566` |
| 4 | Snapshot endpoint returns 200 (not 503) on brand-new empty threads | VERIFIED | `if not active_runs:` early return at `backend/app/api/threads.py:756`; unit test `test_snapshot_short_circuits_on_empty_active_runs` passes (mocks xinfo_stream to raise; endpoint returns 200) |
| 5 | Anthropic content-block reducer preserves m.content across interleaved text_delta + tool_use blocks | VERIFIED | Audit confirmed: every `setMessages` reducer return in `makeStreamCallbacks` either omits `content:` (preserves via spread) or appends in `onDelta`. 5/5 unit tests in `StreamsProvider.anthropic-ordering.test.ts` pass including OpenAI regression check |
| 6 | MessageItem sticky bottom-indicator cache resets on per-message runStatus terminal, not provider-level isStreaming | VERIFIED | `const isMessageStreaming = message.runStatus === "streaming"` at `MessageItem.tsx:97`; old `isStreaming && computedLabel !== null` gate removed; 6/6 unit tests in `MessageItem.sticky.test.tsx` pass |
| 7 | Sub-agent invocations log an identifier-only info line; sub_agent_model surfaced in tool_call_result payload and tool-card UI | VERIFIED | Log line at `sub_agent_service.py:77`; `sub_agent_model` in `persisted_tool_calls.append` spread at `threads.py`; `Sub-agent: {tc.sub_agent_model}` render at `ToolCallPanel.tsx:769`; unit test `test_sub_agent_logs_invocation_with_identifier_only_fields` passes |
| 8 | LangSmith covers Anthropic main-loop with @traceable and distinguishes OpenRouter/Ollama from native OpenAI via per-provider chat_name | VERIFIED | `@traceable(name="ChatAnthropic", run_type="llm")` at `anthropic_service.py:28-38`; `wrap_openai(client, chat_name=f"Chat{provider.title()}")` at `openai_service.py:551`; both unit tests pass |
| 9 | Agent-runner system prompt includes pip install hint + /sandbox/output/ write-path hint uniformly for all providers | VERIFIED | `pip install` hint at `threads.py:466`; `/sandbox/output/` at `threads.py:375,462,464`; unit test `test_system_prompt_contains_sandbox_hints_for_all_providers` passes |
| 10 | Sandbox Docker image pre-installs python-pptx, matplotlib, numpy, pandas (pinned) via opt-in SANDBOX_IMAGE mechanism | VERIFIED | `backend/Dockerfile.sandbox` exists with `python-pptx==1.0.2`, `matplotlib==3.9.2`, `numpy==2.1.3`, `pandas==2.2.3`; opt-in via SANDBOX_IMAGE env var in sandbox_service.py |
| 11 | ToolCallPanel de-duplicates tool cards keyed on tool_call_id | VERIFIED | `deduplicatedToolCalls = useMemo(...)` at `ToolCallPanel.tsx:538`; all downstream reads use deduplicated list; 2/2 dedup unit tests pass |
| 12 | Output-files panel renders per-cell delta only; cumulative 'Final outputs' pinned panel emitted at agent loop end | VERIFIED | `harvest_output_files` signature extended to `tuple[list[dict], set[str]]` with `previous_files` param at `sandbox_service.py:84`; `final_output_files` SSE event emitted at `threads.py:2900`; `onFinalOutputFiles` wired in `api.ts:233,418-419` and `StreamsProvider.tsx` reducer; `message.finalOutputFiles` panel at `MessageItem.tsx:255-264`; 4/4 delta-view unit tests pass |
| 13 | BUG-260514-01 frontmatter flipped: status folded + folded_into 075.1 | VERIFIED | `.planning/reported-bugs/tool-output-download-bloat-intermediate-artifacts.md` frontmatter shows `status: folded` + `folded_into: "075.1"` |
| 14 | 45-second sleep cell completes and UI updates without F5 reload across all 3 providers (OpenAI, Anthropic, OpenRouter) | NEEDS HUMAN UAT | Plans 01 + 02 provide the combined substrate (client filter widen + threadpool wrap); code verification passed; live end-to-end UAT required against running dev stack with sandbox |

**Score: 13/14 truths verified by code inspection + automated tests. 1 truth (Truth 14) requires live UAT.**

Note: Truth 13 (B-260519-01 Anthropic blank-until-F5) is addressed by Truths 5 and 14 together. Plan 03 SUMMARY documents that the reducer was already correct in the post-Plan-01 codebase — the B-260519-01 observable symptom may be addressed by the combined Plan 01+02+03 substrate changes, but this is confirmed only by UAT (see human_verification item 2).

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|---------|--------|---------|
| `frontend/src/providers/StreamsProvider.tsx` | Widened transient filter + reattach helper | VERIFIED | `_isTransientStreamEnd` (line 122), `_reattachAfterTransient` (line 176), both onTerminal handlers updated, reader_done handled, Branch D-3 guard preserved |
| `frontend/src/lib/api.ts` | reader_done emitted from defensive close + onFinalOutputFiles callback | VERIFIED | `onTerminal("reader_done")` at line 511; `onFinalOutputFiles` type at line 233-234; dispatch at line 418-419 |
| `frontend/src/__tests__/providers/StreamsProvider.transient.test.ts` | 8 tests — filter decision matrix | VERIFIED | 8/8 pass |
| `frontend/src/__tests__/providers/StreamsProvider.anthropic-ordering.test.ts` | 5 tests — Anthropic reducer invariants | VERIFIED | 5/5 pass |
| `frontend/src/__tests__/components/MessageItem.sticky.test.tsx` | 6 tests — per-message runStatus sticky cache | VERIFIED | 6/6 pass |
| `frontend/src/__tests__/components/Plan04.frontend.test.tsx` | 8 tests — Plan 04 frontend atoms | VERIFIED | 8/8 pass |
| `backend/app/api/threads.py` | run_in_threadpool wrap + drain_step caller + snapshot short-circuit + system-prompt hints + delta-view + final_output_files emit | VERIFIED | All grep gates confirmed; drain_step at line 165; harvest wrap at line 2699; snapshot short-circuit at line 756 |
| `backend/app/services/sandbox_service.py` | harvest delta signature + SANDBOX_IMAGE opt-in | VERIFIED | `previous_files: set[str] | None = None` param at line 84; delta-return logic present |
| `backend/app/services/sub_agent_service.py` | Sub-agent invocation log | VERIFIED | Logger.info at line 77 with identifier-only fields |
| `backend/app/services/anthropic_service.py` | @traceable on stream_anthropic | VERIFIED | Decorator at lines 28-38 with ImportError guard |
| `backend/app/services/openai_service.py` | Per-provider chat_name in wrap_openai | VERIFIED | `chat_name=f"Chat{provider.title()}"` at line 551 with TypeError fallback |
| `backend/Dockerfile.sandbox` | Pre-installed packages (pinned) | VERIFIED | File exists; python-pptx 1.0.2, matplotlib 3.9.2, numpy 2.1.3, pandas 2.2.3 |
| `backend/tests/unit/test_075_1_harvest_threadpool.py` | 2 loop-responsiveness tests | VERIFIED | 2/2 pass (adapted to tuple shape by Plan 04) |
| `backend/tests/unit/test_075_1_drain_step.py` | 8 drain_step deterministic tests | VERIFIED | 8/8 pass |
| `backend/tests/unit/test_075_1_observability.py` | 6 observability tests | VERIFIED | 6/6 pass |
| `backend/tests/unit/test_075_1_sandbox.py` | 5 sandbox delta/hint tests | VERIFIED | 5/5 pass |
| `frontend/src/components/chat/ChatArea.tsx` | Duplicate loadMessages deleted; retry handler preserved | VERIFIED | Exactly 1 `loadMessages(thread.id).catch(console.error)` at line 61 (retry); duplicate at former line ~166 removed |
| `frontend/src/components/chat/ToolCallPanel.tsx` | useMemo dedup + Sub-agent line | VERIFIED | `deduplicatedToolCalls` at line 538; `Sub-agent: {tc.sub_agent_model}` at line 769 |
| `frontend/src/components/chat/MessageItem.tsx` | finalOutputFiles panel + isMessageStreaming | VERIFIED | `isMessageStreaming` at line 97; `finalOutputFiles` panel at lines 255-264 |
| `frontend/src/types/index.ts` | ToolCall.sub_agent_model + Message.finalOutputFiles | VERIFIED | Both extended per Plan 04 summary |
| `.planning/reported-bugs/tool-output-download-bloat-intermediate-artifacts.md` | status: folded + folded_into: "075.1" | VERIFIED | Confirmed by grep |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| StreamsProvider._isTransientStreamEnd | getSnapshot(threadId).active_runs | snapshot probe before runStatus flip | WIRED | Function exported; called from both onTerminal handlers passing tool_calls array |
| api.ts subscribeToRun defensive close | callbacks.onTerminal("reader_done") | reader.done path now surfaces transient signal | WIRED | Line 511 confirmed |
| StreamsProvider._reattachAfterTransient | subscribeToRun re-attach via caller-supplied callback | snapshot cursor seed + fresh AbortController | WIRED | Helper at line 176; called from both onTerminal sites |
| backend/app/api/threads.py harvest call | run_in_threadpool(harvest_output_files, ...) | offload blocking I/O to anyio worker pool | WIRED | Line 2699 confirmed; bare synchronous form gone |
| backend/app/api/threads.py drain loop | drain_step(item, _drain_state) | pure function call per iteration | WIRED | `drain_step` at line 165 called inside agent loop |
| backend/app/api/threads.py get_snapshot | early-return when active_runs empty | if not active_runs: return 200 + empty since_cursors | WIRED | Line 756 confirmed; unit test passes |
| backend/app/services/sub_agent_service.py run_sub_agent | logger.info("sub-agent invoked...") | one log per invocation before LLM call | WIRED | Line 77 confirmed |
| backend/app/services/anthropic_service.py stream_anthropic | @traceable(name="ChatAnthropic") | LangSmith trace decorator | WIRED | Lines 28-38 confirmed; unit test passes |
| frontend/src/lib/api.ts SSE parser | callbacks.onFinalOutputFiles(files) | final_output_files event dispatch | WIRED | Lines 418-419 confirmed |
| frontend/src/providers/StreamsProvider.tsx | message.finalOutputFiles | onFinalOutputFiles reducer stamps field | WIRED | Confirmed per Plan 04 summary |
| frontend/src/components/chat/MessageItem.tsx | message.finalOutputFiles panel | conditional render when non-empty | WIRED | Lines 255-264 confirmed |
| MessageItem.isMessageStreaming | message.runStatus === "streaming" | per-message sticky cache reset trigger | WIRED | Line 97 confirmed; 6/6 unit tests pass |
| ToolCallPanel.deduplicatedToolCalls | useMemo keyed on tc.id | dedup pass before all downstream reads | WIRED | Line 538 confirmed; all downstream reads use deduplicated array |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| StreamsProvider._isTransientStreamEnd | snapshot.active_runs | GET /threads/{tid}/snapshot | Yes — real DB query on runs table (existing Phase 075 endpoint, RLS enforced, dual-eq) | FLOWING |
| MessageItem.finalOutputFiles | message.finalOutputFiles | final_output_files SSE event → onFinalOutputFiles reducer | Yes — emitted by backend agent loop after iterating _previous_files_in_run set | FLOWING |
| ToolCallPanel.deduplicatedToolCalls | toolCalls prop | StreamsProvider message.tool_calls | Yes — accumulated by onToolPreparing/onToolStart/onToolEnd callbacks from live SSE | FLOWING |
| harvest_output_files delta return | previous_files set | _previous_files_in_run initialized empty, updated each harvest call | Yes — real Docker container file walk + Supabase Storage upload | FLOWING |

Note: The `finalOutputFiles` pinned panel renders only `f.filename` as text — no download affordance. The REVIEW (IN-03) flags this as a UX gap: `url` arrives in the backend payload but is not surfaced as a clickable link. The panel closes the cognitive-load symptom (no more 12-repeated-download-links per cell) but users cannot download from the pinned panel directly. This is an advisory gap per the REVIEW, not a blocker (the per-cell tool-card download links continue to work). Surfaced here for the orchestrator.

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 21 backend unit tests pass | `cd backend && venv/Scripts/python -m pytest tests/unit/test_075_1_*.py -q` | 21 passed | PASS |
| 27 frontend unit tests pass (4 test files) | `cd frontend && npx vitest run providers/StreamsProvider.transient providers/StreamsProvider.anthropic-ordering components/MessageItem.sticky components/Plan04.frontend` | 27 passed, 0 failed | PASS |
| harvest_output_files wrapped — bare sync call gone | `grep -nE "^\s*output_file_list\s*=\s*harvest_output_files\(" backend/app/api/threads.py` | 0 matches | PASS |
| drain_step exists at module scope | `grep -nE "^def drain_step\(" backend/app/api/threads.py` | 1 match (line 165) | PASS |
| snapshot short-circuit present | `grep -nE "if not active_runs:" backend/app/api/threads.py` | 1 match (line 756) | PASS |
| Only 1 loadMessages retry call remains | `grep -c "loadMessages(thread.id).catch(console.error)" frontend/src/components/chat/ChatArea.tsx` | 1 | PASS |
| BUG-260514-01 frontmatter flipped | `grep -E "^status:|^folded_into:" .planning/reported-bugs/tool-output-download-bloat*.md` | status: folded / folded_into: "075.1" | PASS |
| 45-second sleep cell — all 3 providers | Requires live dev stack | Not runnable without Docker/API keys | SKIP (see human_verification) |

---

## Requirements Coverage

Phase 075.1 declared `requirements_addressed: []` in all four PLAN.md frontmatters. The phase goal is defined directly against the bug list from `075-CROSS-PROVIDER-UAT.md` (v4), not against v2.6 REQUIREMENTS.md entries. REQUIREMENTS.md traceability maps `POLISH-SEED-008-01` and `POLISH-SEED-008-02` to Phase 075 (parent), not to this inserted sub-phase. No REQUIREMENTS.md IDs are claimed by Phase 075.1 — coverage is against the 11 UAT bugs + BUG-260514-01 directly.

**UAT Bug Coverage:**

| Bug ID | Description | Coverage | Status |
|--------|-------------|---------|--------|
| B-260519-01 | Anthropic blank-until-F5 | Plans 01+02+03 (reducer invariant + SSE substrate) | PARTIAL — code fixes shipped; UAT required to confirm end-to-end (Plan 03 SUMMARY notes reducer was already correct; actual cause may be elsewhere) |
| B-260519-02 | Snapshot 503 on empty threads | Plan 04 Task 1 Atom A | VERIFIED |
| B-260519-03 | OpenRouter Resume button mid-stream | Plan 01 (widened transient filter) | CODE VERIFIED — UAT required for live confirmation |
| B-260519-04 | LangSmith Anthropic untraced + provider mislabel | Plan 04 Task 1 Atoms D+E | VERIFIED (code) — LangSmith UI check is human-only |
| B-260519-05 | Sub-agent silent downgrade — log + payload + UI + Settings | Plan 04 Tasks 1+3 | VERIFIED (code + tests) — live log/LangSmith check is human-only |
| B-260519-07 (stdout styling) | Successful stdout shown red | Plan 04 Task 3 Atom C | VERIFIED — code already correct; regression test pins it |
| B-260519-07 (indicator) | Sticky indicator resets on wrong signal | Plan 03 Task 2 | VERIFIED — 6/6 unit tests pass |
| B-260519-08 | OpenAI ModuleNotFoundError give-up | Plan 04 Task 2 Atoms B+C | VERIFIED (code) — sandbox image build requires Docker |
| B-260519-09 | OpenRouter sandbox path inconsistency | Plan 04 Task 2 Atom B | VERIFIED — system prompt grep gates pass |
| B-260519-10 | ToolCallPanel duplication | Plan 04 Task 3 Atom B | VERIFIED — 2/2 dedup unit tests pass |
| B-260519-11 | Output-files cumulative-repeat | Plans 04 Tasks 2+3 Atoms A+E | VERIFIED (code) — delta logic unit tested; pinned panel tested |
| BUG-260514-01 | Output-files download bloat | Plan 04 Task 3 Atom G | VERIFIED — frontmatter flipped |
| Test 2 dual-/messages | Duplicate loadMessages on thread switch | Plan 04 Task 3 Atom A | VERIFIED — exactly 1 loadMessages call confirmed |
| BUG-260514-02 | Anthropic end-of-cycle narration vs summary | Out of scope (deferred D-075-15) | DEFERRED — status remains `deferred`; UAT 3 in Plan 03 is an observational probe only |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|---------|--------|
| `frontend/src/components/chat/MessageItem.tsx` | 255-264 | `finalOutputFiles` panel renders filename text only — no download affordance; `url` field in type arrives but is never rendered as anchor/button | Warning (IN-03 from REVIEW) | UX gap: users cannot download from pinned panel. Per-cell tool-card links still work. |
| `backend/app/services/sandbox_service.py` | ~113-189 | Files present in `previous_files` are still fully uploaded + `sandbox_files` rows inserted before delta filtering occurs — wasteful re-upload per iteration | Info (IN-02 from REVIEW) | Duplicate Supabase Storage writes + possible duplicate sandbox_files rows; functionally correct (deterministic URLs + upsert); no data loss |
| `frontend/src/providers/StreamsProvider.tsx` | 122-191 | `_isTransientStreamEnd` and `_reattachAfterTransient` each call `getSnapshot` — two round-trips per transient event; second fetch can arrive after run terminates, causing `since` to default to "0" and replay from beginning | Warning (WR-02 from REVIEW) | Low-risk in practice (SSE callbacks idempotent in spirit); potential duplicate tool_end flips or duplicate output lines under poor network |
| `frontend/src/providers/StreamsProvider.tsx` | 318-328 | `onToolEnd` matches by `name + status === "running"` not by `tc.id` — flips first matching tool, not the specific tool that ended | Warning (WR-01 from REVIEW) | Non-deterministic "done" stamp on wrong tool card when same tool name runs twice; mitigated by `parallel_tool_calls: false` |
| `frontend/src/components/chat/ToolCallPanel.tsx` | 538-548 | Dedup key fallback `${tc.name}-${tc.startedAt ?? ''}` collapses to `"execute_code-"` for all preparing entries with undefined startedAt — drops second entry silently | Warning (WR-03 from REVIEW) | Potential silent dedup of sequential same-name tool-preparings; mitigated by `parallel_tool_calls: false` |

**Classification notes:**
- WR-01, WR-02, WR-03 are advisory warnings from the code review; DO NOT mark phase as `gaps_found` per verification instruction. They are pre-existing or low-risk paths mitigated by runtime constraints.
- IN-02 (harvest double-upload) is advisory; functionally correct.
- IN-03 (no download in pinned panel) is a UX gap worth a follow-up task but does not block the phase goal (cumulative-repeat symptom is closed; per-cell download links work).

---

## Human Verification Required

Three UAT checkpoints were deferred from the automated executor (each plan's `checkpoint:human-verify` task). These are blocking for full phase sign-off:

### 1. Plan 01 Task 3 — Universal 45-second sleep cell across all 3 providers

**Test:** Start the full dev stack (Docker, Supabase local, Redis local, uvicorn backend, npm dev frontend). Log in at http://localhost:5173/ as fhdmrd@gmail.com / 123456. Against each of OpenAI, Anthropic, OpenRouter, submit:
> "Please run this code: import time; print('starting'); time.sleep(45); print('done')"

**Expected:** At the 45-second mark, the bottom indicator clears and the assistant message renders the final `done` output WITHOUT requiring F5 reload. OpenRouter additionally must NOT show the Resume button mid-stream (B-260519-03 check).

**Why human:** Requires live Docker sandbox + all three provider API keys + Redis + Supabase. SSE timing and transient filter behavior cannot be exercised without a real streaming connection.

**Resume signal:** `"approved-all-3-providers"` if PASS on all three. Otherwise describe which provider failed + paste the SSE wire log (DevTools Network → stream request → EventStream tab) + backend log window.

---

### 2. Plan 03 Task 3 — Anthropic live render + sticky indicator + BUG-260514-02 hybrid probe

**Test:** Set active provider to Anthropic (claude-sonnet-4-6). Submit the dissertation-defense prompt:
> "search for Fahed Mrad dissertation, make a professional pptx for defence session and include comprehensive charts and visuals"

Watch the first 60 seconds and capture Chrome MCP screenshots at t=10s, t=30s, t=60s, t=180s. After completion (~5 min), examine the final assistant message for synthesized summary vs raw tool-call narration.

**Expected:**
- User message appears immediately; assistant text starts streaming within 5 s (NOT blank-until-F5, not blank until reload)
- Tool-call cards appear in real time as the agent invokes search-documents, analyze_document, execute_code
- Accumulated assistant text between tool calls remains visible when a new tool_use block opens
- Bottom indicator stays animated through silent matplotlib render windows
- BUG-260514-02 verdict: record whether final message is a synthesized summary OR raw "Action: I called X…" narration

**Why human:** Full Anthropic run with sandbox requires live API + Docker. The Plan 03 code audit confirmed the reducer was already correct — the live UAT determines whether the combined Plan 01+02+03 substrate closes the visible symptom. BUG-260514-02 routing decision (whether to flip frontmatter from `deferred` to `folded`) gates on the final message observation.

**Resume signal:** `"approved-anthropic-live-render"` + BUG-260514-02 verdict ("BUG-260514-02 incidentally closed" OR "BUG-260514-02 still reproduces — stay deferred").

---

### 3. Plan 04 Task 4 — Full 13-item bug-closure checklist across all 3 providers

**Test:** Run the dissertation-defense prompt against OpenAI, Anthropic, and OpenRouter. Verify each of 13 bug-closure assertions:
- B-260519-02: new thread snapshot returns 200 + empty since_cursors (DevTools Network)
- B-260519-04: LangSmith shows ChatAnthropic trace (Anthropic) and ChatOpenrouter (OpenRouter)
- B-260519-05: backend log line, payload field, UI line, Settings dropdown
- B-260519-07: stdout neutral/emerald, stderr red (visual inspection of execute_code output)
- B-260519-08: dissertation pptx succeeds first try (requires SANDBOX_IMAGE env var set + image built)
- B-260519-09: output files at /sandbox/output/ with working download links
- B-260519-10: exactly one tool card per tool_call_id (visual inspection)
- B-260519-11 + BUG-260514-01: per-cell delta files only + pinned Final outputs panel at end
- Test 2 dual-/messages: DevTools Network shows 1 /snapshot, 0 /messages per thread switch

**Why human:** 13-item checklist across 3 providers. Requires live LangSmith, Docker sandbox image build, live Supabase + Redis, all provider API keys. LangSmith trace names (ChatAnthropic vs ChatOpenrouter) can only be verified in the LangSmith UI.

**Setup note:** Build sandbox image first: `docker build -f backend/Dockerfile.sandbox -t agentic-rag-sandbox:075.1 backend/` then set `SANDBOX_IMAGE=agentic-rag-sandbox:075.1` in `backend/.env` and restart uvicorn.

**Resume signal:** `"approved-plan04-all-bugs-closed"` with count of checklist items verified. If any item fails, list bug ID + evidence + recommendation (re-plan vs accept-as-known-issue).

---

## Gaps Summary

No hard gaps requiring re-planning exist. All 13 automatable must-haves are VERIFIED. The remaining `human_needed` status reflects three deferred UAT checkpoint tasks (one per Plan 01, 03, 04) that the parallel-executor intentionally parked for the orchestrator to route as HUMAN-UAT.md.

**Advisory items from code review (non-blocking, no re-plan required):**
- IN-03: `finalOutputFiles` pinned panel shows filenames only — no download affordance. The `url` field arrives but is not rendered as a link/button. Recommend a narrow follow-up task to add `downloadSandboxOutput(f.url, f.filename)` button per the REVIEW's suggested fix.
- IN-02: `harvest_output_files` still uploads all files before delta-filtering — wasteful re-upload per iteration. Can be fixed by skipping upload for filenames already in `previous_files` (REVIEW IN-02 fix suggestion).
- WR-02: Double `getSnapshot` call per transient event. Low-risk in practice; can be eliminated by threading the snapshot object from `_isTransientStreamEnd` to `_reattachAfterTransient` (REVIEW WR-02 fix).
- WR-01: `onToolEnd` matches by name, not id — non-deterministic under same-name parallel tools. Mitigated by `parallel_tool_calls: false` constraint but worth fixing via optional `id` parameter (REVIEW WR-01 fix).
- WR-03: ToolCallPanel dedup fallback key collapses for preparing entries with undefined startedAt. Mitigated by `parallel_tool_calls: false`. Fix: include array index as tiebreaker (REVIEW WR-03 fix).

---

_Verified: 2026-05-20T01:00:00Z_
_Verifier: Claude (gsd-verifier)_
