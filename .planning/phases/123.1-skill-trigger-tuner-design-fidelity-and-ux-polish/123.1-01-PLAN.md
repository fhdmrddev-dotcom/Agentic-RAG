---
phase: 123.1-skill-trigger-tuner-design-fidelity-and-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/077_tuner_runs.sql
  - supabase/full-schema.sql
  - backend/app/api/skill_tuner.py
  - backend/app/services/skill_tuner_service.py
  - backend/tests/integration/test_skill_tuner_routes.py
  - frontend/src/lib/api.ts
autonomous: false
requirements: [D-01, D-05, D-07, D-08]
user_setup: []

must_haves:
  truths:
    - "D-01 (scope anchor): This phase delivers the HIGH trio (scoreboard layout, seeded-case visibility, result persistence) + the MED items across plans 01-04; LOW cosmetics are deferred (none planned). This plan owns the HIGH backend half (D-05/D-07/D-08); D-02/D-03/D-04/D-06/D-09/D-10/D-11/D-12 are covered by plans 02-04."
    - "D-08: A second user CANNOT read another user's non-global skill's latest tuner run — the GET-latest route returns 404 (never 403)."
    - "D-08: A global skill's latest tuner run is visible to any user who can see the skill (owner OR is_global)."
    - "D-07: After a tuning run completes, the latest result is upserted to tuner_runs (one row per skill, UNIQUE(skill_id)) and survives a Redis flush / new request."
    - "D-07: A second run for the same skill OVERWRITES the row (latest-wins via on_conflict=skill_id), never accumulates."
    - "D-05: The seeded-cases GET returns should_fire + should_not prompts WITH provenance (seeded/sibling) for an owned skill; 404 cross-user; sibling list stays owner-scoped (no catalog leak)."
  artifacts:
    - path: "supabase/migrations/077_tuner_runs.sql"
      provides: "Owner-scoped tuner_runs table + RLS + UNIQUE(skill_id)"
      contains: "CREATE TABLE"
    - path: "backend/app/api/skill_tuner.py"
      provides: "GET /tuner/runs/latest, GET /tuner/cases/seeded, durable upsert in _run_tuner_job"
      contains: "tuner/runs/latest"
    - path: "backend/app/services/skill_tuner_service.py"
      provides: "Provenance-carrying seeded cases"
      contains: "provenance"
    - path: "frontend/src/lib/api.ts"
      provides: "getTunerLatest + getSeededCases wire functions + types"
      contains: "getTunerLatest"
    - path: "backend/tests/integration/test_skill_tuner_routes.py"
      provides: "Cross-user-404 + persistence + seeded-cases tests"
      contains: "test_get_latest_cross_user_returns_404"
  key_links:
    - from: "backend/app/api/skill_tuner.py GET /tuner/runs/latest"
      to: "_fetch_owned_or_global_skill"
      via: "owner-scope 404 gate before the tuner_runs read"
      pattern: "_fetch_owned_or_global_skill"
    - from: "backend/app/api/skill_tuner.py _run_tuner_job"
      to: "tuner_runs table"
      via: "run_in_threadpool upsert on_conflict=skill_id alongside the Redis stash"
      pattern: "on_conflict.*skill_id"
---

<objective>
Build the durable-persistence + seeded-case backend seam — the highest-risk, net-new infrastructure of Phase 123.1. Create the owner-scoped `tuner_runs` table (migration 077), persist the latest result per skill on every run (latest-wins upsert), expose a GET-latest route for rehydration-on-open, and expose the already-computed seeded cases (with provenance) via a lightweight GET so the editor can show + edit them before a run. Add the matching frontend wire functions/types.

Purpose: Phase-123 tuner results live only in Redis (`tuner_result:{run_id}`, TTL 600s) → lost on refresh (BUG-260624-01 HIGH #3); seeded cases are computed server-side but never echoed → editor shows "0 cases" (HIGH #2 / WR-05). This plan closes both at the data layer. It is the dependency root for the frontend hydrate/rehydrate work (Plan 04).
Output: migration 077 (applied + full-schema regenerated), two new owner-scoped GET routes, a DB upsert in the background job, provenance-carrying seed output, frontend wire functions, and the load-bearing cross-user-404 + persistence tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-CONTEXT.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-RESEARCH.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-PATTERNS.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the shipped Phase-123 code. Use directly — no codebase exploration. -->

The owner-scope gate to REUSE on every new route (skill_tuner.py:133-162 — the SOLE leak gate; get_supabase() is service-role, RLS bypassed):
  async def _fetch_owned_or_global_skill(supabase, skill_id, user_id) -> dict
    # supabase.table("skills").select(...).eq("id", skill_id).or_(f"user_id.eq.{user_id},is_global.eq.true").limit(1)
    # wrapped in run_in_threadpool; 404 (never 403) on miss; PostgREST malformed-uuid → 404.

The GET to MIRROR for the new GET-latest (skill_tuner.py:550-578, get_tuner_results):
  @router.get("/{skill_id}/tuner/runs/{run_id}")
  async def get_tuner_results(skill_id, run_id: UUID, current_user=Depends(get_current_user),
                              supabase=Depends(get_supabase), redis=Depends(get_redis)):
      await _fetch_owned_or_global_skill(supabase, skill_id, current_user["id"])   # 404 gate
      # ...reads redis tuner_result:{run_id}, returns TunerScoreboard JSON...

The existing Redis stash site to add the DB upsert ALONGSIDE (skill_tuner.py:381-389, inside _run_tuner_job):
  try:
      await redis.set(f"tuner_result:{run_id}", json.dumps(scoreboard), ex=_TUNER_BUFFER_TTL_S)
  except Exception:
      logger.exception("tuner result stash failed for run %s", run_id)

_run_tuner_job CURRENT signature (skill_tuner.py:200-210) — keyword-only, does NOT receive supabase:
  async def _run_tuner_job(*, redis, run_id: UUID, skill_id: str, skill: dict,
                           cases: list[dict], targets: list[dict], n: int, user_id: str) -> None
  # builder_model is resolved inside the job (~:239); cases/targets are passed in.
  # The asyncio.create_task(_run_tuner_job(...)) call site is at skill_tuner.py:493-504.
  # The route that spawns it HAS supabase via Depends(get_supabase) (~:415).

The seed function to extend with provenance (skill_tuner_service.py:234-268, auto_seed_cases):
  def auto_seed_cases(skill: dict, sibling_skills: list[dict]) -> dict[str, list[str]]
  # should_fire: skill.description, "I need to {description}", "Use the {name} skill." → "seeded"
  # should_not:  each sibling.description (owner-scoped) → "sibling"; + _GENERIC_OFF_TOPIC (5 prompts) → "seeded"
  # Siblings come ONLY from fetch_owner_scoped_siblings (:271-293, .or_(user_id.eq,is_global.eq.true)) — keep this.

Frontend wire analog to MIRROR (api.ts:2727-2740, getTunerResults) and the tuner wire-type block (api.ts:2650-2697):
  export async function getTunerResults(skillId, runId): Promise<TunerScoreboard>
  # uses getAuthHeaders() + ApiError; 404 → throws. For getTunerLatest, 404 → return null (no run yet).
  # TunerScoreboard / TunerCandidate / TunerCell already defined (:2663-2688); TunerCell.score exists (:2666-2671).

RLS templates (verbatim) to compose 077 from:
  - 017_skills.sql:73-82 — child-of-skills SELECT (owner OR EXISTS(skills.is_global)) — USE THIS form for tuner_runs SELECT.
  - 035_runs_table.sql:47-53 — service-role-write, SELECT-only RLS (NO insert/update/delete policies — service-role bypasses RLS).
  - 056_workflow_definitions.sql:9-11,16-33 — jsonb column + UNIQUE constraint + the apply-discipline header comment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — author migration 077 + author the failing backend tests</name>
  <files>supabase/migrations/077_tuner_runs.sql, backend/tests/integration/test_skill_tuner_routes.py</files>
  <read_first>
    - supabase/migrations/017_skills.sql (RLS template — :25-41 parent, :73-82 child-of-skills SELECT)
    - supabase/migrations/035_runs_table.sql (:15-17,47-53 — service-role-write, SELECT-only, durable-companion-to-Redis framing)
    - supabase/migrations/056_workflow_definitions.sql (:9-11 apply-discipline header, :16-33 jsonb + UNIQUE + index)
    - supabase/migrations/076_messages_origin.sql (confirms 077 is the next number — no letter suffix)
    - backend/tests/integration/test_skill_tuner_routes.py (:162-196 test_start_cross_user_returns_404 with the `assert skills_execute_called` anti-vacuous guard; :199-224 test_get_results_cross_user_returns_404 — the exact shape to mirror)
  </read_first>
  <behavior>
    - test_get_latest_cross_user_returns_404: user B reading user A's non-global skill latest run → 404, detail "Skill not found".
    - test_get_latest_global_skill_visible: a global skill's latest run is readable by a second user (owner-OR-global gate).
    - test_latest_result_persists_after_run: after a run, the tuner_runs row exists for that skill_id and GET-latest returns its scoreboard.
    - test_rerun_overwrites_latest: a second run for the same skill upserts (one row, UNIQUE(skill_id)) — not two rows.
    - test_seeded_cases_returned: GET seeded-cases returns should_fire + should_not WITH provenance for an owned skill; cross-user → 404.
    - Reproduce the `assert skills_execute_called` anti-vacuous guard (mirror :195-196) so a new test cannot pass against an unregistered route.
  </behavior>
  <action>
    Author `supabase/migrations/077_tuner_runs.sql` — AUTHOR ONLY (Task 2 applies it). Table `public.tuner_runs`: `id uuid PK DEFAULT gen_random_uuid()`, `skill_id uuid NOT NULL REFERENCES public.skills(id) ON DELETE CASCADE`, `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE` (whoever last ran it), `run_id uuid NOT NULL`, `scoreboard jsonb NOT NULL DEFAULT '{}'` (the full TunerScoreboard), `builder_model text NOT NULL DEFAULT ''`, `target_count integer NOT NULL DEFAULT 0`, `case_count integer NOT NULL DEFAULT 0`, `created_at`/`updated_at timestamptz NOT NULL DEFAULT now()`, and `CONSTRAINT tuner_runs_skill_unique UNIQUE (skill_id)` (the latest-wins upsert key — resolved A3 = one shared latest per skill). Add `CREATE INDEX idx_tuner_runs_user_id ON public.tuner_runs (user_id)`. `ENABLE ROW LEVEL SECURITY`. ONE SELECT policy mirroring the `skill_files` child-of-skills form (`auth.uid() = user_id OR EXISTS(SELECT 1 FROM public.skills WHERE skills.id = tuner_runs.skill_id AND skills.is_global = true)`). NO INSERT/UPDATE/DELETE policies (service-role writes bypass RLS — 035 pattern). Reproduce the 056 apply-discipline header comment (apply via Supabase SQL editor, NOT db push/reset; then regenerate full-schema; authored-only, Plan applies in Task 2). Document the global-skill latest-ownership semantics (user_id = whoever last ran; SELECT-by-skill identical for all global viewers) in a table comment.

    Then add the five tests to `test_skill_tuner_routes.py`, mirroring the existing cross-user-404 shape verbatim (dependency_overrides for get_supabase/get_current_user/get_redis; httpx ASGITransport; 404 assert with body in the message). Use the `_supabase_returning_skill([])` no-row fixture for the cross-user cases. These tests will be RED until Task 3 ships the routes/upsert and Task 2 applies the table — that is expected.
  </action>
  <verify>
    <automated>cd backend && grep -c "tuner_runs_skill_unique UNIQUE (skill_id)" ../supabase/migrations/077_tuner_runs.sql</automated>
    <automated>cd backend && python -m pytest tests/integration/test_skill_tuner_routes.py -k "latest or seeded or overwrite or persists" --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `077_tuner_runs.sql` exists; `grep "UNIQUE (skill_id)"` → 1; contains exactly ONE `FOR SELECT` policy and ZERO `FOR INSERT|UPDATE|DELETE` policies (`grep -cE "FOR (INSERT|UPDATE|DELETE)" supabase/migrations/077_tuner_runs.sql` == 0).
    - The migration header contains the apply-via-SQL-editor instruction string and "never `db push`" (or equivalent), copied from the 056 pattern.
    - `pytest --collect-only` lists all five new test ids: `test_get_latest_cross_user_returns_404`, `test_get_latest_global_skill_visible`, `test_latest_result_persists_after_run`, `test_rerun_overwrites_latest`, `test_seeded_cases_returned`.
    - The new tests reproduce the `assert ..._execute_called` anti-vacuous guard (grep the test file for an execute-called assertion in the new tests).
    - No `077` letter-suffix variant exists; the file name is exactly `077_tuner_runs.sql`.
  </acceptance_criteria>
  <done>Migration 077 authored with UNIQUE(skill_id) + child-of-skills SELECT RLS + no write policies; five failing/collected tests present mirroring the cross-user-404 pattern with anti-vacuous guards.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply migration 077 to the live local DB + regenerate full-schema</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - supabase/migrations/077_tuner_runs.sql (the file authored in Task 1 — paste its full contents)
    - CLAUDE.md (migration discipline: apply via SQL editor, NEVER `supabase db push`/`db reset`; then regenerate `full-schema.sql`)
    - supabase/SETUP.md (the migration-apply + full-schema regeneration story)
  </read_first>
  <action>
    This is the mandatory manual schema-apply gate (CLAUDE.md forbids `supabase db push`/`db reset` for local dev — they wipe dev data). The executor pauses here for the operator to: (1) paste the full contents of `supabase/migrations/077_tuner_runs.sql` into the Supabase SQL editor for the local project and run it; (2) run `bash scripts/regenerate-full-schema.sh` from the repo root (NO `--reset` — live-DB dump only) to rebuild `supabase/full-schema.sql`; (3) confirm the `tuner_runs` table is live; (4) commit BOTH `077_tuner_runs.sql` and the regenerated `full-schema.sql`. This ordering is load-bearing: build/type checks pass WITHOUT the table (a false-positive state), so the table MUST physically exist in the live DB before the Task-3 persistence + RLS-leak integration tests can be trusted. Do NOT auto-advance — `autonomous: false`.
  </action>
  <what-built>
    Migration `077_tuner_runs.sql` is authored on disk but NOT yet in the live local DB. Build/type checks pass WITHOUT the table — a false-positive verification state. The D-07 persistence + RLS-leak integration tests (Task 3 / Plan 04) cannot be trusted until the `tuner_runs` table physically exists in the live DB. This is the project's mandatory manual schema-apply step (CLAUDE.md forbids `db push`/`db reset`).
  </what-built>
  <how-to-verify>
    1. Open the Supabase SQL editor for the local project (`supabase start` already running).
    2. Paste the full contents of `supabase/migrations/077_tuner_runs.sql` and run it. Confirm no errors.
    3. From the repo root run: `bash scripts/regenerate-full-schema.sh` (NO `--reset` — live-DB dump only; `--reset` is destructive and forbidden here).
    4. Verify the table exists: confirm `tuner_runs` appears in `supabase/full-schema.sql` (e.g. `grep -n "CREATE TABLE public.tuner_runs" supabase/full-schema.sql`) AND in the live DB (`\dt` in the SQL editor / psql shows `public.tuner_runs`).
    5. Commit BOTH `supabase/migrations/077_tuner_runs.sql` and the regenerated `supabase/full-schema.sql`. Never hand-edit `full-schema.sql`.
  </how-to-verify>
  <verify>
    <automated>grep -c "CREATE TABLE public.tuner_runs" supabase/full-schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE public.tuner_runs" supabase/full-schema.sql` == 1 (the regenerated artifact contains the new table).
    - `full-schema.sql` also contains the `tuner_runs` SELECT RLS policy and the `UNIQUE (skill_id)` constraint (proves the live DB picked up the full migration, not a partial paste).
    - The live local DB shows `public.tuner_runs` in `\dt` (operator-confirmed).
    - `full-schema.sql` was produced by `regenerate-full-schema.sh` (no `--reset`), not hand-edited.
  </acceptance_criteria>
  <done>Migration 077 applied to the live local DB via the SQL editor; `full-schema.sql` regenerated (no --reset) and contains the tuner_runs table + SELECT RLS + UNIQUE(skill_id); both files committed.</done>
  <resume-signal>Type "applied" once the table is live and full-schema.sql is regenerated + committed, or describe any apply error.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Ship the two GET routes, the durable upsert, provenance seeding, and the frontend wire</name>
  <files>backend/app/api/skill_tuner.py, backend/app/services/skill_tuner_service.py, frontend/src/lib/api.ts</files>
  <read_first>
    - backend/app/api/skill_tuner.py (:133-162 owner-scope gate; :200-210 _run_tuner_job signature; :381-389 Redis stash site; :415 route's get_supabase Depends; :466-477 the `if body.cases:` no-reseed branch; :493-504 create_task call site; :515-521 POST response; :550-578 get_tuner_results — the GET to mirror)
    - backend/app/services/skill_tuner_service.py (:61-67 _GENERIC_OFF_TOPIC; :234-268 auto_seed_cases; :271-293 fetch_owner_scoped_siblings — the owner-scoped leak gate to preserve)
    - frontend/src/lib/api.ts (:2650-2697 tuner wire types incl. TunerScoreboard/TunerCell.score; :2727-2740 getTunerResults — the fn to mirror)
  </read_first>
  <action>
    Backend service (`skill_tuner_service.py`): extend `auto_seed_cases` (or add a sibling fn `seed_cases_with_provenance`) so the seed carries provenance — return `should_fire`/`should_not` as lists of `(prompt, provenance)` pairs where the skill's own description/paraphrase/"Use the {name} skill." are `"seeded"`, sibling descriptions are `"sibling"`, and `_GENERIC_OFF_TOPIC` are `"seeded"`. Do NOT let the function read the DB itself — siblings still arrive only from the owner-scoped `fetch_owner_scoped_siblings`. `held` is NOT authored by the backend (the 60/40 split is computed at scoring time) — return only `seeded`/`sibling`. Preserve the existing `auto_seed_cases` string-only contract used by the run path (either keep a thin string-only wrapper or update both call sites — do not break the `_run_tuner_job` seed).

    Backend route (`skill_tuner.py`): (a) Add `GET /{skill_id}/tuner/runs/latest` — mirror `get_tuner_results`: `_fetch_owned_or_global_skill` 404-gate, then a `run_in_threadpool`-wrapped read of `tuner_runs` by `skill_id` (`.eq("skill_id", skill_id).limit(1)`); return the stored `scoreboard` (plus `builder_model`/`target_count`/`case_count` for attribution) or 404/empty-null when no row exists. (b) Add `GET /{skill_id}/tuner/cases/seeded` — owner-gate, fetch siblings via `fetch_owner_scoped_siblings` (already threadpool-wrapped), call the provenance seed, return `{ should_fire: [{prompt, provenance}], should_not: [{prompt, provenance}] }`. (c) Thread `supabase` into `_run_tuner_job`: add `supabase` to the keyword-only signature (:200-210) AND the `asyncio.create_task(_run_tuner_job(...))` call (:493-504, the route has it via Depends at :415). At the Redis-stash site (:381-389), add a best-effort `run_in_threadpool` upsert into `tuner_runs` with `on_conflict="skill_id"` (payload: `skill_id`, `user_id`, `run_id`, `scoreboard`, `builder_model` resolved at :239, `target_count=len(targets)`, `case_count=len(cases)`, `updated_at="now()"`), wrapped in `try/except` + `logger.exception` so a DB hiccup never crashes the run (Redis stash still serves the per-run-id GET). Do NOT modify the shared run-buffer consumer / agent loop / trim-pin (red line).

    Frontend (`api.ts`): add `getTunerLatest(skillId): Promise<TunerScoreboard | null>` (mirror `getTunerResults`; 404 → `null`, not throw) and `getSeededCases(skillId): Promise<{ should_fire: {prompt:string; provenance:string}[]; should_not: {prompt:string; provenance:string}[] }>` with matching `interface`s next to the existing tuner wire-type block. Reuse `getAuthHeaders()` + `ApiError`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_skill_tuner_routes.py -k "latest or seeded or overwrite or persists or cross_user" -x</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pytest test_skill_tuner_routes.py -k "latest or seeded or overwrite or persists or cross_user"` → all GREEN (the five Task-1 tests now pass against the live table from Task 2).
    - `skill_tuner.py` contains a `@router.get(".../tuner/runs/latest")` and a `@router.get(".../tuner/cases/seeded")`, each calling `_fetch_owned_or_global_skill` before any read (`grep -c _fetch_owned_or_global_skill` increased by ≥2).
    - `_run_tuner_job` signature includes `supabase`, and the `asyncio.create_task` call passes it (`grep -A8 "create_task(_run_tuner_job" skill_tuner.py` shows `supabase=`).
    - The upsert uses `on_conflict="skill_id"` and is wrapped in `run_in_threadpool` (`grep -E "on_conflict.*skill_id"` → ≥1; the call is inside a `run_in_threadpool(...)`).
    - The seed provenance output contains `"sibling"` and `"seeded"` and never `"held"` (`grep -c '"held"'` in the new seed fn == 0).
    - `tsc --noEmit` clean; `api.ts` exports `getTunerLatest` and `getSeededCases`.
    - No edit to the shared consumer / agent_loop / trim path (`git diff --name-only` lists only the three files in this task + Task 1/2 files).
  </acceptance_criteria>
  <done>GET-latest + seeded-cases routes live and owner-gated; durable upsert persists latest-per-skill (overwrite on re-run); seed carries seeded/sibling provenance with the leak gate intact; frontend wire functions exist and typecheck; all five backend tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tuner GET routes | Untrusted `skill_id` path param; caller identity from bearer token. `get_supabase()` is service-role (RLS bypassed) so app-code `.or_()` scoping is the SOLE leak gate. |
| background job → tuner_runs table | Service-role write must stamp the correct `user_id`; a wrong stamp leaks/mis-attributes a result. |
| seed → client | Seeded `should_not` cases include sibling-skill descriptions — must stay owner-scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123.1-01 | Information Disclosure | `GET /tuner/runs/latest` | mitigate | Reuse `_fetch_owned_or_global_skill` 404-gate before the `tuner_runs` read; RLS owner+global SELECT as defense-in-depth. Load-bearing proof: `test_get_latest_cross_user_returns_404` (non-vacuous — drives the real route with two distinct users). |
| T-123.1-02 | Information Disclosure | `GET /tuner/cases/seeded` | mitigate | Owner-gate the skill; siblings come ONLY from `fetch_owner_scoped_siblings` (`.or_(user_id.eq,is_global.eq.true)`); another user's private skill never reaches the seed. Cross-user → 404. |
| T-123.1-03 | Elevation / Disclosure | `_run_tuner_job` upsert | mitigate | Service-role write always stamps `user_id` from the job's `user_id` (originating `current_user["id"]`); never an unscoped `select *`; read path goes through the app-code owner-scope. |
| T-123.1-04 | Information Disclosure | malformed `skill_id` | mitigate | `_fetch_owned_or_global_skill` catches PostgREST errors → 404 (never a 500/error-shape leak); the new routes reuse it. |
| T-123.1-05 | Tampering | global-skill latest-result row | accept | A global skill's `user_id` is "whoever last ran it"; the SELECT-by-skill is identical for all global viewers (documented in the migration comment). Low risk — global skills are an explicitly shared scope. |
| T-123.1-SC | Tampering | npm/pip installs | mitigate | This plan installs NO packages (zero new deps per RESEARCH Package Legitimacy Audit). Only artifact is the project-internal migration 077. No slopcheck needed. |
</threat_model>

<verification>
- Backend: `pytest backend/tests/integration/test_skill_tuner_routes.py -x` green (incl. the five new tests).
- Migration applied to live DB and `full-schema.sql` regenerated (Task 2 gate); `grep "CREATE TABLE public.tuner_runs" supabase/full-schema.sql` == 1.
- Frontend: `npx tsc --noEmit` clean.
- Red line: `git diff` touches NO shared chat-path / agent-loop / trim-pin file.
</verification>

<success_criteria>
- `tuner_runs` table live (owner-scoped RLS, UNIQUE(skill_id)); latest result persists + overwrites on re-run; survives Redis flush.
- Two owner-gated GET routes (latest + seeded-cases) return correctly and 404 cross-user.
- Seed output carries `seeded`/`sibling` provenance; sibling list stays owner-scoped.
- Frontend wire functions/types exist and typecheck.
- D-01 (scope anchor), D-05, D-07, D-08 covered.
</success_criteria>

<output>
Create `.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-01-SUMMARY.md` when done
</output>
