---
phase: 123.1-skill-trigger-tuner-design-fidelity-and-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/skills/tuner/ProviderScoreboard.tsx
  - frontend/src/components/skills/tuner/ProviderScoreboard.test.tsx
  - frontend/src/components/skills/tuner/CandidateCard.tsx
  - frontend/src/components/skills/tuner/CandidateCard.test.tsx
autonomous: true
requirements: [D-02, D-04, D-11, D-12]
user_setup: []

must_haves:
  truths:
    - "D-02: ProviderScoreboard renders as vertical full-width provider rows (not a fixed repeat(N,1fr) horizontal grid), legible at 8+ providers."
    - "D-02/D-12: Each row shows a colored magnitude bar driven by the server-computed combined score (TunerCell.score), with the leading combined score number."
    - "D-02: Both honest sub-scores (fires recall + no-false rail) remain visible on every row — the false-fire rail is never a hidden aggregate."
    - "D-11: A ~1500-char candidate description line-clamps with an expand toggle in CandidateCard header AND in the diff-confirm strip (old + new), so the held-out score and the Use action stay visible."
    - "D-12: The per-cell colored magnitude bar + leading combined score (TunerCell.score, previously unused) are rendered."
  artifacts:
    - path: "frontend/src/components/skills/tuner/ProviderScoreboard.tsx"
      provides: "Vertical-rows scoreboard with magnitude bar + combined score"
      contains: "score"
    - path: "frontend/src/components/skills/tuner/CandidateCard.tsx"
      provides: "Line-clamp + expand on descriptions"
      contains: "line-clamp"
  key_links:
    - from: "ProviderScoreboard.tsx row"
      to: "TunerCell.score"
      via: "magnitude bar width + leading number derived from the server-computed combined score"
      pattern: "\\.score"
    - from: "CandidateCard.tsx description"
      to: "expand toggle state"
      via: "useState show-more controlling line-clamp"
      pattern: "line-clamp"
---

<objective>
Restore the approved sketch-041/042 scoreboard design and add description line-clamps — the well-understood frontend polish that fixes the "cramped/illegible at 8 providers" defect (BUG-260624-01 HIGH #1) and the "1500-char description buries the score" defect (MED #5). Re-render `ProviderScoreboard` as vertical full-width provider rows with a per-row colored magnitude bar driven by the server's combined score, keeping both honest sub-scores visible; line-clamp candidate descriptions with an expand toggle in `CandidateCard`.

Purpose: The build replaced sketch 041's `flex-direction:column` vertical rows with a fixed `repeat(N,1fr)` horizontal grid that crams at the org's real provider count (8). `TunerCell.score` (server-computed `(fires+no_false)/2`) is already on the wire but unused. Long descriptions (e.g. `docx` ~1500 chars) are uncapped and bury the held-out score + Use action. This is component-level work against an already-approved sketch — no backend dependency.
Output: a legible vertical-rows scoreboard with magnitude bars + combined score, and clamp/expand on every description surface.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-CONTEXT.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@.planning/sketches/041-tuner-surface-shell/index.html
@.planning/sketches/042-scoreboard-and-candidates/index.html

<interfaces>
<!-- Current shapes the executor extends. From the shipped Phase-123 code. -->

ProviderScoreboard.tsx CURRENT grid to REPLACE (:49-87):
  <div data-testid="provider-scoreboard" className="grid gap-2"
       style={{ gridTemplateColumns: `repeat(${Math.max(cells.length,1)}, minmax(0,1fr))` }}>
  # KEEP: railTone() tinting (:38-47); the BOTH-sub-scores-always-visible block (:72-82);
  #       data-testid="scoreboard-cell" / "cell-fires" / "cell-no-false" (the existing test reads them).

TunerCell wire type (api.ts:2666-2671 — already on the wire, currently UNUSED in this component):
  interface TunerCell { provider: string; model: string; fires: number; no_false: number; score: number; ... }
  # score = server-computed (fires + no_false)/2. Drive the magnitude bar width + leading number from it.

TunerCandidate.is_baseline (api.ts:2679; set in the job at skill_tuner.py:269-270) — the CURRENT/live description is the baseline candidate (index 0). D-04's standalone block renders ProviderScoreboard from scoreboard.candidates.find(c => c.is_baseline).cells.

Sketch 041 vertical-rows target (verbatim — translate CSS vars to the project's existing Tailwind / hsl(var(--panel-status-*)) tokens already used at ProviderScoreboard.tsx:41,44-46):
  .scoreboard { display: flex; flex-direction: column; gap: 8px; }
  .sb-row { display: grid; grid-template-columns: 1.3fr 1fr 1fr; gap:10px; align-items:center; padding:9px 12px; border:1px solid var(--color-border); border-radius:var(--radius-md); }
  .bar { height:5px; border-radius:3px; overflow:hidden; }
  .bar > i.good { background:var(--color-success); }  /* >= ~0.85 */
  .bar > i.mid  { background:var(--color-warning); }  /* ~0.6-0.85 */
  .bar > i.bad  { background:var(--color-danger); }   /* < 0.6 */

CandidateCard.tsx uncapped descriptions to clamp:
  :72   header desc  <p className="text-sm ... whitespace-pre-wrap break-words">{candidate.description}</p>
  :96-108 diff strip — BOTH old (line-through currentDescription) AND new (candidate.description) uncapped.
  # Existing useState idiom at :32-34 (confirming/saving/done) — add a "show more" toggle the same way.
  # Clamp must NOT bury the held-out score (:74-79) or the Use action (:127-132).

A1 RESOLVED: project Tailwind is 3.4.19 → native `line-clamp-N` ships. No plugin needed. Confirm as the FIRST acceptance criterion below; if a future build lacks it, enable @tailwindcss/line-clamp.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProviderScoreboard → vertical rows + magnitude bar + combined score (D-02/D-04/D-12)</name>
  <files>frontend/src/components/skills/tuner/ProviderScoreboard.tsx, frontend/src/components/skills/tuner/ProviderScoreboard.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/tuner/ProviderScoreboard.tsx (:1-21 docstring "renders server-computed scores only"; :38-47 railTone; :49-87 the grid to replace; :72-82 both-sub-scores rule)
    - frontend/src/components/skills/tuner/ProviderScoreboard.test.tsx (existing test — extend, don't rewrite; it reads scoreboard-cell / cell-fires / cell-no-false testids)
    - .planning/sketches/041-tuner-surface-shell/index.html (:144 .scoreboard column, :158-162 .bar, :276-294 standalone per-provider block)
    - .planning/sketches/042-scoreboard-and-candidates/index.html (:180-182 two-sub-scores rule, :202-234 .pbar/.big per-cell bar + combined score)
  </read_first>
  <behavior>
    - Renders one full-width row per provider cell (flex-direction column container), NOT a repeat(N,1fr) horizontal grid.
    - Each row renders a colored magnitude bar whose width + tone come from TunerCell.score (good ≥~0.85, mid ~0.6-0.85, bad <0.6) and a leading combined-score number.
    - Both fires (recall) and no-false (false-fire rail) remain visible on every row — neither hidden behind the aggregate.
    - 8-cell input renders 8 legible rows (no horizontal cram).
    - The existing testids (scoreboard-cell / cell-fires / cell-no-false) still resolve.
  </behavior>
  <action>
    Replace the horizontal `grid` + `gridTemplateColumns: repeat(...)` (:50-55) with a vertical flex-column container per sketch 041 `.scoreboard`. Render each cell as an `.sb-row`-equivalent: provider · model | leading combined-score number (`cell.score`, formatted) | colored magnitude bar (`<div>` track + inner `<i>`-equivalent width `${cell.score*100}%`, tone class by threshold). Reuse the existing `railTone()` thresholds (:38-47) and translate sketch CSS vars to the project's Tailwind / `hsl(var(--panel-status-*))` tokens the component already uses. KEEP both-sub-scores-always-visible (`fires` + `no-false` rails, :72-82) and KEEP the `data-testid` attributes the existing test reads. Add a `data-testid="scoreboard-bar"` (or similar) so the magnitude bar is assertable. Render server scores ONLY — never compute the held-out split or `(fires+no_false)/2` in the component (the docstring rule; `score` already arrives on the wire). Extend `ProviderScoreboard.test.tsx`: assert 8 cells → 8 rows, the bar element renders with a width derived from `score`, the leading combined-score number shows, and both sub-scores are present.
  </action>
  <verify>
    <automated>cd frontend && npm test -- ProviderScoreboard --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "line-clamp" frontend/tailwind.config.*` OR Tailwind 3.4.19 native line-clamp confirmed (relevant to Task 2; for this task: no horizontal grid remains — `grep -c "repeat(" ProviderScoreboard.tsx` == 0).
    - `npm test -- ProviderScoreboard --run` GREEN; the extended test asserts: 8 cells → 8 rows; a magnitude-bar element renders with a width tied to `cell.score`; the leading combined-score number renders; both `cell-fires` and `cell-no-false` present per row.
    - `grep -c "flex-col" ProviderScoreboard.tsx` ≥ 1 (vertical container) and the `repeat(N,1fr)` style is gone.
    - The component still imports/uses no client-side score math — `grep -E "fires \\+ no_false|/ 2" ProviderScoreboard.tsx` == 0 (uses server `score`).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Vertical full-width rows with per-row colored magnitude bar + leading combined score from TunerCell.score; both sub-scores visible; existing testids preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Line-clamp + expand on candidate descriptions (D-11)</name>
  <files>frontend/src/components/skills/tuner/CandidateCard.tsx, frontend/src/components/skills/tuner/CandidateCard.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/tuner/CandidateCard.tsx (:32-34 existing useState idiom; :72 header desc uncapped; :74-79 held-out score that must stay visible; :96-108 diff strip both old+new uncapped; :127-132 the Use action that must stay visible)
    - .planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-PATTERNS.md ("No Analog Found" — clamp is net-new; Tailwind line-clamp + useState toggle)
  </read_first>
  <behavior>
    - A long (~1500-char) description renders clamped (default ~3 lines) with a visible "Show more"/"Show less" toggle.
    - Expanding reveals the full text; collapsing re-clamps.
    - The held-out score and the Use action remain visible regardless of description length (clamp prevents the description from pushing them off-screen).
    - Both the header description AND both sides of the diff-confirm strip (line-through current + new) clamp+expand.
  </behavior>
  <action>
    Add a local `useState` "expanded" toggle (mirror the existing `confirming`/`saving`/`done` idiom at :32-34). Apply Tailwind `line-clamp-3` (native — Tailwind 3.4.19) to the header description (:72) and to BOTH strings in the diff-confirm strip (:96-108), toggled off when expanded. Add a small "Show more"/"Show less" text button that flips the state; keep it out of the tab order trap (a `<button>` with accessible label). Ensure the clamp wraps the description ONLY — the held-out score (:74-79) and the Use action (:127-132) are siblings, not clamped. Extend `CandidateCard.test.tsx`: render with a ~1500-char description, assert the clamp class is present by default, assert the toggle reveals full text (class removed) on click, and assert the Use action + held-out score are present in both states.
  </action>
  <verify>
    <automated>cd frontend && npm test -- CandidateCard --run</automated>
  </verify>
  <acceptance_criteria>
    - FIRST: confirm native `line-clamp-N` is available — Tailwind is 3.4.19 (`grep '"tailwindcss"' frontend/package.json` shows ^3.4.x ≥3.3); no plugin needed. (If a future build downgrades below 3.3, the task enables `@tailwindcss/line-clamp`.)
    - `grep -c "line-clamp" CandidateCard.tsx` ≥ 1 (applied to header + diff strip).
    - `npm test -- CandidateCard --run` GREEN; the test asserts default-clamped, toggle-reveals-full, and the Use action + held-out score visible in both states.
    - The clamp wraps only the description elements — the Use button and held-out score are not inside a clamped container.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Header description and both diff-strip sides clamp at ~3 lines with a working show-more/less toggle; held-out score and Use action stay visible; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server scores → client render | The scoreboard must render server-computed scores ONLY; client-side fabrication of the held-out split or combined score would mislead the operator (an honesty boundary, not an auth boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123.1-02-01 | Tampering (display integrity) | ProviderScoreboard render | mitigate | Drive the bar/number from the server `TunerCell.score` only; never compute `(fires+no_false)/2` or the held-out split client-side (enforced by acceptance grep == 0 for client math). Both honest sub-scores always visible. |
| T-123.1-02-02 | Information Disclosure | description clamp | accept | Clamp only hides text behind an expand toggle (full text always reachable) — no data is removed or transformed; purely presentational. |
| T-123.1-02-SC | Tampering | npm installs | mitigate | No new packages (native Tailwind line-clamp; lucide/shadcn already present). No slopcheck needed. |
</threat_model>

<verification>
- `npm test -- ProviderScoreboard --run` and `npm test -- CandidateCard --run` green.
- `npx tsc --noEmit` clean.
- Visual fidelity confirmed against sketches 041/042 at the lived-experience UAT (8-provider legibility — manual, per VALIDATION.md).
- No client-side score math introduced (grep gate).
</verification>

<success_criteria>
- ProviderScoreboard is vertical full-width rows with per-row magnitude bar + leading combined score; both sub-scores visible; legible at 8 providers.
- CandidateCard descriptions clamp+expand in header and diff strip; held-out score + Use action never buried.
- D-02, D-04 (standalone-block render path), D-11, D-12 (per-cell bar/combined score) covered at the component level.
</success_criteria>

<output>
Create `.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-02-SUMMARY.md` when done
</output>
