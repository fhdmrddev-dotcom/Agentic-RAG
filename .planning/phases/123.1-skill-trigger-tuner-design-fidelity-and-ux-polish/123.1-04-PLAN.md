---
phase: 123.1-skill-trigger-tuner-design-fidelity-and-ux-polish
plan: 04
type: execute
wave: 2
depends_on: ["123.1-01", "123.1-02"]
files_modified:
  - frontend/src/pages/SkillTunerPage.tsx
  - frontend/src/pages/SkillTunerPage.test.tsx
  - frontend/src/components/skills/tuner/CaseEditor.tsx
  - frontend/src/components/skills/tuner/CaseEditor.test.tsx
autonomous: true
requirements: [D-03, D-05, D-06, D-07, D-12]
user_setup: []

must_haves:
  truths:
    - "D-03: The results/candidates area spans full width (or the right column gets materially more room) instead of the 50/50 split that wedged it into a half column."
    - "D-05/D-06: On open, the editor hydrates from the backend's seeded cases (real seeded/sibling provenance, no longer 'you'-only) and they are fully editable (edit / remove / add) before the run."
    - "D-04: A standalone per-provider scoreboard block renders at the top of the results area from the BASELINE candidate's cells (the current/live description's scores), separate from per-candidate grids."
    - "D-07: On open, the latest persisted result rehydrates via getTunerLatest → setScoreboard, so a completed result survives refresh/navigation/restart (reconcile-via-fetch, Realtime/SSE is a hint)."
    - "D-12: The pre-run cost preview shows the model count BEFORE the first run (resolved client-side from configured providers, not empty until kickoff), and the 'Built by {builder model} · measured on N models' attribution renders from the persisted run metadata."
  artifacts:
    - path: "frontend/src/pages/SkillTunerPage.tsx"
      provides: "Full-width layout + case hydration + result rehydration + pre-run cost preview"
      contains: "getTunerLatest"
    - path: "frontend/src/components/skills/tuner/CaseEditor.tsx"
      provides: "Hydrated seeded-case rendering (real provenance) — held tag resolved"
      contains: "provenance"
  key_links:
    - from: "SkillTunerPage mount effect"
      to: "getSeededCases + getTunerLatest"
      via: "fetch on open → setCases (real provenance) + setScoreboard (persisted result)"
      pattern: "getTunerLatest|getSeededCases"
    - from: "SkillTunerPage results area"
      to: "baseline candidate scoreboard"
      via: "scoreboard.candidates.find(c => c.is_baseline) rendered via ProviderScoreboard"
      pattern: "is_baseline"
---

<objective>
Wire the backend seam (Plan 01 endpoints) and the polished components (Plan 02) into the page: widen results to full width, hydrate the case editor from the backend's seeded cases so they show + edit before a run, render the standalone per-provider scoreboard block for the live description, rehydrate the persisted latest result on open, and populate the pre-run cost preview + attribution. This is the integration wave — it makes the "0 cases" / "lost on refresh" / "cramped half-column" defects disappear in the lived UI.

Purpose: Closes the page-level halves of BUG-260624-01 HIGH #1/#2/#3 and MED #6/#7. The editor inits `cases=[]` (shows "0 cases" while the run uses hidden seeded cases); the result lives only in Redis (lost on refresh); results are wedged into the half-width column; the cost preview shows no model count until kickoff. Depends on Plan 01 (the GET-latest + seeded-cases routes and `api.ts` wire functions) and Plan 02 (the vertical-rows `ProviderScoreboard` it renders for the standalone block).
Output: a full-width results layout with hydrated/editable seeded cases, a standalone live-description scoreboard, rehydrated persisted results, and a populated pre-run cost preview + attribution.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-CONTEXT.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-PATTERNS.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@.planning/sketches/041-tuner-surface-shell/index.html
@.planning/sketches/043-case-editor-and-live-run/index.html
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-01-PLAN.md

<interfaces>
<!-- Current page/editor shapes + the Plan-01 wire functions this plan consumes. -->

Plan-01 wire functions (api.ts — exist after Plan 01):
  getTunerLatest(skillId): Promise<TunerScoreboard | null>   // 404 → null (no run yet, don't error)
  getSeededCases(skillId): Promise<{ should_fire: {prompt:string; provenance:string}[]; should_not: {prompt:string; provenance:string}[] }>

SkillTunerPage.tsx state + sites:
  :69   const [cases, setCases] = useState<TunerCase[]>([])       // the [] init to replace with hydrate-on-open
  :80   const [scoreboard, setScoreboard] = useState<TunerScoreboard | null>(null)  // rehydrate target
  :97-110 startRun — already maps cases into the POST body (:102-104); a non-empty body means the backend
          uses them verbatim and does NOT re-seed (skill_tuner.py:466). setTargets(started.targets) at :105.
  :280  the 50/50 split to widen:
        <div className="grid gap-6 px-8 py-6 lg:grid-cols-[minmax(0,1fr)_minmax(0,1fr)]">
  :300-304 cost preview — `× ${targets.length} models` is empty until kickoff (targets=[] pre-run):
        {cases.length} case{...} {targets.length>0 ? ` × ${targets.length} models` : ""} × 3 repeats

CaseEditor.tsx:
  :22-27 provenance union (seeded/sibling/held/you) — currently dead (only "you" ever set)
  :46-52,:99 ProvenanceTag already renders the tag
  :122-125 the editor's existing 60/40 split math
  :127-135 addCase (sets provenance "you"); :137 removeCase — already make cases fully editable
  :191-199 the train/held split bar

TunerCandidate.is_baseline (api.ts:2679) — the live/current description is the baseline candidate (index 0).
  D-04 standalone block = render ProviderScoreboard from scoreboard.candidates.find(c => c.is_baseline).cells.

D-12 pre-run model count: resolve the configured-target count client-side from FullAppSettings.providers
  with a key (mirror the backend `configured_targets` presence logic) so the preview is populated on open.
  Attribution "Built by {builder_model} · measured on N models" reads tuner_runs.builder_model/target_count
  (now on the GET-latest payload from Plan 01).

Held provenance RESOLVED (Pitfall 4): backend returns only seeded/sibling. In CaseEditor, treat `held` as a
  DERIVED display tag from the existing 60/40 math (:122-125) — or drop it from the union. Do not leave it dead.

Red line: reconcile-via-fetch on open (D-v2.5-03); SSE/Realtime is a hint. Do NOT touch the shared chat
  subscribeToRun consumer — the tuner uses its own streamTunerRun reader.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hydrate + edit seeded cases; resolve the held tag (D-05/D-06)</name>
  <files>frontend/src/pages/SkillTunerPage.tsx, frontend/src/components/skills/tuner/CaseEditor.tsx, frontend/src/components/skills/tuner/CaseEditor.test.tsx</files>
  <read_first>
    - frontend/src/pages/SkillTunerPage.tsx (:69 cases=[]; :97-110 startRun + POST body cases; the mount-effect region where the skill is loaded)
    - frontend/src/components/skills/tuner/CaseEditor.tsx (:22-27 provenance union; :46-52,:99 ProvenanceTag; :122-125 60/40 math; :127-135 addCase; :137 removeCase; :191-199 split bar)
    - frontend/src/components/skills/tuner/CaseEditor.test.tsx (extend — assert hydrated cases render + are editable)
    - .planning/sketches/043-case-editor-and-live-run/index.html (seeded cases shown with provenance, edit/approve-before-run)
    - .planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-01-PLAN.md (the getSeededCases contract)
  </read_first>
  <behavior>
    - On opening the tuner for a skill, the editor renders the backend's seeded cases (should_fire + should_not) with real `seeded`/`sibling` provenance tags — not an empty "0 cases" list.
    - The hydrated cases are fully editable: a case can be removed, edited, and new cases added (provenance "you") before the run.
    - When the author runs, the edited cases are sent verbatim (the backend does not re-seed).
    - The `held` tag is either a derived display tag from the 60/40 split math or removed from the union — never a dead value contradicting the split bar.
  </behavior>
  <action>
    In `SkillTunerPage`, on mount/skill-load, call `getSeededCases(skillId)` and `setCases(...)` mapping the returned `{prompt, provenance}` pairs into `TunerCase[]` (should_fire → `should_fire:true`, should_not → `should_fire:false`, carrying the real `seeded`/`sibling` provenance + stable ids). Replace the `cases=[]` reliance (:69) so the editor hydrates-on-open. Keep `startRun` as-is — it already maps `cases` into the POST body (:102-104) and a non-empty body skips re-seeding. In `CaseEditor`, resolve the `held` provenance per the resolved decision: compute it as a DERIVED display tag from the existing 60/40 math (:122-125) shown only in the split context, OR drop `held` from the `provenance` union (:22-27) and rely on the split bar (:191-199) for train/held — pick one and document it in the SUMMARY so the tag is neither dead nor fabricated. Preserve add/edit/remove. Extend `CaseEditor.test.tsx`: render with hydrated seeded cases (mixed seeded/sibling provenance), assert the provenance tags render, assert removeCase drops a case, assert addCase appends a "you" case.
  </action>
  <verify>
    <automated>cd frontend && npm test -- CaseEditor --run</automated>
    <automated>cd frontend && npm test -- SkillTunerPage --run</automated>
  </verify>
  <acceptance_criteria>
    - `SkillTunerPage` calls `getSeededCases` on open and `setCases` with real provenance (`grep -c getSeededCases SkillTunerPage.tsx` ≥ 1).
    - The editor renders hydrated seeded/sibling cases; `npm test -- CaseEditor --run` GREEN asserting provenance tags render + cases are removable/addable.
    - `held` is resolved: either `grep -c '"held"' CaseEditor.tsx` shows it derived from the 60/40 math, OR it is removed from the provenance union — not left as a dead string (documented in SUMMARY).
    - `startRun` still sends the (now-hydrated/edited) cases verbatim — the POST body mapping at :102-104 is unchanged.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Editor hydrates from the backend seeded cases with real seeded/sibling provenance and is fully editable before the run; the held tag is derived-or-dropped (not dead); edited cases run verbatim; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full-width layout, standalone scoreboard block, result rehydration, cost preview + attribution (D-03/D-04/D-07/D-12)</name>
  <files>frontend/src/pages/SkillTunerPage.tsx, frontend/src/pages/SkillTunerPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/SkillTunerPage.tsx (:80 scoreboard state; :97-110 startRun + setTargets(:105); :280 the 50/50 grid; :300-304 the empty-`× M models` cost preview; the mount-effect region)
    - frontend/src/pages/SkillTunerPage.test.tsx (extend — assert rehydration + cost preview)
    - frontend/src/components/skills/tuner/ProviderScoreboard.tsx (Plan 02 — the vertical-rows component; the standalone block reuses it for the baseline candidate)
    - .planning/sketches/041-tuner-surface-shell/index.html (:265 attribution; :276-294 standalone per-provider held-out block)
    - .planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-01-PLAN.md (getTunerLatest contract + the persisted builder_model/target_count fields)
  </read_first>
  <behavior>
    - The results/candidates area spans full width (or the right column gets materially more room) — no longer wedged into the 50/50 half column.
    - A standalone per-provider scoreboard block renders at the top of the results area from the baseline candidate's cells (the live description's scores), distinct from per-candidate grids.
    - On open, getTunerLatest rehydrates the persisted result via setScoreboard so a completed result survives refresh/navigation; a 404 (no run yet) leaves the page empty without erroring.
    - The pre-run cost preview shows the model count BEFORE the first run (resolved client-side from configured providers), and the "Built by {builder model} · measured on N models" attribution renders from the persisted run metadata.
  </behavior>
  <action>
    Widen the layout (:280): give the results/candidates column materially more room or let candidates span full width below the config (e.g. narrower left config + wider results, or a single full-width results section). Render the D-04 standalone block: derive `scoreboard.candidates.find(c => c.is_baseline)` and render its `cells` via the Plan-02 `ProviderScoreboard` at the top of the results area (label it as the current/live description's scores). Add result rehydration: in the mount/skill-load effect, call `getTunerLatest(skillId)`; on a non-null result `setScoreboard(...)` (reconcile-via-fetch on open, D-v2.5-03 — SSE is a hint); a `null`/404 leaves `scoreboard` null (no error). Fix the cost preview (:300-304): resolve the configured-target count client-side from `FullAppSettings.providers` with a key (mirror `configured_targets` presence logic) so `× M models` is populated on open, not only after `setTargets` post-kickoff. Render the "Built by {builder_model} · measured on {target_count} production models" attribution from the persisted run metadata now on the GET-latest payload (and from the resolved skill-builder model + configured target count pre-run). Do NOT touch the shared chat subscribeToRun consumer (the tuner uses its own streamTunerRun reader — red line). Extend `SkillTunerPage.test.tsx`: mock `getTunerLatest` returning a stored scoreboard → assert it rehydrates on open; mock configured providers → assert the cost-preview model count shows pre-run; assert the standalone baseline-scoreboard block renders.
  </action>
  <verify>
    <automated>cd frontend && npm test -- SkillTunerPage --run</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lg:grid-cols-\\[minmax(0,1fr)_minmax(0,1fr)\\]" SkillTunerPage.tsx` == 0 (the 50/50 split is gone) and the results area is full-width / materially wider.
    - `grep -c "is_baseline" SkillTunerPage.tsx` ≥ 1 (standalone baseline-scoreboard block) rendered via the Plan-02 `ProviderScoreboard`.
    - `grep -c "getTunerLatest" SkillTunerPage.tsx` ≥ 1; the mount effect calls it and `setScoreboard` on a non-null result; a null/404 does not throw.
    - The cost preview shows a model count derived from configured providers BEFORE the first run (the `× M models` no longer depends solely on `targets.length` post-kickoff).
    - The "Built by {builder model} · measured on N models" attribution renders from the persisted/resolved metadata.
    - `npm test -- SkillTunerPage --run` GREEN asserting rehydration-on-open, pre-run cost-preview model count, and the standalone block.
    - No edit to the shared `subscribeToRun` consumer (`git diff` lists only the four files across this plan's tasks).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Results span full width; standalone live-description scoreboard renders from the baseline candidate; the persisted latest result rehydrates on open (survives refresh); cost preview shows the model count pre-run and attribution renders; tests green; shared consumer untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET-latest / seeded-cases responses → page render | The page consumes the owner-gated Plan-01 endpoints; it must reconcile-via-fetch on open (not trust a stale SSE/Realtime hint) and must not fabricate scores. |
| page → shared run-buffer consumer | The tuner streams via its own `streamTunerRun` reader; touching the shared `subscribeToRun` consumer is the WR-06 red line (a tuner change reached the shared chat path → 400). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123.1-04-01 | Information Disclosure | rehydrate via getTunerLatest | mitigate | `getTunerLatest` hits the owner-gated Plan-01 route (404 cross-user); the page renders only what the owner-scoped endpoint returns. No client-side cross-skill read. |
| T-123.1-04-02 | Tampering (shared-path regression) | streaming consumer | mitigate | Use ONLY the tuner's `streamTunerRun` reader + reconcile-via-fetch on open; do NOT modify the shared `subscribeToRun` / agent loop / trim-pin (WR-06 red line). Enforced by the git-diff acceptance gate. |
| T-123.1-04-03 | Tampering (display integrity) | standalone scoreboard render | mitigate | The standalone block renders the baseline candidate's server-computed cells via the Plan-02 component (server scores only — no client fabrication). |
| T-123.1-04-SC | Tampering | npm installs | mitigate | No new packages. No slopcheck needed. |
</threat_model>

<verification>
- `npm test -- SkillTunerPage --run` and `npm test -- CaseEditor --run` green; `npx tsc --noEmit` clean.
- Manual UAT (VALIDATION.md): open the tuner for skill `docx` with 8 providers — seeded cases show + edit before run; results span full width; standalone live-description block present; complete a run, restart backend+Redis, reopen → the latest result re-renders.
- Red line: `git diff` touches NO shared chat-path / agent-loop / trim-pin file.
</verification>

<success_criteria>
- Full-width results; hydrated/editable seeded cases before run; standalone live-description scoreboard; rehydrated persisted result on open; populated pre-run cost preview + attribution.
- D-03, D-05 (page hydrate), D-06, D-07 (page rehydrate), D-12 covered.
- Shared chat path / agent loop / trim-pin untouched.
</success_criteria>

<output>
Create `.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-04-SUMMARY.md` when done
</output>
