---
phase: 123.1-skill-trigger-tuner-design-fidelity-and-ux-polish
plan: 09
type: execute
wave: 5
depends_on: ["123.1-05", "123.1-08"]
gap_closure: true
autonomous: true
requirements: [BUG-260624-01, TT-11]
files_modified:
  - frontend/src/pages/SkillsPage.tsx
  - frontend/src/pages/SkillTunerPage.tsx

must_haves:
  truths:
    - "The two decorative `w-16 shrink-0 bg-sidebar` rails (one in SkillsPage, one in SkillTunerPage) are removed so the Skills + Tuner surfaces no longer show a 'double-sidebar' dead band next to the real NavPanel — the operator's reported layout gap is gone (TT-11)."
    - "Both rails are removed in this one change; the sibling header/content already carries px-8 so content spacing is unchanged after the rail is gone."
    - "No other layout, no functional behavior, and no other file changes — the change is confined to the two page files, removing only the decorative rail divs."
  artifacts:
    - path: "frontend/src/pages/SkillsPage.tsx"
      provides: "the decorative bg-sidebar rail removed"
      contains: "flex h-full"
    - path: "frontend/src/pages/SkillTunerPage.tsx"
      provides: "the decorative bg-sidebar rail removed"
      contains: "flex h-full"
  key_links:
    - from: "frontend/src/pages/SkillsPage.tsx"
      to: "the page content column"
      via: "removing the `w-16 shrink-0 bg-sidebar` first child; content keeps its px-8"
      pattern: "bg-sidebar"
    - from: "frontend/src/pages/SkillTunerPage.tsx"
      to: "the page content column"
      via: "removing the `w-16 shrink-0 bg-sidebar` first child; header keeps its px-8"
      pattern: "bg-sidebar"
---

<objective>
Close TT-11 from the 123.1 live-UAT audit (backlog §2 — the "double-sidebar" dead band): SkillsPage and SkillTunerPage each insert a decorative `w-16 shrink-0 bg-sidebar` div as the first child, using the SAME token as the real NavPanel. Next to the actual nav, that reads as a redundant second sidebar — the operator's reported layout gap. Only these two files use that decorative rail. Delete BOTH rail divs in one change; the sibling header/content already carries `px-8`, so content spacing is unchanged.

This plan runs LAST so it lands on top of 123.1-05's full-width Tuner restructure and the 123.1-08 run-resilience edits to SkillTunerPage. If 05 already removed the SkillTunerPage rail as part of its full-width stack, this plan reconciles (removes whichever rail remains) and still removes the SkillsPage rail — the must-have is that NEITHER page renders a `w-16 ... bg-sidebar` decorative rail when this plan completes.

Purpose: Remove the redundant decorative second-sidebar band on the Skills + Tuner surfaces.
Output: Both decorative rails removed; content spacing preserved via the existing px-8.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-AUDIT-BACKLOG.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly, no exploration.
     Re-read both files: 05 + 08 changed SkillTunerPage; SkillsPage is otherwise untouched this phase. -->

frontend/src/pages/SkillsPage.tsx:70-72 — the decorative rail to delete:
  <div className="flex h-full overflow-hidden">
    {/* Pane 1: Decorative left — tonal depth anchor */}
    <div className="w-16 shrink-0 bg-sidebar" />
    {/* Pane 2: Center — page header + scrollable skill list */}
    <div className="flex flex-col flex-1 min-w-0 overflow-hidden border-r border-border/10"> ...
  # The center pane header already has px-8 (line 77). Removing the rail leaves the content
  # flush against the real NavPanel, which is the intent.

frontend/src/pages/SkillTunerPage.tsx:380-381 — the matching decorative rail (PRE-05; re-read
  after 05's full-width restructure — 05 may have already removed/restructured this):
  <div className="flex h-full overflow-hidden">
    {/* Decorative left rail — tonal depth anchor (mirrors SkillsPage pane 1). */}
    <div className="w-16 shrink-0 bg-sidebar" />
    <div className="flex flex-col flex-1 min-w-0 overflow-hidden"> ...
  # The focused-surface header already carries px-8 (line 385).

frontend/src/components/layout/ChatLayout.tsx:289 — the real NavPanel mount (the ACTUAL sidebar).
  Do NOT touch this — it is the legitimate nav. TT-11 removes only the DECORATIVE duplicates in
  the two page bodies.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove both decorative bg-sidebar rails (SkillsPage + SkillTunerPage)</name>
  <files>frontend/src/pages/SkillsPage.tsx, frontend/src/pages/SkillTunerPage.tsx</files>
  <read_first>
    - frontend/src/pages/SkillsPage.tsx (lines 69-100: the outer flex container, the decorative rail at 71-72, the center pane header with px-8 at 77)
    - frontend/src/pages/SkillTunerPage.tsx (lines 378-405: the outer flex container + the decorative rail at 380-381 + the header with px-8 — re-read because 05's full-width restructure + 08's edits changed this region; the rail may already be gone)
    - .planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-AUDIT-BACKLOG.md (§2 TT-11: delete both rail divs; the sibling header already carries px-8; only these two files use the class)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (the Skills/Tuner surface visual language — the real NavPanel is the only sidebar)
  </read_first>
  <action>
    In SkillsPage.tsx, delete the decorative rail div `<div className="w-16 shrink-0 bg-sidebar" />` (and its `{/* Pane 1: Decorative left ... */}` comment) at lines 71-72. Leave the outer `<div className="flex h-full overflow-hidden">` and the center content pane as the now-first child — the content keeps its existing px-8, so it sits flush against the real NavPanel. Do not change the center pane's classes (it can keep `border-r border-border/10` or not — leave it as-is; only the rail div is removed).

    In SkillTunerPage.tsx, re-read the outer container region FIRST (123.1-05 restructured the body to a full-width stack and may already have removed this rail). If the `<div className="w-16 shrink-0 bg-sidebar" />` decorative rail (and its comment) still exists, delete it; the focused-surface header already carries px-8. If 05 already removed it, make NO change to SkillTunerPage and note that in the SUMMARY (the must-have — "neither page renders the decorative rail" — is already satisfied for that file). Either way, do NOT touch the real NavPanel (ChatLayout.tsx:289) and do NOT remove any px-8 / header / content structure — only the decorative `w-16 ... bg-sidebar` div.

    After both edits, confirm via grep that NO `w-16 shrink-0 bg-sidebar` (or any `bg-sidebar` decorative rail) remains in either page file.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-sidebar" frontend/src/pages/SkillsPage.tsx` == 0 (the decorative rail is gone)
    - `grep -c "bg-sidebar" frontend/src/pages/SkillTunerPage.tsx` == 0 (the decorative rail is gone)
    - `grep -c "w-16 shrink-0" frontend/src/pages/SkillsPage.tsx` == 0
    - `grep -c "w-16 shrink-0" frontend/src/pages/SkillTunerPage.tsx` == 0
    - `grep -c "flex h-full" frontend/src/pages/SkillsPage.tsx` >= 1 (the outer container is preserved)
    - `grep -c "px-8" frontend/src/pages/SkillsPage.tsx` >= 1 (content spacing preserved)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `git diff --name-only` lists ONLY the two page files (+ .planning/ docs) — no match for: `ChatLayout`, `NavPanel`, `StreamsProvider`, `useMessages`, `skill_tuner`
  </acceptance_criteria>
  <done>Neither SkillsPage nor SkillTunerPage renders the decorative `w-16 shrink-0 bg-sidebar` rail; the outer flex container + the content's px-8 are preserved (content sits flush against the real NavPanel); the real NavPanel is untouched; tsc is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational removal of a decorative div; no data, auth, or input boundary is involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123.1-09-01 | (n/a — cosmetic) | decorative rail removal | accept | Removing a decorative `bg-sidebar` div has no security surface: no data path, no auth check, no input handling. The real NavPanel (the legitimate nav) is explicitly untouched. Lowest-risk change in the phase. |
| T-123.1-09-SC | Tampering | npm/pip/cargo installs | accept | ZERO new packages. Pure markup deletion. No supply-chain surface introduced. |
</threat_model>

<verification>
- Frontend: `cd frontend && npx tsc --noEmit` — clean type-check.
- Red line: `git diff --name-only` confined to the two page files + .planning/ docs; no ChatLayout/NavPanel/shared-component change.
- Manual (HUMAN-UAT, carries to verify): open Skills and the Trigger Tuner and confirm there is no redundant grey band between the real NavPanel and the page content.
</verification>

<success_criteria>
- Neither SkillsPage nor SkillTunerPage renders the decorative `w-16 shrink-0 bg-sidebar` rail (TT-11).
- The outer container + content px-8 are preserved; the real NavPanel is untouched.
- The diff is confined to the two page files.
</success_criteria>

<output>
Create `.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-09-SUMMARY.md` when done.
</output>
