---
phase: 123.1-skill-trigger-tuner-design-fidelity-and-ux-polish
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [BUG-260624-01, TT-10]
files_modified:
  - backend/app/main.py
  - backend/.env.example

must_haves:
  truths:
    - "The LangSmith background uploader's 429 'Failed to multipart ingest' flood is silenced to ERROR-level — the tuner backend log is readable during a run instead of being drowned by the quota-exceeded retry noise."
    - "The silence is scoped to the langsmith logger ONLY — app/uvicorn/asyncio logging is unchanged; real LangSmith ERROR-and-above (auth failure, hard client error) still surfaces."
    - "An optional LANGSMITH_TRACING_SAMPLING_RATE knob is documented in .env.example so an operator over the monthly unique-trace quota can throttle uploads at the source (no code change required to use it)."
    - "The git diff is confined to main.py + .env.example — no tuner service/route/component, no scoring core, no agent_loop/threads/anthropic_service/StreamsProvider/useMessages change."
  artifacts:
    - path: "backend/app/main.py"
      provides: "langsmith logger silenced to ERROR near the existing asyncio suppressor"
      contains: "langsmith"
    - path: "backend/.env.example"
      provides: "documented optional LANGSMITH_TRACING_SAMPLING_RATE knob"
      contains: "LANGSMITH_TRACING_SAMPLING_RATE"
  key_links:
    - from: "backend/app/main.py"
      to: "logging.getLogger(\"langsmith\")"
      via: "setLevel(logging.ERROR) near the asyncio suppressor"
      pattern: "getLogger\\(\"langsmith\"\\)"
---

<objective>
Close TT-10 from the 123.1 live-UAT audit backlog: during a tuner run (and any heavy run) the LangSmith background uploader thread floods the backend log with "Failed to multipart ingest runs: ... 429 Client Error: Too Many Requests" once the monthly unique-trace quota is exceeded. This is NOT on the latency path (the audit confirmed it does not compound tuner slowness — TT-06), but it makes the tuner log unreadable during the exact runs an operator needs to debug.

The smallest correct fix (backlog §2 TT-10): silence the `langsmith` logger to ERROR level near the existing `asyncio` suppressor in `main.py:21`, and document an optional `LANGSMITH_TRACING_SAMPLING_RATE` knob in `.env.example` so an over-quota operator can throttle uploads at the source.

Purpose: Make the tuner backend log legible during a run without disabling tracing.
Output: A scoped langsmith-logger silence + a documented sampling-rate knob.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-AUDIT-BACKLOG.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly, no exploration. -->

backend/app/main.py:21 — the EXISTING module-level logger-suppression precedent to mirror:
  # Suppress asyncio transport-level "socket.send() raised exception." warnings.
  logging.getLogger("asyncio").setLevel(logging.ERROR)
  # `logging` is already imported at the top of main.py (line 2). The new langsmith
  # suppressor sits right next to this one — same pattern, same rationale (noise, not signal).

backend/app/main.py:76-80 — LangSmith env wiring (the upload behavior this throttles):
  os.environ["LANGSMITH_TRACING"] = settings.langsmith_tracing
  os.environ["LANGSMITH_PROJECT"] = settings.langsmith_project
  if settings.langsmith_api_key:
      os.environ["LANGSMITH_API_KEY"] = settings.langsmith_api_key
  # The langsmith client reads LANGSMITH_TRACING_SAMPLING_RATE from os.environ at import;
  # load_dotenv (main.py:15) already populates os.environ from backend/.env, so an
  # operator-set value in .env reaches the client with NO code change — this plan only
  # DOCUMENTS the knob; it does not need to wire a Settings field for it.

backend/.env.example:173-175 — the LangSmith block to extend:
  LANGSMITH_API_KEY=
  LANGSMITH_PROJECT=agentic-rag
  LANGSMITH_TRACING=true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Silence the langsmith logger to ERROR + document the sampling-rate knob</name>
  <files>backend/app/main.py, backend/.env.example</files>
  <read_first>
    - .planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-AUDIT-BACKLOG.md (§2 TT-10 row: root cause = background uploader 429 flood when monthly unique-trace quota exceeded; NOT on the latency path; smallest fix = setLevel ERROR + optional sampling-rate)
    - backend/app/main.py (lines 1-23: the top imports + the asyncio suppressor at line 21 — the precedent to mirror; lines 76-80: the LangSmith env wiring so you place the suppressor coherently with the tracing setup)
    - backend/.env.example (lines 170-176: the LANGSMITH_* block to extend)
    - ./CLAUDE.md (Observability = LangSmith; do not disable tracing — only silence the noise)
  </read_first>
  <action>
    In backend/app/main.py, add ONE line silencing the LangSmith background-uploader logger to ERROR, directly beneath the existing `logging.getLogger("asyncio").setLevel(logging.ERROR)` at line 21: `logging.getLogger("langsmith").setLevel(logging.ERROR)`. Add a short comment explaining WHY (the background uploader emits a "Failed to multipart ingest runs: 429 Too Many Requests" WARNING flood once the monthly unique-trace quota is exceeded; this is upload-retry noise, not the latency path — it does not slow the run, it just drowns the log during the exact runs an operator needs to read; ERROR-and-above still surfaces real LangSmith failures). Use the SAME `logging` import already present at main.py:2 — do NOT add a new import. Place it at module scope (so it applies before any LangSmith client logs), NOT inside a function or the lifespan handler. Do NOT touch the `os.environ["LANGSMITH_*"]` wiring at lines 76-80, do NOT call `langsmith.client` directly, and do NOT change the asyncio suppressor.

    In backend/.env.example, extend the LANGSMITH_* block (after line 175 `LANGSMITH_TRACING=true`) with a documented, commented-out optional knob: `# LANGSMITH_TRACING_SAMPLING_RATE=0.1` plus a one-line comment that this throttles the fraction of traces uploaded (0.0-1.0) — set it below 1.0 if you exceed the monthly unique-trace quota and see "Failed to multipart ingest ... 429" in the log; the langsmith client reads it from the environment (load_dotenv populates os.environ), so no code change is needed. Keep it commented out so the default behavior (full tracing) is unchanged for installs that are not over quota.
  </action>
  <verify>
    <automated>cd backend && source venv/Scripts/activate && python -c "import logging; import app.main; assert logging.getLogger('langsmith').level == logging.ERROR, 'langsmith logger not silenced to ERROR'; assert logging.getLogger('asyncio').level == logging.ERROR, 'asyncio suppressor regressed'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getLogger("langsmith")' backend/app/main.py` >= 1 (the suppressor exists)
    - `grep -n 'getLogger("langsmith").setLevel(logging.ERROR)' backend/app/main.py` matches (silenced to ERROR, not a different level)
    - `grep -c 'getLogger("asyncio")' backend/app/main.py` >= 1 (the existing asyncio suppressor is preserved, not replaced)
    - `grep -c "LANGSMITH_TRACING_SAMPLING_RATE" backend/.env.example` >= 1 (knob documented)
    - `grep -c "^import logging" backend/app/main.py` == 1 (no duplicate logging import was added — the existing one at line 2 is reused)
    - `cd backend && source venv/Scripts/activate && python -c "import logging; import app.main; assert logging.getLogger('langsmith').level == logging.ERROR; print('OK')"` prints OK (importing main applies the suppressor at module scope)
    - `git diff --name-only` lists ONLY backend/app/main.py + backend/.env.example (+ planning docs) — no match for: `skill_tuner`, `agent_loop`, `threads.py`, `anthropic_service`, `StreamsProvider`, `useMessages`, `SkillTunerPage`, `runs.py`
  </acceptance_criteria>
  <done>The langsmith background-uploader logger is silenced to ERROR at module scope next to the asyncio suppressor (real LangSmith errors still surface, app/uvicorn logging unchanged), an optional LANGSMITH_TRACING_SAMPLING_RATE knob is documented (commented-out, default unchanged) in .env.example, and the diff is confined to main.py + .env.example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend process → log sink | Log output legibility; no user input crosses here. |
| backend → LangSmith uploader | Outbound observability traffic; no inbound trust boundary changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123.1-10-01 | Information Disclosure | langsmith logger silence | accept | Raising the langsmith logger to ERROR REDUCES log volume; it never logs MORE. ERROR-and-above (auth failures, hard client errors) still surfaces. No new data is logged; no secret is exposed (the sampling-rate knob is a float, not a credential, and stays commented out). |
| T-123.1-10-02 | Denial of Service | LANGSMITH_TRACING_SAMPLING_RATE | accept | The documented knob is operator-set and OPTIONAL (commented out = default full tracing). Lowering it REDUCES outbound upload volume (mitigates the 429 source); it cannot increase load. No runtime input path. |
| T-123.1-10-SC | Tampering | npm/pip/cargo installs | accept | ZERO new packages (uses stdlib `logging` already imported; documents an existing langsmith client env var). No supply-chain surface introduced. |
</threat_model>

<verification>
- `cd backend && source venv/Scripts/activate && python -c "import logging; import app.main; assert logging.getLogger('langsmith').level == logging.ERROR; print('OK')"` — the suppressor applies at module scope.
- Red line: `git diff --name-only` confined to main.py + .env.example + .planning/ docs; no tuner/scoring/agent-loop/shared-path change.
- Manual (optional, carries to verify): start a tuner run with an over-quota LangSmith key and confirm the backend log no longer shows the repeating "Failed to multipart ingest ... 429" WARNING flood.
</verification>

<success_criteria>
- The langsmith logger is silenced to ERROR near the asyncio suppressor in main.py (scoped to langsmith only).
- The optional LANGSMITH_TRACING_SAMPLING_RATE knob is documented (commented out) in .env.example.
- No new import, no change to the asyncio suppressor or the LangSmith env wiring.
- The git diff is confined to main.py + .env.example + planning docs.
</success_criteria>

<output>
Create `.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-10-SUMMARY.md` when done.
</output>
