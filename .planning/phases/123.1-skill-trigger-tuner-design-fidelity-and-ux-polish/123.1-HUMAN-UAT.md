---
status: partial
phase: 123.1-skill-trigger-tuner-design-fidelity-and-ux-polish
source: [123.1-VERIFICATION.md]
started: 2026-06-24T09:00:00Z
updated: 2026-06-24T09:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Scoreboard legibility at the org's real provider count
expected: |
  Pre-run editor + scoreboard read cleanly at the org's real scale.
awaiting: scope decision (issue logged — see Gaps)

## Tests

### 1. Scoreboard legibility at the org's real provider count (D-02/D-03/D-04/D-12 — BUG-260624-01 HIGH #1)
expected: Open the Trigger Tuner for a skill with the org's full configured provider set (the original report ran 8). The `ProviderScoreboard` renders as vertical full-width provider ROWS (not a cramped N-column grid), each row legible with provider/model names un-truncated, a per-row colored magnitude bar driven by the combined score, and BOTH the fires (recall) and no-false (false-fire rail) sub-scores visible. Results span the full width (the old 50/50 split is gone — narrow config rail + full-width results). The standalone per-provider "baseline" scoreboard block for the live description is present above the candidate cards.
result: issue
reported: "are you joking? what is this design, just have a look before I do anything [screenshot: pre-run Tuner for weekly-report-writer — dozens of seeded cases rendered as an illegible wall of tiny cramped text in the ~360px narrow left rail, while the wide results column sits empty]"
severity: major
note: |
  The scoreboard itself was NOT reached (no run completed in the screenshot). The
  failure is the PRE-RUN case editor + idle page layout — the same "doesn't read at
  the org's real scale" class as BUG-260624-01, now in the editor instead of the
  scoreboard. Hydration (Test 2 data wiring) WORKS; rendering the hydrated set is the
  defect. See Gaps for full diagnosis.

### 2. Seeded benchmark cases are visible AND editable before a run (D-05/D-06 — BUG-260624-01 HIGH #2 / WR-05)
expected: On opening the Tuner, the case editor is HYDRATED with the server's auto-seeded cases (no more "0 cases") — should-fire and should-not columns populated with `seeded`/`sibling` provenance. The cases are editable (add/remove/edit) before starting a run, and the run benchmarks the edited set (not a hidden one).
result: [pending]

### 3. A completed result survives refresh / restart (D-07 — BUG-260624-01 HIGH #3)
expected: Run a tune to completion, then hard-refresh the page (and/or restart backend + flush Redis). The completed scoreboard REHYDRATES on open from the durable `tuner_runs` row (latest-per-skill), including the "Built by {builder} · measured on N models" attribution. A second run for the same skill OVERWRITES (one row, never accumulates). The persisted `updated_at` is a real timestamp (WR-07 fix).
result: [pending]

### 4. Builder model is choosable from configured models (D-08/D-09/D-10 — BUG-260624-01 MED #4)
expected: In Settings → AI Model, the Skill-builder model dropdown is driven by the user's CONFIGURED models (grouped by provider) — strong models (Sonnet/Opus, GPT-pro, Gemini-Pro) are selectable, not just the cheap tier. A cheap "Auto" default stays pre-selected (not forced). Selecting an unverified model shows a SOFT amber hint but never blocks (no disabled option). The old placeholder local ids (`lm-studio/qwen3`, `openai-compat/local-model`) are gone.
result: [pending]

### 5. SC#10 cross-provider regression — shared path unaffected (red line)
expected: Run the Tuner across 2+ providers (OpenAI / Anthropic / Google / OpenRouter), then send a normal chat message in a separate thread. The tuner's background job + `tuner_*` SSE events never contaminate chat context or a pinned `load_skill` group; chat streaming + the CTX-03 trim-pin behave exactly as before (Deep byte-identical). Cross-user: a second user cannot read another user's non-global skill's latest run (404) or a foreign run_id's buffer (CR-01 fix — 404).
result: [pending]

## Summary

total: 5
passed: 0
issues: 1
pending: 4
skipped: 0
blocked: 0

## Gaps

- truth: "The Trigger Tuner reads cleanly at the org's real scale — the pre-run case editor is legible and usable, not an illegible wall of tiny text"
  status: failed
  reason: "User reported: 'are you joking? what is this design' — pre-run editor for weekly-report-writer renders dozens of seeded cases as a towering wall of tiny (text-xs/[10px]/[9px]) cramped text in the ~360px narrow left rail while the wide results column sits empty (idle state)."
  severity: major
  test: 1
  root_cause: "Three compounding causes: (1) auto_seed_cases seeds ONE full sibling-skill description per enabled owned/global skill into should_not, UNCAPPED (skill_tuner_service.py:258-263) → dozens of long cases for a user with many skills; (2) CaseEditor case columns are an unbounded <ul> with no max-height/scroll and 9-11px text (CaseEditor.tsx:110,117,69,102); (3) idle layout is inverted — the editor (primary pre-run interaction) is jammed into the 360px config rail, split into two ~170px columns, while the wide 1fr results column is empty until a run (SkillTunerPage.tsx:423)."
  artifacts:
    - path: "frontend/src/components/skills/tuner/CaseEditor.tsx"
      issue: "Case columns: unbounded <ul> (line 110), no max-height/scroll, microscopic text (text-xs line 117, text-[10px] line 102, text-[9px] line 69); two columns inside a 360px rail = ~170px each."
    - path: "frontend/src/pages/SkillTunerPage.tsx"
      issue: "Idle-state layout imbalance: editor in 360px narrow rail (line 423) vs empty full-width results column (lines 511-519 placeholder)."
    - path: "backend/app/services/skill_tuner_service.py"
      issue: "auto_seed_cases pushes a full sibling description per sibling skill into should_not with no cap (lines 258-263) — case count scales with the user's skill library."
  missing:
    - "Cap / sample the auto-seeded case count (e.g. N siblings max) so the editor opens with a sane, reviewable set — not the entire skill library."
    - "Bound the case columns with a max-height + internal scroll (and/or collapse beyond the first few), bump text legibility."
    - "Rebalance the pre-run layout so the case editor — the thing you edit before a run — gets real width instead of the 360px rail while the results column sits empty."
  debug_session: ""
  approved_design: ".planning/sketches/045-tuner-prerun-editor-at-scale/ (winner: Variant B — Full-width stack). The README '## Decision' section is the BUILD CONTRACT: (1) backend caps the auto-seeded should-NOT set in skill_tuner_service.py + GET /tuner/cases/seeded returns capped slice + total ('showing N of M — capped', never silent); (2) SkillTunerPage.tsx:423 drops the pre-run 360px rail for a full-width single-column stack (description -> CaseEditor -> run bar); scoreboard + CandidateCards render full-width AFTER a run (reuse existing 123.1 components untouched); (3) CaseEditor.tsx columns get max-height + internal scroll, text 9-11px -> text-sm, per-column count + cap banner, full-width two-up grid; (4) RED LINE preserved — no scoring core / agent loop / shared chat path / CTX-03 trim-pin edits."
