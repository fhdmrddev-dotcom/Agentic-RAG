---
phase: 075.8-live-execution-visual-polish
verified: 2026-05-24T00:00:00Z
human_verified: 2026-05-24T21:30:00Z
status: passed
must_haves_total: 7
must_haves_verified: 7
must_haves_failed: 0
human_verification_complete: true
human_verification_source: 075.8-HUMAN-UAT.md (5/5 passed, 0 issues)
score: 7/7 must-haves code-verified + 5/5 lived-experience UAT passed
overrides_applied: 0
human_verification:
  - test: "Single tool, fast (< 5s) — execute_code 'print hello' against Google model"
    expected: "StatusPill shows 'running' with bouncing dot during execution; transitions to 'done · X.Xs' on completion; editor inset shows numbered gutter + Shiki-highlighted Python code; run-card auto-collapses after completion"
    why_human: "All visual state transitions (pill animation, glow, auto-collapse) and the Shiki WASM rendering require a live browser session — static code analysis cannot confirm CSS animation fires or that the async Shiki highlighter renders in the actual DOM"
  - test: "Multi-tool (3+), Focus Mode — execute_code → search_documents → execute_code → web_search"
    expected: "Past completed steps render as '→ {summary}' mono rows (not aggregate toggle); active tool is fully expanded with tc-active-wrap glow + bottom shimmer; 'Next: deciding next step…' dashed-border footer appears between tool completions"
    why_human: "The per-step result-summary row rendering depends on Focus Mode state (shouldCollapse=true + stepsCollapsed=true + activeIndex detection) — visually confirming all three conditions fire together requires a live 3+-tool run"
  - test: "Long-running — execute_code 'sleep 30s' (or equivalent)"
    expected: ".tc-active-wrap box-shadow glow visible on the active tool card; 2px bottom progress shimmer (tool-progress-bar absolute bottom) animates along the bottom edge; run-card sticky header stays visible while scrolling"
    why_human: "box-shadow glow (0 0 24px hsl(primary/0.18)) and bottom shimmer animation are CSS-only effects — visible only in a real browser render; sticky header pin against Radix ScrollArea Viewport requires scroll interaction"
  - test: "Compact 💭 thinking row — any Anthropic or OpenAI tool-using turn where isPlanning fires"
    expected: "One-line dim italic '💭 Thinking · planning next step' row appears at top of run-body WHILE isPlanning is true (streaming only); row disappears once isPlanning clears and active tool takes over"
    why_human: "isPlanning is a transient streaming state — the window where it's true (post-tool, pre-next-tool) is typically < 1s; requires live stream observation to confirm the row appears and disappears correctly"
  - test: "Cross-provider — repeat Scenario 1 against Anthropic claude-haiku-4-5 + OpenAI gpt-5"
    expected: "StatusPill, editor inset, STDOUT/STDERR regions, and glow all render identically regardless of provider — no provider-specific CSS branches"
    why_human: "Confirms the provider-agnostic rendering contract (CLAUDE.md SC#10 4-axis) — requires actual cross-provider execution, not just code review"
---

# Phase 075.8: Live-Execution Visual Polish — Verification Report

**Phase Goal:** Close the 7 documented gaps between the shipped Phase 075.7 architecture and the locked sketch findings (001/002/003). Pure rendering-layer + CSS work. After this phase the assistant chat surface visually matches the gold-standard sketches under all four lived-experience scenarios.

**Verified:** 2026-05-24
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Must-Have (Sketch Decision) | Code Status | Evidence |
|---|---|---|---|
| 1 | 002 D5 — shared `<StatusPill>` component with `dotBounce` animation, duration on done | ✓ VERIFIED | `StatusPill.tsx` exists at 90 lines; 5-variant type; `dotBounce` keyframe confirmed at `index.css:148-157`; `animate-dotBounce` on dot when `isActive`; duration suffix on done/failed/interrupted |
| 2 | 002 D6 — `.tc-active-wrap` glow + bottom (not top) 2px progress shimmer | ✓ VERIFIED | `.tc-active-wrap` class in `index.css:274-279` with `box-shadow: 0 0 24px hsl(var(--primary) / 0.18)` + soft gradient; bottom-positioned `tool-progress-bar` shimmer at `ToolCallPanel.tsx:683-688` (absolute bottom, not top); applied via `isToolActive` guard |
| 3 | 001 D3 — past completed steps in Focus Mode render `→ {summary}` mono lines | ✓ VERIFIED | `ToolCallPanel.tsx:455-490` — `shouldCollapse && stepsCollapsed && i < activeIndex` now renders `<button data-testid="step-summary-row">` with `→` + `summarizeToolCall(tc)` fallback to `toolLabel`; aggregate toggle removed; test coverage in `ToolCallPanel.test.tsx` asserts 5 per-step rows for 5-done+1-active scenario |
| 4 | 001 D4 — `Next: [hint] queued` dashed-border footer at bottom of active run-body | ✓ VERIFIED | `RunCard.tsx:273-284` — `isStreamingNow && shouldShowNextUp(message, activeTool)` renders `data-testid="next-up-footer"` with `border-dashed` + `Next` / hint / `queued` spans; `shouldShowNextUp` helper at lines 299-308; shows when `isPlanning` OR last tool done with no active tool |
| 5 | 001 D7 — one-line dim italic 💭 thinking row when `isPlanning` true | ✓ VERIFIED | `RunCard.tsx:241-257` — `isStreamingNow && message.isPlanning` renders `data-testid="thinking-row"` with `italic text-muted-foreground/80`; 💭 glyph; `Thinking · planning next step` text; elapsed timer suffix |
| 6 | 002 D1 — editor inset: line-number gutter + Shiki syntax highlighting + Python chip + max-height 180px | ✓ VERIFIED | `ExecuteCodeBody.tsx:259-283` — `data-testid="tc-editor"` grid with 32px gutter column; `data-testid="tc-gutter"` with per-line numbers; `<Suspense>` + lazy `<ShikiCode>` with `language="python" theme="github-dark"`; `max-h-[180px]`; Python lang chip in header row; `ShikiCode.tsx` module-scope singleton pattern; `shiki@^4.1.0` in `package.json` |
| 7 | 002 D3 — STDOUT and STDERR as two visually-distinct labeled regions | ✓ VERIFIED | `ExecuteCodeBody.tsx:52-144` — `TerminalOutput` partitions lines by `ln.kind === "stderr"`; `data-testid="tc-divider-stdout"` with uppercase STDOUT label in muted divider band; `data-testid="tc-divider-stderr"` with uppercase STDERR label in destructive bg divider band; streaming dot on whichever region last received a line |

**Score: 7/7 truths code-verified**

### Required Artifacts

| Artifact | Status | Details |
|---|---|---|
| `frontend/src/components/chat/StatusPill.tsx` | ✓ VERIFIED | Created, 90 lines, 5 variants, `dotBounce` wired, exports `ToolStatus` type |
| `frontend/src/components/chat/StatusPill.test.tsx` | ✓ VERIFIED | Created, 8 vitest assertions across all variants + override paths |
| `frontend/src/components/chat/tool-bodies/ShikiCode.tsx` | ✓ VERIFIED | Created, module-scope singleton, lazy async WASM, `dangerouslySetInnerHTML` on highlighted output, plain-pre fallback |
| `frontend/src/components/chat/ToolCallPanel.tsx` | ✓ VERIFIED | Modified — StatusPill swap at line 614, `tc-active-wrap` conditional at line 535, per-step summary rows at lines 455-490, `pillStatus()` mapper at line 68 |
| `frontend/src/components/chat/RunCard.tsx` | ✓ VERIFIED | Modified — thinking row at lines 241-257, next-up footer at lines 273-284, `shouldShowNextUp` + `nextHint` helpers |
| `frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx` | ✓ VERIFIED | Modified — StatusPill swap at line 244, Shiki editor inset at lines 259-283, TerminalOutput STDOUT/STDERR regions at lines 52-144 |
| `frontend/src/index.css` | ✓ VERIFIED | `.tc-active-wrap` at lines 274-279; `dotBounce` keyframe at lines 148-157 (pre-existing, confirmed present) |
| `frontend/package.json` | ✓ VERIFIED | `shiki@^4.1.0` added at line 42 |
| `frontend/src/__tests__/components/ToolCallPanel.test.tsx` | ✓ VERIFIED | Rewritten to assert `step-summary-row` (new design) instead of `collapsed-steps-summary` (old aggregate) |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `ToolCallPanel.tsx` | `StatusPill.tsx` | import at line 14; `<StatusPill>` at line 614 | ✓ WIRED | Used in generic-tool branch; `pillStatus()` mapper translates ToolCall.status |
| `ExecuteCodeBody.tsx` | `StatusPill.tsx` | import at line 6; `<StatusPill>` at line 244 | ✓ WIRED | Status derived from `isRunning`/`isError`; duration from `executionDurationMs` |
| `ExecuteCodeBody.tsx` | `ShikiCode.tsx` | lazy import at line 12; `<ShikiCode>` inside `<Suspense>` at line 280 | ✓ WIRED | `code={tc.args.code}` flowing from real tool args |
| `RunCard.tsx` | thinking row | `isStreamingNow && message.isPlanning` at line 241 | ✓ WIRED | Condition gates on live `message.isPlanning` prop |
| `RunCard.tsx` | next-up footer | `isStreamingNow && shouldShowNextUp(message, activeTool)` at line 273 | ✓ WIRED | `shouldShowNextUp` checks `isPlanning` + last tool `done` state |
| `ToolCallPanel.tsx` | per-step summary rows | `shouldCollapse && stepsCollapsed && i < activeIndex` at line 455 | ✓ WIRED | `summarizeToolCall(collapsedTc)` from tool-bodies registry |
| `index.css` `.tc-active-wrap` | `ToolCallPanel.tsx` | class applied at line 535: `isToolActive && "tc-active-wrap rounded-md px-2"` | ✓ WIRED | `position: relative` required for bottom shimmer is on the class |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Status |
|---|---|---|---|
| `StatusPill` in `ToolCallPanel` | `tc.status`, `tc.startedAt/endedAt` | `ToolCall` props flowing from SSE stream via `useMessages` | ✓ FLOWING — real per-tool status from backend |
| `StatusPill` in `ExecuteCodeBody` | `isRunning`, `executionDurationMs` | `tc.status` + `tc.result` JSON parsed for duration | ✓ FLOWING — real execution data |
| `ShikiCode` in `ExecuteCodeBody` | `tc.args.code` | ToolCall args from LLM-authored Python code | ✓ FLOWING — real code string, not hardcoded |
| Thinking row in `RunCard` | `message.isPlanning` | SSE `isPlanning` event on `Message` | ✓ FLOWING — transient streaming flag from backend |
| Next-up footer in `RunCard` | `message.isPlanning`, `activeTool.status` | Same `message` prop as rest of RunCard | ✓ FLOWING — derived from real message state |
| Per-step summary rows | `summarizeToolCall(collapsedTc)` | Tool call registry in `tool-bodies/index.ts` | ✓ FLOWING — result-registry lookup against completed tool data |
| STDOUT/STDERR regions | `lines: OutputLine[]` | `tc.outputLines` (SSE stream) or parsed from `tc.result` | ✓ FLOWING — real sandbox output; no stub |

### Behavioral Spot-Checks

Step 7b: SKIPPED — all deliverables are rendering-layer CSS/React with no independently runnable entry points. Visual/animation behaviors require a live browser. Static gates (tsc, vitest, vite build) were verified by the executor and reported clean.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|---|---|---|---|
| `ShikiCode.tsx:33-47` | Rejected promise not cleared — poisoned singleton on WASM init failure | ⚠️ Warning | If Shiki WASM fails to load (ad-blocker, offline), all subsequent mounts fall through to plain-pre for the page lifetime — no retry. Functional fallback exists; not a blocker. Documented as WR-01 in REVIEW.md with fix pattern. |
| `ShikiCode.tsx:101-107` | `dangerouslySetInnerHTML` trust boundary implicit | ⚠️ Warning | Low risk today (only call site is `tc.args.code` — LLM output, not user HTML); documented contract missing. WR-02 in REVIEW.md. |
| `ToolCallPanel.tsx:660-661` | `tc.id` optional but used as Record key — pre-existing from 075.6 | ⚠️ Warning | Pre-existing pattern, not introduced by 075.8. WR-03 in REVIEW.md. |
| `StatusPill.tsx`, `ToolCallPanel.tsx`, `ExecuteCodeBody.tsx` | `formatDuration` duplicated in 3 files | ℹ️ Info | No behavioral impact; future format change requires 3-site update. IN-03 in REVIEW.md. |
| `ToolCallPanel.tsx:3` | `CheckCircle2`, `Loader2` still imported | ℹ️ Info | Both are still USED in `SubAgentBlock` (Loader2 for running sub-agent) and `SkillRow` (CheckCircle2 for loaded state). These are distinct sub-components with different semantics from the per-tool status indicator — not sweep targets per PLAN scope. Not a stub. |

No blockers. No stubs introduced. The 3 warnings are documented in REVIEW.md with concrete fix patterns; none prevents the 7 must-haves from functioning.

### Human Verification Required

All 7 must-haves are CODE-VERIFIED. The phase goal includes "visually matches the gold-standard sketches under all four lived-experience scenarios" — this eyeball-vs-sketches acceptance criterion is human-only per G-4 (CLAUDE.md: "Lived-experience UAT gate" — mandatory for any phase touching user-visible UI). The PLAN explicitly states "Chrome MCP 4-scenario UAT passes (eyeball vs sketches)" as a verification gate. The executor confirmed Chrome MCP was unavailable in its toolset.

#### 1. Single Tool, Fast (< 5s)

**Test:** Open http://localhost:5173/. Send `execute_code "print('hello')"` using Google model.
**Expected:** During execution — StatusPill shows bouncing-dot `running` pill; editor inset shows `1 | print('hello')` with Shiki syntax coloring on dark `#0d1117` background; `.tc-active-wrap` glow visible on the tool card. On completion — pill transitions to `done · X.Xs` in success green; run-card auto-collapses to summary row after a moment.
**Why human:** Animation state (bouncing dot, glow), async Shiki WASM render, and auto-collapse timing require a live browser.

#### 2. Multi-Tool Focus Mode (3+ tools)

**Test:** Send a prompt that chains `execute_code → search_documents → execute_code → web_search` in sequence.
**Expected:** Past completed steps show as `→ {summary text}` inline rows (NOT a single "Show N earlier steps" aggregate button). The currently-running tool is fully expanded with glow + bottom shimmer. Between tool completions, a dashed-border `Next: deciding next step… queued` footer appears at the bottom of the run body.
**Why human:** Focus Mode collapse requires ≥ 3 completed tools before the active step; the per-step vs aggregate toggle distinction is visual; Next-up footer timing (appears between tools, disappears when next tool starts) requires live observation.

#### 3. Long-Running with Progress

**Test:** Send `execute_code "import time; time.sleep(25)"` or similar.
**Expected:** `.tc-active-wrap` box-shadow glow visible around the active tool card during the 25s run; 2px bottom progress shimmer animates along the BOTTOM edge (not top — top is reserved for run-card header shimmer); sticky run-card header remains visible while the user scrolls the thread.
**Why human:** box-shadow and animation require real render; sticky positioning requires scroll interaction against the Radix ScrollArea Viewport.

#### 4. Compact 💭 Thinking Row

**Test:** Use any Anthropic or OpenAI model on a multi-step task. Watch the transition between tool completion and next tool start.
**Expected:** A one-line `💭 Thinking · planning next step X.Xs` row appears in dim italic at the top of the run body during the `isPlanning=true` window. The row disappears when the next tool begins.
**Why human:** `isPlanning` is a transient SSE state typically < 1s; confirming the row appears and clears correctly requires live stream observation.

#### 5. Cross-Provider Rendering Parity

**Test:** Repeat Scenario 1 against Anthropic claude-haiku-4-5 and OpenAI gpt-5.
**Expected:** StatusPill, editor inset, STDOUT/STDERR regions, and glow all render identically to the Google baseline — no provider-specific CSS branches.
**Why human:** Confirms CLAUDE.md SC#10 4-axis cross-provider bandwidth; requires actual cross-provider execution.

### Gaps Summary

No gaps. All 7 must-haves exist and are substantively implemented and wired. Status is `human_needed` because:

1. G-4 (CLAUDE.md mandatory lived-experience UAT gate) applies to all UI phases
2. The PLAN's own verification gate explicitly requires "Chrome MCP 4-scenario UAT passes (eyeball vs sketches)"
3. The executor documented that Chrome MCP was unavailable in its toolset

The code is complete and correct. Human UAT is the remaining gate.

---

_Verified: 2026-05-24_
_Verifier: Claude (gsd-verifier)_
