---
phase: 133-eval-runner-with-skill-vs-without-skill
plan: 05
type: execute
wave: 4
depends_on: ["133-04"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/skills/SkillEvalSection.tsx
  - frontend/src/components/skills/SkillFormDialog.tsx
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "Per D-07, a thin --skip-ui surface lets a user pick a provider/model, press 'Run eval', watch a live case-by-case progress list, and read a plain per-case with/without results readout — enough to drive + watch a real run end-to-end"
    - "Per RESEARCH Pattern 3, the surface reuses the existing run-stream client (subscribeToRun + getActiveRuns) verbatim — zero new stream code — because the eval run wrote a companion public.runs row in Plan 04"
    - "Per D-06/SC#3, on reload the surface reattaches to a still-live run (getActiveRuns -> subscribeToRun since=0) and the durable readout is always re-fetched from GET /skills/{id}/evals/runs/{run_id}"
    - "Per D-07, the surface is intentionally non-designed and must NOT pre-empt or constrain the Phase 137 designed Skill Evals panel (PANEL-01, G-2)"
  artifacts:
    - path: "frontend/src/components/skills/SkillEvalSection.tsx"
      provides: "thin run/watch/results functional surface for a skill"
      contains: "Run eval"
    - path: "frontend/src/lib/api.ts"
      provides: "startEvalRun + getEvalRun client functions (mirror existing fetch + getAuthHeaders shape)"
      contains: "startEvalRun"
  key_links:
    - from: "SkillEvalSection"
      to: "subscribeToRun / getActiveRuns (existing client)"
      via: "companion runs row reattach (Pattern 3)"
      pattern: "subscribeToRun"
    - from: "SkillEvalSection"
      to: "GET /skills/{id}/evals/runs/{run_id}"
      via: "getEvalRun durable readout"
      pattern: "getEvalRun"
---

<objective>
Ship the thin, non-designed functional surface (D-07, mirrors the Phase 132 `--skip-ui` model): a "Run eval" control with a provider/model picker, a live case-by-case progress list driven by the eval_* SSE events over the EXISTING run-stream client, and a plain per-case with/without results readout.

Purpose: SC#1-SC#3 need a way to drive + watch a real run end-to-end. Because Plan 04 wrote a companion `public.runs` row, the existing `subscribeToRun`/`getActiveRuns` client reattaches with ZERO new stream code (RESEARCH Pattern 3). This surface is deliberately minimal — Phase 137 (PANEL-01, G-2) owns the real designed panel and this must not constrain it.
Output: `startEvalRun`/`getEvalRun` client fns in `api.ts`, the `SkillEvalSection.tsx` component, mounted in the existing skill detail surface alongside SkillTestCasesSection.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-CONTEXT.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-PATTERNS.md

<interfaces>
<!-- Existing client to reuse verbatim + placement. From PATTERNS.md / codebase grep. -->
frontend/src/lib/api.ts:503 export async function subscribeToRun(runId, since, callbacks, signal) — SSE reattach client (reused verbatim).
frontend/src/lib/api.ts:848 export async function getActiveRuns(threadId, signal) — active-run discovery (reused verbatim).
New client fns mirror the existing fetch + getAuthHeaders() shape (api.ts:509/852): startEvalRun(skillId, {provider, model}) -> {run_id}; getEvalRun(skillId, runId) -> {eval_run, eval_results}.
Placement: SkillTestCasesSection is rendered in frontend/src/components/skills/SkillFormDialog.tsx:536 as `<SkillTestCasesSection skillId={savedSkillId} />`. Add `<SkillEvalSection skillId={savedSkillId} />` as a sibling.
Backend routes (Plan 04): POST /skills/{skill_id}/evals/runs ; GET /skills/{skill_id}/evals/runs/{run_id} ; GET /runs/{run_id}/stream?since= (reused) ; DELETE /runs/{run_id} (reused).
SSE event vocabulary on run:{run_id}: eval_case_started, eval_case_done (carries variant + output + status), eval_complete, then terminal done/error.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts client fns + SkillEvalSection thin surface</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/skills/SkillEvalSection.tsx, frontend/src/components/skills/SkillFormDialog.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts:503-522 (subscribeToRun shape) + :848-859 (getActiveRuns) + :509/852 (the fetch + getAuthHeaders() shape new fns mirror)
    - frontend/src/components/skills/SkillTestCasesSection.tsx (the sibling thin --skip-ui component to match in style/structure; how it takes skillId + loads/renders)
    - frontend/src/components/skills/SkillFormDialog.tsx:536 (where SkillTestCasesSection is rendered — add SkillEvalSection as a sibling)
    - .planning/phases/133-eval-runner-with-skill-vs-without-skill/133-PATTERNS.md lines 217-223 (thin-surface scope + reattach flow)
  </read_first>
  <action>
    Add `startEvalRun(skillId, body)` and `getEvalRun(skillId, runId)` to `frontend/src/lib/api.ts`, mirroring the existing fetch + `getAuthHeaders()` shape (POST /skills/{skillId}/evals/runs returns {run_id}; GET /skills/{skillId}/evals/runs/{runId} returns the eval_run + eval_results). Create `frontend/src/components/skills/SkillEvalSection.tsx` taking `{ skillId }`: a provider/model picker (reuse whatever model-list source the app already exposes; validate is backend's job), a "Run eval" button that calls startEvalRun then `subscribeToRun(run_id, "0", callbacks)` to drive a live case-by-case progress list from the eval_* events (eval_case_started -> mark case/variant running; eval_case_done -> show that arm's output + status), and a plain per-case with/without results readout re-fetched from `getEvalRun` on terminal done. On mount, call `getActiveRuns` for the eval thread and, if a run is still streaming, reattach via `subscribeToRun(run_id, "0")` (D-06 reattach). Keep it intentionally plain (no design polish, no new design-system primitives) — it must NOT pre-empt Phase 137. Render `<SkillEvalSection skillId={savedSkillId} />` as a sibling of SkillTestCasesSection in SkillFormDialog.tsx (~:536).
    Do NOT add a new SSE/stream client — reuse subscribeToRun/getActiveRuns verbatim.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | grep -v -e "SEED-056" | grep -e "SkillEvalSection\|evals" ; echo "tsc-done"; npx vitest run src/components/skills/SkillEvalSection 2>&1 | tail -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/skills/SkillEvalSection.tsx` exists and contains a "Run eval" control
    - `grep -c "startEvalRun\|getEvalRun" frontend/src/lib/api.ts` >= 2
    - `grep -c "subscribeToRun" frontend/src/components/skills/SkillEvalSection.tsx` >= 1 and NO bespoke EventSource/fetch-stream is introduced (`grep -ci "new EventSource" frontend/src/components/skills/SkillEvalSection.tsx` == 0)
    - `grep -c "SkillEvalSection" frontend/src/components/skills/SkillFormDialog.tsx` >= 2 (import + render)
    - `npx tsc -b --noEmit` introduces NO new type errors attributable to SkillEvalSection / the new api.ts fns (pre-existing SEED-056 rot excluded)
  </acceptance_criteria>
  <done>A user can pick a provider/model, run an eval, watch live per-case progress, read the with/without results, and reattach after reload — all on the reused run-stream client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → eval routes | The surface calls POST/GET eval routes; all authz is enforced server-side (Plan 04) — the client adds no trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Information Disclosure | client run/results fetch | mitigate | Authz is server-side (Plan 04 owner gate + 404); the thin surface only renders what the owner-scoped routes return |
| T-133-09 | Tampering | client-chosen provider/model | mitigate | Server validates model via get_model_capability (Plan 04); the picker is a convenience, not a trust boundary |
| T-133-SC | Tampering | package installs | accept | No new frontend packages — reuses the existing run-stream client and design primitives |
</threat_model>

<verification>
- tsc clean (no new errors); SkillEvalSection renders + drives a run via the reused client.
- No bespoke SSE client introduced (Pattern 3 reuse).
- Surface stays thin (no design-system additions) — does not constrain Phase 137.
</verification>

<success_criteria>
A thin functional surface drives + watches a real eval run end-to-end (run button, live case-by-case progress, with/without readout, reattach-on-reload), reusing subscribeToRun/getActiveRuns verbatim.
</success_criteria>

<output>
Create `.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-05-SUMMARY.md` when done.
</output>
