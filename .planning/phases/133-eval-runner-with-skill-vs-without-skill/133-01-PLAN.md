---
phase: 133-eval-runner-with-skill-vs-without-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/080_eval_runs_and_results.sql
  - supabase/full-schema.sql
  - backend/app/models/eval_run.py
autonomous: false
requirements: [EVAL-02]

must_haves:
  truths:
    - "Per D-08, eval_runs + eval_results tables exist in the live local DB with owner-only RLS SELECT and no INSERT/UPDATE/DELETE policies (service-role writes are the only writer)"
    - "Per Phase-132 D-10, eval_runs.skill_version_id FKs skill_versions.id and eval_results.test_case_id FKs skill_test_cases.id (traceability to the exact instruction state)"
    - "Per D-02, eval_results carries provider + model on every row (provider-keyed even though a run is single-provider per D-01)"
    - "Per D-04, eval_results.variant is a CHECK-constrained discriminator restricted to with_skill / without_skill"
    - "Pydantic StartEvalRunBody / EvalRunResponse / EvalResultResponse exist with flat single-typed fields (no multi-type unions — Gemini schema trap)"
  artifacts:
    - path: "supabase/migrations/080_eval_runs_and_results.sql"
      provides: "eval_runs + eval_results tables, FK CASCADE, indexes, owner-only RLS"
      contains: "CREATE TABLE public.eval_runs"
    - path: "backend/app/models/eval_run.py"
      provides: "Pydantic request/response models for the eval runner"
      exports: ["StartEvalRunBody", "EvalRunResponse", "EvalResultResponse"]
  key_links:
    - from: "eval_runs.skill_version_id"
      to: "skill_versions.id"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES public.skill_versions"
    - from: "eval_results.test_case_id"
      to: "skill_test_cases.id"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES public.skill_test_cases"
---

<objective>
Lay the eval-run persistence foundation: migration 080 (the eval_runs + eval_results tables Phase 132 D-09 deferred) and the Pydantic models the runner service + router will use.

Purpose: A run must be traceable to the exact skill version state (Phase-132 D-10) and each per-case result must survive Redis TTL and a backend restart (D-06 / SC#3). The schema is provider-keyed (D-02) so a later multi-provider fan-out is additive, not a rewrite.
Output: `supabase/migrations/080_eval_runs_and_results.sql` applied to the live local DB, regenerated `supabase/full-schema.sql`, and `backend/app/models/eval_run.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-CONTEXT.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-RESEARCH.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 080 SQL + Pydantic models</name>
  <files>supabase/migrations/080_eval_runs_and_results.sql, backend/app/models/eval_run.py</files>
  <read_first>
    - supabase/migrations/079_skill_versions_and_test_cases.sql (header block lines 35-39; FK targets; RLS owner-only SELECT block; index + COMMENT ON TABLE conventions — the exact analog)
    - supabase/migrations/035_runs_table.sql (durable run-audit status enum model: running/completed/failed/cancelled/interrupted)
    - .planning/phases/133-eval-runner-with-skill-vs-without-skill/133-RESEARCH.md lines 208-261 (the drafted 080 SQL — use it verbatim; honor every note)
    - backend/app/models/skill_test_case.py and backend/app/models/skill_version.py (Pydantic shape: from __future__ import annotations, plain BaseModel, flat single-typed fields, user_id/skill_id NEVER in a Create body — T-132-07)
    - .planning/phases/133-eval-runner-with-skill-vs-without-skill/133-PATTERNS.md lines 161-184 (the model field lists to mirror)
  </read_first>
  <action>
    Create `supabase/migrations/080_eval_runs_and_results.sql` from the RESEARCH-drafted SQL (lines 208-255). It MUST contain: `eval_runs` (columns id uuid PK gen_random_uuid, skill_id FK skills.id CASCADE, skill_version_id FK skill_versions.id CASCADE, user_id FK auth.users.id CASCADE, provider text, model text, status text CHECK in running/completed/failed/cancelled/interrupted default running, case_count int default 0, error text, created_at timestamptz default now, completed_at timestamptz) and `eval_results` (id uuid PK, eval_run_id FK eval_runs.id CASCADE, test_case_id FK skill_test_cases.id CASCADE, user_id FK auth.users.id CASCADE, variant text CHECK in with_skill/without_skill, provider text, model text, output text default '', status text CHECK in completed/failed/timed_out/cancelled default completed, error text, input_tokens int, output_tokens int, created_at timestamptz default now). Add indexes on every FK column + user_id. Enable RLS on both tables; add owner-only SELECT policies `USING (auth.uid() = user_id)`; add NO INSERT/UPDATE/DELETE policies (all writes go through the service-role background task — the 079/035 precedent; app-code `.eq("user_id")` is the real gate). Add COMMENT ON TABLE on both citing EVAL-02 + D-02/D-04/D-08 + Phase-132 D-10. Use a 079-style header comment block. Filename digits-only prefix `080_` (no letter suffix — silently skipped by the Supabase CLI).
    Create `backend/app/models/eval_run.py` mirroring skill_test_case.py: `from __future__ import annotations`, plain BaseModel. Define `StartEvalRunBody {provider: str, model: str}` (single provider/model per run — D-01; user_id/skill_id come from path + current_user, never the body). Define `EvalRunResponse` and `EvalResultResponse` with every table column flat-typed (use `str | None` / `int | None` / `datetime | None` — never a multi-type list union, Gemini schema trap). Field lists are in PATTERNS.md:174-184.
    Do NOT apply the migration in this task — Task 2 (a human/orchestrator checkpoint) applies it.
  </action>
  <verify>
    <automated>cd backend && . venv/Scripts/activate && python -c "from app.models.eval_run import StartEvalRunBody, EvalRunResponse, EvalResultResponse; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/080_eval_runs_and_results.sql` exists and `grep -c "CREATE TABLE public.eval_runs" supabase/migrations/080_eval_runs_and_results.sql` returns 1
    - `grep -v '^--' supabase/migrations/080_eval_runs_and_results.sql | grep -c "REFERENCES public.skill_versions"` >= 1 and same for `REFERENCES public.skill_test_cases`
    - `grep -v '^--' supabase/migrations/080_eval_runs_and_results.sql | grep -c "FOR SELECT USING (auth.uid() = user_id)"` == 2 and `grep -ci "FOR INSERT\|FOR UPDATE\|FOR DELETE" supabase/migrations/080_eval_runs_and_results.sql` == 0
    - `grep -c "with_skill" supabase/migrations/080_eval_runs_and_results.sql` >= 1 (variant discriminator present)
    - The python import command above exits 0
  </acceptance_criteria>
  <done>Migration SQL file written matching the RESEARCH draft + eval_run.py models import cleanly.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply migration 080 to the live local DB (operator/orchestrator)</name>
  <action>The orchestrator/operator applies migration 080 to the live LOCAL DB via the SQL editor or psycopg2 :54322 (NEVER db push/db reset), regenerates full-schema.sql (no --reset), and commits both. This is a human/orchestrator checkpoint — see how-to-verify for the exact steps.</action>
  <what-built>Migration 080 SQL is written but NOT yet applied to any database.</what-built>
  <how-to-verify>
    Per the PROJECT convention (CLAUDE.md schema rules; Phase 132 precedent), the orchestrator/operator applies migration 080 to the live LOCAL DB — NEVER `supabase db push` / `db reset` (those wipe dev data). Two equivalent options:
    1. Paste the full contents of `supabase/migrations/080_eval_runs_and_results.sql` into the Supabase SQL editor and run it; OR
    2. Apply via psycopg2 against the local DB at port 54322 (the Phase 132 model — orchestrator runs it).
    After applying, regenerate the bootstrap artifact WITHOUT a reset:
    `bash scripts/regenerate-full-schema.sh` (no `--reset` — live-DB dump rebuilds `supabase/full-schema.sql`).
    Then confirm the tables exist:
    psql/psycopg2 query `SELECT to_regclass('public.eval_runs'), to_regclass('public.eval_results');` returns both non-null.
    Commit `supabase/migrations/080_eval_runs_and_results.sql` + the regenerated `supabase/full-schema.sql` together.
  </how-to-verify>
  <acceptance_criteria>
    - `to_regclass('public.eval_runs')` and `to_regclass('public.eval_results')` both non-null on the live local DB
    - `supabase/full-schema.sql` regenerated (no reset) and contains `eval_runs` + `eval_results`
    - Migration NOT applied via `db push`/`db reset` (dev data preserved)
  </acceptance_criteria>
  <resume-signal>Type "applied" once both tables exist on the live local DB and full-schema.sql is regenerated, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-role client → Postgres | All eval writes use the service-role key which BYPASSES RLS; the table's RLS SELECT policy is defense-in-depth only |
| migration apply → live dev DB | A wrong apply path (`db push`/`db reset`) destroys dev data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Information Disclosure | eval_runs / eval_results SELECT | mitigate | Owner-only RLS `USING (auth.uid() = user_id)` as defense-in-depth; the real gate is app-code `.eq("user_id")` (Plans 03/04) |
| T-133-EoP | Elevation of Privilege | INSERT/UPDATE/DELETE policies | mitigate | No write policies — only the service-role background task writes (079/035 precedent), preventing client-forged rows |
| T-133-DATA | Tampering | dev DB during migration apply | mitigate | Apply via SQL editor / psycopg2 :54322 only; never `db push`/`db reset` (checkpoint enforces) |
| T-133-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase — zero new dependencies (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Migration 080 applied to live local DB; both tables resolve via `to_regclass`.
- `full-schema.sql` regenerated (no reset) and committed alongside the migration.
- `backend/app/models/eval_run.py` imports cleanly.
</verification>

<success_criteria>
eval_runs + eval_results exist with the D-08 shape, owner-only RLS SELECT, FK CASCADE to skill_versions/skill_test_cases (D-10), provider-keyed results (D-02), with_skill/without_skill discriminator (D-04); Pydantic models exist.
</success_criteria>

<output>
Create `.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-01-SUMMARY.md` when done.
</output>
