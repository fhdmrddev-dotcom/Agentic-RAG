---
phase: 133-eval-runner-with-skill-vs-without-skill
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/services/agent_loop.py
  - backend/tests/test_agent_loop_catalog_override.py
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "Per D-03/D-04, RunContext exposes one additive default-off field skill_catalog_override (None = query DB; () = inject nothing; (skill,) = inject exactly that skill)"
    - "Per SC#4 (D-14 red line), with skill_catalog_override=None the skill-injection block runs the IDENTICAL DB query + produces the IDENTICAL system prompt as before the field landed (Deep Mode byte-identical)"
    - "Per D-03, skill_catalog_override=(skill,) injects ONLY that skill into the '## Available Skills' note"
    - "Per D-04, skill_catalog_override=() injects no catalog note at all (the existing `if enabled_skills:` guard short-circuits)"
    - "Every existing RunContext call site (threads.py:1490-1500) is byte-unchanged because the new field defaults to None"
  artifacts:
    - path: "backend/app/services/agent_loop.py"
      provides: "RunContext.skill_catalog_override additive field + the single read-site change in the injection block"
      contains: "skill_catalog_override"
    - path: "backend/tests/test_agent_loop_catalog_override.py"
      provides: "RED-first regression guard for the additive field + Deep-Mode-unchanged proof"
      contains: "test_deep_mode_unchanged"
  key_links:
    - from: "RunContext.skill_catalog_override"
      to: "agent_loop skill-injection block (:1174-1195)"
      via: "data-source branch inside `if body.agent_mode != 'explorer'`"
      pattern: "skill_catalog_override is None"
---

<objective>
Add the ONE additive, default-off `RunContext.skill_catalog_override` field that makes the honest A/B possible (D-03 WITH = target-skill-only; D-04 WITHOUT = empty catalog), modeled byte-for-byte on the Phase 092 `resume_dropped_tool_calls` in-file precedent, and guard it with a Deep-Mode-byte-identical regression test (SC#4).

Purpose: The skill-catalog injection block (agent_loop.py:1174-1195) is hardcoded with no override hook, and the only existing "no skills" lever (`agent_mode == "explorer"`) also swaps the system prompt, toolset, and iteration cap — a dishonest A/B. The sanctioned extension is a single additive input the SAME code path reads (not a fork — D-14 forbids forking the provider path, not additive inputs). This is a G-5 hot-file touch on the Deep/agent-loop path, so it ships with a regression guard FIRST (RED → GREEN).
Output: the field + the single read-site change in `agent_loop.py`, plus `test_agent_loop_catalog_override.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-RESEARCH.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-PATTERNS.md

<interfaces>
<!-- The exact in-file precedent and read-site the executor edits. Extracted from PATTERNS.md / RESEARCH.md — no codebase scavenger hunt needed. -->
RunContext is a @dataclass(frozen=True) at agent_loop.py:166-194. Phase 092 added two additive default-off fields at the end: `resume_dropped_tool_calls: bool = False` and `dropped_tool_calls: tuple = ()`. The new field is appended in the same style:
  skill_catalog_override: tuple[dict, ...] | None = None
The skill-injection read site is agent_loop.py:1174-1195, inside `if body.agent_mode != "explorer":` — it runs `supabase.table("skills").select("name, description").or_(f"user_id.eq.{current_user['id']},is_global.eq.true").eq("is_enabled", True).order("name")`, sets `enabled_skills = resp.data or []`, then `if enabled_skills:` builds the `## Available Skills` catalog_note (reads only `s['name']` / `s['description']`) and appends it to `active_system_prompt`.
The only existing RunContext construction is threads.py:1490-1500 (Deep branch) — it does NOT pass the new field, so it stays byte-identical.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the catalog-override + Deep-unchanged regression test</name>
  <files>backend/tests/test_agent_loop_catalog_override.py</files>
  <read_first>
    - backend/app/services/agent_loop.py:166-194 (RunContext + the Phase 092 additive fields) and :1174-1195 (the injection block to assert against)
    - .planning/phases/133-eval-runner-with-skill-vs-without-skill/133-PATTERNS.md lines 103-157 (the exact field + read-site change spec)
    - backend/tests/test_skill_tuner_routes.py and backend/tests/test_132_test_cases.py (mock supabase + fixture patterns; how the existing suite mocks the skills table query)
    - .planning/phases/133-eval-runner-with-skill-vs-without-skill/133-VALIDATION.md lines 44-50 (the SC#4 commands these tests must satisfy)
  </read_first>
  <behavior>
    - test_deep_mode_unchanged: with `skill_catalog_override=None`, the injection block issues the existing `skills` query (`.or_(user_id.eq...,is_global.eq.true).eq(is_enabled, True)`) and the resulting `active_system_prompt` equals the prompt produced by the pre-field behavior given the same mocked skills rows (assert the DB query path is taken AND the `## Available Skills` note is built from the query results).
    - test_with_arm_injects_only_target: with `skill_catalog_override=({"name": "X", "description": "d"},)`, NO skills DB query is issued and the catalog note contains exactly skill "X" and no other.
    - test_without_arm_injects_nothing: with `skill_catalog_override=()`, NO skills DB query is issued and NO `## Available Skills` note is appended (the `if enabled_skills:` guard short-circuits).
  </behavior>
  <action>
    Create `backend/tests/test_agent_loop_catalog_override.py`. Mock the supabase skills-table query (assert it IS called when override is None, assert it is NOT called when override is `()` or a tuple). Exercise the injection block by constructing a `RunContext` with each override value and inspecting the produced `active_system_prompt` (test the smallest unit that exercises the :1174-1195 block — extract a helper call or drive `run_agent_loop` with the heavy work mocked, mirroring how the tuner tests isolate the skills query). Tests MUST fail RED now because the field does not yet exist (constructing RunContext with `skill_catalog_override=` raises TypeError) — that failing state is expected and correct.
    Do NOT modify agent_loop.py in this task.
  </action>
  <verify>
    <automated>cd backend && . venv/Scripts/activate && pytest tests/test_agent_loop_catalog_override.py -x 2>&1 | grep -q -e TypeError -e "unexpected keyword" -e FAILED && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_agent_loop_catalog_override.py` exists and defines `test_deep_mode_unchanged`, `test_with_arm_injects_only_target`, `test_without_arm_injects_nothing`
    - Running it now fails (RED) because RunContext has no `skill_catalog_override` field
  </acceptance_criteria>
  <done>Three failing tests describe the additive field + the Deep-unchanged guarantee.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add the additive field + the single read-site branch</name>
  <files>backend/app/services/agent_loop.py</files>
  <read_first>
    - backend/app/services/agent_loop.py:186-194 (append the field right after `dropped_tool_calls`, same default-off discipline) and :1093 (where ctx-resolved locals are aliased, if the body reads via a local name) and :1174-1195 (the read site)
    - .planning/phases/133-eval-runner-with-skill-vs-without-skill/133-PATTERNS.md lines 125-155 (the exact field comment + the data-source branch — the catalog_note build stays byte-identical)
  </read_first>
  <action>
    Add `skill_catalog_override: tuple[dict, ...] | None = None` to the `RunContext` frozen dataclass immediately after `dropped_tool_calls`, with a comment citing Phase 133 / EVAL-02 / D-03 / D-04 / the 092 default-off precedent. In the injection block (:1174-1195), change ONLY the data source: when `ctx.skill_catalog_override is None` run the existing DB query unchanged (current behavior); otherwise set `enabled_skills = list(ctx.skill_catalog_override)`. Leave the `if enabled_skills:` guard and the `## Available Skills` catalog_note build byte-identical below it. If the loop body reads ctx fields via local aliases near :1093, alias `skill_catalog_override` the same way. Do NOT touch the `agent_mode == "explorer"` branch, the system prompt, the toolset, or `max_iterations`. Do NOT change any RunContext call site — the default keeps threads.py:1490-1500 byte-identical.
  </action>
  <verify>
    <automated>cd backend && . venv/Scripts/activate && pytest tests/test_agent_loop_catalog_override.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_agent_loop_catalog_override.py -x` exits 0 (all three tests GREEN, including `test_deep_mode_unchanged`)
    - `grep -c "skill_catalog_override" backend/app/services/agent_loop.py` >= 2 (field declaration + read-site branch)
    - `grep -c "agent_mode" backend/app/services/agent_loop.py` is unchanged from before the edit (explorer branch untouched)
    - The existing agent-loop / threads test suite still passes (no Deep regression): `pytest tests/ -k "agent_loop or threads or tuner" -q` shows no NEW failures vs baseline
  </acceptance_criteria>
  <done>The additive field + one read-site branch land; Deep Mode byte-identical proven by test_deep_mode_unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval service → RunContext → shared agent loop | The eval arms drive the shared Deep/agent-loop path; any change here risks Deep Mode regression (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-05 | Tampering | shared agent_loop catalog injection (G-5 hot file) | mitigate | Additive default-off field only; single read-site data-source branch; `test_deep_mode_unchanged` regression guard proves the None path is byte-identical (SC#4) |
| T-133-06 | Elevation of Privilege | catalog override content | mitigate | Override dicts read only `name`/`description`; eval service (Plan 03) sources them from the owner-scoped version snapshot, never arbitrary client input |
| T-133-SC | Tampering | package installs | accept | No installs in this phase |
</threat_model>

<verification>
- RED then GREEN on test_agent_loop_catalog_override.py.
- No new failures in the agent_loop/threads/tuner test slice (Deep path intact).
- agent_loop.py appears in this plan's files_modified (G-5 ledger audit).
</verification>

<success_criteria>
One additive default-off `skill_catalog_override` field exists; the injection block honors None (DB query, Deep byte-identical), `()` (inject nothing), and `(skill,)` (inject only that skill); SC#4 regression test green.
</success_criteria>

<output>
Create `.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-02-SUMMARY.md` when done.
</output>
