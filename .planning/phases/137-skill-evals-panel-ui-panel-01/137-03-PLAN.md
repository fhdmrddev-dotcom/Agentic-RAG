---
phase: 137-skill-evals-panel-ui-panel-01
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/skills/studio/RunCaseDetail.tsx
  - frontend/src/components/skills/studio/RunCaseDetail.test.tsx
  - frontend/src/components/skills/studio/RunHistory.tsx
  - frontend/src/components/skills/studio/RunHistory.test.tsx
  - frontend/src/components/skills/studio/RunBar.tsx
  - frontend/src/components/skills/studio/RunBar.test.tsx
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "Run history is a 055-B list of expandable rows: provider logo (048 @lobehub/icons map) + model + version binding + honest rollup per row, expanding IN PLACE to per-case detail (D-04)"
    - "The honest rollup appends '· N not measured' when measured_count < case_count (D-04)"
    - "not_measured renders as excluded-never-failed and judge_error as neither-pass-nor-fail — NEVER colored as fail (D-04)"
    - "An interrupted run shows a banner + re-run affordance, never a silent failure; a running run shows live per-arm progress with NO mid-run verdicts (D-04)"
    - "Per-case detail shows side-by-side WITH/WITHOUT arms + judge verdict chip/score/reason + token counts + 'your rating' thumbs labeled DISTINCT from the judge verdict (D-04)"
    - "Case rows are prompt-first (prompt text truncated one-line, expected_behavior as the quiet second line); the uuid does not appear as a label (D-11)"
    - "A compact run bar (provider/model picker + Run) is the launch surface; the live run appears as the top expandable row (D-12)"
    - "A finished run with >= 1 failed measured case surfaces an inline 'Propose an improvement?' post-run nudge pointing to the existing propose action (D-09)"
  artifacts:
    - path: "frontend/src/components/skills/studio/RunCaseDetail.tsx"
      provides: "Per-case side-by-side arms + honest verdict badge + labeled thumbs"
      min_lines: 90
    - path: "frontend/src/components/skills/studio/RunHistory.tsx"
      provides: "055-B expandable run rows with provider logos + honest rollup + interrupted/running states"
      min_lines: 90
    - path: "frontend/src/components/skills/studio/RunBar.tsx"
      provides: "Compact provider/model picker + Run launch bar"
      min_lines: 50
  key_links:
    - from: "RunHistory.tsx"
      to: "RunCaseDetail.tsx"
      via: "row expand renders the per-case detail in place"
      pattern: "RunCaseDetail"
    - from: "RunHistory.tsx"
      to: "providerLogo util"
      via: "row avatar"
      pattern: "providerLogo"
---

<objective>
Build the three presentational run-display components of the Evals tab — `RunCaseDetail` (per-case side-by-side arms + labeled thumbs), `RunHistory` (055-B expandable rows), and `RunBar` (picker + Run). These are the leaves the stateful `EvalsTab` container (Plan 05) lifts its handlers into; they define the prop contracts and carry the load-bearing honest-state set + the prompt-first case labels.

Purpose: Deliver the PANEL-01 run history + per-run detail + inline ratings subsurfaces with the D-04 honesty locks and the D-11 prompt-first clarity fix, sourced verbatim from the battle-tested `SkillEvalSection` render logic (lift the honesty helpers, re-skin the chrome to 055-B).
Output: `RunCaseDetail.tsx`, `RunHistory.tsx`, `RunBar.tsx` (+ specs).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-RESEARCH.md
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-PATTERNS.md
@.planning/sketches/055-run-history-and-detail/README.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/skills/SkillEvalSection.tsx
@frontend/src/lib/providerLogo.tsx

<interfaces>
<!-- Honest verdict helper to LIFT VERBATIM (never fabricate pass/fail). SkillEvalSection :68-79 -->
verdict_state "graded" -> verdict_passed ? "PASS" : "FAIL"
verdict_state "not_measured" -> "not measured"   // excluded-never-failed
verdict_state "judge_error" -> "judge error"     // neither pass nor fail
default -> null

Types (frontend/src/types/index.ts): EvalRun { id, provider, model, skill_version_id, status, passed_count, measured_count, case_count, created_at }, EvalResult { id, test_case_id, variant, verdict_state, verdict_passed, score, reason, tokens..., rating }.
VARIANTS = ["with_skill", "without_skill"] (SkillEvalSection :60). Per-case grouping byCase (:525-531).
providerLogo(provider): ProviderMark | null  (frontend/src/lib/providerLogo.tsx :89-92; keys are runs.provider strings: zhipu not glm, moonshot not kimi; null -> caller renders a Bot fallback).
Rollup line (SkillEvalSection :631-637): "{passed}/{measured} with-skill cases passed" + when measured<case_count append " · {case_count-measured} not measured".
Picker + Run (SkillEvalSection :569-614 + handleRun :390-410); provider list from getProviders() (:337-342).
Handlers are passed in as props by EvalsTab (Plan 05): onRun, onRate, onExpand, onRerun, onProposeFromRun.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunCaseDetail — side-by-side arms + honest verdict + labeled thumbs</name>
  <files>frontend/src/components/skills/studio/RunCaseDetail.tsx, frontend/src/components/skills/studio/RunCaseDetail.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.tsx (verdictBadge :68-79 — LIFT VERBATIM; per-arm render :836-916; handleRate :419-427; VARIANTS :60; byCase :525-531)
    - .planning/sketches/055-run-history-and-detail/README.md (the five honest run states + the "your rating" distinct-from-judge lock)
    - frontend/src/types/index.ts (EvalResult, EvalRun)
  </read_first>
  <behavior>
    - Renders per-case: the prompt (leading, per D-11) + expected_behavior quiet second line; the uuid is NOT shown as a label.
    - Shows the two arms WITH_SKILL and WITHOUT_SKILL side-by-side (from VARIANTS + byCase grouping).
    - Judge block: verdict chip (PASS/FAIL/"not measured"/"judge error" via the lifted verdictBadge) + score + reason + token counts.
    - not_measured and judge_error are visually neutral — NOT colored as FAIL.
    - "Your rating" thumbs up/down with aria-pressed reflecting the current rating, visually separated from the judge chip (two truths, never blended); clicking calls the onRate prop.
  </behavior>
  <action>
    Create `RunCaseDetail` (props `{ testCase, results: EvalResult[], onRate: (resultId: string, choice: "up"|"down"|null) => void }`). Lift `verdictBadge` verbatim from SkillEvalSection :68-79 (do not re-derive pass/fail). Group results by variant using the VARIANTS order ["with_skill","without_skill"] and render side-by-side arms with judge chip + score + reason + tokens. Lead each case with the prompt text truncated to one line (per D-11) with expected_behavior as a dimmed second line; do not render the test-case id as a visible label. Render the thumbs as a separate "Your rating" control with `aria-pressed` bound to `result.rating` and an `onRate` click that toggles (choosing the same rating clears to null). Keep the thumbs visually distinct from the judge verdict chip. Author the co-located spec fresh (mock nothing — pure presentational) asserting: PASS/FAIL/"not measured"/"judge error" render from verdict_state; not_measured/judge_error carry no fail styling; both arms render; the prompt leads and the uuid is absent; clicking a thumb calls onRate with the expected args.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/RunCaseDetail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source lifts the honest verdict mapping: `grep -c 'not_measured\|judge_error' frontend/src/components/skills/studio/RunCaseDetail.tsx` >= 2.
    - Thumbs use aria-pressed: grep matches `aria-pressed`.
    - Spec asserts not_measured/judge_error are not styled as fail AND that onRate fires with the toggle semantics.
    - No test-case uuid rendered as a label (spec asserts the id string is absent from the visible output).
  </acceptance_criteria>
  <done>RunCaseDetail renders honest side-by-side arms + labeled thumbs + prompt-first case text; spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunHistory — 055-B expandable rows with honest states</name>
  <files>frontend/src/components/skills/studio/RunHistory.tsx, frontend/src/components/skills/studio/RunHistory.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.tsx (durable readout :836-921; rollup line :631-637; live per-arm state)
    - frontend/src/lib/providerLogo.tsx (:89-92 — providerLogo returns a mark or null; Bot fallback)
    - .planning/sketches/055-run-history-and-detail/README.md (expandable rows winner B + the five honest run states)
    - frontend/src/components/skills/studio/RunCaseDetail.tsx (the expanded body, from Task 1)
  </read_first>
  <behavior>
    - Lists runs newest-first; each row = provider logo + model + version binding + honest rollup.
    - Rollup appends " · N not measured" when measured_count < case_count.
    - Rows expand IN PLACE to render RunCaseDetail for that run's results.
    - status="interrupted" -> a banner + a re-run affordance (calls onRerun), never a silent failure.
    - A running run is the top row showing live per-arm progress with NO mid-run verdicts (verdicts land only at finalize).
  </behavior>
  <action>
    Create `RunHistory` (props `{ runs: EvalRun[], expandedRunId, onToggleExpand, resultsByRun, liveByCase, onRate, onRerun, onProposeFromRun }`). For each row render `providerLogo(run.provider)` (null -> a `Bot` fallback), the model, the version binding, and the honest rollup (port :631-637, appending "· N not measured" when measured_count < case_count). Expanding a row renders `<RunCaseDetail>` for that run. For `status === "interrupted"` render a banner + a Re-run button wired to `onRerun(run.id)`. For a running run (top row), render live per-arm progress from `liveByCase` with NO pass/fail verdict shown mid-run. Add the D-09 post-run "Propose an improvement?" affordance on a finished row that has >= 1 failed measured case, wired to `onProposeFromRun(run.id)`. Author the co-located spec fresh (no rotted-sibling imports): rollup "· N not measured" appears when measured<case_count; interrupted row shows the banner + re-run; a running row shows no verdict; expand renders RunCaseDetail; the propose nudge appears only when a finished run has a failed measured case.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/RunHistory.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source renders provider logos: grep matches `providerLogo`.
    - Rollup honesty present: `grep -c 'not measured' frontend/src/components/skills/studio/RunHistory.tsx` >= 1.
    - Interrupted handling present: grep matches `interrupted`.
    - Spec asserts: the "· N not measured" append, the interrupted banner + re-run, no mid-run verdict on a running row, and the conditional propose nudge.
  </acceptance_criteria>
  <done>RunHistory renders 055-B expandable rows with all honest states + the D-09 nudge; spec green.</done>
</task>

<task type="auto">
  <name>Task 3: RunBar — compact provider/model picker + Run</name>
  <files>frontend/src/components/skills/studio/RunBar.tsx, frontend/src/components/skills/studio/RunBar.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.tsx (picker + Run :569-614; provider hydration :337-342; handleRun :390-410)
    - .planning/sketches/055-run-history-and-detail/README.md (the run-bar / D-12 compact launch bar placement)
  </read_first>
  <action>
    Create `RunBar` (props `{ providers, provider, model, models, onProviderChange, onModelChange, running, onRun }`) as a compact one-row control: a provider `<select>` (changing it resets model to models[0]), a model `<select>`, and a Run button disabled while `running || !provider || !model` (Play icon at rest, Loader2 spinner while running). This is a pure controlled component — EvalsTab (Plan 05) owns the provider/model state so selection is preserved across skills (D-12). Author the co-located spec: the Run button is disabled while running and when provider/model is empty; changing provider triggers onProviderChange; clicking Run calls onRun.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/RunBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Run button disabled state gates on `running` and empty provider/model (spec asserts both).
    - Clicking Run calls `onRun` (spec asserts).
    - RunBar holds no fetch/launch logic itself — it is a controlled component receiving state + callbacks as props (no `startEvalRun` import in the file).
  </acceptance_criteria>
  <done>RunBar is a compact controlled picker + Run bar; spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server EvalResult/EvalRun -> client render | The client renders the durable server readout; verdicts are server truth. |
| rating write -> owner-scoped PUT | Ratings are server writes (IDOR-404 hardened); the client toggles then refetches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-04 | Tampering | RunCaseDetail thumbs / RunHistory rows | mitigate | Thumbs render `result.rating` from the server readout and call `onRate` (which Plan 05 wires to a PUT-then-refetch) — no optimistic local rating truth (T-135-04). RunHistory shows verdicts only at finalize, never mid-run. |
| T-137-05 | Injection (XSS) | eval output / reason / prompt render | mitigate | Eval outputs, judge reason, prompt text, and expected_behavior render as React text nodes; no `dangerouslySetInnerHTML`. |
| T-137-03 | Information Disclosure | run/result reads | mitigate | Components render only the runs/results passed in from owner-scoped fetches; they construct no ids and echo server responses verbatim (masked/absent states render honestly). |
| T-137-SC | Tampering (supply chain) | npm install surface | accept | Zero new packages; @lobehub/icons already installed; slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/skills/studio/RunCaseDetail.test.tsx src/components/skills/studio/RunHistory.test.tsx src/components/skills/studio/RunBar.test.tsx` all green.
- Grep gates confirm the honest verdict mapping, the "not measured" rollup, and no dangerouslySetInnerHTML.
</verification>

<success_criteria>
The three run-display leaves render the PANEL-01 run history + per-run detail + inline ratings subsurfaces with every D-04 honest state, D-11 prompt-first labels, and the D-12 compact run bar, as controlled components ready for EvalsTab to wire.
</success_criteria>

<output>
Create `.planning/phases/137-skill-evals-panel-ui-panel-01/137-03-SUMMARY.md` when done.
</output>
