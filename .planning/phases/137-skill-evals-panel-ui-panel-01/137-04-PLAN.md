---
phase: 137-skill-evals-panel-ui-panel-01
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/skills/studio/ProposalCard.tsx
  - frontend/src/components/skills/studio/ProposalCard.test.tsx
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "The 135 propose->review->approve card is re-skinned for the Studio (chrome aligned to the 055 chips/tokens/honesty language), flow untouched (D-08)"
    - "The proposal diff renders via the SAME lineDiff util the Versions tab uses (one renderer, two homes) (D-08)"
    - "All 135 honesty locks are preserved: proposer rationale + evidence, PromotionGate counts, the override_forced record, and the honest 'interrupted — not promoted' state (D-08)"
    - "Status-driven action rows are correct: proposed->Approve/Reject; re_evaling/approved->live progress (+Reject escape); promoted->'(forced override)' + gate counts; not_promoted->destructive line + Force-promote + gate counts; interrupted->'not promoted' + Re-run (D-08)"
    - "Proposal state is rendered from the reconciled server row — never optimistic (a card cannot show 'promoted' unless the server reconciled it) (D-08)"
  artifacts:
    - path: "frontend/src/components/skills/studio/ProposalCard.tsx"
      provides: "Re-skinned proposal lifecycle card (render only; handlers injected as props by EvalsTab)"
      min_lines: 110
    - path: "frontend/src/components/skills/studio/ProposalCard.test.tsx"
      provides: "Unit coverage of status-driven rows + gate counts + override_forced + interrupted + diff"
      min_lines: 70
  key_links:
    - from: "ProposalCard.tsx"
      to: "lineDiff util"
      via: "proposal diff render (base_instructions vs proposed_instructions)"
      pattern: "lineDiff"
    - from: "ProposalCard.tsx"
      to: "PromotionGate (server truth)"
      via: "renderGateCounts on both promoted + not_promoted branches"
      pattern: "override_forced|gate"
---

<objective>
Re-skin the 135 self-improvement proposal card for the Studio — the propose->diff->approve/reject->auto-re-eval->promote/not-promote lifecycle. This is render-only: the handlers are the battle-tested `SkillEvalSection` handlers that the stateful `EvalsTab` (Plan 05) lifts and injects as props. Every 135 honesty lock is preserved; only the chrome changes (D-08).

Purpose: Deliver the proposal-loop home in the Evals tab (D-08) without rewriting the lifecycle logic, keeping the override_forced record, PromotionGate counts, and the honest interrupted state un-softened.
Output: `ProposalCard.tsx` (+ spec).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-RESEARCH.md
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-PATTERNS.md
@.planning/phases/135-self-improvement-loop-si-01/135-CONTEXT.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/skills/SkillEvalSection.tsx
@frontend/src/lib/lineDiff.ts

<interfaces>
<!-- Proposal render analogs to lift (render only; handlers arrive as props). -->
SkillEvalSection.tsx:
- proposal block :650-834 (the status-driven render)
- renderGateCounts :97-112 (honest terminal counts; if !gate render nothing, never a fabricated pass)
- diff render :687-706 (lineDiff rows: add=emerald, remove=destructive, context=foreground/70)
Types (frontend/src/types/index.ts): SkillProposal { id, status: "proposed"|"approved"|"re_evaling"|"promoted"|"not_promoted"|"interrupted", base_instructions, proposed_instructions, rationale, evidence, override_forced, gate: PromotionGate | null, re_eval_run_id }, PromotionGate { passed, no_regression, improved, prev_pass, still_pass, newly_pass, not_measured, ... }.
Handlers injected by EvalsTab (Plan 05): onPropose, onApprove, onReject, onRerun, onForcePromote.
lineDiff(base, next): DiffRow[] — the SAME util VersionsTab uses.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the re-skinned ProposalCard</name>
  <files>frontend/src/components/skills/studio/ProposalCard.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.tsx (proposal block :650-834; renderGateCounts :97-112 — LIFT VERBATIM; diff render :687-706)
    - .planning/phases/135-self-improvement-loop-si-01/135-CONTEXT.md (the proposal lifecycle D-05..D-10 + the honest 'interrupted — not promoted' D-14 lock)
    - .planning/sketches/055-run-history-and-detail/README.md (the 055 chip/token/honesty language to align the chrome to — D-08 "lightly re-skinned")
    - frontend/src/types/index.ts (SkillProposal, PromotionGate)
  </read_first>
  <behavior>
    - proposed -> Approve / Reject buttons.
    - re_evaling / approved -> live re-eval progress + a Reject escape.
    - promoted -> "Promoted… (forced override)" when override_forced + the honest gate counts.
    - not_promoted -> a destructive line + a Force-promote action + the honest gate counts.
    - interrupted -> "not promoted" + a Re-run affordance (never a silent success).
    - Always renders proposer rationale + evidence and the diff (lineDiff).
    - renderGateCounts returns nothing when gate is null (never a fabricated pass).
    - Renders the reconciled server proposal state only — never an optimistic "promoted".
  </behavior>
  <action>
    Create `ProposalCard` (props `{ proposal: SkillProposal | null, reEvalLive, onPropose, onApprove, onReject, onRerun, onForcePromote }`). Lift `renderGateCounts` verbatim from SkillEvalSection :97-112 (returns null when gate is absent). Render the status-driven action rows exactly per the six SkillProposal statuses (port :728-830 semantics). Render the proposer rationale + evidence, and the diff via `lineDiff(proposal.base_instructions, proposal.proposed_instructions)` using the add=emerald / remove=destructive / context=foreground/70 row JSX. Preserve the override_forced amber record on the promoted branch and the honest "interrupted — not promoted" copy + Re-run on the interrupted branch. Align chrome (chips, tokens, spacing, honesty wording) to the 055 run-row language per D-08 — do NOT alter the lifecycle flow or the copy semantics. Buttons call the injected handler props only (no api.ts calls in this file). Render all copy/diff/instruction text as React text nodes (no dangerouslySetInnerHTML).

    Note: the paired behavioral spec is authored in Task 2 (fresh). This task's automated gate is `cd frontend && npx tsc -p tsconfig.json --noEmit` — the completed component compiles clean under the project's `noUnusedLocals`/`noUnusedParameters`. Task 2's cases are the real green gate.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source renders the diff via lineDiff: grep matches `lineDiff`.
    - The override_forced record and gate counts render: `grep -c 'override_forced\|gate' frontend/src/components/skills/studio/ProposalCard.tsx` >= 2.
    - The card imports NO api.ts function (handlers are props): `grep -c "from \"@/lib/api\"" frontend/src/components/skills/studio/ProposalCard.tsx` == 0.
    - No `dangerouslySetInnerHTML` in the file.
    - `cd frontend && npx tsc -p tsconfig.json --noEmit` reports no new type errors.
  </acceptance_criteria>
  <done>ProposalCard renders all six statuses with the honesty locks preserved and the diff via lineDiff; handlers injected as props; typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author the ProposalCard spec (fresh)</name>
  <files>frontend/src/components/skills/studio/ProposalCard.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.test.tsx (:1-55 — fresh-mock pattern)
    - frontend/src/components/skills/studio/ProposalCard.tsx (component under test)
  </read_first>
  <action>
    Author `ProposalCard.test.tsx` fresh (no rotted-sibling imports). Cover, one case per status: (1) proposed -> Approve + Reject present, clicking Approve calls onApprove; (2) re_evaling -> live progress + Reject escape, no promote yet; (3) promoted with override_forced=true -> the "(forced override)" record + gate counts render; (4) not_promoted -> the destructive line + Force-promote + gate counts render; (5) interrupted -> "not promoted" + Re-run present (no silent success); (6) the diff renders lineDiff rows (assert an add + a remove row); (7) gate=null -> renderGateCounts contributes nothing (no fabricated pass count).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/ProposalCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/skills/studio/ProposalCard.test.tsx` passes with >= 6 cases across the statuses.
    - The interrupted case asserts "not promoted" + Re-run (honest, not a silent success).
    - The gate=null case asserts no fabricated pass count is rendered.
  </acceptance_criteria>
  <done>Spec green; all six statuses + the honesty locks asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server SkillProposal row -> client render | The proposal state is the reconciled DB row; the client renders it, never derives it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-04 | Tampering | ProposalCard status render | mitigate | The card renders the reconciled `proposal.status` from the server row and never shows an optimistic "promoted" (T-135-04); handlers are injected props that Plan 05 wires to endpoint-then-refetch. |
| T-137-01 | Elevation of Privilege | force-promote branch | mitigate | Force-promote only calls the injected `onForcePromote` handler; the server re-verifies and records the override (already shipped 136). The card echoes `override_forced` un-softened, never softens or hides it. |
| T-137-05 | Injection (XSS) | diff + rationale + evidence render | mitigate | Diff rows, rationale, evidence, and instruction bodies render as React text nodes; no `dangerouslySetInnerHTML`. |
| T-137-SC | Tampering (supply chain) | npm install surface | accept | Zero new packages; slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/skills/studio/ProposalCard.test.tsx` green; `cd frontend && npx tsc -p tsconfig.json --noEmit` clean.
- Grep gates confirm the diff via lineDiff, the override_forced/gate render, no api.ts import, no dangerouslySetInnerHTML.
</verification>

<success_criteria>
The proposal card is re-skinned to the Studio chrome with every 135 honesty lock intact (rationale/evidence, gate counts, override_forced, honest interrupted), the diff via the shared lineDiff, and no lifecycle logic rewritten.
</success_criteria>

<output>
Create `.planning/phases/137-skill-evals-panel-ui-panel-01/137-04-SUMMARY.md` when done.
</output>
