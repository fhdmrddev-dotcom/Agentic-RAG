---
phase: 137-skill-evals-panel-ui-panel-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/skills/studio/LifecycleStepper.tsx
  - frontend/src/components/skills/studio/LifecycleStepper.test.tsx
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "The status surface renders as the 054-B Cases -> Eval -> Gate -> Published lifecycle stepper with counts on stage nodes (D-03)"
    - "The stepper NEVER recomputes publish-readiness client-side — it renders getPublishGate fields only (met/state/measured/passed/passing_run_id/last_override) (D-03)"
    - "The ⚡ collision (gate met on an older passing run + newest run failed) renders as a met Gate node WITH an Eval node carrying the newest run's honest count — two labeled facts, never a flat contradiction (D-03)"
    - "passed_on_older_version reads 'the passing eval is stale — it measured vN-1, the live skill is vN' (D-03)"
    - "The force-publish override record (last_override) ALWAYS renders as an amber receipt, never softened (D-03)"
    - "ONE shared component serves both the Studio header (condensed strip variant) and the slim detail panel (full variant) (D-10)"
  artifacts:
    - path: "frontend/src/components/skills/studio/LifecycleStepper.tsx"
      provides: "Shared 054-B lifecycle stepper over PublishGate (variant: full | strip)"
      min_lines: 80
    - path: "frontend/src/components/skills/studio/LifecycleStepper.test.tsx"
      provides: "Unit coverage of 4 gate states + collision + no-recompute + both variants"
      min_lines: 60
  key_links:
    - from: "LifecycleStepper.tsx"
      to: "PublishGate (server truth)"
      via: "props render of met/state/measured/passed/passing_run_id/last_override"
      pattern: "publishGate\\.(met|state|measured|passed|last_override)"
---

<objective>
Build the shared 054-B lifecycle stepper — the ONE status-truth component consumed by both the Studio header (condensed) and the slimmed detail panel (full). It renders the server `PublishGate` verbatim and resolves the 1/1-vs-0/2 "flat contradiction" (the literal 136-UAT complaint) as two labeled, version-bound facts.

Purpose: Establish the single truth-teller for publish status before any surface consumes it (D-03/D-10). This is the trickiest net-new chrome and the phase's core honesty lock, so it is built first and in isolation.
Output: `LifecycleStepper.tsx` (+ spec) under `frontend/src/components/skills/studio/`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-RESEARCH.md
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-PATTERNS.md
@.planning/sketches/054-one-truth-status/README.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/skills/SkillEvalSection.tsx

<interfaces>
<!-- The stepper's ONLY status source. Render these fields; never derive `met`. -->
From frontend/src/types/index.ts (PublishGate, ~:654-662):
```typescript
interface PublishGate {
  met: boolean
  state: "never_evaled" | "latest_failed" | "passed_on_older_version" | "passed"
  measured: number | null
  passed: number | null
  passing_run_id: string | null
  reason: string
  last_override: { gate_state: string; created_at: string } | null
}
```
Data analog to lift (do NOT re-skin its chrome — that is net-new sketch 054-B):
- SkillEvalSection.tsx gate render :539-567 (the one-truth server render)
- SkillEvalSection.tsx unmetGateLine :118-128 (state -> human copy map)
Case count for the "Cases" stage node comes from the test-case count passed in as a prop (caseCount), sourced by consumers from listTestCases length.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared LifecycleStepper component</name>
  <files>frontend/src/components/skills/studio/LifecycleStepper.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.tsx (gate render :539-567 + unmetGateLine :118-128 — the data/state->copy source of truth to lift, NOT the chrome)
    - .planning/sketches/054-one-truth-status/README.md (stepper winner B, the four gate states, the ⚡ collision acceptance test, honesty locks — the visual contract)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (Aether Deep Midnight tokens; sketch->React is a straight token port)
    - frontend/src/types/index.ts (PublishGate shape)
  </read_first>
  <behavior>
    - Renders 4 stage nodes: Cases -> Eval -> Gate -> Published, each carrying its count/state on the node.
    - state="passed" + met=true -> Gate node met, "Publish ready — eval passed {passed}/{measured} on the current version".
    - state="never_evaled" -> Eval node empty, current-stage message points to running an eval.
    - state="latest_failed" -> Eval node shows the newest run's honest count, Gate not met.
    - state="passed_on_older_version" (the ⚡ collision) -> Gate node MET (bound to passing_run_id) AND Eval node carries the newest run's failing count, EACH labeled with its run/version binding; the current-stage message reads "the passing eval is stale — it measured vN-1, the live skill is vN". Two labeled facts, never one flat contradiction.
    - last_override != null -> ALWAYS render an amber "Published without a passing eval on {date}" receipt, un-softened.
    - Only the CURRENT stage narrates itself in one message box; other nodes stay quiet.
    - variant="strip" -> a compact one-line condensation for the Studio header; variant="full" -> the full vertical stepper for the slim panel. Same PublishGate in, no divergent truth.
    - NEVER computes met/readiness from passed/measured — reads publishGate.met and publishGate.state only.
  </behavior>
  <action>
    Create `LifecycleStepper` exporting a component with props `{ publishGate: PublishGate | null, caseCount: number, skillVersion: number, variant?: "full" | "strip", proposalState?: string | null, onNavigateStage?: (stage: "cases" | "eval" | "gate") => void }`. Render the four stage nodes (Cases/Eval/Gate/Published) using Aether Deep Midnight tokens ported from sketch 054-B. Drive all publish status off `publishGate.met` and `publishGate.state` — port the state->copy mapping from SkillEvalSection.unmetGateLine (:118-128) and the met render (:543-567); do NOT thread `passed >= measured` logic anywhere. For `passed_on_older_version`, render Gate as MET labeled with `passing_run_id` binding AND Eval with the newest run's honest count, plus the stale-eval message naming vN-1 vs the live `skillVersion`. Always render the `last_override` amber receipt when present. The `variant="strip"` path collapses to a single header line (state word + counts) that is a pure condensation of the same fields. Compose `proposalState` into the current-stage read only as a label (never a second truth-teller). Wire `onNavigateStage` on the Cases/Eval/Gate nodes for deep-link scroll (consumers pass a handler). Do NOT use dangerouslySetInnerHTML; render all copy as text.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/LifecycleStepper.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `LifecycleStepper.tsx` exports a component accepting `publishGate`, `caseCount`, `skillVersion`, and `variant`.
    - Source contains no arithmetic gate derivation: grep for a client re-compute of readiness returns nothing — `grep -nE 'passed.*(>=|>|<).*measured|measured.*(>=|<).*passed' frontend/src/components/skills/studio/LifecycleStepper.tsx` prints no line that computes `met`.
    - Source references `publishGate.met` and `publishGate.state` (renders server truth): `grep -c 'publishGate\.\(met\|state\)' frontend/src/components/skills/studio/LifecycleStepper.tsx` is >= 2.
    - Source renders `last_override` (grep matches `last_override`).
    - No `dangerouslySetInnerHTML` in the file.
  </acceptance_criteria>
  <done>The stepper renders all four PublishGate states + the ⚡ collision as two labeled facts, renders the override receipt, supports full/strip variants, and derives nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Author the LifecycleStepper spec (fresh, fully mocked)</name>
  <files>frontend/src/components/skills/studio/LifecycleStepper.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.test.tsx (:1-55 — the fresh-authored, fully-mocked pattern; header note "not leaning on a rotted sibling")
    - frontend/src/components/skills/studio/LifecycleStepper.tsx (the component under test, from Task 1)
  </read_first>
  <action>
    Author `LifecycleStepper.test.tsx` fresh per the SEED-056 rot-avoidance pattern (do NOT import helpers from rotted siblings; this component has no `@/lib/api` dependency so no api mock is needed). Add render assertions for: (1) state="passed"+met -> "Publish ready" copy present; (2) state="never_evaled" -> no fabricated pass, Eval node empty-state copy; (3) state="passed_on_older_version" -> BOTH a met-Gate label AND the newest run's failing count are present AND the stale-eval "measured vN-1" message is present (the ⚡ collision as two labeled facts); (4) last_override present -> the amber override receipt copy renders; (5) variant="strip" -> renders a single condensed line from the same PublishGate. Assert the component reads server truth: pass a PublishGate with met=false but passed>measured-favorable numbers and confirm it still renders the not-met branch (proves no client recompute).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/LifecycleStepper.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/skills/studio/LifecycleStepper.test.tsx` passes with >= 5 test cases (4 states + strip variant + no-recompute).
    - The spec has no import from a known-rotted sibling spec (SEED-056).
    - The ⚡ collision test asserts BOTH a met-Gate and a failing Eval count are in the DOM.
  </acceptance_criteria>
  <done>Spec is green, covers all four PublishGate states + the collision + both variants + the no-recompute proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server PublishGate -> client render | The server (`publish_gate_service`) owns readiness; the client is a pure renderer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Elevation of Privilege | LifecycleStepper (status surface) | mitigate | The stepper renders `publishGate.met`/`state` verbatim and never recomputes readiness from `passed`/`measured` (D-03/054 one-truth lock); acceptance criteria grep-gate the absence of any client-side gate arithmetic. A stepper that fabricated "ready" would imply a skill is publishable the server would block. |
| T-137-05 | Tampering / Injection (XSS) | LifecycleStepper copy render | mitigate | All stage copy + the override receipt + the reason string render as React text nodes; no `dangerouslySetInnerHTML`. |
| T-137-SC | Tampering (supply chain) | npm install surface | accept | This plan installs zero packages; slopcheck/registry verification N/A. RESEARCH Package Legitimacy Audit = N/A. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/skills/studio/LifecycleStepper.test.tsx` green.
- Manual grep gates in acceptance criteria confirm no client-side `met` recompute and no `dangerouslySetInnerHTML`.
</verification>

<success_criteria>
The shared lifecycle stepper renders all four PublishGate states + the ⚡ collision honestly, supports both the header-strip and full-panel variants from one component, and derives nothing client-side. Spec green.
</success_criteria>

<output>
Create `.planning/phases/137-skill-evals-panel-ui-panel-01/137-01-SUMMARY.md` when done.
</output>
