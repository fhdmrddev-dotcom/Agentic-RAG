---
phase: 137-skill-evals-panel-ui-panel-01
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/skills/studio/VersionsTab.tsx
  - frontend/src/components/skills/studio/VersionsTab.test.tsx
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "The Versions tab is a scan-first 056-B table: version · origin chip · eval-on-this-version binding · date, with the live version badged (D-05)"
    - "An any-to-any Compare v[x] ↔ v[y] picker renders ONE unified diff via the existing lineDiff util (D-05)"
    - "Provenance chips map the FIVE real SkillVersion.source values (manual/import/tuner/self_improve/backfill) — there is NO 'forced' source value (D-05, RESEARCH Pitfall 1)"
    - "A force-promoted version's un-softened failed PromotionGate evidence is shown by joining the version to its proposal (SkillProposal.override_forced on proposal.new_skill_version_id === version.id) (D-05)"
    - "The eval-on-this-version binding is a client-side join of listSkillVersions × listEvalRuns grouped by EvalRun.skill_version_id — zero backend change, no migration (D-15, RESEARCH Pitfall 2)"
    - "A 'Restore' action is deliberately ABSENT (versions immutable, VER-01) (D-05)"
  artifacts:
    - path: "frontend/src/components/skills/studio/VersionsTab.tsx"
      provides: "056-B version table + any-to-any compare picker + client-side joins + provenance chips"
      min_lines: 120
    - path: "frontend/src/components/skills/studio/VersionsTab.test.tsx"
      provides: "Unit coverage of table render, provenance mapping, compare diff, no-Restore, joins"
      min_lines: 70
  key_links:
    - from: "VersionsTab.tsx"
      to: "listSkillVersions + listEvalRuns + listProposals"
      via: "client-side joins on skill_version_id and new_skill_version_id"
      pattern: "listSkillVersions|listEvalRuns|listProposals"
    - from: "VersionsTab.tsx"
      to: "lineDiff util"
      via: "compare picker unified diff render"
      pattern: "lineDiff"
---

<objective>
Build the Versions tab (056-B): a scan-first version table with provenance chips + the eval-on-this-version binding, plus an any-to-any compare picker rendering one unified diff. The two "bindings" (version→eval rollup, version→forced-proposal evidence) are client-side joins over already-shipped endpoints — zero backend change, no migration.

Purpose: Deliver the VER-01 diff-viewable version history requirement using the real `SkillVersion.source` enum (which has NO "forced" value — RESEARCH Pitfall 1) and the real binding source (EvalRun.skill_version_id, NOT the versions endpoint — RESEARCH Pitfall 2).
Output: `VersionsTab.tsx` (+ spec).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-RESEARCH.md
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-PATTERNS.md
@.planning/sketches/056-version-history-and-diff/README.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/skills/SkillTestCasesSection.tsx
@frontend/src/lib/lineDiff.ts

<interfaces>
<!-- The REAL SkillVersion.source enum (provenance chip source of truth). NO "forced" value. -->
"manual" -> hand-edited · "import" -> imported · "tuner" -> tuner-promoted
"self_improve" -> proposal-promoted · "backfill" -> original/migrated

Client fns (all already in frontend/src/lib/api.ts):
- listSkillVersions(skillId): Promise<SkillVersion[]>   // :1670 — id, version_number, name, description, instructions, source, created_at. NO eval binding.
- listEvalRuns(skillId): Promise<EvalRun[]>              // :1726 — each carries skill_version_id, passed_count, measured_count, case_count, provider, model, status, created_at.
- listProposals(skillId): Promise<SkillProposal[]>       // rows carry new_skill_version_id, override_forced, gate (PromotionGate).
- lineDiff(base, next): DiffRow[]                        // frontend/src/lib/lineDiff.ts — { type: "add"|"remove"|"context", text }
Diff render analog (JSX): SkillEvalSection.tsx :687-706 (add=emerald, remove=destructive, context=foreground/70).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version data joins + provenance chip mapping</name>
  <files>frontend/src/components/skills/studio/VersionsTab.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillTestCasesSection.tsx (version list source :48, :200-214 — the listSkillVersions call + row shape)
    - frontend/src/lib/api.ts (listSkillVersions :1670, listEvalRuns :1726, listProposals — confirm exact signatures)
    - .planning/phases/137-skill-evals-panel-ui-panel-01/137-RESEARCH.md (Pitfall 1 provenance enum + Pitfall 2 version→eval join — the two joins are the load-bearing correctness details)
    - frontend/src/types/index.ts (SkillVersion, EvalRun, SkillProposal, PromotionGate)
  </read_first>
  <action>
    In `VersionsTab` (props `{ skillId: string, liveVersionNumber: number }`), fetch `listSkillVersions(skillId)`, `listEvalRuns(skillId)`, and `listProposals(skillId)` in parallel on mount. Build two client-side joins: (a) version→eval rollup — group eval runs by `EvalRun.skill_version_id`, and for each version surface its latest run's `passed_count`/`measured_count` rollup or a "never evaled" marker; (b) version→forced-proposal — match `proposal.new_skill_version_id === version.id` and, where `override_forced` is true, carry the proposal's failed `PromotionGate` for un-softened evidence. Write a `provenanceChip(source)` mapper covering EXACTLY the five real values manual/import/tuner/self_improve/backfill (hand-edited / imported / tuner-promoted / proposal-promoted / original) — do NOT add a `"forced"` branch (dead code). Carry a skill-switch guard: capture the requested skillId and bail before setState if `skillId` changed mid-fetch. Do not add any new api.ts function — both source endpoints already exist.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/VersionsTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source calls all three existing clients: `grep -c 'listSkillVersions\|listEvalRuns\|listProposals' frontend/src/components/skills/studio/VersionsTab.tsx` >= 3.
    - The provenance mapper covers the five real values and NOT "forced": `grep -c '\"forced\"' frontend/src/components/skills/studio/VersionsTab.tsx` == 0, and `grep -c 'self_improve' frontend/src/components/skills/studio/VersionsTab.tsx` >= 1.
    - The version→eval join keys on skill_version_id: grep matches `skill_version_id`.
    - No new function added to frontend/src/lib/api.ts (this plan's files_modified excludes api.ts).
  </acceptance_criteria>
  <done>VersionsTab hydrates versions + the two client-side joins with correct provenance mapping and no dead "forced" branch.</done>
</task>

<task type="auto">
  <name>Task 2: Scan-first table + any-to-any compare picker + diff render</name>
  <files>frontend/src/components/skills/studio/VersionsTab.tsx</files>
  <read_first>
    - .planning/sketches/056-version-history-and-diff/README.md (table+compare winner B + provenance chips + the no-Restore lock — the visual contract)
    - frontend/src/components/skills/SkillEvalSection.tsx (diff render :687-706 — the add/remove/context row JSX to reuse)
    - frontend/src/lib/lineDiff.ts (lineDiff signature + DiffRow shape)
  </read_first>
  <action>
    Render the 056-B scan-first table: one row per version = version_number · provenance chip · eval-on-this-version binding (rollup or "never evaled") · created_at date, with the row whose `version_number === liveVersionNumber` carrying a LIVE badge. For force-promoted versions, render the joined failed PromotionGate evidence un-softened inline. Add an any-to-any Compare picker: two selectors choosing v[x] and v[y] from the version list, rendering ONE unified diff via `lineDiff(vx.instructions, vy.instructions)` using the add=emerald / remove=destructive / context=foreground/70 row JSX (the SkillEvalSection :687-706 pattern). Do NOT add any "Restore" / "revert" action (versions are immutable, VER-01). Use Aether Deep Midnight tokens ported from sketch 056-B. Render diff/instruction text as React text nodes only (no dangerouslySetInnerHTML).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/VersionsTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Table renders one row per version with a provenance chip + date; the live version row carries a LIVE badge.
    - Compare picker renders exactly one unified `lineDiff` output for the selected v[x]/v[y]: grep matches `lineDiff`.
    - NO restore affordance: `grep -ci 'restore\|revert' frontend/src/components/skills/studio/VersionsTab.tsx` == 0.
    - No `dangerouslySetInnerHTML` in the file.
  </acceptance_criteria>
  <done>The Versions tab shows the scan-first table + LIVE badge + provenance chips + eval bindings + an any-to-any compare diff, with no Restore action.</done>
</task>

<task type="auto">
  <name>Task 3: Author the VersionsTab spec (fresh, fully mocked)</name>
  <files>frontend/src/components/skills/studio/VersionsTab.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.test.tsx (:1-55 — the fully-mocked `@/lib/api` fresh pattern)
    - frontend/src/components/skills/studio/VersionsTab.tsx (component under test)
  </read_first>
  <action>
    Author `VersionsTab.test.tsx` fresh, fully mocking `@/lib/api` (`listSkillVersions`, `listEvalRuns`, `listProposals`) and not importing from rotted siblings (SEED-056). Cover: (1) table renders a row per version with the correct provenance chip word for each of the five source values; (2) the live version row shows a LIVE badge; (3) a version with a matching eval run shows its passed/measured rollup; a version with no run shows "never evaled"; (4) a version whose proposal has override_forced=true shows the un-softened failed-gate evidence; (5) selecting two versions in the compare picker renders a single unified diff (assert add + remove rows present); (6) NO "Restore"/"revert" control exists in the DOM.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/VersionsTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/skills/studio/VersionsTab.test.tsx` passes with >= 5 test cases including the provenance-mapping case and the no-Restore assertion.
    - `@/lib/api` is mocked; no import from a rotted sibling spec.
  </acceptance_criteria>
  <done>Spec green; provenance mapping, joins, compare diff, and the no-Restore lock are all asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skillId param -> owner-scoped reads | listSkillVersions/listEvalRuns/listProposals are owner-scoped server-side (404-never-403). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-03 | Information Disclosure (IDOR) | version/eval/proposal reads | mitigate | The tab passes only `skillId` (owner-scoped path); it never constructs cross-user ids and renders 404/empty server responses verbatim. The client join reads only rows the owner-scoped endpoints already returned — it cannot widen scope. |
| T-137-02 | Information Disclosure | cross-skill stale state | mitigate | A skill-switch guard captures the requested skillId and bails before setState if the active skill changed mid-fetch (BUG-260701-02 pattern). |
| T-137-05 | Tampering / Injection (XSS) | diff + instruction body render | mitigate | Version instructions and lineDiff rows render as React text nodes only; no `dangerouslySetInnerHTML`. |
| T-137-SC | Tampering (supply chain) | npm install surface | accept | Zero new packages; slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/skills/studio/VersionsTab.test.tsx` green.
- Grep gates confirm no "forced" source branch, no Restore action, no dangerouslySetInnerHTML.
</verification>

<success_criteria>
The Versions tab satisfies VER-01: scan-first table + provenance chips (5 real values) + eval-on-this-version binding (client join) + any-to-any compare diff, with immutability preserved (no Restore) and no backend change.
</success_criteria>

<output>
Create `.planning/phases/137-skill-evals-panel-ui-panel-01/137-02-SUMMARY.md` when done.
</output>
