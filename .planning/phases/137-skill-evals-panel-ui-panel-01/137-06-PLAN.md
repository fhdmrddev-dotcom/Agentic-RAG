---
phase: 137-skill-evals-panel-ui-panel-01
plan: 06
type: execute
wave: 3
depends_on: [137-01, 137-02, 137-05]
files_modified:
  - frontend/src/pages/SkillStudioPage.tsx
  - frontend/src/pages/SkillStudioPage.test.tsx
  - frontend/src/lib/skillVersion.ts
  - frontend/src/components/skills/studio/TriggeringTab.tsx
  - frontend/src/pages/SkillTunerPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/pages/SkillsPage.tsx
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "A net-new skill-studio ActiveView value with a tab param renders ONE focused full-surface Studio; the legacy skill-tuner value REDIRECTS to skill-studio?tab=triggering (no orphan Tuner surface) (D-01)"
    - "'‹ Skills' returns preserving the selected skill + panel state; the landing tab is Evals (D-01)"
    - "A persistent header on every tab shows skill name + vN + LIVE badge + a compact one-line gate strip (the LifecycleStepper strip variant — a condensation of the SAME server PublishGate, never a second truth-teller) (D-01, D-03)"
    - "The live version number ('vN') is derived ONCE at the shell via the shared `deriveLiveVersion(skill, versions)` helper (frontend/src/lib/skillVersion.ts — content-equality against the live skill; fallback: max version_number; none: 1) and threaded as a prop to the header, the strip stepper, EvalsTab, and VersionsTab — no consumer re-derives it; the SAME helper is consumed by Plan 07's panel (one rule, one implementation) (D-03, D-05)"
    - "The header gate strip's caseCount is sourced from a shell-level listTestCases(skillId) call (caseCount = cases.length) — the SAME server list EvalsTab reads, never hardcoded — so the persistent-header strip and the Evals tab body show the same case count (D-03)"
    - "Evals · Triggering · Versions tabs switch and are deep-linkable via the tab param (D-01, D-06)"
    - "The Triggering tab mounts the shipped SkillTunerPage with a SINGLE additive `embedded` header-suppression prop; all tuner internals/behavior stay untouched (041/042/043/045 winners intact) — the one-line render guard does not violate D-02 (which locks tuner behavior/internals, not a render flag) (D-02)"
    - "The onOpenStudio/onReviewEvals navigators are defined in App and threaded through ChatLayout to the Skills surface (the actual 'Open studio' button lives in the slim detail panel — Plan 07 — so there is exactly ONE 'Open studio' control); the lint/'Tune triggers' handoff auto-re-points to Studio·Triggering via the repointed handleTuneSkill (D-06)"
  artifacts:
    - path: "frontend/src/pages/SkillStudioPage.tsx"
      provides: "Focused full-surface Studio: persistent header + gate strip (shell-sourced caseCount from listTestCases) + Evals/Triggering/Versions tabs + shell-level live-version derivation via the shared helper"
      min_lines: 90
    - path: "frontend/src/lib/skillVersion.ts"
      provides: "Shared pure `deriveLiveVersion(skill, versions)` content-equality helper (one rule, one implementation — consumed by the Studio shell here AND the Plan 07 panel)"
      min_lines: 6
    - path: "frontend/src/components/skills/studio/TriggeringTab.tsx"
      provides: "Thin wrapper mounting SkillTunerPage with the embedded header-suppression prop"
      min_lines: 15
  key_links:
    - from: "frontend/src/App.tsx"
      to: "ActiveView union + handleOpenStudio + legacy skill-tuner redirect"
      via: "setActiveView('skill-studio') + studioSkillId/studioTab state"
      pattern: "skill-studio"
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "SkillStudioPage mount branch"
      via: "activeView === 'skill-studio'"
      pattern: "SkillStudioPage"
    - from: "SkillStudioPage.tsx"
      to: "EvalsTab / TriggeringTab / VersionsTab"
      via: "tab-driven body render + threaded liveVersionNumber"
      pattern: "EvalsTab|TriggeringTab|VersionsTab"
    - from: "SkillStudioPage.tsx"
      to: "listTestCases -> caseCount (LifecycleStepper strip)"
      via: "shell-level fetch; caseCount = cases.length feeds the strip's REQUIRED caseCount prop (Plan 01 contract), never hardcoded"
      pattern: "listTestCases"
    - from: "SkillStudioPage.tsx"
      to: "deriveLiveVersion (shared helper)"
      via: "listSkillVersions fetch + deriveLiveVersion(skill, versions)"
      pattern: "deriveLiveVersion"
---

<objective>
Make the Studio reachable and assemble the shell: the `SkillStudioPage` (persistent header with a condensed gate strip + the three tabs, Evals landing, Triggering = the tuner mounted with a one-line `embedded` header guard, Versions), the `skill-studio` `ActiveView` (mirroring the shipped `skill-tuner` precedent, no router), the legacy `skill-tuner` redirect, and the navigator threading. The shell is built FIRST (Task 1) so the wiring task (Task 2) can reference an existing `SkillStudioPage` and typecheck. Task 1 also creates the shared `deriveLiveVersion` helper (the single live-version rule) that Plan 07's panel imports.

Purpose: Deliver the D-01 shell + D-02 tuner absorption + the reachability triad (App union + ChatLayout mount + navigator threading) owned in-phase, so the Studio is not a built-but-unreachable surface. The single "Open studio" button lives in the slim detail panel (Plan 07); this plan only defines + threads the navigator (no duplicate button).
Output: `SkillStudioPage.tsx` + `TriggeringTab.tsx` + `skillVersion.ts` (new) + a one-line `embedded` prop on `SkillTunerPage.tsx` + `App.tsx` / `ChatLayout.tsx` / `SkillsPage.tsx` wiring (+ shell spec).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-RESEARCH.md
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-PATTERNS.md
@.planning/sketches/053-eval-studio-shell/README.md
@.planning/sketches/057-skill-studio-linkage/README.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/App.tsx
@frontend/src/components/layout/ChatLayout.tsx
@frontend/src/pages/SkillTunerPage.tsx
@frontend/src/pages/SkillsPage.tsx
@frontend/src/components/skills/studio/LifecycleStepper.tsx
@frontend/src/components/skills/studio/EvalsTab.tsx
@frontend/src/components/skills/studio/VersionsTab.tsx

<interfaces>
<!-- The shipped precedent this shell clones (VERIFIED). -->
App.tsx :9 ActiveView union (…| "skill-tuner"); :21-25 tunerSkillId + handleTuneSkill(skillId){ setTunerSkillId; setActiveView("skill-tuner") }; threaded to <ChatLayout> :42-50.
ChatLayout.tsx :42/:46 Props (tunerSkillId, onTuneSkill); :316-324 mount branch: activeView === "skill-tuner" ? <SkillTunerPage skillId={tunerSkillId} onBack={() => onNavigate("skills")} /> : <KnowledgeHealthPage/>.
SkillTunerPage.tsx :51-57 Props { skillId, onBack }; :87-94 useSkills + skill lookup guard; :537-556 focused-surface header chrome (the block the `embedded` prop suppresses).
SkillsPage.tsx :22 SkillsPage({ onTryInChat, onTuneSkill }); :187-193 the "Tune triggers" entry-action precedent; :202-206 SkillDetailPanel mount.
Studio tab type: "evals" | "triggering" | "versions" (landing = "evals").
Components to compose: EvalsTab (Plan 05), VersionsTab (Plan 02), LifecycleStepper strip variant (Plan 01).
LifecycleStepper (Plan 01) REQUIRES `caseCount: number` — per Plan 01 it is "sourced by consumers from listTestCases length"; this shell is the consumer that sources it.

<!-- The SINGLE live-version derivation rule, now a SHARED helper (W1 — one rule, one implementation, two homes: this shell + Plan 07's panel). -->
Neither `Skill` nor `PublishGate` carries a version number; the only source is `SkillVersion.version_number` via `listSkillVersions`, and there is no `is_current` flag. This plan CREATES the shared pure helper:
  `deriveLiveVersion(skill: Skill, versions: SkillVersion[]): number` in `frontend/src/lib/skillVersion.ts` =
      the `version_number` of the SkillVersion whose `instructions` === `skill.instructions`
      (content-equality — mirrors the backend publish-gate D-04 binding, publish_gate_service.py:145-147);
      if none matches, the MAX `version_number`; if there are no versions, 1.
The shell fetches `listSkillVersions(skillId)` once (alongside `getPublishGate`), then calls `deriveLiveVersion(skill, versions)` once and threads the number down. Plan 07's panel imports the SAME helper (no re-derived inline copy).

<!-- B1 fix — the header gate strip's caseCount source (Plan 01: caseCount is sourced by consumers from listTestCases length). -->
The `<LifecycleStepper variant="strip" … caseCount={caseCount} …>` REQUIRES a `caseCount` (Plan 01 contract). Fetch `listTestCases(skillId)` once at the shell alongside getPublishGate/listSkillVersions; `caseCount = cases.length`. This is the SAME server `listTestCases` source EvalsTab reads for its own case count — the two counts share one source of truth and cannot STRUCTURALLY diverge; the strip is never a hardcoded `0`. The shell's copy refreshes on skill switch (and on any shell re-fetch). A transient drift is possible only if the operator adds/removes a case inside the Evals tab before the shell re-reads — acceptable, and fully closable if EvalsTab bubbles an `onCasesChanged` that re-triggers the shell fetch (optional, executor discretion). State the chosen behavior explicitly in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SkillStudioPage + TriggeringTab + deriveLiveVersion — shell, header, gate strip, tabs, shared live-version helper</name>
  <files>frontend/src/pages/SkillStudioPage.tsx, frontend/src/lib/skillVersion.ts, frontend/src/components/skills/studio/TriggeringTab.tsx, frontend/src/pages/SkillTunerPage.tsx, frontend/src/pages/SkillStudioPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/SkillTunerPage.tsx (:51-57 Props + :87-94 guard + :534-556 header chrome — the focused-surface precedent to adapt AND the header block to gate on `embedded`)
    - .planning/sketches/053-eval-studio-shell/README.md (shell winner A + Build Handover) and .planning/sketches/057-skill-studio-linkage/README.md (the persistent header + 3 tabs + landing=Evals + deep-linkable tabs contract; the trigPane() mockup showing NO duplicate back-header inside the Triggering tab)
    - frontend/src/components/skills/studio/LifecycleStepper.tsx (strip variant for the header gate strip — note its REQUIRED caseCount prop), EvalsTab.tsx, VersionsTab.tsx (the tab bodies to compose)
    - frontend/src/hooks/useSkills.ts (skill identity for the header)
    - frontend/src/lib/api.ts (getPublishGate, listSkillVersions, listTestCases — the shell's gate + version-number + case-count sources)
  </read_first>
  <behavior>
    - Renders a focused full-surface (no router) with a persistent header on every tab: "‹ Skills" back, skill name + vN + LIVE badge + a one-line gate strip (LifecycleStepper variant="strip").
    - The gate strip condenses the SAME getPublishGate the EvalsTab stepper renders — never a second truth-teller.
    - The "vN" is the shell-derived live version number (via the shared deriveLiveVersion helper), threaded to the strip, EvalsTab, and VersionsTab so all four surfaces agree.
    - The gate strip's caseCount is the shell's listTestCases(skillId) length — the same case count the Evals tab shows, never a hardcoded value.
    - Tab chrome Evals · Triggering · Versions, driven by the `tab` prop; landing = Evals; switching a tab calls onTabChange (deep-linkable).
    - Evals -> <EvalsTab>; Triggering -> <TriggeringTab> (mounts SkillTunerPage with `embedded` so its own back-header is suppressed — no doubled header); Versions -> <VersionsTab>.
    - skillId null -> a calm centered guard with a "‹ Skills" back button (the SkillTunerPage guard pattern).
    - "‹ Skills" calls onBack (returns to the Skills page preserving selection).
  </behavior>
  <action>
    (a) SkillTunerPage.tsx — add ONE additive optional prop `embedded?: boolean` to its Props (:51-57) and wrap the focused-surface header block (:537-556, the "‹ Skills" back button + "Trigger Tuner · {name}" title + subtitle) in `{!embedded && (…)}`. Change NOTHING else — no behavior, no internals, no `components/skills/tuner/*`. This is the checker-sanctioned resolution of the D-01-vs-D-02 doubled-header conflict: a one-line render guard is NOT a behavior change, so it honors D-02's intent (which locks tuner behavior/internals). When `embedded` is unset the standalone page renders exactly as today.

    (b) TriggeringTab.tsx — create `TriggeringTab` (props `{ skillId, onBack }`) as a thin wrapper mounting `<SkillTunerPage skillId={skillId} onBack={onBack} embedded />`. Do NOT import or edit SkillTunerPage internals or `components/skills/tuner/*`; the header suppression is entirely the `embedded` prop.

    (c) frontend/src/lib/skillVersion.ts + SkillStudioPage.tsx — FIRST create the shared pure helper `deriveLiveVersion(skill: Skill, versions: SkillVersion[]): number` in `frontend/src/lib/skillVersion.ts` implementing the content-equality rule in <interfaces> (instructions match → that version_number; else MAX version_number; no versions → 1). No fetch, no React — a pure, unit-testable function imported by BOTH this shell and Plan 07's panel (W1: one rule, one implementation). THEN create `SkillStudioPage` (props `{ skillId: string | null, tab: "evals"|"triggering"|"versions", onTabChange: (t) => void, onBack: () => void }`): look up the skill via useSkills (calm centered guard + "‹ Skills" when skillId is null). Fetch `getPublishGate(skillId)`, `listSkillVersions(skillId)` AND `listTestCases(skillId)` once at the shell (a single skill-switch-guarded effect); derive `liveVersionNumber = deriveLiveVersion(skill, versions)` (the shared helper) and `caseCount = cases.length` (the strip's required count — never hardcoded). Render the persistent header (‹ Skills, skill name · v{liveVersionNumber} · LIVE badge · `<LifecycleStepper variant="strip" publishGate={gate} caseCount={caseCount} skillVersion={liveVersionNumber} />`). Render the tab bar (Evals/Triggering/Versions) driven by `tab` with `onTabChange`. Render the active tab body — `<EvalsTab skillId skillVersion={liveVersionNumber} />` / `<TriggeringTab skillId onBack={onBack} />` / `<VersionsTab skillId liveVersionNumber={liveVersionNumber} />`. Carry a skill-switch guard on the shell's gate/version/cases fetch.

    (d) SkillStudioPage.test.tsx — author co-located spec fresh (mock `@/lib/api` incl. getPublishGate + listSkillVersions + listTestCases; stub EvalsTab/VersionsTab/SkillTunerPage as lightweight components): landing renders Evals; clicking Triggering calls onTabChange and renders the tuner stub; clicking Versions renders the versions stub; a null skillId renders the calm guard + back; "‹ Skills" calls onBack; the header shows the skill name + the derived "vN"; the strip receives the listTestCases-derived caseCount (assert the mocked listTestCases return length flows to the header strip — not a hardcoded 0).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/SkillStudioPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SkillTunerPage gains the additive prop and gates its header: `grep -c 'embedded' frontend/src/pages/SkillTunerPage.tsx` >= 2 (the prop + the `{!embedded &&` guard); no other tuner behavior changed.
    - SkillStudioPage composes the three tabs: `grep -c 'EvalsTab\|TriggeringTab\|VersionsTab' frontend/src/pages/SkillStudioPage.tsx` >= 3.
    - The header uses the strip variant: grep matches `variant="strip"` (or `variant='strip'`).
    - The shell fetches versions from listSkillVersions: `grep -c 'listSkillVersions' frontend/src/pages/SkillStudioPage.tsx` >= 1.
    - The live-version rule is the SHARED helper (W1 — not an inline copy): `frontend/src/lib/skillVersion.ts` exports `deriveLiveVersion` (`grep -c 'export.*deriveLiveVersion' frontend/src/lib/skillVersion.ts` >= 1) AND the shell calls it (`grep -c 'deriveLiveVersion' frontend/src/pages/SkillStudioPage.tsx` >= 1).
    - The shell SOURCES the strip's case count from listTestCases (Plan 01 contract) and never hardcodes it: `grep -c 'listTestCases' frontend/src/pages/SkillStudioPage.tsx` >= 1 AND `grep -c 'caseCount={0}' frontend/src/pages/SkillStudioPage.tsx` == 0.
    - TriggeringTab mounts SkillTunerPage with `embedded` and does NOT import from components/skills/tuner: `grep -c 'SkillTunerPage' frontend/src/components/skills/studio/TriggeringTab.tsx` >= 1, `grep -c 'embedded' frontend/src/components/skills/studio/TriggeringTab.tsx` >= 1, and `grep -c 'skills/tuner' frontend/src/components/skills/studio/TriggeringTab.tsx` == 0.
    - Spec asserts: landing = Evals; tab switch calls onTabChange; null skillId -> guard + back; back calls onBack; header shows the derived vN AND the listTestCases-derived caseCount.
  </acceptance_criteria>
  <done>The Studio shell renders the persistent header + condensed gate strip (caseCount sourced from listTestCases, vN via the shared deriveLiveVersion helper) + three tabs (landing Evals); the tuner is absorbed with a one-line `embedded` header guard (no doubled header, internals untouched); spec green.</done>
</task>

<task type="auto">
  <name>Task 2: App + ChatLayout + SkillsPage — the ActiveView shell, state, navigators, legacy redirect (no button)</name>
  <files>frontend/src/App.tsx, frontend/src/components/layout/ChatLayout.tsx, frontend/src/pages/SkillsPage.tsx</files>
  <read_first>
    - frontend/src/App.tsx (:9 ActiveView union; :21-25 tunerSkillId + handleTuneSkill; :42-50 the ChatLayout prop threading — the exact shape to clone)
    - frontend/src/components/layout/ChatLayout.tsx (:42/:46 Props; :316-324 the skill-tuner mount branch — add the analogous skill-studio branch BEFORE the trailing else)
    - frontend/src/pages/SkillsPage.tsx (:19-22 Props + signature; the SkillsPage mount inside ChatLayout — where onOpenStudio/onReviewEvals are passed through)
    - frontend/src/pages/SkillStudioPage.tsx (from Task 1 — the component ChatLayout mounts)
    - .planning/phases/137-skill-evals-panel-ui-panel-01/137-CONTEXT.md (D-01 shell + legacy redirect; D-06 lint re-point)
  </read_first>
  <action>
    In App.tsx: add `"skill-studio"` to the `ActiveView` union (:9); add `studioSkillId: string | null` + `studioTab: "evals" | "triggering" | "versions"` state; add `handleOpenStudio(skillId, tab = "evals")` (sets both then `setActiveView("skill-studio")`), `handleReviewEvals(skillId)` (= `handleOpenStudio(skillId, "evals")`), and `handleStudioTabChange(tab)` (sets studioTab). Implement the LEGACY REDIRECT (D-01): repoint the existing `handleTuneSkill(skillId)` to `handleOpenStudio(skillId, "triggering")` so the shipped "Tune triggers" / lint "Tune this →" callers land in Studio·Triggering with no orphan Tuner surface (this is the D-06 lint re-point, achieved by repointing the one navigator). Thread `studioSkillId`, `studioTab`, `onOpenStudio={handleOpenStudio}`, `onReviewEvals={handleReviewEvals}`, `onStudioTabChange={handleStudioTabChange}` down to `<ChatLayout>` exactly as `tunerSkillId`/`onTuneSkill` are threaded (:42-50).

    In ChatLayout.tsx: extend the Props interface (:42) + destructure (:46) for the new props; add an `activeView === "skill-studio"` mount branch BEFORE the trailing `KnowledgeHealthPage` else (mirror :316-324) rendering `<SkillStudioPage skillId={studioSkillId} tab={studioTab} onTabChange={onStudioTabChange} onBack={() => onNavigate("skills")} />`; pass `onOpenStudio` + `onReviewEvals` through to `<SkillsPage>` (both are USED here, so no unused-local error).

    In SkillsPage.tsx: add `onOpenStudio?: (skillId: string, tab?: "evals"|"triggering"|"versions") => void` and `onReviewEvals?: (skillId: string) => void` to the Props INTERFACE ONLY so ChatLayout's pass-through typechecks. Do NOT destructure or consume them here and do NOT add any "Open studio" button — Plan 07 owns the button (in the slim detail panel, the sole "Open studio" control per the 057 MAP) and destructures + drills these navigators to their consumers. Leaving the two props undestructured avoids a `noUnusedLocals`/`noUnusedParameters` error in this plan while keeping the interface ready for Plan 07. Keep the existing "Tune triggers" button unchanged (its `onTuneSkill` now auto-lands in Studio·Triggering via the App redirect).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/SkillStudioPage.test.tsx && npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'skill-studio' frontend/src/App.tsx` >= 2 (union + setActiveView).
    - `handleTuneSkill` no longer sets `"skill-tuner"` directly: `grep -c 'setActiveView(\"skill-tuner\")' frontend/src/App.tsx` == 0 (the redirect routes it to skill-studio?tab=triggering).
    - ChatLayout has the mount branch: `grep -c 'skill-studio' frontend/src/components/layout/ChatLayout.tsx` >= 1 and `grep -c 'SkillStudioPage' frontend/src/components/layout/ChatLayout.tsx` >= 1.
    - SkillsPage Props gain the navigators without a duplicate button: `grep -c 'onOpenStudio\|onReviewEvals' frontend/src/pages/SkillsPage.tsx` >= 2 and there is NO "Open studio" button in SkillsPage (`grep -ci 'open studio' frontend/src/pages/SkillsPage.tsx` == 0).
    - The existing "Tune triggers" onTuneSkill call is preserved (grep still matches `onTuneSkill`).
    - `npx tsc -p tsconfig.json --noEmit` reports no new type errors introduced by these files.
  </acceptance_criteria>
  <done>The skill-studio ActiveView + navigators + legacy redirect are wired; ChatLayout mounts the Studio; SkillsPage accepts (but does not consume) the navigators — no duplicate "Open studio" button; the app typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ActiveView nav state -> Studio mount | Nav is client `useState<ActiveView>` (no router); no new server trust boundary. |
| server PublishGate -> header gate strip | The header strip condenses the same server gate the EvalsTab stepper renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Elevation of Privilege | header gate strip | mitigate | The header gate strip is `LifecycleStepper variant="strip"` fed the same `getPublishGate` response with a `caseCount` read from the same owner-scoped `listTestCases` list — a pure condensation, never a second truth-teller and never a client recompute of `met` (D-01/D-03). |
| T-137-02 | Information Disclosure | Studio skill identity | mitigate | The Studio renders only the skill selected in App state; the skill lookup + version derivation + case-count read the owner-scoped `useSkills` list + `listSkillVersions` + `listTestCases`; a null/absent skill renders the calm guard, never a leaked id. |
| T-137-06 | Tampering (design integrity) | SkillTunerPage absorption | mitigate | TriggeringTab mounts SkillTunerPage with a SINGLE additive `embedded` header-suppression flag and imports none of its internals (`components/skills/tuner/*`); the tuner's behavior + live-run machinery + the 041/042/043/045 winners are untouched (D-02 — a one-line render guard is not a behavior change). |
| T-137-SC | Tampering (supply chain) | npm install surface | accept | Zero new packages; slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/SkillStudioPage.test.tsx` green; `npx tsc -p tsconfig.json --noEmit` clean for the touched files.
- Grep gates confirm the ActiveView shell, the mount branch, the strip variant, the shell-level version derivation via the shared `deriveLiveVersion` helper, the listTestCases-sourced caseCount (no hardcoded 0), the `embedded`-guarded tuner mount, and the absence of a duplicate "Open studio" button.
</verification>

<success_criteria>
The Studio is reachable via a net-new skill-studio ActiveView (with the legacy skill-tuner redirect), renders the persistent header + condensed gate strip (caseCount sourced from listTestCases) + shell-derived vN (via the shared deriveLiveVersion helper) + three tabs (landing Evals, Triggering = tuner absorbed via the one-line `embedded` header guard, Versions), and the Skills surface threads the navigators (the single "Open studio" button lands in Plan 07). Spec green.
</success_criteria>

<output>
Create `.planning/phases/137-skill-evals-panel-ui-panel-01/137-06-SUMMARY.md` when done.
</output>
</content>
</invoke>
