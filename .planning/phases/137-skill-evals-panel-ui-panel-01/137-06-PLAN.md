---
phase: 137-skill-evals-panel-ui-panel-01
plan: 06
type: execute
wave: 3
depends_on: [137-01, 137-02, 137-05]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/pages/SkillStudioPage.tsx
  - frontend/src/pages/SkillStudioPage.test.tsx
  - frontend/src/components/skills/studio/TriggeringTab.tsx
  - frontend/src/pages/SkillsPage.tsx
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "A net-new skill-studio ActiveView value with a tab param renders ONE focused full-surface Studio; the legacy skill-tuner value REDIRECTS to skill-studio?tab=triggering (no orphan Tuner surface) (D-01)"
    - "'‹ Skills' returns preserving the selected skill + panel state; the landing tab is Evals (D-01)"
    - "A persistent header on every tab shows skill name + vN + LIVE badge + a compact one-line gate strip (the LifecycleStepper strip variant — a condensation of the SAME server PublishGate, never a second truth-teller) (D-01, D-03)"
    - "Evals · Triggering · Versions tabs switch and are deep-linkable via the tab param (D-01, D-06)"
    - "The Triggering tab mounts the shipped SkillTunerPage UNMODIFIED (its internals untouched) (D-02)"
    - "An 'Open studio' entry from the Skills detail surface reaches the Studio (Evals tab); the lint/'Tune triggers' handoff auto-re-points to Studio·Triggering via the repointed handleTuneSkill (D-06)"
  artifacts:
    - path: "frontend/src/pages/SkillStudioPage.tsx"
      provides: "Focused full-surface Studio: persistent header + gate strip + Evals/Triggering/Versions tabs"
      min_lines: 90
    - path: "frontend/src/components/skills/studio/TriggeringTab.tsx"
      provides: "Thin wrapper mounting SkillTunerPage unmodified"
      min_lines: 15
  key_links:
    - from: "frontend/src/App.tsx"
      to: "ActiveView union + handleOpenStudio + legacy skill-tuner redirect"
      via: "setActiveView('skill-studio') + studioSkillId/studioTab state"
      pattern: "skill-studio"
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "SkillStudioPage mount branch"
      via: "activeView === 'skill-studio'"
      pattern: "SkillStudioPage"
    - from: "SkillStudioPage.tsx"
      to: "EvalsTab / TriggeringTab / VersionsTab"
      via: "tab-driven body render"
      pattern: "EvalsTab|TriggeringTab|VersionsTab"
---

<objective>
Make the Studio reachable and assemble the shell: the `skill-studio` `ActiveView` (mirroring the shipped `skill-tuner` precedent, no router), the legacy `skill-tuner` redirect, the persistent header with a condensed gate strip, the three tabs (Evals landing, Triggering = the tuner mounted unmodified, Versions), and the "Open studio" entry from the Skills detail surface.

Purpose: Deliver the D-01 shell + D-02 tuner absorption + the reachability triad (App union + ChatLayout mount + entry action) owned in-phase, so the Studio is not a built-but-unreachable surface.
Output: `SkillStudioPage.tsx` + `TriggeringTab.tsx` (new) + `App.tsx` / `ChatLayout.tsx` / `SkillsPage.tsx` wiring (+ shell spec).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-RESEARCH.md
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-PATTERNS.md
@.planning/sketches/053-eval-studio-shell/README.md
@.planning/sketches/057-skill-studio-linkage/README.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/App.tsx
@frontend/src/components/layout/ChatLayout.tsx
@frontend/src/pages/SkillTunerPage.tsx
@frontend/src/pages/SkillsPage.tsx
@frontend/src/components/skills/studio/LifecycleStepper.tsx
@frontend/src/components/skills/studio/EvalsTab.tsx
@frontend/src/components/skills/studio/VersionsTab.tsx

<interfaces>
<!-- The shipped precedent this shell clones (VERIFIED). -->
App.tsx :9 ActiveView union (…| "skill-tuner"); :21-25 tunerSkillId + handleTuneSkill(skillId){ setTunerSkillId; setActiveView("skill-tuner") }; threaded to <ChatLayout> :42-50.
ChatLayout.tsx :42/:46 Props (tunerSkillId, onTuneSkill); :316-324 mount branch: activeView === "skill-tuner" ? <SkillTunerPage skillId={tunerSkillId} onBack={() => onNavigate("skills")} /> : <KnowledgeHealthPage/>.
SkillTunerPage.tsx :51-57 Props { skillId, onBack }; :87-94 useSkills + skill lookup guard; :537-556 focused-surface header chrome.
SkillsPage.tsx :22 SkillsPage({ onTryInChat, onTuneSkill }); :187-193 the "Tune triggers" entry-action precedent; :202-206 SkillDetailPanel mount.
Studio tab type: "evals" | "triggering" | "versions" (landing = "evals").
Components to compose: EvalsTab (Plan 05), VersionsTab (Plan 02), LifecycleStepper strip variant (Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App + ChatLayout — the ActiveView shell, state, navigators, legacy redirect</name>
  <files>frontend/src/App.tsx, frontend/src/components/layout/ChatLayout.tsx</files>
  <read_first>
    - frontend/src/App.tsx (:9 ActiveView union; :21-25 tunerSkillId + handleTuneSkill; :42-50 the ChatLayout prop threading — the exact shape to clone)
    - frontend/src/components/layout/ChatLayout.tsx (:42/:46 Props; :316-324 the skill-tuner mount branch — add the analogous skill-studio branch BEFORE the trailing else)
    - .planning/phases/137-skill-evals-panel-ui-panel-01/137-CONTEXT.md (D-01 shell + legacy redirect; D-06 lint re-point)
  </read_first>
  <action>
    In App.tsx: add `"skill-studio"` to the `ActiveView` union (:9); add `studioSkillId: string | null` + `studioTab: "evals" | "triggering" | "versions"` state; add `handleOpenStudio(skillId, tab = "evals")` (sets both then `setActiveView("skill-studio")`) and `handleReviewEvals(skillId)` (= `handleOpenStudio(skillId, "evals")`); add `handleStudioTabChange(tab)` (sets studioTab). Implement the LEGACY REDIRECT (D-01): repoint the existing `handleTuneSkill(skillId)` to `handleOpenStudio(skillId, "triggering")` so the shipped "Tune triggers" / lint "Tune this →" callers land in Studio·Triggering with no orphan Tuner surface (this is the D-06 lint re-point, achieved by repointing the one navigator). Thread `studioSkillId`, `studioTab`, `onOpenStudio={handleOpenStudio}`, `onReviewEvals={handleReviewEvals}`, `onStudioTabChange={handleStudioTabChange}` down to `<ChatLayout>` exactly as `tunerSkillId`/`onTuneSkill` are threaded (:42-50). In ChatLayout.tsx: extend the Props interface (:42) + destructure (:46) for the new props; add an `activeView === "skill-studio"` mount branch BEFORE the trailing `KnowledgeHealthPage` else (mirror :316-324) rendering `<SkillStudioPage skillId={studioSkillId} tab={studioTab} onTabChange={onStudioTabChange} onBack={() => onNavigate("skills")} />`; pass `onOpenStudio` + `onReviewEvals` through to `<SkillsPage>`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/SkillStudioPage.test.tsx && npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'skill-studio' frontend/src/App.tsx` >= 2 (union + setActiveView).
    - `handleTuneSkill` no longer sets `"skill-tuner"` directly: `grep -c 'setActiveView(\"skill-tuner\")' frontend/src/App.tsx` == 0 (the redirect routes it to skill-studio?tab=triggering).
    - ChatLayout has the mount branch: `grep -c 'skill-studio' frontend/src/components/layout/ChatLayout.tsx` >= 1 and `grep -c 'SkillStudioPage' frontend/src/components/layout/ChatLayout.tsx` >= 1.
    - `npx tsc -p tsconfig.json --noEmit` reports no new type errors introduced by these files.
  </acceptance_criteria>
  <done>The skill-studio ActiveView + navigators + legacy redirect are wired; ChatLayout mounts the Studio; the app typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SkillStudioPage + TriggeringTab — header, gate strip, tabs</name>
  <files>frontend/src/pages/SkillStudioPage.tsx, frontend/src/components/skills/studio/TriggeringTab.tsx, frontend/src/pages/SkillStudioPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/SkillTunerPage.tsx (:51-57 Props + :87-94 guard + :537-556 header chrome — the focused-surface precedent to adapt)
    - .planning/sketches/053-eval-studio-shell/README.md (shell winner A + Build Handover) and .planning/sketches/057-skill-studio-linkage/README.md (the persistent header + 3 tabs + landing=Evals + deep-linkable tabs contract)
    - frontend/src/components/skills/studio/LifecycleStepper.tsx (strip variant for the header gate strip), EvalsTab.tsx, VersionsTab.tsx (the tab bodies to compose)
    - frontend/src/hooks/useSkills.ts (skill identity for the header)
  </read_first>
  <behavior>
    - Renders a focused full-surface (no router) with a persistent header on every tab: "‹ Skills" back, skill name + vN + LIVE badge + a one-line gate strip (LifecycleStepper variant="strip").
    - The gate strip condenses the SAME getPublishGate the EvalsTab stepper renders — never a second truth-teller.
    - Tab chrome Evals · Triggering · Versions, driven by the `tab` prop; landing = Evals; switching a tab calls onTabChange (deep-linkable).
    - Evals -> <EvalsTab>; Triggering -> <TriggeringTab> (mounts SkillTunerPage unmodified); Versions -> <VersionsTab>.
    - skillId null -> a calm centered guard with a "‹ Skills" back button (the SkillTunerPage guard pattern).
    - "‹ Skills" calls onBack (returns to the Skills page preserving selection).
  </behavior>
  <action>
    Create `TriggeringTab` (props `{ skillId, onBack }`) as a thin wrapper mounting `<SkillTunerPage skillId={skillId} onBack={onBack} />` UNMODIFIED — do NOT edit SkillTunerPage internals or components/skills/tuner/*. Keep any needed header suppression on the wrapper only (verify against the 057-A sketch so the tuner sits cleanly inside the tab). Create `SkillStudioPage` (props `{ skillId, tab, onTabChange, onBack }`): look up the skill via useSkills (calm guard when skillId is null); render the persistent header (‹ Skills, skill name · vN · LIVE badge · `<LifecycleStepper variant="strip" publishGate=… caseCount=… skillVersion=… />`); render the tab bar (Evals/Triggering/Versions) driven by `tab` with `onTabChange`; render the active tab body — `<EvalsTab skillId skillVersion=… />` / `<TriggeringTab skillId onBack=… />` / `<VersionsTab skillId liveVersionNumber=… />`. Fetch the publish gate once at the shell level (getPublishGate) and feed the strip so it is the same source as EvalsTab's stepper. Author the co-located spec fresh (mock `@/lib/api` + mock EvalsTab/VersionsTab/SkillTunerPage as stubs): landing renders Evals; clicking Triggering calls onTabChange and renders the tuner stub; clicking Versions renders the versions stub; a null skillId renders the calm guard + back; "‹ Skills" calls onBack; the header shows the skill name.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/SkillStudioPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SkillStudioPage composes the three tabs: `grep -c 'EvalsTab\|TriggeringTab\|VersionsTab' frontend/src/pages/SkillStudioPage.tsx` >= 3.
    - The header uses the strip variant: grep matches `variant="strip"` (or `variant='strip'`).
    - TriggeringTab mounts SkillTunerPage and does NOT import from components/skills/tuner: `grep -c 'SkillTunerPage' frontend/src/components/skills/studio/TriggeringTab.tsx` >= 1 and `grep -c 'skills/tuner' frontend/src/components/skills/studio/TriggeringTab.tsx` == 0.
    - Spec asserts: landing = Evals; tab switch calls onTabChange; null skillId -> guard + back; back calls onBack.
  </acceptance_criteria>
  <done>The Studio shell renders the persistent header + condensed gate strip + three tabs (landing Evals), with the tuner absorbed unmodified; spec green.</done>
</task>

<task type="auto">
  <name>Task 3: SkillsPage — the "Open studio" entry action</name>
  <files>frontend/src/pages/SkillsPage.tsx</files>
  <read_first>
    - frontend/src/pages/SkillsPage.tsx (:19-22 Props + SkillsPage signature; :187-193 the "Tune triggers" entry-action precedent; :202-206 the SkillDetailPanel mount)
    - .planning/sketches/057-skill-studio-linkage/README.md (the MAP: panel "Open studio" -> Studio·Evals)
  </read_first>
  <action>
    Extend `SkillsPage` Props with `onOpenStudio?: (skillId: string, tab?: "evals"|"triggering"|"versions") => void` and `onReviewEvals?: (skillId: string) => void`, destructure them in the signature (:22). Add an "Open studio" entry button in the detail-surface action area (clone the :187-193 "Tune triggers" precedent) that calls `onOpenStudio(selectedSkill.id, "evals")`. Keep the existing "Tune triggers" button (its `onTuneSkill` now auto-lands in Studio·Triggering via the App redirect — no change needed here). Do NOT yet drill onOpenStudio/onReviewEvals into SkillCard or SkillDetailPanel (Plan 07 owns that drill + the slim-down). Panel-state preservation on return is inherent to the existing selectedSkill + useResizablePanel state.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/SkillStudioPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SkillsPage Props include onOpenStudio + onReviewEvals: `grep -c 'onOpenStudio\|onReviewEvals' frontend/src/pages/SkillsPage.tsx` >= 2.
    - An "Open studio" control calls onOpenStudio with the selected skill id and "evals": grep matches `onOpenStudio(` .
    - The existing "Tune triggers" onTuneSkill call is preserved (grep still matches `onTuneSkill`).
  </acceptance_criteria>
  <done>The Skills detail surface offers an "Open studio" entry reaching Studio·Evals; the tuner handoff auto-re-points via the App redirect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ActiveView nav state -> Studio mount | Nav is client `useState<ActiveView>` (no router); no new server trust boundary. |
| server PublishGate -> header gate strip | The header strip condenses the same server gate the EvalsTab stepper renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Elevation of Privilege | header gate strip | mitigate | The header gate strip is `LifecycleStepper variant="strip"` fed the same `getPublishGate` response — a pure condensation, never a second truth-teller and never a client recompute of `met` (D-01/D-03). |
| T-137-02 | Information Disclosure | Studio skill identity | mitigate | The Studio renders only the skill selected in App state; the skill lookup is over the owner-scoped `useSkills` list; a null/absent skill renders the calm guard, never a leaked id. |
| T-137-06 | Tampering (design integrity) | SkillTunerPage absorption | mitigate | TriggeringTab mounts SkillTunerPage unmodified and does not import its internals (`components/skills/tuner/*`), preserving the 041/042/043/045 winners + the tuner's own live-run machinery (D-02). |
| T-137-SC | Tampering (supply chain) | npm install surface | accept | Zero new packages; slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/SkillStudioPage.test.tsx` green; `npx tsc -p tsconfig.json --noEmit` clean for the touched files.
- Grep gates confirm the ActiveView shell, the mount branch, the strip variant, the unmodified tuner mount, and the Open studio entry.
</verification>

<success_criteria>
The Studio is reachable via a net-new skill-studio ActiveView (with the legacy skill-tuner redirect), renders the persistent header + condensed gate strip + three tabs (landing Evals, Triggering = tuner unmodified, Versions), and the Skills detail surface offers an "Open studio" entry. Spec green.
</success_criteria>

<output>
Create `.planning/phases/137-skill-evals-panel-ui-panel-01/137-06-SUMMARY.md` when done.
</output>
