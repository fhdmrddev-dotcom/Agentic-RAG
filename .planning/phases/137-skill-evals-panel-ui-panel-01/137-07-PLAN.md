---
phase: 137-skill-evals-panel-ui-panel-01
plan: 07
type: execute
wave: 4
depends_on: [137-01, 137-06]
files_modified:
  - frontend/src/pages/SkillsPage.tsx
  - frontend/src/components/skills/SkillCard.tsx
  - frontend/src/components/skills/PublishGateDialog.tsx
  - frontend/src/components/skills/PublishGateDialog.test.tsx
  - frontend/src/components/skills/SkillFormDialog.tsx
  - frontend/src/components/skills/SkillFormDialog.test.tsx
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "The detail panel slims down: SkillEvalSection + SkillTestCasesSection LEAVE the edit dialog; the panel keeps the edit form + a status section + 'Open studio' + counts (D-07)"
    - "The panel status section renders the FULL 054-B LifecycleStepper (the SAME shared component the Studio header condenses) — dissolving the 'Publish ready 1/1 vs 0/2 cases' contradiction where the operator first hit it (D-07, D-10)"
    - "The 136 PublishGateDialog gains ONE net-new 'Review evals →' link on the unmet branch, wired to onReviewEvals -> Studio·Evals (D-06)"
    - "The Studio is otherwise additive — no Skills-tab redesign beyond the deliberate detail-panel slim-down (PANEL-01 exclusion)"
  artifacts:
    - path: "frontend/src/components/skills/SkillFormDialog.tsx"
      provides: "Slimmed detail panel: sections removed, shared LifecycleStepper + Open studio + counts added"
    - path: "frontend/src/components/skills/PublishGateDialog.tsx"
      provides: "'Review evals →' link on the unmet branch"
  key_links:
    - from: "SkillFormDialog.tsx (SkillDetailPanel)"
      to: "LifecycleStepper (shared, full variant)"
      via: "panel status section"
      pattern: "LifecycleStepper"
    - from: "PublishGateDialog.tsx"
      to: "onReviewEvals -> Studio·Evals"
      via: "unmet-branch 'Review evals →' link"
      pattern: "onReviewEvals"
---

<objective>
Slim the Skills detail panel and wire the remaining 057-MAP navigation links. The two heavy sections (`SkillEvalSection` + `SkillTestCasesSection`) leave the edit dialog; in their place the panel renders the SHARED full `LifecycleStepper` (the same one the Studio header condenses — one truth-teller, two homes) + an "Open studio" button + counts. The 136 `PublishGateDialog` gains its one net-new "Review evals →" link on the unmet branch.

Purpose: Close the "messy UI" complaint (the U11 panel-density finding) with a calm slim panel (D-07), dissolve the 1/1-vs-0/2 contradiction where the operator first hit it via the shared stepper (D-10), and make the gate discoverable from the publish flow (D-06) — additive, with no Skills-tab redesign.
Output: slimmed `SkillFormDialog.tsx` (SkillDetailPanel), `PublishGateDialog.tsx` link, `SkillCard.tsx` + `SkillsPage.tsx` navigator drill (+ spec extensions).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-RESEARCH.md
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-PATTERNS.md
@.planning/sketches/057-skill-studio-linkage/README.md
@.planning/phases/136-skill-publish-gate-gate-01/136-CONTEXT.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/skills/SkillFormDialog.tsx
@frontend/src/components/skills/PublishGateDialog.tsx
@frontend/src/components/skills/SkillCard.tsx
@frontend/src/pages/SkillsPage.tsx
@frontend/src/components/skills/studio/LifecycleStepper.tsx

<interfaces>
<!-- Slim-down target: SkillDetailPanel (exported from SkillFormDialog.tsx). -->
SkillFormDialog.tsx: imports :14-15 (SkillTestCasesSection, SkillEvalSection — REMOVE); SkillDetailPanel savedSkillId :426; onTuneThis prop precedent :44/:64/:524-528; the two section mounts :535-548 (REMOVE, replace with stepper + Open studio + counts).
PublishGateDialog.tsx: unmet branch :117-126 (UNMET_COPY[gate.state] + gate.reason + "Run an eval…"); SkillCard mounts it :269-271 with showPublishDialog.
SkillCard.tsx: :39 showPublishDialog; :269-271 PublishGateDialog mount.
SkillsPage.tsx: onOpenStudio/onReviewEvals props (added in Plan 06); SkillCard mount :141; SkillDetailPanel mount :202-206.
LifecycleStepper (Plan 01): props { publishGate, caseCount, skillVersion, variant?: "full"|"strip", onNavigateStage? }; use variant="full" here.
Navigators (from App via Plan 06): onOpenStudio(skillId, tab?), onReviewEvals(skillId).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slim the detail panel — remove sections, add shared stepper + Open studio</name>
  <files>frontend/src/components/skills/SkillFormDialog.tsx, frontend/src/components/skills/SkillFormDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillFormDialog.tsx (imports :14-15; SkillDetailPanel savedSkillId :426; onTuneThis precedent :524-528; the two section mounts :535-548 to remove)
    - frontend/src/components/skills/studio/LifecycleStepper.tsx (the shared full-variant stepper to mount here)
    - .planning/sketches/057-skill-studio-linkage/README.md + .planning/sketches/053-eval-studio-shell/README.md (the slim-panel "calm at rest" bar + the panel status = full stepper, D-10)
    - frontend/src/lib/api.ts (getPublishGate, listTestCases — for the panel's gate + case count)
  </read_first>
  <behavior>
    - The SkillEvalSection + SkillTestCasesSection mounts are removed from the detail panel.
    - In their place: a status section rendering the SHARED <LifecycleStepper variant="full"> fed the skill's getPublishGate + case count + version; an "Open studio" button (calls onOpenStudio(savedSkillId, "evals")); and counts (cases / latest run summary).
    - The panel at rest is calm (closes the U11 density complaint); the "Publish ready 1/1 vs 0/2" contradiction is dissolved by the stepper (D-10).
    - The edit form itself is unchanged.
  </behavior>
  <action>
    In SkillFormDialog.tsx (the exported `SkillDetailPanel`): remove the `SkillTestCasesSection` + `SkillEvalSection` imports (:14-15) and their two mounts (:535-548). Add an `onOpenStudio?: (skillId: string, tab?: "evals"|"triggering"|"versions") => void` prop (thread it like the existing `onTuneThis`/`onTuneSkill` precedent). In place of the removed sections render: (1) a status section with `<LifecycleStepper variant="full" publishGate={gate} caseCount={caseCount} skillVersion={version} />` where `gate` is a `getPublishGate(savedSkillId)` fetch and `caseCount` is a `listTestCases(savedSkillId)` length; (2) an "Open studio" button calling `onOpenStudio(savedSkillId, "evals")`; (3) a small counts line. Keep the skill-switch guard on the panel's gate/case fetch. Do NOT reintroduce eval-run or case-CRUD machinery in the panel — that now lives in the Studio. Extend `SkillFormDialog.test.tsx`: assert the two sections are gone (SkillTestCasesSection/SkillEvalSection not rendered), the LifecycleStepper renders, and the "Open studio" button calls onOpenStudio with (skillId, "evals").
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/SkillFormDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The section imports are gone: `grep -c 'SkillTestCasesSection\|SkillEvalSection' frontend/src/components/skills/SkillFormDialog.tsx` == 0.
    - The shared stepper is mounted full-variant: grep matches `LifecycleStepper` and `variant="full"`.
    - The Open studio button calls onOpenStudio: grep matches `onOpenStudio(`.
    - Spec asserts the sections are absent + the stepper present + Open studio wired.
  </acceptance_criteria>
  <done>The detail panel is slimmed to form + shared stepper + Open studio + counts; the two heavy sections are gone; spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PublishGateDialog "Review evals →" + the navigator drill</name>
  <files>frontend/src/components/skills/PublishGateDialog.tsx, frontend/src/components/skills/PublishGateDialog.test.tsx, frontend/src/components/skills/SkillCard.tsx, frontend/src/pages/SkillsPage.tsx</files>
  <read_first>
    - frontend/src/components/skills/PublishGateDialog.tsx (the unmet branch :117-126 — add the link after the "Run an eval…" line)
    - frontend/src/components/skills/SkillCard.tsx (:39 showPublishDialog; :269-271 the PublishGateDialog mount — pass onReviewEvals through)
    - frontend/src/pages/SkillsPage.tsx (the SkillCard mount :141 + the SkillDetailPanel mount :202-206 — drill onReviewEvals to SkillCard and onOpenStudio to SkillDetailPanel)
    - .planning/sketches/057-skill-studio-linkage/README.md (the MAP: PublishGateDialog unmet -> "Review evals →" -> Studio·Evals)
  </read_first>
  <behavior>
    - The PublishGateDialog unmet branch (gate && !gate.met) shows a "Review evals →" link that calls onReviewEvals(skillId) -> the Studio Evals tab.
    - The link is only on the UNMET branch (the "gate only discoverable in the dialog" gap closes).
    - onReviewEvals threads App -> ChatLayout -> SkillsPage -> SkillCard -> PublishGateDialog; onOpenStudio threads SkillsPage -> SkillDetailPanel.
  </behavior>
  <action>
    In PublishGateDialog.tsx: add an `onReviewEvals?: (skillId: string) => void` prop; on the unmet branch (:117-126), after the existing "Run an eval…" copy, render a "Review evals →" link/button that calls `onReviewEvals?.(skillId)`. In SkillCard.tsx: add an `onReviewEvals?: (skillId: string) => void` prop and pass it to the `<PublishGateDialog … onReviewEvals={onReviewEvals} />` mount (:269-271). In SkillsPage.tsx: drill `onReviewEvals` into the `<SkillCard>` mount (:141) and `onOpenStudio` into the `<SkillDetailPanel>` mount (:202-206) so the navigators reach their consumers. Extend `PublishGateDialog.test.tsx`: on an unmet gate the "Review evals →" link renders and clicking it calls onReviewEvals with the skill id; on a met gate the link is absent.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/PublishGateDialog.test.tsx && npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PublishGateDialog renders the link only on unmet: `grep -c 'onReviewEvals' frontend/src/components/skills/PublishGateDialog.tsx` >= 1 and the spec asserts the link is absent when gate.met is true.
    - SkillCard passes onReviewEvals to the dialog: `grep -c 'onReviewEvals' frontend/src/components/skills/SkillCard.tsx` >= 1.
    - SkillsPage drills both navigators: `grep -c 'onReviewEvals\|onOpenStudio' frontend/src/pages/SkillsPage.tsx` >= 2.
    - `npx tsc -p tsconfig.json --noEmit` clean (the full navigator drill typechecks end to end).
  </acceptance_criteria>
  <done>The unmet PublishGateDialog offers "Review evals →" to Studio·Evals; both navigators are drilled to their consumers; the app typechecks; specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server PublishGate -> panel status section | The slim panel's stepper renders the same server gate the Studio header condenses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Elevation of Privilege | panel status stepper / Publish button | mitigate | The slim panel renders the SHARED `LifecycleStepper` fed the server `getPublishGate` — it never recomputes `met` (D-07/054 one-truth lock); the same component the Studio header condenses (one truth-teller, two homes, D-10). |
| T-137-02 | Information Disclosure | panel gate/case fetch on skill switch | mitigate | The panel's gate/case fetch carries the skill-switch guard so a late fetch never renders under a different skill. |
| T-137-03 | Information Disclosure | Review-evals navigation | mitigate | The "Review evals →" link passes only the owner's `skillId` to the navigator; it opens the owner-scoped Studio and constructs no cross-user id. |
| T-137-05 | Injection (XSS) | panel counts / gate copy render | mitigate | The panel status copy, counts, and gate reason render as React text nodes; no `dangerouslySetInnerHTML`. |
| T-137-SC | Tampering (supply chain) | npm install surface | accept | Zero new packages; slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/skills/SkillFormDialog.test.tsx src/components/skills/PublishGateDialog.test.tsx` green; `npx tsc -p tsconfig.json --noEmit` clean.
- Grep gates confirm the removed sections, the shared full stepper in the panel, the unmet-only "Review evals →" link, and the drilled navigators.
</verification>

<success_criteria>
The detail panel is slimmed to a calm form + shared full stepper + Open studio + counts (D-07/D-10), the PublishGateDialog unmet branch offers "Review evals →" (D-06), and the whole navigator drill typechecks — additive, no Skills-tab redesign. Specs green.
</success_criteria>

<output>
Create `.planning/phases/137-skill-evals-panel-ui-panel-01/137-07-SUMMARY.md` when done.
</output>
