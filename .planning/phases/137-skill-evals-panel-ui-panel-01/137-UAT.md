---
status: complete
phase: 137-skill-evals-panel-ui-panel-01
source: [137-VALIDATION.md (SC#10 U1-U7 + G-4 U8-U13), 137-01..07-SUMMARY.md]
started: 2026-07-04T00:50:00Z
updated: 2026-07-04T02:10:00Z
preflight: "tsc --noEmit clean; vitest 1143 passed / 18 failed — all 18 in documented SEED-056 rot files (useMessages / StreamsProvider / MessageItem / PublishGauntlet / model-info / soulData), 0 failures in Phase-137 specs. No NEW failures beyond baseline."
session_notes: "Pre-session organic findings already fixed this session: LifecycleStepper connector-through-circles (fa253174); eval baseline-arm contamination engine fix (f47d6736, backend — outside PANEL-01 scope but affects live eval runs used in testing)."
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. U1 — Cross-provider render: OpenAI
expected: OpenAI run row shows the OpenAI provider logo, honest WITH/WITHOUT rollup, per-case verdict chips only at finalize (no mid-run verdict). psycopg2 cross-check of eval_runs/eval_results matches the render.
result: pass

### 2. U2 — Cross-provider render: Anthropic
expected: Same as U1 on an Anthropic model row — Anthropic logo, honest rollup, finalize-only verdicts; DB cross-check matches.
result: pass

### 3. U3 — Cross-provider render: Google
expected: Same as U1 on a Google model row — Google logo, honest rollup, finalize-only verdicts; DB cross-check matches.
result: pass

### 4. U4 — Cross-provider render: OpenRouter
expected: Same as U1 on an OpenRouter row — logo (or the correct Bot fallback for unknown marks), honest rollup. Known third-party upstream 404 → record BLOCKED with evidence; axis satisfied by U1-U3 per prior-phase precedent.
result: pass

### 5. U5 — Multi-tool case render
expected: Expand a run whose case exercised 2+ tools (e.g. search_documents + execute_code). Both WITH/WITHOUT arms render the multi-tool output + judge reason + token counts without truncation or a fabricated verdict; the case leads with its PROMPT text, never a uuid.
result: pass

### 6. U6 — Parallel-thread isolation
expected: Start an eval for Skill A in the Studio; in a second tab open the Studio for Skill B and start its own eval. Each Studio shows ONLY its own run (no cross-skill leak), live rows self-heal on a transient drop, durable readout survives a reload. DB/Redis snapshot confirms two distinct runs.
result: pass
evidence: "DB cross-check 22:10Z — two distinct concurrent runs on two skills: docx/gpt-5.4-mini (92ad15a2, completed 22:10) overlapping pptx/deepseek-v4-flash (f603a150, running from 22:10)."

### 7. U7 — Long-message case render
expected: An eval case with a ≥5KB prompt renders both arms' long content in the widened full surface without crash or clipping; the readout stays honest.
result: pass
evidence: "Claude-driven via Chrome DevTools MCP + psycopg2. Inserted a 7.7KB-prompt probe case on weekly-report-writer, ran openai/gpt-5.4-mini eval live. Mid-run: row showed 'running', NO verdict chips (finalize-only honesty confirmed). Finalized: honest '1/2 passed' rollup; expanded detail rendered both cases side-by-side — full judge reasons, full answers, token counts (17897→157), PASS·88/FAIL·0 chips; case header truncates the long prompt gracefully with ellipsis; zero horizontal overflow (scrollWidth==clientWidth==1536). Screenshot verified. All probe artifacts deleted after (case, run, results, 2 eval threads) — skill restored to 1 case / never_evaled."
note: "Discovered separate issue during this test — logged in Gaps (stepper stale after in-session eval completion)."

### 8. U8 — Tab switching + deep-links
expected: Panel gate → lands on Evals tab. Lint "Tune this →" → lands on Triggering (tuner intact, NO doubled back-header). Version row → lands on Versions. Switching all three tabs keeps the persistent header + condensed gate strip stable, agreeing with the EvalsTab full stepper (one truth-teller) and the same "vN".
result: pass

### 9. U9 — Panel calm-at-rest + collapse
expected: The slimmed detail panel reads calm at rest — form + ONE status stepper + a SINGLE "Open studio" button + honest counts, no stacked eval/case machinery. Collapsed/resized to 384px the stepper stays legible (connector no longer crosses the circles — fixed fa253174).
result: pass

### 10. U10 — Mid-run navigate away/return
expected: Start an eval, navigate away (‹ Skills) and reopen the Studio mid-run: the live row re-attaches, the readout is durable (not restarted, no false error terminal); at finalize the verdicts render honestly.
result: pass

### 11. U11 — Interrupted run honesty
expected: A run interrupted mid-flight (e.g. backend restart orphan) renders an interrupted banner + a re-run affordance — never a silent success, failure, or perpetual "running". (Session note: the orphaned 2026-07-02 gemini run was flipped to interrupted in DB — its row is live test material.)
result: pass

### 12. U12 — Gate states + ⚡ stale-pass collision
expected: Gate-UNMET skill — the stepper's current stage narrates the unmet reason; header strip agrees. passed_on_older_version skill — the ⚡ collision reads as TWO labeled facts (met Gate bound to the older passing run + Eval node carrying the newest honest count + "measured vN-1, live is vN"), never a flat contradiction. Force-publish override receipt renders un-softened where present. PublishGateDialog unmet branch shows "Review evals →" landing on Studio·Evals.
result: pass

### 13. U13 — Versions table, compare + provenance
expected: Versions tab — scan-first table with LIVE badge + provenance chips mapping the real source values; picking any two versions renders ONE unified diff; a force-promoted version shows its un-softened failed-gate evidence; there is NO "Restore" action.
result: pass

## Summary

total: 13
passed: 13
issues: 0
pending: 0
skipped: 0
gaps: 1 found → 1 CLOSED (stepper/strip stale after in-session eval completion — fixed 40e2f8a3, live-verified)

## Gaps

```yaml
- truth: "The Studio header strip + lifecycle stepper reflect the server publish-gate after an eval run completes in-session (one truth-teller, D-03/D-10)"
  status: closed
  fixed_by: "40e2f8a3 — EvalsTab refetches the gate on the running→false finalize edge + new onGateStale prop pings the shell strip; spec 2b locks the edge. Verified LIVE post-fix: ran an eval in the open Studio, strip flipped to 'Latest eval failed · 0/1 on v1' + stepper to 'Eval 0/1 / Gate not met' at finalize with ZERO reloads. G-3 quick-fix path, operator-approved."
  reason: "Observed live during U7: ran an eval to completion inside the open Studio — the run row finalized honestly ('1/2 passed') but the header strip kept reading 'Not evaluated · 2 cases ready' and the full stepper kept 'Eval not run / Gate —'. Only a full page reload updated them to the correct 'Latest eval failed · 1/2 on v1' + 'Eval 1/2 / Gate not met'. The EvalsTab refetches the run readout on eval_complete but the PublishGate feeding the stepper/strip is never refetched."
  severity: major
  test: 7
  artifacts: ["frontend/src/components/skills/studio/EvalsTab.tsx (onEvalComplete refetch machinery)", "frontend/src/pages/SkillStudioPage.tsx (shell-owned getPublishGate feeding strip + tab stepper)"]
  missing: ["gate refetch on eval terminal (eval_complete) so LifecycleStepper + header strip update without a reload"]
```

**Session data-fixes (not code gaps):**
- 3 stale `[eval] skill A/B run` threads (created 2026-07-01 21:29-21:40, the pre-134.1-fix window) had `is_eval=false` and leaked into the chat sidebar — flipped to `true` in DB (mig-082 backfill gap; all 101 threads created since are properly flagged, no live leak).
