---
phase: 088-cross-cutting-verification-accessibility
plan: 04
type: execute
wave: 2
depends_on: ["088-02"]
files_modified:
  - .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md
  - backend/app/api/threads.py
  - backend/app/services/openai_service.py
autonomous: false
requirements: [A11Y-01]
user_setup:
  - service: backend-uvicorn
    why: "The fold-gate runs the eval script live (before + after the text change) — operator starts uvicorn; provider keys must be configured"
    env_vars: []
    dashboard_config:
      - task: "OpenAI / Anthropic / Google(3.x) / OpenRouter keys configured so the eval script can measure all four providers"
        location: "Settings UI / backend/.env"

must_haves:
  truths:
    - "The eval script (Plan 02) is run as a BASELINE across all 4 providers and the per-row results recorded in 088-VALIDATION.md"
    - "A fold/no-fold DECISION is recorded in 088-VALIDATION.md per the D-05 gate (all three conditions evaluated with evidence)"
    - "IF folded: the change is TEXT-ONLY (SYSTEM_PROMPT@threads.py:336 string + openai_service get_tools() description strings) — a git diff proves zero structural/schema/per-provider change"
    - "IF folded: a re-run shows >=1 previously-failing model now invokes correctly AND ZERO strong-model regressions"
    - "IF NOT folded: 'route SEED-034 to v2.8' is recorded as a VALID completion (not a failure), threads.py is NOT touched"
  artifacts:
    - path: ".planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md"
      provides: "SEED-034 Fold-Gate Decision section filled with before/after evidence + the recorded decision"
      contains: "Fold-Gate Decision"
  key_links:
    - from: "scripts/eval_cross_provider.py"
      to: "088-VALIDATION.md SEED-034 Fold-Gate Decision"
      via: "before/after PASS-FAIL evidence recorded"
      pattern: "Fold-Gate Decision"
    - from: "backend/app/api/threads.py:336 SYSTEM_PROMPT (only if folded)"
      to: "universal tool-use directive (text-only)"
      via: "string-literal addition under the Tool selection guide"
      pattern: "ALWAYS"
---

<objective>
Resolve the SEED-034 conditional fold (D-04/D-05/D-06) via measure -> gate -> conditional-apply. 088 MEASURES per-provider tool-use reliability with the Plan 02 eval script, then EITHER folds a UNIVERSAL-ONLY text tuning slice into 088 (if the fold-gate passes) OR records "route SEED-034 architecture to v2.8" — a VALID completion, not a failure.

The fold-gate (D-05), ALL three must hold:
- (a) text-only: the change touches ONLY the SYSTEM_PROMPT string at threads.py:336 and/or the *_TOOL["description"] strings in openai_service.get_tools() — ZERO schema/logic/per-provider/MODEL_CAPABILITIES change (a git diff of only string literals proves it).
- (b) improvement: re-running the eval script shows >=1 previously-failing model now invokes the tool correctly.
- (c) zero regression: every previously-passing (provider x prompt) row still passes.

This plan touches the G-5 hot file backend/app/api/threads.py ONLY in the conditional, text-only, reversible way permitted by the hard constraints (D-05 condition-a; ZERO structural change to the agent loop, no new branches, no per-provider logic). If the gate fails, threads.py is NOT touched at all.

Because the fold is a consequential decision on a G-5 hot file, the apply step is gated behind a checkpoint:decision — the operator confirms fold-and-apply vs route-to-v2.8 after seeing the eval evidence.

Purpose: the ROADMAP-mandated SEED-034 strategic decision, made with evidence (not assumption), provider-docs-first (D-07).
Output: 088-VALIDATION.md SEED-034 Fold-Gate Decision section filled; IF folded, a text-only universal directive in SYSTEM_PROMPT + clarified tool descriptions, re-verified.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-CONTEXT.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-RESEARCH.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md
@.planning/seeds/SEED-034-system-prompt-cross-provider-tool-use.md

# Depends on Plan 02 — read the eval script it produced:
@scripts/eval_cross_provider.py

<interfaces>
<!-- Source-of-truth verified this session. Use directly. -->

THE FOLD TARGET (research correction #1 — CORRECTED vs stale CONTEXT/SEED):
- SYSTEM_PROMPT lives at backend/app/api/threads.py:336 (a string literal: SYSTEM_PROMPT = ( "You are a helpful AI assistant..." )), NOT openai_service.py.
- The active_system_prompt assembly is threads.py:1510-1596 (sent IDENTICALLY to every provider).
- Tool description strings: openai_service.get_tools() at line 761 — the *_TOOL["description"] fields (e.g. WRITE_TODOS_TOOL, SEARCH_DOCUMENTS_TOOL). openai_service.py:738 holds EXPLORER_SYSTEM_PROMPT (a DIFFERENT prompt — not the fold target).
- SYSTEM_PROMPT (threads.py:336-477) already has a "## Tool selection guide" but NO explicit "ALWAYS call X before answering" universal directive for the two measured weak spots.

Provider-docs-first directive CANDIDATES (RESEARCH Code Examples, D-06/D-07 — already cited to official docs):

Candidate A — search-before-answer (insert into SYSTEM_PROMPT under "## Tool selection guide"):
  "- ALWAYS search before answering questions about the user's documents. For ANY question that
   could be answered by the user's uploaded documents or files, you MUST call search_documents
   (or query_documents/query_tables for structural/tabular questions) BEFORE answering. Never
   answer a documents question from prior knowledge — the user's documents are the source of truth."

Candidate B — write_todos for multi-step (insert into SYSTEM_PROMPT):
  "- When the user asks you to track, plan, or work through multiple steps, ALWAYS call write_todos
   to record the task list — do not just narrate the steps in text. The user sees the todo list in
   their workspace panel; a narrated list they cannot see is not tracking."

Candidate C — richer tool description (openai_service.get_tools(), strengthen description TEXT only, NOT schema/args):
  "description": "Record or update the task list for a multi-step request. Call this whenever the
   user asks you to plan, track, or work through several steps so the task list appears in their
   workspace panel. Use it at the START of multi-step work and again to flip a todo's status as you
   complete it. Do NOT use it for single-step requests or to narrate — this tool persists a real,
   user-visible list."

[CITED: Google "Be extremely clear and specific in your descriptions" — ai.google.dev/gemini-api/docs/function-calling; OpenAI "the description matters... Vague descriptions lead to wrong tool selection" — developers.openai.com/api/docs/guides/function-calling; Anthropic "extremely detailed descriptions... by far the most important factor" + "add explicit instructions in a user message" — platform.claude.com/docs/en/agents-and-tools/tool-use/implement-tool-use]

ANTI-PATTERN (out of scope, do NOT do): forcing tool use via tool_choice: any/tool — all three providers' docs say this is per-call architecture; it risks regressing strong models (changes their natural narration) and is NOT universal-text-only (RESEARCH Anti-Patterns).

Fold-gate recommended concrete thresholds (D-05, Claude's discretion — RESEARCH Code Examples):
- (a) text-only: a git diff touching ONLY SYSTEM_PROMPT string content + *_TOOL["description"] strings. ZERO changes to argument schemas, get_tools() logic, any *_service.py adapter, any per-provider branch, MODEL_CAPABILITIES.
- (b) improvement: >=1 model that FAILED a canonical-prompt assertion before now PASSES (measured by messages.tool_calls + DB rows).
- (c) zero regression: EVERY (provider x prompt) that PASSED before still PASSES (strong callers gpt-5.4-mini, claude-haiku/opus, deepseek, kimi, gemini-3.5-flash show no new failures).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the eval BASELINE across all 4 providers + record evidence in VALIDATION.md</name>
  <read_first>
    - scripts/eval_cross_provider.py (the Plan 02 script — its CLI + output format)
    - .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md (the "SEED-034 Fold-Gate Decision" section to fill, lines 99-106)
    - .planning/seeds/SEED-034-system-prompt-cross-provider-tool-use.md (the SEED-034 evidence: gemini-2.5 narrate-without-calling, weak-model answer-from-training — the failing cases to look for)
    - 088-RESEARCH.md Code Examples (the N=4 prompts + the gate thresholds) + Assumption A3 (the failing cases must still reproduce at 088 time, else the gate self-routes to v2.8)
  </read_first>
  <action>
Establish the empirical baseline (D-02 measurement, the fold-gate's "before").

PREREQUISITE: the operator must have the local backend (uvicorn) running and OpenAI/Anthropic/Google(3.x)/OpenRouter keys configured. The eval script is localhost-hard-gated (Plan 02) — confirm it refuses cloud.

1. Run the eval script across all 4 providers (representative models per D-01/D-03: OpenAI gpt-5.4-mini, Anthropic claude-haiku-4-5, Google gemini-3.5-flash, OpenRouter z-ai/glm-5.1):
   python scripts/eval_cross_provider.py
   Capture the per-row PASS/FAIL output (provider x canonical-prompt x {invoked, arg_shape, persisted}).

2. Record this BASELINE table verbatim in 088-VALIDATION.md under the "SEED-034 Fold-Gate Decision" section as the "Before" evidence. Note which rows FAIL (e.g. a model answering a doc question without search_documents, or narrating todos without write_todos — the SEED-034 evidence cases).

3. Evaluate gate condition-b feasibility: is there >=1 FAILING row that a universal text directive (Candidate A/B/C) could plausibly fix?
   - If ZERO rows fail (every provider already passes all canonical prompts) -> condition-b CANNOT be met by a prompt change -> the gate self-routes SEED-034 to v2.8 (RESEARCH Assumption A3 — this is the designed self-correcting outcome, NOT a phase failure). Record this and SKIP the apply (Task 3 becomes "record route-to-v2.8").
   - If >=1 row fails -> proceed to the checkpoint (Task 2) with the candidate directive(s) targeting the failing capability.

Optionally also capture the observe-run.py-style Redis/LangSmith evidence for a failing row (richer provenance). gemini-2.5 is NOT gated (D-03) — if recorded, mark it "known-degraded — not gating".
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -c "Fold-Gate Decision" .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 088-VALIDATION.md "SEED-034 Fold-Gate Decision" section contains a BASELINE per-row PASS/FAIL table across all 4 providers (the "Before" evidence)
    - The section explicitly states whether >=1 row FAILED (the condition-b precondition) or all passed (-> self-route to v2.8)
    - gemini-2.5, if present, is marked "known-degraded — not gating" (D-03)
    - The eval was run against localhost (the script's hard-gate confirmed)
  </acceptance_criteria>
  <done>Baseline eval recorded in VALIDATION.md with explicit identification of failing rows (or none); the condition-b precondition is decided on evidence.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CHECKPOINT — fold-and-apply vs route-to-v2.8</name>
  <action>Present the Task-1 baseline eval evidence to the operator and pause for a binding decision: fold the universal-only text tuning slice into 088 now (proceed to Task 3 apply) OR route the SEED-034 architecture to v2.8 (Task 3 records the route). Do NOT touch threads.py before this decision is returned. Capture the chosen branch + (if folding) which candidate directive(s) A/B/C.</action>
  <decision>Fold a universal-only text tuning slice into 088 now, OR route the SEED-034 architecture to v2.8?</decision>
  <context>
This decision touches the G-5 hot file backend/app/api/threads.py (9+ phases) — even a text-only change to SYSTEM_PROMPT@:336 warrants explicit operator confirmation, and a shared-path change must not regress any working provider ([[feedback_no_cross_provider_regressions]]). The Task-1 baseline evidence is now in 088-VALIDATION.md. The fold is CONDITIONAL (D-04/D-05): it proceeds ONLY if the operator agrees a universal text directive can plausibly fix >=1 failing row without risking strong-model regression. Routing to v2.8 is an EQUALLY VALID outcome (D-08 — the eval script is the v2.8 harness seed).
  </context>
  <options>
    <option id="fold-and-apply">
      <name>Fold: apply the universal-only text directive + re-verify (Task 3)</name>
      <pros>Cheap reliability lift for weak/new models; closes the SEED-034 088 measurement loop with a shipped improvement; uses already-cited provider-docs-first candidates.</pros>
      <cons>Touches the threads.py G-5 hot file (text-only, reversible); requires a clean re-run proving >=1 improvement + zero regression before it can stay.</cons>
    </option>
    <option id="route-to-v2.8">
      <name>Do NOT fold: record "route SEED-034 architecture to v2.8"</name>
      <pros>Zero risk to the shared path at milestone close; honors D-16 (don't let close-out become open-ended); the eval script already seeds v2.8 (D-08); a VALID completion.</pros>
      <cons>Weak/new-model tool-use reliability stays as-is until v2.8.</cons>
    </option>
  </options>
  <resume-signal>Select: fold-and-apply OR route-to-v2.8 (state which, and which candidate directive(s) A/B/C if folding)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Conditionally apply the universal text directive + re-run the gate (or record route-to-v2.8)</name>
  <read_first>
    - backend/app/api/threads.py (lines 336-477 — the SYSTEM_PROMPT string + its "## Tool selection guide" section; ONLY edit string content here)
    - backend/app/services/openai_service.py (around line 761 — get_tools(); ONLY edit *_TOOL["description"] string content)
    - scripts/eval_cross_provider.py (to re-run the gate with --provider/--prompt for the affected cells)
    - 088-RESEARCH.md Code Examples (candidates A/B/C + the thresholds) + Anti-Patterns (no tool_choice forcing)
  </read_first>
  <action>
Branch on the Task-2 decision.

IF route-to-v2.8: Do NOT touch threads.py or openai_service.py. In 088-VALIDATION.md, record the decision: "DO NOT FOLD — route SEED-034 architecture to v2.8" with the rationale (which gate condition could not be met — e.g. no failing row reproduced, or the operator judged regression risk too high at milestone close). State that the eval script (Plan 02) is the v2.8 harness seed (D-08). This is a VALID completion. STOP here.

IF fold-and-apply: Apply the chosen candidate(s), TEXT-ONLY (D-05 condition-a, RESEARCH Code Examples):
1. Edit ONLY string literals:
   - SYSTEM_PROMPT at threads.py:336 — insert Candidate A and/or B under the "## Tool selection guide" section (string content only).
   - and/or openai_service.get_tools() — strengthen the relevant *_TOOL["description"] string (Candidate C) — NOT the schema, NOT args, NOT get_tools() logic.
   - ZERO changes to: argument schemas, get_tools() control flow, any *_service.py adapter, any per-provider branch, MODEL_CAPABILITIES. NO tool_choice forcing.
2. Prove text-only: run git diff on backend/app/api/threads.py backend/app/services/openai_service.py and confirm the diff is exclusively within string literals. Paste the diff (or its shape) into VALIDATION.md as the condition-a evidence.
3. Restart the backend (operator: scripts/restart-backend.ps1) so the new prompt is live, then RE-RUN the eval script across all 4 providers:
   python scripts/eval_cross_provider.py
4. Evaluate the gate against the baseline:
   - condition-b: >=1 previously-FAILING row now PASSES?
   - condition-c: EVERY previously-PASSING row still PASSES (zero strong-model regression)?
5. Record the AFTER table + the verdict in VALIDATION.md:
   - If (a)+(b)+(c) ALL hold -> KEEP the change; record "FOLDED" with the before/after evidence + the git-diff proof.
   - If condition-b fails (no improvement) OR condition-c fails (a strong row regressed) -> REVERT the text change (it's reversible — restore the original strings), and record "DID NOT FOLD — route to v2.8" with which condition failed. The revert is mandatory: a change that doesn't improve or that regresses MUST NOT ship.

Either branch ends with the SEED-034 Fold-Gate Decision section in VALIDATION.md fully recorded with evidence. If folded, the change must remain TEXT-ONLY and must not break the cross-provider scoreboard (Plan 05 re-confirms live).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -Eiq "FOLDED|DO NOT FOLD|route .*v2.8" .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md && echo "decision recorded"</automated>
  </verify>
  <acceptance_criteria>
    - 088-VALIDATION.md SEED-034 Fold-Gate Decision section records an explicit verdict ("FOLDED" with before/after evidence, OR "DO NOT FOLD — route to v2.8" with the failing condition)
    - IF folded: git diff of backend/app/api/threads.py + backend/app/services/openai_service.py is confined to string literals (no schema/logic/per-provider/MODEL_CAPABILITIES change); the AFTER eval table shows >=1 improved row AND zero strong-model regressions
    - IF folded: backend/app/api/threads.py contains the universal directive text (e.g. "ALWAYS") under the Tool selection guide; NO tool_choice forcing introduced
    - IF NOT folded: backend/app/api/threads.py and backend/app/services/openai_service.py are UNCHANGED from baseline (git diff empty for those files)
  </acceptance_criteria>
  <done>SEED-034 resolved with evidence: either a text-only universal directive folded + re-verified (>=1 improvement, zero regression, git-diff-proven text-only), or "route to v2.8" recorded as a valid completion with threads.py untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval re-run -> live LLM providers + DB | The before/after gate runs real agent loops writing test data |
| prompt-string edit -> shared agent loop (all providers) | A SYSTEM_PROMPT/tool-description change affects every provider identically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-088-04-01 | Tampering (regression of a working provider) | SYSTEM_PROMPT@threads.py:336 shared-path text change | mitigate | Gate condition-c REQUIRES zero strong-model regression (every previously-passing row still passes), measured by the eval re-run; if any strong row regresses the change is REVERTED. No per-provider branching ([[feedback_no_cross_provider_regressions]]). |
| T-088-04-02 | Elevation / scope creep on a G-5 hot file | threads.py structural change beyond text | mitigate | Gate condition-a REQUIRES a git diff confined to string literals; acceptance criteria assert no schema/logic/per-provider/MODEL_CAPABILITIES change; checkpoint:decision forces operator confirmation before any threads.py touch. |
| T-088-04-03 | Tampering / DoS | eval re-run against the wrong DB | mitigate | The eval script (Plan 02) is localhost-hard-gated; this plan only re-runs it (inherits the gate). |
| T-088-04-04 | Information disclosure | secrets in eval output pasted into VALIDATION.md | mitigate | Record PASS/FAIL rows + the git-diff shape only — never paste env values or API keys ([[feedback_env_secrets_handling]]); the eval script already prints presence/absence only. |
</threat_model>

<verification>
- 088-VALIDATION.md SEED-034 Fold-Gate Decision section has a baseline table + an explicit fold/no-fold verdict with evidence.
- IF folded: git diff of threads.py + openai_service.py is string-literal-only; AFTER eval shows >=1 improvement + zero regression.
- IF NOT folded: threads.py + openai_service.py unchanged (git diff empty for those files); route-to-v2.8 recorded.
- The checkpoint:decision was honored (operator confirmed the branch).
- No secrets/keys pasted into VALIDATION.md.
</verification>

<success_criteria>
- The ROADMAP-mandated SEED-034 decision is made on EVIDENCE (eval before/after), provider-docs-first (D-07), not assumption.
- Any fold is UNIVERSAL-ONLY (D-06), text-only, reversible, with zero strong-model regression (D-05 condition-c) and >=1 measured improvement (condition-b).
- The G-5 threads.py hot file is touched only in the permitted conditional/text-only way, or not at all.
- Route-to-v2.8 is treated as a valid completion (the eval script seeds the v2.8 harness, D-08).
</success_criteria>

<output>
After completion, create `.planning/phases/088-cross-cutting-verification-accessibility/088-04-SUMMARY.md`
</output>
