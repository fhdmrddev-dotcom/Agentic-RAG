---
phase: 251-register-integrity
plan: 03
type: execute
wave: 3
depends_on: ["251-02"]
files_modified:
  - .planning/seeds/
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [REG-01]
user_setup: []

must_haves:
  truths:
    - "D-05: the younger seed of each of the 8 pairs carries a fresh id (277-284) and the old id carries a REDIRECT STUB at `status: superseded-id` naming BOTH resolutions, so any historical reference lands somewhere that disambiguates."
    - "D-07: the OLDEST seed keeps the id, by date — deterministic, needing no judgement, and matching what every archived reference written before the collision can only have meant."
    - "D-20: the date rule reads `created` IF PRESENT, ELSE `planted` — 5 of the 8 pairs carry only `planted:` — and the two same-day ties (231, 253) break on the git ADD-COMMIT timestamp, derived rather than judged."
    - "D-06: ⛔ the 378+ references inside sealed `.planning/milestones/` are NOT rewritten; the stub is what keeps them followable. Live references in `.planning/`, `docs/` and `CLAUDE.md` ARE updated."
    - "D-17: NO product source file is touched. The 33 files in `backend/`, `frontend/` and `scripts/` that reference a moved id are DELIBERATELY left pointing at the stub, recorded in writing, and the affected stubs are worded to serve a developer reading a code comment."
    - "REG-01: no two seeds share an id — a reference by id resolves to exactly one thing, and the gate's `[duplicate-id]` arm reads 0."
  artifacts:
    - path: ".planning/seeds/"
      provides: "8 renamed seeds at ids 277-284 and 8 redirect stubs at the original ids"
      contains: "superseded-id"
    - path: ".planning/phases/251-register-integrity/251-RENUMBER-LEDGER.md"
      provides: "the derived keep/move verdict for all 8 pairs with both dates, the tie-breaks, and the 33 files left on a stub"
  key_links:
    - from: ".planning/seeds/SEED-0NN-<old>.md (stub)"
      to: "both resolutions"
      via: "renumbered_from / renumbered_because and an explicit two-way body"
      pattern: "superseded-id"
    - from: "scripts/check-seeds-register.cjs"
      to: "the 8 stubs"
      via: "the duplicate-id carve-out: a group with exactly one superseded-id member is a resolution, not a duplicate"
      pattern: "duplicate-id"
---

<objective>
Make a reference by id resolve to exactly one thing — REG-01's whole requirement — without rewriting
one byte of history and without putting a single product source file in the blast radius.

Purpose: 8 ids each name two distinct seeds, so every citation of them is ambiguous. D-07 resolves
which side moves by a rule that needs no judgement; D-05's stub is what keeps the 378 archived
references and the 33 product-source references followable after the move.

Output: 8 renamed seeds at 277-284, 8 redirect stubs, live references updated within the permitted
file set, and a written record of every reference deliberately left on a stub.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/251-register-integrity/251-CONTEXT.md
@.planning/phases/251-register-integrity/251-RESEARCH.md
@.planning/phases/251-register-integrity/251-PATTERNS.md
@.planning/phases/251-register-integrity/251-GATE-BASELINE.md
@.planning/seeds/TEMPLATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the 8 verdicts, then rename the 8 movers to 277-284</name>
  <files>.planning/seeds/, .planning/phases/251-register-integrity/251-RENUMBER-LEDGER.md</files>
  <read_first>
    - `251-RESEARCH.md` §2.6 — the full 8-pair table with both dates, both git add-commit timestamps
      and the two tie-breaks, plus the three problems with D-07 as written. ⚠ This is the EXPECTED
      result, not the authority: re-derive it and stop if the derivation disagrees.
    - `251-CONTEXT.md` D-07 and D-20 — the rule and its amendment, verbatim.
    - `251-PATTERNS.md` §4 — `SEED-224:1-15`, the one prior renumber done well, with
      `renumbered_from` / `renumbered_because` already in the register's vocabulary; and
      `SEED-068:1-3`, the bad precedent (an inline `#` comment inside the `seed_id` value) so the
      plan can say what it is not doing.
    - `251-PATTERNS.md` § No Analog Found row 3 — `check-gap-closure-rounds.cjs:84-88, 189` for the
      `git()` spawnSync shape. ⛔ `--date=iso`, never `--date=short`: two pairs tie on the DAY.
    - `.planning/seeds/TEMPLATE.md` — the contract the renamed files and the stubs must satisfy.
  </read_first>
  <action>
    Derive, do not transcribe. For each of the 8 duplicate ids read both members' `created`-else-
    `planted` value from the MIGRATED frontmatter and compare. Where the dates are equal, and only
    then, break the tie with `git log --diff-filter=A --format=%ad --date=iso -1 --` on each file.
    Record in the ledger, per pair: both filenames, both dates, which key supplied each date, the
    verdict, and — for the two ties — both ISO timestamps and the delta. ⚠ State in the ledger that
    the FRONTMATTER date is authoritative and git is the tie-break only; they disagree on two pairs
    (`SEED-022-timeout-settings-ui` reads 2026-05-25 but was added 2026-05-24; both the 228 and 229
    movers read 2026-08-31 but were added 2026-09-01) without flipping a verdict, and the script must
    not pick silently.

    Compare the derivation against RESEARCH §2.6's table. If any verdict disagrees, ⛔ STOP and
    report rather than proceeding — a rule that produces a different answer on two runs is not the
    deterministic rule D-07 was chosen for.

    Assign fresh ids in ascending order of the OLD id, so the mapping is itself derivable:
    022→277, 092→278, 228→279, 229→280, 231→281, 253→282, 259→283, 269→284. Record the rule, not
    just the result. ⚠ Re-derive the highest existing id first (276 at planning time) and start from
    `max + 1`; if the register has gained a seed since, shift the whole block and say so.

    `git mv` each mover to its new filename, preserving the slug exactly. Inside each moved file
    update `seed_id` to the new value and add `renumbered_from` (the old `SEED-NNN`) and
    `renumbered_because` — a folded scalar citing the date that decided it, and for 231 and 253 the
    git tie-break with both timestamps. ⚠ Copy `SEED-224`'s KEYS, not its reasoning:
    `SEED-224`'s own `renumbered_because` chose the mover by reference weight, which D-07 rejects by
    name. The new text must cite the DATE.

    ⚠ Note for the executor: `SEED-231-nobody-is-told-an-approval-is-waiting.md` is the one seed in
    the register that already carried a `status_note` before Plan 02. It is a mover. Leave its
    `status_note` untouched.

    Also record, as an observation beside the rule and never as a reason to depart from it (CONTEXT
    asks for this explicitly): `SEED-253` is the heaviest pair at ~53 referencing files, and D-07
    hands the id to `mobile-has-no-drawer-trigger…`, which is referenced essentially nowhere outside
    its own file and the archives, while the mover is what every live reference means.
  </action>
  <acceptance_criteria>
    - `.planning/seeds/` contains exactly 8 files matching `SEED-2(77|78|79|80|81|82|83|84)-*.md`,
      and each carries `renumbered_from` naming its old id and a `renumbered_because` whose text
      contains the deciding date.
    - The derived verdict table matches RESEARCH §2.6 on all 8 pairs; any disagreement is reported
      and the task stops.
    - `ls .planning/seeds/ | sed -n 's/^SEED-\([0-9]\{3\}\).*/\1/p' | sort | uniq -c | awk '$1>1'`
      prints nothing BEFORE the stubs are written (the 8 collisions are gone by renaming alone).
    - `git log --follow` resolves each moved file's history — the moves are `git mv`, not
      delete-and-create.
    - `251-RENUMBER-LEDGER.md` records both dates and the supplying key for all 16 files, and both
      ISO timestamps with deltas for the two ties (`SEED-231`: 63 minutes; `SEED-253`: 13h 24m).
    - `grep -n "renumbered_because" .planning/seeds/SEED-28*.md` shows no text choosing a side by
      reference count.
  </acceptance_criteria>
  <verify>
    <automated>ls .planning/seeds/ | sed -n 's/^SEED-\([0-9]\{3\}\).*/\1/p' | sort | uniq -c | awk '$1>1' | wc -l</automated>
  </verify>
  <done>Eight movers carry fresh ids derived by a rule that reproduces, each recording which date
  decided it, and the register has no filename-level collision.</done>
</task>

<task type="auto">
  <name>Task 2: Eight redirect stubs — and three of them are read inside product code</name>
  <files>.planning/seeds/</files>
  <read_first>
    - `251-CONTEXT.md` D-05 (the stub's purpose and that the gate treats it as a resolution) and
      D-17 (the stub carries the product-source references; its wording must serve a developer
      reading a code comment).
    - `251-PATTERNS.md` §4 — the `renumbered_from`/`renumbered_because` vocabulary and D-17's
      wording obligation restated with the nine backend modules and five test files that will point
      at the `SEED-253` stub.
    - `.planning/seeds/TEMPLATE.md` — the stubs are the FIRST new seeds written against it, which
      is a free end-to-end check of the template.
    - `scripts/check-seeds-register.cjs` — the `[duplicate-id]` carve-out it implements: a group with
      exactly one `superseded-id` member is a resolution, not a duplicate.
    - `251-RESEARCH.md` §1.8 and §2.7 — the per-file reference concentrations.
  </read_first>
  <action>
    Write 8 stub files at the ORIGINAL ids, named `SEED-0NN-superseded-id.md` (or the original slug
    of neither side — pick one shape and use it for all 8 so the set is recognisable). Each stub
    carries the full D-09 contract: `seed_id` matching its filename, a `title` that reads as a
    disambiguation, `status: superseded-id`, `surface: Agentic-RAG`, `trigger_when: unset`, and
    `renumbered_from`/`renumbered_because` inverted to point FORWARD — plus an explicit body.

    The body is the deliverable, not the frontmatter. It must, in plain sentences: state that this id
    was claimed by two distinct seeds; name BOTH resolutions by full current filename and one-line
    summary; say which date decided which side kept the id; and say plainly WHICH of the two a reader
    arriving from a given kind of reference most likely means. ⛔ A stub that only says "this id
    moved" fails its whole purpose — it is read by someone who has just followed a citation and needs
    to know which of two things was meant.

    ⚠ **THREE of the eight are read inside product code, not one.** D-17 names `SEED-253`; two more
    were measured at planning and must get the same treatment:

    - `SEED-253` — every live `backend/`/`frontend/` reference means the MOVER
      (`source-file-path-is-synthetic-no-adapter-populates-it`, now 282). 25+ references across
      `microsoft_graph.py`, `mcp_source.py`, `preview_service.py`, `watch_service.py`, `base.py`,
      `import_service.py`, `ingest_enrich.py`, `mock_source.py`, `mailbox.py` and five backend test
      files. The stub must say so in one sentence a developer can act on.
    - `SEED-229` — `scripts/vitest-count-gate.cjs` carries 3 references at lines 2917, 3100 and 4937,
      and all three mean the MOVER (`five-suites-in-neither-count-gate-knob`, now 280), not the
      keeper (`does-the-golden-run-hang…`). ⛔ **This extends D-17's class beyond the one id it
      names**, and it was found at planning, not at discuss-phase — record it as such.
    - `SEED-231` — `frontend/src/components/layout/attentionConditions.ts` carries 3 references at
      lines 11, 104 and 145, and all three name the MOVER by its own title
      (*nobody is told an approval is waiting*, now 281).

    ⭐ `SEED-259` is the harmless one and the plan should say so: D-07 keeps the id on the seed the
    source code actually means, so `test_259_argument_shapes_are_rows_too.py` — a FILENAME carrying
    the id — stays correct and needs no change.

    ⛔ Do not edit any file under `backend/`, `frontend/` or `scripts/` in this task or any other in
    this plan. All 84 such references are comments or test docstrings; none is an executing
    identifier. They stay on the stub by decision, and the decision is written down.
  </action>
  <acceptance_criteria>
    - Exactly 8 files exist with `status: superseded-id`, one per original duplicate id, and each
      names BOTH resolutions by their full current filenames in its body.
    - Each of the `SEED-229`, `SEED-231` and `SEED-253` stubs contains a sentence naming the
      file-kind that will arrive there (a `scripts/` gate comment, a frontend layout module, the
      backend source adapters respectively) and stating which resolution that reader means.
    - `node scripts/check-seeds-register.cjs` reports `0` `[duplicate-id]` findings — the carve-out
      recognises a group whose second member is a stub.
    - `node scripts/check-seeds-register.cjs` reports `0` `[missing-key]` and `0`
      `[unknown-status]`: the stubs satisfy the same contract every other seed does.
    - `git diff --name-only backend/ frontend/ scripts/` is EMPTY for this plan.
    - `git diff --name-only .planning/milestones/` is EMPTY — archives are not rewritten (D-06).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-seeds-register.cjs; echo "exit=$?"; git diff --name-only backend/ frontend/ scripts/ | wc -l</automated>
  </verify>
  <done>Every one of the 8 old ids resolves to a stub that disambiguates rather than redirects
  blindly, three of them worded for a developer arriving from code, and no product file moved.</done>
</task>

<task type="auto">
  <name>Task 3: Live references updated inside the permitted set — and the 33 left on a stub, in writing</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/, .planning/seeds/, docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - `251-CONTEXT.md` D-06 — the permitted live file set, named: `.planning/seeds/`,
      `.planning/ROADMAP.md`, `REQUIREMENTS.md`, `.planning/phases/`, `CLAUDE.md`, `docs/`.
      ⛔ `.planning/milestones/` is sealed.
    - `251-RESEARCH.md` §1.7 — the measured totals (144 files / 559 occurrences; 403 in archives;
      156 live across 54 files) and ⚠ §8.11: `rg` skips dot-directories without `--hidden`, which
      produced a confidently wrong zero during research. Every scan in this task needs the flag.
    - `251-RESEARCH.md` §2.7 — the live `SEED-253` reference sites already enumerated:
      `CLAUDE.md:717`, `docs/HOT-FILE-LEDGER.md` ×4, `247-CONTEXT.md` ×2, `.continue-here.md` ×3.
    - `CLAUDE.md` § "CLAUDE.md context budget" — the 200-char ledger disposition cap and the rule to
      measure with `check-claude-md-size.cjs`, never `wc`.
  </read_first>
  <action>
    Enumerate every live reference with `rg --hidden -n 'SEED-(022|092|228|229|231|253|259|269)'`
    restricted to the permitted set and excluding `.planning/milestones/`, `node_modules`,
    `backend/venv` and `__pycache__`. ⛔ Without `--hidden` this returns a confident, wrong,
    near-zero.

    For each hit, decide by READING which of the two seeds it means, then rewrite the id only when it
    means the MOVER. A reference that means the keeper is already correct and must be left
    byte-unchanged — a blanket find-and-replace would silently re-point half of them at the wrong
    seed and is forbidden here.

    Where a reference is genuinely ambiguous from its context, leave it pointing at the stub and list
    it in the ledger rather than guessing.

    ⚠ `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md` both carry `SEED-253` inside hot-file rows. CLAUDE.md
    rows have a hard 200-character disposition cap enforced by `check-claude-md-size.cjs`; a
    three-digit id becoming a three-digit id is length-neutral, but re-run the gate anyway and
    record the reading.

    Finally, extend `251-RENUMBER-LEDGER.md` with the deliberate-non-edit record: the count and the
    full list of files under `backend/`, `frontend/` and `scripts/` that reference a moved id, each
    with the id it means and the stub it now lands on. State in one sentence that this is D-17's
    ruling being executed, not an oversight, and that all of them are comments or test docstrings
    with no executing identifier among them.
  </action>
  <acceptance_criteria>
    - `rg --hidden -c 'SEED-(022|092|228|229|231|253|259|269)' .planning/milestones/` returns the
      SAME per-file counts as before this plan — archives byte-unchanged, proven by
      `git diff --name-only .planning/milestones/` being empty.
    - Every live reference to a MOVED seed inside the permitted set now names its new id; verified by
      re-running the enumeration and reading each remaining old-id hit to confirm it means the
      keeper or is a deliberate stub landing.
    - `251-RENUMBER-LEDGER.md` lists the ~33 `backend/`/`frontend/`/`scripts/` files left on a stub,
      each with the id it means, and states D-17 as the ruling.
    - `node scripts/check-claude-md-size.cjs` exits `0`; the char count is recorded in the SUMMARY.
    - `node scripts/check-seeds-register.cjs` exits `0` — the FIRST fully green reading of the
      register in this phase, with the verdict line's ratio recorded verbatim.
    - `git diff --name-only backend/ frontend/ scripts/` is EMPTY for the whole plan.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-seeds-register.cjs; echo "gate exit=$?"; node scripts/check-claude-md-size.cjs; git diff --name-only backend/ frontend/ scripts/ .planning/milestones/ | wc -l</automated>
  </verify>
  <done>Live citations resolve to the seed they mean, archives are untouched, and every reference
  deliberately left on a stub is listed by file with the reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renumber → historical record | 403 archived references were written when the id meant something specific; changing them would edit history |
| renumber → product source | 84 references in `backend/`, `frontend/` and `scripts/` sit outside the phase boundary and cannot be corrected |
| stub → future reader | the stub is the only thing standing between a citation and an ambiguous answer, forever |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-251-14 | Tampering | `.planning/milestones/` (sealed archives) | mitigate | D-06 forbids the rewrite; `git diff --name-only .planning/milestones/` asserted EMPTY as an acceptance criterion in Task 3 |
| T-251-15 | Spoofing | a blanket find-and-replace re-pointing keeper references at the mover | mitigate | every hit is READ and decided individually; a reference meaning the keeper is left byte-unchanged. ⛔ No `sed -i` over the id set appears in any task |
| T-251-16 | Repudiation | 33 product-source references silently wrong after the move | mitigate | D-17 is executed as a WRITTEN record in `251-RENUMBER-LEDGER.md` listing every file and the id it means; three stubs (253, 229, 231) are worded for a developer arriving from code. ⚠ Two of those three were found at PLANNING, extending D-17 beyond the single id it names |
| T-251-17 | Tampering | git history of the 8 moved files | mitigate | `git mv`, asserted by `git log --follow` resolving — a delete-and-create would sever every file's history at the rename |
| T-251-18 | Denial of service | the gate reading the 8 stubs as 8 new duplicates | mitigate | the `[duplicate-id]` carve-out (one `superseded-id` member = a resolution) ships in Plan 01 and is asserted here by a `0` finding count |
| T-251-19 | Tampering | CLAUDE.md ledger rows crossing the 200-char disposition cap | mitigate | `node scripts/check-claude-md-size.cjs` is an acceptance criterion; the edit is length-neutral by construction (a 3-digit id for a 3-digit id) |
| T-251-SC | Tampering | npm/pip/cargo installs | mitigate | zero install tasks in this plan; no dependency is added or changed |
</threat_model>

<verification>
1. `ls .planning/seeds/ | sed -n 's/^SEED-\([0-9]\{3\}\).*/\1/p' | sort | uniq -c | awk '$1>1'` →
   the 8 collisions resolve to stub pairs only, and the gate's `[duplicate-id]` count is 0.
2. `node scripts/check-seeds-register.cjs` → exit `0`, the first fully green register reading.
3. `git diff --name-only backend/ frontend/ scripts/ .planning/milestones/` → EMPTY.
4. `git log --follow` on each of the 8 renamed files resolves through the rename.
5. `node scripts/check-claude-md-size.cjs` → exit 0, reading recorded.
6. `node scripts/check-hot-file-ledger.cjs 251` → re-run and record; this plan's `files_modified`
   contains no product source, so a `[no-row]` reading is a finding about the gate's scan set.
</verification>

<success_criteria>
- No two seeds share an id; each of the 8 old ids resolves to exactly one stub that names both
  resolutions (REG-01, D-05, D-07, D-20).
- Archives are byte-unchanged and no product file is touched (D-06, D-17).
- The 33 deliberately-unfixed references are listed by file with the id each means, and the three
  stubs they land on are worded for a developer reading a code comment.
- `check-seeds-register.cjs` exits 0 over the whole register.
</success_criteria>

<output>
Create `.planning/phases/251-register-integrity/251-03-SUMMARY.md` when done, carrying the 8-pair
verdict table with both dates and both tie-break timestamps, and the deliberate-non-edit list.
</output>
