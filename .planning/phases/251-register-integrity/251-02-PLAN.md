---
phase: 251-register-integrity
plan: 02
type: execute
wave: 2
depends_on: ["251-01"]
files_modified:
  - scripts/migrate-seeds-frontmatter.cjs
  - .planning/seeds/
  - .planning/seeds/TEMPLATE.md
  - .claude/get-shit-done/workflows/plant-seed.md
  - CLAUDE.md
autonomous: true
requirements: [REG-01, REG-02]
user_setup: []

must_haves:
  truths:
    - "D-09: all 283 seeds carry the required-key contract — `seed_id`, `title`, `status`, `surface`, `trigger_when` — so the sweep can never again be blind to half its own input."
    - "D-10: `status` is a bare enum token and every byte of discarded prose lives in `status_note`, verbatim. Nothing is lost."
    - "D-16: `partially-folded` becomes `status: folded` + `partial: true`; the 12 orphan tokens each have an individually listed mapping in this plan and none is silently coerced."
    - "D-11: every seed's BODY is md5-identical before and after, hashed over RAW Buffers — not over normalised text, and NOT verified with `git diff`, which cannot see the change."
    - "D-02: the 126 seeds with no trigger carry `trigger_when: unset` so they are visible as unswept rather than silently absent."
    - "D-18: the backfill is MECHANICAL ONLY — `trigger_paths` from paths the prose already names, `trigger_surfaces` from a small controlled enum — and the gate's two unswept figures are both printed and both honest."
    - "D-01: structured trigger keys land BESIDE `trigger_when`, never replacing it — the prose explanation survives for every one of the 157 seeds that has one."
    - "D-08 (allocator half): `/gsd:capture --seed` derives the next id as `max(id) + 1` with `10#` base-forcing, not `count(files) + 1` — which emits SEED-284 today when the highest id is 276."
    - "`.planning/seeds/TEMPLATE.md` EXISTS, carries the enum in a trailing comment on the key's own line, and agrees byte-for-byte with the gate's `STATUS_ENUM` and `REQUIRED_KEYS`."
    - "`plant-seed.md`'s de-facto template writes the same contract, so the very next `/gsd:capture --seed` does not re-introduce the defect this migration just removed."
    - "CLAUDE.md's seeds cross-check no longer instructs a filter that sees 128 of 283, and the correction sits BESIDE the original, never over it."
  artifacts:
    - path: "scripts/migrate-seeds-frontmatter.cjs"
      provides: "the one-shot, dry-run-default, idempotent frontmatter migration; the only file-writing .cjs in scripts/"
      contains: "createHash"
    - path: ".planning/seeds/TEMPLATE.md"
      provides: "the frontmatter contract's first written home in this register's life"
      contains: "status:"
  key_links:
    - from: "scripts/migrate-seeds-frontmatter.cjs"
      to: ".planning/seeds/*.md"
      via: "Buffer read, frontmatter-only rewrite, body Buffer passed through untouched"
      pattern: "readFileSync"
    - from: ".planning/seeds/TEMPLATE.md"
      to: "scripts/check-seeds-register.cjs"
      via: "the same REQUIRED_KEYS and STATUS_ENUM values (S-5 same-commit sync rule)"
      pattern: "superseded-id"
---

<objective>
Give the register the frontmatter contract it has never had, across all 283 files, in one pass that
proves it touched nothing but the frontmatter — and write that contract down in the three places that
would otherwise undo it on the next seed authored.

Purpose: D-09's own finding is that `.planning/seeds/TEMPLATE.md` does not exist, which is a
sufficient explanation on its own for 40+ `status` spellings and 106 seeds with no `seed_id`. This is
not tightening a loose rule; it is writing the first one.

Output: a committed, re-runnable migration script; 283 normalised seeds with byte-identical bodies;
`TEMPLATE.md`; a corrected `plant-seed.md`; a corrected CLAUDE.md sweep rule.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/251-register-integrity/251-CONTEXT.md
@.planning/phases/251-register-integrity/251-RESEARCH.md
@.planning/phases/251-register-integrity/251-PATTERNS.md
@.planning/phases/251-register-integrity/251-GATE-BASELINE.md
</context>

<interfaces>
<!-- The contract, fixed here, consumed by Plan 03's stubs and Plan 04's wiring. -->

REQUIRED_KEYS (D-09):  seed_id · title · status · surface · trigger_when
STATUS_ENUM  (D-10/D-16, 10 values):
  planted · dormant · open · partially-answered · answered · folded · shipped · closed ·
  deferred · superseded-id
Sibling keys:  partial (boolean, D-16) · status_note (free prose, D-10) ·
  trigger_paths (globs, D-01/D-18) · trigger_surfaces (controlled enum, D-18) ·
  renumbered_from / renumbered_because (D-05, Plan 03 only)

⛔ trigger_phase_touches is NOT part of the contract (D-18).

These are the SAME values `scripts/check-seeds-register.cjs` already enforces. S-5 same-commit sync
rule: gate enum ↔ TEMPLATE.md enum comment ↔ plant-seed.md's emitted block are ONE triple.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The migration script — Buffer pass-through, md5 body invariant, dry-run by default</name>
  <files>scripts/migrate-seeds-frontmatter.cjs</files>
  <read_first>
    - `251-PATTERNS.md` §2a — the measured fact that NO `.cjs` in `scripts/` writes a file
      (`grep -ln "writeFileSync" scripts/*.cjs` → zero), so this is the first, and why it must not
      be named `check-*`.
    - `251-PATTERNS.md` §2b — `scripts/repair_dirty_workflow_phases.py:1-28` and `:87-149`, quoted:
      the five properties to carry (dry-run default · explicit `--apply` · scoped predicate ·
      idempotent · read-back assertion before the commit).
    - `251-PATTERNS.md` §2c — `scripts/agent-bus.sh:150-155`, the md5-before/md5-after gate on a
      write claim, and `:145-147`'s sentence: *gate the success line on the write, not on reach*.
    - `251-PATTERNS.md` §2d — the raw-Buffer requirement in full, the 6 mixed-line-ending files and
      the 5 no-frontmatter files, both named.
    - `scripts/check-seeds-register.cjs` (Plan 01, whole file) — reuse its `frontmatter` boundary
      regex and `readKey` continuation reader rather than writing a second parser that disagrees.
    - `251-RESEARCH.md` §2.2 (the 25 raw status tokens with counts), §2.4 (the split rule and its
      proof across all 278), §3.3 (what is mechanically extractable: 20 full paths, +25 bare
      filenames, 33 nothing), §3.4 (`trigger_surfaces` must be a controlled enum — the most frequent
      quoted term in the whole register appears TWICE), §7.1-7.2 (the boundary and the line-ending
      contract), §1.10 (the 5 files with no frontmatter block).
  </read_first>
  <behavior>
    - `node scripts/migrate-seeds-frontmatter.cjs` with no flags CHANGES NOTHING and prints a
      per-file plan: keys to add, the status token and its `status_note` split, and the totals.
    - `--apply` writes, and for every file asserts the body md5 computed over the RAW Buffer is
      identical before and after. One mismatched body aborts the whole run before any further write.
    - A second `--apply` run reports `0 files changed` — idempotent, and it refuses to double-wrap a
      `status_note` that already exists (one seed, SEED-231, already has one).
    - The 5 files with no `---` block get a complete block PREPENDED; their body md5 is the md5 of
      the entire current file, unchanged.
    - The 6 mixed-line-ending files come through with their bodies byte-identical, because the body
      Buffer is never re-serialised from a string.
    - `git diff --stat .planning/seeds/` is NOT used as evidence anywhere — it cannot see this.
  </behavior>
  <action>
    Create `scripts/migrate-seeds-frontmatter.cjs` (⛔ NOT `check-*`; the `check-` prefix in this
    repo means read-only). House header: `WHY THIS EXISTS` naming the measured cause (no
    `TEMPLATE.md` has ever existed, 91 distinct keys, 40+ status spellings), `USAGE` showing the
    dry-run and `--apply` forms, `EXIT 0 = clear · 1 = refused · 2 = harness error`, and the
    ZERO-DEPENDENCIES paragraph. Requires: `fs`, `path`, `crypto` only.

    Read every file as a Buffer. Locate the frontmatter boundary by BYTE scan: the file must begin
    with `---` followed by `\r\n` or `\n`; the block ends at the FIRST subsequent line that is
    exactly `---`. ⛔ Non-greedy, first-match — 102 bare `---` horizontal rules live inside seed
    bodies and a greedy match or a `split('---')` destroys ~40% of the register. Slice the body as a
    Buffer starting at the byte after that delimiter line's terminator. Compute
    `crypto.createHash('md5').update(bodyBuf).digest('hex')` on that Buffer. Rebuild the file as
    `newFrontmatterBuf + bodyBuf` — the body Buffer is concatenated untouched, never re-serialised.
    Emit new frontmatter lines using the line ending that dominates that file's EXISTING frontmatter
    block, and leave the body's endings alone entirely. After writing, re-read the file, re-slice the
    body and re-hash; a mismatch aborts the run with a non-zero exit and a named file.

    Transform each seed's frontmatter, in this order:

    `seed_id` — derive from the FILENAME's three-digit id, always. 177 files carry `seed_id`, 101
    carry `id`, and they disagree with the filename in at least one case (SEED-068, whose value
    carries an inline `#` comment). Normalise to `seed_id`; when a differing `id` key existed,
    preserve the old key's full original line inside `status_note`'s sibling `migration_note` rather
    than dropping it.

    `title` — from the H1 (`# ...`) when absent. When there is no H1, leave the key out and let the
    gate's `[missing-key]` name it rather than inventing a title.

    `surface` — default `Agentic-RAG` when absent. This is provably safe: only two values exist in
    the whole register and both begin `Agentic-RAG` (RESEARCH §1.4). Normalise the one compound
    value (`Agentic-RAG / retrieval / database`) to `Agentic-RAG` and carry the original into
    `migration_note`.

    `status` + `status_note` (D-10) — the value is the FIRST whitespace-delimited token; everything
    after it, with only leading whitespace stripped, moves byte-for-byte into `status_note`. RESEARCH
    §2.4 proves no legal status value contains a space, across all 278. 21 of the 26 prose-carrying
    lines put their prose after a `#`; that prose is a deliverable here, not a comment to discard.

    The complete mapping, and ⛔ every one of these is listed because D-10 and D-16 forbid a silent
    coercion:

    Clean 1:1 (9 tokens, 251 files): planted→planted · open→open · closed→closed · folded→folded ·
    dormant→dormant · partially-answered→partially-answered · answered→answered · shipped→shipped ·
    deferred→deferred.

    The `partially-*` family (15 files) — base token plus `partial: true` (D-16):
    partially-folded (7)→folded · partially-shipped (4)→shipped · shipped-in-part (1)→shipped ·
    partially-resolved (1)→answered · partial (1)→⚠ READ the seed and choose the base from the enum ·
    partial-consumed (1)→⚠ READ the seed; `folded` unless its prose says otherwise.

    The 12 orphans, individually (D-16 names this obligation explicitly):
    promoted (2 — SEED-100, SEED-101)→⚠ READ BOTH FIRST; if it means *became a requirement* the
    target is `folded` with the destination recorded in `status_note`, and the reading goes in the
    SUMMARY ·
    DONE (2)→⚠ read each: `shipped` when its own prose says code shipped, `closed` when a question
    was settled without code ·
    done (1)→the same rule as DONE, decided by READING — ⛔ case-folding is explicitly NOT a rule ·
    routed (1)→folded ·
    scheduled (1)→deferred · queued (1)→deferred ·
    in_progress (1)→open · active (1)→open ·
    resolved (1)→⚠ read: `answered` if a question was answered, `closed` if code shipped ·
    fixed (1)→closed.

    Every mapped file's ORIGINAL raw status line is preserved verbatim at the head of `status_note`,
    prefixed so it reads as the original rather than as new prose.

    The 5 no-frontmatter files (SEED-084, 163, 164, 165, 166) take the SECOND code path: prepend a
    complete block; `status` is `planted` with a `status_note` stating plainly that no status was
    recorded and the value is a migration default, never a claim about the seed. Their body md5 is
    the md5 of the entire current file.

    `trigger_when` (D-02) — the 126 files without one get the literal value `unset`. ⛔ Never invent
    a trigger.

    `trigger_paths` (D-01/D-18, MECHANICAL ONLY) — extract only tokens that are unambiguously paths:
    a repo-relative path containing a `/` and a known top-level segment (`backend/`, `frontend/`,
    `scripts/`, `docs/`, `.planning/`, `.claude/`, `supabase/`) becomes that path plus a trailing
    `/**` when it names a directory; a bare filename with a known code extension becomes
    `**/<name><ext>`. Everything else is left out. Expect roughly 45 of 157 to yield anything —
    RESEARCH §3.3 measures 20 full paths and 25 bare filenames — and the script must print that
    figure rather than a claim.

    `trigger_surfaces` (D-18, CONTROLLED ENUM, NOT free extraction) — define a single list of ~15
    domain words in the script (workflow, connectors, retrieval, settings, chat, skills, ingestion,
    sandbox, provider, auth, admin, panel, library, harness, deployment) and map a quoted or
    backticked term onto it ONLY on exact match or a documented alias. ⛔ Unmapped terms are LEFT
    OUT — free extraction yields ~53 seeds tagged with strings no other seed shares and no phase
    will ever declare. The enum list is written into `TEMPLATE.md` in Task 3 (S-5).

    ⛔ Do NOT emit `trigger_phase_touches` (D-18). Do NOT reorder or drop any key the migration does
    not own — unknown keys pass through in their original order and with their original text.

    Report, printed and gated on the write per `agent-bus.sh:145-147`: files scanned, files changed,
    keys added per file, the status mapping tally token-by-token, the two D-18 figures, and
    `bodies verified md5-identical: N/N`. `N files changed` is only printable when N writes landed
    AND N body hashes matched.
  </action>
  <acceptance_criteria>
    - `node scripts/migrate-seeds-frontmatter.cjs` (no flags) leaves `git status --porcelain
      .planning/seeds/` EMPTY and prints a plan naming 283 scanned files.
    - `grep -n "split('---')\|--date\|normalize\|replace(/\\\\r\\\\n/g" scripts/migrate-seeds-frontmatter.cjs`
      shows no whole-file CRLF normalisation on any write path.
    - `grep -c "createHash" scripts/migrate-seeds-frontmatter.cjs` is at least 1 and the hashed
      argument is a Buffer slice, not a string produced by `toString()`.
    - The script's own output line reads `bodies verified md5-identical: 283/283` after `--apply`,
      and the digests are computed in the SAME process run, before and after.
    - `grep -n "git diff" scripts/migrate-seeds-frontmatter.cjs` returns nothing — `git diff` cannot
      verify D-11 and must not appear as evidence.
    - The mapping table in the script source lists all 25 raw tokens; `grep -c` over the source for
      each of `promoted`, `routed`, `scheduled`, `queued`, `in_progress`, `active`, `resolved`,
      `fixed`, `DONE`, `done`, `partial-consumed`, `shipped-in-part` is ≥ 1 for every one.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/migrate-seeds-frontmatter.cjs; echo "exit=$?"; git status --porcelain .planning/seeds/ | wc -l</automated>
  </verify>
  <done>The migration exists, defaults to dry-run, refuses to write on a body-hash mismatch, and
  every one of the 25 status tokens has a listed destination.</done>
</task>

<task type="auto">
  <name>Task 2: Run it — 283 files migrated, 283 bodies proven identical, the Plan 01 gate green</name>
  <files>.planning/seeds/</files>
  <read_first>
    - `.planning/phases/251-register-integrity/251-GATE-BASELINE.md` — Plan 01's pre-migration
      census. Every figure this task claims is measured against THAT file, not against CONTEXT.md.
    - `251-RESEARCH.md` §1.9 — the six mixed-line-ending files by name (SEED-013, 144, 145, 171,
      193, 194) and §1.10 — the five no-frontmatter files by name (SEED-084, 163, 164, 165, 166).
    - `251-PATTERNS.md` §2b — the dry-run → review → apply → read-back posture being followed.
    - `scripts/check-seeds-register.cjs` — the acceptance instrument. Its `[missing-key]` and
      `[unknown-status]` arms ARE this task's test.
  </read_first>
  <action>
    Run the dry-run first and READ its per-file plan, specifically the `partial`/orphan rows the
    script flagged for a human reading: the two `promoted` seeds, the three `DONE`/`done` seeds, the
    single `resolved`, `partial` and `partial-consumed`. Decide each by opening the seed, record the
    decision and the sentence that decided it, then encode the decision in the script's mapping table
    (not as a one-off patch to a file) so a re-run reproduces it.

    Capture a directory-wide body digest baseline before applying: for every seed, the md5 of its
    body Buffer, written to a scratch file OUTSIDE the repo (the session scratchpad, per the
    no-scratch-in-tree rule).

    Apply. Then, independently of the script's own claim, re-derive the body digests with a separate
    one-off node expression and diff the two digest SETS — not their counts. The script proving
    itself is the weakest possible evidence; this is the same lesson as *matching a baseline COUNT is
    not matching its SET*.

    Run the migration a second time with `--apply` and confirm it reports `0 files changed`.

    Then run `node scripts/check-seeds-register.cjs`. Expect `[missing-key]` and `[unknown-status]`
    to be GONE and `[no-frontmatter]` to be GONE. ⚠ Expect the 8 `[duplicate-id]` findings to REMAIN
    — they are Plan 03's work, and a green gate here would mean the gate stopped seeing them. Record
    that partial-red as the expected state, with the exit code.

    Verify the six mixed-line-ending files individually by name: their body bytes must be identical
    to the baseline digests, and their frontmatter must be internally consistent.
  </action>
  <acceptance_criteria>
    - The independently-derived body-digest SET after the migration is identical to the pre-migration
      SET for all 283 files — compared as sets of `path:md5` strings, with the diff in both
      directions empty. ⛔ Equal counts are not the criterion.
    - `node scripts/migrate-seeds-frontmatter.cjs --apply` run a second time prints
      `0 files changed` and exits `0`.
    - `node scripts/check-seeds-register.cjs` prints `0` `[missing-key]`, `0` `[unknown-status]` and
      `0` `[no-frontmatter]` findings, and still prints exactly `8` `[duplicate-id]` findings.
      Exit code is `1`, and that is the expected reading at this wave.
    - The gate's unswept line reports two figures, and the `carry no trigger_when at all` figure is
      `0` (every seed now carries the key, 126 of them reading `unset`).
    - `status` values across the register resolve to exactly the 10 enum members:
      `grep -h '^status:' .planning/seeds/*.md | sed 's/\r$//' | awk '{print $2}' | sort -u`
      prints only enum members and nothing else.
    - Every file that received a `partial: true` also carries a legal `status`; the count of
      `partial: true` is `15`.
    - The six named mixed-line-ending files and the five named no-frontmatter files each appear by
      name in the SUMMARY with their before/after body md5.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/migrate-seeds-frontmatter.cjs --apply; node scripts/check-seeds-register.cjs; echo "gate exit=$?"</automated>
  </verify>
  <done>283 seeds carry the contract, 283 bodies are proven byte-identical by an independently
  derived digest set, the migration is idempotent, and the only remaining gate findings are the 8
  duplicate ids Plan 03 owns.</done>
</task>

<task type="auto">
  <name>Task 3: The contract's three written homes — TEMPLATE.md, plant-seed.md, CLAUDE.md</name>
  <files>.planning/seeds/TEMPLATE.md, .claude/get-shit-done/workflows/plant-seed.md, CLAUDE.md</files>
  <read_first>
    - `.planning/reported-bugs/TEMPLATE.md` (all 54 lines) — the stated model, quoted in full at
      `251-PATTERNS.md` §3, with its three structural conventions: the enum in a trailing `#`
      comment on the key's own line; a conditional key saying so in its comment; body headings as
      square-bracket prompts.
    - `251-PATTERNS.md` §3 — the key-by-key mapping table from reported-bugs onto D-09/D-10/D-16,
      and the three body-shape exemplars (`SEED-275`, `SEED-276`, `SEED-231` — the ONE existing
      `status_note` in the register).
    - `.claude/get-shit-done/workflows/plant-seed.md` lines 53-104 — BOTH the broken allocator
      (`count(files) + 1`) and the de-facto template block that re-introduces D-09's defect on the
      next seed.
    - `251-PATTERNS.md` §7 — `scripts/agent-bus.sh:26-33` `next_id()`, the correct in-repo
      `max(id)+1` allocator, with the `10#` base-forcing comment.
    - `CLAUDE.md` § "Seeds register cross-check (MANDATORY)" (starts at line 488) and § "Reported
      bugs cross-check (MANDATORY)" — the 4-touchpoint table that is its own in-file model.
    - `CLAUDE.md` § "CLAUDE.md context budget (MANDATORY)" — measure with the gate, never `wc`.
  </read_first>
  <action>
    Create `.planning/seeds/TEMPLATE.md` modelled on `.planning/reported-bugs/TEMPLATE.md`. The
    frontmatter block carries every key of the contract with its enum or its conditionality in a
    trailing `#` comment on the key's own line: `seed_id` (⚠ must match the filename), `title`,
    `created` (absolute date; note `planted` is the legacy spelling and D-20 reads `created` first),
    `surface`, `status` with all ten enum members listed in the comment, `partial` with the comment
    *true when the status is settled on one axis only*, `status_note` with *required when the status
    line carried prose*, `trigger_when` with *prose, for a human; `unset` when there is none*,
    `trigger_paths` with the one-line `path.matchesGlob` semantic and a worked example,
    `trigger_surfaces` with the full controlled enum inline, `relates_to`, `folded_into`. ⛔ No
    `trigger_phase_touches`. Body headings as square-bracket prompts so a half-filled template is
    visibly half-filled. Add one short section stating the same-commit sync rule in words: this file,
    `scripts/check-seeds-register.cjs`'s `STATUS_ENUM`/`REQUIRED_KEYS`, and `plant-seed.md`'s emitted
    block are one triple, and a change to any of the three is a change to all three.

    Edit `.claude/get-shit-done/workflows/plant-seed.md` twice, in one commit:

    (a) `<step name="generate-seed-id">` — replace the `count(files)+1` allocation with a
    `max(id)+1` derivation over the filename id space, modelled line-for-line on
    `scripts/agent-bus.sh:26-33`. ⛔ `10#` base-forcing is load-bearing and must be present with its
    reason in a comment: seed ids are zero-padded and bash treats `022` and `092` as invalid octal,
    so `$(( 092 + 1 ))` is a hard error without it. Keep the existing `printf "%03d"`. Add a comment
    recording the measured defect being replaced: 283 files, highest id 276, so the old form emitted
    `SEED-284`.

    (b) the de-facto template block at lines 64-104 — replace it with the D-09 contract block,
    identical in keys and comments to `TEMPLATE.md`. It currently writes `id:` (not `seed_id:`),
    `status: dormant`, `trigger_when: when relevant`, `scope: unknown`, and carries NO `surface:`,
    no `title:`. ⛔ This block is the mechanism that re-introduces the defect on the very next
    `/gsd:capture --seed`; it and `TEMPLATE.md` must land in the same commit as the migration or the
    backfill is undone tomorrow.

    ⚠ Record in the SUMMARY that `.claude/get-shit-done/` is a VENDORED framework at v1.42.3 and
    this edit is a known re-apply risk. The precedent (`9d3d887de`, the G-7 wiring) survived one
    framework update. Plan 04 records the wiring sites in CLAUDE.md; name this file there too.

    Edit `CLAUDE.md` § "Seeds register cross-check (MANDATORY)": the `/gsd:new-milestone` row's
    instruction to list seeds with `status: planted` AND `surface: Agentic-RAG` is replaced by the
    executable call, and the sweep rule now points at `node scripts/check-seeds-register.cjs`.
    ⛔ The correction sits BESIDE the original, never over it, per this project's own convention:
    keep the original sentence, strike it through, and state the measurement that refuted it — the
    `surface` filter was present on 128 of 283 files, so the rule could not see 55% of its own
    register, and five seeds carried no `status` at all. Keep the section's true parts unchanged:
    *a seed is answered by editing the seed*, and *`status:` frontmatter IS the index*. Add one line
    naming `.planning/seeds/TEMPLATE.md` as the contract's home.
  </action>
  <acceptance_criteria>
    - `.planning/seeds/TEMPLATE.md` exists, and the ten enum members it lists in the `status:`
      comment are exactly the ten in `scripts/check-seeds-register.cjs`'s `STATUS_ENUM` — compared
      as sorted token sets, not by eye.
    - `grep -n "trigger_phase_touches" .planning/seeds/TEMPLATE.md` returns nothing.
    - `grep -n "EXISTING + 1\|wc -l" .claude/get-shit-done/workflows/plant-seed.md` returns nothing
      on the id-allocation path, and `grep -c "10#" .claude/get-shit-done/workflows/plant-seed.md`
      is at least 1.
    - Running the new allocator snippet by hand in the repo root prints `SEED-277` today (highest id
      276), not `SEED-284`.
    - `grep -n "seed_id:" .claude/get-shit-done/workflows/plant-seed.md` returns a line inside the
      emitted template block, and `grep -n "^id: SEED-"` inside that block returns nothing.
    - `node scripts/check-claude-md-size.cjs` exits `0` and its printed char count for CLAUDE.md is
      recorded in the SUMMARY. ⛔ Measured with the gate, never with `wc`.
    - CLAUDE.md's seeds section still contains the original `surface: Agentic-RAG` sentence, marked
      as struck-through/corrected rather than deleted.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs; echo "exit=$?"; grep -c "10#" .claude/get-shit-done/workflows/plant-seed.md</automated>
  </verify>
  <done>The contract has a written home, the seed-authoring path emits it, the allocator derives
  `max(id)+1`, and CLAUDE.md's rule no longer describes a filter that sees less than half the
  register.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → `.planning/seeds/` | the first file-writing `.cjs` in `scripts/`, rewriting 283 files in one pass |
| vendored framework ↔ project edits | `.claude/get-shit-done/` is at v1.42.3 with a SessionStart update checker |
| CLAUDE.md ↔ every agent | a rule here is loaded into every session and subagent system prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-251-07 | Tampering | 283 seed BODIES | mitigate | body sliced and hashed as a raw Buffer, concatenated through untouched, re-hashed after the write in the same process; one mismatch aborts the run. ⛔ `git diff` is explicitly NOT evidence — `core.autocrlf=true` with no `.gitattributes` means a normalising rewrite of 153 CRLF files produces NO diff while changing every line (Task 1) |
| T-251-08 | Tampering | the frontmatter/body boundary | mitigate | non-greedy first-match delimiter anchored at line 1; 102 bare `---` horizontal rules live inside bodies and a greedy match or `split('---')` silently eats body content on ~40% of the register (Task 1) |
| T-251-09 | Repudiation | the migration's own success report | mitigate | the success line is gated on the write, not on reach (`agent-bus.sh:145-147`); and Task 2 re-derives the digest SET independently of the script, because the script proving itself is the weakest evidence available |
| T-251-10 | Tampering | destruction of information the migration claims to preserve | mitigate | every discarded byte of a `status` line moves verbatim into `status_note`; the gate's `[unknown-status]` remedy text explicitly forbids greening it by deleting prose; the 21 `#`-comment cases are a deliverable, which is a second reason a YAML dependency is refused |
| T-251-11 | Elevation of privilege | script writes outside its subject | mitigate | every write path resolves under `SEEDS_DIR` and asserts the resolved path starts with it; the script takes no path argument from the CLI |
| T-251-12 | Tampering | a later `chore(gsd): apply the pending GSD framework update` reverting `plant-seed.md` | mitigate | recorded as a known risk in the SUMMARY and in CLAUDE.md beside the G-7 entry (Plan 04), so the edit can be re-applied rather than silently lost. ⚠ Accepted, not eliminated — the precedent survived one update |
| T-251-13 | Denial of service | CLAUDE.md crossing the 150,000-char refusal limit, silently disabling every instruction in it for every agent | mitigate | `node scripts/check-claude-md-size.cjs` is an acceptance criterion; the correction replaces prose rather than only appending (Task 3) |
| T-251-SC | Tampering | npm/pip/cargo installs | mitigate | zero install tasks; `fs`, `path`, `crypto` are Node built-ins; `js-yaml`/`gray-matter` forbidden with two measured reasons |
</threat_model>

<verification>
1. Body invariant: an independently derived `path:md5` SET over all 283 bodies, diffed both
   directions against the pre-apply set — empty both ways. ⛔ Not `git diff`, not a count.
2. Idempotency: a second `--apply` prints `0 files changed`.
3. `node scripts/check-seeds-register.cjs` → `[missing-key]`, `[unknown-status]` and
   `[no-frontmatter]` all zero; `[duplicate-id]` still exactly 8 (Plan 03's work); exit 1.
4. `node scripts/check-claude-md-size.cjs` → exit 0, char count recorded.
5. `node scripts/check-hot-file-ledger.cjs 251` → re-run and record; no product source is in this
   plan's `files_modified`, so a `[no-row]` reading is a finding about the gate's scan set, not a
   licence to add ledger rows for tooling.
</verification>

<success_criteria>
- All 283 seeds carry `seed_id`, `title`, `status`, `surface`, `trigger_when` (D-09).
- `status` is one of ten enum tokens on every file; 15 carry `partial: true`; every byte of
  displaced prose is in `status_note` (D-10, D-16).
- 283/283 bodies proven md5-identical over raw Buffers by an independent derivation (D-11).
- The 126 trigger-less seeds read `trigger_when: unset`, and the gate prints two separate unswept
  figures (D-02, D-18).
- `TEMPLATE.md` exists and agrees with the gate and with `plant-seed.md`'s emitted block (S-5).
- The next `/gsd:capture --seed` would allocate `SEED-277`, not `SEED-284` (D-08).
</success_criteria>

<output>
Create `.planning/phases/251-register-integrity/251-02-SUMMARY.md` when done, carrying the per-file
decisions for the 8 read-first status tokens, the before/after digests for the 6 mixed and 5
no-frontmatter files, and the CLAUDE.md char reading.
</output>
