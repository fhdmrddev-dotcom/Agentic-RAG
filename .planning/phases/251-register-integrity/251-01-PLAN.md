---
phase: 251-register-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-seeds-register.cjs
  - .planning/phases/251-register-integrity/251-GATE-BASELINE.md
autonomous: true
requirements: [REG-02]
user_setup: []

must_haves:
  truths:
    - "D-03: one command — `node scripts/check-seeds-register.cjs` — reads the whole register, prints its own derivation, and exits 0 clear / 1 violation / 2 harness error, indistinguishable in shape from its four siblings."
    - "D-04 arm 1: a planted duplicate seed id makes the gate exit 1 and name `[duplicate-id]` with both file paths."
    - "D-04 arm 2: a seed with a missing or unknown `status` makes the gate exit 1 and name `[no-frontmatter]` or `[unknown-status]`."
    - "D-04 arm 3: a seed whose `trigger_paths` matches a phase's `files_modified` is PRINTED, with the matched token quoted."
    - "D-04 arm 4 (the counterfactual): a seed whose `trigger_paths` does NOT match is ABSENT from the output — the gate does not print everything."
    - "D-04 count assertion: the parsed-file count is non-zero AND equals the register size derived by readdir; every skip increments a printed counter, so `283 in / 283 accounted for` or the run is a harness error (exit 2)."
    - "D-01: `trigger_paths` globs are matched with `path.matchesGlob` against `files_modified`, both sides normalised to forward slashes — zero dependencies."
    - "D-18: the gate prints TWO separate unswept figures on the same line — `N carry no trigger_when at all` and `M carry prose but no structured trigger` — never summed."
    - "D-09: the gate fails `[missing-key]` when a seed lacks any of `seed_id`, `title`, `status`, `surface`, `trigger_when`."
    - "D-10 / D-16: the gate fails `[unknown-status]` for a status token outside the 10-value enum, and validates `partial:` INDEPENDENTLY of `status`."
    - "D-20: the gate encodes the date rule as `created` if present ELSE `planted`, so it can evaluate its own D-07 rule on the 5 of 8 pairs that carry only `planted:`."
    - "D-08 (gate half): `[duplicate-id]` is a failing code, so a ninth collision cannot survive a commit."
  artifacts:
    - path: "scripts/check-seeds-register.cjs"
      provides: "the REG-02 sweep instrument, read-only, zero dependencies"
      contains: "MIN_SUBJECT_FILES"
    - path: ".planning/phases/251-register-integrity/251-GATE-BASELINE.md"
      provides: "the register census BEFORE any migration — the number Plan 02 and Plan 03 are measured against"
  key_links:
    - from: "scripts/check-seeds-register.cjs"
      to: ".planning/seeds/"
      via: "fs.readdirSync + per-file Buffer read"
      pattern: "readdirSync"
    - from: "scripts/check-seeds-register.cjs --self-test"
      to: "os.tmpdir()"
      via: "fs.mkdtempSync fixture register"
      pattern: "mkdtempSync"
---

<objective>
Build the executable sweep REG-02 requires, and drive it RED on all four of D-04's arms plus the
count assertion BEFORE the register it sweeps is touched.

Purpose: REG-02's success criterion is *"a person can run one command"*. This plan is that command.
It lands FIRST on purpose — D-04 arms 1 and 2 are the acceptance test for Plan 02's migration output,
and a gate written after the migration is a gate fitted to its own result.

Output: `scripts/check-seeds-register.cjs` (read-only, zero dependencies, `--self-test` mode) and
`251-GATE-BASELINE.md` — the pre-migration census every later plan is measured against.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/251-register-integrity/251-CONTEXT.md
@.planning/phases/251-register-integrity/251-RESEARCH.md
@.planning/phases/251-register-integrity/251-PATTERNS.md
</context>

<interfaces>
<!-- The contract this gate publishes. Plans 02, 03 and 04 all consume it; do not change these
     names later without updating them. -->

Command surface:
  node scripts/check-seeds-register.cjs                       # full register scan
  node scripts/check-seeds-register.cjs --files a.md b.md     # per-file mode (no count floor)
  node scripts/check-seeds-register.cjs --phase <NNN>         # trigger sweep against a phase's files_modified
  node scripts/check-seeds-register.cjs --self-test           # D-04's five arms against a temp fixture register

Exit codes: 0 = clear · 1 = violation · 2 = harness error

Module surface (consumed by Plan 02's migration — this file is BOTH a CLI and a library):
  module.exports = { frontmatter, readKey, stripComment, STATUS_ENUM, REQUIRED_KEYS, analyse }
  The CLI entry point is guarded by `if (require.main === module)`, so a `require()` of this file
  parses no argv, prints nothing and calls no `process.exit`. ⚠ Without that guard a `require()`
  would run the gate's own CLI and kill the importing process — which is exactly what Plan 02's
  migration does.

Failure codes emitted:
  [no-frontmatter]   reused VERBATIM from check-verification-honesty.cjs — no `---` block at line 1
  [duplicate-id]     two seed files claim the same NNN (naming precedent: [duplicate-row])
  [missing-key]      a required key of D-09's contract is absent
  [unknown-status]   the status token is outside D-10/D-16's 10-value enum

The D-09 required-key contract the gate enforces (seed_id, title, status, surface, trigger_when)
and the D-10/D-16 enum (planted, dormant, open, partially-answered, answered, folded, shipped,
closed, deferred, superseded-id) are the SAME values `.planning/seeds/TEMPLATE.md` will carry in
Plan 02. S-5 same-commit sync rule: the gate's enum and the template's enum comment are one pair.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The reader — frontmatter, continuation scalars, and the count that cannot lie</name>
  <files>scripts/check-seeds-register.cjs</files>
  <read_first>
    - `251-PATTERNS.md` §1a-1b (header contract, `fail()`, root resolution) and §1c — quotes
      `scripts/check-verification-honesty.cjs:219-225` (`MIN_SUBJECT_FILES`) and `:418-440` (the
      two-mode application), which is the pattern this task copies.
    - `251-PATTERNS.md` §1d — quotes `scripts/check-gap-closure-rounds.cjs:90-117`
      (`frontmatter()` / `scalar()` / `list()`, the only CRLF-tolerant sibling) and
      `scripts/check-verification-honesty.cjs:271-297` (`stripComment`, `frontmatter` returning
      `null` not `""`).
    - `scripts/check-verification-honesty.cjs` lines 1-33, 178-245, 271-297, 418-440 — the primary
      analog. Where the four siblings disagree, THIS ONE WINS.
    - `scripts/check-gap-closure-rounds.cjs` lines 84-117 — the frontmatter/list readers and the
      `git()` spawnSync wrapper.
    - `scripts/check-hot-file-ledger.cjs` lines 105-113 — the CAUTIONARY analog. Copy the non-greedy
      delimiter regex; do NOT copy the two uncounted `continue`s at :108 and :110.
    - `251-RESEARCH.md` §3.1 (shape census: 78 folded, 33 list, 45 plain, 1 mixed), §1.9 (line
      endings), §1.10 (the 5 no-frontmatter seeds), §7.1 (102 bare `---` in bodies).
  </read_first>
  <behavior>
    - Reads exactly 283 `SEED-\d{3}-*.md` files from `.planning/seeds/` today, and prints
      `register: 283 files · parsed: 283`.
    - Reads `trigger_when` on exactly 157 files — folded block scalars (`key: >`, 78 files),
      YAML lists (`key:` then `  - ` bullets, 33 files) and plain scalars (45) all resolve to a
      non-empty value. A plain `^trigger_when:\s*(.*)$` reader would return empty for 112 of them.
    - Returns `[no-frontmatter]` for exactly 5 files: SEED-084, SEED-163, SEED-164, SEED-165,
      SEED-166 — they have NO `---` block at all, which is a different state from a missing key.
    - Counts a seed whose body contains a bare `---` horizontal rule as ONE parsed file with its
      frontmatter intact (102 such rules exist across the register).
    - With a deliberately emptied scan set, exits 2 with a FATAL naming the floor — never 0.
  </behavior>
  <action>
    Create `scripts/check-seeds-register.cjs` with the house header block (`WHY THIS EXISTS` naming
    the measured failure — `SEED-172` reachable for four weeks, and Phase 242's
    `subject: 0 files · ledger gate OK` — then `USAGE`, then `EXIT 0 = clear · 1 = violation ·
    2 = harness error`). Copy the ZERO DEPENDENCIES paragraph from
    `check-verification-honesty.cjs:76-80` and add this phase's SECOND measured reason beside it:
    a real YAML parser silently discards the `#`-comment prose on 21 seeds that D-10 requires be
    preserved. Permitted requires: `fs`, `path`, `crypto`, `os`, `child_process` (for the D-20
    tie-break only) — all Node built-ins, so the rule holds.

    Implement, in this order:

    `fail(msg)` exactly as `check-verification-honesty.cjs:232-235` — `FATAL: ${msg}` to stderr,
    `process.exit(2)`. Use the name `fail`, not `fatal`. Colour constants RED/YEL/GRN/RST.
    `root = path.resolve(__dirname, '..')`, `SEEDS_DIR = path.join(root, '.planning', 'seeds')`.
    Take `SEEDS_DIR` from an internal parameter, not a module constant baked into every function —
    `--self-test` must be able to point the same code at a temp fixture directory.

    `readRegister(dir)` — `fs.readdirSync(dir)` filtered on `/^SEED-\d{3}-.*\.md$/`. This filtered
    length is `registerSize` and is the ONLY authority for how many files must be accounted for.
    Read each file with `fs.readFileSync(p)` as a Buffer, then `toString('utf8')` for parsing only;
    do NOT normalise line endings on disk — this gate never writes.

    `frontmatter(text)` — `/^---\r?\n([\s\S]*?)\r?\n---(\r?\n|$)/`, NON-GREEDY, anchored at line 1,
    returning `null` when absent (not `''` — the gate must distinguish "no block" from "empty
    block"). `\r?` is mandatory: 159 of 283 seeds contain CR and 6 are mixed within one file.
    ⭐ **RETURN SHAPE ON A HIT IS PART OF THE CONTRACT, because Plan 02 consumes it:** return the
    **whole match object** from `String.prototype.match` — i.e. `m` such that `m[1]` is the captured
    block and **`m[0].length` is the exact string offset of the block's end**, valid because the
    regex is anchored at index 0. ⛔ Do NOT return the bare captured string: Plan 02 must convert
    that offset to a BYTE offset (`Buffer.byteLength`) to slice the raw body Buffer for D-11, and
    recovering the offset from a bare string would need `text.indexOf(block)`, which returns `0` for
    an EMPTY frontmatter block and silently mis-slices the body. One seed with an empty block is
    enough to void the md5 proof on that file, so the shape is pinned here rather than hedged there.

    `readKey(fm, key)` — the NEW continuation reader, and the piece with no sibling. It must handle
    three shapes and return a single joined string plus a shape tag: plain scalar on the key's own
    line; folded block scalar when the value is exactly `>` or `>-` or `|`, consuming every
    following line that is more-indented and stopping at the first line matching `^\S`; and YAML
    list when the value is empty and following lines match `^[ \t]+-[ \t]+`. `list()` at
    `check-gap-closure-rounds.cjs:103-117` is the starting point — it already walks continuation
    lines and already stops with `if (/^\S/.test(lines[i])) break`.

    `stripComment(line)` — lift `check-verification-honesty.cjs:271-290` verbatim, quote-aware. Here
    it is used to FIND the note (the right-hand side), which is the opposite of how the analog uses
    it: this gate reports the split, Plan 02's migration performs it.

    Accounting: maintain a `skipped` Map keyed by reason. EVERY early `continue` increments it.
    After the walk assert `parsed + skippedTotal === registerSize`; if not, `fail()` — it is a
    harness error, not a violation. Then the count assertion proper: in scan mode, if
    `registerSize === 0` or `parsed !== registerSize`, `fail()` with a message naming both numbers
    and the sentence *"a gate that passes over nothing is worse than absent"*. Keep the `--files`
    carve-out from `check-verification-honesty.cjs:136-145`: in `--files` mode one file is a
    legitimate resolution and neither the floor nor the equality applies; `--files` with zero
    arguments is still a `fail()`.

    Derivation print, as its own lines before any verdict, in the
    `check-hot-file-ledger.cjs:159-160` style:
    `register: <N> files · parsed: <N> · skipped: <n> · duplicate ids: <n>` and a second line for
    D-18's two figures (task 2 fills them).

    Entry point: `main()` + `try { process.exit(main()); } catch (e) { fail(...) }` per
    `check-hot-file-ledger.cjs:187-191` — an uncaught throw must become exit 2, never Node's default 1.
    ⚠ **One deliberate departure from that analog, and it is load-bearing:** wrap that try/catch in
    `if (require.main === module) { … }`, and end the file with a `module.exports` object exposing
    `frontmatter`, `readKey`, `stripComment` and (once Task 2 defines them) `STATUS_ENUM` and
    `REQUIRED_KEYS`; Task 3 adds `analyse` to the SAME object. No sibling `check-*.cjs` does this
    today (`grep -l "module.exports" scripts/*.cjs` is empty), so state the measured reason in the
    header: **Plan 02's migration `require()`s this file** to reuse ONE frontmatter boundary regex
    and ONE continuation reader, and an unguarded top-level `process.exit(main())` would run this
    gate's CLI and kill the importing process. This is `251-PATTERNS.md` § *the two copies of the
    parse* applied forward instead of paid for again — its verdict on `age_days()` is *do not write
    it a third time; guard the entry or extract*, and here the second writing has not happened yet.
    ⚠ CLI behaviour must be unchanged by the guard: same stdout, same exit codes.
  </action>
  <acceptance_criteria>
    - `node scripts/check-seeds-register.cjs` prints a line whose `register:` and `parsed:` figures
      are BOTH `283` and whose `skipped:` figure is `0`, re-derived at run time (a hardcoded 283
      anywhere in the file is a defect — grep the source for `283` and expect zero matches outside
      comments).
    - The run reports exactly `5` files with `[no-frontmatter]`, and the five paths printed are
      SEED-084, SEED-163, SEED-164, SEED-165, SEED-166.
    - A one-off node expression that loads the file's `readKey` over all 283 seeds counts exactly
      `157` files with a non-empty `trigger_when`, and the shape tally is `78` folded + `33` list +
      `45` plain + `1` other.
    - `node scripts/check-seeds-register.cjs --files` (no arguments) exits `2` and prints a FATAL
      naming the Phase-242 vacuous pass.
    - `grep -c "require(" scripts/check-seeds-register.cjs` matches only `fs`, `path`, `crypto`,
      `os`, `child_process` — no third-party module name appears.
    - `grep -n "split('---')\|split(\"---\")" scripts/check-seeds-register.cjs` returns nothing
      (the 102-horizontal-rule hazard).
    - `node -e "const m=require('./scripts/check-seeds-register.cjs'); console.log(typeof m.frontmatter, typeof m.readKey, typeof m.stripComment)"`
      prints `function function function`, **exits 0, and prints nothing else** — no derivation line,
      no verdict, no FATAL. A `require()` that produces gate output, or that exits non-zero, means the
      `require.main === module` guard is missing and Plan 02 Task 1 cannot be executed as written.
    - `grep -c "require.main === module" scripts/check-seeds-register.cjs` is `1`, and
      `grep -c "^module.exports" scripts/check-seeds-register.cjs` is `1`.
    - The guard changes no CLI behaviour: `node scripts/check-seeds-register.cjs` prints the same
      `register:`/`parsed:` line and exits with the same code before and after the guard is added —
      both readings recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-seeds-register.cjs; echo "exit=$?"</automated>
  </verify>
  <done>The reader accounts for all 283 files with zero silent skips, resolves 157 `trigger_when`
  values across all three YAML shapes, and cannot exit 0 over a collapsed scan set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The four validation arms, the trigger match, and the honest verdict</name>
  <files>scripts/check-seeds-register.cjs</files>
  <read_first>
    - `251-PATTERNS.md` §1f — the derivation print shapes, including the exact two-line form D-18
      requires (`register: … · parsed: … · duplicate ids: …` then
      `unswept:  N carry no trigger_when at all · M carry prose but no structured trigger`).
    - `251-PATTERNS.md` §1g — `check-verification-honesty.cjs:524-530` (code + indented reason,
      accumulated as `[code, why]` tuples at `:364-397`) and `check-hot-file-ledger.cjs:167-170`.
    - `251-PATTERNS.md` §1h — `check-gap-closure-rounds.cjs:261-278` and
      `check-verification-honesty.cjs:503-521`: the green line states WHAT IS TRUE with a ratio,
      never a bare "OK"; an override prints `passed WITH OVERRIDES`, never `clear`.
    - `251-PATTERNS.md` §1i — `check-hot-file-ledger.cjs:171-184` and
      `check-verification-honesty.cjs:531-547`: the remedy block, and the
      *do-not-satisfy-this-by-deleting-prose* clause that `[unknown-status]` must imitate.
    - `251-PATTERNS.md` §1k — `check-hot-file-ledger.cjs:113` and
      `check-verification-honesty.cjs:237-245` (`norm` / `toRepoRel`): normalise BOTH sides.
    - `251-PATTERNS.md` § No Analog Found, row 3 — `check-gap-closure-rounds.cjs:84-88, 189` for
      the `git()` spawnSync wrapper shape. Use `--date=iso`, never `--date=short`.
    - `251-RESEARCH.md` §2.2 (the 25 raw status tokens and their counts), §2.4 (the split rule:
      value = first whitespace-delimited token), §2.6 (D-07 evaluated over all 8 pairs), §4.6
      (`path.matchesGlob` on Node v24.19.0), §3.3 (the honest unswept arithmetic: ~238 of 283).
  </read_first>
  <behavior>
    - Over the register as it stands today the gate exits `1` and names `8` `[duplicate-id]`
      findings — ids 022, 092, 228, 229, 231, 253, 259, 269 — each printing both file paths.
    - It names `[missing-key]` for the measured gaps: `surface` absent on 155, `seed_id` absent on
      106, `trigger_when` absent on 126, `title` absent on 29.
    - It names `[unknown-status]` for the 16 tokens outside the enum covering 27 files, and does NOT
      fire on `partial: true` beside a legal `status` — `status ∈ enum` is validated independently
      of `partial`.
    - `--phase 251` prints seeds whose `trigger_paths` glob matches an entry of that phase's
      `files_modified`, with the matched glob and the matched path both quoted; seeds whose globs
      do not match do NOT appear anywhere in the output.
    - The unswept line reports two separate figures that are never added together.
  </behavior>
  <action>
    Extend the same file. Define `REQUIRED_KEYS` as the ordered list `seed_id, title, status,
    surface, trigger_when` (D-09) and `STATUS_ENUM` as the ordered 10-value list `planted, dormant,
    open, partially-answered, answered, folded, shipped, closed, deferred, superseded-id`
    (D-10 as amended by D-16). Both are exported-by-comment as the same values `TEMPLATE.md` will
    carry — S-5 same-commit sync rule, named in the header.

    `[duplicate-id]` (D-08 gate half): group the filtered readdir list by the three-digit id from the
    FILENAME, not from frontmatter. Any group of size > 1 is a finding UNLESS exactly one member
    carries `status: superseded-id` — D-05's redirect stub is a legitimate resolution, not a
    duplicate, and the gate must say so in its own header or Plan 03's output will read as a
    regression. Print both paths and, beside each, its `created`-else-`planted` date so a reader can
    see the D-07 verdict without re-deriving it.

    `[missing-key]` (D-09): for each parsed seed, any member of `REQUIRED_KEYS` with no value is a
    finding carrying a `why` naming which key and what a scan loses without it. A file that already
    returned `[no-frontmatter]` reports THAT code only — do not emit five `[missing-key]` findings
    on top of it.

    `[unknown-status]` (D-10/D-16): take the first whitespace-delimited token of the `status` value
    (RESEARCH §2.4 proves no legal status value contains a space, across all 278). Membership test
    against `STATUS_ENUM`, case-sensitive, and say so — `DONE` and `done` are BOTH findings, and
    case-folding is an explicit non-rule. Validate `partial` separately: it must be absent or the
    literal `true`/`false`; a `partial` value beside an illegal `status` produces two findings, not
    one merged one. The remedy text for this code must end with the imitation of
    `check-verification-honesty.cjs:502-503`: *do NOT satisfy this gate by deleting the prose —
    move it byte-for-byte into `status_note`*, because the cheapest way to green this code is to
    destroy exactly what D-10 exists to preserve.

    The D-20 date rule: a `seedDate(seed)` helper returning `created` if present, else `planted`,
    else `null`, and a `gitAddedAt(path)` helper using the `git()` spawnSync wrapper with
    `log --diff-filter=A --format=%ad --date=iso -1 --` for the tie-break ONLY. State in a comment
    that the frontmatter date is AUTHORITATIVE and git is consulted only when two members of a
    duplicate group carry the same date — they disagree on two pairs without flipping a verdict,
    and the script must not pick silently.

    The trigger sweep (D-01): `--phase <NNN>` resolves that phase directory under
    `.planning/phases/` (falling back to `.planning/milestones/*-phases/`), reads every `*-PLAN.md`
    frontmatter `files_modified` list with the same non-greedy reader, normalises each entry with
    `split(path.sep).join('/')`, and tests each seed's `trigger_paths` entries with
    `path.matchesGlob(filePath, glob)` — normalising BOTH sides, because on Windows a silent
    non-match is D-04 arm 3 failing invisibly. A hit prints the seed id, its title, the glob that
    matched and the path it matched. `trigger_surfaces` matches by exact membership against a
    surfaces list the phase declares; when the phase declares none, report that fact rather than
    matching everything. `trigger_phase_touches` is NOT implemented — D-18 rules it out, and a
    comment must say so, so a later reader does not "restore" it.

    D-18's two figures: count `noTrigger` (seeds with no `trigger_when` at all) and `proseOnly`
    (seeds with `trigger_when` but no non-empty `trigger_paths` AND no non-empty
    `trigger_surfaces`). Print them as two labelled fields on ONE line, never summed, with the
    comment carrying D-18's sentence: a gate reporting 126 while 238 are unswept is the comfortable
    lie REG-02 exists to end.

    Verdicts: on zero findings print
    `seeds register gate OK — <parsed>/<registerSize> parsed, 0 duplicate ids, <n> carry all 5
    required keys`, i.e. a ratio, never a bare OK. Ship NO escape hatch — state in the header that
    none of D-04's arms warrants one, so that if a future phase adds one it must add the
    `passed WITH OVERRIDES` verdict with it (S-3).
  </action>
  <acceptance_criteria>
    - `node scripts/check-seeds-register.cjs` exits `1` today and its output contains exactly 8
      lines beginning `  [duplicate-id]` whose ids are `022 092 228 229 231 253 259 269`.
    - The output contains exactly 5 `[no-frontmatter]` lines and zero `[missing-key]` lines for
      those same 5 files.
    - `grep -c '\[unknown-status\]'` over the output is `27` — one per file carrying a token outside
      the enum, matching RESEARCH §2.2's 16-token / 27-file measurement.
    - The output carries a line matching `unswept:` with TWO labelled numbers on it, and the two
      numbers do not appear summed anywhere in the output.
    - `node scripts/check-seeds-register.cjs --phase 251` runs and exits without a harness error;
      with no `trigger_paths` in the register yet, it prints `0 seeds matched` rather than printing
      the register.
    - `grep -n "trigger_phase_touches" scripts/check-seeds-register.cjs` returns exactly one line
      and it is a comment stating D-18 ruled it out.
    - `grep -n "toLowerCase()" scripts/check-seeds-register.cjs` returns nothing on the status path
      (case-folding is an explicit non-rule).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-seeds-register.cjs; test $? -eq 1 && echo "RED as expected on the unmigrated register"</automated>
  </verify>
  <done>All four failure codes fire on the real register today with the measured counts, the trigger
  sweep matches by glob on both-sides-normalised paths, and the unswept figures are two numbers that
  cannot hide behind each other.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `--self-test` — D-04's five arms made durable, plus the pre-migration baseline</name>
  <files>scripts/check-seeds-register.cjs, .planning/phases/251-register-integrity/251-GATE-BASELINE.md</files>
  <read_first>
    - `251-PATTERNS.md` § S-7 — *a guard nobody has seen fire is not a guard*, and the measured fact
      that no `check-*.cjs` in this repo has any test, runner or self-test mode.
    - `251-RESEARCH.md` §4.5 — the house RED method (plant → run → confirm → restore → prove
      md5-identical) and the recommendation that a temp-dir `--self-test` removes the worktree
      hazard entirely.
    - `251-CONTEXT.md` D-04 — the four arms and the count assertion, verbatim.
    - `scripts/check-verification-honesty.cjs:418-440` — the two-mode argv split this mode sits
      beside.
    - `.planning/phases/248-the-credential-boundary/248-GATE-BASELINE.md` if present — the house
      shape for a pre-work baseline document.
  </read_first>
  <behavior>
    - `--self-test` builds a fixture register of ~6 synthetic seeds under `fs.mkdtempSync` and runs
      five named arms against it, printing one PASS/FAIL line per arm and exiting 1 if any arm fails.
    - Arm 1 (duplicate id): two fixture files claiming `SEED-901` → the analyser returns a
      `[duplicate-id]` finding. The same fixture with one member carrying `status: superseded-id`
      returns NO finding — the stub carve-out is itself driven.
    - Arm 2 (bad status): a fixture with `status: DONE` returns `[unknown-status]`; a fixture with no
      `---` block returns `[no-frontmatter]`.
    - Arm 3 (match): a fixture carrying `trigger_paths: ["backend/app/services/**"]` against a
      declared blast radius of `backend/app/services/agent_loop.py` IS in the matched set.
    - Arm 4 (counterfactual): a fixture carrying `trigger_paths: ["frontend/src/pages/**"]` against
      that same blast radius is ABSENT from the matched set. Asserted as absence, not as a smaller
      count.
    - Arm 5 (count): pointing the analyser at an EMPTY fixture directory raises the harness error
      rather than returning a clean result — asserted by catching the thrown/returned harness
      condition, not by reading exit 0.
    - The real `.planning/seeds/` is never written by any path in this file.
  </behavior>
  <action>
    Refactor the analysis into a pure `analyse({dir, filesModified, surfaces})` returning
    `{registerSize, parsed, skipped, findings, matched, noTrigger, proseOnly}` so both the CLI and
    `--self-test` drive the SAME code. The CLI becomes a thin printer over it. Add `analyse` to Task 1's
    `module.exports` object — same object, one more key; the `require.main === module` guard already
    established there is what keeps a `require()` silent. This is the one
    refactor that makes arm 4 assertable at all — a counterfactual cannot be asserted against stdout
    scraping.

    Add `runSelfTest()`: create the fixture root with
    `fs.mkdtempSync(path.join(os.tmpdir(), 'seeds-selftest-'))`, write the fixtures, call `analyse`
    per arm, assert, and remove the temp tree in a `finally`. Guard every write with an assertion
    that the target path starts with the mkdtemp root — a script that can write outside
    `.planning/seeds/` by accident is this phase's own threat (T-251-03), and the guard is cheap.
    Print each arm as `  arm N <name> … PASS` / `FAIL` with the expected-vs-actual on a failure, and
    finish with a ratio line (`self-test 5/5 arms PASS`), never a bare OK.

    Then drive the arms AGAINST THE REAL REGISTER once, by hand, and record the transcript: plant a
    ninth duplicate by copying an existing seed to a colliding id, run the gate, capture the exact
    `[duplicate-id]` line and the exit code, delete the plant, and prove the register is unchanged by
    comparing `md5sum` over the whole directory before and after. ⛔ Do this in the main working
    tree only, before Plan 02 starts — never concurrently with the migration.

    Write `251-GATE-BASELINE.md` capturing, verbatim from the gate's own output and never from this
    plan's prose: register size, parsed count, the 8 duplicate ids with both paths each, the
    per-key missing counts, the 27 unknown-status files with their raw tokens, D-18's two unswept
    figures, and the `--self-test` transcript. This file is the number Plans 02 and 03 are measured
    against; a figure quoted from CONTEXT.md instead of re-derived here is the rot this phase exists
    to end.
  </action>
  <acceptance_criteria>
    - `node scripts/check-seeds-register.cjs --self-test` prints five arm lines and a
      `self-test 5/5 arms PASS` ratio line, and exits `0`.
    - Breaking any single arm's expectation in a scratch copy makes `--self-test` exit `1` and name
      that arm — driven once for arm 4 specifically (the counterfactual), since a gate that prints
      everything passes arms 1-3 and is useless.
    - `git status --porcelain .planning/seeds/` is EMPTY after the real-register RED drive, and the
      recorded before/after directory md5 digests are identical strings.
    - `grep -n "writeFileSync\|rmSync\|unlinkSync" scripts/check-seeds-register.cjs` shows every
      such call inside `runSelfTest` and guarded by the mkdtemp-root assertion; none can reach
      `SEEDS_DIR`.
    - `251-GATE-BASELINE.md` exists and every number in it is pasted from a command's output with
      the command named above it.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-seeds-register.cjs --self-test; echo "exit=$?"; git status --porcelain .planning/seeds/ | wc -l</automated>
  </verify>
  <done>D-04's four arms and the count assertion are re-runnable forever by one command, arm 4 is
  asserted as ABSENCE, the real-register drive is recorded with a byte-identity proof, and the
  pre-migration census is on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tooling → `.planning/seeds/` | a script in `scripts/` reads (and, in `--self-test`, writes) the register that is this project's deferral index |
| gate verdict → human trust | a printed `OK` is taken by every later agent as a guarantee about 283 files nobody re-reads |
| this phase ships no product surface | no user input, no network, no credentials, no DB — say so rather than padding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-251-01 | Tampering | `check-seeds-register.cjs` scan set | mitigate | `registerSize` derived by readdir; `parsed + skipped === registerSize` asserted; every `continue` increments a printed counter; scan-mode equality check `fail()`s at exit 2. This is the Phase-242 defect, guarded on the SUBJECT side, which `check-hot-file-ledger.cjs` still is not (Task 1, Task 2) |
| T-251-02 | Repudiation | the green verdict line | mitigate | verdict states a ratio and D-18's two figures, never a bare `OK`; no escape hatch ships, and the header records that adding one requires the `passed WITH OVERRIDES` verdict with it (Task 2) |
| T-251-03 | Tampering | `--self-test` fixture writes | mitigate | all writes under `fs.mkdtempSync(os.tmpdir())`, each guarded by an assertion that the target path starts with the temp root; `SEEDS_DIR` is unreachable from any write path; verified by `git status --porcelain .planning/seeds/` empty (Task 3) |
| T-251-04 | Tampering | the real-register RED drive (planting a duplicate into the live register) | mitigate | main working tree only, before Plan 02 begins, with a directory-wide md5 digest captured before and restored-to after; ⛔ never concurrent with Plan 02's migration — the serial wave order is the control (Task 3) |
| T-251-05 | Denial of service | gate runtime at every `/gsd:discuss-phase` (wired in Plan 04) | accept | one readdir plus ~291 small file reads; measured in the baseline. If it ever exceeds a second, the wiring in Plan 04 is where it is gated, not here |
| T-251-06 | Information disclosure | — | accept | the gate reads planning markdown only; no secrets, no env, no network, no product source |
| T-251-SC | Tampering | npm/pip/cargo installs | mitigate | ⛔ **zero install tasks exist in this phase.** `fs`, `path`, `crypto`, `os`, `child_process` are Node built-ins; `js-yaml`/`gray-matter` are forbidden with two independent measured reasons (RESEARCH §4.3, §2.4). The package legitimacy gate has no subject here, and an acceptance criterion greps the source to keep it that way |
</threat_model>

<verification>
1. `node scripts/check-seeds-register.cjs --self-test` → exit 0, `5/5 arms PASS`.
2. `node scripts/check-seeds-register.cjs` → exit 1 today, naming 8 `[duplicate-id]`, 5
   `[no-frontmatter]`, 27 `[unknown-status]` and the per-key `[missing-key]` counts.
3. `git status --porcelain .planning/seeds/` → empty. This plan does not change one byte of the
   register.
4. `node scripts/check-hot-file-ledger.cjs 251` → run it and record the reading. ⚠ If it reports
   `[no-row]` for `scripts/check-seeds-register.cjs`, that is a finding about the gate's own scan
   set (its WATCHED list is `backend/app/` and `frontend/src/` only) — record it in the SUMMARY,
   do NOT add a spurious ledger row for tooling.
</verification>

<success_criteria>
- One command reads the whole register, prints its derivation, and exits 0/1/2 (D-03, REG-02).
- D-04's four arms plus the count assertion are each separately driven and each separately
  re-runnable by `--self-test`; arm 4 is asserted as ABSENCE from the matched set.
- The gate cannot exit 0 over a collapsed scan set, and every skip is counted and printed.
- `251-GATE-BASELINE.md` records the pre-migration census, every figure pasted from a named command.
</success_criteria>

<output>
Create `.planning/phases/251-register-integrity/251-01-SUMMARY.md` when done, carrying the
`--self-test` transcript and the real-register RED-drive transcript verbatim.
</output>
