---
phase: 102-reusable-validation-gate-library-output-quality-gate
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [QUAL-01]

must_haves:
  truths:
    - "Migration 070 is applied to the live local DB (:54322) — the harness_audit CHECK accepts all 22 kinds"
    - "workflow_runs.is_golden_run column exists on the live DB with default false"
    - "supabase/full-schema.sql is regenerated (no reset) and reflects both changes"
    - "_AUDIT_EVENT_TYPES (code, from Plan 01) and the live CHECK are in lockstep"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated bootstrap artifact reflecting migration 070"
      contains: "is_golden_run"
  key_links:
    - from: "supabase/migrations/070_harness_validation_gate_library.sql"
      to: "live Postgres :54322 harness_audit_event_type_check"
      via: "psycopg2 / SQL editor apply"
      pattern: "publish_succeeded"
---

<objective>
[BLOCKING] Apply migration 070 (authored in Plan 01) to the live local DB and regenerate
the full-schema bootstrap artifact.

Purpose: Plan 05's publish path writes `publish_attempted`/`publish_blocked`/`publish_succeeded`/
`judge_verdict` audit receipts and `create_workflow_run(... is_golden_run=True)`. Both the
CHECK kinds and the column MUST exist on the live DB BEFORE any test or live run that depends
on them. This plan is the gate between the migration author (Plan 01) and the consumers
(Plans 03/04/05 tests + the publish path).

Output: migration 070 applied to :54322 + `supabase/full-schema.sql` regenerated + both committed.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-reusable-validation-gate-library-output-quality-gate/102-CONTEXT.md
@supabase/migrations/070_harness_validation_gate_library.sql

<interfaces>
<!-- The apply discipline is a HARD project rule (CLAUDE.md). -->
- PROJECT RULE OVERRIDE (CLAUDE.md, MANDATORY): NEVER `supabase db push` / `db reset`.
  Apply by pasting migration 070 into the Supabase SQL editor OR psycopg2-direct against
  localhost:54322 (the Phase 100 migration-068 + Phase 101.1 migration-069 precedent).
- After apply: `bash scripts/regenerate-full-schema.sh` (DEFAULT = live-DB dump, NO reset).
- The local Supabase stack must be UP (`supabase start`).
- `backend/.env` carries the DB connection; psycopg2 connects to `localhost:54322` (postgres/postgres).
- Precedent: Phase 101.1 Plan 05 applied 069 via psycopg2-direct from a script (autonomous psycopg2 acceptable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration 070 to :54322 (psycopg2-direct) + regenerate full-schema</name>
  <files>supabase/full-schema.sql (regenerated); supabase/migrations/070_harness_validation_gate_library.sql (applied, not edited)</files>
  <read_first>
    - supabase/migrations/070_harness_validation_gate_library.sql (the exact SQL to apply — authored by Plan 01)
    - supabase/migrations/069_harness_audit_emit_kinds.sql (the 101.1 precedent — same ALTER shape, applied the same way)
    - scripts/regenerate-full-schema.sh (the no-reset live-dump regen — read its header to confirm default is no-reset)
  </read_first>
  <action>
Apply migration 070 to the live local DB via psycopg2-direct (the Phase 100/101.1 precedent — autonomous psycopg2 is acceptable; no operator paste required if the stack is up). Write a small throwaway apply script OUTSIDE the uvicorn-watched `backend/` tree (e.g. under `scripts/` or a temp path — NEVER a `.py` inside `backend/app/**`, per the no-scratch-in-watched-tree rule):

1. Read `supabase/migrations/070_harness_validation_gate_library.sql` verbatim.
2. Connect psycopg2 to `localhost:54322` (dbname=postgres, user=postgres, password=postgres — the Supabase CLI local defaults; confirm against `backend/.env` / `supabase status` if they differ).
3. Execute the migration SQL in one transaction; commit.
4. Read-back assertions (psycopg2 SELECT):
   - `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='harness_audit_event_type_check'` returns a def containing all 6 new kinds (`judge_verdict`, `publish_attempted`, `publish_blocked`, `publish_succeeded`, `policy_applied`, `validator_ask_user_approved`).
   - `SELECT column_name FROM information_schema.columns WHERE table_name='workflow_runs' AND column_name='is_golden_run'` returns one row.
5. Idempotency: the ALTER DROP/ADD CONSTRAINT is re-runnable; `ADD COLUMN IF NOT EXISTS` is idempotent. A second run must succeed (or no-op) without error.

Then regenerate the bootstrap artifact: `bash scripts/regenerate-full-schema.sh` (DEFAULT no-reset live dump — do NOT pass `--reset`). This rewrites `supabase/full-schema.sql` from the live DB.

If the local Supabase stack is DOWN or psycopg2 cannot connect, STOP and surface to the operator with the exact paste-into-SQL-editor fallback (paste `070_*.sql` into the Supabase Studio SQL editor at the local URL, then run the regen). This is why the plan is `autonomous: false` — the apply may need the operator to start the stack.

Delete the throwaway apply script after the read-back passes (leave no scratch .py behind).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import psycopg2; c=psycopg2.connect(host='localhost',port=54322,dbname='postgres',user='postgres',password='postgres'); cur=c.cursor(); cur.execute(\"SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='harness_audit_event_type_check'\"); d=cur.fetchone()[0]; assert all(k in d for k in ('judge_verdict','publish_attempted','publish_blocked','publish_succeeded','policy_applied','validator_ask_user_approved')), d; cur.execute(\"SELECT 1 FROM information_schema.columns WHERE table_name='workflow_runs' AND column_name='is_golden_run'\"); assert cur.fetchone() is not None; print('OK migration 070 live')"</automated>
  </verify>
  <acceptance_criteria>
    - The live CHECK def for `harness_audit_event_type_check` contains all 6 new kinds (verify one-liner prints `OK migration 070 live`).
    - `workflow_runs.is_golden_run` column exists on :54322.
    - `supabase/full-schema.sql` contains `is_golden_run` after regen (grep).
    - No scratch `.py` left under `backend/app/**` (the apply script is deleted or under `scripts/`).
    - `supabase db push` / `db reset` was NOT used (the apply was psycopg2/SQL-editor).
  </acceptance_criteria>
  <done>Migration 070 is live on :54322 (CHECK + column); full-schema.sql regenerated; both committable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms migration 070 is live on :54322</name>
  <files>none — verification only</files>
  <read_first>
    - .planning/phases/102-reusable-validation-gate-library-output-quality-gate/102-02-PLAN.md (this plan — Task 1's read-back assertions)
  </read_first>
  <action>
CHECKPOINT (human-verify): confirm migration 070 is live before downstream plans run.
What was built: migration 070 applied to the live local DB (psycopg2-direct, no reset) + full-schema.sql regenerated.
How to verify:
  1. Confirm the local Supabase stack was up during the apply (the Task-1 verify one-liner connects to :54322).
  2. Confirm the verify command printed `OK migration 070 live`.
  3. If the stack was down and the operator had to paste into the SQL editor, confirm the paste + regen completed.
Resume signal: type "approved" once migration 070 is confirmed live on :54322 and full-schema.sql is regenerated, or describe the apply issue.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import psycopg2; c=psycopg2.connect(host='localhost',port=54322,dbname='postgres',user='postgres',password='postgres'); cur=c.cursor(); cur.execute(\"SELECT 1 FROM information_schema.columns WHERE table_name='workflow_runs' AND column_name='is_golden_run'\"); assert cur.fetchone() is not None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The operator (or the orchestrator) confirms the Task-1 verify printed `OK migration 070 live`.
    - The verify one-liner here re-confirms the `is_golden_run` column exists on :54322.
  </acceptance_criteria>
  <done>Operator approved — migration 070 confirmed live on :54322; full-schema.sql regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file -> live DB | The authored 070 SQL crosses into the live :54322 schema; a bad apply could corrupt the CHECK. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-02-01 | Tampering | a partial/failed migration apply leaving the CHECK inconsistent with `_AUDIT_EVENT_TYPES` | mitigate | Apply in one transaction; read-back assertions (constraint def + column) BEFORE declaring done; the ALTER is re-runnable (idempotent). |
| T-102-02-02 | Denial of Service | `db reset` wiping dev data | mitigate | PROJECT RULE: NEVER db push/db reset — psycopg2/SQL-editor apply + no-reset regen only (CLAUDE.md). |
</threat_model>

<verification>
- The verify one-liner prints `OK migration 070 live` (CHECK + column present on :54322).
- `supabase/full-schema.sql` regenerated (no reset), contains `is_golden_run`.
- Idempotent: a second apply run succeeds without error.
</verification>

<success_criteria>
- Migration 070 live on :54322 — the harness_audit CHECK accepts all 22 kinds + `workflow_runs.is_golden_run` exists.
- `supabase/full-schema.sql` regenerated and committed.
- Code `_AUDIT_EVENT_TYPES` (Plan 01) and the live CHECK are in lockstep.
</success_criteria>

<output>
After completion, create `.planning/phases/102-reusable-validation-gate-library-output-quality-gate/102-02-SUMMARY.md`
</output>
