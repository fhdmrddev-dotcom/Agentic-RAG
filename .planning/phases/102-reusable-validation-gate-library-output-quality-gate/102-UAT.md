---
status: testing
phase: 102-reusable-validation-gate-library-output-quality-gate
source: [102-06-SUMMARY.md, 102-07-SUMMARY.md, 102-08-SUMMARY.md, 102-09-SUMMARY.md, 102-VALIDATION.md (SC#10 manual rows)]
started: 2026-06-13T15:07:24Z
updated: 2026-06-13T15:07:24Z
re_verify: true
re_verify_reason: "Gap plans 06-09 closed CR-01/WR-01/CR-02/WR-02-04. Original verify (2026-06-12) = gaps_found. Unit tests are gateway-mocked (false-green risk) — the real acceptance bar is the LIVE publish-gate golden run (VALIDATION.md, D-05 no-mock)."
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Good output publishes — cross-provider judge verdict (CR-01 keystone)
expected: |
  Author a lint-clean workflow with a declared business_requirement + a good golden
  input, then POST /workflows/{id}/publish with harness_judge_model set in turn to a
  representative of OpenAI, Anthropic, Google, OpenRouter.

  EXPECTED: each judge renders a structured JudgeVerdict (overall_passed=true on good
  output) → publish SUCCEEDS and the definition flips to published. A Kimi/Moonshot
  coerce-tier judge still produces an honest verdict OR an honest forced-emit failure —
  never a silent empty "pass".

  WHY THIS MATTERS: before plan 06, EVERY publish blocked at blocked_stage="judge" with
  "the judge produced no verdict" because forced_emit validated the verdict against
  EmitFieldMap. CR-01 added the schema_model=JudgeVerdict seam. This test proves the
  judge path now flows a real verdict on the LIVE provider call (not the mocked unit path).
awaiting: user response

## Tests

### 1. Good output publishes — cross-provider judge verdict (CR-01 keystone)
expected: |
  POST /workflows/{id}/publish on a lint-clean workflow with a good golden input, with
  harness_judge_model set in turn to OpenAI / Anthropic / Google / OpenRouter reps.
  Each renders a real JudgeVerdict (overall_passed=true) → publish succeeds, definition
  flips to published. Kimi/Moonshot coerce-tier judge: honest verdict OR honest failure,
  never a silent empty pass. (Before CR-01: every publish blocked at "judge".)
result: pass
note: "PASS — GOOD workflow publishes live (def aec4a23f then 8025ad75 — HTTP 200 {published:true, version:1, golden_run_id}, judge_verdict overall_passed=true, publish_succeeded, definition flipped). Required FIVE fixes (GAP-01 FK + GAP-02 judge key/base_url + FINDING-03 publish-retry + FINDING-04 strict-schema + FINDING-05 base_url). CROSS-PROVIDER axis now 4/4: forced_emit judge verified live emitting a real JudgeVerdict on OpenAI (gpt-4o, gpt-4.1) ✓ + Anthropic (claude-opus-4-8) ✓ + OpenRouter (z-ai/glm-5.1, deepseek/deepseek-chat, deepseek/deepseek-r1) ✓ + Google (gemini-2.5-pro) code-OK (its 429 is a billing spend-cap on the key, NOT a code issue). ROBUSTNESS: judge emits 5/5 on a good output; a transient run-stub/no-verdict is absorbed by D-02 bounded retry (max_retries=2) + the FINDING-03 publish-stage retry — a max_retries=0 fixture flaked once, max_retries=2 published reliably."
reported: "LIVE publish of a GOOD lint-clean workflow surfaced TWO distinct blockers the mocked unit tests never hit (forced_emit/gateway are mocked). BLOCKER-1 (golden-run FK, FIXED): first publish returned blocked_stage='golden_run_error' — 'runs_parent_run_id_fkey: parent_run_id=<workflow_run id> not present in runs'. _drive_golden_run set ctx.producer_run_id = the workflow_run id (a workflow_runs row), but a harness sub-agent's parent_run_id FKs runs.run_id; the live/resume ctx build sites mint a producer-shell runs row, the publish build site did not. Fixed by minting+terminalizing a producer-shell runs row (mirrors _build_resume_context Facet C). After fix the golden run completes end-to-end. BLOCKER-2 (judge cross-provider key, OPEN — architectural): re-publish advanced to blocked_stage='structural_gate' — 'llm_judge_rubric: the judge produced no verdict'. Proven via direct open_stream replay: the judge (claude-opus-4-8/Anthropic) call raises AuthenticationError 401 invalid x-api-key because provider_gateway/anthropic.py:59-63 resolves api_key = user_settings.llm_api_key or settings.llm_api_key (the ACTIVE provider's key = OpenAI here), NEVER settings.anthropic_api_key. Injecting the real Anthropic key as llm_api_key makes the judge emit a perfect JudgeVerdict (overall_passed=true). The judge gate cannot authenticate whenever the judge model's provider != the user's active provider — i.e. it has never worked live. Driver: uat-files/uat102.py; repro: uat-files/judge_repro2.py."
severity: blocker

### 2. Judge blocks bad output (QUAL-01 hard blocker)
expected: |
  Author a lint-clean workflow whose golden run delegates the work back to the user
  (the SEED-050 trap) and POST /publish. The judge must FAIL it: response is
  blocked_stage="judge" with the per-criterion critique + the golden_run_id, and the
  definition STAYS draft. A structurally clean workflow that produces bad output cannot
  publish — this is the phase's hard blocker.
result: pass
note: "PASS both surfaces. (a) IN-RUN llm_judge_rubric validator (def b48dc838) → blocked_stage=structural_gate with a precise critique: 'a 12-step meta-methodology telling the user how to assemble the summary themselves … a direct delegation of the requested work back to the user. Does not pass.' (b) PUBLISH-STAGE judge with NO in-run validator (def 81482264 bad_nv) → blocked_stage=judge, published=false, definition stays draft. Both: the SEED-050 delegation trap is caught, bad output cannot publish. The publish-stage judge hit a transient model_failed_to_emit (FINDING-3 — no retry; honest-fail-only, never a false pass; repro proves the verdict is deterministically overall_passed=false 3/3)."

### 3. Multi-tool golden run graded end-to-end
expected: |
  Publish a workflow whose golden run uses 2+ tools (search_documents + render_template).
  The judge grades the multi-tool final output and the verdict + the structural gates
  (citations / integrity) persist as harness_audit receipts tied to the golden_run_id.
result: pass
note: "PASS after FINDING-06 fix. Multi-tool WIRING verified (def 286a2428: search_documents gathered cited risk evidence → render_template produced a 37533-byte cited /risk-register.docx). Initially blocked at judge because _judge_golden_output graded only final_output.text (a bare 'Produced the deliverable' confirmation) and couldn't see the field_map → failed grounding (FINDING-06). FIX: shared _judge_graded_text helper now appends a readable rendering of the cited field_map (value + source per cell) to the judge's graded input (both publish-stage + in-run judge). RE-VERIFIED LIVE: golden run 366c6de1 → judge overall_passed=true score=88, summary explicitly grades the cells ('each populated cell carries a citation to a named knowledge-base document, unsupported fields honestly left null and flagged [UNCITED] rather than invented') → published. The judge correctly noted its boundary (can't independently verify each citation backs its claim — the deterministic citation gate covers that per D-06)."

### 4. Parallel-thread publish does not wedge the composer
expected: |
  Start a golden-run publish on Thread A; while it streams, accept a new prompt on
  Thread B. Thread B stays responsive and answers; Thread A's publish completes or blocks
  honestly. The synchronous publish (asyncio deadline, WR-04) never wedges the UI.
result: [pending]

### 5. Long-message golden run — truncation guard holds
expected: |
  Publish a workflow whose golden run carries a long KB context / >= 50 prior messages.
  The forced_emit truncation guard REJECTS a cut-off verdict rather than passing a
  half-verdict — the verdict is either complete-and-honest or an honest forced-emit
  failure, never a silently truncated "pass".
result: [pending]

### 6. freshness ask_user pause fires live (WR-01 + GATE-01)
expected: |
  Attach a freshness validator (timing=pre, on_failure=ask_user, a small max_age_days)
  over a STALE folder, then run the workflow. The run PAUSES with a choice prompt:
  - Proceed → run continues + writes a validator_ask_user_approved receipt
  - Abort → honest fail_run
  - Unanswered → expires to an honest fail (never a hung run)
  (Before WR-01: the live path always failed "no KB scope context" because the validator
  read scope_folder_ids/folder_scope instead of folder_subtree_ids — the preflight never
  fired. This proves "check the date first" is now live.)
result: [pending]

### 7. flag / partial / draft citation delivery marks (CR-02 + GATE-01)
expected: |
  Run an emit phase under each NON-strict citation_policy and open the delivered file:
  - flag → "[unverified]" marks + a coverage summary, file IS delivered
  - partial → blanked cells (null-over-invent) + a gap list, file IS delivered
  - draft → a visible DRAFT label, file IS delivered
  No non-strict mode silently passes off unverified data as authoritative; strict still
  hard-rejects. (Before CR-02: the render gate re-ran check_coverage and rejected every
  non-strict map — flag/partial/draft never delivered.)
result: [pending]

## Summary

total: 7
passed: 2
issues: 0
pending: 5
skipped: 0
blocked: 0

findings_all_fixed_verified: "GAP-01 FK ✓; GAP-02 judge-key+base_url ✓ (native + openai-compat); FINDING-03 publish-stage retry ✓; FINDING-04 OpenAI strict-schema (evidence required) ✓; FINDING-05 cross-provider base_url ✓. All 5 verified live. 4 source files: publish_service.py (FK + retry), forced_emit.py (key + base_url), validator_kinds.py (evidence required)."
note: "Tests 1+2 PASS — the QUAL-01 gate is FUNCTIONAL and cross-provider (judge verified live on OpenAI/Anthropic/OpenRouter + Google code-OK). Good publishes, bad blocks, robust under transient flakes via bounded retry. Tests 3-7 (multi-tool, parallel, long-message, freshness pause, citation_policy delivery) use the working judge and are next."

## Gaps

- truth: "A workflow declares one business_requirement; an llm_judge gate + publish-time golden run are a HARD publish blocker — and a GOOD workflow can publish."
  status: failed
  reason: "Live publish of a GOOD workflow could not complete the golden run, then could not produce a judge verdict — two distinct live-only blockers (gateway/forced_emit are mocked in unit tests)."
  severity: blocker
  test: 1

  # GAP-102-UAT-01 — golden-run producer-shell FK (FIXED in working tree, verified)
  root_cause: "_drive_golden_run (publish_service.py:551-553) set ctx.producer_run_id = run_id (the workflow_runs id). A harness sub-agent's parent_run_id FKs runs.run_id (phase_types._build_sub_agent_parent_context:294-311, which fail-closes only on a MISSING producer_run_id, not a set-but-wrong one). The live + both resume ctx build sites mint/borrow a real runs row; the publish build site was never patched — the unpatched case the code comment explicitly warns about. Sub-agent spawn → INSERT into runs with parent_run_id=<workflow_run id> → runs_parent_run_id_fkey violation → golden_run_error."
  fix_applied: "publish_service._drive_golden_run now mints a producer-shell runs row via insert_run (status=streaming, placeholder model/provider, parent_run_id=None), sets ctx.producer_run_id to it, and terminalizes it via finalize_run in a finally (mirrors _build_resume_context Facet C). VERIFIED: golden run now completes both phases end-to-end (run fbe62c36)."
  artifacts:
    - path: "backend/app/services/harness/publish_service.py"
      issue: "producer_run_id reused the workflow_run id instead of a real runs-row id (FIXED)"

  # GAP-102-UAT-02 — judge cross-provider api-key resolution (OPEN, architectural)
  root_cause: "The provider-gateway adapters resolve api_key from the user's SINGLE active-provider key — provider_gateway/anthropic.py:59-63 `_ant_api_key = user_settings.llm_api_key or settings.llm_api_key or ''` (google.py:66 + the OpenAI path identical). They never consult the per-provider settings.<provider>_api_key (all 8 are loaded). forced_emit passes user_settings only (not GatewayRequest.api_key). The independent judge (D-03, default claude-opus-4-8/Anthropic) runs on a provider != the user's active provider (OpenAI), so the Anthropic call is handed the OpenAI key → AuthenticationError 401 invalid x-api-key → forced_emit swallows it to failure='provider_error' → the gate fails honest ('the judge produced no verdict'). Unit tests mock open_stream so they never see it. CONSEQUENCE: the publish judge gate (and any cross-provider forced_emit) cannot authenticate unless the judge model's provider == the user's active provider — the QUAL-01 hard blocker has never functioned live; the SC#10 cross-provider judge axis would fail for every non-active provider."
  proof: "Direct open_stream replay (uat-files/judge_repro2.py): (A) owner_settings (active=openai) → AuthenticationError 401 invalid x-api-key; (B) same settings with llm_api_key:=settings.anthropic_api_key → judge emits a valid JudgeVerdict (overall_passed=true, full criteria). The CR-01 JudgeVerdict schema ($defs/$ref) is fine on Anthropic — key is the sole blocker."
  proposed_fix: "Resolve the TARGET provider's key for a forced_emit/judge call. Least-invasive: in forced_emit, set GatewayRequest.api_key (or override user_settings.llm_api_key on a copy) to settings.<active_provider_name>_api_key, and have the adapters honor request.api_key first (request.api_key or user_settings.llm_api_key or settings.llm_api_key — additive, default '' keeps Deep byte-identical). DESIGN NUANCE for operator: env per-provider key vs a user's personal key for a non-active provider; must stay Deep byte-identical across ALL adapters (anthropic/google/openai/openai_compat for deepseek-moonshot-minimax-zhipu)."
  artifacts:
    - path: "backend/app/services/provider_gateway/anthropic.py"
      issue: "api_key resolved from active-provider llm_api_key only; no per-provider lookup (line 59-63)"
    - path: "backend/app/services/provider_gateway/google.py"
      issue: "same single-active-key resolution (line 66)"
    - path: "backend/app/services/forced_emit.py"
      issue: "passes user_settings only; never resolves/sets the target provider's key for a cross-provider judge shot"
  fix_applied: "forced_emit (lines ~241+) now resolves settings.<provider>_api_key for a cross-provider shot (provider != active AND a real settings model) and injects it on a user_settings copy. VERIFIED LIVE: GOOD publishes via the default Anthropic judge; the judge authenticates on Anthropic ✓ + Google ✓. INCOMPLETE for openai-compat target providers — see FINDING-05 (base_url)."
  status: fixed-for-native-providers (anthropic/google); openai-compat needs base_url (FINDING-05)

# FINDING-03 — publish-stage judge has no retry (reliability, minor)
- truth: "A transient judge no-verdict should not block a publish (a flaky emission must not reject a good workflow)."
  status: minor
  severity: minor
  test: 2
  root_cause: "publish_service._judge_golden_output runs forced_emit ONCE with no retry (forced_emit never retries by design). The in-run llm_judge_rubric gate retries ≤3 inside the gate loop (D-02), but the publish-stage judge does not. A transient model_failed_to_emit (observed live once on bad_nv; repro shows the verdict is deterministically overall_passed=false 3/3) blocks the publish at stage='judge'. SAFE (honest-fail only — never a silent pass), but a flake could block a GOOD publish."
  proposed_fix: "Wrap _judge_golden_output's forced_emit in a small bounded retry (e.g. 2-3 attempts) on a model_failed_to_emit/None result — mirrors the in-run D-02 retry; only retries a NON-verdict, never re-judges a real verdict."
  artifacts:
    - path: "backend/app/services/harness/publish_service.py"
      issue: "_judge_golden_output forced_emit single-shot, no retry on a transient no-verdict (line ~704)"

# FINDING-04 — JudgeVerdict schema incompatible with OpenAI strict structured-output
- truth: "The judge must be able to run on an OpenAI model (SC#10 cross-provider axis)."
  status: failed
  severity: major
  test: 1
  root_cause: "OpenAI strict structured-output (gpt-4o/gpt-4.1/gpt-5.x all have strict_json_schema=True) requires EVERY property to appear in `required`. JudgeVerdict.JudgeCriterionVerdict.evidence is optional (str | None = None, not in required) → OpenAI rejects the judge_verdict tool schema with 400 'Invalid schema … required … Missing evidence'. Proven via direct open_stream (uat-files/judge_repro6.py openai gpt-4o). Anthropic/Google don't enforce this → they pass."
  proposed_fix: "Make JudgeVerdict OpenAI-strict-safe: either mark `evidence` required (str, default '') so it's in `required`, OR build the judge tool schema with all-keys-required when strict (the openai forced-emit strict-schema convention)."
  artifacts:
    - path: "backend/app/services/harness/validator_kinds.py"
      issue: "JudgeCriterionVerdict.evidence optional → not in JSON-schema `required` → OpenAI strict mode 400 (line ~88-94)"

# FINDING-05 — cross-provider base_url not resolved for openai-compat target providers
- truth: "The judge must be able to run on OpenRouter (and other openai-compat providers) per the SC#10 axis."
  status: failed
  severity: major
  test: 1
  root_cause: "The openai-compat gateway path resolves base_url from the user's ACTIVE-provider settings (user_settings.llm_base_url), not the REQUEST's provider. The forced_emit key fix (GAP-02) injects the openrouter key but the call still goes to platform.openai.com → 401 'Incorrect API key … sk-or-v1…'. Affects openrouter/deepseek/moonshot/minimax/zhipu as JUDGE providers (native anthropic/google are unaffected — fixed endpoints). Proven via direct open_stream (judge_repro6.py openrouter deepseek/deepseek-chat)."
  proposed_fix: "Extend the cross-provider resolution to also set the target provider's base_url (from _PROVIDER_BASE_URLS[provider] / settings.<provider> base) for an openai-compat cross-provider forced_emit, alongside the key. Keep Deep byte-identical (active-provider path unchanged)."
  artifacts:
    - path: "backend/app/services/forced_emit.py"
      issue: "key resolved for cross-provider, but base_url still the active provider's → openai-compat target routes to the wrong endpoint"
    - path: "backend/app/services/provider_gateway/openai_compat.py"
      issue: "base_url sourced from active-provider user_settings, not the request's provider"

# FINDING-06 — judge can't grade a document/template-fill deliverable (MAJOR)
- truth: "A template-fill / document-deliverable workflow that produces a correctly-cited .docx must be able to PASS the QUAL-01 judge (the Phase 100/101 primary use case)."
  status: failed
  severity: major
  test: 3
  root_cause: "publish_service._judge_golden_output builds the judge's graded input from final_output.get('text') only (lines 674-680). For an llm_emit / render_template deliverable, .text is a bare confirmation ('Produced the filled deliverable: /risk-register.docx') — the ACTUAL cited content lives in final_output['field_map'] (value + source_chunk_id per cell), which the judge never sees. So the judge fails grounded_in_evidence + answers_business_requirement on EVERY document deliverable, blocking it at stage='judge' even when the deliverable is fully cited (verified by the emit phase's internal citation gate). Proven live: def 286a2428 golden run 9d558546 produced a 37533-byte cited .docx but the judge saw only the confirmation string and failed it."
  proposed_fix: "In _judge_golden_output, include a readable rendering of final_output['field_map'] (the filled cells + their source citations) in the graded input when present — so the judge grades the deliverable's actual content/coverage, not a content-free confirmation. (The deterministic citations_required / emit citation gate already verifies per-cell citation; the judge assesses overall quality.) DESIGN NUANCE for operator: field_map-only vs also threading the upstream retrieval evidence so the judge can verify each citation; how the judge's grounding criterion interacts with the deterministic citation gate (D-06). Same blindness affects the in-run llm_judge_rubric validator's _output_text construction if attached to an emit phase."
  artifacts:
    - path: "backend/app/services/harness/publish_service.py"
      issue: "_judge_golden_output grades final_output['text'] only; ignores final_output['field_map'] (the deliverable content) — line 674-680"
    - path: "backend/app/services/harness/validator_kinds.py"
      issue: "_output_text (used by the in-run llm_judge_rubric) likely has the same emit-deliverable blindness"
