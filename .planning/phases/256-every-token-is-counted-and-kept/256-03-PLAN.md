---
phase: 256-every-token-is-counted-and-kept
plan: 03
type: execute
wave: 2
depends_on: ["256-01"]
files_modified:
  - backend/app/api/runs.py
  - backend/app/services/harness/publish_service.py
  - backend/app/services/scheduler_service.py
  - backend/app/services/eval_runner_service.py
  - backend/tests/unit/test_256_producer_shells.py
  - backend/tests/unit/test_256_eval_usage_rollup.py
autonomous: true
requirements: [METER-05]
mutates_local_db: false

must_haves:
  truths:
    - "D-256-08: FOUR producer shells that wrote the literal `input_tokens=None` now write the segment box's real values — `api/runs.py:677` (ask_user re-drive), `api/runs.py:1331` (continuation), `publish_service.py:1671` (publish gauntlet / golden run), `scheduler_service.py:296` (scheduled run). Site 3 was closed by plan 256-01; site 7 is REGISTERED (SEED-299); site 6 is the eval path, fixed here."
    - "SC#3 verbatim: a chat run that paused for `ask_user` and was answered, and a chat run that was continued, both finish carrying real token counts — NEITHER finalize site writes input_tokens=None."
    - "Each of the five sites emits the DOCUMENTED missing-usage warning BEFORE its finalize call. ⛔ All five currently VIOLATE that contract (`db/runs.py:93-99`), so mirroring the warning is part of the fix, not an extra."
    - "An absent measurement stays `None` all the way to the column: `_box.get(\"input_tokens\")` with NO default and NO `or 0` (D-256-06). A provider that emitted no usage is a DIFFERENT fact from one that used zero tokens."
    - "D-256-03 grain: the box holds the SEGMENT's spend (reset at harness_engine.py:1844) and each shell is a per-segment `runs` row, so writing segment onto segment is correct. ⛔ No shell writes the `workflow_runs` cumulative."
    - "The eval path is FIXED via a LOCAL accumulator, not via ctx.run_usage_box — measured, that box has exactly ONE writer (harness_engine.py:1844) and the eval path never reaches it. ⛔ The producer-shell shape must NOT be copied there."
    - "The eval run's total includes BOTH graded arms because you were billed for both (D-256-12's logic); the judge shot is NOT counted and that is named in SEED-300 rather than silently folded in."
  artifacts:
    - path: "backend/tests/unit/test_256_producer_shells.py"
      provides: "per-site drives: real totals passed through, None-not-zero on an empty box, warning-before-finalize, identifier-only format string"
    - path: "backend/tests/unit/test_256_eval_usage_rollup.py"
      provides: "the eval accumulator across BOTH arms, and the explicit judge-shot exclusion"
  key_links:
    - from: "backend/app/api/runs.py"
      to: "backend/app/db/runs.py::finalize_run"
      via: "input_tokens=_box.get('input_tokens') at the ask_user re-drive and continuation shells"
      pattern: "input_tokens=_"
    - from: "backend/app/services/eval_runner_service.py"
      to: "backend/app/db/runs.py::finalize_run"
      via: "a run-local accumulator summed across both graded arms and handed to the job's finally"
      pattern: "input_tokens="
---

<objective>
Stop the producer shells throwing the number away. Four `finally:` blocks that pass the literal
`input_tokens=None` are given the live segment box's values plus the missing-usage warning the
codebase's own contract already requires of them; and the eval path — which has no `ctx` box at all
— gets a run-local accumulator across both graded arms.

Purpose: METER-05 / SC#3. ⛔ **D-256-08 corrects the requirement text AND `256-PREFLIGHT.md`:** the
requirement names TWO sites, the PREFLIGHT repeated it, and `grep -rn "input_tokens=None" backend/app`
returns **SEVEN argument sites** plus one default parameter. Fixing only the two named sites would
leave five known holes in a number Phase 257 turns into dollars, in a phase whose own goal sentence
is *"no run loses its token count."*
Output: four shells + the eval path writing real totals, five new identifier-only warnings, two
unit drives.

**This plan mutates NO database.** It is wave 2 rather than wave 1 because CLAUDE.md's measured
guidance caps **concurrent test-running agents at 2** and because the phase-level
`input_tokens=None` accounting is only checkable once plan 256-01 has closed site 3. ⚠ Its
`files_modified` set does not overlap plan 256-04's, so the two can run in parallel.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION IN A WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**Assert the base SHA** you were given (⚠ worktrees start on the default branch, `master`, stuck at
the v3.9 ship commit — five consecutive executors have had to reset).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/256-every-token-is-counted-and-kept/256-CONTEXT.md
@.planning/phases/256-every-token-is-counted-and-kept/256-RESEARCH.md
@.planning/phases/256-every-token-is-counted-and-kept/256-PATTERNS.md
@.planning/phases/256-every-token-is-counted-and-kept/256-BASELINE-backend-failing-set.txt
@CLAUDE.md
@docs/HOT-FILE-LEDGER.md

<interfaces>
<!-- The exact contract every shell writes through. Do not explore for it. -->

backend/app/db/runs.py:82
  async def finalize_run(pool, *, run_id, status, error, completed_at,
                         message_id=None, input_tokens=None, output_tokens=None) -> None
      # :93-99 — THE WARNING CONTRACT, verbatim in the source:
      #   "Callers that detect this case MUST emit
      #    logger.warning('runs.usage missing for run=%s provider=%s model=%s', ...)
      #    BEFORE calling finalize_run with None/None."
      # ⛔ input_tokens may LEGITIMATELY be None. Nullability is deliberate.

backend/app/services/run_lifecycle.py:359
  async def finalize_run_terminal(*, pool, redis, run_id, thread_id, status, error,
                                  completed_at, message_id=None,
                                  input_tokens=None, output_tokens=None) -> None
      # ⛔ A PASS-THROUGH with None DEFAULTS at :369-370, forwarding at :393.
      #    It ORIGINATES nothing. ⛔ DO NOT COPY THIS AS THE SHAPE.

The value to pass, at every shell:
  _box = getattr(ctx, "run_usage_box", None) or {}
  _in  = _box.get("input_tokens")      # int or None — ⛔ NEVER `or 0`, ⛔ NEVER a default
  _out = _box.get("output_tokens")

The warning literal, unchanged from the shipped one:
  "runs.usage missing for run=%s provider=%s model=%s"
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: The four producer shells — real segment totals plus the warning they all currently owe</name>
  <files>backend/app/api/runs.py, backend/app/services/harness/publish_service.py, backend/app/services/scheduler_service.py, backend/tests/unit/test_256_producer_shells.py</files>

  <read_first>
    - `backend/app/services/run_producer.py:229-247` — ⭐ **THE SHAPE TO COPY.** It measures, WARNS,
      then writes (`256-PATTERNS.md` §6 quotes it verbatim). ⛔ **NOT `run_lifecycle.py:386`** —
      RESEARCH.md C-5 and `256-PATTERNS.md` §6 both measure that to be `finalize_run_terminal`'s
      body, a pass-through with `None` DEFAULTS and no warning. ⚠ **CONTEXT.md's `<code_context>`
      names BOTH as "the two call sites that already pass real totals"; that register is WRONG** —
      copying `run_lifecycle.py` would reproduce the defect.
    - `backend/app/api/runs.py:611-690` — **site 1**, the ask_user re-drive. `ctx` from
      `_build_resume_context(dict(row), redis, pool)` at `:657`, passed to `_resume_run` at `:661`,
      `_pid = getattr(ctx, "producer_run_id", None)` at `:664`; the `finally` closes over `ctx`; the
      literal is at `:677-678`. `256-PATTERNS.md` §6 quotes the exact block (`:670-679`).
    - `backend/app/api/runs.py:1280-1338` — **site 2**, the continuation. `wf_ctx` built inline and
      passed to `run_workflow` at `:1312-1315`; the `finally` at `:1319` is in the same closure;
      the writer is `_finalize_run` (an alias of `db.runs.finalize_run`); the literal is at `:1331`.
    - `backend/app/services/harness/publish_service.py:1640-1677` — **site 4**, the publish
      gauntlet. `ctx` built at `:1600-1638` with `is_golden_run=True`, passed to `run_workflow` at
      `:1647`; the literal is at `:1671`. ⭐ A golden run is real money.
    - `backend/app/services/scheduler_service.py:250-303` — **site 5, the ODD ONE.** `ctx` is passed
      to `run_workflow` at `:274` and `pid` is read at `:279` behind an explicit
      `if ctx is not None`; the writer is ⚠ **`finalize_run_terminal`, NOT `finalize_run`** —
      `:282-286` explains why (the DB-only writer would leave the run in `runs:active` as a GHOST).
      The literal is at `:296`. ⛔ Handle `ctx is None` here; the other three cannot be None.
    - `backend/app/db/runs.py:82-122` — the writer and its `:93-99` warning contract.
    - `backend/tests/unit/test_token_accumulator_missing_usage.py:41`, `:44-50`, `:79-93` — the
      `_MISSING_USAGE_FORMAT` module constant, the `(None, None)` case, and the T-073-04 negative
      assertions. ⛔ **Copy its CASES and its negative-assertion discipline, NOT its method** — it
      reimplements the accumulator locally at `:23-31` and inlines the `if/else` it claims to test,
      so it pins a SHAPE and would not go red on a drift in these five sites.
    - `256-PATTERNS.md` §6 (lines 467-593) and §S-1, §S-3.
  </read_first>

  <behavior>
    Per site, in `backend/tests/unit/test_256_producer_shells.py` — four sites × three cases:
    - With a box holding `{"input_tokens": 1234, "output_tokens": 567}`, the finalize writer
      receives `input_tokens=1234, output_tokens=567`. Assert on the CALL ARGUMENTS of the patched
      writer, not on a log line.
    - With an EMPTY box (`{}`) or a `ctx` with no `run_usage_box` attribute at all, the writer
      receives `None`/`None` — ⛔ **never `0`/`0`**. This is the case `openai_compat.py:485` makes
      common rather than theoretical (*"Only emit when a usage payload was seen"*, six of eight
      providers).
    - In that same empty case a `logger.warning` fires **BEFORE** the finalize call (assert the
      ORDER, e.g. via a shared mock manager or a recorded call sequence — ⛔ not merely that both
      happened).
    - Plus, once for the module: the warning format string is exactly
      `"runs.usage missing for run=%s provider=%s model=%s"` and contains `run=%s`, `provider=%s`,
      `model=%s` while containing NONE of `tokens=`, `value=`, `usage_dict` (T-073-04).
    - Site 5 only: with `ctx is None`, the shell still finalizes, writes `None`/`None`, warns, and
      does NOT raise. ⛔ The existing `if ctx is not None` guard must not be broken.
    - Site 5 only: the writer called is still `finalize_run_terminal` — ⛔ not `finalize_run`. A
      swap would leave the run in `runs:active` as a ghost (`scheduler_service.py:282-286`).
  </behavior>

  <action>
At each of the four sites, immediately before the finalize call and inside the same `try`, read the
segment box as `_box = getattr(ctx, "run_usage_box", None) or {}` (site 5: guard on its existing
`ctx is not None` first), take `_box.get("input_tokens")` and `_box.get("output_tokens")`, emit the
missing-usage `logger.warning` when BOTH are `None`, and replace the two literal `None` arguments
with those values.

⛔ **NO default and NO `or 0` on the `.get()`** — an absent key stays `None` all the way to the
column (D-256-06; `db/runs.py:93-97`; `phase_types.py:780`). Collapsing it would make an
uninstrumented run indistinguishable from a free one, which is the exact defect Phase 257 exists to
prevent.
⛔ **The warning format string is the shipped literal**, identifier arguments only. **T-073-04
forbids token VALUES in it.** ⛔ Do not add a second format string or a variant wording — five call
sites sharing one literal is what makes the pinned assertion meaningful.
⛔ **The warning goes BEFORE the finalize call**, matching the analog's ordering.
⚠ **Grain (D-256-03):** the box holds the SEGMENT's spend and each shell is a per-segment `runs`
row, so writing segment onto segment is correct here. ⛔ **Do NOT write the `workflow_runs`
cumulative from any shell** — that is plan 256-01's writer, at a different grain, and the two must
never be summed.
⛔ **Site 5 keeps `finalize_run_terminal`.** Do not "normalise" it to `finalize_run`.

**No new branches beyond the one `if` the warning needs, and NO new state.** Arithmetic to record
per file: literal `input_tokens=None` occurrences before → after; `logger.warning(` call sites
before → after (**+1 each**); new functions (**0**); new `try`/`except` blocks (**0**).

⚠ **How RED is proven without touching the 71-name ceiling** (D-256-15, zero headroom): each RED is
recorded from a TARGETED run (`pytest tests/unit/test_256_producer_shells.py -x`), never from a
full-gate figure. The full gate runs ONCE at this plan's close and the SET is `comm`-diffed against
the committed 71 names. A test left RED at the close is a +1 above 71 and breaks the gate.
  </action>

  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_producer_shells.py -q` → passes, 0 failed.
    - `grep -c "input_tokens=None" backend/app/api/runs.py` → `0` (was `2`).
    - `grep -c "input_tokens=None" backend/app/services/harness/publish_service.py` → `0` (was `1`).
    - `grep -c "input_tokens=None" backend/app/services/scheduler_service.py` → `0` (was `1`).
    - `grep -c "runs.usage missing for run=%s provider=%s model=%s" backend/app/api/runs.py` → `2`;
      the same grep on `publish_service.py` → `1`; on `scheduler_service.py` → `1`.
    - `grep -cE "\.get\(\"input_tokens\", 0\)|\.get\('input_tokens', 0\)|input_tokens\") or 0" backend/app/api/runs.py backend/app/services/harness/publish_service.py backend/app/services/scheduler_service.py`
      → `0` in every file. ⛔ No default, no `or 0`, anywhere.
    - `grep -c "finalize_run_terminal(" backend/app/services/scheduler_service.py` → UNCHANGED from
      its pre-task value (record both).
    - `grep -c "workflow_runs" backend/app/api/runs.py` recorded before and after — no shell started
      writing the workflow grain.
    - Per file, the SUMMARY records: `input_tokens=None` before → after, `logger.warning(` before →
      after, new functions `0`, new `try` blocks `0`.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_token_accumulator_missing_usage.py -q` → still green
      (the T-073-04 pin is untouched).
  </acceptance_criteria>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_producer_shells.py tests/unit/test_token_accumulator_missing_usage.py -q</automated>
  </verify>

  <done>All four shells read the segment box with no default, pass real values or `None` (never
  `0`), and emit the shipped identifier-only warning before finalizing; site 5 still uses
  `finalize_run_terminal` and still tolerates `ctx is None`; and every count is recorded before and
  after.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: The eval path — a run-local accumulator across BOTH paid arms, with the judge shot named not hidden</name>
  <files>backend/app/services/eval_runner_service.py, backend/tests/unit/test_256_eval_usage_rollup.py</files>

  <read_first>
    - `256-RESEARCH.md` §Q1 (lines 86-135) — the verdict, the exhaustive proof that
      `ctx.run_usage_box` has exactly ONE writer, and the TWO decisions this task owes.
    - `256-PATTERNS.md` §7 (from line 596) — ⚠ **PARTIAL ANALOG ONLY: the producer-shell shape of
      §6 MUST NOT be copied here.**
    - `backend/app/services/eval_runner_service.py:561-576` — the per-arm `RunContext` built by this
      service itself; `:592` calls `run_agent_loop` DIRECTLY. It never calls `run_workflow`, so
      **no `run_usage_box` is ever set on it.**
    - `backend/app/services/eval_runner_service.py:590-594` — the totals ARE measured:
      `result.input_tokens_total` / `result.output_tokens_total` from `AgentLoopResult`.
    - `backend/app/services/eval_runner_service.py:682-683` — they are already persisted per arm to
      `eval_results.input_tokens` / `.output_tokens` (a DIFFERENT table, migration 080; unchanged
      here).
    - `backend/app/services/eval_runner_service.py:702` — ⛔ the 3-tuple `return` that DROPS them.
    - `backend/app/services/eval_runner_service.py:852-855` and `:869` — the outer collector
      `with_outcomes`, and its own comment citing D-PRD-12 for why run state is LOCAL and never a
      module global.
    - `backend/app/services/eval_runner_service.py:901-908` — the WITHOUT arm, whose return is
      *"intentionally ignored"* (OQ3) — correct for a VERDICT rollup.
    - `backend/app/services/eval_runner_service.py:648` — `_judge_eval_answer`, the third paid call.
    - `backend/app/services/eval_runner_service.py:938-948` — the broken finalize, in the job's
      `finally:`, on the companion `runs` row (`input_tokens=None` at `:946`).
    - `256-PATTERNS.md` §S-4 — declare the accumulator ABOVE the loop; ⛔ run-LOCAL, never a module
      global (`WORKER_COUNT=2` is the shipped default).
  </read_first>

  <behavior>
    In `backend/tests/unit/test_256_eval_usage_rollup.py`:
    - A job with two cases, each running a WITH arm and a WITHOUT arm, finalizes the companion
      `runs` row with the SUM of **all four** arms' `input_tokens_total` / `output_tokens_total`.
      ⭐ **Both arms count** — you were billed for both (D-256-12's logic: *"you were billed for each
      one the provider served"*). ⛔ The WITHOUT arm's deliberate exclusion at `:852-855` is a
      VERDICT-rollup narrowing and must NOT be inherited by a SPEND rollup.
    - A job where every arm reports `None`/`None` finalizes with `None`/`None` — ⛔ never `0`/`0` —
      and emits the missing-usage warning first.
    - A job with ONE arm reporting real numbers and another reporting `None` finalizes with the real
      numbers (a `None` ADDS NOTHING; it does not poison the sum, and it does not become a zero).
    - The accumulator is declared ABOVE the case loop and is per-job: two concurrently-constructed
      jobs do not see each other's totals. ⛔ Assert there is no module-global accumulator.
    - The judge shot is NOT included, and the test says so by name, so a future reader cannot mistake
      the omission for an oversight. Cross-reference `SEED-300`.
  </behavior>

  <action>
Thread a **run-local** `dict`-or-two-`int | None`-locals accumulator from `run_eval_job` down, or
widen `_run_arm`'s 3-tuple return to carry `input_tokens` / `output_tokens` — either is acceptable;
choose the one with the smaller diff and record which, with its `return`-statement count before and
after. Sum in `run_eval_job`, and hand the totals to the `finally`'s `finalize_run` at `:946`,
replacing the literal `None`s.

⛔ **Do NOT use `ctx.run_usage_box` here.** Measured: `grep -rn "run_usage_box" backend/app` shows
its ONLY assignment is `harness_engine.py:1844`, and `eval_runner_service` never calls
`run_workflow`. Reaching for the box would silently produce `None` forever, which is the quietest
possible way to fail this requirement.
⛔ **Collect the WITHOUT arm's totals too** — `:901-908` currently throws its whole return away.
This is the decision RESEARCH.md Q1 flagged as owed and it is decided here: **a spend rollup does
NOT inherit a verdict rollup's narrowing.**
⛔ **The judge shot (`:648`) is NOT counted, and that is DECIDED, not forgotten.** Its usage is
measured nowhere in the codebase, so counting it would require new instrumentation this requirement
does not ask for. **It is named in SEED-300 (plan 256-02) with a concrete re-open trigger**, and a
source comment beside the accumulator says so. ⛔ Silently folding it in — or silently omitting it
— are both the "spend figure with an unnamed hole" this phase exists to prevent.
⛔ **Run-LOCAL, never a module global** (S-4; `eval_runner_service.py:852` cites D-PRD-12 for
exactly this, and `WORKER_COUNT=2` is the shipped default).
Add the missing-usage `logger.warning` before the finalize, with the same shipped literal and
identifier-only arguments (T-073-04).
⛔ **`eval_results` (migration 080) is UNCHANGED** — the per-arm writes at `:682-683` stay exactly
as they are. This task adds a run-level rollup; it does not re-plumb the per-arm persistence.
  </action>

  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_eval_usage_rollup.py -q` → passes, 0 failed.
    - `grep -c "input_tokens=None" backend/app/services/eval_runner_service.py` → `0` (was `1`).
    - `grep -c "runs.usage missing for run=%s provider=%s model=%s" backend/app/services/eval_runner_service.py` → `1`.
    - `grep -c "run_usage_box" backend/app/services/eval_runner_service.py` → `0`. ⛔ The box is not
      reachable from this path and must not be referenced.
    - `grep -c "SEED-300" backend/app/services/eval_runner_service.py` → at least `1` (the judge-shot
      exclusion names its register entry in the source).
    - `grep -cE "^_[a-z_]*tokens|^[A-Z_]+_TOKENS" backend/app/services/eval_runner_service.py` → `0`
      at module level: ⛔ no module-global accumulator.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_eval_forced_emit.py -q` and the
      other existing eval suites → still green; record their figures.
    - `git diff backend/app/services/eval_runner_service.py | grep -c "eval_results"` → `0` (the
      per-arm writes are untouched).
    - The SUMMARY records: which threading approach was chosen and why; `return`-statement count in
      the touched function before → after; `logger.warning(` before → after; new functions; new
      module state (must be `0`).
  </acceptance_criteria>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_eval_usage_rollup.py tests/unit/test_eval_forced_emit.py -q</automated>
  </verify>

  <done>An eval job's companion `runs` row carries the summed totals of BOTH graded arms across all
  cases, `None` where nothing was measured, via a run-local accumulator that never touches
  `ctx.run_usage_box`; the judge shot's exclusion is decided, commented in the source and registered
  in SEED-300; and `eval_results` is byte-untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider stream → `ctx.run_usage_box` → `runs.input_tokens` | A provider-reported number becomes a durable per-segment figure. Influenceable only by a party who already controls the model response. |
| log sink → operator / log aggregator | Five NEW `logger.warning` call sites. A format string is the only thing standing between an identifier and a leaked value. |
| eval job → the companion `runs` row | A service-role write on a row the job created; no request-supplied id crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-256-14 | Information disclosure | the five new `logger.warning` call sites | mitigate | ⛔ **T-073-04:** the shipped literal `"runs.usage missing for run=%s provider=%s model=%s"` with IDENTIFIER arguments only. Pinned negatively (`tokens=`, `value=`, `usage_dict` all absent) by `test_token_accumulator_missing_usage.py:79-93` and re-asserted here. ⛔ One shared literal across all five sites — a variant wording makes the pin vacuous. |
| T-256-15 | Repudiation | a `0` written where the truth is UNKNOWN | mitigate | `.get()` with NO default and NO `or 0` at all five sites, asserted by grep AND by a behavioural case. ⛔ `NULL ≠ 0` is a SECURITY property here (D-256-06): collapsing them makes an uninstrumented run indistinguishable from a free one, which is repudiation of real spend. |
| T-256-16 | Tampering / integrity | writing the WORKFLOW cumulative onto a SEGMENT row | mitigate | D-256-03: each shell writes only its own segment box onto its own segment `runs` row. ⛔ Never sum across `runs` and `workflow_runs`; grep records that no shell started referencing `workflow_runs`. |
| T-256-17 | Tampering | a run-level accumulator shared across concurrent eval jobs | mitigate | Run-LOCAL only, declared above the loop (S-4). ⛔ No module global — `WORKER_COUNT=2` is the shipped default and `eval_runner_service.py:852` already cites D-PRD-12 for exactly this. Asserted by a grep for module-level token state AND a two-job isolation case. |
| T-256-18 | Repudiation | site 5 silently changing its writer | mitigate | `finalize_run_terminal(` count recorded unchanged. ⛔ A swap to `finalize_run` would leave the run in `runs:active` as a GHOST (`scheduler_service.py:282-286`) — a correctness regression disguised as a tidy-up. |
| T-256-19 | Repudiation | the eval judge shot counted nowhere | accept, NAMED | ⭐ Explicitly excluded, commented in the source, and registered in `SEED-300` with a concrete re-open trigger. ⛔ It may not be silently folded in nor silently omitted — *"a spend figure with an unnamed hole in it is worse than no figure."* |
| T-256-SC | Tampering | npm/pip/cargo installs | mitigate | ⭐ No package is installed by this plan. `git diff --numstat backend/requirements.txt frontend/package.json` is EMPTY. |
</threat_model>

<verification>
1. The two targeted suites (per task).
2. ⛔ **THE BACKEND BASELINE, ONCE, SET-DIFFED** (D-256-15, zero headroom):
   `cd backend && venv/Scripts/python -m pytest tests/unit -q --continue-on-collection-errors 2>&1 | grep "^FAILED" | sort`
   then `comm -13` and `comm -23` against `256-BASELINE-backend-failing-set.txt`. ⛔ Count with
   `grep -c`, **never `| tail`** — a `tail` keeps the count and throws the set away, which is how
   this project once published 71 when the truth was 72. Explain any name that LEFT the set too.
3. `cd backend && venv/Scripts/python -m pytest tests/unit/test_scheduler_circuit_breaker.py tests/unit/test_085_task_service.py tests/unit/test_db_runs.py -q`
   — the nearest existing suites to the touched seams.
4. `node scripts/check-hot-file-ledger.cjs 256` — read `watched: N`. All four source files this
   plan modifies HAVE rows (`api/runs.py`, `publish_service.py`, `scheduler_service.py`,
   `eval_runner_service.py`); ⚠ `publish_service.py`'s triple was measured STALE (`25` → `26`
   commits) and its correction is plan 256-02's Task 2.
5. `node scripts/check-extension-contract.cjs` — this plan modifies none of the six trigger files;
   must stay green.
6. **Frontend: SKIPPED, deliberately.** Zero `frontend/` files modified, so
   `vitest-count-gate.cjs` has no subject. ⛔ Do not run it and do not quote a figure from it
   (D-256-14 — the gate is NON-DETERMINISTIC at base, `failed 3` then `failed 0` on a byte-identical
   tree at cap 2).
</verification>

<success_criteria>
- `grep -rn "input_tokens=None" backend/app/` over THIS plan's four files → `0` occurrences
  (`api/runs.py` 2→0, `publish_service.py` 1→0, `scheduler_service.py` 1→0,
  `eval_runner_service.py` 1→0). ⚠ Phase-level, `run_reconciler.py:245` remains by DECISION
  (SEED-299) and `run_lifecycle.py:369` is a default PARAMETER, not a site.
- Five new `logger.warning` call sites, all using the ONE shipped identifier-only literal, all
  BEFORE their finalize call (order asserted, not assumed).
- Zero `.get(..., 0)` and zero `or 0` on a token read anywhere in the four files (D-256-06).
- Site 5 still calls `finalize_run_terminal` and still tolerates `ctx is None`.
- The eval rollup sums BOTH graded arms, uses no `ctx.run_usage_box`, holds no module-global state,
  leaves `eval_results` untouched, and names the judge-shot exclusion in the source with a
  `SEED-300` reference.
- The backend failing SET is byte-identical to the committed 71 names in both `comm` directions.
- The SUMMARY carries the arithmetic, not the adjective (D-256-13), per file.
</success_criteria>

<output>
Create `.planning/phases/256-every-token-is-counted-and-kept/256-03-SUMMARY.md` when done.

It MUST contain: the asserted base SHA; per-file `input_tokens=None` and `logger.warning(` counts
before → after; the chosen eval threading approach with its `return`-count delta; the
`finalize_run_terminal(` count proving site 5's writer was not swapped; the two decisions taken on
the eval path (the WITHOUT arm IS counted; the judge shot is NOT, and where it is registered); and
the backend SET diff in both directions.
</output>
