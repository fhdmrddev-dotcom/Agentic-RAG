---
phase: 256-every-token-is-counted-and-kept
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_closure_round: 1
files_modified:
  - backend/app/services/harness_engine.py
  - backend/app/services/harness/validator_kinds.py
  - backend/app/services/harness/publish_service.py
  - backend/app/db/workflows.py
  - backend/tests/unit/test_256_run_exit_usage_flush.py
  - backend/tests/unit/test_256_judge_usage_counted.py
  - .planning/seeds/SEED-300-three-token-holes-surviving-phase-256.md
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
mutates_local_db: false
requirements: [METER-03, METER-06]

must_haves:
  truths:
    - "SC#1 / METER-03 (ROADMAP verbatim): *A completed workflow / harness run has its token totals persisted — `workflow_runs` carries them, and re-reading the run after the process restarts returns the same totals the in-memory ceiling saw during the run.* ⛔ The word that failed was **the run**, not *the completed run*: a run that PAUSES on a human gate, FAILS after exhausting gate retries, or dies on a dangling `skip_to` target is a run, and today the phase that ran immediately before each of those three returns is never persisted. After this plan, every exit from the phase loop that carries a measured delta has written it."
    - "The flush is ONE new call site, not three, and it sits where every outcome arm flows THROUGH it — immediately after the phase's `try/except BaseException` block closes and BEFORE `if outcome.kind == \"pause_run\":`. New branches: **0**. New state: **0**. This is the `phase_types.py:1586` precedent applied one layer up (*\"PLACED IMMEDIATELY AFTER THE CALL, AND THE PLACEMENT IS THE WHOLE POINT … every arm below this line flows THROUGH here, so the recording needs no branch of its own\"*), and it covers a FIFTH arm nobody has written yet."
    - "⛔ The persist has exactly ONE home. `_enforce_budget`'s two lines MOVE into a nested `_flush_run_usage()` that `_enforce_budget` calls as its first statement, so the absorb+persist pair is written once and reached from three call sites. ⛔ NO `try` is added anywhere inside `_enforce_budget` — its own docstring forbids it (*\"a `try/except` here would add a branch to a function whose branch count is itself fenced\"*) and this change adds none."
    - "The headline RED case drives a **DISARMED** breaker — an interactive harness run configures neither ceiling, and `test_256_enforce_budget_persist.py`'s own header records that a scheduled-run-only fixture passed over exactly this class of defect once already. A pause fixture with a budget would repeat that mistake."
    - "SC#4 / METER-06 (ROADMAP verbatim): *The `llm_emit` / `forced_emit` usage is **either** included in the persisted totals **or** named in a register entry with a concrete re-open trigger — and which of the two is true is discoverable from the run's own totals, not from someone's memory.* Per **D-256-18 (operator ruling, Option A)**: the two in-run judge sites are **COUNTED**, so the 4-leg marker stops over-claiming by becoming TRUE rather than by lowering its claim."
    - "`validator_kinds.py:592` and `publish_service.py:1779` are **NOT symmetric**, and the plan is shaped by that measurement rather than by the review's *\"roughly two lines at each site\"*. The validator has a live in-run `ctx` carrying `run_usage_box` → `_record_run_usage(ctx, …)`, function-local import (a module-level one is an import CYCLE: `phase_types.py:84` imports `validator_kinds`). The publish judge has **NO ctx and no live box** — its golden run's box was already closed and finalized by `_drive_golden_run`'s `finally` before the judge shot happens — so it accumulates into a **caller-supplied `usage_box`** (the `task_service`/`eval_runner_service` house pattern) which the caller at `publish_service.py:424` persists with `persist_run_usage(pool, golden_run_id, …)`."
    - "⛔ Both recordings sit IMMEDIATELY AFTER the `forced_emit` call and ABOVE every failure arm. `validator_kinds.py`'s `if result.get(\"failure\") or result.get(\"emitted\") is None: return GateResult(False, …)` sits two lines below the call, and `publish_service.py:424`'s `_block` return sits below the judge call — recording under either would count the cheap outcomes and skip the expensive ones. Up to **3 billed shots** happen in `publish_service.py`'s `for _attempt in range(3)` loop and **every served shot counts**, per D-256-12 and the `forced_emit.py` ladder-accumulator precedent (accumulators init `None` never `0`, declared ABOVE the loop so a failed rung counts)."
    - "⭐ **NO NEW MIGRATION, and that is a MEASURED answer to the operator's open question, not a convenience.** Migration 182's applied `COMMENT ON COLUMN public.workflow_runs.token_coverage` delegates the legs' meaning to its code home verbatim — *\"Written from ONE module-level constant, db.workflows.TOKEN_COVERAGE_LEGS\"* — and that phrase is present in BOTH `supabase/migrations/182_workflow_runs_token_totals.sql` and `supabase/full-schema.sql` (grep-verified, 1 hit each). So the durable comment does not become false; the record belongs in the constant's own comment block, which this plan updates. ⛔ `TOKEN_COVERAGE_LEGS` stays at FOUR legs: a 5th `\"judge\"` leg would be Option B's index-predicate migration by another name, and the judge shot is not a fifth instrumentation mechanism — it is the SAME `forced_emit` drain the `\"emit\"` leg already names."
    - "`persist_run_usage`'s unconditional `token_coverage = list(TOKEN_COVERAGE_LEGS)` write is left **byte-unchanged**, and that is correct once the claim is true: the column records *\"WHICH COUNTING LEGS the instrumentation covers — NOT which legs a given run happened to use\"* (its own comment, `db/workflows.py:61-63`). The defect was a FALSE claim, not a conditional one."
    - "The `forced_emit` call-site set is re-derived and asserted as a **SET with a disposition per site, never a count** — `grep -rn \"await forced_emit(\\|= forced_emit(\" backend/app --include=*.py` measures **TEN** sites, not the two the ruling names. A new site added anywhere in `backend/app` fails the fence until it is classified, which is the only mechanism that makes SC#4's *\"discoverable, not from someone's memory\"* survive the next author."
    - "A seed is answered by EDITING the seed. `SEED-300`'s hole #2 was closed by plan 256-03 at `eval_runner_service.py:971` (`usage_acc=usage_acc`, verified in source) and its `status:` still reads `planted` — `status:` IS the index and body prose is invisible to the scan, so the frontmatter flips. Its stale `relates_to` pins (`eval_runner_service.py:648` / `:901`) are corrected to the measured `:289` / `:344` / `:971`."
    - "⛔ METER-04 and METER-05 are VERIFIED and are NOT re-opened. `test_256_token_sum_narrowing.py`'s Fence 1 and the `input_tokens=None` set must both still hold after this plan — re-measured at planning time as exactly two entries, `run_lifecycle.py:369` (a default PARAMETER) and `run_reconciler.py:245` (SEED-299, registered)."
  artifacts:
    - path: "backend/app/services/harness_engine.py"
      provides: "`_flush_run_usage()` — the ONE home of absorb+persist — called by `_enforce_budget` and once more, unconditionally, after the phase's escape block and above the outcome-arm dispatch"
      contains: "_flush_run_usage"
    - path: "backend/tests/unit/test_256_run_exit_usage_flush.py"
      provides: "CR-01's RED-driven proof on the real engine seam: a DISARMED run that pauses on a human gate, a run that reaches fail_run, and a run whose skip target is dangling, each persisting the phase that ran immediately before the exit"
    - path: "backend/app/services/harness/validator_kinds.py"
      provides: "the in-run `llm_judge_rubric` judge shot's ladder totals folded into the run-level box, above every failure arm"
      contains: "_record_run_usage"
    - path: "backend/app/services/harness/publish_service.py"
      provides: "`_judge_golden_output`'s caller-supplied `usage_box` accumulated across all three attempts, and the caller at the QUAL-01 stage persisting it onto the golden run's `workflow_runs` row"
      contains: "persist_run_usage"
    - path: "backend/tests/unit/test_256_judge_usage_counted.py"
      provides: "both judge sites driven behaviourally (a FAILED judge shot's tokens counted too), plus the ten-site `forced_emit` disposition SET fence"
    - path: "backend/app/db/workflows.py"
      provides: "the `TOKEN_COVERAGE_LEGS` comment block stating what the `\"emit\"` leg COVERS — every `forced_emit`-borne shot inside a harness run, judge shots included — and why no fifth leg and no second migration ship"
    - path: ".planning/seeds/SEED-300-three-token-holes-surviving-phase-256.md"
      provides: "status flipped to partially-answered with hole #2 recorded as closed, the stale line pins corrected, and the raised-rung residual (IN-03) named"
  key_links:
    - from: "backend/app/services/harness_engine.py::run_workflow (post-escape, pre-arm-dispatch)"
      to: "backend/app/db/workflows.py::persist_run_usage"
      via: "await _flush_run_usage() — one site, reached by the pause_run / fail_run / dangling-skip / completed arms alike"
      pattern: "_flush_run_usage\\("
    - from: "backend/app/services/harness/validator_kinds.py::_validate_llm_judge_rubric"
      to: "backend/app/services/harness/phase_types.py::_record_run_usage"
      via: "function-local import (module-level would cycle), called immediately after forced_emit and above the failure arm"
      pattern: "_record_run_usage\\(ctx,"
    - from: "backend/app/services/harness/publish_service.py (QUAL-01 stage, ~:424)"
      to: "backend/app/db/workflows.py::persist_run_usage"
      via: "a caller-supplied usage_box accumulated inside _judge_golden_output's 3-attempt loop, persisted onto golden_run_id before the _block branch"
      pattern: "persist_run_usage\\(\\s*pool"
---

<objective>
Close the two ROADMAP success criteria Phase 256 failed, and nothing else.

**SC#1 / METER-03** persists a harness run's spend at two loop-boundary sites only. Three ordinary,
first-class returns — a human-gate pause, a run that fails after exhausting gate retries, and a
dangling `skip_to` target — leave `run_workflow` without persisting the phase that just ran, and the
loss is permanent (the next segment starts with `ctx.run_usage_box = {}` and a breaker at 0, and
`D-256-03` forbids recovering it by summing across `runs` and `workflow_runs`).

**SC#4 / METER-06** counts the one `forced_emit` call site wired to `_record_run_usage` while the
coverage marker claims complete four-leg coverage over two in-run judge shots whose measured tokens
are dropped on the floor — so a run with real uncounted spend is invisible to
`idx_workflow_runs_org_coverage_incomplete`, the one access path Phase 257's METER-07 *"what it
cannot see"* view reads.

Purpose: **D-256-17** (operator) — one gap-closure round now, while the context is live, because
Phase 257 would otherwise build its headline surface on a marker already known to over-claim.
**D-256-18** (operator) — CR-02 takes **Option A: COUNT the judge spend.** Option B (narrow
`TOKEN_COVERAGE_LEGS` + a second migration altering the index predicate) was considered and
**REJECTED**; ⛔ do not silently revisit it. The reason is the phase's own premise: *every token is
counted*. A marker that is honest about not counting is a weaker deliverable than a marker with
nothing left to omit.

Output: one nested flush helper and one new call site in `harness_engine.py`; two judge sites wired,
each by the shape its own scope actually permits; a comment block in `db/workflows.py` that records
what the `"emit"` leg covers; two new test modules; `SEED-300` answered; four stale ledger rows
re-derived.

⛔ **THIS IS A CLOSURE ROUND (round 1 of the 2 G-7 allows), AND IT INTRODUCES NO NEW CAPABILITY.**
No column, no endpoint, no view, no surface, no migration. Both fixes restore a criterion already
written. If any task starts to look like a new capability, **stop and say so** rather than shipping
it here — G-7's own record is that closure rounds smuggling in features is how 15 plans became 29.

⛔ **NOT IN SCOPE, STATED SO IT CANNOT DRIFT IN:** METER-04 and METER-05 are VERIFIED. Do not
rebuild `test_256_token_sum_narrowing.py`'s Fence 1 and do not touch the five producer shells. Their
acceptance here is that they still hold, asserted below, and nothing more.

⭐ **NO MIGRATION, SO `autonomous: true`.** The operator's open question — *"decide deliberately
whether the `COMMENT ON COLUMN` moves or whether the record lives here and in `docs/`"* — is
answered by measurement in Task 2 and recorded in the constant's own home. ⛔ If your own
re-measurement contradicts that finding, STOP and surface it: a migration makes this plan
`autonomous: false` and it must not be applied silently.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION IN A WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"` — before the HEAD
assertion, before reading anything. A worktree that skipped it reports green typechecks and red
tests for reasons that look like this plan's fault.
**Assert the base SHA you were given.** ⚠ Worktrees start on the default branch (`origin/HEAD` =
`master`, stuck at the v3.9 ship commit); five consecutive executors have had to reset.

⛔ **NEVER `rm -rf` a worktree** — a recursive delete follows the junction and destroys the
operator's real 1.7 GB `venv`. Tear down with `bash scripts/teardown-worktree.sh <path>`.

⚠ **`scripts/check-extension-contract.cjs` runs on a PostToolUse hook** and `validator_kinds.py` is
one of `SEED-291`'s six trigger paths. Nothing here may make a counter, emitter, executor or
validator resolvable from data, config, a database row or a user-supplied name — and nothing here
does: every change is a call to an existing module-level function, and
`PROGRAMMATIC_VALIDATOR_REGISTRY` / `EMITTER_REGISTRY` are untouched.

⚠ **`.claude/hooks/hot-file-ledger-guard.js` runs `check-hot-file-ledger.cjs` in the turn a PLAN.md
is written.** `files_modified` above is a YAML **block list** deliberately: the gate's regex is
`files_modified:\s*\n((?:\s*-\s*.+\n)+)`, so an inline `[]` array parses to ZERO files and the gate
passes **vacuously** (the Phase 242 finding). Read the `watched:` count in its output, never the
exit code alone.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/256-every-token-is-counted-and-kept/256-CONTEXT.md
@.planning/phases/256-every-token-is-counted-and-kept/256-VERIFICATION.md
@.planning/phases/256-every-token-is-counted-and-kept/256-REVIEW.md
@.planning/phases/256-every-token-is-counted-and-kept/256-PATTERNS.md
@.planning/phases/256-every-token-is-counted-and-kept/256-BASELINE-backend-failing-set.txt
@CLAUDE.md
@docs/HOT-FILE-LEDGER.md

<interfaces>
<!-- Measured in source at the merged tree during planning. Use these directly; do not go exploring. -->

backend/app/db/workflows.py
    TOKEN_COVERAGE_LEGS: tuple[str, ...] = ("agent", "single", "batch", "emit")   # :94
    async def persist_run_usage(pool, run_id: UUID, *, input_delta: int|None,
                                output_delta: int|None) -> None                   # :2072
        # ADD-not-SET. Returns WITHOUT touching the DB on a falsy/zero delta.
        # Writes token_coverage = list(TOKEN_COVERAGE_LEGS) unconditionally. ⛔ LEAVE IT.

backend/app/services/circuit_breaker.py
    CircuitBreaker.absorb_usage_box(box: dict|None) -> tuple[int, int]
        # Returns the clamped DELTA (max(0, …) on both) and MUTATES the breaker's
        # watermark. ⚠ So it is NOT free to call twice in a row expecting the same
        # answer — the second call yields (0,0). That is what makes the flush
        # idempotent, and it is a property to RE-DERIVE, not to assume.
    CircuitBreaker.armed -> bool     # "is either ceiling configured?" — False for
                                     # every interactive harness run.

backend/app/services/harness_engine.py (inside `run_workflow`)
    :1884   ctx.run_usage_box = {}                      (per SEGMENT, in a try/except)
    :1888   async def _enforce_budget(where: str) -> None
    :1928       _d_in, _d_out = breaker.absorb_usage_box(getattr(ctx, "run_usage_box", None))
    :1929       await persist_run_usage(pool, run_id, input_delta=_d_in, output_delta=_d_out)
    :1932       if not breaker.armed: return             ⛔ THE PERSIST STAYS ABOVE THIS (D-256-04 corrected)
    :2036   await _enforce_budget("phase_boundary")      (top of the loop body)
    :2127   outcome = await _run_phase_with_gates(...)   (inside `async with` + `try`)
    :2145   except BaseException as _escape: ... :2245  raise
    :2246   ⭐ THE INSERTION POINT — 8-space indent, `while`-body level, BLANK today
    :2294   if outcome.kind == "pause_run":  ... :2308  return
    :2310   if outcome.kind == "fail_run":   ... :2336  return
    :2338   if outcome.kind == "skip_to":    ... :2377  return   (dangling-target arm)
    :2605   await _enforce_budget("phase_completed")

backend/app/services/harness/phase_types.py
    def _record_run_usage(ctx, input_tokens, output_tokens) -> None               # :768
        # No-op when the ctx has no box (`_run_usage_box` -> None -> return).
        # `if input_tokens:` / `if output_tokens:` — None AND 0 both add nothing.
    :1586   _record_run_usage(ctx, result.get("input_tokens"), result.get("output_tokens"))
        # ⭐ THE ONE WIRED CALL SITE and the shape to mirror. Read its comment: placed
        # immediately after the call so every arm below flows through it, 0 new branches.
    ⛔ :84   from app.services.harness.validator_kinds import CITATION_MARKER_GUIDANCE
        # → validator_kinds importing phase_types AT MODULE LEVEL IS A CYCLE. Function-local only.

backend/app/services/forced_emit.py
    forced_emit(...) -> dict with keys including:
        "emitted", "failure", "input_tokens": int|None, "output_tokens": int|None
        # ⚠ LADDER TOTALS, not winning-rung totals (:384-388). `None` when no usage
        # payload was ever seen — never 0.

backend/app/services/harness/validator_kinds.py
    @register_validator("llm_judge_rubric")
    async def _validate_llm_judge_rubric(output: dict, config: dict, ctx) -> GateResult   # :514
    :592   result = await forced_emit(... emitter="judge_verdict" ...)
    :593   if result.get("failure") or result.get("emitted") is None: return GateResult(False, …)
        # ⛔ THE RECORDING GOES BETWEEN 592 AND 593. Below it, a failed judge shot's
        #    billed tokens vanish.

backend/app/services/harness/publish_service.py
    async def _judge_golden_output(*, definition, final_output, pool, owner_settings=None) -> dict   # :1700
        :1776   for _attempt in range(3):
        :1779       result = await forced_emit(...)
        :1786       except Exception as e: ... continue      ⛔ a RAISED shot returns nothing to count (IN-03)
        # ⛔ NO ctx, NO run_usage_box. The golden run's box was read and finalized by
        #   _drive_golden_run's own `finally` (:1662-1693) BEFORE this function is called.
        # Returns a VERDICT dict (or {"failure": …}) that the caller parses — ⛔ do NOT
        #   smuggle token keys into it; JudgeVerdict.model_validate would reject or drop them.
    caller, stage 4 (QUAL-01), ~:424:
        verdict = await _judge_golden_output(definition=…, final_output=…, pool=pool,
                                            owner_settings=owner_settings)
        # `golden_run_id` and `pool` ARE in scope here. `golden_run_id` is a
        # workflow_runs row (created by create_workflow_run in _drive_golden_run) —
        # the correct grain for persist_run_usage (D-256-03). `_producer_id` is the
        # separate `runs` shell; ⛔ never write the workflow figure onto it.
        # ⚠ The `_block(...)` return for a failing verdict is ~10 lines BELOW — the
        #   persist must be ABOVE it or a blocked publish loses its judge spend.

backend/tests/unit/test_scheduler_circuit_breaker.py — THE ENGINE HARNESS. Import, never copy.
    _RecordingPool(*, budget: dict|None = None, rows=None)   # .calls == [(sql, args)] IN ORDER
        # budget=None  -> fetchrow returns None -> DISARMED breaker (the interactive shape)
        # budget={"metadata": {"max_tokens_per_run": N}} -> ARMED
    _rows(n)          # n pending phase rows
    _FakeRedis()
    _MeteredProvider(*, tokens_per_phase=300)  # bills through ctx.run_usage_box — THE REAL CHANNEL
    async def _drive(pool, redis, run_id, definition, execute, ctx=None)
        # patches harness_engine._execute_phase with `execute` and awaits run_workflow
    `build_workflow_definition` — a conftest fixture; see test_256_enforce_budget_persist.py

backend/tests/unit/test_256_enforce_budget_persist.py
    _USAGE_WRITE = "input_tokens = COALESCE(input_tokens, 0)"
    def _usage_writes(pool) -> list[tuple[str, tuple]]   # ⭐ copy this reader's shape

backend/app/services/harness/human_input.py:60   class HumanInputTimeout(Exception)
    # Raised by the ask_user gate; caught at harness_engine.py:1114 and converted to
    # PhaseOutcome("pause_run", …). ⭐ THE CHEAPEST WAY TO DRIVE THE PAUSE ARM: have the
    # patched _execute_phase bill the box, then raise it.
</interfaces>

<measured_corrections>
⛔ **Re-derive every figure below rather than trusting it.** This project's recorded failure mode is
a number written at one moment and quoted after the tree moved. Where your measurement disagrees
with this block, with `256-REVIEW.md`, with `256-VERIFICATION.md` or with `256-CONTEXT.md`, record
the correction **beside** the original, never over it, and say so in the SUMMARY.

1. ⛔ **THE TWO CR-02 SITES ARE NOT SYMMETRIC, and the review's *"roughly two lines at each site"*
   is TRUE AT ONE SITE ONLY.** `validator_kinds.py:592` has a live in-run `ctx`; `publish_service.py:1779`
   has no `ctx` and no live usage box, because it is called from `:424` — **after** `_drive_golden_run`
   returned AND after that function's own `finally` already read `ctx.run_usage_box` and finalized the
   producer shell. Verified by reading both functions end to end during planning.

2. ⛔ **`forced_emit` HAS TEN CALL SITES IN `backend/app`, NOT TWO.** Measured
   (`grep -rn "await forced_emit(\|= forced_emit(" backend/app --include=*.py`):
   `embedding_service.py:338` · `eval_runner_service.py:344` · `harness/phase_types.py:1561` ·
   `harness/publish_service.py:1779` · `harness/validator_kinds.py:592` ·
   `skill_proposer_service.py:392` · `skill_tuner_service.py:225` · `:283` · `:340` ·
   `workflow_authoring.py:752`. The fence asserts the **SET with a disposition each**, never a count.

3. ⛔ **`grep -c run_usage_box` is ZERO in all four non-harness host files**
   (`embedding_service.py`, `skill_proposer_service.py`, `skill_tuner_service.py`,
   `workflow_authoring.py`) and zero in `eval_runner_service.py`. Only `eval_runner_service.py`
   has a run at all, and it is a `runs` shell with a local `usage_acc` — no `workflow_runs` row and
   therefore **no `token_coverage` marker that could over-claim over it.** That is the argument for
   its disposition, and the plan must state it rather than leave the site unnamed.

4. ⚠ **`256-VERIFICATION.md` says `run_reconciler.py:236`; the measured line is `:245`.** The
   `input_tokens=None` set is exactly two entries, `run_lifecycle.py:369` (a default PARAMETER, not
   a call site) and `run_reconciler.py:245` (SEED-299, registered). `256-CONTEXT.md` D-256-08 had
   `:245` and was right. Recorded so the METER-05 acceptance criterion below is checkable.

5. ⚠ **FOUR of the five hot-file rows are STALE.** Re-derived 2026-09-19 with CLAUDE.md's recipe
   (six-digit dated quick-task buckets subtracted):

   | file | CLAUDE.md row says | **measured 2026-09-19** |
   |---|---|---|
   | `backend/app/services/harness_engine.py` | 54 / 20 / 3135 | **57 / 21 / 3215** |
   | `backend/app/services/harness/phase_types.py` | 53 / 26 / 2937 | **54 / 27 / 2954** |
   | `backend/app/db/workflows.py` | 48 / 25 / 2585 | **50 / 26 / 2677** |
   | `backend/app/services/harness/validator_kinds.py` | 14 / 6 / 762 | 14 / 6 / 762 (agrees) |
   | `backend/app/services/harness/publish_service.py` | 27 / 12 / 1830 | 27 / 12 / 1830 (agrees) |

   ⚠ `phase_types.py` is listed because its row is stale, **not** because this plan modifies it —
   it does not, and that is deliberate (a function-local import instead of an extraction).

6. ⭐ **MIGRATION 182 IS IN NEITHER `origin/master` NOR `origin/production`** (`git log --oneline
   origin/production -- supabase/migrations/182_*.sql` → empty; same for master). So the only rows
   anywhere carrying a 4-leg `token_coverage` marker over uncounted judge spend are **local dev rows
   from the last day**. The residual is real and must be NAMED, but it is not a production
   correctness debt, and that is why a retro-marking migration is not proposed.
</measured_corrections>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: CR-01 / SC#1 — one flush site so every exit from the phase loop persists the phase that just ran</name>
  <files>backend/app/services/harness_engine.py, backend/tests/unit/test_256_run_exit_usage_flush.py</files>

  <read_first>
    - `backend/app/services/harness_engine.py:1888-1945` — `_enforce_budget` in full, **docstring
      included**. ⛔ Read the two prohibitions verbatim before writing anything: *"IT RAISES
      `CircuitBreakerTrippedError` AND **NOT** `asyncio.CancelledError`"* and *"NO `try` AROUND THE
      PERSIST … a `try/except` here would add a branch to a function whose branch count is itself
      fenced."* Your change must honour both; neither is negotiable.
    - `backend/app/services/harness_engine.py:2120-2145` — the `async with cancellation_watch(...)`
      + `try:` that produces `outcome`, and `:2145-2245` — the `except BaseException as _escape:`
      block that ends in a bare `raise`. ⚠ **Confirm for yourself that line 2246 is blank and that
      `if outcome.kind == "pause_run":` at `:2294` sits at the same 8-space indent.** If the tree has
      moved, re-find the boundary; do not trust these numbers.
    - `backend/app/services/harness_engine.py:2294-2308`, `:2310-2336`, `:2338-2377` — the three
      arms. Read the pause arm's comment block on what it must NOT do.
    - `backend/app/services/circuit_breaker.py` — `absorb_usage_box`. ⛔ **Re-derive the idempotency
      claim rather than inheriting it:** the delta is clamped `max(0, …)` and the watermark is
      MUTATED, so a second call on an unchanged box returns `(0, 0)` and `persist_run_usage` returns
      before touching the DB. Prove that with a case; do not assert it in prose alone.
    - `backend/tests/unit/test_256_enforce_budget_persist.py` — **the whole file.** Its header is the
      recorded reason the headline case must drive a DISARMED breaker; `_usage_writes` is the reader
      to copy; `test_the_twice_per_phase_enforcement_writes_once_per_phase` is the case your change
      must not disturb.
    - `backend/tests/unit/test_scheduler_circuit_breaker.py:109-330` — `_FakeRedis`,
      `_RecordingPool`, `_rows`, `_MeteredProvider`, `_drive`. ⛔ **IMPORT them, never copy** —
      `_MeteredProvider` bills through the real `ctx.run_usage_box` channel and a second copy could
      drift into billing through a pipe production does not have.
    - `backend/tests/test_200_human_gate_pause.py:302-345` — ⚠ **the two source-slicing fences over
      the pause arm.** They slice `src[src.index('if outcome.kind == "pause_run":') : src.index("# ── fail_run:")]`
      and forbid `CancelledError`, `finish_run(`, `cancel_phase(`, `_expire_pending_ask_user(` inside
      it. Your flush must not trip either — and note that placing it at `:2246` puts it **outside**
      the slice entirely, which is one more reason for the single-site shape.
    - `backend/app/services/harness/human_input.py:60` — `HumanInputTimeout`, and
      `harness_engine.py:1090-1115` — where it is caught and converted to `PhaseOutcome("pause_run", …)`.
    - `backend/app/services/harness_engine.py:1120-1130` and `:1191-1212` (`_route_on_failure`) — the
      `asyncio.TimeoutError` path that reaches `fail_run` without any gate retries.
    - `256-PATTERNS.md` §3 (lines 209-275) and §S-5.
  </read_first>

  <behavior>
    New module `backend/tests/unit/test_256_run_exit_usage_flush.py`, driven through the REAL engine
    seam (`_drive` + the real `_enforce_budget`, the real `CircuitBreaker`, the real
    `persist_run_usage`, the real `ctx.run_usage_box`), asserting the SQL the pool was actually
    handed — so a writer that was called but issued nothing cannot pass.

    ⛔ **EVERY CASE BELOW MUST BE DRIVEN RED AGAINST THE CURRENT TREE FIRST**, and red for the RIGHT
    reason: `_usage_writes(pool) == []` because no persist is reachable from the arm, **not** because
    a fixture is malformed. Record the red output verbatim in the SUMMARY.

    - ⭐ **THE HEADLINE CASE — a DISARMED two-phase run that PAUSES on the human gate.**
      `_RecordingPool(rows=_rows(2))` with **no `budget`** (an interactive run configures no
      ceilings). Phase 1's patched executor bills the box (`_MeteredProvider`'s shape) and then
      raises `HumanInputTimeout`. Assert: the run reaches the pause arm (`"paused"` among the
      `workflow_runs` status writes, or `run_paused` emitted), **and** exactly one usage write
      exists carrying the phase-1 delta — non-zero, and equal to what the executor billed.
      ⛔ A version of this case with a `budget=` fixture would pass over the defect; the module
      docstring must say so, citing `test_256_enforce_budget_persist.py`'s own header.
    - **A run that reaches `fail_run`** — the worse arm, because a phase only gets there after
      `_run_phase_with_gates` exhausted its retries, so it is the most expensive phase whose spend
      was being dropped. Drive it the cheapest honest way (a patched executor that bills and then
      raises `asyncio.TimeoutError`, which `:1120` converts through `_route_on_failure` to
      `fail_run` with no retry loop). Assert the run flips to `failed` **and** the billed delta was
      persisted.
    - **A run whose `skip_to` target is dangling** — the third return. Assert the persist happened
      before the run was terminalized.
    - **A completed run's write count is UNCHANGED: N phases → N writes.** ⛔ This is the
      regression case that proves the flush did not double-book. Three phases, `_MeteredProvider`,
      exactly 3 usage writes, each a `(150, 150)` DELTA — byte-identical expectations to
      `test_256_enforce_budget_persist.py`'s existing case, asserted here too because this plan is
      what could break it.
    - **A phase that reports NO usage and then pauses writes NOTHING.** ⛔ Not `(0, 0)`. A zero here
      would read as "measured as zero" and make an uninstrumented run look free (D-256-06).
    - **A ctx that refuses `run_usage_box` pauses harmlessly** — reuse the `_NoUsageBoxCtx` shape
      from `test_256_enforce_budget_persist.py` (import it or restate it, and say which).
    - **The flush is idempotent by the watermark, driven rather than asserted:** a run that pauses
      after the `phase_completed` enforcement has already absorbed the box issues **no second
      write** — the delta is `(0, 0)` and `persist_run_usage` returns before the DB.
    - **An ARMED run still trips with `CircuitBreakerTrippedError` and not `CancelledError`**, and a
      tripped run still persists the spend that killed it. ⛔ The flush must not change which
      exception leaves the engine; the Phase-194 `cancel_phase` single-call-site fence depends on it.
  </behavior>

  <action>
    Extract the absorb+persist pair into a nested `async def _flush_run_usage() -> None` declared
    immediately above `_enforce_budget` inside `run_workflow` (it closes over `breaker`, `ctx`,
    `pool`, `run_id` exactly as `_enforce_budget` does). Its docstring states that it is the ONE
    home of the durable token write, that it is safe to call more than once because
    `absorb_usage_box` derives a delta from its own mutated watermark and `persist_run_usage`
    returns on a falsy delta, and that it writes nothing when the ctx carries no box.

    Make `_enforce_budget`'s FIRST statement `await _flush_run_usage()`, replacing the two lines at
    `:1928-1931`. ⛔ The call stays **above** the `if not breaker.armed: return` guard — that
    ordering is D-256-04 as corrected and SC#1 depends on it for every interactive run. ⛔ Add no
    `try`, no `except` and no branch inside `_enforce_budget`; its branch count must read the same
    before and after, and the SUMMARY must carry that arithmetic rather than the adjective.

    Add exactly ONE new call — `await _flush_run_usage()` — in the `while` body at 8-space indent,
    immediately after the phase's `try`/`except BaseException` block closes (the blank line at
    `:2246`) and **above** the `# ── pause_run:` comment block. Its comment states: every outcome
    arm below this line flows through it, so the flush needs no branch of its own and a FIFTH arm
    added later is covered by construction; and it names the three arms CR-01 found unreachable.

    ⛔ **Do NOT reach for a `try:`/`finally:` around the `while` loop, and record why in the
    comment rather than leaving the rejection silent.** A `finally` also runs on
    `asyncio.CancelledError`, where an unshielded `await` is itself cancelled — that would change
    which exception leaves the engine on a user Stop, which is precisely what `_enforce_budget`'s
    docstring and the Phase-194 `cancel_phase` fence exist to protect. The escape path's own
    cleanup is `asyncio.shield`-wrapped for exactly this reason. ⚠ **The consequence is a residual
    and it is named, not hidden:** a phase that CRASHES or is CANCELLED mid-work still loses its
    delta from `workflow_runs`. That is outside CR-01 (which names three *ordinary* returns) and
    outside SC#1's *"after the process restarts"* wording; it is recorded in Task 3's register work
    with a concrete trigger, never left silent.

    Correct any `file:line` pins in this module's own comments that this edit staled (`:1928`,
    `:1978`, `:2547` appear in nearby prose and in `persist_run_usage`'s docstring), recording the
    original beside the correction per S-5 where the prose is load-bearing.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_run_exit_usage_flush.py tests/unit/test_256_enforce_budget_persist.py tests/unit/test_256_persist_run_usage.py tests/unit/test_scheduler_circuit_breaker.py -q</automated>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/test_200_human_gate_pause.py tests/test_094_rc4_failure.py tests/test_096_askuser_cleanup.py tests/test_harness_engine.py tests/test_120_origin_filter.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "_flush_run_usage" backend/app/services/harness_engine.py` reports **3** (the `def`,
      the call inside `_enforce_budget`, and the one call in the `while` body). A 4th hit means the
      three-site shape crept back in; state it rather than re-baselining the number.
    - `grep -n "persist_run_usage(" backend/app/services/harness_engine.py` reports exactly **one**
      call site, inside `_flush_run_usage` — the one-home property, asserted as a SET not a count in
      the new module.
    - `python - <<` an `ast.parse` over `harness_engine.py` finds `_flush_run_usage` called
      unconditionally in `run_workflow`'s `while` body at statement level — not inside an `if`, a
      `try` or an `except`. Equivalent: the source between `raise` (end of the escape block) and
      `if outcome.kind == "pause_run":` contains `await _flush_run_usage()` and no `if`/`try`.
    - `_enforce_budget` contains **no** `try`, `except` or new conditional: its `if` count is
      identical before and after (measure both, publish both).
    - Both `<automated>` commands exit 0. ⚠ The second runs modules under `backend/tests/` which are
      **invisible to the `pytest tests/unit` baseline** — they carry the pause-arm source fences and
      must be run explicitly or the fences prove nothing here.
    - Every new case was observed RED first, for the stated reason, and the red output is quoted in
      the SUMMARY. ⛔ A case that was green on first run is not evidence and must be reported as
      such rather than counted.
    - `cd backend && venv/Scripts/python -m pytest tests/unit -q --continue-on-collection-errors`
      → the failing **SET** diffed against `.planning/phases/256-every-token-is-counted-and-kept/256-BASELINE-backend-failing-set.txt`
      is **empty in the added direction** (0 collection errors). ⛔ Diff the set with `comm`/`diff`;
      ⛔ never `| tail`, which keeps a count and throws the set away.
  </acceptance_criteria>

  <done>
    A DISARMED harness run that pauses on a human gate, one that reaches `fail_run`, and one whose
    skip target is dangling each leave `workflow_runs.input_tokens` non-NULL and equal to the spend
    of the phase that ran immediately before the exit; a completed run's write count is unchanged at
    one per phase; and the flush exists in exactly one home reached from three call sites with zero
    new branches.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: CR-02 / SC#4 — count both in-run judge shots (D-256-18 Option A), and record what the "emit" leg covers</name>
  <files>backend/app/services/harness/validator_kinds.py, backend/app/services/harness/publish_service.py, backend/app/db/workflows.py, backend/tests/unit/test_256_judge_usage_counted.py</files>

  <read_first>
    - `backend/app/services/harness/validator_kinds.py:513-610` — `_validate_llm_judge_rubric` end
      to end. ⚠ Note that the pre-computed-verdict seam at `:532` returns **before** any provider
      call, and that `:593`'s failure arm returns **two lines below** the `forced_emit` call.
    - `backend/app/services/harness/validator_kinds.py:26-32` — the module's own **Pitfall 4**
      note: `forced_emit`, `template_render_service` and `docxtpl` are function-local by contract.
      Your `_record_run_usage` import follows the same rule, and for a second reason:
      `phase_types.py:84` imports this module, so a module-level import back is a **cycle**.
    - `backend/app/services/harness/publish_service.py:1700-1810` — `_judge_golden_output` end to
      end: the 3-attempt loop at `:1776`, the `except Exception … continue` at `:1786-1789`, and the
      `JudgeVerdict.model_validate(raw).model_dump()` return. ⛔ Confirm for yourself that the
      return value is a VERDICT the caller parses, so token keys cannot ride on it.
    - `backend/app/services/harness/publish_service.py:1639-1698` — `_drive_golden_run`'s `finally`.
      ⭐ **This is the measurement that shapes the whole publish half:** it reads
      `ctx.run_usage_box` and calls `finalize_run` on `_producer_id` BEFORE `:424` runs the judge.
      The box is closed. There is no ctx to record into.
    - `backend/app/services/harness/publish_service.py:400-455` — the QUAL-01 stage. Confirm
      `golden_run_id` and `pool` are in scope and that `_block(...)` returns ~10 lines below the
      judge call.
    - `backend/app/services/harness/publish_service.py:85`, `:573`, `:789`, `:1467` — the
      established **function-local** `from app.db.workflows import …` shape in this file.
    - `backend/app/services/harness/phase_types.py:752-790` (`_run_usage_box`, `_record_run_usage`)
      and `:1561-1590` — ⭐ the ONE wired call site and its comment on why placement above the
      failure arms is the whole point. Mirror that comment's reasoning, do not paraphrase its words.
    - `backend/app/services/forced_emit.py:380-395` — the result-dict contract, and `:560-590` —
      the ladder accumulators declared ABOVE the rung loop (S-4; the precedent for counting a
      FAILED rung).
    - `backend/app/db/workflows.py:60-95` — the `TOKEN_COVERAGE_LEGS` comment block, the home the
      durable column comment delegates to. `:2072-2128` — `persist_run_usage`. ⛔ Its body is **not**
      edited by this task.
    - `supabase/migrations/182_workflow_runs_token_totals.sql` — the `COMMENT ON COLUMN
      public.workflow_runs.token_coverage` body **and** the partial index predicate at the end.
    - `backend/tests/unit/test_256_persist_run_usage.py:199-226` — the fence pinning
      `TOKEN_COVERAGE_LEGS == ("agent","single","batch","emit")` and `len == 4`. ⛔ It stays green;
      if you find yourself wanting to edit it, you are drifting into the rejected Option B.
    - `backend/tests/unit/test_validator_kinds.py` and `backend/tests/unit/test_publish_service.py`
      — the existing driving conventions for both subjects.
    - `.planning/phases/256-every-token-is-counted-and-kept/256-REVIEW.md` § CR-02 in full, and
      `256-VERIFICATION.md`'s operator ruling § D-256-18.
  </read_first>

  <behavior>
    New module `backend/tests/unit/test_256_judge_usage_counted.py`. ⛔ **Driven behaviourally
    against the real functions with `forced_emit` patched to return a known token-bearing result —
    NEVER by grepping for `_record_run_usage`.** A `_record_run_usage(ctx, None, None)` would
    satisfy a grep and ship the defect; *presence assertions cannot see content drift.*

    Each case RED first against the current tree, red because the tokens are absent from the box /
    absent from the SQL — not because a fixture is wrong.

    **The in-run validator (`validator_kinds.py`):**
    - A ctx carrying a real `run_usage_box` + a `forced_emit` returning a VALID verdict with
      `input_tokens=N, output_tokens=M` leaves the box carrying exactly `N`/`M`.
    - ⭐ **A FAILED judge shot's tokens are counted too** — `forced_emit` returns
      `{"failure": …, "emitted": None, "input_tokens": N, "output_tokens": M}`, the gate returns
      `GateResult(False, …)` **and** the box carries `N`/`M`. ⛔ This is the case that fails if the
      recording is placed below `:593`, and it is the expensive outcome.
    - `input_tokens=None, output_tokens=None` (a provider that emitted no usage) adds nothing and
      raises nothing — ⛔ not `0`.
    - A ctx with **no** box (the Deep / unit-stub / golden-run shape) is a harmless no-op and the
      gate's verdict is byte-identical to what it returns today.
    - The pre-computed-verdict seam (`output["_judge_verdict"]` present) makes no provider call and
      records nothing — asserted so the recording cannot have been hoisted above the seam.

    **The publish judge (`publish_service.py`):**
    - `_judge_golden_output` accumulates into a caller-supplied box across **all three attempts**:
      two non-verdict results followed by a valid one leaves the box carrying the SUM of all three
      shots' tokens, not the winning shot's. ⛔ The accumulator is declared ABOVE the loop and
      initialises to `None`, never `0` (S-4 / the `forced_emit` ladder precedent).
    - A shot that RAISES contributes nothing — ⚠ asserted as the **named residual** it is (IN-03),
      with the reason written in the case body: `forced_emit` returned no dict, so there is nothing
      to read. ⛔ Do not manufacture a number for it.
    - The caller at the QUAL-01 stage persists the accumulated delta onto `golden_run_id` with
      `persist_run_usage`, **and does so on the BLOCKED path too** — a verdict with
      `overall_passed: False` still issues the usage write before `_block` returns.
    - A publish whose judge reported no usage at all issues **no** usage write.

    **The disposition fence (this is what makes SC#4 survive the next author):**
    - Re-derive the `forced_emit` call-site SET from source (`ast` walk or a file+function-scoped
      regex over `backend/app/**/*.py`) and compare it against a pinned map of
      `file:function -> disposition`, where every entry is one of `COUNTED-INTO-RUN-BOX`,
      `COUNTED-VIA-PERSIST`, `NO-RUN (no runs row, no workflow_runs row, nothing to over-claim)` or
      `REGISTERED (SEED-300 hole 3 — an eval `runs` row, which carries no token_coverage marker)`.
      A site that appears, disappears or moves between files **fails** with the measured set
      printed beside the expected one. ⛔ Assert the SET, never `len(...) == 10`.
    - ⛔ **Drive this fence RED against a PLANTED eleventh call site** in a throwaway module under
      `backend/app/`, confirm it fails naming the new site, remove the plant, and **prove the
      removal by md5 / file absence — never by timing** (Phase 255's planted `eval()` sat live).
    - A companion case asserts the `"emit"` leg's meaning is written down where the durable column
      comment points: the `TOKEN_COVERAGE_LEGS` comment block in `db/workflows.py` contains a
      statement that the leg covers judge shots, and `migration 182`'s column comment still contains
      the delegating phrase *"Written from ONE module-level constant, db.workflows.TOKEN_COVERAGE_LEGS"*
      — which is the fact that makes "no second migration" correct rather than convenient.
  </behavior>

  <action>
    **`validator_kinds.py`** — immediately after the `forced_emit` call at `:592` and **above** the
    `if result.get("failure") …` arm, add a function-local `from app.services.harness.phase_types
    import _record_run_usage` and one call
    `_record_run_usage(ctx, result.get("input_tokens"), result.get("output_tokens"))`. The comment
    states: the import is function-local because `phase_types` imports this module (a cycle) AND
    because of the module's own Pitfall-4 contract; the placement is above every failure arm because
    a judge shot that produced no verdict was still served and billed; and this is the same shape as
    `phase_types.py:1586`. **New branches: 0.**

    **`publish_service.py`** — widen `_judge_golden_output` with an additive keyword-only
    `usage_box: dict | None = None` (the house caller-supplied-box pattern —
    `task_service._stream_one_iteration`, `eval_runner_service`'s `usage_acc`; `None` keeps every
    existing caller byte-identical). Declare the two accumulators ABOVE the `for _attempt in
    range(3)` loop initialised to `None`, add each served shot's `result` totals immediately after
    the call and above the verdict checks, and fold them into `usage_box` before returning — on
    EVERY return path, including `{"failure": …}`. ⛔ The returned verdict dict gains **no** token
    keys; `JudgeVerdict.model_validate` owns that shape.

    At the QUAL-01 stage (`~:424`), create the box, pass it, and immediately after the call —
    **above** the `_safe_audit` and **above** the `_block` return — persist it with a function-local
    `from app.db.workflows import persist_run_usage` onto `golden_run_id`. ⛔ `golden_run_id` and not
    `_producer_id`: the `workflow_runs` grain is the one `persist_run_usage` writes and the one
    `token_coverage` lives on (D-256-03). The comment states why the recording cannot live inside
    `_judge_golden_output` the way the validator's does — no ctx, no live box, the golden run's box
    was closed and finalized by `_drive_golden_run`'s `finally` before this line runs.

    **`db/workflows.py`** — comment-only. Extend the `TOKEN_COVERAGE_LEGS` block with a paragraph
    recording, for Phase 257 and for the next author:
      · what `"emit"` COVERS — every `forced_emit`-borne shot inside a harness run, **including the
        in-run `llm_judge_rubric` validator and the publish-gauntlet judge**, wired in this plan;
      · that **Option A was taken** per D-256-18 and that Option B (a narrowed leg set + a second
        migration altering the partial-index predicate) was **considered and REJECTED**, with the
        operator's reason: *every token is counted* — a marker honest about not counting is weaker
        than a marker with nothing left to omit;
      · why **no fifth leg and no second migration** ship: the judge shot is not a fifth
        instrumentation mechanism, and the durable `COMMENT ON COLUMN` delegates the legs' meaning
        to THIS constant verbatim, so the durable comment does not become false;
      · the named residual — a `forced_emit` shot that RAISES returns no dict, so its already-billed
        tokens are unreachable (IN-03), registered in Task 3.
    ⛔ `persist_run_usage`'s body, its SQL and the `token_coverage = $4` write stay byte-unchanged;
    `TOKEN_COVERAGE_LEGS` stays a 4-tuple.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_judge_usage_counted.py tests/unit/test_256_persist_run_usage.py tests/unit/test_256_forced_emit_usage.py tests/unit/test_256_llm_emit_rollup.py tests/unit/test_validator_kinds.py tests/unit/test_publish_service.py -q</automated>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_token_sum_narrowing.py -q &amp;&amp; grep -rn "input_tokens=None" app | sort</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "_record_run_usage" backend/app/services/harness/validator_kinds.py` reports exactly
      two hits (the function-local import and the one call), and the call's line number is **less
      than** the line number of `if result.get("failure")` in the same function — the placement
      property, checked as an ordering rather than as presence.
    - `grep -n "persist_run_usage" backend/app/services/harness/publish_service.py` reports exactly
      two hits (the function-local import and the one call), and the call's line number is **less
      than** the line of the `_block(` return in the QUAL-01 stage.
    - `grep -n "usage_box" backend/app/services/harness/publish_service.py` shows the parameter
      declared keyword-only with default `None`, and every pre-existing call site of
      `_judge_golden_output` other than the QUAL-01 one (if any exist — re-derive with
      `grep -rn "_judge_golden_output(" backend/`) is byte-unchanged.
    - `python -c "from app.db.workflows import TOKEN_COVERAGE_LEGS; print(TOKEN_COVERAGE_LEGS)"`
      prints `('agent', 'single', 'batch', 'emit')`; `git diff --stat supabase/` is **empty** and
      `ls supabase/migrations/ | grep -c "^18[3-9]"` is **0** — no migration shipped.
    - `git diff backend/app/db/workflows.py` touches **only** comment lines: the diff contains no
      change to any line inside `persist_run_usage`'s body and no change to the
      `TOKEN_COVERAGE_LEGS` assignment itself.
    - The ten-site disposition fence is present, every one of the measured sites carries a
      disposition, and it was **driven RED against a planted eleventh site**; the plant's removal is
      proven by md5 or by the file's absence, quoted in the SUMMARY. ⛔ A fence nobody has seen fire
      is not a fence.
    - The failed-judge-shot case and the three-attempt accumulation case were both observed RED
      first, with the red output quoted.
    - `tests/unit/test_256_token_sum_narrowing.py` passes (METER-04 untouched) and
      `grep -rn "input_tokens=None" backend/app | sort` returns **exactly** two lines —
      `run_lifecycle.py:369` and `run_reconciler.py:245` (METER-05 untouched). ⚠ If either figure
      differs, STOP: this plan has re-opened a verified criterion.
    - Both `<automated>` commands exit 0, and the full `pytest tests/unit` failing **SET** still
      diffs empty in the added direction against the 71-name baseline.
  </acceptance_criteria>

  <done>
    Both in-run judge shots' tokens reach a persisted total — the validator's through the run-level
    box, the publish judge's through `persist_run_usage` on the golden run's `workflow_runs` row,
    including on the blocked path and including failed attempts — so the unconditional four-leg
    `token_coverage` marker is TRUE rather than lowered; the `"emit"` leg's meaning is written where
    the durable column comment points; no migration and no fifth leg ship; and an eleventh
    `forced_emit` call site anywhere in `backend/app` now fails a fence until it is classified.
  </done>
</task>

<task type="auto">
  <name>Task 3: the registers — SEED-300 answered, four stale ledger rows re-derived, every named residual carried</name>
  <files>.planning/seeds/SEED-300-three-token-holes-surviving-phase-256.md, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>

  <read_first>
    - `.planning/seeds/SEED-300-three-token-holes-surviving-phase-256.md` in full — all three holes
      and the whole frontmatter block.
    - `.planning/seeds/TEMPLATE.md` — the frontmatter contract. ⚠ The `status` enum is a **TRIPLE**
      (TEMPLATE comment · `scripts/check-seeds-register.cjs`'s `STATUS_ENUM` · `plant-seed.md`);
      `partially-answered` is a valid value and requires `partial: true` plus a `status_note`.
    - `backend/app/services/eval_runner_service.py:888`, `:940`, `:971`, `:1008-1009` — ⛔ **verify
      the 256-03 fix is really there before flipping anything.** `:971` must read
      `usage_acc=usage_acc` with the *"you were billed for this arm too"* comment, and `:888` must
      declare the accumulator above the loop. A seed flipped over a fix that is not there is worse
      than an unflipped seed.
    - `backend/app/services/eval_runner_service.py:289` and `:344` — the judge shot
      (`_judge_eval_answer` + its `forced_emit` call), so SEED-300's stale `relates_to` pins
      (`:648`, `:901`) can be corrected to measured lines rather than guessed ones.
    - `CLAUDE.md` § "Hot-file ledger" — the row format, the recipe, and the **200-character cap on
      the disposition cell** enforced by `scripts/check-claude-md-size.cjs`.
    - `docs/HOT-FILE-LEDGER.md` — the "Scan list" table and the sections for the four files this
      plan modifies. ⚠ The **same-commit sync rule**: a row and its section move together.
    - `.planning/phases/256-every-token-is-counted-and-kept/256-REVIEW.md` § IN-03 and § WR-04.
  </read_first>

  <action>
    **SEED-300.** Flip `status: planted` → `status: partially-answered`, set `partial: true`, and
    write a `status_note` recording that **hole #2 (the eval WITHOUT arm's discarded return) was
    CLOSED by plan 256-03 at `eval_runner_service.py:971`** — verified in source, not inherited —
    while holes #1 (no per-rung attribution) and #3 (the eval judge shot counted nowhere) remain
    open with their triggers intact. Correct the stale `relates_to` pins to the measured lines
    (`eval_runner_service.py:289` / `:344` for the judge shot, `:971` for the WITHOUT arm),
    recording each original beside its correction per S-5. Leave `trigger_when` and `trigger_paths`
    otherwise intact — ⛔ a narrowed trigger is a silent deletion.

    Add to the seed body, as named residuals of THIS round rather than new findings:
      · **a `forced_emit` shot that RAISES** (`publish_service.py:1786`'s `except Exception …
        continue`) returns no dict, so its already-billed tokens are unreachable — IN-03, with the
        concrete trigger *"any phase that gives `forced_emit` a raise-path usage channel, or any
        per-rung cost breakdown"*;
      · **a harness phase that CRASHES or is CANCELLED mid-work** still loses its delta from
        `workflow_runs`, because a `try`/`finally` around the phase loop was rejected in Task 1 for
        a stated reason (an unshielded `await` in a `finally` changes which exception leaves the
        engine on a user Stop). Trigger: *"any phase that makes mid-phase spend durable, or that
        touches the escape handler's shielded cleanup."*
      · **the local-dev rows persisted between plan 256-04 and this round** carry a four-leg marker
        over uncounted judge spend. ⭐ Measured: migration 182 is in **neither `origin/master` nor
        `origin/production`**, so there is no production row in this state and no retro-marking
        migration is proposed. Trigger: *"migration 182 reaching a deploy branch with pre-round-1
        rows still present."*

    **Hot-file ledger.** Re-derive the triple for each of the four files this plan modifies with
    CLAUDE.md's recipe verbatim (⚠ subtract the six-digit dated quick-task buckets), then update the
    CLAUDE.md scan-list row **and** its `docs/HOT-FILE-LEDGER.md` section in the **same commit**.
    Publish the measured-vs-row table and mark each stale row as such — ⚠ three of the five were
    measured stale at planning time (`harness_engine.py`, `phase_types.py`, `db/workflows.py`), and
    `phase_types.py`'s row is stale even though this plan does **not** modify it, which is worth
    recording as an observation rather than fixing in silence.

    Every one of the four is a **G-5-FIRING** row, so each owes an explicit **"honoured by
    construction"** statement naming what was added and what was deliberately NOT added — ⛔ proven
    by arithmetic, not asserted (the 249-02 precedent, *"state hooks 9→9, effect hooks 10→10"*):
      · `harness_engine.py` — one nested helper, one new call, **branches 0→0** inside
        `_enforce_budget`; the `try`/`finally` seam REJECTED with its reason; no touch to the escape
        handler, `cancel_phase`, `finish_run` or the pause arm's slice.
      · `validator_kinds.py` — one function-local import + one call above the failure arm;
        **branches 0 new**; `PROGRAMMATIC_VALIDATOR_REGISTRY` and `@register_validator` untouched.
      · `publish_service.py` — one additive keyword-only parameter defaulting to `None`, two
        accumulators above an existing loop, one persist at an existing call site; **no new branch,
        no new stage word, no new `blocked_stage`**.
      · `db/workflows.py` — **comment-only**; `persist_run_usage`'s body and `TOKEN_COVERAGE_LEGS`
        byte-unchanged.
    ⛔ Keep each disposition cell **under 200 characters**; the reasons go in the detail file. That
    column reaching 45% of CLAUDE.md is what forced the 2026-08-25 split.

    **Commit discipline.** The register updates land in the SAME commit as the source edits they
    describe (the row↔section pair must move together, and an "honoured by construction" claim in a
    follow-up commit satisfies the gate while breaking the rule). TDD RED commits carrying only test
    files are fine and expected.

    **Close-out (evidence, never a claim).** Post to the agent bus `--to gemini` on `BUS-264`: the
    two criteria closed, the commands run, the RED-first observations, the measured-vs-published
    figures, and the named residuals. ⛔ Post evidence; gemini reviews. ⛔ Plan no self-verification
    that reads as an independent review — the builder does not verify (AGENTS.md 6.3).
  </action>

  <verify>
    <automated>node scripts/check-seeds-register.cjs &amp;&amp; node scripts/check-seeds-register.cjs --phase 256</automated>
    <automated>node scripts/check-hot-file-ledger.cjs 256 &amp;&amp; node scripts/check-claude-md-size.cjs &amp;&amp; node scripts/check-gap-closure-rounds.cjs 256</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "^status:\|^partial:\|^status_note:" .planning/seeds/SEED-300-*.md` shows
      `partially-answered`, `partial: true` and a non-empty `status_note`. ⚠ `status:` IS the index —
      a note in the body claiming hole 2 is closed is invisible to the sweep and does not count.
    - `node scripts/check-seeds-register.cjs` exits 0 and reports its derived scan set **non-collapsed**
      (measured at planning time: `308/308 parsed, 0 duplicate ids, 308/308 carry all 5 required
      keys` — a 0 or a collapsed count is the vacuous pass); `--phase 256` exits 0 and its matched
      list is quoted in the SUMMARY with a **leave / fold** disposition for each.
      ⚠ **Measured at planning time, so you are not discovering it from zero: `--phase 256` fires on
      SEED-266, SEED-284, SEED-290, SEED-291, SEED-292, SEED-295, SEED-296, SEED-298, SEED-300 and
      SEED-301** — the sweep reads ALL FIVE plans in the directory, so most matches belong to plans
      256-01..04 and were already routed at this phase's open. ⛔ **Write frontmatter back ONLY where
      this round CHANGES the disposition** — that is `SEED-300` and nothing else. The other nine are
      **LEAVE**, recorded as such in the SUMMARY; editing a seed this round did not answer would be
      register churn dressed as diligence, and widening scope inside a closure round is exactly what
      G-7 exists to stop.
    - `node scripts/check-hot-file-ledger.cjs 256` exits 0 **and** prints `watched:` ≥ 4 — ⛔ a zero
      or absent `watched` count is the vacuous pass (Phase 242); report the number, never the exit
      code alone.
    - `node scripts/check-claude-md-size.cjs` exits 0, with no `[disposition-too-long]`,
      `[duplicate-row]` or `[malformed-row]`.
    - `node scripts/check-gap-closure-rounds.cjs 256` reports **1** gap-closure plan and
      `highest explicit gap_closure_round: 1` — the field is present and correct, because a round-2
      decision depends on it.
    - For each of the four modified files, the SUMMARY carries: re-derived triple, the row's prior
      value, whether it was stale, and the by-construction arithmetic (counts before → after).
    - `git log --oneline -1 --stat` shows the register files landing in the same commit as the
      source files they describe.
    - The bus post exists (`bash scripts/agent-bus.sh list` shows it) and contains commands and
      measured output, not adjectives.
  </acceptance_criteria>

  <done>
    `SEED-300` states what shipped and what did not, with corrected pins and three named residuals
    each carrying a concrete trigger; four G-5-firing ledger rows carry re-derived triples and an
    arithmetic by-construction statement in both registers in one commit; all five gates exit 0 with
    non-vacuous counts reported; and gemini has the evidence rather than a claim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider gateway → `forced_emit` result dict | untrusted provider-reported token counts cross here and become a durable financial figure |
| service pool → `workflow_runs` | `persist_run_usage` runs on the **service-role** pool, which bypasses RLS; `WHERE id = $1` is the entire access boundary |
| worker A ↔ worker B (`WORKER_COUNT=2`) | two producers can reach the same run's writes concurrently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-256.05-01 | Tampering | `persist_run_usage` reached from a third call site (`_flush_run_usage`) | mitigate | parameterised `$1..$4` and `WHERE id = $1` unchanged; the writer is not edited, only reached from one more place; acceptance criteria pin `persist_run_usage(` to exactly one call site in `harness_engine.py` |
| T-256.05-02 | Repudiation | a paused/failed run's spend absent from `workflow_runs` — the ledger shows less money than was spent | mitigate | Task 1's flush, driven RED on all three arms; the `fail_run` arm is the expensive one (retries already exhausted) |
| T-256.05-03 | Information disclosure / false assurance | `token_coverage` claiming coverage it does not have, so Phase 257's blind-spot view under-reports | mitigate | Task 2 Option A makes the claim true; the ten-site disposition fence fails on an eleventh uncounted site |
| T-256.05-04 | Tampering (double-book) | the new flush re-adding a delta already persisted | mitigate | idempotency re-derived from `absorb_usage_box`'s mutated watermark + `persist_run_usage`'s falsy-delta short-circuit, and **driven** (a pause after `phase_completed` issues no second write); the N-phases→N-writes regression case is asserted |
| T-256.05-05 | Denial of service | an unshielded `await` in a `finally` on the cancel path changing which exception leaves the engine | mitigate | the `try`/`finally` shape is explicitly REJECTED with its reason recorded in source; the escape handler is not touched; the Phase-194 `cancel_phase` single-site fence and the pause-arm source fences are run explicitly |
| T-256.05-06 | Elevation of privilege | the publish judge's spend written onto the wrong grain (`_producer_id`, a `runs` row) | mitigate | acceptance criteria pin `golden_run_id`; D-256-03's never-sum-across-grains rule restated in the comment |
| T-256.05-SC | Tampering | npm/pip/cargo installs | accept | **no packages are installed by this plan.** `git diff` must leave `backend/requirements*.txt`, `package.json` and every lockfile byte-unchanged; if that is false, stop — a legitimacy gate is owed and this plan does not carry one |
</threat_model>

<verification>
Run from the repo root unless a command says otherwise.

1. **Backend baseline, the SET not the count** — `cd backend && venv/Scripts/python -m pytest
   tests/unit -q --continue-on-collection-errors`, then diff the failing node ids against
   `.planning/phases/256-every-token-is-counted-and-kept/256-BASELINE-backend-failing-set.txt` with
   `comm -13`. ⛔ Zero names in the added direction, 0 collection errors. ⛔ Never `| tail`.
   ⚠ **Do not raise the 71 ceiling for `SEED-301`** (the unit suite's real billed provider calls) —
   it is deliberately out of scope and must not be absorbed.
2. **The fences outside the baseline** — `cd backend && venv/Scripts/python -m pytest
   tests/test_200_human_gate_pause.py tests/test_094_rc4_failure.py tests/test_096_askuser_cleanup.py
   tests/test_harness_engine.py tests/test_120_origin_filter.py -q`. These live under
   `backend/tests/` and are **invisible** to step 1; they hold the pause-arm and engine-source
   fences this plan's insertion point sits beside.
3. **The two verified criteria still hold** —
   `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_token_sum_narrowing.py -q`
   (METER-04 Fence 1) and `grep -rn "input_tokens=None" backend/app | sort` → exactly
   `run_lifecycle.py:369` and `run_reconciler.py:245` (METER-05). ⚠ Either failing means a verified
   criterion was re-opened — stop and report.
4. **Guardrail gates** — `node scripts/check-hot-file-ledger.cjs 256` (read `watched:`),
   `node scripts/check-claude-md-size.cjs`, `node scripts/check-seeds-register.cjs` (+ `--phase 256`),
   `node scripts/check-gap-closure-rounds.cjs 256` (must report 1 gap plan, round 1),
   `node scripts/check-extension-contract.cjs`. All exit 0; report each one's own counts, not just
   the exit code.
5. **No schema drift** — `git status --short supabase/` is empty and no `18[3-9]_*.sql` exists.
   ⛔ If a migration turns out to be needed, this plan becomes `autonomous: false` and the operator
   pastes it into the Supabase SQL editor — never `db push` / `db reset`.
6. **Frontend untouched** — `git status --short frontend/` is empty, so the count gate is not run.
   ⚠ If the frontend is touched, run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`
   from the repo root and ⛔ never quote `failed 0` as "green at base" (`256-BASELINE-frontend.md`
   records two runs on a byte-identical tree reading `failed 3` and `failed 0`); ⛔ never reach for
   the worker cap on a red run.
7. **RED-first evidence** — every new case's red output is quoted in the SUMMARY, and the
   disposition fence's planted-eleventh-site drive is quoted with its md5 removal proof.
</verification>

<success_criteria>
- **SC#1 / METER-03:** a DISARMED harness run that pauses on a human gate, one that reaches
  `fail_run`, and one whose `skip_to` target is dangling each leave `workflow_runs.input_tokens`
  non-NULL, carrying the spend of the phase that ran immediately before the exit — driven on the
  real engine seam and RED first. A completed run's write count is unchanged at one per phase.
- **SC#4 / METER-06:** both in-run judge shots' tokens reach a persisted total (the validator's via
  the run-level box; the publish judge's via `persist_run_usage` on the golden run's `workflow_runs`
  row, on the blocked path too, summed across all served attempts), so the unconditional four-leg
  `token_coverage` marker is TRUE. Every one of the ten `forced_emit` call sites carries a
  disposition in a fence that fails on an eleventh, driven RED against a plant with md5-proved
  removal. The `"emit"` leg's meaning is recorded where the durable column comment delegates it.
- **No new capability, no new migration, no new leg.** `TOKEN_COVERAGE_LEGS` is still a 4-tuple,
  `persist_run_usage`'s body is byte-unchanged, `supabase/` is clean, and no surface, column,
  endpoint or view was added.
- **METER-04 and METER-05 are intact**, asserted (Fence 1 green; the `input_tokens=None` set exactly
  two entries), not rebuilt.
- **All gates exit 0 with non-vacuous counts reported**, and the backend failing SET diffs empty in
  the added direction against the 71-name baseline.
- **Every residual is named with a concrete trigger** in `SEED-300` — the raised rung, the crashed /
  cancelled phase, and the local-dev rows marked before this round — and `SEED-300`'s own `status:`
  reflects that hole #2 shipped.
- **Four G-5-firing ledger rows** carry re-derived triples and an arithmetic by-construction
  statement, in both registers, in the same commit as the source they describe.
- **Gemini has evidence, not a claim** (`BUS-264`), and nothing in this plan reads as the builder
  reviewing itself.
</success_criteria>

<output>
Create `.planning/phases/256-every-token-is-counted-and-kept/256-05-SUMMARY.md` when done.

It must carry, because an adjective is not evidence here:
- the RED output for every new case, verbatim, and for the planted eleventh `forced_emit` site;
- the by-construction arithmetic per modified file (counts before → after);
- the re-derived hot-file triples beside the values the rows carried, marking each stale one;
- the backend failing-SET diff (both directions), never a count;
- every measurement that disagreed with `256-REVIEW.md`, `256-VERIFICATION.md`, `256-CONTEXT.md` or
  this plan's `<measured_corrections>` block — recorded **beside** the original, never over it;
- what this round did **not** prove: no `kill -9` mid-phase, no crashed/cancelled-phase durability,
  no raised-rung token recovery, and no live cross-provider UAT of a judge shot's reported usage.
</output>
