---
phase: 256-every-token-is-counted-and-kept
plan: 04
type: execute
wave: 2
depends_on: ["256-01", "256-02"]
files_modified:
  - backend/app/services/forced_emit.py
  - backend/app/services/harness/phase_types.py
  - backend/app/services/harness_engine.py
  - backend/app/db/workflows.py
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - backend/tests/unit/test_256_forced_emit_usage.py
  - backend/tests/unit/test_256_llm_emit_rollup.py
autonomous: true
mutates_local_db: false
requirements: [METER-06]

must_haves:
  truths:
    - "D-256-11: the `llm_emit` / `forced_emit` blind spot is COUNTED, not registered. `forced_emit._drain` already receives the gateway's `usage` and `usage_delta` events and silently drops both; the two arms are mirrored from `task_service._drain:414-430`, the box and the summer already exist, and this is the LAST UNWIRED LEG."
    - "D-256-12: EVERY RUNG of the recovery ladder counts, including FAILED rungs — you were billed for each shot the provider served, whether or not it validated. Counting only the successful shot systematically under-reports the runs that cost the MOST, and the under-report is invisible."
    - "A shot that completes with NO usage payload yields `None`, never `0` — source-proven at `openai_compat.py:482-494` (*\"Only emit when a usage payload was seen\"*), which serves six of the eight providers and emits no `usage_delta` at all. ⛔ The drain's accumulators initialise to `None`."
    - "`_exec_llm_emit` records into the run-level box on BOTH the success and the FAILURE path — the early `return _emit_failure_output(...)` currently exits before any recording could happen."
    - "D-256-07 / O-4: `TOKEN_COVERAGE_LEGS` gains `\"emit\"` in the SAME COMMIT as the drain arms. ⛔ A run marked as covering `emit` before this leg shipped would be a lie in a column built to prevent lies."
    - "F-4: the source comment at `harness_engine.py:1837-1842` — *\"`llm_emit` PHASES ARE NOT COUNTED … a different file and a different plan\"* — is corrected in the same commit as the drain arms. That different plan is THIS one. A phase whose thesis is that a named hole beats a silent one must not leave a source comment claiming a hole it just closed."
    - "D-256-13 / O-6: `backend/app/services/forced_emit.py` has had NO ledger row for its ENTIRE LIFE while FIRING at 5 phases — the `config.py` failure repeating. The row lands in BOTH registers in the same commit as this file's first edit."
  artifacts:
    - path: "backend/tests/unit/test_256_forced_emit_usage.py"
      provides: "Fence 2 — behavioural drives of the REAL `forced_emit._drain` over a fake event stream, plus the multi-rung and failed-rung accumulation cases"
    - path: "backend/tests/unit/test_256_llm_emit_rollup.py"
      provides: "`_exec_llm_emit` records into the run box on the success AND failure paths"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "the forced_emit.py scan-list row and its own section"
  key_links:
    - from: "backend/app/services/forced_emit.py"
      to: "backend/app/services/harness/phase_types.py::_record_run_usage"
      via: "the 5-tuple's token fields surfaced through forced_emit's result dict and recorded by _exec_llm_emit"
      pattern: "_record_run_usage\\("
    - from: "backend/app/db/workflows.py::TOKEN_COVERAGE_LEGS"
      to: "workflow_runs.token_coverage"
      via: "the constant gains 'emit' in this commit, so a persisted marker becomes true at the moment the leg ships"
      pattern: "\"emit\""
---

<objective>
Count the last uncounted provider call. `forced_emit`'s recovery ladder makes real, billed calls
through the same `open_stream` gateway as everything else, receives the same `usage` /
`usage_delta` events, and drops them on the floor. Two arms mirrored from the canonical reader, one
accumulator declared above the rung loop, one recording line in `_exec_llm_emit` — and the two prose
registers that claim the hole is still open are corrected in the same commit.

Purpose: METER-06 / SC#4. ⛔ **D-256-11 chose COUNT over REGISTER because the measurement showed it
is ~10 lines mirroring an existing drain.** A register entry for a hole this cheap to close is a
deferral nobody would defend at Phase 257, where METER-07 would otherwise have to print *"we cannot
see `llm_emit` spend"* on most published workflows.
Output: the drain arms, the ladder accumulator, the `_exec_llm_emit` line, `"emit"` added to the
coverage constant, two corrected source comments, and `forced_emit.py`'s first ledger row.

⛔ **COMMIT DISCIPLINE — O-6 IS A SAME-COMMIT RULE AND IT CONSTRAINS HOW THIS PLAN COMMITS.**
`forced_emit.py`'s first edit (Task 1) and its ledger row (Task 3) MUST land in **ONE commit**, in
both registers. So: do NOT commit after Task 1. Stage Task 1's work, complete Tasks 2 and 3, then
make a SINGLE commit carrying `forced_emit.py` + `CLAUDE.md` + `docs/HOT-FILE-LEDGER.md` together
(Task 2's `phase_types.py` may ride along or be its own commit — only the `forced_emit` trio is
bound). ⚠ A row added in a follow-up commit satisfies the gate and breaks the rule, which is
exactly the drift the same-commit rule exists to stop.

**This plan mutates NO database.** Its `files_modified` set does not overlap plan 256-03's, so the
two run in parallel in wave 2. It is wave 2 because `harness_engine.py` and `db/workflows.py` are
owned by 256-01 in wave 1, and `CLAUDE.md` / `docs/HOT-FILE-LEDGER.md` by 256-02 — worktrees
isolate files, so a shared file means a later wave.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION IN A WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**Assert the base SHA** you were given (⚠ worktrees start on the default branch).

⚠ **`backend/app/services/harness/phase_types.py` is one of the SIX Extension Contract trigger
files.** `scripts/check-extension-contract.cjs` runs on a PostToolUse hook and WILL fire. Nothing
in this plan may make a counter, emitter, executor or validator resolvable from data, config, a
database row or a user-supplied name — and nothing here does: the change is one call to an existing
module-level function.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/256-every-token-is-counted-and-kept/256-CONTEXT.md
@.planning/phases/256-every-token-is-counted-and-kept/256-RESEARCH.md
@.planning/phases/256-every-token-is-counted-and-kept/256-PATTERNS.md
@.planning/phases/256-every-token-is-counted-and-kept/256-BASELINE-backend-failing-set.txt
@CLAUDE.md
@docs/HOT-FILE-LEDGER.md
@docs/EXTENSION-CONTRACT.md

<interfaces>
<!-- The canonical two-arm reader this plan MIRRORS, and the summer it feeds. Do not explore. -->

backend/app/services/task_service.py:414-430   (`_drain`) — THE analog. Two arms:
    the `usage` arm and the `usage_delta` arm, including the `if in_tok is None`
    first-event branch. ⛔ Copy these VERBATIM in shape; do not paraphrase them.
backend/app/services/task_service.py:451-455   — `if _in_tok is not None:` — the
    load-bearing test: a genuine measured **0** IS written into the box while an
    ABSENT measurement is not. This is the measurer's role, so the distinction is
    the whole information.

backend/app/services/harness/phase_types.py:768  (`_record_run_usage`) — the summer.
    Its own guards are `if input_tokens:` / `if output_tokens:`, so `None` AND `0`
    both add nothing; its docstring says *"None ADDS NOTHING. A provider that emitted
    no usage must not be read as zero; the two are different facts."*
    ⭐ Existing call-site precedents to mirror VERBATIM: `phase_types.py:917` and `:1028`.

backend/app/db/workflows.py (created by plan 256-01):
    TOKEN_COVERAGE_LEGS: tuple[str, ...] = ("agent", "single", "batch")
    # ⛔ THIS PLAN appends "emit" — in the same commit as the drain arms (O-4).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: forced_emit — two drain arms, a ladder accumulator above the loop, and _failure() carrying the totals</name>
  <files>backend/app/services/forced_emit.py, backend/tests/unit/test_256_forced_emit_usage.py</files>

  <read_first>
    - `backend/app/services/forced_emit.py:516-578` — `_drain`, the subject. Verify for yourself
      that it has NO `usage` and NO `usage_delta` arm and note its current return arity.
    - `backend/app/services/forced_emit.py:40-52` and `:460-473` — it imports and calls the SAME
      `open_stream` gateway as `task_service` (`stream, calling_mode = await open_stream(provider, req)`
      at `:464`, drained at `:465`), so identical events arrive. ⚠ Read `:466-473`: the exception arm
      sets `last_failure` and `continue`s — it does NOT call `_failure()`.
    - `backend/app/services/forced_emit.py:318` — `forced_emit` itself, the rung loop.
    - `backend/app/services/forced_emit.py:433-435` — ⭐ **the accumulator analog is IN THIS VERY
      FILE**: `last_failure` / `last_truncated` declared ABOVE the loop (S-4).
    - `backend/app/services/forced_emit.py:179-204` (`_failure`) and `:491-500` (the success
      return). ⛔ **CORRECTION TO RESEARCH.md, measured:** `_failure()` has **ONE** call site,
      `:507` (the exhausted-ladder floor) — `grep -rn "[^_a-z]_failure(" backend/app/` returns only
      that. RESEARCH.md §Q4's *"and from a raised-exception backstop"* is WRONG; the change is
      cheaper than it implies. Record the correction beside the original (S-5).
    - `backend/app/services/task_service.py:414-430` and `:451-455` — the arms to mirror.
    - `backend/app/services/provider_gateway/openai_compat.py:140-142` and `:482-494` — the proof a
      shot can complete with NO usage event at all (*"Only emit when a usage payload was seen"*),
      and that this adapter emits **no `usage_delta`** — it serves six of the eight providers.
    - `backend/app/services/anthropic_service.py:234-238`, `:343-347` and
      `backend/app/services/google_service.py:492`, `:496-499` — the other two emission shapes
      (two-event with a ramping delta; cumulative-per-chunk converted to a delta).
    - `backend/tests/unit/test_token_accumulator_missing_usage.py` — copy its CASES and its
      negative-assertion discipline. ⛔ **Do NOT copy its METHOD:** it reimplements the accumulator
      locally at `:23-31` and inlines the `if/else` it claims to test, so it pins a SHAPE and would
      not go red on a real drift.
    - `backend/tests/unit/test_forced_emit.py` — ⭐ the REAL-DRAIN precedent: it exercises the actual
      module. Fence 2 requires behavioural driving, not grepping for arm names.
    - `256-RESEARCH.md` §Q4 (lines 330-403) and §"Fences To Build" → Fence 2 (lines 656-663).
    - `256-PATTERNS.md` §5a / §5b (lines 334-437), §S-1, §S-4.
  </read_first>

  <behavior>
    Fence 2, in `backend/tests/unit/test_256_forced_emit_usage.py` — ⛔ **driven behaviourally
    against the REAL `forced_emit._drain` over a fake event stream, NEVER by grepping for the arm
    names.** *"Presence assertions cannot see content drift"* — an `elif et == "usage": pass` would
    satisfy a grep and ship the defect.

    - A stream emitting a single terminal `usage` event (the `openai_compat` shape: accumulated
      totals, no deltas) yields those totals from `_drain`.
    - A stream emitting an initial `usage` then several `usage_delta` events (the `anthropic` /
      `google` shape) yields the correct accumulated output total — the `if in_tok is None`
      first-event branch behaves like `task_service._drain`'s.
    - ⭐ A stream emitting **NO** usage event at all yields `(None, None)`, ⛔ **never `(0, 0)`**.
    - A stream whose `usage` reports a genuine measured **`0`** yields `0`, ⛔ not `None`. The two
      are different facts and this is the measurer's role, so the distinction is the whole
      information.
    - ⭐ **MULTI-RUNG (D-256-12):** a `forced_emit` call that takes THREE rungs — the first two
      FAILING validation, the third succeeding — returns totals summing **all three** shots. You
      were billed for each one the provider served.
    - ⭐ **EXHAUSTED LADDER:** a `forced_emit` call where EVERY rung fails returns, via `_failure()`,
      a result dict that CARRIES the token keys with the summed totals of every attempted shot. ⛔ A
      failure that reports no spend is the under-report that is invisible.
    - The success return at `:491-500` also carries the token keys.
  </behavior>

  <action>
**(a) `_drain` (`forced_emit.py:516`).** Initialise `in_tok: int | None = None` and
`out_tok: int | None = None`, and add the **two arms copied in shape from
`task_service.py:414-430`**: the `usage` arm and the `usage_delta` arm, including the
`if in_tok is None` first-event branch. Widen the return tuple to carry both.
⛔ **NEVER initialise to `0`** — that is the exact `None`-vs-`0` collapse D-256-06 forbids, one
layer up, and `openai_compat.py:485` makes the no-usage case common rather than theoretical.
⛔ Use `is not None` tests, not truthiness, wherever a measured `0` must survive.

**(b) The ladder accumulator (`forced_emit` at `:318`).** Declare two `int | None` locals **ABOVE**
the rung loop — mirroring `last_failure` / `last_truncated` at `:433-435`, which sit in this very
file (S-4) — and add each rung's drained totals into them, **including rungs that fail validation
and rungs whose exception arm at `:466-473` `continue`s** (D-256-12).
⛔ **Run-LOCAL, never a module global** (`WORKER_COUNT=2` is the shipped default).
⚠ Adding `None` to `None` must stay `None`; adding a real number to `None` must yield that number.
Write that explicitly rather than relying on operator behaviour.

**(c) Both exits carry the totals.** Attach the two token keys to the success return (`:491-500`)
**and** to `_failure()`'s dict (`:179-204`). ⛔ **CORRECTION, and record it beside the original:**
RESEARCH.md §Q4 says `_failure` is reached from two places; **measured, it has ONE call site
(`:507`)** — so pass the totals as explicit parameters from that site rather than defaulting them.
Do not add a second `_failure` call site.

**Arithmetic to record:** `_drain` `elif` arms **+2**; `return` statements in `forced_emit`
**2 → 2**; new functions **0**; new state **exactly 2 function-local ints**; `_failure(` call sites
**1 → 1**; new `try`/`except` blocks **0**.

**THE RED DRIVE — mandatory (`tdd_mode: true`; G-8 lists TDD RED drives among the four things that
may NEVER be cut):**
1. `md5sum backend/app/services/forced_emit.py` → digest #1.
2. Author the cases; run ONLY this file; record the RED from that targeted run.
3. Implement; run green.
4. **Plant violation A:** change the `usage` arm's initialisation from `None` to `0`. The
   no-usage case MUST go RED. Record it.
5. **Plant violation B:** move the accumulator INSIDE the rung loop. The multi-rung case MUST go
   RED. Record it.
6. Remove both plants; `md5sum` → digest #3, which MUST be byte-identical to digest #1 after the
   real implementation is restored. ⛔ **md5 or an armed watcher — never timing, never "I reverted
   it."** The Phase 255 `emitters.py` incident (a planted `eval()` sat live for a period) is why.
   Record every digest.

⚠ RED is always recorded from a TARGETED run, never from a full-gate figure, because the backend
ceiling is **71 with ZERO headroom** (D-256-15) and a test left RED at the close is a +1 that
breaks the gate.
  </action>

  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_forced_emit_usage.py -q` → passes, 0 failed.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_forced_emit.py tests/unit/test_103_forced_emit_strict.py tests/unit/test_111_1_forced_emit_local.py tests/unit/test_llm_emit_executor.py -q`
      → all still green; record their figures.
    - `grep -c "usage_delta" backend/app/services/forced_emit.py` → at least `1` (was `0`).
    - `grep -cE "in_tok: int \| None = None|in_tok = None" backend/app/services/forced_emit.py` → at
      least `1`. ⛔ `grep -cE "in_tok(: int)? = 0"` → `0`.
    - `grep -c "def _failure" backend/app/services/forced_emit.py` → `1`, and
      `grep -rn "[^_a-z]_failure(" backend/app/` → exactly `1` call site (unchanged).
    - `grep -c "return " backend/app/services/forced_emit.py` recorded before and after — the
      `forced_emit` function's own `return` count is `2 → 2`.
    - `grep -cE "^[A-Z_]+_TOKENS|^_?total_(in|out)" backend/app/services/forced_emit.py` → `0` at
      module level: ⛔ no module-global accumulator.
    - The SUMMARY records BOTH planted violations, the RED they produced, and all three md5 digests
      with #1 == #3.
    - The SUMMARY records the `_failure()` call-site correction BESIDE RESEARCH.md's original claim,
      never overwriting it.
  </acceptance_criteria>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_forced_emit_usage.py tests/unit/test_forced_emit.py tests/unit/test_llm_emit_executor.py -q</automated>
  </verify>

  <done>`_drain` reads both event arms with `int | None` accumulators; every rung's spend — failed
  rungs included — is summed in a function-local accumulator declared above the loop; both exits
  carry the token keys; two planted violations were each observed RED; and the file is restored
  md5-identical to its pre-plant state.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: _exec_llm_emit records into the run-level box — on the failure path too</name>
  <files>backend/app/services/harness/phase_types.py, backend/tests/unit/test_256_llm_emit_rollup.py</files>

  <read_first>
    - `backend/app/services/harness/phase_types.py:1407-1600` — `_exec_llm_emit`; the `forced_emit`
      call at `:1561`; ⚠ the early `return _emit_failure_output(...)` at `:1592`, which currently
      exits before any recording could happen.
    - `backend/app/services/harness/phase_types.py:914-917` and `:1025-1028` — ⭐ the TWO existing
      `_record_run_usage(ctx, ...)` call-site precedents. **Mirror them VERBATIM** in shape.
    - `backend/app/services/harness/phase_types.py:752-766` (`_run_usage_box`) and `:768-789`
      (`_record_run_usage`) — the `getattr`-with-safe-default reader and the summer. ⭐ Read the
      docstring at `:758-762`: *"a Deep run, a unit stub and a publish golden run all reach these
      executors with a ctx that has no box — they get `None`."* **Absence is the disarmed case and
      it is the COMMON one.**
    - `docs/EXTENSION-CONTRACT.md` — `phase_types.py` is one of the six trigger files.
    - `256-RESEARCH.md` §Q4 step 3 (lines 398-403).
    - `256-PATTERNS.md` §5c (lines 438-466).
  </read_first>

  <behavior>
    In `backend/tests/unit/test_256_llm_emit_rollup.py`:
    - On the SUCCESS path, `_exec_llm_emit` calls `_record_run_usage(ctx, <input>, <output>)` with
      the token values `forced_emit` returned, and the run-level box's totals increase by exactly
      those amounts.
    - ⭐ On the FAILURE path (the early `return _emit_failure_output(...)` at `:1592`), the recording
      STILL happens — the ladder's spend is real whether or not the emit validated (D-256-12).
    - With a `ctx` that has NO `run_usage_box` attribute (the Deep-run / unit-stub / publish
      golden-run case), `_exec_llm_emit` records nothing and raises nothing.
    - With `None` token values, the box is unchanged — `None` ADDS NOTHING, and it does not become a
      zero.
    - A run with TWO `llm_emit` phases accumulates both into the one run-level box (this is the
      METER-04 rollup path the phase does not rebuild, exercised through the new leg).
  </behavior>

  <action>
Add the recording call to `_exec_llm_emit`, mirroring `phase_types.py:917` and `:1028` verbatim in
shape: `_record_run_usage(ctx, result.get("input_tokens"), result.get("output_tokens"))`.
⛔ **It must fire on the FAILURE path too** — the early `return _emit_failure_output(...)` at
`:1592` currently exits first, so the recording must be placed above it or duplicated onto that arm.
Prefer ONE placement that both arms pass through; if that is impossible without adding a branch,
state which you chose and why in the SUMMARY, with the branch count before and after.

⛔ **`_record_run_usage` is NOT to be modified.** It is already the rollup METER-04 needs; its
`if input_tokens:` / `if output_tokens:` guards already make `None` and `0` both add nothing, and
its docstring already explains why a sub-agent keeps its own local box. **Reuse, do not touch.**
⛔ **Extension Contract:** add no dynamic dispatch, no registry lookup, no name-to-callable
resolution in this file. The change is one call to an existing module-level function.

**Arithmetic to record:** `_record_run_usage(` call sites in `phase_types.py` **2 → 3**; new
functions **0**; new branches **0** (or, if one was unavoidable, the exact before → after with the
reason); new state **0**.
  </action>

  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_llm_emit_rollup.py -q` → passes, 0 failed.
    - `grep -c "_record_run_usage(" backend/app/services/harness/phase_types.py` → `4` (the `def`
      plus three call sites; record the pre-task value, expected `3`).
    - `grep -cE "importlib|eval\s*\(|exec\s*\(|__import__" backend/app/services/harness/phase_types.py`
      → UNCHANGED from its pre-task value (record both).
    - `node scripts/check-extension-contract.cjs` → exit `0`.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_255_extension_contract_guard.py -q` → green.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_200_1_phase_output_shape.py -q` and the other
      `phase_types`-touching suites → green; record their figures.
    - `git diff backend/app/services/harness/phase_types.py | grep -c "def _record_run_usage"` → `0`
      (the summer itself is untouched).
    - The SUMMARY records `_record_run_usage(` call sites 2 → 3, new branches, and — if a branch was
      added — exactly why one placement could not serve both arms.
  </acceptance_criteria>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_llm_emit_rollup.py tests/unit/test_255_extension_contract_guard.py -q &amp;&amp; node ../scripts/check-extension-contract.cjs</automated>
  </verify>

  <done>`_exec_llm_emit` records the emit leg's spend into the run-level box on both the success and
  the failure path, `_record_run_usage` itself is byte-untouched, the extension-contract gate is
  green, and the call-site arithmetic is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Make the registers true — "emit" in the coverage constant, the stale comment corrected, and forced_emit's first ledger row</name>
  <files>backend/app/db/workflows.py, backend/app/services/harness_engine.py, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>

  <read_first>
    - `backend/app/db/workflows.py` — `TOKEN_COVERAGE_LEGS` as plan 256-01 created it.
    - `backend/app/services/harness_engine.py:1837-1842` — the comment this task falsifies, quoted
      in `256-PATTERNS.md` §3 verbatim: *"⚠ `llm_emit` PHASES ARE NOT COUNTED, AND THAT IS NAMED
      RATHER THAN HIDDEN. `forced_emit` measures no usage anywhere in its own module … Wiring it
      means instrumenting the forcing seam, which is a different file and a different plan."*
      ⭐ **That different plan is THIS one.**
    - `256-PATTERNS.md` §S-5 — record the CORRECTION beside the original; retire a claim
      DELIBERATELY. Precedents: `db/workflows.py:2172-2190`; `test_189_no_egress.py:246-259`
      (D-206-07's conscious retirement, the reason written into the body).
    - `CLAUDE.md` §"Hot-file ledger" — the scan-list table, the same-commit sync rule, the
      **200-character** disposition cap.
    - `CLAUDE.md` §"CLAUDE.md context budget (MANDATORY)" — the **150,000-CHARACTER** gate.
    - `docs/HOT-FILE-LEDGER.md` — the `## Scan list` section and an existing per-file section as a
      style model.
    - `256-RESEARCH.md` §"G-5 Arithmetic" and §"Plan-Ordering Consequences" O-4 / O-6.
  </read_first>

  <action>
**(a) O-4 — `TOKEN_COVERAGE_LEGS` gains `"emit"`, in THIS commit.** Append `"emit"` to the tuple in
`backend/app/db/workflows.py`, in the same commit as Task 1's drain arms. ⛔ **Not earlier.** A run
whose `token_coverage` says `emit` before this leg shipped is a lie in a column built specifically
to prevent lies (D-256-07 / SC#4).

⭐ **STATE U-5's READING IN THE CONSTANT'S COMMENT, or Phase 257 will guess.** The decision, taken
here: **`token_coverage` records what the INSTRUMENTATION covers, and it is written only when a real
delta is persisted.** Consequences to write down beside the constant:
  · A run with LLM phases whose provider emitted no usage, and an ALL-`programmatic` workflow run
    with no LLM phase at all, both read `input_tokens IS NULL · output_tokens IS NULL ·
    token_coverage IS NULL`.
  · ⛔ **Phase 257 must read that NULL triple as *"no instrumented leg reported usage for this
    run"* — NEVER as `$0.00` and never as "unrated".**
  · The reason this reading was chosen over writing a full marker with a real `0`/`0`: it preserves
    the delta-derived idempotency that makes the write safe with **no key, no lock and no upsert**,
    and it adds **zero branches**, which is what D-256-13's by-construction claim requires. `NULL`
    is already the "never measured" state by D-256-06, and a run with nothing to measure genuinely
    measured nothing.
  · The migration-182 partial index treats a NULL marker as *not fully covered*, which is correct
    under this reading.

**(b) F-4 — correct the `harness_engine.py:1837-1842` comment, in THIS commit.** Rewrite it to say
that the `llm_emit` leg IS now counted, by which mechanism (`forced_emit._drain`'s two arms →
`forced_emit`'s ladder accumulator → `_exec_llm_emit`'s `_record_run_usage` → `ctx.run_usage_box` →
`persist_run_usage`), and **keep the original text quoted beside the correction rather than deleting
it** (S-5) — the project's standing rule, and the reason the original is worth keeping is that it
was a hole NAMED rather than hidden, in a register (a source comment) that nothing sweeps.
⛔ A phase whose thesis is *"a named hole beats a silent one"* must not leave a source comment
claiming a hole it just closed.
⚠ The weaker stale passage at `:1826` is **plan 256-01's** (it corrects the `input_tokens=None`
count). If 256-01 has already landed, do not re-edit it; if the two collide at merge, 256-01's
version wins and this plan's SUMMARY says so.

**(c) O-6 / D-256-13 — `backend/app/services/forced_emit.py`'s ledger row, in THIS commit.** It is
**FIRING at 5 phases and has had no row for its ENTIRE LIFE** — the `config.py` failure repeating,
and `check-hot-file-ledger.cjs 256` fires `[no-row]` on it today.
⛔ **RE-DERIVE the triple; do not copy `8 / 5 / 578` from CONTEXT.md or RESEARCH.md.** Use CLAUDE.md's
own recipe and drop six-digit dated quick-task buckets. Add the scan-list row in `CLAUDE.md` **and**
the file's own section in `docs/HOT-FILE-LEDGER.md`, **in the SAME COMMIT** (same-commit sync rule).
⛔ **Disposition cell ≤ 200 characters** — verdict in the cell, reasons in the section.
The verdict: honoured by construction (256-04) — two `elif` arms mirrored into an existing drain,
two function-local ints, `_failure()` gains parameters; new functions 0, new branches beyond the
arms 0. The section names the binding invariants: ⛔ the accumulators initialise to `None`, never
`0`; ⛔ they are declared ABOVE the rung loop, so a FAILED rung's spend is still counted; and
⛔ `_failure()` has exactly ONE call site (`:507`) — a second would need the totals threaded again.

Then run `node scripts/check-claude-md-size.cjs`. ⚠ Hard limit **150,000 characters**, warn band
**120,000**; measure with the gate, ⛔ never with `wc -c` (they disagree by thousands — multi-byte
glyphs).
  </action>

  <acceptance_criteria>
    - `grep -c '"emit"' backend/app/db/workflows.py` → at least `1`, and the tuple has exactly FOUR
      members: `grep -o 'TOKEN_COVERAGE_LEGS[^)]*)' backend/app/db/workflows.py` shows
      `("agent", "single", "batch", "emit")`.
    - The `TOKEN_COVERAGE_LEGS` comment contains the words `NULL` and `$0.00` (the U-5 reading and
      the explicit instruction to Phase 257). ⛔ Assert the CONTENT, not that a comment exists —
      presence assertions cannot see content drift.
    - `grep -c "PHASES ARE NOT COUNTED" backend/app/services/harness_engine.py` → still `1` (the
      ORIGINAL is quoted, not deleted) AND the file now also contains a correction: e.g.
      `grep -c "CORRECTED.*256" backend/app/services/harness_engine.py` → at least `1`.
    - `node scripts/check-hot-file-ledger.cjs 256` → exit `0` with **NO `[no-row]` line at all**,
      and its printed `watched: N` is non-zero. ⛔ Read `watched: N`, never a bare exit 0.
    - `grep -c "backend/app/services/forced_emit.py" CLAUDE.md` → at least `1` AND
      `grep -c "backend/app/services/forced_emit.py" docs/HOT-FILE-LEDGER.md` → at least `2`.
    - `node scripts/check-claude-md-size.cjs` → exit `0`; the character count it prints is recorded.
    - `git diff --stat CLAUDE.md docs/HOT-FILE-LEDGER.md backend/app/services/forced_emit.py` shows
      all three in the SAME commit (O-6).
    - The SUMMARY carries the re-derived `forced_emit.py` triple with the three commands' output, and
      marks it CONFIRMED or CORRECTED against CONTEXT.md's `8 / 5 / 578`.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/check-hot-file-ledger.cjs 256 &amp;&amp; node scripts/check-claude-md-size.cjs</automated>
  </verify>

  <done>`TOKEN_COVERAGE_LEGS` is `("agent", "single", "batch", "emit")` with U-5's reading written
  beside it including the explicit `$0.00` prohibition for Phase 257; the `llm_emit`-not-counted
  comment is corrected with its original quoted beside it; `forced_emit.py` has a re-derived
  scan-list row and its own section in the same commit as its first edit; and the ledger gate
  reports no `[no-row]` for this phase at all.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider stream (8 providers, 3 emission shapes) → `forced_emit._drain` → the run box | A newly-counted, billed call path. Three adapters synthesize usage differently and one of them can emit nothing at all. |
| `phase_types.py` ↔ the Phase 255 Extension Contract | This file is one of the SIX trigger paths; the guard runs on a PostToolUse hook. |
| `TOKEN_COVERAGE_LEGS` → `workflow_runs.token_coverage` → Phase 257's METER-07 | A constant becomes a durable claim about what a number includes. A premature `"emit"` is a false claim in the column built to prevent false claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-256-20 | Repudiation | a `0` written where the truth is UNKNOWN, in the new drain | mitigate | `int \| None` initialisation, `is not None` tests, and a behavioural case pinning `(None, None)` on a no-usage stream. ⛔ Source-proven reachable at `openai_compat.py:482-494` for SIX of eight providers, so this is the COMMON path, not an edge case. A planted `0`-init must drive that case RED. |
| T-256-21 | Repudiation | counting only the SUCCESSFUL rung | mitigate | D-256-12. The accumulator is declared ABOVE the rung loop and `_failure()` carries the totals; a planted move-inside-the-loop must drive the multi-rung case RED. ⛔ Under-counting retry-heavy runs under-reports **the runs that cost the most**, invisibly. |
| T-256-22 | Spoofing / Tampering | the Extension Contract — a counter made resolvable from data | mitigate | ⛔ Nothing here registers, looks up or name-resolves a callable. `_exec_llm_emit` gains ONE call to an existing module-level function; `TOKEN_COVERAGE_LEGS` is a DATA value written into a column and resolves nothing. Asserted by `node scripts/check-extension-contract.cjs` AND by an unchanged `importlib|eval|exec|__import__` count in `phase_types.py`. |
| T-256-23 | Repudiation | a coverage marker claiming a leg that has not shipped | mitigate | O-4: `"emit"` is appended in the SAME COMMIT as the drain arms, and the tuple's member count is asserted at exactly four. ⛔ Earlier would be a lie in a column built to prevent lies. |
| T-256-24 | Information disclosure | a token VALUE reaching a log line from the new drain | mitigate | ⛔ **T-073-04:** this plan adds NO new log format string. If one proves unavoidable it is identifier-only and is pinned negatively against `tokens=`, `value=`, `usage_dict` — the discipline of `test_token_accumulator_missing_usage.py:79-93`. |
| T-256-25 | Tampering | the planted violations in `forced_emit.py` outliving the RED drive | mitigate | md5 digests before / plant A / plant B / after, with the final digest byte-identical to the first, plus `git diff --numstat` empty for the plant-only edits. ⚠ The Phase 255 `emitters.py` incident is precisely this failure — a planted `eval()` sat live for a period. |
| T-256-26 | Denial of service | CLAUDE.md crossing its 150,000-CHARACTER gate | mitigate | `node scripts/check-claude-md-size.cjs` is RUN; disposition cell ≤ 200 chars; reasons pushed into `docs/HOT-FILE-LEDGER.md`. ⚠ Above the limit **every instruction in CLAUDE.md silently stops applying, to every agent, everywhere.** |
| T-256-SC | Tampering | npm/pip/cargo installs | mitigate | ⭐ No package is installed by this plan. `git diff --numstat backend/requirements.txt frontend/package.json` is EMPTY. |
</threat_model>

<verification>
1. The two targeted suites plus every existing `forced_emit` / `llm_emit` / `phase_types` suite named
   in the acceptance criteria.
2. ⛔ **THE BACKEND BASELINE, ONCE, SET-DIFFED** (D-256-15, zero headroom):
   `cd backend && venv/Scripts/python -m pytest tests/unit -q --continue-on-collection-errors 2>&1 | grep "^FAILED" | sort`
   then `comm -13` and `comm -23` against `256-BASELINE-backend-failing-set.txt`. ⛔ Count with
   `grep -c`, **never `| tail`**. A name that LEFT the set must be explained too.
3. `cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py -q` — ⚠ in `tests/`,
   NOT `tests/unit/`, therefore OUTSIDE the canonical gate: a RED there is invisible to the 71-name
   baseline. This plan edits a comment in that module, so run it and record the figure.
4. `node scripts/check-extension-contract.cjs` — ⚠ `phase_types.py` is a trigger file. MUST be
   exit `0`.
5. `node scripts/check-hot-file-ledger.cjs 256` — ⛔ expect **NO `[no-row]` line at all** once this
   plan lands, and read `watched: N`.
6. `node scripts/check-claude-md-size.cjs` — record the character count.
7. `node scripts/check-seeds-register.cjs --phase 256` — re-run; record `N matched` and BOTH unswept
   figures, ⛔ unsummed.
8. **Frontend: SKIPPED, deliberately.** Zero `frontend/` files modified, so
   `vitest-count-gate.cjs` has no subject. ⛔ Do not run it and do not quote a figure from it
   (D-256-14).
</verification>

<success_criteria>
- `forced_emit._drain` handles BOTH `usage` and `usage_delta`, proven BEHAVIOURALLY against the real
  module over a fake event stream — ⛔ not by a grep for arm names.
- A no-usage stream yields `(None, None)`; a measured `0` yields `0`. Both pinned, and a planted
  `0`-initialisation drives the first case RED.
- A three-rung ladder with two FAILED rungs reports the sum of all three shots (D-256-12); a
  fully-exhausted ladder reports its spend through `_failure()`; a planted in-loop accumulator
  drives that RED.
- `_exec_llm_emit` records into the run box on the success AND failure paths; `_record_run_usage`
  itself byte-untouched; `_record_run_usage(` call sites 2 → 3.
- `TOKEN_COVERAGE_LEGS == ("agent", "single", "batch", "emit")`, appended in the SAME COMMIT as the
  drain arms (O-4), with U-5's reading and the `$0.00` prohibition written beside it.
- The `harness_engine.py:1837-1842` comment is corrected with the original quoted beside it (F-4 /
  S-5).
- `forced_emit.py` has a RE-DERIVED scan-list row and its own ledger section, in the same commit as
  its first edit (O-6), and `check-hot-file-ledger.cjs 256` reports NO `[no-row]` for the phase.
- `check-extension-contract.cjs` and `check-claude-md-size.cjs` both exit `0`, with their output
  recorded.
- The backend failing SET is byte-identical to the committed 71 names in both `comm` directions.
- The SUMMARY carries the arithmetic, not the adjective (D-256-13).
</success_criteria>

<output>
Create `.planning/phases/256-every-token-is-counted-and-kept/256-04-SUMMARY.md` when done.

It MUST contain: the asserted base SHA; all four md5 digests of `forced_emit.py` (pre / plant A /
plant B / restored) with the first and last identical; the RED output for BOTH planted violations;
the `_failure()` call-site correction recorded BESIDE RESEARCH.md's original two-site claim; the
re-derived `forced_emit.py` triple marked CONFIRMED or CORRECTED against `8 / 5 / 578`; the
`_record_run_usage(` and `_drain` arm arithmetic; the CLAUDE.md character count; the
extension-contract gate output; the ledger gate's `watched: N` line; and the backend SET diff in
both directions.
</output>
