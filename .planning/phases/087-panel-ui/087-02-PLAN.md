---
phase: 087-panel-ui
plan: 02
type: execute
wave: 3
depends_on: [01, 03, 04, 05]
files_modified:
  - frontend/src/components/panel/WorkspacePanel.tsx
  - frontend/src/components/panel/PanelRail.tsx
  - frontend/src/components/panel/PanelSection.tsx
  - frontend/src/components/panel/PanelEmpty.tsx
  - frontend/src/components/panel/TodosSection.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx
  - frontend/src/components/panel/__tests__/TodosSection.test.tsx
autonomous: true
requirements: [PANEL-01, PANEL-02]

must_haves:
  truths:
    - "A collapsible right-side panel (~30% width) appears next to chat, togglable via header button + ⌘./Ctrl+."
    - "Panel has three desktop states (open / rail-52px / hidden) animated via the grid columns; <768px renders as a bottom-sheet (Sheet)"
    - "Collapsed rail shows per-section count badges + an amber warn badge when a question is pending; toggle shows a pulsing amber dot when Q pending AND panel closed"
    - "Zero workspace activity short-circuits to ONE centered PanelEmpty — never four empty headers"
    - "Todos section renders the live todo list with pending/in-progress/completed status, updating with no refresh"
    - "Sections render in fixed order Todos · Files · Pending question · Versions; a pending ask_user pins to the very top regardless"
  artifacts:
    - path: "frontend/src/components/panel/WorkspacePanel.tsx"
      provides: "Panel shell, grid-state machine, ⌘. handler, empty short-circuit, section composition"
      contains: "useViewingThread"
    - path: "frontend/src/components/panel/PanelSection.tsx"
      provides: "Collapsible .sec-head(button aria-expanded)/.sec-body primitive"
      contains: "aria-expanded"
    - path: "frontend/src/components/panel/TodosSection.tsx"
      provides: "Live todo list with status indicators"
      contains: "useTodos"
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "Additive WorkspacePanel sibling mount (chat view only)"
      contains: "WorkspacePanel"
  key_links:
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "WorkspacePanel"
      via: "additive sibling render when activeView==='chat'"
      pattern: "WorkspacePanel"
    - from: "WorkspacePanel"
      to: "PendingAskCard / FilesSection / VersionDiff / TodosSection"
      via: "section composition"
      pattern: "FilesSection|PendingAskCard"
---

<objective>
Build the panel shell (PANEL-01) and Todos section (PANEL-02), then mount it: `WorkspacePanel` (grid-state machine open/rail/hidden + mobile Sheet + `⌘.` + empty short-circuit), `PanelRail`/`PanelSection`/`PanelEmpty` primitives, `TodosSection`, and one ADDITIVE sibling render in `ChatLayout`. Composes the Wave-2 sections (Files/Versions/PendingAsk) into the fixed-order accordion.

Purpose: Deliver the visible, collapsible, responsive panel that hosts every section and toggles into/out of the chat layout — the capstone that makes Waves 1-2 reachable by the user.
Output: 5 new components + 1 additive ChatLayout touch + 2 tests flipped GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/087-panel-ui/087-RESEARCH.md
@.planning/phases/087-panel-ui/087-PATTERNS.md
@.planning/phases/087-panel-ui/087-UI-SPEC.md
@.planning/phases/087-panel-ui/087-01-SUMMARY.md
@.planning/phases/087-panel-ui/087-03-SUMMARY.md
@.planning/phases/087-panel-ui/087-04-SUMMARY.md
@.planning/phases/087-panel-ui/087-05-SUMMARY.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- VERIFIED. -->

From frontend/src/providers/StreamsProvider.tsx (Phase 086):
```typescript
useViewingThread(): string | null
useTodos(threadId): { data: Todo[]; isLoading; error; reconcile }            // data never undefined
useWorkspaceFiles(threadId): { data: WorkspaceFile[]; … }
useAskUserPrompt(threadId): { data: PendingAsk[]; … }
```

Wave-2 section components (import these — do NOT rebuild):
```typescript
// Plan 03
import { FilesSection } from "@/components/panel/FilesSection"
// Plan 04
import { VersionDiff } from "@/components/panel/VersionDiff"
// Plan 05
import { PendingAskStack /* or PendingAskCard stack */ } from "@/components/panel/PendingAskCard"
```

From frontend/src/components/ui/sheet.tsx (Plan 01): Sheet, SheetContent (side="bottom"), SheetHeader, SheetTitle.

ChatLayout mount point (VERIFIED ChatLayout.tsx:184-197):
```tsx
<main className="flex-1 overflow-hidden">
  { /* … activeView switch … */ activeView is "chat" → <ChatArea thread={selectedThread} …/> }
</main>
{/* ADD HERE — additive sibling, chat-view only: */}
{activeView === "chat" && <WorkspacePanel selectedThread={selectedThread} />}
```
The live layout is `<div className="flex h-screen …">` — NOT a grid (Pitfall 3). Introduce the push/split grid INSIDE WorkspacePanel's own container; keep the ChatLayout change to one additive sibling render.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Panel primitives — PanelSection, PanelEmpty, PanelRail</name>
  <read_first>
    - 087-PATTERNS.md §"PanelSection" (collapsible .sec-head must be real <button aria-expanded>, chevron aria-hidden, Enter/Space toggle, count badge right-aligned mono, .count.warn amber) + §"PanelEmpty" (ChatArea.tsx:302-333 centered analog) + §"PanelRail" (ChatLayout.tsx:156-180 icon-button row analog)
    - 087-UI-SPEC.md §"Layout & Structure" (rail 52px, count badges Todos 2/3 / Files 4 / amber when Q pending) + §"Accessibility Contract" (Rail icon buttons: real <button> with aria-label INCLUDING live count; visual badge aria-hidden) + §"Typography" (count badge 600/font-semibold, the ONLY non-400 weight) + Copywriting (empty heading "No workspace activity yet")
    - frontend/src/components/ui/collapsible.tsx (Radix collapsible analog) + frontend/src/components/chat/ChatArea.tsx:288-336 (empty-state analog)
  </read_first>
  <action>
    - `PanelSection.tsx` `{ title: string; count?: { done?: number; total?: number } | number; warn?: boolean; defaultOpen?: boolean; children }` — `.sec-head` is a real `<button aria-expanded>` (Enter/Space toggle, chevron `aria-hidden` rotates), section label uppercase 0.72rem/400/letter-spacing 0.07em; right-aligned mono count badge (`margin-left:auto`, `font-semibold`/600 — the only non-400 weight); `.count.warn` → amber (`--warning`) when `warn`. `role="region"` body + `aria-labelledby` on the head. Use the spacing tokens (12px 16px head padding). Respect prefers-reduced-motion for the collapse.
    - `PanelEmpty.tsx` — centered (`flex items-center justify-center`) icon + heading `No workspace activity yet` + body `When the agent writes files, tracks todos, or needs your input, it'll show up here.` (Claude's Discretion copy confirmed in CONTEXT). Calm, must fill the panel body.
    - `PanelRail.tsx` `{ todos: {done,total}; filesCount: number; pendingCount: number; onExpand: () => void }` — 52px vertical icon strip; each icon is a real `<button>` whose `aria-label` INCLUDES the live count ("Todos — 2 of 3 done", "Files — 4", "Pending question — needs your answer"); the visual count/warn badge is `aria-hidden="true"` (no double-announce); amber warn badge when `pendingCount>0`. Clicking expands the panel.
  </action>
  <verify>
    <automated>cd frontend && node node_modules/typescript/bin/tsc --noEmit && npx vitest run src/components/panel/</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist under `frontend/src/components/panel/`
    - `grep "aria-expanded" frontend/src/components/panel/PanelSection.tsx` matches (real button accordion)
    - `grep "No workspace activity yet" frontend/src/components/panel/PanelEmpty.tsx` matches
    - `grep "aria-label" frontend/src/components/panel/PanelRail.tsx` shows count-bearing labels; `grep "aria-hidden" frontend/src/components/panel/PanelRail.tsx` matches (badge decorative)
    - `tsc --noEmit` exits 0; `npx vitest run src/components/panel/` GREEN
  </acceptance_criteria>
  <done>Accessible accordion primitive, empty state, and count-badged rail all compile and render.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TodosSection — live todo list with status indicators (PANEL-02)</name>
  <read_first>
    - 087-PATTERNS.md §"TodosSection" (useTodos → Todo[]; MessageList live-array→row map analog; checkPop on complete; full-replace identity)
    - 087-UI-SPEC.md §"Component Inventory" TodosSection + §"Color" (green=done) + Typography (list 0.82rem)
    - frontend/src/components/panel/__tests__/TodosSection.test.tsx + fixtures.ts (mockTodos with pending/in-progress/completed)
    - frontend/src/types/index.ts (Todo shape — status field)
  </read_first>
  <behavior>
    - Test: given mockTodos with the three statuses, renders one row per todo with a distinct status indicator for pending / in-progress / completed.
    - Test: completed todos use the green/success treatment (and a check); status conveyed non-color-only (icon/text), not color alone.
    - Test: re-rendering with an updated todos array (status flip) reflects the new status with no manual refresh call (reactive — driven by the useTodos mock data change).
  </behavior>
  <action>
    Create `TodosSection.tsx` consuming `useTodos(threadId)`. Render `data` as a list (full-replace identity per 086 — key by `id`); each row: status indicator (pending → muted/circle, in-progress → primary/spinner-dot respecting reduced-motion, completed → green check `checkPop` 1-shot) + the todo text (0.82rem). Status must be conveyed by icon/text, not color alone (A11Y). No fetch logic here — the hook is reactive (PANEL-02: updates on `write_todos` SSE with no refresh). Exported for WorkspacePanel to place in the fixed-order accordion.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/TodosSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useTodos" frontend/src/components/panel/TodosSection.tsx` matches
    - `grep "completed\|in_progress\|pending\|in-progress" frontend/src/components/panel/TodosSection.tsx` matches (status handling)
    - `npx vitest run …/TodosSection.test.tsx` all GREEN (no .todo for PANEL-02)
  </acceptance_criteria>
  <done>Live todo list renders all three statuses non-color-only and updates reactively; tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: WorkspacePanel shell + ⌘. + grid-state machine + empty short-circuit + ChatLayout mount</name>
  <read_first>
    - 087-PATTERNS.md §"WorkspacePanel" (ChatArea.tsx:61-68 null-safe hook consumption; empty short-circuit D3; grid-state machine classes panel-collapsed|panel-rail|open; grid-template-columns 52px 1fr clamp(300px,30%,420px); ⌘./Ctrl+. toggle; <aside role=complementary aria-label="Agent workspace">) + §"ChatLayout" (additive sibling only)
    - 087-UI-SPEC.md §"Layout & Structure" (3 states, transition 320ms, laptop-squeeze guard clamp(300px,30%,420px), chat ≥600px floor at 1024px) + §"Responsive Breakpoints" (375/768/1024/1440; <768px = Sheet bottom-sheet, must not occlude composer, grip dismisses, touch targets ≥44px) + §"Toggle" (pulse amber dot when Q pending + closed)
    - frontend/src/components/layout/ChatLayout.tsx:184-197 (the flex host + the activeView switch — mount the additive sibling here)
    - frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx + fixtures.ts (toggle state-machine + empty short-circuit cases)
    - Sheet primitive from Plan 01 (frontend/src/components/ui/sheet.tsx)
  </read_first>
  <action>
    Create `WorkspacePanel.tsx` `{ selectedThread: Thread | null }`. Consume `useViewingThread()` + `useTodos`/`useWorkspaceFiles`/`useAskUserPrompt` (null-safe; data never undefined). State machine: `"open" | "rail" | "hidden"` (default open when there's activity, else collapsible). Render the push/split grid INSIDE this component's own container (the live ChatLayout is flex, not grid — Pitfall 3): grid columns `52px 1fr clamp(300px,30%,420px)` for open, rail collapses the panel column to 52px (`<PanelRail>`), hidden to 0; transition `grid-template-columns 320ms`. **Laptop-squeeze guard:** below 1024px keep chat ≥600px — drop to rail if it would starve. **Mobile (<768px):** render the panel body inside the `<Sheet>` (`SheetContent side="bottom"`, top:30%, grip dismisses, never occlude composer, ≥44px touch targets) instead of the side column. Header: `Workspace` title + toggle button; `⌘.`/`Ctrl+.` keydown handler toggles open↔hidden; toggle shows a pulsing amber dot when `pendingAsks.length>0` AND panel is closed. **Empty short-circuit (D3):** when `todos.length===0 && files.length===0 && pendingAsks.length===0` (+ no versions context) → render ONE `<PanelEmpty/>`, never four empty headers. **Section composition (fixed order D2):** Todos (`<TodosSection/>`) · Files (`<FilesSection/>`) · Pending question · Versions (`<VersionDiff/>` for the selected file) — each in a `<PanelSection>` with its count badge; the `<PendingAskStack/>` from Plan 05 pins to the very top (`position:sticky top:0 z-index:4`) regardless of section order. `<aside role="complementary" aria-label="Agent workspace">`. Respect prefers-reduced-motion for the grid transition.

    Then modify `ChatLayout.tsx` — ADDITIVE only: after the `<main>` block (~line 196), add `{activeView === "chat" && <WorkspacePanel selectedThread={selectedThread} />}`. Do NOT restructure the existing flex layout, NavPanel, drawer, or `<main>` — one new sibling render. (If the panel needs the chat+panel region to be grid, scope that wrapper INSIDE WorkspacePanel or add a minimal wrapper without touching NavPanel/drawer.)
  </action>
  <verify>
    <automated>cd frontend && node node_modules/typescript/bin/tsc --noEmit && npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useViewingThread" frontend/src/components/panel/WorkspacePanel.tsx` matches
    - `grep "role=\"complementary\"" frontend/src/components/panel/WorkspacePanel.tsx` matches
    - `grep "Meta\|Ctrl\|key === \".\"\|'.'" frontend/src/components/panel/WorkspacePanel.tsx` shows the ⌘./Ctrl+. handler
    - `grep "PanelEmpty" frontend/src/components/panel/WorkspacePanel.tsx` matches (empty short-circuit)
    - `grep "FilesSection\|TodosSection\|VersionDiff\|PendingAsk" frontend/src/components/panel/WorkspacePanel.tsx` matches all four section imports
    - `grep "Sheet" frontend/src/components/panel/WorkspacePanel.tsx` matches (mobile bottom-sheet)
    - `grep "WorkspacePanel" frontend/src/components/layout/ChatLayout.tsx` matches the additive mount; `grep "activeView === \"chat\"" frontend/src/components/layout/ChatLayout.tsx` gates it
    - `git diff --stat frontend/src/components/layout/ChatLayout.tsx` shows additions-dominant (NavPanel/drawer/main preserved)
    - `tsc --noEmit` exits 0; `npx vitest run …/WorkspacePanel.test.tsx` GREEN (PANEL-01); full `npm test` not regressed past baseline
  </acceptance_criteria>
  <done>Panel toggles open/rail/hidden + mobile sheet via button & ⌘.; empty short-circuits; composes all 4 sections in fixed order with pinned asks; mounts additively in ChatLayout; tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel events→chat render | Panel reads the shared store; must not trigger chat message-list re-renders (PANEL-06, inherited) |
| client→API | useViewingThread-keyed reconcile fetches cross to RLS-protected endpoints (inherited from Phase 086 hooks) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-087-15 | Information Disclosure (cross-thread bleed) | WorkspacePanel hook consumption | mitigate | Consumes the Phase-086 hooks as-is (isolated per-thread Maps + usePanelReconcile abort); keys all section data by `useViewingThread()`. No new fetch logic. Re-verified live in the carry-forward UAT (087-VALIDATION.md). |
| T-087-16 | Tampering (G-5 regression in ChatLayout) | ChatLayout mount | mitigate | One additive sibling render gated on `activeView==="chat"`; NavPanel/drawer/`<main>` preserved (grep + diff review). |
| T-087-17 | Information Disclosure (panel re-renders chat — PANEL-06 regression) | WorkspacePanel | mitigate | Hooks already use isolated Maps + stable EMPTY refs; consume directly, no broad store selector shared with chat. Verified live via Chrome MCP (no chat flicker on panel events) per 087-VALIDATION.md. |
</threat_model>

<verification>
- `tsc --noEmit` clean across frontend.
- `npx vitest run src/components/panel/` fully GREEN (WorkspacePanel + TodosSection now live → all 7 panel test files GREEN, all req .todos flipped).
- `npm test` full suite not regressed past the known ~17-failure baseline.
- ChatLayout diff additive (NavPanel/drawer/main intact).
</verification>

<success_criteria>
A ~30% collapsible right panel toggles via button + ⌘. (rail/hidden/open, mobile bottom-sheet), short-circuits to one calm empty state when idle, renders the live status-indicated todo list with no refresh, and composes Files/Versions/PendingAsk in fixed order with pinned questions — mounted additively into the chat layout without disturbing existing chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/087-panel-ui/087-02-SUMMARY.md`
</output>
