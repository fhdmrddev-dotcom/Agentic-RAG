---
status: testing
phase: 087-panel-ui
source: [087-01-SUMMARY.md, 087-02-SUMMARY.md, 087-03-SUMMARY.md, 087-04-SUMMARY.md, 087-05-SUMMARY.md]
started: "2026-05-29T06:55:06Z"
updated: "2026-05-29T06:55:06Z"
verification_method: chrome-devtools-mcp
---

## Current Test

[testing complete — 2 issues found, diagnosed]

## Tests

### 1. Desktop panel position + width (open state)
expected: Panel sits flush against the right viewport edge at a stable ~30% width (clamp 300–420px), identical position regardless of which thread/provider is open; no horizontal scrollbar; content never clipped off-screen.
result: issue
reported: "Layout/positioning of the panel is not consistent — different position per provider or thread, with a notable gap to the right side of the panel. Confirmed live via Chrome MCP: panel column resolves to a fixed 300px that OVERFLOWS the viewport right edge (documentScrollW 1562 vs viewport 1536 on an empty thread → 26px off-screen; 1688 vs 1536 on a content-heavy thread → 152px off-screen). Panel left edge shifts 1262→1388px between threads. Empty-state text and the /test.md file row are clipped at the right edge; a horizontal scrollbar appears."
severity: major

### 2. Dev-only debug overlay leaks into production UI
expected: No developer/test scaffolding visible in the running app.
result: issue
reported: "A dark 'chat surface / mock-eval surface / tick' debug overlay renders pinned bottom-right on every chat view (both light and dark themes), overlapping the composer. This is the Phase 086 StreamsProvider binding-gate harness (DevTwoPaneMock) mounted unconditionally. Confirmed live: document.querySelector('[data-testid=\"pane-mock-eval\"]') is present in the production tree."
severity: major

### 3. Mobile bottom-sheet (<768px)
expected: Below 768px the panel renders as a bottom Sheet with drag handle, header, and the same sections; no overflow.
result: pass

### 4. Panel state machine — rail (52px) and hidden
expected: Header collapse button cycles open → rail → hidden; rail shows count badges; no overflow in rail/hidden.
result: pass

### 5. File preview drill-in (PANEL-03)
expected: Clicking a file full-replaces the list with a per-type preview + a '‹ Files' back button.
result: pass

### 6. Section composition (Todos · Files · Versions + pending-ask pin)
expected: Sections render in fixed order with live counts; empty thread short-circuits to one centered PanelEmpty.
result: pass

## Summary

total: 6
passed: 4
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Panel sits flush at a stable viewport-relative ~30% width (300–420px), identical position across threads/providers, no overflow, content never clipped"
  status: failed
  reason: "User reported + Chrome MCP confirmed: panel overflows viewport right edge and repositions per thread."
  severity: major
  test: 1
  artifacts:
    - "frontend/src/components/panel/WorkspacePanel.tsx (desktop branch, lines 195-231)"
    - "frontend/src/components/layout/ChatLayout.tsx (lines 70-203, flex row + flex-1 main + aside sibling)"
  root_cause: "WorkspacePanel renders an <aside class='grid ...' style='grid-template-columns: 0 clamp(300px,30%,420px)'> as a flex sibling of <main class='flex-1'>. (a) The 30% in the clamp resolves against the aside's OWN indefinite inline size (a flex item with flex:0 1 auto and no basis), so it collapses to the 300px floor — never the intended ~30%/420px. (b) <main class='flex-1'> greedily grows with chat content, shrinking the aside's flex allocation below 300px (268px empty, 143px content-heavy), but the internal grid column is a hard 300px → it overflows the aside box and the viewport. Overflow + panel left-edge position therefore vary with each thread's content. asideGridCols measured = '0px 300px' in both threads; documentScrollW 1562 / 1688 vs viewport 1536."
  missing:
    - "A viewport/row-relative definite width for the panel column (so clamp 30% resolves against the full flex row, not the aside itself)"
    - "Removal of the self-referential internal grid hack (grid-template-columns: 0 clamp(...)) — animate width or flex-basis instead, or hoist the chat|panel split to a ChatLayout-level grid so the chat column is 1fr and the panel column is the clamp"
    - "Ensure the panel never overflows: chat <main> must yield the panel's width rather than the panel shrinking below its content"

- truth: "No developer/test scaffolding is visible in the running production app"
  status: failed
  reason: "DevTwoPaneMock debug overlay renders in production on every chat view."
  severity: major
  test: 2
  artifacts:
    - "frontend/src/App.tsx (line 8 import, line 39 <DevTwoPaneMock /> mount)"
    - "frontend/src/components/dev/DevTwoPaneMock.tsx (the leaked harness)"
  root_cause: "App.tsx mounts <DevTwoPaneMock /> unconditionally as a sibling of <ChatLayout />. It is a Phase 086 StreamsProvider binding-gate demo harness ('chat surface' / 'mock-eval surface' / 'tick' fixed-position overlay) that was never gated or removed before shipping. Origin is Phase 086, surfaced during 087 panel UAT."
  missing:
    - "Remove the import + mount from App.tsx (preferred — it has no production purpose), OR gate it behind import.meta.env.DEV plus an explicit opt-in flag so it can never render in a normal session"
