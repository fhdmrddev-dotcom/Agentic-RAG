---
phase: 087
slug: panel-ui
status: draft
shadcn_initialized: true
preset: "style=default · baseColor=slate · cssVariables=true · icon=lucide (detected from frontend/components.json)"
created: 2026-05-29
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04, PANEL-07]
design_substrate: sketch-findings-agentic-rag (sketches 004–007, all winners LOCKED)
---

# Phase 087 — UI Design Contract: Panel UI

> Visual and interaction contract for the right-side workspace panel (todos · files · pending question · versions). Generated by gsd-ui-researcher. Every visual decision below is TRANSLATED from operator-approved sketches 004–007 (G-2 satisfied) — it does not redesign them. Where a sketch already decided something it is locked verbatim and cited. Genuinely-unresolved contracts the sketches did not cover are called out in `## Open Contracts (flagged)`.

---

## Source of Truth & Citation Key

| Citation | File | Sketch | Winner |
|----------|------|--------|--------|
| `[shell]` | `references/panel-shell.md` | 004 | B — Stacked accordion |
| `[files]` | `references/file-browser-and-diff.md` | 005 | A — Full-replace drill-in + in-column unified diff |
| `[ask]` | `references/pending-question.md` | 006 | C — Dual-surface calm-pin + chat cue |
| `[seam]` | `references/chat-panel-seam.md` | 007 | C — Live-pointer / reload-resolved |
| `[theme]` | `sources/themes/default.css` (mirrors `frontend/src/index.css :.dark`) | — | Aether Deep Midnight |

These decisions are **locked**. The planner/executor implement them; they do not re-open them.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn (initialized) | `frontend/components.json` |
| Preset | style=default · baseColor=slate · cssVariables=true · tsx=true · rsc=false | `frontend/components.json` |
| Component library | Radix UI (via shadcn primitives) | `frontend/package.json` (`@radix-ui/*`) |
| Icon library | lucide-react `^0.577.0` | `frontend/components.json` + `package.json` |
| Fonts | Inter (body) · Manrope (headlines, `.font-headline`) · JetBrains Mono (code/status/identifiers) | `[theme]`, `index.css` |
| Theme | Aether Deep Midnight — `.dark` is the canonical app theme; all tokens below are HSL CSS vars already live in `index.css` | `index.css :.dark` |
| New UI deps | **NONE** — SC#3 mandates reuse of existing `MarkdownRenderer` (`frontend/src/components/chat/MarkdownRenderer.tsx`) + existing `react-syntax-highlighter` | CLAUDE.md, ROADMAP SC#3 |

**Reuse-first inventory (already in repo — do NOT re-add):**
- `MarkdownRenderer.tsx` → `.md` previews and `ask_user` rich text `[files]`
- `react-syntax-highlighter` → code-file previews + diff syntax `[files]` PANEL-07
- shadcn `button`, `dialog`, `scroll-area` (present in `components/ui/`) → panel buttons, the opt-in wide-diff overlay (`dialog`), scrollable panel body
- Phase 086 hooks (live, exported from `StreamsProvider.tsx`): `useTodos`, `useWorkspaceFiles`, `useAskUserPrompt` (returns `PendingAsk[]`), `useTasks`, `usePanelReconcile`, `useViewingThread`. **The panel consumes these reactively — assume no page refresh.**

---

## Token Mapping: Sketch var → App token

The sketch CSS uses `--color-*` / `--text-*` / `--space-*` names. These are sketch-local aliases; the **live app** uses shadcn HSL channel vars. Executor maps as follows (no new tokens introduced):

| Sketch var `[theme]` | App token (`index.css :.dark`) | Tailwind class |
|----------------------|--------------------------------|----------------|
| `--color-bg` | `--background` (216 45% 4%) | `bg-background` |
| `--color-bg-elev1` | `--sidebar` (220 40% 5%) | panel surface |
| `--color-surface` | `--card` (220 30% 7%) | `bg-card` |
| `--color-muted` | `--muted` (220 30% 11%) | `bg-muted` |
| `--color-accent` | `--accent` (220 25% 14%) | `bg-accent` (hover) |
| `--color-text` | `--foreground` (226 60% 97%) | `text-foreground` |
| `--color-text-muted` | `--muted-foreground` (220 16% 65%) | `text-muted-foreground` |
| `--color-text-dim` | (220 16% 45%) — add as local CSS var | dim labels |
| `--color-border` | `--border` (220 20% 16%) | `border-border` |
| `--color-primary` | `--primary` (239 100% 82%) | live/pointer |
| `--color-success` | `--success` (142 71% 45%) | done/answered/diff-added |
| `--color-warning` | (38 92% 60%) — **add `--warning` to `index.css`** (not yet present) | needs-you/paused |
| `--color-danger` | `--destructive` (0 72% 51%) | diff-removed/base |

**Token gap to close (executor action):** `--warning` (38 92% 60%) and `--color-text-dim` (220 16% 45%) are used pervasively by the amber `ask_user` treatment and dim section labels but are **not** in `index.css` yet (only `--success` was added). Add both as `.dark` HSL channel vars before building. This is a token addition, not a redesign.

---

## Spacing Scale

4px grid `[theme]` (`--space-1`..`--space-16`). Matches the 8-point system (4 is the base unit). Declared values used by panel surfaces:

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| space-1 | 4px | diff sign-gutter inner gap, file-row icon gap fine-tune |
| space-2 | 8px | compact element gaps (chips, file rows, choice-chip stack), section-body row gap (`6px` exception below) |
| space-3 | 12px | section-head padding (`12px 16px`), card padding, ask-card internal gap |
| space-4 | 16px | default section padding, panel-head padding |
| space-6 | 24px | empty-state padding, rail vertical padding rhythm |
| space-8 | 32px | major panel breaks |

**Exceptions (declared, sketch-sourced):**
- `6px` row gap inside `.sec-body` and choice-chip / version-pill stacks `[shell][ask][files]` — sub-grid micro-rhythm, intentional and below the 8px floor.
- `.diff .sign { width: 16px }` fixed gutter `[files]` — load-bearing for diff legibility, not a spacing token.
- Touch targets: file rows, choice chips, version pills, rail icons MUST be **≥ 44px** tall on mobile (<768px) even though visual padding is tighter on desktop — accessibility floor for Phase 088 (A11Y).

---

## Typography

Sketch `--text-*` scale `[theme]` maps to rem sizes. The panel uses a tight 4-role set (within the 3–4 sizes / 2 weights contract):

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Section label | 0.72rem (`text-xs`, ~11.5px) | 400, `uppercase`, `letter-spacing 0.07em` | 1.4 | Inter | `.sec-head`, `.qlbl`, `.sc-head`, version "Compare" label |
| Body | 0.9rem (`text-base`, ~14.4px) | 400 | 1.5 | Inter | `.qtext` question text, empty-state, answered rows |
| List / control | 0.82rem (`text-sm`, ~13px) | 400 (600 on selected) | 1.5 | Inter | file rows, choice chips, submit button label, todo text |
| Mono / identifiers | 0.72rem (`text-xs`) | 400 (700 on rail badge) | 1.6 (diff) | JetBrains Mono | filenames, file meta, version pills, diff lines, count badges, pointers, countdown clock |

**Weights: exactly two — 400 (regular) and 600 (semibold).** Rail count badge uses 700 `[shell]` — declared exception (single use, 9px mono badge). No other weights.

**Line heights:** body/controls 1.5; section labels 1.4 (tight uppercase); diff 1.6 (`.diff { line-height: 1.6 }` `[files]` — legibility of `+/−` lines); empty-state hint 1.6 `[shell]`.

---

## Color

60 / 30 / 10 split, all from the Deep Midnight `.dark` palette:

| Role | Value (HSL) | Usage |
|------|-------------|-------|
| Dominant (60%) | `--background` 216 45% 4% + panel surface `--sidebar` 220 40% 5% | App background, panel field |
| Secondary (30%) | `--card` 220 30% 7% / `--muted` 220 30% 11% / `--accent` 220 25% 14% | File rows, preview head, version pills, choice chips, seam cards, hover states |
| Accent (10%) | `--primary` 239 100% 82% (soft indigo) | See reserved-for list below |
| Semantic — warning | 38 92% 60% (amber) | `ask_user` needs-you / paused run-card / locked-composer hint / rail warn badge / toggle pulse-dot |
| Semantic — success | `--success` 142 71% 45% (green) | todo done · answered ask · diff-added · file-flash · version "target/after" pill |
| Semantic — destructive | `--destructive` 0 72% 51% (red) | diff-removed · version "base/before" pill (NO delete action in this phase — see Copywriting) |

**Accent (primary indigo) reserved EXCLUSIVELY for** `[seam][shell]`:
1. Live one-line pointers in chat (`→ updated todos · see panel`) — arrow + "see panel" link
2. Selected file row (`.file.sel` — `primary-dim` bg + `primary-glow` border)
3. Diff hunk headers (`@@ … @@`)
4. Rail count badges (non-warning)
5. "open panel ↗" link on reloaded seam cards
6. Focus ring (`--ring` is already primary) — visible focus indicator (A11Y)

Accent is **never** used for generic buttons, section headers, or decoration. Warning/success/destructive carry all status meaning; primary carries "live / pointer / selected / focus" only.

**Color-language invariants (LOCKED `[shell]` design_direction):** amber = needs-you/paused · green = done/answered/diff-added · red = diff-removed/base-version · primary = live/pointer. Never swap these.

**Contrast (Phase 088 gate, bake in now):** all text ≥ 4.5:1 on its surface. Amber-on-amber-dim and the `#06061a` text-on-amber-fill submit button are the two at-risk pairs `[ask]` — executor MUST verify both hit 4.5:1; if amber-dim body text fails, darken the dim background rather than lightening text.

---

## Layout & Structure (LOCKED from `[shell]`)

**Three-column push/split grid — never overlay (D1):**
```
grid-template-columns: 52px [leftnav] · 1fr [chat] · var(--panel-w, 30%) [panel]
```
- Open: `52px 1fr 30%`
- Rail (collapsed-with-glance): `52px 1fr 52px`
- Hidden: `52px 1fr 0`
- Mobile (<768px): `1fr` + panel becomes bottom-sheet
- Transition: `grid-template-columns var(--dur-slow) var(--ease-out)` (320ms)
- **Laptop-squeeze guard (D-shell "What to Avoid"):** clamp `--panel-w` with `min()` so chat keeps a readable floor at ~1024px. Contract: `--panel-w: clamp(300px, 30%, 420px)`; below 1024px chat must retain ≥ 600px before the panel is forced to rail. Executor verifies at the 1024px breakpoint that run-card tool output does not wrap illegibly.

**Four sections, fixed order (D2):** Todos · Files · Pending question · Versions. Each = `.sec-head` (click-collapse, chevron rotates) + `.sec-body`. A pending `ask_user` pins to the very top via `position: sticky; top: 0; z-index: 4` regardless of section order `[ask]` D1.

**Empty short-circuit (D3):** when there is zero workspace activity (the common plain-Q&A case), the whole panel body renders ONE centered `.panel-empty` state — **never four empty section headers.**

**Collapse-to-rail (D4):** desktop collapse leaves a 52px vertical icon strip with per-section count badges (`Todos 2/3`, `Files 4`) and an **amber** badge if a question is pending. A pending `ask_user` must NEVER go silent on collapse — rail warn badge + toggle pulse-dot are mandatory.

**Toggle (D6):** header button + `⌘.` / `Ctrl+.`. Toggle shows a pulsing amber dot when a question is pending AND the panel is closed.

---

## Component Inventory

New components (prefer NEW files over re-touching G-5 hot files `ToolCallPanel/MessageItem/StreamsProvider/useMessages` — per CLAUDE.md). Suggested home: `frontend/src/components/panel/`.

| Component | Responsibility | Consumes | Sketch |
|-----------|----------------|----------|--------|
| `WorkspacePanel` | Outer aside + grid-column state machine (open/rail/hidden/mobile-sheet), header, `⌘.` handler, empty short-circuit | `useViewingThread`, all panel hooks | `[shell]` |
| `PanelRail` | 52px collapsed strip with count badges (+ amber warn badge) | counts from hooks | `[shell]` D4 |
| `PanelSection` | Generic collapsible `.sec-head`+`.sec-body` primitive (chevron, count, collapsed state) | — | `[shell]` D2 |
| `TodosSection` | Live todo list, status indicators (pending/in-progress/completed), `checkPop` on complete | `useTodos` | PANEL-02 |
| `FilesSection` | File list rows (icon, mono name, meta `size · vN`), `fileFlash` on fresh write, click → drill-in | `useWorkspaceFiles` | `[files]` D1/D2 |
| `FilePreview` | Full-replace drill-in: `‹ Files` back, per-type routing (md→MarkdownRenderer, code→highlighter, csv→table, image→framed, else→graceful fallback) | file content API | `[files]` D1/D2 |
| `VersionDiff` | Version pills (red base / green target), `+N/−M` summary, unified in-column `+/−` diff, `⤢` expand-to-overlay | versions + diff API (PANEL-07) | `[files]` D3/D4/D5 |
| `DiffExpandOverlay` | Opt-in wide diff in a shadcn `dialog` over chat — **never auto-widens the panel** | — | `[files]` D4 |
| `PendingAskCard` | Pinned amber card: label+pip+countdown, optional choice chips, always-present free-text, submit | `useAskUserPrompt` (`PendingAsk[]`) | `[ask]` D1/D3 |
| `PausedRunCue` | Chat-side pointer cue inside paused amber run-card ("⏸ ask_user · awaiting your answer →") + composer-lock hint. **Additive renderer — do NOT fold into broader chat-tool-card unification (BUG-260529-02 is a separate phase).** | streaming run state | `[ask]` D2, `[seam]` |
| `SeamPointer` | Live one-line pointers in chat for `write_todos`/`workspace_write`/`ask_user` (`→ … · see panel`) | live event mode | `[seam]` D2 |
| `SeamCard` | Reloaded self-contained cards: answered Q&A (closes ask_user reload gap), file chip, todo final-state note | reloaded event mode | `[seam]` D3 |
| `PanelEmpty` | Centered calm empty short-circuit + collapse-to-rail affordance | — | `[shell]` D3 |

**Mode-aware seam renderer (`[seam]` impl note):** the 3 panel-owned tools render `live → SeamPointer`, `reloaded → SeamCard`. Mode = "is this event from the currently-streaming run, or rehydrated from history?" Multiple parallel `ask_user` prompts are possible (hook returns `PendingAsk[]`) — render a stack; each pins.

---

## Interaction States (per surface, sketch-locked)

**Files / Preview `[files]`:** default · `:hover` (`bg-accent`) · `.sel` (primary-dim + primary-glow border) · `.flash` (green `fileFlash` 1.4s on fresh write) · drill-in preview (full-replace, `‹ Files` back mandatory) · preview fallback (too-large / binary / no-preview → calm "no preview available · download" notice — mandatory, must not break layout).

**Versions / Diff `[files]`:** pills default · `.a` red base/before · `.b` green target/after · `Compare v2↔v3` one-click latest-two default · `+N/−M` summary · in-column unified diff (sign gutter fixed 16px, `.diff` scrolls-x rather than wraps) · `⤢` opt-in overlay (panel never auto-widens).

**Pending ask `[ask]`:** pending (amber, pip `dotBounce`, countdown clock) · submit disabled until pick-or-type (D3) · choice chips optional, free-text always present · answered (green `.answered`, records "You answered **X**", run un-pauses, composer unlocks — D4) · expired (calm grey `.expired`, "No response within 5:00 — agent stopped" — never a crash/hang — D5). Parallel asks stack.

**Chat seam `[seam]`:** live → quiet one-line pointer only (no full card — avoids tall-transcript fatigue) · reloaded → compact self-contained card (Q&A / file chip / todo note) · no raw-JSON leak ever · single source of truth (live state in panel, history in transcript — never the same data rendered richly in both).

**Panel shell `[shell]`:** open ↔ rail ↔ hidden (animated grid columns) · mobile bottom-sheet slide (`translateY`) · empty short-circuit · toggle pulse-dot when Q pending + closed.

---

## Responsive Breakpoints (REQUIRED by ROADMAP)

| Breakpoint | Behavior | Source |
|------------|----------|--------|
| 375px | Bottom-sheet (`top: 30%`, slide-up, `.sheet-grip` handle); must NOT occlude composer; grip dismisses; touch targets ≥ 44px | `[shell]` D5 |
| 768px | **Boundary** — at/below = bottom-sheet, above = side push/split column | `[shell]` D5 (the `<768px` rule) |
| 1024px | **Laptop-squeeze guard** — `--panel-w` clamped via `min()`/`clamp()`; chat keeps readable floor (≥600px), run-card tool output must not wrap illegibly; panel may drop to rail if chat would starve | `[shell]` "What to Avoid" |
| 1440px | Full side panel at `clamp(300px, 30%, 420px)`; comfortable two-canvas | `[shell]` D1 |

Mobile bottom-sheet must never trap the user — grip dismisses back to chat; the panel must never cover the composer.

---

## Accessibility Contract (Phase 088 gate — bake in NOW, A11Y-01/02)

| Concern | Contract |
|---------|----------|
| Panel landmark | `<aside role="complementary" aria-label="Agent workspace">`; sections use `role="region"` + `aria-labelledby` on each `.sec-head` |
| Accordion a11y | `.sec-head` is a `<button>` with `aria-expanded`; Enter/Space toggle; chevron decorative (`aria-hidden`) |
| File list | `role="listbox"`/`option` or arrow-key roving tabindex; Enter/Space opens preview; Tab/Shift-Tab traverses; **no mouse-only path** (A11Y-02) |
| Preview | `Escape` closes drill-in back to list (mirror the `‹ Files` button); focus returns to the originating file row |
| Version picker | pills keyboard-operable; selected base/target announced (not color-only — add text/aria, e.g. `aria-label="base version 2"` / `"target version 3"`) since red/green alone fails color-blind users |
| Diff | container `role="region" aria-label="Diff v2 to v3"`; `+/−` conveyed via the sign-gutter text (not color alone) |
| `⤢` overlay | shadcn `dialog` → focus trap + `Escape` close + focus restore on close (Radix gives this free) |
| ask_user form | label associated with input; choice chips are real `<button role="radio">` in a `radiogroup`; submit `aria-disabled` until valid; on answer, move focus + announce "Answered, agent resumed" via `aria-live="polite"` |
| Pending-Q signalling | the pending state must be conveyed non-visually — `aria-live="assertive"` region announces "Agent needs your input"; rail warn badge + toggle dot have text alternatives |
| Focus ring | visible focus indicator on ALL interactive elements (use `--ring` primary; never `outline:none` without replacement) |
| Contrast | all text ≥ 4.5:1 (see Color section at-risk pairs) |
| Motion | respect `prefers-reduced-motion` for `dotBounce`/`fileFlash`/`progressShimmer`/grid transition (Phase 088 will check) |

---

## Copywriting Contract

| Element | Copy | Source |
|---------|------|--------|
| Panel title | `Workspace` | `[shell]` |
| Primary CTA (ask_user submit) | `Send` (disabled until pick-or-type) | `[ask]` |
| ask_user label | `Needs you` (with countdown `m:ss`) | `[ask]` |
| ask_user free-text placeholder | `Type an answer…` | `[ask]` |
| ask_user "or" divider | `or` (uppercase, between chips and free-text) | `[ask]` |
| Answered confirmation | `Answered · agent resumed` + `You answered <value>` | `[ask]` D4 |
| Timeout (expired) | `No response within <m:ss> — agent stopped` | `[ask]` D5 |
| Empty-state heading | `No workspace activity yet` | default (sketch shows icon + hint; heading not literal — see flag) |
| Empty-state body | `When the agent writes files, tracks todos, or needs your input, it'll show up here.` + collapse-to-rail affordance | derived from `[shell]` D3 |
| Files back button | `‹ Files` | `[files]` |
| File preview fallback | `No preview available · Download` and `File too large to preview` | `[files]` D2 |
| Versions compare label | `Compare` + one-click default `Compare v2↔v3` | `[files]` D5 |
| Diff summary | `+N` / `−M` | `[files]` D3 |
| Live seam pointers | `→ updated todos · see panel` · `→ wrote <file> · see panel` | `[seam]` D2 |
| Reloaded todo note | `☑ 3 todos · all done` (counts from final state) | `[seam]` D3 |
| Reloaded seam card link | `open panel ↗` | `[seam]` D4 |
| Composer-locked hint | `Agent is paused` | `[ask]` D2 |
| Paused run-step | `⏸ ask_user · awaiting your answer →` + `Answer in panel` pointer | `[ask]` D2 |
| Rail count badges | `Todos 2/3` · `Files 4` · amber badge when Q pending | `[shell]` D4 |

**Destructive actions in THIS phase: NONE.** `workspace_delete` is an agent tool (Phase 084/085), not a user-facing panel control — PANEL-01..04/07 scope is read/preview/diff/answer only (REQUIREMENTS "Out of Scope": user inline editing deferred to v2.8). No destructive confirmation dialogs are in scope. If a file "Download" link is added it is non-destructive.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `button`, `dialog`, `scroll-area` (already in `components/ui/`); possibly `sheet` for mobile bottom-sheet | not required (official) |
| Third-party | **NONE** | not applicable |

No third-party registries declared. SC#3 forbids new UI libraries (reuse `MarkdownRenderer` + `react-syntax-highlighter`). Registry vetting gate: **not triggered.**

---

## Open Contracts (flagged — sketches did not fully resolve)

These are NOT redesigns; they are gaps the executor/planner must resolve, with a recommended default:

1. **`--warning` + `--text-dim` token addition.** Amber and dim-label colors are used everywhere but absent from `index.css`. **Recommend:** add `.dark { --warning: 38 92% 60%; --warning-foreground: 240 60% 8%; }` and a dim text var, matching `[theme]`. (Token addition, not a visual decision.)
2. **Empty-state exact heading.** Sketch shows icon + hint, no literal heading string. **Recommend** the copy above; confirm at plan-phase if a different voice is wanted.
3. **CSV table preview component.** `[files]` D2 says "rendered as a table" but no existing CSV renderer was found in repo scan. **Recommend** a minimal in-panel `<table>` (no new dep) with the graceful fallback for malformed/huge CSV. Flag for planner — this is the one preview type without a ready renderer.
4. **Multiple parallel `ask_user` stacking order.** Hook returns `PendingAsk[]`; sketches show a single pinned card. **Recommend** stack newest-pinned-on-top, each its own card, all sticky — but visual density of 2+ simultaneous asks is unverified by sketch. Low risk (rare); confirm if a cap/scroll is needed.
5. **Diff source.** PANEL-07 says "pre-computed version deltas" and Phase 084 `workspace_diff` returns a structured diff. **Confirm** the REST diff shape (hunks/lines) the `VersionDiff` component renders — wire contract lives in Phase 084/086 API, not a visual question, but the planner must point `VersionDiff` at the real endpoint.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
