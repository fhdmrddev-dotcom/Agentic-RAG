---
phase: 262-an-expert-you-can-discover
plan: 05
type: tdd
wave: 5
depends_on: ["262-01", "262-04"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/lib/nav-items.ts
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/components/layout/__tests__/NavPanel.test.tsx
  - frontend/src/lib/nav-items.test.ts
  - frontend/src/components/chat/__tests__/ComposerExpert.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .claude/skills/sketch-findings-agentic-rag/SKILL.md
  - .claude/skills/sketch-findings-agentic-rag/references/app-information-architecture.md
autonomous: true
requirements: [PACK-11, PACK-12, PACK-13]

must_haves:
  truths:
    - "A normal user reaches the Expert catalog from the nav rail, from the mobile drawer, and from the composer + menu — without typing a URL."
    - "The `experts` ActiveView member, its ChatLayout branch and its entry action ship in ONE commit, and the fence built in plan 01 goes from RED to GREEN on real code."
    - "One action from the detail view lands the caller in a chat thread the server already knows is scoped to that Expert."
    - "The navigation/IA contract records that the app grew a home, in the same commit that grew it, in every register that states it."
  artifacts:
    - path: "frontend/src/lib/nav-items.ts"
      provides: "The eighth NAV_ITEMS entry — rail AND mobile drawer from one array"
      contains: "view: \"experts\""
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "The ExpertCatalogPage mount, placed before the trailing UnknownViewFallback"
    - path: "frontend/src/components/layout/__tests__/NavPanel.test.tsx"
      provides: "The entry-action cases — the leg no typecheck and no unit test notices on its own"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/layout/ChatLayout.tsx"
      via: "the ActiveView member and its matching positional branch"
      pattern: "activeView === \"experts\""
    - from: "frontend/src/lib/nav-items.ts"
      to: "frontend/src/App.tsx handleNavigate"
      via: "NavPanel onNavigate(item.view)"
      pattern: "view: \"experts\""
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "frontend/src/components/experts/catalog/startScopedChat.ts"
      via: "onStartChat wired from newThread / setThreadActiveExpert / loadThreads / selectThread / onNavigate"
      pattern: "startScopedChat"
    - from: "frontend/src/components/chat/MessageInput.tsx"
      to: "frontend/src/components/layout/ChatLayout.tsx"
      via: "onBrowseExperts prop chain, the onOpenConnections precedent"
      pattern: "onBrowseExperts"
---

<objective>
Make the catalog reachable, in one commit, with the guard that can fail if it is not — and put the
IA change on the record in the same commit that makes it.

Purpose: D-262-03's reachability triad. A union member is not reachability. `ChatLayout`'s trailing
else is a POSITIONAL FALLBACK, and RESEARCH R-2 measured that **nothing automatic catches a
branchless member** — not `tsc` (`activeView as never` is always a legal assertion), not
`ChatLayout.fallback.test.tsx` (four source-text assertions that mount nothing), not
`renameFence.test.ts:142` (`members.length >= 10`). Phase 257 shipped the spend cockpit with its
member and its mount and **no entry action**, and the only way in was typing a URL; its own test
file records the diagnosis. This plan does not repeat that.

⚠ AND IT CORRECTS A CLAIM THAT ROTTED IN THREE REGISTERS AT ONCE. `App.tsx:103-105`, this phase's
own CONTEXT, and `Skill("sketch-findings-agentic-rag")`'s IA reference D6 all still say the trailing
else is `<KnowledgeHealthPage />` and that a branchless member *"silently renders Knowledge Health"*.
It has been `<UnknownViewFallback view={activeView as never} />` since Phase **217.1-14**. The
discipline survives; the consequence is wrong; the prose is the rot and `App.tsx` is its source.

Output: the triad, both entry actions, the one-action start wired, and three registers corrected.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md
@.planning/phases/262-an-expert-you-can-discover/262-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Extracted at the phase base + plans 03/04's declared contracts. -->

frontend/src/components/experts/catalog/ExpertCatalogPage.tsx (plan 03/04) — props:
  { folders: Folder[]; onStartChat(expert: ExpertBundle): void | Promise<void>; onInspect?(expert): void }

frontend/src/components/experts/catalog/startScopedChat.ts (plan 04):
  startScopedChat(deps: { createThread(): Promise<Thread>; setExpert(threadId, expertId): Promise<Thread>;
                          refreshThreads(): Promise<void>; selectThread(t: Thread): void; navigate(): void },
                  expert: ExpertBundle): Promise<void>

frontend/src/components/layout/ChatLayout.tsx — already in scope, nothing new to fetch:
  :135-144  const { threads, selectedThread, loadThreads, selectThread, newThread, … } = useThreads()
            const { folders } = useFolders()
  :702      the mobile drawer maps the SAME `navItems` array as the rail
  :828      onOpenConnections={() => onNavigate("connections")}   ← the prop-chain precedent to copy
  :979-983  ) : activeView === "admin-spend" ? ( <AdminSpendPage … /> ) : ( <UnknownViewFallback … /> )

frontend/src/components/chat/ChatArea.tsx — the prop-chain waypoints:
  :36 and :58 the Props interface and the destructure; :540 the MessageInput mount

frontend/src/components/chat/MessageInput.tsx:591-601 — the `+` menu section that already hosts
  `Invite Expert...` as a `DropdownMenuItem` with `data-testid="invite-expert-door"`.

frontend/src/lib/nav-items.ts — `NavItem { view; icon; label; feature?: GovernedFeature }`,
  `NAV_ITEMS` with SEVEN entries (chat · workflows · documents · classification-rules ·
  connections · skills · settings), and `visibleNavItems` filtering on `features[item.feature] !== false`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The triad in one commit — the member, the branch, the entry, and the IA contract on the record</name>
  <files>frontend/src/App.tsx, frontend/src/components/layout/ChatLayout.tsx, frontend/src/lib/nav-items.ts, frontend/src/components/layout/__tests__/NavPanel.test.tsx, frontend/src/lib/nav-items.test.ts, CLAUDE.md, .claude/skills/sketch-findings-agentic-rag/SKILL.md, .claude/skills/sketch-findings-agentic-rag/references/app-information-architecture.md</files>
  <read_first>
    - frontend/src/App.tsx (lines 90-195 — the union, the triad comment block, the 235-08 refusal, handleNavigate)
    - frontend/src/components/layout/ChatLayout.tsx (lines 845-990 — the whole ladder and its trailing else)
    - frontend/src/lib/nav-items.ts (all 118 lines)
    - frontend/src/components/layout/__tests__/NavPanel.test.tsx (lines 90-135 — the Phase 257.1 entry-action cases; this is the shape to copy)
    - frontend/src/pages/__tests__/LibraryPage.initialTab.test.tsx (lines 490-520 — P-3, the NO TWELFTH non-vacuity pair)
    - frontend/src/__tests__/library/renameFence.test.ts (lines 110-160 — P-11, the "the three-homes contract holds" needle, and the ActiveView member-count arm)
    - .claude/skills/sketch-findings-agentic-rag/references/app-information-architecture.md (D1 and D6 — the three-homes rule and the stale KnowledgeHealthPage sentence)
    - .planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md §3 (the ordered edit list and the exact insertion point) and §8 P-1, P-2, P-3, P-10, P-11
  </read_first>
  <behavior>
    - `activeViewReachability(App.tsx?raw, ChatLayout.tsx?raw).unbranched` is empty AFTER this task,
      and the same call against the pre-task App source with the post-task layout — or with the
      branch deleted — reports the new member. That is the RED.
    - Clicking the Experts entry on the rail calls `onNavigate("experts")`.
    - `NAV_ITEMS` contains an `experts` entry; `visibleNavItems({})` still returns it (it is
      ungoverned), so it never vanishes for a member whose feature map has not resolved.
    - `LibraryPage.initialTab.test.tsx` and `renameFence.test.ts` stay green without being weakened.
  </behavior>
  <action>
Land all three legs in ONE commit.

**Leg 1 — the union.** Append the new member to `ActiveView` at `App.tsx:107`. ⛔ Do NOT spell that
literal in any NEW comment in `App.tsx` (D-262-04): the acceptance fences count occurrences in this
file, so prose repeating the member makes a code measurement satisfiable by a comment — the 187-24
lesson, met again three times in Phase 264's wave 3.

**Leg 2 — the branch.** Import `ExpertCatalogPage` in `ChatLayout.tsx`'s import block and insert the
branch between `:980` (`<AdminSpendPage onBack={() => onNavigate("control-room")} />`) and `:981`
(`) : (`), so it is the last-but-one arm and `<UnknownViewFallback` stays last. This preserves
`ChatLayout.launch.test.tsx:547-556`, which asserts `indexOf('activeView === "workflow-run"')` is
less than `indexOf("<UnknownViewFallback")`. Mount it with `folders={folders}` — `useFolders()` is
already held at `:142` and already threaded to three mounts, so this is a fourth consumer and zero
new fetches — and with `onStartChat` built from `startScopedChat`, passing ChatLayout's four shipped
seams: `newThread`, `setThreadActiveExpert`, `loadThreads`, `selectThread`, plus
`() => onNavigate("chat")`.
⛔ P-10: do not write the tokens `<ChatArea` or `<WorkspacePanel` in any new comment in this file —
`ChatLayout.launch.test.tsx:558-576` measures that those tags appear only before the `<main>` split,
and `ChatLayout.tsx:962-965` already records that trap.

**Leg 3 — the entry action.** Add `{ view: "experts", icon: Sparkles, label: "Experts" }` to
`NAV_ITEMS`, importing `Sparkles` from `lucide-react` alongside the existing glyphs. ⭐ It belongs IN
the array — unlike `control-room` and `admin-spend`, which are deliberately outside it — precisely
because the array also feeds ChatLayout's mobile drawer (`:702`), so the catalog is not
desktop-only. Add the Phase-257.1-shaped cases to `NavPanel.test.tsx`: the entry renders, and
clicking it calls `onNavigate` with the new view. Add a case to `nav-items.test.ts` pinning that the
entry is present and carries NO `feature` key.

⛔ **NAV GOVERNANCE: UNGOVERNED, AND THE REASON IS RECORDED BECAUSE D-262-09 ASKED THE RIGHT QUESTION
OF THE WRONG SYSTEM.** `experts` is NOT a `GovernedFeature` — that union is closed at six members
(`lib/api/_core.ts:104-110`, mirrored `user_settings.py:1578-1606`). `require_capability("experts")`
is a per-ORG TIER entitlement through `tier_capabilities`, a different axis with no frontend read
path at all, so tagging the entry would not prevent the 403 D-262-09 worried about; it would add a
second, unrelated governance axis controlled by the operator's audience map rather than by the org's
plan. The precedent is `connections` — ungoverned by design, pinned by `navItemsConnections.test.ts:44-49`
— and `visibleNavItems` has been fail-OPEN since 2026-09-09 (`:118`, *"the API is the wall"*). The
catalog renders the server's refusal honestly instead (plan 03).

**THREE PITFALLS, EACH WITH AN EXPLICIT OUTCOME:**

⛔ **P-3 — `App.tsx:171`'s `⛔ NO TWELFTH ActiveView MEMBER` comment stays.** It is not merely
impolite to remove: `LibraryPage.initialTab.test.tsx:502-503` asserts `APP` contains `"NO TWELFTH"`
and `APP_CODE` does not — it is that suite's COMMENT-STRIPPER NON-VACUITY CONTROL, the pair that
reds if `codeOf` starts returning `""` and silently turns every `not.toContain` arm green. Deleting
it reds a pinned suite for a reason that has nothing to do with it. Leave it addressed to its own
subject (Phase 235-08's plan-scoped refusal about the Library's tab) and add, beside it, one line
saying why this phase's member is not the thing it refuses — ⛔ without naming the member literal.

⛔ **P-11 — `renameFence.test.ts:121-127` greps the literal `"the three-homes contract holds"`, and
that exact string is `App.tsx:100` — the prose D-262-01 mandates changing.** Change the SURROUNDING
sentence and leave that substring intact, so the fence keeps its needle. If the substring must go,
update the fence in this same commit and write the reason in the fence's own body. ⛔ Decide
deliberately; do not discover it from a red run.

⛔ **The stale-prose correction (R-1).** `App.tsx:103-105` claims the trailing else is
`<KnowledgeHealthPage />` and that a branchless member *"silently renders Knowledge Health"*. It has
been `UnknownViewFallback` since 217.1-14. Correct that comment BESIDE its original rather than
overwriting it — in this repository a claim that rots is the finding — and do it by editing the
EXISTING comment, ⛔ never by adding a new one that would repeat the member name.

**THE REGISTERS, SAME COMMIT (D-262-01).** The app grew a home; say so where the contract is
written.
- `.claude/skills/sketch-findings-agentic-rag/references/app-information-architecture.md`: correct
  D6's *"before the trailing `: <KnowledgeHealthPage/>` else"* sentence beside its original — this is
  the THIRD register carrying the same rot — and add one line to D1 recording that the three-homes
  rule is about the WORKFLOW concern triad (Authoring / Library+Launch / Execution) and was never a
  count of nav entries.
- `.claude/skills/sketch-findings-agentic-rag/SKILL.md`: one clause beside the existing
  "three-homes app navigation/IA contract" phrasing, naming Phase 262 and `BUS-303`.
- `CLAUDE.md`: the `frontend/src/lib/nav-items.ts` ledger-row disposition carries the verdict,
  ⛔ 200 characters maximum; the reasons go in that file's section in `docs/HOT-FILE-LEDGER.md`.

⚠ **AND WRITE IT ACCURATELY, because "three → four" is not what the array says.** RESEARCH R-6
measured `NAV_ITEMS` at SEVEN entries before this phase, with the operator shield (`NavPanel:353`)
and the Spend entry (`:395`) rendered outside the array. This is the **eighth `NAV_ITEMS` entry** and
the **tenth rail affordance**. A register corrected with a wrong number is the rot repeating.

⚠ **CLAUDE.md IS 1,209 CHARACTERS FROM ITS 120,000 WARN BAND** (measured at planning, after this
phase's seven at-creation ledger rows landed). Keep every CLAUDE.md edit at net-zero or negative and
put narrative in `docs/HOT-FILE-LEDGER.md`. If the file crosses the band anyway, CLAUDE.md's own
rule applies: **the split is SCHEDULED, not scrambled, and never skipped by dropping the edit** —
record the crossing in the SUMMARY and propose the split at phase close.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/__tests__/activeViewReachability.test.ts src/lib/nav-items.test.ts src/components/layout/__tests__ src/pages/__tests__/LibraryPage.initialTab.test.tsx src/__tests__/library/renameFence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `activeViewReachability` run against the shipped pair reports `unbranched: []` and `fallbackIsLast: true`; the SUMMARY quotes the RED captured with the ChatLayout branch removed.
    - `grep -c 'NO TWELFTH' frontend/src/App.tsx` is at least 1, and `LibraryPage.initialTab.test.tsx` is byte-unchanged and green.
    - `renameFence.test.ts` is green; if its `"the three-homes contract holds"` needle was changed, the diff shows the fence updated in this same commit with the reason in its body.
    - `grep -c 'experts' frontend/src/lib/nav-items.ts` is at least 1 and the entry carries no `feature:` key (a case in `nav-items.test.ts` pins this).
    - `NavPanel.test.tsx` gains at least 2 cases: the entry renders, and clicking it calls `onNavigate` with the new view.
    - `awk` or an index check on `ChatLayout.tsx` shows the new branch's `activeView === ` comparison occurring BEFORE `<UnknownViewFallback`, and `ChatLayout.launch.test.tsx` is green and byte-unchanged.
    - `git show --stat HEAD` for this task's commit lists `App.tsx`, `ChatLayout.tsx` and `nav-items.ts` together — the triad is one commit, not three.
    - `node scripts/check-claude-md-size.cjs` exits 0 with no `[disposition-too-long]`.
  </acceptance_criteria>
  <done>The catalog has a member, a branch and a door, a fence that fails when any of the three is missing, and an IA contract that describes the app as it now is.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The composer entry — a second door, deliberately, on the shipped prop-chain precedent</name>
  <files>frontend/src/components/layout/ChatLayout.tsx, frontend/src/components/chat/ChatArea.tsx, frontend/src/components/chat/MessageInput.tsx, frontend/src/components/chat/__tests__/ComposerExpert.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageInput.tsx (lines 75-95, 140-175 and 585-620 — the Props interface, the destructure, and the `+` menu section holding `Invite Expert...`)
    - frontend/src/components/chat/ChatArea.tsx (lines 30-60 and 535-560 — the Props interface, the destructure, the MessageInput mount)
    - frontend/src/components/layout/ChatLayout.tsx (lines 810-835 — the ChatArea mount and the `onOpenConnections` line to copy)
    - frontend/src/components/chat/__tests__/ComposerExpert.test.tsx (lines 1-70 — the mock factory and the ZERO-new-top-level-controls budget)
    - .planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md §3c edits 4-5 and §8 P-9
  </read_first>
  <behavior>
    - Opening the composer `+` menu reveals a `Browse Expert Catalog…` item beside the shipped
      `Invite Expert...` item.
    - Selecting it closes the menu and invokes the `onBrowseExperts` callback exactly once.
    - The composer's top-level toolbar gains ZERO new buttons — the shipped UI budget holds, because
      a `DropdownMenuItem` is not a toolbar `<button>`.
    - When `onBrowseExperts` is not provided the item does not render, so the four shipped suites
      that mount `MessageInput` from their own prop objects are unaffected.
  </behavior>
  <action>
Thread one optional callback down the shipped chain, copying `onOpenConnections` line for line:
`ChatLayout.tsx:828` passes `onBrowseExperts={() => onNavigate("experts")}` to `ChatArea`;
`ChatArea.tsx` declares it in `Props`, destructures it, and forwards it to `MessageInput`;
`MessageInput.tsx` declares it in `Props`, destructures it, and renders a `DropdownMenuItem` in the
`+` menu's expert section beside `invite-expert-door`, with its own `data-testid` and a `Sparkles`
glyph, whose `onSelect` closes the menu and calls the callback.

⛔ THE PROP IS OPTIONAL AT EVERY HOP. ChatLayout's own header records why: four shipped suites mount
these components from their own prop objects, and a REQUIRED prop would redden `tsc` in files this
plan does not own. Optional costs nothing and keeps the measured typecheck baseline.

⭐ THIS SECOND DOOR IS A DELIBERATE REDUNDANCY, NOT PART OF THE TRIAD. Task 1's nav entry already
discharges D-262-03's third leg. This one exists because the operator's `BUS-303` ruling named both,
and because the mid-thread moment — "I want an Expert for this" — is where a person actually is when
they want one. Say so in the code comment rather than letting a reader infer that one of the two is
the required leg.

⚠ Check `ComposerExpert.test.tsx`'s budget case rather than assuming it: it counts `<button>`
elements inside the toolbar container, and P-9 measured that a `DropdownMenuItem` does not register
there. Add the two new cases to that suite and RAISE its BASELINE pin to the gate's own printed
figure — ⛔ never lower it.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/ComposerExpert.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/chat/__tests__/ComposerExpert.test.tsx` exits 0 with at least 7 cases, including the UI-budget case still asserting zero new toolbar buttons.
    - `grep -c "onBrowseExperts" frontend/src/components/layout/ChatLayout.tsx frontend/src/components/chat/ChatArea.tsx frontend/src/components/chat/MessageInput.tsx` returns at least 1 for each of the three files.
    - `grep -c "onBrowseExperts?:" frontend/src/components/chat/MessageInput.tsx` returns 1 — optional at the leaf.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` is at most 70.
    - The `ComposerExpert.test.tsx` pin in `scripts/vitest-count-gate.cjs` is RAISED, with a comment naming this plan.
  </acceptance_criteria>
  <done>A person mid-conversation can reach the catalog without leaving the composer, and the composer's control budget is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Close-out — re-derive every triple, state every G-5 disposition, re-run every gate</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - CLAUDE.md § "Hot-file ledger" — the three-command recipe and the same-commit sync rule
    - .planning/phases/262-an-expert-you-can-discover/262-01-SUMMARY.md (the triples plan 01 corrected, so this task can show the delta this phase itself caused)
    - .planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md §7b
  </read_first>
  <action>
Re-derive, at this worktree's HEAD, the triple for every source file this PHASE modified: the five
G-5-firing files this plan edited (`App.tsx`, `ChatLayout.tsx`, `nav-items.ts`, `ChatArea.tsx`,
`MessageInput.tsx`), the three other firing files the phase touched (`NavPanel.tsx` if edited,
`types/index.ts`, `lib/api/experts.ts` — record "NOT modified by 262" where true, as an observation
rather than a claim), and all seven files created at planning-time rows
(`activeViewReachability.ts`, `expertIcon.tsx`, and the five under `components/experts/catalog/`).

⚠ Plan 01 corrected the INHERITED staleness; this task records the delta THIS PHASE caused, so the
two are distinguishable in the record. Every previous figure stays readable beside its replacement.

For each of the five files this plan edited, write the G-5 disposition explicitly. The expected
verdict is **honoured by construction**, and G-5 requires that to be STATED AND JUSTIFIED PER FILE
rather than assumed — with the arithmetic, not an argument: `App.tsx` gains one union member and a
corrected comment; `ChatLayout.tsx` gains one import and one branch; `nav-items.ts` gains one array
entry and one lucide import; `ChatArea.tsx` and `MessageInput.tsx` each gain one optional prop and
one forward. ⛔ If any file's edit turns out to be larger than that — a new `useState`, a new
`useEffect`, a new branch beyond the mount — the verdict is NOT "honoured by construction" and the
SUMMARY says so and names the seam the next refactor should take.

Then re-run the gates and record the verdict lines verbatim, and re-run
`node scripts/check-seeds-register.cjs --phase 262` now that every `files_modified` exists.
⚠ **EIGHT seeds fire** — `SEED-185`, `188`, `280`, `284`, `286`, `287`, `296`, `303` — measured at
planning and routed, with a reason each, in
`.planning/phases/262-an-expert-you-can-discover/262-SEEDS-ROUTING.md`. Read that file, confirm or
correct each routing against what the phase actually shipped, and **write the final routing into
each seed's own frontmatter**. ⛔ A seed that shipped and still reads `planted` is re-proposed
forever, and `status:` IS the index — prose inside a seed body saying "still open" is invisible to
the scan. Three of the eight carry a concrete update this phase earned: `SEED-185`'s view count
(twelve → thirteen, still zero addressable), `SEED-287`'s fourth data point (three plans each
hand-adding both knobs), and `SEED-296`'s note that the reachability triad is now fenced. ⛔ And
`SEED-280` is the one to CHECK rather than assume: confirm whether the two nav-items suites plan 01
adopted were among its named five.
  </action>
  <verify>
    <automated>node scripts/check-hot-file-ledger.cjs .planning/phases/262-an-expert-you-can-discover &amp;&amp; node scripts/check-claude-md-size.cjs &amp;&amp; node scripts/check-seeds-register.cjs --phase 262</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-hot-file-ledger.cjs .planning/phases/262-an-expert-you-can-discover` exits 0.
    - `node scripts/check-claude-md-size.cjs` exits 0 with no `[disposition-too-long]`, `[duplicate-row]` or `[malformed-row]`.
    - Every one of the fifteen files named above has a re-derived triple in the SUMMARY with its three command outputs, and both registers agree.
    - Each of the five edited firing files carries an explicit G-5 disposition sentence naming the arithmetic.
    - `node scripts/check-seeds-register.cjs --phase 262` exits 0, and the SUMMARY names each fired seed and its routing, with the seed's frontmatter updated.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root: no per-file DECREASE, and no failing filename beyond `sketchComposition.test.tsx`.
  </acceptance_criteria>
  <done>The registers describe the repository as this phase left it, and the seeds that fired were routed rather than ignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav rail / composer → a new top-level surface | a door into a view that reads org data |
| client → `PATCH /threads/{id}` | the one-action start's write, issued on the caller's behalf |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-17 | Elevation of privilege | the ungoverned nav entry | accept | `experts` is a per-ORG tier entitlement with no frontend read path; `NavItem.feature` governs a different, closed six-member axis and could not prevent the 403. The API is the wall (`require_capability('experts')`), and `visibleNavItems` has been fail-OPEN since 2026-09-09 by a recorded decision. Precedent: `connections` |
| T-262-18 | Information disclosure | the catalog mounted from ChatLayout | mitigate | The branch passes only `folders` (already held by `useFolders()` for the caller) and callbacks. ⛔ No org-wide list, no admin arm, no `for_management` reaches this mount |
| T-262-19 | Spoofing | the one-action start writing an expert onto a thread | mitigate | The PATCH targets a thread the caller just created via `newThread()`; the server authorises it. `startScopedChat` refuses to navigate when the patch rejects, so a failure is never dressed as a scoped chat |
| T-262-20 | Denial of service | the branch inserted in the wrong position | mitigate | `activeViewReachability` asserts `fallbackIsLast`, and `ChatLayout.launch.test.tsx:547-556` independently asserts the positional order. Two fences, one from this phase and one inherited |
| T-262-21 | Repudiation | the IA contract silently contradicted by shipped code | mitigate | D-262-01's same-commit register update, with the corrected count measured (eighth `NAV_ITEMS` entry, tenth rail affordance), and the three-register KnowledgeHealthPage rot corrected beside its originals |
| T-262-SC | Tampering | npm/pip/cargo installs | mitigate | ⛔ NONE. `lucide-react` is already a dependency; no package-legitimacy checkpoint is owed |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib src/components/layout src/components/chat src/components/experts src/pages/__tests__/LibraryPage.initialTab.test.tsx src/__tests__/library/renameFence.test.ts` — exits 0.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — no per-file DECREASE; no failing filename beyond `frontend/src/components/library/__tests__/sketchComposition.test.tsx`. ⛔ `count gate OK` is NOT a reachable criterion: the gate is RED at this phase's base with `failed 2`, both in that suite, proven inherited by an empty `git diff --stat HEAD -- frontend/`.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` — at most 70, the measured base. ⛔ Never "zero"; bare `npx tsc --noEmit` checks ZERO files and exits 0 over anything.
- `node scripts/check-hot-file-ledger.cjs .planning/phases/262-an-expert-you-can-discover` — exit 0.
- `node scripts/check-claude-md-size.cjs` — exit 0.
- `node scripts/check-seeds-register.cjs --phase 262` — exit 0, with every fired seed routed.
- `node scripts/check-backend-unit-baseline.cjs` — not required: this plan touches zero backend files.
</verification>

<success_criteria>
- The union member, the ChatLayout branch and the `NAV_ITEMS` entry appear in ONE commit, shown by `git show --stat`.
- `activeViewReachability` reports `unbranched: []` on the shipped pair, and the SUMMARY quotes the RED taken with the branch removed.
- `LibraryPage.initialTab.test.tsx`, `renameFence.test.ts`, `ChatLayout.launch.test.tsx` and `ChatLayout.fallback.test.tsx` are all green, and none was weakened to make this phase pass.
- The composer `+` menu offers the catalog and the toolbar gains zero buttons.
- The three registers carrying the stale `KnowledgeHealthPage` claim are corrected beside their originals, and the IA contract records an eighth `NAV_ITEMS` entry — not "three → four".
</success_criteria>

<output>
Create `.planning/phases/262-an-expert-you-can-discover/262-05-SUMMARY.md` when done.
</output>
