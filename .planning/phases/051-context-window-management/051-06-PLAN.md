---
phase: 051-context-window-management
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/user_settings.py
  - backend/app/api/settings.py
  - frontend/src/lib/api.ts
  - frontend/src/pages/SettingsPage.tsx
autonomous: true
requirements: [CTX-03]
gap_closure: true

must_haves:
  truths:
    - "GET /api/settings response includes sub_agent_model field (string, default empty string)"
    - "PUT /api/settings with sub_agent_model='gpt-4o-mini' saves and is returned in subsequent GET"
    - "PUT /api/settings with sub_agent_model='' (empty string) clears the override (auto mode)"
    - "Settings AI Model tab Context & Sub-Agent SectionCard has a dropdown for sub-agent model override after the two sliders"
    - "Dropdown options are the active provider's models plus an empty-string leading option labelled Auto (cheapest)"
    - "Saving AI Model settings sends sub_agent_model in the request body"
  artifacts:
    - path: "backend/app/models/user_settings.py"
      provides: "sub_agent_model: str field in UserEffectiveSettings and load_app_settings() wiring"
      contains: "sub_agent_model: str"
    - path: "backend/app/api/settings.py"
      provides: "sub_agent_model in FullSettingsResponse, SettingsUpdate, _build_response, update_settings"
      contains: "sub_agent_model"
    - path: "frontend/src/lib/api.ts"
      provides: "sub_agent_model: string in FullAppSettings; sub_agent_model?: string in SettingsUpdate"
      contains: "sub_agent_model"
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "subAgentModel state + hydrate binding + handleSaveAIModel body entry + FieldRow select dropdown in Context & Sub-Agent SectionCard"
      contains: "subAgentModel"
  key_links:
    - from: "SettingsPage.tsx hydrate()"
      to: "FullAppSettings.sub_agent_model"
      via: "setSubAgentModel(data.sub_agent_model ?? \"\")"
      pattern: "sub_agent_model"
    - from: "SettingsPage.tsx handleSaveAIModel()"
      to: "PUT /api/settings"
      via: "body includes sub_agent_model: subAgentModel"
      pattern: "sub_agent_model: subAgentModel"
    - from: "_build_response()"
      to: "UserEffectiveSettings.sub_agent_model"
      via: "sub_agent_model=s.sub_agent_model"
      pattern: "sub_agent_model=s.sub_agent_model"
---

<objective>
Thread `sub_agent_model` through all six layers of the settings stack: UserEffectiveSettings → settings.py (response + update models + handler + _build_response) → api.ts types → SettingsPage.tsx (state + hydrate + save + dropdown). Add the sub-agent model override dropdown to the existing "Context & Sub-Agent" SectionCard (after the two sliders, before the closing tag).

Per D-09 (CONTEXT.md): dropdown shows available models for the active provider (same source as main model selector — `activeModels` derived from `providerStates`). Empty/blank = auto (cheapest). Saved alongside other AI Model settings via the existing Save AI Model button (D-10).

Purpose: Administrators can override the sub-agent model from the Settings UI without editing .env files. Empty string preserves the existing auto-select-cheapest behaviour in sub_agent_service.py.
Output: Complete 6-layer settings stack for sub_agent_model; dropdown visible in Context & Sub-Agent SectionCard; frontend builds clean.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/051-context-window-management/051-CONTEXT.md
@.planning/phases/051-context-window-management/051-PATTERNS.md
@.planning/phases/051-context-window-management/051-UI-SPEC.md
</context>

<interfaces>
<!--
CURRENT STATE (confirmed by grep — do NOT re-add these, they already exist):
  backend/app/models/user_settings.py line 84:  sub_agent_max_output_tokens: int
  backend/app/models/user_settings.py line 261: sub_agent_max_output_tokens=_int(...)
  backend/app/api/settings.py line 59:           sub_agent_max_output_tokens: int  (FullSettingsResponse)
  backend/app/api/settings.py line 101:          sub_agent_max_output_tokens: int | None = Field(...)  (SettingsUpdate)
  backend/app/api/settings.py line 144:          sub_agent_max_output_tokens=s.sub_agent_max_output_tokens  (_build_response)
  backend/app/api/settings.py line 221:          if body.sub_agent_max_output_tokens is not None: ...  (update_settings)
  frontend/src/lib/api.ts line 413:              sub_agent_max_output_tokens: number  (FullAppSettings)
  frontend/src/lib/api.ts line 449:              sub_agent_max_output_tokens?: number  (SettingsUpdate)
  frontend/src/pages/SettingsPage.tsx line 521:  const [subAgentMaxOutputTokens, setSubAgentMaxOutputTokens] = useState(8192)
  frontend/src/pages/SettingsPage.tsx line 553:  setSubAgentMaxOutputTokens(data.sub_agent_max_output_tokens ?? 8192)
  frontend/src/pages/SettingsPage.tsx line 577:  sub_agent_max_output_tokens: subAgentMaxOutputTokens,
  frontend/src/pages/SettingsPage.tsx lines 762-787: Context & Sub-Agent SectionCard with 2 SliderInput rows

TARGET ADDITIONS (what this plan adds — exact field names and types):

Layer 1 — backend/app/models/user_settings.py
  In UserEffectiveSettings class, after line 84 (sub_agent_max_output_tokens: int):
    sub_agent_model: str

  In load_app_settings() return, after sub_agent_max_output_tokens= line (~261):
    sub_agent_model=_str(override, "sub_agent_model", env_settings.sub_agent_model),

  The _str() helper already exists in this file — grep confirms pattern:
    _bool(override, "sandbox_enabled", env_settings.sandbox_enabled)
    _int(override, "sub_agent_max_output_tokens", env_settings.sub_agent_max_output_tokens)
  Use the same _str() pattern.

Layer 2 — backend/app/api/settings.py
  In FullSettingsResponse, after sub_agent_max_output_tokens: int (line 59):
    sub_agent_model: str

  In SettingsUpdate, after sub_agent_max_output_tokens: int | None = Field(...) (line 101):
    sub_agent_model: str | None = None

  In _build_response() return, after sub_agent_max_output_tokens=s.sub_agent_max_output_tokens (line 144):
    sub_agent_model=s.sub_agent_model,

  In update_settings() handler, after the sub_agent_max_output_tokens if-block (line 221):
    if body.sub_agent_model is not None:
        updates["sub_agent_model"] = body.sub_agent_model

Layer 3 — frontend/src/lib/api.ts
  In FullAppSettings interface, after sub_agent_max_output_tokens: number (line 413):
    sub_agent_model: string

  In SettingsUpdate interface, after sub_agent_max_output_tokens?: number (line 449):
    sub_agent_model?: string

Layer 4 — frontend/src/pages/SettingsPage.tsx
  State (after subAgentMaxOutputTokens state, line 521):
    const [subAgentModel, setSubAgentModel] = useState("")

  hydrate() (after setSubAgentMaxOutputTokens call, line 553):
    setSubAgentModel(data.sub_agent_model ?? "")

  handleSaveAIModel() body (after sub_agent_max_output_tokens: subAgentMaxOutputTokens, line 577):
    sub_agent_model: subAgentModel,

  JSX — inside Context & Sub-Agent SectionCard, AFTER the closing </FieldRow> of the
  "Sub-agent output tokens" row (after line 786), BEFORE </SectionCard> (line 787):

    <FieldRow label="Sub-agent model">
      <select
        value={subAgentModel}
        onChange={(e) => setSubAgentModel(e.target.value)}
        className="w-full h-8 text-xs font-mono bg-muted/30 border border-input rounded px-2 text-foreground focus:outline-none focus:ring-2 focus:ring-ring"
      >
        <option value="">Auto (cheapest)</option>
        {activeModels
          .split(",")
          .map((m) => m.trim())
          .filter(Boolean)
          .map((m) => (
            <option key={m} value={m}>{m}</option>
          ))}
      </select>
    </FieldRow>

  activeModels is already defined at line 667:
    const activeModels = providerStates.find((p) => p.id === activeProvider)?.models ?? ""
  No new variable needed — use it directly.

  NOTE: Do NOT use shadcn Select component — a plain HTML <select> avoids portalling
  issues inside DropdownMenuContent and matches the minimal styling of the SliderInput spans.
  The className uses existing design tokens (bg-muted/30, border-input, text-foreground, ring).
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Thread sub_agent_model through backend settings stack</name>
  <read_first>
    - backend/app/models/user_settings.py (lines 75-95 for UserEffectiveSettings fields; lines 255-270 for load_app_settings() return statement — confirm _str helper exists and find insertion points)
    - backend/app/api/settings.py (full file read — confirm current state of FullSettingsResponse, SettingsUpdate, _build_response, update_settings)
    - backend/app/config.py (line ~186 — confirm sub_agent_model: str = "" exists)
  </read_first>
  <files>backend/app/models/user_settings.py, backend/app/api/settings.py</files>
  <action>
PART A — backend/app/models/user_settings.py

Read the file first. In `UserEffectiveSettings`, locate the line:
  sub_agent_max_output_tokens: int
Add immediately after it:
  sub_agent_model: str

In `load_app_settings()` return statement, locate the line:
  sub_agent_max_output_tokens=_int(override, "sub_agent_max_output_tokens", env_settings.sub_agent_max_output_tokens),
Add immediately after it:
  sub_agent_model=_str(override, "sub_agent_model", env_settings.sub_agent_model),

Confirm the `_str()` helper is defined in this file (grep for `def _str`). If absent, add it adjacent to the existing `_int` and `_bool` helpers:
  def _str(override: dict, key: str, default: str) -> str:
      v = override.get(key)
      return str(v) if v is not None else default

PART B — backend/app/api/settings.py

Read the full file. Make four targeted insertions:

1. In `FullSettingsResponse` — after `sub_agent_max_output_tokens: int`, add:
   sub_agent_model: str

2. In `SettingsUpdate` — after `sub_agent_max_output_tokens: int | None = Field(default=None, ge=4096, le=65536)`, add:
   sub_agent_model: str | None = None

3. In `_build_response()` return — after `sub_agent_max_output_tokens=s.sub_agent_max_output_tokens,`, add:
   sub_agent_model=s.sub_agent_model,

4. In `update_settings()` handler — after the `if body.sub_agent_max_output_tokens is not None:` block, add:
   if body.sub_agent_model is not None:
       updates["sub_agent_model"] = body.sub_agent_model
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && python -c "
from app.api.settings import FullSettingsResponse, SettingsUpdate
from app.models.user_settings import UserEffectiveSettings
assert 'sub_agent_model' in FullSettingsResponse.model_fields, 'Missing from FullSettingsResponse'
assert 'sub_agent_model' in SettingsUpdate.model_fields, 'Missing from SettingsUpdate'
assert 'sub_agent_model' in UserEffectiveSettings.model_fields, 'Missing from UserEffectiveSettings'
# Confirm empty string is valid (default)
u = SettingsUpdate(sub_agent_model='')
assert u.sub_agent_model == ''
u2 = SettingsUpdate(sub_agent_model='gpt-4o-mini')
assert u2.sub_agent_model == 'gpt-4o-mini'
print('All assertions passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "sub_agent_model" backend/app/models/user_settings.py` returns at least 2 matches (field + load_app_settings wiring)
    - `grep "sub_agent_model" backend/app/api/settings.py` returns at least 4 matches (FullSettingsResponse, SettingsUpdate, _build_response, update_settings handler)
    - `python -c "from app.api.settings import FullSettingsResponse; assert 'sub_agent_model' in FullSettingsResponse.model_fields"` exits 0
    - `python -c "from app.models.user_settings import UserEffectiveSettings; assert 'sub_agent_model' in UserEffectiveSettings.model_fields"` exits 0
    - `python -m pytest backend/tests/unit/test_settings.py -x -q` exits 0 (existing tests still green)
  </acceptance_criteria>
  <done>sub_agent_model: str threaded through UserEffectiveSettings, FullSettingsResponse, SettingsUpdate, _build_response, and update_settings handler; existing test suite still passes</done>
</task>

<task type="auto">
  <name>Task 2: Add sub_agent_model to frontend types and SettingsPage dropdown</name>
  <read_first>
    - frontend/src/lib/api.ts (lines 388-460 — FullAppSettings and SettingsUpdate interfaces; confirm current trailing fields after sub_agent_max_output_tokens)
    - frontend/src/pages/SettingsPage.tsx (lines 515-590 — state declarations, hydrate(), handleSaveAIModel(); lines 660-670 for activeModels derivation; lines 762-800 for Context & Sub-Agent SectionCard JSX and the Save button that follows it)
  </read_first>
  <files>frontend/src/lib/api.ts, frontend/src/pages/SettingsPage.tsx</files>
  <action>
PART A — frontend/src/lib/api.ts

Read the interfaces. In `FullAppSettings`, after `sub_agent_max_output_tokens: number`, add:
  sub_agent_model: string

In `SettingsUpdate`, after `sub_agent_max_output_tokens?: number`, add:
  sub_agent_model?: string

PART B — frontend/src/pages/SettingsPage.tsx

Read lines 515-590 and 662-800 before editing.

B1 — State variable. After the line:
  const [subAgentMaxOutputTokens, setSubAgentMaxOutputTokens] = useState(8192)
Add:
  const [subAgentModel, setSubAgentModel] = useState("")

B2 — hydrate(). After the line:
  setSubAgentMaxOutputTokens(data.sub_agent_max_output_tokens ?? 8192)
Add:
  setSubAgentModel(data.sub_agent_model ?? "")

B3 — handleSaveAIModel() body. Inside the `const body: SettingsUpdate = { ... }` block, after:
  sub_agent_max_output_tokens: subAgentMaxOutputTokens,
Add:
  sub_agent_model: subAgentModel,

B4 — JSX dropdown. Inside the "Context & Sub-Agent" SectionCard, locate the closing
`</FieldRow>` of the "Sub-agent output tokens" row (after the SliderInput for subAgentMaxOutputTokens).
Insert the following FieldRow AFTER that closing tag, BEFORE the `</SectionCard>` closing tag:

```tsx
<FieldRow label="Sub-agent model">
  <select
    value={subAgentModel}
    onChange={(e) => setSubAgentModel(e.target.value)}
    className="w-full h-8 text-xs font-mono bg-muted/30 border border-input rounded px-2 text-foreground focus:outline-none focus:ring-2 focus:ring-ring"
  >
    <option value="">Auto (cheapest)</option>
    {activeModels
      .split(",")
      .map((m) => m.trim())
      .filter(Boolean)
      .map((m) => (
        <option key={m} value={m}>{m}</option>
      ))}
  </select>
</FieldRow>
```

`activeModels` is already defined at line ~667 as:
  const activeModels = providerStates.find((p) => p.id === activeProvider)?.models ?? ""
Do NOT redefine it — reference the existing variable directly.

Do NOT add a new Save button. The existing "Save AI Model" button (after the SectionCard closing tag) saves all fields including sub_agent_model via handleSaveAIModel() (D-10).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (zero TypeScript errors)
    - `grep "sub_agent_model: string" frontend/src/lib/api.ts` returns 1 match (FullAppSettings)
    - `grep "sub_agent_model\?: string" frontend/src/lib/api.ts` returns 1 match (SettingsUpdate)
    - `grep "subAgentModel" frontend/src/pages/SettingsPage.tsx` returns at least 4 matches (state decl, hydrate, handleSaveAIModel body, JSX)
    - `grep "Auto (cheapest)" frontend/src/pages/SettingsPage.tsx` returns 1 match (dropdown option)
    - `grep "Sub-agent model" frontend/src/pages/SettingsPage.tsx` returns 1 match (FieldRow label)
  </acceptance_criteria>
  <done>Frontend builds clean; sub_agent_model in api.ts interfaces; subAgentModel state + hydrate + save wired; dropdown renders in Context & Sub-Agent SectionCard with Auto (cheapest) as leading option and active provider models as remaining options</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PUT /api/settings | Authenticated user sends sub_agent_model string value |
| SettingsUpdate Pydantic model | Validates before any persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-051-06-01 | Tampering | sub_agent_model string in PUT /api/settings | accept | String field with no range constraint — sub_agent_service.py already falls back to _SUB_AGENT_MODEL_DEFAULTS when the model string is unrecognised. No injection risk: value is stored in settings_override.json and passed as a string to the provider SDK which validates model names against its own API. |
| T-051-06-02 | Elevation of Privilege | Settings API | accept | /api/settings already uses get_current_user dependency — unchanged by this plan. sub_agent_model flows through the same authenticated path. |
</threat_model>

<verification>
After both tasks complete:

```bash
# Backend import check
cd "C:/Vibe Apps/Agentic RAG/backend" && python -c "
from app.api.settings import FullSettingsResponse, SettingsUpdate
from app.models.user_settings import UserEffectiveSettings
print('FullSettingsResponse fields:', list(FullSettingsResponse.model_fields.keys()))
print('sub_agent_model in FullSettingsResponse:', 'sub_agent_model' in FullSettingsResponse.model_fields)
print('sub_agent_model in SettingsUpdate:', 'sub_agent_model' in SettingsUpdate.model_fields)
print('sub_agent_model in UserEffectiveSettings:', 'sub_agent_model' in UserEffectiveSettings.model_fields)
"

# Existing tests still green
cd "C:/Vibe Apps/Agentic RAG/backend" && python -m pytest tests/unit/test_settings.py -x -q

# Frontend build
cd "C:/Vibe Apps/Agentic RAG/frontend" && npm run build
```
</verification>

<success_criteria>
- `python -c "from app.api.settings import FullSettingsResponse; assert 'sub_agent_model' in FullSettingsResponse.model_fields"` exits 0
- `python -c "from app.models.user_settings import UserEffectiveSettings; assert 'sub_agent_model' in UserEffectiveSettings.model_fields"` exits 0
- `python -m pytest backend/tests/unit/test_settings.py -x -q` exits 0
- `npm run build` in frontend exits 0
- `grep "subAgentModel" frontend/src/pages/SettingsPage.tsx` returns at least 4 matches
- `grep "Auto (cheapest)" frontend/src/pages/SettingsPage.tsx` returns 1 match
- `grep "sub_agent_model" backend/app/api/settings.py` returns at least 4 matches
</success_criteria>

<output>
After completion, create `.planning/phases/051-context-window-management/051-06-SUMMARY.md`
</output>
