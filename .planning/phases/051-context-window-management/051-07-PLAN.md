---
phase: 051-context-window-management
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/model-info.ts
  - frontend/src/components/chat/MessageInput.tsx
autonomous: true
requirements: [CTX-04]
gap_closure: true

must_haves:
  truths:
    - "ModelInfo interface has a costTier field typed 'low' | 'mid' | 'high'"
    - "All 11 model entries in MODEL_INFO have a costTier value"
    - "Model info tooltip in MessageInput shows a Cost tier row between Max output and Best for"
    - "Tooltip cost tier row displays 'Low ($)' / 'Mid ($$)' / 'High ($$$)' based on the costTier value"
  artifacts:
    - path: "frontend/src/lib/model-info.ts"
      provides: "ModelInfo interface with costTier field + all 11 entries populated"
      contains: "costTier"
    - path: "frontend/src/components/chat/MessageInput.tsx"
      provides: "Cost tier row in TooltipContent"
      contains: "Cost tier"
  key_links:
    - from: "MessageInput.tsx TooltipContent"
      to: "MODEL_INFO[m].costTier"
      via: "info.costTier lookup in existing info && block"
      pattern: "info.costTier"
---

<objective>
Add `costTier: 'low' | 'mid' | 'high'` to the `ModelInfo` interface in `model-info.ts`, populate it for all 11 model entries, then render it as a fourth line in the `TooltipContent` in `MessageInput.tsx`.

REQUIREMENTS.md CTX-04: "Each model entry in the model selector shows an inline info card with: context window size, max output tokens, cost tier, and best-use-case label."

NOTE: CONTEXT.md D-12 deferred cost tier as "too volatile". However, REQUIREMENTS.md CTX-04 and ROADMAP SC #4 explicitly require it, and VERIFICATION.md confirmed it as a BLOCKER gap. The requirement contract takes precedence over the deferred-idea note. The string label approach (`"low" | "mid" | "high"`) keeps the display stable even if exact pricing changes — the tier label remains accurate at relative terms.

Purpose: Model info tooltips are complete per the requirements contract — 4 data points shown.
Output: Updated model-info.ts with costTier on all entries; MessageInput tooltip renders Cost tier row.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/051-context-window-management/051-CONTEXT.md
@.planning/phases/051-context-window-management/051-PATTERNS.md
</context>

<interfaces>
<!--
CURRENT STATE — frontend/src/lib/model-info.ts (confirmed by file read):

  export interface ModelInfo {
    contextWindow: number
    maxOutputTokens: number
    bestFor: string
  }

  export const MODEL_INFO: Record<string, ModelInfo> = {
    "gpt-4o":          { contextWindow: 100_000, maxOutputTokens: 16384, bestFor: "General purpose, vision" },
    "gpt-4o-mini":     { contextWindow: 100_000, maxOutputTokens: 16384, bestFor: "Fast, cost-efficient" },
    "gpt-4.1":         { contextWindow: 400_000, maxOutputTokens: 32768, bestFor: "Long context, coding" },
    "gpt-4.1-mini":    { contextWindow: 400_000, maxOutputTokens: 32768, bestFor: "Fast long context" },
    "gpt-4.1-nano":    { contextWindow: 400_000, maxOutputTokens: 16384, bestFor: "Ultra-fast, low cost" },
    "claude-sonnet-4-6":         { contextWindow: 150_000, maxOutputTokens: 32768, bestFor: "Reasoning, long docs" },
    "claude-opus-4-6":           { contextWindow: 150_000, maxOutputTokens: 16384, bestFor: "Complex tasks, analysis" },
    "claude-haiku-4-5-20251001": { contextWindow: 150_000, maxOutputTokens: 8192,  bestFor: "Fast analysis, summaries" },
    "gemini-2.5-pro":            { contextWindow: 600_000, maxOutputTokens: 32768, bestFor: "Very long context, research" },
    "gemini-2.5-flash":          { contextWindow: 600_000, maxOutputTokens: 32768, bestFor: "Fast, multimodal" },
    "gemini-2.5-flash-lite":     { contextWindow: 600_000, maxOutputTokens: 16384, bestFor: "Ultra-fast, high volume" },
  }

CURRENT STATE — frontend/src/components/chat/MessageInput.tsx TooltipContent (lines 226-230):

  <TooltipContent side="right" className="max-w-[200px] text-xs space-y-1">
    <div><span className="font-bold">Context:</span> {info.contextWindow.toLocaleString()} tokens</div>
    <div><span className="font-bold">Max output:</span> {info.maxOutputTokens.toLocaleString()} tokens</div>
    <div><span className="font-bold">Best for:</span> {info.bestFor}</div>
  </TooltipContent>

TARGET STATE — model-info.ts interface (add costTier field):

  export interface ModelInfo {
    contextWindow: number
    maxOutputTokens: number
    /** Relative cost tier: 'low' ($), 'mid' ($$), 'high' ($$$) */
    costTier: 'low' | 'mid' | 'high'
    bestFor: string
  }

TARGET STATE — costTier values per model (from gap closure instructions):
  "gpt-4o":                  high
  "gpt-4o-mini":             low
  "gpt-4.1":                 high
  "gpt-4.1-mini":            mid
  "gpt-4.1-nano":            low
  "claude-sonnet-4-6":       high
  "claude-opus-4-6":         high
  "claude-haiku-4-5-20251001": low
  "gemini-2.5-pro":          high
  "gemini-2.5-flash":        mid
  "gemini-2.5-flash-lite":   low

TARGET STATE — MessageInput.tsx TooltipContent (insert Cost tier row between Max output and Best for):

  <TooltipContent side="right" className="max-w-[200px] text-xs space-y-1">
    <div><span className="font-bold">Context:</span> {info.contextWindow.toLocaleString()} tokens</div>
    <div><span className="font-bold">Max output:</span> {info.maxOutputTokens.toLocaleString()} tokens</div>
    <div><span className="font-bold">Cost tier:</span> {info.costTier === 'low' ? 'Low ($)' : info.costTier === 'mid' ? 'Mid ($$)' : 'High ($$$)'}</div>
    <div><span className="font-bold">Best for:</span> {info.bestFor}</div>
  </TooltipContent>
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add costTier to ModelInfo interface and all 11 model entries</name>
  <read_first>
    - frontend/src/lib/model-info.ts (full file — confirm current interface fields and all 11 entry keys)
  </read_first>
  <files>frontend/src/lib/model-info.ts</files>
  <action>
Read the full file. Replace its content with the updated version that:

1. Adds `costTier: 'low' | 'mid' | 'high'` to the `ModelInfo` interface, after `maxOutputTokens` and before `bestFor`, with a JSDoc comment:
   /** Relative cost tier: 'low' ($), 'mid' ($$), 'high' ($$$) */

2. Adds `costTier: 'high' | 'mid' | 'low'` to each of the 11 model entries using these exact values:
   - "gpt-4o":                    costTier: 'high'
   - "gpt-4o-mini":               costTier: 'low'
   - "gpt-4.1":                   costTier: 'high'
   - "gpt-4.1-mini":              costTier: 'mid'
   - "gpt-4.1-nano":              costTier: 'low'
   - "claude-sonnet-4-6":         costTier: 'high'
   - "claude-opus-4-6":           costTier: 'high'
   - "claude-haiku-4-5-20251001": costTier: 'low'
   - "gemini-2.5-pro":            costTier: 'high'
   - "gemini-2.5-flash":          costTier: 'mid'
   - "gemini-2.5-flash-lite":     costTier: 'low'

Keep all other fields (contextWindow, maxOutputTokens, bestFor) and all comments unchanged.
The file header comment block must be preserved as-is.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && node -e "
const fs = require('fs');
const content = fs.readFileSync('src/lib/model-info.ts', 'utf8');
const models = ['gpt-4o','gpt-4o-mini','gpt-4.1','gpt-4.1-mini','gpt-4.1-nano','claude-sonnet-4-6','claude-opus-4-6','claude-haiku-4-5-20251001','gemini-2.5-pro','gemini-2.5-flash','gemini-2.5-flash-lite'];
models.forEach(m => { if (!content.includes('\"' + m + '\"')) throw new Error('Missing model: ' + m); });
if (!content.includes('costTier')) throw new Error('costTier field missing');
const tierCount = (content.match(/costTier/g) || []).length;
if (tierCount < 12) throw new Error('Expected >=12 costTier occurrences (interface + 11 entries), got ' + tierCount);
console.log('All checks passed, costTier occurrences:', tierCount);
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "costTier" frontend/src/lib/model-info.ts` returns at least 12 matches (1 interface field + 11 model entries)
    - `grep "'low' | 'mid' | 'high'" frontend/src/lib/model-info.ts` returns 1 match (interface type)
    - All 11 model keys still present in file (no entries accidentally removed)
    - File content is valid TypeScript (confirmed by npm run build in Task 2)
  </acceptance_criteria>
  <done>ModelInfo interface has costTier field; all 11 MODEL_INFO entries have correct costTier value; no entries removed or renamed</done>
</task>

<task type="auto">
  <name>Task 2: Render cost tier row in MessageInput tooltip</name>
  <read_first>
    - frontend/src/components/chat/MessageInput.tsx (lines 195-240 — the models.map block containing the Tooltip, TooltipTrigger, TooltipContent, and the three existing div rows)
  </read_first>
  <files>frontend/src/components/chat/MessageInput.tsx</files>
  <action>
Read lines 195-240 of the file. Locate the `TooltipContent` block:

```tsx
<TooltipContent side="right" className="max-w-[200px] text-xs space-y-1">
  <div><span className="font-bold">Context:</span> {info.contextWindow.toLocaleString()} tokens</div>
  <div><span className="font-bold">Max output:</span> {info.maxOutputTokens.toLocaleString()} tokens</div>
  <div><span className="font-bold">Best for:</span> {info.bestFor}</div>
</TooltipContent>
```

Insert one new `<div>` row BETWEEN the "Max output" row and the "Best for" row:

```tsx
<div><span className="font-bold">Cost tier:</span> {info.costTier === 'low' ? 'Low ($)' : info.costTier === 'mid' ? 'Mid ($$)' : 'High ($$$)'}</div>
```

The resulting TooltipContent must be:

```tsx
<TooltipContent side="right" className="max-w-[200px] text-xs space-y-1">
  <div><span className="font-bold">Context:</span> {info.contextWindow.toLocaleString()} tokens</div>
  <div><span className="font-bold">Max output:</span> {info.maxOutputTokens.toLocaleString()} tokens</div>
  <div><span className="font-bold">Cost tier:</span> {info.costTier === 'low' ? 'Low ($)' : info.costTier === 'mid' ? 'Mid ($$)' : 'High ($$$)'}</div>
  <div><span className="font-bold">Best for:</span> {info.bestFor}</div>
</TooltipContent>
```

No other changes to this file. Do not add imports — `Info`, `Tooltip`, `TooltipTrigger`, `TooltipContent`, `TooltipProvider` are already imported.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (zero TypeScript errors — costTier type narrows correctly in ternary)
    - `grep "Cost tier" frontend/src/components/chat/MessageInput.tsx` returns 1 match
    - `grep "info.costTier" frontend/src/components/chat/MessageInput.tsx` returns 1 match
    - `grep "Low (\$)" frontend/src/components/chat/MessageInput.tsx` returns 1 match
    - `grep "Best for" frontend/src/components/chat/MessageInput.tsx` still returns 1 match (not accidentally removed)
  </acceptance_criteria>
  <done>Tooltip renders 4 data points: Context, Max output, Cost tier, Best for; frontend builds with no TypeScript errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static file → browser render | MODEL_INFO is static data, no user input, no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-051-07-01 | Information Disclosure | costTier display in tooltip | accept | Cost tier labels ('Low ($)' etc.) are static marketing-tier labels, not actual pricing. No sensitive data exposed. Labels are stable relative indicators and do not constitute financial advice. |
</threat_model>

<verification>
After both tasks complete:

```bash
# Check costTier field present in interface and all entries
grep -c "costTier" "C:/Vibe Apps/Agentic RAG/frontend/src/lib/model-info.ts"
# Expected: 12 or more

# Check tooltip row present
grep "Cost tier" "C:/Vibe Apps/Agentic RAG/frontend/src/components/chat/MessageInput.tsx"
# Expected: 1 match

# Full frontend build — must exit 0
cd "C:/Vibe Apps/Agentic RAG/frontend" && npm run build
```
</verification>

<success_criteria>
- `npm run build` in frontend exits 0
- `grep "costTier" frontend/src/lib/model-info.ts` returns at least 12 matches
- `grep "Cost tier" frontend/src/components/chat/MessageInput.tsx` returns 1 match
- `grep "info.costTier" frontend/src/components/chat/MessageInput.tsx` returns 1 match
- All 11 model IDs still present in MODEL_INFO (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/051-context-window-management/051-07-SUMMARY.md`
</output>
