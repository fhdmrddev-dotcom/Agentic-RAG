---
phase: 193.1-template-first-authoring
plan: 05
subsystem: workflow-authoring
tags: [G-5, extraction, esm-cycle-fence, copy-fence, characterization-baseline, AUTH-03]
requires:
  - "frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx (193.1-01's six captures, on the UNMOVED tree)"
  - "frontend/src/components/workflows/builderStore.ts (BuilderStore / BuilderPhase, and the setDrafted action the definition type is derived from)"
  - "frontend/src/components/workflows/library/useWorkflowFork.ts (the MODULE analog)"
  - "frontend/src/components/workflows/DoorHeaderStrip.test.tsx (the ESM-cycle fence analog)"
provides:
  - "useTemplateFirstDraft — the pre-draft describe→generate concern in its own module, off the page"
  - "its own suite (29 cases) incl. a D-24(b) ESM-cycle fence over BOTH hosts, driven RED three times"
  - "SWEPT_SOURCES widened 3 → 4, driven RED in the new file"
  - "count-gate pins for both moved knobs"
affects:
  - "Plan 07, which adds the pre-draft template read state machine into the room this cut made"
  - "Plan 08, which mounts the same hook from the loose door — the second back-edge the fence already covers"
tech-stack:
  added: []
  patterns:
    - "extraction-before-feature (192.1 D-01 wave order), baselines captured on the UNMOVED tree"
    - "page concerns arrive as callbacks; the definition shape is DERIVED from the store action, never imported from the page"
    - "a fence whose subject MOVES is re-scoped across the seam, never deleted or weakened"
key-files:
  created:
    - frontend/src/components/workflows/useTemplateFirstDraft.ts
    - frontend/src/components/workflows/useTemplateFirstDraft.test.tsx
  modified:
    - frontend/src/pages/WorkflowBuilderPage.tsx
    - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
    - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
    - scripts/vitest-count-gate.cjs
decisions:
  - "The definition shape is DERIVED from `setDrafted`'s own parameter rather than declared locally — useWorkflowFork's answer in its third form (builderStore.ts:167-180), so no second field list can drift."
  - "The two page pre-flight resets (selectedSlug, draftId) travel as ONE `onDraftStarted` callback, not two, so a future author cannot wire one and forget the other."
  - "The module spells NEITHER host name anywhere including prose, so `grep -c <host>` → 0 is a check runnable without knowing the fence's regexes."
metrics:
  duration: ~75 min
  completed: 2026-08-15
---

# Phase 193.1 Plan 05: The G-5 Extraction Summary

The pre-draft describe→generate concern now lives in `useTemplateFirstDraft.ts`, cut off
`WorkflowBuilderPage.tsx` before the feature it makes room for — and `193.1-01`'s six pre-move
captures held green with **zero re-capture**.

## Commits

| SHA | What |
|---|---|
| `8a5e0f80` | `refactor(193.1-05)` — the cut: the module, the page's hook call, the re-scoped 187-22 fence |
| `421bb83c` | `test(193.1-05)` — the module's own suite (29 cases) + the D-24(b) ESM-cycle fence |
| `cd04146c` | `test(193.1-05)` — D-14: `SWEPT_SOURCES` 3 → 4, both count-gate knobs |
| `6c831af9` | `docs(193.1-05)` — the measured subtree delta, recorded where the growth is |

## ⚠ THE HEADLINE PROOF — verbatim, not a sentence

```
$ git diff --numstat 99a6cc14..HEAD -- frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx
(empty)

$ git diff --numstat 99a6cc14..HEAD -- frontend/src/pages/WorkflowBuilderPage.header.test.tsx \
                                       frontend/src/pages/WorkflowBuilderPage.describe.test.tsx
(empty)
```

The six whole-`container.innerHTML` captures, `FLAG_OFF_HEADER_MARKUP` (`header.test.tsx:382`) and
`FLAG_OFF_DESCRIBE_MARKUP` (`describe.test.tsx:307`) all survived the move **untouched**. Nothing
was re-captured, and nothing needed to be.

The whole change set, so the claim above is auditable rather than selective:

```
$ git diff --numstat 99a6cc14..HEAD
31	3	frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
480	0	frontend/src/components/workflows/useTemplateFirstDraft.test.tsx
279	0	frontend/src/components/workflows/useTemplateFirstDraft.ts
37	7	frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
43	71	frontend/src/pages/WorkflowBuilderPage.tsx
36	2	scripts/vitest-count-gate.cjs
```

## Gates

| Gate | Result |
|---|---|
| `npx tsc --noEmit -p tsconfig.app.json` | **33** — unmoved, measured at base and after every task |
| `node scripts/vitest-count-gate.cjs` (cap 2) | **total 3650 · failed 0 · 70/70 pinned · pinned total 3626** — `count gate OK`, run **3×**, identical every time |
| the six page suites (cap 2) | **238 passed / 238**, zero edits to any of them |
| `useTemplateFirstDraft.test.tsx` (cap 2) | **29 passed / 29** |
| `WorkflowDoorSwitch.test.tsx` (cap 2) | **35 passed / 35** |
| `eslint` on both new files | **0 problems** |

**Per-file deltas, read from the gate's OWN `actual` column** (never hand-counted):

| File | pinned | actual | delta |
|---|---|---|---|
| `WorkflowDoorSwitch.test.tsx` | 34 → **35** | 35 | +1 (the fourth swept source's `it.each` row) |
| `useTemplateFirstDraft.test.tsx` | — → **29** | 29 | new |
| `WorkflowBuilderPage.canvas.test.tsx` | 128 | 133 | **+5, PRE-EXISTING** — see contradiction 7 |

No per-file count decreased. `BASELINE_TOTAL`'s annotation moved `3596 → 3626` in the same commit
as the pins, and the gate's printed `pinned total` agrees exactly.

## The two fences, each OBSERVED RED against a real plant

### 1 — D-24(b), the ESM-cycle fence (three plants, three REDs)

Pre-plant blob md5: `48ace8a1adfe44d5b97f9d2bb2188086`.

**Plant A — the bare specifier** (`import type { BuilderDefinition } from "@/pages/WorkflowBuilderPage"`):

```
FAIL  useTemplateFirstDraft D-24(b) … > THE REAL SOURCE names NEITHER host in ANY import form
AssertionError: expected '/**\n * Phase 193.1-05 Task 1 (AUTH-0…' not to match
  /from\s+["'][^"']*WorkflowBuilderPage(\.[jt]sx?)?["']/

FAIL  … > …and names neither host AT ALL — the raw `grep -c` property the header records
AssertionError: expected '/**\n * Phase 193.1-05 Task 1 (AUTH-0…' not to contain 'WorkflowBuilderPage'

Tests  2 failed | 27 passed (29)
```

**Plant B — the `.tsx`-suffixed spelling** (`… from "@/pages/WorkflowBuilderPage.tsx"`) — the same
two cases red, proving the optional `(\.[jt]sx?)?` group takes its branch:

```
AssertionError: expected '…' not to match /from\s+["'][^"']*WorkflowBuilderPage(\.[jt]sx?)?["']/
Tests  2 failed | 27 passed (29)
```

**Plant C — the SECOND host, in the dynamic-`import()` shape** (`await import("./WorkflowDoorSwitch.tsx")`).
Driven deliberately, because the door back-edge is the one **nobody had ever seen fire** — Plan 08
creates it:

```
AssertionError: expected '…' not to match
  /import\s*\(\s*["'][^"']*WorkflowDoorSwitch(\.[jt]sx?)?["']\s*\)/
AssertionError: expected '…' not to contain 'WorkflowDoorSwitch'
Tests  2 failed | 27 passed (29)
```

Restored: on-disk md5 back to `48ace8a1adfe44d5b97f9d2bb2188086`, `git diff --numstat` empty.

### 2 — D-14, the copy fence, driven RED **in the new file**

The literal was **read off `doorVocabulary` at plant time**, never re-typed. The failure has exactly
the shape 193's WR-01 fix recorded:

```
×  ./useTemplateFirstDraft.ts carries none of the 21 governed words, in either spelling

FAIL  D-24(a) — no governed door word survives as a literal in any swept module
AssertionError: expected [ 'DESCRIBE_CTA/plain' ] to deeply equal []
- []
+ [ "DESCRIBE_CTA/plain" ]

Tests  1 failed | 34 passed (35)
```

**The 1000-character floor was MEASURED before the entry was added, not assumed:**
`wc -c useTemplateFirstDraft.ts` → **14062** at the time of the check (16566 after the delta
docblock). Far above the floor; no reason to touch it.

**Governed-id count stays 21** — this plan adds no governed string. (D-21's 22nd is Plan 06's.)

**No `TARGETS` edit was needed, and that was CHECKED rather than assumed:** `src/components/workflows`
is a directory entry at `vitest-count-gate.cjs:1869`, so the gate printed
`useTemplateFirstDraft.test.tsx — 29 new` before the `BASELINE` line was written.

## ⚠ THE SUBTREE GREW — stated rather than smoothed

Classifier (blank / comment / code, every line exactly one of the three, JSX comment blocks counted
as comment) **RE-VALIDATED before any figure was trusted**:
`git show 95a4c915:frontend/src/components/workflows/PhaseNodeCard.tsx | node classify.cjs` →
**`797 / 518 / 249 / 30`**, 188.2's published known-good, reproduced exactly.

| | total | comment | code | blank |
|---|---|---|---|---|
| **PAGE before** (`99a6cc14`) | 2073 | 1146 | 835 | 92 |
| **PAGE after** | **2045** (−1.4 %) | 1144 | **809** (−3.1 %) | 92 |
| **new module** | 279 | 184 | 85 | 10 |
| **SUBTREE before** | 2073 | 1146 | 835 | 92 |
| **SUBTREE after** | **2324 (+12.1 %)** | **1328 (+15.9 %)** | **894 (+7.1 %)** | 102 |

**Yes — the subtree grew, by +251 lines.** The two per-file deltas sum to exactly +251 (page −28,
module +279): no unattributed residual.

**COMMENT is the dominant term for the fifth cut running: +182 L, 73 % of the growth**, against
CODE +59 (23.5 %).

⚠ **This is the SMALLEST growth of the five cuts this project has done, and the reason is structural
rather than virtuous.** 188.2 measured +67.1 %, Phase 192 +126.2 %, 192.1 +52.7 %, Phase 193
+124.2 %. Two of those REWROTE while they moved, and every one landed in **five or more** destination
modules, each minting its own header, imports and props type. This is one concern into **one**
module, so exactly one header's worth of prose was written.

⚠ **The page fell by 28 lines while 85 code lines landed in the new module**, which is stated rather
than smoothed: the cut left receipt prose at each site it emptied (this project's recorded habit —
`useWorkflowFork.ts:92-95`). Pretending the delta is the span is how the next estimate goes wrong.

Re-derive: `wc -l` on the page and the module; `git show 99a6cc14:<page> | wc -l` → 2073.

## What moved, and what deliberately did not

**MOVED** (all five items the plan named, with their comments): `describe` / `setDescribe`;
`projectFolderId` / `setProjectFolderId` and its seed initializer; `canDraft`; `onDraft` entire —
the `generateWorkflow` call, the Phase 103-ux `project_folder_id` comment, the SINGLE STATE
TRANSITION block, the stamp, both failure arms; and the 124 CR-01 one-shot auto-draft effect with
`autoDraftFiredRef`.

**STAYED, each for a measured reason:** `draftId` (read at four sites in the drafted view — only its
RESET is this concern's); `showReceipt` / `receiptPhases` (a DRAFTED-view surface this concern merely
triggers); `selectedSlug` (the page's one selection contract, shared by two graph views).
`boundFolderId` consumes the hook's returned `projectFolderId` unchanged.

`grep -c "generateWorkflow(" WorkflowBuilderPage.tsx` → **0**.
`grep -c "WorkflowBuilderPage" useTemplateFirstDraft.ts` → **0**. `WorkflowDoorSwitch` → **0**.

## ⚠ Measurements that CONTRADICT the plan, CONTEXT, PATTERNS or an inherited claim

Stated beside the claimed value, never over it.

**1. The worktree forked from the WRONG base — the 15th occurrence, and the 4th on this phase.**
`git rev-parse HEAD` on arrival returned **`fda792141b0129de7b15dd40ddc1082e76f95a2a`** — the
`develop`→`master` merge tip, exactly as `193.1-01` reported. `git merge-base --is-ancestor
99a6cc14 HEAD` → **WRONG BASE**. Recovered with
`git reset --hard 99a6cc144ba04a4ccbade671f0a5c07e40ee34d5`, re-asserted **BASE OK**, and every
number here is measured after that reset. Had it gone unnoticed, this plan's entire product — a
proof that a move changed nothing — would have been measured against a tree carrying merge content
the baselines never saw, i.e. worth nothing.

**2. ⚠ A FILE OUTSIDE `files_modified` HAD TO BE EDITED, and it is the most important thing in this
summary after the headline proof.** `WorkflowBuilderPage.canvas.test.tsx` is not in the plan's
`files_modified`, and the cut **failed it**:

```
FAIL  WorkflowBuilderPage 187-22 — CR-04 > SOURCE FENCE — the card is handed the arrival
      snapshot, and the snapshot is taken beside setDrafted
AssertionError: expected -1 to be greater than -1
  at src/pages/WorkflowBuilderPage.canvas.test.tsx:2746
```

The `-1` is `builderSource.indexOf("setDrafted(def)")`: 187-22's fence asserts the receipt snapshot
is taken **beside** the single state transition rather than in an effect that could observe an
already-edited store — and the transition had just moved into the hook. **This is a fence whose
SUBJECT moved, which is precisely the situation 188.2's "re-scope every source fence to the subtree
BEFORE one line moves" rule exists for, and neither the plan nor PATTERNS named this fence.**

It was **re-scoped, not weakened** (Rule 3, blocking): the property is now asserted across the seam —
in the hook, the raise (`onDraftedRef.current(def)`) sits within 500 chars of `setDrafted(def)` **with
a positive control proving the window can miss**; on the page, `setReceiptPhases(def.phases)` occurs
exactly once and inside the `onDrafted:` callback that raise reaches. Deleting the adjacency half
would have been the easy fix and would have retired the only guard against the snapshot drifting.
**The edit adds no test case** — `git diff | grep -c "^+.*it("` → 0 — so no re-pin was owed.

**3. The plan's `grep -c "WorkflowBuilderPage" … == 0` criterion conflicts with writing an honest
docblock, and the criterion won.** The first draft of the module explained the D-24(b) hazard by
naming the page three times in prose, which is not an import and which no fence regex would catch —
but it made the plan's crudest check read `3`. The docblock was reworded to name both hosts by ROLE
(pointing at `library/useWorkflowFork.ts:112-127` and `builderStore.ts:167-180` for the precedents),
and the suite now **asserts that property directly** (`not.toContain(PAGE_HOST)` with its own
positive control) so the docblock's claim is guarded rather than merely stated. ⚠ The fence file
itself therefore assembles both host names **from parts**, or a subtree-wide grep would read the
guard as a violation.

**4. The plan's args list cannot express two page resets that happen BEFORE the await.** The plan
specifies `store`, `builderPhase`, `initialDescribe`, `initialProjectFolderId`, `autoDraft`,
`onDrafted(def)` — and separately says *"`setDraftId(null)` inside `onDraft` moves with the callback
body"*. Those two statements are not jointly satisfiable: the moved body also calls
`setSelectedSlug(null)`, and neither setter is in the arg list. **One extra member was added —
`onDraftStarted: () => void`** — carrying both resets as ONE call, deliberately not two, so a future
author cannot wire one and forget the other. Ordering is preserved exactly (`setComposing()` → the
reset → the await).

**5. The `projectFolderId` initializer reads a value the arg list does not carry.** Its shipped
expression reads `initial?.definition.project_folder_id` — i.e. the page's `BuilderInitial`, whose
type lives on the page and is therefore the D-24(b) hazard in another shape. **A second member,
`initialDefinitionFolderId?: string | null`, was added** and the page passes the value (not the
prop), so the `typeof … === "string"` narrowing travels verbatim and no page type is imported. Its
precedence over `initialProjectFolderId` is now pinned by four cases in the new suite — behaviour
that was previously asserted nowhere.

**6. The md5-identical restoration claim needs one qualification, and it is a Windows fact rather
than a content one.** After the copy-fence plant, `git checkout --` restored the file and the
**on-disk** md5 read `556b359c437b8793b99b020b32948f68`, not the pre-plant `48ace8a1…`. That is
`core.autocrlf` re-writing LF → CRLF on checkout, not a content change: `git status` reports the file
unmodified, `git diff --numstat` is empty, and `git show HEAD:<path> | md5sum` → **`48ace8a1adfe44d5b97f9d2bb2188086`**,
identical to the pre-plant working copy. The three suites were re-run against the CRLF-on-disk copy
and stayed green (197/197). ⚠ **A future plan that claims "restored md5-identical" after a
`git checkout` on this box should compare the BLOB, not the working file**, or it will report a
mismatch that is not one.

**7. `WorkflowBuilderPage.canvas.test.tsx` is pinned at 128 while it actually runs 133 — and that
drift PREDATES this plan.** The gate reports `+5` and has done since before the cut; my edit adds no
case (measured, contradiction 2). Five of its cases are therefore unguarded by the pin. **Left alone
deliberately** under the scope boundary — it is not this plan's file and not this plan's drift — but
recorded here rather than left for someone to rediscover, because an unpinned case is not a lightly
guarded one, it is an unguarded one.

**8. Two inherited pointer corrections, both minor, both stated rather than silently used.**
The plan's `<interfaces>` places the governed-id count at `WorkflowDoorSwitch.test.tsx:527`;
measured at this plan's base it is **`:528`** (PATTERNS §6 says `:527-528` and is the accurate one).
Every other pointer the plan supplied survived verbatim at `99a6cc14`: `SWEPT_SOURCES` at `:488`,
`toHaveLength(3)` at `:522`, the 1000-char floor at `:524`, `BuilderDefinition` at
`WorkflowBuilderPage.tsx:478`, `describe` at `:609`, `projectFolderId` at `:634`, `canDraft` at
`:687`, `onDraft` at `:1256-1297`, the auto-draft effect at `:1303-1314`, `describeScreen` at
`:1533-1615`. ⚠ Note three different HEADs are quoted across this phase's documents for one phase
(`<interfaces>` says `568afc7a`, PATTERNS says `0bb189e4`, the base is `99a6cc14`) — `193.1-01`
flagged the same thing and it is still true.

**9. `GSD_VITEST_MAX_WORKERS=2` behaved DETERMINISTICALLY here, which does not refute `193.1-01`.**
The full gate ran three times on the committed tree and returned `total 3650 · failed 0` every time,
plus a fourth green run mid-plan. `193.1-01` measured a one-off `failed 3` at 3620 cases on an
identical tree. Both are consistent with oversubscription being intermittent; **neither settles the
question**, and the standing advice — re-run before believing a red, and capture the failing
filenames first — is unchanged. This plan hit no red it had to re-run.

**10. One inherited claim CONFIRMED rather than corrected, since the phase's habit is to say so.**
PATTERNS §12 says `WorkflowDoorSwitch.test.tsx` is pinned at **34** and that a new file under
`src/components/workflows` needs only a `BASELINE` entry. Both measured true.

## Deviations from Plan

### Auto-fixed (no Rule 4 territory)

**1. [Rule 3 — blocking] Re-scoped 187-22's adjacency fence in `WorkflowBuilderPage.canvas.test.tsx`.**
- **Found during:** Task 1 verification. **Full detail in contradiction 2 above.**
- **Fix:** the property is asserted across the seam, with a positive control on the 500-char window;
  the fence is stronger than it was, and no case count changed.
- **Files modified:** one outside `files_modified`. **Commit:** `8a5e0f80`.

**2. [Rule 2 — missing coverage] Two args the plan's list omits (`onDraftStarted`,
`initialDefinitionFolderId`).** Contradictions 4 and 5. Without them the move is not behaviour-
preserving: the pre-flight resets and the OPEN/TWEAK folder seed would both have been dropped.

**3. [judgement] The two page callbacks are read through refs.**
- **Why:** `onDraft`'s shipped dependency list is `[describe, projectFolderId, store]`, and the
  auto-draft effect depends on `onDraft`'s identity. A page-supplied inline callback in the
  dependency list would re-create `onDraft` every render and re-run that effect — a one-shot turned
  into a per-render effect. The refs preserve the shipped identity contract **by construction**
  rather than by every future call site remembering `useCallback` — and Plan 08 adds a second call
  site, so "remembering" would have to happen twice.
- **Cost, stated:** ~10 lines of machinery that did not exist on the page, and it is counted in the
  +59 CODE figure above rather than excluded from it.

### Not done, and why

- **No `.planning/STATE.md` guardrail override was added.** Verified rather than assumed:
  `## Guardrail overrides` (`:1072`) records none for Phase 193.1, and three separate places in
  STATE.md already say the waiver was **offered and declined**. That absence is a measurement.
- **No package installed. No migration.** `T-193.1-05-SC` discharged: zero installs of any kind.
- **`WorkflowDoorSwitch.tsx` is untouched.** The hook's second mount is Plan 08's; this plan only
  fences the back-edge that mount will create.
- **The `canvas.test.tsx` 128 vs 133 pin drift was left alone** — contradiction 7.

## Known Stubs

None. This plan moves shipped code and adds two fences. It renders nothing new, sends nothing new,
and adds no user-visible string.

## Threat Flags

None. No route, no input, no new render path, no data crossing a trust boundary — the threat model's
own `none introduced` row holds.

- `T-193.1-05-01` (tampering, the move) — mitigated: the six captures and both byte-exact pins are
  `git diff --numstat` EMPTY, pasted verbatim above.
- `T-193.1-05-02` (an ESM cycle) — mitigated: the fence covers BOTH hosts × four import forms × two
  suffix spellings, observed RED three times against real plants.
- `T-193.1-05-03` (a governed word re-typed into the new module) — mitigated: `SWEPT_SOURCES` 3 → 4,
  observed RED in that file with the WR-01 message shape.
- `T-193.1-05-04` (repudiation) — mitigated: every claim above is a pasted command output.

## What the next wave inherits

- **The room the cut made.** Plan 07's read state machine goes into `useTemplateFirstDraft.ts`, whose
  analog for that half is **`frontend/src/hooks/useTemplatePlaceholders.ts:79-86, :149-153`** (D-25) —
  *the answer is stored with the subject it answered for, and `loading` is derived when they differ* —
  **not** `useWorkflowFork.ts`, which has no fetch state machine at all.
- **A fence that already covers the door.** Plan 08's second mount creates the back-edge to
  `WorkflowDoorSwitch`; that edge has been fenced and driven RED **before** it exists.
- **Two `/generate` key-set literals to move.** They now live in TWO places — the page-level pin in
  `193.1-01`'s baseline suite, and the module-level pin added here. The wire wave must move **both**
  in the commit that adds `template_placeholders`, and the module-level one is the more precise of
  the two.
- **`CLAUDE.md`'s ledger row for `WorkflowBuilderPage.tsx` is now stale in both figures** — it reads
  33 commits / 2055 L; measured at this plan's close (`git log --oneline -- <file> | wc -l`;
  `wc -l`) it is **35 commits / 2045 L** across the same 10 phases plus 193.1. Only ONE of this
  plan's four commits touched the page, which is why 34 → 35 rather than 34 → 38. The phase owes
  that row a correction at close, exactly as 193 owed `WorkflowDoorSwitch.tsx` one. ⚠ **`useTemplateFirstDraft.ts`
  is a brand-new file and is ABSENT from the ledger** — the standing lesson is that a guardrail
  cannot see what is not on its list, so it should be added at close with a measured
  **2 commits / 1 phase / 279 L** rather than re-derived in 197.

## Self-Check: PASSED

```
FOUND: frontend/src/components/workflows/useTemplateFirstDraft.ts
FOUND: frontend/src/components/workflows/useTemplateFirstDraft.test.tsx
FOUND: frontend/src/pages/WorkflowBuilderPage.tsx (modified)
FOUND: frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx (modified)
FOUND: frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx (modified)
FOUND: scripts/vitest-count-gate.cjs (modified)
FOUND: 8a5e0f80  refactor(193.1-05): cut the pre-draft describe→generate concern out under G-5
FOUND: 421bb83c  test(193.1-05): the moved concern's own suite, with the ESM-cycle fence driven RED
FOUND: cd04146c  test(193.1-05): D-14 — widen the copy fence to the new consumer, and pin both knobs
FOUND: 6c831af9  docs(193.1-05): record the measured subtree delta where the growth is
```
