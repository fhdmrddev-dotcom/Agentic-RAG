---
phase: 193.1-template-first-authoring
plan: 05
type: execute
wave: 2
depends_on: ["193.1-01"]
files_modified:
  - frontend/src/components/workflows/useTemplateFirstDraft.ts
  - frontend/src/components/workflows/useTemplateFirstDraft.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-01: `WorkflowBuilderPage.tsx` loses the pre-draft draft concern to its own module, and the cut ships BEFORE the feature it makes room for"
    - "D-02: Plan 01's six captures stay green with ZERO re-capture — `git diff --numstat` on the baseline suite is EMPTY across this plan"
    - "D-25: the module shape follows `useWorkflowFork.ts`; every page concern arrives as a callback and no page-declared type is imported"
    - "D-24(b): the extracted module imports neither `WorkflowBuilderPage` nor `WorkflowDoorSwitch`, in any spelling, and the fence has been observed RED"
    - "D-14: the new module is a new consumer of the D-24(a) copy fence, added to `SWEPT_SOURCES` in this same commit and driven RED against a real plant in that file"
    - "No guardrail override is recorded for Phase 193.1 — a waiver was offered and declined (D-01)"
  artifacts:
    - path: "frontend/src/components/workflows/useTemplateFirstDraft.ts"
      provides: "the pre-draft describe→generate concern, moved verbatim out of the page"
      min_lines: 90
    - path: "frontend/src/components/workflows/useTemplateFirstDraft.test.tsx"
      provides: "the module's own suite incl. the ESM-cycle fence with positive and negative controls"
    - path: "frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx"
      provides: "SWEPT_SOURCES widened 3 → 4, count bumped, plant driven RED"
      contains: "useTemplateFirstDraft"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/components/workflows/useTemplateFirstDraft.ts"
      via: "a value import at module scope; the hook returns describe/projectFolderId/canDraft/onDraft"
      pattern: "useTemplateFirstDraft"
---

<objective>
Honour G-5 on `frontend/src/pages/WorkflowBuilderPage.tsx` by CUTTING the pre-draft
describe→generate concern out into its own module — **first, in an early wave, before the feature it
makes room for** (D-01, the 192.1 D-01 order).

Purpose: the page gains a pre-draft template read state machine in Plan 07 — a **sixth** concern
beside the Builder shell, the canvas mount, the publish gauntlet, the asset-descriptor resolution and
the save/concurrency machinery. Measured at plan time: **34 commits · 10 phases · 2073 L**
(`CLAUDE.md`'s cell said 33 / 2055 and was stale by one commit). A G-5 waiver was **offered and
declined**, so `.planning/STATE.md` must record **no guardrail override for Phase 193.1** — and that
absence is a measurement, not an omission.

⚠ **THIS IS A MOVE, NOT A FEATURE.** No behaviour changes. No template code lands here. Plan 01's
six captures are the proof, and they must hold with **ZERO re-capture**.

Output: `useTemplateFirstDraft.ts`, its own suite with two fences, and a widened copy fence.
</objective>

<worktree_protocol>
1. FIRST action: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
2. THEN assert `git rev-parse HEAD` equals the dispatched base SHA. A mismatch is a STOP.
3. `GSD_VITEST_MAX_WORKERS=2` on EVERY vitest invocation — `CLAUDE.md`'s table still says 4 and that
   value has ROTTED. Keep concurrent test-running agents to ≤ 2.
4. Typecheck is `cd frontend && npx tsc --noEmit -p tsconfig.app.json`, baseline **33**. ⚠ `--noEmit`
   WITHOUT `-p tsconfig.app.json` checks ZERO files.
</worktree_protocol>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md
@.planning/phases/193.1-template-first-authoring/193.1-PATTERNS.md
@.planning/phases/193.1-template-first-authoring/193.1-RESEARCH.md
@.planning/phases/193.1-template-first-authoring/193.1-01-SUMMARY.md
</context>

<interfaces>
<!-- Extracted at HEAD 568afc7a. Use these directly — no exploration needed. -->

**WHAT MOVES (the pre-draft describe→generate concern), all verified at HEAD:**
- `const [describe, setDescribe] = useState(initialDescribe ?? "")` — `WorkflowBuilderPage.tsx:609`
- `const [projectFolderId, setProjectFolderId] = useState<string>(…)` — `:634` (its initializer reads
  the `initialProjectFolderId` prop)
- `const canDraft = describe.trim().length > 0 && builderPhase !== "composing"` — `:687`
- `const onDraft = useCallback(async () => {…}, [describe, projectFolderId, store])` — `:1256-1297`,
  including the `generateWorkflow` call at `:1267-1272`, the `setDrafted` transition, and the
  `project_folder_id` stamp at `:1279`
- the one-shot auto-draft effect — `:1303-1314` (`autoDraftFiredRef`, guarded to `builderPhase === "empty"`)

**WHAT STAYS ON THE PAGE, and why:**
- `draftId` / `setDraftId` (`:647`) — read at `:994`, `:1408`, `:1982`, `:2057`. The hook must not own it.
- `showReceipt` / `receiptPhases` (`:654`, `:658`) — rendered at `:1743-1746`, a DRAFTED-view concern.
  `onDraft` sets them today; after the move the hook calls an `onDrafted(def)` callback the page
  supplies, which sets both. Page concerns arrive as callbacks — the `useWorkflowFork.ts:146-165`
  contract.
- `boundFolderId` (`:1244`) reads `projectFolderId`; it consumes the hook's returned value.

**Analog A — the MODULE contract: `frontend/src/components/workflows/library/useWorkflowFork.ts` (517 L).**
- `:146-165` a NAMED args interface where every PAGE concern arrives as a callback and none is
  reached for; `:167-188` a NAMED return interface with one JSDoc line per member; `:190` the
  `useWorkflowFork(args): WorkflowFork` signature.
- `:112-127` is the D-24(b) answer already solved once in this repo: `ForkSeed` is DECLARED locally
  rather than imported, because the page's `BuilderInitial` lives in `@/pages/WorkflowBuilderPage`
  which no fence in that subtree catches.
- ⚠ **`BuilderDefinition` is declared ON THE PAGE at `WorkflowBuilderPage.tsx:478` (VERIFIED).** The
  hazard is live. **Declare the definition-shaped values locally or take them as a type parameter;
  never `import type … from "@/pages/WorkflowBuilderPage"`.** `builderStore.ts:167-180` shows the
  third option (a key-remapped mapped type) if a structural tie is genuinely wanted.

**Analog B — the ESM-cycle fence: `frontend/src/components/workflows/DoorHeaderStrip.test.tsx`.**
- `:250-256` records WHY both suffix spellings are required: `frontend/tsconfig.app.json:13-14` sets
  `moduleResolution: "bundler"` WITH `allowImportingTsExtensions: true` (VERIFIED), so
  `from "./X.tsx"` compiles and would build a real TDZ cycle.
- `:256-257` the two matchers (a `from "…"` form and a dynamic `import("…")` form, each with an
  optional `(\.[jt]sx?)?` suffix group).
- `:262-278` POSITIVE CONTROLS FIRST, one per import form — bare `import`, `import type`,
  `export … from`, dynamic `import()` — and each again with the suffix.
- `:279-285` NEGATIVE controls: `?raw` and `.tsx?raw` must keep MISSING under both branches.
- `:288` non-vacuity: `expect(source.length).toBeGreaterThan(500)`.
- ⚠ `:305-310` the `/g` trap: mixing `toMatch` (which advances `lastIndex`) with `String.match()`
  makes the count order-dependent. `/g` belongs ONLY on the matcher used for counting.
- ⚠ The `specifiersOf(...)` EXACT-EQUALITY form at `:311-347` does **not** transfer verbatim — this
  hook legitimately imports `react` and `@/lib/api`. Use the NEGATIVE-regex form plus the
  non-vacuity floor.

**The D-24(a) copy fence to widen — `frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx`
(all four line numbers VERIFIED at HEAD):**
- `:488-492` `SWEPT_SOURCES`, THREE entries today (`./WorkflowDoorSwitch.tsx`,
  `./DoorHeaderStrip.tsx`, `@/pages/WorkflowBuilderPage.tsx` — the third added late by 193's WR-01).
- `:522` `expect(SWEPT_SOURCES).toHaveLength(3)` — a hardcoded count; bump it deliberately.
- `:524` `expect(source.length, …).toBeGreaterThan(1000)` — ⚠ **a new module under 1000 CHARACTERS
  reds here.** Measure with `wc -c` before assuming; lower the floor only with a stated reason,
  never silently.
- `:527` `expect(new Set(NEEDLES.map(n => n.id)).size).toBe(21)` — the governed-id count. **This plan
  adds NO governed id, so it stays 21.** (Plan 06 moves it to 22 under D-21.)
- `:539-548` the positive-control idiom (`hitsIn`), and `:536` the test-file exclusion.
- ⚠ **THE SWEEP IS RAW, so PROSE COUNTS.** A docblock in the new module quoting *"Write the first
  draft"* reds the fence — that is the fence working. **Name identifiers, never words.**
- ⚠ `NEEDLES` is built by `Object.entries(doorVocabulary).flatMap(…)` and does
  `(value as string).replace(/&/g, "&amp;")`. **A non-string export in `doorVocabulary.ts` would
  throw inside the needle builder.** Do not add one; Plan 06 records the same constraint.

**Count-gate knobs:** `src/components/workflows` is already a `TARGETS` **directory** entry, so a new
suite there RUNS the moment it exists and needs only a `BASELINE` pin. `WorkflowDoorSwitch.test.tsx`
is pinned at **34** and must be re-pinned in the same commit that adds cases to it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cut `useTemplateFirstDraft.ts` out — a verbatim move, no behaviour change</name>
  <files>frontend/src/components/workflows/useTemplateFirstDraft.ts, frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - frontend/src/components/workflows/library/useWorkflowFork.ts lines 100-210 (the module contract, the locally-declared type, the args/return interfaces)
    - frontend/src/pages/WorkflowBuilderPage.tsx lines 595-700 (props + the state that moves) and 1250-1320 (`onDraft` + the auto-draft effect) and 1533-1614 (the consumers)
    - .planning/phases/193.1-template-first-authoring/193.1-PATTERNS.md § C-2 and § 3 (two analogs, one per half — this task is the MODULE half only)
  </read_first>
  <action>
    Create `frontend/src/components/workflows/useTemplateFirstDraft.ts` exporting one hook,
    `useTemplateFirstDraft`, with a NAMED args interface and a NAMED return interface in
    `useWorkflowFork.ts`'s shape.

    Args (every page concern arrives as a callback — reach for nothing):
    `store`, `builderPhase`, `initialDescribe`, `initialProjectFolderId`, `autoDraft`, and
    `onDrafted(def)`. Returns: `describe`, `setDescribe`, `projectFolderId`, `setProjectFolderId`,
    `canDraft`, `onDraft`.

    Move the five items named in `<interfaces>` VERBATIM. Preserve every comment that travels with
    them — the Phase 103-ux `project_folder_id` comment at `:1269-1270`, the 186-07 reset comment at
    `:1261-1264`, the SINGLE-STATE-TRANSITION comment, the 187-15 and 187-22 receipt comments, and
    the Phase 124 CR-01 auto-draft comment. A comment left behind on the page is a comment that now
    describes code that is not there.

    ⚠ Declare the definition shape LOCALLY or accept it as a type parameter. Do NOT
    `import type { BuilderDefinition } from "@/pages/WorkflowBuilderPage"` — that is the live
    D-24(b) hazard `useWorkflowFork.ts:112-127` already solved once. Copy that docblock's REASONING
    into the new declaration so the next reader does not "tidy" it back into an import.

    Replace the moved code on the page with a single hook call and destructuring. `showReceipt` /
    `receiptPhases` stay on the page and are set inside the `onDrafted` callback the page passes in.
    `setDraftId(null)` inside `onDraft` moves with the callback body; `draftId` itself stays.

    ⚠ **Name no governed door word in any prose in the new module.** The D-24(a) sweep is RAW and
    Task 3 adds this file to it. Refer to the CTA by its identifier, never by its words.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/workflows/useTemplateFirstDraft.ts` exists, exports exactly one hook plus its two interfaces, and `wc -c` on it is > 1000 (record the number).
    - `grep -c "WorkflowBuilderPage" frontend/src/components/workflows/useTemplateFirstDraft.ts` == 0.
    - `grep -c "useState\|useCallback" frontend/src/pages/WorkflowBuilderPage.tsx` decreases; `grep -c "generateWorkflow(" frontend/src/pages/WorkflowBuilderPage.tsx` == 0.
    - `wc -l frontend/src/pages/WorkflowBuilderPage.tsx` is strictly less than 2073 (record the number).
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports **33** errors — unmoved.
    - **`git diff --numstat frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx` is EMPTY.** The six captures hold with ZERO re-capture. This is the plan's headline proof.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx src/pages/WorkflowBuilderPage.test.tsx` exits 0 with 0 failing and **no edit to any of those files**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx</automated>
  </verify>
  <done>The concern lives in its own module; the page is smaller; six captures and two byte-exact pins are green with zero edits.</done>
</task>

<task type="auto">
  <name>Task 2: The module's own suite, with the ESM-cycle fence driven RED</name>
  <files>frontend/src/components/workflows/useTemplateFirstDraft.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/DoorHeaderStrip.test.tsx lines 240-350 (the fence: both spellings, positive controls first, negative `?raw` controls, the non-vacuity floor, the `/g` trap)
    - frontend/src/components/workflows/library/useWorkflowFork.ts (what a hook of this shape is tested through)
  </read_first>
  <behavior>
    - `canDraft` is false with empty text, true with text, and false while `builderPhase` is
      `"composing"` — the three arms it has today, asserted through the hook, not through the page.
    - `onDraft` calls `generateWorkflow` with a body whose sorted key set is exactly `["describe"]`
      with no folder picked, and exactly `["describe","project_folder_id"]` with one picked. (The
      same assertion Plan 01 pinned at the page level; here it guards the moved code directly.)
    - `onDraft` on `{ok:false}` sets the store's error state and calls `onDrafted` ZERO times.
    - `onDraft` on `{ok:true}` calls `onDrafted` exactly once with the returned definition, and stamps
      `project_folder_id` onto it only when the generator did not already bind one.
    - The auto-draft one-shot fires exactly once when `autoDraft` is true, `initialDescribe` is
      non-empty and `builderPhase === "empty"` — and never a second time on re-render.
    - ESM-cycle fence: the module source names NEITHER `WorkflowBuilderPage` NOR `WorkflowDoorSwitch`
      in any of the four import forms, in either suffix spelling.
  </behavior>
  <action>
    Write the suite. Test the hook directly with a render-hook helper; do not mount the page.

    For the fence, copy `DoorHeaderStrip.test.tsx`'s structure and widen its SUBJECT to two modules
    (`WorkflowBuilderPage` and `WorkflowDoorSwitch`) rather than one — the cut creates a hook the
    page imports at module scope AND a hook Plan 08 will import from `WorkflowDoorSwitch.tsx`, so
    both back-edges are live.

    Order is not optional: **non-vacuity first** (`source.length > 500`, the `?raw` import really
    loaded), **then the positive controls** (one per import form × both spellings, each proved to be
    CAUGHT), **then the negative `?raw` controls** (proved to stay MISSING), **then** the assertion
    over the real source. A matcher that cannot match passes vacuously — the 192.1 E-2 lesson.

    Keep `/g` only on the matcher used for counting; the `/g` trap is recorded at
    `DoorHeaderStrip.test.tsx:305-310`.

    Then DRIVE IT RED for real: plant an actual back-import of `@/pages/WorkflowBuilderPage` in
    `useTemplateFirstDraft.ts`, observe the fence fail, record the failure message verbatim in the
    SUMMARY, remove the plant and confirm the file is restored **md5-identical**. Repeat for the
    `.tsx`-suffixed spelling. A fence nobody has seen fire is a fence nobody knows is connected.
  </action>
  <acceptance_criteria>
    - The suite covers all six behaviours above.
    - The fence has positive controls for four import forms × two spellings and negative `?raw` controls, all before the real-source assertion.
    - The SUMMARY records TWO observed RED messages (bare and `.tsx`-suffixed) and the md5 of the restored file matching its pre-plant md5.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/useTemplateFirstDraft.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/useTemplateFirstDraft.test.tsx</automated>
  </verify>
  <done>The moved concern is tested in its own module and provably cannot import its hosts back.</done>
</task>

<task type="auto">
  <name>Task 3: D-14 — widen the copy fence to the new consumer, and drive it RED in that file</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx lines 460-560 (the fence's docblock, `SWEPT_SOURCES`, the scope case, the positive control, the sweep case)
    - .planning/phases/193-authoring-doors-template-placement/193-REVIEW.md WR-01 (the fence swept two files while a third consumer existed)
  </read_first>
  <action>
    Add a fourth `SWEPT_SOURCES` entry for `./useTemplateFirstDraft.ts` with its `?raw` import beside
    the existing ones (`:21-29`), and bump `expect(SWEPT_SOURCES).toHaveLength(3)` at `:522` to **4**.
    Leave the governed-id count at **21** — this plan adds no governed string.

    Before trusting the widened fence, check the `>1000` character floor at `:524` against the
    measured `wc -c` from Task 1 and record it. If the new module is under the floor, do NOT lower
    it silently: report the number and the reason and stop.

    Then DRIVE THE FENCE RED IN THE NEW FILE: plant one real governed door literal (take its value
    from the `doorVocabulary` module at test time — do not re-type the words) into
    `useTemplateFirstDraft.ts`, run the sweep, record the failure verbatim (the shape 193's WR-01 fix
    recorded was `expected [ 'DESCRIBE_CTA/plain' ] to deeply equal []`), remove the plant and
    confirm md5-identical restoration.

    Write the reason for the entry beside it, in the fence's own voice: any file that imports from
    `doorVocabulary` belongs here — **and so does any new module on this surface**, because WR-01
    proved a fence cannot see a file that is not in its list. Note explicitly that
    `useTemplateFirstDraft.ts` does not import `doorVocabulary` today and is swept anyway, because
    the sweep is RAW and its docblocks discuss the CTA.

    Re-pin `WorkflowDoorSwitch.test.tsx` in `scripts/vitest-count-gate.cjs` (34 → the new measured
    value) and add a `BASELINE` entry for `useTemplateFirstDraft.test.tsx`. Take both numbers from
    THIS SCRIPT'S OWN `actual` column. ⚠ No `TARGETS` edit is needed for the new suite —
    `src/components/workflows` is already a directory entry; state that this was CHECKED, not assumed.
  </action>
  <acceptance_criteria>
    - `grep -c "useTemplateFirstDraft" frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx` >= 2 (the `?raw` import and the `SWEPT_SOURCES` entry).
    - The scope case asserts `toHaveLength(4)` and the governed-id case still asserts `21`.
    - The SUMMARY records the RED sweep message, the restored md5, and the `wc -c` measurement against the 1000-char floor.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` exits 0, `failed 0`, and lists both `WorkflowDoorSwitch.test.tsx` (new count) and `useTemplateFirstDraft.test.tsx` as pinned.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports **33**.
  </acceptance_criteria>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The copy fence sweeps every consumer this plan created, and it has been seen to fire in that file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | A pure client-side module move. No new route, no new input, no new render path, no new data crossing a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.1-05-01 | Tampering | the move itself | mitigate | Plan 01's six whole-container captures plus the two shipped byte-exact pins (`FLAG_OFF_DESCRIBE_MARKUP`, `describe.test.tsx:307`; `FLAG_OFF_HEADER_MARKUP`, `header.test.tsx:382`) must all hold with ZERO edits — asserted as `git diff --numstat` EMPTY on the baseline suite. |
| T-193.1-05-02 | Denial of service (dev-time) | an ESM import cycle | mitigate | Task 2's fence over BOTH hosts, both import forms, both suffix spellings, driven RED twice. `allowImportingTsExtensions: true` makes the suffixed spelling a real TDZ hazard, not a theoretical one. |
| T-193.1-05-03 | Tampering | a governed door word re-typed into the new module | mitigate | Task 3's D-14 widening, driven RED in the new file. WR-01 is the precedent: the exact regression Phase 193 closed could have recurred with every gate green. |
| T-193.1-05-04 | Repudiation | claiming the pins held | mitigate | The claim is a `git diff --numstat` output pasted into the SUMMARY, not a sentence. |
| T-193.1-05-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** No `## Package Legitimacy Audit` exists in RESEARCH because no external package is added. |
</threat_model>

<verification>
- The page is measurably smaller and its five suites are green with no edits to any of them.
- `git diff --numstat` on `WorkflowBuilderPage.preDraft.baseline.test.tsx` is EMPTY.
- The count gate is green at cap 2 with both new/updated pins.
- `tsc -p tsconfig.app.json` reports 33.
- **No `.planning/STATE.md` guardrail override is added** — a waiver was offered and declined (D-01).
</verification>

<success_criteria>
`WorkflowBuilderPage.tsx` no longer owns the describe→generate concern; the concern lives in a module
with its own suite, two driven fences and a copy-fence entry; and the pre-move captures prove the
screen did not move a pixel.
</success_criteria>

<output>
Create `.planning/phases/193.1-template-first-authoring/193.1-05-SUMMARY.md` when done.
</output>
