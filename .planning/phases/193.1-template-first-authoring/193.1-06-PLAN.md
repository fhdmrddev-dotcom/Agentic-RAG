---
phase: 193.1-template-first-authoring
plan: 06
type: execute
wave: 3
depends_on: ["193.1-04", "193.1-05"]
files_modified:
  - frontend/src/components/workflows/doorVocabulary.ts
  - frontend/src/components/workflows/doorVocabulary.test.ts
  - frontend/src/components/workflows/templateFirstVocabulary.ts
  - frontend/src/components/workflows/templateFirstVocabulary.test.ts
  - frontend/src/components/workflows/DescribeTemplateRow.tsx
  - frontend/src/components/workflows/DescribeTemplateRow.test.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
  - frontend/src/lib/api.ts
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-21: the NEW control is reworded to avoid the word `template`; the shipped governed `STARTER_DOOR_LINE` is not touched"
    - "D-13: the three governed hint fragments stay byte-exact — `hint.withTemplate` is DECLINED, and the two decisions align mechanically to keep `FLAG_OFF_DESCRIBE_MARKUP` whole"
    - "D-09: 166-B's footing line exists for all five reading arms, each a distinct value, so a disabled CTA can always say why"
    - "Every count this surface states is DERIVED from the list rendered above it, never hardcoded — an `8` beside a list of 5 is the class of silent lie this phase removes"
    - "D-14: `DescribeTemplateRow.tsx` is added to the copy fence in this same commit and the fence is driven RED against a real plant in it"
    - "D-04: every string comes from a generated BUILD-CONTRACT or a shipped exported identifier; none is re-derived by eye (SEED-155)"
  artifacts:
    - path: "frontend/src/components/workflows/templateFirstVocabulary.ts"
      provides: "the 165-C / 166-B / 167-C strings and the count-deriving message functions"
    - path: "frontend/src/components/workflows/DescribeTemplateRow.tsx"
      provides: "the pre-draft attach control + spec block + five-arm footing line; presentational, owns no fetch"
      min_lines: 120
    - path: "frontend/src/lib/api.ts"
      provides: "the abortable client for POST /workflows/template/placeholders"
      contains: "readTemplatePlaceholdersFromFile"
  key_links:
    - from: "frontend/src/components/workflows/DescribeTemplateRow.tsx"
      to: "frontend/src/components/workflows/TemplateAttachSection.tsx"
      via: "imported string constants — inherited, never re-typed"
      pattern: "TEMPLATE_FIELDS_HEADING"
    - from: "frontend/src/lib/api.ts"
      to: "POST /workflows/template/placeholders"
      via: "FormData + Bearer, no Content-Type, abortable"
      pattern: "template/placeholders"
---

<objective>
Build everything the pre-draft template row needs — its words, its component and its client — with
nothing mounted yet. No user-reachable behaviour changes in this plan.

Purpose: the row must exist and be provably honest before either describe screen mounts it (Plan 08)
and before the wire that depends on it ships (Plan 07). Splitting it out keeps each plan inside its
context budget and keeps the mount plan's diff small enough to read against a byte-exact re-capture.

Output: two vocabulary modules, one presentational component, one API client function, and a widened
copy fence.
</objective>

<worktree_protocol>
1. FIRST action: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
2. THEN assert `git rev-parse HEAD` equals the dispatched base SHA. A mismatch is a STOP.
3. `GSD_VITEST_MAX_WORKERS=2` on EVERY vitest invocation; ≤ 2 concurrent test-running agents.
4. `cd frontend && npx tsc --noEmit -p tsconfig.app.json`, baseline **33**.
</worktree_protocol>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md
@.planning/phases/193.1-template-first-authoring/193.1-PATTERNS.md
@.planning/phases/193.1-template-first-authoring/193.1-UAT.md
@.planning/sketches/165-the-template-lands-first/BUILD-CONTRACT.generated.md
@.planning/sketches/166-when-there-are-no-eight-fields/BUILD-CONTRACT.generated.md
@.planning/sketches/167-the-template-arrives-late/BUILD-CONTRACT.generated.md
</context>

<interfaces>
<!-- Extracted at HEAD 568afc7a. -->

**⚠ CORRECTION TO PATTERNS.md, stated beside the claim rather than over it.** PATTERNS' file table
lists *"`TemplateAttachSection.tsx` — the 165-C `scale` prop | ⚠ genuinely NEW"*. **Measured, a
`scale` prop cannot deliver 165-C**: `TemplateAttachSection` calls
`useTemplatePlaceholders(definitionId, assetId)` internally (`TemplateAttachSection.tsx:220-235`,
hook effect key at `useTemplatePlaceholders.ts:147`) and **neither value exists before a draft** —
which is the whole reason D-05 needs a stateless read. Mounting it pre-draft is not a styling change,
it is a data-contract change on a component that shipped three weeks ago with a 43-case pinned suite.
**Ruling: `DescribeTemplateRow.tsx` is a NEW component that INHERITS the shipped string identifiers
by import and re-types none of them** — which is exactly what sketch 165's contract asks for
(`spec.heading` and `ctrl.types` are marked `SHIPPED`: *"Inherit it — do not re-type it into new
JSX"*). **`TemplateAttachSection.tsx` is NOT edited by this phase.**

**The shipped identifiers to IMPORT (all exported from `TemplateAttachSection.tsx`, verified):**
`TEMPLATE_ATTACH_LABEL` (`:73`), `TEMPLATE_TYPES_NOTE` (`:69`), `TEMPLATE_ACCEPT` (`:107`),
`TEMPLATE_FIELDS_HEADING` (`:124`), `TEMPLATE_FIELDS_LOADING` (`:128`), `TEMPLATE_FIELDS_NONE`
(`:135`), `TEMPLATE_FIELDS_UNAVAILABLE` (`:142`), `TEMPLATE_FIELDS_NOT_WORD` (`:155`).
⚠ `TEMPLATE_FIELDS_NONE` and `TEMPLATE_FIELDS_UNAVAILABLE` carry a docblock at `:130-143` saying they
**may never merge** — `none` means we opened it and found nothing; `unavailable` means we never
opened it. That constraint is INHERITED and must be restated in the new component.

**The five shipped reading arms and their testids** (166's build contract, extracted from the real
rendered DOM): `template-fields` · `template-fields-loading` · `template-fields-none` ·
`template-fields-not-word` · `template-fields-unavailable`. Sketch 166's audit #6 mechanises
*"these sentences may never merge"* as **20 pairwise checks**: no arm renders any other arm's node.
The new component's suite must carry the same pairwise property over ITS OWN testids.

**The 166-B footing strings — verbatim from `166/BUILD-CONTRACT.generated.md`, never re-typed by eye:**
- `footing.fields` — *"Your draft will be built to fill these 8 fields."* ⚠ STATES A COUNT ⇒ a
  FUNCTION taking the length of the list rendered directly above it.
- `footing.none` — *"This template has no fill-in fields, so your draft will be written from your
  description alone."* (the contract calls this THE MOST IMPORTANT STRING ON THE PAGE)
- `footing.notWord` — *"We can only read fields from Word documents, so your draft will be written
  from your description alone."*
- `footing.unavailable` — *"We could not read this template's fields, so your draft will be written
  from your description alone."* ⚠ MUST NOT MERGE WITH `footing.none`.
- `footing.loading` — *"Reading the template — the draft will be built to whatever it asks for."*

**The 165 strings:** `ctrl.note` — *"Attach the document this should fill in — the draft is built to
its fields."* · `spec.filenameLead` — *"Drafting to"* (variant C only) · `ctrl.button` is
**deliberately** the shipped `TEMPLATE_ATTACH_LABEL`, character for character.
⚠ **`spec.promise` is NOT rendered.** 165-C's own line and 166-B's `footing.fields` say the same
thing; the operator picked 166-B for the line, and CONTEXT §specifics describes the winning shape as
the spec block *"with ONE line underneath"*. Two sentences saying one thing would be a second home
for it. **This is a decision, not an omission** — record it in the component's docblock.
⚠ **`hint.withTemplate` is DECLINED (D-13)** — the three governed hint fragments stay byte-exact.

**D-21's new governed string.** The label lands in `doorVocabulary.ts` beside `DESCRIBE_CTA`
(`:227`) and must avoid the word *template*. **Value: `DESCRIBE_ATTACH_PROMPT = "Filling in a
document you already have?"`** — a QUESTION (165's own rule: the KB picker above already asks one,
and a bare noun label would read as a third field to fill). It is unshipped and unpinned, so it is
overridable cheaply; record the reasoning beside it.
⚠ **Adding it moves TWO counts** (PATTERNS' correction to RESEARCH §D-4, which said both stay 21
under D-13 alone): `WorkflowDoorSwitch.test.tsx:527` **21 → 22** and `GOVERNED_ID_COUNT` in
`doorVocabulary.test.ts:90` **21 → 22**. `doorVocabulary.test.ts` also re-parses
`165/BUILD-CONTRACT.generated.md`-class contracts at test time in its contract-agreement case — read
that case before touching the table.

**⚠ WHY THE OTHER STRINGS MAY NOT LIVE IN `doorVocabulary.ts` — a mechanical reason, not a
preference.** The D-24(a) fence builds its needles with
`Object.entries(doorVocabulary).flatMap(([id, value]) => { const plain = value as string; const
escaped = plain.replace(/&/g, "&amp;") … })` (`WorkflowDoorSwitch.test.tsx:499-509`). **A FUNCTION
export in that module would throw inside `.replace`.** 165-C's promise line and 166-B's
`footing.fields` both state a DERIVED COUNT and are therefore functions (`libraryVocabulary.ts:149-153`
is the shipped shape). So they live in `templateFirstVocabulary.ts`, alongside every other string on
this surface, exactly as `governanceVocabulary.ts` sits beside `GovernanceSection.tsx`.
⚠ A runtime FUNCTION exported beside a COMPONENT is a `react-refresh/only-export-components` lint
ERROR (measured, `TemplateAttachSection.tsx:164-169`, `ExternalActionSection.tsx:77`) — which is the
second reason the functions belong in a `.ts` module.

**The API client analogs (`frontend/src/lib/api.ts`):** `uploadWorkflowTemplate` (`:3548-3571`) for
the FormData + Bearer + **NO `Content-Type`** shape and the `WorkflowTemplateUploadError`
(`:3524-3533`, `status: number | "network"`) it throws; `getWorkflowTemplatePlaceholders`
(`:3603-3615`) for the `signal?: AbortSignal` tail and the `WorkflowTemplatePlaceholders`
(`{read, placeholders}`) return type, which is **reused verbatim**.
⚠ `GenerateWorkflowBody` already declares `template_placeholders?: string[]` (`:3414`) — no edit.

**Refusals are relayed, not re-worded** (`TemplateAttachSection.tsx:41-48`): 422 and 502 relay the
server's own sentence; only 404 is worded client-side, because the server made not-found and
not-yours deliberately indistinguishable.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The two vocabulary modules — one new governed id, one new surface module</name>
  <files>frontend/src/components/workflows/doorVocabulary.ts, frontend/src/components/workflows/doorVocabulary.test.ts, frontend/src/components/workflows/templateFirstVocabulary.ts, frontend/src/components/workflows/templateFirstVocabulary.test.ts</files>
  <read_first>
    - frontend/src/components/workflows/doorVocabulary.ts lines 215-269 (the describe-door block; the new id goes beside `DESCRIBE_CTA` at :227)
    - frontend/src/components/workflows/doorVocabulary.test.ts (the 39 cases: `GOVERNED_ID_COUNT`, the zero-import leaf claim, the pairwise-distinctness case with its declared exception set, the contract-agreement case)
    - frontend/src/components/workflows/library/libraryVocabulary.ts lines 140-160 (`forkFailedMessage` — the message-FUNCTION shape and its docblock rule)
    - .planning/phases/193.1-template-first-authoring/193.1-UAT.md § "167-C copy ruling" (the operator's exact strings for `NAME_CHECK_HEADING` / `NAME_CHECK_ACTION`)
  </read_first>
  <action>
    **`doorVocabulary.ts`** — add exactly ONE export, `DESCRIBE_ATTACH_PROMPT`, with the value in
    `<interfaces>`, in the describe-door block beside `DESCRIBE_CTA`. One JSDoc block, flat
    `export const NAME = "…"` shape, no object, no nesting. Its docblock records D-21's reasoning: it
    lands ~2 inches from the shipped governed `STARTER_DOOR_LINE`
    (*"Not sure where to start? Start from a template."*, `definitionOps.ts:857`) on the Builder's
    describe screen, which seeds the describe box from a curated starter WORKFLOW — two meanings, one
    word, one screen — and rewording the SHIPPED line instead would reopen a Phase 187 governed
    literal, which D-13 and the 166-C precedent already declined.
    ⚠ **Add NO function to this module** — the fence's needle builder coerces every export with
    `.replace` and would throw.

    **`doorVocabulary.test.ts`** — move `GOVERNED_ID_COUNT` 21 → 22 and add the new id to every
    whole-table case (non-emptiness, pairwise distinctness against the declared exception set, the
    contract-agreement case if it enumerates ids). Do not weaken any case to accommodate it.

    **`templateFirstVocabulary.ts`** — a NEW `.ts` module holding every string on this surface that
    is not already a shipped identifier and not a governed door word:
    - `ATTACH_NOTE` (165 `ctrl.note`), `SPEC_FILENAME_LEAD` (165 `spec.filenameLead`)
    - `FOOTING_NONE`, `FOOTING_NOT_WORD`, `FOOTING_UNAVAILABLE`, `FOOTING_LOADING` (166-B, verbatim)
    - `footingFields(count: number): string` (166-B `footing.fields`) — the count is a PARAMETER
    - `templateBindFailedMessage(filename: string): string` — D-06's honest failure sentence, which
      must carry the CONSEQUENCE, not just the fact: the draft was built to fill a template that is
      now not attached, and the fix is to attach it on the step's rail. (RESEARCH §B: the run-time
      failure is `no_template_bound` and is TERMINAL.)
    - the 167-C strings: `NAME_CHECK_DISCLAIM` verbatim from `167/BUILD-CONTRACT.generated.md`,
      `nameCheckRunInputNote(count: number)`, the three bucket labels, `showAllLabel(count: number)`,
      and `NAME_CHECK_HEADING` / `NAME_CHECK_ACTION` **exactly as ruled in `193.1-UAT.md`** — do not
      invent copy here.
    Copy `libraryVocabulary.ts:149-153`'s docblock rule onto every function: the interpolated half is
    DATA, and a template assembled at the call site is a string that leaks out of this module's fences.

    **`templateFirstVocabulary.test.ts`** — whole-table properties in `doorVocabulary.test.ts`'s
    shape: every export non-empty; every constant pairwise distinct; **`FOOTING_NONE`,
    `FOOTING_NOT_WORD` and `FOOTING_UNAVAILABLE` asserted pairwise distinct BY NAME**, restating the
    shipped may-never-merge rule one level down; each function asserted to interpolate its argument
    (a function that ignored its count would be the hardcoded-`8` lie); and the module asserted to
    import NOTHING, so it is a cycle-safe leaf like `doorVocabulary.ts`.
  </action>
  <acceptance_criteria>
    - `grep -c "^export const DESCRIBE_ATTACH_PROMPT" frontend/src/components/workflows/doorVocabulary.ts` == 1 and `grep -c "^export function\|=> " frontend/src/components/workflows/doorVocabulary.ts` == 0.
    - `grep -c "template" frontend/src/components/workflows/doorVocabulary.ts` shows no NEW occurrence inside `DESCRIBE_ATTACH_PROMPT`'s value (the value must not contain the word).
    - `doorVocabulary.test.ts` asserts `GOVERNED_ID_COUNT` 22.
    - `templateFirstVocabulary.test.ts` asserts the three no-merge sentences pairwise distinct, and asserts each message function's output changes with its argument.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/doorVocabulary.test.ts src/components/workflows/templateFirstVocabulary.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/doorVocabulary.test.ts src/components/workflows/templateFirstVocabulary.test.ts</automated>
  </verify>
  <done>One governed id added and counted; every other string on this surface has one home and a whole-table suite.</done>
</task>

<task type="auto">
  <name>Task 2: `DescribeTemplateRow.tsx` — five arms, one live region, every count derived</name>
  <files>frontend/src/components/workflows/DescribeTemplateRow.tsx, frontend/src/components/workflows/DescribeTemplateRow.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/TemplateAttachSection.tsx lines 55-160 (the exported sentences and the may-never-merge docblock) and 280-390 (the five-arm render, the ONE `role="status"` wrapper, the plain-text-children rule)
    - frontend/src/components/workflows/library/ForkNameDialog.tsx (the hook-consuming presentational sibling that owns no fetch)
    - .planning/sketches/165-the-template-lands-first/BUILD-CONTRACT.generated.md and .planning/sketches/166-when-there-are-no-eight-fields/BUILD-CONTRACT.generated.md (the acceptance bar — consume them, never re-derive geometry by eye: SEED-155)
  </read_first>
  <behavior>
    - `idle` (no file held): the control and its note render; NO fields region, NO footing line.
    - `loading`: `TEMPLATE_FIELDS_LOADING` and `FOOTING_LOADING`, and neither any other arm's node.
    - `fields` with N names: `TEMPLATE_FIELDS_HEADING`, a list of exactly N items in the SERVER'S
      order (never re-sorted), `SPEC_FILENAME_LEAD` + the filename, and `footingFields(N)` whose
      rendered number equals the number of `<li>` elements actually present.
    - `none`: `TEMPLATE_FIELDS_NONE` + `FOOTING_NONE`, and neither `TEMPLATE_FIELDS_UNAVAILABLE` nor
      `FOOTING_UNAVAILABLE`.
    - `notWord`: `TEMPLATE_FIELDS_NOT_WORD` + `FOOTING_NOT_WORD`.
    - `unavailable`: `TEMPLATE_FIELDS_UNAVAILABLE` + `FOOTING_UNAVAILABLE`, and neither `none` node.
    - Pairwise: for every ordered pair of the five arms, rendering arm A produces NONE of arm B's
      testids — the mechanised form of *"these sentences may never merge"* (166's audit #6).
    - A placeholder name containing HTML (`<img src=x onerror=…>`) renders as visible TEXT and
      produces no element from it.
  </behavior>
  <action>
    Create the component. It is PRESENTATIONAL: it receives the read state and a filename as props,
    plus an `onPickFile(file: File)` and an `onClear()` callback. **It owns no fetch, no
    `AbortController` and no network import** — `ForkNameDialog.tsx` is the shape.

    Render, in this order (the 165-C shape, splice point handled by Plan 08):
    the reworded prompt `DESCRIBE_ATTACH_PROMPT`; `ATTACH_NOTE`; the file input labelled with the
    imported `TEMPLATE_ATTACH_LABEL` and accepting the imported `TEMPLATE_ACCEPT`; the imported
    `TEMPLATE_TYPES_NOTE`; then the spec block; then EXACTLY ONE footing line.

    Copy `TemplateAttachSection`'s render discipline verbatim in kind: ONE stable `role="status"`
    wrapper present across all arms (one announcement, not four — its `:288-293` explains why); ONE
    `data-testid` per arm so a plant that renders one arm's node for another turns a test red; and
    placeholder names as ordinary React text children, escaped by construction, with a comment
    saying nothing goes near `dangerouslySetInnerHTML`.

    ⚠ Derive the count. `footingFields` takes `state.fields.length` — never a literal, never a prop
    the caller could pass wrong. The suite asserts the rendered number equals the `<li>` count, so a
    hardcoded value cannot pass.

    ⚠ Restate the INHERITED constraint in the component's docblock: `none` and `unavailable` — and
    their two footing lines — may never merge. `none` is a fact about the document; `unavailable` is
    a fact about us. The consequence for the draft happens to be identical, which is exactly why the
    wording must not be.

    ⚠ Record the two deliberate omissions in the docblock so they read as decisions: `spec.promise`
    is not rendered (166-B's footing is the ONE line), and `hint.withTemplate` is declined (D-13), so
    the three governed hint fragments and `FLAG_OFF_DESCRIBE_MARKUP` stay whole.

    ⚠ **Name no governed door word in prose.** Task 3 adds this file to the RAW copy sweep.
  </action>
  <acceptance_criteria>
    - The component's import list contains no `@/lib/api` and no `AbortController`; `grep -c "fetch(" frontend/src/components/workflows/DescribeTemplateRow.tsx` == 0.
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/workflows/DescribeTemplateRow.tsx` == 0.
    - The suite contains a loop asserting all 20 ordered arm pairs, and a case asserting the rendered footing count equals `container.querySelectorAll("li").length`.
    - The XSS case asserts the injected string is present as text and that `container.querySelector("img")` is null.
    - `wc -c frontend/src/components/workflows/DescribeTemplateRow.tsx` > 1000 (record it — the copy fence's floor).
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/DescribeTemplateRow.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/DescribeTemplateRow.test.tsx</automated>
  </verify>
  <done>The row renders five unconfusable arms, states a derived count, and cannot render one arm's sentence for another.</done>
</task>

<task type="auto">
  <name>Task 3: The stateless-read client, and the copy fence's fifth source</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - frontend/src/lib/api.ts lines 3520-3620 (`WorkflowTemplateUploadError`, `uploadWorkflowTemplate`, `WorkflowTemplatePlaceholders`, `getWorkflowTemplatePlaceholders`)
    - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx lines 460-560 (the fence, now with FOUR sources after Plan 05)
    - .planning/phases/193.1-template-first-authoring/193.1-02-SUMMARY.md (the route's exact path, its refusal arms, and what `read` can be)
  </read_first>
  <action>
    **`api.ts`** — add `readTemplatePlaceholdersFromFile(file: File, signal?: AbortSignal):
    Promise<WorkflowTemplatePlaceholders>` beside the two shipped template functions. It is
    `uploadWorkflowTemplate`'s BODY (FormData, single part named `file`, `Authorization: Bearer`,
    **no `Content-Type`** so the browser writes the boundary) with `getWorkflowTemplatePlaceholders`'s
    SIGNATURE TAIL (`signal`) and RETURN TYPE (`WorkflowTemplatePlaceholders`, reused verbatim — one
    wire shape for both read doors means the client derives the same arms).

    Throw `WorkflowTemplateUploadError` on failure, carrying `status: number | "network"` and the
    server's `detail` when the body has one, so the caller can relay a 422's own sentence rather than
    re-word it (`TemplateAttachSection.tsx:41-48`).

    Write the docblock recording what this function is FOR and what it is not: it takes bytes and
    returns names and **persists nothing** — no definition, no Storage object, no draft row — which
    is why it needs no `definitionId`. Name `getWorkflowTemplatePlaceholders` as its bound-template
    sibling so a reader is not left to guess why there are two.

    ⚠ Do NOT touch `GenerateWorkflowBody` — `template_placeholders?: string[]` is already declared at
    `:3414`.

    **The fence** — add a fifth `SWEPT_SOURCES` entry for `./DescribeTemplateRow.tsx` with its `?raw`
    import, bump the scope count 4 → **5**, and move the governed-id assertion at `:527` **21 → 22**
    to match Task 1. Then DRIVE IT RED: plant a real governed door literal (read the value from the
    module at test time — do not re-type the words) into `DescribeTemplateRow.tsx`, record the
    failure verbatim, remove the plant, confirm md5-identical restoration. Check the `>1000` floor
    against Task 2's measured `wc -c` and report the number.

    **The count gate** — add `BASELINE` entries for `templateFirstVocabulary.test.ts` and
    `DescribeTemplateRow.test.tsx`, and re-pin `doorVocabulary.test.ts` (39 → new) and
    `WorkflowDoorSwitch.test.tsx` (Plan 05's value → new). All four numbers come from THIS SCRIPT'S
    OWN `actual` column. No `TARGETS` edit is needed — `src/components/workflows` is a directory
    entry; state that this was CHECKED.
  </action>
  <acceptance_criteria>
    - `grep -c "readTemplatePlaceholdersFromFile" frontend/src/lib/api.ts` >= 1 and the function's fetch call carries no `Content-Type` header.
    - `git diff frontend/src/lib/api.ts | grep -c "GenerateWorkflowBody"` == 0 (untouched).
    - `WorkflowDoorSwitch.test.tsx` asserts `toHaveLength(5)` and the governed-id count **22**.
    - The SUMMARY records the RED sweep message from the plant in `DescribeTemplateRow.tsx`, the restored md5, and the `wc -c` measurement.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` exits 0 with `failed 0` and all four pins present.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports **33**.
  </acceptance_criteria>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The client exists, the fence sweeps five sources and has fired in the new one, and every touched suite is re-pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document text → DOM | Placeholder names parsed from an attacker-supplied `.docx` are rendered by `DescribeTemplateRow.tsx`. |
| browser → API | `readTemplatePlaceholdersFromFile` posts raw file bytes with a bearer token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.1-06-01 | Tampering (XSS) | `DescribeTemplateRow.tsx` | mitigate | Names render as ordinary React text children — escaped by construction. `dangerouslySetInnerHTML` occurs zero times in the file (asserted), and the suite carries a live injection case proving the payload renders as text and creates no element. |
| T-193.1-06-02 | Information disclosure | `readTemplatePlaceholdersFromFile` | mitigate | `Authorization: Bearer` only; no `Content-Type` so the browser owns the boundary. No path, no id and no user identifier is sent — the route has nothing to authorize against, by construction (Plan 02). |
| T-193.1-06-03 | Spoofing / integrity | a re-worded server refusal | mitigate | 422/502 relay the server's own sentence (`TemplateAttachSection.tsx:41-48`); the typed error carries `status` and `detail` so the caller cannot need to invent one. |
| T-193.1-06-04 | Tampering | a governed door word re-typed into the new component | mitigate | The RAW D-24(a) sweep gains `DescribeTemplateRow.tsx` in this same commit and is driven RED against a real plant in it (D-14 / WR-01). |
| T-193.1-06-05 | Repudiation | a stated count that does not match the list | mitigate | `footingFields` takes the rendered list's length; the suite asserts the rendered number equals the `<li>` count, so a hardcoded value cannot pass. |
| T-193.1-06-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** No `## Package Legitimacy Audit` exists in RESEARCH because no external package is added. |
</threat_model>

<verification>
- All five touched/new frontend suites green; count gate green at cap 2 with four pins updated.
- `tsc -p tsconfig.app.json` reports 33.
- `FLAG_OFF_DESCRIBE_MARKUP` and `FLAG_OFF_HEADER_MARKUP` are untouched — nothing is mounted yet.
- `TemplateAttachSection.tsx` has ZERO diff (the `scale`-prop correction).
</verification>

<success_criteria>
The pre-draft row's words, component and client all exist and are provably honest — five
unconfusable arms, a derived count, a swept-and-fired copy fence, one new governed id counted in two
places — with nothing yet mounted and no user-visible change.
</success_criteria>

<output>
Create `.planning/phases/193.1-template-first-authoring/193.1-06-SUMMARY.md` when done.
</output>
