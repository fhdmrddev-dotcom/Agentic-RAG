---
phase: 193.1-template-first-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-02: the pre-draft describe screen has a byte-exact characterization capture that PREDATES every source change this phase makes"
    - "D-01: the capture commit modifies no file under frontend/src or backend/app other than adding the new suite"
    - "SEED-157's wire defect is pinned: the `/generate` request body is asserted as a KEY SET before the wire grows"
    - "The captures cover flag ON and flag OFF across all three builderPhase arms — six rows, not one"
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx"
      provides: "Six whole-innerHTML captures of the pre-draft describe screen + the onDraft key-set assertion"
      contains: "CAPTURE_SHA"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "TARGETS + BASELINE entries for the new suite (two knobs, both required for a src/pages file)"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx"
      to: "frontend/src/pages/WorkflowBuilderPage.tsx"
      via: "render of the real page through the real EffectiveFeaturesProvider"
      pattern: "WorkflowBuilderPage"
---

<objective>
Capture the pre-draft describe screen on the **UNMOVED** tree, before Plan 04 cuts the draft
concern out of `WorkflowBuilderPage.tsx`.

Purpose: the 188.1 lesson — *a characterization baseline only proves something if it PREDATES the
change*. Plan 04 must hold these strings green with **ZERO re-capture**; Plan 07 re-captures them
ONCE, declared and dated. Neither claim is available unless this suite exists first.

Output: one new test file, pinned in the same commit, plus the `onDraft` request-body key-set
assertion that would have caught `SEED-157` in 2026-06.
</objective>

<worktree_protocol>
1. FIRST action in the worktree, before reading anything: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
2. THEN assert the dispatched base SHA: `git rev-parse HEAD` must equal the base SHA given in the
   dispatch prompt. Worktrees have forked from the wrong base 12/12 times in Phase 192 and again on
   a single worktree on 2026-08-14 — a mismatch is a STOP, not a warning.
3. `GSD_VITEST_MAX_WORKERS=2` on EVERY vitest invocation. `CLAUDE.md`'s table still says 4 and that
   value has ROTTED (measured at HEAD: cap 2 → exit 0, total 3604, failed 0, 68/68 pinned).
</worktree_protocol>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md
@.planning/phases/193.1-template-first-authoring/193.1-PATTERNS.md
@.planning/phases/193.1-template-first-authoring/193.1-VALIDATION.md
</context>

<interfaces>
<!-- Extracted at HEAD 568afc7a. Use these directly; no codebase exploration needed. -->

The analog suite is `frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx`
(Phase 193-01, 17 cases, pinned). Its five load-bearing properties, all of which this suite copies:

- A NEW FILE, never an append (`:3-6`) — so `git diff --name-only` over the capture commit is the
  proof rather than a promise.
- The predate proof recorded verbatim in the docblock as re-runnable commands (`:22-30`), plus
  `const CAPTURE_SHA = "…"` (`:98`) and a test asserting it (`:382`).
- ONE render helper shared by capture and assertion (`:148-186`) — *"a second helper is exactly how
  a capture and the assertion that guards it drift apart"*. It reads
  `rendered.container.innerHTML`, NEVER a subtree (`:157-159`).
- Deterministic inputs: a fixed host node and a plain-function observer, never a `vi.fn()`
  (`:111-114`).
- The row table declared ONCE, keys asserted equal on both sides (`:389-403`), then
  `expect(await capture(ROWS[row])).toBe(BASELINE[row])` (`:406-411`).
- The flag-ON capture drives the REAL `useCanvasGate` through the REAL `EffectiveFeaturesProvider`
  by dynamic import (`:132-146`, `:164-176`) rather than mocking the module.

The subject, at HEAD:
- `WorkflowBuilderPage.tsx:1532-1614` — `describeScreen`, built once and returned from THREE
  `builderPhase` arms (`"empty" | "composing" | "error"`, branch opens at `:1532`).
- `WorkflowBuilderPage.tsx:1596` — `{canvasEnabled && <StarterTemplatePicker onChoose={setDescribe} />}`
  is INSIDE the CTA group, which is why the flag-ON capture is not the flag-OFF one.
- `WorkflowBuilderPage.tsx:1583` — the CTA's `onClick={onDraft}`.
- `WorkflowBuilderPage.tsx:1267-1272` — the `/generate` payload today: `{describe}` plus a
  conditional `project_folder_id` spread. Nothing else.
- `frontend/src/lib/api.ts:3409-3415` — `GenerateWorkflowBody` already declares
  `template_placeholders?: string[]`.

Count-gate knobs (`scripts/vitest-count-gate.cjs`): `TARGETS` decides what RUNS, `BASELINE` decides
what is GUARDED. MEASURED at HEAD: `TARGETS` lists `src/pages/WorkflowBuilderPage.*` FILE BY FILE
(`.test.tsx`, `.canvas.test.tsx`, `.header.test.tsx`, `.describe.test.tsx`, `.session.test.tsx`) —
there is NO `src/pages` directory entry. **A new `src/pages/…` suite therefore needs BOTH knobs**,
unlike a new `src/components/workflows/…` suite which needs only `BASELINE`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-draft describe screen — six rows, on the unmoved tree</name>
  <files>frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx (the analog — read it whole; copy its five properties, not its subject)
    - frontend/src/pages/WorkflowBuilderPage.tsx lines 1525-1620 (the subject) and 595-700 (the props and the state the screen reads)
    - frontend/src/pages/WorkflowBuilderPage.describe.test.tsx (how this page is already rendered under test, including its mocks)
  </read_first>
  <action>
    Create the new suite. Declare `CAPTURE_SHA` as the worktree's actual base SHA (`git rev-parse HEAD`)
    and assert it in a test, exactly as the analog does at its `:98` / `:382`.

    Record the predate proof in the module docblock as re-runnable commands whose output is pasted
    beside them: `git rev-parse HEAD`, and `git show HEAD:frontend/src/components/workflows/useTemplateFirstDraft.ts`
    plus `git show HEAD:frontend/src/components/workflows/DescribeTemplateRow.tsx` — both must report
    `fatal: path … does not exist in 'HEAD'` (exit 128). Those two paths are the modules Plans 04 and
    05 create; their non-existence at this commit is what makes these captures a baseline rather than
    a snapshot.

    Declare a SINGLE render helper used by both capture and assertion. It renders the real
    `WorkflowBuilderPage` and returns `rendered.container.innerHTML` — the WHOLE container, never a
    subtree; the root's own attributes are part of what must not change.

    Declare the row table ONCE as six keys and assert the `BASELINE` object's keys deep-equal the
    `ROWS` object's keys before comparing any value:
    `flagOff:empty`, `flagOff:composing`, `flagOff:error`, `flagOn:empty`, `flagOn:composing`,
    `flagOn:error`.

    Reach each `builderPhase` arm the way the page does, not by mocking the store's internals:
    `empty` is the default mount; `composing` is reached by clicking the CTA with a `generateWorkflow`
    stub that never resolves; `error` is reached with a stub resolving `{ok:false, error, detail}`.
    Use plain functions for those stubs, never `vi.fn()` — a row whose behaviour lived in a mock
    implementation is one `clearAllMocks` away from capturing a different state.

    Drive the canvas flag through the REAL `useCanvasGate` and the REAL `EffectiveFeaturesProvider`
    by dynamic import, copying the analog's `canvasOn` mechanism (`:132-146`, `:164-176`). Do NOT
    mock `useCanvasGate` — the flag-ON screen is the one users get (`visual_workflow_canvas.audience`
    is `everyone`) and it is the only capture that can see a re-parenting the flag-OFF pin cannot.

    Add the module-level re-capture standard verbatim in spirit: a diff against these strings is a
    BEHAVIOUR CHANGE TO EXPLAIN, never a test to update; `git diff --numstat` on this file must show
    ZERO deletions through Plan 04.
  </action>
  <acceptance_criteria>
    - The file exports/declares exactly six baseline strings, keyed as listed above, and a test
      asserts `Object.keys(BASELINE)` deep-equals `Object.keys(ROWS)`.
    - A test asserts `CAPTURE_SHA` equals the value in the docblock's `git rev-parse HEAD` line.
    - `grep -c "container.innerHTML" <file>` >= 1 and `grep -c "getByTestId(.*).innerHTML" <file>` == 0
      (whole-container capture, never a subtree).
    - `grep -c "vi.fn()" <file>` == 0.
    - `grep -c "vi.mock(\"@/hooks/useCanvasGate\"" <file>` == 0 and the source names
      `EffectiveFeaturesProvider`.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx`
      exits 0 with 0 failing.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx</automated>
  </verify>
  <done>Six byte-exact captures exist, all green, with the predate proof and CAPTURE_SHA recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the `/generate` request body as a KEY SET, before the wire grows</name>
  <files>frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.tsx lines 1256-1300 (`onDraft` and its payload)
    - frontend/src/lib/api.ts lines 3405-3416 (`GenerateResult`, `GenerateWorkflowBody`)
    - .planning/phases/193.1-template-first-authoring/193.1-RESEARCH.md § "The one-line wire change"
  </read_first>
  <action>
    Add two cases to the same suite that capture what `onDraft` sends TODAY.

    Case A — no knowledge base picked: click the CTA with typed text and assert the object handed to
    the `generateWorkflow` stub has EXACTLY the key set `["describe"]`. Assert on
    `Object.keys(body).sort()` deep-equality, never `toMatchObject` / `toContain`: the whole point is
    that a key which is ABSENT today must be visible when it arrives.

    Case B — a knowledge base picked: select a folder in `project-folder-picker`, click the CTA, and
    assert the key set is EXACTLY `["describe","project_folder_id"]`.

    Both cases carry a docblock naming what they exist for: `template_placeholders` has been accepted
    by `POST /workflows/generate` since Phase 103 and the frontend has never sent it (`SEED-157`).
    Plan 06 adds that key and MUST move these two literals in the same commit — that movement is the
    phase's mechanical evidence that the wire changed.
  </action>
  <acceptance_criteria>
    - Two new cases exist; each asserts a sorted key-set deep-equality, and neither uses
      `toMatchObject`, `toContain` or `expect.objectContaining` on the request body.
    - `grep -c "template_placeholders" <file>` >= 1 (named in the docblock as the key that is absent).
    - The suite still exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx</automated>
  </verify>
  <done>The pre-change wire is asserted as a key set in two folder states.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the suite in BOTH count-gate knobs, and prove the commit touched no source</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - scripts/vitest-count-gate.cjs — the `TARGETS` array and the `BASELINE` object, including the
      193-01 and 193-05 comment blocks that record the two-knob rule
  </read_first>
  <action>
    Add `"src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx"` to `TARGETS` and a
    `"WorkflowBuilderPage.preDraft.baseline.test.tsx": <measured count>` entry to `BASELINE`, in this
    same commit. Take the count from THIS SCRIPT'S OWN `actual` column on the run that first executes
    the file — never from a hand count.

    Write the comment beside both entries recording the MEASURED reason both knobs are needed here
    and only one was needed in 193-01: `TARGETS` lists the five `src/pages/WorkflowBuilderPage.*`
    suites file by file and carries no `src/pages` directory entry, so a new page-level suite is not
    executed by the gate at all until it is named. A file the gate never runs has falsified nothing.

    Then run the full gate and record its `total` / `failed` / pinned-file count in the SUMMARY.

    Finally, assert the capture property mechanically: `git diff --name-only <base>..HEAD` for this
    plan's commits must list ONLY the new `.test.tsx` file and `scripts/vitest-count-gate.cjs`. No
    file under `frontend/src` other than the new suite, and no file under `backend/app`, may appear.
    Paste the command and its output into the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('fs').readFileSync('scripts/vitest-count-gate.cjs','utf8'); process.exit(s.includes('WorkflowBuilderPage.preDraft.baseline.test.tsx')?0:1)"` exits 0, and the string occurs at least twice (once per knob).
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` exits 0 with `failed 0` and the
      new file listed as pinned.
    - `git diff --name-only` over this plan's commits lists exactly two paths, and neither is an
      existing file under `frontend/src` or `backend/app`.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports the baseline **33** errors.
  </acceptance_criteria>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The suite runs and is guarded; the commit is proved to have touched no source file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This plan adds test code and one build-script pin. No route, no input, no render path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.1-01-01 | Tampering | `scripts/vitest-count-gate.cjs` | mitigate | A `BASELINE` key naming a path that does not exist makes the gate ERROR (exit 2), not fail — the pin therefore lands in the commit that creates the file, and the acceptance criteria run the gate to prove exit 0. |
| T-193.1-01-02 | Repudiation | the capture's predate claim | mitigate | `CAPTURE_SHA` is asserted in-suite and the `git show … exit 128` proof for both future module paths is recorded in the docblock, so "this baseline predates the change" is re-runnable rather than asserted. |
| T-193.1-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan** — `RESEARCH.md` § Standard Stack traces every capability to a shipped symbol and records no `## Package Legitimacy Audit` because no external package is added. |
</threat_model>

<verification>
- The new suite is green and pinned in both knobs.
- The full count gate is green at cap 2 and its numbers are recorded.
- `tsc -p tsconfig.app.json` is unmoved at 33.
- `git diff --name-only` proves no source file was touched.
</verification>

<success_criteria>
Six whole-container captures of the pre-draft describe screen (flag ON and OFF × three
`builderPhase` arms) and two `/generate` key-set assertions exist, are guarded by the count gate,
and are provably older than every source change this phase will make.
</success_criteria>

<output>
Create `.planning/phases/193.1-template-first-authoring/193.1-01-SUMMARY.md` when done.
</output>
