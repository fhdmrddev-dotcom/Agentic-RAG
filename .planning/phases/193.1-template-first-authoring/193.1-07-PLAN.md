---
phase: 193.1-template-first-authoring
plan: 07
type: execute
wave: 4
depends_on: ["193.1-02", "193.1-03", "193.1-06"]
files_modified:
  - frontend/src/components/workflows/useTemplateFirstDraft.ts
  - frontend/src/components/workflows/useTemplateFirstDraft.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx
  - frontend/src/hooks/useDraftPersistence.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-19: the wire and the bind land in ONE plan and cannot be merged apart — a draft aimed at a template that never got bound is TERMINAL at run (`no_template_bound`) and strictly worse than the blind draft it replaces"
    - "SC#1: `/generate` carries `template_placeholders` derived from the real fields of the file the author supplied"
    - "D-06: the held file is uploaded the moment a `definition_id` first exists — one file, one hand-off, through the SHIPPED `POST /workflows/{id}/template`"
    - "D-06 rule 1: a throwing bind can NEVER produce a save-error state and can NEVER trip `haltedRef`"
    - "D-06 rule 2: the silent bind's failure has its OWN honest line, never the save's success or refusal path, and the sentence carries the CONSEQUENCE not just the fact"
    - "D-07: the draft CTA is disabled while the read is in flight, so the blind draft is IMPOSSIBLE rather than recoverable"
    - "D-08: SC#4 holds BY CONSTRUCTION — no file held ⇒ never `loading` ⇒ never disabled. ONE term, no second conditional"
    - "D-25: `useDraftPersistence.ts` is NOT edited — the callback is supplied at `WorkflowBuilderPage.tsx:921`; the hook gains a test case only"
  artifacts:
    - path: "frontend/src/components/workflows/useTemplateFirstDraft.ts"
      provides: "the held File, the five-arm read, the CTA gate term, the placeholder wire, and the fire-and-forget bind"
    - path: "frontend/src/hooks/useDraftPersistence.test.tsx"
      provides: "the throwing-bind case — a genuinely new failure mode no shipped test covers"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "TARGETS + BASELINE for `useDraftPersistence.test.tsx`, which is MEASURED to be in NEITHER today"
  key_links:
    - from: "frontend/src/components/workflows/useTemplateFirstDraft.ts"
      to: "POST /workflows/generate"
      via: "a third conditional spread carrying template_placeholders"
      pattern: "template_placeholders"
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "POST /workflows/{id}/template"
      via: "onDraftCreated → a void async bind that catches everything internally"
      pattern: "uploadWorkflowTemplate"
---

<objective>
Send the template's real field names on the real `/generate` call, **and** bind the same bytes to the
definition the moment a row exists — in one indivisible plan.

Purpose (D-19, measured across four hops): `template_placeholders` is **not a hint**. It is rendered
into the authoring prompt at `grounding.py:576-577`, and `AUTHORING_SYSTEM_PROMPT`'s
*"DELIVERABLE RULE (CRITICAL)"* branches on exactly that section — names present ⇒ `llm_emit` /
`render_template`; absent ⇒ plain text, and `render_template` FORBIDDEN. So sending the field flips a
branch. **A `render_template` draft whose template was never bound fails at run with
`no_template_bound` (`phase_types.py:1271`) and can never publish** — strictly worse than today's
blind draft, which at least runs. **The wire and the bind may not ship in different waves such that
the wire is mergeable alone.**

Output: the read state machine, the wire, the bind, the CTA gate, and the one failure mode no shipped
test covers.
</objective>

<worktree_protocol>
1. FIRST action: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
2. THEN assert `git rev-parse HEAD` equals the dispatched base SHA. A mismatch is a STOP.
3. `GSD_VITEST_MAX_WORKERS=2` on EVERY vitest invocation; ≤ 2 concurrent test-running agents.
4. `cd frontend && npx tsc --noEmit -p tsconfig.app.json`, baseline **33**.
</worktree_protocol>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md
@.planning/phases/193.1-template-first-authoring/193.1-RESEARCH.md
@.planning/phases/193.1-template-first-authoring/193.1-PATTERNS.md
@.planning/phases/193.1-template-first-authoring/193.1-05-SUMMARY.md
@.planning/phases/193.1-template-first-authoring/193.1-06-SUMMARY.md
</context>

<interfaces>
<!-- Extracted at HEAD 568afc7a. -->

**⚠ TWO ANALOGS, ONE PER HALF (D-25 / PATTERNS §C-2). CONTEXT names one precedent where the cut needs
two, and the missing one is the harder half.**

*The read state machine* — `frontend/src/hooks/useTemplatePlaceholders.ts` (154 L), NOT
`useWorkflowFork.ts` (which has **no fetch state machine at all**: no `loading` member, no
`AbortController`, no staleness rule; copying it here yields a hook with no honest in-flight arm,
which is exactly what D-07 and D-09 exist to provide):
- `:56-70` the five honest readings — `idle | loading | fields | none | unavailable`.
- `:47-50` **`fields` carries a NON-EMPTY list BY CONTRACT** — an empty read resolves to `none`,
  never to `fields` with `[]`. That single rule is what makes 166-B's five arms unconfusable; it must
  survive the copy.
- `:76-77` the two waiting readings frozen at module scope so identity compares hold across renders.
- `:79-86` **THE RULE: the answer is stored WITH the subject it answered for.**
- `:149-153` and **`loading` is DERIVED when the stored subject differs** — never stored.
- `:89-94` the double-shaped `isAbortError` (itself from `usePanelReconcile.ts:84-92`); `:119-122`
  the post-await guard.
- `:83` **COMPARE POSITIVELY**: `read !== "unreadable"` would wave through `undefined`; the shipped
  hook checks `res.read !== "ok"` for exactly that reason.
- ⚠ Its effect key is `[definitionId, assetId]` (`:147`) and **neither value exists pre-draft**. It
  is a SIBLING, not a parameterisation. The new subject is the held `File` — compare by IDENTITY
  (`File` objects are stable across renders); **do not key on `file.name`**, which two different
  documents can share. No shipped hook proves `File`-identity keying, so assert it in the suite.

*The module contract* — `frontend/src/components/workflows/library/useWorkflowFork.ts`:
`:205` and its docblock `:193-204` — a failure state that is a two-field object which
**structurally cannot hold server prose, a status code or an id**; the raw error goes to
`console.error` at the boundary. D-06's bind failure takes exactly this posture.

**THE D-06 SEAM, verified at HEAD:**
- `useDraftPersistence.ts:342` — `onDraftCreated: (id: string) => void`; `:553-556` held in a ref;
  `:645` `onDraftCreatedRef.current(created.id)`.
- `:635` `if (draftIdRef.current === null)` — **the create branch, reached exactly once**, itself
  guarded against re-entry by `creatingRef` (`:639`). "First save" is distinguished by CONTROL FLOW,
  not by a boolean — no flag is needed.
- `WorkflowBuilderPage.tsx:921` — `onDraftCreated: setDraftId`. **THIS is the line that changes.**
- ⚠ **D-25/PATTERNS §C-3: `useDraftPersistence.ts` needs NO SOURCE EDIT.** `:645` is the hook
  CALLING the callback. A plan that opens the hook "to add the bind" is editing the wrong module and
  will be tempted to change the try/catch at `:635-660` — which is the one thing D-06 rule 1 forbids.
- ⚠ **`:645` sits INSIDE the `try` whose `catch` is at `:656`.** That catch calls `refusalOf(err)`,
  may set `haltedRef.current = true` for a terminal refusal, `setState(refusal)` and `break`s the
  drain loop — and `:971` documents that **nothing clears the halt**. So a SYNCHRONOUS throw out of
  `onDraftCreated` is rendered as a failed save for a row that WAS created, and can freeze autosave
  for the session. **The bind must be `void`-ed into an `async` function that catches everything
  internally.**

**What the bind then does, and why it composes without new machinery:**
`uploadWorkflowTemplate(id, file)` (`api.ts:3548`) returns the descriptor → hand it to the SHIPPED
`onTemplateAttached` (`WorkflowBuilderPage.tsx:1364-1370`) → `setTemplateAsset` + `saveNow()`.
- `setTemplateAsset` (`builderStore.ts:697-705`) **replaces** any prior `kind==="template"` entry
  rather than appending, and guards `builderPhase !== "drafted"` — satisfied, because `performWrite`
  only reaches the create branch when the snapshot is `"drafted"` (`:618`).
- If the bind resolves while the create's write is still in flight, `saveNow` → `performWrite` hits
  the single-flight guard (`:603-606`), sets `pendingRef`, and the drain's supersession test (`:689`,
  `:716`) issues the follow-up. **No second writer, no race — by construction.**
- `onTemplateAttached`'s docblock (`:1348-1363`) already proves `saveNow` reads `store.getState()` at
  FIRE time, so the descriptor written one statement earlier is in the payload — this is NOT a
  closure over a stale rendered value.

**WHERE THE HONEST FAILURE LINE GOES — measured, there is no ready region (PATTERNS §9):**
| Candidate | Verdict |
|---|---|
| the `generate-error` block (`:1600-1612`, gated `builderPhase === "error"`) | ❌ pre-draft only; by bind time the phase is `"drafted"` |
| `saveRefusalSentence` → the toolbar (`:1441`, consumed `:1453`/`:1473`) | ❌ **forbidden by D-06 rule 2 — this IS the save's path** |
| `canvasNotice` (state `:1035`, consumed `:1670`) | ⚠ canvas-only (flag-off Spine never renders it) and retired on a history step (`:807`, `:1457`) — a bind failure that vanishes on an undo is a failure nobody saw |
⇒ **Build the 192 shape** (`WorkflowsPage.tsx:955-985`): state in the hook, JSX on the host, message
from a `.ts` vocabulary function, `role="status"`, **exactly one node**. There is **no toast library**
in this repo (verified in 192: zero `sonner` imports, no `ui/toast`) — do not add one.

**The `canDraft` gate site:** `WorkflowBuilderPage.tsx:687` today is
`describe.trim().length > 0 && builderPhase !== "composing"`; it now lives in the extracted hook
(Plan 05). D-08 holds by construction **only if the new term is a single `&&` against the read
state's `loading` arm**, which is unreachable when no file is held. There is no second conditional to
get wrong.

**The count-gate measurement that matters here:** `TARGETS` contains **no `src/hooks` entry** and
`BASELINE` contains **no `useDraftPersistence.test.tsx` key** — verified at HEAD. **The gate does not
execute that suite at all today.** A test that does not run has falsified nothing (verbatim Phase
187's round-5 "verification truth 14"). Both knobs must be set in the same commit.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The pre-draft read — five arms keyed on the held File, seedable across the handoff</name>
  <files>frontend/src/components/workflows/useTemplateFirstDraft.ts, frontend/src/components/workflows/useTemplateFirstDraft.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useTemplatePlaceholders.ts (the whole file — the union, the frozen waiting states, the answer-with-its-subject rule, the abort predicate, the positive comparison)
    - frontend/src/components/workflows/useTemplateFirstDraft.ts (as Plan 05 left it)
    - frontend/src/lib/api.ts — `readTemplatePlaceholdersFromFile` as Plan 06 shipped it
  </read_first>
  <behavior>
    - No file held ⇒ `idle`. **Never `loading`.** (This is D-08's whole mechanism.)
    - A file just picked, answer outstanding ⇒ `loading`, DERIVED from the stored answer's subject
      differing from the held file — never stored.
    - Server `{read:"ok", placeholders:[…]}` with a `.docx` ⇒ `fields` with a NON-EMPTY list in the
      server's order.
    - Server `{read:"ok", placeholders:[]}` with a `.docx` ⇒ `none`. With `.pptx`/`.xlsx` ⇒ `notWord`.
    - Server `{read:"unreadable"}` ⇒ `unavailable{reason:"unreadable"}`; a rejected fetch or a 4xx/5xx
      ⇒ `unavailable{reason:"unreachable"}`. An `AbortError` is SILENT — neither arm.
    - Replacing file A with file B while A's answer is outstanding ⇒ `loading`, never A's stale answer.
    - **Two distinct `File` objects with the SAME `name`** are different subjects: B's read is issued
      and A's answer never satisfies B.
    - A seeded answer `{file, state}` resolves IMMEDIATELY to `state` and issues **zero** requests.
    - `canDraft` is false while `loading` and unchanged in every state where no file is held.
  </behavior>
  <action>
    Add `useTemplateRead` to `useTemplateFirstDraft.ts` (same module, second export) and consume it
    from `useTemplateFirstDraft`. Export the state union and the `Answer` shape — Plan 08 passes an
    `Answer` across the door handoff.

    Copy `useTemplatePlaceholders`'s five properties by NAME, not by feel: the frozen module-scope
    `idle`/`loading` values; the answer stored with its subject; `loading` derived; the double-shaped
    abort predicate plus the post-await `signal.aborted` guard; and the POSITIVE `res.read !== "ok"`
    comparison (a negative test would wave through `undefined` from an older server).

    Preserve the non-empty-by-contract rule: an `ok` read with zero names becomes `none` (or
    `notWord`, decided from the held file's extension against the three accepted ones), never `fields`
    with `[]`. Derive `notWord` HERE rather than in the component, so the union is the whole answer
    and the row stays purely presentational.

    Key the effect on the `File` OBJECT IDENTITY. Write the docblock stating that no shipped hook
    proves `File`-identity keying and that the suite asserts it — including the same-`name`,
    different-object case.

    Accept an optional seed argument shaped as an `Answer`. Seeding must install the answer for that
    exact file so `loading` is not derived and no request is issued — this is the mechanism that
    carries the fast door's already-completed read across the handoff (Plan 08) instead of re-reading
    and briefly re-opening the blind-draft race.

    Add the D-07 term to `canDraft`: ONE `&&` against the read state's `loading` arm. **Do not add a
    "has a template" conditional** — D-08 says the absence of a read IS the absence of a gate, and a
    second conditional is one that can be got wrong.
  </action>
  <acceptance_criteria>
    - The suite covers all nine behaviours above, including the same-`name`-different-`File` case and the seeded-answer zero-request case.
    - `grep -c "file.name ===\|file.name !==" frontend/src/components/workflows/useTemplateFirstDraft.ts` == 0 (the subject is identity, not name).
    - `grep -c "res.read === \"unreadable\"" frontend/src/components/workflows/useTemplateFirstDraft.ts` == 0 (the comparison is positive).
    - The `canDraft` expression contains exactly one new `&&` term and no reference to whether a file is held.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/useTemplateFirstDraft.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/useTemplateFirstDraft.test.tsx</automated>
  </verify>
  <done>Five honest arms over a held file, no stale answer survives a replacement, and the CTA gate is one term.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The wire and the bind — indivisible, with the bind unable to fail the save</name>
  <files>frontend/src/components/workflows/useTemplateFirstDraft.ts, frontend/src/components/workflows/useTemplateFirstDraft.test.tsx, frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.tsx lines 905-925 (the `useDraftPersistence` call and `onDraftCreated: setDraftId`), 1340-1372 (`onTemplateAttached` and its docblock), 1990-2020 (the drafted return: `actionGroup`, the header branch, and `builder-grid`)
    - frontend/src/hooks/useDraftPersistence.ts lines 590-670 (`performWrite`, the create branch, the `try`/`catch` and `haltedRef`) and 960-980 (the note that nothing clears the halt)
    - frontend/src/pages/WorkflowsPage.tsx lines 950-990 (the 192 `forkFailed` precedent — state in the hook, JSX on the host, one `role="status"` node)
  </read_first>
  <behavior>
    - `onDraft` sends a body whose sorted key set is exactly `["describe","template_placeholders"]`
      when a file is held and the read resolved to `fields`, and the values ARE the read's names.
    - `onDraft` sends NO `template_placeholders` key when the read resolved to `none`, `notWord` or
      `unavailable` — an empty array is not sent, because `(none)` and `[]` mean different things to
      the prompt's `", ".join`.
    - `onDraft` is not reachable while `loading` (the CTA is disabled), and the auto-draft one-shot
      also refuses to fire while `loading`.
    - On the create branch, the held file is uploaded exactly ONCE via `uploadWorkflowTemplate`, and
      the returned descriptor reaches `onTemplateAttached`.
    - **A bind that throws SYNCHRONOUSLY leaves the persist state NOT an error and `haltedRef`
      untripped**, and `draftId` still holds the created id.
    - A bind that rejects sets `bindFailed` and renders exactly one `role="status"` node carrying
      `templateBindFailedMessage(filename)`; the toolbar's save reading is unchanged.
    - With no bind failure, the drafted view's DOM is byte-identical to today's.
  </behavior>
  <action>
    **The wire.** Add a THIRD conditional spread to the `generateWorkflow` payload, in the exact shape
    of the shipped `project_folder_id` one, carrying the read's names when and only when the state is
    `fields`. `GenerateWorkflowBody` already declares the key — no API-client edit.

    Then MOVE Plan 01's two key-set literals to their new values in
    `WorkflowBuilderPage.preDraft.baseline.test.tsx` and ADD the four new cases the behaviours above
    describe. **Declare the move in the case's docblock, dated**: this is the phase's mechanical
    evidence that the wire changed, and `SEED-157` is what it closes. The SIX baseline CAPTURES in
    that file must NOT move — `git diff` on this file must touch only the key-set cases.

    **The bind.** Add `bindHeldTemplate(definitionId)` to the hook: an `async` function that uploads
    the held `File` through the shipped `uploadWorkflowTemplate`, hands the descriptor to an
    `onTemplateBound(descriptor)` callback supplied by the page, and **catches everything
    internally** — setting a two-field `bindFailed` object that structurally cannot hold server prose,
    a status code or an id (the `useWorkflowFork.ts:205` posture), and sending the raw error to
    `console.error` at the boundary.

    Wire it at `WorkflowBuilderPage.tsx:921` as a composition that cannot throw synchronously:
    `setDraftId(id)` followed by a `void`-ed call into the async bind. **Do not edit
    `useDraftPersistence.ts`** — `:645` is the hook calling the callback, and touching the try/catch
    at `:635-660` is the one thing D-06 rule 1 forbids. Have the page pass the shipped
    `onTemplateAttached` as `onTemplateBound`, so `setTemplateAsset` + `saveNow` remain the single
    writer on the definition JSONB.

    **The failure line.** Render exactly ONE `<p data-testid="template-bind-failed" role="status">`
    in the drafted view, immediately before the `builder-grid` container, carrying
    `templateBindFailedMessage(filename)` from `templateFirstVocabulary.ts`. It renders only when
    `bindFailed` is non-null, so with no failure the drafted DOM is unchanged and
    `FLAG_OFF_HEADER_MARKUP` (`header.test.tsx:382`) is untouched — state that as the reason for the
    placement. Do NOT route it through `saveRefusalSentence`, `canvasNotice` or any toast.

    The sentence must carry the CONSEQUENCE, not the fact: the draft was built to fill a template that
    is now not attached, and the fix is to attach it on the step's rail. RESEARCH §B is the reason —
    an unbound `render_template` draft is terminal at run.
  </action>
  <acceptance_criteria>
    - `grep -c "template_placeholders" frontend/src/components/workflows/useTemplateFirstDraft.ts` >= 1 and `git diff frontend/src/lib/api.ts` is empty.
    - `git diff --numstat frontend/src/hooks/useDraftPersistence.ts` is EMPTY (D-25: no source edit).
    - `git diff frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx` touches only the two key-set cases and adds new ones — the six `BASELINE` capture strings are unchanged (show the diff in the SUMMARY).
    - `grep -c "template-bind-failed" frontend/src/pages/WorkflowBuilderPage.tsx` == 1.
    - `grep -c "saveRefusalSentence\|canvasNotice" ` in the diff of the bind-failure region == 0.
    - The suite has a case that plants a synchronously-throwing bind and asserts the persist state is NOT an error.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/useTemplateFirstDraft.test.tsx src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx` exits 0.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/useTemplateFirstDraft.test.tsx src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx</automated>
  </verify>
  <done>The names reach `/generate` and the bytes reach the definition, in one commit, with the save unable to be failed by the bind.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the throwing bind at the persistence hook, and put that suite inside the gate</name>
  <files>frontend/src/hooks/useDraftPersistence.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - frontend/src/hooks/useDraftPersistence.test.tsx (the existing harness — how a write is driven and how persist state is asserted)
    - frontend/src/hooks/useDraftPersistence.ts lines 590-670 and 960-980 (the create branch, its `try`/`catch`, `refusalOf`, `haltedRef`, and the note that nothing clears the halt)
    - scripts/vitest-count-gate.cjs — the `TARGETS` array and the 188-01 comment block recording the two-knob rule for suites outside the covered directories
  </read_first>
  <action>
    Add the case at the HOOK's own level, where the failure mode actually lives: supply an
    `onDraftCreated` that throws synchronously, drive a first write, and assert (a) the created id is
    still installed, (b) the persist state is NOT an error/refusal, and (c) the write loop is not
    halted — a subsequent write still issues. Add a second case where `onDraftCreated` returns a
    rejected promise and assert the same three properties.

    Write the docblock naming what this guards: `onDraftCreated` LOOKS like a fire-and-forget
    notification and is not — it is called at `:645` inside the write's `try`, whose `catch` at `:656`
    can set `haltedRef` for a terminal refusal, and `:971` records that nothing clears the halt. **No
    shipped test covers this.**

    Then put the suite inside the gate. **MEASURED at HEAD: `TARGETS` has no `src/hooks` entry and
    `BASELINE` has no `useDraftPersistence.test.tsx` key — the gate does not execute this file at
    all.** Add the FILE-level `TARGETS` entry (not a bare `src/hooks` directory entry — the directory
    holds four other suites this phase does not read, and adopting them would make this phase the
    owner of their future rot; the 188-01 comment records exactly that reasoning for
    `src/components/panel/__tests__`) and a `BASELINE` pin taken from the script's own `actual`
    column. Write both reasons beside both entries.
  </action>
  <acceptance_criteria>
    - Two new cases exist (synchronous throw, rejected promise), each asserting id installed + state not error + loop not halted.
    - `node -e "const s=require('fs').readFileSync('scripts/vitest-count-gate.cjs','utf8'); const t=(s.match(/useDraftPersistence.test.tsx/g)||[]).length; process.exit(t>=2?0:1)"` exits 0 (both knobs).
    - `grep -c "\"src/hooks\"" scripts/vitest-count-gate.cjs` == 0 (file-level, not the bare directory).
    - `git diff --numstat frontend/src/hooks/useDraftPersistence.ts` is EMPTY.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` exits 0 with `failed 0` and `useDraftPersistence.test.tsx` listed as pinned.
  </acceptance_criteria>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The one genuinely new failure mode is proved at its own seam, in a suite the gate now actually runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /workflows/generate` | Document-derived placeholder names cross into an LLM prompt. |
| browser → `POST /workflows/{id}/template` | Raw file bytes cross into Storage under a user-keyed prefix. |
| bind callback → the autosave write loop | A callback invoked INSIDE the write's `try` can corrupt the save's reading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.1-07-01 | Denial of service / integrity | `onDraftCreated` → `performWrite`'s `catch` (`useDraftPersistence.ts:656`) | mitigate | The bind is `void`-ed into an `async` function that catches everything internally; `useDraftPersistence.ts` is not edited. Task 3 plants a synchronously-throwing bind and asserts the state is not an error and `haltedRef` is untripped — a terminal refusal would freeze autosave for the whole session (`:971`: nothing clears the halt). |
| T-193.1-07-02 | Integrity | an unbound `render_template` draft | mitigate | D-19: the wire and the bind ship in ONE plan. The failure path has its own `role="status"` node whose sentence names the CONSEQUENCE (`no_template_bound` is terminal at run, `phase_types.py:1271`), not merely that an upload failed. |
| T-193.1-07-03 | Tampering | a blind draft sent while the read is in flight | mitigate | D-07: `canDraft` gains one `&&` against the `loading` arm, and the auto-draft one-shot carries the same refusal. D-08 keeps it safe when no file is held — `idle` is never `loading`, so no second conditional exists to get wrong. |
| T-193.1-07-04 | Tampering | two writers on the definition JSONB | mitigate | The backend mints the descriptor and stops; the client writes `assets[]` through the shipped `onTemplateAttached` → `setTemplateAsset` → `saveNow`, which `performWrite`'s single-flight + supersession drain already serialises against Phase 186's `If-Match` token. No new sequencing machinery. |
| T-193.1-07-05 | Information disclosure | the failure sentence | mitigate | `bindFailed` is a two-field object that structurally cannot hold server prose, a status code or an id (the `useWorkflowFork.ts:205` posture); the raw error goes to `console.error`. |
| T-193.1-07-06 | Tampering | prompt injection via placeholder names | **accept (bounded)** | PRE-EXISTING since Phase 103 via `template_asset_id`. Bounded by `_check_grounding_fidelity` server-side AFTER the emit (`workflow_authoring.py:203-230`) and `extra="forbid"`. Pinned by Plan 03; neither introduced nor closed here. |
| T-193.1-07-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed** — and specifically **no toast library**: the repo has zero `sonner` imports and no `ui/toast` (verified in Phase 192), and a notice region is built rather than acquired. |
</threat_model>

<verification>
- The six pre-draft captures are unchanged; only the key-set cases move, and the move is declared.
- `FLAG_OFF_DESCRIBE_MARKUP` and `FLAG_OFF_HEADER_MARKUP` green with zero edits.
- `useDraftPersistence.ts` has an EMPTY diff.
- Count gate green at cap 2 with `useDraftPersistence.test.tsx` newly executed and pinned.
- `tsc -p tsconfig.app.json` reports 33.
</verification>

<success_criteria>
A draft requested with a supplied template is generated from that template's real field names, and
the same bytes are bound to the definition on the first save — so the run fills the template the
author supplied. Neither half can merge without the other, and neither can break the save.
</success_criteria>

<output>
Create `.planning/phases/193.1-template-first-authoring/193.1-07-SUMMARY.md` when done.
</output>
