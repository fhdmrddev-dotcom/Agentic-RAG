---
phase: 194-stop-a-running-workflow
plan: 09
type: execute
wave: 5
depends_on: ["194-01", "194-06"]
files_modified:
  - backend/app/services/run_lifecycle.py
  - backend/tests/test_062_cancel_run.py
  - backend/tests/test_run_lifecycle.py
autonomous: true
requirements: [RUN-01]
validation_rows: [V-09, V-10, V-11, V-12, V-13, V-17, V-18, V-19]
must_haves:
  truths:
    - "A Stop that lands on a worker where the producer task is dead still marks workflow_runs cancelled and terminalizes the interrupted phase — 'stop at ANY point' is true, not qualified."
    - "A Deep run takes the same arm byte-identically; nothing about the Deep cancel path changes."
    - "Step 3b carries NO is_app_shutting_down() gate, and the F2 terminalize still DOES — restart-resumability is unbroken."
    - "A Redis outage cannot fail a Stop; Postgres is the durable cancel record."
    - "Completed phases survive a stop, and no cancel path writes failed or skipped onto a phase row."
  artifacts:
    - path: "backend/app/services/run_lifecycle.py"
      provides: "Step 3b's workflow co-write + the ONE exported composition the DELETE route's no-producer arm also calls"
      contains: "finish_run"
  key_links:
    - from: "backend/app/services/run_lifecycle.py Step 3b"
      to: "backend/app/db/workflows.py::finish_run + cancel_active_phases"
      via: "the anchor read performed BEFORE the shipped 092-03 clear"
      pattern: "active_workflow_run_id"
    - from: "run_lifecycle Step 3b"
      to: "the Deep path"
      via: "a wf_id guard, so active_workflow_run_id IS NULL skips the branch byte-identically"
      pattern: "if wf_id"
---

<objective>
SC#2, and it is this phase's load-bearing backend change. `_cancel_run_internals` Step 3b heals
**only** the `runs` row. With `WORKER_COUNT=2` and a **per-process** `RUN_TASKS` dict, a Stop
landing on the wrong worker takes this arm — **roughly half of all missed Stops, not an edge case** —
so `workflow_runs` stays `active` forever.

⚠ **CONTEXT D-09's stated consequence is measurably WRONG and the correction changes the argument,
so it is stated rather than inherited.** D-09 says *"the thread anchor is never cleared ⇒ the thread
is permanently wedged."* **The anchor IS cleared** — `run_lifecycle.py:249-259`, Phase 092
(092-03 / SC#2, MODE-02), best-effort via `aexec`. The gap is real; the consequence is not. What
actually persists is **worse in a different way**: `workflow_runs.status='active'` forever with **no
anchor pointing at it**, invisible to `find_resumable_runs` (which requires
`t.active_workflow_run_id = wr.id`), so no sweep will ever touch it and it renders as a live run to
anything reading run status without joining the anchor. **An orphaned lie, not a lock.**

Output: Step 3b's workflow co-write, the ONE exported composition plan 194-10's no-producer arm also
calls, and eight validation rows with five fences driven RED.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@.planning/phases/194-stop-a-running-workflow/194-BASELINE.md
@backend/app/api/workflows.py
@CLAUDE.md

**Base commit — ASSERT FIRST.** Dispatched against `d2a3b51b` on `develop`. In a worktree, run
`bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert `d2a3b51b` is the base ancestor.
Tear down only with `bash scripts/teardown-worktree.sh <path>`; never `rm -rf`.

**Serialization:** every test in this plan mocks `asyncpg.Pool` and the supabase builder — **it must
seed NOTHING in the live database**, which is what keeps it parallel-safe under CLAUDE.md rule 4.
⚠ If a case cannot be written without seeding `workflow_runs` / `workflow_phases`, do not write it
here: hand it to the serialized wave (194-12) and say so.

<interfaces>
`backend/app/services/run_lifecycle.py::_cancel_run_internals(*, run_id, status, thread_id, redis, supabase) -> str`
returns one of `"terminal_noop"` / `"task_cancelled"` / `"zombie_healed"`. Step 3b (`:225-291`) does
FOUR things today, in this order:
1. `finalize_run_terminal(...)` → `runs.status='cancelled'` + both mirror ZREMs, atomically
   (`:231-243`), inside its own `try/except` + `logger.exception`.
2. ⚠ **the Phase 092 anchor clear** — `UPDATE threads SET active_workflow_run_id = NULL WHERE id = thread_id`,
   best-effort via `aexec` (`:246-259`). ⚠ RESEARCH cited `:256-268`; PATTERNS measured **`:249-259`**
   with the comment starting `:246` — corrected beside.
3. a SETNX-gated synthetic terminal sentinel + `_emit_terminal(..., reason="zombie_healed")` (`:260-282`).
4. `EXPIRE run:{id} 60` (`:284-288`).

`backend/app/db/workflows.py` (from plan 194-06): `finish_run(pool, run_id, status)` — the
`workflow_runs.status` write + the anchor clear in ONE transaction, idempotent;
`cancel_active_phases(pool, workflow_run_id)` — the RUN-KEYED set-predicate phase terminalize.

**The composition already exists, in the wrong file** — `api/workflows.py:1494-1518`
(`delete_workflow_cascade`): for each in-flight row it calls `_cancel_run_internals` when
`producer_id is not None`, then `publish_cancel_sentinel(redis, wf_id)`, then
`finish_run(pool, wf_id, "cancelled")` **unconditionally**. 194's backend work is largely moving
that composition into the shared writer so all three callers get it.

`run_producer.py:246-277` — the F2 harness terminalize, gated by `is_app_shutting_down()` at
`:254-258`, with the Deep skip stated at `:246-247`: *"Deep runs / continuations
(`active_workflow_run_id is None`) skip this entirely (byte-identical)."*
</interfaces>
</context>

<non_goals>
- ⛔ **Do NOT add `is_app_shutting_down()` to Step 3b.** RESEARCH proves it three independent ways:
  (1) Step 3b is only reachable on explicit cancel intent — its two callers are `DELETE /runs/{id}`
  and `POST /admin/runs/{id}/kill`, neither of which fires during a shutdown, whereas F2 runs on
  ANY producer exit; (2) a shutdown-affected run cannot reach Step 3b at all — the producer still
  writes `runs.status='cancelled'`, so a later DELETE hits **Step 2 `terminal_noop`** with no
  writes; (3) resumability is **already forfeit** at Step 3b, because the shipped 092-03 anchor
  clear runs there unconditionally and `find_resumable_runs` requires the anchor. Adding the status
  write cannot remove resumability that is already gone — it only stops the row lying about itself.
- ⛔ **Do NOT remove or move the gate at `run_producer.py:254-258`**, and do not touch
  `run_producer.py` or `harness_engine.py:1630` at all. That gate is Phase 096-09 / UAT Test 2.
- ⛔ **Do NOT delete the shipped standalone anchor clear.** It covers the case where the anchor is
  set but the `workflow_runs` read fails, and it is the Deep path's no-op. Deleting it is a Deep-path
  behaviour change — forbidden by the deferred list.
- ⛔ **Do NOT insert anything between Step 3a's `publish_cancel_sentinel` and `task.cancel()`**
  (D-085-04). New work goes AFTER Step 3a's `return` or INSIDE Step 3b.
- ⛔ **Do NOT fork a second cancel writer or a second `workflow_runs` status writer** (D-08/D-10).
- ⛔ **Do NOT wire the DELETE route here.** `api/runs.py` belongs to plan 194-10.
- ⛔ No new package.
</non_goals>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Step 3b's workflow co-write + the ONE exported composition (V-09, V-10, V-13, V-17)</name>
  <files>backend/app/services/run_lifecycle.py, backend/tests/test_062_cancel_run.py</files>
  <behavior>
    - **V-09:** on the zombie arm with a thread holding an anchor, `finish_run` is awaited once with
      that `workflow_runs.id` and the status `"cancelled"`.
    - **V-10:** the anchor is READ before the shipped 092-03 clear runs — asserted by call ORDER,
      not by reading the source.
    - **V-17:** `cancel_active_phases` is awaited once with the same `workflow_runs.id`.
    - **V-13:** calling `_cancel_run_internals` twice for the same run is a no-op the second time —
      `finish_run` may be awaited again and neither write nor the discriminator changes.
    - The returned discriminator is still `"zombie_healed"`, and the shipped Redis ops (SETNX,
      `_emit_terminal`, EXPIRE) still fire.
  </behavior>
  <read_first>
    - `backend/app/services/run_lifecycle.py` — **the whole file (299 L)**: `finalize_run_terminal` `:117-155` (the co-write discipline D-10 mirrors) and `_cancel_run_internals` `:158-291` with all four Step-3b arms and their per-op `try/except`
    - `backend/app/api/workflows.py:1478-1518` — `delete_workflow_cascade`, **the composition already written**, with its own comment naming the `RUN_TASKS`-is-keyed-by-the-producer-id rule
    - `backend/app/db/workflows.py` — `finish_run`, `cancel_active_phases`, `get_active_phase` (from plan 194-06)
    - `backend/tests/test_062_cancel_run.py:60-79` (the supabase builder mocks) and `:141-174` (`test_internals_zombie_heal_routes_finalize_run_terminal` — **the exact template to extend**)
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *G-B: the zombie arm — what is actually missing*, incl. the three properties table and the five-axis `finish_run` safety check
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 2 and § S2, § S3, § S4
  </read_first>
  <action>
Inside Step 3b, **before** the shipped 092-03 anchor clear, read the thread's
`active_workflow_run_id`. ⚠ **The order is load-bearing:** `finish_run` keys its own anchor clear on
`active_workflow_run_id = $1`, so if the standalone clear ran first the id is gone and the
`workflow_runs` row is unreachable. Wrap the read in its OWN `try/except` + `logger.exception`, per
D-062-13 — *"Postgres `runs.status` is the durable cancel record"*; a workflow-side failure must
never fail the cancel.

If a `wf_id` was found, call a **new module-level helper in this file** — one composition, two
callers — that awaits `finish_run(pool, wf_id, "cancelled")` and then
`cancel_active_phases(pool, wf_id)`. Export it (add to `__all__`) so plan 194-10's no-producer arm
calls the SAME composition rather than re-composing it; that is D-08/D-10's "one mechanism" applied
to the server side, and it is why `delete_workflow_cascade`'s version is the shape being moved
rather than copied. Give it its own `try/except` + `logger.exception` and let the discriminator
return regardless.

**Keep the shipped standalone anchor clear where it is**, unchanged — it is the belt-and-braces arm
for the `wf_id`-is-None case and the Deep path's no-op.

Use the file's shipped **late-import** discipline (`# noqa: PLC0415`) for `finish_run` /
`cancel_active_phases`; this module already late-imports from `app.api.threads`,
`app.dependencies`, `app.services.ask_user_service` and `app.utils.db`. The pool is already in hand
at `:237-238`.

⚠ **Record, in a comment beside the co-write, the benign cross-worker interleave** RESEARCH named
rather than guarded: a Stop on worker A (Step 3b → `finish_run`) while worker B's producer is
mid-F2 can interleave two identical `UPDATE … SET status='cancelled'` writes. Both write the SAME
value; row-level locking serialises them; the anchor clear is idempotent. **Benign by
value-identity, not by exclusion** — and the one thing a caller must never do is make the two writes
disagree. Write that sentence on ONE line so it stays grep-able.

⚠ **Also record, beside D-09's original wording, the correction this plan rests on** — the anchor IS
cleared (092-03, `:249-259`), so the missing piece is only `workflow_runs.status`, and the true
consequence is an orphaned row invisible to `find_resumable_runs` rather than a wedged thread.

**⚠ DRIVE F-2 RED (V-10).** Plant, in production source: move the anchor READ **below** the existing
`UPDATE threads SET active_workflow_run_id = NULL` block → the V-10 ordering assertion must RED
because `wf_id` is then `None` → revert → assert `run_lifecycle.py` restores **md5-identical**.
Record the planted line, the failing output and the md5.

Extend `test_062_cancel_run.py` with the five `<behavior>` cases, using `:141-174` as the template.
⚠ **Mock at the DATA-ACCESS layer** — the file's own docblock: *"Mocking matches the DATA-ACCESS
layer (MEMORY lesson, Phase 146) … We do NOT assert against the stale supabase `runs.update` layer
the 145-03 co-write refactor retired."* A fence asserting on a supabase `.update()` would be
asserting against a retired layer.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_062_cancel_run.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All five cases pass and every shipped case in `test_062_cancel_run.py` is green with **no
      assertion edited** (`git diff` shows additions only).
    - V-10 is asserted by **call ORDER** on the mocks (the anchor read precedes the standalone
      clear), never by a source grep.
    - The composition is a single exported helper in `run_lifecycle.py`, and `grep -c "finish_run("
      backend/app/services/run_lifecycle.py` shows it called from exactly ONE place in this module.
    - Every new operation has its OWN `try/except` + `logger.exception`, matching the four shipped
      Step-3b blocks.
    - `grep -c "is_app_shutting_down" backend/app/services/run_lifecycle.py` returns **0**.
    - `git diff --numstat -- backend/app/services/run_producer.py backend/app/services/harness_engine.py`
      is **empty**.
    - The shipped standalone anchor clear is still present and unchanged.
    - **The F-2 RED observation is recorded**: the planted line, the failing assertion output, and
      the post-revert md5.
    - `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
  </acceptance_criteria>
  <done>A Stop that misses the producer now terminalizes the workflow run AND its interrupted phase through one exported composition, the anchor is read before the clear (proved by order and by a RED plant), and the shipped arms are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The scope fences — Deep byte-identity, the shutdown-gate scope (TWO plants), Redis best-effort, sentinel ordering (V-11, V-12; F-3, F-4, F-11, F-12)</name>
  <files>backend/tests/test_run_lifecycle.py, backend/app/services/run_lifecycle.py</files>
  <behavior>
    - **V-11 / F-3:** a Deep run (`threads.active_workflow_run_id IS NULL`) takes Step 3b with the
      workflow branch NOT entered: `finish_run` and `cancel_active_phases` are awaited **zero**
      times, `finalize_run_terminal` once, the shipped anchor clear once, and the discriminator is
      still `"zombie_healed"`.
    - **V-12 / F-4 arm (a):** Step 3b carries **no** `is_app_shutting_down()` gate — the workflow
      co-write fires even when the shutdown flag is set.
    - **V-12 / F-4 arm (b):** the F2 terminalize in `run_producer.py` **still does** carry the gate.
    - **F-11:** with every Redis op raising, `_cancel_run_internals` still returns `"zombie_healed"`
      and the Postgres writes still happen.
    - **F-12:** on the happy arm, `publish_cancel_sentinel` is awaited BEFORE `task.cancel()`.
  </behavior>
  <read_first>
    - `backend/tests/test_run_lifecycle.py` — the whole file, for its fixtures and house style
    - `backend/app/services/run_producer.py:240-290` — the F2 block, the `is_app_shutting_down()` gate at `:254-258`, and the Deep skip comment at `:246-247`
    - `backend/app/services/harness_engine.py:92-94` — `is_app_shutting_down`
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *Does the zombie co-write need the `is_app_shutting_down()` gate?* (the three proofs) and § *The fences* rows **F-3, F-4, F-11, F-12**
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § S2 and § S4
  </read_first>
  <action>
Add the five `<behavior>` fences to `backend/tests/test_run_lifecycle.py`.

⚠ **F-4 NEEDS TWO PLANTS, DRIVEN SEPARATELY, AND THIS IS THE 193.2 LESSON APPLIED.** A fence
asserting only that Step 3b lacks the gate leaves the OTHER arm — that F2 still HAS it — completely
undefended, and a phase that broke restart-resumability would ship green. Write **two independent
assertions** and drive **two independent plants**:
- (a) add `and not is_app_shutting_down()` to Step 3b's workflow branch in
  `run_lifecycle.py` → the arm-(a) assertion must RED → revert → md5-identical.
- (b) delete the gate from `run_producer.py:254-258` → the arm-(b) assertion must RED → revert →
  md5-identical. ⚠ `run_producer.py` is otherwise out of scope: this is a **plant**, reverted in the
  same task, and the revert must be proved by md5, not by intention.

⚠ **F-3's plant is the SCOPE proof, not a formality:** remove the `if wf_id:` guard so the workflow
branch runs unconditionally → the Deep-run case must RED → revert → md5-identical. Without this
plant, "the Deep path is byte-identical" is a claim.

**F-11:** make one Redis op inside Step 3b raise **outside** its `try` and confirm the
204/`"zombie_healed"` contract still holds (D-062-13). If the current structure makes that
impossible to plant honestly, say so and assert the property with every Redis op raising **inside**
its try instead — and record which form was used and why. A fence whose plant cannot be constructed
is a fence whose scope is wrong.

**F-12:** assert the ordering on the happy arm by call sequence on the mocks; plant by swapping the
two statements at `run_lifecycle.py:206-217` → observe RED → revert → md5-identical. D-085-04: the
sentinel is published FIRST so a paused `_handle_ask_user` wakes and returns a normal `ToolResult`
before `CancelledError` propagates. ⚠ **This is `BUG-260808-02`'s folded half** — stopping a run
that is waiting at an approval — so the fence is the evidence that fold is honoured.

⚠ **Record, beside CONTEXT's warning, the measured reason Step 3b must NOT carry the gate.** CONTEXT
says *"any new terminalize must carry the same gate or it will break restart-resumability."* That is
true of F2 and **false of Step 3b**, for the three reasons in RESEARCH. Write them into the fence's
docstring so the next reader finds the argument rather than re-deriving it — and so nobody "fixes"
the fence by adding the gate.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_run_lifecycle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All five fences pass on the unmodified tree.
    - **Five RED observations recorded** — F-3, F-4(a), F-4(b), F-11, F-12 — each with the planted
      file and line, the failing assertion output, and the post-revert md5 proving the production
      file was restored byte-identical. ⚠ F-4's two plants are recorded **separately**; one RED
      covering both is not acceptable.
    - The F-4 arms are **two distinct assertions** in the test file, not one compound assertion —
      verified by reading the test, and by the fact that plant (a) reds only arm (a) and plant (b)
      reds only arm (b).
    - The Deep case asserts `finish_run` and `cancel_active_phases` await counts of **0**, and
      `finalize_run_terminal` of 1.
    - `git diff --numstat -- backend/app/services/run_producer.py` is **empty at task end** (the
      plant was reverted), and the md5 proves it.
    - The fence docstring carries RESEARCH's three reasons Step 3b must not carry the gate.
    - `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
  </acceptance_criteria>
  <done>The Deep path, the shutdown-gate scope (both arms, separately), the Redis best-effort contract and the ask_user sentinel ordering are all fenced, and every fence has been watched to FAIL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The phase-honesty fences — completed untouched, and never the failed/skipped vocabulary (V-18, V-19; F-5, F-6)</name>
  <files>backend/tests/test_062_cancel_run.py</files>
  <behavior>
    - **V-18 / F-5:** on the cancel path, phase rows whose status is `completed` (and `failed`,
      `skipped`, `recorded_not_sent`) are NOT written — the terminalize's predicate matches only
      `active`.
    - **V-19 / F-6:** no cancel path composes a `workflow_phases` write whose status VALUE is
      `failed` or `skipped`; the only value written is the migration-119 slug.
  </behavior>
  <read_first>
    - `backend/app/db/workflows.py` — `cancel_active_phases`, `cancel_phase`, and the five shipped phase writers, so the fence knows exactly which values are legitimate
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *The fences* rows **F-5** and **F-6** — and F-6's scoping warning
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § S6's second scoping lesson
    - `.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md` D-04, D-07, D-13
    - `supabase/migrations/119_workflow_phases_cancelled.sql`
  </read_first>
  <action>
Add both fences to `backend/tests/test_062_cancel_run.py`, asserting on the SQL and the binds handed
to the mocked pool.

**F-5 / V-18** — assert the terminalize's predicate restricts to `status = 'active'`, and assert
behaviourally that a run whose phases are `[completed, active, pending]` produces a write matching
only the `active` row. **Plant:** change the predicate from `status='active'` to
`status IN ('active','completed')` in `backend/app/db/workflows.py` → observe RED → revert →
md5-identical.

**F-6 / V-19** — ⚠ **SCOPE THE FENCE OVER THE COMPOSED SQL / VALUE, NOT THE MODULE SOURCE.** This is
the 193.2 F-3 lesson, and it applies directly: `db/workflows.py`'s docblocks legitimately name
`failed` and `skipped` (the five shipped writers live there and D-04's rejection is recorded there
in prose), so a raw source sweep would red on the prose documenting the rule. Assert instead that
the status value inside the composed cancel-path `UPDATE` is neither `failed` nor `skipped`, and is
byte-identical to migration 119's new literal. **Plant:** change `'cancelled'` to `'failed'` in the
new phase writer → observe RED → revert → md5-identical. ⚠ Then plant `'skipped'` **separately** and
observe RED again — a fence proved against one wrong value has not been proved against the other,
and D-04 rejected BOTH by name.

⚠ **Record the honesty argument in the fence docstring**, so the fence carries its own reason: the
phase did not fail and was not skipped — it ran and was interrupted; reusing either word was
OFFERED AND REJECTED (D-04) in a phase whose entire requirement is honesty.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_062_cancel_run.py tests/test_workflow_phase_cancel.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both fences pass on the unmodified tree.
    - **Three RED observations recorded** — the widened predicate, the `'failed'` value, and the
      `'skipped'` value — each with the planted line, the failing output and the post-revert md5.
      ⚠ The `'failed'` and `'skipped'` plants are recorded **separately**.
    - F-6 is scoped over the composed value, **not** the module source — demonstrate this by
      confirming the fence stays GREEN with `db/workflows.py`'s existing `failed` / `skipped`
      docblock prose in place (which it is), and state that this was checked rather than assumed.
    - The written slug is compared byte-for-byte against migration 119's new literal by grepping
      both files, never by eye.
    - `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
  </acceptance_criteria>
  <done>A stop cannot repaint a completed phase and cannot enter the failed or skipped vocabulary, and both fences have been watched to FAIL against three separate real plants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cancel request → `public.workflow_runs` / `public.workflow_phases` | ⚠ The workflow cluster writes through a **service-role pool that BYPASSES RLS**; ownership is enforced by the CALLER, never here |
| `_cancel_run_internals` → Redis | Best-effort mirror ops that must never fail the cancel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-09-01 | Elevation of Privilege | `_cancel_run_internals` | transfer | This helper deliberately performs **NO ownership check** — the caller owns that decision (T-147-06; the privileged skip lives only inside the `require_operator` router). Plan 194-10 adds the owner-scoped, anchor-confirmed gate for the new id shape. Documented at the helper, not assumed. |
| T-194-09-02 | Tampering | `workflow_runs` / `workflow_phases` | mitigate | All writes go through `finish_run` / `cancel_active_phases` with `$N` binds; no f-string reaches SQL. The `AND status='active'` predicate is fenced by F-5 against a widened-predicate plant. |
| T-194-09-03 | Denial of Service | a legitimate Stop | mitigate | Every new op has its own `try/except` + `logger.exception`; Postgres is the durable cancel record (D-062-13). F-11 proves a Redis outage still returns `"zombie_healed"`. |
| T-194-09-04 | Information Disclosure | error paths | mitigate | `logger.exception` only; no run content, prompt or provider payload is logged, and no stack trace reaches the client (T-062-03). |
| T-194-09-05 | Repudiation | restart-resumability | mitigate | F-4's **two** plants prove Step 3b carries no shutdown gate AND that F2 still does. Breaking either would silently terminalize runs the boot sweep should have resumed (Phase 096 UAT Test 2). |
| T-194-09-06 | Tampering | a paused approval | mitigate | F-12 pins `publish_cancel_sentinel` BEFORE `task.cancel()` (D-085-04) so a paused prompt cannot strand as a submittable-but-dead card — `BUG-260808-02`'s folded half. |
| T-194-09-SC | Tampering | pip installs | mitigate | None owed — no package is added. |
</threat_model>

<verification>
- `pytest tests/test_062_cancel_run.py tests/test_run_lifecycle.py tests/test_cancel_run.py tests/test_workflow_phase_cancel.py -q` passes.
- `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
- `git diff --numstat -- backend/app/services/run_producer.py backend/app/services/harness_engine.py backend/app/api/runs.py` is **empty**.
- `grep -c "is_app_shutting_down" backend/app/services/run_lifecycle.py` returns 0.
- **Nine RED observations recorded across the plan** (F-2; F-3; F-4a; F-4b; F-11; F-12; F-5;
  F-6/`failed`; F-6/`skipped`), each with a post-revert md5.
- ⚠ **Measure this file for the hot-file ledger.** `run_lifecycle.py` is not a ledger row; derive
  `git log --oneline -- backend/app/services/run_lifecycle.py | wc -l`, the bucket recipe with
  non-phase buckets subtracted and NAMED, and `wc -l`, and hand the triple to plan 194-13. *A hot
  file missing from the table is permanently invisible to its own guardrail.*
</verification>

<success_criteria>
- V-09, V-10, V-13, V-17 green: the zombie arm co-writes `workflow_runs` and terminalizes the
  interrupted phase, reading the anchor before the shipped clear, idempotently.
- V-11, V-12 green with **four** separate plants: the Deep path is byte-identical and the shutdown
  gate's scope is pinned on both arms.
- V-18, V-19 green with three separate plants: completed phases survive and no cancel writes the
  `failed` or `skipped` vocabulary.
- One exported composition, callable by plan 194-10 — no second cancel writer.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-09-SUMMARY.md` when done.
</output>
