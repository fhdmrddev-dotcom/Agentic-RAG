---
phase: 194-stop-a-running-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/194-stop-a-running-workflow/194-BASELINE.md
  - .planning/phases/194-stop-a-running-workflow/194-MEASUREMENTS.md
  - .planning/phases/194-stop-a-running-workflow/194-UAT.md
autonomous: false
requirements: [RUN-01]
validation_rows: []
must_haves:
  truths:
    - "Every gate this phase is scored against has a figure measured at this phase's own base commit, not inherited from RESEARCH."
    - "The duplicate-assistant-icon symptom has a MEASURED verdict (174-04 race vs BUG-260609-03 class) or a recorded deferral with a trigger — never an assumption."
    - "The 8-row cross-provider UAT scoreboard exists with its roster DERIVED from MODEL_CAPABILITIES, never re-typed."
  artifacts:
    - path: ".planning/phases/194-stop-a-running-workflow/194-BASELINE.md"
      provides: "The four re-derived gate baselines + the six G-5 figures, each with the command that produced it"
      contains: "count gate"
    - path: ".planning/phases/194-stop-a-running-workflow/194-MEASUREMENTS.md"
      provides: "The duplicate-icon bucket measurement and its routing verdict"
      contains: "duplicate"
    - path: ".planning/phases/194-stop-a-running-workflow/194-UAT.md"
      provides: "The 8-row cross-provider scoreboard + the 4-axis rows + the four G-4 lived-experience rows"
      contains: "MODEL_CAPABILITIES"
  key_links:
    - from: "194-BASELINE.md"
      to: "backend/app/config.py MODEL_CAPABILITIES"
      via: "the roster derivation command, executed not transcribed"
      pattern: "MODEL_CAPABILITIES"
---

<objective>
Wave 0. Re-derive every baseline this phase will be scored against at **this phase's own base
commit**, measure the one thing RESEARCH could not (the duplicate-assistant-icon root cause,
assumption A1, MEDIUM confidence), and author the UAT scoreboard SC#10 requires.

Purpose: this repository's figures rot fast — the `WorkflowsPage.tsx` ledger cell has gone stale
four consecutive times and once **within a single day**. RESEARCH measured at `05f664a0`;
PATTERNS at `0dcb4a8e`; this phase runs at `d2a3b51b`. A figure inherited across three commits is
a claim, not a measurement.

Output: `194-BASELINE.md`, `194-MEASUREMENTS.md`, `194-UAT.md`. **No source file is touched.**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-VALIDATION.md
@.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@CLAUDE.md

**Base commit — ASSERT BEFORE ANY OTHER ACTION.** This plan is dispatched against `d2a3b51b` on
branch `develop`. Worktrees in this repository have forked from the WRONG base 12/12 in Phase 192
and again on a single worktree on 2026-08-14. If this plan runs in a worktree, the FIRST command
is `bash scripts/bootstrap-worktree.sh "$(pwd)"` (idempotent, fails loudly), and the SECOND is a
`git rev-parse HEAD` assertion that the base ancestor is `d2a3b51b`. Never `rm -rf` a worktree —
tear down only with `bash scripts/teardown-worktree.sh <path>`.
</context>

<non_goals>
- ⛔ No source file, test file, migration or schema artifact is modified by this plan.
- ⛔ Mount 4 (`frontend/src/pages/WorkflowsPage.tsx`, `frontend/src/components/workflows/library/`)
  is DESCOPED (D-16) and is not measured, mapped or mentioned as future work in any artifact here
  beyond its recorded trigger.
- ⛔ No run-history surface (BUG-260815-03 → Phase 195, D-15).
- ⛔ No fix is attempted for the duplicate assistant icon in this plan. This plan MEASURES; the
  routing verdict decides whether a later plan fixes it or it is deferred with a trigger.
</non_goals>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the four gate baselines and the six G-5 figures at d2a3b51b</name>
  <files>.planning/phases/194-stop-a-running-workflow/194-BASELINE.md</files>
  <read_first>
    - `.planning/phases/194-stop-a-running-workflow/194-VALIDATION.md` § *Baselines measured at `05f664a0` — RE-DERIVE at Wave 0* and § *Test Infrastructure* (the exact commands)
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *Answers to the frontend questions → A. G-5 re-verification* (the three commands per file and the `streaming` bucket subtraction)
    - `CLAUDE.md` § *Parallel execution* (the `GSD_VITEST_MAX_WORKERS=2` rule and why the cap ROTTED from 4)
    - `CLAUDE.md` hot-file ledger, the `backend/app/db/workflows.py` row (the shape of a re-derivable figure)
  </read_first>
  <action>
Create `194-BASELINE.md`. Record, each with the exact command that produced it and its raw output
line:

(a) **Frontend count gate** — `cd frontend && GSD_VITEST_MAX_WORKERS=2 node ../scripts/vitest-count-gate.cjs`.
Record the **verdict line verbatim**, never a summary: the `count gate` word, total, failed, pinned
total, and the `N/N pinned files present` figure. ⚠ A GROWING total is the gate WORKING — its
contract is *no per-file decrease* and *zero failing*, never a fixed grand total. State that
sentence in the file so a later reader who sees a larger number does not read it as a regression.
⚠ Cap 2 is NOT deterministic (193.2 measured `failed` 4, 11 and 6 on three consecutive runs of an
unchanged tree): if `failed` is non-zero, **capture the failing FILENAMES before re-running
anything**, then re-run once and record both readings side by side.

(b) **Typecheck** — `cd frontend && npx tsc -p tsconfig.app.json --noEmit`, error count. ⚠ A bare
`--noEmit` without `-p tsconfig.app.json` checks ZERO files; the `-p` flag is load-bearing.
Expect the documented, unmoved baseline of 33.

(c) **Cancel-path pytest** — `cd backend && venv/Scripts/python.exe -m pytest tests/test_062_cancel_run.py tests/test_cancel_run.py tests/test_run_lifecycle.py tests/test_migration_115.py -q`. Record pass/fail.

(d) **Backend unit rot baseline** — `cd backend && venv/Scripts/python.exe -m pytest tests/unit -q`.
Record the count **and the full list of failing test NAMES**, because every later comparison in this
phase is by NAME, never by count (SEED-056 / SEED-165).

(e) **The six G-5 figures**, for `frontend/src/components/chat/RunCard.tsx` and
`frontend/src/components/panel/WorkspacePanel.tsx`: `git log --oneline -- <file> | wc -l`; the
standard bucket recipe `git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | sort -u`;
and `wc -l <file>`. ⚠ Subtract non-phase buckets explicitly and NAME them — RESEARCH measured
`RunCard.tsx`'s ninth bucket to be `streaming` (an untagged `fix(streaming)` + `revert(streaming)`
075.x follow-up pair), so the PHASE count is 8 while the raw recipe prints 9. Record both, the
loser beside the winner. Also run `grep -o "RunCard.tsx" CLAUDE.md | wc -l` and
`grep -o "WorkspacePanel.tsx" CLAUDE.md | wc -l` and record the results — CONTEXT D-01 claims both
names "occur only inside other rows' prose" and RESEARCH measured both to be **0**.

Every figure that differs from RESEARCH's or PATTERNS' value is recorded **beside** the earlier
value with the earlier commit named, never over it. That is this project's standing habit and it is
the entire point of this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/194-stop-a-running-workflow/194-BASELINE.md &amp;&amp; grep -q "count gate" .planning/phases/194-stop-a-running-workflow/194-BASELINE.md &amp;&amp; grep -q "tsconfig.app.json" .planning/phases/194-stop-a-running-workflow/194-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `194-BASELINE.md` exists and contains all five measurement groups (a)-(e), each with its
      command and its raw output line.
    - The count-gate entry quotes the **verdict line**, and carries the sentence that a growing
      total is the gate working.
    - The typecheck command in the file includes `-p tsconfig.app.json`.
    - The backend-unit entry lists failing test NAMES, not only a count.
    - Both G-5 files have three figures each, and the non-phase bucket subtraction is NAMED (not
      merely applied).
    - Every figure that moved since `05f664a0` or `0dcb4a8e` is recorded beside the earlier value
      with the earlier commit named — `grep -c` for the word "beside" or an explicit "was N at
      &lt;sha&gt;" form returns ≥ 1 where any figure moved. If NO figure moved, the file says so
      explicitly ("re-derived at `d2a3b51b`; all figures unmoved") rather than silently omitting it.
  </acceptance_criteria>
  <done>Every gate and every G-5 figure this phase will be scored against has a value measured at `d2a3b51b`, with its command, and every drift from RESEARCH/PATTERNS is recorded beside the original.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: MEASURE the duplicate-assistant-icon root cause (RESEARCH A1) — do not assume it</name>
  <files>.planning/phases/194-stop-a-running-workflow/194-MEASUREMENTS.md</files>
  <read_first>
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *The duplicate assistant icon — prior art checked, and it is NOT the one the report names* and Assumptions Log A1
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 10 (the banner mechanism and the prior-art pointers)
    - `frontend/src/lib/dedupMessages.ts:1-50` (the runId-dedup exemption for harness rows, and the 174-04 narrow collapse)
    - `frontend/src/providers/StreamsProvider.tsx:2485-2505` (BUG-260609-03: harness leaves `runs.message_id` NULL)
    - `.planning/reported-bugs/chat-stuck-on-starting-workflow-with-duplicate-icon.md`
  </read_first>
  <what-built>
Nothing is built. RESEARCH's A1 is a MEDIUM-confidence hypothesis — that the stuck banner and the
duplicate assistant icon share one root (`runs.message_id` is NULL for a harness run, so the chat
surface has no runId-keyed identity for a harness assistant row). The report's own named prior art
(`toolcallpanel-dedup-duplicates-tool-card.md`, BUG-260521-01) was measured to be the WRONG one —
a transient tool-card duplicate, different symptom, different surface, different lifetime. RESEARCH
specified the cheapest disproof and it requires a live run, which no automated test can drive.
  </what-built>
  <how-to-verify>
1. Start the local stack (`powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1`)
   and the backend + frontend dev servers. Open the app at `http://localhost:5173`.
2. Launch a real workflow (harness) run from the Workflows page and let it reach at least phase 2.
3. While it is streaming, in the browser console dump the chat bucket for that thread:
   read `useStreamsStore.getState().bucketsBySurface.get("chat").get("<threadId>")` and list, for
   every `role === "assistant"` row: its `id` (note whether it starts with `temp-`), its `runId`
   (present or `undefined`), and its `runStatus`.
4. **The verdict is mechanical, not judgemental:**
   - **TWO `temp-` rows** with `runStatus:"streaming"` ⇒ the **174-04 mount/first-SSE race**
     (`dedupMessages.ts:31-45`).
   - **ONE `temp-` row and ONE persisted row** ⇒ the **BUG-260609-03 class** (`runs.message_id`
     NULL ⇒ `dbRunIds` never holds the run_id ⇒ the cached temp and the DB copy both render).
   - **Exactly ONE assistant row** ⇒ the duplicate icon does **not** reproduce at HEAD; record that
     as the measurement and say so.
5. Paste the row dump back into this checkpoint.
  </how-to-verify>
  <action>
Write `194-MEASUREMENTS.md` recording: the dumped assistant rows verbatim, the mechanical verdict
from step 4, and the **routing decision** it forces:

- If the verdict is the 174-04 race or the BUG-260609-03 class and the fix is ≤ 1 file / ≤ 10 lines
  with no schema or API surface, route it to `/gsd:fast` under G-3 and record that — a 5-line fix
  is not a plan.
- If the fix is larger, record it as **DEFERRED with a written trigger** and update
  `.planning/reported-bugs/chat-stuck-on-starting-workflow-with-duplicate-icon.md` accordingly in
  plan 194-03. The report's own `re_open_trigger` already anticipates this: *"Re-open if 194 closes
  only the banner-advance half."*
- If it does not reproduce, record the non-reproduction with the commit and the date — that is a
  measurement, not an omission.

⚠ Also record the correction RESEARCH made and this file inherits: the report's named prior art is
the wrong one, and the correct prior art is `dedupMessages.ts:20-26`, `StreamsProvider.tsx:2490-2500`
(BUG-260609-03) and `dedupMessages.ts:31-45` (BUG-260610-01). Record it **beside** the report's
original pointer, never over it.
  </action>
  <verify>
    <automated>test -f .planning/phases/194-stop-a-running-workflow/194-MEASUREMENTS.md &amp;&amp; grep -qiE "174-04|260609-03|does not reproduce" .planning/phases/194-stop-a-running-workflow/194-MEASUREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `194-MEASUREMENTS.md` exists and contains the verbatim assistant-row dump (id / runId /
      runStatus per row), not a paraphrase.
    - It states ONE of the three mechanical verdicts and names the evidence that selected it.
    - It carries an explicit routing decision (fast-fix / deferred-with-trigger / does-not-reproduce)
      — the word "probably" appears nowhere in the verdict.
    - The prior-art correction is recorded beside the report's original pointer.
  </acceptance_criteria>
  <done>The duplicate-icon symptom has a mechanical verdict backed by a verbatim row dump, and an explicit routing decision (fast-fix / deferred-with-trigger / does-not-reproduce) — never an assumption.</done>
  <resume-signal>Paste the assistant-row dump, or type "does not reproduce" with the commit you tested at.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author 194-UAT.md — the 8-row roster DERIVED, plus the 4 axes and the G-4 rows</name>
  <files>.planning/phases/194-stop-a-running-workflow/194-UAT.md</files>
  <read_first>
    - `CLAUDE.md` § *UAT scoreboard recipe (MANDATORY)* — the 4-axis bandwidth table, the cross-provider roster rule, and the "rows may be blocked, but never silently omitted" rule
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *UAT scoreboard — SC#10 fires* (the derivation command, verbatim, and the G-4 lived-experience rows incl. the negative row)
    - `.planning/phases/194-stop-a-running-workflow/194-VALIDATION.md` § *Manual-Only Verifications*
  </read_first>
  <action>
Create `194-UAT.md`.

**Derive the roster; never re-type it.** Execute RESEARCH's command verbatim from
`194-RESEARCH.md` § *UAT scoreboard* — it groups `MODEL_CAPABILITIES` by `provider` and prints one
representative per group. Paste the raw output into the file above the table, then build the table
from that output. Prefer the **newest, registry-backed** id per provider: an id absent from
`MODEL_CAPABILITIES` resolves `capability_source=inferred` and silently loses `emit_tier`, so a row
driven on such an id measures a weaker configuration than the one that ships (SEED-040, SEED-135).

The scoreboard is **8 rows**, one per provider group. Each row: provider · model id ·
`native_tools` · `emit_tier` · the Stop mount driven · verdict (⬜ pending / ✅ / ⛔ blocked) ·
evidence (the `workflow_runs` / `workflow_phases` / `harness_audit` rows read). ⚠ A row with no
funded key or one blocked by a known defect is recorded **⛔ with the reason and the blocking issue
id** — never dropped. A scoreboard listing only what passed is not a scoreboard.

Add the three remaining axes as their own rows: **multi-tool** (stop a run mid-`execute_code` or
mid-`search_documents`), **parallel-thread** (thread A's workflow streaming while thread B accepts a
prompt; Stop A; assert B unaffected and B's composer never locked), **long-message** (≥ 50 prior
messages OR a ≥ 5 KB prompt).

Add the four **G-4 lived-experience rows** from RESEARCH, including the negative one, which is the
row that would have caught the id landmine: **reload the page mid-run** (so the mount reconcile
overwrites `workflowLock.runId` with the `workflow_runs.id`) **and then press Stop** — before the
fix this is a silent no-op; after it, it stops. Drive with Chrome MCP; ⚠ `take_screenshot` times
out on this box, so read DOM geometry via `evaluate_script`.

Record the **cheapest honest method** (Phase 185, re-recommended): drive each row as a real run with
a **per-request** `model` + `provider` on `POST /threads/{id}/messages`, then Stop it and read the
verdict from the DB. That scores the whole board **without mutating any global setting**, so the
operator's environment is untouched and rows cannot contaminate each other.

⚠ **These rows are authored here and driven at phase verification — they are NOT tasks in any
PLAN.md.** State that in the file so a later reader does not go looking for them in a plan.
  </action>
  <verify>
    <automated>grep -q "MODEL_CAPABILITIES" .planning/phases/194-stop-a-running-workflow/194-UAT.md &amp;&amp; test $(grep -cE "^\| *(anthropic|deepseek|google|minimax|moonshot|openai|openrouter|zhipu)" .planning/phases/194-stop-a-running-workflow/194-UAT.md) -ge 8</automated>
  </verify>
  <acceptance_criteria>
    - The raw output of the `MODEL_CAPABILITIES` derivation command is pasted into the file above
      the table (proving derivation, not transcription).
    - The scoreboard has **8 provider rows** and the automated grep above returns ≥ 8.
    - Multi-tool, parallel-thread and long-message each have at least one row.
    - All four G-4 rows are present, including the reload-then-Stop negative row.
    - The file states that blocked rows are recorded ⛔ with a reason and a blocking id, never omitted.
    - The file states that these rows are not PLAN.md tasks.
  </acceptance_criteria>
  <done>`194-UAT.md` carries a derived 8-row roster, all four axes and the four G-4 rows, with the no-global-mutation driving method recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → live local app | The Task-2 measurement drives a real run against real data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-01-01 | Information Disclosure | the pasted console dump in `194-MEASUREMENTS.md` | mitigate | Record row `id` / `runId` / `runStatus` ONLY. No message `content`, no auth header, no provider key may enter the artifact. |
| T-194-01-02 | Tampering | the UAT driving method | mitigate | Per-request `model` + `provider` on `POST /threads/{id}/messages`; **no global setting is mutated**, so the operator's environment is untouched and rows cannot contaminate each other. |
| T-194-01-SC | Tampering | npm/pip installs | mitigate | **None owed.** This phase installs no package (RESEARCH § *Package Legitimacy Audit*). If any plan later proposes one, the Package Legitimacy Gate is owed first — prior art: Phase 192's gap round explicitly REFUSED to acquire `sonner`. |
</threat_model>

<verification>
- `194-BASELINE.md`, `194-MEASUREMENTS.md` and `194-UAT.md` all exist under the phase directory.
- Every later plan in this phase compares its gates to `194-BASELINE.md`, not to RESEARCH's or
  PATTERNS' figures.
- No file outside `.planning/phases/194-stop-a-running-workflow/` is modified — `git status
  --porcelain` shows changes only under that directory.
</verification>

<success_criteria>
- The four gate baselines and the six G-5 figures are measured at `d2a3b51b`, each with its command.
- The duplicate-icon symptom has a mechanical verdict and a recorded routing decision.
- The 8-row cross-provider scoreboard exists with a DERIVED roster and all four axes.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-01-SUMMARY.md` when done.
</output>

