---
phase: 206.1-settings-connections-finishes-the-mcp-story-insert
plan: 02
type: execute
wave: 2
depends_on: ["206.1-01"]
files_modified:
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/connectionsCopy.ts
  - frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
autonomous: false
requirements: [CONN-02]
worktrees: forbidden
gap_closure: false

must_haves:
  truths:
    - "ITEM 2 ONLY — this plan is one of three separable plans per D-206.1-01; it can be reverted without touching item 1 or item 3."
    - "D-206.1-12: the dense shape is a `dense` prop threaded from the SAME `panel && !isMobile` expression that opens the 400px track at `ConnectionsTab.tsx:274` — one condition, one home, two consequences. Container queries are REFUSED: no `@tailwindcss/container-queries` is installed (a second new dependency) and jsdom evaluates no layout, so a container-driven shape would be untestable by the suite that has to pin it."
    - "D-206.1-13: the dense shape REFLOWS and deletes NO cell. All five columns (`Connection`, `Sends to`, `Used by`, `Credential`, `State`) and the action cell are present in BOTH renders, asserted by querying every row testid in both — never by inspection."
    - "D-206.1-20: the dense shape drops the five-word COLUMN HEADER and nothing else, because stacked cells form no columns for a header to head. `CONNECTIONS_COLUMNS` stays exported and stays rendered by the wide shape with its character identity intact, and `Used by` / `Credential` each gain a visible inline label DERIVED from `CONNECTIONS_COLUMNS[2]` / `[3]`."
    - "D-206.1-21: the destination cell must WRAP. `scrollWidth <= clientWidth` is UNSATISFIABLE by widening alone while the cell is `white-space: nowrap` — a dense layout that only gives the cell more room fails SC#2 SILENTLY. `truncate` appears TWICE today (on the cell AND on the inner span) and both must go in the dense variant."
    - "D-206.1-14: SC#2's geometry CANNOT be proven in vitest and this plan does not pretend it can — jsdom returns `scrollWidth 0 / clientWidth 0 / offsetWidth 0`, so the assertion passes VACUOUSLY on markup overflowing by ~400px. The proof is split into a vitest SHAPE half (Task 2) and a real-browser GEOMETRY half (Task 3), and both are required."
    - "D-206.1-21 (second half): 1280px and 1536px are EXPECTED TO AGREE — `SettingsPage.tsx:861` caps this surface at `max-w-3xl` (768px), so the open list track is ~302px at both. An identical pair is CONFIRMATION, not a copy-paste error."
    - "The WIDE render is proved byte-identical to this plan's own base by an `outerHTML` capture committed BEFORE the dense branch lands — the `PhaseNodeCard.tsx` `CARD_HTML_BASELINE` precedent. A pin that passes UNEDITED is evidence; a re-captured one is only a record of what the code now does."
    - "A `title` tooltip is FORBIDDEN. `ConnectionsTab.test.tsx:505-515` asserts ZERO `[title]` nodes with a menu AND a sheet open — wrapping is the answer BECAUSE the tooltip is not available."
    - "D-206.1-02: `frontend/src/lib/api.ts` is NOT edited by this plan."
    - "D-206.1-15 / D-206.1-16 / D-206.1-17: baselines re-derived at this plan's own base, `tsc -p tsconfig.app.json`, and a red gate triaged from its persisted JSON before any re-run."
  artifacts:
    - path: "frontend/src/components/settings/ConnectionsTab.tsx"
      provides: "The `dense` prop threaded from the one condition, and the three-line dense row shape beside the untouched wide one"
      contains: "data-dense"
    - path: "frontend/src/components/settings/connectionsCopy.ts"
      provides: "The two dense inline labels, DERIVED from CONNECTIONS_COLUMNS"
      exports: ["CONNECTIONS_DENSE_LABEL_USED_BY", "CONNECTIONS_DENSE_LABEL_CREDENTIAL"]
    - path: "frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx"
      provides: "The wide-render byte-identity capture, the dense shape's six-testid sweep, the no-truncate assertion, the header-absent assertion"
  key_links:
    - from: "frontend/src/components/settings/ConnectionsTab.tsx:274 (panel && !isMobile)"
      to: "ConnectionRow's dense prop"
      via: "one expression assigned to a const and passed to both the grid style and every row"
      pattern: "dense"
    - from: "connectionsCopy.ts CONNECTIONS_DENSE_LABEL_USED_BY"
      to: "CONNECTIONS_COLUMNS[2]"
      via: "derivation, never a re-typed literal"
      pattern: "CONNECTIONS_COLUMNS\\[2\\]"
---

<objective>
Item 2 of Phase 206.1 — **the dense row shape**. When the 400px panel opens, the list column drops
from ~718px to ~302px and the row collapses into itself: the operator measured two destination cells
truncating at `clientW 93` and `47` against `scrollW 272` and `169`, rendering
`https://fhdmrdautomation.a…` and `slack.com/api · #C0BNK1QCF…`.

Purpose: **the destination is the one column a person reads to approve a send.** The ROADMAP names
hiding it as the failure mode — *"hiding it is worse than truncating it"* — so the fix is a row
SHAPE for the narrow column, not a nudge and not a deleted column.

⚠ **The decisive measurement, which no amount of re-budgeting fixes.** Four cells are
constitutionally unable to shrink (`w-24` + `w-32` + `w-36` + `w-8` = 400px) and the row's
structural overhead claims another 88px, so the non-negotiable budget is **488px** against a **302px**
track. And the destination cell is `truncate` = `white-space: nowrap`, under which `scrollWidth` is
the full un-wrapped text width — so **`scrollWidth <= clientWidth` is UNSATISFIABLE BY WIDENING
ALONE.** The dense shape must let the destination **WRAP**.

Output: an ADDITIVE dense branch behind a prop, a byte-identity proof that the wide render did not
move, and browser-measured geometry at 1280 and 1536.
</objective>

<constraints>
⚠ **WORKTREES ARE FORBIDDEN FOR THIS ENTIRE PHASE.** This plan edits `ConnectionsTab.tsx` and its
suite, which plans 01 and 03 also edit. Waves are serial: 01 → 02 → 03. Do not run
`scripts/bootstrap-worktree.sh`; do not create a worktree.

⚠ **NEVER EDIT `frontend/src/lib/api.ts`.** Phase 207 owns that split.

⚠ **BACKEND IS UNTOUCHED.** No migration, no model, no route.

⚠ **THIS PLAN'S BASE IS PLAN 01'S TIP,** not the phase base. Plan 01 changed the marks inside the
row, so the wide render's DOM has already moved once. Task 1's capture is taken at THIS plan's base
and its only job is to prove the DENSE change did not disturb the wide shape.
</constraints>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-CONTEXT.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-UI-SPEC.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-PATTERNS.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-RESEARCH.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-VALIDATION.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-01-SUMMARY.md
</context>

<interfaces>
<!-- Read out of the codebase at HEAD by the pattern pass. No exploration needed. -->

From `frontend/src/components/settings/ConnectionsTab.tsx:270-276` — the ONE condition:

  <div data-testid="connections-split"
       className="grid min-h-0 min-w-0 gap-4 motion-safe:transition-[grid-template-columns] motion-safe:duration-300"
       style={{ gridTemplateColumns: panel && !isMobile ? "minmax(0,1fr) 400px" : "minmax(0,1fr)" }}>

From `ConnectionsTab.tsx:196-226` — `ConnectionsTabView`'s props interface. ⚠ Every prop carries a
docblock naming what its ABSENCE means (`onCheck?` at `:214`: *"Absent ⇒ the menu item is REMOVED,
never rendered inert"*). Copy that habit for `dense`.

From `ConnectionsTab.tsx:501-519` — `ConnectionRow`'s prop block; `:468-478` — the row call site.

From `ConnectionsTab.tsx:443-464` — the column header, mapping `CONNECTIONS_COLUMNS` with
per-index widths `flex-[2] / flex-[2] / w-24 / w-32 / w-36` plus a trailing `w-8` spacer.

From `ConnectionsTab.tsx:583-595` — the destination cell. ⚠ `truncate` appears TWICE, on the CELL
and on the inner SPAN:

  <div data-testid="connections-row-destination"
       className="flex min-w-0 flex-[2] items-center gap-1.5 truncate font-mono text-[11px] text-muted-foreground">
    <span aria-hidden="true">🔒</span>
    <span className="truncate">{facts.join(" · ")}</span>
    {isSlack && <span className="flex-none rounded border border-border px-1 text-[11px] text-muted-foreground">{CONNECTION_FIXED_TAG}</span>}
  </div>

From `connectionsCopy.ts` — `CONNECTIONS_COLUMNS` is `as const`, so `CONNECTIONS_COLUMNS[2]` has the
literal type `"Used by"` and `[3]` has `"Credential"`.

From `frontend/src/components/files/FileRow.tsx:73-90, 141-149, 217` — ⭐ the PROP-DRIVEN density
analog: a `DensityKey`, a frozen `Record<DensityKey, DensitySpec>` of CLASS STRINGS, and ONE
resolution `const d = DENSITY[density]`. Every cell renders unconditionally; density changes only
class strings and sizes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Capture the WIDE render byte-for-byte, BEFORE the dense branch exists</name>

  <read_first>
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx:2640-2647` (the `cardHtml` capture
      helper), `:2649-2673` (⭐ the capture-rule docblock whose WORDING is copied), `:2765-2790`
      (the `CARD_HTML_BASELINE` record and its non-vacuity line), `:2792-2825` (the MARKER ROWS —
      the second half of the idiom, without which byte-identity against an empty capture passes
      forever)
    - `frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx:107-121` — `renderTab`,
      which NEVER passes `panel`, so all 36 shipped cases render the WIDE shape
    - `frontend/src/components/settings/ConnectionsTab.tsx:219-220` — the `now` prop, which exists
      precisely so a render is not a clock reading
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-PATTERNS.md`
      § Analog 6 and its *What NOT to copy*
  </read_first>

  <files>frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx</files>

  <action>
    ⚠ **This task lands and COMMITS before any dense code exists.** A baseline only proves something
    if it PREDATES the change — the `cardFace.ts` precedent, where the characterization pin was
    committed ONE COMMIT BEFORE the seam existed and then passed with an empty `numstat`. There is
    **no `outerHTML` capture anywhere in the settings suites today** (measured — the only
    `innerHTML` uses there are needle scans), so this idiom is IMPORTED from `PhaseNodeCard.test.tsx`,
    not extended from a local example.

    Add a `describe` block holding:

    **(a) A capture helper.** One render, its container's `innerHTML`, unmounted — shared by the
    capture and the assertion so both read the DOM the same way (`PhaseNodeCard.test.tsx:2640-2647`).

    **(b) An explicit, FIXED `now`.** ⚠ `ConnectionRow` renders
    `credentialLabel(connection.last_checked_at, now)`. Pass a constant `now` through the existing
    `ConnectionsTabView` prop, and a constant `last_checked_at`, or the capture is a clock reading
    that will differ tomorrow (`PhaseNodeCard`'s captures are safe only because a subtree fence
    forbids `Date.now`/`Math.random`; this file has no such fence, so the discipline is the fixture).

    **(c) Three capture rows**, each a distinct shape so the baseline says something: one
    `send_email` row (long SMTP destination), one `post_message` row (which carries the `fixed` tag),
    and one row with `mcp_server_url` set and `capability: null` (the shape items 1 and 3 both care
    about). Each with `is_enabled: true`, a fixed `last_checked_at`, and a fixed `usedBy` count.

    **(d) The capture-rule docblock, copying `PhaseNodeCard.test.tsx:2649-2673`'s WORDING:** every
    character below was READ OUT of the rendered DOM at this commit by running the helper and pasting
    what it printed; not one attribute was typed from source, computed by hand, or reasoned about; an
    expectation records what its author BELIEVED the geometry to be, and a move that changed the
    geometry to match that belief would pass it. Record that the capture was **OBSERVED TWICE on the
    unchanged tree and the two runs agreed byte for byte**, so it is a baseline rather than one
    sample of something that might vary — and run it twice for real before committing. State the
    consequence: **a diff against this record is a BEHAVIOUR CHANGE, not a test to update**;
    re-capturing it to make it green deletes the only evidence anybody has that the wide row still
    renders what it rendered.

    **(e) A non-vacuity line FIRST in each case** — each baseline string's length is greater than
    zero. Byte-identity against an empty capture passes forever.

    **(f) MARKER ROWS** (`PhaseNodeCard.test.tsx:2792-2825`) — assertions over the COMMITTED baseline
    strings (never a fresh render) saying WHAT each capture contains, so a row that quietly stopped
    painting is a failure rather than a pass. At minimum: the `send_email` capture contains
    `connections-row-destination` and `🔒`; the `post_message` capture contains the `fixed` tag text;
    the MCP capture contains `connections-row-name`; and every capture contains
    `connections-row-usedby`, `connections-row-credential`, `connections-row-state` and
    `connections-row-more`.

    ⚠ **Do NOT hand-edit a capture string.** ⚠ **Do NOT capture from a tree that already has the
    dense branch.**
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx --maxWorkers=2</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx --maxWorkers=2`
      exits 0, `failed 0`, and reports **>= 39** cases (36 shipped + at least 3 capture cases)
    - Running that command TWICE in a row produces identical results (the capture is stable, not a
      clock reading) — record both runs in the SUMMARY
    - `git diff --numstat -- frontend/src/components/settings/ConnectionsTab.tsx` for this task's
      commit reads **`0  0`** or the file is absent from the diff entirely — ⚠ **this task changes
      NO source**, which is what makes the pin a baseline
    - `grep -c "outerHTML\|innerHTML" frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx`
      is greater than its pre-task count (the capture helper is present)
    - Each committed baseline string has non-zero length, asserted in the test body BEFORE the
      comparison
    - The docblock contains the sentence that a diff here is a behaviour change and not a test to
      update — `grep -ci "not a test to update" frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx`
      reads `>= 1`
  </acceptance_criteria>

  <done>
    The wide `ConnectionRow` render is pinned byte-for-byte at this plan's base, in three shapes,
    with a fixed clock, a non-vacuity line and marker rows — committed before one line of dense code
    exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread `dense` from the one condition and build the three-line dense row</name>

  <read_first>
    - `frontend/src/components/settings/ConnectionsTab.tsx:270-276` (the ONE condition), `:443-464`
      (the column header), `:468-478` (the row call site), `:501-519` (`ConnectionRow`'s props),
      `:196-226` (the props interface and its absence-docblock habit), `:549-628` (the whole wide
      row: name cell, destination cell at `:583-595`, `usedby`, `credential`, `state`, and the
      `w-8` action cell that holds `connections-row-more` OR `connections-receipt`)
    - `frontend/src/components/files/FileRow.tsx:58-67` (the `DENSITY` charter), `:73-90` (the spec
      shape), `:141-149` and `:217` (the prop and the ONE resolution), `:156-176` (how every cell
      renders unconditionally and omission is a CALLER decision, never a density decision)
    - `frontend/src/components/panel/PhaseCard.tsx:445-456` — ⚠ copy its STACKED IDENTITY COLUMN
      markup (`flex min-w-0 flex-col`, one truncating lead line, meta lines beneath) and **NOT** its
      conditional-content decision: it FOLDS A FACT AWAY at higher density, which is exactly what
      D-206.1-13 forbids
    - `frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx:216-232` (state chips,
      textContent EQUALITY against `CONNECTION_STATE_WORDS`), `:505-515` (ZERO `[title]`),
      `:619-622` (the five headings, `.toEqual` exact), `:624` (no `<table>`), `:626-635`
      (destination substrings incl. `🔒` and the `fixed` tag), `:640-641` (⚠ `-credential`
      textContent `.toBe("never checked")` by EXACT EQUALITY, and `/^checked /` anchored at start)
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-UI-SPEC.md`
      § Surface 2 (the locked layout, the line-by-line class lists, and why layouts A / B / C were
      each not taken as written) and § Spacing Scale exception 4
  </read_first>

  <behavior>
    Write the assertions first, RED, then make them green. The SHAPE half of SC#2 (SC#2a):

    - **Both variants render.** A new render helper passes `panel` (or the `dense` prop directly)
      so a dense render is reachable from the suite for the first time — ⚠ measured, `renderTab`
      never passes `panel`, so **no existing case exercises dense at all**.
    - **`data-dense` reflects the flag with BOTH values present.** `"true"` in the dense render and
      `"false"` in the wide one. ⚠ An ABSENT attribute reading as false is the *"0 is a fact, absence
      is a different one"* trap — assert the attribute exists in both.
    - **All SIX row testids resolve in BOTH renders**, queried by name rather than eyeballed:
      `connections-row-name`, `connections-row-destination`, `connections-row-usedby`,
      `connections-row-credential`, `connections-row-state`, `connections-row-more`. This is
      D-206.1-13 asserted.
    - **The dense destination cell's class list contains NO `truncate`**, on the cell AND on the
      inner value span — both, because `truncate` ships TWICE today and removing one leaves the
      other. Assert the value span carries `whitespace-normal` and `break-all`.
    - **`break-all`, not `break-words`** — a URL has no spaces, so `break-words` will not break it.
      Assert the token.
    - **The column header is ABSENT in dense and PRESENT in wide**:
      `queryByTestId("connections-header")` is null in dense, non-null in wide, and the wide one
      still yields exactly the five locked words in order (D-206.1-20).
    - **`connections-row-credential`'s textContent is CHARACTER-IDENTICAL in both shapes** — the
      shipped `.toBe("never checked")` equality holds for the DENSE render too. ⚠ That is what
      forces the inline label to be a SIBLING node, outside the testid'd node.
    - **The inline labels are DERIVED, not re-typed**: the dense render contains the exact strings
      `CONNECTIONS_COLUMNS[2]` and `CONNECTIONS_COLUMNS[3]`, asserted against the imported constants
      rather than against literals — and asserted a second time as
      `CONNECTIONS_DENSE_LABEL_USED_BY === CONNECTIONS_COLUMNS[2]` so the two shapes are structurally
      unable to disagree.
    - **The labels are NOT `aria-hidden`** — they carry meaning for AT users exactly as for sighted
      ones ("Credential checked 3d ago").
    - **The state chip's textContent is unchanged in dense** — the four glyph-AND-word readings hold
      in both shapes (WCAG 1.4.1 by construction; remove every colour and all four still read).
    - **ZERO `[title]` nodes in the DENSE render too**, asserted with the ⋯ menu open and a confirm
      sheet open — the shipped fence extended to the new shape.
    - **The ⋯ / receipt either-or survives**: in dense, `connections-row-more` and
      `connections-receipt` occupy the same action cell, exactly as `:625-637` ships.
    - **`canWrite` still gates the ⋯ in dense** — a non-admin dense render has no
      `connections-row-more`, mirroring the shipped removed-not-disabled rule.
    - **No `<table>` primitive in either shape.**
    - **The WIDE capture from Task 1 still passes UNEDITED** — no baseline string is touched.
  </behavior>

  <files>
    frontend/src/components/settings/ConnectionsTab.tsx,
    frontend/src/components/settings/connectionsCopy.ts,
    frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
  </files>

  <action>
    **`connectionsCopy.ts` — two new exports, both DERIVED.** Add
    `CONNECTIONS_DENSE_LABEL_USED_BY = CONNECTIONS_COLUMNS[2]` and
    `CONNECTIONS_DENSE_LABEL_CREDENTIAL = CONNECTIONS_COLUMNS[3]`. ⚠ **Derived, never re-typed** —
    the dense inline label and the wide column header are the same word, and deriving it means the
    two shapes cannot disagree. `CONNECTIONS_COLUMNS` is `as const`, so the types are the literals.
    Give each a one-line docblock saying that.

    **`ConnectionsTab.tsx` — thread the flag from the ONE condition.** At `:270-276`, assign the
    existing `panel && !isMobile` expression to a single `const dense = Boolean(panel) && !isMobile`
    and use it for BOTH the `gridTemplateColumns` style and the rows. Pass `dense` down through
    `ConnectionRow`'s props. ⚠ **One condition, one home, two consequences** — do not introduce a
    second expression, a media query, or a container query. Container queries are refused and not
    revisited: no `@tailwindcss/container-queries` is installed (a second new dependency) and jsdom
    evaluates no layout, so a container-driven shape would be untestable by the suite that must pin
    it. Give `dense` a props docblock in the `:196-226` habit stating what its ABSENCE means:
    *absent or false ⇒ the shipped wide five-column row, byte-identical.*

    **The column header (`:443-464`)** renders only when `!dense`. `CONNECTIONS_COLUMNS` stays
    exported and stays rendered by the wide shape with its character identity intact. Say why in a
    comment: in the dense shape the cells no longer form five aligned columns, so a five-word header
    would label a grid that is not there — and **this is the ONE thing the dense shape drops; cells
    are never dropped** (D-206.1-20 vs D-206.1-13, both cited).

    **The row.** Add `data-dense={dense ? "true" : "false"}` to the `connections-row` element —
    **always present, both values.** Then branch the row's INTERIOR only. Follow the `FileRow.tsx`
    habit: every cell renders unconditionally in both shapes, and what changes is the container
    structure and the class strings.

    **The locked dense shape — three lines, ⋯ on line 1** (UI-SPEC § Surface 2, which locked a
    fourth layout that is RESEARCH's B line-order with A's action placement; A was refused because it
    squeezes the NAME to ~110px on the one surface whose defect IS squeezing, and C was refused
    because `row-span-3` moves an already-28px hit target):

    - **Container:** `flex flex-col gap-1.5 px-3.5 py-3`, plus `!is_enabled && "bg-muted/20"`.
      ⚠ `px-3.5 py-3` is REUSED UNCHANGED from the wide shape so the two shapes share one outer box.
      ⚠ `gap-1.5` (6px) is the ONE spacing decision this phase makes and it is an EXTENSION of a
      shipped exception: 6px ships today only as mark→text INSIDE the destination cell, and here it
      spaces three stacked LINES. 8px is the row's own inter-column gap (reusing it makes the lines
      read as gapped siblings rather than one row) and 4px is the label→value step (it collides the
      11px/13px baselines). Record that justification in a comment. ⚠ `gap-1.5` may appear on the
      dense container and nowhere else new.
    - **Line 1** `flex items-center gap-2`: the mark (`ConnectionMarkGlyph`, `size="row"`, unchanged
      from plan 01) · the name cell `flex-1 min-w-0 truncate text-[13px] font-medium text-foreground`
      · an action cell `flex min-w-[2rem] flex-none items-center justify-end` holding the shipped
      either/or of `connections-row-more` and `connections-receipt`.
    - **Line 2** — the destination, `data-testid="connections-row-destination"`,
      `flex min-w-0 items-start gap-1.5 font-mono text-[11px] text-muted-foreground`. Inside: the
      `🔒` span (`aria-hidden`, `flex-none`) · the value span `min-w-0 whitespace-normal break-all`
      · the `fixed` tag on Slack only (`flex-none`). ⚠ **NO `truncate` on either node.** ⚠ **This
      line is where SC#2 is won or lost:** with `nowrap`, `scrollWidth` is the full un-wrapped text
      width, so `scrollWidth <= clientWidth` cannot be satisfied by widening. Write that sentence
      into the source beside the class list.
    - **Line 3** `flex flex-wrap items-baseline gap-x-3 gap-y-1 text-[11px]`: the state cell
      (`connections-row-state`, inner span and `STATE_TONE[state]` unchanged) · a `Used by` group ·
      a `Credential` group.
    - **The two labelled groups.** Each is
      `<span className="inline-flex items-baseline gap-1">` holding a label span
      (`text-muted-foreground`, rendering the derived constant) followed by the EXISTING testid'd
      value node, unchanged. ⚠ **The label is a SIBLING, NEVER inside the testid'd node** —
      `ConnectionsTab.test.tsx:640` asserts `connections-row-credential`'s textContent
      `.toBe("never checked")` by EXACT EQUALITY and `:641` anchors `/^checked /` at the start.
      Keeping the label outside means the value node's textContent is character-identical in BOTH
      shapes, so the shipped assertion holds for dense too. Putting it inside would force a
      re-baseline — of a pin, on the surface whose whole lesson is that re-baselined pins are not
      evidence. ⚠ The label is **not** `aria-hidden`. ⚠ The label is `text-muted-foreground` and
      **never `text-muted-foreground-dim`**, which has no Tailwind key and compiles to NOTHING (the
      `bg-warning` defect Phase 192.2 found).
    - `connections-row-state` needs no label (glyph + word are self-describing) and
      `connections-row-destination` needs none either (the `🔒` leads it and it is the only
      `font-mono` line).

    **Forbidden, explicitly:** any new spacing value outside the shipped scale — in particular no
    `py-2.5`, `gap-2.5` or `px-2` on the row. No new `text-[Npx]`: this surface ships exactly TWO
    sizes, `text-[11px]` and `text-[13px]`, and nothing else. No `font-semibold` (it exists at ONE
    node on this surface, the panel title, and is an inherited exception). ⚠ **No `title` attribute
    anywhere** — wrapping is the answer BECAUSE the tooltip is not available. ⚠ **Do not restructure
    the wide shape**; Task 1's capture must pass UNEDITED.

    Height cost, recorded as a DECISION and not left as a discovery: wide ≈ 44px, dense ≈ 72-88px for
    a destination wrapping to 1-2 lines. At 24 rows that is a longer scroll — accepted, because the
    alternative is a column a person cannot read, and the dense shape applies exactly when the panel
    is open, i.e. when the person is looking at ONE connection.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/components/settings --maxWorkers=2</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/settings --maxWorkers=2` exits 0, `failed 0`
    - `ConnectionsTab.test.tsx` reports **>= 48** cases (36 shipped + Task 1's captures + the dense
      shape block), and **every one of the 36 shipped cases passes UNEDITED**
    - Task 1's baseline strings are byte-identical in `git diff` — `git diff -- frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx`
      shows **no modification to any committed capture literal**. ⚠ A changed capture is a BEHAVIOUR
      CHANGE in the wide row and must be reported, not re-captured.
    - `grep -c "data-dense" frontend/src/components/settings/ConnectionsTab.tsx` reads `>= 1`
    - `grep -c "@container\|container-type\|@tailwindcss/container-queries" frontend/src/components/settings/ConnectionsTab.tsx frontend/tailwind.config.js`
      reads `0` (D-206.1-12: container queries refused)
    - `grep -c "title=" frontend/src/components/settings/ConnectionsTab.tsx` reads `0`
    - `grep -c "text-muted-foreground-dim" frontend/src/components/settings/ConnectionsTab.tsx` reads `0`
    - `grep -oE "text-\[[0-9]+px\]" frontend/src/components/settings/ConnectionsTab.tsx | sort -u`
      lists exactly `text-[11px]` and `text-[13px]` and nothing else
    - `grep -c "CONNECTIONS_COLUMNS\[2\]\|CONNECTIONS_COLUMNS\[3\]" frontend/src/components/settings/connectionsCopy.ts`
      reads `2` — the labels are DERIVED, and a re-typed `"Used by"` literal fails this
    - The dense render's destination cell and its value span each have a class list with **no
      `truncate` token**, asserted in the suite
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` reads the
      re-derived base count (expected **34**), zero in any `settings/Connection*` file
    - `git diff --numstat -- frontend/src/lib/api.ts` is EMPTY
  </acceptance_criteria>

  <done>
    A dense three-line row exists behind a prop threaded from the one condition that opens the panel;
    all five columns and the action cell are present in BOTH shapes; the destination WRAPS with
    `break-all` and carries no `truncate`; the header is the only thing dense drops; the credential
    cell's textContent is character-identical in both; and the wide render is proved byte-unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BROWSER TIER — measure the real geometry at 1280 and 1536 (SC#2b)</name>

  <read_first>
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-RESEARCH.md`
      § OQ#6 — the jsdom zero-measurement probe and the full Chrome DevTools MCP recipe, steps 1-5,
      including the measurement snippet
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-VALIDATION.md`
      § Manual-Only Verifications — the SC#2b row
    - `frontend/src/pages/SettingsPage.tsx:861` (`max-w-3xl` — the 768px cap that makes 1280 and 1536
      identical) and `:531,536` (`localStorage.getItem/setItem("settings_active_tab")`); `:900`
      (`<TabsTrigger value="5">Connections</TabsTrigger>` — the key/value verified against shipped
      source, not assumed)
  </read_first>

  <what-built>
    Tasks 1 and 2 shipped the dense row shape and pinned its SHAPE in vitest. ⚠ **That vitest run is
    NOT SC#2 evidence and this task exists because it is not.** Measured: jsdom reports
    `scrollWidth 0 / clientWidth 0 / offsetWidth 0 / getBoundingClientRect().width 0` for every
    element, so `expect(el.scrollWidth <= el.clientWidth).toBe(true)` **PASSES VACUOUSLY** on markup
    that overflows by ~400px in a real browser. No alternative box metric rescues it.

    This task measures the real thing, in a real browser, via Chrome DevTools MCP `evaluate_script`.
  </what-built>

  <how-to-verify>
    **Preconditions.** Local infra UP: backend `:8000`, frontend `:5173`, Supabase `:54321`. Bring it
    up with `powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1` if needed. At
    least one connection row must exist whose destination exceeds ~45 characters — an
    `https://fhdmrdautomation.app.n8n.cloud/mcp/...` or `https://mcp.deepwiki.com/mcp` row both
    qualify and are real.

    **Step 1 — reach the tab.** ⚠ **There is NO URL router** (SEED-185), and `SettingsPage.tsx`
    states in writing not to add one to reach this tab. `navigate_page` to `http://localhost:5173/`,
    then `evaluate_script` to run `localStorage.setItem("settings_active_tab", "5")` and read it back,
    then `navigate_page` again so the Tabs default picks it up. `"5"` is Connections — verified
    against `SettingsPage.tsx:900`, not assumed.

    **Step 2 — confirm the tab.** `take_snapshot`, click the Settings nav item, then `evaluate_script`
    `() => !!document.querySelector('[data-testid="connections-tab"]')`. If false, click the
    `Connections` tab trigger from the snapshot and re-assert.

    **Step 3 — `resize_page` to 1280 x 900, then OPEN THE PANEL** by clicking the
    `connections-row-name` button of the row with the LONGEST destination. Confirm
    `[data-testid="connection-form-panel"]` exists.

    **Step 4 — run RESEARCH §OQ#6's measurement snippet verbatim** and record its full JSON. It
    returns, per destination cell: `text`, `whiteSpace`, `cellScrollW`, `cellClientW`, `cellFits`,
    `spanScrollW`, `spanClientW`, `spanFits` — plus `viewport`, `panelOpen`, `panelLayout`,
    `gridCols`, `splitW`, `listW`, and the `control` block.

    **Step 5 — `resize_page` to 1536 x 900 and run the identical snippet.**

    ⚠ `take_screenshot` is recorded as TIMING OUT in this environment — read DOM geometry via
    `evaluate_script` throughout, which is what this recipe does.
  </how-to-verify>

  <acceptance_criteria>
    **At BOTH 1280 and 1536, with `panelOpen === true` and `panelLayout === "split"`:**
    - `control.bodyClientW > 0` — ⚠ **the NON-VACUITY CONTROL, and it is the reason this task
      exists.** A `0` means the measurement ran under a layout-free engine and proves nothing.
    - For **EVERY** `connections-row-destination` cell, including at least one whose `text` exceeds
      45 characters: `cellFits === true` **AND** `spanFits === true`
    - For every such cell: `whiteSpace !== "nowrap"`. ⚠ `cellFits: true` **while** `nowrap` means a
      short fixture was measured, not a working shape — it is a FAILURE, not a pass.
    - `gridCols` shows the two-track `... 400px` layout, confirming the dense condition is the one
      that is live
    - The row elements carry `data-dense="true"`

    **The invariance finding, stated so it reads as confirmation:**
    - ⚠ **`listW` is EXPECTED TO BE IDENTICAL at 1280 and 1536.** `SettingsPage.tsx:861` caps this
      surface at `max-w-3xl` = 768px, so the open list track is ~302px at both viewports.
      **An identical pair is CONFIRMATION, not a copy-paste error** (D-206.1-21), and both readings
      are recorded rather than one being dropped as a duplicate. Do not re-run in search of a
      difference. A future change to that cap is exactly what the second reading would catch.
    - RESEARCH's arithmetic gives 718 / 302 and the operator measured 717 / ~317; **the measurement
      is the authority** and this task re-measures rather than reconciling either by assumption.
      Record what it actually reads.

    **Recording:**
    - Evidence is recorded **per step and per viewport**, never as one blob at the end
    - The full JSON of both `evaluate_script` returns is pasted into the SUMMARY
    - Any `cellFits: false` is recorded with the cell's `text`, `cellScrollW` and `cellClientW`, and
      is a FAILURE of SC#2 — not a note
  </acceptance_criteria>

  <action>
    CHECKPOINT TASK — the work is the measurement, not a code change. Drive Chrome DevTools MCP
    through RESEARCH §OQ#6's five steps: seed `localStorage.settings_active_tab = "5"` and reload,
    confirm `[data-testid="connections-tab"]`, `resize_page` to 1280x900, open the panel on the row
    with the LONGEST destination, run §OQ#6's measurement snippet VERBATIM via `evaluate_script`,
    then repeat at 1536x900. Paste both full JSON returns into the SUMMARY. ⚠ Read geometry via
    `evaluate_script` — `take_screenshot` is recorded as timing out in this environment.
  </action>

  <done>
    Both viewports measured with `panelOpen === true`, `bodyClientW > 0`, `cellFits` and `spanFits`
    true on every destination cell including one over 45 characters, `whiteSpace !== "nowrap"`, and
    `listW` recorded as identical at both widths with that identity named as the expected invariance.
  </done>

  <resume-signal>Type "approved" or paste the failing measurement</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response (`ConnectorConnection`) → the destination cell | The host a person's organisation is about to send data to crosses here and is rendered for approval |
| The `dense` flag → the row's write affordances | A second render path is a second place the `canWrite` / kill-switch removal rule can be forgotten |
| The committed capture literal → the wide render | A pin that can be re-captured is a pin that proves nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-206.1-02-ID | Information Disclosure | `connections-row-destination` truncating the host beside an open panel | mitigate | The dense cell WRAPS (`whitespace-normal break-all`, no `truncate` on cell or span) and the result is measured in a real browser at 1280 and 1536 with a non-vacuity control. ⚠ This column is the ONE place a person is told where their org's data is about to go; a truncated host is a host they approved without reading. |
| T-206.1-02-EL | Elevation of Privilege | the ⋯ menu in the dense shape | mitigate | `canWrite` still gates `connections-row-more` in dense, asserted by a non-admin dense render. The shipped rule is REMOVED, never disabled — a `toBeDisabled()` assertion was measured unable to see plant C at 190-16. |
| T-206.1-02-TA | Tampering | the wide-render byte-identity capture | mitigate | Captured and COMMITTED before any dense code exists (the `cardFace.ts` precedent), observed twice on the unchanged tree, with a non-vacuity line and marker rows. A diff is reported as a behaviour change and never re-captured. |
| T-206.1-02-SP | Spoofing | a `title` tooltip standing in for the real value | mitigate | FORBIDDEN and asserted at zero, with a menu and a sheet open. Wrapping is the answer BECAUSE the tooltip is unavailable. |
| T-206.1-02-RE | Repudiation | a green vitest run recorded as SC#2 evidence | mitigate | D-206.1-14 is written into this plan's `must_haves`; the geometry acceptance lives in a separate browser-tier task whose FIRST criterion is `bodyClientW > 0`. |
| T-206.1-02-DOS | Denial of Service (readability) | dense row height at 24 rows | accept | ~72-88px vs ~44px is a longer scroll, accepted and recorded as a decision: the alternative is a column a person cannot read, and the dense shape applies exactly when the person is looking at ONE connection. |
</threat_model>

<verification>
Run at the END of this plan:

1. `cd frontend && npx vitest run src/components/settings --maxWorkers=2` — exits 0, `failed 0`,
   `ConnectionsTab.test.tsx >= 48`, all 36 shipped cases UNEDITED, every Task-1 capture literal
   byte-unchanged
2. `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` — the re-derived
   base count (expected **34**), zero in `settings/Connection*`
3. `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — verdict line
   VERBATIM. `count gate OK`, no per-file DECREASE, `failed 0`; a larger total is the gate WORKING.
   ⚠ If red, capture failing filenames from the persisted JSON BEFORE re-running, check each against
   `git diff --numstat`, then decide flake vs real (SEED-171). **Never reach for the worker cap.**
4. `cd backend && venv/Scripts/python -m pytest tests/unit -q` — still `68 failed`
5. Task 3's browser JSON for BOTH viewports pasted into the SUMMARY, with `bodyClientW > 0`,
   `cellFits`/`spanFits` true everywhere, `whiteSpace !== "nowrap"`, and `listW` recorded as
   identical at both widths with that identity named as the expected invariance finding
</verification>

<success_criteria>
- SC#2 is met on BOTH tiers: the vitest SHAPE half (both variants, six testids in both, `data-dense`
  with both values, no `truncate` in dense, header absent in dense, wide render byte-identical) AND
  the real-browser GEOMETRY half (`scrollWidth <= clientWidth` and `whiteSpace !== "nowrap"` on every
  destination cell, at 1280 and 1536, panel open, with a non-vacuity control).
- No column was deleted — all five plus the action cell render in both shapes (D-206.1-13).
- Only the five-word HEADER is dropped in dense, and `CONNECTIONS_COLUMNS` keeps its character
  identity in the wide shape (D-206.1-20).
- Baselines unchanged: tsc at its re-derived base, count gate `OK` with no per-file decrease, backend
  `68 failed`.
</success_criteria>

<output>
Create `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-02-SUMMARY.md`
when done. Record: the two identical Task-1 capture runs; the verbatim count-gate verdict line and
the per-file deltas; the full browser JSON for both viewports; and the `listW` invariance stated as
confirmation rather than as a duplicate reading.
</output>
