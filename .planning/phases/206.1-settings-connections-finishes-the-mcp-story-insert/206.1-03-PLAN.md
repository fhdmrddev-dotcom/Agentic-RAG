---
phase: 206.1-settings-connections-finishes-the-mcp-story-insert
plan: 03
type: execute
wave: 3
depends_on: ["206.1-01", "206.1-02"]
files_modified:
  - frontend/src/components/settings/connectionFormCopy.ts
  - frontend/src/components/settings/connectionsCopy.ts
  - frontend/src/components/settings/ConnectionFormPanel.tsx
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx
  - frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: false
requirements: [CONN-02]
worktrees: forbidden
gap_closure: false

must_haves:
  truths:
    - "ITEM 1 ONLY (plus its unnamed sibling defect) — this plan is one of three separable plans per D-206.1-01. ⚠ It carries BOTH a new capability (the create door) AND a defect fix on shipped code (D-206.1-22, the edit path). That is stated in writing so a reviewer is not surprised to find repair work in a feature plan and so G-7's feature/defect separability stays legible."
    - "D-206.1-03: the MCP shape is a FOURTH `<option>` in the existing native `<select>`, not a second form. Same panel, same Save, same refusal vocabulary; only the per-shape field block switches, exactly as `send_email` / `create_ticket` / `post_message` already do at `ConnectionFormPanel.tsx:899-1008`."
    - "D-206.1-04: the create body is `{ name, mcp_server_url, config: { headers: {} }, secret? }` with `capability` ABSENT. Measured at the model — `_validate_connection_shape` branches on `mcp_server_url` FIRST and returns before the capability arm; `McpConfig` is `extra=forbid` with exactly one field. A capability alongside a URL, or a `SendEmailConfig`-shaped config, is a 422. ⚠ `secret: \"\"` is OMITTED, never sent — the server's `NonEmpty` rejects it as a present value."
    - "D-206.1-05: the panel refuses a non-`https://` URL with a DISABLED Save and a real DOM reason, and the source says where the check lives that the panel is a COURTESY and never the security boundary — the model raises and `validate_mcp_destination` refuses again at call time."
    - "D-206.1-06: the credential is OPTIONAL for the MCP shape and ONLY for it. The panel does not require one and does not present its absence as an error state. A capability connection still requires a secret (WR-05)."
    - "D-206.1-22: the EDIT path renders the MCP shape. Today an MCP row's panel renders the SMTP form — both `ConnectionFormPanel.tsx:714` and `draftFromConnection` fall back to `send_email` when `connection.capability` is null, so a person sees `Send an email`, SMTP host/port, an `App password` label and a Save that 422s. Detection is from the ROW (`connection.mcp_server_url != null`), NEVER from `capability === null`."
    - "SC#4 EXTENDED — all FOUR positional ladders get NAMED arms and a synthetic-FIFTH-shape negative control: `configFromDraft` (`connectionFormCopy.ts:265` → `{default_channel:\"\"}` → 422), `destinationFooterOf` (`:377-387` → the SMTP footer), `typedHost` (`ConnectionFormPanel.tsx:733` → the SMTP host), `secretLabel` (`:740` → \"Bot token\"). `destinationFactsOf` is already explicit and its control is a REGRESSION guard. D-206.1-11's rule, applied five times."
    - "D-206.1-19: an MCP row does NOT offer `Check credential`, and the affordance is REMOVED, not disabled — the check path is capability-shaped and has no MCP arm. The reason is stated in SOURCE at the removal site, never as a user-visible sentence."
    - "D-206.1-18: Discover and Grant keep their existing home at the BUILDER seam (`McpToolPicker.tsx`'s `mcp-discover-btn`, mounted through `ConnectionPicker.tsx`). Settings gains neither — a second door would be a NEW capability inside a phase that already carries one."
    - "D-206.1-07: acceptance is the DRIVEN round trip, not a 201 — create in the UI → discover → grant → a workflow step calls a granted tool and the run records a send receipt. ⚠ NO `INSERT INTO connector_connections` anywhere in the evidence."
    - "D-206.1-24: all four settings files cross the G-5 threshold in THIS phase's own commit and all four are absent from CLAUDE.md's table AND from `docs/HOT-FILE-LEDGER.md`. Four rows + four sections land in the SAME commit, with RE-DERIVED triples and a named seam or a recorded honoured-by-construction reason each."
    - "D-206.1-02: `frontend/src/lib/api.ts` is NOT edited. Every MCP type is already exported there (`ConnectorConnectionCreate` with `capability?: ... | null` and `mcp_server_url?: string | null`, `ConnectorConnection`, `McpDiscoveredTool`). A task that believes it needs a new export has mis-read the file."
    - "D-206.1-15 / D-206.1-16 / D-206.1-17: baselines re-derived at this plan's own base, `tsc -p tsconfig.app.json` (the bare `--noEmit` checks ZERO files), and a red gate triaged from its persisted JSON before any re-run."
  artifacts:
    - path: "frontend/src/components/settings/connectionFormCopy.ts"
      provides: "ConnectionShape, mcpServerUrl on the draft, the fourth chooser choice, FIELD_COUNTS.mcp, the two copy-module ladder arms, every new MCP string, and draftFromConnection's edit-shape fix"
      exports: ["ConnectionShape", "CAPABILITY_CHOICE_MCP_LABEL", "FIELD_MCP_URL_LABEL", "FIELD_MCP_URL_PLACEHOLDER", "FIELD_MCP_URL_HELP", "FIELD_MCP_SECRET_LABEL", "FIELD_MCP_SECRET_OPTIONAL_NOTE", "MCP_SAVE_DISABLED_REASON"]
    - path: "frontend/src/components/settings/connectionsCopy.ts"
      provides: "AR-05 — CREDENTIAL_NO_CHECK_FOR_KIND and credentialReadingOf, with credentialLabel left byte-identical"
      exports: ["CREDENTIAL_NO_CHECK_FOR_KIND", "credentialReadingOf"]
    - path: "frontend/src/components/settings/ConnectionFormPanel.tsx"
      provides: "The fourth chooser option, the three-field MCP block, the two panel ladder arms, the MCP create/update body, the second disabled-Save condition with its OWN aria-describedby id, and the edit-mode MCP shape"
      contains: "connection-mcp-save-disabled-reason"
    - path: "CLAUDE.md"
      provides: "Four hot-file ledger rows (the audit scan list)"
      contains: "settings/ConnectionsTab.tsx"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "Four detail sections, same commit as the rows"
      contains: "components/settings/ConnectionFormPanel.tsx"
  key_links:
    - from: "ConnectionFormPanel.tsx handleSave (create branch)"
      to: "POST /connector-connections"
      via: "onCreate with a body whose key set is {name, mcp_server_url, config} + secret only when typed"
      pattern: "mcp_server_url"
    - from: "ConnectionFormPanel.tsx draft.capability === \"mcp\""
      to: "connectionFormCopy.configFromDraft"
      via: "an explicitly-named mcp arm returning { headers: {} }"
      pattern: "headers"
    - from: "the Save button's aria-describedby"
      to: "#{fieldId}-mcp-save-disabled-reason"
      via: "a SECOND, DISTINCT id — never saveDisabledReasonId"
      pattern: "mcp-save-disabled-reason"
---

<objective>
Item 1 of Phase 206.1 — **the MCP creation door**, plus the live edit-mode defect research found
while measuring it.

Phase 206 shipped a complete, working MCP connector path — client, SSRF defense, discovery, per-tool
grants, executor dispatch, audit — and **shipped no door onto it**. Measured in the browser:
`mentionsMcp: false`; the chooser offers `send_email` / `create_ticket` / `post_message` and nothing
else, and there is no `mcp_server_url` field anywhere in the panel. **Every part of 206 downstream of
the row works.** The only thing between the operator and the feature is a form.

⚠ **This plan carries a NEW CAPABILITY and a DEFECT FIX together, deliberately and in writing.** The
edit path is item 1's unnamed sibling: opening an existing MCP row renders the SMTP form, composes a
`SendEmailConfig` body, and 422s into the generic *"Couldn't save that — try again."* Both halves are
the MCP shape arriving in one panel, so they ship in one plan — and saying so is what keeps G-7's
feature/defect separability legible rather than accidental.

Purpose: this is the Phase-118 *built-and-unreachable* shape, and it must not stay that way.

Output: the fourth chooser option and its three-field block, four ladders with named arms and a
synthetic-fifth-shape control, the edit-mode fix, a driven round trip, and the phase's hot-file
ledger debt paid in this phase's own commit.
</objective>

<constraints>
⚠ **WORKTREES ARE FORBIDDEN FOR THIS ENTIRE PHASE.** This plan edits `ConnectionsTab.tsx` and its
suite, which plans 01 and 02 also edit. Waves are serial: 01 → 02 → 03. Do not run
`scripts/bootstrap-worktree.sh`; do not create a worktree.

⚠ **NEVER EDIT `frontend/src/lib/api.ts`.** Phase 207 exists BECAUSE Phase 206 added two runtime
exports there and the ledger trigger fired; the trigger says the next phase TAKES the split or
escalates, and *it may NOT re-decline*. Every type this plan needs is already at `api.ts:6318-6426`.
Read it once, edit nothing. A task that believes it needs a new export has mis-read the file.

⚠ **BACKEND IS UNTOUCHED.** No migration, no model, no route. The API has accepted this exact body
since `147f3c57`.

⚠ **`ConnectionFormPanel.tsx` may import NOTHING from `PhaseFormPanel`** — source fence at
`ConnectionFormPanel.test.tsx:826-834`.
</constraints>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-CONTEXT.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-UI-SPEC.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-PATTERNS.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-RESEARCH.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-VALIDATION.md
@.planning/phases/206-mcp-connector-client-workflow-scoped/206-01-SUMMARY.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-01-SUMMARY.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-02-SUMMARY.md
</context>

<interfaces>
<!-- Read out of the codebase at HEAD by the research and pattern passes. No exploration needed. -->

**The wire contract, at the model** — `backend/app/models/connector.py:247-268`, READ ONLY:

  # :247-254 — the MCP branch RETURNS BEFORE the capability arm
  if self.mcp_server_url:
      if not self.mcp_server_url.startswith("https://"):
          raise ValueError("mcp_server_url must be an HTTPS URL")
      return self
  # :257 — only reached when mcp_server_url is falsy
  if not self.capability:
      raise ValueError("Either capability or mcp_server_url must be provided")

  config: ConnectorConfig = Field(default_factory=McpConfig)   # :210 — optional on the wire
  class McpConfig(_StrictBase): headers: dict[str,str]         # :150-153 — extra="forbid", ONE field
  secret: NonEmpty | None = None                               # the WR-05 check sits AFTER the MCP return

**The draft type** — `connectionFormCopy.ts:169-197`. `ConnectionDraft.capability` is
`ConnectorCapability`, non-nullable; `EMPTY_DRAFT.capability = "send_email"`.

**The chooser** — `ConnectionFormPanel.tsx:843-872`, a native `<select>` mapping
`CAPABILITY_CHOICES` (`connectionFormCopy.ts:81-88`, typed
`ReadonlyArray<{ capability: ConnectorCapability; label: string }>`), with a
`connection-capability-static` block for `mode === "edit"` carrying `CAPABILITY_LOCKED_NOTE`.

**The block to copy** — the single-`Field` `post_message` arm, `ConnectionFormPanel.tsx:993-1008`.
`Field` (`:261-290`) carries `data-testid="connection-field"` at `:275`. `TextControl` (`:296+`) is
*"NOT a `disabled` input: a control that could never do anything is REMOVED"*.

**The FOUR positional ladders, trailing arm quoted by line:**
  1. `configFromDraft`      `connectionFormCopy.ts:265`      `return { default_channel: draft.channel.trim() }`
  2. `destinationFooterOf`  `connectionFormCopy.ts:377-387`  the SMTP body (host/port/tlsMode/from)
  3. `typedHost`            `ConnectionFormPanel.tsx:733`    `: draft.host.trim()`
  4. `secretLabel`          `ConnectionFormPanel.tsx:740`    `: FIELD_SLACK_SECRET_LABEL`

**The FIXED shape to copy** — `connectionsCopy.ts:181-222` (the `147f3c57` repair): every arm names
its own condition, the MCP arm identifies itself by `connection.mcp_server_url`, and the tail is a
NEUTRAL `return []`.

**`refusalOf`** — `connectionFormCopy.ts:319-333`. Already returns `REFUSAL_PLAINTEXT_SCHEME` for any
non-`https`/`smtps` scheme and `REFUSAL_PRIVATE_LITERAL` for `localhost` / `::1` / RFC1918. **Reuse
it verbatim — a second URL validator is a second answer.**

**The disabled-Save machinery** — `ConnectionFormPanel.tsx:625-626` (`saveBlocked` is ONLY
`saveRefusal?.kind === "cipher"`; `saveDisabledReasonId`), `:1479-1491` (the button's
`aria-describedby`), `:1128-1137` (the ONE reason node). Its constraining assertion —
`ConnectionFormPanel.test.tsx:975-998` — resolves the id with `container.querySelector`, which
returns the **FIRST** match.

**The edit-mode defect's two sites** — `ConnectionFormPanel.tsx:712-716` and
`connectionFormCopy.ts:214-217`. ⚠ Both docblocks are statements of the defect written as if they
were decisions.

**`frontend/src/lib/api.ts:6318-6426`** — READ, NEVER EDIT.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: connectionFormCopy.ts + connectionsCopy.ts — the MCP shape's types, strings, and two named ladder arms</name>

  <read_first>
    - `frontend/src/components/settings/connectionFormCopy.ts:1-34` (the module charter — why a
      `.ts` and not part of the `.tsx`), `:81-89` (`CAPABILITY_CHOICES`), `:96-97`
      (`capabilityLabelOf`, which is `...?.label ?? capability` and would render the raw string
      `"mcp"` if the choice were not added), `:111-119` (⭐ the SHAPE a new exported string takes —
      `FIELD_SMTP_HOST_HELP` and its *"states the plaintext refusal BEFORE a person can trip it"*
      docblock), `:151-155` (`FIELD_COUNTS`), `:169-197` (`ConnectionDraft` + `EMPTY_DRAFT`),
      `:214-217` (⚠ `draftFromConnection`'s defect docblock), `:248-266` (`configFromDraft`, trailing
      arm at `:265`), `:290-304` (`FOOTER_SERVER_NOTE` — the S7 *"the API is the wall"* idiom),
      `:319-333` (`refusalOf` — reuse verbatim), `:354-388` (`destinationFooterOf`, trailing SMTP arm
      at `:377-387`)
    - `frontend/src/components/settings/connectionsCopy.ts:181-222` (⭐ the FIXED explicit-arms shape
      and its `147f3c57` docblock), and `credentialLabel` (referenced from `ConnectionsTab.tsx:610`)
    - `backend/app/models/connector.py:150-153, 210, 222-268, 273-286` — READ ONLY. The two-shape
      validator, `McpConfig`'s `extra="forbid"`, and `ConnectorConnectionUpdate`'s ABSENCE of
      cross-field validation
    - `frontend/src/components/workflows/library/runFacts.test.ts:168-213` — ⭐ the three-part
      synthetic-shape negative control: near-miss values, the prototype-key set, and an explicit
      POSITIVE CONTROL
    - `frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx:258-300`
      (⚠ `stringsOfModule()` walks **`connectionRefusalCopy` ONLY**), `:595-673` (the footer's five
      cases incl. the unit half), `:1402-1445` (the nine-banned-terms fence),
      `:1160-1198` (the absolute-verb fence)
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-UI-SPEC.md`
      § Copywriting Contract (the exact new strings and the reason for each wording) and § Surface 1
  </read_first>

  <behavior>
    Assertions first, RED, then green.

    **The sentinel and the draft.**
    - `ConnectionShape` is `ConnectorCapability | "mcp"`; `CAPABILITY_CHOICES` has FOUR entries with
      `"mcp"` LAST; `capabilityLabelOf("mcp")` returns `CAPABILITY_CHOICE_MCP_LABEL` and NOT the raw
      string `"mcp"`.
    - `FIELD_COUNTS.mcp === 3`, and `FIELD_COUNTS` is total over all four members of
      `ConnectionShape`.
    - `EMPTY_DRAFT.mcpServerUrl === ""`.

    **`configFromDraft` — ladder 1.**
    - An `mcp` draft returns exactly `{ headers: {} }` — key set asserted, not just shape.
    - A `post_message` draft still returns `{ default_channel: ... }` (POSITIVE CONTROL).
    - ⚠ **The synthetic FIFTH shape** — a draft whose `capability` is `"not_a_capability"` cast
      through `ConnectionShape` — returns a NEUTRAL value and **NOT** `{ default_channel: "" }`.
      That literal shape is the live 422.
    - The prototype-key set (`constructor`, `toString`, `__proto__`, `hasOwnProperty`, `valueOf`) as
      `capability` values also returns the neutral.

    **`destinationFooterOf` — ladder 2.**
    - An `mcp` draft with an empty URL returns `destination === FOOTER_NOTHING_YET` and `tags` `[]`.
    - An `mcp` draft with an accepted `https://` URL returns that URL as the destination and
      `[FOOTER_TAG_TLS]`.
    - An `mcp` draft with a URL `refusalOf` refuses returns `[FOOTER_TAG_REFUSED]` plus the
      `refusedReason`, and that reason is `refusalOf`'s OWN return — asserted by comparing against a
      direct `refusalOf` call, so a re-implementation fails.
    - A `send_email` draft still returns the SMTP footer with its TLS-mode tag (POSITIVE CONTROL),
      and the three shipped footer cases at `:595-673` still pass UNEDITED.
    - ⚠ **The synthetic FIFTH shape returns a NEUTRAL footer — not the SMTP one.** No `STARTTLS`
      tag, no `from` detail.

    **`draftFromConnection` — the edit-shape fix (D-206.1-22).**
    - A connection with `mcp_server_url` set and `capability: null` seeds
      `capability: "mcp"` and `mcpServerUrl` equal to the row's URL.
    - A connection with `capability: "send_email"` and no URL still seeds `"send_email"` (POSITIVE
      CONTROL).
    - ⚠ A connection with `capability: null` and **NO** `mcp_server_url` still seeds the empty
      draft's default — absence alone is not MCP (D-206.1-11: absence read as a default is the
      defect).
    - ⚠ A connection with BOTH a capability and an `mcp_server_url` seeds `"mcp"` — the URL wins,
      because MCP is a shape and not a capability.

    **AR-05 — `credentialReadingOf` in `connectionsCopy.ts`.**
    - An MCP row (URL present, never checked) reads `CREDENTIAL_NO_CHECK_FOR_KIND`, NOT
      `"never checked"`. ⚠ *nobody has checked it* and *it cannot be checked* are two different
      facts, and folding them is the `runFacts.ts` CR-01 / `DecisionsList` D-20 defect a fifth time.
    - A capability row with no check still reads exactly `"never checked"`.
    - A capability row with a check still matches `/^checked /`.
    - ⚠ **`credentialLabel` is BYTE-IDENTICAL** — asserted by a source diff being empty for that
      function, so every pinned unit call on it stays intact.

    **The string-sweep gap, stated rather than claimed away.**
    - Either extend a sweep to `connectionFormCopy`'s new identifiers (no `always`, `never fails`,
      `guaranteed`, `secure`, `safe`; no absolute verb), **or** record in the SUMMARY in writing that
      they are UNSWEPT. ⚠ `stringsOfModule()` walks `connectionRefusalCopy` ONLY — measured — so the
      nine-banned-terms and absolute-verb fences **do not** cover these strings today. **Never claim
      inherited coverage.** Extending the sweep is preferred and is cheap.
  </behavior>

  <files>
    frontend/src/components/settings/connectionFormCopy.ts,
    frontend/src/components/settings/connectionsCopy.ts,
    frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx
  </files>

  <action>
    **Widen the type, do not fork the state.** Add `export type ConnectionShape = ConnectorCapability | "mcp"`
    beside `ConnectionDraft` and change `ConnectionDraft.capability` to `ConnectionShape`. Every
    shipped `capability === "send_email" | "create_ticket" | "post_message"` guard stays LITERALLY
    unchanged and simply does not match. ⚠ Two rejected alternatives, recorded so they are not
    re-proposed: a separate `shape` field makes ~10 render guards two-term conjunctions and lets two
    fields disagree; `capability: ... | null` gives `<select value={null}>` an uncontrolled-input
    warning and makes the `:714` `??` fallback ambiguous. The sentinel `"mcp"` NEVER reaches the wire
    — `handleSave` branches before composing the body.

    Add `mcpServerUrl: string` to `ConnectionDraft` (flat, all-string — the footer derives during
    render) and `mcpServerUrl: ""` to `EMPTY_DRAFT`. Widen `CAPABILITY_CHOICES`'s element type to
    `ConnectionShape` and append `{ capability: "mcp", label: CAPABILITY_CHOICE_MCP_LABEL }` **fourth
    and last** — the three capabilities keep their mig-116 order and an appended member cannot
    re-order them. Widen `FIELD_COUNTS` to `Record<ConnectionShape, number>` and add `mcp: 3`.

    **The new strings, each an exported `const` with a one-paragraph docblock naming the decision it
    encodes** — the `connectionFormCopy.ts:111-119` shape:
    - `CAPABILITY_CHOICE_MCP_LABEL` = `Use tools from an MCP server`. The other three name ONE
      action; MCP is not one action, because the grants decide which tools — so the label names the
      CAPABILITY, not the act. ⚠ It must not contain `Jira site` or `SMTP host and port`
      (`ConnectionFormPanel.test.tsx:561-574` asserts both absent on non-matching shapes).
    - `FIELD_MCP_URL_LABEL` = `MCP server URL`. Deliberately distinct from every shipped label so the
      negative `queryByLabelText` cases stay meaningful.
    - `FIELD_MCP_URL_PLACEHOLDER` = `https://mcp.example.com/mcp`. ⚠ A RESERVED example domain, never
      a real vendor's endpoint — a placeholder naming a live third party is an endorsement the form
      is not entitled to make.
    - `FIELD_MCP_URL_HELP` = `Must be an https:// address reachable on the public internet. A plain http:// address is refused, and so is an address inside this network.` — the
      `FIELD_SMTP_HOST_HELP` shape verbatim: state the refusal BEFORE a person can trip it.
    - `FIELD_MCP_SECRET_LABEL` = `Access token`. The client sends Bearer or Basic. ⚠ Not `Bot token`
      (Slack's), not `API token` (Jira's) — a shared label across kinds is how the fourth positional
      ladder becomes invisible.
    - `FIELD_MCP_SECRET_OPTIONAL_NOTE` = `Optional — many MCP servers ask for none. Leaving this empty saves a connection with no credential.` —
      D-206.1-06. Affirmative, not a warning, carrying NO destructive tone, and it renders BESIDE
      `SECRET_CREATE_HELP`, never replacing it (the encryption promise is told for every kind).
    - `MCP_SAVE_DISABLED_REASON` = `Save is off until the server address starts with https://. This form refuses it here; the server refuses it again before anything is sent.` —
      ⚠ **the second clause is LOAD-BEARING and must not be trimmed**: the panel is a courtesy, never
      the security boundary, and this is the only place a person can see that.

    **Ladder 1 — `configFromDraft` (`:248-266`).** Rewrite to the `connectionsCopy.ts:181-222` shape:
    add an explicit `if (draft.capability === "mcp") return { headers: {} }` **first**, make the
    Slack arm name `post_message` explicitly, and end with a NEUTRAL terminal return. ⚠ Today the
    trailing statement IS the Slack arm and the fallback at once, so an MCP draft emits a
    `PostMessageConfig` with an empty `default_channel`, which `NonEmpty` rejects → **422**. Carry a
    docblock naming this as the `destinationFactsOf` defect's shape.

    **Ladder 2 — `destinationFooterOf` (`:354-388`).** Add an explicit `mcp` arm; make the SMTP body
    name `send_email` explicitly; end with a neutral terminal `return`. The MCP arm composes from the
    **shipped** tag vocabulary — `FOOTER_NOTHING_YET` when the URL is empty, `[FOOTER_TAG_TLS]` when
    accepted, `[FOOTER_TAG_REFUSED]` plus `refusedReason` when `refusalOf` refuses. **No new footer
    tag.** ⚠ **Reuse `refusalOf` verbatim; do not re-implement it** — it is already a text-only
    mirror of `validate_mcp_destination`.

    **`draftFromConnection` (`:214-217`) — the edit fix.** Seed
    `capability: connection.mcp_server_url ? "mcp" : (connection.capability ?? EMPTY_DRAFT.capability)`
    and `mcpServerUrl: connection.mcp_server_url ?? ""`. ⚠ **CORRECT THE EXISTING DOCBLOCK BESIDE
    ITS ORIGINAL, never delete it** — it currently reads as a decision (*"this draft describes the
    CAPABILITY form"*) and is in fact a statement of the defect. Keep the original marked superseded,
    and write beside it that the shape is read from the ROW (`connection.mcp_server_url != null`),
    never from a missing capability.

    **AR-05 in `connectionsCopy.ts`.** Add `CREDENTIAL_NO_CHECK_FOR_KIND = "no check for this kind"`
    and a NEW `credentialReadingOf(connection, now)` with EXPLICIT arms: MCP shape → the new string;
    otherwise delegate to the untouched `credentialLabel`. ⚠ **`credentialLabel` stays
    BYTE-IDENTICAL** so every pinned unit call on it stays intact. Docblock the reason: an MCP row
    can NEVER be checked, so rendering `never checked` forever folds *nobody has checked it* and *it
    cannot be checked* into one word — the `runFacts.ts` CR-01 shape a fifth time. Record that this
    is **copy plus one pure derivation arm, not a capability**: no control, no endpoint. Re-open
    trigger: *`Check credential` gains an MCP arm, at which point this string becomes wrong.*

    Write the tests described in `<behavior>` into `ConnectionFormPanel.test.tsx` as new `describe`
    blocks. ⚠ **ADD, never re-baseline** — the 63 shipped cases must pass unedited.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/components/settings --maxWorkers=2</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/settings --maxWorkers=2` exits 0, `failed 0`,
      and `ConnectionFormPanel.test.tsx` reports **>= 63** cases with all 63 shipped ones UNEDITED
    - `git diff -- frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx` shows
      **zero deleted assertion lines** in the shipped cases
    - `grep -c 'capability: "mcp"' frontend/src/components/settings/connectionFormCopy.ts` reads `>= 1`
    - `grep -c "ConnectionShape" frontend/src/components/settings/connectionFormCopy.ts` reads `>= 4`
    - Comment lines stripped, `configFromDraft` has an explicit `post_message` condition:
      `sed -n '/export function configFromDraft/,/^}/p' frontend/src/components/settings/connectionFormCopy.ts | grep -v '^\s*[/*]' | grep -c 'post_message'`
      reads `>= 1`
    - Comment lines stripped, `destinationFooterOf` has an explicit `send_email` condition:
      `sed -n '/export function destinationFooterOf/,/^}/p' frontend/src/components/settings/connectionFormCopy.ts | grep -v '^\s*[/*]' | grep -c 'send_email'`
      reads `>= 1`
    - `grep -c "REFUSAL_PLAINTEXT_SCHEME\|REFUSAL_PRIVATE_LITERAL" frontend/src/components/settings/connectionFormCopy.ts`
      is UNCHANGED from base — `refusalOf` was reused, not re-implemented
    - `grep -c "credentialLabel" frontend/src/components/settings/connectionsCopy.ts` is unchanged,
      and `git diff -- frontend/src/components/settings/connectionsCopy.ts` shows **no modified line
      inside `credentialLabel`'s body**
    - `grep -c "superseded" frontend/src/components/settings/connectionFormCopy.ts` reads `>= 1`, and
      the original `draftFromConnection` docblock text still appears
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` reads the
      re-derived base count (expected **34**), zero in any `settings/Connection*` file
    - `git diff --numstat -- frontend/src/lib/api.ts backend/` is EMPTY
  </acceptance_criteria>

  <done>
    The MCP shape exists in the copy layer: a sentinel that never reaches the wire, a fourth chooser
    entry, three new fields' worth of strings, two ladders rewritten to explicit arms with a
    synthetic-fifth-shape control, the edit-mode seed fixed with its docblock corrected beside the
    original, and an honest credential reading for a row that can never be checked.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConnectionFormPanel.tsx — the fourth option, the MCP field block, the disabled Save, the edit shape, and the two panel ladders</name>

  <read_first>
    - `frontend/src/components/settings/ConnectionFormPanel.tsx:261-290` (`Field`, carrying
      `data-testid="connection-field"` at `:275`), `:292-295` and `:296+` (`TextControl` and its
      *"NOT a `disabled` input"* charter), `:625-626` (`saveBlocked` + `saveDisabledReasonId`),
      `:677-708` (`handleSave` — the create branch at `:684-689` sends `secret: draft.secret`
      UNCONDITIONALLY; the edit branch at `:698` shows the omit idiom), `:712-716` (⚠ the edit-mode
      defect site), `:728-733` (`typedHost`, ladder 3), `:735-740` (`secretLabel`, ladder 4),
      `:843-872` (the chooser), `:874-882` (the static-kind block), `:899-1008` (the three per-shape
      blocks; `:993-1008` is the single-`Field` arm to copy; `:945-965` is the site-field pattern),
      `:1010-1095` (the secret block), `:1128-1137` (the ONE reason node), `:1479-1491` (the Save
      button and its `aria-describedby`)
    - `frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx:48` (`panelSource`
      `?raw`), `:550-560` (⚠ `getAllByTestId("connection-field")` **equals** `FIELD_COUNTS[capability]`,
      walking a literal 3-member array), `:561-574` (the two absent labels), `:583-590` (⚠ on EDIT
      the capability is STATIC TEXT through `connection-capability-static`), `:676-738` (the
      write-only secret cases), `:740-753` (⚠ ZERO `[title]`, create AND edit), `:755-823`
      (non-admin and kill-switch: every write REMOVED), `:826-834` (the `PhaseFormPanel` import
      fence), `:929-1024` (⚠ §4b — EGRESS leaves Save ENABLED, CIPHER disables it and
      `aria-describedby` resolves to a node whose text IS the reason; `:975-998` resolves the id with
      `container.querySelector`, which returns the FIRST match), `:1536-1557` (⚠ the panel authors NO
      sentence of its own)
    - `frontend/src/components/settings/ConnectionsTab.tsx:203-207` (the S7 *"the API is the wall"*
      prop docblock), `:214` and `:650-665` (the `onCheck &&` guard and the
      `connections-action-check` menu item — the D-206.1-19 removal site), `:610` (the credential
      cell's `credentialLabel` call — becomes `credentialReadingOf`), `:958` and `:980`
      (`handleCheck` / `handlePanelCheck`)
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-UI-SPEC.md`
      § Surface 1 (the field table, the footer table, the disabled-Save contract, the edit table, the
      four-ladder table, and § *What the MCP shape does NOT get*)
  </read_first>

  <behavior>
    Assertions first, RED, then green.

    **The chooser and the field set (SC#1a).**
    - The create chooser renders FOUR `<option>`s and the fourth's label is
      `CAPABILITY_CHOICE_MCP_LABEL`, LAST.
    - Selecting MCP renders exactly `FIELD_COUNTS.mcp` = **3** nodes with
      `data-testid="connection-field"`, and the MCP URL input carries `font-mono`.
    - ⚠ The MCP field block does **not leak** into the other three renders — the shipped
      `it.each(["send_email","create_ticket","post_message"])` count case at `:550-560` still passes
      UNEDITED, and a NEW case walks `"mcp"`.
    - `queryByLabelText("Jira site")` and `queryByLabelText("SMTP host and port")` are absent on the
      MCP render (the collision the label was chosen to avoid).

    **The create body (SC#1a, D-206.1-04).**
    - Saving an MCP draft calls `onCreate` with a body whose **key set is exactly**
      `{name, mcp_server_url, config}` — asserted with `Object.keys(...).sort()`, not with
      `toMatchObject`, so an extra key FAILS.
    - ⚠ The body carries **NO `capability` key at all** — `"capability" in body` is `false`. Present
      and null is not the same as absent; assert absence.
    - `config` deep-equals `{ headers: {} }`.
    - With a typed credential the key set gains `secret`; with an EMPTY credential ⚠ the `secret` key
      is **OMITTED**, never sent as `""` (the server's `NonEmpty` rejects `""` as a present value).
    - POSITIVE CONTROL: a `post_message` draft still sends `capability` and a
      `{ default_channel }` config.

    **The disabled Save (D-206.1-05).**
    - With MCP selected and the URL empty, or `http://...`, or `ftp://...`, Save is `disabled` and a
      node with `data-testid="connection-mcp-save-disabled-reason"` renders
      `MCP_SAVE_DISABLED_REASON`.
    - ⚠ **`aria-describedby` resolves to THAT node**, and the shipped CIPHER case at `:975-998` still
      resolves to `CIPHER_UNAVAILABLE_SAVE_DISABLED_REASON` — asserted in the SAME run, because
      `querySelector` returns the FIRST match and two nodes sharing one id is exactly how the shipped
      assertion breaks silently.
    - With a valid `https://` URL, Save is ENABLED.
    - ⚠ ZERO `[title]` attributes in the MCP create render AND the MCP edit render.
    - The reason node is `text-destructive` and sits ABOVE the Save/Cancel footer cluster, inside the
      scrolling body.

    **The edit shape (D-206.1-22).**
    - Opening a connection with `mcp_server_url` set and `capability: null` renders
      `connection-capability-static` containing `CAPABILITY_CHOICE_MCP_LABEL`, the MCP URL field
      seeded with the row's URL, and `CAPABILITY_LOCKED_NOTE`.
    - ⚠ **REGRESSION PINS, all four, because all four are live today:** that render does NOT contain
      `Send an email`, does NOT render an SMTP host or port field, does NOT label the secret
      `App password`, and its footer is not the SMTP footer.
    - The update body for an MCP row is `{ name, mcp_server_url, config: { headers: {} } }`.
    - POSITIVE CONTROL: editing a `send_email` row still renders the SMTP form unchanged.

    **Ladders 3 and 4 (SC#4 extended).**
    - `typedHost` for an MCP draft is the typed URL's host, and for the ⚠ **synthetic FIFTH shape**
      is a NEUTRAL value — **not** `draft.host`.
    - `secretLabel` for an MCP draft is `FIELD_MCP_SECRET_LABEL`, and for the ⚠ synthetic fifth shape
      is a NEUTRAL value — **not** `"Bot token"`.
    - POSITIVE CONTROL in the same block: the three mapped capabilities still resolve their own host
      and label.
    - ⚠ Because both live inside the component, drive them through the RENDER (the secret field's
      label, the footer's host reading) rather than by exporting them — do not widen the module's
      export surface to make a test easier.

    **`destinationFactsOf` regression control (SC#4's named subject).**
    - The synthetic fifth shape (unknown capability, no URL) returns `[]` and **never**
      `slack.com/api`. Already explicit; this is the guard that keeps it so.

    **Removed, never disabled (D-206.1-19 + the shipped rule).**
    - An MCP row's ⋯ menu has NO `connections-action-check` item; a capability row's still does
      (POSITIVE CONTROL).
    - An MCP row's credential cell reads `CREDENTIAL_NO_CHECK_FOR_KIND`; a capability row's still
      reads exactly `never checked` — the `.toBe` equality at `ConnectionsTab.test.tsx:640` holds
      UNEDITED.
    - Under a non-admin caller and under `live_connectors` OFF, the MCP chooser option, the MCP field
      block and Save are all **REMOVED** — mirroring the shipped rule that a `toBeDisabled()`
      assertion was measured unable to see (plant C, 190-16).

    **The source fence.**
    - `panelSource` still contains no inline user-visible sentence — every new MCP string is an
      imported identifier (`ConnectionFormPanel.test.tsx:1536-1557` passes UNEDITED).
    - `panelSource` still imports nothing from `PhaseFormPanel` (`:826-834` passes UNEDITED).
  </behavior>

  <files>
    frontend/src/components/settings/ConnectionFormPanel.tsx,
    frontend/src/components/settings/ConnectionsTab.tsx,
    frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx,
    frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
  </files>

  <action>
    **The chooser (`:843-872`).** Widen the `onChange` cast to `as ConnectionShape`; the
    `CAPABILITY_CHOICES.map` picks up the fourth option with no other change. The markup, classes and
    `focus:border-primary` stay UNCHANGED. On EDIT the kind stays STATIC TEXT through the SAME
    `connection-capability-static` block (`:874-882`) — **not a new block** — rendering
    `capabilityLabelOf(capability)`, with `CAPABILITY_LOCKED_NOTE` still rendering because a kind
    change is still a new row.

    **`const capability` (`:712-716`).** Change the type to `ConnectionShape` and read the shape from
    the ROW: `connection.mcp_server_url != null` ⇒ `"mcp"`. ⚠ **NEVER from `capability === null`
    alone** — absence read as a default is exactly the shape D-206.1-11 forbids. ⚠ **CORRECT THE
    EXISTING DOCBLOCK BESIDE ITS ORIGINAL, never delete it**: it currently reads as a decision
    (*"the draft's own value is the fallback, not a cast: this panel edits capability connections"*)
    and is in fact the defect. Mark the original superseded with its date and write the corrected
    rule beneath it.

    **The MCP field block (inside `:899-1008`).** Add `{capability === "mcp" && ( ... )}` holding ONE
    `Field` for the server URL, following the single-`Field` `post_message` arm at `:993-1008` and
    the site-field pattern at `:945-965`. ⚠ It **MUST** use the shipped `Field`, which is what
    carries `data-testid="connection-field"` — or the count idiom cannot be reused. The control is a
    `TextControl` with `className="font-mono"` (a machine value, like the Jira site field's sibling),
    **full width — one input, one column**, NOT the `1fr 92px` two-column grid (that shape exists for
    host+port, and an MCP URL has no second part). Label `FIELD_MCP_URL_LABEL`, placeholder
    `FIELD_MCP_URL_PLACEHOLDER`, help `FIELD_MCP_URL_HELP`. ⚠ The label and help are prose and are
    **not** `font-mono`.

    **No fourth field.** No headers editor, no transport picker, no timeout, no "test connection" —
    each is surface nobody threat-modelled (the shipped D-32 / T-190-17-SCOPE rule). The wire's
    `config` is `{ headers: {} }` and the form does not offer to fill it.

    **The secret block (`:1010-1095`).** Structurally unchanged. The LABEL comes from ladder 4's new
    arm; `FIELD_MCP_SECRET_OPTIONAL_NOTE` renders **under** `SECRET_CREATE_HELP`, never replacing it.
    ⚠ D-206.1-06: the panel must not require a credential and **must not present its absence as an
    error state** — no destructive tone, no warning treatment, no red.

    **Ladder 3 — `typedHost` (`:728-733`).** Add an explicit `capability === "mcp"` arm returning the
    typed URL's host; end with a neutral terminal value. ⚠ Today an MCP draft falls to
    `draft.host.trim()`, the SMTP host.

    **Ladder 4 — `secretLabel` (`:735-740`).** Add an explicit `capability === "mcp"` arm returning
    `FIELD_MCP_SECRET_LABEL`; end with a neutral terminal value. ⚠ Today an MCP draft falls to
    `FIELD_SLACK_SECRET_LABEL` = **"Bot token"**.

    **`handleSave` (`:677-708`).** In the create branch, branch on `draft.capability === "mcp"` and
    compose `{ name, mcp_server_url: <trimmed typed URL>, config: { headers: {} } }`, spreading
    `secret` **only when the typed secret is non-empty** — copy the `:698` edit-branch omit idiom.
    ⚠ **`capability` is omitted entirely**, not sent as `null`. In the edit branch, an MCP row's
    update body is `{ name, mcp_server_url, config: { headers: {} } }` — ⚠ `ConnectorConnectionUpdate`
    performs NO cross-field validation, so the panel composing it correctly is the only guard, and
    that sentence goes in the source.

    **The disabled Save — net-new machinery, not a tweak.** Today `saveBlocked` is ONLY
    `saveRefusal?.kind === "cipher"`. Add a SECOND, independent condition:
    `draft.capability === "mcp"` AND the typed URL does not start with `https://` (including empty).
    ⚠ **The reason node needs its OWN id — `${fieldId}-mcp-save-disabled-reason` — NEVER
    `saveDisabledReasonId`.** `ConnectionFormPanel.test.tsx:975-998` resolves `aria-describedby` with
    `container.querySelector`, which returns the FIRST match, so two simultaneously-rendered nodes
    carrying one id break the shipped cipher assertion SILENTLY. `aria-describedby` on the Save button
    must SELECT between the two ids. The node is a real DOM `<p>`,
    `text-[11px] leading-snug text-destructive`,
    `data-testid="connection-mcp-save-disabled-reason"`, rendering `MCP_SAVE_DISABLED_REASON`, placed
    directly above the Save/Cancel footer cluster inside the scrolling body — a reason a person must
    scroll to find is a reason they will not read. The Save button RENDERS, `disabled`, at
    `disabled:opacity-60` (the shipped disabled treatment) — **not removed**, because removal is
    reserved for the kill-switch and non-admin cases, which are a different fact.
    ⚠ **A `title` tooltip is FORBIDDEN** (`:740-753` asserts zero `[title]` in create AND edit). The
    reason is DOM text or it does not exist.
    ⚠ **Write the S7 sentence in the source beside the predicate**: the panel's check is a COURTESY,
    never the security boundary — the model raises on a non-HTTPS `mcp_server_url` and
    `validate_mcp_destination` refuses again at call time, after the address resolves, every time a
    step sends. Cite `models/connector.py`'s own WR-05 note: *a disabled Save is kinder than a 422.*

    **`ConnectionsTab.tsx` — D-206.1-19 and AR-05.** In the ⋯ menu (`:650-665`), gate
    `connections-action-check` so an MCP row does not render it at all — keep the shipped
    `onCheck &&` guard AND add the shape condition. ⚠ **The affordance is REMOVED, not disabled**,
    and the reason goes in SOURCE at the removal site, **not** as a user-visible sentence (a removed
    affordance that explains itself is a disabled affordance wearing a disguise): the check path is
    capability-shaped (SMTP login / Jira auth / Slack `auth.test`) and has **no MCP arm**, so a
    rendered control the API refuses is the defect this rule exists to prevent. Switch the credential
    cell (`:610`) from `credentialLabel` to `credentialReadingOf`. ⚠ **Do not disturb the dense
    shape plan 02 built** — the credential value node's textContent must stay character-identical for
    capability rows.

    **What this phase does NOT add, recorded with its re-open trigger:** no Discover/Grant door in
    Settings (both already ship at the builder seam — `McpToolPicker.tsx`'s `mcp-discover-btn`,
    mounted through `ConnectionPicker.tsx`; a second door is a NEW capability inside a phase that
    already carries one). Re-open: *the first time a person must inspect or revoke a grant without
    opening a workflow.* No fourth filter chip (`CONNECTIONS_FILTER_CHIPS` is capability-keyed; MCP
    rows are reachable under `All`). No fifth state word (`CONNECTION_STATE_WORDS` is walked by a
    character-identity assertion with one fixture row per member; an MCP row renders `◌ Not checked`,
    which is literally true, and the *why* is carried by the Credential cell instead).
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/components/settings --maxWorkers=2</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/settings --maxWorkers=2` exits 0, `failed 0`
    - `ConnectionFormPanel.test.tsx` reports **>= 78** cases with all 63 shipped ones UNEDITED;
      `ConnectionsTab.test.tsx` reports **>= 52** with all 36 shipped ones UNEDITED
    - `git diff -- frontend/src/components/settings/__tests__/` shows **zero deleted assertion lines**
      in any shipped case, and **no modification to plan 02's committed capture literals**
    - `grep -c "connection-mcp-save-disabled-reason" frontend/src/components/settings/ConnectionFormPanel.tsx`
      reads `>= 1`
    - The two reason ids are DISTINCT — `grep -c "saveDisabledReasonId" frontend/src/components/settings/ConnectionFormPanel.tsx`
      is unchanged from base, and the new id string is a different literal
    - `grep -c "title=" frontend/src/components/settings/ConnectionFormPanel.tsx` reads `0`
    - Comment lines stripped, the panel has explicit MCP arms in both ladders:
      `grep -v '^\s*[/*]' frontend/src/components/settings/ConnectionFormPanel.tsx | grep -c '=== "mcp"'`
      reads `>= 4` (typedHost, secretLabel, the field block, the save branch)
    - `grep -c "superseded" frontend/src/components/settings/ConnectionFormPanel.tsx` reads `>= 1`,
      and the original `:712-716` docblock text still appears
    - `grep -c "PhaseFormPanel" frontend/src/components/settings/ConnectionFormPanel.tsx` reads `0`
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` reads the
      re-derived base count (expected **34**), zero in any `settings/Connection*` file
    - `git diff --numstat -- frontend/src/lib/api.ts backend/ supabase/` is EMPTY
  </acceptance_criteria>

  <done>
    `Add a connection` offers a fourth shape; selecting it gives exactly three fields; Save is
    disabled with a real DOM reason on a non-HTTPS URL and its `aria-describedby` resolves to a node
    distinct from the cipher one; the create body carries `mcp_server_url` and NO `capability`; an
    empty credential is omitted rather than sent; editing an existing MCP row renders the MCP shape
    instead of the SMTP form; all four positional ladders have named arms proved against a synthetic
    fifth shape; and an MCP row offers no `Check credential` and says so honestly in its credential
    cell.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BROWSER TIER — drive the round trip end to end, no SQL (SC#1b)</name>

  <read_first>
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-RESEARCH.md`
      § OQ#6 Steps 1-2 (reaching the tab without a URL router) and § Environment Availability
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-VALIDATION.md`
      § Manual-Only Verifications — the SC#1b row
    - `frontend/src/pages/SettingsPage.tsx:531,536` (`localStorage settings_active_tab`) and `:900`
      (`<TabsTrigger value="5">Connections</TabsTrigger>` — the key AND value verified against
      shipped source, not assumed)
    - `frontend/src/components/workflows/McpToolPicker.tsx` — `data-testid="mcp-discover-btn"` and
      the per-tool grant controls; `frontend/src/components/workflows/ConnectionPicker.tsx` — how
      the picker is mounted inside the workflow builder (D-206.1-18: this is where Discover and
      Grant live, and Settings gains neither)
    - `.planning/phases/206-mcp-connector-client-workflow-scoped/206-01-SUMMARY.md` — what 206
      shipped downstream of the row
  </read_first>

  <what-built>
    Tasks 1 and 2 shipped the create door and the edit-shape fix, and pinned their SHAPE in vitest
    (SC#1a). ⚠ **SC#1's acceptance is not *"the POST returns 201"*** — the ROADMAP's first named
    failure mode is *"the form accepts an MCP connection but the row it creates is one the picker
    cannot bind."* This task proves it can.

    ⚠ **THE OPERATOR IS AWAY AND WILL NOT BE TYPING "approved". A checkpoint that waits for a human
    is, in this run, a checkpoint that never returns** — and this one gates the ONLY evidence for
    SC#1b. Every acceptance criterion below is decided by a recorded `evaluate_script` reading; there
    is nothing left for a human to judge that the measurement has not already decided.

    > If **every** acceptance criterion below is objectively met by the recorded measurements, record
    > this checkpoint as `auto-approved-with-evidence`, attach the raw readings, and continue.
    > **Escalate and STOP only when a measurement actually fails**, is missing, or the non-vacuity
    > control (the per-step round-trip evidence, recorded step by step rather than as one blob) did not hold — those are the cases a human is needed for, and a green run
    > is not one of them.

    ⚠ This is a real gate, not a rubber stamp: the escape hatch is conditional on the readings, and a
    FAILING or ABSENT reading still stops the plan.
  </what-built>

  <how-to-verify>
    **Preconditions.** Local infra UP: backend `:8000`, frontend `:5173`, Supabase `:54321`. Bring it
    up with `powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1` if needed.
    A live MCP server that answers `tools/list` unauthenticated: `https://mcp.deepwiki.com/mcp` —
    206 drove discovery against it.

    ⚠ **There is NO URL router** (SEED-185). Reach the tab by `navigate_page` to
    `http://localhost:5173/`, `evaluate_script` setting `localStorage.setItem("settings_active_tab", "5")`
    and reading it back, then `navigate_page` again. `"5"` is Connections, verified against
    `SettingsPage.tsx:900`.

    ⚠ `take_screenshot` is recorded as TIMING OUT here — read DOM state via `evaluate_script`.

    **The steps, each with its OWN recorded evidence — never one blob at the end:**

    1. **Add a connection → MCP.** Click `connections-add`, select the fourth chooser option. Record
       the chooser's four option labels and the rendered `connection-field` count (must be 3).
    2. **Type `https://mcp.deepwiki.com/mcp`, leave the credential EMPTY.** Record that Save is
       ENABLED and that `connection-mcp-save-disabled-reason` is absent. ⚠ Also record the negative:
       type `http://mcp.deepwiki.com/mcp` first, confirm Save is DISABLED and the reason node renders
       `MCP_SAVE_DISABLED_REASON`, then correct it.
    3. **Save.** Record the network response status and the returned row's `capability` (must be
       `null`) and `mcp_server_url`.
    4. **The row.** Record that the list row's destination reads `mcp.deepwiki.com` and that its mark
       is the MCP mark — assert the mark's `innerHTML` is non-empty AND its resolved `fill` is not
       `rgb(0, 0, 0)` (⚠ the black-on-black trap from plan 01; the operator's eye or a computed-style
       read are the only tiers that reach it). Record that the row's ⋯ menu has NO
       `connections-action-check` item.
    5. **Discover.** Open a workflow's `external_action` step in the BUILDER, pick this connection,
       click `mcp-discover-btn`. Record the number of tools discovered — must be **>= 1**.
    6. **Grant.** Grant exactly one tool. Record the tool name and the grant response.
    7. **Call.** Run the workflow so the `external_action` step calls the granted tool. Record the run
       id, the step's outcome, and the **send receipt** the run records.
    8. **Re-open the row for EDIT.** Record that the panel renders the MCP kind (not `Send an email`),
       seeds the URL, and shows no SMTP host/port field and no `App password` label. ⚠ This is
       D-206.1-22 proved in the browser, where it was found.

    **Deploy-parity note that must be recorded, not discovered later:** `BUG-260810-01` is open —
    *Settings → Connections shows no Add connection button on cloud (local is fine)*, very likely
    `feature_visibility` drift. **This phase adds a FOURTH write affordance to the exact surface that
    is already invisible in cloud, so the MCP option will be missing in cloud for the same reason the
    Add button is.** Record that in the SUMMARY under a deploy-parity note. ⚠ **Do NOT fold or close
    that bug here** — this phase is frontend-only and local-only, and "fixing" it would mean weakening
    the render-side mirror of a real server gate, which both suites explicitly forbid.
  </how-to-verify>

  <acceptance_criteria>
    - Evidence recorded **per step, all eight**, in one Chrome session
    - ⚠ **NO `INSERT INTO connector_connections` anywhere in the evidence** — and no other direct
      database write. The row is created through the UI or the criterion is not met (D-206.1-07)
    - The created row's `capability` is `null` and its `mcp_server_url` is the typed URL
    - Discovery returns **>= 1** tool — ⚠ a 404 or 422 here means the picker CANNOT BIND the row,
      which is the ROADMAP's first named failure mode and a FAILURE of SC#1, not a note
    - Exactly one tool is granted, and the run's `external_action` step calls it and records a send
      receipt
    - Step 2's negative half is recorded: `http://` ⇒ Save disabled + the reason node;
      `https://` ⇒ Save enabled + no reason node
    - Step 4's mark check records both `innerHTML.length > 0` AND a resolved `fill` that is not pure
      black
    - Step 8 records the four edit-mode regression facts (MCP kind, seeded URL, no SMTP fields, no
      `App password`)
    - The `BUG-260810-01` deploy-parity note is recorded and the bug's `status` is left `open`
  </acceptance_criteria>

  <action>
    CHECKPOINT TASK — the work is the driven round trip, not a code change. Run the eight steps in
    `<how-to-verify>` in ONE Chrome DevTools MCP session, recording evidence per step: create via
    `Add a connection` -> MCP -> `https://mcp.deepwiki.com/mcp` with an empty credential (including
    the `http://` negative half), Save, read the row's destination and mark, open the workflow
    builder's `external_action` step, `mcp-discover-btn`, grant one tool, run the workflow and read
    the send receipt, then re-open the row for EDIT. ⚠ NO direct database write anywhere in the
    evidence. Record the `BUG-260810-01` deploy-parity note and leave that bug `open`.
  </action>

  <done>
    An MCP connection was created entirely in the UI, the picker BOUND it (discovery returned at
    least one tool), one tool was granted, a workflow step called it and the run recorded a send
    receipt, the edit path rendered the MCP shape, and no `INSERT INTO connector_connections` appears
    anywhere in the evidence.
  </done>

  <resume-signal>Type "approved" or paste the failing step's evidence</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Pay the hot-file ledger debt — four rows and four sections, same commit — and re-derive every phase baseline</name>

  <read_first>
    - `CLAUDE.md` § *Hot-file ledger* — the table's own charter (*"this table is the AUDIT SCAN LIST
      and is deliberately complete"*), the ⚠ SAME-COMMIT SYNC RULE, and the re-derive recipe
    - `docs/HOT-FILE-LEDGER.md:15-20` (the sync rule verbatim and the *never overwrite a superseded
      figure* rule), `:550-562` (`WorkflowDoorSwitch.tsx` — the G-5-FIRES section shape),
      `:925-945` (`toolNames.ts` — the young-file section shape), and
      `backend/app/db/workflows.py`'s section, which ⚠ **names `ConnectionsTab.tsx:841` as a fourth
      consumer with no row of its own** — the `useTemplateFirstDraft.ts` drift
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-PATTERNS.md`
      § Analog 8 (the row format cell-by-cell, both section shapes, the anchor spellings, the four
      pairs owed, and its *What NOT to copy*)
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-RESEARCH.md`
      § OQ#8 (the measured triples, the ledger-absence greps, and the candidate seams)
    - `scripts/check-claude-md-size.cjs` — the 150,000-character hard limit and the 120,000 warn band
  </read_first>

  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>

  <action>
    ⚠ **All four files cross the G-5 threshold in THIS phase's own commit, and all four are absent
    from CLAUDE.md's table AND from `docs/HOT-FILE-LEDGER.md`** — measured:
    `grep -c "ConnectionsTab.tsx\|ConnectionFormPanel.tsx\|connectionsCopy.ts\|connectionFormCopy.ts" CLAUDE.md`
    returned **0**. An absent row means G-5 could never have fired on any of them at any count — the
    `backend/app/config.py` headline (invisible for the project's entire life), repeated on the
    Settings surface. ⚠ And `ConnectionsTab.tsx` is already NAMED inside another file's section with
    no row of its own, which is exactly the drift the same-commit sync rule forbids. **Adding its row
    does not license removing that mention; leave `db/workflows.py`'s section alone.**

    **Step 1 — RE-DERIVE all five triples at THIS commit.** Do not copy RESEARCH's figures
    (`5/2/991`, `3/2/1584`, `2/2/477`, `2/2/531`) and do not increment a cell by hand — this
    ledger's own repeated finding is that a triple goes stale on the next commit that touches the
    file, and five rows were found wrong the last time the table was re-derived, three of them
    reading `satisfied`. Use CLAUDE.md's own recipe per file:
    `git log --oneline -- <file> | wc -l` for commits;
    `git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | grep -E '^[0-9]+(\.[0-9]+)?$' | sort -u | wc -l`
    for phases; `wc -l <file>` for lines. ⚠ Six-digit dated-quick-task buckets are NOT phases —
    subtract them (RESEARCH measured **none** here; every bucket was `190` or `206`, and `206.1`
    joins them).

    **Step 2 — four rows in CLAUDE.md's table**, five cells each in the shipped format: a linked path
    (anchor = the path lowercased with non-alphanumerics stripped) · the re-derived
    `commits / phases / lines` · the verdict (`⚠ **FIRES**` / `**FIRES**`) · the disposition with the
    ⚠ notes a future editor is bound by. ⚠ Each row must say **explicitly** that the file was
    **ABSENT from this table** until 206.1 and that G-5 could therefore never have fired on it —
    that sentence is the point of the row, not decoration.

    **Step 3 — four sections in `docs/HOT-FILE-LEDGER.md`, in the SAME COMMIT.** A row without a
    section, or a section without a row, IS the drift. Each section carries the re-derived triple,
    the G-5 verdict, the phases-touched derivation, and either the SEAM TAKEN or the
    honoured-by-construction reason. ⚠ **Do not claim a seam was taken when it was honoured by
    construction** — say which, and name the NEXT seam explicitly. Three of this table's rows read
    `satisfied` for ~29 phases while being wrong.

    Seams, named rather than left implied:
    - `ConnectionsTab.tsx` — **honoured by construction (206.1)**; next seam NAMED: `ConnectionRow` +
      its two confirm sheets (`:495-775`, ~280 L) → `ConnectionRow.tsx`. Item 2 DOUBLED this
      component's render logic, so the case is now stronger than when the phase started; record that.
      ⚠ Also record the invariants a future editor is bound by: ZERO `[title]` nodes on this surface
      (asserted with a menu and a sheet open, so a tooltip is not available as a truncation fix); the
      wide render is pinned byte-for-byte by an `outerHTML` capture and a diff there is a BEHAVIOUR
      CHANGE, not a test to update; `connections-row-credential`'s textContent is asserted by EXACT
      EQUALITY, which is why the dense inline label is a SIBLING node.
    - `ConnectionFormPanel.tsx` — **honoured by construction (206.1)**; next seam NAMED: the now-FOUR
      per-shape field blocks (`:899-1008`) → `ConnectionShapeFields.tsx`, exactly the seam
      `PhaseFormPanel.tsx`'s row already names for its seven per-type blocks. ⚠ **This phase added
      the FOURTH block, which is the moment to name it.** Invariants: the panel authors NO sentence of
      its own (source fence); it imports NOTHING from `PhaseFormPanel` (source fence); ⚠ **two
      simultaneously-rendered disabled-Save reason nodes must carry DISTINCT ids**, because
      `aria-describedby` is resolved with `querySelector`, which returns the FIRST match.
    - `connectionsCopy.ts` — **no seam proposed**, the `libraryVocabulary.ts` verdict (a vocabulary
      doing one thing many times is the right shape); **the value of the row is that the audit can
      ASK.** ⚠ Invariant: `destinationFactsOf`'s arms are EXPLICIT and its tail is NEUTRAL — the
      `147f3c57` repair, after a positional trailing `return` made every MCP row claim it sends to
      `slack.com/api`, seen on screen in live UAT.
    - `connectionFormCopy.ts` — **no seam proposed**, same verdict. ⚠ Invariants: its four ladders
      end with a NEUTRAL terminal return and every arm names its own condition (206.1 rewrote two of
      them here and two in the panel); ⚠ **`stringsOfModule()` walks `connectionRefusalCopy` ONLY**,
      so strings added here are NOT swept by the banned-terms or absolute-verb fences — record
      whichever this phase did (extended the sweep, or left them unswept) and never let a future
      reader infer inherited coverage.
    - `connectionMark.tsx` — a **young** row and section (the `toolNames.ts` shape), ⚠ **listed BELOW
      the threshold ON PURPOSE** (the `fileIcon.tsx` / `libraryRow.ts` precedent), because a file
      escapes G-5 for years purely by not being written down — `WorkflowsPage.tsx` for ten phases,
      `config.py` for its entire life. Invariants: read through `hasOwnProperty.call`, NEVER a
      coalesced bracket read; the MCP arm is resolved by `mcp_server_url`, never by a missing
      capability; the miss returns the NAMED NEUTRAL, never `null`; ⚠ the MCP mark carries
      `fill-current` because its body has ZERO fills and would otherwise ship INVISIBLE on Deep
      Midnight while every test stayed green; ⚠ a `fill-*` utility must NEVER touch a lucide glyph.

    **Step 4 — the size gate.** Run `node scripts/check-claude-md-size.cjs` after editing CLAUDE.md.
    ⚠ Hard limit **150,000 characters**, warn band 120,000, and the file has already tripped once and
    was over for three commits before anyone noticed. **Keep the prose in the detail file** — the
    table keeps the verdict and the binding ⚠ notes; the narrative goes to
    `docs/HOT-FILE-LEDGER.md`. Five rows of this length are ~2-4k characters. Record the gate's exit
    code and the measured character count in the SUMMARY.
    ⚠ **MEASURED AT PLANNING TIME (2026-08-25): `116,960` chars — 78% of the hard limit, with
    `33,040` of headroom but only `3,040` before the 120,000 WARN BAND.** Five rows will very likely
    cross that band. **Crossing the warn band is not a failure and must not be worked around by
    writing a thinner row** — the band exists so the split is SCHEDULED rather than scrambled. If the
    gate reports the warn band, record it in the SUMMARY and say plainly that CLAUDE.md is now due a
    split-by-FUNCTION (the verdict / index / audit-scan-list stays; the narrative moves to
    `docs/<TOPIC>.md` under the same-commit sync rule). ⚠ **The TABLE stays complete** — a hot file
    missing from the scan list is permanently invisible to its own guardrail, which is the exact
    failure these five rows exist to end. Only PROSE leaves.

    **Step 5 — re-derive and record every phase baseline (SC#5).** Backend unit, project-scoped tsc,
    and the count gate, all three at this commit, with the count gate's verdict line read VERBATIM.
  </action>

  <verify>
    <automated>node scripts/check-claude-md-size.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/check-claude-md-size.cjs` exits **0**, and the measured character count is
      recorded in the SUMMARY
    - `grep -c "components/settings/ConnectionsTab.tsx" CLAUDE.md` reads `>= 1`, and the same for
      `ConnectionFormPanel.tsx`, `connectionsCopy.ts`, `connectionFormCopy.ts` and `connectionMark.tsx`
    - `grep -c "components/settings/ConnectionsTab.tsx" docs/HOT-FILE-LEDGER.md` reads `>= 1`, and the
      same for the other four — ⚠ **five rows and five sections, in the SAME commit.** A row without
      a section, or a section without a row, IS the drift
    - Every anchor in a new CLAUDE.md row resolves to a heading that exists in
      `docs/HOT-FILE-LEDGER.md` (check each link target by grepping the heading text)
    - The triples in the rows equal the output of CLAUDE.md's own recipe run at this commit —
      re-derived, not copied from RESEARCH's `5/2/991` / `3/2/1584` / `2/2/477` / `2/2/531`
    - `git diff -- docs/HOT-FILE-LEDGER.md` shows **no modification** to `backend/app/db/workflows.py`'s
      section — its mention of `ConnectionsTab.tsx:841` is left alone
    - Each of the four FIRES sections says **honoured by construction** or **seam taken**, explicitly,
      and names the NEXT seam where the seam was not taken
    - `cd backend && venv/Scripts/python -m pytest tests/unit -q` reads **`68 failed`** (unchanged)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` reads the
      re-derived base count (expected **34**), with **zero** errors in any `settings/Connection*` or
      `connectionMark*` file — ⚠ there were zero there at the phase base, so any error there is
      unambiguously this phase's
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT exits 0 with
      `count gate OK`, no per-file DECREASE, `failed 0`. The verdict line is pasted VERBATIM into the
      SUMMARY alongside the phase-base reading
      (`total 5467 · failed 0 · pinned total 5004` / `110/110 pinned files present`), and a LARGER
      grand total is recorded as **the gate WORKING**, never as drift
  </acceptance_criteria>

  <done>
    Five hot-file rows and five detail sections exist in the same commit, with triples re-derived at
    this commit rather than quoted, each carrying its G-5 verdict, its named seam or its recorded
    honoured-by-construction reason, and the ⚠ invariants a future editor is bound by. CLAUDE.md is
    under the size gate. Every phase baseline is re-derived and recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author-supplied `mcp_server_url` → the backend's outbound HTTP client | An arbitrary URL typed by a person becomes a destination the server connects to |
| The form's composed body → `ConnectorConnectionCreate` | A client-composed JSON object crosses into a strict Pydantic validator |
| The optional credential → cipher → storage | A plaintext secret exists in the browser for exactly one form submission |
| `isOrgAdmin` / `liveConnectorsOn` → the write affordances | A render-only mirror of two real API gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-206.1-03-SSRF | Information Disclosure / Elevation | `mcp_server_url` | transfer + mitigate | **The wall is the server**: `_validate_connection_shape` raises on any non-`https://` URL, and `validate_mcp_destination` refuses unresolvable / loopback / RFC1918 / `169.254.169.254` / IPv6 site-local again AT CALL TIME. The panel mirrors it with `refusalOf` and a disabled Save — a COURTESY, and the source says so where the predicate lives (D-206.1-05). ⚠ The panel is never described as the gate. |
| T-206.1-03-SP | Spoofing | the four positional ladders | mitigate | Each gets a NAMED arm plus a synthetic-FIFTH-shape negative control returning a neutral — never Slack's host, `{default_channel:""}`, the SMTP footer or `"Bot token"`. This is the `destinationFactsOf` defect, which shipped and made every MCP row claim it sends to `slack.com/api` (seen on screen in live UAT). |
| T-206.1-03-EL | Elevation of Privilege | the MCP chooser option and field block | mitigate | Under a non-admin caller or `live_connectors` OFF, the option, the block and Save are all **REMOVED**, never disabled — the shipped 185 rule, asserted, because a `toBeDisabled()` assertion was measured unable to see plant C at 190-16. The real walls are `require_org_manage` and `require_visible("live_connectors")` on the API. |
| T-206.1-03-ID | Information Disclosure | the optional MCP credential | mitigate | Unchanged shipped machinery: a write-only `<input type="password">` on create, `SECRET_DOTS` in a `<span>` on edit, and the needle scan over `container.innerHTML`. ⚠ `secret: ""` is OMITTED, never sent — a present empty value is a 422 and a pointless plaintext round trip. |
| T-206.1-03-TA | Tampering | the `aria-describedby` resolution on Save | mitigate | The MCP reason node gets its OWN id (`${fieldId}-mcp-save-disabled-reason`). ⚠ Two nodes sharing `saveDisabledReasonId` break `querySelector`'s FIRST-match resolution and would silently invalidate the shipped cipher assertion — asserted in the same run. |
| T-206.1-03-RE | Repudiation | the edit path's silent 422 | mitigate | Today an MCP row's panel composes a `SendEmailConfig` body that 422s into the generic *"Couldn't save that — try again."* — a failure a person cannot attribute. D-206.1-22's fix is pinned with four regression assertions. |
| T-206.1-03-VIS | Elevation (by absent control) | `Check credential` on an MCP row | mitigate | REMOVED, not disabled, with the reason in SOURCE at the removal site — a rendered control the API refuses is the defect that rule exists to prevent. The credential cell reads `no check for this kind` rather than folding two different facts into `never checked`. |
| T-206.1-03-SC | Tampering | package installs | n/a | **No package is installed by this plan.** The single install is plan 01's, gated there. |
| T-206.1-03-PARITY | Denial of Service (by configuration) | the MCP option in cloud | accept + record | `BUG-260810-01` is open: this surface's Add button is already invisible in cloud, very likely `feature_visibility` drift. The MCP option will be missing there for the same reason. Recorded as a deploy-parity note; **not fixed here**, because "fixing" it means weakening the render-side mirror of a real server gate. |
</threat_model>

<verification>
Run at the END of this plan:

1. `cd frontend && npx vitest run src/components/settings --maxWorkers=2` — exits 0, `failed 0`,
   `ConnectionFormPanel.test.tsx >= 78`, `ConnectionsTab.test.tsx >= 52`, every shipped case and
   every plan-02 capture literal UNEDITED
2. `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` — the re-derived
   base count (expected **34**), zero in `settings/Connection*` or `connectionMark*`. ⚠ The bare
   `npx tsc --noEmit` checks ZERO files (D-206.1-16) and must not be used
3. `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — verdict line
   VERBATIM. `count gate OK`, no per-file DECREASE, `failed 0`. ⚠ If red, capture failing filenames
   from the gate's own persisted JSON BEFORE re-running, check each against `git diff --numstat`,
   then decide flake vs real (SEED-171; `196-08` measured 249 failures that were ALL real).
   **Never reach for the worker cap.**
4. `cd backend && venv/Scripts/python -m pytest tests/unit -q` — still `68 failed`
5. `node scripts/check-claude-md-size.cjs` — exit 0
6. `bash scripts/check-deploy-drift.sh` — exit code recorded
7. `node scripts/check-gap-closure-rounds.cjs 206.1` — run BEFORE emitting any `--gaps` routing (G-7)
8. Task 3's eight-step browser evidence pasted into the SUMMARY, with the absence of any direct
   database write stated explicitly
</verification>

<success_criteria>
- **SC#1** is met on both tiers: the vitest SHAPE half (four chooser options, exactly three MCP
  fields, a create body whose key set is `{name, mcp_server_url, config}` with NO `capability` key,
  the edit-mode MCP shape) AND the driven round trip — create in the UI → discover ≥ 1 tool → grant
  one → an `external_action` step calls it and the run records a send receipt, **with no
  `INSERT INTO connector_connections` anywhere in the evidence**.
- **SC#4** is met and EXTENDED past its own wording: `destinationFactsOf` stays explicit, and all
  four form-file ladders gain named arms proved against a synthetic fifth shape with a positive
  control.
- **SC#5** is met: backend `68 failed` unchanged, tsc at its re-derived base with zero errors in any
  `settings/Connection*` file, count gate `OK` with no per-file decrease and zero failing.
- D-206.1-22's edit defect is fixed and pinned; D-206.1-19's `Check credential` is REMOVED with its
  reason in source; D-206.1-18's Discover/Grant stay at the builder seam.
- Five hot-file rows and five detail sections land in this phase's own commit with re-derived triples
  (D-206.1-24), and CLAUDE.md is under the 150,000-character gate.
- `frontend/src/lib/api.ts`, `backend/` and `supabase/` are all untouched.
</success_criteria>

<output>
Create `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-03-SUMMARY.md`
when done. Record: the eight-step browser evidence with the no-direct-DB-write statement; the
re-derived ledger triples beside RESEARCH's originals (never over them); the CLAUDE.md character
count and gate exit; the verbatim count-gate verdict line beside the phase-base reading, with any
growth named as the gate WORKING; whether the `connectionFormCopy` string sweep was EXTENDED or the
new strings were left UNSWEPT (stated plainly, never as inherited coverage); and the
`BUG-260810-01` deploy-parity note with the bug left `open`.
</output>
