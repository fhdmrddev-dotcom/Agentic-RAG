---
phase: 206.1
slug: settings-connections-finishes-the-mcp-story-insert
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-25
---

# Phase 206.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `206.1-RESEARCH.md` § Validation Architecture — every figure below was
> re-derived at this phase's base commit, not quoted from a prior commit's message.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.x, `environment: "jsdom"`, `globals: true` |
| **Config file** | `frontend/vitest.config.ts` — `plugins: [react(), Icons({ compiler: "jsx", jsx: "react" })]` |
| **Quick run command** | `cd frontend && npx vitest run src/components/settings --maxWorkers=2` |
| **Full suite command** | *(from the REPO ROOT)* `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` |
| **Typecheck** | `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — **project-scoped or it measures nothing** |
| **Backend (regression only)** | `cd backend && venv/Scripts/python -m pytest tests/unit -q` |
| **Estimated runtime** | quick ~25 s · typecheck ~40 s · gate ~4 min · backend ~3 min |

### Baselines, re-derived at this phase's base

| Gate | Base reading |
|---|---|
| backend unit | `68 failed, 2605 passed` |
| tsc `-p tsconfig.app.json` | **34** errors — **zero** in any `settings/Connection*` file |
| count gate verdict line (verbatim) | `total 5467 · failed 0 · pinned total 5004` / `count gate OK — 110/110 pinned files present, no per-file decrease, 0 failing.` |
| `ConnectionsTab.test.tsx` | 36 cases |
| `ConnectionFormPanel.test.tsx` | 63 cases |

⚠ A **larger** grand total is the gate WORKING. Its contract is *no per-file DECREASE* and *zero
failing* — never a fixed total.

---

## Sampling Rate

- **After every task commit:** `cd frontend && npx vitest run src/components/settings --maxWorkers=2`
- **After every plan:** `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` (must read **34**)
- **Before `/gsd:verify-work`:** the full count gate **plus the browser evidence for SC#1 and SC#2**
- **Max feedback latency:** ~25 s (quick), ~4 min (gate)

---

## Per-Criterion Verification Map

| SC | What PROVES it | What DISPROVES it | Tier | Command |
|----|----------------|-------------------|------|---------|
| **SC#1a** (shape) | chooser renders 4 options; selecting MCP renders exactly the MCP field set; the create body's key set is `{name, mcp_server_url, config}` (+`secret` only when typed) and carries **no `capability` key**; edit of an MCP row renders the MCP kind | any `capability` key on the body; edit rendering "Send an email" | vitest ✅ | `npx vitest run src/components/settings/__tests__/ConnectionFormPanel.test.tsx --maxWorkers=2` |
| **SC#1b** (round trip) | one Chrome session, no SQL: Add → MCP → `https://mcp.deepwiki.com/mcp` → Save → row shows `mcp.deepwiki.com` + MCP mark → Discover ≥ 1 tool → grant one → an `external_action` step calls it and the run records a send receipt. **Evidence per step.** | discover 404/422 (the picker cannot bind it); a non-null `capability` on the created row; an `INSERT INTO connector_connections` anywhere in the evidence | **browser + live API** ❌ vitest | Chrome DevTools MCP |
| **SC#2a** (shape) | both variants render; `data-dense` reflects the flag; **all six** row testids resolve in BOTH; the dense destination cell's class list has **no `truncate`**; the WIDE render is byte-identical to base (`outerHTML` capture) | a testid missing from either render; `truncate` surviving in dense | vitest ✅ | `npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx --maxWorkers=2` |
| **SC#2b** (geometry) | at 1280 **and** 1536, panel open: `bodyClientW > 0` (non-vacuity control), and for **every** `connections-row-destination` incl. one > 45 chars: `scrollWidth <= clientWidth` **and** `whiteSpace !== "nowrap"` | any `cellFits: false`; `cellFits: true` while `nowrap` (a short fixture, not a working shape); `bodyClientW === 0` (measured nothing) | **browser** ❌ vitest | Chrome DevTools MCP `evaluate_script` |
| **SC#3** | the `~icons/logos/...` imports resolve (**the suite loading at all is the proof**); each mark's `svg.innerHTML.length > 0`; the four bodies pairwise DISTINCT; an `mcp_server_url` row's mark `!==` Slack's and `!==` Jira's; an unknown shape returns the **named neutral**, never nothing; marks come from ONE module | `Icon \`logos/x\` not found` at import; any body length 0; two services sharing a body; an MCP row wearing Slack's mark; a blank fallback | vitest ✅ **fully** | `npx vitest run src/components/settings --maxWorkers=2` |
| **SC#4** | all four shipped shapes return the right facts, **plus a synthetic FIFTH shape** (unknown capability, no URL) returning `[]`. ⚠ **Extended to the four ladders in the form file** — `configFromDraft`, `destinationFooterOf`, `typedHost`, `secretLabel` | the fifth shape returning Slack's host, `{default_channel:""}`, an SMTP footer, or "Bot token" | vitest ✅ **fully** | `npx vitest run src/components/settings --maxWorkers=2` |
| **SC#5** | backend `68 failed`; tsc `34`; gate verdict `count gate OK`, `ConnectionsTab.test.tsx` ≥ 36, `ConnectionFormPanel.test.tsx` ≥ 63 | backend ≠ 68; tsc > 34 (**any error in a `settings/Connection*` file is unambiguously this phase's** — there are zero today); any per-file decrease; any `failed > 0` surviving SEED-171 triage | ✅ three commands | see § Test Infrastructure |

---

## Wave 0 Requirements

- [ ] `frontend/src/components/settings/__tests__/connectionMark.test.tsx` — **NEW**, covers SC#3 end to end
- [ ] New cases in `ConnectionsTab.test.tsx` — the dense variant's shape half (SC#2a) and the wide render's byte-identity control
- [ ] New cases in `ConnectionFormPanel.test.tsx` — the fourth chooser option, the MCP field set, the create body's key set, the **edit-mode shape** defect (D-206.1-22), the disabled-Save `aria-describedby` resolution
- [ ] A **positional-fallback sweep** driven with a synthetic fifth shape over `configFromDraft` / `destinationFooterOf` / `typedHost` / `secretLabel` (SC#4, extended past its own wording)
- [x] **No framework install needed** — vitest, jsdom and `unplugin-icons` are all already wired
- [ ] ⚠ **A browser tier must be arranged** for SC#1b and SC#2b. This is the one Wave-0 gap no code closes. Local infra was UP when this was written (backend `:8000`, frontend `:5173`, Supabase `:54321` all 200).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The MCP create → discover → grant → call round trip (SC#1b) | CONN-02 | needs a live backend, a live remote MCP server, and a real workflow run; no in-suite tier reaches any of them | Chrome DevTools MCP; there is **no URL router** for Settings tabs — seed `localStorage.settings_active_tab = "5"` then load `/`. Record evidence per step, never one screenshot at the end. |
| The destination cell's real geometry beside the open panel (SC#2b) | CONN-02 | ⚠ **measured:** jsdom reports `scrollWidth 0 / clientWidth 0 / offsetWidth 0 / getBoundingClientRect().width 0`, so the assertion passes **vacuously** on markup overflowing by ~400px. **A green vitest run is not SC#2 evidence.** | `evaluate_script` reading `scrollWidth`/`clientWidth`/`whiteSpace` off every `[data-testid="connections-row-destination"]`, plus a `document.body.clientWidth > 0` non-vacuity control, at viewport 1280 and 1536 with the panel open. ⚠ **The two readings are EXPECTED TO AGREE** — `SettingsPage.tsx:861` caps this surface at `max-w-3xl` (768px), so both viewports give a 302px open track. An identical pair is CONFIRMATION, not a copy-paste error (D-206.1-21). |
| The operator's lived-experience pass (G-4) | CONN-02 | the phase exists because live UAT saw what every green gate missed | Open Settings → Connections with a real row list; open the panel; read the destination column; create an MCP row; look at the four marks side by side. |

---

## Validation Sign-Off

- [ ] Every task has an automated verify, or an explicit browser-tier reference
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags anywhere
- [ ] SC#1b and SC#2b evidence captured in a browser and recorded per step
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
