---
phase: 071.4
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - backend/app/api/documents.py
  - backend/tests/integration/test_documents.py
  - .planning/reported-bugs/reingest-endpoint-does-not-delete-prior-tables-and-images.md
autonomous: false
requirements: []
related_bugs: [BUG-260516-04]
related_seeds: [SEED-022]
notes: |
  Direct port of /reextract's delete cascade into /reingest. 4 LOC of code
  + 1 integration test + 1 operator-run SQL cleanup + 1 verification
  reingest. Plan is autonomous: false because Tasks 3 + 4 require the
  operator to (a) run SQL cleanup on the local DB and (b) trigger a
  verification reingest and paste the count.

  This bug discovered during 071.4-01 Task 3 — when the operator ran
  /reingest twice with the camelot precision floor in place, the table
  count grew by a fixed amount each time rather than staying stable. The
  +39 / +48 / +20 deltas were the actual fresh-extract counts being
  appended on top of prior rows.

must_haves:
  truths:
    - "backend/app/api/documents.py::reingest_document deletes prior document_tables AND document_images rows BEFORE queueing the BackgroundTask"
    - "Integration test asserts reingest leaves tables + images count == fresh extract output (no accumulation)"
    - "SQL cleanup script produced for the operator to run on the affected thesis PDF + DOCX rows"
    - "Verification reingest on thesis PDF shows tables ≈ 48 (camelot fresh post-floor) and images ≈ 20"
    - "Verification reingest on thesis DOCX shows tables ≈ 39 (matches ground truth) and images ≈ 20"
    - "BUG-260516-04 frontmatter flipped from status: open to status: closed"
---

<objective>
Add the missing `document_tables` + `document_images` delete calls to
`/reingest` so it mirrors `/reextract`'s cleanup contract. Verify on the
operator's thesis (PDF + DOCX) that fresh post-fix reingests land on stable
ground-truth-adjacent counts.
</objective>

<tasks>

<task id="1" name="Patch /reingest endpoint">
- Edit `backend/app/api/documents.py::reingest_document` at the section between
  the storage download (line 686) and the status UPDATE (line 691).
- Add two `await run_in_threadpool(...)` calls mirroring `/reextract`'s lines
  807 + 810 — one for `document_tables`, one for `document_images`. Order:
  tables first (per D-071-10 cascade order — children before parent), then
  images.
- Chunks are intentionally NOT deleted here because the existing
  `_upload_pipeline` BackgroundTask path handles chunk cleanup downstream.
  Keep parity with the existing structure.
- Add a 1-line comment explaining: "BUG-260516-04: /reingest must clear
  prior tables + images to prevent accumulation across multiple reingests
  (chunks are cleaned downstream in _upload_pipeline)."
- Commit: `fix(071.4-04): /reingest deletes prior tables + images — BUG-260516-04`
</task>

<task id="2" name="Add regression test">
- Edit `backend/tests/integration/test_documents.py`.
- Add `TestReingestDocument::test_reingest_clears_prior_tables_and_images`:
  - Seed `documents` row + 5 `document_tables` rows + 5 `document_images` rows
    for a known document_id under a test user.
  - Mock or short-circuit the BackgroundTask path to skip the actual extraction
    (per existing patterns in this file).
  - POST `/documents/{id}/reingest` with the test user's auth.
  - Assert: `document_tables` count for that document_id is 0 AND
    `document_images` count is 0 BEFORE the BackgroundTask runs.
  - Assert HTTP 200 (matches existing /reingest contract).
- Run the test: `python -m pytest tests/integration/test_documents.py::TestReingestDocument -q --tb=short`
- Commit: `test(071.4-04): regression for /reingest table+image cleanup`
</task>

<task id="3" name="Operator SQL cleanup + verification reingest checkpoint">
- Produce SQL the operator can paste into Supabase SQL editor to clean the
  polluted rows on the thesis PDF + DOCX. Use document name to locate IDs:
  ```sql
  -- 1. Locate the two thesis rows
  SELECT id, filename FROM documents WHERE filename ILIKE '%Fahed Mrad%';

  -- 2. Clean both (paste the two IDs from above)
  DELETE FROM document_tables WHERE document_id IN ('<pdf-id>', '<docx-id>');
  DELETE FROM document_images WHERE document_id IN ('<pdf-id>', '<docx-id>');
  ```
- Operator restarts backend (so the Plan 04 commit is loaded), runs the SQL
  cleanup, then triggers `/reingest` once per document via the Library
  Health page.
- Operator pastes the new counts. Expected:
  - PDF: tables ≈ 48 (camelot fresh post-floor), images ≈ 20, chunks ≈ 441
  - DOCX: tables ≈ 39 (ground truth), images ≈ 20, chunks ≈ 402
- If counts match expected (±5%), Task 3 passes. If counts are wildly off,
  investigate before proceeding.

**This is a checkpoint:human-action** — operator runs SQL + reingest + paste counts.
</task>

<task id="4" name="Close bug + Plan 04 SUMMARY">
- Edit `.planning/reported-bugs/reingest-endpoint-does-not-delete-prior-tables-and-images.md` frontmatter:
  `status: open` → `status: closed`. Add closing note with commit SHAs +
  verification counts + the operator-confirmed post-fix steady-state.
- Write `.planning/phases/071.4-post-071-3-polish-bundle/071.4-04-SUMMARY.md`.
  Include the pre-fix accumulation table from the bug report, the post-fix
  steady-state, and the SEED-022 precision-floor effectiveness magnitude
  (214 → 48 PDF = 4.4x reduction) — this is genuinely good news that was
  hidden by the bug.
- Commit: `docs(071.4-04): close BUG-260516-04 + SUMMARY`
</task>

</tasks>

<success_criteria>
- [ ] `/reingest` has the same delete cascade as `/reextract` (tables + images)
- [ ] Regression test asserts reingest clears tables + images before extract
- [ ] Operator-confirmed verification counts on thesis PDF + DOCX match
      fresh-extract expectations within ±5%
- [ ] BUG-260516-04 status flipped to closed
- [ ] SUMMARY.md committed
- [ ] SEED-022's effective precision-floor magnitude (214 → 48) is recorded
      in SUMMARY for future Phase 076 plan-phase reference
</success_criteria>
