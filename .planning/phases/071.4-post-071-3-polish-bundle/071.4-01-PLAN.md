---
phase: 071.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/extractors/aspects/tables.py
  - backend/tests/unit/test_aspect_engines_tables.py
  - .planning/seeds/SEED-022-camelot-pdf-table-precision-audit.md
autonomous: true
requirements: []
related_seeds: [SEED-022]
notes: |
  Quick mitigation for SEED-022's 5.5x camelot precision inflation. Reject
  any Table object where the dataframe has < 2 rows or < 2 columns — these
  cannot represent a real table by any reasonable definition (a table with
  one row is a header alone; a table with one column is a list). Real
  borderless academic tables always have >= 2 rows and >= 2 cols.

  Expected impact on the user's thesis: 214 → ~80-120 stored tables
  (uncertain magnitude — depends on how many camelot regions are header-or-list
  artifacts). NOT a full precision fix — content-density and figure-bbox-overlap
  filters are deferred to Phase 076's prerequisite. This is damage control.

  After landing the floor, update SEED-022 with the new mitigated count from
  a thesis re-extract via /reextract, so the seed has accurate numbers when
  Phase 076 plan-phase opens it.

must_haves:
  truths:
    - "backend/app/services/extractors/aspects/tables.py::camelot_tables rejects Table objects where len(rows) < 2 OR len(cols) < 2"
    - "Unit test added covering single-row + single-col rejection"
    - "11 + new test pass on existing tests/unit/test_aspect_engines_tables.py"
    - "User re-extracts thesis post-mitigation; new document_tables count recorded in SEED-022"
---

<objective>
Add a precision floor to `camelot_tables` that rejects degenerate Table regions
(< 2 rows or < 2 columns). Verify via unit test. Have the operator re-extract
the thesis and record the post-mitigation count in SEED-022 for Phase 076's
benefit.
</objective>

<tasks>

<task id="1" name="Add row/col floor to camelot_tables">
- Edit `backend/app/services/extractors/aspects/tables.py`
- In the `camelot_tables(raw, mime)` function, after camelot returns its TableList, iterate and skip any Table where `len(table.df.index) < 2 OR len(table.df.columns) < 2`. The exact attribute name depends on the camelot Table API — verify by reading the function.
- Add a 2-line code comment explaining: "precision floor (SEED-022): camelot at flavor=stream over-detects single-row formula blocks and single-col reference lists as tables. Reject."
- Commit: `fix(071.4-01): add row/col precision floor to camelot_tables (SEED-022)`
</task>

<task id="2" name="Add unit test for the floor">
- Edit `backend/tests/unit/test_aspect_engines_tables.py`
- Add `test_camelot_tables_rejects_single_row` and `test_camelot_tables_rejects_single_col` (or equivalent — verify by reading existing test patterns; may need synthetic camelot Table fixtures).
- Run `python -m pytest tests/unit/test_aspect_engines_tables.py -q` — all 11 + new tests must pass.
- Commit: `test(071.4-01): unit tests for camelot row/col floor`
</task>

<task id="3" name="Update SEED-022 with post-mitigation thesis count + checkpoint">
- After Tasks 1+2 land, ask the operator to run a `/reextract` on the user's thesis (BUG-260516-01 means they may need to refresh after — workaround acceptable).
- Operator pastes the new `SELECT COUNT(*) FROM document_tables WHERE document_id = '<thesis-doc-id>'` result.
- Update SEED-022's "What we observed" section with the post-mitigation number — e.g., "After 071.4-01 floor: thesis 214 → N tables." The remaining N - 39 false-positive delta is what Phase 076 prerequisite must address (content-density / figure-bbox-overlap filters).
- Commit: `docs(071.4-01): record post-mitigation thesis count in SEED-022`

**This is a checkpoint:human-action** — operator runs /reextract. After they paste the count, continuation proceeds.
</task>

<task id="4" name="SUMMARY.md">
- Write `.planning/phases/071.4-post-071-3-polish-bundle/071.4-01-SUMMARY.md` per the SUMMARY template.
- Include: commits list, mitigation magnitude (214 → N), explicit note that this is partial-close of SEED-022 (the precision audit itself is still planted with re-open trigger before Phase 076).
- Commit: `docs(071.4-01): SUMMARY`
</task>

</tasks>

<success_criteria>
- [ ] Camelot row/col floor implemented + committed
- [ ] Unit tests pass (existing 11 + 2 new)
- [ ] SEED-022 updated with post-mitigation count
- [ ] SUMMARY.md committed
</success_criteria>
