---
phase: 067.5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useMessages.ts
  - frontend/src/__tests__/hooks/useMessages.test.ts
autonomous: true
requirements:
  - STREAM-04-correctness-round3

must_haves:
  truths:
    - "Reconcile (the focus/visibilitychange/pageshow/mount-driven path) reads the freshest possible per-thread state — never a one-tick-stale ref-mirror snapshot — when deciding whether to insert a placeholder, dedup against an existing DB row, or invoke loadMessages MERGE."
    - "loadMessages cancellation (loadAbortRef.current?.abort() at useMessages.ts:593) DOES NOT cascade-abort any live SSE consumer in subscriptionsRef. Cross-thread navigation that triggers loadMessages on Thread B leaves Thread A's SSE consumer (and its onDone/onTerminal write to messagesByThread.get(threadA)) intact."
    - "loadMessages MERGE (lines 639-651) does not strand an empty-content placeholder when the DB has a populated row for the same runId — empty placeholders for completed runs are dropped, populated DB rows win."
    - "messagesByThread per-thread Map shape and per-thread bucket semantics (Phase 067.3 D-067.3-R1) are preserved — no breaking change to the hook's public API or to the bucket-keyed write contract that all 21 in-factory handlers (RESEARCH.md §2.2 audit) depend on."
    - "A vitest regression test deterministically reproduces the stale-ref / cross-thread-reconcile failure pre-fix and passes post-fix; the existing R-1 cross-thread switch tests in useMessages.test.ts still pass (no regression)."
  artifacts:
    - path: "frontend/src/hooks/useMessages.ts"
      provides: "Reconcile / ref-mirror / loadMessages-cascade fix targeting one of the 4 ref-mirror surfaces or the loadMessages MERGE path"
      contains: "Phase 067.5"
    - path: "frontend/src/__tests__/hooks/useMessages.test.ts"
      provides: "Regression test for the empty-thread-until-refresh repro (Thread A stream → switch away → done arrives → switch back → assert messagesByThread.get(threadA) has populated content, NOT empty)"
      contains: "Phase 067.5"
  key_links:
    - from: "frontend/src/hooks/useMessages.ts reconcile (lines 935-1131)"
      to: "messagesByThread state via setMessagesForThread / messagesByThreadRef"
      via: "ref-mirror useEffect at lines 409-412 OR direct state read"
      pattern: "messagesByThreadRef\\.current\\.get"
    - from: "frontend/src/hooks/useMessages.ts loadMessages (lines 590-658)"
      to: "subscriptionsRef.current (live SSE consumers)"
      via: "loadAbortRef cancellation MUST NOT cascade to subscriptionsRef entries"
      pattern: "loadAbortRef\\.current\\?\\.abort"
    - from: "frontend/src/hooks/useMessages.ts loadMessages MERGE (lines 639-651)"
      to: "data (DB rows) + liveTempPlaceholders (live in-flight runs)"
      via: "the three-clause filter (temp- prefix + runId presence + (!dbRunIds.has(m.runId) || subscriptionsRef.current.has(m.runId)))"
      pattern: "subscriptionsRef\\.current\\.has"
---

<objective>
Close the Phase 067.4 Row 11 RED — the user-reproduced empty-thread-until-refresh symptom on 1 of 4 streaming threads. Phase 067.4 Plan 02 shipped a static audit confirming all 21 in-factory `setMessages` handlers in `useMessages.ts` are bucket-targeted post-R-1, but the static audit explicitly enumerated 6 outside-factory write sites + 4 ref-mirror surfaces (per `.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md` §2.2 lines 257-269) as risk surface that the static audit could not exercise. Plan 01 reproduces the failure under Chrome MCP, branches the fix by runtime evidence (stale ref-mirror vs SSE consumer abort cascade vs loadMessages MERGE stranding), implements the targeted fix on the implicated surface, and adds a vitest regression test.

Purpose: Without this fix, Phase 067.4 stays partially closed (Row 11 RED), STREAM-04-correctness-round3 stays pending, and the v2.5 milestone gate cannot close. The user-driven 4-thread protocol (Bread / Physicists / Photosynthesis / pgvector across mixed providers) WILL reproduce the freeze again on the next milestone-close attempt unless this surface is fixed.

Output: Single targeted code fix in `frontend/src/hooks/useMessages.ts` + a new vitest regression test in `frontend/src/__tests__/hooks/useMessages.test.ts`. No API surface change, no `messagesByThread` shape change, no breaking change to the 21 in-factory handlers.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Vibe Apps/Agentic RAG/.planning/PROJECT.md
@C:/Vibe Apps/Agentic RAG/.planning/ROADMAP.md
@C:/Vibe Apps/Agentic RAG/.planning/STATE.md
@C:/Vibe Apps/Agentic RAG/CLAUDE.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-CONTEXT.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-02-SUMMARY.md
@C:/Vibe Apps/Agentic RAG/frontend/src/hooks/useMessages.ts
@C:/Vibe Apps/Agentic RAG/frontend/src/components/chat/ChatArea.tsx
@C:/Vibe Apps/Agentic RAG/frontend/src/__tests__/hooks/useMessages.test.ts

<interfaces>
<!-- Live line numbers as of 2026-05-09 (RESEARCH.md was authored before 067.4 Plan 02 landed; numbers have shifted ±15-30 lines). Confirmed via Read+Grep against the file at write time. Re-anchor in Task 1 if the executor sees drift. -->

`frontend/src/hooks/useMessages.ts` SURFACES IN SCOPE FOR PLAN 01 (live line numbers, 1216 LOC total):

- **messagesByThreadRef state-mirror useEffect:** lines 409-412
  ```typescript
  const messagesByThreadRef = useRef<Map<string, Message[]>>(messagesByThread)
  useEffect(() => {
    messagesByThreadRef.current = messagesByThread
  }, [messagesByThread])
  ```
  RESEARCH.md flagged this as Surface #4 ("Potential lag: ref updates after state setter in the same React batch; on a fresh thread switch firing reconcile, the ref may be one tick behind").

- **messagesRef state-mirror useEffect:** lines 470-475
  ```typescript
  const messagesRef = useRef<Message[]>([])
  useEffect(() => {
    const stid = streamingThreadIdRef.current
    const fallbackId = stid ?? activeThreadIdRef.current
    messagesRef.current = fallbackId ? messagesByThread.get(fallbackId) ?? [] : []
  }, [messagesByThread])
  ```
  RESEARCH.md flagged this as Surface #3 ("Potential lag: the useEffect fires on `messagesByThread` dep, but if `streamingThreadIdRef` hasn't been re-pointed when stop fires immediately after thread-switch, `messagesRef.current` may have wrong bucket").

- **loadMessages function:** lines 590-658 (loadAbortRef abort at line 593)
  ```typescript
  const loadMessages = useCallback(async (threadId: string) => {
    loadAbortRef.current?.abort()
    const controller = new AbortController()
    loadAbortRef.current = controller
    try {
      const data = await getMessages(threadId, controller.signal)
      if (activeThreadIdRef.current !== threadId) return
      if (isSendingRef.current) return
      setMessagesForThread(threadId, (prev) => {
        const dbRunIds = new Set(data.filter((m) => m.runId).map((m) => m.runId))
        const liveTempPlaceholders = prev.filter(
          (m) =>
            m.id.startsWith("temp-") &&
            m.runId &&
            (!dbRunIds.has(m.runId) || subscriptionsRef.current.has(m.runId)),
        )
        return [...data, ...liveTempPlaceholders]
      })
    } catch (err) {
      if (err instanceof Error && err.name === "AbortError") return
      throw err
    }
  }, [setMessagesForThread])
  ```
  RESEARCH.md flagged the abort at line 593 (= old line 568) as the cascade-abort-risk site. The MERGE logic at lines 639-651 is the "may keep empty placeholder while DB-persisted row sits unfetched" surface from 067.4-HUMAN-UAT Row 11 line 345.

- **reconcile function:** lines 935-1131; `messagesByThreadRef.current.get(threadId)` read at line 977
  ```typescript
  const threadMessages = messagesByThreadRef.current.get(threadId) ?? []
  const existingByRunId = threadMessages.find((m) => m.runId === run.run_id)
  const targetId = existingByRunId?.id ?? `temp-${run.run_id}`
  ```
  RESEARCH.md flagged this as the "ref may be one tick behind" surface.

- **sendMessage finally block:** lines ~862-915 (outside-factory write site #1)
- **sendMessage catch block:** lines ~878-889 (outside-factory write site #2)
- **stopStreaming callback:** lines 477-495 (outside-factory write site #3, uses messagesRef.current)
- **ChatArea reconcile-trigger useEffect:** `frontend/src/components/chat/ChatArea.tsx` lines 162-188 (mount + visibilitychange + focus + pageshow → reconcileRef.current(tid))

`frontend/src/__tests__/hooks/useMessages.test.ts` (362 lines):
- vi.hoisted + vi.mock pattern for `@/lib/api` and `@/lib/supabase` already established
- `makeSseRecorder()` helper captures StreamCallbacks per runId; tests synchronously invoke captured callbacks inside `act()` to simulate SSE event arrival
- Header docstring quotes D-067.4-R4-01 (LOCKED) — the surgical-not-broad-refactor mandate that Plan 01 must respect
</interfaces>

<gap_source>
**Primary gap source — Phase 067.4 Row 11 RED (`067.4-HUMAN-UAT.md` lines 333-362):**
- Symptom: 4 streaming threads (Bread / Physicists / Photosynthesis / pgvector across mixed providers OpenAI/Anthropic/OpenRouter); auto-titles generated on all 4; no cross-thread leak; **1 of 4 rendered EMPTY in UI even after waiting "for minutes"**; **page refresh repaired it** (data persisted to Postgres, React in-memory state stale).
- Diagnostic implication (line 343): the data WAS persisted; in-memory React state for that thread was NOT updated when user navigated to it. RESEARCH.md flagged 6 outside-factory + 4 ref-mirror surfaces as the risk that the in-factory bucket-targeted refactor did NOT cover.
- Likely root cause hypothesis (line 345): cross-thread switch mid-stream + run completes while another thread is active viewing target → terminal `done` event processed but `loadMessages` reconcile on switch-back doesn't pick up persisted DB row → `messagesByThreadRef` for destination thread holds stale snapshot from moment-of-switch + MERGE logic at lines 639-651 may keep empty placeholder.
- Verdict: **RED.** Real Plan 02 R-4 regression. Dual-pass implication (D-067.4-QUALITY-01): Row 6 R-1 cross-thread is likely same root cause and probably also fails.

**Substrate research — Phase 067.4 RESEARCH.md §2.2 lines 257-285:**
- 6 outside-factory write sites enumerated (sendMessage finally, sendMessage catch, stopStreaming, reconcile, ChatArea reconcile-trigger useEffect, global setIsStreaming(false))
- 4 ref-mirror surfaces enumerated (messagesByThreadRef useEffect, messagesRef useEffect, reconcile's messagesByThreadRef.current.get(threadId) read, loadMessages MERGE)
- "Most-likely R-4 root cause" hypothesis at line 271: stale `messagesRef.current` / `messagesByThreadRef.current` during reconcile-on-(re)visit window; OR the SSE consumer abruptly closes on cross-thread navigation, leaving the bucket without the `tool_end` write
- Plan 02 task structure outline at lines 273-285: runtime repro → if freeze repros, capture network panel for `/runs/{id}/stream?since=N` connection state + React DevTools `subscriptionsRef.current` state → fix in `loadMessages` if consumer aborted (verify `loadAbortRef.current?.abort()` doesn't cascade-abort SSE consumer) → vitest regression test
</gap_source>

<deep_work_rules_applied>
Every task in this plan honors:
1. `<read_first>` — mandatory file reads BEFORE acting (the live `useMessages.ts`, the RESEARCH §2.2 line range, the 067.4-HUMAN-UAT Row 11 evidence)
2. `<acceptance_criteria>` — grep / file-content / Chrome MCP DOM probe / vitest assertion verifiable; NO subjective language
3. `<action>` — concrete code-shape references with literal patterns / function names / line ranges
4. RUNTIME REPRO BEFORE FIX (Task 2). Phase 067.5 ROADMAP risk note 524: "Don't ship a speculative fix without runtime confirmation."
</deep_work_rules_applied>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor surfaces against the live useMessages.ts</name>
  <files>frontend/src/hooks/useMessages.ts (READ ONLY — no edit in this task), .planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md (CREATE)</files>

  <read_first>
    1. `C:/Vibe Apps/Agentic RAG/frontend/src/hooks/useMessages.ts` — read the FULL file (1216 lines as of 2026-05-09). RESEARCH.md was authored before 067.4 Plan 02 landed; line numbers have shifted ±15-30 lines. Confirm the live line ranges for ALL 6 outside-factory + 4 ref-mirror surfaces.
    2. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md` — §2.2 lines 222-285 (the static audit + outside-factory enumeration + Plan 02 task structure outline). DO NOT re-litigate the in-factory handler audit — Phase 067.4 Plan 02 already shipped that.
    3. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` lines 333-362 — Row 11 RED evidence (4-thread protocol; 1 of 4 empty; F5 repairs; root cause hypothesis).
    4. `C:/Vibe Apps/Agentic RAG/frontend/src/components/chat/ChatArea.tsx` lines 155-188 — the reconcile-trigger useEffect (mount + visibilitychange + focus + pageshow).
  </read_first>

  <action>
Create `.planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md` with the following exact section headings, populated from the live file:

```
# Plan 01 surface map (live line numbers, 2026-05-09)

## 4 ref-mirror surfaces

| # | Surface | RESEARCH §2.2 line | Live line | Drift | Notes |
|---|---------|---------------------|-----------|-------|-------|
| 1 | messagesByThreadRef useEffect | 384-387 | <fill> | <delta> | <one-line note> |
| 2 | messagesRef useEffect         | 445-450 | <fill> | <delta> | <one-line note> |
| 3 | reconcile read messagesByThreadRef.current.get(threadId) | 952 | <fill> | <delta> | <one-line note> |
| 4 | loadMessages MERGE three-clause filter | 639-651 | <fill> | <delta> | <one-line note> |

## 6 outside-factory write sites

| # | Site | RESEARCH §2.2 line | Live line | Drift | Notes |
|---|------|---------------------|-----------|-------|-------|
| 1 | sendMessage finally block | 834-894 | <fill> | <delta> | <note> |
| 2 | sendMessage catch block   | 822-833 | <fill> | <delta> | <note> |
| 3 | stopStreaming callback    | 452-470 | <fill> | <delta> | <note> |
| 4 | reconcile body            | 910-1106 | <fill> | <delta> | <note> |
| 5 | ChatArea reconcile-trigger useEffect | 163-188 (ChatArea.tsx) | <fill> | <delta> | <note> |
| 6 | global setIsStreaming(false) at line 838 | 838 | <fill> | <delta> | <note> |

## loadAbortRef cascade-abort risk site

| Surface | RESEARCH §2.2 line | Live line | Notes |
|---------|---------------------|-----------|-------|
| loadAbortRef.current?.abort() at start of loadMessages | 568 | <fill> | <does it cascade to subscriptionsRef entries? Y/N + reasoning> |

## D-067.3-R1 invariants to preserve (DO NOT TOUCH WITHOUT JUSTIFICATION)

- messagesByThread: Map<string, Message[]> shape
- setMessagesForThread(threadId, updater) write contract
- All 21 in-factory handlers in makeStreamCallbacks (RESEARCH §2.2 lines 226-251)
- subscriptionsRef.current Map<string, AbortController> keyed by run_id
- D-063.1-11 reconcileInFlightRef serialization
```

Fill every `<fill>`, `<delta>`, `<note>`, `<does it cascade...>` slot from the live `useMessages.ts`. The drift column is the literal numeric delta (e.g., `+25` if RESEARCH said 384 and the live line is 409).

DO NOT modify any source files in this task. This is a read-and-document task — its sole output is the surface-map artifact that Tasks 2 and 4 will consume to anchor their fix and test code shape.

(Per D-067.4-R4-01 LOCKED quote in `useMessages.test.ts` header: "Plan 02 takes a surgical handler-by-handler patch approach to frontend/src/hooks/useMessages.ts. NO broad refactor of the SSE event dispatcher." Phase 067.5 inherits that constraint.)
  </action>

  <acceptance_criteria>
    - File exists at exact path: `.planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md`
    - Verifiable: `grep -c '^| [1-6] |' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md` returns at least 10 (4 ref-mirror rows + 6 outside-factory rows)
    - Verifiable: `grep -c '<fill>' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md` returns 0 (all placeholder slots filled)
    - Verifiable: `grep -c 'D-067.3-R1' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md` returns at least 1 (preserve-invariants section present)
    - The "Live line" column for ref-mirror Surface #1 (messagesByThreadRef useEffect) MUST equal a number where reading `useMessages.ts` at that offset shows `messagesByThreadRef = useRef` text — sanity-check via `grep -n 'messagesByThreadRef = useRef' frontend/src/hooks/useMessages.ts` should return that line ±2.
    - No source file under `frontend/src/` modified. Verifiable: `git diff --name-only frontend/ | wc -l` returns 0.
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md &amp;&amp; ! grep -q '&lt;fill&gt;' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md &amp;&amp; [ "$(grep -c '^| [1-6] |' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md)" -ge 10 ] &amp;&amp; [ "$(git diff --name-only frontend/ | wc -l)" = "0" ]</automated>
  </verify>

  <done>Surface map artifact written with all live line numbers and drift deltas filled in; no source files touched; ready to feed Task 2's repro and Task 4's fix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Runtime repro of Row 11 empty-thread-until-refresh under Chrome MCP — capture branch evidence</name>

  <files>.planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md (CREATE)</files>

  <read_first>
    1. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md` (Task 1 output — live line numbers + cascade-abort note)
    2. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-HUMAN-UAT.md` lines 349-362 (Row 11 "How to test" protocol — 4 threads, mixed providers, mid-stream nav)
    3. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md` §2.2 lines 271-285 (Most-likely R-4 root cause + Plan 02 task structure step 3 — "capture network panel for /runs/{id}/stream?since=N connection state + React DevTools subscriptionsRef.current state")
    4. `C:/Users/fhdmr/.claude/CLAUDE.md` memory references: `feedback_chrome_mcp_testing.md` (Chrome DevTools MCP available — drive a real browser) + `reference_local_dev_app.md` (dev app at http://localhost:5173/, login fhdmrd@gmail.com / 123456)
  </read_first>

  <what-built>
    Task 1 produced the surface map. NO production code change yet. This task uses Chrome DevTools MCP (per `feedback_chrome_mcp_testing.md` memory) to drive the dev app at `http://localhost:5173/` and reproduce the Row 11 empty-thread-until-refresh symptom AT LEAST ONCE — ideally 3 times across separate browser sessions per ROADMAP risk note 524 ("Plan 01 must build repro confidence (e.g., 3+ reproductions across separate browser sessions) BEFORE shipping a fix; otherwise the 'fix' may merely shift timing").
  </what-built>

  <how-to-verify>
    Executor steps (autonomous via Chrome DevTools MCP — no human action required for this gate, but the gate is `blocking` because the branch decision in Task 4 depends on the captured evidence):

    1. **Confirm dev server up.** `curl -fsS http://localhost:5173/ -o /dev/null` returns 0. If not, instruct user to start it (`npm run dev` from `frontend/`).

    2. **Authenticate via Chrome MCP.** Navigate to `http://localhost:5173/`. Log in with `fhdmrd@gmail.com` / `123456` per `reference_local_dev_app.md`.

    3. **Run the Row 11 protocol with diagnostics enabled — minimum 3 attempts.** Per attempt:
       - Open Chrome DevTools Network panel; filter to `/runs/`. Open Console panel.
       - Open 4 streaming threads with diverse prompts (mixed providers — OpenAI gpt-5.4-mini, Anthropic claude-sonnet-4-6, OpenRouter moonshotai/kimi-k2.5, OpenRouter minimax/minimax-m2.7 if creds permit; otherwise repeat OpenAI/Anthropic):
         - Thread A: "Plan a sourdough bread recipe step by step" (Bread)
         - Thread B: "List 5 famous physicists and their key contributions" (Physicists)
         - Thread C: "Explain photosynthesis in simple terms" (Photosynthesis)
         - Thread D: "Summarize what pgvector does in Postgres" (pgvector)
       - Submit each prompt back-to-back; navigate AWAY mid-stream on at least 2 of the 4 threads (e.g., submit on A, switch to B and submit, switch to C and submit, switch to D and submit, then cycle A→B→C→D→A while streams complete in arbitrary order).
       - Wait ~60-120s for all streams to finish (titles generated, no "New Chat" remaining).
       - For each of the 4 threads, navigate to it and observe: does the assistant message body render? Or is the bubble empty/missing?
       - **Capture per attempt:** which thread (A/B/C/D) reproduced empty (if any); the run_id of the empty thread (from Network panel `/runs/{id}/stream` URL); whether the SSE stream connection state at the moment of cross-nav was `closed`/`open`/`pending` (Network panel → Stream → Status); and the JSON dump of `subscriptionsRef.current.size` and `subscriptionsRef.current.keys()` at the moment user views the empty thread (read via React DevTools or via temporary `console.warn` instrumentation — DO NOT commit instrumentation to the repo; remove before git operations).

    4. **Branch decision capture.** After at least 3 attempts (3+ reproductions OR 3+ non-reproductions across separate browser sessions per ROADMAP line 524), populate `.planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md` with the following EXACT section headings:

       ```
       # Plan 01 runtime repro evidence (2026-05-09)

       ## Protocol summary
       - Attempts: <N>
       - Reproductions: <M> of <N>
       - Per-attempt log: <table with columns: attempt#, prompts-by-thread, run_ids, empty-thread (Y/N which one), SSE stream state at empty-thread-view, subscriptionsRef state at empty-thread-view>

       ## Diagnostic findings
       - SSE consumer state for empty thread at view-time: <closed | open | not in subscriptionsRef>
       - DB has the persisted assistant row: <Y/N — verified via Network panel /threads/{id}/messages refetch>
       - messagesByThread.get(threadId) contents at view-time: <empty-array | placeholder-only | populated-but-missing-content | etc.>
       - Refresh (F5) repairs render: <Y/N>

       ## Branch decision (which fix surface)
       Based on the evidence, the fix routes to ONE of:
       - **Branch A — SSE consumer aborted by loadMessages cascade.** Evidence pattern: SSE stream state = `closed` at view-time AND `subscriptionsRef.current.has(run_id)` returns false. Fix surface: `loadMessages` lines 590-658 — verify `loadAbortRef.current?.abort()` at line 593 does NOT cancel a controller that is also in `subscriptionsRef`. (Per RESEARCH.md §2.2 step 4.)
       - **Branch B — Stale ref-mirror during reconcile-on-(re)visit window.** Evidence pattern: SSE stream state = `closed` cleanly at run completion AND DB has the row AND `messagesByThread.get(threadId)` is empty/placeholder-only AND `messagesByThreadRef.current.get(threadId)` matches. Fix surface: ref-mirror useEffect at lines 409-412 OR reconcile read at line 977 — switch reconcile to read fresh state via a state-aware path (e.g., move messagesByThreadRef update into a useLayoutEffect so it lands in the same React tick as the state setter, OR change reconcile to take the threadMessages from a synchronous source).
       - **Branch C — loadMessages MERGE strands empty placeholder.** Evidence pattern: SSE stream state = `closed` cleanly AND DB has the row AND `messagesByThread.get(threadId)` has a `temp-${run_id}` entry with empty content AND the DB row also has the same runId AND `subscriptionsRef.current.has(run_id)` is true (the third-clause `||` keeps the placeholder alive even though DB has caught up). Fix surface: MERGE filter at lines 639-651 — narrow the placeholder-keep predicate so completed-with-empty-content placeholders are dropped when DB has a populated row.
       - **Branch D — divergent surface NOT in the 6+4 enumeration.** Evidence pattern: none of A/B/C match. Document in `<deviation_note>` and propose a new fix surface; include the exact code shape required.

       ## Selected branch: <A | B | C | D>
       ## Rationale: <2-3 sentences citing the evidence row that disambiguates>
       ```

    5. **If 0 reproductions across 3+ attempts:** Per ROADMAP line 524 pivot decision, ship instrumentation-as-detection on a dev-mode flag (`if (import.meta.env.DEV) console.warn(...)`) at the suspected surface (default to Branch B — messagesByThreadRef stale snapshot during reconcile) and stop here. Document in REPRO-EVIDENCE.md under `## Selected branch: INSTRUMENT` and DO NOT proceed to Task 4 fix in this run; raise to user via the checkpoint resume signal that re-test under user-driven 4-thread protocol is needed to capture a reproduction with the instrumentation live.

    Pause for human review of the captured REPRO-EVIDENCE.md before resuming to Task 4.
  </how-to-verify>

  <acceptance_criteria>
    - File exists at exact path: `.planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md`
    - Verifiable: `grep -c '## Selected branch:' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md` returns 1
    - Verifiable: `grep -E '## Selected branch: (A|B|C|D|INSTRUMENT)' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md` returns a match (one of the 5 valid branch labels)
    - Verifiable: `grep -c 'Attempts:' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md` returns at least 1, and the integer following is `>= 3`
    - Verifiable: `grep -c 'run_id' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md` returns at least 1 (at least one captured run_id)
    - No production source files modified yet. Verifiable: `git diff --name-only frontend/src/ | wc -l` returns 0.
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md &amp;&amp; grep -qE '## Selected branch: (A|B|C|D|INSTRUMENT)' .planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md &amp;&amp; [ "$(git diff --name-only frontend/src/ | wc -l)" = "0" ]</automated>
  </verify>

  <resume-signal>Type "approved" to proceed to Task 3 (vitest regression test for the selected branch). Reply "instrument" if Task 4 should ship instrumentation-only and pause for user re-test. Reply with branch reassignment ("branch A" / "branch B" / "branch C" / "branch D") if you want to override the executor's evidence-driven branch selection.</resume-signal>

  <done>REPRO-EVIDENCE.md committed with at least 3 attempts logged, a selected branch (A/B/C/D/INSTRUMENT), and the diagnostic evidence that disambiguates which surface to fix. User has reviewed and approved the branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest regression test for the selected branch (RED phase — must fail before fix)</name>

  <files>frontend/src/__tests__/hooks/useMessages.test.ts (MODIFY — add a new describe block)</files>

  <read_first>
    1. `C:/Vibe Apps/Agentic RAG/frontend/src/__tests__/hooks/useMessages.test.ts` (the FULL file, 362 lines) — study the `makeSseRecorder()` helper (lines ~76-100), the existing R-4 active-thread test, and the R-1 cross-thread test to mirror their mock pattern.
    2. `C:/Vibe Apps/Agentic RAG/frontend/src/hooks/useMessages.ts` — the surfaces of the SELECTED branch from Task 2's REPRO-EVIDENCE.md (anchored via Task 1's SURFACE-MAP.md).
    3. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md` (Task 2 output) — the `## Selected branch:` and `## Rationale:` sections.
    4. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md` §2.2 line 284 (Plan 02 step 5 spec for the regression test: "(a) Thread A streaming with mock `tool_end` event, (b) thread switch, (c) thread switch back, (d) assert `messagesByThread.get(threadA)` has the `tool_end`-applied state").
  </read_first>

  <behavior>
    Test name: `describe("Phase 067.5 — Row 11 empty-thread-until-refresh regression", ...)`

    Test cases (the test MUST fail against the current `useMessages.ts` — RED — and pass after Task 4's fix lands — GREEN):

    - **Test 1 (REQUIRED for all branches): "reconcile on switch-back surfaces the post-`done` state in messagesByThread.get(threadA), not an empty placeholder"**
      - Render `useMessages` with viewing thread = A.
      - `mockGetMessages` for thread A returns `[]` initially.
      - `mockGetActiveRuns` for thread A returns `[{run_id: "run-A", started_at: "2026-05-09T10:00:00Z"}]`.
      - Trigger `reconcile("threadA")`. Captures Thread A's SSE callbacks via `makeSseRecorder()`.
      - Stream a few `delta` events into Thread A's onDelta with content `"hello "` and `"world"`.
      - Switch viewing thread to B (call `setViewingThread("threadB")`).
      - `mockGetMessages` for thread B returns `[]`.
      - Trigger `reconcile("threadB")` (simulating ChatArea's mount/visibility-change firing on the new thread).
      - From the captured Thread A callbacks, fire `onDone` then `onTerminal({ status: "completed" })`.
      - `mockGetMessages` for thread A NOW returns `[{id: "db-A", thread_id: "threadA", role: "assistant", content: "hello world", runId: "run-A", runStatus: "completed", ...}]` (DB has caught up).
      - Switch viewing thread back to A: `setViewingThread("threadA")`.
      - Trigger `reconcile("threadA")` again (this is the visibilitychange-on-switch-back fire).
      - Wait for state propagation (`waitFor(...)`).
      - **Assertion:** `result.current.messages` (the useMemo derived from `messagesByThread.get(viewedThreadId)`) MUST contain a message with `content === "hello world"` AND `runStatus === "completed"`. It MUST NOT be empty, MUST NOT be only a `temp-run-A` placeholder with empty content.

    - **Test 2 (BRANCH A only — SSE cascade abort): "loadMessages on Thread B does not abort Thread A's live SSE consumer"**
      - Setup similar to Test 1; instead of firing onDone before switch-back, first switch B → A → B → A in rapid succession (each triggering loadMessages).
      - Capture each `signal.aborted` value passed to mockSubscribeToRun for run-A.
      - **Assertion:** The signal stored in `subscriptionsRef.current.get("run-A")` (the SSE consumer's signal — accessible via the recorder) MUST NOT be aborted at any point during the sequence. Only the loadMessages-internal `loadAbortRef` should abort, and it must be a SEPARATE controller from any subscription.

    - **Test 3 (BRANCH C only — MERGE strands placeholder): "loadMessages MERGE drops temp- placeholder when DB has populated row AND consumer no longer in subscriptionsRef"**
      - Insert a `temp-run-A` placeholder via `setMessagesForThread("threadA", prev => [...prev, {id: "temp-run-A", runId: "run-A", content: "", ...}])`.
      - Mark consumer as terminated: clear `subscriptionsRef` for run-A by firing onTerminal.
      - Mock `getMessages("threadA")` returns `[{id: "db-A", runId: "run-A", content: "hello world", ...}]`.
      - Call `loadMessages("threadA")`.
      - **Assertion:** `messagesByThread.get("threadA")` MUST contain ONE message with `id === "db-A"` and content `"hello world"`. It MUST NOT contain the `temp-run-A` placeholder.

    - **Test 4 (BRANCH B only — stale ref-mirror): "messagesByThreadRef sees the latest state when reconcile reads it (not one tick behind)"**
      - Apply a `setMessagesForThread("threadA", prev => [...prev, msg])` in an `act()`.
      - Synchronously (in the same `act()` block, after the setter) call `reconcile("threadA")` such that its `messagesByThreadRef.current.get("threadA")` read fires before React's commit phase finishes.
      - **Assertion:** The read returns the array WITH `msg` appended (matching the latest state, not the pre-append snapshot).

    R-1 NO-REGRESSION assertion (REQUIRED for all branches): the existing R-1 cross-thread test (Phase 067.4 Plan 02's vitest at the existing R-1 describe block in `useMessages.test.ts`) MUST still pass. Verifiable via the test runner exit code.
  </behavior>

  <action>
**STEP 1 — write the failing test (RED).** Open `frontend/src/__tests__/hooks/useMessages.test.ts`. Append a new `describe` block after the existing tests:

```typescript
describe("Phase 067.5 — Row 11 empty-thread-until-refresh regression", () => {
  // Test 1: REQUIRED for all branches — reconcile-on-switch-back must surface post-done state
  it("reconcile on switch-back surfaces the post-done state, not an empty placeholder", async () => {
    // <implementation per <behavior> Test 1 spec above>
  })

  // Branch-conditional test from REPRO-EVIDENCE.md selected branch
  // (include Test 2 if branch=A; Test 3 if branch=C; Test 4 if branch=B; Test 1 only if branch=D)
})
```

Add EITHER Test 2 (Branch A), Test 3 (Branch C), or Test 4 (Branch B) per the `## Selected branch:` value in `067.5-01-REPRO-EVIDENCE.md`. If branch = D, add only Test 1 plus a custom assertion specific to the divergent surface documented in REPRO-EVIDENCE.md's `<deviation_note>`.

Use `vi.hoisted` + `vi.mock` patterns already established at the top of the file. Use `makeSseRecorder()` for SSE callback capture. Use `act()` and `waitFor()` from `@testing-library/react` consistent with the existing tests' style.

**STEP 2 — run the test (must FAIL).** From the `frontend/` directory:

```
npm test -- --run useMessages.test
```

The new Phase 067.5 describe block tests MUST fail. Capture the failure output verbatim in the commit body for traceability. If they pass against the current (broken) `useMessages.ts`, the test is wrong (false GREEN) — re-write to match the actual repro.

**STEP 3 — commit RED.** Single commit, message:
```
test(067.5-01): RED — Phase 067.5 regression test for Row 11 empty-thread-until-refresh

Per REPRO-EVIDENCE.md selected branch <X>, this test simulates the
empty-thread-until-refresh sequence and asserts messagesByThread.get(threadA)
holds the post-done state. Currently FAILS against the unfixed useMessages.ts;
will pass after Task 4 ships the targeted fix.
```

(Per `feedback_vibe_coder_communication.md` memory: keep test file comments precise; the RED→GREEN ceremony is the standard TDD pattern from prior phases.)

**STEP 4 — DO NOT touch `useMessages.ts` in this task.** Production code change is Task 4's job.
  </action>

  <acceptance_criteria>
    - Test file `frontend/src/__tests__/hooks/useMessages.test.ts` contains a new `describe` block with the literal string `"Phase 067.5 — Row 11 empty-thread-until-refresh regression"`. Verifiable: `grep -c 'Phase 067.5 — Row 11 empty-thread-until-refresh regression' frontend/src/__tests__/hooks/useMessages.test.ts` returns at least 1.
    - The describe block contains Test 1 (REQUIRED) plus the branch-specific test (Test 2, 3, or 4 per REPRO-EVIDENCE.md) — minimum 2 `it(...)` blocks inside the new describe. Verifiable: count of `it(` between the new `describe(` opening and its closing brace is `>= 2`.
    - Running `cd frontend && npx vitest run src/__tests__/hooks/useMessages.test.ts` produces output where the Phase 067.5 tests are listed under `FAIL` (RED). Verifiable: the run exit code is non-zero AND the output contains both the literal string `"Phase 067.5"` and `"FAIL"` for those tests.
    - Existing R-1 cross-thread tests and R-4 active-thread tests in the file still pass (no regression to existing test suite). Verifiable: ALL pre-067.5 `it(...)` blocks listed under `PASS` in the same vitest run.
    - `frontend/src/hooks/useMessages.ts` UNMODIFIED in this task. Verifiable: `git diff --name-only frontend/src/hooks/ | wc -l` returns 0.
    - Commit subject contains the verb `RED` and the phrase `Phase 067.5`. Verifiable: `git log -1 --pretty=%s | grep -qE 'RED .* Phase 067.5'` (the `.*` absorbs any dash variant — em-dash, en-dash, or hyphen — surviving Windows PowerShell locale-dependent UTF-8 quoting).
    - **W-5 — describe-block it() count gate:** the new Phase 067.5 describe block contains at least 2 `it(` blocks. Verifiable: `awk '/Phase 067.5 — Row 11/,/^})/' frontend/src/__tests__/hooks/useMessages.test.ts | grep -c 'it(' | (read N; [ "$N" -ge 2 ])` (extracts text between the new describe header and the next top-level `})` close, then asserts the inner `it(` count is ≥ 2 — Test 1 REQUIRED + branch-specific Test 2/3/4).

    **Vitest runtime fallback (per Phase 063.1 Plan 03 Rule 3 deferred-item precedent in STATE.md):** If `npx vitest` fails to run on this machine due to the rolldown native binding cascade (`@rolldown/binding-win32-x64-msvc + @jridgewell/sourcemap-codec` missing), the executor MAY fall back to TypeScript-gated verification: `npx tsc --noEmit -p frontend/tsconfig.json` (or `cd frontend && npx tsc --noEmit`) MUST be clean. The RED commit body must explicitly note "vitest runtime unavailable on this machine — gated via TypeScript + grep; behavior cases documented in commit body" mirroring the prior phases' precedent. Re-running tests in CI / fresh `npm install` environment then provides the runtime gate.
  </acceptance_criteria>

  <verify>
    <automated>grep -q 'Phase 067.5 — Row 11 empty-thread-until-refresh regression' frontend/src/__tests__/hooks/useMessages.test.ts &amp;&amp; [ "$(git diff --name-only frontend/src/hooks/ | wc -l)" = "0" ] &amp;&amp; git log -1 --pretty=%s | grep -qE 'RED .* Phase 067.5' &amp;&amp; awk '/Phase 067.5 — Row 11/,/^})/' frontend/src/__tests__/hooks/useMessages.test.ts | grep -c 'it(' | (read N; [ "$N" -ge 2 ]) &amp;&amp; (cd frontend &amp;&amp; npx vitest run src/__tests__/hooks/useMessages.test.ts 2&gt;&amp;1 | grep -E '(FAIL|Phase 067.5)' || npx tsc --noEmit -p frontend/tsconfig.json)</automated>
  </verify>

  <done>RED test committed; Phase 067.5 describe block fails against the unfixed useMessages.ts (or gated via TypeScript + behavior comments per the vitest fallback); existing test suite still passes; useMessages.ts not yet modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement the targeted fix on the selected surface (GREEN phase — RED test must now pass)</name>

  <files>frontend/src/hooks/useMessages.ts (MODIFY — surgical edit to ONE of the 4 ref-mirror surfaces, OR loadMessages cascade-abort path, OR loadMessages MERGE filter, per Task 2's selected branch)</files>

  <read_first>
    1. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md` (Task 1 output — live line numbers for the implicated surface).
    2. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md` (Task 2 output — selected branch + rationale).
    3. `C:/Vibe Apps/Agentic RAG/frontend/src/hooks/useMessages.ts` (the live file — read the CURRENT state of the implicated surface immediately before editing; if drift has occurred since Task 1, re-anchor before the edit).
    4. `C:/Vibe Apps/Agentic RAG/frontend/src/__tests__/hooks/useMessages.test.ts` (the RED test from Task 3 — the assertion shapes drive the fix shape).
    5. `C:/Vibe Apps/Agentic RAG/.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md` §2.2 lines 257-285 (the 6 outside-factory + 4 ref-mirror enumeration + Plan 02 task-structure outline).
    6. `C:/Vibe Apps/Agentic RAG/CLAUDE.md` rule: D-v2.5-03 ("Realtime is best-effort hint, not source of truth — always reconcile via fetch on (re)connect"). Reconcile is the source-of-truth path; the fix MUST preserve it as the recovery mechanism.
  </read_first>

  <behavior>
    The fix MUST cause the Task 3 RED test to turn GREEN. Branch-specific fix shapes (anchor to live line numbers from SURFACE-MAP.md; the literal line numbers below assume no further drift since Task 1):

    - **Branch A — SSE consumer aborted by loadMessages cascade.** Confirm at the code level that `loadAbortRef` and the AbortControllers stored in `subscriptionsRef` are SEPARATE Controller instances (the live read at Task 1 time confirms this — `loadAbortRef.current = controller` at line 595 vs reconcile's `subscriptionsRef.current.set(run.run_id, controller)` at line 1022 use distinct `new AbortController()` instances at lines 594 and 1021 respectively). If the test still reproduces under this branch, the cascade is INDIRECT — likely via the `for (const run of activeRuns)` loop's `if (activeThreadIdRef.current !== threadId) return` early-return at reconcile line 963, which fires when loadMessages's await yields and a thread switch happens during it. Fix: hoist the activeThreadIdRef gate so it does NOT prevent re-attaching to subscriptionsRef entries that already exist for the original threadId — i.e., the early-return's intent is to skip NEW attaches, not to abort EXISTING consumers. Add a comment block citing Phase 067.5 + the gap source.

    - **Branch B — Stale ref-mirror during reconcile-on-(re)visit window.** The current ref-mirror useEffect at lines 410-412 fires after React commit (passive effect). Reconcile's `messagesByThreadRef.current.get(threadId)` read at line 977 may run before the commit if reconcile fires synchronously after a setMessagesForThread call. Fix: switch the ref-mirror useEffect to `useLayoutEffect` — runs synchronously after DOM mutations but BEFORE the browser paints, eliminating the one-tick lag for any reconcile triggered via subsequent React event handler. Same pattern can apply to `messagesRef` useEffect at lines 471-475 if Test 4 implicates it. Update the comment block to cite "Phase 067.5: useLayoutEffect closes the reconcile-on-(re)visit ref-mirror lag window — see 067.5-01-REPRO-EVIDENCE.md Branch B."

    - **Branch C — loadMessages MERGE strands empty placeholder.** The current MERGE filter at lines 643-650 keeps a temp- placeholder if its runId is NOT in dbRunIds OR if `subscriptionsRef.current.has(m.runId)` is true. The `||` is the strand source: when the SSE consumer is still in subscriptionsRef (e.g., onTerminal hasn't fired yet) AND the DB has the populated row, the placeholder lives on with empty content while the populated DB row also lives — but React keys on `id` so both render. If the placeholder is `temp-${run_id}` and the DB row has its own id, both bubbles render. Fix: tighten the predicate — drop the placeholder when the DB has a populated row (content non-empty) for the same runId, regardless of subscriptionsRef state, since the DB row is now canonical and the SSE consumer should target the DB row's id via reconcile's runId-match dedup at lines 977-979 anyway. Patch shape:

    ```typescript
    const liveTempPlaceholders = prev.filter(
      (m) => {
        if (!m.id.startsWith("temp-") || !m.runId) return false
        const dbHasRun = dbRunIds.has(m.runId)
        const dbRowForRun = dbHasRun ? data.find((d) => d.runId === m.runId) : undefined
        // Phase 067.5 (Branch C): drop placeholder when DB has a POPULATED row
        // for this runId. Pre-067.5 logic kept the placeholder if subscriptionsRef
        // still had the run_id (the live-consumer guard) — but if the DB row has
        // populated content, the consumer's reconcile runId-match dedup at line 977-979
        // will route subsequent SSE events to the DB row's id. Keeping the empty
        // placeholder in that window strands an empty bubble in the UI (Row 11 RED
        // root cause per 067.5-01-REPRO-EVIDENCE.md Branch C).
        if (dbRowForRun && dbRowForRun.content && dbRowForRun.content.length > 0) return false
        // Pre-067.5 third clause: keep when DB doesn't have it OR a live SSE consumer is bound.
        return !dbHasRun || subscriptionsRef.current.has(m.runId)
      },
    )
    ```

    - **Branch D — divergent surface.** Implement the fix per the `<deviation_note>` in REPRO-EVIDENCE.md. The fix MUST be on a NEW surface not in the 6+4 enumeration AND MUST be documented inline in `useMessages.ts` with a `// Phase 067.5 deviation:` comment block citing the evidence row. If Branch D requires touching more than 30 LOC, escalate via STOP signal to the user (the action exceeds Plan 01's narrow-scope budget per ROADMAP risk note 524).

    The fix MUST preserve:
    - `messagesByThread: Map<string, Message[]>` shape (D-067.3-R1 invariant)
    - `setMessagesForThread(threadId, updater)` write contract
    - All 21 in-factory handlers in `makeStreamCallbacks` (untouched)
    - `subscriptionsRef.current Map<string, AbortController>` keyed by run_id
    - D-063.1-11 `reconcileInFlightRef` serialization
    - D-060-01 `setViewingThread` sole-writer of `activeThreadIdRef`
    - D-063.1-12 the temp-placeholder MERGE protection for genuinely-in-flight runs (only narrow the predicate, never delete the filter wholesale)
  </behavior>

  <action>
**STEP 1 — re-anchor against the live file.** Read the current `useMessages.ts` and confirm the line numbers for the implicated surface match Task 1's SURFACE-MAP.md. If drift, update SURFACE-MAP.md inline (small edit) and proceed against current line numbers.

**STEP 2 — apply the fix per the selected branch shape.** Use the Edit tool (NOT Write — preserve unrelated content). The diff MUST be:
- For Branch A: ≤ 15 LOC of changes in reconcile (lines 935-1131)
- For Branch B: ≤ 5 LOC of changes (s/useEffect/useLayoutEffect/ + import, plus comment block)
- For Branch C: ≤ 25 LOC of changes in loadMessages MERGE (lines 639-651, replacing the filter callback)
- For Branch D: ≤ 30 LOC of changes (per deviation_note bound; otherwise escalate)

EVERY changed block MUST have a comment block prefix like:
```typescript
// Phase 067.5 (Branch <X>): <one-line summary of the fix>.
// Gap source: 067.4-HUMAN-UAT.md Row 11 (lines 333-362).
// Surface: <ref-mirror useEffect | loadMessages MERGE | loadMessages cascade-abort | <other>>.
// Rationale: <2-3 lines citing 067.5-01-REPRO-EVIDENCE.md and the test that drives this>.
```

**STEP 3 — run the test (must PASS).** From the `frontend/` directory:
```
npm test -- --run useMessages.test
```
The Phase 067.5 describe block tests MUST now PASS (GREEN). Existing R-1 cross-thread + R-4 active-thread tests MUST also still PASS (no regression).

**STEP 4 — TypeScript clean.** From the `frontend/` directory:
```
npx tsc --noEmit
```
Must complete with zero errors.

**STEP 5 — commit GREEN.** Single commit, message:
```
fix(067.5-01): GREEN — close Row 11 empty-thread-until-refresh via Branch <X>

Per 067.5-01-REPRO-EVIDENCE.md selected branch <X>, applies a surgical fix to
<implicated surface> in frontend/src/hooks/useMessages.ts. Phase 067.5 RED test
from Task 3 now passes. R-1 cross-thread + R-4 active-thread tests still pass.

Preserves D-067.3-R1 messagesByThread Map shape and setMessagesForThread write
contract; preserves D-063.1-11 reconcileInFlightRef serialization; preserves
D-060-01 activeThreadIdRef sole-writer invariant.

TypeScript clean; vitest <Phase 067.5 tests | TypeScript-gated per fallback>.
```

**STEP 6 — vitest fallback acknowledgement.** If the vitest runtime is unavailable on this machine (per the Phase 063.1 Plan 03 Rule 3 precedent), the GREEN commit body MUST include the literal sentence: "vitest runtime unavailable on this machine — gated via TypeScript + RED test diff inspection; runtime green deferred to CI / fresh npm install environment." Plan 02's closing UAT (Chrome MCP runtime) IS the lived-experience GREEN gate, so vitest-unavailable does not block Plan 01 closure.
  </action>

  <acceptance_criteria>
    - File `frontend/src/hooks/useMessages.ts` MODIFIED. Verifiable: `git diff --name-only HEAD~1 -- frontend/src/hooks/useMessages.ts | wc -l` returns 1.
    - The diff contains a comment block citing Phase 067.5. Verifiable: `git diff HEAD~1 -- frontend/src/hooks/useMessages.ts | grep -c 'Phase 067.5'` returns at least 1.
    - The diff is bounded per branch (Branch A ≤ 15, Branch B ≤ 5, Branch C ≤ 25, Branch D ≤ 30 added LOC excluding comments). Verifiable: `git diff --stat HEAD~1 -- frontend/src/hooks/useMessages.ts` shows insertions ≤ 30 + comment overhead (allow up to 60 with comments).
    - The implicated surface from REPRO-EVIDENCE.md `## Selected branch:` was actually touched. Verifiable: `git diff HEAD~1 -- frontend/src/hooks/useMessages.ts | grep -E '<branch-specific anchor>'` returns at least 1 — for Branch A: `reconcile|activeRuns|activeThreadIdRef`; for Branch B: `useLayoutEffect|messagesByThreadRef|messagesRef`; for Branch C: `liveTempPlaceholders|dbRunIds|dbRowForRun`; for Branch D: per deviation_note anchor.
    - Vitest Phase 067.5 tests pass. Verifiable: `cd frontend && npx vitest run src/__tests__/hooks/useMessages.test.ts 2>&1 | grep 'Phase 067.5'` shows the tests under PASS (or the vitest fallback applies and TypeScript is clean).
    - All pre-067.5 tests still pass (NO regression). Verifiable: vitest run exit code is 0 (or the TypeScript fallback applies and `npx tsc --noEmit` exit code is 0).
    - TypeScript clean. Verifiable: `cd frontend && npx tsc --noEmit` exit code is 0.
    - `messagesByThread: Map` shape preserved (no API surface change). Verifiable: `grep -cE 'Map<string, Message\[\]>' frontend/src/hooks/useMessages.ts` returns at least 2 (the state declaration at line 394 + the ref declaration at line 409 must BOTH survive — preserves the D-067.3-R1 invariant correctly).
    - `setMessagesForThread` write contract preserved. Verifiable: `grep -c 'setMessagesForThread(threadId,' frontend/src/hooks/useMessages.ts` returns the SAME count as pre-fix (no calls added or removed unless the fix is explicitly in setMessagesForThread itself).
    - Commit subject contains the verb `GREEN` and the phrase `close Row 11`. Verifiable: `git log -1 --pretty=%s | grep -qE 'GREEN .* close Row 11'` (the `.*` absorbs any dash variant — em-dash, en-dash, or hyphen — surviving Windows PowerShell locale-dependent UTF-8 quoting).
  </acceptance_criteria>

  <verify>
    <automated>git diff --name-only HEAD~1 -- frontend/src/hooks/useMessages.ts | grep -q useMessages.ts &amp;&amp; git diff HEAD~1 -- frontend/src/hooks/useMessages.ts | grep -q 'Phase 067.5' &amp;&amp; (cd frontend &amp;&amp; npx tsc --noEmit) &amp;&amp; (cd frontend &amp;&amp; npx vitest run src/__tests__/hooks/useMessages.test.ts 2&gt;&amp;1 | grep -q 'Phase 067.5' || true) &amp;&amp; git log -1 --pretty=%s | grep -qE 'GREEN .* close Row 11'</automated>
  </verify>

  <done>useMessages.ts patched on the implicated surface; Phase 067.5 RED→GREEN cycle complete; existing test suite + TypeScript clean; D-067.3-R1 invariants preserved; commit captures the surface, branch, and rationale.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete:

1. **Source-truth grep:** `grep -c 'Phase 067.5' frontend/src/hooks/useMessages.ts` returns at least 1; `grep -c 'Phase 067.5 — Row 11 empty-thread-until-refresh regression' frontend/src/__tests__/hooks/useMessages.test.ts` returns at least 1.
2. **TypeScript clean:** `cd frontend && npx tsc --noEmit` exit code 0.
3. **Vitest GREEN (or TypeScript fallback):** `cd frontend && npx vitest run src/__tests__/hooks/useMessages.test.ts` exit code 0 (or vitest runtime unavailable AND TypeScript clean per fallback).
4. **Surface artifacts present:** Both `.planning/phases/067.5-frontend-reconcile-fix/067.5-01-SURFACE-MAP.md` and `.planning/phases/067.5-frontend-reconcile-fix/067.5-01-REPRO-EVIDENCE.md` exist.
5. **D-067.3-R1 invariants preserved:** `grep -cE 'Map<string, Message\[\]>' frontend/src/hooks/useMessages.ts` returns at least 2 (state declaration at line 394 + ref declaration at line 409 must BOTH survive).
6. **No re-litigation of in-factory handlers:** `git diff HEAD~3 -- frontend/src/hooks/useMessages.ts | grep -c 'makeStreamCallbacks'` returns 0 (the factory is untouched OR only its function-signature comment changed; do not pad LOC).
</verification>

<success_criteria>
- [ ] Plan 01 RED→GREEN cycle complete: vitest Phase 067.5 describe block fails before Task 4, passes after Task 4 (or TypeScript fallback gated)
- [ ] `frontend/src/hooks/useMessages.ts` modified surgically on ONE of the 4 ref-mirror surfaces, OR loadMessages cascade-abort path, OR loadMessages MERGE filter, per Task 2's evidence-driven branch selection
- [ ] No regression to R-1 cross-thread switch tests (Phase 067.3 invariant preserved)
- [ ] No regression to R-4 active-thread tool-stage tests (Phase 067.4 invariant preserved)
- [ ] D-067.3-R1 messagesByThread Map shape and setMessagesForThread write contract preserved
- [ ] In-factory handlers (RESEARCH §2.2 lines 226-251) untouched
- [ ] Surface map + repro evidence artifacts committed under `.planning/phases/067.5-frontend-reconcile-fix/`
- [ ] At least 3 runtime repro attempts logged in REPRO-EVIDENCE.md OR an explicit "0 reproductions, ship instrumentation" pivot per ROADMAP risk note 524
- [ ] All commits cite Phase 067.5 and reference the gap source (067.4-HUMAN-UAT Row 11)
</success_criteria>

<output>
After completion, create `.planning/phases/067.5-frontend-reconcile-fix/067.5-01-SUMMARY.md` covering:
- Selected branch (A/B/C/D) and rationale
- The exact surface fixed (file:line range)
- Pre-fix vs post-fix vitest output (Phase 067.5 describe block) — RED → GREEN
- Pre-fix vs post-fix R-1 + R-4 vitest output — both PASS pre and post
- Whether the runtime repro fired in 3+ attempts (or pivot reason)
- D-067.3-R1 invariant preservation evidence (the `messagesByThread: Map` grep)
- Pointer to Plan 02's closing UAT for the lived-experience GREEN gate
</output>
