# Phase 067.5 closing UAT (2026-05-09)

User-driven 5-cycle Chrome runtime test of the Branch D-3-CLEAR-WIPES-STREAMING-BUCKET fix shipped in `frontend/src/hooks/useMessages.ts` at commit `3d040c7`. Plus R-1 cross-thread dual-pass and Phase 067.4 Plans 01/02/03 GREEN regression spot-check.

## Setup

- Dev frontend: `http://localhost:5173/` (HTTP 200 confirmed at runtime)
- Dev backend: `http://localhost:8000/health` (HTTP 200 confirmed)
- Branch: `v2.5-dev` (latest commit `cd043b5`, fix at `3d040c7`)
- Vite HMR will have hot-reloaded `useMessages.ts` automatically; a hard reload (`Ctrl+Shift+R`) is recommended at session start to flush any cached bundle.
- Login: `fhdmrd@gmail.com` / `123456`

## Phase A — Row 11 protocol, 5 consecutive cycles (D-067.4-QUALITY-01)

**Per-cycle steps (~60-120s each):**

1. Open 4 fresh streaming threads via "New Chat" with diverse prompts:
   - **A: Bread** — `Plan a sourdough bread recipe step by step`
   - **B: Physicists** — `List 5 famous physicists and their key contributions`
   - **C: Photosynthesis** — `Explain photosynthesis in simple terms`
   - **D: pgvector** — `Summarize what pgvector does in Postgres`
2. Mix providers across threads (OpenAI, Anthropic, OpenRouter if creds permit; otherwise repeat).
3. Submit prompts back-to-back; **navigate AWAY mid-stream on at least 2 of the 4** threads.
4. Wait ~60-120s for all streams to complete (titles generated, no "New Chat" remaining).
5. Navigate to each thread; verify the assistant message body renders WITHOUT F5.
6. **Cycle pass criterion:** 4/4 threads render their body without F5 AND 4/4 have non-`New Chat` titles AND no cross-thread leak.

### Cycle 1 (2026-05-09, user-driven)

| Thread | Prompt | Provider | run_id | Title | Body renders without F5? |
|--------|--------|----------|--------|-------|--------------------------|
| A | Bread | mixed (per protocol) | not captured | auto-generated | YES |
| B | Physicists | mixed | not captured | auto-generated | YES |
| C | Photosynthesis | mixed | not captured | auto-generated | YES |
| D | pgvector | mixed | not captured | auto-generated | YES |

**Cycle 1 verdict:** **PASS** — none of the 4 threads reproduced empty. User report verbatim: "which thread (A/B/C/D) reproduced empty: none". Backend health probed `{status: ok, redis: ok}`; no errors observed.

### Cycles 2-5 (2026-05-09, user-driven)

| Cycle | Verdict | Empty-thread reproduced? | Notes |
|-------|---------|--------------------------|-------|
| 2 | **PASS** | none | 4/4 threads rendered without F5 |
| 3 | **PASS** | none | 4/4 threads rendered without F5 |
| 4 | **PASS** | none | 4/4 threads rendered without F5 |
| 5 | **PASS** | none | 4/4 threads rendered without F5 |

User report verbatim: "all pass after all". One thread on cycle 5 (thread_id `6e2913b7-6d93-49ae-aa02-c65c843c7970`, run_id `8ca784f2-1e5e-4918-9784-c79cadee145f`) hit a backend `BadRequestError` from Anthropic API: `max_tokens: 65536 > 64000` for `claude-haiku-4-5-20251001`. **This is unrelated to Phase 067.5** — it's a model-config mismatch (backend sending 65536 max_tokens, but the model's hard cap is 64000). User clicked "Resume" → second POST succeeded with run `53ee4e5a-692a-4c0d-bfe9-874248da6d31`; thread rendered cleanly. Resume path is D-063-04 working as designed. Captured as separate carry-forward observation (worthy of a seed); does NOT invalidate the 5/5 Row 11 verdict.

## Phase B — R-1 cross-thread switch dual-pass (D-067.4-QUALITY-01)

**SKIPPED — rationale:**

R-1 cross-thread protection is already covered by the vitest at `frontend/src/__tests__/hooks/useMessages.test.ts` (it block: "preserves both threads' renders during cross-thread switch (R-1 protection)"). That test exercises the exact cross-thread mid-stream scenario at the hook level and PASSED both pre-fix (commit `b313ba4`, RED test isolated to Branch D-3) and post-fix (commit `3d040c7`, all 6 tests GREEN). The Branch D-3 fix is structurally additive — it adds a guard to `clearMessages` and does not modify the per-thread bucket write contract that R-1 depends on. User opted to skip the runtime spot-check at the closure gate; vitest evidence stands as the R-1 dual-pass guard.

## Phase C — Phase 067.4 Plans 01/02/03 GREEN regression spot-check

**SKIPPED — rationale:**

Plans 01 (model→provider router), 02 (R-3 suggestion-pills + R-4 active-thread tool-stage), and 03 (R-5 code-execution heartbeat) all closed GREEN at Phase 067.4 closure. The vitest at `frontend/src/__tests__/hooks/useMessages.test.ts` covers R-4 (`clears spinner on tool_end WITHOUT thread navigation`) and R-5 (`code_executing handler does NOT alter outputLines write semantics` + `R-5 elapsed_seconds — only updates execute_code tools with status=running`) — all 3 still pass post-fix (commit `3d040c7`). R-3 suggestion-pills is a backend SSE emit path, untouched by Plan 01's frontend-only fix. The Branch D-3 fix is scoped to `clearMessages` and does not touch the SSE event dispatcher or the code-execution heartbeat handler. User opted to skip the runtime spot-check; vitest + scope-limited diff stand as the no-regression evidence.

## Phase 067.5 closing-UAT verdict

- **Row 11 (D-067.4-QUALITY-01 5/5 cycles):** **PASS — 5/5 GREEN** (Phase A above; user-driven 2026-05-09).
- **R-1 cross-thread dual-pass:** vitest GREEN (existing test in useMessages.test.ts continues to pass post-fix).
- **Phase 067.4 Plans 01/02/03 regression:** vitest GREEN (4 inherited tests still pass post-fix; no source change to R-3/R-4/R-5 surfaces).

**Closure decision:** **GATE MET** — Row 11 5/5 cycles confirm the fix at runtime; vitest covers R-1/R-4/R-5 protection. Proceed to Task 2 (mirror Row 11 verdict to 067.4-HUMAN-UAT.md + update STATE.md + write 067.5-02-SUMMARY.md + single closure commit).

## Carry-forward observation (separate from Phase 067.5 scope)

During Cycle 5 of Phase A, one thread (`6e2913b7-6d93-49ae-aa02-c65c843c7970`, run `8ca784f2-1e5e-4918-9784-c79cadee145f`) hit a backend `BadRequestError` from the Anthropic API: `max_tokens: 65536 > 64000, which is the maximum allowed number of output tokens for claude-haiku-4-5-20251001`. The error surfaced via the existing `runStatus="failed"` path; the user clicked Resume → new run `53ee4e5a-692a-4c0d-bfe9-874248da6d31` succeeded; thread rendered cleanly. This is unrelated to Phase 067.5's frontend reconcile fix — it's a model-config mismatch in the backend Anthropic service path. Worthy of a separate seed (`SEED-NNN: claude-haiku-4-5 max_tokens default exceeds model cap`); does NOT invalidate Plan 02's GATE MET verdict.
