---
phase: 146-operator-foundation
plan: 03
type: execute
wave: 2
depends_on: [146-02]
files_modified:
  - backend/app/main.py
  - backend/tests/test_146_operator_seed.py
autonomous: true
requirements: [ADMIN-01]
user_setup:
  - service: operator-bootstrap
    why: "First operator is seeded from OPERATOR_EMAILS on backend startup (D-01)"
    env_vars:
      - name: OPERATOR_EMAILS
        source: "backend/.env (local) / Coolify env (cloud) — comma-separated emails of users that already have an auth.users row"

must_haves:
  truths:
    - "OPERATOR_EMAILS is idempotently upserted into operator_users on backend startup per D-01 — the DB table is the runtime source of truth, env is bootstrap-only"
    - "The seed is safe under multi-worker startup (WORKER_COUNT=2) via INSERT ... ON CONFLICT (user_id) DO NOTHING per D-01 — both workers race, first wins, second no-ops"
    - "A seed failure logs and the app continues (best-effort, never blocks startup) per D-01"
    - "An OPERATOR_EMAILS entry with no matching auth.users row is skipped with a warning (bootstrap, not error) and seeds on a later restart once the user signs up"
  artifacts:
    - path: "backend/app/main.py"
      provides: "lifespan call to seed_operators_from_env, best-effort wrapped"
      contains: "seed_operators_from_env"
    - path: "backend/tests/test_146_operator_seed.py"
      provides: "idempotent multi-worker seed test"
      contains: "ON CONFLICT"
  key_links:
    - from: "backend/app/main.py"
      to: "operator_service.seed_operators_from_env"
      via: "lifespan startup call after get_pg_pool()"
      pattern: "seed_operators_from_env"
---

<objective>
Wire the operator seed into backend startup: after the asyncpg pool is ready in `lifespan`, call
`seed_operators_from_env()` (authored in Plan 02) inside the same best-effort try/except the
settings-migration block uses, so a seed failure logs and the app continues. Add the idempotent-seed
test.

Purpose: D-01 — the first operator is bootstrapped from `OPERATOR_EMAILS` with no manual SQL, and it
must be concurrent-safe under `WORKER_COUNT=2`.
Output: the lifespan seed call in `main.py` + `test_146_operator_seed.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-operator-foundation/146-CONTEXT.md
@.planning/phases/146-operator-foundation/146-RESEARCH.md
@.planning/phases/146-operator-foundation/146-PATTERNS.md
@backend/app/main.py
@backend/app/services/operator_service.py

<interfaces>
<!-- seed_operators_from_env() lives in operator_service.py (Plan 02). main.py only calls it. -->
<!-- Lifespan wiring precedent (main.py:237-243) — best-effort, logs + continues:
       from app.dependencies import get_pg_pool
       try:
           await get_pg_pool()          # ensure pool exists before the write
           await _migrate_settings_override()
       except Exception as e:
           logger.error("Settings migration failed (app continues with file fallback): %s", e) -->
<!-- Idempotent-write precedent (main.py:124-214) — _migrate_settings_override: per-worker, ON-CONFLICT-safe, best-effort. -->
<!-- Recorder-pool test idiom (conftest.py:448-513, _MockAsyncpgPool): .calls lists every (sql, args); set_fetch_result([{id,email}]) for the auth.users resolve. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire seed_operators_from_env into the lifespan startup</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py (the lifespan function + the _migrate_settings_override block at :237-243, and _migrate_settings_override itself at :124-214 — the exact best-effort try/except shape to mirror)
    - backend/app/services/operator_service.py (seed_operators_from_env signature)
    - .planning/phases/146-operator-foundation/146-PATTERNS.md (§main.py — the seed to add + the lifespan wiring precedent)
  </read_first>
  <action>
    In `backend/app/main.py`, import `seed_operators_from_env` from `app.services.operator_service`. In the `lifespan` startup path, after the pool is ensured (`await get_pg_pool()`), call `await seed_operators_from_env()` inside a try/except that logs on failure and continues — mirror the existing `_migrate_settings_override` block at :237-243 exactly (best-effort, never blocks startup). Place the seed call adjacent to (after) the settings-migration block so both idempotent startup writes run per worker. Do NOT add locking/leader-election — `ON CONFLICT DO NOTHING` inside the seed makes it concurrent-safe under WORKER_COUNT=2 by construction. Log a single info line naming how many operators were resolved (optional, matches the codebase's startup logging style) so the operator can see the bootstrap happen in the uvicorn console.
  </action>
  <verify>
    <automated>cd backend && grep -q "seed_operators_from_env" app/main.py && venv/Scripts/python -c "import ast,sys; ast.parse(open('app/main.py').read())" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` imports and awaits `seed_operators_from_env()` in the lifespan startup path
    - The call is wrapped in a try/except that logs and continues (never re-raises to block startup)
    - The call is placed after `get_pg_pool()` so the pool exists
    - `main.py` parses cleanly
  </acceptance_criteria>
  <done>The operator seed runs at startup, best-effort, after the pool is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent multi-worker seed test</name>
  <files>backend/tests/test_146_operator_seed.py</files>
  <read_first>
    - backend/tests/conftest.py (the _MockAsyncpgPool / mock_asyncpg_pool recorder fixture at :448-513 — .calls, set_fetch_result)
    - backend/app/services/operator_service.py (seed_operators_from_env — the SQL it issues)
    - .planning/phases/146-operator-foundation/146-RESEARCH.md (§Validation Architecture — the seed test row)
  </read_first>
  <action>
    Create `backend/tests/test_146_operator_seed.py` driving `seed_operators_from_env()` with the `_MockAsyncpgPool` recorder. Set `settings.operator_emails` to a two-email fixture; `set_fetch_result([{ "id": <uuid>, "email": <email> }])` for the auth.users resolve. Assert: (1) the recorded SQL contains `INSERT INTO operator_users` with `ON CONFLICT (user_id) DO NOTHING`; (2) the resolve query is parameterized (`$1::text[]`), no email string interpolated into the SQL; (3) running the seed a SECOND time against the same pool produces the same conflict-safe INSERT (idempotence under concurrent startup — the ON CONFLICT path is exercised, no duplicate-key error surfaced); (4) an email with no matching auth.users row logs a warning and is NOT inserted (drive by returning fewer rows than emails). Follow the recorder-pool assertion idiom (inspect `pool.calls`).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_146_operator_seed.py -x -q && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts `ON CONFLICT (user_id) DO NOTHING` appears in the recorded INSERT SQL
    - Test asserts the auth.users resolve is parameterized (`$1::text[]`)
    - Test asserts a second seed run is conflict-safe (no duplicate write / no raised error)
    - Test asserts an unmatched email is skipped (warned, not inserted)
    - `pytest tests/test_146_operator_seed.py` passes green
  </acceptance_criteria>
  <done>The seed is proven idempotent + parameterized + concurrent-safe, matching D-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OPERATOR_EMAILS env → operator_users rows | Env-supplied emails become privileged principals; the resolve must not be SQL-injectable |
| concurrent workers → operator_users | Two workers seeding the same rows must not corrupt or duplicate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-05 | Tampering | SQL injection via the email seed | mitigate | Parameterized `$1::text[]` (no f-string SQL); Task 2 asserts parameterization. Matches the coerce_uid discipline. |
| T-146-08 | Tampering / Availability | seed race under WORKER_COUNT=2 | mitigate | `INSERT ... ON CONFLICT (user_id) DO NOTHING` — concurrent-safe by construction; best-effort try/except so a seed failure never blocks startup. Task 2 exercises the second-run idempotence. |
| T-146-SC | Tampering | npm/pip installs | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `venv/Scripts/python -m pytest tests/test_146_operator_seed.py -x` green.
- `main.py` calls `seed_operators_from_env` in the lifespan, best-effort wrapped; module parses.
- Full backend suite green.
</verification>

<success_criteria>
- Operators are idempotently seeded from OPERATOR_EMAILS at startup, concurrent-safe (D-01).
- Unmatched emails warn-and-defer; seed failure never blocks startup.
</success_criteria>

<output>
Create `.planning/phases/146-operator-foundation/146-03-SUMMARY.md` when done.
</output>
