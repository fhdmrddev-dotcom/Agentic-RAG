---
phase: 140-smart-dispatch-relevance-pre-filter-stretch
plan: 04
type: execute
wave: 2
depends_on: [140-01, 140-03]
files_modified:
  - backend/app/services/agent_loop.py
  - backend/tests/test_agent_loop_catalog_override.py
  - backend/tests/integration/test_140_escape_hatch.py
autonomous: true
requirements: [TRIG-02]

must_haves:
  truths:
    - "The pre-filter lives ONLY inside the `skill_catalog_override is None` branch — the tuple/() eval branch is byte-unchanged (D-06 eval seam + Deep Mode intact)"
    - "The query turn is embedded ONLY when the full catalog exceeds budget; the fits-budget fast path makes ZERO embed call and is byte-identical to today (D-03, Pitfall 1)"
    - "embed_texts runs via run_in_threadpool and any embed/RPC failure fails open to inject-all-up-to-budget (sim_by_id=None), never crashing or emptying the catalog (D-05)"
    - "match_skills is called with the current embedding_model (D-10 stale guard) and returns only this user's owner+global enabled skills (no cross-user leak, V4)"
    - "load_skill loads any enabled skill by exact name even when it was trimmed from the menu — the escape hatch is verified, not rebuilt (D-02 SC#3)"
  artifacts:
    - path: "backend/app/services/agent_loop.py"
      provides: "pre-filter wiring inside the override-None branch: budget gate → threadpool embed → match_skills RPC → build_skill_catalog_block; fail-open"
      contains: "build_skill_catalog_block"
    - path: "backend/tests/test_agent_loop_catalog_override.py"
      provides: "extended: fits-budget byte-identical + over-budget-trim + fail-open cases; None/()/tuple branches still green"
      exports: ["test_deep_mode_unchanged"]
    - path: "backend/tests/integration/test_140_escape_hatch.py"
      provides: "proves _handle_load_skill loads a menu-absent skill by exact name"
      exports: ["test_load_skill_by_name_bypasses_catalog"]
  key_links:
    - from: "backend/app/services/agent_loop.py (over-budget branch)"
      to: "supabase.rpc('match_skills', ...) + build_skill_catalog_block"
      via: "await run_in_threadpool(embed_texts, [body.content]) → RPC → sim_by_id → pure fn"
      pattern: "match_skills"
    - from: "backend/app/services/agent_loop.py (pin-scan)"
      to: "history_resp.data load_skill tool_calls"
      via: "_recently_loaded_skill_names(history_resp.data) → mapped to enabled skill ids"
      pattern: "_recently_loaded_skill_names"
---

<objective>
Wire the pre-filter into the ONE sanctioned spot: the `skill_catalog_override is None` branch of the catalog
injection at `agent_loop.py:1197-1225`. Fits-budget → byte-identical fast path (no embed). Over-budget →
embed the turn (threadpool), rank via `match_skills`, assemble via `build_skill_catalog_block` (Plan 03),
fail open on any embed/RPC error. The eval-tuple branch (D-06) and Deep Mode stay byte-identical. Also verify
(do not rebuild) the `load_skill`-by-name escape hatch.

Purpose: This is the consumer that turns the Plan 01 RPC + Plan 03 pure fn into live behavior — the SC#1/
SC#2/SC#3 deliverable on the real hot path, inside the G-5 fence (agent_loop.py only; NEVER threads.py).

Output: `agent_loop.py` wiring + extended `test_agent_loop_catalog_override.py` + `test_140_escape_hatch.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-CONTEXT.md
@.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-RESEARCH.md
@.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-PATTERNS.md

<interfaces>
<!-- From Plan 03 (backend/app/services/skill_catalog_filter.py): -->
<!--   resolve_skill_catalog_budget(app_settings) -> int -->
<!--   build_skill_catalog_block(enabled, budget, model, pinned_recent_ids, sim_by_id) -> str -->
<!--   _recently_loaded_skill_names(history_rows) -> set[str] -->
<!-- From Plan 01 (migration 091): RPC match_skills(query_embedding, match_user_id, p_embedding_model). -->
<!-- Already imported in agent_loop.py: run_in_threadpool (line 45), LOAD_SKILL_POLICY. -->
<!-- MUST add import: embed_texts (from app.services.openai_service — currently NOT imported at line 48). -->
<!-- Existing catalog block to modify (agent_loop.py:1197-1225) — the override-None branch ONLY. -->
<!-- User turn = body.content (used at agent_loop.py:2483, 2671). History raw rows = history_resp.data (fetched @1166-1173). -->
<!-- RPC call idiom (retrieval_service.py:50-65): current_model = getattr(user_settings,"embedding_model","") or "text-embedding-3-small"; await aexec(supabase.rpc("match_skills", params)). -->
<!-- Existing seam test harness: tests/test_agent_loop_catalog_override.py (_capture_system_prompt via create_adaptive_streaming_chat patch). -->
<!-- Escape hatch to VERIFY (not rebuild): _handle_load_skill (tool_dispatcher.py:666-683) queries skills by exact name + is_enabled, catalog-independent. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the pre-filter into the override-None branch (budget gate → threadpool embed → match_skills → build_skill_catalog_block; fail-open) + extend the seam test</name>
  <files>backend/app/services/agent_loop.py, backend/tests/test_agent_loop_catalog_override.py</files>
  <read_first>
    - backend/app/services/agent_loop.py (lines 1197-1225 the catalog block to modify; 1166-1173 the history query; 915-917 the _pinned_skill provenance; 45 the run_in_threadpool import; 48-53 the openai_service import block to extend with embed_texts).
    - backend/app/services/retrieval_service.py (lines 42-65) — the threadpool-wrapped embed + RPC-call idiom (current_model default, aexec(supabase.rpc(...))).
    - backend/app/services/skill_catalog_filter.py (from Plan 03) — the three functions this task imports.
    - backend/tests/test_agent_loop_catalog_override.py (full file) — the `_capture_system_prompt` harness + the existing None/()/tuple cases (`test_deep_mode_unchanged`) that MUST stay green (D-06).
  </read_first>
  <behavior>
    - Test 1 (fits-budget byte-identical): with a small enabled set under budget and `skill_catalog_override=None`, the captured system prompt's `## Available Skills` block equals today's exact output, and NO embed/RPC mock is invoked (fast path, D-03).
    - Test 2 (over-budget trim): with an over-budget enabled set, a mocked `embed_texts` + mocked `match_skills` returning similarities, the captured prompt contains the high-sim skills, omits the lowest-sim non-pinned skill, and ends with the honest marker.
    - Test 3 (fail-open): with `embed_texts` raising, the captured prompt still contains a catalog (inject-all-up-to-budget by name), no exception propagates (D-05).
    - Test 4 (D-06 unchanged): the existing `None` / `()` / `(skill,)` override cases (`test_deep_mode_unchanged`) remain byte-identical — the tuple branch never embeds or ranks.
  </behavior>
  <action>
    Modify ONLY the `if skill_catalog_override is None:` branch of the catalog block (agent_loop.py:1203-1225).
    Add `embed_texts` to the `from app.services.openai_service import (...)` block (line 48) and import
    `resolve_skill_catalog_budget, build_skill_catalog_block, _recently_loaded_skill_names` from
    `app.services.skill_catalog_filter`. In the None branch: change the skills `.select("name, description")`
    to `.select("id, name, description")` (the pure fn + RPC key on id, avoiding the SEED-102 name-collision).
    After `enabled_skills = _skills_resp.data or []` and inside the existing `if enabled_skills:` guard,
    replace the hardcoded `catalog_lines`/`catalog_note` assembly with:
    (1) `budget = resolve_skill_catalog_budget(user_settings)`;
    (2) `pinned_names = _recently_loaded_skill_names(history_resp.data)` mapped to enabled skill ids →
    `pinned_recent_ids`;
    (3) compute the full-block token estimate; if `budget > 0` AND the full block exceeds budget, embed the
    turn: `q_vec = (await run_in_threadpool(embed_texts, [body.content], user_settings=user_settings))[0]`,
    then `ranked = await aexec(supabase.rpc("match_skills", {"query_embedding": q_vec, "match_user_id":
    current_user["id"], "p_embedding_model": getattr(user_settings, "embedding_model", "") or
    "text-embedding-3-small"}))`, `sim_by_id = {r["id"]: r["similarity"] for r in (ranked.data or [])}` —
    ALL inside `try/except Exception` that `logger.warning("skill pre-filter embed/rank failed; failing
    open", exc_info=True)` and sets `sim_by_id = None` (D-05 fail-open); otherwise `sim_by_id = None` (fast
    path — NO embed call, Pitfall 1);
    (4) `catalog_note = build_skill_catalog_block(enabled_skills, budget, user_settings.llm_model,
    pinned_recent_ids, sim_by_id)`; `active_system_prompt = active_system_prompt + catalog_note`.
    Leave the `else:` (eval-tuple) branch and everything after it UNCHANGED (D-06). Do NOT touch
    threads.py. Extend `test_agent_loop_catalog_override.py` with the four behavior cases above (mock
    `embed_texts` + `supabase.rpc("match_skills")`), keeping the existing seam tests green.
  </action>
  <acceptance_criteria>
    - The pre-filter code is strictly inside `if skill_catalog_override is None:`; the `else` tuple branch is textually unchanged (diff shows no edits below the None branch).
    - Fast path: when the full block fits budget, no `embed_texts`/`match_skills` call is made and the emitted block is byte-identical to today (Test 1 asserts the mock was not called + string equality).
    - Over-budget: `embed_texts` is wrapped in `run_in_threadpool`; `match_skills` is called with `p_embedding_model` set; the emitted block omits the lowest-sim non-pinned skill and ends with the marker.
    - Embed failure → warning logged, `sim_by_id=None`, catalog still emitted (no raise) — Test 3.
    - `test_deep_mode_unchanged` and the existing `()`/tuple cases stay green (D-06).
    - `grep -c "threads" ` on the diff surface = 0 changes to `backend/app/api/threads.py` (G-5): `files_modified` excludes it.
    - `pytest tests/test_agent_loop_catalog_override.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && python -m pytest tests/test_agent_loop_catalog_override.py -x && grep -q "build_skill_catalog_block" app/services/agent_loop.py && grep -q "run_in_threadpool(embed_texts" app/services/agent_loop.py && echo OK</automated>
  </verify>
  <done>Pre-filter wired into the override-None branch only: fits→byte-identical no-embed fast path, over-budget→threadpool embed + match_skills + pure trim, fail-open on error; eval tuple branch + Deep Mode byte-unchanged (D-06). Seam tests extended and green.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the load_skill-by-name escape hatch (D-02 SC#3) — integration test, not a rebuild</name>
  <files>backend/tests/integration/test_140_escape_hatch.py</files>
  <read_first>
    - backend/app/services/tool_dispatcher.py (lines 666-683 `_handle_load_skill`) — confirm it queries `skills` by exact `name` + `is_enabled` independent of the injected catalog.
    - backend/tests/integration/ — an existing tool_dispatcher/load_skill test to mirror the fixture + dispatch style.
  </read_first>
  <action>
    Create `backend/tests/integration/test_140_escape_hatch.py` proving the D-02 escape hatch already holds:
    construct a scenario where a skill exists + is_enabled but is NOT in the (trimmed) injected catalog, then
    dispatch `load_skill(skill_name=<that exact name>)` through `_handle_load_skill` and assert it resolves +
    loads the skill (returns the instructions / success), because `_handle_load_skill` keys on name +
    is_enabled, not on the catalog. This is a VERIFICATION test — do NOT modify `_handle_load_skill`. Mock
    the supabase skill lookup to return the enabled skill by name. Name the test
    `test_load_skill_by_name_bypasses_catalog`.
  </action>
  <acceptance_criteria>
    - The test loads a skill by exact name that is absent from the injected menu, and asserts a successful load — proving the escape hatch (SC#3 second half).
    - `_handle_load_skill` (tool_dispatcher.py) is NOT modified (this plan's `files_modified` excludes tool_dispatcher.py).
    - `pytest tests/integration/test_140_escape_hatch.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && python -m pytest tests/integration/test_140_escape_hatch.py -x</automated>
  </verify>
  <done>Escape hatch proven: a menu-absent enabled skill still loads by exact name; no rebuild of _handle_load_skill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user turn (`body.content`) → embedding input | Untrusted text used only as embed input; the query vector is bound as an RPC param, never string-interpolated. |
| `match_skills` RPC → injected system prompt | The ranked set must be exactly this user's owner+global enabled skills — no cross-user leak. |
| eval-override seam | The pre-filter must not alter the tuple branch nor let eval inputs bleed into the live None branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-07 | Information Disclosure | over-budget branch surfaces another user's skill | mitigate | `match_skills` reproduces today's owner+global+is_enabled scope (Plan 01 T-140-01); fail-open injects ONLY this user's `enabled_skills` set (the same set today's query returns) up to budget — never a wider set. Test asserts a private skill of user B never ranks for user A (extend override test with a scoped RPC mock). |
| T-140-08 | Denial of Service | per-turn embed as latency amplifier | mitigate | Embed ONLY on the over-budget branch (fast path makes zero embed call, Pitfall 1); `run_in_threadpool`; fail-open on timeout — the hot path never blocks on the embedder. |
| T-140-09 | Tampering | pre-filter breaks the eval A/B seam (D-06) | mitigate | Pre-filter lives strictly inside `if skill_catalog_override is None:`; the `else` tuple branch is byte-unchanged; `test_deep_mode_unchanged` + the `()`/tuple cases stay green. |
| T-140-13 | Tampering | malicious skill description as prompt injection | accept | Pre-existing surface — the catalog already injects descriptions today; this phase does not widen exposure (it only trims). No new mitigation required. |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH audit = none). |
</threat_model>

<verification>
- `pytest tests/test_agent_loop_catalog_override.py tests/integration/test_140_escape_hatch.py -x` green.
- `grep` confirms `build_skill_catalog_block` + `run_in_threadpool(embed_texts` present in agent_loop.py.
- `files_modified` excludes `backend/app/api/threads.py` (G-5) and `tool_dispatcher.py` (escape hatch verified, not rebuilt).
</verification>

<success_criteria>
- Pre-filter live on the hot path inside the override-None branch only: byte-identical fast path, over-budget threadpool embed + match_skills + pure trim, fail-open.
- D-06 eval seam + Deep Mode byte-identical (seam tests green).
- Escape hatch verified (menu-absent skill loads by name).
</success_criteria>

<output>
Create `.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-04-SUMMARY.md` when done.
</output>
