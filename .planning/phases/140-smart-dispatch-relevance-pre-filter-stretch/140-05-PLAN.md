---
phase: 140-smart-dispatch-relevance-pre-filter-stretch
plan: 05
type: execute
wave: 3
depends_on: [140-01, 140-02, 140-04]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [TRIG-02]
user_setup:
  - service: supabase-local
    why: "Migration 091 must be applied to the live LOCAL DB via the SQL editor — this project NEVER runs supabase db push/db reset (preserves dev data, CLAUDE.md)."
    dashboard_config:
      - task: "Paste supabase/migrations/091_skill_embeddings.sql into the Supabase SQL editor and run it against the local DB"
        location: "Supabase Studio (local) → SQL editor"

must_haves:
  truths:
    - "Migration 091 is applied to the live LOCAL DB via the SQL editor (NEVER db push/db reset) — the DB-CHECK test (Plan 01) flips green"
    - "The skill-vector backfill job has run once so existing enabled skills have vectors in skill_embeddings (D-01 population); absence of a row degrades to D-05 fail-open until then"
    - "supabase/full-schema.sql is regenerated from the live DB (no --reset) and committed alongside migration 091"
    - "The skill_catalog_max_tokens app_settings row + the cloud migration-apply are recorded as non-code deploy-parity artifacts (D-04 default ships in the column default; a non-default value is hand-applied to cloud)"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated single-file deploy artifact including the migration-091 objects"
      contains: "skill_embeddings"
  key_links:
    - from: "supabase/migrations/091_skill_embeddings.sql"
      to: "live local Postgres (:54322)"
      via: "operator paste into Supabase SQL editor (never db push)"
      pattern: "skill_embeddings"
---

<objective>
[BLOCKING] Apply migration 091 to the live LOCAL DB (operator-gated — this project never runs `supabase db
push`/`db reset`), run the one-time skill-vector backfill so existing skills get vectors, regenerate + commit
`full-schema.sql`, and record the cloud deploy-parity artifacts. After this plan, the Plan 01 DB-CHECK and
the Plan 02 service integration tests flip green — without this apply, build/type checks pass but the pre-
filter has no live table (false-positive verification), so this plan is MANDATORY before `/gsd:verify-work`.

Purpose: Turn the authored schema + backfill job into live runtime state. This is the non-code half of the
phase (CLAUDE.md deploy-parity discipline).

Output: applied migration + populated `skill_embeddings` + regenerated `supabase/full-schema.sql`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-CONTEXT.md
@.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-RESEARCH.md
@supabase/migrations/091_skill_embeddings.sql
@backend/app/services/skill_embedding_service.py

<interfaces>
<!-- CLAUDE.md migration flow: paste the numbered SQL into the Supabase SQL editor (NEVER db push/db reset), -->
<!--   then `bash scripts/regenerate-full-schema.sh` (live-DB dump, NO --reset), commit BOTH the migration + full-schema.sql. -->
<!-- Backfill entrypoint: skill_embedding_service.skill_reembed_job(supabase, user_id, app_settings). -->
<!-- Deploy parity precedent: harness_judge_model / mig 078 — a non-default app_settings value is re-applied to cloud by hand. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: [BLOCKING] Operator applies migration 091 to the live LOCAL DB</name>
  <what-built>Migration `supabase/migrations/091_skill_embeddings.sql` (Plan 01) — the `skill_embeddings` table + owner-only RLS + two mark-stale triggers + the `match_skills` RPC + the `app_settings.skill_catalog_max_tokens` column.</what-built>
  <how-to-verify>
    1. Open Supabase Studio (local) → SQL editor.
    2. Paste the ENTIRE contents of `supabase/migrations/091_skill_embeddings.sql` and run it against the local DB.
       Do NOT run `supabase db push` or `supabase db reset` (they wipe/preserve incorrectly — CLAUDE.md rule).
    3. Confirm no SQL errors. The objects are idempotent (`CREATE OR REPLACE` / `IF NOT EXISTS` / `DROP TRIGGER IF EXISTS`), so a re-run is safe.
    4. (Deploy parity — note only, not now) the SAME migration must later be applied by hand to CLOUD Supabase, and any non-default `skill_catalog_max_tokens` value re-applied to cloud `app_settings` (mig 078 / harness_judge_model precedent).
  </how-to-verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/integration/test_140_migration_091.py -x` exits 0 (all DB-CHECK assertions green — table, vector column, both triggers, match_skills RPC, budget column, RLS policy).
  </acceptance_criteria>
  <resume-signal>Type "applied" once migration 091 runs clean in the SQL editor and the DB-CHECK test passes.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the one-time skill-vector backfill + confirm service integration green</name>
  <files>(no source files — runtime data population + test run)</files>
  <read_first>
    - backend/app/services/skill_embedding_service.py (`skill_reembed_job`) — the backfill entrypoint + its args.
    - backend/tests/integration/test_140_skill_embedding_service.py — the integration assertions that flip green once the table exists.
    - .planning references: reference_local_dev_app.md (test user id d8a54002-... / login) — the owner whose skills to backfill locally.
  </read_first>
  <action>
    Invoke `skill_reembed_job(supabase, user_id, app_settings)` once for the local owner(s) with enabled
    skills, so existing skills get vectors written into `skill_embeddings` (a real embedding round-trip — the
    reembed-shaped job, off the hot path). Use a throwaway invocation script OUTSIDE `backend/` (never write
    scratch `.py` into the uvicorn-watched `backend/` tree — feedback_no_scratch_in_watched_tree), or the
    existing reembed CLI/admin entrypoint if one exists. Then run the Plan 02 service integration test +
    the Plan 01 DB-CHECK against the live table. Confirm a `SELECT count(*) FROM skill_embeddings` returns a
    non-zero row count for the seeded owner (psycopg2 :54322). If the embedding provider is unavailable,
    note it — the system stays D-05 fail-open (inject-all) until the backfill completes, so this is
    recall-degraded, not broken.
  </action>
  <acceptance_criteria>
    - After the backfill, `skill_embeddings` has ≥ 1 row for the local owner with enabled skills (psycopg2 count > 0).
    - `cd backend && python -m pytest tests/integration/test_140_skill_embedding_service.py tests/integration/test_140_migration_091.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && python -m pytest tests/integration/test_140_skill_embedding_service.py tests/integration/test_140_migration_091.py -x</automated>
  </verify>
  <done>Backfill run once; skill_embeddings populated for the local owner; service integration + DB-CHECK green against the live table.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate full-schema.sql + record deploy-parity artifacts</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - CLAUDE.md (migration discipline paragraph) — the regen command + the "never hand-edit full-schema.sql" rule.
    - scripts/regenerate-full-schema.sh — confirm it dumps the live DB with NO reset by default.
  </read_first>
  <action>
    Run `bash scripts/regenerate-full-schema.sh` (default = live-DB dump, NO `--reset` — never wipe local
    data). Confirm the regenerated `supabase/full-schema.sql` now contains the `skill_embeddings` table, the
    `match_skills` function, and the `skill_catalog_max_tokens` column. Do NOT hand-edit the file. Commit
    BOTH `supabase/migrations/091_skill_embeddings.sql` (from Plan 01) and the regenerated
    `supabase/full-schema.sql` together. In the SUMMARY, record the deploy-parity checklist for cloud
    (non-code half, flagged at deploy time, NOT executed now): (1) apply migration 091 to cloud Supabase by
    hand via the SQL editor; (2) run the cloud skill-vector backfill; (3) re-apply any non-default
    `skill_catalog_max_tokens` value to cloud `app_settings` (default 1500 ships in the column default, so
    the default needs nothing).
  </action>
  <acceptance_criteria>
    - `grep -q "skill_embeddings" supabase/full-schema.sql` AND `grep -q "match_skills" supabase/full-schema.sql` AND `grep -q "skill_catalog_max_tokens" supabase/full-schema.sql` all succeed.
    - `full-schema.sql` was produced by the regen script (not hand-edited).
    - The SUMMARY records the 3-item cloud deploy-parity checklist.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "skill_embeddings" supabase/full-schema.sql && grep -q "match_skills" supabase/full-schema.sql && grep -q "skill_catalog_max_tokens" supabase/full-schema.sql && echo OK</automated>
  </verify>
  <done>full-schema.sql regenerated (no reset) with the migration-091 objects; migration + full-schema committed together; cloud deploy-parity checklist recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SQL → live DB | Applying DDL to the live DB is an operator-gated, reviewed step (never automated `db push`). |
| service-role backfill → skill_embeddings | The backfill writes as service-role; scope is enforced in `skill_reembed_job` (Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-10 | Tampering / Elevation | applying unreviewed SQL to the live DB | mitigate | Operator pastes the EXACT reviewed `091_skill_embeddings.sql` into the SQL editor (blocking-human gate); no `db push`/`db reset` (data-preserving, CLAUDE.md); local-first, cloud apply is a separate later deploy step. Objects are idempotent. |
| T-140-11 | Information Disclosure | backfill writing/reading cross-user vectors | mitigate | `skill_reembed_job` (Plan 02) hand-scopes `.eq("user_id", ...)`; owner-only RLS on `skill_embeddings`. Verified by the Plan 02 service test + this plan's non-zero owner-scoped count. |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH audit = none). |
</threat_model>

<verification>
- Plan 01 DB-CHECK + Plan 02 service integration green against the LIVE table (post-apply).
- `full-schema.sql` regenerated and contains the migration-091 objects.
- Full suite green (`cd backend && python -m pytest`) before `/gsd:verify-work`, then the 4-axis SC#10 UAT (140-VALIDATION.md).
- `files_modified` is only `supabase/full-schema.sql` (+ the committed migration from Plan 01); no threads.py (G-5).
</verification>

<success_criteria>
- Migration 091 applied to the live LOCAL DB (SQL editor, no db push); DB-CHECK green.
- skill_embeddings populated by the one-time backfill; service integration green.
- full-schema.sql regenerated + committed; cloud deploy-parity checklist recorded.
</success_criteria>

<output>
Create `.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-05-SUMMARY.md` when done.
</output>
