---
phase: 199-the-component-map
plan: 10
type: execute
wave: 3
depends_on: []
files_modified:
  - frontend/src/components/workflows/library/LibraryToolbar.tsx
  - frontend/src/components/workflows/library/RunModal.tsx
  - frontend/src/components/workflows/library/ForkNameDialog.tsx
  - frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx
  - frontend/src/components/workflows/library/LibraryToolbar.test.tsx
  - frontend/src/components/workflows/library/ForkNameDialog.test.tsx
  - frontend/src/pages/__tests__/RunModal.test.tsx
  - frontend/src/pages/__tests__/PublishedCardDelete.test.tsx
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "The three library dialogs read in a colleague's language — no machine vocabulary and no fabricated estimate reaches any of them"
    - "The delete sheet stays the HEAVIEST of the three action-guard grades, and the fork stays the lightest — the graded-guard vocabulary is not spent on a harmless action"
    - "The toolbar's create control is still FIRST in DOM order, asserted rather than assumed"
    - "None of the four surfaces renders MORE at rest than before"
  artifacts:
    - path: "frontend/src/components/workflows/library/RunModal.tsx"
      provides: "The run launch dialog, re-presented"
    - path: "frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx"
      provides: "The victim-naming delete guard, re-presented without weakening"
  key_links:
    - from: "frontend/src/components/workflows/library/LibraryToolbar.tsx"
      to: "frontend/src/components/workflows/library/libraryVocabulary.ts"
      via: "every library word has ONE home"
      pattern: "libraryVocabulary"
    - from: "frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx"
      to: "the exact server counts fetched BEFORE the destructive action is offered"
      via: "victim naming — the heaviest guard grade"
      pattern: "delete"
---

<objective>
Re-present the library toolbar and the three library dialogs in the adopted design language, against
sheet `c6-library-dialogs`.

Purpose: DES-01 on the surfaces a person passes through to LAUNCH, COPY or DESTROY a workflow. Sheet
c6 *"draws the hard cases nobody draws — an empty name, a failed run on the card, and three
identical titles adjacent — plus the toolbar on one shared baseline."*

Output: a re-presented toolbar and three dialogs, with the machine vocabulary and the fabricated
estimate in the sheet refused rather than transcribed.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c6-library-dialogs.html
@.claude/skills/sketch-findings-agentic-rag/references/workflows-page.md
@.claude/skills/sketch-findings-agentic-rag/references/control-room-shell-and-receipts.md
@docs/HOT-FILE-LEDGER.md
</context>

<wave_rationale>
⚠ **`depends_on` IS EMPTY AND THAT IS CORRECT — this plan has no dependency on any other plan in the
phase.** Its wave-3 placement is a CONCURRENCY-BUDGET decision, not a dependency: CLAUDE.md records
that at THREE concurrent test-running agents the count gate goes non-deterministic regardless of the
worker cap, so the ten plans are spread across three waves rather than dispatched at once. Stated
here so a checker reading `wave: 3` with an empty `depends_on` finds the reason rather than a gap.
This plan may be pulled forward into any earlier wave that has spare concurrency.
</wave_rationale>

<already_built_do_not_rebuild>
⚠ **THE CARD HALF OF SHEET C6 IS ALREADY BUILT — Phase 192.2 shipped it, and it is the ONE sheet of
the eleven delivered before this phase.** `WorkflowCard.tsx` renders sketch 179 variant C: the run
gutter, the NAME leading line 1, and line 2 saying the run truth then the state in business words.

**`WorkflowCard.tsx` and `WorkflowsPage.tsx` are NOT in this plan's `files_modified` and must not be
edited.** If a change appears to require either, the task **STOPS and reports** — both carry recent
characterization pins (`WorkflowCard.baseline.test.tsx` at 29 cases, byte-unchanged through two
waves) that this phase has no mandate to disturb.

⚠ **A new module under `library/` must be added to `LIBRARY_SUBTREE_PATHS`**
(`librarySubtree.fences.test.ts`) **in the same commit**, or it is swept by six fences **not at all**
while every fence stays green. Phase 192.2 recorded this after adding `cardFace.ts`.
</already_built_do_not_rebuild>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports**.

**THE CANNOT-EXPRESS RULE (`SEED-155`).** If the shipped component cannot express the sheet, report
it — what the sheet asks for, what the component can do, and the gap. Never fake it.
</scope_fence>

<known_sheet_flaws_do_not_inherit>
Two elements of sheet c6's run dialog are refused on sight, and the reasons are this project's own
recorded rules rather than taste:
1. ⚠ **`Target nodes: production-cluster`** — infrastructure vocabulary printed to a business user.
   DES-01's own clause: *the mechanism is never printed to the user*. **REFUSED.**
2. ⚠ **`Estimated time: ~45s`** — a determinate estimate nothing in this system computes. It is the
   same class of fabricated precision as sheet c3's `(4/12)`. **REFUSED.** The dialog's shipped
   honest destination line is the right shape and stays.
</known_sheet_flaws_do_not_inherit>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the toolbar and the three dialogs, and reconcile them against sheet c6</name>
  <files>frontend/src/components/workflows/library/LibraryToolbar.test.tsx, frontend/src/pages/__tests__/RunModal.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c6-library-dialogs.html` — the toolbar, the card system (already built), the run dialog, the fork dialog, the delete sheet
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheets 4-9"
    - `frontend/src/components/workflows/library/LibraryToolbar.tsx` — ⚠ the create control leads by COMPOSITION, not by vigilance, and its DOM ORDER is asserted with `compareDocumentPosition` rather than assumed
    - `frontend/src/components/workflows/library/RunModal.tsx` — ⚠ it closes over NOTHING from the page; that is the property that made it liftable and the property the next author must keep true. Its `:NNN` comment references point at the PRE-MOVE page and are kept deliberately.
    - `frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx` — ⚠ the HEAVIEST of three graded action-guards: it fetches EXACT server counts BEFORE offering the destructive action, names the victim, raises the amber cancel-first banner only when a run is live, and has NO optimistic vanish and NO undo. **D-15 and D-18 are arguments made BY REFERENCE to this guard and become FALSE the moment it quietly weakens.**
    - `frontend/src/components/workflows/library/ForkNameDialog.tsx` — ⚠ D-15: the fork ships NO confirm sheet, because a heavy guard on a harmless action spends the vocabulary the delete guard relies on
    - `frontend/src/components/workflows/library/libraryVocabulary.ts` — every library word has ONE home
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` and its `LIBRARY_SUBTREE_PATHS`
  </read_first>
  <action>
Measure before changing anything. No source byte outside the two named test files may move.

Inventory the RESTING atoms of all four surfaces as literal strings — the toolbar, the run dialog,
the fork dialog in each name-check state, and the delete sheet with and without a live run — pinned
PRESENT so a later removal is proved by INVERSION, never by deletion.

Then reconcile against the sheet, with a verdict per element:
  - **The toolbar on one shared baseline.** Record what the toolbar renders today and whether the
    create control, the always-on search, the counted chips, the quiet in-flight marker, the project
    select with its honest line, and the way out of an over-filtered list already share one baseline.
  - **The run dialog.** Record `Target nodes` and `Estimated time` as REFUSED with their reasons.
    Record the shipped honest destination line as ALREADY-SHIPPED. The remaining rows are the KB
    scope select, the staged template upload, the kickoff textarea and the two footer buttons.
  - **The fork dialog.** The sheet draws a name field with an availability check. Record what the
    shipped dialog does. ⚠ Do NOT add a confirm step — D-15 forbids it by name, and adding one
    silently falsifies the delete guard's own argument.
  - **The delete sheet.** ⚠ The sheet's consequence line (*"12 historical runs and 5 configured
    phases"*) is ALREADY-SHIPPED and heavier than the drawing. Record ALREADY-SHIPPED and assert the
    guard's four properties still hold: exact server counts fetched before the offer, victim named,
    banner raised only when a run is live, no optimistic vanish and no undo. **Any weakening is a
    blocker, not a finding.**
  - **The card cases** the sheet draws (empty name, failed run, three identical titles adjacent) —
    record as ALREADY-BUILT by Phase 192.2 with the reference; do not touch `WorkflowCard.tsx`.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/library/LibraryToolbar.test.tsx src/components/workflows/library/ForkNameDialog.test.tsx src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx src/pages/__tests__/PublishedCardDelete.test.tsx src/components/workflows/library/librarySubtree.fences.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The inventory pins pass and name atoms as literals across all four surfaces.
    - `Target nodes` and `Estimated time` are recorded REFUSED with their reasons.
    - The delete guard's four properties are each asserted, and the assertions pass.
    - `git status --short` shows exactly the two named test files, and NO `WorkflowCard.tsx` or `WorkflowsPage.tsx`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Re-present the toolbar and the run dialog</name>
  <files>frontend/src/components/workflows/library/LibraryToolbar.tsx, frontend/src/components/workflows/library/RunModal.tsx, frontend/src/components/workflows/library/LibraryToolbar.test.tsx, frontend/src/pages/__tests__/RunModal.test.tsx</files>
  <read_first>
    - Task 1's reconciliation table — the work list
    - `LibraryToolbar.tsx`'s D-02 block — an affordance that leads a persistent toolbar cannot drift back down a grid, because there is no grid above it to drift below
  </read_first>
  <action>
Apply ONLY the BUILT toolbar and run-dialog rows. Hierarchy, typography, spacing, tone and removal.

Binding constraints:
  - **The create control stays FIRST in DOM order**, asserted with `compareDocumentPosition`. A
    visual re-order that leaves DOM order intact is a keyboard regression; a DOM re-order that
    leaves the visual intact is a different one. Assert both.
  - **`RunModal` must continue to close over NOTHING from the page** — no page state, no page-scoped
    const, no closure over a page-body handler.
  - **No machine vocabulary and no estimate.** `Target nodes` and `Estimated time` do not ship.
  - **Every word comes from `libraryVocabulary.ts`.** Nothing is re-spelled in a component.
  - **Do not create a new module under `library/`** unless it is added to `LIBRARY_SUBTREE_PATHS` in
    the same commit.
  - **Verify every colour token the sheet asks for RESOLVES** in `frontend/tailwind.config.js` — a
    class that compiles to nothing renders identically to a deliberately unpainted arm.

Where the sheet removes an atom, invert the Task-1 assertion; delete none. Never re-baseline a
characterization pin to turn red green.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/library/ src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx src/pages/WorkflowsPage.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - Neither surface renders MORE at rest than the Task-1 inventory recorded; removals proved by inversion, zero assertions deleted.
    - The create control is asserted FIRST in DOM order AND visually first.
    - `RunModal` closes over nothing from the page — asserted or re-verified by reading its prop list.
    - `WorkflowCard.baseline.test.tsx` is byte-unchanged (`git diff --numstat` empty) and still passes unedited.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33, ZERO under `components/workflows/library/` or `pages/`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Re-present the fork dialog and the delete sheet without changing their guard grades</name>
  <files>frontend/src/components/workflows/library/ForkNameDialog.tsx, frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx, frontend/src/components/workflows/library/ForkNameDialog.test.tsx, frontend/src/pages/__tests__/PublishedCardDelete.test.tsx</files>
  <read_first>
    - Task 1's fork and delete rows, and the four asserted delete-guard properties
    - `WorkflowDeleteSheet.tsx`'s two invariant comments — they travelled BYTE-FOR-BYTE rather than being re-typed, because they are the written record of the guard's reasons
  </read_first>
  <action>
Apply ONLY the BUILT fork and delete rows. This is the most consequence-bearing pair in the phase, so
the constraint is stated as a grade rather than as a style:

  - **The delete sheet must remain DEMONSTRABLY HEAVIER than the fork dialog after this change.**
    Assert the grade difference directly — the delete sheet names its victim and states exact
    counts; the fork dialog does neither and offers no confirm step. If a re-presentation makes them
    look alike, the phase has spent the vocabulary the delete guard depends on.
  - **All four delete-guard properties stay true**: exact server counts fetched before the offer,
    victim named, amber cancel-first banner ONLY when a run is live, no optimistic vanish and no
    undo. Re-run the Task-1 assertions after the change.
  - **The fork stays confirm-free** (D-15). Do not add a step, a warning or a second click.
  - **The name-check states stay mutually distinguishable WITHOUT colour** — available, taken, still
    checking — and "still checking" must never read as either verdict. Prove it by stripping every
    `class` attribute off the rendered DOM and re-asserting mutual distinctness.
  - **The two invariant comments are not re-typed.** If they must move, move them byte-for-byte.
  - Words come from `libraryVocabulary.ts`; nothing is re-spelled.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/library/ForkNameDialog.test.tsx src/components/workflows/library/libraryFork.test.ts src/pages/__tests__/PublishedCardDelete.test.tsx src/components/workflows/library/librarySubtree.fences.test.ts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - All four delete-guard properties re-asserted and passing after the change.
    - The fork dialog has no confirm step — asserted, not assumed.
    - The three name-check states are mutually distinct with every `class` attribute stripped.
    - `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` reports `failed 0` and no per-file decrease.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library row → destructive action | the delete sheet is the last surface before irreversible data loss |
| launch inputs → run creation | the KB scope, staged template and kickoff text become a real, costed run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-10-01 | Elevation of privilege | `WorkflowDeleteSheet.tsx` | mitigate | The four guard properties are asserted before AND after the change; a presentation pass may not soften the heaviest action guard in the product. |
| T-199-10-02 | Spoofing | `RunModal.tsx` | mitigate | No fabricated estimate and no infrastructure identifier ships; the honest destination line remains the only claim about where the run goes. |
| T-199-10-03 | Tampering | `library/` subtree | mitigate | Any new module is registered in `LIBRARY_SUBTREE_PATHS` in the same commit, so it cannot be invisible to six fences that all stay green. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33, unmoved
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease
- `git diff --stat -- backend supabase` → empty
- `git diff --numstat -- frontend/src/components/workflows/library/WorkflowCard.tsx frontend/src/pages/WorkflowsPage.tsx` → empty
- ⚠ `docs/HOT-FILE-LEDGER.md` records `WorkflowCard.tsx` at a cell that `192.2-11` FLAGGED as stale (`8 / 3 / 818` against a re-derived `15 / 4 / 1262`). This plan does not modify that file, so it owes no ledger edit — but do not trust the cell if a decision depends on it.
</verification>

<success_criteria>
- Every element of sheet c6 carries a verdict, including the already-built card half (SC#1).
- No backend, migration, endpoint or wire model modified (SC#2).
- None of the four surfaces renders more at rest than before, proved against a pre-change inventory (SC#3).
- The mechanism is not printed to the user; `Target nodes` and `Estimated time` are refused (SC#4).
- Gates hold (SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-10-SUMMARY.md` when done, carrying the
reconciliation table, the two refused sheet elements with their reasons, the re-asserted delete-guard
properties, and every CANNOT-EXPRESS report.
</output>
