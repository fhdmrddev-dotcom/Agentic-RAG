---
phase: 199-the-component-map
plan: 08
type: execute
wave: 2
depends_on: ["199-03"]
files_modified:
  - frontend/src/components/workflows/WorkflowDoorSwitch.tsx
  - frontend/src/components/workflows/DoorHeaderStrip.tsx
  - frontend/src/components/workflows/DescribeKbPicker.tsx
  - frontend/src/components/workflows/DescribeTemplateRow.tsx
  - frontend/src/components/workflows/TemplateNameCheck.tsx
  - frontend/src/components/workflows/doorVocabulary.ts
  - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
  - frontend/src/components/workflows/DoorHeaderStrip.test.tsx
  - frontend/src/components/workflows/DescribeKbPicker.test.tsx
  - frontend/src/components/workflows/DescribeTemplateRow.test.tsx
  - frontend/src/components/workflows/TemplateNameCheck.test.tsx
  - frontend/src/components/workflows/doorVocabulary.test.ts
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "The two doors are tellable apart at rest, and the chosen door is legible without reading a paragraph"
    - "The describe box REFUSES an input too short to act on, out loud, rather than only disabling a button"
    - "The knowledge picker has three readings: none chosen, one chosen, and none available — the third says so"
    - "The doors render no MORE at rest than before; nothing is lost by picking the fast door"
  artifacts:
    - path: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      provides: "The two-door authoring entry, re-presented"
    - path: "frontend/src/components/workflows/doorVocabulary.ts"
      provides: "Every governed door string, including the refusal sentence"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      to: "frontend/src/components/workflows/doorVocabulary.ts"
      via: "every governed string reaches the JSX as an imported constant, never quoted"
      pattern: "doorVocabulary"
    - from: "frontend/src/components/workflows/DoorHeaderStrip.tsx"
      to: "the persistent chosen-door reading"
      via: "the strip that designs out the duplicated describe box"
      pattern: "DoorHeaderStrip"
---

<objective>
Re-present the two authoring doors and the describe surface in the adopted design language, against
sheet `c9-doors-describe`.

Purpose: DES-01 on the first screen an author meets. Sheet c9's finding is that the doors *"design
out the duplicated describe box via a persistent header strip, and show the box REFUSING an input
too short to act on rather than just disabling a button"* — refusal as information, not as absence.

Output: re-presented doors, a describe box that refuses out loud, and a knowledge picker whose third
reading says what it means.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c9-doors-describe.html
@.claude/skills/sketch-findings-agentic-rag/references/workflow-soul-and-two-doors.md
@.claude/skills/sketch-findings-agentic-rag/references/workflow-authoring.md
@.claude/skills/sketch-findings-agentic-rag/references/ai-seed-and-templates.md
@docs/HOT-FILE-LEDGER.md
</context>

<file_ownership_seam>
⚠ **THERE ARE TWO DESCRIBE BOXES IN THIS APPLICATION AND THIS PLAN OWNS ONLY ONE OF THEM.**
`WorkflowDoorSwitch.tsx` renders the loose door's describe box; `WorkflowBuilderPage.tsx:1917`
renders the pre-draft describe screen (`placeholder="Describe the goal in plain language…"`,
`data-testid="describe-hint"`). That duplication is precisely what sheet c9's header strip designs
out, and it is a **shipped** duplication, not one this phase introduces.

**`WorkflowBuilderPage.tsx` belongs to plan `199-09`, in the NEXT wave. This plan must not touch it.**
The seam is the vocabulary module: land the refusal sentence and any other new door word in
`doorVocabulary.ts` here, and `199-09` imports the SAME constant for the builder's pre-draft box in
wave 3. A second spelling of a governed string is how a governed string stops being governed.

If this plan finds it cannot land its change without editing `WorkflowBuilderPage.tsx`, it **STOPS
and reports** rather than editing a file another plan owns.
</file_ownership_seam>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports**.

⚠ **A refusal SENTENCE is presentation; a refusal RULE is not.** If the shipped describe box already
gates its CTA on an input-length or content predicate, saying so out loud is presentation and is
BUILT. If no predicate exists, inventing one is new behaviour — **report it, do not build it.**

**THE CANNOT-EXPRESS RULE (`SEED-155`).** If the shipped component cannot express the sheet, report
it — what the sheet asks for, what the component can do, and the gap. Never fake it.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the doors and reconcile them against sheet c9</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c9-doors-describe.html` — §1 the door switch (three states), §2 the header strip, §3 the describe box (empty / filled / refusal), §4 the knowledge picker (none / one / none-available), §5 the template row, §6 the name check (available / taken / checking)
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheets 4-9"
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` — ⚠ THE DOORS' WORDS ARE NAMED BY IDENTIFIER AND NEVER QUOTED (D-24(a)); every governed string lives in `doorVocabulary.ts`. Prose that spells a word out is a SECOND HOME for it.
    - `frontend/src/components/workflows/doorVocabulary.ts` and its suite — ⚠ the D-24(a) copy fence coerces EVERY export through a string `.replace`, so a FUNCTION export throws inside the needle builder. A new value that must interpolate belongs in a sibling `.ts` vocabulary module, NOT here.
    - `frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` — a shipped characterization pin. Invert, never delete.
    - `frontend/src/components/workflows/DoorHeaderStrip.tsx`, `DescribeKbPicker.tsx`, `DescribeTemplateRow.tsx`, `TemplateNameCheck.tsx`
  </read_first>
  <action>
Measure before changing anything. No source byte outside the named test file may move.

Inventory the RESTING atoms of the door surface as literal strings — the chooser at rest, each door
chosen, the header strip in both arms, the describe box empty and filled, the knowledge picker in
all three readings, the template row, and the name check in all three states — pinned PRESENT so a
later removal is proved by INVERSION, never by deletion.

Then reconcile against the sheet, with a verdict per element:
  - **The three door-switch states.** Record how the chosen door is signalled today and whether it
    is legible without reading a paragraph.
  - **The header strip.** ⚠ It SHIPS (Phase 193). Record ALREADY-SHIPPED; do not rebuild it. Check
    only whether its reading survives the re-presentation.
  - **The describe box's REFUSAL.** Determine whether a gating predicate already exists on the CTA.
    If it does, the refusal sentence is BUILT. If it does not, record CANNOT-EXPRESS with its
    reason — a new gating rule is behaviour, not presentation, and it is out of this phase's fence.
  - **The knowledge picker's three readings** — none chosen / one chosen / none available. The third
    must SAY there are none, not render an empty control. Record which arms exist today.
  - **The template row** and **the name check**. ⚠ `TemplateNameCheck.tsx` already ships all three
    states; verify rather than rebuild.
  - ⚠ Record the SECOND describe box (`WorkflowBuilderPage.tsx:1917`) as OWNED-BY-199-09, naming the
    vocabulary constant it will import, so the seam is explicit rather than assumed.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/WorkflowDoorSwitch.test.tsx src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/components/workflows/DoorHeaderStrip.test.tsx src/components/workflows/doorVocabulary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The inventory pin passes and names atoms as literals.
    - Every sheet element carries a verdict; the refusal row explicitly states whether a gating predicate already exists.
    - The 199-09 seam is recorded with the constant name.
    - `git status --short` shows exactly the one named test file. `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Re-present the door switch, the header strip and the describe box</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.tsx, frontend/src/components/workflows/DoorHeaderStrip.tsx, frontend/src/components/workflows/doorVocabulary.ts, frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx, frontend/src/components/workflows/DoorHeaderStrip.test.tsx, frontend/src/components/workflows/doorVocabulary.test.ts</files>
  <read_first>
    - Task 1's reconciliation table — the work list
    - `WorkflowDoorSwitch.tsx`'s RED LINES block — the library-card Run path is NOT wrapped by this fork; the shell adds NO API call and NO tier re-derivation; a STRICT workflow can never be silently downgraded here
  </read_first>
  <action>
Apply ONLY the BUILT rows. Hierarchy, typography, spacing, tone and removal.

Binding constraints:
  - **Every governed word is an imported constant.** Do not quote a door string in JSX, in a comment
    or in a test needle built by hand — the copy fence builds its needles FROM the module.
  - **A new interpolating value does NOT go in `doorVocabulary.ts`** — a function export throws
    inside the D-24(a) needle builder. Put it in a sibling `.ts` vocabulary module beside its
    surface, the shipped answer (`libraryVocabulary.ts`, `templateFirstVocabulary.ts`).
  - **Nothing is lost by picking the fast door** (D-05) — the soul preview and the visible switch
    strip stay.
  - **The refusal SAYS what is missing.** A disabled CTA with no sentence is the failure state the
    sheet is correcting. If Task 1 found no gating predicate, this row is a REPORT, not a build.
  - **Verify every colour token the sheet asks for RESOLVES** in `frontend/tailwind.config.js`.

Where the sheet removes an atom, invert the Task-1 assertion; delete none, and leave
`WorkflowDoorSwitch.baseline.test.tsx` byte-unchanged unless a deliberate, stated, proved
re-baseline is required — in which case say so, scope it, and prove it.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/WorkflowDoorSwitch.test.tsx src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/components/workflows/DoorHeaderStrip.test.tsx src/components/workflows/doorVocabulary.test.ts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - The door surface renders no MORE at rest than the Task-1 inventory recorded.
    - No governed string is quoted outside its vocabulary module — proved by the shipped copy fence passing.
    - `WorkflowDoorSwitch.baseline.test.tsx` is byte-unchanged, or its re-baseline is stated, scoped and proved.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33, ZERO under `components/workflows/`.
    - `git status --short` shows NO `frontend/src/pages/WorkflowBuilderPage.tsx`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Re-present the knowledge picker, the template row and the name check</name>
  <files>frontend/src/components/workflows/DescribeKbPicker.tsx, frontend/src/components/workflows/DescribeTemplateRow.tsx, frontend/src/components/workflows/TemplateNameCheck.tsx, frontend/src/components/workflows/DescribeKbPicker.test.tsx, frontend/src/components/workflows/DescribeTemplateRow.test.tsx, frontend/src/components/workflows/TemplateNameCheck.test.tsx</files>
  <read_first>
    - Task 1's picker / template / name-check rows
    - `frontend/src/components/workflows/DescribeTemplateRow.tsx` — its docblock names `ForkNameDialog.tsx` as the shape: fed entirely from props, no network import, no request
  </read_first>
  <action>
Apply ONLY the BUILT rows for the three describe-surface leaves.

Binding constraints:
  - **The picker's third reading SAYS there are none available.** An empty control and an
    unavailable source are two different facts; rendering them identically is the same class of lie
    as `Never run` on a workflow somebody else ran.
  - **The name check keeps all three states distinguishable WITHOUT colour** — available, taken,
    still checking — and "still checking" must never read as either verdict.
  - **These leaves stay fed entirely from props** — no network import, no request added.
  - **Verify every colour token the sheet asks for RESOLVES** in `frontend/tailwind.config.js`.
  - Words are imported from their vocabulary home; nothing is re-spelled here.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/DescribeKbPicker.test.tsx src/components/workflows/DescribeTemplateRow.test.tsx src/components/workflows/TemplateNameCheck.test.tsx src/components/workflows/templateNameBuckets.test.ts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - The three picker readings and the three name-check states are each mutually distinct with every `class` attribute stripped off the rendered DOM.
    - No network call is added to any of the three leaves.
    - `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` reports `failed 0` and no per-file decrease.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author's free text → describe box | the describe input is unvalidated free text that becomes a generation prompt |
| knowledge-base list → picker | folder names and counts arrive from an authenticated read and drive a grounding choice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-08-01 | Tampering | `WorkflowDoorSwitch.tsx` | mitigate | Describe text and soul-preview strings stay plain React text children (T-124-05 is a shipped clause); no `dangerouslySetInnerHTML`. |
| T-199-08-02 | Elevation of privilege | door shell | mitigate | The shell still adds NO API call and NO tier re-derivation; a STRICT workflow cannot be silently downgraded by a presentation change. Asserted by the shipped red-line tests. |
| T-199-08-03 | Spoofing | `DescribeKbPicker.tsx` | mitigate | "none available" and "none chosen" render distinct readings, so a user cannot believe they grounded a step against a source that was never offered. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33, unmoved
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease
- `git diff --stat -- backend supabase` → empty
- `git status --short` shows NO `frontend/src/pages/WorkflowBuilderPage.tsx` (owned by 199-09)
- ⚠ `docs/HOT-FILE-LEDGER.md` records `WorkflowDoorSwitch.tsx` at 12 / 8 / 522 — G-5 FIRES, honoured by construction (193 / 193.1). Re-derive before trusting the cell.
</verification>

<success_criteria>
- Every element of sheet c9 carries a verdict; none is silently dropped (SC#1).
- No backend, migration, endpoint or wire model modified (SC#2).
- The door surface renders no more at rest than before, proved against a pre-change inventory (SC#3).
- The mechanism is not printed to the user (SC#4).
- Gates hold (SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-08-SUMMARY.md` when done, carrying the
reconciliation table, the refusal-predicate finding, the named seam for 199-09, and every
CANNOT-EXPRESS report.
</output>
