---
phase: 212-the-catalog-and-its-doors
plan: 05
type: execute
wave: 4
depends_on: [212-03, 212-04]
files_modified:
  - frontend/src/lib/__tests__/apiBarrel.test.ts
  - frontend/src/components/settings/__tests__/connectionVerbFence.test.ts
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-05, CONN-06, CONN-07]

must_haves:
  truths:
    - "D-207-06 is guarded: all symbols in frontend/src/lib/api/connectors.ts are re-exported by lib/api.ts without drift"
    - "Zero verb filter chips or organizing verb axes remain in any settings component (connectionVerbFence)"
    - "G-5 hot-file ledger rows and detail sections are updated with git-derived counts for ConnectionsTab.tsx, ConnectionFormPanel.tsx, connectionsCopy.ts, connectionFormCopy.ts, SettingsPage.tsx, and ModelPillRow.tsx"
    - "All gates pass: tsc error count <= 34 (baseline), vitest count gate OK (116/116 pinned, 0 failing), backend rot set <= 68"
  artifacts:
    - path: "frontend/src/lib/__tests__/apiBarrel.test.ts"
      provides: "Automated test asserting D-207-06 barrel completeness for lib/api.ts"
    - path: "CLAUDE.md"
      provides: "Updated G-5 hot-file ledger rows with git-verified commit/phase/line counts"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "Updated G-5 ledger sections and history entries"
  key_links:
    - from: "frontend/src/lib/__tests__/apiBarrel.test.ts"
      to: "frontend/src/lib/api.ts"
      via: "barrel inspection"
      pattern: "connectors"
---

<objective>
Execute cross-plan integration checks, barrel completeness tests (`D-207-06`), G-5 hot-file ledger audits, and the full multi-gate verification gauntlet.

Purpose: Guarantee that all Phase 212 deliverables join seamlessly, no symbols drift from the `lib/api` barrel, hot-file ledger records are accurately updated, and all quality gates are 100% green.

Output: `apiBarrel.test.ts`, updated `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md`, and clean gate verification outputs.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-the-catalog-and-its-doors/212-CONTEXT.md
@.planning/phases/212-the-catalog-and-its-doors/212-RESEARCH.md
@.planning/phases/212-the-catalog-and-its-doors/212-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" id="212-05-01">
<name>Add D-207-06 barrel completeness test and verify connection verb fences</name>
<files>
  - frontend/src/lib/__tests__/apiBarrel.test.ts
  - frontend/src/components/settings/__tests__/connectionVerbFence.test.ts
</files>
<action>
1. Create `frontend/src/lib/__tests__/apiBarrel.test.ts`:
   - Inspect all exported symbols from `frontend/src/lib/api/connectors.ts`.
   - Assert every exported function and type is re-exported from `frontend/src/lib/api.ts`.
   - This provides the permanent executable guard for `D-207-06`.
2. Run `connectionVerbFence.test.ts` and verify all verb fence assertions pass.
</action>
<verify>
npx vitest run src/lib/__tests__/apiBarrel.test.ts src/components/settings/__tests__/connectionVerbFence.test.ts
</verify>
<done>
`apiBarrel.test.ts` and `connectionVerbFence.test.ts` pass with 0 failures.
</done>
</task>

<task type="auto" id="212-05-02">
<name>Update G-5 hot-file ledger in CLAUDE.md and docs/HOT-FILE-LEDGER.md</name>
<files>
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
</files>
<action>
1. Re-derive G-5 metrics for files touched/affected by Phase 212:
   - `frontend/src/components/settings/ConnectionsTab.tsx`
   - `frontend/src/components/settings/ConnectionFormPanel.tsx`
   - `frontend/src/components/settings/connectionsCopy.ts`
   - `frontend/src/components/settings/connectionFormCopy.ts`
   - `frontend/src/pages/SettingsPage.tsx`
   - `frontend/src/components/settings/ModelPillRow.tsx`
2. Update the ledger rows in `CLAUDE.md` and detail sections in `docs/HOT-FILE-LEDGER.md` with git-verified counts and Phase 212 disposition summaries.
</action>
<verify>
node scripts/check-claude-md-size.cjs
</verify>
<done>
G-5 hot-file ledger rows and detail sections are accurately updated and CLAUDE.md size remains well within bounds.
</done>
</task>

<task type="auto" id="212-05-03">
<name>Run full multi-gate verification battery</name>
<files>
  - package.json
</files>
<action>
1. Run frontend typecheck:
   `npx tsc --noEmit -p tsconfig.app.json` (from `frontend/`) → verify <= 34 baseline errors.
2. Run frontend Vitest Count Gate:
   `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → verify OK, 116/116 pinned files, 0 failed.
3. Run backend unit tests:
   `./venv/Scripts/python.exe -m pytest tests/unit -q` → verify rot set <= 68 baseline and 0 connector failures.
</action>
<verify>
GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs
</verify>
<done>
All gates pass cleanly across frontend and backend suites.
</done>
</task>

</tasks>
