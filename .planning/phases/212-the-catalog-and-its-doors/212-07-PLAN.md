---
phase: 212-the-catalog-and-its-doors
plan: 212-07
wave: 6
gap_closure: true
gap_closure_round: 2
unmet_criterion: "SC#2: MCP and uncheckable working connections were conflated into Not connected"
files_modified:
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/connectionsCopy.ts
  - frontend/src/components/settings/ConnectionFormPanel.tsx
  - frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
  - frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx
---

# Plan 212-07: Gap Closure Round 2 — Filter Partition Honesty, Accurate Count Noun, Description Axis & Panel Layout

## Goal
Resolve the 3 browser-driven findings from reviewer check and polish drawer layout:
1. **SC#2 Filter Partition:** "Not connected" means zero configured connections (`item.kind === "catalog"`), so working connections with no credential checks (e.g. MCP / DeepWiki) stay under "Connected".
2. **Honest Count Noun:** Change `{total} connections` to `{total} services` in `connectionsCountLabel` so the count never over-claims configured connections against unconfigured catalog services.
3. **Section Description:** Rewrite `CONNECTIONS_SECTION_DESCRIPTION` to `"Services you can connect, and what they let the agent do for you. A step sends nothing until a person approves it in the run."`
4. **Drawer Layout Flow:** Place action controls (`Check credential`, `Disable`, `Delete`, `Cancel`, `Save changes`) and destination preview sequentially inside `connection-form-body` directly after form fields.

## Tasks

### Task 1: Update Copy and Filter Predicates
- Update `connectionsCountLabel` and `CONNECTIONS_SECTION_DESCRIPTION` in `connectionsCopy.ts`.
- Update state filtering in `ConnectionsTab.tsx` so "Connected" filters `item.kind === "configured"` and "Not connected" filters `item.kind === "catalog"`.

### Task 2: Update ConnectionFormPanel Layout
- Flow destination footer, actions, and drawer buttons sequentially inside `connection-form-body`.

### Task 3: Test Verification
- Run all 8 settings test suites (318/318 passing).
- Run typecheck (`tsc -p tsconfig.app.json`).
