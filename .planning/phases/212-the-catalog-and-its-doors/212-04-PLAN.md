---
phase: 212-the-catalog-and-its-doors
plan: 04
type: execute
wave: 3
depends_on: [212-02]
files_modified:
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/connectionsCopy.ts
  - frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-05]

must_haves:
  truths:
    - "ConnectionsTab renders a top Popular Services card row with quick Connect actions alongside the searchable service directory (CAT-01, CAT-02)"
    - "Filter chips are strictly state-based (All / Connected / Not connected); zero verb/capability filter chips appear anywhere (CAT-03)"
    - "Service cards aggregate multiple configured instances under the service identity, displaying active status badges and Add another triggers"
    - "When live_connectors is off (cloud cold default), the tab displays an honest platform policy banner with a direct link to the Control Room switch for org admins (CAT-05, BUG-260810-01)"
  artifacts:
    - path: "frontend/src/components/settings/ConnectionsTab.tsx"
      provides: "The redesigned Connections catalog view with Popular row, state filter chips, search, and cloud policy guidance"
      contains: "POPULAR_SERVICES"
    - path: "frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx"
      provides: "Unit tests asserting catalog layout, search, state filtering, multi-instance aggregation, and cloud parity"
  key_links:
    - from: "frontend/src/components/settings/ConnectionsTab.tsx"
      to: "frontend/src/components/settings/servicesCatalog.ts"
      via: "POPULAR_SERVICES, getServiceCatalogEntry"
      pattern: "POPULAR_SERVICES"
    - from: "frontend/src/components/settings/ConnectionsTab.tsx"
      to: "frontend/src/components/settings/connectionMark.tsx"
      via: "ConnectionMarkGlyph"
      pattern: "ConnectionMarkGlyph"
---

<objective>
Implement the unified Services Catalog view, Popular card row, search/filter controls, and cloud parity guidance in ConnectionsTab.

Purpose: Provide a modern, searchable catalog interface (`CAT-01`, `CAT-02`), enforce state-only filtering (`CAT-03`), and resolve the cloud "missing add button" issue (`CAT-05`, `BUG-260810-01`) with clear, actionable navigation.

Output: Updated `ConnectionsTab.tsx`, `connectionsCopy.ts`, and updated `ConnectionsTab.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-the-catalog-and-its-doors/212-CONTEXT.md
@.planning/phases/212-the-catalog-and-its-doors/212-RESEARCH.md
@.planning/phases/212-the-catalog-and-its-doors/212-UI-SPEC.md
@.planning/phases/212-the-catalog-and-its-doors/212-VALIDATION.md
@CLAUDE.md

<interfaces>
`frontend/src/components/settings/servicesCatalog.ts`:
- `POPULAR_SERVICES: CatalogServiceEntry[]`
- `CATALOG_SERVICES: CatalogServiceEntry[]`
- `getServiceCatalogEntry(serviceId: string | null | undefined): CatalogServiceEntry`
</interfaces>
</context>

<tasks>

<task type="auto" id="212-04-01">
<name>Render top Popular Services row and service card directory in ConnectionsTab</name>
<files>
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/connectionsCopy.ts
</files>
<action>
1. In `frontend/src/components/settings/ConnectionsTab.tsx`:
   - Import `POPULAR_SERVICES`, `CATALOG_SERVICES`, `getServiceCatalogEntry` from `servicesCatalog.ts`.
   - Render the top "Popular Services" card grid displaying Slack, Jira, SMTP, GitHub, Google, Notion, and "+ Custom MCP" card with brand marks, taglines, and quick Connect buttons.
   - Aggregate configured connections by `service_id` (computing active count per service).
   - Render the main searchable directory list below the popular section, showing each service's official mark, name, tagline, configured instances count badge (e.g. "1 Connected", "2 Connected"), available tools badge, and action trigger.
   - Ensure clicking a service card opens the connection panel with pre-selected `service_id`.
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx
</verify>
<done>
ConnectionsTab renders the Popular services card row and aggregated services directory.
</done>
</task>

<task type="auto" id="212-04-02">
<name>Implement state-based filtering and cloud live_connectors guidance banner</name>
<files>
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/connectionsCopy.ts
</files>
<action>
1. In `ConnectionsTab.tsx`:
   - Wire state filter chips: `All` | `Connected` | `Not connected` (`filterState`).
   - Filter directory rows:
     - `All`: show all catalog services and custom MCP connections.
     - `Connected`: show only services with >= 1 configured connection.
     - `Not connected`: show catalog services with 0 configured connections.
   - Assert zero verb/capability filter chips are present in DOM.
   - Update platform policy banner for when `liveConnectorsOn` is false:
     - Explain clearly that live external sending is disabled platform-wide.
     - If `isOrgAdmin` is true, render direct action button/link to Control Room (`/admin` → Feature Visibility) to flip `live_connectors`.
     - Allow read-only browsing of catalog and existing connections.
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx
</verify>
<done>
State-based filtering and cloud policy banner with Control Room link are active and tested.
</done>
</task>

<task type="auto" id="212-04-03">
<name>Update ConnectionsTab unit test suite</name>
<files>
  - frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
</files>
<action>
1. Update `ConnectionsTab.test.tsx`:
   - Test rendering of Popular Services cards row.
   - Test search filtering by service name and tagline.
   - Test state filter chips (`All`, `Connected`, `Not connected`) and assert absence of verb chips.
   - Test cloud parity: when `liveConnectorsOn` is false, assert policy banner renders with Control Room link for admins.
   - Verify all tests pass cleanly.
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx
</verify>
<done>
All ConnectionsTab unit tests pass with 0 failures.
</done>
</task>

</tasks>
