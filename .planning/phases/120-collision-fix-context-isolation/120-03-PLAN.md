---
phase: 120-collision-fix-context-isolation
plan: 03
type: execute
wave: 2
depends_on: ["120-02"]
files_modified:
  - supabase/full-schema.sql
  - backend/tests/integration/test_120_migration.py
autonomous: false
requirements: [CTX-01]
user_setup: []

must_haves:
  truths:
    - "Migration 076 is applied to the live local DB so messages.origin exists with NOT NULL DEFAULT 'deep' and CHECK (origin IN ('deep','harness'))."
    - "Every pre-existing messages row has origin='deep' post-apply — zero NULLs (the load-bearing NULL-trap guard)."
    - "supabase/full-schema.sql is regenerated from the live DB (never hand-edited) and reflects the new column."
    - "The live-DB integration test confirms the CHECK accepts 'deep'/'harness' and rejects any other value."
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "Regenerated single-file deploy artifact reflecting messages.origin (via scripts/regenerate-full-schema.sh, no --reset)"
      contains: "origin"
    - path: "backend/tests/integration/test_120_migration.py"
      provides: "Live-DB integration test: zero NULL origin, CHECK behavior (accepts deep/harness, rejects other)"
      contains: "origin IS NULL"
  key_links:
    - from: "supabase/migrations/076_messages_origin.sql"
      to: "live local DB (:54322 messages table)"
      via: "manual paste into Supabase SQL editor (CLAUDE.md mandate — never db push/reset)"
      pattern: "ALTER TABLE public.messages"
    - from: "live DB schema"
      to: "supabase/full-schema.sql"
      via: "bash scripts/regenerate-full-schema.sh (no --reset)"
      pattern: "regenerate-full-schema"
---

<objective>
[BLOCKING] Apply migration 076 (authored in Plan 02) to the live local DB, then
regenerate `full-schema.sql`, then run the live-DB integration test that proves
the column exists with the load-bearing semantics. This step is interactive
(manual SQL-editor paste per CLAUDE.md — NEVER `supabase db push`/`db reset`),
so the plan is `autonomous: false`.

Purpose: CTX-01 cannot pass verification without the column on the live DB —
build/type checks pass without it (types come from config, not the live DB),
creating a false-positive verification state, and any test that queries/filters
`origin` fails until the column exists.
Output: applied migration on the live DB + regenerated `full-schema.sql` + a green
live-DB integration test (SC#3 migration semantics).

DEPENDS ON Plan 02 (which AUTHORS `076_messages_origin.sql`). This plan does NOT
re-author the SQL — it applies the existing file and verifies.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/120-collision-fix-context-isolation/120-RESEARCH.md
@.planning/phases/120-collision-fix-context-isolation/120-PATTERNS.md
@supabase/SETUP.md

<interfaces>
<!-- Apply + regen contract (CLAUDE.md / migration apply rule). -->
Apply: paste the FULL contents of supabase/migrations/076_messages_origin.sql into the
  Supabase SQL editor against the live local DB (:54322). NEVER `supabase db push` / `db reset`
  (preserves dev data). (psycopg2-direct to :54322 is the accepted alternative per the
  100/099/101.1/102/110/111/114 precedent.)
Regen: bash scripts/regenerate-full-schema.sh   (NO --reset — dumps live schema, no wipe).
  Never hand-edit full-schema.sql. Commit migration + regenerated full-schema.sql together.
Post-apply invariant (the load-bearing guard): SELECT count(*) FROM messages WHERE origin IS NULL  → 0
CHECK behavior: INSERT origin='deep' and 'harness' succeed; any other value is rejected by messages_origin_check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the live-DB integration test (runs after apply)</name>
  <files>backend/tests/integration/test_120_migration.py</files>
  <read_first>
    - backend/tests/integration/conftest.py (live-DB connection fixture pattern — how integration tests reach :54322)
    - backend/tests/integration/_run_helpers.py (existing integration helpers, if a DB cursor/pool helper exists)
    - .planning/phases/120-collision-fix-context-isolation/120-RESEARCH.md (Validation Architecture SC#3 row: zero NULL origin, CHECK accepts deep/harness rejects other)
    - supabase/migrations/076_messages_origin.sql (the migration Plan 02 authored — the column/constraint names to assert against)
  </read_first>
  <action>
    Create `backend/tests/integration/test_120_migration.py` using the existing integration-test DB connection pattern (read conftest.py for the live :54322 fixture). Three checks: (1) `SELECT count(*) FROM messages WHERE origin IS NULL` returns 0 (the load-bearing NULL-trap guard — Pitfall 1); (2) the column exists with `NOT NULL` and a default of `'deep'` (query `information_schema.columns` for `column_default` / `is_nullable`); (3) the CHECK rejects an out-of-domain value — attempt an INSERT (or a constraint-probe) with `origin='other'` inside a rolled-back transaction and assert it raises a check-constraint violation, while `'deep'` and `'harness'` are accepted. Use a transaction that ROLLS BACK so the test never mutates dev data. If the column does not yet exist (migration not applied), the test should FAIL clearly (it is the apply gate).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import ast; ast.parse(open('tests/integration/test_120_migration.py').read()); print('parses OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/integration/test_120_migration.py` exists and parses.
    - It asserts zero NULL `origin`, the column's NOT NULL + DEFAULT 'deep', and CHECK accept/reject behavior.
    - All DB writes occur inside a rolled-back transaction (no dev-data mutation).
  </acceptance_criteria>
  <done>The live-DB integration test exists and is ready to run after the migration is applied; it gates on the column existing with the correct semantics.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 076 to the live local DB (manual SQL-editor paste) + regenerate full-schema.sql</name>
  <files>supabase/migrations/076_messages_origin.sql, supabase/full-schema.sql</files>
  <action>
    [HUMAN ACTION — cannot be automated per CLAUDE.md.] Paste the full contents of
    `supabase/migrations/076_messages_origin.sql` into the Supabase SQL editor against the live
    local DB (:54322) and run it (NEVER `supabase db push` / `db reset` — preserves dev data;
    psycopg2-direct to :54322 is the accepted alternative). Then run
    `bash scripts/regenerate-full-schema.sh` (NO `--reset`) to rebuild `supabase/full-schema.sql`
    from the live DB. Do NOT hand-edit `full-schema.sql`. Full operator steps in the checkpoint
    fields below.
  </action>
  <verify>
    <human-check>SELECT count(*) FROM messages WHERE origin IS NULL returns 0; full-schema.sql contains the origin column (regenerated, not hand-edited).</human-check>
  </verify>
  <done>Migration 076 is on the live DB (column exists with NOT NULL DEFAULT 'deep' + CHECK, zero NULL origin) and full-schema.sql is regenerated.</done>
  <what-built>
    Plan 02 authored `supabase/migrations/076_messages_origin.sql` (ADD COLUMN origin NOT NULL
    DEFAULT 'deep' + CHECK). It is NOT yet on the live DB. Build/type checks pass without it
    (false-positive), and the CTX-01 integration test + live UAT fail until the column exists.
    This step cannot be automated per CLAUDE.md — the migration MUST be pasted into the Supabase
    SQL editor (NEVER `supabase db push` / `db reset`, which would wipe dev data).
  </what-built>
  <how-to-verify>
    1. Open the Supabase SQL editor against the live LOCAL DB (:54322).
    2. Paste the FULL contents of `supabase/migrations/076_messages_origin.sql` and run it.
       (psycopg2-direct to :54322 is the accepted alternative per the 100/099/110/111/114 precedent.)
    3. Confirm it succeeds (the `IF NOT EXISTS` / `DROP CONSTRAINT IF EXISTS` guards make re-runs safe).
    4. Run `bash scripts/regenerate-full-schema.sh` (NO `--reset`) to rebuild `supabase/full-schema.sql`
       from the live DB. Do NOT hand-edit `full-schema.sql`.
    5. Confirm: `SELECT count(*) FROM messages WHERE origin IS NULL;` returns 0.
  </how-to-verify>
  <resume-signal>Type "applied" once 076 is on the live DB and full-schema.sql is regenerated (or describe any error).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the live-DB integration test + the full phase test set + regression sweep</name>
  <files>backend/tests/integration/test_120_migration.py</files>
  <read_first>
    - backend/tests/integration/test_120_migration.py (Task 1's test)
    - backend/tests/unit/test_120_collision_regression.py (Plan 01)
    - backend/tests/test_120_origin_filter.py (Plan 02)
    - .planning/phases/120-collision-fix-context-isolation/120-RESEARCH.md (Sampling Rate — phase gate: full suite green + migration applied + full-schema regenerated)
  </read_first>
  <action>
    With migration 076 now on the live DB, run the CTX-01 live-DB integration test, the full Phase 120 test set (Plan 01 + Plan 02 + this integration test), and a regression sweep of the shared machinery (harvest/dedup + dual-mode + shared persist helper) to confirm no regression. If any test is red, stop and report — do not paper over a real failure.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_120_migration.py tests/unit/test_120_collision_regression.py tests/test_120_origin_filter.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/test_120_migration.py` exits 0 (zero NULL origin; CHECK accept/reject confirmed on the live DB).
    - The combined Phase 120 test set (Plan 01 + Plan 02 + integration) exits 0.
    - Regression sweep `venv/Scripts/python -m pytest tests/unit/test_075_4_dedup_supersedes.py tests/unit/test_sandbox_service.py -q` plus a dual-mode/093 sweep stays green.
    - `supabase/full-schema.sql` reflects the new `origin` column (regenerated, not hand-edited).
  </acceptance_criteria>
  <done>Migration 076 is applied and verified on the live DB; the full Phase 120 test set is green; full-schema.sql is regenerated; no regression in the shared harvest/persist machinery. The phase is ready for /gsd:verify-work + the SC#10 4-axis live UAT (VALIDATION.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → live local DB | The DDL crosses into the persistent dev DB; the apply method must preserve dev data (no reset). |
| Schema dump → full-schema.sql artifact | The regenerated artifact is the greenfield deploy source of truth; a hand-edit would diverge from the live schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-07 | Information Disclosure (regression) | Legacy NULL `origin` rows silently dropped from Deep replay | mitigate | The migration's `NOT NULL DEFAULT 'deep'` fills existing rows; the integration test asserts `count(*) WHERE origin IS NULL == 0` on the live DB (the gate that proves the NULL-trap is closed). |
| T-120-09 | Denial of Service / Data Loss | `supabase db push`/`db reset` wiping dev data during apply | mitigate | Apply by SQL-editor paste ONLY (CLAUDE.md mandate); the blocking human-action task spells out NEVER db push/reset; the migration is idempotent (IF NOT EXISTS / DROP CONSTRAINT IF EXISTS). |
| T-120-08 | Input Validation | Out-of-domain `origin` value persisted | mitigate | Live-DB CHECK `IN ('deep','harness')` verified by the integration test (rejects 'other' in a rolled-back txn). |
| T-120-SC | Tampering | npm/pip/cargo installs | accept | No package installs (no new deps — RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `pytest tests/integration/test_120_migration.py -x` exits 0 (live DB).
- The full Phase 120 test set (01 + 02 + integration) exits 0.
- `supabase/full-schema.sql` contains the `origin` column and was produced by `regenerate-full-schema.sh` (not hand-edited).
- `SELECT count(*) FROM messages WHERE origin IS NULL` returns 0 on the live DB.
</verification>

<success_criteria>
- SC#3 (CTX-01 migration semantics): `messages.origin` exists on the live DB with NOT NULL DEFAULT 'deep' + CHECK; zero NULL rows; CHECK accepts deep/harness, rejects other.
- `full-schema.sql` regenerated from the live DB (never hand-edited); migration + artifact committed together.
- The whole Phase 120 test set green; no regression in shared harvest/persist machinery.
- The phase is ready for /gsd:verify-work and the SC#10 4-axis live UAT.
</success_criteria>

<output>
Create `.planning/phases/120-collision-fix-context-isolation/120-03-SUMMARY.md` when done.
</output>
