---
phase: 224-what-the-agent-is-doing-reads-like-a-sentence
plan: 05
type: execute
wave: 3
depends_on:
  - "01"
  - "03"
  - "04"
files_modified:
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/CitationList.tsx
  - frontend/src/components/chat/RunCard.tsx
  - frontend/src/components/panel/TodosSection.tsx
  - frontend/src/components/chat/__tests__/CitationList.test.tsx
  - frontend/src/components/panel/__tests__/TodosSection.test.tsx
autonomous: true
requirements:
  - BUG-260902-07
  - SEED-240
user_setup: []

must_haves:
  truths:
    - "References in MessageItem.tsx are folded by default (defaultOpen={false}, reversing Phase 153 D-06/D-07)"
    - "CitationList and RunCard Thinking block share the identical subtle bordered pill fold trigger affordance"
    - "RunCard Thinking block default remains folded (useState(false) unchanged)"
    - "TodoRow in TodosSection.tsx wraps naturally at the 308px panel floor without premature truncation from the badge"
    - "TypeScript tsc baseline holds at 66 errors (0 new errors)"
    - "vitest count gate OK with 0 failing"
  artifacts:
    - frontend/src/components/chat/CitationList.tsx
    - frontend/src/components/panel/TodosSection.tsx
  key_links:
    - "MessageItem sets defaultOpen={false} on CitationList"
    - "CitationList and RunCard share fold trigger styling"
---

<objective>
Polish fold affordances, resolve panel todo label wrapping, and execute full mechanical verification:
1. Fix `BUG-260902-07`: References fold by default in `MessageItem.tsx`.
2. Implement the shared subtle bordered pill fold trigger across `CitationList.tsx` and `RunCard.tsx` (Thinking block), keeping Thinking default folded.
3. In `TodosSection.tsx`, restructure `TodoRow` layout so long sentences wrap gracefully across full width at the 308px panel floor rather than premature truncation dictated by the badge.
4. Execute all gates: `tsc -p tsconfig.app.json`, `vitest-count-gate.cjs`, and backend unit suites.

Output: Polished affordances, responsive todo rows, and green baseline gates.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-CONTEXT.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-PREFLIGHT.md
@frontend/src/components/chat/MessageItem.tsx
@frontend/src/components/chat/CitationList.tsx
@frontend/src/components/chat/RunCard.tsx
@frontend/src/components/panel/TodosSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold References by default and implement shared fold affordance (BUG-260902-07)</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/components/chat/CitationList.tsx, frontend/src/components/chat/RunCard.tsx</files>
  <read_first>frontend/src/components/chat/MessageItem.tsx, frontend/src/components/chat/CitationList.tsx, frontend/src/components/chat/RunCard.tsx</read_first>
  <action>
    1. In `frontend/src/components/chat/MessageItem.tsx` around line 619:
       Change `defaultOpen={hasInRangeMarker(...)}` to `defaultOpen={false}`.
       Add a comment noting the conscious reversal of Phase 153's D-06/D-07 contract per operator directive in Phase 224 (`BUG-260902-07`).
    2. In `frontend/src/components/chat/CitationList.tsx`:
       Update the CollapsibleTrigger styling to use the subtle bordered badge/pill:
       `inline-flex items-center gap-1.5 px-2 py-0.5 rounded border border-border/50 bg-muted/30 hover:bg-muted/60 text-xs font-medium text-muted-foreground transition-colors cursor-pointer`
    3. In `frontend/src/components/chat/RunCard.tsx`:
       Update the Thinking block CollapsibleTrigger around line 481 to use the same bordered badge/pill styling.
       Keep `const [expanded, setExpanded] = useState(false)` untouched (Thinking block stays folded by default).
  </action>
  <verify>
    Run `npx vitest run src/components/chat/__tests__/CitationList.test.tsx` from `frontend/`.
  </verify>
  <acceptance_criteria>
    - References in `MessageItem.tsx` default to closed.
    - Both `CitationList` and `RunCard` Thinking fold share the bordered pill trigger affordance.
    - Thinking block default remains folded.
  </acceptance_criteria>
  <done>BUG-260902-07 resolved with shared fold affordance.</done>
</task>

<task type="auto">
  <name>Task 2: Fix TodoRow wrapping at the 308px panel floor</name>
  <files>frontend/src/components/panel/TodosSection.tsx</files>
  <read_first>frontend/src/components/panel/TodosSection.tsx</read_first>
  <action>
    In `frontend/src/components/panel/TodosSection.tsx` inside `TodoRow`:
    1. Restructure the row layout to prevent rigid horizontal badge crowding:
       ```tsx
       <li className="flex items-start gap-2 px-3 py-1.5 text-[0.82rem] leading-relaxed">
         <span className="mt-0.5 flex-none">
           <StatusIndicator status={status} />
         </span>
         <div className="min-w-0 flex-1 flex flex-col gap-0.5">
           <span
             className={cn(
               "text-foreground/90 break-words",
               status === "completed" && "text-panel-muted-foreground line-through",
             )}
           >
             {todo.content}
           </span>
           <span
             className={cn(
               "font-mono text-[0.62rem] uppercase tracking-wider",
               STATUS_TEXT_COLOR[status],
             )}
           >
             {STATUS_LABEL[status]}
           </span>
         </div>
       </li>
       ```
    2. This allows `todo.content` to use 100% of the horizontal space beside the icon, eliminating premature wrapping caused by trailing badges.
  </action>
  <verify>
    Run `npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx` from `frontend/`.
  </verify>
  <acceptance_criteria>
    - `TodoRow` provides full width to `todo.content`.
    - Panel tests pass.
  </acceptance_criteria>
  <done>TodoRow wrapping layout corrected for 308px floor.</done>
</task>

<task type="auto">
  <name>Task 3: Adopt touched suites into vitest-count-gate.cjs and execute all gates</name>
  <files>scripts/vitest-count-gate.cjs, frontend/src/types/index.ts</files>
  <read_first>scripts/vitest-count-gate.cjs, .planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-PREFLIGHT.md</read_first>
  <action>
    1. In `scripts/vitest-count-gate.cjs`:
       Adopt the panel/seam test suite into `TARGETS` and `BASELINE`:
       - Add `"src/components/panel/__tests__/Seam.test.tsx"` to `TARGETS`.
       - Add `"Seam.test.tsx": 8` to `BASELINE` (read from the actual test run after Plan 224-01's arm pruning).
       - This permanently guards the chat/panel seam surface in CI, preventing silent regressions.
    2. Run TypeScript typecheck:
       `npx tsc -p tsconfig.app.json` inside `frontend/`.
       Verify error count does not exceed baseline 66 (0 new errors).
    3. Run Vitest count gate:
       `$env:GSD_VITEST_MAX_WORKERS="2"; node scripts/vitest-count-gate.cjs` from repo root.
       Verify all pinned files present (189/189 with Seam.test.tsx adopted) and 0 failing.
    4. Run Phase 224 backend tests:
       `pytest backend/tests/test_224_approval_deadline.py -v`.
  </action>
  <verify>
    Confirm all three gates output passing status.
  </verify>
  <acceptance_criteria>
    - `Seam.test.tsx` is pinned in `vitest-count-gate.cjs`.
    - `tsc` reports 66 errors (baseline unchanged).
    - `vitest-count-gate.cjs` reports OK with 0 failing.
    - Backend unit tests pass.
  </acceptance_criteria>
  <done>Touched suites adopted and mechanical verification gates passed.</done>
</task>

</tasks>

<threat_model>
- Threat: Altering TodoRow DOM could break test assertions expecting status label as a direct sibling of `<li>`.
- Mitigation: Status text remains within `<li>` in accessible tree with same test IDs and text content.
</threat_model>
