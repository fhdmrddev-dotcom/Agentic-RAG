---
phase: 224-what-the-agent-is-doing-reads-like-a-sentence
plan: 03
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api/threads.ts
  - frontend/src/components/chat/ChatToolApprovalCard.tsx
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/chat/__tests__/ToolApproval.test.tsx
autonomous: true
requirements:
  - BUG-260902-04
user_setup: []

must_haves:
  truths:
    - "ToolApprovalRequest has optional expiresAt string property"
    - "threads.ts maps parsed.expires_at to expiresAt on tool_approval_required events"
    - "ChatToolApprovalCard displays a calm ticking countdown when expiresAt is provided"
    - "When remaining time <= 0, card displays 'Approval timed out' and disables action buttons"
    - "Pending approval card docks above MessageInput in ChatArea.tsx, always reachable on arrival"
    - "Upon decision or expiration, the card resolves into the transcript as a historic settled card"
  artifacts:
    - frontend/src/components/chat/ChatToolApprovalCard.tsx
    - frontend/src/components/chat/ChatArea.tsx
    - frontend/src/components/chat/__tests__/ToolApproval.test.tsx
  key_links:
    - "ChatToolApprovalCard renders countdown from expiresAt"
    - "ChatArea docks pending card above MessageInput"
---

<objective>
Deliver the reachable approval card and ticking clock on the frontend (Sketch 226 Winner A):
1. Update `frontend/src/types/index.ts` and `frontend/src/lib/api/threads.ts` to carry `expiresAt`.
2. Add ticking countdown to `ChatToolApprovalCard.tsx` derived from `expiresAt`, with timeout state handling.
3. In `ChatArea.tsx`, dock the pending approval card directly above `MessageInput` so it is always on screen while waiting for the operator's decision.
4. Resolve the card back into the transcript once decided or timed out.
5. Add comprehensive unit tests in `ToolApproval.test.tsx`.

Output: Docked approval card with countdown in `ChatArea.tsx` and passing tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-CONTEXT.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-PREFLIGHT.md
@.planning/sketches/226-the-card-you-can-reach-and-its-clock/index.html
@frontend/src/components/chat/ChatToolApprovalCard.tsx
@frontend/src/components/chat/ChatArea.tsx
@frontend/src/lib/api/threads.ts
@frontend/src/components/chat/__tests__/ToolApproval.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire expiresAt into wire types and ChatToolApprovalCard countdown</name>
  <files>frontend/src/types/index.ts, frontend/src/lib/api/threads.ts, frontend/src/components/chat/ChatToolApprovalCard.tsx</files>
  <read_first>frontend/src/lib/api/threads.ts, frontend/src/components/chat/ChatToolApprovalCard.tsx</read_first>
  <action>
    1. In `frontend/src/components/chat/ChatToolApprovalCard.tsx` (and `frontend/src/types/index.ts` if declared there):
       Add `expiresAt?: string` to `ToolApprovalRequest`.
    2. In `frontend/src/lib/api/threads.ts`:
       In `onToolApprovalRequired` type definition and in the SSE event dispatch block (line ~733), add:
       `expiresAt: (parsed.expires_at ?? parsed.expiresAt) as string | undefined`
    3. In `frontend/src/components/chat/ChatToolApprovalCard.tsx`:
       - Add countdown state:
         ```typescript
         const [secondsRemaining, setSecondsRemaining] = useState<number | null>(() => {
           if (!approval.expiresAt) return null
           const diff = Math.max(0, Math.round((Date.parse(approval.expiresAt) - Date.now()) / 1000))
           return diff
         })
         ```
       - Add `useEffect` ticking every 1s when `approval.expiresAt` is present.
       - Format countdown label:
         When `secondsRemaining > 60`: `${Math.floor(secondsRemaining / 60)}m ${secondsRemaining % 60}s remaining`
         When `0 < secondsRemaining <= 60`: `${secondsRemaining}s remaining`
         When `secondsRemaining === 0`: `Approval timed out`
       - When timed out (`secondsRemaining === 0`), disable the Allow, Always, and Reject buttons with calm muted appearance.
       - Render the clock indicator beside the service name/tool header.
  </action>
  <verify>
    Run `npx vitest run src/components/chat/__tests__/ToolApproval.test.tsx` from `frontend/`.
  </verify>
  <acceptance_criteria>
    - `ToolApprovalRequest` contains `expiresAt`.
    - `ChatToolApprovalCard` renders countdown string when `expiresAt` is passed.
    - Buttons are disabled when `secondsRemaining === 0`.
  </acceptance_criteria>
  <done>Countdown clock wired to wire expiresAt in ChatToolApprovalCard.</done>
</task>

<task type="auto">
  <name>Task 2: Dock pending approval card above MessageInput in ChatArea.tsx</name>
  <files>frontend/src/components/chat/ChatArea.tsx, frontend/src/components/chat/MessageItem.tsx</files>
  <read_first>frontend/src/components/chat/ChatArea.tsx, frontend/src/components/chat/MessageItem.tsx</read_first>
  <action>
    1. In `frontend/src/components/chat/ChatArea.tsx`:
       - Identify active pending approval on the thread:
         Inspect messages for an unresolved `toolApproval` (or active stream pending approval).
       - When a pending approval exists:
         Mount the card in a docked container directly above `inputBar`:
         ```tsx
         {pendingApproval && (
           <div className="mx-4 mb-2 p-1 rounded-lg border border-border/80 bg-background/95 backdrop-blur shadow-lg z-10 animate-fadeSlideUp">
             <ChatToolApprovalCard
               threadId={thread?.id ?? ""}
               approval={pendingApproval}
               onDecision={() => {
                 // Trigger transcript scroll or settle
               }}
             />
           </div>
         )}
         ```
       - If the user is scrolled away from bottom, the docked card remains fully visible and interactable.
    2. In `frontend/src/components/chat/MessageItem.tsx`:
       - Only render inline `ChatToolApprovalCard` if it is ALREADY settled/decided (or when not docked), preventing duplicate active cards.
  </action>
  <verify>
    Run `npx vitest run src/components/chat/__tests__/ToolApproval.test.tsx` from `frontend/`.
  </verify>
  <acceptance_criteria>
    - Pending approval docks above `MessageInput` in `ChatArea.tsx`.
    - Settled decisions resolve cleanly in transcript without duplicate active controls.
  </acceptance_criteria>
  <done>Approval card docked above composer while pending.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests in ToolApproval.test.tsx</name>
  <files>frontend/src/components/chat/__tests__/ToolApproval.test.tsx</files>
  <read_first>frontend/src/components/chat/__tests__/ToolApproval.test.tsx</read_first>
  <action>
    In `frontend/src/components/chat/__tests__/ToolApproval.test.tsx`:
    1. Add test: "renders ticking countdown clock when expiresAt is provided".
    2. Add test: "disables action buttons and shows timed out copy when expiresAt has passed".
    3. Add test: "gracefully renders standard card when expiresAt is omitted (backward compatibility)".
  </action>
  <verify>
    Run `npx vitest run src/components/chat/__tests__/ToolApproval.test.tsx` from `frontend/`.
  </verify>
  <acceptance_criteria>
    - All tests in `ToolApproval.test.tsx` pass.
  </acceptance_criteria>
  <done>Unit tests for countdown and timeout state passing.</done>
</task>

</tasks>

<threat_model>
- Threat: Duplicate card mounting between MessageItem (transcript) and ChatArea (docked).
- Mitigation: Gated by pending status: while pending, it is docked; upon resolution or reload, it settles into MessageItem in the transcript.
</threat_model>
