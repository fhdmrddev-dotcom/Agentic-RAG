---
phase: 224-what-the-agent-is-doing-reads-like-a-sentence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/tool_dispatcher.py
  - backend/tests/test_224_approval_deadline.py
autonomous: true
requirements:
  - BUG-260902-04
user_setup: []

must_haves:
  truths:
    - "When posture == 'ask', tool_dispatcher computes expires_at as ISO 8601 UTC timestamp and includes it in tool_approval_required emit"
    - "The client receives an explicit server deadline on the wire rather than inventing a client-side timer"
    - "Unit test verifies expires_at is present, valid ISO format, and within 120s of now"
  artifacts:
    - backend/app/services/tool_dispatcher.py
    - backend/tests/test_224_approval_deadline.py
  key_links:
    - "ctx.emit('tool_approval_required', ..., expires_at=expires_at)"
---

<objective>
Put the approval deadline on the wire from the backend:
1. In `backend/app/services/tool_dispatcher.py`, when a connector tool pauses on `posture == "ask"`, compute `expires_at` as an ISO 8601 UTC string (`(datetime.now(timezone.utc) + timedelta(seconds=120.0)).isoformat()`).
2. Pass `expires_at` to `ctx.emit(..., "tool_approval_required", ..., expires_at=expires_at)`.
3. Create `backend/tests/test_224_approval_deadline.py` verifying the emitted event payload.

Output: Instrumented `tool_dispatcher.py` and passing `test_224_approval_deadline.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-CONTEXT.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-PREFLIGHT.md
@backend/app/services/tool_dispatcher.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute and emit expires_at in tool_dispatcher.py</name>
  <files>backend/app/services/tool_dispatcher.py</files>
  <read_first>backend/app/services/tool_dispatcher.py</read_first>
  <action>
    In `backend/app/services/tool_dispatcher.py` around lines 4441-4460:
    1. In `_handle_connector_chat_tool`, right when `if posture == "ask" and getattr(ctx, "redis", None) is not None:` begins:
       Compute the deadline timestamp:
       ```python
       now_utc = datetime.now(timezone.utc)
       deadline_utc = now_utc + timedelta(seconds=120.0)
       expires_at_iso = deadline_utc.isoformat()
       ```
    2. Pass `expires_at=expires_at_iso` into `ctx.emit`:
       ```python
       await ctx.emit(
           ctx.redis,
           getattr(ctx, "run_id", None),
           "tool_approval_required",
           connection_id=str(matched_conn.id),
           service_id=service_id,
           service_name=matched_conn.name,
           tool_name=action_tool_name,
           args=args,
           expires_at=expires_at_iso,
       )
       ```
  </action>
  <verify>
    Inspect line diffs in `tool_dispatcher.py` to confirm `expires_at` is emitted in `tool_approval_required`.
  </verify>
  <acceptance_criteria>
    - `tool_dispatcher.py` passes `expires_at` to `ctx.emit("tool_approval_required", ...)`.
  </acceptance_criteria>
  <done>Deadline timestamp added to tool_approval_required emit.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests in backend/tests/test_224_approval_deadline.py</name>
  <files>backend/tests/test_224_approval_deadline.py</files>
  <read_first>backend/tests/test_223_dispatcher_audit.py</read_first>
  <action>
    Create `backend/tests/test_224_approval_deadline.py`:
    1. Set up test for `_handle_connector_chat_tool` when `posture == "ask"`:
       - Mock `ctx.redis` and `ctx.emit`.
       - Mock pubsub message or timeout.
       - Assert `ctx.emit` was called with event name `"tool_approval_required"` and keyword argument `expires_at`.
       - Validate that `datetime.fromisoformat(expires_at)` is in UTC and is approximately 120s from test execution time (e.g. `118.0 <= diff <= 122.0`).
  </action>
  <verify>
    Run `pytest backend/tests/test_224_approval_deadline.py -v` from repo root.
  </verify>
  <acceptance_criteria>
    - `test_224_approval_deadline.py` passes with 0 failures.
  </acceptance_criteria>
  <done>Unit tests for approval wire deadline passing.</done>
</task>

</tasks>

<threat_model>
- Threat: Clock differences between client and server could cause premature expiration if client compares server timestamp to local clock.
- Mitigation: In Plan 224-03, client anchors countdown to arrival time: remaining duration is computed relative to arrival time or clamped to >= 0.
</threat_model>
