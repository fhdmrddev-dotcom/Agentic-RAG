---
phase: 182-server-validation-seam
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/harness/scope.py
  - backend/app/services/harness/grounding.py
  - backend/tests/unit/test_182_validate.py
  - backend/tests/unit/test_182_folder_scope_keying.py
autonomous: true
gap_closure: true
requirements: [VALID-01]

must_haves:
  truths:
    - "The `folder_scope` verdict emitted by `grounding.grounding_verdicts` carries `phase == <the offending phase slug>` — NOT `None` — so a canvas consumer keys it to a node without parsing prose (SC#4 / D-182-03 per-node keying)."
    - "The offending slug reaches the verdict STRUCTURALLY: `assert_folder_scopes_subset` raises a typed exception carrying the slug as an attribute, and `grounding.py` reads that attribute. No regex/split/parse of the message text exists anywhere in the backend, and no second folder-subtree ⊆ walk is introduced (D-182-06 red line / D-182-02 reuse-verbatim)."
    - "The raised message string is BYTE-IDENTICAL to the pre-change wording, so `_check_grounding_fidelity`'s `{ok, error, detail}` short-circuit dict and every test asserting on `detail`/`str(exc)` are unchanged (D-182-02 extraction-parity guarantee)."
    - "Every existing caller of `assert_folder_scopes_subset` that catches `ValueError` still catches the new exception and behaves identically — `workflow_kickoff` still 400s, `runs.py` and `harness_engine.py` still degrade best-effort, `test_098_scope_governance` still passes with `pytest.raises(ValueError)`."
    - "A ValueError raised by the ⊆ path WITHOUT a slug attribute (e.g. a test double or a future non-phase-specific failure) still produces a `folder_scope` verdict with `phase: None` and never raises — the threading degrades safely."
    - "A regression test FAILS if `folder_scope`'s `phase` reverts to `None`, and the test that previously froze the bug (`test_folder_scope_verdict_is_workflow_global`) no longer exists under a name asserting the wrong contract."
  artifacts:
    - path: "backend/app/services/harness/scope.py"
      provides: "A ValueError subclass carrying the offending phase slug, raised by assert_folder_scopes_subset"
      contains: "phase_slug"
    - path: "backend/app/services/harness/grounding.py"
      provides: "_folder_scope_violation returns the message AND the slug; grounding_verdicts populates the folder_scope verdict's phase field"
      contains: "folder_scope"
    - path: "backend/tests/unit/test_182_folder_scope_keying.py"
      provides: "Falsification-tested regression proof — scope raises the typed exception with the right slug; the verdict is keyed; every existing caller still behaves"
      min_lines: 80
    - path: "backend/tests/unit/test_182_validate.py"
      provides: "The route-level folder_scope verdict test, renamed and re-asserted onto per-node keying"
      min_lines: 300
  key_links:
    - from: "backend/app/services/harness/grounding.py"
      to: "backend/app/services/harness/scope.py"
      via: "typed exception attribute read inside _folder_scope_violation (never a message parse)"
      pattern: "phase_slug"
    - from: "backend/app/services/harness/grounding.py"
      to: "grounding_verdicts folder_scope branch"
      via: "the slug threaded into the verdict dict's phase field"
      pattern: "\"code\": \"folder_scope\""
---

<objective>
Close the SC#4 BLOCKER from `182-VERIFICATION.md`: the `folder_scope` grounding verdict — the one
grounding-fidelity rule that is genuinely per-phase — is emitted with `phase: None`, stranding the
offending slug in free-text prose. A canvas consumer can only attribute it to a node by regexing the
message, which is exactly the client-side re-derivation the D-182-06 red line forbids.

Thread the slug STRUCTURALLY out of the existing `assert_folder_scopes_subset` check (never
re-deriving the ⊆ walk), so `folder_scope` is keyed the same way `unregistered_tool` /
`unregistered_skill` already are.

Purpose: make the phase's own per-node-keying contract (ROADMAP SC#4) true for all 10 verdict codes,
so Phase 184 can paint per-node badges from the verdict alone.
Output: a typed exception in `scope.py`, a slug-carrying funnel in `grounding.py`, a populated
`phase` on the `folder_scope` verdict, and a falsification-tested regression suite.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-server-validation-seam/182-CONTEXT.md
@.planning/phases/182-server-validation-seam/182-VERIFICATION.md
@.planning/phases/182-server-validation-seam/182-PATTERNS.md
@.planning/phases/182-server-validation-seam/182-01-SUMMARY.md

<interfaces>
<!-- Exact current contracts. Use these directly — no codebase exploration needed. -->

backend/app/services/harness/scope.py (the check to thread, NEVER to re-derive):
  async def assert_folder_scopes_subset(definition, *, supabase: Client, user_id: str) -> None
    # :197-231. Resolves the project subtree via resolve_project_subtree, then for each phase whose
    # config.folder_scope has ids OUTSIDE the subtree raises:
    #   ValueError(f"phase '{phase.slug}' folder_scope is not a subset of the project subtree: {sorted(outside)}")
    # (the message is split across two f-string fragments in source — "...subset of the " + "project subtree: ...")
    # Returns early (no-op) when resolve_project_subtree returns None (unbound workflow).
  async def resolve_project_subtree(project_folder_id, *, supabase, user_id) -> list[str] | None   # :51

backend/app/services/harness/grounding.py (the single funnel + the collector):
  async def _folder_scope_violation(wd, *, supabase, user_id: str) -> str | None                    # :348-363
    # function-local `from app.services.harness.scope import assert_folder_scopes_subset`
    # (the Phase-103 monkeypatch seam — KEEP the function-local import)
    # try/except ValueError -> return str(exc); else None
  async def grounding_verdicts(wd, *, supabase, user_id, tool_names, skill_ids) -> list[dict]       # :389-435
    # Rule 1 branch (:409-412): out.append({"code": "folder_scope", "phase": None, "message": violation})
    # Rules 2+3 (:415-434): {"code": "unregistered_tool"|"unregistered_skill", "phase": phase.slug, "message": ...}
  async def _check_grounding_fidelity(wd, *, supabase, user_id, tool_names, skill_ids) -> dict|None # :441-475
    # Rule-1 consumer: `if violation is not None: return _grounding_failed(violation)`
  def _grounding_failed(detail: str) -> dict -> {"ok": False, "error": "grounding_failed", "detail": detail}  # :240

EVERY caller of assert_folder_scopes_subset in the backend (audit these — do not break them):
  backend/app/services/harness/grounding.py:360      — except ValueError -> str(exc)              (this plan changes it)
  backend/app/services/workflow_kickoff.py:246-253   — except ValueError -> HTTPException(400, detail=str(err))
  backend/app/api/runs.py:946                        — inside a broad try; best-effort, never blocks the Continue
  backend/app/services/harness_engine.py:1588        — inside a broad try; degrades to unscoped fallback
  backend/tests/test_098_scope_governance.py:109     — `with pytest.raises(ValueError, match="is not a subset")`
  backend/tests/test_harness_resume.py:578           — monkeypatches the module attr with a fake (never raises)
  backend/tests/unit/test_103_grounding_fidelity.py:107 — monkeypatches with a fake raising a PLAIN ValueError
  backend/tests/unit/test_182_validate.py:220        — monkeypatches with a fake raising a PLAIN ValueError

Tests whose current assertions depend on the message string staying byte-identical:
  backend/tests/test_182_extraction_parity.py::test_two_presentations_over_the_same_rules
  backend/tests/unit/test_103_grounding_fidelity.py  (the {ok, error, detail} short-circuit dict)
  backend/tests/test_098_scope_governance.py::test_narrow_only_reject (match="is not a subset")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typed folder-scope exception in scope.py carrying the offending phase slug</name>
  <files>backend/app/services/harness/scope.py, backend/tests/unit/test_182_folder_scope_keying.py</files>
  <read_first>
    - backend/app/services/harness/scope.py (lines 1-50 for the module docstring/imports posture; lines 197-231 — the full `assert_folder_scopes_subset` body and the exact raise site)
    - backend/app/services/workflow_kickoff.py (lines 240-255 — the `except ValueError` -> HTTPException(400) caller that MUST keep working)
    - backend/app/api/runs.py (lines 930-965 — the best-effort broad-try caller)
    - backend/app/services/harness_engine.py (lines 1560-1600 — the resume-path caller inside a broad try)
    - backend/tests/test_098_scope_governance.py (lines 70-110 — `pytest.raises(ValueError, match="is not a subset")`, the existing contract that must not break)
  </read_first>
  <behavior>
    - `assert_folder_scopes_subset` raises an exception that IS a `ValueError` (isinstance check passes) and carries the offending phase slug as attribute `phase_slug`.
    - `str(exc)` is byte-identical to the current message: `phase '<slug>' folder_scope is not a subset of the project subtree: <sorted list of outside ids>`.
    - `pytest.raises(ValueError, match="is not a subset")` still catches it.
    - `workflow_kickoff`'s 400 detail text is unchanged.
    - A clean (⊆) definition and an unbound (`project_folder_id is None`) definition still raise nothing.
  </behavior>
  <action>
    In `backend/app/services/harness/scope.py`, define a module-level exception class named `FolderScopeSubsetError` that SUBCLASSES `ValueError`. Its `__init__` accepts the message positionally plus a keyword-only `phase_slug: str | None = None`, calls `super().__init__(message)` so `str(exc)` and `args` are unchanged, and stores `self.phase_slug = phase_slug`. Export it at module level (no `__all__` gymnastics needed — the module has none today).

    Give the class a docstring stating the contract explicitly: it is a `ValueError` SUBCLASS on purpose so that every pre-existing `except ValueError` handler (`workflow_kickoff.py:249` -> HTTP 400, `runs.py:942` best-effort, `harness_engine.py` resume fallback, `test_098_scope_governance.py`'s `pytest.raises(ValueError)`) keeps working by construction; the added `phase_slug` attribute exists so the Phase-182 `/validate` seam can key the `folder_scope` verdict to a NODE without parsing the message prose (SC#4 / D-182-06 — the canvas must never re-derive server rules client-side).

    Change the single `raise ValueError(...)` inside `assert_folder_scopes_subset`'s per-phase loop to raise `FolderScopeSubsetError` with the SAME message expression (both f-string fragments concatenated exactly as they are today — do not reflow, reword, or re-space the string) and `phase_slug=phase.slug`. Do NOT change the loop, the subtree resolution, the unbound-workflow early return, the cycle guard, or the owner scoping — this is the security-reviewed T-098-02 check and the ⊆ walk stays exactly as-is.

    Update the `assert_folder_scopes_subset` docstring to name the new exception type and to state that `phase_slug` is the structural channel for the offending phase (replacing message-parsing).

    Create `backend/tests/unit/test_182_folder_scope_keying.py` with the SCOPE-LEVEL half of the regression proof (the route-level half lands in Task 2). Follow the repo test conventions: imports INSIDE test bodies (Phase 102 posture), `pytest.mark.asyncio` on async tests, monkeypatch the MODULE attribute. Mirror `test_098_scope_governance.py::test_narrow_only_reject` for the definition builder and the `resolve_project_subtree` monkeypatch (patch `scope_mod.resolve_project_subtree` — `assert_folder_scopes_subset` looks it up as a module global, so no DB is needed). Cover:
      (a) a non-⊆ definition raises `FolderScopeSubsetError`, `isinstance(exc, ValueError)` is True, and `exc.phase_slug` equals the offending phase's slug — this is the NON-TAUTOLOGICAL proof that the real check (not a test double) supplies the slug;
      (b) `str(exc)` equals an explicitly written-out golden literal of the full message for a known slug + known outside id (write the expected string as a literal, do NOT re-derive it from the source) — the byte-identical guarantee;
      (c) `pytest.raises(ValueError, match="is not a subset")` still catches it — the caller-compatibility proof;
      (d) a definition whose scopes ARE ⊆ raises nothing, and an unbound definition (`project_folder_id` absent) raises nothing.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_folder_scope_keying.py tests/test_098_scope_governance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_folder_scope_keying.py tests/test_098_scope_governance.py -q` exits 0 with at least 10 passed (4 new + the 6 existing 098 tests).
    - `grep -c "class FolderScopeSubsetError" backend/app/services/harness/scope.py` returns 1, and the class line contains `(ValueError)`.
    - `grep -c "raise ValueError" backend/app/services/harness/scope.py` returns 0 (the only raise in this file is now the typed one).
    - `grep -c "phase_slug=phase.slug" backend/app/services/harness/scope.py` returns 1.
    - A test in the new file asserts `str(exc)` against an explicitly written string literal (not a re-derivation) — `grep -c "is not a subset of the project subtree" backend/tests/unit/test_182_folder_scope_keying.py` >= 1.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_103_grounding_fidelity.py tests/test_harness_resume.py -q` still passes — the monkeypatch seams that raise a PLAIN ValueError are unaffected.
    - `cd backend && venv/Scripts/python.exe -c "import app.services.workflow_kickoff, app.api.runs, app.services.harness_engine"` exits 0 (no import-time breakage in the three production callers).
  </acceptance_criteria>
  <done>`assert_folder_scopes_subset` raises a ValueError subclass carrying `phase_slug`, with a byte-identical message and every existing caller proven unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the slug into the folder_scope verdict + replace the test that froze the bug</name>
  <files>backend/app/services/harness/grounding.py, backend/tests/unit/test_182_validate.py, backend/tests/unit/test_182_folder_scope_keying.py</files>
  <read_first>
    - backend/app/services/harness/grounding.py (lines 338-475 — the three atomic rules, `_folder_scope_violation`, `grounding_verdicts`, `_check_grounding_fidelity`; and lines 1-50 — the module docstring's "two presentations over ONE rule set" contract)
    - backend/app/services/harness/scope.py (the `FolderScopeSubsetError` created in Task 1)
    - backend/tests/unit/test_182_validate.py (lines 75-120 — `_patch_bundle` / `_validate` / `_by_code` helpers; lines 203-232 — `test_folder_scope_verdict_is_workflow_global`, the test to REPLACE)
    - backend/tests/test_182_extraction_parity.py (lines 162-225 — `test_grounding_is_the_one_source` source assertions; lines 227-288 — `test_two_presentations_over_the_same_rules`, which pins the short-circuit `detail` string)
  </read_first>
  <behavior>
    - `grounding_verdicts` on a definition with a non-⊆ `folder_scope` emits `{"code": "folder_scope", "phase": "<offending slug>", "message": "<unchanged message>"}`.
    - The `/validate` route response's `folder_scope` verdict has `verdict.phase == "<offending slug>"` and `verdict.severity == "error"`.
    - `_check_grounding_fidelity` still returns `{"ok": False, "error": "grounding_failed", "detail": "<the same message>"}` — no shape or string change.
    - A ⊆-path `ValueError` with NO `phase_slug` attribute still yields `phase: None` and never raises.
  </behavior>
  <action>
    In `backend/app/services/harness/grounding.py`, change `_folder_scope_violation` so it returns BOTH the message and the offending slug instead of a bare string. Concrete target shape: return `tuple[str, str | None] | None` — `None` when clean, otherwise `(str(exc), getattr(exc, "phase_slug", None))`. Keep the `try` / `except ValueError` (NOT `except FolderScopeSubsetError`) and keep the function-local `from app.services.harness.scope import assert_folder_scopes_subset` import — that is the Phase-103 monkeypatch seam and narrowing the except clause would break the existing test doubles that raise a plain `ValueError`. Use `getattr(exc, "phase_slug", None)` so a plain `ValueError` degrades to `None` rather than raising `AttributeError`.

    Update `_folder_scope_violation`'s docstring: it still reuses `assert_folder_scopes_subset` VERBATIM and never re-derives the ⊆ walk; the slug now travels STRUCTURALLY on the exception (the D-182-06 red line — the canvas must never parse the message to attribute a node); a non-typed `ValueError` degrades to a `None` slug.

    In `grounding_verdicts`, replace the Rule-1 branch so it unpacks the tuple and emits `{"code": "folder_scope", "phase": <the slug>, "message": <the message>}`. Delete the now-false inline comment that calls Rule 1 "workflow-global (the offending phase slug is inside the message)" and replace it with a comment stating Rule 1 is PER-NODE like Rules 2 and 3: the slug is threaded off `FolderScopeSubsetError.phase_slug` (SC#4), and only a non-typed `ValueError` leaves `phase` as `None`.

    In `_check_grounding_fidelity`, adapt the Rule-1 consumer to the new tuple return — it must pass ONLY the message into `_grounding_failed(...)` so the `{ok, error, detail}` dict is byte-identical to today. Do not change its short-circuit order, its rule order, or any message string.

    Update the `grounding.py` MODULE docstring where it describes `grounding_verdicts` as collecting `{code, phase, message}` "keyed by phase slug" — state explicitly that all three fidelity rules now key to a phase slug, including `folder_scope`.

    In `backend/tests/unit/test_182_validate.py`, REPLACE `test_folder_scope_verdict_is_workflow_global` (lines 203-232). Rename it to `test_folder_scope_verdict_is_keyed_to_the_phase` so the name no longer asserts the wrong contract, rewrite the docstring (the slug travels structurally on the exception, not in prose), change the monkeypatched fake to raise `FolderScopeSubsetError` (imported from `app.services.harness.scope`) with the SAME message text and `phase_slug="answer"`, and change the assertions to `verdict.phase == "answer"` plus `verdict.severity == "error"`. Keep the `"answer" in verdict.message` assertion (the message is unchanged) and keep the `scope_mod` module-attribute monkeypatch posture. Do NOT delete any other test in the file — the file must still hold 12 test definitions.

    In `backend/tests/unit/test_182_folder_scope_keying.py`, add the COLLECTOR-level and DEGRADATION halves of the regression proof:
      (a) call `grounding.grounding_verdicts` directly with `assert_folder_scopes_subset` monkeypatched to raise `FolderScopeSubsetError(msg, phase_slug="p1")`, and assert the emitted verdict dict has `phase == "p1"` — with an assertion message naming this as the SC#4 regression guard that must FAIL if `phase` reverts to `None`;
      (b) an END-TO-END-ish variant that does NOT hand-fake the exception: build a real non-⊆ definition, monkeypatch only `scope_mod.resolve_project_subtree` (so the REAL `assert_folder_scopes_subset` runs and raises for real), call `grounding_verdicts`, and assert the verdict's `phase` equals the real offending slug — this proves the whole chain, not a mock's attribute;
      (c) the degradation case: monkeypatch `assert_folder_scopes_subset` to raise a PLAIN `ValueError("something else went wrong")` and assert `grounding_verdicts` returns a `folder_scope` verdict with `phase is None` and does not raise;
      (d) a one-source guard asserting there is NO message-parsing anywhere: read `backend/app/services/harness/grounding.py` and `backend/app/api/workflows.py` as text and assert none of the substrings `re.search`, `re.match`, `.split("'")`, or `verdict["message"]` used for slug extraction appear in the folder-scope path — phrase this as an explicit list of forbidden tokens with a failure message citing D-182-06.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_validate.py tests/test_182_extraction_parity.py tests/unit/test_103_grounding_fidelity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_validate.py tests/test_182_extraction_parity.py tests/unit/test_103_grounding_fidelity.py -q` exits 0.
    - `grep -c '"phase": None' backend/app/services/harness/grounding.py` returns 0 (no hardcoded null phase remains in any verdict emit site).
    - `grep -c "test_folder_scope_verdict_is_workflow_global" backend/tests/unit/test_182_validate.py` returns 0; `grep -c "test_folder_scope_verdict_is_keyed_to_the_phase" backend/tests/unit/test_182_validate.py` returns 1.
    - `grep -c "^\s*\(async \)\?def test_" backend/tests/unit/test_182_validate.py` returns 12 — the count is UNCHANGED (Phase-177 coverage-loss lesson: a rename must not silently drop a test).
    - `grep -c "getattr(exc, \"phase_slug\", None)" backend/app/services/harness/grounding.py` returns 1 — the degrade-safe read, not a bare attribute access.
    - `grep -c "except ValueError" backend/app/services/harness/grounding.py` returns 1 — the except clause was NOT narrowed to the subclass (the plain-ValueError test doubles still work).
    - The full Phase-182 baseline suite still passes with NO reduction in test count: `cd backend && venv/Scripts/python.exe -m pytest tests/test_182_extraction_parity.py tests/test_182_grounding_bundle.py tests/unit/test_182_validate.py tests/test_revert_byte_identical.py tests/test_181_flip_on.py tests/test_181_off_audience.py tests/unit/test_103_nl_generate.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_103_lint_block.py -q` reports at least `54 passed` (the verified pre-change baseline) and 0 failed.
    - A test exists that drives the REAL `assert_folder_scopes_subset` (only `resolve_project_subtree` faked) and asserts the verdict's `phase` — i.e. the proof is not purely a mock-attribute echo.
  </acceptance_criteria>
  <done>The `folder_scope` verdict is keyed to its phase slug end-to-end, the message and short-circuit dict are byte-identical, and a falsification-tested regression fails if `phase` reverts to `None`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `POST /workflows/validate` | An untrusted `WorkflowDefinition` body supplies the `folder_scope` UUIDs and the phase slugs that now ride back out on the verdict's `phase` field. |
| API → Supabase (owner-scoped ⊆ walk) | `assert_folder_scopes_subset` → `resolve_project_subtree` → `fetch_visible_folders` is the owner+org-scoped read (T-098-02); this plan must not alter its scoping. |
| exception → verdict envelope | The new `phase_slug` attribute is a new data channel from a security-reviewed check into a client-visible response. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-08 | Tampering / Elevation | `assert_folder_scopes_subset` in `scope.py` | mitigate | The ⊆ walk, subtree resolution, cycle guard and owner scoping are NOT touched — only the exception TYPE and one added attribute change. Acceptance criteria pin `grep -c "raise ValueError"` to 0 and forbid any second ⊆ walk; `test_098_scope_governance.py` (6 tests) is re-run unchanged as the behavioral proof. |
| T-182-09 | Information Disclosure | the `phase` field on the `folder_scope` verdict | accept | `phase_slug` is a slug the CALLER supplied in their own request body — the response echoes back the caller's own definition, never a server-side row, folder name, or another tenant's identifier. The outside-folder UUIDs already in the message are likewise caller-supplied. No new information crosses the boundary. |
| T-182-10 | Denial of Service | broadened exception handling in `grounding.py` | mitigate | The `except ValueError` clause is deliberately NOT narrowed and the slug is read via `getattr(..., None)`, so a non-typed `ValueError` from any future ⊆-path change degrades to `phase: None` instead of raising an `AttributeError` inside a route documented "ALWAYS HTTP 200". A dedicated degradation test pins this. |
| T-182-11 | Repudiation / Tampering | the three best-effort production callers (`runs.py`, `harness_engine.py`, `workflow_kickoff.py`) | mitigate | The new exception is a `ValueError` SUBCLASS, so every existing `except ValueError` handler catches it by construction; an import smoke-check plus the 098 suite is the acceptance gate. `workflow_kickoff`'s 400 `detail=str(err)` is unchanged because `super().__init__(message)` preserves `str(exc)`. |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan — no `requirements.txt` / `package.json` change. Nothing to audit. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_validate.py tests/test_182_extraction_parity.py tests/test_098_scope_governance.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_103_nl_generate.py tests/test_harness_resume.py -q` green.
- Phase-182 combined baseline (9 files) reports >= 54 passed, 0 failed — the test COUNT must not shrink.
- `grep -rn "assert_folder_scopes_subset" backend/app/` shows exactly the 4 pre-existing production call sites (grounding, workflow_kickoff, runs, harness_engine) — no new caller, no second ⊆ implementation.
</verification>

<success_criteria>
- The `folder_scope` verdict carries the offending phase slug in `phase`, exactly as `unregistered_tool` / `unregistered_skill` do (ROADMAP SC#4 satisfied for all 10 codes).
- The slug travels via a typed exception attribute — no regex, no message parsing, no re-derived ⊆ walk (D-182-06).
- The message string, the `{ok, error, detail}` short-circuit dict, and every existing `except ValueError` caller are byte-for-byte unchanged in behavior.
- The test that froze the bug is gone; a falsification-tested regression fails if `phase` reverts to `None`.
</success_criteria>

<output>
Create `.planning/phases/182-server-validation-seam/182-04-SUMMARY.md` when done.
</output>
