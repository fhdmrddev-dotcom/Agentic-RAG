---
phase: 182-server-validation-seam
plan: 10
type: execute
wave: 2
depends_on: ["182-08"]
files_modified:
  - backend/app/services/harness/scope.py
  - backend/app/services/harness/grounding.py
  - backend/tests/unit/test_182_folder_scope_keying.py
  - backend/tests/unit/test_182_validate.py
autonomous: true
gap_closure: true
requirements: [VALID-01]

must_haves:
  truths:
    - "WR-04 closed (item 6 of the D-182-R2-03 scope): `grounding_verdicts` emits ONE `folder_scope` verdict per offending phase. Today `assert_folder_scopes_subset` raises inside its `for phase in definition.phases` loop on the FIRST offender, so a definition with three out-of-subtree phases produces exactly one verdict — the author fixes node A, re-validates, and node B lights up: the whack-a-mole loop the per-node collector exists to eliminate."
    - "This completes the SC#4 work 182-04 started. 182-04 fixed the KEYING (the verdict now names its phase); this fixes the MULTIPLICITY (every offending phase gets its own keyed verdict), which is what Phase 184's per-node badges actually consume — later offenders would otherwise render clean."
    - "The ⊆ walk is NOT re-derived (D-182-06 red line). `scope.py` gains ONE non-raising collector that owns the walk, and `assert_folder_scopes_subset` becomes a thin presentation over it that raises the first violation — one source, two presentations, exactly the pattern `grounding.py` already uses for its two fidelity presentations."
    - "The structural slug-threading 182-04 built is preserved end to end: each violation is a `FolderScopeSubsetError` carrying `phase_slug`, and the collector reads it via `getattr(exc, \"phase_slug\", None)` — never by parsing the message prose."
    - "Byte-identical everywhere it was: `str(exc)` and `exc.args` for the raised first violation are unchanged, so `workflow_kickoff`'s HTTP-400 `detail`, `runs.py`'s Continue fallback, `harness_engine`'s resume fallback, `test_098_scope_governance`'s `match=\"is not a subset\"`, and `_check_grounding_fidelity`'s `{ok, error, detail}` NL-gen dict all behave exactly as today."
    - "The overstated docstring is corrected: `grounding.py`'s module header currently claims `folder_scope` keys per-node 'so Phase 184 can paint a per-node badge from the verdict alone' with no mention that it painted exactly one badge. After this plan the claim is true as written."
  artifacts:
    - path: "backend/app/services/harness/scope.py"
      provides: "folder_scope_violations — the ONE ⊆ walk, non-raising, collecting every offending phase; assert_folder_scopes_subset becomes its raising presentation"
      contains: "folder_scope_violations"
    - path: "backend/app/services/harness/grounding.py"
      provides: "A plural _folder_scope_violations helper feeding grounding_verdicts one verdict per violation; the singular helper retained verbatim for the NL-gen short-circuit"
      contains: "_folder_scope_violations"
    - path: "backend/tests/unit/test_182_folder_scope_keying.py"
      provides: "A multi-offender regression proving every out-of-subtree phase gets its own keyed verdict, plus the repointed degradation seam"
      min_lines: 330
  key_links:
    - from: "backend/app/services/harness/grounding.py"
      to: "backend/app/services/harness/scope.py"
      via: "folder_scope_violations imported function-locally so the monkeypatch seam is honored"
      pattern: "folder_scope_violations"
    - from: "backend/app/services/harness/scope.py (assert_folder_scopes_subset)"
      to: "backend/app/services/harness/scope.py (folder_scope_violations)"
      via: "delegation — the raising form re-raises violations[0]"
      pattern: "raise violations\\[0\\]"
---

<objective>
Close WR-04 from `182-REVIEW.md` (round 2), selected by the operator as item 6 of the
D-182-R2-03 scope.

`grounding_verdicts`' own docstring says it "COLLECTS every violation ... the canvas needs all of
them at once". For two of its three rules that is true — `test_182_extraction_parity.py`'s
`test_two_presentations_over_the_same_rules` explicitly proves rule 2 reports BOTH offending
phases. For rule 1 it is false: `assert_folder_scopes_subset` (`scope.py:258-270`) `raise`s inside
its `for phase in definition.phases` loop, so at most one `folder_scope` verdict is ever emitted no
matter how many phases are out of subtree. The asymmetry is visible inside the suite itself.

Phase 184 paints per-node badges from exactly this verdict (VALID-03), so today the second and
third offending nodes would render clean and the author would discover them one at a time.

Two constraints the operator named explicitly: the fix must NOT re-derive the ⊆ walk client-side
or anywhere else (the D-182-06 red line), and it must keep the structural slug-threading 182-04
built (`FolderScopeSubsetError.phase_slug`, never a regex over the message).

Purpose: make the collector's "every violation" contract true for all three rules.
Output: one non-raising ⊆ collector in `scope.py` with the raising form as a thin presentation over
it, a plural grounding helper, and a multi-offender regression that FAILS against today's code.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-server-validation-seam/182-CONTEXT.md
@.planning/phases/182-server-validation-seam/182-REVIEW.md
@.planning/phases/182-server-validation-seam/182-04-SUMMARY.md

<interfaces>
<!-- Exact current contracts, verified by reading the source this planning session. -->

backend/app/services/harness/scope.py
  line 51-76   class FolderScopeSubsetError(ValueError):
                   def __init__(self, message: str, *, phase_slug: str | None = None) -> None:
                       super().__init__(message)      # str(exc) / args byte-identical
                       self.phase_slug = phase_slug
  line 79      async def resolve_project_subtree(project_folder_id, *, supabase, user_id) -> list[str] | None
               # None in -> None out (unbound workflow). Resolved as a MODULE GLOBAL by the
               # function below, which is what makes the offline test seam work.
  line 225-270 async def assert_folder_scopes_subset(definition, *, supabase, user_id) -> None
                   subtree = await resolve_project_subtree(definition.project_folder_id,
                                                           supabase=supabase, user_id=user_id)
                   if subtree is None: return                       # unbound -> no-op
                   allowed = set(subtree)
                   for phase in definition.phases:
                       scope = getattr(phase.config, "folder_scope", None)
                       if scope:
                           outside = {str(f) for f in scope} - allowed
                           if outside:
                               raise FolderScopeSubsetError(
                                   f"phase '{phase.slug}' folder_scope is not a subset of the "
                                   f"project subtree: {sorted(outside)}",
                                   phase_slug=phase.slug,
                               )
               # THE MESSAGE EXPRESSION IS A BYTE-IDENTITY CONTRACT — both f-string fragments
               # must move verbatim into the new collector, character for character.

  Every other caller of assert_folder_scopes_subset (all keep the RAISING form, untouched):
    app/services/workflow_kickoff.py:246   -> except ValueError -> HTTP 400 detail=str(err)
    app/api/runs.py:946                    -> best-effort Continue broad-try
    app/services/harness_engine.py:1588    -> resume unscoped-fallback
    app/services/harness/grounding.py:404  -> the SINGULAR _folder_scope_violation (NL-gen path)

backend/app/services/harness/grounding.py
  line 23-26   the module-header claim to correct:
               "ALL THREE fidelity rules key to a phase slug — ``folder_scope`` included ...
                so Phase 184 can paint a per-node badge from the verdict alone."
  line 377-407 async def _folder_scope_violation(wd, *, supabase, user_id) -> tuple[str, str|None] | None
                   from app.services.harness.scope import assert_folder_scopes_subset  # function-local
                   try:
                       await assert_folder_scopes_subset(wd, supabase=supabase, user_id=user_id)
                   except ValueError as exc:
                       return str(exc), getattr(exc, "phase_slug", None)
                   return None
               # KEEP THIS FUNCTION. _check_grounding_fidelity (line 508) calls it and returns
               # violation[0] as the historical grounding_failed detail — byte-identical NL-gen.
  line 433-483 async def grounding_verdicts(...) -> list[dict]
                   violation = await _folder_scope_violation(wd, supabase=supabase, user_id=user_id)
                   if violation is not None:
                       message, phase_slug = violation
                       out.append({"code": "folder_scope", "phase": phase_slug, "message": message})
                   for phase in wd.phases:  # rules 2 + 3, already per-node and already plural
                       ...
  line 358-364 GROUNDING_VERDICT_CODES = frozenset({"folder_scope", "unregistered_skill",
                                                    "unregistered_tool"})
               # UNCHANGED by this plan — no new code is emitted, only more instances of an
               # existing one. tests/unit/test_182_severity_codes.py's drift detector scans the
               # emit sites in this file, so the appended verdict dict must keep the same
               # `"code": "folder_scope"` literal form the scanner matches.

Source guards that constrain this edit (read them before writing):
  tests/test_182_extraction_parity.py:201  asserts "assert_folder_scopes_subset" IS present in
      grounding.py's source -> the singular helper's function-local import must stay.
  tests/unit/test_182_folder_scope_keying.py:292-317
      test_no_message_parsing_exists_in_the_folder_scope_path asserts that NONE of
      "re.search", "re.match", "re.findall", ".split(\"'\")", ".split(\"'\", ", 'verdict["message"]'
      appears anywhere in grounding.py or app/api/workflows.py, AND that
      'getattr(exc, "phase_slug", None)' IS present in grounding.py. The new plural helper must
      therefore also read the slug via that exact getattr idiom.
      (Round-2 review WR-10 criticises this guard's breadth. WR-10 is NOT in this round's scope —
      leave the guard as it is; just do not violate it.)
  tests/test_182_extraction_parity.py:226-287  test_two_presentations_over_the_same_rules drives
      UNBOUND definitions (no project_folder_id), so resolve_project_subtree returns None and the
      ⊆ path is a no-op there. It must stay green untouched.

Monkeypatch seams that MUST be repointed by this plan (they patch the raising form, which the
collector will no longer call):
  tests/unit/test_182_folder_scope_keying.py:189  monkeypatch.setattr(scope_mod, "assert_folder_scopes_subset", _raise_typed)
  tests/unit/test_182_folder_scope_keying.py:250  monkeypatch.setattr(scope_mod, "assert_folder_scopes_subset", _raise_plain)
  tests/unit/test_182_validate.py:227             monkeypatch.setattr(scope_mod, "assert_folder_scopes_subset", _raise_subset)
Seams that must NOT be repointed (they exercise the NL-gen short-circuit, which still uses the
raising form):
  tests/unit/test_103_grounding_fidelity.py:107
  tests/unit/test_182_folder_scope_keying.py:73-80  (_patch_subtree patches resolve_project_subtree,
      a module global inside scope.py — it keeps working for BOTH forms and is the seam the
      end-to-end tests use)

EXPLICIT NON-GOAL — do NOT narrow the `except ValueError` in `_folder_scope_violation` (or in the
new plural helper) to `FolderScopeSubsetError`. That is round-2 review WR-03 (a
`pydantic.ValidationError` is a `ValueError` subclass in Pydantic v2 and would render as a false
`folder_scope` verdict). WR-03 is NOT in this round's operator-selected scope; changing the catch
breadth here would be unrequested scope creep and would break
`test_plain_value_error_degrades_to_an_unkeyed_verdict`'s contract. Preserve today's breadth
exactly and say so in a comment so the next reviewer sees it was a decision, not an oversight.

Known-good baseline, re-measured this session: 80 passed / 0 failed across
  tests/unit/test_182_validate.py tests/unit/test_182_folder_scope_keying.py
  tests/unit/test_182_publish_grounding_stage.py tests/unit/test_182_severity_codes.py
  tests/unit/test_publish_service.py tests/test_182_extraction_parity.py
  tests/test_182_grounding_bundle.py tests/test_revert_byte_identical.py tests/test_181_flip_on.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One non-raising ⊆ collector in scope.py, with the raising form as its thin presentation</name>
  <files>backend/app/services/harness/scope.py</files>
  <read_first>
    - backend/app/services/harness/scope.py lines 1-50 (the module docstring's two documented responsibilities and the T-098-02 / T-098-11 threat notes), lines 51-78 (`FolderScopeSubsetError` and its byte-identity contract), lines 79-130 (`resolve_project_subtree` — the walk being reused, and the fact it is a module global), lines 225-271 (`assert_folder_scopes_subset` in full)
    - backend/app/services/harness/grounding.py lines 17-27 (the "two presentations over ONE rule set" pattern this change mirrors one layer down) and lines 366-375 (the section banner describing the three atomic rules)
    - .planning/phases/182-server-validation-seam/182-REVIEW.md section "WR-04" (the evidence, the suggested `folder_scope_violations` shape, and the note that `_check_grounding_fidelity` keeps calling the raising form)
    - backend/tests/test_098_scope_governance.py lines 70-115 (`test_narrow_only_reject` — the `pytest.raises(ValueError, match="is not a subset")` caller contract that must survive)
  </read_first>
  <behavior>
    - A definition with THREE out-of-subtree phases yields a three-element list from the new collector, in `definition.phases` order, each element a `FolderScopeSubsetError` whose `phase_slug` names its own phase.
    - A definition with one out-of-subtree phase yields a one-element list.
    - A clean definition yields an empty list; an unbound definition (`project_folder_id is None`) yields an empty list without walking anything.
    - `assert_folder_scopes_subset` still raises `FolderScopeSubsetError` for the FIRST offending phase, with `str(exc)` and `exc.args` byte-identical to today.
    - `assert_folder_scopes_subset` still returns `None` for clean and unbound definitions.
    - The subtree is resolved exactly ONCE per collector call — the walk is not repeated per phase.
  </behavior>
  <action>
    In `backend/app/services/harness/scope.py`, add a new module-level async function
    `folder_scope_violations(definition, *, supabase, user_id) -> list[FolderScopeSubsetError]`
    placed immediately ABOVE `assert_folder_scopes_subset`.

    Move the body of today's `assert_folder_scopes_subset` into it, changing only the terminal
    action: resolve the subtree ONCE via the module-global `resolve_project_subtree(definition.project_folder_id, supabase=supabase, user_id=user_id)`; return an empty list when it is `None` (unbound — nothing to bound against); build `allowed = set(subtree)`; iterate `definition.phases` in order; for each phase read `getattr(phase.config, "folder_scope", None)`; when it is truthy compute `outside = {str(f) for f in scope} - allowed`; and when `outside` is non-empty APPEND a `FolderScopeSubsetError` instead of raising. Return the accumulated list.

    The message expression must move VERBATIM — both f-string fragments, character for character, including the `sorted(outside)` call and the exact wording "folder_scope is not a subset of the project subtree: ". Pass `phase_slug=phase.slug` exactly as today. This is a byte-identity contract asserted against a hand-written golden literal in `test_182_folder_scope_keying.py`, and it feeds `workflow_kickoff`'s HTTP-400 `detail` and `test_098_scope_governance`'s `match=` predicate.

    Docstring `folder_scope_violations` as the ONE ⊆ walk and the source of truth for rule 1 — one source, two presentations, the same pattern `grounding.py` documents for its short-circuit dict versus its per-node collector. State that it never raises for a rule violation (a raise is a PRESENTATION choice, made by the sibling below), that it preserves phase order so the canvas can render findings in the order the author drew them, and that the subtree is resolved once for the whole definition. Keep the existing owner-scoping and cycle-guard threat notes (T-098-02) attached — reference them rather than re-explaining.

    Rewrite `assert_folder_scopes_subset` as a thin presentation: call `folder_scope_violations(definition, supabase=supabase, user_id=user_id)`, and `raise violations[0]` when the list is non-empty, otherwise return `None`. Do not re-implement any part of the walk here. Update its docstring to say it is the SHORT-CIRCUIT presentation of `folder_scope_violations` — same rule, first offender only — and that its raise-type, message, `args` and `phase_slug` are byte-identical to the pre-WR-04 behaviour so every `except ValueError` caller (workflow_kickoff's 400, runs.py's Continue fallback, harness_engine's resume fallback, and the NL-gen short-circuit in grounding.py) is unaffected by construction. Explicitly note that raising `violations[0]` rather than a synthesized error is what preserves that identity — the object raised IS the first violation the collector built.

    Update the module docstring's responsibility (2) so it names both forms and states which callers use which: run-start callers and the NL-gen short-circuit take the raising form; the `/validate` per-node collector takes the list form because the canvas needs every offending node at once (ROADMAP SC#4 / VALID-03).

    Do not change `resolve_project_subtree`, `FolderScopeSubsetError`, or the folder-override helper in this file. Do not add any new import.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_098_scope_governance.py tests/test_152_folder_override.py tests/unit/test_182_folder_scope_keying.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_098_scope_governance.py tests/test_152_folder_override.py tests/unit/test_182_folder_scope_keying.py tests/unit/test_103_grounding_fidelity.py -q` reports 0 failed — every existing caller contract survives.
    - `grep -c "async def folder_scope_violations" backend/app/services/harness/scope.py` returns 1.
    - The walk exists exactly once: `grep -c "is not a subset of the" backend/app/services/harness/scope.py` returns 1, and `grep -c "for phase in definition.phases" backend/app/services/harness/scope.py` returns 1 within the two functions (record the exact count and its locations in the SUMMARY if the folder-override helper contributes another).
    - `assert_folder_scopes_subset` no longer contains the walk: its body references `folder_scope_violations` and contains no `resolve_project_subtree` call and no `folder_scope` attribute read.
    - The raised object is the collector's own: `grep -c "raise violations\[0\]" backend/app/services/harness/scope.py` returns 1.
    - `cd backend && venv/Scripts/python.exe -c "import app.services.harness.scope"` exits 0.
  </acceptance_criteria>
  <done>`scope.py` owns exactly one ⊆ walk, exposed as a non-raising collector and a byte-identical raising short-circuit over it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: grounding_verdicts emits one folder_scope verdict per offending phase</name>
  <files>backend/app/services/harness/grounding.py</files>
  <read_first>
    - backend/app/services/harness/grounding.py lines 1-53 (the module docstring, especially lines 17-27 which carry the overstated per-node claim to correct), lines 366-408 (the atomic-rules banner and `_folder_scope_violation` in full, including the T-182-10 degradation paragraph), lines 430-483 (`grounding_verdicts`), lines 486-526 (`_check_grounding_fidelity` — the caller that must keep using the singular form)
    - backend/app/services/harness/scope.py (as rewritten in Task 1 — `folder_scope_violations`' exact signature and return type)
    - backend/tests/unit/test_182_folder_scope_keying.py lines 289-318 (`test_no_message_parsing_exists_in_the_folder_scope_path` — the forbidden-token list and the required `getattr(exc, "phase_slug", None)` idiom)
    - backend/tests/unit/test_182_severity_codes.py lines 217-245 (the grounding drift detector's regex over `"code": "..."` emit sites — the appended verdict must keep that literal form)
    - .planning/phases/182-server-validation-seam/182-REVIEW.md section "WR-04" (in particular the closing instruction that until this ships, the module docstring must say "the first offending phase")
  </read_first>
  <behavior>
    - A bound definition with three out-of-subtree phases produces three `folder_scope` verdicts from `grounding_verdicts`, keyed to `["a", "b", "c"]` in phase order, each carrying its own message.
    - A bound definition with one out-of-subtree phase produces exactly one `folder_scope` verdict, keyed to that phase — the 182-04 SC#4 behaviour is preserved.
    - A clean or unbound definition produces no `folder_scope` verdict.
    - A plain `ValueError` raised off the ⊆ path still degrades to a SINGLE unkeyed verdict (`phase: None`) carrying `str(exc)` — the always-HTTP-200 contract holds and nothing raises.
    - `_check_grounding_fidelity` still returns the historical `{ok, error, detail}` dict naming the FIRST violation only, byte-identical.
    - Rules 2 and 3 (`unregistered_tool`, `unregistered_skill`) are completely unchanged.
  </behavior>
  <action>
    In `backend/app/services/harness/grounding.py`, add a plural sibling
    `_folder_scope_violations(wd, *, supabase, user_id) -> list[tuple[str, str | None]]`
    directly BELOW the existing singular `_folder_scope_violation`.

    It imports `folder_scope_violations` from `app.services.harness.scope` FUNCTION-LOCALLY — the same import posture the singular helper uses, and the reason a monkeypatch on `app.services.harness.scope` is honored by the tests. Inside a `try`, await it and return a list comprehension mapping each violation to `(str(exc), getattr(exc, "phase_slug", None))`. Use that exact `getattr(exc, "phase_slug", None)` idiom — a source guard asserts its literal presence in this file, and it is the structural channel 182-04 built (the slug must never be recovered by regexing or splitting the message).

    Give it the SAME degradation posture as the singular helper: `except ValueError as exc:` returns a one-element list `[(str(exc), getattr(exc, "phase_slug", None))]`. Preserve the catch breadth exactly — do NOT narrow it to `FolderScopeSubsetError`. Add a comment stating that the breadth is deliberate and unchanged, that round-2 review WR-03 argues for narrowing it (a `pydantic.ValidationError` is a `ValueError` subclass in Pydantic v2), and that WR-03 is deferred and explicitly out of this round's operator-selected scope — so a future reader sees a recorded decision rather than an oversight.

    Docstring it as the per-node presentation of rule 1: the canvas needs EVERY offending node at once, because Phase 184 paints a badge per node and a short-circuited rule would leave later offenders rendering clean. Cross-reference the singular helper as the short-circuit presentation NL generation keeps using.

    In `grounding_verdicts`, replace the singular call and its single append with the plural call and a loop that appends one `{"code": "folder_scope", "phase": phase_slug, "message": message}` dict per violation. Keep the dict literal in exactly the shape it has today (same key order, same `"code": "folder_scope"` literal on its own line) so `test_182_severity_codes.py`'s emit-site scanner still matches it. Keep rule 1 positioned FIRST, before the per-phase loop for rules 2 and 3 — the verdict ordering the existing tests assert.

    Update the comment above rule 1 inside `grounding_verdicts`: it currently says "A non-⊆ folder_scope is declared by ONE phase, so the verdict keys to that phase". Replace it with the accurate statement — a non-⊆ folder_scope is declared PER PHASE, and every offending phase now gets its own keyed verdict, exactly like rules 2 and 3; the ⊆ walk itself is still the single copy in `scope.py` and is never re-derived here.

    Leave `_folder_scope_violation` (singular) and `_check_grounding_fidelity` untouched. The singular helper's function-local `assert_folder_scopes_subset` import is also what keeps that symbol present in this file's source, which `test_182_extraction_parity.py:201` asserts.

    Correct the module docstring at lines 17-27. Today it claims all three rules key to a phase slug "so Phase 184 can paint a per-node badge from the verdict alone", with no mention that `folder_scope` painted exactly one badge per definition. Rewrite that paragraph to state the now-true property: all three rules key per node AND all three report every violation, so the canvas can paint a complete badge set from one call. Add a one-line note that rule 1's multiplicity was added in the Phase-182 gap closure (WR-04) after 182-04 fixed its keying, so the provenance is legible.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_182_extraction_parity.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_103_nl_generate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_182_extraction_parity.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_103_nl_generate.py tests/unit/test_103_lint_block.py -q` reports 0 failed — the NL-gen short-circuit and the extraction-parity guards are unaffected.
    - `grep -c "async def _folder_scope_violations" backend/app/services/harness/grounding.py` returns 1, and `grep -c "async def _folder_scope_violation\b" backend/app/services/harness/grounding.py` returns 1 — both presentations exist.
    - `grep -c "assert_folder_scopes_subset" backend/app/services/harness/grounding.py` returns >= 1 — the extraction-parity source guard still holds.
    - `grep -c 'getattr(exc, "phase_slug", None)' backend/app/services/harness/grounding.py` returns >= 2 — the structural slug channel is used by both helpers.
    - `grep -c "re.search\|re.match\|re.findall" backend/app/services/harness/grounding.py` returns 0 — no message parsing was introduced.
    - The ⊆ walk was not duplicated: `grep -c "is not a subset of the" backend/app/services/harness/grounding.py` returns 0, and `grep -c "resolve_project_subtree" backend/app/services/harness/grounding.py` returns 0.
    - The verdict emit site is still scannable: `grep -c '"code": "folder_scope"' backend/app/services/harness/grounding.py` returns 1.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_severity_codes.py -q` exits 0 — the drift detector still matches the emit site.
    - The corrected docstring no longer overstates: `backend/app/services/harness/grounding.py` no longer contains the unqualified phrase "paint a per-node badge from the verdict alone" without the multiplicity statement (record the replacement text in the SUMMARY).
  </acceptance_criteria>
  <done>Every out-of-subtree phase produces its own keyed `folder_scope` verdict, the ⊆ walk is still a single copy in `scope.py`, and the NL-gen short-circuit is byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Multi-offender regression, repointed degradation seams, and the falsification record</name>
  <files>backend/tests/unit/test_182_folder_scope_keying.py, backend/tests/unit/test_182_validate.py</files>
  <read_first>
    - backend/tests/unit/test_182_folder_scope_keying.py (the whole file — the four documented layers in its docstring, the `_scoped_definition` and `_patch_subtree` helpers, the `_GOLDEN_MESSAGE` literal, and the two monkeypatch seams at lines 189 and 250 that must be repointed)
    - backend/tests/unit/test_182_validate.py lines 200-240 (`test_folder_scope_verdict_is_keyed_to_the_phase` and its `_raise_subset` seam at line 227 — the third seam to repoint). NOTE: plan 182-08 edits a DIFFERENT part of this file in the previous wave; re-read the file at HEAD before editing so the edit lands on the current text.
    - backend/tests/test_182_extraction_parity.py lines 226-287 (`test_two_presentations_over_the_same_rules` — the multi-offender shape for rule 2 that this task mirrors for rule 1)
    - backend/app/services/harness/scope.py and backend/app/services/harness/grounding.py (as rewritten in Tasks 1 and 2)
    - .planning/phases/182-server-validation-seam/182-REVIEW.md section "WR-04" (the "add a two-bad-phase test mirroring test_two_presentations_over_the_same_rules" instruction)
  </read_first>
  <behavior>
    - A bound definition whose phases `a`, `b` and `c` all declare out-of-subtree `folder_scope` produces three `folder_scope` verdicts keyed `["a", "b", "c"]`, driven END TO END through the REAL ⊆ walk with only `resolve_project_subtree` faked.
    - The same definition still yields exactly ONE `grounding_failed` dict from `_check_grounding_fidelity`, naming phase `a` only.
    - A mixed definition (one out-of-subtree phase plus one phase with an unregistered tool) produces both verdict codes, each keyed to its own phase.
    - The single-offender SC#4 assertion from 182-04 still passes unchanged in substance.
    - The plain-`ValueError` degradation still yields one unkeyed verdict and never raises.
    - `test_182_validate.py`'s folder-scope verdict test still proves per-node keying through the route.
  </behavior>
  <action>
    In `backend/tests/unit/test_182_folder_scope_keying.py`:

    Extend `_scoped_definition` (or add a sibling builder next to it) so a definition can carry
    SEVERAL phases, each with its own slug, `phase_index` and `folder_scope`. Keep the existing
    single-phase helper working so no current test needs rewriting beyond the seam repoints.

    Add a MULTIPLICITY test — the WR-04 regression guard. Build a bound definition with three
    phases `a`, `b`, `c`, all declaring the same out-of-subtree folder id, fake only
    `resolve_project_subtree` via the existing `_patch_subtree` helper so the REAL ⊆ walk runs, call
    `grounding.grounding_verdicts`, and assert the `folder_scope` verdicts' `phase` values are
    exactly `["a", "b", "c"]` in that order. Assert each verdict's message names its own slug. The
    failure message must state the WR-04 defect in full: a short-circuited rule 1 emits one verdict
    for a three-offender definition, so Phase 184 paints one badge and the author fixes nodes one at
    a time — and it must say that this assertion fails against the pre-WR-04 code with a single
    verdict keyed to `a`.

    Add the SHORT-CIRCUIT counterpart in the same test (or immediately after it): the SAME
    definition through `_check_grounding_fidelity` still returns exactly one `grounding_failed`
    dict whose `detail` names phase `a` — proving the two presentations diverge deliberately and
    that NL generation is unchanged. Mirror the structure of
    `test_182_extraction_parity.py::test_two_presentations_over_the_same_rules`.

    Add a MIXED-RULE test: a bound definition where phase `a` is out of subtree and phase `b`
    declares an unregistered tool. Assert the verdict set contains a `folder_scope` keyed to `a`
    and an `unregistered_tool` keyed to `b` — proving rule 1's new plurality composes with the
    rules that were already plural, and that rule ordering (rule 1 first, then the per-phase loop)
    is preserved.

    Repoint the two collector-level monkeypatch seams. At line 189, replace
    `monkeypatch.setattr(scope_mod, "assert_folder_scopes_subset", _raise_typed)` with a patch of
    `scope_mod.folder_scope_violations` returning a one-element LIST containing the same
    `FolderScopeSubsetError`; keep every assertion in that test unchanged, because its subject —
    "the slug reaches `verdict['phase']`" — is unchanged. At line 250, replace the
    `assert_folder_scopes_subset` patch with a patch of `scope_mod.folder_scope_violations` that
    RAISES the same plain `ValueError`; keep the assertions that the verdict is unkeyed and that
    nothing raised. Update each test's docstring to name the seam it now patches and why (the
    collector consumes the list form; the raising form is the NL-gen presentation).

    Do NOT repoint `_patch_subtree` (it patches `resolve_project_subtree`, a module global inside
    `scope.py`, and keeps working for both forms — it is what makes the end-to-end tests genuinely
    end to end). Do NOT repoint `test_short_circuit_dict_is_byte_identical_after_the_tuple_change`
    or any Phase-103 seam — those exercise the raising form on purpose.

    Update the file's module docstring: its four-layer description gains the multiplicity layer,
    and the DEGRADATION layer's wording is adjusted to name the plural seam.

    In `backend/tests/unit/test_182_validate.py`: repoint ONLY the `_raise_subset` seam at line 227
    from `assert_folder_scopes_subset` to `folder_scope_violations`, returning a one-element list
    carrying the same `FolderScopeSubsetError`. Keep every assertion in
    `test_folder_scope_verdict_is_keyed_to_the_phase` unchanged and update its docstring sentence
    that says "We monkeypatch the check to raise" to describe the list-returning seam. Touch
    nothing else in that file — plan 182-08 owns its other edits and landed them in the previous
    wave.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_validate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_validate.py -q` exits 0.
    - The multiplicity guard exists and is exact: the file contains an equality assertion of the collected `phase` values against the literal list `["a", "b", "c"]` (or the chosen slugs), not a length-only or membership assertion.
    - Test count did not shrink: `grep -c "^\s*\(async \)\?def test_" backend/tests/unit/test_182_folder_scope_keying.py` returns at least 12 (9 at HEAD plus the new multiplicity, short-circuit-counterpart and mixed-rule tests). Record the HEAD count in the SUMMARY before editing.
    - No seam was lost: `grep -c "assert_folder_scopes_subset" backend/tests/unit/test_182_folder_scope_keying.py` still returns >= 4 (the scope-level layer-1 tests keep driving the raising form directly).
    - The collector seams are repointed: `grep -c "folder_scope_violations" backend/tests/unit/test_182_folder_scope_keying.py` returns >= 2 and `grep -c "folder_scope_violations" backend/tests/unit/test_182_validate.py` returns >= 1.
    - Falsification recorded in the SUMMARY: temporarily change `grounding_verdicts` to append only the FIRST element of the plural helper's list, confirm the multiplicity test FAILS with the observed `["a"]`, then revert. Record the exact observed failure message.
    - Second falsification recorded in the SUMMARY: temporarily change `assert_folder_scopes_subset` to raise a NEW `FolderScopeSubsetError` built from `violations[0]`'s message instead of re-raising the object, confirm the byte-identity tests still pass but note in the SUMMARY why re-raising the object is nonetheless the stronger contract (identity of `args`, `phase_slug` and type in one step), then revert.
    - No count regression on the phase surface: `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_publish_grounding_stage.py tests/unit/test_182_severity_codes.py tests/unit/test_publish_service.py tests/test_182_extraction_parity.py tests/test_182_grounding_bundle.py tests/test_revert_byte_identical.py tests/test_181_flip_on.py -q` reports at least 80 passed and 0 failed.
  </acceptance_criteria>
  <done>A three-offender definition is proven to produce three keyed verdicts through the real ⊆ walk, the short-circuit presentation is proven to still report one, and both degradation seams are repointed without losing coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored definition -> server ⊆ rule | `folder_scope` is the author-declared narrowing of a workflow's KB retrieval. The ⊆ check is the server-side guarantee that a phase cannot widen beyond the bound project subtree (D-07 / PROJ-02) — an author-supplied value crossing into an enforcement decision. |
| server verdict -> canvas badge | Phase 184 derives per-node validation status from this verdict list (VALID-03). A verdict the server omits is a node the canvas renders as clean. |
| rule source -> its two presentations | The ⊆ walk is security-reviewed (T-098-02, owner-scoped, cycle-guarded). Any second copy of it is a second thing to review and a place for the two to disagree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-38 | Spoofing (false-clean signal) | `grounding_verdicts` rule 1 multiplicity | mitigate | THE GAP BEING CLOSED. A definition with several out-of-subtree phases reported ONE finding, so Phase 184 would paint later offending nodes clean — a scope-governance rule presenting as satisfied when it is not. Every offending phase now emits its own keyed verdict, pinned by a three-offender end-to-end test driving the REAL ⊆ walk and falsified by reverting the loop to a first-element append. |
| T-182-39 | Tampering (rule divergence) | a second copy of the ⊆ walk | mitigate | The walk is moved, not duplicated: `scope.folder_scope_violations` is the ONE implementation and `assert_folder_scopes_subset` re-raises `violations[0]` rather than re-deriving anything. Grep criteria assert the message expression appears exactly once in `scope.py` and zero times in `grounding.py`, and that `resolve_project_subtree` is never called from `grounding.py`. |
| T-182-40 | Tampering (silent contract break on a security-relevant path) | `except ValueError` callers of `assert_folder_scopes_subset` | mitigate | The raised object is the collector's own `FolderScopeSubsetError`, so type, `str(exc)`, `args` and `phase_slug` are identical by construction rather than by careful copying. `workflow_kickoff`'s HTTP 400, `runs.py`'s Continue fallback, `harness_engine`'s resume fallback and `test_098_scope_governance`'s `match="is not a subset"` are all in this plan's verify command. |
| T-182-41 | Information Disclosure | verdict messages naming folder ids | accept | Unchanged from today: the message lists the offending folder ids the AUTHOR declared in their own definition, returned to that same author on a route that already 404s for anyone outside the canvas audience. No new identifier is introduced and no foreign-tenant id can enter the message, because `outside` is a subset of the author's own declared `folder_scope`. |
| T-182-42 | Spoofing (infrastructure error rendered as a rule finding) | the broad `except ValueError` in both grounding helpers | accept | Deliberately UNCHANGED and recorded. Round-2 review WR-03 shows a `pydantic.ValidationError` is a `ValueError` subclass in Pydantic v2 and would render as a false red `folder_scope` verdict. WR-03 is not in this round's operator-selected scope; narrowing the catch here would be unrequested scope creep and would break the documented always-HTTP-200 degradation. The decision is written into the source comment so it reads as a choice, and the residual is restated in this plan's SUMMARY. |
| T-182-43 | Denial of Service | walking every phase instead of stopping at the first | accept | The walk was already O(phases x scope-ids) against an in-memory `allowed` set; the change removes an early exit, not an added query. The subtree is resolved exactly ONCE per collector call (pinned by an acceptance criterion), so no additional DB round-trip is introduced. Workflows are 5-50 phases (ROADMAP Phase 191 sizing). |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs — no `requirements.txt` change, no new import in either source file. Nothing to audit. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_validate.py tests/test_182_extraction_parity.py tests/test_098_scope_governance.py tests/test_152_folder_override.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_103_nl_generate.py tests/unit/test_103_lint_block.py tests/unit/test_182_severity_codes.py -q` reports 0 failed.
- The full phase-182 surface stays at or above the measured baseline of 80 passed / 0 failed.
- `cd backend && venv/Scripts/python.exe -c "import app.main"` exits 0.
- Two falsification observations recorded in the SUMMARY (first-element-only append -> multiplicity test fails; re-raise vs re-synthesize note) with the exact observed messages.
- `git show --stat` on this plan's commits touches only the four files in `files_modified`. Stage by explicit path only — the working tree carries ~400 unrelated modifications under `.claude/` and 4 untracked files under `backend/`; never `git add -A` and never `git add .`.
- Do NOT create scratch `.py` files anywhere under `backend/` — uvicorn's `--reload` watcher wedges on Windows. Use the session scratchpad for any ad-hoc probe.
</verification>

<success_criteria>
- Every out-of-subtree phase produces its own keyed `folder_scope` verdict, proven end to end through the real ⊆ walk and falsified against a first-element-only implementation.
- Exactly one ⊆ walk exists in the codebase, in `scope.py`; `grounding.py` re-derives nothing.
- The raising presentation is byte-identical in type, message, `args` and `phase_slug`; every existing `except ValueError` caller and the NL-gen short-circuit dict are unaffected.
- The structural slug channel (`FolderScopeSubsetError.phase_slug` read via `getattr`) is preserved; no message parsing is introduced anywhere.
- The overstated module docstring is corrected to match the shipped behaviour.
- WR-03's catch breadth is deliberately unchanged and recorded as a decision, not silently altered.
- No migration, no new dependency, no frontend file.
</success_criteria>

<output>
Create `.planning/phases/182-server-validation-seam/182-10-SUMMARY.md` when done.
</output>
