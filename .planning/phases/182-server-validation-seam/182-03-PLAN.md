---
phase: 182-server-validation-seam
plan: 03
type: execute
wave: 3
depends_on: ["182-02"]
files_modified:
  - backend/app/api/canvas_canary.py
  - backend/app/main.py
  - backend/tests/test_revert_byte_identical.py
  - backend/tests/test_181_flip_on.py
autonomous: true
requirements: [VALID-01]

must_haves:
  truths:
    - "The temporary 181 `/canvas/ping` canary is fully removed — `backend/app/api/canvas_canary.py` is deleted and its `main.py` import + include are gone (D-182-04)."
    - "The D-182-04 retirement repoints BOTH test files — `test_revert_byte_identical.py` AND `test_181_flip_on.py` — off `/canvas/ping` onto the real routes (D-182-04, RESEARCH Open Q1)."
    - "The '404 when off' + pre-auth-404 assertions target `GET /workflows/grounding-bundle` (a clean GET, no body confound) plus a `POST /workflows/validate` probe with a MINIMAL VALID body so the only thing that can 404 is the flag (D-182-05, Pitfall 5)."
    - "`test_canvas_ping_200_after_flip_on` is repointed onto `GET /workflows/grounding-bundle` (200 when the flag is on, via the `authenticate_canvas_request` monkeypatch seam) (D-182-04)."
    - "The byte-identical 404 posture is proven on the REAL canvas routes for everyone incl. operators, and pre-auth for anonymous/bogus-token callers (D-182-05, D-182-06 red line)."
  artifacts:
    - path: "backend/app/main.py"
      provides: "canary import + include removed"
      contains: "app.include_router"
    - path: "backend/tests/test_revert_byte_identical.py"
      provides: "404-when-off + pre-auth-404 assertions repointed onto GET /grounding-bundle + POST /validate"
      contains: "workflows/grounding-bundle"
    - path: "backend/tests/test_181_flip_on.py"
      provides: "canary 404-when-off + 200-when-on assertions repointed onto GET /grounding-bundle"
      contains: "workflows/grounding-bundle"
  key_links:
    - from: "backend/tests/test_revert_byte_identical.py"
      to: "backend/app/api/workflows.py"
      via: "real-route 404-when-off probe"
      pattern: "workflows/(grounding-bundle|validate)"
    - from: "backend/tests/test_181_flip_on.py"
      to: "backend/app/api/workflows.py"
      via: "real-route 200-when-on probe"
      pattern: "workflows/grounding-bundle"
---

<objective>
Retire the throwaway Phase-181 `/canvas/ping` canary now that the real `require_canvas`-gated
routes exist (D-182-04). Delete `canvas_canary.py` + its `main.py` wiring, and repoint the
canary's revert/flip-on assertions onto the real `POST /workflows/validate` + `GET /workflows/grounding-bundle`
routes. The blast radius is WIDER than CONTEXT named: `test_181_flip_on.py` ALSO hits `/canvas/ping`
(its `test_canvas_ping_200_after_flip_on` would FAIL on delete), so BOTH test files must be
repointed (RESEARCH Open Q1). This keeps the "a canvas route 404s when off" acceptance gate
provably true on the real surface it now protects (SC#3).

Purpose: kill dead code a phase early and make the byte-identical-404 gate ride the real routes.
Output: canary deleted, `main.py` cleaned, both 181 test files repointed and green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-server-validation-seam/182-CONTEXT.md
@.planning/phases/182-server-validation-seam/182-RESEARCH.md
@.planning/phases/182-server-validation-seam/182-PATTERNS.md
@.planning/phases/182-server-validation-seam/182-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. From the codebase + 182-02 output. -->

Canary wiring to remove:
  backend/app/api/canvas_canary.py — the whole file (GET /canvas/ping behind require_canvas())
  backend/app/main.py:657 — import list ends `... , me_preferences, canvas_canary  # noqa: E402` → drop `canvas_canary`
  backend/app/main.py:688 — `app.include_router(canvas_canary.router)  # Phase 181 REVERT-01 ...` → delete the whole line

Real routes now available (from 182-02):
  GET  /workflows/grounding-bundle   (require_canvas alone; 200 when on, 404 when off; a clean GET — no body)
  POST /workflows/validate           (require_canvas alone; accepts a WorkflowDefinition body)

test_revert_byte_identical.py — the 3 tests + helper:
  _cold_off(monkeypatch)                         # :38-42 — reuse verbatim
  test_require_canvas_404s_when_off              # :71-92 — swap /canvas/ping → real routes
  test_require_canvas_404s_pre_auth_when_off     # :97-138 — CR-01 pre-auth path; keep the app.dependency_overrides.pop(get_current_user, None) at :121
  test_existing_governed_features_unchanged      # :143-168 — touches /features only, LEAVE UNCHANGED

test_181_flip_on.py — the 3 canary tests + helpers (leave the 5 /features tests unchanged):
  _cold_off / _flipped_on / _is_op_true / _is_op_false            # :24-49
  test_canvas_ping_404s_when_off_for_operator    # :54-61 — swap /canvas/ping → GET /workflows/grounding-bundle
  test_canvas_ping_404s_when_off_for_user        # :64-71 — swap /canvas/ping → GET /workflows/grounding-bundle
  test_canvas_ping_200_after_flip_on             # :74-95 — repoint onto GET /workflows/grounding-bundle; keep the authenticate_canvas_request monkeypatch seam (:89-92)

Minimal valid WorkflowDefinition body (for the POST /validate 404-when-off probe — avoids the 422-vs-404 race, Pitfall 5):
  build the smallest schema-valid definition (one phase, a business_requirement string) — mirror the builders in test_103_grounding_fidelity.py:23-42 / test_103_lint_block.py; the flag-off 404 fires from require_canvas BEFORE body parsing matters, but a VALID body guarantees the only 404 source is the gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the canary route + remove its `main.py` wiring</name>
  <files>backend/app/api/canvas_canary.py, backend/app/main.py</files>
  <read_first>
    - backend/app/api/canvas_canary.py (the file to delete — its docstring already anticipates 182 removal)
    - backend/app/main.py (line 657 import list + line 688 include — the two references to remove)
  </read_first>
  <action>
    Delete the file `backend/app/api/canvas_canary.py` entirely. In `backend/app/main.py`, remove `canvas_canary` from the `from app.api import ...` import list at line 657 (leave every other imported module intact), and delete the whole `app.include_router(canvas_canary.router)  # Phase 181 REVERT-01 ...` line at 688. No other line in `main.py` references the canary. Do NOT touch any other route include or import.
  </action>
  <verify>
    <automated>cd backend && test ! -f app/api/canvas_canary.py && ! grep -rn "canvas_canary" app/ && venv/Scripts/python -c "import app.main; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/api/canvas_canary.py` no longer exists.
    - `grep -rn "canvas_canary" backend/app/` returns nothing (import + include both gone).
    - `python -c "import app.main"` succeeds (the app still boots with the canary removed).
  </acceptance_criteria>
  <done>The temporary canary is fully retired from the app; the app imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint BOTH 181 test files onto the real routes</name>
  <files>backend/tests/test_revert_byte_identical.py, backend/tests/test_181_flip_on.py</files>
  <read_first>
    - backend/tests/test_revert_byte_identical.py (lines 38-138 — the `_cold_off` helper + the two 404 tests to repoint; the CR-01 pre-auth override-pop at :121)
    - backend/tests/test_181_flip_on.py (lines 24-95 — helpers + the 3 canary tests to repoint; the `authenticate_canvas_request` monkeypatch seam at :89-92)
    - backend/app/api/workflows.py (182-02 — the real GET /workflows/grounding-bundle + POST /workflows/validate routes now available)
  </read_first>
  <action>
    Repoint `test_revert_byte_identical.py`: in `test_require_canvas_404s_when_off`, replace `client.get("/canvas/ping")` with `client.get("/workflows/grounding-bundle")` (a clean GET, no body confound) for BOTH the operator and user cases, keeping the `assert status_code == 404` + `!= 403` checks; ADD a `client.post("/workflows/validate", json=<minimal valid WorkflowDefinition>)` probe asserting 404-when-off (a VALID body so the only possible 404 is the flag gate, not a 422 — Pitfall 5). In `test_require_canvas_404s_pre_auth_when_off`, repoint the anonymous + bogus-token probes onto `GET /workflows/grounding-bundle`, keeping the `app.dependency_overrides.pop(get_current_user, None)` pattern and the unbuilt-route parity assertion verbatim. Leave `test_existing_governed_features_unchanged` untouched (it only hits `/features`). Update the module docstring's `/canvas/ping` mention to the real routes.
    Repoint `test_181_flip_on.py`: in `test_canvas_ping_404s_when_off_for_operator` and `..._for_user`, swap `/canvas/ping` → `GET /workflows/grounding-bundle` (keep the assertions). In `test_canvas_ping_200_after_flip_on`, repoint onto `GET /workflows/grounding-bundle`, KEEP the `_flipped_on` + `is_operator` false + `authenticate_canvas_request` fake-caller monkeypatch seam (so the flag-on no-op is proven without a live token); since a real bundle read hits the DB, ALSO monkeypatch `assemble_grounding_bundle` (in `app.api.workflows` or `app.services.harness.grounding`) to return an empty `GroundingBundle` so the 200 is provable offline, and assert `resp.status_code == 200`. Leave the 5 `/features` tests (`:100-148`) untouched. Optionally rename the repointed test functions to drop the stale `canvas_ping` wording (keep behavior identical).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_revert_byte_identical.py tests/test_181_flip_on.py tests/test_181_off_audience.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_revert_byte_identical.py tests/test_181_flip_on.py tests/test_181_off_audience.py` exits 0.
    - `grep -c "canvas/ping" backend/tests/test_revert_byte_identical.py backend/tests/test_181_flip_on.py` == 0 (no stale canary references remain).
    - Both files reference `workflows/grounding-bundle`; `test_revert_byte_identical.py` also probes `workflows/validate` with a valid body for the 404-when-off case.
    - `test_canvas_ping_200_after_flip_on` (repointed) asserts 200 on `GET /workflows/grounding-bundle` when the flag is on.
  </acceptance_criteria>
  <done>Both 181 test files exercise the real routes; the byte-identical-404 gate now rides the real surface (SC#3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag-off → 404 | Removing the canary must not weaken the byte-identical-404 posture — the real routes now carry it, proven by the repointed tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-01 | Information Disclosure | route-existence leak when flag off | mitigate | The repointed `test_revert_byte_identical.py` + `test_181_flip_on.py` assert `require_canvas` returns a byte-identical 404 (never 403/401) on the REAL routes — for operator, user, and pre-auth callers (D-182-05). The gate grows WITH the surface it protects. |
| T-182-02 | Tampering | `POST /validate` 404-when-off probe | mitigate | The off-flag probe sends a MINIMAL VALID `WorkflowDefinition` body so a 422 can never race the 404 (Pitfall 5) — the only 404 source is the flag gate. `extra="forbid"` on the model is unchanged. |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase — deletion + test repoint only; no new dependency. |
</threat_model>

<verification>
- `pytest tests/test_revert_byte_identical.py tests/test_181_flip_on.py tests/test_181_off_audience.py -q` green.
- `python -c "import app.main"` succeeds; `grep -rn "canvas_canary" backend/app/` and `grep -c "canvas/ping" backend/tests/test_*.py` both empty.
- Full `backend` suite green before `/gsd:verify-work` (esp. the untouched NL-gen tests proving zero behavior change).
</verification>

<success_criteria>
- The `/canvas/ping` canary is fully removed (file + `main.py` wiring); the app boots.
- BOTH `test_revert_byte_identical.py` AND `test_181_flip_on.py` are repointed onto the real routes and green (D-182-04, RESEARCH Open Q1).
- The byte-identical-404 gate is proven on `POST /workflows/validate` + `GET /workflows/grounding-bundle` for operator, user, and pre-auth callers (SC#3).
</success_criteria>

<output>
Create `.planning/phases/182-server-validation-seam/182-03-SUMMARY.md` when done.
</output>
