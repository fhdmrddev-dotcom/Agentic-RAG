---
phase: 182-server-validation-seam
plan: 11
type: execute
wave: 3
depends_on: ["182-09", "182-10"]
files_modified:
  - backend/app/utils/folder_utils.py
  - backend/app/services/harness/grounding.py
  - backend/app/api/workflows.py
  - backend/app/services/harness/publish_service.py
  - backend/tests/unit/test_182_grounding_degradation.py
  - backend/tests/unit/test_182_severity_codes.py
  - backend/tests/unit/test_182_grounding_skill_org_gate.py
  - .planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md
autonomous: true
gap_closure: true
requirements: [VALID-01]

must_haves:
  truths:
    - "WR-01 closed (item 5 of the D-182-R2-03 scope): a transient skills-read failure no longer blocks publish with a FALSE `unregistered_skill` naming the author's CORRECT `skill_ref`. `_skill_registry`'s fail-closed exit used to sit INSIDE `assemble_grounding_bundle`, producing `skills = []` and a SUCCESSFUL bundle, so stage 2.6's wrapper never saw a failure and every `skill_ref` was reported unregistered. The degradation now travels on the bundle and both consumers report the honest `grounding_unavailable` — 'we could not CHECK' — instead of a factual accusation against a valid definition."
    - "WR-02 closed: `/validate` is SEALED for its two grounding I/O stages. A real PostgREST error from the palette read or the ⊆ walk used to escape as HTTP 500 while the SAME collector on the publish side returned a structured block; both sides now have the same failure posture, which is what the seam header's 'one shared copy' claim actually promises."
    - "WR-07 closed: the unbounded full-table `folders` read is now TRUNCATION-AWARE on the two gate call sites. A PostgREST `max-rows` cap used to silently return a prefix, which shrinks the resolved project subtree and produces a FALSE `folder_scope` violation — accusatory in exactly the wrong direction, and since 182-06 that false violation BLOCKS publish. A detected truncation now degrades to `grounding_unavailable` instead."
    - "The three fixes share ONE mechanism: `GroundingBundle.degraded` carries which registry could not be resolved, and both consumers branch on it identically. There is no second copy of the degradation decision and no second `grounding_unavailable` message."
    - "`grounding_unavailable` is now a code `/validate` can emit, so it is composed into `_KNOWN_CODES` and classifies as `error` — never the soft `incomplete`. 'We could not verify' must never paint as 'still building' (the WR-05 posture 182-07 established)."
    - "The seal is SCOPED and its boundary is recorded: this plan seals the two grounding I/O stages and gives them an honest verdict vocabulary. The remaining SEED-131 scope — the envelope-level design question (a top-level degraded flag versus a verdict code), the `@model_validator` 422s that bypass the envelope entirely (SEED-132), and a contract test enforcing always-200 across the whole handler — stays DEFERRED to Phase 184, and SEED-131 is updated to say exactly what shipped here."
    - "Structural verdicts still work when grounding is unavailable: `lint_workflow`, the D-13 business-requirement check and the interactive-phase check are pure and keep running, so a degraded read costs the author the grounding rules only, not the whole validation."
  artifacts:
    - path: "backend/app/utils/folder_utils.py"
      provides: "FolderReadTruncatedError + an opt-in strict, count-checked folders read; every existing caller byte-identical by default"
      contains: "FolderReadTruncatedError"
    - path: "backend/app/services/harness/grounding.py"
      provides: "GroundingBundle.degraded + honest skills/folders degradation + the ONE grounding_unavailable code and finding builder both consumers share"
      contains: "GROUNDING_UNAVAILABLE_CODE"
    - path: "backend/app/api/workflows.py"
      provides: "The sealed /validate grounding section emitting grounding_unavailable, and that code composed into the severity taxonomy as an error"
      contains: "GROUNDING_UNAVAILABLE_CODE"
    - path: "backend/app/services/harness/publish_service.py"
      provides: "Stage 2.6 branching on bundle.degraded through the shared finding builder instead of a local literal"
      contains: "grounding_unavailable_finding"
    - path: "backend/tests/unit/test_182_grounding_degradation.py"
      provides: "Failure-injection proof for all three findings: skills raise, folders truncate, collector raises — each must produce grounding_unavailable and never a false unregistered_* or folder_scope"
      min_lines: 200
  key_links:
    - from: "backend/app/services/harness/grounding.py (assemble_grounding_bundle)"
      to: "backend/app/utils/folder_utils.py (fetch_visible_folders strict=True)"
      via: "opt-in truncation-aware read whose failure marks the bundle degraded"
      pattern: "strict=True"
    - from: "backend/app/api/workflows.py (validate_workflow)"
      to: "backend/app/services/harness/grounding.py (grounding_unavailable_finding)"
      via: "the sealed grounding section emitting the shared honest finding"
      pattern: "grounding_unavailable_finding"
    - from: "backend/app/services/harness/publish_service.py (_grounding_fidelity_failures)"
      to: "backend/app/services/harness/grounding.py (GroundingBundle.degraded)"
      via: "stage 2.6 blocking on a degraded bundle instead of trusting an empty registry"
      pattern: "degraded"
---

<objective>
Close the fail-honesty trio from `182-REVIEW.md` (round 2) — WR-01, WR-02 and WR-07 — selected by
the operator as item 5 of the D-182-R2-03 scope. All three are the same defect wearing three
costumes: a transient infrastructure failure is rendered to the author as a FALSE, specific,
actionable-looking accusation against a definition that is actually correct.

WR-01. `grounding._skill_registry`'s fail-closed `except Exception: return []` sits INSIDE
`assemble_grounding_bundle`, so a PostgREST 5xx / timeout / reset on the skills read yields
`skills = []`, `skill_ids = set()`, and a bundle that returns SUCCESSFULLY. Stage 2.6's `try` never
sees an exception, so `_unregistered_skill_ref` reports EVERY `skill_ref` as unregistered and publish
blocks with `{"code": "unregistered_skill", "phase": "<slug>", "message": "phase '<slug>' references
a non-registered skill_ref '<uuid>'"}` — a factual accusation against a correct definition, and
`/validate` paints that node red with the same message, actively directing the author to "fix" a
valid reference. The honest answer already exists three lines away. The stage-2.6 docstring's claim
that the two sides share "the same fail-closed posture" is wrong and must be corrected too.

WR-02. A real `postgrest.exceptions.APIError` from either the palette read or the ⊆ walk escapes
`validate_workflow` as HTTP 500, while the identical failure on the publish side returns a structured
block. The reviewer proved both paths at HEAD. `APIError` is not a `ValueError`, so the ⊆ rule's
catch does not see it. The route that fires on every canvas edit is the unsealed one.

WR-07. `fetch_visible_folders` -> `fetch_all_folders(fields="*")` has no filter and no limit, and it
runs TWICE per `/validate` call (once for the palette, once inside the ⊆ walk). PostgREST caps
unbounded selects at `max-rows`; past the cap the read silently returns a prefix, the resolved
subtree shrinks, and `assert_folder_scopes_subset` reports a FALSE `folder_scope` violation — which,
post-182-06, BLOCKS publish.

Purpose: make every unresolvable-registry outcome say "we could not check" instead of "your
definition is wrong", identically on both sides of the shared collector.
Output: one degradation signal on the bundle, one shared honest finding, a sealed `/validate`
grounding section, a truncation-aware opt-in folders read, and failure-injection tests that FAIL
against today's code.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-server-validation-seam/182-CONTEXT.md
@.planning/phases/182-server-validation-seam/182-REVIEW.md
@.planning/phases/182-server-validation-seam/182-06-SUMMARY.md
@.planning/phases/182-server-validation-seam/182-07-SUMMARY.md
@.planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md

<interfaces>
<!-- Exact current contracts, verified by reading the source this planning session. -->

backend/app/utils/folder_utils.py
  line 8-24    async def fetch_all_folders(supabase, fields: str = "id, user_id, name, parent_id, is_org_shared, org_id") -> list[dict]
                   from app.utils.db import aexec
                   resp = await aexec(supabase.table("folders").select(fields))
                   return resp.data or []
               # NO filter, NO limit, NO count. Called by fetch_visible_folders(fields="*") and
               # get_globally_visible_folder_ids.
  line 27-50   async def _resolve_caller_org_ids(supabase, user_id) -> set[str]     # unguarded aexec
  line 119-129 async def fetch_visible_folders(supabase, user_id) -> list[dict]
                   caller_org_ids = await _resolve_caller_org_ids(supabase, user_id)
                   all_folders = await fetch_all_folders(supabase, fields="*")
                   ... owner OR is_in_global_subtree(...) ...
  Other callers of fetch_visible_folders (ALL must stay byte-identical — the new kwarg is
  keyword-only with a default, so they are unaffected by construction):
    app/api/folders.py:17,30,45,218 | app/api/kb.py:9 (aliased) | app/services/agent_loop.py:1209
    app/services/document_view_resolver.py:220 | app/services/harness/scope.py:99,187
    app/services/workflow_kickoff.py:376

postgrest 2.29.0 (confirmed in the venv):
  request_builder.select(*columns, count: Optional[CountMethod] = None, head=None)
  base_request_builder APIResponse carries `count: Optional[int]`, populated from the
  Content-Range header ONLY when a count method was requested.
  APIError is a plain Exception subclass — issubclass(APIError, ValueError) is False.

backend/app/services/harness/grounding.py
  line 79-98   @dataclass class GroundingBundle:
                   tools/list[str], tool_names/set[str], folders/list[dict], skills/list[dict],
                   skill_ids/set[str], placeholders/list[str]   — all `field(default_factory=...)`
  line 104-163 def _skill_registry(supabase, user_id, caller_org_ids) -> list[dict]   # SYNC by contract
                   caller = coerce_uid(user_id)                    # deliberately OUTSIDE the try
                   org_ids = {coerce_uid(o) for o in (caller_org_ids or set())}
                   try: rows = supabase.table("skills").select(...).or_(...).execute().data or []
                   except Exception:
                       logger.warning("grounding: org-gated skills read failed; using empty skill set", exc_info=True)
                       return []                                   # <-- WR-01: the swallow to remove
                   return [r for r in rows if r.get("is_enabled") and skill_row_visible(...)]
  line 253-306 async def assemble_grounding_bundle(*, supabase, pool=None, user_id, project_folder_id=None,
                                                   template_asset_id=None, template_placeholders=None)
                   folders = await fetch_visible_folders(supabase, user_id)          # WR-07 site
                   tool_names = {t["function"]["name"] for t in get_tools(None)}     # in-process
                   caller_org_ids = await _resolve_caller_org_ids(supabase, user_id)
                   skills = await run_in_threadpool(_skill_registry, supabase, user_id, caller_org_ids)
                   skill_ids = {str(s["id"]) for s in skills}
                   placeholders = await _resolve_template_placeholders(...)
                   return GroundingBundle(...)
  line 358-364 GROUNDING_VERDICT_CODES = frozenset({"folder_scope","unregistered_skill","unregistered_tool"})
               # The DELIBERATELY-NOT-INCLUDED paragraph above it names grounding_unavailable as
               # publish-only. That paragraph becomes FALSE in this plan and must be rewritten.

backend/app/api/workflows.py
  line 362-427 the severity taxonomy block: _ROUTE_ASSIGNED_CODES, _INCOMPLETE_CODES,
               _DUAL_SOURCE_CODES, _KNOWN_CODES = LINT_CODES | grounding.GROUNDING_VERDICT_CODES
               | _ROUTE_ASSIGNED_CODES, and the DERIVED
               _ERROR_CODES = _KNOWN_CODES - _INCOMPLETE_CODES - _DUAL_SOURCE_CODES.
               The comment at 383-390 says grounding_unavailable is publish-only and "cannot reach
               this classifier" — that becomes FALSE in this plan.
  line 503-531 the two grounding I/O stages to seal:
                   bundle = await grounding.assemble_grounding_bundle(supabase=..., user_id=...)
                   findings.extend({"code": e.code, ...} for e in lint_workflow(body))   # PURE
                   findings.extend(await grounding.grounding_verdicts(body, supabase=..., user_id=...,
                                       tool_names=bundle.tool_names, skill_ids=bundle.skill_ids))
               NOTE: `bundle` is assembled BEFORE lint today. The seal must keep lint, the D-13
               business-requirement check and the interactive-phase check running even when
               grounding is unavailable — they are pure and need no registry.
  line 496-501 the handler docstring's "ALWAYS HTTP 200" promise — the thing WR-02 says nothing enforces.

backend/app/services/harness/publish_service.py
  line 499-522 async def _resolve_publish_supabase(supabase, *, definition_id, pool)     # returns a client
  line 525-589 async def _grounding_fidelity_failures(definition, *, definition_id, user_id, pool, supabase)
                   docstring claims "the same fail-closed posture grounding._skill_registry already
                   takes on its own read (CR-01)" — FALSE per WR-01; correct it.
                   try: resolved = await _resolve_publish_supabase(...)
                        bundle = await assemble_grounding_bundle(supabase=resolved, user_id=str(user_id))
                        return await grounding_verdicts(definition, supabase=resolved, user_id=str(user_id),
                                                        tool_names=bundle.tool_names, skill_ids=bundle.skill_ids)
                   except Exception:
                        logger.exception(...); return [{"code": "grounding_unavailable", "phase": None,
                                                        "message": "the grounding registry could not be
                                                        resolved, so grounding fidelity could not be
                                                        verified — publish is blocked"}]
  line 199-225 stage 2.6's call site, before the golden run at :239.

Tests that MUST be updated by this plan (each currently pins behaviour this plan deliberately changes):
  tests/unit/test_182_grounding_skill_org_gate.py:214-220
      asserts `_skill_registry(_Boom(), _CALLER, {_ORG_MINE}) == []` — the swallow being removed.
      It must become an assertion that the read RAISES, plus a sibling asserting that
      assemble_grounding_bundle converts that raise into a degraded bundle (never a silent []).
      Do NOT delete the test — convert it, so coverage does not shrink (the Phase-177 lesson).
  tests/unit/test_182_severity_codes.py
      :244-300  test_known_codes_compose_from_the_owning_modules_with_no_orphan asserts
                `set(_KNOWN_CODES) == owned | set(_ROUTE_ASSIGNED_CODES)` and that `_ERROR_CODES`
                equals an exact 6-literal set. Both must gain the new code.
      :351-383  test_publish_only_codes_are_an_acknowledged_boundary asserts
                `publish_codes - set(_KNOWN_CODES) == {"grounding_unavailable"}`. After this plan
                the boundary set is EMPTY (both sides emit it and /validate classifies it). Rewrite
                the test to assert emptiness AND to explain the change, keeping the two
                `_severity("grounding_unavailable", ...) == "error"` assertions.
      The grounding drift scanner is `re.findall(r'"code":\s*"([a-z_]+)"', grounding_src)` and it
      strips whole-line comments only, NOT docstrings (IN-07). CONSEQUENCE: never write the literal
      token sequence `"code": "grounding_unavailable"` anywhere in grounding.py — not in code, not
      in a docstring, not in an inline comment — or the drift detector will demand it be added to
      GROUNDING_VERDICT_CODES, which would be wrong (grounding_verdicts does not emit it). Build the
      dict from the CONSTANT instead, and add an explicit test linking the constant to _KNOWN_CODES
      so the scanner's blind spot is covered by an assertion rather than left as a loophole.

Known-good baseline, re-measured this session: 80 passed / 0 failed across
  tests/unit/test_182_validate.py tests/unit/test_182_folder_scope_keying.py
  tests/unit/test_182_publish_grounding_stage.py tests/unit/test_182_severity_codes.py
  tests/unit/test_publish_service.py tests/test_182_extraction_parity.py
  tests/test_182_grounding_bundle.py tests/test_revert_byte_identical.py tests/test_181_flip_on.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One degradation signal — a truncation-aware opt-in folders read and an honest bundle</name>
  <files>backend/app/utils/folder_utils.py, backend/app/services/harness/grounding.py</files>
  <read_first>
    - backend/app/utils/folder_utils.py (the whole file — `fetch_all_folders`, `_resolve_caller_org_ids`, `is_in_global_subtree`, `fetch_visible_folders`, `get_globally_visible_folder_ids`; note every call shape before adding a keyword)
    - backend/app/utils/db.py (`aexec` — how the blocking builder is awaited, so the count-bearing response object is understood)
    - backend/app/services/harness/grounding.py lines 35-53 (the module docstring's blocking-I/O and org-scoping contracts), lines 79-99 (`GroundingBundle`), lines 104-164 (`_skill_registry` including the deliberately-outside-the-try `coerce_uid` paragraph), lines 253-307 (`assemble_grounding_bundle`), lines 342-365 (`GROUNDING_VERDICT_CODES` and the now-false publish-only paragraph)
    - backend/tests/unit/test_182_grounding_skill_org_gate.py lines 200-235 (the `_Boom` fail-closed test being converted, and the `coerce_uid` raise test that must keep passing)
    - .planning/phases/182-server-validation-seam/182-REVIEW.md sections "WR-01" and "WR-07" (the evidence, the suggested `degraded` field, and the truncation chain folder_map -> is_in_global_subtree -> resolve_project_subtree -> false folder_scope)
  </read_first>
  <behavior>
    - `fetch_all_folders(supabase, fields="*")` with no `strict` argument issues exactly the query it issues today — no count header, no extra round trip, same returned rows.
    - `fetch_all_folders(..., strict=True)` requests an exact count; when the returned row count is less than the reported total it logs a loud warning naming both numbers and raises `FolderReadTruncatedError`.
    - `fetch_all_folders(..., strict=True)` against a response whose `count` is absent or not an integer (a mock, or a server that did not honour the header) returns normally — a missing count is never treated as a truncation.
    - `assemble_grounding_bundle` returns a bundle whose `degraded` is empty on the happy path.
    - When the skills read raises, the bundle returns with `skills == []`, `skill_ids == set()` and `degraded == {"skills"}` — and the same warning is still logged.
    - When the folders read raises or truncates, the bundle returns with `folders == []` and `"folders" in degraded`.
    - Nothing in `assemble_grounding_bundle` raises to its caller for either degradation — the bundle is always returned.
  </behavior>
  <action>
    In `backend/app/utils/folder_utils.py`:

    Add a module-level `class FolderReadTruncatedError(RuntimeError)` with a docstring explaining
    WR-07: PostgREST caps an unbounded select at `max-rows` (Supabase's Data API default is 1000
    rows) and silently returns a prefix, so a caller that GATES on the folder tree — the grounding
    palette and the project-subtree ⊆ walk — would compute a shrunken subtree and accuse a correct
    definition of a `folder_scope` violation, which since 182-06 blocks publish. Deliberately a
    `RuntimeError`, NOT a `ValueError`: the ⊆ rule's `except ValueError` must NOT mistake an
    infrastructure truncation for a rule finding — it has to reach the caller that knows how to say
    "we could not check". State that explicitly in the docstring.

    Add a keyword-only `strict: bool = False` parameter to `fetch_all_folders`. When it is False the
    function must behave EXACTLY as today — same `.select(fields)` call, no `count` argument, no
    extra COUNT query. That default is load-bearing: this helper is on the chat agent-loop path
    (`agent_loop.py:1209`) and on four `/folders` routes, and an unconditional `count="exact"` would
    add a COUNT(*) to every one of them. When `strict` is True, pass `count="exact"` to `.select`,
    read the total from the response's `count` attribute via `getattr(resp, "count", None)`, and
    only when that value `isinstance(..., int)` and exceeds `len(rows)` log a warning naming the
    returned count and the total and raise `FolderReadTruncatedError`. The `isinstance` guard is
    required, not defensive noise: conftest's supabase is a `MagicMock`, so an unguarded truthiness
    check on a mock attribute would raise on every test.

    Add the same keyword-only `strict: bool = False` to `fetch_visible_folders` and thread it into
    its `fetch_all_folders` call. Do not change its positional signature, its return type, or the
    visibility rule — several tests drive it positionally and `scope.py` patches it as a module
    global. Leave `get_globally_visible_folder_ids` and `_resolve_caller_org_ids` alone.

    In `backend/app/services/harness/grounding.py`:

    Add `degraded: frozenset[str] = frozenset()` as a field on `GroundingBundle`, documented as the
    set of registry names that could NOT be resolved for this bundle — currently `"folders"` and/or
    `"skills"`. State the contract both consumers rely on: a non-empty `degraded` means the fidelity
    rules MUST NOT be run and MUST NOT be reported, because an unresolved registry makes every
    membership test vacuously false, which reads to the author as a specific accusation against a
    correct definition (WR-01). Note that a dataclass field with a default must be declared after
    the existing defaulted fields.

    Remove the `except Exception: ... return []` swallow from `_skill_registry` so a read failure
    propagates to its async caller. Keep the `coerce_uid` calls exactly where they are. Rewrite the
    fail-closed paragraph in its docstring: the fail-closed DECISION has not been abandoned, it has
    MOVED to the caller that can describe it honestly — `assemble_grounding_bundle` still yields no
    skill grounding, but it now also records the degradation so the consumer says "could not check"
    rather than "this skill does not exist". Note the consequence for the identity path too: a
    malformed caller id raised by `coerce_uid` now also surfaces as a degradation rather than
    propagating as an unhandled error, which is strictly better on a route documented ALWAYS-200.

    In `assemble_grounding_bundle`, build a local mutable degradation set and:
    (a) wrap the `fetch_visible_folders(supabase, user_id, strict=True)` call in `try/except
    Exception`; on failure log a warning naming the folders read and set `folders = []` plus add
    `"folders"` to the set;
    (b) wrap the `_resolve_caller_org_ids` + `run_in_threadpool(_skill_registry, ...)` pair in
    `try/except Exception`; on failure log the SAME warning text `_skill_registry` used to log
    ("grounding: org-gated skills read failed; using empty skill set", `exc_info=True`) so the
    operational signal is unchanged, and set `skills = []` plus add `"skills"` to the set;
    (c) pass `degraded=frozenset(...)` into the returned `GroundingBundle`.
    Leave `tool_names` (in-process registry) and `_resolve_template_placeholders` (already
    soft-failing by design, D-103-3) exactly as they are.

    Add a comment above the folders call recording WHY `strict=True` here covers the ⊆ walk too:
    `assemble_grounding_bundle` runs FIRST on both `/validate` and publish stage 2.6, and it performs
    the SAME `fetch_visible_folders` read that `resolve_project_subtree` will later perform against
    the same table under the same cap — so a truncation is detected before the ⊆ walk is ever
    reached, and the consumer skips the fidelity rules entirely. Record the bounded residual
    honestly in the same comment: the two reads are separate round-trips, so a truncation that
    appears only on the second one is not caught by this check.

    Publish `GROUNDING_UNAVAILABLE_CODE: str = "grounding_unavailable"` and a single builder
    `grounding_unavailable_finding(degraded: frozenset[str] | None = None) -> dict` returning the
    `{code, phase, message}` finding both consumers emit, with `phase` None and a message that names
    the unresolved registries when they are known and otherwise falls back to the wording
    `publish_service` uses today. Build the dict from the CONSTANT, never from a quoted literal —
    `test_182_severity_codes.py`'s drift scanner matches the token sequence `"code": "<name>"` in
    this file's source (docstrings included) and would demand the code join
    `GROUNDING_VERDICT_CODES`, which would be wrong because `grounding_verdicts` does not emit it.
    Say that in a comment at the builder. Then rewrite the DELIBERATELY-NOT-INCLUDED paragraph above
    `GROUNDING_VERDICT_CODES`: the code is no longer publish-only, it is minted by BOTH consumers
    from this shared builder and is composed into `/validate`'s known set, while
    `GROUNDING_VERDICT_CODES` remains exactly the three codes `grounding_verdicts` itself emits.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_165_folder_utils_org_scope.py tests/integration/test_folders.py tests/test_182_extraction_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_165_folder_utils_org_scope.py tests/test_182_extraction_parity.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_103_nl_generate.py -q` reports 0 failed.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_folders.py tests/integration/test_kb.py -q` — record the pass/fail counts BEFORE the edit and after, in the SUMMARY. The after-count must be no worse than the before-count (these suites sit inside the known ~200-failure pre-existing rot; this plan must not add to it).
    - The default path is untouched: `grep -n "count=\"exact\"" backend/app/utils/folder_utils.py` shows the count argument only inside a `strict`-guarded branch, and `grep -c "strict: bool = False" backend/app/utils/folder_utils.py` returns 2.
    - `grep -c "class FolderReadTruncatedError(RuntimeError)" backend/app/utils/folder_utils.py` returns 1 — and NOT `(ValueError)`, so the ⊆ rule's catch cannot mistake it for a rule finding.
    - `grep -c "degraded" backend/app/services/harness/grounding.py` returns >= 4 — the dataclass field, both degradation branches, and the constructor argument.
    - The swallow is gone: within `_skill_registry`, `grep -c "return \[\]" backend/app/services/harness/grounding.py` no longer counts an occurrence inside that function (record the before/after locations in the SUMMARY).
    - `grep -c "GROUNDING_UNAVAILABLE_CODE" backend/app/services/harness/grounding.py` returns >= 2 (declaration plus use in the builder).
    - The drift-scanner trap is avoided: `grep -c '"code": "grounding_unavailable"' backend/app/services/harness/grounding.py` returns 0.
    - `cd backend && venv/Scripts/python.exe -c "import app.main"` exits 0.
  </acceptance_criteria>
  <done>The bundle carries an honest degradation signal, the folders read can detect a truncation without changing any other caller, and the skills swallow has moved to the one place that can describe it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Both consumers report "could not check" — seal /validate and branch publish stage 2.6</name>
  <files>backend/app/api/workflows.py, backend/app/services/harness/publish_service.py</files>
  <read_first>
    - backend/app/api/workflows.py lines 234-272 (the seam header's per-check parity table — the "one shared copy" claim WR-02 says has opposite failure postures), lines 362-427 (the severity taxonomy block, especially the now-false publish-only paragraph at 383-390 and the DERIVED `_ERROR_CODES`), lines 469-566 (`validate_workflow` in full, including the ALWAYS-HTTP-200 docstring promise and the four aggregation stages in order)
    - backend/app/services/harness/publish_service.py lines 199-226 (stage 2.6's call site and its ordering rationale) and lines 499-590 (`_resolve_publish_supabase` and `_grounding_fidelity_failures`, including the false "same fail-closed posture" claim)
    - backend/app/services/harness/grounding.py (as rewritten in Task 1 — `GroundingBundle.degraded`, `GROUNDING_UNAVAILABLE_CODE`, `grounding_unavailable_finding`)
    - backend/tests/unit/test_182_publish_grounding_stage.py (the whole file — it patches `_resolve_publish_supabase` and drives stage 2.6; the new degraded branch must not break its existing agreement test)
    - .planning/phases/182-server-validation-seam/182-REVIEW.md sections "WR-01" and "WR-02" (specifically the proven escape paths: palette APIError and subset APIError both reaching FastAPI as 500)
    - .planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md (the full seed — this task narrows it, so the boundary between what ships here and what stays deferred must be stated precisely)
  </read_first>
  <behavior>
    - `/validate` with a bundle whose `degraded` is non-empty returns HTTP 200 with a single `grounding_unavailable` verdict at severity `error`, and NO `unregistered_tool`, `unregistered_skill` or `folder_scope` verdict.
    - `/validate` when `assemble_grounding_bundle` raises returns HTTP 200 with the same single `grounding_unavailable` verdict — not a 500.
    - `/validate` when `grounding_verdicts` raises (for example a `postgrest` `APIError` off the ⊆ walk) returns HTTP 200 with the same verdict — not a 500.
    - In all three cases the structural verdicts still appear: a definition that is also structurally broken still reports its lint codes, its missing business requirement and its interactive-phase findings.
    - `_severity("grounding_unavailable", phases_empty=False)` and `(..., phases_empty=True)` both return `"error"`.
    - Publish stage 2.6 with a degraded bundle blocks at `blocked_stage="grounding_fidelity"` with a single `grounding_unavailable` named failure and never with `unregistered_skill`.
    - Publish stage 2.6 on the happy path is completely unchanged, including the existing `/validate`-versus-publish agreement test.
  </behavior>
  <action>
    In `backend/app/api/workflows.py`:

    Compose the new code into the taxonomy. Add a `_DEGRADED_CODES: frozenset[str]` bucket holding
    `grounding.GROUNDING_UNAVAILABLE_CODE`, and extend `_KNOWN_CODES` to be the union of
    `LINT_CODES`, `grounding.GROUNDING_VERDICT_CODES`, `_ROUTE_ASSIGNED_CODES` and `_DEGRADED_CODES`.
    Leave `_ERROR_CODES` derived exactly as it is — subtracting `_INCOMPLETE_CODES` and
    `_DUAL_SOURCE_CODES` from the widened `_KNOWN_CODES` puts the new code in the error bucket
    automatically, which is the required classification: "we could not verify" must never paint the
    soft `incomplete`. Do NOT add it to `_INCOMPLETE_CODES`.

    Replace the DELIBERATELY-NOT-INCLUDED / PUBLISH-ONLY paragraph in the taxonomy comment. Its
    claim that the code "cannot reach this classifier" is now false. State the new truth: both
    consumers of the shared collector mint it from `grounding.grounding_unavailable_finding`, it is
    an infrastructure-honesty code rather than a rule finding, it is classified `error` because an
    unverifiable definition must not look publishable, and its string lives in `grounding.py` so
    neither consumer carries a literal.

    Seal the grounding section of `validate_workflow`. Wrap BOTH grounding I/O stages — the
    `assemble_grounding_bundle` call and the `grounding_verdicts` call — in one `try/except
    Exception`. Inside the try, after the bundle resolves, branch on `bundle.degraded`: when it is
    non-empty, append `grounding.grounding_unavailable_finding(bundle.degraded)` and SKIP the
    fidelity collector entirely. When it is empty, run the collector as today. In the `except`, log
    a warning with `exc_info=True` saying grounding could not be resolved and that structural
    verdicts are still being returned, then append `grounding.grounding_unavailable_finding()`.

    Restructure so the PURE checks are unaffected by a grounding failure: `lint_workflow`, the D-13
    `business_requirement_missing` check and `publish_service._interactive_phase_failures` need no
    registry, so they must run outside the sealed block and still contribute their findings. Keep
    their relative order in the `findings` list stable with respect to today wherever the grounding
    stage succeeds — `tests/unit/test_182_validate.py` asserts on codes and phases rather than list
    order, but do not gratuitously reorder. Add a comment stating why the pure checks stay outside
    the seal: a registry blip should cost the author the three grounding rules, not the whole
    validation.

    Update the handler docstring: the ALWAYS-HTTP-200 promise is now ENFORCED for the grounding
    stages specifically, name `grounding_unavailable` as the honest degraded outcome, and record
    the remaining boundary explicitly — the envelope-level design question (a top-level degraded
    flag or a third severity instead of a verdict code) and the `@model_validator` 422s that bypass
    the envelope before the handler is even entered are SEED-131 / SEED-132 and stay deferred to
    Phase 184.

    Update the seam header's parity table (lines 234-272) to add a row or a sentence recording that
    both sides now share the same FAILURE posture as well as the same rules — that is the specific
    claim WR-02 showed was false.

    In `backend/app/services/harness/publish_service.py`:

    In `_grounding_fidelity_failures`, after the bundle resolves inside the existing `try`, branch on
    `bundle.degraded`: when non-empty, return a single-element list containing
    `grounding_unavailable_finding(bundle.degraded)` WITHOUT calling `grounding_verdicts`. Replace
    the local literal dict in the `except` block with a call to the same shared builder so the code
    string and the message exist in exactly one place. Import the builder alongside the two existing
    function-local grounding imports.

    Correct the docstring's false claim. It currently says this stage takes "the same fail-closed
    posture `grounding._skill_registry` already takes on its own read (CR-01)". Replace it with the
    accurate description: `_skill_registry`'s swallow used to be INVISIBLE to this wrapper, so a
    transient skills-read failure produced an empty registry and a bundle that returned
    successfully, and this stage then blocked publish with a false `unregistered_skill` naming the
    author's valid `skill_ref` (WR-01). The degradation now travels on `bundle.degraded` and this
    stage reports `grounding_unavailable` instead. Keep the surrounding fail-closed rationale — a
    publish that cannot verify grounding must not mint a version — and keep the never-raise contract.

    Do not change stage 2.6's position relative to the golden run, do not change `_block`'s
    arguments, and do not change `_resolve_publish_supabase` in this plan (its org-scoping is plan
    182-12's subject).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py tests/unit/test_182_publish_grounding_stage.py tests/unit/test_publish_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py tests/unit/test_182_publish_grounding_stage.py tests/unit/test_publish_service.py tests/unit/test_182_folder_scope_keying.py -q` reports 0 failed.
    - `grep -c "_DEGRADED_CODES" backend/app/api/workflows.py` returns >= 2 and the `_KNOWN_CODES` assignment references it.
    - `_ERROR_CODES` is still derived, with no verdict-code string literal on its assignment line.
    - Neither consumer carries the code literal: `grep -c '"grounding_unavailable"' backend/app/api/workflows.py` returns 0 and `grep -c '"code": "grounding_unavailable"' backend/app/services/harness/publish_service.py` returns 0.
    - Both consumers use the shared builder: `grep -c "grounding_unavailable_finding" backend/app/api/workflows.py` returns >= 1 and `grep -c "grounding_unavailable_finding" backend/app/services/harness/publish_service.py` returns >= 2 (the degraded branch and the except branch).
    - The pure checks are outside the seal: in `validate_workflow`, the `lint_workflow` call, the `business_requirement_missing` call and the `_interactive_phase_failures` call are NOT inside the `try` block that wraps the grounding stages (verify by reading the final function; record the resulting structure in the SUMMARY).
    - The false parity claim is gone: `grep -c "the same fail-closed posture" backend/app/services/harness/publish_service.py` returns 0.
    - `cd backend && venv/Scripts/python.exe -c "import app.main"` exits 0.
  </acceptance_criteria>
  <done>Both callers of the shared collector have the same failure posture, `/validate` cannot 500 on a grounding read, and a degraded registry is reported as "could not check" rather than as a false accusation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Failure-injection proof for all three findings, converted coverage, and the SEED-131 narrowing</name>
  <files>backend/tests/unit/test_182_grounding_degradation.py, backend/tests/unit/test_182_severity_codes.py, backend/tests/unit/test_182_grounding_skill_org_gate.py, .planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md</files>
  <read_first>
    - backend/tests/unit/test_182_validate.py lines 1-115 (the module docstring's conventions, `_definition` / `_llm_agent` / `_llm_single` builders, `_patch_bundle`, and the direct-handler-call `_validate` helper this new file reuses in shape)
    - backend/tests/unit/test_182_publish_grounding_stage.py (the whole file — how stage 2.6 is driven with `_resolve_publish_supabase` patched, and the existing `/validate`-versus-publish agreement test that must stay green)
    - backend/tests/unit/test_182_grounding_skill_org_gate.py lines 195-235 (the `_Boom` fail-closed test to CONVERT and the `coerce_uid` raise test to preserve)
    - backend/tests/unit/test_182_severity_codes.py lines 244-300 and 351-383 (the composition/partition test and the publish-only boundary test — both change)
    - backend/app/services/harness/grounding.py, backend/app/api/workflows.py, backend/app/services/harness/publish_service.py, backend/app/utils/folder_utils.py (all as rewritten in Tasks 1 and 2)
    - .planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md (the whole seed, including its `confirmed` list which names the exact unguarded call sites — several of which this plan now guards)
  </read_first>
  <behavior>
    - Injecting a raising skills read produces a bundle with `degraded == {"skills"}` and no exception.
    - Driving `/validate` with that bundle yields exactly one `grounding_unavailable` verdict at severity `error` and ZERO `unregistered_skill` verdicts, even for a definition whose phases carry real `skill_ref` values.
    - Driving publish stage 2.6 with that bundle blocks at `grounding_fidelity` with `grounding_unavailable` and never with `unregistered_skill`.
    - A truncated folders read (row count below the reported total) raises `FolderReadTruncatedError` from the strict read, degrades the bundle, and yields `grounding_unavailable` rather than a false `folder_scope`.
    - A `postgrest`-style `APIError` raised from `grounding_verdicts` yields HTTP 200 with `grounding_unavailable`, not a 500 — the direct WR-02 reproduction.
    - A structurally broken AND grounding-degraded definition reports both its structural verdicts and the `grounding_unavailable` verdict.
    - `grounding.GROUNDING_UNAVAILABLE_CODE` is in `workflows._KNOWN_CODES` and classifies `error` in both `phases_empty` states.
  </behavior>
  <action>
    Create `backend/tests/unit/test_182_grounding_degradation.py`. Module docstring: name the phase,
    VALID-01, and the three findings WR-01 / WR-02 / WR-07; state the shared defect in one sentence
    (a transient infrastructure failure rendered as a false, specific accusation against a correct
    definition) and state that every test here FAILS against the pre-fix code, naming the pre-fix
    outcome for each — a false `unregistered_skill` naming the author's real `skill_ref`, an HTTP
    500, and a false `folder_scope` block. Follow repo conventions: imports inside test bodies,
    `pytest.mark.asyncio` where needed, and the direct-handler-call posture from
    `test_182_validate.py` so the suite stays offline.

    Write these tests.

    (1) WR-01 at the bundle. Monkeypatch `grounding._skill_registry` to raise, drive the REAL
    `assemble_grounding_bundle` with a fake supabase and a patched `fetch_visible_folders`, and
    assert `bundle.degraded == frozenset({"skills"})`, `bundle.skills == []`, `bundle.skill_ids ==
    set()`, and that no exception escaped.

    (2) WR-01 at `/validate`. With that degraded bundle patched in, drive the handler with a
    definition whose phase declares a real `skill_ref`, and assert the verdict codes contain
    `grounding_unavailable` and do NOT contain `unregistered_skill`. The failure message must quote
    the pre-fix behaviour: the author was told their valid `skill_ref` does not exist.

    (3) WR-01 at publish. Drive stage 2.6 with the same degraded bundle (patching
    `_resolve_publish_supabase` exactly as `test_182_publish_grounding_stage.py` does) and assert
    the returned named failures are exactly one `grounding_unavailable` and contain no
    `unregistered_skill`.

    (4) WR-07 truncation, at the read. Build a fake supabase whose folders select returns N rows
    while reporting a total greater than N, call `fetch_all_folders(..., strict=True)`, and assert
    `FolderReadTruncatedError` is raised. Add the negative control: the same fake with a count EQUAL
    to the row count returns normally, and a fake whose `count` is None or a non-integer also returns
    normally. Add the byte-identity control: the same fake called WITHOUT `strict` returns the rows
    and never raises, proving no other caller's behaviour changed.

    (5) WR-07 end to end. With the truncating read wired under `assemble_grounding_bundle`, assert
    the bundle is degraded on `"folders"` and that `/validate` reports `grounding_unavailable` and
    NOT `folder_scope`, for a bound definition whose `folder_scope` would have looked out-of-subtree
    against the truncated tree.

    (6) WR-02 direct reproduction. Define a local exception class that is NOT a `ValueError`
    (mirroring `postgrest.exceptions.APIError`'s shape — assert `issubclass(..., ValueError) is
    False` inside the test so the premise is pinned), make `grounding.grounding_verdicts` raise it,
    and assert the handler returns normally with a `grounding_unavailable` verdict rather than
    propagating. Add a sibling asserting the same when `assemble_grounding_bundle` itself raises.

    (7) Structural verdicts survive. With grounding degraded, drive a definition that is ALSO
    structurally broken (an unreachable terminal or a missing `business_requirement`) and assert
    both the structural verdict codes and `grounding_unavailable` are present, and that `ok` is
    False.

    (8) Severity and composition. Assert `grounding.GROUNDING_UNAVAILABLE_CODE in
    workflows._KNOWN_CODES`, and that `workflows._severity(grounding.GROUNDING_UNAVAILABLE_CODE,
    phases_empty=False)` and `(..., phases_empty=True)` both return `"error"`. Comment that this
    assertion exists specifically because the drift scanner in `test_182_severity_codes.py` cannot
    see this code (it is built from a constant, not a quoted literal), so the link between the
    published constant and the classifier is pinned HERE rather than left to the scanner.

    In `backend/tests/unit/test_182_grounding_skill_org_gate.py`: CONVERT the `_Boom` test rather
    than deleting it. Its subject changes from "a read failure returns `[]`" to "a read failure
    RAISES so the caller can describe it" — assert the raise, and add a sibling asserting that
    `assemble_grounding_bundle` turns that raise into a degraded bundle with an empty skill set,
    so the fail-closed OUTCOME is still proven and coverage does not shrink. Update the test's
    docstring to name WR-01 and explain that the fail-closed decision moved rather than disappeared.
    Leave the `coerce_uid` raise test and every org-gate assertion untouched.

    In `backend/tests/unit/test_182_severity_codes.py`: update the two tests the taxonomy change
    invalidates. In `test_known_codes_compose_from_the_owning_modules_with_no_orphan`, extend the
    composition assertion to include `_DEGRADED_CODES` and add the new code to the exact
    `_ERROR_CODES` literal set, keeping the partition assertions intact and adding a comment that
    the code is an infrastructure-honesty code rather than a rule finding, which is why it is
    classified `error` and not `incomplete`. In
    `test_publish_only_codes_are_an_acknowledged_boundary`, the acknowledged set is now EMPTY —
    rewrite it to assert `publish_codes - set(workflows._KNOWN_CODES) == set()`, keep both
    `_severity(...) == "error"` assertions, and rewrite the docstring to record the change: publish
    no longer mints any code `/validate` cannot classify, because the two sides now share the same
    honest degraded finding. Keep the test's forcing function intact — a future publish-only code
    must still fail here and force the author to decide its home.

    In `.planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md`: do NOT close the seed.
    Add a dated "Partially addressed in Phase 182 gap closure (plan 182-11)" section recording
    precisely what shipped — the two grounding I/O stages of `/validate` are sealed, the honest
    `grounding_unavailable` code exists and classifies `error`, the folders read is truncation-aware
    at the two gate call sites, and publish and `/validate` now share one failure posture — and
    precisely what remains for Phase 184: the envelope-level design question the seed raises (a
    top-level degraded marker or a third severity versus a verdict code), the `@model_validator`
    422s that bypass the envelope before the handler runs (SEED-132), any other unguarded read on
    the handler, and a contract test that enforces always-200 across the whole handler rather than
    across the grounding stages only. Update the `confirmed` entries that this plan falsifies (the
    ones naming `fetch_visible_folders` and `resolve_project_subtree` as unguarded on the `/validate`
    path) so a future reader is not chasing a fixed condition, and keep the seed `status: open`
    with its `re_open_triggers` intact.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_grounding_degradation.py tests/unit/test_182_severity_codes.py tests/unit/test_182_grounding_skill_org_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_grounding_degradation.py tests/unit/test_182_severity_codes.py tests/unit/test_182_grounding_skill_org_gate.py -q` exits 0.
    - `grep -c "^\s*\(async \)\?def test_" backend/tests/unit/test_182_grounding_degradation.py` returns >= 9 — the eight behaviours plus the truncation negative controls.
    - The WR-02 premise is pinned, not assumed: the file contains an assertion that the injected exception class is NOT a `ValueError` subclass.
    - The false-accusation assertions are explicit negatives, not just positives: the file asserts `"unregistered_skill" not in codes` and `"folder_scope" not in codes` in the relevant tests.
    - Coverage was converted, not lost: `grep -c "^\s*def test_" backend/tests/unit/test_182_grounding_skill_org_gate.py` is >= its HEAD value (record the HEAD value in the SUMMARY before editing), and the file still contains the `coerce_uid` raise test.
    - `grep -c "== \[\]" backend/tests/unit/test_182_grounding_skill_org_gate.py` no longer counts the `_Boom` assertion (record the before/after in the SUMMARY).
    - SEED-131 is narrowed, not closed: the file still has `status: open`, and it now contains a section naming plan 182-11 and listing what remains for Phase 184.
    - Falsification recorded in the SUMMARY, three separate observations, each reverted afterwards: (a) restore `_skill_registry`'s `except Exception: return []` and confirm tests (1)-(3) FAIL with a false `unregistered_skill` — record the exact verdict dict observed; (b) remove the `try/except` seal around the grounding stages in `validate_workflow` and confirm test (6) FAILS by propagating the exception; (c) drop the `strict=True` argument on the folders read and confirm tests (4)-(5) FAIL. Record each observed failure.
    - No count regression on the phase surface: `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_publish_grounding_stage.py tests/unit/test_182_severity_codes.py tests/unit/test_publish_service.py tests/test_182_extraction_parity.py tests/test_182_grounding_bundle.py tests/test_revert_byte_identical.py tests/test_181_flip_on.py -q` reports at least 80 passed and 0 failed.
  </acceptance_criteria>
  <done>Every one of the three false-accusation paths is proven closed by injecting the real failure, the converted tests keep their coverage, and SEED-131 records exactly what shipped and what remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external datastore -> grounding registry | Supabase/PostgREST is a remote dependency that can time out, 5xx, reset, or silently truncate. Everything downstream treats its answer as authoritative — an empty or partial registry becomes a membership verdict about the author's definition. |
| registry outcome -> author-facing verdict | The verdict is the product's honesty surface. "This skill does not exist" and "we could not reach the skill registry" are different claims, and only one of them is ever true during an outage. |
| `/validate` -> publish | The seam header claims both run the SAME copy of the rules. If the two disagree about what a FAILURE means, the anti-drift claim is only half true. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-44 | Spoofing (false accusation) | `_skill_registry`'s swallow inside `assemble_grounding_bundle` | mitigate | THE WR-01 GAP. A skills-read blip produced an empty registry and a SUCCESSFUL bundle, so `_unregistered_skill_ref` reported every valid `skill_ref` as unregistered, blocking publish and painting the node red with a factual accusation. The swallow moves to `assemble_grounding_bundle`, which records `degraded={"skills"}`; both consumers then emit `grounding_unavailable`. Falsified by restoring the swallow and observing the false `unregistered_skill` verdict. |
| T-182-45 | Denial of Service | unsealed grounding I/O on `POST /workflows/validate` | mitigate | THE WR-02 GAP. `APIError` is not a `ValueError`, so it escaped the ⊆ rule's catch and reached FastAPI as a 500 on a route Phase 184 calls on every canvas edit — a single transient blip becomes a 500 storm mid-authoring. Both grounding stages are now wrapped and degrade to a structured 200. The pure structural checks stay OUTSIDE the seal so a registry blip costs three rules, not the whole validation. |
| T-182-46 | Spoofing (false scope violation) | unbounded `folders` read feeding the ⊆ walk | mitigate | THE WR-07 GAP. Past PostgREST's `max-rows` cap the read silently returns a prefix; `folder_map` loses ancestors, org-shared folders vanish from the palette, the resolved subtree shrinks, and `assert_folder_scopes_subset` accuses a correct definition — which since 182-06 BLOCKS publish. The two gate call sites now read with an exact count and raise `FolderReadTruncatedError` on a short read, which degrades the bundle to `grounding_unavailable`. This project targets org-scale multi-tenant production, where 1000 folders across all tenants is not a large deployment. |
| T-182-47 | Tampering (error-class confusion) | `FolderReadTruncatedError`'s base class | mitigate | Deliberately a `RuntimeError`, NOT a `ValueError`. The ⊆ rule's broad `except ValueError` (round-2 WR-03, out of scope this round) would otherwise catch a truncation and render it as a `folder_scope` verdict — re-creating the exact false accusation this finding is about, one layer down. Pinned by a grep acceptance criterion on the base class. |
| T-182-48 | Denial of Service | adding an exact COUNT to every folders read | mitigate | The count is requested ONLY when `strict=True`, which only the two grounding gate call sites pass. `fetch_all_folders` is on the chat agent-loop path and on four `/folders` routes; an unconditional `count="exact"` would add a COUNT(*) to all of them. The default path is asserted unchanged by a grep criterion and by re-running the folders/kb suites with before/after counts recorded. |
| T-182-49 | Information Disclosure | the `grounding_unavailable` message naming unresolved registries | accept | The message names registry KINDS ("folders", "skills"), never rows, ids, counts, connection strings or driver text. The underlying exception is logged server-side with `exc_info` and never serialized to the caller. This is strictly less disclosure than today's HTTP 500, whose traceback handling is outside this route's control. |
| T-182-50 | Elevation of Privilege | degrading to an empty registry instead of failing closed | mitigate | A degraded bundle never allows anything through. `/validate` reports `grounding_unavailable` at severity `error` (so `ok` is False and the draft does not look publishable), and publish stage 2.6 BLOCKS at `grounding_fidelity` before the golden run. The classification is derived, not asserted: adding the code to `_KNOWN_CODES` without adding it to `_INCOMPLETE_CODES` puts it in the derived error bucket, and a test pins both `phases_empty` states. |
| T-182-51 | Tampering (silent taxonomy drift) | a code invisible to the drift scanner | mitigate | The scanner matches the token sequence `"code": "<name>"` in `grounding.py`, and the new code is built from a constant so the scanner cannot see it. Rather than leave that as a loophole, the constant-to-classifier link is pinned by an explicit assertion (`GROUNDING_UNAVAILABLE_CODE in _KNOWN_CODES` plus both severity states), and the publish-only boundary test is rewritten to assert the boundary set is now empty while keeping its forcing function for any future publish-only code. |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs — no `requirements.txt` change. `count="exact"` is an existing parameter of the already-installed postgrest 2.29.0 `select` builder. Nothing to audit. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_grounding_degradation.py tests/unit/test_182_validate.py tests/unit/test_182_severity_codes.py tests/unit/test_182_grounding_skill_org_gate.py tests/unit/test_182_publish_grounding_stage.py tests/unit/test_publish_service.py tests/unit/test_182_folder_scope_keying.py tests/test_182_extraction_parity.py tests/test_182_grounding_bundle.py tests/unit/test_165_folder_utils_org_scope.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_103_nl_generate.py -q` reports 0 failed.
- The full phase-182 surface stays at or above the measured baseline of 80 passed / 0 failed.
- `tests/integration/test_folders.py` and `tests/integration/test_kb.py` pass/fail counts are recorded before and after; the after-count is no worse (these sit inside the known ~200-failure pre-existing rot, which this plan must neither fix nor worsen).
- `cd backend && venv/Scripts/python.exe -c "import app.main"` exits 0.
- Three falsification observations recorded in the SUMMARY (swallow restored, seal removed, strict dropped) with the exact observed failures.
- `git show --stat` on this plan's commits touches only the eight files in `files_modified`. Stage by explicit path only — the working tree carries ~400 unrelated modifications under `.claude/` and 4 untracked files under `backend/`; never `git add -A` and never `git add .`.
- Do NOT create scratch `.py` files anywhere under `backend/` — uvicorn's `--reload` watcher wedges on Windows. Use the session scratchpad for any ad-hoc probe.
</verification>

<success_criteria>
- A transient skills-read failure produces `grounding_unavailable`, never a false `unregistered_skill` naming a valid `skill_ref`, on BOTH `/validate` and publish.
- A PostgREST error from either grounding stage returns HTTP 200 with a structured degraded verdict, never a 500.
- A truncated folders read produces `grounding_unavailable`, never a false `folder_scope` block.
- One degradation signal, one honest finding builder, one code string — no duplicated decision and no duplicated message.
- `grounding_unavailable` classifies `error` in both `phases_empty` states and is composed into the known set rather than hardcoded.
- Every other caller of `fetch_all_folders` / `fetch_visible_folders` is byte-identical; no extra COUNT is issued anywhere outside the two grounding gate call sites.
- SEED-131 is narrowed with a dated record of what shipped and what remains for Phase 184; it is NOT closed.
- No migration, no new dependency, no frontend file.
</success_criteria>

<output>
Create `.planning/phases/182-server-validation-seam/182-11-SUMMARY.md` when done.
</output>
