---
phase: 127-gauntlet-pip-strip-quiet-idle-cards
plan: 03
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - frontend/src/components/panel/PhaseCard.tsx
  - frontend/src/components/panel/PhaseTimeline.tsx
  - frontend/src/components/panel/PhaseCard.test.tsx
autonomous: true
requirements: [WUX-03]

user_setup: []

must_haves:
  truths:
    - "An idle (pending) PhaseCard is quiet: NO oneLiner type-lecture, NO running activity line, NO pulse/animation — one dim still line (title + faint 'Locked') (SC#2)."
    - "The active (running) PhaseCard blooms: amber wash + left bar + glowing node + a vertical energy comet flowing in + the running-ONLY activity line + the AI-engine chip; the eye lands there instantly."
    - "A done PhaseCard folds to a one-line essence (✓ + title); a failed PhaseCard renders the closed-taxonomy reason + where-line UNCHANGED (failed-as-failed, never empty)."
    - "Phase-type glyphs are the shared 3D vocabulary (Plan 01 phaseGlyph) with the unknown-type unicode fallback retained; the activity line + engine chip are RUNNING-only and never fabricated on idle/done."
    - "Every G-5 a11y/honesty contract survives: status = glyph + word + colour (never colour-alone); APG accordion (aria-expanded/role=region/hidden); indeterminate role=progressbar; role=alert failure + reason_unknown sentinel; aria-busy true-while-running; no dangerouslySetInnerHTML; soul stays an additive SIBLING (no soul prop threaded into PhaseCard/PhaseTimeline)."
  artifacts:
    - path: "frontend/src/components/panel/PhaseCard.tsx"
      provides: "Density-by-status re-skin: quiet idle / bloomed active (+ activity line + engine chip) / folded done / unchanged failed taxonomy; 3D glyph via phaseGlyph"
      contains: "phaseGlyph"
    - path: "frontend/src/components/panel/PhaseTimeline.tsx"
      provides: "Presentational energy-connector spine; structure unchanged (<ol> of PhaseCard); reduced-motion-honored comet"
      contains: "PhaseCard"
    - path: "frontend/src/components/panel/PhaseCard.test.tsx"
      provides: "Extended assertions: idle quiet (no oneLiner/activity/animation), active bloom (activity line + engine chip), done folds — plus all existing GAP-C/failure/XSS assertions stay green"
      contains: "pending"
  key_links:
    - from: "frontend/src/components/panel/PhaseCard.tsx (running branch)"
      to: "frontend/src/lib/providerLogo.tsx"
      via: "engine chip from phase.subAgents[].provider (honest source) → Bot fallback / absent"
      pattern: "providerLogo"
    - from: "frontend/src/components/panel/PhaseCard.tsx"
      to: "frontend/src/lib/phaseGlyph.tsx"
      via: "3D phase-type glyph render with ?? unicode fallback"
      pattern: "phaseGlyph"
---

<objective>
Re-skin the live phase spine (`PhaseCard.tsx` / `PhaseTimeline.tsx`, winner 052-A) for "calm at rest, comprehensive on the active step", riding UNDER the unchanged Phase-124 `<WorkflowSoul scale="run">` header: idle steps go quiet/still (no type-lecture, no activity line, no animation — SC#2), the active step blooms (glow + a vertical energy comet flowing in + the running-only activity line + the AI-engine chip), done folds to a one-line essence, and failed renders the closed-taxonomy reason unchanged.

⚠ G-5 LOAD-BEARING: `PhaseCard.tsx` and `PhaseTimeline.tsx` are hot files SHARED with the live harness run surface (touched 094 / 101.1 / 124). This re-skin is VISUAL-ONLY. The status/data model (`Phase` shape, `STATUS_META` literals, `classifyFailure`, `SUBSTEP_META`, the APG accordion scaffolding), the demux in StreamsProvider, and the soul-as-additive-sibling separation DO NOT change. The soul is NOT threaded into PhaseCard (red line). Every existing replay/component test (`PhaseTimeline.test.tsx`, `FailReason.test.tsx`, `PhaseCard.test.tsx`) MUST be re-run green UNCHANGED — that green run is the structural proof the re-skin is visual-only.

Purpose: Deliver the 022-A target the shipped 094 card never reached — the run feels quiet where nothing is happening and unmistakably alive on the one live step, without fabricating any count or provider and without softening any a11y/honesty contract.
Output: `PhaseCard.tsx` density-by-status re-skin (+ 3D glyph + running-only activity line + engine chip); `PhaseTimeline.tsx` presentational energy-connector (structure unchanged); `PhaseCard.test.tsx` extended with idle-quiet + active-bloom + done-fold assertions.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-RESEARCH.md
@.planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-PATTERNS.md
@.planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/references/workflow-energized-reskin.md
@.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md
@.claude/skills/sketch-findings-agentic-rag/sources/052-living-step-flow/README.md

<interfaces>
<!-- The G-5 hot-file seams. Re-skin MARKUP/CSS; preserve EVERY status word, role, testid, and the data model. -->

PhaseCard.tsx density target — the unconditional oneLiner (line 357): `<p …>{meta.oneLiner}</p>` renders for ALL open phases today. SC#2 requires it GONE from idle (and the sketch drops it from idle AND done; it belongs only on the active step's running-only activity line — a DIFFERENT string).

The root status branch (PhaseCard.tsx:282-294) — the `cn()` already tints by isFailed/isRunning/retrying/llm_batch_agents. EXTEND it for the bloom (don't replace): running = amber wash + left bar + glow; pending = dim/quiet (opacity); done = folded.

The status atom (328-343) — `STATUS_META[status]` glyph + REAL text (`status.text` / "Attempt N") + AA color class + the indeterminate `role="progressbar"` while running. KEEP all of it (status = glyph+word+colour, WCAG 1.4.1).

The APG accordion (296-356) — `<h3>` > `<button aria-expanded aria-controls aria-disabled>`; the body `<div role="region" aria-labelledby hidden aria-busy>`. KEEP unchanged. The failure block (388-396) — `role="alert"` + `classifyFailure` reason/where. KEEP unchanged. The emit sub-step row (364-384, `data-emit-substep`). KEEP unchanged.

The SECOND glyph map — PhaseCard.tsx PHASE_TYPE_LABEL (41-48) holds its OWN glyph copy + UNKNOWN_PHASE_META.glyph "•" (48). Per icon-convention §2 (ONE source), render the glyph via Plan 01's `phaseGlyph(phase.phaseType)` with the existing `?? "•"`/UNKNOWN fallback retained when phaseGlyph returns null. Keep `meta.label` (the type label) and `meta.oneLiner` available for the active step / expanded; only the GLYPH render switches to 3D. The glyph wrapper stays `aria-hidden="true"` (313) — the label carries meaning.

Engine chip — the Phase shape has NO `provider`/`name` field (types/index.ts:652-667). The honest per-phase source is `phase.subAgents[].provider` (TaskRunIndexItem.provider, types/index.ts:598). NOTE: `phase.subAgents` is currently ALWAYS [] (PhaseCard.tsx:398-402 — no demux handler populates it), so for most phases the chip renders HONESTLY-ABSENT. Render the chip ONLY when a real provider exists on a RUNNING phase: `const provider = phase.subAgents?.[0]?.provider; const EngineMark = provider ? providerLogo(provider) : null; {isRunning && EngineMark && <EngineMark size={18}/>}`. Do NOT add a backend wire; do NOT fabricate a provider; do NOT render the chip on pending/done (PATTERNS call-out #2 / RESEARCH Open Q1 — render honestly-absent).

phase.name (the human title the sketch shows) is NET-NEW wire NOT on the Phase type — fall back to `phase.slug` exactly as PhaseSpine.tsx:61 does (`p.name?.trim() || p.slug`). 127 renders the slug honestly; do not invent a title.

Engine-chip / providerLogo consumer pattern (mirror RunCard.tsx:256,310-311): `import { providerLogo } from "@/lib/providerLogo"`, `import { Bot } from "lucide-react"`, `EngineMark ? <EngineMark size={18}/> : <Bot/>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Density-by-status re-skin of PhaseCard (quiet idle / bloom active / fold done) + 3D glyph + running-only activity line + engine chip</name>
  <files>frontend/src/components/panel/PhaseCard.tsx</files>
  <read_first>
    - frontend/src/components/panel/PhaseCard.tsx (the FULL file — the data model + a11y scaffolding to PRESERVE; the oneLiner at 357 + the root branch at 282-294 + the glyph render at 312-315 are the touch points)
    - frontend/src/lib/phaseGlyph.tsx (Plan 01 — the 3D phase-type resolver to consume)
    - frontend/src/lib/providerLogo.tsx (the engine-chip seam; null → Bot fallback)
    - frontend/src/components/chat/RunCard.tsx:252-256, 304-314 (the canonical providerLogo() → Bot consumer)
    - .claude/skills/sketch-findings-agentic-rag/references/workflow-energized-reskin.md (CSS Patterns: `.vrow.pending .pcard {opacity:.6}` quiet, `.vrow.run .pcard {…amber wash + border-left + box-shadow…}` bloom, `.econn .comet` vertical comet, `.enode.run::before` aura; HTML Structures: the 052 `.vrow run` row with the engine chip; "What to Avoid": no motion/type-education on idle/done)
    - .claude/skills/sketch-findings-agentic-rag/sources/052-living-step-flow/README.md (the quiet-idle / alive-active intent + the running-phase-only activity line + the NET-NEW phase.name flag + the honesty/a11y contracts)
    - frontend/src/index.css (real tokens: --panel-status-active = amber, --panel-status-done = green, --accent-violet; the file already uses these — do NOT invent --neon-cyan/--grad-energy)
  </read_first>
  <behavior>
    - A `pending` PhaseCard renders NO `meta.oneLiner` text, NO running activity line, and NO pulse/animation — a quiet still line (the title/slug + the "Locked" status word).
    - A `running` PhaseCard renders the bloom (amber wash + left bar + glow), the running-ONLY activity line, and — IF a real `phase.subAgents[0].provider` exists — the engine chip; status still reads "Running" with the indeterminate progressbar.
    - A `done` PhaseCard folds to a one-line essence (the `✓ Complete` status + the title), no oneLiner, no activity line, no chip.
    - A `failed` PhaseCard renders the existing `classifyFailure` reason + where-line in role=alert, unchanged; reason_unknown sentinel unchanged; failure suppresses the sub-step.
    - The phase-type glyph renders as the shared 3D component (phaseGlyph) with the unicode "•" fallback for an unknown type; the glyph wrapper stays aria-hidden.
  </behavior>
  <action>
    Branch the card body presentation on `phase.status` WITHOUT changing the data model or a11y scaffolding:

    EXTEND the root `cn()` status branch (282-294) so: `pending` → quiet/dim (the `.vrow.pending` opacity treatment), `running` → bloom (amber wash + `border-l` left bar + glow box-shadow, the `.vrow.run` treatment), `done`/`skipped` → folded calm, `failed`/`retrying` → keep the existing destructive/violet tints. Do not remove the existing llm_batch_agents accent-violet left-border.

    The oneLiner (357): make it CONDITIONAL — render `meta.oneLiner` ONLY on the active/expanded step where it adds honest context; render NOTHING on idle (pending) and on done (the sketch folds done to a one-line essence). Concretely: drop the unconditional `<p>{meta.oneLiner}</p>` from the idle and done renders. The idle body is the quiet still line; the done body folds to the title essence.

    Add the RUNNING-ONLY activity line: a separate line shown ONLY when `isRunning`, carrying the live "what it's doing" string from real wire where available (the activity line is a DIFFERENT string from the oneLiner type-lecture; if no live activity string is available, the bloom + status + progressbar already convey "alive" — do NOT fabricate a count like "14 agents"). Never render the activity line on pending/done (RESEARCH Pitfall 4).

    Add the engine chip on the RUNNING branch only: `const provider = phase.subAgents?.[0]?.provider; const EngineMark = provider ? providerLogo(provider) : null;` then render `{isRunning && EngineMark ? <EngineMark size={18}/> : null}` (no Bot fabrication on the live card — if there is no real provider, render no chip; the chip is honestly-absent). This reuses Phase-128's seam exactly; do not re-map providers; do not add a wire.

    Swap the glyph render (312-315): resolve `const Glyph = phaseGlyph(phase.phaseType)` and render `Glyph ? <Glyph /> : <span aria-hidden="true">{meta.glyph}</span>` (meta.glyph keeps the existing "•"/UNKNOWN fallback). Keep the wrapper `aria-hidden="true"`; keep `meta.label` rendering ("· {label}") and `phase.slug` as today (phase.name is NET-NEW, not on the type — slug is the honest title).

    Add a vertical energy comet flowing into the active node as presentation (CSS), gated behind `prefers-reduced-motion` (no comet/aura when set — follow the existing PanelSection/PanelRail reduced-motion pattern). Motion ONLY on the active step (sketch "What to Avoid": no animation on idle/done).

    DO NOT: thread a soul prop into PhaseCard (G-5 red line); change `STATUS_META`/`SUBSTEP_META`/`classifyFailure`/`phaseTypeMeta` logic; touch the APG accordion `<h3>/<button>/role=region/hidden/aria-busy`, the indeterminate progressbar, the role=alert failure block, or the `data-emit-substep` sub-row; use `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/PhaseCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - A `pending` card renders no `meta.oneLiner` text, no activity line, and no `animate-*`/pulse class on the card body.
    - A `running` card renders the bloom treatment + a running-only activity line region; the engine chip renders only when a real `phase.subAgents[0].provider` exists (and is absent otherwise — no Bot fabricated on the live card).
    - A `done` card folds (no oneLiner, no activity line, no chip); status reads "Complete".
    - The glyph renders via `phaseGlyph(phase.phaseType)` with the `?? meta.glyph ("•")` fallback; the glyph wrapper stays `aria-hidden="true"`.
    - Motion (comet/aura) is gated behind `prefers-reduced-motion`.
    - ALL existing PhaseCard.test.tsx assertions stay green UNCHANGED: the 6 emit sub-steps render on the rail; the 5 emit-failure states render distinct reasons in role=alert; failed-as-failed before status flips; failure suppresses the sub-step; XSS (no dangerouslySetInnerHTML); a plain non-emit done phase reads "Complete" with no sub-step.
    - PhaseCard.tsx threads NO soul prop (G-5); the `Phase` data model is unchanged.
    - `cd frontend && npx vitest run src/components/panel/PhaseCard.test.tsx` passes.
  </acceptance_criteria>
  <done>The card is quiet at rest and alive on the live step; the glyph is 3D; the activity line + engine chip are running-only and honest; every a11y/honesty contract is intact; no soul prop threaded.</done>
</task>

<task type="auto">
  <name>Task 2: Presentational energy-connector spine in PhaseTimeline + extend PhaseCard tests (idle-quiet / active-bloom / done-fold)</name>
  <files>frontend/src/components/panel/PhaseTimeline.tsx, frontend/src/components/panel/PhaseCard.test.tsx</files>
  <read_first>
    - frontend/src/components/panel/PhaseTimeline.tsx (the FULL file — the structure to PRESERVE: the `<ol aria-label="Phases" aria-busy>` of `<li><PhaseCard/></li>` at 193-201; the role="status" announcer 172-174; the forward-only counter 108-127; the reconcile frame fetch 87-106 — touch NONE of these behaviorally)
    - frontend/src/components/panel/PhaseCard.test.tsx (the FULL file — mirror `fillPhase(overrides)` 23-33 to craft Phase objects; the existing contracts at 91-92/94-96/112-118/120-130/132-140/143-161 to keep green)
    - frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx (the G-5 PROOF — replay-driven axe/announcer/accordion/aria-busy; this file is NOT edited here, it must stay green as the structural proof)
    - .claude/skills/sketch-findings-agentic-rag/references/workflow-energized-reskin.md (the vertical comet/rail CSS: `.econn.flowing`, `.vcomet`, `.seg.top.flow`; reduced-motion override)
    - .planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-VALIDATION.md (Wave 0: assert idle pending = no activity/oneLiner/animation; running = activity line + engine chip; done folds)
  </read_first>
  <action>
    In PhaseTimeline.tsx, add ONLY the presentational vertical energy-connector spine between/around the `<li>` rows (the `.econn`/`.vcomet` rail adapted to Tailwind + the app amber/green tokens), gated behind `prefers-reduced-motion`. The `<ol>`/`<li>` structure, the `key`, the `<PhaseCard phase={phase} position={i}/>` contract, the `aria-label="Phases"`, the `aria-busy`, the role="status" announcer, the "Phase i / N" forward-only counter, the agent tally, and the reconcile frame fetch are byte-behavior-unchanged. The energy connector is decorative (aria-hidden); it must NOT add focusable/interactive elements or alter the list semantics that the axe gate locks. Thread NO soul prop (G-5).

    EXTEND PhaseCard.test.tsx (do NOT modify existing assertions) using the `fillPhase` helper: add tests that (a) a `pending` card renders NO oneLiner type-lecture text (assert the `PHASE_TYPE_LABEL.oneLiner` copy for that type is NOT present), NO running activity line, and NO pulse/`animate-` class on the body; (b) a `running` card renders the activity line region AND — when `subAgents:[{provider:"anthropic", …}]` is supplied — an engine chip (assert an SVG/brand mark is present), AND when `subAgents:[]` the chip is ABSENT (honestly-absent, no Bot on the live card); (c) a `done` card folds (no oneLiner, no activity line). Use minimal valid `TaskRunIndexItem` shapes for the subAgents fixture (sub_run_id/parent_run_id/status/model/provider).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/PhaseCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx src/components/panel/__tests__/FailReason.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PhaseTimeline.tsx: the `<ol>/<li>/<PhaseCard>` structure, `aria-label`, `aria-busy`, role="status" announcer, forward-only counter, and reconcile fetch are unchanged; the energy connector is decorative (aria-hidden, non-interactive) and reduced-motion-gated.
    - PhaseCard.test.tsx gains the idle-quiet / active-bloom (+ engine-chip-present-with-provider / absent-without) / done-fold assertions.
    - The G-5 replay tests stay green UNCHANGED: `PhaseTimeline.test.tsx` (axe ZERO violations in all 5 states, role=status at load, APG aria-expanded flips, aria-busy true-while-running/cleared-on-terminal) and `FailReason.test.tsx` (failed-as-failed + reason_unknown sentinel).
    - `cd frontend && npx vitest run src/components/panel/PhaseCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx src/components/panel/__tests__/FailReason.test.tsx` passes.
  </acceptance_criteria>
  <done>The timeline carries a decorative energy spine without touching its semantics; the new density contracts are asserted; the G-5 replay/axe proof is green unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE wire → Phase[] → PhaseCard render | Agent/LLM-authored strings (phase slug, error/reason, activity line) cross into the live run cards. |
| sub-agent provider value → engine chip | `phase.subAgents[].provider` (resolved server truth) feeds the engine chip. |
| bundled phase glyphs → DOM | The decorative 3D phase-type SVGs render on every card. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-07 | Tampering / Elevation | phase slug / error / activity-line strings | mitigate | All rendered as plain React text children (auto-escaped); the XSS no-`dangerouslySetInnerHTML` rule is preserved (PhaseCard.tsx:25-27) and grep-guarded by PhaseCard.test.tsx:132-140. The re-skin adds no raw-HTML path. |
| T-127-08 | Repudiation | Fabricated engine chip / activity count on the live card | mitigate | The engine chip renders ONLY from a real `phase.subAgents[0].provider` on a running phase (else absent — no Bot fabrication on the live card); the activity line is running-only and never a fabricated count (RESEARCH Pitfall 4). |
| T-127-09 | Tampering | G-5 a11y/honesty regression while re-skinning | mitigate | The status atom (glyph+word+colour), APG accordion, indeterminate progressbar, role=alert failure + reason_unknown, and aria-busy are all preserved; the replay-driven `PhaseTimeline.test.tsx` axe gate + `FailReason.test.tsx` re-run green UNCHANGED as the structural visual-only proof. Soul stays an additive sibling (no soul prop into PhaseCard/PhaseTimeline). |
| T-127-02b | Tampering | Empty phase-type glyph | mitigate | The glyph renders via the build-time-bundled `phaseGlyph()` (Plan 01) with the `?? "•"`/UNKNOWN fallback — no mapped glyph can render empty; no runtime CDN. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/panel/PhaseCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx src/components/panel/__tests__/FailReason.test.tsx` — all green (existing G-5 a11y/honesty + the new density assertions).
- The replay-driven axe gate (`PhaseTimeline.test.tsx`) reports ZERO violations in all 5 states UNCHANGED — the structural visual-only proof.
- No soul prop threaded into PhaseCard/PhaseTimeline; the `Phase` data model unchanged; no `dangerouslySetInnerHTML`; no `iconify-icon`/CDN.
- Per-wave merge gate: `cd frontend && npm test` (full Vitest suite) green.
- MANUAL UAT (authored in 127-VALIDATION.md, NOT duplicated here): the SC#2 density read (idle quiet / active unmistakable / done calm), the cross-provider engine-chip logo check (SC#10), and the 4-axis UAT-scoreboard (cross-provider × multi-tool × parallel-thread × long-message) — PhaseCard/PhaseTimeline ARE the live-run cards.
</verification>

<success_criteria>
- SC#2 met: idle PhaseCards stay quiet (no oneLiner, no activity line, no animation); the active step blooms with the running-only activity line + the honest engine chip; done folds; failed renders the closed-taxonomy reason unchanged.
- Every G-5 a11y/honesty contract (status glyph+word+colour, APG accordion, indeterminate progressbar, role=alert + reason_unknown, aria-busy, no dangerouslySetInnerHTML, soul-as-sibling) stays green unchanged — the visual-only proof.
- The shared 3D phase-type glyph is used with an honest unicode fallback; no fabricated provider/count anywhere.
</success_criteria>

<output>
Create `.planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-03-SUMMARY.md` when done.
</output>
