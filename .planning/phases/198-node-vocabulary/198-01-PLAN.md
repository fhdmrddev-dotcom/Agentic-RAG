---
phase: 198-node-vocabulary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_198_node_vocabulary.py
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [NODE-01, NODE-02]
tags: [NODE-01, NODE-02, backend, unit-tests, human-input, timeout-guard]
must_haves:
  truths:
    - "NODE-01: Corpus invariant test verifies that 0 prompts exist solely to reshape data between steps"
    - "NODE-02: Human input executor timeout transitions to paused state rather than auto-approval (BUG-260816-06 guard)"
    - "NODE-02: Authored choices are cleanly serialized into the prompt payload"
    - "All existing workflow frontend and backend test suites pass with zero regressions"
  artifacts:
    - path: backend/tests/unit/test_198_node_vocabulary.py
      provides: "Automated regression suite for Phase 198 invariants"
---

<objective>
Execute the Phase 198 verification plan:
1. Author `backend/tests/unit/test_198_node_vocabulary.py` covering NODE-01 corpus reshape invariant and NODE-02 human input timeout/pause guarantees.
2. Run full backend pytest and frontend vitest suites.
3. Update `HOT-FILE-LEDGER.md` and `CLAUDE.md` to record the Phase 198 disposition.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Backend Regression Suite for NODE-01 & NODE-02</name>
  <files>backend/tests/unit/test_198_node_vocabulary.py</files>
  <action>
    Create `backend/tests/unit/test_198_node_vocabulary.py` with:
    1. A test evaluating prompt text classification logic against reshape verbs (`reformat`, `convert`, `transform`, `restructure`, `reshape`, etc.) with positive and negative controls.
    2. Unit tests for `backend/app/services/harness/human_input.py` verifying that when an interactive step times out without an answer, the status transitions to `paused` (never `completed` with empty string).
    3. Unit tests verifying that custom authored choices (e.g. `['Approve', 'Request Changes', 'Reject']`) are properly attached to the pending prompt payload.
  </action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/unit/test_198_node_vocabulary.py</automated>
  </verify>
  <done>All tests in `test_198_node_vocabulary.py` pass cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend & Full Test Suite Verification</name>
  <files>frontend/src/components/workflows/RunSpine.test.tsx</files>
  <action>
    Execute frontend vitest across workflow components and run the vitest count gate.
  </action>
  <verify>
    <automated>npx vitest run src/components/workflows/RunSpine.test.tsx src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>
  <done>Frontend test suites pass with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger & CLAUDE.md Sync</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <action>
    Record Phase 198 disposition in `docs/HOT-FILE-LEDGER.md` and verify `node scripts/check-claude-md-size.cjs`.
  </action>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <done>CLAUDE.md passes size check with >30k headroom.</done>
</task>

</tasks>

<success_criteria>
- NODE-01 and NODE-02 have automated regression tests in `test_198_node_vocabulary.py`.
- Vitest count gate passes with 0 failures across all 110 pinned files.
- `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` are synchronized.
</success_criteria>
