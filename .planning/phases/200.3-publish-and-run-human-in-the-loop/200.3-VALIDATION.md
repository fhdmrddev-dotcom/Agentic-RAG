---
phase: 200.3
slug: publish-and-run-human-in-the-loop
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-24
validated: 2026-08-24
---

# Phase 200.3 — Validation Strategy: Publish & Run Human-in-the-Loop Workflows (SEED-164)

> Per-phase validation contract for feedback sampling during execution.
> Requirements: `SEED-164` (Publishing & testing human approval workflows + draft test-run execution).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Frontend framework** | Vitest (+ React Testing Library), run via `scripts/vitest-count-gate.cjs` with `GSD_VITEST_MAX_WORKERS=2` |
| **Frontend config** | `frontend/vitest.config.*`; count gate `scripts/vitest-count-gate.cjs` (TARGETS list) |
| **Backend framework** | pytest, async execution |
| **Backend quick run** | `cd backend && venv/Scripts/python -m pytest tests/unit/test_publish_service.py tests/unit/test_198_node_vocabulary.py -x` |
| **Frontend quick run** | `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx` |
| **Full suite** | `node scripts/vitest-count-gate.cjs` (repo root) + `cd backend && venv/Scripts/python -m pytest tests/unit` |
| **Typecheck** | `cd frontend && npx tsc --noEmit` (0 errors) |

---

## Sampling Rate

- **After every task commit:** the affected backend test files + affected frontend suites (quick run commands above).
- **After every plan wave:** full count gate + full backend unit suite.
- **Before `/gsd:verify-work`:** Full test suites must be green + manual verification on real workflows.
- **Max feedback latency:** ~10 s (quick runs); count gate ~50 s.

---

## Per-Task Verification Map

| Behavior | Requirement | Threat Ref | Test Type | Automated Command | File Exists | Status |
|----------|-------------|------------|-----------|-------------------|-------------|--------|
| `_exec_llm_human_input` auto-continues with `options[0]` during background golden run (`is_golden_run=True`) | SEED-164 | Golden run timeout / publish block | unit (backend) | `pytest tests/unit/test_198_node_vocabulary.py -k test_node_02_golden_run_mock_bypass_with_options` | ✅ exists | ✅ green |
| `_exec_llm_human_input` auto-continues with `"Approved"` when options are empty during golden runs | SEED-164 | Golden run empty-options deadlock | unit (backend) | `pytest tests/unit/test_198_node_vocabulary.py -k test_node_02_golden_run_mock_bypass_without_options` | ✅ exists | ✅ green |
| Publish gauntlet permits workflows containing `llm_human_input` nodes to advance to golden validation | SEED-164 | Premature rejection of valid interactive workflows | unit (backend) | `pytest tests/unit/test_publish_service.py` | ✅ exists | ✅ green |
| `preflight_workflow_kickoff` permits draft workflows to run when launched by author | SEED-164 | False 400 Bad Request on draft test runs | unit (backend) | `pytest tests/unit/test_198_node_vocabulary.py -k test_preflight_workflow_kickoff_permits_draft_for_author` | ✅ exists | ✅ green |
| `preflight_workflow_kickoff` refuses draft workflows launched by non-authors (403 Forbidden) | SEED-164 | IDOR on draft workflows | unit (backend) | `pytest tests/unit/test_198_node_vocabulary.py -k test_preflight_workflow_kickoff_refuses_draft_for_non_author` | ✅ exists | ✅ green |
| Builder header renders `▶ Test Run` button for draft workflows (`data-testid="builder-test-run"`) | SEED-164 / D-03 | Missing test-run affordance | unit (frontend) | `vitest run src/pages/WorkflowBuilderPage.header.test.tsx` | ✅ exists | ✅ green |
| Clicking `▶ Test Run` flushes draft save and triggers run launch (`onLaunch`) | SEED-164 / D-03 | Unsaved state loss on test run | unit (frontend) | `vitest run src/pages/WorkflowBuilderPage.header.test.tsx` | ✅ exists | ✅ green |
| Flag-off header markup remains byte-identical to `FLAG_OFF_HEADER_MARKUP` (D-181-01) | D-181-01 | Header regression under feature flag disable | unit (frontend) | `vitest run src/pages/WorkflowBuilderPage.header.test.tsx` | ✅ exists | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Validation Audit 2026-08-24

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |
| Total Automated Tests Verified | 91 backend unit tests (0 fail) + 70 frontend header tests (5437 count gate total, 0 fail) |
| Nyquist Compliance | **100% (nyquist_compliant: true)** |
