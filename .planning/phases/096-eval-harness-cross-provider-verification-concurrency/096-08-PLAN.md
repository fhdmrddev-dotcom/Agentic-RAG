---
phase: 096-eval-harness-cross-provider-verification-concurrency
plan: 08
type: execute
wave: 3
depends_on: ["096-06"]
files_modified:
  - scripts/curate_models.py
  - backend/app/config.py
  - scripts/eval_cross_provider.py
autonomous: false
requirements: [EVAL-01]
tags: [model-curation, config, providers, registry]

must_haves:
  truths:
    - "Every MODEL_CAPABILITIES key, _SUB_AGENT_MODEL_DEFAULTS entry, eval PROVIDERS model, and Settings available_models list is validated against that provider's LIVE /models endpoint (D-05 — the live pass IS the curation; secondary sources are only expectations)"
    - "Model lists are newest-first with newest as default (standing operator preference)"
    - "Missing flagships are registered with exact live IDs (expected from research: Claude Opus 4.8, MiniMax M3 — exact case-sensitive IDs resolved live, never assumed)"
    - "The operator approves the curation diff (names only) BEFORE it ships — model defaults affect live behavior and spend"
  artifacts:
    - path: "scripts/curate_models.py"
      provides: "live /models fetch + names-only diff vs all 4 curation targets"
      contains: "CURATE_"
    - path: "backend/app/config.py"
      provides: "curated registry + defaults table with dated provenance comments"
  key_links:
    - from: "scripts/curate_models.py"
      to: "provider live /models endpoints"
      via: "per-provider GET with operator keys (names-only output)"
      pattern: "models"
    - from: "backend/app/config.py _SUB_AGENT_MODEL_DEFAULTS"
      to: "eval workflow rows"
      via: "this table decides the harness eval's effective model per provider (Pitfall 1 — accepted Open-Q1 path)"
      pattern: "_SUB_AGENT_MODEL_DEFAULTS"
---

<objective>
The D-05 full-registry model curation pass: a names-only curation script hits each provider's live `/models` endpoint and diffs against all four curation targets — `MODEL_CAPABILITIES` (config.py:186-273), `_SUB_AGENT_MODEL_DEFAULTS` (config.py:594-604 — the table the harness eval rows actually use, the accepted Open-Q1 path), the eval `PROVIDERS` constant (eval_cross_provider.py:68-79), and Settings `available_models` (app_settings data). Apply curation newest-first with newest as default; operator approves the diff before commit.

Purpose: the registry is where the case-sensitive silent-downgrade trap lives; research found Claude Opus 4.8 and MiniMax M3 unregistered, deepseek-chat/-reasoner deprecating 2026-07-24, and a stale moonshot comment — the live pass resolves exact IDs (assumptions A1/A2 are pattern-inferred, NOT trusted).
Output: `scripts/curate_models.py`; curated config.py + eval PROVIDERS + app_settings rows; operator-approved diff.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-RESEARCH.md (§Model Curation Findings — per-provider table with endpoints + expected deltas; Assumptions A1/A2; Open-Q4)
@.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 14 — registry row shape + conventions)
@backend/app/config.py (:10-20 _PROVIDER_BASE_URLS; :66-126 MODEL_CONTEXT_DEFAULTS; :186-273 MODEL_CAPABILITIES; :288-340 inference; :594-604 _SUB_AGENT_MODEL_DEFAULTS)
@scripts/eval_cross_provider.py (:68-79 PROVIDERS constant)

<interfaces>
Live /models endpoints (auth via backend/.env key NAMES — never print values):
| Provider | Endpoint | Auth |
|---|---|---|
| openai | GET https://api.openai.com/v1/models | Bearer OPENAI_API_KEY |
| anthropic | GET https://api.anthropic.com/v1/models | x-api-key + anthropic-version header |
| google | GET https://generativelanguage.googleapis.com/v1beta/models?key= | GOOGLE_API_KEY query param |
| deepseek | GET https://api.deepseek.com/models | Bearer |
| moonshot | GET https://api.moonshot.ai/v1/models | Bearer |
| zhipu | GET https://api.z.ai/api/paas/v4/models | Bearer (proven live at D-089) |
| minimax | GET https://api.minimax.io/v1/models | Bearer |
| openrouter | GET https://openrouter.ai/api/v1/models | public (best-effort, D-03) |

Registry row shape (copy field conventions exactly):
```python
"claude-opus-4-7": {"native_tools": True, "provider": "anthropic", "llm_call_timeout_seconds": 900, "max_output_tokens": 128000, "capability_source": "registry"},
```
Expected curation actions (verify EVERY one against the live list before applying):
- anthropic: ADD Opus 4.8 (live ID — A1 guesses claude-opus-4-8); REGISTER the `claude-haiku-4-5` alias (only the dated ID is registered; the alias rides inference today)
- minimax: ADD M3 (live PascalCase ID — A2 guesses MiniMax-M3; API is case-sensitive)
- openai: verify gpt-5.5 mini/nano tiers exist; resolve Open-Q4 (gpt-5.5 timeout 300s vs flagship 600-900s convention)
- google: check gemini-3.1-pro GA vs -preview suffix
- deepseek: flag deepseek-chat + deepseek-reasoner deprecation (2026-07-24)
- moonshot: fix stale comment "api.moonshot.cn" → api.moonshot.ai (config.py:240)
- zhipu: re-validate all 7 IDs (D-089 method, 8 days stale)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/curate_models.py — live /models fetch + 4-target diff (names only)</name>
  <files>scripts/curate_models.py</files>
  <read_first>
    - scripts/eval_cross_provider.py (load_env / report_env_presence / assert_localhost_only patterns; key-handling discipline)
    - backend/app/config.py (:186-273 + :594-604 — the keys to diff against)
    - .planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-RESEARCH.md (§Model Curation Findings table)
  </read_first>
  <action>
    Create `scripts/curate_models.py`:
    1. `load_env()` from backend/.env + `report_env_presence()` (presence-only). For each of the 8 providers in the interface table: GET the live /models endpoint with the env key (emit `CURATE_SKIP <provider> key-missing` if absent — never blocks others). Parse model IDs defensively (openai-compat: `data[].id`; google: `models[].name` stripped of the `models/` prefix; others: read the actual response shape).
    2. Diff targets: import the registry directly (`sys.path.insert` backend, `from app.config import MODEL_CAPABILITIES, _SUB_AGENT_MODEL_DEFAULTS, MODEL_CONTEXT_DEFAULTS`); parse the eval `PROVIDERS` constant (import or regex from scripts/eval_cross_provider.py); read Settings `available_models` from app_settings via psycopg2 (localhost:54322, constant SQL — call `assert_localhost_only()` BEFORE the DB read; the provider HTTP calls are exempt from the gate, the app/DB half is not).
    3. Emit a greppable names-only report per provider:
       - `CURATE_LIVE <provider> <id>` — every live ID (newest-first where the API exposes created timestamps; else lexical)
       - `CURATE_MISSING <provider> <id>` — live flagship-family IDs absent from MODEL_CAPABILITIES
       - `CURATE_STALE <provider> <id> <target>` — registry/defaults/PROVIDERS/available_models IDs NOT in the live list (the case-sensitive trap detector; <target> names which curation target holds the stale ID)
       - `CURATE_DEFAULT <provider> current=<id> live_newest=<id>` — defaults-table row vs live newest
    4. NEVER print key values — model IDs and env-var NAMES only.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m py_compile scripts/curate_models.py && backend/venv/Scripts/python.exe scripts/curate_models.py --help</automated>
  </verify>
  <acceptance_criteria>
    - py_compile + --help exit 0
    - `grep -c "CURATE_LIVE\|CURATE_MISSING\|CURATE_STALE\|CURATE_DEFAULT\|CURATE_SKIP" scripts/curate_models.py` >= 5
    - All 8 provider hosts from the interface table present in the file (grep api.openai.com, api.anthropic.com, generativelanguage.googleapis.com, api.deepseek.com, api.moonshot.ai, api.z.ai, api.minimax.io, openrouter.ai)
    - No env value interpolated into any print/log statement (review diff — names/presence only)
    - A real run completes and emits CURATE_ lines for every provider with a present key (executor runs this — keys are in backend/.env; capture the output as the Task 2 input + checkpoint evidence)
  </acceptance_criteria>
  <done>Curation script runs live, producing the authoritative names-only diff across all 4 targets.</done>
</task>

<task type="auto">
  <name>Task 2: Apply curation — registry, defaults table, eval PROVIDERS, Settings data</name>
  <files>backend/app/config.py, scripts/eval_cross_provider.py</files>
  <read_first>
    - The CURATE_ output from Task 1 (the live truth — RESEARCH expectations are hypotheses to confirm/refute)
    - backend/app/config.py (full registry block conventions: dated provenance comments, google's supports_parallel_tools/max_tools fields, timeout tiers 300/600/900)
    - .planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 14)
  </read_first>
  <action>
    Driven ONLY by Task 1's live output (never by assumption):
    1. **MODEL_CAPABILITIES:** add live-confirmed missing flagships with exact IDs (expected: Opus 4.8 → anthropic, 900s, 128000; MiniMax M3 with the live PascalCase ID; the `claude-haiku-4-5` alias row mirroring the dated row's caps). Resolve Open-Q4: set gpt-5.5 `llm_call_timeout_seconds` to the flagship tier (600) — flag it in the checkpoint for operator confirmation. Add `# DEPRECATED 2026-07-24 (live deprecation notice)` comments on deepseek-chat/deepseek-reasoner (keep rows until the date). Fix the moonshot comment api.moonshot.cn → api.moonshot.ai. Google: apply the GA-vs-preview finding. Every touched provider block gets `# verified against live /models 2026-06-07 (096 D-05 curation)`.
    2. **MODEL_CONTEXT_DEFAULTS:** add context rows for newly registered models (provider-published sizes; conservative when unknown).
    3. **_SUB_AGENT_MODEL_DEFAULTS:** update each provider to the curated preferred default — newest-first preference WITHIN this table's fast/cheap sub-agent tier (a new mini/flash tier supersedes the old one; never put a flagship in a sub-agent default). Comment that this table decides the harness eval's effective model (Pitfall 1).
    4. **eval PROVIDERS constant** (scripts/eval_cross_provider.py:68-79): bump each tuple's model to the curated current ID (Deep-mode rows use these; this is the ONLY edit to the eval script in this plan — keep it one hunk).
    5. **Settings available_models** (app_settings rows, local DB): UPDATE the lists newest-first with newest as default via psycopg2 against localhost:54322 — record the exact UPDATE statements in the SUMMARY for auditability (data change, not a migration — app_settings is settings data per project rules).
    6. Sanity: `venv/Scripts/python.exe -c "from app.config import MODEL_CAPABILITIES"` imports clean; run config-touching tests: `pytest tests -q -k "config or capabilities or registry or model"` — net-new failures 0 vs pre-plan commit (stash pattern).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python.exe -c "from app.config import MODEL_CAPABILITIES, _SUB_AGENT_MODEL_DEFAULTS; print(len(MODEL_CAPABILITIES), 'rows; defaults:', len(_SUB_AGENT_MODEL_DEFAULTS))" && venv/Scripts/python.exe -m pytest tests -q -k "config or capabilities" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - config.py imports clean; `grep -c "2026-06-07" backend/app/config.py` >= 1 (dated provenance)
    - `grep -c "api.moonshot.cn" backend/app/config.py` == 0 (stale comment fixed)
    - `grep -c "DEPRECATED 2026-07-24" backend/app/config.py` >= 1 (deepseek flag) — IF the live list still serves those IDs; else removal with comment
    - Every ID added to MODEL_CAPABILITIES appears in Task 1's CURATE_LIVE output (cross-check in SUMMARY — zero assumed IDs)
    - eval script diff = one hunk (PROVIDERS constant only)
    - pytest -k "config or capabilities": net-new failures 0 vs pre-plan
  </acceptance_criteria>
  <done>All four curation targets updated from live truth, newest-first, provenance-dated, tests clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator approves the curation diff (names only)</name>
  <what-built>The D-05 curation pass: live /models-validated updates to MODEL_CAPABILITIES, MODEL_CONTEXT_DEFAULTS, _SUB_AGENT_MODEL_DEFAULTS, the eval PROVIDERS constant, and Settings available_models (newest-first, newest default). Model defaults affect live behavior and per-call spend, so you approve before it ships.</what-built>
  <how-to-verify>
    Review the summary table Claude presents (per provider: added IDs / removed-or-flagged IDs / new sub-agent default / new Settings default — model NAMES only, no keys). Spot-check anything surprising:
    1. The new sub-agent defaults are the fast/cheap tier you'd expect (these drive harness eval rows + sub-agent spend).
    2. The newest-as-default ordering matches your preference per provider.
    3. The gpt-5.5 timeout change (300s → 600s flagship tier) is acceptable.
    4. Optionally open Settings in the app (http://localhost:5173) and confirm the model dropdowns read newest-first.
  </how-to-verify>
  <files>backend/app/config.py, scripts/eval_cross_provider.py</files>
  <action>Present the per-provider curation summary table (added IDs / removed-or-flagged IDs / new sub-agent default / new Settings default — model NAMES only, never key values) and PAUSE for operator approval. On "approved": commit config.py + the eval PROVIDERS hunk + scripts/curate_models.py with the SUMMARY documenting the app_settings UPDATE statements. On change requests: apply them, re-run the Task 2 sanity checks, re-present.</action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python.exe -c "from app.config import MODEL_CAPABILITIES; print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Operator typed "approved" (or all requested changes applied and re-approved)
    - Final commit contains backend/app/config.py + scripts/eval_cross_provider.py + scripts/curate_models.py
    - SUMMARY records the exact app_settings UPDATE statements executed
  </acceptance_criteria>
  <resume-signal>Type "approved" to commit, or name the rows to change (e.g. "keep gpt-5.5 at 300s", "default zhipu to glm-5.1 not X").</resume-signal>
  <done>Operator approved; curation committed (config.py + eval script + SUMMARY documenting the app_settings UPDATEs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → live provider APIs | operator keys from backend/.env make real authenticated calls |
| script → local DB | app_settings reads/updates via psycopg2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-096-08-01 | Information disclosure | curation output / SUMMARY | mitigate | Names-only discipline (env-var names + model IDs, never values); acceptance review of every print path |
| T-096-08-02 | Tampering | app_settings UPDATEs | mitigate | localhost-only gate before DB writes; constant SQL + params; exact statements recorded in SUMMARY (auditable) |
| T-096-08-03 | Tampering | registry edits | mitigate | Every added ID must appear in CURATE_LIVE output (zero assumed IDs — acceptance criterion); operator checkpoint approves the diff |
| T-096-08-04 | Denial of service | wrong model defaults breaking live runs | mitigate | Checkpoint review BEFORE commit; the Plan 06 eval's runs.model assertion catches any silent-downgrade post-hoc |
</threat_model>

<verification>
- curate_models.py compiles + real run emits CURATE_ lines for all keyed providers
- config.py imports clean; config-touching pytest net-new 0
- Operator approval recorded; commit includes config.py + eval PROVIDERS hunk
</verification>

<success_criteria>
- D-05 delivered: all four curation targets live-validated, newest-first with newest default, the silent-downgrade trap swept (CURATE_STALE empty or resolved)
- The defaults table the harness eval depends on (Pitfall 1) is curated truth
</success_criteria>

<output>
After completion, create `.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-08-SUMMARY.md`
</output>
