---
phase: 174-run-state-lifecycle-honesty
plan: 04
type: execute
wave: 3
depends_on: [174-03]
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/lib/dedupMessages.ts
  - frontend/src/components/chat/MessageList.tsx
  - frontend/src/lib/__tests__/dedupMessages.test.ts
  - frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx
autonomous: true
requirements: [STATE-04]
nyquist_compliant: true

must_haves:
  truths:
    - "STATE-04 timer: the kickoff optimistic assistant placeholder is stamped with startedAt so the run-strip timer derives from a stable wall-clock baseline and keeps climbing on nav-back instead of reseeding from component mount (D-11)"
    - "STATE-04 timer: reuse the Phase 095.1 started_at anchor (RunCard.tsx:122 runStartMs = startedAt ?? created_at) as the CONSUMER — fix the source stamp, never re-implement a setInterval accumulator (D-11)"
    - "STATE-04 avatar: exactly one assistant avatar renders including the pre-runId double-mount window — close the pre-runId gap in dedupMessagesByRunId (and the reconcile-insert seam), preserving the existing keep-persisted-over-temp invariants (D-12)"
    - "STATE-04: backend is already correct (1 assistant row per run) — this is a pure frontend reconcile/dedup + stamp fix, no backend change, no migration (D-12/D-14)"
    - "G-5 hot files (StreamsProvider/MessageList/dedup): re-run the reconcile-race + dedup replay tests; do not regress the shared render path (D-13)"
    - "Design bar 130-C: the run-header owns one anchored timer + exactly one avatar as a single instrument (D-15)"
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "startedAt stamped onto the kickoff placeholder assistant-row branch (StreamsProvider.tsx:1856-1868)"
      contains: "startedAt"
    - path: "frontend/src/lib/dedupMessages.ts"
      provides: "pre-runId fallback collapse in dedupMessagesByRunId (temp assistant placeholder vs incoming assistant row for the same thread when neither has a runId)"
      contains: "dedupMessagesByRunId"
    - path: "frontend/src/lib/__tests__/dedupMessages.test.ts"
      provides: "extended dedup test — a pre-runId duplicate collapses to one; harness rows-without-runId stay untouched"
      contains: "runId"
  key_links:
    - from: "StreamsProvider kickoff placeholder"
      to: "RunCard/run-strip timer"
      via: "startedAt stamp consumed by runStartMs = startedAt ?? created_at"
      pattern: "startedAt"
    - from: "MessageList"
      to: "dedupMessagesByRunId"
      via: "the dedup seam that now also collapses the pre-runId twin"
      pattern: "dedupMessagesByRunId"
---

<objective>
Build STATE-04 (BUG-260610-01): navigating to a streaming workflow run (a) reseeds the elapsed timer from component mount — a multi-minute run reads "28s" — and (b) renders a duplicate empty assistant avatar from the mount / first-SSE double-mount. Fix BOTH at their frontend sources (D-12): stamp `startedAt` on the kickoff optimistic placeholder so the existing Phase-095.1 `started_at`-anchored timer keeps climbing on a nav-back remount, and close the pre-runId gap in `dedupMessagesByRunId` (+ the reconcile-insert seam) so exactly one avatar renders even before a runId exists. Backend is already correct (DB-confirmed 1 assistant row per run) — this is a pure render/reconcile fix, no backend change, no migration (D-14).

Purpose: A nav-back into a long workflow currently lies about elapsed time and shows two avatars. Anchoring the timer and collapsing the pre-runId twin restores the 130-C single-instrument honesty.
Output: The `startedAt` kickoff stamp, the pre-runId dedup fallback, and extended reconcile-race / dedup tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-run-state-lifecycle-honesty/174-SPEC.md
@.planning/phases/174-run-state-lifecycle-honesty/174-CONTEXT.md
@.planning/phases/174-run-state-lifecycle-honesty/174-RESEARCH.md
@.planning/phases/174-run-state-lifecycle-honesty/174-PATTERNS.md
@.planning/phases/174-run-state-lifecycle-honesty/174-UI-SPEC.md
@.planning/reported-bugs/BUG-260610-01-workflow-run-nav-timer-reset-duplicate-avatar.md

<interfaces>
<!-- Extracted from live source this session — executor uses these directly. -->

The kickoff placeholder stamp to extend (StreamsProvider.tsx:1856-1868 — stamps runId/model/provider, NOT startedAt):
```
prev.map((m) => {
  if (m.id === userMsg.id) return { ...m, id: message_id }
  if (m.id === assistantId)
    return { ...m, runId: run_id, model: resolvedModel ?? undefined, provider: resolvedProvider ?? undefined }
  return m
})
```
Note: the kickoff POST response returns only { message_id, run_id, model, provider } (verified :1807-1818) — it does NOT return started_at. Use client send-time `new Date().toISOString()` as the anchor; the persisted `runs.started_at` enrich (api.ts started_at→startedAt, :203) corrects any drift on the next hydrate. Do NOT add a backend field (D-14).

The timer consumer already anchored (RunCard.tsx:122 — the 095.1 fix; CONSUMER ONLY, do NOT edit its runStartMs/frozenEndRef machinery):
```
const runStartMs = message.startedAt ? Date.parse(message.startedAt) : Date.parse(message.created_at)
```

The dedup to extend (dedupMessages.ts:31-53 — keys on msg.runId; the pre-runId window at the `if (!runKey) { result.push(msg); continue }` line is the uncovered gap):
```
const runKey = msg.role === "assistant" && msg.runId ? msg.runId : null
if (!runKey) { result.push(msg); continue }   // ← pre-runId rows pass THROUGH uncollapsed
// … existingIsTemp && !incomingIsTemp → result[existingIdx] = msg (persisted swap) …
```
The MessageList seam: dedup call MessageList.tsx:161; key `run-${runId} ?? id` MessageList.tsx:180 (the key is CORRECT — do not touch it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confirm the workflow-run timer owner, then stamp startedAt on the kickoff placeholder</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx:1856-1868 — the kickoff placeholder stamp (add startedAt in the same assistant-row branch) (174-PATTERNS.md STATE-04 startedAt section)
    - frontend/src/components/chat/RunCard.tsx:122 — the 095.1 `runStartMs = startedAt ?? created_at` anchor (CONSUMER; do NOT edit); and MessageItem.tsx:437 RunCard mount gate (tool_calls>0) — the Wave-0 confirmation point
    - frontend/src/components/chat/RunStatusStrip.tsx — the `⏱ elapsed · Step N · activity` strip that consumes RunCard's elapsedLabel
    - 174-RESEARCH.md Open Q4 + Pitfall 3 + Assumption A1/A3 — fix the SOURCE (placeholder stamp), not the consumer; confirm whether the workflow receipt renders through RunCard (tool_calls>0) or a separate strip
    - .planning/reported-bugs/BUG-260610-01-workflow-run-nav-timer-reset-duplicate-avatar.md — the DB-confirmed render-only evidence
  </read_first>
  <behavior>
    - After kickoff, the assistant placeholder carries `startedAt` (client send-time ISO string) in addition to runId/model/provider
    - RunCard's `runStartMs` now derives from the stamped `message.startedAt` on a nav-back remount instead of falling back to a fresh `created_at` → the timer keeps climbing (no reset to seconds)
    - A Deep run's timer/behavior is unchanged (the stamp is additive; RunCard already read `startedAt ?? created_at`) — D-14 byte-identical
  </behavior>
  <action>
    First, CONFIRM (by reading MessageItem.tsx:437 + RunCard.tsx + RunStatusStrip.tsx) whether the in-chat workflow run-receipt renders through `RunCard` (the `tool_calls>0` gate — harness phases run Deep tools so it typically does) or through a separate strip that owns its own timer. Record the confirmed component in the SUMMARY (resolves RESEARCH Open Q4 / A3). Then stamp the timer SOURCE: in `StreamsProvider.tsx:1856-1868`, add `startedAt: new Date().toISOString()` to the `m.id === assistantId` branch alongside the existing runId/model/provider stamp (client send-time is an acceptable anchor per A1 — the persisted `runs.started_at` enrich corrects drift on the next hydrate; do NOT add a backend field, D-14). If — and only if — the confirmation shows a SEPARATE strip owns the workflow timer and it does NOT already use the `startedAt ?? created_at` derivation, apply that same 095.1 derivation there (mirroring RunCard.tsx:122); otherwise make NO consumer edit (RunCard is already correct — Pitfall 3, do not touch runStartMs/frozenEndRef). Extend the RunCard (or the confirmed strip's) timer test to assert the anchored derivation uses the stamped `startedAt`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- RunCard</automated>
  </verify>
  <acceptance_criteria>
    - Source: `StreamsProvider.tsx` kickoff `m.id === assistantId` branch adds `startedAt: new Date().toISOString()` (additive, alongside runId/model/provider).
    - Source: the SUMMARY records whether the workflow receipt renders via RunCard (tool_calls>0) or a separate strip; RunCard.tsx:122 `runStartMs` derivation is unedited (or the separate strip gained the same `startedAt ?? created_at` derivation).
    - Behavior: a nav-back remount derives elapsed from the stamped `startedAt`, not a fresh `created_at`; Deep run timer unchanged.
    - Test-command: `cd frontend && npm test -- RunCard` differentially green (timer-anchor assertion present).
  </acceptance_criteria>
  <done>Timer owner confirmed + recorded; startedAt stamped on the kickoff placeholder; RunCard machinery untouched (or the separate strip anchored identically); timer test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the pre-runId dedup gap so exactly one avatar renders; extend the reconcile-race + dedup tests</name>
  <files>frontend/src/lib/dedupMessages.ts, frontend/src/components/chat/MessageList.tsx, frontend/src/lib/__tests__/dedupMessages.test.ts, frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx</files>
  <read_first>
    - frontend/src/lib/dedupMessages.ts:31-53 — the function; the `if (!runKey) { result.push(msg); continue }` line is the pre-runId gap to close (174-PATTERNS.md STATE-04 avatar section + RESEARCH Pitfall 4)
    - frontend/src/components/chat/MessageList.tsx:161 (dedup call) + :180 (`run-${runId} ?? id` key — CORRECT, do NOT touch — RESEARCH Pitfall 4) — and the reconcile-insert seam where an orphan assistant row can co-exist with the un-runId'd optimistic placeholder
    - frontend/src/lib/__tests__/dedupMessages.test.ts — existing invariants incl. the harness rows-without-runId case that must stay green
    - frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx — the deterministic pre-runId race harness to extend (174-RESEARCH.md Open Q6 regression list)
  </read_first>
  <behavior>
    - Two `temp-` assistant rows for the same thread with NO runId (the pre-runId double-mount) collapse to ONE (single avatar)
    - A kept `temp-` row is still replaced by its persisted twin once a runId arrives (existing keep-persisted-over-temp invariant preserved)
    - Genuine harness answers that are assistant rows WITHOUT a runId (the existing :61-68 case) stay UNTOUCHED — not collapsed away
    - User rows always pass through untouched; order is preserved
    - The MessageList `run-${runId} ?? id` key is unchanged
  </behavior>
  <action>
    Extend the dedup INPUT to cover the pre-runId window (RESEARCH Pitfall 4): give `dedupMessagesByRunId` a pre-runId fallback that collapses a `temp-` assistant placeholder against an incoming assistant row for the SAME thread when neither yet has a `runId` (matching a temp placeholder to its imminent persisted/reconciled twin), and/or close the reconcile-insert orphan in `MessageList.tsx` so a second un-runId'd assistant row is not inserted while an optimistic placeholder for the same send is present. Preserve EVERY existing invariant: order-preserving, keep-persisted-over-temp, no-op for user rows, and CRITICALLY do not collapse genuine harness answers that are legitimately assistant-without-runId (the existing dedupMessages.test.ts case at :61-68 must stay green). Do NOT touch the `MessageList.tsx:180` key (it is correct — the gap is the dedup INPUT, not the key). Extend `dedupMessages.test.ts` with a pre-runId duplicate case (two temp assistant rows, same thread, no runId → one result) plus a negative case (two distinct harness rows-without-runId → both kept), and extend `streamsProvider_075_7_reconcile_race.test.tsx` to force the pre-runId race ordering deterministically and assert a single avatar/row survives.
  </action>
  <verify>
    <automated>cd frontend && npm test -- dedupMessages.test.ts streamsProvider_075_7_reconcile_race MessageList.dedup</automated>
  </verify>
  <acceptance_criteria>
    - Source: `dedupMessagesByRunId` (and/or the MessageList reconcile-insert seam) collapses the pre-runId temp twin; the `MessageList.tsx:180` key is unchanged.
    - Behavior: all `<behavior>` rows hold — pre-runId twin collapses to one, persisted-swap preserved, harness rows-without-runId untouched, user rows untouched, order preserved.
    - Test-command: `cd frontend && npm test -- dedupMessages.test.ts streamsProvider_075_7_reconcile_race MessageList.dedup` green, including the new pre-runId case AND the existing rows-without-runId negative case.
  </acceptance_criteria>
  <done>Pre-runId dedup gap closed with invariants preserved; reconcile-race + dedup tests extended and green (single avatar proven pre-runId).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure frontend reconcile/dedup + an additive client-timestamp stamp over already-persisted run data. No new endpoint, input, auth, or persistence; backend is already correct (1 assistant row per run). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-04-01 | Information disclosure | Cross-thread avatar/timer bleed in parallel threads | mitigate | All timer/dedup state is per-thread keyed (the dedup operates on a single thread's bucket; `startedAt` is stamped on the owning thread's placeholder). No global flag — Thread A's timer/avatar never bleeds into Thread B (A25). |
| T-174-04-02 | Tampering | The client-send-time `startedAt` anchor | accept | Client send-time is an approximate anchor (drift ≤ one RTT); the authoritative persisted `runs.started_at` corrects it on the next hydrate (api.ts started_at→startedAt). No security impact — display-only elapsed timer. |
| T-174-04-SC | Tampering | npm installs | accept | No package installed this phase — nothing to vet. |

**No new trust boundary, no new attack surface** — reconcile/dedup + a display-only timestamp stamp over already-authoritative run data (D-14). G-5 replay tests (reconcile-race + dedup) are re-run and must stay green (D-13).
</threat_model>

<verification>
- `cd frontend && npm test -- RunCard dedupMessages.test.ts streamsProvider_075_7_reconcile_race MessageList.dedup` green (timer anchor + pre-runId single-avatar + existing invariants).
- `cd frontend && npm test` full suite differentially green (no NEW failures vs the SEED-056 rot baseline — G-5 replay/render tests intact, D-13).
- Manual (live UAT, 174-VALIDATION.md — run at /gsd:verify-work): launch a multi-minute streaming workflow → nav away and back on a fast (OpenAI) and a slow (Google) provider → the timer continues from real elapsed (no reset to seconds) and exactly one assistant avatar renders.
</verification>

<success_criteria>
- The workflow-run timer anchors to the stamped `startedAt` and keeps climbing on nav-back (no mount-reseed), reusing the 095.1 consumer without editing it (D-11).
- Exactly one assistant avatar renders including the pre-runId double-mount window; existing dedup invariants (keep-persisted-over-temp, harness rows-without-runId untouched) are preserved (D-12).
- No backend change, no migration, no shared-path fork; the G-5 reconcile-race + dedup replay tests stay green (D-13/D-14).
</success_criteria>

<output>
Create `.planning/phases/174-run-state-lifecycle-honesty/174-04-SUMMARY.md` when done.
</output>
