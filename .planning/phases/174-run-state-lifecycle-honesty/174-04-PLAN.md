---
phase: 174-run-state-lifecycle-honesty
plan: 04
type: execute
wave: 3
depends_on: [174-03]
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/lib/dedupMessages.ts
  - frontend/src/components/chat/MessageList.tsx
  - frontend/src/lib/__tests__/dedupMessages.test.ts
  - frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx
  # CONDITIONAL (Task 1, Warning-3 fix) — edited ONLY if the Wave-0 confirmation shows a
  # SEPARATE strip (not RunCard) owns the workflow timer AND it lacks the startedAt ?? created_at
  # derivation. On the MEDIUM-HIGH RunCard-only path this file is NOT touched (RunCard is already
  # correct — do not edit its runStartMs/frozenEndRef). If the branch fires, re-run the STATE-04
  # completeness check against it and record the fired branch in the SUMMARY.
  - frontend/src/components/chat/RunStatusStrip.tsx
autonomous: true
requirements: [STATE-04]
nyquist_compliant: true

must_haves:
  truths:
    - "STATE-04 timer: the kickoff optimistic assistant placeholder is stamped with startedAt so the run-strip timer derives from a stable wall-clock baseline and keeps climbing on nav-back instead of reseeding from component mount (D-11)"
    - "STATE-04 timer: reuse the Phase 095.1 started_at anchor (RunCard.tsx:122 runStartMs = startedAt ?? created_at) as the CONSUMER — fix the source stamp, never re-implement a setInterval accumulator (D-11)"
    - "STATE-04 avatar: exactly one assistant avatar renders including the pre-runId double-mount window — collapse ONLY the same-send double-mount twin (the same closure-scoped assistantId at the insert-reconcile seam, or an ADJACENT temp/no-runId assistant row with NO intervening user row), NEVER a blind 'any two temp/no-runId rows' rule, so the STATE-01b amber (blockedNotice) bubble AND the existing failed-send temp placeholder survive alongside the keep-persisted-over-temp invariant (D-12)"
    - "STATE-04: backend is already correct (1 assistant row per run) — this is a pure frontend reconcile/dedup + stamp fix, no backend change, no migration (D-12/D-14)"
    - "G-5 hot files (StreamsProvider/MessageList/dedup): re-run the reconcile-race + dedup replay tests; do not regress the shared render path (D-13)"
    - "Design bar 130-C: the run-header owns one anchored timer + exactly one avatar as a single instrument (D-15)"
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "startedAt stamped onto the kickoff placeholder assistant-row branch (StreamsProvider.tsx:1856-1868)"
      contains: "startedAt"
    - path: "frontend/src/lib/dedupMessages.ts"
      provides: "scoped pre-runId collapse of the same-send double-mount twin only (adjacent temp/no-runId assistant rows with NO intervening user row) — NOT a blind any-two-temp-rows fallback; the STATE-01b amber (blockedNotice) + failed-send temp rows are preserved"
      contains: "dedupMessagesByRunId"
    - path: "frontend/src/lib/__tests__/dedupMessages.test.ts"
      provides: "extended dedup tests — the same-send double-mount twin collapses to one; PLUS the MANDATORY regression: an amber(blockedNotice)/failed temp row FOLLOWED by a new user message + its own temp placeholder must NOT collapse (both survive); harness rows-without-runId stay untouched"
      contains: "runId"
  key_links:
    - from: "StreamsProvider kickoff placeholder"
      to: "RunCard/run-strip timer"
      via: "startedAt stamp consumed by runStartMs = startedAt ?? created_at"
      pattern: "startedAt"
    - from: "MessageList"
      to: "dedupMessagesByRunId"
      via: "the dedup seam that now collapses ONLY the same-send pre-runId twin (never the amber/failed row)"
      pattern: "dedupMessagesByRunId"
---

<objective>
Build STATE-04 (BUG-260610-01): navigating to a streaming workflow run (a) reseeds the elapsed timer from component mount — a multi-minute run reads "28s" — and (b) renders a duplicate empty assistant avatar from the mount / first-SSE double-mount. Fix BOTH at their frontend sources (D-12): stamp `startedAt` on the kickoff optimistic placeholder so the existing Phase-095.1 `started_at`-anchored timer keeps climbing on a nav-back remount, and close the pre-runId duplicate-avatar gap — scoped to the SAME-SEND twin (never a blind "any two temp rows" collapse) — so exactly one avatar renders even before a runId exists. Backend is already correct (DB-confirmed 1 assistant row per run) — this is a pure render/reconcile fix, no backend change, no migration (D-14).

Purpose: A nav-back into a long workflow currently lies about elapsed time and shows two avatars. Anchoring the timer and collapsing ONLY the same-send pre-runId twin restores the 130-C single-instrument honesty — WITHOUT erasing the honest STATE-01b amber bubble or the failed-send placeholder (both are permanent temp/no-runId assistant rows from 174-03 / the existing catch).
Output: The `startedAt` kickoff stamp, the scoped same-send pre-runId collapse, and extended reconcile-race / dedup tests including the mandatory amber/failed-row-survives regression.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-run-state-lifecycle-honesty/174-SPEC.md
@.planning/phases/174-run-state-lifecycle-honesty/174-CONTEXT.md
@.planning/phases/174-run-state-lifecycle-honesty/174-RESEARCH.md
@.planning/phases/174-run-state-lifecycle-honesty/174-PATTERNS.md
@.planning/phases/174-run-state-lifecycle-honesty/174-UI-SPEC.md
@.planning/phases/174-run-state-lifecycle-honesty/174-03-PLAN.md
@.planning/reported-bugs/BUG-260610-01-workflow-run-nav-timer-reset-duplicate-avatar.md

<interfaces>
<!-- Extracted from live source this session — executor uses these directly. -->

The kickoff placeholder stamp to extend (StreamsProvider.tsx:1856-1868 — stamps runId/model/provider, NOT startedAt):
```
prev.map((m) => {
  if (m.id === userMsg.id) return { ...m, id: message_id }
  if (m.id === assistantId)
    return { ...m, runId: run_id, model: resolvedModel ?? undefined, provider: resolvedProvider ?? undefined }
  return m
})
```
Note: the kickoff POST response returns only { message_id, run_id, model, provider } (verified :1807-1818) — it does NOT return started_at. Use client send-time `new Date().toISOString()` as the anchor; the persisted `runs.started_at` enrich (api.ts started_at→startedAt, :203) corrects any drift on the next hydrate. Do NOT add a backend field (D-14).

The timer consumer already anchored (RunCard.tsx:122 — the 095.1 fix; CONSUMER ONLY, do NOT edit its runStartMs/frozenEndRef machinery):
```
const runStartMs = message.startedAt ? Date.parse(message.startedAt) : Date.parse(message.created_at)
```

The dedup to extend (dedupMessages.ts:31-53 — keys on msg.runId; the pre-runId window at the `if (!runKey) { result.push(msg); continue }` line is the uncovered gap):
```
const runKey = msg.role === "assistant" && msg.runId ? msg.runId : null
if (!runKey) { result.push(msg); continue }   // ← pre-runId rows pass THROUGH uncollapsed
// … existingIsTemp && !incomingIsTemp → result[existingIdx] = msg (persisted swap) …
```
The MessageList seam: dedup call MessageList.tsx:161; key `run-${runId} ?? id` MessageList.tsx:180 (the key is CORRECT — do not touch it).

The SAME-SEND identity anchor (for option (a), the scoped insert-seam guard): the optimistic placeholder id is the closure-scoped `assistantId = makeTempId()` (StreamsProvider.tsx:1772); the SSE callbacks bind to THAT id (`makeStreamCallbacks({ assistantId, … })`, :1872), so every SSE update targets `m.id === assistantId`. The genuine double-mount is a SECOND assistant row co-existing with THIS send's `assistantId` placeholder. There is NO `sendId`/`clientRequestId` field on `Message` to correlate "two inserts of the same send" vs "two unrelated no-runId rows" — so the ONLY safe collapse is same-`assistantId` (option a) or adjacent-with-no-intervening-user-row (option b), NEVER "any two temp/no-runId rows".

The STATE-01b amber row that MUST survive (from 174-03): a `makeTempId()` assistant row stamped with `blockedNotice` that NEVER receives a runId (the workflows kill-switch 403 fires before any run/message INSERT) — a PERMANENT temp/no-runId assistant row that lives in the thread bucket for the session. The existing network-failure catch placeholder (`runStatus:'failed'`) is the same shape. A broad "collapse any two temp/no-runId assistant rows for the thread" rule would silently erase these the next time the user sends ANY new message in the same thread (that send's own temp/no-runId placeholder briefly co-exists with the amber/failed row).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confirm the workflow-run timer owner, then stamp startedAt on the kickoff placeholder</name>
  <files>frontend/src/providers/StreamsProvider.tsx (primary — the startedAt stamp); frontend/src/components/chat/RunStatusStrip.tsx (CONDITIONAL — pre-declared; edited ONLY if the Wave-0 confirmation shows a separate strip, not RunCard, owns the workflow timer and it lacks the startedAt ?? created_at derivation)</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx:1856-1868 — the kickoff placeholder stamp (add startedAt in the same assistant-row branch) (174-PATTERNS.md STATE-04 startedAt section)
    - frontend/src/components/chat/RunCard.tsx:122 — the 095.1 `runStartMs = startedAt ?? created_at` anchor (CONSUMER; do NOT edit); and MessageItem.tsx:437 RunCard mount gate (tool_calls>0) — the Wave-0 confirmation point
    - frontend/src/components/chat/RunStatusStrip.tsx — the `⏱ elapsed · Step N · activity` strip that consumes RunCard's elapsedLabel (research: MEDIUM-HIGH it merely consumes RunCard's derivation and owns no independent timer — confirm before editing)
    - 174-RESEARCH.md Open Q4 + Pitfall 3 + Assumption A1/A3 — fix the SOURCE (placeholder stamp), not the consumer; confirm whether the workflow receipt renders through RunCard (tool_calls>0) or a separate strip
    - .planning/reported-bugs/BUG-260610-01-workflow-run-nav-timer-reset-duplicate-avatar.md — the DB-confirmed render-only evidence
  </read_first>
  <behavior>
    - After kickoff, the assistant placeholder carries `startedAt` (client send-time ISO string) in addition to runId/model/provider
    - RunCard's `runStartMs` now derives from the stamped `message.startedAt` on a nav-back remount instead of falling back to a fresh `created_at` → the timer keeps climbing (no reset to seconds)
    - A Deep run's timer/behavior is unchanged (the stamp is additive; RunCard already read `startedAt ?? created_at`) — D-14 byte-identical
  </behavior>
  <action>
    First, CONFIRM (by reading MessageItem.tsx:437 + RunCard.tsx + RunStatusStrip.tsx) whether the in-chat workflow run-receipt renders through `RunCard` (the `tool_calls>0` gate — harness phases run Deep tools so it typically does; research: MEDIUM-HIGH this is the path and RunStatusStrip merely consumes RunCard's `elapsedLabel`) or through a separate strip that owns its OWN timer. Record the confirmed component AND which branch fired in the SUMMARY (resolves RESEARCH Open Q4 / A3). Then stamp the timer SOURCE: in `StreamsProvider.tsx:1856-1868`, add `startedAt: new Date().toISOString()` to the `m.id === assistantId` branch alongside the existing runId/model/provider stamp (client send-time is an acceptable anchor per A1 — the persisted `runs.started_at` enrich corrects drift on the next hydrate; do NOT add a backend field, D-14).

    Then handle the two mutually-exclusive paths:
    - PRIMARY path (RunCard owns the timer — the expected MEDIUM-HIGH outcome): make NO consumer edit (RunCard.tsx:122 is already correct — Pitfall 3, do not touch runStartMs/frozenEndRef). Extend the RunCard timer test to assert the anchored derivation uses the stamped `startedAt`. Record "RunCard-only path — no separate-strip edit; STATE-04 timer completeness satisfied by the kickoff stamp + RunCard consumer" in the SUMMARY. `RunStatusStrip.tsx` stays UNTOUCHED (leave it out of the diff).
    - FALLBACK path (a SEPARATE strip owns the workflow timer AND it does NOT already use the `startedAt ?? created_at` derivation): apply that same 095.1 derivation to the confirmed strip (`RunStatusStrip.tsx`, pre-declared in files_modified — mirror RunCard.tsx:122), extend THAT strip's timer test to assert the anchored derivation, run it via `npm test -- RunStatusStrip`, AND explicitly RE-RUN the STATE-04 timer completeness check against the newly-touched strip (is the anchored derivation now the single owner of the workflow elapsed? no second timer added?). Record the fired branch + the completeness re-run result in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npm test -- RunCard</automated>
    <automated>CONDITIONAL — run ONLY if the FALLBACK (separate-strip) branch fired: cd frontend && npm test -- RunStatusStrip</automated>
  </verify>
  <acceptance_criteria>
    - Source: `StreamsProvider.tsx` kickoff `m.id === assistantId` branch adds `startedAt: new Date().toISOString()` (additive, alongside runId/model/provider).
    - Source: the SUMMARY records whether the workflow receipt renders via RunCard (tool_calls>0) or a separate strip AND which branch fired. On the PRIMARY path RunCard.tsx:122 is unedited and `RunStatusStrip.tsx` is untouched; on the FALLBACK path the confirmed strip gained the same `startedAt ?? created_at` derivation, its timer test was extended + run, and the STATE-04 completeness re-run is recorded.
    - Behavior: a nav-back remount derives elapsed from the stamped `startedAt`, not a fresh `created_at`; Deep run timer unchanged.
    - Test-command: `cd frontend && npm test -- RunCard` differentially green (timer-anchor assertion present); if the fallback fired, `npm test -- RunStatusStrip` also green.
  </acceptance_criteria>
  <done>Timer owner confirmed + fired branch recorded; startedAt stamped on the kickoff placeholder; on the primary path RunCard machinery + RunStatusStrip untouched, on the fallback path the confirmed strip anchored identically with its test + completeness re-run recorded; timer test(s) green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse ONLY the same-send pre-runId twin (single avatar) without erasing the amber/failed temp row; extend the reconcile-race + dedup tests</name>
  <files>frontend/src/lib/dedupMessages.ts, frontend/src/components/chat/MessageList.tsx, frontend/src/lib/__tests__/dedupMessages.test.ts, frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx</files>
  <read_first>
    - frontend/src/lib/dedupMessages.ts:31-53 — the function; the `if (!runKey) { result.push(msg); continue }` line is the pre-runId gap (174-PATTERNS.md STATE-04 avatar section + RESEARCH Pitfall 4)
    - frontend/src/providers/StreamsProvider.tsx:1772 (`assistantId = makeTempId()`) + :1872 (`makeStreamCallbacks({ assistantId })`) — the closure-scoped same-send identity for option (a); and the reconcile-insert seam where a second un-runId'd assistant row can co-exist with THIS send's `assistantId` placeholder
    - .planning/phases/174-run-state-lifecycle-honesty/174-03-PLAN.md — STATE-01b stamps `blockedNotice` on a `makeTempId()` assistant row that NEVER gets a runId (permanent temp/no-runId row); the existing network catch stamps `runStatus:'failed'` the same way. These MUST survive a later send's placeholder co-existing in the same thread.
    - frontend/src/components/chat/MessageList.tsx:161 (dedup call) + :180 (`run-${runId} ?? id` key — CORRECT, do NOT touch — RESEARCH Pitfall 4)
    - frontend/src/lib/__tests__/dedupMessages.test.ts — existing invariants incl. the harness rows-without-runId case (:61-68) that must stay green
    - frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx — the deterministic pre-runId race harness to extend (174-RESEARCH.md Open Q6 regression list)
  </read_first>
  <behavior>
    - The genuine same-send double-mount (two inserts for the SAME send — the closure-scoped `assistantId`, i.e. two ADJACENT temp/no-runId assistant rows with NO intervening user row) collapses to ONE (single avatar)
    - CRITICAL REGRESSION INVARIANT: a temp/no-runId assistant row carrying `blockedNotice` (STATE-01b amber, 174-03) OR `runStatus:'failed'` (existing network-failure catch) is a PERMANENT no-runId assistant row; when the user later sends a NEW message in the same thread, that new send's own temp/no-runId placeholder must NOT collapse the amber/failed row — BOTH rows survive (they are separated by the new user row, and/or carry a different `assistantId`)
    - A kept `temp-` row is still replaced by its persisted twin once a runId arrives (existing keep-persisted-over-temp invariant preserved)
    - Genuine harness answers that are assistant rows WITHOUT a runId (the existing :61-68 case) stay UNTOUCHED — not collapsed away
    - User rows always pass through untouched; order is preserved
    - The MessageList `run-${runId} ?? id` key is unchanged
  </behavior>
  <action>
    Close the pre-runId duplicate-avatar gap (RESEARCH Pitfall 4) WITHOUT the over-broad "any two temp/no-runId rows collapse" rule — that rule would silently erase the STATE-01b amber bubble (a permanent temp/no-runId assistant row) and the existing failed-send placeholder the next time the user sends in the same thread, and there is NO `sendId`/`clientRequestId` on `Message` to tell "the same send's double-mount" apart from "two unrelated no-runId rows". Choose the SAFEST scoping:
    - PREFERRED (option a — root-cause insert-seam guard): at the `StreamsProvider` insert/reconcile seam, use the closure-scoped `assistantId` (the SAME id the SSE callbacks bind to — StreamsProvider.tsx:1872) to prevent a SECOND assistant row from being inserted while THIS send's `assistantId` placeholder is already present. Collapse on same-send identity, never on "any two temp rows".
    - OR (option b — pure-dedup adjacency): if extending `dedupMessagesByRunId`, restrict the pre-runId collapse to ADJACENT temp/no-runId assistant rows with NO intervening user message between them (a genuinely different send ALWAYS puts a new user row in between, so the amber/failed row is never adjacent to the next send's placeholder).
    Do NOT add a blind `dedupMessagesByRunId` fallback over the flat array; do NOT touch the `MessageList.tsx:180` key (it is correct — the gap is the dedup INPUT / insert seam, not the key). Preserve EVERY existing invariant: order-preserving, keep-persisted-over-temp, no-op for user rows, and the genuine harness assistant-without-runId rows (`dedupMessages.test.ts:61-68`) stay green.

    Tests (MANDATORY, alongside each other wherever the collapse lives — `dedupMessages.test.ts` for option b, and/or `streamsProvider_075_7_reconcile_race.test.tsx` for option a, forcing the pre-runId race ordering deterministically):
    1. POSITIVE — the same-send double-mount (two adjacent temp/no-runId assistant rows, no intervening user row) collapses to ONE (single avatar/row survives).
    2. MANDATORY REGRESSION — a temp/no-runId assistant row with `blockedNotice` set (STATE-01b) OR `runStatus:'failed'` (existing), FOLLOWED by a new unrelated user message + its OWN temp/no-runId assistant placeholder in the same thread, must NOT collapse: assert BOTH the amber/failed row AND the new placeholder survive (2 assistant rows).
    3. NEGATIVE (existing) — two distinct harness rows-without-runId (`:61-68`) stay both-kept.
  </action>
  <verify>
    <automated>cd frontend && npm test -- dedupMessages.test.ts streamsProvider_075_7_reconcile_race MessageList.dedup</automated>
  </verify>
  <acceptance_criteria>
    - Source: the collapse is scoped to the same-send twin (option a: the closure-scoped `assistantId` insert-seam guard; or option b: adjacent temp/no-runId assistant rows with NO intervening user row) — NOT a blind any-two-temp-rows rule; the `MessageList.tsx:180` key is unchanged.
    - Behavior: all `<behavior>` rows hold — the same-send twin collapses to one, the amber(blockedNotice)/failed temp row + a later send's placeholder BOTH survive, persisted-swap preserved, harness rows-without-runId untouched, user rows untouched, order preserved.
    - Test-command: `cd frontend && npm test -- dedupMessages.test.ts streamsProvider_075_7_reconcile_race MessageList.dedup` green, including the new same-send POSITIVE case, the MANDATORY amber/failed-row-survives REGRESSION case, AND the existing rows-without-runId negative case.
  </acceptance_criteria>
  <done>Pre-runId duplicate collapsed ONLY for the same-send twin with all invariants preserved; the STATE-01b amber + failed-send temp rows proven to survive a later send; reconcile-race + dedup tests extended and green (single avatar proven pre-runId, amber row proven un-erased).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure frontend reconcile/dedup + an additive client-timestamp stamp over already-persisted run data. No new endpoint, input, auth, or persistence; backend is already correct (1 assistant row per run). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-04-01 | Information disclosure | Cross-thread avatar/timer bleed in parallel threads | mitigate | All timer/dedup state is per-thread keyed (the dedup operates on a single thread's bucket; `startedAt` is stamped on the owning thread's placeholder). No global flag — Thread A's timer/avatar never bleeds into Thread B (A25). |
| T-174-04-02 | Tampering | The client-send-time `startedAt` anchor | accept | Client send-time is an approximate anchor (drift ≤ one RTT); the authoritative persisted `runs.started_at` corrects it on the next hydrate (api.ts started_at→startedAt). No security impact — display-only elapsed timer. |
| T-174-04-SC | Tampering | npm installs | accept | No package installed this phase — nothing to vet. |

**No new trust boundary, no new attack surface** — reconcile/dedup + a display-only timestamp stamp over already-authoritative run data (D-14). G-5 replay tests (reconcile-race + dedup) are re-run and must stay green (D-13).
</threat_model>

<verification>
- `cd frontend && npm test -- RunCard dedupMessages.test.ts streamsProvider_075_7_reconcile_race MessageList.dedup` green (timer anchor + same-send single-avatar + amber/failed-row-survives regression + existing invariants).
- If the Task-1 fallback branch fired: `cd frontend && npm test -- RunStatusStrip` also green.
- `cd frontend && npm test` full suite differentially green (no NEW failures vs the SEED-056 rot baseline — G-5 replay/render tests intact, D-13).
- Manual (live UAT, 174-VALIDATION.md — run at /gsd:verify-work): launch a multi-minute streaming workflow → nav away and back on a fast (OpenAI) and a slow (Google) provider → the timer continues from real elapsed (no reset to seconds) and exactly one assistant avatar renders. Cross-check STATE-01b: a killed-workflow amber bubble followed by a fresh send in the SAME thread keeps BOTH the amber bubble and the new send visible.
</verification>

<success_criteria>
- The workflow-run timer anchors to the stamped `startedAt` and keeps climbing on nav-back (no mount-reseed), reusing the 095.1 consumer without editing it (D-11).
- Exactly one assistant avatar renders including the pre-runId double-mount window; the collapse is scoped to the same-send twin so the STATE-01b amber (blockedNotice) + failed-send temp rows are preserved, alongside the existing keep-persisted-over-temp / harness-rows-without-runId invariants (D-12).
- No backend change, no migration, no shared-path fork; the G-5 reconcile-race + dedup replay tests stay green (D-13/D-14).
</success_criteria>

<output>
Create `.planning/phases/174-run-state-lifecycle-honesty/174-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
