---
phase: 176-chat-render-correctness-exec-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx
  - frontend/src/__tests__/providers/streamsProvider_bug_260707_03_final_answer_resolve.test.tsx
autonomous: true
requirements: [RENDER-01, RENDER-02]
nyquist_compliant: true

must_haves:
  truths:
    - "RENDER-01: a single send renders exactly ONE user bubble — the optimistic temp is dropped once the snapshot holds an identical-content persisted user row newer-or-equal the temp's created_at (D-05 option b)"
    - "RENDER-01: the 075.7 blank-thread preserve-guard is NOT weakened — a temp with no persisted twin in the snapshot is still preserved; dedup is against-the-snapshot, never stop-preserving-temps (D-06)"
    - "RENDER-02: a clean Deep terminal on the mount/reconcile path swaps the run's bucket-message content to the persisted answer keyed on run.run_id — a backgrounded parallel-thread run un-folds without a reload (D-07)"
    - "RENDER-02: SEED-094 backend stray-last-line emit stays OUT of scope — the content-reconcile faithfully renders whatever the backend persisted (D-09)"
    - "Deep Mode byte-identical: both fixes are additive reconcile-logic at existing StreamsProvider seams, no shared-path fork; the applied send-path reconcile, the assistant-side dedup branches, and dedupMessagesByRunId are untouched (D-14)"
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "content-supersede drop in the untyped-temp preserve branch (RENDER-01) + mount-path onTerminal content-reconcile keyed on run.run_id (RENDER-02)"
      contains: "supersededByPersisted"
    - path: "frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx"
      provides: "new user-side content-dedup case (drop-when-twin-present AND preserve-when-twin-absent)"
      contains: "supersededByPersisted"
    - path: "frontend/src/__tests__/providers/streamsProvider_bug_260707_03_final_answer_resolve.test.tsx"
      provides: "new mount-path content-reconcile case (backgrounded run un-folds by run.run_id)"
      contains: "run_id"
  key_links:
    - from: "StreamsProvider preserve-guard merge (untyped-temp branch)"
      to: "snapshot content-supersede check"
      via: "supersededByPersisted"
      pattern: "supersededByPersisted"
    - from: "StreamsProvider mount-path onTerminal"
      to: "getMessages content-swap"
      via: "run.run_id keyed setMessagesForBucket"
      pattern: "run\\.run_id"
---

<objective>
Two surgical reconcile-correctness fixes in `StreamsProvider.tsx`, both at existing seams:

- **RENDER-01** — a single send must render exactly ONE user bubble. The optimistic user temp + the persisted user row currently reconcile to TWO. Fix via D-05 **option (b)**: a content-supersede drop inside the 075.7-widened preserve-guard's untyped-temp branch — drop the temp only once the snapshot holds its identical-content persisted twin (never "stop preserving temps" — D-06).
- **RENDER-02** — the final answer must render un-folded at a clean terminal with no reload, on the mount/reconcile path too. The applied 2026-07-07 **send-path** `onTerminal` content-reconcile stays; extend the SAME reconcile to the **mount/reconcile-path** `onTerminal` (D-07), keyed on `run.run_id`, so a backgrounded parallel-thread run un-folds on switch-back.

Purpose: closes BUG-260712-02 (dup user bubble) and the nav/mount residual of BUG-260707-03 (folded final answer) — the SC#10 parallel-thread case.
Output: an additive content-supersede drop + a mirrored mount-path content-reconcile, both covered by extended unit tests. No migration, no MessageItem edit, Deep byte-identical.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-chat-render-correctness-exec-reliability/176-CONTEXT.md
@.planning/phases/176-chat-render-correctness-exec-reliability/176-RESEARCH.md

# Change surface (re-scouted 2026-07-22; confirm exact lines at edit time — 174/175 shipped same day)
@frontend/src/providers/StreamsProvider.tsx
@frontend/src/lib/dedupMessages.ts
@frontend/src/lib/api.ts

# Sketch anchor (D-13 — NO fresh sketch; sketch 014 unified card frame + StreamingNarration are the visual bar)
# Skill("sketch-findings-agentic-rag") — auto-load when touching StreamsProvider / the chat render surface

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase, no exploration required. -->

StreamsProvider.tsx (current anchors, re-verify before editing):
- :1434-1468  075.7-widened preserve-guard merge; :1440 `const sendInFlightOnThisThread = sendingThreadsRef.current.has(threadId)`;
              :1443-1465 assistant-side dedup precedents (`!dbRunIds.has(m.runId)` — BUG-260609-03/-01); :1466 the untyped-temp `return sendInFlightOnThisThread` (RENDER-01 EDIT POINT)
- :1571-1648  mount/reconcile-path onTerminal — sets runStatus (:1631), cleans subscriptions (:1643); NO content-reconcile (RENDER-02 EXTENSION TARGET; insert after :1648, key on run.run_id)
- :1753-1766  optimistic user temp insert (`id: makeTempId()`, `role:"user"`, no runId); :1856-1858 WR-04 id re-key (already present)
- :2004-2027  APPLIED send-path onTerminal content-reconcile (getMessages → swap content by `registeredRunId`) — the block to MIRROR (do NOT rebuild/modify)

api.ts:
- `getMessages(threadId)` → messages carrying `{ runId, role, content, created_at, ... }` (`_mapRow`, ~:197) — the reconcile predicate source

dedupMessages.ts:64-97:
- `dedupMessagesByRunId` — runId/assistant-oriented; do NOT overload for user rows (Anti-pattern; user temps carry no runId)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RENDER-01 — content-supersede drop in the untyped-temp preserve branch</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx:1434-1468 — the preserve-guard merge; :1440 `sendInFlightOnThisThread`; :1443-1465 the assistant-side dedup precedent to mirror; :1466 the edit point
    - frontend/src/providers/StreamsProvider.tsx:1753-1766 — the optimistic user temp insert; :1856-1858 the WR-04 id re-key (already present — explains why the dup is born in the MERGE, not a missing re-key)
    - 176-RESEARCH.md "Delegated Unknown 2 — RENDER-01 mechanism pick (D-05) → option (b)" — the resolved drop condition, the four-part predicate, and why option (a) is insufficient
    - 176-CONTEXT.md D-05 (no-migration reconcile-identity fix) + D-06 (075.7 preserve-guard load-bearing — do NOT weaken)
    - frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx — the existing reconcile-race harness (snapshot + temp merge) this extends
    - frontend/src/lib/dedupMessages.ts:64-97 — do NOT overload dedupMessagesByRunId for user rows
  </read_first>
  <behavior>
    - Send in flight on thread T AND snapshot holds a persisted user row with identical content, non-temp id, created_at >= temp.created_at → the untyped user temp is DROPPED (exactly one user bubble)
    - Send in flight AND NO identical-content persisted twin in the snapshot → the temp is PRESERVED (075.7 guard held; a fresh thread never blanks)
    - The assistant-side dedup branch and dedupMessagesByRunId are byte-unchanged
  </behavior>
  <action>
    First extend `streamsProvider_075_7_reconcile_race.test.tsx` with a user-side content-dedup case (RED): a reconcile merge where the snapshot already contains a persisted user row identical in `content` to an in-flight temp resolves to ONE user bubble; and a companion case where NO persisted twin exists keeps the temp (075.7 preserve still green). Then in `StreamsProvider.tsx` at the untyped-temp branch (~:1466, currently `return sendInFlightOnThisThread`), compute `const supersededByPersisted = snapshot.messages.some(s => s.role === "user" && s.content === m.content && !s.id.startsWith("temp-") && new Date(s.created_at) >= new Date(m.created_at))` and change the return to `return sendInFlightOnThisThread && !supersededByPersisted`. This drops the temp ONLY once the snapshot holds its persisted twin (dedup-against-snapshot per D-05b) and preserves it otherwise (D-06). Add a comment mirroring the assistant-side dedup precedent (BUG-260609-03/-01), noting user temps match on content, not runId. Do NOT touch the assistant-side `!dbRunIds.has(m.runId)` branch, the `:1440` `sendInFlightOnThisThread` derivation, or `dedupMessagesByRunId`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: the untyped-temp branch computes `supersededByPersisted` against `snapshot.messages` with the four-part predicate (role user, equal content, non-temp id, created_at >= temp) and returns `sendInFlightOnThisThread && !supersededByPersisted` (grep confirms `supersededByPersisted`).
    - Behavior: vitest — a temp + an identical-content persisted user row reconcile to exactly ONE user bubble; a temp with NO persisted twin stays preserved (the existing 075.7 reconcile-race case is still green).
    - Regression: `git diff` shows no edits to `dedupMessages.ts`, the assistant-side dedup branch, or the `:1440` derivation.
  </acceptance_criteria>
  <done>Content-supersede drop added at the untyped-temp branch; new user-dedup case + the existing 075.7 preserve case both green; assistant-side dedup + dedupMessagesByRunId untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RENDER-02 — extend the content-reconcile to the mount-path onTerminal (keyed on run.run_id)</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/__tests__/providers/streamsProvider_bug_260707_03_final_answer_resolve.test.tsx</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx:2004-2027 — the APPLIED send-path onTerminal content-reconcile (getMessages → swap content by registeredRunId); the block to MIRROR — KEEP it unchanged
    - frontend/src/providers/StreamsProvider.tsx:1571-1648 — the mount/reconcile-path onTerminal (D-07 extension target); runStatus set at :1631, subscription cleanup at :1643; insert the mirrored block AFTER :1648
    - 176-RESEARCH.md "RENDER-02 — un-fold final answer" + "Code Examples → RENDER-02 D-07" (the exact mount-path snippet) + Pitfall 2 (key on run.run_id, NOT registeredRunId — undefined on this path)
    - 176-CONTEXT.md D-07 (extend the reconcile to the nav/mount path) + D-08 (live-verify is acceptance — VALIDATION Manual-Only) + D-09 (SEED-094 stray-last-line OUT)
    - frontend/src/lib/api.ts (~:197 `_mapRow`) — getMessages returns runId/role/content
    - frontend/src/__tests__/providers/streamsProvider_bug_260707_03_final_answer_resolve.test.tsx — the existing send-path reconcile test to extend with a mount-path case
  </read_first>
  <behavior>
    - Mount-path onTerminal with kind "done"/"reader_done" → getMessages(threadId) fires; the bucket message with `runId === run.run_id && role === "assistant"` has its `content` swapped to the persisted clean answer (un-folded) with NO reload
    - The swap is CONTENT-ONLY — tool_calls / suggestions / output-files / runStatus on the message are preserved
    - The send-path onTerminal reconcile (:2004-2027) is byte-unchanged
  </behavior>
  <action>
    First extend `streamsProvider_bug_260707_03_final_answer_resolve.test.tsx` with a mount-path case (RED): a backgrounded run reaching a clean terminal on the mount/reconcile-path onTerminal invokes `getMessages` and swaps that run's assistant bucket message content to the persisted answer, keyed on `run.run_id`. Then in `StreamsProvider.tsx`, after the mount-path onTerminal runStatus set + subscription cleanup (~after :1648), add a content-reconcile mirroring the send-path block (:2004-2027) but keyed on `run.run_id` (registeredRunId is undefined here): on `kind === "done" || kind === "reader_done"`, fire-and-forget `getMessages(threadId)` → find the persisted `m.runId === run.run_id && m.role === "assistant"` → `setMessagesForBucket(surfaceId, threadId, prev => prev.map(m => m.runId === run.run_id && m.role === "assistant" && m.content !== answer.content ? { ...m, content: answer.content } : m))`, `.catch(() => {})`. Swap CONTENT ONLY — never a full loadMessages replace (Anti-pattern). Do NOT modify the send-path reconcile. SEED-094 stays OUT (D-09) — the reconcile faithfully shows whatever the backend persisted.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/providers/streamsProvider_bug_260707_03_final_answer_resolve.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: the mount-path onTerminal contains a `getMessages` → `setMessagesForBucket` content-swap keyed on `run.run_id` (grep confirms `run.run_id` inside the mount-path onTerminal block); the send-path block still keys on `registeredRunId`.
    - Behavior: vitest — the mount-path terminal invokes getMessages and updates only the matching run's assistant `content` (no reload); the mapped message retains runStatus/tool fields.
    - Regression: the applied send-path reconcile (:2004-2027) is unchanged (`git diff`).
  </acceptance_criteria>
  <done>Mount-path content-reconcile added keyed on run.run_id; new mount-path case + the existing send-path case green; send-path block unchanged. (Live un-fold-without-reload verification tracked in 176-VALIDATION Manual-Only per D-08.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client ↔ API | Existing, authenticated, RLS-scoped. Unchanged — no new endpoint, no new data crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-01-01 | Information disclosure | StreamsProvider reconcile/render over already-fetched messages | accept | Client-side render of already-authenticated, RLS-scoped data the user can already see; the fixes reorder/dedup existing bucket messages and re-swap persisted content the same `getMessages` seam already returns. No new boundary. |
| T-176-01-02 | Tampering | client Zustand store | accept | The client store is not a trust boundary; server RLS + `runs.status` remain authoritative. |

**Block threshold:** high. No high-severity threats — this plan is client-side render/reconcile logic over already-secured data (per roadmap: no threat model this milestone). Block: none.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx src/__tests__/providers/streamsProvider_bug_260707_03_final_answer_resolve.test.tsx` — both extended files green.
- Wave-merge: `cd frontend && npx vitest run` — no net-new failures vs the SEED-056 vitest-rot baseline (differential, D-14).
- Live (176-VALIDATION Manual-Only, D-08): one Deep run resolving un-folded at run-end with no reload on BOTH the send path and a nav-watched backgrounded run; one send rendering a single user bubble.
</verification>

<success_criteria>
- A send renders exactly one user bubble; the temp is preserved while its persisted twin is absent (RENDER-01, D-05b/D-06).
- A clean terminal on the mount/reconcile path un-folds the final answer by run.run_id with no reload (RENDER-02, D-07).
- The applied send-path reconcile, assistant-side dedup, and dedupMessagesByRunId are untouched; Deep byte-identical (D-14).
</success_criteria>

<output>
Create `.planning/phases/176-chat-render-correctness-exec-reliability/176-01-SUMMARY.md` when done.
</output>
