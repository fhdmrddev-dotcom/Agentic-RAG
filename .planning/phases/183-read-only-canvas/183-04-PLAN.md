---
phase: 183-read-only-canvas
plan: 04
type: execute
wave: 2
depends_on: ["183-02"]
files_modified:
  - frontend/src/components/workflows/PhaseSpineGraph.tsx
  - frontend/src/components/workflows/soulData.ts
  - frontend/src/components/workflows/PhaseFormPanel.tsx
  - frontend/src/components/workflows/PhaseFormPanel.test.tsx
  - frontend/src/components/workflows/PhaseSpineGraph.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/PhaseSpine.test.tsx
  - frontend/src/components/workflows/soulData.test.ts
autonomous: true
requirements: [CANVAS-01]

must_haves:
  truths:
    - "D-183-13: `PhaseSpineGraph` no longer declares a local `PHASE_GLYPHS`, a local `parseSkipTarget`, a local `PHASE_TYPE_LABELS` or local read-shape interfaces — it imports them from the ONE shared home, and the Spine visibly gains the 3D fluent-emoji marks that Phase 127 shipped"
    - "D-183-06: the Spine's node-title fallback becomes the shared plain-language business sentence, so `<type label> · <slug>` is no longer the DEFAULT face on EITHER view; a real `phase.name` still wins, exactly as today"
    - "D-183-08: the slug stays REVEALABLE on the Spine — `PhaseSpineGraph` reads the app-wide `useTechnicalNamesOptional()` state and renders `technicalTitle(phase)` when the ⌥ reveal is on, using the SAME hook, the SAME helper names and the SAME fallback ordering as the Canvas (plan 183-06). The two views therefore name every phase identically in BOTH ⌥ modes, and the G-6 tripwire 'Spine and Canvas disagree on a step's icon, title, or order' cannot fire from a one-sided reveal"
    - "All FIVE importers of `PhaseSpineGraph` symbols are repointed in the SAME commit with NO re-export shim, matching both shipped extraction precedents (Phase 124-01 soulData, Phase 177-01 StatusChip)"
    - "Every stale in-code claim is deleted, not moved: the false parity docblock at `PhaseSpineGraph.tsx:73-77`, the false parity comment at `PhaseSpineGraph.test.tsx:110-111`, and the false extraction claim at `soulData.ts:19-21`"
    - "CANVAS-01: `PhaseSpineGraph`'s shipped STRUCTURE is unchanged — same test ids, same data attributes, same aria-label SHAPE (`Phase N: <title> (<type>)`), same sort, same drag-free `<button>` invariant, same `onSelectNode` callback. Only the resolved `<title>` text moves, per D-183-06 / D-183-08"
    - "The pre-existing RED assertion at `soulData.test.ts:121-132` is explicitly resolved rather than left contradicting the module it declares canonical"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseSpineGraph.tsx"
      provides: "The vertical spine, repointed onto the shared vocabulary, the 3D glyph resolver, and the app-wide ⌥ reveal"
      contains: "phaseVocabulary"
    - path: "frontend/src/components/workflows/PhaseSpineGraph.test.tsx"
      provides: "The migrated glyph assertions, the ⌥ reveal cross-view test, plus the extended no-graph-lib guard"
      contains: "data-phase-type"
  key_links:
    - from: "frontend/src/components/workflows/PhaseSpineGraph.tsx"
      to: "frontend/src/components/workflows/phaseVocabulary.ts"
      via: "named imports of parseSkipTarget / nodeTitle / technicalTitle / the read shapes"
      pattern: "from \"@/components/workflows/phaseVocabulary\""
    - from: "frontend/src/components/workflows/PhaseSpineGraph.tsx"
      to: "frontend/src/providers/TechnicalNamesProvider.tsx"
      via: "useTechnicalNamesOptional — the shipped app-wide single source, the SAME hook the Canvas reads in plan 183-06"
      pattern: "useTechnicalNamesOptional"
    - from: "frontend/src/components/workflows/PhaseSpineGraph.tsx"
      to: "frontend/src/components/workflows/soulData.ts"
      via: "PHASE_GLYPHS imported, never re-declared"
      pattern: "from \"@/components/workflows/soulData\""
    - from: "frontend/src/components/workflows/PhaseSpineGraph.tsx"
      to: "frontend/src/lib/phaseGlyph.tsx"
      via: "phaseGlyph(phase.config.phase_type) with the PHASE_GLYPHS unicode fallback"
      pattern: "phaseGlyph\\("
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/components/workflows/phaseVocabulary.ts"
      via: "the PhaseSpecJSON type import, repointed"
      pattern: "phaseVocabulary"
---

<objective>
Perform the D-183-13 hard cut: delete `PhaseSpineGraph`'s private duplicates, repoint it and all five
importers onto the shared module, finish the Phase-127 3D-glyph migration on the Spine, and wire the
Spine to the SAME app-wide ⌥ reveal the Canvas reads.

Purpose: two views one toggle apart must not disagree about what a step looks like, what it is called,
or where its branch goes. `soulData.ts`'s header already claims this duplicate was removed — it was
not; the file still renders the flat text glyphs (`⚙ ✎ 🤖 ⛓ ☺ ◆`) Phase 127 retired, and it still
carries a `parseSkipTarget` that disagrees with the backend. It is also the surface where the slug
lives today: pre-183 the Spine fallback was ALWAYS `"<type label> · <slug>"`, so moving it to D-183-06's
plain-language sentence without the D-183-08 reveal would leave the slug unreachable on the Spine AND
let a user with ⌥ ON see two different titles for the same phase across the toggle — verbatim the G-6
condition "Spine and Canvas disagree on a step's icon, title, or order". Doing the extraction now means
Phases 184 and 185 inherit one module instead of a third copy — the roadmap's
refactor-before-the-third-consumer ask.
Output: a repointed Spine that visibly gains the 3D marks and the symmetric ⌥ reveal, five repointed
importers, four stale claims deleted, and the pre-existing RED in `soulData.test.ts` resolved.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-read-only-canvas/183-CONTEXT.md
@.planning/phases/183-read-only-canvas/183-PATTERNS.md
@.planning/phases/183-read-only-canvas/183-VALIDATION.md
@.planning/phases/183-read-only-canvas/183-02-SUMMARY.md
@frontend/src/components/workflows/phaseVocabulary.ts
@frontend/src/components/workflows/PhaseSpine.tsx
@frontend/src/providers/TechnicalNamesProvider.tsx
</context>

<interfaces>
<!-- The COMPLETE importer list, independently verified twice (RESEARCH + PATTERNS §F-5).
     A missed one is a build break. -->

| # | File | Line | Statement | Symbols |
|---|------|------|-----------|---------|
| 1 | `frontend/src/pages/WorkflowBuilderPage.tsx` | 31 | `import { PhaseSpineGraph, type PhaseSpecJSON } from "@/components/workflows/PhaseSpineGraph"` | component + type |
| 2 | `frontend/src/components/workflows/PhaseFormPanel.tsx` | 39 | `import type { PhaseSpecJSON } from "./PhaseSpineGraph"` | type only |
| 3 | `frontend/src/components/workflows/PhaseFormPanel.test.tsx` | 25 | `import type { PhaseSpecJSON } from "./PhaseSpineGraph"` | type only — NOT named in CONTEXT.md |
| 4 | `frontend/src/components/workflows/PhaseSpineGraph.test.tsx` | 18-19 | `import phaseSpineGraphSource from "./PhaseSpineGraph?raw"` + `import { PhaseSpineGraph, type PhaseSpecJSON } from "./PhaseSpineGraph"` | raw source + component + type |

`READ_ONLY_LEGEND` (`PhaseSpineGraph.tsx:44-46`) is exported but has ZERO importers outside its own
file — it stays where it is and is not a repoint constraint. `nodeTitle` (`:86-91`) is module-private,
so moving it is free.

The canonical 3D-glyph render pattern to adopt (`PhaseSpine.tsx:87-89`) — 3D component first, the
`PHASE_GLYPHS` slug string is NOT a glyph since Phase 127, so `"•"` is the real visual fallback:
```tsx
<span aria-hidden="true">
  {Glyph ? <Glyph /> : (PHASE_GLYPHS[type] ?? "•")}
</span>
```

The ⌥ reveal contract to MIRROR EXACTLY — this is the same shipped app-wide provider the Canvas reads
in plan 183-06 Task 2, and the same title ordering `PhaseNode` uses in 183-06 Task 1
(`data.technicalTitle` when on, `data.title` when off). One hook, one state, one ordering, two views:
```ts
// frontend/src/providers/TechnicalNamesProvider.tsx:104-106 — non-throwing leaf accessor
export function useTechnicalNamesOptional(): TechnicalNamesValue | null
// TechnicalNamesValue = { showTechnical: boolean; toggle: () => void; setShowTechnical: (v: boolean) => void }
```
The Spine is a leaf CONSUMER of that state, not a second owner of it — the shipped `usePlainLabel`
idiom. The ⌥ CONTROL lives where it already ships (Settings, the operator band) and, from 183 on, in
the Canvas header (plan 183-06). **Do not add a control to the Spine header**: it is a shipped surface
outside this phase's toggle scope, and a second control is precisely the "two toggles disagree"
failure `TechnicalNamesProvider`'s own docblock says it exists to prevent.

The provider-driven test harness idiom to copy (`frontend/src/lib/__tests__/termMap.test.tsx:138-164`)
— a tiny in-file `Harness` calling `useTechnicalNames().toggle` behind a "flip" button, rendered inside
`<TechnicalNamesProvider>`, with `act(() => screen.getByRole("button", { name: "flip" }).click())`
between the two assertions.

The durable test hook migration to copy (`PhaseSpine.test.tsx:38-55`) — `data-phase-type` attribute
assertions plus `querySelector("svg")`, replacing literal unicode assertions.

The `?raw` anti-drift guard to extend (`PhaseSpine.test.tsx:100-106`) — three files already assert
`not.toMatch(/const PHASE_GLYPHS/)`; `PhaseSpineGraph` becomes the fourth.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Repoint PhaseSpineGraph.tsx source onto the shared vocabulary, the 3D marks, and the ⌥ reveal</name>
  <files>frontend/src/components/workflows/PhaseSpineGraph.tsx, frontend/src/components/workflows/soulData.ts, frontend/src/components/workflows/PhaseSpineGraph.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseSpineGraph.tsx` — the whole file (239 lines); you are deleting six blocks from it and must keep everything else byte-identical
    - `frontend/src/components/workflows/phaseVocabulary.ts` — the module written in plan 183-02; the exact export names to import
    - `frontend/src/components/workflows/PhaseSpine.tsx` lines 19-20 and 80-92 — the shipped, already-repointed sibling: this is exactly the end state for the glyph render
    - `frontend/src/lib/phaseGlyph.tsx` — `phaseGlyph(phaseType)` returns a bundled 3D SVG component or null
    - `frontend/src/components/workflows/soulData.ts` lines 19-35 — the `PHASE_GLYPHS` map to import, and the stale claim at `:19-21` to correct
    - `frontend/src/providers/TechnicalNamesProvider.tsx` lines 85-106 — the throwing writer accessor vs the non-throwing `useTechnicalNamesOptional()` leaf accessor. The Spine uses the OPTIONAL one so it still renders in a provider-less unit test
    - `.planning/phases/183-read-only-canvas/183-06-PLAN.md` Task 1 (the `data.technicalTitle` / `data.title` ordering) and Task 2 (the `useTechnicalNamesOptional` wiring) — the Canvas side you are mirroring; the two implementations must not diverge
    - `frontend/src/lib/__tests__/termMap.test.tsx` lines 138-164 — the shipped provider-driven "flip" harness idiom for the reveal test
    - `.planning/phases/183-read-only-canvas/183-PATTERNS.md` §Blocking Findings **F-5** and **F-6**
  </read_first>
  <action>
    Delete these six blocks from `PhaseSpineGraph.tsx`, in place:
    the local `PHASE_GLYPHS` map and its comment (`:22-31`) — the flat text glyphs Phase 127 retired;
    the local `PHASE_TYPE_LABELS` map (`:33-41`);
    the three read-shape interfaces `ValidatorJSON` / `PhaseConfigJSON` / `PhaseSpecJSON` (`:48-71`);
    the FALSE parity docblock (`:73-77`) — delete it, do not move it, it is the C-1 lie;
    the local `parseSkipTarget` (`:78-83`);
    the module-private `nodeTitle` (`:85-91`).

    Import their replacements from `@/components/workflows/phaseVocabulary` (`parseSkipTarget`,
    `nodeTitle`, `technicalTitle`, and the three types), `PHASE_GLYPHS` from
    `@/components/workflows/soulData`, `phaseGlyph` from `@/lib/phaseGlyph`, and
    `useTechnicalNamesOptional` from `@/providers/TechnicalNamesProvider`. Use the `@/` alias, the
    dominant convention (`soulData.ts:17`, `PhaseSpine.tsx:19-20`).

    Leave NO re-export shim. Both shipped extraction precedents — Phase 124-01 `soulData.ts` and Phase
    177-01 `StatusChip.tsx` — repointed every consumer in the same commit and left nothing behind; a
    shim is a second path to the same symbol and calcifies into Phase 184.

    Replace the two text-glyph render sites with the canonical `PhaseSpine.tsx:87-89` pattern: resolve
    `const Glyph = phaseGlyph(phase.config.phase_type)` once per phase inside the map, and at BOTH the
    spine bullet (`:166-177`) and the card glyph (`:197-200`) render the 3D component when present with
    `(PHASE_GLYPHS[phase.config.phase_type] ?? "•")` as the fallback, inside the existing
    `aria-hidden="true"` wrappers. Give the SVG an explicit sizing class consistent with the surrounding
    type scale (the `PhaseCard.tsx:339-344` precedent uses `h-4 w-4`). The Spine visibly GAINING the 3D
    marks is the intended, wanted outcome of D-183-13 — it finishes the migration Phase 127 started.

    **Wire the ⌥ reveal — D-183-06 + D-183-08, and do it symmetrically with the Canvas.** Under
    D-183-06 the shared `nodeTitle` no longer contains the slug, so the Spine — where the slug was
    ALWAYS visible before this phase — would otherwise lose it with no way to get it back on that
    surface. Read the app-wide state once at the top of the component:
    `const showTechnical = useTechnicalNamesOptional()?.showTechnical ?? false`, and resolve each
    node's title as `showTechnical ? technicalTitle(phase) : nodeTitle(phase)`. That is the SAME hook,
    the SAME helper names and the SAME fallback ordering plan 183-06 uses for `PhaseNode`
    (`data.technicalTitle` when on, `data.title` when off), which is what makes it structurally
    impossible for the two views to disagree about a phase's title in either ⌥ mode. Compute the
    resolved title ONCE per phase and use that single value in BOTH places it appears today — the
    `data-testid="node-title"` span (`:205`) and the `aria-label` (`:188`) — so the accessible name
    always matches the visible name (WCAG 2.5.3 label-in-name). The `?? false` on a null context is
    deliberate: it keeps the Spine renderable in a provider-less unit test and falls back to plain
    language, matching the provider's documented leaf contract.

    Do NOT add a ⌥ toggle control to the Spine header. The Spine is a leaf CONSUMER of the shared
    state (the shipped `usePlainLabel` idiom); the control ships in Settings, in the operator band,
    and — from 183 — in the Canvas header (plan 183-06). A second control here would be the exact
    "two toggles disagree" failure `TechnicalNamesProvider`'s docblock exists to prevent, and it would
    add a net-new visible control to a shipped surface that no decision in this phase scopes.

    Keep byte-identical: every `data-testid`, `data-slug`, `data-phase-type`, `data-selected`,
    `aria-pressed` value and the `aria-label` SHAPE (`Phase N: <title> (<type>)` — only the resolved
    `<title>` moves); the `READ_ONLY_LEGEND` constant and its render; the
    `[...phases].sort((a,b) => a.phase_index - b.phase_index)` non-mutating sort; the `slugSet.has(target)`
    skip-edge filter; the `skip-edge` / `data-from-slug` / `data-target-slug` branch row; the
    `onSelectNode` contract; the drag-free `<button>` invariant (no `draggable`, no drag handler, no
    connection handle, no add-node control); and every class string.

    Do NOT change the Spine's silent-drop behaviour for an unresolvable `skip_to_phase`. D-183-10's
    honest broken-reference marker is a CANVAS behaviour that lands in plans 183-05/183-06; the Spine
    keeps its shipped semantics in 183 so this repoint stays behaviour-neutral there.

    Add the reveal's regression test NOW, in the same task that adds the behaviour. Append a
    `describe("⌥ technical names reveal (D-183-06 / D-183-08)")` block to
    `frontend/src/components/workflows/PhaseSpineGraph.test.tsx` using the shipped
    `termMap.test.tsx:138-164` harness idiom: render an unnamed-phase fixture inside
    `<TechnicalNamesProvider>` and assert (a) with the reveal OFF the node title is the plain-language
    sentence and contains neither the slug nor a `·` separator; (b) after flipping the harness button
    the SAME node renders `"<type label> · <slug>"`; (c) rendered with NO provider at all the Spine
    still renders the plain-language title and does not throw. Assert on at least two nodes in (b) so
    "flips every node at once" is actually tested, matching plan 183-06 Task 3's canvas-side wording.
    Name the block so `-t "technical names"` selects it and nothing else — the rest of that file is
    knowingly red until Task 2 migrates its literal-glyph assertions, so this task's gate is the
    scoped run.

    Finally, correct the stale claim at `soulData.ts:19-21` — but ONLY the comment. It asserts the
    `PhaseSpineGraph.tsx:24-31` duplicate was already replaced; after this task that becomes true, so
    reword it to state the fact accurately and cite plan 183-04. Change no `soulData.ts` code.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/workflows/PhaseSpine.test.tsx && npx vitest run src/components/workflows/PhaseSpineGraph.test.tsx -t "technical names"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const PHASE_GLYPHS" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 0
    - `grep -c "const PHASE_TYPE_LABELS" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 0
    - `grep -c "lastIndexOf" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 0
    - `grep -c "function parseSkipTarget" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 0
    - `grep -c "interface PhaseSpecJSON" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 0
    - `grep -c "split on the LAST" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 0
    - `grep -c "export .* from" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 0 (no re-export shim)
    - `grep -c "phaseGlyph" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns ≥ 1
    - **D-183-08 source assertion:** `grep -c "technicalTitle" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns ≥ 1 AND `grep -c "useTechnicalNamesOptional" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 1 — the Spine imports and reads the same reveal the Canvas does
    - **Symmetry check:** `grep -c "useTechnicalNames\b" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 0 (the OPTIONAL accessor only — the throwing one would break provider-less renders), and `grep -c "TechnicalNamesToggle" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 0 (no second control on the Spine)
    - **D-183-08 component assertion:** `cd frontend && npx vitest run src/components/workflows/PhaseSpineGraph.test.tsx -t "technical names"` is green and runs ≥ 3 tests — reveal OFF renders the plain-language sentence with no slug and no `·`, reveal ON renders `"<label> · <slug>"` on at least two nodes, and a provider-less render neither shows the slug nor throws
    - `grep -cE 'data-testid=\{`spine-node-|data-phase-type=|aria-pressed=|READ_ONLY_LEGEND' frontend/src/components/workflows/PhaseSpineGraph.tsx` returns ≥ 4 (the shipped hooks survive)
    - `frontend/src/components/workflows/soulData.ts` diff is comment-only: `git diff -U0 frontend/src/components/workflows/soulData.ts` shows no changed line outside the `:19-27` comment block
    - `cd frontend && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>The Spine imports one shared vocabulary, renders the 3D marks, resolves its title through the same ⌥ reveal the Canvas uses, declares no duplicate, and leaves no shim.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the four remaining importers and migrate PhaseSpineGraph.test.tsx</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/components/workflows/PhaseFormPanel.tsx, frontend/src/components/workflows/PhaseFormPanel.test.tsx, frontend/src/components/workflows/PhaseSpineGraph.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.tsx` lines 29-45 — the import block and `BuilderDefinition`, which is typed by `PhaseSpecJSON[]`
    - `frontend/src/components/workflows/PhaseFormPanel.tsx` line 39 and its surrounding import block
    - `frontend/src/components/workflows/PhaseFormPanel.test.tsx` line 25
    - `frontend/src/components/workflows/PhaseSpineGraph.test.tsx` — the whole file INCLUDING the `technical names` block added in Task 1; specifically `:18-19` (imports), `:36-42` (the stale "type label / slug" fixture comment), `:78-86` (the fallback-title test), `:88-97` (the literal-glyph assertions that BREAK), `:106-113` (the FALSE parity comment), `:153-159` (the no-graph-lib guard)
    - `frontend/src/components/workflows/PhaseSpine.test.tsx` lines 38-55 — the `data-phase-type` + `querySelector("svg")` migration pattern to copy verbatim
  </read_first>
  <action>
    Repoint all four remaining importers in this same commit — a missed one is a build break (F-5).

    `WorkflowBuilderPage.tsx:31` becomes two imports: the component stays from
    `@/components/workflows/PhaseSpineGraph`, and `type PhaseSpecJSON` moves to
    `@/components/workflows/phaseVocabulary`. Change nothing else in this file — the view toggle lands
    in plan 183-07.

    `PhaseFormPanel.tsx:39` and `PhaseFormPanel.test.tsx:25` each become
    `import type { PhaseSpecJSON } from "./phaseVocabulary"`.

    `PhaseSpineGraph.test.tsx` needs four edits (leave Task 1's `technical names` block untouched):

    (a) Imports at `:18-19` — keep the `?raw` source import of `./PhaseSpineGraph`, keep the component
    import, and move `type PhaseSpecJSON` to `./phaseVocabulary`.

    (b) The three literal-glyph assertions at `:94-96` break under the migration (they assert `🤖` and
    `◆`, which no longer render). Migrate them onto the durable hooks exactly as
    `PhaseSpine.test.tsx:38-55` documents: assert `getAttribute("data-phase-type")` equals the expected
    phase type for each node, and assert `querySelector("svg")` is non-null on each node to prove the
    3D mark rendered. Keep the test name meaningful.

    (c) The FALSE parity comment at `:110-111` says the edge "resolves its target the parse_skip_target
    way (split on the last ':')". Delete that claim and replace it with the truth: the target is
    resolved by slicing the `skip_to_phase:` prefix, matching `reachability.py:89-98`. Do NOT change the
    assertion at `:112` — the fixture's `on_failure` is `"skip_to_phase:human-confirm"`, which contains
    no extra colon, so both the old and the corrected parse yield `"human-confirm"` and the expected
    value is unchanged.

    (d) Extend the no-graph-lib source guard at `:158` so its regex also excludes `xyflow`: the Spine
    must never import the canvas library. Also add, in the same guard block, assertions that the source
    no longer matches `/const PHASE_GLYPHS/`, no longer matches `/lastIndexOf/`, and no longer declares
    `parseSkipTarget` — the machine-checkable form of "no third copy exists at phase end" (G-6).

    While you are in the file, correct the now-stale fixture comment at `:39` ("the fallback (type label
    / slug) must render"). Under D-183-06 the DEFAULT fallback is a plain-language sentence with no
    slug; under D-183-08 the `type label · slug` form is still reachable behind the ⌥ reveal, which
    Task 1's `technical names` block already covers — say both, so the comment does not swing from one
    half-truth to another. The assertion at `:85` only requires a non-empty title, so it stays green;
    tighten it if you wish by additionally asserting that, with the reveal OFF, the rendered title does
    NOT contain the string `emit`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseFormPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from \"./PhaseSpineGraph\"\|from \"@/components/workflows/PhaseSpineGraph\"" frontend/src` returns only lines that import the COMPONENT or the `?raw` source — zero remaining `PhaseSpecJSON` / `ValidatorJSON` / `PhaseConfigJSON` / `parseSkipTarget` type-or-value imports from that module
    - `grep -c "phaseVocabulary" frontend/src/pages/WorkflowBuilderPage.tsx` returns ≥ 1
    - `grep -c "phaseVocabulary" frontend/src/components/workflows/PhaseFormPanel.tsx` returns 1
    - `grep -c "phaseVocabulary" frontend/src/components/workflows/PhaseFormPanel.test.tsx` returns 1
    - `grep -c "🤖" frontend/src/components/workflows/PhaseSpineGraph.test.tsx` returns 0
    - `grep -c "split on the last" frontend/src/components/workflows/PhaseSpineGraph.test.tsx` (case-insensitive) returns 0
    - `grep -c "xyflow" frontend/src/components/workflows/PhaseSpineGraph.test.tsx` returns ≥ 1 (inside the guard regex)
    - `grep -c "technical names" frontend/src/components/workflows/PhaseSpineGraph.test.tsx` returns ≥ 1 — Task 1's reveal block survived this task's edits
    - `cd frontend && npx vitest run src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseFormPanel.test.tsx` green, with the collected test count equal to or greater than before the change
    - `cd frontend && npx tsc -b` exits 0
    - `git diff --stat frontend/src/pages/WorkflowBuilderPage.tsx` shows only the import line changed
  </acceptance_criteria>
  <done>All five importers resolve, the glyph assertions are durable, the reveal block is intact, and the false parity comment is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the anti-duplication guards and resolve the pre-existing soulData RED</name>
  <files>frontend/src/components/workflows/PhaseSpine.test.tsx, frontend/src/components/workflows/soulData.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseSpine.test.tsx` lines 13-18 and 95-110 — the `?raw` import block and the G-5 guard to extend
    - `frontend/src/components/workflows/soulData.test.ts` lines 112-145 — the RED assertion at `:121-132` asserting the retired flat glyph set
    - `frontend/src/components/workflows/soulData.ts` lines 19-35 — what `PHASE_GLYPHS` actually holds since Phase 127 (fluent-emoji SLUG strings, not glyphs)
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 1600-1620 (the baseline rot table) and 1839-1848 (Open Question 2, now marked RESOLVED)
    - `.planning/phases/183-read-only-canvas/183-VALIDATION.md` §Wave 0 Requirements — the "decide + record" line
  </read_first>
  <action>
    Extend the shipped anti-duplication guard in `PhaseSpine.test.tsx` (currently at `:100-106`) so it
    also covers `PhaseSpineGraph`. Add a `?raw` import of `./PhaseSpineGraph` to that file and assert
    the same `not.toMatch(/const PHASE_GLYPHS/)` invariant against it. Three files already assert this
    guard (`PhaseSpine.test.tsx`, `WorkflowSoul.test.tsx`, and now `PhaseSpineGraph.test.tsx`);
    covering the fourth consumer here is what makes "the glyph map has ONE home" a tree-wide property
    rather than a per-file habit.

    Then resolve the pre-existing RED. `soulData.test.ts:121-132` asserts `PHASE_GLYPHS` maps the six
    phase types to `⚙ ✎ 🤖 ⛓ ☺ ◆`. That has been FALSE since Phase 127 replaced the flat glyphs with
    verified fluent-emoji slug strings, and it is red at HEAD — inside exactly the territory D-183-13
    makes canonical. Leaving a red assertion about `PHASE_GLYPHS` while declaring `PHASE_GLYPHS` the
    single source of truth is self-contradictory, so FIX it: update the expected mapping to the six
    slug strings actually shipped (`gear`, `memo`, `robot`, `busts-in-silhouette`, `raised-hand`,
    `package`), keep the "exactly the 6 known phase types, no extras" key-set assertion unchanged, and
    rename the test so it describes slugs rather than glyphs. Add a one-line comment naming Phase 127
    as the change that made the old expectation stale, so the correction is visible rather than silent.

    Record the disposition explicitly in the plan SUMMARY: this is a deliberate in-phase fix of a
    pre-existing failure, not an accidental scope creep, and it means the failing-name differential for
    this phase should SHRINK by exactly one name (`soulData.test.ts` in the glyph describe block).
    Capture the failing-test NAME SET and the total test COUNT before and after — the Phase 177 lesson
    is that a failures-only diff cannot see coverage loss, so the count guard is mandatory.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PhaseSpine.test.tsx src/components/workflows/soulData.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/workflows/PhaseSpine.test.tsx` contains a `?raw` import of `./PhaseSpineGraph` and asserts `not.toMatch(/const PHASE_GLYPHS/)` against it
    - `grep -c "⚙" frontend/src/components/workflows/soulData.test.ts` returns 0
    - `soulData.test.ts` asserts the six fluent-emoji slug strings and still asserts the exact 6-key key set
    - `cd frontend && npx vitest run src/components/workflows/soulData.test.ts` is fully green (the pre-existing RED is resolved)
    - `cd frontend && npx vitest run src/components/workflows` reports zero failures across the directory
    - `cd frontend && npm test` shows the failing-test NAME SET reduced by exactly the `soulData` glyph test and no new names, and the total test count equal or higher versus the recorded baseline of 1877
  </acceptance_criteria>
  <done>The one-home guard covers all four consumers and the phase leaves no self-contradicting red assertion behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow definition JSONB → rendered Spine node | Authored `phase.name`, `slug` and `phase_type` reach the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-01 | Tampering | authored `phase.name` / slug rendered into a Spine node | mitigate | Unchanged and preserved: every authored string stays a plain React text child or a `title=`/`aria-label` attribute value; `dangerouslySetInnerHTML` appears nowhere. The shipped clause is documented at `PhaseSpine.tsx:13-14` (T-124-01). This repoint alters only where the strings come FROM, never how they are rendered — including the ⌥ reveal, which selects between two plain strings and constructs no markup. |
| T-183-08 | Tampering | phase-type-derived icon path (SVG injection) | mitigate | Already enforced by `phaseGlyph.tsx:25-33` — icons are deep-subpath `~icons/fluent-emoji/<slug>` imports bundled at build time by `unplugin-icons`, returned as React components, never interpolated markup. This plan adds NO new slug: D-183-14 reserves the `compass` / `handshake` swaps for a separate dedicated commit. A missing slug would fail the build. |
| T-183-09 | Repudiation | a stale in-code parity/extraction claim misleading a future reader into trusting a drifted port | mitigate | The four stale claims are deleted in this plan (`PhaseSpineGraph.tsx:73-77`, `PhaseSpineGraph.test.tsx:110-111`, `soulData.ts:19-21`, plus the `:39` fixture comment), and the `?raw` source guards make their reappearance a test failure. |

## ASVS conclusion (carried from RESEARCH §Security Domain, line 1759)

No ASVS category is newly engaged: this plan moves declarations between client modules, reads one
existing display-only UI context (`TechnicalNamesProvider`, documented at D-05 as non-secret), and
changes no route, no migration, no authz decision, no data path and no input surface. V5 Input
Validation is satisfied by the totality contract already asserted in plan 183-02. The ROADMAP's
"no threat model" flag for Phase 183 stands.

**Frontend-only.** If any task grows a backend touch, this conclusion is VOID.
</threat_model>

<verification>
- `cd frontend && npx tsc -b` exits 0
- `cd frontend && npx vitest run src/components/workflows` reports zero failures across the whole directory
- `cd frontend && npm test` — failing-test NAME SET shrinks by the `soulData` glyph test only; no new names; total count not lower than 1877
- `grep -rn "const PHASE_GLYPHS" frontend/src` returns exactly one hit, in `soulData.ts`
- `grep -rn "function parseSkipTarget" frontend/src` returns exactly one hit, in `phaseVocabulary.ts`
- Cross-view symmetry: `grep -c "useTechnicalNamesOptional" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns 1, and plan 183-06 lands the identical hook in `WorkflowCanvas.tsx` — one reveal state, two views, no third copy
</verification>

<success_criteria>
- One glyph map and one `parseSkipTarget` in the entire frontend tree
- The Spine renders the 3D fluent-emoji marks; its shipped test ids, aria contract and drag-free invariant are unchanged
- The Spine's default title is D-183-06's plain-language sentence and its slug is still revealable via the SAME app-wide ⌥ state the Canvas reads, so the two views agree in both modes (the G-6 tripwire)
- All five importers repointed in one commit; no re-export shim
- Four stale in-code claims deleted; `soulData.test.ts` green
</success_criteria>

<output>
Create `.planning/phases/183-read-only-canvas/183-04-SUMMARY.md` when done.
Record the before/after failing-test NAME SET and total COUNT (the Phase 177 lesson), confirm in
writing that the `soulData.test.ts` RED was fixed rather than accepted, and record the exact title
expression the Spine now uses so plan 183-06 can be checked against it verbatim.
</output>
