---
phase: 183-read-only-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/index.css
  - frontend/src/test-utils/mockReactFlow.ts
  - frontend/src/test-utils/handleSpike.test.tsx
autonomous: true
requirements: [CANVAS-01]

must_haves:
  truths:
    - "CANVAS-01: `@xyflow/react` ^12.11.2 is installed and the toolchain (tsc -b + vite build) is proven green BEFORE any component work (assumption A4)"
    - "The mandatory xyflow stylesheet loads — the canvas can never render as unpositioned stacked divs (Pitfall 2)"
    - "A jsdom helper exists so component suites can render `<ReactFlow>` without ReferenceError, WITHOUT touching the global setupTests.ts (the 1877-test baseline is already flaky)"
    - "Research assumption A1 (does a `<Handle>`-free custom node render edges?) is answered by a committed spike, not by guesswork — it shapes PhaseNode in plan 183-06"
    - "The frozen v11 package name `reactflow` appears nowhere in the tree"
  artifacts:
    - path: "frontend/src/test-utils/mockReactFlow.ts"
      provides: "The official four-mock jsdom recipe, file-local and idempotent"
      exports: ["mockReactFlow"]
    - path: "frontend/src/test-utils/handleSpike.test.tsx"
      provides: "The A1 verdict as an executable assertion"
    - path: "frontend/package.json"
      provides: "The ONE net-new dependency"
      contains: "@xyflow/react"
    - path: "frontend/src/index.css"
      provides: "The xyflow stylesheet, imported above @tailwind base"
      contains: "@xyflow/react/dist/style.css"
  key_links:
    - from: "frontend/src/index.css"
      to: "@xyflow/react/dist/style.css"
      via: "CSS @import as statement #1"
      pattern: "^@import \"@xyflow/react/dist/style.css\";"
    - from: "frontend/src/test-utils/handleSpike.test.tsx"
      to: "frontend/src/test-utils/mockReactFlow.ts"
      via: "file-local import (never setupTests.ts)"
      pattern: "mockReactFlow"
---

<objective>
Stand up the `@xyflow/react` toolchain and the jsdom test harness this phase depends on, and
resolve the one MEDIUM-confidence research assumption (A1) that shapes the node component.

Purpose: every later plan in this phase either imports xyflow types, renders `<ReactFlow>` in
jsdom, or needs the stylesheet loaded. Landing all three here — and proving the build BEFORE any
component work — means a Vite-8/ESM surprise (A4) is discovered in one small commit instead of
inside a half-written canvas.
Output: the installed dependency with a green `tsc -b` + `vite build`, the stylesheet wired at the
one correct place, a file-local jsdom mock helper, and a committed spike answering A1.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-read-only-canvas/183-CONTEXT.md
@.planning/phases/183-read-only-canvas/183-RESEARCH.md
@.planning/phases/183-read-only-canvas/183-PATTERNS.md
@.planning/phases/183-read-only-canvas/183-VALIDATION.md
</context>

<interfaces>
<!-- Extracted so the executor does not have to go looking. -->

`@xyflow/react@12.11.2` registry facts (verified in RESEARCH §Standard Stack, do not re-audit):
- licence MIT · published 2026-07-06 · `scripts.postinstall` absent
- `peerDependencies: { react: ">=17", react-dom: ">=17", @types/react: ">=17", @types/react-dom: ">=17" }` — React 19.2.4 is in range
- `exports`: `"."`, `"./dist/style.css"`, `"./dist/base.css"`
- slopcheck `[OK]` when run with `--ecosystem npm` (the bare run reports a false `[SLOP]` because
  the repo root is auto-detected as PyPI) → **no legitimacy checkpoint is required for this phase**

`frontend/src/index.css` today (the constraint):
```
1  @tailwind base;
2  @tailwind components;
3  @tailwind utilities;
4
5  @layer base {
6    :root {
```

`frontend/src/setupTests.ts` today loads ONLY `@testing-library/jest-dom` and `vitest-axe/matchers`.
There is no `ResizeObserver` shim anywhere in `frontend/src`, and no `test-utils/`, `__mocks__/`, or
`helpers/` directory exists (PATTERNS §No Analog Found) — this plan creates the directory.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install @xyflow/react and prove the toolchain (assumption A4)</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - `frontend/package.json` — confirm React 19.2.4 / Vite 8 / the current `dependencies` block before editing
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 318-415 (§Standard Stack + §Package Legitimacy Audit) — the registry facts and the completed audit
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 1812-1823 (§Assumptions Log) — A4 and A6
    - `frontend/vite.config.ts` — the plugin set the build must keep working
  </read_first>
  <action>
    From `frontend/`, install the ONE net-new dependency of this milestone: `npm install @xyflow/react@^12.11.2`.
    It must land in `dependencies` (not `devDependencies`) with the caret range `^12.11.2`.

    NEVER the frozen v11 package name `reactflow` — same library, frozen old name; every pre-mid-2024
    web recipe uses it and copying one silently gets a v11 API (default export, `reactflow/dist/style.css`,
    `node.parentNode`, `xPos`/`yPos`, `nodeInternals`). The ROADMAP flags this explicitly.

    Do NOT re-run the package legitimacy audit — RESEARCH already ran the Package Legitimacy Gate
    protocol this session and recorded `[OK]` with no postinstall script (see §Package Legitimacy
    Audit). Verify the postinstall fact locally as a cheap confirmation, then stop.

    Then discharge assumption A4 (no explicit Vite-8 statement exists in the official xyflow docs)
    by running BOTH build gates before any component work in this phase begins. `tsc -b` is not
    `tsc --noEmit` (the v3.3 lesson) — run the project-references build, which is what
    `npm run build` runs.

    Add no other dependency. `zundo` is Phase 184; `elkjs`/`dagre` are Phase 191; `tldraw` is out of scope.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` `dependencies` contains the exact entry `"@xyflow/react": "^12.11.2"`
    - `grep -c '"reactflow"' frontend/package.json` returns 0
    - `cd frontend && npm ls @xyflow/react` reports a resolved version matching `12.11.*`
    - `cd frontend && node -e "const p=require('@xyflow/react/package.json');console.log(p.scripts&&p.scripts.postinstall?'POSTINSTALL':'none')"` prints `none`
    - `cd frontend && npx tsc -b` exits 0
    - `cd frontend && npx vite build` exits 0
    - `git status --porcelain` shows only `frontend/package.json` and `frontend/package-lock.json` changed by this task
  </acceptance_criteria>
  <done>The dependency is installed at ^12.11.2, no `reactflow` anywhere, and both build gates are green.</done>
</task>

<task type="auto">
  <name>Task 2: Load the mandatory xyflow stylesheet at index.css line 1</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - `frontend/src/index.css` lines 1-30 — the `@tailwind` directives and the `@layer base` token block that must stay unchanged
    - `frontend/postcss.config.js` — the tailwindcss + autoprefixer pipeline
    - `frontend/src/main.tsx` — where `./index.css` is imported (do NOT add a second stylesheet import here)
    - `.planning/phases/183-read-only-canvas/183-PATTERNS.md` §Blocking Findings row **F-4**
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 1218-1234 (Pitfall 2)
  </read_first>
  <action>
    Without `@xyflow/react/dist/style.css` the canvas renders as unpositioned stacked divs and React
    Flow logs "It seems that you haven't loaded the styles." F-4: `frontend/src/index.css` line 1 is
    `@tailwind base;`, and a CSS `@import` must precede every other statement — so the import cannot
    be appended, it must become the new line 1.

    Insert `@import "@xyflow/react/dist/style.css";` as the FIRST statement of
    `frontend/src/index.css`, directly above `@tailwind base;`. Leave every existing line below it
    byte-unchanged.

    Add a short comment above the import recording the tradeoff explicitly, because it is a real one:
    this loads the (small) xyflow stylesheet eagerly for every user INCLUDING flag-off, which is the
    documented-correct trade — plan 183-07 defers the ~59 KB of JS behind `React.lazy`, and the CSS
    must load above `@tailwind base` so the app's Deep Midnight `@layer base` tokens still win the
    cascade. The alternative (importing the stylesheet inside the lazily-imported module) defers both
    but makes the cascade order relative to Tailwind utilities non-deterministic, which is the worse
    risk on a themed surface.

    Do NOT import the stylesheet from `main.tsx`, from `App.tsx`, or from any component file — the
    official guidance is explicit against that. The import lives in exactly one place.

    The flag-off observable-byte-identity contract (D-181-07 / D-183-03) is unaffected: this
    stylesheet only carries `.react-flow*` / `--xy-*` rules, and with the flag off no element
    carrying those classes ever enters the DOM.
  </action>
  <verify>
    <automated>cd frontend && npx vite build && grep -l "react-flow" dist/assets/*.css</automated>
  </verify>
  <acceptance_criteria>
    - line 1 of `frontend/src/index.css` is exactly `@import "@xyflow/react/dist/style.css";` (a comment may sit above it only if the comment precedes no other statement — prefer comment-then-import as the first two lines)
    - the `@tailwind base;` / `@tailwind components;` / `@tailwind utilities;` trio still appears in that order immediately after the import
    - `cd frontend && npx vite build` exits 0
    - at least one file matching `frontend/dist/assets/*.css` contains the literal string `react-flow`
    - `grep -rn "@xyflow/react/dist/style.css" frontend/src --include=*.ts --include=*.tsx` returns 0 hits (the import exists only in `index.css`)
    - `git diff --stat frontend/src/index.css` shows insertions only, 0 deletions of existing token lines
  </acceptance_criteria>
  <done>The stylesheet is imported once, above `@tailwind base`, and a built CSS asset provably contains the React Flow rules.</done>
</task>

<task type="auto">
  <name>Task 3: jsdom mock helper + the A1 handle spike</name>
  <files>frontend/src/test-utils/mockReactFlow.ts, frontend/src/test-utils/handleSpike.test.tsx</files>
  <read_first>
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 1500-1545 — the official four-mock recipe, verbatim from the React Flow testing guide; copy it exactly
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 1247-1264 (Pitfall 4) and 1826-1834 (Open Question 1) — the A1 statement of the unknown
    - `frontend/src/setupTests.ts` — confirm it contains only jest-dom + vitest-axe, and that nothing is added to it here
    - `frontend/vitest.config.ts` — the jsdom environment, `globals: true`, the `@` alias, and the `unplugin-icons` plugin
    - `frontend/src/components/workflows/PhaseSpineGraph.test.tsx` lines 14-19 and 120-127 — the house import style and the dynamic `@testing-library/user-event` idiom
  </read_first>
  <action>
    Create `frontend/src/test-utils/` (no shared test-helper directory exists anywhere in
    `frontend/src` — this is net-new structure, stated plainly rather than pretending a convention
    exists) and add `mockReactFlow.ts`.

    `mockReactFlow.ts` exports one function, `mockReactFlow()`, implementing the four official mocks
    verbatim: a `ResizeObserver` class whose `observe(target)` invokes the stored callback inside
    `setTimeout(..., 0)`; a `DOMMatrixReadOnly` class that parses a `scale(n)` substring out of the
    transform string into `m22` (defaulting to 1); `Object.defineProperties` on
    `global.HTMLElement.prototype` giving `offsetHeight` / `offsetWidth` getters that return
    `parseFloat(this.style.height)` / `parseFloat(this.style.width)` or `1`; and a
    `SVGElement.prototype.getBBox` stub returning zeroed x/y/width/height. Keep the module-level
    `init` boolean guard so repeated per-file imports are idempotent.

    Do NOT put any of this in `frontend/src/setupTests.ts`. `Object.defineProperties` on
    `HTMLElement.prototype` is a global mutation that would reach all 205 suites, and the measured
    baseline is already flaky at 28-35 failures of 1877 — perturbing it globally would destroy the
    failing-name differential this phase grades on.

    Then write `handleSpike.test.tsx` to answer assumption A1 — the one MEDIUM-confidence claim that
    shapes `PhaseNode` in plan 183-06. The docs say custom nodes replace the default node including
    its handles, but never state the "no handle ⇒ no edge rendered" consequence in as many words.
    Render the SAME two-node / one-edge fixture twice through `<ReactFlow>` with a module-level
    `nodeTypes` map: once with a custom node component that renders NO `<Handle>` at all, and once
    with a component rendering `<Handle type="target" position={Position.Left} />` plus
    `<Handle type="source" position={Position.Right} />`. Call `mockReactFlow()` first, give the
    wrapper an explicit inline width and height (React Flow requires a measured parent), and use
    `waitFor` to count `document.querySelectorAll(".react-flow__edge")` in each case — the mocked
    `ResizeObserver` fires on a `setTimeout(..., 0)`, so a synchronous count is unreliable.

    Assert CONCRETE integers for both counts (whatever the spike actually measures), not a loose
    `toBeGreaterThanOrEqual(0)`. Add a one-line comment above the assertions stating the A1 verdict in
    plain words — "a handle-free custom node renders N edge(s)" — because plan 183-06 reads this file
    to decide whether `PhaseNode` must render hidden handles.

    This spike is a permanent committed test, not a throwaway: it is the machine-readable record of
    the answer, and it re-fails if a future xyflow upgrade changes the behaviour.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/test-utils/handleSpike.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/test-utils/mockReactFlow.ts` exists and exports `mockReactFlow`
    - the helper declares all four mocks: `ResizeObserver`, `DOMMatrixReadOnly`, the `offsetHeight`/`offsetWidth` defineProperties, and `getBBox`
    - `grep -c "ResizeObserver" frontend/src/setupTests.ts` returns 0
    - `grep -c "mockReactFlow" frontend/src/setupTests.ts` returns 0
    - `cd frontend && npx vitest run src/test-utils/handleSpike.test.tsx` passes
    - the spike asserts a literal integer for BOTH the handle-free and the handled edge count (`grep -E "toBe\([0-9]+\)" frontend/src/test-utils/handleSpike.test.tsx` returns at least 2 matches)
    - the spike file contains a comment stating the A1 verdict in prose
    - `cd frontend && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>The jsdom helper exists file-locally, `setupTests.ts` is untouched, and A1 is answered by a green spike with concrete numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine / CI | A net-new third-party package and its transitive tree enter the build |
| bundled CSS → rendered page | Third-party stylesheet rules join the app's cascade |

No user-input boundary is crossed by this plan: no route, no migration, no authz decision, no new
data path, no external input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-SC | Tampering | `npm install @xyflow/react@^12.11.2` | mitigate | Package Legitimacy Gate already run in RESEARCH §Package Legitimacy Audit: MIT, slopcheck `[OK]` under `--ecosystem npm`, `scripts.postinstall` absent (re-confirmed in Task 1's acceptance criteria), package name discovered from official docs not a search result. `[OK]` ⇒ no blocking human checkpoint required. |
| T-183-05 | Tampering | third-party CSS entering the cascade | accept | `@xyflow/react/dist/style.css` scopes its rules to `.react-flow*` selectors and `--xy-*` custom properties; it is imported ABOVE `@tailwind base` so the app's own `@layer base` Deep Midnight tokens win any overlap. Verified by the Task 2 build assertion. |

## ASVS conclusion (carried from RESEARCH §Security Domain, line 1759)

The ROADMAP flags Phase 183 "no threat model" and the research concurs after a full ASVS-L1 walk:
V2 Authentication — not applicable (no auth surface). V3 Session Management — not applicable
(nothing stored). V4 Access Control — inherited, not extended (this plan adds no gated route).
V5 Input Validation — not applicable to this plan (no input surface; totality lands in 183-02/05/06).
V6 Cryptography — not applicable.

**If any task in this plan grows a backend source touch, this conclusion is VOID** and
`/gsd:secure-phase` applies. This plan's only permitted footprint is `frontend/`.
</threat_model>

<verification>
- `cd frontend && npx tsc -b` exits 0
- `cd frontend && npx vite build` exits 0 and a `dist/assets/*.css` asset contains `react-flow`
- `cd frontend && npx vitest run src/test-utils` passes
- `cd frontend && npx vitest run src/components/workflows` produces no NEW failing test names versus the recorded baseline, and no drop in total test count
</verification>

<success_criteria>
- `@xyflow/react@^12.11.2` is a `dependencies` entry; `reactflow` appears nowhere
- `frontend/src/index.css` imports the xyflow stylesheet as its first statement and a built CSS asset proves it
- `frontend/src/test-utils/mockReactFlow.ts` exists; `frontend/src/setupTests.ts` is byte-unchanged
- `handleSpike.test.tsx` is green and records the A1 verdict as literal integers plus a prose line
</success_criteria>

<output>
Create `.planning/phases/183-read-only-canvas/183-01-SUMMARY.md` when done.
Record in it: the A1 verdict (handle-free edge count vs handled edge count) verbatim, because plan
183-06 depends on it; and the `vite build` chunk listing, because plan 183-07 measures the lazy-split
against it (assumption A6).
</output>
