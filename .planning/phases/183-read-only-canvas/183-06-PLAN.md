---
phase: 183-read-only-canvas
plan: 06
type: execute
wave: 3
depends_on: ["183-01", "183-05"]
files_modified:
  - frontend/src/components/workflows/PhaseNode.tsx
  - frontend/src/components/workflows/WorkflowCanvas.tsx
  - frontend/src/components/workflows/WorkflowCanvas.test.tsx
autonomous: true
requirements: [CANVAS-01]

must_haves:
  truths:
    - "CANVAS-01 / SC#1: a workflow renders as an `@xyflow/react` node canvas — nodes are phases, edges are the `phase_index` flow plus parsed `skip_to_phase` branches"
    - "SC#3: the canvas is read-only — no node is draggable, no connection can be made, and `<Controls showInteractive={false}>` removes the interactivity lock button that would otherwise re-enable dragging in two clicks (Pitfall 1)"
    - "D-183-05: clicking a node fires the EXISTING `onSelectNode(slug)` contract with `node.id === phase.slug`; selection never reorders and never moves a node"
    - "D-183-11: a zero-phase definition renders a named empty state with ALL canvas chrome suppressed — no `.react-flow` root is mounted at all, no grid, no zoom controls, no minimap, and no ghost placeholder node"
    - "D-183-08: ONE canvas-level ⌥ Technical names control, reading the app-wide shipped `TechnicalNamesProvider` so two toggles can never disagree — it flips every node at once"
    - "D-183-07: exactly ONE grounding badge on every node face, said in words with a glyph beside it (never colour alone), plus the 'Waits for you' badge on `llm_human_input` phases only"
    - "D-183-10: an unresolvable `skip_to_phase` renders a visibly broken reference marker naming the declared target, connected to no phase node"
    - "D-183-14: 183 ships ONLY the canvas-local icon-well lightening — no `PHASE_GLYPHS` slug is swapped, so no shipped surface changes and a canvas rollback cannot silently revert an app-wide icon decision"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseNode.tsx"
      provides: "The three presentational node components: the phase card, the broken-reference marker, the end cap"
      exports: ["PhaseNode", "UnresolvedSkipNode", "EndCapNode"]
      min_lines: 120
    - path: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      provides: "The read-only <ReactFlow> shell, the empty state, the chrome, and the ⌥ control"
      exports: ["WorkflowCanvas"]
      min_lines: 100
    - path: "frontend/src/components/workflows/WorkflowCanvas.test.tsx"
      provides: "The DOM half — drag-free, showInteractive, empty state, one tab stop, selection, a11y"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      to: "frontend/src/components/workflows/canvasModel.ts"
      via: "toCanvas(phases) inside useMemo"
      pattern: "toCanvas\\("
    - from: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      to: "frontend/src/providers/TechnicalNamesProvider.tsx"
      via: "useTechnicalNamesOptional — the shipped app-wide single source"
      pattern: "useTechnicalNamesOptional"
    - from: "frontend/src/components/workflows/PhaseNode.tsx"
      to: "frontend/src/lib/phaseGlyph.tsx"
      via: "phaseGlyph(data.phaseType) with the PHASE_GLYPHS unicode fallback"
      pattern: "phaseGlyph\\("
---

<objective>
Render the projection: the frosted-glass phase node, the honest broken-reference marker, the end cap,
and the `<ReactFlow>` shell locked down for read-only.

Purpose: every one of xyflow's interaction flags defaults to TRUE, and the default `<Controls>` ships
an interactivity lock button whose handler sets `nodesDraggable` / `nodesConnectable` /
`elementsSelectable` to `!isInteractive` — two clicks turn a shipped read-only canvas into a
draggable one. Read-only here is opt-OUT, not opt-in, and it has to be asserted rather than assumed.
Sketch 137-D is the locked acceptance bar: frosted depth, the 3D mark floating at the left edge, plain
language by default with technical names behind the ⌥ reveal, and the colour budget spent on nothing
but a tint behind the icon.
Output: the node components, the canvas shell, and the DOM assertions the pure model cannot make.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-read-only-canvas/183-CONTEXT.md
@.planning/phases/183-read-only-canvas/183-RESEARCH.md
@.planning/phases/183-read-only-canvas/183-PATTERNS.md
@.planning/phases/183-read-only-canvas/183-VALIDATION.md
@.planning/phases/183-read-only-canvas/183-01-SUMMARY.md
@.planning/phases/183-read-only-canvas/183-05-SUMMARY.md
@.planning/sketches/137-agentic-canvas-look/README.md
@.planning/sketches/136-flow-shape-and-branches/README.md
@.planning/sketches/135-phase-node-anatomy/README.md
@.planning/sketches/134-canvas-frame-and-read-only/README.md
@.claude/get-shit-done/references/icon-convention.md
@frontend/src/components/workflows/canvasModel.ts
@frontend/src/components/workflows/phaseVocabulary.ts
</context>

<interfaces>
<!-- The exact prop set and its VERIFIED defaults. Every flag below defaults to TRUE unless stated. -->

| Prop | xyflow default | Set to in 183 | Why |
|---|---|---|---|
| `nodesDraggable` | `true` | `false` | SC#3 "nodes not draggable" |
| `nodesConnectable` | `true` | `false` | No authoring in 183 |
| `edgesReconnectable` | `true` | `false` | No authoring in 183 |
| `connectOnClick` | `true` | `false` | A click must only select |
| `edgesFocusable` | `true` | `false` | An edge is not a target; halves the tab stops |
| `deleteKeyCode` | `'Backspace'` | `null` | Backspace on a focused node would fire a delete intent |
| `elementsSelectable` | `true` | keep `true` | Drives Enter/Space selection and the `.selected` class |
| `nodesFocusable` | `true` | keep `true` | `false` strips `tabIndex` AND `role` entirely, killing keyboard reachability |
| `panOnDrag` | `true` | keep `true` | Sketch 134-C cursor contract: the plane pans, nodes don't move |
| `zoomOnDoubleClick` | `true` | `false` | A double-click on a node is a plausible mis-click |
| `minZoom` / `maxZoom` | `0.5` / `2` | `0.3` / `2` | A 1,600px 5-phase flow in a ~900px column needs ~0.55×; the 0.5 floor has no margin (Pitfall 5, UAT row U-2) |
| `colorMode` | `'light'` | `"dark"` | Deep Midnight; the bundled stylesheet themes for light by default (Pitfall 6) |
| `onlyRenderVisibleElements` | `false` | leave `false` | Max 5 nodes |
| `nodeOrigin` | `[0,0]` | leave | Makes the fixed edge-anchor arithmetic trivial |

`onNodeClick` fires OUTSIDE the `isSelectable` guard (verified in xyflow's `NodeWrapper`), so
`onNodeClick={(_, node) => onSelectNode(node.id)}` works regardless of the selectability choice.

The props contract to match EXACTLY so the canvas is a substitutable peer of the Spine
(`PhaseSpineGraph.tsx:93-99`):
```ts
export interface PhaseSpineGraphProps {
  phases: PhaseSpecJSON[]
  /** The currently-selected phase slug (the open form anchor), or null at rest. */
  selectedSlug: string | null
  /** Selection only — NEVER reorders. Fires the clicked phase's slug. */
  onSelectNode: (slug: string) => void
}
```

The shipped badge primitive to REUSE, not clone (`frontend/src/components/org/StatusChip.tsx`):
```ts
export type ChipTone = "primary" | "success" | "muted"
const BASE_CLASSES = "inline-flex items-center rounded-full border px-2 py-0.5 text-[11px] font-medium"
export function StatusChip({ tone, children, testId }: StatusChipProps)
```
Its docblock separates the shared COMPONENT (the cohesion win) from the tone MAPPING (domain-specific);
`OrgMembersTab.tsx:57` proves a second domain feeding the same component. A third inline pill would be
exactly the fork Phase 177 retired.

The shipped ⌥ control to REUSE, not clone (`frontend/src/components/admin/TechnicalNamesToggle.tsx:22-38`)
— exported, prop-controlled, `aria-pressed`, the parent owns the state.

The canonical 3D-glyph render (`PhaseSpine.tsx:87-89`) — `PHASE_GLYPHS[type]` is a fluent-emoji SLUG
string since Phase 127, so `"•"` is the real visual fallback:
```tsx
<span aria-hidden="true">{Glyph ? <Glyph /> : (PHASE_GLYPHS[type] ?? "•")}</span>
```

The XSS clause every workflow surface carries verbatim (`PhaseSpine.tsx:13-14`):
```
 * XSS (T-124-01): every authored string (phase names) is rendered as a plain React
 * text child / `title=` attribute value — never `dangerouslySetInnerHTML`.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PhaseNode.tsx — the phase card, the broken-reference marker, the end cap</name>
  <files>frontend/src/components/workflows/PhaseNode.tsx</files>
  <read_first>
    - `.planning/phases/183-read-only-canvas/183-01-SUMMARY.md` — the A1 spike verdict (does a `<Handle>`-free custom node render edges?). This decides whether hidden handles are mandatory
    - `frontend/src/components/panel/PhaseCard.tsx` lines 255-394 — the only shipped component rendering a 3D phase glyph plus a card surface; the Aether / Deep Midnight idiom to match. Anchor to its QUIET/idle class vocabulary (`border-border/50 bg-card/30`), not its run-status colours
    - `frontend/src/components/workflows/PhaseSpine.tsx` lines 13-14 and 80-92 — the XSS clause and the canonical glyph render
    - `frontend/src/components/org/StatusChip.tsx` — the badge primitive to import
    - `frontend/src/components/workflows/WorkflowSoul.tsx` lines 93-108 — the glyph-plus-WORD never-colour-alone chip with its `data-*` assertion hook
    - `frontend/src/components/workflows/PhaseSpineGraph.tsx` lines 216-230 — the shipped honest skip-branch row: amber, dashed, `font-mono` slug, a bare-English sentence
    - `frontend/src/components/workflows/canvasModel.ts` — `PhaseNodeData`, `UnresolvedSkipNodeData`, `CANVAS_LAYOUT.EDGE_ANCHOR_Y`
    - `frontend/tailwind.config.js` and `frontend/src/index.css` — the semantic token names; note `panel-status-*` is scoped to `components/panel/*` only and must NOT be used here
    - `.planning/sketches/137-agentic-canvas-look/README.md` — winner D, the icon luminance table, and the icon-well decision
  </read_first>
  <action>
    Create `frontend/src/components/workflows/PhaseNode.tsx` exporting three presentational
    components: `PhaseNode`, `UnresolvedSkipNode` and `EndCapNode`. They are the values of the
    `nodeTypes` map plan 183-06 Task 2 declares at module scope.

    `PhaseNode` takes `NodeProps` and reads `data` as `PhaseNodeData`. Structure per sketch 137-D:
    the 3D mark floating at the LEFT edge with its own contact shadow and a soft light disc behind it;
    one plain-language title; one supporting line; at most two word-badges. Frosted-glass card,
    NEUTRAL — per-step-type colour is a tint behind the icon ONLY.

    Title: render `data.technicalTitle` when the ⌥ reveal is on, otherwise `data.title`. The reveal
    boolean arrives as a field on `data` (set by the canvas shell in Task 2) so `PhaseNode` stays a
    pure presentational leaf with no context dependency of its own — that keeps it renderable in
    isolation and avoids a second technical-names state. Subtitle: `data.subtitle`, rendered only when
    non-empty.

    Glyph: `const Glyph = phaseGlyph(data.phaseType)`, rendered as
    `Glyph ? <Glyph className="h-5 w-5" /> : (PHASE_GLYPHS[data.phaseType] ?? "•")` inside an
    `aria-hidden="true"` wrapper — the `PhaseSpine.tsx:87-89` pattern. Import `PHASE_GLYPHS` from
    `@/components/workflows/soulData`; do NOT declare a local map.

    D-183-14 — the in-scope icon fix and the fence: `llm_batch_agents`'s `busts-in-silhouette` mark
    measures 34.5 average luminance against the set's 135-185, so it reads as a dark silhouette on a
    dark canvas. Fix it CANVAS-LOCALLY by lightening the icon well: a soft light disc / raised
    background behind the floating mark, applied uniformly to every node so it reads as depth rather
    than as a special case. Do NOT swap any `PHASE_GLYPHS` slug — not `llm_agent` to `compass`, not
    `llm_batch_agents` to `handshake`. Those are one-line map changes that alter five shipped surfaces
    (the workflows-page card, the run and publish soul headers, the gauntlet stages, the live step
    cards) and D-183-14 reserves them for their own dedicated commit with their own before/after check,
    so a canvas rollback cannot silently revert an app-wide icon decision.

    Badges, D-183-07, at most two and spent deliberately. Slot 1 is grounding, ALWAYS present: import
    `StatusChip` from `@/components/org/StatusChip` and feed it a canvas-domain tone mapping (the
    documented reuse shape — the shared component, a domain-specific mapper), rendering
    `data.grounding.glyph` beside `data.grounding.words` with the glyph `aria-hidden` and the WORD
    carrying the meaning (WCAG 1.4.1 — never colour alone). Carry a `data-grounding={data.grounding.mode}`
    attribute as the assertion hook, mirroring `WorkflowSoul.tsx:93-108`'s `data-tier`. Slot 2 is
    "Waits for you", rendered ONLY when `data.waitsForYou` is true. Every other node face stays at ONE
    badge. Do NOT add tool chips, gate identifiers, or `phase_index` to the face.

    Accessibility, Pattern 3 Option A — choose ONE tab stop per node and never both: the canvas sets
    `nodesFocusable` true and the node OBJECT carries `ariaRole: "button"` and `ariaLabel` (already
    emitted by `canvasModel`), so `PhaseNode` itself must contain NO interactive elements. No
    `<button>`, no `<a>`, no `tabIndex`, no `onClick` inside the node — `nodesFocusable={true}` plus an
    inner `<button>` produces two tab stops per node, ten tab presses to traverse `eval_coverage`, and
    a screen reader announcing every step twice. Selection click is handled by the canvas's
    `onNodeClick`.

    Handles: follow the A1 spike verdict recorded in `183-01-SUMMARY.md`. If edges require handles,
    render `<Handle type="target" position={Position.Left} />` and
    `<Handle type="source" position={Position.Right} />`, made non-interactive by the canvas's
    `nodesConnectable={false}` and hidden visually (opacity 0, zero border) per sketch 134's finding
    that visible connection handles read as a broken editor. Position them with an explicit
    `style={{ top: CANVAS_LAYOUT.EDGE_ANCHOR_Y }}` rather than the default 50% — that IS D-183-12's
    "edges anchor to a fixed offset from the node top", and it is what lets a gate-heavy tall card grow
    downward without moving the edge baseline.

    Selection affordance: read `selected` from `NodeProps` and apply the shipped ring idiom
    (`border-primary` plus `shadow-[0_0_0_1px_hsl(var(--primary)/0.4)]`, as at `PhaseSpineGraph.tsx:193`).
    Motion: keys off RUN STATE, never off selection — 183 has no run state, so the node itself must be
    completely still. Any ambient motion belongs to the backdrop in Task 2 and must sit behind a
    `motion-safe:` prefix.

    Data hooks the tests depend on: `data-testid={`canvas-node-${data.slug}`}`, `data-slug`,
    `data-phase-type`, `data-grounding`, and `data-selected`. `data-phase-type` is the durable hook
    (`PhaseSpine.test.tsx:38-45` documents it as the post-3D-swap migration target) and it MUST be
    present or the canvas has no equivalent of the Spine's glyph assertion.

    `UnresolvedSkipNode` (D-183-10) renders the honest broken reference: extend the shipped vocabulary
    at `PhaseSpineGraph.tsx:216-230` — amber for needs-attention, a dashed border for a conditional
    branch, `font-mono` for the slug, and a bare-English sentence carrying the meaning. Wording must
    agree with the backend's `UNSATISFIABLE_SKIP` verdict: it goes to the declared target and no such
    step exists. Carry `data-testid="canvas-unresolved-skip"`, `data-from-slug` and
    `data-target-slug`. Not selectable, not focusable.

    `EndCapNode` renders the explicit `○ end` cap sketch 136 requires — a small, quiet terminal mark
    with an accessible label, `data-testid="canvas-end-cap"`. Never a dangling edge stub.

    Styling vocabulary: semantic tokens only, never a raw hex and never a new colour —
    `bg-card/30`, `border-border/50`, `text-foreground`, `text-muted-foreground`, `backdrop-blur-sm`
    for the frosted surface, and `accent-violet` as the single shipped type-tint precedent. Use
    `cn` from `@/lib/utils` for conditional variants. Do NOT use any `panel-status-*` token
    (`tailwind.config.js` scopes those to `components/panel/*`).

    XSS: copy the T-124-01 clause verbatim into the docblock. Every authored string — the phase name,
    the slug, the declared skip target — is a plain React text child or an attribute value.
    `dangerouslySetInnerHTML` appears nowhere.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/workflows/canvasModel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/workflows/PhaseNode.tsx` exports `PhaseNode`, `UnresolvedSkipNode`, `EndCapNode`
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/workflows/PhaseNode.tsx` returns 0
    - `grep -c "const PHASE_GLYPHS" frontend/src/components/workflows/PhaseNode.tsx` returns 0 and `grep -c "soulData" frontend/src/components/workflows/PhaseNode.tsx` returns ≥ 1
    - `grep -cE "<button|tabIndex|role=\"button\"" frontend/src/components/workflows/PhaseNode.tsx` returns 0 (Pattern 3 Option A — no interactive element inside the node)
    - `grep -c "panel-status" frontend/src/components/workflows/PhaseNode.tsx` returns 0
    - `grep -cE "compass|handshake" frontend/src/components/workflows/PhaseNode.tsx` returns 0 (D-183-14 fence)
    - `grep -c "StatusChip" frontend/src/components/workflows/PhaseNode.tsx` returns ≥ 1 (reuse, not a third inline pill)
    - `grep -cE "data-phase-type|data-grounding|data-slug|data-testid" frontend/src/components/workflows/PhaseNode.tsx` returns ≥ 4
    - the docblock contains the verbatim T-124-01 XSS clause
    - `cd frontend && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>Three presentational node components exist, matching 137-D, reusing the shipped badge and glyph infrastructure, with zero interactive elements inside a node.</done>
</task>

<task type="auto">
  <name>Task 2: WorkflowCanvas.tsx — the read-only shell, the empty state, the chrome, the ⌥ control</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx</files>
  <read_first>
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 1014-1145 (Patterns 1-5) and 1185-1320 (Pitfalls 1-8) — the verified defaults, the `<Controls>` lock-button source, and the empty-state branch
    - `frontend/src/components/workflows/PhaseSpineGraph.tsx` lines 93-141 — the props contract to match exactly, the `flex h-full min-w-0 flex-col` framing, and the shipped `👁 View only` badge
    - `frontend/src/components/workflows/PhaseSpine.tsx` lines 42-48 — the shipped early-return empty state
    - `frontend/src/providers/TechnicalNamesProvider.tsx` lines 90-106 — the throwing vs optional accessors
    - `frontend/src/components/admin/TechnicalNamesToggle.tsx` — the exported, prop-controlled ⌥ control to reuse
    - `frontend/src/components/admin/ControlRoomPage.tsx` lines 79-80, 243, 708-711 — the shipped wiring pattern: read the context, feed the toggle, thread the boolean down as a prop
    - `frontend/src/components/workflows/canvasModel.ts` — `toCanvas`, `CANVAS_LAYOUT`, the node/edge `type` vocabulary
    - `frontend/src/pages/KnowledgeHealthPage.tsx` lines 26-37 — the one shipped `React.lazy` precedent (context for plan 183-07's split; this file is the lazy TARGET)
  </read_first>
  <action>
    Create `frontend/src/components/workflows/WorkflowCanvas.tsx` exporting `WorkflowCanvas` with a
    props interface IDENTICAL to `PhaseSpineGraphProps` — `phases`, `selectedSlug`, `onSelectNode` —
    so it is a drop-in peer at the Builder's mount site and the page keeps owning the
    toggle-off-on-reclick selection semantics.

    Declare `const nodeTypes = { phase: PhaseNode, unresolvedSkip: UnresolvedSkipNode, endCap: EndCapNode }`
    at MODULE scope, never inside the component (Pattern 4 — React Flow otherwise emits "It looks like
    you have created a new nodeTypes object" and re-renders every node on each parent render).

    Read the ⌥ reveal with `useTechnicalNamesOptional()` from `@/providers/TechnicalNamesProvider`
    (D-183-08). This is deliberate and it resolves PATTERNS finding F-3: the provider is already
    mounted at `App.tsx:252` wrapping `<ChatLayout>`, which renders the workflows view, so the Builder
    is ALREADY inside it, and a shipped `TechnicalNamesToggle` control already exists. A canvas-local
    `useState` would reintroduce the exact "two toggles disagree" failure the provider's own docblock
    says it exists to prevent. D-183-08's intent — one state, one assertion, flips every node at once —
    is satisfied better by the app-wide provider. Do NOT add a second technical-names state anywhere.
    Render the shared `TechnicalNamesToggle` in the canvas header, wired to the context's `showTechnical`
    and `toggle`, and render it only when the context is present so no dead control ships; a
    provider-less render (unit tests, isolated renders) falls back to plain language with no control
    and must not crash.

    Compute `const { nodes, edges } = useMemo(() => toCanvas(phases), [phases])`. Apply selection in
    the VIEW, not the model: map the memoized nodes to set `selected: node.id === selectedSlug` and
    merge the `technical` boolean into each phase node's `data` so `PhaseNode` stays a pure leaf. Keep
    that mapping memoized on `[nodes, selectedSlug, showTechnical]`.

    D-183-11 — the empty state returns EARLY, above `<ReactFlow>`: when there are no phase nodes,
    render one honest line on a plain surface ("No steps yet" plus what to do about it) with
    `data-testid="canvas-empty"` and return. That makes the suppression STRUCTURAL rather than five
    separate `false` props — no plane, no dot grid, no zoom controls, no minimap, and no ghost or
    placeholder first node (a ghost node on a read-only canvas implies an affordance that does not
    exist). It also sidesteps the "parent container needs a width and a height" warning path for what
    is the single most common canvas state, and it makes the D-183-11 assertion the same one-liner as
    the D-183-03 flag-off assertion: no `.react-flow` element in the container.

    Non-empty: wrap `<ReactFlow>` in a `div` with an explicit `h-full w-full min-w-0` (Pitfall 3 — the
    parent must have a width and a height) and set the full read-only prop set from the table in
    `<interfaces>`: `nodesDraggable={false}`, `nodesConnectable={false}`, `edgesReconnectable={false}`,
    `connectOnClick={false}`, `edgesFocusable={false}`, `deleteKeyCode={null}`,
    `zoomOnDoubleClick={false}`, keeping `elementsSelectable`, `nodesFocusable` and `panOnDrag` at
    their true defaults, with `fitView`, `fitViewOptions={{ padding: 0.1, minZoom: 0.3 }}`,
    `minZoom={0.3}` and `colorMode="dark"`. Wire
    `onNodeClick={(_, node) => onSelectNode(node.id)}` — D-183-05; the callback fires outside xyflow's
    selectability guard, and `node.id === phase.slug` is what carries SC#3 end to end. Guard it so a
    click on the end-cap or an unresolved-skip stub does not fire `onSelectNode` (those ids are not
    phase slugs).

    Chrome: render `<Background />` and `<Controls showInteractive={false} />`. The `showInteractive`
    prop is NOT optional polish — `<Controls>` defaults it to true, and its handler sets
    `nodesDraggable`, `nodesConnectable` and `elementsSelectable` all to `!isInteractive`, so with this
    phase's prop set one click kills selection and a second click turns dragging AND connecting back
    ON. Two clicks from a shipped read-only canvas to a broken one. Do NOT render a `<MiniMap>` in 183
    — the deepest real workflow is five nodes and a minimap on five nodes is noise (sketch 134's
    quiet-until-touched finding). Do NOT pass `proOptions={{ hideAttribution: true }}`: leave the React
    Flow attribution visible and raise its removal as a separate licensing decision if the operator
    objects at UAT.

    Arrow markers and edge styling live HERE, not in the model: set `defaultEdgeOptions` for the arrow
    marker and give skip edges a dashed stroke, keyed off each edge's `data.kind` (`"flow"` / `"skip"`
    / `"end"`).

    Tell read-only as a deliberate MODE, never as a broken editor: reuse the shipped
    `👁 View only` badge vocabulary from `PhaseSpineGraph.tsx:127-129` in the canvas header rather than
    inventing a second phrase, and honour sketch 134-C's cursor contract — the plane pans, nodes do not
    move.

    Motion: ambient only, and behind `motion-safe:`. 183 has no run state, so a selected node must NOT
    be the thing that animates — that was the defect found and fixed in sketch 137 review. Any drift
    belongs to the backdrop.

    Do NOT call `POST /workflows/validate` or any other endpoint, do not render per-node server verdict
    badges, do not import `zundo`, do not read or write a `grounding_mode` field, do not add run state
    or run colour, and do not persist a view preference. Those are Phases 184, 185, 186 and 188.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/workflows/WorkflowCanvas.tsx` exports `WorkflowCanvas` with exactly the props `phases`, `selectedSlug`, `onSelectNode`
    - `grep -c "const nodeTypes" frontend/src/components/workflows/WorkflowCanvas.tsx` returns 1 and the declaration is at module scope (not inside the component function)
    - `grep -c "showInteractive={false}" frontend/src/components/workflows/WorkflowCanvas.tsx` returns 1
    - all six read-only flags are present: `grep -cE "nodesDraggable=\{false\}|nodesConnectable=\{false\}|edgesReconnectable=\{false\}|connectOnClick=\{false\}|edgesFocusable=\{false\}|deleteKeyCode=\{null\}" ...` returns 6
    - `grep -c "colorMode=\"dark\"" frontend/src/components/workflows/WorkflowCanvas.tsx` returns 1
    - `grep -c "MiniMap" frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0
    - `grep -c "hideAttribution" frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0
    - `grep -c "useTechnicalNamesOptional" frontend/src/components/workflows/WorkflowCanvas.tsx` returns 1 and `grep -c "useState" ...` shows no technical-names state
    - `grep -cE "workflows/validate|grounding_mode|zundo|elkjs|reactflow" frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0
    - the empty-state branch returns BEFORE any `<ReactFlow>` JSX (verify by reading the file: the early return is above the `<ReactFlow` element)
    - `cd frontend && npx tsc -b && npx vite build` both exit 0
  </acceptance_criteria>
  <done>The canvas shell renders read-only by explicit opt-out, suppresses all chrome on an empty definition, and reads the app-wide ⌥ state rather than forking it.</done>
</task>

<task type="auto">
  <name>Task 3: WorkflowCanvas.test.tsx — the DOM assertions the pure model cannot make</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseSpineGraph.test.tsx` lines 120-160 — the dynamic `user-event` import idiom and the STATIC drag-free DOM block to mirror
    - `frontend/src/test-utils/mockReactFlow.ts` — the helper from plan 183-01; import it file-locally and call it before the first render
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 1621-1669 (§How to test an `@xyflow/react` canvas) — the layer split and the `waitFor` requirement for edge counts
    - `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts` — reuse the corpus rather than declaring new inline fixtures
    - `frontend/src/setupTests.ts` — confirm `vitest-axe`'s `toHaveNoViolations` is globally extended, so no per-file a11y setup is needed
    - `frontend/src/providers/TechnicalNamesProvider.tsx` — wrap renders that exercise the ⌥ control
  </read_first>
  <action>
    Write `WorkflowCanvas.test.tsx`. Import `mockReactFlow` from `@/test-utils/mockReactFlow` and call
    it at the top of the file — NOT via `setupTests.ts`. Assert only what DOM proves and the pure
    function cannot; the projection's truth is already covered by plan 183-05's three suites.

    Required assertions:

    SC#3a drag-free DOM — mirror `PhaseSpineGraph.test.tsx:129-145`: zero `[draggable="true"]`, zero
    `[draggable]`, zero `[data-connection-handle]`, zero `[data-add-node]`, and no button whose
    accessible name or text contains "add node".

    Pitfall 1 — assert `container.querySelector(".react-flow__controls-interactive")` is null. The
    padlock button must not exist, because two clicks on it would turn dragging and connecting back on.

    D-183-11 empty state — rendering a zero-phase definition yields `canvas-empty`, and
    `container.querySelector(".react-flow")` is NULL, along with no `.react-flow__controls`, no
    `.react-flow__background` and no `.react-flow__minimap`. Also assert no ghost/placeholder node
    exists (`queryAllByTestId(/^canvas-node-/)` is empty).

    D-183-05 selection — a click on a phase node calls `onSelectNode` with exactly that phase's slug;
    a click on the end cap or on an unresolved-skip stub does NOT call it.

    Pattern 3 one-tab-stop — the count of `.react-flow__node[tabindex="0"]` equals the number of phase
    nodes, AND no `<button>`, `<a>` or `[tabindex]` element exists INSIDE any `.react-flow__node`. This
    catches the double-tab-stop mistake immediately.

    D-183-08 ⌥ reveal — rendered inside a `TechnicalNamesProvider`, the canvas shows plain-language
    titles by default and no slug on any node face; after clicking the ⌥ control every node switches to
    the technical form at once (assert on at least two nodes, so "flips every node" is actually tested).
    Rendered WITHOUT a provider the canvas still renders plain titles, shows no ⌥ control, and does not
    throw.

    D-183-07 badges — every phase node carries exactly one `data-grounding` chip; an `llm_human_input`
    phase additionally shows the "Waits for you" badge and no other phase type does.

    D-183-10 broken reference — the unresolvable-skip fixture renders a `canvas-unresolved-skip` marker
    naming the declared target, and no edge in the DOM terminates on a node id that does not exist.

    Edge presence — use `waitFor` around `container.querySelectorAll(".react-flow__edge")` (the mocked
    `ResizeObserver` fires on a `setTimeout(..., 0)`, so a synchronous count is unreliable) and assert
    the count matches the model's edge count for a known fixture.

    Accessibility — `expect(await axe(container)).toHaveNoViolations()` on a rendered non-empty canvas
    and on the empty state.

    Source guard — a `?raw` block asserting `WorkflowCanvas` source contains `showInteractive={false}`,
    contains no `MiniMap`, contains no `hideAttribution`, and matches neither `/workflows\/validate/`
    nor `/grounding_mode/` — the machine-checkable form of this phase's scope fences.

    Reuse `ALL_FIXTURES` / the named fixtures from `__fixtures__/canvasFixtures.ts` rather than
    declaring new inline ones, so the DOM layer and the projection layer can never test different
    shapes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx` green with a distinct test for each of: drag-free, `showInteractive`, empty state, selection, one-tab-stop, ⌥ reveal (both with and without a provider), grounding badge, "Waits for you", broken reference, edge count, a11y, and the source guard
    - `grep -c "mockReactFlow" frontend/src/components/workflows/WorkflowCanvas.test.tsx` returns ≥ 1 and `grep -c "mockReactFlow" frontend/src/setupTests.ts` returns 0
    - `grep -c "canvasFixtures" frontend/src/components/workflows/WorkflowCanvas.test.tsx` returns ≥ 1
    - temporarily removing `showInteractive={false}` from `WorkflowCanvas.tsx` makes this suite fail (confirm once, then revert)
    - temporarily adding an inner `<button>` to `PhaseNode` makes the one-tab-stop test fail (confirm once, then revert)
    - `cd frontend && npx vitest run src/components/workflows` reports zero failures across the directory
    - `cd frontend && npm test` shows no NEW failing test names and no test-count regression versus the recorded baseline
  </acceptance_criteria>
  <done>Read-only, the empty state, the selection contract, the single tab stop, the badges, the broken reference and a11y are all asserted, and the assertions provably fail when the contract is broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow definition JSONB → rendered DOM | Authored `phase.name`, `slug` and declared skip targets reach the page |
| phase_type string → bundled SVG icon path | A type value selects which icon component renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-01 | Tampering | XSS via an authored `phase.name`, slug or declared skip target rendered into a node | mitigate | Every authored string is a plain React text child or an attribute value; `dangerouslySetInnerHTML` appears nowhere in `PhaseNode.tsx` or `WorkflowCanvas.tsx` (asserted by grep in Task 1). The shipped precedent and its verbatim clause are at `PhaseSpine.tsx:13-14` (T-124-01). xyflow's label props take React children, so this holds unchanged. |
| T-183-08 | Tampering | SVG injection via a phase-type-derived icon path | mitigate | Already enforced by `phaseGlyph.tsx:25-33` plus `unplugin-icons`: icons are deep-subpath `~icons/fluent-emoji/<slug>` imports bundled at build time and returned as React components, never interpolated markup; a missing slug fails the build. This plan adds NO new slug (D-183-14 fence, asserted by grep). |
| T-183-11 | Elevation of Privilege | the read-only contract being defeated at runtime by `<Controls>`'s interactivity lock | mitigate | `<Controls showInteractive={false} />` plus a DOM assertion that `.react-flow__controls-interactive` does not exist. Verified against xyflow's `Controls.tsx` source: its handler sets `nodesDraggable`, `nodesConnectable` and `elementsSelectable` to `!isInteractive`, so two clicks would otherwise re-enable dragging and connecting. |
| T-183-04 | Tampering | layout leaking into the definition | mitigate | The view computes nothing that flows back: selection and the ⌥ boolean are applied to a memoized COPY of the model output, and no code path writes to `phases` or to any definition object. Enforced upstream by plan 183-05's purity suite. |
| T-183-06 | Denial of Service | an unknown `phase_type` or malformed `on_failure` crashing the render | mitigate | Totality: `phaseGlyph` returns null for an unmapped type and the `PHASE_GLYPHS[type] ?? "•"` fallback renders; an unknown grounding mode resolves to `open` upstream; a missing subtitle renders nothing. Exercised across the 14-fixture corpus. |

## ASVS conclusion (carried from RESEARCH §Security Domain, line 1759)

V5 Input Validation applies as rendering-safety over already server-validated data. V2, V3 and V6 do
not apply. V4 Access Control is inherited, not extended — this plan adds no route and makes no authz
decision; the flag gate lands in plan 183-07 and is render-only, with `require_visible` /
`require_canvas` remaining the security wall. The ROADMAP's "no threat model" flag for Phase 183 stands.

**Frontend-only.** If any task grows a backend touch, this conclusion is VOID.
</threat_model>

<verification>
- `cd frontend && npx tsc -b && npx vite build` both exit 0
- `cd frontend && npx vitest run src/components/workflows` reports zero failures across the directory
- `cd frontend && npm test` — no NEW failing test names, no test-count regression versus the recorded baseline
- `grep -rn "dangerouslySetInnerHTML" frontend/src/components/workflows` returns 0 hits
- `git status --porcelain backend supabase scripts` empty
</verification>

<success_criteria>
- A non-empty definition renders as an `@xyflow/react` canvas with one node per phase and edges for the flow plus resolved `skip_to_phase` branches
- Read-only is proven: no draggable element, no connection handle affordance, no interactivity lock button
- A zero-phase definition mounts no `.react-flow` root at all — no grid, no controls, no minimap, no ghost node
- Clicking a phase node fires `onSelectNode(slug)`; clicking the cap or a stub does not
- Exactly one tab stop per node; axe reports no violations
- One grounding badge always, "Waits for you" only on `llm_human_input`, no `PHASE_GLYPHS` slug swapped
</success_criteria>

<output>
Create `.planning/phases/183-read-only-canvas/183-06-SUMMARY.md` when done.
Record the final `WorkflowCanvas` props signature and the module path — plan 183-07 lazy-imports this
exact named export.
</output>
