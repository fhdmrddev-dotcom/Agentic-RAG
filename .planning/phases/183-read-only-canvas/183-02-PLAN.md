---
phase: 183-read-only-canvas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/phaseVocabulary.ts
  - frontend/src/components/workflows/phaseVocabulary.test.ts
  - frontend/src/components/workflows/__fixtures__/skipParseCases.json
  - frontend/tsconfig.app.json
  - backend/tests/unit/test_183_skip_parse_parity.py
autonomous: true
requirements: [CANVAS-01]

must_haves:
  truths:
    - "D-183-13: ONE shared phase-vocabulary module exists — the glyph map is imported from soulData, never re-declared, and `parseSkipTarget` / the node-title resolution / the plain-language sentences live in exactly one home"
    - "D-183-15: the client `parseSkipTarget` is byte-semantically the backend `parse_skip_target`, and a parity test pinned to a SHARED case table proves it in BOTH languages — including the operator-approved C-1 correction that `skip_to_phase:a:b` resolves to `a:b`, not `b`"
    - "D-183-06: the node-title fallback is a plain-language business sentence per phase_type; the slug never appears in the default title; a real `phase.name` still wins"
    - "D-183-07: grounding is DERIVED from `citation_policy` plus a `citations_required` validator, carries a glyph beside its words (never colour alone), and `waitsForYou` is true only on `llm_human_input` — no authored `grounding_mode` field is invented (that is Phase 185)"
    - "CANVAS-01: every exported resolver is TOTAL — an unknown phase_type, an unknown citation_policy, an unknown validator kind, a malformed on_failure and a missing validators array all resolve honestly and never throw"
  artifacts:
    - path: "frontend/src/components/workflows/phaseVocabulary.ts"
      provides: "The single shared phase vocabulary: parse, titles, labels, grounding, read shapes"
      exports: ["parseSkipTarget", "nodeTitle", "technicalTitle", "groundingFor", "waitsForYou", "PHASE_TYPE_SENTENCES", "PHASE_TYPE_SUBTITLES", "PHASE_TYPE_LABELS", "SKIP_PREFIX", "PhaseSpecJSON", "PhaseConfigJSON", "ValidatorJSON"]
      min_lines: 120
    - path: "frontend/src/components/workflows/__fixtures__/skipParseCases.json"
      provides: "The ONE canonical C-1 case table, read by both the vitest suite and the pytest suite"
      contains: "skip_to_phase:a:b"
    - path: "backend/tests/unit/test_183_skip_parse_parity.py"
      provides: "The control that would have caught C-1 — the Python half of the parity pin"
    - path: "frontend/tsconfig.app.json"
      provides: "resolveJsonModule enabled so the shared table compiles"
      contains: "resolveJsonModule"
  key_links:
    - from: "frontend/src/components/workflows/phaseVocabulary.ts"
      to: "backend/app/services/harness/reachability.py parse_skip_target"
      via: "prefix-length slice, pinned by the shared case table"
      pattern: "slice\\(SKIP_PREFIX\\.length\\)"
    - from: "backend/tests/unit/test_183_skip_parse_parity.py"
      to: "frontend/src/components/workflows/__fixtures__/skipParseCases.json"
      via: "Path(__file__).resolve().parents[3] cross-language read"
      pattern: "skipParseCases\\.json"
---

<objective>
Create the ONE shared phase-vocabulary module both graph views will read, with the `skip_to_phase`
parse corrected to the backend's real semantics and pinned by a cross-language parity test.

Purpose: D-183-13's whole point is that two views one toggle apart must not disagree about what a
step is called or where its branch goes. Correction C-1 proved the current "port" already drifted —
`PhaseSpineGraph.tsx:78-83` splits on `lastIndexOf(":")` while `reachability.py:89-98` slices the
literal prefix, and the frontend's own docblock falsely claimed parity, a false claim that then
propagated into a test comment and into D-183-15's case table. Prose parity is what drifted; an
executable, cross-language, shared-table parity test is what cannot.
Output: `phaseVocabulary.ts`, the shared JSON case table, the TypeScript parity block, and the pytest
control. No consumer is repointed here — plan 183-04 does the hard cut.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-read-only-canvas/183-CONTEXT.md
@.planning/phases/183-read-only-canvas/183-RESEARCH.md
@.planning/phases/183-read-only-canvas/183-PATTERNS.md
@.planning/phases/183-read-only-canvas/183-VALIDATION.md
@frontend/src/components/workflows/PhaseSpineGraph.tsx
@frontend/src/components/workflows/soulData.ts
@frontend/src/components/workflows/deriveTier.ts
@backend/app/services/harness/reachability.py
</context>

<interfaces>
<!-- The exact contracts to move / mirror. Extracted so the executor writes, not explores. -->

The AUTHORITATIVE parse (`backend/app/services/harness/reachability.py:89-98`) — slice by prefix
LENGTH, trim, empty becomes None. Note it is NOT a `lastIndexOf(":")` split:
```python
def parse_skip_target(on_failure: str) -> str | None:
    prefix = "skip_to_phase:"
    if isinstance(on_failure, str) and on_failure.startswith(prefix):
        target = on_failure[len(prefix):].strip()
        return target or None
    return None
```

The three read shapes to MOVE VERBATIM (with their JSDoc) out of `PhaseSpineGraph.tsx:48-71`:
```ts
export interface ValidatorJSON { kind?: string; on_failure?: string; [k: string]: unknown }
export interface PhaseConfigJSON { phase_type: string; [k: string]: unknown }
export interface PhaseSpecJSON {
  slug: string
  phase_index: number
  name?: string | null
  config: PhaseConfigJSON
  validators?: ValidatorJSON[]
}
```

The vocabulary to MOVE VERBATIM out of `PhaseSpineGraph.tsx:34-41` (the ⌥ Technical-names reveal):
```ts
const PHASE_TYPE_LABELS: Record<string, string> = {
  programmatic: "Server step", llm_single: "AI write step", llm_agent: "AI agent step",
  llm_batch_agents: "Parallel agents", llm_human_input: "Needs you", llm_emit: "Deliverable",
}
```

The glyph map that must be IMPORTED, never re-declared (`soulData.ts:28-35`) — the six fluent-emoji
slug strings `gear / memo / robot / busts-in-silhouette / raised-hand / package`, resolved to 3D SVG
components by `phaseGlyph(phaseType)` at `frontend/src/lib/phaseGlyph.tsx:63-66`.

The house docblock shape to copy (`soulData.ts:1-16`): what one thing this module owns, what drift it
forbids, its purity contract. The house exhaustiveness/total-resolver idiom lives at
`deriveTier.ts:119-127` and `phaseGlyph.tsx:63-66` (`?? null` / `?? fallback`, never a bare map read).

The tier glyph vocabulary to reuse for the grounding badge (`deriveTier.ts:57-79`): STRICT `🔒`,
MIDDLE `◐`, LOOSE `○`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create phaseVocabulary.ts — the one shared module</name>
  <files>frontend/src/components/workflows/phaseVocabulary.ts, frontend/src/components/workflows/phaseVocabulary.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseSpineGraph.tsx` — the donor; read lines 22-99 in full (the local glyph map, the labels, the read shapes, the FALSE parity docblock, `parseSkipTarget`, `nodeTitle`)
    - `frontend/src/components/workflows/soulData.ts` — the sibling module whose docblock shape and null-tolerant derivation style this must match, and the `PHASE_GLYPHS` map it must NOT re-declare
    - `frontend/src/components/workflows/deriveTier.ts` — `CitationPolicy`, `ValidatorKind`, the `TIERS` glyph vocabulary, and the exhaustiveness-guard idiom at `:119-127`
    - `backend/app/services/harness/reachability.py` lines 85-110 — the authoritative parse and the skip-target collector
    - `.planning/phases/183-read-only-canvas/183-CONTEXT.md` — D-183-06, D-183-07, D-183-13, and the D-183-15 AMENDMENT block
    - `.planning/sketches/137-agentic-canvas-look/README.md` — the plain-language anchor ("Search the knowledge base", not `llm_agent · retrieve`)
    - `frontend/src/components/workflows/soulData.test.ts` lines 17-96 and 190-201 — the test shape and the `?raw` purity-grep template
  </read_first>
  <behavior>
    - `parseSkipTarget("skip_to_phase:escalate")` returns `"escalate"`
    - `parseSkipTarget("skip_to_phase:a:b")` returns `"a:b"` — backend semantics, the C-1 correction
    - `parseSkipTarget("skip_to_phase:")` returns `null`; `parseSkipTarget("skip_to_phase:   ")` returns `null`
    - `parseSkipTarget("skip_to_phase:  gather  ")` returns `"gather"` (trimmed)
    - `parseSkipTarget` returns `null` for `"fail_run"`, `"ask_user"`, `"retry"`, `""`, `undefined`, `null`, `"skip_to_phase"` (no colon) and `"SKIP_TO_PHASE:gather"` (case-sensitive prefix)
    - `nodeTitle` returns `phase.name` when it trims non-empty
    - `nodeTitle` returns the plain-language sentence when `name` is absent, and the returned string does NOT contain `phase.slug`
    - `nodeTitle` on an unknown `phase_type` echoes the raw type string and does not throw
    - `technicalTitle` returns `"<label> · <slug>"` — today's shipped fallback, preserved for the ⌥ reveal
    - `groundingFor` returns mode `"strict"` for `citation_policy: "strict"` OR any `citations_required` validator; `"flag"` for `citation_policy: "flag"`; `"open"` for `partial`, `draft`, absent, and any unknown value
    - `groundingFor` never throws on a missing `validators` array, an unknown `validator.kind` such as `regex_match`, or a `config` with no `citation_policy`
    - every `Grounding` carries a non-empty `words` string AND a non-empty `glyph` (never colour alone)
    - `waitsForYou` is true only for `phase_type === "llm_human_input"`
  </behavior>
  <action>
    Create `frontend/src/components/workflows/phaseVocabulary.ts` as a sibling of `soulData.ts` /
    `deriveTier.ts` — one file per concern, matching the existing split (RESEARCH §Recommended Project
    Structure recommends a separate file over folding into `soulData.ts`, because `soulData`'s own
    docblock scopes it to the three "soul" sizes and it already carries a demonstrably-stale extraction
    claim).

    Copy the `soulData.ts:1-16` docblock SHAPE: what one thing this module owns, what drift it forbids,
    its purity contract. Cite D-183-13. Critically, do NOT repeat `soulData.ts:19-21`'s mistake of
    asserting an extraction that has not happened — this module's docblock must say the consumer
    repoint lands in plan 183-04, not that it already did. In-code claims of prior extraction are the
    exact anti-drift hazard CONTEXT warns about.

    Export exactly these names:

    `ValidatorJSON`, `PhaseConfigJSON`, `PhaseSpecJSON` — moved VERBATIM from `PhaseSpineGraph.tsx:48-71`
    including their JSDoc comments. Do not re-type them, do not tighten them; their looseness is
    deliberate and documented.

    `SKIP_PREFIX` — the module-level literal `"skip_to_phase:"`.

    `parseSkipTarget(onFailure: string | null | undefined): string | null` — EXACT backend semantics per
    the operator-approved C-1 amendment recorded under D-183-15. Return null unless the value is a
    string starting with `SKIP_PREFIX`; otherwise slice off the prefix by LENGTH using
    `onFailure.slice(SKIP_PREFIX.length)`, `.trim()`, and return the result or null when empty. NEVER
    `lastIndexOf(":")`. Its docblock cites `backend/app/services/harness/reachability.py:89-98`, states
    the prefix-slice semantics in one sentence, and names the parity test as the pin. Do NOT carry over
    `PhaseSpineGraph.tsx:73-77`'s false "split on the LAST ':'" claim in any form.

    `PHASE_TYPE_SENTENCES: Record<string, string>` — D-183-06's plain-language business sentence per
    `phase_type`, anchored to sketch 137-D. Use exactly these strings: `programmatic` →
    "Prepare the inputs"; `llm_single` → "Write it up"; `llm_agent` → "Work out how to do it";
    `llm_batch_agents` → "Work on the parts together"; `llm_human_input` → "Check with you";
    `llm_emit` → "Produce the deliverable".

    `PHASE_TYPE_SUBTITLES: Record<string, string>` — the ONE supporting line 137-D allows under the
    title. Use exactly: `programmatic` → "A fixed step the server runs"; `llm_single` →
    "Writes one piece in one pass"; `llm_agent` → "Searches and decides its own next move";
    `llm_batch_agents` → "Several assistants work in parallel"; `llm_human_input` →
    "Pauses here until you answer"; `llm_emit` → "Fills your template and produces the file".

    `PHASE_TYPE_LABELS: Record<string, string>` — moved VERBATIM from `PhaseSpineGraph.tsx:34-41`,
    values unchanged. This is the ⌥ Technical-names reveal vocabulary, not the default face.

    `nodeTitle(phase: PhaseSpecJSON): string` — the D-183-06 resolution. `phase.name?.trim()` wins when
    non-empty; otherwise `PHASE_TYPE_SENTENCES[phase.config.phase_type]`; otherwise echo the raw
    `phase.config.phase_type` honestly (the `PhaseSpineGraph.tsx:89` precedent — never invent a sentence
    for an unknown type). The slug NEVER appears in this string; it lives behind the ⌥ reveal.

    `technicalTitle(phase: PhaseSpecJSON): string` — the ⌥ reveal form, which is exactly today's shipped
    fallback preserved: the `PHASE_TYPE_LABELS` entry (or the raw type when unmapped), then " · ", then
    `phase.slug`.

    `Grounding` — the type `{ mode: "strict" | "flag" | "open"; words: string; glyph: string }` — and
    `groundingFor(phase: PhaseSpecJSON): Grounding`, D-183-07 slot 1, DERIVED only from fields that
    exist today. Mode is `"strict"` when `phase.config.citation_policy === "strict"` OR any entry of
    `phase.validators ?? []` has `kind === "citations_required"`; `"flag"` when
    `phase.config.citation_policy === "flag"`; otherwise `"open"` — which deliberately includes
    `"partial"`, `"draft"`, an absent value, and any future/unknown value, so the function is TOTAL and
    never throws. Compare `v.kind` as a plain string; do NOT cast it into `ValidatorKind` (the backend
    declares nine kinds and `deriveTier.ts:28-33` narrows to five, so a cast would be a lie and
    `regex_match` must simply be ignored). Words: strict → "Must cite its sources"; flag →
    "Flags uncited claims"; open → "No sources needed". Glyphs, reusing the shipped `deriveTier.ts:57-79`
    vocabulary so the app speaks one strictness language: strict → 🔒, flag → ◐, open → ○. The glyph
    beside the words is the WCAG 1.4.1 never-colour-alone control. Note in a comment that C-8 holds —
    `citation_policy` exists only on `llm_emit` configs, so a non-emit phase reaches strict only via the
    `citations_required` validator, which is intended.

    `waitsForYou(phase: PhaseSpecJSON): boolean` — D-183-07 slot 2: true only when
    `phase.config.phase_type === "llm_human_input"`. Every other node face stays at ONE badge.

    Hard prohibitions for this module: do NOT declare a local `PHASE_GLYPHS` (import it from
    `@/components/workflows/soulData` if needed); do NOT invent a `grounding_mode` field (Phase 185
    owns that); do NOT import anything from `@/lib/api` (derive, never fetch); do NOT add layout,
    position, x, y or DOM reads. Use the `@/` alias for cross-module imports, matching `soulData.ts:17`.

    Write `phaseVocabulary.test.ts` alongside it covering every behaviour row above, plus the house
    `?raw` purity/anti-duplication block modelled on `soulData.test.ts:190-201`: the source must not
    match `/from\s+["']@\/lib\/api["']/`, must not match `/const PHASE_GLYPHS/`, must not match
    `/lastIndexOf/`, must not match `/grounding_mode/`, and must contain exactly ONE
    `export function parseSkipTarget` declaration.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/workflows/phaseVocabulary.ts` exists and exports every name listed in the action, with the three read-shape interfaces byte-identical in field set to `PhaseSpineGraph.tsx:48-71`
    - `grep -c "lastIndexOf" frontend/src/components/workflows/phaseVocabulary.ts` returns 0
    - `grep -c "const PHASE_GLYPHS" frontend/src/components/workflows/phaseVocabulary.ts` returns 0
    - `grep -c "grounding_mode" frontend/src/components/workflows/phaseVocabulary.ts` returns 0
    - `grep -c "@/lib/api" frontend/src/components/workflows/phaseVocabulary.ts` returns 0
    - `grep -cE "getBoundingClientRect|offsetHeight|offsetWidth|document\.|window\." frontend/src/components/workflows/phaseVocabulary.ts` returns 0
    - the module docblock does NOT claim the `PhaseSpineGraph` duplicate has already been removed; it names plan 183-04 as the repoint
    - `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts` is green with at least one assertion per behaviour row above
    - `cd frontend && npx tsc -b` exits 0
    - `git status --porcelain frontend/src/components/workflows/PhaseSpineGraph.tsx` is empty (this task adds; 183-04 subtracts)
  </acceptance_criteria>
  <done>The shared module exists, is total over every malformed input, carries no false extraction claim, and its unit suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: The shared C-1 case table + resolveJsonModule + the TypeScript parity block</name>
  <files>frontend/src/components/workflows/__fixtures__/skipParseCases.json, frontend/tsconfig.app.json, frontend/src/components/workflows/phaseVocabulary.test.ts</files>
  <read_first>
    - `frontend/tsconfig.app.json` — the full `compilerOptions` block; note `moduleResolution: "bundler"` and the absence of `resolveJsonModule`
    - `.planning/phases/183-read-only-canvas/183-PATTERNS.md` §Blocking Findings row **F-1** and §No Analog Found (the cross-language fixture gap, stated plainly)
    - `backend/app/services/harness/reachability.py` lines 85-110 — the authoritative expected values
    - `.planning/phases/183-read-only-canvas/183-CONTEXT.md` — D-183-15 and its AMENDMENT block
    - `frontend/src/components/org/StatusChip.test.tsx` lines 88-104 — the `it.each` table-driven idiom
  </read_first>
  <action>
    F-1 is a build-breaker: `resolveJsonModule` is NOT enabled in `frontend/tsconfig.app.json` and
    there is ZERO `.json` import anywhere in `frontend/src`, so importing a JSON fixture fails
    `npx tsc -b` with TS2732 — and `tsc -b` is a phase gate. Fix it as a NAMED, deliberate change, not
    an incidental edit: add `"resolveJsonModule": true` to the `compilerOptions` of
    `frontend/tsconfig.app.json`. It is the standard, additive fix and with no existing `.json` import
    in the tree it cannot regress anything. Change nothing else in that file.

    Then create `frontend/src/components/workflows/__fixtures__/skipParseCases.json` as the ONE
    canonical C-1 case table, read by BOTH the vitest suite (this task) and the pytest suite (Task 3).
    Two hand-maintained copies are precisely the drift C-1 already demonstrated and are forbidden.
    Note honestly in the plan record that this is net-new structure: the frontend has no
    `__fixtures__` directory and no cross-language shared-fixture precedent; the only related shipped
    pattern is the inverse direction, `backend/tests/test_seed091_owner_nulling.py:188` reading a
    frontend `.ts` file.

    Shape: a JSON object with a `note` string naming the operator-approved amendment and stating that
    `backend/app/services/harness/reachability.py:89-98` is authoritative, and a `cases` array of
    `{ "on_failure": <string|null>, "expected": <string|null>, "why": <string> }` rows. Required rows,
    with the expected values taken from the backend function, not from the superseded D-183-15 example:
    `"skip_to_phase:escalate"` → `"escalate"`; `"skip_to_phase:a:b"` → `"a:b"`; `"skip_to_phase:"` → null;
    `"skip_to_phase:   "` → null; `"skip_to_phase:  gather  "` → `"gather"`; `"fail_run"` → null;
    `"ask_user"` → null; `"retry"` → null; `""` → null; `null` → null; `"skip_to_phase"` → null;
    `"SKIP_TO_PHASE:gather"` → null.

    Extend `phaseVocabulary.test.ts` with a `describe` block named so it matches `-t "parity"`. Import
    the JSON module and drive an `it.each` over `cases` (the `StatusChip.test.tsx:88-104` idiom),
    asserting `parseSkipTarget(row.on_failure)` equals `row.expected` for every row. The test must read
    the JSON — do not hand-copy the rows into the test file, because that recreates the two-copy
    problem the table exists to kill. Add one guard assertion that the table has at least 12 rows and
    contains the `"skip_to_phase:a:b"` row, so a truncated table cannot silently weaken the control.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/workflows/phaseVocabulary.test.ts -t "parity"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/tsconfig.app.json` `compilerOptions` contains `"resolveJsonModule": true` and no other key changed (`git diff frontend/tsconfig.app.json` shows exactly one added line)
    - `cd frontend && npx tsc -b` exits 0 — proving TS2732 is gone
    - `node -e "const t=require('./frontend/src/components/workflows/__fixtures__/skipParseCases.json');console.log(t.cases.length)"` prints a number ≥ 12
    - the table contains a row with `on_failure` exactly `"skip_to_phase:a:b"` and `expected` exactly `"a:b"`
    - `grep -c "skipParseCases" frontend/src/components/workflows/phaseVocabulary.test.ts` returns ≥ 1, and no literal array of parse cases is declared in the test file
    - `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts -t "parity"` runs at least 12 assertions and is green
  </acceptance_criteria>
  <done>`resolveJsonModule` is on, the shared table exists with the corrected `a:b` row, and the TypeScript half of the parity pin is green.</done>
</task>

<task type="auto">
  <name>Task 3: The Python half of the parity control</name>
  <files>backend/tests/unit/test_183_skip_parse_parity.py</files>
  <read_first>
    - `backend/tests/test_harness_reachability.py` lines 1-50 — the direct ancestor test and the import style
    - `backend/tests/integration/test_seed_pm_pack.py` lines 40-55 — the `parents[N] == repo root` arithmetic comment style to copy
    - `backend/tests/test_seed091_owner_nulling.py` lines 178-217 — the shipped precedent for a pytest reading a file out of `frontend/`
    - `backend/app/services/harness/__init__.py` — confirm `parse_skip_target` is re-exported (lines 17 and 32)
    - `frontend/src/components/workflows/__fixtures__/skipParseCases.json` — the table written in Task 2
  </read_first>
  <action>
    Add the control that would have caught C-1 in the first place. This is the ONLY permitted backend
    file in the entire phase, and it is a test — no backend source, no route, no migration.

    Import `parse_skip_target` from `app.services.harness` (it is re-exported at
    `harness/__init__.py:17,32`, matching `test_harness_reachability.py:41`'s import style).

    Resolve the shared table with
    `Path(__file__).resolve().parents[3] / "frontend" / "src" / "components" / "workflows" / "__fixtures__" / "skipParseCases.json"`
    and carry an inline comment stating the arithmetic — `backend/tests/unit/... -> parents[3] == repo
    root` — copying the `test_seed_pm_pack.py:46-47` comment style so a future file move cannot silently
    break the path. Assert the file exists with an error message naming the resolved path before
    loading it, so a broken path fails loudly instead of silently skipping.

    Parametrize over the loaded `cases` and assert `parse_skip_target(on_failure) == expected` for every
    row, passing a JSON `null` `on_failure` through as Python `None` (the backend guards with
    `isinstance(on_failure, str)`, so `None` is a legitimate input and must return `None`).

    Add one guard test asserting the table has at least 12 rows AND contains the `"skip_to_phase:a:b"`
    row with expected `"a:b"` — the same truncation guard as the TypeScript half, so neither language
    can be weakened alone.

    Do not modify `reachability.py`, `harness/__init__.py`, or any other backend file.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_183_skip_parse_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/unit/test_183_skip_parse_parity.py` exists and is the ONLY file added under `backend/` by this phase
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_183_skip_parse_parity.py -q` reports all passed with at least 13 collected tests
    - `git status --porcelain backend/app` is empty
    - `grep -c "skipParseCases.json" backend/tests/unit/test_183_skip_parse_parity.py` returns 1 and no Python literal case list exists in the file
    - the file contains an inline comment stating the `parents[3] == repo root` arithmetic
    - deliberately corrupting one `expected` value in the JSON makes BOTH `npx vitest run ... -t "parity"` and this pytest fail (confirm once, then revert)
  </acceptance_criteria>
  <done>Both languages read one table and agree; the parity claim is executable in Python as well as TypeScript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow definition JSONB → client derivation | LLM/user-authored `phase.name`, `slug`, `on_failure`, `citation_policy` and `validators` are read by these resolvers |

No network boundary is crossed: this module imports nothing from the API client and performs no fetch.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-01 | Tampering | authored `phase.name` / `slug` flowing through `nodeTitle` / `technicalTitle` | mitigate | These resolvers return plain strings only; they build no markup and never touch `dangerouslySetInnerHTML`. Rendering safety is enforced at the consumer (`PhaseNode` in plan 183-06) per the shipped `PhaseSpine.tsx:13-14` T-124-01 clause. |
| T-183-06 | Denial of Service | malformed definition JSONB crashing the projection | mitigate | Totality is the live control and is asserted in the behaviour block: unknown `phase_type` echoes the raw type, unknown/absent `citation_policy` resolves to `open`, unknown `validator.kind` (e.g. `regex_match`) is ignored without a cast, a missing `validators` array defaults to `[]`, and a malformed `on_failure` returns `null`. No resolver throws. |
| T-183-07 | Tampering | client parse diverging from the server's reachability adjacency | mitigate | The shared JSON case table plus the two-language parity tests. This is the C-1 control; a prose parity claim is what already drifted. |

## ASVS conclusion (carried from RESEARCH §Security Domain, line 1759)

V5 Input Validation is the only category that applies, and it applies as TOTALITY over already
server-validated data (the definition JSONB passes Pydantic `_StrictBase` / `extra="forbid"` at
`harness.py:30` before it can reach the client). V2, V3, V4 and V6 do not apply: no auth surface, no
stored state, no authz decision, no cryptography. The ROADMAP's "no threat model" flag for Phase 183
stands.

**The single backend file in this plan is a TEST.** If any task grows a backend SOURCE touch, this
conclusion is VOID and `/gsd:secure-phase` applies.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts` green
- `cd frontend && npx tsc -b` exits 0
- `cd backend && venv/Scripts/python -m pytest tests/unit/test_183_skip_parse_parity.py -q` all passed
- `cd frontend && npx vitest run src/components/workflows` shows no NEW failing test names and no test-count drop versus the recorded baseline
- `git status --porcelain backend/app` empty; `git status --porcelain frontend/src/components/workflows/PhaseSpineGraph.tsx` empty
</verification>

<success_criteria>
- One shared vocabulary module exists, importing `PHASE_GLYPHS` rather than re-declaring it
- `parseSkipTarget` implements the backend's prefix-length slice and never `lastIndexOf`
- One JSON case table drives both a vitest `it.each` block and a pytest parametrize
- Every resolver is total over unknown / malformed / missing definition fields
- No consumer has been repointed yet — that is plan 183-04's hard cut
</success_criteria>

<output>
Create `.planning/phases/183-read-only-canvas/183-02-SUMMARY.md` when done.
Record the exported symbol list verbatim (plans 183-04, 183-05 and 183-06 all import from this module)
and the final row count of the shared case table.
</output>
