---
phase: 183-read-only-canvas
plan: 07
type: execute
wave: 4
depends_on: ["183-03", "183-04", "183-06"]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
  - frontend/src/components/admin/revertByteIdentical.test.tsx
autonomous: true
requirements: [CANVAS-01]

must_haves:
  truths:
    - "D-183-01: the canvas is an in-Builder `[≣ Spine] [⬡ Canvas]` view toggle on the existing graph column — no new ActiveView, no router, and NO `NAV_ITEMS` entry tagged `visual_workflow_canvas`, so the 181 scope-freeze assertion keeps holding"
    - "D-183-02: Spine stays the default view — the Builder opens exactly as it does today and the toggle is session state that cold-starts on Spine, with no persisted preference"
    - "D-183-03: with `visual_workflow_canvas` off the toggle strip does not render at all and the graph column is byte-identical to today's shipped Builder for EVERYONE including operators — no `.react-flow` root enters the DOM and no space is reserved"
    - "D-183-04: the published-workflow canvas door is DEFERRED, not built — this plan adds no Tweak path, no `createWorkflowDraft` call, and nothing that could mint a `workflow_definitions` row or bump a version by looking"
    - "D-183-05: the canvas is wired to the SAME `onSelectNode` callback the Spine uses, so the shipped 400px `PhaseFormPanel` opens on the clicked phase with zero net-new panel work"
    - "CANVAS-01: opening the Canvas view performs no network call and writes nothing"
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "The flag-gated view toggle and the swapped graph-column child"
      contains: "WorkflowCanvas"
    - path: "frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx"
      provides: "The flag-off vanish proof and the canvas-branch selection wiring test"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/providers/EffectiveFeaturesProvider.tsx"
      via: "useEffectiveFeaturesOptional, treating null as fail-closed"
      pattern: "useEffectiveFeaturesOptional"
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      via: "React.lazy dynamic import behind the toggle"
      pattern: "lazy\\(\\(\\) => import\\("
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      via: "the unchanged selectedSlug contract shared by both views"
      pattern: "setSelectedSlug"
---

<objective>
Land the door: a flag-gated `[≣ Spine] [⬡ Canvas]` toggle on the Builder's existing graph column,
with the canvas swapped in as a drop-in peer and flag-off proving byte-identical to today.

Purpose: D-183-01 chose an in-Builder toggle over a standalone page precisely because the app has no
router — navigation is a `useState<ActiveView>` switch — so a "new page" would be net-new state
plumbing for no user benefit, and Phase 184 upgrades this SAME component in place from read-only to
editable. The load-bearing property is the flag-off half: with `visual_workflow_canvas` off the strip
must VANISH (not render disabled, not reserve space), matching the Phase 148 vanish convention and the
Phase 181 revert semantics, for everyone including operators.
Output: the toggle, the swap, the flag-off DOM proof, and the corrections to the last stale in-code
claims this phase touches.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-read-only-canvas/183-CONTEXT.md
@.planning/phases/183-read-only-canvas/183-PATTERNS.md
@.planning/phases/183-read-only-canvas/183-VALIDATION.md
@.planning/phases/181-revert-foundation/181-CONTEXT.md
@.planning/phases/183-read-only-canvas/183-01-SUMMARY.md
@.planning/phases/183-read-only-canvas/183-03-SUMMARY.md
@.planning/phases/183-read-only-canvas/183-06-SUMMARY.md
@frontend/src/pages/WorkflowBuilderPage.tsx
@frontend/src/components/workflows/WorkflowCanvas.tsx
</context>

<interfaces>
<!-- The mount site being modified, verbatim (WorkflowBuilderPage.tsx:446-465). The graph column is
     the FIRST grid child; the swap is a single-child substitution with NO grid change. -->
```tsx
<div
  data-testid="builder-grid"
  className="grid min-h-0 min-w-0 flex-1 overflow-hidden motion-safe:transition-[grid-template-columns] motion-safe:duration-300"
  style={{ gridTemplateColumns: "minmax(0,1fr) " + (panelOpen ? "400px" : "44px") }}
>
  <PhaseSpineGraph
    phases={state.definition.phases}
    selectedSlug={selectedSlug}
    onSelectNode={(slug) => setSelectedSlug((cur) => (cur === slug ? null : slug))}
  />
  <PhaseFormPanel … />
</div>
```

The segmented-control markup the house uses (`SkillStudioPage.tsx:191-212`) — `role="tablist"` on a
`<div>` host (never a `<nav>`; `frontend/eslint.a11y.config.js` flags that per the Phase-155 A11Y-01
rule), `role="tab"` + `aria-selected` on each button, `cn(...)` for the active variant:
```tsx
<div className="flex items-center gap-1" role="tablist" aria-label="Skill Studio tabs">
  {TABS.map((t) => (
    <button key={t.id} type="button" role="tab" aria-selected={active} onClick={() => onTabChange(t.id)}
      className={cn("rounded-md px-3 py-1.5 text-sm transition-colors",
        active ? "bg-primary/10 font-semibold text-primary"
               : "text-muted-foreground hover:bg-accent/40 hover:text-foreground")}>
      {t.label}
    </button>
  ))}
</div>
```

The VANISH semantics to carry into the Builder branch (`lib/nav-items.ts:61-73`): strict `=== true`,
never truthy — an absent key must hide. Fail-CLOSED by construction.

The lazy-load precedent — the ONE shipped code-split in the app (`KnowledgeHealthPage.tsx:28-32`),
with the exact named-export shim:
```ts
const RetrievalTrendChart = lazy(() => import("@/components/health/RetrievalTrendChart").then((m) => ({ default: m.RetrievalTrendChart })))
```

The 181 gate whose assertions must stay byte-identical (`revertByteIdentical.test.tsx:62-64`):
```ts
it("no NAV_ITEMS entry is tagged visual_workflow_canvas (scope-freeze guard)", () => {
  expect(NAV_ITEMS.some((item) => item.feature === "visual_workflow_canvas")).toBe(false)
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The flag-gated [≣ Spine] [⬡ Canvas] toggle and the graph-column swap</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.tsx` — the whole file; specifically the import block at `:29-33`, the state block at `:92-131`, and the grid at `:446-465`
    - `frontend/src/components/workflows/WorkflowCanvas.tsx` — the exact named export and props signature from plan 183-06
    - `frontend/src/providers/EffectiveFeaturesProvider.tsx` — `useEffectiveFeaturesOptional` and the null-context-is-fail-closed rule from plan 183-03
    - `frontend/src/pages/SkillStudioPage.tsx` lines 185-214 — the leaner of the two shipped tablist precedents, including the A11Y-01 `<div>`-host comment
    - `frontend/src/components/admin/ControlRoomPage.tsx` lines 645-690 — the second tablist precedent
    - `frontend/src/pages/KnowledgeHealthPage.tsx` lines 26-37 and 478-488 — the shipped `React.lazy` + `<Suspense>` pattern
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` lines 30-60 — the shipped in-page view fork inside the workflows surface, the nearest conceptual sibling
    - `frontend/src/lib/nav-items.ts` lines 55-75 — the strict `=== true` VANISH semantics
    - `.planning/phases/183-read-only-canvas/183-CONTEXT.md` — D-183-01 through D-183-05
  </read_first>
  <action>
    Add ONE piece of session state to `WorkflowBuilderPage`:
    `const [graphView, setGraphView] = useState<"spine" | "canvas">("spine")`. D-183-02 — Spine is the
    default, the Builder opens exactly as it does today, and the toggle cold-starts on Spine on every
    mount. Do NOT persist the preference to `localStorage`, to `user_settings`, or anywhere else.

    Gate it on the flag with the provider from plan 183-03:
    `const featuresCtx = useEffectiveFeaturesOptional()` and
    `const canvasEnabled = featuresCtx !== null && !featuresCtx.loading && featuresCtx.features.visual_workflow_canvas === true`.
    Three things matter here and each is load-bearing. Use the OPTIONAL accessor, not the throwing one,
    so the shipped `WorkflowBuilderPage.test.tsx` suite — which renders the page with no provider —
    keeps passing unchanged. Treat a null context as FAIL-CLOSED, identical to `{}`. Compare strictly
    against `true`, never truthily, so an absent key hides (the `visibleNavItems` contract). And gate on
    `!loading` as well, so the strip does not flash in and shift layout ~200 ms after load — a flag-off
    user must see byte-identical output, which forbids a reserved placeholder.

    Derive the effective view so the flag can never be out-ranked by stale state:
    `const activeGraphView = canvasEnabled ? graphView : "spine"`. If the map is tightened mid-session
    while the user is on Canvas, the column falls back to the Spine rather than stranding them on a
    surface that just vanished.

    Render the toggle strip ONLY when `canvasEnabled` (D-183-03 — with the flag off the strip does not
    render, the `@xyflow` subtree stays out of the render path entirely, and the graph column is
    byte-identical to today for EVERYONE including operators, per D-181-01). Use the house segmented
    control: a `<div role="tablist">` host (never a `<nav>` — the A11Y-01 rule), two
    `<button type="button" role="tab" aria-selected={...}>` children reading `≣ Spine` and `⬡ Canvas`,
    `cn(...)` for the active variant, and `data-testid` values `builder-view-toggle`,
    `builder-view-spine` and `builder-view-canvas`. Do NOT mix `role="tab"` with `aria-pressed` — pick
    the tablist semantics and stay in them.

    Place the strip inside the graph COLUMN, above the graph, so the flag-off DOM is the graph column
    exactly as shipped and the `builder-grid` element and its inline `gridTemplateColumns` style are
    untouched. Preserve `motion-safe:transition-[grid-template-columns] motion-safe:duration-300`.

    Swap the graph-column child on `activeGraphView`: `"spine"` renders the existing
    `<PhaseSpineGraph …/>` with its three props unchanged; `"canvas"` renders `<WorkflowCanvas …/>`
    with the IDENTICAL three props. In particular pass the same
    `onSelectNode={(slug) => setSelectedSlug((cur) => (cur === slug ? null : slug))}` callback to both
    (D-183-05) — the toggle-off-on-reclick selection semantics stay at the page, not in the canvas, so
    the shipped 400px `PhaseFormPanel` opens on the clicked phase with zero net-new panel work and
    `panelOpen` keeps working identically in both views.

    Code-split the canvas: `const WorkflowCanvas = lazy(() => import("@/components/workflows/WorkflowCanvas").then((m) => ({ default: m.WorkflowCanvas })))`
    at module scope, following the shipped `KnowledgeHealthPage.tsx:28-32` shim, and wrap the canvas
    branch in `<Suspense>` with a quiet fallback matching the graph column's surface. `@xyflow/react` is
    ~59 KB gzip and the flag cold-defaults to `"off"` for everyone, so without the split today's
    shipped users would pay for a subtree that never renders. The dynamic import also makes D-183-03's
    "the `@xyflow` subtree stays out of the render path entirely" a BUILD-level guarantee rather than a
    render-branch claim. (The stylesheet still loads eagerly from `index.css` — that is the documented
    correct trade from plan 183-01 Task 2.)

    Change nothing else. Do NOT add a `NAV_ITEMS` entry tagged `visual_workflow_canvas` — D-183-01
    formally released Phase 181's deferred promise and `revertByteIdentical.test.tsx:62-64` depends on
    its continued absence. Do NOT add a published-workflow canvas door: D-183-04 defers it precisely
    because viewing a published definition's canvas would require Tweak, which calls
    `createWorkflowDraft` — an INSERT, so merely looking would mint a v(N+1) draft row. Do NOT touch
    `draftId`, `draftIdRef`, `creatingRef`, `onPersist`, `onSaveDraft` or any save path; opening the
    Canvas view must not mint a row, bump a version, or fire any request. Do NOT flip the default to
    Canvas (Phase 184's call) and do NOT persist the view preference.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/pages/WorkflowBuilderPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useEffectiveFeaturesOptional" frontend/src/pages/WorkflowBuilderPage.tsx` returns 1, and `grep -c "useEffectiveFeaturesContext" ...` returns 0
    - `grep -c "visual_workflow_canvas === true" frontend/src/pages/WorkflowBuilderPage.tsx` returns 1 (strict comparison, not truthy)
    - `grep -c "lazy(() => import(\"@/components/workflows/WorkflowCanvas\")" frontend/src/pages/WorkflowBuilderPage.tsx` returns 1, declared at module scope
    - `grep -cE "role=\"tablist\"|role=\"tab\"|aria-selected" frontend/src/pages/WorkflowBuilderPage.tsx` returns ≥ 3, and `grep -c "<nav" ...` returns 0
    - `grep -c "aria-pressed" frontend/src/pages/WorkflowBuilderPage.tsx` returns 0 (tablist semantics only, not mixed)
    - `grep -cE "localStorage|user_settings" frontend/src/pages/WorkflowBuilderPage.tsx` returns 0 (session state only)
    - `grep -c "createWorkflowDraft" frontend/src/pages/WorkflowBuilderPage.tsx` returns exactly the pre-existing count — `git diff frontend/src/pages/WorkflowBuilderPage.tsx` shows no change to any save/persist path
    - the inline `gridTemplateColumns` style string and the `motion-safe:transition-[grid-template-columns]` class on `builder-grid` are unchanged in the diff
    - `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.test.tsx` green — the shipped suite renders the page with no provider and must be unaffected
    - `cd frontend && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>The toggle exists behind a strict, fail-closed flag check; both views share one selection contract; the canvas is code-split; no save path is touched.</done>
</task>

<task type="auto">
  <name>Task 2: The flag-off vanish proof and the canvas-branch wiring test</name>
  <files>frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx</files>
  <read_first>
    - `frontend/src/components/admin/revertByteIdentical.test.tsx` lines 22-65 — the `afterEach(cleanup)` and map-literal-per-assert style this file follows
    - `frontend/src/pages/WorkflowBuilderPage.test.tsx` lines 1-60 — the existing mocks for `listFolders` / `listSkills` / `generateWorkflow`, which this suite needs too
    - `frontend/src/pages/WorkflowBuilderPage.tsx` — the branch under test after Task 1
    - `frontend/src/providers/EffectiveFeaturesProvider.tsx` — how to wrap a render with a controlled feature map
    - `frontend/src/test-utils/mockReactFlow.ts` — required before rendering the canvas branch
    - `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts` — reuse a real fixture as the `initial.definition.phases`
  </read_first>
  <action>
    Write `frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx`, the D-183-03 proof. Boot the
    Builder straight into the drafted editing view by passing `initial={{ definition, draftId }}` with a
    definition built from a real fixture, so no describe/generate flow is needed. Call `mockReactFlow()`
    before any render that reaches the canvas branch, and mock the api-client calls the page makes on
    mount exactly as `WorkflowBuilderPage.test.tsx` already does.

    FLAG OFF — wrap the render in `EffectiveFeaturesProvider` with
    `value={{ features: {}, loading: false, refetch: vi.fn() }}` and assert: the
    `builder-view-toggle` strip is ABSENT from the DOM; `container.querySelector(".react-flow")` is
    null; the shipped `PhaseSpineGraph` is still rendered (query one of its `spine-node-<slug>` test
    ids); and no element reserves space for the missing strip. Repeat the same assertions with
    `features: { visual_workflow_canvas: false }` and with an "operator-like" map where every OTHER
    governed key is true but the canvas key is absent — D-181-01 requires flag-off to be byte-identical
    for EVERYONE including operators, so the operator case must behave identically.

    NO PROVIDER — render with no `EffectiveFeaturesProvider` at all and assert the same absence set.
    That is the null-context-is-fail-closed rule, and it is what keeps the shipped
    `WorkflowBuilderPage.test.tsx` suite honest.

    LOADING — render with `value={{ features: { visual_workflow_canvas: true }, loading: true, refetch: vi.fn() }}`
    and assert the strip is still absent, so it cannot flash in and shift layout mid-load.

    FLAG ON — render with `value={{ features: { visual_workflow_canvas: true }, loading: false, refetch: vi.fn() }}`
    and assert: the `builder-view-toggle` strip IS present with exactly two `role="tab"` children; the
    default active tab is Spine and the Spine is what renders (D-183-02); clicking the Canvas tab
    switches `aria-selected` and — after `waitFor`, because the component is lazy — mounts a
    `.react-flow` root; and clicking back returns to the Spine.

    SELECTION WIRING (D-183-05) — on the Canvas branch, clicking a phase node opens the form panel on
    that phase: assert the panel's open state changes and that the same slug is anchored, using
    whatever the shipped `PhaseFormPanel` exposes as its open/anchor hook. Then assert the toggle-off
    behaviour: clicking the SAME node again closes it (the `cur === slug ? null : slug` semantics the
    page owns for both views).

    NO WRITES (D-183-04 / G-6) — assert that mounting the Builder and switching to Canvas calls
    NEITHER `createWorkflowDraft` NOR `updateWorkflowDraft` (both already mocked), so looking cannot
    mint a `workflow_definitions` row or bump a version.

    Add a `?raw` source guard on `WorkflowBuilderPage` asserting the source does not match
    `/localStorage/`, does not match `/aria-pressed/`, and does not match `/reactflow/` (the frozen v11
    name).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx` green with distinct tests for: flag absent, flag false, operator-like map without the key, no provider, loading, flag on, tab switching, selection open, selection toggle-close, and no-writes
    - every flag-off variant asserts BOTH `queryByTestId("builder-view-toggle")` is null AND `container.querySelector(".react-flow")` is null
    - the flag-on canvas assertion uses `waitFor` (the component is lazily imported)
    - the no-writes test asserts both draft mutation mocks have zero calls
    - temporarily changing the gate from `=== true` to a truthy check makes the "flag absent" test fail (confirm once, then revert)
    - `cd frontend && npx vitest run src/pages src/components/workflows` reports zero failures
  </acceptance_criteria>
  <done>Flag-off is proven byte-identical at the DOM level in five variants, flag-on mounts the canvas, and looking writes nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the last stale claims, measure the lazy chunk, and record the suite differential</name>
  <files>frontend/src/components/admin/revertByteIdentical.test.tsx</files>
  <read_first>
    - `frontend/src/components/admin/revertByteIdentical.test.tsx` — the whole file; the docblock at `:1-21` (note the stale sentence at `:9`), the inline comment at `:58`, and the assertions at `:49`, `:59` and `:62-64` that must stay byte-identical
    - `.planning/phases/183-read-only-canvas/183-PATTERNS.md` §Blocking Findings **F-6** — the fourth stale in-code claim
    - `.planning/phases/183-read-only-canvas/183-CONTEXT.md` — D-183-01's explicit instruction that the scope-freeze assertion CONTINUES to hold and must not be deleted or weakened
    - `.planning/phases/183-read-only-canvas/183-01-SUMMARY.md` — the pre-canvas `vite build` chunk listing, for the A6 comparison
    - `.planning/phases/183-read-only-canvas/183-VALIDATION.md` — the baseline (33 failed / 1844 passed of 1877, flaky 28-35) and the grading rule
  </read_first>
  <action>
    `revertByteIdentical.test.tsx:9` and `:58` both still say the canvas nav entry "lands WITH the view
    in 183". D-183-01 released that promise — the in-Builder toggle needs no nav entry — so both
    sentences are now false, and stale in-code claims are exactly the drift hazard this phase has
    already been bitten by four times.

    Correct BOTH comments, and ONLY the comments. Line 9's docblock sentence becomes an accurate
    statement: no `NAV_ITEMS` entry is tagged `visual_workflow_canvas` because Phase 183 shipped the
    canvas as an in-Builder view toggle (D-183-01), so the scope-freeze guard is a permanent invariant
    rather than a temporary one. Line 58's inline comment becomes the same fact in one line.

    Do NOT touch a single `expect(...)` in this file. The assertions at `:49`, `:59` and especially the
    scope-freeze guard at `:62-64` must remain byte-identical — D-183-01 explicitly forbids deleting or
    weakening them, and it forbids adding a nav entry to satisfy a stale reading of the 181 note. After
    the edit, `git diff` on this file must show changed lines only inside comments.

    Then discharge assumption A6 — that the `React.lazy` split yields a real first-load saving rather
    than pure complexity. Run `npx vite build` and compare the emitted chunk listing against the one
    recorded in `183-01-SUMMARY.md`. Confirm that `@xyflow` lands in a chunk SEPARATE from the main
    entry chunk. If the bundler hoists it into the entry anyway, the saving is zero and the lazy
    boundary is complexity for nothing — in that case remove the `lazy`/`Suspense` wrapper from
    `WorkflowBuilderPage.tsx`, import `WorkflowCanvas` directly, and record the finding. Decide from
    the measurement, not from the assumption.

    Finally record the phase-level suite differential, which is how this phase is graded. Capture the
    failing-test NAME SET and the TOTAL test count from `npm test` and compare against the recorded
    baseline of 33 failed / 1844 passed of 1877 (flaky in the 28-35 range, so an absolute count means
    nothing). The required outcome: no NEW failing names, the `soulData.test.ts` glyph name REMOVED by
    plan 183-04, and a total count HIGHER than 1877 since this phase only adds tests. A drop in the
    total is a regression even if the failure count looks flat — that is the Phase 177 lesson, where a
    "net-new" file replacing an existing suite lost coverage a failures-only diff could not see.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/revertByteIdentical.test.tsx && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/admin/revertByteIdentical.test.tsx` green, including the scope-freeze test
    - `git diff -U0 frontend/src/components/admin/revertByteIdentical.test.tsx` shows changed lines ONLY inside comment text — zero `expect(` lines changed, zero assertions added or removed
    - `grep -c "lands WITH the view in 183" frontend/src/components/admin/revertByteIdentical.test.tsx` returns 0
    - `grep -c "visual_workflow_canvas" frontend/src/lib/nav-items.ts` returns 0
    - `cd frontend && npx vite build` exits 0, and the chunk decision is recorded: either a chunk containing `xyflow` exists that is NOT the main entry chunk (`grep -l xyflow dist/assets/*.js` names a non-entry file), or the lazy wrapper was removed with the measurement recorded in the SUMMARY
    - `cd frontend && npm test` — the failing-test NAME SET contains no name absent from the recorded baseline, no longer contains the `soulData` glyph test, and the total test count is greater than 1877
  </acceptance_criteria>
  <done>The last stale claim is corrected without touching an assertion, the lazy split is decided by measurement, and the phase-level differential is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /features` map → Builder render branch | The effective-features map decides whether the Canvas toggle exists |
| Builder view state → workflow_definitions | The direction that MUST stay closed: viewing must never write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-02 | Elevation of Privilege | treating the client feature map as the security boundary | accept | The client hide is cosmetic and render-only by design; the backend `require_visible` gate (Phase 148-05) and `require_canvas`'s pre-auth 404 (Phase 181, D-181-02 / WR-02) are the wall. This plan adds NO route, NO endpoint and NO gated data path — there is nothing new to bypass. |
| T-183-03 | Information Disclosure | a governed surface flashing to a non-audience user pre-resolve, on a fetch blip, or with no provider mounted | mitigate | Three-part gate asserted in Task 2 across five render variants: strict `=== true` (an absent key hides), `!loading` (no flash-then-vanish and no reserved space), and null-context-is-fail-closed. Matches the `visibleNavItems` VANISH contract and D-181-01's for-everyone-including-operators requirement. |
| T-183-12 | Tampering | opening a view mutating persisted state — the D-183-04 / G-6 failure | mitigate | No save path is touched: `draftId`, `draftIdRef`, `creatingRef`, `onPersist` and `onSaveDraft` are unchanged, no published-workflow Tweak door is added, and Task 2 asserts that mounting plus switching to Canvas calls neither `createWorkflowDraft` nor `updateWorkflowDraft`. Looking cannot mint a `workflow_definitions` row or bump a version. |
| T-183-SC | Tampering | the lazily-imported chunk being fetched at runtime | accept | The dynamic `import()` resolves a same-origin, build-time-emitted asset with Vite's content hash; no CDN, no runtime URL construction, no user-controlled specifier. |

## ASVS conclusion (carried from RESEARCH §Security Domain, line 1759)

V4 Access Control applies **partially and inherited, not extended**: this plan gates a render branch on
an existing render-only signal and introduces no gated route, no authz decision and no new data path.
V2, V3, V5 and V6 do not apply — no auth surface, no stored state (the toggle is session `useState`
with no `localStorage`), no new input surface, no cryptography. The ROADMAP's "no threat model" flag
for Phase 183 stands.

**Frontend-only.** If any task grows a backend touch, this conclusion is VOID and `/gsd:secure-phase`
applies.
</threat_model>

<verification>
- `cd frontend && npx tsc -b && npx vite build` both exit 0
- `cd frontend && npx vitest run src/pages src/components/workflows src/components/admin src/providers src/test-utils` reports zero failures
- `cd frontend && npm test` — no NEW failing test names, the `soulData` glyph name gone, total count above 1877
- `cd backend && venv/Scripts/python -m pytest tests/unit/test_183_skip_parse_parity.py -q` all passed
- `git status --porcelain backend/app supabase scripts` empty — no backend source, no migration, no cloud parity owed
- `grep -rn "visual_workflow_canvas" frontend/src/lib/nav-items.ts` returns nothing
</verification>

<success_criteria>
- The Builder shows a `[≣ Spine] [⬡ Canvas]` toggle when and only when `visual_workflow_canvas` resolves strictly true and the map has resolved
- Spine is the default; the preference is session-only and cold-starts on Spine
- Flag off (absent / false / operator-like / no provider / loading) yields no strip and no `.react-flow` root, in five asserted variants
- Clicking a canvas node opens the shipped `PhaseFormPanel` on that phase through the same callback the Spine uses; clicking it again closes it
- Opening the Canvas view issues no request and writes nothing
- `revertByteIdentical.test.tsx` is green with every assertion byte-identical
</success_criteria>

<output>
Create `.planning/phases/183-read-only-canvas/183-07-SUMMARY.md` when done.
Record: the lazy-chunk measurement and the resulting keep-or-drop decision (assumption A6); the
before/after failing-test NAME SET and total COUNT; and an explicit note that the four G-4
lived-experience rows (U-1 … U-4) in `183-VALIDATION.md` remain OUTSTANDING and must be driven live
before `/gsd:verify-work` — they are operator-defined and cannot be automated away.
</output>
