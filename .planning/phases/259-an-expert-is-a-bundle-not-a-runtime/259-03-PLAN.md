---
phase: 259-an-expert-is-a-bundle-not-a-runtime
plan: 03
type: execute
wave: 3
depends_on: ['259-02']
files_modified:
  - backend/app/api/experts.py
  - backend/app/main.py
  - backend/tests/unit/test_259_expert_entitlement_gate.py
  - backend/tests/unit/test_259_closed_core_inventory.py
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [PACK-01, PACK-06]
mutates_local_db: false

must_haves:
  truths:
    - "All Expert endpoints (POST /experts, GET /experts, GET /experts/{id}, GET /experts/{id}/resolve, PATCH /experts/{id}, DELETE /experts/{id}) are guarded by Phase 258 require_capability('experts') (D-259-05, PACK-06)."
    - "A Standard or Pro org requesting /experts endpoints receives a structured HTTP 403 naming 'enterprise' tier and upgrade hint — never a bare 403 (TIER-03, PACK-06)."
    - "The executor, emitter, and tool dispatcher inventory is measurably unchanged from the base commit: zero new executors, zero expert-specific agent loops, zero parallel dispatchers (D-259-06, PACK-01, EXT-01 Red Line)."
    - "The closed-core inventory fence in test_259_closed_core_inventory.py is proven non-vacuous by driving it RED against a planted executor, with plant removal proven by md5."
    - "Router mount in backend/app/main.py honours G-5 by construction (2 lines added: 1 import + 1 include_router, 0 logic branches)."
    - "Row added AT CREATION in docs/HOT-FILE-LEDGER.md and CLAUDE.md for backend/app/api/experts.py (0/0/0)."
  artifacts:
    - path: "backend/app/api/experts.py"
      provides: "REST API router for expert bundles with require_capability('experts') dependency"
      exports: ["router"]
    - path: "backend/tests/unit/test_259_expert_entitlement_gate.py"
      provides: "HTTP and service level tests verifying tier gating, structured 403 refusal payload, and add-on override handling"
    - path: "backend/tests/unit/test_259_closed_core_inventory.py"
      provides: "AST mechanical inventory fence asserting PHASE_TYPE_REGISTRY_ENTRIES, EMITTER_REGISTRY, and _TOOL_REGISTRY remain closed with zero expert runtimes"
  key_links:
    - from: "backend/app/api/experts.py"
      to: "backend/app/services/entitlement_service.py"
      via: "FastAPI dependency require_capability('experts')"
      pattern: "require_capability\\(\"experts\"\\)"
    - from: "backend/app/main.py"
      to: "backend/app/api/experts.py"
      via: "mounts experts_router at prefix /experts"
      pattern: "include_router.*experts_router"
    - from: "backend/tests/unit/test_259_closed_core_inventory.py"
      to: "backend/app/services/harness/phase_types.py"
      via: "AST asserts PHASE_TYPE_REGISTRY_ENTRIES is strictly 7 elements"
      pattern: "PHASE_TYPE_REGISTRY_ENTRIES"
---

<objective>
Deliver the Expert REST API router (`backend/app/api/experts.py`), mount it in `backend/app/main.py`, enforce Phase 258 entitlement gating (`require_capability('experts')`), and Author the mechanical closed-core inventory fence (`test_259_closed_core_inventory.py`) driven RED against a planted executor.

Purpose: PACK-06 (Entitlement gating via TIER-01) and PACK-01 (Inventory invariant: Expert is data, not a runtime).
Output: `backend/app/api/experts.py` mounted in `main.py`, unit tests in `test_259_expert_entitlement_gate.py`, and AST inventory fence in `test_259_closed_core_inventory.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

Base SHA to assert: `10b34fe78` on `develop`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/259-an-expert-is-a-bundle-not-a-runtime/259-CONTEXT.md
@.planning/phases/259-an-expert-is-a-bundle-not-a-runtime/259-RESEARCH.md
@.planning/phases/259-an-expert-is-a-bundle-not-a-runtime/259-PATTERNS.md
@docs/EXTENSION-CONTRACT.md
@CLAUDE.md
@backend/app/services/entitlement_service.py
@backend/app/services/expert_service.py
@backend/app/main.py

<interfaces>
NEW in backend/app/api/experts.py:

```python
from __future__ import annotations

from uuid import UUID
from fastapi import APIRouter, Depends, HTTPException, status
import asyncpg

from app.dependencies import get_active_org_id, get_current_user, get_user_pg_connection
from app.models.expert import ExpertBundle, ExpertBundleCreate, ExpertBundleUpdate
from app.services.entitlement_service import require_capability
from app.services.expert_service import (
    ResolvedExpertBundle,
    create_expert_service,
    get_expert_service,
    list_experts_service,
    resolve_expert_bundle,
    update_expert_service,
    delete_expert_service,
)

router = APIRouter(
    prefix="/experts",
    tags=["experts"],
    dependencies=[Depends(require_capability("experts"))],
)
```
</interfaces>
</context>

<tasks>

<task type="execute" name="task-259-03-01-api-router-and-mount">
  <read_first>
    - backend/app/services/expert_service.py
    - backend/app/services/entitlement_service.py
    - backend/app/main.py
  </read_first>
  <action>
    1. Create `backend/app/api/experts.py` implementing:
       - `POST /experts`: Create custom expert bundle for caller's active org.
       - `GET /experts`: List accessible expert bundles (system + org).
       - `GET /experts/{bundle_id}`: Fetch raw expert bundle.
       - `GET /experts/{bundle_id}/resolve`: Fetch resolved bundle with two-phase member boundary checks (scrubbing foreign org members).
       - `PATCH /experts/{bundle_id}`: Update non-system bundle owned by caller org.
       - `DELETE /experts/{bundle_id}`: Delete non-system bundle owned by caller org.
       - All routes guarded by `Depends(require_capability("experts"))`.
    2. Mount in `backend/app/main.py`:
       - Add import: `from app.api.experts import router as experts_router`
       - Add route mount: `app.include_router(experts_router)`
       - G-5 honoured by construction: strictly compositional (2 lines added, 0 logic change).
    3. Update `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md`:
       - Add row for `backend/app/api/experts.py` (0/0/0) AT CREATION.
       - Update triple for `backend/app/main.py` (`83 / 60 / 952`).
  </action>
  <acceptance_criteria>
    - `backend/app/api/experts.py` defines router with `prefix="/experts"` and `Depends(require_capability("experts"))`
    - `backend/app/main.py` contains `include_router(experts_router)`
    - `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` updated with `backend/app/api/experts.py`
  </acceptance_criteria>
</task>

<task type="execute" name="task-259-03-02-entitlement-tests">
  <read_first>
    - backend/app/api/experts.py
    - backend/tests/unit/test_258_workflow_entitlement_gate.py
  </read_first>
  <action>
    Create `backend/tests/unit/test_259_expert_entitlement_gate.py`:
    1. Test refusal on Standard org:
       - Request to `GET /experts` or `POST /experts` from an org on `standard` tier returns HTTP 403 Forbidden.
       - Assert response JSON contains `{"error": "entitlement_required", "capability": "experts", "required_tier": "enterprise", "current_tier": "standard"}`.
    2. Test admission on Enterprise org:
       - Request from an org on `enterprise` tier proceeds (returns 200 OK / 201 Created).
    3. Test additive add-on override:
       - Request from an org on `standard` tier with `organizations.add_ons = {"experts": true}` is admitted.
    4. Test single-home compliance:
       - AST scan verifies that `backend/app/api/experts.py` contains 0 direct queries or attribute accesses of `subscription_tier`.
  </action>
  <acceptance_criteria>
    - `pytest backend/tests/unit/test_259_expert_entitlement_gate.py` exits 0 with all test cases green
  </acceptance_criteria>
</task>

<task type="execute" name="task-259-03-03-closed-core-inventory-fence">
  <read_first>
    - backend/app/services/harness/phase_types.py
    - backend/app/services/harness/emitters.py
    - backend/app/services/tool_dispatcher.py
    - docs/EXTENSION-CONTRACT.md
  </read_first>
  <action>
    Create `backend/tests/unit/test_259_closed_core_inventory.py`:
    1. Use `ast.parse` to inspect `backend/app/services/harness/phase_types.py`:
       - Assert `len(PHASE_TYPE_REGISTRY_ENTRIES) == 7`.
       - Assert no entry name starts with `expert`.
    2. Inspect `backend/app/services/harness/emitters.py`:
       - Assert `len(EMITTER_REGISTRY) == 4`.
    3. Inspect `backend/app/services/tool_dispatcher.py`:
       - Assert `_TOOL_REGISTRY` contains exactly the established closed core tools.
       - Assert no dynamic registration or `expert_*` execution dispatcher exists.
    4. Walk `backend/app/services/`:
       - Assert 0 files match `*expert*agent*`, `*expert*loop*`, or `*expert*runtime*`.
    5. Non-vacuity RED drive:
       - Plant a mock executor in `phase_types.py` or a scratch module; assert test fails RED.
       - Remove plant, verify test PASS and md5 hash integrity.
  </action>
  <acceptance_criteria>
    - `pytest backend/tests/unit/test_259_closed_core_inventory.py` exits 0 with all test cases green
    - Non-vacuous test failure verified against planted violation and restored
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pytest backend/tests/unit/test_259_expert_entitlement_gate.py` passes 100%.
- `pytest backend/tests/unit/test_259_closed_core_inventory.py` passes 100%.
- `node scripts/check-hot-file-ledger.cjs 259` reports OK.
</verification>
