---
phase: 196-registry-backed-model-picker-canvas
plan: 05
type: execute
wave: 3
depends_on: ["196-04"]
files_modified:
  - frontend/src/components/workflows/modelFitness.ts
  - frontend/src/components/workflows/modelFitness.test.ts
  - frontend/src/components/workflows/ModelField.tsx
  - frontend/src/components/workflows/ModelField.test.tsx
  - frontend/src/hooks/useModelRegistry.ts
  - frontend/src/hooks/__tests__/useModelRegistry.test.ts
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "AUTH-04: the control that sets a step's model offers only ids the live registry knows — there is no free-text path through it."
    - "D-04: blank stays the empty string on the wire; the picker changes no persisted shape and no stored value migrates."
    - "D-05: blank is a NAMED leading option — `Use the run's model` — not an empty slot."
    - "D-06: the inherit option's own LABEL carries the hedge and names the value the run would actually inherit; when the server resolves none, it degrades to the bare sentence and never to a guessed id. There is NO always-on `Effective: X` footer — that shape asserts a fixed default the code does not implement, which is the exact class of lie this phase removes."
    - "D-07: a disabled model is never offered but is KEPT as `(current)` if already stored — and opening the form NEVER calls onChange or onPersist. Viewing a workflow is not editing it."
    - "D-08: an unknown model is kept as `(current)` and NAMES its consequence in user words; saving is not blocked."
    - "D-12: fitness is annotated on `llm_emit` ONLY — the other three step types get a plain flat list, because on them the tier predicts nothing and a warning that predicts nothing trains people to ignore the ones that do."
    - "D-15: engine words never become user words — the tier token appears only under the ⌥ Technical-names reveal."
    - "A `coerce` model is distinguishable from a `force`/`force_strict` one BEFORE selection, structurally rather than by scanning: grouping means a coerce row can never sit adjacent to a force row."
    - "The picker holds NO component state and NO effect at all, fenced on its own source with a positive control — a component with no effect cannot fire a write on mount."
  artifacts:
    - path: "frontend/src/components/workflows/modelFitness.ts"
      provides: "the tier -> user-words mapping plus the `?? coerce` read-time default and its boundary guard"
      min_lines: 40
    - path: "frontend/src/components/workflows/ModelField.tsx"
      provides: "the registry-backed picker — pure function of props, no fetch, no state, no effect"
      min_lines: 70
    - path: "frontend/src/hooks/useModelRegistry.ts"
      provides: "the ONE fetch of the author registry, owned above the panel"
      min_lines: 40
  key_links:
    - from: "frontend/src/hooks/useModelRegistry.ts"
      to: "getAuthorModelRegistry"
      via: "a single load effect in a leaf hook, never inside the panel"
      pattern: "getAuthorModelRegistry"
    - from: "frontend/src/components/workflows/ModelField.tsx"
      to: "frontend/src/components/workflows/modelFitness.ts"
      via: "optgroup labels for the llm_emit mount only"
      pattern: "modelFitness"
---

<objective>
Build the picker itself, as three new leaf files that the panel can mount in one line each.

Purpose: AUTH-04 in one sentence — *"a user selects the model for a step from the live model registry,
rather than typing a model name or slug by hand."* Today the four `AI model` inputs in
`PhaseFormPanel.tsx` (`:877`, `:913`, `:965`, `:1067`) are free-text `TextField`s validated by nothing.

⚠ **This plan builds the components; plan 196-08 mounts them.** The split is not bureaucratic: the panel
is a G-5-firing file under a standing one-gated-line order and a mechanical source fence, so everything
the picker computes must exist OUTSIDE it before the mount is written. A planner who builds and mounts in
one plan is one refactor away from putting `useMemo`/`.filter(`/`.map(` into the guarded file.

⚠ **G-2 (sketch before UI) fired and was DECLINED on a reason (D-21):** the picker idiom ships twice
already — `JudgeModelPicker.tsx` (Phase 137.1, sketch 024-A) and `ModelDefaultPreference.tsx` (Phase 167)
— so the acceptance bar exists in code, not in a drawing. Re-drawing an atom that already ships is
precisely the structural sketch→build drift `SEED-155` records. **Build against the shipped components,
not against a mockup.**

⚠ **The closest analog must be partially SUBTRACTED, not copied.** `ModelDefaultPreference.tsx` gives the
`(current)` retention, the dedupe/sort, the option order and the disabled classes — but its
`useState`/`useEffect` fetch pair is exactly what this plan's source fence forbids, because it mounts
ONCE as a page-level card while `ModelField` mounts FOUR times inside one panel. And its always-on
footer (``Effective model: ${effective}``) is the exact assertion D-06 forbids.

Output: `modelFitness.ts` (pure vocabulary), `ModelField.tsx` (pure presentation), `useModelRegistry.ts`
(the one fetch), each with its own test, and the two ungated suites adopted into the count gate.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-CONTEXT.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-RESEARCH.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-PATTERNS.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-VALIDATION.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-04-SUMMARY.md
@CLAUDE.md

**Project skill — load before writing any markup:** `Skill("sketch-findings-agentic-rag")` for the 024-A
picker idiom, the Phase 127 ICON CONVENTION, and SEED-085's two-audience ⌥ Technical-names vocabulary.

<interfaces>
<!-- Extracted this session. Re-open every file — line numbers rot. -->

frontend/src/lib/api.ts (added by plan 196-04):
  interface AuthorModelRow { model_id, provider, capability_source, enabled, deprecated,
                             emit_tier: "force_strict" | "force" | "coerce" | null }
  getAuthorModelRegistry(): Promise<{ models: AuthorModelRow[]; run_default_model: string | null }>

frontend/src/components/settings/ModelDefaultPreference.tsx — CLONE these three, SUBTRACT the fetch:
  :67-70  const models = Array.from(new Set(...)).sort((a,b) => a.localeCompare(b))
          const currentIsUnknown = !!defaultModel && !models.includes(defaultModel)
  :94-103 leading auto option -> `(current)` option -> models.map(...)
  :92     "...disabled:cursor-not-allowed disabled:opacity-70"
  ❌ :28-45 the useState + useEffect fetch pair — DO NOT CLONE.
  ❌ :122-124 the always-on ``Effective model: ${effective}`` footer — the exact shape D-06 forbids.

frontend/src/components/workflows/PhaseFormPanel.tsx:336-376 — `SelectField`, the panel primitive to
  COMPOSE WITH (copy four things verbatim: `const id = useId()` + `<FieldLabel htmlFor={id} ...>`;
  onChange on the <select> and onPersist on onBlur; the exact className array; and the `caption` slot).
  ⚠ It takes `options: readonly string[]` and CANNOT express <optgroup>. Do NOT widen it — that puts
  picker logic back inside the file the D-20 fence guards. ModelField renders its own <select> and
  reuses FieldLabel.

frontend/src/components/workflows/PhaseFormPanel.tsx:876-884 — the field being replaced. Note that
  `hint` (technical, behind ⓘ) and `help` (always-visible plain sentence) are the panel's EXISTING
  two-audience seam — reuse them rather than inventing a third slot.

frontend/src/components/workflows/runVocabulary.ts — the exact template for modelFitness.ts: a
  `Record` keyed on the engine value + a lookup function, pure, no React, colocated, own .test.ts,
  with a docblock stating the fence the file exists to keep.
  ⚠ It also records the 187-24 lesson: *a comment that quotes the token a fence forbids makes the
  fence vacuous.*

backend/app/services/forced_emit.py:376-378 — the read-time default the client must MIRROR exactly:
  emit_tier = cap.get("emit_tier", "coerce")
  if emit_tier not in _RUNGS_BY_TIER: emit_tier = "coerce"   # boundary guard: unknown is coerce-safe

frontend/src/hooks/useTemplatePlaceholders.ts:1-18 — the leaf-hook template consumed by
  WorkflowBuilderPage at one call line; note "a failed read resolves to a distinct union member rather
  than to an empty success."

frontend/src/components/settings/ModelPillRow.tsx — the badging vocabulary and the ICON CONVENTION seam:
  :2   import { providerLogo, modelLogo } from "@/lib/providerLogo"
  :105 const PillMark = modelLogo(m) ?? ProviderMark      (modelLogo FIRST, providerLogo fallback)
  :128-134 the informational `deprecated` badge — deprecated ≠ disabled, the model STAYS SELECTABLE.
  ⚠ Its chips are DOM siblings of a <button>. A native <option> cannot host a chip or a logo at all.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The fitness vocabulary leaf</name>
  <files>frontend/src/components/workflows/modelFitness.ts, frontend/src/components/workflows/modelFitness.test.ts</files>
  <behavior>
    - `force_strict` maps to the user sentence "Can fill a document — guaranteed format".
    - `force` maps to "Can fill a document".
    - `coerce` maps to "Best-effort only — may not fill a document".
    - `null` and `undefined` map to the `coerce` sentence — the read-time default, mirroring the backend.
    - An UNRECOGNISED tier string (an operator typo) maps to the `coerce` sentence, never to blank — the boundary guard, in the pessimistic direction.
    - The technical form for each tier is exactly `emit_tier: <value>`, and for an absent/unknown tier it is `emit_tier: coerce` (the value the engine will actually use), never the raw unknown string.
    - Grouping order is deterministic and puts the strongest guarantee first: force_strict, force, coerce.
  </behavior>
  <read_first>
    - frontend/src/components/workflows/runVocabulary.ts (the whole file — its docblock's fence statement, its purity declaration, its `Record` + lookup-function export shape)
    - frontend/src/components/workflows/runVocabulary.test.ts
    - backend/app/services/forced_emit.py:210-230, :370-380 (`_RUNGS_BY_TIER` and the read-time default with its boundary guard)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (SEED-085's two-audience ⌥ Technical-names vocabulary)
  </read_first>
  <action>
Create `frontend/src/components/workflows/modelFitness.ts`, modelled on `runVocabulary.ts`: pure data and
pure functions, no React, no hooks, no JSX, type-only imports where possible so the module contributes
nothing to the runtime graph and an ESM cycle is impossible by construction.

Export a `Record` keyed on the tier engine value mapping to the three user sentences above, plus a lookup
function taking `string | null | undefined` and applying the `?? "coerce"` default AND the boundary guard
— an unrecognised string lands on `coerce`, never on blank. ⚠ Copy the guard, not only the default: a DB
row an operator typo'd must show as best-effort, not as unlabelled. Blank is the optimistic direction and
optimism here is the failure mode.

Export a second lookup returning the technical form (`emit_tier: force_strict` etc.) for the ⌥
Technical-names reveal, and a deterministic tier ORDER constant putting the strongest guarantee first.

Write the docblock in `runVocabulary.ts`'s register: state the fence this file exists to keep — the
DERIVATION of a tier lives on the server (`forced_emit.py`'s ladder) and this module holds no derivation
at all; this module owns the canvas's WORDS for those tiers and holds no ladder logic at all. Name D-12,
D-13 and D-15.

⚠ **Check the fence's scope before writing prose.** `runVocabulary.ts:19-33` records the 187-24 lesson
that *a comment that quotes the token a fence forbids makes the fence vacuous*. Task 2 adds a source
fence over `ModelField.tsx`'s own `?raw` text asserting zero `useEffect` / `useState`. If that fence's
scope is ever widened to include this file, its docblock must not contain those tokens either — so do not
write them here.

Write `modelFitness.test.ts` covering every bullet in `<behavior>`, including the unrecognised-string case
and the absent-tier case. Drive it RED first (the module does not exist), then GREEN.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/modelFitness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'from "react"|useState|useEffect|useMemo|JSX|tsx' frontend/src/components/workflows/modelFitness.ts` is 0.
    - The three user sentences appear verbatim in the module, exactly as written in `<behavior>`.
    - `modelFitness.test.ts` contains a case for an unrecognised tier string and a case for `null`, and both assert the `coerce` sentence.
    - The suite passes; its case count is recorded.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0.
  </acceptance_criteria>
  <done>One module owns the tier vocabulary in both audiences, defaults and guards exactly as the backend ladder does, and holds no derivation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The picker component and its no-write-on-open fence</name>
  <files>frontend/src/components/workflows/ModelField.tsx, frontend/src/components/workflows/ModelField.test.tsx</files>
  <behavior>
    - Registry-only: the option set is exactly [inherit] + [optional `(current)`] + the enabled rows from the `models` prop. A model absent from the prop is never selectable, and there is no text input anywhere in the rendered output.
    - The leading option has value `""` and reads `Use the run's model — today that would be <runDefaultModel>` when `runDefaultModel` is a non-empty string.
    - When `runDefaultModel` is `null`, the same option reads the bare `Use the run's model` — no clause, no guessed id, and NO footer asserting a default anywhere in the component.
    - A stored value that is DISABLED in the registry (`gpt-5.2`) is not in the offered list but IS present as `gpt-5.2 (current)` and is the `<select>`'s selected value.
    - A stored value that is UNKNOWN (`not-a-real-model`) is present as `not-a-real-model (current) — not in the registry`, is selected, and the field renders the caption `not in the registry — forced emission unavailable, document steps run best-effort`.
    - ⚠ Rendering with a disabled value, and separately with an unknown value, calls `onChange` ZERO times and `onPersist` ZERO times.
    - `onPersist` fires on blur; `onChange` fires only from the `<select>`'s own change event.
    - A DEPRECATED but enabled model IS offered and selectable (deprecated ≠ disabled).
    - With `showFitness` set, options are grouped and a `coerce` model sits under a different group label than a `force_strict` one; the group labels are the user sentences from `modelFitness.ts`.
    - Without `showFitness`, there are no groups at all and no tier text appears anywhere.
    - With `showTechnical` set, the group labels carry the `emit_tier: <value>` token; without it, that token appears nowhere in the rendered output.
    - Options are deduped and sorted by `localeCompare` within their group.
  </behavior>
  <read_first>
    - frontend/src/components/settings/ModelDefaultPreference.tsx (FULL — clone :67-70, :94-103, :92; explicitly DO NOT clone :28-45 or :122-124)
    - frontend/src/components/settings/ModelDefaultPreference.test.tsx (FULL — the option-set assertion with `within(select).getAllByRole("option")`, the `(current)` retention case, and the fixture-builder + `beforeEach` pattern)
    - frontend/src/components/settings/JudgeModelPicker.tsx (the leading auto/default option shape and its own `(current)` idiom)
    - frontend/src/components/workflows/PhaseFormPanel.tsx:336-380 (`SelectField` — compose with `FieldLabel`, `useId`, the className array and the `caption` slot; do NOT widen it)
    - frontend/src/components/workflows/PhaseFormPanel.tsx:868-925 (the `TextField` mounts being replaced, and the `hint` / `help` two-audience seam)
    - frontend/src/components/settings/ModelPillRow.tsx:95-140 (the deprecated-badge vocabulary and the `modelLogo(m) ?? ProviderMark` order)
    - frontend/src/components/workflows/PhaseFormPanel.test.tsx:20-30, :475-500 (the `?raw` import form and the shipped source-fence shape)
    - frontend/src/components/chat/StopControl.test.tsx (a second shipped source-fence example)
  </read_first>
  <action>
Create `frontend/src/components/workflows/ModelField.tsx`.

**Props** (this exact set): `value: string` (the stored `cfg.model`, `""` meaning inherit);
`onChange: (v: string) => void`; `onPersist: () => void`; `models: AuthorModelRow[]` (the union, fetched
ONCE by an owner above the panel); `runDefaultModel: string | null`; `showFitness?: boolean` (D-12 —
true on the `llm_emit` mount only); `showTechnical?: boolean` (the ⌥ reveal, threaded from the panel's
existing flag); `disabled?: boolean`.

**It is a pure function of props. NO `useState`. NO `useEffect`. NO fetch.** This is not a style
preference: the component mounts four times inside one panel, four fetches per open is wrong, and a
component with no effect **cannot** fire a write on mount — which is how D-07's *"the form must NOT
rewrite a stored value as a side effect of being opened"* becomes a guarantee rather than an accident.
Both shipped pickers get that property right by luck (they persist only in `onSelect`); this one gets it
by construction. `useId` is permitted and expected (it is not state).

⚠ **Do not write the words `useState` or `useEffect` anywhere in this file — not even in a comment.**
The source fence below greps the file's own `?raw` text, and `runVocabulary.ts:19-33` records the 187-24
lesson that a comment quoting a forbidden token makes the fence vacuous.

**Option assembly, in this order:**
1. **Inherit** — `value=""`, label `Use the run's model` plus, only when `runDefaultModel` is non-empty,
   the clause ` — today that would be <id>`. ⚠ The hedge lives in THIS LABEL. Do **not** add an always-on
   `Effective: <id>` footer: that is the shape both shipped pickers use and it asserts a fixed default
   the code does not implement — `_effective_model` inherits *whatever model started the run*, knowable
   at run time and not at authoring time. When `runDefaultModel` is `null`, degrade to the bare sentence,
   **never** to a guessed id.
2. **`(current)` retention** — rendered if and only if `value !== ""` and `value` is not among the
   offerable rows. ONE branch covers both D-07 (disabled) and D-08 (unknown); only the SUFFIX differs, by
   whether the id appears in `models` at all. Disabled -> `<id> (current)`. Unknown ->
   `<id> (current) — not in the registry`, plus the caption
   `not in the registry — forced emission unavailable, document steps run best-effort` in `SelectField`'s
   `caption` slot shape. Saving is **not** blocked (D-08): an operator retiring a registry row must not
   make existing workflows unsaveable.
3. **Offerable rows** — `models` filtered to `enabled`, deduped with the `Array.from(new Set(...))` idiom
   and sorted by `localeCompare`. A **deprecated** row stays offered and selectable (D-149-04 — deprecated
   is not disabled); only `enabled === false` removes an option.

**Fitness (D-12, `showFitness` only).** Group the offerable rows into `<optgroup>`s whose `label` is the
user sentence from `modelFitness.ts`, in the strongest-first order that module exports. Grouping is chosen
over a per-option suffix for a measured reason worth keeping in a comment: a native `<option>` cannot
render markup, so a "chip" is a text suffix either way — and with grouping a `coerce` model can never
appear adjacent to a `force` one, so the distinction survives inattention rather than requiring a user to
read all 69 lines. Under `showTechnical`, the group label additionally carries `emit_tier: <value>`; the
**option text (the model id) never changes**, so the value the user picks is never ambiguous (D-15,
SEED-085).

The other three step types get a **plain flat list, no groups, no tier text** — D-12's reason, worth a
comment: on `llm_single`/`llm_agent` the tier predicts nothing, and a warning that predicts nothing
trains people to ignore the ones that do.

Compose with the panel's primitives: `const id = useId()`, `<FieldLabel htmlFor={id} ...>`, the exact
`className` array `SelectField` uses, `onChange` on the `<select>`, `onPersist` on `onBlur`, and the
caption paragraph shape. Reuse the panel's `hint` (technical, behind ⓘ) and `help` (plain sentence)
two-audience seam rather than inventing a third slot.

⚠ **No logo.** The Phase 127 ICON CONVENTION's seam is `@/lib/providerLogo` (`modelLogo` first,
`providerLogo` fallback) — but a native `<option>` cannot host a mark at all, so any mark on this surface
would have to sit outside the `<select>`. This task adds none; if one is ever wanted it comes from that
module and never from a direct `@lobehub/icons` import.

Create `frontend/src/components/workflows/ModelField.test.tsx` covering every bullet in `<behavior>`.
Three things about it are load-bearing:

- **There is NO `vi.mock("@/lib/api")` block.** The component takes `models` as a prop, so there is
  nothing to mock. That absence is itself the proof of the no-fetch property — say so in a comment.
- **The no-write-on-open cases are genuinely novel** — no shipped test in this tree asserts a picker
  performs zero writes on mount. Transpose the backend idiom (`assert not pool.calls, "the guard must
  409 BEFORE any write"`) into `expect(onChange).toHaveBeenCalledTimes(0)` and
  `expect(onPersist).toHaveBeenCalledTimes(0)`, and additionally assert the `<select>`'s `.value` equals
  the stored id.
- **The fixture must use the interesting values.** `gpt-5.4` is registry-known, enabled and
  `force_strict` — it passes every check this plan adds and proves nothing. Build the `models` fixture
  from `gpt-5.2` (disabled), `gpt-5.5` (code-only, `force_strict`), `glm-4.7-flash` (DB-only, local,
  `emit_tier: null` -> coerce), `gemini-3.6-flash` (DB-only, `emit_tier: null`), `kimi-k2.6` (the only
  native `coerce` rows) and one deprecated-but-enabled row.

Add a **SOURCE fence** in `ModelField.test.tsx` using the `?raw` import form
(`import modelFieldSource from "./ModelField?raw"`): assert the source contains zero occurrences of
`useEffect` and zero of `useState`, with (a) a POSITIVE CONTROL running the same extractor over an inline
fixture string that DOES contain them, and (b) a NON-VACUITY assertion that the source is non-empty and
contains `<select`. A fence with no observed control and no vacuity floor is not evidence.

Also add an idempotence case for A6: blurring the `<select>` without changing it calls `onPersist`, and
the component reports the same `value` afterwards — the panel persists on blur and a user tabbing past
the field must not alter the stored value. `SelectField` has wired `onBlur={onPersist}` for
`citation_policy` since Phase 185, but *"the old field did it too"* is not the same as *"it is correct"*,
so assert it for the new field.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/ModelField.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'useState|useEffect' frontend/src/components/workflows/ModelField.tsx` is **0** (comments included).
    - `grep -cE '<input|type="text"|contentEditable' frontend/src/components/workflows/ModelField.tsx` is 0 — there is no free-text path.
    - `grep -c 'Effective model:\|Effective:' frontend/src/components/workflows/ModelField.tsx` is 0 — D-06's forbidden footer shape is absent.
    - `grep -c '@lobehub/icons' frontend/src/components/workflows/ModelField.tsx` is 0.
    - `grep -c 'gpt-5.4' frontend/src/components/workflows/ModelField.tsx` is 0 — no hardcoded default id anywhere in the component.
    - The exact strings `Use the run's model`, `(current)`, `not in the registry`, and `not in the registry — forced emission unavailable, document steps run best-effort` all appear in the component source.
    - `ModelField.test.tsx` contains no `vi.mock("@/lib/api")`, contains the source fence WITH a positive control and a non-vacuity assertion, and contains at least one case each for: registry-only option set, the hedged inherit label, the null-runDefault degradation, disabled `(current)`, unknown `(current)` + caption, zero-writes-on-open (both values), deprecated-stays-selectable, fitness grouping under `showFitness`, no grouping without it, `emit_tier:` present only under `showTechnical`, and blur idempotence.
    - The fixture contains `gpt-5.2`, `gpt-5.5`, `glm-4.7-flash`, `gemini-3.6-flash` and `kimi-k2.6`.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 and the suite passes; the case count is recorded.
  </acceptance_criteria>
  <done>A stateless, fetchless, registry-only picker exists; it hedges rather than asserts, keeps what it cannot offer, names the consequence, distinguishes fitness structurally on the deliverable step only, and provably writes nothing on open.</done>
</task>

<task type="auto">
  <name>Task 3: The one fetch, in a leaf hook, and the two gate adoptions</name>
  <files>frontend/src/hooks/useModelRegistry.ts, frontend/src/hooks/__tests__/useModelRegistry.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - frontend/src/hooks/useTemplatePlaceholders.ts (FULL — the leaf-hook template: derived-and-frozen waiting readings, the abort predicate, and "a failed read resolves to a distinct union member rather than to an empty success")
    - frontend/src/hooks/useGroundingBundle.ts (the module shape `useTemplatePlaceholders` names as its own model)
    - frontend/src/pages/WorkflowBuilderPage.tsx:975-990, :1135-1150 (how the two existing leaf hooks are consumed — ONE call line each; plan 196-08 adds the third)
    - frontend/src/lib/api.ts (`getAuthorModelRegistry`, added by plan 196-04)
    - scripts/vitest-count-gate.cjs:2280-2400 (the TARGETS/BASELINE two-knob rule and the "every adoption AND every decline carries a written reason" habit; and the rule that a BASELINE key naming a path that does not yet exist makes the gate ERROR, so the pin ships in the creating commit)
  </read_first>
  <action>
Create `frontend/src/hooks/useModelRegistry.ts` — the ONE place this phase calls
`getAuthorModelRegistry()`. Model it on `useTemplatePlaceholders.ts`: a single load effect with the
shipped abort discipline, an exported named return type rather than an inline object type, and a docblock
that spells the states as bullets before any code.

Return the rows, the `run_default_model`, and a reading for the not-yet-loaded and failed states.
⚠ Follow the sibling's rule: **a failed read must resolve to a distinct member, not to an empty
success.** An empty `models` array from a failed fetch is indistinguishable from a genuinely empty
registry, and the picker would then silently offer nothing but the inherit option — which looks exactly
like a correct render. Model it so that is unconstructable.

State in the docblock why the fetch lives here and not in the component: `ModelField` mounts four times
inside one panel, so four fetches per open would be wrong, and the registry payload is a panel-level
concern. This siting is also what makes the D-20 fence satisfiable — the panel body gains no effect.
Record Open Q3's answer too: no pagination, 69 rows at measurement time, revisit at ~500.

Create `frontend/src/hooks/__tests__/useModelRegistry.test.ts`. Mock `@/lib/api` (this hook DOES fetch,
unlike the component) and cover: a successful load exposes the rows and `run_default_model`; a failed read
resolves to the distinct failure reading and NOT to an empty success; an in-flight read is distinguishable
from a completed empty one; and the hook calls `getAuthorModelRegistry` exactly once per mount.

Finally, adopt the hook suite into the count gate. Measured: `scripts/vitest-count-gate.cjs`'s `TARGETS`
has exactly ONE bare-directory entry, `src/components/workflows`, so Tasks 1 and 2's suites are gated
automatically and need **BASELINE pins only**. `src/hooks` has no entry at all, so this task's suite is
**UNGATED** until added. Add:
- `src/hooks/__tests__/useModelRegistry.test.ts` to **TARGETS** (it runs) **and** to **BASELINE** (it is
  pinned), in this same commit — a BASELINE key naming a path that does not yet exist makes the gate
  ERROR, so the pin belongs here and nowhere else.
- BASELINE pins for `src/components/workflows/modelFitness.test.ts` and
  `src/components/workflows/ModelField.test.tsx`.

Write the stated reason in the script's own comment register for each: an unpinned file is not a
lightly-guarded one, it is an unguarded one — and `ModelField.test.tsx` in particular carries the source
fence that is the only mechanical guard on the no-write-on-open property.

⚠ **Set every pin from the gate's own printed `actual` column, never from a number quoted in any
document** — including this one. Then run the gate and read the verdict line.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/hooks/__tests__/useModelRegistry.test.ts src/components/workflows/ModelField.test.tsx src/components/workflows/modelFitness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getAuthorModelRegistry' frontend/src/hooks/useModelRegistry.ts` is at least 1, and `grep -rc 'getAuthorModelRegistry' frontend/src/components/` is 0 — the component never fetches.
    - The hook's return type is an exported named type, and the docblock lists its states as bullets and names the failed-read-is-not-an-empty-success rule.
    - The hook test asserts `getAuthorModelRegistry` was called exactly once per mount, and has a distinct case for the failure reading.
    - `scripts/vitest-count-gate.cjs` contains `src/hooks/__tests__/useModelRegistry.test.ts` in BOTH `TARGETS` and `BASELINE`, and `ModelField.test.tsx` + `modelFitness.test.ts` in `BASELINE`, each with an adjacent comment stating the reason.
    - The three suites named in the verify command all pass, and each case count is recorded.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` is run and its verdict line pasted verbatim into the summary, paired with the per-file deltas for the three new files. ⚠ Do NOT make "the gate is green" the bar on its own. If it reds: capture failing filenames from the gate's persisted JSON BEFORE re-running, check `git diff --numstat` and `git status --short`, and if the file is byte-unchanged and one of SEED-171's three (`src/pages/WorkflowsPage.test.tsx`, `src/components/workflows/library/WorkflowCard.test.tsx`, `src/pages/WorkflowBuilderPage.session.test.tsx`) record it as an observation and write "provably unmodified", never "fine". ⚠ **Do NOT adjust `GSD_VITEST_MAX_WORKERS`** — SEED-171 measures that adjusting the cap does not fix these, and all three of those suites run on every gate run for this phase because one of them lives inside the bare `src/components/workflows` target.
    - `git diff --name-only` contains neither `SettingsPage.tsx` nor `ModelPillRow.tsx` (SC#3 fence).
  </acceptance_criteria>
  <done>One leaf hook owns the single registry fetch with an honest failure reading, and all three new suites run inside the gate with pins taken from the gate's own output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server payload → browser render | registry rows cross into option labels and group labels |
| user gesture → stored workflow definition | the only path by which this component may change a persisted value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-TAMP1 | Tampering | `ModelField` mount lifecycle | mitigate | The component holds no state and no effect, fenced on its own `?raw` source with a positive control and a non-vacuity floor, plus behavioural cases asserting `onChange`/`onPersist` are called zero times on render with a disabled value and with an unknown value. A form that rewrites on open is a silent integrity change to a stored definition |
| T-196-TAMP2 | Tampering | blur-without-change | mitigate | An explicit idempotence case (A6). "The old field did it too" is not a proof |
| T-196-XSS | Information Disclosure / Injection | option and optgroup labels | accept | Labels are React text children of `<option>` / `<optgroup label>`; no `dangerouslySetInnerHTML`, no HTML interpolation. Model ids and provider names are server-derived from an operator-controlled allowlisted table |
| T-196-LEAK-UI | Information Disclosure | the props surface | mitigate | The component consumes `AuthorModelRow` — the six-field author projection from plan 196-04 — never `ModelRegistryRow`. `deprecated_reason` is structurally unreachable from here |
| T-196-HONEST | Spoofing (a control that lies) | the inherit option's label | mitigate | The hedge is in the label and the id is server-resolved; there is no `Effective:` footer and no hardcoded `gpt-5.4`. Fenced by a grep for both |
| T-196-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — the `-p` is load-bearing
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/modelFitness.test.ts src/components/workflows/ModelField.test.tsx src/hooks/__tests__/useModelRegistry.test.ts` — the three in-scope, deterministic suites
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — verdict line recorded verbatim, paired with per-file deltas; SEED-171 triage protocol applies if it reds
- ⚠ NOT provable here: *"a `coerce` model is distinguishable BEFORE selection"* as a HUMAN experience. A render test proves the markup exists; it cannot prove a person sees it. That is G-4 row **U-A2** in `196-VALIDATION.md`
</verification>

<success_criteria>
- Three new leaf files exist, each with its own passing suite, and none of them fetches from inside a component
- The picker offers registry ids only, hedges the inherit option instead of asserting a default, keeps disabled and unknown values as `(current)`, names the unknown consequence, and groups fitness on the deliverable step only
- The no-write-on-open property is fenced on source AND asserted behaviourally, with a positive control observed
- The fitness vocabulary defaults and guards exactly as `forced_emit.py` does
- All three suites are inside the count gate with pins read from the gate's own output
</success_criteria>

<output>
Create `.planning/phases/196-registry-backed-model-picker-canvas/196-05-SUMMARY.md` when done.
Record verbatim: the source fence's positive-control output, each suite's case count, the gate verdict
line with per-file deltas, any SEED-171 triage observations stated as "provably unmodified", and the
exact inherit-option label string in both the resolved and the degraded form.
</output>
