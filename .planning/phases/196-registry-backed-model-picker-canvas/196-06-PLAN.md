---
phase: 196-registry-backed-model-picker-canvas
plan: 06
type: execute
wave: 3
depends_on: ["196-04"]
files_modified:
  - backend/app/services/model_registry.py
  - backend/app/api/workflows.py
  - backend/tests/test_196_save_refusal.py
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "SC#2 / D-09: an unregistered model cannot be silently selected — the refusal is on the SERVER, so it holds against a direct API call and not merely against the form."
    - "D-04: a blank model still saves. Zero data change; 239 of 257 stored phases carry no model and every one of them must round-trip untouched."
    - "D-08: an already-stored unregistered value still saves — an operator retiring a registry row must never make an existing workflow unsaveable. The refusal is scoped to NEWLY-introduced values."
    - "D-11 (CONFIRMED by measurement, not inherited): the AI-draft path emits no `config.model` at all AND `generate_workflow` returns an unpersisted definition, so the save-path refusal covers it structurally with no separate guard."
    - "The refusal fires AFTER the existing ownership resolution, so a 400 never distinguishes `not yours` from `bad model`."
    - "The refusal is an OBJECT carrying a `code`, joining `update_draft`'s existing refusal register rather than sitting above it — the client branches on `code`, never on prose."
    - "The check is a route-level policy, NOT a Pydantic validator — it is async, the draft path persists `model_dump(mode=\"json\")`, and a parse-level invariant would make an existing definition unparseable the day a row is retired."
    - "The publish path is a measured-EMPTY gap, recorded with a re-open trigger rather than closed with an eighth gauntlet stage."
  artifacts:
    - path: "backend/tests/test_196_save_refusal.py"
      provides: "the 400 on a new unregistered model, blank still saves, stored-unknown still saves, and the ordering proof"
      min_lines: 90
  key_links:
    - from: "backend/app/api/workflows.py"
      to: "backend/app/services/model_registry.assert_phase_models_registered"
      via: "one awaited call in create_draft and one in update_draft"
      pattern: "assert_phase_models_registered"
    - from: "backend/app/services/model_registry.py"
      to: "build_model_registry_rows"
      via: "registered_model_ids derived from the SAME union the picker reads"
      pattern: "registered_model_ids"
---

<objective>
Make SC#2 true on the wire, not merely in the form.

Purpose: measured — **`config.model` is validated by NOTHING today.** Not on save, not at publish, not at
run. Neither `create_draft` (`workflows.py:1147-1178`) nor `update_draft` (`:1218-1312`) inspects
`phases[*].config.model`, and there is no `model_validator` on any of the four config classes touching it.
A client-only list would reduce SC#2 to *"cannot be selected through the form"*, which is weaker than the
criterion's words and is bypassable by any direct API call. *The disable is courtesy, the server is the
wall* (T-167-14b).

⚠ **The refusal and D-08 must be reconciled deliberately, and this plan resolves it.** D-09 says refuse;
D-08 says *"saving is not blocked"* for an already-stored value, because an operator retiring a registry
row must not brick an existing workflow. Both are true if the refusal is scoped to **newly-introduced**
values: on create, any unregistered model is refused; on update, an unregistered model is refused **unless
the same phase in the STORED definition already carries that exact value**. Measured blast radius today:
**zero** phases are in the unknown state, so the grandfather branch is dead code right now — it exists
precisely so D-08's promise is true the day it stops being dead.

Output: two helpers in the union leaf, two call lines in the workflow save path, and a test file that
proves the refusal, the two allowances, and the ordering.

**G-5, honoured by construction and said so rather than assumed.** `backend/app/api/workflows.py`
measures **35 commits / 17 phases / 1962 L** and G-5 FIRES; its ledger row reads *"extraction due — not
taken in q5r (no 2nd concern)"* and names the seam: one module hosting definition CRUD, the validate/lint
surface, the grounding palette, the publish gauntlet, the run launcher and the template door. This plan
adds **a validation on the definition CRUD this module already owns**, with the LOGIC living in the
`model_registry.py` leaf; the file's diff is two call lines and one import. Under the ledger's own
repeated test — *does this add a genuinely SECOND concern?*, applied twice already — that is a call-out,
not a concern. Taking the named five-way split of a 1962-line module inside a model-picker phase would be
smuggling a refactor phase, which is exactly what G-5 exists to prevent. Plan 196-09 re-derives this
file's row and detail section at close (D-23).

**Parallel-safety:** every test patches the pool and mutates zero rows. CLAUDE.md rule 4 does not bind.

⚠ **File collision, flagged:** `backend/app/services/model_registry.py` is CREATED by plan 196-04 and
EXTENDED here. Worktrees isolate files, not merges — this plan runs in a later wave for that reason, and
the executor must confirm 196-04's version is present at HEAD before editing.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-CONTEXT.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-RESEARCH.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-PATTERNS.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-VALIDATION.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-04-SUMMARY.md
@docs/HOT-FILE-LEDGER.md
@CLAUDE.md

<interfaces>
<!-- Extracted this session. Re-open every file — line numbers rot. -->

The TWO write doors, both taking `body: WorkflowDefinition`:
  POST  /workflows                      create_draft   backend/app/api/workflows.py:1147-1178
  PATCH /workflows/{definition_id}      update_draft   backend/app/api/workflows.py:1218-1312

`config.model` lands on four _StrictBase (extra="forbid") classes, all `str | None = None`:
  models/harness.py:80  LlmSinglePhaseConfig
  models/harness.py:103 LlmAgentPhaseConfig
  models/harness.py:122 LlmBatchAgentsPhaseConfig
  models/harness.py:157 LlmEmitPhaseConfig

The hook point in create_draft (workflows.py:1166-1168) — the refusal goes AFTER this block:
  pool = await get_pg_pool()
  user_id = _coerce_user_id(current_user)
  # Force draft status server-side — never trust the client's ``status``:
  body = body.model_copy(update={"status": "draft"})

The refusal register to JOIN, workflows.py:1296-1310 — and its two binding comments:
  if cause == "stale_token":
      raise HTTPException(409, detail={"code": "stale_token", "message": "...", "token": ...})
      # THE CLIENT BRANCHES ON ``code``, NEVER ON THIS PROSE.
  # ``not_found`` — and any cause this route does not recognise — fails closed to the dullest
  # answer there is. A new refusal cause must be mapped deliberately.
  raise HTTPException(404, detail="draft not found")
  ⚠ update_draft's not_found is deliberately "the dullest of them" (:1256-1260) — the model refusal
    must fire AFTER ownership resolution so a 400 never acts as an existence oracle.

The prose register for the message, me_preferences.py:87-96:
  raise HTTPException(400, detail="That model is not in your organization's enabled set. "
                                  "Pick a model your administrator has enabled.")

Why NOT a Pydantic validator — three measured reasons:
  1. the check is async (it reads the override cache); Pydantic v2 validators are sync
  2. models/harness.py:384-389 and :428-433 record the file's own standing rule — the draft save path
     persists model_dump(mode="json"), so a derivation living in the model would be BAKED into the JSONB
  3. D-08 requires an already-stored unregistered value to stay saveable, which needs a route-level
     policy, not a parse-level invariant

The AI-draft path, measured:
  api/workflows.py:1577  generate_workflow returns {ok, definition} and DOES NOT PERSIST; the client
    then POSTs/PATCHes it, so create_draft/update_draft cover it structurally.
  grep '"model"' backend/app/services/workflow_authoring.py -> ZERO hits emitting a phase config.model.

The no-write assertion shape to transpose, tests/test_149_default_guard.py:31-40, :76-86:
  class _RecordingPool: ... async def execute(self, sql, *args): self.calls.append(...)
  assert not pool.calls, "the disable guard must 409 BEFORE any capability write"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The refusal helpers in the leaf, driven RED</name>
  <files>backend/app/services/model_registry.py, backend/tests/test_196_save_refusal.py</files>
  <behavior>
    - `POST /workflows` whose definition carries `config.model = "not-a-real-model"` on any phase returns **400** with an object detail carrying `code == "unknown_model"`, the offending `phase_slug` and the offending `model`.
    - The same request reaches **no write**: the recording pool's `.calls` is empty.
    - `POST /workflows` with every phase blank (`config.model` absent or `""`) succeeds — the 93 % case.
    - `POST /workflows` with a registered-and-enabled model succeeds.
    - `POST /workflows` with a registered but DISABLED model (`gpt-5.2`) succeeds — disabled is a run-time concern (D-10), not a save-time refusal. Saving is about registry membership, not availability.
    - `PATCH /workflows/{id}` introducing a NEW unregistered value returns 400 with the same shape.
    - `PATCH /workflows/{id}` where the stored definition ALREADY carries that exact unregistered value on that same phase slug SUCCEEDS — D-08's grandfather.
    - A `PATCH` from a user who does not own the draft returns the SAME dull `404` it returns today, even when the body also carries an unregistered model — the refusal never acts as an existence oracle.
  </behavior>
  <read_first>
    - backend/app/services/model_registry.py (as created by plan 196-04 — confirm `build_model_registry_rows` and `to_author_row` are present at HEAD before editing)
    - backend/app/api/workflows.py:1140-1185 (create_draft) and :1218-1315 (update_draft, including the refusal register and the deliberately-dull not_found)
    - backend/app/models/harness.py:70-165, :380-435 (the four config classes and the file's own standing rule about derivations being baked into the JSONB)
    - backend/app/api/me_preferences.py:80-100 (the 400 prose register)
    - backend/tests/test_149_default_guard.py:30-45, :76-90 (`_RecordingPool` and the empty-`.calls` assertion)
    - backend/tests/test_149_registry_read.py:34-38, :89-102 (`_reset_override_caches` and `_prime_endpoint`)
  </read_first>
  <action>
Add two functions to `backend/app/services/model_registry.py`.

`async def registered_model_ids() -> set[str]` — the set of every `model_id` in
`build_model_registry_rows()`. ⚠ It must derive from **that same union function**, never from
`enabled_model_allowed_set()`. Two `enabled` semantics ship today (an absent override row means *enabled*
to `_registry_row` and *not offerable* to `enabled_model_allowed_set`), and using the narrower one here
would refuse a save for 32 models the runtime happily runs. `enabled_model_allowed_set` must not appear
in this plan's diff.

⚠ Membership, not availability: the set is every id the registry KNOWS, including disabled and deprecated
rows. A disabled model is a run-time concern that plan 196-03 handles with a fallback and a notice; making
it a save-time refusal would mean disabling a model retroactively breaks every workflow that names it,
which is the same brittleness D-08 forbids. Say this in the docstring.

`async def assert_phase_models_registered(definition, *, previous=None) -> None` — walks
`definition.phases`, and for each phase whose `config.model` is a non-empty string that is **not** in
`registered_model_ids()`, raises `HTTPException(400, detail={...})` with `code="unknown_model"`, a
human-actionable `message` naming the model and what to do (the `me_preferences.py:87-96` register: name
the model, name the remedy — *pick one from the list, or leave it blank to use the run's model*), plus
`phase_slug` and `model`. It is an **object**, not bare prose, because `workflows.py`'s own comment states
the client branches on `code` and never on the message — and the Builder will want to focus the offending
phase's field.

The `previous` parameter is D-08's grandfather: when supplied (the update path), a phase whose stored
counterpart — matched by phase slug — already carries the identical value is skipped. Document the
measured blast radius in the docstring: zero phases are in the unknown state today, so this branch is
currently dead, and it exists so that retiring a registry row tomorrow does not brick a workflow that
names the retired id.

A blank or absent model is always a no-op. So is a definition with no phases.

Create `backend/tests/test_196_save_refusal.py` covering every bullet in `<behavior>`, using the
`_prime_endpoint` / `_reset_override_caches` seeding so nothing is written to the database, and a
`_RecordingPool`-shaped stand-in so the *"no write reached"* claim is asserted rather than assumed. Drive
the file RED first — the helpers do not exist yet, and the route hooks do not exist until Task 2, so
expect failures across the board and record them.

⚠ Use `not-a-real-model` for the refusal cases and `gpt-5.2` for the disabled-still-saves case;
`gpt-5.4` alone proves nothing.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./venv/Scripts/python.exe -m pytest tests/test_196_save_refusal.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'enabled_model_allowed_set' backend/app/services/model_registry.py` is 0.
    - `registered_model_ids` derives its set from `build_model_registry_rows` (verified by reading the body) and its docstring states the membership-not-availability rule.
    - `assert_phase_models_registered` raises with a `dict` detail containing the keys `code`, `message`, `phase_slug`, `model`, and `code` equals `unknown_model`.
    - `backend/tests/test_196_save_refusal.py` contains at least eight cases covering every `<behavior>` bullet, including the ownership/existence-oracle case.
    - `grep -cE 'INSERT INTO|UPDATE workflow_definitions' backend/tests/test_196_save_refusal.py` is 0.
    - The run FAILS at this task and the verbatim output is pasted into the plan summary.
  </acceptance_criteria>
  <done>The refusal logic lives in the leaf with the grandfather branch and the membership-not-availability rule stated; the full behaviour set is observed RED.</done>
</task>

<task type="auto">
  <name>Task 2: Hook the refusal into both write doors, after ownership</name>
  <files>backend/app/api/workflows.py</files>
  <read_first>
    - backend/app/api/workflows.py:1140-1185 (create_draft — the status force at :1166-1168 is the anchor)
    - backend/app/api/workflows.py:1218-1315 (update_draft — the ownership/`not_found` resolution and the refusal register at :1288-1312)
    - backend/app/api/workflows.py:1560-1600 (`generate_workflow` — confirm by reading that it returns unpersisted, so no separate AI-path guard is owed)
    - backend/app/api/workflows.py:1040-1070 (the publish route and the doubly-documented 8-stage gauntlet ordering — read it to confirm this plan does NOT touch it)
    - docs/HOT-FILE-LEDGER.md#backendappapiworkflowspy (the named seam and the "does this add a genuinely SECOND concern?" test)
  </read_first>
  <action>
Add one awaited call to each write door, delegating to the leaf. Import it **function-locally**, matching
the module's own discipline.

In `create_draft`, place the call **after** the server-side status force
(`body = body.model_copy(update={"status": "draft"})`) and **before** the definition is written. No
`previous` argument — a create has no prior definition, so any unregistered model is refused.

In `update_draft`, place the call **after** the existing ownership/`not_found` resolution has produced the
stored row, and pass that row's definition as `previous`. ⚠ **The ordering is security-relevant, not
cosmetic**: `update_draft`'s `not_found` is deliberately *"the dullest of them"*, and a 400 that fired
before ownership was resolved would turn the model check into an existence oracle telling a stranger that
a given definition id exists. The Task 1 test asserts this ordering directly; do not weaken it to a
comment.

⚠ **Do NOT implement this as a Pydantic `model_validator`** on any of the four config classes. Three
measured reasons: the check is async and Pydantic v2 validators are sync; `models/harness.py:384-389` and
`:428-433` record that file's own standing rule that the draft path persists `model_dump(mode="json")` so
a derivation living in the model would be baked into the JSONB; and D-08 needs an already-stored value to
remain saveable, which a parse-level invariant makes impossible.

⚠ **Do NOT add a second refusal to the publish gauntlet.** Publish reads the STORED definition, so a
definition saved before this phase can carry an unregistered model and publish without ever meeting the
refusal — that is real, and it is also **measured empty**: 18 stored phases carry `gpt-5.4` (registry-known
and enabled) and **zero** phases are in the unknown state. Adding a stage would be an eighth stage in an
8-stage gauntlet whose ordering is doubly documented (*"the two docstrings must never disagree"*) — a
genuine second concern in `publish_service.py` (19 commits / 7 phases, G-5 FIRES) — to catch an empty
population. And D-10's run-time enabled-check already covers the dangerous half for every path including
publish. **Record it in the summary as a known, measured, empty gap with a re-open trigger: the first
definition observed carrying an unregistered `config.model`.** Re-derive that population at execution time
against the live DB rather than trusting this paragraph — an operator can author a workflow between
planning and execution.

⚠ **D-11 is confirmed, not inherited, and needs no code.** Re-run
`grep -n '"model"' backend/app/services/workflow_authoring.py` and confirm zero hits emitting a phase
`config.model`, and read `generate_workflow` to confirm it returns `{ok, definition}` unpersisted. Record
both in the summary. The AI-draft path is therefore covered structurally by the two hooks above, with no
separate guard — and *having the drafter choose a fit model* is explicitly OUT (it collides with AUTH-02 /
Phase 197).

Re-run the Task 1 test file. Every case must be green with assertions unchanged.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./venv/Scripts/python.exe -m pytest tests/test_196_save_refusal.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'assert_phase_models_registered' backend/app/api/workflows.py` is exactly 3 (one import + two call sites) — any other number means a door was missed or duplicated.
    - `git diff --stat backend/app/api/workflows.py` shows fewer than 15 changed lines — the call-out-not-a-concern shape.
    - `git diff backend/app/api/workflows.py` contains no change inside the publish route or the gauntlet stage list, and `git diff --name-only` does not contain `backend/app/services/harness/publish_service.py`.
    - `grep -c 'model_validator' backend/app/models/harness.py` is unchanged from the pre-plan value (the two shipped validators, neither touching `model`).
    - All cases in `tests/test_196_save_refusal.py` pass with assertion text byte-identical to Task 1.
    - The ownership-ordering case passes: a non-owner PATCH carrying an unregistered model returns 404, not 400.
    - The live re-derivation of the stored-model corpus is run and recorded — the command from RESEARCH.md M-3 over `workflow_definitions` — and the count of phases in the D-08 unknown state is stated as a MEASUREMENT.
    - The `grep '"model"' backend/app/services/workflow_authoring.py` output is pasted into the summary (D-11 confirmed at execution time, not inherited).
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q` shows no NEW failures against the pre-plan baseline.
  </acceptance_criteria>
  <done>Both write doors refuse a newly-introduced unregistered model with an object-shaped 400 after ownership resolution; blank, disabled and grandfathered values all still save; publish is left alone with its gap recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author browser (or any HTTP client) → `POST` / `PATCH /workflows` | an arbitrary `config.model` string crosses into a stored definition |
| stored definition → run time | a persisted model string is later handed to a provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-196-IV1** | Tampering / Input Validation (ASVS V5) | `create_draft` / `update_draft` | mitigate | `assert_phase_models_registered` raises 400 BEFORE any write, asserted by an empty `.calls` on a recording pool. A client-only list is bypassable by any direct API call — the disable is courtesy, the server is the wall |
| T-196-ORACLE | Information Disclosure | `update_draft` refusal ordering | mitigate | The model check fires AFTER ownership resolution, so a 400 never distinguishes "not yours" from "bad model". `update_draft`'s `not_found` stays deliberately the dullest answer. Asserted directly by a non-owner case, not left to a comment |
| T-196-BRICK | Denial of Service (availability of the author's own data) | retiring a registry row | mitigate | The refusal is scoped to NEWLY-introduced values; a stored value grandfathers by phase slug. A parse-level invariant would have made an existing definition unparseable, which is why this is route-level policy |
| T-196-PUB | Tampering | the publish path reading a stored definition | accept, with a re-open trigger | Measured population: zero definitions carry an unregistered model. D-10's run-time check covers the dangerous half for every path. Re-open trigger: the first definition observed with an unregistered `config.model`. Re-derived at execution time rather than trusted |
| T-196-SEMANTIC | Spoofing (a control that lies) | which "enabled" set the refusal reads | mitigate | Derived from `build_model_registry_rows`, the same union the picker reads. Using `enabled_model_allowed_set` would make the save path refuse 32 models the runtime runs — a *different* lie from the one being removed |
| T-196-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/test_196_save_refusal.py -q`
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q` — no new failures vs the recorded baseline
- Live re-derivation over `workflow_definitions` of: total phases, phases with no model, phases with a model, and phases whose model is absent from the union. State each as a reading with its command
- ⚠ NOT provable here: *"an unregistered model cannot be **SILENTLY** selected"* — coverage of the honest-consequence copy is a judgement. That is operator UAT in `196-VALIDATION.md`
</verification>

<success_criteria>
- A direct API call carrying a new unregistered model is refused 400 before any write, with an object-shaped detail carrying `code`
- Blank, disabled and already-stored-unregistered values all still save
- The refusal never fires before ownership resolution
- `workflows.py`'s diff is two call lines and an import; the publish gauntlet is untouched and its empty gap is recorded with a re-open trigger
- D-11 is re-confirmed by command at execution time, with the output recorded
</success_criteria>

<output>
Create `.planning/phases/196-registry-backed-model-picker-canvas/196-06-SUMMARY.md` when done.
Record verbatim: the Task 1 RED output, the `workflows.py` diffstat, the live re-derivation of the stored
model corpus with its command, the D-11 grep output, the publish-path gap statement with its re-open
trigger, and the backend baseline and post numbers.
</output>
