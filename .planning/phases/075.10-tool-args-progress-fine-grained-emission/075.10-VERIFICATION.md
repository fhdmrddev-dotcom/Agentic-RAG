---
phase: 075.10-tool-args-progress-fine-grained-emission
verified: 2026-05-25T03:30:00Z
status: passed
score: 7/7 code-verifiable + 6/7 human gates (Gate 3a deferred — OpenAI quota 429, same code path as Gate 3d PASS)
overrides_applied: 0
human_uat_results: |
  Gate 1 Apply migration ............. PASS (user)
  Gate 2 Regen full-schema .......... PASS (commit 1d9337f)
  Gate 3a OpenAI gpt-5.4 ............ DEFERRED (account quota 429; same code path as 3d)
  Gate 3b Anthropic claude-opus-4-6 .. PASS (13 emits, 257→3248 bytes)
  Gate 3c Google gemini-2.5-flash .... PASS (1 emit @1507 bytes, WR-02 atomic-args bar)
  Gate 3d OpenRouter llama-3.3-70b ... PASS (4 emits, 263→1024 bytes)
  Gate 3e 075.9-HUMAN-UAT items 2+3 .. PASS (flipped blocked → pass)
human_verification:
  - test: "Gate 1 — Apply migration 049 via Supabase SQL editor"
    expected: "After paste + run, `select chat_tool_args_progress_emit_boundary_bytes from app_settings limit 1;` returns 256. Backend boots without migration error."
    why_human: "CLAUDE.md migration rule — operator must paste `supabase/migrations/049_tool_args_progress_boundary.sql` into the live Supabase SQL editor; `supabase db push` / `db reset` are forbidden. Worktree executor cannot reach the operator's local Supabase container by name."
  - test: "Gate 2 — Regenerate `supabase/full-schema.sql` and commit"
    expected: "`bash scripts/regenerate-full-schema.sh` (no `--reset`) updates `supabase/full-schema.sql` to include the new column; `git diff` shows the new `chat_tool_args_progress_emit_boundary_bytes` line; commit lands."
    why_human: "Worktree's Supabase container is reachable by name but not by the script's project-ref grep; verified absent from full-schema.sql today (Grep on file produced 0 matches as of 2026-05-24)."
  - test: "Gate 3a — OpenAI gpt-5.4: SSE intercept for `execute_code 'for i in range(10): print(i)'`"
    expected: "≥ 3 `tool_args_progress` events between `tool_preparing` and `tool_start`. Editor inset shows Shiki-highlighted code visibly growing during preparing (not all-at-once at tool_start). Was 0 events pre-075.10."
    why_human: "Failure criterion #1 from PLAN.md — runtime/lived-experience assertion. Requires Chrome MCP + authenticated session + SSE intercept; cannot be verified by static code analysis."
  - test: "Gate 3b — Anthropic claude-opus-4-6: SSE intercept for the same prompt"
    expected: "≥ 3 `tool_args_progress` events between `tool_preparing` and `tool_start`. Visible character-by-character rendering. Was ≤ 1 emit pre-075.10."
    why_human: "Failure criterion #2 from PLAN.md — lived-experience G-4 mandatory."
  - test: "Gate 3c — Google gemini-2.5-flash: SSE intercept for the same prompt"
    expected: "≥ 1 `tool_args_progress` event AND visible editor-inset population before `tool_start`. ≥3 is NOT the bar for Google because the SDK delivers function_call args atomically per chunk (WR-02 — documented divergence in google_service.py:471-486)."
    why_human: "Failure criterion #2 (Google sub-axis) from PLAN.md — WR-02 caveat preserved in inline comment."
  - test: "Gate 3d — OpenRouter (any model): SSE intercept for the same prompt"
    expected: "≥ 3 `tool_args_progress` events between `tool_preparing` and `tool_start`. Visible character-by-character rendering. OpenRouter shares `_on_chunk_openai` with OpenAI native (per-provider boundary state separation preserved from Phase 075.6 Plan 01 Req #3)."
    why_human: "Failure criterion #1 (OpenRouter axis) from PLAN.md — Chrome MCP + free-tier OpenRouter session required."
  - test: "Gate 3e — 075.9 HUMAN-UAT items #2 and #3 re-run on all 4 providers"
    expected: "Items #2 (Live code stream) and #3 (Long code stream) flip from `blocked` → `pass` across all 4 providers. Final rendered code matches input byte-for-byte; no plain-text moment; no blink/flash at tool_start."
    why_human: "Parent UAT lived-experience gate — visual parity check against Claude.ai's gold-standard render cadence."
---

# Phase 075.10: Fine-Grained `tool_args_progress` Emission Verification Report

**Phase Goal:** Close the backend gap exposed in 075.9 HUMAN-UAT — `tool_args_progress` SSE events either don't fire (OpenAI/OpenRouter) or fire at a 5120-byte boundary too coarse for typical code (<5 KB on Anthropic/Google). After this phase, the frontend's already-correct streaming wiring receives chunks fine-grained enough for visible character-by-character code rendering during the args-generation window — matching Claude.ai's live-code experience.

**Verified:** 2026-05-24
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | New `app_settings.chat_tool_args_progress_emit_boundary_bytes` column exists in migration 049 with DEFAULT 256 | VERIFIED | `supabase/migrations/049_tool_args_progress_boundary.sql:17-18` — `ALTER TABLE app_settings ADD COLUMN IF NOT EXISTS chat_tool_args_progress_emit_boundary_bytes INTEGER NOT NULL DEFAULT 256;` plus descriptive COMMENT at L:20-23. |
| 2   | `tool_args_progress_emit_boundary_bytes()` defensive helper exists with non-positive clamp + 5120 fallback | VERIFIED | `backend/app/models/user_settings.py:413-444` — wraps `load_app_settings()` in `try/except Exception` (L:435), returns `_FALLBACK_TOOL_ARGS_EMIT_BOUNDARY_BYTES = 5120` (L:410) on any failure, AND clamps `value is None or value <= 0` to fallback (L:442-443). |
| 3   | `anthropic_service.py` no longer divides by hardcoded 5120 — uses helper-derived `_emit_boundary_bytes` | VERIFIED | `backend/app/services/anthropic_service.py:204-205` imports helper + captures once per stream; L:273 uses `_bytes_total // _emit_boundary_bytes` (was hardcoded `// 5120`); tail slice at L:211 + L:284 widens to `max(5120, _emit_boundary_bytes * 4)`. |
| 4   | `google_service.py` same change — config-backed boundary read | VERIFIED | `backend/app/services/google_service.py:375-377` same per-stream capture pattern; L:497 `_new_boundary = _bytes_total // _emit_boundary_bytes`; L:505 tail slice uses `_emit_tail_bytes`. WR-02 atomic-emit caveat documented inline at L:471-486. |
| 5   | `threads.py:_on_chunk_openai` (Rule 1 deviation per SUMMARY D-075.10-05) uses same config-backed boundary; OpenRouter inherits via shared callback | VERIFIED | `backend/app/api/threads.py:2121-2123` imports helper + captures `_emit_boundary_bytes` + `_emit_tail_bytes` at iteration scope (outside chunk closure, closure-captured); L:2249 `_new_boundary = _bytes_total // _emit_boundary_bytes`; L:2261 tail slice uses `_emit_tail_bytes`. Per-provider boundary dicts (`_emit_boundary_openai_native` / `_emit_boundary_openrouter`) preserved from Phase 075.6 Plan 01 Req #3 at L:2106-2107. |
| 6   | Tail slice widens proportionally to `max(5120, _emit_boundary_bytes * 4)` at every emit site | VERIFIED | anthropic_service.py:211 + threads.py:2123 + google_service.py:377 all set `_emit_tail_bytes = max(5120, _emit_boundary_bytes * 4)` at function/iteration entry; emit sites at L:284 (anthropic), L:505 (google), L:2261 (threads) all use `_emit_tail_bytes` in the slice. |
| 7   | No leftover hardcoded `// 5120` boundary divisions in production code | VERIFIED | Grep for `// 5120` across `backend/` produces ONE match — `backend/tests/integration/test_075_tool_args_progress.py:297` — and that match is a comment string inside a test fixture, NOT a production code path. |

**Score:** 7/7 code-verifiable truths VERIFIED

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `supabase/migrations/049_tool_args_progress_boundary.sql` | NEW migration: `ADD COLUMN IF NOT EXISTS chat_tool_args_progress_emit_boundary_bytes INTEGER NOT NULL DEFAULT 256` + COMMENT | VERIFIED | 23 lines, exists, content matches spec. Idempotent (`IF NOT EXISTS`). |
| `backend/app/models/user_settings.py` | UserEffectiveSettings field at default 256 + load_app_settings wiring + `tool_args_progress_emit_boundary_bytes()` defensive helper | VERIFIED | Field at L:142; loader at L:366-367 (`_int(override, …, 256)`); helper at L:413-444 with 5120 fallback + try/except + non-positive clamp. Behavioral spot-check: `python -c "from app.models.user_settings import tool_args_progress_emit_boundary_bytes; print(tool_args_progress_emit_boundary_bytes())"` returns `256` (helper-OK). |
| `backend/app/services/anthropic_service.py` | Per-stream `_emit_boundary_bytes` + tail slice widening at input_json_delta path | VERIFIED | L:204-211 + L:266-284 — correct shape. |
| `backend/app/services/google_service.py` | Same pattern at function_call args path; WR-02 caveat preserved | VERIFIED | L:375-378 + L:487-520 — correct shape; WR-02 documented at L:471-486. |
| `backend/app/api/threads.py` | Same pattern in `_on_chunk_openai` (Rule 1 deviation from PLAN — OpenAI emission code already lived here since 075.6) | VERIFIED | L:2106-2123 + L:2240-2273 — correct shape; per-provider boundary dict separation preserved. |
| `supabase/full-schema.sql` | Regenerated to include the new column | NOT YET REGENERATED | Grep for `chat_tool_args_progress_emit_boundary_bytes` against `supabase/full-schema.sql` returns 0 matches. **Gate 2 outstanding** — operator must run `bash scripts/regenerate-full-schema.sh` post-migration-paste. Documented in SUMMARY Human-UAT Gates §Gate 2. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `anthropic_service.stream_anthropic` | `tool_args_progress` SSE emit | per-stream helper call → `_emit_boundary_bytes` → `// _emit_boundary_bytes` per chunk | WIRED | Helper imported L:204, captured L:205, consumed at L:273 (boundary compute) + L:284 (tail slice). `yield` at L:286-302 ships both `args_so_far` (tail) and `code_so_far` (full cumulative). |
| `google_service.stream_google` | `tool_args_progress` SSE emit | same pattern, function_call branch | WIRED | Helper imported L:375, captured L:376, consumed at L:497 + L:505. `yield` at L:507-520 ships `code_so_far`. WR-02 (Google SDK atomic-emit) documented inline — semantic divergence is by-design. |
| `threads.py:_on_chunk_openai` | `tool_args_progress` SSE emit (covers BOTH OpenAI native AND OpenRouter) | helper call at iteration scope → closure capture → per-chunk boundary check | WIRED | Helper imported L:2121, captured L:2122 OUTSIDE the closure, consumed at L:2249 (boundary compute) + L:2261 (tail slice). `_emit` at L:2263-2273 with both `args_so_far` and `code_so_far`. Per-provider boundary state separation preserved (L:2235-2239 branches `_emit_boundary` based on `active_provider_name`). |
| `app_settings.chat_tool_args_progress_emit_boundary_bytes` | runtime helper return | `load_app_settings()` → Pydantic field → `tool_args_progress_emit_boundary_bytes()` | WIRED | Pydantic field at user_settings.py:142; loader at L:366-367 reads from override file with default 256. Live spot-check returns 256 (matches migration default). |
| migration 049 | `app_settings` live schema | Supabase SQL editor paste (operator) | NOT WIRED — Gate 1 outstanding | Operator action required per CLAUDE.md migration rule. Helper is defensive (returns 5120 fallback if column missing), so backend won't crash pre-migration — but the tunability surface is dead until Gate 1 runs. |
| `supabase/full-schema.sql` | greenfield deploy artifact | `bash scripts/regenerate-full-schema.sh` post Gate 1 (operator) | NOT WIRED — Gate 2 outstanding | Verified absent from current full-schema.sql via Grep (0 matches). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| anthropic_service `tool_args_progress` event | `_args_so_far`, `code_so_far`, `total_args_bytes_so_far` | `tb["arguments"]` (cumulative buffer mutated in `content_block_delta`/`input_json_delta` branch L:248-251) | Yes — live SDK delta stream | FLOWING |
| google_service `tool_args_progress` event | `_args_so_far`, `code_so_far` | `tool_blocks[idx]["arguments"]` populated from `fc.args` at L:441 | Yes — but atomic-emit per chunk on Google SDK (WR-02); boundary loop fires ≤ 1× per tool_call | FLOWING (atomic-cadence by Google SDK design) |
| threads.py `tool_args_progress` event | `args_so_far`, `code_so_far` | `tool_calls_buffer[idx]["arguments"]` accumulated from `tc.function.arguments` at L:2211 | Yes — live SDK delta stream (OpenAI native + OpenRouter) | FLOWING |
| `tool_args_progress_emit_boundary_bytes()` return | `value` | `load_app_settings().chat_tool_args_progress_emit_boundary_bytes` (Pydantic field, defaulted to 256 in code) | Yes — verified live: helper returns 256 pre-migration via Pydantic default | FLOWING |

All 4 data-flow paths produce real data. No HOLLOW_PROP / STATIC / DISCONNECTED findings.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Helper returns positive int by default | `./venv/Scripts/python.exe -c "from app.models.user_settings import tool_args_progress_emit_boundary_bytes; print(tool_args_progress_emit_boundary_bytes())"` | `256` | PASS |
| Backend modules compile cleanly post-edit | `./venv/Scripts/python.exe -c "import app.api.threads; import app.services.anthropic_service; import app.services.google_service; print('imports-OK')"` | `imports-OK` (with unrelated urllib3 RequestsDependencyWarning) | PASS |
| All 4 task commits exist | `git log --oneline -5 -- <phase files>` | `17bc67f` `bf99d29` `8be7cee` `da909d7` — 4/4 present | PASS |
| No leftover production hardcoded `// 5120` | Grep `// 5120` across `backend/` (excluding tests) | 1 match — `tests/integration/test_075_tool_args_progress.py:297` (test comment, NOT production) | PASS |
| migration 049 idempotency | `ADD COLUMN IF NOT EXISTS` present | confirmed at line 18 | PASS |
| SSE intercept ≥ 3 events per provider | Chrome MCP + 4-provider authenticated session | — | SKIP (Gate 3 — human verification) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| POLISH-TOOL-PROG-01 | 075.10-01 (Plan 01) frontmatter `requirements-completed` | "Tools other than `execute_code` emit `tool_args_progress` SSE events when their argument JSON exceeds 5 KB during streaming." | SATISFIED (code) — runtime confirmation pending Gate 3 | Code change directly enlarges the emission contract: boundary is now configurable (default 256 B << 5 KB) AND the `execute_code` name filter was already removed in Phase 075.6 + 075.4 + reaffirmed inline in anthropic_service.py:257-262, google_service.py:466-469, threads.py:2218-2223. Any tool — `execute_code`, `search_documents`, `analyze_document`, etc. — whose args exceed 256 B will now emit. REQUIREMENTS.md row reads `[x]` (already checked) and maps the ID to "Phase 075 — SEED-008 + tool_args_progress Polish Bundle" — that mapping pre-dates 075.10 but 075.10 is the phase that genuinely satisfies it. |

Orphaned requirements (REQUIREMENTS.md mapping → no plan claim): none. The PLAN frontmatter `requirements-completed: [POLISH-TOOL-PROG-01]` covers the single ID surfaced by SUMMARY.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `backend/app/api/threads.py` | 2122 | Event-loop thread blocking I/O on TTL miss (REVIEW WR-01) — `tool_args_progress_emit_boundary_bytes()` called without `run_in_threadpool` wrap inside the async `agent_runner` coroutine | Warning | Formal violation of CLAUDE.md D-v2.5-01. Magnitude is small (~50-100 µs JSON read on 5s TTL miss, file local). Documented in REVIEW.md WR-01; suggested fix is one-line wrap. Does NOT block Phase 075.10 goal achievement but should be reconciled in a follow-on hardening pass. |
| `backend/app/models/user_settings.py` | 430-431 | Docstring drift (REVIEW IN-01) — claims "clamps non-positive up to 1" but code returns 5120 fallback | Info | Documentation lies about the contract; runtime behavior is actually safer than the docstring promises. Pure docs fix. |
| `backend/app/services/anthropic_service.py:204`, `google_service.py:375`, `threads.py:2121` | — | Lazy-import comment cites "module-load-time cycle" that doesn't actually exist (REVIEW IN-02) | Info | Harmless — over-commented justification. The lazy import itself is correct; just the rationale is misstated. |
| `backend/app/services/anthropic_service.py:211`, `google_service.py:377`, `threads.py:2123` | — | `_emit_tail_bytes = max(5120, _emit_boundary_bytes * 4)` floor → for boundary ≤ 1280 the tail stays 5120 (REVIEW IN-03) | Info | Documented as intentional ("legacy 5120-byte tail still ships plenty of cumulative context per emit"); no telemetry verifies SSE bandwidth doesn't spike for very long tool-arg streams. NOT a bug — `code_so_far` is the canonical full-cumulative field; `args_so_far` floor is backward-compat + bandwidth safety belt. |

No Blocker (🛑) anti-patterns found. All Warning + Info findings come from `075.10-REVIEW.md` and are documented in the code review pipeline.

### Human Verification Required

7 items requiring human action — see YAML frontmatter for full structure:

1. **Gate 1** — Apply migration 049 via Supabase SQL editor (CLAUDE.md migration rule; verify `select chat_tool_args_progress_emit_boundary_bytes from app_settings limit 1;` returns 256)
2. **Gate 2** — Regenerate `supabase/full-schema.sql` post-Gate-1 + commit (Grep confirms column absent from full-schema.sql today)
3. **Gate 3a** — OpenAI gpt-5.4 Chrome MCP + SSE intercept ≥ 3 `tool_args_progress` events for ~1 KB code prompt
4. **Gate 3b** — Anthropic claude-opus-4-6 same test
5. **Gate 3c** — Google gemini-2.5-flash same test (WR-02 caveat: ≥ 1 event is the realistic floor due to atomic SDK emit)
6. **Gate 3d** — OpenRouter (any model) same test (inherits `_on_chunk_openai` from OpenAI native)
7. **Gate 3e** — 075.9-HUMAN-UAT.md items #2 and #3 re-run on all 4 providers; flip from `blocked` → `pass`; visual parity with Claude.ai's live-code render cadence

These gates correspond directly to PLAN.md G-6 failure criteria #1–#3, which are runtime/lived-experience assertions that **cannot** be verified by static code analysis.

### Gaps Summary

**No code gaps.** All 7 code-verifiable observable truths VERIFIED:

- Migration 049 content correct + idempotent
- Defensive helper present with correct fallback + clamp semantics
- All 3 streaming sites (Anthropic + Google + OpenAI/OpenRouter shared callback) use config-backed boundary + widened tail
- Per-provider boundary state separation preserved from 075.6
- No leftover hardcoded `// 5120` divisions in production code
- Helper returns positive int (live spot-check)
- Backend imports compile cleanly post-edit

**Three outstanding human-action gates** documented in SUMMARY.md and tracked in this report's `human_verification` frontmatter:

1. Gate 1 (apply migration via SQL editor) — required by CLAUDE.md migration rule; backend won't crash without it (helper is defensive) but operator tunability is dead until column exists
2. Gate 2 (regen `full-schema.sql`) — verified column NOT in full-schema.sql today; pure artifact regen, no code change
3. Gate 3 (Chrome MCP + SSE intercept on 4 providers + eyeball parity vs Claude.ai) — lived-experience G-4 gate per CLAUDE.md guardrail; cannot be verified statically

Phase status is `human_needed` (not `passed`, not `gaps_found`) — the code change is correct and complete by static analysis, but the failure-criteria assertions in PLAN.md G-6 are runtime/visual and require operator + Chrome MCP execution. Code-review (075.10-REVIEW.md) found 1 warning + 3 info, none blocking goal achievement.

---

_Verified: 2026-05-24T17:05:00Z_
_Verifier: Claude (gsd-verifier)_
