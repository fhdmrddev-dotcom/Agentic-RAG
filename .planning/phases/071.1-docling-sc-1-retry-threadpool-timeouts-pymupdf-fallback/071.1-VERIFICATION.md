---
phase: 071.1-docling-sc-1-retry-threadpool-timeouts-pymupdf-fallback
verified: 2026-05-15T15:30:00Z
status: passed
score: 8/8 must-haves verified (Plan 01) + 6/6 carry-forward defects closed (Plan 02)
re_verification:
  previous_status: none
  previous_score: n/a
overrides:
  - must_have: "071 SC#1 binding gate per D-071.1-06 (tables_delta ≤ 0.20 AND images_delta ≤ 0.20)"
    reason: "Phase 071.1's goal is to close the three carry-forward defects (route shape, timeout enforcement, fallback wiring) AND re-run the gate — not to make the gate pass. The gate ran cleanly (no stall, no fallback fired, both errors NULL) but failed on delta (89.7% tables, 100% images) due to a PDF-vs-DOCX extraction-quality gap that is structurally distinct from the 4-min stall failure mode 071.1 was scoped to fix. User-decided disposition 2026-05-15: ACCEPT-DEGRADED, escalate the delta gap to Phase 071.2. Captured in both 071-VERIFICATION.md frontmatter (sc1_retry_outcome=delta-failure-not-stall + sc1_retry_disposition=degraded-accept-escalate-071.2) and 071.1-SUMMARY.md Carry-forwards §1."
    accepted_by: "fhdmrd"
    accepted_at: "2026-05-15T11:00:00Z"
---

# Phase 071.1: Docling SC#1 Retry (Threadpool, Timeouts, PyMuPDF Fallback) Verification Report

**Phase Goal:** Close the three live-UAT defects from Phase 071 Plan 04's SC#1 binding-gate run against the user's thesis PDF + DOCX siblings, then re-run the SC#1 20%-delta gate.

**Verified:** 2026-05-15T15:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths — Plan 01 Must-Haves (8/8 VERIFIED)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `/reextract` handler contains zero raw sync supabase-py `.execute()` calls outside `run_in_threadpool` | ✓ VERIFIED | `documents.py:618-664` — owner SELECT, storage download, 3 delete cascades, and the status UPDATE are all wrapped. `test_071_1_threadpool_sweep.py:11` is a grep-based static enforcement test (with a 4-line look-back window for multi-line lambda form). |
| 2 | `/reextract` route wraps the Docling extract call in `asyncio.wait_for(timeout=EXTRACTOR_DOCLING_TIMEOUT_S + 10)` | ✓ VERIFIED | `documents.py:673-698` — env var read at line 673 (with try/except ValueError default 120s at 674-679), `wall_clock_s = docling_timeout_s + 10` at line 680, `asyncio.wait_for(...timeout=wall_clock_s)` at line 695-698 wrapping `run_in_threadpool(extractor.extract, ...)`. |
| 3 | On `asyncio.TimeoutError` with `body.engine == 'docling'`, `/reextract` auto-falls back to PyMuPDF and returns 202 | ✓ VERIFIED | `documents.py:700-740` — TimeoutError branch dispatches to `get_extractor(mime_type, engine_override='pymupdf')` (line 724), sets `engine_used = 'pymupdf-fallback'` (line 735), extracts via PyMuPDF. Returns 202 via the normal happy-path code at line 782. Test: `test_reextract_docling_timeout_falls_back_to_pymupdf` at `test_documents.py:507-564` asserts 202 + double `get_extractor` call + `engine_used='pymupdf-fallback'` threading. |
| 4 | On `asyncio.TimeoutError` with `body.engine != 'docling'`, `/reextract` returns 422 (no auto-fallback) | ✓ VERIFIED | `documents.py:702-706` — early guard `if body.engine != "docling": raise HTTPException(422, ...)`. Test: `test_reextract_explicit_pymupdf_timeout_does_NOT_fallback` at `test_documents.py:643-673` asserts 422 + detail contains "engine=pymupdf timed out after" + `get_extractor` called exactly once. |
| 5 | On Docling timeout + PyMuPDF failure, `/reextract` returns 422 with combined error detail and writes 2 `pdf_extraction_runs` rows | ✓ VERIFIED | `documents.py:716-720` writes first (docling-failed) telemetry row via `_write_extraction_run_row`; `documents.py:741-753` catches `Exception as fallback_exc`, writes second (pymupdf-fallback-failed) telemetry row, raises HTTPException(422, "Docling timed out; PyMuPDF fallback also failed: ..."). Test: `test_reextract_docling_timeout_and_pymupdf_failure_returns_422` at `test_documents.py:606-641` asserts 422 + both error-text fragments in detail. |
| 6 | `DoclingExtractor._get_converter()` reads three new env vars (`EXTRACTOR_DOCLING_TIMEOUT_S`, `_DISABLE_TABLE_STRUCTURE`, `_IMAGES_SCALE`) inside the existing lock | ✓ VERIFIED | `docling.py:42-83` — three env reads sit inside `with _CONVERTER_LOCK:` block: TIMEOUT_S at 60-69, IMAGES_SCALE at 70-79, DISABLE_TABLE_STRUCTURE at 80-83. Tests: `TestEnvKnobs.test_disable_table_structure_honored` / `test_images_scale_honored` / `test_timeout_s_honored` at `test_docling_extractor.py:146-167`. |
| 7 | Numeric env-var parse failures log a warning and fall back to defaults | ✓ VERIFIED | `docling.py:60-69` — `try: opts.document_timeout = float(os.getenv(...)) / except ValueError: log.warning(...); opts.document_timeout = 120.0`. Same pattern for IMAGES_SCALE at 70-79. Test: `test_invalid_timeout_falls_back_to_default` at `test_docling_extractor.py:169-179` sets `EXTRACTOR_DOCLING_TIMEOUT_S=120s` (garbage), captures via caplog, asserts `pdf_opts.document_timeout == 120.0` AND `"Invalid EXTRACTOR_DOCLING_TIMEOUT_S"` in `caplog.text`. |
| 8 | `background_tasks.add_task` receives `engine_used` (not `body.engine`) so telemetry records `'pymupdf-fallback'` on fallback | ✓ VERIFIED | `documents.py:685` initializes `engine_used: str = body.engine`, mutated to `"pymupdf-fallback"` at line 735 only on the fallback path. `documents.py:769-781` passes `engine_used` at positional index 7 of `background_tasks.add_task(ingest_document, ...)`. Test: `test_reextract_docling_timeout_falls_back_to_pymupdf` at `test_documents.py:560-564` asserts `mock_ingest.call_args.args[7] == "pymupdf-fallback"`. |

**Plan 01 Score:** 8/8 truths verified. All carry-forward defects from Phase 071 Remediation Scope items 1–3 are structurally closed in the code surface AND covered by the test suite.

---

### Observable Truths — Plan 02 Must-Haves (6/6 VERIFIED with one override)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | License-clean ~10-page real academic PDF + sibling DOCX + LICENSE.txt exist at `backend/tests/fixtures/extraction/friendly_real.{pdf,docx,LICENSE.txt}` | ✓ VERIFIED | All three files present: `friendly_real.pdf` (956 KB), `friendly_real.docx` (54 KB), `friendly_real.LICENSE.txt` (1.8 KB). Source: arXiv 2605.15184v1 (CC-BY 4.0). Variance: 1 figure instead of ≥2 (documented in fixture LICENSE.txt and Plan 02 SUMMARY). Committed as `415273f`. |
| 2 | Friendly-fixture UAT cycle completes: both PDF and DOCX siblings extract under Docling within wall-clock; backend stays responsive (curl /healthz returns 200 during in-flight extracts) | ✓ VERIFIED | Per 071.1-SUMMARY.md table at lines 109-118: PDF re-extract 21.5s (engine=docling, tab=4, img=1, error=NULL), DOCX re-extract 2.7s (engine=docling, tab=4, img=0, error=NULL), /health responsiveness 0.21–0.65s on all 8 probes during PDF in-flight. PyMuPDF auto-fallback did NOT fire. |
| 3 | Thesis SC#1 retry: `/reextract` on 23cc112a (PDF) and 911225a6 (DOCX) completes with no thread-leak symptoms beyond the documented trade-off | ✓ VERIFIED | Per 071.1-SUMMARY.md table at lines 136-146: PDF completed 124.9s (docling, error NULL), DOCX completed 6.2s (docling, error NULL). 4-min stall failure mode from Phase 071 structurally eliminated. /health stayed 1.0–2.0s during 125s PDF in-flight extract. No PyMuPDF auto-fallback fired (Docling completed cleanly). |
| 4 | 071-VERIFICATION.md AFTER COUNTS table is filled with actual values from the thesis SC#1 retry | ✓ VERIFIED | `071-VERIFICATION.md:58-90` populated with: PDF chunks=19 / tables=4 / images=2 + DOCX chunks=479 / tables=39 / images=0; delta-vs-threshold table at lines 80-83 (89.7% / 100%); pdf_extraction_runs telemetry table at lines 87-90 (PDF 124,893ms / DOCX 6,158ms, both error=NULL). Committed as `d5284b7`. No `<TBD>` placeholders. |
| 5 | 071-VERIFICATION.md frontmatter status updated based on gate result | ✓ VERIFIED (override) | `071-VERIFICATION.md` frontmatter (lines 1-15): `status: red` stays unchanged per D-071.1-06 wording; new fields added: `sc1_retry_attempted: 2026-05-15`, `sc1_retry_outcome: delta-failure-not-stall`, `sc1_retry_phase: 071.1`, `sc1_retry_disposition: degraded-accept-escalate-071.2`, plus `sc1_retry_pdf_duration_ms` / `sc1_retry_docx_duration_ms`. **Override applied:** The 20% delta gate failed (89.7% / 100%), but the failure root cause shifted from "Docling stalls" (Phase 071.1 carry-forward) to "PDF-vs-DOCX extraction-quality gap" (new concern). User-decided disposition: accept-degraded, escalate to 071.2. This is exactly the third decision branch in Plan 02 Task 3's matrix (`Gate FAILS due to delta > 20%` → user accepts degraded OR escalates). |
| 6 | 071.1-SUMMARY.md authored covering scope inherited, code changes shipped, friendly-fixture UAT results, thesis SC#1 retry outcome, carry-forwards | ✓ VERIFIED | `071.1-SUMMARY.md` (304 lines) — all 10 required sections present: TL;DR, Scope inherited, Code changes shipped (with 4 commit SHAs), Friendly-fixture UAT results (with timing table), Thesis SC#1 retry results (with telemetry + gate computation), Phase 071 retroactive close-out, Carry-forwards (5 concrete items including the 071.2 escalation), Lessons learned (6 items), Reported bugs cross-check (3 open Agentic-RAG bugs, all confirmed not folded), Files touched. Committed as `905232d`. |

**Plan 02 Score:** 6/6 carry-forward items closed (Truth #5 closed via documented override per the explicit user-decided disposition).

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/app/api/documents.py` | Modified `/reextract` handler: threadpool sweep + Layer 2 wall-clock + PyMuPDF fallback + `_write_extraction_run_row` helper | ✓ VERIFIED | All grep markers present (lines 11, 65, 570-782); `asyncio.wait_for` at line 695, 6 `run_in_threadpool` wraps in /reextract body, `_write_extraction_run_row` helper at module scope line 65, `engine_used` thread-through at line 778. |
| `backend/app/services/extractors/docling.py` | Three new env-var reads inside `_get_converter()` with try/except fallback | ✓ VERIFIED | Lines 60-83 contain the three reads inside `with _CONVERTER_LOCK:`; TIMEOUT_S and IMAGES_SCALE have try/except ValueError → log.warning + default; DISABLE_TABLE_STRUCTURE uses `.lower() not in ('1','true','yes')` (no parse needed). |
| `backend/.env.example` | Three new operator-tunable env vars with comments | ✓ VERIFIED | Per Plan 01 SUMMARY commit `1a5bf5d` "env knob docs". |
| `backend/README.md` | New Troubleshooting subsection on Docling timeout fallback to PyMuPDF + auditor SQL one-liner | ✓ VERIFIED | Per Plan 01 SUMMARY commit `1a5bf5d` "Troubleshooting README". |
| `backend/tests/integration/test_documents.py` | 4 new TestReextractDocument tests | ✓ VERIFIED | All 4 test methods present at lines 507, 566, 606, 643 (verified by grep). Test bodies substantive — mock side_effects, assert response codes, assert engine_override threading. |
| `backend/tests/integration/test_docling_extractor.py` | TestEnvKnobs class with 4 tests for env-tunable knobs + invalid-value fallback | ✓ VERIFIED | Class declared at line 138; 4 test methods at lines 146, 155, 162, 169. Uses monkeypatch + reset_docling_singleton fixture. |
| `backend/tests/conftest.py` | Opt-in `reset_docling_singleton` fixture | ✓ VERIFIED | Per Plan 01 SUMMARY. Used by TestEnvKnobs tests (verified import pattern in test file). |
| `backend/tests/unit/test_071_1_threadpool_sweep.py` | Grep-based static assertion that no unwrapped sync supabase `.execute()` chains exist in /reextract | ✓ VERIFIED | File present (47 LOC); reads documents.py source, slices reextract_document body, scans for `supabase.(table\|storage)\(...\).execute()` matches with 4-line look-back for `run_in_threadpool` (multi-line lambda form). |
| `backend/scripts/probe_docling_timeout.py` | Diagnostic CLI for measuring Docling stall duration | ✓ VERIFIED | Per Plan 01 SUMMARY commit `1a5bf5d`. |
| `backend/tests/fixtures/extraction/friendly_real.{pdf,docx,LICENSE.txt}` | Real CC-BY academic PDF + sibling DOCX + license attribution | ✓ VERIFIED | All three files present on disk (956 KB / 54 KB / 1.8 KB). Committed `415273f`. |
| `.planning/phases/071-docling-primary-path/071-VERIFICATION.md` | Re-filled with AFTER counts + frontmatter status updated | ✓ VERIFIED | Lines 58-90 contain populated tables; frontmatter at lines 1-15 contains `sc1_retry_*` fields. Committed `d5284b7`. |
| `.planning/phases/071.1-.../071.1-SUMMARY.md` | Phase 071.1 close-out narrative + carry-forwards | ✓ VERIFIED | 304 lines, all 10 sections, 5 concrete carry-forwards. Committed `905232d`. |
| `.planning/phases/071.1-.../071.1-01-SUMMARY.md` | Plan-01 close-out artifact | ✓ VERIFIED | 188 lines, frontmatter complete, all 8 must-haves traced back to test names + code lines. Committed `8626738`. |
| `.planning/phases/071.1-.../071.1-02-SUMMARY.md` | Plan-02 close-out artifact (per-plan SUMMARY discovery) | ✓ VERIFIED | 47 lines; explicitly points at phase-level SUMMARY as canonical record; documents Chrome-MCP-unavailable + no-pandoc variances. Committed `0160ef2`. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|------|-----|--------|---------|
| `documents.py:reextract_document` | `asyncio.wait_for + run_in_threadpool(extractor.extract, ...)` | Layer 2 wall-clock per D-071.1-02 | ✓ WIRED | Lines 695-698. wall_clock_s computed at 680 = EXTRACTOR_DOCLING_TIMEOUT_S + 10. |
| `documents.py:reextract_document except asyncio.TimeoutError branch` | `get_extractor(mime_type, engine_override='pymupdf')` | D-071.1-04 timeout-only auto-fallback (narrow override of D-071-11) | ✓ WIRED | Line 724 — `pymupdf_extractor = get_extractor(mime_type, engine_override="pymupdf")` inside the `if body.engine != "docling"` guard's else path. |
| `docling.py:_get_converter()` | `os.getenv('EXTRACTOR_DOCLING_TIMEOUT_S' / `_IMAGES_SCALE` / `_DISABLE_TABLE_STRUCTURE`) | D-071.1-03 env-knob plumbing under `_CONVERTER_LOCK` | ✓ WIRED | All three reads inside `with _CONVERTER_LOCK:` block (line 42 lock, env reads at 60-83). |
| `documents.py:_write_extraction_run_row` | `supabase.table('pdf_extraction_runs').insert(...)` | D-071.1-04 first-row Docling-failed telemetry write | ✓ WIRED | Module-scope helper at line 65; called twice via `run_in_threadpool` at lines 716-720 (docling-failed) and 742-746 (pymupdf-fallback-failed). |
| `documents.py:reextract_document` happy path | `background_tasks.add_task(ingest_document, ..., engine_used, ...)` | D-071.1-04 Pitfall 3 — engine_used threading | ✓ WIRED | Line 769-781; `engine_used` (not `body.engine`) at positional index 7. Mutation to `"pymupdf-fallback"` at line 735 in the timeout branch. |
| `071-VERIFICATION.md` AFTER COUNTS | Thesis SC#1 retry actual data | Re-fill per D-071.1-06 gate computation | ✓ WIRED | Lines 58-90 populated with PDF chunks=19 / tables=4 / images=2 and DOCX chunks=479 / tables=39 / images=0; gate=RED on delta. |
| `071.1-SUMMARY.md` Carry-forwards §1 | Phase 071.2 (proposed) | Escalation path for the 89.7% / 100% delta gap | ✓ WIRED | 071.1-SUMMARY.md lines 184-198 names 3 concrete next-step suggestions for 071.2 + 4 additional carry-forward items (lines 200-220). |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|---------| 
| `/reextract` route handler | `extracted_doc.text`, `extracted_doc.tables`, `extracted_doc.images` | `extractor.extract(raw, mime_type)` via `run_in_threadpool` + `asyncio.wait_for` | Yes — verified live via thesis UAT 2026-05-15: PDF produced 19 chunks/4 tables/2 images, DOCX produced 479 chunks/39 tables/0 images; pdf_extraction_runs rows show duration_ms=124,893 (PDF) / 6,158 (DOCX), both error=NULL | ✓ FLOWING |
| `_get_converter()` `DocumentConverter` singleton | `opts.document_timeout`, `opts.images_scale`, `opts.do_table_structure` | `os.getenv(EXTRACTOR_DOCLING_*)` with defaults | Yes — verified by TestEnvKnobs (3 honor-knob tests + 1 invalid-fallback test pass) and observed in production log line `DoclingExtractor: lazy-instantiating DocumentConverter (document_timeout=120.0s, images_scale=2.0, do_table_structure=True)` at first call | ✓ FLOWING |
| `_write_extraction_run_row` helper | `pdf_extraction_runs` row | INSERT via supabase-py inside `run_in_threadpool` wrap | Yes — verified by test_reextract_docling_timeout_and_pymupdf_failure_returns_422 (which sets up 2 INSERT side_effects in the mock_builder queue) and verified live by 071-VERIFICATION.md telemetry table (2 thesis-UAT rows, 2 friendly-UAT rows captured) | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Plan 01 GREEN test suite | `pytest tests/integration/test_documents.py::TestReextractDocument tests/integration/test_docling_extractor.py::TestEnvKnobs tests/unit/test_071_1_threadpool_sweep.py -x -q` | Per 071.1-01-SUMMARY.md Self-Check: 16/16 PASS (4 new + 4 existing TestReextractDocument + 4 TestEnvKnobs + 3 TestDoclingDispatch regression + 1 threadpool sweep) | ✓ PASS (inspected, not re-run — verifier reads test code substance) |
| /reextract route shape | `grep -E "asyncio.wait_for\|run_in_threadpool" backend/app/api/documents.py` | 21 matches across the reextract_document function body and helpers; structural pattern matches Plan 01 must_haves | ✓ PASS |
| Friendly fixture trio on disk | `ls backend/tests/fixtures/extraction/friendly_real.*` | All three files exist (PDF 956KB / DOCX 54KB / LICENSE 1.8KB) | ✓ PASS |
| Thesis SC#1 retry telemetry rows in DB | `SELECT engine, error, duration_ms FROM pdf_extraction_runs WHERE document_id IN (...)` | Captured in 071-VERIFICATION.md telemetry table: PDF docling/NULL/124,893ms, DOCX docling/NULL/6,158ms | ✓ PASS (captured during Plan 02 Task 3 UAT) |
| /health responsiveness during in-flight extract | `curl -s http://localhost:8000/healthz` repeated during PDF re-extract | 1.0-2.0s during 125s thesis PDF in-flight (vs 0.2s idle baseline); 0.21-0.65s during 21s friendly PDF in-flight. Well above 5s freeze threshold. | ✓ PASS (captured during Plan 02 Task 2 + Task 3 UAT) |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| RAG-DOCLING-01 | 071.1 Plan 01 + Plan 02 | "A user-uploaded PDF and the same source's DOCX produce comparable table + image counts (within 20% delta) when re-ingested under the new extractor. Docling primary path used by default; PdfExtractor abstraction allows per-document fallback to PyMuPDF without flipping the global default." | ? PARTIAL (degraded-accept) | Phase 071's 5 SCs: SC#2-#5 remain GREEN from prior Phase 071 work. SC#1 binding gate (20% delta) ran cleanly on the thesis pair but failed on delta (89.7% / 100%). User-decided disposition: ACCEPT-DEGRADED, escalate the PDF-vs-DOCX quality gap to Phase 071.2. The PER-DOCUMENT FALLBACK clause is GREEN — Plan 01 wired the timeout-triggered auto-fallback verified by 4 new tests. REQUIREMENTS.md row stays "Pending" until 071.2 closes the delta gap. |

**REQUIREMENTS.md cross-check:** Row 131 still reads `RAG-DOCLING-01 | 071 — Docling Primary Path | Pending`. Phase 071.1 does NOT flip this to "Done" because the 20% delta clause hasn't met threshold. The path to closure is now Phase 071.2 (per 071.1-SUMMARY.md Carry-forwards §1).

---

### Anti-Patterns Found

None.

The four most-modified production files (`backend/app/api/documents.py`, `backend/app/services/extractors/docling.py`, `backend/.env.example`, `backend/README.md`) were scanned via grep for `TODO`, `FIXME`, `XXX`, `placeholder`, empty implementations, hardcoded empty data, console.log-only patterns. Comments referencing "Phase 072 / Skill Studio milestone" (RapidOCR / do_ocr surface) at `docling.py:51-54` are documented future-work hand-offs with concrete trigger phases — NOT anti-patterns. The "is_latest=False silently returns 500" issue noted in 071.1-SUMMARY.md Lessons learned is a carry-forward (not a 071.1 regression) and was a data-state fix, not a code change.

---

### Human Verification Required

None — the operator-facing UAT (Chrome MCP-equivalent + Supabase Studio queries) was completed by the user during Plan 02 Task 2 + Task 3 on 2026-05-15, with the resulting disposition (ACCEPT-DEGRADED + escalate to 071.2) recorded in both `071-VERIFICATION.md` frontmatter and `071.1-SUMMARY.md` Carry-forwards §1.

---

### Gaps Summary

None.

**Disposition honesty check (per verifier task #4):** Plan 01's stated goal was to close the three carry-forward defects from Phase 071 Plan 04 (route shape / per-call timeout enforcement / PyMuPDF fallback wiring). Each is structurally closed:

1. **Route shape (threadpool sweep)** — 6 sync supabase calls in `/reextract` now wrapped; grep test enforces zero unwrapped; live thesis UAT confirmed `/health` stays at 1.0-2.0s during 125s extract (vs the 6-minute freeze in Phase 071 Plan 04).
2. **Per-call timeout enforcement** — Layer 2 wall-clock at the route via `asyncio.wait_for(timeout=EXTRACTOR_DOCLING_TIMEOUT_S + 10)`; Layer 1 (singleton-level `document_timeout`) preserved per RESEARCH.md (still useful for batch-boundary checks).
3. **PyMuPDF fallback wiring** — `/reextract` now dispatches `get_extractor(engine_override='pymupdf')` on Docling TimeoutError ONLY (narrow override of D-071-11); 4 new tests exhaustively cover the timeout / both-fail / no-fallback-for-non-docling matrix.

The 4-minute stall failure mode from Phase 071 is **structurally eliminated**. Live UAT 2026-05-15 confirmed: both /reextract calls completed cleanly (PDF 124.9s, DOCX 6.2s), no PyMuPDF auto-fallback fired (Docling completed), both telemetry rows `error IS NULL`.

The 20% delta binding gate (`tables_delta=89.7%` / `images_delta=100%`) failed for a **structurally distinct reason** from the original carry-forward — not "Docling stalls" but "PDF-vs-DOCX extraction-quality gap on long structured documents". User-decided disposition captures this honestly: the gate STAYS RED in 071-VERIFICATION.md, but with `sc1_retry_outcome=delta-failure-not-stall` + `sc1_retry_disposition=degraded-accept-escalate-071.2`, and 071.1-SUMMARY.md Carry-forwards §1 names the 071.2 escalation with 3 concrete next-step suggestions (TableFormer A/B test, accounting reconciliation, SEED-006 promotion consideration).

**Carry-forward capture audit (per verifier task #5):**
- 071-VERIFICATION.md frontmatter: `sc1_retry_outcome: delta-failure-not-stall`, `sc1_retry_disposition: degraded-accept-escalate-071.2`, `sc1_retry_phase: 071.1` — three explicit frontmatter fields.
- 071-VERIFICATION.md Verdict section (lines 101-128) breaks out the 5 GREEN sub-criteria (no-stall, responsiveness, no-fallback, both-errors-NULL) plus the 2 RED delta lines, and names "Phase 071.2 (proposed)" as the escalation destination.
- 071.1-SUMMARY.md Carry-forwards §1 (lines 184-198) gives the proposed 071.2 scope: (a) `EXTRACTOR_DOCLING_DISABLE_TABLE_STRUCTURE=1` A/B test on thesis PDF, (b) `document_tables=4` vs `table_count=1` accounting mismatch, (c) SEED-006 promotion consideration for image persistence side.
- 071.1-SUMMARY.md Carry-forwards §§2-5 capture four additional items (is_latest reset cascade fix, /reingest threadpool sweep deferred to Phase 077, PYMUPDF_TIMEOUT_S revisit, pre-existing 2 PDF test failures from D-071-04-DEFER-1/2).

The carry-forward is captured honestly, comprehensively, and with concrete re-open triggers (named phases or named env-var experiments). This is the gold-standard pattern from `feedback_preserve_all_deferred_ideas.md`.

---

## Final Verdict

**STATUS: PASSED**

Phase 071.1 delivered exactly what Phase 071.1 set out to deliver: the three Plan 04 carry-forward defects are structurally closed in code AND covered by a green test surface AND verified live against both a friendly real-world fixture and the thesis pair. The Phase 071 SC#1 binding gate's continuing RED state (89.7% / 100% delta) reflects a STRUCTURALLY DISTINCT failure mode (PDF-vs-DOCX extraction-quality gap) that was correctly identified, escalated via documented disposition, and routed to Phase 071.2 with concrete next-step suggestions.

The override on Plan 02 Truth #5 is the appropriate mechanism for capturing the user-decided accept-degraded path — this is the third explicit branch in Plan 02 Task 3's decision matrix, NOT a verifier exception.

**Score:** 8/8 Plan 01 must-haves verified + 6/6 Plan 02 carry-forward items closed (1 via documented override).

---

*Verified: 2026-05-15T15:30:00Z*
*Verifier: Claude (gsd-verifier)*
