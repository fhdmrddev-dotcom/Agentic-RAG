---
phase: 092.5-provider-gateway-extraction
plan: 06
type: execute
wave: 5
depends_on: ["092.5-02", "092.5-05"]
files_modified:
  - scripts/.sse_after/openai.json
  - scripts/.sse_after/deepseek.json
  - scripts/.sse_after/moonshot.json
  - scripts/.sse_after/zhipu.json
  - scripts/.sse_after/minimax.json
autonomous: false
requirements: [GATEWAY-01]

must_haves:
  truths:
    - "After the FULL extraction (all 3 adapters, incl. the entangled OpenAI-compat), Deep is byte-identical on the native-7: the structural-skeleton SSE diff is empty for EVERY native-7 provider."
    - "All 8 D-07 per-provider round-trip invariants (I1-I8) are observed and NAMED with evidence in the phase verification."
    - "The eval scoreboard + E2E backstop are GREEN and non-regressing vs the BEFORE reference."
  artifacts:
    - path: "scripts/.sse_after/openai.json"
      provides: "Post-full-extraction SSE capture for the final native-7 AFTER diff"
  key_links:
    - from: "scripts/capture_sse_baseline.py --mode after"
      to: "scripts/.sse_baseline/<provider>.json"
      via: "diff_skeletons == [] for ALL native-7 (incl. openai/deepseek/moonshot/zhipu/minimax)"
      pattern: "SSE_DIFF_RESULT: PASS"
---

<objective>
FINAL PROOF GATE (operator-run): after the entangled OpenAI-compat adapter lands (Plan 05), prove Deep is byte-identical on the FULL native-7 via the 089 method — the OpenAI-path providers (openai/deepseek/moonshot/zhipu/minimax) are now the high-risk axis. This is THE red-line gate (D-06): a persistent native-7 skeleton diff BLOCKS the phase. This plan also collects + NAMES the 8 D-07 round-trip invariants (I1-I8) with their evidence surfaces in the phase verification — the explicit checklist CONTEXT.md D-07 requires.

Purpose: Close the phase. Prove the hard adapter byte-identical; name every invariant; confirm the seam is ready for 093 to consume.
Output: `SSE_DIFF_RESULT: PASS` (full native-7) + I1-I8 named-and-evidenced + non-regressing eval + E2E GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-VALIDATION.md
@scripts/SSE_DIFF_RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator proves Deep byte-identical after the FULL extraction (full native-7 SSE-diff + eval + E2E) and confirms I1-I8</name>
  <read_first>
    - scripts/SSE_DIFF_RUNBOOK.md
    - scripts/capture_sse_baseline.py:180-194 (SSE_DIFF_RESULT: PASS/BLOCK strings)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md §"The exact 089 proof procedure" (lines 401-411) + §"D-07 Per-Provider Round-Trip Invariants" (lines 382-394, the I1-I8 evidence-surface table)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-VALIDATION.md §"D-07 Per-Provider Round-Trip Invariants" (the I1-I8 how-to-observe table)
  </read_first>
  <what-built>
    Plan 05 extracted the entangled OpenAI-compat branch into `provider_gateway/openai_compat.py` (the `<think>` machine, reasoning, `_accumulate_chunk_usage`, per-provider 5KB boundary dicts) and folded the third `_on_chunk` into the shared consumer handler, keeping `parse_structured_tool_calls` + injection + retry consumer-side. The orchestrator already proved the seam test all-green + full suite zero net-new failures. This checkpoint is the LIVE byte-identical proof for the OpenAI-path providers (the high-risk axis) + the D-07 invariant naming.
  </what-built>
  <how-to-verify>
    1. Operator: RESTART uvicorn (load the full new module graph) in a VISIBLE terminal (never background). Local Supabase + Redis up; native-7 keys in `backend/.env`.
    2. Operator: run the FINAL AFTER diff against the Plan-02 baselines:
       `backend/venv/Scripts/python.exe scripts/capture_sse_baseline.py --mode after`
       Expected: every native-7 provider `PASS — skeleton identical`; final `SSE_DIFF_RESULT: PASS`. PAY SPECIAL ATTENTION to the OpenAI-path providers (openai, deepseek, moonshot, zhipu, minimax) — they are the just-extracted high-risk axis. If any shows `BLOCK — N structural skeleton edits`, re-run 2-3× (LLM tool-path non-determinism); a PERSISTENT diff is a BLOCKER → the OpenAI cut was wrong (fix before phase close).
    3. Operator: eval backstop — `scripts/eval_cross_provider.py` → `EVAL_SUMMARY` equal-or-non-regressing vs the Plan-02 BEFORE reference.
    4. Operator: E2E — `frontend/tests/e2e/scenario-*.spec.ts` GREEN.
    5. Operator + orchestrator: confirm + NAME each D-07 invariant with evidence (the table below). The skeleton gate covers I1/I2/I3/I4/I6 structurally; I5/I7/I8 via backend log + Supabase + the I8 unit test:
       - I1 Anthropic end_turn tool exec → anthropic.json skeleton shows dispatch + Supabase messages.tool_calls populated.
       - I2 Google thought_signature echo → google.json + 2+-round Google run does NOT 400 + messages.tool_calls[].thought_signature present.
       - I3 DeepSeek reasoning_content round-trip → deepseek.json has reasoning_delta + no "reasoning_content must be passed back" 400.
       - I4 Moonshot/Kimi <think>-strip → moonshot/minimax/zhipu.json reasoning_delta, no <think> leak into delta.
       - I5 empty-retry → backend log "retrying once".
       - I6 force_no_tools-on-last-iteration + cap-pause → cap_paused event + Supabase carrier row.
       - I7 iteration cap (explorer=8 / general=15) → backend log iteration count terminates at cap.
       - I8 usage accounting (Google cumulative vs OpenAI +=) → runs row token columns + the green unit test.
  </how-to-verify>
  <acceptance_criteria>
    - `capture_sse_baseline.py --mode after` prints `SSE_DIFF_RESULT: PASS` with ALL native-7 (incl. openai/deepseek/moonshot/zhipu/minimax) showing `skeleton identical`.
    - `eval_cross_provider.py` `EVAL_SUMMARY` N/M >= the Plan-02 BEFORE reference (no regression).
    - Playwright `scenario-*.spec.ts` GREEN.
    - All 8 D-07 invariants (I1-I8) confirmed with the named evidence surface above (this is the D-07 "named in VERIFICATION" requirement).
    - OpenRouter best-effort (a diff there does NOT block).
    - Any persistent native-7 skeleton diff → checkpoint returns gaps_found (the OpenAI cut needs a fix; do NOT close the phase).
  </acceptance_criteria>
  <resume-signal>Type "full native-7 byte-identical PASS, I1-I8 confirmed" (with EVAL_SUMMARY N/M), or paste the BLOCK output + which provider/invariant diverged.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture/eval scripts → live provider APIs | Existing 089 harness; operator keys; localhost-only gate. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092.5-10 | Information Disclosure | capture_sse_baseline.py / eval_cross_provider.py | accept | Scripts print key PRESENCE only; `normalize` masks ids; localhost hard-gate; AFTER captures hold masked skeletons, no secrets. Unchanged 089 controls. |

**Statement (per security_gate):** NO HIGH threats. Verification-only plan; existing proof harness; no code/control change. This plan does NOT perform the v2.8 closure SECURITY PASS (that anchors on Phase 092 per ROADMAP) — it confirms the refactor introduced no behavioral change, which is itself the security guarantee (every control byte-identical).
</threat_model>

<verification>
- `SSE_DIFF_RESULT: PASS` for the FULL native-7 (incl. the OpenAI-path high-risk axis) after the complete extraction.
- All 8 D-07 invariants I1-I8 named + evidenced (the D-07 "named in VERIFICATION" requirement).
- `EVAL_SUMMARY` non-regressing; E2E GREEN.
</verification>

<success_criteria>
- Deep is proven byte-identical on the FULL native-7 after the complete provider-gateway extraction (D-06 red line met).
- All 8 per-provider round-trip invariants preserved AND named (D-07 met).
- The gateway (3 adapters + dispatcher + canonical events + surfaced calling_mode) is a single home consumable by 093's harness sub-agent loop without re-deriving provider logic (SC#3) — no task_service rewire here (D-05).
- GATEWAY-01 complete: all 3 ROADMAP success criteria satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-06-SUMMARY.md` (record the per-provider PASS, the I1-I8 evidence, and the EVAL_SUMMARY N/M — this SUMMARY is the D-07 named-invariants record for the phase).
</output>
