---
phase: 092.5-provider-gateway-extraction
plan: 04
type: execute
wave: 3
depends_on: ["092.5-02", "092.5-03"]
files_modified:
  - scripts/.sse_after/anthropic.json
  - scripts/.sse_after/google.json
autonomous: false
requirements: [GATEWAY-01]

must_haves:
  truths:
    - "After the clean-providers extraction (Plan 03), Deep is byte-identical on native-7: the structural-skeleton SSE diff is empty for every native-7 provider (especially anthropic + google, the two just extracted)."
    - "The cross-provider eval scoreboard is unchanged or strictly non-regressing vs the Plan-02 BEFORE reference."
  artifacts:
    - path: "scripts/.sse_after/anthropic.json"
      provides: "Post-clean-providers-extraction SSE capture for the AFTER diff"
  key_links:
    - from: "scripts/capture_sse_baseline.py --mode after"
      to: "scripts/.sse_baseline/<provider>.json"
      via: "diff_skeletons (structural skeleton gate) == [] for native-7"
      pattern: "SSE_DIFF_RESULT: PASS"
---

<objective>
PROOF GATE #1 (operator-run): after extracting the two CLEAN providers (Anthropic + Google) into the gateway (Plan 03), prove Deep is byte-identical on the native-7 via the 089 method — `capture_sse_baseline.py --mode after` against the Plan-02 BEFORE baselines, the eval scoreboard, and the E2E backstop. A persistent non-empty structural-skeleton diff on ANY native-7 provider BLOCKS the phase (a verbatim move is AST-equivalent → any persistent skeleton edit means it was NOT byte-identical). This isolates the clean-providers cut as its own provable step (D-04) BEFORE the entangled OpenAI adapter lands.

Purpose: Gate the easy half before touching the hard unit. If anthropic.json/google.json diff, the clean-providers cut was wrong — fix it before Wave 4, not after both are tangled together.
Output: `SSE_DIFF_RESULT: PASS` (native-7) + non-regressing `EVAL_SUMMARY` + E2E GREEN — recorded in the summary.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-VALIDATION.md
@scripts/SSE_DIFF_RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator proves Deep byte-identical after clean-providers extraction (native-7 SSE-diff + eval + E2E)</name>
  <read_first>
    - scripts/SSE_DIFF_RUNBOOK.md (the before/after procedure)
    - scripts/capture_sse_baseline.py:88-100 + :180-194 (the --mode after diff + the SSE_DIFF_RESULT: PASS/BLOCK output strings)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md §"The exact 089 proof procedure" steps 3-4 (lines 406-409) + §"Don't Hand-Roll" (diff_skeletons is the only valid gate, raw diff false-positives)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-VALIDATION.md §"Manual-Only Verifications"
  </read_first>
  <what-built>
    Plan 03 extracted the Anthropic + Google branches into `provider_gateway/anthropic.py` + `google.py` and pointed `agent_loop` at `gateway.open_stream(...)` for those two providers, collapsing their two `_on_chunk` callbacks into one shared consumer handler. The orchestrator already proved the seam test GREEN + full suite zero net-new failures. This checkpoint is the LIVE byte-identical proof the orchestrator cannot run (needs native-7 keys + operator-started uvicorn).
  </what-built>
  <how-to-verify>
    1. Operator: RESTART uvicorn (load the new module graph — the gateway package + the rewired agent_loop) in a VISIBLE terminal (never background). Local Supabase + Redis up; native-7 keys in `backend/.env`.
    2. Operator: run the AFTER diff against the Plan-02 baselines:
       `backend/venv/Scripts/python.exe scripts/capture_sse_baseline.py --mode after`
       Expected per-provider: `PASS — skeleton identical` for each native-7 provider; final line `SSE_DIFF_RESULT: PASS — all native-7 diffs empty`.
       If any provider shows `BLOCK — N structural skeleton edits`, re-run 2-3× to rule out LLM tool-path non-determinism. A PERSISTENT skeleton diff on anthropic/google (the two extracted) or any native-7 provider is a BLOCKER → report it; the clean-providers cut needs a fix (do NOT proceed to Wave 4).
    3. Operator: eval backstop — `backend/venv/Scripts/python.exe scripts/eval_cross_provider.py` → `EVAL_SUMMARY` must equal or strictly non-regress vs the Plan-02 BEFORE reference.
    4. Operator (or orchestrator if a headless run is wired): E2E backstop — `frontend/tests/e2e/scenario-*.spec.ts` GREEN.
  </how-to-verify>
  <acceptance_criteria>
    - `capture_sse_baseline.py --mode after` prints `SSE_DIFF_RESULT: PASS` with anthropic + google showing `skeleton identical` (and all other native-7 providers, which were not extracted, trivially identical).
    - `eval_cross_provider.py` `EVAL_SUMMARY` N/M >= the Plan-02 BEFORE reference (no regression).
    - Playwright `scenario-*.spec.ts` GREEN.
    - OpenRouter is best-effort — a diff there does NOT block.
    - Any persistent native-7 skeleton diff → checkpoint returns gaps_found (do NOT proceed to Wave 4 until clean).
  </acceptance_criteria>
  <resume-signal>Type "clean-providers byte-identical PASS" (with the EVAL_SUMMARY N/M), or paste the BLOCK output + which provider diverged.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture/eval scripts → live provider APIs | Existing 089 harness; operator keys; localhost-only gate. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092.5-06 | Information Disclosure | capture_sse_baseline.py / eval_cross_provider.py | accept | Scripts print key PRESENCE only; `normalize` masks ids; localhost hard-gate. Unchanged 089 controls. The AFTER captures saved to `scripts/.sse_after/` contain masked event skeletons, no secrets. |

**Statement (per security_gate):** NO HIGH threats. This is a verification-only plan running the existing proof harness. No code or control changes.
</threat_model>

<verification>
- `SSE_DIFF_RESULT: PASS` native-7 after the clean-providers extraction.
- `EVAL_SUMMARY` non-regressing vs Plan-02 reference.
- E2E GREEN.
</verification>

<success_criteria>
- Deep is proven byte-identical on native-7 after extracting Anthropic + Google — the clean half of the cut is locked before the entangled OpenAI adapter (Wave 4) is touched.
</success_criteria>

<output>
After completion, create `.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-04-SUMMARY.md` (record the per-provider PASS + EVAL_SUMMARY N/M).
</output>
