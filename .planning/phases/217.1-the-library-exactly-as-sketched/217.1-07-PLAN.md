---
phase: 217.1-the-library-exactly-as-sketched
plan: 07
type: execute
wave: 6
depends_on: ["217.1-05", "217.1-06"]
files_modified:
  - frontend/src/components/library/ViewCard.tsx
  - frontend/src/components/ingestion/viewRuleWords.ts
  - frontend/src/components/ingestion/FilterBar.tsx
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - frontend/src/components/library/ViewsTab.tsx
  - frontend/src/components/library/__tests__/ViewCardGrid.test.tsx
  - frontend/src/components/library/__tests__/viewRulePhrase.test.ts
  - frontend/src/components/ingestion/__tests__/FilterBar.test.tsx
autonomous: true
requirements: [LIB-01]
must_haves:
  truths:
    - "Views renders a 2-column card grid instead of the sidebar-with-more-clicks list"
    - "Each card shows name, match count, the rule in plain words, a rule bar, N of M documents, and a menu"
    - "A view matching zero documents renders the amber zero-match card: 0, Matches nothing right now"
    - "Selection stays on librarySelection's single reducer — the card grid dispatches the SAME SELECT_VIEW action, and the reducer's 6-action set is unchanged"
  artifacts:
    - path: "frontend/src/components/library/ViewCard.tsx"
      provides: "one view as a card, modeled on WorkflowCard.tsx"
    - path: "frontend/src/components/ingestion/viewRuleWords.ts"
      provides: "EXTRACTED from FilterBar.tsx's CHIP_OP_LABEL + chipSummary — the plain-words rule renderer, not a new build"
  key_links:
    - from: "frontend/src/components/library/ViewCard.tsx"
      to: "frontend/src/pages/librarySelection.ts"
      via: "dispatches the same SELECT_VIEW action the sidebar row already dispatches; zero new reducer actions"
      pattern: "SELECT_VIEW"
---

<objective>
Stop Views from being "the sidebar with more clicks" (the sketch README's own prediction, which shipped
exactly as predicted): build the sketch's 2-column card grid, with the rule stated in plain words, a
`New view` affordance, and the zero-match card the README calls "what makes this a tab." Extract
`viewRuleWords.ts` from `FilterBar.tsx:31-55`'s already-shipped `CHIP_OP_LABEL` + `chipSummary` — this is
an EXTRACTION, not a build, per the pattern map. Selection stays on `librarySelection`'s single
six-action reducer (217-05 / SC#5) — the card grid must dispatch the identical `SELECT_VIEW` the sidebar
row already dispatches, and the sidebar mount stays alive beside the tab body (`LibraryPage.tsx:566-568`'s
own deliberate design: "the sidebar mount and the Views tab mount are alive at the same moment — which is
what makes 'they can never disagree' observable"). Also adopt `FilterBar.test.tsx` (217's deferred §3,
whose re-open trigger fires here, since this plan modifies `FilterBar.tsx` to extract the rule-words
leaf).

Purpose: this is the tab whose own sketch annotation predicted the shipped failure mode in writing.
Output: `ViewCardGrid` composed of `ViewCard`s; the rule-in-words extraction; the zero-match card.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217.1-the-library-exactly-as-sketched/217.1-CONTEXT.md
@frontend/src/components/library/ViewsTab.tsx
@frontend/src/components/ingestion/FilterBar.tsx
@frontend/src/components/ingestion/ViewsGroup.tsx
@frontend/src/pages/librarySelection.ts
@frontend/src/components/workflows/library/WorkflowCard.tsx
</context>

<interfaces>
<!-- Extracted from 217.1-RESEARCH.md §D.2 and 217.1-PATTERNS.md "Views tab (Wave 4)". -->

**`ViewsTab.tsx` owns NOTHING today, and that is load-bearing.** Its own docblock states: *"NO STATE…
no `useState`, no `useReducer`."* It receives `selectedViewId` + six callbacks from `LibraryPage`.
`LibraryAction` has exactly **six** members (`librarySelection.ts:162-167`), pinned by a 25-case suite.
⛔ **The card grid takes the IDENTICAL prop surface `ViewsTab` already has** — `views`, `selectedViewId`,
`onSelectView`, `onEditView`, `onRenameView`, `onDeleted` — a card's click dispatches the same
`SELECT_VIEW` the sidebar row does. **Zero new actions, zero new state.** The only new local state a card
grid may own is the cached match count, and it must share a cache with the sidebar rather than fetch
twice — reuse `ViewsGroup.tsx:63-95`'s lazy `fetchCount` shape, lifted into a shared `useViewCounts()` hook
if convenient, or duplicate the identical fetch-on-mount-with-cache pattern.

**`viewRuleWords.ts` — EXTRACTION, not build.** `FilterBar.tsx:31-55` already has `CHIP_OP_LABEL` and
`chipSummary` — these ALREADY ARE the rule-in-plain-words renderer (the sketch's own `type is contract ·
added in the last 30 days` shape). Extract them into `components/ingestion/viewRuleWords.ts` as a pure
leaf, re-export from `FilterBar.tsx` so its own consumers are unaffected, and this is 217's deferred §3
trigger for `FilterBar.test.tsx` (currently in neither count-gate knob) — adopt it here (mechanically:
verify it passes against the extracted module; the TARGETS/BASELINE entry itself happens in the phase's
final closing plan, 217.1-18, alongside every other new suite, per Pitfall 4).

**`ViewCard.tsx` — analog: `components/workflows/library/WorkflowCard.tsx`**, mounted in a grid at
`WorkflowsPage.tsx:1238`. Card composition per the sketch: name · match count · rule in plain words · rule
bar · `N of M documents` · a `…` menu (edit/rename/delete via the existing `onEditView`/`onRenameView`/
`onDeleted` callbacks). `Built in · shared with everyone` renders on the footer when `view.is_system_global`
(the `G` pill data already exists, per `NavRow`/`ViewsGroup`'s current usage).

**Zero-match card:** `total === 0` (from `resolveView(id, {count_only: true})` → `{total}`, already
shipped) renders the sketch's amber-outline `.vcard.vempty` treatment: `0`, `Matches nothing right now`.

**`New view`:** dispatches into the same flow `FilterBar`'s `onViewSaved` path already drives
(`LibraryPage.tsx:427`) — open the filter bar / `CHANGE_FILTER` action, not a new modal.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the rule-words leaf and adopt FilterBar.test.tsx</name>
  <files>frontend/src/components/ingestion/viewRuleWords.ts, frontend/src/components/ingestion/FilterBar.tsx, frontend/src/components/library/__tests__/viewRulePhrase.test.ts, frontend/src/components/ingestion/__tests__/FilterBar.test.tsx</files>
  <behavior>
    - Test (whole-table property, not one example): every `filter_expr` shape `viewRuleWords` is given
      produces a plain-English sentence using `CHIP_OP_LABEL`'s vocabulary, matching `FilterBar`'s own
      `chipSummary` output for the same input — proving the extraction is behavior-identical, not a
      re-implementation.
    - Test: the sketch's own example shape (`type is contract · added in the last 30 days`) round-trips
      correctly.
    - Test (regression): `FilterBar.test.tsx`'s existing suite passes unmodified after the extraction.
  </behavior>
  <action>
  Extract `CHIP_OP_LABEL` and `chipSummary` from `FilterBar.tsx:31-55` into `viewRuleWords.ts` as a pure
  `ruleInWords(filterExpr): string` leaf (keep names close to the original for grep-ability), and have
  `FilterBar.tsx` import and re-export from the new module rather than duplicating the logic — this is an
  in-file relocation with a re-export, not a rewrite. Run `FilterBar.test.tsx` to confirm no regression.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library/__tests__/viewRulePhrase.test.ts src/components/ingestion/__tests__/FilterBar.test.tsx --reporter=basic</automated>
  </verify>
  <done>viewRuleWords.ts exists and is behavior-identical to FilterBar's original chipSummary; FilterBar.test.tsx passes unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ViewCard, ViewCardGrid, and the zero-match card — same reducer, no new state</name>
  <files>frontend/src/components/library/ViewCard.tsx, frontend/src/components/library/ViewsTab.tsx, frontend/src/components/library/__tests__/ViewCardGrid.test.tsx</files>
  <behavior>
    - Test: a grid of `ViewCard`s renders name, match count, the rule in plain words (via
      `viewRuleWords.ts`), a rule bar, `N of M documents`, and a `…` menu per view.
    - Test: clicking a card dispatches the SAME `onSelectView`/`SELECT_VIEW` the sidebar row dispatches —
      assert against the mock callback, not a new local handler.
    - Test: a system-global view's card footer renders `Built in · shared with everyone`.
    - Test: a view with `total === 0` renders the amber zero-match card — `0`, `Matches nothing right now`.
    - Test: `New view` opens the same filter-bar flow `LibraryPage.tsx:427` already drives (assert the
      callback invoked, not a new modal component).
    - Test (regression, the reducer contract): re-run `librarySelection.test.ts`'s existing 25-case suite —
      the action-set count stays exactly 6, no new action was added.
  </behavior>
  <action>
  Build `ViewCard.tsx` per the `WorkflowCard.tsx` analog and the composition in the interface block. In
  `ViewsTab.tsx`, replace the mount of `ViewsGroup` inside the tab body (NOT the sidebar mount, which stays
  alive per `LibraryPage.tsx:566-568`'s deliberate design) with a `ViewCardGrid` composed of `ViewCard`s,
  receiving the identical `views`/`selectedViewId`/six-callback prop surface `ViewsTab` already has. Share
  the match-count cache with the sidebar (reuse or lift `ViewsGroup.tsx:63-95`'s `fetchCount` shape) rather
  than fetching twice. ⚠ **Same-commit ledger hygiene:** `ViewsGroup.tsx` fires G-5 (5 commits /
  3 phases / 259 lines, measured 2026-08-29) and has NO row and NO section in `docs/HOT-FILE-LEDGER.md`
  or `CLAUDE.md` today — this task touches it (the tab-body mount moves off it), so add both in THIS
  commit, not deferred to a later plan.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library/__tests__/ViewCardGrid.test.tsx --reporter=basic && npx vitest run src/pages/__tests__/librarySelection.test.ts --reporter=basic</automated>
  </verify>
  <done>The card grid renders correctly including the zero-match card; it dispatches the identical reducer action the sidebar does; librarySelection's action-set count is unchanged at 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | client-side composition over already-authenticated `listViews()`/`resolveView()` data; no new endpoint, no new mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217.1-12a | Tampering | a second selection state diverging from `librarySelection` | mitigate | Task 2's regression test asserts the reducer's action-set count is unchanged at 6; the card grid takes the identical prop surface, never its own `useState<selectedViewId>` |
| T-217.1-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/components/library/__tests__/viewRulePhrase.test.ts src/components/ingestion/__tests__/FilterBar.test.tsx --reporter=basic`
2. `cd frontend && npx vitest run src/components/library/__tests__/ViewCardGrid.test.tsx --reporter=basic`
3. `cd frontend && npx vitest run src/pages/__tests__/librarySelection.test.ts --reporter=basic`
4. `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs`
</verification>

<success_criteria>
ROADMAP SC#5 satisfied: the Views tab is a 2-column card grid with the rule in plain words, `New view`,
the zero-match card, and selection unchanged on the single reducer.
</success_criteria>

<output>
Create `.planning/phases/217.1-the-library-exactly-as-sketched/217.1-07-SUMMARY.md` when done
</output>
