---
phase: 217.1-the-library-exactly-as-sketched
plan: 12
type: execute
wave: 11
depends_on: ["217.1-11"]
files_modified:
  - frontend/src/components/library/HealthTab.tsx
  - frontend/src/components/library/CoverageRing.tsx
  - frontend/src/components/health/RetrievalTrendChart.tsx
  - frontend/src/components/library/HealthSignalChips.tsx
  - frontend/src/components/library/HealthDocumentBars.tsx
  - frontend/src/pages/LibraryPage.tsx
  - frontend/src/components/library/__tests__/HealthTiles.test.tsx
  - frontend/src/components/library/__tests__/HealthSignalChips.test.tsx
autonomous: true
requirements: [LIB-05]
must_haves:
  truths:
    - "The fifth tab (Health) is mounted in LibraryPage as one TabsTrigger and one TabsContent — no branch is added anywhere"
    - "The coverage ring renders numerator AND denominator, never a bare 0-100 grade word"
    - "The stacked chart renders three segments including could not search, lazy-loaded exactly as the shipped Coverage Trend chart already is"
    - "Five tiles render: Documents, Searches, Never found, Checked queries, and MATCH STRENGTH as the fifth (D-217.1-29)"
    - "Seven chips render in two groups; the two Low Confidence signals are renamed apart into Weak matches and Unsure metadata (D-217.1-11); the word golden appears nowhere"
    - "The stale-bar legend states the measured meaning (added more than 90 days ago); the re-indexing arm is DROPPED, not stubbed (D-217.1-28)"
  artifacts:
    - path: "frontend/src/components/library/HealthTab.tsx"
      provides: "the fifth tab body, mounted as a sibling of IngestionTab/IndexingTab — honours G-5 by construction"
    - path: "frontend/src/components/library/CoverageRing.tsx"
      provides: "the ring geometry copied from HealthScoreGauge.tsx, with the grade word deleted and numerator-and-denominator in its place"
  key_links:
    - from: "frontend/src/pages/LibraryPage.tsx"
      to: "frontend/src/components/library/HealthTab.tsx"
      via: "one TabsTrigger (:613-618) + one TabsContent (:681-707), fifth sibling of IngestionTab/IndexingTab"
      pattern: "HealthTab"
    - from: "frontend/src/components/library/HealthSignalChips.tsx"
      to: "@/lib/api/knowledge"
      via: "the seven shipped endpoints — getMostRetrieved, getNeverRetrieved, getLowConfidenceQueries, getStaleDocs, getGovBroken, getGovUnclassified, getGovLowConfidence"
      pattern: "getGov"
---

<objective>
Build the fifth tab. Every one of the seven signals already has a shipped component and a shipped API
call — this is a MOVE for six of seven blocks, plus one genuine build (`Checked queries`, deferred to
Plan 17/BE-6). Copy the coverage ring's geometry from `HealthScoreGauge.tsx` but DELETE its 0-100 grade
word (`"Healthy"`/`"Needs Attention"`/`"At Risk"`) — the sketch bans a bare-proportion-as-quality-grade —
and render numerator-and-denominator instead. Extend `RetrievalTrendChart` from one `<Area>` to three
stacked `<Area stackId>` segments, keeping its `lazy()` + `<Suspense>` wrapper (the app's ONE shipped
code-split; do not eagerly import recharts into the Library). Move the four tiles from
`HealthStatBar.tsx`'s shape verbatim, add `MATCH STRENGTH` as an operator-ruled fifth tile
(D-217.1-29 — `index.html` draws four tiles with no such element; its placement was undrawn, not decided
against). Move the seven chips into two labeled groups, with the two `Low Confidence` signals renamed
apart (D-217.1-11: `Weak matches` at `0.38` retrieval similarity vs `Unsure metadata` at `0.5` extraction
confidence — two thresholds, two data sources, one collided word today). The three Governance-sourced
chips are BUILT HERE UNGATED — Plan 13 adds the `governance_health` chip-level gate in the next wave, per
the phase's own sequencing (governance gating is Wave 7's seam, not Wave 6's composition).

Purpose: this is the fifth tab, absorbing Phase 218's whole scope into the Library.
Output: `HealthTab.tsx` mounted as `LibraryPage`'s fifth sibling; ring, chart, five tiles, seven chips,
per-document bars.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217.1-the-library-exactly-as-sketched/217.1-CONTEXT.md
@frontend/src/pages/KnowledgeHealthPage.tsx
@frontend/src/components/health/HealthScoreGauge.tsx
@frontend/src/components/health/HealthStatBar.tsx
@frontend/src/components/health/RetrievalTrendChart.tsx
@frontend/src/components/health/HealthDocumentRow.tsx
@frontend/src/pages/GovernancePage.tsx
@frontend/src/lib/api/knowledge.ts
@frontend/src/pages/LibraryPage.tsx
</context>

<interfaces>
<!-- Extracted from 217.1-RESEARCH.md §B.3, §D.5, §H notes 3/5/6 and 217.1-PATTERNS.md "Health tab (Wave 6+7)". -->

**MOVE THE MATH, DROP THE SEMANTICS — the ring.** `HealthScoreGauge.tsx` (SVG `<circle>` +
`strokeDasharray`, 61 L) renders a 0-100 score with a grade word (`:11`). Copy the ring geometry
(`:16-48`); replace the centre label with `${retrieved} of ${total}` (numerator and denominator, both
required — "a numerator alone would invite the reader to supply their own denominator", `IndexingTab.tsx:
70-72`'s own rule); DELETE the grade word entirely. Data: `getHealthOverview()` →
`retrieved_this_month`/`total_documents`.

**MOVE + EXTEND — the chart.** `RetrievalTrendChart.tsx` (recharts `AreaChart`, `lazy()`-loaded at
`KnowledgeHealthPage.tsx:29-32`). Today ONE `<Area dataKey="retrieval_count">`. Extend to THREE stacked
`<Area stackId="s" dataKey="found_something">`, `dataKey="found_nothing"`, `dataKey="could_not_search"` —
the three series Plan 11's `_fetch_retrieval_trend` now returns. ⚠ **Keep the `lazy()` + `<Suspense
fallback={<ChartSkeleton/>}>` wrapper** — recharts is ~200 KB and the Library is not a chart page. 7d/30d/
90d selector reuses the existing `days` param (`knowledge_health.py:663-666`, already `ge=1 le=365`).

**MOVE, near-verbatim — four tiles, plus the fifth.** `HealthStatBar.tsx` is already four cards, already
`label / value / description`. First three (`Documents`, `Searches`, `Never found`) come from
`getHealthOverview()`; the fourth (`Checked queries`) is a placeholder tile wired to real data only once
Plan 17/BE-6 ships (until then it renders the honest-unknown arm — this is NOT a violation of
build-or-drop, because the CAPABILITY ships in this same phase, Wave 9; this plan builds the tile's shape,
Plan 17 wires its data). The FIFTH tile, `MATCH STRENGTH` (D-217.1-03's qualified arm, D-217.1-29's ruled
placement): value `0.61`-shaped from `getHealthOverview()`'s score, description *"average similarity of
what searches returned"* — `COPY.SHIPPED_HEALTH_TILES` names `Retrieval Score` among the tiles that must
not be lost, so a fifth tile in the same row is the shape that keeps it.

**MOVE — chips, two groups, renamed apart:**
| Group | Chip | Endpoint | Threshold |
|---|---|---|---|
| Being used | `Most found` | `getMostRetrieved()` | count in `WINDOW_DAYS=30` |
| Being used | `Never found` | `getNeverRetrieved()` | absent from every `search.query` row |
| Being used | **`Weak matches`** | `getLowConfidenceQueries()` | retrieval similarity `< 0.38` (`knowledge_health.py:25`) |
| In good shape | `Stale` | `getStaleDocs()` | `created_at < now-90d` |
| In good shape | `Broken links` | `getGovBroken()` | — |
| In good shape | `Unclassified` | `getGovUnclassified()` | — |
| In good shape | **`Unsure metadata`** | `getGovLowConfidence()` | extraction confidence `< 0.5` (`document_governance.py:73`) |
⛔ **The word `golden` appears nowhere** — assemble the forbidden-needle check at runtime in the test, per
Pitfall 8; never spell it in a docblock the fence would also match. **This plan does NOT gate the three
Governance chips on `governance_health`** — they render unconditionally here; Plan 13 (Wave 7) adds the
gate as its own seam, matching the phase's Wave 6/Wave 7 split.

**MOVE — per-document bars, legend states the MEASURED meaning, re-indexing arm DROPPED (D-217.1-28):**
`HealthDocumentRow.tsx` already shipped. The legend reads `ready` / `stale — added more than 90 days ago`
— never *"the file changed after it was indexed"* (`_fetch_stale` selects `created_at < now-90d`, age not
modification; there is no "changed after indexing" signal anywhere). ⛔ **No `re-indexing` arm** — it has
no per-document source at all; building it would be exactly the placeholder build-or-drop forbids.

**Mounting the fifth tab — the G-5 seam, honoured by construction:**
```tsx
// LibraryPage.tsx:613-618 — add a fifth <TabsTrigger value="health">Health</TabsTrigger>
// LibraryPage.tsx:681-707 — add a fifth <TabsContent value="health"><HealthTab /></TabsContent>
```
No `if`, no new `useState`. `HealthTab` is the fifth sibling of `<IngestionTab />`/`<IndexingTab />`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coverage ring, three-segment chart, five tiles</name>
  <files>frontend/src/components/library/CoverageRing.tsx, frontend/src/components/health/RetrievalTrendChart.tsx, frontend/src/components/library/__tests__/HealthTiles.test.tsx</files>
  <behavior>
    - Test: `CoverageRing` renders `${retrieved} of ${total}` and contains NO grade word (assert absence of
      `Healthy`/`Needs Attention`/`At Risk` text nodes).
    - Test: `RetrievalTrendChart` renders three stacked `Area` elements keyed `found_something`,
      `found_nothing`, `could_not_search`, still wrapped in `lazy()`/`Suspense` (assert the component module
      is still dynamically imported, not statically bundled — check the existing lazy-loading test pattern
      if one exists, else assert the `ChartSkeleton` fallback renders before data resolves).
    - Test: the tile row renders exactly five tiles in order `Documents · Searches · Never found · Checked
      queries · MATCH STRENGTH`; `MATCH STRENGTH` renders its value and the qualifier sentence *"average
      similarity of what searches returned"*.
    - Test: `Checked queries` renders the honest-unknown arm (no data source wired yet in this plan).
  </behavior>
  <action>
  Build `CoverageRing.tsx` copying `HealthScoreGauge.tsx:16-48`'s SVG geometry, replacing the centre label
  and deleting the grade-word logic entirely. Extend `RetrievalTrendChart.tsx` from one `Area` to three
  stacked `Area`s over the new three-series shape, preserving the `lazy()`/`Suspense` wrapper unchanged.
  Build the five-tile row reusing `HealthStatBar.tsx`'s card shape, with `Checked queries` in its honest-
  unknown state pending Plan 17.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library/__tests__/HealthTiles.test.tsx --reporter=basic</automated>
  </verify>
  <done>Ring shows numerator-and-denominator with no grade word; chart shows three stacked segments, still lazy-loaded; five tiles render in the correct order with MATCH STRENGTH's qualifier sentence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seven chips (renamed apart, ungated for now), per-document bars, HealthTab assembly</name>
  <files>frontend/src/components/library/HealthSignalChips.tsx, frontend/src/components/library/HealthDocumentBars.tsx, frontend/src/components/library/HealthTab.tsx, frontend/src/pages/LibraryPage.tsx, frontend/src/components/library/__tests__/HealthSignalChips.test.tsx</files>
  <behavior>
    - Test: seven chips render in two labeled groups (`Being used` / `In good shape`) with the exact
      renamed labels — `Most found`, `Never found`, `Weak matches`, `Stale`, `Broken links`,
      `Unclassified`, `Unsure metadata`.
    - Test: `Weak matches` and `Unsure metadata` are backed by DIFFERENT endpoints
      (`getLowConfidenceQueries` vs `getGovLowConfidence`) and different thresholds — assert both client
      fns are called, and the two chip labels are distinct DOM text nodes (proving the rename split
      actually landed, not a single relabeled chip).
    - Test (Pitfall 8-safe): the assembled forbidden needle `"golden"` (built at runtime, e.g.
      `["g","o","l","d","e","n"].join("")`, never spelled literally in a comment) does not appear anywhere
      in the rendered Health tab's text content.
    - Test: `HealthDocumentBars` legend reads *"added more than 90 days ago"* for the stale arm; NO
      `re-indexing` legend entry or bar-state exists anywhere in the component (assert the legend has
      exactly two entries: `ready`, `stale`).
    - Test: `LibraryPage` renders a fifth `TabsTrigger`/`TabsContent` pair for Health, sibling to
      Ingestion/Indexing — assert no new conditional branch was introduced (a structural assertion: count
      `TabsContent` children stays a flat sibling list, no nested `if`).
  </behavior>
  <action>
  Build `HealthSignalChips.tsx` with the seven chips in two groups per the interface table, calling all
  seven shipped endpoints (Governance's three ungated in this plan — Plan 13 adds the gate next wave).
  Build `HealthDocumentBars.tsx` reusing `HealthDocumentRow.tsx`'s shipped per-document row, with the
  two-arm legend and the measured-meaning sentence; do not build a `re-indexing` arm anywhere. Assemble
  `HealthTab.tsx` composing Task 1's ring/chart/tiles plus this task's chips/bars. Mount `HealthTab` as
  `LibraryPage`'s fifth `TabsTrigger`(`:613-618`)/`TabsContent`(`:681-707`) sibling — additive only, no
  branch.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library/__tests__/HealthSignalChips.test.tsx --reporter=basic    <automated>cd frontend && npx vitest run src/components/library/__tests__/HealthSignalChips.test.tsx --reporter=basic</automated>
  </verify>
  <done>Seven chips render correctly, renamed apart, both endpoints provably distinct; the golden needle is absent; the per-document legend states the measured meaning with no re-indexing arm; the fifth tab mounts as a sibling with no new branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client render → Governance-sourced endpoints | this plan calls all seven signal endpoints, including the three governance ones, UNGATED — Plan 13 (next wave) adds the client-side render gate. This plan alone does not close T-217.1-04; it is explicitly deferred one wave, stated here so it is never mistaken for done |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217.1-04 | Elevation of Privilege | the three Governance chips rendering ungated in this plan | mitigate (deferred one wave, tracked) | Plan 13 adds the `governance_health` render gate and its negative test in the immediately following wave; this plan's summary must state the gate is NOT yet applied so no reviewer mistakes this plan as closing T-217.1-04 |
| T-217.1-16a | Tampering | the ring/chart/tiles silently reverting to a bare-proportion grade word on a future edit | mitigate | the "no grade word" and "numerator-and-denominator" assertions in Task 1 are the standing regression fence |
| T-217.1-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/components/library/__tests__/HealthTiles.test.tsx --reporter=basic`
2. `cd frontend && npx vitest run src/components/library/__tests__/HealthSignalChips.test.tsx --reporter=basic`
3. `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs`
</verification>

<success_criteria>
ROADMAP SC#7 and part of SC#8 satisfied: the fifth tab exists with the ring, chart, five tiles, seven
renamed chips, and per-document bars — governance gating is explicitly deferred to Plan 13, not silently
skipped.
</success_criteria>

<output>
Create `.planning/phases/217.1-the-library-exactly-as-sketched/217.1-12-SUMMARY.md` when done
</output>
