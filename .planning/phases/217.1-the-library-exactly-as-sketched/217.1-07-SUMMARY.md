---
phase: 217.1-the-library-exactly-as-sketched
plan: 07
status: complete
started: 2026-08-29T22:20:00.000Z
completed: 2026-08-29T23:58:00.000Z
tasks: 2
---

## Plan 217.1-07: The Views tab is a card grid, not "the sidebar with more clicks"

ROADMAP SC#5 satisfied. The sketch's own README predicted the failure — "the sidebar with more clicks" — and it shipped exactly as predicted. Plan 07 rebuilt the tab body.

### Task 1 (committed by a sub-agent before it died — `d58e0cd59`): rule-words extraction

`viewRuleWords.ts` — `CHIP_OP_LABEL` + `chipSummary` extracted from `FilterBar.tsx:31-55` into a pure leaf, plus a `ruleInWords(filterExpr)` that joins conditions with `·`. `FilterBar` re-exports, so consumers are unaffected. `viewRulePhrase.test.ts` added. 14 tests pass (4 phrase + 10 FilterBar regression).

### Task 2 (executed inline — sub-agent spawns all hit gateway 503s): the card grid

**`ViewCard.tsx`** — one view as a card, modeled on `WorkflowCard` + the sketch's `.vcard`:
- name (+ `G` pill for system-global), match count, rule in plain words, rule bar, `N of M documents`, `⋯` menu.
- **Zero-match card** (the sketch's "what makes this a tab"): `total === 0` → amber treatment, `0`, `Matches nothing right now`.
- **Selected marker = the sidebar's class**: the name button carries `bg-primary/10` (the exact class `LibraryPage.test.tsx:298`'s SC#5 check asserts) plus a whole-card ring. "Two renderings can never disagree" holds observably.

**`ViewCardGrid.tsx`** — 2-column grid. `useViewCounts` lifts `ViewsGroup.tsx:63-95`'s lazy+cached `fetchCount` shape so the grid and the sidebar share one cache (never fetch a view's count twice). **Zero new selection state**: a card's click dispatches the identical `onSelectView`/`SELECT_VIEW` the sidebar row dispatches.

**`ViewsTab.tsx`** — the tab body mounts `ViewCardGrid` with the identical 5-callback prop surface the sidebar's `ViewsGroup` mount receives (plus optional `corpusCount`/`onNewView`). `pagehead`/`tabslist` fence hooks stay red by design (they're LibraryPage-level; plan 18 turns the whole fence green).

**`LibraryPage.tsx`** — threads `corpusCount` + `onNewView`; `New view` dispatches `CHANGE_FILTER` (empty filter) + expands the filter bar — the SAME compose flow FilterBar's `onViewSaved` path drives, never a new modal.

### Ledger hygiene (same commit, per the plan's hard constraint)
`ViewsGroup.tsx` — **5 / 3 / 259, G-5 FIRES, absent for its entire life.** Row added to `CLAUDE.md`'s table + a full section in `docs/HOT-FILE-LEDGER.md`, naming the shared count-cache as the refactor seam.

### Verification
- 5 suites, 63 tests: `ViewCardGrid` (8) · `viewRulePhrase` (4) · `FilterBar` (10) · `librarySelection` (25, reducer unchanged) · `LibraryPage` (16, incl. the SC#5 class check)
- `npx tsc --noEmit` clean · `npx vite build --logLevel error` clean
- `drive.cjs` — 221 passed · 0 failed
- `check-claude-md-size.cjs` — OK

### Self-Check: PASSED
- [x] 2-column card grid instead of the sidebar-with-more-clicks list
- [x] Cards show name, match count, rule in plain words, rule bar, N of M documents, menu
- [x] Zero-match view renders amber `0` / `Matches nothing right now`
- [x] Selection stays on librarySelection's single reducer — 6-action set unchanged

⚠ Note: the `sketchComposition` fence remains deliberately RED (its BASELINE contract) — plan 18 turns it green at phase close. The `views-vgrid` hook + `New view` button this plan built DO pass; the remaining red is `pagehead`/`tabslist` hooks that only a full `LibraryPage` mount can satisfy.
