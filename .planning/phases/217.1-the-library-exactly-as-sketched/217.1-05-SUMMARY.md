---
phase: 217.1-the-library-exactly-as-sketched
plan: 05
status: complete
started: 2026-08-29T21:25:00.000Z
completed: 2026-08-29T21:50:00.000Z
tasks: 2
---

## Plan 217.1-05: DocumentRow.tsx — the 12-phase G-5 seam, discharged

Part of ROADMAP SC#4. The `DocumentList.tsx` G-5 named seam (*"the row IS the extraction"*), owed since 12 phases ago, discharged at the exact moment the sketch's five per-row affordances forced it.

### What shipped (one commit, both tasks — the extraction file IS the new affordances)

**`frontend/src/components/ingestion/DocumentRow.tsx`** — the extracted row. Owns:
1. **Folder tag pill** — `Root` when `folder_id` is null (D-217.1-31, never `Uncategorized`), resolved name otherwise; `folders` threaded from `LibraryPage`'s `useFolders`, no new fetch.
2. **`ChunkProportionBar`** in the Chunks cell — track + fill + tabular count, role-matched to `ConfidenceChip`; denominator computed ONCE in `DocumentList` over the loaded rows (`maxChunkCount`).
3. **Expand chevron** — still version-gated (`version_number > 1`), unchanged per D-217.1-30.
4. **Inline `IngestionStrip`** for `processing` rows — one component, two mounts (Ingestion tab + here), never a fork.
5. **Failure sentence** for `failed` rows — `classifyIngestionError` from Wave 1, imported, not re-implemented.

`VersionHistoryPanel` and `ClassificationRowChip` moved into the row file unchanged. `hasVersions`/`isExpandable` exported from `DocumentRow` (the delete dialog still needs `hasVersions`).

**`frontend/src/components/ingestion/DocumentList.tsx`** — now owns ONLY the `<table>`, the seven-column `<thead>` ordering, the CSS shed, the delete/move dialogs, and the `reingestingId`/`expanded` state. Maps `filtered` → `<DocumentRow ...>`.

**`frontend/src/pages/LibraryPage.tsx`** — threads `folders` to the `DocumentList` mount.

**`.planning/sketches/218-the-library-and-its-tabs/drive.cjs`** — `D5c-b`'s source grep now reads `DocumentRow.tsx` (the row IS the document list's per-row renderer); the content (the `table_count` pill) is unchanged, only relocated.

### Shed invariant — re-verified, not trusted
- Exactly 7 `<th>` in order chevron/Filename/Type/Size/Chunks/Status/Actions (jsdom assertion)
- `<th>` class strings keep `px-4 py-3` byte-identical (jsdom assertion)
- Row renders exactly 7 `<td>` (jsdom assertion) — `nth-child(3-5)` shed hides Type/Size/Chunks by position
- `drive.cjs` A1/A2 + all 221 assertions pass

### Verification
- `npx tsc --noEmit` — clean · `npx vite build --logLevel error` — clean (exit 0)
- Existing suites UNMODIFIED: `DocumentList.test.tsx` + `DocumentStatusBadge.a11y.test.tsx` + `DocumentStatusBadge.test.tsx` — **45/45 pass**
- New tests: 12 (column count/classes, td count, folder pill both arms, proportion bar + zero arm, strip/sentence/badge status arms) — **17/17 in the file**
- `node drive.cjs` — **221 passed · 0 failed**

⚠ Noted for plan 18: the two adopted `DocumentStatusBadge` suites are NOT added to the count-gate BASELINE here (that is 217.1-18's job, per the plan's Pitfall 4).

### Self-Check: PASSED
