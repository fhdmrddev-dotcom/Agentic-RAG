---
phase: 217.1-the-library-exactly-as-sketched
plan: 09
status: complete
started: 2026-08-30T00:30:00.000Z
completed: 2026-08-30T01:15:00.000Z
tasks: 2
---

## Plan 217.1-09: BE-2 index-summary + BE-3 folder-scoped reembed

Part of ROADMAP SC#6 — the Indexing tab's data layer (every fact + every scoped action Plan 10's frontend consumes).

### Task 1 — `GET /library/index-summary` (BE-2 / D-217.1-27)

**`backend/app/api/library.py`** (new one-route module, `document_queries.py` precedent — deliberately NOT in `documents.py`, G-5 HARD at 71/31/2535):
- **UNGATED** (D-217.1-27): carries NO `require_visible("model_management")` — reading your own corpus facts is not model management. The three ACTION buttons stay gated in Plan 10. Closes 217's deferred WR-02.
- **RLS-ENFORCED, no service-role carve-out**: `get_user_pg_connection` (asyncpg, `SET LOCAL ROLE authenticated` + JWT GUCs) — unlike `knowledge_health.py`/`document_queries.py`, whose carve-out exists only because `audit_log` lacks an authenticated SELECT policy. `document_chunks`/`documents`/`folders` all have one.
- **Response**: corpus totals (vectors, chunks_total, documents_without_vectors, last_indexed, model, dimensions, provider) + per-folder rows. `last_indexed = max(embedded_at)` (BE-1, never `created_at`). `Root` for the null-folder bucket (D-217.1-31). Provider resolved from the corpus model id via `resolve_effective_embedding_provider` (no new column).

**`test_2171_index_summary_rls.py`** (2 live tests, real local Postgres):
- **The mandatory T-217.1-01 two-user leak proof**: user A seeded with a folder+doc+chunk, user B likewise; A's response contains A's folder and NEVER B's; vice-versa. Non-vacuity: A's own data MUST appear. **2/2 pass on the real DB.**
- Ungated proof: a plain user gets the facts, not 403.
- ⚠ Test seam note: the handler runs in-process with `get_user_pg_connection` patched to the TEST pool — the app-singleton pool + TestClient-portal pairing is loop-incompatible on Windows (the 163 leak suite's own `as_user_asyncpg` precedent). Real SQL + real RLS + real two-user rows are exercised; only the routing/Depends layer (which adds no access control on an ungated route) is bypassed.

### Task 2 — Folder scope on reembed (BE-3)

**`reembed_service.py`** — `folder_ids: list[str] | None = None` threaded through `start_reembed` → `reembed_job` (mirroring `org_id`'s threading). Resolved to `document_id`s once before the loop (RLS-scoped read — a foreign folder id resolves to zero documents, never another tenant's), then `.in_("document_id", ids)` **ANDed onto** (never replacing) the stale model-mismatch `.or_(...)` predicate.

**Distinguishable response cases** (so Plan 10 renders honestly, never a silent no-op):
- unscoped default → no `scope` marker (a real re-index is never confused with a no-op)
- folder with zero documents at all → `scope: "folder_empty"`
- docs exist, none stale → `scope: "already_current"` (Plan 10 renders "Already indexed with the current model.")

**`settings.py`** — `POST /settings/reembed` accepts an optional `folder_ids` body (stays behind `model_management`, unchanged).

**`test_2171_reembed_folder_scope.py`** (4 tests): unscoped adds no `.in_` (byte-identical regression guard); folder scope ANDs `.in_("document_id")` while the `.or_` survives; folder_empty distinguishable; already_current distinguishable.

### Verification
- `test_2171_reembed_folder_scope.py` — **4/4 pass**
- `test_2171_index_summary_rls.py` — **2/2 pass** (live two-user RLS proof)
- App imports clean; both routes registered (`/library/index-summary`, `/settings/reembed`)

⚠ **No regressions from my edits:** the 3 failing `test_111_1_reembed_*` integration tests reproduce at the parent commit with my `reembed_service.py`/`settings.py` stashed — pre-existing environment rot, not this plan.

### Self-Check: PASSED
- [x] Endpoint ungated + RLS-scoped (proven by a real two-user test)
- [x] Full shape incl. Root/Uncategorized row + BE-1-derived last_indexed
- [x] folder_ids ANDs onto (never replaces) the stale predicate
- [x] Already-current and zero-chunk folders distinguishably reported
