# Phase 217.1 — BASELINE

**The captured RED run of the `sketch-composition` fence, before any wave built anything.**

Captured by plan `217.1-01` (Wave 1), 2026-08-29, in a bootstrapped worktree.

> **A guard nobody has seen fire is not a guard.** Phase 217 shipped against a contract whose every
> assertion was green while four of the five tabs carried none of the sketch's furniture — because the
> contract pinned WORDS and could not name a MISSING BLOCK. This file is the proof that its replacement
> can. Every figure below carries the command that produced it, so the next reader **re-derives rather
> than trusts**.

⛔ **THIS FILE IS WRITTEN ONCE AND NEVER EDITED BY A LATER WAVE** (T-217.1-06b). A wave that turns a
block green records that in its own SUMMARY; overwriting the red evidence here would destroy the only
record that the guard was ever seen to fire.

---

## §0 The base — asserted, not assumed

| Property | Value |
|---|---|
| Dispatched base SHA | `1a509f4a8d5d67436d2aa4cf324fea68509370ad` |
| Worktree branch | `worktree-agent-adc48de782baef494` |
| Arrival `git merge-base HEAD <base>` | `f7cfa4a53d7a6006d89423e1c65e6f8f95a85c33` — **did NOT descend** |
| Correction | `git reset --hard 1a509f4a8…` → `HEAD is now at 1a509f4a8 docs(217.1): plan list, progress row, and the plan-phase closeout` |

⚠ **THE WORKTREE FORKED FROM THE WRONG BASE AND WAS CORRECTED.** Recorded rather than silently fixed,
because this project has measured it repeatedly (12/12 worktrees in Phase 192; again 2026-08-14 and at
`194.1-01`) and **a silent correction is indistinguishable from it never having happened.** The
`<worktree_branch_check>` step is what caught it.

---

## §1 The emitter — `drive.cjs`, before and after

```
$ node .planning/sketches/218-the-library-and-its-tabs/drive.cjs      # BEFORE the composition walk
  sketch 218 — the Library and its tabs
  200 passed · 0 failed · 200 assertions

$ node .planning/sketches/218-the-library-and-its-tabs/drive.cjs      # AFTER (§K spliced in)
  sketch 218 — the Library and its tabs
  221 passed · 0 failed · 221 assertions
```

`+21` assertions, `0` failing. The tagging pass that preceded them (`32` `data-block` attributes,
`32 insertions / 32 deletions`, **attributes only** — no visible text, class or composition changed)
was run against the 200-assertion suite first and left it at `200 passed · 0 failed`, which is how the
tagging is known to be inert.

**Determinism, measured rather than asserted** (the `git diff --exit-code` drift signal T-217.1-06a
depends on it):

```
$ node …/drive.cjs --emit ; md5sum …/BUILD-CONTRACT.composition.json
a47c412d288868459d91e86988c8250e   (run 1)
a47c412d288868459d91e86988c8250e   (run 2)
a47c412d288868459d91e86988c8250e   frontend/src/components/library/__generated__/sketchComposition.json
```

The JSON carries **no timestamp**. The markdown does (one `Generated:` date line), and its body hashes
identically across re-runs within a day: `66342ac3c8f4e708d41bb8b6873e1414` twice.

### The emitted composition — what the sketch actually draws

| screen | blocks, in document order | named buttons |
|---|---|---|
| `documents` | `pagehead · tabslist · sidebar · stat-tiles · doclist · tfoot` | _none_ |
| `views` | `pagehead · tabslist · vgrid` | **New view** |
| `ingestion` | `pagehead · tabslist · subnav · dropbig · folder-picker · stagecards · queue · needs-attention` | **Choose files** · **Try again** |
| `indexing` | `pagehead · tabslist · vector-store-card · embedding-model-card · folders-table` | **Change model** · **Re-index everything** · **Re-index selected** |
| `health` | `pagehead · tabslist · coverage-ring · searches-chart · stat-tiles · chipgroup-being-used · chipgroup-in-good-shape · document-bars · checks · match-strength-tile` | **Add a check** |

---

## §2 THE RED RUN — verbatim

```
$ cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run \
    src/components/library/__tests__/sketchComposition.test.tsx \
    --reporter=verbose --reporter=json --outputFile.json=../.vitest-sketchcomposition.json

 Test Files  1 failed (1)
      Tests  40 failed | 7 passed (47)
   Start at  19:35:33
   Duration  3.26s (transform 868ms, setup 84ms, import 149ms, tests 2.31s, environment 587ms)
```

⚠ **`--reporter=basic` does not exist in this repo's vitest** — the plan's verify command was written
with it and it dies at startup with `Failed to load custom Reporter from basic` before running a single
case. Substituted `--reporter=verbose` plus the JSON reporter, which is also what makes the list below
machine-extracted rather than eyeballed. Recorded as a deviation, not silently corrected.

### §2.1 The 7 that PASSED — the positive controls

**These are load-bearing.** A run in which *every* case is red is indistinguishable from a run whose
mount harness or import path is wrong (217.1-RESEARCH §C.5 point 4).

```
✓ §1 the contract itself · resolves to a non-empty object
✓ §1 the contract itself · carries all five screens
✓ §1 the contract itself · carries a character floor of real content
✓ §1 the contract itself · names at least three blocks on every screen
✓ §2 positive controls · the page renders its heading — the mount harness works
✓ §2 positive controls · the four shipped tab triggers render — the tab bar is already built
✓ §2 positive controls · the shipped tab-body hooks render — `data-testid` is the right convention
```

So: **the JSON import resolves and is non-empty, the mount harness mounts, the shipped hooks are
findable by the exact query the failing cases use.** The 40 reds below are therefore missing
composition, not broken plumbing.

### §2.2 The 40 that FAILED — extracted from the vitest JSON report, before any re-run

**32 missing blocks.** Every one fails with
`TestingLibraryElementError: Unable to find an element by: [data-testid="…"]`:

| screen | missing hooks |
|---|---|
| `documents` | `documents-pagehead` · `documents-tabslist` · `documents-sidebar` · `documents-stat-tiles` · `documents-doclist` · `documents-tfoot` |
| `views` | `views-pagehead` · `views-tabslist` · `views-vgrid` |
| `ingestion` | `ingestion-pagehead` · `ingestion-tabslist` · `ingestion-subnav` · `ingestion-dropbig` · `ingestion-folder-picker` · `ingestion-stagecards` · `ingestion-queue` · `ingestion-needs-attention` |
| `indexing` | `indexing-pagehead` · `indexing-tabslist` · `indexing-vector-store-card` · `indexing-embedding-model-card` · `indexing-folders-table` |
| `health` | `health-pagehead` · `health-tabslist` · `health-coverage-ring` · `health-searches-chart` · `health-stat-tiles` · `health-chipgroup-being-used` · `health-chipgroup-in-good-shape` · `health-document-bars` · `health-checks` · `health-match-strength-tile` |

**1 missing tab**, asserted as its own case rather than left implicit in ten missing blocks:

```
✗ §3 health · mounts a Health tab at all
    TestingLibraryElementError: Unable to find an accessible element with the role "tab" and name "Health"
```

`librarySelection.ts:94` types `LibraryTab = "documents" | "views" | "ingestion" | "indexing"`. A fifth
tab is a change to the page's reducer, **not** a component that forgot a `data-testid` — which is why it
is a separate case with a separate message.

**7 missing named buttons.** Every one fails with
`TestingLibraryElementError: Unable to find an accessible element with the role "button" and name "…"`:

```
✗ views     · New view
✗ ingestion · Choose files
✗ ingestion · Try again
✗ indexing  · Change model
✗ indexing  · Re-index everything
✗ indexing  · Re-index selected
✗ health    · Add a check
```

⚠ **The three `indexing` buttons are OPERATOR-ONLY (D-217.1-27)** and must render **as absent** for
everyone else, never as dead controls. This suite's current mount grants nothing, so the wave that
builds them owns both the gate and the mock that satisfies it. **The red is correct today for two
different reasons at once, and the wave must distinguish them.**

---

## §3 Non-adoption — measured, not promised

```
$ grep -c sketchComposition scripts/vitest-count-gate.cjs
0
```

⛔ **The suite is in NEITHER count-gate knob**, and it is not even *reachable* by the gate: `TARGETS`
holds exactly one bare-directory entry (`"src/components/workflows"`, `:3386`) and every other entry is
a NAMED FILE. `grep -n "components/library" scripts/vitest-count-gate.cjs` returns **nothing**, so
`src/components/library/__tests__/` is not executed by the gate at any grain.

**This is deliberate and it is sequenced.** The gate's contract is *zero failing, forever*; adopting a
deliberately-red suite would turn every subsequent plan's gate red for reasons it did not cause.
**Plan `217.1-18` adopts it into both knobs**, once each wave has turned its own blocks green.

⚠ **TARGETS decides what RUNS; BASELINE decides what is GUARDED**, and a suite can sit on the wrong side
of exactly one of them (the `WorkflowScheduleModal.test.tsx` finding, Phase 214). Plan 18 must add
**both**, and must state the pin it is adding.

---

## §4 The count gate at this phase's base — ALREADY VIOLATED, and not by this plan

Quoted from `217.1-RESEARCH.md` §C.6, measured this session at the phase's base:

```
  total                                      5932    6666    +734
  total 6666  ·  failed 1  ·  pinned total 5932
--------------------------------------------------------------
RESULT: COUNT GATE VIOLATED (1 reason(s))
  FAIL  [failing-tests] 1 test(s) failed — the gate requires 0.

FILE: frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
   x  canvas door — flag ON (D-183-01) clicking Canvas flips aria-selected and mounts the canvas;
      clicking Spine returns  ||  Error: STACK_TRACE_ERROR
```

It is **one of SEED-171's five named cap-independent flaky suites**, and it is **provably unmodified by
this plan**: `git diff --numstat 1a509f4a8… HEAD -- frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx`
is empty. ⚠ **Not "fine"** — one green sample of a flaky suite proves nothing and one red sample proves
nothing either. **The cap was neither adjusted nor needed** (`GSD_VITEST_MAX_WORKERS=2` throughout).

---

## §5 Pre-existing `tsc` errors — out of scope, recorded so nobody claims this plan caused them

```
$ cd frontend && npx tsc -p tsconfig.app.json --noEmit | grep -c "^src/"
34
```

**None is in this plan's files.** `npx tsc … | grep "sketchComposition\|components/library"` returns
**nothing**. The 34 live in `SettingsPage.tsx` / `SettingsPage.test.tsx`, `OrgProvider.test.tsx`,
`StreamsProvider.tsx`, `streamsStore.ts` and neighbours — the long-standing rot, untouched here.
⚠ Recorded because *"the typecheck is red"* is exactly the kind of observation that gets attributed to
whichever plan noticed it.
