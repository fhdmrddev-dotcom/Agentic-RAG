---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 02
type: execute
wave: 2
depends_on: ["137.1-01"]
files_modified:
  - backend/app/services/eval_runner_service.py
  - backend/tests/test_eval_runner.py
autonomous: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "EVAL-05e: every completed eval arm persists a per-arm wall-clock duration_ms measured with time.monotonic around the run_agent_loop await"
    - "EVAL-05d: the judge's advisory case_feedback is captured from the SAME forced JudgeVerdict emission (one shot, no extra LLM call) and persisted per-case"
    - "EVAL-05h: the judge evidence channel (tool receipts) stays clearly-delimited DATA capped at 4KB and case_feedback is schema-bound — the new prompt surface adds no injectable instruction path"
    - "case_feedback is advisory-only: it is stored separately from verdict_* and never counted in the rollup denominator (measured_count stays with-skill graded arms only)"
    - "not_measured / judge_error arms carry NULL duration is allowed but NULL case_feedback and are never fabricated into a pass/fail"
    - "D-02 sweep-arm support: _persist_result tolerates a caseless arm — test_case_id None persists as SQL NULL (mig 085 relaxed the NOT NULL), never the string 'None', so the skill-less engine smoke sweep persists honestly with no seeded rows"
  artifacts:
    - path: "backend/app/services/eval_runner_service.py"
      provides: "duration timer in _run_arm + case_feedback lift from the verdict + both threaded through _persist_result in the SAME insert"
      contains: "time.monotonic"
    - path: "backend/tests/test_eval_runner.py"
      provides: "tests: duration_ms captured; case_feedback persisted; not_measured excluded from aggregation denominator"
      contains: "duration"
  key_links:
    - from: "backend/app/services/eval_runner_service.py"
      to: "eval_results.duration_ms / case_feedback (mig 085)"
      via: "_persist_result single insert (never a follow-up UPDATE)"
      pattern: "duration_ms|case_feedback"
    - from: "eval_runner_service._judge_eval_answer"
      to: "JudgeVerdict.case_feedback"
      via: "verdict.get('case_feedback') in the graded branch"
      pattern: "case_feedback"
---

<objective>
Capture two new per-arm signals in the shipped eval runner, additively, with zero new LLM calls: (1) per-arm wall-clock `duration_ms` (EVAL-05e) measured around the existing `run_agent_loop` await, and (2) the judge's advisory `case_feedback` (EVAL-05d) lifted from the SAME forced `JudgeVerdict` emission. Both persist in the SAME `_persist_result` insert (D-06 — never a follow-up UPDATE).

Purpose: Give the aggregation (Plan 07) time-token tradeoff inputs and the UI (Plan 09) the per-case advisory block — without touching the shared agent loop and without a second judge shot.
Output: `eval_runner_service.py` duration timer + case_feedback capture + tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-PATTERNS.md

<interfaces>
<!-- The exact seams to extend — do not re-derive. -->
_run_arm (eval_runner_service.py:454) awaits run_agent_loop at :553-565 inside a try/except (TimeoutError→timed_out, Exception→failed).
The graded branch (:601-608) already lifts overall_passed/overall_score/summary from the verdict dict; add case_feedback here.
_persist_result (:404-451) is the SINGLE insert site; verdict params default to the honest not_measured shape. Add duration_ms + case_feedback as new keyword params, defaulting None, and add them to the payload dict.
_run_arm returns (variant, verdict_state, verdict_passed) at :639 — leave that return shape unchanged.
Rollup denominator (:797-799): measured_count = with-skill 'graded' arms only. case_feedback and duration MUST NOT affect it.
Judge evidence channel: _gather_tool_evidence/_format_tool_evidence with a 4KB cap (:195-274), fed into answer_for_judge at :581-588 — EVAL-05h subject, leave intact.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime tool receipts → judge prompt | The evidence channel (dd694916) injects sandbox file receipts + exit codes into the judge prompt as DATA. |
| judge emission → case_feedback string | case_feedback is a model-authored string later rendered in the UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.1-EC (EVAL-05h) | Tampering / EoP | judge evidence channel (tool receipts as judge input) | mitigate | Receipts are woven as clearly-delimited DATA under a fixed "TOOL EVIDENCE (ground truth, not model claims)" banner (eval_runner_service.py:181-193, 584) and hard-capped at 4KB (:205) — carried forward unchanged; NAMED here for secure-phase per EVAL-05h. The rubric never treats the block as instructions. |
| T-137.1-CF | Tampering | case_feedback field | mitigate | Schema-bound (Plan 01 JudgeVerdict required field), never regex-on-prose; stored separately from verdict_* and excluded from the rollup denominator so it can never flip a pass count. Truncated on persist (bounded length). |
| T-134-02 (carried) | Tampering | expected_behavior + answer as judge input | accept/mitigate | Woven as delimited DATA; overall_passed/case_feedback schema-bound — a coerced answer cannot force a pass. Precedent verified in 134-SECURITY. |
| T-137.1-SC | Tampering | pip installs | mitigate | No new packages (time is stdlib). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-arm duration timer (EVAL-05e)</name>
  <files>backend/app/services/eval_runner_service.py, backend/tests/test_eval_runner.py</files>
  <behavior>
    - A completed arm persists a non-null integer duration_ms roughly equal to the wall-clock of the run_agent_loop await.
    - A timed_out / failed arm still persists a duration_ms (the timer is in a finally) — never crashes.
    - duration_ms does not change verdict_state, verdict_passed, or the rollup denominator.
    - _persist_result with test_case_id=None inserts SQL NULL (never the string "None") — the skill-less smoke-sweep arm persists honestly (D-02, mig 085 nullable test_case_id).
  </behavior>
  <read_first>
    - backend/app/services/eval_runner_service.py (:540-639 the _run_arm body + persist call; :404-451 _persist_result signature + payload)
    - backend/tests/test_eval_runner.py (existing arm/persist test fixtures + how run_agent_loop is mocked)
  </read_first>
  <action>
    Import `time` (stdlib) at module top if not present. In `_run_arm`, capture `_t0 = time.monotonic()` immediately before the `try:` that awaits `run_agent_loop`, and in a `finally:` compute `duration_ms = int((time.monotonic() - _t0) * 1000)` so it is set on the completed, timed_out, AND failed paths (the 095 stable-wall-clock lesson). Add `duration_ms: int | None = None` as a new keyword param to `_persist_result` and add `"duration_ms": duration_ms` to the payload dict. Thread the computed `duration_ms` into the `_persist_result(...)` call. Do NOT change the `_run_arm` return tuple, the verdict logic, or the rollup. ALSO make `_persist_result` NULL-tolerant for caseless sweep arms (checker Blocker 1 / D-02): the payload currently does `"test_case_id": str(test_case_id)` — change it to persist `None` when `test_case_id` is falsy (`str(test_case_id) if test_case_id else None`), never the string "None" (mig 085 relaxed the NOT NULL; the FK stays enforced when non-NULL). Event emission (`_emit_eval(... test_case_id=...)`) may keep a synthetic display id for sweep arms — events are not FK-constrained. Extend `test_eval_runner.py` with tests asserting (a) a completed arm's persisted row carries a non-null integer duration_ms with the rollup measured_count unaffected, and (b) `_persist_result` with `test_case_id=None` inserts SQL NULL, not "None".
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_eval_runner.py -k "duration or null_case" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `_persist_result` accepts `duration_ms` and includes it in the insert payload.
    - The timer lives in a `finally` so timed_out/failed arms also persist duration_ms.
    - New test asserts non-null integer duration_ms on a completed arm; measured_count unchanged.
    - `grep -n "time.monotonic" backend/app/services/eval_runner_service.py` returns a match inside `_run_arm`.
    - `_persist_result` with `test_case_id=None` inserts SQL NULL (test-proven; `grep -n 'str(test_case_id) if test_case_id' eval_runner_service.py` or equivalent guard present).
  </acceptance_criteria>
  <done>Every arm persists a wall-clock duration_ms in the same insert; rollup math untouched; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Advisory case_feedback capture (EVAL-05d, one shot)</name>
  <files>backend/app/services/eval_runner_service.py, backend/tests/test_eval_runner.py</files>
  <behavior>
    - A graded arm whose verdict dict includes case_feedback persists that string (bounded length) in eval_results.case_feedback.
    - A not_measured or judge_error arm persists NULL case_feedback.
    - case_feedback never affects verdict_state, verdict_passed, or measured_count (advisory-only).
  </behavior>
  <read_first>
    - backend/app/services/eval_runner_service.py (:576-608 the judge branch — where overall_passed/summary are lifted; :797-799 rollup denominator)
    - backend/app/services/harness/validator_kinds.py (JudgeVerdict now has case_feedback — Plan 01)
    - backend/tests/test_eval_runner.py (how the judge/_judge_eval_answer result is mocked)
  </read_first>
  <action>
    In the graded branch of `_run_arm` (the `else` after the judge failure check, alongside `verdict_passed = bool(verdict.get("overall_passed"))`), lift `case_feedback = (verdict.get("case_feedback") or "")[:1000]` (bounded — advisory critique of the CASE). Default `case_feedback = None` at the top with the other verdict locals so not_measured/judge_error arms carry NULL. Add `case_feedback: str | None = None` as a keyword param to `_persist_result` and `"case_feedback": case_feedback` to the payload. Thread it into the persist call. Do NOT add a second judge shot (RESEARCH OQ3 — reuse the existing forced emission). Do NOT feed case_feedback into the rollup. Extend `test_eval_runner.py`: (a) a graded arm with a mocked verdict carrying case_feedback persists it; (b) a not_measured arm persists NULL case_feedback; (c) measured_count/passed_count are identical whether or not case_feedback is present.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_eval_runner.py -k "case_feedback" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The graded branch lifts `verdict.get("case_feedback")` (bounded to <=1000 chars) and no separate judge call is added (`grep -c "_judge_eval_answer" eval_runner_service.py` unchanged from baseline count).
    - `_persist_result` includes `case_feedback` in the payload; not_measured/judge_error persist NULL.
    - Test proves rollup denominator is unaffected by presence/absence of case_feedback.
  </acceptance_criteria>
  <done>case_feedback captured from the single forced emission, persisted advisory-only, rollup untouched, tests green.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_eval_runner.py -x` green.
- duration_ms + case_feedback in the `_persist_result` payload; both defaulted None.
- No new LLM call; rollup denominator unchanged.
</verification>

<success_criteria>
Every eval arm now records per-arm wall-clock and the judge's advisory per-case critique in the same insert, additive to the shipped runner, with the evidence-channel data-posture + 4KB cap formalized in this plan's threat model (EVAL-05h).
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-02-SUMMARY.md` when done.
</output>
