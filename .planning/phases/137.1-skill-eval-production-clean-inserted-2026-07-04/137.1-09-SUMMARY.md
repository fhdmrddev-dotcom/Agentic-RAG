---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 09
subsystem: ui
tags: [react, vitest, eval, skill-studio, aggregation, provider-parity, honesty-lock]

# Dependency graph
requires:
  - phase: 137.1-02
    provides: "mig 085 EvalResult.case_feedback + duration_ms persisted (advisory + metadata)"
  - phase: 137.1-07
    provides: "GET /skills/{id}/evals/aggregate endpoint + getEvalAggregate(skillId) + EvalAggregate/EvalConfigAgg types"
  - phase: 137.1-08
    provides: "MatrixCard grouped card + reused RunRow + determinate unit bar, with an explicit footer slot"
provides:
  - "RunHistory matrix-card aggregation FOOTER: per-config mean±σ grid + Δ skill-lift column + deterministic analyst-note rows, all rendered VERBATIM from the Plan 07 aggregate endpoint (no client stats)"
  - "RunCaseDetail inline violet '◇ Judge on this case' case_feedback advisory (never a verdict, never in the rollup) + per-arm ⏱ duration beside tokens"
affects: [137.1-verify, 137.1-secure, skill-eval-studio, EvalsTab]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Verbatim-server-render: stats + notes echoed from the server (with_mean/with_stddev/delta/analyst_notes) — the component computes no statistics (T-137.1-U2)"
    - "Advisory vocabulary: violet (accent-violet) = info/critique; green/red reserved for verdicts; case_feedback excluded from verdict + rollup (T-137.1-U3)"
    - "Additive component-fetch (useEffect + getEvalAggregate) gated to finalize + cancellation-guarded; failed fetch renders nothing"

key-files:
  created: []
  modified:
    - "frontend/src/components/skills/studio/RunHistory.tsx — MatrixFooter (mean±σ/Δ grid + verbatim analyst notes) wired into MatrixCard at finalize"
    - "frontend/src/components/skills/studio/RunHistory.test.tsx — api mock + 4 footer tests (1-run vs 2+-run σ, verbatim notes, no-footer-while-running)"
    - "frontend/src/components/skills/studio/RunCaseDetail.tsx — inline violet case_feedback advisory + ⏱ duration on the token line"
    - "frontend/src/components/skills/studio/RunCaseDetail.test.tsx — makeResult type fix + 3 advisory/duration tests"

key-decisions:
  - "Footer renders ONLY when no arm is running (aggregation lands at finalize — 058 design lock); mid-run shows the determinate bar, never aggregation"
  - "MatrixFooter fetches getEvalAggregate itself (useEffect) keyed on arms[0].skill_id — mirrors the sibling EngineHealthCard fetch-on-mount + test-mocks-api pattern; single-run rows stay purely presentational"
  - "case_feedback sourced from the first arm carrying a non-empty value (the judge annotates the case once), rendered once under the case header"
  - "Δ and mean use toFixed for DISPLAY formatting only — no Math/stddev/pstdev computation in the footer (with_stddev is a server-field READ, not a compute)"

patterns-established:
  - "Honesty lock preserved: server-authored notes + advisory case_feedback + duration=metadata, verdictBadge untouched"

requirements-completed: [EVAL-05]

# Metrics
duration: ~12 min
completed: 2026-07-04
---

# Phase 137.1 Plan 09: Studio Finalized-Display (058-A footer + 059-A advisory) Summary

**Matrix-card aggregation footer (per-config mean±σ / Δ skill-lift + deterministic analyst notes rendered verbatim from the Plan 07 endpoint) plus RunCaseDetail's inline violet "◇ Judge on this case" advisory and per-arm ⏱ duration — the last sketch-locked eval production-clean surfaces.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-07-04T20:26Z (approx)
- **Completed:** 2026-07-04T20:32:18Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- **058-A aggregation FOOTER (D-07/D-08):** `MatrixFooter` fetches `getEvalAggregate(skillId)` and renders, per config, a mean±σ grid (σ only when the server sends `with_stddev` at `run_count>=2`, otherwise "first run — no spread yet") + a Δ skill-lift column + the server's `analyst_notes` as tagged fixed-phrasing rows — ALL verbatim, zero client-side statistics. Renders at finalize only (no arm running); additive fetch failures render nothing.
- **059-A advisory (EVAL-05d/e):** a non-empty `case_feedback` renders inline under the case header as a violet dashed-left "◇ Judge on this case" block, captioned "feedback only — never blocks the run", visually distinct from PASS/FAIL and never in the rollup; `duration_ms` appends `· ⏱ {ms}ms` to the existing token line as metadata. `verdictBadge` untouched (honesty lock).
- Fixed a pre-existing type gap in `RunCaseDetail.test.tsx` (`makeResult` omitted the now-required `EvalResult.duration_ms`/`case_feedback`) — net −1 app typecheck error, zero new errors introduced.

## Task Commits

Each task was committed atomically:

1. **Task 1: Matrix card aggregation footer + analyst notes (058-A / D-07/D-08)** — `07d28348` (feat)
2. **Task 2: Inline violet case_feedback + ⏱ duration in RunCaseDetail (059-A / EVAL-05d/e)** — `a28dc99a` (feat)

## Files Created/Modified

- `frontend/src/components/skills/studio/RunHistory.tsx` — new `MatrixFooter` component (mean±σ/Δ grid + verbatim analyst-note rows off `getEvalAggregate`); wired into `MatrixCard`'s open block, gated on `!anyRunning && skillId`.
- `frontend/src/components/skills/studio/RunHistory.test.tsx` — `vi.mock("@/lib/api")` + safe `beforeEach` default; 4 new tests (1-run "no spread yet", 2+-run numeric σ + Δ, verbatim analyst notes, no-footer-while-running).
- `frontend/src/components/skills/studio/RunCaseDetail.tsx` — `caseFeedback` derivation + inline violet advisory block under the header; `⏱ duration_ms` appended to the token line.
- `frontend/src/components/skills/studio/RunCaseDetail.test.tsx` — `makeResult` now sets `duration_ms`/`case_feedback` (type fix); 3 new tests (advisory violet/no-verdict + verdict untouched, duration render, null-feedback no-block).

## Verification

- **`npx vitest run src/components/skills/studio/RunHistory.test.tsx`** → **16/16 passed** (12 existing + 4 new).
- **`npx vitest run src/components/skills/studio/RunCaseDetail.test.tsx`** → **10/10 passed** (7 existing + 3 new).
- **`npx vitest run src/components/skills/studio`** (plan-level) → **68/68 passed across 8 files**.
- **`npx tsc --noEmit`** (literal, as specified) → **exit 0**. Note: the root `tsconfig.json` is solution-style (`files: []` + references) so this command type-checks nothing. The MEANINGFUL app typecheck is `tsc -p tsconfig.app.json --noEmit` (what `tsc -b` / the `build` script runs).
- **`npx tsc -p tsconfig.app.json --noEmit`** → 31 errors, **all pre-existing SEED-056 rot in unrelated files** (IngestionPage, useMessages, ChatAreaMode, MessageSkeleton, SkillFormDialog, FilesSection, FilePreview, MemorySection, NavPanel, ChatLayoutLaunch, useDocuments, useFolders). All FOUR files I touched are clean (grep for `studio/RunHistory` and `studio/RunCaseDetail` returns empty). Net −1 vs baseline (32→31): I fixed the pre-existing `RunCaseDetail.test.tsx` type gap and added zero new errors.

## Acceptance Criteria

**Task 1:**
- Footer renders mean±σ + Δ per config from `getEvalAggregate`; σ shows only when the server provides it (else "first run — no spread yet") — proven by the 1-run vs 2+-run tests.
- No client stats: `grep -n "Math\.\|stddev\|pstdev" RunHistory.tsx` matches only (a) the pre-existing `unitProgress` progress-bar `Math.min`/`Math.round` (NOT the footer) and (b) `c.with_stddev` field READS in the footer (rendering the server value, `.toFixed` display-formatting) — no statistic is computed for the footer.
- Analyst notes render verbatim from the server payload — proven by the exact-string test.

**Task 2:**
- Non-empty `case_feedback` → violet dashed "◇ Judge on this case" advisory, captioned never-blocks, `outerHTML` matches `/accent-violet/` and NOT `/destructive|emerald|amber/`.
- `verdictBadge` unchanged (honesty lock); the advisory never alters the verdict/rollup — asserted PASS still reads PASS with the advisory present.
- `duration_ms` → `⏱` beside tokens; null duration renders nothing extra.

## Decisions Made

- **Finalize-only footer:** the aggregation footer is gated on `!anyRunning`, honoring the 058 lock "aggregation lands only at finalize" — a running matrix shows the determinate bar, never aggregation. Conveniently avoids fetching during the running-arm path.
- **Component-fetch over prop-drill:** `MatrixFooter` fetches `getEvalAggregate` itself (useEffect, cancellation-guarded) keyed on the arms' shared `skill_id`, matching the sibling `EngineHealthCard` pattern; the single-run RunRow path stays purely presentational and never fetches.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed a pre-existing EvalResult type gap in RunCaseDetail.test.tsx**
- **Found during:** Task 2 (updating the test's `makeResult`)
- **Issue:** `makeResult` was written in Phase 137 Plan 03, before mig-085 added the now-required `EvalResult.duration_ms` / `case_feedback` fields. It therefore returned an object missing two required properties — a latent `tsc -p tsconfig.app.json` error (TS2322 at line 46) that the no-op root `tsc --noEmit` never surfaced.
- **Fix:** Added `duration_ms: null` + `case_feedback: null` to the base `makeResult` (matching the already-correct `RunHistory.test.tsx` helper), so overrides can set them per-test.
- **Files modified:** frontend/src/components/skills/studio/RunCaseDetail.test.tsx
- **Verification:** `tsc -p tsconfig.app.json --noEmit` error count dropped 32→31; the `studio/RunCaseDetail` grep is now empty.
- **Committed in:** a28dc99a (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — a pre-existing type gap in a file this plan already owns).
**Impact on plan:** Necessary for a clean typecheck on the touched file; no scope creep — the file is in `files_modified` and the fix is confined to the test helper.

## Issues Encountered

- The literal verification command `npx tsc --noEmit` is a no-op here (root `tsconfig.json` is solution-style with `files: []` + `references`, and non-build-mode `tsc` does not traverse references). The meaningful typecheck is `tsc -p tsconfig.app.json --noEmit` / `tsc -b`. Recorded both; my four files are verified clean under the meaningful check.

## Known Stubs

None — the footer wires the real Plan 07 aggregate endpoint and the advisory/duration render real mig-085 fields. The empty-config footer state is an honest "no history yet" message (not a fake-data stub); the advisory renders only when real `case_feedback` is present.

## Threat Flags

None — no new security-relevant surface. The footer consumes the existing Plan 07 aggregate endpoint (owner-scoped server-side) and renders it verbatim; the advisory renders an existing mig-085 field. No new endpoints, auth paths, file access, or schema changes. The plan's threat register (T-137.1-U2 verbatim stats/no client compute; T-137.1-U3 violet advisory excluded from verdict/rollup) is exactly what this plan implements.

## Next Phase Readiness

- The sketch-locked eval production-clean display surfaces (058-A footer + 059-A advisory) are complete. Ready for `/gsd:verify-work 137.1` (SC#10 4-axis cross-provider UAT + G-4 lived-experience: watch a matrix run live, kill the backend mid-run).
- No blockers. Pre-existing SEED-056 frontend typecheck rot remains out of scope.

---
*Phase: 137.1-skill-eval-production-clean-inserted-2026-07-04*
*Completed: 2026-07-04*
