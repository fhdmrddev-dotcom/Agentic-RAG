---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 08
type: execute
wave: 4
depends_on: ["137.1-01", "137.1-04"]
files_modified:
  - frontend/src/components/skills/studio/RunBar.tsx
  - frontend/src/components/skills/studio/EvalsTab.tsx
  - frontend/src/components/skills/studio/RunHistory.tsx
  - frontend/src/components/skills/studio/RunBar.test.tsx
  - frontend/src/components/skills/studio/RunHistory.test.tsx
autonomous: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "EVAL-05b UI: a matrix run renders as ONE grouped collapsible card in RunHistory whose N sub-rows ARE the shipped 055-B run rows (058-A) — resting history never floods (a collapsed matrix = one row)"
    - "D-05 UI: exactly ONE sub-row carries the '▣ feeds gate' chip and the card header states the semantics once ('gate reads X only — other arms are analysis-only'); the launcher offers one-click all-configured + a gate-feeder select defaulting to the active provider"
    - "EVAL-05c: a running eval row shows a thin DETERMINATE unit bar (units = cases × 2 arms + 1 judge step) that scales to any case count — NO mid-run verdicts; verdicts land only at finalize"
    - "D-06 UI: RunBar disables while a matrix is live with the one-claim-per-skill note; N parallel arms are N existing per-run subscriptions (no bespoke matrix SSE)"
    - "RunBar stays a pure controlled component; EvalsTab owns the matrix launch (startMatrixRun) + the N-run subscribe wiring"
  artifacts:
    - path: "frontend/src/components/skills/studio/RunBar.tsx"
      provides: "matrix launcher ('Run matrix (N configured)') + gate-feeder select, controlled (no internal fetch)"
      contains: "matrix"
    - path: "frontend/src/components/skills/studio/EvalsTab.tsx"
      provides: "matrix launch call + N-run subscribe wiring + gate-stale refresh on matrix finalize"
      contains: "startMatrixRun"
    - path: "frontend/src/components/skills/studio/RunHistory.tsx"
      provides: "grouped matrix card (collapsible, 055-B sub-rows, one feeds-gate chip) + determinate unit bar on running rows"
      contains: "feeds gate"
  key_links:
    - from: "RunBar matrix launcher"
      to: "EvalsTab startMatrixRun (api.ts)"
      via: "onRunMatrix controlled callback"
      pattern: "startMatrixRun"
    - from: "RunHistory determinate bar"
      to: "the live liveByCase map (existing per-arm events)"
      via: "done-units / (case_count*2 + 1) — pure FE math, no backend event"
      pattern: "case_count"
---

<objective>
Ship the Studio matrix live experience (058-A launch + grouped card; 059-A determinate bar): a one-click "Run matrix (N configured)" launcher + gate-feeder select in RunBar, EvalsTab wiring the `startMatrixRun` call and N per-run subscriptions, and RunHistory rendering the matrix as ONE collapsible grouped card of shipped 055-B sub-rows with exactly one "▣ feeds gate" chip + a thin determinate unit bar on running rows. Every hard part reuses shipped machinery (the run-row grammar, the subscribe/heartbeat/re-attach) — the matrix is a group OF rows, never a new surface.

Purpose: Make the multi-provider run watchable live and legible without flooding history, honoring the operator-locked sketches 058-A/059-A.
Output: RunBar launcher, EvalsTab matrix wiring, RunHistory grouped card + determinate bar, tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/references/eval-production-clean.md

<interfaces>
<!-- Shipped seams to extend; sketch 058-A/059-A is the acceptance bar. -->
RunBar (RunBar.tsx:6-10, 38-48): a PURE controlled component — EvalsTab owns state; RunBar holds NO fetch. Props: providers/provider/model/models/onProviderChange/onModelChange/running/onRun. Add matrix affordance + gate-feeder select as new controlled props/callbacks.
EvalsTab (EvalsTab.tsx:143-217): owns the live status map keyed `${testCaseId}:${variant}` from onEvalCaseStarted/onEvalCaseDone/onEvalVerdict; subscribeToRun at :143; onEvalComplete loads the durable DB readout (:161-164); the gate-stale refresh path onGateStale/refreshGate (:208-217, 328-338 — matrix finalize must also trigger it, per 40e2f8a3).
RunHistory (RunHistory.tsx): 055-B expandable row grammar; rollupSummary (:55-75) + rollupTone (neutral never red); renderLive (:93-114) reads liveByCase with NO mid-run verdicts + "Verdicts land when the run finalizes"; interrupted banner (:117-143); provider logo via providerLogo (:31,206).
api.ts: startMatrixRun (Plan 01) returns MatrixRunKickoff (group_id + N EvalRunKickoff). Types EvalRun.matrix_group_id/feeds_gate (Plan 01).
Sketch locks (058-A/059-A): collapsed matrix = one row; ONE feeds-gate chip; header states gate semantics once; determinate unit bar = 3px gradient in a dim track + mono `case i/N · arm · u/U`; judge = ONE unit completing at eval_complete; NO segmented pips (die past ~5 cases).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server run readout → UI render | The UI must render server verdicts verbatim; no client-side pass/fail derivation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-04 (carried) | Tampering (fabricated state) | mid-run verdicts | mitigate | The determinate bar shows completion only; verdicts load from the durable DB readout at eval_complete (EvalsTab:161-164); no mid-run pass/fail is rendered. |
| T-137.1-U1 | Tampering | feeds-gate label integrity | mitigate | The '▣ feeds gate' chip renders off EvalRun.feeds_gate (a server flag) — a label, never a client-side gate recomputation. |
| T-137.1-SC | Tampering | npm installs | mitigate | No new packages (reuses providerLogo/@lobehub/icons already present). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: RunBar matrix launcher + gate-feeder select (058-A)</name>
  <files>frontend/src/components/skills/studio/RunBar.tsx, frontend/src/components/skills/studio/RunBar.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/studio/RunBar.tsx (the full controlled-component contract — props, disable-while-running)
    - .claude/skills/sketch-findings-agentic-rag/references/eval-production-clean.md (058-A launcher: one-click "⧉ Run matrix (N configured)" + inline gate-feeder select beside the UNCHANGED single-run RunBar; all-configured default)
    - frontend/src/components/skills/studio/RunBar.test.tsx (existing controlled-component test style)
  </read_first>
  <action>
    Extend RunBar (keeping it a dumb controlled component — NO fetch inside) with: a "⧉ Run matrix (N configured)" affordance beside the existing single-run controls, and an inline gate-feeder `<select>` bound to a new controlled `gateProvider` prop (default = the active provider) listing the configured providers. Add new controlled props: `configuredProviders`, `gateProvider`, `onGateProviderChange`, `onRunMatrix`, and reuse `running` to disable both single + matrix launch while any run is live (with the one-claim-per-skill note text). The single-run RunBar behavior stays UNCHANGED. Extend RunBar.test.tsx: the matrix button calls `onRunMatrix`; the gate-feeder select calls `onGateProviderChange`; both disable while `running`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/RunBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RunBar renders a matrix-launch affordance + a gate-feeder select; both are controlled (no fetch inside RunBar — `grep -n "fetch\|api\." RunBar.tsx` shows none added).
    - The matrix button invokes `onRunMatrix`; the select invokes `onGateProviderChange`; both disabled while `running`.
    - The single-run path is unchanged (existing RunBar tests still pass).
  </acceptance_criteria>
  <done>RunBar offers one-click all-configured matrix launch + gate-feeder select, controlled; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: EvalsTab matrix launch + N-run subscribe wiring + gate-stale refresh</name>
  <files>frontend/src/components/skills/studio/EvalsTab.tsx</files>
  <read_first>
    - frontend/src/components/skills/studio/EvalsTab.tsx (:143-217 the subscribe + live-map + onEvalComplete + gate-stale wiring)
    - frontend/src/lib/api.ts (startMatrixRun signature + MatrixRunKickoff shape — Plan 01)
  </read_first>
  <action>
    In EvalsTab, add the matrix launch handler: call `startMatrixRun(skillId, { gate_provider })` (the controlled gateProvider from RunBar), receive the `MatrixRunKickoff` (group_id + N EvalRunKickoff), and open ONE `subscribeToRun` per returned run_id (reuse the existing single-run subscribe machinery — N parallel subscriptions, no bespoke matrix SSE). Track the live status maps per run_id so RunHistory can render each arm's progress. On each arm's `onEvalComplete`, load its durable DB readout (existing path) and, when the group's gate-feeding arm finalizes, trigger the existing `onGateStale`/`refreshGate` path (per 40e2f8a3 — the matrix finalize must refresh the gate just like a single run). Pass the group_id + per-run state + gateProvider controls down to RunBar (launcher) and RunHistory (grouped card). Keep RunBar/RunHistory dumb; EvalsTab owns all state + fetches.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/EvalsTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EvalsTab calls `startMatrixRun` and opens one subscription per returned run_id (reusing subscribeToRun; `grep -n "subscribeToRun" EvalsTab.tsx` unchanged in mechanism).
    - Matrix arm finalize triggers the gate-stale refresh path (the 40e2f8a3 path is reused).
    - No bespoke matrix SSE is introduced.
  </acceptance_criteria>
  <done>EvalsTab launches the matrix, subscribes N runs, and refreshes the gate on the gate-feeder's finalize.</done>
</task>

<task type="auto">
  <name>Task 3: RunHistory grouped matrix card + determinate unit bar (058-A/059-A)</name>
  <files>frontend/src/components/skills/studio/RunHistory.tsx, frontend/src/components/skills/studio/RunHistory.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/studio/RunHistory.tsx (:55-75 rollupSummary/tone; :93-114 renderLive; :117-143 interrupted banner; :31,206 providerLogo)
    - .claude/skills/sketch-findings-agentic-rag/references/eval-production-clean.md (058-A matrix-card anatomy: `⧉ Matrix run · N configs · M cases` header → one gate-semantics line → 055-B sub-rows → [footer added in Plan 09]; 059-A determinate bar mechanics + "no mid-run verdicts")
    - frontend/src/components/skills/studio/RunHistory.test.tsx (existing row-grammar assertions)
  </read_first>
  <action>
    Group runs sharing a `matrix_group_id` into ONE collapsible matrix card whose sub-rows ARE the existing 055-B run rows (reuse the row renderer — do NOT fork it): a card header `⧉ Matrix run · N configs · M cases`, ONE gate-semantics line ("gate reads {gate provider} only — other arms are analysis-only and never flip the publish gate"), and exactly ONE sub-row carrying a "▣ feeds gate" chip (rendered off `EvalRun.feeds_gate`). A collapsed matrix occupies ONE resting row (058-A anti-flood). On the running rows, add a thin DETERMINATE unit bar per 059-A: `total = case_count * 2 + 1` (judge = one unit completing at eval_complete), `done = ` the count of finished units from the existing `liveByCase` map, rendered as a 3px gradient bar in a dim track + a mono `case i/N · arm · u/U · %` caption — scaling to ANY case count (NO segmented pips). Preserve the "Verdicts land when the run finalizes" line and the neutral-never-red rollup tone. Leave the aggregation FOOTER + analyst notes to Plan 09. Extend RunHistory.test.tsx: N runs with the same matrix_group_id collapse into one card with exactly one feeds-gate chip; the determinate bar math = done/(case_count*2+1); no mid-run verdict is shown.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/RunHistory.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Runs sharing a `matrix_group_id` render as ONE collapsible card; collapsed = one resting row (test-proven).
    - Exactly ONE sub-row shows the "▣ feeds gate" chip (off `feeds_gate`); the header states the gate semantics once.
    - Sub-rows reuse the shipped 055-B row renderer (not a fork).
    - The determinate bar total = `case_count*2+1` and increments off the existing live map; no mid-run pass/fail is rendered.
  </acceptance_criteria>
  <done>Matrix renders as one grouped card of 055-B sub-rows with one gate chip + a scaling determinate unit bar; tests green.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/components/skills/studio` green.
- Matrix = one collapsible card, one feeds-gate chip, gate semantics stated once.
- Determinate bar scales to any case count; no mid-run verdicts.
</verification>

<success_criteria>
A one-click matrix run fans across all configured providers and renders as a single grouped, collapsible card of shipped run-rows with one designated gate-feeder and a scaling determinate progress bar — watchable live, honest, and non-flooding, per sketches 058-A/059-A.
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-08-SUMMARY.md` when done.
</output>
