---
status: complete
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
source: [137.1-01..10-SUMMARY.md, 137.1-VALIDATION.md]
started: 2026-07-04T22:40:00Z
updated: 2026-07-05T00:15:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Settings page loads (the 500 regression fix)
expected: Opening Settings no longer throws "failed to fetch"/500. All tabs render; the AI Model tab shows the new Engine Health + Judge Model cards beneath the save cycle.
result: pass
note: "Verified live this session — root-caused to UserEffectiveSettings missing harness_judge_model (AttributeError at settings.py); fixed by the field + loader + migration 086; user confirmed Settings loads."

### 2. Cross-provider matrix run — grouped card, live, honest (U2 + U6 + U3)
expected: One-click "Run matrix (N configured)" fans the skill's eval across every configured provider as N parallel arms under ONE collapsible card with exactly one "▣ feeds gate" chip. Determinate progress bars advance while running with NO mid-run pass/fail; at finalize the aggregation footer (per-config mean±σ / Δ skill-lift + analyst notes) appears. Multi-tool skills grade on real judge evidence (the dd694916 tool-receipt flip).
result: pass
note: "Verified live this session — user ran an 8-provider matrix on the docx skill; grouped card + aggregation footer confirmed via screenshots; 6/8 green. Judge-evidence flip proven pre-session (dd694916: WITH PASS·72 / WITHOUT FAIL·40)."

### 3. Skill-lift readability + matrix-run cost confirm (this session's polish)
expected: On the matrix aggregation card, each Δ skill-lift shows a directional trend icon + colored number — green ↗ on a gain, red ↘ on a regression, neutral — flat at 0 (direction, sign, and color all agree). Clicking "Run matrix" first pops an in-app confirmation dialog (warns it runs all providers / costs tokens) with Cancel / Run matrix.
result: pass
note: "Built + shown this session (commits 179828a5, 1e896ebe); user confirmed the screenshot 'looks good'; the static Δ up-triangle replaced with a directional icon per user feedback."

### 4. Claude 4.6+/5 family works in evals — no prefill 400 (U11)
expected: A single-run or matrix eval on a Claude 4.6+/5 model (claude-sonnet-5 / opus-4-8) completes graded on BOTH arms — the without-skill arm no longer 400s on an assistant-prefill request shape.
result: pass
note: "Verified live this session — the 8-provider matrix flipped the Claude arms from the deterministic 5× prefill-400 (DB evidence through 2026-07-02) to working, after the adapter-boundary fix (config.py supports_assistant_prefill + open_anthropic_stream strip). Deep byte-identical (D-14 held)."

### 5. DeepSeek without-skill arm clean — no reasoning_content 400 (U10)
expected: A DeepSeek eval's without-skill arm completes (graded / not_measured), no request-shape 400.
result: pass
note: "Verified via DB evidence this session — eval_results clean from 2026-07-03 22:12 onward (root-caused to f47d6736 reset-before-each-arm); BUG-260630-01 closed with zero code change. Operator's live DeepSeek test corroborated."

### 6. Engine smoke-sweep health board (Settings) (U1)
expected: Settings → AI Model → Engine Health card → "Run sweep" produces one ✓/✗ tile per configured provider with verbatim errors on failures, an "N/total engines healthy" header, always-shown staleness, and a resolvable inline tile deep-link (click a tile → in-card run detail). A provider with no key = honest ✗ with its real auth error (correct, not a bug).
result: pass
note: "RE-VERIFIED live 2026-07-04 after fix 5f8bb2a5 — user confirmed 'pass'. The board no longer freezes on '0/8 healthy': tiles settle to their true ✓/✗ as each engine grades (button keeps spinning until all terminal) and the header climbs to the real healthy count (OpenRouter stays ✗ with its real 404, correct). Original issue (this session, now closed): onRunSweep cleared its poll the instant the spawn-and-return POST resolved, so nothing watched the arms grade 10-20s later → frozen neutral board. The 100/100 with==without observed earlier was NOT a bug — the ENGINE smoke sweep runs a trivial built-in synthetic case (answer 'PONG'), so both arms trivially pass; it proves the engine works end-to-end, not skill quality (ENGINE health ≠ model quality)."

### 7. Judge-model knob (Settings) (U9)
expected: Settings → AI Model → Judge Model picker offers ONLY registry-known models; the always-on 🔒 footer shows the EFFECTIVE judge (claude-opus-4-8 when unset — never blank). Changing it persists; a later eval + a publish-gauntlet run both use the chosen judge.
result: pass
note: "Verified live 2026-07-05 — user confirmed 'pass'. The Judge Model picker lists only registry-known models and the always-on 🔒 footer shows the effective judge (never blank / never a bare id). A changed pick persists across reload. D-11 single shared resolver (resolve_judge_model) means eval + publish-gauntlet both consume the same chosen judge."

### 8. case_feedback advisory reads as advisory, never a verdict (U8)
expected: Expanding a graded case in a run shows the judge's case_feedback as a violet "◇ Judge on this case" block captioned "feedback only — never blocks the run", visually distinct from PASS/FAIL, and the pass count is unchanged by its presence. Per-arm ⏱ duration shows beside tokens.
result: pass
note: "Verified via screenshot (Screenshot 2026-07-04 224123) — the engine-sweep detail rendered the violet '◇ Judge on this case: ... ' block captioned 'FEEDBACK ONLY — NEVER BLOCKS THE RUN', with WITH SKILL PASS·100.00 / WITHOUT SKILL PASS·100.00 badges unaffected (advisory separate from verdict). Per-arm ⏱ ms shown on the token line."

### 9. Kill-backend-mid-run reconciliation (U7 — G-4 lived-experience)
expected: Start a matrix/eval run; kill the backend (Ctrl-C) while it's running; restart. The boot reconciler flips the orphaned eval run → interrupted (chat runs → failed), drops the stale Redis stream, and Studio shows an honest interrupted banner + re-run — nothing counted, nothing fabricated. A fresh run started post-boot survives (not flipped).
result: pass
note: "Verified live 2026-07-05 (guided walk-through). User launched a 9-arm matrix on the docx skill, Ctrl-C'd uvicorn mid-stream (backend logs: KeyboardInterrupt during `GET /runs/6249f374-.../stream` — the arm was actively streaming), then restarted. RESULT (screenshot 00_12_52): the killed arm (gpt-5.4-mini) renders `interrupted` with the honest banner 'This run was interrupted (the backend restarted mid-run). No cases were measured — nothing was counted, nothing was fabricated' + Re-run. DB cross-check (psycopg2 :54322): eval_run 6249f374 status='interrupted'; the whole table holds EXACTLY 1 interrupted / 19 completed with ZERO rows stuck 'running' — the boot reconciler surgically flipped only the orphan (RC1 'never flip a live run'), and a fresh post-restart arm (deepseek-v4-flash) shows '1/1 passed' (live run untouched). RC1/RC2/RC3 confirmed lived, not just in code."

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "After a sweep, the engine-health board shows each completed+graded engine as green ✓ and the header reflects the true healthy count (not a frozen 0/N)."
  status: verified
  reason: "User reported 0/8 healthy with neutral tiles after the sweep; DB showed 7/8 arms completed+graded. Root cause: onRunSweep cleared its poll the instant the (non-blocking, spawn-and-return) POST resolved, so the board froze on the 'freshly running' snapshot."
  severity: major
  test: 6
  root_cause: "EngineHealthCard.onRunSweep cleared the running-poll in finally right after runEngineSweep() resolved; the route returns arms 'freshly running' and they grade 10-20s later with nothing polling."
  artifacts:
    - path: "frontend/src/components/settings/EngineHealthCard.tsx"
      issue: "poll cleared before arms grade → stale 0/N board"
  fix: "Poll until every tile is terminal (hasInFlight stop-condition, bounded by SWEEP_MAX_POLLS); refresh() returns the board. +3 predicate tests; settings suite 16/16."
  fix_commit: 5f8bb2a5
  debug_session: ""

## Coverage notes

- SC#10 4-axis: cross-provider (U1/U2 — all 8), multi-tool (U3, folded into Test 2), parallel-thread (U4 — exercised by proxy via the live 8-arm matrix's isolated per-arm streams; automated determinate-bar tests back it), long-message (U5 — determinate unit bar `cases×2+judge` proven by automated tests; live long-prompt run optional). U4/U5 not separately gated here — flag if the security/verify auditor requires explicit live rows.
- Automated layer green before UAT: backend phase suite 72 passed (test_eval_runner / test_evals_router / test_run_reconciler / test_eval_aggregation / test_settings / test_provider_gateway_seam); frontend studio 68 passed; frontend settings 13 passed.
