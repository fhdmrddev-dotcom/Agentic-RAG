---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/run_reconciler.py
  - backend/app/main.py
  - backend/tests/test_run_reconciler.py
autonomous: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "D-09 reconciliation runs once at boot in a standalone module mounted in the FastAPI lifespan, guarded by a single-shot Redis SET NX so WORKER_COUNT=2 never double-sweeps"
    - "D-10 sweep scope covers eval AND chat runs: a non-terminal eval run flips to 'interrupted', a non-terminal chat run flips to 'failed', and the stale run:{run_id} stream is dropped"
    - "The staleness predicate is non-terminal-in-Postgres AND absent-from-runs:active — a run present in the Redis runs:active set is NEVER flipped (Pitfall 6)"
    - "The reconciler writes DB statuses + drops streams only — it is additive, NOT in threads.py (G-5) and touches no agent-loop code (D-14)"
    - "EVAL-05g: the BUG-260702-02 restart-orphan case is closed — a run left 'running' by a backend restart is honestly terminalized on next boot"
  artifacts:
    - path: "backend/app/services/run_reconciler.py"
      provides: "reconcile_orphaned_runs(*, pool, redis, supabase) -> int — CAS-guarded boot sweep mirroring resume_stranded_workflows"
      contains: "reconcile_orphaned_runs"
    - path: "backend/app/main.py"
      provides: "lifespan mount: asyncio.create_task(_reconcile_orphans()) alongside _resume_stranded, best-effort"
      contains: "reconcile_orphaned_runs"
    - path: "backend/tests/test_run_reconciler.py"
      provides: "unit tests: eval→interrupted, chat→failed, live-run-skipped, single-shot guard"
      contains: "runs:active"
  key_links:
    - from: "backend/app/main.py lifespan"
      to: "run_reconciler.reconcile_orphaned_runs"
      via: "asyncio.create_task best-effort background task after pg pool + redis are up"
      pattern: "reconcile_orphaned_runs"
    - from: "run_reconciler"
      to: "Redis runs:active sorted set"
      via: "authoritative liveness cross-check before any flip"
      pattern: "runs:active"
---

<objective>
Close the BUG-260702-02 restart-orphan gap (EVAL-05g): when a backend restart leaves runs stuck non-terminal, a boot-time sweep terminalizes them honestly. A NEW standalone `run_reconciler.py` module — a near-copy of the shipped `resume_stranded_workflows` — flips non-terminal-and-not-live eval runs to `interrupted`, chat runs to `failed`, and drops their stale Redis streams, guarded by a single-shot CAS so multi-worker boot never double-sweeps.

Purpose: The display half already shipped in 137 (interrupted banner + re-run); this ships the writer half so orphans are never silently `running` forever.
Output: `run_reconciler.py`, the `main.py` lifespan mount, and unit tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-PATTERNS.md
@.planning/reported-bugs/BUG-260702-02-in-flight-runs-orphaned-on-backend-restart-no-reconciliation.md

<interfaces>
<!-- Exact precedent to mirror — the reconciler is NOT novel. -->
resume_stranded_workflows (backend/app/services/harness_engine.py:1672-1780): the CAS-guarded, lifespan-spawned sweep pattern (claim_run → act → skip-if-claimed). Copy its shape.
main.py lifespan (:257-269): the _resume_stranded best-effort background-task block — copy verbatim as _reconcile_orphans, placed alongside it, AFTER the pg pool + redis are up, before `yield`.
Redis run-buffer keys (CLAUDE.md): run:{run_id} = per-run stream; runs_by_thread:{thread_id}; runs:active = sorted set of currently-streaming run_ids (AUTHORITATIVE liveness).
db/runs.finalize_run (backend/app/db/runs.py:69): companion runs-row status writer; the runs status CHECK is streaming/completed/failed/cancelled/timed_out (db/runs.py:41) → chat orphans go to 'failed'.
eval_runs status: use the supabase eval_runs UPDATE for eval orphans → 'interrupted'.
The 2026-07-02 manual cleanup this automates: eval→interrupted, chat→failed, drop stale run:* streams.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot process → run status writes | The reconciler mutates run/eval_run statuses at startup based on Redis liveness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.1-RC1 | Tampering (data integrity) | reconciler flipping a live run | mitigate | Predicate = non-terminal in Postgres AND absent from `runs:active` (the authoritative streaming set); a run present in runs:active is skipped (Pitfall 6). Sweep runs once at boot before new runs start. |
| T-137.1-RC2 | DoS (double-sweep across workers) | WORKER_COUNT=2 | mitigate | Single-shot Redis `SET NX` guard key (e.g. `run_reconcile_lock`) so exactly one worker performs the sweep; the racing sibling no-ops. |
| T-137.1-RC3 | DoS (startup blocked) | slow sweep | mitigate | Spawned as a best-effort `asyncio.create_task`; failure logs and the app continues (mirrors _resume_stranded). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_reconciler.py — CAS-guarded boot orphan sweep</name>
  <files>backend/app/services/run_reconciler.py, backend/tests/test_run_reconciler.py</files>
  <behavior>
    - A non-terminal eval_runs row (status 'running') whose run_id is absent from runs:active → flipped to 'interrupted'; its run:{run_id} stream dropped.
    - A non-terminal chat run (companion runs row status 'streaming'/'cap_paused') absent from runs:active → flipped to 'failed'; stream dropped.
    - A run PRESENT in runs:active → skipped (never flipped).
    - The SET NX guard: if a sibling worker holds the lock, reconcile_orphaned_runs returns 0 immediately (no double-sweep).
    - Returns the count of runs terminalized.
  </behavior>
  <read_first>
    - backend/app/services/harness_engine.py (:1672-1780 resume_stranded_workflows — the CAS + sweep shape to mirror; its __all__ export at :1823)
    - backend/app/db/runs.py (:41 the status CHECK values; :69 finalize_run signature)
    - .planning/reported-bugs/BUG-260702-02-in-flight-runs-orphaned-on-backend-restart-no-reconciliation.md (the exact orphan symptom + the manual-cleanup spec this automates)
    - CLAUDE.md run-buffer key conventions (run:{run_id}, runs:active)
  </read_first>
  <action>
    Create `backend/app/services/run_reconciler.py` exporting `async def reconcile_orphaned_runs(*, pool, redis, supabase) -> int`. First, acquire a single-shot guard: `await redis.set("run_reconcile_lock", "1", nx=True, ex=<a few minutes>)`; if not acquired, return 0. Then: (a) query eval_runs for `status = 'running'`; for each whose `id` is NOT a member of the `runs:active` sorted set (use `zscore`/`zrank`), UPDATE eval_runs status → `'interrupted'` (service-role supabase, wrapped in `run_in_threadpool` per D-v2.5-01) and delete the `run:{id}` Redis stream. (b) query the companion `public.runs` (via `pool`) for non-terminal statuses (`streaming`, `cap_paused` if present) absent from runs:active; finalize each → `'failed'` via `db/runs.finalize_run` and drop its stream. Also remove terminalized ids from `runs:active`. Count and return the total flipped. Best-effort per-row try/except so one bad row never aborts the sweep. Do NOT import or call agent-loop code (D-14); do NOT put any of this in threads.py (G-5). Add `__all__ = ["reconcile_orphaned_runs"]`. Write `test_run_reconciler.py` mocking redis (runs:active membership) + pool + supabase to assert: eval→interrupted, chat→failed, live-run-skipped, and the SET NX guard returning 0 when the lock is held.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_run_reconciler.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `reconcile_orphaned_runs(*, pool, redis, supabase)` exists and is exported in `__all__`.
    - Test: a 'running' eval run absent from runs:active → asserted UPDATE to 'interrupted'.
    - Test: a non-terminal chat run absent from runs:active → asserted finalize to 'failed'.
    - Test: a run present in runs:active is NOT flipped.
    - Test: when the SET NX guard is already held, the fn returns 0 and performs no flips.
    - `grep -n "agent_loop" backend/app/services/run_reconciler.py` returns nothing (D-14 boundary).
  </acceptance_criteria>
  <done>Reconciler module + tests green; live runs protected; single-shot guarded.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the reconciler in the FastAPI lifespan</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py (:250-291 the lifespan block — _resume_stranded at :257-269 and _sweep_expired_templates at :279-291 are the exact best-effort background-task patterns to copy)
    - backend/app/services/run_reconciler.py (the fn signature from Task 1)
  </read_first>
  <action>
    In the lifespan, immediately after the `_resume_stranded` `asyncio.create_task(...)` (and after the pg pool + redis are confirmed up), add an inner `async def _reconcile_orphans():` that function-locally imports `reconcile_orphaned_runs` + `get_redis` + `get_supabase`, calls it with `pool=await get_pg_pool(), redis=get_redis(), supabase=get_supabase()`, logs `"Run reconciler closed %d orphaned run(s)"` when count>0, and wraps the whole body in try/except that logs `"Run reconciler failed (app continues)"` and continues. Spawn it with `asyncio.create_task(_reconcile_orphans())`. Mirror the _resume_stranded structure exactly. Do NOT block startup on it.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast,sys; src=open('app/main.py').read(); assert 'reconcile_orphaned_runs' in src and '_reconcile_orphans' in src; ast.parse(src); print('OK mounted + parses')"</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` contains `_reconcile_orphans` spawned via `asyncio.create_task`, placed after `_resume_stranded`.
    - The body is wrapped in try/except that logs and continues (best-effort).
    - `app/main.py` parses (ast.parse) and the backend imports cleanly.
  </acceptance_criteria>
  <done>Reconciler runs once per boot as a best-effort background task; startup never blocked.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_run_reconciler.py -x` green.
- Reconciler skips runs present in runs:active; flips eval→interrupted, chat→failed.
- Mounted in lifespan best-effort; no agent-loop/threads.py touch.
</verification>

<success_criteria>
A backend restart no longer strands runs: on next boot the reconciler honestly terminalizes orphaned non-terminal runs (eval→interrupted, chat→failed) and drops their stale streams, once, worker-safe — closing BUG-260702-02.
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-03-SUMMARY.md` when done.
</output>
