---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 10
type: execute
wave: 4
depends_on: ["137.1-01", "137.1-04", "137.1-05"]
files_modified:
  - frontend/src/components/settings/EngineHealthCard.tsx
  - frontend/src/components/settings/JudgeModelPicker.tsx
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/components/settings/EngineHealthCard.test.tsx
  - frontend/src/components/settings/JudgeModelPicker.test.tsx
autonomous: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "D-03 the Engine-health card is a Settings tile board: one compact ✓/✗ tile per CONFIGURED provider (logo + representative model) with the last-sweep result + a 'Run sweep' button; a failing tile shows the VERBATIM provider error (never an engine-shaped error); each tile links to its smoke run — the link RESOLVES: tile click opens an inline expandable run-detail section within the card (fed by the Plan 04 skill-less readout via getEvalRunById, rendered with the reused RunCaseDetail leaf), because a NULL-skill sweep run cannot mount in the skill-scoped Studio panel"
    - "D-04 the board is on-demand only with staleness ALWAYS shown (amber when old → green 'just now' after a sweep); no scheduler, no config-change nudge"
    - "D-01/D-02 UI: 'Run sweep' POSTs the engine smoke sweep (a matrix run over the hidden built-in smoke case); the subtitle carries the load-bearing semantics ENGINE health ≠ model quality — a model may honestly fail the smoke case and still be a healthy tile"
    - "D-11/D-12 the judge-model knob sits beneath as its own card using the 024-A picker pattern, offering ONLY registry-known models, with an always-on 🔒 footer showing the EFFECTIVE judge (claude-opus-4-8 when unset) and its double duty (eval verdicts + publish-gauntlet judge)"
  artifacts:
    - path: "frontend/src/components/settings/EngineHealthCard.tsx"
      provides: "per-provider ✓/✗ tile board + staleness header + Run sweep, self-fetch/reconcile-on-fetch/light-poll; tile click -> inline expandable sweep-run detail (getEvalRunById + reused RunCaseDetail)"
      contains: "Run sweep"
    - path: "frontend/src/components/settings/JudgeModelPicker.tsx"
      provides: "024-A registry-only judge picker + always-on 🔒 effective-judge footer"
      contains: "harness_judge_model"
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "mounts EngineHealthCard + JudgeModelPicker"
      contains: "EngineHealthCard"
  key_links:
    - from: "EngineHealthCard Run sweep"
      to: "runEngineSweep + getEngineHealth (api.ts / Plan 04)"
      via: "POST sweep → poll board; tiles deep-link to run detail"
      pattern: "runEngineSweep|getEngineHealth"
    - from: "EngineHealthCard tile click"
      to: "getEvalRunById (api.ts / Plan 04 skill-less readout) + RunCaseDetail (reused presentational leaf)"
      via: "inline expandable run-detail section within the card — the resolvable D-03/060-A deep-link destination"
      pattern: "getEvalRunById|RunCaseDetail"
    - from: "JudgeModelPicker"
      to: "getJudgeModel/setJudgeModel (api.ts / Plan 05 settings API)"
      via: "registry-only options + effective-default footer"
      pattern: "harness_judge_model"
---

<objective>
Ship the Settings surface (060-A): the Engine-health tile board that answers the operator's verbatim trust bar ("how do I know it reflects reality for each model without hand-running all 8") and the judge-model knob beneath it. The board is per-provider ✓/✗ tiles with staleness always shown + a "Run sweep" button (POSTing the Plan 04 smoke sweep); a failing tile shows the verbatim provider error. The judge picker reuses the 024-A registry-only picker with an always-on 🔒 effective-judge footer.

Purpose: Per-provider, honest, timestamped engine health in one saccade + a judge knob for single-provider/local orgs.
Output: EngineHealthCard + JudgeModelPicker mounted on the Settings page + tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.claude/skills/sketch-findings-agentic-rag/references/eval-production-clean.md
@.claude/skills/sketch-findings-agentic-rag/references/settings-provider-picker-and-reembed.md

<interfaces>
<!-- Analogs to copy + the sketch locks (060-A / 024-A). -->
ReembedStatusCard.tsx (:42-76): the self-fetch + reconcile-on-fetch (keep last-good on failed fetch) + light-poll-while-running + re-kick shape → the Engine-health board's fetch/poll/Run-sweep skeleton.
ProviderPicker.tsx (:184-222) + ModelPillRow.tsx: the 024-A preset `<select>` + always-on 🔒 endpoint footer idiom → the judge picker (one select bound to harness_judge_model + an always-visible effective-judge footer).
providerLogo (frontend/src/lib/providerLogo.ts, @lobehub/icons): per-provider marks on tiles (single-source icon convention).
api.ts (Plan 01): getEngineHealth() → EngineHealthBoard (tiles: provider, model, healthy, error|null, run_id, last_swept_at); runEngineSweep(); getEvalRunById(runId) → the get_eval_run-shaped run+results readout (the Plan 04 SKILL-LESS route — a sweep run has skill_id NULL and can never match the /skills/{skill_id}/... readout); getJudgeModel()/setJudgeModel() (mirroring the skill_builder_model client wiring).
RunCaseDetail (frontend/src/components/skills/studio/RunCaseDetail.tsx): a PURE presentational leaf (no fetch, no EvalsTab dependency) — reusable outside the Studio; the tile detail view renders each sweep arm's results with it.
Tile-link destination (checker iteration-2 resolution, preserves the 060-A lock without a sketch amendment): tile click expands an INLINE run-detail section within the engine-health card (below the tile grid, one tile expanded at a time) — NOT a navigation into the skill Studio (a NULL-skill sweep run has no Studio home).
SettingsPage.tsx: hosts ReembedStatusCard + ProviderPicker today — mount the two new cards here.
Sketch locks (060-A): N logo tiles ✓/✗ + representative model; a failing tile tints red with the VERBATIM provider error demoted below the grid (never engine-shaped); header "N/8 engines healthy" + staleness ALWAYS shown (amber when old → green just-now) + "Run sweep"; subtitle "ENGINE health ≠ model quality"; ≈24 LLM calls, no user data; tiles link to the smoke run in the Studio. Judge knob (024-A): registry-only options, always-on 🔒 footer showing the EFFECTIVE judge (claude-opus-4-8 when unset — never blank) + double duty (eval + publish judge). UI-only — no migration.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → judge-model write | The judge picker writes the shared harness_judge_model. |
| provider error → tile render | A failing tile surfaces a provider error string verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.1-S1 | Tampering | judge picker options | mitigate | Offers ONLY registry-known models (D-12), backed by the Plan 05 server-side registry validation (400 on unknown); the effective default is always shown so unset never reads as blank/misconfigured. |
| T-137.1-S2 | Info disclosure | verbatim provider error on a tile | mitigate | The error is the provider's own message (already surfaced honestly by the engine); tiles carry NO user data (smoke fixture, D-02); no secrets are echoed (provider gateway redaction precedent). |
| T-137.1-S3 | Tampering (fabricated health) | engine ≠ quality | mitigate | The subtitle states ENGINE health ≠ model quality; a model that honestly fails the smoke case but completes is a healthy tile — no fabricated pass/fail. |
| T-137.1-SC | Tampering | npm installs | mitigate | No new packages (reuses providerLogo/@lobehub/icons). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: EngineHealthCard — per-provider ✓/✗ tile board + Run sweep + staleness (D-01/D-02/D-03/D-04)</name>
  <files>frontend/src/components/settings/EngineHealthCard.tsx, frontend/src/components/settings/EngineHealthCard.test.tsx</files>
  <read_first>
    - frontend/src/components/settings/ReembedStatusCard.tsx (:42-76 the self-fetch/reconcile/light-poll/re-kick skeleton to copy)
    - frontend/src/lib/providerLogo.ts (the single-source provider marks)
    - frontend/src/lib/api.ts (getEngineHealth / runEngineSweep / getEvalRunById — Plan 01/04)
    - frontend/src/components/skills/studio/RunCaseDetail.tsx (the pure presentational leaf the tile detail view reuses — props shape, verdict/duration/case_feedback rendering)
    - .claude/skills/sketch-findings-agentic-rag/references/eval-production-clean.md (060-A tile-board anatomy + the exact honesty copy)
  </read_first>
  <action>
    Create `EngineHealthCard.tsx` mirroring the ReembedStatusCard fetch/poll skeleton: on mount `getEngineHealth()`; render one compact tile per CONFIGURED provider — `providerLogo` + provider name + representative model + a ✓/✗ health mark. A ✗ tile tints red and its VERBATIM provider error is demoted below the grid (never an engine-shaped error). Header: "N/{total} engines healthy" + staleness ALWAYS shown (amber when the last sweep is old → green "just now" right after a sweep — derive from `last_swept_at`) + a "Run sweep" button that POSTs `runEngineSweep()` then polls `getEngineHealth()` until the sweep finalizes (light poll while running, like the re-embed card). Subtitle carries the load-bearing semantics VERBATIM: "ENGINE health ≠ model quality — a model may honestly fail the smoke case and still be a healthy engine"; note ≈24 LLM calls, no user data. Tile click = the resolvable deep-link (checker iteration-2): clicking a tile expands an INLINE run-detail section within the card, directly below the tile grid (one tile expanded at a time; click again to collapse) — it fetches `getEvalRunById(tile.run_id)` (the Plan 04 skill-less readout; a NULL-skill sweep run can never match the skill-scoped readout) and renders the sweep run's per-arm results by REUSING `RunCaseDetail` (a pure presentational leaf — do NOT fork it, do NOT navigate into the skill Studio). A failed detail fetch renders an honest inline error, never a dead link. Keep last-good board on a failed fetch. Write EngineHealthCard.test.tsx: renders one tile per configured provider; a ✗ tile shows the verbatim error; staleness renders amber-when-old / green-after-sweep; "Run sweep" calls runEngineSweep then re-polls; the "ENGINE health ≠ model quality" subtitle is present; tile click calls getEvalRunById(run_id) and renders the expanded inline detail (RunCaseDetail content visible) — the deep-link RESOLVES to a rendered view, not just a run_id.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/settings/EngineHealthCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Renders one ✓/✗ tile per configured provider with logo + representative model; a ✗ tile shows the verbatim provider error below the grid.
    - Staleness is always shown (amber when old, green just-now post-sweep); "Run sweep" POSTs runEngineSweep then re-polls the board.
    - The "ENGINE health ≠ model quality" subtitle is rendered.
    - Tile click RESOLVES: an inline expandable detail section opens within the card, fed by getEvalRunById (the skill-less readout) and rendering per-arm results via the reused RunCaseDetail — test-proven (mock getEvalRunById; assert the detail content renders), never a dead run_id.
    - No scheduler/nudge logic exists (on-demand only) — `grep -n "setInterval" EngineHealthCard.tsx` shows only the running-poll, not a background scheduler.
  </acceptance_criteria>
  <done>Engine-health tile board renders honest per-provider health with staleness + Run sweep, on-demand; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: JudgeModelPicker (024-A registry-only + 🔒 effective-judge footer) + mount both cards (D-11/D-12)</name>
  <files>frontend/src/components/settings/JudgeModelPicker.tsx, frontend/src/pages/SettingsPage.tsx, frontend/src/components/settings/JudgeModelPicker.test.tsx</files>
  <read_first>
    - frontend/src/components/settings/ProviderPicker.tsx (:184-222 the preset-select + always-on 🔒 footer idiom to mirror) + ModelPillRow.tsx
    - frontend/src/lib/api.ts (getJudgeModel/setJudgeModel — Plan 01 wiring over the Plan 05 settings API)
    - frontend/src/pages/SettingsPage.tsx (where ReembedStatusCard + ProviderPicker mount — add the two new cards here)
    - .claude/skills/sketch-findings-agentic-rag/references/settings-provider-picker-and-reembed.md (024-A: never pick a model without seeing where it runs; the always-on footer)
  </read_first>
  <action>
    Create `JudgeModelPicker.tsx` reusing the 024-A picker idiom: one `<select>` bound to `harness_judge_model` offering ONLY registry-known models (from the registry model list; the Plan 05 server validates on write). An always-on 🔒 footer shows the EFFECTIVE judge — `resolved_harness_judge_model` (claude-opus-4-8 when unset) — never a blank that implies "configured", plus a one-line note of its double duty (eval verdicts + the publish-gauntlet judge, D-11). Wire it through `getJudgeModel()`/`setJudgeModel()`. Mount BOTH `EngineHealthCard` and `JudgeModelPicker` on `SettingsPage.tsx` (the judge knob sits beneath the engine board, per 060-A). Write JudgeModelPicker.test.tsx: the select lists only registry models; selecting one calls setJudgeModel; the footer shows the effective judge when unset (claude-opus-4-8) and never renders blank.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/settings/JudgeModelPicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The judge `<select>` lists ONLY registry-known models; selecting one calls `setJudgeModel`.
    - The always-on 🔒 footer shows the effective judge (claude-opus-4-8 when unset), never blank, and notes the double duty.
    - `SettingsPage.tsx` mounts both `EngineHealthCard` and `JudgeModelPicker`.
  </acceptance_criteria>
  <done>Registry-only judge picker with an effective-default footer, mounted beneath the engine board on the Settings page; tests green.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/components/settings` green.
- Engine board: per-provider ✓/✗, verbatim error, staleness always shown, Run sweep, ENGINE≠quality subtitle.
- Judge picker: registry-only, effective-default footer, mounted with the board.
</verification>

<success_criteria>
Settings answers "how do I know it reflects reality for each model without hand-running all 8" with a per-provider engine-health tile board (honest, timestamped, on-demand) and a registry-validated judge-model knob with an always-visible effective judge — per sketch 060-A.
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-10-SUMMARY.md` when done.
</output>
