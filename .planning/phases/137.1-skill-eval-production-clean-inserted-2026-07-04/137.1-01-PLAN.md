---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/085_eval_matrix_duration_case_feedback.sql
  - supabase/full-schema.sql
  - backend/app/services/harness/validator_kinds.py
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
autonomous: false
requirements: [EVAL-05]
must_haves:
  truths:
    - "D-05 gate-feeder flag exists as a persisted column (eval_runs.feeds_gate boolean NOT NULL DEFAULT false) so exactly one arm per group can be marked gate-feeding"
    - "D-06 matrix grouping exists as a persisted column (eval_runs.matrix_group_id uuid, NULL for single runs) so N arms share one group identity"
    - "D-07 aggregation has the per-arm inputs it needs: eval_results.duration_ms + the provider/model already present enable mean±stddev/delta over run history"
    - "EVAL-05d: JudgeVerdict carries case_feedback as a schema-bound required field (no default) so it is OpenAI-strict-safe on all providers and can never be regex-parsed from prose"
    - "EVAL-05e: eval_results.duration_ms column exists to persist per-arm wall-clock alongside the existing token columns"
    - "The migration is additive and NULL-backfill-safe: every pre-085 eval_runs/eval_results row is valid unchanged"
  artifacts:
    - path: "supabase/migrations/085_eval_matrix_duration_case_feedback.sql"
      provides: "additive columns matrix_group_id+feeds_gate on eval_runs, duration_ms+case_feedback on eval_results, plus index on matrix_group_id"
      contains: "ALTER TABLE public.eval_runs"
    - path: "backend/app/services/harness/validator_kinds.py"
      provides: "JudgeVerdict.case_feedback field (required, no default)"
      contains: "case_feedback"
    - path: "frontend/src/types/index.ts"
      provides: "EvalRun.matrix_group_id/feeds_gate + EvalResult.duration_ms/case_feedback + matrix/sweep/aggregate/judge api types"
      contains: "matrix_group_id"
    - path: "frontend/src/lib/api.ts"
      provides: "client fns: startMatrixRun, runEngineSweep, getEvalAggregate, getEngineHealth, judge-model get/set"
      contains: "startMatrixRun"
  key_links:
    - from: "backend/app/services/eval_runner_service.py"
      to: "eval_results.duration_ms / case_feedback columns"
      via: "_persist_result insert (Plan 02 wires the values)"
      pattern: "duration_ms|case_feedback"
    - from: "frontend/src/components/skills/studio/RunHistory.tsx"
      to: "EvalRun.matrix_group_id / feeds_gate"
      via: "typed readout (Plans 08/09 consume)"
      pattern: "matrix_group_id|feeds_gate"
---

<objective>
Establish the data + interface contracts the whole phase builds on: ONE additive migration (085) adding matrix + duration + case_feedback columns, the `case_feedback` schema field on the shared `JudgeVerdict`, the TypeScript type additions, and the frontend api-client function signatures. Interface-first: every downstream plan (matrix routes, runner, aggregation, Studio UI, Settings) consumes these contracts without re-deriving them.

Purpose: Remove the "scavenger hunt" — downstream executors receive the exact column names, the strict-safe verdict field, and the client fn shapes here.
Output: migration 085 applied live to the local DB, `full-schema.sql` regenerated, `JudgeVerdict.case_feedback` field, TS types, api.ts client fns.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-RESEARCH.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor extends. Do NOT explore the codebase for these — they are here. -->

Migration precedent (copy the header + additive ALTER style): supabase/migrations/081_eval_verdict_and_ratings.sql
 - eval_results already has: verdict_state, verdict_passed, verdict_score, verdict_reason, judge_model, input_tokens, output_tokens.
 - eval_runs already has: passed_count, measured_count, verdict_summary, status, provider, model, case_count, skill_id, skill_version_id.

JudgeVerdict (backend/app/services/harness/validator_kinds.py:103-113) — flat, ConfigDict(extra="forbid"):
 overall_passed:bool, overall_score:int, grounded_in_evidence:bool, answers_business_requirement:bool,
 did_the_work_not_delegated:bool, criteria:list[JudgeCriterionVerdict], summary:str
 FINDING-04 lock (validator_kinds.py:94-100): OpenAI strict mode requires EVERY property in `required`; a field with a default is dropped from `required` → 400. New fields MUST have NO default.

EvalRun / EvalResult TS types (frontend/src/types/index.ts:583-633) — current shape read; add the new optional columns.

api.ts skill_builder_model wiring is the precedent for the settings judge-model client fns.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator SQL editor → local DB | The migration is applied by a human pasting SQL; the executor authors it, the operator runs it. |
| downstream judge emission → JudgeVerdict schema | case_feedback becomes a judge-authored string that later renders in the UI — must be a schema-bound field, never parsed from prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.1-01 | Tampering | migration 085 columns | mitigate | Additive-only ALTER TABLE ADD COLUMN; NULL-safe backfill; no data migration; owner-scoping unchanged (service-role writes + app-code `.eq("user_id")` remains the gate — no new RLS write policy). |
| T-137.1-02 | Tampering | JudgeVerdict.case_feedback | mitigate | Declared as a required schema field with NO default (FINDING-04) so it is strict-safe on OpenAI and can never be gamed by a narrated/truncated verdict; it is advisory data, never a gate input (verified downstream). |
| T-137.1-SC | Tampering | npm/pip installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit: none). No install task; supply-chain surface unchanged. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 085 (matrix + duration + case_feedback columns)</name>
  <files>supabase/migrations/085_eval_matrix_duration_case_feedback.sql</files>
  <read_first>
    - supabase/migrations/081_eval_verdict_and_ratings.sql (header discipline + additive ALTER style + COMMENT ON COLUMN convention to copy verbatim)
    - supabase/migrations/080_eval_runs_and_results.sql (eval_runs/eval_results base shape + existing indexes)
    - .planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-PATTERNS.md (the "085 shape (planner-designed)" block)
  </read_first>
  <action>
    Create the migration file with additive-only statements: on `public.eval_runs` add `matrix_group_id uuid` (nullable — NULL for single runs; pre-085 rows backfill NULL, which is correct because they were never matrix runs) and `feeds_gate boolean NOT NULL DEFAULT false` (exactly one TRUE per matrix group; single runs get false and their gate read is unchanged). On `public.eval_results` add `duration_ms integer` (nullable — NULL on pre-085 rows; per-arm wall-clock, EVAL-05e) and `case_feedback text` (nullable — advisory judge critique of the CASE, EVAL-05d, never in rollup math). Add `CREATE INDEX idx_eval_runs_matrix_group_id ON public.eval_runs (matrix_group_id);`. Copy the 081 header block verbatim in intent: the "apply via Supabase SQL editor OR psycopg2 at 127.0.0.1:54322, then regenerate full-schema.sql with NO --reset, commit both, NEVER db push/reset" discipline, and the ownership note that service-role writes + the app-code `.eq("user_id")` filter remain the gate (no new RLS write policy). Add COMMENT ON COLUMN for all four columns explaining semantics (feeds_gate = exactly-one-per-group gate-feeder D-05; matrix_group_id = shared group id D-06; duration_ms = per-arm wall-clock; case_feedback = advisory, never a verdict, never in rollup). Do NOT create a separate eval_matrix_groups table (RESEARCH OQ2 chose nullable columns).
  </action>
  <verify>
    <automated>grep -v '^--' "supabase/migrations/085_eval_matrix_duration_case_feedback.sql" | grep -Ec "matrix_group_id|feeds_gate|duration_ms|case_feedback|idx_eval_runs_matrix_group_id" | grep -q 5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/085_eval_matrix_duration_case_feedback.sql` exists and matches the `^[0-9]+_name.sql` convention.
    - Contains `ALTER TABLE public.eval_runs` adding both `matrix_group_id uuid` and `feeds_gate boolean NOT NULL DEFAULT false`.
    - Contains `ALTER TABLE public.eval_results` adding both `duration_ms integer` and `case_feedback text`.
    - Contains `CREATE INDEX idx_eval_runs_matrix_group_id`.
    - No `DROP`, no `db push`, no data-mutating UPDATE; additive statements only.
  </acceptance_criteria>
  <done>Migration file authored, additive-only, all four columns + the index present.</done>
</task>

<task type="auto">
  <name>Task 2: Add case_feedback to JudgeVerdict + TS types + api.ts client fns</name>
  <files>backend/app/services/harness/validator_kinds.py, frontend/src/types/index.ts, frontend/src/lib/api.ts</files>
  <read_first>
    - backend/app/services/harness/validator_kinds.py (JudgeVerdict at :103-113 + the FINDING-04 comment at :94-100 — new fields MUST have no default)
    - frontend/src/types/index.ts (EvalRun :583-602, EvalResult :605-633 — current fields + comment style)
    - frontend/src/lib/api.ts (the skill_builder_model client-fn wiring to mirror + the existing eval run/readout fetch fns to mirror for matrix/sweep/aggregate)
  </read_first>
  <action>
    In `validator_kinds.py` add `case_feedback: str` to `JudgeVerdict` as the LAST field, with NO default (FINDING-04 — so it lands in the strict `required` list and is OpenAI-safe); add an inline comment marking it advisory critique of the CASE, never blocking, never a gate input. In `frontend/src/types/index.ts` extend `EvalRun` with `matrix_group_id: string | null` and `feeds_gate: boolean` (comment: Phase 137.1 mig 085 — matrix grouping + gate-feeder flag; NULL/false on single runs), and extend `EvalResult` with `duration_ms: number | null` (per-arm wall-clock) and `case_feedback: string | null` (advisory judge critique, never a verdict). Add TS shapes for the new endpoints: a `MatrixRunKickoff` (group_id + array of per-arm EvalRunKickoff), an `EngineHealthBoard`/`EngineHealthTile` (provider, model, healthy boolean, verbatim error string|null, run_id, last_swept_at), and an `EvalAggregate`/`EvalConfigAgg` (per provider+model: run_count, with_mean, without_mean, with_stddev|null, delta, analyst_notes string array). In `frontend/src/lib/api.ts` add thin fetch client fns mirroring the existing eval fetch style: `startMatrixRun(skillId, {gate_provider})`, `runEngineSweep()`, `getEngineHealth()`, `getEvalAggregate(skillId)`, and `getJudgeModel()`/`setJudgeModel(model)` (mirror the skill_builder_model settings client wiring). These call the routes Plans 04/05/07 create — they are thin wrappers; do not implement backend logic here.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.services.harness.validator_kinds import JudgeVerdict; assert 'case_feedback' in JudgeVerdict.model_fields; import json; s=JudgeVerdict.model_json_schema(); assert 'case_feedback' in s['required'], s['required']; print('OK strict-safe')"</automated>
  </verify>
  <acceptance_criteria>
    - `JudgeVerdict.model_fields` includes `case_feedback` AND `case_feedback` appears in the JSON-schema `required` array (proves no default → strict-safe).
    - `frontend/src/types/index.ts` grep shows `matrix_group_id`, `feeds_gate`, `duration_ms`, `case_feedback` added to the eval interfaces.
    - `frontend/src/lib/api.ts` grep shows `startMatrixRun`, `runEngineSweep`, `getEngineHealth`, `getEvalAggregate`, and a judge-model getter/setter.
    - `cd frontend && npx tsc --noEmit` passes (types compile).
  </acceptance_criteria>
  <done>JudgeVerdict has a strict-safe case_feedback field; TS types + api client fns compile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply migration 085 live + regenerate full-schema</name>
  <files>supabase/migrations/085_eval_matrix_duration_case_feedback.sql, supabase/full-schema.sql</files>
  <read_first>
    - supabase/migrations/085_eval_matrix_duration_case_feedback.sql (the SQL authored in Task 1 — paste this exact content)
    - CLAUDE.md migration discipline (apply via SQL editor; NEVER db push/reset; regen with NO --reset)
    - scripts/regenerate-full-schema.sh (the live-DB dump that rebuilds full-schema.sql)
  </read_first>
  <action>
    OPERATOR-GATED (autonomous:false) — the SQL editor is interactive. Migration 085 is authored but NOT yet applied; later-wave backend code references these columns and will fail until they exist. Steps: (1) Open the Supabase SQL editor against the LIVE LOCAL DB (127.0.0.1:54322), paste the entire contents of `supabase/migrations/085_eval_matrix_duration_case_feedback.sql`, and run it — NEVER `supabase db push` / `db reset` (CLAUDE.md). (2) Confirm the four columns exist: query `information_schema.columns` for `eval_runs` in (`matrix_group_id`,`feeds_gate`) → 2 rows, and `eval_results` in (`duration_ms`,`case_feedback`) → 2 rows. (3) Regenerate the bootstrap artifact with `bash scripts/regenerate-full-schema.sh` (default live-DB dump, NO --reset — preserves dev data). (4) Commit the migration + regenerated `supabase/full-schema.sql` together. Pause for the operator to run the SQL editor step and confirm before continuing.
  </action>
  <verify>
    <automated>grep -Ec "matrix_group_id|feeds_gate|duration_ms|case_feedback" supabase/full-schema.sql | awk '{ if ($1>=4) print "OK"; else { print "MISSING"; exit 1 } }'</automated>
  </verify>
  <resume-signal>Type "applied" once the four columns are live and full-schema.sql is regenerated + committed, or describe the error.</resume-signal>
  <acceptance_criteria>
    - `information_schema.columns` shows all four new columns on the live local DB.
    - `supabase/full-schema.sql` grep shows the four columns (regenerated, not hand-edited).
    - Migration + full-schema committed together.
  </acceptance_criteria>
  <done>Migration 085 applied live; full-schema.sql regenerated + committed; all four columns queryable on the local DB.</done>
</task>

</tasks>

<verification>
- Migration file additive-only, four columns + index present.
- `JudgeVerdict.case_feedback` in the strict `required` list.
- TS + api.ts compile.
- Four columns live on the local DB; full-schema.sql regenerated.
</verification>

<success_criteria>
The phase's data + interface contracts exist and are live: matrix/gate columns, duration+case_feedback columns, the strict-safe verdict field, TS types, and api client fns — every downstream plan can build against them.
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-01-SUMMARY.md` when done.
</output>
