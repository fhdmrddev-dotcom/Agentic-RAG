---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 07
type: execute
wave: 4
depends_on: ["137.1-01", "137.1-04"]
files_modified:
  - backend/app/services/eval_aggregation.py
  - backend/app/api/evals.py
  - backend/tests/test_eval_aggregation.py
autonomous: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "D-07 aggregation computes per-config (skill, provider, model) mean±stddev/delta over accumulated eval_runs HISTORY, not the single click — stddev appears ONLY once a config has >=2 runs; a first run reports 'first run — no spread yet'"
    - "D-08 analyst annotations are deterministic rules with fixed plain-language phrasing (non-discriminating case / flaky variance / time-token tradeoff) — NEVER an LLM-written paragraph"
    - "Aggregation inherits the honesty locks: the mean/pass math counts only with-skill 'graded' arms; not_measured / judge_error are never counted as fail; delta = with-mean minus without-mean judge score"
    - "The aggregation endpoint is owner-scoped (404-never-403) and reads history for the caller's own runs only"
  artifacts:
    - path: "backend/app/services/eval_aggregation.py"
      provides: "pure deterministic functions: per-config mean/stddev/delta over run history + fixed-phrasing analyst-note rules"
      contains: "def "
    - path: "backend/app/api/evals.py"
      provides: "GET aggregate endpoint returning per-config aggregation + analyst notes for a skill"
      contains: "aggregate"
    - path: "backend/tests/test_eval_aggregation.py"
      provides: "unit tests: stddev only at n>=2; not_measured excluded; each analyst-note rule fires on its exact condition"
      contains: "stddev"
  key_links:
    - from: "backend/app/api/evals.py aggregate endpoint"
      to: "eval_aggregation pure functions"
      via: "history rows → deterministic mean/stddev/delta + notes"
      pattern: "eval_aggregation"
    - from: "eval_aggregation"
      to: "eval_runs/eval_results history (with-skill graded arms)"
      via: "the with-skill graded denominator inheritance"
      pattern: "graded"
---

<objective>
Compute the matrix aggregation server-side (D-07/D-08) as a small, testable, deterministic module: per-config mean±stddev/delta over accumulated eval-run HISTORY, plus fixed-phrasing analyst notes. Backend-computed (RESEARCH OQ4) keeps the honesty phrasing in one place and unit-testable, and inherits the shipped rollup honesty (with-skill graded arms only; not_measured/judge_error never fail).

Purpose: The Studio display (Plan 09) renders this verbatim — no aggregation math or phrasing in the frontend.
Output: `eval_aggregation.py` pure functions + an owner-scoped aggregate endpoint in `evals.py` + tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-RESEARCH.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-PATTERNS.md

<interfaces>
<!-- The honesty locks aggregation MUST inherit + the shape it feeds. -->
Rollup denominator (eval_runner_service.py:797-799): measured_count = with-skill 'graded' arms only; passed_count = with-skill graded PASS. not_measured/judge_error are NEVER fail.
eval_runs history per (skill, provider, model): status/created_at/passed_count/measured_count/verdict_summary + the per-run eval_results (verdict_score for the mean, duration_ms for time-token notes — from Plan 01/02).
Delta = with-skill mean judge score − without-skill mean judge score (skill lift).
D-08 analyst-note rules (fixed phrasing): (a) non-discriminating case — all configs pass BOTH arms (skill adds nothing measurable on that case); (b) flaky variance — high stddev across a config's runs; (c) time-token tradeoff — a config is markedly slower/cheaper using the new duration_ms + existing tokens.
Owner gate: _verify_owned_skill (evals.py:106-135) — 404-never-403.
The frontend consumes the EvalAggregate TS shape defined in Plan 01 (api.ts getEvalAggregate).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → aggregate read | Untrusted skill_id crosses into an owner-scoped history read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 (carried) | Info disclosure / EoP | aggregate read | mitigate | Owner gate first (`_verify_owned_skill`); 404-never-403; history read filtered `.eq("user_id")`. |
| T-137.1-A1 | Tampering (misleading stats) | aggregation honesty | mitigate | Deterministic math only; with-skill graded denominator; stddev only at n>=2 ("first run — no spread yet" otherwise); notes are fixed-phrasing rules, never an LLM paragraph (D-08). |
| T-137.1-SC | Tampering | pip installs | mitigate | No new packages (statistics is stdlib). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: eval_aggregation.py — deterministic per-config stats + fixed-phrasing notes</name>
  <files>backend/app/services/eval_aggregation.py, backend/tests/test_eval_aggregation.py</files>
  <behavior>
    - Given history rows grouped by (provider, model): mean judge score over with-skill graded arms; stddev only when a config has >=2 runs (else a "first run — no spread yet" marker); delta = with-mean − without-mean.
    - not_measured / judge_error arms are excluded from the mean/denominator (never counted as fail).
    - Analyst notes: the non-discriminating rule fires when all configs pass both arms; the flaky-variance rule fires above a stddev threshold; the time-token rule fires on a marked duration/token gap. Each note is fixed phrasing with a stable tag; no LLM text.
  </behavior>
  <read_first>
    - backend/app/services/eval_runner_service.py (:797-799 the rollup denominator to inherit)
    - .planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-RESEARCH.md (OQ4 + Pitfall 4 — spread from history, not the single click)
    - .claude/skills/sketch-findings-agentic-rag/references/eval-production-clean.md (058-A tagged analyst-note rules + Δ column vocabulary)
  </read_first>
  <action>
    Create `backend/app/services/eval_aggregation.py` with PURE functions (no I/O): (1) `aggregate_configs(history)` grouping run history by (provider, model) and returning per-config `{run_count, with_mean, without_mean, with_stddev (None if run_count<2), delta}` where the means average only with-skill `graded` arm verdict_scores (inherit the eval_runner_service.py:797-799 denominator; use `statistics.pstdev`/`stdev` from stdlib) and delta = with_mean − without_mean; (2) `analyst_notes(configs, cases)` returning a list of `{tag, message}` fixed-phrasing notes for the three D-08 rules (non-discriminating case, flaky variance at a named stddev threshold, time-token tradeoff from duration_ms + tokens) — deterministic, no LLM. Where run_count<2, the config carries a "first run — no spread yet" marker instead of a numeric stddev. Write `test_eval_aggregation.py`: (a) stddev is None on a 1-run config and numeric at 2+ runs; (b) a not_measured arm is excluded from the mean; (c) each of the three note rules fires on its exact condition and NOT otherwise.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_eval_aggregation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `aggregate_configs` returns `with_stddev=None` for a 1-run config and a numeric stddev at >=2 runs.
    - A not_measured/judge_error arm never enters the mean/denominator (test-proven).
    - `analyst_notes` returns fixed-phrasing tagged notes; the three rules each fire only on their condition; no LLM/network call in the module (`grep -n "await\|forced_emit\|client" eval_aggregation.py` shows no LLM call).
  </acceptance_criteria>
  <done>Deterministic aggregation + analyst notes as pure, tested functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Owner-scoped aggregate endpoint in evals.py</name>
  <files>backend/app/api/evals.py, backend/tests/test_eval_aggregation.py</files>
  <read_first>
    - backend/app/api/evals.py (:106-135 owner gate; the runs-history read helpers + run_in_threadpool wrapping)
    - backend/app/services/eval_aggregation.py (the pure fns from Task 1)
  </read_first>
  <action>
    Add a `GET /skills/{skill_id}/evals/aggregate` route: owner-gate FIRST (`_verify_owned_skill`, 404-never-403), read this skill's accumulated eval-run history for the caller (`.eq("user_id")`, wrapped in `run_in_threadpool` per D-v2.5-01), feed the rows into `eval_aggregation.aggregate_configs` + `analyst_notes`, and return the `EvalAggregate` shape (per-config stats + notes) matching the Plan 01 TS type. Do NOT recompute anything in the route beyond calling the pure functions. Extend the test to assert the endpoint returns the aggregate for an owned skill and 404s cross-user.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_eval_aggregation.py -k "endpoint or route or aggregate_api" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The route owner-gates first (cross-user → 404) and reads only the caller's history.
    - The route delegates all math to `eval_aggregation` (no inline stats in evals.py).
    - Response shape matches the Plan 01 `EvalAggregate` TS type.
  </acceptance_criteria>
  <done>Aggregate endpoint returns deterministic per-config stats + notes, owner-scoped; tests green.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_eval_aggregation.py -x` green.
- Stddev only at n>=2; not_measured excluded; notes deterministic + fixed-phrasing.
- Aggregate endpoint owner-scoped, delegates to the pure module.
</verification>

<success_criteria>
Per-config aggregation (mean±stddev/delta over run history) + deterministic analyst notes are computed server-side, honest (with-skill graded only; spread from history), and exposed through an owner-scoped endpoint the Studio renders verbatim.
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-07-SUMMARY.md` when done.
</output>
