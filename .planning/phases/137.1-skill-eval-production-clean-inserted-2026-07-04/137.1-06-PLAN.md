---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/provider_gateway/anthropic.py
  - backend/app/services/provider_gateway/openai_compat.py
  - backend/app/config.py
  - .planning/reported-bugs/BUG-260701-01-agent-loop-assistant-prefill-400-newer-models.md
  - .planning/reported-bugs/BUG-260630-01-deepseek-without-skill-reasoning-content-400.md
autonomous: false
requirements: [EVAL-05]
must_haves:
  truths:
    - "EVAL-05g: BUG-260701-01 (Claude prefill 400) and BUG-260630-01 (DeepSeek reasoning_content 400) are RE-TESTED live post-f47d6736 BEFORE any fix is written (baseline contamination may already have closed BUG-260630-01)"
    - "D-14 red line: any provider request-shape fix lands ONLY at the gateway/adapter/sanitizer boundary (anthropic.py / openai_compat.py / a MODEL_CAPABILITIES flag) — the shared agent-loop/Deep path is NEVER forked and stays byte-identical"
    - "A bug that no longer reproduces is CLOSED on a clean re-verify (its report frontmatter status flips to closed) with no code change — no adapter diff lands without reproduction evidence"
    - "If a bug still reproduces, the adapter fix is provider-scoped and additive; Deep Mode and other providers are unaffected"
  artifacts:
    - path: ".planning/reported-bugs/BUG-260630-01-deepseek-without-skill-reasoning-content-400.md"
      provides: "re-verify outcome recorded in frontmatter (closed on clean re-verify, or fix-linked if still live)"
      contains: "status:"
    - path: ".planning/reported-bugs/BUG-260701-01-agent-loop-assistant-prefill-400-newer-models.md"
      provides: "re-verify outcome recorded in frontmatter"
      contains: "status:"
  key_links:
    - from: "provider adapter fix (IF needed)"
      to: "MODEL_CAPABILITIES flag (config.py)"
      via: "supports_assistant_prefill / reasoning_content shape gating at the adapter boundary only"
      pattern: "provider_gateway"
---

<objective>
Honor EVAL-05g + the D-14 red line: RE-TEST the two provider request-shape bugs live post-`f47d6736` BEFORE writing any fix, then apply an adapter-boundary fix ONLY for whichever still reproduces. `f47d6736` (reset-before-each-arm) likely already closed BUG-260630-01 (DeepSeek reasoning_content 400); BUG-260701-01 (Claude 4.6+/5 prefill 400) may be a genuinely separate trap. No adapter diff lands without reproduction evidence.

Purpose: The matrix aggregation (Plans 07/09) is only meaningful on trustworthy per-provider verdicts — the re-tests come first, and every provider fix stays at the gateway/adapter boundary so the shared path never forks.
Output: recorded re-verify outcomes + (conditionally) provider-scoped adapter fixes.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-RESEARCH.md
@.planning/reported-bugs/BUG-260701-01-agent-loop-assistant-prefill-400-newer-models.md
@.planning/reported-bugs/BUG-260630-01-deepseek-without-skill-reasoning-content-400.md

<interfaces>
<!-- Where a fix would land IF a bug reproduces — adapter boundary ONLY (D-14). -->
provider_gateway/anthropic.py — Anthropic adapter; the prefill-construction site for BUG-260701-01 (RESEARCH could NOT locate the exact line — A2 LOW confidence; the re-test drives it). A fix strips/skips the assistant prefill per model capability.
provider_gateway/openai_compat.py:267-268 — existing DeepSeek reasoning_content emit-side handling; the nearest fix-shape precedent for BUG-260630-01 IF it still reproduces (request-shape sanitize).
config.py MODEL_CAPABILITIES (:237-267) — a possible additive `supports_assistant_prefill` flag to gate the anthropic fix per model.
f47d6736 = reset-before-each-arm (removed the replayed with-skill assistant turn that likely CAUSED BUG-260630-01).
Live test vehicle: the EXISTING single-run eval route (shipped 133/134) — run an owned skill's eval on a DeepSeek model (without-skill arm) and on a Claude 4.6+/5 model. The smoke sweep (Plan 04) is a later wave, so this plan uses the existing single-run eval.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider adapter → provider API request shape | Model-specific request-shape quirks are absorbed here, never in the shared loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.1-D14 | Tampering (shared-path regression) | provider fixes | mitigate | D-14: any fix lands in anthropic.py / openai_compat.py / a MODEL_CAPABILITIES flag ONLY; agent_loop.py is untouched; a `test_deep_mode_unchanged`-style regression proves Deep byte-identical. |
| T-137.1-D14b | Tampering | speculative fix without repro | mitigate | No adapter diff without live reproduction evidence (Pitfall 5); a clean re-verify CLOSES the bug with zero code change. |
| T-137.1-SC | Tampering | pip installs | mitigate | No new packages. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live re-test both provider bugs post-f47d6736 (before any fix)</name>
  <files>.planning/reported-bugs/BUG-260701-01-agent-loop-assistant-prefill-400-newer-models.md, .planning/reported-bugs/BUG-260630-01-deepseek-without-skill-reasoning-content-400.md</files>
  <read_first>
    - .planning/reported-bugs/BUG-260630-01-deepseek-without-skill-reasoning-content-400.md (the original DeepSeek reasoning_content 400 symptom)
    - .planning/reported-bugs/BUG-260701-01-agent-loop-assistant-prefill-400-newer-models.md (the Claude prefill 400 symptom)
    - .planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-RESEARCH.md (Pitfall 5 + A1/A2 — f47d6736 likely already closed -260630-01; -260701-01 may be separate)
  </read_first>
  <action>
    OPERATOR-GATED (autonomous:false) — needs live provider keys + human observation; no new code. Re-test whether the two request-shape 400s still reproduce, using the EXISTING single-run eval route (shipped 133/134). Steps: (1) Ensure the backend runs current code with DeepSeek + Anthropic (Claude 4.6+/5) keys configured. (2) BUG-260630-01: run a single-run eval on an owned skill with a DeepSeek model; watch the WITHOUT-skill arm; record whether it returns a real answer / honest not_measured OR a `reasoning_content` 400 (capture verdict_state + any verbatim provider error from the run detail / backend logs / the eval_results row). (3) BUG-260701-01: run a single-run eval with a Claude 4.6+/5 model; record whether an assistant-prefill 400 occurs. (4) Report per bug: CLEAN or REPRODUCES + verbatim error. This evidence decides whether Task 2 writes an adapter fix or only closes the report — no adapter diff lands without reproduction (Pitfall 5). Do NOT write any fix in this task.
  </action>
  <verify>
    <automated>MISSING — live cross-provider re-test is operator-run; the outcome is recorded in the two BUG report frontmatters and consumed by Task 2 (see 137.1-VALIDATION.md U10/U11).</automated>
  </verify>
  <resume-signal>Report each bug as "CLEAN" or "REPRODUCES: &lt;verbatim error&gt;". This decides whether Task 2 writes a fix or only closes the report.</resume-signal>
  <acceptance_criteria>
    - A live per-bug outcome is recorded (CLEAN or REPRODUCES + verbatim error) from real provider calls, not assumption.
    - Evidence source named (run detail / backend log line / eval_results row).
  </acceptance_criteria>
  <done>Both bugs re-verified live post-f47d6736; per-bug CLEAN/REPRODUCES outcome recorded before any fix is written.</done>
</task>

<task type="auto">
  <name>Task 2: Conditionally apply the adapter-boundary fix (or close the report)</name>
  <files>backend/app/services/provider_gateway/anthropic.py, backend/app/services/provider_gateway/openai_compat.py, backend/app/config.py, .planning/reported-bugs/BUG-260701-01-agent-loop-assistant-prefill-400-newer-models.md, .planning/reported-bugs/BUG-260630-01-deepseek-without-skill-reasoning-content-400.md</files>
  <read_first>
    - Task 1's reported outcomes (CLEAN vs REPRODUCES per bug)
    - backend/app/services/provider_gateway/openai_compat.py (:267-268 reasoning_content handling — the fix-shape precedent)
    - backend/app/services/provider_gateway/anthropic.py (the request-construction / message-assembly site)
    - backend/app/config.py (:237-267 MODEL_CAPABILITIES entry shape — for a possible supports_assistant_prefill flag)
  </read_first>
  <action>
    Branch on Task 1's evidence, per bug:
    - If a bug is CLEAN: make NO code change. Update that bug report's frontmatter `status` to `closed` with a note citing the clean live re-verify post-f47d6736 (BUG-260630-01 is the expected-closed case). Do NOT touch any adapter file for it.
    - If BUG-260630-01 REPRODUCES: add a provider-scoped request-shape sanitize in `openai_compat.py` (mirror the existing :267-268 reasoning_content handling) that strips/repairs the offending `reasoning_content` on the request path for DeepSeek only. Add a regression test. Do NOT touch agent_loop.py.
    - If BUG-260701-01 REPRODUCES: strip/skip the assistant prefill in `anthropic.py` gated by an additive `supports_assistant_prefill` flag in `config.py` MODEL_CAPABILITIES (default such that affected Claude 4.6+/5 models skip the prefill). Add a regression test. Do NOT touch agent_loop.py.
    In every reproduce case, keep the fix additive and provider-scoped; add/keep a Deep-mode-unchanged assertion so the shared path stays byte-identical (D-14). Record the fix (commit-linked) in the bug report frontmatter and flip its status to `closed` once the re-run passes.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ -k "prefill or reasoning_content or deep_mode_unchanged" -q; grep -Eic "status:\s*(closed|folded)" ".planning/reported-bugs/BUG-260630-01-deepseek-without-skill-reasoning-content-400.md"</automated>
  </verify>
  <acceptance_criteria>
    - Each bug report frontmatter `status` reflects the live outcome (closed on clean re-verify; closed + fix-linked if a fix landed).
    - Any fix touches ONLY anthropic.py / openai_compat.py / config.py — `git diff --name-only` shows NO change to `agent_loop.py` or `threads.py`.
    - If a fix landed, a provider-scoped regression test passes AND a Deep-mode-unchanged assertion passes.
    - If both bugs were CLEAN, no adapter file is modified (only the two report frontmatters change).
  </acceptance_criteria>
  <done>Both bugs re-verified; adapter fix applied only where reproduced (boundary-only, Deep byte-identical); reports closed.</done>
</task>

</tasks>

<verification>
- Live re-test outcomes recorded per bug before any fix.
- No adapter diff without reproduction evidence; any fix at the adapter boundary only.
- `git diff --name-only` shows no agent_loop.py / threads.py change.
</verification>

<success_criteria>
Both provider request-shape bugs are honestly re-verified post-f47d6736; whichever no longer reproduces is closed with zero code change, and any genuine remaining fix lands provider-scoped at the gateway/adapter boundary with Deep Mode byte-identical (D-14).
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-06-SUMMARY.md` when done.
</output>
