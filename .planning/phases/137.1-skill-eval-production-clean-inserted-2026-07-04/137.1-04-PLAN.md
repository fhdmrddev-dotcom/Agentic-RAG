---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 04
type: execute
wave: 3
depends_on: ["137.1-01", "137.1-02"]
files_modified:
  - backend/app/api/evals.py
  - backend/tests/test_evals_router.py
autonomous: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "D-01 the smoke sweep IS a matrix run over a built-in smoke case — one machinery serves EVAL-05a and EVAL-05b (the sweep route reuses the matrix fan-out)"
    - "D-02 the smoke fixture is a built-in synthetic skill + one case, synthesized IN-MEMORY and persisted SKILL-LESS (NULL skill_id/skill_version_id on eval_runs, NULL test_case_id on eval_results per mig 085 — NO rows in skills/skill_versions/skill_test_cases, hidden from the Skills list by construction), asserting ENGINE health only — an arm completes graded or honest not_measured carrying a REAL provider error"
    - "D-05 the matrix launcher designates exactly ONE config as gate-feeding (default = the user's active provider); that arm gets feeds_gate=true, the other N-1 are analysis-only and never flip the publish gate"
    - "D-06 the existing per-skill Redis SET NX claim is acquired ONCE per matrix group (value = group_id), N arms run in parallel underneath it, and no other run can start on that skill while the matrix is live (409 unchanged)"
    - "The publish gate reads ONLY the feeds_gate=true run in the group — the gate-feeder is a label on data, never a second gate computation (137 D-03 one-truth-teller)"
    - "Every matrix/sweep route inherits owner-scoping: 404-never-403, arms owner-stamped from current_user never the body; sweep runs never leak into skill-scoped Studio RunHistory views (skill_id IS NULL excludes them from .eq('skill_id', ...) reads by construction)"
    - "D-03 read half: an owner-scoped GET engine-health endpoint returns the caller's LATEST smoke-sweep board (per-tile provider, model, healthy, verbatim error, run_id, last_swept_at) — the Settings card's data source"
  artifacts:
    - path: "backend/app/api/evals.py"
      provides: "POST matrix-launch route (N single-provider run_eval_job under one group claim, one feeds_gate); POST engine-smoke-sweep route (in-memory synthetic skill+case persisted skill-less over the matrix machinery); GET engine-health route (latest sweep board, EngineHealthBoard shape); gate read scoped to feeds_gate run"
      contains: "matrix_group_id"
    - path: "backend/tests/test_evals_router.py"
      provides: "tests: one group claim + N rows + exactly one feeds_gate; 409 contention; gate reads feeds_gate run only; owner 404; sweep persists skill-less (no skills/skill_versions/skill_test_cases rows, NULL FKs) + no skill-view leak; engine-health GET owner-scoped latest-sweep board"
      contains: "feeds_gate"
  key_links:
    - from: "matrix launch route"
      to: "eval_runner_service.run_eval_job (unchanged)"
      via: "N asyncio.create_task spawns, one per configured provider, under one SET NX group claim"
      pattern: "run_eval_job"
    - from: "publish gate read"
      to: "the single feeds_gate=true eval_runs row"
      via: "scoped gate compute (no second met derivation)"
      pattern: "feeds_gate"
    - from: "GET engine-health route"
      to: "the caller's latest sweep group (matrix_group_id set, skill_id IS NULL)"
      via: "owner-scoped latest-group read returning the EngineHealthBoard shape Plan 10 consumes"
      pattern: "engine.health|engine_health"
---

<objective>
Add the matrix fan-out that turns one click into N parallel single-provider eval runs (EVAL-05b) and the engine smoke sweep that rides the same machinery over a built-in in-memory synthetic case (EVAL-05a). Both live in `evals.py` (the eval home, NOT a ledger file — G-5). The hard parts are already solved: each arm is an existing self-contained `run_eval_job`; N rows are N existing per-run subscriptions. Net-new is the group claim (ONE per group, D-06), the one gate-feeder flag (D-05), and the synthesized smoke fixture (D-02).

Purpose: One machinery serves both requirements; do not hand-roll parallel orchestration or a bespoke matrix SSE (the substrate encodes a year of race fixes).
Output: matrix-launch route + smoke-sweep route + gate-read scoping + tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-RESEARCH.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-PATTERNS.md

<interfaces>
<!-- The exact seams to reuse — do not rewrite orchestration. -->
start_eval_run (evals.py:139-374): the single-run precedent. Owner gate first (_verify_owned_skill, :106-135, 404-never-403). Model validation (:159-175): get_model_capability(model).capability_source must == "registry"; provider must match cap.provider.
The claim (evals.py:229-236): redis.set(_inflight_key(skill_id), value, nx=True, ex=_INFLIGHT_TTL_S) → 409 on contention. _inflight_key = f"eval_inflight:{skill_id}" (:85).
Pitfall 1 (D-06): claim VALUE must be the GROUP id, not a per-arm run_id. The per-job finally CAS-releases via _release_inflight_if_owned (eval_runner_service.py:152-159) which NO-OPS when the stored value differs — so an early arm's release is harmless; release the group claim once after ALL arms finish.
Per-arm spawn (evals.py:270-359): insert eval_runs row (+ NEW matrix_group_id=group_id, feeds_gate), insert companion runs row (:291-299), redis.zadd("runs:active", ...), emit EVENT_RUN_STARTED, then asyncio.create_task(run_eval_job(run_id=..., provider=cfg.provider, model=cfg.model, ...)); RUN_TASKS[run_id]=task.
Gate compute: _compute_gate / promotion_gate (evals.py:1059, 1111) already read a single run's rows — scope to the feeds_gate=true run.
Smoke fixture (D-02, revised per checker Blocker 1 — option b): run_eval_job ALREADY accepts `skill_version: dict` + `cases: list[dict]` as params (eval_runner_service.py:678-696) — the sweep route synthesizes both in-memory and passes them directly; NO _read_cases/_read_latest_version, NO rows in skills/skill_versions/skill_test_cases. Persistence: mig 085 (Plan 01) relaxed eval_runs.skill_id/skill_version_id + eval_results.test_case_id to NULLABLE, and Plan 02 made _persist_result NULL-tolerant (test_case_id None -> SQL NULL) — the sweep's eval_runs rows carry NULL skill_id/skill_version_id (matrix_group_id set), its eval_results rows carry NULL test_case_id. This is why this plan depends on 137.1-02. Skill-scoped Studio queries (.eq("skill_id", ...)) exclude NULL-skill sweep rows by construction — no RunHistory leak. Sweep claim key: skill-less, so use a per-user sweep key (e.g. eval_inflight:engine-sweep:{user_id}) instead of _inflight_key(skill_id). eval_runs.user_id still owns every row (owner-scoping unchanged).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → matrix/sweep launch route | Untrusted skill_id + gate-provider choice cross here; owner-scoping is the sole runtime gate. |
| launcher → N paid provider calls | A matrix/sweep fans out to many billed LLM calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 (carried) | Info disclosure / EoP | matrix/sweep owner gate | mitigate | `_verify_owned_skill` FIRST on every route; 404-never-403; arms owner-stamped from current_user, never the body (T-133-03). |
| T-137.1-M1 | DoS | matrix fan-out (N paid calls) | mitigate | ONE SET NX claim per skill (D-06) bounds concurrency to one matrix per skill; ×1 run per provider per click (D-07, no repeat knob); smoke sweep ~24 calls accepted budget (D-03). |
| T-137.1-M2 | Tampering | gate-feeder integrity | mitigate | Exactly one feeds_gate=true per group enforced at insert (default = active provider); the gate read is scoped to that one run — no second `met` computation (137 D-03). |
| T-137.1-EC (EVAL-05h) | Tampering / EoP | judge evidence channel surfaced by these arms | mitigate | Receipts stay delimited DATA + 4KB cap (eval_runner_service.py:184-205), carried unchanged; named here because matrix/sweep arms exercise it across all providers. |
| T-137.1-SC | Tampering | pip installs | mitigate | No new packages. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Matrix launch route — N arms under ONE group claim (D-05/D-06)</name>
  <files>backend/app/api/evals.py, backend/tests/test_evals_router.py</files>
  <behavior>
    - POST matrix launch with a gate_provider spawns one run per configured+registry-valid provider, all sharing one matrix_group_id, exactly one with feeds_gate=true (the gate_provider, default = active provider).
    - The Redis SET NX claim is taken ONCE keyed by skill_id with value = group_id; a second launch on the same skill 409s while the matrix is live.
    - The publish gate for the group reads ONLY the feeds_gate run's rows.
    - Cross-user skill_id → 404 before anything spawns.
  </behavior>
  <read_first>
    - backend/app/api/evals.py (:80-135 keys + owner gate; :139-374 start_eval_run; :1059,:1111 gate compute)
    - backend/app/services/eval_runner_service.py (:152-159 _release_inflight_if_owned CAS no-op; run_eval_job signature)
    - .planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-PATTERNS.md (evals.py matrix section — the claim-once + per-arm-spawn blocks + Pitfall 1)
  </read_first>
  <action>
    Add a matrix-launch route (e.g. `POST /skills/{skill_id}/evals/matrix`) with a body carrying `gate_provider: str | None` (default = the user's active provider from user_settings) and optionally an explicit provider list (default = all configured registry-valid providers, one representative model each). Owner-gate FIRST (`_verify_owned_skill`). Build the config list: one registry-known model per configured provider (validate each via `get_model_capability` — capability_source == "registry"; skip/omit providers with no registry model). Generate `group_id = uuid4()`. Take the claim ONCE: `redis.set(_inflight_key(skill_id), str(group_id), nx=True, ex=_INFLIGHT_TTL_S)`; if not claimed → 409 "An eval run is already in progress for this skill". For each cfg: `run_id=uuid4()`, `feeds_gate = (cfg.provider == gate_provider)` (ensure exactly ONE True — the gate_provider must be in the config list; if not, add it or fall back to the first config), insert the eval_runs row with `matrix_group_id=str(group_id)` + `feeds_gate`, insert the companion runs row, `zadd("runs:active", ...)`, emit EVENT_RUN_STARTED, then `asyncio.create_task(run_eval_job(run_id=run_id, provider=cfg.provider, model=cfg.model, ...))` and store in RUN_TASKS. Release the group claim ONLY after all arms finish (rely on the CAS no-op: the group claim value is group_id, so a per-arm run_id release no-ops per Pitfall 1; add a group-level release coordinator that CAS-releases keyed to group_id once the last arm's task completes). Scope the publish-gate read to the `feeds_gate=true` run (do NOT add a second `met` computation). Return a `MatrixRunKickoff` (group_id + the per-arm kickoffs). Extend `test_evals_router.py`: assert one claim/one group/N rows/exactly one feeds_gate; 409 on a second launch; the gate read uses the feeds_gate run only; cross-user 404.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_evals_router.py -k "matrix or gate_feeder" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Route spawns N `run_eval_job` tasks sharing one `matrix_group_id`; test asserts exactly one row has `feeds_gate=true`.
    - The SET NX claim value is `group_id` (grep the route for the claim with the group id), taken once; a second launch on the same skill returns 409.
    - The publish-gate read is scoped to the feeds_gate run; test asserts the other arms never feed the gate.
    - Cross-user launch returns 404 (owner gate first).
  </acceptance_criteria>
  <done>Matrix launch fans N arms under one group claim with one gate-feeder; gate reads that one run; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine smoke-sweep route — in-memory synthetic fixture over the matrix (D-01/D-02)</name>
  <files>backend/app/api/evals.py, backend/tests/test_evals_router.py</files>
  <behavior>
    - POST engine-sweep fans one representative model per configured provider over a BUILT-IN synthesized smoke skill + single case, creating NO rows in skills/skill_versions/skill_test_cases; its eval_runs rows persist with NULL skill_id/skill_version_id and its eval_results rows with NULL test_case_id (mig 085 + Plan 02 NULL-tolerant persist).
    - Each arm asserts ENGINE health: it completes graded, or an honest not_measured carrying a REAL provider error — never an engine-shaped error.
    - The sweep reuses the matrix fan-out (one group, N arms); the latest sweep group is queryable (matrix_group_id set + skill_id IS NULL + user_id) for the engine-health read.
    - Sweep rows never appear in skill-scoped RunHistory reads (NULL skill_id is excluded by .eq("skill_id", ...) by construction).
  </behavior>
  <read_first>
    - backend/app/api/evals.py (the matrix route from Task 1 — the sweep reuses it; the _insert_eval_run sites at :270/:1349 whose payload gains NULL skill fields for sweep rows)
    - backend/app/services/eval_runner_service.py (:678-696 run_eval_job signature — skill_version + cases are ALREADY params; the Plan 02 NULL-tolerant _persist_result)
    - supabase/migrations/085_eval_matrix_duration_case_feedback.sql (the DROP NOT NULL relaxations this rides on)
    - .planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-RESEARCH.md (OQ1 RESOLVED: skill-less nullable-columns path; Pitfall 2)
  </read_first>
  <action>
    Add an engine-smoke-sweep route (e.g. `POST /evals/engine-sweep`). Synthesize the fixture IN-MEMORY (RESEARCH OQ1 RESOLVED — the skill-less path): a deterministic, token-cheap smoke `skill_version` dict (a trivial no-op skill instruction) + a single synthetic `cases` entry whose prompt is deterministic and cheap (e.g. instruct the agent to reply with a fixed short token) and whose `expected_behavior` is judge-gradeable (e.g. "the response contains the token"), with `id: None` so the arm persists NULL test_case_id (Plan 02 NULL-tolerant _persist_result). Pass both directly to `run_eval_job` via its EXISTING `skill_version` + `cases` params — never `_read_cases`/`_read_latest_version`, never a skills/skill_versions/skill_test_cases INSERT. Drive the sweep through the SAME matrix fan-out as Task 1 (one group, one representative registry model per configured provider — exclude local per D-01), with the eval_runs rows inserted carrying `matrix_group_id=group_id`, `skill_id=None`, `skill_version_id=None` (mig 085 made them nullable; FKs still enforced when non-NULL) and owner-stamped `user_id` from current_user. The sweep claim is skill-less: use a per-user sweep key (e.g. `eval_inflight:engine-sweep:{user_id}`) with the same SET NX + group_id-value + CAS-release semantics as the matrix claim (D-06 preserved; no per-skill 409 collateral). The gate is irrelevant for the sweep — feeds_gate=false on all sweep arms. The latest-sweep lookup contract: the caller most-recent group where `matrix_group_id IS NOT NULL AND skill_id IS NULL` (this is what Task 3 GET reads). Each arm honest verdict (graded / not_measured-with-real-error) IS the engine-health signal. Coordinate note: if Phase 137.2 (CREATE-01) later lands a shared hidden/built-in skill mechanism, the fixture MAY migrate onto it — leave a comment flagging that coordination. Extend `test_evals_router.py`: assert the sweep creates NO rows in skills/skill_versions/skill_test_cases; its eval_runs rows carry NULL skill_id/skill_version_id (owner-stamped from current_user); a skill-scoped run-list read for any real skill does NOT return sweep rows; and the not_measured path carries the real provider error.</action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_evals_router.py -k "sweep or engine" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The sweep route creates NO row in `skills`, `skill_versions`, or `skill_test_cases` (test asserts all three untouched) — in-memory synthesis + NULL-FK persistence only.
    - Sweep eval_runs rows persist with `skill_id IS NULL` + `skill_version_id IS NULL` + `matrix_group_id` set + `feeds_gate=false`, owner-stamped from current_user; eval_results rows persist `test_case_id IS NULL`.
    - The sweep spawns one arm per configured provider via run_eval_job EXISTING skill_version/cases params (no runner signature change in this plan).
    - A skill-scoped run-history read returns NO sweep rows (no Studio leak — test-proven).
    - The latest-sweep lookup (`matrix_group_id IS NOT NULL AND skill_id IS NULL`, newest per user) is implemented and test-covered (Task 3 consumes it).
    - A provider with no key surfaces an honest not_measured carrying the real provider error (not an engine-shaped error).
  </acceptance_criteria>
  <done>Engine sweep rides the matrix machinery over an in-memory skill-less fixture (NULL FKs, no seeded rows); no Skills-list or RunHistory pollution; honest per-arm engine health; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET engine-health route — the latest-sweep board read (Blocker 2 / D-03)</name>
  <files>backend/app/api/evals.py, backend/tests/test_evals_router.py</files>
  <behavior>
    - GET engine-health returns the caller LATEST smoke-sweep board: one tile per swept provider with provider, model, healthy, error (verbatim provider error or null), run_id, last_swept_at — the EngineHealthBoard shape from Plan 01 api.ts/types.
    - Owner-scoped: it reads only the caller sweep groups (.eq("user_id")); another user sweep is invisible; never-swept returns an honest empty board (tiles: [], last_swept_at: null), not a 404.
    - healthy derivation is server-side and honest: an arm that completed with a graded verdict is healthy; a run/arm that failed or is not_measured renders healthy=false with the VERBATIM provider error — never an engine-shaped error.
  </behavior>
  <read_first>
    - backend/app/api/evals.py (Task 2 latest-sweep lookup — matrix_group_id IS NOT NULL AND skill_id IS NULL, newest per user; the existing owner-scoped GET readout helpers + run_in_threadpool wrapping)
    - frontend/src/types/index.ts (the EngineHealthBoard/EngineHealthTile shape from Plan 01 — mirror it exactly)
  </read_first>
  <action>
    Add `GET /evals/engine-health` (or the path Plan 01 api.ts getEngineHealth targets — keep them identical): resolve the caller latest sweep group (newest eval_runs.created_at where `matrix_group_id IS NOT NULL AND skill_id IS NULL AND user_id = current_user`, wrapped in run_in_threadpool per D-v2.5-01), then compose one tile per arm in that group: `provider`, `model`, `healthy` (true iff the arm run completed AND its with/without arms reached honest terminals with a graded verdict; false for failed/not_measured arms), `error` (the VERBATIM provider/arm error string when unhealthy, else null — never an engine-shaped message), `run_id` (deep-link target for the Studio run detail), and `last_swept_at` (the group created_at). Never swept -> `{tiles: [], last_swept_at: null}` (200, honest empty — the Settings card renders "never swept"). No caching, no scheduler (D-04 — on-demand read). Extend `test_evals_router.py`: latest-group-only (an older sweep group is ignored), owner-scoping (user B sweep invisible to user A), verbatim error included on an unhealthy tile, and the never-swept empty shape.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_evals_router.py -k "engine_health or health_board" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The GET route returns the EngineHealthBoard shape (tiles: provider/model/healthy/error/run_id + last_swept_at) for the caller LATEST sweep group only.
    - Owner-scoped: cross-user sweeps are invisible (test-proven); never-swept returns the honest empty board, not 404/500.
    - An unhealthy tile carries the verbatim provider error; a healthy tile carries error=null.
    - The route path matches Plan 01 api.ts getEngineHealth (one contract, no drift).
  </acceptance_criteria>
  <done>Plan 10 getEngineHealth() has a real backend: the owner-scoped latest-sweep board endpoint exists with honest tiles; tests green.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_evals_router.py -x` green.
- One SET NX claim per group; exactly one feeds_gate per matrix; gate reads that run only.
- Sweep creates no skills/skill_versions/skill_test_cases rows; NULL-FK persistence; no skill-view leak; honest not_measured preserved.
- GET engine-health returns the caller latest sweep board (owner-scoped, verbatim errors, honest empty when never swept).
</verification>

<success_criteria>
One click fans a skill's eval across N providers as N parallel runs under one group claim with one designated gate-feeder; the engine smoke sweep rides that same machinery over a built-in in-memory SKILL-LESS fixture (NULL FKs, zero seeded rows — deploy-parity-safe) to assert per-provider ENGINE health; and the GET engine-health endpoint serves the latest board to Settings — all owner-scoped, none hand-rolling orchestration.
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-04-SUMMARY.md` when done.
</output>
