---
phase: 104-pm-flagship-content-pack
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - scripts/pm-pack/scoreboard_smoke.py
  - .planning/phases/104-pm-flagship-content-pack/104-VALIDATION.md
  - .planning/phases/104-pm-flagship-content-pack/104-HUMAN-UAT.md
autonomous: false
requirements: [PM-01]

must_haves:
  truths:
    - "The headline demo runs end-to-end: kicking off the seeded Weekly Status Report def via the shipped single-textarea kickoff produces a cited, integrity-checked .docx from the demo KB (SC#2)"
    - "A domain author authors the Project Charter live through the Workflows page (describe→draft→refine→publish, TEXT deliverable, no template, no code) — the SC#1/SC#3 author-driven proof"
    - "A domain author re-authors a seeded pack workflow via Tweak→v(N+1) and re-drives the live publish gauntlet (golden run + judge) — proving QUAL-01 + the SC#3 re-author path, with v1 frozen by the immutability trigger"
    - "The SC#10 4-axis cross-provider scoreboard is authored in 104-VALIDATION.md (cross-provider tiers + multi-tool + parallel-thread + long-message truncation row) using EXACT registry model IDs"
  artifacts:
    - path: "scripts/pm-pack/scoreboard_smoke.py"
      provides: "An opt-in cross-provider kickoff harness that runs the seeded Status def per pinned model ID and records the per-model outcome (the automatable backbone of the SC#10 scoreboard)"
      min_lines: 80
    - path: ".planning/phases/104-pm-flagship-content-pack/104-VALIDATION.md"
      provides: "The finalized Nyquist contract + per-task map + the SC#10 manual scoreboard rows"
    - path: ".planning/phases/104-pm-flagship-content-pack/104-HUMAN-UAT.md"
      provides: "The live UAT runbook (headline run + Charter author proof + Tweak/publish proof + the scoreboard)"
  key_links:
    - from: "scripts/pm-pack/scoreboard_smoke.py"
      to: "the seeded Status def via POST /threads + POST /threads/{id}/messages"
      via: "workflow_definition_id from pm_pack_ids.json, model override per row"
      pattern: "workflow_definition_id"
    - from: "104-VALIDATION.md"
      to: "the SC#10 4-axis bandwidth"
      via: "cross-provider tier rows + the multi-tool/parallel-thread/long-message axes"
      pattern: "MiniMax-M3"
---

<objective>
Stand up the live proof orchestration for the phase's three Success Criteria: the headline status-report run (SC#2), the Charter author-driven proof + the Tweak→v(N+1)/publish-gauntlet proof (SC#1/SC#3), and the SC#10 cross-provider scoreboard. Author the cross-provider kickoff harness, finalize 104-VALIDATION.md, and write the live UAT runbook.

Purpose: The seeded defs (Plan 02) ship as data — the QUAL-01 quality bar (golden run + judge) is proven ONCE on the live gauntlet via a verifier-driven Tweak fork (RESEARCH §6 reasoned recommendation), and the SC#10 cross-provider coverage is a model-list problem run through ONE shared gateway path. This plan does NOT seed new defs and ships ZERO engine code — it drives the shipped kickoff/publish/authoring surfaces and records evidence.

Output: the cross-provider smoke harness + the finalized VALIDATION.md + the HUMAN-UAT runbook, plus the live runs they describe executed at the human-verify checkpoint.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-pm-flagship-content-pack/104-CONTEXT.md
@.planning/phases/104-pm-flagship-content-pack/104-RESEARCH.md
@.planning/phases/104-pm-flagship-content-pack/104-PATTERNS.md
@.planning/phases/104-pm-flagship-content-pack/104-VALIDATION.md
@.planning/phases/104-pm-flagship-content-pack/104-02-SUMMARY.md
@scripts/pm-pack/pm_pack_ids.json

<interfaces>
<!-- The shipped surfaces this plan DRIVES (read-only reuse; no code change to any of them). -->

Run kickoff (no bespoke route — threads.py byte-identical, G-5):
  POST /threads  → returns a thread id.
  POST /threads/{id}/messages with MessageCreate { content: "<kickoff_prompt>", workflow_definition_id: <UUID> }
  (message.py:8-18 — content + workflow_definition_id are the only run-bearing fields).
  Scope is baked into the def (definition.project_folder_id, threads.py:1326-1359) — NOT chosen at send-time.

Publish gauntlet (the QUAL-01 quality bar — driven on a Tweak fork, not at seed time):
  POST /workflows/{id}/publish with PublishRequest { golden_input } → PublishVerdict
  (workflows.py:166-213). Stages: definition validation → business_requirement (400 if missing) →
  structural lint → interactive-phase pre-block → REAL golden run (is_golden_run=true, no mocks) →
  judge HARD blocker → flip. Independent cross-provider judge (resolve_judge_model).

Tweak → v(N+1) fork (the SC#3 re-author path):
  WorkflowsPage onTweak (WorkflowsPage.tsx:285-310) → createWorkflowDraft INSERT with
  version=published_N+1, status="draft", SAME slug → loads the fork into the Builder with a NEW id.
  The frozen published v1 is NEVER UPDATEd (immutability trigger).

Charter author-driven proof (SC#1/SC#3 — TEXT deliverable, page-authored):
  POST /workflows/generate (workflow_authoring.py — DELIVERABLE RULE: no template listed →
  llm_single/llm_agent TEXT, never render_template). Then create-draft → publish through the gauntlet.

Cross-provider = a model-list problem (S-7): the emit makes ONE sealed forced_emit shot
  (phase_types.py:1187-1195) — no provider branch. The only per-provider branch is dispatcher.py:106-127.
  EXACT registry model IDs (config.py:206-313) — a case miss silently degrades to coerce.
</interfaces>

<scoreboard_model_ids>
<!-- EXACT registry IDs (config.py) — D-104-6 / S-7. A wrong case → silent coerce. -->
TIER-FORCE + strict:   OpenAI gpt-5.4 ; DeepSeek deepseek-v4-pro
TIER-FORCE (no strict): Anthropic claude-opus-4-8 (thinking off) ; Google gemini-2.5-pro ; MiniMax MiniMax-M3 (PascalCase) ; GLM glm-4.6
TIER-COERCE:           Moonshot kimi-k2.6
Long-deliverable row:  DeepSeek deepseek-v4-pro OR Moonshot kimi-k2.6 — expected to fire is_truncated as an HONEST failure, never a half-emitted .docx.
Charter NL-authoring row: OpenAI gpt-5.4 + DeepSeek deepseek-v4-pro — confirm the Phase-103 strict=False override avoids a strict-mode 400.
Known risk: reported-bug minimax-m3-invalid-tool-args-400 — flag the MiniMax row as a watch; the exact PascalCase ID mitigates the case-drop path.
</scoreboard_model_ids>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-provider kickoff smoke harness + finalize VALIDATION.md</name>
  <read_first>
    - scripts/longmsg_workflow_smoke.py (an existing opt-in workflow-kickoff smoke harness to mirror — auth, thread create, message kickoff)
    - scripts/eval_cross_provider.py (the existing cross-provider model-iteration pattern)
    - backend/app/api/threads.py:1155,1326-1359 (the kickoff path — read-only; scope from definition.project_folder_id)
    - scripts/pm-pack/pm_pack_ids.json (the seeded def ids + folder id the harness consumes)
    - .planning/phases/104-pm-flagship-content-pack/104-VALIDATION.md (the scaffold to finalize)
    - .planning/phases/104-pm-flagship-content-pack/104-RESEARCH.md (## Validation Architecture + Q8 scoreboard rows)
  </read_first>
  <action>
Create `scripts/pm-pack/scoreboard_smoke.py` — an opt-in harness (mirror `scripts/longmsg_workflow_smoke.py`'s auth + thread-create + kickoff mechanics) that, for each pinned model ID (the `<scoreboard_model_ids>` list, passed as `--models` or defaulted), kicks off the seeded Weekly Status Report def: read `scripts/pm-pack/pm_pack_ids.json` for the Status `def_id`; `POST /threads` → `POST /threads/{id}/messages` with `{content: "<kickoff_prompt>", workflow_definition_id: <status def_id>}` and the model override per row; poll the run to terminal; record per-model outcome to `scripts/pm-pack/out/scoreboard-<timestamp>.json`: `{model, tier, run_id, terminal_state, produced_file (path or null), citation_gate (pass/fail), integrity_gate (pass/fail), truncated (bool), honest_failure (bool), narrated_text (bool)}`. Bind the model override the same way the existing smoke harness does (per-message model field or the thread's model setting — match the shipped kickoff contract; do NOT invent a new field). The harness MUST NOT modify any backend code — it is a pure client. Bound it opt-in (a `--run` flag or an env guard) so it never auto-fires expensive provider calls.

Then finalize `.planning/phases/104-pm-flagship-content-pack/104-VALIDATION.md`:
  - Set the Test Infrastructure quick-run command to `cd backend && venv/Scripts/python -m pytest tests/unit/test_pm_pack_templates.py tests/integration/test_seed_pm_pack.py -q` and confirm runtime.
  - Populate the Per-Task Verification Map with every task across plans 01/02 (this plan's runs are manual): 104-01-01..03 (template/corpus + template-shape test, unit, `pytest tests/unit/test_pm_pack_templates.py`), 104-02-01..03 (seed script + def-shape + seed-smoke/RLS/immutability, integration, `pytest tests/integration/test_seed_pm_pack.py`), with their threat refs (T-104-01-01, T-104-02-01..06) and the secure behavior each asserts.
  - Map the critical failure modes from RESEARCH §Validation Architecture to coverage: citation-gate false-green (Plan 02 def shape + the live headline run red-case), integrity-gate false-green (output_file_valid unit + live), RLS pollution (Plan 02 Test 4), immutability UPDATE (Plan 02 Test 3), GLM/MiniMax case-miss silent coerce (the scoreboard exact-ID rows + a wrong-case negative control), reasoning-truncation half-emit (the long-deliverable manual row).
  - Author the Manual-Only SC#10 4-axis scoreboard rows VERBATIM from `<scoreboard_model_ids>`: cross-provider (one row per tier representative), multi-tool (the 2-phase retrieve+emit is inherently 2-tool — note it), parallel-thread (Thread A streaming a status run while Thread B accepts a new prompt), long-message (the DeepSeek/Moonshot truncation row, MANUAL per provider). Add the Charter NL-authoring OpenAI+DeepSeek row. Flag the MiniMax row with the `minimax-m3-invalid-tool-args-400` watch.
  - Set `nyquist_compliant: true` in the frontmatter and complete the Validation Sign-Off checklist.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -c "import ast; ast.parse(open('scripts/pm-pack/scoreboard_smoke.py').read()); print('harness parses ok')" && grep -q "MiniMax-M3" ".planning/phases/104-pm-flagship-content-pack/104-VALIDATION.md" && grep -q "nyquist_compliant: true" ".planning/phases/104-pm-flagship-content-pack/104-VALIDATION.md" && echo "VALIDATION finalized"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/pm-pack/scoreboard_smoke.py` exists, parses, reads `pm_pack_ids.json`, kicks off via `POST /threads/{id}/messages` with `workflow_definition_id` (grep: `workflow_definition_id`), and is opt-in gated (grep: a `--run` flag or env guard).
    - The harness modifies NO backend code (it is a pure client; grep confirms no import of `app.api`/`app.services` write paths beyond read-only helpers).
    - 104-VALIDATION.md has `nyquist_compliant: true`, a populated Per-Task Map covering all Plan 01/02 tasks with threat refs, and the SC#10 manual scoreboard rows containing the EXACT IDs gpt-5.4, deepseek-v4-pro, claude-opus-4-8, gemini-2.5-pro, MiniMax-M3, glm-4.6, kimi-k2.6.
    - The MiniMax row carries the `minimax-m3-invalid-tool-args-400` watch note.
  </acceptance_criteria>
  <done>The cross-provider kickoff harness exists and VALIDATION.md is finalized with the full SC#10 4-axis scoreboard + per-task map + nyquist_compliant true.</done>
</task>

<task type="auto">
  <name>Task 2: Write the live UAT runbook (104-HUMAN-UAT.md)</name>
  <read_first>
    - .planning/phases/103-workflows-page-authoring-api-nl-authoring/103-HUMAN-UAT.md (the prior phase's UAT runbook format to mirror)
    - .planning/phases/104-pm-flagship-content-pack/104-RESEARCH.md (Q5 Charter proof, Q6 publish gauntlet, Q7 kickoff, Q8 scoreboard)
    - scripts/pm-pack/pm_pack_ids.json (the seeded def ids the runbook references)
    - C:/Users/fhdmr/.claude/projects/.../MEMORY.md context: dev app http://localhost:5173/, login fhdmrd@gmail.com / 123456 (the live UAT target)
  </read_first>
  <action>
Create `.planning/phases/104-pm-flagship-content-pack/104-HUMAN-UAT.md` — the operator/orchestrator live-UAT runbook (mirror 103-HUMAN-UAT.md format). Sections:
  - **Pre-req:** the seed has run (Plan 02), `pm_pack_ids.json` exists, an embedding key + provider keys are configured, the app is at http://localhost:5173/ (login fhdmrd@gmail.com / 123456), the backend is up.
  - **UAT-1 SC#2 headline run:** kick off the seeded Weekly Status Report (via the Workflows page Run modal OR `scripts/pm-pack/scoreboard_smoke.py --models gpt-5.4 --run`); assert a `.docx` is produced, citations_required passes (no uncited/invented leaves), output_file_valid re-opens it clean; download the `.docx` and confirm it OPENS in a real editor with cited non-null values + a non-blank Risk-style/section content. The strongest evidence bar (the spike's) = opens clean in a real editor.
  - **UAT-2 SC#2 citation red-case:** kick off the Status def against a model/prompt that would invent (or point at an empty scope) — assert it HONEST-FAILS the citation gate (a visible failure naming the gate), NEVER a silent .docx with invented values.
  - **UAT-3 SC#1/SC#3 Charter author-driven proof:** through the Workflows page, describe a Project Charter (no template) → AI drafts a TEXT (llm_single/llm_agent) deliverable, NOT a render_template phase → refine by form → publish through the live gauntlet → confirm published. (Author rows for OpenAI gpt-5.4 + DeepSeek deepseek-v4-pro to confirm the strict=False override avoids a 400.)
  - **UAT-4 SC#3 + QUAL-01 Tweak→v(N+1) + live publish gauntlet:** open the seeded Weekly Status Report → Tweak → a v2 DRAFT (new id, same slug, version 2) → edit a prompt → drive `POST /workflows/{id}/publish` with a golden_input → confirm the REAL golden run + judge PASS and the def flips published; psycopg2-confirm the v1 row is UNCHANGED (immutability) and a v2 row exists. This single live run is the QUAL-01 proof (RESEARCH §6) AND the SC#3 re-author proof. Repeat the publish fork on the Risk Register to confirm IT passes the gauntlet too.
  - **UAT-5 SC#10 scoreboard (the headline acceptance bar):** drive `scripts/pm-pack/scoreboard_smoke.py --run` across the pinned tier IDs; fill the 104-VALIDATION.md scoreboard from `scripts/pm-pack/out/scoreboard-*.json`. Assert: FORCE-tier produce a clean cited `.docx`; COERCE-tier (kimi-k2.6) either coerce-succeed OR honest-fail (NEVER silent narration); the long-deliverable DeepSeek/Moonshot row fires `is_truncated` as an HONEST failure (not a half-emit); the parallel-thread axis (Thread A streaming while Thread B accepts a prompt) holds; the multi-tool axis is satisfied by the 2-phase pipeline. Use EXACT PascalCase for MiniMax-M3 / glm-4.6; watch minimax-m3-invalid-tool-args-400.
  Each UAT row has: steps, expected observable, pass/fail, evidence (run_id / downloaded-file path / psql read-back).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "UAT-1" ".planning/phases/104-pm-flagship-content-pack/104-HUMAN-UAT.md" && grep -q "Tweak" ".planning/phases/104-pm-flagship-content-pack/104-HUMAN-UAT.md" && grep -q "kimi-k2.6" ".planning/phases/104-pm-flagship-content-pack/104-HUMAN-UAT.md" && echo "UAT runbook authored"</automated>
  </verify>
  <acceptance_criteria>
    - `104-HUMAN-UAT.md` exists with 5 UAT sections (headline run, citation red-case, Charter author proof, Tweak/publish gauntlet, cross-provider scoreboard).
    - UAT-4 explicitly drives the LIVE publish gauntlet on a Tweak v2 fork (the QUAL-01 + SC#3 proof) and asserts v1 immutability via psql read-back.
    - UAT-5 lists the EXACT tier model IDs + the long-deliverable truncation row + the parallel-thread axis + the MiniMax watch.
    - Each UAT row specifies steps, expected observable, and an evidence field.
  </acceptance_criteria>
  <done>The live UAT runbook is authored covering all 3 SCs + SC#10 with concrete steps + evidence fields.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the live UAT — SC#2 headline + SC#1/SC#3 author proofs + SC#10 scoreboard</name>
  <what-built>
    The seeded PM pack (2 published 2-phase defs + bound templates + synthetic KB, Plan 02), the cross-provider kickoff harness + finalized SC#10 scoreboard (Task 1), and the live UAT runbook (Task 2). Everything needed to run the phase's three Success Criteria live is in place.
  </what-built>
  <how-to-verify>
    Run the 104-HUMAN-UAT.md runbook against the live app (http://localhost:5173/, login fhdmrd@gmail.com / 123456; backend up; embedding + provider keys configured; the seed already run):
    1. **SC#2 headline (UAT-1/2):** kick off the seeded Weekly Status Report → a cited, integrity-checked `.docx` is produced; download it and confirm it opens clean in a real editor with cited non-null values. Confirm the citation red-case honest-fails (no silent invented .docx).
    2. **SC#1/SC#3 Charter (UAT-3):** author a Project Charter live through the Workflows page (describe→draft→refine→publish, TEXT deliverable, no template) for OpenAI + DeepSeek authoring rows.
    3. **SC#3 + QUAL-01 (UAT-4):** Tweak the seeded Status def to a v2 draft → drive the live publish gauntlet (golden run + judge) → confirm it passes and flips published; psql-confirm v1 is frozen. Repeat the publish fork on the Risk Register.
    4. **SC#10 (UAT-5):** run `scripts/pm-pack/scoreboard_smoke.py --run` across the pinned tier IDs; fill the VALIDATION.md scoreboard. Confirm FORCE-tier produce clean cited .docx, COERCE-tier honest-fail-or-coerce (no silent narration), the long-deliverable row fires is_truncated as an honest failure, and the parallel-thread axis holds. Use EXACT MiniMax-M3 / glm-4.6.
    Record run_ids / downloaded-file paths / psql read-backs as evidence in 104-HUMAN-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" once all UAT rows pass (or describe the failures — gaps route to a 104 gap-closure plan).</resume-signal>
  <action>Execute the 104-HUMAN-UAT.md runbook live (see how-to-verify): run SC#2 headline + citation red-case, SC#1/SC#3 Charter author proof, SC#3+QUAL-01 Tweak→v(N+1) live publish gauntlet, and the SC#10 cross-provider scoreboard. Record run_ids / downloaded-file paths / psql read-backs as evidence in 104-HUMAN-UAT.md. This task is operator/orchestrator-driven (no autonomous code change).</action>
  <verify>All 5 UAT rows in 104-HUMAN-UAT.md pass with recorded evidence; the SC#10 scoreboard in 104-VALIDATION.md is filled from scripts/pm-pack/out/scoreboard-*.json.</verify>
  <done>SC#2 produces a cited integrity-checked .docx that opens clean; the Charter is page-authored (TEXT, no template); a seeded def re-passes the live publish gauntlet on a Tweak v2 fork with v1 frozen; the cross-provider scoreboard is filled (FORCE-tier clean cited .docx, COERCE-tier honest-fail-or-coerce, long-deliverable honest truncation, parallel-thread holds).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scoreboard harness → kickoff API | The harness is a client posting kickoffs as the demo user — it inherits the shipped run path's RLS/scope binding (scope baked into the def, not chosen at send-time). |
| Tweak fork → publish gauntlet | Re-authoring forks a NEW draft (INSERT) and re-drives the real golden run + judge — the published v1 stays frozen by the immutability trigger. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03-01 | Spoofing (scoreboard false-green) | A model that narrates the field-map as text instead of forcing | mitigate | The scoreboard records `narrated_text`/`honest_failure` distinctly; a COERCE-tier (kimi) row passes ONLY on a real coerce-success or an honest failure, NEVER silent narration. EXACT registry IDs (no case-miss coerce). The acceptance bar in UAT-5 enforces this. |
| T-104-03-02 | Information Disclosure (scoreboard burns provider spend) | The cross-provider runs | accept | The harness is opt-in gated (--run / env guard) and bounded to the pinned representative IDs; a real golden run + per-tier kickoff is the intended cost of the SC#10 acceptance bar. No control beyond opt-in gating. |
| T-104-03-03 | Tampering (publish bypasses the judge) | The Tweak-fork publish | mitigate | The publish gauntlet's judge is a HARD blocker (publish_service.py:264-294) — driven live on the real golden run, no mocks. UAT-4 confirms a lint-clean + judge-fail CANNOT publish. Engine-side control (shipped); UAT exercises it live. |
| T-104-03-04 | Tampering (re-author mutates v1) | The Tweak→v(N+1) fork | mitigate | Tweak is an INSERT of a NEW draft (new id, same slug, version+1); the immutability trigger blocks any UPDATE of the published v1. UAT-4 psql-confirms v1 is unchanged. |
</threat_model>

<verification>
- `scripts/pm-pack/scoreboard_smoke.py` parses and kicks off the seeded Status def per pinned model.
- 104-VALIDATION.md is finalized (nyquist_compliant true, per-task map, SC#10 scoreboard rows).
- 104-HUMAN-UAT.md covers all 3 SCs + SC#10.
- The human-verify checkpoint passes: the headline run produces a cited .docx, the Charter is page-authored, the Tweak fork passes the live gauntlet, and the cross-provider scoreboard is filled.
</verification>

<success_criteria>
- SC#2: the seeded Weekly Status Report produces a cited, integrity-checked `.docx` from the demo KB via the shipped kickoff (no bespoke route).
- SC#1/SC#3: the Charter is authored live through the Workflows page (TEXT, no template, no code); a seeded def is re-authored via Tweak→v(N+1) and re-passes the live publish gauntlet with v1 frozen.
- SC#10: the 4-axis cross-provider scoreboard is authored in VALIDATION.md and run live (FORCE-tier clean cited .docx; COERCE-tier honest-fail-or-coerce; long-deliverable honest truncation; parallel-thread holds).
- ZERO engine code, ZERO new route, ZERO new migration; threads.py byte-identical (read-only reuse).
</success_criteria>

<output>
After completion, create `.planning/phases/104-pm-flagship-content-pack/104-03-SUMMARY.md`
</output>
