---
phase: 104-pm-flagship-content-pack
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/seed-pm-pack.py
  - scripts/pm-pack/pm_pack_ids.json
  - backend/tests/integration/test_seed_pm_pack.py
autonomous: true
requirements: [PM-01]
user_setup:
  - service: openai-embeddings
    why: "One live embeddings call per corpus doc at seed time (the documents ingest path is hard-wired to OpenAI embeddings)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "backend/.env (already present for the running app) — read NAME-ONLY by the seed via dotenv"

must_haves:
  truths:
    - "Running scripts/seed-pm-pack.py provisions the whole pack atomically + idempotently: demo folder → 5 corpus docs ingested → 2 docx templates uploaded to Storage → 2 published workflow_definitions rows → pm_pack_ids.json"
    - "Re-running the seed produces NO duplicate rows (corpus dedups via sha256; defs refresh via DELETE-then-INSERT, never UPDATE)"
    - "The two seeded defs are 2-phase llm_agent(search_documents)→llm_emit(render_template) with assets[kind=template], gates, business_requirement, project_folder_id, is_global=false"
    - "Every seeded row + Storage object is scoped to the demo user's uid (RLS-correct, no cross-tenant leak)"
  artifacts:
    - path: "scripts/seed-pm-pack.py"
      provides: "The idempotent opt-in provisioning script (the ONLY substantive net-new code)"
      min_lines: 200
    - path: "scripts/pm-pack/pm_pack_ids.json"
      provides: "The emitted fixture manifest (def ids, asset paths, folder id) for the verifier + scoreboard"
    - path: "backend/tests/integration/test_seed_pm_pack.py"
      provides: "Seed-smoke + idempotency + RLS-isolation + immutability + def-shape assertions"
  key_links:
    - from: "scripts/seed-pm-pack.py"
      to: "Supabase Storage workspace-files bucket at {demo_uid}/_library/<slug>.docx"
      via: "supabase.storage.upload + the exact path string becomes AssetRef.asset_id"
      pattern: "_library/"
    - from: "the seeded def JSONB"
      to: "_emit_bound_asset_ref server-side selection"
      via: "assets[] entry with kind=='template'"
      pattern: "\"kind\": \"template\""
    - from: "the seeded emit phase"
      to: "the upstream retrieval phase's source_refs (the citation valid-id set)"
      via: "2-phase llm_agent→llm_emit ordering (phase_index 0 then 1)"
      pattern: "search_documents"
---

<objective>
Write `scripts/seed-pm-pack.py` — the idempotent, opt-in provisioning script that ships the PM pack as DATA on the already-shipped primitives. It mirrors `backend/tests/fixtures/seed_library_asset.py` mechanic-for-mechanic but authors the def JSONB to the 2-phase `llm_emit` shape (the S-5 DRIFT FLAG). Plus the seed-smoke / idempotency / RLS / immutability / def-shape test.

Purpose: This script is the integration point — it ingests the Plan-01 corpus, uploads the Plan-01 templates, resolves the demo folder id, and INSERTs the two published 2-phase defs with `assets[]` baked in. It is the ONLY substantive net-new code in the phase. ZERO engine code, ZERO new route, ZERO new migration.

Output: The provisioning script + the `pm_pack_ids.json` manifest it emits + the integration test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-pm-flagship-content-pack/104-CONTEXT.md
@.planning/phases/104-pm-flagship-content-pack/104-RESEARCH.md
@.planning/phases/104-pm-flagship-content-pack/104-PATTERNS.md
@.planning/phases/104-pm-flagship-content-pack/104-01-SUMMARY.md

<canonical_analog>
THE recipe to mirror mechanic-for-mechanic: `backend/tests/fixtures/seed_library_asset.py` (read it in full).
Copy: the dotenv NAME-ONLY bootstrap, `get_supabase()` service-role client, `_db_dsn()`, the
`BUCKET="workspace-files"` + `{uid}/_library/<slug>.docx` Storage upload with byte-round-trip assert,
the DELETE-then-INSERT `upsert_definition` (SELECT existing definition::text → if differs DELETE →
INSERT 'published' → ON CONFLICT DO NOTHING → read-back assert), and `emit_ids`.
DEVIATE ONLY on: (a) `parents[1]` not `parents[3]` (the script lives at repo-root `scripts/`);
(b) the def JSONB is the 2-phase shape (S-5); (c) a NET-NEW corpus-ingest step before the def INSERT.
</canonical_analog>

<interfaces>
<!-- Exact field contracts the seeded def JSONB MUST satisfy (harness.py, verified this session). -->

LlmAgentPhaseConfig (harness.py:77-91): `available_tools: list[str]` is REQUIRED (no default).
  The retrieval phase MUST declare `["search_documents"]` or model_validate() fails.
LlmEmitPhaseConfig (harness.py:123-154): phase_type "llm_emit", prompt, emitter default "render_template",
  citation_policy/integrity_policy default "strict". The emit phase does NOT carry available_tools
  (it is a sealed forced shot; the emitter resolves the bound template server-side).
ValidatorSpec (harness.py:170-186): kind includes "citations_required" + "output_file_valid";
  config defaults {}; on_failure defaults "fail_run"; timing defaults "post".
AssetRef (harness.py:214-218): {asset_id:str (the Storage PATH string), filename:str,
  kind:Literal["template","reference"], mime:str}.
WorkflowDefinition (harness.py:221-254): _folder_scope_requires_project model_validator RAISES if
  any phase declares folder_scope without project_folder_id set → both PM phases set folder_scope, so
  project_folder_id MUST be set. business_requirement OPTIONAL on schema, REQUIRED at publish.

The retrieval grounding output shape the emit consumes (test_llm_emit_executor.py:111-116):
  `{"retrieval": {"text": "...", "source_refs": [{"chunk_id": "<id>"}, ...]}}` — the upstream
  `llm_agent` `search_documents` phase threads this up; `_emit_evidence`/`_retrieved_ids` build the
  citation valid-id set from it.

The 2-phase def JSONB skeleton (the EXACT shape to author per def — RESEARCH Q2 / PATTERNS):
{
  "slug": "pm-weekly-status-report", "version": 1, "name": "Weekly Status Report", "status": "published",
  "project_folder_id": "<demo folder id resolved at seed time>",
  "business_requirement": "Produce a cited weekly status report from the project KB with overall RAG status, accomplishments, planned work, risks/blockers, and milestones.",
  "phases": [
    { "slug": "retrieve", "phase_index": 0,
      "config": { "phase_type": "llm_agent",
        "prompt": "Search the project KB for the latest reporting-period status: accomplishments, planned next steps, risks/blockers, milestones, overall RAG status.",
        "available_tools": ["search_documents"],
        "folder_scope": ["<demo folder id>"] },
      "validators": [] },
    { "slug": "emit", "phase_index": 1,
      "config": { "phase_type": "llm_emit", "emitter": "render_template",
        "prompt": "Fill the weekly-status-report template from the retrieved KB evidence. Cite every non-null value; set null where the sources do not support a value.",
        "folder_scope": ["<demo folder id>"],
        "citation_policy": "strict", "integrity_policy": "strict" },
      "validators": [
        { "kind": "citations_required", "config": { "mode": "deterministic" }, "on_failure": "fail_run" },
        { "kind": "output_file_valid", "config": {}, "on_failure": "fail_run" }
      ] } ],
  "assets": [ { "asset_id": "{demo_uid}/_library/pm-weekly-status-report.docx",
    "filename": "weekly-status-report.docx", "kind": "template",
    "mime": "application/vnd.openxmlformats-officedocument.wordprocessingml.document" } ]
}

The Risk Register def is structurally identical: slug "pm-risk-register", name "Risk Register",
a risk-flavored business_requirement + prompts, assets[0].asset_id pointing at the risk-register.docx
Storage path, filename "risk-register.docx".
</interfaces>

<load_bearing_constraints>
- **2-phase, NOT 1-phase (S-5 DRIFT FLAG, load-bearing):** the fixture's fill is a single `llm_agent`
  with `available_tools:["search_documents","render_template"]`. The PM defs MUST be the 2-phase
  `llm_agent`(search_documents)→`llm_emit`(render_template) shape. A lone emit phase has an empty
  citation valid-id set (`_emit_evidence`/`_retrieved_ids`, phase_types.py:789-857) → every value
  reads "invented" → honest-fail under strict. Do NOT put `render_template` in any `available_tools`
  (the emit phase resolves the template server-side; the retrieval phase only needs `search_documents`).
- **The read-back assert CHANGES from the fixture:** assert the def has a `kind:"template"` entry in
  `assets[]` AND the RETRIEVAL phase declares `available_tools:["search_documents"]` AND the emit
  phase is `phase_type:"llm_emit"`. Do NOT copy the fixture's `render_template`-on-the-agent-phase
  assertion (that is the 1-phase path).
- **NO interactive phase / NO ask_user-routed validator** (S-8): the publish gauntlet pre-run-blocks a
  def with an `llm_human_input` phase or a validator whose `on_failure` routes to `ask_user`
  (publish_service.py:153-171). Keep every gate `on_failure:"fail_run"`. Do NOT attach a `freshness`
  validator with `on_failure:"ask_user"`.
- **DELETE-then-INSERT only (S-3):** the immutability trigger (full-schema.sql:226-247) blocks UPDATE
  on a published row. The seeder SELECTs existing `definition::text`; if it differs, DELETE then
  re-INSERT; never UPDATE.
- **Per-account, is_global=false (S-6):** every doc/def/Storage key scoped to the demo uid. NEVER global.
- **Slug guard (security):** each slug is an author-fixed constant; assert it matches `^[a-z0-9-]+$`
  before building the server-side Storage key (no path traversal).
</load_bearing_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the seed/provisioning script (bootstrap + corpus ingest + template upload)</name>
  <read_first>
    - backend/tests/fixtures/seed_library_asset.py (the FULL recipe — bootstrap, get_supabase, _db_dsn, upload_template, DELETE-then-INSERT, emit_ids)
    - backend/app/api/documents.py:338-449 (the upload route + ALLOWED_MIME_TYPES + sha256 dedup + filename versioning — the ingest mechanics)
    - backend/app/services/embedding_service.py:94-97 (the embedding hard-dep — one live call per doc)
    - .planning/phases/104-pm-flagship-content-pack/104-PATTERNS.md (S-1 Storage key, S-4 secrets, S-6 corpus ingest)
    - scripts/pm-pack/templates/ + scripts/pm-pack/sample-corpus/ (the Plan-01 artifacts this consumes)
  </read_first>
  <action>
Create `scripts/seed-pm-pack.py` at the repo root (NEVER under backend/ — uvicorn --reload watches that tree). Mirror `seed_library_asset.py`:
  - Bootstrap: `REPO_ROOT = Path(__file__).resolve().parents[1]` (the script is at scripts/, so parents[1] = repo root — NOT parents[3]); `BACKEND_DIR = REPO_ROOT / "backend"`; `sys.path.insert(0, str(BACKEND_DIR))`; `load_dotenv(BACKEND_DIR / ".env")` (secrets NAME-ONLY, never printed).
  - Constants: `DEMO_USER_ID = "d8a54002-6a29-4b88-b918-cff2aa4a06d5"` (the local dev login fhdmrd@gmail.com, from STATE.md / spike-097); `BUCKET = "workspace-files"`; `MIME = "application/vnd.openxmlformats-officedocument.wordprocessingml.document"`; `DEMO_FOLDER_NAME = "PM Demo Project (sample data)"`. Two fixed def UUIDs (e.g. `00000000-0000-0000-0000-0000001040a0` status, `00000000-0000-0000-0000-0000001040a1` risk) so re-runs are idempotent. Two template specs: (slug "pm-weekly-status-report", local path scripts/pm-pack/templates/weekly-status-report.docx, filename "weekly-status-report.docx") and (slug "pm-risk-register", local path scripts/pm-pack/templates/risk-register.docx, filename "risk-register.docx"). Assert each slug matches `^[a-z0-9-]+$` before use.
  - `get_supabase()` + `_db_dsn()`: copy verbatim from the fixture (service-role key name-only; default DSN `postgresql://postgres:postgres@127.0.0.1:54322/postgres`).
  - `resolve_demo_folder(conn) -> str`: SELECT id FROM folders WHERE user_id=DEMO_USER_ID AND name=DEMO_FOLDER_NAME AND is_global=false LIMIT 1; if absent, INSERT a per-account folder (user_id=DEMO_USER_ID, name=DEMO_FOLDER_NAME, is_global=false, parent_id NULL) RETURNING id. Return the id string. Read the folders table columns first (psql `\d folders` or grep the migrations) and match the actual schema; folders is RLS-scoped on user_id.
  - `ingest_corpus(folder_id)`: for each `scripts/pm-pack/sample-corpus/*.md`, ingest into the demo folder. PREFERRED path (RESEARCH Q4) = POST to the running backend `POST /documents/upload` (multipart `file` + Form `folder_id`) with a bearer for DEMO_USER_ID — byte-identical to a human upload; sha256 dedup (documents.py:402-423) makes re-runs short-circuit 200. If no server is up (a /health ping fails), FALL BACK to calling the in-process `_upload_pipeline` directly with a service-role supabase + DEMO_USER_ID (CONTEXT discretion — both are sanctioned). Document the chosen path in the module docstring. One live OpenAI embeddings call fires per doc — bounded to the 5 Plan-01 docs.
  - `upload_template(supabase, local_path, slug) -> str`: build `path = f"{DEMO_USER_ID}/_library/{slug}.docx"`; read the local docx bytes; `supabase.storage.from_(BUCKET).upload(path, data, {"content-type": MIME, "upsert": "true"})`; download round-trip + byte-length assert (copy the fixture's upload_template). Return the `path` string (this IS the AssetRef.asset_id).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -c "import ast; ast.parse(open('scripts/seed-pm-pack.py').read()); print('parses ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/seed-pm-pack.py` exists at the repo root (NOT under backend/), parses without SyntaxError.
    - It contains `DEMO_USER_ID = "d8a54002-6a29-4b88-b918-cff2aa4a06d5"`, `BUCKET = "workspace-files"`, and builds the Storage key as `f"{DEMO_USER_ID}/_library/{slug}.docx"` (grep: `_library/`).
    - It reads secrets via `os.environ.get(...)` only (grep finds no literal service-role/api-key strings); `load_dotenv(BACKEND_DIR / ".env")` present.
    - It asserts each slug matches `^[a-z0-9-]+$` before building the Storage key.
    - It resolves the demo folder per-account (is_global=false) and ingests the 5 corpus markdown docs.
  </acceptance_criteria>
  <done>The bootstrap + folder-resolve + corpus-ingest + template-upload halves of the seed script are written, parse clean, and follow the fixture's RLS-correct + name-only-secrets mechanics.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 2-phase def JSONB + DELETE-then-INSERT + emit the manifest</name>
  <read_first>
    - backend/tests/fixtures/seed_library_asset.py:152-216 (the DELETE-then-INSERT upsert_definition + read-back assert to generalize)
    - backend/app/models/harness.py:77-91,123-254 (LlmAgentPhaseConfig/LlmEmitPhaseConfig/ValidatorSpec/AssetRef/WorkflowDefinition + the model_validator)
    - backend/tests/unit/test_llm_emit_executor.py:111-116 (the retrieval grounding output shape)
    - supabase/migrations/061_harness_seed_templates.sql (the published-seed INSERT column set)
    - supabase/full-schema.sql:226-247 (the immutability trigger — WHY DELETE-then-INSERT)
  </read_first>
  <action>
Add to `scripts/seed-pm-pack.py`:
  - `build_status_def(folder_id, asset_path) -> dict` and `build_risk_def(folder_id, asset_path) -> dict`: return the FULL WorkflowDefinition JSONB exactly as the `<interfaces>` skeleton — 2 phases (retrieve `llm_agent` with `available_tools:["search_documents"]` + `folder_scope:[folder_id]`, phase_index 0; emit `llm_emit` `emitter:"render_template"` + `folder_scope:[folder_id]` + `citation_policy:"strict"` + `integrity_policy:"strict"`, phase_index 1 with the `citations_required`(mode deterministic) + `output_file_valid` validators, both `on_failure:"fail_run"`); `project_folder_id: folder_id`; `business_requirement` set (status: a status-report requirement string; risk: a risk-register requirement string); `assets:[{asset_id: asset_path, filename, kind:"template", mime: MIME}]`. The risk def's emit prompt instructs filling the 9-column register, citing every non-null cell, leaving null where unsupported (Score is computed by the template Jinja, not emitted by the model).
  - Validate each def dict through `from app.models.harness import WorkflowDefinition; WorkflowDefinition.model_validate(def_dict)` BEFORE the INSERT — a ValidationError must abort the seed loudly (a mis-shaped def must never land). This exercises the model_validator (folder_scope needs project_folder_id) + the discriminated-union phase configs.
  - `upsert_definition(conn, def_id, def_dict)`: generalize the fixture's recipe — `target_def = json.dumps(def_dict)`; SELECT existing `definition::text` WHERE id=def_id; if it exists and differs, DELETE (the immutability trigger is BEFORE-UPDATE only — DELETE is allowed); INSERT into `public.workflow_definitions (id, slug, version, name, status, definition, created_by, is_global) VALUES (%s,%s,%s,%s,'published',%s::jsonb,%s,false) ON CONFLICT (id) DO NOTHING`; commit. Read-back assert (psycopg2 JSONB path queries): `definition->'assets'->0->>'kind'` == 'template'; `definition->'phases'->0->'config'->'available_tools'` contains 'search_documents'; `definition->'phases'->1->'config'->>'phase_type'` == 'llm_emit'; `definition->>'business_requirement'` IS NOT NULL. Do NOT assert render_template on an agent phase (S-5).
  - `emit_ids(folder_id, status_asset, risk_asset)`: write `scripts/pm-pack/pm_pack_ids.json` with `{demo_user_id, demo_folder_id, definitions:[{def_id, slug, version, asset_id, filename}, ...]}` (a list of BOTH defs) for the verifier + scoreboard harness (Plan 03).
  - `main()`: connect psycopg2 → resolve_demo_folder → ingest_corpus → upload both templates (capture both asset paths) → build+validate+upsert both defs → emit_ids → print a one-line summary (def ids + asset paths + folder id; NO secrets). Wrap in try/finally to close the connection.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -c "import sys; sys.path.insert(0,'backend'); from dotenv import load_dotenv; load_dotenv('backend/.env'); import importlib.util as u; s=u.spec_from_file_location('seed','scripts/seed-pm-pack.py'); m=u.module_from_spec(s); s.loader.exec_module(m); from app.models.harness import WorkflowDefinition; f='11111111-1111-1111-1111-111111111111'; d=m.build_status_def(f,'d8a54002-6a29-4b88-b918-cff2aa4a06d5/_library/pm-weekly-status-report.docx'); WorkflowDefinition.model_validate(d); r=m.build_risk_def(f,'d8a54002-6a29-4b88-b918-cff2aa4a06d5/_library/pm-risk-register.docx'); WorkflowDefinition.model_validate(r); assert len(d['phases'])==2 and len(r['phases'])==2; assert d['phases'][0]['config']['available_tools']==['search_documents']; assert d['phases'][1]['config']['phase_type']=='llm_emit'; assert d['assets'][0]['kind']=='template'; print('both defs validate 2-phase + template asset')"</automated>
  </verify>
  <acceptance_criteria>
    - `build_status_def` + `build_risk_def` return dicts that pass `WorkflowDefinition.model_validate()`.
    - Each def has exactly 2 phases: phase[0] `llm_agent` with `available_tools == ["search_documents"]`; phase[1] `llm_emit` with `emitter == "render_template"`, `citation_policy == "strict"`, and the 2 validators (citations_required + output_file_valid, both on_failure "fail_run").
    - Each def has `assets[0].kind == "template"` with `asset_id` == the `{demo_uid}/_library/<slug>.docx` Storage path string; `project_folder_id` set; `business_requirement` non-null.
    - Neither def contains any `llm_human_input` phase or any validator with `on_failure: "ask_user"` (grep negative).
    - `upsert_definition` does an UPDATE-free DELETE-then-INSERT (grep: contains "DELETE FROM public.workflow_definitions" and "INSERT INTO public.workflow_definitions", no "UPDATE public.workflow_definitions" on the published row).
  </acceptance_criteria>
  <done>Both seeded defs are authored to the validated 2-phase shape with the bound template asset + strict gates + a business_requirement, refreshed via DELETE-then-INSERT, and the manifest is emitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seed-smoke + idempotency + RLS-isolation + immutability + def-shape test</name>
  <read_first>
    - backend/tests/fixtures/seed_library_asset.py (the live-DB seeding pattern this asserts the result of)
    - .planning/phases/104-pm-flagship-content-pack/104-RESEARCH.md (## Validation Architecture — the critical failure modes + Wave 0 gaps)
    - backend/tests/unit/test_publish_flip.py (the live psycopg2 :54322 pattern + immutability-trigger CheckViolation assertion to mirror)
    - supabase/migrations/019_global_folder_subtree_visibility.sql:54-62 (the pollution RLS — what RLS-isolation must prove)
  </read_first>
  <behavior>
    - Test 1 (seed smoke + idempotency): run the seed twice; after both runs exactly 2 PM workflow_definitions rows exist (slugs pm-weekly-status-report + pm-risk-register), is_global=false, created_by=DEMO_USER_ID, status='published' — no duplicates.
    - Test 2 (def shape): each seeded row's definition JSONB is 2-phase (phase[0] llm_agent search_documents, phase[1] llm_emit render_template) with assets[0].kind=='template', a non-null business_requirement, and NO ask_user/llm_human_input (so it would pass the publish gauntlet's interactive-phase pre-block).
    - Test 3 (immutability): a deliberate UPDATE of a seeded published row's definition RAISES the trigger's CheckViolation (proving DELETE-then-INSERT is the only refresh path) — then rollback (no pollution).
    - Test 4 (RLS isolation): the demo folder is is_global=false; a documents SELECT as a DIFFERENT user_id over the demo folder returns 0 rows (the corpus does not leak cross-tenant).
  </behavior>
  <action>
Create `backend/tests/integration/test_seed_pm_pack.py`. Guard the whole module with a skip if the local stack is unreachable: try a psycopg2 connect to `postgresql://postgres:postgres@127.0.0.1:54322/postgres` (or `LOCAL_DB_DSN`) in a module fixture; `pytest.skip("local Supabase :54322 not reachable")` on failure (these are live-DB integration tests, not pure units — mirror `test_publish_flip.py`'s skip-guard).

Test 1+2: import the seed module via importlib (it lives at `scripts/seed-pm-pack.py`), run `main()` twice (or call the steps with a service-role supabase + psycopg2 conn). Then psycopg2-query `SELECT slug, is_global, created_by, status, definition FROM public.workflow_definitions WHERE created_by=%s AND slug IN ('pm-weekly-status-report','pm-risk-register')` — assert exactly 2 rows, all is_global=false / status='published'. For each, parse `definition` JSON and assert: 2 phases; phase[0].config.phase_type=='llm_agent' with 'search_documents' in available_tools; phase[1].config.phase_type=='llm_emit' and emitter=='render_template'; assets[0].kind=='template'; business_requirement truthy; and assert no phase has phase_type=='llm_human_input' and no validator has on_failure=='ask_user'. (If running the full seed requires an embedding key the test env lacks, make Test 1's RE-RUN assert idempotency on the DEF rows only, and seed the corpus step behind a `SEED_PM_RUN_INGEST` env guard — the def + immutability + RLS assertions are the load-bearing ones and need no embeddings.)

Test 3: mirror test_publish_flip.py — within a transaction, `UPDATE public.workflow_definitions SET definition = definition WHERE id=<seeded status def id>` (or change a field) and assert it raises `psycopg2.errors.CheckViolation` (the workflow_definitions_block_published trigger); rollback.

Test 4: ensure the demo folder exists (is_global=false); run a documents SELECT bound as a different user — either `SELECT count(*) FROM documents WHERE folder_id=<demo folder> AND user_id <> %s` returns rows belonging only to DEMO_USER_ID, and assert the demo folder row has is_global=false (so the pollution RLS at 019 cannot surface it to other tenants). The falsifiable check: `SELECT is_global FROM folders WHERE id=<demo folder>` is false AND no document in that folder has user_id != DEMO_USER_ID.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python.exe -m pytest tests/integration/test_seed_pm_pack.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/integration/test_seed_pm_pack.py` exists; all tests PASS when :54322 is up (or SKIP cleanly when it is not).
    - Test 1 proves re-running the seed leaves exactly 2 PM def rows (idempotent, no duplicates).
    - Test 2 proves both rows are the 2-phase shape with a template asset + business_requirement + no interactive phase.
    - Test 3 proves a published-row UPDATE raises CheckViolation (immutability holds; DELETE-then-INSERT is the only path).
    - Test 4 proves the demo folder is is_global=false and the corpus is owner-scoped (no cross-tenant leak).
  </acceptance_criteria>
  <done>The seed-smoke/idempotency/immutability/RLS/def-shape integration test passes live (or skips cleanly without the stack), proving the seed provisions correctly and safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → Supabase Storage | The script writes Storage objects with the service-role key (bypasses RLS by design) — it MUST key every object under the demo uid's folder. |
| seed script → workflow_definitions / documents / folders | Service-role psycopg2 writes — RLS bypassed by design; the script MUST write only DEMO_USER_ID into every user_id/created_by/Storage path. |
| corpus content → retrieval/citation gate | Synthetic KB text is retrieved + cited; a malicious citation string in content could attempt a false-green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-02-01 | Tampering (path traversal) | The Storage key built from `<slug>` | mitigate | Each slug is an author-fixed constant; Task 1 asserts `^[a-z0-9-]+$` before building the server-side `f"{DEMO_USER_ID}/_library/{slug}.docx"` key — no user-supplied path component, no `../`. Acceptance criterion enforces the regex guard. |
| T-104-02-02 | Information Disclosure (cross-tenant pollution) | The demo corpus folder | mitigate | Seed the demo folder + every doc `is_global=false`, per-account (DEMO_USER_ID) — verified pollution mechanism (019 RLS surfaces global-folder-subtree docs to ALL users with no exclusion). Test 4 proves is_global=false + owner-scoping. NEVER seed global. |
| T-104-02-03 | Information Disclosure (secret leakage) | SUPABASE_SERVICE_ROLE_KEY / OPENAI_API_KEY / DSN | mitigate | Read NAME-ONLY from backend/.env via dotenv; never hard-coded, never printed (the fixture pattern). Acceptance criterion greps for no literal key strings; the summary line prints ids/paths only. |
| T-104-02-04 | Tampering (immutability bypass) | UPDATE of a published pack def | mitigate | DELETE-then-INSERT only; the BEFORE-UPDATE trigger backstops a stray UPDATE. Test 3 proves a published-row UPDATE raises CheckViolation. |
| T-104-02-05 | Spoofing (citation false-green via content injection) | The retrieved corpus text | accept | The emit valid-id set is computed SERVER-SIDE from retrieved refs (`_emit_evidence`, phase_types.py:800-804) — a fake `<doc id=...>` in passage text is NOT in the set. Engine-side control (shipped). Authoring discipline: the synthetic corpus contains no adversarial citation strings. No net-new control. |
| T-104-02-06 | Elevation of Privilege (wrong-uid write) | Service-role writes | mitigate | The script writes ONLY DEMO_USER_ID into every user_id/created_by/Storage path (a single constant). Test 1/4 assert created_by==DEMO_USER_ID on every seeded row. |
</threat_model>

<verification>
- `scripts/seed-pm-pack.py` parses and its `build_*_def` functions produce `WorkflowDefinition.model_validate()`-passing 2-phase defs.
- Running the seed against a live :54322 + embedding key provisions: the demo folder, 5 ingested docs, 2 uploaded templates, 2 published defs, and `pm_pack_ids.json`.
- `backend/venv/Scripts/python.exe -m pytest tests/integration/test_seed_pm_pack.py -q` is green (or skips cleanly).
</verification>

<success_criteria>
- The idempotent, opt-in seed script provisions the whole pack per-account (is_global=false) with the 2-phase defs + bound template assets + strict gates + business_requirement, and emits `pm_pack_ids.json`.
- Re-running the seed is idempotent (corpus dedups; defs DELETE-then-INSERT).
- The integration test proves seed-smoke, idempotency, immutability, RLS isolation, and the 2-phase def shape.
- ZERO files under `backend/app/**` modified; ZERO new route; ZERO new migration.
</success_criteria>

<output>
After completion, create `.planning/phases/104-pm-flagship-content-pack/104-02-SUMMARY.md`
</output>
