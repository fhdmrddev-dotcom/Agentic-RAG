---
phase: 190-live-connector-slice-connector-security-stretch
plan: 12
type: execute
wave: 4
depends_on: ["190-09"]
files_modified:
  - frontend/src/components/workflows/SelectedPhaseSlugContext.tsx
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/ConnectionPicker.tsx
  - frontend/src/components/workflows/ExternalActionSection.tsx
  - frontend/src/components/workflows/ConnectionPicker.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-02, CONN-03]

must_haves:
  truths:
    - "D-23 — frontend/src/components/workflows/PhaseFormPanel.tsx has a MEASURED git diff --numstat of 0 0; the picker is a NET-NEW SIBLING CHILD of ExternalActionSection, because onChange={set(\"capability\")} is key-bound and a fourth prop would make the panel's numstat non-zero (CONN-02)"
    - "ExternalActionSection.tsx gains EXACTLY one import and one JSX line, and its SIX source-purity fences are RE-RUN and still GREEN after the mount lands — the child import is the relative sibling form ./ConnectionPicker, which cannot match the route-string fence's leading-slash regex (CONN-02)"
    - "D-13 — the picker writes ONLY connection_id into the definition JSONB via patchConfig; T6 asserts no secret, host or token key is ever written (CONN-03)"
    - "UI-SPEC U-07a door (b) — Gate 1 is CLIENT-ONLY: a last_check_verdict=failed connection is rendered aria-disabled and refused inline, and NO copy in this plan uses an absolute verb. A binding that predates the failure is never silently unbound (CONN-02)"
    - "U-08 — the write seam is a new SelectedPhaseSlugContext mounted in WorkflowBuilderPage.tsx; the picker DEGRADES rather than throwing when no store or provider is present, because ExternalActionSection.test.tsx renders the section standalone (CONN-02)"
  artifacts:
    - path: "frontend/src/components/workflows/ConnectionPicker.tsx"
      provides: "the author-side picker — owns the fetch AND the store write"
    - path: "frontend/src/components/workflows/SelectedPhaseSlugContext.tsx"
      provides: "the slug seam; renders children only, emits no DOM"
    - path: "frontend/src/components/workflows/ConnectionPicker.test.tsx"
      provides: "the seven picker states plus T6"
  key_links:
    - from: "frontend/src/components/workflows/ExternalActionSection.tsx"
      to: "./ConnectionPicker"
      via: "one relative sibling import plus one JSX line"
      pattern: "\\./ConnectionPicker"
    - from: "frontend/src/components/workflows/ConnectionPicker.tsx"
      to: "builderStore.patchConfig"
      via: "store.getState().patchConfig(slug, { connection_id }) then flushHistory()"
      pattern: "patchConfig"
---

<objective>
Build the author-side connection picker as a NET-NEW SIBLING CHILD of `ExternalActionSection`,
with its own context-based write seam — so `PhaseFormPanel.tsx` keeps a `0 0` diff and all six
of `ExternalActionSection`'s shipped source-purity fences stay green.

Purpose: **CONTEXT D-23 says the picker "extends the EXISTING `ExternalActionSection.tsx`",
claiming it has six props. Measured, it has THREE** (`:88-98`). The "six" are six source-purity
fences at `ExternalActionSection.test.tsx:381-412` — a different thing, and a much more binding
one: a fetch inside that file turns all six RED. And `onChange={set("capability")}` is key-bound,
so writing `connection_id` through it needs a fourth prop on the section, which needs a fourth
argument from `PhaseFormPanel.tsx`, which makes D-23's `0 0` impossible.

Output: the child component, the slug context, one import and one JSX line, and the fences
re-run rather than trusted.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-RESEARCH.md
</context>

<interfaces>
<!-- ⚠ CORRECTION #3, re-confirmed at HEAD 2026-08-08. The props (`ExternalActionSection.tsx:88-98`): -->

```ts
export interface ExternalActionSectionProps {
  capability: string
  onChange: (value: string) => void
  onPersist: () => void
}
```

**THREE props.** The "six" are these fences (`ExternalActionSection.test.tsx:381-412`), each of
which already carries its own positive control at `:398-412`:

```ts
expect(src).not.toMatch(/from\s+["']@\/lib\/api["']/)
expect(src).not.toMatch(/fetch\(/)
expect(src).not.toMatch(/XMLHttpRequest|EventSource|navigator\.sendBeacon/)
expect(src).not.toMatch(/["'`]\/(workflows|api)\//)      // nor any route string
expect(src).not.toMatch(/useContext|useStore|useEffect|zustand/)
expect(src).not.toMatch(/title=/)
```

⚠ The route-string regex requires a **leading slash**, so the relative sibling import
`"./ConnectionPicker"` cannot match — but **run the fences after the mount lands; do not trust
that sentence** (research assumption A3).

**The write seam.** `patchConfig(slug, patch)` lives on the builder store
(`builderStore.ts:255`), but the store does NOT hold the selected slug: `selectedSlug` is React
state at `WorkflowBuilderPage.tsx:582`, and the panel is mounted inside
`<BuilderStoreProvider store={store}>`. Measured at HEAD, that provider is mounted at **two**
sites (`:1530` and `:1808`) — re-grep and wrap the subtree(s) that actually carry the panel.
The existing write idiom to mirror is `onPhaseChange` (`:1262-1268`).

**The context analog is `BuilderStoreProvider.tsx` (81 lines) — copy its whole shape**: a
`createContext<T | null>(null)`, a children-only provider that **emits no DOM element** (that
property is load-bearing: wrapping the page must not move `graphChild` out of the grid's
first-child position), a throwing accessor for writers and a non-throwing accessor for leaves.

**Mount point:** one JSX line inside the existing `<section data-section="external-action">`,
after the radiogroup closes at `ExternalActionSection.tsx:220` and before the
`selected === null` note at `:227`. **190 does not change that note** — its docblock at
`:222-231` already anticipates this phase.

**The picker's seven states are the contract** (UI-SPEC §6d) and its copy strings are exported
identifiers asserted by character-identity (the `GovernanceSection.tsx:10-17` idiom), never
inline literals.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SelectedPhaseSlugContext — the write seam that keeps PhaseFormPanel at 0 0</name>
  <files>frontend/src/components/workflows/SelectedPhaseSlugContext.tsx, frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/BuilderStoreProvider.tsx` — the WHOLE 81-line file, including the docblock at `:18-21` explaining why it renders children only, and the non-throwing accessor at `:41-58`
    - `frontend/src/pages/WorkflowBuilderPage.tsx` — `selectedSlug` at `:582`, `onPhaseChange` at `:1262-1268`, and BOTH `<BuilderStoreProvider store={store}>` mounts (re-grep for them; measured at `:1530` and `:1808`)
    - `frontend/src/components/workflows/builderStore.ts:250-275` — `patchConfig` and `flushHistory`
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §6c and §13 U-08
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md` §3.14
  </read_first>
  <action>
Create `frontend/src/components/workflows/SelectedPhaseSlugContext.tsx`, copying
`BuilderStoreProvider.tsx`'s whole shape:
- `createContext<string | null>(null)`
- `SelectedPhaseSlugProvider({ slug, children })` — **renders children only, emits no DOM
  element.** Carry the reason forward in its docblock: a wrapper that emits a DOM node can move
  a grid's first child and break a shipped flag-off contract.
- `useSelectedPhaseSlug()` — the **non-throwing** accessor returning `null` outside a provider,
  in the `useBuilderStoreOptional` idiom. A leaf must still render in a provider-less unit test.

Then wrap the existing subtree in `WorkflowBuilderPage.tsx`: re-grep for
`<BuilderStoreProvider store={store}>` and wrap the one(s) that carry the phase form panel,
passing the existing `selectedSlug` state. Roughly three lines. Do not lift `selectedSlug` into
the store and do not add a prop to `PhaseFormPanel`.

**Do not open `PhaseFormPanel.tsx`.** If the wiring seems to need it, stop — that is the exact
collision this seam exists to avoid.
  </action>
  <acceptance_criteria>
    - `git diff --numstat frontend/src/components/workflows/PhaseFormPanel.tsx` prints nothing
    - `cd frontend && npx vitest run src/components/workflows` reports `0 failed`
    - `grep -c "export function useSelectedPhaseSlug" frontend/src/components/workflows/SelectedPhaseSlugContext.tsx` prints `1`, and calling it outside a provider returns `null` (asserted by a test in Task 3)
    - `grep -cE "<div|<span|<section" frontend/src/components/workflows/SelectedPhaseSlugContext.tsx` prints `0` (children only, no DOM)
    - `git diff --numstat frontend/src/pages/WorkflowBuilderPage.tsx` shows fewer than 10 changed lines
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` is at or below the re-measured baseline
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows && git diff --numstat frontend/src/components/workflows/PhaseFormPanel.tsx</automated>
  </verify>
  <done>The slug context exists in `BuilderStoreProvider`'s shape, emits no DOM, degrades to `null`, is mounted in the builder page, and `PhaseFormPanel.tsx` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: ConnectionPicker.tsx — seven states, Gate 1, and a copy contract with no absolute verbs</name>
  <files>frontend/src/components/workflows/ConnectionPicker.tsx</files>
  <read_first>
    - `frontend/src/components/settings/ProviderPicker.tsx:150-225` — the always-on 🔒 endpoint footer and the derived-footer idiom (`footerParts` built during render, never from state). ⚠ **Copy the structure, NOT its `title=` attribute**
    - `frontend/src/components/workflows/GovernanceSection.tsx:1-40` — the "authors no sentence of its own" docblock and the exported-identifier copy discipline
    - `frontend/src/components/workflows/BuilderStoreProvider.tsx:41-58` — the optional accessor
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §6b, §6d (the seven states, verbatim), §5a Gate 1, §5b's clause→mechanism table, §11d rule 5 (no absolute verb for a client-only guarantee), §12 (a11y)
    - `frontend/src/lib/api.ts` — `listConnectorConnections` (from plan 190-09)
  </read_first>
  <action>
Create `frontend/src/components/workflows/ConnectionPicker.tsx`. It owns the `useEffect`, the
API read and the store write — which is precisely why it is a child rather than a change to
`ExternalActionSection`.

**Control:** a shadcn `Select` with a real `<label>`. Not a typeahead: the candidate set is
per-capability and org-scoped, in the low tens by construction (U-09).

**The seven states, verbatim from UI-SPEC §6d** — implement all seven, because an honest state
set is populated ≠ empty ≠ loading ≠ error:
1. Loading — one dim line, no spinner (this is a 400px panel field, not a run surface)
2. None exist for this capability — `No {email|ticket|message} connection yet — add one in
   Settings → Connections.` **Text only, NO link**: the three-homes IA has no router here and a
   leaf inside the builder cannot switch `ActiveView`. Naming the destination is honest; a dead
   link is not
3. Nothing bound (the default) — `— none —` with the footer
   `🔒 nothing bound — this step will record, not send`
4. Bound — `{name}` with the footer `🔒 {sends-to} · {detail}`
5. Bound, credential failing — `{name}  ✕ credential failed` plus the §5a refusal line. **This
   state is REACHABLE by design** (Gate 2 accepts such a write) and a binding that predates the
   failure is never silently unbound — so render it rather than assume it away
6. Not checked — `{name}  ◌ not checked`, **selectable** (an unchecked connection is not a
   failed one)
7. Read failed — `Could not load connections. The step will record, not send, until one is
   bound.` with `role="alert"`

**Gate 1 (client, real):** a connection whose `last_check_verdict` is `failed` renders
`aria-disabled="true"` and is not selectable; choosing it is refused inline with
`This connection's credential is failing. Fix it in Settings → Connections, then pick it here.`

⚠ **Copy rule 5 binds every string in this file:** no sentence may use an absolute verb
(*cannot*, *never*, *no step will be allowed*) for a guarantee only this client gate provides.
Gate 2 validates org and `is_enabled` **only**. Say *the picker*, and nothing stronger.

**Copy discipline:** every user-visible string is an exported identifier from this module (or an
imported one), asserted by character-identity in Task 3 — never an inline literal buried in JSX.

**The footer** uses the same 🔒 destination form as the Settings table's `Sends to` column and
the Settings panel: one mark, one shape, three surfaces. Build it from parts during render.
**No `title=` attribute anywhere** — reasons are real DOM text wired by `aria-describedby`
(142-B, the 184-07 lesson).

**Degrade, never throw:** use `useBuilderStoreOptional()` and the null-tolerant
`useSelectedPhaseSlug()`. With no store or no provider, render the disconnected state. Otherwise
190 turns a shipped suite RED for a reason that has nothing to do with connections.

**The write:** `store.getState().patchConfig(slug, { connection_id: id })` then
`store.getState().flushHistory()`. **Write `connection_id` and nothing else** — no host, no
name, no capability copy, no secret. That is D-13 and CONN-03 SC#4 read literally.
  </action>
  <acceptance_criteria>
    - `grep -c "title=" frontend/src/components/workflows/ConnectionPicker.tsx` prints `0`
    - `grep -c "aria-disabled" frontend/src/components/workflows/ConnectionPicker.tsx` is `>= 1`
    - `grep -c "role=\"alert\"" frontend/src/components/workflows/ConnectionPicker.tsx` is `>= 1`
    - `grep -ciE "\bcannot\b|no step will be allowed|never bound" frontend/src/components/workflows/ConnectionPicker.tsx` prints `0` (UI-SPEC §11d rule 5 / U-07a)
    - `grep -c "patchConfig" frontend/src/components/workflows/ConnectionPicker.tsx` is `>= 1`, and the patch object literal contains ONLY `connection_id` (verify by printing the patch call site)
    - `grep -cE "useBuilderStoreOptional|useSelectedPhaseSlug" frontend/src/components/workflows/ConnectionPicker.tsx` is `>= 2`
    - `grep -c "export const" frontend/src/components/workflows/ConnectionPicker.tsx` is `>= 5` (the copy strings are exported identifiers)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` is at or below the re-measured baseline
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>The picker renders all seven states, enforces Gate 1 with `aria-disabled` and an inline reason, uses no absolute verb and no `title`, writes only `connection_id`, and degrades to a disconnected render with no provider.</done>
</task>

<task type="auto">
  <name>Task 3: Mount it in ONE JSX line, re-run the six purity fences, and pin the new suite</name>
  <files>frontend/src/components/workflows/ExternalActionSection.tsx, frontend/src/components/workflows/ConnectionPicker.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/ExternalActionSection.tsx:200-236` — the radiogroup close at `:220`, the nothing-chosen note at `:222-231`, and the section element
    - `frontend/src/components/workflows/ExternalActionSection.test.tsx:375-415` — the six purity fences AND their positive controls, plus the file's stated rule at `:12` that a new suite is registered with the count gate in the commit that creates it
    - `scripts/vitest-count-gate.cjs` — the pin block and the header rules for adding a pinned file
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §6b, §6d, §12
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` § Source Fences (§M4) and row T6
  </read_first>
  <action>
**A. The mount — exactly one import and one JSX line.** Add
`import { ConnectionPicker } from "./ConnectionPicker"` (the **relative sibling** form; a path
containing `/workflows/` or `/api/` would trip the route-string fence) and one JSX line inside
the existing `<section data-section="external-action">`, after the radiogroup closes and before
the `selected === null` note, rendered **only when a capability is selected** (the two questions
are ordered: *what does this step do?* then *where does it send?*). **Do not touch the note.**

**B. RE-RUN THE SIX FENCES.** Run `ExternalActionSection.test.tsx` and confirm all six purity
tests still pass. Record their names and the pass output verbatim. Research flagged this as
assumption A3 — *the regex requires a leading slash, so a relative import cannot match* — and
explicitly said to run it rather than trust it.

**C. `ConnectionPicker.test.tsx`** — cover all seven states, plus:
- `Gate 1`: a `failed` option is `aria-disabled` and selecting it does not write; the inline
  refusal text is asserted by character-identity against the exported identifier
- a `disabled` connection is **not listed at all** — it is not a choice
- **T6**: assert the object passed to `patchConfig` has exactly the key `connection_id` and that
  no key matching `secret|token|password|host|base_url` is ever written. **Plant:** write
  `config["smtp_password"]` in the picker, observe RED, restore md5-identical
- the standalone render: mounted with no store and no provider, the picker renders its
  disconnected state and does not throw — the case that protects the shipped
  `ExternalActionSection` suite
- a11y: the `Select` has a real `<label>`, and the failing option's reason is adjacent DOM text
  rather than living only in the option label

**D. Register the new suite with the count gate IN THIS COMMIT** — the rule
`ExternalActionSection.test.tsx:12` states verbatim. Run `node scripts/vitest-count-gate.cjs`,
read the printed numbers, add `"ConnectionPicker.test.tsx"` with its measured count and a dated
comment naming plan 190-12, and re-run until the gate reports OK.

**E. Prove the fences again.** Run the eight-file empty-diff numstat and record it.
  </action>
  <acceptance_criteria>
    - `git diff --numstat frontend/src/components/workflows/ExternalActionSection.tsx` shows exactly `2` insertions and `0` deletions
    - `cd frontend && npx vitest run src/components/workflows/ExternalActionSection.test.tsx` reports `0 failed`, and the summary records all six purity-fence test names as PASSING after the mount
    - `cd frontend && npx vitest run src/components/workflows/ConnectionPicker.test.tsx` reports `0 failed` with at least 10 tests
    - The summary records the T6 plant (`config["smtp_password"]`) observed RED with verbatim output, and `grep -c PLANT frontend/src/components/workflows/ConnectionPicker.tsx` prints `0`
    - `node scripts/vitest-count-gate.cjs` exits 0 printing `count gate OK`, and `grep -c "ConnectionPicker.test.tsx" scripts/vitest-count-gate.cjs` is `>= 1`
    - `git diff --numstat HEAD -- frontend/src/components/workflows/PhaseFormPanel.tsx frontend/src/components/workflows/PhaseNode.tsx frontend/src/components/workflows/PhaseNodeCard.tsx frontend/src/components/workflows/phaseNodeCardContract.ts frontend/src/components/workflows/ownProperty.ts frontend/src/components/workflows/NodeCornerMarks.tsx frontend/src/components/workflows/NodeRunOverlay.tsx frontend/src/components/workflows/NodeIconWell.tsx` prints NOTHING
    - `cd frontend && npm test` reports `0 failed`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test && cd .. && node scripts/vitest-count-gate.cjs && git diff --numstat HEAD -- frontend/src/components/workflows/PhaseFormPanel.tsx frontend/src/components/workflows/PhaseNodeCard.tsx</automated>
  </verify>
  <done>The mount is two insertions, the six purity fences were re-run and are green, the picker suite covers seven states plus Gate 1 and T6, the new suite is pinned in the same commit, and the eight fenced files still show an empty diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker → definition JSONB | Only a reference id may cross; a host or a secret written here would be published into a definition anyone in the org can read |
| client gate → the user's belief | A sentence that claims more than the gate delivers is the U-07a defect |
| new child component → a shipped suite's purity fences | Six fences read `ExternalActionSection.tsx`'s own source; the mount must not trip any of them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-12-T6 | Information disclosure | `patchConfig` write | mitigate | The patch object carries exactly `connection_id`; a test asserts no `secret\|token\|password\|host\|base_url` key is ever written. Plant = write `config["smtp_password"]`, observed RED |
| T-190-12-U07a | Repudiation | picker copy | mitigate | Copy rule 5 asserted by grep: no `cannot` / `no step will be allowed` / `never bound`. Gate 1 is client-only and the sentence says *the picker*, matching its exact reach |
| T-190-12-FENCE | Tampering | `ExternalActionSection.tsx` purity | mitigate | Relative sibling import; the six fences are RE-RUN after the mount and their names recorded, rather than trusting research assumption A3 |
| T-190-12-D23 | Tampering | `PhaseFormPanel.tsx` | mitigate | `git diff --numstat` asserted EMPTY as an automated verify; the write seam is a context, not a fourth prop |
| T-190-12-A11Y | Denial of service (to a screen-reader user) | the Select and the failing option | mitigate | Real `<label>`; failing options `aria-disabled` with the reason as adjacent DOM text; `role="alert"` on the read failure; no `title=` anywhere |
| T-190-12-CRASH | Denial of service | provider-less render | mitigate | Optional accessors and a disconnected state, asserted by a standalone-render test — otherwise a shipped suite goes RED for an unrelated reason |
| T-190-SC | Tampering | npm/cargo/pip installs | mitigate | Zero installs; only already-vendored shadcn primitives (`select`, `label`) are used. Acceptance: `git diff frontend/package.json frontend/package-lock.json` prints nothing |
</threat_model>

<verification>
- `cd frontend && npm test` — `0 failed`
- `node scripts/vitest-count-gate.cjs` — `count gate OK`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` — at or below baseline
- `git diff --numstat HEAD -- <the eight fenced files>` — empty
- `git diff --numstat frontend/src/components/workflows/ExternalActionSection.tsx` — `2 0`
</verification>

<success_criteria>
- The picker is a net-new child owning the fetch and the write; `PhaseFormPanel.tsx` diff is `0 0`
- All six shipped purity fences were re-run after the mount and are green
- Seven states, Gate 1 with `aria-disabled`, and no absolute verb anywhere in the copy
- Only `connection_id` is written, proved by a plant observed RED
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-12-SUMMARY.md` when done, recording the six fence names with their post-mount PASS output, the T6 plant cycle, the gate's printed totals and the empty numstat.
</output>
