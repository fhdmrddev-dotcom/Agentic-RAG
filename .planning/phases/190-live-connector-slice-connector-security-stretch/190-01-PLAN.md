---
phase: 190-live-connector-slice-connector-security-stretch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_190_cross_org_credential.py
  - backend/tests/unit/test_190_egress_ordering.py
  - backend/tests/unit/test_189_no_egress.py
autonomous: true
requirements: [CONN-03]

must_haves:
  truths:
    - "D-14 — a cross-org credential-leak drive exists as a real test file and has been OBSERVED FAILING before any resolver, adapter or egress module exists (CONN-03)"
    - "D-06 — an ordering drive exists that asserts the egress refusal arrives BEFORE any missing-credential error, and it has been OBSERVED FAILING first (CONN-03)"
    - "D-16 — the shipped golden-run fence test_a_golden_run_of_an_external_action_performs_no_egress is re-measured GREEN-because-inert at this HEAD and its baseline is recorded; the file is NOT edited by this plan"
    - "D-28 — every falsification row in this plan carries a named RED observation with its exact failure text, not a claim that it would fail"
    - "The transport sentinel _block_all_http in test_189_no_egress.py is byte-identical after this plan; only Case B's DRIVE is re-scoped and a new bound-connection positive case is added"
  artifacts:
    - path: "backend/tests/unit/test_190_cross_org_credential.py"
      provides: "W0-2 — org A definition carrying org B's connection_id must not resolve"
    - path: "backend/tests/unit/test_190_egress_ordering.py"
      provides: "W0-3 — the guard runs before credential resolution, unconditionally"
    - path: "backend/tests/unit/test_189_no_egress.py"
      provides: "the re-scoped Case B drive plus the new bound-connection positive case"
  key_links:
    - from: "backend/tests/test_harness_engine.py:1139"
      to: "backend/tests/unit/test_189_no_egress.py::_block_all_http"
      via: "import"
      pattern: "from tests.unit.test_189_no_egress import _block_all_http"
---

<objective>
Author the Wave-0 RED drives for the two latent defects this phase's own commit creates
(D-14 cross-org credential resolution, D-16 publish-time egress) plus the third of the same
shape found by research (D-06 guard-before-credential ordering), and re-scope the shipped
transport sentinel deliberately rather than deleting it.

Purpose: **a test that has never been observed RED proves nothing.** CONTEXT `<specifics>` is
explicit — plan these into Wave 0, RED, *before any adapter exists*. This plan runs before
`egress.py`, `connector_service.py` and `connectors/` exist, so each drive fails for a reason
that is recorded verbatim now and re-checked when the mechanism lands.

Output: three test files (two net-new, one re-scoped), each with a recorded RED observation,
and a recorded green baseline for the already-armed golden-run fence.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md
</context>

<interfaces>
<!-- Re-derived at HEAD 2026-08-08. Re-derive again before editing; never cite these numbers. -->

The one function 190 replaces (`backend/app/services/harness/phase_types.py`, measured
`:1810-1885` — NOT the `:1667-1903` range CONTEXT gives):

```python
async def _exec_external_action(phase, accumulated_outputs: dict, ctx) -> dict:
    ...
    return {
        "text": _external_action_body(capability, resolved),
        RECORDED_INTENT_KEY: {"capability": capability, "inputs": resolved},
    }
```

`RECORDED_INTENT_KEY = "recorded_intent"` at `phase_types.py:1705`.
`_external_action_body` at `phase_types.py:1778`.

The sentinel this plan re-scopes (`backend/tests/unit/test_189_no_egress.py:84-145`) — its
five monkeypatches and its recorded ordering constraint `⚠ ORDER MATTERS:
socket.socket.connect goes on LAST.` It is imported by
`backend/tests/test_harness_engine.py:1139`.

The Windows trap, recorded in the fence's own words at
`backend/tests/test_harness_engine.py:1143-1148`: the event loop MUST be built BEFORE
`_block_all_http(monkeypatch)` arms, because `asyncio.run` creates a proactor self-pipe via
`socketpair()` — itself a `socket.connect` the sentinel would catch.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: W0-2 — author the cross-org credential-leak drive and observe it RED</name>
  <files>backend/tests/unit/test_190_cross_org_credential.py</files>
  <read_first>
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` § "Wave 0 Requirements" row W0-2 and § "The Security Falsification Set" row T8
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-14, D-15
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md` §3.6 (the `classification_rule_service` security-invariant header and `sso_provider_service.py:82-94`)
    - `backend/tests/unit/test_189_no_egress.py` (the fence + positive-control idiom, `:206-262`)
    - `backend/app/services/harness/phase_types.py:1700-1903` (read-only — the executor this drive will eventually reach)
  </read_first>
  <action>
Create `backend/tests/unit/test_190_cross_org_credential.py`. It drives the D-14 defect NAMED
IN ADVANCE: a workflow definition owned by org A carries a `connection_id` whose
`connector_connections` row belongs to org B.

Import target (does not exist yet, and that is the point of this plan):
`from app.services.connector_service import resolve_connection, ConnectorNotFound`.

Author three test functions:

1. `test_a_connection_id_from_another_org_does_not_resolve` — arrange two fake org ids
   (`ORG_A`, `ORG_B`) and a stubbed row store holding one connection owned by `ORG_B`.
   Call `resolve_connection(connection_id=<the org B id>, org_id=ORG_A, ...)` and assert it
   raises `ConnectorNotFound`. Assert the raised message contains NEITHER the secret
   ciphertext NOR the word `forbidden` — a cross-org id must read as absent, never as
   forbidden (the `api/classification_rules.py:11-19` no-existence-leak rule).
2. `test_the_same_connection_id_DOES_resolve_for_its_owning_org` — the non-vacuity control.
   Without it, a resolver that refuses everything passes test 1 forever.
3. `test_the_resolver_signature_takes_an_org_id` — a source/signature assertion using
   `inspect.signature(resolve_connection)`; assert `org_id` is a REQUIRED parameter (no
   default). An optional `org_id` is the id-only `SELECT` wearing a disguise.

Use the project's stub idiom rather than a live DB: the drive stubs the row fetch at the
service seam so it runs in `tests/unit/` with no Postgres. Name the stub fetch
`_fake_fetch_row(connection_id, org_id)` so a resolver that ignores `org_id` is structurally
visible in the drive.

Mark nothing `xfail`, `skip` or `skipif`. The file MUST fail at collection today.

Record the RED observation VERBATIM in the file's module docstring under a heading
`RED OBSERVED (Wave 0, plan 190-01)` — the exact pytest output line, including the
`ModuleNotFoundError: No module named 'app.services.connector_service'` text and the date.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_190_cross_org_credential.py -q` exits NON-ZERO and its output contains `No module named 'app.services.connector_service'`
    - `grep -c "def test_" backend/tests/unit/test_190_cross_org_credential.py` prints `3`
    - `grep -cE "xfail|@pytest.mark.skip" backend/tests/unit/test_190_cross_org_credential.py` prints `0`
    - The module docstring contains the literal heading `RED OBSERVED (Wave 0, plan 190-01)` and the verbatim pytest failure line
    - `grep -c "org_id" backend/tests/unit/test_190_cross_org_credential.py` is `>= 4`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_190_cross_org_credential.py -q; test $? -ne 0 && echo "RED as required"</automated>
  </verify>
  <done>The W0-2 drive exists, names three functions including a non-vacuity control and a required-`org_id` signature assertion, and its RED failure text is recorded in its own docstring.</done>
</task>

<task type="auto">
  <name>Task 2: W0-3 — author the guard-before-credential ordering drive and observe it RED</name>
  <files>backend/tests/unit/test_190_egress_ordering.py</files>
  <read_first>
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` row W0-3
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-06, D-07, D-08
    - `.planning/research/deep-dive/N8N.md` (the CVE class this test inverts) — read the "guarded only when a credential is attached" section
    - `backend/tests/unit/test_190_cross_org_credential.py` (just authored — reuse its stub idiom)
  </read_first>
  <action>
Create `backend/tests/unit/test_190_egress_ordering.py`. This is the n8n CVE class inverted
into a test: the egress guard must refuse **before** credential resolution is even attempted,
and it must do so **unconditionally** — including when no credential is bound at all.

Import targets (neither exists yet): `from app.security.egress import EgressRefused` and the
executor entry `from app.services.harness.phase_types import _exec_external_action`.

Author three test functions:

1. `test_a_send_with_NO_credential_bound_raises_the_egress_refusal_not_a_credential_error` —
   drive `_exec_external_action` for a `create_ticket` phase whose config carries a
   `base_url` pointing at a refused destination (`https://169.254.169.254/`) and **no
   `connection_id` at all**. Assert the raised exception is `EgressRefused`. Assert it is NOT
   any credential-shaped error: assert the exception type name does not contain `Credential`
   and the message does not contain `credential`, `token` or `password`.
2. `test_the_refusal_names_the_host_and_the_reason_and_NOTHING_else` — assert the
   `EgressRefused` instance exposes `reason_code` and `host` attributes, that `str(exc)`
   contains the host, and that it contains no substring of the stubbed secret. This is D-08 as
   an assertion rather than a promise.
3. `test_the_guard_is_called_before_the_resolver` — install ordered call-recording stubs on
   the egress validator and the connector resolver, drive one send with a credential bound,
   and assert the recorded call order is `["egress", "resolve"]` exactly. Ordering is
   invisible to any test that binds a credential and only checks the outcome — this is the
   assertion that survives a line reorder.

Mark nothing `xfail` or `skip`.

Record the RED observation VERBATIM in the module docstring under
`RED OBSERVED (Wave 0, plan 190-01)`, including the `ModuleNotFoundError: No module named
'app.security.egress'` text and the date. Add a `TO BE RE-OBSERVED AT PLAN 190-13` note stating
that the *meaningful* RED for function 1 is against a resolver-before-guard ordering in the
real executor, and that 190-13 must drive that ordering RED and record its `MissingCredential`
failure text before reordering.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_190_egress_ordering.py -q` exits NON-ZERO and its output contains `No module named 'app.security.egress'`
    - `grep -c "def test_" backend/tests/unit/test_190_egress_ordering.py` prints `3`
    - The file contains the literal strings `RED OBSERVED (Wave 0, plan 190-01)` and `TO BE RE-OBSERVED AT PLAN 190-13`
    - `grep -c 'reason_code' backend/tests/unit/test_190_egress_ordering.py` is `>= 1`
    - `grep -cE "xfail|@pytest.mark.skip" backend/tests/unit/test_190_egress_ordering.py` prints `0`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_190_egress_ordering.py -q; test $? -ne 0 && echo "RED as required"</automated>
  </verify>
  <done>The W0-3 drive exists with three functions covering error identity, refusal content and call ordering, and its RED failure text plus the 190-13 re-observation contract are recorded in its docstring.</done>
</task>

<task type="auto">
  <name>Task 3: Re-scope the transport sentinel's DRIVE (never the sentinel) and record the W0-1 baseline</name>
  <files>backend/tests/unit/test_189_no_egress.py</files>
  <read_first>
    - `backend/tests/unit/test_189_no_egress.py` — the whole file, in particular `_block_all_http` (`:84-145`), its five inertness controls (`:266-335`), and Case A (`test_no_mcp_identifiers_in_backend_app`, `:206-262`)
    - `backend/tests/test_harness_engine.py:1106-1180` — the golden-run fence, its `from tests.unit.test_189_no_egress import _block_all_http` import at `:1139`, and the Windows loop-ordering comment at `:1143-1148`
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-RESEARCH.md` §M5 ("How to re-scope the sentinel deliberately")
    - `.planning/phases/189-governed-external-action-node-model/deferred-items.md` D-189-DEF-04
  </read_first>
  <action>
Three changes, in this order, in `backend/tests/unit/test_189_no_egress.py`:

1. **Leave `_block_all_http` byte-identical.** It is imported by the golden-run fence; a change
   here silently changes a different suite. Prove non-modification by hashing the function
   body before and after (record both hashes in the plan summary).
2. **Leave Case A (`test_no_mcp_identifiers_in_backend_app`) untouched and green.** D-01 builds
   NO MCP client, so this fence's continued passing is part of the amendment's evidence. Add a
   one-line comment above it citing `D-01` and saying exactly that — nothing else.
3. **Re-scope Case B's DRIVE, not its sentinel.** Case B falsifies `_exec_external_action`
   under the armed sentinel. Today it is true unconditionally; after 190 it is true only for a
   step with **no `connection_id`**. Edit the case so its precondition is explicit — the phase
   config carries no `connection_id`, which is D-17's permanent `recorded_not_sent` terminal
   and a real shipping state, not a test convenience. Rename it so the precondition is in the
   name (for example `..._with_no_connection_bound_...`).
4. **Add the NEW positive case**
   `test_a_bound_connection_under_the_armed_sentinel_MUST_attempt_egress` — arm
   `_block_all_http`, drive `_exec_external_action` for a phase WITH a `connection_id` bound
   and a live (non-golden) ctx, and assert `_EgressAttempted` is raised. This is the case that
   proves the send is genuinely live rather than accidentally still inert. **It is RED today**
   (the executor sends nothing), and that RED is this task's evidence.
   ⚠ Build the event loop BEFORE arming the sentinel, exactly as
   `test_harness_engine.py:1143-1148` records — the Windows proactor self-pipe is itself a
   `socket.connect`.

Then run — do NOT edit — `backend/tests/test_harness_engine.py` and record the W0-1 baseline:
`test_a_golden_run_of_an_external_action_performs_no_egress` passes today BECAUSE the step is
inert. Record its passing output and the whole-file count in the plan summary as the baseline
plan 190-13 will drive RED.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_189_no_egress.py -q` reports exactly ONE failing test and its name is `test_a_bound_connection_under_the_armed_sentinel_MUST_attempt_egress`
    - `git diff -U0 backend/tests/unit/test_189_no_egress.py` shows NO changed line between the `def _block_all_http` line and its closing — verified by extracting the function and comparing md5 before/after; both hashes recorded in the summary
    - `grep -c "_EgressAttempted" backend/tests/unit/test_189_no_egress.py` increases by at least 1 versus HEAD~
    - `cd backend && python -m pytest tests/test_harness_engine.py -q` reports `46 passed` (re-measure and record the actual number; if it differs, use the measured number and say so)
    - `git diff --numstat backend/tests/test_harness_engine.py` prints nothing (this plan does not edit that file)
    - `grep -n "asyncio" backend/tests/unit/test_189_no_egress.py` shows the loop is created before `_block_all_http(` in the new case
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_189_no_egress.py -q; python -m pytest tests/test_harness_engine.py -q</automated>
  </verify>
  <done>Case B's drive names its no-connection precondition, the new bound-connection positive case exists and is RED, `_block_all_http` and Case A are unchanged, `test_harness_engine.py` is untouched, and the W0-1 green-because-inert baseline is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production source | A fence that never fails proves nothing; a vacuous drive is a false attestation of security |
| org A run → org B credential row | The tenant boundary D-14 protects; crossed by an id-only `SELECT` |
| publish (golden run) → the wire | D-16; crossed on the exact commit that adds a send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-01-T8 | Elevation of privilege | `connector_service.resolve_connection` | mitigate | W0-2 drive authored here and observed RED; `org_id` asserted a REQUIRED parameter via `inspect.signature`, plus a non-vacuity control |
| T-190-01-T10 | Tampering | `harness_engine` golden-run branch | mitigate | W0-1 baseline recorded green-because-inert; the RED observation is contractually owned by plan 190-13 in the SAME commit as the send |
| T-190-01-D06 | Tampering | `_exec_external_action` ordering | mitigate | W0-3 drive authored here, asserting error IDENTITY and recorded call order, not just failure |
| T-190-01-VAC | Repudiation | this plan's own tests | mitigate | Every drive carries a non-vacuity control; every RED observation is recorded VERBATIM in the file's own docstring rather than only in a summary |
| T-190-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NOTHING. `190-RESEARCH.md` § Package Legitimacy Audit records "NOT APPLICABLE — zero external packages". Acceptance: `git diff backend/requirements.txt frontend/package.json` prints nothing |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/unit/test_190_cross_org_credential.py tests/unit/test_190_egress_ordering.py -q` — both RED, for the recorded module-missing reasons
- `cd backend && python -m pytest tests/unit/test_189_no_egress.py -q` — exactly one RED, the new bound-connection positive case
- `cd backend && python -m pytest tests/test_harness_engine.py -q` — unchanged from baseline
- `git diff --numstat backend/requirements.txt` prints nothing
</verification>

<success_criteria>
- Three RED observations exist, each with its verbatim failure text recorded in the test file itself
- `_block_all_http` is byte-identical; Case A is untouched; `test_harness_engine.py` is untouched
- No `xfail`, `skip` or `skipif` anywhere in the new files
- No package installed, no production source file modified by this plan
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-01-SUMMARY.md` when done, recording each RED observation verbatim and the two `_block_all_http` md5 hashes.
</output>
