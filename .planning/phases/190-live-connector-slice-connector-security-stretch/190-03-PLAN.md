---
phase: 190-live-connector-slice-connector-security-stretch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/116_connector_connections.sql
  - supabase/migrations/117_harness_audit_external_action_sent.sql
  - backend/app/db/workflows.py
  - supabase/full-schema.sql
autonomous: false
requirements: [CONN-03]

must_haves:
  truths:
    - "D-10 — connector_connections exists in migration 116 with org_id, created_by, capability, name, config jsonb, secret_ciphertext, is_enabled, last_checked_at, last_check_verdict, timestamps (CONN-03)"
    - "D-12 — the SELECT policy is org-wide with NO is_system / is_system_global escape branch, and that ABSENCE is stated in the migration's own header comment (CONN-03)"
    - "D-20 — TWO migrations ship, 116 and 117; 117 adds exactly ONE literal to harness_audit_event_type_check and the Python mirror moves in the SAME commit (CONN-03)"
    - "D-21 — the migrations are applied by the operator pasting into the Supabase SQL editor, then `bash scripts/regenerate-full-schema.sh` with NO --reset; `supabase db push` and `db reset` are never run, and any deviation is RECORDED rather than normalised (CONN-03)"
    - "The audit slug is LOCKED against test_audit_event_registration.py, which 189 kept passing unchanged and which this phase deliberately moves"
  artifacts:
    - path: "supabase/migrations/116_connector_connections.sql"
      provides: "the org-scoped connector credential table + four RLS policies + indexes + the autofill trigger"
      contains: "current_user_org_ids"
    - path: "supabase/migrations/117_harness_audit_external_action_sent.sql"
      provides: "one added event_type literal, 23 -> 24"
    - path: "backend/app/db/workflows.py"
      provides: "_AUDIT_EVENT_TYPES mirror + the appended provenance string"
  key_links:
    - from: "supabase/migrations/117_harness_audit_external_action_sent.sql"
      to: "backend/app/db/workflows.py::_AUDIT_EVENT_TYPES"
      via: "the two-directional G2 fence in test_audit_event_registration.py"
      pattern: "external_action_sent"
---

<objective>
Author migrations **116** (`connector_connections`) and **117** (one added `harness_audit`
event_type literal), move the Python mirror in the same commit, and have the operator apply
both by the project's non-negotiable discipline.

Purpose: everything downstream — the org-scoped resolver (D-14), the credential cipher
(D-11), the send receipt — depends on this table and this literal existing in the live local
DB. This plan is `autonomous: false` because step 1 of the apply discipline requires a human
with the Supabase SQL editor open.

Output: two migration files, the moved Python mirror, a regenerated `full-schema.sql`, and a
recorded apply (or a recorded deviation).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@supabase/SETUP.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md
</context>

<interfaces>
<!-- Re-derived at HEAD 2026-08-08. RE-DERIVE BEFORE WRITING — never trust these numbers. -->

```
$ ls supabase/migrations/ | tail -1     → 115_workflow_phases_recorded_not_sent.sql
$ ls supabase/migrations/ | wc -l       → 109
```
Head is **115**; next free is **116**. **Filenames must match `<digits>_name.sql`** — a letter
suffix like `116b` is SILENTLY SKIPPED by the Supabase CLI.

The RLS analog to mirror is `sso_configs` (`104_org_dept_role_schema.sql:153-163` for the table,
`:372-390` for the four policies) — a CLOSER fit than the `skills` policy, because it reads
org-wide and writes behind `current_user_has_permission(org_id, …)`, which is exactly
`190-UI-SPEC.md` U-02's shape:

```sql
CREATE POLICY sso_configs_select ON public.sso_configs
  FOR SELECT TO authenticated USING (org_id IN (SELECT public.current_user_org_ids()));
CREATE POLICY sso_configs_insert ON public.sso_configs
  FOR INSERT TO authenticated
  WITH CHECK (public.current_user_has_permission(org_id, 'sso:manage') AND org_id IN (SELECT public.current_user_org_ids()));
```

`org:manage` is already seeded to `super-admin` + `org-admin` at `104:416-426` — reuse it
rather than minting a new permission key.

The autofill trigger (`106_org_id_autofill_trigger.sql:74-107`) is `SECURITY DEFINER`,
`SET search_path = ''`, a forward-compat no-op when `org_id` is supplied, and fail-safe `NULL`
which the `NOT NULL` column then rejects. `connector_connections` owns by `created_by` →
GROUP 2 → `TG_ARGV[0]` is `'created_by'`.

The one-literal precedent is `114_harness_audit_action_risk_pending.sql` — **49 lines, the whole
shape**. The Python mirror is `backend/app/db/workflows.py:107-137` and the hand-maintained
provenance string is at `:1161` and `:1194`:

```python
f"(059 + 069 + 070 + 114), got {event_type!r}"
```

The G2 fence (`test_audit_event_registration.py:101-114`) reads *the highest-numbered migration
that DEFINES the CHECK*, not the highest-numbered migration — so 116 is invisible to it and 117
becomes the source of truth. It is two-directional: a literal in Python-but-not-SQL is a mid-run
Postgres 23514; a literal in SQL-but-not-Python is a `ValueError` before the INSERT.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 116 — connector_connections, its RLS and its trigger</name>
  <files>supabase/migrations/116_connector_connections.sql</files>
  <read_first>
    - `supabase/migrations/104_org_dept_role_schema.sql` lines 140-175 (the `sso_configs` table), 365-395 (its four policies) and 410-430 (the `org:manage` permission seed)
    - `supabase/migrations/106_org_id_autofill_trigger.sql:60-115` (the trigger function and its GROUP 1 / GROUP 2 argument rule)
    - `supabase/migrations/114_harness_audit_action_risk_pending.sql` (the whole file — the header-states-the-discipline shape)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-10, D-11, D-12
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §2c (the `Credential` column, which is what forces `last_checked_at` / `last_check_verdict`) and §13 U-02
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md` §3.10
  </read_first>
  <action>
**FIRST, re-derive the head**: `ls supabase/migrations/ | tail -1`. If it is not
`115_…`, use the measured next free number and say so in the summary — do not trust this plan's
number.

Create `supabase/migrations/116_connector_connections.sql` (no letter suffix).

**Header comment** — in `114_…`'s shape: name the phase, the requirement (CONN-03), the
decisions (D-10, D-11, D-12), the apply discipline (paste into the Supabase SQL editor, then
`regenerate-full-schema.sh`, never `db push`), and — this one is mandatory —
**state D-12's ABSENCE explicitly**: there is no `is_system` / `is_system_global` branch in the
SELECT policy, and that absence IS the decision, because it is the exact branch SEED-125 /
migration 112 had to close for skill files. An absence that is not written down reads as an
oversight to the next author.

**Table** (`CREATE TABLE IF NOT EXISTS public.connector_connections`, the re-paste-safety rule
`104:23` states):
- `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`
- `org_id uuid NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE`
- `created_by uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE` — match the sibling
  tables' exact reference target and delete rule; read one before choosing
- `capability text NOT NULL` with a `CHECK (capability IN ('send_email','create_ticket','post_message'))`
  — the closed set D-04 pins, spelled here because SQL cannot import the frozenset
- `name text NOT NULL`
- `config jsonb NOT NULL DEFAULT '{}'` — **NON-secret only**: host, port, base_url,
  from_address, default_channel, project_key
- `secret_ciphertext text` — nullable, `enc:v1:` envelope only
- `is_enabled boolean NOT NULL DEFAULT true`
- `last_checked_at timestamptz` and `last_check_verdict text` with a
  `CHECK (last_check_verdict IN ('not_checked','ok','failed'))` — forced by UI-SPEC §2c's
  `Credential` column and §5's gates
- `created_at timestamptz NOT NULL DEFAULT now()`, `updated_at timestamptz NOT NULL DEFAULT now()`

**`COMMENT ON COLUMN`** for the three non-obvious columns, each carrying its decision id:
`secret_ciphertext` (D-11 — `enc:v1:` through the shipped cipher, fail-CLOSED when unkeyed),
`config` (D-13/SC#4 — NON-secret facts only; no token, no password ever lands here) and
`last_check_verdict` (UI-SPEC §5a — a quality hint, NOT an authorization boundary).

**Indexes**: btree on `org_id`; a composite on `(org_id, capability)` because the picker's read
is always capability-scoped; a btree on `created_by`.

**RLS**: `ALTER TABLE … ENABLE ROW LEVEL SECURITY` plus four policies mirroring
`sso_configs`, each preceded by `DROP POLICY IF EXISTS`:
- SELECT — `org_id IN (SELECT public.current_user_org_ids())`, org-wide, no escape branch
- INSERT — `public.current_user_has_permission(org_id, 'org:manage') AND org_id IN (SELECT public.current_user_org_ids())`
- UPDATE — USING the permission, WITH CHECK the permission AND the org membership
- DELETE — USING the permission

**Trigger**: `DROP TRIGGER IF EXISTS connector_connections_autofill_org_id …` then
`CREATE TRIGGER … BEFORE INSERT … FOR EACH ROW EXECUTE FUNCTION public.autofill_org_id_by_owner('created_by')`.

**`updated_at`**: reuse whichever `updated_at` touch trigger the sibling tables already carry —
grep for one before writing a new function; do not mint a second.

Do NOT apply it in this task.
  </action>
  <acceptance_criteria>
    - `ls supabase/migrations/ | tail -2` shows `116_connector_connections.sql` immediately after the previously-highest file, and the filename matches `^[0-9]+_[a-z0-9_]+\.sql$` (no letter suffix)
    - `grep -c "current_user_org_ids" supabase/migrations/116_connector_connections.sql` is `>= 3`
    - `grep -c "current_user_has_permission" supabase/migrations/116_connector_connections.sql` is `>= 4`
    - `grep -c "is_system" supabase/migrations/116_connector_connections.sql` prints `0` in the POLICY bodies, and the header comment contains the literal phrase `the absence` explaining D-12
    - `grep -c "autofill_org_id_by_owner('created_by')" supabase/migrations/116_connector_connections.sql` prints `1`
    - `grep -c "COMMENT ON COLUMN" supabase/migrations/116_connector_connections.sql` is `>= 3`
    - `grep -cE "send_email|create_ticket|post_message" supabase/migrations/116_connector_connections.sql` is `>= 3`
  </acceptance_criteria>
  <verify>
    <automated>ls supabase/migrations/ | tail -2 && grep -c "current_user_org_ids\|current_user_has_permission\|autofill_org_id_by_owner" supabase/migrations/116_connector_connections.sql</automated>
  </verify>
  <done>Migration 116 exists with the table, three column comments, three indexes, four RLS policies mirroring `sso_configs`, the autofill trigger, and D-12's absence stated in its header.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 117 and move the Python mirror in the SAME commit</name>
  <files>supabase/migrations/117_harness_audit_external_action_sent.sql, backend/app/db/workflows.py</files>
  <read_first>
    - `supabase/migrations/114_harness_audit_action_risk_pending.sql` — the whole 49-line file
    - `backend/app/db/workflows.py:100-140` (`_AUDIT_EVENT_TYPES` and its per-migration comment convention) and `:1155-1200` (`write_audit`'s derived-count error message and the hand-maintained provenance string)
    - `backend/tests/unit/test_audit_event_registration.py:90-140` (`_highest_numbered_check_migration`, the two-directional assertions, the non-vacuity and no-duplicates checks, and `test_g2_parser_survives_parenthesised_grouping_comments`)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-20 and `190-RESEARCH.md` §M10 and Assumption A6
  </read_first>
  <action>
**Lock the slug first.** Read `test_audit_event_registration.py`'s extraction regex and confirm
`external_action_sent` is a legal literal under it (lowercase, underscores, no digits issue).
CONTEXT calls it a *working name*; this task makes it the name. Record the decision explicitly
in the summary: the slug is `external_action_sent`, locked at plan 190-03.

Create `supabase/migrations/117_harness_audit_external_action_sent.sql`, copying
`114_…`'s whole shape: header stating the phase, requirement, decision (D-20), the measured
fact it answers (the CHECK held 23 literals at `full-schema.sql:1124`), the apply discipline,
then `ALTER TABLE public.harness_audit DROP CONSTRAINT harness_audit_event_type_check;`
followed by `ADD CONSTRAINT … CHECK (event_type IN ( … ))` listing **all 24** literals — the
existing 23 in the CHECK's own order plus `external_action_sent` last, under a comment
`-- 117 (Phase 190 CONN-02/CONN-03) — the send receipt: a real consequence, not an intention`.

**Re-derive the 23**, do not retype them from any document:
`sed -n '1124p' supabase/full-schema.sql | grep -o "'[a-z_]*'::text"` and use that output.

In the SAME commit, edit `backend/app/db/workflows.py`:
- add `"external_action_sent"` to `_AUDIT_EVENT_TYPES` with the same per-migration comment
  convention the `# 114 (Phase 185 …)` block uses
- append `+ 117` to the hand-maintained provenance string in BOTH places it appears (the
  docblock near `:1161` and `write_audit`'s `ValueError` message near `:1194`). If it is not
  appended, that error message starts lying — the count it derives will say 24 while the
  provenance says the constraint came from four migrations.
- do NOT retype the count anywhere: `len(_AUDIT_EVENT_TYPES)` already derives it.

Do NOT apply either migration in this task.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_audit_event_registration.py -q` reports `0 failed`
    - `cd backend && python -c "from app.db.workflows import _AUDIT_EVENT_TYPES as t; print(len(t)); print('external_action_sent' in t)"` prints `24` then `True`
    - `sed -n '/ADD CONSTRAINT/,/);/p' supabase/migrations/117_harness_audit_external_action_sent.sql | grep -o "'[a-z_]*'" | sort -u | wc -l` prints `24`
    - `grep -c "059 + 069 + 070 + 114 + 117" backend/app/db/workflows.py` prints `2`
    - `grep -c "len(_AUDIT_EVENT_TYPES)" backend/app/db/workflows.py` is `>= 1` and no hard-coded `24` appears in that error message
    - `git diff --name-only` for this task lists exactly the migration and `backend/app/db/workflows.py`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_audit_event_registration.py -q</automated>
  </verify>
  <done>Migration 117 lists 24 literals derived from the live CHECK, the Python mirror carries the 24th with its provenance comment, both provenance strings read `+ 117`, and the two-directional G2 fence is green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Operator applies 116 then 117 via the Supabase SQL editor, then regenerates full-schema.sql</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - `CLAUDE.md` § "Rules" — the migration paragraph (`supabase db push` / `db reset` are BANNED)
    - `supabase/SETUP.md` — the full apply story
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-21 (and 189-06's recorded deviation precedent)
    - `scripts/regenerate-full-schema.sh` — read it before running it, and confirm the default path takes NO `--reset`
  </read_first>
  <what-built>
Migrations `116_connector_connections.sql` and `117_harness_audit_external_action_sent.sql`
are authored and committed alongside the moved Python mirror. Nothing has been applied to the
database yet. Every downstream plan that touches `connector_connections` or writes the
`external_action_sent` receipt is blocked on this step.
  </what-built>
  <how-to-verify>
1. Open the local Supabase SQL editor (the local stack, not cloud).
2. Paste the **entire contents of `supabase/migrations/116_connector_connections.sql`** and run
   it. Confirm it reports success. **Do not run `supabase db push`. Do not run
   `supabase db reset`.** Both are banned by CLAUDE.md and would destroy dev data.
3. Paste the **entire contents of `supabase/migrations/117_harness_audit_external_action_sent.sql`**
   and run it. Confirm success.
4. Sanity-check both, in the editor:
   - `SELECT count(*) FROM public.connector_connections;` returns `0` (the table exists)
   - `SELECT relrowsecurity FROM pg_class WHERE relname = 'connector_connections';` returns `t`
   - `SELECT count(*) FROM pg_policies WHERE tablename = 'connector_connections';` returns `4`
   - `INSERT INTO public.harness_audit (…, event_type) VALUES (…, 'external_action_sent')` on a
     throwaway row succeeds, then delete it — or, if inserting is impractical, run
     `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'harness_audit_event_type_check';`
     and confirm `external_action_sent` appears in the printed definition
5. Back in the terminal run `bash scripts/regenerate-full-schema.sh` — **with NO `--reset`**.
6. Confirm `git diff --stat supabase/full-schema.sql` shows the new table and the widened CHECK,
   and that no unrelated object was dropped.
7. Commit the two migrations, `backend/app/db/workflows.py` and the regenerated
   `supabase/full-schema.sql` **together**.

⚠ **If the SQL editor path is impossible for any reason, do NOT normalise a workaround.** Apply
it by whatever means works, and RECORD THE DEVIATION explicitly in the plan summary — naming
what was used and why — exactly as 189-06 recorded its `psycopg2` deviation while the ban
itself held.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c "connector_connections" supabase/full-schema.sql` is `>= 5` (table + policies + indexes present in the regenerated artifact)
    - `grep -c "external_action_sent" supabase/full-schema.sql` is `>= 1`
    - `git log -1 --name-only` for the apply commit lists all four files: both migrations, `backend/app/db/workflows.py`, `supabase/full-schema.sql`
    - `git diff --stat supabase/full-schema.sql` shows insertions only for the new objects; no unrelated `DROP` appears in the diff
    - The plan summary states either "applied via the Supabase SQL editor" or a RECORDED DEVIATION naming the method used and why
    - `grep -rn "supabase db push\|supabase db reset" .planning/phases/190-*/190-03-SUMMARY.md` prints nothing
  </acceptance_criteria>
  <action>
Present BOTH migration files to the operator in full (they must paste them, so they must see
them), then PAUSE for the human step described below. **Do not attempt to apply them yourself
with `supabase db push` or `supabase db reset` — both are banned by CLAUDE.md and `db reset`
would destroy dev data.**

After the operator confirms the paste succeeded:
1. Run `bash scripts/regenerate-full-schema.sh` — **with NO `--reset`**. Never hand-edit
   `supabase/full-schema.sql`.
2. Inspect `git diff --stat supabase/full-schema.sql` and confirm the new table, its four
   policies, its indexes and the widened CHECK all appear, and that no unrelated object was
   dropped.
3. Commit `116_connector_connections.sql`, `117_harness_audit_external_action_sent.sql`,
   `backend/app/db/workflows.py` and the regenerated `supabase/full-schema.sql` TOGETHER.

If the SQL-editor path proved impossible, RECORD THE DEVIATION in the summary — naming the
method used and why — exactly as 189-06 recorded its `psycopg2` deviation while the ban itself
held. Do not normalise a workaround into the discipline.
  </action>
  <verify>
    <automated>grep -c "connector_connections" supabase/full-schema.sql && grep -c "external_action_sent" supabase/full-schema.sql && cd backend && python -m pytest tests/unit/test_audit_event_registration.py -q</automated>
  </verify>
  <resume-signal>Type "applied" once both migrations have run in the SQL editor and `regenerate-full-schema.sh` has completed, or describe what blocked it.</resume-signal>
  <done>Both migrations are live in the local DB, `full-schema.sql` is regenerated by script (never hand-edited), and all four files are committed together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org A session → org B connection row | Enforced by RLS at the DB and by the application org filter at the resolver (D-15) |
| plain member → connection creation | `org:manage` on write; read stays org-wide (UI-SPEC U-02) |
| Python audit writer → Postgres CHECK | Two-directional: a mismatch is either a 23514 mid-run or a `ValueError` before the INSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-03-T8 | Elevation of privilege | `connector_connections` RLS | mitigate | Four policies mirroring `sso_configs`; SELECT is org-scoped with NO `is_system` escape branch, and the absence is stated in the header. Verified by `pg_policies` count = 4 and `relrowsecurity = t` |
| T-190-03-PRIV | Elevation of privilege | INSERT/UPDATE/DELETE | mitigate | `current_user_has_permission(org_id, 'org:manage')` — the already-seeded permission, not a new key. This is UI-SPEC U-02's server half; the hidden Add button is NOT the gate |
| T-190-03-SECRET | Information disclosure | `config jsonb` | mitigate | `COMMENT ON COLUMN` pins it to NON-secret facts; the real fence is plan 190-06's model (no secret key accepted) and plan 190-12's T6 drive over the definition JSONB |
| T-190-03-ORPHAN | Tampering | `org_id` on INSERT | mitigate | `autofill_org_id_by_owner('created_by')` + `NOT NULL`; the trigger fails safe to NULL which the column rejects |
| T-190-03-MIG | Tampering | migration filename | mitigate | `^[0-9]+_[a-z0-9_]+\.sql$` asserted — a letter suffix like `116b` is silently skipped by the Supabase CLI and would ship a table that does not exist |
| T-190-03-DATA | Denial of service | dev database | mitigate | `supabase db push` / `db reset` never run; asserted by grepping the summary. SQL-editor paste preserves dev data |
| T-190-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs. Acceptance: `git diff backend/requirements.txt frontend/package.json` prints nothing |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/unit/test_audit_event_registration.py -q` — `0 failed`
- `cd backend && python -c "from app.db.workflows import _AUDIT_EVENT_TYPES as t; print(len(t))"` — `24`
- In the SQL editor: `SELECT count(*) FROM pg_policies WHERE tablename='connector_connections'` — `4`
- `git diff --stat supabase/full-schema.sql` — new objects only
</verification>

<success_criteria>
- Two migrations exist with digit-only filenames, applied via the SQL editor (or with a recorded deviation)
- `full-schema.sql` regenerated by script, never hand-edited, committed with the migrations
- The 24-literal CHECK and the 24-member Python frozenset agree, proved by the two-directional G2 fence
- D-12's absent escape branch is stated in migration 116's own header comment
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-03-SUMMARY.md` when done, recording the re-derived migration head, the locked audit slug, the four SQL-editor sanity-check outputs, and either the SQL-editor apply or the recorded deviation.
</output>
