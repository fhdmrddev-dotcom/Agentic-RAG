---
phase: 190-live-connector-slice-connector-security-stretch
plan: 09
type: execute
wave: 3
depends_on: ["190-06"]
files_modified:
  - backend/app/api/connectors.py
  - backend/app/main.py
  - backend/app/api/admin.py
  - backend/app/models/user_settings.py
  - backend/tests/unit/test_190_connectors_api.py
  - frontend/src/lib/api.ts
autonomous: true
requirements: [CONN-02, CONN-03]

must_haves:
  truths:
    - "D-25 — connections are managed in SETTINGS, not the Control Room: the operator sets the allowed-set and the lock, the org admin sets the preference (CONN-02)"
    - "D-26 — the live_connectors kill-switch joins _VISIBILITY_FEATURES in admin.py and _GOVERNED_FEATURES in user_settings.py with the cold default off, costing ZERO migrations. This is the allowlist Phase 181 actually used for visual_workflow_canvas; CONTEXT D-26 names _FLAG_HUMAN_NAMES, which would cost migration 118 (CONN-03)"
    - "D-15 — the API CRUD path runs on the per-request user-JWT connection where RLS is a real runtime gate, and the module header says so per call site (CONN-03)"
    - "T7 — GET /connectors/connections returns no secret_ciphertext and no secret; the Pydantic response model is the gate and the TS type is documentation (CONN-03)"
    - "A connection id belonging to another org returns 404, never 403 — no existence leak, inherited verbatim from api/classification_rules.py:11-19"
    - "UI-SPEC U-02 — create/edit/delete are API-ENFORCED on org-admin via current_user_has_permission(org_id, 'org:manage'); read and bind stay org-wide. A hidden button is not a gate"
  artifacts:
    - path: "backend/app/api/connectors.py"
      provides: "org-scoped connector CRUD with the API-enforced org-admin write gate"
    - path: "backend/app/api/admin.py"
      provides: "live_connectors inside _VISIBILITY_FEATURES"
      contains: "live_connectors"
    - path: "backend/app/models/user_settings.py"
      provides: "the cold default live_connectors = off"
  key_links:
    - from: "backend/app/api/connectors.py"
      to: "backend/app/services/connector_service.py"
      via: "every route delegates; no SQL in the router"
      pattern: "connector_service\\."
    - from: "backend/app/main.py"
      to: "backend/app/api/connectors.py"
      via: "include_router with a phase-tagged comment"
      pattern: "connectors"
---

<objective>
Expose the connector CRUD surface, register the router, and add the `live_connectors`
kill-switch to the allowlist Phase 181 actually used.

Purpose: one of this phase's decisions was written against a wrong measurement and is corrected
here. **D-26 names `admin.py`'s `/admin/flags` kill-switch allowlist while citing the Phase-181
precedent — but `visual_workflow_canvas` is at `admin.py:104`, inside `_VISIBILITY_FEATURES`,
not `_FLAG_HUMAN_NAMES` at `:67-79`.** Taking the wrong one costs an `app_settings` boolean
column and therefore a needless migration 118. Taking the right one costs two lines across two
files and zero migrations — which is exactly what D-26 claims it costs.

Output: `api/connectors.py`, its registration, the two-line kill-switch, the API client
functions, and the T7 response fence.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md
</context>

<interfaces>
<!-- ⚠ CORRECTION #6, re-derived at HEAD 2026-08-08:
     $ grep -n "visual_workflow_canvas" backend/app/api/admin.py    → 104
     Line 104 is inside _VISIBILITY_FEATURES (:97-105), NOT _FLAG_HUMAN_NAMES (:67-79). -->

```python
_VISIBILITY_FEATURES = {
    "skill_studio",
    "model_management",
    "workflow_authoring",
    "governance_health",
    # Phase 181 (REVERT-01 / T-181-03): the v3.6 canvas flag is operator-writable through
    # the SAME allowlisted PUT /admin/visibility path (Off = "off", On = "everyone").
    "visual_workflow_canvas",
}
_VISIBILITY_AUDIENCES = {"everyone", "operators", "role", "off"}
```

And the cold default that makes "zero migration" true (`user_settings.py:1192-1204`):

```python
_GOVERNED_FEATURES: dict[str, str] = {
    ...
    # Phase 181 (REVERT-01 / D-181-01,05): … the 5th audience enum member "off" —
    # hidden from EVERYONE, operators included. This is the ONE authoritative cold default
    # (an unseeded feature_visibility key falls through to it), which is why NO migration is
    # needed: the app_settings.feature_visibility JSONB gains the key only on an operator
    # flip via set_feature_visibility's atomic `||` merge ("off" -> "everyone" and back).
    "visual_workflow_canvas": "off",
}
```

| Allowlist | Endpoint | Storage | Cost of one entry |
|---|---|---|---|
| `_FLAG_HUMAN_NAMES` (`:67`) | `PUT /admin/flags` | an `app_settings` **boolean COLUMN** | **a third migration (118)** |
| `_VISIBILITY_FEATURES` (`:98`) | `PUT /admin/visibility` | mig-098 JSONB, audience enum | **ZERO migrations** |

Consumers already exist: `dependencies.py:672` `feature_audience(...)`, `dependencies.py:693`
`resolve_feature_access(...)`, `api/features.py:69` (an `"off"` feature is hidden), and
`dependencies.py:518-557` `require_visible(feature)` — a dependency FACTORY whose own docstring
says to attach it **per-endpoint on governed authoring/management endpoints ONLY**, never at a
router level that would gate a Run/chat carve-out.

The router analog is `backend/app/api/classification_rules.py` (172 L) — the house CRUD shape,
itself cloned from `api/document_views.py`. Two rules from its docblock (`:11-19`) that 190
inherits verbatim:
- *"Every cross-user/unseeable miss collapses to a generic 404, NEVER the forbidden status — no
  existence leak."*
- *"UPDATE validates ownership FIRST … so an unowned/absent id uniformly 404s regardless of
  whether the submitted body is valid — no 422-vs-404 ordering oracle."*

The role gate is `current_user_has_permission(org_id, 'org:manage')`, already seeded to
`super-admin` + `org-admin` at `104_org_dept_role_schema.sql:416-426`. Reuse it; do not mint
`connectors:manage`.

The api.ts house shape (`api.ts:130-136`) — a bare `fetch` per function with `getAuthHeaders()`,
an explicit `res.ok` check and a typed cast. **There is no generic request wrapper; do not
introduce one for this phase.**
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: api/connectors.py — CRUD with the API-enforced org-admin gate and the no-existence-leak rule</name>
  <files>backend/app/api/connectors.py, backend/tests/unit/test_190_connectors_api.py</files>
  <read_first>
    - `backend/app/api/classification_rules.py` — the WHOLE file (172 lines): the router shape, the docblock's two rules, the dependency wiring
    - `backend/app/api/dependencies.py:510-560` (`require_visible`) and `:660-700` (`feature_audience`, `resolve_feature_access`)
    - `backend/app/services/connector_service.py` (from plan 190-06) — every route delegates to it
    - `backend/app/models/connector.py` (from plan 190-06)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §2b (U-02, and the sentence *"the gate must be API-enforced, not merely hidden"*) and §13 U-02
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-15, D-25
  </read_first>
  <action>
Create `backend/app/api/connectors.py` with `APIRouter(prefix="/connectors",
tags=["connectors"])`, in `classification_rules.py`'s shape. **No SQL in the router** — every
route delegates to `connector_service`.

Routes:
- `GET /connectors/connections` — optional `capability` query param. Org-scoped read, available
  to **everyone in the org** (UI-SPEC U-02: read and bind are org-wide). Returns
  `list[ConnectorConnectionResponse]`, which carries `last_check_verdict` per row because the
  picker's Gate 1 consumes it.
- `GET /connectors/connections/{id}` — 404 on a cross-org or absent id. **Never 403.**
- `POST /connectors/connections` — **org-admin only**, enforced server-side with
  `current_user_has_permission(org_id, 'org:manage')`. A `ConnectorCipherUnavailable` from the
  service becomes **HTTP 503** with a stable machine-readable reason code, which the panel
  renders as UI-SPEC §4b moment 9 (Save DISABLED — a refusal the person cannot fix).
- `PATCH /connectors/connections/{id}` — org-admin only; validate ownership FIRST so an unowned
  or absent id uniformly 404s regardless of body validity (no 422-vs-404 ordering oracle).
- `DELETE /connectors/connections/{id}` — org-admin only.

**Feature gate:** attach `require_visible("live_connectors")` **per endpoint** on the WRITE
endpoints, following `dependencies.py:518-557`'s own instruction never to gate at router level.
Decide and STATE in the module header whether the READ endpoint is gated: recommended NOT, so
the Settings tab can render its OFF banner (UI-SPEC §2h) rather than 404ing — and write that
reason down so the asymmetry reads as a decision rather than an oversight.

**Module header** must answer D-15 for this file specifically: these routes run on the
per-request user-JWT connection, so RLS is a real runtime gate here — unlike the harness
resolver, which runs on the BYPASSRLS pool where the application org filter is the gate and RLS
is the backstop.

Create `backend/tests/unit/test_190_connectors_api.py` covering, each RED-first against a named
plant in production source:
1. a cross-org id returns **404, not 403**, and the body names no owner (plant: return 403)
2. a plain member (no `org:manage`) is refused on POST/PATCH/DELETE **by the API alone**, with
   no UI involved (plant: drop the permission check; observe the member create succeed)
3. a plain member CAN list and get — the non-vacuity control for case 2
4. `ConnectorCipherUnavailable` surfaces as 503 with a stable reason code
5. an unowned id sent with an INVALID body still 404s, never 422 (the ordering oracle)
6. **T7** — the serialised response for a connection whose row HAS a `secret_ciphertext`
   contains neither the field name nor the ciphertext substring anywhere in the JSON (plant: add
   the field to the response model; observe RED)
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_190_connectors_api.py -q` reports `0 failed` and at least 6 passing tests
    - The summary records SIX plant→RED→restore cycles with verbatim failure text; `grep -c PLANT backend/app/api/connectors.py backend/app/models/connector.py` prints `0` for both
    - `grep -c "org:manage" backend/app/api/connectors.py` is `>= 3`
    - `grep -cE "403|HTTP_403" backend/app/api/connectors.py` prints `0`
    - `grep -c "require_visible" backend/app/api/connectors.py` is `>= 1`, and no `dependencies=` appears on the `APIRouter(` constructor line
    - `grep -ciE "select |insert into|update .* set" backend/app/api/connectors.py` prints `0` (no SQL in the router)
    - `grep -c "BYPASSRLS" backend/app/api/connectors.py` is `>= 1` (D-15 answered per call site in the header)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_190_connectors_api.py -q</automated>
  </verify>
  <done>The router exists with org-wide reads, API-enforced org-admin writes, 404-not-403 misses, a 503 for the unkeyed-cipher refusal, and a T7 fence proved by a plant.</done>
</task>

<task type="auto">
  <name>Task 2: Register the router and add the live_connectors kill-switch — two lines, zero migrations</name>
  <files>backend/app/main.py, backend/app/api/admin.py, backend/app/models/user_settings.py, backend/tests/unit/test_190_connectors_api.py</files>
  <read_first>
    - `backend/app/api/admin.py:60-115` — BOTH allowlists, so the correction is made against the source rather than against CONTEXT's prose
    - `backend/app/models/user_settings.py:1185-1210` — `_GOVERNED_FEATURES` and Phase 181's cold-default comment, verbatim
    - `backend/app/main.py:695-725` — the import block and the `include_router` lines with their phase-tagged comments
    - `backend/app/api/features.py:60-85` — how an `"off"` feature is honoured over the operator short-circuit
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md` §3.12 and `190-RESEARCH.md` §M9 plus Open Question #1
  </read_first>
  <action>
**A. Re-derive the correction before editing.** Run
`grep -n "visual_workflow_canvas" backend/app/api/admin.py` and read `sed -n '60,115p'
backend/app/api/admin.py`. Confirm with your own eyes which set contains it. Record the result
in the summary as the answer to research Open Question #1, together with the consequence: taking
`_FLAG_HUMAN_NAMES` would require an `app_settings` boolean column and therefore migration 118;
`_VISIBILITY_FEATURES` costs zero migrations. **If the measurement disagrees with this plan,
follow the measurement and say so.**

**B. `backend/app/api/admin.py`** — add `"live_connectors"` to `_VISIBILITY_FEATURES` with a
Phase-190-tagged comment in Phase 181's exact style, naming the requirement (CONN-03) and the
decision (D-26), and stating that Off = `"off"` and On = `"everyone"`.

**C. `backend/app/models/user_settings.py`** — add `"live_connectors": "off"` to
`_GOVERNED_FEATURES` with a comment matching Phase 181's, stating why no migration is needed:
this is the ONE authoritative cold default, an unseeded `feature_visibility` key falls through
to it, and the JSONB gains the key only on an operator flip via `set_feature_visibility`'s
atomic `||` merge.

**D. `backend/app/main.py`** — one import name plus one `include_router` line with a
phase-tagged comment, matching the shape of the neighbouring registrations.

**E. Add three assertions** to `backend/tests/unit/test_190_connectors_api.py`:
- `live_connectors` is present in `_VISIBILITY_FEATURES` and ABSENT from `_FLAG_HUMAN_NAMES` — a
  fence against a later "fix" that moves it and silently needs a migration
- its cold default is `"off"`, read from `_GOVERNED_FEATURES` rather than retyped
- with the audience `"off"` the write endpoints refuse, and with `"everyone"` they do not — the
  kill-switch exercised in **BOTH directions in code**, per G-4's *exercise toggles BOTH ways*

**F. Create no migration.** If any part of this task seems to need one, stop: that is the signal
that the wrong allowlist was taken.
  </action>
  <acceptance_criteria>
    - `cd backend && python -c "from app.api.admin import _VISIBILITY_FEATURES as V, _FLAG_HUMAN_NAMES as F; print('live_connectors' in V, 'live_connectors' in F)"` prints `True False`
    - `cd backend && python -c "from app.models.user_settings import _GOVERNED_FEATURES as G; print(G['live_connectors'])"` prints `off`
    - `ls supabase/migrations/ | wc -l` is unchanged from the count recorded at the end of plan 190-03 (no migration 118 was created)
    - `grep -c "connectors" backend/app/main.py` is `>= 2` (import plus `include_router`), and the `include_router` line carries a phase-tagged comment
    - `cd backend && python -c "import app.main"` exits 0 (the router registers with no import cycle)
    - `cd backend && python -m pytest tests/unit/test_190_connectors_api.py -q` reports `0 failed`, including the both-directions kill-switch case
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "from app.api.admin import _VISIBILITY_FEATURES as V, _FLAG_HUMAN_NAMES as F; assert 'live_connectors' in V and 'live_connectors' not in F; from app.models.user_settings import _GOVERNED_FEATURES as G; assert G['live_connectors']=='off'; import app.main; print('kill-switch OK, zero migrations')"</automated>
  </verify>
  <done>The router is registered, `live_connectors` sits in the zero-migration allowlist with an `off` cold default, and the switch is exercised in both directions by a test.</done>
</task>

<task type="auto">
  <name>Task 3: frontend/src/lib/api.ts — the connector client functions</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - `frontend/src/lib/api.ts:120-160` — the house shape: a bare `fetch` per function with `getAuthHeaders()`, an explicit `res.ok` check and a typed cast. There is NO generic wrapper and this phase must not add one
    - `backend/app/models/connector.py` — the response model, which the TS types mirror
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §6d (the picker's four honest states: populated / empty / loading / error) and §5c (the check's dedicated action)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md` §3.19
  </read_first>
  <action>
Add to `frontend/src/lib/api.ts`, each function in the file's existing bare-`fetch` shape:

- `listConnectorConnections(capability?: string): Promise<ConnectorConnection[]>`
- `getConnectorConnection(id: string)`
- `createConnectorConnection(body: ConnectorConnectionCreate)`
- `updateConnectorConnection(id: string, body: ConnectorConnectionUpdate)`
- `deleteConnectorConnection(id: string)`

And the TS types `ConnectorConnection`, `ConnectorConnectionCreate`, `ConnectorConnectionUpdate`.

**`ConnectorConnection` must not declare `secret_ciphertext` or `secret`.** The Pydantic model
is the real gate (proved in Task 1); the TS type is documentation, and documentation that names
a field the server never sends invites a component to render it.

**Error handling must distinguish the three states UI-SPEC §4d separates** — refused,
unreachable, rejected — rather than collapsing them into one "could not connect". Surface the
server's `reason_code` on a typed error so the panel can render the right heading from the
closed §4c map. **Do not author any user-facing sentence here**: the copy lives in the component
layer as exported identifiers (the `GovernanceSection.tsx:10-17` idiom), so it can be asserted
by character-identity.

The check endpoint's client function is deliberately NOT added here — it lands with the check
action in plan 190-15, so the function and the endpoint arrive together.
  </action>
  <acceptance_criteria>
    - `grep -c "ConnectorConnection" frontend/src/lib/api.ts` is `>= 5`
    - `grep -cE "secret_ciphertext|\bsecret\b" frontend/src/lib/api.ts` prints `0` inside the `ConnectorConnection` type declaration (verify by printing the type block)
    - `grep -c "reason_code" frontend/src/lib/api.ts` is `>= 1`
    - No generic request wrapper was introduced: `git diff -U0 frontend/src/lib/api.ts | grep -ciE "function (request|apiFetch|http)\(" ` prints `0`
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` is at or below the re-measured baseline
    - `cd frontend && npm test` reports `0 failed`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>Five client functions and three types exist in the house shape, the connection type cannot carry a secret, the three error states stay distinguishable, and `tsc` is at or below baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated member → connection write | A member who can create a connection binds colleagues' published workflows to a destination they chose (UI-SPEC §2b) |
| API response → browser | T7: the ciphertext must never leave the server, for any caller |
| operator flag → the send path | D-26: with `live_connectors` off, an `external_action` step must behave exactly as it does today |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-09-U02 | Elevation of privilege | POST/PATCH/DELETE `/connectors/connections` | mitigate | `current_user_has_permission(org_id,'org:manage')` enforced server-side, driven by a test that calls the API with no UI. A hidden Add button is explicitly NOT the gate (UI-SPEC §2b). Plant = drop the check, observed RED |
| T-190-09-T7 | Information disclosure | `ConnectorConnectionResponse` + the TS type | mitigate | Neither declares `secret_ciphertext`; a test asserts the ciphertext substring is absent from the serialised JSON. Plant = add the field, observed RED |
| T-190-09-LEAK | Information disclosure | 403-vs-404 | mitigate | Cross-org and absent ids collapse to 404; `grep` asserts no 403 exists in the router. Ownership is validated before body validity, so there is no 422-vs-404 oracle |
| T-190-09-FLAG | Tampering | `_VISIBILITY_FEATURES` / `_GOVERNED_FEATURES` | mitigate | The correct allowlist is re-derived by grep before editing; a test asserts membership in one set and ABSENCE from the other, so a later move that silently needs migration 118 fails loudly. Cold default `off`, exercised in both directions |
| T-190-09-503 | Denial of service | unkeyed-cipher refusal | accept | A 503 with a stable reason code is the correct, honest answer when the platform cannot store a tenant credential safely (D-11). The cost is stated in UI-SPEC §4b moment 9 rather than hidden |
| T-190-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs; no generic HTTP wrapper and no client SDK added. Acceptance: `git diff backend/requirements.txt frontend/package.json` prints nothing |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/unit/test_190_connectors_api.py -q` — `0 failed`
- `cd backend && python -c "import app.main"` — exits 0
- `ls supabase/migrations/ | wc -l` — unchanged since plan 190-03
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` — at or below baseline
- `git diff --numstat backend/requirements.txt frontend/package.json` prints nothing
</verification>

<success_criteria>
- Connector CRUD is exposed with org-wide reads and API-enforced org-admin writes
- The kill-switch costs two lines and zero migrations, and is proved to be in the right allowlist
- No secret can reach a response, proved by a plant on the response model
- The client functions match the house shape and introduce no wrapper
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-09-SUMMARY.md` when done, recording the re-derived allowlist measurement (research Open Question #1's answer), the six plant cycles, and the unchanged migration count.
</output>
