---
phase: 190-live-connector-slice-connector-security-stretch
plan: 19
type: execute
wave: 8
depends_on: ["190-01", "190-02", "190-03", "190-04", "190-05", "190-06", "190-07", "190-08", "190-09", "190-10", "190-11", "190-12", "190-13", "190-14", "190-15", "190-16", "190-17", "190-18"]
files_modified:
  - .planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [CONN-02, CONN-03]

must_haves:
  truths:
    - "D-28 — every row of the 14-row falsification set T1..T14 was OBSERVED RED against a real plant in production source, every plant was restored md5-identical, and `grep -c PLANT` reads 0 across the whole tree; the phase is ready for /gsd:secure-phase with threats_open: 0 (CONN-03)"
    - "D-29 — the SC#10 roster is DERIVED by grouping MODEL_CAPABILITIES, never re-typed; all 8 provider rows are present and every one is PASS or ⛔ with a named reason and a blocking issue id. A scoreboard that lists only what passed is not a scoreboard (CONN-02)"
    - "D-30 — the three live-send rows require an operator-provided mailbox, Jira project and Slack channel; the phase MAY close with them owed, but only as a stated DECISION recorded in the ROADMAP progress row and STATE.md, naming which row to run first (post_message / Slack) — never as a claim that everything ran (CONN-02)"
    - "D-31 — every G-6 failure condition maps to a named row that can actually catch it, and the mapping is written down rather than asserted (CONN-03)"
    - "D-32 — the scope fence is re-checked at close: no broad catalog, no inbound webhook, no public REST API, no MCP client, no service account, no OAuth flow, no 4th capability, no retry/idempotency/queue, no scheduling, no contact directory, no expression language or arbitrary-code node, and no run-surface approval affordance was built"
    - "D-23 and D-24 — the eight fenced files end the phase with a MEASURED `git diff --numstat` against the phase's base commit of 0 0, verified by a real command whose output is recorded verbatim"
    - "G-7 — node scripts/check-gap-closure-rounds.cjs 190 is run at close and its verdict recorded, so a later gaps round starts from a measured number rather than a remembered one"
  artifacts:
    - path: ".planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md"
      provides: "the filled per-task verification map, the derived 8-row scoreboard and the sign-off"
  key_links:
    - from: ".planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md"
      to: "backend/app/config.py::MODEL_CAPABILITIES"
      via: "the roster is derived by grouping, never re-typed"
      pattern: "MODEL_CAPABILITIES"
---

<objective>
Close the phase honestly: fill VALIDATION.md's per-task map, derive the SC#10 roster from
`MODEL_CAPABILITIES`, run the phase-wide fence sweep, and record the owed manual rows as a
DECISION rather than as a claim.

Purpose: this project's recurring failure is a scoreboard that lists only what passed. **Rows
may be ⛔, but never silently omitted**, and a phase that closes with owed UAT rows closes on a
stated decision naming which row to run first. This plan is `autonomous: false` because the
owed-rows decision and the D-30 dependency belong to the operator.

Output: a filled VALIDATION.md, a measured fence sweep, a derived scoreboard, and a ROADMAP /
STATE record that a reader can audit.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md
</context>

<interfaces>
<!-- The roster rule, from CLAUDE.md, amended 2026-07-31 by the operator: the required set is
     the FULL NATIVE ROSTER plus OpenRouter — 8 rows, not 4. Measured 2026-08-08: -->

```
TOTAL model ids: 61   providers: 8
  anthropic n=7 · deepseek n=2 · google n=7 · minimax n=8
  moonshot n=3 · openai n=17 · openrouter n=9 · zhipu n=8
```

**Derive the roster, never re-type it.** Group `MODEL_CAPABILITIES` by `provider` and take one
representative per group. Prefer the **newest** model per provider and prefer a
**registry-backed** id: an id absent from `MODEL_CAPABILITIES` resolves
`capability_source=inferred` and silently loses `emit_tier`, so the row would measure a weaker
configuration than the one that ships (SEED-040, SEED-135).

**The measured method (§M1) — and it INVERTS D-29's caution.** There is no workflow-run POST
route; a run launches through `POST /threads/{thread_id}/messages`, and `MessageCreate` carries
`model`, `provider` **and** `workflow_definition_id` on one request. Per-request routing WORKS,
so no global setting is mutated, the operator's environment is untouched and rows cannot
contaminate each other. Verdicts read from `workflow_runs` / `workflow_phases` / `harness_audit`.

**The measured cost:** the per-thread anchor takes a **409 lock**
(`workflow_kickoff.py:146-173`), so **8 rows need 8 DISTINCT THREADS to run in parallel; two rows
on one thread are serial.** This is a plan input, not a mid-UAT discovery.

**The other three SC#10 axes:** multi-tool (a workflow with `retrieve` + `external_action` in
one run) · parallel-thread (Thread A streaming while Thread B accepts a prompt, 409-safe on
different threads) · long-message (≥ 50 prior messages or a ≥ 5 KB prompt).

**The demo sentence — BOTH halves are UAT rows:**
> *"a published workflow paused for approval, the operator approved, and an email actually
> arrived — and the same workflow, with the connection unbound, still says 'Not sent — recorded'
> rather than pretending."*

Row **A** is the send (⛔ until D-30's destinations exist). Row **B** is `recorded_not_sent` on
the SAME definition with the connection unbound — **it has no operator dependency and should be
driven FIRST**. It is the half competitors do not have, and the only row that proves D-17's
terminal survived the phase that made sending real.

**Recommended first live row: `post_message` (Slack)** — it needs only a bot token and a channel
id, its host is a code constant so it has the smallest SSRF surface, and it is the one row that
falsifies **T13**, the likeliest shipped defect.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The phase-wide fence sweep and the measured gate re-run</name>
  <files>.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` — § Source Fences, § The Security Falsification Set, § Validation Sign-Off
    - Every `190-NN-SUMMARY.md` produced so far — the RED observations and md5 pairs live there, and this task aggregates rather than re-derives them
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §1b (the eight fenced files) and §14
  </read_first>
  <action>
Run the whole sweep and record every output VERBATIM. Do not summarise a number you did not
just produce.

**A. The eight empty diffs (D-23 / D-24).** Determine the phase's base commit (the commit before
plan 190-01's first commit; derive it with `git log --oneline` and name it in the summary), then
run:
`git diff --numstat <base> -- frontend/src/components/workflows/PhaseFormPanel.tsx
frontend/src/components/workflows/PhaseNode.tsx frontend/src/components/workflows/PhaseNodeCard.tsx
frontend/src/components/workflows/phaseNodeCardContract.ts frontend/src/components/workflows/ownProperty.ts
frontend/src/components/workflows/NodeCornerMarks.tsx frontend/src/components/workflows/NodeRunOverlay.tsx
frontend/src/components/workflows/NodeIconWell.tsx`
It must print NOTHING. **This is the phase-wide fence** — the per-plan checks were checkpoints;
this one is against the phase's own base, which is the only comparison that catches a file
opened in one plan and closed in another.

**B. No plants survive.** `grep -rc PLANT backend/app backend/tests frontend/src` → 0 everywhere.

**C. Both suites and both gates**, each command run and its output recorded:
- `cd backend && python -m pytest tests/ -q`
- `cd frontend && npm test`
- `node scripts/vitest-count-gate.cjs`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"`
Compare each against the baseline measured at the START of this phase (running 2727 / pinned
2714 / 48 files, `tsc` 33 on 2026-08-08) — **but re-measure the baseline from the phase's base
commit rather than quoting those numbers**, because pins drift silently and this phase already
met a +13 drift.

**D. Zero installs.** `git diff --numstat <base> -- backend/requirements.txt frontend/package.json
frontend/package-lock.json` prints nothing. Research recorded the Package Legitimacy Audit as
NOT APPLICABLE precisely because this phase installs nothing; if that is no longer true, the
audit becomes mandatory and this task stops.

**E. Fill VALIDATION.md's § Source Fences** with the actual result of each of the seven fences,
naming the test that carries it and the plan that drove its positive control.

**F. Fill § The Security Falsification Set** — for each of T1…T14, the plan number, the test
name, the plant, and the VERBATIM RED observation. A row without a recorded RED observation is
not done; say so rather than marking it green.
  </action>
  <acceptance_criteria>
    - `git diff --numstat <base> -- <the eight fenced files>` prints NOTHING, and that empty output plus the base commit sha are recorded verbatim in VALIDATION.md
    - `grep -rc PLANT backend/app backend/tests frontend/src` reports `0` for every file
    - `cd backend && python -m pytest tests/ -q` — result recorded; no new failures versus the re-measured base-commit baseline
    - `node scripts/vitest-count-gate.cjs` exits 0 printing `count gate OK`, and the printed running/pinned/file counts are recorded
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` is at or below the re-measured base-commit number, and BOTH numbers are recorded
    - `git diff --numstat <base> -- backend/requirements.txt frontend/package.json frontend/package-lock.json` prints nothing
    - VALIDATION.md's falsification table has all 14 rows filled with a plan number, a test name, a plant and a verbatim RED observation — or is explicitly marked incomplete for any row that lacks one
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/ -q; cd ../frontend && npm test; cd .. && node scripts/vitest-count-gate.cjs && grep -rc PLANT backend/app frontend/src | grep -v ':0' | wc -l</automated>
  </verify>
  <done>All four gates re-measured against the phase's own base commit, the eight-file diff is provably empty, no plant survives, nothing was installed, and VALIDATION.md's fence and falsification tables are filled from recorded observations.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the SC#10 roster, fill the per-task verification map, and drive Row B</name>
  <files>.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md</files>
  <read_first>
    - `CLAUDE.md` § "UAT scoreboard recipe" — the full-native-roster rule and the ⛔-never-omitted rule, verbatim
    - `backend/app/config.py` — `MODEL_CAPABILITIES`, so the roster is DERIVED by grouping
    - `backend/app/models/message.py` — `MessageCreate` (`model`, `provider`, `workflow_definition_id` on one request)
    - `backend/app/services/workflow_kickoff.py:140-180` — the 409 anchor lock that forces 8 distinct threads
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` § SC#10 and § Manual-Only Verifications
  </read_first>
  <action>
**A. Derive the roster.** Run a grouping over `MODEL_CAPABILITIES` (one command, output
recorded) and pick one representative per provider — newest, registry-backed. Paste the command
AND its output into VALIDATION.md above the table, so the next reader can re-derive rather than
trust. **Never re-type the list**: an id absent from the registry resolves
`capability_source=inferred` and silently loses `emit_tier`, measuring a weaker configuration
than the one that ships.

**B. Run what can be run.** Each row is one `POST /threads/{id}/messages` with
`{content, model, provider, workflow_definition_id}` naming a published workflow that contains
an `external_action` step. **Use 8 DISTINCT THREADS** — the per-thread anchor 409s, so two rows
on one thread are serial. Read verdicts from `workflow_runs` / `workflow_phases` /
`harness_audit`. **No global setting is mutated**, so the operator's environment is untouched.

For any provider with no key configured or blocked by a known defect, record **⛔ with the reason
and the blocking issue id** — never drop the row.

**C. Drive Row B now — it has NO operator dependency.** The SAME definition with the connection
**unbound**: run it, and confirm the step reads *"Not sent — recorded"*, status
`recorded_not_sent`, never *"Complete"*. Record the run id and the `workflow_phases` row.
**Row B is not optional** — it is the half competitors do not have and the only row that proves
D-17's terminal survived the phase that made sending real.

**D. Drive the kill-switch BOTH ways (G-4).** Flip `live_connectors` off → an `external_action`
step records and does not send, reading *"Not sent — recorded"*. Flip on → it sends (or reaches
the send and fails honestly, if D-30's destination is still missing). Record both directions
with their run ids. Exercising a toggle in one direction only is the lived-experience gap
CLAUDE.md names.

**E. Fill the per-task verification map** — one row per task across plans 190-01…190-18, each
with its wave, requirement, threat ref, secure behaviour, test type, automated command and
status. Then check the sampling-continuity rule: **no 3 consecutive tasks without an automated
verify**. If the map shows a run of three, say so plainly rather than adjusting the map.

**F. Fill the other three SC#10 axes** — multi-tool, parallel-thread and long-message — driving
what can be driven and marking the rest ⛔ or ⬜ with a reason.
  </action>
  <acceptance_criteria>
    - VALIDATION.md's SC#10 table has exactly 8 rows, one per provider, each naming a representative model id that EXISTS in `MODEL_CAPABILITIES` — verified by a command whose output is pasted above the table
    - Every SC#10 row reads PASS or ⛔-with-reason-and-issue-id; no row is blank and none is omitted
    - Row B is driven and its run id plus the `workflow_phases` status (`recorded_not_sent`) are recorded
    - The kill-switch is recorded in BOTH directions with two run ids
    - The per-task verification map has a row for every task in plans 190-01…190-18, each with an automated command
    - The sampling-continuity check is stated explicitly: either "no 3 consecutive tasks without an automated verify" or a named exception
    - `cd backend && python -c "from app.config import MODEL_CAPABILITIES as M; import collections; print(collections.Counter(v['provider'] for v in M.values()))"` output is pasted verbatim into VALIDATION.md
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "from app.config import MODEL_CAPABILITIES as M; import collections; c=collections.Counter(v['provider'] for v in M.values()); print(len(M), len(c)); print(sorted(c.items()))"</automated>
  </verify>
  <done>The roster is derived and pasted with its command, all 8 rows carry a verdict or a named ⛔, Row B and both kill-switch directions are driven with run ids, and the per-task map is complete with its continuity check stated.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: [BLOCKING] Record the owed manual rows as a DECISION, and run the G-7 check</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md</files>
  <read_first>
    - `CLAUDE.md` § "Workflow guardrails" G-7 in full, including the three points at which `check-gap-closure-rounds.cjs` must run and the worded escape hatches
    - `scripts/check-gap-closure-rounds.cjs` — read it before running it, so its output is understood rather than pasted
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` § Manual-Only Verifications and § D-30
    - `.planning/ROADMAP.md` — the Progress table row for Phase 190
    - `.planning/STATE.md` — the `stopped_at` / `last_activity` conventions. ⚠ `state.record-session` has corrupted this file before: **always `git diff .planning/STATE.md` after that verb**
  </read_first>
  <decision>
Phase 190's three **live-send** UAT rows (a real email arriving, a real Jira ticket being
created, a real Slack message being posted) require operator-provided destinations: a throwaway
mailbox, a Jira project and a Slack channel (**D-30**). Every adapter is unit-proven against a
stub and every security property is proved without a real destination — what is unproven is that
a real message **arrives**.

**The phase may legitimately close with these rows owed — but only as a stated DECISION, never
as a claim that everything ran.**
  </decision>
  <context>
Everything else is measurable and has been measured: the 14-row falsification set, the seven
source fences, the eight empty diffs, the four gates, Row B (`recorded_not_sent` on the same
definition, no operator dependency) and the kill-switch in both directions.

The recommended first live row is **`post_message` (Slack)**: it needs only a bot token and a
channel id, its host is a code constant so it carries the smallest SSRF surface, and it is the
one row that falsifies **T13** — the likeliest defect this phase ships.
  </context>
  <options>
    <option id="close-with-owed-rows">
      <name>Close the phase with the three live-send rows owed, recorded as a decision</name>
      <pros>Every automatable property is proved; the phase does not idle waiting on external accounts; the owed rows are named, dated and prioritised so they cannot be forgotten</pros>
      <cons>CONN-02's demo sentence is only half-proved until a real message arrives; assumption A1 (Jira's error envelope) stays unverified</cons>
    </option>
    <option id="hold-for-destinations">
      <name>Hold the phase open until the operator supplies all three destinations</name>
      <pros>The demo sentence is proved end to end before close; A1 is verified against a live Jira</pros>
      <cons>Blocks close on an external dependency with no fallback; the code is already fully fenced and would sit unmerged</cons>
    </option>
    <option id="slack-only">
      <name>Hold only for the Slack row, close with the email and Jira rows owed</name>
      <pros>Falsifies T13 — the likeliest shipped defect — against a real host, at the smallest setup cost</pros>
      <cons>Still leaves A1 unverified; still an external dependency, just a cheaper one</cons>
    </option>
  </options>
  <action>
Whatever the operator chooses, record it in three places and run the G-7 check.

**A. Run `node scripts/check-gap-closure-rounds.cjs 190`** and record its exit code and printed
derivation verbatim. Run it at close so a later `--gaps` routing starts from a measured number
rather than a remembered one. Exit `0` = G-7 clear, `1` = G-7 fires, `2` = harness error.

**B. `.planning/ROADMAP.md`** — update the Phase 190 Progress row with the plan count, the
status, and the owed rows **named** (which capability, why owed, which to run first). Do not
write "Complete" over a phase with owed rows; write what is true.

**C. `.planning/STATE.md`** — record the decision, the owed rows, the recommended first row
(`post_message`), assumption A1's verification trigger, the cloud-parity queue now including
migrations 116 and 117, and RESIDUAL-190-01 with its trigger. ⚠ **Always `git diff
.planning/STATE.md` after any `state.record-session`** — that verb has silently deleted
`stopped_at`, truncated `last_activity` and rewritten unrelated sections in this file before.

**D. `190-VALIDATION.md`** — set the sign-off checkboxes to what is actually true, mark the owed
rows ⛔ *awaiting operator-provided destination (D-30)*, and set `nyquist_compliant` to the value
the filled map supports rather than to `true` by default.

**E. Restate the D-32 scope fence at close**, one line, so a gap-closure round has a written line
to argue against rather than a remembered one: no broad catalog, no inbound webhooks, no public
REST API, no MCP client, no service accounts, no OAuth flow, no 4th capability, no
retries/idempotency/queue, no scheduling, no contact directory resolution, no expression
language or arbitrary-code node, and no run-surface approval affordance (BUG-260808-02 stays
DEFERRED with its trigger).

**F. Confirm no reported bug was folded.** Per CONTEXT `<deferred>`, no report carries
`folded_into: 190`. Verify with a grep over `.planning/reported-bugs/` and record the result; if
one does, it must be addressed by a task or its status reverted.
  </action>
  <acceptance_criteria>
    - `node scripts/check-gap-closure-rounds.cjs 190` was run and its exit code plus printed derivation are recorded verbatim
    - The ROADMAP Phase 190 progress row names the owed rows and does not read "Complete" while rows are owed
    - `grep -c "post_message" .planning/STATE.md` is `>= 1` and STATE.md names it as the first owed row to run
    - `grep -cE "116|117" .planning/STATE.md` shows both migrations added to the cloud-parity queue line
    - `grep -c "RESIDUAL-190-01" .planning/STATE.md` is `>= 1`, with its trigger (a Python or httpx version bump)
    - `grep -rl "folded_into: 190" .planning/reported-bugs/` prints nothing, and that result is recorded
    - `190-VALIDATION.md`'s sign-off reflects what was actually run; every owed row reads ⛔ with `D-30` named
    - `git diff .planning/STATE.md` was inspected after any SDK write, and the summary says so
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-gap-closure-rounds.cjs 190; echo "exit=$?"; grep -rl "folded_into: 190" .planning/reported-bugs/ | wc -l</automated>
  </verify>
  <resume-signal>Select: close-with-owed-rows, hold-for-destinations, or slack-only — or describe a different call.</resume-signal>
  <done>The operator's decision is recorded in the ROADMAP, STATE.md and VALIDATION.md; G-7's check has been run and its verdict recorded; the scope fence and the owed-row priority are written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded verdict → the next reader | A scoreboard that lists only what passed is how a gap survives a phase close |
| phase close → a later gaps round | G-7: without a measured round count, the loop manufactures its own next round |
| owed UAT rows → the claim of completeness | Closing with owed rows is legitimate; claiming everything ran is not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-19-SCORE | Repudiation | the SC#10 scoreboard | mitigate | Roster DERIVED by a pasted command over `MODEL_CAPABILITIES`; 8 rows, each PASS or ⛔-with-reason-and-issue-id, none omitted |
| T-190-19-FENCE | Tampering | the eight fenced card-subtree files | mitigate | `git diff --numstat <phase base> -- <eight files>` asserted empty against the PHASE'S OWN BASE, which is the only comparison that catches a file opened in one plan and closed in another |
| T-190-19-PLANT | Tampering | production source | mitigate | `grep -rc PLANT` over `backend/app`, `backend/tests` and `frontend/src` reads 0 everywhere; every plant's md5 restore is recorded in its own plan's summary |
| T-190-19-D30 | Repudiation | the live-send rows | accept-with-decision | The rows are ⛔ *awaiting operator-provided destination*, recorded as a DECISION in the ROADMAP and STATE.md naming `post_message` as the first to run. Never recorded as passed |
| T-190-19-A1 | Repudiation | Jira's assumed error envelope | accept-with-trigger | Unverified until the first live `create_ticket` row; the trigger is written into SECURITY.md and STATE.md rather than remembered |
| T-190-19-RES | Tampering | the DNS-pin recipes | accept-with-fence | RESIDUAL-190-01 recorded with its `inspect.getsource` fence and its trigger (a Python or httpx version bump in `requirements.txt`) |
| T-190-19-G7 | Repudiation | a future gaps round | mitigate | `check-gap-closure-rounds.cjs 190` run at close with its derivation recorded, and D-32's fence restated as a written line |
| T-190-SC | Tampering | npm/pip/cargo installs | mitigate | `git diff --numstat <base> -- backend/requirements.txt frontend/package.json frontend/package-lock.json` prints nothing; the Package Legitimacy Audit stays NOT APPLICABLE. If that changes, this plan stops and the audit becomes mandatory |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/ -q` and `cd frontend && npm test` — recorded, no new failures versus the base-commit baseline
- `node scripts/vitest-count-gate.cjs` — `count gate OK`, totals recorded
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` — at or below the base-commit number
- `git diff --numstat <base> -- <the eight fenced files>` — empty
- `grep -rc PLANT backend/app backend/tests frontend/src` — 0 everywhere
- `node scripts/check-gap-closure-rounds.cjs 190` — exit code recorded
</verification>

<success_criteria>
- VALIDATION.md carries a filled per-task map, all 14 falsification rows with verbatim RED observations, and seven fence results
- The SC#10 roster is derived with its command pasted, and all 8 rows carry a verdict or a named ⛔
- Row B and both kill-switch directions are driven with run ids
- The owed live-send rows are recorded as a DECISION in the ROADMAP and STATE.md, naming Slack as the first to run, and G-7's check has been run
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-19-SUMMARY.md` when done, recording the base commit sha, every gate's measured output, the derived roster command and its output, the G-7 verdict, and the operator's decision on the owed rows.
</output>
