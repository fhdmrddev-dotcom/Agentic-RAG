---
phase: 190-live-connector-slice-connector-security-stretch
plan: 14
type: execute
wave: 5
depends_on: ["190-08", "190-10", "190-11"]
files_modified:
  - backend/tests/unit/test_190_connector_source_fence.py
  - backend/tests/unit/test_190_ssti_fence.py
autonomous: true
requirements: [CONN-03]

must_haves:
  truths:
    - "D-05 — backend/app/services/connectors/** contains ZERO occurrences of httpx., requests., smtplib., urllib.request, socket. and .sendmail( ; the fence carries a non-vacuity floor and was observed RED against a real planted httpx.AsyncClient() in a real adapter file (CONN-03)"
    - "D-04 — the adapter registry's key set == EXTERNAL_ACTION_CAPABILITIES, asserted at module scope and observed firing at IMPORT against a planted fourth key; every capability resolves to an importable adapter (CONN-03)"
    - "D-09 — CONN-03 SC#3 is satisfied by NOT adding an evaluator: a fence proves no new expression language exists on this path, and a field containing {{7*7}} or {{''.__class__}} renders LITERALLY. The plan says it PROVED an existing property rather than claiming to have built something (CONN-03)"
    - "D-32 — no arbitrary-code node and no expression surface exists on the business canvas; the fence asserts the absence rather than trusting it"
    - "Every fence in this plan lists its positive control FIRST, in the shipped test_189_no_egress.py idiom, so a typo in a matcher cannot make it green forever"
  artifacts:
    - path: "backend/tests/unit/test_190_connector_source_fence.py"
      provides: "the D-05 banned-token walk with its non-vacuity floor and planted control, plus the D-04 registry assert"
    - path: "backend/tests/unit/test_190_ssti_fence.py"
      provides: "the SC#3 proof — no evaluator added, and composed fields render literally"
  key_links:
    - from: "backend/tests/unit/test_190_connector_source_fence.py"
      to: "backend/app/services/connectors/"
      via: "a walk over every .py file in the package"
      pattern: "connectors"
---

<objective>
Land the three fences that guard properties rather than patches: the D-05 no-transport walk over
`connectors/**`, the D-04 closed-registry assert, and the D-09/SC#3 no-evaluator proof.

Purpose: **CONN-03 SC#3 is satisfied by NOT adding an evaluator.** The plan must say that
plainly rather than claim to have built sandboxing — the shipped
`SandboxedEnvironment(autoescape=True)` is already the only Jinja path in this app, and SC#3's
proof is therefore *a fence over an existing property*. Over-claiming here would be the exact
failure mode this whole node type exists to avoid.

Output: two test files, three fences, each with a positive control listed first and each driven
RED against a real plant in production source.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md
</context>

<interfaces>
<!-- The fence idiom is MANDATORY and shipped: test_189_no_egress.py:206-262.
     A fence gets a positive control TEST OF ITS OWN, listed FIRST, with both a fires-on corpus
     and a must-not-fire corpus: -->

```python
def test_the_mcp_matcher_actually_matches():
    """V11 positive control — the fence's matcher fires on a haystack that HAS the token.

    Without this, a typo in the regex (or an accidental over-escape) would make
    ``test_no_mcp_identifiers_in_backend_app`` green forever while checking nothing at all.
    """
    for haystack in fires_on:
        assert _MCP_TOKEN.search(haystack), (…)
    for haystack in ["mcpherson", "compute", "dmcpx", "camp", "McPherson"]:
        assert not _MCP_TOKEN.search(haystack), (…)
```

and the fence asserts **non-vacuity of its own walk** before asserting the property:

```python
    files = _app_python_files()
    assert len(files) > 100, (
        f"the fence walked only {len(files)} python files under {_APP_ROOT} - the walk is "
        "broken and the fence proves nothing (measured at plan time: 160)"
    )
```

**D-05's banned token list**, including the one research §R13 added: `httpx.`, `requests.`,
`smtplib.`, `urllib.request`, `socket.`, **`.sendmail(`**.

**The SSTI containment SC#3 is proved against** — the shipped, only Jinja path:
`template_render_service.py:657` and `tool_dispatcher.py:2473`, both
`SandboxedEnvironment(autoescape=True)`.

**D-09, verbatim from CONTEXT:** *"A new expression syntax on the business canvas is out of
scope and out of character — it is the same supply-chain-shaped affordance
`docs/CONNECTOR-ARCHITECTURE.md` rules out 'ever'. SC#3's proof is therefore largely a fence
over an existing property, and the plan must say so rather than claiming to have built
something."*
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The D-05 source fence — banned tokens over connectors/**, with its planted control</name>
  <files>backend/tests/unit/test_190_connector_source_fence.py</files>
  <read_first>
    - `backend/tests/unit/test_189_no_egress.py:200-270` — the fence idiom in full: the positive control listed first, the fires-on and must-not-fire corpora, and the non-vacuity floor on the walk
    - `backend/app/services/connectors/` — every file, so the walk's expected file count is measured rather than guessed
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` § Source Fences (the D-05 row and its named plant)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-05
  </read_first>
  <action>
Create `backend/tests/unit/test_190_connector_source_fence.py`.

1. **`test_the_banned_token_matcher_actually_matches` — FIRST in the file.** A fires-on corpus
   (`httpx.AsyncClient()`, `import requests`… as it would appear in real source,
   `smtplib.SMTP_SSL(`, `urllib.request.urlopen(`, `socket.socket(`, `smtp.sendmail(`) and a
   must-not-fire corpus that proves the matcher is not a bare substring search (for example a
   comment mentioning the word `socket` in prose, an identifier like `websocket_handler`, and
   the string `sendmailer`). Without this test a typo makes the fence green forever.
2. **`test_the_walk_is_not_vacuous`** — assert the walk finds at least the number of `.py` files
   the package actually contains. **Measure it now** with `ls backend/app/services/connectors/`
   and put the measured number in the assertion message, in the shipped idiom's style
   ("measured at plan time: N"). A fence that walks zero files proves nothing.
3. **`test_no_connector_module_names_a_transport`** — walk `backend/app/services/connectors/**`
   and assert zero occurrences of every banned token. Report the offending file and line in the
   assertion message, so a failure is actionable rather than a boolean.
4. **`test_every_capability_resolves_to_an_importable_adapter`** — now that all three adapters
   exist, import each through the registry and assert its `CAPABILITY` attribute equals the key
   it was registered under. A lazy-import registry that names a missing module fails here rather
   than at 3 a.m. on a run.
5. **`test_the_registry_key_set_is_derived_not_retyped`** — assert
   `set(_ADAPTERS) == set(EXTERNAL_ACTION_CAPABILITIES)`, and assert on source that
   `registry.py` references `EXTERNAL_ACTION_CAPABILITIES` (so a future edit cannot satisfy the
   equality with a hand-typed list that happens to match today).

**Then drive TWO plants**, each in production source and each restored md5-identical:
- PLANT A — insert a real `httpx.AsyncClient()` into `slack_adapter.py`. Observe test 3 RED,
  naming the file and line.
- PLANT B — add a fourth key to `registry.py`. Observe the module-scope `assert` fire **at
  import**, and test 5 RED.

Confirm `grep -rc PLANT backend/app/services/connectors/` is `0` in every file afterwards.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_190_connector_source_fence.py -q` reports `0 failed` with at least 5 passing tests
    - The positive-control test is the FIRST test function in the file (verify with `grep -n "def test_" | head -1`)
    - The summary records PLANT A and PLANT B, each with verbatim RED output, and the md5 of `slack_adapter.py` and `registry.py` before and after (identical)
    - `grep -rc PLANT backend/app/services/connectors/` prints `0` for every file
    - The non-vacuity floor in the test names a measured file count, produced by `ls backend/app/services/connectors/*.py | wc -l`
    - `grep -c "sendmail" backend/tests/unit/test_190_connector_source_fence.py` is `>= 1`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_190_connector_source_fence.py -q</automated>
  </verify>
  <done>The D-05 walk is non-vacuous and green, its matcher is proved to fire and not over-fire, every capability resolves, and both plants were observed RED and restored.</done>
</task>

<task type="auto">
  <name>Task 2: The SC#3 fence — prove no evaluator was added and composed fields render literally</name>
  <files>backend/tests/unit/test_190_ssti_fence.py</files>
  <read_first>
    - `backend/app/services/template_render_service.py:639-668` and `backend/app/services/tool_dispatcher.py:2469-2473` — the two shipped `SandboxedEnvironment(autoescape=True)` sites
    - `backend/app/services/harness/phase_types.py` — `_external_action_inputs` and the rewritten `_exec_external_action` (from plan 190-13)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-09, D-32
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` row T9
  </read_first>
  <action>
Create `backend/tests/unit/test_190_ssti_fence.py`. Its module docstring must open by saying
plainly what this file proves and what it does not: **190 added no evaluator. SC#3 is a fence
over an existing property, not a new mitigation.**

1. **`test_the_template_matcher_actually_matches`** — the positive control, first: prove the
   matcher used below fires on a haystack containing `Environment(` and does not fire on
   `SandboxedEnvironment(`.
2. **`test_no_unsandboxed_jinja_environment_exists_on_the_connector_path`** — walk
   `backend/app/services/connectors/**`, `backend/app/security/egress.py` and
   `backend/app/services/harness/phase_types.py` for a bare `Environment(`, `eval(`, `exec(`,
   `compile(` and `__import__`. Zero. **Plant:** swap a shipped `SandboxedEnvironment` for
   `Environment` in one of those files, observe RED, restore md5-identical.
3. **`test_a_composed_field_containing_a_template_expression_renders_LITERALLY`** — drive a send
   whose composed input contains `{{7*7}}` and `{{''.__class__}}`; assert the payload that
   reaches the adapter contains those characters verbatim and does NOT contain `49`. This is T9
   as behaviour rather than as source.
4. **`test_no_expression_language_was_added_to_the_phase_config`** — assert
   `ExternalActionPhaseConfig` gained exactly ONE new field in this phase (`connection_id`) and
   no field whose name suggests an expression, template or mapping surface. D-32's fence,
   asserted rather than remembered.
5. **`test_the_shipped_sandboxed_path_is_still_the_only_jinja_path`** — a source assertion over
   the two shipped sites, with a comment recording that these are the files SC#3 is proved
   against and that this test is a REGRESSION fence on someone else's code, not a claim about
   190's own.

⚠ **Say it in the summary too:** SC#3 was PROVED, not BUILT. The honest sentence is the point.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_190_ssti_fence.py -q` reports `0 failed` with at least 5 passing tests
    - The module docstring contains the literal sentence fragment `added no evaluator`
    - The summary records the `SandboxedEnvironment` → `Environment` plant with verbatim RED output and the md5 pair; `grep -c PLANT` over the touched file prints `0`
    - Test 3's assertion proves the literal `{{7*7}}` reaches the adapter payload and that `49` does not appear in it
    - `cd backend && python -c "
from app.models.harness import ExternalActionPhaseConfig as C
new=[f for f in C.model_fields if f in ('connection_id',)]
bad=[f for f in C.model_fields if any(k in f for k in ('expr','template','mapping','script'))]
print(new, bad)"` prints `['connection_id'] []`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_190_ssti_fence.py -q</automated>
  </verify>
  <done>SC#3 is proved by a fence over the shipped sandboxed path plus a behavioural literal-rendering drive, the `Environment` plant was observed RED, and the summary states the property was proved rather than built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter source → the socket layer | D-05: the fence is what keeps "one module owns one dangerous thing" true tomorrow |
| capability set → the registry | D-04: a fourth key is the cheapest way to widen this phase's surface silently |
| composed workflow text → a template engine | D-09/SC#3: the mitigation is that no evaluator exists on this path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-14-D05 | Tampering | `backend/app/services/connectors/**` | mitigate | Banned-token walk with a non-vacuity floor and a matcher positive control; PLANT A (`httpx.AsyncClient()` in `slack_adapter.py`) observed RED and restored md5-identical |
| T-190-14-D04 | Elevation of privilege | `registry.py` | mitigate | Module-scope `assert` on the key set, derived from `EXTERNAL_ACTION_CAPABILITIES`; PLANT B (a fourth key) observed firing AT IMPORT. Every capability is proved to resolve to an importable adapter |
| T-190-14-T9 | Tampering | composed fields | mitigate | Both a source fence (no bare `Environment(`, `eval(`, `exec(`, `compile(`, `__import__` on this path) and a behavioural drive (`{{7*7}}` reaches the adapter literally, `49` does not appear). Plant = swap `SandboxedEnvironment` for `Environment`, observed RED |
| T-190-14-D32 | Tampering | `ExternalActionPhaseConfig` | mitigate | Exactly one new field this phase, and no field whose name suggests an expression, template or mapping surface — asserted, not remembered |
| T-190-14-VAC | Repudiation | the fences themselves | mitigate | Every fence lists its positive control FIRST and asserts non-vacuity of its own walk, in the shipped `test_189_no_egress.py` idiom |
| T-190-SC | Tampering | npm/pip/cargo installs | mitigate | Test-only plan; zero installs. Acceptance: `git diff backend/requirements.txt` prints nothing |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/unit/test_190_connector_source_fence.py tests/unit/test_190_ssti_fence.py -q` — `0 failed`
- `grep -rc PLANT backend/app/services/connectors/ backend/app/security/` — `0` everywhere
- `cd backend && python -m pytest tests/ -q` — no new failures versus baseline
</verification>

<success_criteria>
- The D-05 walk is non-vacuous, its matcher proved, and a real planted client was observed RED
- The registry assert fires at import against a fourth key
- SC#3 is proved by both a source fence and a behavioural literal-rendering drive, and the summary says PROVED rather than BUILT
- Every plant restored md5-identical with `grep -c PLANT` → 0
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-14-SUMMARY.md` when done, recording the three plant cycles verbatim, the measured walk size, and the explicit SC#3 "proved, not built" sentence.
</output>
