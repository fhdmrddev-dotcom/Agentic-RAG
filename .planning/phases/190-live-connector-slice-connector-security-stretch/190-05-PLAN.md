---
phase: 190-live-connector-slice-connector-security-stretch
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/phaseVocabulary.ts
  - frontend/src/components/workflows/phaseVocabulary.test.ts
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-02]

must_haves:
  truths:
    - "D-24 — the `Not connected` badge retires by DATA, in exactly ONE line inside notConnectedOf, and PhaseNode.tsx / PhaseNodeCard.tsx / phaseNodeCardContract.ts / ownProperty.ts / NodeCornerMarks.tsx / NodeRunOverlay.tsx / NodeIconWell.tsx all end this plan with a MEASURED `git diff --numstat` of 0 0 (CONN-02)"
    - "D-23 — frontend/src/components/workflows/PhaseFormPanel.tsx has a MEASURED `git diff --numstat` of 0 0 after this plan"
    - "D-13 — the retiring test reads phase.config.connection_id, the reference field the definition JSONB carries; no secret, no host and no token is ever read by the canvas"
    - "phaseVocabulary.ts still has ZERO import statements after the edit, proved by a planted import observed RED and restored"
    - "The suite gains its FIRST genuine bound case — an external_action phase WITH a connection_id asserting the badge is ABSENT — alongside the shipped case asserting it is present without one"
  artifacts:
    - path: "frontend/src/components/workflows/phaseVocabulary.ts"
      provides: "notConnectedOf's state test, now falsifiable"
    - path: "frontend/src/components/workflows/phaseVocabulary.test.ts"
      provides: "the bound/unbound pair plus the zero-import property fence"
  key_links:
    - from: "frontend/src/components/workflows/phaseVocabulary.ts::notConnectedOf"
      to: "phase.config.connection_id"
      via: "a pure read of the parameter it already receives"
      pattern: "connection_id"
---

<objective>
Retire the canvas `Not connected` badge by DATA — one line inside `notConnectedOf` — and give
the previously-unreachable false branch its first real test.

Purpose: 189 spent the canvas's LAST free word-badge slot on this word and wrote
`notConnectedOf`'s type test and state test on **separate lines specifically so 190 could edit
only the second**. `PhaseNode.tsx`, `PhaseNodeCard.tsx` and the six fenced card-subtree modules
must all end this plan with an empty diff. **If a plan needs to open one of them, the plan is
wrong, not the code** (D-24, verbatim).

Output: one changed line, a bound/unbound test pair, a zero-import property fence, and the
measured empty-diff evidence for eight files.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md
</context>

<interfaces>
<!-- ⚠ THE LINE NUMBER IN CONTEXT (:811-814), IN <canonical_refs> (:788-814) AND IN THE
     PLANNING PROMPT (:788-814) ARE ALL WRONG. Measured at HEAD 2026-08-08:
       $ grep -n "notConnectedOf" .../phaseVocabulary.ts   → 810
       $ wc -l .../phaseVocabulary.ts                       → 813
     The function spans :810-813 and the line 190 edits is 812. RE-GREP BEFORE EDITING. -->

```ts
export function notConnectedOf(phase: PhaseSpecJSON): boolean {
  if (phase.config?.phase_type !== EXTERNAL_ACTION_PHASE_TYPE) return false
  return true                    // ← LINE 812 — the ONLY line 190 edits
}
```

`EXTERNAL_ACTION_PHASE_TYPE` is a **file-local `const`** at `phaseVocabulary.ts:500`, not an
import — which is why the zero-import property survives.

The zero-import property, measured holding at HEAD:
```
$ grep -nE "\bimport\b|\brequire\(|from ['\"]" frontend/src/components/workflows/phaseVocabulary.ts
509: * why it is EXPORTED from a module the picker can import rather than kept private.
679:  //     module has ZERO import statements today and this is not the change that should
```
Two hits, **both inside comments**. Zero real import statements.

The eight empty-diff fenced files (`190-UI-SPEC.md` §1b, PATTERNS §1):
`PhaseFormPanel.tsx` · `PhaseNode.tsx` · `PhaseNodeCard.tsx` · `phaseNodeCardContract.ts` ·
`ownProperty.ts` · `NodeCornerMarks.tsx` · `NodeRunOverlay.tsx` · `NodeIconWell.tsx`

The count-gate pin for this suite, measured at `scripts/vitest-count-gate.cjs:164`:
`"phaseVocabulary.test.ts": 112`. ⚠ The gate only fails on a DECREASE, so pins drift silently —
**run `node scripts/vitest-count-gate.cjs` and read the printed number; never quote a pin.**
Measured HEAD 2026-08-08: running **2727** / pinned **2714** / failed **0** / **48** files,
already drifted **+13**.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the bound/unbound test pair and the zero-import fence — RED first</name>
  <files>frontend/src/components/workflows/phaseVocabulary.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/phaseVocabulary.ts` lines 780-813 — the docblock that names its own successor and the function itself
    - `frontend/src/components/workflows/phaseVocabulary.test.ts` — the shipped `notConnectedOf` cases, so the new pair matches the file's existing idiom
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §7a, §7b, §7c
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md` §3.15
  </read_first>
  <action>
Add to `frontend/src/components/workflows/phaseVocabulary.test.ts`, inside the existing
`notConnectedOf` describe block:

1. `it("returns FALSE for an external_action phase that HAS a connection_id bound")` — the
   first genuine bound case in the suite's history. Build a `PhaseSpecJSON`-shaped fixture with
   `config.phase_type === "external_action"` and `config.connection_id` set to a uuid string;
   assert `notConnectedOf(phase) === false`.
2. `it("returns TRUE for an external_action phase with NO connection_id")` — keep or extend the
   shipped case so both polarities live side by side and neither can be deleted without the
   other looking odd.
3. `it("returns TRUE when connection_id is present but empty or whitespace")` — an empty string
   is not a destination. This pins the *shape* of the state test so a truthiness bug that
   accepts `""` fails here rather than on a canvas.
4. `it("still returns FALSE for every non-external_action phase type, bound or not")` — the
   TYPE half, re-asserted, so the one-line edit cannot accidentally widen the badge to another
   type (a `190-UI-SPEC.md` §14 failure condition, verbatim).
5. `it("phaseVocabulary.ts has ZERO import statements")` — read the module source with vitest's
   `?raw` idiom and assert no line outside a comment matches an import form. This is a
   PROPERTY fence and it needs a positive control: the plant in Task 3 drives it.

Run the suite now. Cases 1 and 3 MUST FAIL — the state test currently returns `true`
unconditionally. Record the failure output verbatim in the test file's own comment block under
`RED OBSERVED (plan 190-05)`.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts` reports at least 2 FAILING tests, and their names are the bound case and the empty-string case
    - `grep -c "connection_id" frontend/src/components/workflows/phaseVocabulary.test.ts` is `>= 4`
    - The test file contains the literal string `RED OBSERVED (plan 190-05)` with the verbatim failure output
    - `git diff --numstat frontend/src/components/workflows/phaseVocabulary.ts` prints nothing (production source is untouched by this task)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts; test $? -ne 0 && echo "RED as required"</automated>
  </verify>
  <done>Five cases exist covering both polarities, the empty-string shape, the type half and the zero-import property; two are RED and the failure text is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Edit exactly one line and drive the pair green</name>
  <files>frontend/src/components/workflows/phaseVocabulary.ts</files>
  <read_first>
    - `frontend/src/components/workflows/phaseVocabulary.ts` — re-grep `notConnectedOf` and re-run `wc -l` BEFORE editing; the numbers in CONTEXT and in the planning prompt are wrong and this file is the only authority
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §7a
    - `frontend/src/components/workflows/phaseVocabulary.test.ts` (just authored — it is the specification)
  </read_first>
  <action>
Re-derive the target with `grep -n "notConnectedOf" frontend/src/components/workflows/phaseVocabulary.ts`
and `wc -l` on the same file. The line to replace is the `return true` inside `notConnectedOf`
(measured **812** on 2026-08-08 — use YOUR measurement).

Replace that single line with the no-destination test: a pure read of `phase.config`, the
parameter the function already receives. It must treat an absent key, `null`, `undefined`, an
empty string and a whitespace-only string as *not connected*, and any non-empty
`connection_id` string as connected. Keep the type test on its own line, above, untouched.

Update the two-line docblock immediately above the function so it describes what is now TRUE
rather than what was planned — the successor it named has arrived. Keep the docblock's
statement that the type test and the state test are on separate lines on purpose; that is still
the reason the card subtree stays closed.

**Add no import.** The state test needs nothing that is not already a parameter. If you find
yourself reaching for a type or a constant from another module, stop — that is the change the
docblock at `:679` warns against, and the zero-import fence will catch it.

Do not touch `canvasModel.ts`, `PhaseNode.tsx` or anything in the card subtree. `notConnected`
is already resolved once in `canvasModel.buildPhaseData` and passed as DATA; nothing new has to
reach it.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts` reports `0 failed`
    - `git diff --numstat frontend/src/components/workflows/phaseVocabulary.ts` shows the code change confined to `notConnectedOf` plus its docblock — verified by `git diff -U0` showing no changed line outside the function and its immediately preceding comment
    - `grep -nE "^\s*(import|const .* = require\()" frontend/src/components/workflows/phaseVocabulary.ts` prints nothing
    - `git diff --numstat frontend/src/components/workflows/PhaseNode.tsx frontend/src/components/workflows/PhaseNodeCard.tsx frontend/src/components/workflows/canvasModel.ts` prints nothing
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` is at or below the measured baseline (33 on 2026-08-08 — RE-MEASURE at HEAD before the edit and compare against your own number)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts</automated>
  </verify>
  <done>One line changed, the pair is green, zero imports remain, the card subtree and `canvasModel.ts` are untouched, and `tsc` is at or below the re-measured baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Plant the zero-import violation, observe RED, restore — then prove eight empty diffs</name>
  <files>frontend/src/components/workflows/phaseVocabulary.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/phaseVocabulary.ts` (just edited)
    - `scripts/vitest-count-gate.cjs` — the pin block around `"phaseVocabulary.test.ts"` (measured at `:164`, value `112`) and the file's header rules about when a pin may move
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` § Source Fences (rows D-23, D-24, §M3)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-RESEARCH.md` Pitfall 4 (why a pin must never be quoted)
  </read_first>
  <action>
**A. The plant.** Insert `import type { Foo } from "./canvasModel"` at the top of
`phaseVocabulary.ts` — a REAL edit to production source, not a fixture. Run the zero-import
fence and observe it RED, recording the failure text. Restore the file **md5-identical** to its
post-Task-2 state (record the hash before and after; they must match) and confirm
`grep -c PLANT frontend/src/components/workflows/phaseVocabulary.ts` prints `0`.

**B. Move the count-gate pin IN THIS COMMIT.** Run `node scripts/vitest-count-gate.cjs` and
read the printed running/pinned numbers — do not quote any number from any document. Update
`"phaseVocabulary.test.ts"`'s pin to the count this suite now runs, and add a dated comment in
the file's existing convention naming plan 190-05 and the delta. Re-run the gate and confirm it
reports OK with no per-file decrease and zero failures.

**C. Prove the eight empty diffs, now.** Run
`git diff --numstat HEAD -- frontend/src/components/workflows/PhaseFormPanel.tsx
frontend/src/components/workflows/PhaseNode.tsx frontend/src/components/workflows/PhaseNodeCard.tsx
frontend/src/components/workflows/phaseNodeCardContract.ts frontend/src/components/workflows/ownProperty.ts
frontend/src/components/workflows/NodeCornerMarks.tsx frontend/src/components/workflows/NodeRunOverlay.tsx
frontend/src/components/workflows/NodeIconWell.tsx`
and record its output verbatim. It must print NOTHING. This is a per-plan checkpoint; the
phase-wide fence is re-run in plan 190-19.
  </action>
  <acceptance_criteria>
    - The summary records the planted-import RED observation verbatim, plus the pre-plant and post-restore md5 of `phaseVocabulary.ts` (identical)
    - `grep -c PLANT frontend/src/components/workflows/phaseVocabulary.ts` prints `0`
    - `node scripts/vitest-count-gate.cjs` exits 0 and prints `count gate OK`; the printed running and pinned totals are recorded in the summary
    - `git diff --numstat HEAD -- <the eight fenced files>` prints NOTHING, and that empty output is recorded verbatim
    - `cd frontend && npm test` reports `0 failed`
    - `git diff -U0 scripts/vitest-count-gate.cjs` shows only the `phaseVocabulary.test.ts` pin and its dated comment changing
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test && cd .. && node scripts/vitest-count-gate.cjs && git diff --numstat HEAD -- frontend/src/components/workflows/PhaseFormPanel.tsx frontend/src/components/workflows/PhaseNode.tsx frontend/src/components/workflows/PhaseNodeCard.tsx frontend/src/components/workflows/phaseNodeCardContract.ts frontend/src/components/workflows/ownProperty.ts frontend/src/components/workflows/NodeCornerMarks.tsx frontend/src/components/workflows/NodeRunOverlay.tsx frontend/src/components/workflows/NodeIconWell.tsx</automated>
  </verify>
  <done>The zero-import fence is proved to bite, the plant is restored md5-identical, the count-gate pin moved in the same commit that changed the count, and the eight fenced files have a measured empty diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| definition JSONB → canvas render | The canvas may read the connection REFERENCE and nothing else; a host or a token must never reach a rendered string |
| one-line data edit → the fenced card subtree | The whole 188.2 extraction and 189-15's slot spend exist to keep this boundary closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-05-T6 | Information disclosure | `notConnectedOf` | mitigate | The state test reads ONLY `phase.config.connection_id`; a test asserts no host/token/secret key is read, and the canvas renders no new string at all (UI-SPEC §7b: the bound state is ABSENT and nothing replaces it) |
| T-190-05-FENCE | Tampering | the eight fenced card-subtree files | mitigate | `git diff --numstat HEAD -- <eight files>` asserted EMPTY as an automated verify, not as a promise |
| T-190-05-IMPORT | Tampering | `phaseVocabulary.ts` purity | mitigate | Zero-import fence with a PLANTED import observed RED and restored md5-identical, `grep -c PLANT` → 0 |
| T-190-05-BADGE | Spoofing | canvas badge slots | mitigate | No badge is added: the badge maximum is 2 and a third is a typecheck error, mechanically guarded since 188.2-01. `tsc -p tsconfig.app.json` asserted at or below the re-measured baseline |
| T-190-05-PIN | Repudiation | `scripts/vitest-count-gate.cjs` | mitigate | The pin moves in the SAME commit as the count, and the gate is RUN rather than quoted (Pitfall 4: the gate only fails on a decrease, so drift is silent) |
| T-190-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs. Acceptance: `git diff frontend/package.json frontend/package-lock.json` prints nothing |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts` — `0 failed`
- `cd frontend && npm test` — `0 failed`
- `node scripts/vitest-count-gate.cjs` — `count gate OK`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` — at or below the re-measured baseline
- `git diff --numstat HEAD -- <the eight fenced files>` — empty
</verification>

<success_criteria>
- Exactly one production line changed inside `notConnectedOf`, plus its docblock
- The suite carries its first genuine bound case and both polarities
- `phaseVocabulary.ts` keeps zero imports, proved by a planted violation observed RED
- The eight fenced files have a measured `0 0` diff, and the count-gate pin moved with the count
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-05-SUMMARY.md` when done, recording the re-derived line number, the RED observations, the md5 pair, the gate's printed totals and the verbatim empty numstat output.
</output>
