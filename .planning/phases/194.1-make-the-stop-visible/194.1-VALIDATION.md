---
phase: 194.1
slug: make-the-stop-visible
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-16
---

# Phase 194.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> **Authority for the fence set:** `194.1-RESEARCH.md` § *Validation Architecture* — the
> requirement → signal → sampling-point → **negative control** table. It is NOT duplicated here;
> this file carries the infrastructure contract, the Wave 0 list, the manual rows and the sign-off.

---

## The standing lesson this file exists to obey

**A fence that cannot fire is worse than no fence.** Phases 190, 192.1 and 193.1 each registered
fences later found to be sweeping the empty string, asserting the absence of a *field* where a *set
member* had been added, or scoped to a renamed module. In 193.1 **four** such fences were caught —
every one of them by *planting*, none by reading.

⇒ **Every fence in this phase carries a named plant, and the plant must be observed RED before the
fix lands.** A plan that reports a green fence without having seen it red has not validated anything.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3 + jsdom + @testing-library/react (`frontend/vite.config.ts`, setup `./src/setupTests.ts`); pytest under `backend/venv` |
| **Config file** | `frontend/vite.config.ts` · `backend/pytest.ini` |
| **Quick run command** | `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run <file>` |
| **Full suite command** | `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` |
| **Typecheck** | `npx tsc --noEmit -p tsconfig.app.json` — ⚠ the bare `--noEmit` form checks **zero** files |
| **Backend** | `cd backend && venv/Scripts/python -m pytest tests/unit -q` |
| **Estimated runtime** | quick ~5–20 s · full suite ~3–5 min |

⚠ **`GSD_VITEST_MAX_WORKERS=2` is mandatory on every run** (CLAUDE.md — the `4` has rotted twice with
suite size). Read the count gate's **verdict line**, never a summary. Its contract is *no per-file
DECREASE* and *zero failing* — a grand total larger than any figure quoted in CLAUDE.md is the gate
working, not breaking.

### ⚠ Two count-gate pins this phase must bump IN THE SAME COMMIT

`scripts/vitest-count-gate.cjs`'s `TARGETS` does **not** include `src/components/chat` — so
`MessageList.test.tsx`, `MessageInput`, `ActiveRunsTray.test.tsx`, `ComposerStopHarness.test.tsx` and
the provider suites are **ungated**. Two files this phase touches **are** pinned:

| Pinned file | Current pin | Owed by |
|---|---|---|
| `src/pages/WorkflowRunPage.test.tsx` | **89** | the R3 plan |
| `src/components/panel/__tests__/WorkspacePanel.test.tsx` | **41** | the R7a plan |

An extension bumps the pin in the same commit (a *lowering* additionally needs the deletion in the
same commit — gate rule at `:24-45`). **Two concurrent plans editing this one object literal is a
merge conflict with no `files_modified` signal** — the two plans that own these pins must SERIALIZE
against each other (CLAUDE.md worktree rule; see RESEARCH.md Q8).

---

## Sampling Rate

- **After every task commit:** the quick run for the file(s) that task touched
- **After every plan wave:** the full count gate
- **Before `/gsd:verify-work`:** count gate `OK` · `failed 0` · no per-file DECREASE
- **Max feedback latency:** ~20 s per task, ~5 min per wave

---

## Per-Task Verification Map

*Filled by `gsd-planner` (2026-08-16) with the task set, the requirement, the command and **the plant
each task must drive RED**. The "Negative control observed RED" column carries the plant's ID here and
is updated to `✔ <plant> — <case that fired>` by the executing plan's SUMMARY. Plan 08 task 2
reconciles the whole column from the SUMMARYs.*

⚠ **A row whose negative-control cell is blank is not a validated task.** If a plant genuinely cannot
be observed RED, write `NOT OBSERVED` with the reason — blank is not an option, and a green fence
reported without ever having been seen red has validated nothing.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | Negative control observed RED | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------------------------|--------|
| 01-T1 | 01 | 1 | RUN-01 | T-194.1-01-01 | Every figure this phase quotes is re-derivable from a recorded command; the base SHA is asserted mechanically | source/CLI | `test "$(git rev-parse HEAD)" = f9e55b6d…` | *(baseline task — its control is that the tree is provably unmoved, 01-T5)* | ⬜ pending |
| 01-T2 | 01 | 1 | RUN-01 | T-194.1-01-02 | The three shipped Stop mounts and the run surface's absence are pinned BEFORE any of them moves | component + source fence | `npx vitest run src/components/chat/__tests__/StopControl.baseline.test.tsx` | swept-length assertion on the `WorkflowRunPage` source fence — a renamed file must red, not pass green | ⬜ pending |
| 01-T3 | 01 | 1 | RUN-01 | T-194.1-01-02 | Both resolvers, the kickoff placeholder and the two honesty arms are pinned before they move | provider integration | `npx vitest run src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts` | cases 6/7 must be shown FIREABLE at base, or recorded un-fireable in BASELINE §8 | ⬜ pending |
| 01-T4 | 01 | 1 | RUN-01 | T-194.1-01-02 | The transcript's silence and the approval's dispatchability are pinned; the three shipped retirement literals are read, not re-typed | component + source fence | `npx vitest run src/__tests__/components/chat/MessageList.runline.baseline.test.tsx src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx` | source fence prints swept length and asserts non-zero | ⬜ pending |
| 01-T5 | 01 | 1 | RUN-01 | T-194.1-01-02 | "The tree was unmoved" is EVIDENCED by a verbatim numstat, never asserted | CLI | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` | a numstat naming any source path fails the task by its own contract | ⬜ pending |
| 02-T1 | 02 | 2 | RUN-01 | T-194.1-02-05 | Three additive-OPTIONAL fields; zero deletions; a Deep thread reads three `None`s | unit (pydantic probe) | `venv/Scripts/python -c "…model_fields…"` | *(structural — 0 deleted lines IS the control; a modification produces one)* | ⬜ pending |
| 02-T2 | 02 | 2 | RUN-01 | T-194.1-02-05 | Both arms populate from the ALREADY-resolved id; the query count does not move | unit + CLI | `venv/Scripts/python -m pytest tests/unit -q` | `_rls_fetchrow(`/`_rls_fetch(` count identical before and after | ⬜ pending |
| 02-T3 | 02 | 2 | RUN-01 | **T-194.1-02-01**, T-194.1-02-02 | Foreign thread → **404 on body and status**; `HTTP_403` count 0; the three fields survive the anchor NULL | unit (mocked pool) | `venv/Scripts/python -m pytest tests/unit/test_194_1_thread_workflow_last_run.py -q` | **P1** drop ARM B's `status` · **P2** key off `active_workflow_run_id` · **P3** swap `run_status`/`last_run_status` | ⬜ pending |
| 02-T4 | 02 | 2 | RUN-01 | T-194.1-02-04 | Client type mirrors the three fields; the seven-literal vocabulary is stated where six were listed | typecheck | `npx tsc --noEmit -p tsconfig.app.json` | *(docstring-only; the control is the confined numstat on `workflow_runs.py`)* | ⬜ pending |
| 03-T1 | 03 | 2 | RUN-01 | T-194.1-03-03 | Three pure-data Sets; **no timer handle in the store**; three primitive-returning selectors | source fence + typecheck | `npx tsc --noEmit -p tsconfig.app.json` | `grep -c "setTimeout\|setInterval" streamsStore.ts` == 0 | ⬜ pending |
| 03-T2 | 03 | 2 | RUN-01 | T-194.1-03-03, **T-194.1-03-05** | Press is synchronous; climb-down at exactly 8s; the clear covers ALL FOUR terminal routes; the terminal reading is never suppressed | provider integration (fake timers) | `npx vitest run src/__tests__/providers/StreamsProvider.stopping.test.ts` | **P1** 3000 ms timeout → t+7000 case · **P2** no `clearTimeout` → 2s case · **P3** clear on `onTerminal` only → reconcile-derive case · **P4** suppress the terminal reading → L-01 boundary case | ⬜ pending |
| 03-T3 | 03 | 2 | RUN-01 | **T-194.1-03-01**, T-194.1-03-02 | The id resolves without the chat bucket, on BOTH resolvers; no console line claims an unestablished cause | provider integration + source fence | `npx vitest run src/__tests__/providers/StreamsProvider.stopping.test.ts src/components/chat/__tests__/ComposerStopHarness.test.tsx` | **P5** `toHaveBeenCalled()` alone passes a wrong id · **P6** reorder the resolvers · **P7** restore the string in `stopStream` ONLY | ⬜ pending |
| 03-T4 | 03 | 2 | RUN-01 | T-194.1-03-04 | Zero assistant nodes at harness kickoff; Deep byte-identical; liveness marked synchronously | provider integration | `npx vitest run src/__tests__/providers/StreamsProvider.stopping.test.ts` | **P8** hide with CSS instead of skipping the insert · **P9** gate on `agentMode` instead of `workflowDefinitionId` | ⬜ pending |
| 03-T5 | 03 | 2 | RUN-01 | **T-194.1-03-04** | The administrator refusal and the network failure both still reach the screen with no placeholder | provider integration | `npx vitest run src/__tests__/providers/StreamsProvider.stopping.test.ts src/__tests__/providers/streamsProvider_state01b_403.test.tsx` | **P10** ship the gate WITHOUT the repair — both arms must red. The single most important RED in the plan | ⬜ pending |
| 03-T6 | 03 | 2 | RUN-01 | T-194.1-03-03 | `useState(` unmoved on the provider; the ONE `useEffect(` increment is declared, not drifted | CLI | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` | D-03 counts published before/after; a `useState(` increment voids D-02 | ⬜ pending |
| 04-T1 | 04 | 3 | RUN-01 | **T-194.1-04-01**, T-194.1-04-02 | While stopping the control is ABSENT from the DOM; the slot's sizing tokens are identical across arms | component | `npx vitest run src/components/chat/__tests__/StopControl.test.tsx` | **P1** render control + reading together — a presence-only assertion passes this · **P2** `h-6 w-auto` on the stopping wrapper · **P3** plant `cancelRun(` | ⬜ pending |
| 04-T2 | 04 | 3 | RUN-01 | **T-194.1-04-04** | R2's three timing points; two instances for one thread move together | component (fake timers) | `npx vitest run src/components/chat/__tests__/StopControl.test.tsx` | **P4** 3000 ms timeout · **P5** `useState` inside `StopControl` — only the two-instance case sees it · **P6** no `clearTimeout` | ⬜ pending |
| 04-T3 | 04 | 3 | RUN-01 | T-194.1-04-02, T-194.1-04-04 | The composer owns zero new state; `onStop` retired; `stopStream` survives with its deferral recorded | source fence + typecheck | `npx vitest run src/components/chat/__tests__/ src/__tests__/components/chat/ && npx tsc --noEmit -p tsconfig.app.json` | `useState(`/`useEffect(` must be UNMOVED on `MessageInput`/`ChatArea` — movement voids D-02 | ⬜ pending |
| 04-T4 | 04 | 3 | RUN-01 | — | The gate is green and the pin map is untouched | CLI | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` | `git diff scripts/vitest-count-gate.cjs` must be EMPTY | ⬜ pending |
| 05-T1 | 05 | 4 | RUN-01 | **T-194.1-05-01**, **T-194.1-05-02** | No Stop on a completed run **AND** the timeline still renders | component | `npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx` | **P1** narrow `showTimeline` itself — the no-Stop case alone passes it · **P2** gate on `phases.length > 0` | ⬜ pending |
| 05-T2 | 05 | 4 | RUN-01 | T-194.1-05-01 | Panel and tray own zero new state; `Stop all` byte-untouched | source fence | `npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx src/components/chat/__tests__/ActiveRunsTray.test.tsx src/components/chat/__tests__/StopControl.baseline.test.tsx` | comment-stripped `workflowLock\|cancelRun` count == 0 on both; `useState(` unmoved | ⬜ pending |
| 05-T3 | 05 | 4 | RUN-01 | **T-194.1-05-03**, **T-194.1-05-04**, T-194.1-05-05 | The card is still in the DOM, visibly retired, non-dispatchable, and says something the two shipped expiries do not | component | `npx vitest run src/components/panel/__tests__/PendingAskCard.test.tsx src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx` | **P3** remove the card · **P4** retire but leave `disabled={false}` · **P5** reuse `:280`'s string — all three pairs asserted | ⬜ pending |
| 05-T4 | 05 | 4 | RUN-01 | **T-194.1-05-01** | One press on the panel removes the composer's control for the same thread | component (two real mounts) | `npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx` | **P6** `useState` inside `MessageInput` — only the cross-mount case sees it; every single-mount case stays green | ⬜ pending |
| 05-T5 | 05 | 4 | RUN-01 | — | The `WorkspacePanel.test.tsx` pin is bumped in the SAME commit as its cases; the sibling entry untouched | CLI | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` | figure taken from the gate's `actual` on two agreeing runs, never a hand count | ⬜ pending |
| 06-T1 | 06 | 4 | RUN-01 | T-194.1-06-03 | The step count is TOTAL over seven literals parsed from migration 119; `0 of 0` refused; one formatter, not four | unit + source fence | `npx vitest run src/lib/__tests__/runStepCount.test.ts src/pages/WorkflowRunPage.test.tsx` | **P1** `switch` missing `cancelled` · **P2** return `{0,0}` instead of `null` · **P3** one character inside the hoisted body | ⬜ pending |
| 06-T2 | 06 | 4 | RUN-01 | **T-194.1-06-02**, **T-194.1-06-03**, T-194.1-06-05 | Two states, one component; elapsed anchor disclosed; silent on completed and failed; seam fails closed | component | `npx vitest run src/__tests__/components/chat/MessageList.test.tsx` | **P4** derive from a live-store read · **P5** render on any terminal · **P6** `phases: []` must omit the segment, never print `0 of 0` · **P7** mount the live arm on first delta | ⬜ pending |
| 06-T3 | 06 | 4 | RUN-01 | T-194.1-06-04 | List-level mount, sibling of the shipped chip (D-17); neither mount gained state | source fence + typecheck | `npx vitest run src/__tests__/components/chat/ && npx tsc --noEmit -p tsconfig.app.json` | `git diff -U0` must show no changed line inside the `showJumpToLive` block | ⬜ pending |
| 06-T4 | 06 | 4 | RUN-01 | **T-194.1-06-04** | The stopped receipt renders with the streaming store STRUCTURALLY empty; kickoff carries an instrument with no dead frame | component + provider integration | `npx vitest run src/__tests__/components/chat/ src/__tests__/providers/StreamsProvider.stopping.test.ts` | **P8** live-store derivation · **P9** render on any terminal · **P10** mount on first delta, asserted WITHOUT advancing timers | ⬜ pending |
| 06-T5 | 06 | 4 | RUN-01 | — | Gate green; the pin map left untouched for plan 07 | CLI | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` | `git diff scripts/vitest-count-gate.cjs` EMPTY; no DECREASE on `WorkflowRunPage.test.tsx` | ⬜ pending |
| 07-T1 | 07 | 5 | RUN-01 | **T-194.1-07-01**, T-194.1-07-04 | The Stop is offered while the run is going and gone once it is over; the page owns zero new state | component | `npx vitest run src/pages/WorkflowRunPage.test.tsx` | **P1** gate on `runStatus !== "completed"` — a `completed`-only test does NOT catch it · **P2** remove the gate | ⬜ pending |
| 07-T2 | 07 | 5 | RUN-01 | T-194.1-07-01, T-194.1-07-03 | Five shipped band states driven; ONE cancel by VALUE; direct flip with no confirm step | component | `npx vitest run src/pages/WorkflowRunPage.test.tsx` | **P3** add a confirmation step → the no-guard case reds | ⬜ pending |
| 07-T3 | 07 | 5 | RUN-01 | **T-194.1-07-02** | The page never reaches past the one durable cancel path | source fence | `npx vitest run src/pages/WorkflowRunPage.test.tsx` | **F1** plant `cancelRun(` in production source · **F2** plant `workflowLock.runId`; both fences print + assert a non-zero swept length | ⬜ pending |
| 07-T4 | 07 | 5 | RUN-01 | — | The `WorkflowRunPage.test.tsx` pin is bumped in the SAME commit as its cases; plan 05's entry untouched | CLI | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` | map RE-READ at HEAD before writing; figure from the gate's `actual` on two agreeing runs | ⬜ pending |
| 08-T1 | 08 | 6 | RUN-01 | **T-194.1-08-01**, **T-194.1-08-02** | Every hot file this phase touched is VISIBLE to the G-5 audit; the stale row is corrected beside its old value | source fence (CLI) | `for f in …; do grep -c "$f" CLAUDE.md; done && grep -q "5+" CLAUDE.md` | a row present-and-WRONG answers the auditor `satisfied` and stops the audit — the `5+` must survive beside the correction | ⬜ pending |
| 08-T2 | 08 | 6 | RUN-01 | **T-194.1-08-03** | No green fence is claimed that was never seen red | doc reconciliation | `! grep -q "planner fills" 194.1-VALIDATION.md` | every plant id from plans 02-07 present in this column, sourced from a SUMMARY; `NOT OBSERVED` permitted, blank is not | ⬜ pending |
| 08-T3 | 08 | 6 | RUN-01 | **T-194.1-08-04** | Every deferral carries a re-open trigger; the four folds are verified in FRONTMATTER | CLI | `grep -l 'folded_into: "194.1"' .planning/reported-bugs/*.md \| wc -l` == 4 | a prose claim the frontmatter does not record is worse than no claim — `BUG-260610-01` stayed invisible two months on exactly that | ⬜ pending |
| 08-T4 | 08 | 6 | RUN-01 | **T-194.1-08-05** | RUN-01 stays UNTICKED with its reason; the whole tree is green; deploy-artifact parity checked, not asserted | CLI | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` · `bash scripts/check-deploy-drift.sh` | `REQUIREMENTS.md` numstat must be EMPTY — the tick is the operator's | ⬜ pending |
| 08-T5 | 08 | 6 | RUN-01 | **T-194.1-08-05** | Lived experience judged by the operator, with the L-01 behaviour named up front so it is not mis-filed as a defect | **manual — G-4, blocking** | *(human-check: Press · Canvas · Return)* | ⛔ rows recorded with a reason, never silently omitted; row 2 requires `visual_workflow_canvas` **ON** | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Coverage check.** 38 task rows across 8 plans. Sampling continuity holds — **no 3 consecutive tasks
without an automated verify**. The only non-automated row is `08-T5`, the terminal G-4 checkpoint,
which is manual by definition (CLAUDE.md **G-4**: wire format and screenshots are *insufficient*).

---

## Wave 0 Requirements

Baselines must be captured on the **UNMOVED** tree, in commits touching no source file — a
characterization baseline only proves something if it **predates** the change (the 188.1 lesson,
re-proved in 193 and 193.1).

- [ ] `frontend/src/components/chat/__tests__/StopControl.test.tsx` — R1: the four mounts, the
      **cross-mount** case (D-06's only proof), the slot-token reservation fence
- [ ] `frontend/src/__tests__/providers/StreamsProvider.stopping.test.ts` — R2 (three timing points +
      **all four** terminal clear routes), R6 (both resolvers), R5's three arms, the **L-01 boundary**
- [ ] extend `frontend/src/__tests__/components/chat/MessageList.test.tsx` — R4: store-EMPTY render,
      absent-on-`completed`, absent-on-`failed`
- [ ] a pure unit file for the step-count derivation — R4 totality over the seven `workflow_phases`
      status literals, the expected set **derived** from migrations 119 + 115, never hand-typed
- [ ] extend `WorkflowRunPage.test.tsx` (**pin 89**) — R3, five status cases not two
- [ ] extend `WorkspacePanel.test.tsx` (**pin 41**) — R7a, *two* assertions: no Stop **and** timeline
      still renders
- [ ] extend `PendingAskCard.test.tsx` — R7b, **both** plants (removed-card; retired-but-dispatchable)
- [ ] backend: one owner-scope test per widened `ThreadWorkflowState` field, mocked pool — the
      `backend/tests/test_062_cancel_run.py` shape

---

## Manual-Only Verifications

### The three operator-driven G-4 rows

Per CLAUDE.md **G-4**, these are operator-defined at scope time and driven by the operator at
verification. Wire format and screenshots are **insufficient**.

| # | Row | Drive | Named failure shape | Requirement |
|---|---|---|---|---|
| 1 | **Press** | Stop in the composer on a live run | *fails if the button sits inert or the row twitches* | R1, R2 |
| 2 | **Canvas** | launch via `▶ Run workflow`, stop from the run surface **without navigating** | *fails if there is no control, or the control persists after the run is terminal* | R3 |
| 3 | **Return** | stop, navigate away, come back | *fails if the thread ends at the original prompt* | R4 |

⚠ **Row 2 requires `visual_workflow_canvas` ON.** It ships **off** (`models/user_settings.py:1197-1203`)
— confirm the local flag before driving, or the row measures the gate rather than the feature.

### The cross-provider axis — a stated decision, not a silent omission

Per CLAUDE.md's UAT scoreboard recipe, a phase touching streaming or UI state owes the 4-axis
bandwidth. **This phase does not re-drive the 8-row native roster, and that is a decision:** it adds
**no provider-facing behaviour** — no prompt, no tool call, no emission path, no model routing — and
the durable half it renders was already scored on **seven live runs** in Phase 194. The parallel-thread
and multi-tool axes are likewise unmoved.
**Re-open trigger:** any plan that ends up touching a provider service, `MODEL_CAPABILITIES`, or the
emission path owes the full roster before this phase closes.

### The L-01 boundary — owed as a STATEMENT, never as a fix

At the shipped `WORKER_COUNT=2`, roughly **half of all stops end with the run reporting `completed`**
(Phase 194 SC#2, FAILED; `RUN_TASKS` is per-process and `finish_run` carries no terminal guard,
`db/workflows.py:1458-1463`). **This phase's honest UI makes that lie MORE visible, not less** — the
surface will truthfully show `⊘ Stopping this run…` and then land on `✓ Complete`.

⚠ **That is not a defect in 194.1's work, and NO plan may "fix" it by suppressing the terminal
reading.** A fence exists in the fence set specifically to red against that well-meaning suppression.
**RUN-01 stays UNTICKED until the inserted L-01 phase ships.**

---

## Validation Sign-Off

- [ ] All tasks have an `<automated>` verify or a Wave 0 dependency
- [ ] **Every fence has a named plant, and every plant was observed RED before its fix landed**
- [ ] Every source fence prints its swept file-list length and asserts it is non-empty *(the 192.1
      lesson: a renamed module was swept against the empty string and passed green)*
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Both count-gate pins bumped in the commit that adds their cases
- [ ] The three G-4 rows driven by the operator, each recorded with its outcome — ⛔ rows recorded
      with a reason, **never silently omitted**
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
