---
phase: 194.1-make-the-stop-visible
plan: 07
type: execute
wave: 5
depends_on: ["194.1-04", "194.1-05", "194.1-06"]
files_modified:
  - frontend/src/pages/WorkflowRunPage.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "The surface that ▶ Run workflow lands on can stop the run it is showing, without navigating anywhere"
    - "That control is offered while the run is going and is gone once it is over"
    - "Pressing it issues exactly one cancel through the same path every other Stop uses, with no confirmation step"
  artifacts:
    - path: "frontend/src/pages/WorkflowRunPage.tsx"
      provides: "A Stop in the title row opposite the seam link, gated on the isTerminal already in scope"
      contains: "StopControl"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "The WorkflowRunPage.test.tsx pin, bumped in the same commit as its new cases"
  key_links:
    - from: "frontend/src/pages/WorkflowRunPage.tsx"
      to: "isTerminal"
      via: "the liveness gate is one clause reading a const already computed at :592"
      pattern: "isTerminal"
    - from: "frontend/src/pages/WorkflowRunPage.tsx"
      to: "StopControl"
      via: "the same shared mechanism the other three mounts use — never cancelRun directly"
      pattern: "<StopControl"
---

<objective>
Put a Stop in `WorkflowRunPage.tsx`'s title row.

Purpose: `▶ Run workflow` lands the user **here**, and measured at HEAD
`grep -c "onStop|stopThread|cancelRun|Stop" frontend/src/pages/WorkflowRunPage.tsx` returns **0** —
the natural launch path has no working Stop at all. The operator hit this in 194 UAT and it is half of
`BUG-260816-01`.

Sketch **169-A** settles the placement (*the title row, opposite the seam link*) over the sketch's own
lean toward B, and settles the guard: **direct flip — no sheet, no arm-to-confirm.** The shipped
action-risk ladder is *irreversible + names a victim → sheet; consequential but reversible →
arm-to-confirm; reversible with no victim → direct flip.* A Stop is **your own run, which you launched
and are watching**; Phase 194 verified on seven live runs that completed phases survive and only the
interrupted one is marked. The asymmetry, stated once: *a Stop that is too easy costs you one run; a
Stop that is too hard costs you the reason the control exists.*

Output: one mount, one gate clause, five status cases, two source fences, one pin bump.

⚠ **SERIALIZATION — this plan must land AFTER plan 05.** Both edit `scripts/vitest-count-gate.cjs`'s
`BASELINE` object literal. Two concurrent edits to one object literal is a merge conflict with **no
`files_modified` signal**, which is why they sit in different waves. **RE-READ the map before writing**
— plan 05 has already changed the `WorkspacePanel.test.tsx` entry beside the one this plan owns.

⚠ **This file is also touched by plan 06** (the verbatim `fmtElapsed` hoist). Confirm that landed
before starting; if it has not, the import this plan reads will not be there.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194.1-make-the-stop-visible/194.1-SPEC.md
@.planning/phases/194.1-make-the-stop-visible/194.1-CONTEXT.md
@.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md
@.planning/sketches/169-a-stop-on-the-runs-own-surface/README.md
@CLAUDE.md

<interfaces>
<!-- Verbatim at base f9e55b6d. Everything R3 needs is ALREADY in scope in this component. -->

`frontend/src/pages/WorkflowRunPage.tsx`
  :74    `TERMINAL_RUN_STATUSES` — imported
  :83    `useStreamActions` — ALREADY imported, and already used at :497 (`reconcile`)
  :109   `const COPY_OPEN_THREAD = "Open the chat thread"`
  :592   `const isTerminal = TERMINAL_RUN_STATUSES.has(runStatus)`  ← already computed, component-level,
         hoisted above the memo below it on purpose (F5) so downstream blocks read it rather than
         re-derive it. **R3 needs no new derivation.**
  :872-883  THE MOUNT POINT — the title row:
         <div className="flex items-center gap-3">
           <h1 …>{run?.workflow_name || "Workflow"}</h1>
           <span className="font-mono text-xs …">v{run?.workflow_version}</span>
           <button type="button" onClick={() => run && onOpenThread(run.thread_id)}
             className="ml-auto text-xs font-medium text-primary …">{COPY_OPEN_THREAD}</button>
         </div>
         ⚠ `COPY_OPEN_THREAD` carries `ml-auto` — the Stop goes BESIDE it, inside that same flex row.
  :885-895  the state row below: `{band.sentence}` + the anchored clock (`data-testid="run-elapsed"`)
         and, under the ⌥ reveal, `data-testid="run-elapsed-technical"`.
         ⚠ **Sketch 169 rejected B (the state row) partly on this**: that row is `flex-wrap` and grows
         a long `claimed_at … → created_at … → updated_at …` string under the reveal, so a control
         there wraps unpredictably. The title row does not have that hazard.
  :683-731  the polling concern for this surface — already owned here; R3 adds nothing to it.

  The shipped band sentences (`:279-302`), which are what the five status cases must drive:
    `● Running` · `Paused for your answer` · `✓ Complete` · `⊘ Cancelled` · `✕ Failed at "…"`

`frontend/src/components/chat/StopControl.tsx` (plan 04) — `variant="page"`, `threadId`, optional
  `label` / `ariaLabel` / `testId`. It dispatches `stopThread(threadId)` itself; the page passes no
  handler and writes no dispatch.

⚠ **Header docblock, quoted because R3 spends against it:** the header is *"orientation, never a focal
point. The only accent it spends is the seam link."* R3 asks it to spend a second accent, and places a
**primary-tinted link** next to a **destructive control**. That was weighed and accepted at sketch
time (169-A over the sketch's own lean to B). Record it in the mount's comment rather than letting a
later reader think nobody noticed.

⚠ **This page's own G-5 status: `10 commits / 2 phases / 1046 L` — G-5 does NOT fire (2 < 3).** It is
nevertheless owed a ledger row by **D-04**, and plan 08 writes it. This plan does not touch `CLAUDE.md`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount the shared control in the title row, gated on the const already in scope</name>
  <files>frontend/src/pages/WorkflowRunPage.tsx, frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowRunPage.tsx` lines 70-115 (imports and `COPY_OPEN_THREAD`), 580-600 (`isTerminal`), 855-900 (the header)
    - `frontend/src/components/chat/StopControl.tsx` (plan 04's component and its `variant="page"` defaults)
    - `.planning/sketches/169-a-stop-on-the-runs-own-surface/README.md` (winner A, the guard recommendation, the PROVENANCE table)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §3 and §4
  </read_first>
  <behavior>
    - `● Running` → the control renders
    - `Paused for your answer` → the control renders
    - `✓ Complete` / `⊘ Cancelled` / `✕ Failed` → the control is absent
    - Pressing it issues exactly ONE cancel, and the stopping reading takes its slot
    - No confirmation sheet, no arm-to-confirm, no second click required
  </behavior>
  <action>
FIRST ACTION IN THE WORKTREE: `bash scripts/bootstrap-worktree.sh "$(pwd)"`. Assert the dispatched
base SHA and STOP if it does not match. Then confirm plans 04, 05 and 06 have landed on this branch —
`StopControl.tsx` must exist and `WorkflowRunPage.tsx` must already import `fmtElapsed` from `lib/`.

Mount `<StopControl threadId={run?.thread_id ?? null} variant="page" />` inside the title row's flex
container at `:872-883`, **beside** the `COPY_OPEN_THREAD` button and inside the same row, gated on
`!isTerminal` — **D-19 exactly: read the `isTerminal` const already computed at `:592`, mount opposite
`COPY_OPEN_THREAD` (`:109`), direct flip, and keep `grep -c "cancelRun(" WorkflowRunPage.tsx` at 0** — one clause reading the const already computed at `:592`. Do not re-derive terminality;
do not add a second `TERMINAL_RUN_STATUSES` read.

⚠ **`COPY_OPEN_THREAD` carries `ml-auto`.** Place the Stop so the seam link keeps its right-edge
position and the row does not reflow when the control appears and disappears — the arrangement is at
your discretion, but the property is not: a control that shifts the seam link on every state change is
the "row twitches" failure G-4 row 2 will judge.

Write ONE comment above the mount recording, in order and no more:
 - **sketch 169-A**, chosen over the sketch's own lean toward B, and why: `isTerminal` is a
   component-level const already in scope for BOTH rows, so A's liveness gate is one clause reading the
   same variable, not a new derivation — the sketch's *"the gate comes free"* argument for B overstated
   the difference. And the state row is `flex-wrap` and grows a long technical string under the ⌥
   reveal, so a control there wraps unpredictably.
 - **the guard: direct flip**, with the ladder and the asymmetry in one sentence each.
 - **the accent cost**: the header's docblock says it is orientation and spends only the seam link;
   this mount spends a second accent and places a primary-tinted link beside a destructive control.
   Weighed and accepted at sketch time.
 - **C was rejected on canvas-vocabulary grounds** — the lane beside a node already spends itself on
   `＋ insert` and `✕ remove`, and Phase 188.2 pins that no focusable control may live inside
   `PhaseNodeCard`. Recorded so it is not re-proposed.

⚠ **PUBLISH THE D-03 STOP-CONDITION COUNTS, before and after** — `grep -c 'useState[(<]'`,
`grep -c 'useEffect('`, and `wc -l`, against `194.1-BASELINE.md` §3 (WorkflowRunPage 6/7/1046, minus
plan 06's `fmtElapsed` removal). **`useState(` and `useEffect(` must be UNMOVED.** If either moves,
the pressed state has leaked into a mount, D-02 is void, and a G-5 refactor recommendation becomes
owed before this feature — stop and escalate rather than proceeding.

Update plan 01's `WorkflowRunPage` absence capture IN PLACE — it is now legitimately inverted. Quote
the original under a `SUPERSEDED` marker with the reason; never delete it.

**The named plants, each observed RED:**
 - P1: gate on `runStatus !== "completed"` → the `cancelled` and `failed` cases red. ⚠ **A
   `completed`-only test does not catch this — FIVE cases, not two.**
 - P2: remove the gate entirely → the three terminal cases red.
  </action>
  <acceptance_criteria>
    - `grep -c "<StopControl" frontend/src/pages/WorkflowRunPage.tsx` is exactly 1
    - The mount sits inside the same flex container as `COPY_OPEN_THREAD` — assert by DOM containment in a test, not by line number
    - `useState(`/`useEffect(` UNMOVED on `WorkflowRunPage.tsx`, published in the SUMMARY
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — error count unmoved from §11
    - The superseded plan-01 capture carries its original
    - P1 and P2 recorded in the SUMMARY as observed RED
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>
  <done>The run surface offers a Stop in its title row, gated on the const already in scope, owning zero new state, with the sketch's rejected arms recorded rather than forgotten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Five status cases, and one cancel — not two, not none</name>
  <files>frontend/src/pages/WorkflowRunPage.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowRunPage.test.tsx` (the existing 89-case harness — how it seeds a run and its status)
    - `frontend/src/pages/WorkflowRunPage.tsx` lines 275-305 (the five shipped band sentences)
    - `frontend/src/components/chat/__tests__/ComposerStopHarness.test.tsx` lines 16-19 and 78-112 (assert the VALUE; the mock bundle)
  </read_first>
  <behavior>
    - Five separate cases, one per shipped band state: `active`, `paused`, `completed`, `cancelled`, `failed`
    - Pressing on a live run calls the cancel path exactly ONCE, with the run's thread's id resolving to exactly one run id
    - After the press the control is ABSENT and the stopping reading is present, in the title row
  </behavior>
  <action>
Add five status cases to `WorkflowRunPage.test.tsx`, driven off the run's status so each maps to one
of the five shipped band sentences. Assert presence on the two live states and **absence** on all
three terminal states.

⚠ **Five, not two.** A `completed`-only case passes P1's `runStatus !== "completed"` plant, and
`cancelled` is the state the user lands on immediately after using this very control — a dead Stop
there would be the same defect this plan exists to remove, one screen later.

Add the press case: `cancelRun` is called exactly once and **with a VALUE**, never
`expect(cancelRun).toHaveBeenCalled()` alone — that assertion passes under the two-id landmine
(`ComposerStopHarness.test.tsx:16-19`; `WorkflowLock.runId` carries two id types while its JSDoc
asserts one). Then assert the control's testid is absent and the stopping reading is present.

Add the **no-guard** case: after ONE press the cancel has already been issued — there is no
intermediate confirm element, no second control to click, and no `role="alertdialog"` in the tree.
Sketch 169's guard question is settled as *direct flip*, and an untested settlement is a hope.

**The named plant, observed RED:** P3 — add a confirmation step before the dispatch. The no-guard case
reds.
  </action>
  <acceptance_criteria>
    - Five status cases exist, each asserting presence or absence, and each named for its band state
    - The press case asserts the VALUE handed to the cancel path and a call count of exactly 1
    - The no-guard case asserts no confirm element and no `role="alertdialog"` after one press
    - P3 recorded in the SUMMARY as observed RED
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>
  <done>All five shipped run states are driven, one press issues exactly one cancel by value, and the direct-flip guard is tested rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: The two source fences, each swept over a provably non-empty file</name>
  <files>frontend/src/pages/WorkflowRunPage.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowRunPage.test.tsx` (its existing `?raw` source-fence idiom, if any; otherwise `frontend/src/components/workflows/__tests__` for the shipped AST/raw fence shapes)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §4
    - `frontend/src/components/panel/WorkspacePanel.tsx` lines 155-170 (the two-id-types finding this fence exists to protect)
  </read_first>
  <action>
Add two source fences to `WorkflowRunPage.test.tsx`:

 F1: `grep -c "cancelRun("` over `WorkflowRunPage.tsx` is **0** — it goes through `stopThread`, per
     Phase 194 D-08. **Plant a literal `cancelRun(` in the production file and observe it RED.**
 F2: `grep -cE "workflowLock\??\.runId|cancelRun\("` over the same file is **0** — the *four mounts,
     ONE mechanism* rule, held mechanically rather than by discipline. ⚠ The reason it matters is
     specific and is worth one line in the fence's docblock: `WorkflowLock.runId` carries **two id
     types** while its JSDoc asserts one (recorded at `WorkspacePanel.tsx:161-165` and copied into
     `api/runs.py`'s route header), so a mount that reaches for it typechecks and then resolves
     nothing.

⚠ **BOTH fences must print the swept source's length and assert it is NON-EMPTY before asserting the
count.** The 192.1 lesson: a fence swept against the empty string passes green and defends nothing —
three such fences shipped in that phase with the property holding and nothing guarding it. If the
fence reads the file by path, a rename silently empties it.

⚠ Comment-strip before counting if the file's own prose would otherwise trip the needle, and say so in
one line — `194.1-BASELINE.md` §4 records which files this applies to. A fence that fires on a docblock
explaining the rule is a fence that will be deleted by the next person who hits it.
  </action>
  <acceptance_criteria>
    - Both fences green, and both print + assert a non-zero swept length
    - Each fence was driven RED against a real plant in `WorkflowRunPage.tsx` (production source, not a fixture), and both plants are recorded in the SUMMARY
    - The production file is restored identically after each plant — verify with `git diff --numstat` EMPTY on it before committing
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>
  <done>Two fences that can fire, each proved against a real plant in production source, with the file restored byte-identically.</done>
</task>

<task type="auto">
  <name>Task 4: Bump the WorkflowRunPage pin in this commit, and gate</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` lines 1-60 (the extension-vs-lowering rule at :24-45) and 895-920 (⚠ RE-READ: plan 05 changed the neighbouring entry)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §1 and §11
    - `.planning/phases/194.1-make-the-stop-visible/194.1-05-SUMMARY.md` (what plan 05 wrote into the same object literal)
  </read_first>
  <action>
RE-READ `scripts/vitest-count-gate.cjs`'s `BASELINE` map at HEAD before writing — plan 05 has already
edited the entry beside this one, and writing from a remembered shape is how a merge conflict becomes
a silent overwrite.

Run the gate to get the ACTUAL count for `WorkflowRunPage.test.tsx`, then set its entry to that
number. ⚠ Take the figure from the gate's own `actual` column on **two agreeing runs**, per the
precedent the gate's header records for prior extensions — never a hand count.

This is an **EXTENSION** (cases added, none deleted), so no deletion rides along — that requirement is
on LOWERINGS only (`:24-45`). The bump lands in **THIS** commit, with its cases.

⚠ **Do not touch `"WorkspacePanel.test.tsx"`'s entry** (plan 05 owns it) and **do not add anything to
`TARGETS`** — `src/components/chat` is deliberately absent (`194.1-BASELINE.md` §2) and widening the
gate's scope is a change this phase has not scoped.

Then: `cd frontend && npx tsc --noEmit -p tsconfig.app.json` (never the bare form) — record the error
count against §11. Then `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` —
read the **verdict line**; record `total` / `failed` / `pinned total` / verdict. ⚠ If `failed` is
non-zero, CAPTURE THE FAILING FILENAMES BEFORE RE-RUNNING ANYTHING; cap 2 is not deterministic and
every reading is recorded, not the best one.
  </action>
  <acceptance_criteria>
    - `"WorkflowRunPage.test.tsx"`'s pin equals the gate's `actual` on two agreeing runs, and is **greater than 89**
    - `git diff scripts/vitest-count-gate.cjs` touches ONLY that one entry — `"WorkspacePanel.test.tsx"` and `TARGETS` byte-unchanged
    - `tsc` error count recorded and unmoved from §11
    - Count gate verdict `OK`, `failed 0`, no per-file DECREASE — or every failing filename captured before any re-run
    - The pin bump is in the SAME commit as the cases that raised it
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The pin is bumped from the gate's own reading, in the same commit as its cases, with plan 05's sibling entry provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → `stopThread(run.thread_id)` → `DELETE /runs/{id}` | A user-triggered destructive action on a canvas-gated page. The API's owner-scoping (`api/runs.py:1211-1253`) is the gate and is byte-untouched. |
| the canvas gate | This page is canvas-only (`visual_workflow_canvas`, shipped OFF). The control inherits that gate; it does not create a second reachability path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194.1-07-01 | Spoofing | a Stop offered on a terminal run — a control claiming a capability it does not have | mitigate | Gated on the shipped `isTerminal` const. P1 drives it RED with a `completed`-only plant, which is why FIVE cases are required and not two |
| T-194.1-07-02 | Elevation of privilege | the page reaching past the one durable cancel path | mitigate | F1 and F2, each swept over a provably non-empty source and each driven RED against a real plant in production source |
| T-194.1-07-03 | Tampering | an accidental press on a click-to-pan surface | accept | Sketch 169 weighed it: C (the lane) was the only variant that earned a guard, and C was rejected on canvas-vocabulary grounds. The title row is not a pan region. *A Stop that is too hard costs you the reason the control exists.* Recorded in the mount's comment, not left implicit |
| T-194.1-07-04 | Repudiation | the pressed state leaking into this 1046-line page | mitigate | D-03's counts published before/after and required unmoved; `StopControl` owns no state either |
| T-194.1-07-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** No new dependency is required. A task reaching for one stops and escalates to a blocking human checkpoint |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `OK`, `failed 0`, no per-file DECREASE
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → unmoved from §11
- `grep -cE "workflowLock\??\.runId|cancelRun\(" frontend/src/pages/WorkflowRunPage.tsx` → 0
- `useState(`/`useEffect(` unmoved on `WorkflowRunPage.tsx`
- `git diff scripts/vitest-count-gate.cjs` touches one `BASELINE` entry only
</verification>

<success_criteria>
- A Stop renders in the title row on `● Running` and `Paused for your answer`, and is absent on
  `✓ Complete`, `⊘ Cancelled` and `✕ Failed` — five cases
- One press issues exactly one cancel, asserted by VALUE, with no confirmation step
- F1 and F2 green, both swept over a provably non-empty source, both driven RED against real plants
- `useState(`/`useEffect(` unmoved; the page owns no new state
- The `WorkflowRunPage.test.tsx` pin bumped in the same commit as its cases, with plan 05's entry untouched
</success_criteria>

<output>
Create `.planning/phases/194.1-make-the-stop-visible/194.1-07-SUMMARY.md` when done.
</output>
