---
phase: 194.1-make-the-stop-visible
plan: 05
type: execute
wave: 4
depends_on: ["194.1-04"]
files_modified:
  - frontend/src/components/panel/WorkspacePanel.tsx
  - frontend/src/components/chat/ActiveRunsTray.tsx
  - frontend/src/components/panel/PendingAskCard.tsx
  - frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx
  - frontend/src/components/panel/__tests__/PendingAskCard.test.tsx
  - frontend/src/components/chat/__tests__/ActiveRunsTray.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "A completed run offers no Stop control in the workspace panel, and its timeline still renders"
    - "Pressing Stop in the panel makes the composer's control disappear for the same thread"
    - "An approval waiting when the run was stopped stays on screen, visibly retired, and cannot be submitted"
    - "The retired sentence says something the two shipped expiry sentences do not"
  artifacts:
    - path: "frontend/src/components/panel/WorkspacePanel.tsx"
      provides: "The Stop row gated on isHarness; showTimeline byte-unchanged"
      contains: "StopControl"
    - path: "frontend/src/components/panel/PendingAskCard.tsx"
      provides: "The retired-not-removed arm and its own sentence"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "The WorkspacePanel.test.tsx pin, bumped in the same commit as its new cases"
  key_links:
    - from: "frontend/src/components/panel/WorkspacePanel.tsx"
      to: "isHarness"
      via: "the Stop row's own gate — a SECOND boolean, not a narrowed showTimeline"
      pattern: "isHarness && threadId"
    - from: "frontend/src/components/panel/PendingAskCard.tsx"
      to: "the retired render arm"
      via: "a DERIVED arm that short-circuits above canSubmit, never a new useState"
      pattern: "retired"
---

<objective>
Stop offering a dead control, mount the shared one on the panel and the tray, and retire a pending
approval with a sentence instead of a disappearance.

Purpose:
- **R7(a)** — `WorkspacePanel.tsx:453` renders "THIS RUN — Stop" on a **finished** run, because its
  gate is `showTimeline = isHarness || phases.length > 0` and phase rows **outlive the run**. Driven
  in 194 UAT-03 on a completed run: no network request, `document.body.innerText` byte-identical
  (`diffLen: 0`). A control that does nothing is the exact lie this phase exists to remove.
- **R7(b)** — an approval card stays actionable after a stop while Postgres already reads `cancelled`.
- **R1 across mounts** — the panel and the tray join the composer on the one shared mechanism.

Output: two booleans where there was one, two `<StopControl>` mounts, one derived retirement arm, and
the cross-mount case that is D-06's real proof.

⚠ **D-25 — `showTimeline` itself must NOT change.** Its `|| phases.length > 0` disjunct is
load-bearing: it is what keeps a **finished** run's timeline visible (Phase 098 UAT run-honesty fix B,
`threads.py:1176-1186`). Narrowing it regresses that. **Two consumers of one boolean become two
booleans.** This refines D-20 — which correctly located the gate at `:354` — by naming what may and
may not change there.

⚠ **D-20/BUG-260816-01 line correction, repeated because the bug report still reads the old way:**
`:453` is where the row RENDERS; `:354` is where the gate is DEFINED. A plan aimed at `:453` alone
would not find the gate; a plan aimed at `:354` alone would not find the row. Both lines are edited.

⚠ **SERIALIZATION.** This plan and plan 06 both edit `scripts/vitest-count-gate.cjs`'s `BASELINE`
object literal. Two concurrent edits to one object literal is a merge conflict with **no
`files_modified` signal**. They are placed in different waves for exactly that reason. **This plan
must land before 06 starts.**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194.1-make-the-stop-visible/194.1-SPEC.md
@.planning/phases/194.1-make-the-stop-visible/194.1-CONTEXT.md
@.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md
@.planning/sketches/170-the-thread-that-remembers-the-stop/README.md
@CLAUDE.md

<interfaces>
<!-- Verbatim at base f9e55b6d -->

`WorkspacePanel.tsx:335-354`
  const { data: phases } = usePhases(threadId)
  const streamActions = useStreamActions()
  const workflowLock  = useWorkflowLockForThread(threadId)
  const isHarness     = workflowLock != null
  const showTimeline  = isHarness || phases.length > 0
  const showBatchResults = showTimeline && tasks.length > 0     ← a THIRD consumer; leave it alone

`WorkspacePanel.tsx:453-470` — the Stop row, gated on `showTimeline && threadId`, carrying
  `PANEL_STOP_LEAD` (a `<span>` lead), and a `<button data-testid="panel-stop-run"
  onClick={() => void streamActions.stopThread(threadId)} aria-label={PANEL_STOP_ARIA}>
  <Square className="h-2.5 w-2.5 fill-current"/> {PANEL_STOP_LABEL}</button>`.
  Its own comment records that the row is mounted at RUN level and deliberately NOT beside any phase
  row, and that "the `threadId &&` clause is a type narrowing, not a second gate."

`streamsStore.ts:161-174` — THE INVARIANT that makes `isHarness` the correct non-terminal signal:
  "A thread holds a lock **iff a non-terminal** Harness workflow run owns its `active_workflow_run_id`
  anchor." Cleared on every terminal — `StreamsProvider.tsx:2202` (send-path onTerminal, "a TERMINAL
  kind unlocks the thread"), `:2269`, and on reconcile at `:1855-1857` ("Stale / terminal / Deep →
  unlock").

`ActiveRunsTray.tsx:105-112` — the `Stop all` bulk button (renders at 2+ runs), dispatching
  `runningIds.forEach((id) => void streamActions.stopThread(id))`.
`ActiveRunsTray.tsx:126-134` — the per-row Stop.

`PendingAskCard.tsx`
  :247  `const canSubmit = hasAnswer && runReady && !submitting && state === "pending"`
        consumed at :426-430 as BOTH `aria-disabled={!canSubmit}` and `disabled={!canSubmit}`
  :276-281  the 404-on-submit path — "the run is terminal … surface it honestly as the calm expired
        state instead of a dead-but-submittable card" → sets `expiredMessage` and `state="expired"`
  :293-322  the `expired` render arm — "calm grey, never a crash", `role="status"` root, a message
        slot at :315-318 (`expiredMessage ?? (timeout ? "No response within …" : "This prompt is no
        longer active")`)
  :460-486  `PendingAskStack` — takes NO props; calls `useViewingThread()` and
        `useAskUserPrompt(threadId)` itself. Mounted at `WorkspacePanel.tsx:386` as `<PendingAskStack />`.

`pendingAsksByThread` is written ONLY by `addPendingAskForThread` / `removePendingAskForThread`
(`StreamsProvider.tsx:939`, the `ask_user_response` SSE) and `replacePendingAsksForThread`
(reconcile). **No terminal handler clears it** — so R7(b) is a RENDER change, not a preservation
problem.

`scripts/vitest-count-gate.cjs` — `"WorkspacePanel.test.tsx": 41` (~:913) and
`"src/components/panel/__tests__/WorkspacePanel.test.tsx"` in `TARGETS` (~:2222). Its rule at :24-45:
an EXTENSION bumps the pin in the same commit and needs no deletion; a LOWERING additionally needs the
deletion in the same commit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R7(a) — two booleans, and the timeline that must survive</name>
  <files>frontend/src/components/panel/WorkspacePanel.tsx, frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx</files>
  <read_first>
    - `frontend/src/components/panel/WorkspacePanel.tsx` lines 330-400 and 445-500
    - `frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx` (the whole existing harness — how it seeds a lock and phases)
    - `frontend/src/stores/streamsStore.ts` lines 155-180 (the lock invariant)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §3
  </read_first>
  <behavior>
    - On a run with a live lock: the Stop row renders AND the timeline renders
    - On a COMPLETED run (lock cleared, phases still present): the Stop row is ABSENT and the timeline STILL RENDERS
    - On a Deep thread (no lock, no phases): neither renders — unchanged
  </behavior>
  <action>
FIRST ACTION IN THE WORKTREE: `bash scripts/bootstrap-worktree.sh "$(pwd)"`. Assert the dispatched
base SHA and STOP if it does not match.

At `:354`, ADD a second boolean beside `showTimeline` — a `showStopRow` (name at your discretion)
defined as `isHarness && threadId != null`. **Do not touch `showTimeline`'s definition**, and do not
touch `showBatchResults`, which is a third consumer of it.

At `:453`, change the row's gate from `showTimeline && threadId` to the new boolean.

Write ONE comment above the new boolean recording why there are now two: the lock invariant
(`streamsStore.ts:161-174`) makes `isHarness` the honest *"the run is still going"* signal and it is
cleared on every terminal route; while `showTimeline`'s `|| phases.length > 0` disjunct is what keeps
a **finished** run's timeline visible (Phase 098 UAT run-honesty fix B) and **narrowing it would
regress that**. State it on ONE LINE where the rule itself is quoted — ⚠ a rule written WRAPPED fails
its own line-oriented `grep` and reads as "already fixed" (193.2-08, re-proved by WR-05).

**The named plants, each observed RED:**
 - P1: narrow `showTimeline` itself instead of adding a boolean → the **timeline-still-renders** case
   reds. **TWO assertions are needed, not one** — a "no Stop on a completed run" case alone passes
   this plant, which is precisely how the regression would ship.
 - P2: gate the row on `phases.length > 0` (the shipped bug) → the no-Stop-on-completed case reds.
  </action>
  <acceptance_criteria>
    - `grep -n "showTimeline" frontend/src/components/panel/WorkspacePanel.tsx` still shows its shipped definition byte-identical, and it is still consumed by the timeline section and by `showBatchResults`
    - A test asserts, on a completed run: `queryByTestId("panel-stop-run")` is null AND the timeline section is present
    - A test asserts, on a live run: both present
    - The one-line rule comment matches `grep -n "098" frontend/src/components/panel/WorkspacePanel.tsx` on a line that also names `showTimeline`
    - P1 and P2 recorded in the SUMMARY as observed RED, each naming the case that fired
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx</automated>
  </verify>
  <done>Two booleans exist, `showTimeline` is byte-unchanged, and the completed-run case asserts both the absence and the survival.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the shared control on the panel and the tray</name>
  <files>frontend/src/components/panel/WorkspacePanel.tsx, frontend/src/components/chat/ActiveRunsTray.tsx, frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx</files>
  <read_first>
    - `frontend/src/components/chat/StopControl.tsx` (plan 04's component and its variant defaults)
    - `frontend/src/components/panel/WorkspacePanel.tsx` lines 296-310 (the `PANEL_STOP_*` constants) and 445-472
    - `frontend/src/components/chat/ActiveRunsTray.tsx` (whole — 141 lines)
    - `frontend/src/components/chat/__tests__/ActiveRunsTray.test.tsx`
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §3 and §4
  </read_first>
  <action>
**Panel:** replace the inline `<button data-testid="panel-stop-run" …>` with
`<StopControl threadId={threadId} variant="panel" label={PANEL_STOP_LABEL} ariaLabel={PANEL_STOP_ARIA}
testId="panel-stop-run" />`. The `PANEL_STOP_*` module constants stay exactly where they are — they are
already extracted, and moving them would be a second change dressed as tidying. The `PANEL_STOP_LEAD`
`<span>` and the row's wrapper `<div>` are byte-untouched.

**Tray:** replace the per-row `<button … aria-label={`Stop run on ${titleFor(id)}`}>` with
`<StopControl threadId={id} variant="tray" ariaLabel={`Stop run on ${titleFor(id)}`} />`.

⚠ **`Stop all` (`:105-112`) stays BYTE-UNTOUCHED, and that is a decision rather than an omission.** It
is a bulk control over N threads, not a per-thread mount; per-row acknowledgement is what each row's
own `<StopControl>` now provides, and pressing `Stop all` will put every row into its stopping reading
through the shared store. Record that sentence in the SUMMARY so the next reader finds a decision.

⚠ **PUBLISH THE D-03 STOP-CONDITION COUNTS, before and after, for both files** —
`grep -c 'useState[(<]'`, `grep -c 'useEffect('`, props-interface member count, `wc -l`, against
`194.1-BASELINE.md` §3 (WorkspacePanel 4/3/580, ActiveRunsTray 2/3/141). **`useState(` and
`useEffect(` must be UNMOVED on both.** If either moves, the pressed state has leaked into a mount,
D-02 is void and a G-5 refactor recommendation becomes owed FIRST — stop and escalate rather than
proceeding.

Re-measure `grep -cE "workflowLock\??\.runId|cancelRun\("` on both files against `194.1-BASELINE.md`
§4. ⚠ `WorkspacePanel.tsx`'s reading may be non-zero because its `:161-165` comment names
`workflowLock.runId` in PROSE — scope the fence to code lines (strip comments before counting) and say
so in one line, rather than asserting a raw 0 the file cannot honestly hold.

Update plan 01's `StopControl.baseline.test.tsx` panel and tray captures IN PLACE, quoting each
superseded assertion under a `SUPERSEDED` marker with its reason.
  </action>
  <acceptance_criteria>
    - `grep -c "<StopControl" frontend/src/components/panel/WorkspacePanel.tsx` is 1; same for `ActiveRunsTray.tsx`
    - `grep -c "stopThread(" frontend/src/components/panel/WorkspacePanel.tsx` is **0** (the dispatch moved into the shared component)
    - `ActiveRunsTray.tsx` still contains exactly ONE `stopThread(` — inside `Stop all`, byte-untouched
    - `useState(`/`useEffect(` UNMOVED on both files, published in the SUMMARY
    - The comment-stripped `workflowLock\??\.runId|cancelRun\(` count is 0 on both, with the stripping rule stated in one line
    - Superseded baseline assertions carry their originals
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx src/components/chat/__tests__/ActiveRunsTray.test.tsx src/components/chat/__tests__/StopControl.baseline.test.tsx</automated>
  </verify>
  <done>Panel and tray render the shared control, own zero new state, and `Stop all` is provably untouched with the reason recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: R7(b) — the approval is retired with a sentence, never removed</name>
  <files>frontend/src/components/panel/PendingAskCard.tsx, frontend/src/components/panel/__tests__/PendingAskCard.test.tsx</files>
  <read_first>
    - `frontend/src/components/panel/PendingAskCard.tsx` lines 160-340 and 420-486
    - `frontend/src/components/panel/__tests__/PendingAskCard.test.tsx` (the whole harness)
    - `frontend/src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx` (plan 01's before)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §9 (the three shipped retirement literals, quoted verbatim)
  </read_first>
  <behavior>
    - After the run is over, a pending card is STILL IN THE DOM
    - It renders visibly retired, with a sentence explaining that the run was stopped
    - Its submit action is not dispatchable — `disabled` and `aria-disabled` both true
    - The retired sentence is pairwise-distinct from BOTH shipped expiry sentences
    - A Deep thread and a live harness run are unaffected
  </behavior>
  <action>
Derive the signal — **no new fetch, no new `useState`**. In `PendingAskStack` (which already calls
`useViewingThread()`), add `useWorkflowLockForThread(threadId)` and `usePhases(threadId)` and compute
`runIsOver = workflowLock == null && phases.length > 0`: a harness run existed (durable phases survive
a terminal, `threads.py:1176-1186`) and no longer holds the anchor. False on a Deep thread (phases
empty); false mid-run (lock present). Pass it to `PendingAskCard` as one new prop.

In `PendingAskCard`, **derive** the render arm — `const arm = runIsOver && state === "pending" ?
"retired" : state` — and short-circuit to a retired render **above** the existing `expired` arm.
`canSubmit`'s `state === "pending"` clause is byte-untouched because the arm short-circuits above it.
⚠ **Do NOT add a `useState`.** The card already carries nine; a tenth for a derivable fact would be
the second-concern failure D-03 forbids.

The retired arm reuses the shipped `expired` arm's SHAPE (calm grey, `role="status"` root, a message
slot) — that is adopted design, not new. Its **sentence is new** and must state that the **run was
stopped**. Add it as a module constant beside the shipped literals.

⚠ **Three retirements wearing one coat are three lies.** The retired sentence must be
**pairwise-distinct** from the 404-expiry literal (`:280`) and the countdown-timeout literal (`:317`),
read out of `194.1-BASELINE.md` §9 rather than re-typed — a fence built on a re-typed literal tests
the typing. Assert **all three pairs**; an `a !== b` fence over two of the three passes a plant that
reuses the third.

**The named plants, each observed RED:**
 - P3 (A): REMOVE the card when the run is over → the **still-in-DOM** assertion reds.
 - P4 (B): retire it visually but leave `disabled={false}` → the **dispatchability** assertion reds.
   ⚠ Both plants are needed — either one alone leaves half the requirement unguarded.
 - P5: reuse `:280`'s string as the retired sentence → the pairwise-distinct fence reds.
  </action>
  <acceptance_criteria>
    - A test asserts the card element is present in the DOM after `runIsOver` flips true
    - A test asserts the submit control has BOTH `disabled` and `aria-disabled="true"`
    - A test asserts all three pairwise inequalities among the three sentences, with the two shipped ones read from source rather than re-typed
    - `grep -c "useState[(<]" frontend/src/components/panel/PendingAskCard.tsx` is **9** — unmoved from `194.1-BASELINE.md` §3
    - P3, P4, P5 recorded in the SUMMARY as observed RED
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/__tests__/PendingAskCard.test.tsx src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx</automated>
  </verify>
  <done>A stopped run's approval stays on screen, says why, cannot be submitted, and says something the two shipped expiries do not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: The REAL cross-mount case — D-06's only proof</name>
  <files>frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx</files>
  <read_first>
    - `frontend/src/components/chat/__tests__/StopControl.test.tsx` (plan 04's two-instance ancestor case)
    - `frontend/src/components/chat/__tests__/ComposerStopHarness.test.tsx` lines 78-112 (the mock bundle)
    - `frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx` (how it mounts the panel with a lock)
  </read_first>
  <behavior>
    - The composer and the panel, mounted for the SAME thread under ONE provider: pressing the PANEL's Stop makes the COMPOSER's control disappear
  </behavior>
  <action>
Add one case rendering `MessageInput` (composer variant) and `WorkspacePanel` (panel variant) for the
same `threadId` under one `<StreamsProvider>`. Press the panel's Stop; assert
`queryByTestId("composer-stop")` is null AND `queryByTestId("panel-stop-run")` is null AND the
stopping reading appears in both slots.

⚠ **This is the case a single-mount test structurally cannot see, and it is the only proof of D-06.**
Plan 04's two-instance case is its ancestor; this one is the real one, because it uses the two mounts
that a user can genuinely have on screen simultaneously for one thread.

**The named plant, observed RED:** P6 — move the flag into a `useState` inside `MessageInput`. This
case reds; every single-mount case in the phase stays green. Record both facts.

Place the case in `WorkspacePanel.test.tsx` (it is the pinned suite this plan already bumps) rather
than creating a fourth file, and note in one line why it lives there.
  </action>
  <acceptance_criteria>
    - The cross-mount case exists and is green
    - It asserts BOTH testids are null after ONE press
    - P6 recorded in the SUMMARY as observed RED, together with the observation that the single-mount cases stayed green under it
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx</automated>
  </verify>
  <done>One press on the panel removes the composer's control for the same thread, proved under a plant that no single-mount case can catch.</done>
</task>

<task type="auto">
  <name>Task 5: Bump the WorkspacePanel pin in this commit, and gate</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` lines 1-60 (the gate's own extension-vs-lowering rule at :24-45) and 895-920
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §1 and §11
    - `CLAUDE.md` § *Parallel execution* (the cap-2 rule and its two documented rots)
  </read_first>
  <action>
Run the gate to get the ACTUAL count for `WorkspacePanel.test.tsx`, then set its `BASELINE` entry to
that number. ⚠ Take the figure from the gate's own `actual` column on **two agreeing runs**, per the
precedent the gate's header records for prior extensions — never from a hand count.

This is an **EXTENSION** (cases were added, none deleted), so it needs no deletion riding along —
that requirement is on LOWERINGS only (`:24-45`). The bump lands in **THIS** commit, with its cases.

⚠ **Do NOT touch `"WorkflowRunPage.test.tsx"`'s pin.** Plan 06 owns it and edits the same object
literal; two concurrent edits are a merge conflict with no `files_modified` signal. If plan 06 has
somehow already landed, RE-READ the map before writing rather than assuming its shape.

⚠ **Do NOT add any new file to `TARGETS`.** `src/components/chat` is deliberately absent
(`194.1-BASELINE.md` §2) and widening the gate's scope is a change this phase has not scoped.

Then: `cd frontend && npx tsc --noEmit -p tsconfig.app.json` (never the bare `--noEmit`) — record the
error count against §11. Then `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`
— read the **verdict line**, record `total` / `failed` / `pinned total` / verdict. ⚠ If `failed` is
non-zero, CAPTURE THE FAILING FILENAMES BEFORE RE-RUNNING ANYTHING; cap 2 is not deterministic and
every reading is recorded, not the best one.
  </action>
  <acceptance_criteria>
    - `"WorkspacePanel.test.tsx"`'s pin equals the gate's `actual` for that file on two agreeing runs, and is **greater than 41**
    - `git diff scripts/vitest-count-gate.cjs` touches ONLY that one entry — `"WorkflowRunPage.test.tsx"` and `TARGETS` are byte-unchanged
    - `tsc` error count unmoved from §11
    - Count gate verdict `OK`, `failed 0` — or every failing filename captured verbatim before any re-run
    - The pin bump is in the SAME commit as the cases that raised it
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The pin is bumped from the gate's own reading, in the same commit as its cases, with the sibling pin untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → `stopThread` → `DELETE /runs/{id}` | Unchanged; the API's owner-scoping is byte-untouched. |
| the approval card's submit → `answerAskUser(run_id, …)` | The server already 404s a terminal run (`PendingAskCard.tsx:276-281`). This plan makes the client stop OFFERING it first — defence in depth, not a replacement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194.1-05-01 | Spoofing | a Stop control offered on a finished run — a control that claims a capability it does not have | mitigate | The row gates on `isHarness`, whose invariant (`streamsStore.ts:161-174`) is cleared on every terminal route. P2 drives it RED against the shipped `phases.length > 0` gate |
| T-194.1-05-02 | Tampering | narrowing `showTimeline` and regressing the Phase-098 finished-run timeline | mitigate | P1 — the timeline-still-renders assertion. TWO assertions, because the single one passes the plant |
| T-194.1-05-03 | Tampering | submitting an answer into a cancelled run | mitigate | The retired arm short-circuits above `canSubmit`; P4 (retired-but-dispatchable) reds. The server's 404 remains the authority |
| T-194.1-05-04 | Repudiation | a card that vanishes mid-read | mitigate | P3 — the still-in-DOM assertion. "Retired with a sentence, never removed" is a rule across all sketch-170 variants, not a design choice |
| T-194.1-05-05 | Spoofing | three retirements wearing one coat | mitigate | The pairwise-distinct fence over ALL THREE pairs, with the two shipped literals read from source. P5 reds a reuse of `:280` |
| T-194.1-05-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** No new dependency is required. A task reaching for one stops and escalates to a blocking human checkpoint |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `OK`, `failed 0`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → unmoved from §11
- `useState(`/`useEffect(` unmoved on `WorkspacePanel.tsx`, `ActiveRunsTray.tsx` and `PendingAskCard.tsx`
- `showTimeline`'s definition byte-identical; `showBatchResults` untouched
- `git diff scripts/vitest-count-gate.cjs` touches one `BASELINE` entry only
</verification>

<success_criteria>
- A completed run shows no panel Stop AND still shows its timeline
- Panel and tray render the shared `<StopControl>`; `Stop all` is byte-untouched with the reason recorded
- A stopped run's approval is present, retired, non-dispatchable, and says something the two shipped
  expiries do not
- The composer+panel cross-mount case is green and P6 was observed RED against it
- P1-P6 each observed RED; the `WorkspacePanel.test.tsx` pin bumped in the same commit as its cases
</success_criteria>

<output>
Create `.planning/phases/194.1-make-the-stop-visible/194.1-05-SUMMARY.md` when done.
</output>
