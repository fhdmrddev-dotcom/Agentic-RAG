---
phase: 194.1-make-the-stop-visible
plan: 06
type: execute
wave: 4
depends_on: ["194.1-02", "194.1-03"]
files_modified:
  - frontend/src/lib/runStepCount.ts
  - frontend/src/lib/fmtElapsed.ts
  - frontend/src/components/chat/ThreadRunLine.tsx
  - frontend/src/components/chat/MessageList.tsx
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/pages/WorkflowRunPage.tsx
  - frontend/src/lib/__tests__/runStepCount.test.ts
  - frontend/src/__tests__/components/chat/MessageList.test.tsx
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "Returning to a stopped workflow thread shows that the run was stopped by the user, with how far it got and how long it ran"
    - "That reading is derived from persisted state — it renders with the live streaming store empty"
    - "It is absent on a thread whose workflow completed normally, and absent on one that failed"
    - "At harness kickoff the transcript carries a run instrument from the first frame, and no assistant node"
  artifacts:
    - path: "frontend/src/components/chat/ThreadRunLine.tsx"
      provides: "ONE run-anchored line with a live state and a stopped state"
      contains: "Stopped by you"
    - path: "frontend/src/lib/runStepCount.ts"
      provides: "A step-count derivation TOTAL over all seven workflow_phases status literals"
    - path: "frontend/src/lib/fmtElapsed.ts"
      provides: "The shipped elapsed formatter, hoisted verbatim rather than copied a fourth time"
  key_links:
    - from: "frontend/src/components/chat/ThreadRunLine.tsx"
      to: "getThreadWorkflow"
      via: "the stopped receipt is derived from persisted state, never from the streaming store"
      pattern: "getThreadWorkflow\\("
    - from: "frontend/src/components/chat/MessageList.tsx"
      to: "ThreadRunLine"
      via: "list level, after the last message, a SIBLING of the shipped jump-to-live strip"
      pattern: "<ThreadRunLine"
---

<objective>
Build the ONE run-anchored, list-level line that sketches 170-B and 171-C converge on — **one
component, two states** — and mount it in `MessageList`.

Purpose: this convergence is the whole reason the phase is small. Treating R4 and R5 as two features
is the mis-read the SPEC names by name.

- **R4** (the return): a stopped workflow thread ends at the original prompt today — indistinguishable
  from a run that never started or one that finished. The signal is not faint; it is **absent**. The
  stop **NULLs** `threads.active_workflow_run_id`, so `RunSoul` and `RunSeam` both read from a place
  the stop has already emptied — which is why the reading must come from the persisted run row (plan
  02's three fields), and why 174 **D1** (*derived from persisted state, never from live streaming
  state*) is the constraint, not a preference. A live-only reading is precisely why `BUG-260610-01` is
  two months old.
- **R5** (the kickoff): plan 03 stopped inserting the assistant placeholder. Sketch 171-C **as drawn
  was refuted by measurement** — it claimed *"the never-vanishes strip covers the gap"* and there is
  **no such strip at kickoff**: `MessageList.tsx:217`'s `RunStatusStrip` is gated on
  `showJumpToLive = !isPinned && isStreaming`, i.e. it appears only after scrolling away, and the only
  always-present strip is inside `RunCard.tsx:336` — inside the very message C proposes not to render.
  **C survives its refutation only because the line it needs is the line 170-B already needs.** That
  line is this component's live state, and building it is what stops C producing dead air.

Output: one component, one total step-count derivation, one hoisted formatter, one list-level mount,
one prop.

⚠ **D-16 — the line is absent on `completed` (R4's acceptance) and ALSO absent on `failed`.** Stated
as a boundary, not a silent omission: `RunCard` and the run surface already carry failure in their own
vocabulary, and a failure reading here is a second capability. **An untested decision is a hope** —
the `failed` absence gets its own case. Re-open trigger: an operator observation that a failed workflow
thread is as silent as a stopped one was before this phase.

⚠ **D-17 — `MessageList.tsx:217`'s `RunStatusStrip` is NOT replaced and NOT moved.** It lives inside
the floating jump-to-live chip and is a different affordance. The new line is a **sibling**. Any edit
to that block owes a stated reason.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194.1-make-the-stop-visible/194.1-SPEC.md
@.planning/phases/194.1-make-the-stop-visible/194.1-CONTEXT.md
@.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md
@.planning/sketches/170-the-thread-that-remembers-the-stop/README.md
@.planning/sketches/171-one-run-one-slot/README.md
@CLAUDE.md

<interfaces>
<!-- Verbatim at base f9e55b6d, plus plan 02's and plan 03's additions -->

From plan 02 — `ThreadWorkflowState` (client mirror in `frontend/src/lib/api.ts`, fetched by
`getThreadWorkflow(threadId, signal?)` at :1392; owner-scoped; **NOT canvas-gated**):
  `active_workflow_run_id?: string | null`   — NULL after a stop
  `last_workflow_run_id?: string | null`     — the anchor-then-latest id that SURVIVES the NULL
  `last_run_status?: string | null`          — NEW
  `last_run_created_at?: string | null`      — NEW
  `last_run_updated_at?: string | null`      — NEW
  `phases?: WorkflowPhaseState[] | null`     — `{slug, phase_index, status, phase_type?}`, ordered

From plan 03 — `useHarnessLiveForThread(threadId): boolean` (true from the SYNCHRONOUS kickoff mark
through to the terminal, with zero round trips).

`frontend/src/components/chat/MessageList.tsx`
  Props (:11-22) — `messages, isStreaming, isLoading?, onSendMessage?, showSuggestions?, onResume?`.
    **No `threadId`.** This plan adds it.
  :190      `<div ref={bottomRef} />` — the auto-scroll anchor, AFTER the messages map
  :192-231  the floating `↓ Jump to live` chip, gated on `showJumpToLive` — **D-17: untouched**
  :24-43    `formatFloatingElapsed` — a LOCAL formatter; not the one to reuse

`frontend/src/components/chat/ChatArea.tsx:552-559` — the `<MessageList …/>` mount; `thread?.id ?? null`
  is already computed and passed to `MessageInput` at :364.

`frontend/src/pages/WorkflowRunPage.tsx:195-201` — `fmtElapsed(ms)`: `<60s → "12s"` · `<60m →
  "4m 12s"` · else `"1h 06m"`. **LOCAL, not exported.** RESEARCH: hoist it; do not copy it a fourth
  time (`RunCard`'s `formatElapsed`, `MessageList`'s `formatFloatingElapsed` and `ActiveRunsTray`'s
  usage are already three).

`frontend/src/pages/WorkflowRunPage.tsx:758-790` — **THE ELAPSED-ANCHOR RULE, shipped and binding:**
  *"a number is only ever shown beside the field it came from … A queued-anchored figure is not a lie
  — an UNLABELLED one is."* And the measurement behind it: `workflow_runs` 181 total, **5** with
  `claimed_at`; completed 149 rows, **0** with `claimed_at`. `claim_run`'s CAS lease is the
  distributed-worker path and the in-process producer never takes it. ⇒ the anchor is
  `created_at → updated_at`, and it must be DISCLOSED.

`frontend/src/lib/toolMeta.ts:182` — `return isHarness ? "Starting workflow…" : "Setting up agent…"`.
  The live word is SHIPPED (Phase 174 D4). Reuse it; do not mint a second one.

`supabase/migrations/119_workflow_phases_cancelled.sql` — the CURRENT complete constraint:
  `ARRAY['pending','active','completed','failed','skipped','recorded_not_sent','cancelled']` — SEVEN.
  This file is the derivation source for the totality fence; do not hand-type the set.

`frontend/src/__tests__/components/chat/MessageList.test.tsx:20-23` — `renderML()` renders inside a
  bare `TooltipProvider` with **NO `StreamsProvider`**, so "the streaming store is empty" is
  STRUCTURAL. That is the property R4's central fence rests on.

Canvas visibility, for the optional seam: `useEffectiveFeaturesOptional()` →
  `?.features.visual_workflow_canvas === true` (the shipped strict read at `ChatLayout.tsx:249-250`,
  whose own comment records that a NULL context reads exactly like an empty map: HIDDEN — "the
  Phase-148 vanish convention", failing closed).

⚠ **No net-new glyph (D-18).** `◆` is in `icon-convention.md` §4's table (`⛨ 🔒 ⤳ ＋ ✕ ↶ ↷ ◆`) and `⊘` is
shipped tier-1 vocabulary (174 D1/D2). Use those two and nothing else.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A step-count derivation TOTAL over seven literals, and the formatter hoisted not copied</name>
  <files>frontend/src/lib/runStepCount.ts, frontend/src/lib/fmtElapsed.ts, frontend/src/lib/__tests__/runStepCount.test.ts, frontend/src/pages/WorkflowRunPage.tsx</files>
  <read_first>
    - `supabase/migrations/119_workflow_phases_cancelled.sql` lines 110-130
    - `frontend/src/pages/WorkflowRunPage.tsx` lines 185-215 (`fmtElapsed` and its docblock) and every call site of it in that file
    - `frontend/src/lib/stepCount.ts` (the shipped `unifiedStepCount` — a DIFFERENT concern, over messages; do not extend it)
  </read_first>
  <behavior>
    - `stepsFrom([])` and any all-zero-length input return `null` — a `0 of 0` reading is REFUSED, not printed
    - Only `completed` counts toward the numerator; the other SIX literals do not
    - The denominator is the number of phase rows
    - The derivation does not throw on an unknown literal — it counts it as not-completed
  </behavior>
  <action>
FIRST ACTION IN THE WORKTREE: `bash scripts/bootstrap-worktree.sh "$(pwd)"`. Assert the dispatched
base SHA and STOP if it does not match.

Create `frontend/src/lib/runStepCount.ts` exporting a pure function taking the wire `phases` array and
returning `{ done, total } | null`. Return `null` when `total === 0`.

⚠ **The derivation must be TOTAL over all SEVEN literals.** Since migration 119 the vocabulary is
`pending`, `active`, `completed`, `failed`, `skipped`, `recorded_not_sent`, `cancelled` — and a
five-literal derivation is one of RESEARCH's named pitfalls. `skipped`, `recorded_not_sent`,
`cancelled`, `failed`, `active` and `pending` all count as **not completed**, which is exactly what
makes `2 of 3 steps` honest after a stop (194 **D-13**: completed phases survive a stop, and the
reading must not suggest nothing did).

Prefer a shape with no `switch` at all — `done = rows.filter(r => r.status === "completed").length` is
total by construction and cannot omit a literal. Say so in the docblock: the totality is structural,
not enumerated, and the test below exists to prove it stays that way.

Create `frontend/src/lib/__tests__/runStepCount.test.ts`. ⚠ **DERIVE the expected literal set from
`supabase/migrations/119_workflow_phases_cancelled.sql`** — read the file, parse its `ADD CONSTRAINT`
`ARRAY[…]`, assert the parsed set has **7** members, and drive the derivation once per literal. Do
NOT hand-type the seven; a hand-typed set tests the typing, and this is the exact drift the
`workflow_runs.py:87-95` docstring already suffered.

**Hoist, do not copy, the formatter.** Move `fmtElapsed` from `WorkflowRunPage.tsx:195-201`
**verbatim** into `frontend/src/lib/fmtElapsed.ts` and import it back into `WorkflowRunPage.tsx`. The
function BODY must be byte-identical — assert it, and record `git diff --numstat` on
`WorkflowRunPage.tsx` showing only the removal and the import. This file already has three elapsed
formatters; a fourth is what RESEARCH's *Don't Hand-Roll* table forbids.
⚠ `WorkflowRunPage.test.tsx` is PINNED at 89. A verbatim move adds no cases, so **no pin bump is owed
here and none may be made** — plan 07 owns that entry.

**The named plants, each observed RED:**
 - P1: a `switch` missing `cancelled` → the totality test reds on the `cancelled` literal.
 - P2: return `{done:0,total:0}` instead of `null` for an empty array → the refuse-`0 of 0` case reds.
 - P3: change one character inside the hoisted `fmtElapsed` body → the byte-identity fence reds.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/__tests__/runStepCount.test.ts` exits 0
    - The test asserts the parsed literal set from migration 119 has exactly 7 members BEFORE driving the derivation, and the parse is asserted non-empty
    - `grep -c "fmtElapsed" frontend/src/pages/WorkflowRunPage.tsx` ≥ 1 (an import + its call sites), and the function DEFINITION is gone from that file
    - `git diff --numstat frontend/src/pages/WorkflowRunPage.tsx` shows a net removal, no behaviour line changed
    - `git diff scripts/vitest-count-gate.cjs` is EMPTY
    - P1, P2, P3 recorded in the SUMMARY as observed RED
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/__tests__/runStepCount.test.ts src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>
  <done>A structurally-total step count that refuses `0 of 0`, proved against a literal set parsed from migration 119, and one formatter where there would have been four.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThreadRunLine — one component, two states</name>
  <files>frontend/src/components/chat/ThreadRunLine.tsx</files>
  <read_first>
    - `.planning/sketches/170-the-thread-that-remembers-the-stop/README.md` (winner B, and its "Binds regardless of winner" list)
    - `.planning/sketches/171-one-run-one-slot/README.md` (the amendment: "One component, two states" and its literals)
    - `frontend/src/lib/api.ts` lines 1290-1340 and 1388-1402
    - `frontend/src/pages/WorkflowRunPage.tsx` lines 750-800 (the shipped elapsed-anchor disclosure rule and the `claimed_at` measurement)
    - `frontend/src/lib/toolMeta.ts` lines 130-190
    - `frontend/src/components/layout/ChatLayout.tsx` lines 240-255 (the strict canvas read and its fail-closed comment)
  </read_first>
  <behavior>
    - `last_run_status === "cancelled"` and not live → `⊘ Stopped by you · 2 of 3 steps · 2m 18s`
    - live (from the synchronous kickoff mark onward) → `◆ Starting workflow… · Step 2 of 3 · 2m 18s`
    - `completed` → renders nothing; `failed` → renders nothing
    - no run at all, or `threadId == null` → renders nothing
    - the step count is omitted rather than printed as `0 of 0` when there are no phase rows
    - the elapsed figure carries a disclosure naming the field it came from
  </behavior>
  <action>
Create `frontend/src/components/chat/ThreadRunLine.tsx`, props `{ threadId: string | null }`, with one
stable `data-testid` (e.g. `thread-run-line`) present in BOTH states so a consumer can assert
presence/absence without knowing which state is showing.

**Data:**
 - live: `useHarnessLiveForThread(threadId)` — a store read, SYNCHRONOUS from kickoff. ⚠ It **must**
   be the presence signal for the live arm. A fetch-driven presence would leave one kickoff round trip
   in which the transcript shows a prompt and no run instrument — exactly the interval R5's acceptance
   forbids, and exactly the dead air sketch 171-C was refuted for.
 - the row: `getThreadWorkflow(threadId)` in a `useEffect` keyed on `[threadId, isLive]`. That gives a
   fetch on mount (the returning reading) and a fetch on each liveness transition (kickoff, and again
   at the terminal so the stopped receipt is final). **This is fetch-on-mount-and-transition, NOT a
   poll** — D-11, and `WorkflowRunPage.tsx:683-731` already owns the polling concern for the run
   surface. Abort in the effect's cleanup.
 - step count: plan 06 task 1's derivation over `frame.phases` (stopped arm) or `usePhases(threadId)`
   (live arm). Omit the segment entirely when it returns `null`.
 - elapsed: `fmtElapsed(Date.parse(last_run_updated_at) - Date.parse(last_run_created_at))` for the
   stopped arm; for the live arm, anchor on `last_run_created_at` and tick once a second. Unparseable
   or absent → omit the segment; **never render `NaN`** and never render a number-shaped string derived
   from a missing anchor.

**Copy** (sketch 171's amendment, verbatim):
  stopped: `⊘ Stopped by you · {done} of {total} steps · {elapsed}`  (+ the seam, below)
  live:    `◆ {word} · Step {done+1} of {total} · {elapsed}`
Take `{word}` from the shipped harness activity word at `toolMeta.ts:182` — export it or call the
function that returns it; **do not copy the string**. Phase 174 D4 already chose that sentence.

⚠ **194 D-13 — the step count is in the SAME SENTENCE as the word, in both states.** A stop must not
read as though nothing survived: completed phases survive, and the database still holds the evidence.

⚠ **THE ELAPSED ANCHOR MUST BE DISCLOSED.** The figure is `created_at → updated_at`, i.e. **from
queued to last update**, not from when work began — `claimed_at` is present on 5 of 181
`workflow_runs` and on **0 of 149** completed rows, so anchoring there would show nothing at all. The
shipped rule (`WorkflowRunPage.tsx:770-773`) is *"a queued-anchored figure is not a lie — an
UNLABELLED one is."* The chat subtree has **no `title=` fence** (the F1 no-`title=` fence is scoped to
`components/workflows/library/`, and `ActiveRunsTray.tsx:122` already uses one), so a `title` on the
elapsed segment is the cheapest honest disclosure. Assert the disclosure exists and names the two
fields.

**The seam.** The stopped arm may carry `Open the run ›` to the run surface — gated on
`useEffectiveFeaturesOptional()?.features.visual_workflow_canvas === true`, strict, so it degrades to
nothing when the canvas is off (which it does by default) and in a bare test render. `WorkflowRunPage`
is itself canvas-only, so a seam offered with the canvas off would be a link to a locked door. Assert
BOTH: present with the flag true, absent with it absent.

⚠ **D-16 — absent on `completed` AND absent on `failed`.** Two cases, not one.

**The named plants, each observed RED:**
 - P4: derive the stopped arm from `useStreamingForThread` (a live-store read) → the store-empty case
   in task 4 reds with `undefined`/absent. This is 174 D1 made mechanical.
 - P5: render on any terminal status → the `completed` case AND the `failed` case red.
 - P6: return `phases: []` from the frame → the reading must be `⊘ Stopped by you · 2m 18s` with the
   step segment ABSENT, never `0 of 0 steps`.
 - P7: mount the live arm on the first delta instead of on the synchronous kickoff mark → task 4's
   no-dead-interval case reds.
  </action>
  <acceptance_criteria>
    - `grep -c "useStreamingForThread\|bucketsBySurface" frontend/src/components/chat/ThreadRunLine.tsx` is **0** — the stopped receipt reads no live-message state
    - `grep -c "⏹\|■" frontend/src/components/chat/ThreadRunLine.tsx` is **0**; `⊘` and `◆` each appear
    - The elapsed segment carries a `title` naming both `created_at` and `updated_at` in words
    - Cases exist for: cancelled+phases, cancelled+no-phases (step segment omitted), completed (nothing), failed (nothing), no-run (nothing), live (present with the shipped word)
    - Seam present with the canvas flag true, absent without it
    - P4-P7 recorded in the SUMMARY as observed RED
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/components/chat/MessageList.test.tsx</automated>
  </verify>
  <done>One component renders the live reading and the stopped receipt, refuses `0 of 0`, discloses its elapsed anchor, and is silent on completed and failed.</done>
</task>

<task type="auto">
  <name>Task 3: Mount it at list level, and thread the one prop through</name>
  <files>frontend/src/components/chat/MessageList.tsx, frontend/src/components/chat/ChatArea.tsx, frontend/src/__tests__/components/chat/MessageList.runline.baseline.test.tsx</files>
  <read_first>
    - `frontend/src/components/chat/MessageList.tsx` (whole — 234 lines; Props at :11-22, the render tail at :180-232)
    - `frontend/src/components/chat/ChatArea.tsx` lines 545-562
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §3
  </read_first>
  <action>
Add `threadId?: string | null` to `MessageList`'s Props and mount
`<ThreadRunLine threadId={threadId ?? null} />` **after the messages map and before**
`<div ref={bottomRef} />` (:190), so the run line is at LIST level — not inside the transcript
(D-15) — and the auto-scroll anchor still lands at the true bottom.

In `ChatArea.tsx`, pass `threadId={thread?.id ?? null}` on the `<MessageList …/>` mount at :552-559.
That expression is already computed one screen up for `MessageInput` (:364); reuse the same shape.

⚠ **D-17 — the `showJumpToLive` block at `:192-231` and its `RunStatusStrip` are BYTE-UNTOUCHED.** It
is the floating jump-to-live chip, a different affordance, and the new line is its sibling. Assert
`git diff -U0` shows no changed line inside that block.

⚠ **PUBLISH THE D-03 STOP-CONDITION COUNTS, before and after, for both files** —
`grep -c 'useState[(<]'`, `grep -c 'useEffect('`, props-interface member count, `wc -l`, against
`194.1-BASELINE.md` §3 (MessageList 0/1/234, ChatArea 7/4/563). **`useState(` and `useEffect(` must be
UNMOVED on both**; the prop count on `MessageList` goes UP by one and that is the only movement
allowed. ⚠ `ThreadRunLine` legitimately owns a `useState`/`useEffect` for its own fetch and its own
tick — that is **not** a D-03 violation: D-03 forbids the **pressed state** living inside a **mount
component**, and this is neither. State that distinction explicitly in the SUMMARY so a reader does
not have to infer it.

Update plan 01's `MessageList.runline.baseline.test.tsx` captures IN PLACE. Two are legitimately
inverted (the `data-testid` absence, and the "Props carries no `threadId`" source fence). Quote each
original under a `SUPERSEDED` marker with its reason — never delete.
  </action>
  <acceptance_criteria>
    - `grep -c "<ThreadRunLine" frontend/src/components/chat/MessageList.tsx` is exactly 1
    - `git diff -U0 frontend/src/components/chat/MessageList.tsx` shows no changed line between the `showJumpToLive` open and its close
    - `useState(`/`useEffect(` UNMOVED on `MessageList.tsx` (0/1) and `ChatArea.tsx` (7/4), published in the SUMMARY
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — error count unmoved from §11
    - Both superseded baseline assertions carry their originals
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/components/chat/ && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>The line mounts at list level as a sibling of the shipped chip, one prop is threaded, and neither mount gained state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: R4 and R5's fences — the store-empty render and the no-dead-interval window</name>
  <files>frontend/src/__tests__/components/chat/MessageList.test.tsx</files>
  <read_first>
    - `frontend/src/__tests__/components/chat/MessageList.test.tsx` lines 1-60 and 94-97
    - `frontend/src/components/chat/__tests__/ComposerStopHarness.test.tsx` lines 78-112 (the mock bundle shape, for the provider-backed cases)
    - `frontend/src/__tests__/providers/StreamsProvider.stopping.test.ts` (plan 03's kickoff cases — the zero-assistant-node half is already proved there; do not duplicate it)
  </read_first>
  <behavior>
    - The stopped line renders in a bare `MessageList` render with NO `StreamsProvider` — the streaming store is empty by construction
    - It names the step count and a non-empty duration
    - It is absent for a `completed` thread and for a `failed` thread
    - At harness kickoff, the transcript carries the live line AND zero assistant nodes
    - On a slow-first-token mock there is no frame in which the transcript shows a prompt with no run instrument
  </behavior>
  <action>
Extend `frontend/src/__tests__/components/chat/MessageList.test.tsx` using the shipped `renderML()`
(bare `TooltipProvider`, **no `StreamsProvider`**) with `@/lib/api`'s `getThreadWorkflow` mocked. That
harness makes "the streaming store is empty" **structural** rather than arranged, which is what R4's
central acceptance requires. ⚠ Do **not** add a `StreamsProvider` to this suite; if a case genuinely
needs one, put THAT case in a provider-backed file and say in one line why it moved.

Cover: store-empty stopped render (with step count and a non-empty duration asserted as VALUES, not as
presence); absent on `completed`; absent on `failed`.

Then the two R5 halves, in a provider-backed suite:
 - **Kickoff:** drive `sendMessage` with `workflowDefinitionId` set and assert, in the SAME rendered
   tree, zero `role === "assistant"` nodes AND the run line present. Plan 03 proved the zero-node half
   at store level; this proves the two facts hold **together on screen**, which is what the acceptance
   actually claims ("so the artefact cannot be drawn twice **regardless of which of the three
   candidate mechanisms in `194-MEASUREMENTS.md` is live**").
 - **No dead interval:** with a slow-first-token mock, assert the run line is present at the frame
   immediately after the send and BEFORE any delta arrives. ⚠ Assert it **without advancing timers or
   flushing the fetch** — if the line only appears after a promise resolves, the interval exists and
   the case must red.

⚠ **Record the boundary rather than over-claiming.** R5 removes the surface the duplicate avatar was
drawn on; it does **not** repair the double-mount race. If the same race later reaches a
content-bearing message the artefact returns, and nothing here would have prevented it. Put that
sentence in the suite's docblock — sketch 171's own "NOT guaranteed" row, carried forward so a future
reader finds a boundary rather than a claim.

**The named plants, each observed RED:**
 - P8: derive the line from a live-store read → the store-empty case reds.
 - P9: render on any terminal status → the `completed` and `failed` cases red.
 - P10: mount the live arm on the first delta → the no-dead-interval case reds.
  </action>
  <acceptance_criteria>
    - The store-empty case renders through `renderML()` with no `StreamsProvider` and asserts the step-count and duration VALUES
    - Absent-on-`completed` and absent-on-`failed` are two separate cases
    - The kickoff case asserts zero assistant nodes AND the line's presence in one tree
    - The no-dead-interval case asserts presence WITHOUT advancing timers or flushing the fetch
    - The suite docblock records that the double-mount race is NOT repaired
    - P8, P9, P10 recorded in the SUMMARY as observed RED
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/components/chat/ src/__tests__/providers/StreamsProvider.stopping.test.ts</automated>
  </verify>
  <done>R4's persisted-state derivation is proved by a structurally empty store, D-16's two absences are tested rather than hoped, and R5's kickoff carries an instrument with no dead frame.</done>
</task>

<task type="auto">
  <name>Task 5: Gate the wave</name>
  <files>frontend/src/components/chat/ThreadRunLine.tsx</files>
  <read_first>
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §1, §2 and §11
    - `CLAUDE.md` § *Parallel execution* (the cap-2 rule and its two documented rots)
  </read_first>
  <action>
`cd frontend && npx tsc --noEmit -p tsconfig.app.json` (⚠ never the bare form — it checks ZERO files);
record the error count against §11.

`cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`; read the **verdict
line**; record `total` / `failed` / `pinned total` / verdict. ⚠ If `failed` is non-zero, CAPTURE THE
FAILING FILENAMES BEFORE RE-RUNNING ANYTHING.

⚠ **`WorkflowRunPage.test.tsx` is PINNED at 89 and this plan adds NO case to it** — the `fmtElapsed`
hoist is a verbatim move. Assert `git diff scripts/vitest-count-gate.cjs` is **EMPTY**: plan 07 owns
that entry and two concurrent edits to one object literal is a merge conflict with no `files_modified`
signal. If the gate reports a DECREASE on `WorkflowRunPage.test.tsx`, the hoist broke a case — fix the
case, do not lower the pin.

⚠ This plan ran in the same wave as plan 05. **Keep to ≤ 2 concurrent test-running agents** — at three
the gate goes non-deterministic regardless of cap.
  </action>
  <acceptance_criteria>
    - `tsc` error count recorded and unmoved from §11
    - Count gate verdict `OK`, `failed 0` — or every failing filename captured before any re-run
    - `git diff scripts/vitest-count-gate.cjs` is EMPTY
    - No per-file DECREASE on `WorkflowRunPage.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Typecheck unmoved, gate green, and the pin map left untouched for plan 07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /threads/{id}/workflow` | A new client call site for the shipped, owner-scoped, **ungated** read. The thread id comes from the app's own routing, never from user text. |
| rendered copy → the user | The line makes a claim about what happened to a run. A wrong claim here is the exact class of defect this phase exists to remove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194.1-06-01 | Information disclosure | reading another account's run status / step count / timings | transfer | The read is owner-scoped server-side (plan 02's `get_thread_workflow` gate, 404 never 403). This component adds no id it did not receive from the app's own routing and constructs no query |
| T-194.1-06-02 | Spoofing | an UNLABELLED elapsed figure — a number the reader will anchor on the wrong field | mitigate | The disclosure `title` names `created_at` and `updated_at` in words. The shipped rule: *"a queued-anchored figure is not a lie — an UNLABELLED one is"* (`WorkflowRunPage.tsx:770-773`) |
| T-194.1-06-03 | Spoofing | `0 of 3 steps` printed after a stop, reading as though nothing survived | mitigate | The derivation returns `null` for a zero denominator and the segment is omitted; P6 reds against printing it. 194 D-13 |
| T-194.1-06-04 | Spoofing | a live-only reading that vanishes on navigation | mitigate | The stopped arm reads the persisted row and NOTHING from the streaming store; P4/P8 red against a live-store derivation. 174 D1 — a live-only badge is *how* `BUG-260610-01` is two months old |
| T-194.1-06-05 | Spoofing | a seam offered to a canvas-only page while the canvas is off | mitigate | Strict `=== true` read, failing closed, per the Phase-148 vanish convention; both directions asserted |
| T-194.1-06-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** No date library, no new dependency — RESEARCH's *Don't Hand-Roll* row names `fmtElapsed` as the answer. A task reaching for one stops and escalates to a blocking human checkpoint |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `OK`, `failed 0`, no per-file DECREASE
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → unmoved from §11
- `grep -c "useStreamingForThread\|bucketsBySurface" frontend/src/components/chat/ThreadRunLine.tsx` → 0
- `useState(`/`useEffect(` unmoved on `MessageList.tsx` and `ChatArea.tsx`
- `git diff scripts/vitest-count-gate.cjs` → EMPTY
</verification>

<success_criteria>
- One component carries both states; the stopped receipt renders with the streaming store structurally empty
- Absent on `completed` and on `failed`, each with its own case (D-16 tested, not hoped)
- The step count is total over seven literals derived from migration 119, and `0 of 0` is refused
- The elapsed anchor is disclosed in words
- At kickoff: zero assistant nodes AND the live line, together in one tree, with no dead frame
- `fmtElapsed` hoisted verbatim; no fourth formatter; the pin map untouched
- P1-P10 each observed RED before its fix
</success_criteria>

<output>
Create `.planning/phases/194.1-make-the-stop-visible/194.1-06-SUMMARY.md` when done.
</output>
