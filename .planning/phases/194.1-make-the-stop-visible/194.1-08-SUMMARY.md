---
phase: 194.1-make-the-stop-visible
plan: 08
subsystem: phase-close-out
tags: [g-5, hot-file-ledger, validation-contract, deferral-register, roadmap, RUN-01, G-4, checkpoint]
requires:
  - "194.1-01..07 — all seven executed and merged; their SUMMARYs are this plan's only source for the RED column"
  - "194.1-BASELINE.md §7 — the WAVE-1 ledger triples this plan was required to RE-DERIVE, never trust"
provides:
  - "CLAUDE.md — EIGHT hot-file ledger rows: 5 added, 2 updated in place, 1 corrected BESIDE its old value"
  - "194.1-VALIDATION.md — the filled 38-row Per-Task Verification Map, 41 plants, zero NOT OBSERVED"
  - "194.1-DEFERRED.md — 15 entries, every one carrying a re-open trigger"
  - "ROADMAP.md — 8 plans marked, the G-5 flag corrected on measurement, the RUN-01 untick confirmed"
affects:
  - "every future /gsd:discuss-phase — five hot files that were INVISIBLE to the G-5 audit are now rows"
  - "the L-01 phase, which inherits a written boundary instead of a UAT surprise"
tech-stack:
  added: []
  patterns:
    - "a corrected figure recorded BESIDE its superseded value, never over it"
    - "the raw sed bucket recipe's non-phase buckets named and subtracted — the loser beside the winner"
    - "a needle named by ROLE when spelling it would make the file contain it"
key-files:
  created:
    - .planning/phases/194.1-make-the-stop-visible/194.1-DEFERRED.md
  modified:
    - CLAUDE.md
    - .planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md
    - .planning/ROADMAP.md
decisions:
  - "EIGHT ledger rows, not the six scoped — ChatArea.tsx (29 phases) and PendingAskCard.tsx (5) were G-5-firing, edited by this phase, ABSENT from CLAUDE.md, and missing from CONTEXT D-01's own audit"
  - "CONTEXT D-04's 'WorkflowRunPage on the count of 2 (G-5 does NOT fire)' quoted and corrected on measurement — 3 phases, G-5 FIRES"
  - "nyquist_compliant stays FALSE and 08-T5 is named — a phase whose bar is an operator's lived experience cannot be fully automated"
  - "The three young 1-phase files get a named prose paragraph, not rows, with that weakness stated explicitly"
  - "RUN-01 NOT ticked; REQUIREMENTS.md not opened"
metrics:
  duration: ~1h
  completed: 2026-08-16
  tasks: 5 of 5 (task 5 — the blocking G-4 checkpoint — DISCHARGED 2026-08-16 by the operator)
  files_created: 1
  files_modified: 3
  commits: 4
---

# Phase 194.1 Plan 08: The Close-Out — Summary

**Five hot files that G-5 could not see are now rows, one of them stale by twenty-eight phases; the
verification map is filled from the SUMMARYs with zero blank negative controls; every deferral carries
a trigger; and RUN-01 is explicitly NOT ticked.** Tasks 1–4 are complete and committed. **Task 5 is a
blocking human checkpoint and this plan stops there.**

## What shipped, task by task

| Task | Commit | What |
|---|---|---|
| 1 | `7b39dd5f` | Eight `CLAUDE.md` ledger rows — 5 added, 2 updated, 1 corrected beside its old value |
| 2 | `2ce289ad` | The 38-row Per-Task Verification Map, filled from the SUMMARYs |
| 3 | `ce016582` | `194.1-DEFERRED.md` — 15 entries, every one with a re-open trigger |
| 4 | `a2bc71ac` | ROADMAP + the final four-gate reading + the deploy-drift check |
| 5 | — | ✅ **DISCHARGED 2026-08-16** — all three G-4 rows driven by the operator and PASSED, plus both jsdom-undischargeable judgements. 5 of 5 rows, 5 PASS. Record: `194.1-UAT.md` (status `complete`) and `194.1-VALIDATION.md` § *CHECKPOINT DISCHARGED*. ⚠ RUN-01 remains UNTICKED by the same decision this plan recorded — the rows passing and the requirement being met are different statements. |

**Whole-plan numstat: 4 files, `git diff --diff-filter=D` EMPTY.** `.planning/STATE.md` and
`.planning/REQUIREMENTS.md` were never opened for writing, and **no `gsd-sdk query state.*`,
`requirements.mark-complete` or `roadmap.update-plan-progress` verb was invoked at any point** —
ROADMAP.md was hand-edited, as the prompt requires.

---

## The finding: G-5 fires on SEVEN files, not the three the ROADMAP predicted or the five D-01 found

Every triple was **re-derived at this commit**, not inherited. `194.1-BASELINE.md` §7 warned that its
own figures were a wave-1 reading and would be stale; **five of its six had moved.**

| File | ROADMAP flag | BASELINE §7 (wave 1) | **Re-derived at `14283ba8`** | On the ledger before? |
|---|---|---|---|---|
| `providers/StreamsProvider.tsx` | *not named* | 77 / 33 / 3660 | **82 / 34 / 4035** | ⚠ **row present and WRONG** |
| `components/chat/ChatArea.tsx` | *not named* | *not measured* | **61 / 29 / 587** | ❌ **absent entirely** |
| `components/chat/MessageInput.tsx` | ✔ predicted | 24 / 12 / 446 | **25 / 13 / 478** | ❌ absent (prose only) |
| `components/chat/MessageList.tsx` | *not named* | 18 / 7 / 234 | **19 / 8 / 267** | ❌ absent entirely |
| `components/panel/PendingAskCard.tsx` | *not named* | *not measured* | **10 / 5 / 629** | ❌ absent entirely |
| `components/panel/WorkspacePanel.tsx` | ✔ predicted | 14 / 9 / 580 | **16 / 10 / 646** | ✔ present — updated in place |
| `pages/WorkflowRunPage.tsx` | *not named* | 10 / **2** / 1046 | **12 / 3 / 1101** | ❌ absent entirely |
| `components/chat/RunCard.tsx` | ✔ predicted | 21 / 9 / 608 | **21 / 9 / 608 — unmoved** | ✔ present — *checked, unmoved* |

**The worst row was the one that was already there.** `StreamsProvider.tsx` read
`068 / 075 / 075.4 / 075.6 / 075.7 (5+)` · *"satisfied (075.7 — 2026-05-24)"* while measuring **34
phases** — ⚠ **stale by twenty-eight phases, on the largest file in the frontend tree.** An auditor
reaching that row concludes the guardrail was honoured in May and **stops looking**; an absent row at
least forces a re-derivation. **The old text is kept verbatim beside the correction**, because the
point of the row is that it was wrong.

### ⚠ Deviation: EIGHT rows, not the six the plan scoped — and the reason is the plan's own objective

`ChatArea.tsx` (**29 phases** — the second-hottest frontend module in the tree) and
`PendingAskCard.tsx` (**5 phases**) are both G-5-firing, both **edited by this phase**, both **absent
from `CLAUDE.md` entirely**, and **neither appears in `194.1-CONTEXT.md` D-01's own eight-file G-5
audit.** Leaving them out would have been precisely the failure this plan exists to fix, so both were
added under deviation **Rule 2** (missing critical functionality).

**The audit that exists to catch invisible hot files was itself reading a short list.** `194.1-05`'s
SUMMARY flagged `PendingAskCard.tsx` for plan 08 and that flag is what carried it; **`ChatArea.tsx`
was flagged by nobody** and was found only by measuring every file the phase touched rather than every
file the plan named.

### D-04 corrected on measurement, its original left standing

CONTEXT **D-04** instructs that `WorkflowRunPage.tsx`'s row be written *"on the count of 2 (G-5 does
NOT fire)"*. **That was true when D-04 was written and false by the time the row could be written** —
this phase's own two commits are the third bucket. `194.1-07` measured `12 / 3 / 1101` and explicitly
warned plan 08 not to follow D-04 verbatim. **The row is written on the measurement, states BOTH
readings so neither can be quoted as the other, and quotes D-04's original.**

### The declined override — verified, not assumed

```
grep -n "Guardrail overrides" .planning/STATE.md   →  :8  :75  :402  :535  :686  :874  :1729
```

`.planning/STATE.md:1729` carries a dedicated section reading **"Phase 194.1 — NONE. G-5 fired on
FIVE files and an override was OFFERED AND DECLINED at `/gsd:discuss-phase 194.1`"**, with the
by-construction claim and its D-03 stop condition. ⚠ **This is STRONGER than the absence the plan's
acceptance criterion anticipated** (*"showing no Phase-194.1 entry"*): there IS an entry, and it
records the decline explicitly, so *"honoured by construction"* is distinguishable from *"nobody
checked"* by a positive statement rather than by silence. **194.1 is the FIFTH consecutive phase to
honour G-5 by construction** (193, 193.1, 193.2, 194, 194.1). ⚠ The STATE.md section says *"FIVE
files"*; measured, it is **seven** — recorded here rather than edited, because STATE.md is not this
plan's to write.

---

## The plan-count decision — 8 plans, with 7 evaluated and REJECTED on context cost

Recorded because the operator set it as a gate. **The phase shipped in EIGHT plans against a directive
of *at most 8, fewer if honest*.** Recent comparable phases ran 11 (193.1), 13 (194) and 29 (187).

**Seven was genuinely evaluated**: merging the panel plan (05) and the run-surface plan (07), which
would additionally have removed the count-gate-pin serialization **by construction** — one plan owning
both `BASELINE` entries cannot conflict with itself.

**It was rejected on CONTEXT COST, not on difficulty.** A merged plan's `<read_first>` spans
`WorkflowRunPage.tsx` (1046 L), `WorkspacePanel.tsx` (580 L), `PendingAskCard.tsx` (486 L) and
`ActiveRunsTray.tsx` (141 L) **plus** their four suites, one of which carries 89 cases. That exceeds a
single agent's context budget, which is one of the three legitimate reasons to split.

**Both levers the directive named were taken:**
- **R4 and R6 share ONE read** — plan 02 serves both, and D-09b then measured that **R6 needed no
  backend work at all** (`last_workflow_run_id` + `194-11`'s dual-id fallback are shipped end to end).
- **R4 and R5 are ONE component in two states** — plan 06's `ThreadRunLine`, live and stopped.

⚠ **One correction against RESEARCH's Q8, recorded beside the original:** Q8 proposed the same eight
plans but placed **the ledger rows in plan 01**. They were moved to plan 08, and the reason is a rule
worth keeping: **a ledger row must POSTDATE the change to be accurate, while a characterization
baseline must PREDATE it.** Had the rows been written in wave 1 they would have recorded
`77 / 33 / 3660` for a file that finished the phase at `82 / 34 / 4035` — five of the six triples moved.

---

## The final gate — all four, on a QUIET tree

| Check | Result | Against |
|---|---|---|
| `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` | **`count gate OK`** · total **3972** · failed **0** · pinned total **3898** · **75/75** present · **no per-file DECREASE** | matches plan 07's post-bump reading exactly |
| `npx tsc --noEmit -p tsconfig.app.json` | **33** | **unmoved** from `194.1-BASELINE.md` §11 |
| `cd backend && venv/Scripts/python -m pytest tests/unit -q` | **62 failed / 2251 passed** / 2 xfailed / 2 xpassed | **exactly** plan 02's recorded figures — **no new failures** |
| `bash scripts/check-deploy-drift.sh` | **`RESULT: PASS`**, exit 0 | 2 pre-existing non-blocking WARNs |

⚠ **`total 3972` is larger than the `3918` `CLAUDE.md` quotes and larger than BASELINE §11's `3954`.
That is the gate WORKING** — its contract is *no per-file DECREASE* and *zero failing*, never a fixed
grand total.

⚠ **The deployment-artifact exemption was RUN, not asserted**, per the task's instruction. Both WARNs
are pre-existing and neither is this phase's: migrations above #089 carrying seed-like statements (a
standing review item since Phase 118) and `docker compose` being unavailable in this shell (CI runs
the authoritative parse). **This phase adds no env var, no seed-bearing migration, no bundled service
and no sandbox-image tag change** — verified against the phase's own 45-file diff.

⚠ **A measurement that points AGAINST an open deferral, recorded because a register that only records
confirmations is not a register:** plan 06 logged `WorkflowsPage.test.tsx` failing non-deterministically
in the gate (`7 / 0 / 5 / 1` on a frozen tree), with a re-open trigger of *"a failure observed on a
QUIET tree with no sibling executor"*. **This close-out gate ran on exactly such a tree and read
`failed 0`.** That does **not** close the entry — the trigger is a *failure*, and one clean run is not
proof of absence for an intermittent condition — but it is consistent with the oversubscription
reading. The entry stays open with its trigger unchanged.

---

## The verification map — 41 plants, zero blank cells, zero `NOT OBSERVED`

Every plant named by plans 01–07 appears in the Negative-control column, **sourced from the SUMMARY
that recorded the observation**, never re-derived from plan text. **Blank cells: 0. `NOT OBSERVED`: 0.**

**Eight cells carry an explicit "what stayed GREEN" clause**, because *a plant that reds everything
proves less than one that reds exactly its own case*. The sharpest:

- **P5 (plan 03)** — with `expect(cancelRun).toHaveBeenCalled()` alone the suite was **fully green and
  a wrong-id landmine invisible**; only the VALUE assertion red it.
- **P1 (plan 04)** — the presence-only assertion `getByText(COPY_STOPPING)` **stayed GREEN** under a
  plant that rendered the control and the reading together. The whole argument for asserting ABSENCE.
- **P1 (plan 05)** — the no-Stop half of a case stayed GREEN while its timeline half red, proving the
  single-assertion version would have shipped the Phase-098 regression.
- **P5b (plan 05)** — the four-way set-size clause **stayed GREEN at 4** under a partial sentence
  reuse; only the shared-complete-sentence clause fired. The 193.2 shape, reproduced.
- **P1 (plan 07)** — `✓ Complete` **stayed GREEN** under a `completed`-only gate while `⊘ Cancelled`
  and `✕ Failed` red. The five-cases-not-two argument, measured.
- **P8 (plan 03)** — both honesty arms went **GREEN** under a hide-it-with-CSS implementation, which
  is why R5's acceptance counts NODES rather than measuring visibility.

**Two honest qualifications were kept rather than rounded up:** plan 02's **P1** cannot fire as
literally worded in a mocked-pool test (a SQL-projection case was added to make it fireable, rather
than record a plant credited with catching something it cannot see), and plan 07's **P3b** fired on
the press case rather than the no-guard case — **an ordering artefact, explicitly not a second
independent detection**.

### The sign-off: 5 ticked, 3 explicitly NOT ticked

| Item | State |
|---|---|
| Every fence has a named plant, observed RED | ✅ 41 plants, zero `NOT OBSERVED` |
| Every source fence prints its swept length, non-empty | ✅ |
| Sampling continuity | ✅ 37 of 38 rows carry a command |
| Wave 0 complete | ✅ all eight items |
| No watch-mode flags | ✅ |
| Both count-gate pins bumped with their cases | ✅ `717b5b92` (41→58), `c567a5a6` (89→102) |
| **All tasks have an `<automated>` verify** | ⛔ **NOT TICKED — `08-T5` has none and cannot** |
| **The three G-4 rows driven** | ⛔ **NOT TICKED — all three OWED and UNDRIVEN** |
| **`nyquist_compliant: true`** | ⛔ **NOT SET — left `false`, with `08-T5` named** |

⚠ **`nyquist_compliant` stays FALSE deliberately.** The criterion is *every task row carries an
`<automated>` command*; `08-T5` is the blocking G-4 checkpoint, manual by definition (CLAUDE.md G-4:
wire format and screenshots are *insufficient*). **A phase whose acceptance bar is an operator's lived
experience cannot be fully automated, and rounding the flag up would be exactly the dishonesty this
phase exists to remove.**

---

## Deviations from Plan

### 1. [Rule 2 — missing critical functionality] Two extra ledger rows

Covered above. `ChatArea.tsx` and `PendingAskCard.tsx`. **The plan's `must_haves` name "six ledger
rows"; eight shipped, and the two extras satisfy the same must_have truth** (*"Every hot file this
phase touched is VISIBLE to the G-5 audit"*) that six would have left false.

### 2. [Rule 1 — a fence that red on itself] The map's own verify command became its own needle

`08-T2`'s automated command is `! grep -q "<the planner's placeholder phrase>" 194.1-VALIDATION.md`.
**Transcribing it verbatim into the map made the file contain the needle** — `grep -c` returned **1**,
the sole hit being the cell quoting the command.

⚠ **This is the phase's own recurring lesson landing in its close-out plan**, after five instances in
plans 02, 03, 04 and 06 (*a raw sweep reds on the prose documenting the thing it sweeps for*). Fixed
the way this repository already prescribes — **the needle is named by ROLE and deliberately not
spelled** (the `toolMeta.ts` / 187-24 convention, and the `run_lifecycle.py` app-shutdown-gate
discipline). Re-measured: **0**. ⚠ **A later editor who writes the literal phrase back into that cell
breaks a real fence silently.**

### 3. [Correction] The ROADMAP block already carried `**Plans**: 8 plans in 6 waves`

The task text says to *"replace the `**Plans**: TBD` line"*. **There is no `TBD` line** — the
plan-writing step had already filled it. What was actually owed was marking the eight entries `[x]`
and stating the G-4 debt, both done. Recorded rather than silently skipped.

### 4. [Self-inflicted, caught and fixed before commit] I duplicated the RUN-01 paragraph

My ROADMAP replacement included a copy of the `⚠ RUN-01 STAYS UNTICKED` paragraph that already existed
below the block. **Removed my duplicate and extended the original in place** — the correct shape, since
the original is the record and a second copy is a second thing to keep in sync.

### 5. [Scope, stated] Three young files get a prose paragraph, not rows

`StopControl.tsx` (`3 / 1 / 315`), `ThreadRunLine.tsx` (`1 / 1 / 357`) and `ActiveRunsTray.tsx`
(`2 / 1 / 164`) are all **1 phase** — G-5 does not fire. They are named in a paragraph after the table
so a `grep` finds them, **with the weakness stated explicitly in that same paragraph**: *"present but
only in prose" is precisely the state a row-scanning audit cannot see.* **The moment any reaches a
second phase, whoever touches it owes a real row** (the `library/WorkflowCard.tsx` precedent).

### No deviations of note

**No package was installed** (T-194.1-08-SC honoured) — this plan edits documentation only.
`.planning/STATE.md` and `.planning/REQUIREMENTS.md` were never opened for writing.

---

## What `.planning/STATE.md` should say (this plan does NOT write it)

Handed to the orchestrator rather than written, per the prompt:

1. **Phase 194.1 executed — 8 plans, 6 waves, all merged.** Tasks 1–4 of plan 08 complete; **plan 08
   task 5 is a BLOCKING G-4 checkpoint and the three rows are OWED and UNDRIVEN.**
2. **RUN-01 stays UNTICKED.** `REQUIREMENTS.md` is byte-unchanged across the whole phase (numstat
   EMPTY) and four SUMMARYs state the untick independently. The tick is the operator's.
3. ⚠ **The `Guardrail overrides` entry at `:1729` says "G-5 fired on FIVE files". Measured at close it
   is SEVEN** — `ChatArea.tsx` (29 phases) and `PendingAskCard.tsx` (5) were found by plan 08. The
   decline still stands and is still honoured by construction; only the count is stale.
4. **The L-01 phase is OWED and should be inserted next** — see `194.1-DEFERRED.md` §1. It is the only
   thing standing between this phase and RUN-01.
5. **Gate at close:** count gate `OK` / 3972 / 0 / 3898 / 75-75 · `tsc` 33 · backend 62 failed
   (pre-existing) / 2251 passed · deploy-drift PASS.
6. ⚠ **The wrong-base worktree failure was 7/7 this phase** — every executor arrived on `fda79214`, a
   `master` merge with no descent from its dispatched SHA, and every one caught it only because the
   prompt made the assertion explicit. Phase 192 measured 12/12. **Keep asserting the base.**

---

## Known Stubs

None. This plan wrote documentation only; every figure it records was produced by a command recorded
beside it.

## Threat Flags

None. No route, no query, no schema, no client-visible behaviour changed. `git diff --name-only`
names four files, all documentation.

**Threat register disposition:** T-194.1-08-01 (a hot file invisible to G-5) — **mitigated, and more
broadly than scoped**: eight rows, each verified as a table ROW rather than prose. T-194.1-08-02 (a
stale row answering `satisfied`) — **mitigated**: corrected beside its old value with the 28-phase
staleness stated plainly. T-194.1-08-03 (a green fence never seen red) — **mitigated**: 41 plants
sourced from SUMMARYs, `NOT OBSERVED` permitted and unused. T-194.1-08-04 (a deferral in a deleted
line) — **mitigated**: 15 entries, every one with a trigger, three additionally carried in production
files. T-194.1-08-05 (RUN-01 read as satisfied) — **mitigated**: untick stated on the ROADMAP with its
reason, `REQUIREMENTS.md` untouched, and the tick handed to the operator at task 5.
T-194.1-08-SC — **no package installed.**

---

## Self-Check: PASSED

**Files claimed created/modified — all present on disk:**

```
FOUND: CLAUDE.md
FOUND: .planning/ROADMAP.md
FOUND: .planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md
FOUND: .planning/phases/194.1-make-the-stop-visible/194.1-DEFERRED.md
```

**Commits claimed — all four resolve in `git log --all`:**

```
FOUND: 7b39dd5f  docs(194.1-08): EIGHT G-5 ledger rows — the stale StreamsProvider row corrected BESIDE its old value
FOUND: 2ce289ad  docs(194.1-08): fill the Per-Task Verification Map — 38 rows, 41 plants, zero NOT OBSERVED
FOUND: ce016582  docs(194.1-08): the deferral register — 15 entries, every one with a re-open trigger
FOUND: a2bc71ac  docs(194.1-08): ROADMAP — 8 plans marked, the G-5 flag corrected on measurement, RUN-01 untick confirmed
```

**Mechanical acceptance, re-run at close:**

```
eight files, grep -c "<file>" CLAUDE.md              →  all ≥ 1
exactly one ROW per file (grep -c "^| `<path>`")     →  1, 1, 1, 1, 1, 1, 1, 1
StreamsProvider row contains "(5+)" AND the triple   →  1 and 1  (BOTH_PRESENT)
grep -l 'folded_into: "194.1"' … | wc -l             →  4        (FOLD_OK)
every ## section of 194.1-DEFERRED.md has a trigger  →  no gaps
"planner fills" in 194.1-VALIDATION.md               →  0
git diff --numstat 14283ba8 HEAD -- .planning/STATE.md         →  EMPTY
git diff --numstat 14283ba8 HEAD -- .planning/REQUIREMENTS.md  →  EMPTY
git diff --diff-filter=D --name-only 14283ba8 HEAD             →  EMPTY (no file deleted)
```

**No `.planning/STATE.md` or `.planning/REQUIREMENTS.md` write**, and **no `gsd-sdk query state.*` /
`requirements.mark-complete` / `roadmap.update-plan-progress` verb was invoked at any point.**

---

## ⛔ Task 5 is NOT complete — the checkpoint is open

The three operator-driven **G-4** rows (**Press · Canvas · Return**) are **OWED and UNDRIVEN**, and
the **RUN-01 tick decision is the operator's**. Both are recorded as owed in `194.1-VALIDATION.md`,
never as run. ⚠ **Row 2 requires `visual_workflow_canvas` ON** — it ships **off**, and driving it off
measures the gate rather than the feature. ⚠ **The L-01 behaviour must be read to the operator BEFORE
row 1**, or a truthful `⊘ Stopping this run…` landing on `✓ Complete` will be filed as a defect of
this phase when it is the known boundary this phase made visible.
