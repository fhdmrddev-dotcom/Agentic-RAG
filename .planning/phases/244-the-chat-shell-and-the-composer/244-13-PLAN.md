---
phase: 244-the-chat-shell-and-the-composer
plan: 13
type: tdd
wave: 3
# G-7's detector (scripts/check-gap-closure-rounds.cjs) keys on `gap_closure`, NOT on
# `gap_closure_round`. Measured while authoring this plan set: with only the round field
# present the gate read `0 gap-closure` and a future round 2 would never have been capped.
gap_closure: true
gap_closure_round: 1
depends_on: ["244-11", "244-12"]
files_modified:
  - frontend/src/stores/streamsStore.ts
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx
  - frontend/src/components/chat/__tests__/ThreadRunLineKickoff.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/phases/244-the-chat-shell-and-the-composer/deferred-items.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-13-UAT-ROW.md
autonomous: true
requirements: [SHELL-02]

must_haves:
  truths:
    - "A cap-paused DEEP thread renders NO live workflow run line — no '* Starting workflow…', no live clock, for a run that is stopped and a workflow that never existed."
    - "A cap-paused DEEP thread renders the DEEP pre-tools banner, not the harness one — and mounts no component that fires a getThreadWorkflow fetch."
    - "A genuine harness run STILL renders its live run line, its harness banner and its advancing phase progress — the fix must not trade the phantom for a missing one."
    - "A genuine harness run that is itself cap-paused keeps the composer LOCKED (WR-07) — the composer may never invite a send the server answers with 409."
    - "A Deep cap-pause still unlocks the composer and still shows the Continue-limit sentence — SHELL-02's shipped half is preserved exactly."
    - "Every file this plan names has a hot-file ledger row, including the two that have never had one."
  artifacts:
    - path: "frontend/src/stores/streamsStore.ts"
      provides: "WorkflowLock.mode widened to a real discriminator — the lock says what it IS"
      contains: "cap_paused"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "every lock write site setting the discriminator from the server's own mode"
    - path: "frontend/src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx"
      provides: "RED-driven proof that a Deep cap-pause unlocks and a harness cap-pause does NOT"
  key_links:
    - from: "backend GET /threads/{id}/workflow (mode: deep|harness)"
      to: "frontend WorkflowLock.mode"
      via: "the reconcile branches in ChatArea.tsx and StreamsProvider.tsx"
      pattern: "cap_paused"
    - from: "frontend/src/stores/streamsStore.ts WorkflowLock.mode"
      to: "useHarnessLiveForThread / ThreadRunLine / MessageItem's banner branch"
      via: "a mode test replacing a presence test"
      pattern: "mode === \"harness\""
---

<objective>
Close **G-1** and the open review finding **WR-07** with one change, because they are the same
defect from two directions: **the thread lock does not say what it is.**

**G-1, driven 2026-09-12 while running L-2.** On a Deep (General-mode) thread whose latest run is
`cap_paused`, the transcript renders a LIVE harness run receipt that contradicts the card 41px
above it. Both on screen, in one `getBoundingClientRect` pass:

    amber card, top=272px:  "Reached the Continue limit — this run is stopped. Start a new
                             message to keep going."
    run line,   top=313px:  "* Starting workflow…"   data-run-line-state="live"

**There is no workflow. The thread has never had one.** The line also runs a 1 s `setInterval`
clock (`ThreadRunLine.tsx:243-249`) for a run that is stopped.

Cause, measured: `244-03` made the server populate the thread lock for a cap-paused DEEP run
(`threads.py:1237-1276`) — the lock used to be harness-only. But `useHarnessLiveForThread`
(`StreamsProvider.tsx:4568-4573`) still reads **presence**:

    s.harnessKickoffThreads.has(threadId) || s.workflowLockByThread.has(threadId)

`ThreadRunLine.tsx:207` consumes that as `isLive`, takes the STATE-1 LIVE arm (`:257-274`) and calls
`outerBannerLabel(..., isHarness=true)` (`:187-191`), whose pre-tools value is the harness string
(`toolMeta.ts:197`). **A 244-03 server change reached a consumer written against the old invariant.**

**Second consumer, same cause:** `MessageItem.tsx:787` branches
`workflowLock != null ? <HarnessOuterBanner/> : <Deep span>`, and its own docblock (`:214-215`)
asserts *"only when `workflowLock != null` => a DEEP thread never mounts it, so the Deep path costs
zero fetches and zero subscriptions"*. **244-03 made that sentence FALSE.** Beyond the wrong copy,
`HarnessOuterBanner` calls `usePhases`, which mounts `usePanelReconcile` and fires a
`getThreadWorkflow` **FETCH** — the exact cost the comment claims the Deep path does not pay
(`T-194-07-03`'s disposition).

**WR-07 (`244-REVIEW.md`, open) is the same hole from the other side:**
`ChatArea.tsx:140`'s `workflowLock !== null && !workflowLock.capPaused` unlocks the composer for a
GENUINE harness run that is itself cap-paused. `workflow_runs.status = 'cap_paused'` is schema-valid
(`063_dual_mode_continue.sql:57`) and non-terminal, so the server can answer
`locked: true, cap_paused: true` today; the person would type, send, and get
**409 "Thread is workflow-locked"** — *"the UI instructs an action it forbids"*, one branch over
from the inversion this phase exists to remove. ⚠ No current writer of that value on
`workflow_runs` was found, so it is **latent, not firing** — say so, and fix it anyway, because
`D-244-08` asked for both conjuncts and only one was implemented.

⭐ **One fix for both:** `WorkflowLock.mode` is currently the single-member literal `"harness"`
(`streamsStore.ts:89`) and is **HARD-CODED `"harness"` at every write site, including the Deep
cap-pause branch** — which is why `C-1` measured `D-244-08`'s first proposal to be a no-op. Make it
a real discriminator, set it from the server's own `mode`, and replace three PRESENCE tests with
MODE tests.

⛔ **This round does not re-open SHELL-05** (L-7 passed both ways) **and does not re-open the parts
of SHELL-02 that passed** — L-2's six steps and L-3's genuine-harness lock are green and must stay
green; they are this plan's anti-regression half, not its target.

**Solo run (D-244-21 / OV-SOLO-01):** no independent reviewer. SELF-verification only.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then `git rev-parse HEAD` —
MUST be a descendant of `760c758fc` on `develop` **including the merges of `244-09`, `244-10`,
`244-11` and `244-12`**. This plan edits `StreamsProvider.tsx` and `ChatArea.tsx` (owned by
`244-11` in wave 2) and `MessageItem.tsx` (owned by `244-12`), so a base missing either merge will
produce a conflict that looks like a defect. ⚠ A fresh worktree starts on the DEFAULT branch
(`master`, at the v3.9 ship commit) — assert the SHA before reading anything.
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

**⚠ THIS PLAN RUNS ALONE IN ITS WAVE.** The tree should be quiet for its gate runs. ⚠ Phase 244
already measured another writer active in this working tree during `244-08`; if `git status` shows
files this plan did not touch, **say so in the SUMMARY** — a gate run while another agent writes the
same tree measures both of you.

**⚠ Gate state, inherited:**
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`, **from the repo root**. Contract:
  *no per-file DECREASE* + *zero failing*. A bigger grand total is the gate WORKING.
- ⛔ Capture failing FILENAMES from the gate's own persisted JSON **BEFORE** any re-run; check each
  against `git diff --numstat`. `SEED-171`'s five flaky suites are cap-independent and three fail
  with a plain `AssertionError`. ⛔ Never touch the cap.
- ⛔ `npx tsc --noEmit` in `frontend/` checks ZERO files. Use
  `npx tsc -p tsconfig.app.json --noEmit` with a **SET DIFF** against the ~67-error base.
  ⭐ For THIS plan the typechecker is a primary instrument: widening a literal union surfaces every
  site that assumed the single member. **Read the set diff as a worklist, not only as a verdict.**

**⚠ Registry files** (`scripts/vitest-count-gate.cjs`, `docs/HOT-FILE-LEDGER.md`, `CLAUDE.md`) are
append-only and this plan is alone in its wave, so no hand re-application is owed — but re-run
`node scripts/check-hot-file-ledger.cjs 244` and `node scripts/check-claude-md-size.cjs` from the
repo root after the merge regardless.

**⚠ G-2 does not fire.** Bug fix on a shipped surface, causes named at `file:line` (`D-244-18`).
</execution_context>

<context>
@.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-REVIEW.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md
@.planning/phases/244-the-chat-shell-and-the-composer/deferred-items.md
@CLAUDE.md

⛔ **Read the `G-1` YAML block at the bottom of `244-UAT.md` in full**, including
`second_consumer_same_cause`, `relation_to_known_findings` and `why_no_fence_caught_it` (*"the
invariant that broke lived in a COMMENT, not in an assertion"* — this plan's job is to move it into
an assertion).
⛔ **Read `244-REVIEW.md` § WR-07** end to end, including its reachability paragraph and its
proposed fix, and `244-CONTEXT.md` **D-244-08 / D-244-09 / D-244-10** plus `244-PATTERNS.md` **C-1**
and **C-2** — C-1 is the measurement that killed the obvious version of this fix.

@frontend/src/stores/streamsStore.ts
@frontend/src/providers/StreamsProvider.tsx
@frontend/src/components/chat/ChatArea.tsx
@frontend/src/components/chat/ThreadRunLine.tsx
@frontend/src/components/chat/MessageItem.tsx
@docs/HOT-FILE-LEDGER.md

<interfaces>
The type as shipped (`streamsStore.ts:51-95`) — `mode` is a ONE-MEMBER literal, which is exactly
why `C-1` measured `D-244-08`'s proposed gate to be a no-op:

    export interface WorkflowLock {
      runId: string
      mode: "harness"
      capPaused: boolean
      continuesRemaining: number
    }

**Every write site of `mode`, measured at planning — there are SIX, and five hard-code `"harness"`:**

| # | site | today | what it actually is |
|---|---|---|---|
| 1 | `StreamsProvider.tsx:1193` — the `onCapPaused` SSE | `"harness"` | either, depending on the run |
| 2 | `StreamsProvider.tsx:2311` — reconcile, `wf.locked` branch | `"harness"` | genuinely harness |
| 3 | `StreamsProvider.tsx:2331` — reconcile, `wf.cap_paused` branch | `"harness"` | usually a DEEP cap-pause |
| 4 | `StreamsProvider.tsx:2507` — kickoff seed (`opts.workflowDefinitionId`) | `"harness"` | genuinely harness |
| 5 | `ChatArea.tsx:204` — reconcile, `state.locked` branch | `"harness"` | genuinely harness |
| 6 | `ChatArea.tsx:234` — reconcile, `state.cap_paused` branch | `"harness"` | **the phantom's source** |

`StreamsProvider.tsx:3762` (the producer re-subscribe) spreads `...existing` and therefore
PRESERVES whatever mode is already set — ⛔ it must keep doing exactly that.

**The server already computes the answer** (`threads.py:1195`, on the wire as
`ThreadWorkflowState.mode`, `models/thread.py:361`):

    mode = "harness" if active_workflow_run_id is not None else "deep"

The three PRESENCE tests that must become MODE tests:

    StreamsProvider.tsx:4568-4573   s.harnessKickoffThreads.has(threadId) || s.workflowLockByThread.has(threadId)
    MessageItem.tsx:787             workflowLock != null ? <HarnessOuterBanner/> : <span className="italic">…</span>
    ChatArea.tsx:140                workflowLock !== null && !workflowLock.capPaused        // WR-07

⚠ **`WorkflowLock.mode` has ZERO production consumers today** — measured with
`grep -rn "\.mode ===" frontend/src`: every hit is a test fixture writing `mode: "harness"` (which
stays valid after the widening) or an unrelated `builder.mode === "edit"`. That is what makes
widening the union a contained change rather than a sweep.

The two ledger blind spots, measured with the gate itself:

    node scripts/check-hot-file-ledger.cjs --files frontend/src/lib/toolMeta.ts frontend/src/stores/streamsStore.ts
    => [no-row] frontend/src/lib/toolMeta.ts        (10 commits / 6 phases / 218 lines — G-5 FIRES)
    => [no-row] frontend/src/stores/streamsStore.ts (19 commits / 12 phases / 484 lines — G-5 FIRES)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — a DEEP cap-pause claims to be a live workflow, and a harness cap-pause unlocks the composer</name>
  <files>frontend/src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx, frontend/src/components/chat/__tests__/ThreadRunLineKickoff.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — the `G-1` block and test row 1 (L-2), including its `method_caveat` (the cap state was SEEDED, and what that does and does not prove)
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-REVIEW.md` § WR-07
    - `frontend/src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx` — the whole file, once
    - `frontend/src/components/chat/__tests__/ThreadRunLineKickoff.test.tsx` — the whole file, once
    - `frontend/src/components/chat/ThreadRunLine.tsx:180-280`
  </read_first>
  <behavior>
    - **Test D1 (the phantom — MUST be RED):** a thread whose lock is the DEEP cap-pause shape (the
      one `ChatArea.tsx:234` writes: a `runId` from `latest_producer_run_id`, `capPaused: true`,
      and the server reporting `mode: "deep"`) renders **no** element with
      `data-run-line-state="live"` and **no** "Starting workflow…" text. ⛔ Assert on the rendered
      CONTENT and the state attribute together — *"presence assertions cannot see content drift"*.
    - **Test D2 (no phantom clock):** in that same state, **no** 1 s interval is registered.
      Use fake timers and assert the render is stable across an advance of several seconds, or spy
      on `window.setInterval`. ⛔ A stopped run must not tick.
    - **Test D3 (the Deep banner, and its zero-fetch promise):** in that same state,
      `MessageItem`'s pre-tools banner renders the DEEP string and `HarnessOuterBanner` is **not**
      mounted — assert **zero** `getThreadWorkflow` calls. ⚠ This is `T-194-07-03`'s disposition,
      which 244-03 silently voided; it belongs in an assertion now, not in a comment.
    - **Test D4 (a genuine harness run is UNHARMED):** a real harness lock (the shape
      `StreamsProvider.tsx:2311` / `ChatArea.tsx:204` writes, server `mode: "harness"`) still
      renders the live run line, still takes the harness banner, and still advances with phases.
      ⛔ The anti-regression half — a fix that silences the phantom by silencing the real one is
      not a fix.
    - **Test D5 (WR-07 — MUST be RED):** a lock that is BOTH genuinely harness AND cap-paused keeps
      the composer **DISABLED**, with the shipped placeholder and title
      `"Workflow running — Cancel to switch back"` on both axes. ⛔ The composer may never invite a
      send that `preflight_workflow_kickoff:174` answers with 409.
    - **Test D6 (SHELL-02's shipped half is preserved):** the DEEP cap-pause still ENABLES the
      composer with placeholder `"Ask anything…"`, and the Continue-limit sentence still renders.
      ⚠ ⛔ **The ROADMAP's named anti-fix is deleting that sentence** — this case is what stops it
      happening by accident.
  </behavior>
  <action>
Add D1-D4 to `ThreadRunLineKickoff.test.tsx` (it already owns the run-line surface) and D5-D6 to
`ChatArea.capPausedComposer.test.tsx`.

⚠⚠ **`ThreadRunLineKickoff.test.tsx` IS IN NEITHER GATE KNOB — measured at planning.**
`grep -n "ThreadRunLineKickoff" scripts/vitest-count-gate.cjs` returns nothing, so the suite is not
in `TARGETS` and has no `BASELINE` pin: **it guards nothing today.** Register it in **BOTH** knobs
as part of this task, and record the adoption in the SUMMARY. ⭐ Adopting a suite RAISES the grand
total — that is the desirable direction and must not be read as drift; attribute the `+N` exactly.

⛔⛔ **RE-AIM the shipped fixtures, do not delete them, and expect some to go RED — that is the
finding, not a problem.** `ChatArea.capPausedComposer.test.tsx` currently represents the DEEP
cap-pause with `mode: "harness" as const, capPaused: true` (`:127`, `:279`), because that is what
the product writes today. After the discriminator lands, that fixture no longer describes a Deep
cap-pause; it describes exactly WR-07's harness-cap-pause state. **Update each fixture to the shape
the product will write, and add a docblock line recording what it used to say and why it changed.**
⛔ The per-file case count must not DECREASE.

⛔ **Run and SEE D1 and D5 RED**, quoting both failures verbatim. Record the measured colour of
D2/D3/D4/D6 at RED time — ⚠ D3's fetch count in particular is data, and if it is already non-zero
on the Deep path that is the second consumer firing exactly as the gap describes.

⛔ `git diff -- frontend/src/stores/streamsStore.ts frontend/src/providers/StreamsProvider.tsx frontend/src/components/chat/ChatArea.tsx frontend/src/components/chat/MessageItem.tsx`
must be **EMPTY** at the end of this task.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/ThreadRunLineKickoff.test.tsx src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx` runs; **D1 and D5 FAIL** with both failures quoted verbatim in the SUMMARY.
    - The SUMMARY records the measured colour of D2/D3/D4/D6 and D3's `getThreadWorkflow` call count on the Deep path.
    - `grep -c "ThreadRunLineKickoff.test.tsx" scripts/vitest-count-gate.cjs` returns exactly `2` — the suite is newly in BOTH knobs.
    - No case was deleted from either suite: both per-file counts are `>=` their pre-edit values (pre-edit: `ThreadRunLineKickoff` 4 cases and NO pin; `ChatArea.capPausedComposer` pinned at 6).
    - `git diff -- frontend/src/stores/streamsStore.ts frontend/src/providers/StreamsProvider.tsx frontend/src/components/chat/ChatArea.tsx frontend/src/components/chat/MessageItem.tsx` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/ThreadRunLineKickoff.test.tsx src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx</automated>
  </verify>
  <done>D1 and D5 are RED against unmoved production code; an ungated suite is adopted into both knobs; no fixture was deleted.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — the lock says what it IS, and three presence tests become mode tests</name>
  <files>frontend/src/stores/streamsStore.ts, frontend/src/providers/StreamsProvider.tsx, frontend/src/components/chat/ChatArea.tsx, frontend/src/components/chat/MessageItem.tsx</files>
  <read_first>
    - `frontend/src/stores/streamsStore.ts:42-95` — the `WorkflowLock` declaration and the two-id landmine JSDoc it carries
    - `frontend/src/providers/StreamsProvider.tsx` — ONLY `:1188-1200`, `:2300-2345`, `:2500-2515`, `:3755-3770`, `:4560-4575`
    - `frontend/src/components/chat/ChatArea.tsx` — ONLY `:112-142` and `:196-245`
    - `frontend/src/components/chat/MessageItem.tsx` — ONLY `:196-232` (the `HarnessOuterBanner` docblock) and `:778-795` (the branch)
    - `backend/app/models/thread.py:349-380` — the wire contract (`mode: Literal["deep","harness"]`)
  </read_first>
  <action>
**(a) Widen the discriminator (`streamsStore.ts`).** Change `mode: "harness"` to
`mode: "harness" | "cap_paused"` and document it AT THE TYPE, in the style that file already uses
for the two-id landmine — because *"a measurement that lives in one consumer's comment is invisible
to the next phase"* is this exact file's own recorded lesson, and G-1 is that lesson recurring:
- `"harness"` = a non-terminal workflow run owns this thread's anchor;
- `"cap_paused"` = a DEEP run paused at its iteration cap; **there is no workflow**, so nothing may
  read this lock as evidence of a live harness run.
⚠ Existing test fixtures writing `mode: "harness"` stay valid — they describe harness locks — which
is why the union is widened rather than replaced.

**(b) Set it from the server's own answer, at all six write sites.** ⛔ Do not infer it from
`capPaused`:
- sites **2, 4, 5** (the `wf.locked` / kickoff / `state.locked` branches) — `"harness"`, explicitly;
- sites **3 and 6** (the `cap_paused`-only reconcile branches) — derive from the wire:
  `wf.mode === "harness" ? "harness" : "cap_paused"`. ⚠ The server already computes this at
  `threads.py:1195`; ⛔ do not recompute it client-side from `active_workflow_run_id`, which would
  be a second derivation of one fact and is how these two registers drifted in the first place;
- site **1** (the `onCapPaused` SSE) — the event carries no mode, so **inherit**: read the thread's
  existing lock and keep `"harness"` if it already holds one, else `"cap_paused"`. Record that rule
  in the comment as an inference with its reason, not as a fact off the wire;
- `StreamsProvider.tsx:3762` — ⛔ leave byte-unchanged; `...existing` already preserves mode.

**(c) Three presence tests become mode tests:**
- `useHarnessLiveForThread` (`:4568-4573`): replace `s.workflowLockByThread.has(threadId)` with a
  read of that thread's lock and a `mode === "harness"` test. ⛔ **It must still return a
  BOOLEAN**, never the lock object — that file's own comment records why (Zustand's default
  equality is `Object.is`; a fresh object re-renders every subscriber on every unrelated write, and
  handing back the record leaks the `runId` two-id landmine). Keep the `harnessKickoffThreads`
  disjunct exactly as is: it covers the synchronous pre-lock kickoff window.
- `MessageItem.tsx:787`: `workflowLock?.mode === "harness" ? <HarnessOuterBanner/> : <Deep span>`.
  ⛔ Then **repair the docblock at `:214-215`**, which 244-03 made false. Record the correction
  BESIDE the original sentence rather than overwriting it, in this project's house style: the
  invariant *"a DEEP thread never mounts it, so the Deep path costs zero fetches"* was FALSE from
  244-03 until now, and the assertion that keeps it true is Test D3 — not this comment.
- `ChatArea.tsx:140` (**WR-07**): `workflowLock !== null && workflowLock.mode === "harness"`.
  ⚠ Record beside `C-1`'s existing note that `D-244-08`'s original proposal
  (`workflowLock?.mode === "harness"`) was a no-op **only because the type had one member and every
  writer hard-coded it** — and that widening the type is what makes the decision's first arm
  correct after all. ⛔ Keep C-1's measurement visible; it was right when written.
  ⛔ **Nothing else in the composer chain changes**: `MessageInput.tsx:287/337-339` key `canSend`,
  the placeholder, the `title` and `disabled` off this ONE boolean, so `D-244-10` ("the harness copy
  is untouched") holds by construction rather than by a second branch.

⚠ **Use the typechecker as a worklist.** Run `npx tsc -p tsconfig.app.json --noEmit` immediately
after step (a) and before step (c): widening a literal union surfaces every site that assumed the
single member. Any site the table above does not name is a **finding** — record it rather than
absorbing it quietly.

⚠ **`MessageItem.tsx` was G-5-DISCHARGED at Phase 227.** Report `useState` / `useEffect` / prop
counts before and after; all three must be unmoved.

Run D1-D6 green, then the surrounding shipped suites: `MessageItem.capPaused.test.tsx`,
`MessageItem.harnessBanner.test.tsx`, `MessageList.runline.baseline.test.tsx`,
`ComposerStopHarness.test.tsx`, `StopControl.baseline.test.tsx`, `PhaseReconcile.test.tsx` — each
constructs a `mode: "harness"` lock and each is a genuine consumer of this behaviour. Report every
count.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/ThreadRunLineKickoff.test.tsx src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx src/components/chat/__tests__/MessageItem.capPaused.test.tsx src/components/chat/__tests__/MessageItem.harnessBanner.test.tsx src/components/chat/__tests__/ComposerStopHarness.test.tsx src/__tests__/components/chat/MessageList.runline.baseline.test.tsx` — `failed 0`, every count reported.
    - `grep -c 'mode: "harness"' frontend/src/providers/StreamsProvider.tsx frontend/src/components/chat/ChatArea.tsx` — the combined count DROPS by exactly `2` (sites 3 and 6 become derived), and the two derived expressions are quoted in the SUMMARY.
    - `grep -c 'cap_paused' frontend/src/stores/streamsStore.ts` returns `>= 1` — the union is widened at the type.
    - `useHarnessLiveForThread` still returns a boolean: `grep -A6 "export const useHarnessLiveForThread" frontend/src/providers/StreamsProvider.tsx` shows a `: boolean` return type and contains neither a bare `.get(threadId)` return nor `runId`.
    - `grep -c "workflowLock != null ?" frontend/src/components/chat/MessageItem.tsx` returns `0`; `grep -c 'workflowLock?.mode === "harness"' frontend/src/components/chat/MessageItem.tsx` returns `1`.
    - `grep -c "a DEEP thread never mounts it" frontend/src/components/chat/MessageItem.tsx` returns `1` — the original sentence is PRESERVED, with the correction recorded beside it.
    - `grep -c "useState" frontend/src/components/chat/MessageItem.tsx` and `grep -c "useEffect" frontend/src/components/chat/MessageItem.tsx` are byte-identical before and after; both numbers quoted.
    - `git diff -- frontend/src/providers/StreamsProvider.tsx | grep -c "producerRunId"` returns `0` — the re-subscribe site (`:3762`) is byte-unchanged.
    - `npx tsc -p tsconfig.app.json --noEmit` — SET diff against the base capture EMPTY both ways, and the SUMMARY lists any site the interfaces table did not predict.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/ThreadRunLineKickoff.test.tsx src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx src/components/chat/__tests__/MessageItem.capPaused.test.tsx src/components/chat/__tests__/MessageItem.harnessBanner.test.tsx</automated>
  </verify>
  <done>The lock carries a real discriminator set from the server's own mode; the phantom run line, the phantom clock, the wrong banner, the stray fetch and WR-07 are all closed by one change.</done>
</task>

<task type="execute">
  <name>Task 3: Two files G-5 could never fire on, the deferrals, and the round's board</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md, .planning/phases/244-the-chat-shell-and-the-composer/deferred-items.md, .planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md, .planning/phases/244-the-chat-shell-and-the-composer/244-13-UAT-ROW.md</files>
  <read_first>
    - `docs/HOT-FILE-LEDGER.md` §§ `StreamsProvider.tsx`, `ChatArea.tsx`, `MessageItem.tsx`, `ThreadRunLine.tsx` and the § "Scan list" table
    - `CLAUDE.md` — the G-5-FIRING table rows for those files, and the ledger's own re-derivation recipe
    - `.planning/phases/244-the-chat-shell-and-the-composer/deferred-items.md` — the whole file; it is APPEND-ONLY and a later entry goes at the BOTTOM
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-REVIEW.md` — WR-04, WR-08 and IN-01..IN-09 (which of these are already recorded in `deferred-items.md`)
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — the `## Gaps` section and the `## Summary` block
  </read_first>
  <action>
**(a) Two ledger rows that have never existed.** Measured with the gate itself at planning time:

    node scripts/check-hot-file-ledger.cjs --files frontend/src/lib/toolMeta.ts frontend/src/stores/streamsStore.ts
    => [no-row] frontend/src/lib/toolMeta.ts         (10 / 6 / 218 — G-5 FIRES)
    => [no-row] frontend/src/stores/streamsStore.ts  (19 / 12 / 484 — G-5 FIRES)

`streamsStore.ts` is in this plan's `files_modified`, so the gate will fail without its row.
⚠ **`toolMeta.ts` is NOT modified by this plan and gets a row anyway** — G-1's own artifact list
names it (`toolMeta.ts:197` supplies the harness string the phantom rendered), it fires G-5 at six
phases, and an absent row is invisible to its own guardrail **at any commit count, forever,
silently**. ⛔ The reason this is worth a row rather than a note: the file the phase's criterion
turned on being outside the audit is precisely what happened to `NavPanel.tsx` for eleven phases,
and to `App.tsx` for twenty-three.

Add BOTH rows to `CLAUDE.md` § "Scan list" **and** both sections to `docs/HOT-FILE-LEDGER.md`, **in
the same commit**, re-deriving each triple with the recipe (subtract six-digit dated buckets).
Disposition cells <= 200 chars; reasons in the sections. For `toolMeta.ts`, name the invariant that
matters: **it is the single home of the harness activity string, called rather than copied by
`ThreadRunLine.liveWord` and by `MessageItem`'s banner, so a literal copied anywhere else makes its
pin vacuous** (the 187-24 lesson `toolMeta.ts:160-173` already records at length).

Also re-derive and update the rows for `StreamsProvider.tsx`, `ChatArea.tsx`, `MessageItem.tsx` and
`ThreadRunLine.tsx`. ⚠ `ThreadRunLine.tsx` measured `1 / 1 / 357` at the phase's start and this
round makes it **phase 2**; its ledger prose says it owes a detail section at its THIRD phase —
record that it is now one phase away, so the next plan naming it cannot be surprised.

**(b) The deferrals, each with a concrete re-open trigger.** APPEND to the BOTTOM of
`deferred-items.md` (⛔ never rewrite it). Every item below is a real finding this round chose not
to take, and each needs a trigger a later reader can actually fire — *a `trigger_when` nobody reads
is a deferral with no re-open, which is a deletion that looks like a decision*:

1. **L-5 defect 6a — the sent-message chip is LATE, not absent.** ⚠ RETRACTED and re-scored MINOR
   during the UAT itself; the chip appears in the settled transcript, so `D-244-22`'s obligation IS
   met. Deferred because a person who attaches and immediately re-reads their own message sees no
   scope word for the duration of the run. ⚠ Likely the same mechanism as **IN-09**
   (client-stamped message time vs Postgres-stamped file time). **Re-open trigger:** the next plan
   whose `files_modified` names `ChatAttachmentChip.tsx` or `useComposerAttachments.ts`.
2. **The Google-native Drive file cannot be attached** (`Unsupported type (none)`). Two problems:
   native Docs/Sheets/Slides are the most common thing in a Drive, and the copy leaks the
   extension check's internal state. ⚠ The Library ingest path DOES handle them (Drive read/EXPORT
   pins in `app/security/egress.py`), so the composer door is missing an export step the other door
   already has — ⛔ **stated as the likely asymmetry to check, NOT as a measured fact.**
   **Re-open trigger:** the next phase touching the composer's cloud door or
   `backend/app/api/workspace.py`'s `_ALLOWED_EXT`.
3. **A Library import answered HTTP 200 and created nothing** — NOT REPRODUCED; the response body
   was not captured. **Re-open trigger:** any further Library import that answers 200 without
   producing a `documents` row — **capture the response BODY that time.**
4. **`Retry-After: 10` is still discarded** (`244-11` makes the failure visible but adds no
   automatic retry, deliberately — an auto-retry is a new behaviour, not a gap fix).
   **Re-open trigger:** a phase that adds any automatic retry to the snapshot path.
5. **WR-08** — the transcript's `Read <file>` line renders in Explorer/harness turns where the
   agent could not have read the file. The mode gate is a code fix; the wording change is an
   OPERATOR call because the strings are ported from `COPY.js`. **Re-open trigger:** the next plan
   naming `MessageItem.tsx:455-465` or `composerCopy.ts`.
6. **WR-04** — the cloud attach buffers the whole provider file before the 10 MB cap is applied.
   **Re-open trigger:** the next plan naming `backend/app/api/workspace.py`.
⚠ Cross-check IN-01..IN-09 against what `deferred-items.md` items 10-13 already record and ⛔ **do
not duplicate an entry** — add only what is genuinely missing, and say which you checked.

**(c) The round's board.** Consolidate the five per-plan row files
(`244-09-UAT-ROW.md` … `244-13-UAT-ROW.md`) into a new `## Round 2 — gap-closure re-drive rows`
section at the END of `244-UAT.md`. ⛔ **Append only — do not edit the existing rows, the `## Gaps`
YAML blocks or the `## Summary` block**, whose recorded corrections and retractions are the phase's
most valuable record. Each row keeps its `pending` verdict and names which gap it closes
(G-1..G-6). This plan runs ALONE in its wave, which is why the consolidation is safe to do here.

**(d) This plan's own browser row.** Create `244-13-UAT-ROW.md`, unfilled:
- **Arm 1 (G-1 closed):** reproduce L-2's cap-paused Deep thread — ⚠ the UAT's `method_caveat`
  applies: no `cap_paused` run has EVER existed organically in this local DB (measured: 1293
  completed / 171 failed / 53 cancelled / 8 timed_out / **0 cap_paused**), so the state is SEEDED by
  setting a real run's row to `status='cap_paused'`, `continues_used=3` plus the `role='system'`
  carrier — **the exact rows `agent_loop.persist_cap_paused` writes**. ⛔ Record that it is seeded.
  ⛔ **What it therefore does NOT prove: that the cap FIRES correctly** — that is Phase 092's
  criterion, not SHELL-02's. Then assert, in ONE `getBoundingClientRect` pass: the Continue-limit
  card is present, and **no** element carries `data-run-line-state="live"`.
- **Arm 2 (the real one still works):** start a genuine workflow run; the live run line renders,
  advances, and the composer is DISABLED with `"Workflow running — Cancel to switch back"` on BOTH
  the placeholder and the title (L-3's exact reading — this is what must not be traded away).
- **Arm 3 (SHELL-02's shipped half):** on the cap-paused Deep thread, after a REAL reload
  (`performance.getEntriesByType('navigation')[0].type === "reload"`), the composer is enabled with
  `"Ask anything…"`, posting starts a NEW run that streams, navigating away and back does not
  re-lock, and the Continue-limit sentence is still on screen.
  ⚠ L-2's harness note applies: a first attempt recorded no new run because the viewport changed
  and the click landed outside the textarea. **Drive by element ref and assert `textarea.value`
  BEFORE pressing Return** — a mis-click published as a defect is what this project keeps paying for.
⛔ Every verdict field reads `pending`.
  </action>
  <acceptance_criteria>
    - `node scripts/check-hot-file-ledger.cjs --files frontend/src/lib/toolMeta.ts frontend/src/stores/streamsStore.ts` reports **no** `[no-row]` and exits `0`.
    - `node scripts/check-hot-file-ledger.cjs 244` exits `0` AND reports a non-zero `subject:` file count — ⛔ a run reading `subject: 0 files` passed vacuously (Phase 242 measured exactly that on a CRLF plan).
    - `grep -c "frontend/src/lib/toolMeta.ts" CLAUDE.md` and `grep -c "frontend/src/stores/streamsStore.ts" CLAUDE.md` each return exactly `1`.
    - `docs/HOT-FILE-LEDGER.md` contains exactly one section per file for both new entries, added in the SAME commit as their rows.
    - `node scripts/check-claude-md-size.cjs` exits `0` — no `[disposition-too-long]`, `[duplicate-row]` or `[malformed-row]`.
    - `deferred-items.md` grew only at the BOTTOM: `git diff -- .planning/phases/244-the-chat-shell-and-the-composer/deferred-items.md` shows additions only, zero deletions.
    - Each of the six deferrals names a concrete re-open trigger naming a FILE or an observable event — not "a future phase".
    - `244-UAT.md` grew only at the end: `git diff` shows zero deletions and zero modifications to the `## Gaps` YAML blocks.
    - All five per-plan row files are referenced from the Round 2 section, each tagged with the gap id it closes, and none carries a `pass` verdict.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-hot-file-ledger.cjs 244 &amp;&amp; node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <done>Two files that were invisible to G-5 now have rows and sections; six findings are deferred with triggers a reader can fire; the round's board is consolidated without touching a single recorded correction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server `ThreadWorkflowState.mode` -> client lock discriminator | A server-computed mode now drives whether the composer is locked and whether a run is presented as live. |
| composer enablement -> `POST /threads/{id}/messages` | An enabled composer is an invitation; the server may still refuse with 409. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-13-01 | Spoofing | `ThreadRunLine` / `HarnessOuterBanner` | mitigate | A run line may claim "live" only when the lock's `mode === "harness"`. D1/D2/D3 assert no live state, no clock and no harness banner for a Deep cap-pause; D4 asserts the genuine harness reading is unchanged. **A UI that reports a run nobody started is a spoof of the system's own state.** |
| T-244-13-02 | Elevation of Privilege | `ChatArea.workflowLocked` (WR-07) | mitigate | `mode === "harness"` keeps the composer locked for a harness run that is itself cap-paused, so the UI cannot invite a send the server answers 409. ⚠ Latent today — no writer of `cap_paused` onto `workflow_runs.status` was found — and fixed anyway, because `D-244-08` asked for both conjuncts. |
| T-244-13-03 | Denial of Service | `usePhases` -> `getThreadWorkflow` on the Deep path | mitigate | D3 asserts **zero** `getThreadWorkflow` calls for a Deep cap-paused thread. This is `T-194-07-03`'s disposition, voided by 244-03 and restored here **as an assertion rather than a comment** — which is `why_no_fence_caught_it`'s whole point. |
| T-244-13-04 | Tampering | `WorkflowLock.runId` two-id landmine | accept (unchanged) | `useHarnessLiveForThread` still returns a BOOLEAN and still never hands back the lock object; the `:3762` re-subscribe is byte-unchanged. The `194-03` F-1 fence (no Stop mount may read `runId` or call `cancelRun(` directly) is untouched and must stay green. |
| T-244-13-05 | Repudiation | the ledger's two blind spots | mitigate | Rows + sections for `toolMeta.ts` and `streamsStore.ts` in one commit, verified by the gate reporting no `[no-row]`. An absent row makes G-5 absent forever, silently — the `NavPanel.tsx` (11 phases) and `App.tsx` (23 phases) failure. |
| T-244-13-SC | Tampering | npm/pip/cargo installs | n/a | **No package is installed.** `git diff -- frontend/package.json frontend/package-lock.json` MUST be empty; no legitimacy audit is owed. ⚠ If one becomes necessary, STOP — the audit table comes first. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the **repo root**: `failed 0`,
  no per-file DECREASE, every `+N` attributed with no residual. ⭐ The grand total will rise by more
  than this plan's new cases because `ThreadRunLineKickoff.test.tsx` is being ADOPTED — attribute
  the adoption separately from the new cases so the two are not confused.
  ⛔ Capture failing filenames from the gate's own JSON BEFORE any re-run; check each against
  `git diff --numstat`. ⛔ Never adjust the cap.
- `npx tsc -p tsconfig.app.json --noEmit` — SET diff against the base capture empty both ways, and
  every site the widening surfaced is listed in the SUMMARY (predicted or not).
- `node scripts/check-hot-file-ledger.cjs 244` (non-vacuous parse) and
  `node scripts/check-claude-md-size.cjs` — both exit 0 from the repo root.
- ⛔ **The backend is untouched:** `git diff --stat -- backend/ supabase/` EMPTY, so the
  zero-headroom backend baseline (71) is not at risk and is not re-run for this plan.
- ⛔ **NOT verified here, stated as owed:** `244-13-UAT-ROW.md` and the four sibling row files are
  driven at `/gsd:verify-work`. **This plan may not report SHELL-02 as closed.**
- ⚠ If `git status` shows files this plan did not touch, report it — Phase 244 already measured
  another writer in this tree, and a gate run on a noisy tree measures both agents.
</verification>

<success_criteria>
- `WorkflowLock.mode` is a real discriminator, set from the server's own `mode` at every write site,
  and documented at the TYPE rather than in one consumer's comment.
- D1 and D5 were seen RED; D1-D6 green; the genuine harness reading is provably unchanged.
- Three presence tests became mode tests; `useHarnessLiveForThread` still returns a boolean;
  `MessageItem` is not re-hollowed and its false docblock is corrected beside its original.
- `ThreadRunLineKickoff.test.tsx` is adopted into both gate knobs, with its adoption attributed
  separately from new cases.
- `toolMeta.ts` and `streamsStore.ts` have ledger rows and sections; six findings are deferred with
  fireable triggers; the Round 2 board is consolidated append-only.
- The SUMMARY says **built, drive owed** for SHELL-02 — never closed — and says SELF-verified.
</success_criteria>

<output>
Create `.planning/phases/244-the-chat-shell-and-the-composer/244-13-SUMMARY.md` when done.
</output>
