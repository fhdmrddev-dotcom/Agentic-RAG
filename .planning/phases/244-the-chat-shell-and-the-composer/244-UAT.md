---
status: complete
phase: 244-the-chat-shell-and-the-composer
source:
  - 244-01-SUMMARY.md
  - 244-02-SUMMARY.md
  - 244-03-SUMMARY.md
  - 244-04-SUMMARY.md
  - 244-05-SUMMARY.md
  - 244-06-SUMMARY.md
  - 244-07-SUMMARY.md
  - 244-08-SUMMARY.md
started: 2026-09-12T00:00:00Z
updated: 2026-09-12T16:30:00Z
---

<!--
  Rows are 244-VALIDATION.md's own owed rows, ordered by 244-VERIFICATION.md's
  stated priority (L-2 first, then L-1, then the rest). Solo run (D-244-21 /
  OV-SOLO-01): this board is SELF-VERIFIED, never "reviewed".
  A row may be BLOCKED with a reason — never silently omitted.
-->

## Current Test

[testing complete — all 9 rows driven]

## Tests

### 1. L-2 — the cap-paused composer, AFTER A RELOAD
expected: Deep run driven to its iteration cap (Continue card visible). After a real page reload the composer is enabled, placeholder "Ask anything…"; posting starts a NEW run that streams; navigating away and back does not re-lock; the "Reached the Continue limit…" sentence is still rendered.
closes: SHELL-02 (half), BUG-260904-05 (half)
result: pass
driven_by: Claude (Chrome MCP), 2026-09-12, local dev (fe :5173 / be :8000)
fixture: thread 68818c2a-ddde-4ce6-9945-f87e0ac12d1c ("Ready"), run 05fea6e9-824e-4060-8b5c-ca50a5136be3
method_caveat: |
  ⚠ THE CAP STATE WAS SEEDED, NOT ORGANICALLY DRIVEN, and that is recorded rather than hidden.
  A genuine cap fire needs 15 tool-calling iterations AND the model to disobey tool_choice="none"
  at the final one (agent_loop.py:2104 force_no_tools + :2575 `if force_no_tools and
  tool_calls_buffer`). No cap_paused run has EVER existed in this local DB (measured: runs by
  status = completed 1293 / failed 171 / cancelled 53 / timed_out 8, zero cap_paused).
  So: a REAL Deep run was driven in the browser, then its runs row was set status='cap_paused',
  continues_used=3 and the role='system' carrier row inserted — the exact rows
  agent_loop.persist_cap_paused writes. Everything downstream of that value (threads.py's read,
  the thread lock, ChatArea's boolean, the POST path) is untouched product code.
  ⛔ What this row therefore does NOT prove: that the cap FIRES correctly. That is Phase 092's
  criterion, not SHELL-02's.
observations:
  - step: 1 (cap state present)
    result: seeded — Continue card renders, continuesRemaining 0
  - step: 2 (real reload, not a soft re-render)
    result: PASS — performance.getEntriesByType('navigation')[0].type === "reload" (full document
      teardown; the row's wording asks for "navigate" — reload is the stronger boundary, recorded
      verbatim rather than reworded). A separate away-and-back navigation is step 5.
  - step: 3 (composer enabled, placeholder)
    result: PASS — measured textarea.disabled=false, readOnly=false, aria-disabled=null,
      placeholder="Ask anything…". A 12s/109-sample 200ms sampler across thread-open showed
      disabled=false on EVERY sample; the Continue sentence appeared at 4231ms.
  - step: 4 (post → NEW runs row, streams)
    result: PASS ⭐ — run 7c7d424a-a184-4cc3-95f2-36ea7384ed04 minted at 07:00:49Z and reached
      'completed'; the answer streamed into the transcript. THIS IS D-244-09's LOAD-BEARING
      UNVERIFIED CLAIM ("drive it; do not reason about it") — now verified TRUE.
    harness_note: |
      ⚠ A first attempt recorded no new run. That was a HARNESS MISS, not a product defect —
      the viewport changed (1568x710 → 1536x639) and the click at (880,596) landed outside the
      textarea, so nothing was typed. Re-driven by element ref with textarea.value asserted
      ("Say the single word: second") BEFORE pressing Return. Recorded because a mis-click
      published as a defect is exactly what this project keeps paying for.
  - step: 5 (navigate away and back — lock does not return)
    result: PASS — Chat → Library → Chat → reopen thread: disabled=false,
      placeholder="Ask anything…", no Continue card, no run line. The lock did NOT return.
  - step: 6 ("Reached the Continue limit…" sentence still on screen)
    result: PASS — rendered verbatim while capPaused held; the ROADMAP's named anti-fix
      (deleting the sentence) has NOT been taken.
found_while_driving: gap G-1 below (phantom live workflow run line) — a NEW defect this row
  surfaced, logged separately rather than folded into this verdict.

### 2. L-1 — the frame holds, MEASURED at 6 samples
expected: On a long transcript, at 3 viewport heights x panel CLOSED and OPEN — the page root does not overflow (scrollHeight <= clientHeight), a LEAF element inside the nav rail has an unchanged bounding rect before/after scrolling, and the composer's bottom is within 1px of the viewport's usable bottom. All 6 samples hold. No app code calls scrollIntoView during the run.
closes: SHELL-01, BUG-260828-08
result: issue
reported: "Driven by Claude in Chrome after the operator un-maximised the window. 2 of the 6 samples FAIL: below a viewport height of ~540px the page ROOT overflows and the whole page scrolls — the ROADMAP's own named failure mode, and BUG-260828-08's exact complaint."
severity: major
driven_by: Claude (Chrome MCP), 2026-09-12
fixture: thread 261d5f57-36fb-40ec-bb0b-1c72b7550350 ("UAT 243 L-2 — long thread"), 78 messages
samples: |
  | Sample | Viewport h | Panel | root sh / ch | root overflow | composer gap | footer gap | rail top | Verdict |
  |---|---|---|---|---|---|---|---|---|
  | L-1-a | 436 | CLOSED | 540 / 436 | **+104px** | 37.0 | 12.0 | 162 | ⛔ FAIL |
  | L-1-d | 436 | OPEN   | 540 / 436 | **+104px** | 37.0 | 12.0 | 162 | ⛔ FAIL |
  | L-1-b | 576 | CLOSED | 576 / 576 | 0 | 37.0 | 12.0 | 162 | ✅ pass |
  | L-1-e | 576 | OPEN   | 576 / 576 | 0 | 37.0 | 12.0 | 162 | ✅ pass |
  | L-1-c | 696 | CLOSED | 696 / 696 | 0 | 37.0 | 12.0 | 162 | ✅ pass |
  | L-1-f | 696 | OPEN   | 696 / 696 | 0 | 37.0 | 12.0 | 162 | ✅ pass |

  Threshold bisected: h=516 already overflows by 24px; h=576 is clean. `rootScrollHeight` is
  PINNED AT 540px at every viewport, so the failure is "viewport shorter than 540".
  ⭐ PANEL STATE IS IRRELEVANT — open and closed are byte-identical at every height. That is
  itself diagnostic: the overflowing element is not in the panel or the message column.
rail_and_scroll_properties_all_pass: |
  Measured with a REAL trusted scroll (a synthetic WheelEvent moved the list 0px and was
  discarded), with the list asserted to have actually moved before reading the rail, and with
  Element.prototype.scrollIntoView instrumented for the whole run:
    · h=696 panel CLOSED — list moved -1600px, rail Δ dTop 0 / dLeft 0, scrollIntoView calls 0
    · h=695 panel OPEN   — list moved +1000px, rail Δ dTop 0 / dLeft 0, scrollIntoView calls 0
    · h=695 panel CLOSED — list moved +1000px, rail Δ dTop 0 / dLeft 0, scrollIntoView calls 0
    · h=436 panel CLOSED — list moved +2000px, rail Δ dTop 0 / dLeft 0, scrollIntoView calls 0
  ⇒ "the rail does not move while the messages scroll" HOLDS everywhere, including where the
  root overflows. The defect is not rail drift; it is that the rail cannot FIT.
correction_recorded_not_overwritten: |
  ⚠ An earlier reading of this row logged `composerGap 106.6px` at h=436 and called the gap
  "grown". THAT WAS AN ARTIFACT OF MY OWN MEASUREMENT, not a product fact: because the root
  overflows at that height, my scroll had scrolled the DOCUMENT, so the composer's rect was
  read against a scrolled root. Re-measured unscrolled, the composer gap is **37.0px at every
  one of the 6 samples** and the footer gap 12.0px at all 6 — both perfectly stable.
  ⛔ The composer gap is NOT the failing measure. `rootOverflowBy` is the only one that moves,
  and the original wrong number is kept here rather than deleted.
root_cause: |
  MEASURED to the element, then confirmed in source.
  The overflowing element is THE NAV RAIL, not the transcript and not the panel.
    · Rail root — `NavPanel.tsx:205` — "hidden md:flex flex-col h-full shrink-0 ... py-3".
      Computed: height = viewport, **overflow-y: visible, min-height: auto**. It has NO
      `overflow-y-auto` and NO `min-h-0`, so it can neither scroll nor clip its own content.
    · Its bottom block — `NavPanel.tsx:290` — "flex flex-col gap-1 mt-auto" (Organization admin
      / Control Room / user row), height 128px, measured bottom **540px** — i.e. 24px BELOW the
      rail's own 516px box at h=516. With overflow visible, that escapes to the document root.
    · Walking the parent chain confirms nothing between that block and <html> clips: every
      ancestor reads overflow-y: visible, and `div.flex.h-screen.bg-background` is height-
      pinned to the viewport, so the excess has nowhere to go but the page scrollbar.
  ⭐ THE POINT: 244-01 applied its `min-h-0` chain at 5 sites — ChatLayout.tsx:803,809;
  ChatArea.tsx:501,584; MessageList.tsx:219 — ALL in the MESSAGE column. The sibling RAIL
  column was never touched. The workspace `<aside>` DOES carry `min-h-0` (measured:
  "flex h-screen min-h-0 min-w-0 flex-col overflow-hidden"). The rail is the one column in the
  shell that got neither treatment.
  ⚠ And `NavPanel.tsx` has NO hot-file ledger row — CLAUDE.md records it "absent from BOTH for
  its ENTIRE LIFE at 11 phases". A file invisible to G-5 was the one the phase's own criterion
  turned on.
instrument_correction_recorded_not_overwritten: |
  ⚠⚠ THE FIRST INSTRUMENT WAS CONTAMINATED BY MY OWN TOOLING AND THE FINDING WAS RE-DRIVEN
  BEFORE IT WAS ALLOWED TO STAND. The table above originally read `document.scrollingElement`
  (= documentElement). That is WRONG in this browser: the automation extension injects its own
  nodes into <body> (`claude-agent-glow-border`, `claude-phantom-cursor`, plus a `scite-extension-
  marker`), and `documentElement.scrollHeight` measured **1522** at a viewport of 696 — a number
  that has NOTHING to do with the app. Caught when a later reading claimed an 826px overflow at a
  height that had just passed.
  ⛔ THE CORRECT INSTRUMENT IS THE APP'S OWN CONTAINER: `document.getElementById('root')
  .scrollHeight` vs its own rect height (`document.body.scrollHeight` agrees, because the
  extension's nodes are position:fixed or zero-height and contribute nothing to it).
  Re-driven on the corrected instrument, every figure in the table above is REPRODUCED:
    · h=436 → appRootSh 540 / appRootH 436 → **overflow +104px**  (bodySh 540 agrees)
    · h=576 → appRootSh 576 / appRootH 576 → 0                    (bodySh 576 agrees)
    · h=696 → appRootSh 696 / appRootH 696 → 0                    (bodySh 696 agrees)
  ⭐ So the defect is REAL and the 540px floor is the APP's, not the harness's — and the
  "second instance at h=696 with a tall panel" that I briefly believed in was PURELY my own
  measurement error and is NOT a finding. It is written down here rather than quietly dropped.
fix_shape: |
  `min-h-0` + `overflow-y-auto` on the rail root (NavPanel.tsx:205), so the rail scrolls its own
  content instead of pushing the document. ⛔ Do NOT add a min-height to the page — that makes
  the page scroll deliberately, which is the bug.
  ⛔ A fence must assert the PAGE ROOT does not overflow at a SHORT viewport (< 540px). jsdom
  performs no layout, so this cannot be a vitest fence; it needs the browser drive, which is why
  the criterion is written this way.

### 3. L-3 — SHELL-02 negative: a genuine harness run still locks
expected: Start a real workflow run. The composer is DISABLED and both the placeholder and the title attribute read "Workflow running — Cancel to switch back".
closes: SHELL-02 (half), BUG-260904-05 (half)
result: pass
driven_by: Claude (Chrome MCP), 2026-09-12
fixture: workflow "200-Word Essay Writer" (published), real run 7524dab9-e2de-4118-8b1b-8dfc3c9d3a06, thread 0d8fda6d-69c8-4270-a358-8541c410e1c4
evidence: |
  Measured on the live thread while the harness run was active, read off the element rather
  than from a screenshot:
    textarea.disabled                = true
    textarea.placeholder             = "Workflow running — Cancel to switch back"
    textarea.title                   = "Workflow running — Cancel to switch back"
    nearest [title] ancestor's title = "Workflow running — Cancel to switch back"
  ⇒ D-244-10's copy is untouched and BOTH the placeholder and the title carry it, which is what
  the row asks for. The genuine harness lock still locks — so 244-03's cap_paused unlock did not
  weaken the real one.

### 4. L-4 — the approval, answered from BOTH homes
expected: Arm a real ask_user approval. In the chat thread, at the paused message, the two actions are present — click one; the workspace panel shows the pause settled there too, with no manual refresh (read the rendered sentence, not a testid). Arm a second approval, answer it in the PANEL; back in the thread the inline controls are gone and the row reads settled. WorkflowRunPage still renders its own PendingAskCard correctly.
closes: SHELL-03, BUG-260828-07
result: issue
reported: "Driven on a REAL workflow-raised approval. The chat thread renders NO approval controls at all — all three actions live only in the workspace panel. This is BUG-260828-07's original complaint, unfixed on this path."
severity: blocker
driven_by: Claude (Chrome MCP), 2026-09-12
fixture: same run as L-3 — 200-Word Essay Writer, step 2 "act" marked needs-approval
evidence: |
  The pause is genuinely the ask_user mechanism (the panel's card carries data-testid
  "ask-free-label") and offers three controls: "Approve this step", "Do not run it", "Send Answer".
  Measured by position against the panel's own left edge (aside.left = 1082):
    · "Approve this step" left=1103 → IN PANEL
    · "Do not run it"     left=1103 → IN PANEL
    · "Send Answer"       left=1244 → IN PANEL
    · anyApprovalControlInChatColumn = FALSE
    · chatColumnHasPausedCue          = FALSE   (no PausedRunCue either)
  A sweep of the chat column for any ask/pending/approval/paused data-testid returned NOTHING.
root_cause: |
  MEASURED against the live rows, not inferred. MessageItem.tsx:92-98 gates the inline mount on
    hasPendingAsk(tool_calls) = tool_calls.some(tc => tc.name === "ask_user"
                                && (tc.status === "running" || tc.status === "interrupted"))
  and the mount sits inside the `isMessageStreaming` arm (assistant rows only, per its own
  docblock at :529-533).
  But the row the harness actually writes for this pause is:
    role    = "system"
    content = "Step 2 of 2, \"act\", is about to run. This step is marked as ..."
    tool_calls = [ { kind: "ask_user_prompt" } ]          ← NO `name`, NO `status`
  ⇒ `hasPendingAsk` is FALSE for the shape the product writes, and the row is `system`, not a
  streaming assistant row — so the arm is unreachable twice over for a workflow-raised approval.
  ⭐ THE SHAPE IS THE SAME FAMILY AS THE cap_paused CARRIER (agent_loop.persist_cap_paused writes
  role='system' + a `kind`-tagged tool_calls entry). The harness speaks `kind`; the predicate
  reads `name`/`status`.
why_the_fence_did_not_catch_it: |
  244-03 shipped `__tests__/MessageItem.inlineApproval.test.tsx` and it is GREEN — because the
  test CONSTRUCTS a message with `tool_calls: [{name:"ask_user", status:"running"}]`, which is the
  Deep-agent shape. The fence proves the component renders the stack WHEN HANDED that shape; it
  never asks whether the product ever produces it for a workflow run. This is the project's own
  recorded lesson — a test pinning a value proves the value is right, not that anything uses it.
  ⚠ 244-VERIFICATION.md called this criterion "structural ... follows by construction" from the
  shared store selector. The selector IS shared; the MOUNT CONDITION is not reached.
not_yet_established: |
  ⛔ The Deep-mode path (an agent calling ask_user as a tool mid-turn) was NOT driven and may well
  work — this row only proves the WORKFLOW path fails. L-4's direction-2 and the WorkflowRunPage
  third home were not reached either, because there are no chat-side controls to compare against.
  Say "the workflow-raised approval has no chat controls", never "the inline approval is broken".
operator_safety_note: |
  ⛔ The pending step was `send_email` over SMTP — an outward-facing, irreversible action. I did
  NOT click "Approve this step". The approval was settled with "Do not run it" + Send Answer,
  which is non-destructive and still exercises the answer path.

### 5. L-5 — the local attach, end to end
expected: The + menu shows exactly three items in this order — "Attach a file", "From cloud storage", "Tools and connectors". Attaching an .xlsx puts a chip in the EXISTING chips row beside a connector chip reading "this chat only · 24h". The SENT message carries the chip ABOVE the text with "this chat only" on it. The agent's reply shows a one-line "Read <file>" pointer and real content. NEGATIVE — no documents row exists for that filename. A .pdf is accepted and readable. A .zip is refused with the server's verbatim sentence "Unsupported type .zip. Allowed: <sorted list including .pdf>". With no cloud connection the + menu shows the local item alone with no orphaned divider. A past-TTL attachment renders "No longer available", struck through, titled "Files attached to a chat are kept for 24 hours."
closes: SHELL-04 (half), BUG-260905-01 (half)
result: issue
reported: "Steps 1-5 and 7 PASS. Step 6 (.pdf) exposes ONE confirmed defect: the second attachment in a thread is never hydrated into /sandbox/attachments, so the agent burned 10+ rounds hunting for it. A second claim (no chip on the sent message) was RETRACTED the same session — see the retraction below; the chip is LATE, not absent."
severity: major
driven_by: Claude (Chrome MCP), 2026-09-12
fixture: thread 79d18aff-81ac-4579-9ee3-dd950ed6235b ("Attached File Row Listing"); files authored for this row — Meridian-Q4-pricing.xlsx (4 rows), uat-note.pdf, uat-blocked.zip
steps: |
  | # | What | Result |
  |---|---|---|
  | 1 | `+` menu, three items in DRAWN order | ✅ PASS — measured top-to-bottom by bounding rect: "Attach a file" (188, role=menuitem), "From cloud storage" (216, menuitem), "Tools and connectors" (242, section). Order is exactly as specified. |
  | 2 | chip reads `this chat only · 24h` | ✅ PASS on the words — chip renders "…4.9 KB · this chat only · 24h · ×". ⚠ but see the NAME defect below |
  | 3 | SENT message carries the chip ABOVE the text with `this chat only` | ✅ PASS for the FIRST attachment (D-244-22's build obligation, the reason variant A won) |
  | 4 | agent shows a one-line `Read <file>` pointer and real content | ✅ PASS — "Read c679b991-Meridian-Q4-pricing.xlsx" rendered, and the answer reproduced the fixture EXACTLY: Q4 sheet, 4x4, EMEA/Meridian Pro/1499/212, APAC/1399/164, AMER/Meridian Lite/649/908 |
  | 5 | ⛔ NEGATIVE — no `documents` row | ✅ PASS — `select … from documents where filename ilike '%Meridian-Q4-pricing%'` returns **0 rows**. The structural "not in the KB" guarantee holds. |
  | 6 | `.pdf` accepted and readable | ⚠ MIXED — accepted and stored (`/902f62ba-uat-note.pdf`, 1446 B, kind=template_input), but see the two defects below |
  | 7 | `.zip` refused with the server's VERBATIM sentence | ✅ PASS — on-screen: "Unsupported type .zip. Allowed: .csv, .docx, .gif, .jpeg, .jpg, .js, .json, .md, .pdf, .png, .pptx, .py, .sh, .txt, .webp, .xlsx" — and `.pdf` IS in the list, which is 244-02's ruling proven from the running server, not from source. |
  | 8 | one-item menu with no cloud connection | ⬜ NOT RUN — this account has connectors configured; would need a second account or a disconnect, which mutates the operator's environment |
  | 9 | expired chip (`No longer available`, struck through) | ⬜ NOT RUN — TTL is 24h; the row would need a clock or a seeded past `expires_at` |
defect_6a_RETRACTED_chip_is_LATE_not_ABSENT: |
  ⚠⚠ RETRACTED AND CORRECTED THE SAME SESSION — the original claim is kept below rather than
  deleted, because publishing it as "no chip at all" would have been a false defect.
  ORIGINALLY RECORDED: "the PDF message carries NO chip at all — measured hasScopeWord FALSE,
  hasPdfName FALSE, while the xlsx message carries one."
  ⛔ THAT WAS MEASURED DURING THE STREAMING RUN, and it did not survive. Re-measured after the
  run finished, BOTH user rows carry the chip:
      row 1 → "c679b991-Meridian-Q4-pricing.xlsx · 4.9 KB · this chat only · 24h · What is in…"
      row 2 → "902f62ba-uat-note.pdf · 1.4 KB · this chat only · 24h · Read the attached PDF…"
  ⇒ THE CORRECTED FINDING IS A LAG, NOT AN ABSENCE: the second attachment's chip was missing
  from the sent message at send time and stayed missing for the whole ~49 s run, then appeared.
  D-244-22's obligation IS met in the settled transcript.
  Severity accordingly MAJOR → MINOR. Worth a look (a person who attaches and immediately reads
  their own message sees no scope word on it), but it is not the missing-chip defect I first
  wrote down. ⛔ The lesson is the project's own: a transcript read mid-stream is not the
  transcript.

defect_6b_second_attachment_never_reaches_the_sandbox: |
  ⛔ THE AGENT'S OWN OUTPUT IS THE EVIDENCE, round 4 of its run:
        /sandbox/attachments [] ['c679b991-Meridian-Q4-pricing.xlsx']
  — the directory holds ONLY the first file. The PDF is absent. The agent then spent rounds 5-12
  searching the filesystem ("Broader filesystem search for the PDF", "Looking for workspace or
  mount directories", "Listing common mount directories") before recovering it at round 10 via
  the `workspace_read` fallback.
  ⚠ CONSISTENT WITH A KNOWN, DOCUMENTED MECHANISM: CLAUDE.md records that sandbox sessions are
  cached per `thread_id` until idle eviction. Hydration evidently runs at session CREATION, so a
  file attached after the sandbox exists never lands in it.
  ⇒ SHELL-04's "a person attaches a file AND THE AGENT CAN USE IT" is true for the first
  attachment and only accidentally true for later ones — it survived here because
  `workspace_read` exists, at a cost of ~10 wasted rounds and 49s.
  ⛔ NOT ESTABLISHED: whether a fresh thread's FIRST pdf hydrates correctly. The ordering
  (xlsx first, pdf second) is confounded with the file TYPE in this single run. Say "the second
  attachment did not hydrate", never ".pdf does not hydrate".
naming_defects_deliberate_storage_leaking_into_copy: |
  The chip, the `Read <file>` pointer AND the panel all show `c679b991-Meridian-Q4-pricing.xlsx`
  — the person attached `Meridian-Q4-pricing.xlsx`. And the workspace panel labels a chat
  attachment **"Template"**.
  ⚠ BOTH COME FROM DELIBERATE STORAGE DECISIONS, verified in source rather than assumed:
    · `workspace.py:336` — path = f"/{uuid4().hex[:8]}-{safe_name}"   (collision avoidance)
    · `workspace.py:350` — kind="template_input"                       (D-12, untrusted provenance)
  So the storage design is sound and defensible; what is wrong is that the STORAGE DETAIL is what
  the person is shown. This is a presentation fix, not a storage change. ⛔ Do not "fix" it by
  removing the uuid prefix or the kind.

### 6. L-6 — the two doors, un-inverted
expected: From the composer's cloud item, picking a Drive file confirms with the word "Attach" (never "Import"), produces a chip reading "this chat only", creates NO documents row, and leaves the textarea's value unchanged. From the Library with a folder selected, the single-file cloud door lands the document in THAT folder (documents.folder_id), and with no folder selected it REFUSES with a reason on screen.
closes: SHELL-04 (half), BUG-260905-01 (half)
result: issue
reported: "All 6 steps PASS — the two doors are genuinely un-inverted. Two separate defects found alongside: a Google-native Drive file cannot be attached ('Unsupported type (none)'), and one Library import returned HTTP 200 and created nothing."
severity: minor
driven_by: Claude (Chrome MCP), 2026-09-12
steps: |
  | # | What | Result |
  |---|---|---|
  | 1 | composer cloud door confirms with **`Attach`**, never `Import` | ✅ PASS — modal "Choose a file", confirm reads "Attach". ⭐ And the LIBRARY door's modal reads "Choose a file to **import**" with confirm "**Import here**" — the two doors are worded DIFFERENTLY, which is D-244-05's whole point. |
  | 2 | chip reads `this chat only` — both composer doors mean the same thing | ✅ PASS — cloud-attached `.docx` produced "53e4c237-Fahed_Mra… · 12.5 KB · this chat only · 24h ·×", identical in shape to the local-attach chip |
  | 3 | ⛔ NEGATIVE — no `documents` row from the composer door | ✅ PASS — 0 rows for `Fahed_Mrad_CV_Rewritten`; only a `workspace_files` row (`/53e4c237-…`, kind=template_input). The composer door does not mint a Library document. |
  | 4 | ⛔ the textarea's value is UNCHANGED | ✅ PASS — typed "SENTINEL words typed by the person" with real keystrokes, ran a cloud attach, re-read: value preserved byte-for-byte |
  | 5 | Library door lands the document in the SELECTED folder, not root | ✅ PASS — imported `VISA_Fahed Mrad.pdf` with DBA selected → `documents.folder_id = 37380338-2ccd-4eec-84be-645036d37a09`, which is DBA's own id (verified by joining `folders`). Not root. |
  | 6 | with NO folder selected the door REFUSES with a reason on screen | ✅ PASS — with Root selected the control is `disabled: true` AND renders the visible sentence "Choose a folder first — an import needs somewhere to land." Selecting DBA flips it to `disabled: false`, the sentence disappears, and the affordance names its destination ("Imports into DBA"). ⛔ This is the requirement's harder half — a silent grey-out would have failed, and it does not. |
method_correction_recorded_not_overwritten: |
  ⚠ Step 4 was FIRST driven by setting `textarea.value` from JavaScript and dispatching an
  `input` event. That reported "unchanged: false" — and it was an INVALID PROBE, not a defect:
  the composer is a React-CONTROLLED input, so a directly-assigned value is not the component's
  state and is discarded on the next render. Re-driven with real keystrokes through the
  computer tool, the value is preserved. ⛔ The false reading is recorded rather than deleted,
  because publishing it would have been a fabricated defect.
defect_google_native_files_cannot_be_attached: |
  ⛔ Picking the Drive file "Sara school" (a native Google Doc, which has NO file extension) and
  confirming produced, verbatim on screen:
      Sara school — Unsupported type (none). Allowed: .csv, .docx, .gif, .jpeg, .jpg, .json,
      .md, .pdf, .png, .pptx, .py, .sh, .txt, .webp, .xlsx
  Two problems: (a) native Google Docs/Sheets/Slides are the MOST COMMON thing in a Drive and
  none of them can be attached; (b) the copy says "(none)", which is the extension-check's
  internal state leaking — it tells the person nothing about what went wrong or what to do.
  ⚠ The Library ingest path DOES handle Google-native files (CLAUDE.md records Drive
  read/EXPORT pins in `app/security/egress.py`), so the composer door is missing an export step
  the other door already has. ⛔ NOT verified end-to-end here — stated as the likely asymmetry to
  check, not as a measured fact.
observation_one_import_200_created_nothing_NOT_REPRODUCED: |
  ⚠ The FIRST Library import (`Fahed_Mrad_CV_Rewritten.docx`) POSTed
  `/connectors/connections/…/files/…/import` and got **HTTP 200**, and 40 s later there was NO
  `documents` row and NO `ingestion_jobs` row — nothing at all. The SECOND import, same endpoint
  and same connection, returned 200 with a full document body and DID create the row.
  ⛔ I did not capture the first attempt's response body (the logging wrapper was only armed for
  the second), so the cause is UNKNOWN and this is recorded as an OBSERVATION, not a confirmed
  defect. It is written down because a 200 that creates nothing is exactly the class of silent
  failure this phase exists to remove, and because it will be invisible to anyone who only
  checks status codes.
  Re-open trigger: any further Library import that answers 200 without producing a `documents`
  row — capture the response BODY that time.
environment_note: |
  This row created one REAL Library document in the operator's local DBA folder:
  `VISA_Fahed Mrad.pdf` (id 5242c0c1-0370-458c-8339-a4496efb66ce). Left in place as the row's
  evidence; safe to delete.

### 7. L-7 — the app-shell signal, driven end to end for the first time
expected: (a) Break a watched source genuinely; past the debounce, while on the Chat view doing something else, the rail badge appears and its popover names the source with its cause sentence. (b) With only healthy sources the badge does NOT appear. (c) Clicking through opens the Library on the Health tab carrying the count while the other four tabs carry nothing; navigating away and back into the Library opens on the Library's OWN default — the one-shot hand-off stays spent.
closes: SHELL-05, BUG-260911-03
result: pass
driven_by: Claude (Chrome MCP + local DB fixture), 2026-09-12
significance: |
  ⭐ THIS CRITERION HAD NEVER BEEN DRIVEN END TO END. 244-VERIFICATION.md called it "the highest-
  risk unverified criterion in the phase" and both 244-CONTEXT.md D-244-15 and 244-04-SUMMARY.md
  state that Phase 235 closed SURF-03 UNTICKED and nothing has observed the badge mechanism in a
  browser across two milestones. It is now driven, in both directions, and it WORKS.
arms: |
  **(b) THE NEGATIVE — run FIRST, deliberately, because it is the ROADMAP's named failure mode
  ("a signal nobody will trust after the first false one").**
  With both watches healthy (`connector_watches.last_error IS NULL` on both), on the Chat view:
  the rail's Library row reads exactly `"Library"` — no badge, no digit, and
  `[data-testid="rail-attention-trigger"]` is absent from the DOM. ✅ No false signal.

  **(a) THE POSITIVE.** A hard failure was seeded into `connector_sync_runs` for watch
  5d52d519… — status=failed, failure_cause=token_revoked, last_error="HTTP 401 invalid_grant:
  Token has been expired or revoked." (`token_revoked` is in HARD_CAUSES, so it stops on the
  FIRST failure — SOFT_FAILURE_THRESHOLD=3 does not apply). Server verdict confirmed straight
  from `GET /sources/health`:
      stopped: [ { source_folder_name: "CV", cause: "token_revoked", hard: true,
                   stopped_since: …, last_good_at: … } ], reader_running: true
  ⭐ ON THE CHAT VIEW, WHILE DOING SOMETHING ELSE, the badge appeared: rail row `"Library1"`,
  amber. Opening `[data-testid="rail-attention-trigger"]` (aria-label "1 source stopped reading")
  showed the popover naming the source and its cause:
      title  → "CV"
      detail → "Access to Google Workspace was withdrawn — the connection needs to be
                authorised again."
  ⛔ Note what that sentence does: it says what is true OF THE SOURCE, never what the code
  experienced — no status code, no exception. That is the vocabulary rule holding in the product.

  **(c) THE ATTRIBUTION (BUG-260911-03).** Opening the Library, exactly ONE tab carries the
  count and the other four carry nothing:
      Documents · Views · Ingestion · Indexing · **Health1**
  Clicking the popover's "Open Library health" lands on **Health**, selected.
handoff_stays_spent_PROVEN_NOT_ASSUMED: |
  ⚠ The naive check FAILED and the honest one PASSED — recorded because the difference is the
  whole finding. Navigating away and back landed on **Health** again, which looks exactly like
  the Phase 235 plan-15 defect (a one-shot hand-off re-firing).
  ⛔ It is NOT. Decisive discriminator: manually select **Documents**, navigate away, come back —
  it lands on **Documents**. So the Library REMEMBERS THE LAST-VIEWED TAB; the hand-off is spent
  and "its own default" is the sticky tab, not a fixed Documents.
  ⇒ The Phase 235 plan-15 defect stays CLOSED. Had I stopped at the first reading I would have
  published a regression that does not exist.
environment_restored: |
  The seeded failure row was DELETED after the row was driven, and the badge's disappearance was
  confirmed by polling until `rail-attention-trigger` left the DOM (rail back to plain "Library").
  One PRE-EXISTING failed run remains (06:07:15Z, `[Errno 11001] getaddrinfo failed`,
  failure_cause `unknown`) — it is NOT mine and does not trip the badge, because `unknown` is a
  SOFT cause needing 3 consecutive failures.

### 8. A — the 8-row cross-provider board (system-prompt attachment line)
expected: Roster DERIVED from MODEL_CAPABILITIES, never re-typed; one registry-backed representative per provider (OpenAI, Anthropic, Google, DeepSeek, Zhipu/GLM, MiniMax, Moonshot/Kimi, OpenRouter). Per row, on a fresh thread with an .xlsx attached and the prompt "What is in the attached file?", record O1 (prompt line present in the turn), O2 (model acknowledges unprompted), O3 (model reads /sandbox/attachments/<name> and answers with real content), O4 (no regression). Provider attribution READ BACK from the run record, never from the control that was clicked.
closes: SHELL-04 cross-provider obligation (CLAUDE.md UAT roster rule)
result: issue
reported: "The full 8-row board was DRIVEN — the first time this project has completed one. 7 of 8 pass. OpenAI FAILS reproducibly: gpt-5.6-luna never calls execute_code and answers 'I couldn't access the spreadsheet contents.'"
severity: major
driven_by: Claude (API, per-request model+provider), 2026-09-12
roster_derivation: |
  ⛔ DERIVED FROM `MODEL_CAPABILITIES`, NEVER RE-TYPED — grouped by `provider`, newest
  registry-backed representative per group. The registry yields EXACTLY the 8 groups CLAUDE.md
  names: anthropic · deepseek · google · minimax · moonshot · openai · openrouter · zhipu.
  Every representative resolved `capability_source: registry` (so none silently lost `emit_tier`,
  SEED-040 / SEED-135).
method: |
  The Phase 185 method: a REAL run per row with a PER-REQUEST `model` + `provider` on
  `POST /threads/{id}/messages`. ⛔ NO GLOBAL SETTING WAS MUTATED — the operator's environment is
  untouched and rows cannot contaminate each other. Each row is a FRESH thread with the same
  `Meridian-Q4-pricing.xlsx` fixture (cloned at the `workspace_files` row, content_inline and
  all) and the same prompt: "What is in the attached file? List every row."
  ⭐ **THE PHASE 243 BLOCKER IS RESOLVED, AND THAT IS THE REUSABLE PART.** That board was
  ABANDONED because the driver could not verify which provider a row actually ran on (a click
  for `anthropic` selected `minimax`). Here attribution is READ BACK FROM THE RUN RECORD —
  `runs.model` / `runs.provider` — never from the control that was clicked. All 8 rows match
  what was requested, verified in SQL. No coordinate-clicking of the provider picker was
  involved, and no guard was worked around: the session token came from the app's own page
  context, exactly as the app itself uses it.
board: |
  | # | Provider | Model id (derived) | emit_tier | native_tools | O1 | O2 | O3 | O4 | Verdict |
  |---|---|---|---|---|---|---|---|---|---|
  | P-1 | OpenAI | `gpt-5.6-luna` | force_strict | True | ~ | ⛔ | ⛔ | ✅ | ⛔ **FAIL** |
  | P-2 | Anthropic (native SDK) | `claude-sonnet-5` | force | True | ~ | ✅ | ✅ | ✅ | ✅ pass |
  | P-3 | Google | `gemini-3.5-flash` | force | True | ~ | ✅ | ✅ | ✅ | ✅ pass |
  | P-4 | DeepSeek | `deepseek-v4-pro` | force | True | ~ | ✅ | ✅ | ✅ | ✅ pass |
  | P-5 | Zhipu / GLM | `glm-5.2` | force | True | ~ | ✅ | ✅ | ✅ | ✅ pass |
  | P-6 | MiniMax | `MiniMax-M3` | force | True | ~ | ✅ | ✅ | ✅ | ✅ pass |
  | P-7 | Moonshot / Kimi | `kimi-k2.6` | **coerce** | True | ~ | ✅ | ✅ | ✅ | ✅ pass |
  | P-8 | OpenRouter | `deepseek/deepseek-v4-pro` | force | **False** | ~ | ✅ | ✅ | ✅ | ✅ pass |

  O2 = named the file or called a tool on the first turn · O3 = `execute_code` read it and the
  answer carries REAL fixture content (`Meridian`, and one of 1499 / 1399 / 908) · O4 = run
  reached `completed` with `error IS NULL`.
o1_not_directly_verified: |
  ⚠ O1 ("the prompt line was present in the turn") is marked `~` on EVERY row and is NOT claimed
  as verified. Its stated method is a LangSmith trace read, and **the LangSmith MCP server failed
  to connect this session**. What IS established: 7 of 8 providers acted on the attachment
  unprompted, and the line is appended on the SHARED path above the service boundary — so O1 is
  very probably satisfied for all 8. ⛔ That is an inference, not a measurement, and it is the one
  thing that would distinguish "OpenAI ignored the line" from "OpenAI never got it".
the_openai_failure: |
  ⛔ REPRODUCED, NOT A ONE-OFF — driven twice on two separate fresh threads.
    run 1: no tool calls at all; answer, in full — "I'm sorry, but I couldn't access the
           spreadsheet contents." (58 chars)
    run 2: called `load_skill` ONLY, never `execute_code`; answer — "I'm sorry, but I wasn't
           able to read the attached spreadsheet."
  Both runs reached `completed` with `error IS NULL` and correct attribution
  (`gpt-5.6-luna` / `openai`), so this is NOT a transport failure, NOT a missing key, and NOT a
  registry miss — the model ran and declined to open the file that six other native providers
  opened from the identical fixture and prompt.
  ⚠ **AND IT IS THE HIGHEST-`emit_tier` ROW THAT FAILED.** `gpt-5.6-luna` is `force_strict`, the
  STRONGEST emission guarantee in the registry, while `kimi-k2.6` — `coerce`, the weakest, and
  the row CLAUDE.md calls "the most valuable one to drive" — PASSED. The board's own prior
  expectation is inverted, which is precisely why the roster rule says to drive all eight rather
  than reason about them.
  ⛔ NOT ESTABLISHED: whether another OpenAI model behaves the same. One representative per
  provider is the roster rule; say "gpt-5.6-luna fails this row", never "OpenAI is broken".

### 9. B — the three four-axis rows
expected: X-1 multi-tool — one prompt exercising 2+ tools (search_documents AND execute_code on the attachment), and the attachment does NOT appear in search_documents results. X-2 parallel-thread — Thread A streaming while Thread B accepts a new prompt; A's chip and B's composer are independent. X-3 long-message — a thread with >= 50 prior messages or a >= 5KB prompt; the attachment prompt line still lands and the transcript chip still renders.
closes: CLAUDE.md SC#10 four-axis bandwidth
result: pass
driven_by: Claude (API, real runs), 2026-09-12
rows: |
  **X-1 — MULTI-TOOL (2+ tools in one prompt) ✅ PASS.**
  Thread d8d77452…, one prompt asking for BOTH a `search_documents` KB search for "Meridian" AND
  an `execute_code` read of the attachment. Tools actually called, from the persisted rows:
  **`['execute_code', 'search_documents']`** — both, one turn. The answer reports the two
  results separately and carries real fixture content.
  ⭐ **AND IT PROVES D-244-03 NEGATIVELY, which is the row's real purpose.** `search_documents`
  returned 5 documents — `Project-Meridian-Status-Report-Week09.docx`, `project-charter-source.md`,
  `weekly-meeting-notes-week9.md` and two more — all PRE-EXISTING KB material about Project
  Meridian. **The attached `Meridian-Q4-pricing.xlsx` is NOT among them**, despite being the most
  literally "Meridian" thing in the thread. That is the structural guarantee holding under the
  one query most likely to expose it, and it agrees with L-5 step 5's direct check (0 `documents`
  rows for that filename) — the attachment cannot be returned because it was never minted.

  **X-2 — PARALLEL-THREAD ✅ PASS.** Two threads, timings read from `runs`:
      thread A  started 08:33:51 → completed 08:34:40   (attachment + execute_code + 300-word task)
      thread B  started 08:33:59 → completed 08:34:05
  ⇒ B was accepted, ran AND finished entirely INSIDE A's window, while A was still streaming.
  B's post returned 201 in **234 ms** with A mid-run. Independence confirmed on content too:
  B called NO tools and its answer is exactly "independent" — A's attachment did not leak into
  B's turn, and B's composer was not blocked by A's run.

  **X-3 — LONG-MESSAGE ✅ PASS.** Thread ccc43405… seeded to **52 prior messages** (26 user +
  26 assistant) before the attachment prompt. The run completed, `execute_code` was called, and
  the answer carries real fixture content — so the attachment prompt line still lands and is
  still acted on at >50 messages of history.
scope_note: |
  ⛔ All three axis rows ran on the DEFAULT provider, not across the roster — the cross-provider
  axis is row A's job and is scored there. Say "the axis holds on the default provider", never
  "the axis holds everywhere".

## Fixtures left in the local dev environment (all deletable)

Named so the operator can remove them, and so a later reader knows they are UAT residue and not
real data. ⛔ Nothing was written to cloud; nothing outside this local DB was mutated.

| What | Where | Note |
|---|---|---|
| 14 threads titled `UAT244-*` | local Supabase `threads` | the 8 provider rows + probe + openai retry + 4 axis threads |
| `Attached File Row Listing` thread | local `threads` | L-5's local-attach thread (xlsx, pdf, zip refusal) |
| `Ready` thread + a `cap_paused` run | local `threads` / `runs` | L-2's seeded cap state — run 05fea6e9… still reads `cap_paused` with `continues_used=3` |
| `VISA_Fahed Mrad.pdf` | local `documents`, folder DBA | L-6 step 5's real import (id 5242c0c1…) |
| `200-Word Essay Writer` run + thread | local `workflow_runs` | L-3/L-4's real workflow run, approval answered "Do not run it" |
| workspace_files clones | local `workspace_files` | the xlsx fixture cloned into each board thread |

⭐ **REVERTED deliberately:** the seeded `connector_sync_runs` failure for L-7 was DELETED and the
badge's disappearance confirmed, so the operator is not left with a source that looks broken.

## Summary

total: 9
passed: 4
issues: 5
pending: 0
blocked: 0
skipped: 0
blocked: 0

## Gaps


```yaml
- id: G-1
  truth: "A cap-paused DEEP thread's chrome tells the truth about what is running"
  status: failed
  reason: |
    DRIVEN 2026-09-12 while running L-2. On a Deep (General-mode) thread whose latest run is
    cap_paused, the chat transcript renders a LIVE harness run receipt that contradicts the
    card 41px above it. Both were on screen simultaneously (measured, one getBoundingClientRect
    pass):
      · amber card, top=272px:  "Reached the Continue limit - this run is stopped. Start a new
                                 message to keep going."
      · run line,   top=313px:  "* Starting workflow..."  with data-run-line-state="live"
    There is no workflow. The thread has never had one. The line also runs a 1s setInterval
    clock (ThreadRunLine.tsx:243-249) for a run that is stopped.
  severity: major
  test: 1 (L-2)
  root_cause: |
    MEASURED, not inferred. Phase 244-03 made the server populate the thread lock for a
    cap_paused DEEP run (threads.py:1237-1276) - the lock used to be harness-only. But
    `useHarnessLiveForThread` (StreamsProvider.tsx:4568-4573) still reads
      s.harnessKickoffThreads.has(threadId) || s.workflowLockByThread.has(threadId)
    i.e. it treats ANY non-null workflow lock as "a harness run is live". ThreadRunLine.tsx:207
    consumes that as `isLive`, takes the STATE-1 LIVE arm (:257-274) and calls
    outerBannerLabel(..., isHarness=true) (:187-191), whose pre-tools value is the harness
    string (toolMeta.ts:197). So the 244-03 server change reached a consumer that was written
    against the old invariant.
  second_consumer_same_cause: |
    MessageItem.tsx:787 branches `workflowLock != null ? <HarnessOuterBanner/> : <Deep span>`,
    and its own docblock (:214-215) asserts "only when `workflowLock != null` => a DEEP thread
    never mounts it, so the Deep path costs zero fetches and zero subscriptions". 244-03 made
    that sentence FALSE. Beyond the wrong copy, HarnessOuterBanner calls usePhases, which mounts
    usePanelReconcile and fires a getThreadWorkflow FETCH - the exact cost the comment claims
    the Deep path does not pay (T-194-07-03's disposition).
  relation_to_known_findings: |
    Same family as WR-07 (VERIFICATION.md: "workflowLocked could unlock a genuine harness lock
    ... no current writer found", status open-latent) - but this is a DIFFERENT consumer
    (useHarnessLiveForThread, not ChatArea.tsx:140) and it is NOT latent: it fires on the
    primary Deep path and was observed on screen. WR-07 asked "what if workflow_runs.status is
    ever written cap_paused"; this needs no such writer - runs.status='cap_paused' is enough.
  why_no_fence_caught_it: |
    Both the ThreadRunLine and MessageItem suites construct their harness state directly, so no
    test ever asks "what does a DEEP thread with a cap_paused lock render?" The invariant that
    broke lived in a COMMENT, not in an assertion.
  artifacts:
    - frontend/src/providers/StreamsProvider.tsx:4568-4573
    - frontend/src/components/chat/ThreadRunLine.tsx:207,257-274,187-191
    - frontend/src/components/chat/MessageItem.tsx:787,214-215
    - frontend/src/lib/toolMeta.ts:197
    - backend/app/api/threads.py:1237-1276
  missing:
    - "a lock-KIND discriminator: the thread lock must say whether it is harness or cap_paused, so a Deep cap-pause cannot read as a live workflow"
    - "a fence asserting a DEEP cap_paused thread renders NO thread-run-line and the Deep banner copy"
```

```yaml
- id: G-6
  truth: "SHELL-03 — an approval is answerable from the chat thread, with the panel's same two actions"
  status: failed
  reason: |
    DRIVEN 2026-09-12 on a REAL workflow-raised approval (200-Word Essay Writer, step 2 "act").
    The chat thread renders NO approval controls and NO paused cue. All three controls
    ("Approve this step", "Do not run it", "Send Answer") measured at left>=1103 against the
    panel's left edge of 1082 — every one inside the panel, none in the chat column.
    This is BUG-260828-07's original complaint ("it looked right and did nothing") still true on
    this path, and the phase's headline criterion.
  severity: blocker
  test: 4 (L-4)
  root_cause: |
    MessageItem.tsx:92-98 `hasPendingAsk` requires tc.name === "ask_user" AND tc.status in
    (running, interrupted), and the mount is additionally inside the isMessageStreaming
    (assistant-row) arm. The row the harness WRITES for this pause is role='system' with
    tool_calls = [{kind: "ask_user_prompt"}] — no `name`, no `status`. The predicate is false and
    the row is not an assistant row, so the arm is unreachable twice over.
    The harness speaks `kind` (same family as persist_cap_paused's carrier); the predicate reads
    `name`/`status`.
  why_the_green_fence_missed_it: |
    __tests__/MessageItem.inlineApproval.test.tsx is GREEN and CONSTRUCTS
    tool_calls:[{name:"ask_user",status:"running"}] — the Deep-agent shape. It proves the
    component renders when handed that shape; it never asks whether the product emits it for a
    workflow run. 244-VERIFICATION.md called the criterion "structural — follows by construction"
    from the shared selector; the selector is shared, but the MOUNT CONDITION is never reached.
  scope_limit: |
    ⛔ Only the WORKFLOW path is proven broken. The Deep-mode ask_user path was NOT driven and may
    work. Do not generalise.
  artifacts:
    - frontend/src/components/chat/MessageItem.tsx:92-98
    - frontend/src/components/chat/MessageItem.tsx:545-548
    - "measured row: role=system, tool_calls=[{kind:'ask_user_prompt'}] on thread 0d8fda6d-69c8-4270-a358-8541c410e1c4"
  missing:
    - "hasPendingAsk must recognise the harness carrier shape (kind='ask_user_prompt'), and the mount must be reachable from a role='system' carrier row"
    - "a fence driven against the shape the PRODUCT writes for a workflow run, not one the test constructs"
```

```yaml
- id: G-5
  truth: "SHELL-01 — the page root never scrolls; the rail stays put and the messages move inside"
  status: failed
  reason: |
    DRIVEN 2026-09-12, 6 samples (3 heights x panel open/closed). FAILS at viewport heights below
    ~540px: the page ROOT overflows (h=436 -> scrollHeight 540 vs clientHeight 436, +104px; h=516
    -> +24px) and the whole page scrolls. h=576 and h=696 are clean. rootScrollHeight is PINNED at
    540px at every viewport, and panel state makes no difference at all.
    This is BUG-260828-08's exact complaint and the ROADMAP's exact named failure mode: "fixed at
    one window height and the dead space returns at another."
  severity: major
  test: 2 (L-1)
  root_cause: |
    The NAV RAIL overflows, not the transcript. NavPanel.tsx:205's rail root is
    "hidden md:flex flex-col h-full shrink-0 ... py-3" with computed overflow-y: visible and
    min-height: auto — NO overflow-y-auto, NO min-h-0. Its mt-auto footer block (NavPanel.tsx:290,
    Organization admin / Control Room / user row, 128px tall) measures bottom=540px, past the
    rail's own box, and every ancestor up to <html> is overflow-y: visible, so it escapes to the
    page scrollbar.
  why_244_01_missed_it: |
    244-01's min-h-0 chain landed at 5 sites, ALL in the message column (ChatLayout.tsx:803,809;
    ChatArea.tsx:501,584; MessageList.tsx:219). The workspace <aside> already carries min-h-0.
    The RAIL column got neither. ⚠ NavPanel.tsx has NO hot-file ledger row — CLAUDE.md records it
    "absent from BOTH for its ENTIRE LIFE at 11 phases" — so G-5 could never have fired on it, and
    it is the file this phase's own first criterion turns on.
  artifacts:
    - frontend/src/components/layout/NavPanel.tsx:205
    - frontend/src/components/layout/NavPanel.tsx:290
  missing:
    - "min-h-0 + overflow-y-auto on the rail root so the rail scrolls its own content"
    - "a browser-driven check that the page root does not overflow at a viewport shorter than 540px — jsdom cannot lay out, so this can never be a vitest fence"
    - "a hot-file ledger row for NavPanel.tsx (11 phases, never listed)"
```

```yaml
- id: G-3
  truth: "When the thread snapshot fails, the chat shell tells the person something went wrong"
  status: failed
  reason: |
    DRIVEN 2026-09-12 while setting up L-1. `GET /threads/{id}/snapshot` returned **503** on 2 of
    4 observed calls (intermittent, not reproducible on demand). When it fails the thread header
    and composer render normally and the TRANSCRIPT IS SILENTLY INCOMPLETE - no banner, no retry,
    no indication at all. First observed as a 78-message thread opening completely EMPTY; a
    second click on the same row loaded it (snapshot 200).
    Confirmed by FORCING the failure rather than waiting for it: window.fetch patched to answer
    every /snapshot with the endpoint's own 503 body. Re-opening a thread then rendered a partial
    transcript, and a sweep of EVERY leaf element for /unavailable|error|failed|retry|try again|
    something went wrong/i returned ZERO matches (the single hit was an unrelated CSS comment).
    The backend's own body says "Streaming infrastructure unavailable" and sets Retry-After: 10 -
    the server is explicit and the UI discards both.
  severity: major
  test: 2 (L-1 setup)
  root_cause_partial: |
    The 503 is raised at threads.py:517 and :526, from `redis.xinfo_stream(f"run:{rid}")` either
    timing out (2.0s) or erroring. A `ResponseError('no such key')` is correctly DEGRADED (:508,
    `continue`) because a GC'd buffer just means that run is terminal - but a TIMEOUT takes the
    whole-snapshot 503 path. ⚠ Note a cap_paused run is NON-TERMINAL, so it keeps being probed on
    every thread open; the seeded cap_paused thread is the one that produced both 503s. Whether
    that is causal was NOT established and must not be written down as though it were.
  why_it_belongs_to_this_phase: |
    SHELL-01's goal sentence is "the chrome around a conversation stops getting in the way of it."
    A thread that renders its frame perfectly and silently omits the conversation is that failure
    in its purest form. It is also adjacent to BUG-260911-02 ("first click does not open") - the
    OBSERVED SYMPTOM here (first click empty, second click fine) matches that report's complaint,
    and 244-01's trace should be re-read against this mechanism before BUG-260911-02 is retired.
  artifacts:
    - backend/app/api/threads.py:495-530
    - "observed: 2/4 GET /threads/68818c2a-.../snapshot -> 503"
  missing:
    - "a visible failure state for a snapshot that 503s — the endpoint already sends Retry-After: 10 and the UI ignores it"
    - "a decision on whether an empty transcript may EVER render without an error, given the header renders fine"
```

```yaml
- id: G-4
  truth: "BUG-260911-02 (first click does not open) is closed on a traced mechanism"
  status: needs-recheck
  reason: |
    OBSERVED 2026-09-12: the first click on the 78-message fixture row opened the thread HEADER
    but left the transcript EMPTY; a second click rendered all 78 messages. That is the reported
    symptom. 244-01 Task 2 traced this and 244-01-BUG-260911-02-TRACE.md exists — but G-3 above
    supplies a DIFFERENT candidate mechanism (a silent snapshot 503) that the trace should be
    checked against before the report is retired.
  severity: minor
  test: 2 (L-1 setup)
  artifacts:
    - .planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md
  missing:
    - "re-read the 244-01 trace against the snapshot-503 mechanism; if they are the same defect, say so, and if not, BUG-260911-02 has a second cause"
```

```yaml
- id: G-2
  truth: "The thinking badge sits in the order the operator expects relative to the tool-call container"
  status: failed
  reason: "Operator reported during UAT 2026-09-12: 'the thinking badge it's recommended to be below the container of the tools not above'. Observed live. MessageItem.tsx:479 renders <ThinkingBlock> BEFORE <RunCard> (the tools container, :484-486)."
  severity: minor
  test: observation-during-uat
  conflicts_with: "Phase 243 locked rationale at MessageItem.tsx:477 — 'ORDER IS THE ORDER IN TIME - above the run card's tool rows and above the answer. A sibling of WorkingBadge and RunCard.' Moving it is an OPERATOR OVERRIDE of that decision, not a defect fix."
  fence_at_risk: "frontend/src/components/chat/ThinkingBlock.characterization.test.tsx §12 pins the mount site on source; a move must re-drive that fence, not delete it."
  artifacts: []
  missing: []
```

---

## Round 2 — gap-closure re-drive rows

⛔ **APPEND-ONLY, ADDED BY `244-13`. Nothing above this line was edited** — not a row, not a
`## Gaps` YAML block, not the `## Summary` counts. Those carry the round's recorded corrections
and retractions (`G-1`'s `why_no_fence_caught_it`, `L-5` defect 6a's retraction to MINOR, `L-2`'s
`method_caveat` and its `harness_note`), and they are this phase's most valuable record precisely
because they are what a later reader would otherwise re-derive from scratch.

⛔ **EVERY ROW BELOW IS `pending`, AND NOT ONE WAS DRIVEN BY AN EXECUTOR.** Gap-closure round 1
BUILT five fixes and FENCED them in jsdom; **a jsdom fence proves a component renders when handed
a shape, never that the product hands it that shape.** `244-03` shipped a green mount fence over
`BUG-260828-07` and the operator found the blocker live nineteen plans later — which is the whole
reason `D-244-14` and `D-244-19` require a DRIVEN row. `/gsd:verify-work` owns these five files.

⚠ **Solo run (`D-244-21` / `OV-SOLO-01`):** Gemini is unavailable, so every build in this round is
**SELF-verified**. There is no independent reviewer standing behind any row below; driving them is
the first outside look this work gets.

| # | Row file | Requirement | Closes gap | Severity of the gap | Verdict |
|---|---|---|---|---|---|
| R2-1 | [`244-09-UAT-ROW.md`](244-09-UAT-ROW.md) | `SHELL-01` / `BUG-260828-08` | **G-5** — the page root overflows below ~540px because the NAV RAIL, not the transcript, escapes its box | major | ✅ **PASS** |
| R2-2 | [`244-10-UAT-ROW.md`](244-10-UAT-ROW.md) | `SHELL-04` | **`L-5` defect 6b** — the second and later attachments in a thread | major | ⚠ **PARTIAL** — Arms 1-3 PASS, Arm 4 steps 3-4 FAIL (`G-7`) |
| R2-3 | [`244-11-UAT-ROW.md`](244-11-UAT-ROW.md) | `SHELL-01` | **G-3** (a snapshot 503 is now visible) and the live observation **G-4**'s ruling turns on | major / needs-recheck | ✅ **PASS** (G-4 ruled) |
| R2-4 | [`244-12-UAT-ROW.md`](244-12-UAT-ROW.md) | `SHELL-03` / `BUG-260828-07` | **G-6** — a workflow-raised approval has no chat controls | **blocker** | ⚠ **PARTIAL** — Arm 1 PASS, Arm 2 FAIL (`G-8`) |
| R2-5 | [`244-13-UAT-ROW.md`](244-13-UAT-ROW.md) | `SHELL-02` | **G-1** — a DEEP cap-pause renders a phantom LIVE workflow receipt (and review finding `WR-07`) | major | ✅ **PASS**, Arm 4 ⛔ BLOCKED |
| R2-6 | ⛔ **ADDED BY `244-14`** (review IN-03) — no row file; drive it on any thread with a tool call and a reasoning block | `G-2`, the operator override | **G-2** — the thinking badge sits ABOVE the tools; the operator's ruling is that it belongs BELOW them | cosmetic / operator ruling | ✅ **PASS** — operator confirmation owed |

⚠ ⛔ **CORRECTED 2026-09-12 BY `244-14` (review IN-03). The sentence below is the ORIGINAL and is
struck through rather than deleted, because the correction is one word wide and easy to re-make:**

> ~~**`G-2` HAS NO ROW IN THIS TABLE AND THAT IS DELIBERATE, NOT AN OMISSION.** It is an OPERATOR
> OVERRIDE of a Phase-243 locked decision rather than a defect — its own YAML block says so — and
> it was taken outside the closure round.~~

**The DECISION was taken outside the round; the CHANGE was not.** `5953ef1de` is a `feat(244-12)`
commit INSIDE gap-closure round 1: it re-orders `MessageItem.tsx:527-533` and re-drives two
fences. *"No row"* and *"outside the round"* are different claims and only the first was true.

⚠ **AND THE CONSEQUENCE IS WORTH MORE THAN THE CORRECTION: the round's ONLY user-visible visual
change was the one item with no UAT row**, in a round whose whole premise is *built, drive owed*.
R2-6 above closes that, so the operator's own override is confirmed on screen rather than assumed.
⛔ It is still **not** a G-7 violation — a re-order is not a new capability — and the source
fences were driven RED against the old order in both files.

**R2-6, in three steps** (no row file: the claim is one rendered order):
1. Open a thread whose last assistant turn made a tool call AND emitted reasoning.
2. In ONE `getBoundingClientRect` pass, read the thinking badge's `top` and the tool-call block's
   `top`. ⛔ **Expected: the badge's `top` is GREATER** — it sits BELOW the tools.
3. Confirm the badge still renders its duration and still expands. A re-order that silently drops
   either half is the anti-fix.

⚠ **`G-4` STAYS `needs-recheck` UNTIL R2-3 IS DRIVEN.** Its ruling depends on whether the
snapshot-503 mechanism and `244-01`'s traced mechanism are the same defect; only a live
observation can say, and the `## Gaps` block above is the record of that question.

⛔ **WHAT THIS SECTION DOES NOT DO: it does not close a single requirement.** `SHELL-01`,
`SHELL-02`, `SHELL-03` and `SHELL-04` all remain **built, drive owed**.

---

## Round 2 — DRIVEN 2026-09-12 by `/gsd:verify-work 244`

⛔ **APPEND-ONLY. Nothing above this line was edited except the six `Verdict` cells in the
table and the frontmatter `updated` stamp.** The round-1 record, its corrections and its
retractions stand verbatim.

**Driver:** Claude (Chrome DevTools MCP), local dev (fe :5173 / be :8000 / Supabase :54322),
signed in as the operator's own local-dev account. ⚠ **Solo run (`D-244-21` / `OV-SOLO-01`):
Gemini is unavailable, so whoever built these fixes also drove them. This board is a
SELF-VERIFICATION and must never be read as a review.**

| # | Row | Verdict | What it bought |
|---|---|---|---|
| R2-1 | `244-09` — G-5, the rail overflow | ✅ **PASS** | 8/8 samples `rootOverflowBy 0`; the overflow moved INTO the rail; the rail still does not drift |
| R2-2 | `244-10` — defect 6b, the 2nd attachment | ⚠ **PARTIAL** | Arms 1-3 PASS — defect 6b is CLOSED and the type/ordering confound is resolved. Arm 4 steps 3-4 FAIL → gap **G-7** |
| R2-3 | `244-11` — G-3 / G-4, the snapshot 503 | ✅ **PASS** | all 4 arms; both banner sentences driven; G-4 ruled |
| R2-4 | `244-12` — G-6, the approval | ⚠ **PARTIAL** | Arm 1 PASS — **the blocker is fixed**. Arm 2 FAILS in BOTH directions → gap **G-8** |
| R2-5 | `244-13` — G-1, the phantom run line | ✅ **PASS** | Arms 1-3 all pass; Arm 4 (`WR-07`) ⛔ BLOCKED, with the reason |
| R2-6 | `244-14` IN-03 — G-2, the badge order | ✅ **PASS** | tools → thinking → answer, measured; operator confirmation still owed |

⭐ **What this round DOES close:** `SHELL-01` (both halves: the frame holds at every sampled
height, and a failed snapshot is now visible), `SHELL-02`, and gaps `G-1`, `G-3`, `G-5`,
`G-6`'s headline defect and `G-2`. `G-4` is RULED (see R2-3 Arm 4).

⛔ **What it does NOT close:** `SHELL-03` and `SHELL-04` each had their headline defect fixed
and each surfaced a NEW adjacent defect while being driven (`G-8` and `G-7`). Both are
recorded below rather than folded into a verdict.

### The six rows, as driven


#### R2-1 (244-09, gap G-5) — DRIVEN 2026-09-12, Chrome MCP, width 1442, thread 261d5f57 (78 msgs, transcript sh=24127 confirmed long)

##### Instrument (calibrated at h=698 against the pre-fix controls)
composerGap = innerHeight - (textarea.parentElement.parentElement).getBoundingClientRect().bottom   -> 37.4 (spec 37.0 +-1) OK
footerGap   = innerHeight - rail.querySelector('.mt-auto').getBoundingClientRect().bottom            -> 12.4 (spec 12.0 +-1) OK
root        = document.getElementById('root')   (NEVER documentElement)

##### 8 samples — every reading with the document unscrolled
| # | innerH | panel | rootSh | rootCh | bodySh | overflow | composerGap | footerGap | railSh/railCh | verdict |
|---|---|---|---|---|---|---|---|---|---|---|
| R-1 | 434 | CLOSED (aside.left 1390.4) | 434 | 434 | 434 | **0** | 36.6 | -106 | 552/434 | PASS |
| R-2 | 434 | OPEN   (aside.left 1142.4) | 434 | 434 | 434 | **0** | 36.6 | -106 | 552/434 | PASS |
| R-3 | 514 | CLOSED | 514 | 514 | 514 | **0** | 36.6 | -26 | 552/514 | PASS |
| R-4 | 514 | OPEN   | 514 | 514 | 514 | **0** | 36.6 | -26 | 552/514 | PASS |
| R-5 | 578 | CLOSED | 578 | 578 | 578 | **0** | 37.4 | 12.4 | 578/578 | PASS |
| R-6 | 578 | OPEN   | 578 | 578 | 578 | **0** | 37.4 | 12.4 | 578/578 | PASS |
| R-7 | 694 | CLOSED | 694 | 694 | 694 | **0** | 36.6 | 11.6 | 694/694 | PASS |
| R-8 | 698 | OPEN   | 698 | 698 | 698 | **0** | 37.4 | 12.4 | 698/698 | PASS |

Pre-fix, R-1/R-2 read 540/436 = +104px and R-3/R-4 read +24px. Both are now 0.
bodyScrollHeight AGREES with rootScrollHeight at all 8.

### footerGap is negative at 434/514 and that is the FIX, not a drift
The control (12.0px) was measured pre-fix, when the footer ESCAPED the rail's box to the page.
Post-fix the footer sits inside the rail's own scroll content, so innerHeight - footer.bottom is
negative until the rail is scrolled. Scrolling the rail to its end at h=434 puts the footer back at
footerGap 11.6 (see S4 below) — the control holds, it just now requires the rail's own scrollbar.
Unscrolled footer.bottom at h=434 reads **540** — the exact pre-fix pinned number, now CONTAINED.

##### S4 — the overflow moved INTO the rail (h=434, panel CLOSED)
rail.scrollHeight 552 > rail.clientHeight 434                      -> PASS
computed overflow-y = "auto" (was "visible")                        -> PASS
footer reachable by scrolling the rail (rail.scrollTop 0 -> 117.6,
  footer.bottom 540 -> 422.4, footerGap 11.6)                       -> PASS
computed overflow-x = "hidden"; scrollWidth 52 <= clientWidth 52     -> PASS   (244-14 IN-01)
collapse<->expand across the ~300ms transition, 4 runs, sampled every 16-30ms:
  scrollLeft NEVER non-zero; worst scrollWidth-clientWidth delta = 0 on 3 of 4 runs,
  a single transient +1px on one 30ms sample with overflow-x already "hidden" (no scrollbar can
  render). Recorded as an observation, not a failure.                -> PASS   (244-14 IN-01)
collapsed rail: width 58, right-most descendant right = 45.8 -> ends 12.2px inside the box;
  zero descendants exceed the rail's right edge. Badge not clipped.  -> PASS

##### S5 — the rail still holds while the transcript scrolls (h=434, panel CLOSED, rail expanded)
Scroll driven by TRUSTED PageDown key events (chrome-devtools Input domain) with focus inside the
Radix ScrollArea viewport. NOTE: the row asked for a trusted WHEEL; no wheel tool is exposed by
chrome-devtools MCP, so a trusted KEY scroll was used instead. It is an input-domain event, not a
dispatchEvent, so the 0px-synthetic-wheel failure the row guards against cannot apply — and the
precondition below proves real movement independently of scrollTop.
  message-content LEAF rect: top 80.00 -> -379.20  => the transcript moved -459.2px   (precondition PASS)
  rail LEAF ("New chat") rect: top 74.00 -> 74.00, left 52.00 -> 52.00  => dTop 0, dLeft 0   PASS
  app Element.prototype.scrollIntoView calls during the run: **0**                            PASS
  root overflow 0 and window.scrollY 0 throughout                                             PASS

##### VERDICT: PASS — G-5 closed.

#### R2-2 (244-10, L-5b / defect 6b) - DRIVEN 2026-09-12, Chrome MCP, deepseek-v4-flash (General/Deep)

Scored on the AGENT'S OWN PRINTED DIRECTORY LISTING, never on whether it eventually answered.

##### Arm 1 - the first attachment still works (the control).  PASS
Fresh thread 953f973c-2a52-4adb-82ae-6e60c708adcc, no prior execute_code.
Attached Meridian-Q4-pricing.xlsx (4,995 B, real sheet "Q4", 5 rows x 4 cols).
Run 3f872a08, status completed. Agent's verbatim listing:
    ['ead69eda-Meridian-Q4-pricing.xlsx']
and it reproduced real cell content (North/Meridian Pro/1240/89.5 ... APAC/Meridian Lite/1775/42).

##### Arm 2 - THE DEFECT: a second attachment in the SAME thread.  PASS - the defect is FIXED
Same thread, sandbox session now warm. Attached uat-note.pdf (1,450 B) whose first line is a canary
I authored: "ATTACHMENT HYDRATION CANARY LINE 7734."
Run ce47d6e9, status completed. Agent's verbatim listing IN THAT SECOND RUN:
    ['ead69eda-Meridian-Q4-pricing.xlsx', 'cdcb5fc8-uat-note.pdf']
BOTH filenames present. The shipped defect was ['...xlsx'] ALONE.
And it quoted the PDF: "ATTACHMENT HYDRATION CANARY LINE 7734."
Rounds observed: 2 execute_code calls (the defect run took 12, reaching the file at round 10 via the
workspace_read fallback). Recorded as an observation, NOT a pass condition.

##### Arm 3 - the ordering/type confound, RESOLVED.  PASS
SECOND fresh thread dbc428fb, uat-note.pdf as its FIRST attachment. Run 72695719, completed, 1 round.
    ['549b4e15-uat-note.pdf']   + first line quoted verbatim.
=> A fresh thread's first .pdf hydrates fine. This licenses the sentence "the SECOND attachment did
   not hydrate" rather than ".pdf does not hydrate", and 244-10's fix is the whole story.

##### Arm 4 (244-14 / review WR-03) - a failed copy must not go quiet.  PARTIAL: steps 1-2 PASS, 3-4 FAIL
Failure induced HONESTLY, by the row's own sanctioned method - no code was edited. A third
attachment big-canary.csv (1,077,795 B) was attached to thread 953f973c; because it exceeds
DEFAULT_INLINE_THRESHOLD (256 KB, workspace_service.py:35) it is the only one of the three stored in
Supabase Storage rather than inline. Its storage.objects row was renamed aside (reversibly) BEFORE
any execute_code ran, so the workspace_files row still lists and only the content read fails.

| # | Step | Expected | Measured | Verdict |
|---|---|---|---|---|
| 1 | attach 2, break the SECOND, run execute_code | the tool result names the file; the others still arrive | Note verbatim: "Could not load the attached file 6f8bd7dd-big-canary.csv into /sandbox/attachments/ (StorageApiError). It is still readable with workspace_read if it is a text file." Listing still carried the .pdf and .xlsx | PASS |
| 2 | run a SECOND time, still broken | the note is PRESENT AGAIN (the half that was silent) | present again, byte-identical | PASS |
| 3 | run a THIRD time | the note is GONE - given up on, deliberately and once | **note STILL PRESENT.** Also present on attempts 4, 5 and 8 - five separate execute_code calls, never once absent | **FAIL** |
| 4 | repair Storage after ONE failure, run again | the file ARRIVES - a blip does not cost the file for the session | storage.objects restored and VERIFIED downloadable (GET /storage/v1/object/workspace-files/... -> HTTP 200, 1,077,795 bytes, service role). The very next execute_code still reported StorageApiError and the CSV still did NOT appear in the listing | **FAIL** |

### What was ruled OUT by measurement, so the finding is not guesswork
- The CONTAINER persists across calls: a marker file appended once per run accumulated
  (1789229711.99 then 1789229820.63 - 2 lines on the next run).
- The SUCCESS record (`_hydrated_files`) persists and works: the container copy of the .pdf was
  overwritten in-container with the bytes b'TAMPERED', and the NEXT execute_code still read
  b'TAMPERED' - i.e. the already-copied file was correctly NOT re-copied. So `already` is doing its
  job for succeeded paths.
=> Therefore the fault is specific to the FAILED-path bookkeeping, not to session lifetime and not
   to the success path.

### Consequence, stated at its real size
The half WR-03 most cared about WORKS: the failure is NAMED on every attempt, so the ten-wasted-round
silence that cost L-5 cannot recur. What does NOT hold is the cap's own contract, in BOTH directions:
the give-up never fires (unbounded retries + an endlessly repeated note), and - the one that matters
to a person - **a transient Storage failure still costs the file for the session even after the
transient clears**, which is the exact sentence step 4 exists to disprove.
Severity: MAJOR.

### Cause: NOT ESTABLISHED. Recorded as a candidate, never as a fact.
Leading candidate is multi-worker state split: `_sessions`, `_hydrated_files` and
`_hydration_failures` are module-level / weak-keyed PER PROCESS (sandbox_service.py:16-17,
tool_dispatcher.py:1838), while `WORKER_COUNT=2` is this project's default and
`_find_existing_container` lets a second worker RE-ATTACH to the same container - which would
reproduce exactly what was measured (one container, but per-worker counters that each advance at
half rate). This project has already recorded this hazard class at config.py:1346 ("with
WORKER_COUNT=2 the Continue-handling worker may differ from the one that hit [it]").
It does NOT by itself explain step 4, so it is a lead, not a diagnosis.
LIMITATION of the induction: the outage was induced by renaming the storage.objects row rather than
by a network transient. That is the method the row itself sanctions, but a backend that caches
anything keyed to that row would look the same - worth ruling out before fixing.

##### Environment - rows created, named rather than silently left
threads 953f973c (3 attachments) and dbc428fb (1) - operator's LOCAL db only.
workspace_files /ead69eda-Meridian-Q4-pricing.xlsx, /cdcb5fc8-uat-note.pdf, /6f8bd7dd-big-canary.csv,
/549b4e15-uat-note.pdf. LEFT IN PLACE deliberately - 24 h TTL, which the row explicitly permits;
deleting the operator's rows is the more destructive option.
documents rows created: **0** - SHELL-04's negative guarantee re-confirmed.
storage.objects: the renamed object was RESTORED and verified (HTTP 200).

##### ROW VERDICT: Arms 1-3 PASS (defect 6b CLOSED, confound resolved); Arm 4 steps 1-2 PASS,
#####              steps 3-4 FAIL -> a NEW gap.

#### R2-3 (244-11, gaps G-3 + G-4) — DRIVEN 2026-09-12, Chrome MCP, thread 261d5f57

Method: window.fetch patched to answer every /snapshot with the endpoint's own 503
({"detail":"Streaming infrastructure unavailable"}, Retry-After: 10). Armed via navigate_page
initScript so it is in place BEFORE any app script runs. No Redis outage was seeded.

MEASURED SIDE-FACT worth recording: /snapshot is the endpoint that CARRIES THE TRANSCRIPT. With the
patch armed and the localStorage stream cache intact the thread still rendered 22,491px of messages
FROM CACHE. Only after clearing `agentic-rag.streams.v1.<uid>` is the transcript genuinely empty.
That is why BOTH sentences are reachable, and why each had to be driven separately.

##### Arm 1a — cache present (transcript NOT empty, sh 22491 / ch 513)
sentence: "Couldn't load latest messages. Showing cached version."   left 534  visible
Retry button: present, enabled, left 1056.5
=> the sentence is TRUE of the state: cached content really is on screen.

##### Arm 1b — cache cleared, transcript GENUINELY EMPTY (sh 513 == ch 513)   <-- the row's stated case
sentence: "Couldn't load this conversation. It's still there — try again."   left 534  visible
  ⭐ VERBATIM MATCH to 244-11-UAT-ROW.md Arm 1, and it is NOT "Showing cached version".
Retry button: present, enabled, not disabled, left 1056.5
sweep (leaf elements matching /unavailable|error|failed|retry|try again|something went wrong/i,
  STYLE excluded): 2 hits INSIDE the chat column (banner at left 534, Retry at left 1056.5),
  against aside.left 1142.4. Pre-fix the same sweep returned ZERO.
VERDICT: PASS

##### Arm 2 — Retry recovers
window.fetch restored, Retry clicked (no manual refresh):
  transcript 513 -> 22491 (populated), banner sweep -> [], Retry button gone.
VERDICT: PASS

##### Arm 3 — the negative
Full reload with NO patch, opened an ordinary thread ("Wolf, Goat, Cabbage River Puzzle"):
  transcript sh 5334, banner sweep -> [], no Retry anywhere.
VERDICT: PASS  (a signal nobody will trust after the first false one — no false one fired)

##### Arm 4 — G-4's live discriminator, on the failed open (Arm 1b)
  welcome "How can I help you?"          : FALSE
  welcome chips (Search connected files /
    Draft a team update)                 : FALSE
  thread title "UAT 243 L-2" rendered    : TRUE
  composer present                       : TRUE
  transcript empty + NEW banner          : TRUE
=> THREAD FRAME, not the welcome pane. Per the row's own table this means THE CLICK DID SELECT,
   and the snapshot-503 path is the live cause of the reported "first click opens empty" symptom.
   G-4's ruling: the 2026-09-12 observation and 244-01's traced mechanism are the SAME defect
   surfacing; the trace's §6 "second cause" reading is what the browser supports.
   ⛔ This does NOT close BUG-260911-02 — its own re_open_trigger (right-hand-third click on a real
   row, UNPATCHED) is a different visit and was not driven here. status stays `folded`.
VERDICT: PASS

##### ROW VERDICT: PASS — G-3 closed; G-4 ruled (same defect), BUG-260911-02 stays folded.

#### R2-4 (244-12, gap G-6 / SHELL-03 / BUG-260828-07) — DRIVEN 2026-09-12, Chrome MCP

Fixture: 200-Word Essay Writer, step 2 "act" marked needs-approval, pending action send_email over SMTP.
OPERATOR SAFETY HELD: the step is outward-facing and irreversible. BOTH runs were settled with
"Do not run it" + Send Answer. "Approve this step" was NEVER clicked.
Two real runs driven: b713430f (thread d747b28e) and be8fca20 (thread cf36c3d4).

##### Arm 1 — the blocker, re-driven.  PASS — this is the fix landing.
aside.left = 1136.8 (the panel's own edge). Controls measured by POSITION, not by testid:

| Control | L-4 measured (before) | measured NOW | where |
|---|---|---|---|
| Approve this step | 1103 (IN PANEL) | 546.8 | CHAT COLUMN |
| Do not run it | 1103 (IN PANEL) | 546.8 | CHAT COLUMN |
| Send Answer | 1244 (IN PANEL) | 988.4 | CHAT COLUMN |
| (panel's own copies) | - | 1158.4 / 1158.4 / 1298.8 | still in the panel, unchanged |

anyApprovalControlInChatColumn: TRUE (was FALSE at L-4)
chat_column_renders_the_question: TRUE - verbatim, at left 546.8:
  'Step 2 of 2, "act", is about to run. This step is marked as needing your approval first.
   The run is waiting here and will not continue until you answer. ...'
chatColumnHasPausedCue: FALSE - recorded as an OBSERVATION, exactly as the row said to. The
  PausedRunCue stayed at message level by decision; a workflow pause has no row to mark.
screenshot: R2-4-arm1-chat-approval.png - the chat card renders question + both choices + a REASON
  box + Send Answer, with the composer beneath reading "Workflow running - Cancel to switch back".

##### Arm 2 - both directions settle.  FAIL, in BOTH directions, for ONE cause.

### Direction 1 - answered from the THREAD (run b713430f)
Clicked "Do not run it" then "Send Answer" in the CHAT column.
SERVER: correct. messages gained role='system' tool_calls=[{kind:'ask_user_response',
  choice_index:1, ...}]; workflow_runs.status -> 'failed'; threads.active_workflow_run_id -> NULL.
CHAT: correct - its three controls disappeared.
PANEL: WRONG - still rendered "NEEDS YOU ... the run is waiting for your answer and will not
  continue on its own" with live Approve / Do not run it / Send Answer, re-read at +14s and +3min.
RUN LINE: WRONG - still data-run-line-state="live", clock still climbing (2m 35s) on a finished run.
COMPOSER: WRONG - still disabled, still "Workflow running - Cancel to switch back".
A manual reload fixes everything: panel reads "Phase 2 of 2, act, failed", zero controls anywhere,
no run line, composer enabled "Ask anything...".
=> thread_to_panel_settles_without_refresh: FALSE
   panel_sentence_read (verbatim, ~3 min after the answer was accepted):
     "NEEDS YOU - No deadline - the run is waiting for your answer and will not continue on its own"

### Direction 2 - answered from the PANEL (run be8fca20) - the MIRROR IMAGE
Clicked "Do not run it" then "Send Answer" in the PANEL.
PANEL: correct - reads "ANSWERED . AGENT RESUMED", its controls gone.
CHAT: WRONG - all three controls still rendered at left 546.8 / 988.4, re-read at +6s and +20s.
RUN LINE / COMPOSER: same two wrongs as Direction 1.
=> panel_to_thread_clears_controls: FALSE ; thread_row_reads_settled: FALSE

### Severity, scoped rather than inflated
MAJOR, not blocker. The stale "Approve this step" cannot re-send the declined email: the server's
anchor confirm (runs.py:775-785) requires threads.active_workflow_run_id == run_id and that column
is NULL once the run settles, so a second answer 404s. The defect is a LIE ON SCREEN plus a
composer the person cannot use until they guess that a refresh is needed - not a data hazard.
Stated as reasoning over source + the measured NULL anchor. It was NOT driven by clicking the
stale Approve, because doing so risks a real outward-facing SMTP send to prove a negative.

### Root cause, one sentence
Each home settles its OWN state and neither invalidates the other's, and nothing re-reads the thread
lock after the answer - so the run line and the composer keep a lock the server has already dropped.
This is decision D-v2.5-03 verbatim ("Realtime is a hint, not truth - always reconcile via fetch on
(re)connect") not applied to the answer transition.

##### Arm 2 - the THIRD home: PASS
WorkflowRunPage rendered its own PendingAskCard correctly for run be8fca20 - "NEEDS YOU" plus all
three controls (left 1144 / 1144 / 1288.4), advancing spine "wrote an answer" / "Sends an email".

##### Arm 3 - the G-2 order, on screen (this is also R2-6)
Thread "UAT244-B X-1 multi-tool" - an assistant turn with BOTH a tool call and reasoning.
ONE getBoundingClientRect pass:
  tool-call container [data-testid="run-card"]         top = 290.6
  thinking badge      [data-testid="thinking-trigger"] top = 434.6
  answer prose                                         top = 480.3
order_observed: tools -> thinking -> answer. The badge's top is GREATER. The operator's requested
  order is what is on screen.
badge still expands: YES - aria-expanded false -> true, rendered text grew 28,740 -> 29,805 chars
  and the reasoning body appeared.
badge duration: reads "Thinking" on a REPLAYED thread. Not a regression - ThinkingBlock.tsx:149-155
  thoughtForLabel(undefined) === "Thinking" is the designed honest fallback when the client-only
  reasoningMs is absent. A freshly streamed turn was observed reading "Thought for 1 second"
  (Capseed thread, same session).
operator_confirms_override: PENDING - this needs the operator's word, not mine. It reverses a
  rationale the operator approved at Phase 243 and was changed on the operator's own live direction
  during 244's UAT. The row requires it be presented as an override awaiting confirmation.

##### ROW VERDICT: Arm 1 PASS (G-6 closed - SHELL-03's headline defect is fixed);
#####              Arm 2 FAIL (a NEW gap: neither home reconciles the other);
#####              third home PASS; Arm 3 PASS on the order, operator confirmation owed.

#### R2-5 (244-13, gap G-1 + WR-07) — DRIVEN 2026-09-12, Chrome MCP

##### Setup — the cap state is SEEDED, recorded not hidden
The OLD fixture (thread 68818c2a / run 05fea6e9) could NOT be reused: measured, that run now reads
status='failed' and a LATER completed run (7c7d424a) sits after it, so it is no longer the thread's
latest run and no lock would apply. runs by status across the whole local DB, re-measured today:
completed 1310 / failed 172 / cancelled 53 / timed_out 8 / **cap_paused 0**.
So a NEW real Deep run was driven in the browser ("Say the single word: capseed", deepseek-v4-flash,
General/Deep mode) -> thread ee63dffa-f25c-4dc2-bad7-9b1b9b5214f8, run e8f1bcc0-93ab-4977-8a6b-cae9cdf13ed3,
which completed normally. It was THEN seeded to status='cap_paused', continues_used=3, plus the
role='system' carrier row with tool_calls [{kind:'iteration_cap_paused', name:'execute_code', ...}]
— the exact rows agent_loop.persist_cap_paused writes (agent_loop.py:308-385).
⛔ This row therefore does NOT prove the cap FIRES correctly (Phase 092's criterion, not SHELL-02's).

##### Arm 1 — G-1 is CLOSED: the phantom is gone
| # | assertion | measured | verdict |
|---|---|---|---|
| 1 | amber Continue card renders | yes, top 419.6 | PASS |
| 2 | document.querySelectorAll('[data-run-line-state]').length | **0** | PASS |
| 3 | leaf sweep /Starting workflow\|Working on phase\|phases? done/i | **0 matches** | PASS |
| 4 | card text verbatim | "Reached the Continue limit — this run is stopped. Start a new message to keep going." (the DEEP wording, correct for a Deep thread) | PASS |
| 5 | SAME rect pass: no element carries data-run-line-state="live" | liveNodes 0 while the card is at top 419.6 | PASS |
| 6 | setInterval instrumented BEFORE the open, 5s wait: no 1000ms registration | **zero intervals of any delay** | PASS |
| 7 | GET /threads/{id}/workflow count, recorded not asserted | **5** on the cap-paused thread vs **6** on an ordinary Deep thread with no lock ("Wolf, Goat, Cabbage River Puzzle"), same instrument, same 5s window | PASS — not higher; the per-assistant-row HarnessOuterBanner cost is gone |

Pre-fix this same thread showed the amber card at top 272 and, 41px below it, a run line reading
"* Starting workflow..." with data-run-line-state="live" plus a 1s clock. None of that is present.

⭐ BONUS, UNFORCED, LIVE EVIDENCE FOR G-3: on the very first open of this cap-paused thread,
`GET /threads/ee63dffa/snapshot` failed for real (net::ERR_FAILED, no patch armed) and the NEW
banner rendered "Couldn't load latest messages. Showing cached version." with a Retry control.
That is G-3's fix catching a genuine failure, not a forced one.
⚠ It also makes the cap_paused <-> snapshot-failure correlation 3 of 3 observations. Still NOT
established as causal — say so, do not write it down as a cause.

##### Arm 3 — SHELL-02's shipped half is preserved
| # | assertion | measured | verdict |
|---|---|---|---|
| 1 | REAL reload | performance.getEntriesByType('navigation')[0].type === "reload" | PASS |
| 2 | composer after reload | disabled=false, readOnly=false, aria-disabled=null, placeholder "Ask anything…" | PASS |
| 3 | typed BY ELEMENT REF, value asserted BEFORE Return | textarea.value === "Say the single word: unlocked" | PASS |
| 4 | Return mints a NEW run that streams | run 69f2b66b-69af-45ea-92ec-1468ac999208 minted 15:46:56Z, status completed; assistant row "unlocked" rendered | PASS |
| 5 | Chat -> Library -> Chat, reopen: composer still enabled, lock does not return | disabled=false, placeholder "Ask anything…", runLineNodes 0 | PASS |
| 6 | Continue-limit sentence still rendered | PASS **scored while capPaused holds** (step 2, post-reload, pre-post): rendered verbatim. After step 4's new run supersedes the pause the card correctly clears — which is EXACTLY what L-2 itself recorded at its own step 5 ("no Continue card ... the lock did NOT return"). The ROADMAP's named anti-fix (deleting the sentence) has NOT been taken. | PASS |

⚠ Observation, not a defect: a reload does NOT restore the selected thread — the app returns to the
chat list and the thread must be re-opened. Recorded because Arm 3 step 1's wording assumes otherwise.

##### Arm 2 — the REAL workflow run still works (the anti-regression half)
Driven on a genuine published-workflow launch (200-Word Essay Writer, thread d747b28e).
| # | assertion | measured | verdict |
|---|---|---|---|
| 1 | a run starts | yes, from the Workflows page Run modal | PASS |
| 2 | run line present, data-run-line-state="live" | live | PASS |
| 3 | step segment advances + elapsed climbs over ~30s | "Working on phase 2 ... Step 2 of 2 . 12s" -> "1m 06s"; the step had advanced from phase 1 | PASS |
| 4 | composer PLACEHOLDER verbatim | "Workflow running — Cancel to switch back" | PASS |
| 5 | composer TITLE attribute byte-identical (D-244-10, BOTH axes) | "Workflow running — Cancel to switch back" | PASS |
| 6 | textarea.disabled | true | PASS |

METHOD CORRECTION, recorded because it nearly became a false finding: a first read of
document.querySelector('textarea') returned placeholder "Type an answer..." and disabled=false, and
that is NOT the composer — it is the REASON box inside the chat-column approval card, which is now
earlier in DOM order. Re-read by enumerating ALL textareas. A single-element query over a surface
that just gained a second textarea is exactly how a fix gets published as a regression.

##### Arm 4 — WR-07: BLOCKED, with the reason (never silently omitted)
Step 1 is not reachable in reasonable time. A harness run hitting its own iteration cap needs 15
tool-calling iterations AND the model to disobey tool_choice="none" at the final one
(agent_loop.py:2104 + :2575). Two genuine workflow runs were driven today; both reached their
approval gate at step 2 of 2 and neither approached a cap. The reconcile route is LATENT by the
row's own finding (no writer of 'cap_paused' onto workflow_runs.status exists) and the row
explicitly forbids seeding one. Steps 2 and 2b therefore could not be observed.
Standing evidence: jsdom fences ChatArea.capPausedComposer.test.tsx D5 and
ThreadRunLineKickoff.test.tsx D5b(a).
VERDICT: BLOCKED — not passed, not failed.

##### ROW VERDICT: PASS on Arms 1-3 (G-1 CLOSED, SHELL-02 shipped half preserved); Arm 4 BLOCKED.

---

## Gaps — round 2 (APPEND-ONLY; the round-1 `## Gaps` block above is untouched)

```yaml
- id: G-8
  truth: "SHELL-03 — answering an approval in one home settles it in the other, with no manual refresh"
  status: failed
  reason: |
    DRIVEN 2026-09-12 on two REAL workflow runs (200-Word Essay Writer, step 2 "act", send_email
    over SMTP; both settled with "Do not run it", never approved). The BLOCKER G-6 reported is
    FIXED — the chat column now carries the question and all three controls (measured at left
    546.8 / 988.4 against the panel edge 1136.8, where L-4 measured every control at >=1103).
    What is NEW: the two homes do not settle each other.
      · Answer from the THREAD  -> chat clears, PANEL still says "NEEDS YOU ... waiting for your
        answer" with live Approve / Do not run it / Send Answer, re-read at +14s and +3min.
      · Answer from the PANEL   -> panel reads "ANSWERED · AGENT RESUMED", CHAT still renders all
        three controls, re-read at +6s and +20s.
      · BOTH directions additionally leave the run line at data-run-line-state="live" with a
        climbing clock, and the composer DISABLED on "Workflow running — Cancel to switch back",
        for a run the server has already finished.
    A manual reload corrects everything (panel "Phase 2 of 2, act, failed", zero controls, no run
    line, composer "Ask anything…"), which is what proves it is stale CLIENT state.
    SERVER-SIDE EVERYTHING IS CORRECT: messages gained role='system'
    tool_calls=[{kind:'ask_user_response', choice_index:1}]; workflow_runs.status -> 'failed';
    threads.active_workflow_run_id -> NULL.
  severity: major
  test: R2-4 (Arm 2, both directions)
  not_a_data_hazard: |
    ⛔ Scoped rather than inflated. The stale "Approve this step" CANNOT re-send the declined
    email: submit_ask_user_response's anchor confirm (runs.py:775-785) requires
    threads.active_workflow_run_id == run_id, and that column is NULL once the run settles, so a
    second answer 404s. The harm is a LIE ON SCREEN plus a composer the person cannot use until
    they guess a refresh is needed. ⚠ Established by reading source + the measured NULL anchor,
    NOT by clicking the stale Approve — doing that risks a real outward-facing SMTP send to prove
    a negative.
  root_cause: |
    Each home settles its OWN state and neither invalidates the other's, and nothing re-reads the
    thread lock after an answer — so the run line and the composer keep a lock the server already
    dropped. This is decision D-v2.5-03 verbatim ("Realtime is a hint, not truth — always
    reconcile via fetch on (re)connect") not applied to the ANSWER transition. The +4 fetches per
    thread open that the list-level PendingAskStack mount already buys (docs/HOT-FILE-LEDGER.md,
    MessageList.tsx) are spent on OPEN and not on SETTLE.
  why_no_fence_caught_it: |
    Every fence 244-12 added proves the approval MOUNTS. The plan said so in its own words —
    "not one of them proves it ANSWERS" — and that is exactly the half that broke. A jsdom mount
    cannot observe two sibling surfaces disagreeing after a network round trip.
  artifacts:
    - frontend/src/components/chat/MessageList.tsx
    - frontend/src/components/panel/PendingAskCard.tsx
    - frontend/src/components/chat/ThreadRunLine.tsx
    - frontend/src/components/chat/ChatArea.tsx
    - backend/app/api/runs.py:775-785
    - "measured: runs b713430f (thread d747b28e) and be8fca20 (thread cf36c3d4)"
  missing:
    - "one settle path that invalidates BOTH homes' pending-ask state and re-reads the thread lock — a fetch-based reconcile, not a second optimistic update"
    - "a driven fence (not jsdom) that answers in one home and asserts the other settles"
```

```yaml
- id: G-7
  status_note: "DEFERRED 2026-09-12 by operator ruling -> SEED-272. Re-open trigger lives in the seed."
  truth: "WR-03 — a failed attachment copy is retried a bounded number of times, then given up on once, and a transient failure does not cost the file for the session"
  status: failed
  reason: |
    DRIVEN 2026-09-12. The half WR-03 most cared about WORKS: the failure is NAMED on every
    attempt — "Could not load the attached file 6f8bd7dd-big-canary.csv into
    /sandbox/attachments/ (StorageApiError). It is still readable with workspace_read if it is a
    text file." — so the silence that cost L-5 ten wasted agent rounds cannot recur (Arm 4 steps
    1 and 2 PASS). The CAP's own contract fails in both directions:
      · step 3 — the note must be GONE on the third attempt (given up on, once). It was PRESENT
        on attempts 3, 4, 5 AND 8 — five separate execute_code calls, never once absent.
      · step 4 — after repairing Storage the file must ARRIVE. The storage.objects row was
        restored and VERIFIED downloadable (GET /storage/v1/object/workspace-files/... -> HTTP
        200, 1,077,795 bytes, service role), and the very next execute_code still reported
        StorageApiError and the CSV still did not appear in the listing.
    ⇒ "a single blip costs the file for the session", the exact sentence 244-14 set out to
    disprove, is still true.
  severity: major
  test: R2-2 (Arm 4)
  ruled_out_by_measurement: |
    Not guesswork — two controls were driven:
      · the CONTAINER persists across calls (a marker file appended once per run accumulated to
        2 lines);
      · the SUCCESS record works (the container copy of the .pdf was overwritten in-container
        with b'TAMPERED' and the NEXT call still read b'TAMPERED' — the already-copied file was
        correctly NOT re-copied).
    So the fault is specific to the FAILED-path bookkeeping, not to session lifetime and not to
    `_hydrated_files`.
  cause_not_established: |
    ⛔ Recorded as a CANDIDATE, never as a diagnosis. `_sessions`, `_hydrated_files` and
    `_hydration_failures` are module-level / weak-keyed PER PROCESS (sandbox_service.py:16-17,
    tool_dispatcher.py:1838) while WORKER_COUNT=2 is this project's default, and
    `_find_existing_container` lets a second worker RE-ATTACH to the same container — which would
    reproduce one container with per-worker counters each advancing at half rate. config.py:1346
    already records this hazard class. It does NOT by itself explain step 4, so it is a lead.
    ⚠ LIMITATION of the induction: the outage was made by renaming the storage.objects row (the
    method 244-10-UAT-ROW.md itself sanctions), not by a network transient. A backend caching
    anything keyed to that row would look identical — rule that out before fixing.
  artifacts:
    - backend/app/services/tool_dispatcher.py:1838-1844
    - backend/app/services/tool_dispatcher.py:2000-2035
    - backend/app/services/sandbox_service.py:16-17
    - backend/tests/unit/test_244_attachment_hydration.py
  missing:
    - "the give-up arm must actually fire — the failure counter must survive wherever the session does, across workers if the container does"
    - "a recovery path: once Storage is healthy again the file must hydrate, not stay failed for the session"
    - "a test driven against a REAL session across more than one execute_code call — every existing case runs on a MagicMock, which cannot show this"
```

---

## Summary — round 2

```
total rows:  6
passed:      4   (R2-1, R2-3, R2-5, R2-6)
partial:     2   (R2-2, R2-4 — each closed its headline defect and surfaced a NEW adjacent one)
blocked:     1 arm (R2-5 Arm 4 / WR-07 — a harness cap is not reachable in reasonable time)
new gaps:    2   (G-7, G-8)
```

⛔ **Requirement status after this round — stated as a decision, not as a claim that all is well:**

| Requirement | Status |
|---|---|
| `SHELL-01` | ✅ **CLOSED** — the frame holds at 8/8 samples and a failed snapshot is now visible |
| `SHELL-02` | ✅ **CLOSED** — the phantom is gone and the shipped half is preserved |
| `SHELL-03` | ⚠ **headline defect closed, requirement NOT closed** — the approval is answerable from the thread (G-6 closed), but answering does not settle the other home (G-8) |
| `SHELL-04` | ⚠ **headline defect closed, requirement NOT closed** — the second attachment hydrates (defect 6b closed), but a failed copy is never given up on and never recovers (G-7) |

⚠ **Owed, and named so it cannot be mistaken for absent:**
- **R2-6 needs the OPERATOR's word.** The thinking-badge re-order is on screen and correct, but it
  reverses a rationale the operator approved at Phase 243. It was changed on that same operator's
  live direction during this phase's UAT. Presented as an override awaiting confirmation.
- **R2-5 Arm 4 (`WR-07`) is BLOCKED**, not passed — a harness run cannot be driven to its iteration
  cap in reasonable time and the row forbids seeding one.
- **`BUG-260911-02` stays `folded`.** R2-3 Arm 4 ruled that the snapshot-503 path is the live cause
  of the reported symptom, but the report's own `re_open_trigger` (a right-hand-third click on a
  real row, unpatched) is a different visit and was not driven.

---

## Operator rulings — 2026-09-12, at the close of round 2

**R2-6 / `G-2` — the thinking-badge order: OVERRIDE CONFIRMED.** The operator confirmed the badge
stays **below** the tool-call container. The Phase-243 locked rationale (*"ORDER IS THE ORDER IN
TIME — above the run card's tool rows and above the answer"*, `MessageItem.tsx:477`) is therefore
**consciously superseded by an operator ruling**, not drifted from. ⛔ It is superseded, not deleted:
a later phase that finds the old rationale in source must read it as history, and
`ThinkingBlock.characterization.test.tsx` §12 keeps pinning the mount site on source.
`G-2` is **CLOSED**.

**Round 2 scope: `G-8` only.** The operator's call, on the evidence that `G-8` is the sole unmet
ROADMAP success criterion (**SC#3**'s second clause, *"and answering it in either home settles it in
both"*) while `G-7` is a robustness gap adjacent to a criterion that is met. `G-7` is **DEFERRED to
[`SEED-272`](../../seeds/SEED-272-a-failed-attachment-copy-never-gives-up-and-never-recovers.md)** with a concrete re-open trigger rather than left open here — a deferral with no re-open
is a deletion that looks like a decision.
