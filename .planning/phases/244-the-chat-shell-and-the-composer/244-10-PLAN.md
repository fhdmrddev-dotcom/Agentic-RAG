---
phase: 244-the-chat-shell-and-the-composer
plan: 10
type: tdd
wave: 1
# G-7's detector (scripts/check-gap-closure-rounds.cjs) keys on `gap_closure`, NOT on
# `gap_closure_round`. Measured while authoring this plan set: with only the round field
# present the gate read `0 gap-closure` and a future round 2 would never have been capped.
gap_closure: true
gap_closure_round: 1
depends_on: []
files_modified:
  - backend/app/services/tool_dispatcher.py
  - backend/tests/unit/test_244_attachment_hydration.py
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-10-UAT-ROW.md
autonomous: true
requirements: [SHELL-04]

must_haves:
  truths:
    - "A file attached to a thread that ALREADY has a live sandbox session lands in /sandbox/attachments/ on the next execute_code call — the SECOND attachment is reachable, not only the first."
    - "A file already copied into a session is NOT copied again — WR-02's DoS mitigation survives the fix, per FILE rather than per session."
    - "The per-session copy budget is still bounded: a thread cannot exceed the 50-file cap by attaching files across several calls."
    - "A thread with no attachments still performs no mkdir, no copy, no note — the zero-attachment path costs exactly nothing (Deep and Harness share this handler)."
    - "The expiry gate is still decided in ONE place (ws_list_files' SQL predicate) — the incremental copy adds no second expiry rule."
  artifacts:
    - path: "backend/app/services/tool_dispatcher.py"
      provides: "a per-session record of WHICH attachments were copied, not merely THAT hydration ran"
      contains: "_hydrated_files"
    - path: "backend/tests/unit/test_244_attachment_hydration.py"
      provides: "RED-driven proof that a file attached after the session exists is copied, and that an already-copied file is not"
  key_links:
    - from: "backend/app/services/tool_dispatcher.py::_handle_execute_code"
      to: "_hydrate_thread_attachments"
      via: "the per-session copied-path set, consulted every call instead of a boolean once"
      pattern: "_hydrated_files"
---

<objective>
Close the **L-5 defect 6b** hole in SHELL-04: *"the SECOND attachment in a thread never reaches
`/sandbox/attachments`."*

The agent's own round-4 output is the evidence:

    /sandbox/attachments [] ['c679b991-Meridian-Q4-pricing.xlsx']

— the directory holds only the FIRST file. It then burned rounds 5-12 searching the filesystem and
recovered the second file at round 10 via the `workspace_read` fallback, ~49 s later.

⭐ **The cause is now IN THE CODE and documented there, which is why this is a fix and not an
investigation.** `244-07` closed WR-02 by moving the hydration marker from the per-iteration
`ToolContext` onto the SANDBOX SESSION (`_hydrated_sessions`, a `WeakSet`,
`tool_dispatcher.py:1802` + the call site at `:2048`). That was right for the DoS arm it named and
**wrong for this one**: `SandboxSessionManager` caches the session per `thread_id` until idle
eviction, so once a session is marked, *no later attachment is ever copied into it*.

⛔ **SAY "the second attachment did not hydrate", NEVER ".pdf does not hydrate".** The UAT's single
run confounds ordering with file type; that distinction is written into the row and must survive.

Purpose: SHELL-04's criterion is *"a person attaches a local file **and the agent can use it**"*.
It is true for the first attachment and only ACCIDENTALLY true for later ones — it survived the
UAT because `workspace_read` exists, at a cost of ten wasted rounds.
Output: a marker that records WHICH files were copied instead of merely THAT hydration ran.

⛔ **This round does not re-open the rest of SHELL-04.** L-5 steps 1-5 and 7 passed, L-6's six
steps all passed, and defect 6a was **RETRACTED the same session** (the chip is LATE, not absent —
severity MINOR). None of that is touched here.

**Solo run (D-244-21 / OV-SOLO-01):** no independent reviewer exists. This is a SELF-verification.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then `git rev-parse HEAD` —
MUST be `760c758fc` or a descendant on `develop`. ⚠ A fresh worktree starts on the DEFAULT branch
(`master`, at the v3.9 ship commit). Assert the SHA.
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

**⚠⚠ THE BACKEND BASELINE IS AT ITS CEILING — 71 failed, ZERO HEADROOM.**
Canonical command, in `backend/` with the venv:
`pytest tests/unit -q --continue-on-collection-errors`
⛔ **Capture the failing test-id SET by filename BEFORE you start, and DIFF SETS.** Count with
`grep -c`; ⛔ **never `| tail`** — that keeps a count and throws the set away, and it published a
false baseline of 71 when the truth was 72. A one-in/one-out swap is invisible to a count.

**⚠ DB-MUTATING TESTS: SERIALIZE.** Worktrees isolate files, not Postgres. Every test here MUST be
stubbed against fakes (`ws_list_files`, `get_file_by_path`, `_get_file_content`, the session
double) exactly as `test_244_attachment_hydration.py` already does. ⛔ Do not read or write the
live local Supabase in a unit test.

**⚠ THE FRONTEND IS UNTOUCHED BY THIS PLAN.** `git diff --stat -- frontend/` must be EMPTY. ⛔ Do
**not** run the vitest count gate for this plan: the concurrent wave-1 plan `244-09` is running it,
and CLAUDE.md measures the shared gate going non-deterministic at three concurrent test-running
agents. One frontend gate per wave, owned by `244-09`.

**⛔ REGISTRY MERGE ORDER.** This wave runs `244-09` and `244-10` concurrently; both append into
`docs/HOT-FILE-LEDGER.md` and `CLAUDE.md`. **`244-09` merges FIRST, whole.** Rebase this plan onto
that merge and **re-apply its ledger row + section by hand**. ⛔ No automatic resolution on the
registry files. Re-run `node scripts/check-hot-file-ledger.cjs 244` and
`node scripts/check-claude-md-size.cjs` from the repo root AFTER the second merge.
</execution_context>

<context>
@.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-REVIEW.md
@CLAUDE.md

⛔ **Read `244-UAT.md` test row 5 (L-5) in full** — the step table, `defect_6b_second_attachment_
never_reaches_the_sandbox`, `defect_6a_RETRACTED_chip_is_LATE_not_ABSENT` (so the retraction is not
re-litigated) and `naming_defects_deliberate_storage_leaking_into_copy` (⛔ **do not "fix" the uuid
prefix or the `template_input` kind** — both are deliberate storage decisions verified in source).
⛔ **Read `244-REVIEW.md` § WR-02** — it is the finding whose fix created this hole, and its
reasoning about the `MagicMock` truthiness trap is why the marker is membership-based.

@backend/app/services/tool_dispatcher.py
@backend/tests/unit/test_244_attachment_hydration.py
@docs/HOT-FILE-LEDGER.md

<interfaces>
The marker as shipped (`tool_dispatcher.py:1802`):

    _hydrated_sessions: weakref.WeakSet = weakref.WeakSet()

Its call site (`tool_dispatcher.py:2047-2054`), inside `_handle_execute_code`:

    _attachment_notes: list[str] = []
    if session not in _hydrated_sessions:
        try:
            _hydrated_sessions.add(session)
        except TypeError:
            logger.warning("attachment hydration: session is not weak-referenceable")
        _attachment_notes = await _hydrate_thread_attachments(ctx, session)

The helper's signature and the two facts that make an incremental copy cheap
(`tool_dispatcher.py:1834-1908`):

    async def _hydrate_thread_attachments(ctx: ToolContext, session) -> list[str]

  - it ALREADY re-lists every call it is invoked on, via
    `await ws_list_files(ctx.pool, thread_id=UUID(ctx.thread_id))`;
  - each row's identity is `row.get("path")`, the workspace path — unique per row and stable
    (`workspace.py:336` mints `/{uuid4().hex[:8]}-{safe_name}`), so it is a sound copy key.

The bound it applies today (`tool_dispatcher.py:1779, 1867-1872`):

    _ATTACHMENT_HYDRATION_MAX_FILES = 50   # applied to len(rows) on a single call

The lifetime fact this whole gap turns on, from CLAUDE.md: sandbox sessions are cached per
`thread_id` by `SandboxSessionManager.get_or_create` until idle eviction (default 30 min).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — a file attached after the session exists is never copied</name>
  <files>backend/tests/unit/test_244_attachment_hydration.py</files>
  <read_first>
    - `backend/tests/unit/test_244_attachment_hydration.py` — the whole file, once; reuse its `_FakeExecResult` double, its stubbing style and its existing fixtures
    - `backend/app/services/tool_dispatcher.py:1770-1910` and `:2010-2060` — the marker, the helper and the call site
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — L-5's `defect_6b_…` block
  </read_first>
  <behavior>
    - **Test A (the gap, MUST be RED before Task 2):** one session, TWO `execute_code` calls. The
      first call's listing returns `[fileA]`; the SECOND call's listing returns `[fileA, fileB]`
      (fileB is the attachment added between calls — the shape the product produces). Assert
      `fileB`'s bytes were copied into `/sandbox/attachments/` by the end of call 2.
      ⛔ Assert on the **copy target paths actually passed to `copy_to_runtime`**, not on a note
      string and not on a call count — the words are not the deliverable here, the file is.
    - **Test B (WR-02 survives, per FILE):** in the same two-call run, `fileA` is copied
      **exactly once** across both calls. Today's marker gets this right and it must keep being
      right; this is the case that stops the fix from becoming "re-copy everything every call".
    - **Test C (the budget is per SESSION, not per call):** with `_ATTACHMENT_HYDRATION_MAX_FILES`
      monkeypatched to a small value (e.g. 2), three calls each adding one new file copy at most
      that many files in total, and the truncation is NAMED in the returned notes. ⛔ Without this
      the cap becomes per-call and a thread can exceed it by attaching across calls — the DoS arm
      of `T-244-02-05`, re-opened by the fix that closes this gap.
    - **Test D (the zero-attachment path still costs nothing):** a thread whose listing returns
      `[]` performs **no** `mkdir`, **no** copy and returns **no** note, on every one of three
      consecutive calls. ⛔ Deep and Harness share this handler; a listing that is empty must not
      become a per-call `mkdir`.
    - **Test E (ONE expiry gate):** an expired row absent from the listing is never copied, and
      `tool_dispatcher.py` gains **no** second expiry predicate. Re-drive the shipped
      `test_hydration_adds_no_second_expiry_rule` shape against the new code rather than deleting it.
  </behavior>
  <action>
Add the five cases above to `backend/tests/unit/test_244_attachment_hydration.py`.

⛔ **Re-drive, never delete, the two shipped cases the marker's shape invalidates:**
- `test_hydration_runs_once_per_session` (`:186`) asserts `session in
  tool_dispatcher._hydrated_sessions`. That is an assertion about the MARKER'S TYPE, and the type
  is what changes. Re-aim it at the property that actually matters — **the same file is copied
  once per session** — and record in the case's docstring that the old assertion was a membership
  check on a `WeakSet` and why it was replaced. ⛔ Do not delete the case; a per-file count is
  strictly stronger than the boolean it replaces.
- `test_hydration_runs_once_across_agent_loop_iterations` (`:209`) — same treatment. Its claim
  ("eight iterations do not copy the same 10 MB attachment eight times") is still exactly right;
  only its instrument moves.

⚠ Keep the `MagicMock` trap WR-02 recorded in view: `getattr(mock, flag, False)` on a `MagicMock`
returns a truthy child mock, which is why the marker is membership-based rather than an attribute
on the session. Any new marker must keep that property — membership/keying, never `getattr`.

⛔ **Run the suite and SEE TEST A RED**, quoting the verbatim failure in the SUMMARY. A case that
has never failed proves nothing. Tests B, D and E should be GREEN at this point (they describe
shipped behaviour); Test C will be RED or green depending on the shipped cap's placement — record
which, verbatim, rather than assuming.
  </action>
  <acceptance_criteria>
    - `cd backend && pytest tests/unit/test_244_attachment_hydration.py -q` runs, and Test A FAILS with a message quoted verbatim in the SUMMARY naming `fileB` absent from the copy targets.
    - The SUMMARY records, per case, whether B/C/D/E were green or red at RED time — measured, not assumed.
    - `git diff -- backend/app/services/tool_dispatcher.py` is **EMPTY** at the end of this task (RED is driven against unmoved production code).
    - Neither shipped case name is deleted: `grep -c "def test_hydration_runs_once_per_session\|def test_hydration_runs_once_across_agent_loop_iterations" backend/tests/unit/test_244_attachment_hydration.py` returns `2`.
    - No test touches a real database: `grep -c "supabase.table\|asyncpg.connect\|create_pool" backend/tests/unit/test_244_attachment_hydration.py` returns `0`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/unit/test_244_attachment_hydration.py -q</automated>
  </verify>
  <done>Test A is RED against unmoved production code, with its failure text quoted; the four control cases are written and their starting colour recorded.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — the marker records WHICH files were copied, not merely THAT hydration ran</name>
  <files>backend/app/services/tool_dispatcher.py, backend/tests/unit/test_244_attachment_hydration.py</files>
  <read_first>
    - `backend/app/services/tool_dispatcher.py:1770-1910` (the marker + the helper) and `:2010-2060` (the call site) — re-read only the ranges you are editing
    - `backend/tests/unit/test_244_attachment_hydration.py` — Task 1's five cases
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-REVIEW.md` § WR-02
  </read_first>
  <action>
Replace `_hydrated_sessions: weakref.WeakSet` with a per-session record of copied WORKSPACE PATHS:

    _hydrated_files: "weakref.WeakKeyDictionary[object, set[str]]" = weakref.WeakKeyDictionary()

⚠ Keep every property WR-02's comment block established, and say in the new comment which ones are
inherited rather than re-argued:
- **keyed by the SESSION**, so the record's lifetime is exactly the lifetime of the
  `/sandbox/attachments` directory it describes — a new container (eviction, worker bounce) is a
  new key and re-hydrates, which is correct rather than incidental;
- **weak**, so it cannot pin a session alive;
- **membership/keying, never `getattr`** — the `MagicMock` truthiness trap WR-02 names.

At the call site (`:2047-2054`):
- consult `_hydrated_files.get(session)` for the already-copied set (defaulting to an empty set);
- call `_hydrate_thread_attachments(ctx, session, already=<that set>)` on **every**
  `execute_code` invocation, not only the first;
- have the helper return the set of paths it copied and union it back in. ⛔ Write the record
  BEFORE/around the copy in the same way the shipped code sets its marker before the work — a hard
  failure must not re-attempt container I/O on every subsequent call. Per-FILE failures are already
  named individually by the helper, so nothing is lost.
- keep the `except TypeError` degradation for a session that cannot be weak-referenced: log the
  same warning and fall back to the shipped per-call behaviour rather than breaking the turn.

In `_hydrate_thread_attachments`, take the new `already: set[str]` parameter and:
- filter the listing to rows whose `path` is NOT in `already` **before** the cap is applied;
- apply `_ATTACHMENT_HYDRATION_MAX_FILES` to `len(already) + len(new_rows)` — the **session
  total**, never the per-call count — and name the truncation in the notes exactly as it does
  today. ⛔ This is the DoS arm of `T-244-02-05`: a per-call cap is not a cap.
- when there are no NEW rows, return immediately with **no `mkdir`, no copy, no note** — this is
  what keeps the zero-attachment and the steady-state paths free.

⚠ **NAME THE COST THIS ADDS, in the comment and in the SUMMARY**: hydration previously performed
`ws_list_files` **once per session**; it now performs it **once per `execute_code` call**. That is
one bounded DB listing per call, on a handler that already awaits container I/O — accepted
deliberately, because the alternative (an invalidation signal from the upload door into the
dispatcher) is a second mechanism that can go out of sync, which is how this hole was made.

⛔ **DO NOT CHANGE:** `_attachment_container_path` and its five reductions (T-244-02-02 — the
traversal fence), the `_copy_in` NamedTemporaryFile → `copy_to_runtime` → unlink shape, the
`_output_baseline_seeded` guard beside it (a different claim on a different cadence), the expiry
gate's single home in `ws_list_files`' SQL, or the announcement line in `agent_loop.py`.

Run the suite: all five new cases and both re-driven cases GREEN.
  </action>
  <acceptance_criteria>
    - `cd backend && pytest tests/unit/test_244_attachment_hydration.py -q` — `0 failed`, and Test A passes against the same fixture that produced its RED failure.
    - `grep -c "_hydrated_sessions" backend/app/services/tool_dispatcher.py` returns `0`; `grep -c "_hydrated_files" backend/app/services/tool_dispatcher.py` returns `>= 2` (definition + call site).
    - `grep -c "getattr(session" backend/app/services/tool_dispatcher.py` returns `0` — the `MagicMock` trap is not re-introduced.
    - `grep -n "_ATTACHMENT_HYDRATION_MAX_FILES" backend/app/services/tool_dispatcher.py` shows the cap compared against a SESSION total, and Test C proves it by driving three calls against a patched cap of 2.
    - `git diff -- backend/app/services/tool_dispatcher.py` touches ONLY the marker definition, `_hydrate_thread_attachments`'s signature/filtering, and the `_handle_execute_code` call-site block — `_attachment_container_path` and `_copy_in` are byte-unchanged (assert with `git diff` hunk headers named in the SUMMARY).
    - **Backend gate:** `cd backend && pytest tests/unit -q --continue-on-collection-errors` — the failing test-id **SET** is byte-identical to the pre-work capture (`diff` empty both ways), count `71`. ⛔ Report the SET diff, not the count.
    - `git diff --stat -- frontend/ supabase/` is **EMPTY**.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/unit/test_244_attachment_hydration.py -q</automated>
  </verify>
  <done>A file attached after the sandbox exists is copied on the next `execute_code`; an already-copied file is not; the cap is a session total; the backend failing SET is unchanged.</done>
</task>

<task type="execute">
  <name>Task 3: The ledger row, and the real-run row that scores this</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md, .planning/phases/244-the-chat-shell-and-the-composer/244-10-UAT-ROW.md</files>
  <read_first>
    - `docs/HOT-FILE-LEDGER.md` § `backend/app/services/tool_dispatcher.py` — its whole section
    - `CLAUDE.md` § the scan-list row for `backend/app/services/tool_dispatcher.py` (reads `83 / 35 / 4913`)
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — L-5's fixture list and the `Fixtures left in the local dev environment` table
  </read_first>
  <action>
**(a) The ledger.** `tool_dispatcher.py` FIRES G-5. Re-derive its triple from the repo root with
CLAUDE.md's recipe, **subtracting six-digit dated quick-task buckets**. Measured at planning time:
`83 commits / 35 phases (37 before subtracting dated buckets) / 4913 lines` against a row reading
`83 / 35 / 4913` — ⚠ re-derive anyway, because this plan changes the line count and because this
ledger's own recurring finding is that a cell read at a close is wrong by the afternoon.

Expect **honoured by construction** and say why in the cell (<= 200 chars): the marker changes
TYPE, no new call site, no new handler, the traversal fence and the copy shape byte-unchanged. Put
the reasons in the section: WR-02's fix was right for the arm it named and created this one, and
the invariant that now holds is *"the record says WHICH files, so a claim about hydration can
never again outlive the files it was about."* Update row and section **in the same commit**.

**(b) The real-run row.** ⛔ A unit test with a fake session cannot prove a file lands in a real
container. Create `244-10-UAT-ROW.md` with ONE row, unfilled:

- **Setup:** a FRESH thread. Attach `Meridian-Q4-pricing.xlsx`. Send a prompt that forces
  `execute_code` (*"What is in the attached file? List every row."*). Wait for the run to reach
  `completed`.
- **Then, in the SAME thread:** attach `uat-note.pdf` and send *"Read the attached PDF and quote
  its first line."*
- **Pass condition:** the agent's own listing of `/sandbox/attachments` in that second run contains
  **both** filenames, and the answer carries real content from the PDF. ⛔ Score it on the agent's
  printed directory listing, not on whether it eventually answered — recovery via `workspace_read`
  is exactly what masked this defect for ten rounds.
- **Round-count control:** record the number of agent rounds. The defect run took 12; a fixed run
  should reach the file in its first or second tool call. ⛔ This is an observation, not a pass
  condition — a round count is not a contract.
- **The ordering/type confound, resolved:** add a THIRD arm — a fresh thread whose **FIRST**
  attachment is the `.pdf`. It must hydrate. ⛔ This is what lets the phase say *"the second
  attachment did not hydrate"* rather than *".pdf does not hydrate"*, which the UAT explicitly
  flags as NOT ESTABLISHED from its single confounded run.
- **Environment note:** the row creates thread + `workspace_files` rows in the operator's LOCAL DB
  only. ⛔ Name them for deletion, as L-5 and L-6 did. Nothing cloud is touched.

⛔ Every verdict field reads `pending`; the row is driven at `/gsd:verify-work`, not here.
  </action>
  <acceptance_criteria>
    - `node scripts/check-hot-file-ledger.cjs 244` exits `0` AND its output shows a non-zero `subject:` file count — ⛔ a run reporting `subject: 0 files` passed vacuously (Phase 242 measured exactly that on a CRLF plan).
    - `grep -c "backend/app/services/tool_dispatcher.py" CLAUDE.md` returns exactly `1`.
    - The disposition cell for that row is <= 200 characters; `node scripts/check-claude-md-size.cjs` exits `0`.
    - `docs/HOT-FILE-LEDGER.md` § `tool_dispatcher.py` names WR-02 as the fix that created this hole and states the new invariant.
    - `244-10-UAT-ROW.md` exists, has three arms (first attachment, second attachment, pdf-first control), scores on the agent's printed `/sandbox/attachments` listing, and contains no `pass` verdict.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-hot-file-ledger.cjs 244 && node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <done>The ledger row and section are re-derived and carry the finding; the three-arm real-run row exists and is unfilled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-uploaded file → sandbox container filesystem | Attachment bytes and, critically, attacker-controlled FILENAMES cross into a container path. |
| thread scope → sandbox session | Only THIS thread's non-expired `workspace_files` rows may be copied into this thread's session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-10-01 | Denial of Service | `_hydrate_thread_attachments` per-session budget | mitigate | `_ATTACHMENT_HYDRATION_MAX_FILES` compared against `len(already) + len(new)` — the SESSION total. Test C drives three calls against a patched cap of 2. ⛔ A per-call cap is not a cap, and making the copy incremental is exactly what would have turned it into one. |
| T-244-10-02 | Denial of Service | repeated copies of one file | mitigate | The copied-path set makes a second copy of the same path impossible; Test B asserts `fileA` is copied exactly once across two calls. This is WR-02's original arm, kept, at per-FILE granularity. |
| T-244-10-03 | Tampering | `_attachment_container_path` traversal fence | accept (unchanged) | Byte-unchanged by this plan; the shipped parametrised traversal cases (`test_traversal_cannot_escape_the_attachments_directory`) still run and must stay green. Asserted via the `git diff` hunk list. |
| T-244-10-04 | Information Disclosure | cross-thread / expired file leakage | mitigate | Membership is still decided ONLY by `ws_list_files`' SQL predicate (`thread_id` + `expires_at IS NULL OR expires_at > now()`); the incremental filter is applied to that listing's output and adds no second rule. Test E re-drives the no-second-expiry-rule fence. |
| T-244-10-05 | Elevation of Privilege | the copied-path set as a cache key | accept | The set is keyed by the SESSION OBJECT (weak, identity-based), and a session is already thread-scoped by `SandboxSessionManager`; a path string from thread A cannot reach thread B's set without a session-cache defect that is out of this plan's scope and separately fenced. |
| T-244-10-SC | Tampering | npm/pip/cargo installs | n/a | **No package is installed.** `git diff -- backend/requirements.txt backend/Dockerfile.sandbox` MUST be empty; no `## Package Legitimacy Audit` is owed because no dependency is added. ⚠ If a dependency is found to be needed, STOP — an install needs the audit table first. |
</threat_model>

<verification>
- `cd backend && pytest tests/unit/test_244_attachment_hydration.py -q` — `0 failed`.
- `cd backend && pytest tests/unit -q --continue-on-collection-errors` — failing test-id **SET**
  byte-identical to the pre-work capture. ⛔ Publish the SET diff, never the count alone.
- `git diff --stat -- frontend/ supabase/` EMPTY; the vitest count gate is **not run by this plan**
  (wave ownership: `244-09`).
- `node scripts/check-hot-file-ledger.cjs 244` (non-vacuous parse) and
  `node scripts/check-claude-md-size.cjs` both exit 0, run from the repo root AFTER the wave merge.
- ⛔ **NOT verified here, and stated as owed:** that a real container receives the second file.
  `244-10-UAT-ROW.md` is driven at `/gsd:verify-work`. This plan may not report SHELL-04 as closed.
</verification>

<success_criteria>
- The per-session marker records copied PATHS; a second attachment reaches the container on the
  next `execute_code`; an already-copied file is not re-copied; the cap is a session total.
- Test A was seen RED against unmoved production code, with its failure text quoted.
- Both shipped marker cases were RE-DRIVEN, not deleted.
- The backend failing SET is unchanged — the ceiling of 71 holds with its zero headroom.
- `tool_dispatcher.py`'s ledger row + section are re-derived in one commit and name WR-02 as the
  fix that made this hole.
- `244-10-UAT-ROW.md` exists with its three arms and no pre-filled verdict.
</success_criteria>

<output>
Create `.planning/phases/244-the-chat-shell-and-the-composer/244-10-SUMMARY.md` when done.
</output>
