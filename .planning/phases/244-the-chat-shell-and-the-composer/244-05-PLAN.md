---
phase: 244-the-chat-shell-and-the-composer
plan: 05
type: tdd
wave: 3
depends_on: ["244-02", "244-03"]
files_modified:
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/components/chat/ChatAttachmentChip.tsx
  - frontend/src/components/chat/ActiveConnectorChips.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/lib/api/documents.ts
  - frontend/src/types/index.ts
  - frontend/src/components/chat/composerCopy.ts
  - frontend/src/components/chat/__tests__/ComposerAttach.composition.test.tsx
  - frontend/src/components/chat/__tests__/ChatAttachmentChip.states.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [SHELL-04]

must_haves:
  truths:
    - "A person attaches a local file from the composer's + menu and sees it as a chip before they send (SHELL-04, sketch 236 winner A)."
    - "The chip says `this chat only · 24h` — the promise is made where the file is."
    - "⭐ The scope word SURVIVES into the SENT message, not only the pending composer chip (D-244-22's build obligation — a build that puts it solely in the composer has shipped B's weakness with A's cost)."
    - "A refused file shows the rejected FILENAME, the SERVER's verbatim 422 sentence (never a paraphrase, COPY.engine.REFUSE_*) and a working DISMISS control — the three atoms sketch 236 draws, in that order (D-244-27)."
    - "An expired attachment renders the drawn expired state — struck through, `No longer available` — never a dead link or a silent disappearance (D-244-25)."
    - "The chip is a SIBLING of the connector chip in the EXISTING chips row, not a new region (D-244-26)."
    - "A person with no cloud connection sees the local item alone and it reads correctly (D-244-26, the one-item menu case)."
    - "The confirm word is `Attach`, never `Import` — `Import` is the Library door's word (D-244-23)."
  artifacts:
    - path: "frontend/src/components/chat/ChatAttachmentChip.tsx"
      provides: "the pending / sent / expired chip, one component, three states"
    - path: "frontend/src/components/chat/composerCopy.ts"
      provides: "COPY.a + COPY.shared + COPY.engine PORTED from the sketch, not re-typed"
    - path: "frontend/src/components/chat/__tests__/ComposerAttach.composition.test.tsx"
      provides: "the D-244-27 ORDERED-BLOCK composition fence, driven RED first"
    - path: "frontend/src/components/chat/MessageInput.tsx"
      provides: "the two-item + menu and the hoisted chips row"
  key_links:
    - from: "frontend/src/components/chat/MessageInput.tsx"
      to: "frontend/src/lib/api/documents.ts uploadWorkspaceTemplate"
      via: "the shipped POST /threads/{id}/workspace/files client"
      pattern: "uploadWorkspaceTemplate"
    - from: "frontend/src/components/chat/MessageItem.tsx"
      to: "frontend/src/components/chat/ChatAttachmentChip.tsx"
      via: "the read-only chip above the user's text in the transcript"
      pattern: "ChatAttachmentChip"
    - from: "frontend/src/components/chat/composerCopy.ts"
      to: ".planning/sketches/236-the-file-that-belongs-to-this-chat/COPY.js"
      via: "a ported object, fenced against drift"
      pattern: "this chat only"
---

<objective>
Build the door sketch 236 drew. A person opens the composer's `+`, picks a file off their hard
drive, and sees a chip that says the file belongs to **this chat** — and that sentence rides into
the transcript, where it is still true tomorrow.

Purpose: `SHELL-04`'s local-attach half, criterion 4's first clause. The bytes already land safely
(`244-02` made the agent able to use them); this is the surface that makes the promise legible.

Output: a two-item `+` menu, an attachment chip beside the connector chip, a refusal that carries
the server's own words, an expired state, and the scope word in the sent message.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then
`git rev-parse HEAD` — MUST be `1ff80a1da` or a descendant on `develop`. Wave 3: the base includes
`244-02` (the shared `WORKSPACE_ACCEPT_ATTR` constant this plan consumes) and `244-03`
(`MessageItem.tsx`).
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

**⚠ Gate state INHERITED:** `GSD_VITEST_MAX_WORKERS=2`, gate from the repo root, verdict verbatim.
`sourceComposition.test.tsx` is a standing red in neither knob. `SEED-171`'s five flaky suites exist
— capture filenames from the gate's own JSON before re-running; never touch the cap.
`npx tsc -p tsconfig.app.json --noEmit`, SET DIFF against the **67**-error base.

**⚠ Registry files are append-only** and do NOT create a wave dependency. Take BOTH sides on conflict.

**⚠ D-244-17 DEVIATION — recorded beside the original, never overwriting it. SIX plans, not four.**

D-244-17 locks *"FOUR plans, decomposed by surface seam"*, with plan 2 carrying **all** of the
composer: the cap-pause unlock **plus** the local attach **plus** the cloud re-point **plus** the
Library's folder-asking import. That grouping is not reachable, for the one legitimate split reason
in the planner's own charter — **context cost**, never difficulty:

- **C-2** turns `SHELL-02` from a one-expression frontend change into a **two-layer** change, adding
  a `runs`-read edit on `backend/app/api/threads.py` (244 commits / 81 phases) against a backend
  baseline at its **ceiling of 71 with zero headroom**.
- ⚠ **C-9** (measured during planning, recorded in `244-02`) shows `SHELL-04`'s *"and the agent can
  use it"* clause is unsatisfied for **8 of the 15 accepted extensions** — `workspace_read` returns a
  note for every binary, and the sandbox had no reach. That is a third backend seam inside the same
  requirement.
- `SHELL-04` therefore spans **~16 files across both tiers**: validator category, sandbox hydration,
  system-prompt line, chip component, copy port, composer composition, transcript render, upload
  client, a request model, a route, a frontend client and a Library mount.

One agent carrying that lands far past the ~50% context budget, which is where quality degrades.
**The seam decomposition D-244-17 chose is honoured exactly** — it is `SHELL-04` alone that is cut
into three (**the agent can use it** · **the composer affordance** · **the two doors**), each a
wave-sized unit of 2-3 tasks. ⛔ Nothing was cut to save time: the verifier, the TDD RED drives,
`security_enforcement` / `code_review` and migration discipline all stand (G-8's never-cut list).
⭐ The lever taken instead is G-8's own: **targeted suites per task, FULL gates once per WAVE.**

**⚠ Solo running (D-244-21):** self-review only. "self-verified", never "reviewed".

**⚠⚠ LOAD THE PROJECT SKILL BEFORE WRITING ANY JSX:**
`.claude/skills/sketch-findings-agentic-rag/SKILL.md` — the composer, the chat-surface component
inventory, and `references/icon-convention.md` (provider/model icons are single-source
`@lobehub/icons`; ⛔ do not invent a new mark).

**⭐ G-2 IS DISCHARGED FOR THIS SURFACE AND FIRES ON NOTHING ELSE IN THIS PHASE (D-244-18).**
Sketch 236, winner **A — Scope on the chip** (operator, 2026-09-11), is the acceptance bar.
⛔ `SHELL-05`'s signal is NOT sketched (shipped, F-1); `SHELL-01/02/03` are bug fixes on shipped
surfaces with named causes and need none. **Say this in the SUMMARY explicitly** rather than
sketching everything or nothing.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-PATTERNS.md
@.planning/sketches/236-the-file-that-belongs-to-this-chat/README.md
@.planning/sketches/236-the-file-that-belongs-to-this-chat/COPY.js
@.planning/sketches/236-the-file-that-belongs-to-this-chat/index.html
@.planning/reported-bugs/BUG-260905-01-cloud-import-lives-in-chat-and-dumps-into-library-root.md

<interfaces>
<!-- THE CHIPS ROW THE ATTACHMENT JOINS — MessageInput.tsx:321-327 -->
```tsx
          {/* Active Connector Chips Bar */}
          <div className="px-3 pt-2">
            <ActiveConnectorChips
              connections={connections}
              activeConnectorIds={activeConnectorIds}
              onRemoveConnector={handleRemoveConnector}
            />
          </div>
```

<!-- THE CHIP TEMPLATE — ActiveConnectorChips.tsx (whole file, 61 L). Copy these parts verbatim: -->
```tsx
  if (activeConnectorIds.length === 0) return null          // ← empty ⇒ NOTHING, no reserved space
    <div
      data-testid="active-connector-chips"
      className="flex flex-wrap items-center gap-1.5 px-3 py-1.5 mb-1 bg-muted/40 rounded-lg border border-border/40"
    >
      <span className="text-[10px] uppercase font-semibold text-muted-foreground tracking-wider mr-1">
        Using:
      </span>
        <span
          key={conn.id}
          data-testid={`active-connector-chip-${conn.id}`}
          className={cn(
            "inline-flex items-center gap-1.5 pl-2 pr-1.5 py-0.5 rounded-full text-xs font-medium",
            "bg-background border border-border/80 shadow-xs text-foreground",
            "hover:border-border transition-colors animate-in fade-in zoom-in-95 duration-150",
          )}
        >
          <ConnectionMarkGlyph shape={conn} size="chip" />
          <span className="truncate max-w-[140px]">{conn.name}</span>
          <button type="button" aria-label={`Remove ${conn.name}`} …><X className="h-3 w-3" /></button>
        </span>
```

<!-- THE MENU BEING REPLACED — MessageInput.tsx:363-379. ONE item today, and it is the wrong one. -->
```tsx
                <DropdownMenuContent align="start" side="top" className="p-0 border-border/80 shadow-xl overflow-hidden mb-1">
                  {hasCloudStorage && (
                    <div className="p-1 border-b border-border/50">
                      <DropdownMenuItem
                        onSelect={() => { setPlusMenuOpen(false); setFilePickerOpen(true) }}
                        className="text-xs cursor-pointer gap-2 py-1.5"
                      >
                        <HardDrive className="h-4 w-4 text-primary" />
                        <span>Import from Cloud Storage...</span>
                      </DropdownMenuItem>
                    </div>
                  )}
                  <ConnectorsFlyout … />
                </DropdownMenuContent>
```
<!-- `hasCloudStorage` — MessageInput.tsx:304-311. ⚠ THIS is why the one-item case exists. -->
```tsx
  const hasCloudStorage = connections.some(
    (c) => c.service_id.includes("google") || c.service_id.includes("workspace")
        || c.service_id.includes("drive")  || c.service_id.includes("onedrive"),
  )
```
<!-- ⭐ The + trigger's data-testid is already "composer-plus-btn" (MessageInput.tsx:354) — -->
<!-- it MATCHES COPY.engine.PLUS_BTN_TESTID, so the sketch's hook is real, not invented.    -->

<!-- THE UPLOAD AFFORDANCE TO COPY — TemplateUpload.tsx (whole file, 84 L). Load-bearing parts: -->
```tsx
  const onFileInputChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const f = e.target.files?.[0]
    e.target.value = ""            // ⭐ reset so re-selecting the SAME file fires change again
    if (f) void handleUpload(f)
  }
      {uploadError && (
        <p role="alert" className="px-0.5 text-[11px] text-destructive">{uploadError}</p>
      )}
```
<!-- ⚠ After 244-02 its `accept=` reads WORKSPACE_ACCEPT_ATTR from -->
<!-- frontend/src/lib/workspaceAllowedExt.ts — the composer MUST read the same constant.  -->

<!-- THE SHIPPED CLIENT — lib/api/documents.ts:56-78. ⭐ Its err.detail throw is what carries -->
<!-- the server's VERBATIM 422 to the UI, which is exactly what COPY.engine.REFUSE_* needs.   -->
```ts
export async function uploadWorkspaceTemplate(threadId: string, file: File): Promise<WorkspaceFile> {
  const formData = new FormData()
  formData.append("file", file)
  const res = await fetch(`${API_BASE}/threads/${threadId}/workspace/files`, {
    method: "POST",
    headers: { Authorization: `Bearer ${token}` },  // NO Content-Type — browser sets the boundary
    body: formData,
  })
  if (!res.ok) {
    const err = await res.json().catch(() => ({ detail: "Upload failed" }))
    throw new Error((err as { detail: string }).detail ?? "Upload failed")
  }
  const row = (await res.json()) as WorkspaceFile & { file_id?: string }
  return { ...row, id: row.id ?? row.file_id }     // ⛔ the server returns `file_id`, not `id`
}
```

<!-- THE EXPIRY HELPER — FilesSection.tsx:122-140. ⛔ Its rules bind the expired chip. -->
```ts
const EXPIRY_UNKNOWN = "expiry unknown"
function expiryCaption(expiresAt?: string): string {
  if (!expiresAt) return EXPIRY_UNKNOWN       // the wire did not say — say THAT
  const at = new Date(expiresAt).getTime()
  if (Number.isNaN(at)) return EXPIRY_UNKNOWN // an UNPARSEABLE date is a THIRD case
  const ms = at - Date.now()
  if (ms <= 0) return "expired"
  const h = Math.floor(ms / 3_600_000)
  if (h >= 1) return `expires in ${h}h`
  return `expires in ${Math.max(1, Math.floor(ms / 60_000))}m`
}
```
<!-- ⛔ The word is `expiry unknown`, never `no expiry`; unknown is NOT amber; the return -->
<!-- type is TOTAL (string, never null), which is what makes a blank unrepresentable.     -->

<!-- THE WIRE TYPE — types/index.ts:1064-1074. ⚠ 85/66/1380, seam still OWED; the 243 -->
<!-- precedent for touching it was ONE optional client-only field. Keep to that size.  -->
```ts
export interface WorkspaceFile {
  id?: string
  path: string
  size_bytes: number
  mime_type: string
  version?: number
  created_at?: string
  updated_at?: string
  kind?: string          // 100: 'template_input' for ephemeral uploads (D-02 badge)
  expires_at?: string    // 100: ISO timestamp; drives countdown + amber tint (D-02)
}
```

<!-- THE BUILD CONTRACT — sketch 236 README. D-244-27: assert the ORDERED BLOCKS and their -->
<!-- REQUIRED ATOMS by data-* hook, driven RED first. A text-only contract is NOT enough:  -->
<!-- the 2026-08-29 correction recorded 200 green assertions over a surface the operator    -->
<!-- called "nothing at all like what we designed", because it asserted vocabulary and      -->
<!-- never composition.                                                                    -->

| Block | Required atoms | Ordered fence — owner |
|---|---|---|
| Composer chips row | file chip (icon · name · size · scope · remove) rendered **in the existing `ActiveConnectorChips` row**, not a new region | `244-05` T3 Test 9 (atoms) + Test 8 (row order) |
| `+` menu | local item · cloud item · connectors item, **in that order** | `244-05` T2 Test 1 |
| Cloud modal | title · source line · file list with single-select · cancel + confirm | **`244-06` T3 Tests 7-8 — ADDED at the revision pass** |
| Refusal | filename · the server's verbatim 422 sentence · dismiss | **`244-05` T2 Tests 6 / 6b / 6c — ADDED at the revision pass** |
| Sent message | the chip in read-only form **above the user's text** · the agent's one-line `Read <file>` pointer | `244-05` T3 Tests 10-13 |

⚠ **The third column exists because the first plan set fenced THREE of these five blocks.** The
**Refusal** block was short two of its three atoms **in the BUILD, not merely in the tests** — no task
in any plan rendered a filename or a dismiss control — and the **Cloud modal** had a vocabulary-only
assertion in `244-06`. ⭐ **The arm was chosen by LOOKING at the approved mockup, not by cost:**
`index.html` § `refuseHTML` draws `<b>` filename → `<code>` verbatim 422 → `<button class="ok">OK</button>`,
and `COPY.shared.refusalDismiss` (`"OK"`) is in `COPY.js`. The operator approved all three, so ruling
them out under `D-244-24`'s style was **not available**. Both new fences are driven **RED first**.
</interfaces>

**⛔ THE CHIPS-ROW COMPOSITION RULING — hoist the container; do NOT use a `children` slot.**

D-244-26 says the chip is *"a **sibling** of the connector chip, not a new region"*. The pattern map
flags that this is a composition decision the plan must make, because `ActiveConnectorChips` owns its
own `<div>` **and returns `null` when `activeConnectorIds.length === 0`**.

- ⛔ **A `children`/`extra` slot inside `ActiveConnectorChips` is WRONG**, and the reason is measured,
  not stylistic: the component returns `null` on empty, so the attachment chip would **vanish for a
  person with no connector armed** — which is precisely the one-item case D-244-26 orders checked.
- ⛔ **A second `<div>` beneath it is the "new region" the decision forbids**, and it is the easy
  accident.
- ✅ **Hoist.** `MessageInput` owns the row container and its `Using:` label; `ActiveConnectorChips`
  becomes **bare** — it renders its chips as a fragment and `null` when empty. The row renders when
  **either** an attachment or a connector exists, and renders nothing when neither does (S-2).
  `ActiveConnectorChips` has exactly one mount (`MessageInput.tsx:321`), so this is contained.
  ⛔ Keep `data-testid="active-connector-chips"` reachable so no existing suite silently loses its
  hook — if it moves to the hoisted container, say so and re-run
  `MessageInput.connectors.test.tsx` (BASELINE 5).

**⛔ PORT `COPY.js`; DO NOT RE-TYPE STRINGS.** Create `frontend/src/components/chat/composerCopy.ts`
carrying `COPY.a`, `COPY.shared` and the `COPY.engine` refusal builders **as a port**, and fence it:
a `?raw` test that every string the composer renders appears in the sketch's `COPY.js`. The
sketch-to-build-drift rule exists because a re-typed string is a silently different product.
⚠ `COPY.engine.ALLOWED_EXT` gained `.pdf` in `244-02` — port the current file, not a remembered one.

**⛔ THE SCOPE WORD MUST BE RENDERED BY THE SENT MESSAGE.** D-244-22, verbatim: *"a build that puts
it solely in the composer has shipped B's weakness with A's cost."* This is the single most
skippable-looking requirement in the plan and it is the reason variant A won.

**⛔ NOT TAKEN, recorded rather than dropped (D-244-23):** B's menu header
`Add a file to this chat`, its footer *"Files here stay in this chat. The Library is for files you
keep."*, and its in-modal destination chip. They stay in the sketch under tab B and in `COPY.b`.
⚠ If UAT shows people still expect the Library, **B's footer is the cheapest single addition** — one
line, no layout change — and it composes with A. ⛔ The confirm button stays **`Attach`**, never
`Import`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The chip — one component, three states, ported copy</name>
  <files>frontend/src/components/chat/ChatAttachmentChip.tsx, frontend/src/components/chat/composerCopy.ts, frontend/src/types/index.ts, frontend/src/components/chat/__tests__/ChatAttachmentChip.states.test.tsx, scripts/vitest-count-gate.cjs, docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - .planning/sketches/236-the-file-that-belongs-to-this-chat/COPY.js (the WHOLE object)
    - .planning/sketches/236-the-file-that-belongs-to-this-chat/index.html (the `pending`, `sent`
      and `expired` states — open it and read the markup for those `data-s` values)
    - .planning/sketches/236-the-file-that-belongs-to-this-chat/README.md § "Build Contract"
    - frontend/src/components/chat/ActiveConnectorChips.tsx (the whole file — the visual template)
    - frontend/src/components/panel/FilesSection.tsx :85-145 and :265-330 (the expiry helper and
      the shipped `template_input` row)
    - frontend/src/types/index.ts :1060-1080 (`WorkspaceFile`)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md + references/icon-convention.md
    - docs/HOT-FILE-LEDGER.md § `frontend/src/types/index.ts`
  </read_first>
  <behavior>
    RED first, in `frontend/src/components/chat/__tests__/ChatAttachmentChip.states.test.tsx`:
    - Test 1 (pending): renders the file name, the size label, and the scope text
      `this chat only` and `24h` — from `composerCopy`, matched against `COPY.a.chipScope` and
      `COPY.a.chipTtl`. A remove control with `aria-label` from `COPY.a.chipRemove` is present.
    - Test 2 (sent / read-only): renders the file name **and** `COPY.a.sentNote` (`this chat only`),
      and renders **no** remove control. ⛔ This is D-244-22's build obligation; a chip that drops
      the scope word here fails the phase's headline design decision.
    - Test 3 (expired): with `expires_at` in the past, renders `COPY.shared.expiredChip`
      (`No longer available`), is struck through, and carries
      `title={COPY.shared.expiredWhy}` (`Files attached to a chat are kept for 24 hours.`).
      ⛔ Not a dead link and not a silent disappearance.
    - Test 4 (the three readings, S-3): `expires_at` ABSENT → the `expiry unknown` reading, not
      `expired` and not amber; an UNPARSEABLE date → the same third case. Mirror
      `FilesSection.expiryCaption`'s rules exactly; ⛔ the word is never `no expiry`.
    - Test 5 (copy fence): every user-visible string the chip renders appears in the sketch's
      `COPY.js`, read via `?raw`. ⛔ RED against a hand-typed string.
    - Test 6: the chip's icon comes from the shipped icon vocabulary, not a new mark — assert the
      rendered element against the convention the skill names.
  </behavior>
  <action>
    Create `frontend/src/components/chat/composerCopy.ts` — a PORT of `COPY.a`, `COPY.shared` and
    the `COPY.engine` refusal builders from
    `.planning/sketches/236-the-file-that-belongs-to-this-chat/COPY.js`. ⛔ Do not re-type; read the
    file and port it. Export typed constants; keep the builder signatures
    (`REFUSE_TYPE(ext, allowed)`) so a server sentence can be reconstructed for a test.

    Create `frontend/src/components/chat/ChatAttachmentChip.tsx` — ONE component with a
    `state: "pending" | "sent" | "expired"` prop (derive `expired` from `expires_at` using the
    `FilesSection.expiryCaption` rules; ⛔ do not duplicate the helper's logic — extract or import
    it, and if you extract, keep its total return type and its three readings intact).
    Visual template: `ActiveConnectorChips.tsx`'s chip span, verbatim classes.
    Data hooks for the D-244-27 fence (Task 3 asserts these — name them here so both agree):
    `data-testid="chat-attachment-chip"`, `data-chip-state="{state}"`,
    `data-chip-scope` on the scope text node, `data-chip-remove` on the remove button.

    `frontend/src/types/index.ts`: add **at most one** optional client-only field if the chip needs
    one (e.g. a display size label). ⚠ That file measures 85/66/1380 with its seam still OWED; the
    Phase 243 precedent for touching it was exactly one optional field. If nothing is needed, touch
    it not at all and say so.

    Register the suite in BOTH gate knobs. Re-derive and update the ledger row + section for
    `frontend/src/types/index.ts` if it was touched.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/__tests__/ChatAttachmentChip.states.test.tsx --reporter=basic</automated>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/panel/__tests__/FilesSection.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Test 2 (the SENT chip renders `this chat only`) is green and was seen RED. ⛔ If this criterion
      is not met the phase's headline design decision did not ship, whatever else did.
    - `grep -c 'this chat only' frontend/src/components/chat/ChatAttachmentChip.tsx` is **0** — the
      string comes from `composerCopy`, never from JSX.
    - Every string in `composerCopy.ts` appears in the sketch's `COPY.js` (Test 5, `?raw`).
    - The expired chip renders `No longer available` with `Files attached to a chat are kept for 24 hours.`
      as its title — asserted on rendered text, not on a testid.
    - `expiry unknown` is produced for BOTH an absent and an unparseable `expires_at`, and neither is
      styled amber. ⛔ The string `no expiry` appears nowhere.
    - `FilesSection.test.tsx` still passes at its pinned count — if the expiry helper was extracted,
      its original consumer must be unchanged in behaviour.
    - `git diff --numstat frontend/src/types/index.ts` shows at most a ONE-field addition, or 0 0.
  </acceptance_criteria>
  <done>One chip component covers pending, sent and expired; its words are ported from the sketch and fenced; the scope word is rendered in the sent state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The + menu and the chips row — two items, one hoisted region, the server's refusal</name>
  <files>frontend/src/components/chat/MessageInput.tsx, frontend/src/components/chat/ActiveConnectorChips.tsx, frontend/src/components/chat/__tests__/ComposerAttach.composition.test.tsx, scripts/vitest-count-gate.cjs, docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - frontend/src/components/chat/MessageInput.tsx :280-400 (hasCloudStorage, the chips row, the
      `+` trigger at :354 and the menu at :363-379) and :620-650 (the modal mount)
    - frontend/src/components/chat/ActiveConnectorChips.tsx (the whole file)
    - frontend/src/components/panel/TemplateUpload.tsx (the whole file — the upload handler shape)
    - frontend/src/lib/api/documents.ts :50-80 (`uploadWorkspaceTemplate`, the `err.detail` throw)
    - frontend/src/lib/workspaceAllowedExt.ts (created by 244-02 — the `accept=` source)
    - frontend/src/components/chat/__tests__/MessageInput.connectors.test.tsx (BASELINE 5 —
      it owns the `active-connector-chips` hook)
    - .planning/sketches/236-the-file-that-belongs-to-this-chat/index.html § `data-s="menu"`,
      `data-s="refused"`, `data-s="rest"`
    - docs/HOT-FILE-LEDGER.md § `frontend/src/components/chat/MessageInput.tsx`
  </read_first>
  <behavior>
    RED first, in `ComposerAttach.composition.test.tsx` (shared with Task 3):
    - Test 1: the `+` menu (opened via `data-testid="composer-plus-btn"`) renders THREE items in
      this order: `COPY.a.itemLocal` (`Attach a file`), `COPY.a.itemCloud` (`From cloud storage`),
      `COPY.a.itemConnectors` (`Tools and connectors`). ⛔ Assert the ORDER and the WORDS.
    - Test 2 (the one-item case, D-244-26): with `connections` containing no cloud service,
      `hasCloudStorage` is false and the menu renders the local item and the connectors item, with
      **no orphaned divider or header**, and the local item still reads correctly alone.
    - Test 3: the menu carries **no header and no footer** — variant A's menu is plain. ⛔ Assert the
      absence of `COPY.b.menuTitle` and `COPY.b.menuFooter` so B's arm cannot drift in.
    - Test 4 (the hoisted row): with an attachment and NO connectors, the chips row renders and
      contains the attachment chip. ⛔ RED against a `children` slot inside `ActiveConnectorChips`,
      which returns `null` on empty.
    - Test 5: with a connector and NO attachment, the row renders the connector chip and the
      `Using:` label exactly as today. With neither, the row renders nothing at all (S-2).
    - Test 6 (refusal, the SENTENCE): a rejected upload renders the server's message verbatim in a
      `role="alert"` node — drive it with the exact `COPY.engine.REFUSE_TYPE(".pdf", …)`,
      `COPY.engine.REFUSE_SIZE` and `COPY.engine.REFUSE_EMPTY` sentences thrown from a stubbed
      client. ⛔ Assert the SENTENCE, not that an error element exists.
    - Test 6b (refusal, the ATOMS AND THEIR ORDER — `D-244-27`'s obligation for this block, added at
      the revision pass): the refusal region contains, **in DOCUMENT order**, `[data-refusal-file]`
      carrying the rejected file's NAME → the verbatim sentence node → `[data-refusal-dismiss]`.
      ⛔ Assert with `compareDocumentPosition`, never by index in a query result.
      ⚠ The approved mockup DRAWS all three (`index.html` § `refuseHTML`: `<b>` filename, `<code>`
      sentence, `<button class="ok">`), so none of the three is optional and none may be dropped as
      "redundant" — a silent narrowing here is the precise failure `D-244-27` was written to stop.
    - Test 6c (the dismiss WORKS, and the NEGATIVE that makes 6b real): clicking
      `[data-refusal-dismiss]` removes the refusal region entirely and leaves the composer usable;
      and a fixture that renders the sentence alone, **without** the filename and dismiss atoms,
      drives Test 6b RED. ⛔ Quote that RED — a composition fence nobody has seen fire is a presence
      assertion wearing a costume, and 200 green assertions of that kind is what the 2026-08-29
      correction actually recorded.
    - Test 7: re-selecting the SAME file fires the change handler again — the
      `e.target.value = ""` reset from `TemplateUpload`. (A real bug a build drops silently.)
  </behavior>
  <action>
    **The menu.** Replace the single `hasCloudStorage`-gated item with the two items sketch 236
    draws, in the contract's order: local (`COPY.a.itemLocal`) → cloud (`COPY.a.itemCloud`, still
    gated on `hasCloudStorage`) → the existing `ConnectorsFlyout` (`COPY.a.itemConnectors`).
    ⛔ Keep the trigger's `data-testid="composer-plus-btn"` — it already matches
    `COPY.engine.PLUS_BTN_TESTID`. ⛔ No header, no footer (variant A).
    ⚠ The divider `<div className="p-1 border-b border-border/50">` currently wraps the cloud item
    alone; restructure so a hidden cloud item leaves no dangling rule.

    **The local upload.** A hidden `<input type="file">` whose `accept` reads
    `WORKSPACE_ACCEPT_ATTR` from `frontend/src/lib/workspaceAllowedExt.ts` (created in `244-02`).
    ⛔ Do NOT hand-type the extension list — that is the fourth copy the lockstep fence exists to
    prevent. Handler shape copied from `TemplateUpload.tsx`: `e.target.value = ""` before dispatch,
    `uploadWorkspaceTemplate(threadId, f)`, optimistic
    `setWorkspaceFileForThread(threadId, uploaded)`, and the caught `e.message` rendered in a
    `role="alert"` node. ⭐ That message IS the server's verbatim 422 — the path already exists.

    **The refusal is a REGION with three atoms, in the drawn order (`D-244-27`, added at the revision
    pass).** ⛔ A bare sentence does not satisfy the Build Contract. The `role="alert"` node renders,
    in order: `[data-refusal-file]` carrying the rejected file's name → the server's verbatim message
    → a `[data-refusal-dismiss]` control labelled `COPY.shared.refusalDismiss` (**`OK`**) whose click
    clears the error state. ⛔ Both new strings come from `composerCopy` (the port), never re-typed.
    ⚠ **The existing "a new attempt clears the error" behaviour is NOT a substitute for the dismiss
    control** — the mockup draws an explicit affordance and the operator approved it, and a person who
    picks the wrong file and then walks away must be able to put the composer back without uploading
    something else. Visual template: `TemplateUpload.tsx:76`'s inline `<p role="alert">`, promoted to
    a region with a leading filename and a trailing button (sketch `index.html` § `.refuse`).

    **The chips row — HOIST (see the ruling in `<context>`).** Move the row container and its
    `Using:` label into `MessageInput`. `ActiveConnectorChips` renders its chips as a fragment and
    `null` when empty. The row renders when EITHER an attachment or a connector exists; nothing when
    neither. ⛔ Keep `data-testid="active-connector-chips"` reachable and re-run
    `MessageInput.connectors.test.tsx` (BASELINE 5), quoting the count.

    ⛔ **Do NOT touch `ConnectedFilePickerModal` or the cloud client here** — the cloud re-point is
    `244-06`. This task only re-labels and re-orders the menu item that opens it.

    Re-derive and update the ledger row + section for `MessageInput.tsx` (measured 29/14/643 at
    discuss — re-derive; a row present and WRONG stops the audit). ⭐ **The gate fires `[no-row]` on
    four files this plan names at PLAN time** — `frontend/src/components/chat/ActiveConnectorChips.tsx`
    (which has never had one), the two net-new `ChatAttachmentChip.tsx` and `composerCopy.ts`, and
    `frontend/src/lib/workspaceAllowedExt.ts`.
    ⛔ **THIS PLAN OWNS THREE OF THEM, NOT FOUR.** `workspaceAllowedExt.ts`'s row belongs to
    **`244-02` Task 1**, which lands **two waves earlier**, so by the time this plan runs the row
    **already exists** — **VERIFY it, never ADD it.** `scripts/check-hot-file-ledger.cjs` fails
    `[duplicate-row]`, so a second row turns a bookkeeping reflex into a red gate.
    Add a § "Scan list" row **and** a section for `ActiveConnectorChips.tsx`, `ChatAttachmentChip.tsx`
    and `composerCopy.ts` only, in the same commit. ⛔ A row is added at CREATION, not at the third
    phase. (Full ownership map: `244-PATTERNS.md` § C-8 — one file → exactly one task.)
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/__tests__/ComposerAttach.composition.test.tsx --reporter=basic</automated>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/__tests__/MessageInput.connectors.test.tsx --reporter=basic</automated>
    <automated>cd "C:/Vibe Apps/Agentic RAG" &amp;&amp; node scripts/check-hot-file-ledger.cjs 244</automated>
  </verify>
  <acceptance_criteria>
    - The three menu items render in the contract's order with `COPY.a`'s exact words — asserted on
      text content, in order.
    - `grep -c 'accept="\.' frontend/src/components/chat/MessageInput.tsx` is **0**; the `accept`
      value comes from `WORKSPACE_ACCEPT_ATTR`.
    - Test 4 was seen RED against a slot-inside-`ActiveConnectorChips` implementation; the RED output
      is quoted. ⛔ This is the ruling's executable form — without the RED the ruling is prose.
    - Test 6 asserts all three server sentences verbatim from `COPY.engine`.
    - `MessageInput.connectors.test.tsx` passes at BASELINE 5 or above; the count is quoted.
    - `grep -c 'menuFooter\|menuTitle' frontend/src/components/chat/MessageInput.tsx` is **0** —
      variant B's arm is recorded in the sketch, not shipped.
    - The word `Import` does not appear as a user-facing label on any new control (D-244-23).
      ⛔ Verify by reading the rendered strings, not by grepping the file (the existing
      `ConnectorsFlyout` may legitimately contain the word elsewhere).
    - The refusal region renders **filename → sentence → dismiss** in document order (Test 6b), and
      the sentence-only fixture was seen RED; the RED output is quoted. ⛔ Without that RED this
      block's fence is unproven, which is the state the revision pass found it in.
    - `grep -c 'data-refusal-dismiss' frontend/src/components/chat/MessageInput.tsx` is ≥ 1, and the
      control's label resolves from `composerCopy.shared.refusalDismiss`, not from a JSX literal.
    - Clicking the dismiss control removes the refusal region (Test 6c) — asserted on the region's
      absence, not on a state variable.
    - `node scripts/check-hot-file-ledger.cjs 244` prints no `[no-row]` for this plan's files,
      including the two net-new ones.
    - `grep -c 'workspaceAllowedExt' docs/HOT-FILE-LEDGER.md` is exactly **1** — `244-02` added that
      row two waves ago and this plan did not add a second. ⛔ `[duplicate-row]` is a gate FAILURE.
  </acceptance_criteria>
  <done>The `+` menu offers a local door and a cloud door in the drawn order; the attachment chip sits in one hoisted row beside the connector chip; and a refusal names the file, speaks the server's words and can be dismissed — the three atoms the mockup draws, fenced in that order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The scope word survives into the transcript — the sent-message chip and the D-244-27 composition fence</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/components/chat/__tests__/ComposerAttach.composition.test.tsx, docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - frontend/src/components/chat/MessageItem.tsx — the USER-row render branch (find it; it is the
      host for the read-only chip) and :385-400 (the arm 244-03 mounted the approval into)
    - frontend/src/components/chat/ChatAttachmentChip.tsx (created in Task 1)
    - .planning/sketches/236-the-file-that-belongs-to-this-chat/index.html § `data-s="sent"` —
      the chip ABOVE the user's text, and the agent's one-line `Read <file>` pointer
    - .planning/sketches/236-the-file-that-belongs-to-this-chat/README.md § "Build Contract" and
      § "WINNER — A: Scope on the chip"
    - .planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md § D-244-22, D-244-27
    - docs/HOT-FILE-LEDGER.md § `frontend/src/components/chat/MessageItem.tsx`
  </read_first>
  <behavior>
    RED first — the D-244-27 **ordered-block composition fence**, in
    `ComposerAttach.composition.test.tsx`. ⛔ A text-only contract is NOT sufficient: the 2026-08-29
    correction recorded 200 green assertions over a surface the operator called *"nothing at all like
    what we designed"*, because it asserted vocabulary and never composition.
    - Test 8 (ORDER, composer): within the composer, the DOM order is
      `[data-testid="active-connector-chips"]` (the chips row) → the `+` trigger → the textarea.
      Assert with `compareDocumentPosition`, not by index in a query result.
    - Test 9 (ATOMS, chip): the pending chip contains, in order, an icon node, the file name, the
      size, `[data-chip-scope]`, `[data-chip-remove]`.
    - Test 10 (ORDER, sent message): in a user row carrying an attachment, the
      `[data-testid="chat-attachment-chip"][data-chip-state="sent"]` node precedes the user's text
      node. ⛔ *"the chip in read-only form **above** the user's text"* — above is part of the
      contract, not a detail.
    - Test 11 (ATOM, sent): that chip renders `COPY.a.sentNote` — the scope word in the transcript.
    - Test 12 (ATOM, agent pointer): the assistant row that used the file renders
      `COPY.shared.agentReadLine(name)` (`Read <file>`) as a one-line pointer.
    - Test 13 (the negative that makes the fence real): deliberately reorder the chip BELOW the text
      in a fixture and watch Test 10 go red. ⛔ Quote that RED — a composition fence nobody has seen
      fire is a presence assertion wearing a costume.
  </behavior>
  <action>
    In `MessageItem.tsx`'s USER-row branch, render `<ChatAttachmentChip state="sent" … />` for each
    attachment associated with the message, **above** the user's text. ⛔ Do NOT edit the user's
    typed text — D-244-02 rejects that arm, and the shipped
    `ConnectedFilePickerModal.onFileImported` callback at `MessageInput.tsx:636-638` does exactly
    that today (it appends `Attached file: …` into the draft). ⚠ That callback is `244-06`'s to
    replace; this task must not leave BOTH behaviours live — if the two would double up, note the
    ordering dependency in the SUMMARY and let `244-06` remove the text edit.

    Render `COPY.shared.agentReadLine(name)` as the agent's one-line pointer where the assistant row
    shows it used the file. ⚠ Keep it a POINTER, one line — the sketch draws it that way and the run
    surface already owns the heavier receipt.

    ⛔ **Association rule:** derive which attachments belong to a sent message from data the message
    already carries or from the thread's workspace files filtered by upload time relative to the
    message. **Whichever you pick, state it in the SUMMARY and fence the edge case**: an attachment
    uploaded and then removed before send must not appear on any row. ⛔ Do NOT add a backend field
    or a migration — D-244-01 and the ROADMAP both say `Migrations: none expected`; if you conclude
    one is unavoidable, STOP and surface it rather than shipping one.

    Write the composition fence tests above into `ComposerAttach.composition.test.tsx` and register
    the suite in BOTH gate knobs (done in Task 2 if shared — verify both lines exist).
    Re-derive and update the ledger row + section for `MessageItem.tsx`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/__tests__/ComposerAttach.composition.test.tsx --reporter=basic</automated>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx src/components/chat/__tests__/MessageItem.capPaused.test.tsx --reporter=basic</automated>
    <automated>cd frontend &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - Test 10 and Test 11 are green: the sent chip PRECEDES the user's text and renders
      `this chat only`. ⛔ Together these are D-244-22's build obligation; failing either means the
      winner's whole rationale did not ship.
    - Test 13's deliberate reorder was observed RED and the output is quoted. ⛔ Without it, the
      composition fence is unproven.
    - Order assertions use `compareDocumentPosition` (or an equivalent DOM-order primitive) — ⛔ not
      `getAllByTestId(...)[0]`, which is query order, not document order.
    - `244-03`'s suites (`MessageItem.inlineApproval.test.tsx`, `MessageItem.capPaused.test.tsx`)
      still pass at their counts; the counts are quoted. This file now hosts three of this phase's
      changes and is a 69/33-phase hot file.
    - The association rule is stated in the SUMMARY with its edge case fenced by a test.
    - No migration was added: `git status --short supabase/migrations/` is empty.
    - `npx tsc -p tsconfig.app.json --noEmit` error SET is a subset of the 67-error base set.
  </acceptance_criteria>
  <done>A sent message carries the chip above the text with the scope word on it, the agent's `Read <file>` pointer is one line, and the ordered-block contract is fenced by a test that has been seen fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → browser → `POST /threads/{id}/workspace/files` | untrusted bytes and an untrusted filename leave the user's machine |
| server 422 → rendered UI | a server-authored sentence is rendered to the operator |
| filename → transcript render | attacker-controlled text rendered into the chat history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-05-01 | Spoofing | `accept=` treated as a security control | mitigate | ⛔ It is a UX hint only. The real gate is the server's `validate_upload` (magic bytes + container checks), unchanged here. `TemplateUpload.tsx`'s docblock already states this; the composer reads the same shared constant so the two cannot drift. |
| T-244-05-02 | Tampering | a re-typed extension list drifting from the server's | mitigate | `accept` reads `WORKSPACE_ACCEPT_ATTR`; the `?raw` lockstep fence from `244-02` fails if the TS list and `workspace.py`'s `_ALLOWED_EXT` disagree. |
| T-244-05-03 | Information disclosure | a crafted filename rendered into the transcript | mitigate | React escapes text nodes; the chip truncates with `max-w-[140px]`/`truncate` (the `ActiveConnectorChips` template) so a long name cannot displace the row. ⛔ No `dangerouslySetInnerHTML` — assert with a grep in the suite. |
| T-244-05-04 | Denial of service | a 10 MB+ file uploaded from the composer | mitigate | The server enforces the cap three times including **before body materialisation** (WR-04), and the refusal surfaces as `COPY.engine.REFUSE_SIZE`. The client adds no second cap that could disagree. |
| T-244-05-05 | Repudiation | an expired attachment silently vanishing from an old transcript | mitigate | D-244-25's expired state is shipped and fenced (Task 1 Test 3). ⚠ The TTL is a **read gate, not a delete sweeper** — the row survives invisibly; the chip must say `No longer available`, never disappear. |
| T-244-05-06 | Spoofing | the UI implying the file is in the knowledge base | mitigate | D-244-03 makes *"not in the KB"* structurally true (no `documents` row at all). The chip says `this chat only`; the confirm word is `Attach`, never `Import`. ⛔ The ROADMAP names a quiet Library write as the phase's failure mode. |
| T-244-05-SC | Tampering | npm/pip/cargo installs | mitigate | ⛔ **This plan installs NO packages.** The icon comes from the already-installed `@lobehub/icons` / lucide vocabulary. If a package is needed, STOP: `## Package Legitimacy Audit` first, `[ASSUMED]`/`[SUS]` behind a blocking human checkpoint. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root; verdict verbatim;
  publish the trajectory.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — SET DIFF against the 67-error base.
- `node scripts/check-hot-file-ledger.cjs 244` and `node scripts/check-claude-md-size.cjs`.
- ⛔ `SHELL-04` does not close here. Its driven rows — including the **8-row cross-provider board**
  for D-244-02's system-prompt line — are in `244-VALIDATION.md`.
</verification>

<success_criteria>
- The `+` menu offers the two doors in the drawn order, and reads correctly with the cloud item absent.
- The attachment chip is a sibling in one hoisted row, not a new region.
- A refusal shows the rejected file's NAME, the server's verbatim sentence and a working dismiss
  control, in that document order; an expired chip says `No longer available`.
- ⭐ The SENT message carries the chip, above the text, with `this chat only` on it.
- The ordered-block composition fence exists and has been seen fire.
</success_criteria>

<output>
Create `.planning/phases/244-the-chat-shell-and-the-composer/244-05-SUMMARY.md` when done.
</output>
