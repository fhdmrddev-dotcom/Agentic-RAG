---
phase: 244-the-chat-shell-and-the-composer
plan: 04
type: tdd
wave: 2
depends_on: ["244-01"]
files_modified:
  - frontend/src/components/layout/attentionConditions.ts
  - frontend/src/components/layout/AttentionPopover.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/LibraryPage.tsx
  - frontend/src/components/layout/__tests__/attentionTab.test.ts
  - frontend/src/pages/__tests__/LibraryPage.tabAttention.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [SHELL-05]

must_haves:
  truths:
    - "A watched source that has stopped reading raises a signal the operator sees in the app shell while doing something else — DRIVEN end to end, which has never happened (D-244-15a)."
    - "A HEALTHY source raises nothing — the ROADMAP's named failure mode is a signal nobody trusts after the first false one."
    - "Opening the Library from the badge shows WHICH tab owns the attention — the shell says THAT, the tab says WHERE (BUG-260911-03)."
    - "The attention registry still has exactly ONE producer — D-235-03 forbids a second and NavPanel.badge.test.tsx asserts the count literally."
    - "The tab hand-off is still spent on navigate — no second writer of libraryTab is introduced."
    - "No THIRD useSourceAttention() reader is added — the registry's own re-open trigger arm 1."
  artifacts:
    - path: "frontend/src/components/layout/attentionConditions.ts"
      provides: "an OPTIONAL tab field on AttentionCondition, set by the existing producer"
      contains: "tab"
    - path: "frontend/src/pages/LibraryPage.tsx"
      provides: "per-tab attention rendered on the segmented control derived from TAB_LABELS"
    - path: "frontend/src/components/layout/__tests__/attentionTab.test.ts"
      provides: "RED-driven proof the condition carries a kind and the producer count is still 1"
    - path: "frontend/src/pages/__tests__/LibraryPage.tabAttention.test.tsx"
      provides: "RED-driven proof the tab that owns the attention is marked and the others are not"
  key_links:
    - from: "frontend/src/components/layout/attentionConditions.ts"
      to: "frontend/src/components/layout/ChatLayout.tsx"
      via: "the ATTENTION_PRODUCERS flatMap that already reads them"
      pattern: "ATTENTION_PRODUCERS"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/LibraryPage.tsx"
      via: "the one-shot libraryTab hand-off, spent by libraryTabAfterNavigate"
      pattern: "libraryTabAfterNavigate"
---

<objective>
Finish the signal that already ships. `SHELL-05`'s app-shell badge was built at Phase 235 and
**nobody has ever driven it end to end** — `SURF-03` closed UNTICKED. And the operator's own
complaint is that *"the badge creates a question it then refuses to answer"*: it names a count and
then stops at the Library door, leaving five tabs to hunt through.

Purpose: `SHELL-05` and `BUG-260911-03`. Two halves, in this order (D-244-15): **verify what
shipped**, then **attribute the count to a tab**.

Output: one optional field on the condition, set by the producer that already exists; a tab strip
that says WHERE; and a driven board entry that ticks criterion 5 on evidence rather than a reading.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then
`git rev-parse HEAD` — MUST be `1ff80a1da` or a descendant on `develop`. Wave 2: the base includes
`244-01`'s merge (it also edits `ChatLayout.tsx`).
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

**⚠ Gate state INHERITED:** `GSD_VITEST_MAX_WORKERS=2`, gate from the repo root, verdict verbatim.
`sourceComposition.test.tsx` is a standing red in neither knob. `SEED-171`'s five flaky suites exist
— capture filenames from the gate's own JSON before re-running; never touch the cap.
`npx tsc -p tsconfig.app.json --noEmit`, SET DIFF against the **67**-error base.

**⚠ Registry files are append-only** and do NOT create a wave dependency. Take BOTH sides on conflict.

**⛔ REGISTRY MERGE ORDER — a policy is not a mechanism (added at the revision pass).**
*"Take BOTH sides"* is stated in every plan and **enforced by nothing**. Waves 1 and 2 each run
**two** plans concurrently, and all six append into the **same regions**: the scan-list table in
`CLAUDE.md`, the per-file sections in `docs/HOT-FILE-LEDGER.md`, and the `TARGETS` / `BASELINE`
blocks in `scripts/vitest-count-gate.cjs`. Simultaneous row insertions there produce **real git
conflicts**, not clean auto-merges. ⛔ **So SERIALIZE the registry hunks:**

1. Merge the **lower-numbered** plan of the wave first, whole — wave 1: `244-01`, then `244-02`;
   wave 2: `244-03`, then `244-04`.
2. Rebase the higher-numbered plan onto that merge and **re-apply its registry hunks by hand** —
   its `TARGETS` line, its `BASELINE` pin, its ledger rows and its ledger sections. ⛔ Do not accept
   an automatic resolution on these three files.
3. Re-run the registry gates from the **repo root** AFTER the second merge, never only after the
   first: `node scripts/check-hot-file-ledger.cjs 244` and `node scripts/check-claude-md-size.cjs`,
   then the wave's single full `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`.

⛔ **A row lost to a bad auto-merge is SILENT** — `[no-row]` fires only for files a plan NAMES, so a
row dropped from another plan's file stays invisible until that file's next phase, which is the
`App.tsx` 23-phase failure exactly. ⛔ **A row added twice fails `[duplicate-row]`**, which is why
ownership is **one file → exactly one task** (the map is in `244-PATTERNS.md` § C-8).

**⚠ D-244-17 DEVIATION — recorded beside the original, never overwriting it. SIX plans, not four.**

D-244-17 locks *"FOUR plans, decomposed by surface seam"*, with plan 2 carrying **all** of the
composer: the cap-pause unlock **plus** the local attach **plus** the cloud re-point **plus** the
Library's folder-asking import. That grouping is not reachable, for the one legitimate split reason
in the planner's own charter — **context cost**, never difficulty:

- **C-2** turns `SHELL-02` from a one-expression frontend change into a **two-layer** change, adding
  a `runs`-read edit on `backend/app/api/threads.py` (244 commits / 81 phases) against a backend
  baseline at its **ceiling of 71 with zero headroom**.
- ⚠ **C-9** (measured during planning, recorded in `244-02`) shows `SHELL-04`'s *"and the agent can
  use it"* clause is unsatisfied for **8 of the 15 accepted extensions** — `workspace_read` returns a
  note for every binary, and the sandbox had no reach. That is a third backend seam inside the same
  requirement.
- `SHELL-04` therefore spans **~16 files across both tiers**: validator category, sandbox hydration,
  system-prompt line, chip component, copy port, composer composition, transcript render, upload
  client, a request model, a route, a frontend client and a Library mount.

One agent carrying that lands far past the ~50% context budget, which is where quality degrades.
**The seam decomposition D-244-17 chose is honoured exactly** — it is `SHELL-04` alone that is cut
into three (**the agent can use it** · **the composer affordance** · **the two doors**), each a
wave-sized unit of 2-3 tasks. ⛔ Nothing was cut to save time: the verifier, the TDD RED drives,
`security_enforcement` / `code_review` and migration discipline all stand (G-8's never-cut list).
⭐ The lever taken instead is G-8's own: **targeted suites per task, FULL gates once per WAVE.**

**⚠ Solo running (D-244-21):** self-review only. "self-verified", never "reviewed".

**⚠ LOAD THE PROJECT SKILL:** `.claude/skills/sketch-findings-agentic-rag/SKILL.md` — the nav/IA and
the Library surfaces sections. ⛔ **D-244-18: do NOT sketch this surface.** Per F-1 it is shipped and
the operator approved its design at Phase 235; sketching it would be re-designing live UI. Say so in
the SUMMARY explicitly rather than sketching everything or nothing.

**⚠ OWED AT PHASE CLOSE — one line, and deliberately NOT a task in this plan.**
`.planning/REQUIREMENTS.md:207` still reads
`SHELL-05 | 244 — Chat shell + composer | Pending — ⚠ G-2 sketch owed (net-new surface)`.
That **contradicts locked `D-244-18` and finding `F-1`**: `SHELL-05`'s signal SHIPPED at Phase 235
plan 09 (`attentionConditions.ts`, `AttentionPopover.tsx`, badged on three surfaces), so no G-2
sketch is owed and drawing one would redraw operator-approved live UI. ⛔ **Do NOT add a task for
it** — it is a stale register entry outside this plan set, and REQUIREMENTS.md has now been stale
for four milestones running. **Carry it into this plan's SUMMARY as an owed close-out correction**,
so `/gsd:verify-work` can strike the phrase in one edit. ⭐ Record it as a correction **beside** the
entry, never as a silent overwrite — a register quietly edited to agree with the build is how this
project loses the evidence that it was ever wrong.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-PATTERNS.md
@.planning/reported-bugs/BUG-260911-03-library-badge-does-not-say-which-tab-needs-attention.md
@.planning/seeds/SEED-231-nobody-is-told-an-approval-is-waiting.md
@.planning/seeds/SEED-253-mobile-has-no-drawer-trigger-outside-the-chat-view.md

<interfaces>
<!-- ⚠ C-5 — D-244-15's question, ANSWERED FROM SOURCE. The condition carries NO kind. -->
<!-- frontend/src/components/layout/attentionConditions.ts — the interface is four fields: -->
```ts
export interface AttentionCondition {
  id: string          // stable across polls
  title: string       // what is affected, in the person's words
  detail: string      // a SENTENCE from a vocabulary leaf — never a raw error string
  onOpen: () => void  // injected by the shell; no router (SEED-185)
}
```
<!-- WHERE THE KIND DOES EXIST:                                                            -->
<!--  · AttentionProducer.key — the module constant is [{ key: "stopped-sources", use: …}] -->
<!--  · StoppedSource.cause (lib/api/sources.ts:274, a SourceFailureCause union) — but      -->
<!--    useStoppedSourceConditions CONSUMES AND DISCARDS it into `detail`:                  -->
```ts
export function useStoppedSourceConditions(onOpen: () => void): AttentionCondition[] {
  const { stopped } = useSourceAttention()
  return useMemo(
    () => stopped.map((source) => ({
        id: source.watch_id,
        title: source.source_folder_name,
        detail: SENTENCE_FOR_CAUSE[source.cause](source.connection_name ?? ""),
        onOpen,
      })),
    [stopped, onOpen],
  )
}

export const ATTENTION_PRODUCERS: readonly AttentionProducer[] = Object.freeze([
  { key: "stopped-sources", use: useStoppedSourceConditions },
])
```
<!-- ⛔ A SECOND PRODUCER IS FORBIDDEN — D-235-03, and NavPanel.badge.test.tsx:118-119: -->
```ts
    expect(ATTENTION_PRODUCERS.length).toBe(1)
    expect(ATTENTION_PRODUCERS[0].key).toBe("stopped-sources")
```

<!-- THE ONE READ — ChatLayout.tsx:534-544 -->
```tsx
  const openLibraryHealth = useCallback(() => onOpenLibraryHealth?.(), [onOpenLibraryHealth])
  const attentionConditions = attentionRegistry.ATTENTION_PRODUCERS.flatMap((producer) =>
    producer.use(openLibraryHealth),
  )
  const attentionCount = attentionConditions.length
  const showAttention = attentionCount > 0 && Boolean(onOpenLibraryHealth)
```
<!-- Three renderers hang off it: NavPanel props (:559-560), the drawer nav row badge  -->
<!-- (:720-724), the hamburger dot (ChatArea attentionCount= :808).                    -->

<!-- THE NAVIGATION PATTERN — App.tsx:178-181 -->
```tsx
  const [libraryTab, setLibraryTab] = useState<LibraryTab | undefined>(undefined)
  const handleOpenLibraryHealth = () => {
    setLibraryTab("health")
    setActiveView("documents")
  }
```
<!-- …paired with the ONE-SHOT lifetime rule, App.tsx:192-195 -->
```tsx
  const handleNavigate = (next: ActiveView) => {
    setActiveView(next)
    setLibraryTab((pending) => libraryTabAfterNavigate(pending, next))
  }
```
<!-- ⛔ frontend/src/lib/libraryTabHandoff.ts exists BECAUSE a comment made a claim and     -->
<!-- nothing enforced it: one click on the popover permanently redefined where the Library  -->
<!-- opens. Any per-tab attribution goes THROUGH this spent-on-navigate rule.               -->
<!-- ⛔ NEVER add a second writer of `libraryTab`.                                          -->

<!-- THE TAB VOCABULARY — pages/librarySelection.ts:94 -->
```ts
export type LibraryTab = "documents" | "views" | "ingestion" | "indexing" | "health"
```
<!-- …and the segmented control is DERIVED, never re-typed — LibraryPage.tsx:99-110 -->
```tsx
const TAB_LABELS: Record<LibraryTab, string> = {
  documents: "Documents", views: "Views", ingestion: "Ingestion",
  indexing: "Indexing", health: "Health",
}
const LIBRARY_TABS = (Object.keys(TAB_LABELS) as LibraryTab[]).map(
  (k) => [k, TAB_LABELS[k]] as const,
)
```

<!-- THE RENDERER — AttentionPopover.tsx:73-108. ⛔ It derives NO count and renders NO -->
<!-- verdict; it is handed conditions and draws them. Each row is title + detail; the  -->
<!-- only control is `rail-open-health` → onOpenLibraryHealth.                         -->
</interfaces>

**⭐ THE C-5 RULING — one optional field on the condition, set by the producer that already exists.**

`BUG-260911-03` says *"the likely shape is that `attentionConditions` already knows what KIND each
condition is … **Verify that before building anything** — this report asserts the symptom, not the
cause."* **Verified, and the answer is: it does not — the PRODUCER does.**

So the cheapest honest route is `tab?: LibraryTab` on `AttentionCondition`, set inside
`useStoppedSourceConditions`. It is **additive**, it adds **no second producer**, and
`ATTENTION_PRODUCERS.length === 1` is untouched — which is what keeps `D-235-03` and
`NavPanel.badge.test.tsx` satisfied rather than argued with.

⛔ **`SEED-231` (approvals as a second tenant) is NOT taken here**, even though `SHELL-03` makes it
topical. It stays the registry's *intended* future tenant, re-openable only by a deliberate override
with the count argued — never a fill-in.

**⚠ THE READER-COUNT CONSTRAINT, checked BEFORE adding anything.**

`attentionConditions.ts`'s own three-part re-open trigger names *a third concurrent reader* as arm 1.
Today there are exactly **two** `useSourceAttention()` readers (the shell, plus the Library's active
tab body), pinned by a `?raw` inventory fence at `ChatLayout.badge.test.tsx:286-297, 374`.
⛔ **The tab attribution must NOT add a third.** Thread the existing conditions down as data; do not
call the hook again in `LibraryPage`. ⚠ Note that `244-03` independently fires arm 1 for
`useAskUserPrompt` — a different hook, the same trigger shape; the SUMMARY should cross-reference it
so the deferral is triggered once with both data points, not twice by halves.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The condition learns its tab — one optional field, one producer, one count</name>
  <files>frontend/src/components/layout/attentionConditions.ts, frontend/src/components/layout/AttentionPopover.tsx, frontend/src/components/layout/__tests__/attentionTab.test.ts, scripts/vitest-count-gate.cjs, docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - frontend/src/components/layout/attentionConditions.ts (the WHOLE file, 148 L — its docblock IS
      the decision record: D-235-03 one tenant, D-235-05 no client-side verdict, the two-readers
      correction, the declined threading, the three-part re-open trigger)
    - frontend/src/hooks/useSourceAttention.ts (the whole file, 131 L)
    - frontend/src/lib/api/sources.ts :265-285 (`StoppedSource`, `SourceFailureCause`)
    - frontend/src/pages/librarySelection.ts :85-100 (`LibraryTab`)
    - frontend/src/components/layout/__tests__/NavPanel.badge.test.tsx :110-125 (the literal count)
    - frontend/src/components/layout/__tests__/ChatLayout.badge.test.tsx :280-300, :370-380
      (the `?raw` reader-inventory fence)
    - frontend/src/components/layout/AttentionPopover.tsx (the whole file, 109 L)
    - docs/HOT-FILE-LEDGER.md § `attentionConditions.ts`, § `AttentionPopover.tsx`,
      § `useSourceAttention.ts`
  </read_first>
  <behavior>
    RED first, in `frontend/src/components/layout/__tests__/attentionTab.test.ts`:
    - Test 1: every condition returned by `useStoppedSourceConditions` carries
      `tab === "health"` — the tab that owns a stopped watched source. ⛔ RED: the field does not
      exist today.
    - Test 2: `AttentionCondition.tab` is OPTIONAL — a condition object without it still type-checks
      and still renders in `AttentionPopover`. (A future producer must not be forced to lie.)
    - Test 3: `ATTENTION_PRODUCERS.length === 1` and `ATTENTION_PRODUCERS[0].key === "stopped-sources"`
      — re-asserted HERE, in this plan's own suite, so the constraint travels with the change rather
      than living only in a neighbour's file.
    - Test 4: `detail` is UNCHANGED — still `SENTENCE_FOR_CAUSE[source.cause](...)`. The `cause` is
      still consumed into a sentence; the new field does not leak a raw cause string into the UI.
      ⚠ Assert the rendered sentence, not the presence of a `detail` key.
    - Test 5: the `?raw` reader inventory still counts exactly **TWO** `useSourceAttention()` call
      sites in `src/`. ⛔ RED the moment anyone adds a third — which is the point.
  </behavior>
  <action>
    In `frontend/src/components/layout/attentionConditions.ts`:
    - Add `tab?: LibraryTab` to the `AttentionCondition` interface, with a docblock line saying it is
      the Library tab that OWNS this condition, that it is optional because a future producer may
      have no Library home, and that it exists because the condition carried no kind while the
      producer did (C-5, `BUG-260911-03`).
      ⚠ Import the type from `@/pages/librarySelection` — that file already owns the vocabulary and
      `LibraryPage.tsx` derives its control from it, "never re-typed".
    - In `useStoppedSourceConditions`, set `tab: "health"` on each mapped condition. ⛔ Change
      nothing else in the map — `id`, `title`, `detail` and `onOpen` stay byte-identical.
    - ⛔ Do NOT add a producer. Do NOT change `ATTENTION_PRODUCERS`. Do NOT add a
      `useSourceAttention()` call anywhere.
    - Append to the file's existing docblock: that arm 1 of the three-part re-open trigger was
      CHECKED before this change and the reader count is still two, and that `SEED-231` was
      considered and deliberately not taken (D-235-03).

    In `AttentionPopover.tsx`: nothing is required. ⚠ If the popover row is given a tab hint, it must
    come from the condition's own field and must not change the shipped `title`/`detail` copy — the
    vocabulary leaf owns the words (S-4), and this component *writes no copy at all* today. Prefer
    leaving it untouched and say so.

    Register the new suite in BOTH gate knobs (`src/components/layout` has no bare-directory entry).
    Re-derive and update the ledger rows + sections for `attentionConditions.ts`,
    `AttentionPopover.tsx` and `useSourceAttention.ts`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/layout/__tests__/attentionTab.test.ts --reporter=basic</automated>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/layout/__tests__/NavPanel.badge.test.tsx src/components/layout/__tests__/ChatLayout.badge.test.tsx src/hooks/__tests__/useSourceAttention.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'tab?: LibraryTab' frontend/src/components/layout/attentionConditions.ts` matches.
    - `grep -c 'ATTENTION_PRODUCERS' frontend/src/components/layout/attentionConditions.ts` is
      unchanged from the base, and the array still has exactly one element (Test 3 is green).
    - `grep -rc 'useSourceAttention()' frontend/src --include=*.ts --include=*.tsx` filtered to
      non-test files returns **2**. ⛔ 3 fails this criterion outright.
    - `NavPanel.badge.test.tsx` (BASELINE 14), `ChatLayout.badge.test.tsx` (5) and
      `useSourceAttention.test.tsx` (13) all pass at or above their pinned counts; the counts are
      quoted in the SUMMARY.
    - Test 1 was seen RED; the RED output is quoted.
    - The `detail` sentences produced for each `SourceFailureCause` member are byte-identical to the
      base — asserted by Test 4 against the rendered text, not by a key check.
  </acceptance_criteria>
  <done>The condition carries an optional tab set by the one existing producer; the registry still has one tenant and the reader count is still two.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The tab says WHERE — carry the attribution through the one-shot hand-off</name>
  <files>frontend/src/components/layout/ChatLayout.tsx, frontend/src/App.tsx, frontend/src/pages/LibraryPage.tsx, frontend/src/pages/__tests__/LibraryPage.tabAttention.test.tsx, scripts/vitest-count-gate.cjs, docs/HOT-FILE-LEDGER.md, CLAUDE.md, .planning/reported-bugs/BUG-260911-03-library-badge-does-not-say-which-tab-needs-attention.md</files>
  <read_first>
    - frontend/src/App.tsx :160-200 (the libraryTab state, `handleOpenLibraryHealth`, `handleNavigate`)
    - frontend/src/lib/libraryTabHandoff.ts (the WHOLE file — its docblock is the decision record for
      why there is exactly one writer)
    - frontend/src/components/layout/ChatLayout.tsx :530-570 (the flatMap and the NavPanel props),
      :710-730 (the drawer nav row badge), :800-815 (the hamburger dot)
    - frontend/src/pages/LibraryPage.tsx :95-120 (`TAB_LABELS`, `LIBRARY_TABS`) and the header's
      segmented-control render site
    - frontend/src/pages/librarySelection.ts (the page's reducer + `initialTab` seeding)
    - .planning/reported-bugs/BUG-260911-03-*.md (the operator's framing — keep it in the build)
    - docs/HOT-FILE-LEDGER.md § `ChatLayout.tsx`, § `App.tsx`, § `LibraryPage.tsx`
  </read_first>
  <behavior>
    RED first, in `frontend/src/pages/__tests__/LibraryPage.tabAttention.test.tsx`:
    - Test 1: given one condition with `tab: "health"`, the Health entry of the segmented control
      renders the count **1** and the other four render **nothing at all** (S-2: empty ⇒ render
      nothing; no zero badges, no reserved space). ⛔ RED today — the tabs carry no count.
    - Test 2: given THREE conditions all with `tab: "health"`, Health renders **3** — the operator's
      own point is that *"the cost scales the wrong way"* and the badge is most useful when several
      things need attention.
    - Test 3: a condition with NO `tab` marks no tab and does not crash — the field is optional.
    - Test 4: the mark is derived from `LIBRARY_TABS`/`TAB_LABELS`, not from a re-typed tab list —
      assert by adding a hypothetical sixth key in the test fixture's type and showing the render
      path has exactly one source. ⚠ Alternatively assert the rendered LABEL text for each marked
      tab equals `TAB_LABELS[key]` — the labels are the deliverable, so assert the words.
    - Test 5 (the one-shot rule must survive): after opening the Library from the badge and then
      navigating away and back, the Library opens on its OWN default, not on Health. This is the
      Phase 235 plan-15 defect; it must stay closed. Exercise `libraryTabAfterNavigate` directly —
      it is a strict leaf and needs no mount.
    - Test 6: `setLibraryTab` has exactly the writers it has today plus none — assert via a `?raw`
      count of `setLibraryTab(` occurrences in `App.tsx`, matching the base count.
  </behavior>
  <action>
    Thread the attribution down as **data**, never as a second hook call:

    1. `ChatLayout.tsx` already computes `attentionConditions` at `:534-544`. Pass the conditions (or
       a derived `Record<LibraryTab, number>` count map) down the existing Library render path so the
       page receives them as a prop. ⛔ Do not call `useSourceAttention()` or a producer again —
       arm 1 of the hoist trigger (Task 1 Test 5 fences this).
    2. `App.tsx`: leave `handleOpenLibraryHealth` and `handleNavigate` structurally as they are. ⛔ No
       second writer of `libraryTab`; the hand-off's lifetime stays owned by `libraryTabAfterNavigate`.
       ⚠ `App.tsx` carries a deliberate rule that its own comments must not spell a certain API,
       because an acceptance criterion greps this file for it and prose naming it would make a code
       measurement satisfiable by a comment. **Read that comment before editing and honour it.**
    3. `LibraryPage.tsx`: render the per-tab mark on the segmented control, derived from
       `LIBRARY_TABS` — ⛔ never a re-typed tab list. A tab with zero renders nothing.
       **How it is drawn (count vs dot) is Claude's discretion (D-244-15)** now that the kind question
       is traced — pick one, and record the choice and its one-line reason in the SUMMARY.
       ⚠ Follow the shipped icon/badge vocabulary in
       `.claude/skills/sketch-findings-agentic-rag/SKILL.md`; ⛔ do not invent a new mark — this
       surface is operator-approved (F-1) and D-244-18 forbids re-designing it.

    Register the new suite in BOTH gate knobs (`src/pages` has no bare-directory entry).
    Re-derive and update the ledger rows + sections for `ChatLayout.tsx`, `App.tsx` and
    `LibraryPage.tsx`. ⚠ `ChatLayout.tsx`'s row was stale once at `46/24/921`; `App.tsx` was absent
    from the audit for 23 phases; `LibraryPage.tsx`'s row was stale at `40/12/825`. ⛔ Re-derive.
    ⚠ The CLAUDE.md recipe omits `--follow`: `LibraryPage.tsx` measures **19** commits without it and
    **45** with it. Record which you used, and prefer the larger, honest figure.

    Finally, update `.planning/reported-bugs/BUG-260911-03-*.md` frontmatter: `status:` reflects the
    disposition and `verified_closed_by:` is set only after the driven row in `244-VALIDATION.md`
    passes — ⛔ not on a green unit test. `status:` IS the index.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/pages/__tests__/LibraryPage.tabAttention.test.tsx --reporter=basic</automated>
    <automated>cd frontend &amp;&amp; npx vitest run src/lib/__tests__/libraryTabHandoff.test.ts src/components/layout/__tests__/ChatLayout.badge.test.tsx --reporter=basic</automated>
    <automated>cd frontend &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - The Health tab renders the count when a stopped source exists, and the other four render
      nothing — asserted on the rendered text of each of the five labels, not on block presence.
      ⚠ A presence assertion cannot see content drift; this project shipped 200 green assertions over
      a surface that was "nothing at all like what we designed" for exactly that reason.
    - `grep -c 'setLibraryTab(' frontend/src/App.tsx` equals the base count. ⛔ One more is a second
      writer and fails this criterion.
    - `grep -rn 'useSourceAttention' frontend/src/pages/LibraryPage.tsx` returns **no** new call —
      the page receives data, it does not fetch a verdict.
    - The five tab labels rendered by the control still equal `TAB_LABELS`' values exactly.
    - Test 1 and Test 5 were each seen RED (Test 5 against a deliberately re-introduced second
      writer); both RED outputs quoted in the SUMMARY.
    - `npx tsc -p tsconfig.app.json --noEmit` error SET is a subset of the 67-error base set. ⛔ The
      grep count above is a signal only — the SUMMARY must publish the SET diff, not the number.
    - `ChatLayout.badge.test.tsx` still passes at its pinned BASELINE (5) and the `?raw` reader
      inventory is still two.
    - The SUMMARY states explicitly that `SHELL-05` does NOT close here: D-244-15(a) — a genuinely
      stopped watch raising the badge while the operator is elsewhere, and a HEALTHY source raising
      nothing — is a `244-VALIDATION.md` row and has never been driven.
  </acceptance_criteria>
  <done>The badge's count is attributed to the tab that owns it, threaded as data through the existing one-shot hand-off, with no second producer, no second reader and no second writer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → `useSourceAttention` | the `stopped[]` verdict; the server applies the soft-failure debounce, the client renders it |
| shell → Library page | a navigation intent (`libraryTab`) held above an unmounting page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-04-01 | Spoofing | a FALSE attention signal for a healthy source | mitigate | ⛔ **No client-side verdict** — `D-235-05`. The client renders `stopped[]` as given and derives nothing. The tab field is set from the producer's own key, not from a client heuristic. The healthy-source negative is a driven row in `244-VALIDATION.md` (the ROADMAP's named failure mode). |
| T-244-04-02 | Information disclosure | a raw provider error string reaching the UI via the new field | mitigate | `detail` stays `SENTENCE_FOR_CAUSE[...]` from the vocabulary leaf; the new field is a `LibraryTab` union member, which cannot carry free text. Task 1 Test 4 asserts the sentences are byte-identical. |
| T-244-04-03 | Tampering | a second writer of `libraryTab` silently redefining where the Library opens | mitigate | ⛔ Exactly the Phase 235 plan-15 defect. Task 2 Test 6 pins the writer count by `?raw`; Test 5 exercises the spent-on-navigate rule directly on the strict leaf. |
| T-244-04-04 | Denial of service | a third `useSourceAttention()` reader multiplying the poll | mitigate | The attribution is threaded as data. The `?raw` inventory fence (Task 1 Test 5) turns a third reader red. ⚠ The registry's own re-open trigger arm 1 is recorded as fired-and-not-taken, with `244-03`'s `useAskUserPrompt` data point cross-referenced. |
| T-244-04-05 | Repudiation | `SURF-03` ticked on a code reading for a second milestone | mitigate | The plan states in writing that `SHELL-05` does not close here; the driven halves (a) and (b) are rows in `244-VALIDATION.md`. ⛔ `verified_closed_by:` on `BUG-260911-03` is set only after the driven row. |
| T-244-04-SC | Tampering | npm/pip/cargo installs | mitigate | ⛔ **This plan installs NO packages.** If one is needed, STOP: `## Package Legitimacy Audit` first, `[ASSUMED]`/`[SUS]` behind a blocking human checkpoint. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root; verdict verbatim;
  publish the trajectory.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — SET DIFF against the 67-error base.
- `node scripts/check-hot-file-ledger.cjs 244` — quote the parsed-file count.
- ⛔ `SHELL-05` does not close here. Both halves of D-244-15 are driven rows in `244-VALIDATION.md`:
  **(a)** a genuinely stopped watch raises the badge while the operator is doing something else, and
  a **healthy** source does not; **(b)** the badge's count is attributed to the owning tab.
</verification>

<success_criteria>
- `AttentionCondition` carries an optional `tab`, set by the one existing producer.
- `ATTENTION_PRODUCERS.length === 1` still holds and is asserted in this plan's own suite.
- The owning tab is marked; the other four render nothing.
- No second producer, no third `useSourceAttention()` reader, no second `libraryTab` writer.
- The plan states in writing that this surface is deliberately NOT sketched (D-244-18 / F-1).
</success_criteria>

<output>
Create `.planning/phases/244-the-chat-shell-and-the-composer/244-04-SUMMARY.md` when done.
</output>
