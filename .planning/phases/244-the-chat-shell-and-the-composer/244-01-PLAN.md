---
phase: 244-the-chat-shell-and-the-composer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/chat/MessageList.tsx
  - frontend/src/components/layout/ChatHistoryColumn.tsx
  - frontend/src/hooks/useThreads.ts
  - frontend/src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx
  - frontend/src/components/layout/__tests__/ChatHistoryColumn.rowIdentity.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md
autonomous: true
requirements: [SHELL-01]

must_haves:
  truths:
    - "The operator scrolls a long conversation and the nav rail does not move — measured as an unchanged leaf bounding rect, not a screenshot (SHELL-01, D-244-19)."
    - "No dead space opens under the composer at any window height, nor after the workspace panel opens and closes."
    - "The page root does not overflow: document.scrollingElement.scrollHeight <= clientHeight."
    - "BUG-260911-02 is either closed against a TRACED mechanism named with file:line, or recorded as not-reproducible with the production check written down — never patched from a reading (C-6)."
    - "A folder-scoped thread row shows its title, not its folder: the folder chip can no longer starve the title (BUG-260816-03c)."
    - "An unfiled thread row renders no chip at all, per the shipped 'empty ⇒ render nothing' rule (BUG-260816-03b, S-2)."
  artifacts:
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "min-h-0 on the grid track and the <main> cell"
      contains: "min-h-0"
    - path: "frontend/src/components/chat/ChatArea.tsx"
      provides: "min-h-0 on the chat column root"
      contains: "min-h-0"
    - path: "frontend/src/components/chat/MessageList.tsx"
      provides: "a BOUNDED ScrollArea"
      contains: "min-h-0"
    - path: "frontend/src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx"
      provides: "a ?raw fence pinning all four links of the min-h-0 chain so a class cannot be deleted silently"
    - path: "frontend/src/components/layout/__tests__/ChatHistoryColumn.rowIdentity.test.tsx"
      provides: "driven-RED assertions on the row's width budget and the absent Unfiled chip"
    - path: ".planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md"
      provides: "the production check and the traced mechanism for BUG-260911-02"
  key_links:
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "frontend/src/components/chat/ChatArea.tsx"
      via: "the grid track → <main> → chat column height chain"
      pattern: "min-h-0"
    - from: "frontend/src/components/chat/ChatArea.tsx"
      to: "frontend/src/components/chat/MessageList.tsx"
      via: "flex-1 child whose min-height must be 0 for ScrollArea to bound"
      pattern: "min-h-0"
---

<objective>
Bound the chat frame so the message list scrolls **inside** the chat pane, and repair the two
chat-list defects that make the rail lie about what it is showing.

Purpose: `SHELL-01` / `BUG-260828-08`. The rail moving and dead space under the composer are the
two most-felt defects on the app's default surface. `BUG-260911-02` and `BUG-260816-03` fold in
here (D-244-16) because they contend for the same files — G-8 says two plans contending for the
same files are one plan with tasks.

Output: a four-link `min-h-0` chain with a `?raw` fence; a written trace closing (or honestly
retiring) `BUG-260911-02`; a thread row that spends its width on the thread's identity.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION, before reading the plan and before any other command:**

```bash
bash scripts/bootstrap-worktree.sh "$(pwd)"
```

Then assert the base. Agent worktrees start on the DEFAULT branch (`master`), which is stale —
five consecutive executors had to reset:

```bash
git rev-parse HEAD          # MUST resolve to 1ff80a1da or a descendant on develop
```

⛔ NEVER `rm -rf` a worktree — tear down with `bash scripts/teardown-worktree.sh <path>`.

**⚠ Gate state INHERITED at this base. Do not misread it as this plan's damage:**
- `GSD_VITEST_MAX_WORKERS=2`, gate run from the **repo root**, verdict line read **verbatim**.
- `src/components/sources/sourceComposition.test.tsx` is a standing red (~18 failed / 31 passed) in
  **neither** gate knob, by a Phase 235 decision.
- `SEED-171`'s five cap-independent flaky suites exist. ⛔ Capture failing filenames from the gate's
  **own persisted JSON** BEFORE re-running anything. Never reach for the cap.
- `npx tsc --noEmit` in `frontend/` checks **ZERO** files (solution-style config). Use
  `npx tsc -p tsconfig.app.json --noEmit` and measure a **SET DIFF** — base is **67** errors, so
  "zero errors" is not a reachable criterion.

**⚠ Registry files are append-only and do NOT create a wave dependency.**
`scripts/vitest-count-gate.cjs`, `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md` are listed in
`files_modified` for auditability. Every plan in this phase appends its own block in its own
region. A merge conflict here is resolved by taking **BOTH** sides. ⛔ Never delete another plan's
`TARGETS` line, `BASELINE` pin or ledger row.

**⛔ REGISTRY MERGE ORDER — a policy is not a mechanism (added at the revision pass).**
*"Take BOTH sides"* is stated in every plan and **enforced by nothing**. Waves 1 and 2 each run
**two** plans concurrently, and all six append into the **same regions**: the scan-list table in
`CLAUDE.md`, the per-file sections in `docs/HOT-FILE-LEDGER.md`, and the `TARGETS` / `BASELINE`
blocks in `scripts/vitest-count-gate.cjs`. Simultaneous row insertions there produce **real git
conflicts**, not clean auto-merges. ⛔ **So SERIALIZE the registry hunks:**

1. Merge the **lower-numbered** plan of the wave first, whole — wave 1: `244-01`, then `244-02`;
   wave 2: `244-03`, then `244-04`.
2. Rebase the higher-numbered plan onto that merge and **re-apply its registry hunks by hand** —
   its `TARGETS` line, its `BASELINE` pin, its ledger rows and its ledger sections. ⛔ Do not accept
   an automatic resolution on these three files.
3. Re-run the registry gates from the **repo root** AFTER the second merge, never only after the
   first: `node scripts/check-hot-file-ledger.cjs 244` and `node scripts/check-claude-md-size.cjs`,
   then the wave's single full `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`.

⛔ **A row lost to a bad auto-merge is SILENT** — `[no-row]` fires only for files a plan NAMES, so a
row dropped from another plan's file stays invisible until that file's next phase, which is the
`App.tsx` 23-phase failure exactly. ⛔ **A row added twice fails `[duplicate-row]`**, which is why
ownership is **one file → exactly one task** (the map is in `244-PATTERNS.md` § C-8).

**⚠ D-244-17 DEVIATION — recorded beside the original, never overwriting it. SIX plans, not four.**

D-244-17 locks *"FOUR plans, decomposed by surface seam"*, with plan 2 carrying **all** of the
composer: the cap-pause unlock **plus** the local attach **plus** the cloud re-point **plus** the
Library's folder-asking import. That grouping is not reachable, for the one legitimate split reason
in the planner's own charter — **context cost**, never difficulty:

- **C-2** turns `SHELL-02` from a one-expression frontend change into a **two-layer** change, adding
  a `runs`-read edit on `backend/app/api/threads.py` (244 commits / 81 phases) against a backend
  baseline at its **ceiling of 71 with zero headroom**.
- ⚠ **C-9** (measured during planning, recorded in `244-02`) shows `SHELL-04`'s *"and the agent can
  use it"* clause is unsatisfied for **8 of the 15 accepted extensions** — `workspace_read` returns a
  note for every binary, and the sandbox had no reach. That is a third backend seam inside the same
  requirement.
- `SHELL-04` therefore spans **~16 files across both tiers**: validator category, sandbox hydration,
  system-prompt line, chip component, copy port, composer composition, transcript render, upload
  client, a request model, a route, a frontend client and a Library mount.

One agent carrying that lands far past the ~50% context budget, which is where quality degrades.
**The seam decomposition D-244-17 chose is honoured exactly** — it is `SHELL-04` alone that is cut
into three (**the agent can use it** · **the composer affordance** · **the two doors**), each a
wave-sized unit of 2-3 tasks. ⛔ Nothing was cut to save time: the verifier, the TDD RED drives,
`security_enforcement` / `code_review` and migration discipline all stand (G-8's never-cut list).
⭐ The lever taken instead is G-8's own: **targeted suites per task, FULL gates once per WAVE.**

**⚠ Solo running (D-244-21):** Gemini is unavailable. Any review of this work is a **self-review**.
Never write "reviewed"; write "self-verified".
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-PATTERNS.md
@.planning/reported-bugs/BUG-260828-08-whole-page-scrolls-in-chat.md
@.planning/reported-bugs/BUG-260911-02-first-click-on-a-thread-selects-it-but-does-not-open-it.md
@.planning/reported-bugs/BUG-260816-03-thread-row-identity-icon-folder-chip-title-truncation.md

<interfaces>
<!-- The CORRECT chain, shipped and working — copy this, do not invent one. -->
<!-- frontend/src/components/metadata/DocumentDetailPanel.tsx:257 and :300 -->

```tsx
    <div className="flex h-full min-h-0 flex-col">
      {/* fixed header … */}
      <div className="min-h-0 flex-1 overflow-y-auto">
```

<!-- The BROKEN chain, measured. It is FOUR links, not the two F-5 names. -->

| # | File:line | Class list today | `min-h-0`? |
|---|---|---|---|
| 1 | `ChatLayout.tsx:546` | `flex h-screen bg-background` | n/a (root) |
| 2 | `ChatLayout.tsx:790` | `grid min-w-0 flex-1 overflow-hidden motion-safe:transition-[grid-template-columns] motion-safe:duration-300` | ⛔ missing |
| 3 | `ChatLayout.tsx:796` | `<main className="min-w-0 overflow-hidden">` | ⛔ missing |
| 4 | `ChatArea.tsx:552` | `flex flex-col h-full bg-background` | ⛔ missing |
| 5 | `MessageList.tsx:212` | `<ScrollArea className="flex-1">` | ⛔ missing |

<!-- Why the primitive cannot save it — frontend/src/components/ui/scroll-area.tsx:12-14, 31 -->
<!-- Root is `relative overflow-hidden`; Viewport is `h-full w-full`. The Root's height comes  -->
<!-- from flex-1 on an item whose min-height is `auto`, so it grows to content and the         -->
<!-- Viewport's h-full resolves to that grown height. Nothing scrolls internally.              -->
<!-- The sibling call site PROVES the mechanism: ReadDocumentBody.tsx:53 uses                  -->
<!-- `<ScrollArea className="max-h-64">` — an explicit bound — and scrolls correctly.          -->

<!-- The thread row today — frontend/src/components/layout/ChatHistoryColumn.tsx:168-194 -->

```tsx
              <div className="px-3 flex items-center gap-2 overflow-hidden whitespace-nowrap">
                <MessageSquare className="h-3.5 w-3.5 shrink-0 opacity-50" aria-hidden="true" />
                <span className="text-sm truncate flex-1 min-w-0" title={thread.title}>
                  <HighlightTitle title={thread.title} query={query} />
                </span>
                {groupMode === "folder" ? (
                  <span className="inline-flex items-center gap-1 text-[10px] shrink-0 whitespace-nowrap text-muted-foreground">
                    {bucketFor(thread.updated_at)}
                  </span>
                ) : (
                  <span className={cn(
                      "inline-flex items-center gap-1 text-[10px] shrink-0 whitespace-nowrap",
                      thread.folder_id ? "text-muted-foreground" : "text-muted-foreground/70 italic",
                    )}>
                    {thread.folder_id && <FolderIcon className="h-3 w-3 shrink-0" aria-hidden="true" />}
                    {folderLabel(folders, thread.folder_id)}
                  </span>
                )}
              </div>
```

<!-- The click path — single-step, measured (C-6). There is NO select-then-open two-step. -->
<!-- ChatHistoryColumn.tsx:166  onClick={() => onSelectThread(thread)}                     -->
<!-- useThreads.ts:35-37        selectThread = (t) => setSelectedThread(t)                 -->
<!-- ChatLayout.tsx:796         <main> → <ChatArea thread={selectedThread} …/>             -->

<!-- The `Thread` wire type — frontend/src/types/index.ts:7-14. NOTE: no workflow/kind     -->
<!-- discriminator exists, which is why BUG-260816-03(a) is OUT of this plan (see below).  -->
```ts
export interface Thread {
  id: string
  user_id: string
  title: string
  folder_id: string | null
  created_at: string
  updated_at: string
}
```
</interfaces>

**⚠ SCOPE RULING on `BUG-260816-03`, made rather than discovered — and it starts from C-7.**

⭐ **C-7: the report is PARTLY ALREADY BUILT.** Measured at this base, `ChatHistoryColumn.tsx:168-194`
already renders an icon (`<MessageSquare … opacity-50 />`), a truncating title
(`truncate flex-1 min-w-0` + `title={thread.title}` + `HighlightTitle`), and a folder chip with an
italic "Unfiled" fallback, mode-switched against a date bucket. ⛔ **Read the bug report against
THIS markup before planning work** — this is the F-4 pattern (already-shipped) one layer down, and
rebuilding shipped code is the Phase 242 failure this CONTEXT's F-1..F-4 exist to stop repeating.

The report has three sub-defects. **(b)** the repeated "Unfiled" chip and **(c)** the folder chip
truncating the title are both planned here: each has a measured cause in the markup above and the
report states its own answer (*"what wins when space is short — title, always"*; *"whether unscoped
renders nothing"*). **(a)** — one icon for every thread type — is **NOT** taken here, and the reason
is structural, not a difficulty judgement:

1. `Thread` (above) carries **no** kind/workflow discriminator, so (a) needs a **feed change**
   (`GET /threads` gaining a field) before any row can branch. That data is present in no source
   artifact for this phase.
2. The report itself says **"G-2 FIRES, HARD, AND THIS ROUTES TO `/gsd:sketch` BEFORE ANY SPEC OR
   DISCUSS"**, and **D-244-18 rules that G-2 fires on exactly one surface in Phase 244** (sketch
   236, the composer). Sketching a second surface here would contradict a locked decision.

⛔ The executor MUST record this in the SUMMARY as an **explicit deferral with a re-open trigger**
(*the next phase that adds a thread-kind field to `GET /threads`, or the next chat-list sketch*),
and MUST leave `BUG-260816-03`'s `status:` as `folded` — **not** `closed` — with a note naming (a)
as the open half. ⛔ Never silently ship (b)+(c) and call the report done.

**⚠ C-8 — `ChatHistoryColumn.tsx` has NO HOT-FILE-LEDGER ROW. A correction to D-244-20, found at
planning and recorded beside the original rather than overwriting it.**
D-244-20 states *"all of this phase's hot files HAVE ledger rows"*. Measured at this base:
`grep -c ChatHistoryColumn docs/HOT-FILE-LEDGER.md` → **0**, and `grep -c` in `CLAUDE.md` → **0**.
Its triple is **5 / 1 / 480**. It is below the G-5 threshold today, but a row must be added **at
creation, not at the third phase** — the `settingsSearchPayload.ts` precedent. Add it in the same
commit.

⭐ **CONFIRMED BY THE GATE, not merely reasoned:** `node scripts/check-hot-file-ledger.cjs
.planning/phases/244-the-chat-shell-and-the-composer` fires `[no-row]` on **TWO** files named by this
plan — `frontend/src/components/layout/ChatHistoryColumn.tsx` **and
`frontend/src/hooks/useThreads.ts`**. Both need a `docs/HOT-FILE-LEDGER.md` § "Scan list" row **and**
their own section, in the SAME commit. Re-derive both triples with the recipe; never guess.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound the chat frame — the four-link min-h-0 chain, fenced</name>
  <files>frontend/src/components/layout/ChatLayout.tsx, frontend/src/components/chat/ChatArea.tsx, frontend/src/components/chat/MessageList.tsx, frontend/src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx, scripts/vitest-count-gate.cjs, docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - frontend/src/components/layout/ChatLayout.tsx (read :540-560 and :780-840)
    - frontend/src/components/chat/ChatArea.tsx (read :545-575)
    - frontend/src/components/chat/MessageList.tsx (read :200-225)
    - frontend/src/components/metadata/DocumentDetailPanel.tsx :250-305 — THE ANALOG
    - frontend/src/components/panel/WorkspacePanel.tsx :615-630 — the same rule at a shell root
    - frontend/src/components/ui/scroll-area.tsx :1-40 — why the primitive cannot save it
    - frontend/src/components/panel/ReadDocumentBody.tsx :45-60 — the bounded sibling call site
    - docs/HOT-FILE-LEDGER.md — the sections for ChatLayout.tsx, ChatArea.tsx, MessageList.tsx
    - .planning/phases/244-the-chat-shell-and-the-composer/244-PATTERNS.md § "1. SHELL-01"
  </read_first>
  <behavior>
    RED first — write the fence before the fix and watch it fail:
    - Test 1: a `?raw` import of `ChatLayout.tsx` contains `min-h-0` on BOTH the grid-track div
      (the one carrying `grid min-w-0 flex-1 overflow-hidden`) and the `<main>` element.
    - Test 2: a `?raw` import of `ChatArea.tsx` shows the chat-column root class list carries
      `min-h-0` alongside `flex flex-col h-full`.
    - Test 3: a `?raw` import of `MessageList.tsx` shows the `<ScrollArea` call site is bounded —
      it carries `min-h-0` alongside `flex-1`.
    - Test 4 (the anti-regression the fence exists for): `src/` contains exactly TWO `<ScrollArea`
      call sites, and NEITHER is unbounded (each carries `min-h-0`, `max-h-`, or `h-`).
      ⚠ Count with `grep -v '^\s*//'`-equivalent filtering so a comment mentioning `ScrollArea`
      cannot satisfy or break the count.
  </behavior>
  <action>
    Add `min-h-0` to the four links measured in the `<interfaces>` table, and NOTHING else — this is
    a class-list change, not a layout rewrite (D-243-08 carries: no streaming or frame rewrite).

    1. `ChatLayout.tsx` — the grid track div (currently `grid min-w-0 flex-1 overflow-hidden
       motion-safe:transition-[grid-template-columns] motion-safe:duration-300`) gains `min-h-0`.
    2. `ChatLayout.tsx` — `<main className="min-w-0 overflow-hidden">` becomes
       `<main className="min-w-0 min-h-0 overflow-hidden">`.
    3. `ChatArea.tsx:552` — `flex flex-col h-full bg-background` gains `min-h-0`.
    4. `MessageList.tsx:212` — `<ScrollArea className="flex-1">` becomes
       `<ScrollArea className="min-h-0 flex-1">`.

    Create `frontend/src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx` holding the
    four RED-first assertions above, using the `?raw` import idiom this repo already uses for
    source fences. In its docblock, record: the mechanism (a flex child's `min-height` defaults to
    `auto`, so `flex-1` does not bound it), the analog (`DocumentDetailPanel.tsx:257/300`), and
    ⛔ the sentence **"a fence alone is a presence assertion and does NOT satisfy SHELL-01 —
    D-244-19's measured bound lives in 244-VALIDATION.md"**.

    Register the suite in BOTH gate knobs of `scripts/vitest-count-gate.cjs`, in the same commit —
    a `TARGETS` file-by-name entry AND a `BASELINE` pin. `src/components/layout` has **no** bare
    directory entry (measured), so both lines are required (the Phase 214 lesson: TARGETS decides
    what RUNS, BASELINE decides what is GUARDED).

    Re-derive the ledger triples for `ChatLayout.tsx`, `ChatArea.tsx` and `MessageList.tsx` with
    the CLAUDE.md recipe, update BOTH the CLAUDE.md row and the `docs/HOT-FILE-LEDGER.md` section
    in this same commit. Disposition cell **≤ 200 chars**; the reasoning goes in the detail file.
    ⚠ The CLAUDE.md recipe omits `--follow`, so a renamed file under-reports — if the derived
    number is LOWER than the existing row, re-derive with `--follow` and record which you used.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx --reporter=basic</automated>
    <automated>cd "C:/Vibe Apps/Agentic RAG" &amp;&amp; node scripts/check-claude-md-size.cjs</automated>
    <automated>cd "C:/Vibe Apps/Agentic RAG" &amp;&amp; node scripts/check-hot-file-ledger.cjs 244</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'min-h-0' frontend/src/components/chat/MessageList.tsx` ≥ 1 and the match is on the
      `<ScrollArea` line.
    - `grep -n 'min-h-0' frontend/src/components/layout/ChatLayout.tsx` returns ≥ 2 matches, one on
      the grid-track div and one on `<main`.
    - `grep -n 'min-h-0' frontend/src/components/chat/ChatArea.tsx` returns a match on the root
      `flex flex-col h-full` div.
    - The fence suite ran RED before the class changes — the SUMMARY quotes the RED failure output
      verbatim. ⛔ A fence nobody has seen fire is not a fence.
    - `node scripts/check-claude-md-size.cjs` exits 0 (no `[disposition-too-long]`,
      `[duplicate-row]` or `[malformed-row]`).
    - `node scripts/check-hot-file-ledger.cjs 244` exits 0. ⚠ Exit 0 over **0 parsed files** is a
      VACUOUS pass (measured at Phase 242 on a CRLF plan) — the SUMMARY must quote the script's
      parsed-file count, not just the exit code.
    - `npx tsc -p tsconfig.app.json --noEmit` in `frontend/` reports a SET of errors that is a
      subset of the 67-error base set. ⛔ Not "zero errors" — that is unreachable here.
  </acceptance_criteria>
  <done>All four links carry `min-h-0`; a RED-driven `?raw` fence pins them; both gate knobs and both ledger registers updated in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Trace BUG-260911-02 against production before touching a handler</name>
  <files>frontend/src/components/layout/ChatHistoryColumn.tsx, frontend/src/hooks/useThreads.ts, frontend/src/components/layout/ChatLayout.tsx, .planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md, .planning/reported-bugs/BUG-260911-02-first-click-on-a-thread-selects-it-but-does-not-open-it.md</files>
  <read_first>
    - .planning/reported-bugs/BUG-260911-02-first-click-on-a-thread-selects-it-but-does-not-open-it.md
    - .planning/phases/244-the-chat-shell-and-the-composer/244-PATTERNS.md § "C-6"
    - frontend/src/components/layout/ChatHistoryColumn.tsx :120-200
    - frontend/src/hooks/useThreads.ts (the whole file — it is short)
    - frontend/src/components/layout/ChatLayout.tsx :640-700 (the mobile drawer row) and :780-840
    - frontend/src/components/chat/ChatArea.tsx — the welcome-vs-thread render gate
  </read_first>
  <action>
    ⛔ **Do not patch a handler.** C-6 measured that the click path is already single-step:
    `ChatHistoryColumn.tsx:166` → `useThreads.ts:35` `setSelectedThread(thread)` →
    `ChatLayout.tsx:796` `thread={selectedThread}`. The "first click selects but does not open"
    symptom therefore has some OTHER mechanism, and the report's own first instruction is
    *"whether it reproduces on `master` / `production` … Nobody has checked."*

    Produce `.planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md`
    containing, in this order:

    1. **The production check.** Load the live app and attempt the repro. Record the outcome as
       `reproduces` / `does not reproduce` / `could not check — <reason>`. The Supabase MCP read
       path is available and needs no approval, so a DB-side check of whether a user message row
       was written is cheap evidence — use it.
    2. **The develop check**, on this worktree's base, with the same steps.
    3. **The trace.** Drive the three candidate causes C-6 names, in order, and name the winner
       with `file:line`:
       (a) `activeView` is not `"chat"` at click time, so the column is not mounted and the click
           landed on the drawer/palette path;
       (b) a re-render/remount discards the first `setState`;
       (c) it is `ChatArea`'s own render gate (welcome vs thread branch) rather than selection.
       ⚠ Measure with a **leaf element assertion**, the way the report did
       (`document.querySelectorAll('[data-testid="thinking-trigger"]').length`), not by eye.
    4. **The disposition.** Either (i) the traced mechanism plus the minimal fix and a driven-RED
       regression test in `ChatHistoryColumn.rowIdentity.test.tsx` (or a sibling suite), or
       (ii) `not reproducible` with the two checks above as the evidence — in which case the bug
       report's `status:` stays `folded`, gains a `re_open_trigger` naming the exact steps, and the
       SUMMARY says so plainly. ⛔ Never close a bug on an un-reproduced reading.

    Update `.planning/reported-bugs/BUG-260911-02-...md` frontmatter to match the disposition
    (`status:` IS the index — prose in the body saying "still open" is invisible to the scan).
  </action>
  <verify>
    <automated>test -f .planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md &amp;&amp; grep -qE "^## (1\. )?Production check" .planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md &amp;&amp; echo TRACE_OK</automated>
  </verify>
  <acceptance_criteria>
    - The trace file exists and its production-check section records one of exactly three verdicts:
      `reproduces`, `does not reproduce`, `could not check — <reason>`. ⛔ A blank or hedged verdict
      fails this criterion.
    - The trace names a mechanism with a `file:line`, OR states `not reproducible` with both checks
      recorded. ⛔ "Likely" / "appears to" without a driven observation fails.
    - If a fix landed: a regression test exists that was seen RED against the pre-fix tree, and its
      RED output is quoted in the SUMMARY.
    - `.planning/reported-bugs/BUG-260911-02-*.md` frontmatter `status:` reflects the disposition,
      and `re_open_trigger:` is non-null whenever `status:` is not `closed`.
    - The SUMMARY does not contain the strings "two-step", "select-then-open" as a stated CAUSE
      unless the trace measured one — C-6 measured that no such handler exists.
  </acceptance_criteria>
  <done>BUG-260911-02 is closed against a traced mechanism, or honestly retired with the production check written down and a re-open trigger set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The thread row spends its width on the thread — BUG-260816-03 (b) and (c)</name>
  <files>frontend/src/components/layout/ChatHistoryColumn.tsx, frontend/src/components/layout/__tests__/ChatHistoryColumn.rowIdentity.test.tsx, scripts/vitest-count-gate.cjs, docs/HOT-FILE-LEDGER.md, CLAUDE.md, .planning/reported-bugs/BUG-260816-03-thread-row-identity-icon-folder-chip-title-truncation.md</files>
  <read_first>
    - frontend/src/components/layout/ChatHistoryColumn.tsx :120-200 (the whole row block)
    - frontend/src/lib/threadGroups.tsx — `folderLabel`, `bucketFor`, `HighlightTitle`
    - .planning/reported-bugs/BUG-260816-03-thread-row-identity-icon-folder-chip-title-truncation.md
    - frontend/src/components/chat/ActiveConnectorChips.tsx :20-30 — the shipped
      "empty ⇒ render nothing" rule (S-2) this task rides
    - docs/HOT-FILE-LEDGER.md — ⚠ there is NO section for ChatHistoryColumn.tsx; you are adding one
  </read_first>
  <behavior>
    RED first:
    - Test 1 (c): given a thread with `folder_id` set and a long folder name (use the report's own
      `"Project Meridian — Risks"`), the folder chip element's class list carries a `max-w-*`
      bound AND `truncate`. Today it carries `shrink-0` with no bound — this assertion is RED.
    - Test 2 (c): the title span keeps `truncate flex-1 min-w-0` and is NOT `shrink-0` — the
      priority ordering is title-first.
    - Test 3 (b): a thread with `folder_id === null` in DATE group mode renders **no** chip element
      at all — `queryByText(/Unfiled/i)` is null. Today it renders an italic "Unfiled" — RED.
    - Test 4 (b): a thread WITH a folder still renders the chip, with the folder's name and the
      folder icon. (The negative alone would pass on a component that renders nothing.)
    - Test 5: the row still renders the title text in full in the `title=` attribute (the shipped
      tooltip workaround must not regress).
    ⚠ These assert **rendered content and class contracts**, not block presence — a presence
    assertion cannot see content drift.
  </behavior>
  <action>
    In `ChatHistoryColumn.tsx`'s row block (`:168-194`):

    (c) **Cap the meta chip so the title wins.** Replace the folder chip's unbounded `shrink-0`
    with a bounded chip: keep `shrink-0` for layout stability but add `max-w-[96px]` and
    `truncate`, and add `title={folderLabel(folders, thread.folder_id)}` so the full folder name is
    still reachable on hover. The title span's `truncate flex-1 min-w-0` is UNCHANGED — it is
    already correct; the defect was the chip's unbounded width, not the title's.

    (b) **An unscoped row renders no chip.** Guard the DATE-mode branch: when
    `thread.folder_id` is null, render `null` instead of the italic `folderLabel(...)` fallback.
    ⭐ This rides the shipped `S-2` rule — `ActiveConnectorChips.tsx:24`,
    `AttentionPopover.tsx:75`, `PendingAskCard.tsx:733` all return `null` on empty — so it is an
    application of an established project pattern, not a new design.
    ⚠ `folderLabel` is shared with other call sites; **do not change `folderLabel` itself** — the
    absence decision belongs to this row, not to the vocabulary leaf.

    (a) is **NOT** taken — see the SCOPE RULING in `<context>`. Record the deferral and the
    re-open trigger in the SUMMARY and in the bug report's frontmatter; leave `status: folded`.

    Create `frontend/src/components/layout/__tests__/ChatHistoryColumn.rowIdentity.test.tsx` with
    the five assertions above. Register it in BOTH gate knobs in the same commit.

    Add a `docs/HOT-FILE-LEDGER.md` section AND a `CLAUDE.md` ledger row for
    `frontend/src/components/layout/ChatHistoryColumn.tsx` — it has neither today (measured
    **5 / 1 / 480**). ⛔ A row is added at CREATION, not at the third phase; an absent row is
    invisible to G-5 at any count.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/layout/__tests__/ChatHistoryColumn.rowIdentity.test.tsx --reporter=basic</automated>
    <automated>cd "C:/Vibe Apps/Agentic RAG" &amp;&amp; node scripts/check-hot-file-ledger.cjs 244</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'max-w-' frontend/src/components/layout/ChatHistoryColumn.tsx` matches on the DATE-mode
      folder chip span.
    - The literal string `Unfiled` no longer reaches the DOM for an unscoped row: the suite's
      `queryByText(/Unfiled/i)` assertion is green and was RED before the change.
    - `grep -c "ChatHistoryColumn" docs/HOT-FILE-LEDGER.md` ≥ 1 and `grep -c "ChatHistoryColumn" CLAUDE.md` ≥ 1.
    - `grep -c "useThreads.ts" docs/HOT-FILE-LEDGER.md` ≥ 1 — the gate fires `[no-row]` on it too.
    - `node scripts/check-hot-file-ledger.cjs .planning/phases/244-the-chat-shell-and-the-composer`
      prints NO `[no-row]` line for this plan's files.
    - Both a `TARGETS` line and a `BASELINE` pin exist for `ChatHistoryColumn.rowIdentity.test.tsx`
      in `scripts/vitest-count-gate.cjs` — verified by two greps, not one.
    - `.planning/reported-bugs/BUG-260816-03-*.md` frontmatter reads `status: folded` (NOT `closed`)
      with a body note naming sub-defect (a) as the open half and its re-open trigger.
    - The SUMMARY states the (a) deferral with its two structural reasons (no `Thread` discriminator;
      D-244-18 forbids a second sketch) — ⛔ never as "out of scope" without the reasons.
  </acceptance_criteria>
  <done>A folder-scoped row shows its title; an unfiled row shows no chip; (a) is deferred in writing with a re-open trigger; the ledger gained the row it never had.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → chat-list render | `thread.title` and `folderLabel(...)` are user-authored text rendered into the rail and into a `title=` attribute |
| browser → app shell | scroll/layout state; no server data crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-01-01 | Information disclosure | `ChatHistoryColumn.tsx` folder chip `title=` attribute | mitigate | React escapes the attribute value; the chip is bounded by `max-w-[96px]`+`truncate`, so a crafted long folder name cannot push the title out of the row (that IS the defect being fixed). No `dangerouslySetInnerHTML` is introduced — assert this with a grep in the suite. |
| T-244-01-02 | Tampering | `scripts/vitest-count-gate.cjs` knob edits | mitigate | Append-only; the plan adds TARGETS + BASELINE lines and deletes none. Verified by `git diff --numstat` showing 0 deletions on that file. |
| T-244-01-03 | Repudiation | `BUG-260911-02` disposition | mitigate | The trace file records the production check, the develop check and the verdict verbatim, so a "not reproducible" closure is auditable rather than asserted. |
| T-244-01-04 | Denial of service | unbounded `ScrollArea` rendering N messages with no viewport bound | mitigate | The `min-h-0` chain restores internal scrolling so the browser is not laying out the full transcript outside a bounded viewport at every window height. |
| T-244-01-SC | Tampering | npm/pip/cargo installs | mitigate | ⛔ **This plan installs NO packages.** If any task finds it needs one, STOP: a `## Package Legitimacy Audit` is required in RESEARCH.md first, and `[ASSUMED]`/`[SUS]` packages need a blocking human checkpoint. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the **repo root**, verdict line
  read **verbatim**. Record the grand total, pinned total and pinned-file count in the SUMMARY as a
  TRAJECTORY against the base reading — a growing number is the gate WORKING, not drift.
  ⛔ If it reds, capture failing filenames from the gate's own persisted JSON BEFORE re-running, and
  check each against `git diff --numstat`. Never touch the cap.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — a SET DIFF against the 67-error base.
- `node scripts/check-hot-file-ledger.cjs 244` — quote the parsed-file count, not just exit 0.
- `node scripts/check-claude-md-size.cjs` — exit 0.
- ⛔ `SHELL-01` does **not** close here. Its measured bound (D-244-19) is a G-4 row in
  `244-VALIDATION.md` and is driven at `/gsd:verify-work`.
</verification>

<success_criteria>
- All four links of the chain carry `min-h-0`, pinned by a fence that was seen RED.
- `BUG-260911-02` has a written production check and a traced mechanism or an honest retirement.
- A folder-scoped thread row renders its title; an unfiled row renders no chip.
- `ChatHistoryColumn.tsx` has a ledger row and a detail section it never had.
- Both new suites are in BOTH gate knobs, in the same commit.
</success_criteria>

<output>
Create `.planning/phases/244-the-chat-shell-and-the-composer/244-01-SUMMARY.md` when done.
</output>
