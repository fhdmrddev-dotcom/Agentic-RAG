---
phase: 165-is-global-retirement-cleanup
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useFolders.ts
  - frontend/src/hooks/useSkills.ts
  - frontend/src/components/ingestion/FolderCreateInput.tsx
  - frontend/src/components/ingestion/FolderDetail.tsx
  - frontend/src/components/ingestion/FolderNode.tsx
  - frontend/src/components/ingestion/FolderTree.tsx
  - frontend/src/components/ingestion/NavRow.tsx
  - frontend/src/components/ingestion/ViewsGroup.tsx
  - frontend/src/components/ingestion/AutomationGroup.tsx
  - frontend/src/components/skills/SkillCard.tsx
  - frontend/src/components/skills/SkillFormDialog.tsx
  - frontend/src/components/skills/tuner/CaseEditor.tsx
  - frontend/src/components/classification/RuleBuilderPanel.tsx
  - frontend/src/components/chat/tool-bodies/TreeBody.tsx
  - frontend/src/components/chat/tool-bodies/LsBody.tsx
  - frontend/src/pages/WorkflowsPage.tsx
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01 + D-165-06: frontend interface fields + component props mirror the wire — folders/skills become is_org_shared (FUNCTIONAL isOrgShared toggle); document_views/classification_rules/metadata_field_definitions/workflow_definitions become is_system_global (DISPLAY-ONLY isSystemGlobal badge)"
    - "D-165-07 (SC#2): the folder + skill share toggles are relabeled 'Global' -> 'Shared with org' and stay FUNCTIONAL (no silent un-share, no solo-org special-casing)"
    - "D-165-02: Skill.is_system frontend field is NOT renamed (the 'Built-in' pill)"
    - "The whole frontend type-checks (tsc --noEmit clean) against the renamed contract — the static consistency proof"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "the client contract mirroring the renamed wire fields"
      contains: "is_org_shared"
    - path: "frontend/src/components/ingestion/FolderDetail.tsx"
      provides: "'Shared with org' relabel on the folder toggle"
      contains: "Shared with org"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "backend wire fields"
      via: "createFolder/createSkill bodies send is_org_shared; responses read is_org_shared/is_system_global"
      pattern: "is_org_shared"
---

<objective>
Mirror the DB/backend rename in the frontend CONTRACT + COMPONENTS, and relabel the folder + skill share toggles "Global" -> "Shared with org". Per B2, each component is annotated FUNCTIONAL (`isOrgShared` toggle — folders/skills) vs DISPLAY-ONLY (`isSystemGlobal` badge — workflows/views) because several files mix both.

Purpose: MIG-02 / SC#1 / SC#2 — the frontend contract follows the wire; the folder + skill toggles read "Shared with org" and stay functional; the display-only platform badges relabel to system/built-in wording.

Output: A type-clean frontend (tsc passes) with renamed fields/props and relabeled toggles. (Frontend test files are plan 09.)
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
frontend/src/types/index.ts current is_global fields (from Read):
- MetadataFieldDef.is_global (line 248)   -> is_system_global (metadata_field_definitions) — DISPLAY-ONLY
- SavedView.is_global (line 319)          -> is_system_global (document_views) — DISPLAY-ONLY
- ClassificationRule.is_global (line 426) -> is_system_global (classification_rules) — DISPLAY-ONLY
- Folder.is_global (line 454)             -> is_org_shared — FUNCTIONAL
- Skill.is_global (line 500)              -> is_org_shared — FUNCTIONAL;  Skill.is_system (line 501) UNCHANGED
- SkillCreate.is_global (line 518)        -> is_org_shared — FUNCTIONAL

frontend/src/lib/api.ts: createFolder(name, parentId, isGlobal=false) sends body { is_global: isGlobal } -> rename param isGlobal->isOrgShared, body key -> is_org_shared. The classification-rule/workflow create comments "never supplies is_global" -> is_system_global (still server-owned; frontend never sends it).
</interfaces>

<per_component_functional_vs_display>
FUNCTIONAL isOrgShared toggle (folders/skills) — incl. the "Global"->"Shared with org" copy (SC#2):
- FolderCreateInput.tsx, FolderDetail.tsx, FolderNode.tsx, FolderTree.tsx (folder share toggle/badge)
- SkillCard.tsx, SkillFormDialog.tsx (skill share toggle; KEEP the is_system "Built-in" pill separate)
- NavRow.tsx (folder/view row — folder branch is functional isOrgShared; a view branch is display isSystemGlobal — MIXED, split per-line)
- TreeBody.tsx, LsBody.tsx (render folder is_org_shared on the ls/tree cards)
- tuner/CaseEditor.tsx (skills tuner — isOrgShared)

DISPLAY-ONLY isSystemGlobal badge (platform tables):
- ViewsGroup.tsx (saved-views 'G' pill -> system/built-in badge)
- WorkflowsPage.tsx (workflow seeded badge)
- RuleBuilderPanel.tsx (classification rules)
- AutomationGroup.tsx (views/rules automations — display)

MIXED (contains both — split per-line): NavRow.tsx, AutomationGroup.tsx. Read each occurrence's field/table before renaming.
</per_component_functional_vs_display>

<rename_map>
- folders/skills:  is_global -> is_org_shared ;  isGlobal -> isOrgShared
- document_views/classification_rules/metadata_field_definitions/workflow_definitions:  is_global -> is_system_global ;  isGlobal -> isSystemGlobal
- Skill.is_system / isSystem: UNCHANGED (D-165-02, the 'Built-in' pill)
- UI copy on folder + skill share toggles: "Global" -> "Shared with org" (D-165-07); display-only platform badges -> system/built-in wording
</rename_map>

<dev_server_note>
W1 (dev-server): this plan reads renamed wire fields; a running vite dev server pointed at a still-old backend (pre-apply) will show broken folder/skill/view data until plan 165-10 applies the migration. Operator: stop dev servers before Wave 1, restart after 165-10.
</dev_server_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the frontend contract layer (types + api + hooks)</name>
  <files>frontend/src/types/index.ts, frontend/src/lib/api.ts, frontend/src/hooks/useFolders.ts, frontend/src/hooks/useSkills.ts</files>
  <read_first>
    - frontend/src/types/index.ts (the 6 is_global fields at lines 248/319/426/454/500/518 + line 501 is_system)
    - frontend/src/lib/api.ts (is_global occurrences: createFolder ~1579-1584 + the classification-rule/workflow comments)
    - frontend/src/hooks/useFolders.ts, frontend/src/hooks/useSkills.ts (is_global/isGlobal in the folder/skill CRUD hooks)
  </read_first>
  <action>
    types/index.ts: Folder/Skill/SkillCreate.is_global -> is_org_shared (Skill.is_system stays); MetadataFieldDef/SavedView/ClassificationRule.is_global -> is_system_global. Update the doc comments naming the flag to the new field name.
    api.ts: rename the createFolder param isGlobal -> isOrgShared and the request body key is_global -> is_org_shared; update response field reads + the server-owned comments (classification rules/workflows -> is_system_global). Any skill-create body is_global -> is_org_shared.
    hooks: useFolders.ts (folders -> is_org_shared), useSkills.ts (skills -> is_org_shared; keep is_system).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -Ec "is_global|isGlobal" | sed 's/^/tsc-token-refs-in-contract: /'</automated>
  </verify>
  <acceptance_criteria>
    - The four files carry 0 six-table `is_global`/`isGlobal` (any unrelated token documented).
    - types/index.ts: Folder/Skill/SkillCreate carry is_org_shared; SavedView/ClassificationRule/MetadataFieldDef carry is_system_global; Skill.is_system unchanged.
    - api.ts createFolder sends is_org_shared in its body.
    - Remaining tsc errors (if any) reference only Task-2 component files.
  </acceptance_criteria>
  <done>The contract layer mirrors the renamed wire; is_system preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rename components (functional vs display per annotation) + relabel toggles "Global" -> "Shared with org"</name>
  <files>frontend/src/components/ingestion/FolderCreateInput.tsx, frontend/src/components/ingestion/FolderDetail.tsx, frontend/src/components/ingestion/FolderNode.tsx, frontend/src/components/ingestion/FolderTree.tsx, frontend/src/components/ingestion/NavRow.tsx, frontend/src/components/ingestion/ViewsGroup.tsx, frontend/src/components/ingestion/AutomationGroup.tsx, frontend/src/components/skills/SkillCard.tsx, frontend/src/components/skills/SkillFormDialog.tsx, frontend/src/components/skills/tuner/CaseEditor.tsx, frontend/src/components/classification/RuleBuilderPanel.tsx, frontend/src/components/chat/tool-bodies/TreeBody.tsx, frontend/src/components/chat/tool-bodies/LsBody.tsx, frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - grep -rn "is_global|isGlobal|Global" across the listed component files (find field usage AND the user-facing "Global" label on the folder/skill toggles)
    - The per_component_functional_vs_display block above (which files are FUNCTIONAL toggles vs DISPLAY badges; NavRow + AutomationGroup are MIXED)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (keep the relabel consistent with FolderNode/FolderDetail/SkillCard/ViewsGroup — cosmetic relabel of existing toggles, no new UI)
    - frontend/src/types/index.ts (the renamed field names from Task 1)
  </read_first>
  <action>
    Rename per the per_component annotation: folder/skill functional files + tool-bodies + tuner/CaseEditor -> is_org_shared/isOrgShared (Skill.is_system stays); ViewsGroup/WorkflowsPage/RuleBuilderPanel display badges -> is_system_global/isSystemGlobal. For the MIXED files (NavRow, AutomationGroup) read each occurrence's field/table: a folder branch -> is_org_shared, a view/rule branch -> is_system_global.

    UI copy (D-165-07 / SC#2): on the FOLDER and SKILL share toggles/labels, change the user-facing "Global" text to "Shared with org". The toggle stays functional — do NOT special-case solo orgs, do NOT disable it. For the DISPLAY-ONLY is_system_global badges (ViewsGroup 'G' pill, WorkflowsPage seeded badge), relabel to system/built-in wording per the sketch-findings icon/badge convention — display-only, not a user toggle.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the whole app type-checks against the renamed types — the load-bearing static proof the rename is consistent end-to-end).
    - The six-table is_global/isGlobal fields across the components return 0 (grep).
    - The folder + skill toggle label reads "Shared with org" (`grep -rn "Shared with org" frontend/src/components` shows the folder + skill toggle sites); no lingering user-facing "Global" toggle label on folders/skills.
    - Skill.is_system ("Built-in" pill) usages unchanged.
    - MIXED files (NavRow, AutomationGroup): folder branch is is_org_shared, view/rule branch is is_system_global (spot-check).
  </acceptance_criteria>
  <done>Components consume the renamed fields with functional/display split correct, the folder/skill toggles read "Shared with org", and the whole frontend type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client field name -> wire field name | The frontend interface must mirror the renamed wire or reads silently return undefined |
| toggle label -> user mental model | "Shared with org" must remain a functional share toggle (no silent un-share) — SC#2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-19 | Info-disclosure (UX) | a folder/skill silently un-shares because the toggle stops writing the flag | mitigate | Value-preserving rename (field renamed, wired the same); tsc + the plan-09 toggle test assert the toggle still POSTs is_org_shared; no solo-org special-casing (D-165-07). |
| T-165-20 | Tampering | a display-only is_system_global badge implies a settable control | accept | is_system_global is display-only in the frontend; the server owns the write-lock (plan 03) — a spoofed client body cannot set it. |
| T-165-21 | Tampering | a functional/display or folder/view mis-rename in a MIXED file (NavRow/AutomationGroup) | mitigate | Per-occurrence table read + the tsc gate; the MIXED files are called out explicitly. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (whole-app type consistency — the frontend static proof).
- Zero six-table is_global/isGlobal tokens remain in the component/contract files; folder + skill toggles read "Shared with org"; Skill.is_system preserved.
- MIXED files split correctly (folder->is_org_shared, view/rule->is_system_global).
</verification>

<success_criteria>
The frontend contract + components mirror the renamed wire (`is_org_shared`/`is_system_global`) with the functional/display split correct, relabel the folder + skill toggles to "Shared with org" while keeping them functional, preserve `Skill.is_system`, and type-check clean.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-08-SUMMARY.md` when done.
</output>
