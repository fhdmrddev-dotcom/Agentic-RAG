---
phase: 165-is-global-retirement-cleanup
plan: 08
type: execute
wave: 3
depends_on: ["165-07"]
files_modified:
  - backend/tests/integration/test_v3_4_org_isolation.py
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "SC#4 / SEED-124: test_browse_tools_cross_org_leak_KNOWN_OPEN_seed124 flips xfail(strict)->XPASS and its xfail marker is REMOVED, leaving a normal passing assertion — the two-org live drive is the arbiter, not code inspection"
    - "D-165-01: the exit-gate fixture's raw-SQL is_global literals are renamed (folders/skills -> is_org_shared) and the _DEFINER_FUNCTIONS tuple + folder leg reference folder_is_org_shared; skills.is_system literals unchanged"
    - "The two-org isolation suite is GREEN end to end (23 passed once the leak test flips + its marker is removed) — no over-widening regression on the 15 seeded workflows"
    - "MIG-02 rename-regression: the broader backend RLS/model suite runs green against the renamed DB (no is_global-literal breakage remains)"
  artifacts:
    - path: "backend/tests/integration/test_v3_4_org_isolation.py"
      provides: "exit gate with the leak test converted to a normal passing assertion + renamed literals"
      contains: "folder_is_org_shared"
  key_links:
    - from: "backend/tests/integration/test_v3_4_org_isolation.py"
      to: "backend/app/utils/folder_utils.get_globally_visible_folder_ids"
      via: "the browse-tool leak test drives the org-scoped helper as disjoint-org B"
      pattern: "get_globally_visible_folder_ids"
---

<objective>
Close SC#4 for real: flip the SEED-124 known-open leak test to XPASS by running the plan-02 org-scoped helpers against the applied-renamed DB, then REMOVE its xfail marker so it stands as a normal passing assertion. Rename the exit-gate fixture's remaining `is_global` literals + the `folder_is_globally_visible` references, and run the two-org suite + the broader backend RLS/model suite green.

Purpose: The phase's acceptance arbiter (per CONTEXT <specifics>): SC#4 is only closed when `test_browse_tools_cross_org_leak_KNOWN_OPEN_seed124` flips xfail->XPASS AND is removed — never on code inspection alone. Plus MIG-02 rename-regression: the renamed DB + code must run the suite green.

Output: A green two-org exit gate (23 passed), the leak test as a normal passing test with the marker gone, and a clean backend regression sweep.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md

<interfaces>
backend/tests/integration/test_v3_4_org_isolation.py — the edit targets (from Read):
- Line 72-77: `_DEFINER_FUNCTIONS` tuple includes "folder_is_globally_visible" -> rename to "folder_is_org_shared".
- Lines 170-173: fixture seeds `INSERT INTO public.folders (id, user_id, org_id, name, is_global) VALUES (…, true)` -> is_org_shared.
- Lines 615-624: `_seed_system_skill` INSERT `skills (…, is_system, is_global) VALUES (…, true, true)` -> is_org_shared (keep is_system); comment "is_system=true + is_global=true" -> "is_system=true + is_org_shared=true".
- Lines 669-674: `test_user_is_global_stays_org_scoped` INSERT `skills (…, is_global) VALUES (…, true)` -> is_org_shared (rename the test name/body wording to is_org_shared as desired but keep behavior).
- Lines 419-426 + 71: the `folder_is_globally_visible` DEFINER leg (call + docstring + pg_proc audit) -> folder_is_org_shared.
- Lines 866-903: `test_browse_tools_cross_org_leak_KNOWN_OPEN_seed124` — the @pytest.mark.xfail(strict=True, …) marker + the KNOWN_OPEN naming.

Post-plan-02 the helper `get_globally_visible_folder_ids(sb, b["uid"])` is org-scoped, so as disjoint-org B it returns 0 of A's shared folder → the existing assertion `a["shared_folder_id"] not in visible_ids` PASSES.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the exit-gate fixture literals + folder_is_globally_visible references</name>
  <files>backend/tests/integration/test_v3_4_org_isolation.py</files>
  <read_first>
    - backend/tests/integration/test_v3_4_org_isolation.py (the whole file — all is_global raw-SQL literals + the _DEFINER_FUNCTIONS tuple + the folder_is_globally_visible leg)
    - supabase/migrations/111_is_global_retirement_rename.sql (the authoritative new names now live in the DB)
    - backend/app/utils/folder_utils.py (plan 02 — the org-scoped helper the leak test drives)
  </read_first>
  <action>
    Rename per the interfaces block: the folders raw-SQL literal is_global -> is_org_shared; the skills raw-SQL literals is_global -> is_org_shared (keep is_system); the `_DEFINER_FUNCTIONS` tuple entry + the fourth DEFINER leg's function call + docstring + pg_proc `proname` assertion `folder_is_globally_visible` -> `folder_is_org_shared`. Update comment wording (is_system/is_global -> is_system/is_org_shared) to stay honest. Do NOT touch the xfail leak test yet (Task 2). Do NOT change any assertion VALUES or the two-DISJOINT-org topology — token rename only.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_v3_4_org_isolation.py --collect-only -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The only remaining `is_global` token in the file is inside the still-marked leak test / SEED-124 comment block (Task 2 removes it): `grep -n is_global backend/tests/integration/test_v3_4_org_isolation.py` shows only the KNOWN_OPEN test region.
    - `folder_is_globally_visible` is fully replaced by `folder_is_org_shared` in the tuple + the DEFINER leg.
    - skills `is_system` literals preserved.
    - `--collect-only` exits 0.
  </acceptance_criteria>
  <done>The fixture literals + DEFINER-fn references use the renamed identifiers; only the leak-test region still references is_global.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the SEED-124 leak test to XPASS, remove the xfail marker, and run the two-org suite green (SC#4 arbiter)</name>
  <files>backend/tests/integration/test_v3_4_org_isolation.py</files>
  <read_first>
    - backend/tests/integration/test_v3_4_org_isolation.py lines 795-903 (the SEED-124 KNOWN-OPEN section header comment + the xfail(strict) marker + test_browse_tools_cross_org_leak_KNOWN_OPEN_seed124)
    - backend/app/utils/folder_utils.py (plan 02 — proves the helper is org-scoped so the assertion now passes)
    - reference_local_dev_app / reference_evidence_tools_inventory (MEMORY: local Supabase @ :54321/:54322 for the service-role probe the leak test uses)
  </read_first>
  <action>
    First run the leak test AS-IS (still xfail-marked) against the applied DB: with plan-02's org-scoping live, the assertion `a["shared_folder_id"] not in visible_ids` now PASSES → the strict-xfail test XPASSES → strict xfail FAILS (this is the DESIGNED signal that the leak is closed).

    Then REMOVE the `@pytest.mark.xfail(strict=True, …)` decorator entirely, and rename the test from `test_browse_tools_cross_org_leak_KNOWN_OPEN_seed124` to a normal passing name (e.g. `test_browse_tools_cross_org_isolation_seed124_closed`) — leaving the SAME assertion body as a normal passing test. Update the section header comment from "KNOWN-OPEN (folded to Phase 165)" to "CLOSED by Phase 165 (SEED-124 / is_global retirement)". Do NOT weaken the assertion — the two-org drive against the real service-role client is the arbiter.

    Then run the FULL two-org suite. Expect 23 passed (the prior 22 passed + the now-normal-passing former-xfail; skip legs remain skip-guarded if the local Supabase REST gate is unreachable — in that case the leak leg SKIPS, and the operator must confirm it PASSES once the stack is up). If the leak leg cannot run (service-role gate down), surface that as a BLOCKING note: SC#4 is not arbitrated until it passes live.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_v3_4_org_isolation.py -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The `xfail(strict=True)` marker on the SEED-124 leak test is REMOVED; the test is renamed to a non-KNOWN_OPEN passing name; the assertion body is unchanged (not weakened).
    - `pytest tests/integration/test_v3_4_org_isolation.py` reports 0 failed / 0 xpassed-as-fail; the former-xfail test PASSES (or SKIPS with an explicit operator-confirmation note if the local service-role REST gate is unreachable — SC#4 requires the live PASS).
    - `grep -c "is_global" backend/tests/integration/test_v3_4_org_isolation.py` == 0 and `grep -c "KNOWN_OPEN\\|xfail" backend/tests/integration/test_v3_4_org_isolation.py` == 0 (the marker + known-open naming are gone).
    - The suite total is 23 passed (was 22 passed / 1 xfailed) — the platform-universal guard (test_is_system_stays_universal) + over-widening guard (test_user_is_global_stays_org_scoped, renamed) still pass, proving the 15 seeded workflows / is_system escape stay correct and no cross-org over-widening.
  </acceptance_criteria>
  <done>SC#4 is arbitrated: the leak test passes live as a normal test with the xfail marker removed, and the full two-org suite is green (23 passed) with no over-widening.</done>
</task>

<task type="auto">
  <name>Task 3: Backend rename-regression sweep (MIG-02)</name>
  <files>backend/tests/integration/test_v3_4_org_isolation.py</files>
  <read_first>
    - The plan 05 SUMMARY (which backend test files were renamed) + plan 02/03/04 SUMMARYs (the renamed code)
    - MEMORY: project_e2e_suite_rotted / frontend_vitest_rot / any known pre-existing backend rot baselines so a pre-existing failure is not mis-attributed to the rename
  </read_first>
  <action>
    Run the backend RLS + model + skills + workflow test suites that reference the renamed columns (at minimum: tests/integration/test_163_rls_*.py, test_163_rls_platform_universal.py, test_v3_4_org_isolation.py, the skills/views/rules/metadata/workflow test modules touched in plan 05). Confirm they pass against the renamed live DB. Any failure must be triaged: rename-induced (fix the missed literal in the owning plan's file — record it) vs pre-existing rot (document, do not fix here). Do NOT modify app code here (that is plans 02–04); this task only proves the rename is complete + records residual rot.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_rls_platform_universal.py tests/integration/test_163_rls_documents.py tests/integration/test_v3_4_org_isolation.py -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The named RLS suites pass against the renamed DB (no is_global-literal or folder_is_globally_visible breakage remains).
    - Any residual failure is triaged in the summary as rename-induced (with the fix location) or pre-existing rot (with a baseline reference).
    - A repo-wide `grep -rln "\\bis_global\\b" backend/app backend/tests supabase/migrations/111_is_global_retirement_rename.sql supabase/full-schema.sql --include=*.py --include=*.sql` returns 0 (except any `is_system`-adjacent false positive, documented) — the bare token is fully retired across backend + migration + schema.
  </acceptance_criteria>
  <done>The backend rename-regression suites pass against the renamed DB, is_global is fully retired repo-wide, and residual rot (if any) is documented, not silently absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disjoint-org agent (service-role) -> another org's shared folder | The leak test is the live proof this boundary holds after plan-02's org-scoping |
| xfail marker -> honest known-open tracking | Removing the marker converts tracked-known-open into an enforced passing invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-26 | Info-disclosure | SC#4 declared closed on code inspection, not live drive | mitigate | The arbiter is the live two-org XPASS of the SEED-124 test against the real service-role client; if the REST gate is down the leg SKIPS and SC#4 stays BLOCKING until an operator-confirmed live PASS. |
| T-165-27 | Tampering | the leak assertion weakened to force a pass | mitigate | Acceptance forbids changing the assertion body; only the xfail marker + KNOWN_OPEN name are removed. |
| T-165-28 | Info-disclosure | over-widening slips through (15 workflows / is_system escape) | mitigate | test_is_system_stays_universal + test_user_is_global_stays_org_scoped (renamed) must still pass in the 23-green run — they guard the universal/org-scoped split. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
- `pytest tests/integration/test_v3_4_org_isolation.py` = 23 passed, 0 failed, 0 xfail/xpass; the former-xfail SEED-124 test passes live as a normal test (or SKIPS with an explicit operator-confirmation requirement).
- The xfail marker + KNOWN_OPEN naming are removed; assertion body unchanged.
- Named backend RLS suites pass against the renamed DB; `is_global` fully retired repo-wide (backend + migration + full-schema).
- Residual rot triaged, not absorbed.
</verification>

<success_criteria>
SC#4 is arbitrated by the live two-org drive: the SEED-124 leak test passes as a normal test with its xfail marker removed, the two-org exit gate is green (23 passed) with the universal/org-scoped guards intact, and the backend rename-regression sweep confirms `is_global` is fully retired with no over-widening.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-08-SUMMARY.md` when done.
</output>
