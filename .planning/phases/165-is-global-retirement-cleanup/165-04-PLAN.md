---
phase: 165-is-global-retirement-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/agent_loop.py
  - backend/app/services/openai_service.py
  - backend/app/services/document_view_service.py
  - backend/app/services/document_relationship_service.py
  - backend/app/services/classification_matcher.py
  - backend/app/services/classification_rule_service.py
  - backend/app/services/metadata_field_service.py
  - backend/app/services/embedding_service.py
  - backend/app/services/workflow_authoring.py
  - backend/app/services/workflow_kickoff.py
  - backend/app/services/skill_tuner_service.py
  - backend/app/services/publish_gate_service.py
  - backend/app/services/harness/skill_snapshot.py
  - backend/app/db/workflows.py
  - backend/app/main.py
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01: is_global in the service/db/seed layer is renamed to is_org_shared (skills/folders domain) or is_system_global (workflow_definitions/document_views/classification_rules/metadata_field_definitions) per the owning table"
    - "D-165-03: main.py starter/harness/eval workflow seeding sets is_system_global=true (the write-locked universal flag) so the 15 seeded workflows stay cross-org by construction; the skill-creator seed keeps is_system"
    - "D-165-02: skills.is_system references in the seed/snapshot/load-skill path are NOT renamed"
    - "D-14: agent_loop.py + openai_service.py diffs are TOKEN-ONLY (no structural change to the shared Deep-mode path) — asserted by a changed-line-count == is_global-occurrence-count check"
  artifacts:
    - path: "backend/app/main.py"
      provides: "seed path setting is_system_global on platform workflows / is_system on skill-creator"
      contains: "is_system_global"
    - path: "backend/app/db/workflows.py"
      provides: "workflow_definitions queries using is_system_global"
      contains: "is_system_global"
  key_links:
    - from: "backend/app/main.py"
      to: "workflow_definitions.is_system_global"
      via: "starter-workflow seeding writes the write-locked universal flag"
      pattern: "is_system_global"
---

<objective>
Mechanically rename `is_global` -> its D-165-01 split target across the backend SERVICE and DB layers, and (as an isolated task) the app-seed path in `main.py`. The split target is determined by the table each token belongs to.

Purpose: MIG-02 / SC#1 / SC#3 — complete the backend rename so no runtime path reads the old column name after the migration applies. Preserve the seed path's `is_system_global=true` on the 15 platform workflows (D-165-03) and the skill-creator's `is_system` (D-165-02); keep the Deep-mode shared path byte-identical (D-14).

Output: The service/db/seed layer using the split target names; a token-only agent-loop diff; the 15-workflow universal seed preserved.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md

<rename_map>
Determine each token's target by the TABLE it references (read the surrounding query/field before renaming):
- skills/folders domain -> is_org_shared: skill_tuner_service.py, publish_gate_service.py, harness/skill_snapshot.py, embedding_service.py (skill embeddings), and the skills/folders references in agent_loop.py / openai_service.py.
- platform tables -> is_system_global: document_view_service.py, document_relationship_service.py (views), classification_matcher.py, classification_rule_service.py, metadata_field_service.py, workflow_authoring.py, workflow_kickoff.py, db/workflows.py (workflow_definitions).
- main.py: MIXED — starter/harness/eval workflow_definitions seeding -> is_system_global (write-locked universal, keep value true); the skill-creator seed -> is_system (UNCHANGED, D-165-02); any folders/skills seed -> is_org_shared.
- skills.is_system anywhere -> UNCHANGED.
</rename_map>

<dev_server_note>
W1 (dev-server / this project's convention): this plan commits code that reads the RENAMED columns BEFORE the Wave-2 migration apply (plan 165-10). A running backend (`uvicorn --reload`) or frontend (`vite`) dev server would 500 on folder/skill/view/workflow endpoints in the window between this commit and the apply. Operator must STOP any running backend/frontend dev server before Wave 1 begins and restart only AFTER plan 165-10 applies the migration + regenerates full-schema.
</dev_server_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the platform-table service + db files (views/rules/metadata/workflows)</name>
  <files>backend/app/services/document_view_service.py, backend/app/services/document_relationship_service.py, backend/app/services/classification_matcher.py, backend/app/services/classification_rule_service.py, backend/app/services/metadata_field_service.py, backend/app/services/workflow_authoring.py, backend/app/services/workflow_kickoff.py, backend/app/db/workflows.py</files>
  <read_first>
    - Each file's is_global occurrences (grep each first — confirm the owning table is one of the four write-locked platform tables)
    - backend/app/models/document_view.py, classification_rule.py, metadata_field.py (the renamed field names from plan 03 these services construct)
    - supabase/migrations/108_rls_membership_rewrite.sql lines 137-243, 484-500 (the is_global=false write-lock semantics these services honor)
  </read_first>
  <action>
    Apply is_global -> is_system_global across all eight files (all reference the four write-locked platform tables). Update Pydantic field access, response dict keys, .select("...") field strings, .eq("is_global", ...) filters, and any service-side hard-set of the flag to false (rename is_global=false -> is_system_global=false, keep it server-owned). Where a service constructs a seeded/platform row that must be cross-org universal (service-role write), set is_system_global=true — do NOT change it to an org-scoped flag.
  </action>
  <verify>
    <automated>cd backend && python -c "import app.services.document_view_service, app.services.document_relationship_service, app.services.classification_matcher, app.services.classification_rule_service, app.services.metadata_field_service, app.services.workflow_authoring, app.services.workflow_kickoff, app.db.workflows; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' across the 8 files returns 0 bare is_global: for f in backend/app/services/document_view_service.py backend/app/services/document_relationship_service.py backend/app/services/classification_matcher.py backend/app/services/classification_rule_service.py backend/app/services/metadata_field_service.py backend/app/services/workflow_authoring.py backend/app/services/workflow_kickoff.py backend/app/db/workflows.py; do grep -v '^\\s*#' "$f" | grep -c is_global; done all == 0.
    - is_system_global present in each file that previously referenced the flag.
    - Any preserved write-lock hard-set now reads is_system_global=false.
    - All 8 modules import clean.
  </acceptance_criteria>
  <done>The platform-table service/db files use is_system_global; write-locks and universal seeds preserved; imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the skills/folders-domain service files (D-14 token-only on the shared path)</name>
  <files>backend/app/services/skill_tuner_service.py, backend/app/services/publish_gate_service.py, backend/app/services/harness/skill_snapshot.py, backend/app/services/embedding_service.py, backend/app/services/agent_loop.py, backend/app/services/openai_service.py</files>
  <read_first>
    - Each file's is_global + is_system occurrences (grep each first)
    - backend/app/models/skill.py (renamed is_org_shared field + preserved is_system from plan 03)
    - CLAUDE.md D-14 (Deep Mode byte-identical; provider differences only at the gateway/adapter boundary)
  </read_first>
  <action>
    Apply the rename_map. skill_tuner_service.py / publish_gate_service.py / harness/skill_snapshot.py / embedding_service.py: is_global -> is_org_shared (skills domain); keep is_system verbatim. agent_loop.py / openai_service.py: rename each is_global by its owning table (skills/folders -> is_org_shared) — touch ONLY the column token; make NO other edit to these two files (D-14 byte-identical except the token).
  </action>
  <verify>
    <automated>cd backend && python -c "import app.services.skill_tuner_service, app.services.publish_gate_service, app.services.harness.skill_snapshot, app.services.embedding_service, app.services.agent_loop, app.services.openai_service; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' across the 6 files returns 0 bare is_global (is_system preserved where present).
    - D-14 token-only check (agent_loop.py + openai_service.py): for each file the changed-line count equals the pre-edit is_global-line count with NO net line insert/delete — run `git diff --numstat -- backend/app/services/agent_loop.py backend/app/services/openai_service.py` and confirm, per file, added == removed == (HEAD count of lines containing is_global). Record the three numbers per file in the summary. added != removed => a structural change => revert and redo as pure token substitution.
    - is_system usages unchanged.
    - Both modules import clean.
  </acceptance_criteria>
  <done>The skills/folders-domain services use is_org_shared; agent_loop.py + openai_service.py diffs are provably token-only (added==removed==is_global-line-count); is_system preserved; imports clean.</done>
</task>

<task type="auto">
  <name>Task 3: Rename main.py seed path (the highest-value mixed-target seed file)</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py — every is_global + is_system occurrence (grep first); identify each token's owning table: the starter/harness/eval workflow_definitions seeding vs the skill-creator is_system seed vs any folders/skills seed
    - backend/tests/unit/test_starter_workflows.py (what the seed writes — the 15 workflows that must stay is_system_global=true)
    - .planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md <specifics> (live-stakes: workflow_definitions is_global=15/69 — these 15 are the regression risk)
  </read_first>
  <action>
    main.py is the single seed file that mixes all three targets — isolate it here so the 15-workflow universal seed is not lost in a bulk rename.
    - starter/harness/eval workflow_definitions seeding: rename is_global -> is_system_global and KEEP the seeded value true (the 15 write-locked universal workflows — D-165-03). These must stay cross-org by construction.
    - skill-creator seed: KEEP is_system (D-165-02, unchanged) and rename any skills is_global -> is_org_shared.
    - any folders seed: is_global -> is_org_shared.
    Read each occurrence's context before renaming — do NOT blanket-rename (a workflow seed wrongly set is_org_shared would org-scope the 15 workflows = the mig-109 Test-7 regression this phase exists to prevent).
  </action>
  <verify>
    <automated>cd backend && python -c "import app.main; print('ok')" && python -m pytest tests/unit/test_starter_workflows.py --collect-only -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' backend/app/main.py returns 0 bare is_global.
    - The workflow-definitions seed writes is_system_global=true (grep the seed block); the skill-creator seed still writes is_system (unchanged).
    - No workflow seed sets is_org_shared (a mis-target); confirm by reading the seed block.
    - app.main imports clean; test_starter_workflows.py collects (its assertion-level green is plan 11 post-migration; if it asserts is_global literally it is in plan 08 scope — record the cross-reference).
  </acceptance_criteria>
  <done>main.py's seed path uses the correct split target per table, the 15 workflows stay is_system_global=true, is_system is preserved, and app.main imports clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-role seed -> is_system_global | Only the seed/service-role path writes the universal flag true; a wrong split target would org-scope the 15 platform workflows |
| shared Deep-mode agent path | Provider-uniform path must stay byte-identical except the mechanical token (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-14 | Info-disclosure | main.py workflow seed uses wrong split target (is_org_shared) | mitigate | Task 3 isolates main.py; the workflow seed writes is_system_global=true (write-locked universal); the exit-gate platform-universal guard (plan 11) proves the 15 workflows stay cross-org. |
| T-165-15 | Tampering | agent_loop.py shared path restructured during rename | mitigate | Task 2 D-14 check requires added==removed==is_global-line-count (token-only diff); any structural change fails acceptance. |
| T-165-16 | Tampering | skills.is_system misrenamed in seed/snapshot | mitigate | is_system explicitly excluded (D-165-02); grep confirms is_system count unchanged in the skills seed/snapshot path. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- Zero bare `is_global` (non-comment) across the 15 service/db/main files.
- Split targets applied per rename_map; `is_system` preserved; the 15 workflows seeded is_system_global=true.
- agent_loop.py + openai_service.py: token-only diff (added==removed==is_global-line-count).
- All modules import; the starter-workflow seed test collects (assertion-level green is plan 11 post-migration).
</verification>

<success_criteria>
The backend service/db/seed layer renames `is_global` to its correct split target per table, isolates the high-value `main.py` seed rename (15 workflows stay `is_system_global=true`), preserves `skills.is_system`, keeps `agent_loop.py`/`openai_service.py` provably token-only, and imports clean.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-04-SUMMARY.md` when done.
</output>
