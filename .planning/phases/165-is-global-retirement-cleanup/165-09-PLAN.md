---
phase: 165-is-global-retirement-cleanup
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01: every frontend TEST reference to the six renamed table fields is renamed per the owning field — folders/skills -> is_org_shared/isOrgShared; views/rules/metadata/workflows -> is_system_global/isSystemGlobal"
    - "UNRELATED isGlobal tokens (ChatHistoryColumn.test.tsx, threadGroups.test.tsx, and any other non-six-table use) are NOT renamed"
    - "D-165-07 (SC#2): tests asserting the old 'Global' toggle label are updated to 'Shared with org'"
    - "vitest shows no NEW rename-induced failures beyond the documented pre-existing rot baseline"
  artifacts:
    - path: "frontend/src/components/skills/SkillCard.test.tsx"
      provides: "skill test mirroring the renamed is_org_shared field + 'Shared with org' copy"
      contains: "is_org_shared"
  key_links:
    - from: "frontend/src/components/ingestion/__tests__/FolderNode.test.tsx"
      to: "frontend/src/components/ingestion/FolderNode.tsx"
      via: "test mock data mirrors the renamed component prop"
      pattern: "is_org_shared|isOrgShared"
---

<objective>
Update the frontend TEST files to mirror the renamed identifiers and toggle copy, while explicitly leaving UNRELATED `isGlobal` tokens untouched (per B2: `ChatHistoryColumn.test.tsx`, `threadGroups.test.tsx`, and any other non-six-table use). Then run vitest and confirm no rename-induced regressions beyond the known rot baseline.

Purpose: MIG-02 rename-regression coverage for the frontend test surface; preserve the tsc + vitest self-verification the component plan (08) started.

Output: Frontend tests referencing the renamed identifiers/copy; unrelated tokens preserved; vitest clean vs baseline.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md

<rename_map>
Rename ONLY tokens referring to the six renamed table fields (read each occurrence's mock/field context):
- folders/skills field -> is_org_shared ; isGlobal -> isOrgShared
- document_views/classification_rules/metadata_field_definitions/workflow_definitions field -> is_system_global ; isGlobal -> isSystemGlobal
- Skill.is_system / isSystem: UNCHANGED
- assertions on the old "Global" folder/skill toggle label -> "Shared with org"

UNRELATED — DO NOT RENAME (leave verbatim): the `isGlobal` in `ChatHistoryColumn.test.tsx`, `ChatHistoryColumn.a11y.test.tsx`, and `threadGroups.test.tsx` (a thread-grouping flag, not one of the six table fields). Grep each occurrence's context; if a token is not one of the six table fields, leave it.
</rename_map>

<file_domains>
Approximate per-file target (VERIFY per occurrence; the file set is `grep -rln "is_global\\|isGlobal" frontend/src --include=*.test.tsx --include=*.test.ts`):
- folders -> is_org_shared: FolderTree.test.tsx, FolderNode.test.tsx, useFolders.test.ts, buildFolderTree.test.ts
- skills -> is_org_shared: SkillCard.test.tsx, SkillFormDialog.test.tsx, SkillTunerPage.test.tsx, SkillStudioPage.test.tsx, tuner/CaseEditor.test.tsx
- views/rules/metadata -> is_system_global: ViewsGroup.test.tsx, RuleBuilderPanel.test.tsx, ClassificationRulesPage.test.tsx, ConditionPopover.test.tsx, FilterBar.test.tsx
- workflows -> is_system_global: RunModal.test.tsx, RunModal.a11y.test.tsx, PublishedCardDelete.test.tsx, WorkflowsPage.test.tsx, WorkflowBuilderPage.test.tsx
- MIXED (per-line): NavRow.test.tsx, AutomationGroup.test.tsx, IngestionPage.test.tsx, api.test.ts
- UNRELATED (leave): ChatHistoryColumn.test.tsx, ChatHistoryColumn.a11y.test.tsx, threadGroups.test.tsx
</file_domains>

<dev_server_note>
W1 (dev-server): vitest tests are pure-JS (mocked API, no live DB), so they run green in Wave 1 independent of the migration. Still: the operator should keep dev servers stopped through Wave 1 per plans 01-08. This plan self-verifies via vitest now (no apply dependency).
</dev_server_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the six-table fields across frontend test files (leave unrelated tokens)</name>
  <files>frontend/src/</files>
  <read_first>
    - Output of: grep -rln "is_global\\|isGlobal" frontend/src --include=*.test.tsx --include=*.test.ts (the ~25-file set)
    - The rename_map + file_domains above (per-file target + the explicit UNRELATED exclusions)
    - frontend/src/types/index.ts + the components from plan 08 (the renamed identifiers the tests must match)
    - project_frontend_vitest_rot (MEMORY): ~14-17 vitest tests are pre-existing ROT at baseline — the bar is NO NEW rename-induced failures, not all-green
  </read_first>
  <action>
    For every frontend test referencing one of the six table fields, rename per the rename_map by owning field (mock data, assertions, prop passing). Update any assertion of the old "Global" folder/skill toggle label to "Shared with org". EXPLICITLY LEAVE the unrelated isGlobal in ChatHistoryColumn.test.tsx / ChatHistoryColumn.a11y.test.tsx / threadGroups.test.tsx and any other non-six-table token (grep-confirm each). For MIXED test files (NavRow, AutomationGroup, IngestionPage, api.test.ts) read each occurrence's field/table and apply the correct target.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln "is_global\\|isGlobal" frontend/src --include=*.test.tsx --include=*.test.ts` returns ONLY the three UNRELATED files (ChatHistoryColumn.test.tsx, ChatHistoryColumn.a11y.test.tsx, threadGroups.test.tsx) — every six-table token is renamed, every unrelated token is preserved.
    - `npx vitest run` shows no NEW failures attributable to the rename vs the pre-existing ~14-17 rot baseline (record the failing-set delta in the summary); the renamed folder/skill/view/rule/workflow test suites pass.
    - Tests asserting the folder/skill toggle copy assert "Shared with org".
    - `npx tsc --noEmit` still exits 0 (component plan 08 + these tests together type-check).
  </acceptance_criteria>
  <done>Frontend tests reference the renamed identifiers and toggle copy, unrelated isGlobal tokens are preserved, and vitest + tsc show no rename-induced regressions beyond baseline rot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test mock field -> component prop | Mock data must mirror the renamed prop or the test asserts against a stale field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-33 | Tampering | an UNRELATED isGlobal (threadGroups/ChatHistoryColumn) wrongly renamed, breaking a passing test | mitigate | Explicit UNRELATED exclusion list + per-occurrence context read; the final grep asserts exactly those three files retain isGlobal. |
| T-165-34 | Tampering | a six-table token missed, leaving a stale field that false-passes a mock-based test | mitigate | The final grep + vitest run + tsc gate catch a missed/wrong rename. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- Final grep leaves isGlobal ONLY in the three unrelated files; all six-table test tokens renamed.
- `npx vitest run` + `npx tsc --noEmit` show no rename-induced regressions beyond the documented rot baseline.
- Folder/skill toggle-copy tests assert "Shared with org".
</verification>

<success_criteria>
Frontend tests mirror the renamed identifiers and toggle copy, unrelated `isGlobal` tokens are preserved verbatim, and vitest + tsc pass with no rename-induced regressions beyond baseline rot.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-09-SUMMARY.md` when done.
</output>
