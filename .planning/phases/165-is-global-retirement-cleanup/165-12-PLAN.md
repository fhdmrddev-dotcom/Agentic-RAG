---
phase: 165-is-global-retirement-cleanup
plan: 12
type: execute
wave: 4
gap_closure: true
depends_on: ["165-11"]
files_modified:
  - backend/tests/integration/test_kb.py
  - backend/tests/integration/test_folders.py
  - backend/tests/integration/test_documents.py
  - backend/tests/integration/test_116_tool_leak.py
  - backend/tests/integration/test_117_route_leak.py
  - backend/tests/integration/test_119_leak.py
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "The kb/folders/documents integration suites run GREEN against the migrated live DB — their supabase mocks budget for the new leading org_members query that Plan 165-02 added to folder_utils.py (MIG-02 'tests' pillar closed)"
    - "The test_116/117/119 leak suites run GREEN with both callers reseeded into the SAME org — the relationship-masking behavior under test is exercised WITHOUT relying on the cross-org shared-folder visibility that CR-01 correctly closed; no masking/leak assertion is weakened"
    - "Zero phase-165-introduced test regressions remain (the 30 failures enumerated in 165-VERIFICATION.md all pass or are explicitly, reasonably deferred)"
  artifacts:
    - path: "backend/tests/integration/test_kb.py"
      provides: "kb ls/tree/grep/glob tests green with org_members-aware mocks"
      contains: "org_members"
  key_links: []
---

<objective>
Close the MIG-02 "tests" gap from 165-VERIFICATION.md (status: gaps_found, 6/7). The Phase-165 PRODUCTION code is independently verified correct against the live DB; only pre-existing test harnesses are stale, in two distinct root-cause classes. This is test-maintenance ONLY — do NOT change any production source file.

Root cause A (25 tests — mock sequencing): Plan 165-02 added `_resolve_caller_org_ids` → a `supabase.table("org_members").select("org_id").eq("user_id", user_id)` query at the FRONT of `fetch_visible_folders` / `get_globally_visible_folder_ids` in `folder_utils.py`. The affected tests mock supabase with a strict ordered `mock_builder.execute.side_effect = [...]` list that never budgeted for this new leading call, so responses are consumed off-by-one (KeyError 'user_id' / StopIteration / wrong status).

Root cause B (5 tests — cross-org fixture precondition): `test_116/117/119` seed two users who each get a DIFFERENT auto-provisioned personal org, then rely on a shared `is_org_shared` folder being visible CROSS-ORG as a test-setup precondition. That is the exact leaky pre-165 behavior CR-01 (this phase's flagship SC#4 fix) correctly closes — so the precondition assertion now fails before the test reaches its actual intent (relationship-leak masking).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-is-global-retirement-cleanup/165-VERIFICATION.md
@.planning/phases/165-is-global-retirement-cleanup/165-02-SUMMARY.md
@backend/app/utils/folder_utils.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Budget the mock sequences for the new leading org_members call (Root cause A)</name>
  <files>backend/tests/integration/test_kb.py, backend/tests/integration/test_folders.py, backend/tests/integration/test_documents.py</files>
  <read_first>
    - backend/app/utils/folder_utils.py — see EXACTLY where `_resolve_caller_org_ids` runs (the `org_members` SELECT) relative to the `folders` SELECT in `fetch_visible_folders` / `get_globally_visible_folder_ids`, so the mock response for org_members is inserted at the correct position in each ordered `execute.side_effect`.
    - Each failing test's current `mock_builder.execute.side_effect = [...]` (and any `.table(...).select(...)` call-order assumptions).
  </read_first>
  <action>
    For every failing test in test_kb.py (ls/tree/grep/glob — 16), test_folders.py (create/move — 4), and test_documents.py (list/upload/full-markdown — 5): update the mocked supabase call sequence so it accounts for the extra leading `org_members` query. Prefer the ROBUST fix where feasible — refactor the mock to be table-name-keyed (dispatch the response by the `.table("<name>")` argument) rather than a positional `side_effect` list — so future insertions don't re-break call-order. If a positional `side_effect` is kept, insert a correctly-shaped `org_members` response (rows of `{"org_id": ...}` for the caller's org) at the exact position folder_utils issues it. Do NOT touch any production source. Do NOT weaken what each test asserts about the endpoint result — only fix the mock plumbing so the endpoint reaches its real assertions.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/integration/test_kb.py backend/tests/integration/test_folders.py backend/tests/integration/test_documents.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test_kb.py, test_folders.py, test_documents.py all pass (0 failed) against the migrated DB / with corrected mocks.
    - No production file modified (git diff --stat shows only test files).
  </acceptance_criteria>
  <done>The three suites are green; mocks budget for the org_members call; no production change.</done>
</task>

<task type="auto">
  <name>Task 2: Reseed the leak-test two-user fixtures into the SAME org (Root cause B)</name>
  <files>backend/tests/integration/test_116_tool_leak.py, backend/tests/integration/test_117_route_leak.py, backend/tests/integration/test_119_leak.py</files>
  <read_first>
    - backend/tests/integration/test_v3_4_org_isolation.py — the canonical explicit org-seeding pattern (two users placed in the SAME org via org_members) to mirror. This is the reference for how to make two callers co-org without relying on cross-org shared-folder visibility.
    - Each failing test's fixture (`_seed_global_folder` / two-user setup) and the precondition assertion that now fails ("B must be able to read A's global-folder subject").
  </read_first>
  <action>
    Update the two-user fixtures in test_116_tool_leak.py (1), test_117_route_leak.py (1), and test_119_leak.py (3) so BOTH callers are members of the SAME org (mirroring test_v3_4_org_isolation.py's org-seeding), so the shared/`is_org_shared` folder subject is legitimately visible to both WITHIN the org boundary. The test's ACTUAL intent (relationship/endpoint masking between two viewers) must still be exercised and its masking assertions must remain intact and meaningful. CRITICAL: do NOT delete or soften any masking/leak assertion, and do NOT re-introduce cross-org visibility to make the test pass — the whole point of CR-01 is that cross-org shared folders are NOT visible. If any test's intent genuinely cannot be expressed within a single org, STOP and document it as a reasoned defer in the SUMMARY rather than forcing it.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/integration/test_116_tool_leak.py backend/tests/integration/test_117_route_leak.py backend/tests/integration/test_119_leak.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The three leak suites pass (0 failed) with both callers co-org; masking assertions preserved (not weakened).
    - No production file modified; no cross-org visibility re-introduced.
  </acceptance_criteria>
  <done>The leak suites are green with co-org fixtures; masking behavior still proven; no production change.</done>
</task>

</tasks>

<verification>
- All 6 files' suites green against the migrated live DB.
- `git diff --stat` for this plan shows ONLY the 6 test files — zero production source changes.
- No masking/leak assertion weakened; no cross-org visibility re-introduced (Task 2 preserves CR-01's closure).
</verification>

<success_criteria>
The 30 phase-165-introduced test failures enumerated in 165-VERIFICATION.md are resolved (green) via test-harness-only fixes — mock sequences budget for the new org_members call, and the leak-test fixtures co-locate both callers in one org — with every masking assertion preserved and zero production-code change, closing the MIG-02 "tests" pillar.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-12-SUMMARY.md` when done.
</output>
