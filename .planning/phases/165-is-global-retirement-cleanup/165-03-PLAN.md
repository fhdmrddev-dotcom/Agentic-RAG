---
phase: 165-is-global-retirement-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/folder.py
  - backend/app/models/skill.py
  - backend/app/models/kb.py
  - backend/app/models/document_view.py
  - backend/app/models/classification_rule.py
  - backend/app/models/metadata_field.py
  - backend/app/api/folders.py
  - backend/app/api/skills.py
  - backend/app/api/documents.py
  - backend/app/api/document_views.py
  - backend/app/api/classification_rules.py
  - backend/app/api/metadata_fields.py
  - backend/app/api/workflows.py
  - backend/app/api/skill_tuner.py
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01 + D-165-06: the Pydantic model fields + API wire fields for folders/skills become is_org_shared; for workflow_definitions/document_views/classification_rules/metadata_field_definitions become is_system_global"
    - "D-165-03: the API create/update paths that server-own the flag keep hard-setting is_system_global=false (write-lock) for the four platform tables — no route becomes able to set is_system_global"
    - "D-165-02: skills.is_system model/wire field is NOT renamed"
    - "The wire contract the frontend mirrors (plan 06) matches these renamed field names exactly"
  artifacts:
    - path: "backend/app/models/skill.py"
      provides: "SkillResponse/SkillCreate with is_org_shared (is_system kept)"
      contains: "is_org_shared"
    - path: "backend/app/models/document_view.py"
      provides: "view model with is_system_global"
      contains: "is_system_global"
  key_links:
    - from: "backend/app/api/classification_rules.py"
      to: "backend/app/models/classification_rule.py"
      via: "server hard-sets is_system_global=false on create (write-lock preserved)"
      pattern: "is_system_global"
---

<objective>
Mechanically rename `is_global` -> its D-165-01 split target across the backend MODEL layer (Pydantic response/create/update models) and the API layer (route handlers, request/response shaping). These are the wire contract the frontend (plan 06) and the DB (plan 01) must agree on.

Purpose: MIG-02 / SC#1 / SC#3 — the API response/request fields follow the DB column names; the four platform tables keep their server-owned write-lock (`is_system_global=false` hard-set on create).

Output: Model + API layer using `is_org_shared` (folders/skills) and `is_system_global` (workflows/views/rules/metadata); `is_system` (skills) untouched.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md

<rename_map>
| owner table (by file) | old | NEW |
|---|---|---|
| folders (folder.py, api/folders.py, kb.py folder dicts) | is_global | is_org_shared |
| skills (skill.py, api/skills.py, api/skill_tuner.py) | is_global | is_org_shared |
| document_views (document_view.py, api/document_views.py) | is_global | is_system_global |
| classification_rules (classification_rule.py, api/classification_rules.py) | is_global | is_system_global |
| metadata_field_definitions (metadata_field.py, api/metadata_fields.py) | is_global | is_system_global |
| workflow_definitions (api/workflows.py) | is_global | is_system_global |
| documents (api/documents.py) | is_global refers to the FOLDER visibility branch | is_org_shared |
| skills.is_system | UNCHANGED | is_system |

api/documents.py: any `is_global` refers to folder-scoped visibility (folders) -> is_org_shared; confirm per-line by reading the surrounding query/field context before renaming.
</rename_map>

<dev_server_note>
W1 (dev-server / this project's convention): this plan commits code that reads the RENAMED columns BEFORE the Wave-2 migration apply (plan 165-10). A running backend (`uvicorn --reload`) or frontend (`vite`) dev server would 500 on folder/skill/view/workflow endpoints in the window between this commit and the apply. Operator must STOP any running backend/frontend dev server before Wave 1 begins and restart only AFTER plan 165-10 applies the migration + regenerates full-schema.
</dev_server_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the model layer (6 Pydantic model files)</name>
  <files>backend/app/models/folder.py, backend/app/models/skill.py, backend/app/models/kb.py, backend/app/models/document_view.py, backend/app/models/classification_rule.py, backend/app/models/metadata_field.py</files>
  <read_first>
    - backend/app/models/folder.py (is_global field ~lines 10, 23, 29 + the D-164-05 owner-null docstring)
    - backend/app/models/skill.py (is_global ~lines 11, 30 + is_system references — DO NOT rename is_system)
    - backend/app/models/kb.py (two is_global fields ~lines 16, 29 — folder context -> is_org_shared)
    - backend/app/models/document_view.py (is_global ~line 91 -> is_system_global)
    - backend/app/models/classification_rule.py (is_global ~line 50 + the "HARD-SET False" docstring -> is_system_global)
    - backend/app/models/metadata_field.py (is_global ~line 67 + the T-111-03-01 server-side docstring -> is_system_global)
  </read_first>
  <action>
    Apply the rename_map per file. folder.py + skill.py + kb.py: is_global -> is_org_shared (skill.py keeps is_system verbatim, including its docstrings referencing the is_system universal marker). document_view.py + classification_rule.py + metadata_field.py: is_global -> is_system_global. Update the field names, any Field(...) defaults (is_org_shared: bool = False / is_system_global: bool = False), and the surrounding docstrings that name the flag (e.g. classification_rule.py's "is_global is HARD-SET False by the service on create" -> "is_system_global is HARD-SET False ..."; skill.py/folder.py owner-null docstrings that mention "an is_global OR is_system" -> "an is_org_shared OR is_system"). Keep the D-165-02 note where relevant: is_system is the skills allow-list, distinct from the four tables' is_system_global.
  </action>
  <verify>
    <automated>cd backend && python -c "import app.models.folder, app.models.skill, app.models.kb, app.models.document_view, app.models.classification_rule, app.models.metadata_field; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' across the six model files returns 0 bare `is_global` (is_system in skill.py preserved): for f in backend/app/models/{folder,skill,kb,document_view,classification_rule,metadata_field}.py; do grep -v '^\\s*#' "$f" | grep -c "is_global"; done all == 0.
    - `is_org_shared` present in folder.py/skill.py/kb.py; `is_system_global` present in document_view.py/classification_rule.py/metadata_field.py.
    - `grep -c "is_system" backend/app/models/skill.py` unchanged from HEAD (is_system field + docstrings intact).
    - All six modules import without error.
  </acceptance_criteria>
  <done>The six model files use the split target names; is_system is preserved; all import clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the API layer (8 route files) + preserve the platform write-lock</name>
  <files>backend/app/api/folders.py, backend/app/api/skills.py, backend/app/api/documents.py, backend/app/api/document_views.py, backend/app/api/classification_rules.py, backend/app/api/metadata_fields.py, backend/app/api/workflows.py, backend/app/api/skill_tuner.py</files>
  <read_first>
    - Each of the 8 files' is_global occurrences (grep each first to see exact lines + context)
    - backend/app/api/classification_rules.py + document_views.py + metadata_fields.py (the create paths that server-hard-set the flag false — the write-lock that must survive the rename)
    - backend/app/models/* (the renamed field names from Task 1 that these routes construct/return)
  </read_first>
  <action>
    Apply the rename_map per file. folders.py + skills.py + skill_tuner.py: is_global -> is_org_shared (keep is_system). documents.py: rename the folder-visibility is_global -> is_org_shared (verify each token is folder-scoped before renaming). document_views.py + classification_rules.py + metadata_fields.py + workflows.py: is_global -> is_system_global.

    CRITICAL (D-165-03 write-lock): wherever a create/update handler hard-sets the flag server-side for a platform table (e.g. classification_rules/document_views/metadata_fields create hard-setting `is_global=false`), rename that literal to `is_system_global=false` — do NOT remove it and do NOT make the flag settable from the request body. The frontend create bodies never supply it; the server keeps owning it false. Update any response-dict keys, query .select() field strings, and `.eq("is_global", ...)` filters to the renamed column name for that table.
  </action>
  <verify>
    <automated>cd backend && python -c "import app.api.folders, app.api.skills, app.api.documents, app.api.document_views, app.api.classification_rules, app.api.metadata_fields, app.api.workflows, app.api.skill_tuner; print('api imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' across the 8 api files returns 0 bare `is_global`: for f in backend/app/api/{folders,skills,documents,document_views,classification_rules,metadata_fields,workflows,skill_tuner}.py; do grep -v '^\\s*#' "$f" | grep -c "is_global"; done all == 0.
    - The platform-table create handlers still hard-set the renamed flag false: `grep -rn "is_system_global=false\\|is_system_global = false\\|\"is_system_global\": False" backend/app/api/{classification_rules,document_views,metadata_fields}.py` shows the preserved write-lock (no route accepts it from the body).
    - `is_org_shared` present in folders.py/skills.py/skill_tuner.py; `is_system_global` present in the four platform-table route files.
    - All 8 modules import without error.
  </acceptance_criteria>
  <done>The API layer uses the split target names, the platform write-lock (is_system_global hard-set false on create) is preserved, and all modules import clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request body -> platform flag | Authenticated create/update bodies must NOT be able to set is_system_global; the server owns it |
| API response -> frontend contract | The renamed wire field names must match the frontend mirror (plan 06) and the DB columns (plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-11 | Elevation | platform-table create/update handlers | mitigate | Rename `is_global=false` server hard-set to `is_system_global=false`; keep it server-owned so no authenticated route can set the universal flag (defense-in-depth atop the DB WITH-CHECK). |
| T-165-12 | Tampering | is_system misrenamed to is_system_global on skills | mitigate | skills.is_system is explicitly excluded from the rename (D-165-02); grep asserts is_system count is unchanged in skill.py/skills.py. |
| T-165-13 | Info-disclosure | a wrong-target rename (is_org_shared vs is_system_global) drifts the flag semantics | mitigate | Per-file rename_map + per-line context read; import + (plan-08) wire tests catch drift; documents.py tokens verified folder-scoped before rename. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- Zero bare `is_global` (non-comment) across the 6 model + 8 api files.
- Split targets applied correctly per rename_map; `is_system` preserved in skills.
- Platform-table write-lock (`is_system_global=false` server hard-set) present on the four platform create paths.
- All 14 modules import without error (static; wire-level green run is plan 08 post-migration).
</verification>

<success_criteria>
The backend model + API layer renames `is_global` to `is_org_shared` (folders/skills) and `is_system_global` (workflows/views/rules/metadata) exactly, preserves `skills.is_system` and the platform write-lock, and imports clean.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-03-SUMMARY.md` when done.
</output>
