---
phase: 165-is-global-retirement-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/folder.py
  - backend/app/models/skill.py
  - backend/app/models/kb.py
  - backend/app/models/document_view.py
  - backend/app/models/classification_rule.py
  - backend/app/models/metadata_field.py
  - backend/app/api/folders.py
  - backend/app/api/skills.py
  - backend/app/api/documents.py
  - backend/app/api/document_views.py
  - backend/app/api/classification_rules.py
  - backend/app/api/metadata_fields.py
  - backend/app/api/workflows.py
  - backend/app/api/skill_tuner.py
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01 + D-165-06: each file's Pydantic + API-wire is_global is renamed to the target VERIFIED against the actual supabase.table()/SQL context at each occurrence (per the explicit per-file mapping) — folders/skills -> is_org_shared; document_views/classification_rules/metadata_field_definitions/workflow_definitions -> is_system_global"
    - "api/documents.py is MIXED: its folders occurrences (~L1348-1351, ~L1525-1528) -> is_org_shared; its classification_rules occurrences (~L1911-1932, the ingest auto-classification .or_/.order/.get) -> is_system_global — a per-line target, not a single-table blanket"
    - "D-165-03: the platform-table create/update paths that server-own the flag keep hard-setting is_system_global=false (write-lock); no route becomes able to set is_system_global"
    - "D-165-02: skills.is_system model/wire field is NOT renamed"
    - "Wrong-rename guard: each file's post-rename token(s) match the table(s) it actually queries (verified per the explicit mapping), not just a 0-bare-is_global grep"
  artifacts:
    - path: "backend/app/api/documents.py"
      provides: "ingest path with folders->is_org_shared + classification_rules->is_system_global (MIXED)"
      contains: "is_system_global"
    - path: "backend/app/models/skill.py"
      provides: "SkillResponse/SkillCreate with is_org_shared (is_system kept)"
      contains: "is_org_shared"
  key_links:
    - from: "backend/app/api/classification_rules.py"
      to: "backend/app/models/classification_rule.py"
      via: "server hard-sets is_system_global=false on create (write-lock preserved)"
      pattern: "is_system_global"
---

<objective>
Rename `is_global` -> its D-165-01 split target across the backend MODEL + API layers, deriving each target from the ACTUAL table each occurrence queries (verified against live source — not inventory). `api/documents.py` is MIXED (folders + classification_rules). These are the wire contract the frontend (plans 08/09) and the DB (plan 01) must agree on.

Purpose: MIG-02 / SC#1 / SC#3 — API response/request fields follow the DB columns; the four platform tables keep their server-owned write-lock (`is_system_global=false` hard-set on create).

Output: Model + API layer using the correct per-table split target; `skills.is_system` preserved; platform write-lock intact.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md

<explicit_per_file_mapping>
Target VERIFIED against live source (the `supabase.table("X")` / model / raw SQL at each occurrence). is_system (skills) is NEVER renamed.

MODELS (self-evident by model↔table):
| File | occurrences | target |
|---|---|---|
| models/folder.py | is_global field/default/docstrings | folders -> is_org_shared |
| models/skill.py | is_global (KEEP is_system) | skills -> is_org_shared |
| models/kb.py | two is_global (ls/tree folder repr) | folders -> is_org_shared |
| models/document_view.py | is_global | document_views -> is_system_global |
| models/classification_rule.py | is_global (+ "HARD-SET False" docstring) | classification_rules -> is_system_global |
| models/metadata_field.py | is_global (+ mfd_reachable docstring) | metadata_field_definitions -> is_system_global |

API (verified single-table EXCEPT documents.py):
| File | verified table(s) | target |
|---|---|---|
| api/folders.py | .table("folders") — L74 body key, L178/194/198 toggle | folders -> is_org_shared (body DOES supply it — folders are a genuine toggle) |
| api/skills.py | .table("skills") — L183/205/253/326/575/633/683/685 (+ L221/700-702 is_system/comment) | skills -> is_org_shared; KEEP is_system |
| api/document_views.py | document_views service hard-set — L8/104/116 | document_views -> is_system_global |
| api/classification_rules.py | classification_rules hard-set — L10/22/88/94 | classification_rules -> is_system_global |
| api/metadata_fields.py | metadata_field_definitions hard-set — L6/67 | metadata_field_definitions -> is_system_global |
| api/workflows.py | workflow_definitions — L8/57/186/189/310/531 | workflow_definitions -> is_system_global |
| api/skill_tuner.py | .table("skills") — L204/213/215/983 | skills -> is_org_shared |
| **api/documents.py — MIXED** | folders (.table("folders")) L1348-1351, L1525-1528 | **-> is_org_shared** |
| **api/documents.py — MIXED** | classification_rules (.table("classification_rules")) L1911 comment, L1919/1922 .or_, L1924 .order, L1932 r.get | **-> is_system_global** |
</explicit_per_file_mapping>

<dev_server_note>
W1 (dev-server / this project's convention): this plan commits code that reads the RENAMED columns BEFORE the Wave-2 migration apply (plan 165-10). A running backend (`uvicorn --reload`) or frontend (`vite`) dev server would 500 on folder/skill/view/workflow endpoints in the window between this commit and the apply. Operator must STOP any running backend/frontend dev server before Wave 1 begins and restart only AFTER plan 165-10 applies the migration + regenerates full-schema.
</dev_server_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the model layer (6 Pydantic model files)</name>
  <files>backend/app/models/folder.py, backend/app/models/skill.py, backend/app/models/kb.py, backend/app/models/document_view.py, backend/app/models/classification_rule.py, backend/app/models/metadata_field.py</files>
  <read_first>
    - Each model file's is_global + is_system lines (grep -n each first)
    - The explicit_per_file_mapping MODELS table above (authoritative target per file)
  </read_first>
  <action>
    Apply the MODELS mapping. folder.py + skill.py + kb.py: is_global -> is_org_shared (skill.py keeps is_system verbatim, including the is_system docstrings). document_view.py + classification_rule.py + metadata_field.py: is_global -> is_system_global. Update field names, Field(...) defaults (is_org_shared: bool = False / is_system_global: bool = False), and docstrings naming the flag. Keep the D-165-02 note where relevant: is_system is the skills allow-list, distinct from the four tables' is_system_global.
  </action>
  <verify>
    <automated>cd backend && python -c "import app.models.folder, app.models.skill, app.models.kb, app.models.document_view, app.models.classification_rule, app.models.metadata_field; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' across the six model files returns 0 bare `is_global`: for f in backend/app/models/folder.py backend/app/models/skill.py backend/app/models/kb.py backend/app/models/document_view.py backend/app/models/classification_rule.py backend/app/models/metadata_field.py; do grep -v '^\\s*#' "$f" | grep -c "is_global"; done all == 0.
    - Wrong-rename guard: is_org_shared present ONLY in folder.py/skill.py/kb.py; is_system_global present ONLY in document_view.py/classification_rule.py/metadata_field.py (cross-check: `grep -c is_system_global backend/app/models/{folder,skill,kb}.py` == 0; `grep -c is_org_shared backend/app/models/{document_view,classification_rule,metadata_field}.py` == 0).
    - `grep -c "is_system" backend/app/models/skill.py` unchanged from HEAD.
    - All six modules import.
  </acceptance_criteria>
  <done>The six model files use the split target names verified per model↔table; is_system preserved; imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the API layer (8 route files, documents.py MIXED) + preserve the platform write-lock</name>
  <files>backend/app/api/folders.py, backend/app/api/skills.py, backend/app/api/documents.py, backend/app/api/document_views.py, backend/app/api/classification_rules.py, backend/app/api/metadata_fields.py, backend/app/api/workflows.py, backend/app/api/skill_tuner.py</files>
  <read_first>
    - Each file's is_global occurrences WITH the nearest `supabase.table("…")` / SQL context (grep -n -B3 each first — confirm the owning table BEFORE renaming)
    - The explicit_per_file_mapping API table above — ESPECIALLY the documents.py MIXED rows (folders vs classification_rules)
    - backend/app/api/documents.py lines 1340-1360, 1520-1535, 1905-1935 (the two folders reads + the ingest classification_rules block)
    - backend/app/models/* (the renamed field names from Task 1 these routes construct/return)
  </read_first>
  <action>
    Apply the API mapping per file, deriving each occurrence's target from its nearest `supabase.table("…")`:
    - folders.py + skills.py + skill_tuner.py: is_global -> is_org_shared (keep is_system).
    - document_views.py + classification_rules.py + metadata_fields.py + workflows.py: is_global -> is_system_global.
    - **documents.py (MIXED)**: the two `.table("folders")` reads (~L1348-1351 and ~L1525-1528, the `.or_(user_id.eq…,is_global.eq.true)` folder-scope filters) -> is_org_shared. The classification_rules ingest block (~L1911 comment, ~L1919 `.table("classification_rules")`, ~L1922 `.or_(…is_global.eq.true)`, ~L1924 `.order("is_global")`, ~L1932 `r.get("is_global")`) -> is_system_global. Do NOT blanket-rename documents.py to one target.

    CRITICAL (D-165-03 write-lock): wherever a create/update handler hard-sets the flag server-side for a platform table (classification_rules/document_views/metadata_fields create hard-setting `is_global=false`), rename that literal to `is_system_global=false` — do NOT remove it and do NOT make the flag settable from the request body. folders.py's create DOES supply the flag from the body (folders are a genuine user toggle) -> rename that body key to is_org_shared, keep it body-supplied.
  </action>
  <verify>
    <automated>cd backend && python -c "import app.api.folders, app.api.skills, app.api.documents, app.api.document_views, app.api.classification_rules, app.api.metadata_fields, app.api.workflows, app.api.skill_tuner; print('api imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' across the 8 api files returns 0 bare `is_global`: for f in backend/app/api/folders.py backend/app/api/skills.py backend/app/api/documents.py backend/app/api/document_views.py backend/app/api/classification_rules.py backend/app/api/metadata_fields.py backend/app/api/workflows.py backend/app/api/skill_tuner.py; do grep -v '^\\s*#' "$f" | grep -c "is_global"; done all == 0.
    - Wrong-rename guard (per-table): folders.py/skills.py/skill_tuner.py contain is_org_shared and 0 is_system_global; document_views.py/classification_rules.py/metadata_fields.py/workflows.py contain is_system_global and 0 is_org_shared; documents.py contains BOTH is_org_shared (folder reads) AND is_system_global (classification_rules ingest) — `grep -c is_org_shared backend/app/api/documents.py` >= 1 AND `grep -c is_system_global backend/app/api/documents.py` >= 1.
    - The platform-table create handlers still hard-set the renamed flag false: `grep -rn "is_system_global=false\\|is_system_global = false\\|\"is_system_global\": False" backend/app/api/classification_rules.py backend/app/api/document_views.py backend/app/api/metadata_fields.py` shows the preserved write-lock.
    - All 8 modules import.
  </acceptance_criteria>
  <done>The API layer uses the per-table split target (documents.py MIXED verified with both targets present), the platform write-lock is preserved, and all modules import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request body -> platform flag | Authenticated create/update bodies must NOT set is_system_global; the server owns it |
| per-occurrence table context -> target name | Each is_global must be renamed to the column of the table THAT occurrence queries — a MIXED file mis-renamed silently drifts visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-11 | Elevation | platform-table create/update handlers | mitigate | Rename `is_global=false` server hard-set to `is_system_global=false`; keep it server-owned so no authenticated route can set the universal flag. |
| T-165-12 | Tampering | is_system misrenamed to is_system_global on skills | mitigate | skills.is_system excluded (D-165-02); grep asserts is_system count unchanged in skill.py/skills.py. |
| T-165-13 | Info-disclosure | wrong-table rename (documents.py folders vs classification_rules; is_org_shared vs is_system_global) drifts visibility yet passes a bare-is_global grep | mitigate | Per-occurrence `supabase.table()` verification + the wrong-rename guard (each file's post-rename tokens must match the table it queries; documents.py must carry BOTH targets); plan-11 RLS/model suite is the runtime arbiter. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- Zero bare `is_global` (non-comment) across the 6 model + 8 api files.
- Wrong-rename guard passes: single-table files carry ONLY their target; documents.py carries BOTH is_org_shared (folders) AND is_system_global (classification_rules).
- Platform-table write-lock (`is_system_global=false` server hard-set) present on the four platform create paths.
- All 14 modules import (static; wire-level green run is plan 11 post-migration).
</verification>

<success_criteria>
The backend model + API layer renames `is_global` to the target verified against each occurrence's actual table (documents.py MIXED), preserves `skills.is_system` and the platform write-lock, passes the wrong-rename guard, and imports clean.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-03-SUMMARY.md` when done.
</output>
