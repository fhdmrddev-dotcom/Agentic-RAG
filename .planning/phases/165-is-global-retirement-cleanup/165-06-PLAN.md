---
phase: 165-is-global-retirement-cleanup
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/test_folders.py
  - backend/tests/integration/test_kb.py
  - backend/tests/integration/test_118_accept.py
  - backend/tests/integration/test_118_dismiss_undo.py
  - backend/tests/integration/test_123_1_tuner_runs_timestamp.py
  - backend/tests/integration/test_132_skill_versions.py
  - backend/tests/integration/test_132_test_cases.py
  - backend/tests/integration/test_137_2_skill_creator_seed_content.py
  - backend/tests/integration/test_140_escape_hatch.py
  - backend/tests/integration/test_skill_tuner_routes.py
  - backend/tests/integration/test_skills.py
  - backend/tests/integration/test_skills_import_export.py
  - backend/tests/integration/test_skills_lint.py
  - backend/tests/test_139_migration_090.py
  - backend/tests/test_load_skill_collision.py
  - backend/tests/test_publish_gate.py
  - backend/tests/unit/test_142_load_skill_flag.py
  - backend/tests/unit/test_151_attach_handler.py
  - backend/tests/unit/test_skill_tuner_service.py
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01: every is_global literal in these skills/folders-domain tests is renamed to is_org_shared (the single target for this whole plan — no is_system_global appears here)"
    - "D-165-02: skills.is_system literals (skill-creator seed, load-skill collision tie-break, badge tests) are NOT renamed"
    - "Single-target plan: because every file here owns folders/skills is_global only, there is zero is_org_shared-vs-is_system_global ambiguity — the classification risk is eliminated by grouping"
  artifacts:
    - path: "backend/tests/test_publish_gate.py"
      provides: "skills publish-gate tests using is_org_shared (is_system preserved)"
      contains: "is_org_shared"
  key_links:
    - from: "backend/tests/integration/test_skills.py"
      to: "skills.is_org_shared (mig 111)"
      via: "raw-SQL/model literals match the renamed skills column"
      pattern: "is_org_shared"
---

<objective>
Rename `is_global` -> `is_org_shared` across the SKILLS/FOLDERS-domain backend tests. Every file here owns only folders/skills `is_global`, so the whole plan has ONE target (`is_org_shared`) — grouping by domain removes the cross-target misclassification risk. `skills.is_system` is preserved throughout.

Purpose: MIG-02 rename-regression coverage for the skills/folders test surface.

Output: The skills/folders test set referencing `is_org_shared` (is_system intact); `--collect-only` clean.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md

<explicit_per_file_mapping>
ALL files in this plan: is_global -> is_org_shared (folders or skills). NONE map to is_system_global. is_system (skills) is NEVER renamed.
- folders domain: test_folders.py, test_kb.py, test_118_accept.py, test_118_dismiss_undo.py
- skills domain: test_123_1_tuner_runs_timestamp.py, test_132_skill_versions.py, test_132_test_cases.py, test_137_2_skill_creator_seed_content.py (KEEP is_system), test_140_escape_hatch.py, test_skill_tuner_routes.py, test_skills.py, test_skills_import_export.py, test_skills_lint.py, test_139_migration_090.py, test_load_skill_collision.py (KEEP is_system), test_publish_gate.py, test_142_load_skill_flag.py (KEEP is_system), test_151_attach_handler.py, test_skill_tuner_service.py
GUARD: if any file unexpectedly contains a workflow/view/rule/metadata is_global literal (owning table is one of the four write-locked tables), STOP and flag it — it belongs in plan 07, not here (no is_system_global should be introduced by this plan).
</explicit_per_file_mapping>

<dev_server_note>
W1 (dev-server): these tests reference the RENAMED columns; assertion-level green is plan 11 (post-apply). Operator must STOP any running backend/frontend dev server before Wave 1 and restart only AFTER plan 165-10 applies the migration + regenerates full-schema. This plan's gate is grep + `--collect-only`.
</dev_server_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the skills-domain tests (integration + root + unit)</name>
  <files>backend/tests/integration/test_123_1_tuner_runs_timestamp.py, backend/tests/integration/test_132_skill_versions.py, backend/tests/integration/test_132_test_cases.py, backend/tests/integration/test_137_2_skill_creator_seed_content.py, backend/tests/integration/test_140_escape_hatch.py, backend/tests/integration/test_skill_tuner_routes.py, backend/tests/integration/test_skills.py, backend/tests/integration/test_skills_import_export.py, backend/tests/integration/test_skills_lint.py, backend/tests/test_139_migration_090.py, backend/tests/test_load_skill_collision.py, backend/tests/test_publish_gate.py, backend/tests/unit/test_142_load_skill_flag.py, backend/tests/unit/test_151_attach_handler.py, backend/tests/unit/test_skill_tuner_service.py</files>
  <read_first>
    - Each file's is_global + is_system lines (grep -n each first — confirm every is_global is a skills literal)
    - backend/app/models/skill.py (plan 03 — the renamed is_org_shared field + preserved is_system)
    - test_load_skill_collision.py + test_142_load_skill_flag.py + test_137_2_skill_creator_seed_content.py (the is_system tie-break/seed tests — is_system stays)
  </read_first>
  <action>
    Rename is_global -> is_org_shared across all 15 skills-domain files (raw SQL, model kwargs, dict-key assertions, .eq()/.select() filters). KEEP every is_system literal verbatim. Token rename only. If any file surprises with a non-skills is_global, STOP and flag per the guard.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_skills.py tests/test_publish_gate.py tests/unit/test_142_load_skill_flag.py --collect-only -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 15 files: `grep -c "is_global" <file>` == 0; is_org_shared present where the flag was referenced.
    - `grep -c "is_system_global" <file>` == 0 for every file (this plan introduces NO is_system_global — proves no cross-target error).
    - is_system counts unchanged vs HEAD in the three is_system tests.
    - `--collect-only` exits 0 on the sampled subset.
  </acceptance_criteria>
  <done>The 15 skills-domain tests use is_org_shared; no is_system_global introduced; is_system preserved; collection clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the folders-domain tests</name>
  <files>backend/tests/integration/test_folders.py, backend/tests/integration/test_kb.py, backend/tests/integration/test_118_accept.py, backend/tests/integration/test_118_dismiss_undo.py</files>
  <read_first>
    - Each file's is_global lines (grep -n each first — confirm folders context)
    - backend/app/models/folder.py + backend/app/api/kb.py (plan 02/03 — the renamed is_org_shared field + folder dict keys)
  </read_first>
  <action>
    Rename is_global -> is_org_shared across the 4 folders-domain files (raw SQL, folder dict keys, assertions). test_118_accept/dismiss reference the folder move target — rename their folders is_global only. Token rename only.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_folders.py tests/integration/test_kb.py --collect-only -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 files: `grep -c "is_global" <file>` == 0 and `grep -c "is_system_global" <file>` == 0; is_org_shared present.
    - `--collect-only` exits 0.
    - Final: `grep -rln "is_global" <this plan's 19 files>` returns none.
  </acceptance_criteria>
  <done>The folders-domain tests use is_org_shared; no is_system_global introduced; collection clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test skills/folders literal -> DB column | Must match the renamed is_org_shared column; a stray is_system_global would be a cross-target error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-29 | Tampering | a workflow/view literal hiding in a "skills" test wrongly renamed to is_org_shared | mitigate | Single-target plan + a `grep -c is_system_global == 0` acceptance check per file proves no cross-target rename; the guard routes any surprise platform literal to plan 07. |
| T-165-30 | Tampering | is_system renamed in the skill-creator/collision tie-break tests | mitigate | is_system excluded; grep asserts is_system counts unchanged in the three is_system tests. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
- All 19 files: 0 bare `is_global`, 0 `is_system_global` (single-target proof); `is_system` preserved.
- `--collect-only` clean; assertion-level green is plan 11 post-migration.
</verification>

<success_criteria>
Every skills/folders-domain test renames `is_global` to `is_org_shared` only, introduces no `is_system_global`, preserves `skills.is_system`, and collects clean.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-06-SUMMARY.md` when done.
</output>
