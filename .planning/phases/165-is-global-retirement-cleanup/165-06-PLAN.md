---
phase: 165-is-global-retirement-cleanup
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useFolders.ts
  - frontend/src/hooks/useSkills.ts
  - frontend/src/components/ingestion/
  - frontend/src/components/skills/
  - frontend/src/components/classification/RuleBuilderPanel.tsx
  - frontend/src/components/chat/tool-bodies/
  - frontend/src/pages/WorkflowsPage.tsx
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01 + D-165-06: frontend interface fields + props mirror the wire — folders/skills become is_org_shared (prop isOrgShared); document_views/classification_rules/metadata_field_definitions/workflow_definitions become is_system_global (display-only isSystemGlobal)"
    - "D-165-07 (SC#2): the folder + skill share toggles are relabeled 'Global' -> 'Shared with org'; the toggle stays functional (no silent un-share)"
    - "D-165-02: Skill.is_system frontend field is NOT renamed (the 'Built-in' pill)"
    - "SC#2: no previously-shared folder/skill/view silently un-shares — the rename is value-preserving on visibility"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "the client contract mirroring the renamed wire fields"
      contains: "is_org_shared"
    - path: "frontend/src/components/ingestion/FolderDetail.tsx"
      provides: "'Shared with org' relabel on the folder toggle"
      contains: "Shared with org"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "backend wire fields"
      via: "createFolder/createSkill bodies send is_org_shared; responses read is_org_shared/is_system_global"
      pattern: "is_org_shared"
---

<objective>
Mirror the DB/backend rename in the frontend: interface fields, hook/prop names, request/response field names, and the user-facing toggle copy. This is the SC#2 surface — "Shared with org" replaces "Global" and no shared resource silently un-shares.

Purpose: MIG-02 / SC#1 / SC#2 — the frontend contract follows the wire (`is_org_shared` for folders/skills, `is_system_global` for views/rules/metadata/workflows); the folder + skill toggles read "Shared with org".

Output: A type-clean frontend (tsc passes) with renamed fields/props and relabeled toggles; frontend tests updated and green (no rename-induced failures).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
frontend/src/types/index.ts current is_global fields (from Read):
- MetadataFieldDef.is_global (line 248)   -> is_system_global (metadata_field_definitions)
- SavedView.is_global (line 319)          -> is_system_global (document_views)
- ClassificationRule.is_global (line 426) -> is_system_global (classification_rules)
- Folder.is_global (line 454)             -> is_org_shared
- Skill.is_global (line 500)              -> is_org_shared;  Skill.is_system (line 501) UNCHANGED
- SkillCreate.is_global (line 518)        -> is_org_shared

frontend/src/lib/api.ts: createFolder(name, parentId, isGlobal=false) sends body { is_global: isGlobal } -> rename param isGlobal->isOrgShared, body key -> is_org_shared. Classification-rule create hard-sets nothing (server-owned) — leave the "never supplies is_global" comment updated to is_system_global.
</interfaces>

<rename_map>
Frontend interface field (snake_case, mirrors wire) + camelCase prop/param:
- folders/skills:  is_global -> is_org_shared ;  isGlobal -> isOrgShared
- document_views/classification_rules/metadata_field_definitions/workflow_definitions:  is_global -> is_system_global ;  isGlobal -> isSystemGlobal
- Skill.is_system / isSystem: UNCHANGED (D-165-02, the 'Built-in' pill)
- UI copy on folder + skill share toggles: "Global" -> "Shared with org" (D-165-07)
GUARD: rename ONLY tokens that refer to the six renamed tables' fields. If a test/component references an UNRELATED isGlobal (not one of these six fields), leave it — grep each occurrence's context first.
</rename_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the frontend contract layer (types + api + hooks)</name>
  <files>frontend/src/types/index.ts, frontend/src/lib/api.ts, frontend/src/hooks/useFolders.ts, frontend/src/hooks/useSkills.ts</files>
  <read_first>
    - frontend/src/types/index.ts (the 6 is_global fields at lines 248/319/426/454/500/518 + line 501 is_system)
    - frontend/src/lib/api.ts (is_global occurrences: createFolder ~1579-1584 + the classification-rule/workflow comments)
    - frontend/src/hooks/useFolders.ts, frontend/src/hooks/useSkills.ts (is_global/isGlobal in the folder/skill CRUD hooks)
  </read_first>
  <action>
    types/index.ts: rename per the interfaces block — Folder/Skill/SkillCreate.is_global -> is_org_shared (Skill.is_system stays); MetadataFieldDef/SavedView/ClassificationRule.is_global -> is_system_global. Update the doc comments naming the flag (e.g. the SavedView 'G' pill comment, the ClassificationRule 'server-owned' comment) to the new field name.
    api.ts: rename the createFolder param isGlobal -> isOrgShared and the request body key is_global -> is_org_shared; update any response field reads + the is_global-server-owned comments (classification rules/workflows -> is_system_global). Any skill-create body is_global -> is_org_shared.
    hooks: rename is_global/isGlobal in useFolders.ts (folders -> is_org_shared) and useSkills.ts (skills -> is_org_shared; keep is_system).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "is_global|isGlobal" | head; echo "tsc-token-check-done"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `is_global`/`isGlobal` in the four files returns 0 for the six-table fields (any unrelated token left is documented).
    - types/index.ts: Folder/Skill/SkillCreate carry is_org_shared; SavedView/ClassificationRule/MetadataFieldDef carry is_system_global; Skill.is_system unchanged.
    - api.ts createFolder sends is_org_shared in its body.
    - `npx tsc --noEmit` reports no errors mentioning is_global/isGlobal from these files (remaining errors, if any, come from components fixed in Task 2).
  </acceptance_criteria>
  <done>The contract layer mirrors the renamed wire; is_system preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rename components + relabel toggles "Global" -> "Shared with org" (D-165-07)</name>
  <files>frontend/src/components/ingestion/FolderCreateInput.tsx, frontend/src/components/ingestion/FolderDetail.tsx, frontend/src/components/ingestion/FolderNode.tsx, frontend/src/components/ingestion/FolderTree.tsx, frontend/src/components/ingestion/NavRow.tsx, frontend/src/components/ingestion/ViewsGroup.tsx, frontend/src/components/ingestion/AutomationGroup.tsx, frontend/src/components/skills/SkillCard.tsx, frontend/src/components/skills/SkillFormDialog.tsx, frontend/src/components/skills/tuner/CaseEditor.tsx, frontend/src/components/classification/RuleBuilderPanel.tsx, frontend/src/components/chat/tool-bodies/TreeBody.tsx, frontend/src/components/chat/tool-bodies/LsBody.tsx, frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - grep -rn "is_global\\|isGlobal\\|Global" across the listed component files (find both the field usage AND the user-facing "Global" label text on the folder/skill toggles)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (keep the relabel visually consistent with FolderNode/FolderDetail/SkillCard/ViewsGroup — cosmetic relabel of existing toggles, no new UI)
    - frontend/src/types/index.ts (the renamed field names from Task 1 these components consume)
  </read_first>
  <action>
    Rename field/prop usages per the rename_map: ingestion folder components + skills components + tuner/CaseEditor + tool-bodies (TreeBody/LsBody render folder is_org_shared) -> is_org_shared / isOrgShared (Skill.is_system stays); ViewsGroup (saved views) + RuleBuilderPanel (classification rules) + WorkflowsPage (workflow_definitions) -> is_system_global / isSystemGlobal.

    UI copy (D-165-07 / SC#2): on the FOLDER and SKILL share toggles/labels (FolderCreateInput, FolderDetail, FolderNode, SkillCard, SkillFormDialog wherever the toggle/badge text reads "Global"), change the user-facing label to "Shared with org". The toggle stays functional — do NOT special-case solo orgs, do NOT disable it. For the display-only is_system_global badges on ViewsGroup/WorkflowsPage (seeded 'G'/'Global' pills), relabel their tooltip/label to reflect platform/system-provided wording (e.g. "Built-in"/"System" per the sketch-findings convention) — display-only, not a user toggle.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the whole app type-checks against the renamed types — this is the load-bearing static proof the rename is consistent end-to-end in the frontend).
    - grep for the six-table is_global/isGlobal fields across the components returns 0.
    - The folder + skill toggle label text reads "Shared with org" (grep -rn "Shared with org" frontend/src/components shows the folder + skill toggle sites); no lingering user-facing "Global" toggle label on folders/skills.
    - Skill.is_system usages (the "Built-in" pill) unchanged.
  </acceptance_criteria>
  <done>Components consume the renamed fields, the folder/skill toggles read "Shared with org", and the whole frontend type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update frontend tests + run vitest green</name>
  <files>frontend/src/</files>
  <read_first>
    - grep -rln "is_global\\|isGlobal" frontend/src --include=*.test.tsx --include=*.test.ts (the test file set — ~25 files; note some, e.g. ChatHistoryColumn/threadGroups, may reference an UNRELATED isGlobal — check context)
    - project_frontend_vitest_rot (MEMORY): ~14-17 vitest tests are pre-existing ROT at baseline — the bar is NO NEW rename-induced failures, not all-green
    - frontend/src/types/index.ts + the components from Tasks 1-2 (the renamed identifiers the tests must match)
  </read_first>
  <action>
    Update every frontend test that references the six-table is_global/isGlobal fields (mock data, assertions, prop passing) to the renamed identifiers per the rename_map. Update any test asserting the old "Global" toggle label to "Shared with org". Leave unrelated isGlobal tokens (verify context first). Then run vitest and confirm no NEW failures versus the documented baseline rot.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep for the six-table is_global/isGlobal fields across frontend/src test files returns 0 (unrelated tokens documented).
    - `npx vitest run` shows no NEW failures attributable to the rename (compare failing-test set to the pre-existing ~14-17 rot baseline recorded in MEMORY / a pre-change run); the renamed folder/skill/view/rule/workflow test suites pass.
    - Tests asserting the folder/skill toggle copy assert "Shared with org".
  </acceptance_criteria>
  <done>Frontend tests reference the renamed identifiers and toggle copy; vitest shows no rename-induced regressions beyond baseline rot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client field name -> wire field name | The frontend interface must mirror the renamed wire exactly or reads silently return undefined |
| toggle label -> user mental model | "Shared with org" must remain a functional share toggle (no silent un-share) — SC#2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-19 | Info-disclosure (UX) | a folder/skill silently un-shares because the toggle stops writing the flag | mitigate | The rename is value-preserving (field renamed, wired the same); tsc + the toggle test assert the toggle still POSTs is_org_shared; no solo-org special-casing (D-165-07). |
| T-165-20 | Tampering | client is NOT a trust boundary — a display-only is_system_global badge implies a settable control | accept | is_system_global is display-only in the frontend (workflows/views seeded badge); the server owns write-lock (plan 03) — a spoofed client body cannot set it. |
| T-165-21 | Tampering | an unrelated isGlobal token wrongly renamed, breaking an unrelated feature | mitigate | Per-occurrence context read + the tsc/vitest gate catch a wrong rename. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (whole-app type consistency — the frontend static proof).
- `npx vitest run` shows no new rename-induced failures beyond the documented baseline rot.
- Zero six-table is_global/isGlobal tokens remain in frontend/src; folder + skill toggles read "Shared with org"; Skill.is_system preserved.
</verification>

<success_criteria>
The frontend mirrors the renamed wire (`is_org_shared`/`is_system_global`), relabels the folder + skill toggles to "Shared with org" while keeping them functional, preserves `Skill.is_system`, type-checks clean, and its tests pass with no rename-induced regressions.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-06-SUMMARY.md` when done.
</output>
