---
phase: 165-is-global-retirement-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/
  - backend/tests/unit/
  - backend/tests/
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01: every backend test's is_global reference (raw SQL literals, Pydantic model kwargs, dict assertions, .eq() filters) is renamed to its owning table's split target (is_org_shared or is_system_global)"
    - "D-165-06: test references to the folder_is_globally_visible DEFINER fn are renamed to folder_is_org_shared"
    - "D-165-02: is_system references in the skills/skill-creator tests are NOT renamed"
    - "The exit-gate file test_v3_4_org_isolation.py is EXCLUDED here (owned by plan 08)"
  artifacts:
    - path: "backend/tests/integration/test_163_rls_platform_universal.py"
      provides: "platform-universal RLS test using the renamed columns + folder_is_org_shared"
      contains: "is_system_global"
  key_links:
    - from: "backend/tests/integration/"
      to: "supabase renamed columns"
      via: "raw SQL INSERT/SELECT literals match the migration-111 column names"
      pattern: "is_org_shared|is_system_global"
---

<objective>
Mechanically update every backend test that references `is_global` (or the `folder_is_globally_visible` fn) to the post-rename identifiers, so the suite runs green against the renamed DB after the migration applies (plan 07). EXCLUDES `test_v3_4_org_isolation.py` (the exit gate — plan 08 owns its literal renames + xfail flip together).

Purpose: MIG-02 rename-regression coverage — the ~55 backend test files that seed/assert via raw SQL literals, model kwargs, and dict keys must reference the new column/function names or they break at run time after the migration.

Output: A backend test tree with zero bare `is_global` (except `is_system`) outside the exit-gate file; `--collect-only` clean.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md

<rename_map>
Determine each token's target by the TABLE the surrounding test seeds/asserts on (read the SQL/model context):
- folders / skills -> is_org_shared
- workflow_definitions / document_views / classification_rules / metadata_field_definitions -> is_system_global
- the DEFINER fn folder_is_globally_visible -> folder_is_org_shared (function name + any pg_proc proname assertion)
- skills.is_system -> UNCHANGED (many skill-creator / load-skill / badge-spoof tests assert is_system — leave those)
EXCLUDE backend/tests/integration/test_v3_4_org_isolation.py (plan 08).
Note: backend/tests/integration/test_163_factories.py has NO is_global reference (verified) — no edit needed.
</rename_map>

<discovery>
Enumerate the exact edit set first:
`grep -rln "is_global\\|folder_is_globally_visible" backend/tests` gives ~55 files (minus test_v3_4_org_isolation.py). Split them across the three tasks below by directory. For each file, `grep -n "is_global\\|folder_is_globally_visible" <file>` to see every occurrence + its table context, then apply the rename_map. This is a pure token rename — no test LOGIC changes.
</discovery>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename backend/tests/integration/*.py (except the exit gate) — part A</name>
  <files>backend/tests/integration/</files>
  <read_first>
    - Output of: grep -rln "is_global\\|folder_is_globally_visible" backend/tests/integration (the file set; exclude test_v3_4_org_isolation.py)
    - backend/tests/integration/test_163_rls_documents.py + test_163_rls_platform_universal.py (heavy folder_is_globally_visible + is_global + is_system usage — the reference patterns)
    - supabase/migrations/111_is_global_retirement_rename.sql (plan 01 — the authoritative new names these raw-SQL tests must match)
  </read_first>
  <action>
    Take the FIRST half (alphabetical) of the integration test files that reference is_global / folder_is_globally_visible (excluding test_v3_4_org_isolation.py). For each occurrence apply the rename_map by owning table: folders/skills -> is_org_shared; the four platform tables -> is_system_global; folder_is_globally_visible -> folder_is_org_shared. Leave is_system verbatim. Update raw SQL INSERT/SELECT column lists, .eq()/.select() filter strings, model kwargs, and dict-key assertions alike. Do NOT change any assertion values or test structure — token rename only.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration --collect-only -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Every file in this task's half has 0 bare `is_global` (non-comment) and 0 `folder_is_globally_visible`: verified per file via grep.
    - `is_system` counts unchanged in the skills/skill-creator tests touched.
    - `pytest tests/integration --collect-only` exits 0 (no import/collection errors introduced).
  </acceptance_criteria>
  <done>Part-A integration tests carry only the renamed identifiers; collection is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rename backend/tests/integration/*.py (except the exit gate) — part B</name>
  <files>backend/tests/integration/</files>
  <read_first>
    - Output of: grep -rln "is_global\\|folder_is_globally_visible" backend/tests/integration (the remaining half)
    - supabase/migrations/111_is_global_retirement_rename.sql (authoritative new names)
  </read_first>
  <action>
    Take the SECOND half (alphabetical) of the integration test files. Apply the same rename_map as Task 1. Token rename only.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration --collect-only -q 2>&1 | tail -15 && grep -rln "is_global\\|folder_is_globally_visible" backend/tests/integration | grep -v test_v3_4_org_isolation.py | grep -v test_163_factories.py</automated>
  </verify>
  <acceptance_criteria>
    - The final grep for `is_global`/`folder_is_globally_visible` under tests/integration returns ONLY test_v3_4_org_isolation.py (plan 08) — every other integration file is renamed.
    - `pytest tests/integration --collect-only` exits 0.
    - is_system preserved where present.
  </acceptance_criteria>
  <done>All integration tests except the exit gate carry the renamed identifiers; collection clean.</done>
</task>

<task type="auto">
  <name>Task 3: Rename backend/tests/unit/*.py + backend/tests/*.py + fixtures</name>
  <files>backend/tests/unit/, backend/tests/</files>
  <read_first>
    - Output of: grep -rln "is_global\\|folder_is_globally_visible" backend/tests/unit backend/tests --include=*.py (the unit + root + fixtures file set; exclude the tests/integration subtree handled in Tasks 1-2, and exclude test_v3_4_org_isolation.py)
    - backend/tests/unit/test_111_metadata_field_model.py, test_113_view_service_guards.py, test_118_rule_validation.py (platform-table model tests -> is_system_global)
    - backend/tests/unit/test_142_load_skill_flag.py, test_load_skill_collision.py, test_publish_gate.py (skills tests — is_org_shared vs preserved is_system)
    - backend/tests/fixtures/seed_llm_emit_fixture.py, seed_library_asset.py (fixture seeds)
  </read_first>
  <action>
    Apply the rename_map across every unit test, root-level test, and fixture file that references is_global / folder_is_globally_visible. Owning-table determination as before; is_system left verbatim. Token rename only — no logic/value changes. (Do NOT touch backend/tests/integration/*, already handled; do NOT touch test_v3_4_org_isolation.py.)
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit --collect-only -q 2>&1 | tail -10 && grep -rln "is_global" backend/tests --include=*.py | grep -v "tests/integration/test_v3_4_org_isolation.py"</automated>
  </verify>
  <acceptance_criteria>
    - The final repo-wide grep `grep -rln "is_global" backend/tests --include=*.py` returns ONLY `backend/tests/integration/test_v3_4_org_isolation.py` (everything else renamed; plan 08 finishes that file).
    - `grep -rln "folder_is_globally_visible" backend/tests --include=*.py` returns only test_v3_4_org_isolation.py.
    - `pytest tests/unit --collect-only` exits 0.
    - is_system preserved in the skills tests.
  </acceptance_criteria>
  <done>Unit/root/fixture tests carry the renamed identifiers; only the exit-gate file still references is_global (by design, closed in plan 08); collection clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test raw-SQL literal -> DB column | A test's INSERT/SELECT literal must match the migration-111 column name or the isolation suite silently mis-seeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-17 | Tampering | a mis-targeted rename (is_org_shared vs is_system_global) in an RLS regression test | mitigate | Per-table rename_map + read each occurrence's context; the plan-08 green run of the full RLS suite (test_163_rls_*.py + platform-universal guards) is the arbiter that the seeded/asserted columns match the migration. |
| T-165-18 | Tampering | is_system accidentally renamed in a badge-spoof/load-skill test, masking a real regression | mitigate | is_system excluded from the rename; grep asserts is_system counts unchanged in touched skills tests. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- Repo-wide `grep -rln "is_global\\|folder_is_globally_visible" backend/tests --include=*.py` returns ONLY `test_v3_4_org_isolation.py` (plan 08 finishes it).
- `pytest tests/integration --collect-only` and `pytest tests/unit --collect-only` exit 0.
- is_system preserved. Full green run happens in plan 08 post-migration.
</verification>

<success_criteria>
Every backend test except the exit gate references the post-rename identifiers (is_org_shared / is_system_global / folder_is_org_shared), is_system is preserved, and test collection is clean.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-05-SUMMARY.md` when done.
</output>
