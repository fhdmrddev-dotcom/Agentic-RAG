---
phase: 165-is-global-retirement-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/utils/folder_utils.py
  - backend/app/api/kb.py
  - backend/app/services/tool_dispatcher.py
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-04 (SC#4 / SEED-124 CR-01): the service-role folder-visibility helpers (is_in_global_subtree / fetch_visible_folders / get_globally_visible_folder_ids) org-scope a folder as org-shared-visible iff its org_id is in the caller's org set — the agent's ls/tree/read_document/fetch_document_file KB tools can no longer enumerate another org's is_org_shared folders"
    - "D-165-04 fail-closed: a caller with no resolvable org set sees 0 shared folders (over-restrict, never over-share)"
    - "D-165-05 (WR-01): owner user_id is nulled on ANY non-owned visible folder — the shared row AND any non-shared descendant visible via a shared ancestor"
    - "D-165-01: folders.is_global references in these three files are renamed to is_org_shared (skills.is_system unchanged)"
    - "The public helper signatures (get_globally_visible_folder_ids(supabase, user_id) etc.) are UNCHANGED so the exit-gate test drives them as-is — org resolution happens INSIDE the helper from user_id"
  artifacts:
    - path: "backend/app/utils/folder_utils.py"
      provides: "org-aware, fail-closed folder-visibility helpers + broadened owner-nulling"
      contains: "org_members"
    - path: "backend/app/api/kb.py"
      provides: "is_org_shared-renamed folder dict keys on the ls/tree/read browse path"
      contains: "is_org_shared"
  key_links:
    - from: "backend/app/utils/folder_utils.py"
      to: "org_members"
      via: "SELECT org_id FROM org_members WHERE user_id = caller (caller-org resolution)"
      pattern: "org_members"
    - from: "backend/app/services/tool_dispatcher.py"
      to: "backend/app/utils/folder_utils.py"
      via: "ls/tree/grep/read handlers pass current_user id -> helper resolves caller org"
      pattern: "current_user"
---

<objective>
Close the last folded cross-org leak (SC#4 / [[SEED-124]] — CR-01 + WR-01) by org-scoping the service-role folder-visibility helpers, and rename `folders.is_global` -> `is_org_shared` within the three service-role browse-path files.

Purpose: The agent's KB browse/read tools (`ls`/`tree`/`read_document`/`fetch_document_file`) run on the service-role BYPASSRLS client, so RLS/DEFINER org-gates never reach them — the org scope MUST live in `folder_utils.py`. This plan makes them fail-closed org-aware and broadens owner-nulling to subtree descendants, then flips the `is_global` token to `is_org_shared` in the same files.

Output: Org-scoped, fail-closed helpers that make the exit-gate leak test (`test_browse_tools_cross_org_leak_KNOWN_OPEN_seed124`, flipped in plan 08) XPASS.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md
@.planning/seeds/SEED-124-browse-tool-service-role-cross-org-folder-leak.md

<interfaces>
Current helper contracts (backend/app/utils/folder_utils.py — signatures MUST stay stable for the exit-gate test):
- async fetch_all_folders(supabase, fields="id, user_id, name, parent_id, is_global") -> list[dict]
- def is_in_global_subtree(folder_id, folder_map, cache=None) -> bool   (recurses on f["is_global"])
- def _null_foreign_global_owner(rows, caller_id) -> list[dict]   (nulls user_id where (row.get("is_global") or row.get("is_system")) and str(row["user_id"]) != cid)
- async fetch_visible_folders(supabase, user_id) -> list[dict]
- async get_globally_visible_folder_ids(supabase, user_id) -> list[str]

The membership resolver used everywhere else: public.current_user_org_ids() (mig 104) wraps SELECT org_id FROM org_members WHERE user_id = auth.uid(). On the service-role client there is no auth.uid(); resolve the caller's org set directly with a service-role SELECT org_id FROM org_members WHERE user_id = <caller> query (via aexec / supabase.table("org_members")), threaded from the tool's user_id.

tool_dispatcher.py browse handlers (already pass ctx.current_user["id"] + ctx.supabase = get_supabase() service-role): _handle_ls->ls_path, _handle_tree->tree_path, _handle_grep->grep_path, _handle_read_document->read_path, plus _fetch_owned_document_bytes (fetch_document_file).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Org-scope the folder-visibility helpers (D-165-04, CR-01) + fail-closed caller-org resolution</name>
  <files>backend/app/utils/folder_utils.py, backend/tests/unit/test_165_folder_utils_org_scope.py</files>
  <behavior>
    - Given user B (org Y) and an is_org_shared folder owned by user A (org X, disjoint): get_globally_visible_folder_ids(service_role_sb, B_uid) does NOT include A's folder id (CR-01 closed).
    - Given user A: A's OWN is_org_shared folder + its descendants remain visible to A (positive control preserved).
    - Given a caller with NO org membership rows: helpers return an empty shared set (fail-closed — 0 shared folders).
    - is_in_global_subtree treats a folder as shared-visible only when the ancestor with is_org_shared=true has org_id in caller_org_ids (folders have no is_system/platform-universal branch — that is skills-only per SEED-124).
  </behavior>
  <read_first>
    - backend/app/utils/folder_utils.py (the whole file — all five helpers)
    - backend/app/api/kb.py lines 1-140 (how the helpers are consumed on the ls/tree/read path)
    - backend/tests/integration/test_v3_4_org_isolation.py lines 866-903 (the leak test that drives get_globally_visible_folder_ids(sb, b["uid"]) — the acceptance arbiter; signature MUST stay (supabase, user_id))
    - supabase/migrations/104_org_dept_role_schema.sql (org_members table + current_user_org_ids definition — the caller-org query shape to mirror)
  </read_first>
  <action>
    Make the service-role helpers org-aware WITHOUT changing their public signatures (the exit-gate test calls get_globally_visible_folder_ids(sb, b["uid"]) positionally).

    1. fetch_all_folders: change the default fields string to "id, user_id, name, parent_id, is_org_shared, org_id" (adds org_id, renames is_global). Callers passing fields="*" already receive org_id.

    2. Add a private async _resolve_caller_org_ids(supabase, user_id) -> set[str]: run a service-role query aexec(supabase.table("org_members").select("org_id").eq("user_id", user_id)) and return the set of org_id strings. Empty result -> empty set (fail-closed).

    3. is_in_global_subtree(folder_id, folder_map, cache, caller_org_ids): add a caller_org_ids: set[str] param. A folder counts as an org-shared ancestor ONLY when f["is_org_shared"] is true AND str(f["org_id"]) in caller_org_ids. Keep the recursive ancestor-walk + cache; update the in-file recursive call to pass caller_org_ids.

    4. fetch_visible_folders(supabase, user_id): resolve caller_org_ids = await _resolve_caller_org_ids(...) once, build folder_map, return folders where f["user_id"] == user_id OR is_in_global_subtree(f["id"], folder_map, cache, caller_org_ids).

    5. get_globally_visible_folder_ids(supabase, user_id): resolve caller_org_ids once, select fields "id, user_id, parent_id, is_org_shared, org_id", return ids where f["user_id"] != user_id AND is_in_global_subtree(f["id"], folder_map, cache, caller_org_ids).

    Fail-closed is load-bearing: when caller_org_ids is empty, is_in_global_subtree can never return True for a non-owned folder -> 0 shared folders. Mirror 164's fail-closed org derivation.

    Create backend/tests/unit/test_165_folder_utils_org_scope.py (the Nyquist scaffold) with a lightweight fake supabase stub (in-memory folders list + org_members list; .table().select().eq().execute() returning the right rows via the existing aexec seam) exercising the four behaviors above — no live DB.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_165_folder_utils_org_scope.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The new unit test file exercises all four behaviors with a mocked/stub supabase (no live DB) and all pass: (a) disjoint-org caller excludes the shared folder; (b) owner still sees own shared subtree; (c) empty org set -> 0 shared; (d) is_in_global_subtree honors org_id membership on the shared ancestor.
    - get_globally_visible_folder_ids and fetch_visible_folders keep their (supabase, user_id) positional signatures (no added required positional param on the public functions).
    - grep -n "org_members" backend/app/utils/folder_utils.py shows the caller-org resolution query.
    - grep -v '^#' backend/app/utils/folder_utils.py | grep -c "is_global" == 0 (bare token retired; is_system untouched).
  </acceptance_criteria>
  <done>The helpers resolve the caller's org set from user_id, treat a folder as shared-visible only within the caller's orgs, fail closed on an empty org set, and the new unit test proves the disjoint-org leak is closed at the helper level.</done>
</task>

<task type="auto">
  <name>Task 2: Broaden owner-nulling to subtree descendants (D-165-05 / WR-01) + rename in kb.py & tool_dispatcher.py</name>
  <files>backend/app/utils/folder_utils.py, backend/app/api/kb.py, backend/app/services/tool_dispatcher.py</files>
  <read_first>
    - backend/app/utils/folder_utils.py (the _null_foreign_global_owner helper as updated in Task 1)
    - backend/app/api/kb.py (all is_global occurrences: the ls/tree node dicts ~lines 63,79,213,222 + the _null_foreign_global_owner call site ~line 118 + get_globally_visible_folder_ids uses)
    - backend/app/services/tool_dispatcher.py (is_global occurrences on the browse/read tool dispatch)
    - backend/tests/test_seed091_owner_nulling.py (the existing owner-nulling precedent D-164-05 this broadens)
  </read_first>
  <action>
    WR-01 (D-165-05): in the folders list/serialize path, null the seeder's user_id on ANY folder the caller SEES but does NOT own — including a non-is_org_shared DESCENDANT visible via a shared ancestor (the current _null_foreign_global_owner only nulls rows that are themselves is_global/is_system and misses subtree descendants).

    Implement in the kb.py folders-serialize path (the site currently calling _null_foreign_global_owner(all_folders, user_id)): compute the non-owned-visible id set from get_globally_visible_folder_ids (already available on that path), and null user_id for every returned folder whose user_id != caller AND (id in that non-owned-visible set OR the existing is_org_shared/is_system rule matches). Keep _null_foreign_global_owner for the skills/views callers (no subtree); rename its predicate row.get("is_global") -> row.get("is_org_shared") (is_system branch stays), and add an optional visible-non-owned id-set parameter the folders caller passes so the subtree-descendant broadening lives in the shared helper rather than duplicated.

    Then rename every remaining is_global folder-dict key in kb.py (the ls/tree node dicts) -> is_org_shared, and in tool_dispatcher.py rename its is_global occurrences -> is_org_shared. All three files are folders/skills domain -> is_org_shared; skills.is_system is never renamed.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_seed091_owner_nulling.py tests/unit/test_165_folder_utils_org_scope.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - A non-owner reader of a shared folder subtree sees user_id == null on both the shared row AND its non-shared descendants (add/extend a unit assertion for the descendant case in the Task-1 test file).
    - grep -v '^#' on all three files shows 0 bare is_global tokens: for f in backend/app/utils/folder_utils.py backend/app/api/kb.py backend/app/services/tool_dispatcher.py; do grep -v '^#' "$f" | grep -c is_global; done all == 0.
    - is_org_shared now appears in kb.py's ls/tree node dicts (grep confirms).
    - The existing owner-nulling test (test_seed091_owner_nulling.py) still passes after the predicate rename (or is updated in-place if it references the old is_global key — record which).
    - python -c "import app.api.kb, app.services.tool_dispatcher, app.utils.folder_utils" imports clean (no NameError from the rename).
  </acceptance_criteria>
  <done>Owner-nulling covers shared rows AND subtree descendants; all three files carry only is_org_shared (no bare is_global); imports and the owner-nulling tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent KB tool (service-role) -> folder helper | ls/tree/read/fetch run on BYPASSRLS get_supabase(); the helper is the ONLY cross-org gate on this path |
| caller user_id -> caller org set | org membership is derived from org_members off user_id, never a caller-supplied org hint |
| serialize projection -> owner identity | RLS gates rows not columns; owner user_id disclosure is prevented at serialize time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-07 | Info-disclosure | get_globally_visible_folder_ids / fetch_visible_folders on the service-role browse path | mitigate | Org-scope the shared-visibility predicate to caller_org_ids resolved from org_members; the disjoint-org leak (CR-01) is closed and proven by the exit-gate XPASS (plan 08). |
| T-165-08 | Elevation | caller with unresolved/empty org set | mitigate | Fail-closed: empty caller_org_ids -> is_in_global_subtree returns False for non-owned folders -> 0 shared folders visible (over-restrict, never over-share). |
| T-165-09 | Info-disclosure | seeder owner user_id on non-owned visible folders + subtree descendants (WR-01) | mitigate | Serialize-time null of user_id for every non-owned visible folder including non-shared descendants of a shared ancestor. |
| T-165-10 | Tampering | rename introduces a KeyError/NameError that fails-open the visibility filter | mitigate | Unit tests + import check gate the rename; helper defaults over-restrict; the browse path never widens on error. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
- New unit test `tests/unit/test_165_folder_utils_org_scope.py` proves CR-01 (disjoint-org exclusion) + fail-closed + WR-01 descendant nulling at the helper level (mocked supabase — runs green in Wave 1, pre-migration).
- `test_seed091_owner_nulling.py` remains green.
- Zero bare `is_global` tokens (non-comment) across the three files; `is_org_shared` present on the browse-path dicts.
- Public helper signatures unchanged (the exit-gate leak test drives them positionally in plan 08; the live two-org XPASS is the SC#4 arbiter there).
</verification>

<success_criteria>
The service-role folder-visibility helpers are org-aware and fail-closed, owner-nulling covers shared rows and their non-shared subtree descendants, and all `is_global` references in `folder_utils.py`/`kb.py`/`tool_dispatcher.py` are renamed to `is_org_shared` — with a mockable unit test proving the CR-01/WR-01 closure ahead of the live exit-gate arbitration.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-02-SUMMARY.md` when done.
</output>
