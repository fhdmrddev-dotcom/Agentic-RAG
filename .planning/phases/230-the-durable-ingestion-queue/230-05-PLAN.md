---
phase: 230-the-durable-ingestion-queue
plan: 05
type: execute
wave: 4
depends_on:
  - 230-04
files_modified:
  - frontend/src/components/documents/DocumentList.tsx
  - frontend/src/components/documents/IngestionPauseBanner.tsx
  - tests/eval/test_retrieval_recall_baseline.py
  - scripts/measure-recall.py
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements:
  - QUEUE-05
user_setup: []

must_haves:
  truths:
    - "IngestionPauseBanner renders in DocumentList when ingestion is paused or rate-limited, displaying the exact provider name and reason (SC#3, closing BUG-260815-05)"
    - "Upload experience remains unchanged in standard conditions (SC#5)"
    - "tests/eval/test_retrieval_recall_baseline.py and scripts/measure-recall.py measure Hit@K and MRR over the 77 baseline documents, establishing the ground truth Phase 241 measures against"
    - "G-5 hot files main.py and config.py are updated in both CLAUDE.md and docs/HOT-FILE-LEDGER.md with disposition cells under 200 characters"
    - "vitest count gate and check-claude-md-size.cjs pass"
  artifacts:
    - frontend/src/components/documents/IngestionPauseBanner.tsx
    - tests/eval/test_retrieval_recall_baseline.py
    - scripts/measure-recall.py
    - CLAUDE.md
    - docs/HOT-FILE-LEDGER.md
  key_links:
    - "Closes BUG-260815-05: Outage is surfaced with named provider refusal, never 'your documents returned nothing'"
    - "Carries the recall harness Phase 241 measures against without postponing until larger corpus"
---

<objective>
Implement the user-visible paused state and named provider refusal banner, author the Phase 241 recall baseline harness, and discharge G-5 hot-file documentation (`QUEUE-05`, `BUG-260815-05`, SC#3, SC#5, D-10..D-11):
1. Build `IngestionPauseBanner.tsx` and integrate it into `DocumentList.tsx` aligned with the approved G-2 sketch (BUS-110).
2. Author `tests/eval/test_retrieval_recall_baseline.py` and `scripts/measure-recall.py` computing Hit@K and MRR across the 77 baseline documents.
3. Update `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` for `main.py` and `config.py` in the same commit.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/230-the-durable-ingestion-queue/230-CONTEXT.md
@.planning/reported-bugs/BUG-260815-05-provider-outage-is-reported-as-zero-sources.md
@frontend/src/components/documents/DocumentList.tsx
@CLAUDE.md
@docs/HOT-FILE-LEDGER.md
</execution_context>

<threat_model>
<trust_boundaries>
Backend Refusal Metadata -> API Response -> Frontend IngestionPauseBanner
</threat_model>
<threats>
- threat_id: TM-230-10
  category: Information Dishonesty / Masquerading
  component: IngestionPauseBanner
  disposition: mitigated
  mitigation_plan: Explicitly identify the failing provider name (e.g. OpenAI) and status code (429) rather than generic error copy, preventing misdirection to operator.
- threat_id: TM-230-11
  category: XSS / Untrusted Content Injection
  component: IngestionPauseBanner error rendering
  disposition: mitigated
  mitigation_plan: Sanitize and escape provider refusal strings before rendering in React JSX.
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build IngestionPauseBanner and Integrate into DocumentList</name>
  <files>
    frontend/src/components/documents/IngestionPauseBanner.tsx
    frontend/src/components/documents/DocumentList.tsx
  </files>
  <read_first>
    frontend/src/components/documents/DocumentList.tsx
  </read_first>
  <action>
    1. Create `frontend/src/components/documents/IngestionPauseBanner.tsx`:
       - Renders an alert banner in Aether Deep Midnight styling (amber warning border/accent, transparent background).
       - Displays provider mark/name (e.g. "OpenAI"), error summary ("Rate limit reached (429)"), and automatic retry countdown / resume status.
       - Never displays misleading copy like "no documents found".
    2. In `frontend/src/components/documents/DocumentList.tsx`:
       - Inspect documents / queue status: If any document is in status `paused` or carries provider refusal details, render `IngestionPauseBanner` above the document table.
       - Standard upload flows and document rows remain identical (SC#5).
  </action>
  <verify>
    Run frontend build and typecheck:
    `npm --prefix frontend run build`
  </verify>
</task>

<task type="auto">
  <name>Task 2: Author Phase 241 Recall Baseline Evaluation Harness</name>
  <files>
    tests/eval/test_retrieval_recall_baseline.py
    scripts/measure-recall.py
  </files>
  <read_first>
    backend/app/services/retrieval_service.py
  </read_first>
  <action>
    1. Create `scripts/measure-recall.py`:
       - Connects to database and tests vector retrieval against the 77 baseline documents.
       - Runs a standardized probe query set (e.g. 10 grounded questions with known source document targets).
       - Computes and prints Hit@1, Hit@3, Hit@5, and Mean Reciprocal Rank (MRR).
    2. Create `tests/eval/test_retrieval_recall_baseline.py`:
       - Automated test verifying that `measure_retrieval_recall()` produces Hit@5 >= 0.80 and MRR >= 0.70 on the baseline corpus.
  </action>
  <verify>
    Run the recall evaluation test:
    `backend/venv/Scripts/pytest tests/eval/test_retrieval_recall_baseline.py -v`
  </verify>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md and Hot-File Ledger for main.py and config.py</name>
  <files>
    CLAUDE.md
    docs/HOT-FILE-LEDGER.md
  </files>
  <read_first>
    CLAUDE.md
    docs/HOT-FILE-LEDGER.md
  </read_first>
  <action>
    1. Update G-5 entries in `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` in the same commit:
       - `main.py`: Update commit count, phase count, line count; ensure disposition cell <= 200 chars.
       - `config.py`: Update commit count, phase count, line count; note `MODEL_CAPABILITIES` seam stays OWED; ensure disposition cell <= 200 chars.
    2. Verify character size limit using `node scripts/check-claude-md-size.cjs`.
  </action>
  <verify>
    Run CLAUDE.md size check:
    `node scripts/check-claude-md-size.cjs`
  </verify>
</task>

</tasks>

<acceptance_criteria>
- `IngestionPauseBanner` surfaces honest provider refusals on rate limit/outage (SC#3).
- `tests/eval/test_retrieval_recall_baseline.py` establishes the baseline retrieval benchmark for Phase 241.
- `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` updated in the same commit with valid line counts and disposition cells under 200 chars.
- All gates pass: `check-claude-md-size.cjs`, `check-deploy-drift.sh`.
</acceptance_criteria>
