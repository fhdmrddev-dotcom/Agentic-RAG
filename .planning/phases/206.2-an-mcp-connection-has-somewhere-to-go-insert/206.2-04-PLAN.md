---
phase: 206.2-an-mcp-connection-has-somewhere-to-go-insert
plan: 04
type: execute
wave: 4
depends_on: ["206.2-03"]
files_modified:
  - frontend/src/components/workflows/McpToolPicker.tsx
  - frontend/src/components/workflows/ConnectionPicker.tsx
  - frontend/src/components/workflows/McpToolPicker.test.tsx
  - frontend/src/components/workflows/McpToolPicker.reachability.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.describe.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.session.test.tsx
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .planning/ROADMAP.md
  - .planning/seeds/SEED-146-integration-capability-surface.md
autonomous: false
requirements: [CONN-02, CONN-03]
worktrees: forbidden
gap_closure: false

must_haves:
  truths:
    - "D-206.2-04 — **the grant control ships in THIS phase, and it is not a follow-on.** Phase 206's enforcement is `tool_grants.get(tool_name) is True`, so **a missing key DENIES**. A step that can be bound but whose tools can never be granted is a SECOND built-but-unreachable feature — the exact defect this phase exists to close. Binding without granting is not a smaller version of this phase; it is **this phase's own failure mode**."
    - "D-206.2-05 — SEED-146's standing rule (*never add an outbound capability before the approval model exists*) is ANSWERED IN WRITING, at the control's own site: **the per-tool grant IS the approval model for this surface.** Phase 206 shipped the egress, the SSRF guard, the dispatch, the enforcement and the `tool_refused` receipt; what was missing is the human-facing switch the enforcement already reads. **This phase adds no new outbound capability.** ⚠ If an executor finds that reading does not hold, that is a **STOP** — escalate rather than shipping the control."
    - "D-206.2-16 / UI-SPEC — ⚠ **`updateConnectorGrants` IS A WHOLE-COLUMN REPLACE AND THE NAIVE TOGGLE IS A LOST-UPDATE BUG THAT TYPECHECKS PERFECTLY.** A payload of `{[tool]: next}` alone **wipes every other grant on the connection** (measured at `connector_service.py:786-793`, and asserted by `206.2-01`'s new backend case). Four parts, all copied from the ONE shipped UI toggle whose write sends a whole collection: send the FULL MERGED MAP derived from the **SERVER-OWNED PROP** (never a `useState` copy); `if (busy) return` at the top; **NO OPTIMISTIC FLIP**; a `✎` receipt on success and a plain-language retry on failure."
    - "UI-SPEC — ⚠ **THE FRESHNESS ROUTE IS STATED, AND IT IS `ADOPT THE RESPONSE ROW`.** `updateConnectorGrants` returns the full updated `ConnectorConnection`, so a new callback prop replaces the matching entry in `ConnectionPicker`'s `read.connections`; `bound` re-derives, badge and switch both move, and the NEXT toggle merges from a fresh map. **The callback is not an optimisation — it is what makes the route correct.** If the returned row is not written back, the two go out of sync and the next toggle reads a stale map. A re-fetch nonce is the REJECTED alternative, and it is rejected for a measured reason: it re-issues the whole list on every toggle inside a 400px panel field, and the response IS the row."
    - "D-206.2-13 / UI-SPEC AR-01 — ⚠ **TWO AUDIENCES ON ONE PANEL, THREE ARMS.** `PATCH /grants` and `POST /discover` are `require_org_manage` (**403**, plus an RLS policy in both `USING` and `WITH CHECK`); `GET /connections` is deliberately ungated org-wide. **admin** sees the Discover button and the switch. **member** sees NEITHER — they are **REMOVED, not disabled** — with ONE sentence in their place, and `MCP_NO_TOOLS_ADMIN_ONLY` replaces the discover-shaped empty note. **unknown/loading** renders **NOTHING** — no sentence at all. ⚠ **A NON-ADMIN MUST NEVER SEE A DISCOVER BUTTON THAT 403s.**"
    - "UI-SPEC AR-01 — ⚠ **REMOVAL, AND WHY IT IS REMOVAL RATHER THAN `aria-disabled`-WITH-A-REASON, WHEN TWO SHIPPED RULES POINT THE OTHER WAY:** *a control is OFFERED-AND-REFUSED when the thing blocking it is fixable by the same person; it is REMOVED when that person can never make it work.* Gate 1 refuses a failing CREDENTIAL, which the author can go and fix. **Org role is not fixable by the person looking at it.** ⚠ **AND REMOVAL ALONE WOULD BE THE FOLD THIS TREE HAS RECORDED FIVE TIMES** — *this tool is not granted* and *you cannot grant it* are TWO facts. The sentence is the second fact, not decoration."
    - "UI-SPEC AR-01 — ⚠ **THE `unknown` ARM ASSERTS NOTHING, AND THAT HAS A SECOND LOAD-BEARING CONSEQUENCE: IN A PROVIDER-LESS RENDER THE GRANT CONTROL ADDS ZERO NEW NODES.** `ExternalActionSection.test.tsx`, `PhaseFormPanel.rails.test.tsx` and the four `WorkflowBuilderPage.*` suites all mount without an `OrgProvider`. `org === null` ⇒ nothing new appears, nothing throws, no request opens. This is *degrade, never throw* applied to a permission read — and printing *\"only an organisation admin can…\"* while the probe is still in flight would state a fact about the caller that has not been measured."
    - "D-206.2-22 — the grant STATE lives in `McpToolPicker.tsx` and nowhere else. `PhaseFormPanel.tsx` holds an ABSOLUTE-ZERO hook pin; `ExternalActionSection.tsx`'s source fence forbids `useContext` — and `useOrgOptional` USES `useContext`. `McpToolPicker.tsx` carries no such fence and already holds five `useState`s; its own docblock names the pin as the reason all this state lives there."
    - "D-206.2-07 / D-206.2-12 — ⚠ **THE REACHABILITY GUARD IS TWO-LEGGED OR IT IS VACUOUS, AND THIS IS THE MOST IMPORTANT SENTENCE IN THE PHASE.** Measured: `McpToolPicker` ALREADY has exactly one production importer (`ConnectionPicker.tsx:61` / `:426`), so the `RunReceipt.test.tsx:249-287` sweep inverted from `0` to `≥1` **PASSES GREEN ON THE BROKEN TREE**. *A guard satisfied by the defect it was written to catch is worse than no guard, because it reads as coverage.* **Leg (a)** is that sweep, with a positive control and a non-vacuity check — the cheap third leg, never the evidence. **Leg (b) is the one that would have FAILED before this phase**: a render through the REAL chain with only `@/lib/api` mocked, **constructing NO component prop by hand**, whose headline is `expect(listMock).toHaveBeenCalledWith()` — ZERO ARGUMENTS — and which reaches `mcp-tool-picker`. **A plan shipping only leg (a) has re-created the defect inside its own guard.**"
    - "D-206.2-14 — ⚠ **`McpToolPicker.test.tsx` IS NOT PINNED.** `grep -n \"Mcp\" scripts/vitest-count-gate.cjs` returns **nothing**. `src/components/workflows` is a `TARGETS` DIRECTORY entry so the suite RUNS, but it carries no `BASELINE` pin: **its 12 cases could have been deleted with the gate green.** 188-12's rule verbatim — *an unpinned file is not lightly guarded, it is UNGUARDED.* It is pinned here, in the same commit as the code that makes it meaningful, and so is the net-new reachability suite."
    - "⚠ **THE MOCK BUDGET IS SPENT IN THE SAME COMMIT, AND THE 196-08 LESSON IS THAT AN INERT FACTORY FAILS AT MOUNT, LOUDLY — 249 red tests from one added export.** Measured: FOUR suites mock only `listConnectorConnections` (`WorkflowBuilderPage.test.tsx:37`, `.describe.test.tsx:150`, `.preDraft.baseline.test.tsx:145`, `.session.test.tsx:134`). They pass today only because their list resolves `[]` so `McpToolPicker` never mounts. **Do not rely on a mount never happening.**"
    - "D-206.2-06 — ⚠ **A DENIED TOOL STAYS DENIED AND THE REFUSAL KEEPS ITS RECEIPT, AND THE NEGATIVE CASE IS DRIVEN FIRST.** The `tool_refused` audit event still fires for an ungranted tool. ⚠ `tool_refused` is a **SHARED `event_type`** — the agent tool-whitelist path writes it with a different metadata shape (`{\"tool\":…, \"allowed\":[…]}`; four such rows already sit in the local `harness_audit`). **Filter on `metadata->>'connection_id'` or `metadata->>'reason' = 'permission_denied'`, NEVER on the name alone.**"
    - "D-206.2-23 — ⚠ **CHROME DEVTOOLS MCP IS NOT AVAILABLE; PLAYWRIGHT + CHROMIUM (`chromium-1223`) ARE.** And ⚠ **206.1's `localStorage` TRICK DOES NOT TRANSFER** — `activeView` is unpersisted `useState` with **no URL router**, so the workflows home is reached by CLICKING `[aria-label=\"Workflows\"]`. Live fixture confirmed: MCP row `7ca5e114-c5cd-4f7f-8c08-8885278bf42d`, `tool_grants = {}`, 3 cached tools; `live_connectors` and `visual_workflow_canvas` both `\"everyone\"`. ⚠ Radix `DropdownMenuTrigger` opens on `pointerdown` — a synthetic `node.click()` leaves menus shut."
    - "D-206.2-19 — ⚠ **SC#3 IS DRIVEN THROUGH ▶ Test Run, A DRAFT PATH WITH NO VERDICT GATE — AND THAT IS SAID OUT LOUD SO A GREEN TEST RUN IS NEVER REPORTED AS *MCP STEPS WORK END TO END*.** `206.2-01` fixed the publish validator, so the SUMMARY must state which of the two paths was actually driven and what the other one's evidence is."
    - "D-206.2-10 — ⚠ **THIS PHASE CLOSES PHASE 206.1's OWED SC#1b, AND THE ROADMAP ROW IS UPDATED IN THE SAME COMMIT AS THE DRIVE.** An owed criterion nobody goes back to mark is a deferral with no re-open."
    - "D-206.2-11 — ⚠ **CLAUDE.md IS PAST ITS WARN BAND (124,357 chars, 82.9%, 25,643 of headroom) AND THIS PHASE ADDS UP TO SIX ROWS PLUS FOUR CORRECTIONS.** **The fix is NEVER a thinner row** — the table is the audit SCAN LIST and stays complete; only prose leaves. Run `node scripts/check-claude-md-size.cjs`, record the new figure, and **ESCALATE THE SPLIT** (`Workflow guardrails (MANDATORY)` is 76,531 chars — 62% of the file) rather than shaving a cell. **The split is a phase, not a task.**"
    - "D-206.2-11 — ⚠ **RE-DERIVE EVERY TRIPLE AT THE LEDGER COMMIT, NEVER INCREMENT BY HAND AND NEVER QUOTE.** Four rows are ALREADY stale or absent at this phase's base and this phase's own three earlier waves move four more. **A row that is present and WRONG answers the auditor with `satisfied` and stops the audit, which is worse than an absent row.**"
  artifacts:
    - path: "frontend/src/components/workflows/McpToolPicker.tsx"
      provides: "the three-arm audience render, the native `<button role=\"switch\">`, the full-merged-map write with no optimistic flip, the ✎ receipts, and every new string as an exported identifier"
      exports: ["MCP_GRANT_TOGGLE_LABEL", "MCP_GRANT_SCOPE_NOTE", "MCP_GRANT_SAVING", "MCP_GRANT_RECEIPT_ALLOWED", "MCP_GRANT_RECEIPT_DENIED", "MCP_GRANT_WRITE_FAILED", "MCP_GRANT_ADMIN_ONLY_NOTE", "MCP_NO_TOOLS_ADMIN_ONLY"]
    - path: "frontend/src/components/workflows/McpToolPicker.reachability.test.tsx"
      provides: "the TWO-LEGGED guard — leg (a) the importer sweep with a positive control, leg (b) the data-path render through the real chain constructing no component prop"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "BASELINE pins for `McpToolPicker.test.tsx` (net-new pin on a suite that has been unguarded since Phase 206) and for the reachability suite"
    - path: "CLAUDE.md"
      provides: "the hot-file rows this phase owes, each paired with its section in the SAME COMMIT"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "the matching sections — measured triple, the commit it was measured at, what the file IS, and `### Invariants a future editor is bound by`"
    - path: ".planning/ROADMAP.md"
      provides: "206.1's owed SC#1b marked closed, in the same commit as the drive that closed it"
  key_links:
    - from: "frontend/src/components/workflows/McpToolPicker.tsx"
      to: "frontend/src/lib/api.ts (updateConnectorGrants)"
      via: "the FIRST production caller this function has ever had, sending the full merged map"
      pattern: "updateConnectorGrants"
    - from: "frontend/src/components/workflows/McpToolPicker.tsx"
      to: "frontend/src/components/workflows/ConnectionPicker.tsx"
      via: "`onConnectionUpdated(row)` — the response row is adopted into `read.connections` so `bound` re-derives"
      pattern: "onConnectionUpdated"
    - from: "frontend/src/components/workflows/McpToolPicker.reachability.test.tsx"
      to: "frontend/src/components/workflows/ExternalActionSection.tsx"
      via: "leg (b) drives the PRODUCTION parent — the shape control, then the picker — and constructs no `McpToolPicker` prop at all"
      pattern: "ExternalActionSection"
---

<objective>
Ship the write this whole feature has been missing, prove that the door is reachable in a way the
shipped gates could not, drive the round trip in a real browser, and pay the ledger debt.

`updateConnectorGrants` has existed since Phase 206 with **zero production callers**. Phase 206's
enforcement is `tool_grants.get(tool_name) is True` — **a missing key DENIES** — so a step that can be
bound but whose tools can never be granted is a second built-but-unreachable feature. That is not a
smaller version of this phase; it is this phase's own named failure mode.

⚠ **AND THE GUARD IS THE POINT.** Every gate was green while this defect shipped, because
`McpToolPicker.test.tsx` mounts the component directly with a connection prop **no production code
path could produce** — and the `import.meta.glob` sweep SEED-200 asked for is **green at this
phase's base, against the defect**, since the import edge already exists. The unreachability lives one
level up, in the props. So the guard is two-legged, and leg (b) — a render through the real chain that
constructs no component prop — is the one that would have failed.

**Output:** the grant control with three audience arms, a two-legged reachability guard whose second
leg was RED before this phase, a driven round trip that includes the DENY case, and six hot-file rows
with their six sections in the same commit.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-CONTEXT.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-UI-SPEC.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-PATTERNS.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-RESEARCH.md
@.planning/seeds/SEED-200-mcp-connection-cannot-be-bound-to-any-step.md
@.planning/seeds/SEED-146-integration-capability-surface.md
@.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-VERIFICATION.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-01-SUMMARY.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-02-SUMMARY.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-03-SUMMARY.md
</context>

<interfaces>
<!-- Read at HEAD by the research + pattern passes and re-read by this planner. No exploration needed. -->

**`McpToolPicker.tsx`** — `:5-13` the docblock naming the zero-hook pin as the reason all state lives
here; `:15-23` its imports (`useCallback, useState`; `AlertTriangle, CheckCircle2, RefreshCw, Wrench`;
`Button`, `Label`, `Textarea`; `discoverConnectorTools` — **`updateConnectorGrants` is NOT imported
yet**); `:25-35` nine exported strings including `MCP_NO_TOOLS_DISCOVERED` =
`"No tools discovered yet. Click Discover Tools to fetch available actions from the remote server."`;
`:37-45` `McpToolPickerProps`; `:47-57` ⭐ `isToolGranted` — the own-property form, mirroring the
server's `grants.get(t) is True`; `:66-76` its five `useState`s; `:131-133` the
`if (!connection?.mcp_server_url) return null` guard; `:143-166` the header row with
`data-testid="mcp-discover-btn"`; `:177-183` `mcp-no-tools`; `:184-208` the tool `<select>`
(`id="mcp-tool-select"` **hardcoded, not `useId`**); `:210-241` ⭐ the
`data-testid="mcp-grant-status"` badge with `data-granted`, `MCP_GRANT_GRANTED_LABEL` /
`MCP_GRANT_DENIED_LABEL` / `MCP_GRANT_DENIED_WARNING`, and its `bg-emerald-500/10` /
`bg-amber-500/10` raw-palette tones; `:243-251` `mcp-tool-description`; `:253-275` the args textarea.

**The org seam** — `frontend/src/providers/OrgProvider.tsx:215-217`,
`export function useOrgOptional(): OrgValue | null { return useContext(OrgContext) }` — **non-throwing,
returns `null` outside a provider**. `OrgValue` carries `canManage: boolean` (`:51`) and
`loading: boolean` (`:58`). ⚠ `:20` says in its own words that `canManage` decides **RENDERING ONLY**;
the wall is the server. ⭐ The mocking precedent is `frontend/src/components/layout/NavPanel.test.tsx:30-58`
(`vi.mock("@/providers/OrgProvider", () => ({ useOrgOptional: vi.fn() }))` then
`vi.mocked(useOrgOptional).mockReturnValue(org)`).

**The write** — `frontend/src/lib/api.ts:6656-6674` (**READ, NEVER EDIT**):
`updateConnectorGrants(id: string, grants: Record<string, boolean>): Promise<ConnectorConnection>`,
`PATCH …/grants`, body is `JSON.stringify(grants)` — a **bare** object at the top level — returning
the full updated row.

**⭐ The ONE shipped UI toggle whose write sends a whole collection** —
`frontend/src/components/admin/FeatureVisibility.tsx:213-258`: `busy` / `failed` / `receipt` state,
`write(next, nextRoles)` with `setBusy(true)` → `await` → `setReceipt(…)` →
`window.setTimeout(() => setReceipt(null), 4000)` → `catch { setFailed(true) }` → `finally
{ setBusy(false) }`; and `toggleRole` opening with **`if (busy) return`** and deriving `nextRoles` from
the **server-owned prop**. `:194-195` verbatim: *"Owns only its transient write state (busy/error) +
the ✎ receipt; the audience value itself is the shell's source of truth (no optimistic flip)."*
⚠ **WHAT NOT TO COPY:** `CapabilityGrid.tsx:146-160` flips a single boolean with
`await onToggle(def.key, !on)` — **that expression shape IS the lost-update bug when the endpoint is a
map replace.** Copy its `busy`/`failed` scaffolding, never its payload.

**The mount and its props** — `ConnectionPicker.tsx:425-433`. `bound` is
`connections.find(row => row.id === boundId)` (`:368`) over `read.connections` (`:354`).

**The model sweep to invert for leg (a)** — `frontend/src/components/workflows/RunReceipt.test.tsx:249-287`,
in full: the glob options string, the `> 200` non-vacuity, the self-exclusion of the component's own
two files, `toStrictEqual` on basenames (never `toContain`), the explicitly-named negatives, and the
separate positive-control `it()`. ⚠ Its self-exclusion names TWO files; `McpToolPicker` will have
THREE, so use the broader predicate from `WorkspacePanel.test.tsx:1148-1155`
(*"a fence that swept its own test files would red on itself"*), which filters `__tests__` and
`/\.test\.tsx?$/`. ⚠ `import.meta.glob` contributes the **EMPTY STRING** for an absent path and never
throws — **non-vacuity is asserted BEFORE contents.**

**The four mock factories to widen** — `frontend/src/pages/WorkflowBuilderPage.test.tsx:37`,
`.describe.test.tsx:150`, `.preDraft.baseline.test.tsx:145`, `.session.test.tsx:134`. Each carries
`listConnectorConnections` and neither of the other two connector functions. ⭐ The already-complete
shape to copy is `WorkflowDoorSwitch.test.tsx:79-81`, which carries all three.
⚠ `McpToolPicker.test.tsx:13-15` is a **ONE-KEY** factory (`discoverConnectorTools` only) and must
gain `updateConnectorGrants` in the SAME commit as the import.

**The gate's two knobs** — `TARGETS` at `scripts/vitest-count-gate.cjs:3009-3011` already carries the
DIRECTORY `src/components/workflows`, so both suites RUN; `BASELINE` is what makes them GUARDED. The
script's own comment at `:1596-1603` refuses a redundant file-level `TARGETS` entry beside a directory
that already covers it — so **"both knobs" is satisfied by the existing directory entry plus a new
BASELINE pin**, and the SUMMARY says so with the grep that proves it.

**The live fixture, read from the local DB 2026-08-25** — `connector_connections` has 2 rows:
`Slack-rag-test` (`post_message`) and `DeepWiki (206.1 UAT)` =
`7ca5e114-c5cd-4f7f-8c08-8885278bf42d`, `capability = NULL`,
`mcp_server_url = https://mcp.deepwiki.com/mcp`, `is_enabled = true`, **`tool_grants = {}`**,
`discovered_tools` = `ask_question` (needs `repoName`, `question`), `read_wiki_contents` (`repoName`),
`read_wiki_structure` (`repoName`) — **no `annotations` key**. `app_settings.workflows_enabled` is
`true`; `feature_visibility.live_connectors` and `.visual_workflow_canvas` are both `"everyone"`.
Existing drafts: `vendor-renewal-brief-a49f6398` ("Northwind QBR") + two `zz-200-06-fixture-*`, **none
with an `external_action` step**.

**The Test Run control** — `WorkflowBuilderPage.tsx:2660-2669`, gated on
`canvasEnabled && draftId && onTestRun`, disabled only by
`testRunInFlight || persistState.kind === "saving"` — **no verdict gate**.

**The run-time gates** — `phase_types.py`: G1 golden run · G2 `live_connectors == "everyone"` ·
G3 egress · G4 `connection_id` · G5 `resolve(org)` · **G6 `grants.get(tool_name) is True`** → denied
writes `tool_refused` with `{"phase", "connection_id", "tool_name", "reason": "permission_denied"}`
and returns a `failure`; granted → `mcp_client.call_tool` → `_write_send_receipt(ctx, phase,
capability="mcp", connection_id=…, host=connection.mcp_server_url, raw_status=200)`.
⚠ **THE RECEIPT DOES NOT CARRY THE TOOL NAME.** Do not claim it does.
⚠ `capability="mcp"` is a metadata STRING in an audit row, not a `connector_connections.capability`
value. **Do not "fix" it.**

**Hot-file triples, RE-DERIVED at this phase's base `4c9b9a7f` — ⚠ RE-DERIVE AGAIN AT THE LEDGER
COMMIT, because waves 1-3 move four of them:**

| File | base triple | state |
|---|---|---|
| `frontend/src/components/workflows/phaseVocabulary.ts` | 16 / 6 / 990 | ⚠ **FIRES at six phases and has NEVER been in the table** — named only inside `WorkflowCanvas.tsx`'s cell |
| `frontend/src/components/workflows/ConnectionPicker.tsx` | 2 / 2 / 439 | ⚠ absent from BOTH; **crosses the threshold in this phase's own commit** |
| `frontend/src/components/workflows/ExternalActionSection.tsx` | 3 / 2 / 238 | ⚠ absent from BOTH — **zero mentions anywhere** |
| `frontend/src/components/workflows/McpToolPicker.tsx` | 1 / 1 / 284 | ⚠ **named inside Phase 206's SECTION with no ROW of its own** — the drift the same-commit rule forbids |
| `frontend/src/components/workflows/PhaseFormPanel.tsx` | 27 / 12 / 1560 | ✅ row exists, reads `24 / 11 / 1375` — **STALE**, missing Phase 205 |
| `backend/app/services/harness/grounding.py` | 18 / 5 / 1252 | ✅ row exists and was CURRENT at base — wave 1 moved it |
| `frontend/src/components/settings/connectionsCopy.ts` | 4 / 3 / 541 | ✅ row exists — wave 2 moved it |
| `scripts/vitest-count-gate.cjs` | 115 / 20 / 3845 | ✅ row exists, reads `114 / 19 / 3797` — **STALE** |
| `frontend/src/components/workflows/externalShapeVocabulary.ts` | NEW | list **below** the threshold on purpose — the `fileIcon.tsx` / `connectionMark.tsx` precedent |
| `frontend/src/components/workflows/stepReadinessContext.ts` | 2 / 1 / 183 | below threshold — **decline a row, in writing, with a trigger** |

**The ledger pair's shape** — commit `5343b1e8` (*"five hot-file rows and five detail sections, same
commit"*): an anchor link in the row → a `## \`path\`` heading in `docs/HOT-FILE-LEDGER.md`; the
measured triple + **the commit it was measured at** + who created it; a paragraph on why it is / is
not above threshold; one paragraph on what the file IS; then
`### Invariants a future editor is bound by` as ⚠-led bullets, each stating a rule AND the failure it
prevents.

**READ, NEVER EDIT:** `frontend/src/lib/api.ts` (Phase 207).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The grant control — three audience arms, a native `role="switch"`, the FULL MERGED MAP, no optimistic flip, and the response row adopted upstream</name>
  <files>frontend/src/components/workflows/McpToolPicker.tsx, frontend/src/components/workflows/ConnectionPicker.tsx</files>

  <read_first>
    - `frontend/src/components/workflows/McpToolPicker.tsx` **in full** (284 L) — every export, the
      five `useState`s, the `!connection?.mcp_server_url` guard, and ⭐ `:210-241`, the badge region
      SC#2c requires to pass UNEDITED
    - ⭐ `frontend/src/components/admin/FeatureVisibility.tsx:190-260` — the four-part write, in full,
      including its no-optimistic-flip sentence at `:194-195`
    - ⚠ `frontend/src/components/admin/CapabilityGrid.tsx:146-160` — the shape **NOT** to copy
    - `frontend/src/providers/OrgProvider.tsx:15-25` (⭐ the RENDERING-ONLY note), `:45-60`
      (`OrgValue`), `:205-217` (`useOrg` throws / `useOrgOptional` does not)
    - `frontend/src/components/workflows/ConnectionPicker.tsx:340-433` — `read.connections`, `bound`,
      and the mount; this is where the adopted row must land
    - `frontend/src/lib/api.ts:6650-6680` — `updateConnectorGrants`. **READ ONLY.**
    - `backend/app/api/connectors.py:574-630` and `backend/app/dependencies.py:894-911` — the two
      org-admin routes and the 403, so the render decision matches the real wall
    - `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-UI-SPEC.md`
      § "Surface 3" **in full**, § "Copywriting Contract" and § "Auto-Resolved Decisions" AR-01/02/06
    - `.planning/seeds/SEED-146-integration-capability-surface.md` — the standing rule D-206.2-05
      answers
  </read_first>

  <behavior>
    - **admin** (`org?.canManage === true`): the Discover button renders byte-identically to today; the
      switch renders; `MCP_GRANT_SCOPE_NOTE` renders beneath it.
    - **member** (`org !== null && org.loading === false && org.canManage === false`): **NO Discover
      button, NO switch**, `MCP_GRANT_ADMIN_ONLY_NOTE` in their place at `data-testid="mcp-grant-admin-only"`,
      and `MCP_NO_TOOLS_ADMIN_ONLY` replacing `MCP_NO_TOOLS_DISCOVERED` when no tools are cached.
    - **unknown** (`org === null` **or** `org.loading === true`): **nothing at all** — no switch, no
      button, no sentence. The badge and its warning still render, because those are facts about the
      GRANT, not about the caller.
    - The switch is a native `<button role="switch" aria-checked={isGranted}
      data-testid="mcp-grant-toggle" data-grant-write="idle|pending|failed">` spanning the row's full
      width at `px-2 py-1` around an `h-4 w-7` track with an `h-3 w-3` thumb, WITH its label inside —
      one ~26px × ~370px target, not a 28px × 16px one.
    - **The write:** `if (busy) return` first; then
      `updateConnectorGrants(connection.id, { ...(connection.tool_grants ?? {}), [toolName]: next })`
      where `connection` is the PROP. **Never a `useState` copy. Never `{[tool]: next}` alone.**
    - **No optimistic flip.** The switch does not move on press; it moves when the row it renders from
      changes. `pending` sets `aria-busy="true"` and renders `MCP_GRANT_SAVING` in a `role="status"`.
    - On success the response row is handed upstream via a new optional `onConnectionUpdated(row)`
      prop; `ConnectionPicker` replaces the matching entry in `read.connections`, `bound` re-derives,
      badge and switch both move, and the next toggle merges from a fresh map. A `✎` receipt renders
      in place and clears after 4000ms — **two directions, two receipts**; a single "Saved" would hide
      which way it went.
    - On failure: `data-grant-write="failed"`, `MCP_GRANT_WRITE_FAILED` in a `role="alert"`, and the
      switch at the SERVER's old value — **it never moved, so there is nothing to roll back.**
    - The badge (`mcp-grant-status`) re-derives from the same adopted row and gets **no state of its
      own**. `isToolGranted` is the ONE grant predicate and is not forked.
  </behavior>

  <action>
    Add the eight strings to `McpToolPicker.tsx` beside its shipped nine, verbatim from the UI-SPEC
    contract table. Each carries its reason: `MCP_GRANT_TOGGLE_LABEL` says *on this connection*, not
    *for this step*, because `PATCH /grants` writes a column on the CONNECTION, org-wide — a label
    saying *this step* would be a lie the API itself refuses to make true. `MCP_GRANT_SCOPE_NOTE` is
    **D-206.2-05 in the person's words, at the control's own site**, and **must not be trimmed to one
    clause** — the second clause is the org-wide consequence and it is the whole point.
    `MCP_GRANT_SAVING` is REQUIRED by the no-optimistic-flip rule: a person presses a switch that does
    not move, and without this reading the control looks broken. `MCP_NO_TOOLS_ADMIN_ONLY` exists
    because the shipped `MCP_NO_TOOLS_DISCOVERED` says *"Click Discover Tools…"* to someone who has no
    such button — **a measured lie this phase would otherwise ship** — and it names no second location,
    because nothing was measured that lets a member reach discovery anywhere.

    Import `useOrgOptional` and read it. Derive the three arms exactly as `<behavior>` states.
    ⚠ **`useOrgOptional`, never `useOrg`** — the latter throws outside a provider and six shipped
    suites mount without one. Record at the site that the `unknown` arm asserting NOTHING is what keeps
    those six renders free of new nodes, and that printing an admin sentence during an in-flight
    permission probe would state a fact about the caller that has not been measured.

    Add the switch, the `role="status"` write-state line, the `role="alert"` failure line and the scope
    note INSIDE the existing `{toolName && …}` block, in the UI-SPEC's order, **OUTSIDE and BELOW the
    badge**. ⚠ **Do not nest the control inside the badge** — the badge is a tinted box carrying STATE;
    nesting a control in it makes a state reading look like a button and puts a control inside the one
    region SC#2c requires to pass unedited.

    ⚠ **THE CONTROL SPENDS NO COLOUR.** Track `bg-foreground/25` when on, `bg-muted-foreground/25` when
    off; the differentiator is the THUMB'S POSITION plus `role="switch"` + `aria-checked`. Measured
    reason, recorded at the site: a token-green switch (`--success`, hue 142) beside the shipped
    emerald badge (hue 160) puts **two different greens meaning the same thing two lines apart**, and a
    raw-emerald switch would extend a bounded palette exception. **No new accent site** — the reserved
    list is the focus ring and the `Wrench` glyph, and a third KIND is a contract violation.
    ⚠ **No `title=` anywhere.** ⚠ **No `npx shadcn add switch`** — `src/components/ui/switch.tsx` does
    not exist (16 primitives are vendored and Switch is not one), and a `role="switch"` button IS a
    real switch by Phase 185's own words. **No package is installed by this phase.**

    Write the handler with `if (busy) return` at the top and the FULL MERGED MAP derived from the
    `connection` PROP. Record at the site, in a `── D-206.2-16 ──` block: that the endpoint is a
    whole-column REPLACE (measured at `connector_service.py:786-793` and asserted by `206.2-01`'s
    backend case), that `{[tool]: next}` alone would wipe every other grant, that this is still a
    read-modify-write over a client snapshot and two admins in two browsers can clobber each other,
    and that **the honest mitigation is the scope SENTENCE, not a lock** — `tool_grants` carries no
    version column and adding one is a migration this phase forbids.

    Add the optional `onConnectionUpdated?: (row: ConnectorConnection) => void` prop and call it with
    the response. In `ConnectionPicker.tsx`, pass a handler that replaces the matching entry in the
    settled `read.connections` by id. Record there that **the callback is not an optimisation — it is
    what makes the adopt-the-response route correct**; without it the two go out of sync and the next
    toggle reads a stale map. Record the REJECTED alternative (a re-fetch nonce) and why: it re-issues
    the whole connection list on every toggle inside a 400px panel field, and the response IS the row.
    Re-open trigger: *a second writer of `tool_grants` appearing anywhere in the builder, or the first
    observed clobber.*

    ⚠ **DO NOT EDIT `frontend/src/lib/api.ts`.** `updateConnectorGrants` already ships.
    ⚠ **Do not change the badge's text, glyphs or tone classes.** ⚠ **Do not fork `isToolGranted`.**
    ⚠ **STOP CONDITION (D-206.2-05):** if, while writing this, the reading *"the per-tool grant IS the
    approval model for this surface"* turns out not to hold — for example if some other outbound
    capability is found to be reachable without a grant — **escalate rather than shipping the
    control.**
  </action>

  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/McpToolPicker.test.tsx src/components/workflows/ConnectionPicker.test.tsx src/components/workflows/ExternalActionSection.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --numstat -- frontend/src/lib/api.ts | wc -l` == `0`.
    - `grep -rn "updateConnectorGrants" frontend/src --include=*.tsx --include=*.ts | grep -v "\.test\." | grep -v "lib/api.ts" | wc -l` >= `1`
      where it was `0` at this phase's base. **Record both readings** — this is SC#2's caller census.
    - `grep -c "useOrgOptional" frontend/src/components/workflows/McpToolPicker.tsx` == 1 and
      `grep -c "useOrg(" frontend/src/components/workflows/McpToolPicker.tsx` == `0`.
    - `grep -c 'role="switch"' frontend/src/components/workflows/McpToolPicker.tsx` == 1;
      `grep -c "shadcn" frontend/package.json` unchanged; `git diff --numstat -- frontend/package.json frontend/package-lock.json | wc -l` == `0`.
    - `grep -cE 'title=|bg-emerald|bg-amber' frontend/src/components/workflows/McpToolPicker.tsx` — the
      `title=` count is `0` and the emerald/amber counts are **UNCHANGED** from base (record both).
    - The merged-map payload is asserted, not described: a test drives a toggle against a connection
      whose `tool_grants` is `{"a": true}` and asserts the mock was called with a payload whose key set
      is `{"a", "<toolName>"}` — **SET EQUALITY**, because a superset check would pass a payload that
      dropped `"a"`. (The case itself lands in task 2; this criterion is the contract it asserts.)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — count equals this plan's re-derived
      base, zero in any touched file.
  </acceptance_criteria>

  <done>
    `updateConnectorGrants` has its first production caller. The switch never moves on its own, sends
    the whole map, adopts the response upstream, and renders for exactly one of three audiences — with
    a provider-less render adding no node at all.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: THE TWO-LEGGED REACHABILITY GUARD — leg (b) is the one that would have failed — plus the grant control's cases, the mock budget, and the two BASELINE pins</name>
  <files>frontend/src/components/workflows/McpToolPicker.reachability.test.tsx, frontend/src/components/workflows/McpToolPicker.test.tsx, frontend/src/pages/WorkflowBuilderPage.test.tsx, frontend/src/pages/WorkflowBuilderPage.describe.test.tsx, frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx, frontend/src/pages/WorkflowBuilderPage.session.test.tsx, scripts/vitest-count-gate.cjs</files>

  <read_first>
    - ⭐ `frontend/src/components/workflows/RunReceipt.test.tsx:249-287` **verbatim** — the sweep, its
      inversion rationale (*"a removed assertion proves nothing afterwards, whereas a live one that
      now names the ONE permitted importer reddens again if a SECOND mount ever appears"*) and its
      separate positive-control `it()`
    - ⭐ `frontend/src/components/panel/WorkspacePanel.test.tsx:1148-1155` — `productionOnly()`, and
      *"a fence that swept its own test files would red on itself"*
    - `frontend/src/components/workflows/McpToolPicker.test.tsx` **in full** (190 L) — especially
      `:13-15` (the ONE-KEY api factory), `:27-55` (⭐ `mockMcpConnection`, **the hand-built prop
      SEED-200 names as the exhibit — KEEP IT**, the unit contract is fine), `:57-74` (`isToolGranted`),
      `:97-120` (⭐ the two badge labels SC#2c requires UNEDITED), `:81-88`, `:135-157`
    - `frontend/src/components/workflows/ConnectionPicker.test.tsx:110-149` — `draftedStore` /
      `renderPicker`, the harness leg (b) must NOT reuse verbatim (it constructs a picker prop)
    - `frontend/src/components/layout/NavPanel.test.tsx:28-60` — ⭐ the `useOrgOptional` mocking idiom
    - `frontend/src/pages/WorkflowBuilderPage.test.tsx:30-45`, `.describe.test.tsx:145-155`,
      `.preDraft.baseline.test.tsx:140-150`, `.session.test.tsx:128-140` — the four one-key factories
    - ⭐ `frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx:75-85` — the already-complete
      three-key factory to copy
    - `scripts/vitest-count-gate.cjs:1590-1630` — the pin comments' house style and ⭐ the two-knob rule
    - `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-PATTERNS.md` § M-1 and
      § M-2, and `206.2-RESEARCH.md` § OQ#5
  </read_first>

  <behavior>
    - **LEG (a) — the importer sweep.** `import.meta.glob("/src/**/*.{ts,tsx}", {query:"?raw",
      import:"default", eager:true})`, filtered by `productionOnly()`, matching
      `/from\s+["'][^"']*McpToolPicker["']/` or `/<McpToolPicker[\s/>]/`, yields basenames
      `toStrictEqual(["ConnectionPicker.tsx"])` — **exactly one, named**, never `toContain`.
      Non-vacuity (`Object.keys(modules).length > 200`) is asserted BEFORE contents. A separate
      positive-control `it()` proves both regexes match a planted literal.
      ⚠ **This leg is GREEN AT THIS PHASE'S BASE, against the defect** — the plan says so IN THE
      SOURCE, so nobody reads it as this phase's evidence.
    - **LEG (b) — the data path.** Render the PRODUCTION parent `ExternalActionSection` inside a real
      `BuilderStoreProvider` + `SelectedPhaseSlugProvider`, with **ONLY `@/lib/api` mocked** and
      `listConnectorConnections` resolving a wire-shaped MCP row. Press the MCP shape segment, then
      select the row in `connection-picker-select`. Assert, in order:
      1. `expect(listMock).toHaveBeenCalledWith()` — **ZERO ARGUMENTS**. *This single assertion is the
         line SEED-200 is about.*
      2. `screen.getByTestId("mcp-tool-picker")` is in the document.
      3. `screen.getByTestId("mcp-tool-select")` is present and its options contain the tool name.
      ⚠ **NO `McpToolPicker` PROP IS CONSTRUCTED ANYWHERE IN THIS FILE.** The only thing the test
      supplies is the WIRE RESPONSE. A test that hands `ConnectionPicker` a `connection` prop is the
      same defect wearing a different file name.
    - **LEG (b)'s POSITIVE CONTROL is a real falsification, not a regex smoke test:** run the same
      render with the row's `mcp_server_url` stripped and assert
      `queryByTestId("mcp-tool-picker")` is `null`.
    - **The cross-shape negative, in the same file:** with the CAPABILITY shape selected the MCP row is
      not listed, and with the MCP shape selected a `post_message` row is not.
    - **The grant control's cases** (in `McpToolPicker.test.tsx`): the three audience arms; the
      full-merged-map payload asserted by SET EQUALITY; `if (busy) return` (two rapid presses ⇒ ONE
      call); no optimistic flip (`aria-checked` unchanged while pending); the `✎` receipt in both
      directions; the failure arm rendering `role="alert"` with the switch still at the server's value;
      and `onConnectionUpdated` called with the response row.
    - **The `unknown` arm adds ZERO new nodes** in a provider-less render — asserted as a node COUNT or
      an `innerHTML` identity against the pre-change render, with a non-vacuity control.
    - **`MCP_GRANT_GRANTED_LABEL` / `MCP_GRANT_DENIED_LABEL` cases pass UNEDITED** — proved by a
      zero-deletion diff over their line range, not by re-running.
  </behavior>

  <action>
    Create `frontend/src/components/workflows/McpToolPicker.reachability.test.tsx` carrying BOTH legs,
    and open it with a header stating the tier discipline in the file's own words: T1 is the tier that
    SHIPPED the defect; T2 (leg a) proves an import edge and **cannot** prove the props that travel it;
    T3 (leg b) is the tier that would have FAILED before this phase and is the one SC#4 turns on.

    ⚠ **OBSERVE LEG (b) RED BEFORE TRUSTING IT.** Check the phase's base commit's
    `ExternalActionSection.tsx` and `ConnectionPicker.tsx` out to a scratch path, run leg (b) against
    them, and record the failure verbatim. If that is impractical, achieve the same falsification by
    temporarily reverting the MCP read to `listConnectorConnections(capability)` in the working tree,
    observing RED, and restoring md5-identical — the `ConnectionPicker.test.tsx:11-13` method.
    **Paste the RED output into the commit message.** A guard first observed GREEN is not a guard.

    Widen `McpToolPicker.test.tsx`'s api factory to carry `updateConnectorGrants` in the SAME commit as
    the import, and add the grant cases below every shipped case under the
    `⚠ ADDED, NEVER RE-BASELINED` banner. Mock `useOrgOptional` with the `NavPanel.test.tsx` idiom.
    **Keep `mockMcpConnection`** — the unit contract is fine and deleting it would remove coverage; the
    phase's job is to ADD the data-path tier, not to remove the component tier.

    Widen the four `WorkflowBuilderPage.*` factories to the complete three-key shape, copying
    `WorkflowDoorSwitch.test.tsx:79-81`. ⚠ Spend this budget even though those suites' lists resolve
    `[]` today: the 196-08 lesson is that an inert factory fails at MOUNT, loudly (249 red tests from
    one added export), and **a plan should not rely on a mount never happening.**

    Add TWO `BASELINE` entries to `scripts/vitest-count-gate.cjs`: `"McpToolPicker.test.tsx"` (a
    NET-NEW pin on a suite that has been running-but-unguarded since Phase 206 — quote 188-12's rule)
    and `"McpToolPicker.reachability.test.tsx"` (pinned in the commit that creates it). Read both
    numbers **out of the gate's own `actual` column**, never counted by hand. In the pin comment,
    record the two-knob measurement: `grep -n "src/components/workflows" scripts/vitest-count-gate.cjs`
    shows the DIRECTORY `TARGETS` entry that already makes both files RUN, so no `TARGETS` edit
    accompanies these pins — the script's own `:1596-1603` comment refuses a redundant entry that would
    state a dependency that is not real. **That is how "both knobs" is satisfied here, and the SUMMARY
    says so with the grep.**
  </action>

  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs 2>&1 | tail -25</automated>
  </verify>

  <acceptance_criteria>
    - Leg (b)'s RED is recorded VERBATIM in the commit message, together with the restoration proof
      (an empty `git diff` or an md5 match) for whichever falsification method was used.
    - `grep -c "McpToolPicker" frontend/src/components/workflows/McpToolPicker.reachability.test.tsx`
      >= 1 **and** the file contains **no** construction of a `connection={` prop for it:
      `grep -c "connection={" frontend/src/components/workflows/McpToolPicker.reachability.test.tsx`
      == `0`. This is leg (b)'s defining property and it is asserted mechanically.
    - `toHaveBeenCalledWith()` with zero arguments appears in leg (b), and the file also contains the
      capability-shape counterpart with a capability string.
    - Leg (a) uses `toStrictEqual` (not `toContain`), asserts `> 200` modules BEFORE contents, uses
      `productionOnly()`, and has a separate positive-control `it()`.
    - The source states leg (a) was green at the base:
      `grep -ci "green at" frontend/src/components/workflows/McpToolPicker.reachability.test.tsx` >= 1.
    - `git diff -U0 -- frontend/src/components/workflows/McpToolPicker.test.tsx | grep '^-[^-]'`
      contains **only** the api-factory line being widened — the two badge-label cases show zero
      deletions.
    - All four `WorkflowBuilderPage.*` factories carry all three connector functions:
      `for f in ...; do grep -c updateConnectorGrants $f; done` returns `1` for each.
    - `grep -c "McpToolPicker.test.tsx" scripts/vitest-count-gate.cjs` >= 1 where it was `0`
      (`grep -n "Mcp" scripts/vitest-count-gate.cjs` returned NOTHING at base — record both).
    - The gate's verdict line is read VERBATIM from the repo root: `failed 0`, no per-file DECREASE,
      and the pinned FILE count is `+2`.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — count equals this plan's base, zero in
      any touched file.
    - ⚠ A red gate is triaged from its PERSISTED JSON before any re-run; failing filenames are checked
      against `git diff --numstat`. **Do not reach for the worker cap.** A red gate is sometimes REAL.
  </acceptance_criteria>

  <done>
    The guard has two legs, leg (b) was observed RED against the pre-phase data path, and leg (a) says
    in its own source that it was green at the base. Two suites that could have been deleted with the
    gate green are now pinned. Four incomplete mock factories are complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BROWSER TIER — drive the round trip, DENY FIRST, then the grant flip and the run (SC#1, SC#2, SC#3, and 206.1's owed SC#1b)</name>
  <files>(none — evidence only. This task writes no source file; it records raw browser, network and SQL readings into the plan's SUMMARY.)</files>

  <read_first>
    - `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-RESEARCH.md` § OQ#7 —
      the full recipe, the live fixture, and the routing findings
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-03-SUMMARY.md`
      — how 206.1's executor drove Playwright, minted a local-only session, and recorded teardown
    - `frontend/src/App.tsx:120-135` and `:220-230` — ⚠ `activeView` is unpersisted `useState` and
      there is **no URL router**
    - `frontend/src/lib/nav-items.ts:30-40` — the rail item whose name becomes `aria-label="Workflows"`
    - `frontend/src/pages/WorkflowsPage.tsx:630-720` — the library → builder path and
      `data-testid="builder-back"`
    - `frontend/src/pages/WorkflowBuilderPage.tsx:2655-2675` — the ▶ Test Run control and its gating
    - `backend/app/services/harness/phase_types.py:2430-2500` — GATE 6, the `tool_refused` metadata
      shape, and `_write_send_receipt`'s arguments
  </read_first>

  <what-built>
    Waves 1-3 plus task 1 of this plan: the publish validator admits an MCP step's own tool name; the
    picker has an MCP shape with an unfiltered read and two filters; the section offers a shape choice
    that clears the other shape's keys; the grant control writes the full merged map for an org admin
    and renders nothing for an unknown caller.
  </what-built>

  <how-to-verify>
    ⚠ **THE OPERATOR IS AWAY AND WILL NOT BE TYPING "approved". A checkpoint that waits for a human
    is, in this run, a checkpoint that never returns** — and this one gates the only evidence for the
    phase's headline criteria.

    > **CONDITIONAL ESCAPE (the 206.1 precedent):** if every acceptance criterion below is objectively
    > met by the recorded measurements, record this checkpoint as `auto-approved-with-evidence`,
    > attach the raw readings, and continue. **Escalate and STOP only when a measurement actually
    > FAILS, is MISSING, or a non-vacuity control did not hold — a green run is not one of those
    > cases.**

    **0.** `git rev-parse HEAD`; `git status --short -- frontend/src backend/app`. Assert the dev
    server serves HEAD — check for a marker string in the served bundle, or start a fresh Vite and
    **record the port**. ⚠ 206.1's executor found `5173`/`5174` bound to an IPv6 `localhost` its shell
    could not see and ended up on `5175`; a browser reading against a stale bundle proves nothing.

    **1.** Mint a LOCAL-ONLY session for the org admin (`fhdmrd@gmail.com`) via Supabase admin
    `generate_link` + verify, **behind a guard that REFUSES any non-loopback `SUPABASE_URL`**.
    ⚠ The org-admin identity matters TWICE here — `/discover` AND `/grants` both require `org:manage`.

    **2.** `page.goto("http://localhost:<port>/")`; wait for the chat home.

    **3.** Click `[aria-label="Workflows"]`. ⚠ **206.1's `localStorage` trick does NOT transfer** —
    there is no router and `activeView` is unpersisted state, so the rail must be CLICKED.

    **4.** Open the "Northwind QBR" draft's Edit path → `data-testid="builder-back"` is present.

    **5.** Add a step of type `external_action` (the type sentence is *"Reach outside"*).
    ⚠ Radix menus open on `pointerdown`; use real pointer clicks, not `node.click()`.

    **6.** Select the step → the panel's card-outside section renders.

    **7.** Press the MCP shape segment (`external-action-shape-option`, the second one). Record
    `data-shape` on `[data-section="external-action"]`.

    **8.** Read `connection-picker-select`. It **MUST list `DeepWiki (206.1 UAT)`** and **MUST NOT list
    `Slack-rag-test`** — the negative half of D-206.2-03, in the SAME reading.

    **9.** Select it. `data-testid="mcp-tool-picker"` mounts. **Record the footer's text** — it must
    name `mcp.deepwiki.com` and must NOT contain `slack.com/api` (D-206.2-20, driven).

    **10.** Click `data-testid="mcp-discover-btn"` — optional for the tool list (3 are cached) but it
    is the one reading that proves the ORG-ADMIN discover path. Record the result.

    **11.** Select tool **`read_wiki_structure`** in `mcp-tool-select`. ⚠ Pick the SIMPLEST tool: one
    required string arg. `ask_question` needs two and costs an LLM call on the remote server.

    **12.** ⚠ **DRIVE THE NEGATIVE FIRST (D-206.2-06).** With the grant still OFF, type the args
    `{ "repoName": "facebook/react" }`, then ▶ Test Run. Then read:
    `select event_type, metadata from harness_audit where event_type='tool_refused' and
    metadata->>'connection_id' = '7ca5e114-c5cd-4f7f-8c08-8885278bf42d' order by created_at desc limit 3`
    ⚠ **`tool_refused` is a SHARED `event_type`** — the agent tool-whitelist path already writes four
    rows locally with `{"tool":…, "allowed":[…]}`. **Filter on `connection_id` or
    `metadata->>'reason' = 'permission_denied'`, NEVER on the name alone.** Record the step's own text
    too — it begins `Tool execution refused:`.

    **13.** Flip the grant ON with `mcp-grant-toggle`. Capture the **network PATCH body** and assert it
    is the FULL MERGED MAP (for this fixture `tool_grants` starts `{}`, so record that the base map was
    empty and the body carries exactly the one key — and say so, rather than claiming a merge was
    demonstrated). Then read
    `select tool_grants from connector_connections where id = '7ca5e114-…'` — it must read
    `{"read_wiki_structure": true}`. Record the `✎ Allowed` receipt and that the switch did **not**
    move before the response arrived.

    **14.** ▶ Test Run again (**NOT publish** — see below). Read:
    `select created_at, metadata from harness_audit where event_type='external_action_sent'
     order by created_at desc limit 1`
    Expected metadata: `{"capability":"mcp", "connection_id":"7ca5e114-…",
    "destination_host":"https://mcp.deepwiki.com/mcp", "raw_status":200, "phase":"<slug>", "phase_index":N}`.
    ⚠ **THE RECEIPT DOES NOT CARRY THE TOOL NAME.** Do not claim it does.
    ⚠ **A real remote call leaves the machine.** `https://mcp.deepwiki.com/mcp` answers unauthenticated
    and `read_wiki_structure` on a public repo is a READ. Say so in the summary.

    **15.** **FALSIFY ASSUMPTION A1 AT T4.** Read
    `select definition from workflow_definitions where …` for this draft and assert the MCP step's
    `config` carries **no `capability` key** and **no stale `connection_id`**. Then press the
    capability segment, let it autosave, re-read, and assert the config carries **no `tool_name` and no
    `tool_args`** — the direction D-206.2-17 names as the dangerous one.

    **16.** **THE PUBLISH PATH.** `206.2-01` fixed the validator, so attempt `POST /workflows/validate`
    (or the gauntlet) on a definition containing this step and record whether an `unregistered_tool`
    finding appears. ⚠ **State plainly which path was driven for SC#3 — Test Run, publish, or both.**
    A green ▶ Test Run must **never** be reported as *"MCP steps work end to end"*.

    **17.** **TEARDOWN through the UI where a UI path exists** (206.1's precedent). Revert the grant
    through the same control. **Record exactly what was left behind** — the fixture row itself stays,
    as 206.1 deliberately left it.
  </how-to-verify>

  <action>
    Execute every numbered step of `<how-to-verify>` in ONE Playwright session against real Chromium,
    recording evidence per step. ⚠ **Chrome DevTools MCP is NOT available here** (measured); Playwright
    + `chromium-1223` are, and 206.1's two executors set the precedent — a deviation in METHOD, not in
    evidence, provided the plan RECORDS which was used.

    Drive the DENY case (step 12) BEFORE flipping the grant (step 13). That order is D-206.2-06 and it
    is not interchangeable: once a grant is on, the refusal path can only be re-reached by turning it
    off again, and a refusal observed after a success is a weaker reading than one observed before.

    Take every reading from the SOURCE OF TRUTH rather than from the UI where both exist: the PATCH
    body from the network log, the grant from `connector_connections.tool_grants`, the receipts from
    `harness_audit`, the cleared keys from `workflow_definitions.definition`. ⚠ The UI agreeing with
    itself is not evidence.

    ⚠ **Filter the `tool_refused` read on `metadata->>'connection_id'` or
    `metadata->>'reason' = 'permission_denied'`, NEVER on `event_type` alone** — the agent
    tool-whitelist path already writes four rows locally under that same name with a different
    metadata shape.

    ⚠ **Use real pointer clicks for any menu interaction** — Radix `DropdownMenuTrigger` opens on
    `pointerdown` and a synthetic `node.click()` leaves menus shut (206.1's measured trap).

    Write every reading into the plan's SUMMARY verbatim, including anything that could NOT be driven,
    with its reason and its blocking id. **A scoreboard that lists only what passed is not a
    scoreboard.**
  <acceptance_criteria>
    - Every reading in steps 7-16 is recorded with its RAW output — DOM text, network body, SQL result
      — never a summary. **A row that lists only what passed is not a scoreboard**; a step that could
      not be driven is recorded ⛔ with its reason and its blocking id, never omitted.
    - Step 8 records BOTH halves: the MCP row present AND the Slack row absent.
    - Step 9's footer text is recorded verbatim and contains `mcp.deepwiki.com` and not `slack.com/api`.
    - Step 12 produces a `tool_refused` row **filtered by `connection_id` or `reason`**, and the query
      used is pasted in.
    - Step 13 records the PATCH body, the DB column afterwards, the `✎ Allowed` receipt, and the fact
      that the switch did not move before the response.
    - Step 14 produces an `external_action_sent` row whose `capability` is `"mcp"`, and the summary
      does **not** claim the receipt names the tool.
    - Step 15 records both directions of the clear, read from the SAVED definition.
    - Step 16 states which path SC#3 was driven through and what the other path's evidence is.
    - The browser tool actually used is named (**Playwright + Chromium**, per D-206.2-23 — Chrome
      DevTools MCP is not available), and the dev-server port is recorded.
    - Teardown is recorded, including anything deliberately left behind.
    - ⚠ **UAT scoreboard scope, stated rather than skipped:** this phase touches neither streaming, the
      agent loop, provider routing nor chat UI state, so the 4-axis cross-provider roster does **not**
      apply. Say so in the summary; do not silently omit it.
  </acceptance_criteria>

  <resume-signal>
    Type "approved" or describe issues. ⚠ If the conditional escape above applies, record
    `auto-approved-with-evidence` with the raw readings attached and continue to task 4.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Pay the hot-file ledger debt — six rows and six sections, SAME COMMIT — close 206.1's owed SC#1b, answer SEED-146, and re-derive every phase baseline</name>
  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md, .planning/ROADMAP.md, .planning/seeds/SEED-146-integration-capability-surface.md</files>

  <read_first>
    - `CLAUDE.md` § "Hot-file ledger" — the whole table, the RE-DERIVE recipe, ⚠ the six-digit-bucket
      subtraction rule, and ⚠ the SAME-COMMIT SYNC RULE
    - `CLAUDE.md` § "CLAUDE.md context budget (MANDATORY)" — the 150,000 hard limit, the 120,000 warn
      band, **split by FUNCTION never by deletion**, and *the table stays complete and only prose
      leaves*
    - ⭐ commit `5343b1e8` (`git show --stat 5343b1e8`, then the `CLAUDE.md` and
      `docs/HOT-FILE-LEDGER.md` halves) — five rows and five sections in one commit; copy the SHAPE
    - `docs/HOT-FILE-LEDGER.md` — the existing sections for `PhaseFormPanel.tsx`,
      `backend/app/services/harness/grounding.py`, `frontend/src/components/settings/connectionsCopy.ts`
      and `scripts/vitest-count-gate.cjs`, so the corrections land beside their originals
    - `.planning/ROADMAP.md:41` (the 206.1 progress cell), `:96`, `:100` (⚠ the OWED SC#1b entry) and
      `:101` (the 206.2 entry), plus `:553-601` (Phase 206.2's own section and its six success criteria)
    - `.planning/seeds/SEED-146-integration-capability-surface.md` — its frontmatter, which the
      register scan reads
    - `.planning/reported-bugs/` — the mandatory cross-check
  </read_first>

  <action>
    **RE-DERIVE EVERY TRIPLE AT THIS COMMIT**, with CLAUDE.md's own recipe and the six-digit-bucket
    subtraction. ⚠ **Do not quote the base figures in this plan's `<interfaces>` table and do not
    increment a cell by hand** — waves 1-3 moved at least four of them, and this ledger's own repeated
    finding is that a figure written at a phase's close goes stale on the next commit, sometimes the
    same afternoon.

    Author SIX rows in `CLAUDE.md`, each with its anchor link, and SIX matching sections in
    `docs/HOT-FILE-LEDGER.md` in the SAME COMMIT:

    - `frontend/src/components/workflows/phaseVocabulary.ts` — ⚠ **FIRES at six+ phases and has NEVER
      been in the table**, named only inside `WorkflowCanvas.tsx`'s cell. This is the `config.py` /
      D-22 headline again. ⚠ **It was NOT edited by this phase** — the row exists because the file was
      NAMED in the blast radius and MEASURED FIRING, which is the only way a row like this ever gets
      written. Its invariants: `EXTERNAL_CAPABILITY_SENTENCES` has exactly three keys and is a
      cross-language fence with six backend counterparts, two of which are module-scope `assert`s that
      fail at IMPORT; `derivedFace` reads it through `hasOwnProperty.call`, never a coalesced bracket
      read. Propose a seam or record honoured-by-construction — do not leave the cell verdict-less.
    - `frontend/src/components/workflows/ConnectionPicker.tsx` — ⚠ absent from BOTH and it **crossed
      the threshold in this phase's own commit** (the `FlowEdge.tsx` / `phaseStatusMeta.ts` state, the
      one where a missing row costs most). Invariants: `bind()` writes EXACTLY `["connection_id"]`,
      swept over every driven call and OBSERVED RED against a planted write; the guard is on the
      EFFECT, not just the write, so a provider-less render opens no request; every arm of
      `destinationPartsOf` NAMES its own condition and the tail is neutral; the `is_enabled` filter
      applies to BOTH shapes.
    - `frontend/src/components/workflows/ExternalActionSection.tsx` — ⚠ absent from BOTH, **zero
      mentions anywhere**. Invariants: exactly THREE `external-action-option` / `role="radio"` children
      in the capability group, pinned at five sites; the source authors NO sentence and names only
      imported identifiers; no `useContext|useStore|useEffect|zustand`, no `title=`; the multi-key
      shape patch is the ONE place a config key is cleared, and it clears `{tool_name, tool_args}` as a
      SET plus `connection_id`; `key={phase.slug}` is what stops a pressed segment leaking across steps.
    - `frontend/src/components/workflows/McpToolPicker.tsx` — ⚠ **named inside Phase 206's SECTION with
      no ROW of its own**, the `useTemplateFirstDraft.ts` drift the same-commit rule forbids.
      Invariants: `isToolGranted` is the ONE grant predicate and mirrors the server's
      `grants.get(t) is True`, ⚠ though the client accepts any truthy value and the server requires
      `is True` — reachable only through a hand-edited row, because the service sanitizes on write;
      the grant write sends the FULL MERGED MAP from the SERVER-OWNED prop and never a
      `{[tool]: next}`; **no optimistic flip**; the three audience arms, with `unknown` asserting
      NOTHING so a provider-less render adds no node.
    - `frontend/src/components/workflows/externalShapeVocabulary.ts` — **listed BELOW the threshold ON
      PURPOSE** (the `fileIcon.tsx` / `connectionMark.tsx` precedent), because *a file escapes G-5 for
      years purely by not being written down*: `WorkflowsPage.tsx` for ten phases, `config.py` for its
      entire life. Invariants: a TRUE leaf that imports nothing and exports no component; declaration
      order is load-bearing (a TDZ `ReferenceError` no type checker reports); it spells no capability
      id.
    - `frontend/src/components/workflows/McpToolPicker.reachability.test.tsx` — the two-legged guard,
      listed below the threshold on purpose. Invariant: ⚠ **leg (a) alone is VACUOUS and was green at
      this phase's base against the defect**; leg (b) constructs NO component prop and its headline is
      a zero-argument call assertion. A future editor who deletes leg (b) has deleted the guard.

    Correct FOUR existing rows by re-derivation, never by increment:
    `frontend/src/components/workflows/PhaseFormPanel.tsx` (⚠ reads `24 / 11 / 1375`, **stale by three
    commits and a whole phase — the missing one is 205** — and this phase moved it again);
    `backend/app/services/harness/grounding.py` (was CURRENT at this phase's base and wave 1 moved it —
    say that, because *a cell measured and confirmed is evidence, and one nobody re-derived is not, and
    the two look identical*); `frontend/src/components/settings/connectionsCopy.ts` (wave 2 moved it);
    `scripts/vitest-count-gate.cjs` (⚠ reads `114 / 19 / 3797`, already stale at base).

    **DECLINE a row for `frontend/src/components/workflows/stepReadinessContext.ts` IN WRITING**
    (measured 2/1/183 at base, below threshold and young), with the re-open trigger *the first phase
    that takes it to three*. A recorded decline is a decision; silence is a deletion.

    Run `node scripts/check-claude-md-size.cjs`, **record the new figure verbatim**, and **ESCALATE THE
    SPLIT** — the gate names its own target (`Workflow guardrails (MANDATORY)`, 76,531 chars, 62% of
    the file). ⚠ **Do NOT shave a cell to make the number smaller.** The split is a phase, not a task;
    propose it as a ROADMAP entry.

    Update `.planning/ROADMAP.md` **in the same commit as task 3's drive**: mark 206.1's owed SC#1b
    closed at `:100`, update the 206.1 progress cell at `:41`, and fill in Phase 206.2's own row and
    checklist entry with what was actually driven. ⚠ **State which path SC#3 was driven through**
    (Test Run and/or publish) rather than letting `driven` imply both.

    Answer **SEED-146** by editing the seed itself: record that D-206.2-05's reading (*the per-tool
    grant IS the approval model for this surface*) was taken, where the answer is written in source,
    and that this phase added no new outbound capability. ⚠ **A seed answered only in a plan is
    invisible to the register scan — `status:` frontmatter IS the index.** Do not flip `status` to
    closed: SEED-146 is an umbrella and only this clause is answered; add the answer and leave the
    umbrella planted, with the narrower clause marked.

    Run the reported-bugs cross-check: list `.planning/reported-bugs/*.md` with `status: open` AND
    `surface: Agentic-RAG`, and confirm none carries `folded_into: 206.2`. ⚠ Measured at plan time:
    **none does**, so the obligation is vacuously satisfied — **record that it was RUN**, because a
    check nobody records is indistinguishable from one nobody ran. Note
    `BUG-260810-01-cloud-settings-connections-no-add-button` as adjacent (settings/connections) and
    explicitly NOT folded: it is cloud-parity, not the builder surface.

    Re-derive all four phase baselines and record them VERBATIM: backend
    `venv/Scripts/python -m pytest tests/unit -q`; `npx tsc --noEmit -p tsconfig.app.json`;
    `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root;
    `node scripts/check-claude-md-size.cjs`.
  </action>

  <verify>
    <automated>node scripts/check-claude-md-size.cjs; GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - Six new rows in `CLAUDE.md` and six new `## \`path\`` sections in `docs/HOT-FILE-LEDGER.md`, in
      **ONE commit** — `git show --stat HEAD` names both files. Every row carries an anchor link that
      resolves to its section (verify each anchor by grepping the heading).
    - Every triple in every new or corrected row was produced by running the recipe **at this commit**,
      and the SUMMARY pastes the command output. `PhaseFormPanel.tsx`'s row no longer reads
      `24 / 11 / 1375`; `scripts/vitest-count-gate.cjs`'s no longer reads `114 / 19 / 3797`.
    - Each section ends with `### Invariants a future editor is bound by` and each bullet states a rule
      **and the failure it prevents**.
    - The `stepReadinessContext.ts` decline is written down with its trigger.
    - `node scripts/check-claude-md-size.cjs` output is recorded verbatim, including the new char
      count and the warn/OK verdict, and the SPLIT is escalated as a proposed ROADMAP entry rather
      than performed here.
    - `.planning/ROADMAP.md` marks 206.1's SC#1b closed **in the same commit as the drive's evidence**,
      and Phase 206.2's row states which path SC#3 was driven through.
    - `.planning/seeds/SEED-146-integration-capability-surface.md` carries the D-206.2-05 answer and
      its `status:` reflects the routing.
    - The reported-bugs scan is recorded as RUN, with its result.
    - All four baselines are recorded verbatim, at this commit, against the figures re-derived at each
      wave's own base. Contract: backend `failed` unchanged; tsc unchanged with zero in any touched
      file; gate `failed 0` with no per-file DECREASE.
  </acceptance_criteria>

  <done>
    Every file this phase touched or named is in CLAUDE.md's audit scan list with a matching section
    from the same commit; four stale cells are corrected by re-derivation; one absence is declined in
    writing; CLAUDE.md's growth is measured and its split escalated; 206.1's owed criterion is marked
    where it was owed; SEED-146 carries its answer in the register the scan actually reads.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any org member → `GET /connectors/connections` | ungated org-wide read; `discovered_tools` and `tool_grants` are readable by a member |
| org admin → `POST /discover` and `PATCH /grants` | `require_org_manage` (403) **plus** an RLS policy in both `USING` and `WITH CHECK` — two independent layers, neither of which is the UI's job |
| the browser → `connector_connections.tool_grants` | a whole-column REPLACE with no version column |
| remote MCP server → `discovered_tools` → the DOM | remote-authored text rendered as UI |
| the harness executor → the remote MCP server | real egress; GATE 6 decides whether it happens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-206.2-04-T1 | Tampering | the grant toggle's payload — lost update | mitigate | Send `{...(connection.tool_grants ?? {}), [tool]: next}` from the **SERVER-OWNED PROP**, `if (busy) return`, no optimistic flip, and adopt the response row upstream so the next toggle merges from a fresh map. Asserted by SET EQUALITY on the mock's payload against a connection seeded with an unrelated grant, and by `206.2-01`'s backend REPLACE case. ⚠ Two admins in two browsers can still clobber each other; the honest mitigation is `MCP_GRANT_SCOPE_NOTE`, **not a lock** — a version column is a migration this phase forbids. |
| T-206.2-04-E1 | Elevation of privilege | a member reaching an org-admin write | mitigate (render) / accept (enforcement) | The UI REMOVES both write affordances for a member and renders nothing for an unknown caller. ⚠ **The wall is `require_org_manage` plus RLS; the UI's job is to be honest, never to be the gate.** No test simulates either layer. ⚠ A rendered control the API refuses is the defect the Settings-side rule exists to prevent — and a member must never see a Discover button that 403s. |
| T-206.2-04-E2 | Elevation of privilege | a workflow calling a tool nobody approved | mitigate | GATE 6 is `grants.get(tool_name) is True` — **a missing key DENIES** — with a `tool_refused` receipt. The UI never re-derives it and does not fork `isToolGranted`. **The deny path is DRIVEN FIRST in task 3**, before the grant is flipped, so the refusal is measured rather than assumed. |
| T-206.2-04-I1 | Information disclosure | a credential reaching the browser | mitigate (inherited) | `ConnectorConnection` declares no secret field, fenced by a module-scope assert; the credential is `enc:v1:`-enveloped and write-only at the API boundary. This plan adds no new field to that type. |
| T-206.2-04-I2 | Information disclosure | an existence oracle over another org's ids | accept (inherited) | The grant write is scoped by `id` AND `org_id` server-side (asserted in `206.2-01`); a wrong-org id returns `ConnectorNotFound`, which is the same answer as a deleted row. |
| T-206.2-04-T2 | Tampering / XSS | remote tool names and descriptions rendered as UI | mitigate | Rendered as escaped React children in `<option>` and `<p>`. ⚠ **Never `dangerouslySetInnerHTML` for a tool name or description** — T-206-03. This plan adds no new render of remote text beyond the existing ones. |
| T-206.2-04-R1 | Repudiation | a grant change with no receipt | mitigate | Two directions, two `✎` receipts (`MCP_GRANT_RECEIPT_ALLOWED` / `MCP_GRANT_RECEIPT_DENIED`) — a single "Saved" would hide which way it went. The server-side audit trail for the RUN is `tool_refused` / `external_action_sent`, both read in task 3. ⚠ `tool_refused` is a SHARED `event_type`; the query filters on `connection_id` / `reason`. |
| T-206.2-04-D1 | Denial of service | a real remote call from a test run | accept, disclosed | Task 3 makes ONE call to `https://mcp.deepwiki.com/mcp` with `read_wiki_structure` on a public repo — a READ, unauthenticated, against the fixture 206.1 deliberately left in place. **Said out loud in the summary**, not glossed. |
| T-206.2-04-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed and no `npx shadcn add` runs** — the switch is a native `<button role="switch">` because `src/components/ui/switch.tsx` does not exist and vendoring one would put a registry block into a phase that otherwise installs nothing. `frontend/package.json` and `package-lock.json` are asserted unmodified. The Package Legitimacy Gate does not fire; a proposed dependency is a scope deviation and the gate runs first. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — verdict line
  VERBATIM at this plan's base and close. `failed 0`, no per-file DECREASE, pinned FILE count `+2`.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — unchanged from this plan's base, zero in any
  touched file.
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` — `failed` unchanged from wave 1's close.
- `node scripts/check-claude-md-size.cjs` — exit 0 (WARNINGS acceptable, over-limit is not), figure
  recorded.
- `git diff --numstat -- frontend/src/lib/api.ts | wc -l` == 0.
- `git diff --numstat -- supabase/migrations | wc -l` == 0.
- `git diff --numstat -- frontend/package.json frontend/package-lock.json | wc -l` == 0.
- `grep -rn "updateConnectorGrants" frontend/src | grep -v "\.test\." | grep -v "lib/api.ts" | wc -l`
  >= 1 — SC#2's caller census, where it was `0` at this phase's base.
</verification>

<success_criteria>
- **SC#1** — an `external_action` step is pointed at an MCP connection **in a real browser**, and the
  picker lists the MCP row and not the Slack one, with both halves recorded.
- **SC#2** — the server's tools are discovered and **at least one grant is flipped through the UI**;
  `updateConnectorGrants` has its first production caller; the PATCH body and the DB column afterwards
  are both recorded. **SC#2b** — with the grant OFF, the run is refused and writes a `tool_refused`
  row filtered by `connection_id` / `reason`.
- **SC#3** — the run calls the granted tool and records an `external_action_sent` receipt whose
  `capability` is `"mcp"`. **The path driven is named**, and a green ▶ Test Run is never reported as
  end-to-end on its own.
- **SC#4** — the reachability guard has TWO legs; leg (b) constructs no component prop, its headline is
  a zero-argument call assertion, and it was **observed RED** against the pre-phase data path. Leg (a)
  says in its own source that it was green at the base.
- **SC#5** — the three shipped capability shapes are untouched (carried by waves 2-3 and re-verified by
  the gate here); `MCP_GRANT_GRANTED_LABEL` / `MCP_GRANT_DENIED_LABEL` cases pass UNEDITED.
- **SC#6** — all four baselines re-derived at this commit and recorded verbatim; `McpToolPicker.test.tsx`
  is PINNED where it had never been.
- 206.1's owed SC#1b is marked closed in the same commit as the evidence that closed it; six hot-file
  rows land with six sections; CLAUDE.md's new size is recorded and its split escalated.
</success_criteria>

<output>
Create `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-04-SUMMARY.md` when
done. Record: the leg-(b) RED output and its restoration proof; the raw browser and SQL readings from
task 3, including the DENY case and the two clear directions; the PATCH body and the `tool_grants`
column before and after; which path SC#3 was driven through; every re-derived triple with the command
that produced it; the new CLAUDE.md char count; the two deliberate non-changes flagged for UAT by
`206.2-03` (the MCP step's canvas face and its effect banner); and the statement that the 4-axis
cross-provider UAT roster does not apply to this phase, with the reason.
</output>
