---
phase: 142-non-python-skill-script-honesty-stretch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/openai_service.py
  - backend/tests/unit/test_sandbox_tools.py
autonomous: true
requirements: [SRH-01]

must_haves:
  truths:
    - "D-05 / D-05a / D-04: `EXECUTE_CODE_TOOL.description` states the sandbox is Python-only, lists the available pre-installed pip set, names the NOT-available tokens (node/npm/npx, soffice/libreoffice, pandoc, pdftoppm, markitdown, scripts/office/*), and instructs the model NOT to shell out to them (do the equivalent in-memory or tell the user) — the proactive layer (D-05 tool-DESCRIPTION home) that prevents the first dead call"
    - "D-14 red line: the capability facts live on the tool CONTRACT string (single source `get_tools`, translated uniformly by every provider gateway), NOT on SYSTEM_PROMPT — Deep Mode stays byte-identical and this is a uniform additive change across every provider, not a fork"
    - "D-02: no capability is added — the description only DECLARES the existing Python-only reality; sandbox_service.py:41 and Dockerfile.sandbox are untouched"
  artifacts:
    - path: "backend/app/services/openai_service.py"
      provides: "EXECUTE_CODE_TOOL.description extended with the generic capability facts"
      contains: "NOT available"
    - path: "backend/tests/unit/test_sandbox_tools.py"
      provides: "assertion that the description carries the available + NOT-available tokens"
      contains: "test_execute_code_capability_facts"
  key_links:
    - from: "EXECUTE_CODE_TOOL.description"
      to: "get_tools() single source → all provider gateways"
      via: "uniform tool-schema translation (openai/anthropic/google)"
      pattern: "EXECUTE_CODE_TOOL"
---

<objective>
Add the proactive half of the honesty gate (D-04/D-05a): extend the
`EXECUTE_CODE_TOOL` description so the model knows, BEFORE it plans a call, that
this sandbox runs Python only — listing what IS pre-installed and, critically,
what is NOT available and cannot be installed (Node/npm, LibreOffice `soffice`,
pandoc, Poppler `pdftoppm`, `markitdown`, and bundled `scripts/office/*` helpers)
— and telling it to do the equivalent work in-memory or tell the user rather than
shell out to those. This is the layer that prevents the first dead call; Plan 02's
reactive guard backstops what slips through.

Purpose: the capability fact belongs on the tool contract (single source for all
providers), NOT on the shared system prompt — this keeps the D-14 byte-identical
Deep invariant intact while reaching every provider uniformly.
Output: the extended description + a unit assertion in `test_sandbox_tools.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/142-non-python-skill-script-honesty-stretch/142-CONTEXT.md
@.planning/phases/142-non-python-skill-script-honesty-stretch/142-RESEARCH.md
@.planning/phases/142-non-python-skill-script-honesty-stretch/142-PATTERNS.md
@.planning/phases/142-non-python-skill-script-honesty-stretch/142-VALIDATION.md
@backend/app/services/openai_service.py
@backend/tests/unit/test_sandbox_tools.py

<interfaces>
<!-- The single-source tool schema to extend. READ these lines. -->

EXECUTE_CODE_TOOL (openai_service.py:581, description at :585-596) — extend THIS description string:
```python
EXECUTE_CODE_TOOL = {
  "type": "function",
  "function": {
    "name": "execute_code",
    "description": ("Execute Python code in a sandboxed Docker container. IMPORTANT: The sandbox "
        "has only the Python standard library pre-installed. You MUST pass the `libraries` "
        "parameter for every third-party package ..."),
    "parameters": {...},
  },
}
```

Single-source proof (D-14-safe): get_tools() appends EXECUTE_CODE_TOOL once (openai_service.py:1043);
the Anthropic (provider_gateway/anthropic.py:88) and Google (google.py:71) gateways convert the SAME
list. Editing the description = one uniform additive change across all providers, NOT a fork. It is NOT
SYSTEM_PROMPT (agent_loop.py:567), which is what the D-14 byte-identical invariant guards.
</interfaces>

<authored_content>
<!-- Exact sentence(s) to append to the description (reproduce verbatim; tone modeled on
     BUG-260707-02-pptx-skill-instructions-sandbox-aware.md). -->
```
This sandbox runs Python only. Pre-installed: python-pptx, matplotlib, numpy, pandas, openpyxl,
python-docx, pypdf, reportlab, docxtpl, seaborn, scipy, scikit-learn, plotly (install other PyPI
packages via `libraries`). NOT available and cannot be installed here: Node/npm/npx, LibreOffice
(soffice), pandoc, Poppler (pdftoppm), markitdown, and any bundled scripts/office/* helpers. If a
skill's instructions tell you to shell out to those, do NOT — they will fail; do the equivalent work
in-memory with the Python libraries above, or tell the user it is not available.
```
</authored_content>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert the capability facts in test_sandbox_tools.py</name>
  <files>backend/tests/unit/test_sandbox_tools.py</files>
  <read_first>
    - backend/tests/unit/test_sandbox_tools.py (existing tool-schema assertion conventions — how EXECUTE_CODE_TOOL / get_tools are imported and asserted)
    - backend/app/services/openai_service.py:581-596 (the current description)
    - .planning/phases/142-non-python-skill-script-honesty-stretch/142-RESEARCH.md §"Proactive Wording + Placement" (a)
  </read_first>
  <action>
    Add `test_execute_code_capability_facts` to `test_sandbox_tools.py`: import `EXECUTE_CODE_TOOL`
    from `app.services.openai_service`, read `EXECUTE_CODE_TOOL["function"]["description"]`
    (lower-cased), and assert it contains the NOT-available tokens `soffice`, `pandoc`,
    `pdftoppm`, `markitdown`, and `node` (or `npm`), AND at least one available library name
    (e.g. `python-pptx`), AND a do-not-instruction phrase (`do not` / `will fail`). Keep it red
    until Task 2. Do NOT assert against SYSTEM_PROMPT — the facts must NOT be there.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_sandbox_tools.py::test_execute_code_capability_facts -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The test exists and fails (RED) before Task 2.
    - It asserts NOT-available tokens + an available library + a "do not/will fail" phrase.
  </acceptance_criteria>
  <done>test_execute_code_capability_facts present; red pending the description edit.</done>
</task>

<task type="auto">
  <name>Task 2: Append the capability facts to EXECUTE_CODE_TOOL.description</name>
  <files>backend/app/services/openai_service.py</files>
  <read_first>
    - backend/app/services/openai_service.py:581-596 (the description string to extend) and :1043 (get_tools single source)
    - .planning/phases/142-non-python-skill-script-honesty-stretch/142-PATTERNS.md §"openai_service.py → EXECUTE_CODE_TOOL.description" (why single-source = D-14-safe)
    - The `<authored_content>` sentence(s) above (reproduce verbatim)
  </read_first>
  <action>
    Append the authored capability-facts sentence(s) to the existing `EXECUTE_CODE_TOOL`
    `description` string (~:585-596) — concatenate onto the current string, do NOT create a new
    field. Make NO change to `SYSTEM_PROMPT` (`agent_loop.py:567`) and NO change to
    `sandbox_service.py:41` / `Dockerfile.sandbox` (D-02/D-05a — declare, don't add capability).
    Because `get_tools()` is the single source translated by every gateway, this one edit reaches
    OpenAI, Anthropic, and Google uniformly (D-14-safe).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_sandbox_tools.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_execute_code_capability_facts` passes GREEN; the rest of `test_sandbox_tools.py` stays green.
    - `git diff` touches ONLY `openai_service.py` (description) + the test — NOT `agent_loop.py` SYSTEM_PROMPT, `sandbox_service.py`, or `Dockerfile.sandbox`.
  </acceptance_criteria>
  <done>The tool description carries the generic capability facts across all providers; system prompt + capability files untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool schema string → all provider gateways | A static, developer-authored contract string; no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-D14 | Tampering (shared-path fork) | capability facts placement | mitigate | Facts go on the single-source `EXECUTE_CODE_TOOL` contract (uniformly translated by every gateway), explicitly NOT on `SYSTEM_PROMPT`. Verified by the diff touching only the tool description; the D-14 byte-identical Deep invariant is preserved. |

No untrusted input, no new read/write authority, no package-manager install → no T-142-SC checkpoint; this plan carries no other STRIDE-relevant surface.
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit/test_sandbox_tools.py -q` → green.
- `git diff --name-only` = `openai_service.py` + `test_sandbox_tools.py` only.
</verification>

<success_criteria>
- D-05a/D-04: the model is proactively told the sandbox is Python-only with the exact NOT-available tokens and a forward instruction — preventing the first dead call.
- D-14: the fact lives on the tool contract (single source, all providers), not the system prompt.
- D-02: declaration only; no capability added.
</success_criteria>

<output>
Create `.planning/phases/142-non-python-skill-script-honesty-stretch/142-04-SUMMARY.md` when done.
</output>
