---
phase: 095-chat-tool-card-unification
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - frontend/src/components/chat/MessageList.tsx
  - frontend/src/hooks/useFollowScroll.ts
  - frontend/src/hooks/__tests__/useFollowScroll.test.ts
  - frontend/__tests__/components/chat/MessageList.test.tsx
autonomous: true
requirements: [CHAT-04]

must_haves:
  truths:
    - "While the user is at/near the bottom, the chat follows the live edge during streaming; the instant they scroll UP, auto-follow releases and leaves them in place; it re-arms when they return to the bottom (D-03, BUG-260529-02 #1)."
    - "A '↓ Jump to live' affordance appears whenever the user has scrolled away during a live run, and clicking it re-pins + scrolls to the live edge."
    - "Programmatic scrolls (the auto-follow itself) do NOT trip the release — only user-initiated scroll-up releases the pin."
  artifacts:
    - path: "frontend/src/hooks/useFollowScroll.ts"
      provides: "the follow/release/re-arm state machine (isPinned, isNearBottom, showJumpToLive) + jumpToLive()"
      contains: "export function useFollowScroll"
    - path: "frontend/src/components/chat/MessageList.tsx"
      provides: "useFollowScroll wired over the Radix viewport + the floating JumpToLive chip (reuses RunStatusStrip placement=floating)"
      contains: "useFollowScroll"
  key_links:
    - from: "frontend/src/components/chat/MessageList.tsx"
      to: "frontend/src/hooks/useFollowScroll.ts"
      via: "useFollowScroll(viewportRef, isStreaming)"
      pattern: "useFollowScroll"
    - from: "frontend/src/components/chat/MessageList.tsx JumpToLive chip"
      to: "frontend/src/components/chat/RunStatusStrip.tsx placement=floating"
      via: "the floating live-chip reuses RunStatusStrip (Plan 02) — no second strip"
      pattern: "RunStatusStrip"
---

<objective>
Build the D-03 follow-but-release scroll discipline — the one genuinely-new behavior on the chat scroll container — and the "↓ Jump to live" affordance, reusing the floating placement of the `RunStatusStrip` from Plan 02 (no second strip).

The gap (RESEARCH): MessageList today auto-scrolls only on new-message or streaming+near-bottom (`isNearBottomRef < 120`), with NO explicit release-on-scroll-up / re-arm-at-bottom state machine and NO Jump-to-live affordance. This plan adds the `useFollowScroll` hook (the state machine) and wires it + the chip over the existing Radix ScrollArea viewport, extending — never replacing — the BL-05 `scrollListenerAttachedRef` guard and the 083-safe scroll handling.

Purpose: SKETCH-CONSISTENCY §1 (`useFollowScroll` + `JumpToLive` NEW; the chip reuses the one RunStatusStrip floating wrapper) + sketch 015 winner C (hybrid: header strip while in view + bottom Jump-to-live chip on scroll-away).
Output: useFollowScroll hook + test; MessageList wires it + the floating chip; MessageList test extended.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/095-chat-tool-card-unification/095-RESEARCH.md
@.planning/phases/095-chat-tool-card-unification/095-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md

<interfaces>
<!-- From Plan 02 -->
```tsx
export function RunStatusStrip(props: { elapsedLabel: string; stepCount: number; activityVerb: string | null; placement: "header" | "floating" }): JSX.Element
```

<!-- Current MessageList scroll (MessageList.tsx:21-99) — EXTEND, do not replace -->
```tsx
const isNearBottomRef = useRef(true)
const scrollListenerAttachedRef = useRef(false)   // BL-05 guard — keep
const handleScroll = () => {
  const el = containerRef.current?.closest("[data-radix-scroll-area-viewport]") as HTMLElement | null
  if (!el) return
  const distFromBottom = el.scrollHeight - el.scrollTop - el.clientHeight
  isNearBottomRef.current = distFromBottom < 120          // near-bottom heuristic — reuse
}
// useLayoutEffect attaches handleScroll to the Radix viewport (45-68) — extend it
// auto-scroll effect (70-99): on newCount>prev OR (isStreaming && isNearBottom),
//   prefers the [data-tool-status="preparing"] element, else bottomRef.scrollIntoView
//   ({behavior: isStreaming ? "instant" : "smooth"})  — keep the active-preparing priority for Focus Mode
```

<!-- Sketch follow-scroll skeleton (chat-tool-card-unification.md §"Smart follow-scroll"):
     nearBottom(vp) = scrollHeight - scrollTop - clientHeight < 56  (D-03 threshold; reuse the existing 120 or align — Claude's discretion, document the choice)
     following = nearBottom on scroll; autoFollow() scrolls only if following; jumpToLive() sets following=true + scrolls -->
```

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useFollowScroll state machine (follow / release / re-arm / jump)</name>
  <files>frontend/src/hooks/useFollowScroll.ts, frontend/src/hooks/__tests__/useFollowScroll.test.ts</files>
  <read_first>
    - frontend/src/components/chat/MessageList.tsx:21-99 (the full current scroll model: isNearBottomRef, the BL-05 scrollListenerAttachedRef guard 25-30/45-68, the auto-scroll effect 70-99 with the active-preparing priority)
    - 095-RESEARCH.md §"D-03 — Follow-but-release scroll" (the hook shape: isPinned / isNearBottom / showJumpToLive = !isPinned && isStreaming; release-on-scroll-up distinguished from programmatic via an isProgrammaticScrollRef set before each scrollIntoView and cleared next frame; re-arm at bottom; reuse the BL-05 guard, don't replace)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md §"Smart follow-scroll (D-03)" + §B (hybrid placement)
    - 095-VALIDATION.md → D-03 follow-scroll row
  </read_first>
  <behavior>
    - Initial: isPinned = true, showJumpToLive = false.
    - User scrolls UP (distFromBottom > threshold, NOT programmatic) → isPinned = false; while isStreaming, showJumpToLive = true.
    - User scrolls back to bottom (distFromBottom < threshold) → isPinned = true; showJumpToLive = false.
    - A programmatic scroll (isProgrammaticScrollRef set) does NOT flip isPinned to false.
    - jumpToLive() → isPinned = true, scrolls to the live edge, showJumpToLive = false.
    - When NOT streaming, showJumpToLive is false even if not pinned (the chip is a live-run affordance).
  </behavior>
  <action>
    Create `frontend/src/hooks/useFollowScroll.ts`. Export `export function useFollowScroll(getViewport: () => HTMLElement | null, isStreaming: boolean)` returning `{ isPinned, showJumpToLive, jumpToLive, onScroll, beginProgrammaticScroll }`.

    Implement the state machine per RESEARCH §D-03:
    - `isPinnedRef` (default true) + `isProgrammaticScrollRef` + an `isNearBottom` derivation (`scrollHeight - scrollTop - clientHeight < THRESHOLD`; reuse the existing 120px or the sketch's 56 — pick one, document it; the existing 120 is safest to avoid changing felt behavior).
    - `onScroll()`: compute distFromBottom from the viewport; if `isProgrammaticScrollRef.current` is set, skip the release logic (it's our own scroll); else if distFromBottom > THRESHOLD → set isPinned=false; if distFromBottom < THRESHOLD → set isPinned=true.
    - `showJumpToLive` = `!isPinned && isStreaming`.
    - `beginProgrammaticScroll()`: set `isProgrammaticScrollRef = true`, clear it on the next `requestAnimationFrame` (so the programmatic scroll event doesn't trip release).
    - `jumpToLive()`: set isPinned=true; call beginProgrammaticScroll() then scroll the viewport to bottom.

    Use refs + a small piece of state for the values that drive render (isPinned/showJumpToLive must be reactive so the chip shows/hides). Pure hook, no DOM-coupling beyond `getViewport()`. Do NOT import RunStatusStrip here (the chip is composed in MessageList).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/__tests__/useFollowScroll.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useFollowScroll.ts` exists; `grep -c "export function useFollowScroll" useFollowScroll.ts` ≥ 1
    - `grep -E "isPinned|showJumpToLive|jumpToLive|Programmatic" frontend/src/hooks/useFollowScroll.ts` matches all four
    - NEW useFollowScroll.test.ts cases: scroll-up → isPinned=false + showJumpToLive=true (when streaming); scroll-to-bottom → isPinned=true + showJumpToLive=false; programmatic scroll does NOT release; jumpToLive() re-pins; not-streaming → showJumpToLive=false (095-VALIDATION.md D-03 pass conditions)
    - `cd frontend && npx vitest run src/hooks/__tests__/useFollowScroll.test.ts` exits 0
  </acceptance_criteria>
  <done>useFollowScroll exposes isPinned/showJumpToLive/jumpToLive/onScroll/beginProgrammaticScroll; the state-machine test proves follow/release/re-arm/jump + programmatic-immunity; per-file vitest exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useFollowScroll + the floating JumpToLive chip into MessageList</name>
  <files>frontend/src/components/chat/MessageList.tsx, frontend/__tests__/components/chat/MessageList.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageList.tsx (the whole file — 132 lines; the scroll model 21-99, the render 100-132)
    - frontend/src/hooks/useFollowScroll.ts (Task 1)
    - frontend/src/components/chat/RunStatusStrip.tsx (Plan 02 — the floating placement to reuse for the chip)
    - 095-RESEARCH.md §"D-03" (follow only while isPinned; reuse the bottomRef.scrollIntoView; keep the active-preparing-tool scroll-target priority for Focus Mode; extend the BL-05 guard)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md §"Status strip — one component, two homes" + §B + the ❌ anti-patterns ("Auto-scroll on every update"; "A bottom status chip that occludes the live stream while following"; "Caging the chat in a bordered fixed-height scroll box")
    - 095-VALIDATION.md → D-03 row
  </read_first>
  <action>
    Wire the hook over the existing Radix viewport, gating auto-follow on `isPinned`, and render the floating chip on scroll-away.

    (1) In `MessageList`, call `const { isPinned, showJumpToLive, jumpToLive, onScroll, beginProgrammaticScroll } = useFollowScroll(() => containerRef.current?.closest("[data-radix-scroll-area-viewport]") as HTMLElement | null, isStreaming)`. In the `useLayoutEffect` that attaches the listener (45-68), attach `onScroll` (replacing/augmenting the current `handleScroll`) — KEEP the BL-05 `scrollListenerAttachedRef` retry-on-rAF guard.

    (2) In the auto-scroll effect (70-99): gate the scroll on `isPinned` (only follow while pinned), and call `beginProgrammaticScroll()` immediately BEFORE each `scrollIntoView`/`scrollTop` write so the resulting scroll event doesn't trip release. Keep the active-`[data-tool-status="preparing"]` element priority (Focus Mode) and the `behavior: isStreaming ? "instant" : "smooth"` choice. Do NOT auto-scroll when `!isPinned` (this is the release).

    (3) Render the floating `JumpToLive` chip: when `showJumpToLive`, render `<RunStatusStrip placement="floating" ... />` (reusing Plan 02's component — NO second strip) wrapped as a clickable pill at the bottom of the viewport with a "↓ Jump to live" affordance; `onClick={jumpToLive}`. The chip appears ONLY on scroll-away (never occludes the live stream while following). Position it absolutely within the message-list viewport (the sketch `.live-chip`); reuse the allowed CSS (the `.live-chip`/`.done` modifier semantics map to Tailwind utility classes already in use; `animate-fadeSlideUp` for entry). The chip carries the run status (elapsed/step/activity) — derive these from the currently-streaming message if MessageList has it, OR pass minimal props (Claude's discretion: if MessageList lacks the streaming message's elapsed/step, render the chip with just the "↓ Jump to live" label + activity — the timer identity lives in the header strip per the hybrid). Document the choice.

    Do NOT cage the chat in a bordered fixed-height box (the seamless-scroll rule). Do NOT change the RunCard sticky header.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run __tests__/components/chat/MessageList.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useFollowScroll" frontend/src/components/chat/MessageList.tsx` ≥ 1
    - `grep -c "isPinned" frontend/src/components/chat/MessageList.tsx` ≥ 1 (auto-follow gated on it)
    - `grep -c "beginProgrammaticScroll" frontend/src/components/chat/MessageList.tsx` ≥ 1 (programmatic scrolls flagged before scrollIntoView)
    - `grep -c "RunStatusStrip\|Jump to live\|jumpToLive" frontend/src/components/chat/MessageList.tsx` ≥ 1 (the floating chip reuses RunStatusStrip; the jump affordance present)
    - `grep -c "scrollListenerAttachedRef" frontend/src/components/chat/MessageList.tsx` ≥ baseline (BL-05 guard preserved, not removed)
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/MessageList.tsx` == 0
    - EXTENDED MessageList.test.tsx: simulate scroll-up during streaming → the Jump-to-live chip renders; simulate scroll-to-bottom → chip hidden + follow re-armed (095-VALIDATION.md D-03 pass condition)
    - `cd frontend && npx vitest run __tests__/components/chat/MessageList.test.tsx` exits 0
  </acceptance_criteria>
  <done>MessageList wires useFollowScroll, gates auto-follow on isPinned, flags programmatic scrolls, and renders the floating JumpToLive chip via the one RunStatusStrip; BL-05 guard preserved; D-03 test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM scroll events → state | User scroll input drives the pin state machine; no untrusted data crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-04-01 | Tampering/XSS | JumpToLive chip / RunStatusStrip | mitigate | Chip renders controlled labels via RunStatusStrip (text children); NO `dangerouslySetInnerHTML`. Asserted by grep == 0. |
| T-095-04-02 | DoS (scroll-fight) | useFollowScroll / auto-scroll effect | mitigate | Programmatic scrolls are flagged (isProgrammaticScrollRef) so the auto-follow cannot fight a scrolled-up user; release-on-user-scroll-up is the explicit guard. Asserted by the programmatic-immunity test. |
| T-095-04-03 | PANEL-06 isolation breach | MessageList render | accept→assert | MessageList reads chat messages only; this plan adds no panel-store read. Asserted by grep. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/hooks/__tests__/useFollowScroll.test.ts __tests__/components/chat/MessageList.test.tsx` — green.
- `cd frontend && npm test` — full suite green (no regression to the existing MessageList scroll tests / BL-05).
- `cd frontend && npx tsc -b` — zero net-new type errors.
- PANEL-06: grep MessageList.tsx + useFollowScroll.ts add no read of todosByThread/tasksByThread/phasesByThread/workspaceFilesByThread/pendingAsksByThread.
</verification>

<success_criteria>
- Follow-the-edge while at bottom; release on user scroll-up; re-arm at bottom; programmatic scrolls don't release (D-03, BUG-260529-02 #1).
- "↓ Jump to live" chip appears on scroll-away during a live run and re-pins on click, reusing the ONE RunStatusStrip (no second strip).
- BL-05 guard + the seamless-scroll rule preserved; full suite green; PANEL-06 held.
</success_criteria>

<output>
After completion, create `.planning/phases/095-chat-tool-card-unification/095-04-SUMMARY.md`
</output>
