---
phase: 095-chat-tool-card-unification
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [CHAT-04]
files_modified:
  - frontend/src/components/chat/RunStatusStrip.tsx
  - frontend/src/components/chat/RunCard.tsx
  - frontend/src/components/chat/RunCard.test.tsx

must_haves:
  truths:
    - "The header status strip reads as a rounded-full pill (subtle bg + 1px border + divider bars), not bare middot text (GAP-095-03 MED chip chrome)."
    - "The activity verb renders exactly ONCE (in the strip), never duplicated in the title line (GAP-095-03 MED single verb)."
    - "A `model · turn` run-sub subline is restored under a calm run-identity title — no new backend field (GAP-095-03 MED run-sub)."
  artifacts:
    - path: "frontend/src/components/chat/RunStatusStrip.tsx"
      provides: "header placement gets pill chrome (bg + border + rounded-full + divider bars)"
      contains: "rounded-full"
    - path: "frontend/src/components/chat/RunCard.tsx"
      provides: "title = run identity (not the activity verb); model·turn subline; activity verb only in the strip"
      contains: "turn"
  key_links:
    - from: "RunCard header title"
      to: "a derived run-identity string (NOT outerBannerLabel)"
      via: "a calm title derived from data already on the message"
      pattern: "headerTitle"
    - from: "RunCard subline"
      to: "message model + iteration/turn"
      via: "a `${model} · turn ${n}` run-sub"
      pattern: "turn"
---

<objective>
Land the three MED header divergences from sketch 014: the header status strip pill chrome, the double-verb removal, and the restored `model · turn` run-sub subline.

Today: (1) the strip's `placement="header"` branch is bare middot text — the sketch wants the chip chrome (subtle bg + 1px border + rounded-full + vertical divider bars). (2) The activity verb renders TWICE — once as `headerTitle = outerBannerLabel(...)` AND once in the strip's `activityVerb` (the same call). (3) The sketch header shows a calm run identity title + a `kimi-k2.6 · turn 1` run-sub; today the verb occupies the title line and the run-sub is gone.

Operator decision (locked): the verb lives in the STRIP ONLY; the title becomes a run identity; restore a `model · turn` subline from data already on the message. NO new backend field, NO migration.

Purpose: make the run header match the sketch — one verb, a pill-chrome strip, a model·turn subline.
Output: RunStatusStrip header placement carries the pill chrome; RunCard title is a calm run identity with a model·turn subline; the verb appears only in the strip.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/095-chat-tool-card-unification/095-VERIFICATION.md
@.planning/phases/095-chat-tool-card-unification/095-HUMAN-UAT.md

# THE design contract — invoke Skill("sketch-findings-agentic-rag"), match sketch 014.
@.claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md

<interfaces>
<!-- The strip's current header branch (bare text) — RunStatusStrip.tsx:44-51 -->
```tsx
placement === "header"
  ? "text-muted-foreground"
  : "rounded-full border border-primary/40 bg-popover/92 px-3 py-1.5 shadow-lg backdrop-blur-md",
```

<!-- The double-verb sites in RunCard.tsx -->
```tsx
// :170-174 — the TITLE currently IS the activity verb while streaming:
const headerTitle = isStreamingNow
  ? outerBannerLabel(activeTool, hasTools, message.isPlanning ?? false)
  : hasTools
    ? `Run · ${stepCount} step${stepCount === 1 ? "" : "s"} · ${statusGlyph(message.runStatus)} ${statusWord(message.runStatus, message.runError)}`
    : statusWord(message.runStatus, message.runError)

// :230-235 — the strip ALSO renders the same verb:
activityVerb={
  isStreamingNow
    ? outerBannerLabel(activeTool, hasTools, message.isPlanning ?? false)
    : null
}
```

<!-- Data ALREADY on the message you can use for the run-sub (NO backend field needed).
     Confirm exact field names by reading the Message type before use. -->
- model / provider: read whatever the Message type exposes (e.g. `message.model`). If no model field exists on Message, fall back to a calm generic ("Agent run") — do NOT invent a backend field.
- turn/iteration: `message.iterationCount` (0-based) — render as `turn ${(iterationCount ?? 0) + 1}`.
- The sketch run-sub exemplar is `kimi-k2.6 · turn 1` (chat-tool-card-unification.md HTML "run-card with the header strip").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header status strip pill chrome (GAP-095-03 MED)</name>
  <files>frontend/src/components/chat/RunStatusStrip.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunStatusStrip.tsx (full file — 1-83)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md (the CSS "Status strip — one component, two homes" block + the HTML "run-card with the header strip")
  </read_first>
  <action>
    Give the `placement="header"` branch the sketch chip chrome. The strip currently renders bare `text-muted-foreground` text with `·` middots between segments. Per the sketch `.status-strip` (`background: hsl(220 30% 11% / .85)` + `1px solid var(--color-border)` + `border-radius: var(--radius-full)` + small padding) and the HTML which uses `<span class="divider">` bars between segments:

    1. Change the header branch className from `"text-muted-foreground"` to the chip chrome:
       `"rounded-full border border-border bg-[hsl(220_30%_11%/0.8)] px-2.5 py-1 text-muted-foreground"`
       (keep the floating branch unchanged — it already has its own pill chrome).

    2. Replace the three `·` middot separators with 1px vertical divider bars between segments (the sketch `.divider`). Render a thin vertical rule between the time segment, the Step segment, and the activity segment, e.g.:
       `<span aria-hidden="true" className="h-3 w-px bg-border/60" />`
       in place of each `<span ...>·</span>` separator. Keep them `aria-hidden`.

    3. Keep the `.done` modifier behavior (terminal strip recedes to `text-success`) and the bouncing activity dot. Keep the strip visually distinct from the title line (the pill bg + border does this).

    4. XSS-safe: `activityVerb` stays a controlled enum string rendered as React text children — no `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/RunCard.test.tsx && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-full" frontend/src/components/chat/RunStatusStrip.tsx` is >= 2 (header branch now has it too; floating branch already did).
    - `grep -c "w-px bg-border" frontend/src/components/chat/RunStatusStrip.tsx` is >= 1 (the divider bars exist).
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/RunStatusStrip.tsx` returns 0.
    - The header branch no longer renders bare `·` middots: the `<span ...>·</span>` separator count in RunStatusStrip.tsx is reduced (divider bars replace them). Confirm by inspecting the diff.
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <done>The header status strip renders as a rounded-full pill with a subtle bg + 1px border + divider bars between segments, matching sketch 014.</done>
</task>

<task type="auto">
  <name>Task 2: Single verb + calm run-identity title + model·turn run-sub (GAP-095-03 MED)</name>
  <files>frontend/src/components/chat/RunCard.tsx, frontend/src/components/chat/RunCard.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunCard.tsx (lines 139-238 — headerTitle derivation, the activeTool derivation, the strip mount; AND the Message type import to confirm available fields)
    - frontend/src/types/index.ts (the Message type — confirm whether a model/provider field exists, and the iterationCount field)
    - frontend/src/components/chat/RunCard.test.tsx (the existing suite — to update the title assertion)
  </read_first>
  <action>
    The activity verb must live in the STRIP ONLY. The title line becomes a calm run identity, with a `model · turn` run-sub.

    1. STOP putting the verb in the title. Change `headerTitle` (lines ~170-174) so the STREAMING branch no longer returns `outerBannerLabel(...)`. Make the title a calm run identity for BOTH streaming and terminal:
       - Derive a simple, generic run title. Preferred: a calm generic constant `"Agent run"` (the operator chose model·turn for identity, not a prompt-derived name — keep it simple and deterministic). If the first user prompt is trivially available on the message, a short snippet is acceptable, but do NOT add plumbing to fetch it — `"Agent run"` is the safe default.
       - Keep the terminal-state status summary somewhere honest: the collapsed-row (lines ~261-288) ALREADY shows `Run · N steps · ✓ done · elapsed`, so the title does NOT need to carry the status word. For the EXPANDED terminal header, the strip (now `.done`, success-toned, verb=null) + the title `"Agent run"` is sufficient. Confirm the collapsed-row still renders the full status line unchanged.

    2. KEEP the strip's `activityVerb` exactly as-is (lines ~230-235): `isStreamingNow ? outerBannerLabel(...) : null`. This is now the SOLE verb render site.

    3. ADD a `model · turn` run-sub subline UNDER the title (between the title `<div>` at ~223 and the `<RunStatusStrip>` at ~227). Derive it from data already on the message:
       - model: read the Message model field if present (confirm the exact name in types/index.ts — e.g. `message.model`); if no such field exists, omit the model segment and show just `turn ${n}` (do NOT invent a backend field).
       - turn: `turn ${(message.iterationCount ?? 0) + 1}`.
       - Render as a muted `font-mono text-xs text-muted-foreground/70` line, e.g. `{model ? `${model} · ` : ""}turn {turn}`.
       - The run-sub is its own line; the strip stays below it. This matches the sketch HTML: title + `run-sub` + `hdr-strip`.

    4. Update RunCard.test.tsx: any assertion that the streaming header title equals the activity verb (e.g. asserts `outerBannerLabel`'s output in the title) must be retargeted — the title is now `"Agent run"` and the verb is asserted in the strip (`data-testid="run-status-strip"`). Add a test that the verb appears EXACTLY ONCE in the header (not in the title). Add a test that the run-sub shows `turn N`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/RunCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "outerBannerLabel" frontend/src/components/chat/RunCard.tsx` is exactly 1 (only the strip's `activityVerb` calls it; the `headerTitle` streaming branch no longer does). NOTE: `outerBannerLabel` is also imported — so the count of CALL sites (excluding the import line) must be 1. Verify the import line + exactly one call.
    - `grep -c "turn " frontend/src/components/chat/RunCard.tsx` is >= 1 (the run-sub renders `turn N`).
    - The RunCard test asserts the activity verb renders once and lives in the strip (`run-status-strip`), not the title.
    - The RunCard test asserts a `turn` subline is present.
    - `cd frontend && npx vitest run src/components/chat/RunCard.test.tsx` passes (former title-equals-verb assertions retargeted).
  </acceptance_criteria>
  <done>The activity verb renders exactly once (in the strip); the title is a calm run identity; a `model · turn` run-sub subline is restored from existing message data with no backend field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message data -> RunCard header | model/iteration fields cross into the DOM as text; React-escaped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-07-01 | Tampering (XSS) | run-sub render of `message.model` | mitigate | render as React text children only — no `dangerouslySetInnerHTML`. |
| T-095-07-02 | Information disclosure | model name in run-sub | accept | the user's own model selection is already visible elsewhere; no new data exposed. |
</threat_model>

<verification>
- `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/RunCard.tsx` returns 0.
- `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/RunStatusStrip.tsx` returns 0.
- The D-06 timer derivation (Date.parse(created_at), freeze-once, never-vanishes) is UNTOUCHED — no change to `startMs`/`frozenEndRef`/`elapsedLabel`.
- The D-04 `unifiedStepCount` wiring on all three count sites is UNTOUCHED.
- `tsc -b` reports no NET-NEW errors referencing RunCard.tsx / RunStatusStrip.tsx.
</verification>

<success_criteria>
- GAP-095-03 (MED chip chrome + MED single verb + MED model·turn run-sub) closed.
- No new backend field / migration (operator decision honored).
- RunCard test suite green; `vite build` clean; frontend-only, provider-agnostic.
</success_criteria>

<output>
After completion, create `.planning/phases/095-chat-tool-card-unification/095-07-SUMMARY.md`
</output>
