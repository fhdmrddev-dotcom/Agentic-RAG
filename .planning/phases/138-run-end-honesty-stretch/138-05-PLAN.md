---
phase: 138-run-end-honesty-stretch
plan: 05
type: execute
wave: 2
depends_on: [138-04]
gap_closure: true
files_modified: []
autonomous: false
requirements: [RUN-01]

must_haves:
  truths:
    - "RUN-01b live-surfacing (Scenario B re-verify): a clean Deep run that leaves open todos shows each open item's content suffixed '(run ended — not completed)' in the live Workspace TODOS panel AT RUN-END — with NO thread-switch and NO refresh (the exact gap 138-03 caught)"
    - "Reconcile timing: the marker appears within a couple seconds of the run-terminal (driven by the fetch-on-terminal), not only after a manual reconcile — reproduced the OPPOSITE of the 12+-second unmarked-panel failure"
    - "D-14 red line live: a normal Deep run with NO todos shows no marker text anywhere and no spurious panel change (the clean-terminal reconcile is a harmless empty→empty no-op)"
    - "SC#10 spirit: the live-surfacing behaviour holds on a second provider (shared provider-agnostic onTerminal/emit path — no provider fork)"
    - "psycopg2 cross-check: the browser panel state at run-end matches the DB (open rows carry the exact suffix in content; status values unchanged) — browser and DB agree WITHOUT a refresh"
  artifacts: []
  key_links: []
---

<objective>
Live re-verify the ONE scenario Phase 138 failed (Scenario B / VERIFICATION.md must-have #5): the `(run ended — not completed)` marker must now surface in the live Workspace TODOS panel at run-end, with no manual thread-switch/refresh, after the 138-04 fetch-on-terminal fix. This is the acceptance bar for closing SEED-094 — the phase mandated a live UAT precisely because this class of lived-experience gap passes straight through code-level must_haves (all 138-01/138-02 unit tests were green while the live panel stayed stuck).

Purpose: 138-04 ships the unit-tested helper + wiring; this checkpoint proves the lived experience — the marker actually appearing live at run-end — which unit tests cannot reach (the real StreamsProvider onTerminal fires only against a running agent loop). It also re-confirms the D-14 red line live (a no-todo Deep run stays byte-identical, no spurious marker) and spot-checks a second provider (shared path — no fork).

Output: Operator verdict recorded in the SUMMARY; on pass, SEED-094 is closable and Phase 138 completes. On failure, capture what was observed and route to `/gsd:plan-phase 138 --gaps`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/138-run-end-honesty-stretch/138-CONTEXT.md
@.planning/phases/138-run-end-honesty-stretch/138-VERIFICATION.md
@.planning/phases/138-run-end-honesty-stretch/138-03-SUMMARY.md
@.planning/phases/138-run-end-honesty-stretch/138-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator live-verifies the run-end panel marker surfaces WITHOUT a refresh (+ D-14 red line + cross-provider spot check)</name>
  <files>none — verification-only plan (no code changes)</files>
  <read_first>
    - .planning/phases/138-run-end-honesty-stretch/138-04-SUMMARY.md (what the fetch-on-terminal fix changed + the exact clean-completion gate)
    - .planning/phases/138-run-end-honesty-stretch/138-03-SUMMARY.md (Scenario B — the exact gap being re-verified + the 12+-second reproduction)
    - .planning/phases/138-run-end-honesty-stretch/138-VERIFICATION.md (must-have #5 — the live-surfacing requirement)
  </read_first>
  <what-built>
    - 138-04: a shared `_reconcileTodosOnTerminal(threadId, kind)` helper wired into BOTH StreamsProvider onTerminal handlers. On a genuinely-clean run terminal (kind 'done'/'reader_done') it re-fetches GET /threads/{id}/todos (reusing `getThreadTodos`) and full-state-replaces the panel via `replaceTodosForThread` — so the backend-committed `(run ended — not completed)` marker surfaces LIVE with no refresh. Best-effort, additive-only, gated to clean completion (no fetch on cancelled/error/timed_out). Backend (138-02) unchanged; TodosSection.tsx unchanged.
  </what-built>
  <action>
    Operator executes the scenarios below against the live app and reports a pass/fail verdict per scenario. This is a manual verification gate — the executor writes NO code; it pauses and resumes only on the operator's signal. Give the operator the steps verbatim, trust their observation, and offer the psycopg2 cross-check where noted. On any failure, capture what was observed (especially: did the marker appear at run-end, or only after a refresh?) and route to `/gsd:plan-phase 138 --gaps`. Drive the live UI via Chrome DevTools MCP where available; fall back to operator clicks + psycopg2 if the MCP hangs.
  </action>
  <how-to-verify>
    Pre-req: local backend running (operator starts uvicorn — never start it for them), dev app at http://localhost:5173/, login fhdmrd@gmail.com / 123456. DB cross-check uses psycopg2 against Postgres :54322 (`SELECT content, status FROM todos WHERE thread_id = '<id>' ORDER BY order_index, created_at`). Do NOT refresh or switch threads during a scenario unless the step says to — the whole point is that the marker appears WITHOUT a manual reconcile.

    Scenario B (re-verify — THE GAP) — RUN-01b marker surfaces LIVE at run-end:
    1. New chat (a Deep run). Prompt something that makes the model write a multi-step todo list AND leave some steps open, then end cleanly, e.g.: "Create a todo list with 4 steps for building a report, mark step 1 completed and start step 2, then stop and summarize — do not finish the remaining steps."
    2. Watch the Workspace TODOS panel as the run reaches its terminal. Do NOT switch threads and do NOT refresh.
    3. PASS if, within a couple seconds of the run ending, each still-open (pending/in_progress) item's text ends with ` (run ended — not completed)`, the completed step does NOT, and no status flipped to completed — all WITHOUT any refresh/thread-switch. FAIL if the panel still shows unmarked open todos at run-end and the marker only appears after a manual reconcile (the original gap).
    4. Cross-check via psycopg2: the open rows carry the suffix in `content` and `status` is unchanged — confirm the browser panel matches the DB without a refresh.

    Scenario B-neg (D-14 red line) — normal Deep run, no todos:
    5. New chat, normal Deep prompt with NO todos and NO file generation ("Explain what RAG means in two sentences."). Let it end cleanly.
    6. PASS if the reply is normal, NO todo panel marker text appears anywhere, and there is no spurious panel flicker/change at run-end (the clean-terminal reconcile is a harmless empty→empty no-op). FAIL if any marker text or unexpected todo panel content appears.

    Scenario B-xp (SC#10 cross-provider spot check):
    7. Repeat Scenario B on a DIFFERENT provider than step 1 (switch the model to another of OpenAI / Anthropic / Google / OpenRouter — pick one representative). PASS if the marker surfaces LIVE at run-end identically (shared provider-agnostic onTerminal/emit path — no provider fork).
  </how-to-verify>
  <verify>
    <human-check>Scenario B passes (marker surfaces live at run-end with no refresh, browser matches DB via psycopg2), Scenario B-neg passes (D-14 red line — no marker on a no-todo run), and Scenario B-xp passes (identical on a second provider).</human-check>
  </verify>
  <acceptance_criteria>
    - Scenario B: every open todo's `content` shows the exact suffix ` (run ended — not completed)` in the LIVE panel at run-end, with NO thread-switch and NO refresh; completed items and all `status` values unchanged; browser and psycopg2 agree.
    - Scenario B (timing): the marker appears within a couple seconds of the run-terminal — NOT only after a manual reconcile (the opposite of the 12+-second unmarked reproduction in 138-03).
    - Scenario B-neg: a no-todo Deep run shows zero marker text and no spurious panel change — D-14 red line intact.
    - Scenario B-xp: marker surfaces live identically on a second provider — no provider fork.
  </acceptance_criteria>
  <resume-signal>Type "approved" if Scenario B (live marker, no refresh), B-neg (red line), and B-xp (cross-provider) all pass — SEED-094 is then closable. Otherwise describe what you observed (especially whether the marker needed a refresh) and route to `/gsd:plan-phase 138 --gaps`.</resume-signal>
  <done>Operator recorded a pass/fail verdict; on pass, the RUN-01b live-surfacing gap is closed, SEED-094 is closable, and Phase 138 completes; any failure is routed to `/gsd:plan-phase 138 --gaps`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (verification-only plan) | This plan changes no code and adds no attack surface; it validates the live behaviour of the 138-04 fetch-on-terminal reconcile (the mitigations for T-138-10/11/12 shipped by 138-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-13 | Tampering (honesty regression) | live run-end panel state | mitigate | Scenario B live-exercises the exact lived-experience gap that code-level must_haves passed through — confirms the marker surfaces live and the browser matches the DB (psycopg2) without a refresh. |
| T-138-14 | Information Disclosure | cross-thread todo isolation | mitigate | Scenarios run in owned threads under the test login; the 138-04 thread-keyed `replaceTodosForThread` (T-138-11) is observed to affect only the acting thread — a second thread's panel is unchanged. |
| T-138-SC | Tampering | npm/pip/cargo installs | accept | Verification-only — no packages installed. |
</threat_model>

<verification>
- Operator runs Scenario B + B-neg + B-xp against the live app; all must PASS.
- psycopg2 cross-checks `todos.content`/`status` for Scenario B and confirms the browser panel matches the DB at run-end WITHOUT a refresh.
</verification>

<success_criteria>
- Scenario B passes live: the `(run ended — not completed)` marker surfaces in the Workspace TODOS panel at run-end with no thread-switch/refresh (the closed gap).
- D-14 red line holds live (no marker on a no-todo Deep run) and the behaviour is identical on a second provider (SC#10 spirit).
- SEED-094 can be marked closed (both re-open triggers now verified fixed live — baseline leak already closed in 138-03; stuck-todo live-surfacing closed here).
</success_criteria>

<output>
Create `.planning/phases/138-run-end-honesty-stretch/138-05-SUMMARY.md` when done (record the operator verdict + any observations).
</output>
