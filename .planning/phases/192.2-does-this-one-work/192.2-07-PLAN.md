---
phase: 192.2-does-this-one-work
plan: 07
type: execute
wave: 1
gap_closure: true
gap_closure_round: 1
depends_on: []
files_modified:
  - frontend/tailwind.config.js
  - frontend/src/index.css
  - frontend/src/components/workflows/library/gutterTokens.fences.test.ts
autonomous: true
requirements: [LIB-06]

must_haves:
  truths:
    - "The `stopped` run gutter and the word `Stopped` are actually painted — they are not visually identical to the `unknown` arm the same docblock says is DELIBERATELY unpainted"
    - "Every colour utility the card's two tone maps name resolves to a real Tailwind key backed by a CSS variable defined in BOTH themes"
    - "The gap cannot reopen silently: a utility that stops resolving turns a test RED, where today the suite asserts `data-run` and never a class"
  artifacts:
    - path: "frontend/tailwind.config.js"
      provides: "the `warning` colour key, beside `success`"
      contains: "warning"
    - path: "frontend/src/index.css"
      provides: "--warning / --warning-foreground in `:root`, not only in `.dark`"
      contains: "--warning"
    - path: "frontend/src/components/workflows/library/gutterTokens.fences.test.ts"
      provides: "the fence that would have caught this — utility → config key → CSS var, in both themes, with a synthetic positive control"
  key_links:
    - from: "WorkflowCard.tsx GUTTER_TONE / RUN_TONE"
      to: "tailwind.config.js theme.extend.colors"
      via: "every `bg-*`/`text-*` literal must name a declared key"
      pattern: "bg-warning"
    - from: "tailwind.config.js warning key"
      to: "index.css :root and .dark"
      via: "hsl(var(--warning))"
      pattern: "--warning"
---

<objective>
Close **WR-01**: `bg-warning` and `text-warning` compile to nothing, so a cancelled run's 3px gutter
renders **unpainted — visually identical to the `unknown` arm**, which the same docblock says is
deliberately unpainted precisely so the two "cannot be mistaken."

Purpose: this is the colour half of D-01's standing constraint (*colour, and never colour alone*).
The WORD still shows, so nothing is blank or wrong — but one of five arms silently lost its half of
the encoding, and the block that lost it asserts *"every one of these is already in `index.css` and
none is a new hex."* Both halves of that sentence are false today. ⚠ **It was unguarded for a
structural reason, not a careless one: the suite asserts `data-run` and explicitly NOT a class**
(`WorkflowCard.test.tsx:1597`, `:1631`, `:1643`), so no assertion in this repository can see a
Tailwind utility that does not exist. The fix is worth little without the fence.

Output: one Tailwind colour key, two CSS variables promoted out of `.dark`, and a fence that goes
RED against the tree as it stands right now.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192.2-does-this-one-work/192.2-REVIEW.md
@.planning/phases/192.2-does-this-one-work/CONTEXT.md

<measured_facts>
Measured against the tree at the phase tip, 2026-08-19. These are FACTS, not the plan's beliefs —
re-derive any you intend to act on differently.

- `frontend/tailwind.config.js` `theme.extend.colors` declares `accent`, `destructive`, `success`,
  `border`, `input`, `ring`, `sidebar`, `accent-violet`, `accent-violet-text`, `muted`,
  `panel-muted-foreground`… and **no `warning` key at all**. `success` sits at `:73`.
- `frontend/src/index.css`: `:root` spans `:17-90`, `.dark` spans `:92-173`.
  - `--success: 142 71% 45%` appears at **`:35` (`:root`) and `:110` (`.dark`) — BYTE-IDENTICAL in
    both themes.** That is the house precedent this plan follows.
  - `--destructive` appears in both (`:33` / `:108`), with DIFFERENT values per theme.
  - `--warning: 38 92% 60%` and `--warning-foreground: 240 60% 8%` appear **ONLY at `:116-117`,
    inside `.dark`** — the Phase 087 amber `ask_user` treatment.
- `WorkflowCard.tsx:491-506` — `GUTTER_TONE` names `bg-success` · `bg-destructive` · `bg-warning` ·
  `bg-border` · `bg-transparent`; `RUN_TONE` names `text-success` · `text-destructive` ·
  `text-warning` · `text-muted-foreground` ×2.
- ⚠ **This is a repo-wide latent bug, not one this phase invented.** `ModelDiscoveryPanel.tsx`,
  `ModelRegistryTab.tsx` and `ConnectionFormPanel.tsx` all use `bg-warning` today. The config fix
  repairs those too — which is a side effect to RECORD, never a licence to go edit them.
</measured_facts>

<gate_baselines>
At HEAD (the phase tip). Read the verdict line, never a summary.
- count gate, from the **REPO ROOT**, `GSD_VITEST_MAX_WORKERS=2`: `total 4594 · failed 0 · pinned total 4328 · OK — 92/92`
- `npx tsc --noEmit -p tsconfig.app.json` (from `frontend/`): **33 errors, ZERO under `library/`**
- library subtree: 12 files / 640 tests / 0 failed
⚠ `scripts/vitest-count-gate.cjs` lives at the **REPO ROOT**. The `cd frontend && … | tail` form
makes `MODULE_NOT_FOUND` exit **0** — a broken invocation reads as a GREEN gate. Never write it.
⚠ A red frontend run may be a SEED-171 flake. Capture failing filenames from the gate's own
persisted JSON BEFORE re-running anything, check each against `git diff --numstat`, and **never
adjust `GSD_VITEST_MAX_WORKERS`** — that remedy is REFUTED by measurement.
</gate_baselines>
</context>

<decisions>
**DEC-07-A — `:root` gets the SAME values `.dark` already carries, and that is a precedent, not a
guess.** `--success` is byte-identical in both themes in this file. Copying `.dark`'s amber into
`:root` follows that precedent exactly and is also what `192.2-REVIEW.md`'s WR-01 fix proposes.
Inventing a second light-mode amber would be a design decision this round has no mandate for, and
the product runs in Deep Midnight (dark) regardless.

**DEC-07-B — this plan does NOT touch `WorkflowCard.tsx`.** Two reasons, one mechanical: (1) the
false sentence in `GUTTER_TONE`'s docblock (*"every one of these is already in `index.css`"*)
becomes TRUE the moment this plan lands, so there is nothing left to correct — the amendment owed
is the RECORD that it was false when written, and that belongs in the SUMMARY; (2) `WorkflowCard.tsx`
is claimed by plans `192.2-09` and `192.2-11`, and a same-wave file collision is a planning defect.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Land the fence RED first — it must fail against the tree as it stands</name>

  <read_first>
    - `frontend/src/components/workflows/library/WorkflowCard.tsx:479-510` — the `RunGutter` type and
      the two tone maps. **READ ONLY. This plan modifies no byte of this file (DEC-07-B).**
    - `frontend/tailwind.config.js:30-85` — `theme.extend.colors`, and the shape of the `success` key.
    - `frontend/src/index.css:17-90` (`:root`) and `:92-173` (`.dark`).
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` — the house fence
      idiom, and specifically its **synthetic positive control** paragraph (`:56-61`): *"A fence whose
      matcher is broken passes vacuously and looks exactly like a fence that holds."*
  </read_first>

  <files>frontend/src/components/workflows/library/gutterTokens.fences.test.ts</files>

  <action>
    Create the fence. It reads three files as source and asserts a chain, and it must be committed
    and RUN while it is still RED — recording the failure verbatim in the SUMMARY before Task 2
    makes it green. A fence first seen green proves nothing about what it would catch.

    Read sources with Vite `?raw` imports, the idiom `librarySubtree.fences.test.ts` already uses:
    `WorkflowCard.tsx?raw`, `../../../../tailwind.config.js?raw` and `@/index.css?raw`. Verify each
    specifier resolves before writing assertions against it — a `?raw` import that fails to resolve
    is a suite-level error, not a red fence, and the two do not read the same.

    Assert, in this order:

    1. **Extraction.** From `WorkflowCard.tsx`'s source, extract every colour-utility string literal
       inside the `GUTTER_TONE` and `RUN_TONE` object bodies. Expect exactly the ten literals named
       in `<measured_facts>` above; assert the extracted SET is non-empty and assert its size, so a
       future refactor that renames the maps fails loudly rather than sweeping nothing. ⚠ **A fence
       that sweeps the empty string passes green while defending nothing** — this project has
       measured that happening. The non-vacuity assertion is not optional.

       ⚠ **KEEP THE SIZE ASSERTION EXACT — and put the number in ONE named, commented place.**
       `192.2-11` (wave 3) adds a SIXTH arm to both tone maps and is the plan AUTHORISED to
       re-derive this count; it has an explicit task and acceptance criteria for doing so.
       **That is not licence to soften this fence now.** Do NOT pre-emptively write a `>=`, a
       range, a tolerance, or a computed-from-the-source size to "survive" that change — a fence
       that cannot go red is not a fence, and an exact count is the whole mechanism here. Write the
       expected size as a single named constant with a comment saying what shape it was measured
       against (five arms, at this SHA), so wave 3 can re-derive it in one obvious location instead
       of hunting a magic number. State the measured count in your SUMMARY so wave 3 has the
       before-figure to show its arithmetic against.
    2. **Utility → key.** Strip the `bg-`/`text-` prefix. Every remaining key must be either a member
       of the explicit built-in allowlist `["transparent", "current", "inherit"]` or a declared key
       under `theme.extend.colors` in `tailwind.config.js` source. Match the key as a property name
       at the start of a line (`/^\s*"?<key>"?\s*:/m`) so a mention inside a COMMENT cannot satisfy
       it — the 187-24 trap, which this subtree has already hit four times.
    3. **Key → variable, in BOTH themes.** For each config key that resolves, read the `hsl(var(--X))`
       variable name out of its config entry, then assert `--X:` is declared inside the `:root` block
       AND inside the `.dark` block of `index.css`. Split the two blocks by their brace bounds rather
       than by line number, so the fence survives the file growing.
    4. **Synthetic positive control (mandatory).** Run the *same* detector functions over an in-test
       source string whose tone map names `bg-does-not-exist`, and assert the detector reports it as
       unresolved. Then run it over an in-test config declaring a key whose variable is missing from
       `:root`, and assert the theme check reports it. Both controls prove the matcher can fail.

    **What would make this fence go RED after Task 2 lands:** deleting the `warning` key from
    `tailwind.config.js`; deleting `--warning` from `:root` (leaving it dark-only, i.e. today's
    state); adding a sixth arm to `GUTTER_TONE` whose utility names a key nobody declared. Write
    those three sentences into the fence's own docblock so a later reader does not have to derive
    them.

    ⚠ **The fence is a TEST file.** `LIBRARY_SUBTREE_PATHS` in `librarySubtree.fences.test.ts` is a
    list of SOURCE modules and test files are outside that sweep by construction — read its
    docblock at `:50-54` and confirm this before deciding not to edit it. Record the confirmation.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library/gutterTokens.fences.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - The command above **FAILS**, and the failure names `bg-warning` and/or `text-warning` as
      unresolved. The verbatim failure output is pasted into the SUMMARY.
    - The extraction assertion proves the swept set is non-empty and of the expected size — a
      recorded number, not a `.length > 0`.
    - Both synthetic positive controls PASS (i.e. the detector catches the planted violations) even
      while the real assertion is red. If a control cannot pass, the detector is broken and the red
      above is meaningless — say so and stop.
    - `git diff --numstat -- frontend/src/components/workflows/library/WorkflowCard.tsx` is **empty**.
  </acceptance_criteria>

  <done>A fence exists, is red for the right reason, and its matcher is proven able to fail.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the `warning` token, in the config and in both themes</name>

  <read_first>
    - `frontend/tailwind.config.js:73-76` — the `success` key, whose shape this copies exactly.
    - `frontend/src/index.css:33-36` (`:root` destructive + success) and `:108-117` (`.dark`'s
      destructive, success and the Phase-087 warning block with its contrast rationale).
  </read_first>

  <files>
    frontend/tailwind.config.js
    frontend/src/index.css
  </files>

  <action>
    In `tailwind.config.js`, add a `warning` key under `theme.extend.colors`, immediately after
    `success`, with the identical two-member shape: `DEFAULT: "hsl(var(--warning))"` and
    `foreground: "hsl(var(--warning-foreground))"`. Add a one-line comment naming Phase 192.2 WR-01
    and stating that the variables have existed in `.dark` since Phase 087 while the config key never
    did — so the next reader knows this was a missing declaration, not a new colour.

    In `index.css`, add `--warning: 38 92% 60%;` and `--warning-foreground: 240 60% 8%;` inside the
    `:root` block, beside `--success` at `:35-36`. Use the values `.dark` already carries, per
    DEC-07-A, and put the reason in the comment: `--success` is byte-identical across both themes in
    this file, and that is the precedent being followed.

    ⚠ **Do not touch the `.dark` block.** Its values and its Phase-087 contrast rationale stand
    unchanged; this task only stops the token being dark-only.

    ⚠ **Do not go and edit `ModelDiscoveryPanel.tsx`, `ModelRegistryTab.tsx` or
    `ConnectionFormPanel.tsx`.** They are repaired by this config change without being touched.
    RECORD in the SUMMARY that three unrelated surfaces gain a colour they have been silently
    missing — that is a real consequence and it should be visible, not discovered later.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library/gutterTokens.fences.test.ts &amp;&amp; npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>

  <acceptance_criteria>
    - The fence from Task 1 now **PASSES**, including both synthetic controls.
    - `grep -n "warning" frontend/tailwind.config.js` exits **0** and names the key (it exited 1 before).
    - `--warning` is declared inside `index.css`'s `:root` block — proved by extracting the `:root`
      brace-bounded body and grepping THAT, not the whole file (the whole file already contained it,
      which is exactly how the defect hid).
    - `npx tsc --noEmit -p tsconfig.app.json` reports **33 errors, zero under `library/`** — byte-identical
      to the recorded baseline. A CSS/config change that moves the typecheck count is a signal to stop.
    - `git diff --numstat` names exactly the two files in `<files>` (plus the Task-1 fence).
  </acceptance_criteria>

  <done>`bg-warning` and `text-warning` resolve to real CSS, in both themes, and a fence guards it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| build-time config → rendered stylesheet | a class named in source that the build silently drops. No untrusted input crosses here; the risk is a false design claim, not an injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-192.2-30 | Information disclosure (inverse — information WITHHELD) | `GUTTER_TONE.stopped` | mitigate | The `stopped` arm renders indistinguishable from `unknown`, so a person reading a column of gutters cannot tell *a run was stopped* from *we have no information*. Task 2 restores the paint; Task 1's fence keeps it. |
| T-192.2-31 | Tampering | `tailwind.config.js` | mitigate | A colour key added here affects every surface in the app. Mitigated by adding an ADDITIVE key only (no existing key edited, no palette override), and by asserting the typecheck baseline is unmoved. |
| T-192.2-32 | Repudiation | the fence itself | mitigate | A fence first observed GREEN proves nothing. Task 1 requires it committed and run RED, with the failure pasted verbatim, plus two synthetic positive controls proving the matcher can fail at all. |
| T-192.2-SC | Tampering | npm/pip/cargo installs | accept | **This plan installs no package.** No `package.json` / `requirements.txt` change is in scope; a plan that finds it needs one has left its envelope and must stop. |
</threat_model>

<verification>
- From the **repo root**: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — read the
  verdict line verbatim. Expect `failed 0`, `92/92` pinned files present, no per-file DECREASE, and
  a grand total ≥ 4594 (the new fence's cases make it grow; **a growing total is the gate WORKING**).
- From `frontend/`: `npx tsc --noEmit -p tsconfig.app.json` — 33, zero under `library/`.
- From `frontend/`: `GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library` —
  12 existing files + the new fence, 0 failed.
- `git diff --numstat` against the plan's base SHA names exactly three files.
</verification>

<success_criteria>
- [ ] `warning` is a declared Tailwind colour key
- [ ] `--warning` / `--warning-foreground` exist in `:root` as well as `.dark`
- [ ] The `stopped` gutter and word are painted, and provably distinct from the `unknown` arm
- [ ] A fence exists that was RED before the fix, is GREEN after, and carries two synthetic positive controls
- [ ] `WorkflowCard.tsx` is byte-unchanged by this plan
- [ ] The repo-wide side effect (three unrelated surfaces repaired) is RECORDED, not acted on
</success_criteria>

<output>
`.planning/phases/192.2-does-this-one-work/192.2-07-SUMMARY.md`
</output>
</content>
</invoke>
