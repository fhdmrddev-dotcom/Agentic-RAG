---
phase: 195-show-the-deliverable
plan: 07
type: execute
wave: 4
depends_on: ["195-04", "195-05", "195-06"]
files_modified:
  - frontend/src/components/files/__tests__/FileRow.sweep.test.ts
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "A source sweep proves there is exactly ONE formatBytes, ONE icon path and ONE row markup in the tree — and it has been shown to fail once per swept file"
    - "The sweep cannot pass vacuously: every ?raw import is length- and identity-guarded, and the comment stripper is itself proved to strip"
    - "Chat's public capability is pinned: OutputFileCard's prop set is unchanged and its two call sites are byte-identical to a NAMED base"
    - "Every pin this phase moved is raised to a number the gate itself printed, so a future deletion cannot pass silently"
  artifacts:
    - path: "frontend/src/components/files/__tests__/FileRow.sweep.test.ts"
      provides: "The SC#2 one-of-each source sweep over the four in-scope files, with guards, a stripper non-vacuity case, and the chat-capability arm"
      contains: "toBeGreaterThan"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "The sweep's file-level entries plus every pin this phase moved, each read from the gate's own actual column"
      contains: "FileRow.sweep.test.ts"
  key_links:
    - from: "frontend/src/components/files/__tests__/FileRow.sweep.test.ts"
      to: "frontend/src/pages/WorkflowRunPage.tsx, frontend/src/components/panel/FilesSection.tsx, frontend/src/components/chat/OutputFileCard.tsx, frontend/src/lib/fileIcon.tsx"
      via: "four independently guarded ?raw imports, swept over codeOf(source)"
      pattern: "\\?raw"
---

<objective>
Prove SC#2 — *no second file UI* — as a measurement rather than a claim, and reconcile every count-gate
pin this phase moved.

Purpose: RUN-03 says a produced file's presentation must reuse the shipped one rather than a second one.
After three conversions that is *believed*; nothing in the tree checks it. ⚠ **Three of the four
in-scope source files are swept by NOTHING today** — only `WorkflowRunPage.tsx` has a source fence — so
this sweep is the only artefact standing between SC#2 and a future re-duplication. And ⚠ **its own guards
are the only thing standing between the sweep and vacuity**: 192.1 measured a renamed module swept
against the **empty string**, passing green.
Output: one sweep suite inside the gate, and a gate file whose every Phase-195 pin equals the number the
gate printed.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/195-show-the-deliverable/195-CONTEXT.md
@.planning/phases/195-show-the-deliverable/195-RESEARCH.md
@.planning/phases/195-show-the-deliverable/195-PATTERNS.md
@.planning/phases/195-show-the-deliverable/195-VALIDATION.md
@.planning/phases/195-show-the-deliverable/195-BASELINE.md
@.planning/phases/195-show-the-deliverable/195-04-SUMMARY.md
@.planning/phases/195-show-the-deliverable/195-05-SUMMARY.md
@.planning/phases/195-show-the-deliverable/195-06-SUMMARY.md

@frontend/src/components/workflows/library/rowIdentity.test.ts
@scripts/vitest-count-gate.cjs

<interfaces>
<!-- The guard template, the correct comment stripper, and the pins to reconcile. -->

**⚠ THERE ARE TWO INCOMPATIBLE `codeOf()` IMPLEMENTATIONS SHIPPED IN THIS TREE. Use the LINE-ANCHORED
one:**

    // WorkflowRunPage.test.tsx:1052 · WorkspacePanel.test.tsx:1247  ← USE THIS
    src.replace(/\/\*[\s\S]*?\*\//g, "").replace(/^\s*\/\/.*$/gm, "")

    // ChatLayout.launch.test.tsx:458  ← DO NOT COPY: the second replace is UNANCHORED and eats from
    // any `//` to end of line, INCLUDING the `//` inside a string literal such as "https://…"

This matters concretely: the sweep reads `OutputFileCard.tsx`, which carries `"/sandbox-outputs/…"`-shaped
strings and a `url.startsWith("/")` branch, and `fileIcon.tsx`. The unanchored form would silently eat
code and make every `not.toMatch` pass.

**The guard template — `components/workflows/library/rowIdentity.test.ts:99-115`**, which exists BECAUSE
of the 192.1 finding. Three things to copy, each load-bearing:
  1. **length AND identity** per import — `expect(source.length).toBeGreaterThan(N)` plus
     `expect(source).toContain("<a symbol unique to that file>")`
  2. **a stripper non-vacuity case** — a token present in PROSE only, asserted present in `source` and
     absent from `code`. ⚠ **`195-PATTERNS.md` recommends this as a 27th plant and it is ADOPTED here:
     without it, a mis-stripping `codeOf` that returns `""` makes every sweep arm pass green.**
  3. **four `?raw` imports guarded INDEPENDENTLY** — the multi-source precedent is
     `pages/__tests__/PublishedCardDelete.test.tsx:692-693` + `:728` and
     `library/librarySubtree.fences.test.ts:164-184`, whose own comment is the 192.1 incident.

**⚠ `OutputFileCardProps` is NOT exported** (`OutputFileCard.tsx:39`), so P7(b)'s prop-set fence must be
a **source** assertion over `OutputFileCard.tsx?raw` — there is no type to import — and **exporting it
would itself be the public-surface change the fence exists to forbid.**

**Pins this phase moved, all reconciled HERE** (this plan is the phase's single pin-reconciliation
point, so no two same-wave plans ever contend for this file):
  - `WorkflowRunPage.test.tsx` — grew in plan 02 and again in plan 06
  - `FilesSection.test.tsx` — grew in plan 05
  - `fileIcon.test.tsx` — grew in plan 03
  - `FileRow.test.tsx`, `fileRowUtils.test.ts` — pinned by plan 03, verify they still match
  - `OutputFileCard.baseline.test.tsx` — pinned by plan 02, verify it still matches
  - `FileRow.sweep.test.ts` — **new here**, so its `TARGETS` and `BASELINE` entries land in THIS commit

**⚠ The gate ERRORS (exit 2), not fails, on a `TARGETS` path that does not exist** — `ls`-confirm before
writing. **⚠ The BASELINE key space is GLOBAL (bare filename)** — confirm uniqueness with
`git ls-files | grep -c`. **⚠ Read every number from the gate's own printed `actual` column across TWO
agreeing runs**, never by hand-counting `it(` literals.
</interfaces>

⚠ **Worktree:** `bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert the dispatched base
SHA. `GSD_VITEST_MAX_WORKERS=2` on every vitest run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SC#2 sweep — four guarded ?raw arms, the stripper non-vacuity case, and the chat-capability arm (P6, P7b)</name>
  <files>frontend/src/components/files/__tests__/FileRow.sweep.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/library/rowIdentity.test.ts` :85-124 — the guard template, the stripper non-vacuity case, and the recorded 187-24 rule about source fences over non-comment lines
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` :150-210 — the 192.1 empty-string incident in its own words
    - `frontend/src/pages/WorkflowRunPage.test.tsx` :1052-1063 — the line-anchored `codeOf` and its self-proving case
    - The four post-conversion sources: `frontend/src/pages/WorkflowRunPage.tsx`, `frontend/src/components/panel/FilesSection.tsx`, `frontend/src/components/chat/OutputFileCard.tsx`, `frontend/src/lib/fileIcon.tsx`
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` rows P6 and P7
  </read_first>
  <action>
Create `frontend/src/components/files/__tests__/FileRow.sweep.test.ts`. Its docblock states what SC#2 is,
that three of the four files it reads had **no** source fence before this phase, and that its guards are
the only thing preventing it from passing vacuously.

Define a local `codeOf` using the **line-anchored** form, and give it its own self-proving case first
(the `:1052-1063` shape): a synthetic sample where the token appears in a block comment and in a line
comment and in real code, asserting it survives only in the code.

**Non-vacuity, per swept file** — four `?raw` imports, each with its own case asserting:
  - `expect(source.length).toBeGreaterThan(N)` with N chosen well below the measured size
  - `expect(source).toContain("<a symbol unique to that file>")` — e.g. `run-deliverables` for the page,
    `Workspace files` for the panel section, `resolveOutputUrl` for the chat card, `EXT_MAP` for the icon
    module. *Identity, not just size: a non-empty sweep of the WRONG file is the same bug*
    (`StopControl.baseline.test.tsx:575-582`).
  - **the stripper non-vacuity pair (the adopted 27th plant)** — a token that exists only in that file's
    prose (pick a real one, e.g. a `D-`decision id or a phase number in a docblock), asserted
    `expect(source).toContain(tok)` **and** `expect(code).not.toContain(tok)`. Without this, a
    mis-stripping `codeOf` returning `""` makes every arm below pass.

**The SC#2 arms, over `codeOf(source)` and never over raw** — all four converted files carry docblocks
that legitimately discuss `formatBytes` and `iconFor` by name, and a raw sweep would **red on its own
explanation** (the 187-24 lesson, hit six times in this repository):
  1. `WorkflowRunPage.tsx` — no `function formatBytes`, no `function baseName`, no `function iconFor`
  2. `FilesSection.tsx` — no `function formatBytes`, no `function iconFor`
  3. `OutputFileCard.tsx` — no `function formatBytes`; and **exactly one** row-markup root in the live
     branch (count the row's own layout class signature, or count `<FileRow` occurrences and assert 2 —
     one per branch — whichever is the more direct measurement of "one row markup"; state which you chose
     and why)
  4. `fileIcon.tsx` — it IS the one icon path: assert `EXT_MAP` and the exported `fileIcon` are here, and
     that no OTHER in-scope file declares an extension-to-glyph map (no `OOXML_DOCX` const outside this
     module, no `codeExts` array anywhere in the three converted files)
  5. positive controls: for each arm, assert the same needle DOES match a planted string containing it,
     so a typo in a needle cannot pass as an absence

**The chat-capability arm (P7b), titled so `-t "chat capability"` selects it:**
  - the prop set of `OutputFileCardProps` is unchanged — a source assertion over
    `OutputFileCard.tsx?raw` listing exactly the six declared keys (`filename`, `url`, `size`,
    `supersedes`, `is_hero`, and the `variant` prop) and asserting **no seventh** appears inside the
    interface block. ⚠ It must be a SOURCE assertion: the interface is not exported and must stay that
    way.
  - `grep`-equivalent assertions that the interface is still **not** exported, and that
    `dangerouslySetInnerHTML` appears in none of the four swept files.

⚠ Also assert, as a recorded boundary rather than as a sweep, that this suite deliberately does **not**
read `frontend/src/lib/fileIcons.tsx` (the documents/health `getFileIcon`) or
`frontend/src/components/panel/SeamCard.tsx` (**the FIFTH presentation, which CONTEXT does not name** —
a `workspace_write` chip with a hardcoded glyph whose own docblock at `:10` falsely claims it *"reuses
OutputFileCard's chip shape"* while importing neither `fileIcon` nor `formatBytes`). Put that in the
docblock with the reason each is out of scope. **This converts a future SC#2 dispute — *"you left a
fifth one"* — into a recorded boundary.**
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/files/__tests__/FileRow.sweep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The suite exists, passes with 0 failing, and its exact count is recorded for task 3's pin
    - It contains exactly four `?raw` imports, and `grep -c "toBeGreaterThan"` returns ≥ 4
    - Each `?raw` import has a paired `toContain` identity assertion naming a symbol unique to that file
    - A stripper non-vacuity pair exists per swept file: `source` contains the prose token, `code` does not
    - `grep -c "replace(/\\\\/\\\\/\\[^\\\\n\\]\\*/g" frontend/src/components/files/__tests__/FileRow.sweep.test.ts` returns **0** — the unanchored `ChatLayout` stripper was NOT copied
    - A case titled to match `-t "chat capability"` exists and asserts the six-key prop set with no seventh
    - The docblock names `frontend/src/lib/fileIcons.tsx` and `frontend/src/components/panel/SeamCard.tsx` as deliberate exclusions, with reasons
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` equals the `195-BASELINE.md` figure
  </acceptance_criteria>
  <done>SC#2 has a fence that reads all four in-scope files, cannot pass on an empty string, cannot red on its own docblocks, and records the two presentations it deliberately does not sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the sweep RED — one plant per swept file, plus the two P7 arms</name>
  <files>frontend/src/components/files/__tests__/FileRow.sweep.test.ts</files>
  <read_first>
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` rows P6 and P7, and the header's plant-discipline warnings
    - `.planning/phases/195-show-the-deliverable/195-BASELINE.md` — `BASE_SHA` and the two md5 sums
    - `.planning/phases/195-show-the-deliverable/195-04-SUMMARY.md` § the P7(a) plant already driven there
  </read_first>
  <action>
**P6 — FOUR plants minimum, one per swept file, because a single-file plant leaves the other three arms
unproven.** Each is a real edit to production source, run, observed RED with the failing case NAMES
recorded, then restored **md5-identical** (record both sums):
  - (a) re-add `function formatBytes` to `frontend/src/pages/WorkflowRunPage.tsx` → arm 1 reds
  - (b) re-add `function formatBytes` to `frontend/src/components/panel/FilesSection.tsx` → arm 2 reds
  - (c) re-add an inline `iconFor` (with a `codeExts` array) to either converted file → arms 2/4 red
  - (d) add a second row-markup root to `frontend/src/components/chat/OutputFileCard.tsx` → arm 3 reds

**Plus the stripper plant, which is what makes the 27th plant real:** break `codeOf` so it returns `""`
and confirm the four **non-vacuity** cases red — **not** the SC#2 arms, which would all pass. ⚠ If the
non-vacuity cases do NOT red, the guard is inert and that is the exact 192.1 failure this suite exists to
prevent: fix it and record the finding.

**P7(a) — re-assert, at the phase's final state.** `git diff --numstat <BASE_SHA> HEAD -- frontend/src/components/chat/MessageItem.tsx frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx`
must still be EMPTY now that all three conversions have landed, with
`git merge-base --is-ancestor <BASE_SHA> HEAD` proving the base, and both md5 sums matching
`195-BASELINE.md`. ⚠ **A `numstat`-empty check passes trivially when the base SHA is wrong** — assert the
base, never assume it. Plan 04 drove the plant; this re-assertion is the phase-level statement.

**P7(b) — plant a fourth optional prop** into `OutputFileCardProps` → the chat-capability case must red.
**P7(c) — plant a second `<button>`** inside a chat row → the DOM control-count case in
`OutputFileCard.baseline.test.tsx` must red (run that suite, not this one, for this arm; name it in the
record).

⚠ **A plant that produces a SKIP rather than a RED has proven nothing** (194-12). ⚠ **Read WHICH cases
failed, never just how many** (194-06 shipped a fence vacuous on its own RED run). ⚠ **`assert`
short-circuits**, so where one plant cannot exercise every clause in a case, add plants until each clause
has been shown to fail — 194-10 needed six where two were named and 194-12 needed eleven. ⚠ **A fence
that fails to red is a FINDING**, to be recorded and fixed.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/files/__tests__/ src/components/chat/__tests__/OutputFileCard.baseline.test.tsx src/components/panel/__tests__/FilesSection.test.tsx src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Four P6 plants recorded, each naming the file edited, the failing case NAMES, the RED output line, and md5 before == after
    - The stripper plant recorded, showing the four non-vacuity cases red and (explicitly) that the SC#2 arms did NOT
    - `git merge-base --is-ancestor <BASE_SHA> HEAD` exits 0 and the SHA is quoted
    - `git diff --numstat <BASE_SHA> HEAD -- MessageItem.tsx ExecuteCodeBody.tsx` is EMPTY and both md5 sums match `195-BASELINE.md`
    - P7(b) and P7(c) plants recorded with failing case names and md5 restored
    - Every file touched by a plant is verified md5-identical to its pre-plant state at the end of the task
    - All four in-scope suites green after restoration
  </acceptance_criteria>
  <done>Every SC#2 arm has been shown to fail against a real re-duplication, the guards have been shown to fail against a broken stripper, and chat's public capability is pinned against a base that was itself proved.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile every count-gate pin this phase moved, and adopt the sweep</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` :1-120, :2113-2220, :2360-2400 — the contract, the two knobs, the timing rule and the file-level house rule
    - `.planning/phases/195-show-the-deliverable/195-02-SUMMARY.md`, `195-03-SUMMARY.md`, `195-05-SUMMARY.md`, `195-06-SUMMARY.md` — the `actual` figures each recorded for reconciliation here
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` row P10
  </read_first>
  <action>
Add `src/components/files/__tests__/FileRow.sweep.test.ts` as a **file-level** `TARGETS` entry and a
`BASELINE` pin, in this commit — the commit that created the file. ⚠ Not the bare directory
`src/components/files`: it would silently adopt every future suite in this phase's own new directory,
the ownership trap the script argues against three times. ⚠ `ls`-confirm the path first — a
non-existent `TARGETS` path makes the gate **ERROR (exit 2)**, not fail. ⚠ Confirm the bare filename is
unique tree-wide — the BASELINE key space is global.

Then **raise every pin this phase moved to the number the gate itself printed**:
`WorkflowRunPage.test.tsx` (grew in plans 02 and 06), `FilesSection.test.tsx` (plan 05),
`fileIcon.test.tsx` (plan 03), plus verify `FileRow.test.tsx`, `fileRowUtils.test.ts` and
`OutputFileCard.baseline.test.tsx` still equal their plan-03 / plan-02 pins.

⚠ **Read every number from the gate's own printed `actual` column, across TWO agreeing runs.** Never
hand-count `it(` literals — the rule the script states about itself, and the reason it prints that
column at all. Quote both runs in the SUMMARY.

⚠ **Why raising matters even though an increase is permitted:** the contract is *no per-file DECREASE*.
A pin left below `actual` leaves slack, and slack is exactly how a case gets deleted later without the
gate noticing — the `WorkspacePanel.test.tsx` pin was found twelve cases behind for that reason. Every
Phase-195 pin ends this plan **AT** its actual.

Write one comment block above the changes carrying: (1) the plan (`195-07`), (2) that the sweep's entry
lands in the same commit as the file, (3) what would be unguarded without the sweep — *"three of the
four in-scope source files had no source fence before Phase 195; without this entry the only artefact
proving SC#2 would itself be outside CI"*, (4) each raised pin with its OLD and NEW value side by side
and the plan that grew it, and (5) that the numbers came from the printed `actual` across two agreeing
runs.

Finally, run **both knobs** and record both: the gate, and the explicit ungated command from
`195-VALIDATION.md`. ⚠ **The gate covers a minority of this phase's suites; a green gate alone is the
symptom, not the reassurance.** Also re-run `tsc -p tsconfig.app.json` and confirm it still equals the
`195-BASELINE.md` figure.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 node ../scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FileRow.sweep.test.ts" scripts/vitest-count-gate.cjs` returns ≥ 2 (TARGETS + BASELINE)
    - `grep -n '"src/components/files"' scripts/vitest-count-gate.cjs` returns nothing
    - The comment block shows OLD → NEW for each raised pin and names the plan that grew each
    - The gate VERDICT line reads `OK` with `failed 0` and no `[count-decrease]` / `[missing-file]`, on TWO agreeing runs, both quoted verbatim
    - Every Phase-195 pin equals the gate's printed `actual` for that file (record the gate's per-file lines for all seven suites)
    - The explicit ungated command from `195-VALIDATION.md` passes with 0 failing
    - `tsc -p tsconfig.app.json` equals the `195-BASELINE.md` figure
    - The gate's `pinned total` and grand `total` are both recorded and both exceed the `195-BASELINE.md` values
  </acceptance_criteria>
  <done>The sweep is inside the gate, every pin this phase moved sits at its measured actual with the old value visible beside it, and both knobs are green with their verdict lines on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **none** | this plan adds one test file and edits one CI script; it ships no runtime code, no route, no fetch, no persistence and no dependency |
| CI trust | the gate is what a future phase relies on to notice a deletion, so the integrity of its numbers IS the asset here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-07-01 | Repudiation | the sweep's own validity | mitigate | A sweep that reads the empty string passes every `not.toMatch` and reports green while measuring nothing (192.1, measured). Mitigated by a length AND identity guard per `?raw` import, a stripper non-vacuity pair per file, and task 2's plant that breaks `codeOf` and requires the non-vacuity cases — not the SC#2 arms — to red. |
| T-195-07-02 | Repudiation | the gate's numbers | mitigate | Every pin is read from the gate's printed `actual` across two agreeing runs, and both runs are quoted. Pins end AT actual rather than below it, because slack is how a future deletion passes unnoticed. |
| T-195-07-03 | Denial of Service (CI) | `scripts/vitest-count-gate.cjs` | mitigate | A `TARGETS` path that does not exist makes the gate exit 2 for every later plan and every later phase. Mitigated by `ls`-confirming the path in the same commit and by running the gate as this task's verify. |
| T-195-07-04 | Tampering | production source during plants | mitigate | Eight plants edit real production files. Each is restored and **verified md5-identical**, with both sums recorded. A plant left behind would ship a re-duplication inside the commit that claims to forbid one. |
| T-195-07-SC | Tampering | package installs | n/a | **No package installed.** `frontend/package.json` and `package-lock.json` untouched — assert `git diff --numstat` EMPTY. No `slopcheck` owed. |

**Honest scale statement:** there is no application attack surface here. The asset this plan protects is
**evidence integrity** — a fence that cannot fire and a pin that carries slack are both ways of shipping
a claim with nothing behind it, and this project has measured 17 such fences across four recent phases.
</threat_model>

<verification>
- `FileRow.sweep.test.ts` green, four guarded `?raw` arms, the stripper proved to strip, the unanchored stripper NOT copied
- Four P6 plants + the stripper plant + P7(b) + P7(c) all observed RED with failing case names; every file md5-restored
- `MessageItem.tsx` and `ExecuteCodeBody.tsx` byte-identical to the named `BASE_SHA`, with the base proved an ancestor
- The sweep adopted file-level; every Phase-195 pin equal to the gate's printed `actual`, old values visible beside new
- Gate `OK` / `failed 0` on two agreeing runs; ungated command green; `tsc` at the baseline figure
</verification>

<success_criteria>
SC#2 is a measurement: exactly one byte formatter, one icon path and one row markup exist in the tree,
proved by a fence that has been shown to fail once per file and that cannot pass on an empty string —
and it runs in CI, as do every other suite this phase touched, each pinned at its measured value.
</success_criteria>

<output>
Create `.planning/phases/195-show-the-deliverable/195-07-SUMMARY.md` when done.
</output>
