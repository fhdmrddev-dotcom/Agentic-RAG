---
phase: 195-show-the-deliverable
plan: 08
type: execute
wave: 4
depends_on: ["195-04", "195-05", "195-06"]
files_modified:
  - .planning/ROADMAP.md
  - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md
  - .planning/phases/195-show-the-deliverable/195-CONTEXT.md
  - .planning/reported-bugs/BUG-260816-05-canvas-run-surface-cannot-answer-human-step.md
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .planning/seeds/SEED-169-runcard-file-badge-reads-zero-for-a-workflow-deliverable.md
  - .planning/seeds/SEED-170-records-that-name-outputfilecard-for-work-it-does-not-do.md
  - .planning/phases/195-show-the-deliverable/195-VALIDATION.md
  - .planning/phases/195-show-the-deliverable/195-UAT.md
autonomous: false
requirements: [RUN-02, RUN-03]

must_haves:
  truths:
    - "The two records this phase depends on no longer point at retired code, and both originals are preserved beside the corrections"
    - "The hot-file ledger has a row AND a matching section for every file this phase touched, with triples re-derived at execution time"
    - "The measured lies this phase declines to fix are filed as seeds with concrete re-open triggers"
    - "D-20: A human launched a real workflow, watched the run surface, downloaded the deliverable and OPENED it — and compared chat, panel and run page side by side. Wire format and screenshots are explicitly insufficient, and the row is NOT located by getElementById (192.1 D-27)"
  artifacts:
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "New sections for OutputFileCard.tsx and FilesSection.tsx, an updated WorkflowRunPage.tsx section, and young-file bullets for the new module"
      contains: "OutputFileCard.tsx"
    - path: ".claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md"
      provides: "The 095.1 hero reversal recorded at every stale site, so the next UI phase cannot build a hero block from this record"
      contains: "095.1"
    - path: ".planning/phases/195-show-the-deliverable/195-UAT.md"
      provides: "The driven G-4 lived-experience evidence, including the opened .docx and the three-surface comparison"
      contains: "opened"
  key_links:
    - from: "CLAUDE.md § Hot-file ledger table"
      to: "docs/HOT-FILE-LEDGER.md sections"
      via: "the SAME-COMMIT SYNC RULE — a row without a section, or a section without a row, is drift"
      pattern: "HOT-FILE-LEDGER"
    - from: ".planning/ROADMAP.md Phase 195 SC#3"
      to: "Phase 095.1 D-095.1-06"
      via: "the correction citing the reversal that retired the hero/working split"
      pattern: "095.1"
---

<objective>
Close the phase's record obligations: correct the stale documents this phase depends on, sync the
hot-file ledger under the same-commit rule, file the measured lies the phase declines to fix, and drive
D-20's lived-experience acceptance.

Purpose: **a record that is present and WRONG answers the auditor and STOPS the audit.** ROADMAP SC#3 and
the design record both describe a pattern Phase 095.1 retired; left alone, the next UI phase builds a
hero block from the skill. Two of the files this phase converted are **invisible to the hot-file ledger
entirely**. And RUN-02's bar is not that the canvas can be watched — it is that work can be **finished**
there, which only a human downloading and opening the file establishes.
Output: four corrected records, the ledger sync across two files, two seeds, the validation sign-off and
the driven G-4 evidence.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/195-show-the-deliverable/195-CONTEXT.md
@.planning/phases/195-show-the-deliverable/195-RESEARCH.md
@.planning/phases/195-show-the-deliverable/195-VALIDATION.md
@.planning/phases/195-show-the-deliverable/195-BASELINE.md
@.planning/phases/195-show-the-deliverable/195-04-SUMMARY.md
@.planning/phases/195-show-the-deliverable/195-05-SUMMARY.md
@.planning/phases/195-show-the-deliverable/195-06-SUMMARY.md
@.planning/phases/195-show-the-deliverable/195-07-SUMMARY.md

@CLAUDE.md
@docs/HOT-FILE-LEDGER.md

⚠ **THE HOUSE RULE THAT GOVERNS EVERY EDIT IN THIS PLAN: correct BESIDE the original, NEVER over it.** It
is a code-level pattern with three shipped instances (`phaseGlyph.tsx:107-119`,
`StopControl.baseline.test.tsx:517-520`, `rowIdentity.test.ts:89-98`), not a planning-document nicety.
Quote what was there, say what is true now, and say **why** it changed.

⚠ **This plan is `autonomous: false`** because task 3 needs a browser and a `.docx` reader. Worktree
executors have neither — Chrome MCP lives with the **orchestrator**. Tasks 1 and 2 are executor work;
task 3 is a blocking checkpoint whose evidence is written up afterwards.

⚠ **This plan writes NO source code.** `files_modified` contains no path under `frontend/` or `backend/`.
If it did, plan 07's SC#2 sweep would be measuring a tree that changed after the sweep ran.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct every stale record this phase depended on (D-11, plus two the phase measured)</name>
  <files>.planning/ROADMAP.md, .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md, .planning/phases/195-show-the-deliverable/195-CONTEXT.md, .planning/reported-bugs/BUG-260816-05-canvas-run-surface-cannot-answer-human-step.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 195: Show the Deliverable" — the three success criteria, SC#3 being the stale one
    - `.claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md` — the **nine** stale sites enumerated in `195-RESEARCH.md` § "CONTEXT premise verification", D-11 row: `:5`, `:61` (§C heading, *"winner: A, Hero block"*), `:63-69`, `:83-84`, `:101` (the build-once inventory row), `:160-166` (the `.hero` CSS and the `.your` "★ Your file"), `:242` (`<div class="hero">` in the HTML fragment), `:272-273` (the ❌ anti-pattern), `:286`
    - `frontend/src/components/chat/OutputFileCard.tsx` :52-63 — the shipped docblocks recording that `variant` is INERT and `is_hero` is written-but-unread
    - `.planning/phases/195-show-the-deliverable/195-CONTEXT.md` D-10, D-11, and the `<code_context>` claim that `fileIcon.tsx` *"needs consumers, not changes"*
    - `.planning/reported-bugs/BUG-260816-05-canvas-run-surface-cannot-answer-human-step.md` — its frontmatter routing and its capability table
    - `./CLAUDE.md` § "Reported bugs cross-check" — the touchpoint obligation
  </read_first>
  <action>
Four records, each corrected beside its original.

**(1) `.planning/ROADMAP.md` Phase 195 SC#3.** It reads *"Multiple produced files are handled with the
shipped hero/working split, not a new pattern."* ⚠ **That criterion CANNOT be satisfied — it names a
RETIRED pattern.** Phase 095.1 (D-095.1-06) reversed the hero/working split by an operator-approved
decision: `OutputFileCard`'s `variant` prop is inert and `is_hero` is written-but-unread. Rewrite SC#3 to
name what actually ships — ONE uniform quiet row for every file, no hero, newest-first — **preserving the
original sentence verbatim in a ⚠ correction note** citing 095.1 / D-095.1-06 as the reason and D-10 as
the decision, and recording the scale fact that made it right: **60 of 61 file-bearing runs have exactly
ONE file**, so reviving a hero signal would build a pattern for a population of one. Note that the
criterion was corrected **by** the phase it belongs to, which is why the phase can satisfy it.

**(2) `.claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md` — NINE sites,
not the four CONTEXT names.** ⚠ **Left alone, the next UI phase builds a hero block from this record**;
that is why this is in scope rather than tidy-up. Record the reversal at **every** site, as an enumerated
set rather than one note at §C:
  - a prominent ⚠ banner near the top (`:5`) stating the reversal, its phase and its decision id, and
    that everything below describing a hero/working split is HISTORICAL
  - at §C (`:61`), the prescription (`:63-69`), *"Why Hero block won"* (`:83-84`) and `:286`: the winner
    is marked SUPERSEDED with the reversal named
  - at the build-once inventory row (`:101`): `OutputFileCard` no longer has Hero/Working variants, and as
    of Phase 195 its markup lives in the shared `components/files/` row
  - at the `.hero` CSS (`:160-166`) and the `<div class="hero">` fragment (`:242`): mark them as
    describing retired markup that no longer exists in the app
  - ⚠ at `:272-273`: the ❌ anti-pattern currently forbids *"a flat, index-ordered output list with no
    hero"* — **which is exactly what ships.** Invert it in place with the original quoted, and note that
    Phase 195 additionally makes the list newest-first client-side (D-12) rather than index-ordered.
  - ⚠ **also correct `:274`'s icon rule beside its original**, because Phase 195 changed the facts it
    asserts. It forbids the flat monochrome form, while the panel's flat 16 px
    `text-panel-muted-foreground` glyph is a deliberate Phase 088-05 AA-contrast decision — the two design
    records disagreed. Record that Phase 195 resolved it by parameterising the ONE icon module
    (`ribbon` / `tone` / `className` / `mimeType`) so both surfaces keep their shipped appearance from one
    code path, and record the resulting glyph delta (`FileSpreadsheet → Table`, `FileCode → Code`,
    `FileImage → Image`, `File → FileText`) as a decision rather than a regression.
  ⚠ Delete no sketch history. The point of the file is the record; the point of the correction is that a
  reader cannot mistake the record for current state.

**(3) `195-CONTEXT.md` `<code_context>`.** Append a dated ⚠ correction note beside the *"`fileIcon.tsx` —
already the intended single icon source; needs consumers, not changes"* bullet: Phase 195 changed it,
**additively**, because adopting it in the panel and the run page without options would have discarded a
deliberate AA-contrast decision and regressed nine code extensions to a worse glyph. Quote the original
bullet. D-05's *exactly ONE icon path* is unchanged and was honoured.

**(4) `BUG-260816-05`.** Its capability table records *"see produced output files | canvas run surface
❌"*. Measured, that row is wrong — the surface has listed and downloaded them since Phase 188-10 — and
Phase 195 unified their presentation and made the region's label honest. Correct the row **beside** the
original and note that the FILE half is now delivered. ⚠ **Leave `status: deferred` and the ask-responder
half exactly as they are, and do not touch `re_open_trigger`.** The frontmatter's own comment explains
why the status must not flip to `folded`: a `folded` record is invisible to the open-report routing scan,
and the ask half must still reach Phase 198 / NODE-02.
  </action>
  <verify>
    <automated>grep -c "095.1" .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 195 SC#3 no longer instructs the hero/working split, and the original sentence is present verbatim inside a correction note citing 095.1 / D-095.1-06 and D-10
    - `grep -c "095.1" .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md` returns ≥ 9
    - A SUPERSEDED / HISTORICAL / reversed marker is present at or adjacent to each of the nine enumerated sites
    - The ❌ anti-pattern entry forbidding a flat no-hero list is inverted in place with its original quoted
    - The icon rule carries a correction naming `ribbon`, `tone`, `className`, `mimeType` and the four-item glyph delta
    - `195-CONTEXT.md` contains a dated correction note quoting the *"needs consumers, not changes"* bullet
    - `BUG-260816-05`'s YAML frontmatter is byte-unchanged (`git diff` shows changes only after the closing `---`), `status:` still reads `deferred`, and the capability row carries a correction beside the original
    - `git diff --numstat` for this task touches no path under `frontend/` or `backend/`
  </acceptance_criteria>
  <done>No record this phase depended on still points at retired code, every original is preserved beside its correction, and the one report routed here has its measured error fixed without disturbing the half still owed to Phase 198.</done>
</task>

<task type="auto">
  <name>Task 2: Hot-file ledger under the same-commit sync rule (D-17), two seeds, and the validation sign-off</name>
  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md, .planning/seeds/SEED-169-runcard-file-badge-reads-zero-for-a-workflow-deliverable.md, .planning/seeds/SEED-170-records-that-name-outputfilecard-for-work-it-does-not-do.md, .planning/phases/195-show-the-deliverable/195-VALIDATION.md</files>
  <read_first>
    - `./CLAUDE.md` § "Hot-file ledger" — the table, ⚠ the re-derive recipe (three commands, six-digit dated quick-task buckets subtracted) and ⚠ the SAME-COMMIT SYNC RULE
    - `docs/HOT-FILE-LEDGER.md` :523-535 (the `WorkflowRunPage.tsx` section, whose closing sentence names five concerns and predicts this phase taking one) and :537-555 (`## Young files`, plus the verbatim paragraph those bullets replaced)
    - `.planning/phases/195-show-the-deliverable/195-BASELINE.md` — the triples measured at wave 1, to be RE-DERIVED not copied
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` § "Hot-file ledger obligations (R5)" — the nine owed edits in table form
    - `frontend/src/components/chat/RunCard.tsx` :179-189 — the badge's `output_files` reduce
    - `backend/app/services/tool_dispatcher.py` :3570 and `frontend/src/components/panel/SeamCard.tsx` :10 — the two false comments
    - `.planning/seeds/SEED-168-the-class-of-work-knowledge-derived-human-accountable.md` — a shipped seed's shape, to match
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` — the Per-Task Verification Map and the Sign-Off list
  </read_first>
  <action>
**(A) Re-derive every triple AT THIS COMMIT.** Do not copy `195-BASELINE.md`'s wave-1 figures and do not
copy RESEARCH's — this phase's own commits have landed since both. Run the recipe's three commands per
file (`git log --oneline | wc -l`; the `sed` bucket pipeline; `wc -l`), **subtract six-digit dated
quick-task buckets and non-numeric buckets**, and print the derivation so the numbers are auditable rather
than asserted. ⚠ **A row that is present and WRONG answers the auditor and stops the audit** — five of
thirty-one rows were found stale on 2026-08-17 and three of them read `satisfied`.

**(B) `CLAUDE.md` rows AND `docs/HOT-FILE-LEDGER.md` sections, in this same commit.** A row without a
section, or a section without a row, is drift.
  - **ADD** `frontend/src/components/chat/OutputFileCard.tsx` — was `7 / 6 / 187`; G-5 **FIRES** (6 vs a
    threshold of 3); disposition *honoured by construction (195)*. ⚠ **ABSENT from the ledger entirely
    today**, so a FULL section is owed, not a young-file bullet. The section records the phases touched,
    that Phase 195 converted its interior onto the shared row while keeping its **public props
    byte-identical** and its two call sites unopened, and the invariants that now bind it: the dead-link
    state's affordance is a `<span aria-disabled>` and **not a `<button>`**, the `Replaces:` subline is
    written in BOTH branches, `variant` is inert, `is_hero` is unread, the `VITE_API_BASE_URL` read and
    the preserved static `href` stay local, and the in-row error auto-clears after 3 s while the run
    page's does not. Name the next seam.
  - **ADD** `frontend/src/components/panel/FilesSection.tsx` — was `6 / 3 / 277`; G-5 **FIRES** at the
    threshold; *honoured by construction (195)*. ⚠ Also absent today, so a FULL section. Record the
    duplications deleted (the copied `formatBytes` **and its own "keep byte-for-byte identical"
    confession**, the inline mime-first `iconFor`) and the invariants that bind it: the section owns
    `useViewingThread`, the preview activation, the listbox/option roles, the roving `rowRefs` focus — so
    the shared row must forward its ref — the fresh-write flash, `TemplateUpload`, the Template badge as
    `trailingSlot`, and the full-path label. Record the glyph delta.
  - **UPDATE** `frontend/src/pages/WorkflowRunPage.tsx` — 195 makes it the **4th** phase. ⚠ Its existing
    section already predicts this: *"the run read + its poll, the phase/spec join, the elapsed anchor,
    **the deliverable list** and the canvas mount are five concerns in one 1101-line component."*
    **Record the seam as TAKEN, beside the prediction, never over it** — 195 took exactly one of the five
    it named — and name the remaining four for the next phase.
  - **ADD young-file bullets** to `## Young files` (matching the `StopControl.tsx` / `ThreadRunLine.tsx` /
    `ActiveRunsTray.tsx` shape) for `frontend/src/components/files/FileRow.tsx`,
    `frontend/src/components/files/fileRowUtils.ts` and `frontend/src/lib/fileIcon.tsx`, each with its
    re-derived triple and the note that a third phase earns a full section. The `fileIcon.tsx` bullet
    carries the ledger's own standing lesson as its reason: `WorkflowsPage.tsx` escaped G-5 for ten phases
    purely by not being written down.
  - ⚠ **`MessageItem.tsx` and `RunCard.tsx` owe NOTHING — and record WHY**, because it is a structural
    claim rather than luck: `OutputFileCard`'s public prop shape stayed byte-identical, so neither file was
    opened (proved by plan 07's `git diff --numstat` against the named `BASE_SHA`). Note that if a future
    plan widens those props, `MessageItem.tsx` (29 phases, extraction due, obligation **UNDISCHARGED**) is
    opened and a refactor recommendation is owed FIRST.
  - ⚠ **Do NOT add a `Phase 195` entry under `STATE.md → Guardrail overrides`.** G-5 fires on five files
    and is honoured by construction — the extraction IS the requirement — and an override was offered and
    DECLINED for the sixth consecutive phase. **The absence of an entry is a MEASUREMENT.** Say so in the
    SUMMARY so a later reader can tell "declined" from "forgotten".

**(C) File two seeds**, each matching a shipped seed's shape and each carrying a concrete, non-date
re-open trigger.
  - **`SEED-169` — `RunCard`'s file badge reads 0 for a real workflow deliverable, and `ThreadRunLine`
    shows no file at all.** Measured: the badge derives its count by parsing `output_files` out of
    `tool_calls` (`RunCard.tsx:179-189`), while a workflow emit returns `path` / `output_file`
    (`phase_types.py` `_exec_llm_emit`) — so chat says the workflow made nothing while a 38-40 KB `.docx`
    sits in the workspace. `ThreadRunLine.tsx` carries zero file data (`grep -c "output_files"` → 0).
    **A MEASURED LIE this phase declines**, per D-14, because fixing it opens `RunCard.tsx` (9 phases) and
    `MessageItem.tsx` (29, extraction due) inside a phase already converting three files. **Re-open
    trigger:** any phase opening `RunCard.tsx` or `MessageItem.tsx`, or a user reporting *"chat says my
    workflow made nothing."*
  - **`SEED-170` — two shipped records name `OutputFileCard` for work it does not do.**
    (a) `tool_dispatcher.py:3570` comments that the `workspace_file_written` event exists *"so
    OutputFileCard renders it"* — it does not; the panel's file section does, via `useWorkspaceFiles`.
    (b) `panel/SeamCard.tsx:10` claims its `workspace_write` chip *"reuses OutputFileCard's chip shape"* —
    it imports neither the icon module nor the byte formatter, and hardcodes a single glyph with no
    extension mapping. ⚠ **`SeamCard` is the FIFTH file presentation, CONTEXT does not name it, and Phase
    195 declares it explicitly OUT of scope** (a chip in a run receipt, not a file row: no size, no
    download, no icon map) — which converts a future SC#2 dispute into a recorded boundary. Both are
    one-line comment corrections and legitimate G-3 `/gsd:fast` candidates. **Re-open trigger:** the next
    phase opening either file, or any reader citing `SeamCard`'s docblock as evidence that the chip already
    shares the file presentation.

**(D) `195-VALIDATION.md` sign-off.** Fill the Per-Task Verification Map's `⬜` column from the plan
SUMMARYs. Tick the Sign-Off list. Record the **total plant count actually driven** beside the estimate of
26 plus the adopted 27th — ⚠ this project's history says the estimate runs low, not high (194-10 needed
six where two were named; 194-12 needed eleven). Name every fence that **failed to red** as a FINDING with
its resolution. Set `nyquist_compliant: true` **only if** every ❌ row is now covered; otherwise leave it
`false` and say which rows are not.
  </action>
  <verify>
    <automated>grep -c "OutputFileCard.tsx" CLAUDE.md docs/HOT-FILE-LEDGER.md && ls .planning/seeds/SEED-169-*.md .planning/seeds/SEED-170-*.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "components/chat/OutputFileCard.tsx" CLAUDE.md` ≥ 1 AND `grep -c "^## .*OutputFileCard.tsx" docs/HOT-FILE-LEDGER.md` returns 1
    - `grep -c "components/panel/FilesSection.tsx" CLAUDE.md` ≥ 1 AND `grep -c "^## .*FilesSection.tsx" docs/HOT-FILE-LEDGER.md` returns 1
    - The `WorkflowRunPage.tsx` row in `CLAUDE.md` shows a re-derived triple with 4 phases, and its ledger section records the seam as TAKEN **beside** the original five-concern prediction, which is still present verbatim
    - `## Young files` contains bullets for `FileRow.tsx`, `fileRowUtils.ts` and `lib/fileIcon.tsx`, each with a triple, and `CLAUDE.md` carries a matching row for each
    - Every triple in this commit is accompanied by the three commands that produced it, with any subtracted buckets named (or an explicit "checked, none exist")
    - `git diff -- CLAUDE.md` and `git diff -- docs/HOT-FILE-LEDGER.md` are in the SAME commit
    - `grep -c "MessageItem" docs/HOT-FILE-LEDGER.md` shows the recorded reason nothing is owed there
    - `.planning/STATE.md` is NOT modified to add a Phase 195 guardrail override (`git diff --numstat -- .planning/STATE.md` shows no override line added)
    - Both seed files exist, each with a `re_open_trigger` that names a file or an observable user report — not a date
    - `195-VALIDATION.md` has zero `⬜` remaining in the Per-Task Verification Map, a driven-plant total recorded beside the 26+1 estimate, and `nyquist_compliant` set with its justification
    - `git diff --numstat` for this task touches no path under `frontend/` or `backend/`
  </acceptance_criteria>
  <done>Every file this phase touched has a ledger row and a matching section built on numbers re-derived at this commit, the declined lies are filed with triggers rather than forgotten, and the validation contract is signed off with its findings named.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Drive D-20 — finish real work on the canvas, and compare the three surfaces</name>
  <files>.planning/phases/195-show-the-deliverable/195-UAT.md</files>
  <read_first>
    - `.planning/phases/195-show-the-deliverable/195-CONTEXT.md` D-20 and the operator's own words in `<specifics>`
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` § "Manual-Only Verifications"
    - `.planning/phases/195-show-the-deliverable/195-BASELINE.md` § "Driven evidence" — the PRE-change readings these rows are compared against
    - `./CLAUDE.md` § "Workflow guardrails" G-4, and § "UAT scoreboard recipe" — ⚠ **rows may be blocked, never silently omitted**
  </read_first>
  <what-built>
The shared file row (`components/files/`), the three converted surfaces (chat card, panel list, run-page
deliverable region), the honest region heading, and the newest-first ordering. Everything the operator
sees on the run surface now renders from one component.
  </what-built>
  <action>
Halt and hand control to the orchestrator, which owns the browser. Drive rows U1-U6 from
`<how-to-verify>` in order, comparing each against `195-BASELINE.md`'s pre-change readings, then write
`.planning/phases/195-show-the-deliverable/195-UAT.md` with one row per verdict and its evidence. Record
any undrivable row as the blocked marker with its blocking condition and the row to run first once
unblocked — never omit it.
  </action>
  <how-to-verify>
⚠ **Wire format and screenshots are EXPLICITLY INSUFFICIENT** — 192, 193.1 and 194 each recorded that
lesson. ⚠ **Frontend is at `http://localhost:5173`; vite binds `[::1]` only on this box**, so an IPv4
probe reports it down while it is running. `take_screenshot` times out — read DOM geometry via script
evaluation. ⚠ **Do NOT locate the row by `getElementById`** (192.1 D-27): a machine check that bypasses
the human's task has not verified the human's task.

**Row U1 — the canvas is a place work can be FINISHED (the phase's headline bar).** Launch
`northwind-qbr-fa65a43c` v1 from the Workflows page. Stay on the run surface. Watch the deliverable
region go from its empty reading to a populated row. Click the produced `.docx`. **OPEN THE DOWNLOADED
FILE.** Evidence: the `run_id`, the region's `h2` text (the NEW heading), the row's `aria-label` and
`title`, the downloaded file's on-disk byte size, and whether it opened cleanly.
⚠ **AVOID `compliance-gap-report`, `pm-risk-register` and `risk-register`** — zero of their runs has ever
produced a file.

**Row U2 — the honest heading.** Read the region's heading aloud and answer one question: does it claim
the run produced everything listed? It must not. Compare against `195-BASELINE.md`'s pre-change reading of
the same region, which said *"What this run produced"*.

**Row U3 — the three-surface side-by-side (RUN-03's human half).** Same thread, three places: the chat
message's output-file card, the workspace panel's file list, and the run page's deliverable row. Judge:
**do they read as the same row?** Capture each row's rendered text and describe its icon. ⚠ Compare
against `195-BASELINE.md`'s arm-3 pre-change capture — the glyph delta the phase recorded as a decision
(tables, code, images and unknown files change lucide glyph on the panel and the run page) should be
visible here, and the operator should be asked to confirm it is acceptable rather than told.

**Row U4 — newest first.** On a thread with two or more files (the template plus the produced deliverable),
confirm the just-produced deliverable sits ABOVE the template it filled — **including immediately after it
appears mid-run**, which is the regime where it carries no `created_at` at all.

**Row U5 — the panel's keyboard model still works.** In the workspace panel's file list, click a row, then
use ArrowDown / ArrowUp, then Enter to open a preview, then `‹ Files` to return. Focus must land back on
the row you opened. This is the property the shared row's ref forwarding exists for.

**Row U6 — the empty state, the most-seen state on this surface** (161 of 222 runs). Open a run that
produced nothing and confirm the reading is the terminal one, unchanged.

⚠ **Any row that cannot be driven is recorded ⛔ with its reason and its blocking condition — never
omitted.** A scoreboard that lists only what passed is not a scoreboard. If `195-BASELINE.md` already
recorded a ⛔ for the browser or the `.docx` reader, that constraint carries forward and must be stated
here as an owed row with the FIRST row to run named.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/195-show-the-deliverable/195-UAT.md` exists with one row per U1-U6, each carrying a verdict of PASS / FAIL / ⛔ and its evidence
    - U1's evidence includes the downloaded file's on-disk byte size and an explicit "opened cleanly: yes/no"
    - U2 quotes the new heading verbatim and the pre-change heading beside it
    - U3 records three rendered rows and three icon descriptions, plus the operator's explicit yes/no on the recorded glyph delta
    - U4 states whether the mid-run (no `created_at`) case was observed, not only the reconciled one
    - Every ⛔ row names its blocking condition and the row to run first once unblocked
    - The file states plainly whether the phase closes with any owed row — as a DECISION, never as a claim that everything ran
  </acceptance_criteria>
  <done>Rows U1-U6 each carry a PASS / FAIL / blocked verdict with its evidence, U1 states whether the
downloaded .docx opened cleanly, and any owed row is stated as a decision rather than as a claim that
everything ran.</done>
  <resume-signal>Paste the U1-U6 verdicts and evidence, or describe what blocked each row.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → frontend → the shipped, RLS-guarded workspace-files read and raw-bytes route | crossed READ-ONLY by task 3, through the same UI any user drives |
| **none** | tasks 1 and 2 edit markdown only; no code, no route, no persistence, no dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-08-01 | Information Disclosure | `195-UAT.md`, `195-BASELINE.md` and the corrected bug report | mitigate | These records carry run ids, thread ids, file ids and file names — all already present across `.planning/`. They MUST NOT carry a bearer token, a Supabase key, a signed URL or any `.env` value. The write-up quotes DOM readback and DB columns only. |
| T-195-08-02 | Repudiation | the hot-file ledger and the guardrail record | **mitigate** | This is the real risk of this plan. A stale-but-present row answers a future G-5 audit `satisfied` and stops it — measured five times on 2026-08-17, three of them reading `satisfied`. Mitigations: triples re-derived at THIS commit rather than copied; the derivation printed; row and section in the same commit; and the deliberate ABSENCE of a guardrail override recorded as a measurement so it cannot later read as an omission. |
| T-195-08-03 | Tampering | `BUG-260816-05`'s routing frontmatter | **mitigate** | Flipping `status` to `folded` would remove the still-owed ask-responder half from the open-report routing scan — the exact failure the report's own comment records (a `folded` record hid a live bug for two months). The acceptance criteria require the YAML frontmatter to be byte-unchanged. |
| T-195-08-04 | Denial of Service (process) | the phase's close | accept | If no browser or `.docx` reader is reachable, task 3 returns ⛔ and the phase closes with owed rows. **Accepted, and recorded as a DECISION rather than as a claim that everything ran** — Phase 194's eight rows went undriven for exactly this reason and the honest form is a stated debt with the first row to run named. |
| T-195-08-SC | Tampering | package installs | n/a | **No package installed.** No `package.json`, lockfile or `requirements*.txt` is touched — assert `git diff --numstat` EMPTY for all. No `slopcheck` owed. |

**Honest scale statement:** two of three tasks edit documentation. The genuine risk here is not an
attacker — it is a **record that is present and wrong**, which this project has measured as more harmful
than an absent one, and which every mitigation above is aimed at.
</threat_model>

<verification>
- ROADMAP SC#3 corrected with the original quoted; the design record carries the reversal at all nine sites plus the icon-rule correction
- `195-CONTEXT.md` and `BUG-260816-05` corrected beside their originals, with the bug's frontmatter byte-unchanged
- `CLAUDE.md` rows and `docs/HOT-FILE-LEDGER.md` sections added/updated in ONE commit, from triples re-derived here with their derivations printed
- No Phase 195 guardrail override added to `STATE.md`; its absence recorded as a measurement
- `SEED-169` and `SEED-170` exist with concrete re-open triggers
- `195-VALIDATION.md` signed off with the driven-plant total and every failed-to-red fence named
- `195-UAT.md` carries U1-U6 with verdicts and evidence, and any owed row stated as a decision
- No path under `frontend/` or `backend/` modified by this plan
</verification>

<success_criteria>
Every record a future auditor will read tells the truth about what ships, the ledger can see all five
files this phase touched, the lies the phase declined are filed with triggers — and a human has finished
real work on the canvas: launched a workflow, watched it, downloaded the deliverable and opened it.
</success_criteria>

<output>
Create `.planning/phases/195-show-the-deliverable/195-08-SUMMARY.md` when done.
</output>
