---
status: complete
phase: 152-workflow-run-inputs
mode: gaps-only
scope_note: "Confirms the D-04 widen gap (152-08) is closed at code + live backend + live UI. Does NOT run the whole-phase SC#10 4-axis cross-provider gate (deferred per operator) — the phase is NOT fully verified until that runs."
source:
  - 152-08-SUMMARY.md
  - 152-VERIFICATION.md (Gap #1 — D-04 widen regression)
  - 152-REVIEW.md (gap-closure re-review — 0 crit / 0 warn / 1 info)
started: 2026-07-15T02:00:00Z
updated: 2026-07-15T02:30:00Z
---

## Current Test

[testing complete — gap-closure re-verification, all 3 tests passed]

## Tests

### 1. Backend live resolver — ancestor override dropped (the gap, against the real DB)
expected: For a workflow bound to project P with phase folder_scope=[A1], a per-run override of R (a strict ancestor of P whose subtree also holds unrelated sibling P2) is DROPPED server-side — resolve_run_scope_root returns P, resolved retrieval subtree = [P, A1], with R and P2 absent. Reproduces the verifier's exact prior FAIL (root='R', subtree=['R','P','A1','P2']) and shows it now PASSES.
result: pass
evidence: "Live probe against local DB (:54322) via app.services.harness.scope.resolve_run_scope_root: root=P (15208000-..-002), subtree=[P, A1]; R in subtree? False; P2 in subtree? False. Control: override=A1 → root=A1 (narrowing still honored)."

### 2. Committed regression tests green (backend + frontend)
expected: The committed strict-ancestor regression tests pass on both sides, replacing the verifier's ad-hoc probe with real committed coverage.
result: pass
evidence: "backend test_152_folder_override.py 10 passed (incl. test_a4_ancestor_override_dropped); frontend RunModal.test.tsx ancestor-not-offered test committed (a6edab25) + code-review clean 0/0/1-info."

### 3. Run modal never OFFERS an escaping folder (frontend mirror — user-reachable surface)
expected: Run modal for "ZZ-152-08 Widen Test Workflow" offers ONLY the workflow default + "ZZ-152-08 A1"; NOT "ZZ-152-08 Root" (ancestor) or "ZZ-152-08 Sibling P2".
result: pass
evidence: "Claude drove Chrome (claude-in-chrome) against the live app. Seeded nested folders + a bound scoped published workflow; opened its Run modal. The KB-scope <select> (combobox ref_34) rendered EXACTLY two options: 'Workflow default — 📁 ZZ-152-08 Project P (bound)' + '📁 ZZ-152-08 A1 (phase scope)'. 'ZZ-152-08 Root' (ancestor) and 'ZZ-152-08 Sibling P2' (sibling) were ABSENT, as were all unrelated flat folders. Also confirmed the WR-05 truthful bound label. Seed cleaned up after (0 rows remain)."

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0

## Gaps

[none — Gap #1 (D-04 widen) confirmed CLOSED on all three surfaces: committed regression tests, live-DB resolver probe, and the live Run modal UI]

## Deferred (whole-phase SC#10 gate — NOT part of this --gaps-only re-verify)

Per operator direction (gap-focused re-verification), the full SC#10 4-axis cross-provider
manual UAT (152-VALIDATION.md) is NOT run here. It stays pending for a consolidated
cross-provider pass; WFIN-01/02/03 remain OPEN at the requirement level until then
(148-151 false-green-avoidance convention). Rows deferred:
- Folder-scope constraint across OpenAI / Anthropic / Google / OpenRouter (live LLM runs)
- Multi-tool: template upload + folder scope in one launch
- Parallel-thread isolation
- Long-message scope enforcement
- Delete-cascade destructive UAT with an in-flight run
- Template SSTI provenance
