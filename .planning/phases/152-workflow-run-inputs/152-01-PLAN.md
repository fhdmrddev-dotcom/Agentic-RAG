---
phase: 152-workflow-run-inputs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/message.py
  - backend/app/services/harness/scope.py
  - backend/app/api/threads.py
  - backend/app/services/harness_engine.py
  - backend/app/api/runs.py
  - backend/tests/test_152_folder_override.py
autonomous: true
requirements: [WFIN-02]
user_setup: []

must_haves:
  truths:
    - "A per-run folder override travels in the existing create_workflow_run.inputs jsonb (no migration) (D-01)"
    - "A never-owned or unreachable override folder_id produces no narrowing / is refused (D-05)"
    - "The author-time retrieval default is the definition's existing project_folder_id, unchanged (D-03)"
    - "Retrieval scope is bound server-side so the model cannot widen it; identical across providers (D-04)"
    - "Absent override = today's behavior — author default / whole-KB, byte-identical (D-06)"
    - "The override persists in workflow_runs.inputs and is honored at kickoff, resume, and Continue (Pitfall 5)"
    - "threads.py shrinks-not-grows: the precedence lands in a scope.py helper, not inline branches (G-5)"
  artifacts:
    - path: "backend/app/models/message.py"
      provides: "MessageCreate.folder_id optional per-run override field"
      contains: "folder_id"
    - path: "backend/app/services/harness/scope.py"
      provides: "resolve_run_scope_root() precedence + D-05 owner-reachability gate"
      contains: "resolve_run_scope_root"
    - path: "backend/app/api/threads.py"
      provides: "kickoff persists folder_id into inputs + resolves scope via helper"
      contains: "resolve_run_scope_root"
    - path: "backend/tests/test_152_folder_override.py"
      provides: "override resolves subtree, D-05 unowned refused, D-06 absent = default"
      contains: "test_"
  key_links:
    - from: "backend/app/api/threads.py"
      to: "backend/app/services/harness/scope.py"
      via: "resolve_run_scope_root call at the kickoff scope block"
      pattern: "resolve_run_scope_root"
    - from: "backend/app/services/harness/scope.py"
      to: "backend/app/utils/folder_utils.py"
      via: "fetch_visible_folders owner-reachability gate"
      pattern: "fetch_visible_folders"
    - from: "backend/app/api/threads.py"
      to: "workflow_runs.inputs"
      via: "create_workflow_run(inputs={... folder_id ...})"
      pattern: "folder_id"
---

<objective>
Add the WFIN-02 per-run folder-scope OVERRIDE channel to the workflow launch path — the backend half. A client-selected `folder_id` travels in the existing `create_workflow_run.inputs` jsonb (no migration), is owner-reachability-gated (D-05), layered on top of the definition's `project_folder_id` author default (D-03), resolved to a folder subtree server-side (D-04), and honored at all three run-start sites: kickoff, resume, and Continue (Pitfall 5).

Purpose: The read-only bound-folder chip becomes a real per-run control. The constraint MUST live at the service boundary so the model cannot widen scope and behavior is identical across OpenAI / Anthropic / Google / OpenRouter (SC#10).
Output: `MessageCreate.folder_id`, a `resolve_run_scope_root()` helper in `scope.py`, kickoff/resume/Continue wiring, and `test_152_folder_override.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-workflow-run-inputs/152-CONTEXT.md
@.planning/phases/152-workflow-run-inputs/152-RESEARCH.md
@.planning/phases/152-workflow-run-inputs/152-PATTERNS.md
@.planning/phases/152-workflow-run-inputs/152-VALIDATION.md

@backend/app/models/message.py
@backend/app/services/harness/scope.py

<interfaces>
<!-- Extracted this session — executor uses these directly, no codebase exploration needed. -->

MessageCreate (backend/app/models/message.py:8-18) — add the override the SAME nullable-default way workflow_definition_id was added:
  class MessageCreate(BaseModel): content, model, provider, agent_mode="default", workflow_definition_id: UUID | None = None
  ADD: folder_id: UUID | None = None   # WFIN-02 per-run override; malformed UUID → FastAPI 422 for free (V5)

scope.py existing seams (backend/app/services/harness/scope.py):
  - resolve_project_subtree(project_folder_id, *, supabase, user_id) -> list[str] | None   (:51)  None→None = whole-KB; owner-scoped fetch_visible_folders; cycle-guarded _walk; returns list NEVER a set (Pitfall 6)
  - assert_folder_scopes_subset(definition, *, supabase, user_id)   (:92)  every per-phase folder_scope ⊆ project subtree; ValueError→400
  - fetch_visible_folders(supabase, user_id) imported at :43 — owner-scoped folder list [{id, name, parent_id}]

threads.py kickoff seams (backend/app/api/threads.py):
  - assert_folder_scopes_subset call site      :1129
  - pin_templates_for_run call                 :1162
  - create_workflow_run(..., inputs={"kickoff_prompt": body.content}, ...)   :1343-1352  ← persist folder_id here
  - kickoff scope block (author-default → thread fallback)  :1534-1559  ← replace with helper call
  - wf_ctx build inputs={"kickoff_prompt": body.content}   :1634  ← mirror folder_id here (live ctx == durable inputs)
  - folder_subtree_ids=_wf_folder_subtree_ids passed to ctx  :1648

resume / Continue seams (re-derive scope from project_folder_id ONLY today — must layer the durable override):
  - harness_engine.py:1515  _resume_folder_subtree_ids   (run.inputs already loaded at :1571)
  - runs.py:918             _cont_subtree
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MessageCreate.folder_id + resolve_run_scope_root() helper with the D-05 owner gate</name>
  <files>backend/tests/test_152_folder_override.py, backend/app/models/message.py, backend/app/services/harness/scope.py</files>
  <read_first>
    - backend/app/services/harness/scope.py (WHOLE FILE — resolve_project_subtree :51-89 owner-scoped walk, assert_folder_scopes_subset :92, module header threat posture T-098-02, fetch_visible_folders import :43)
    - backend/app/models/message.py (:8-18 — MessageCreate, the workflow_definition_id nullable-default analog)
    - backend/tests/test_098_scope_governance.py (:154 — the patch-resolve_project_subtree + assert-run-start-binding scaffold to mirror)
    - 152-PATTERNS.md § "backend/app/services/harness/scope.py" (the owner-gate excerpt + Pitfall 4/Landmine 3 gap)
    - 152-RESEARCH.md § "WFIN-02 — Folder scope (deep dive)" (precedence, D-05 gate, composition landmine A4)
  </read_first>
  <behavior>
    - Test: override folder_id owned by the user → resolve_run_scope_root returns str(override), and resolve_project_subtree(that) is the bound subtree (patch resolve_project_subtree, assert the root passed in).
    - Test (D-05): override folder_id NOT in fetch_visible_folders → helper drops the override and falls back to the author default (project_folder_id) — never scopes to the unowned id.
    - Test (D-06): no override present (run_inputs absent / no folder_id key) → helper returns the author default project_folder_id; when project_folder_id is None → returns the thread folder fallback → None = whole-KB (byte-identical to today).
    - Test (A4 composition): definition declares a per-phase folder_scope AND override root is NOT within the project subtree → override dropped (would silently empty the phase intersection); an override root within the subtree is honored.
    - Test: helper NEVER returns a set (returns str | None); fetch_visible_folders is consulted exactly once when an override is present.
  </behavior>
  <action>
    Add `folder_id: UUID | None = None` to `MessageCreate` (message.py) mirroring the `workflow_definition_id` nullable-default line — absence IS the D-06 path.
    Add `async def resolve_run_scope_root(definition, *, run_inputs, thread_folder_id, supabase, user_id) -> str | None` to scope.py. Precedence: owner-gated override ?? `definition.project_folder_id` ?? `thread_folder_id`. Read `(run_inputs or {}).get("folder_id")`; when present, gate it — `str(override) in {f["id"] for f in await fetch_visible_folders(supabase, user_id)}` — a miss drops the override to None (D-05: no narrowing / refuse; NEVER trust a client folder_id). Composition guard (A4): when the definition declares any per-phase `folder_scope`, require the override to be within `await resolve_project_subtree(definition.project_folder_id, ...)`; if not, drop the override (prevents the silent-empty intersection at phase_types.py:326). Return the resolved scope root as `str | None` (NEVER a set — Pitfall 6). Mirror the existing helpers' signature shape (async, `*`-kwargs, owner-scoped). Do NOT resolve the subtree inside this helper — callers keep calling `resolve_project_subtree(scope_root, ...)` so threads.py stays minimal (G-5).
    Create `backend/tests/test_152_folder_override.py` covering the behaviors above; patch `resolve_project_subtree` / `fetch_visible_folders` per the test_098 scaffold. Measure against the captured baseline (~63 backend pre-existing ROT — SEED-056), not zero-fail.
  </action>
  <acceptance_criteria>
    - `grep -n "folder_id" backend/app/models/message.py` shows `folder_id: UUID | None = None` on MessageCreate.
    - `grep -n "async def resolve_run_scope_root" backend/app/services/harness/scope.py` returns one match.
    - `resolve_run_scope_root` calls `fetch_visible_folders` and NEVER returns a `set` (source review: return type annotation `str | None`).
    - `cd backend && venv/Scripts/python -m pytest tests/test_152_folder_override.py -x -q` passes all new cases (override-owned, D-05 unowned-refused, D-06 absent-default, A4 composition-drop).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_152_folder_override.py -x -q</automated>
  </verify>
  <done>MessageCreate carries folder_id; resolve_run_scope_root enforces precedence + D-05 owner gate + A4 composition; the focused test suite is green against baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the override at kickoff (threads.py) — persist into inputs + resolve via the helper (G-5 shrink-not-grow)</name>
  <files>backend/app/api/threads.py</files>
  <read_first>
    - backend/app/api/threads.py (:1071-1668 — the kickoff branch: assert_folder_scopes_subset :1129, pin_templates_for_run :1162, create_workflow_run :1343-1352, the scope block :1534-1559, the wf_ctx inputs mirror :1634, folder_subtree_ids pass :1648)
    - backend/app/services/harness/scope.py (the resolve_run_scope_root helper added in Task 1 + the existing resolve_project_subtree import at threads.py:54)
    - 152-PATTERNS.md § "backend/app/api/threads.py" (persistence point, scope-root resolution, consumption echo, G-5 shrink-not-grow note)
  </read_first>
  <action>
    At the `create_workflow_run(...)` call (:1343-1352), extend the `inputs` dict to carry the override only when present: `inputs={"kickoff_prompt": body.content, **({"folder_id": str(body.folder_id)} if body.folder_id else {})}`. It persists to `workflow_runs.inputs` jsonb — NO migration (D-01).
    Replace the inline author-default/thread-fallback branch at the scope block (:1534-1559) with a single call to `resolve_run_scope_root(_kickoff_definition, run_inputs={"folder_id": str(body.folder_id)} if body.folder_id else None, thread_folder_id=<the existing thread folder read>, supabase=supabase, user_id=current_user["id"])`, then feed its result to the EXISTING `resolve_project_subtree(...)` that binds `_wf_folder_subtree_ids`. This SHRINKS threads.py (delete the inline if/else) per G-5 — do not add new inline branches; the precedence lives in scope.py.
    Mirror the persisted `folder_id` in the `wf_ctx` `inputs` at :1634 so the live context equals the durable `workflow_runs.inputs` the resume builders read back. Do NOT touch `agent_loop.py` (Landmine 2 — the Deep chat scope path is a different copy, RED LINE). Keep `folder_subtree_ids` a `list` end-to-end (Pitfall 6).
  </action>
  <acceptance_criteria>
    - `grep -n "resolve_run_scope_root" backend/app/api/threads.py` returns one match at the kickoff scope block.
    - The inline `if _kickoff_definition.project_folder_id is not None:` author-default branch at the old :1538 is removed (source review — precedence now delegated to the helper).
    - `create_workflow_run` inputs includes `folder_id` only when `body.folder_id` is truthy; the wf_ctx inputs at :1634 mirror the same key.
    - `git diff --stat` shows `backend/app/api/threads.py` net line count did NOT grow beyond the helper-call swap (G-5 shrink-not-grow); `agent_loop.py` is untouched.
    - `cd backend && venv/Scripts/python -m pytest tests/test_152_folder_override.py -x -q` still green.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_152_folder_override.py -x -q && grep -c "resolve_run_scope_root" backend/app/api/threads.py</automated>
  </verify>
  <done>Kickoff persists the per-run folder override into workflow_runs.inputs and resolves the scope root through the scope.py helper; threads.py shrank; agent_loop.py untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Honor the durable override on resume + Continue (Pitfall 5)</name>
  <files>backend/app/services/harness_engine.py, backend/app/api/runs.py</files>
  <read_first>
    - backend/app/services/harness_engine.py (:1480-1600 — the resume scope read `_resume_folder_subtree_ids` :1515; run.inputs already loaded at :1571)
    - backend/app/api/runs.py (:895-961 — the Continue scope read `_cont_subtree` :918)
    - backend/app/services/harness/scope.py (resolve_run_scope_root from Task 1)
    - 152-PATTERNS.md § "harness_engine.py + runs.py — override persistence on resume/Continue" and 152-RESEARCH.md Pitfall 5
  </read_first>
  <action>
    At resume (`harness_engine.py:1515`) and Continue (`runs.py:918`), where scope is re-derived from `definition.project_folder_id` ONLY, layer the durable override by routing through `resolve_run_scope_root(definition, run_inputs=run.inputs, thread_folder_id=<thread folder>, supabase=supabase, user_id=<run owner id>)` before calling `resolve_project_subtree`. Resume already has `run.inputs` at :1571 — read `folder_id` from there; Continue must load `run.inputs` the same way. This closes Pitfall 5 (a stranded/Continued run silently reverting to the author default). The owner gate inside the helper still applies (the override is re-validated against the owner's visible folders, so a since-deleted folder degrades safely to the author default).
  </action>
  <acceptance_criteria>
    - `grep -n "resolve_run_scope_root" backend/app/services/harness_engine.py backend/app/api/runs.py` returns one match in each file.
    - Source review: both sites read `folder_id` from the run's durable `inputs`, not from a request body.
    - `cd backend && venv/Scripts/python -m pytest tests/test_152_folder_override.py -x -q` green; full backend suite delta vs baseline is 0 new failures.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_152_folder_override.py -x -q && grep -rc "resolve_run_scope_root" backend/app/services/harness_engine.py backend/app/api/runs.py</automated>
  </verify>
  <done>The per-run folder override is honored identically at kickoff, resume, and Continue; no silent revert to the author default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → threads.py kickoff | `MessageCreate.folder_id` is untrusted client input crossing into the run-launch path |
| threads.py → harness/RPC | the resolved `folder_subtree_ids` binds the retrieval scope the model runs under |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01-01 | Info Disclosure / EoP | resolve_run_scope_root / threads.py kickoff | mitigate | Owner-reachability gate: `str(override) in fetch_visible_folders(owner)`; never-owned → drop override (D-05). resolve_project_subtree does NOT validate root ownership — the gate is load-bearing (service role, no RLS backstop). Defense-in-depth: RPC also filters `match_user_id`. |
| T-152-01-02 | Tampering | tool_dispatcher search_documents scope bind | mitigate | Scope is bound server-side onto ctx.folder_subtree_ids; the model calls `search_documents(query)` only and cannot supply `folder_ids`; post-query `scope_violation` clip is the backstop (D-04, SC#10). Provider-uniform because the constraint is not a prompt. |
| T-152-01-03 | Tampering / Integrity | resume + Continue scope re-derivation | mitigate | Read the override from durable `workflow_runs.inputs` at all 3 run-start sites so it cannot silently revert (Pitfall 5). |
| T-152-01-04 | DoS (silent no-retrieval) | phase_types.py per-phase folder_scope intersection | mitigate | A4 composition guard: for workflows declaring per-phase `folder_scope`, drop an override not ⊆ project subtree (would empty the intersection). |
| T-152-01-05 | Tampering (SQLi) | folder_id binding | mitigate | `folder_id` is a Pydantic UUID (422 on malformed); all downstream SQL binds `$N`, never f-string on the value. |
| T-152-01-SC | Tampering | dependency installs | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit = no-op). slopcheck N/A. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_152_folder_override.py -x -q` green (override / D-05 / D-06 / A4).
- Full backend suite delta vs the captured SEED-056 baseline = 0 new failures.
- Source review: helper owner-gates the override; threads.py delegates precedence (shrink-not-grow); agent_loop.py untouched; folder_subtree_ids stays a list.
- SC#10 cross-provider proof is a LIVE UAT authored in 152-VALIDATION.md — NOT duplicated as a task here.
</verification>

<success_criteria>
- A per-run folder override reaches retrieval via the Phase-098 server-side resolver; the model cannot widen it; behavior is identical across providers (WFIN-02, SC#10).
- A never-owned/unreachable override → no narrowing / refuse (D-05).
- Absent override = today's behavior (D-06); the override survives resume/Continue (Pitfall 5).
- No migration added (D-01); project_folder_id remains the author default (D-03).
</success_criteria>

<output>
Create `.planning/phases/152-workflow-run-inputs/152-01-SUMMARY.md` when done.
</output>
