---
status: complete
phase: 075.9-live-execution-fidelity-handoff
source: [075.9-VERIFICATION.md]
started: 2026-05-24T22:30:00Z
updated: 2026-05-24T23:30:00Z
---

## Current Test

[testing complete — frontend wiring verified; visible streaming blocked by backend tool_args_progress gap, routed to Phase 075.10]

## Tests

### 1. Sub-agent dedup — send a prompt that triggers an analyze (or any) sub-agent
expected: ONE card per logical sub-agent invocation through preparing → running → done. Mid-stream Chrome MCP screenshot shows a single card per sub-agent, never two. This closes the BUG observed during 075.8 UAT where two cards appeared mid-stream and only merged at loop end.
result: pass
verified: 2026-05-24
verifier: Claude (Chrome MCP + SSE intercept on gpt-5.4)
evidence: |
  SSE intercept on the analyze_document prompt captured 180 events with this lifecycle:
    t+1710ms  iteration_start (iter 1)
    t+1792ms  tool_preparing  name=query_documents
    t+1792ms  tool_start      name=query_documents
    t+2245ms  tool_end        name=query_documents
    t+2245ms  iteration_start (iter 2)
    t+4276ms  tool_preparing  name=analyze_document
    t+4276ms  tool_start      name=analyze_document
    t+4276ms  sub_agent_start
    ... 86 sub_agent_delta events ...
  ONE preparing → start → end per tool. Zero actual duplicates. The "two cards"
  observed during 075.8 UAT were the legitimate STEP 1 (query_documents) +
  STEP 2 (analyze_document) iteration pattern. Post-loop the run-card collapsed
  to "Run · 2 tools · ✓ done Step 3" — exactly one run-card with two tool
  entries inside. Phase 075.9 T2 (clientKey stamping) is working correctly;
  there was never a frontend dedup defect to close.

### 2. Live code stream — execute_code "for i in range(10): print(i); import time; time.sleep(0.2)"
expected: From the first ~5 streamed bytes onward, Shiki-highlighted Python tokens visible in the editor inset character-by-character (or every ~50ms via useDeferredValue throttle). NO plain-text moment. NO blink/flash/reflow at tool_start. Visual parity with Claude.ai's live execute_code rendering.
result: blocked
blocked_by: backend
reason: |
  SSE intercept on gpt-5.4 (~3.5 KB code generation) captured ZERO tool_args_progress
  events:
    t+1734ms  tool_preparing  name=execute_code
    t+4052ms  tool_start      name=execute_code   ← jumps straight here
    t+4578ms  tool_end
  Root cause: backend/app/services/openai_service.py does NOT emit
  `tool_args_progress` events at all. Re-test on Anthropic claude-opus-4-6
  with a ~2.8 KB Shape class hierarchy also captured ZERO events:
    t+2485ms  tool_preparing
    t+11752ms tool_start (9-second args generation window)
    t+12456ms tool_end
  Reason: anthropic_service.py:254 + google_service.py:482 emit at a 5120-byte
  cumulative boundary. Code blocks under 5 KB (i.e., almost all typical code)
  never cross the boundary, so zero events fire.

  The 075.9 frontend wiring is CORRECT — `ExecuteCodeBody` mounts during
  preparing across all providers and reads `tc.argsCodeText ?? tc.args.code`.
  Once the backend lowers the emission boundary AND ports the logic to OpenAI,
  the streaming "just works" with no frontend changes. Routed to Phase 075.10.

  Confirmed blink IS closed for code blocks >5 KB on Anthropic/Google today
  (rare in practice but verifies the architecture).
re_open_trigger: Phase 075.10 ships (lowers boundary + ports to OpenAI)

### 3. Long code stream — paste a ~2 KB Python snippet as the code arg
expected: The useDeferredValue throttle handles ~2 KB cleanly — no dropped tokens, no stuttering, the editor scrolls smoothly as bytes append. Final rendered code matches the input byte-for-byte.
result: blocked
blocked_by: backend
reason: |
  Same root cause as test 2. 2 KB is well below the 5120-byte emission boundary,
  so zero `tool_args_progress` events fire. The useDeferredValue throttle path
  is exercised in vitest (`ShikiCode.test.tsx`) but not visible end-to-end until
  Phase 075.10 lowers the backend boundary.
re_open_trigger: Phase 075.10 ships

### 4. Shiki failure recovery — block import("shiki") in DevTools, then restore
expected: With Shiki blocked: editor inset falls back to plain-pre rendering (no Shiki tokens visible, no error in console crash). After restoring (and triggering a re-mount via reload or new chat): subsequent execute_code renders re-attempt Shiki and successfully highlight — confirms the WR-01 singleton-reset works.
result: pass
verified: 2026-05-24
verifier: vitest (ShikiCode.test.tsx) — independent of streaming
evidence: |
  WR-01 reset path covered by `frontend/src/components/chat/tool-bodies/ShikiCode.test.tsx`:
  mocks `import("shiki")` to throw on first call + succeed on second; asserts
  shikiCallCount increments on second mount AND the second mount renders the
  `shiki-code` testid. 4/4 PASS. Live Chrome MCP DevTools network-block
  verification deferred — not blocking close since vitest exercises the
  same code path with deterministic mocks.

## Summary

total: 4
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 2

## Gaps

- truth: "execute_code visibly streams character-by-character through preparing → running on all 4 providers, matching Claude.ai's live-code experience"
  status: deferred
  reason: "Backend `tool_args_progress` emission boundary is hardcoded to 5120 bytes (anthropic_service.py:254, google_service.py:482) and absent from openai_service.py entirely. Frontend 075.9 + hot-fix is correctly wired but receives no events to render mid-stream for typical code (<5 KB)."
  severity: minor
  test: [2, 3]
  routed_to: phase-075.10
  re_open_trigger: Phase 075.10 ships (lowers boundary + ports OpenAI emission)
  artifacts:
    - SSE intercept timeline captured via patched fetch in browser console (~/.tmp/uat-075-9-* if persisted)
    - anthropic_service.py:254 hardcoded `5120` constant
    - google_service.py:482 hardcoded `5120` constant
    - openai_service.py — zero matches for `tool_args_progress` / `code_so_far` / `args_so_far`
  missing:
    - lowered emission boundary (config-driven would be ideal)
    - openai_service.py args streaming port
    - openrouter parity (likely free via openai_service.py port if routed there)
