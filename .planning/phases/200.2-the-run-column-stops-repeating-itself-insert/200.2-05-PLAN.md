---
phase: 200.2-the-run-column-stops-repeating-itself-insert
plan: 05
type: execute
wave: 3
depends_on: ["200.2-04"]
files_modified:
  - frontend/sketch/SketchRunColumn.tsx
  - frontend/sketch/fixtures.ts
  - frontend/sketch/index.html
  - frontend/sketch/main.tsx
  - frontend/sketch/smoke.test.tsx
  - frontend/tsconfig.sketch.json
  - frontend/src/__tests__/no-sketch-surface.test.ts
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - frontend/src/components/workflows/RunTranscript.tsx
autonomous: true
requirements: [RUN-05]
tags: [RUN-05, A-10, teardown, hot-file-ledger, CLAUDE-md-gate, same-commit-sync, superseded-not-deleted]
must_haves:
  truths:
    - "frontend/sketch/ and frontend/tsconfig.sketch.json are deleted in ONE commit, and no-sketch-surface.test.ts passes on a plain vitest run (A-10)"
    - "RunTranscript.tsx has its owed docs/HOT-FILE-LEDGER.md section — the obligation its row has carried since 200.1 ('one phase below the threshold… owes a section on the next phase that touches it') — and its stale canvas-off docblock claim is corrected BESIDE the original"
    - "Every hot file this phase touched has its CLAUDE.md row and ledger section re-derived in the SAME commit (the same-commit sync rule)"
    - "CLAUDE.md passes the 150,000-character gate with the warn-band margin measured and recorded (it was 3,613 below the 120,000 warn band at 200.1's close)"
  artifacts:
    - path: docs/HOT-FILE-LEDGER.md
      provides: "the RunTranscript.tsx section (owed), plus re-derived rows/sections for every phase-touched hot file"
    - path: CLAUDE.md
      provides: "the audit scan list, current for this phase's touches"
  key_links:
    - from: frontend/src/__tests__/no-sketch-surface.test.ts
      to: frontend/sketch/
      via: "the expiry assertion — fires RED once a SUMMARY exists, proving the teardown obligation is executable"
      pattern: "phaseHasExecuted"
---

<objective>
The A-10 teardown and the phase's single ledger/CLAUDE.md sync.

Purpose: a sketch surface that outlives its sketch becomes production surface by accident, and
the no-sketch-surface guard is NOT in the count gate's TARGETS — a plain vitest run is the only
evidence. Separately, this phase touches seven hot files, one of which (RunTranscript.tsx) has
an OWED ledger section from 200.1, and the phase's edits must land in the ledger and CLAUDE.md
in one commit — the same-commit sync rule — with exactly one plan writing both documents (two
concurrent worktrees editing the 150,000-character gate file is a race on the one file whose
silent breakage disables every project rule at once).

Output: the sketch deleted, the ledger and CLAUDE.md current, RunTranscript's docblock corrected.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-RESEARCH.md
@frontend/src/__tests__/no-sketch-surface.test.ts
@docs/HOT-FILE-LEDGER.md
@CLAUDE.md

⚠ **Worktree rules:** bootstrap first, `GSD_VITEST_MAX_WORKERS=2`, never `rm -rf`.

⚠ **SEQUENCING:** this plan depends on plan 04 — the teardown guard fires once ANY
`*-SUMMARY.md` exists under the phase dir, which plan 01's SUMMARY already satisfies. Delete the
sketch LAST, after plan 04's page work has landed, so the acceptance bar stays viewable while
the real surface is being composed. Within THIS plan, the teardown is one commit and the ledger
sync is another (or the same commit — either is honest; two is cleaner for review).

**The hot files this phase touches** (re-derive each triple with CLAUDE.md's own recipe —
`git log --oneline -- <file> | wc -l`, the `sed` bucket recipe with six-digit quick-task
buckets SUBTRACTED, `wc -l <file>`; a figure nobody re-derived is not evidence):
`backend/app/api/workflow_runs.py` · `frontend/src/pages/WorkflowRunPage.tsx` ·
`frontend/src/components/workflows/RunTranscript.tsx` (docblock correction only) ·
`frontend/src/components/workflows/RunSpine.tsx` · `frontend/src/components/workflows/phaseDuration.ts`
(named in the blast radius — re-derive even though the phase does NOT edit it; the 200.1 lesson
is that rows go stale on any commit and only the next editor is prompted) ·
`frontend/src/components/workflows/receiptVocabulary.ts` (same) ·
`frontend/src/lib/api.ts` (the 197-decline trigger FIRED — plan 02 recorded it) ·
`backend/app/models/thread.py` (named in the blast radius).

**The new files** (`RunStepList.tsx`, `RunHero.tsx`, `runColumnVocabulary.ts`) get their FIRST
rows + sections in this same commit — the fileIcon.tsx precedent: listed below the threshold on
purpose, because a file with no row is invisible to its own guardrail at any count.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The A-10 teardown — delete the sketch surface in one commit</name>
  <files>frontend/sketch/SketchRunColumn.tsx, frontend/sketch/fixtures.ts, frontend/sketch/index.html, frontend/sketch/main.tsx, frontend/sketch/smoke.test.tsx, frontend/tsconfig.sketch.json</files>
  <read_first>
    - frontend/src/__tests__/no-sketch-surface.test.ts (all three invariants: expiry, the dir↔tsconfig pairing, main.tsx cleanliness — and the note that the count gate does NOT run it)
    - .planning/sketches/202-the-run-column-rendered/README.md (the teardown paragraph)
    - .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md (A-10)
  </read_first>
  <action>
    In ONE commit: `git rm -r frontend/sketch` (all five files) AND `git rm
    frontend/tsconfig.sketch.json`. A half-teardown is as broken as none — the guard's second
    invariant asserts the directory and the tsconfig live and die together. Do NOT touch
    `src/main.tsx` (the third invariant asserts it stays clean; it was never dirtied by the
    sketch's placement outside `src/`). Verify `frontend/vite.config.ts` and
    `frontend/package.json` carry no sketch references (measured at plan time: none — but check
    again before committing; a leftover reference is a broken build).

    Then run the guard on a PLAIN vitest run — not the count gate, which does not include it:
    `cd frontend && npx vitest run src/__tests__/no-sketch-surface.test.ts`. The expiry
    assertion must now PASS (the phase has SUMMARYs and the surface is gone). If any of the
    three invariants reds, fix the state, never the guard.
  </action>
  <acceptance_criteria>
    - `ls frontend/sketch 2>/dev/null` finds nothing; `ls frontend/tsconfig.sketch.json 2>/dev/null` finds nothing.
    - Both deletions are in ONE commit (`git log --oneline --diff-filter=D -- frontend/sketch frontend/tsconfig.sketch.json` shows one commit).
    - `cd frontend && npx vitest run src/__tests__/no-sketch-surface.test.ts` exits 0 — all three invariants green, the expiry arm on its FIRED branch.
    - `grep -rn "sketch" frontend/vite.config.ts frontend/package.json` finds nothing.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/no-sketch-surface.test.ts && (ls sketch 2>/dev/null && exit 1 || exit 0)</automated>
  </verify>
  <done>The sketch surface is gone in one commit, the guard passes on the fired branch, and the build has no dangling references.</done>
</task>

<task type="auto">
  <name>Task 2: RunTranscript's owed section, the docblock correction, and the phase-wide ledger + CLAUDE.md sync</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md, frontend/src/components/workflows/RunTranscript.tsx</files>
  <read_first>
    - docs/HOT-FILE-LEDGER.md (the RunTranscript placeholder section at :3512 — the owed obligation; the api/workflow_runs.py and WorkflowRunPage.tsx sections this phase's plans recorded verdicts for; the same-commit sync rule in the header)
    - CLAUDE.md (the hot-file table — every row this phase touches or names; the CLAUDE.md context budget section's gate command)
    - frontend/src/components/workflows/RunTranscript.tsx (the docblock's canvas-off claim — the stale sentence to correct BESIDE)
    - .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-01-SUMMARY.md and 200.2-04-SUMMARY.md (the G-5 verdict paragraphs those plans recorded — this task publishes them)
  </read_first>
  <action>
    1. **RunTranscript.tsx's docblock correction** (source edit, small): the docblock's reason-1
       block and the module-header claim say the canvas came OFF this page. It came BACK as a
       switch (`c5e3a352`). Correct BESIDE the original, per the CONTEXT's explicit instruction
       and the seed's routing note: the original four reasons are KEPT (they are the argument
       for the log-as-DEFAULT), with an amendment naming the switch and the 200.2 composition
       (the log is now `RunStepList`; `RunTranscript` is unmounted in the product after this
       phase — ⚠ its remaining importer after plan 04 is its own test file; state that
       plainly, and state whether the component is RETAINED-as-tested (kept for the A-variant
       control) or should be deleted in a future pass — the honest default is RETAINED with a
       named trigger, since deleting a tested shipped component is a teardown decision the
       phase's plans did not lock). ⚠ Read `RunTranscript.test.tsx`'s sweep modes before
       writing any prose that names a token.

    2. **The RunTranscript ledger section** (the OWED obligation): replace the placeholder
       paragraph at :3512 with a real section — the re-derived triple, the phase's touch
       (docblock-only correction; assert `git diff --numstat` shows the component's logic
       byte-unchanged if that is what shipped), and the file's binding invariants (the tense
       rule, the zero-glyph invariant, the R-3 refusal with its driven positive control, the
       owns-no-words rule) — so the next reader gets what every other section gives.

    3. **The phase-wide sync, one commit, both documents:** for every hot file listed in the
       context — re-derive the triple, update the CLAUDE.md row and the ledger section in the
       SAME commit, publish each plan's recorded G-5 verdict, and add FIRST rows + sections for
       the three new files (`RunStepList.tsx`, `RunHero.tsx`, `runColumnVocabulary.ts` — young,
       below threshold on purpose). Follow every row with what changed and WHY the verdict
       holds. Re-derive `phaseDuration.ts`'s and `receiptVocabulary.ts`'s rows even though the
       phase does not edit them — the 200.1 finding is that named-in-blast-radius files go
       stale unwatched.

    4. **The CLAUDE.md size gate:** run `node scripts/check-claude-md-size.cjs` before and
       after. The margin was 3,613 characters below the 120,000 WARN BAND at 200.1's close and
       this task adds several rows. If the gate trips the warn band, the CLAUDE.md rule says
       the split is SCHEDULED, not scrambled: move narrative to `docs/HOT-FILE-LEDGER.md`
       (keeping the table's verdicts complete) rather than trimming guardrails, and record the
       scheduling in the SUMMARY. Never exceed 150,000.
  </action>
  <acceptance_criteria>
    - `docs/HOT-FILE-LEDGER.md` contains a non-placeholder `## frontend/src/components/workflows/RunTranscript.tsx` section with a re-derived triple and named invariants.
    - `grep -c "owes a docs section" docs/HOT-FILE-LEDGER.md` — the RunTranscript placeholder's owed-marker is gone (the obligation is discharged).
    - Every re-derived triple is published with its previous value BESIDE it, never over it, and every six-digit quick-task bucket is subtracted (the 200.1 lib/api.ts lesson: a false staleness report edits a right cell to a wrong value).
    - `RunTranscript.tsx`'s canvas claim is corrected beside the original; `grep -c "canvas came OFF" frontend/src/components/workflows/RunTranscript.tsx` still finds the original phrasing (quoted) with the correction adjacent.
    - `node scripts/check-claude-md-size.cjs` exits 0, and the SUMMARY records the measured char count and the warn-band margin.
    - The three new files each have a CLAUDE.md row AND a ledger section (or an explicit named-only deferral with a re-open trigger — the SettingsPage precedent — but a row is the default).
    - `git log --oneline -1 --name-only` for the sync commit lists BOTH `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs && cd frontend && npx vitest run src/components/workflows/RunTranscript.test.tsx src/__tests__/no-sketch-surface.test.ts</automated>
  </verify>
  <done>The teardown is committed, the owed section exists, every touched hot file is current in both documents, and the size gate exits 0 with the margin recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the sketch surface → production | a dev-only Vite entry outside src/; leaving it is the "production surface by accident" failure |
| CLAUDE.md → every agent's system prompt | the 150,000-character gate; a silent breakage disables every project rule at once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.2-14 | Tampering | the leftover sketch surface | mitigate | one-commit deletion of BOTH paths (dir + tsconfig); the guard's three invariants green on the fired branch; plain vitest run, never the count gate, as evidence |
| T-200.2-15 | Tampering (of the guardrail system) | CLAUDE.md | mitigate | size gate run before and after; warn-band trip schedules the split rather than scrambling it; exactly one plan edits both documents this phase (this one) |
| T-200.2-16 | Tampering (of the audit trail) | the ledger | mitigate | same-commit sync for every touched row; new values beside old, never over; the no-row state (invisible to G-5) answered with first rows for the three new files |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/__tests__/no-sketch-surface.test.ts` — green on the fired branch.
- `node scripts/check-claude-md-size.cjs` — exit 0; margin recorded.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — the full-gate close; verdict line verbatim; the sketch's `smoke.test.tsx` leaving the tree is a DELETED-suite event — record which side of the gate it was on (it was NOT in TARGETS; say so).
- `git log --oneline --diff-filter=D -- frontend/sketch frontend/tsconfig.sketch.json` — one commit.
</verification>

<success_criteria>
- The sketch surface is gone, in one commit, with the guard green.
- RunTranscript's owed section exists; its stale docblock claim is corrected beside the original.
- Both guardrail documents are current and within budget.
</success_criteria>

<output>
Create `.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-05-SUMMARY.md` when done.
Record in it: every re-derived triple (old beside new), the CLAUDE.md char count and margin, and
which suites left the tree with the sketch.
</output>
