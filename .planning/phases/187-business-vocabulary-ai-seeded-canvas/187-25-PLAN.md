---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 25
type: execute
wave: 12
depends_on: ["187-24"]
gap_closure: true
files_modified:
  - scripts/vitest-count-gate.cjs
  - frontend/src/components/workflows/SeedReceipt.test.tsx
  - .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
autonomous: true
requirements: [VOCAB-02]

# WAVE 12, last. A pin must be read from the gate's own `actual` column AFTER every test-adding plan
# in the round has landed — pinning earlier would pin a number that is about to move, and pinning
# from a hand count would pin a fiction (`definitionOps.test.ts` declares 118 `it(` literals and
# runs 228 cases, because of `it.each`). The record likewise transcribes measurements the three
# prior plans produced, so it cannot share their wave.

must_haves:
  truths:
    - "The tests that close CR-03 and CR-04 can no longer be deleted with the count gate green: both owning suites are pinned per-file, which is the exact regression class the gate exists for (WR-16, the Phase-177 lesson)"
    - "Both pinned numbers are READ FROM THE GATE'S OWN OUTPUT, never hand-counted and never inherited from any planning document"
    - "`SeedReceipt.test.tsx`'s docblock stops instructing the next reader NOT to pin it — a claim that becomes false the moment this plan lands, repaired in the same commit"
    - "G-4 is honoured for round 4: the user-visible change (a receipt that no longer re-narrates the author's own edits, and a row that no longer names an actor) has lived-experience rows authored in VALIDATION.md, never inside a PLAN task"
    - "The manual board only grows and never softens: no row of M1..M13 is ticked, re-scoped or dropped, and the new row is added as UNPERFORMED"
    - "D-187-14 and the zero-migration gate are RE-MEASURED for round 4 with raw output, not carried forward from round 3"
    - "The known `PublishGauntlet.test.tsx` parallel-execution flake (D-ITEM-187-20-01) is not allowed to be pinned as truth: a flaking baseline is re-run in isolation and recorded as flake, never re-pinned downward"
    - "No false completion record is written: `requirements.mark-complete`, `state.advance-plan` and `roadmap.update-plan-progress` are not called by any task in this plan"
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "per-file pins for the two suites carrying the whole Req-5 governance-honesty estate"
      contains: "SeedReceipt.test.tsx"
    - path: ".planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md"
      provides: "the round-4 record — per-task rows with raw output, every falsification probe, the re-measured gates, and the amended manual board"
      contains: "187-22-T1"
  key_links:
    - from: "scripts/vitest-count-gate.cjs `BASELINE`"
      to: "the CR-03 and CR-04 fences"
      via: "a per-file pin read from the script's own `actual` column"
      pattern: "definitionOps.test.ts"
---

<objective>
Pin the guards this round built, and put the round on the record with the commands that produced
every figure.

Purpose: `SeedReceipt.test.tsx` and `definitionOps.test.ts` are not in the count gate's `BASELINE`.
Both run inside its target glob and count toward the total, but neither has a per-file pin, and the
total sits far enough above the floor to absorb a large deletion. So the thirteen tests that closed
CR-03 — and every test this round added to close CR-04, WR-11, WR-12, WR-13, WR-14 and WR-15 — can
be deleted with the gate green. That is verbatim the Phase-177 lesson the script's own header cites
as its reason for existing.

The file even documents the omission as a deliberate choice, and that choice WAS right when the file
held no load-bearing guard. It is not right now.

Second: this round changed user-visible copy on a sketched, operator-approved surface, so under
CLAUDE.md's G-4 gate it owes lived-experience rows — and the round produced falsification evidence
that is worthless if it lives only in a transcript.

Output: two pins whose numbers came from the gate itself, one repaired docblock, and a record whose
every figure carries its command.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-REVIEW.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin both suites from the gate's own output, and repair the docblock that says not to</name>
  <files>scripts/vitest-count-gate.cjs, frontend/src/components/workflows/SeedReceipt.test.tsx</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` — the whole header (why it exists, the Phase-177 lesson, and
      the RE-PINNED ONCE block that states the only honest way to move a pin is to read the script's
      own `actual` column), the `BASELINE` map, `BASELINE_TOTAL`, and `TARGETS`. Note that `TARGETS`
      already covers `src/components/workflows`, so both files already RUN — only the pin is
      missing, and `TARGETS` needs no edit.
    - `frontend/src/components/workflows/SeedReceipt.test.tsx` — the docblock at the top, especially
      the passage stating the file "postdates the 415 pin, reports to the gate as `new`, and must NOT
      be added to `BASELINE`". That sentence becomes false in this commit and must be repaired in the
      same commit, not left for a later reader to trip over.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md` — the Deferred
      Items entry for `D-ITEM-187-20-01`, the pre-existing `PublishGauntlet.test.tsx`
      parallel-execution flake, and note that 187-20 proved it pre-existing by a restore-and-re-run.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` — the section on
      why the full frontend suite is never a gate and the isolated named sets are.
  </read_first>
  <action>
    Run the gate: `node scripts/vitest-count-gate.cjs` from the repo root. Read the two numbers you
    need out of the printed `actual` column for `SeedReceipt.test.tsx` and `definitionOps.test.ts`.
    Do not count `it(` literals to get them — `definitionOps.test.ts` declares roughly 118 literals
    and runs well over 200 cases because of `it.each`, so a hand count would pin a fiction. Do not
    reuse any number written in this plan, in `187-REVIEW.md`, or in `187-VERIFICATION.md`: three
    plans in this round added tests to both files, so every such figure is stale by construction.

    If the run reports failures, diagnose before pinning. A `PublishGauntlet.test.tsx` red is the
    known pre-existing parallel-execution flake (`D-ITEM-187-20-01`): re-run that file in isolation
    to confirm it is green alone, record both results, and DO NOT lower its pin. A pin is only ever
    lowered alongside a deliberate, plan-authorised deletion — never to quiet a red gate. If any
    other file is red, stop and report rather than pinning around it.

    Add both files to `BASELINE` with the measured numbers, keeping the map's existing
    descending-by-count ordering convention, and write a short comment in the style of the shipped
    185-08 entry: why these two are pinned now (they became load-bearing — they carry the CR-03
    repair, the CR-04 fences, and the standing coverage sweep), and that the numbers were read from
    the script's own output. Leave `BASELINE_TOTAL` computed rather than hand-written; confirm the
    existing reduce still derives it and update the trailing comment that names the old total so it
    is not stale.

    Repair `SeedReceipt.test.tsx`'s docblock in the same commit: replace the "must NOT be added to
    `BASELINE`" instruction with what is now true — the file IS pinned, why the earlier choice was
    right and stopped being right, and the standing rule that its pin moves only alongside an
    authorised deletion. Note also that the file's own `?raw` self-import means its docblock is part
    of `testSource`; the coverage sweep strips comments before its substring check, so this prose
    edit cannot satisfy or break that guard — confirm that by re-running the suite, do not assert it
    from this plan.

    Re-run the gate and confirm exit 0 with both new files reported against their pins at delta 0.
  </action>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/vitest-count-gate.cjs` exits 0, and its printed table shows
      `SeedReceipt.test.tsx` and `definitionOps.test.ts` as PINNED rows with delta `0` — not as
      `new`. The full raw table from both the pre-pin and post-pin runs is transcribed into the
      SUMMARY.
    - Falsification, observed and recorded: delete one `it(` block from `SeedReceipt.test.tsx`,
      re-run the gate, and confirm it exits 1 with a `[count-decrease]` reason naming that file.
      Restore and prove the restore with `git diff --stat`. Repeat for `definitionOps.test.ts`. A
      pin never observed catching a deletion is not known to be a pin.
    - `grep -n "must NOT be added to" frontend/src/components/workflows/SeedReceipt.test.tsx`
      returns nothing.
    - `cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx` exits 0 after
      the docblock edit, with the same count the pin records — the coverage sweep is unaffected by
      prose.
    - Every pinned file that was already in `BASELINE` reports delta ≥ 0. If `PublishGauntlet.test.tsx`
      flaked, both the flaking run and the isolated green run are recorded, and its pin is unchanged.
    - `git diff --name-only -- backend/ supabase/migrations` is empty.
    - No SDK completion verb was called.
  </acceptance_criteria>
  <done>Both suites are pinned from measured output, the stale instruction is gone, and each pin is observed catching a deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Record round 4 — every figure with its command, and the manual board amended, never softened</name>
  <files>.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` — all of it, but
      especially the round-3 gap-closure section (its per-plan table, its per-task rows keyed
      `187-20-T1` etc., its PROBE and COVERAGE subsections, and its GATES subsection). This task adds
      a SIBLING section for round 4 placed after it; it rewrites none of round 3's figures. Also read
      the manual board and find M3, M9 and M13 — M3 and M9 already carry CR-04 warnings that this
      round's fix makes stale.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-22-SUMMARY.md`,
      `187-23-SUMMARY.md`, `187-24-SUMMARY.md` — the raw output to transcribe. Transcribe; do not
      re-derive figures those plans already measured, and name the commit each was measured at.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md` — the `gaps`
      block and the `human_verification` block, so the record names the truth this round closes
      (truth #5, VOCAB-02 / SPEC Req 5) and the seven findings (CR-04, WR-11..WR-16) by id, and so
      the manual board's existing thirteen rows are carried forward exactly.
    - `.claude/skills/sketch-findings-agentic-rag/SKILL.md` and its
      `references/ai-seed-and-templates.md` — sketch 150-B's four load-bearing properties and its
      "here is what I built" framing, which is the design reason the snapshot fix was chosen over
      re-tensing the copy. The new manual row must be written against that framing.
    - `CLAUDE.md` — the G-4 lived-experience gate and the "UAT rows are authored under VALIDATION.md,
      NOT in PLAN.md tasks" rule.
  </read_first>
  <action>
    Add a round-4 section to `187-VALIDATION.md`, a sibling of the round-3 section and placed after
    it, headed with today's date. State the base commit every gate in the section is measured
    against — the tip immediately before 187-22's first commit — and say explicitly that it differs
    from the phase base, from round 2's base and from round 3's `f632f9b6`, so no figure in the file
    is ambiguous about which range it measures.

    The section carries, at minimum:

    A per-plan table naming 187-22 against CR-04 (a Blocker), 187-23 against WR-11 and WR-15,
    187-24 against WR-12, WR-13 and WR-14, and 187-25 against WR-16 plus this record. State the
    scope fence plainly: IN-11, IN-12, IN-13 and IN-14 were shown to the operator and left OUT of
    this round by decision, and WR-08 remains routed to Phase 188 — say both by name, since a reader
    must not infer either was absorbed here. WR-01, WR-04..WR-07 and IN-01..IN-05 remain carried
    forward, unchanged and undropped.

    A per-task verification row for each task in 187-22, 187-23 and 187-24, keyed `187-22-T1` and so
    on, each with its automated command and its measured result.

    A PROBE subsection recording every falsification this round performed — the CR-04 red-before /
    green-after pair, the leaf-caching probe, the reverted-mount-prop probe, 187-23's three
    mutations, and 187-24's nine (two agreement/source, one WR-12 wording, five WR-13 sweep probes,
    and the sibling-attribute probe). For each: the mutation applied, the observed RED verbatim, and
    the clean revert evidence. Frame the subsection with the lesson that made this round necessary:
    round 3's CR-01 fix shipped its own new sentence with zero assertions, and CR-03's own closure
    taught that green over an unrepresentable fixture is not coverage. A probe observed RED is the
    only evidence a guard bites.

    A GATES subsection re-measuring, with raw output at the round-4 tip:
    - the five-suite named set — file count, pass count, fail count, and the total against the
      figure re-measured at the round's start, stating explicitly whether the total ROSE and by how
      much;
    - `node scripts/vitest-count-gate.cjs` exit code and its full table, plus the two new pins and
      the deletion probes that proved them;
    - the zero-migration gate — `git diff --stat <phase base> HEAD -- supabase/migrations` and
      `git status --porcelain supabase/migrations`, both expected empty;
    - the backend-untouched gate — `git diff --name-only <round-4 base> HEAD -- backend/`, expected
      empty;
    - the D-187-14 mount cap — `git diff --numstat <round-4 base> HEAD -- frontend/src/pages/WorkflowBuilderPage.tsx`,
      with the measured insertions stated against the ≤ 15 budget this round set;
    - `tsc --noEmit` against the recorded `D-ITEM-01` baseline of 33, before and after.

    Then amend the manual board, and only in these ways:

    Repair M3's and M9's CR-04 notes. Both currently tell the operator to try leaving the receipt
    open and editing a step, warning that the card may start lying. After 187-22 that is the
    behaviour under test rather than a known defect, so rewrite each note to say what the operator
    should now EXPECT to see, and name 187-22 as what changed it. Do not delete the notes — a
    silently vanishing warning reads as a warning that was never true.

    Add ONE new row, M14, for the property this round built: with the receipt still open, make a real
    edit that would have changed one of its three sentences — switch a KB tool on, add a step, flip
    the grounding dial — and confirm the card does not move, while the CANVAS does. The row must say
    why a person is needed: whether a card that deliberately stops tracking reads as honest history
    or as a stale bug is a judgement only a person makes, and sketch 150-B's "here is what I built"
    framing is the acceptance bar. Add a second row only if 187-23's sentence change genuinely needs
    its own read; if M13 already covers the escalated-only card head-on, extend M13's expectation
    rather than duplicating it, and say which you did.

    Every row's performed/unperformed status stays honest. No row is ticked, softened, re-scoped or
    dropped; the count of manual rows only grows. State at the top of the amended board that all
    rows remain the operator's and remain unperformed at the close of this round.
  </action>
  <verify>
    <automated>grep -c "187-22-T1\|187-23-T1\|187-24-T1\|M14" .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The round-4 section exists as a sibling of round 3's, with its own base commit stated, and
      round 3's section is byte-unchanged apart from the M3 and M9 note repairs (prove it with
      `git diff` on the file and point at the changed hunks).
    - Every figure in the new section is accompanied by the command that produced it. A figure
      without a command does not appear.
    - Per-task rows exist for every task in 187-22, 187-23 and 187-24, each with a command and a
      measured result.
    - The PROBE subsection accounts for every falsification the three prior SUMMARYs record, with no
      probe summarised as "passed" — each carries its observed RED and its revert evidence.
    - The manual board has exactly one more row than it had at the start of the round (or M13 is
      extended instead, with the choice stated), M3's and M9's notes describe the post-fix
      expectation, and no row is marked performed.
    - `grep -rn "requirements.mark-complete\|state.advance-plan\|roadmap.update-plan-progress"` over
      this round's four plan files and four summaries returns only the PROHIBITION text, never a
      call.
    - `git diff --name-only` for this task shows exactly one changed file: `187-VALIDATION.md`.
    - No SDK completion verb was called.
  </acceptance_criteria>
  <done>Round 4 is on the record with commands attached to every figure, the two stale manual notes are repaired, and the board is one row larger and zero rows softer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the count gate → every later reader who treats a green gate as coverage evidence | The gate is the mechanism the project trusts to notice a deleted test. An unpinned load-bearing suite means that trust is misplaced for exactly the files that matter most this phase. |
| the validation record → the verifier, the reviewer and the operator | Everything downstream of this phase reads `187-VALIDATION.md` as the measured account. A figure recorded without its command is an assertion wearing a measurement's clothes. |
| a flaking baseline → a lowered pin | A red gate creates pressure to move a number. Lowering a pin to quiet a flake permanently blinds the gate to the next real deletion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-R4-14 | Tampering | `scripts/vitest-count-gate.cjs` `BASELINE` | mitigate | The CR-03 and CR-04 fences are deletable with the gate green because neither owning suite is pinned. Closed by pinning both from the script's own `actual` column, and proved by deleting one `it(` block per file and observing `[count-decrease]`. |
| T-187-R4-15 | Repudiation | the pin numbers themselves | mitigate | A hand-counted or inherited pin is a fiction that reports as a measurement — `definitionOps.test.ts` runs far more cases than it declares `it(` literals. Closed by requiring the numbers to come from the gate's printed output in the same run, with the raw table transcribed. |
| T-187-R4-16 | Denial of Service | the known `PublishGauntlet.test.tsx` parallel flake | mitigate | A flake creates pressure to lower a pin, which would blind the gate permanently. Closed by an explicit rule in the task: re-run in isolation, record both results, never lower the pin, and stop-and-report if any other file is red. |
| T-187-R4-17 | Repudiation | the manual board | mitigate | The strongest temptation at the end of a fourth closure round is to tick a row nobody performed, or to quietly drop a warning the fix made stale. Closed by explicit criteria: rows only grow, none is marked performed, and the two stale notes are REWRITTEN rather than deleted so a reader can see the warning was once true. |
| T-187-R4-18 | Information Disclosure | — | accept | This plan writes no code that runs in the product. `scripts/vitest-count-gate.cjs` imports nothing from `frontend/src`, needs no database, no backend and no network, and writes its report outside every reload-watched tree — all properties its own header states and this plan does not change. |
| T-187-R4-SC | Tampering | npm / pip / cargo installs | accept | No package-manager install and no new dependency in this plan; the Package Legitimacy Gate is not engaged. Adding a dependency re-opens it and requires the audit table plus a blocking human checkpoint before install. |
</threat_model>

<verification>
- `node scripts/vitest-count-gate.cjs` — exit 0, both new files reported at delta 0, full table transcribed.
- Two deletion probes, one per newly pinned file, each observed producing `[count-decrease]` and each cleanly restored.
- `cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx src/components/workflows/StepTypePicker.test.tsx src/components/workflows/phaseVocabulary.test.ts src/components/workflows/phaseVocabulary.corpus.test.ts src/components/workflows/definitionOps.test.ts` — the five-suite named set; exit 0 and a total above the figure re-measured at the round's start.
- `git diff --stat <phase base> HEAD -- supabase/migrations` and `git status --porcelain supabase/migrations` — both empty.
- `git diff --name-only <round-4 base> HEAD -- backend/` — empty.
- `git diff --numstat <round-4 base> HEAD -- frontend/src/pages/WorkflowBuilderPage.tsx` — ≤ 15 insertions.
- ⚠ Never gate on the full frontend suite. `vitest` is 4.1.0: no `--reporter=basic`.
</verification>

<success_criteria>
- Both suites carrying the Req-5 governance-honesty estate are pinned, from numbers the gate printed, and each pin has been observed catching a deletion.
- The docblock that told the next reader not to pin the file no longer says so.
- Round 4 is recorded with a command behind every figure, every falsification probe transcribed with its observed RED and its revert, and all five gates re-measured rather than carried forward.
- The manual board is one row larger, two stale notes are repaired to describe the post-fix expectation, and no row is claimed as performed.
- Zero migrations, zero backend files, zero SDK completion verbs.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-25-SUMMARY.md` when done.

It MUST carry: the pre-pin and post-pin gate tables raw; both deletion probes with their
`[count-decrease]` output and their restores; the isolated `PublishGauntlet.test.tsx` result if it
flaked; the five-suite total before and after the round; and the five re-measured gates with their
commands. State explicitly that no manual row was performed and no SDK completion verb was called.
</output>
</content>
