---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 08
type: execute
wave: 2
depends_on: ["187-04"]
files_modified:
  - frontend/src/components/workflows/canvasModel.ts
  - frontend/src/components/workflows/WorkflowCanvas.tsx
  - frontend/src/components/workflows/canvasModel.test.ts
  - frontend/src/components/workflows/canvasModel.purity.test.ts
autonomous: true
requirements: [VOCAB-01]

must_haves:
  truths:
    - "D-187-05 — toCanvas gains an OPTIONAL nameContext option with a frozen module-scope empty default, exactly the shipped ToCanvasOptions.kbTools precedent"
    - "toCanvas stays PURE (D-183-12): it fetches nothing, hardcodes nothing, and every existing caller that omits the option projects byte-identically"
    - "WorkflowCanvas.tsx receives a strictly kbTools-shaped pass-through and nothing else — the G-5 ledger fires on this file and its extraction is due in Phase 188"
    - "buildPhaseData passes the context to nodeTitle only; technicalTitle, subtitle, grounded, armed and waitsForYou are unchanged"
  artifacts:
    - path: "frontend/src/components/workflows/canvasModel.ts"
      provides: "ToCanvasOptions.nameContext + NO_NAME_CONTEXT threading into buildPhaseData"
      contains: "ToCanvasOptions"
    - path: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      provides: "the nameContext prop pass-through into the toCanvas useMemo"
      contains: "nameContext"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      to: "canvasModel.toCanvas"
      via: "useMemo(() => toCanvas(phases, { kbTools, nameContext }), [phases, kbTools, nameContext])"
      pattern: "toCanvas\\(phases"
    - from: "frontend/src/components/workflows/canvasModel.ts"
      to: "phaseVocabulary.nodeTitle"
      via: "buildPhaseData(phase, kbTools, nameContext)"
      pattern: "nodeTitle\\(phase"
---

<objective>
Thread the name-lookup context to the canvas projection, keeping `toCanvas` pure and every existing caller byte-identical.

Purpose: the derived tier is useless if the canvas cannot reach it. `toCanvas` has exactly ONE
production call site (`WorkflowCanvas.tsx:879`), and the id→name maps are already fetched once on
mount by `WorkflowBuilderPage` — so this costs zero fetches and zero new state. The safety property is
that the projection never fetches and never hardcodes: the context is passed IN with a safe default,
exactly as `kbTools` was in Phase 185.

Output: `ToCanvasOptions.nameContext`, the frozen default, the `buildPhaseData` thread, and the
kbTools-shaped pass-through on `WorkflowCanvas`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-SPEC.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-PATTERNS.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-04-SUMMARY.md
</context>

<interfaces>
```ts
// frontend/src/components/workflows/canvasModel.ts:295-332 (shipped) — THE PRECEDENT TO COPY
export interface ToCanvasOptions {
  /** The server-supplied KB-reading tool names. Absent or empty marks nothing. */
  kbTools?: readonly string[]
}
const NO_KB_TOOLS: readonly string[] = Object.freeze([])
export function toCanvas(phases: PhaseSpecJSON[], options: ToCanvasOptions = {}): CanvasProjection {
  const kbTools = options.kbTools ?? NO_KB_TOOLS
  // …
}

// :277-291 — the threading point
function buildPhaseData(phase: PhaseSpecJSON, kbTools: readonly string[]): PhaseNodeData {
  return { slug, phaseIndex, phaseType, title: nodeTitle(phase), technicalTitle: technicalTitle(phase),
           subtitle: PHASE_TYPE_SUBTITLES[phaseType] ?? "", grounded: isGrounded(phase, kbTools),
           armed: isArmed(phase), waitsForYou: waitsForYou(phase) }
}
// buildPhaseData is called once, at canvasModel.ts:348

// frontend/src/components/workflows/WorkflowCanvas.tsx (shipped) — the three kbTools lines
:777  kbTools?: readonly string[]
:864  kbTools,
:879  const projection = useMemo(() => toCanvas(phases, { kbTools }), [phases, kbTools])
```

```ts
// frontend/src/components/workflows/phaseVocabulary.ts (from plan 187-04)
export interface NameContext { folderNames?; skillNames?; templateFilename? }
export function nodeTitle(phase: PhaseSpecJSON, ctx?: NameContext): string
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ToCanvasOptions.nameContext and thread it through buildPhaseData</name>
  <files>frontend/src/components/workflows/canvasModel.ts, frontend/src/components/workflows/canvasModel.test.ts, frontend/src/components/workflows/canvasModel.purity.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/canvasModel.ts:277-291` — `buildPhaseData`, the one place every face value is resolved.
    - `frontend/src/components/workflows/canvasModel.ts:295-350` — the PURE-projection docblock, `ToCanvasOptions`, `NO_KB_TOOLS` and its "deterministic to the byte / the snapshot gate depends on it" comment, and the `buildPhaseData` call at `:348`.
    - `frontend/src/components/workflows/canvasModel.purity.test.ts` — the shipped purity assertions (input array never mutated, phases carried by reference).
    - `frontend/src/components/workflows/canvasModel.fixtures.test.ts:80-95` — the `toMatchSnapshot()` over `toCanvas(phases)` that the frozen default protects.
    - `frontend/src/components/workflows/phaseVocabulary.ts` `NameContext` / `nodeTitle` (as landed by plan 187-04).
  </read_first>
  <behavior>
    - `toCanvas(phases)` with no options produces a projection deeply equal to HEAD's for every shipped fixture, and the snapshot is unchanged.
    - `toCanvas(phases, { kbTools })` with no `nameContext` produces a projection deeply equal to HEAD's.
    - `toCanvas(phases, { nameContext })` where a phase's `skill_ref` resolves changes only that node's `title`; `technicalTitle`, `subtitle`, `grounded`, `armed`, `waitsForYou`, node ids, positions and edges are all unchanged.
    - Omitting `nameContext` twice hands the same frozen reference both times (identity, not a fresh object).
    - The input `phases` array and its objects are still never mutated (the purity suite is unchanged and green).
  </behavior>
  <action>
Add `nameContext?: NameContext` to `ToCanvasOptions`, importing the type from `phaseVocabulary`.
Add a module-scope `const NO_NAME_CONTEXT: NameContext = Object.freeze({})` beside `NO_KB_TOOLS`, with
the same comment reasoning: an omitted option must hand the SAME reference on every call, because the
projection has to be deterministic to the byte and a fresh `{}` per call is a needless identity change
the snapshot gate would see.

Resolve it in `toCanvas` the same way `kbTools` is resolved (`options.nameContext ?? NO_NAME_CONTEXT`)
and widen `buildPhaseData`'s signature to take it as a third parameter, passing it **only** to
`nodeTitle`. Do not pass it to `technicalTitle` — the technical title is the slug and must stay the
slug.

Extend the `ToCanvasOptions` docblock in the register of the shipped `kbTools` paragraph: the context
is passed IN so this module stays PURE — it fetches nothing and hardcodes nothing; it is OPTIONAL and
defaults to EMPTY; and the default is the safe direction stated out loud — an absent map renders the
generic type sentence rather than an id-shaped face. Every shipped caller that omits it therefore
projects exactly as it did before.

Extend `canvasModel.test.ts` and `canvasModel.purity.test.ts` with the five behaviours above. Write the
"changes only that node's title" assertion field-by-field against the HEAD projection, not as a
snapshot — a snapshot would hide which field moved.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/canvasModel.test.ts src/components/workflows/canvasModel.purity.test.ts src/components/workflows/canvasModel.fixtures.test.ts src/components/workflows/canvasModel.roundtrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The four canvasModel suites pass with counts strictly greater than HEAD's for `canvasModel.test.ts` and `canvasModel.purity.test.ts`, and unchanged for `canvasModel.fixtures.test.ts` and `canvasModel.roundtrip.test.ts`.
    - No snapshot file under `frontend/src/components/workflows/__snapshots__/` is modified: `git status --porcelain frontend/src/components/workflows/__snapshots__` is empty.
    - `grep -n "NO_NAME_CONTEXT" frontend/src/components/workflows/canvasModel.ts` shows `Object.freeze({})` at module scope.
    - `grep -cE "from ['\"]@/lib/api['\"]|fetch\(" frontend/src/components/workflows/canvasModel.ts` returns 0.
    - `grep -n "technicalTitle(phase" frontend/src/components/workflows/canvasModel.ts` shows it still called with one argument.
    - A test asserts `toCanvas(p) `'s projection deep-equals `toCanvas(p, {})`'s, and that omitting the option twice yields the identical frozen reference.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>The projection accepts an optional name context, stays pure, and is byte-identical when the option is omitted.</done>
</task>

<task type="auto">
  <name>Task 2: Pass the name context through WorkflowCanvas — a strictly kbTools-shaped diff</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:770-790` — the props interface where `kbTools?: readonly string[]` is declared at `:777`, with its surrounding JSDoc.
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:855-885` — the destructure at `:864` and the `toCanvas` `useMemo` at `:879`.
    - `CLAUDE.md` §"Workflow guardrails" — the hot-file ledger row for `WorkflowCanvas.tsx` (9 plans across 3 phases, 1574 L, **G-5 fires — extraction due in Phase 188**).
  </read_first>
  <action>
**G-5 note, to be recorded in the SUMMARY rather than discovered later.** The hot-file ledger fires on
this file and its extraction is due in Phase 188. `187-RESEARCH.md` recommended not touching it at all
— but `toCanvas` has exactly one production call site and it is here, so Req 1's canvas face is
unreachable without a pass-through. This plan therefore takes the **narrowest possible** diff: the
same three lines Phase 185 added for `kbTools`, and nothing else. No new logic, no new state, no new
effect, no new component. The diff size is an acceptance criterion.

Add exactly three things:
  1. `nameContext?: NameContext` on the props interface, immediately beside `kbTools` at `:777`, with a
     one-line JSDoc in the same register: the id→name maps are owned by the page and passed in;
     absent or empty means every derived tier misses and the generic type sentence renders.
  2. the destructure entry beside `kbTools` at `:864`.
  3. the `useMemo` at `:879` becomes `toCanvas(phases, { kbTools, nameContext })` with `nameContext`
     added to the dependency array.

Do not change anything else in this file. In particular do not touch the node-data merge that carries
`technical` onto node data, the toolbar, the edge types map, or any `PhaseNodeCard` prop — a third
badge is a typecheck error and no focusable control may live inside the card.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx src/components/workflows/WorkflowCanvas.editing.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat -- frontend/src/components/workflows/WorkflowCanvas.tsx` shows **at most 6 insertions and at most 2 deletions**. Record the measured numbers in the SUMMARY.
    - `git diff -U0 -- frontend/src/components/workflows/WorkflowCanvas.tsx | grep -cE '^\+.*(function |useState|useEffect|useCallback)'` returns 0 — no new logic, state or effect.
    - `npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx src/components/workflows/WorkflowCanvas.editing.test.tsx` passes with counts unchanged from HEAD (these files are 100% green in isolation — do not run the full suite).
    - `git status --porcelain frontend/src/components/workflows/PhaseNodeCard.tsx` is empty.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>The canvas hands the name context to `toCanvas` in three lines and the hot file grew by nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page state (fetched id→name maps) → pure projection | the projection must not fetch; the caller must supply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-08-01 | Tampering | `toCanvas` purity | mitigate | The context is an optional option with a frozen module-scope default; `grep` for an API-client import returning 0 and the untouched snapshot are acceptance criteria. |
| T-187-08-02 | Information Disclosure | node face | mitigate | An absent map means the tier misses and the generic sentence renders — the safe direction, stated in the docblock and pinned by plan 187-04's tests. |
| T-187-08-03 | Denial of Service | `WorkflowCanvas.tsx` maintainability (G-5) | mitigate | The diff is capped at 6 insertions / 2 deletions and forbidden from adding state, effects or functions — measured, not asserted. Extraction stays Phase 188's. |
| T-187-08-04 | Tampering | `PhaseNodeCard` invariants | mitigate | `git status --porcelain` on that file must be empty; a third badge is a typecheck error and no focusable control may live inside the card. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/canvasModel.test.ts src/components/workflows/canvasModel.purity.test.ts src/components/workflows/canvasModel.roundtrip.test.ts src/components/workflows/canvasModel.fixtures.test.ts src/components/workflows/phaseVocabulary.test.ts src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseSpine.test.tsx src/components/workflows/PhaseNodeCard.test.tsx` → ≥ 863 passed, 0 failed.
- `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/PublishGauntlet.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/workflows/ProblemsTray.test.tsx src/components/workflows/definitionOps.test.ts` → ≥ 381 passed, 0 failed.
- `cd frontend && npx tsc -b` exits 0.
- `git diff --numstat -- frontend/src/components/workflows/WorkflowCanvas.tsx` within the cap.
</verification>

<success_criteria>
- `toCanvas` accepts an optional `nameContext` and is byte-identical when it is omitted.
- The single production call site passes it in a three-line diff.
- No snapshot, no `PhaseNodeCard`, and no other part of `WorkflowCanvas.tsx` moved.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-08-SUMMARY.md` when done.
</output>
