---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 15
type: execute
wave: 4
depends_on: ["187-05", "187-08", "187-09", "187-13", "187-14"]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.describe.test.tsx
  - .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-03]

must_haves:
  truths:
    - "D-187-14 — WorkflowBuilderPage.tsx gains two gated mount lines plus the name-context thread and nothing else; the measured git diff --numstat on that file is a PHASE GATE, and the cap is DERIVED component-by-component in Task 3 rather than chosen round"
    - "The render-body sub-gate is the one D-187-14 literally states: exactly TWO new JSX elements and exactly TWO new props on existing JSX elements — the Phase-185 shape the hot-file ledger praises"
    - "RESEARCH Open Q6 is closed on three of four call sites: canvas + spine (task 1) and both WorkflowBuilderPage announcement sites; ProblemsTray is closed in plan 187-08 at zero cost to this gate; only definitionOps.canRemovePhase is deferred, and it is deferred by NAME with a re-open trigger"
    - "D-187-09 — the receipt's dismissal is in-memory component state, per draft. A page reload re-shows it and that is correct: the grounding it describes is still true and nothing was persisted"
    - "No browser storage is introduced in WorkflowBuilderPage — WorkflowBuilderPage.canvas.test.tsx:414-418 is a shipped guard asserting the page's source names no storage API"
    - "Both new surfaces are gated on canvasEnabled (D-181-01); the flag-OFF describe screen stays byte-identical to the plan 187-05 pin"
    - "The receipt appears on the autoDraft hand-off path too — it is a genuine AI seed; stated here rather than discovered in UAT"
    - "This phase adds ZERO migration files"
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "the name-context thread + the two gated mounts"
      contains: "canvasEnabled"
    - path: ".planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md"
      provides: "the completed per-task verification map and the recorded phase gates"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "WorkflowCanvas + PhaseSpineGraph"
      via: "nameContext built once from folderNames / skillNames / meta.assets"
      pattern: "nameContext"
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "SeedReceipt + StarterTemplatePicker"
      via: "one canvasEnabled-gated mount line each"
      pattern: "canvasEnabled"
---

<objective>
Wire the four new surfaces into the page in the smallest diff that can deliver them, and close the phase gates.

Purpose: `WorkflowBuilderPage.tsx` is 1810 lines and on CLAUDE.md's hot-file ledger. D-187-14 honours G-5
**by construction**: the receipt and the picker each land as their own component file, and the page gains
one gated mount line per surface. This is the shape the ledger explicitly praises from Phase 185 — *"only
4 insertions reach the render body … keep this shape."* The measured diff is a phase gate, not a
preference: if the render body grows beyond a handful of insertions, the surface belongs in its own
component, not the page.

Output: the threaded name context, the two mounts, and the recorded phase gates.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-SPEC.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-PATTERNS.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
</context>

<interfaces>
```tsx
// frontend/src/pages/WorkflowBuilderPage.tsx (shipped, measured 2026-08-02)
:512        initialDescribe  — an UPSTREAM prop, NOT settable from inside the page
:558        const [describe, setDescribe] = useState(initialDescribe ?? "")     ← the picker writes THIS
:587/:589   folderNames / skillNames state
:853        useGroundingBundle(canvasEnabled)                                   ← kbTools, canvas-gated
:1107-1133  the mount effect that builds folderNames + skillNames (already fetched once)
:1164       onDraft
:1181-1188  the SINGLE state transition on success — store.getState().setDrafted(def)
:1202-1216  the autoDraft hand-off path (Phase 124 CR-01 "Describe & run")
:1416-1424  <textarea aria-label="business requirement" value={describe} …>
:1450-1464  the CTA + data-testid="describe-hint"                               ← the door's one line
:1516       <WorkflowCanvas … />        :1544  <PhaseSpineGraph … />
:1551-1555  const graphColumn = canvasEnabled ? ( … ) : graphChild              ← the flag-branch precedent
:1790-1802  {...(canvasEnabled ? { rails, onGovernanceChange } : {})}           ← the SPREAD-CONDITIONAL precedent
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the name context once and thread it to both graph views</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.tsx:580-600` — the `folderNames` / `skillNames` state declarations.
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1107-1133` — the mount effect that fetches folders and skills ONCE and builds both id→name maps, with its non-fatal catch (*"the panel falls back to the raw id"*).
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1500-1560` — the `WorkflowCanvas` mount at `:1516`, the `PhaseSpineGraph` mount at `:1544`, and the `graphColumn` flag branch at `:1551-1555`.
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1770-1805` — the spread-conditional precedent at `:1790-1802` and its D-14 comment (with the flag off the prop must be genuinely ABSENT, not present-and-undefined).
    - `frontend/src/components/workflows/builderStore.ts:170-200` — `DefinitionMeta`, the key-remapped mapped type over `BuilderDefinition`'s index signature, through which `assets` round-trips untouched.
    - `backend/app/models/harness.py:255-282` — `AssetRef` (`kind: Literal["template","reference"]`, `filename`) and `WorkflowDefinition.assets`.
    - `frontend/src/components/workflows/phaseVocabulary.ts` `NameContext` (plan 187-04).
  </read_first>
  <behavior>
    - With the canvas flag ON and both maps populated, a phase with a resolvable `skill_ref` renders its derived face on the canvas AND on the spine, and the two strings are equal.
    - With the maps empty (first paint, before the mount effect resolves), both views render the generic type sentence — no id-shaped face, no placeholder.
    - `templateFilename` is taken from the definition's `assets[]` entry whose `kind === "template"`; a definition with no assets, a null `assets`, or a non-array `assets` yields `undefined` and no template face anywhere, without throwing.
    - With the canvas flag OFF, the `nameContext` prop is genuinely ABSENT from the spine element, not present-and-undefined.
    - The context object identity is stable across re-renders that change none of its three inputs.
    - After adding a phase, the `canvasNotice` announcement names it with the SAME string the new node's card shows — not the generic type sentence the card has moved past.
    - After removing a phase, the `Removed` announcement names it with the same string that node's card showed.
    - A REFUSAL notice (`canRemovePhase`) still names its referrer with the undecorated `nodeTitle` — the deferral is asserted, so it cannot be closed by accident and then quietly reopened.
  </behavior>
  <action>
Add ONE `useMemo` building the `NameContext` from values the page already holds: `folderNames`,
`skillNames`, and the template filename read off `meta.assets` (the definition's `assets[]` entry where
`kind === "template"`). Memoise on those three inputs so the object identity is stable — `toCanvas` is
memoised on its options and an unstable context would re-project on every render.

**The `assets` read is the only genuinely new data read in Req 1.** Measured: `frontend/src` contains
zero references to `assets` today; the builder carries them only incidentally because `DefinitionMeta`
is a mapped type over an index signature, so `assets` round-trips through `meta` untouched. Read it
defensively — `assets` may be absent, null, or not an array — and give the read a comment saying it is
DEFINITION-level, which is why plan 187-04 gates the template tier on `llm_emit`: an ungated tier would
render the same filename on every phase and make distinct steps identical.

Pass `nameContext` to `WorkflowCanvas` (whose prop plan 187-08 added) and to `PhaseSpineGraph` (whose
prop plan 187-09 added). Use the shipped **spread-conditional** shape for anything that must not reach
the flag-off surface: with the flag off the prop must be genuinely ABSENT from the element, never
present-and-undefined — the D-181-01 byte-identity promise.

**Pitfall 1, decided deliberately.** The maps arrive asynchronously, so on first paint every derived tier
misses and faces settle when the fetch resolves. **Accept the settle** — it matches how `PhaseFormPanel`
already behaves (it shows the raw id until the map lands). Record the decision in a comment naming the
alternative that was rejected (holding the derived tier until the maps are non-empty, which would make
the canvas late for a cosmetic reason) and the floor that forbids the third option (never invent a
placeholder). Manual row M4 watches this live.

**Also thread the two announcement sites** (RESEARCH Open Q6, and the reason it is cheap): `:985`
`nodeTitle(added)` and `:1025` `nodeTitle(before[at])` both feed `setCanvasNotice`, which is what a
screen reader reads out the instant a step is added or removed. Untouched, they would announce *"Added
— Do the work"* while the card the user just created reads *"Run the invoice checker"*. Pass
`nameContext` to both and add it to both `useCallback` dependency arrays (measured: both are `[store]`
today). Cost is enumerated in Task 3's derived cap as component 4 — **5 insertions / 4 deletions**.

The tray half of the same disagreement is **already closed, at zero cost to this file**: `ProblemsTray`
is mounted inside `WorkflowCanvas.tsx:1383`, not by this page, so plan 187-08 threads it there. Do not
add a tray prop here; there is no tray element in this file to add it to.

**The one deferral, deferred by name.** Do **not** thread the context to
`definitionOps.canRemovePhase` (`definitionOps.ts:282`, `const lead = \`"${nodeTitle(referrers[0])}"\``).
Widening that pure module's signature is a separate decision, and its refusal sentence is a shape
predicate rather than a node face.

⚠ **Name the consequence, do not bury it.** After this plan the `Added` and `Removed` notices resolve
the derived face while the `refusal` notice in the *same* notice surface does not — so a user can be
told *"Run the invoice checker"* on one notice and *"Do the work"* on the next. Record this in the
SUMMARY as a **named gap** with its concrete **re-open trigger: Phase 188's `WorkflowCanvas.tsx`
extraction**, which reopens these seams anyway (CLAUDE.md hot-file ledger, and plan 187-08's own G-5
note). It is a known, named gap — never an omission.

Extend `WorkflowBuilderPage.canvas.test.tsx` with the five behaviours. Keep its shipped
no-browser-storage guard green.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The four `WorkflowBuilderPage` suites pass; `canvas.test.tsx`'s count is strictly greater than HEAD's and its shipped no-browser-storage guard (`:414-418`) is green.
    - `grep -c "nameContext" frontend/src/pages/WorkflowBuilderPage.tsx` is between 7 and 10 inclusive — derived: 1 memo declaration + 1 canvas prop + 1 spine spread-conditional + 2 announcement call sites + 2 `useCallback` dependency arrays = 7 required lines, with up to 3 lines of comment allowed. (Case matters: the `type NameContext` import does not match this pattern.)
    - `grep -c "nodeTitle(added, nameContext)" …` and `grep -c "nodeTitle(before\[at\], nameContext)" …` each return 1, and `grep -c "\[store\]" frontend/src/pages/WorkflowBuilderPage.tsx` is 2 lower than HEAD's — both announcement callbacks re-declared their dependencies.
    - `grep -c "nodeTitle(referrers" frontend/src/components/workflows/definitionOps.ts` still returns 1 with ONE argument, and `git status --porcelain frontend/src/components/workflows/definitionOps.ts` is empty — the deferral is real and asserted, not assumed.
    - `grep -cE "listFolders\(|listSkills\(" frontend/src/pages/WorkflowBuilderPage.tsx` is unchanged from HEAD — no second fetch.
    - A test asserts that with the flag OFF the spine element carries no `nameContext` key at all (`'nameContext' in props === false`), not merely `undefined`.
    - A test asserts a definition whose `assets` is `null` and one whose `assets` is a non-array both render without throwing and produce no template face.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>Both graph views resolve faces from one memoised context built from data the page already had.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount the receipt and the template door in one gated line each</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx, frontend/src/pages/WorkflowBuilderPage.describe.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1160-1220` — `onDraft`, the SINGLE state transition at `:1181-1188` (*"the graph renders whole, in one DOM batch (no timed reveal)"*), `setDraftId(null)` at `:1173`, and the `autoDraft` hand-off at `:1202-1216`.
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1440-1470` — the CTA and the `describe-hint` line the door sits beside.
    - `frontend/src/pages/WorkflowBuilderPage.tsx:845-895` — `useGroundingBundle(canvasEnabled)` and where `kbTools` lands.
    - `frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx:405-425` — **the shipped guard asserting the page's source names no browser-storage API.** D-187-09's plain `useState` is therefore structurally required, not merely preferred.
    - `frontend/src/pages/WorkflowBuilderPage.describe.test.tsx` — the flag-off pin from plan 187-05, which must stay green for the flag-off render and be extended for the flag-ON door.
    - `frontend/src/components/workflows/SeedReceipt.tsx` (plan 187-13) and `frontend/src/components/workflows/StarterTemplatePicker.tsx` (plan 187-14) — their prop contracts.
  </read_first>
  <behavior>
    - After a successful `onDraft`, the receipt is shown once with the drafted phases and the server `kbTools`.
    - Dismissing it hides it and it does not reappear while the user keeps working on the same draft; a fresh generation shows a fresh receipt.
    - The receipt also appears on the `autoDraft` hand-off path.
    - With the canvas flag OFF, neither the receipt nor the template door renders anywhere, and the plan 187-05 describe-screen pin still matches byte for byte.
    - With the flag ON, choosing a template fills the `business requirement` textarea, leaves the user on the describe screen, and enables the CTA — and `setDrafted` and the create/publish api symbols are never called on that path.
  </behavior>
  <action>
**The receipt.** Add ONE page-level boolean (`useState`) set `true` in `onDraft`'s success branch —
immediately beside the existing single `setDrafted` transition, so the receipt arrives in the same DOM
batch as the canvas and cannot imply progress — and set `false` by the receipt's `onDismiss`.

D-187-09: dismissal is **in-memory, per draft**. A page reload re-shows it and that is correct — the
grounding it describes is still true, nothing was persisted, and there is no storage key to go stale.
Persisting per draft id was rejected because a seeded draft is not persisted at generation time
(`setDraftId(null)` runs in the same handler), so there may be no id to key on until the first save.
**A `localStorage` key here would fail the shipped `canvas.test.tsx:414-418` guard** — the `useState` is
structurally required, not merely preferred. Say so in the comment.

Mount it above the graph column, gated on `canvasEnabled`, using the shipped `graphColumn` flag-branch
shape. **The receipt MUST be canvas-gated**: `useGroundingBundle` is called with `canvasEnabled`, so with
the flag off `kbTools` is `[]` and every step would read as ungrounded — the receipt would claim zero
grounded steps on a workflow that will be gated at run time. That is actively misleading, not merely out
of scope.

**Claude's Discretion, decided and stated here rather than discovered in UAT:** the receipt also appears
on the `autoDraft` hand-off path (`:1202-1216`, the Phase-124 "Describe & run" door). It is a genuine AI
seed. Because both paths funnel through `onDraft`'s success branch this costs no extra line — record it
in the comment so nobody later reads it as an accident.

**The template door.** Mount `StarterTemplatePicker` on ONE `canvasEnabled`-gated line beneath the CTA,
beside the shipped `describe-hint` line, with `onChoose={setDescribe}`. The picker owns its own trigger,
its own open state and its own fetch (plan 187-14), so the page needs nothing else. The describe screen
renders on BOTH flag branches — only the header wrapper is flag-dependent — so an ungated line would ship
flag-off and break D-181-01 **silently**, which is exactly why plan 187-05 wrote the pin first.

Add nothing else to this file. No new helper function, no new component definition, no new effect.

Extend `canvas.test.tsx` for the receipt behaviours and `describe.test.tsx` for the flag-ON door
behaviours. The flag-OFF pin assertions stay untouched and must still pass — if the literal needs
editing, the door leaked flag-off.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All five `WorkflowBuilderPage` suites pass, 0 failed.
    - `git diff -U0 -- frontend/src/pages/WorkflowBuilderPage.describe.test.tsx | grep -c 'FLAG_OFF_DESCRIBE_MARKUP'` returns 0 — the flag-off literal was not edited.
    - `grep -cE "localStorage|sessionStorage|indexedDB|document\.cookie" frontend/src/pages/WorkflowBuilderPage.tsx` returns 0.
    - `grep -c "SeedReceipt" frontend/src/pages/WorkflowBuilderPage.tsx` is ≤ 3 (import + mount + the dismiss handler reference) and `grep -c "StarterTemplatePicker" …` is ≤ 2 (import + mount).
    - A test asserts the receipt appears after an `autoDraft`-driven draft.
    - A test asserts that after choosing a template the textarea's value is non-empty, the CTA is enabled, and the mocked `setDrafted`/create/publish symbols have zero calls.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>Both surfaces are mounted behind the canvas flag in one line each, and the flag-off screen is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Measure and record the phase gates</name>
  <files>.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` — the empty per-task verification map, the measured baseline table, the gate rules, the Wave-0 checklist and the Manual-Only table.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md` — D-187-14 (the diff is a phase gate) and the test-gating corrections.
    - The SUMMARY files of plans 187-01 through 187-14, for the per-task rows.
  </read_first>
  <action>
Measure and record, with the commands and their raw output:

**(a) The D-187-14 gate — a DERIVED number, component by component.**

D-187-14's literal text is *"the page gains one gated mount line per surface **and nothing else**"*,
and CLAUDE.md's ledger praises Phase 185 for *"only 4 insertions reach the render body"*. A round total
does not check either claim, so the gate is **two numbers**: a structural render-body gate that checks
D-187-14 as written, and a total-diff gate whose value is the SUM of its enumerated parts.

**(a1) The render-body sub-gate — this is the one D-187-14 actually states.** Four counts, all run on
`D=$(git diff -U0 -- frontend/src/pages/WorkflowBuilderPage.tsx)`. Comment lines are filtered out first
with `grep -vE '^\+[[:space:]]*(//|\*|\{/\*)'` (call it `NOCMT`) so that naming a component inside a
comment cannot move a count. ⚠ Use `[[:space:]]`, not `\s` — `\s` is a GNU extension and is not
portable across the `grep` builds on this machine. **These four commands were verified against a
synthetic diff at plan time; run them verbatim.**

  1. **Exactly 2 new component mounts.**
     `echo "$D" | grep '^+' | NOCMT | grep -cE '<(SeedReceipt|StarterTemplatePicker)\b'` → **2**
  2. **No OTHER new JSX element.**
     `echo "$D" | grep '^+' | NOCMT | grep -vE '<(SeedReceipt|StarterTemplatePicker)\b' | grep -cE '^\+[[:space:]]*<[A-Z]'` → **0**
     (The `-v` pass is what keeps the two intended mounts from counting against themselves; the
     leading anchor is what keeps `useMemo<NameContext>` from reading as a JSX tag.)
  3. **Exactly 2 new props on existing JSX elements** — `nameContext` on `WorkflowCanvas` and the
     spread-conditional on `PhaseSpineGraph`.
     `echo "$D" | grep -cE '^\+[[:space:]]*(nameContext=|\{\.\.\.\(canvasEnabled)'` → **2**
  4. **No new component or helper function.**
     `echo "$D" | grep -cE '^\+[[:space:]]*(function |const [A-Z][A-Za-z]* = \()'` → **0**

**(a2) The total-diff gate — derived, not chosen.** Record the measured `git diff --numstat` and check
it against this table. Every row is a line budget for a named component; the cap is their sum.

  | # | Component | Ins | Del |
  |---|---|---|---|
  | 1 | imports — `type NameContext` into the existing `phaseVocabulary` block (`:155`), `SeedReceipt`, `StarterTemplatePicker` | 3 | 0 |
  | 2 | the `nameContext` `useMemo` — 6 lines of code (guard `meta.assets`, `find(kind==="template")`, return, deps) + ≤ 8 lines of comment carrying the D-187-04 DEFINITION-level `assets` note and the Pitfall-1 settle decision with its rejected alternative | 14 | 0 |
  | 3 | the two graph-view passes — 1 prop line on `WorkflowCanvas` (`:1516`), 1 spread-conditional line on `PhaseSpineGraph` (`:1544`), + 2 comment lines | 4 | 0 |
  | 4 | the two announcement sites (`:985`, `:1025`) — 2 call-site rewrites + 2 `useCallback` dependency-array rewrites + 1 comment | 5 | 4 |
  | 5 | the receipt's `useState` + its D-187-09 comment (in-memory per draft; the storage guard makes it structural) | 5 | 0 |
  | 6 | `setShowSeedReceipt(true)` in `onDraft`'s success branch + its `autoDraft` comment | 3 | 0 |
  | 7 | the `SeedReceipt` mount — one gated JSX block (≤ 7 lines once Prettier formats 3 props + the gate) + 2 comment lines | 9 | 0 |
  | 8 | the `StarterTemplatePicker` mount — one gated line (`onChoose={setDescribe}`) + 2 comment lines | 3 | 0 |
  | | **TOTAL — the cap** | **46** | **4** |

  **Gate: at most 46 insertions and at most 5 deletions.** The single extra deletion is the one named
  allowance: Prettier may re-wrap a `nodeTitle(…)` call in component 4 that crosses the print width.
  There is no other slack — the number is the sum above.

  **Why the cap ROSE from the pre-revision 45 while the work got tighter:** 45 was round and
  underived. The derived non-W4 portion is **41** (46 − 5), i.e. 4 lines *tighter* than the old
  number; component 4 (the two announcement sites, RESEARCH Open Q6) then adds 5 insertions and 4
  deletions of genuinely new work. Record both figures — 41 derived + 5 for Open Q6 = 46 — so the
  gate reads as arithmetic, not as a renegotiation.

  **If any component overruns its row, the gate fails.** Do not renegotiate the total: identify which
  numbered row grew and why. Rows 5–8 growing means the surface belongs in its own component file, not
  in this page — which is the whole point of D-187-14.

**(a3) The named gap.** Record in `187-VALIDATION.md`, under the gate section, that
`definitionOps.canRemovePhase` (`definitionOps.ts:282`) is **deliberately not threaded**, that its
consequence is a refusal notice naming a step differently from the `Added`/`Removed` notices beside it,
and that the **re-open trigger is Phase 188's `WorkflowCanvas.tsx` extraction**. A screen-reader
disagreement is a named gap or it is a defect; it is never silent.

**(b) The zero-migration gate.** `git diff --stat -- supabase/migrations` against the phase base must be
EMPTY, and `git status --porcelain supabase/migrations` must be empty. Record both.

**(c) The isolated frontend sets.** Run and record counts for both named sets. **Never the full frontend
suite** — measured 42–49 failures, flaky at one commit, with `PublishGauntlet.test.tsx` and
`WorkflowCanvas.test.tsx` 100% green in isolation. Guard test COUNT, not just failures — a "net-new" file
can silently REPLACE a suite (the Phase-177 lesson). Both counts must be ≥ their baselines plus this
phase's net-new.
  - the 8-file vocabulary/canvas set — baseline **863**
  - the 5-file consumer set (`WorkflowCanvas`, `PublishGauntlet`, `WorkflowBuilderPage.canvas`,
    `ProblemsTray`, `definitionOps`) — baseline **381**
  Use the default vitest reporter — vitest is **4.1.0** and `--reporter=basic` no longer exists (it fails
  with `Failed to load url basic`).

**(d) The backend suite.** `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q` — 0 failed,
collection ≥ **3474** plus this phase's net-new.

**(e) Build gates.** `npx tsc -b` exit 0 (⚠ `tsc -b` ≠ `--noEmit` — the v3.3 lesson) and `vite build`
exit 0.

Then fill `187-VALIDATION.md`:
  - the **Per-Task Verification Map** table, one row per task across all 15 plans, each with its plan,
    wave, requirement, threat ref, automated command and status;
  - a `## Phase gates — measured` section carrying (a)–(e) with their raw numbers and the commands used;
  - tick the **Wave 0 Requirements** checklist items this phase actually delivered;
  - leave the eight **Manual-Only** G-4 rows unticked — they are the operator's, and wire format plus a
    screenshot are insufficient for them;
  - set `nyquist_compliant: true` in the frontmatter **only if** every requirement row in the
    requirement→proof map has a green automated command or an explicitly recorded manual owner. If any
    row is unproven, leave it `false` and name the row.

Do not mark any requirement Complete in `REQUIREMENTS.md` and do not advance `STATE.md` — the
orchestrator owns those writes, and the SDK's `requirements.mark-complete` / `state.advance-plan` /
`roadmap.update-plan-progress` verbs write false records when called from an executor.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && git diff --numstat -- frontend/src/pages/WorkflowBuilderPage.tsx && git status --porcelain supabase/migrations && echo "GATES-MEASURED"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat -- frontend/src/pages/WorkflowBuilderPage.tsx` shows **≤ 46 insertions and ≤ 5 deletions**, and the numbers are transcribed into `187-VALIDATION.md` beside the 8-row derivation table.
    - The render-body sub-gate holds exactly, using the four verbatim commands in Task 3 (a1): named mounts **2**, any other new JSX element **0**, new props on existing elements **2**, new functions/components **0**. All four counts are transcribed into `187-VALIDATION.md`.
    - `187-VALIDATION.md` reproduces the 8-row component table with the measured per-component line counts, and states the `41 derived + 5 for Open Q6 = 46` arithmetic.
    - `187-VALIDATION.md` names the `definitionOps.canRemovePhase` gap and its re-open trigger (Phase 188's `WorkflowCanvas.tsx` extraction) in the gate section.
    - `git status --porcelain supabase/migrations` is empty and `git diff --stat -- supabase/migrations` is empty.
    - The 8-file vocabulary set reports ≥ 863 passed / 0 failed; the 5-file consumer set reports ≥ 381 passed / 0 failed. Both counts appear in `187-VALIDATION.md`.
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q` reports 0 failed with collection ≥ 3474.
    - `npx tsc -b` exits 0 and `npx vite build` exits 0.
    - `187-VALIDATION.md`'s Per-Task Verification Map has no `_TBD at plan time_` row remaining and contains a row for every task in plans 187-01..187-15.
    - `187-VALIDATION.md` contains a `## Phase gates — measured` section with raw command output for (a)–(e).
    - The eight Manual-Only rows are present and unticked.
    - `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` is empty.
  </acceptance_criteria>
  <done>Every phase gate is measured with its raw output recorded, and the validation contract is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the `visual_workflow_canvas` flag → the shipped surface | D-181-01 promises byte-identity flag-off, for everyone including operators |
| page state → two independent graph views | both views must resolve a face the same way or they disagree in front of the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-15-01 | Tampering | the flag-off first screen | mitigate | Both mounts are `canvasEnabled`-gated with the shipped spread-conditional/flag-branch shapes; the plan 187-05 pin must match byte for byte and its literal must be unedited (`git diff` grep returns 0). |
| T-187-15-02 | Information Disclosure | the receipt with the flag off | mitigate | `useGroundingBundle(canvasEnabled)` returns `[]` flag-off, so an ungated receipt would claim zero grounded steps on a workflow that IS gated at run time. Gating it is a correctness requirement, not scope hygiene. |
| T-187-15-03 | Denial of Service | `WorkflowBuilderPage.tsx` maintainability (G-5) | mitigate | Two gates, both measured. **Structural:** 4 comment-filtered grep counts on the diff (2 named mounts / 0 other new elements / 2 new props / 0 new functions), verified against a synthetic diff at plan time — counts, not review. **Total:** ≤ 46 insertions / ≤ 5 deletions, DERIVED as the sum of the 8-row component table in Task 3 (41 for the pre-Open-Q6 shape + 5 for the two announcement sites). An overrun names the row that grew rather than moving the number. |
| T-187-15-07 | Information Disclosure | a step named two ways in one surface | accept | `definitionOps.canRemovePhase`'s refusal sentence keeps the undecorated `nodeTitle`, so a refusal notice can name a step differently from the `Added`/`Removed` notices beside it. Deferred **by name** with a concrete re-open trigger (Phase 188's `WorkflowCanvas.tsx` extraction) and asserted by a criterion that fails if it is closed silently. The canvas/spine/announcement/tray sites — the ones a user meets on the happy path — are all closed. |
| T-187-15-04 | Tampering | browser storage in the page | mitigate | The shipped `canvas.test.tsx:414-418` source guard stays green, and a direct grep for storage APIs returning 0 is an acceptance criterion. D-187-09's `useState` is structurally required. |
| T-187-15-05 | Repudiation | false completion records | mitigate | The plan explicitly forbids touching `REQUIREMENTS.md`, `STATE.md` and `ROADMAP.md`; `git status --porcelain` on all three empty is an acceptance criterion. |
| T-187-15-06 | Repudiation | an over-claimed green suite | mitigate | Gating is on the two isolated named sets with non-decreasing COUNTS, never the flaky full frontend suite; the raw numbers are transcribed into `187-VALIDATION.md`. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts src/components/workflows/canvasModel.test.ts src/components/workflows/canvasModel.purity.test.ts src/components/workflows/canvasModel.roundtrip.test.ts src/components/workflows/canvasModel.fixtures.test.ts src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseSpine.test.tsx src/components/workflows/PhaseNodeCard.test.tsx` → ≥ 863 passed, 0 failed.
- `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/PublishGauntlet.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/workflows/ProblemsTray.test.tsx src/components/workflows/definitionOps.test.ts` → ≥ 381 passed, 0 failed.
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q` → 0 failed, collection ≥ 3474 + net-new.
- `cd frontend && npx tsc -b` exit 0; `npx vite build` exit 0.
- `git diff --stat -- supabase/migrations` empty; `git diff --numstat -- frontend/src/pages/WorkflowBuilderPage.tsx` ≤ 46 insertions / ≤ 5 deletions, and the four render-body counts of Task 3 (a1) are 2 / 0 / 2 / 0.
</verification>

<success_criteria>
- Both graph views render the derived face from one memoised context; the canvas and the spine agree.
- Both `canvasNotice` announcement sites name a step the same way its card does.
- The receipt and the template door are mounted behind the canvas flag in one line each.
- The flag-off describe screen is byte-identical to the plan 187-05 pin.
- Every phase gate is measured and recorded in `187-VALIDATION.md`, including the zero-migration gate and the D-187-14 cap **with its 8-row derivation** and its structural render-body sub-gate.
- The one un-threaded `nodeTitle` call site is recorded as a named gap with a re-open trigger, not omitted.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-15-SUMMARY.md` when done.
</output>
