---
status: testing
phase: 187-business-vocabulary-ai-seeded-canvas
source: [187-VALIDATION.md "Manual-Only Verifications" M1-M14, 187-01..187-25 SUMMARY.md]
started: 2026-08-04T00:00:00.000Z
updated: 2026-08-04T01:00:00.000Z
---

## Current Test

number: 3
name: The plain title survives the Technical-names reveal
expected: |
  Toggle Technical-names ON/OFF x3 on BOTH the canvas and the phase-spine view.
  The plain title stays put; the reveal swaps the SUBTITLE. No layout jump,
  no card-height change.
awaiting: user response

## Tests

### 1. Starter template copy reads like a person typed it
source: M5 (Req 6 / VOCAB-03)
expected: Open the starter-template door on the Builder's first screen and pick each of the 3 templates. Each seeds the describe box with text that reads like a person wrote it, not like a form label or filler.
result: pass — operator confirmed

### 2. Every node face says what THAT step does
source: M1 (Req 1 / SC#5)
expected: Describe and generate a real ~5-step workflow. Read every node face on the canvas aloud. Each one should say what that particular step does, in business language — distinct from its neighbours. No raw ids, no placeholder text, no two cards saying the same thing.
result: pass — operator confirmed. 4-step draft: 'Find supplier contracts and delivery incident reports' (llm_agent) / 'Work out which suppliers breached their contracts' (llm_single) / 'Ask a procurement manager to approve the shortlist' (llm_human_input) / 'Write up the short supplier risk report' (llm_single). Distinct, business-language, no raw ids.

### 3. The plain title survives the Technical-names reveal
source: M2 (Req 4)
expected: Hold Option/Alt to toggle Technical-names ON and OFF three times, on BOTH the canvas view and the phase-spine view. The plain title stays put — the reveal swaps the SUBTITLE underneath it, not the title. Layout must not jump, and card height must not change as you toggle.
result: [pending]

### 4. The card states no capability the step lacks — and still states the one it has
source: M11 (Req 1 / VOCAB-01, WR-02)
expected: Bind a folder to a step that CANNOT search (a "Write it up" / single-LLM step, or a human-input step). That card must NOT say "Search {folder}" — it falls back to its plain type sentence, with no folder name and no raw id anywhere. Then bind the SAME folder to an AGENT step: that one DOES say "Search {folder}". Read both cards side by side on the same canvas — the contrast is the test.
result: [pending]

### 5. The + row promises the sentence the card actually says
source: M12 (Req 1 / VOCAB-01, WR-03)
expected: Open the + menu on the lane and read every row aloud. Click the human-input row. The card that lands says the SAME sentence the row promised ("Wait for your approval") — not a second, different wording. Repeat once with another row (e.g. the deliverable step) as a control that nothing else drifted.
result: [pending]

### 6. The face tracks a skill bind/unbind without a save and without flicker
source: M4 (Req 1, Pitfall 1)
expected: Bind a skill to a step, then unbind it, watching the card face the whole time. The face updates without you pressing save. It may briefly show the generic type sentence before the skill name settles — what it must never do is flash a raw id, a placeholder, or blank, or feel like a flicker.
result: [pending]

### 7. The seed receipt arrives ONCE, as one batch
source: M3 (Req 5)
expected: On a throttled connection, generate a REAL draft (spine "llm_agent to llm_emit", with a step that reads documents). Watch the receipt arrive — it enters ONCE, as a single batch, the seal pulses once, and nothing on the card reads as still-deciding or half-arrived. Watch the entrance, not the final state.
result: pass — MEASURED. MutationObserver caught exactly ONE insertion of <section data-testid="seed-receipt"> at t=16.3s with all 347 chars present. No second insertion, no progressive fill. Network throttled to Slow 4G.

### 8. The receipt's two paragraphs read as ONE honest account
source: M9 (Req 5 / VOCAB-02, CR-01)
expected: On the same draft, read the whole card top to bottom as a person. The "reads your documents, so I set them to must prove it" sentence counts ONLY the steps that actually read documents. The carried paragraph states its count and the seal and says nothing about WHAT applied it. Each row beneath names its own cause. Together the two paragraphs must add up to one coherent account — not a contradiction.
result: pass — operator confirmed. Arithmetic verified: detectedCount=1 matches the single search_documents step, which is the one named. Row carries its own cause 'it reads your documents (search_documents)'. No carried paragraph (nothing pre-escalated).

### 9. The receipt is a RECEIPT, not a live readout
source: M14 (Req 5 / VOCAB-02, CR-04) — the round-4 BLOCKER fix
expected: With the receipt still open, go to the step form panel beside it and make an edit that WOULD have changed one of the card's sentences — e.g. type search_documents into an agent step's tools field. The CANVAS updates. The CARD does not move at all. Critically: does the frozen card read as honest history ("here is what I built"), or does it read like a component that forgot to update? That judgement is the test.
result: pass — operator confirmed. CR-04 falsified live: toggled 'Search documents' OFF on step 1 with the card open. Canvas face changed ('Find supplier contracts and delivery incident reports' -> 'Work out how to do it'); receipt text BYTE-IDENTICAL. Toggled back ON; still byte-identical across both edits. Note: name demotion is one-way, so the receipt names a step whose name no longer appears on the canvas - operator judged this honest history, not staleness.

### 10. The zero-detected draft still arrives as one whole thing
source: M10 (Req 5 / D-187-10)
expected: Describe a workflow with NO retrieval at all — nothing that reads documents. The receipt STILL arrives, heading and closing line intact. The "You can't turn that off" sentence is absent. What remains must read as one deliberate arrival, not as a card with a hole punched in it. Watch the entrance.
result: [pending]

### 11. The escalated-only draft's card agrees with itself
source: M13 (Req 5 / VOCAB-02, WR-09)
expected: On that same no-retrieval draft, turn the grounding dial to "must prove it" on ONE step yourself. Now the only sealed step is one YOU escalated by hand. Read the card top to bottom: the paragraph and the row two lines below it must say the same thing about who applied the seal. Note the row should now read "it was set to must prove it by hand" — it must NOT tell you "YOU turned this on by hand".
result: [pending]

### 12. The env path reaches the authoring model resolver
source: M7 (SC#10)
expected: Restart the backend with HARNESS_AUTHORING_MODEL set to a specific model. Generate a workflow. Confirm the model actually used for authoring is the one you set — visible in backend logs or LangSmith, not assumed.
result: [pending]

### 13. An armed checkpoint cannot be preempted (SC#6 live)
source: M6 (Req 7 / SC#6) — the folded SEED-137 threat
expected: Run an armed phase that also carries an author-declared timing="pre" ask_user validator. Answer the AUTHOR's gate with Proceed. The ARMED checkpoint must still appear in the chat PendingAskCard. Refuse it. The step must NOT run, and harness_audit must have ZERO validator_ask_user_approved rows for it.
result: [pending]

### 14. Flag-OFF Builder first screen is unchanged
source: M8 (D-181-01)
expected: Turn the visual_workflow_canvas feature flag OFF. Open the Builder's first screen. It looks exactly as it did before this phase — in particular there is no starter-template line and no canvas affordance anywhere on it. Then turn the flag back ON.
result: [pending]

## Summary

total: 14
passed: 5
issues: 0
pending: 9
skipped: 0
blocked: 0

## Gaps

- truth: "Phase 187's `unbound_retrieval` verdict is canvas-only and does not change what publishes (`backend/app/api/workflows.py:470-473`, D-187-11)"
  status: failed
  reason: "MEASURED FALSE as shipped. `POST /workflows/validate` with a workflow whose ONLY defect is an unbound retrieval (business_requirement declared, no interactive phase) returns `{ok:false, verdicts:[unbound_retrieval]}`. The client's `blockedReason` (`frontend/src/pages/WorkflowBuilderPage.tsx:1117-1126`) returns the first verdict's message for ANY `ok:false`, and that lands on `disabled={blocked}` at `frontend/src/components/workflows/PublishGauntlet.tsx:922`. So the verdict DOES disable Publish. The route correctly kept the code out of the server's shared `grounding_verdicts` collector — the server-side publish gate is genuinely unchanged — but the comment's claim is about what the AUTHOR experiences, and that claim is false. Operator decision 2026-08-04: the BEHAVIOUR is correct (blocking an unbound retrieval workflow is what BUG-260731-03 wanted); the COMMENT is stale and must be corrected. Same defect class as WR-14, which plan 187-24 closed hours earlier: a docblock asserting a property the file does not have."
  severity: minor
  test: n/a — found during operator investigation of a disabled Publish button, not from a UAT row
  artifacts:
    - path: "backend/app/api/workflows.py"
      issue: "lines 470-473 claim these three route-assigned codes are 'canvas-only' and that 'Phase 187 is not scoped to change what publishes'. Reachable counter-example measured above."
  missing:
    - "A corrected comment stating that route-assigned codes stay out of the SERVER publish gate but DO reach the client's blockedReason, so they gate the Publish control."
    - "A regression fence pinning the property, so the claim cannot drift again."

