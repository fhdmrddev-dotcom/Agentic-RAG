---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 16
type: execute
wave: 5
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/components/workflows/SeedReceipt.test.tsx
  - frontend/src/components/workflows/SeedReceipt.tsx
  - frontend/src/components/workflows/definitionOps.ts
  - frontend/src/components/workflows/definitionOps.test.ts
autonomous: true
requirements: [VOCAB-02]

must_haves:
  truths:
    - "CR-01 closed: no sentence on the receipt claims the AI applied a governance action it did not apply — the 'so I set them to must prove it' lead counts ONLY steps whose cause is `detected`"
    - "D-187-08 — the cause still comes from the shipped `groundingCauseOf` over the server's `kb_tools`; this plan adds no classification and invents no reason string client-side"
    - "D-187-10 — with ZERO sealed steps the receipt still appears, minus the grounded list: one component, one arrival behaviour, and the zero case is now driven by a REPRESENTABLE fixture"
    - "D-185-09 — client-predicts / server-enforces is unchanged: the receipt is display-only and the run-time gate stays unconditional and server-side"
    - "CR-02 closed: every `citation_policy` value in the suite is a member of the backend Literal at `backend/app/models/harness.py` (`strict`/`flag`/`partial`/`draft`), and the shipped DEFAULT `strict` is exercised explicitly"
    - "Every step that wears the ⛨ seal on its card is named on the receipt — the sketch's 'never let the seal arrive unexplained' rule survives the fix rather than being traded for it"
    - "The falsification was OBSERVED RED on HEAD before the fix landed, with both sides of the failing assertion recorded verbatim"
  artifacts:
    - path: "frontend/src/components/workflows/SeedReceipt.test.tsx"
      provides: "the regression class CR-02 hid — per-cause coverage over representable fixtures"
      contains: "already-set"
    - path: "frontend/src/components/workflows/SeedReceipt.tsx"
      provides: "the receipt, with per-cause honesty"
      contains: "data-cause"
    - path: "frontend/src/components/workflows/definitionOps.ts"
      provides: "a detected-only lead plus a sibling sentence that claims no authorship"
      contains: "seedReceiptGroundingLead"
  key_links:
    - from: "frontend/src/components/workflows/SeedReceipt.tsx"
      to: "phaseVocabulary.groundingCauseOf"
      via: "per-step cause over the server-supplied kbTools prop, now READ rather than flattened"
      pattern: "groundingCauseOf"
    - from: "frontend/src/components/workflows/SeedReceipt.tsx"
      to: "definitionOps seed-receipt copy"
      via: "imported sentences, one per cause class, never authored in the component"
      pattern: "seedReceipt"
---

<objective>
Close the phase's single BLOCKER: the seed receipt states a governance claim the AI did not make,
on the majority of generated drafts — and the suite that should catch it is blind because its
fixtures carry a value the backend cannot produce.

Purpose: SC#3's whole argument is that safety must be LEGIBLE. A receipt that misexplains
governance is worse than no receipt, because it teaches a wrong mental model of what the AI did.
This plan makes each sentence true of exactly the steps it counts, and makes the regression
observable for the first time.

Output: an honest receipt, and a suite that can see it move.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-SPEC.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-REVIEW.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md
@.claude/skills/sketch-findings-agentic-rag/references/ai-seed-and-templates.md
@.claude/skills/sketch-findings-agentic-rag/references/graded-governance.md
</context>

<decision_taken_at_plan_time>
**The reviewer offered two fixes. This plan takes the SECOND — keep the wider list, give each
cause its own honest sentence — and NOT the minimal `if (cause !== "detected") continue`.**

The scope fence allows the wider list only if the resulting copy is checked against the
sketch-findings skill. That check was performed at plan time and it changed the answer:

1. `canvasModel.isGrounded` is `groundingCauseOf(phase, kbTools) !== null` — measured live at
   `frontend/src/components/workflows/canvasModel.ts` (the `isGrounded` helper feeding
   `buildPhaseData`). So a card wears the ⛨ seal for ALL THREE causes, `already-set` included.
2. `.claude/skills/sketch-findings-agentic-rag/references/ai-seed-and-templates.md`
   §"What to Avoid" states: **"Never let the seal arrive unexplained. That is the whole of SC#3."**
3. Both curated starter spines are `llm_agent → llm_emit`, and every generated `llm_emit` arrives
   with `citation_policy: "strict"`. Filtering the list to `detected` would therefore leave the
   TYPICAL draft's deliverable step wearing a seal the receipt refuses to mention — trading a
   false sentence for an unexplained mark, i.e. re-opening the exact SC#3 hole the receipt exists
   to close.
4. SPEC Req 5's wording is *"a receipt naming exactly those [grounded] steps"* — and "grounded" is
   the seal, `cause !== null`. The wider list matches the acceptance criterion literally. The
   defect is entirely in the LEAD's authorship claim, and that is what this plan repairs.

Sketch 150-B's four load-bearing properties are all preserved: per-step **with its cause**
(`seedReceiptStepReason` already carries honest per-cause strings), the one-way rule stated
plainly, dismissible, and a close that says nothing is committed.

**The one-way rule moves with the lead.** `SEED_RECEIPT_ONE_WAY_RULE` ("You can't turn that off")
is the DETECTED lock (D-185-07 — detection wins and the undo disappears). An `already-set` step is
held by the author's own `citation_policy` dial and an `escalated` step by the author's own hand;
both are undoable by their author. Rendering "you can't turn that off" over them is the same false
claim wearing a different sentence, so it is gated on the detected count too.
</decision_taken_at_plan_time>

<interfaces>
```ts
// frontend/src/components/workflows/phaseVocabulary.ts (shipped, Phase 185 + 187-04)
export type GroundingCause = "detected" | "already-set" | "escalated" | null
export function groundingCauseOf(phase: PhaseSpecJSON, kbTools: readonly string[]): GroundingCause
export function nodeTitle(phase: PhaseSpecJSON, ctx?: NameContext): string

// frontend/src/components/workflows/definitionOps.ts (shipped, plan 187-10)
export const GOVERNANCE_SEAL_LABEL = "Must prove it"
export function seedReceiptGroundingLead(groundedCount: number): string   // ← re-specified here
export function seedReceiptStepReason(cause: GroundingCause, tool?: string | null): string
export const SEED_RECEIPT_ONE_WAY_RULE: string
```

```python
# backend/app/models/harness.py — LlmEmitPhaseConfig (READ-ONLY source of truth)
citation_policy: Literal["strict", "flag", "partial", "draft"] = "strict"
```
`POST /generate` returns `wd.model_dump(mode="json")`, which EMITS defaults — so every generated
`llm_emit` phase reaches the client carrying `citation_policy: "strict"`. `"loose"` is not a
member of that Literal and no `model_validate()`-passing row can carry it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the suite representable, then WATCH IT GO RED</name>
  <files>frontend/src/components/workflows/SeedReceipt.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/SeedReceipt.test.tsx` in full — the file being modified. Note the two fixture blocks `GROUNDED_PHASES` and `BARE_PHASES`, the `GROUNDED_SLUGS` / `UNGROUNDED_SLUGS` pair, the `listedSlugs()` helper and its recorded first-draft mistake, and the six describe blocks.
    - `frontend/src/components/workflows/SeedReceipt.tsx` — the component under test as it stands at HEAD, especially the `rows` `useMemo` and the `groundingLead` conditional.
    - `backend/app/models/harness.py` — `LlmEmitPhaseConfig`. **Re-verify the `citation_policy` Literal members and its default at live HEAD** (the line number in the review may have drifted; find the symbol, not the line). This is the file that decides which fixture values are representable at all.
    - `frontend/src/components/workflows/phaseVocabulary.ts` — `groundingCause` / `groundingCauseOf` and `GROUNDING_DIAL_TYPES`, so the three cause branches are read from source rather than from this plan.
    - `frontend/src/components/workflows/definitionOps.ts` — the seed-receipt copy block (`seedReceiptHeading`, `seedReceiptGroundingLead`, `SEED_RECEIPT_ONE_WAY_RULE`, `seedReceiptStepReason`, `SEED_RECEIPT_NOTHING_COMMITTED`).
    - `.claude/skills/sketch-findings-agentic-rag/references/ai-seed-and-templates.md` — the receipt's four load-bearing properties and the "never let the seal arrive unexplained" rule.
  </read_first>
  <behavior>
    - Every `citation_policy` in the suite is a member of the backend Literal; `"loose"` appears nowhere.
    - The shipped DEFAULT `"strict"` is exercised on an `llm_emit` fixture, because that is the only shape `/generate` ever emits.
    - The receipt lists exactly the SEALED steps (cause not null) — asserted by membership, never a count.
    - The detected lead counts exactly the `detected` steps, and no others.
    - A draft with zero detected steps but one `already-set` step renders NO detected lead and NO one-way rule, while still naming that step with its own reason.
    - An `escalated` step is named with its own reason and is not counted by the detected lead.
    - A draft where nothing is sealed at all still renders heading and closing line and renders no list (D-187-10) — driven by a representable non-strict emit policy.
  </behavior>
  <action>
Repair the fixtures first, then extend.

**The representability repair (CR-02).** In `GROUNDED_PHASES`, change the `emit` phase's
`citation_policy` from `"loose"` to `"strict"` — the real, only-reachable default. In
`BARE_PHASES` — the D-187-10 zero case — `"strict"` would make the draft non-empty, so use a
DIFFERENT representable member (`"draft"`) and say in a comment why the zero case cannot use the
default. Add a short comment block above both fixtures naming `LlmEmitPhaseConfig.citation_policy`
in `backend/app/models/harness.py` as the source of the member set, stating that `"loose"` was
never a member and that a fixture avoiding the default is a fixture avoiding the shipped shape.
Assemble the forbidden literal `"loose"` from parts or omit it entirely so a grep of this guard
file cannot satisfy its own fence (the idiom this suite already uses for the unshipped glyphs).

**Run the suite now and RECORD THE RED.** Do not touch the component. The falsifying assertions
are the ones that matter and their two sides must be captured verbatim into the SUMMARY:
  - `SeedReceipt — the copy is the copy module's › renders each sentence identically…` —
    `seed-receipt-lead` will hold the lead composed for a count of **3** while the expectation is
    `seedReceiptGroundingLead(2)`; record both strings exactly as vitest prints them.
  - `SeedReceipt — the grounded steps and their reasons › lists EXACTLY the steps that read the
    documents, and no others` — `listedSlugs()` will now include `emit`.
  - `SeedReceipt — the zero-grounded draft › renders NO grounded-step list node…` — driven over a
    strict-emit variant of `BARE_PHASES`, this reds because the list exists. (Keep `BARE_PHASES`
    itself at `"draft"`; add the strict variant as its own case so both are covered.)

**Then re-derive the assertions the repair legitimately invalidates — honestly, not weakened.**
Two shipped assertions were only green because the branch they cross was disabled:
  - `lists EXACTLY the steps that read the documents, and no others` is now two separate claims.
    Split it: one case asserting membership of the SEALED list (`GROUNDED_SLUGS` widened to include
    `emit`, renamed to say "sealed"), and one asserting that the DETECTED subset is exactly the two
    KB-reading steps. Introduce `DETECTED_SLUGS` beside the sealed list.
  - `marks the grounded count on the surface itself` now reads **3**, because three steps wear a
    seal. That is the honest number and it is the number of ⛨ marks the canvas will draw. Re-derive
    it against the sealed count and add a companion assertion for a new `data-detected-count`.
State in the file's docblock that these two were re-derived rather than relaxed, and why.

**Then add the coverage CR-02 says was missing.** New cases:
  - an `already-set` step (an `llm_emit` at the default `"strict"`) is LISTED, its reason equals
    `seedReceiptStepReason("already-set")`, and the DETECTED lead does not count it;
  - an `escalated` step (an `llm_agent` with `grounding_escalated: true` and NO KB tool in
    `available_tools`) is LISTED, its reason equals `seedReceiptStepReason("escalated")`, and the
    detected lead does not count it;
  - a draft with **zero detected** and **one already-set** renders neither the detected lead node
    nor `SEED_RECEIPT_ONE_WAY_RULE`, while still rendering the row and its reason;
  - the **authorship word-class fence**: for that same zero-detected draft, the receipt's full
    `textContent` contains no first-person application claim — assert the absence of the substrings
    the lead is built from (the "so I set" construction), assembled from parts, with a POSITIVE
    control asserting the same substring IS present on a detected-bearing draft. This is the guard
    that makes CR-01 a permanent regression class rather than a one-time repair;
  - every listed row exposes `data-cause` equal to the cause `groundingCauseOf` returns for its
    phase — computed in the test from the imported derivation, never hand-typed.

Do not change `listedSlugs()`'s `[data-slug]` resolution strategy — its recorded first-draft
mistake is a comment worth keeping true.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Before any component edit, `cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx` FAILS, and the SUMMARY records at minimum the received-vs-expected pair for `seed-receipt-lead` (received: the lead composed for 3; expected: `seedReceiptGroundingLead(2)`), plus the `listedSlugs()` membership failure naming `emit`.
    - `grep -c 'citation_policy: "loose"' frontend/src/components/workflows/SeedReceipt.test.tsx` returns 0.
    - Every `citation_policy:` value literal in the file is one of `strict` / `flag` / `partial` / `draft` — verified against the `Literal` read out of `backend/app/models/harness.py` at live HEAD, and the member list is quoted in a comment beside the fixtures.
    - The file contains a case whose name mentions `already-set` and a case whose name mentions `escalated`.
    - The file contains a case asserting that a zero-detected / one-already-set draft renders no `seed-receipt-lead` and no `seed-receipt-one-way` node.
    - The authorship word-class fence has a POSITIVE control (it asserts the "so I set" construction IS present on a detected-bearing draft), so it cannot pass by asserting a substring that never existed.
    - `DETECTED_SLUGS` and a sealed-slug list both exist and are distinct arrays.
    - `cd frontend && npx tsc -b` produces no NEW error and none inside `src/components/workflows/` (33 pre-existing errors, `deferred-items.md` D-ITEM-01 — measure before and after and record both).
  </acceptance_criteria>
  <done>The suite exercises only states the backend can produce, covers all three causes, and is RED on HEAD for the reason CR-01 names.</done>
</task>

<task type="auto">
  <name>Task 2: One honest sentence per cause, and a receipt that explains every seal</name>
  <files>frontend/src/components/workflows/definitionOps.ts, frontend/src/components/workflows/definitionOps.test.ts, frontend/src/components/workflows/SeedReceipt.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/definitionOps.ts` — the whole Phase-187 seed-receipt copy block, including the "WHY THE COPY LIVES HERE" and "WHAT IS LOCKED vs WHAT IS CLAUDE'S" notes and the `wholeCount` totality helper. Re-verify the current body of `seedReceiptGroundingLead` at live HEAD before editing.
    - `frontend/src/components/workflows/SeedReceipt.tsx` — the file being modified: the `rows` `useMemo`, `intersectingKbTool`, the `groundingLead` conditional and the `<li>` render.
    - `frontend/src/components/workflows/SeedReceipt.test.tsx` as written in Task 1 — the contract this task must satisfy.
    - `frontend/src/components/workflows/definitionOps.test.ts` — the shipped seed-receipt copy tests and the T-187-10 copy/word-class guard idiom (needles assembled from parts).
    - `frontend/src/components/workflows/phaseVocabulary.ts` — `GroundingCause` and `groundingCause`'s three numbered branches, so the new sentence names causes the derivation actually produces.
    - `.claude/skills/sketch-findings-agentic-rag/references/ai-seed-and-templates.md` — the receipt's voice and its four load-bearing properties; the copy added here must not break any of them.
    - `.claude/skills/sketch-findings-agentic-rag/references/graded-governance.md` — the one-way lock and the honest-refusal vocabulary, so the new sentence does not accidentally promise a lock it does not have.
  </read_first>
  <action>
**`definitionOps.ts` — split the claim, do not widen it.**

Re-specify `seedReceiptGroundingLead` so its parameter is the **detected** count only. The returned
strings are UNCHANGED — they were always true of `detected` steps and only of those. Rewrite its
docblock to say so explicitly, name the defect it is closing (the review's CR-01), and state that
`already-set` and `escalated` are the author's own doing and therefore have no place under a
sentence beginning "so I set". Keep the `GOVERNANCE_SEAL_LABEL` composition and the `wholeCount`
totality treatment exactly as they are.

Add ONE sibling formatter for the carried causes — the `already-set` + `escalated` total. Its
contract, which the tests pin:
  - returns the empty string at zero, exactly as its sibling does, so the component asks the
    question once per paragraph and keeps one arrival shape (D-187-10);
  - **claims no authorship** — no first-person application verb ("I set", "I turned on", "I
    applied"). The step is already held and this generation did not do it;
  - is **self-contained**: the detected sentence may be absent, so this one may render first or
    alone. It must not lean on an antecedent ("that", "those", "more") that only exists when the
    detected sentence is present;
  - is a formatter over a count with singular/plural handled in one rule, not three literals;
  - does not promise a one-way lock — these causes are undoable by their author.
The exact wording is Claude's discretion, bounded by the four properties above and by CONTEXT
§"Claude's Discretion — the receipt's exact copy". Write the reasoning into the docblock so the
next reader does not have to re-derive it.

Extend `definitionOps.test.ts` with the new formatter's unit cases (zero ⇒ empty string; singular;
plural; a non-finite / negative / fractional count resolves, matching the shipped `wholeCount`
treatment) and one word-class guard asserting the new sentence contains no first-person
application construction, with a POSITIVE control against the detected lead which does. Assemble
needles from parts, the idiom this file already uses.

**`SeedReceipt.tsx` — read the cause instead of flattening it.**

Keep `if (cause === null) continue` — every sealed step stays listed, which is what keeps the
canvas's seal explained. Carry `cause` onto the `GroundedRow` and render it as `data-cause` on each
`<li>`, beside the existing `data-slug`.

Derive two counts from `rows`: the number whose cause is `"detected"`, and the remainder. Render
the detected paragraph — `seedReceiptGroundingLead(detectedCount)` plus
`SEED_RECEIPT_ONE_WAY_RULE` — only when the detected count is non-zero, keeping the existing
`data-testid` values (`seed-receipt-grounding`, `seed-receipt-lead`, `seed-receipt-one-way`) so no
shipped consumer assertion moves for the wrong reason. Render the new carried sentence in its own
paragraph, with its own `data-testid`, only when the carried count is non-zero. The list itself
renders whenever `rows` is non-empty.

Expose `data-detected-count` on the `<section>` alongside the existing `data-grounded-count`;
`data-grounded-count` keeps meaning the SEALED total, which is exactly the number of ⛨ marks the
canvas draws for the same draft.

Update the component docblock: the "ONE ARRIVAL BEHAVIOUR" section currently says the whole of the
conditional is `seedReceiptGroundingLead(n) !== ""` — that stops being true and must be restated
rather than left to rot (the IN-02 rule this phase's own review applied). Add a short section
naming CR-01, what the sentence used to claim, and why every sealed step is still listed.

Author no sentence in the component — every user-visible string stays an imported identifier.
Do not add a scheduler call, a frame callback, a per-row stagger or an index-derived delay: the
no-staging source fence must stay satisfiable. Do not touch `frontend/src/pages/WorkflowBuilderPage.tsx`
— the D-187-14 mount cap was measured AT the cap and this plan spends none of it.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx src/components/workflows/definitionOps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx src/components/workflows/definitionOps.test.ts` reports 0 failed, and BOTH per-file counts are strictly greater than their pre-plan values (record both before and after — the Phase-177 count lesson).
    - `grep -c "data-cause" frontend/src/components/workflows/SeedReceipt.tsx` is at least 1.
    - `grep -c "data-detected-count" frontend/src/components/workflows/SeedReceipt.tsx` is at least 1.
    - `grep -cE "from ['\"]@/lib/api['\"]|fetch\(|XMLHttpRequest|EventSource" frontend/src/components/workflows/SeedReceipt.tsx` returns 0.
    - `grep -cE "setTimeout|setInterval|requestAnimationFrame|animationDelay|transitionDelay" frontend/src/components/workflows/SeedReceipt.tsx` returns 0.
    - `grep -c "search_documents" frontend/src/components/workflows/SeedReceipt.tsx` returns 0 — no local KB-tool list.
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/workflows/SeedReceipt.tsx` returns 0.
    - The component docblock no longer asserts that `seedReceiptGroundingLead(n) !== ""` is the whole of the conditional.
    - `git status --porcelain frontend/src/pages/WorkflowBuilderPage.tsx` is empty.
    - `git diff --stat -- supabase/migrations` is empty and `git status --porcelain supabase/migrations` is empty.
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <done>Every sentence on the receipt is true of exactly the steps it counts, every sealed step is still explained, and the regression is now visible to the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a frontend-only, display-only change. The honest threat surface is small and is stated
plainly rather than padded.

| Boundary | Description |
|----------|-------------|
| server `kb_tools` → client grounding read | the safety-defining list is the server's; the client only intersects and renders |
| model-authored step names / tool ids → DOM | untrusted generated text reaches the receipt |
| the receipt's sentences → the user's mental model of what the AI did | the only boundary this plan actually moves |

No package is installed by this plan, so the supply-chain gate has nothing to inspect; no route,
no auth check and no persisted field is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-16-01 | Repudiation | `seedReceiptGroundingLead` / `SeedReceipt` | mitigate | The lead counts only `detected` rows and the one-way rule renders with it; a word-class fence with a positive control asserts no first-person application claim appears over a non-detected draft. This is CR-01 itself. |
| T-187-16-02 | Spoofing | the client-side "grounded" read | accept | Display-only by construction (D-185-09); the run-time citation gate is unconditional and server-side. A wrong read is a display bug, never a safety hole. |
| T-187-16-03 | Tampering | a second grounding derivation | mitigate | `groundingCauseOf` stays the only classifier; the component adds no branch and no KB-tool literal — `grep` for a tool id returns 0. D-182-06 red line. |
| T-187-16-04 | Input Validation (V5) / XSS | generated step names and tool ids | mitigate | Every server- or model-authored string renders as a plain React text child; `dangerouslySetInnerHTML` count 0 is an acceptance criterion (the T-184-08-01 rule). |
| T-187-16-05 | Tampering | test fixtures drifting from representable state | mitigate | Every `citation_policy` literal in the suite is checked against the backend `Literal` at live HEAD, and the default is exercised explicitly. This is CR-02 itself. |
| T-187-16-06 | Repudiation | implying an event that did not occur | mitigate | The no-staging source fence is preserved — no scheduler call, frame callback or index-derived delay is added. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx src/components/workflows/definitionOps.test.ts` → 0 failed, both counts up.
- `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/PublishGauntlet.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/workflows/ProblemsTray.test.tsx src/components/workflows/definitionOps.test.ts` → 0 failed (the shipped 5-file consumer set, run ISOLATED — never the full frontend suite, which is measured flaky at 42–49).
- `cd frontend && npx vite build` exits 0.
- `git status --porcelain frontend/src/pages/WorkflowBuilderPage.tsx supabase/migrations` is empty.
</verification>

<success_criteria>
- The receipt never claims the AI applied a gate the author's own configuration applied.
- Every step wearing a ⛨ seal on the canvas is named on the receipt with a reason true of it.
- A zero-sealed draft still shows the receipt, minus the list (D-187-10), driven by a fixture the backend can produce.
- The falsification was observed RED first, with both sides recorded.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-16-SUMMARY.md` when done.
</output>
