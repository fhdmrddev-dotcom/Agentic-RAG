---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 11
type: execute
wave: 3
depends_on: ["187-06"]
files_modified:
  - backend/tests/unit/test_ask_user_disposition.py
  - backend/tests/unit/test_185_engine_attachment.py
  - backend/tests/unit/test_187_armed_checkpoint_property.py
autonomous: true
requirements: [VOCAB-02]

must_haves:
  truths:
    - "The SC#6 property test is GREEN after the hoist, and every assertion that was RED on HEAD is green for the reason the hoist provides — not because an assertion was weakened"
    - "The falsification is OBSERVED, not assumed: with the checkpoint temporarily removed the property goes RED again for the right reason, and the observation is recorded"
    - "test_185_engine_attachment.py:153-165 (the WR-03 retry-seed proof, the ROADMAP threat-model item) is green UNMODIFIED"
    - "test_a_freshness_pre_gate_still_fails_exactly_as_it_shipped and test_non_armed_free_text_fall_through_is_unchanged are green UNMODIFIED — the asymmetry net"
    - "test_arming_adds_no_phase_and_moves_no_phase_index is visibly re-shaped with the reasoning recorded: its substance (arming adds no phase and shifts no phase_index) still holds; only the mechanism assertion moves"
    - "The armed prompt sentence assertions move onto grounding._approval_sentence directly rather than onto a synthesized ValidatorSpec's config"
  artifacts:
    - path: "backend/tests/unit/test_ask_user_disposition.py"
      provides: "the 8 armed tests re-pointed at the is_action_risk parameter"
    - path: "backend/tests/unit/test_185_engine_attachment.py"
      provides: "the re-shaped criterion-18 test + the re-pointed armed engine tests"
  key_links:
    - from: "backend/tests/unit/test_ask_user_disposition.py"
      to: "harness_engine._resolve_failure_with_ask_user"
      via: "is_action_risk=True keyword instead of the action_risk:approval| finding sniff"
      pattern: "is_action_risk"
---

<objective>
Turn the SC#6 property green, re-shape the shipped test census the hoist changed, and observe the falsification.

Purpose: plan 187-06 leaves nine shipped tests red by construction — eight in
`test_ask_user_disposition.py` drive `_resolve_failure_with_ask_user` with an `action_risk:approval|`
finding string, and `test_arming_adds_no_phase_and_moves_no_phase_index` asserts the synthesized
`ValidatorSpec` that no longer exists. Re-shaping them is planned work, not a side effect. And the
Phase-185 lesson is explicit: **observe falsification RED first, and verify the PROPERTY not the
PATCH** — a property that has never been seen to fail after the fix is not a control either.

Output: a green property, a green backend suite, and a recorded falsification.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-SPEC.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-PATTERNS.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-01-SUMMARY.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-shape the eight armed tests in test_ask_user_disposition.py</name>
  <files>backend/tests/unit/test_ask_user_disposition.py</files>
  <read_first>
    - `backend/tests/unit/test_ask_user_disposition.py:31-120` — `_armed_phase()`, `_ARMED_FINDING`, `_ctx()`, and the rendezvous mock idiom at `:86-120`.
    - `backend/tests/unit/test_ask_user_disposition.py:280-620` — the eight armed tests: `:296` choices, `:351` decline, `:377` approval+receipt, `:405` empty answer, `:431` decline by index, `:539` the typed refusal (the T-185-04-01 proof), `:582` approval by index, and `:599` the **non-armed free-text fall-through that must stay green UNCHANGED**.
    - `backend/app/services/harness_engine.py` `_resolve_failure_with_ask_user` (as edited by plan 187-06) — the `is_action_risk` keyword and the short-circuited disposition.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-06-SUMMARY.md` — the exact failing set recorded at that wave boundary.
  </read_first>
  <action>
`_armed_phase()` currently builds a phase whose `validators[0]` is the synthesized
`action_risk_approval` spec — the shape `effective_phase` used to produce. After the hoist that shape
no longer occurs. Re-shape the fixture to what the engine now hands the helper: a phase with
`action_risk_armed=True` and **the author's own validators** (an empty list by default, plus a
parametrizable author set for the tests that need one).

Re-point all eight armed tests to pass `is_action_risk=True` and `failed_idx=None` instead of relying
on the `_ARMED_FINDING` prefix to trigger the armed treatment. Keep `_ARMED_FINDING` — the finding
string is still how DELTA 1 splits the prompt out, so the tests still pass a prefixed message; what
changes is that the ARMED TREATMENT is now requested explicitly rather than sniffed.

Preserve every assertion's substance. In particular:
  - `:539`'s typed refusal must still assert the run fails and **no** approval receipt is written. This
    is the T-185-04-01 proof; if its assertion has to change shape at all, say exactly how and why in a
    comment.
  - `:377`'s approval receipt assertion must now expect `metadata["validator"]` to be `None` for the
    armed row (D-187-18) while the key remains PRESENT. Add a comment naming D-187-18 and stating that
    this is a deliberate governance-ledger shape change: after the hoist there is no validator index.
  - `:599`'s non-armed free-text fall-through must stay **byte-identical**. It is the asymmetry proof —
    an author's ordinary `ask_user` gate on the same phase must keep its shipped behaviour (its own
    timeout, no shutdown escape, no exact-match allow-list). If this test needs any edit, stop: the
    parameter leaked into the non-armed path.

Add ONE net-new test to this file: an armed call on a phase whose author validators declare
`on_failure: "fail_run"` still ASKS — the Pitfall-4 fail-open, asserted at the helper level rather
than only through the property. Assert `subscribe_for_response` was awaited.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_ask_user_disposition.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_ask_user_disposition.py -q` passes with a count = HEAD's count + 1.
    - `git diff -U0 -- backend/tests/unit/test_ask_user_disposition.py | grep -c 'non_armed_free_text'` returns 0 — that test is unmodified.
    - `grep -c "is_action_risk=True" backend/tests/unit/test_ask_user_disposition.py` is ≥ 8.
    - `_armed_phase` no longer constructs a `ValidatorSpec(kind="action_risk_approval"…)`: `grep -c 'kind="action_risk_approval"' backend/tests/unit/test_ask_user_disposition.py` returns 0.
    - The receipt test asserts `metadata["validator"] is None` AND that `"validator" in metadata` is True, with a D-187-18 comment.
    - A test named with the substring `fail_run` asserts an armed call on a `fail_run`-declaring phase still awaits `subscribe_for_response`.
  </acceptance_criteria>
  <done>The eight armed tests drive the parameter, the receipt's new shape is pinned, and the non-armed proof is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-shape criterion 18 and re-point the armed engine tests</name>
  <files>backend/tests/unit/test_185_engine_attachment.py</files>
  <read_first>
    - `backend/tests/unit/test_185_engine_attachment.py:136-205` — `test_a_deliberately_weak_author_spec_cannot_loosen_the_gate` (`:153-165`, **must stay green UNMODIFIED**) and `test_arming_adds_no_phase_and_moves_no_phase_index` (`:170-201`, whose `:191-196` asserts the synthesized spec's `kind` and `config["prompt"]`).
    - `backend/tests/unit/test_185_engine_attachment.py:205-260` — `test_an_ungoverned_phase_is_returned_by_reference` (which STRENGTHENS after the hoist — more phases now return by reference) and `test_grounding_declares_no_model_validator_in_CODE`.
    - `backend/tests/unit/test_185_engine_attachment.py:320-560` — the armed engine tests to re-point: `:325` subscribes with no timeout, `:376` criterion 19 unanswered armed gate, `:407-451` the character-identical prompt (already imports `_approval_sentence` directly at `:418`), `:452` null deadline never zero, `:492` shutdown mid-wait resumable, and `:523` **shutdown on a NON-armed gate — must stay green UNCHANGED**.
    - `backend/tests/unit/test_185_engine_attachment.py:560-760` — `:560` criterion 20 `llm_human_input` untouched, `:647` an armed pre-gate records a pause not a failure, `:666` never announces gate_failed, `:683` **a freshness pre-gate still fails exactly as it shipped — must stay green UNCHANGED**, `:737-760` the two independent resume predicates.
    - `backend/app/services/harness/grounding.py` `effective_phase` (as edited by plan 187-06).
  </read_first>
  <action>
**Re-shape `test_arming_adds_no_phase_and_moves_no_phase_index` visibly, with the reasoning recorded.**
Its substance — arming adds no phase and shifts no `phase_index` — is still true and still worth
asserting; only the mechanism assertion moves. After the hoist, `effective_phase` on an armed phase
returns either the phase itself by reference (no `citations_required` owed) or a copy carrying only the
`citations_required` spec. Assert that, and assert `len(definition.phases)` and every `phase_index`
are unchanged. Move the SENTENCE assertions onto `grounding._approval_sentence` directly — that is
where the honesty rules live, and `:407-451` already imports it that way.

Write a comment above the re-shaped test recording: what it asserted before, why the mechanism changed
(D-187-01: the guarantee is a property of the phase, not a position in a list), and that its substance
is unchanged. The ROADMAP threat-model item permits a visible re-shape with reasoning recorded; it does
not permit a silent deletion.

**Re-point the six armed engine tests** (`:325`, `:376`, `:452`, `:492`, `:647`, `:666`) at the hoisted
checkpoint's entry point: an armed phase now reaches the pause through `_run_phase_with_gates`'s
explicit checkpoint rather than through a failing pre-gate. Keep every assertion's substance —
no timeout at all on the armed subscribe, a null deadline never zero, an unanswered armed gate does not
advance the run, a shutdown mid-wait leaves the run resumable, an armed pre-gate records a PAUSE
(`action_risk_pending`) not a failure, and it never announces `gate_failed` to the frontend.

**Four tests must stay green UNMODIFIED** and are the regression net; if any needs an edit, stop and
report, because it means the hoist changed a path it should not have:
  - `:153-165` `test_a_deliberately_weak_author_spec_cannot_loosen_the_gate` (the ROADMAP item)
  - `:523` `test_shutdown_on_a_NON_armed_gate_still_fails_the_run` (the asymmetry proof)
  - `:560` `test_criterion_20_exec_llm_human_input_is_untouched`
  - `:683` `test_a_freshness_pre_gate_still_fails_exactly_as_it_shipped` (the regression net)

`test_the_two_resume_predicates_are_independent` (`:737-760`) is unaffected and pins
`_is_armed_action_risk` as the second, independent phase-level armed reading — leave it, and reference
it in the re-shape comment as the reason D-187-03's "exactly ONE reading" was recorded as reconciled
rather than delivered.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_185_engine_attachment.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_185_engine_attachment.py -q` passes with a count ≥ HEAD's (25 tests).
    - `git diff -U0 -- backend/tests/unit/test_185_engine_attachment.py | grep -cE 'weak_author_spec|shutdown_on_a_NON_armed|criterion_20|freshness_pre_gate_still_fails'` returns 0 — the four regression-net tests are unmodified.
    - `test_arming_adds_no_phase_and_moves_no_phase_index` carries a comment naming D-187-01, stating what it asserted before and that its substance is unchanged.
    - `grep -c 'kind == "action_risk_approval"' backend/tests/unit/test_185_engine_attachment.py` returns 0.
    - The re-shaped test asserts both `len(definition.phases)` unchanged and every `phase_index` unchanged.
    - `pytest tests/unit/test_185_engine_attachment.py tests/unit/test_ask_user_disposition.py tests/unit/test_validator_kinds.py tests/unit/test_harness_models.py tests/unit/test_182_severity_codes.py -q --no-header` reports a total ≥ 72 with 0 failed.
  </acceptance_criteria>
  <done>The census is re-shaped visibly, the four regression-net tests are untouched, and the 5-file quick set is green at ≥ 72.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the property green, then observe the falsification</name>
  <files>backend/tests/unit/test_187_armed_checkpoint_property.py</files>
  <read_first>
    - `backend/tests/unit/test_187_armed_checkpoint_property.py` — in particular its `## RED signature observed on HEAD` section.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-01-SUMMARY.md` — the recorded RED signature.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` §"The SC#6 property test — how to make it count", points 1 and 5.
    - `backend/app/services/harness_engine.py` — the hoisted checkpoint (plan 187-06).
  </read_first>
  <action>
**(a) Green.** Run the property. It must pass for every case. If a case needs the `total_phases`
keyword threaded through the direct call, add it — that is a harness fix. Any OTHER change to this
file must be recorded in the SUMMARY with a sentence explaining why it is not a weakening, and must be
checked against the recorded RED signature: an assertion that was red on HEAD must still be capable of
going red. Do not delete a case, do not relax an assertion, and do not add an `xfail`.

**(b) Falsification, OBSERVED.** Temporarily remove the hoisted checkpoint from `harness_engine.py`
(comment out the block), re-run the property, and confirm it goes RED **for the right reason** — the
same assertion and the same parametrize ids as the recorded HEAD RED signature, for the cases that
reach the body. Capture the output. Then restore the checkpoint and re-run to confirm green again.

Record in the file's docblock, under a new `## Falsification observed after the fix` heading: what was
removed, which cases went red, which assertion fired, and that the checkpoint was restored. A property
test that has never been seen to fail after the fix is not a control — this is the Phase-185 lesson
stated as a procedure.

Verify at the end that `git status --porcelain backend/app` is empty: the temporary removal must be
fully reverted. Do not commit the falsified state.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_armed_checkpoint_property.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_187_armed_checkpoint_property.py -q` passes, 0 failed.
    - The file contains a `## Falsification observed after the fix` section naming what was removed, the parametrize ids that went red, and the assertion that fired.
    - The falsification's red set overlaps the HEAD RED signature's set — record both lists side by side in the SUMMARY.
    - `git status --porcelain backend/app` is empty after the task.
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q 2>&1 | tail -3` reports 0 failed and a collection count ≥ 3474 + the phase's net-new tests.
  </acceptance_criteria>
  <done>The SC#6 property is green, and it has been observed to fail again when the checkpoint is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the test suite → the governance guarantee | the suite is the only standing evidence that armed ⇒ asked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-11-01 | Repudiation | the property test's credibility | mitigate | Falsification is OBSERVED, not assumed, and its red set is compared against the recorded HEAD RED signature. Any edit to the property file must be justified as a non-weakening. |
| T-187-11-02 | Elevation of Privilege | the armed treatment leaking to author gates | mitigate | Four regression-net tests must be UNMODIFIED (`git diff` grep returns 0). If any needs an edit, the task stops — that is the signal the parameter leaked. |
| T-187-11-03 | Repudiation | the governance-ledger receipt shape | mitigate | The `validator: null` shape (D-187-18) is pinned by a test asserting both the value AND the key's presence, so a future edit cannot quietly drop the key. |
| T-187-11-04 | Elevation of Privilege | the Pitfall-4 fail-open | mitigate | A net-new helper-level test asserts an armed call on a `fail_run`-declaring phase still awaits the rendezvous, so the short-circuit is proved at the helper as well as through the property. |
| T-187-11-05 | Tampering | production source left falsified | mitigate | `git status --porcelain backend/app` empty is an acceptance criterion of Task 3. |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_armed_checkpoint_property.py tests/unit/test_185_engine_attachment.py tests/unit/test_ask_user_disposition.py tests/unit/test_validator_kinds.py tests/unit/test_harness_models.py tests/unit/test_182_severity_codes.py tests/unit/test_pre_post_timing.py -q --no-header` → 0 failed.
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q 2>&1 | tail -3` → 0 failed, collection ≥ 3474 + net-new.
- `git status --porcelain backend/app` empty.
</verification>

<success_criteria>
- The SC#6 property is green after the hoist and red again when the checkpoint is removed.
- The nine shipped tests the hoist changed are re-shaped with reasoning recorded.
- The four regression-net tests, including the ROADMAP threat-model item, are green unmodified.
- The full backend suite is green.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-11-SUMMARY.md` when done.
</output>
