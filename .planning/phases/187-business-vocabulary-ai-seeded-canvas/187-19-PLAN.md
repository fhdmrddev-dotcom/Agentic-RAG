---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 19
type: execute
wave: 6
depends_on: ["187-16", "187-17", "187-18"]
gap_closure: true
files_modified:
  - .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-03]

must_haves:
  truths:
    - "G-4 is honoured for the closure round: `187-VALIDATION.md` carries a lived-experience row for every user-visible change the three fix plans made, authored in VALIDATION.md and never inside a PLAN task"
    - "M3 is unblocked and says WHY it was blocked and what changed — the operator is no longer being asked to confirm a receipt that lies"
    - "D-187-10 — the zero-sealed arrival behaviour is on the manual board, because a render test cannot say whether a receipt with one fewer paragraph still reads as one arrival"
    - "D-187-14 — the `WorkflowBuilderPage.tsx` mount cap is RE-MEASURED for the closure round and shown to be unmoved: this round spends none of the budget that shipped at the cap"
    - "D-181-01 — the flag-OFF describe-screen row (M8) and the template-copy row (M5) survive the edit unticked and unre-scoped; blocked or unperformed rows are recorded, never silently omitted"
    - "Every phase gate is measured with raw output at the closure commit, and no false completion record is written"
  artifacts:
    - path: ".planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md"
      provides: "the closure round's per-task verification rows, the unblocked M3, and the new manual rows"
      contains: "M3"
  key_links:
    - from: ".planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md"
      to: "the 187-16 / 187-17 / 187-18 task ids"
      via: "the per-task verification map, one row per task with its automated command"
      pattern: "187-16-T1"
---

<objective>
Close the round honestly: put the operator's lived-experience rows where they belong, unblock the
row the BLOCKER was holding, and measure the gates instead of asserting them.

Purpose: the phase's own verification recorded M3 as *"recommend deferring until CR-01 is fixed, or
the operator will be confirming a receipt that lies."* CR-01 is now fixed, so M3 is owed an unblock
— and the three fixes each changed something a person sees, which under G-4 means a row a person
must look at. UAT rows live in VALIDATION.md, never in a PLAN task.

Output: an updated validation contract and a measured gate record for the closure round.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-REVIEW.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unblock M3 and give the closure round its own rows</name>
  <files>.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` in full — the file being modified. Note the frontmatter (`status`, `nyquist_compliant`, `phase_base`), the §"Per-Task Verification Map" table and its column set, §"Phase gates — measured", §"Manual-Only Verifications" (M1–M8) and the closing §"Validation Sign-Off" with its three deliberately-red SC#10 rows.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md` — the `gaps:` frontmatter block and the `human_verification` block, so M3's recorded blocking reason is quoted rather than paraphrased.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-16-SUMMARY.md`, `187-17-SUMMARY.md`, `187-18-SUMMARY.md` — the three closure summaries, for the measured counts and the recorded RED signatures the new rows must reference.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-REVIEW.md` §CR-01 / §CR-02 / §WR-02 / §WR-03 — the four findings whose user-visible effect the new manual rows exist to check.
    - `CLAUDE.md` §"Workflow guardrails" G-4, and §"UAT scoreboard recipe" — specifically the rule that blocked rows are recorded with their reason and blocking id, never omitted.
  </read_first>
  <action>
Edit `187-VALIDATION.md` in place. Do not create a second validation file — one home per concern.

**Add a dated closure-round section** above the existing per-task map, stating that plans 187-16 /
187-17 / 187-18 are gap closure against `187-VERIFICATION.md`'s one BLOCKER plus the two operator-
folded Warnings, and naming the base commit the round's gates are measured against.

**Extend the §"Per-Task Verification Map" table** with one row per task of the three closure plans —
`187-16-T1`, `187-16-T2`, `187-17-T1`, `187-17-T2`, `187-18-T1`, `187-18-T2` — using the shipped
column set (Task ID · Plan · Wave · Requirement · Threat Ref · Secure Behavior · Test Type ·
Automated Command · File Exists · Status). Take each row's automated command from the plan's own
`<verify>` block and each threat ref from its `<threat_model>`. Mark Status from the SUMMARY's
recorded result, not from an assumption.

**Unblock M3.** Rewrite its row so it: states that the CR-01 blocker it was waiting on is closed and
by which plan; keeps the original blocking reason quoted so the history is not erased; and keeps the
row UNPERFORMED and UNTICKED — unblocking a row is not performing it.

**Add the new manual rows** (continue the numbering from M8), each with the four shipped columns
(Behavior · Requirement · Why Manual · Test Instructions). At minimum:
  - **the receipt's new honesty**, on a REAL generated draft whose spine is `llm_agent → llm_emit`
    (the typical shape both curated starters produce): the detected sentence counts only the steps
    that read documents; the deliverable step is still named and still explains its own seal; and no
    sentence claims the AI applied a gate the author's own configuration applied. Why manual: this
    is a judgement about whether the two paragraphs read as one honest account, which no render test
    can make;
  - **the zero-detected draft** (D-187-10): describe a workflow with no retrieval, confirm the
    receipt still arrives with its heading and its closing line, that the "you can't turn that off"
    sentence is absent, and that whatever remains still reads as ONE arrival rather than a card with
    a hole in it;
  - **the non-retrieval node face** (WR-02): bind a folder on a step type that cannot search and
    confirm the card no longer says "Search {folder}"; then bind the same folder on an agent step
    and confirm it still does. Why manual: the positive half is what proves the gate did not
    over-tighten, and the pair is only convincing when a person sees both cards at once;
  - **the picker→card agreement** (WR-03): open `＋` on the lane, read every row aloud, click the
    human-input row, and confirm the card that lands says the same sentence the row promised. Why
    manual: the drift was semantic, not lexical — a person reading two sentences is the instrument.

Leave M1, M2, M4, M5, M6, M7 and M8 exactly as they are, unticked. In particular do not re-scope,
soften or drop M5 (the template copy, VOCAB-03) or M8 (the flag-OFF first screen, D-181-01) — both
remain the operator's and both remain owed.

**Update the frontmatter and the sign-off** to reflect the closure round: keep the three deliberately-
red SC#10 rows and their reasons intact, and keep `nyquist_compliant`'s stated meaning intact — it
claims an instrument was RUN and recorded, never that every instrument came back green. Add the
closure round's own line to the sign-off list rather than rewriting the shipped one.

Change no other planning file. `REQUIREMENTS.md`, `STATE.md` and `ROADMAP.md` are the orchestrator's.
  </action>
  <verify>
    <automated>grep -c "187-16-T1\|187-17-T1\|187-18-T1" .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "187-16-T1" …/187-VALIDATION.md`, `grep -c "187-17-T1" …`, `grep -c "187-18-T1" …` each return at least 1, and all six closure task ids are present.
    - Every new per-task row carries a non-empty Automated Command cell copied from that task's `<verify>` block.
    - The M3 row no longer contains the words recommending deferral, and it names the plan that closed CR-01.
    - The manual table contains at least four rows numbered above M8, and each has a non-empty "Why Manual" cell that is a reason, not a restatement of the behavior.
    - `grep -c "^| M5 \|^| M8 " …/187-VALIDATION.md` still returns 2 — the VOCAB-03 / D-181-01 rows survive the edit.
    - No manual row is marked performed or ticked by this task.
    - `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` is empty.
  </acceptance_criteria>
  <done>The lived-experience board covers every user-visible change this round made, M3 is unblocked without being performed, and nothing shipped was dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the closure round's gates with raw output</name>
  <files>.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` §"Phase gates — measured" as written at the close of 187-15 — the file being modified. Note the (a1)/(a2) `WorkflowBuilderPage.tsx` sub-gates, the (b) zero-migration gate, the (c) isolated frontend sets with their measured counts, the (e) build gates with the 33-error `tsc -b` note, and (f) the no-false-completion-record check.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-16-SUMMARY.md`, `187-17-SUMMARY.md`, `187-18-SUMMARY.md` — for each plan's own measured before/after counts, so this task can cross-check rather than transcribe.
    - `CLAUDE.md` §"Planning workflow" and the migration rule — the zero-migration gate is a truth this phase's verification already pinned (truth #11) and a migration here would falsify it.
  </read_first>
  <action>
Add a dated **"Phase gates — gap-closure round"** subsection under the existing §"Phase gates —
measured". Every number in it must come from a command run now, with its raw output pasted, not
transcribed from a plan's own SUMMARY. Where a figure disagrees with a SUMMARY, record BOTH and say
which is the measurement.

Measure and record:
  - **the zero-migration gate** — `git diff --stat {closure base} HEAD -- supabase/migrations` and
    `git status --porcelain supabase/migrations`, both expected empty. State the base commit used;
  - **the D-187-14 mount cap, re-measured** — `git diff --numstat {closure base} HEAD -- frontend/src/pages/WorkflowBuilderPage.tsx`, expected EMPTY output for this round (the file shipped at its cap and this round spends none of it). If it is not empty, that is a gate failure, not a renegotiation;
  - **the isolated frontend sets** — the 8-file vocabulary/canvas set and the 5-file consumer set,
    each run ISOLATED, with per-set totals and failure counts, compared against the values recorded
    at the close of 187-15. Guard the COUNT, not just the failures (the Phase-177 lesson): every
    touched suite must be at or above its prior count, and no shipped file's count may drop;
  - **the closure round's own suites** — `SeedReceipt.test.tsx`, `definitionOps.test.ts`,
    `phaseVocabulary.test.ts`, `phaseVocabulary.corpus.test.ts`, `StepTypePicker.test.tsx` — before
    and after counts for each;
  - **`tsc -b`** — run it and record the error count and whether any error is inside
    `src/components/workflows/` or `src/pages/WorkflowBuilderPage*`. The criterion is *no NEW error,
    none in the touched files*; `tsc -b` has never exited 0 on this repo (D-ITEM-01). Note again
    that `tsc -b` is not `--noEmit`;
  - **`vite build`** — exit code, expected 0;
  - **no false completion record** — `git status --porcelain .planning/REQUIREMENTS.md
    .planning/STATE.md .planning/ROADMAP.md`, expected empty.

⚠ **Never run the full frontend suite as a gate.** It is measured flaky at 42–49 failures at one
commit; the two isolated named sets are the gate, exactly as the shipped §(c) says.

⚠ **Do not call `requirements.mark-complete`, `state.advance-plan` or `roadmap.update-plan-progress`
from this executor.** All three write false records in this project; the orchestrator owns those
writes.

Close the subsection with a short honest statement of what this round did and did NOT prove — in
particular that the four new manual rows and M1–M8 remain unperformed and are the operator's.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx src/components/workflows/definitionOps.test.ts src/components/workflows/phaseVocabulary.test.ts src/components/workflows/phaseVocabulary.corpus.test.ts src/components/workflows/StepTypePicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The five-suite command above reports 0 failed, and each file's count is recorded before and after in the new subsection.
    - `git diff --stat -- supabase/migrations` and `git status --porcelain supabase/migrations` are both empty, with the raw output pasted into the file.
    - `git diff --numstat {closure base} HEAD -- frontend/src/pages/WorkflowBuilderPage.tsx` produces empty output, pasted verbatim.
    - Both isolated named sets are recorded with per-set totals and 0 failures, and each is at or above its 187-15 value.
    - The `tsc -b` record states the total error count and explicitly states the count inside `src/components/workflows/` and `src/pages/WorkflowBuilderPage*` (expected 0), measured before and after.
    - `npx vite build` exit code 0 is recorded.
    - `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` is empty.
    - The subsection contains no figure that is not accompanied by the command that produced it.
  </acceptance_criteria>
  <done>Every gate for the closure round is measured with raw output, and the round's limits are stated rather than implied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only. This plan changes no source file, ships no code path and installs no package.

| Boundary | Description |
|----------|-------------|
| the validation record → the next reader's belief about what was proved | the only boundary in play: a validation file that overstates coverage is the same failure class as CR-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-19-01 | Repudiation | the gate record | mitigate | Every figure is accompanied by the command that produced it and is measured now, not transcribed from a SUMMARY; disagreements are recorded on both sides. |
| T-187-19-02 | Tampering | a silently dropped manual row | mitigate | An acceptance criterion re-counts the M5 and M8 rows after the edit; the roster rule (recorded, never omitted) is applied to the manual board. |
| T-187-19-03 | Spoofing | a false completion record | mitigate | The three GSD verbs known to write false records are named and forbidden in the task action, and a clean `git status` over the three planning files is an acceptance criterion. |
| T-187-19-04 | Tampering | a migration slipping into a zero-migration phase | mitigate | The zero-migration gate is re-measured against the closure base with raw output; truth #11 would be falsified by any non-empty result. |
</threat_model>

<verification>
- `grep -c "187-16-T1\|187-17-T1\|187-18-T1" .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` ≥ 3.
- `git status --porcelain frontend/src backend/app supabase/migrations` is empty — this plan touches no source.
- `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` is empty.
</verification>

<success_criteria>
- M3 is unblocked, with its history intact and without being performed.
- Four new lived-experience rows cover the receipt's honesty, the zero-detected arrival, the non-retrieval face and the picker→card agreement.
- The closure round's gates are measured with raw output, including the re-measured D-187-14 cap and the zero-migration gate.
- No shipped row was dropped and no false completion record was written.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-19-SUMMARY.md` when done.
</output>
