---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_187_armed_checkpoint_property.py
autonomous: true
requirements: [VOCAB-02]

must_haves:
  truths:
    - "A property test over author-declared validator SETS exists and is observed RED on HEAD, before any fix (SPEC Req 7 acceptance)"
    - "The RED signature (which assertion, which parametrize id, what value) is recorded verbatim in the test file docblock"
    - "The space contains both known bypasses: [pre/ask_user]+Proceed and the Pitfall-4 [pre/fail_run] disposition-routing bypass"
    - "The observable is 'was the body invoked' — a spy over _execute_phase — never 'was a validator present'"
    - "A typed refusal asserts ZERO write_audit awaits with event_type='validator_ask_user_approved' (the Phase-185 BLOCKER was a FALSE receipt, not a missing prompt)"
  artifacts:
    - path: "backend/tests/unit/test_187_armed_checkpoint_property.py"
      provides: "SC#6 property over author validator sets, RED on HEAD"
      contains: "parametrize"
  key_links:
    - from: "backend/tests/unit/test_187_armed_checkpoint_property.py"
      to: "app.services.harness_engine._run_phase_with_gates"
      via: "direct call with the EFFECTIVE phase from grounding.effective_phase"
      pattern: "_run_phase_with_gates"
    - from: "backend/tests/unit/test_187_armed_checkpoint_property.py"
      to: "app.services.harness.grounding._approval_sentence"
      via: "imported to identify the armed prompt — never re-typed"
      pattern: "_approval_sentence"
---

<objective>
Write the SC#6 property test and observe it RED on HEAD, before any fix exists.

Purpose: SPEC Req 7's acceptance is *"a property test over author-declared validator sets is observed
FAILING on HEAD before the fix and passing after"*. The Phase-185 lesson is blunter: *verify the
PROPERTY not the PATCH, and observe falsification RED first*. A property test first run after the fix
proves nothing. This plan exists so the RED observation cannot be skipped or retro-fitted.

Output: `backend/tests/unit/test_187_armed_checkpoint_property.py`, red on HEAD, with the RED
signature recorded in its own docblock.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-SPEC.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-PATTERNS.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
</context>

<interfaces>
The functions this test drives, as they ship at `develop` @ `132b9b26`:

- `harness_engine._run_phase_with_gates(phase, accumulated_outputs, ctx, *, run_id, pool, redis, wall_clock, _audit_user_id, stream_run_id=None) -> PhaseOutcome | None`
- `harness_engine._execute_phase(phase, accumulated, ctx)` — THE BODY, at `harness_engine.py:747`, inside `while True:` at `:743`
- `harness_engine._resolve_failure_with_ask_user(phase, error_message, attempt, failed_idx, *, run_id, pool, redis, ctx, _audit_user_id, stream_run_id=None, produced_output=None, is_pre=False)`
- `harness.grounding.effective_phase(phase, *, total_phases: int) -> PhaseSpec`
- `harness.grounding._approval_sentence(phase, total_phases: int) -> str`
- `ask_user_service.subscribe_for_response(redis, run_id, tool_call_id, timeout)` — patched at `app.services.ask_user_service.subscribe_for_response`
- `harness.validators.run_gates(phase, output, ctx, *, timing) -> GateResult` — full-list `validator_index`, first-failure-wins
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build the armed-phase harness and the author-validator-set space</name>
  <files>backend/tests/unit/test_187_armed_checkpoint_property.py</files>
  <read_first>
    - `backend/tests/unit/test_pre_post_timing.py:1-140` — the `body_ran` sentinel driving the REAL `_run_phase_with_gates` with `_execute_phase` patched. This is the observable shape to copy.
    - `backend/tests/unit/test_ask_user_disposition.py:1-130` — `_armed_phase()`, `_ARMED_FINDING`, `_ctx()`, and the `subscribe_for_response` rendezvous mock at `:86-120`.
    - `backend/tests/unit/test_ask_user_disposition.py:520-600` — `:539` the typed-refusal / T-185-04-01 proof, `:599` the non-armed free-text fall-through that must stay green.
    - `backend/app/services/harness_engine.py:684-760` — the pre-gate block, the bypass comment at `:739`, `attempt = 0` at `:741`, `while True:` at `:743`, the body at `:747`.
    - `backend/app/services/harness/grounding.py:850-953` — `_approval_sentence` at `:850-874`, `effective_phase`'s docblock at `:877-915`, the armed append at `:920-933`, the `citations_required` append at `:935-949`.
    - `backend/app/services/harness/validators.py:214-249` — the timing filter and first-failure-wins.
    - `backend/app/services/harness/validator_kinds.py` — the registered kinds, to pick author gates that pass or fail deterministically.
  </read_first>
  <action>
Create `backend/tests/unit/test_187_armed_checkpoint_property.py`.

The phase under test is the **EFFECTIVE** phase, not the raw one: build a raw `PhaseSpec(slug="send-the-notice", phase_index=1, config={"phase_type": "programmatic", "fn": "noop"}, action_risk_armed=True, validators=<AUTHOR SET>)` and pass it through `grounding.effective_phase(raw, total_phases=4)` before handing it to `_run_phase_with_gates`. This is what `run_workflow` does at `harness_engine.py:1336-1339`, and it is the only construction under which the HEAD bypass is reproducible — a raw armed phase with `validators=[]` never had a gate at all.

Define a module-level `CASES` table. Each entry carries: a stable parametrize id, the AUTHOR validator list, a scripted list of `subscribe_for_response` answers (consumed in call order), and an explicit `reaches_body_without_arming: bool` stating whether that author set would let control reach the body if arming did not exist. Required entries, minimum:

  1. `no_author_validators` — `[]`, `reaches_body_without_arming=True`
  2. `pre_ask_user_proceed` — one `timing="pre"`, `on_failure="ask_user"` gate that fails; answer `"Proceed anyway"`; `reaches_body_without_arming=True`. **This is the known HEAD bypass.**
  3. `pre_fail_run` — one `timing="pre"`, `on_failure="fail_run"` gate that fails; `reaches_body_without_arming=False`. **This is the Pitfall-4 fail-open shape** — after the hoist, `_failing_on_failure(phase, None)` must not be allowed to resolve this author disposition for the armed checkpoint.
  4. `pre_skip_to_phase` — one `timing="pre"`, `on_failure="skip_to_phase:done"` gate that fails; `reaches_body_without_arming=False`
  5. `post_only` — one `timing="post"` gate; `reaches_body_without_arming=True` (post gates cannot preempt a pre-body checkpoint)
  6. `pre_ask_user_plus_post_citations` — `[pre/ask_user, post/citations_required]`; answer `"Proceed anyway"`; `reaches_body_without_arming=True`
  7. `two_pre_ask_user` — two `timing="pre"`, `on_failure="ask_user"` gates; `reaches_body_without_arming=True`
  8. `pre_pass_then_pre_ask_user` — a passing pre gate followed by a failing `ask_user` pre gate (permutation coverage)
  9. `armed_refused_typed` — `[]` author set, armed answer is a **typed free-text refusal** that is NOT the exact approval label; `reaches_body_without_arming=True`

Build author validators from **real registered kinds** (read `validator_kinds.py`); record in a comment which kind each row uses and why it fails or passes deterministically. Do NOT patch `run_gates` if a real kind gives the needed determinism. If a needed shape has no deterministic real kind, patch `app.services.harness.validators.run_gates` with a fake that (a) honours the `timing` filter and (b) returns the FIRST failure carrying its FULL-LIST index — and add one companion test asserting the fake and the real `run_gates` agree on a shared case, so the fake cannot drift.

Provide three shared helpers:
  - `_ctx()` — copy `test_ask_user_disposition.py:60-70` verbatim in shape (`supabase=None`, `thread_id=None`, `current_user={"id": uuid4()}`, `producer_run_id=uuid4()`, `emit=AsyncMock()`, `retry_feedback=None`).
  - `_body_spy()` — an async `_execute_phase` replacement recording `invoked: bool` and the monotonic order index of its first call.
  - `_prompt_recorder()` — a `subscribe_for_response` `AsyncMock` whose `side_effect` pops the scripted answers in order, and which records the `tool_call_id` plus a monotonic order index per call, so "was the armed prompt awaited BEFORE the body" is an ordering assertion, not an inference.

Identify the armed prompt by importing `grounding._approval_sentence` and comparing against the sentence it returns for the phase under test — never by re-typing the sentence, and never by substring-matching a hand-written fragment. The honesty rules of that sentence are already pinned character-identically by `test_185_engine_attachment.py:407-451`.

Patch `harness_engine.write_audit` and `harness_engine._emit` with `AsyncMock`s and keep the write_audit mock addressable so Task 2 can assert on its `await_args_list`.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_armed_checkpoint_property.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_187_armed_checkpoint_property.py --collect-only -q` collects at least 9 parametrized cases and exits 0.
    - The file contains `effective_phase(` — the phase under test is the effective phase, not the raw one.
    - The file contains `_approval_sentence` imported from `app.services.harness.grounding`; `grep -c 'is about to run' backend/tests/unit/test_187_armed_checkpoint_property.py` returns 0 (the sentence is never re-typed).
    - `CASES` contains parametrize ids `pre_ask_user_proceed`, `pre_fail_run` and `armed_refused_typed`.
    - Every case entry declares `reaches_body_without_arming` explicitly (no default).
    - If `run_gates` is patched, a companion test named with the substring `fake_run_gates_agrees_with_the_real_one` exists.
  </acceptance_criteria>
  <done>The property harness and the 9-case author-validator-set space collect cleanly on HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Write the three property assertions, including the negative receipt assertion</name>
  <files>backend/tests/unit/test_187_armed_checkpoint_property.py</files>
  <read_first>
    - `backend/tests/unit/test_187_armed_checkpoint_property.py` (as written in Task 1)
    - `backend/app/services/harness_engine.py:944-1229` — in particular the exact-match approval ALLOW-LIST at `:1194-1198` (the T-185-04-01 fail-open fix) and the receipt write at `:1200-1229` (`event_type="validator_ask_user_approved"`, `metadata={"phase", "validator", "choice", "finding"}`).
    - `backend/tests/unit/test_ask_user_disposition.py:520-560` — the typed-refusal assertion shape.
  </read_first>
  <action>
Add three assertions, run over every case in `CASES`:

**P1 — body implies asked-first (universal).** If the `_execute_phase` spy records `invoked is True`, then the armed prompt (matched against `_approval_sentence`) must have been awaited on `subscribe_for_response` at least once, and its recorded order index must be strictly less than the body's. Assert both the presence and the ordering; do not infer ordering from call counts.

**P2 — reaching the body implies asked (targeted).** For every case whose `reaches_body_without_arming` is `True`, assert the armed prompt was awaited **exactly once**. This is the assertion that catches the HEAD bypass on `pre_ask_user_proceed`: the body runs while the armed prompt count is 0.

**P3 — a refusal writes no receipt (negative).** For the `armed_refused_typed` case assert all three:
  - `_execute_phase` was never invoked;
  - the returned outcome is a `fail_run`;
  - `write_audit` was awaited **ZERO** times with `event_type="validator_ask_user_approved"` — computed by filtering `write_audit.await_args_list` on the `event_type` kwarg, not by asserting a total await count. Phase 185's BLOCKER was a FALSE approval receipt, not a missing prompt, so this assertion is the one that would have caught it.

Add a module docblock stating: (a) that this file is the SC#6 property and was written before any fix; (b) that the observable is body invocation, not validator presence; (c) that the space is an explicit table rather than `hypothesis` (measured: `hypothesis` is not a backend dependency); (d) a placeholder heading `## RED signature observed on HEAD` that Task 3 fills.

Do NOT modify any production source file in this plan.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_armed_checkpoint_property.py -q --no-header 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The file contains an assertion filtering `write_audit.await_args_list` on `event_type` equal to `"validator_ask_user_approved"` and asserting the filtered length is `0`.
    - The file contains an explicit ordering comparison between the armed-prompt call index and the body call index (not a bare `assert body_ran is False`).
    - `git status --porcelain backend/app frontend/src supabase` is empty — this plan touches no production source.
    - The run exits non-zero (the test is expected RED at this point); Task 3 records exactly why.
  </acceptance_criteria>
  <done>P1, P2 and P3 are written; the suite runs and reports failures rather than errors.</done>
</task>

<task type="auto">
  <name>Task 3: Observe RED on HEAD and record the signature</name>
  <files>backend/tests/unit/test_187_armed_checkpoint_property.py</files>
  <read_first>
    - `backend/tests/unit/test_187_armed_checkpoint_property.py` (as written in Task 2)
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` §"The SC#6 property test — how to make it count"
  </read_first>
  <action>
Run the file on unmodified HEAD and capture the failure output verbatim.

Fill the `## RED signature observed on HEAD` section of the module docblock with, for each failing parametrize id: the assertion that failed, the observed value and the expected value, and one sentence naming the mechanism (for `pre_ask_user_proceed`: `run_gates` returns the author's failure first at `validators.py:248`, `_is_action_risk_finding` is False so the armed branch is skipped, and `harness_engine.py:739` falls through to the body without the armed prompt).

If any case is unexpectedly GREEN on HEAD, say so in the docblock and explain why (e.g. `no_author_validators` is green because `effective_phase` appends the armed spec and it is the only pre gate). Do not delete a green case — the property must hold for every set, and the green ones are the regression net.

If NO case is red, stop and report: the property as written does not reproduce the SEED-137 bypass and the space or the construction is wrong. Do not proceed to the fix.

Record the same summary in the plan SUMMARY under a `## RED signature` heading, including the exact command used and the pytest tail output.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_armed_checkpoint_property.py -q --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The pytest run exits non-zero with at least one FAILED case, and `pre_ask_user_proceed` is among the failures.
    - The module docblock's `## RED signature observed on HEAD` section names, for each failure: the parametrize id, the failing assertion, the observed value and the expected value.
    - The docblock cites `harness_engine.py:739` as the mechanism for the `pre_ask_user_proceed` failure.
    - The backend 5-file quick set is still 72 passed: `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_185_engine_attachment.py tests/unit/test_ask_user_disposition.py tests/unit/test_validator_kinds.py tests/unit/test_harness_models.py tests/unit/test_182_severity_codes.py -q --no-header` reports `72 passed`.
    - The SUMMARY contains a `## RED signature` section with the verbatim pytest tail.
  </acceptance_criteria>
  <done>The SC#6 property is RED on HEAD for the right reason, and the reason is written down where the fix plan can be checked against it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-declared definition JSONB → harness engine | An author controls `phase.validators` entirely; the engine must not let that control disable a governance gate |
| human answer (Redis rendezvous) → engine disposition | A free-text answer crosses into an approval decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-01-01 | Elevation of Privilege | `_run_phase_with_gates` pre-body path | mitigate | The property (P1/P2) is the control being authored here; it must be RED on HEAD or it is not a control. Task 3 refuses to proceed if nothing is red. |
| T-187-01-02 | Repudiation | `harness_audit.validator_ask_user_approved` | mitigate | P3 asserts ZERO approval receipts on a typed refusal, filtering `await_args_list` on `event_type` — the exact shape of the Phase-185 BLOCKER T-185-04-01. |
| T-187-01-03 | Tampering | the test's own `run_gates` fake | mitigate | If `run_gates` is patched, a companion test pins the fake against the real function on a shared case, so a fake that drifts fails loudly rather than silently weakening the property. |
| T-187-01-04 | Denial of Service | production source | accept | This plan modifies no production file; `git status --porcelain backend/app` empty is an acceptance criterion. |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_armed_checkpoint_property.py -q --no-header` exits non-zero with `pre_ask_user_proceed` FAILED.
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_185_engine_attachment.py tests/unit/test_ask_user_disposition.py tests/unit/test_validator_kinds.py tests/unit/test_harness_models.py tests/unit/test_182_severity_codes.py -q --no-header` → `72 passed`.
- `git status --porcelain backend/app frontend/src supabase` is empty.
</verification>

<success_criteria>
- The SC#6 property test exists, quantifies over 9+ author validator sets, and is RED on HEAD.
- Both known bypasses (`[pre/ask_user]`+Proceed, `[pre/fail_run]`) are in the space.
- The negative receipt assertion is present and filters on `event_type`.
- The RED signature is recorded in the file and in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-01-SUMMARY.md` when done.
</output>
