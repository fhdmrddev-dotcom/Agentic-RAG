---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 21
type: execute
wave: 8
depends_on: ["187-20"]
gap_closure: true
files_modified:
  - .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
autonomous: true
requirements: [VOCAB-02]

# WAVE 8, not 7, and deliberately so: this plan transcribes 187-20's RAW MEASURED OUTPUT
# (three falsification probes, the testid sweep before and after, the final totals). A plan
# that records another plan's measurements cannot run in the same wave as the plan producing
# them. This is the shape the previous closure round used verbatim — fixes at wave 5,
# 187-19's record at wave 6 — and the ROADMAP's own wave bullets already number it this way.

must_haves:
  truths:
    - "G-4 is honoured for this closure round: `187-VALIDATION.md` carries a lived-experience row for the user-visible sentence 187-20 changed, authored in VALIDATION.md and never inside a PLAN task"
    - "M3 and M9's stale warnings are rewritten: both currently tell the operator an escalated-only draft shows an internally-contradictory card, which stops being true when 187-20 lands"
    - "A NEW manual row covers the escalated-only draft head-on — the exact case WR-09 broke and no shipped row tests directly"
    - "D-187-10 — the zero-carried arrival is on the record with its measured evidence: the receipt still arrives whole with the carried paragraph absent, not an empty one"
    - "D-187-08 — the record states that no second grounding derivation was added; `groundingCauseOf` remains the one home and the receipt still only renders what it is handed"
    - "D-187-14 — the `WorkflowBuilderPage.tsx` mount cap is RE-MEASURED for this round and shown unmoved, and the zero-migration gate is re-measured with raw output"
    - "Every manual row's performed/unperformed status stays honest: no row is ticked, softened, re-scoped or silently dropped, and the count of manual rows only grows"
    - "No false completion record is written: `requirements.mark-complete`, `state.advance-plan` and `roadmap.update-plan-progress` are not called"
  artifacts:
    - path: ".planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md"
      provides: "the round-3 task rows with raw output, the amended M3/M9 notes, and the new escalated-only lived-experience row"
      contains: "187-20-T1"
  key_links:
    - from: ".planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md"
      to: "187-20's three falsification probes"
      via: "a per-probe row recording the mutation, the observed RED and the clean revert"
      pattern: "PROBE"
---

<objective>
Record what round 3 actually measured, and repair the two manual rows this closure makes stale.

Purpose: 187-20 changes a user-visible sentence on a sketched, operator-approved surface, so under
CLAUDE.md's G-4 gate it owes lived-experience rows. It also produces the evidence that distinguishes
this closure from the one before it — three falsification probes observed RED and reverted — and that
evidence is worthless if it lives only in a transcript. Two shipped rows actively mislead once the fix
lands: M3 and M9 both warn the operator that an escalated draft shows a contradictory card.

Output: an updated validation contract whose every figure carries the command that produced it, and a
manual board that is one row larger and zero rows softer.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-20-PLAN.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-20-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record round 3 — the task rows, the probes, and the gates, all with raw output</name>
  <files>.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` — ALL of it, but
      especially the gap-closure section that opens at "## The gap-closure round — 2026-08-02" and its
      per-plan table, and the gate subsections that record the zero-migration and D-187-14 measurements.
      This task ADDS a sibling section for round 3; it does not rewrite round 2's.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-19-PLAN.md` — the shape this task
      copies: every figure accompanied by the command that produced it, disagreements recorded on both
      sides, and the round's limits stated rather than implied.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-20-SUMMARY.md` — the raw output to
      transcribe. Do NOT re-derive figures that plan already measured; transcribe them and say which
      commit they were measured at.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md` — the `gaps` block,
      so the record names the truth this round closes (truth #5, VOCAB-02 Req 5) and the two findings
      (CR-03, WR-09) by id.
  </read_first>
  <action>
    Add a new section to `187-VALIDATION.md`, a sibling of the round-2 section and placed after it,
    headed as the round-3 gap-closure round with today's date. State the base commit for every gate in
    this section explicitly — it is the tip immediately before 187-20's first commit — and note that it
    differs from both `35261e96` (the phase base) and `ee5fff3b` (round 2's base), so no figure in the
    file is ambiguous about which range it measures.

    The section carries, at minimum:

    A per-plan table naming 187-20 against the findings it closes (CR-03, a Blocker; WR-09, a Warning
    that is the substantive half of failed truth #5) and 187-21 as this record. State the scope fence
    plainly: the round is scoped to CR-03 and WR-09 ONLY. WR-08 is recorded as an operator decision to
    roll it into Phase 188, NOT as a closure — say so by name, since it was one of round 2's four
    scoped items and a reader must not infer it was absorbed here. WR-01, WR-04, WR-05, WR-06, WR-07
    and IN-01…IN-05 remain carried forward, unchanged and undropped.

    A per-task verification row for each of 187-20's three tasks, keyed `187-20-T1`, `187-20-T2`,
    `187-20-T3`, each with its automated command and its measured result.

    A PROBE subsection recording all four falsification probes from 187-20 Task 3 — the mutation
    applied, the observed RED (failing case names, verbatim), and the clean revert evidence. Record
    them as the thing that makes this round different from the last one: round 2's own recorded lesson
    was "green over an unrepresentable fixture is not coverage", and its fix shipped protected by no
    fixture at all. State that a probe observed RED is the only evidence a guard bites.

    A COVERAGE subsection recording the testid sweep BEFORE (one id at zero — `seed-receipt-carried`)
    and AFTER (every static id non-zero), with the command that produced both tables.

    A GATES subsection re-measuring, with raw output at the round-3 tip:
    - the five-suite named set: file count, pass count, fail count, and the total against the
      re-measured 452 baseline, stating explicitly that the total ROSE and by how much;
    - `node scripts/vitest-count-gate.cjs` exit code, plus the statement that the pin file was not
      edited by this round;
    - `npx tsc -b` total error count before and after, and the count inside
      `src/components/workflows/` (expected 0). Note that `tsc -b` is not `tsc --noEmit`;
    - `npx vite build` exit code;
    - `git diff --stat 35261e96 HEAD -- supabase/migrations` — expected empty, the phase's
      zero-migration gate;
    - `git diff --numstat <round-3 base> HEAD -- frontend/src/pages/WorkflowBuilderPage.tsx` — expected
      empty, the D-187-14 mount cap, spent by none of this round;
    - `git diff --name-only <round-3 base> HEAD -- backend/` — expected empty;
    - `git diff --stat <round-3 base> HEAD -- frontend/src/components/workflows/StepTypePicker.tsx frontend/src/components/workflows/StepTypePicker.test.tsx`
      — expected empty, the WR-08 out-of-scope fence, measured rather than promised.

    Record honestly, in its own short paragraph, which of 187-20 Task 1's new assertions were GREEN on
    arrival rather than RED. Presence, count and zero-case coverage guard behaviour that was already
    correct; only the cause-honesty fence failed against the shipped sentence. Say that plainly instead
    of implying the whole block was a red-to-green cycle — and say that the probes, not a red, are what
    prove the always-green half bites.

    Do not add any figure without the command that produced it. Do not transcribe a number from memory
    or from `187-REVIEW.md`; every figure comes from `187-20-SUMMARY.md` or from a command run now.

    ⚠ Do NOT call `requirements.mark-complete`, `state.advance-plan` or
    `roadmap.update-plan-progress`. All three write false completion records in this project; the
    orchestrator owns those transitions.
  </action>
  <verify>
    <automated>grep -c "187-20-T1\|187-20-T2\|187-20-T3" .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "187-20-T1" ... 187-VALIDATION.md` ≥ 1, likewise `187-20-T2` and `187-20-T3`.
    - `grep -c "PROBE" ... 187-VALIDATION.md` ≥ 4 — all four probes recorded, none summarised away.
    - `grep -c "CR-03" ... 187-VALIDATION.md` ≥ 1 and `grep -c "WR-09" ... 187-VALIDATION.md` ≥ 1.
    - `grep -c "WR-08" ... 187-VALIDATION.md` ≥ 1 — the out-of-scope item is named, not silently dropped.
    - `grep -c "seed-receipt-carried" ... 187-VALIDATION.md` ≥ 2 — the before and after sweep both recorded.
    - The round-2 section is byte-unchanged apart from additions:
      `git diff -- .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` shows no
      deletion inside the round-2 section (deletions are confined to the M3/M9 notes, which are Task 2's).
    - Every numeric figure in the new section is adjacent to a command string. Verify by reading the
      section end to end and confirming no bare number stands alone.
    - `git status --porcelain frontend/ backend/ supabase/migrations` is empty — this plan touches no source.
    - `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` is empty.
    - `git diff --name-only` for the commit lists exactly one file, `187-VALIDATION.md`.
  </acceptance_criteria>
  <done>Round 3 is recorded with raw output for every task, every probe and every gate, and the round's scope fence is measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the stale M3 / M9 notes and add the escalated-only lived-experience row</name>
  <files>.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` — the
      "## Manual-Only Verifications" table (M1–M12) and the status paragraphs beneath it, including
      the two "Status at the close…" / "Status after the gap-closure round…" blocks that count the rows.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md` — the
      `human_verification` frontmatter block, where M3's and M12's extra notes were added by round 2's
      re-verification. M3's note is the one this task makes stale.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-20-PLAN.md` — the objective's WR-09
      design rationale, so the new row asks the operator to confirm the shape that actually shipped
      (a paragraph attributing no cause, with the cause named per row) rather than a shape from the review.
    - `Skill("sketch-findings-agentic-rag")` → `references/ai-seed-and-templates.md` — sketch 150-B's
      four load-bearing properties, so the new row's instructions are phrased against the approved
      surface rather than inventing acceptance criteria.
  </read_first>
  <action>
    Rewrite M3's note. It currently carries round 2's warning that the operator should read the carried
    sentence against the per-step reasons because an escalated draft shows an internally-contradictory
    card. After 187-20 that warning is stale. Replace it with what the operator should now see, and
    preserve the row's history rather than erasing it: keep the record that M3 was blocked on CR-01,
    unblocked by 187-16, and further repaired by 187-20 — a row whose history is deleted cannot be
    audited. M3 stays UNPERFORMED and UNTICKED; unblocking and repairing are not performing.

    Rewrite M9's note the same way. M9 asks whether the receipt's paragraphs read as ONE honest account.
    Its instruction list must gain the fact that changed: no paragraph now attributes a cause, and the
    cause is named per row — so the operator confirms the paragraph and the rows AGREE rather than
    checking for a contradiction. Keep all four of its existing numbered checks; add the new one rather
    than replacing any.

    Add a new manual row, M13, for the escalated-only draft specifically — the case WR-09 broke and
    which no shipped row tests head-on. Author it in the table's established four-column shape
    (Behavior / Requirement / Why Manual / Test Instructions). Requirement: Req 5 / VOCAB-02 (WR-09).
    Why manual: whether one card's paragraph and its own rows read as agreeing or contradicting, two
    lines apart, is a judgement only a person makes — the suite can prove which words are present and
    absent but not whether they add up. Test instructions must have the operator reach a draft whose
    ONLY sealed step is hand-escalated (no step reading documents), then read the card top to bottom
    and confirm: the paragraph states the count and the seal without saying what applied it; the row
    below names the author's own act; the two do not contradict; the "You can't turn that off" line is
    absent, because that is the detected lock and nothing here was detected. Mark it UNPERFORMED and
    UNTICKED, like every other row.

    Update the row-count statements beneath the table: the board goes from twelve rows to thirteen, all
    UNPERFORMED. Add a short paragraph for round 3 in the same voice as the round-2 one, stating that
    this round amended two notes and added one row, ticked nothing, softened nothing, re-scoped nothing
    and dropped nothing. Re-affirm by name that M5 and M8 are unchanged and still owed, and that M12's
    round-2 note about WR-08 STANDS — WR-08 was not closed by this round, so an operator running M12
    still needs that warning.

    ⚠ Do NOT call `requirements.mark-complete`, `state.advance-plan` or
    `roadmap.update-plan-progress`.
  </action>
  <verify>
    <automated>grep -c "^| \*\*M13\*\*\|^| M13" .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - A row keyed `M13` exists in the Manual-Only Verifications table with all four columns populated.
    - `grep -c "M13" ... 187-VALIDATION.md` ≥ 2 (the row plus the round-3 status paragraph).
    - The manual-row count statement reads THIRTEEN, and the file contains no surviving sentence
      asserting twelve rows as the current total. Verify with
      `grep -n "twelve\|thirteen\|M1–M12\|M1–M13" ... 187-VALIDATION.md` and confirm every remaining
      "twelve"/"M1–M12" occurrence is inside a historical round-2 paragraph describing that round, not a
      present-tense claim.
    - M3's note no longer tells the operator to expect an internally-contradictory card. Verify with
      `grep -n "contradict" ... 187-VALIDATION.md` and confirm no hit inside M3's or M9's instructions
      describes a contradiction the operator should still expect to SEE.
    - M3's note still names CR-01, 187-16 and now 187-20 — the history is preserved, not overwritten.
    - `grep -c "UNPERFORMED" ... 187-VALIDATION.md` did not decrease, and no `[x]` was added to any
      manual row. Verify no manual row is ticked:
      `grep -n "^| \*\*M[0-9]" ... 187-VALIDATION.md` shows no row containing a checked box.
    - M12's WR-08 note is still present: `grep -c "WR-08" ... 187-VALIDATION.md` ≥ 2 (Task 1's scope
      fence plus M12's surviving note).
    - M5 and M8 are still named as unchanged and owed.
    - `git status --porcelain frontend/ backend/ supabase/migrations` is empty.
    - `git diff --name-only` for the commit lists exactly one file, `187-VALIDATION.md`.
  </acceptance_criteria>
  <done>The two stale notes describe what ships, the escalated-only case has its own row, and the board is one row larger with nothing ticked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only. This plan changes no source file, ships no code path, installs no package and
touches no migration.

| Boundary | Description |
|----------|-------------|
| the validation record → the next reader's belief about what was proved | the only boundary in play. A validation file that overstates coverage is the same failure class as CR-02 and CR-03 — a green claim over something nothing measured. |
| a manual row's note → what the operator looks for | a stale note is worse than a missing one: it tells a person to expect a defect that no longer exists, which trains them to distrust a correct surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-21-01 | Repudiation | the round-3 gate record | mitigate | Every figure is adjacent to the command that produced it and is transcribed from `187-20-SUMMARY.md` or measured now — never from `187-REVIEW.md`, which is a round-2 document. |
| T-187-21-02 | Tampering | a silently dropped or ticked manual row | mitigate | Acceptance criteria re-count the board, assert the total moved 12 → 13, assert `UNPERFORMED` did not decrease, and re-confirm M5 / M8 / M12's notes survive by name. |
| T-187-21-03 | Spoofing | a false completion record | mitigate | The three GSD verbs known to write false records are named and forbidden in both task actions, and a clean `git status` over REQUIREMENTS.md / STATE.md / ROADMAP.md is an acceptance criterion. |
| T-187-21-04 | Tampering | an out-of-scope finding silently absorbed | mitigate | WR-08 must be NAMED in the record as an operator decision to roll into Phase 188, and M12's warning about it must survive — both are grep-level acceptance criteria. |
| T-187-21-05 | Information disclosure | raw command output pasted into a planning doc | accept | The gates are test totals, exit codes and `git diff --stat` output over a local repo. No secret, token, connection string or user datum appears in any of them. |
| T-187-21-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** It edits one markdown file. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `grep -c "187-20-T1\|187-20-T2\|187-20-T3" .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` ≥ 3.
- `grep -c "PROBE" .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` ≥ 4.
- `grep -c "M13" .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` ≥ 2.
- `git status --porcelain frontend/ backend/ supabase/migrations` is empty — this plan touches no source.
- `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` is empty.
- `git diff --name-only <round-3 base> HEAD -- .planning/phases/187-business-vocabulary-ai-seeded-canvas/` lists only `187-VALIDATION.md` and this round's PLAN/SUMMARY files.
</verification>

<success_criteria>
- Round 3's task rows, four probes, before/after coverage sweep and every gate are recorded with the command that produced each figure.
- M3 and M9 describe what the operator should now see; their history is preserved and neither is ticked.
- M13 covers the escalated-only draft head-on, UNPERFORMED.
- The board reads thirteen rows, none ticked, none dropped; WR-08's warning on M12 survives.
- No source file, migration or planning-state file is touched, and no false completion record is written.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-21-SUMMARY.md` when done.
</output>
