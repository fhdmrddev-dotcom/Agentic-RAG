---
phase: 103-workflows-page-authoring-api-nl-authoring
plan: 06
type: execute
wave: 4
depends_on: [103-03, 103-04, 103-05]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/pages/WorkflowsPage.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/pages/WorkflowsPage.test.tsx
autonomous: true
requirements: [WFAUTH-04]
must_haves:
  truths:
    - "A WorkflowsPage mounts on activeView==='workflows'"
    - "The Workflows nav entry appears in the mobile drawer from the shared NAV_ITEMS (distinct non-gear icon)"
    - "Selecting a project re-queries GET /workflows/published?project_folder_id=<uuid> and re-renders only that project's published workflows"
    - "The drafts shelf renders ABOVE Published with the dashed 'Build a workflow' build-card; no draft card exposes a Run affordance"
    - "Each card shows icon+name+vN, a client-derived deriveTier() badge, the phase-type chain, and 'entry needs <input_keys>'"
    - "Run on a published card creates a new thread, kicks off a server-side run (active_workflow_run_id set; GET /threads/{id}/workflow -> harness), and switches to Chat"
    - "Tweak forks a v(N+1) draft into the Builder (INSERT, never UPDATE the frozen published row)"
    - "On gauntlet PASS the view auto-returns to the Workflows page with the new version + a Run CTA"
    - "No react-router/useNavigate/URL"
  artifacts:
    - path: "frontend/src/pages/WorkflowsPage.tsx"
      provides: "project-filtered library + drafts shelf + Build-card + Run/Tweak + the Builder host"
      min_lines: 120
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "the 'workflows' render branch + the mobile drawer consuming the shared NAV_ITEMS"
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx doRun"
      to: "POST /threads then POST /threads/{id}/messages with workflow_definition_id"
      via: "createThread + sendMessage (no bespoke /workflows/{id}/run)"
      pattern: "createThread|sendMessage|workflowDefinitionId"
    - from: "frontend/src/pages/WorkflowsPage.tsx filter rail"
      to: "GET /workflows/published?project_folder_id="
      via: "listPublishedWorkflows re-query on project select"
      pattern: "project_folder_id"
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "WorkflowsPage"
      via: "activeView==='workflows' render branch before the trailing KnowledgeHealthPage else"
      pattern: "activeView === \"workflows\""
---

<objective>
Build the project-filtered Workflows page (REQ-7 / WFAUTH-04): the filter rail wired to `?project_folder_id=`, the drafts-above-published shelves with the dashed Build-card, per-card essence + the client-derived `deriveTier()` badge, the Tweak→`v(N+1)` fork, the Run→new-thread launch (reusing the existing kickoff path), and the auto-return-to-page-with-Run-CTA after a gauntlet PASS. Plus the `"workflows"` render branch in ChatLayout + the mobile drawer consuming the shared NAV_ITEMS. No router.

Implements REQ-7 (Workflows page + launch + nav render branch). The page HOSTS the Builder (Plan 04) and the gauntlet UI (Plan 05) as intra-`workflows`-view state (the three-homes contract: Builder/Workflows-page/Chat-thread, no router). Wave 3 — depends on Plan 03 (api fns/types/nav-items) + Plan 04 (the Builder it hosts via the Build-card / Tweak).

Purpose: the browse + launch home. Run = create-thread-then-switch-to-Chat (workflows are a mode of a thread, never page-resident). The drafts-can't-Run + Tweak-forks-v(N+1) + Run-kicks-off-a-real-server-run rules are the G-6 failure modes to avoid.
Output: `WorkflowsPage.tsx`, the ChatLayout edits, and `WorkflowsPage.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-SPEC.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-PATTERNS.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-RESEARCH.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-VALIDATION.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-03-SUMMARY.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-04-SUMMARY.md

<interfaces>
<!-- The sketch-findings-agentic-rag skill (sketch 021-A + 023-A) is the design acceptance bar — auto-loads. -->

From Plan 03 (api.ts): listPublishedWorkflows (extend to accept project_folder_id), listDraftWorkflows, generateWorkflow, createWorkflowDraft, publishWorkflow, PublishVerdict, WorkflowDraftRow.
From Plan 03 (deriveTier.ts): deriveTier, TIERS, ValidatorKind, CitationPolicy.
From Plan 04: WorkflowBuilderPage (the Builder the Build-card + Tweak open).
From Plan 05: PublishGauntlet (the page hosts it; onPublished -> auto-return to the page + Run CTA).

The kickoff launch contract (D-103-CONF-1 — reuse AS-IS, NO bespoke route):
- createThread (api.ts:49-60): POST /threads {title, folder_id?} -> Thread.
- sendMessage with workflowDefinitionId (api.ts:447-459): POST /threads/{id}/messages with body {content, model?, provider?, agent_mode, workflow_definition_id?}. The kickoff carries ONE free-text content -> inputs={"kickoff_prompt": content} server-side (threads.py:1155). NO structured input_keys on the wire.
- Launch sequence: POST /threads -> POST /threads/{id}/messages with workflow_definition_id (TWO calls). active_workflow_run_id is set server-side atomically. Proof of a real run: GET /threads/{id}/workflow -> mode="harness" (api.ts getThreadWorkflow).
- Project scope is BAKED INTO the published definition (definition.project_folder_id); the Run modal DISPLAYS the bound folder read-only, never picks one (D-103-1).

D-103-1 Run modal: a read-only folder chip (name, not path) + ONE textarea ("What should this run work on?") -> content -> kickoff_prompt. Declared inputs/input_keys appear only as a HINT line. The Run button stays ENABLED even on empty input.

D-103-4 drafts shelf: the caller's own drafts (listDraftWorkflows) + the dashed "Build a workflow" build-card. NO pre-seeded starters.

doRun() (the launch + redirect): createThread -> sendMessage(workflowDefinitionId) -> set selectedThread AND fire setViewingThread -> redirect toast -> switch activeView to "chat". (The page passes up the activeView switch + thread selection callbacks from ChatLayout — wire via props.)

ChatLayout render-branch region (frontend/src/components/layout/ChatLayout.tsx:255-267) — add the workflows branch BEFORE the trailing `: <KnowledgeHealthPage/>` else:
    {activeView === "documents" ? <IngestionPage/>
     : activeView === "skills" ? <SkillsPage .../>
     : activeView === "settings" ? <SettingsPage/>
     : activeView === "workflows" ? <WorkflowsPage .../>   // NEW
     : <KnowledgeHealthPage/>}
ChatLayout's NAV_ITEMS_MOBILE (:18-24) -> replace with `import { NAV_ITEMS } from "@/lib/nav-items"` (the shared const incl. the Workflows entry). This is the mobile drawer's source.

Tweak fork (Pitfall 6): Tweak = createWorkflowDraft with version = published_N + 1, same slug, status='draft' (INSERT). NEVER an UPDATE of the published row. Opens the new draft in the Builder.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WorkflowsPage — filter rail + drafts-above-published shelves + Build-card + per-card essence + deriveTier badge</name>
  <read_first>
    - frontend/src/lib/api.ts (listPublishedWorkflows, listDraftWorkflows, createWorkflowDraft, publishWorkflow + PublishVerdict/WorkflowDraftRow from Plan 03)
    - frontend/src/components/workflows/deriveTier.ts (deriveTier/TIERS from Plan 03)
    - frontend/src/pages/WorkflowBuilderPage.tsx (Plan 04 — the Builder the Build-card opens)
    - frontend/src/components/workflows/PublishGauntlet.tsx (Plan 05 — onPublished seam)
    - sketch-findings-agentic-rag SKILL.md (sketch 021-A: left project-folder filter rail = the live GET /workflows/published?project_folder_id query; Drafts shelf above Published; per-card essence D9 icon+name+vN + phase-chain glyphs (6 types, llm_emit ◆ "deliverable") + entry input_keys + tier badge D10; D12 a draft CANNOT be Run; D14 honest net-new violet flags on draft CRUD + the nav entry)
    - 103-SPEC.md REQ-7 acceptance (d)(e)(g)(h) + "How we'd know this failed"
    - frontend/src/hooks/useFolders.ts (the project-folder source — Phase 098 binding; the filter rail's options)
    - frontend/src/pages/WorkflowsPage.test.tsx (create in this task)
  </read_first>
  <behavior>
    - Mounts a project-folder filter rail (All projects / each project / Unbound) that issues GET /workflows/published?project_folder_id=<uuid> (live re-query; owner-scope preserved; filter only narrows).
    - A Drafts/seeds shelf ABOVE Published with the dashed "Build a workflow" build-card (opens the Builder); NO pre-seeded starters.
    - Per-card essence: icon+name+vN, a CLIENT-derived deriveTier() badge, the phase-type chain glyphs, "entry needs <input_keys>".
    - A draft card shows Open✎ + Publish… (NOT Run); only published cards show Run.
    - The Builder host: the Build-card / Tweak set intra-page state to show WorkflowBuilderPage; on publish PASS, auto-return to the page list + a Run CTA.
  </behavior>
  <action>
    Create `frontend/src/pages/WorkflowsPage.tsx`. Props (passed from ChatLayout): `{ folders: Folder[]; onLaunch: (def, kickoff: string) => Promise<void> }` (onLaunch is doRun — Task 2). Intra-page view state: `"library" | "builder"` (the three-homes contract — Builder is reached from the page, no router).

    1. Filter rail: render "All projects" + each project folder (from useFolders / props.folders) + an "Unbound" option. Selecting one sets `selectedProjectId` and re-queries `listPublishedWorkflows(selectedProjectId)` (Plan 03 must extend listPublishedWorkflows to accept an optional project_folder_id -> appends `?project_folder_id=`). "All projects" omits the param. The query is live (owner-scope server-side; the filter only narrows).

    2. Shelves: render the Drafts shelf ABOVE the Published shelf.
       - Drafts shelf: the dashed "Build a workflow" build-card (a button that sets the page view to "builder") FIRST, then `listDraftWorkflows()` cards. A draft card shows Open✎ (open in Builder) + Publish… (open the gauntlet) — NEVER a Run button (D-103-4 / D12: publish is the test).
       - Published shelf: the filtered published cards, each with a Run button (Task 2) + a Tweak button (Task 2).

    3. Per-card essence: icon + name + `vN` (version) + a deriveTier() badge COMPUTED on every render from `(citation_policy + the validator-kind set present in the def)` (NOT a stored label) + the phase-type chain glyphs (⚙/✎/🤖/⛓/☺/◆ — ◆=llm_emit "deliverable") + "entry needs <input_keys>" derived from the def inputs.
       (Note: the published-list rows are id/slug/name only; to derive the tier + phase chain the card needs the full definition — fetch it on demand via a get/expand, or extend the page to read the def. Keep deriveTier client-side: derive from the def's citation_policy + validator kinds, never a stored tier string.)

    4. Builder host: when the page view is "builder", render `<WorkflowBuilderPage .../>` (Plan 04) in place. The Build-card and Tweak both switch to "builder". The Builder's Publish opens `<PublishGauntlet onPublished={(version) => { setPageView("library"); refetchPublished(); setRunCtaFor(slug, version) }}/>` — on PASS, auto-return to the library + surface a Run CTA on the new version (REQ-7 / sketch 023-A: publish success auto-returns to the Workflows page with the new version + a Run CTA).

    5. Honest net-new violet flags (D-103-CONF / sketch D14): mark the draft-CRUD affordances + the Workflows nav entry as honest net-new (a small violet flag/badge) — discretionary presentation; only GET /workflows/published + POST /workflows/{id}/publish are the "live/green" surfaces.

    Also extend `listPublishedWorkflows` in `frontend/src/lib/api.ts` to accept `(projectFolderId?: string | null, signal?)` and append `?project_folder_id=<uuid>` when present (owner-scope stays server-side; the existing no-arg callers keep working — keep the param optional). [This is the one api.ts edit owned by THIS plan; it does not conflict with Plan 03's additive fns since this plan lands after Plan 03 — coordinate the merge by appending, not rewriting.]

    Create `frontend/src/pages/WorkflowsPage.test.tsx` (mock the api fns):
    - selecting a project calls listPublishedWorkflows with that project_folder_id and re-renders only that project's cards; "All projects" calls it with no param.
    - the drafts shelf renders ABOVE the published shelf (assert DOM order) with the dashed build-card.
    - NO draft card exposes a Run button; published cards DO (assert the Run affordance is absent on drafts, present on published).
    - the tier badge is client-derived: a card whose def has citation_policy 'draft' renders a different badge than one with 'strict' — with no extra fetch for the badge.
    - the build-card switches the page view to the Builder.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "project_folder_id" frontend/src/lib/api.ts` shows listPublishedWorkflows appends the query param; `grep -n "listPublishedWorkflows(" frontend/src/pages/WorkflowsPage.tsx` shows the re-query on project select.
    - `grep -n "deriveTier" frontend/src/pages/WorkflowsPage.tsx` confirms the card badge is client-derived (no stored tier string read).
    - WorkflowsPage.test.tsx asserts the drafts shelf is ABOVE published and that NO draft card has a Run affordance.
    - The build-card opens the Builder (intra-page state, no router).
    - `grep -rn "react-router\|useNavigate" frontend/src/pages/WorkflowsPage.tsx` returns NOTHING.
    - WorkflowsPage.test.tsx exits 0 GREEN.
  </acceptance_criteria>
  <done>WorkflowsPage renders the project filter rail (live ?project_folder_id= re-query), drafts-above-published shelves with the dashed Build-card, client-derived tier badges, and a draft cannot Run; the page hosts the Builder; test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run-from-page launch (doRun) + Tweak fork v(N+1) + the ChatLayout 'workflows' branch + mobile drawer shared NAV_ITEMS</name>
  <read_first>
    - frontend/src/lib/api.ts (createThread :49-60, sendMessage :447-459 with workflowDefinitionId, getThreadWorkflow :1126, createWorkflowDraft from Plan 03 — DO NOT modify the kickoff fns)
    - 103-PATTERNS.md (the doRun launch sequence excerpt + the Tweak-fork note Pitfall 6)
    - frontend/src/components/layout/ChatLayout.tsx (the render-branch region :255-267; NAV_ITEMS_MOBILE :18-24; how selectThread/setViewingThread/onNavigate flow — wire doRun's callbacks)
    - frontend/src/lib/nav-items.ts (the shared NAV_ITEMS from Plan 03)
    - sketch-findings-agentic-rag SKILL.md (sketch 023-A: Run = create thread + set workflow mode + switch to Chat; publish-is-the-test; Tweak forks a new-version draft into the Builder)
    - 103-SPEC.md REQ-7 acceptance (a)(b)(c)(f)(g)(i)(j)(k) + "How we'd know this failed" (the Run-leaves-you-on-page / no-server-run / bespoke-route / Tweak-UPDATE failure modes)
  </read_first>
  <behavior>
    - doRun(def, kickoff): createThread -> sendMessage(threadId, kickoff, {workflowDefinitionId: def.id}) -> set selectedThread + setViewingThread -> redirect toast -> switch activeView to "chat". Reuses the existing kickoff (NO bespoke /workflows/{id}/run). A real server-side run is kicked off (active_workflow_run_id set; GET /threads/{id}/workflow -> harness).
    - The Run modal: a read-only folder chip (the bound project folder name) + ONE textarea (-> content -> kickoff_prompt); declared input_keys as a HINT line; Run enabled even on empty.
    - Tweak: createWorkflowDraft with version = published_N + 1, same slug, status='draft' (INSERT) -> open the new draft in the Builder. NEVER UPDATE the frozen published row.
    - ChatLayout: the activeView==='workflows' render branch mounts WorkflowsPage; the mobile drawer consumes the shared NAV_ITEMS (Workflows entry present, distinct icon).
  </behavior>
  <action>
    1. In `frontend/src/pages/WorkflowsPage.tsx`, add the Run modal + `doRun`:
       - The Run button on a published card opens a modal: a read-only chip showing the bound project-folder NAME (from def.project_folder_id resolved to a folder name — never a path) + ONE `<textarea>` ("What should this run work on?"). If the def declares inputs/input_keys, render a HINT line ("This workflow expects: …"). The Run button is ENABLED even on empty input (D-103-1).
       - `doRun`: call `props.onLaunch(def, kickoffText)` (the ChatLayout-provided launcher). onLaunch (defined in ChatLayout, Task 2 step 3) runs the createThread -> sendMessage -> select+view+toast+switch-to-chat sequence.

    2. Add the Tweak button to published cards: `onTweak(def)` -> `createWorkflowDraft({ ...def, version: def.version + 1, status: "draft" })` (same slug, INSERT — never UPDATE the published row) -> on success set the page view to "builder" loaded with the new draft. (REQ-7 g / Pitfall 6: version = N+1; publishing the fork yields a 2nd published row for the same slug satisfying UNIQUE(slug,version) — already proven server-side in Plan 01 test_103_tweak_fork.py.)

    3. In `frontend/src/components/layout/ChatLayout.tsx`:
       - Replace the local `NAV_ITEMS_MOBILE` (:18-24) with `import { NAV_ITEMS } from "@/lib/nav-items"` and use it as the mobile drawer source (the shared const incl. the Workflows entry + distinct icon). Drop the now-unused icon imports that were ONLY for NAV_ITEMS_MOBILE.
       - Add the `activeView === "workflows"` render branch BEFORE the trailing `: <KnowledgeHealthPage/>` else: `: activeView === "workflows" ? (<WorkflowsPage folders={folders} onLaunch={doRun} />)`. Import WorkflowsPage.
       - Define `doRun(def, kickoff)` in ChatLayout (it has access to selectThread/setViewingThread/onNavigate/the thread state): `const thread = await createThread(def.name); await sendMessage(thread.id, kickoff, { workflowDefinitionId: def.id }); selectThread(thread); setViewingThread(thread.id); toast("Launching <name> — opening the run…"); onNavigate("chat")`. (Reuse the EXISTING createThread + sendMessage — NO bespoke route. The exact selectThread/setViewingThread fn names: match what ChatLayout already uses for thread selection + viewing.)

    Add a route-level / page-level Run case to `frontend/src/pages/WorkflowsPage.test.tsx`:
    - Run opens the modal (read-only folder chip + one textarea + a HINT line if input_keys); the Run button is enabled on empty input.
    - clicking Run calls onLaunch(def, kickoff) (assert it is called with the def + the kickoff text).
    - Tweak calls createWorkflowDraft with version = def.version + 1 and the SAME slug + status 'draft' (assert the INSERT args; never an update of the published row) and switches to the Builder.
    Add a ChatLayout-level assertion (a focused test or a tsc check): the workflows branch mounts WorkflowsPage; the mobile drawer source is the shared NAV_ITEMS (Workflows entry present). [If a full ChatLayout render test is heavy, assert via tsc + a grep acceptance criterion below.]
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowsPage.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "createThread|sendMessage" frontend/src/components/layout/ChatLayout.tsx` shows doRun uses the existing kickoff fns; `grep -rn "/workflows/.*/run" frontend/src` returns NOTHING (no bespoke run route).
    - `grep -n "workflowDefinitionId" frontend/src/components/layout/ChatLayout.tsx` confirms the run carries workflow_definition_id (the real server-side kickoff).
    - `grep -n "activeView === \"workflows\"" frontend/src/components/layout/ChatLayout.tsx` shows the render branch BEFORE the KnowledgeHealthPage else.
    - `grep -n "from \"@/lib/nav-items\"" frontend/src/components/layout/ChatLayout.tsx` confirms the mobile drawer consumes the shared NAV_ITEMS; the local NAV_ITEMS_MOBILE array is gone.
    - WorkflowsPage.test.tsx asserts Tweak calls createWorkflowDraft with `version: def.version + 1`, same slug, status 'draft' (INSERT, never UPDATE).
    - WorkflowsPage.test.tsx asserts the Run modal (folder chip name not path + one textarea + enabled-on-empty) and that Run calls onLaunch with the def + kickoff.
    - `git diff a131f05a -- backend/app/api/threads.py` is EMPTY (the launch reuses the existing route; no backend change).
    - `cd frontend && npx tsc -b` exits 0; WorkflowsPage.test.tsx exits 0 GREEN; `grep -rn "react-router\|useNavigate" frontend/src` empty.
  </acceptance_criteria>
  <done>Run creates a thread + kicks off a real server-side run via the existing kickoff (no bespoke route) + switches to Chat; Tweak forks a v(N+1) draft (INSERT) into the Builder; ChatLayout mounts WorkflowsPage on activeView==='workflows' and the mobile drawer uses the shared NAV_ITEMS; tsc clean; no react-router.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Run launch → POST /threads/{id}/messages | a user may only launch a workflow they own/can see |
| project filter → GET /workflows/published | the filter must only narrow, never widen, owner scope |
| Tweak → createWorkflowDraft | the fork must INSERT a new version, never UPDATE the frozen published row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-06-01 | Elevation of Privilege | a user launches a workflow they cannot see | mitigate | The Run list comes from GET /workflows/published (owner-scoped server-side); the kickoff route owner-checks the definition server-side (D-103-CONF-1); the client only passes a definition id it was served |
| T-103-06-02 | Information Disclosure | the project filter widens visibility by guessing a project_folder_id | mitigate | listPublishedWorkflows AND-appends the project filter to the owner-scope clause server-side — it can only NARROW (T-098-09); bound as a positional param (T-098-10); the client cannot widen |
| T-103-06-03 | Tampering | Tweak UPDATEs the frozen published row (immutability bypass) | mitigate | Tweak = createWorkflowDraft INSERT with version=N+1 (Pitfall 6); the published row is never UPDATEd; the DB trigger would 23514 a published UPDATE anyway; test-asserted (INSERT args) |
| T-103-06-04 | Tampering | Run switches the view but never kicks off a server-side run | mitigate | doRun calls sendMessage with workflow_definition_id -> the backend sets active_workflow_run_id atomically; the real-run proof is GET /threads/{id}/workflow -> harness (a view-only switch is a G-6 FAIL) |
| T-103-06-05 | Tampering | a bespoke /workflows/{id}/run route is introduced | mitigate | doRun reuses createThread + sendMessage (the existing kickoff); no bespoke route (grep-asserted); threads.py byte-identical |
| T-103-06-06 | Spoofing | a draft is run directly, bypassing the publish gauntlet (QUAL-01) | mitigate | A draft card exposes NO Run affordance (Run only on published cards); publish is the test (D12); test-asserted |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/WorkflowsPage.test.tsx` — GREEN.
- `cd frontend && npx tsc -b` — exits 0 (the workflows branch + WorkflowsPage import + the shared NAV_ITEMS drawer typecheck).
- `git diff a131f05a -- backend/app/api/threads.py backend/app/services/anthropic_service.py` — EMPTY (launch reuses the existing route).
- `grep -rn "react-router\|useNavigate\|/workflows/.*/run" frontend/src` — empty.
- Manual/lived-experience (VALIDATION.md G-4): Run lands in Chat (never leaves you on the page; the thread enters harness mode — GET /threads/{id}/workflow -> harness); Tweak forks a v(N+1) draft; publish PASS auto-returns to the page with a Run CTA.
- Parallel-thread SC#10 row (VALIDATION.md): Run on Thread A while Thread B streams — both keep correct workflow mode.
- Frontend rot caveat: net-new green via base-checkout (SEED-056).
</verification>

<success_criteria>
- REQ-7: WorkflowsPage mounts on activeView==='workflows'; the Workflows nav entry is in NavPanel (Plan 03) + the mobile drawer from the shared NAV_ITEMS; the filter rail re-queries ?project_folder_id=; drafts shelf above Published; no Run on drafts; Run creates a thread + a real server-side run + switches to Chat; Tweak forks v(N+1) (INSERT); publish PASS auto-returns + Run CTA; the composer picker still launches (D-103-B, untouched); no react-router; Deep byte-identical (threads.py untouched).
- tsc -b clean.
</success_criteria>

<output>
After completion, create `.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-06-SUMMARY.md`
</output>
