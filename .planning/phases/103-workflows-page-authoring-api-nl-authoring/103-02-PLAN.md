---
phase: 103-workflows-page-authoring-api-nl-authoring
plan: 02
type: execute
wave: 2
depends_on: [103-01]
files_modified:
  - backend/app/services/forced_emit.py
  - backend/app/services/workflow_authoring.py
  - backend/app/api/workflows.py
  - backend/tests/unit/test_103_nl_generate.py
  - backend/tests/unit/test_103_grounding_fidelity.py
  - backend/tests/unit/test_103_forced_emit_strict.py
autonomous: true
requirements: [WFAUTH-02]
must_haves:
  truths:
    - "A valid NL prompt yields a definition that model_validate() accepts, making EXACTLY ONE provider call (attempt=1, zero retries)"
    - "A forced first-pass ValidationError triggers EXACTLY TWO provider calls (attempt=1 then 2) and NO third, then an honest structured failure"
    - "A second-failure response never carries a runnable/partial draft — it is an explicit structured error"
    - "Grounding fidelity: every folder_scope/project_folder_id UUID resolves within the bound subtree; every available_tools/skill_ref is in the real registry"
    - "The authoring shot does NOT request strict mode for the optional-heavy WorkflowDefinition (no OpenAI/DeepSeek strict 400)"
    - "The authoring model resolves via resolve_authoring_model(settings) to a forceable default (claude-opus-4-8/gpt-5.5), not the composer's model"
  artifacts:
    - path: "backend/app/services/forced_emit.py"
      provides: "additive strict: bool | None = None override kwarg (default None = byte-identical cap-derived behavior)"
      contains: "strict"
    - path: "backend/app/services/workflow_authoring.py"
      provides: "resolve_authoring_model + grounding assembly + generate->validate->retry->fidelity orchestration"
      exports: ["resolve_authoring_model", "generate_workflow_definition"]
    - path: "backend/app/api/workflows.py"
      provides: "POST /workflows/generate route delegating to workflow_authoring"
  key_links:
    - from: "backend/app/services/workflow_authoring.py"
      to: "forced_emit(schema_model=WorkflowDefinition)"
      via: "the existing substrate (no new SDK path / no agent loop)"
      pattern: "forced_emit\\(.*schema_model"
    - from: "backend/app/services/workflow_authoring.py"
      to: "assert_folder_scopes_subset"
      via: "grounding-fidelity ⊆ check after model_validate"
      pattern: "assert_folder_scopes_subset"
    - from: "backend/app/api/workflows.py /generate"
      to: "workflow_authoring.generate_workflow_definition"
      via: "route delegation (orchestration out of the route body)"
      pattern: "generate_workflow_definition"
---

<objective>
Build the NL one-shot structured-generation path (REQ-2 / WFAUTH-02): the additive `strict` override on `forced_emit` (RESEARCH Pitfall 1 — the one genuine net-new integration risk, planned FIRST), the `workflow_authoring` service (grounding assembly + `resolve_authoring_model` + the generate→validate→retry-once→grounding-fidelity orchestration over `forced_emit(schema_model=WorkflowDefinition)`), and the `POST /workflows/generate` route that returns a draft definition object (NOT persisted) or an honest structured failure.

Implements REQ-2. The NL-gen reuses the EXISTING provider gateway substrate — NO new SDK path, NO agent loop, NO gateway change (RED LINE).

Purpose: This is the headline birth path (D-103-C — describe-first). It feeds the Builder (Plan 04). The exactly-once retry + grounding fidelity + honest-fail contracts are the G-6 silent-invalid-draft guards.
Output: an additive `strict` kwarg on `forced_emit`, the new `workflow_authoring.py` service, the `POST /workflows/generate` route, and 3 filled backend test files.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-SPEC.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-PATTERNS.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-RESEARCH.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-VALIDATION.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-01-SUMMARY.md

<interfaces>
<!-- Extracted from the live codebase + RESEARCH. Use directly. -->

backend/app/services/forced_emit.py — the substrate to REUSE (and minimally extend):
- forced_emit(*, messages, model, provider, emitter, tools, user_settings, system_prompt="", max_tokens=None, schema_model: type[BaseModel] | None = None) -> dict  (line 205; the CR-01 schema_model seam already validates the emit)
- It reads `cap = get_model_capability(model) or {}`; `forced = bool(cap.get("forced_emission", False))`; `strict = bool(cap.get("strict_json_schema", False))` (line ~238) and passes strict_schema=strict to the gateway.
- result["emitted"] = validated model | None; result["failure"] honest-fails (provider_error / model_failed_to_emit / truncated). Provider-error backstop at :312; narration recovery at :98.

backend/app/services/harness/validator_kinds.py — resolve_judge_model (line 58), the EXACT mirror for resolve_authoring_model:
    def resolve_judge_model(settings) -> str | None:
        model = getattr(settings, "harness_judge_model", None)
        if model: return model
        from app.config import get_model_capability  # function-local
        for candidate in ("claude-opus-4-8", "gpt-5.5"):
            if (get_model_capability(candidate) or {}).get("forced_emission"): return candidate
        return None

backend/app/services/harness/scope.py — assert_folder_scopes_subset(definition, *, supabase, user_id) -> None (line 92): resolves the project subtree owner-scoped; raises ValueError on any out-of-subtree folder id. _folder_scope_requires_project (harness.py:241) already forces a project_folder_id when any phase has folder_scope.

scripts/spike-097/authoring_feel.py — the THROWAWAY proof (REPRODUCE, do NOT import):
- _strip_discriminator(node) (line 241): recursively pop "discriminator" keys from the schema before passing it to the emit tool (Pitfall 7). Pydantic re-applies the discriminator at model_validate.
- WF_SCHEMA = _strip_discriminator(copy.deepcopy(WorkflowDefinition.model_json_schema()))
- the by-hand grounding assembly: fetch_visible_folders(supabase, user_id) + tool names from get_tools(None) + the skills query (user_id.eq OR is_global + is_enabled) + the by-hand single-retry loop (lines 296-341).

Grounding accessors (server-side, owner-scoped — REUSE):
- fetch_visible_folders (backend/app/utils/folder_utils.py) — owner-scoped folders.
- get_tools(None) (backend/app/services/openai_service.py:873) — the real tool registry; names via t["function"]["name"].
- resolve_template_source (template_asset_service.py:81) + parse_docx_template_variables (template_render_service.py:356) — OPTIONAL template grounding (D-103-CONF-2): template_asset_id? -> resolve -> parse; OR template_placeholders?: list[str] passed directly.

config: Settings.harness_authoring_model (added Plan 01). Both claude-opus-4-8 + gpt-5.5 are forced_emission:True (config.py).
RESEARCH A2: emit `nl_generation_attempt {attempt: int}` as a structured log / LangSmith span — NOT a harness_audit row (that CHECK is closed; would need a migration).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Pitfall 1 — plan FIRST): additive strict override on forced_emit (default-preserving)</name>
  <read_first>
    - backend/app/services/forced_emit.py (the WHOLE forced_emit signature + the `strict = bool(cap.get("strict_json_schema", False))` line ~238 + where strict_schema is passed to the gateway)
    - backend/app/services/provider_gateway/openai_compat.py (where strict_schema sets "strict": true on the function def — confirm the flow; RESEARCH cites openai_service.py:1392 / the openai_compat adapter)
    - backend/tests/unit/test_103_forced_emit_strict.py (create this file in this task) + the existing forced_emit tests (e.g. test_forced_emit*.py) to confirm the default-byte-identical posture
    - 103-RESEARCH.md Pitfall 1 + Open Q1/A1 + 103-PATTERNS.md Pitfall 1
  </read_first>
  <behavior>
    - forced_emit gains `strict: bool | None = None` as a keyword-only param.
    - strict=None (default) => the existing `cap.get("strict_json_schema")`-derived behavior, BYTE-IDENTICAL for every existing caller (emit + judge).
    - strict=False => the gateway is told NOT to set "strict": true on the function def (forcing-without-strict — the spike's proven OpenAI path).
    - strict=True => forces strict on (reserved; not used by 103).
  </behavior>
  <action>
    Edit `backend/app/services/forced_emit.py`:
    1. Add `strict: bool | None = None` to the `forced_emit` keyword-only signature (after `schema_model`).
    2. Change the strict resolution from `strict = bool(cap.get("strict_json_schema", False))` to:
       `strict = bool(cap.get("strict_json_schema", False)) if strict is None else bool(strict)`
       so `None` preserves the cap-derived value exactly (byte-identical), and an explicit `False`/`True` overrides it. Nothing else in the function changes — `strict` flows to the gateway as before.
    3. Add a one-line comment: `# Phase 103 (REQ-2 / Pitfall 1): strict override — None = cap-derived (emit/judge byte-identical); False = force-without-strict for the optional-heavy WorkflowDefinition authoring shot (avoids OpenAI/DeepSeek strict 400).`

    Create `backend/tests/unit/test_103_forced_emit_strict.py`:
    - test_strict_none_byte_identical: with a strict-capable model cap mocked, strict=None (default) yields the same strict value passed to the gateway as today (mock the gateway boundary / capture the GatewayRequest.strict_schema). Assert it equals the cap-derived value.
    - test_strict_false_overrides: strict=False on a strict-capable model => strict_schema False reaches the gateway (no "strict": true).
    - test_emit_caller_unchanged: a representative existing emit call site path is unaffected (default arg means existing callers don't pass strict).
    Mock at the gateway boundary (patch the gateway open_stream / capture the request), per feedback_mock_completeness.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_103_forced_emit_strict.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "strict: bool | None = None" backend/app/services/forced_emit.py` returns one hit in the `forced_emit` signature.
    - `grep -n "if strict is None else" backend/app/services/forced_emit.py` confirms the default-preserving resolution.
    - The existing forced_emit / emit / judge test suites still pass: `pytest tests/unit/test_forced_emit*.py tests/unit/test_llm_emit_executor.py -q` exits 0 (byte-identical default proven).
    - `grep -c "forced_emit(" backend/app/services/harness/phase_types.py` is UNCHANGED vs base (no new emit shots introduced by this edit).
    - test_103_forced_emit_strict.py exits 0 GREEN.
  </acceptance_criteria>
  <done>forced_emit has an additive strict override; default None is byte-identical for emit/judge (existing suites green); strict=False reaches the gateway; the strict test file is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): workflow_authoring service — resolve_authoring_model + grounding assembly + generate->validate->retry-once->fidelity</name>
  <read_first>
    - backend/app/services/harness/validator_kinds.py (resolve_judge_model :58 — mirror exactly)
    - backend/app/services/harness/scope.py (assert_folder_scopes_subset :92 — reuse for fidelity)
    - scripts/spike-097/authoring_feel.py (_strip_discriminator :241, the grounding assembly, the by-hand retry loop :296-341 — REPRODUCE the logic, do NOT import the spike)
    - backend/app/utils/folder_utils.py (fetch_visible_folders signature) + backend/app/services/openai_service.py:873 (get_tools)
    - backend/app/services/template_asset_service.py:81 (resolve_template_source) + template_render_service.py:356 (parse_docx_template_variables)
    - backend/app/models/harness.py (WorkflowDefinition.model_json_schema, _StrictBase, _folder_scope_requires_project, the PhaseConfig union for available_tools/skill_ref attrs)
    - backend/tests/unit/test_103_nl_generate.py + test_103_grounding_fidelity.py (stubs from Plan 01 Task 1)
    - 103-RESEARCH.md Patterns 1/2/3 + the resolve_authoring_model excerpt
  </read_first>
  <behavior>
    - resolve_authoring_model(settings) returns Settings.harness_authoring_model or a forceable default (claude-opus-4-8 then gpt-5.5).
    - generate_workflow_definition assembles grounding (folders + tool names + skills + optional template placeholders), forces emit_workflow_definition over the discriminator-stripped schema via forced_emit(schema_model=WorkflowDefinition, strict=False), validates, retries EXACTLY once on first-pass failure, then honest-fails; runs grounding fidelity after a successful validate.
    - Exactly ONE provider call on success; exactly TWO on a first-pass ValidationError; never a THIRD.
    - Each provider call emits one nl_generation_attempt {attempt: int} structured-log/span event.
    - Grounding fidelity: folder UUIDs ⊆ subtree (assert_folder_scopes_subset) + every available_tools/skill_ref ∈ the real registry (set membership); a violation is an honest failure, NOT a draft.
  </behavior>
  <action>
    Create `backend/app/services/workflow_authoring.py`:

    1. `def resolve_authoring_model(settings) -> str | None:` — mirror `resolve_judge_model` EXACTLY (getattr harness_authoring_model; else loop ("claude-opus-4-8","gpt-5.5") checking `get_model_capability(c).get("forced_emission")`; function-local import of get_model_capability).

    2. `def _strip_discriminator(node):` — reproduce the spike's recursive discriminator-pop (Pitfall 7). Build `WF_SCHEMA = _strip_discriminator(copy.deepcopy(WorkflowDefinition.model_json_schema()))` and wrap as the emit tool:
       `{"type": "function", "function": {"name": "emit_workflow_definition", "description": "Emit a single valid WorkflowDefinition for this task.", "parameters": WF_SCHEMA}}`

    3. `async def _assemble_grounding(*, supabase, user_id, project_folder_id, template_asset_id=None, template_placeholders=None) -> tuple[str, set[str], set[str]]:`
       - folders = await fetch_visible_folders(supabase, user_id)
       - tool_names = {t["function"]["name"] for t in get_tools(None)}
       - skills = owner+global enabled skills (reproduce the spike's query shape; RESEARCH A3 — if a shipped accessor exists, prefer it); skill_ids = {s["id"] for s in skills}
       - if template_asset_id: resolve via resolve_template_source -> parse_docx_template_variables -> placeholder names; elif template_placeholders: use directly
       - render the folder names+ids, tool names, skill ids, and placeholders into a grounded prompt string. Return (grounded_prompt, tool_names, skill_ids).

    4. `async def generate_workflow_definition(*, describe: str, supabase, user_id, settings, project_folder_id=None, template_asset_id=None, template_placeholders=None) -> dict:`
       - grounded_prompt, tool_names, skill_ids = await _assemble_grounding(...)
       - authoring_model = resolve_authoring_model(settings); resolve provider from the config inference patterns
       - base_messages = [{"role":"user","content": grounded_prompt + "\n\n" + describe}]
       - inner `async def _shot(messages, attempt: int)`: emit a structured log `nl_generation_attempt {attempt}` (logger.info + LangSmith metadata — NO harness_audit row, A2); call `forced_emit(messages=messages, model=authoring_model, provider=provider, emitter="emit_workflow_definition", tools=[emit_tool], user_settings=settings, system_prompt=AUTHORING_SYSTEM_PROMPT, schema_model=WorkflowDefinition, strict=False)`; return res.
       - res1 = await _shot(base_messages, 1); wd = res1["emitted"]
       - if wd is None: append the failure as an assistant+user error turn (the spike's retry message: "Your emission failed strict validation:\n{err}\nRe-emit a COMPLETE valid WorkflowDefinition using ONLY schema fields."); res2 = await _shot(retry_messages, 2); wd = res2["emitted"]; if still None -> return `{"ok": False, "error": "could_not_generate", "detail": <honest failure>}` (NEVER a partial/runnable draft).
       - grounding fidelity (only after a non-None wd): `await assert_folder_scopes_subset(wd, supabase=supabase, user_id=user_id)` (wrap its ValueError -> `{"ok": False, "error": "grounding_failed", "detail": ...}`); then for every phase, every `getattr(phase.config, "available_tools", []) or []` must be in tool_names else grounding_failed; `getattr(phase.config, "skill_ref", None)` if set must be in skill_ids else grounding_failed.
       - return `{"ok": True, "definition": wd.model_dump(mode="json")}` (NOT persisted).

    Add `AUTHORING_SYSTEM_PROMPT` (a concise prompt instructing the model to emit a valid WorkflowDefinition using only the provided folder ids / tool names / skill ids / placeholders — Claude's discretion on copy).

    Fill `test_103_nl_generate.py` (mock `forced_emit`): test_one_call (valid emit -> call_count==1, one attempt=1 log, zero retry), test_retry_once (first emit None, second valid -> call_count==2, attempts [1,2], NO third), test_honest_fail (both None -> ok=False/could_not_generate, no definition), test_no_strict (assert forced_emit is called with strict=False). Fill `test_103_grounding_fidelity.py`: a generated def with an out-of-subtree folder UUID -> grounding_failed (mock assert_folder_scopes_subset to raise); a hallucinated available_tools -> grounding_failed; a clean def -> ok=True. Mock the grounding accessors + forced_emit at their boundaries.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_103_nl_generate.py tests/unit/test_103_grounding_fidelity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "def resolve_authoring_model|async def generate_workflow_definition|def _strip_discriminator" backend/app/services/workflow_authoring.py` returns all three.
    - `grep -n "forced_emit(" backend/app/services/workflow_authoring.py` shows the call passing `schema_model=WorkflowDefinition` AND `strict=False`.
    - `grep -n "assert_folder_scopes_subset" backend/app/services/workflow_authoring.py` confirms the fidelity check is invoked after validate.
    - `grep -rn "agent_loop\|AgentRunner\|open_stream" backend/app/services/workflow_authoring.py` returns NOTHING (no new SDK path / no agent loop — RED LINE).
    - `grep -n "from scripts" backend/app/services/workflow_authoring.py` returns NOTHING (the spike is reproduced, not imported).
    - test_103_nl_generate.py asserts call_count==1 on valid and ==2 (no 3rd) on first-pass invalid, plus strict=False passed.
    - test_103_grounding_fidelity.py asserts an out-of-subtree UUID and a hallucinated tool each yield grounding_failed (not a draft).
    - Both files exit 0 GREEN.
  </acceptance_criteria>
  <done>workflow_authoring service reuses forced_emit(schema_model=WorkflowDefinition, strict=False); exactly-once retry with attempt logging; grounding fidelity rejects out-of-subtree/hallucinated; no agent loop/SDK path; tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): POST /workflows/generate route (delegates to workflow_authoring; returns draft or honest failure)</name>
  <read_first>
    - backend/app/api/workflows.py (the router + get_published_workflows boilerplate + the publish_workflow delegation-to-service pattern :120-126; the Plan-01 draft routes already added)
    - backend/app/services/workflow_authoring.py (generate_workflow_definition signature from Task 2)
    - backend/app/dependencies.py (get_current_user, get_pg_pool; how supabase + settings are obtained in other routes — match the existing access pattern)
    - backend/tests/unit/test_103_nl_generate.py (route-level cases)
  </read_first>
  <behavior>
    - POST /workflows/generate with {describe, project_folder_id?, template_asset_id?, template_placeholders?} returns {ok: true, definition} on success, or a structured {ok: false, error, detail} (the UI surfaces "could not generate") — NEVER auto-persists.
  </behavior>
  <action>
    Add to `backend/app/api/workflows.py`:
    - `class GenerateRequest(BaseModel): describe: str; project_folder_id: UUID | None = None; template_asset_id: UUID | None = None; template_placeholders: list[str] | None = None`
    - `@router.post("/generate")` `async def generate_workflow(body: GenerateRequest, current_user=Depends(get_current_user))`:
      get pool/supabase/settings the way the existing routes do; delegate to `workflow_authoring.generate_workflow_definition(describe=body.describe, supabase=..., user_id=user_id, settings=..., project_folder_id=body.project_folder_id, template_asset_id=body.template_asset_id, template_placeholders=body.template_placeholders)`. Import `workflow_authoring` as a MODULE (patchability, the publish_service precedent at line 25). Return the service result dict directly (FastAPI serializes the {ok, definition} / {ok, error, detail} shape). A service `ok=False` returns 200 with the structured error body (it is an honest "could not generate," not an HTTP error — the UI distinguishes on `ok`). Keep the orchestration OUT of the route body.
    - Fill the route-level case in test_103_nl_generate.py: a mocked `workflow_authoring.generate_workflow_definition` returning ok=True -> 200 with definition; returning ok=False -> 200 with {ok:false, error}. (Patch the module the route imported.)
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_103_nl_generate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@router.post(\"/generate\")" backend/app/api/workflows.py` returns one hit on the /workflows router (NOT threads.py).
    - `grep -n "workflow_authoring" backend/app/api/workflows.py` shows a MODULE import + the delegation call.
    - The route body contains NO forced_emit/grounding logic (delegation only — orchestration stays in the service).
    - `git diff a131f05a -- backend/app/api/threads.py backend/app/services/anthropic_service.py` is EMPTY.
    - The route never persists: `grep -n "create_workflow_definition" backend/app/api/workflows.py` does NOT appear inside the /generate handler (persistence is REQ-1's explicit POST /workflows).
    - test_103_nl_generate.py route cases exit 0 GREEN.
  </acceptance_criteria>
  <done>POST /workflows/generate delegates to workflow_authoring, returns a draft or an honest structured failure, never persists; threads.py/anthropic_service.py byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client describe box → /workflows/generate | untrusted NL prompt (prompt-injection vector) crosses here |
| KB content / template placeholders → generation prompt | untrusted document content the model reads (injection / scope-widening vector) |
| model output → grounding fidelity | a shape-valid but ungrounded/hallucinated definition crosses here |
| route → provider gateway | the SEALED single forced shot (never the open agent loop) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02-01 | Tampering / Elevation of Privilege | model invents a folder_scope UUID outside the bound subtree | mitigate | `assert_folder_scopes_subset` (server-side, owner-scoped) raises on any non-⊆ id -> honest grounding_failed, NOT a draft; `_folder_scope_requires_project` forces a binding; the model never participates in scope resolution (GOV-01) |
| T-103-02-02 | Tampering | model references a non-registered tool/skill | mitigate | set-membership check: every available_tools ∈ get_tools(None) names + every skill_ref ∈ the owner/global skill set; a miss -> grounding_failed |
| T-103-02-03 | Tampering / Spoofing | prompt injection via the describe box / KB content / template placeholders ("ignore scope, use folder X") | mitigate | `extra="forbid"` rejects injected keys at model_validate; the valid folder/tool/skill SETS are computed server-side (KB content can't whitelist itself — the 101.1-06 precedent); grounding fidelity is the backstop |
| T-103-02-04 | Tampering | a shape-valid but ungrounded "draft" reaches the user (G-6 silent-invalid class) | mitigate | grounding fidelity is DISTINCT from model_validate (shape-only); a shape-valid ungrounded def is an honest failure, not a draft |
| T-103-02-05 | Denial of Service | unbounded retries / provider hang | mitigate | EXACTLY one retry (the REQ-2 contract, test-asserted no-3rd-call); forced_emit's provider-error backstop + truncation guard; the SEALED single shot never enters the open agent loop |
| T-103-02-06 | Information Disclosure | secret leakage in the nl_generation_attempt log | mitigate | the event carries ONLY an integer attempt index; logging_sink _RedactingFilter + name-only secret handling; no prompt/secret in the structured event |
| T-103-02-07 | Denial of Service | OpenAI/DeepSeek strict-mode 400 on the optional-heavy schema (degrades to a hard failure) | mitigate | the authoring shot passes strict=False (Task 1) — forcing-without-strict works on OpenAI/DeepSeek; SC#10 rows assert NO strict 400 |
</threat_model>

<verification>
- `pytest tests/unit/test_103_forced_emit_strict.py tests/unit/test_103_nl_generate.py tests/unit/test_103_grounding_fidelity.py -q` — all GREEN.
- `pytest tests/unit/test_forced_emit*.py tests/unit/test_llm_emit_executor.py -q` — GREEN (forced_emit default byte-identical).
- `git diff a131f05a -- backend/app/api/threads.py backend/app/services/anthropic_service.py` — EMPTY.
- `grep -rn "agent_loop\|open_stream" backend/app/services/workflow_authoring.py` — empty (no new SDK path).
- SEED-056 base-checkout net-new-failure proof on forced_emit.py + workflow_authoring.py + api/workflows.py.
- SC#10 cross-provider NL-gen rows live in VALIDATION.md (NOT here) — exercised at /gsd:verify-work.
</verification>

<success_criteria>
- REQ-2: valid prompt -> model_validate-clean def, exactly ONE provider call; first-pass ValidationError -> exactly TWO calls (no 3rd) then honest structured failure (never a runnable/partial draft).
- REQ-2 grounding fidelity: out-of-subtree UUID and hallucinated tool/skill each -> grounding_failed, not a draft.
- Pitfall 1: the authoring shot passes strict=False (no OpenAI/DeepSeek strict 400); forced_emit default behavior byte-identical for emit/judge.
- NL-gen reuses forced_emit/the gateway — no new SDK path, no agent loop; threads.py/anthropic_service.py byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-02-SUMMARY.md`
</output>
