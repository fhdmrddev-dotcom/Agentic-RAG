---
phase: 232-the-source-contract-google-drive
plan: 03
type: execute
wave: 3
depends_on:
  - 232-01
  - 232-02
files_modified:
  - backend/app/services/sources/import_service.py
  - backend/app/api/connectors.py
  - backend/app/services/connectors/service_tools.py
  - backend/app/services/google/sheets.py
  - backend/app/security/egress.py
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - backend/tests/unit/services/sources/test_import_service.py
autonomous: true
requirements:
  - SRC-01
user_setup: []

must_haves:
  truths:
    - "backend/app/services/sources/import_service.py encapsulates single-file import and browse routines using SourceRegistry and async_mint_document_row"
    - "backend/app/api/connectors.py delegates list_connection_files and import_connection_file to import_service.py"
    - "backend/app/services/connectors/service_tools.py calls the source contract instead of reaching into private cloud_storage functions"
    - "backend/app/services/cloud_storage.py is deleted with zero surviving references in the repository"
    - "Single-file import through POST /connections/{connection_id}/files/{file_id}/import works identically and mints via async_mint_document_row"
    - "HOT-FILE-LEDGER.md and CLAUDE.md record the partial discharge of connectors.py in the same commit"
  artifacts:
    - backend/app/services/sources/import_service.py
    - backend/tests/unit/services/sources/test_import_service.py
  key_links:
    - "import_service.py bridges the legacy single-file import endpoint to the new SourceAdapter architecture"
    - "connectors.py partial discharge reduces hot file complexity while maintaining full backward compatibility"
---

<objective>
Retire `cloud_storage.py` and achieve partial discharge of hot file `connectors.py` (SRC-01, G-5, D-232-06):
1. Create `backend/app/services/sources/import_service.py` providing:
   - `browse_connection_files(connection, folder_id, query, page_token, page_size)`
   - `import_single_file(connection, file_id, user_id, active_org, background_tasks, supabase)`:
     - Downloads file bytes via `SourceRegistry.get_adapter(connection).read_file(...)`.
     - Calls `async_mint_document_row` with `source_connection_id` and `ingest_visibility` stamped from `connection`.
     - Dispatches background `splice_document` if not a duplicate.
2. Refactor `backend/app/api/connectors.py`:
   - Re-route `list_connection_files` and `import_connection_file` to delegate to `import_service`.
3. Refactor `backend/app/services/connectors/service_tools.py:1689, 1722`:
   - Replace direct calls to `_list_google_drive_files` and `_fetch_google_drive_file` with calls to `SourceRegistry`.
4. Update references in `backend/app/services/google/sheets.py` and `backend/app/security/egress.py`.
5. Delete `backend/app/services/cloud_storage.py`.
6. Update `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md` reflecting the partial discharge of `connectors.py`.
7. Author unit tests in `test_import_service.py` asserting backward compatibility of file import.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/232-the-source-contract-google-drive/232-CONTEXT.md
@backend/app/api/connectors.py
@backend/app/services/cloud_storage.py
@backend/app/services/connectors/service_tools.py
@docs/HOT-FILE-LEDGER.md
@CLAUDE.md
</execution_context>

<threat_model>
<trust_boundaries>
API / Client -> connectors.py -> import_service.py -> async_mint_document_row -> PostgreSQL
</trust_boundaries>
<threats>
- threat_id: TM-232-05
  category: Elevation of Privilege / Visibility Tampering
  component: import_single_file
  disposition: mitigated
  mitigation_plan: Always read ingest_visibility and default posture from the verified server-side connection record, never from client input.
- threat_id: TM-232-06
  category: Tenant Isolation Breach
  component: import_single_file
  disposition: mitigated
  mitigation_plan: Explicitly pass org_id resolved from active_org to async_mint_document_row, resolving BUG-260905-01.
</threats>
</threat_model>

<tasks>
<task>
<task_id>232-03-01</task_id>
<description>Create backend/app/services/sources/import_service.py</description>
<action>
Author `import_service.py`:
- Implement `browse_connection_files(connection, query=None, folder_id=None, page_token=None, page_size=30)`:
  - Resolves adapter via `SourceRegistry.get_adapter(connection)`.
  - If adapter exists, invokes `adapter.list_files` or `adapter.browse`.
  - Normalizes return shape to match existing `list_cloud_files` API response `{ "files": [...], "next_page_token": ... }` for full backward compatibility.
- Implement `import_single_file(connection, file_id, user_id, org_id, background_tasks, supabase)`:
  - Downloads via `adapter.read_file(connection, file_id)`.
  - Calls `async_mint_document_row(raw=..., filename=..., mime_type=..., user_id=..., org_id=..., source_connection_id=connection.id, ingest_visibility=connection.default_ingest_visibility)`.
  - Enqueues background `splice_document` if not duplicate.
  - Returns document dict.
</action>
<verify>
python -c "from app.services.sources.import_service import browse_connection_files, import_single_file; print('import_service ready')"
</verify>
</task>

<task>
<task_id>232-03-02</task_id>
<description>Re-point connectors.py and service_tools.py; delete cloud_storage.py</description>
<action>
1. In `backend/app/api/connectors.py`:
   - Update `list_connection_files`: Replace `from app.services.cloud_storage import list_cloud_files` with `from app.services.sources.import_service import browse_connection_files`.
   - Update `import_connection_file`: Replace `fetch_cloud_file` with call to `import_service.import_single_file`.
2. In `backend/app/services/connectors/service_tools.py`:
   - Replace `from app.services.cloud_storage import _list_google_drive_files, _fetch_google_drive_file` with calls to `SourceRegistry.get_adapter("google")`.
3. In `backend/app/services/google/sheets.py` and `backend/app/security/egress.py`:
   - Update docstring references mentioning `cloud_storage.py` to reference `app.services.sources`.
4. Delete `backend/app/services/cloud_storage.py`.
5. Run grep to confirm zero surviving references to `cloud_storage`.
</action>
<verify>
git grep "cloud_storage" backend/app/ || echo "Zero surviving references to cloud_storage"
</verify>
</task>

<task>
<task_id>232-03-03</task_id>
<description>Update hot-file ledger and CLAUDE.md for connectors.py partial discharge</description>
<action>
Update `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md`:
- Update `backend/app/api/connectors.py` entry:
  - Increment commits, set phases, update line count.
  - Record disposition: "Phase 232 PARTIAL DISCHARGE: cloud storage browsing and single-file import extracted to app/services/sources/import_service.py".
</action>
<verify>
node scripts/check-claude-md-size.cjs
</verify>
</task>

<task>
<task_id>232-03-04</task_id>
<description>Author unit tests for import_service in test_import_service.py</description>
<action>
Author `backend/tests/unit/services/sources/test_import_service.py`:
- Test `browse_connection_files` with MockSourceAdapter.
- Test `import_single_file` ensuring `async_mint_document_row` is called with connection's `source_connection_id` and `ingest_visibility`.
- Test error handling when adapter is missing or file cannot be downloaded.
</action>
<verify>
pytest backend/tests/unit/services/sources/test_import_service.py -v
</verify>
</task>
</tasks>
