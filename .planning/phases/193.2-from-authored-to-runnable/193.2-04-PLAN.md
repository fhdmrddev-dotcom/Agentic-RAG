---
phase: 193.2-from-authored-to-runnable
plan: 04
type: execute
wave: 3
depends_on: ["193.2-01", "193.2-02"]
files_modified:
  - frontend/src/components/workflows/library/libraryFilter.test.ts
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-17 — server `ORDER BY` is the sole ordering authority; NO client sort is added anywhere, and the shipped T-7/P-4 fence in `rowIdentity.test.ts` stays green and unweakened"
    - "D-18 — the default order is fixed and no sort control is built; a recency ⇄ A–Z toggle belongs to the deferred library sketch, and building it now would be building it twice"
    - "SC#3 is about the RENDERED position, not the SQL: the rendered order is `[all starters] ++ [all published] ++ [all drafts]`, so a recency-first published feed lands the newest row at index `starters.length` — measured as position 4 of 109, and that residual is STATED rather than smoothed"
    - "F-7 is extended: a source sweep proves no new `.sort(` was introduced on the merged list in `libraryFilter.ts` or `WorkflowsPage.tsx`, driven RED against a real plant"
  artifacts:
    - path: "frontend/src/components/workflows/library/libraryFilter.test.ts"
      provides: "The rendered-position arithmetic for SC#3 and the extended no-client-sort sweep"
      contains: "starters.length"
  key_links:
    - from: "frontend/src/components/workflows/library/libraryFilter.test.ts"
      to: "frontend/src/components/workflows/library/libraryFilter.ts"
      via: "mergeLibrary insertion order + filterLibrary order preservation, asserted as pure-function arithmetic"
      pattern: "mergeLibrary"
---

<objective>
SC#3's client half — and the honest part of it. The server change in plan 193.2-03 orders the
published feed by recency; **that is not the same as the newest row appearing at the top of the
rendered library.** `mergeLibrary` concatenates starters first, so the measured after-state is
rendered position **4 of 109**, not 1. This plan pins that arithmetic as a property of two pure
functions, so the claim the phase makes about findability is a measurement rather than a hope.

It also extends F-7. D-17 makes server `ORDER BY` the sole authority, and the shipped T-7/P-4 fence
enforces it inside `rowIdentity.ts` only — nothing today would catch a `.sort(` introduced in
`libraryFilter.ts` or `WorkflowsPage.tsx`.

Purpose: *"the correctness of an identity line is arithmetic, and arithmetic proved through a DOM is
proved expensively and incompletely"* (`rowIdentity.test.ts:4-8`). Both `mergeLibrary` and
`filterLibrary` are pure; prove it there.

Output: new cases in one test file. **No source file is modified by this plan.**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap FIRST:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then assert the
dispatched base SHA against `193.2-BASELINE.md`.
**Every vitest invocation carries `GSD_VITEST_MAX_WORKERS=2`.**

**Runs in parallel with 193.2-03, 193.2-05, 193.2-06** — disjoint files, no database mutation.
</execution_context>

<context>
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the rendered-position arithmetic for SC#3</name>
  <files>frontend/src/components/workflows/library/libraryFilter.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/library/libraryFilter.ts:137-149` — `mergeLibrary`, a `Map` keyed by `id` in INSERTION ORDER (starters, then published, then drafts) with the comment at `:143-144` making insertion order a **dedupe-precedence** decision (*"the later write wins: a row the caller owns should read as theirs rather than as a shared starter"*)
    - `frontend/src/components/workflows/library/libraryFilter.ts:190-197, 225, 257-265` — `matchesQuery` for a blank query, `matchesProject` for `null`, and `filterLibrary`'s `rows.filter(...)` with the `chips.length === 0` short-circuit; **order preserved, no sort**
    - `frontend/src/components/workflows/library/libraryFilter.test.ts:148-181` — the shipped `describe("D-16 — the merge")` block the new cases join
    - `frontend/src/components/workflows/library/libraryFilter.test.ts:460-485` — the source-fence + paired positive-control template
    - `frontend/src/pages/WorkflowsPage.tsx:249-252, 420-428, 446-449, 458-461, 1045-1055` — the defaults (blank query, no chips, All projects), the `Promise.allSettled` mount fetch, the `rows`/`visibleRows` memos, and the *"ONE FLAT LIST (D-02) — No shelf, no section header"* comment
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md` §R2 in full — the pipeline diagram, the measured arithmetic table (28 published / 78 drafts / 3 starters / 109 total; QBR 17 of 109 today → **4 of 109** after), and the three honest readings with option 1 recommended
  </read_first>
  <action>
Add a new `describe` to `frontend/src/components/workflows/library/libraryFilter.test.ts` — join or
sit beside the shipped `describe("D-16 — the merge")` block; do not restructure it. Name it for the
decision, e.g. `describe("193.2 / SC#3 — where a freshly-published row actually lands")`.

Cases to write, all as pure-function arithmetic over hand-built fixtures (no DOM):

1. **The concatenation order is `starters ++ published ++ drafts`.** Build three distinguishable
   feeds and assert `mergeLibrary(published, starters, drafts).map(r => r.id)` equals the expected
   concatenation, and that each block's internal order is the order it arrived in — i.e.
   `mergeLibrary` **preserves feed order and adds none of its own**.

2. **A recency-first published feed lands its newest row at index `starters.length`.** Feed a
   `published` array already ordered `updated_at DESC` (this is what plan 193.2-03's SQL now
   returns — the test simulates the wire, it does not reach the database). Assert
   `merged[starters.length].id` is the newest published row's id. **Assert the index expression
   `starters.length`, not a hard-coded `3`** — a hard-coded number rots the moment the curated shelf
   grows, which is exactly how `GSD_VITEST_MAX_WORKERS=4` rotted.

3. **`filterLibrary` with the page's defaults preserves that index.** Blank `query`, empty `chips`,
   `projectId: null` — assert `filterLibrary(merged, defaults)` is order-identical to `merged` and
   that the newest published row is still at index `starters.length`. This is the case that makes
   the claim about the RENDERED list rather than about the merge.

4. **The residual, pinned as a property rather than left in prose.** Assert that the newest published
   row is **NOT** at index 0 when `starters.length > 0`, and write the reason into the case's own
   docstring: SC#3 says *"at the top of the list, or both"*; **position 4 is not position 1, and no
   SQL change can make it 1** — the starters block is concatenated client-side, and moving it would
   change `mergeLibrary`'s dedupe precedence (the `:143-144` comment) and would need its own
   decision. ⚠ **State the residual; do not smooth it.** Name the two other readings RESEARCH offers
   and record that reading 1 (accept position 4) is the one taken, and that OQ-5 puts the judgement
   in front of the operator at the D-21 UAT run.

⚠ **Do NOT change `libraryFilter.ts`, `WorkflowsPage.tsx`, or `mergeLibrary`'s insertion order.**
Changing the insertion order to `published, drafts, starters` would put the newest row at index 0,
and it is explicitly **not recommended**: it silently inverts dedupe provenance for any future
collision. `files_modified` for this plan contains exactly one file, and it is a test.

⚠ **D-18 — no sort control is added.** A recency ⇄ A–Z toggle on `LibraryToolbar` is deferred to the
library sketch (G-2 fires there, `SEED-155` binds). Building a control the pending sketch may
relocate or redesign is building it twice. Nothing in this plan touches `LibraryToolbar`.

Commit `test(193.2-04): the rendered-position arithmetic for SC#3, residual stated (D-17/D-18)`.
  </action>
  <acceptance_criteria>
    - The new describe contains a case asserting `mergeLibrary`'s output ids equal `[...starterIds, ...publishedIds, ...draftIds]` for distinguishable feeds
    - A case asserts `merged[starters.length].id === newestPublished.id` using the **expression** `starters.length`, and `grep` shows no hard-coded `[3]` index in the new cases
    - A case asserts `filterLibrary(merged, {query: "", chips: [], projectId: null})` is order-identical to `merged` (assert on the mapped id arrays, not on length alone)
    - A case asserts the newest published row is NOT at index 0 while `starters.length > 0`, and its docstring names the residual (`position 4 is not position 1`), the reason (client-side concatenation), and OQ-5
    - `git diff --numstat` shows `libraryFilter.ts`, `WorkflowsPage.tsx` and `LibraryToolbar.tsx` UNCHANGED
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library/libraryFilter.test.ts` → 0 failed
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library/libraryFilter.test.ts</automated>
  </verify>
  <done>The rendered position of a freshly-published row is a pinned arithmetic property, and the honest residual — index `starters.length`, not 0 — is written into the suite rather than into a summary nobody re-reads.</done>
</task>

<task type="auto">
  <name>Task 2: Extend F-7 — no client sort was added anywhere, driven RED against a real plant</name>
  <files>frontend/src/components/workflows/library/libraryFilter.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/library/rowIdentity.test.ts:89-99` (the comment stripper and the 187-24 trap it exists for), `:108-115` (the stripper's own non-vacuity guard), `:167-179` (the T-7/P-4 fence with its INLINE plant at `:169-172`) — **this is the canonical four-part template: inline plant · non-vacuity · absence · stated scope**
    - `frontend/src/components/workflows/library/libraryFilter.test.ts:460-485` — the same repo's paired-case form (fence, then a sibling `it()` running the same needle over a planted string)
    - `frontend/src/components/workflows/library/libraryFilter.ts` in full — the `.sort(` call sites, if any, and whether any of them touches a merged list
    - `frontend/src/pages/WorkflowsPage.tsx:440-470` — the `rows` / `visibleRows` memos
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md` §7 — the already-recorded F-7 RED observation against `rowIdentity.ts`
  </read_first>
  <action>
Add a source fence to `frontend/src/components/workflows/library/libraryFilter.test.ts` extending F-7
beyond `rowIdentity.ts`, using Vite's `?raw` loader to read both
`@/components/workflows/library/libraryFilter?raw` and `@/pages/WorkflowsPage?raw`.

Reproduce **all four** parts of the template — three is not enough:
  - **Scope:** strip comments first
    (`source.replace(/\/\*[\s\S]*?\*\//g, "").replace(/^\s*\/\/.*$/gm, "")`) and pair the stripper
    with its own non-vacuity guard: assert the stripped `code` still contains a known exported
    identifier from each module and that a known prose token (a `D-` id, say) is present in `source`
    and absent from `code`. The 187-24 trap — a module that DOCUMENTS why a symbol is absent reds a
    raw grep for it — has fired six-plus recorded times.
  - **Non-vacuity:** collect the `.sort(` lines from each stripped source into an array and assert
    the collection step is meaningful (if a module has zero `.sort(` lines, assert that explicitly
    and say so in the case name, so the fence cannot pass over an empty corpus by accident).
  - **Absence:** no line in either stripped source matches `/\brows\.sort\(|\bvisibleRows\.sort\(|\bmerged\.sort\(|\bfamily\.sort\(|\blist\.sort\(/`.
  - **The INLINE plant, kept permanently in the file:** assert a literal forbidden string
    (e.g. `"const shown = rows.sort((a, b) => a.name.localeCompare(b.name))"`) DOES match the needle,
    exactly as `rowIdentity.test.ts:169-172` does. This is the RED observation preserved in the file
    rather than performed once and described in a summary.

Then **drive it RED for real, once, and record the observation**: plant a `rows.sort(` line into
`frontend/src/components/workflows/library/libraryFilter.ts`, run the suite, observe the new fence
fail, restore with `git checkout --`, re-run green, and confirm `git diff --numstat` is empty for
that file. Paste both outputs into the plan summary. ⚠ An inline needle-match assertion proves the
REGEX sees the shape; only a real plant proves the FENCE is pointed at the right source. 193.1 found
four fences that could not fire and all four were caught by planting.

⚠ **Do not weaken, edit or duplicate the shipped `rowIdentity.test.ts:167` fence.** It stays green
and unedited; this task adds coverage next to it, in a different module's suite.

Commit `test(193.2-04): F-7 extended to libraryFilter and WorkflowsPage, driven RED against a plant (D-17)`.
  </action>
  <acceptance_criteria>
    - The new fence imports both modules via `?raw`, strips comments, and carries a stripper non-vacuity guard asserting a known export survives and a known prose token does not
    - The fence carries an INLINE plant assertion (a literal forbidden string asserted `toMatch` the needle), permanently in the file
    - The fence asserts absence of the forbidden shapes across both stripped sources
    - The plan summary records the real-plant RED run: the planted line, the failing case name, the failure output, the post-restore GREEN run, and an empty `git diff --numstat` for `libraryFilter.ts`
    - `git diff --numstat` shows `frontend/src/components/workflows/library/rowIdentity.test.ts` UNCHANGED — the shipped fence was not weakened or duplicated
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library/libraryFilter.test.ts src/components/workflows/library/rowIdentity.test.ts` → 0 failed
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` prints `count gate OK`; if the pinned total moved, the pin update lands in this same commit
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library/libraryFilter.test.ts src/components/workflows/library/rowIdentity.test.ts</automated>
  </verify>
  <done>F-7 now watches the two modules that could plausibly grow a client sort, with all four parts of the canonical template, and it has been observed RED against a real plant in production source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan modifies exactly one test file. No runtime code, no network, no database, no schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.2-02 | Information disclosure | the library row pipeline | accept | No predicate, endpoint or feed is touched. The ordering authority stays server-side (D-17); this plan's whole purpose is to prove no client-side ordering was introduced, which is the *defence* of that boundary rather than a change to it |
| T-193.2-04-A | Tampering | the temporary `rows.sort(` plant in `libraryFilter.ts` | mitigate | Restored with `git checkout --`; acceptance criteria require an empty `git diff --numstat` for the file and a post-restore GREEN run |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | No package installed. The `?raw` loader is Vite's, already used by `PhaseSpineGraph.test.tsx`, `WorkflowDoorSwitch.test.tsx` and `WorkflowBuilderPage.header.test.tsx` |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library/libraryFilter.test.ts src/components/workflows/library/rowIdentity.test.ts` → 0 failed
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `count gate OK`
- `git diff --numstat HEAD~2 -- frontend/src/components/workflows/library/libraryFilter.ts frontend/src/pages/WorkflowsPage.tsx frontend/src/components/workflows/library/rowIdentity.test.ts` → EMPTY
</verification>

<success_criteria>
- The rendered position of a freshly-published row is pinned at index `starters.length` by an expression, not a literal
- The residual (position 4, not 1) is stated in the suite with its reason and OQ-5
- F-7 is extended to two more modules with all four template parts and a recorded real-plant RED
- No source file is modified by this plan
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-04-SUMMARY.md` when done.
</output>
