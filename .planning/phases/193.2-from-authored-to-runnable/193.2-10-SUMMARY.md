---
phase: 193.2-from-authored-to-runnable
plan: 10
subsystem: close-out — guardrail ledger, UAT contract, project state
tags: [close-out, hot-file-ledger, G-5, D-03, D-21, UAT, STATE, ROADMAP, owed-rows, checkpoint]
base_sha: bc52d4d7
base_sha_short: bc52d4d7
requires:
  - "193.2-01 — the baselines every gate figure below is judged against"
  - "193.2-02 — the D-10/D-19 rulings and the C-7 jsonb accessor warning"
  - "193.2-03 / -04 — the ORDER BY change and the rendered-position arithmetic"
  - "193.2-05 / -06 / -07 / -09 — the prompt, the refusal, the provenance field and the visible mark"
  - "193.2-08 — 193.2-FREQUENCY.md, the only artifact permitted to state whether any of it works"
provides:
  - "FIVE CLAUDE.md hot-file ledger rows, not the four planned — three files G-5 could never see, plus two more"
  - "the two disputed phase counts RESOLVED BY MEASUREMENT, losers recorded beside winners"
  - "193.2-UAT.md — U1-U5, the D-20 roster statement, all eight rows, every row recorded OWED"
  - "the reported-bug and seed close-out records, each with a concrete re_open_trigger"
  - "STATE.md and ROADMAP.md, hand-edited, with no criterion ticked that a person has driven"
  - "every gate re-run at HEAD on a quiet main tree"
affects:
  - "Phase 194 and every later phase — five hot files are now visible to G-5 for the first time"
  - "197 / AUTH-02 — inherits D-22's three-instance pattern and D-24 as a measurement"
  - "the operator — D-21's run is the blocking checkpoint this plan ends on"
tech-stack:
  added: []
  patterns:
    - "corrections recorded BESIDE the prior value, never over it"
    - "quick-task buckets counted OUT of the phase count, and the raw recipe output published beside"
    - "a measurement about a file NOBODY edited belongs in the ledger for the same reason the absences do"
    - "STATE.md hand-edited; the YAML re-parsed after the edit to prove integrity"
key-files:
  created:
    - .planning/phases/193.2-from-authored-to-runnable/193.2-UAT.md
  modified:
    - CLAUDE.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/reported-bugs/template-first-drafts-cannot-be-published.md
    - .planning/reported-bugs/cannot-find-a-just-published-workflow.md
    - .planning/seeds/SEED-163-authoring-does-not-propose-the-business-requirement.md
decisions:
  - "D-03 honoured and EXCEEDED — five ledger rows, because two more absent hot files were found"
  - "D-01 honoured — STATE.md records NO guardrail override for Phase 193.2; the absence is a measurement"
  - "D-04 honoured — WorkflowCard.tsx was touched by no plan; the row records that as a measurement"
  - "Both blocking bugs stay `folded`, not `closed` — D-08 forbids an absence claim and U1 is owed"
  - "The phase is deliberately NOT marked complete and no SC is ticked that a person has driven"
metrics:
  duration: ~2h
  tasks_completed: 2 of 3 (task 3 is the blocking operator checkpoint)
  commits: 2
  source_files_modified: 0
  completed: 2026-08-15
---

# Phase 193.2 Plan 10: Close-Out — Five Ledger Rows, the UAT Contract, and the Honest Record Summary

**One-liner:** Five hot files that could not be seen by their own guardrail are now on the
`CLAUDE.md` ledger with re-derivable figures — three more than the table knew about and one more than
this phase's own research found — two disputed phase counts were resolved by measurement rather than
by choosing, every gate was re-run at HEAD with no new failure, and the phase is closed **without
ticking a single success criterion a person has not driven**.

## Base commit — the wrong-base assertion did NOT fire (the second non-fire this phase)

| Property | Value |
|---|---|
| Dispatched base | `bc52d4d7` |
| `git rev-parse HEAD` at start | **`bc52d4d7`** — correct |
| Branch | `develop` — **the MAIN WORKING TREE, no worktree** |
| Why no worktree | This plan writes `STATE.md` and `ROADMAP.md`, reads the operator's local Supabase, and ends on an operator checkpoint |

⚠ Recorded because **a NON-fire is as much a data point as a fire.** Across 193.1 / 193.2 the
assertion fired **12 for 12 on worktree dispatches** (22+ project-wide) and **0 for 2 on main-tree
serial dispatches** — `193.2-08` and this plan. The correlation is with the worktree, not with the
orchestrator.

## Task Commits

| Task | Name | Commit | Files |
|---|---|---|---|
| 1 | Five hot-file ledger rows, the stale figures corrected beside | `413aa54a` | `CLAUDE.md` |
| 2 | UAT rows, bug + seed frontmatter, STATE and ROADMAP, gates re-run | `0c87c154` | 6 files under `.planning/` |
| 3 | **D-21 — the operator-driven end-to-end run** | ⏸ **BLOCKING CHECKPOINT** | — |

**Zero source files modified.** `git diff --diff-filter=D` is EMPTY on both commits.

---

## Task 1 — the ledger. ⚠ FIVE rows were owed, not the four the plan anticipated

**Every figure was RE-DERIVED at HEAD.** Not one was copied from CONTEXT, RESEARCH, BASELINE or a
sibling SUMMARY — this phase's own commits moved several of them, and this table has now documented
itself going stale five separate times.

### The commands, run once per file, with their measured outputs

```
git log --oneline -- <file> | wc -l
wc -l <file>
git show c1a6c122:<file> | wc -l                       # the phase base, for the "before" column
git log --format=%s -- <file> \
  | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | sort -u
```

| File | commits | phases | lines (base → HEAD) | On the table before? |
|---|---|---|---|---|
| `backend/app/db/workflows.py` | **31** | **17** | 1296 → **1405** | ❌ **ABSENT** |
| `backend/app/services/harness/publish_service.py` | **17** | **7** | 1047 → **1158** | ❌ **ABSENT** |
| `backend/app/services/workflow_authoring.py` | **11** | **6** | 389 → **540** | ❌ **ABSENT** |
| `backend/app/models/harness.py` | **17** | **16** | 578 → **611** | ❌ **ABSENT** |
| `frontend/src/components/workflows/builderStore.ts` | **11** | **5** | 785 → **837** | ❌ **ABSENT** |
| `frontend/src/pages/WorkflowsPage.tsx` | **34** | **12** | 1176 → **1176** | ✅ stale 4th time |
| `frontend/src/pages/WorkflowBuilderPage.tsx` | **41** | **12** | 2252 → **2348** | ✅ corrected |
| `backend/app/api/workflows.py` | **34** | **17** | 1951 → **1951** | ✅ **unchanged** |
| `.../library/WorkflowCard.tsx` | **8** | **3** | 818 → **818** | ✅ **unchanged** |

**Each of the five new rows carries the analog's seven components in order** (touch list with
quick-task buckets counted out · the three figures · why the row exists and who added it · the
re-derive commands with outputs · the G-5 verdict **with its measured reason** · the binding
invariants · the named next seam ending with the inherited triple), and **each of the five carries
the invisibility sentence** — `grep -c "permanently invisible to its own guardrail" CLAUDE.md` went
from **3 to 7**.

### ⚠ D-03 anticipated THREE; there were FIVE, and the fifth is the sharpest evidence for the rule

- `models/harness.py` was found by **RESEARCH**, and is **not in `193.2-CONTEXT.md` D-01's own
  six-file table** — the phase's discuss step enumerated six hot files and missed a seventh.
- `builderStore.ts` appears in **NO prior artifact of this phase at all** — not CONTEXT, not RESEARCH,
  not `193.2-BASELINE.md` §5's seven triples. **`193.2-09` found it while re-deriving figures for a
  file that WAS on the table.**

**That is the rule demonstrating itself: a guardrail cannot see what is absent from its list, and the
only reliable way anything gets onto the list is somebody deriving hotness with `git log` while doing
something else.**

### ⚠ The two disputed phase counts — RESOLVED BY MEASUREMENT, not by choosing

The close-out brief and `193.2-09-SUMMARY.md` disagreed on two figures. **The recipe was re-run at
HEAD and the disputed buckets inspected commit by commit.**

| File | Plan 09 read | The brief's recipe | **Measured verdict** | The extra bucket(s) |
|---|---|---|---|---|
| `builderStore.ts` | **5 phases** | 6 | **5 — plan 09 is right** | `260809`, from `feat(260809-klo)` — a QUICK TASK |
| `WorkflowBuilderPage.tsx` | **12 phases** | 14 | **12 — plan 09 is right** | `260809` + `260814` — both quick tasks |

**A third was found in passing:** `publish_service.py`'s recipe returns **8** buckets, of which
`quick` (from `fix(quick-260809-klo)`) is a quick task ⇒ **7 phases**. The close-out brief's 8 is what
the command prints.

⚠ **In all three the raw recipe over-counts, exactly as the `WorkflowBuilderPage.tsx` ledger cell has
documented about itself since 2026-08-14.** The recipe is stated in each new row, the losing figures
are recorded **beside** the winners, and every row that has zero quick-task buckets says so — so a
future reader can tell "counted out" from "there were none".

### The four existing cells, corrected beside their previous values

- **`WorkflowsPage.tsx` — the FOURTH consecutive staling**, and the row says so. `33 / 12 / 1160` →
  **`34 / 12 / 1176`**. ⚠ **The interesting half: this phase never touched the file** (only its
  `.test.tsx`), and `git log --oneline c1a6c122 -- <file> | wc -l` already read **34** at the phase's
  base. **The staleness is INHERITED — the figure was already wrong when Phase 192.1 wrote it.** One
  invariant was nevertheless ADDED without editing the file: it is now inside the D-17 no-client-sort
  fence, both arms driven RED against real plants.
- **`WorkflowBuilderPage.tsx`** — `40 / 11 / 2252` → **`41 / 12 / 2348`**, with plan 09's D-02
  argument (one gated sibling inside an affordance the file already owns, **0 deletions**) and the
  measured-false byte-pin claim recorded rather than repeated.
- **`api/workflows.py`** — re-derived **UNCHANGED at `34 / 17 / 1951`**, *and the non-move is written
  down*. Both `193.2-CONTEXT.md` D-02 and the ROADMAP's own flags predicted this file would be
  edited; **D-12 made one string feed both surfaces, so not one line landed here.** A file predicted
  to be edited and measurably not edited is worth recording, because the next reader would otherwise
  assume the prediction held.
- **`WorkflowCard.tsx`** — re-derived **UNCHANGED at `8 / 3 / 818`**, with D-04's scoping constraint
  stated: it appears in **no plan's `files_modified`** and `git diff --numstat c1a6c122 HEAD` on it is
  EMPTY. *A measurement about a file nobody edited belongs in the table for the same reason the
  absences do* — a reader must be able to tell "out of scope by decision" from "nobody checked".

### The self-staling audit — which rows are safe

`CLAUDE.md` is **not one of the counted files**, so a row is only at risk if a later task in this plan
edits a counted file. **Task 2 and Task 3 touch only `.planning/` files.** ⇒ **All nine rows are
safe**, and none of the five new triples can have gone stale on its own commit. (This is the trap the
`WorkflowCard.tsx` cell records about itself: `721` was already stale when written, because the
commit that WROTE the row edited the file it counted.)

### The count-gate constant, corrected beside the stale `3604`

`CLAUDE.md`'s `GSD_VITEST_MAX_WORKERS` paragraph quoted **3604**. Measured at close: **3918**. The
full arc is published because *the rate is the point*: **3604** (2026-08-14) → **3892** (`193.2-01`,
`+288` in one day) → 3895 → 3899 → 3907 → 3911 → **3918**. ⚠ **A constant that rots silently is the
exact failure that rule already documents about itself** — it is the second time this figure has
rotted, and the second rot took one day.

---

## Task 2 — the UAT contract, the frontmatter, STATE, ROADMAP, and every gate at HEAD

### `193.2-UAT.md` — five rows, all OWED, and the reason is structural

| Row | OQ | What it must establish | Verdict |
|---|---|---|---|
| **U1** | D-21 | the end-to-end run, **8+ planted facts**, five moments recorded separately | ⏸ OWED |
| **U2** | OQ-1 | the rendered refusal + `publish-trigger` disabled + `publish_blocked` stays 0 | ⏸ OWED |
| **U3** | OQ-2 | the post-publish Run CTA appears, and how long after the click | ⏸ OWED |
| **U4** | OQ-5 | the rendered position — prediction **4 of 109** | ⏸ OWED |
| **U5** | OQ-6 | the composer's Harness picker under recency | ⏸ OWED |

**Every row needs a browser and a running backend on the operator's machine.** Scoring them from an
agent that has read the source would be the check-that-cannot-fail the file exists to prevent.

**The D-20 roster statement carries all eight rows** — 2 driven (as 20 real paid generations by
`193.2-08`), 6 recorded ⛔ N/A with reasons. ⚠ **All eight provider keys ARE configured, so the six
are blocked by the RESOLVER, not by a missing key** — the stronger and more honest reason. The
measured k/N board is **CITED from `193.2-FREQUENCY.md`, never re-derived**, and the D-08 sentence is
stated verbatim **before any figure appears**.

**One thing was driven here, and it is labelled a BASELINE rather than a verdict:** the
`publish_blocked` re-check against the operator's local Supabase — **0 today, 1 `publish_attempted`,
1 `publish_succeeded`, 32 all-time, newest 2026-08-07**. It uses `count(*)` on `event_type` (a real
column), and the file carries the **C-7 warning** for anyone extending it: `harness_audit.metadata`
is a double-encoded jsonb string, so `->>'blocked_stage'` is NULL on all 32 rows *by definition*.

### The reported bugs and the seed — `folded`, not `closed`, with concrete triggers

| Artifact | Status | Why not `closed` |
|---|---|---|
| `BUG-260815-01` | **folded** | The fix is prompt-level and **non-deterministic**: `llm_human_input` **0/20** against a pre-fix 2/2-with-a-template. **A REDUCTION, NEVER AN ABSENCE (D-08)** — one more generation can produce one, which is why the publish gate stays. Closes only when U1 moment 2 records a real publish without a canvas edit |
| `BUG-260815-02` | **folded** | The **ordering half is closed and proved**; the **findability judgement is not**. Position **4 of 109 is not position 1**, and no `ORDER BY` can make it 1. That is U4's call |
| `SEED-163` | **folded** | Same non-determinism. Its close-out also records the finding that **the mark means "a model wrote this", never "this is durable"** |

Each carries a `re_open_trigger` naming concrete observable conditions, and each close-out block
records what shipped, what was measured, and what was deliberately **not** built.

### STATE.md — HAND-EDITED, and the integrity re-proved after the edit

⚠ **No `gsd-sdk state.*` verb was called by this plan or by any plan in this phase.** Seven of them
write false records; one deleted ~9 KB of locked decisions inside a 193.1 worktree **while reporting
`"updated": true`**.

**Verified after editing** by re-parsing the YAML with `yaml.safe_load`: **8 keys intact**
(`gsd_state_version`, `milestone`, `milestone_name`, `status`, `last_updated`, `last_activity`,
`stopped_at`, `resume_file`), `stopped_at` **5687 chars**, `resume_file` repointed to the UAT file.

It records: the executed position and the next action; every gate beside its baseline; the measured
k/N board with the D-08 rule; D-10's untriggered escape hatch; the five inert-fence findings; the two
jsonb traps; the five ledger rows and the two resolved disputes; the wrong-base tally; **and ten
deferrals each with a concrete re-open trigger** (D-24 → 197, D-25 → `SEED-164`, D-18 and the
card-density question → the library sketch where G-2 fires, the `publish_service.py:196-197`
evaporated deferral → `/gsd:fast`, the `libraryFilter.test.ts` pin at 48 vs 60, the 196-char refusal
worst case, the **undriven attribution arm**, `DEF-193.2-03-01`'s three stale citations, and plan
09's un-UAT'd mark).

⚠ **`grep -c "NONE for Phase 193.2" .planning/STATE.md` → 2.** A G-5 override was **offered and
declined** for the **third consecutive phase**; **that absence is a measurement, not an omission.**
G-1 does not fire — ⚠ **a third `193.x` would trip it**, recorded for whoever comes next. G-2 did not
fire on anything in scope.

### ROADMAP.md

Ten plans ticked with **per-plan outcome lines**; a **per-criterion** SC table rather than an averaged
verdict; the gate figures; and the phase entry left **unchecked**. ⚠ **`**Plans**: TBD` is gone from
the 193.2 section** (the 12 remaining hits belong to phases 194-198). **No success criterion is
ticked that U1 has not driven.**

### Every gate re-run at HEAD, on a quiet main tree

| Gate | `193.2-BASELINE.md` | At HEAD | Verdict |
|---|---|---|---|
| backend `tests/unit` (full) | 62 failed / 2092 passed / 2 xfailed / 2 xpassed | **62 failed / 2154 passed / 2 xfailed / 2 xpassed** | ✅ **failures IDENTICAL** — the SEED-056 rot set |
| six backend workflow suites | 81 / 0 failed | **110 / 0 failed** | ✅ |
| `test_workflow_authoring_requirement.py` + the 187 analog | — | **41 / 0 failed** | ✅ |
| the k/N harness without the opt-in | — | **5 passed · 10 skipped**, zero network | ✅ |
| five frontend workflow suites | 337 / 0 failed | **359 / 0 failed** | ✅ |
| **count gate** (`GSD_VITEST_MAX_WORKERS=2`) | OK · 3892 · failed 0 · 3868 pinned · 75/75 | **`count gate OK` · total 3918 · failed 0 · pinned 3868 · 75/75** | ✅ **two runs, both identical** |
| `tsc -p tsconfig.app.json --noEmit` | 33 | **33** | ✅ unmoved across all ten plans |

**NO NEW FAILURE APPEARED.** ⚠ The bare full frontend run was **not** used as a gate —
`193.2-01` measured that tree at **49 then 46** failures on one identical commit, so it cannot pass
or fail anything.

---

## Deviations from Plan

### 1. [Rule 2 — missing critical coverage] FIVE ledger rows were written, not four

The plan names four files. **Two more absent hot files existed**, and one of them
(`builderStore.ts`) is in no artifact of this phase. Writing four would have left a G-5-firing file
invisible **on the very plan whose stated purpose is to end that invisibility** — the plan's own
objective says *"a hot file missing from the table is permanently invisible to its own guardrail."*

### 2. [Finding] the plan's phase counts were disputed and three were over-counted

See the table above. The plan's brief asserted 6 / 14 where the measured phase counts are 5 / 12, and
a third over-count (`publish_service.py`, 8 vs 7) was found in passing. **All three are the same
trap: the standard `sed` recipe returns quick-task buckets.** The plan itself warned about this in
its `<action>` and the brief's own figures then walked into it — which is why the row for every new
file states the recipe and its output rather than only the conclusion.

### 3. [Deviation, honest] `193.2-UAT.md` records the OQ observations as OWED, not as driven

The plan's artifact spec says the file provides *"the OQ-1/OQ-2/OQ-5/OQ-6 **driven** observations"*.
**None of them can be driven from this agent** — all four need Chrome MCP against a live backend, and
this executor has no browser tooling. They are recorded **OWED with their exact methods and their
FAIL conditions**, which is what the file's own rule requires. The one exception is U2's DB half,
recorded explicitly as a **pre-run baseline and not a verdict**.

### 4. [Structural] the plan's `<verify>` blocks were run against the main tree

Both `<automated>` blocks say `cd "C:/Vibe Apps/Agentic RAG" && …`. This plan **runs on the main
tree**, so unlike plans 02/03/04 (which reported the same absolute-path hazard from inside
worktrees) the paths were correct here. Recorded for symmetry with those three reports.

### 5. [Rule 3 — blocking, resolved] a commit message with backticks was expanded by the shell

The Task-2 commit message contained backtick-quoted identifiers, which Git Bash expanded as command
substitution — the commit failed with `folded: command not found` and a series of `pathspec … did not
match` errors. **No file was staged, unstaged or altered by the failure.** Re-issued with
`git commit -F <file>` from the scratchpad. Recorded because the same hazard will bite any future
plan writing a prose-heavy commit message inline.

---

## Known Stubs

**None.** This plan produces documentation and project state. No source file was modified, no
placeholder, no TODO, no component wired to mock data.

## Threat Flags

**None.** The register's dispositions are discharged on measurement:

- **`T-193.2-10-A` (information disclosure — UAT evidence)** — mitigated **and it was load-bearing.**
  `193.2-02` measured that raw `publish_blocked` payloads contain `definition_id` / `golden_run_id`
  UUIDs and **multi-kilobyte judge prose quoting knowledge-base document names**. `193.2-UAT.md`
  records **only** counts, dates and `event_type` values — **no KB content, no provider key, no
  produced-document body, no user id and no email.** The file states that prohibition in its own U1
  section so the operator does not paste any of it in when the row is driven.
- **`T-193.2-10-B` (repudiation — a row marked pass from another row's evidence)** — mitigated
  structurally: U1's **five moments are separate rows with separate verdicts**, all five rows are
  ⏸ OWED, and the tally reads **`0 driven · 0 pass · 0 fail · 5 OWED`**. *A scoreboard that lists only
  what passed is not a scoreboard.*
- **`T-193.2-10-C` (closing a bug that still reproduces)** — mitigated: **neither blocking report was
  closed.** `BUG-260815-01`'s frontmatter states the claim as a **frequency reduction with the k/N
  cited**, and D-08 forbids the absence claim outright.
- **`T-193.2-02` (the RLS-bypassing feed predicates)** — accept, unchanged. The F-2 predicate
  byte-identity cases are green at HEAD inside the six-suite run.
- **`T-193.2-SC` (installs)** — accept. **No package was installed by this plan or by this phase.** No
  migration was added (`supabase/migrations/` reads 112 throughout).

## Deferred Issues

All ten deferrals are recorded in `.planning/STATE.md` with concrete re-open triggers rather than
here, so they are visible to the next phase rather than to a reader of this file. The two most likely
to be forgotten:

1. **`publish_service.py:196-197` still says *"the deferred Phase-103 rework"*.** ⚠ It is literally the
   evaporated-deferral problem `SEED-164` exists to correct, surviving in a comment. `193.2-06`
   honoured an explicit UNCHANGED constraint on `:190-208` over the docstring rule and **flagged
   rather than hid it**. One `/gsd:fast` line.
2. **Attribution between the suppression clause and the shortened bullet is UNRESOLVED and UNDRIVEN.**
   The phase claims a **combined effect and attributes nothing** — stated plainly in
   `193.2-FREQUENCY.md` §4 and in STATE.md rather than implied.

---

## Task 3 — ⏸ BLOCKING CHECKPOINT (D-21)

**Not performed. It cannot be.** SC#4 is one real operator-driven end-to-end run, and it is the only
row no test in this repository can stand in for — 193.1 measured that a 3,600-case suite could not see
the assumption that mattered, and a `render_template` pin does not prove a template gets FILLED.

The checkpoint's content, the exact steps, and the explicit FAIL conditions are returned to the
orchestrator in the structured checkpoint message and are recorded in `193.2-UAT.md` § U1.

**When the operator answers:** record each of the five moments **separately** in `193.2-UAT.md`,
update the two reported-bug frontmatters and `SEED-163` to match **what was actually observed**, and
name any undriven row as OWED in `STATE.md`. **Do NOT mark a ROADMAP success criterion satisfied that
this run did not drive.**

---

## Self-Check: PASSED

| Claim | Command | Result |
|---|---|---|
| `CLAUDE.md` names `workflow_authoring.py`, `models/harness.py`, `builderStore.ts` | `grep -q` ×3 | **FOUND** |
| the invisibility sentence appears ≥ 3 times | `grep -c "permanently invisible to its own guardrail"` | **7** (was 3) |
| `193.2-UAT.md` exists and carries the literal | `grep -q "8+ planted facts"` | **FOUND** |
| `STATE.md` records the declined override | `grep -c "NONE for Phase 193.2"` | **2** |
| `ROADMAP.md` has no `**Plans**: TBD` for 193.2 and lists ten plans | `awk` over the section | **0 TBD · 10 ticked** |
| `STATE.md` YAML is intact after a hand edit | `yaml.safe_load` | **8 keys · `stopped_at` 5687 chars · `resume_file` repointed** |
| count gate | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` | **`count gate OK` · 3918 · failed 0 · 75/75**, twice |
| commit `413aa54a` | `git log --oneline` | **FOUND** |
| commit `0c87c154` | `git log --oneline` | **FOUND** |
| no file deletions | `git diff --diff-filter=D HEAD~2 HEAD` | **EMPTY** |
| no source file modified | `git diff --name-only HEAD~2 HEAD` | **7 files, all `CLAUDE.md` or under `.planning/`** |

No missing items.
