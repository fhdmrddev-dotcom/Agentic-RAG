---
phase: 193.2-from-authored-to-runnable
plan: 05
subsystem: workflow authoring (NL describe-first birth path)
tags: [prompt, authoring, publish-gate, SEED-163, BUG-260815-01, D-07, D-08, D-11, D-14]
base_sha: 8b371cff7c118a9273d800cc854fdb9cc9c7664a
base_sha_short: 8b371cff
requires:
  - "backend/app/models/harness.py — WorkflowDefinition.business_requirement (:538), read-only"
  - "backend/app/services/harness/grounding.py — the 193.1 D-26 two assertive arms, read-only"
  - "backend/app/services/harness/publish_service.py — _interactive_phase_failures, read-only"
provides:
  - "AUTHORING_SYSTEM_PROMPT: the DELIVERABLE-RULE suppression clause (D-11)"
  - "AUTHORING_SYSTEM_PROMPT: the shortened llm_human_input bullet, 121 -> 74 chars (D-11)"
  - "AUTHORING_SYSTEM_PROMPT: the business_requirement instruction, durable-not-a-copy (D-07)"
  - "F-8 — the FIRST mechanical guard on a prompt-literal length in this repository"
affects:
  - "plan 193.2-07 — adds the provenance stamp + additive model field ON TOP of this prompt change"
  - "plan 193.2-08 — drives real provider calls against this prompt and reports k/N"
tech-stack:
  added: []
  patterns:
    - "prompt properties asserted as SOURCE-LEVEL PROPERTIES, never hand-typed prose (the 187 analog)"
    - "fence corpora DERIVED from the artifact under test, never hand-counted (F-8)"
    - "the locator's identity proved against WF_SCHEMA rather than a hand-typed type list"
key-files:
  created:
    - backend/tests/unit/test_workflow_authoring_requirement.py
  modified:
    - backend/app/services/workflow_authoring.py
decisions:
  - "D-08 honoured in test NAMES, assertions and prose: no case asserts the field is always populated"
  - "D-02 no-second-concern argument proved by measurement, not asserted"
  - "grounding.py NOT touched — the D-26 arms stay, so the two fixes remain independently attributable"
metrics:
  duration: ~50 min
  completed: 2026-08-15
  tasks: 2
  commits: 2
  files_changed: 2
---

# Phase 193.2 Plan 05: The Authoring Prompt Learns What Publish Refuses — Summary

**One-liner:** Three edits inside one Python string literal — the `llm_human_input` bullet cut from
121 to 74 characters, a DELIVERABLE-RULE clause that states the publish consequence and offers an
alternative, and `business_requirement` added to the sentence that already names the definition-level
fields — plus `F-8`, the first mechanical guard on a prompt-literal length this repository has ever
had, driven RED against a real 200-character plant.

---

## Base commit — the wrong-base assertion FIRED again (21st project-wide, 9 for 9 across 193.1/193.2)

| Property | Value |
|---|---|
| Dispatched base | `8b371cff` |
| Worktree arrived at | **`fda792141b0129de7b15dd40ddc1082e76f95a2a`** — a `master` merge tip, **not** the dispatched base |
| `git merge-base --is-ancestor 8b371cff HEAD` | **exit 1** (the base was not even an ancestor) |
| After `git reset --hard 8b371cff` | `8b371cff7c118a9273d800cc854fdb9cc9c7664a` · is-ancestor **exit 0** |
| Branch | `worktree-agent-a8ecbf1baf7c458a0` (in-namespace) |
| Phase artifacts present after correction | `193.2-BASELINE.md` ✅ · `193.2-D10-D19-CONFIRMATION.md` ✅ |

⚠ Recorded because the running count is the evidence that this assertion is load-bearing rather than
ceremonial. `fda79214` is the **same wrong SHA** plans `193.2-01` and `193.2-02` arrived at. All work
below was performed after the reset.

---

## What shipped

### Task 1 — three edits inside `AUTHORING_SYSTEM_PROMPT` (`0e203d64`)

**Every number below was RE-DERIVED from the literal at runtime, never hand-counted.**

#### (a) The `llm_human_input` bullet: **121 → 74 characters**

| Bullet | chars at HEAD | chars now |
|---|---|---|
| `programmatic` | 67 | 67 |
| `llm_single` | 60 | 60 |
| `llm_agent` | 84 | 84 |
| `llm_batch_agents` | 85 | 85 |
| **`llm_human_input`** | **121** ← longest of seven | **74** |
| `llm_emit` | 83 | 83 |
| `external_action` | 104 ← the module's recorded ceiling | 104 |
| **median of the six shipped** | **83.5** | — |
| **max of the seven** | **121** | **104** |

⚠ **What made this bullet a nudge was not only its length — it was its TAIL.** It ended with an
active invitation: *"— use for any 'confirm before finalizing' step"*, recommending the ONE phase
type the synchronous publish gate categorically refuses. Dropping that tail is what took it from 121
to 74; what the type IS stays truthful and complete (it pauses and asks the human; it takes a
`prompt` and optional `options`), and both facts are pinned by the new test file.

#### (b) The DELIVERABLE RULE gains ONE clause — an ADDITION, not a rewrite

Proved rather than claimed. A line-by-line diff of the **rendered prompt** (both versions `eval`'d
out of the source and compared with `difflib`) returns **exactly three changes** across the whole
constant — the shortened bullet, the new clause, and the extended closing sentence:

```
-- llm_human_input: PAUSE and ask the human (`prompt`, optional `options`) — use for any 'confirm before finalizing' step.
+- llm_human_input: PAUSE and ask the human (`prompt`, optional `options`).
+- Do NOT add a step that pauses to ask the human (`llm_human_input`, or a validator whose …
-- Give each phase a short `slug`, … `status` ('draft').
+- Give each phase a short `slug`, … `status` ('draft'), and `business_requirement` — ONE line …
```

And specifically for the DELIVERABLE RULE block:

| Property | Value |
|---|---|
| `CRITICAL` label line | **byte-identical** |
| The shipped template clause | **byte-identical** |
| `new_block[:len(old_block)] == old_block` | **True** — a pure APPENDED line |
| Block size | **769 → 1202 chars** (the clause is **433**) |
| The four other HARD-constraint bullets | byte-identical |

**The clause, as shipped:**

> `- Do NOT add a step that pauses to ask the human (`llm_human_input`, or a validator whose `on_failure` is `ask_user`): publishing VALIDATES a workflow by running it, and a run waiting on a person cannot finish — so a workflow containing one cannot be published at all. When a fact might be missing, have the step gather it with the tools it is given, and have the deliverable say plainly that it could not be found. Never invent it.`

Each constraint the plan set, and how it is met:

| Constraint | How |
|---|---|
| State the CONSEQUENCE, true today | *"publishing VALIDATES a workflow by running it, and a run waiting on a person cannot finish"* — this is what `publish_service.py:190-208` actually does (stage 2.5 blocks pre-run, before any golden run) |
| Tell the model what to do INSTEAD | *"have the step gather it with the tools it is given"* — the same register as the existing template clause's *"`llm_agent` if it needs tools to gather first"* |
| MUST NOT tell the model to invent data | *"have the deliverable say plainly that it could not be found. Never invent it."* (`SEED-159` — a blank that lies is not an improvement) |
| **D-14** — no `planned` / `coming soon` / `deferred` / `future release` | Verified case-insensitively over the whole constant: **all four absent**, and driven RED against a real plant (below) |
| **D-11 proportionality**, measured against WHICH baseline | Stated in the comment beside the edit: measured against **this block (769 chars)**, explicitly NOT the 83.5 type-bullet median, which belongs to the bullet list |
| **D-25** — implies no human-pause capability | The clause suppresses; it names no capability and promises no rework |

#### (c) `business_requirement` joins the SAME sentence

`grep -c business_requirement backend/app/services/workflow_authoring.py` returned **0 at HEAD** (the
whole of `SEED-163` in one number); it now returns **5** (1 in the prompt, 4 in the comment block
recording why). The field is named inside the existing sentence that already sets `slug`, `version`,
`name` and `status` — not a new paragraph:

> *"Set the definition `slug`, `version` (1), `name`, `status` ('draft'), and `business_requirement` —
> ONE line saying what this workflow must deliver on ANY run, phrased so it stays true for the next
> run and the one after, NOT a restatement of the particular request described below."*

D-07's distinction is carried in roughly one clause (*"on ANY run … NOT a restatement of the
particular request"*), which is also part of T-193.2-03's mitigation rather than decoration: a
durable requirement is a **harder** bar for the judge to be satisfied by than a one-run instruction.

#### What was deliberately NOT touched

| File / symbol | Evidence |
|---|---|
| `backend/app/services/harness/grounding.py` | `git diff --numstat HEAD~2 -- …` → **EMPTY** |
| `backend/app/models/harness.py` | same command → **EMPTY** |
| `WF_SCHEMA`, `EMIT_TOOL`, `_strip_discriminator` | `git diff -U0` matches these tokens on exactly **one** line, which is a comment I added; the definitions are untouched |
| The schema `required` list | `WF_SCHEMA["required"] == ["slug", "version", "name", "phases"]` — unchanged, now pinned by a test |

### Task 2 — `backend/tests/unit/test_workflow_authoring_requirement.py` (`82b1324b`)

**659 lines · 15 cases · 15 passed / 0 failed.** No network: `forced_emit` is patched on the module
it is imported from, the grounding accessors are patched inside `workflow_authoring`, and the
provider is the deliberately-fake `STUB_PROVIDER = "stub-provider-not-a-real-one"` — the analog's
device that makes *"this file cannot reach a network"* self-evident rather than a docstring claim.

| # | Case | What it pins |
|---|---|---|
| 1 | `test_the_length_predicate_sees_an_over_long_bullet` | The permanent INLINE 200-char plant |
| 2 | `test_the_bullet_block_is_the_phase_type_block` | Non-vacuity of the locator, proved against **WF_SCHEMA's seven `phase_type` consts** |
| 3 | `test_every_phase_type_bullet_is_within_the_modules_own_length_precedent` | **F-8** — every bullet ≤ 104, lengths re-derived at runtime |
| 4 | `test_the_interactive_bullet_no_longer_leads_the_pack` | `llm_human_input` bullet < 121 and ≤ 104; still names `prompt` + `options` |
| 5 | `test_positive_control_the_promise_regex_matches_a_planted_promise` | The D-14 regex sees each of the four tokens |
| 6 | `test_the_prompt_promises_no_unscheduled_capability` | **D-14** absence |
| 7 | `test_the_deliverable_rule_block_is_locatable_and_non_empty` | Non-vacuity of the block locator (≥ 2 bullets, `CRITICAL` present) |
| 8 | `test_the_deliverable_rule_addresses_the_interactive_case` | **D-11** — both identifiers the gate keys on |
| 9 | `test_the_interactive_clause_offers_an_alternative_and_forbids_inventing_data` | An alternative is offered; fabrication is forbidden |
| 10 | `test_the_prompt_asks_for_the_business_requirement_at_all` | `SEED-163` in one assertion |
| 11 | `test_the_requirement_is_asked_for_at_the_definition_level` | **D-07** first half — definition-scoped |
| 12 | `test_the_requirement_is_asked_for_as_a_DURABLE_statement` | **D-07** second half — durability signal family |
| 13 | `test_the_emit_schema_still_advertises_the_field_and_still_does_not_require_it` | **D-09** — `required` pinned EXACTLY |
| 14 | `test_the_prompt_change_buys_no_extra_provider_shot` | Call budget = 1, and the constant under test **is** the string the service sends |
| 15 | `test_an_emit_that_omits_the_field_still_succeeds_unchanged` | **D-08's fallback** — omission is a clean success with `None` |

**Two derivations worth naming, because both replace a hand-typed list with a measurement:**

- The bullet block is located by **SHAPE** (the first maximal run of `"- "` lines), so rewording the
  header sentence above it cannot silently empty the corpus — and the block's **identity** is then
  proved by checking that every `phase_type` the schema advertises has exactly one bullet in it.
- Those seven type names are read out of `WF_SCHEMA`'s `oneOf` `const`s, not re-typed. A hand-typed
  list would be a second copy of the model's `Literal`, free to drift from it.

---

## The fences were DRIVEN RED FOR REAL — three times, not once

The plan required one real plant (F-8). Three were driven, because this phase's standing lesson is
that **a fence never driven RED has never been shown to see anything** — 193.1 found four such fences
and *all four were caught by PLANTING, none by reading*. An inline needle proves the PREDICATE sees a
shape; only a real plant proves the FENCE is pointed at the right constant.

### Plant 1 — F-8, a real 200-character bullet in `AUTHORING_SYSTEM_PROMPT` (REQUIRED by the plan)

**The plant** — an 8th bullet inserted into the phase-type block, measured at exactly **200**
characters by the same runtime derivation the fence uses:

```python
"- planted_type: xxxxxxxxxxxx…x.\n"    # len(line) == 200
```

**RED observed** — `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflow_authoring_requirement.py -q`:

```
..F............                                                          [100%]
___ test_every_phase_type_bullet_is_within_the_modules_own_length_precedent ___
        over = [(len(b), b) for b in bullets if len(b) > MAX_BULLET_CHARS]
>       assert not over, (
E       AssertionError: phase-type bullet(s) exceed the module's own 104-char precedent
        (the `external_action` bullet): [(200, '- planted_type: xxxxxxxxxxxxxxxx…')]
tests\unit\test_workflow_authoring_requirement.py:243: AssertionError
FAILED tests/unit/test_workflow_authoring_requirement.py::test_every_phase_type_bullet_is_within_the_modules_own_length_precedent
1 failed, 14 passed, 1 warning
```

| Property | Value |
|---|---|
| Failing case | **`test_every_phase_type_bullet_is_within_the_modules_own_length_precedent`** |
| Assertion line | `test_workflow_authoring_requirement.py:243` — `assert not over` |
| Result under plant | **1 failed · 14 passed** |
| The fence reported the plant's real length | **200**, derived — not the constant it was compared against |

**GREEN after restore.**
```
git checkout -- backend/app/services/workflow_authoring.py
git diff --numstat -- backend/app/services/workflow_authoring.py   → EMPTY
cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflow_authoring_requirement.py -q
→ 15 passed, 1 warning in 1.13s
```

### Plant 2 — D-14, the token `deferred` planted in the shipped clause

**The plant:** `"Never invent it.\n\n"` → `"Never invent it. A deferred rework will allow this later.\n\n"`.

```
E       AssertionError: assert <re.Match object; span=(2146, 2154), match='deferred'> is None
FAILED tests/unit/test_workflow_authoring_requirement.py::test_the_prompt_promises_no_unscheduled_capability
1 failed, 14 passed, 1 warning
```

⚠ The plant was written to be *plausible* — a sentence a well-meaning future editor might add to
soften the prohibition — which is exactly the failure mode `SEED-164` exists to prevent. Restored;
`git diff --numstat` EMPTY.

### Plant 3 — D-11, the identifiers removed from the clause

**The plant:** the clause reworded to drop `` `llm_human_input` `` and `` `ask_user` `` while keeping
its plain-language meaning intact — the realistic regression, since the prose still reads correctly.

```
E       AssertionError: the DELIVERABLE RULE does not name the interactive phase type — the model
        has no way to know which composition makes the workflow unpublishable
E       AssertionError: expected exactly one interactive clause in the block
FAILED …::test_the_deliverable_rule_addresses_the_interactive_case
FAILED …::test_the_interactive_clause_offers_an_alternative_and_forbids_inventing_data
2 failed, 13 passed, 1 warning
```

Restored; `git diff --numstat` EMPTY; **15 passed** on re-run.

⚠ **T-193.2-05-A is discharged.** All three plants were temporary probes on tracked source, each
restored with a **file-named** `git checkout --`. No `git clean`, no `git reset --hard` (beyond the
one base correction at agent start), no `git checkout -- .` was used at any point.

---

## D-08 honoured — in test names, assertions AND prose

**Nothing in this plan claims `business_requirement` is always populated, or that `llm_human_input`
never appears.** The prohibition is stated in the new file's own module header, verbatim:

> *"No case in this file may assert that `business_requirement` is always populated, or that
> `llm_human_input` never appears in a generated definition. Those are FREQUENCY claims about a
> language model's output. They are measured as k/N by
> `backend/tests/integration/test_193_2_authoring_frequency.py` (plan 193.2-08) against real provider
> calls, and the phase's claim is **a reduction, never an absence** — the publish gate STAYS precisely
> because a prompt cannot guarantee absence."*

`grep -in "always populated\|never appears"` returns exactly **two** lines, both prose explaining the
prohibition (the module docstring at `:21` and the D-08 fallback case's docstring), **zero
assertions**. Case 15's docstring additionally carries a "READ THIS BEFORE ADDING A SIBLING CASE"
warning so the fallback case cannot later be mistaken for the inverse of an always-populated claim.

⚠ **This plan asserts nothing whatsoever about behavioural effect.** No provider call was made. Every
statement above is about a string constant, a JSON schema, or control flow. Whether the model
actually fills the field or actually stops composing interactive phases is `193.2-08`'s measurement,
and this summary deliberately pre-empts none of it.

---

## D-02 — the no-second-concern argument for `backend/app/services/workflow_authoring.py`, MEASURED

**G-5 status: the file is at `9 commits / 5 phases / 389 lines` (BASELINE §5, row 3) and is ABSENT
from the `CLAUDE.md` hot-file ledger** — one of the four rows D-03 owes. G-5 fires on the count and
is honoured **by construction** here. The argument is a test, not a preference:

| The concern this file already owns | What this plan added to it |
|---|---|
| `AUTHORING_SYSTEM_PROMPT` already carries a **DELIVERABLE RULE about publishability**, labelled *"CRITICAL — a wrong choice makes the workflow unpublishable"* | **ONE clause** appended to that same rule, about another composition with the same consequence |
| The prompt already **names the definition-level fields to set** (`slug`, `version`, `name`, `status`) in one sentence | **ONE field** added to that same sentence |
| The prompt already carries a **phase-type bullet list with a recorded length discipline** (`:70-79`, since Phase 189) | **ONE bullet shortened**, obeying the rule the file already states about itself |

**No new function, no new module, no new import, no control-flow change, no schema change, no
signature change.** The rendered-prompt diff is exactly three lines. If this had added a genuinely
second concern the plan required a STOP and a refactor recommendation first; it did not, and the
measurement above is why rather than an assurance.

⚠ **`grounding.py` was deliberately left alone**, and that is load-bearing rather than tidy. Its own
code states the rule (`:605-607`): *"One home per concern: the rule states the policy, the grounding
states the FACTS the policy reads."* Changing both at once would leave neither attributable when the
fix is re-driven in `193.2-08` — which is precisely the discipline 193.1's D-26 fix used to isolate
its own effect.

---

## ⚠ Downstream gates that now see different input — the `BUG-260815-01` failure mode, applied to this plan

**A prompt change can push a model's output across a gate nobody re-checked.** That is how
`BUG-260815-01` was created by 193.1's own D-26 fix, measured 2 for 2. This plan changes what the
model emits, so the gates that now see different input are named here even where they cannot be
tested without a provider call.

| Gate | What changes | Assessment |
|---|---|---|
| **Publish stage 1** — `business_requirement_missing` (`grounding.py:1007`) | Will now sometimes be satisfied by an AI-authored value instead of blocking | **Intended and accepted (D-09).** The predicate only checks non-emptiness; pressing Publish stays the human checkpoint. No code changed here |
| **The judge's `answers_business_requirement` criterion** | A model now authors the criterion a model later grades against | **T-193.2-03, accepted and recorded.** Format-string injection is not possible (the value is substituted *into* `.format()`, never itself formatted). The residual is a weakened gate; D-07's durability requirement is part of the mitigation, and the mechanical control (refusing to stamp provenance on a byte-identical copy of `describe`) lands in **193.2-07** |
| ⚠ **The Builder's requirement affordance** | A populated `business_requirement` may now arrive from `/generate` **before** 193.2-07 lands the visible AI-proposal mark | **A REAL INTERMEDIATE-STATE RISK, stated rather than assumed away.** Between this commit and 193.2-07, an author could see a pre-filled requirement with nothing marking it as a proposal — which is `SEED-163`'s *"decisions the author is never shown"* root in miniature. It is bounded (the field stays editable, and the two plans ship in the same phase), but **193.2-07 must not be dropped from this phase** without re-opening this |
| ⚠ **`external_action` as a displacement target** | The clause forbids `llm_human_input` / `ask_user`; the model may reach for `external_action`, which also *"always asks approval"* | **UNTESTED HERE and flagged for 193.2-08.** `_interactive_phase_failures` does **not** refuse `external_action`, so such a phase would pass the gate — this is a composition shift the k/N harness should watch for, not only the two counters it currently plans to report |
| **SC#4 control — `render_template` frequency** | The D-26 arms in `grounding.py` are byte-untouched, so the no-regression control is on an unchanged input | Verified by `git diff --numstat HEAD~2` → EMPTY. 193.1 measured 3/3; **it must not fall**, and only 193.2-08 can say whether it did |

---

## Verification

All gates run from this worktree with `backend/venv/Scripts/python.exe`. **No vitest was run** — this
plan owns no frontend file, and the sibling `193.2-03` runs the count gate; three concurrent gate
runs make it non-deterministic.

| Gate | Baseline (`193.2-BASELINE.md` §2) | Measured here | Verdict |
|---|---|---|---|
| `pytest tests/unit -k "authoring or 187"` **before any edit** | — | **49 passed · 0 failed** | pre-change record |
| `pytest tests/unit -k "authoring or 187"` **after both tasks** | 0 failed | **64 passed · 0 failed** (49 + the 15 new cases) | ✅ **no NEW failures** |
| The six workflow backend suites | **81 passed · 0 failed** | **81 passed · 0 failed** | ✅ reproduces exactly |
| `pytest tests/unit/test_workflow_authoring_requirement.py` | — | **15 passed · 0 failed** | ✅ |
| `git diff --numstat HEAD~2 -- grounding.py models/harness.py` | must be EMPTY | **EMPTY** | ✅ |

⚠ **The full-tree backend figure (62 failed at baseline) was NOT used as a gate** and was not re-run:
BASELINE §4 states the rule plainly — *gate on the named suites, never on a bare full-suite run* —
and no failing file in that set is touched by this plan. The `-k "authoring or 187"` subset selects
**64 of 2173** cases and is the authoritative reading for this plan.

### Acceptance criteria

| Criterion | Result |
|---|---|
| `grep -c business_requirement …workflow_authoring.py` ≥ 1; summary records **0** at HEAD | ✅ **0 → 5**; the 0 is recorded above |
| `llm_human_input` bullet ≤ 104 and < 121, both numbers in the comment | ✅ **74**; comment carries `121`, `83.5`, `74`, `104` |
| Prompt matches none of `planned` / `coming soon` / `deferred` / `future release` | ✅ all four absent, case-insensitive; driven RED |
| DELIVERABLE RULE keeps its `CRITICAL` label and template clause byte-unchanged; an ADDITION not a rewrite | ✅ proved by `new_block[:len(old)] == old` |
| `business_requirement` lives in the SAME sentence as `slug` / `version` / `name` / `status` | ✅ located in the literal and verified |
| `git diff --numstat` shows `grounding.py` UNCHANGED | ✅ EMPTY |
| `WF_SCHEMA`, `EMIT_TOOL`, `required` unchanged | ✅ only a comment mentions them in the diff |
| `pytest -k "authoring or 187"` — no NEW failures | ✅ 0 failed |
| Summary states the D-02 argument | ✅ its own section |
| New file exists, 0 failed | ✅ 15 passed |
| F-8 derives lengths at runtime; no hard-coded per-bullet literal but 104 + 121 | ✅ only `MAX_BULLET_CHARS = 104` and `LLM_HUMAN_INPUT_CHARS_BEFORE_193_2 = 121` (plus the plant's own 200) |
| F-8 carries non-vacuity ≥ 7 AND an inline 200-char plant | ✅ both, plus a third guard proving the locator found the right block |
| Summary records the REAL 200-char plant driven RED with command, case name, output, restore, EMPTY numstat | ✅ above, for **three** plants |
| D-14 case with a positive control | ✅ |
| `WF_SCHEMA["required"] == [...]` and `business_requirement` in `properties` | ✅ |
| Call-budget case asserts exactly one call | ✅ (and that the constant sent **is** the one under test) |
| Module docstring contains `D-08` and the prohibition | ✅ |
| `grep -i "always populated"` / `"never appears"` find no ASSERTION | ✅ two docstring mentions, zero assertions |
| No network call; `STUB_PROVIDER` naming; `forced_emit` patched | ✅ |

---

## Deviations from Plan

**None affecting scope or contract.** Three notes, each a strengthening rather than a change:

1. **Three real RED plants instead of the one required.** The plan mandated the F-8 plant; D-14 and
   D-11 were additionally driven RED because a fence that has only ever been green has not been
   shown to see anything. All three restored to an EMPTY numstat.
2. **F-8's locator was hardened beyond the plan's description.** The plan said *"take the lines
   beginning with `- ` that sit inside the phase-type bullet block"*. Locating that block by its
   header sentence would tie the fence to prose the module explicitly licenses as discretionary, so
   it is located by SHAPE and its identity is then proved against `WF_SCHEMA`'s seven `phase_type`
   consts. This makes the non-vacuity guard stronger than a bare `>= 7`, which would pass over the
   *wrong* seven bullets.
3. **Two extra cases beyond the six the plan enumerated** — `test_the_bullet_block_is_the_phase_type_block`
   (the locator guard above) and `test_an_emit_that_omits_the_field_still_succeeds_unchanged`
   (D-08's fallback: an omitted field is still a clean success with `None`). Neither asserts a
   frequency.

**No deviation rules 1–4 fired.** No bug, no missing critical functionality, no blocker, no
architectural question. No package was installed (T-193.2-SC).

---

## Deferred / owed by later plans (NOT this one)

- **`193.2-07`** — the provenance stamp and the additive model field. Deliberately not stubbed here.
  ⚠ See the intermediate-state risk above: until it lands, a pre-filled requirement carries no mark.
- **`193.2-08`** — the k/N frequency measurement. **The only artifact permitted to state whether this
  prompt change works.**
- **`193.2-06`** — the rewritten refusal message (the other half of `BUG-260815-01`, shipping as
  words in `publish_service.py`). Untouched here.
- **D-03's four `CLAUDE.md` ledger rows**, one of which is this file (`9 / 5 / 389`, ABSENT from the
  table). Not this plan's `files_modified`.

---

## Known Stubs

**None.** No placeholder, no TODO, no hardcoded empty value was introduced. The `PLANTED_OVER_LONG_BULLET`
constant is a permanent positive control, not a stub — it is asserted against, never rendered.

## Threat Flags

**None.** No new network endpoint, no auth path, no file access pattern, no schema change at a trust
boundary. The three registered threats are addressed above: T-193.2-03 (accepted, with D-07 as part
of the mitigation and the mechanical control in 193.2-07), T-193.2-03b (no new data path added),
T-193.2-05-A (all plants restored, EMPTY numstat proved for each), T-193.2-SC (nothing installed).

---

## Commits

| Hash | Type | Message |
|---|---|---|
| `0e203d64` | `feat` | the DELIVERABLE RULE learns what publish refuses, and asks for the durable requirement (D-07/D-11) |
| `82b1324b` | `test` | F-8 written from scratch plus the deterministic prompt and schema properties (D-08/D-11/D-14) |

No file deletions in either commit (`git diff --diff-filter=D` empty for both). No `STATE.md` or
`ROADMAP.md` write — the orchestrator owns those after the wave completes.

---

## Self-Check: PASSED

| Claim | Command | Result |
|---|---|---|
| `backend/app/services/workflow_authoring.py` exists | `ls -1` | **FOUND** |
| `backend/tests/unit/test_workflow_authoring_requirement.py` exists | `ls -1` | **FOUND** |
| `.planning/phases/193.2-from-authored-to-runnable/193.2-05-SUMMARY.md` exists | `ls -1` | **FOUND** |
| commit `0e203d64` exists | `git log --oneline --all \| grep` | **FOUND** |
| commit `82b1324b` exists | `git log --oneline --all \| grep` | **FOUND** |

No missing items.
