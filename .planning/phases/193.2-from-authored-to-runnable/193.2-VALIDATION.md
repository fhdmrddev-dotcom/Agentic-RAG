---
phase: 193.2
slug: from-authored-to-runnable
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-15
validated: 2026-08-15
manual_rows_owed: 3
---

# Phase 193.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `193.2-RESEARCH.md` § *Validation Architecture* (measured at HEAD `develop`, 2026-08-15).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Backend framework** | pytest (async via `pytest-asyncio`), run from `backend/venv` |
| **Backend config** | `backend/pytest.ini` / `backend/tests/` |
| **Backend quick run** | `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py tests/unit/test_publish_service.py tests/unit/test_starter_workflows.py tests/unit/test_103_draft_crud.py tests/unit/test_published_workflow_ownership.py tests/unit/test_187_route_assigned_reach.py -q` |
| **Backend full suite** | `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q` |
| **Frontend framework** | vitest 4.1.0 — **ALWAYS** with `GSD_VITEST_MAX_WORKERS=2` |
| **Frontend quick run** | `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run <files>` |
| **Frontend gate** | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` (repo root) |
| **Typecheck** | `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — ⚠ bare `--noEmit` checks **zero** files |
| **Estimated runtime** | backend quick ~1 s · backend full ~29 s · frontend 5 suites ~85 s · count gate ~90 s |

---

## Measured baselines at HEAD — RUN, not estimated (2026-08-15)

| Suite | Result |
|---|---|
| `backend/tests/unit` (full) | **62 failed · 2092 passed · 2 xfailed · 2 xpassed** |
| The six workflow-touching backend suites | **81 passed · 0 failed** |
| The five workflow frontend suites | **337 passed · 0 failed** |
| Frontend **count gate** | **total 3892 · failed 0 · pinned 3868 · 75/75 files → `count gate OK`** |
| Frontend **full** (267 files) | **26 failed · 5595 passed** — all 26 are the pre-existing SEED-056 rot set, **outside the gate** |

⚠ **Two corrections a plan must not mistake for its own breakage:**
1. `CLAUDE.md` records the count gate at **3604**; measured today it is **3892**. It moved because the pinned set grew, not because anything broke.
2. **Gate on the count gate + the named suites, never on a bare full-suite run.** No plan in this phase touches any of the 26 rot-set failures.

---

## Sampling Rate

- **After every task commit:** the relevant quick run (backend quick, or the named vitest file)
- **After every plan wave:** backend full `tests/unit` **and** `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`
- **Before `/gsd:verify-work`:** count gate `failed 0`; the six backend workflow suites `0 failed`; the five frontend workflow suites `0 failed`; `tsc -p tsconfig.app.json` at its baseline
- **Max feedback latency:** ~90 s

---

## Per-Task Verification Map

**Scored 2026-08-15 by `/gsd:validate-phase 193.2` — every status below is a RUN, not a reading.**

| SC | Behaviour | Threat Ref | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|---|
| SC#1 | Prompt asks for `business_requirement`; emit sets it at a **measured frequency** (k/N) | — | integration, real provider calls | `RUN_193_2_AUTHORING=1 pytest tests/integration/test_193_2_authoring_frequency.py -q -s` | ✅ 1118 L | ✅ **green — driven, 20/20** (`193.2-FREQUENCY.md` fig. 1) |
| SC#1 | The AI-proposal mark renders when seeded, NOT when hand-typed / empty | T-193.2-03 | unit (vitest) | ~~`…WorkflowBuilderPage.header.test.tsx`~~ → **`GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx`** | ✅ 7 cases — **⚠ see correction C-4** | ✅ green |
| SC#1 | Empty/whitespace value is **never** stamped (D-06's inherited rule) — **F-6** | — | unit (pytest) | `pytest tests/unit/test_workflow_authoring_requirement.py -q` | ✅ 1027 L new file | ✅ green · plant RED recorded |
| SC#1 | Fallback is byte-identical to today (blank field + shipped `REQUIREMENT_INVITATION`) | — | unit (vitest) | canvas suite — *"EMPTY — no mark, and the shipped invitation still shows (D-08's fallback)"* | ✅ | ✅ green |
| SC#2 | Prompt clause reduces `llm_human_input` frequency — **k/N, never "always"** | — | integration, real calls | same harness as SC#1 (one run answers both) | ✅ | ✅ **green — 0/20 vs a 2/2 pre-fix baseline. A REDUCTION, not an absence** |
| SC#2 | Refusal names the phase's visible `name`, **both arms**, honest degradation on empty name | T-193.2-01 | unit (pytest) | `pytest tests/unit/test_publish_service.py -q` | ✅ `:637`, `:654`, `:681` | ✅ green |
| SC#2 | Refusal contains **no** `llm_human_input` / `ask_user` / slug token — **F-3** | T-193.2-01 | unit (pytest) fence | same (`:578`) | ✅ | ✅ green · plant RED recorded |
| SC#2 | Refusal promises nothing unscheduled (D-14) — **F-4** | — | unit (pytest) fence | same (`:614`) | ✅ | ✅ green · plant RED recorded |
| SC#2 | ONE string feeds **both** surfaces (D-12) — **F-5** | — | pytest + `?raw` sweep fence | ~~`pytest tests/unit/test_187_route_assigned_reach.py …`~~ → **`pytest tests/unit/test_publish_service.py -q` + `npx vitest run src/components/workflows/PublishGauntlet.test.tsx`** | ~~⚠ case new~~ → **⚠ see correction C-F5** | ✅ green — **rewritten under WR-01**; server `:994`, client `:1001+` |
| SC#2 | **G-1 — the stage-2.5 block writes a `publish_blocked` receipt** | T-193.2-01 | unit (pytest) | `pytest tests/unit/test_publish_service.py -q -k publish_blocked_receipt` | ✅ **added by this audit** | ✅ green · **2 plants RED** — see G-1 below |
| SC#2 | Prompt-bullet length discipline holds (D-11 made mechanical) — **F-8** | — | unit (pytest) fence | `pytest tests/unit/test_workflow_authoring_requirement.py -q` | ✅ `:185`, `:202`, `:224`, `:250` | ✅ green · 200-char plant RED |
| SC#3 | Published + drafts `ORDER BY updated_at DESC`; starters stay `ORDER BY name` — **F-1** | T-193.2-02 | unit (pytest), **rewritten 192.1 fence** | `pytest tests/unit/test_workflows_updated_at.py -q` | ✅ `:372`, old reasoning kept `SUPERSEDED` at `:375` | ✅ green · **RED on all 3 clauses** vs pre-change source |
| SC#3 | Every predicate byte-identical (RLS-bypassing boundary untouched) — **F-2** | T-193.2-02 | unit (pytest), shipped | same file, three `*_predicate_is_byte_identical_*` cases | ✅ **verified: 3 functions / 4 predicate shapes** (published is `parametrize`d ×2) | ✅ green · plant RED recorded |
| SC#3 | Rendered merge order is `starters ++ published ++ drafts`; newest published lands at index `starters.length` | — | unit (vitest) | `npx vitest run src/components/workflows/library/libraryFilter.test.ts` | ✅ +485 L, 5 order cases + residual | ✅ green |
| SC#3 | No client sort added anywhere (D-17) — **F-7** | — | unit (vitest), shipped fence | `npx vitest run src/components/workflows/library/rowIdentity.test.ts` | ✅ **widened by WR-04** to every non-mutating form, both homes | ✅ green · inline plant RED |
| SC#3 | Post-publish Run CTA renders after gauntlet PASS once the published refetch lands | — | unit (vitest) | `npx vitest run src/pages/WorkflowsPage.test.tsx` | ✅ **3 cases — its first coverage ever** (was: zero) | ✅ green |
| SC#4 | Template-first run still fills the bound template end to end | — | **manual, operator-driven** | 193.1 UAT kit `C:\Users\fhdmr\Desktop\uat-193.1-qbr\`, rule **8+ planted facts** | manual by D-21 | ✅ **DRIVEN — U1 PASS, 5/5 moments** (`193.2-UAT.md`) |
| SC#4 | The `render_template` control did not fall | — | integration, real calls | same k/N harness | ✅ | ✅ green — **5/5 both arms** vs 193.1's 3/3 like-for-like |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Measured at audit time (2026-08-15) — the runs behind the column above

| Suite | Result |
|---|---|
| The seven workflow backend suites | **213 passed · 0 failed** (was 210 before G-1's 3 cases) |
| The seven workflow frontend suites | **465 passed · 2 failed** → both **Phase-192** timeouts, see below |
| `WorkflowsPage.test.tsx` **run alone** | **55 passed · 0 failed** |
| Frontend **count gate** | **total 3918 · pinned 3868 · failed 1** — the same Phase-192 case |

⚠ **THE ONE GATE FAILURE IS NOT THIS PHASE'S, and it is named rather than waved past.**
`WorkflowsPage — D-17: … a PENDING project re-query never zeroes a count` fails with
`Error: STACK_TRACE_ERROR` / `Test timed out in 5000ms` — the oversubscription signature CLAUDE.md
records, *never* the signature of a real defect. Two independent facts settle it: `git log -S`
attributes the case to **`bf7f986f` (192-11)**, so 193.2 did not author it; and the file **passes
55/55 when run alone**. The failing name was captured from the gate's own JSON report **before**
anything was re-run, per the `193.2-02` lesson. ⚠ `total 3918` **matches** the figure CLAUDE.md
records at this phase's close — the gate's contract is *no per-file decrease* + *zero failing*, and
a growing total is the gate working.

### ⚠ CORRECTION **C-4** — the AI-mark cases live in `canvas.test.tsx`, not `header.test.tsx` (2026-08-15)

**Recorded BESIDE the original rather than over it** — the strikethrough in the SC#1 row preserves
exactly what this document asserted.

**What the row said.** The AI-proposal-mark row named
`frontend/src/pages/WorkflowBuilderPage.header.test.tsx`, and Wave 0 required *"New cases in
`WorkflowBuilderPage.header.test.tsx`"* plus an assertion that `git diff --numstat` stays **empty**
for that file's byte-for-byte flag-off pins.

**What is measured.** `193.2-09` rerouted the cases to **`WorkflowBuilderPage.canvas.test.tsx`**
(commit `ca749b68`, *"routed to canvas.test.tsx where the fixture lives (C-4)"*) — the mark is
`canvasEnabled`-gated exactly as `requirementAffordance` is, and the canvas suite is where a fixture
carrying `business_requirement_seeded_by_ai` actually exists. **Seven cases** shipped there.
`git diff --numstat c1a6c122..HEAD -- …header.test.tsx` returns **nothing**, so the Wave-0 pin
requirement is satisfied in the strongest available form: the pins are not merely unbroken, the file
is **byte-untouched**.

⚠ **The reroute also invalidated a claim the plan made about those pins, and `193.2-09` measured it
FALSE rather than repeating it:** `FLAG_OFF_HEADER_MARKUP` does **not** red when the mark is mounted
outside its gate — its fixture carries no `business_requirement_seeded_by_ai`, so the mark never
renders in that fixture at all. The protection is real and lives in the two new `canvas.test.tsx`
cases instead.

---

### ⚠ ADDITION **G-1** — a property with verification of NEITHER kind, and a UAT expectation that contradicted the code (2026-08-15, this audit)

**Found by this audit; filled by it.** Neither an automated test nor a driven manual row covered
whether the stage-2.5 interactive block leaves an audit receipt — while the two sources that spoke to
it **disagreed**:

| Source | Claim |
|---|---|
| `193.2-UAT.md` row **U2** | *"**no** `publish_blocked` row is written"* |
| `publish_service._block`'s own docstring | *"Write a `publish_blocked` receipt + return the D-08 structured verdict"* — and stage 2.5 (`publish_service.py:223`) returns `await _block(...)` |

**U2 was never driven** — no interactive step appeared across the 20 measured generations
(`193.2-FREQUENCY.md` figure 2, **0/20**), so the surface never rendered and no observation settled
it. The sibling case `test_interactive_phase_blocks_publish_before_golden_run` **already patched
`write_audit` and asserted nothing on it**, which is precisely how a receipt goes unobserved while
looking watched.

**MEASURED: a publish POST that reaches stage 2.5 DOES write a `publish_blocked` receipt**, at
`blocked_stage="interactive_phase"`, with `run_id` NULL and the `definition_id` carried in metadata
(the stage-0/1/2 shape `_block` documents). **U2's sentence is true only of a DIFFERENT path** — the
one where the client greys `publish-trigger` off `/validate`'s `blockedReason` and the POST is never
sent. The two tiers were conflated; they are separated here, and `193.2-UAT.md` is corrected beside
its original.

**What now covers it** — `backend/tests/unit/test_publish_service.py`:

| Case | Role |
|---|---|
| `test_the_interactive_block_writes_a_publish_blocked_receipt[llm_human_input\|ask_user_validator]` | Asserts exactly ONE `publish_blocked` receipt, its `blocked_stage`, its NULL `run_id`, its `definition_id` and its non-empty `named_failures` |
| `test_a_clean_definition_writes_no_interactive_publish_blocked_receipt` | **Positive control** — without it, a mock recording a receipt on *every* publish would satisfy the case above and nobody would know |

**Both plants observed RED, in production source, restored to an EMPTY `git diff --numstat`:**

| Plant | File | Observed |
|---|---|---|
| **A** — `event_type="publish_blocked"` → `"PLANT_no_receipt"` | `publish_service.py:_block` | **RED on both parametrized shapes**; the control correctly stayed GREEN (it asserts an absence) |
| **B** — `_interactive_phase_failures(definition) or [{…}]`, so every definition blocks at stage 2.5 | `publish_service.py:222` | **RED on the control**; the two receipt cases correctly stayed GREEN |

Each plant reds a *different* case, which is the only evidence that the control is not redundant.
**No implementation file was modified** — `git diff --numstat -- backend/app/services/harness/publish_service.py`
is EMPTY at rest.

---

### ⚠ CORRECTION **C-F5** — the F-5 row credited a file that received ZERO changes (2026-08-15, code review `WR-01`)

**Recorded BESIDE the original claim rather than over it**, per this project's habit — the strikethroughs
in the F-5 row above preserve exactly what this document asserted, because a credit that was wrong is
more useful visible than erased.

**What the row said.** F-5's automated command named
`backend/tests/unit/test_187_route_assigned_reach.py` first, and the F-5 fence row below described its
publish half as *"Assert publish path and `/validate` path emit byte-identical `message`"*, marked
`⚠ case new`. `193.2-06-SUMMARY.md` inherited the same pointer, and the rewritten fence's own docstring
repeated it.

**What is measured.** `git diff c1a6c122..HEAD --stat -- backend/tests/unit/test_187_route_assigned_reach.py`
returns **nothing**. That file received **zero changes in this phase** — no new case, no edited case.
The `⚠ case new` mark was false, and it was false in the one column a reader scans to decide whether a
property is covered.

**Compounding — and this is the part that mattered.** The half that *did* ship,
`test_publish_service.py::test_f5_validate_and_the_publish_refusal_emit_the_same_bytes`, was a
**tautology**: it compared `_interactive_phase_failures(definition)` against a list comprehension over
`_interactive_phase_failures(definition)` — the same pure function, the same input, twice — and never
imported `app.api.workflows` at all. So **neither** half of F-5 could observe drift, while the row read
as covered. Driven under a planted `f.get("message")[:20]` in the route's step-(4) projection, the
pre-rewrite case stayed **GREEN on all four parametrized shapes** (measured, not inferred).

**What actually covers F-5 now:**

| Half | File | What it does |
|---|---|---|
| **server** | `backend/tests/unit/test_publish_service.py::test_f5_validate_and_the_publish_refusal_emit_the_same_bytes` | **REWRITTEN under `WR-01`.** Executes the REAL `validate_workflow` handler (`app.api.workflows`) — the posture `test_182_validate.py:117-126`, `test_182_grounding_degradation.py:244` and `test_182_publish_grounding_stage.py:374` already use — and compares its `interactive_phase` verdict `message`s against what the publish path composes. Two call paths, one expected value. **Driven RED on all four shapes** by the `[:20]` plant, and again by dropping step (4) entirely (the non-vacuity clause); restored to an EMPTY `git diff --numstat` both times |
| **client** | `frontend/src/components/workflows/PublishGauntlet.test.tsx:986+` | The `193.2-06` task-3 `?raw` one-home fence — *"the refusal has exactly one home"*. This is what the row should have named all along; `test_187_route_assigned_reach.py` never carried it |

**`test_187_route_assigned_reach.py` is NOT modified by this correction.** It is a shipped, green,
Phase-187 suite about `_ROUTE_ASSIGNED_CODES`; the defect was the credit, not the file.

---

## Fences — every one carries a named positive control

**Project rule (non-negotiable): a fence that has never been driven RED has never been shown to see anything.** 193.1 found FOUR fences that could not fire; all four were caught by planting, none by reading.

| # | Fence | The plant that MUST be observed RED |
|---|---|---|
| **F-1** | `test_no_feed_orders_by_updated_at` **rewritten**: published + draft sources contain `ORDER BY updated_at DESC` and **not** `ORDER BY name`; starter source contains `ORDER BY name` and **not** `updated_at` | Run the rewritten test against the **pre-change** source (`git show <base>:backend/app/db/workflows.py`). It must FAIL on all three clauses |
| **F-2** | The three `*_predicate_is_byte_identical_to_what_shipped` cases stay green | Plant a one-character `WHERE` change (`created_by = $1` → `created_by= $1`), observe RED, restore |
| **F-3** | Refusal contains none of `llm_human_input`, `ask_user`, or the phase `slug` | Plant a definition with slug `zz-plant-slug` and `name=None`; assert absent. Then plant an `f"{slug}"` in the composer and observe RED |
| **F-4** | Refusal promises nothing unscheduled (D-14) | Assert no match for `/plann?ed\|coming\|soon\|deferred\|future release/i`. Plant `planned` into the literal, observe RED |
| **F-5** | ONE message source feeds both surfaces (D-12) | Assert publish path and `/validate` path emit byte-identical `message`; `?raw` sweep proves no copy of the literal in `frontend/src`. Plant a client-side literal, observe RED. ⚠ **AMENDED by correction C-F5** — as first shipped the publish half did NOT call `/validate`, it called the same helper twice (`x == x`) and stayed green under a real route plant. The plant that MUST be observed is now stated precisely: `f.get("message")[:20]` in the route's step-(4) projection in **`app/api/workflows.py`**, restored to an EMPTY `git diff --numstat` — plus a second plant dropping step (4) outright, which the non-vacuity clause catches |
| **F-6** | AI mark never stamped on empty/whitespace requirement | Stub emit returning `business_requirement="   "`; assert flag false. Plant unconditional `True`, observe RED |
| **F-7** | `rowIdentity.test.ts:167` T-7/P-4 stays green — no client sort | Already positive-controlled at `:169-172`. **Do not weaken.** Additionally sweep `libraryFilter.ts` + `WorkflowsPage.tsx` for a new `.sort(` on the merged list; plant `rows.sort(`, observe RED |
| **F-8** | The `llm_human_input` bullet and any new DELIVERABLE-RULE clause stay within the module's own length discipline (`len(bullet) <= 104`, the `external_action` precedent) | Plant a 200-character bullet, observe RED. **This is the mechanical form of D-11, which prose documents and nothing guards today** |

---

## The non-determinism rule (D-08) — REQUIRED test shape

⚠ **Any test written as "the field is always populated" is WRONG and must be rejected at plan review.**

**(a) Deterministic — assert normally:** the prompt literal contains the new clause; `WF_SCHEMA` still advertises `business_requirement` and it is still **not** in `required`; a non-empty emit stamps the flag and an empty/whitespace/absent one does not; the fallback is byte-identical to today.

**(b) Non-deterministic — report k/N, never pass/fail on one call.** Model on (do **not** modify) `backend/tests/integration/test_187_authoring_roster.py`: opt-in env flag, **per-row `SimpleNamespace(harness_authoring_model=…)` so zero global state is mutated**, every row recorded *before* it can pass/fail/block, ⛔ rows always emitted.

The 193.2 sibling must drive **N ≥ 5** real `generate_workflow_definition` calls per row on a describe text **plus a bound template** (the condition `BUG-260815-01` measured 2-for-2), reusing the 193.1 UAT kit's 10-field `.docx` so SC#2 and SC#4 measure the same artefact, and report per row: `business_requirement` non-empty **k/N** · `llm_human_input` present **k/N** · `render_template` present **k/N** (the SC#4 no-regression control — 193.1 measured 3/3, it must not fall).

**The artifact must state the rule verbatim:** *"Baseline (pre-fix): `business_requirement` 0/N; `llm_human_input` 2/2 with a template. Post-fix measured: k/N. The claim is a reduction, not an absence — the publish gate stays because a prompt cannot guarantee absence."*

⚠ **Serialize this plan** (CLAUDE.md rule 4) — real provider calls plus local DB writes.

---

## D-20 — the narrowed roster, with all six N/A rows RECORDED

⚠ **Record all eight; never drop six.** `resolve_authoring_model` (`workflow_authoring.py:108-130`) returns `settings.harness_authoring_model` or the first `forced_emission`-capable entry of `("claude-opus-4-8", "gpt-5.5")` — **explicitly not the composer's model**, so chat's roster rule does not transfer.

| # | Provider | Row | Verdict / reason |
|---|---|---|---|
| 1 | anthropic — `claude-opus-4-8` | **DRIVE** | `forced_emission: True`, `emit_tier: force`, `capability_source: registry`. What the operator's environment actually resolves |
| 2 | openai — `gpt-5.5` | **DRIVE** | `forced_emission: True`, `emit_tier: force_strict`. Declared fallback; reachable via explicit settings stub |
| 3 | google | **N/A** | Not a `resolve_authoring_model` candidate; env-only knob, no UI, needs a restart — would measure a config that does not ship |
| 4 | deepseek | **N/A** | Same. (`strict_json_schema` also inert — D-122-04) |
| 5 | zhipu / GLM | **N/A** | Same |
| 6 | minimax | **N/A** | Same |
| 7 | moonshot / kimi | **N/A** | Same; also weakest tier (`coerce`, no `forced_emission`) — `test_187_authoring_roster.py:44-52` records that an explicitly-set moonshot model hands an unforceable model to `forced_emit`. A real finding, **not this phase's** |
| 8 | openrouter | **N/A** | Same; `native_tools: False`, the non-native tool path |

---

## Wave 0 Requirements

- [x] `backend/tests/integration/test_193_2_authoring_frequency.py` — the k/N harness (SC#1, SC#2, SC#4 control). Model on `test_187_authoring_roster.py`; **do not modify that file** → **1118 L; `test_187_authoring_roster.py` untouched**
- [x] `backend/tests/unit/test_workflow_authoring_requirement.py` — F-6, F-8, deterministic prompt/schema assertions → **1027 L**
- [x] New cases in `backend/tests/unit/test_publish_service.py` — F-3, F-4, F-5, both message arms, empty-`name` degradation → **+580 L**, plus **G-1** from this audit
- [x] **Rewrite** `backend/tests/unit/test_workflows_updated_at.py::test_no_feed_orders_by_updated_at` → F-1, with the old reasoning recorded **beside** the new → **`:372` renamed, `SUPERSEDED` block kept verbatim at `:375`, old name quoted so `git log -S` still finds the thread**
- [x] New cases in `frontend/src/pages/WorkflowsPage.test.tsx` — **the first automated coverage the post-publish Run CTA has ever had** → **3 cases** (absent-until-refetch, positive control, draft-exclusion)
- [x] New cases in `frontend/src/components/workflows/library/libraryFilter.test.ts` — merge-order / rendered-position (SC#3) → **+485 L**, incl. *"THE RESIDUAL — the newest published row is NOT at index 0 while there are starters"*
- [x] ~~New cases in `frontend/src/pages/WorkflowBuilderPage.header.test.tsx`~~ → **rerouted to `WorkflowBuilderPage.canvas.test.tsx` (7 cases) — see correction C-4.** The pin requirement is satisfied in its strongest form: `git diff --numstat` on `header.test.tsx` is **EMPTY** — byte-untouched, not merely unbroken
- [x] Characterization captures taken on the **UNMOVED** tree, in commits touching no source file (the 188.1 lesson, re-proved in 193.1)

---

## Manual-Only Verifications

**Outcomes recorded 2026-08-15 from `193.2-UAT.md`. A row is ✅ only on its own driven evidence — never inferred from another row's.**

| Behaviour | SC | Row | Outcome | Re-open trigger |
|---|---|---|---|---|
| The template-first run still fills the bound template end to end | SC#4 | **U1** | ✅ **PASS — driven, 5 of 5 moments** (M1 pre-filled + marked, M2 published with no canvas edit, M3 findable at position 4, M4 template filled, M5 all planted facts). ⚠ one honesty caveat on M1's *visible* half | Any phase changing the authoring prompt, the deliverable-rule arms in `grounding.py`, or the template bind — re-run the kit |
| Rendered position of a just-published workflow | SC#3 | **U4** | ✅ **PASS — rendered position 4, exactly the measured prediction.** The judgement was the operator's | Any change to `mergeLibrary`'s block order or to the two author feeds' `ORDER BY` |
| The post-publish Run CTA appears | SC#3 | **U3** | ◐ **PARTIAL — PASS on appearance (it appeared and NAMED the workflow); the LATENCY was never timestamped** | ⏱ **OWED.** Re-drive with a timestamp when the publish→refetch path is next touched, or if a user reports the CTA feeling slow |
| The greyed Publish control + `blockedReason` on a real describe→generate→canvas cycle | SC#2 | **U2** | ⏸ **NOT DRIVEN — and it could not be. Recorded as owed, NEVER as a pass.** No interactive step ever appeared (0/20), so the surface under test never rendered. A *different* refusal was met and became `BUG-260815-06` | **OWED.** Fires the moment any generation emits an interactive phase — the frequency is a reduction, not an absence, so this WILL recur. Also fires on any change to `_interactive_phase_failures` or the gauntlet's blocked-reason render. ⚠ Its receipt half is now **automated** by G-1; what remains owed is the *rendered* observation |
| The composer's Harness workflow picker under the new recency ordering | SC#3 | **U5** | ⏸ **NOT DRIVEN (optional row). Recorded as owed, NEVER as a pass.** | **OWED — run this one FIRST.** ⚠ The published feed's reorder reaches **three consumers outside the library** — the composer's Harness picker, `WorkspacePanel`'s run-soul and `threads.py`'s kickoff (`:97`) — all of which moved from alphabetical to recency, and **no test and no frontend module asserts an ordering for any of them**. The F-1 fence guards the SQL at its source; whether the picker *reads sensibly* is an operator judgement that only this row can make |

⚠ **Tally: 3 driven · 2 pass + 1 partial · 0 fail · 2 not driven.** Two rows are owed and one is
partial; the phase closes with them named rather than absorbed. **`nyquist_compliant: true` above
means every automatable requirement has automated verification — it does NOT mean every row ran.**

---

## Validation Sign-Off

- [x] All tasks have an automated verify or a Wave 0 dependency
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all ❌ MISSING references above — **all 8 artifacts exist and run green**
- [x] Every fence F-1..F-8 has its plant **observed RED and recorded**, not asserted — verified against the plan summaries; **G-1 adds a ninth with two plants**
- [x] No watch-mode flags; `GSD_VITEST_MAX_WORKERS=2` on every vitest invocation
- [x] The k/N artifact states the reduction-not-absence rule verbatim (D-08) — **on ONE line, so a literal `grep` can find it**
- [x] All eight roster rows recorded, six as N/A **with reasons** (D-20) — ⚠ all eight keys ARE configured; the six are blocked by the **resolver**, not by a missing key
- [x] `nyquist_compliant: true` set in frontmatter
- [x] **Three manual rows recorded as owed/partial with named re-open triggers — never as passes**

---

## Validation Audit 2026-08-15

| Metric | Count |
|--------|-------|
| Rows audited | 18 (16 automated · 2 documentation corrections) |
| Automated rows COVERED + green | **17 of 17** |
| Gaps found | **1** (G-1 — verification of neither kind, with a UAT expectation contradicting the code) |
| Resolved | **1** (3 cases added, 2 plants observed RED, 0 implementation files modified) |
| Escalated to manual-only | 0 |
| Manual rows owed | **3** (U2 not driven · U5 not driven · U3 partial) |
| Corrections recorded beside originals | **2** (C-4, G-1) |

**What this audit changed in the tree:** `backend/tests/unit/test_publish_service.py` only —
3 cases, +~85 L. **No implementation file was modified**, asserted by an EMPTY
`git diff --numstat` on `publish_service.py` after both plants were restored.

**Approval:** ✅ complete — `nyquist_compliant: true`, with 3 manual rows owed and named above.
