---
phase: 193.2
plan: 01
captured: 2026-08-15
base_sha: c1a6c12261377f2dd6b5c08d3d765111a7e53b59
base_sha_short: c1a6c122
tree_state: UNMOVED
source_files_modified: 0
---

# Phase 193.2 — Measured Baselines at HEAD

> **Every figure below was RE-RUN, not copied.** RESEARCH itself records that the count-gate total
> rotted from 3604 to 3892 in a single day, so a number inherited from CONTEXT, RESEARCH or
> VALIDATION is a number that may already be false. Where a measurement disagrees with a prior
> artifact, **the corrected value is recorded BESIDE the previous one, never over it** — this
> repository's standing habit.
>
> **A baseline only proves something if it PREDATES the change** (the 188.1 lesson, re-proved in
> 193.1). Nine plans downstream will assert *"unchanged"*, *"still green"*, *"no regression"*. None
> of those words mean anything without this dated, pre-change record.

---

## §1 — Base commit

| Property | Value |
|---|---|
| `git rev-parse HEAD` | `c1a6c12261377f2dd6b5c08d3d765111a7e53b59` |
| `git rev-parse --short HEAD` | `c1a6c122` |
| Commit subject | `docs(193.2): record planning completion — 10 plans, 6 waves, ready to execute` |
| `git status --porcelain` | **EMPTY** (no source changes, before and after both probes) |

### ⚠ CORRECTION C-1 — the base SHA is NOT the one this plan asserts, and the difference is inert

`193.2-01-PLAN.md` §`<execution_context>` states: *"`git rev-parse HEAD` MUST report
`3cfafa0d4ae444c4e9a438fa97ee07657fa6316e`. … if the SHA differs, STOP and report."*

The dispatched base is **`c1a6c122`**, not `3cfafa0d`. Both are recorded rather than one
overwriting the other. The plan was written at `3cfafa0d` (*"map patterns to existing analogs"*)
and **two further docs-only commits landed before execution was dispatched**:

```
git log --oneline 3cfafa0d..c1a6c122
  c1a6c122 docs(193.2): record planning completion — 10 plans, 6 waves, ready to execute
  2ddac519 docs(193.2): plan the phase — 10 plans in 6 waves
```

**This is reported, not silently absorbed — and it is then shown to be inert rather than argued to
be.** The one thing that depends on the base identity is plan `193.2-03`'s **F-1**, which must run
the rewritten `test_no_feed_orders_by_updated_at` against the **pre-change** source via
`git show <base>:backend/app/db/workflows.py`. That input is byte-identical at both SHAs:

```
git show 3cfafa0d:backend/app/db/workflows.py | md5sum → 2373680e88132362a0a2ffe665b8ac05
git show c1a6c122:backend/app/db/workflows.py | md5sum → 2373680e88132362a0a2ffe665b8ac05
```

⇒ **`193.2-03` may use EITHER SHA for F-1 and get the same source.** The plan's STOP condition is
satisfied in substance: nothing between the two commits touches a source file.

### ⚠ The wrong-base assertion FIRED, for the 19th time project-wide

This worktree arrived at `fda79214`, **not** the dispatched base, and was corrected by the
`<worktree_branch_check>` reset before any measurement was taken. Branch:
`worktree-agent-a07cc84b1d9cb8ac2` (in-namespace). Recorded because a baseline captured on the
wrong tree proves nothing, and because the running count is the evidence that this assertion is
load-bearing rather than ceremonial.

### F-1 input for plan 193.2-03 — `backend/app/db/workflows.py` at HEAD

| Measurement | Command | Value |
|---|---|---|
| Byte length | `git show HEAD:backend/app/db/workflows.py \| wc -c` | **67205** |
| `ORDER BY name` occurrences | `git show HEAD:backend/app/db/workflows.py \| grep -c "ORDER BY name"` | **4** |

### ⚠ CORRECTION C-2 — the `ORDER BY name` count is 4, not the 3 this plan's acceptance criterion asserts

`193.2-01-PLAN.md` acceptance criterion states: *"§1 records
`git show HEAD:backend/app/db/workflows.py | grep -c "ORDER BY name"` and the number is `3`"*.
**Measured, it is `4`.** Both values are recorded and both are defensible, because they count
different things:

```
git show HEAD:backend/app/db/workflows.py | grep -n "ORDER BY name"
  316:    sql += " ORDER BY name"                ← CODE — list_published_workflows
  344:    # widens; the predicate does NOT — the three-clause WHERE and the ``ORDER BY name``
                                                 ← PROSE — a docblock in list_starter_workflows
  352:        "ORDER BY name"                    ← CODE — list_starter_workflows
  553:        f"ORDER BY name",                  ← CODE — list_draft_workflows
```

- **3** is the number of **call sites** — the three feeds CONTEXT D-16 names at `:316`, `:352`,
  `:553`. That number is correct and is what the phase is about.
- **4** is what the raw `grep -c` returns, because a **docblock at `:344` quotes the clause**.

⚠ **This is the 187-24 trap, on its seventh recorded instance in this repository, and plan
`193.2-03` must not walk into it.** A module that DOCUMENTS a clause reds a raw grep for it. F-1 is
specified in VALIDATION as *"published + draft sources contain `ORDER BY updated_at DESC` and **not**
`ORDER BY name`"* — and `_starter_source()` is the accessor whose docblock carries the prose hit.
**F-1 must be verified over non-comment CODE lines**, exactly as `rowIdentity.test.ts:99` and
`libraryFilter.test.ts:461-468` already do, or it will fail against a comment rather than a clause.

---

## §2 — Backend baselines

Run from `backend/` with `./venv/Scripts/python.exe -m pytest`.

### Full unit suite

```
./venv/Scripts/python.exe -m pytest tests/unit -q
→ 62 failed, 2092 passed, 2 xfailed, 2 xpassed, 34 warnings in 36.84s
```

| Metric | Value |
|---|---|
| failed | **62** |
| passed | **2092** |
| xfailed | **2** |
| xpassed | **2** |
| pytest wall time | **36.84 s** |
| shell wall time | 47.7 s |

✅ Reproduces VALIDATION §*Measured baselines at HEAD* exactly (`62 · 2092 · 2 · 2`).

### The six workflow-touching suites

```
./venv/Scripts/python.exe -m pytest \
  tests/unit/test_workflows_updated_at.py tests/unit/test_publish_service.py \
  tests/unit/test_starter_workflows.py tests/unit/test_103_draft_crud.py \
  tests/unit/test_published_workflow_ownership.py tests/unit/test_187_route_assigned_reach.py -q
→ 81 passed, 1 warning in 1.07s
```

| Metric | Value |
|---|---|
| passed | **81** |
| **failed** | **0** |

✅ Reproduces VALIDATION exactly (81 passed · 0 failed).

`test_workflows_updated_at.py` alone (the F-1 / F-2 file): **23 passed · 0 failed**.

---

## §3 — Frontend baselines

### The five workflow suites

```
cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run \
  src/pages/WorkflowsPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx \
  src/pages/WorkflowBuilderPage.header.test.tsx \
  src/components/workflows/library/libraryFilter.test.ts \
  src/components/workflows/library/rowIdentity.test.ts
→ Test Files 5 passed (5) · Tests 337 passed (337) · Duration 47.47s
```

| Metric | Value |
|---|---|
| files passed | **5 / 5** |
| tests passed | **337** |
| **failed** | **0** |

✅ Reproduces VALIDATION exactly (337 passed · 0 failed).

### The count gate — THE gate

```
GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs
```

Verdict line, **verbatim**:

```
  total                                      3868    3892     +24
  total 3892  ·  failed 0  ·  pinned total 3868
--------------------------------------------------------------
count gate OK — 75/75 pinned files present, no per-file decrease, 0 failing.
```

| Metric | Value |
|---|---|
| total | **3892** |
| failed | **0** |
| pinned total | **3868** |
| files present | **75 / 75** |
| verdict | **`count gate OK`** |

✅ Reproduces VALIDATION exactly (total 3892 · failed 0 · pinned 3868 · 75/75).

### Typecheck

```
cd frontend && npx tsc --noEmit -p tsconfig.app.json
→ 33 errors
```

| Metric | Value |
|---|---|
| `error TS` lines | **33** |

This is the long-standing project baseline (the same 33 the `CLAUDE.md` ledger quotes at 188.1,
188.2 and 192). **A plan that measures 34 has introduced one; a plan that measures 33 has not.**

⚠ **A BARE `tsc --noEmit` CHECKS ZERO FILES AND IS NOT THE GATE.** `-p tsconfig.app.json` is
mandatory. This is recorded in `CLAUDE.md`, in VALIDATION §*Test Infrastructure*, and here, because
a bare invocation returns clean and reads exactly like a pass.

---

## §4 — The two corrections a later plan must not mistake for its own breakage

### (a) The count gate moved 3604 → 3892 because the PINNED SET GREW, not because anything broke

`CLAUDE.md` §*Parallel execution* records the count gate at **3604** (measured by quick task
`260814-q5r` on 2026-08-14). **Measured today: 3892** — `+288` in one day, with `failed 0` and
`75/75` pinned files present on both readings. Phase 193.1 shipped eleven plans between those two
measurements and every one of them added cases.

**A plan that sees 3892 has seen the correct current total.** The gate's own contract is
*no per-file DECREASE* and *zero failing* — not a fixed grand total — so a growing number is the
gate working, not the gate breaking.

### (b) The pre-existing failures OUTSIDE the gate — and this figure is CORRECTED

VALIDATION states: *"Frontend full (267 files) — **26 failed** · 5595 passed — all 26 are the
pre-existing SEED-056 rot set, outside the gate"* and the backend full unit suite carries **62**.

**The backend 62 reproduces exactly. The frontend 26 does NOT, and the corrected measurement is
recorded beside it rather than over it:**

| Run | Command | Result |
|---|---|---|
| VALIDATION (quoted) | full frontend tree | 26 failed · 5595 passed |
| **This plan, run 1** | `GSD_VITEST_MAX_WORKERS=2 npx vitest run` | **49 failed · 5572 passed · 12 files failed / 267** |
| **This plan, run 2** | identical command, identical commit | **46 failed** |

⚠ **THE FULL-TREE FIGURE IS NON-DETERMINISTIC AT CAP 2 — 49, then 46, on ONE identical commit
(`c1a6c122`), with no source file touched between the runs.** That is precisely what 193.1 measured
(`failed 3, then 0, 0, 0` on one tree) and what `CLAUDE.md` warns about: *"Even cap 2 is no longer
fully deterministic. CAPTURE FAILING FILENAMES BEFORE RE-RUNNING."* The failing filenames were
captured before the second run and are recorded below.

⚠ **AND THE FAILING SET INCLUDES SUITES THAT PASS IN ISOLATION — which is the whole reason §4's
rule exists.** `src/pages/WorkflowsPage.test.tsx` (12 failing cases) and
`src/components/workflows/library/WorkflowCard.test.tsx` (4+) both appear in the full-tree failure
list, yet `WorkflowsPage.test.tsx` is one of the FIVE named workflow suites that measured
**337 passed / 0 failed** in §3 minutes earlier, and both files are inside the count gate, which
measured **`failed 0`**. The full-tree failures are oversubscription artifacts, not defects.

Failing files, run 2 (46 failed cases across 12 files):

```
src/pages/WorkflowsPage.test.tsx                       (12)   ← PASSES in isolation (§3)
src/__tests__/providers/streamsProvider.test.tsx       (10)
src/components/workflows/library/WorkflowCard.test.tsx  (4+)  ← inside the count gate (failed 0)
src/__tests__/components/IngestionPage.test.tsx          (4)
src/__tests__/providers/StreamsProvider.dedup.test.ts    (2)
src/lib/model-info.test.ts                               (1)
src/components/workflows/PublishGauntlet.test.tsx        (1)
src/__tests__/components/MessageItem.test.tsx            (1)
src/__tests__/components/Plan04.frontend.test.tsx        (1)
src/__tests__/hooks/useMessages.test.ts                  (1)
src/__tests__/providers/streamsProvider_075_9_clientkey.test.tsx (1)
+ 1 further file
```

### ⇒ THE RULE, STATED PLAINLY FOR EVERY PLAN IN THIS PHASE

**Gate on the count gate PLUS the named suites. NEVER on a bare full-suite run.**

- **No plan in this phase touches any of the 62 backend or 46/49 frontend pre-existing failures.**
- The frontend full-tree number is not a baseline at all — it does not hold still across two runs
  of the same commit, so it can neither pass nor fail a plan.
- A plan reporting a full-tree failure count as evidence of its own breakage is reporting noise.
  The authoritative readings are: **count gate `failed 0`** · **the five frontend suites `0 failed`**
  · **the six backend workflow suites `0 failed`** · **`tsc -p tsconfig.app.json` = 33**.

---

## §5 — Guardrail record (D-01 / D-04)

### D-01 — a G-5 override was OFFERED AND DECLINED; this phase records **NO guardrail override**

Phase 193.2 is the **third consecutive phase** (193, 193.1, 193.2) to be offered a G-5 waiver and
decline it. **`.planning/STATE.md` records NO guardrail override for Phase 193.2, and that absence
is a measurement, not an omission** — verified at HEAD, `STATE.md:51`:

> ⚠ **NO GUARDRAIL OVERRIDE IS RECORDED FOR PHASE 193.2, AND THAT ABSENCE IS A MEASUREMENT (D-01).**
> It is the third consecutive phase (193, 193.1, 193.2) to be offered one and decline it. G-5 is
> honoured **by construction** on all seven hot files, each carrying the D-02 no-second-concern
> argument in its plan.

⚠ **THREE OF THESE FILES ARE ABSENT FROM THE `CLAUDE.md` HOT-FILE LEDGER, so G-5 has never fired on
them once** — the identical invisibility failure `WorkflowsPage.tsx` suffered for ten phases,
`WorkflowDoorSwitch.tsx` for six and `WorkflowBuilderPage.tsx` for ten. **Hotness below was DERIVED
with `git log`, never read off the table**, precisely because the table cannot see what is missing
from it. Writing the missing rows is a phase DELIVERABLE (D-03), not a nicety.

#### The seven files — re-derived at HEAD `c1a6c122`

Commands, per file:
```
git log --oneline -- <file> | wc -l                                        # commits
wc -l <file>                                                               # lines
git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' \
                             | sed -E 's/-.*//' | sort -u                  # buckets
```

⚠ **THE BUCKET RECIPE RETURNS QUICK-TASK BUCKETS ALONGSIDE REAL PHASES, AND THEY ARE COUNTED OUT.**
`quick`, `260809` and `260814` are quick tasks, not phases. Every phase count below is
`buckets − quick-task buckets`, and the subtraction is shown so the next reader is not surprised by
their own command's output — exactly as the `WorkflowBuilderPage.tsx` ledger row already documents
about itself.

| # | File | commits / phases / lines | Buckets returned | Non-phase buckets removed | On the `CLAUDE.md` ledger? |
|---|---|---|---|---|---|
| 1 | `backend/app/db/workflows.py` | **30 / 16 / 1296** | `091 092 098 101.1 102 103 143 152 163 165 185 186 189 190 192 192.1` | none | ❌ **ABSENT** |
| 2 | `backend/app/services/harness/publish_service.py` | **16 / 6 / 1047** | `102 163 182 186 189 190 quick` | `quick` (1) | ❌ **ABSENT** |
| 3 | `backend/app/services/workflow_authoring.py` | **9 / 5 / 389** | `103 165 182 187 189` | none | ❌ **ABSENT** |
| 4 | `backend/app/models/harness.py` | **16 / 15 / 578** | `090 091 093 098 099 100 101 101.1 102 103 143 185 187 189 190` | none | ❌ **ABSENT** |
| 5 | `backend/app/api/workflows.py` | **34 / 17 / 1951** | `092 098 102 103 143 148 152 163 165 182 185 186 187 192 192.1 193 193.1 260814 quick` | `260814`, `quick` (2) | ✅ fires; obligation inherited |
| 6 | `frontend/src/pages/WorkflowsPage.tsx` | **34 / 12 / 1176** | `103 124 143 152 155 165 184 184.1 186 188 192 192.1` | none | ✅ — ⚠ cell **STALE** (see C-3) |
| 7 | `frontend/src/pages/WorkflowBuilderPage.tsx` | **40 / 11 / 2252** | `103 124 183 184 184.1 185 186 187 190 193 193.1 260809 260814` | `260809`, `260814` (2) | ✅ fires; obligation inherited |

#### Agreements and corrections against CONTEXT D-01 / RESEARCH / the ledger

**Agreements (measured, not assumed) — five of the six figures in CONTEXT D-01's table reproduce
EXACTLY:** `db/workflows.py` 30/16/1296 · `publish_service.py` 16/6/1047 ·
`workflow_authoring.py` 9/5/389 · `api/workflows.py` 34/17/1951 · `WorkflowsPage.tsx` 34/12/1176.

**⚠ CORRECTION C-3 — the `WorkflowsPage.tsx` ledger cell is STALE for the FOURTH time.**
The `CLAUDE.md` cell reads **`33 / 12 / 1160`**; measured at HEAD it is **`34 / 12 / 1176`**.
CONTEXT D-01 already flagged this and the measurement CONFIRMS D-01 rather than correcting it.
Recorded beside the previous value, per the table's habit. The **phase** count is unchanged at 12.

**⚠ CORRECTION C-4 — the seventh file. CONTEXT D-01's table lists SIX files; there are SEVEN.**
`backend/app/models/harness.py` (**16 / 15 / 578**) is absent from CONTEXT D-01's table AND absent
from the `CLAUDE.md` ledger, and at **15 phases** it is the third-hottest backend file measured
here — behind only `phase_types.py` (14 phases on the ledger) and `api/workflows.py` (17). RESEARCH
found it and `STATE.md:8` records *"FOUR ledger rows owed not three — a seventh hot file,
`backend/app/models/harness.py` (16/15/578), was absent from the table."* **This baseline confirms
that figure exactly** and carries it forward so D-03's ledger work covers four rows, not three.

**⚠ CORRECTION C-5 — CONTEXT D-01's table includes `WorkflowCard.tsx` as its sixth row; this
baseline does NOT count it among the seven in-scope files.** It is out of scope by D-04 (below).
CONTEXT D-01 and D-04 are in tension on that point; D-04 is the operative constraint because it is
the one that binds `files_modified`, and the measurement is recorded under D-04 rather than dropped.

**The four ledger rows this phase owes (D-03):** `db/workflows.py`, `publish_service.py`,
`workflow_authoring.py`, `models/harness.py` — each with the measured counts, the re-derive
commands, the G-5 verdict and the named next seam. `db/workflows.py` at **16 phases** is the
second-hottest backend file measured in this phase and the row must say so plainly.

### D-04 — `WorkflowCard.tsx` is OUT OF SCOPE, and that is a scoping constraint rather than an accident

| File | commits / phases / lines | Buckets |
|---|---|---|
| `frontend/src/components/workflows/library/WorkflowCard.tsx` | **8 / 3 / 818** | `192 192.1 193` |

✅ Reproduces the `CLAUDE.md` ledger's inherited figure exactly (`8 / 3 / 818`).

**G-5 FIRES on this file** (3 phases against a threshold of ≥ 3) and its ledger cell obliges the
next phase naming it in `files_modified` to produce a **refactor recommendation as its FIRST
option**, before the feature it came to build.

**D-04's standing scope fence, recorded here as a measurement so no plan can drift into it:**

- **`WorkflowCard.tsx` appears in NO plan's `files_modified` in Phase 193.2.**
- **Its inherited G-5 obligation passes forward UNTOUCHED** — 193.2 neither discharges it nor adds
  to it. The next phase still inherits `8 / 3 / 818` and still owes the recommendation.
- **It is a file to READ only.** Every decision in this phase is reachable without editing the card:
  ordering is server-side (D-15/D-16/D-17), the requirement field lives on the Builder page
  (D-06/D-08) and the refusal lives in `publish_service.py` (D-12/D-13).
- **If a plan finds it MUST edit the card, it owes that refactor recommendation BEFORE the feature.**
  No silent edit. That is the escape hatch and it must be used honestly rather than argued around.

---

## §6 — Reported-bugs cross-check (the `plan-phase` touchpoint)

Both blocking reports carry `folded_into: "193.2"` at HEAD, verified rather than assumed:

```
grep -n folded_into .planning/reported-bugs/template-first-drafts-cannot-be-published.md
  9:folded_into: "193.2"
grep -n folded_into .planning/reported-bugs/cannot-find-a-just-published-workflow.md
  9:folded_into: "193.2"
```

| Bug | File | severity | status | folded_into | Covered by |
|---|---|---|---|---|---|
| **`BUG-260815-01`** | `template-first-drafts-cannot-be-published.md` | `blocking` | `folded` | `"193.2"` | **`193.2-05`** (the prompt clause — `AUTHORING_SYSTEM_PROMPT`'s DELIVERABLE RULE) + **`193.2-06`** (the rewritten refusal message in `_interactive_phase_failures`) |
| **`BUG-260815-02`** | `cannot-find-a-just-published-workflow.md` | `blocking` | `folded` | `"193.2"` | **`193.2-03`** (the server `ORDER BY updated_at DESC` on published + drafts) + **`193.2-04`** (the client half — the rendered merge position) |

Objectives confirmed by reading each plan rather than inferring from the numbering:

- `193.2-03` — *"`BUG-260815-02` (blocking): a just-published workflow is unfindable … Change the
  two feeds that hold the author's own work to `ORDER BY updated_at DESC`; leave the curated
  starters shelf alphabetical."*
- `193.2-04` — *"SC#3's client half … The server change in plan 193.2-03 orders the published feed
  by recency; **that is not the same as the newest row appearing at the top of the rendered
  library.**"*
- `193.2-05` — *"Two of this phase's three findings are one Python string literal. `SEED-163`: the
  emit tool **already advertises** `business_requirement` …"*
- `193.2-06` — *"`BUG-260815-01` (blocking), the half that ships as words. Today the refusal reads
  *'interactive phases (llm_human_input / ask_user dispositions) cannot be validated in a
  synchronous publish'* — two internal identifiers …"*

✅ Every report with `folded_into: 193.2` is addressed by at least one plan task. The touchpoint the
project rules require is satisfied.

---

## §7 — The two shipped fences, DRIVEN RED against real plants

> ⚠ **BOTH PROBES WERE RUN ON THE UNMOVED TREE, IN A PLAN THAT MODIFIES NO SOURCE FILE, AND THAT
> ORDERING IS LOAD-BEARING RATHER THAN TIDY.** The point is that these fences are shown to SEE
> something BEFORE this phase changes anything they watch — so a later green reading cannot be
> confused with a fence that was silently blind. 193.1 found **FOUR** fences that could not fire;
> all four were caught by PLANTING and **none by reading**. F-2 and F-7 are inherited fences this
> phase's correctness rests on, so they were planted here, before there was any change to blame.

Both plants are temporary probes, restored byte-identically before the commit. **No test file was
edited by this plan, and neither fence was weakened.**

### Probe A — F-2 (the three predicate byte-identity cases) CAN FIRE

**The plant.** One character removed from a `WHERE` clause in `list_published_workflows`
(`backend/app/db/workflows.py:290`) — the space before `=`:

```diff
-            "WHERE status = 'published' AND created_by = $1"
+            "WHERE status = 'published' AND created_by= $1"
```

**RED observed.**
```
cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py -q
```
```
    def test_published_predicates_are_byte_identical_to_what_shipped(predicate):
        """T-192.1-01. Widening one of these is the mig-116 / CR-01 shape, not a projection."""
>       assert predicate in _published_source()
E       assert "status = 'published' AND created_by = $1" in 'async def list_published_workflows(...)'
E        +  where '...' = _published_source()

tests\unit\test_workflows_updated_at.py:295: AssertionError
=========================== short test summary info ===========================
FAILED tests/unit/test_workflows_updated_at.py::test_published_predicates_are_byte_identical_to_what_shipped[status = 'published' AND created_by = $1]
1 failed, 22 passed, 1 warning in 0.66s
```

| Property | Value |
|---|---|
| Failing test id | **`test_published_predicates_are_byte_identical_to_what_shipped[status = 'published' AND created_by = $1]`** |
| Assertion line | `tests/unit/test_workflows_updated_at.py:295` — `assert predicate in _published_source()` |
| Result under plant | **1 failed · 22 passed** |
| Parametrized arm that fired | the **first** of two (`status = 'published' AND created_by = $1`); the `(is_system_global = true OR created_by = $1)` arm correctly stayed green — the plant touched only one branch |

⚠ **NAMING CORRECTION C-6, confirmed by observation:** the case is **plural and parametrized** —
`test_published_predicates_are_byte_identical_to_what_shipped` (`:286-295`, two arms), NOT the
singular `*_predicate_is_byte_identical_*` shape that VALIDATION's F-2 row and the fence table use.
Its two siblings ARE singular: `test_starter_predicate_is_byte_identical_to_what_shipped` (`:298`)
and `test_draft_predicate_is_byte_identical_to_what_shipped` (`:305`). So "the three
`*_predicate_is_byte_identical_*` cases" matches only two of the three by name. This is the
plan's own correction C-1, verified against the observed failure id rather than against the file.

**GREEN after restore.**
```
git checkout -- backend/app/db/workflows.py
git diff --numstat -- backend/app/db/workflows.py   → EMPTY
cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py -q
→ 23 passed, 1 warning in 0.18s
```

✅ **F-2 can fire.** RED (1 failed / 22 passed) → restore → GREEN (23 passed) → diff EMPTY.

### Probe B — F-7 (`rowIdentity.test.ts` T-7 / P-4, no client sort) CAN FIRE

**The plant.** A dead local carrying the exact forbidden shape the fence's needle is written to
catch, inside `buildIdentityIndex` in
`frontend/src/components/workflows/library/rowIdentity.ts:370` — a non-comment CODE line, so the
module's comment stripper (`:99`) cannot hide it:

```diff
+  const shown = rows.sort((a, b) => a.name.localeCompare(b.name))
+  void shown
+
   // ── pass 1 — O(n). Every lookup a Map, every key a user-influenced string (D-32).
   const bySlug = new Map<string, LibraryRow[]>()
```

**RED observed.**
```
cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run \
  src/components/workflows/library/rowIdentity.test.ts
```
```
 FAIL  src/components/workflows/library/rowIdentity.test.ts > the module's own source keeps the
       promises no test can see > T-7 / P-4 — the ROW LIST is never re-ordered, and every sort
       says what it sorts

AssertionError: expected '  const shown = rows.sort((a, b) => a…' not to match
                         /\brows\.sort\(|\bfamily\.sort\(|\blis…/

- Expected:  /\brows\.sort\(|\bfamily\.sort\(|\blist\.sort\(/
+ Received:  "  const shown = rows.sort((a, b) => a.name.localeCompare(b.name))"

 ❯ src/components/workflows/library/rowIdentity.test.ts:175:24
    173|     expect(sorts.length).toBeGreaterThan(0) // non-vacuity: there ARE sorts to judge
    174|     for (const line of sorts) {
    175|       expect(line).not.toMatch(/\brows\.sort\(|\bfamily\.sort\(|\blist…
       |                        ^

 Test Files  1 failed (1)
      Tests  1 failed | 71 passed (72)
```

| Property | Value |
|---|---|
| Failing case | **`T-7 / P-4 — the ROW LIST is never re-ordered, and every sort says what it sorts`** |
| Suite | `rowIdentity.test.ts > the module's own source keeps the promises no test can see` |
| Assertion line | `rowIdentity.test.ts:175:24` — `expect(line).not.toMatch(/\brows\.sort\(\|\bfamily\.sort\(\|\blist\.sort\(/)` |
| Result under plant | **1 failed · 71 passed (72)** |

**GREEN after restore.**
```
git checkout -- frontend/src/components/workflows/library/rowIdentity.ts
git diff --numstat -- frontend/src/components/workflows/library/rowIdentity.ts   → EMPTY
cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run \
  src/components/workflows/library/rowIdentity.test.ts
→ Test Files 1 passed (1) · Tests 72 passed (72)
```

✅ **F-7 can fire.** RED (1 failed / 71 passed) → restore → GREEN (72 passed) → diff EMPTY.

⚠ **D-17 therefore stands on a fence that has been SHOWN to see something.** Server `ORDER BY` is
the sole authority for row order, and the three-call-site SQL change in `193.2-03` is sufficient
**only while this fence stays green**. Any plan adding client-side ordering breaks it — and it has
now been proved that it would break it loudly rather than silently.

### Post-probe tree assertion

```
git diff --numstat HEAD -- backend frontend   → EMPTY
git status --porcelain                        → EMPTY
```

⚠ One incidental artifact is recorded rather than hidden: the full-tree vitest run in §4 rewrote
`frontend/src/components/workflows/__snapshots__/canvasModel.fixtures.test.ts.snap` with **CRLF
line endings only** (content-identical — `git diff --numstat` never saw it; only
`git status --porcelain` did). It was restored with `git checkout --` on that single file. **No
blanket `git clean`, `git reset --hard` or `git checkout -- .` was used at any point** — per the
destructive-git prohibition, each restore named its one file.

---

## Summary — what a later plan may rely on

| Baseline | Value | Authority |
|---|---|---|
| Base SHA | `c1a6c122` (`3cfafa0d` is an ancestor; `db/workflows.py` md5-identical at both) | §1 |
| `backend/tests/unit` full | 62 failed · 2092 passed · 2 xfailed · 2 xpassed | §2 |
| Six backend workflow suites | **0 failed** · 81 passed | §2 |
| Five frontend workflow suites | **0 failed** · 337 passed | §3 |
| Count gate | **`count gate OK`** · total 3892 · failed 0 · pinned 3868 · 75/75 | §3 |
| `tsc -p tsconfig.app.json` | **33 errors** | §3 |
| Frontend full tree | 49 then 46 — **non-deterministic, NOT a gate** | §4 |
| Guardrail overrides for 193.2 | **NONE** — offered and declined, 3rd consecutive phase | §5 |
| Ledger rows owed (D-03) | **FOUR** (`db/workflows.py`, `publish_service.py`, `workflow_authoring.py`, `models/harness.py`) | §5 |
| `WorkflowCard.tsx` | **out of scope** (D-04); 8 / 3 / 818 inherited untouched | §5 |
| F-2 | **driven RED against a real plant, restored** | §7 |
| F-7 | **driven RED against a real plant, restored** | §7 |

*Captured 2026-08-15 · Phase 193.2 Plan 01 · tree UNMOVED · zero source files modified*
