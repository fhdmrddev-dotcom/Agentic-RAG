# Phase 193.2 — UAT Scoreboard

**Opened:** 2026-08-15 by plan `193.2-10` (Wave 6, close-out)
**Phase goal:** *everything between "the AI wrote my workflow" and "I can run it and find it again."*
**Requirement touched:** `AUTH-03` (already satisfied at 193.1's close — this phase must not regress it)

---

## The rule this file runs by

**A row may be recorded ⛔ / ⏸ with its reason, but NEVER silently omitted.** A scoreboard that lists
only what passed is not a scoreboard. Phase 193 closed owing rows U1/U2 and said so plainly; 193.1
closed owing U2/U3/U4 **by decision**. That is legitimate. Quietly dropping a row is not.

**A PASS is never inferred from another row's evidence.** Each of U1's five moments is recorded
separately, so a partial pass is legible rather than averaged away.

⚠ **THE ONE RULE THAT BINDS EVERY NUMBER IN THIS FILE (D-08), stated before any of them:**

> *"Baseline (pre-fix, from `BUG-260815-01` + `193.1-11-SUMMARY.md`): `business_requirement` 0/N;
> `llm_human_input` 2/2 with a template, 4/6 overall. Post-fix measured: k/N.
> The claim is a reduction, not an absence — the publish gate stays because a prompt cannot guarantee absence."*

The `SEED-163` and `BUG-260815-01` fixes are **prompt-level and therefore non-deterministic**. Nothing
in this file, and nothing anywhere in this phase, may be read as a claim that the requirement is
*always* populated or that the interactive step *never* appears. One more generation can produce
either; when it does, the fallback is **today's exact behaviour**, and the publish gate is what makes
that survivable.

⚠ **Test-data warning, inherited from `193.1-UAT.md` and still true.** Three of the operator's own
workflows bind **SEED-prefixed** templates and correctly render *"could not read"*. A row driven
against one of those measures the seed data, not the feature. **Use the kit.**

---

## Cross-provider roster — the D-20 statement, all EIGHT rows carried

**The rule.** `CLAUDE.md` §*UAT scoreboard recipe* requires the **full native roster (7) plus
OpenRouter** for any phase touching streaming, the agent loop, provider routing or UI state, and
requires that a blocked row be recorded ⛔ with its reason rather than dropped.

**Why it is narrowed here, and this is a measurement rather than a shortcut.** The roster rule is
written for the **chat** path, where a per-request `model`/`provider` reaches the composer. **The
authoring path is not that path.** `resolve_authoring_model` (`workflow_authoring.py`) returns
`settings.harness_authoring_model`, else the first of `("claude-opus-4-8", "gpt-5.5")` whose registry
entry carries a truthy `forced_emission` — its own docstring says *"NOT the composer's selected
model"*. `harness_authoring_model` has **no `app_settings` column, no Settings UI and needs a backend
restart**; it measured `None` in this environment. Scoring the other six would measure a
configuration that does not ship — the exact error the roster rule itself warns about.

**Two rows driven, six recorded N/A.** The two were driven as **20 real paid generations** by plan
`193.2-08`; the full board, the per-arm figures and the reasoning live in
**`193.2-FREQUENCY.md` §2–§3**, which this file **cites rather than re-derives**.

| # | Provider | Model id | Verdict | Reason / result |
|---|---|---|---|---|
| 1 | **anthropic** | `claude-opus-4-8` | ✅ **DRIVEN** | ×10 (both arms). What this environment actually resolves |
| 2 | **openai** | `gpt-5.5` | ✅ **DRIVEN** | ×10 (both arms). The declared fallback, reached through the harness's explicit per-row stub |
| 3 | google | — | ⛔ N/A | Not a `resolve_authoring_model` candidate; the knob is env-only, has no UI and needs a restart |
| 4 | deepseek | — | ⛔ N/A | Same. (`strict_json_schema` is also inert — D-122-04) |
| 5 | zhipu / GLM | — | ⛔ N/A | Same |
| 6 | minimax | — | ⛔ N/A | Same |
| 7 | moonshot / kimi | — | ⛔ N/A | Same. **Plus a real finding that is NOT this phase's:** branch 1 of `resolve_authoring_model` returns `harness_authoring_model` **without checking `forced_emission` / `emit_tier`**, so an explicitly-set moonshot model hands an **unforceable** model (`emit_tier: coerce`) to `forced_emit`. Recorded at `test_187_authoring_roster.py:44-52`, adjacent to open bug `BUG-260731-01`. 193.2 changes nothing on that path |
| 8 | openrouter | — | ⛔ N/A | Same; `native_tools: False` — the non-native tool path |

⚠ **Every one of the eight provider keys is configured in this environment.** The six ⛔ rows are
therefore blocked **by the RESOLVER, not by a missing key** — the stronger and more honest reason,
stated so they cannot later read as an environment gap.

**The measured board, CITED from `193.2-FREQUENCY.md` §3 (N = 5 per row per arm; 20 generations, 20
successes, 0 failed calls):**

| Figure | anth `kit10` | anth `uat8` | oai `kit10` | oai `uat8` | Pre-fix baseline |
|---|---|---|---|---|---|
| `business_requirement` non-empty (SC#1) | **5/5** | **5/5** | **5/5** | **5/5** | **0/N** |
| `llm_human_input` present (SC#2) | **0/5** | **0/5** | **0/5** | **0/5** | **2/2** w/ a template; **4/6** overall |
| `render_template` present (**SC#4 control**) | **5/5** | **5/5** | **5/5** | **5/5** | 193.1's **3/3**, like-for-like on `uat8` |
| `external_action` present (displacement) | **0/5** | **0/5** | **0/5** | **0/5** | not measured pre-fix |

⚠ **A MEASURED REDUCTION, NEVER AN ABSENCE — the D-08 sentence above binds every cell in that table.**
⚠ **The counters were PLANTED before their zeroes were published**, and the positive control ships
permanently in the harness — a zero from a blind counter and a zero the model earned are
indistinguishable in an artifact.
⚠ **The SC#4 control did NOT fall** — 5/5 on the like-for-like `uat8` arm against 193.1's 3/3, on the
same eight keys with the same describe. **But it counts a `render_template` PHASE IN A DRAFT. It does
not prove a template gets FILLED.** Only **U1** can say that, which is precisely why U1 exists.

---

## Rows

| Row | What must be TRUE | OQ | Driven by | Verdict |
|---|---|---|---|---|
| **U1** | **SC#4 + SC#1 + SC#2 + SC#3 in one sitting.** Re-running the 193.1 UAT kit end to end produces a filled template scoring **8+ planted facts**, with the requirement pre-filled and marked, the draft published **without a canvas edit**, and the workflow findable without searching | D-21 | **operator** | ⏸ **OWED** — see the five moments below |
| **U2** | The rendered refusal on a draft carrying an interactive step names the step **by the label on the canvas**, the `publish-trigger` is **disabled**, and **no `publish_blocked` row is written** (the refusal comes from the greyed control, not from a 400) | OQ-1 | **operator** + a DB read | ⏸ **OWED** (its DB half has a pre-run baseline, below — that is a baseline, **not a verdict**) |
| **U3** | The post-publish Run CTA appears, and how long after the click | OQ-2 | **operator** | ⏸ **OWED** |
| **U4** | The rendered position of the just-published workflow in the library, observed **without searching**. Measured prediction: **4 of 109** | OQ-5 | **operator** | ⏸ **OWED** — ⚠ **this row asks the operator's JUDGEMENT and asserts no pass** |
| **U5** | The composer's Harness workflow picker still reads sensibly under the new recency ordering | OQ-6 | **operator** | ⏸ **OWED** |

⚠ **Every row is OWED, and the reason is structural rather than an oversight: all five need a browser
and a running backend on the operator's machine.** The assistant cannot drive them, and scoring them
from an agent that has read the source would be the check-that-cannot-fail this file exists to
prevent. **Nothing below may be recorded as a pass until the operator drives it.**

---

## U1 — D-21, the single end-to-end run. **VERDICT: ⏸ OWED.**

**Why no test can stand in for it.** 193.1 measured that a 3,600-case suite could not see the
assumption that mattered — the model needed to be told a template was **PROVIDED**, not merely handed
its placeholder names, and 0 of 3 runs produced `render_template` until that changed. A
`render_template` pin does not prove a template gets FILLED. **U1 is also the only row that exercises
all three of this phase's fixes together, in the order a person meets them.**

**The instrument.** The Phase 193.1 UAT kit at `C:\Users\fhdmr\Desktop\uat-193.1-qbr\` — its
five-document knowledge base and its ten-field `.docx`. **Scoring rule: 8+ planted facts.**
193.1's own U5 scored **11 of 13** with all ten template fields rendered, zero unrendered `{{ }}`,
zero literal `None` and branding intact; that is the bar this run must not fall below.

**The five moments, each recorded SEPARATELY so a partial pass is legible:**

| # | Moment | What to look for | Verdict |
|---|---|---|---|
| 1 | **The requirement arrives pre-filled and visibly marked** | The business-requirement field is populated before you type anything, and carries the mark **`AI-proposed`** (a small bordered mono label; its `title` reads *"The generator proposed this line when this workflow was created. Edit it and this mark clears."*). It is editable, and **editing it clears the mark** | ⏸ owed |
| 2 | **The draft publishes without a canvas edit** | Press Publish **without touching the canvas**. If it refuses, **copy the refusal verbatim** — it must name a step by the label you can read on the canvas and tell you what to do, and it must NOT contain `llm_human_input` or `ask_user` | ⏸ owed |
| 3 | **The workflow is findable without searching and without knowing its name** | Go to the library. Record the row's **rendered index** and whether the post-publish Run CTA appeared naming it | ⏸ owed |
| 4 | **The run fills the bound template** | Run it. All ten fields rendered; no unrendered `{{ }}`; no literal `None`; branding intact | ⏸ owed |
| 5 | **The deliverable is grounded** | Score the produced document against the kit's planted facts. **8+ is a pass** | ⏸ owed |

⚠ **A partial pass is a legitimate outcome and must be stated as one.** Moment 2 is the one that can
honestly fail without failing the phase: `BUG-260815-01` is **prompt-mitigated, not eliminated**
(0/20 measured, D-08). If a refusal appears, moment 2 fails **and the refusal's WORDS are then the
thing under test** — a refusal that names the visible step and gives an action is 193.2-06 working.

⚠ **What must NOT be pasted into this file** (T-193.2-10-A): knowledge-base content, provider keys,
or the produced document's body. The kit's planted facts are synthetic; the surrounding KB may not
be. Record verdicts, positions, timings and the refusal text.

---

## U2 — OQ-1, the rendered refusal. **VERDICT: ⏸ OWED.**

**What is already settled and must not be re-litigated.** `193.2-02` established from the audit
ledger that **the gate FIRED and the 400 never happened**: on 2026-08-15 there were **0**
`publish_blocked` rows, exactly **1** `publish_attempted` and **1** `publish_succeeded` 7.57 ms
apart. D-10's escape hatch is recorded **NOT TRIGGERED** — both CONTEXT facts are TRUE, and the false
premise was the unstated third one, *that a 400 was received at all*. **The refusal the operator met
came from `blockedReason` beside an already-greyed control.** What the ledger cannot prove is what
pixels were on screen, and that is the whole of this row.

**Method.** Open the Builder on a draft carrying an interactive step. Via Chrome MCP
`evaluate_script` — ⚠ **`take_screenshot` times out on this box; read DOM geometry instead** —
capture:

```js
document.querySelector('[data-testid="publish-blocked-reason"]')?.textContent
document.querySelector('[data-testid="publish-trigger"]')?.disabled
```

Then re-run the audit count and confirm it stays **0**.

**Pre-run BASELINE, measured 2026-08-15 at this plan's HEAD — a baseline, NOT a verdict:**

| Measurement | Value |
|---|---|
| `publish_blocked` rows today | **0** (absent from the grouped result entirely) |
| `publish_attempted` / `publish_succeeded` today | **1** / **1** |
| `publish_blocked` all-time | **32** |
| newest `publish_blocked` anywhere | **2026-08-07 19:39:11** |

⚠ **CORRECTION C-7 applies to anyone extending this query.** `harness_audit.metadata` is a
**DOUBLE-ENCODED jsonb STRING**, so `metadata->>'blocked_stage'` returns **NULL on all 32 rows** by
definition. The working accessor is `metadata #>> '{}'` followed by a JSON parse. The `count(*)`
above is safe because it keys on `event_type`, a real column. *A NULL that reads like "the field is
empty" is indistinguishable from "you asked the wrong way"* — the same class of failure this phase
exists to fix in user-facing copy, appearing in our own diagnostics.

**What the row records when driven:** whether the message names a step the operator can actually see,
whether the control is genuinely disabled, and whether the count stayed 0.

---

## U3 — OQ-2, the CTA latency. **VERDICT: ⏸ OWED.**

Publish from the Builder; timestamp the click and the first appearance of
`[data-testid="run-cta"]`.

**What is already settled.** `193.2-02` ruled **D-19: it fires; SC#3 is met by the sort plus one pin;
NO navigate-to-row is built** — building a second hand-off surface beside a shipped one would be a
genuine second concern on a file carrying an inherited G-5 obligation. ⚠ **The CTA had ZERO automated
coverage from the day it shipped until this phase** (`grep -c "run-cta"` on its suite: **0 → 13**),
and WR-04 had already fixed one silent drop on it that no test would have caught. Its three new cases
pin the refetch race, the name/version hand-off and the draft exclusion, with **presence and absence
driven RED independently** — but a unit pin says nothing about latency on a real network.

**If it never appears, that is a named defect** and this row is the only instrument that would see it.

---

## U4 — OQ-5, the rendered position. **VERDICT: ⏸ OWED. THIS ROW ASKS A JUDGEMENT AND ASSERTS NO PASS.**

After publishing, observe the library **without searching**. Record the row's rendered index.

**The measured prediction is `4 of 109`, and every part of that number is stated rather than rounded:**

- ⚠ **`BUG-260815-02`'s own arithmetic was over the WRONG FEED.** The report's *"129 of 146"* comes
  from a `row_number() over (order by name)` across the **whole** `workflow_definitions` table; the
  page passes **`?scope=mine`**. The true rendered figures are **17 of 109 → 4 of 109**. Both are
  recorded, neither over the other.
- **Position 4 is not position 1, and no `ORDER BY` change can make it 1.** `mergeLibrary`
  concatenates **starters ++ published ++ drafts** client-side, so the newest published row lands at
  index `starters.length`. That index is pinned as an **expression**, never a literal, by
  `libraryFilter.test.ts` — a literal would rot the moment the curated shelf grows by one.
- **The option not taken, with its measured cost:** re-ordering the merge loops (RESEARCH §R2
  reading 2) would put the row at index 0 — and a real plant proved it also **inverts the dedupe
  precedence**, so a row the caller owns would read as a shared starter. That is a separate decision,
  **not** a client sort; D-17's fence forbids client sorts outright and both of its arms were driven
  RED against real plants.

⚠ **Whether position 4 satisfies "findable" is a product judgement, not a measurement, and nothing in
this repository may be read as having answered it.** If the operator says position 1 is required,
that is a scoped decision with the dedupe cost above — and it does not retract the sort, which fixed
17 → 4.

---

## U5 — OQ-6, the composer's Harness picker. **VERDICT: ⏸ OWED.**

Open the composer's Harness workflow picker and say whether the recency ordering reads sensibly.

**Why this row exists at all.** With the default `owned_only=False`, `list_published_workflows` is
**not only** the Workflows-page shelf — it also feeds the **composer's Harness picker**,
**`WorkspacePanel`'s run-soul** and **`threads.py`'s kickoff**. **All three moved from alphabetical to
recency** in `193.2-03`. Measured: no test and no frontend module asserts alphabetical order for any
of them, so nothing broke — but *"nothing asserts it"* and *"it reads well"* are different claims, and
only a person can make the second. **Thirty seconds, and it is the one surface outside the library the
change reaches.**

---

## Tally

**0 driven · 0 pass · 0 fail · 5 OWED.**

⚠ **Closing this phase with owed rows would be a DECISION, never a claim that everything ran.** If it
is taken, **U1 is the row to run first** — it subsumes moments that U3 and U4 also observe, and it is
the only one that can see a regression in `AUTH-03`.

**What is NOT owed, because the code already answers it:** every gate was re-run at HEAD by
`193.2-10` with no NEW failure (backend `tests/unit` **62 failed / 2154 passed** — failures identical
to the pre-change baseline; the six backend workflow suites **110 / 0**; the five frontend workflow
suites **359 / 0**; the count gate **`count gate OK` · total 3918 · failed 0 · 75/75**; `tsc -p
tsconfig.app.json` **33**, unmoved across all ten plans).
