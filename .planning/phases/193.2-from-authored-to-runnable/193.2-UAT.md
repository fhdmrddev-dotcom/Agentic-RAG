# Phase 193.2 — UAT Scoreboard

**Opened:** 2026-08-15 by plan `193.2-10` (Wave 6, close-out)
**Phase goal:** *everything between "the AI wrote my workflow" and "I can run it and find it again."*
**Requirement touched:** `AUTH-03` (already satisfied at 193.1's close — this phase must not regress it)

---

## The rule this file runs by

**A row may be recorded ⛔ / ⏸ with its reason, but NEVER silently omitted.** A scoreboard that lists
only what passed is not a scoreboard. Phase 193 closed owing rows U1/U2 and said so plainly; 193.1
closed owing U2/U3/U4 **by decision**. That is legitimate. Quietly dropping a row is not.

**A PASS is never inferred from another row's evidence.** Each of U1's five moments is recorded
separately, so a partial pass is legible rather than averaged away.

⚠ **THE ONE RULE THAT BINDS EVERY NUMBER IN THIS FILE (D-08), stated before any of them:**

> *"Baseline (pre-fix, from `BUG-260815-01` + `193.1-11-SUMMARY.md`): `business_requirement` 0/N;
> `llm_human_input` 2/2 with a template, 4/6 overall. Post-fix measured: k/N.
> The claim is a reduction, not an absence — the publish gate stays because a prompt cannot guarantee absence."*

The `SEED-163` and `BUG-260815-01` fixes are **prompt-level and therefore non-deterministic**. Nothing
in this file, and nothing anywhere in this phase, may be read as a claim that the requirement is
*always* populated or that the interactive step *never* appears. One more generation can produce
either; when it does, the fallback is **today's exact behaviour**, and the publish gate is what makes
that survivable.

⚠ **Test-data warning, inherited from `193.1-UAT.md` and still true.** Three of the operator's own
workflows bind **SEED-prefixed** templates and correctly render *"could not read"*. A row driven
against one of those measures the seed data, not the feature. **Use the kit.**

---

## Cross-provider roster — the D-20 statement, all EIGHT rows carried

**The rule.** `CLAUDE.md` §*UAT scoreboard recipe* requires the **full native roster (7) plus
OpenRouter** for any phase touching streaming, the agent loop, provider routing or UI state, and
requires that a blocked row be recorded ⛔ with its reason rather than dropped.

**Why it is narrowed here, and this is a measurement rather than a shortcut.** The roster rule is
written for the **chat** path, where a per-request `model`/`provider` reaches the composer. **The
authoring path is not that path.** `resolve_authoring_model` (`workflow_authoring.py`) returns
`settings.harness_authoring_model`, else the first of `("claude-opus-4-8", "gpt-5.5")` whose registry
entry carries a truthy `forced_emission` — its own docstring says *"NOT the composer's selected
model"*. `harness_authoring_model` has **no `app_settings` column, no Settings UI and needs a backend
restart**; it measured `None` in this environment. Scoring the other six would measure a
configuration that does not ship — the exact error the roster rule itself warns about.

**Two rows driven, six recorded N/A.** The two were driven as **20 real paid generations** by plan
`193.2-08`; the full board, the per-arm figures and the reasoning live in
**`193.2-FREQUENCY.md` §2–§3**, which this file **cites rather than re-derives**.

| # | Provider | Model id | Verdict | Reason / result |
|---|---|---|---|---|
| 1 | **anthropic** | `claude-opus-4-8` | ✅ **DRIVEN** | ×10 (both arms). What this environment actually resolves |
| 2 | **openai** | `gpt-5.5` | ✅ **DRIVEN** | ×10 (both arms). The declared fallback, reached through the harness's explicit per-row stub |
| 3 | google | — | ⛔ N/A | Not a `resolve_authoring_model` candidate; the knob is env-only, has no UI and needs a restart |
| 4 | deepseek | — | ⛔ N/A | Same. (`strict_json_schema` is also inert — D-122-04) |
| 5 | zhipu / GLM | — | ⛔ N/A | Same |
| 6 | minimax | — | ⛔ N/A | Same |
| 7 | moonshot / kimi | — | ⛔ N/A | Same. **Plus a real finding that is NOT this phase's:** branch 1 of `resolve_authoring_model` returns `harness_authoring_model` **without checking `forced_emission` / `emit_tier`**, so an explicitly-set moonshot model hands an **unforceable** model (`emit_tier: coerce`) to `forced_emit`. Recorded at `test_187_authoring_roster.py:44-52`, adjacent to open bug `BUG-260731-01`. 193.2 changes nothing on that path |
| 8 | openrouter | — | ⛔ N/A | Same; `native_tools: False` — the non-native tool path |

⚠ **Every one of the eight provider keys is configured in this environment.** The six ⛔ rows are
therefore blocked **by the RESOLVER, not by a missing key** — the stronger and more honest reason,
stated so they cannot later read as an environment gap.

**The measured board, CITED from `193.2-FREQUENCY.md` §3 (N = 5 per row per arm; 20 generations, 20
successes, 0 failed calls):**

| Figure | anth `kit10` | anth `uat8` | oai `kit10` | oai `uat8` | Pre-fix baseline |
|---|---|---|---|---|---|
| `business_requirement` non-empty (SC#1) | **5/5** | **5/5** | **5/5** | **5/5** | **0/N** |
| `llm_human_input` present (SC#2) | **0/5** | **0/5** | **0/5** | **0/5** | **2/2** w/ a template; **4/6** overall |
| `render_template` present (**SC#4 control**) | **5/5** | **5/5** | **5/5** | **5/5** | 193.1's **3/3**, like-for-like on `uat8` |
| `external_action` present (displacement) | **0/5** | **0/5** | **0/5** | **0/5** | not measured pre-fix |

⚠ **A MEASURED REDUCTION, NEVER AN ABSENCE — the D-08 sentence above binds every cell in that table.**
⚠ **The counters were PLANTED before their zeroes were published**, and the positive control ships
permanently in the harness — a zero from a blind counter and a zero the model earned are
indistinguishable in an artifact.
⚠ **The SC#4 control did NOT fall** — 5/5 on the like-for-like `uat8` arm against 193.1's 3/3, on the
same eight keys with the same describe. **But it counts a `render_template` PHASE IN A DRAFT. It does
not prove a template gets FILLED.** Only **U1** can say that, which is precisely why U1 exists.

---

## Rows

| Row | What must be TRUE | OQ | Driven by | Verdict |
|---|---|---|---|---|
| **U1** | **SC#4 + SC#1 + SC#2 + SC#3 in one sitting.** Re-running the 193.1 UAT kit end to end produces a filled template scoring **8+ planted facts**, with the requirement pre-filled and marked, the draft published **without a canvas edit**, and the workflow findable without searching | D-21 | **operator, 2026-08-15** | ✅ **PASS** — 5 moments driven, all 5 pass; see below. ⚠ one honesty caveat on M1 |
| **U2** | The rendered refusal **on a draft carrying an interactive step** names the step **by the label on the canvas**, the `publish-trigger` is **disabled**, and ~~**no `publish_blocked` row is written**~~ → **⚠ CORRECTED, see C-U2 below** | OQ-1 | — | ⏸ **NOT DRIVEN — and it could not be.** ⚠ **No interactive step ever appeared**, so the surface under test never rendered. A *different* refusal was met instead and became `BUG-260815-06`. See below |
| **U3** | The post-publish Run CTA appears, and how long after the click | OQ-2 | **operator, 2026-08-15** | ◐ **PASS on appearance; latency NOT timestamped** |
| **U4** | The rendered position of the just-published workflow in the library, observed **without searching**. Measured prediction: **4 of 109** | OQ-5 | **operator, 2026-08-15** | ✅ **PASS — rendered position 4, exactly as predicted** |
| **U5** | The composer's Harness workflow picker still reads sensibly under the new recency ordering | OQ-6 | **assistant (measured), 2026-08-16** | ✅ **DRIVEN — PASS by measurement.** ⚠ **The picker DOES NOT EXIST**, and 3 of the row's 4 named consumers are order-insensitive or dead. The reorder is user-visible in **exactly one** surface — the library, already driven by U4. See below |

~~**Tally: 3 driven · 2 pass + 1 partial · 0 fail · 2 not driven.**~~ → **AMENDED 2026-08-16 after U5
was driven: 4 driven · 3 pass + 1 partial · 0 fail · 1 not driven (U2 only).** ⚠ **U2 is recorded NOT
DRIVEN rather than passed, and it is not inferred from U1's evidence** — its surface never rendered
because the defect it watches did not occur. **U5 is now a PASS on measurement, not on a look** — and
measuring it proved the row's own premise false; see §U5.

---

## U1 — D-21, the single end-to-end run. **VERDICT: ✅ PASS, 5 of 5 moments.**

**Driven by the operator on 2026-08-15.**

| | Value |
|---|---|
| Run | **`b021c7b0` — COMPLETED**, all 5 phases, **zero gate failures** |
| Definition | `93a86e21`, slug **`northwind-qbr-fa65a43c`** |
| Phase sequence | `gather-usage` → `gather-support` → `gather-commercial` → `synthesize` → `emit-qbr` |

⚠ **`AUTH-03` IS NOW SATISFIED END TO END AND THIS ROW IS THE EVIDENCE:** a template attached **at
authoring time**, and **a real run that FILLS THAT SAME TEMPLATE** — both halves observed on one
sitting. That is the claim 193.1 could make only on unit evidence and deliberately declined to tick.

**Why no test can stand in for it.** 193.1 measured that a 3,600-case suite could not see the
assumption that mattered — the model needed to be told a template was **PROVIDED**, not merely handed
its placeholder names, and 0 of 3 runs produced `render_template` until that changed. A
`render_template` pin does not prove a template gets FILLED. **U1 is also the only row that exercises
all three of this phase's fixes together, in the order a person meets them.**

**The instrument.** The Phase 193.1 UAT kit at `C:\Users\fhdmr\Desktop\uat-193.1-qbr\` — its
five-document knowledge base and its ten-field `.docx`. **Scoring rule: 8+ planted facts.**
193.1's own U5 scored **11 of 13** with all ten template fields rendered, zero unrendered `{{ }}`,
zero literal `None` and branding intact; that is the bar this run must not fall below.

**The six moments, each recorded SEPARATELY so a partial pass is legible rather than averaged away:**

### M1 — the requirement arrives pre-filled and marked. **✅ PASS, with one honesty caveat.**

`business_requirement`, as authored:

> *"Produce a quarterly business review document for a named customer and quarter, filling the QBR
> template from knowledge-base evidence on usage, support, meetings"*

| Property | Observed |
|---|---|
| Populated before the operator typed anything | ✅ |
| **An echo of the describe text?** | ❌ **NO** — so `193.2-07`'s anti-echo predicate **EARNED** the mark rather than defaulting to it |
| `business_requirement_seeded_by_ai` | **`true`** |
| `assets[]` carries the bound template | ✅ — **193.1's auto-bind works** |
| `name_seeded_by_ai` on the phase | `true` |

⚠ **HONESTY CAVEAT, recorded rather than smoothed: the operator confirmed *"I think all pass"*
broadly and did NOT specifically confirm the VISIBLE mark.** So **the DURABLE half is measured and
the VISIBLE half rests on a general confirmation.** This is **not** recorded as a
specifically-observed mark, and `193.2-09`'s own summary already flagged that its mark had had **no
browser UAT** — the contrast of `text-[9px]` against Deep Midnight, and whether *"AI-proposed"* reads
right beside a real sentence, remain operator judgements nobody has made. ⇒ **carried as a residual
in `SEED-163`, not claimed here.**

### M2 — the draft publishes without a canvas edit. **✅ PASS — after three blocked attempts, all environmental.**

**The publish succeeded with the canvas untouched**, which is the whole of `BUG-260815-01`'s ask.

⚠ **Three earlier attempts were blocked (`0f04d659`, `cc4f0e6c`, `d8f87920`) and NONE is a product
defect on the publish path.** Measured cause: the **OpenAI credit balance was exhausted**
(`429 insufficient_quota`, `credit_balance_exhausted`, confirmed by a live `embeddings.create`).
Every document is embedded with `text-embedding-3-small`, so **every search must embed its query**;
with no credits retrieval returned **0 sources**, `citations_required` failed **3× per run**, the
golden run failed, and publish **correctly refused**. **The gauntlet behaved exactly as designed.**

Ruled out **by measurement**, not by assumption: 5 docs / 18 chunks / **0 null embeddings** /
matching `org_id` / the definition byte-comparable to the one that worked at 02:07.
⚠ Attempt 2 additionally bound the **wrong KB folder** (*Project Meridian — Risks*, 3 docs, no
Northwind) — incidental; it would have failed anyway.

**The audit ledger, re-measured after the session** (using the **C-7 accessor** `metadata #>> '{}'`,
which is exactly why the stage is readable at all — `->>'blocked_stage'` would have returned NULL):

| | Value |
|---|---|
| `publish_blocked` today | **3** — at 14:02:21, 14:07:42, 14:10:29, **`blocked_stage: structural_gate` on all three** |
| `publish_attempted` / `publish_succeeded` today | **5** / **2** |
| `publish_blocked` all-time | **35** (was 32 before the session) |

⚠ **This is the first `publish_blocked` row since 2026-08-07, and it CONFIRMS rather than contradicts
`193.2-02`'s D-10 finding.** That finding was that the operator's original refusal produced **no**
`publish_blocked` row and therefore came from the greyed control, not from a 400. These three rows
came from a **different gate** (`structural_gate`, a failed golden run), reached only because the
operator got **past** the interactive-phase question entirely.

### M3 — findable without searching. **✅ PASS — rendered position 4, exactly as predicted.**

| | Value |
|---|---|
| Rendered position | **4 of 112** — 3 starters + 29 published + 80 drafts |
| Predicted | **4** (index `starters.length`) — **the prediction held exactly** |
| First among the operator's own work | ✅ |
| Post-publish **Run CTA** | ✅ **appeared and NAMED the workflow** |

⚠ **The recency claim is proved against a near-identical older row rather than by alphabetical
luck**: the new QBR (**14:17**) sorts **above** the old Q3 one (**02:07**). Two rows whose names
would sort adjacently, ordered correctly by time — that is the discriminating observation, and
without it a pass here would have been ambiguous.

⚠ The CTA is **the surface `193.2-02` pinned for the first time in its life** (`run-cta` occurrences
in its suite: 0 → 13). It had shipped with **zero** automated coverage and WR-04 had already fixed
one silent drop on it that no test would have caught.

### M4 — the run fills the bound template. **✅ PASS.**

| Property | Observed |
|---|---|
| Output | `/Northwind-QBR-Template.docx`, **39,698 B** against a **37,424 B** template |
| Fields filled | **10 of 10**, every one carrying a real `source_doc` |
| Residual `{{ }}` | **0** · literal `None` | **0** |
| `opened` / `residual_clean` | `True` / `True` |
| **Branding** | **intact — verified against an operator SCREENSHOT, not merely the field map**: navy header band, Georgia serif title, amber *"Northwind Logistics"* + ACCOUNT HEALTH block, teal section headings 1-6, grey executive-summary panel, navy footer rule |

⚠ **This is the moment no test in this repository could stand in for.** `193.2-FREQUENCY.md`'s SC#4
control measured `render_template` **5/5** — but that counts a **phase in a draft**. This is the
document coming back **filled**.

### M5 — the planted facts. **✅ PASS — all of them.**

68% utilisation · **412 of 605 seats** · **INC-4471** · a **6h12m outage on 12 Aug** · ARR
**£284,000** · term ends **31 Jan 2027** · Freight Analytics **~£62,000** · health **AMBER**.
**Scoring rule was 8+; all eight above were grounded.**

⚠ **THE 193.1 ATTRIBUTION REGRESSION DID NOT RECUR — Marcus Feld is NAMED TWICE in the rendered
document.** 193.1 observed the model grounding his quote and **stripping his name** (content kept,
attribution lost — fine internally, a downgrade client-facing), and that observation was
deliberately seeded nowhere, held *"until it recurs"*. **This is a second data point in the right
direction, so no seed is owed** — and the deferral's own trigger (*"a second sighting earns a
seed"*) is therefore **not** fired.

### M6 — the composer's Harness picker (optional). **⏸ NOT DRIVEN.**

Recorded as not driven. **Never as a pass.** See U5.

---

⚠ **What is deliberately NOT in this file** (T-193.2-10-A): knowledge-base content, provider keys,
and the produced document's body. The eight planted facts above are the kit's **synthetic** ones, and
the branding verdict is a description of a screenshot rather than the screenshot's contents.

---

## U2 — OQ-1, the rendered refusal. **VERDICT: ⏸ NOT DRIVEN — and it could not have been.**

⚠ **This row watches the refusal shown for an INTERACTIVE STEP, and no interactive step ever
appeared.** The driven run's five phases are `gather-usage` → `gather-support` → `gather-commercial`
→ `synthesize` → `emit-qbr` — not an `llm_human_input` among them, consistent with `193.2-08`'s
measured **0/20**. **The surface under test never rendered, so there was nothing to observe.**

⚠ **That is NOT the same as a pass, and it is deliberately not recorded as one.** *"The defect did
not occur"* and *"the refusal reads well when it does"* are different claims; only the first is
evidenced. **The rewritten two-arm copy has therefore still never been read by a person on a real
screen** — it is pinned by unit cases and driven RED against five real plants, and that is all.
**Re-open trigger: the next time an interactive step appears on a real draft.**

### ⚠ CORRECTION **C-U2** — this row's third clause was FALSE, and it is corrected beside the original (2026-08-15, `/gsd:validate-phase 193.2`, gap **G-1**)

**What the row said:** *"and **no `publish_blocked` row is written**"*. The strikethrough above
preserves it verbatim, because a stated expectation that was wrong is more useful visible than erased.

**What is measured.** Stage 2.5 (`publish_service.py:223`) returns `await _block(...)`, and `_block`'s
first act is `_safe_audit(..., event_type="publish_blocked", metadata={"blocked_stage": stage, …})`.
**A publish POST that reaches the interactive gate DOES write a `publish_blocked` receipt.**

⚠ **This file's own §U2 evidence already showed the mechanism and the row still asserted its
opposite:** the three blocked publishes at 14:02:21 / 14:07:42 / 14:10:29 each wrote a
`publish_blocked` row at `blocked_stage: structural_gate` — a *different* stage, the *same* `_block`
helper. The receipt was never conditional on which gate fired.

**Both readings are true of different tiers, and conflating them is what made the clause wrong:**

| Path | Receipt? |
|---|---|
| Client greys `publish-trigger` off `/validate`'s `blockedReason` → **no POST is sent** | **No row** — this is what the clause was reaching for |
| A POST reaches the server and stage 2.5 blocks it | **One `publish_blocked` row**, `blocked_stage="interactive_phase"`, NULL `run_id`, `definition_id` in metadata |

**The receipt half of this row is no longer manual.** It is covered by
`test_the_interactive_block_writes_a_publish_blocked_receipt` (both interactive shapes) plus a
positive control, each driven RED against a real plant in `publish_service.py` and restored to an
empty `git diff --numstat`. **What remains owed on U2 is the RENDERED observation only** — the
disabled control and the refusal's words on a real screen. The re-open trigger below is unchanged.

⚠ **A DIFFERENT refusal WAS met three times, and it produced a finding rather than a pass.** The
three blocked publishes (M2) came from the **structural gate**, and the operator's reading of that
refusal is now **`BUG-260815-06` (major) — *the structural-gate refusal names a stage, not a
cause***. **That is the same failure class this phase fixed for the interactive gate**, surviving on
the gate next door: 193.2 rewrote one refusal's words and the neighbouring one still names
machinery instead of telling a person what happened. Recorded here because it is the closest thing
to a driven observation this row got, and because it is evidence *against* the sibling surface
rather than *for* this one.

**Everything below was the method and the pre-run baseline; both are preserved unchanged.**

**What is already settled and must not be re-litigated.** `193.2-02` established from the audit
ledger that **the gate FIRED and the 400 never happened**: on 2026-08-15 there were **0**
`publish_blocked` rows, exactly **1** `publish_attempted` and **1** `publish_succeeded` 7.57 ms
apart. D-10's escape hatch is recorded **NOT TRIGGERED** — both CONTEXT facts are TRUE, and the false
premise was the unstated third one, *that a 400 was received at all*. **The refusal the operator met
came from `blockedReason` beside an already-greyed control.** What the ledger cannot prove is what
pixels were on screen, and that is the whole of this row.

**Method.** Open the Builder on a draft carrying an interactive step. Via Chrome MCP
`evaluate_script` — ⚠ **`take_screenshot` times out on this box; read DOM geometry instead** —
capture:

```js
document.querySelector('[data-testid="publish-blocked-reason"]')?.textContent
document.querySelector('[data-testid="publish-trigger"]')?.disabled
```

Then re-run the audit count and confirm it stays **0**.

**Pre-run BASELINE, measured 2026-08-15 at this plan's HEAD — a baseline, NOT a verdict:**

| Measurement | Value |
|---|---|
| `publish_blocked` rows today | **0** (absent from the grouped result entirely) |
| `publish_attempted` / `publish_succeeded` today | **1** / **1** |
| `publish_blocked` all-time | **32** |
| newest `publish_blocked` anywhere | **2026-08-07 19:39:11** |

⚠ **CORRECTION C-7 applies to anyone extending this query.** `harness_audit.metadata` is a
**DOUBLE-ENCODED jsonb STRING**, so `metadata->>'blocked_stage'` returns **NULL on all 32 rows** by
definition. The working accessor is `metadata #>> '{}'` followed by a JSON parse. The `count(*)`
above is safe because it keys on `event_type`, a real column. *A NULL that reads like "the field is
empty" is indistinguishable from "you asked the wrong way"* — the same class of failure this phase
exists to fix in user-facing copy, appearing in our own diagnostics.

**What the row records when driven:** whether the message names a step the operator can actually see,
whether the control is genuinely disabled, and whether the count stayed 0.

---

## U3 — OQ-2, the CTA. **VERDICT: ◐ PASS on appearance; the LATENCY was not timestamped.**

**Driven 2026-08-15 as part of U1 moment 3: the post-publish Run CTA APPEARED and NAMED the
workflow.** That is the half `D-19` turned on — *"the product never told me the name"* is closed on
this path, and `193.2-02`'s ruling (*it fires; SC#3 is met by the sort plus one pin; no
navigate-to-row is built*) is confirmed on a real screen rather than only in source.

⚠ **The row's other half — the interval between the click and the CTA's first paint — was NOT
measured, and the partial is stated as a partial.** The refetch race is source-visible and now
unit-pinned, but no timestamp was taken, so **nothing here may be quoted as a latency figure.**
**Re-open trigger:** a report that the CTA appears late enough to be missed, or the next publish
driven with the clock running.

Publish from the Builder; timestamp the click and the first appearance of
`[data-testid="run-cta"]`.

**What is already settled.** `193.2-02` ruled **D-19: it fires; SC#3 is met by the sort plus one pin;
NO navigate-to-row is built** — building a second hand-off surface beside a shipped one would be a
genuine second concern on a file carrying an inherited G-5 obligation. ⚠ **The CTA had ZERO automated
coverage from the day it shipped until this phase** (`grep -c "run-cta"` on its suite: **0 → 13**),
and WR-04 had already fixed one silent drop on it that no test would have caught. Its three new cases
pin the refetch race, the name/version hand-off and the draft exclusion, with **presence and absence
driven RED independently** — but a unit pin says nothing about latency on a real network.

**If it never appears, that is a named defect** and this row is the only instrument that would see it.

---

## U4 — OQ-5, the rendered position. **VERDICT: ✅ PASS — the judgement was made, and it was the operator's.**

**Driven 2026-08-15: rendered position 4 of 112** (3 starters + 29 published + 80 drafts), **first
among the operator's own work**, found **without searching**. **The measured prediction of index
`starters.length` held exactly.**

⚠ **The pass is the OPERATOR's judgement, not a measurement, and that distinction survives the
result.** This file predicted 4 and refused to call it findable; the operator drove it and did. Had
they said 4 was still too far down, the sort would have been no less correct and the row would have
failed — which is what made it worth asking rather than asserting.

⚠ **The recency claim is proved against a near-identical older row rather than by alphabetical
luck:** the new QBR (14:17) sorts **above** the old Q3 one (02:07). Two rows that would sort
adjacently by name, ordered correctly by time.

**The prediction and its reasoning are preserved below unchanged**, because a prediction that held is
only interesting if the reader can still see what it was.

After publishing, observe the library **without searching**. Record the row's rendered index.

**The measured prediction is `4 of 109`, and every part of that number is stated rather than rounded:**

- ⚠ **`BUG-260815-02`'s own arithmetic was over the WRONG FEED.** The report's *"129 of 146"* comes
  from a `row_number() over (order by name)` across the **whole** `workflow_definitions` table; the
  page passes **`?scope=mine`**. The true rendered figures are **17 of 109 → 4 of 109**. Both are
  recorded, neither over the other.
- **Position 4 is not position 1, and no `ORDER BY` change can make it 1.** `mergeLibrary`
  concatenates **starters ++ published ++ drafts** client-side, so the newest published row lands at
  index `starters.length`. That index is pinned as an **expression**, never a literal, by
  `libraryFilter.test.ts` — a literal would rot the moment the curated shelf grows by one.
- **The option not taken, with its measured cost:** re-ordering the merge loops (RESEARCH §R2
  reading 2) would put the row at index 0 — and a real plant proved it also **inverts the dedupe
  precedence**, so a row the caller owns would read as a shared starter. That is a separate decision,
  **not** a client sort; D-17's fence forbids client sorts outright and both of its arms were driven
  RED against real plants.

⚠ **Whether position 4 satisfies "findable" is a product judgement, not a measurement, and nothing in
this repository may be read as having answered it.** If the operator says position 1 is required,
that is a scoped decision with the dedupe cost above — and it does not retract the sort, which fixed
17 → 4.

---

## U5 — OQ-6, the composer's Harness picker. **VERDICT: ✅ DRIVEN 2026-08-16 — and the row's own premise was FALSE in three of its four consumers.**

⚠ **THE ORIGINAL "NOT DRIVEN" TEXT IS PRESERVED VERBATIM BELOW THE RULE, NOT OVERWRITTEN.** What it
asserted is exactly what a reader needs in order to see what measuring changed.

**The row asked whether the composer's Harness workflow picker reads sensibly under recency ordering.
It cannot be observed, because THE PICKER DOES NOT EXIST.** That is not a blocked row in the U2 sense
(where the surface exists and the condition never arose) — there is no such surface in the product.

### What was measured, consumer by consumer

`list_published_workflows` has **exactly ONE call site in the entire backend** —
`api/workflows.py:339`, the `GET /workflows/published` route. Every claim below is `grep`-derivable.

| Consumer named by the row | Measured | Order-sensitive? |
|---|---|---|
| **the composer's Harness workflow picker** | ⛔ **DOES NOT EXIST.** No production module under `frontend/src` renders an ordered list of published workflows in the composer. `MessageInput.tsx`'s two dropdowns are the **model picker** and the **Agent-mode (General/Explorer) selector** (`data-testid="agent-mode-selector"`). A workflow is launched from the **Workflows page RunModal** and arrives in chat with the composer **LOCKED** (`workflowLocked` → *"Controlled by the active workflow"*) — it is never *chosen* there | **N/A — no surface** |
| **`WorkspacePanel`'s run-soul** | Consumes as `published.find((w) => w.slug === slug)` (`WorkspacePanel.tsx:125-127`) — a keyed lookup that recovers one definition by slug | ❌ **NO** — a `.find()` returns the same row from any permutation |
| **`threads.py`'s kickoff (`:97`)** | ⛔ **DEAD IMPORT.** `grep -n "list_published_workflows" backend/app/api/threads.py` returns **line 97 and nothing else** — imported, never called. The row's own wording (*"`:97` imports it"*) was literally true and was read as *"consumes it"* | ❌ **NO — not a consumer at all** |
| *(a fourth the row never named)* **`ConnectionsTab.tsx`** | `listPublishedWorkflows().then(rows => setUsageCounts(usageCountsFrom(rows)))` (`:841`) — an aggregation into *Used by* counts | ❌ **NO** — a count is permutation-invariant |

### The conclusion this row exists to reach

> **The reorder is user-visible in exactly ONE surface — the library — and that surface was already
> driven by U4 (rendered position 4, PASS).** No unrendered or unexamined surface changed. The
> sentence *"it is the ONE user-visible consequence of `193.2-03` that lands outside the library"* is
> **FALSE**: there is no such consequence.

**This is a stronger result than the observation the row asked for.** *"I opened the picker and it
looked fine"* is one screen at one moment; *"every consumer of this feed is order-insensitive, and the
one that isn't has already been driven"* is a property of the code. The judgement half the row
reserved for a person has **nothing left to judge** — which is why it is closed by measurement rather
than deferred for a thirty-second look that could never have found anything.

### The live feed, driven rather than reasoned about

Queried directly against local Postgres (`asyncpg`, `127.0.0.1:54322`), **156 published rows**:

```
  0  2026-08-15 14:17:32  Quarterly Business Review — Northwind Logistics   ← U1's run
  1  2026-08-15 13:48:17  Global WF
  2  2026-08-15 13:48:17  Preview WF
  3  2026-08-15 13:47:21  Global WF
  …
```

Recency ordering is live and correct, and U1's just-published QBR sits at index **0** of the published
feed (U4's rendered position 4 is that row *after* `mergeLibrary` concatenates starters first — the
two figures agree). ⚠ **Note the identical-timestamp PAIRS at 13:48:17 / 13:47:21** — precisely the
tie the code review's **WR-03** addressed; the shipped clause is `ORDER BY updated_at DESC, id DESC`
on both author feeds (`db/workflows.py:397`, `:704`), with `ORDER BY name` deliberately retained on
starters (`:456`, D-16). Without WR-03's tiebreaker these pairs would have been free to swap between
reads. ⚠ Most rows here are test data — see `project_workflow_rows_are_test_data`.

### Re-open trigger

**If a composer-side workflow picker is ever built**, this row re-opens and must be driven for real —
its premise becomes true at that moment. Also re-opens if any consumer of `GET /workflows/published`
starts rendering the feed **as a list** rather than keying into it.

---

> **PRESERVED — what this section said before it was driven (2026-08-15):**
>
> *"**The optional row was not opened.** ⚠ Recorded as not driven, never as a pass, and NOT inferred
> from U1's evidence — U1 exercised the library, and the picker is a different surface reading the
> same feed. **It is the ONE user-visible consequence of `193.2-03` that lands outside the library and
> that nobody has looked at.** Thirty seconds when someone next opens the composer.*
>
> *Re-open trigger: the next composer session, or any report that the workflow picker's ordering reads
> oddly.*
>
> *Open the composer's Harness workflow picker and say whether the recency ordering reads sensibly.*
>
> ***Why this row exists at all.** With the default `owned_only=False`, `list_published_workflows` is
> **not only** the Workflows-page shelf — it also feeds the **composer's Harness picker**,
> **`WorkspacePanel`'s run-soul** and **`threads.py`'s kickoff**. **All three moved from alphabetical
> to recency** in `193.2-03`. Measured: no test and no frontend module asserts alphabetical order for
> any of them, so nothing broke — but "nothing asserts it" and "it reads well" are different claims,
> and only a person can make the second. **Thirty seconds, and it is the one surface outside the
> library the change reaches.**"*
>
> ⚠ **Three of that paragraph's four factual claims are false**, and it was inherited verbatim from
> `CLAUDE.md`'s hot-file ledger — which is corrected in the same commit. *A claim repeated across
> three artifacts is not three pieces of evidence; it is one unverified sentence with three homes.*

---

## Tally — driven 2026-08-15

~~**3 rows driven · 2 PASS + 1 PARTIAL · 0 FAIL · 2 NOT DRIVEN (U2, U5).**~~
→ **AMENDED 2026-08-16: 4 rows driven · 3 PASS + 1 PARTIAL · 0 FAIL · 1 NOT DRIVEN (U2).**
**U1's six moments: 5 driven, 5 PASS, 1 not driven (M6).**

| Row | Verdict |
|---|---|
| **U1** (D-21, the end-to-end run) | ✅ **PASS** — M1…M5 all pass; M6 not driven; one honesty caveat on M1's *visible* half |
| **U2** (OQ-1, the interactive refusal) | ⏸ **NOT DRIVEN** — the surface never rendered because the defect did not occur. **Not a pass.** ⚠ Its `publish_blocked`-receipt clause was CORRECTED and automated on 2026-08-15 (C-U2 / gap G-1); only the *rendered* observation remains owed |
| **U3** (OQ-2, the CTA) | ◐ **PASS on appearance; latency not timestamped** |
| **U4** (OQ-5, the rendered position) | ✅ **PASS** — position 4, the operator's judgement |
| **U5** (OQ-6, the composer picker) | ✅ **PASS by measurement (2026-08-16).** ⚠ **The picker does not exist**; 3 of 4 named consumers order-insensitive or dead. The reorder reaches exactly one user-visible surface — the library, driven by U4 |

⚠ **`AUTH-03` IS SATISFIED, AND U1 IS THE ONLY REASON IT MAY BE SAID.** A template attached **at
authoring time** and a real run that **fills that same template** — both halves observed end to end
on run `b021c7b0`. 193.1 built the capability and deliberately declined to tick it on unit evidence
alone; this row is the evidence it was waiting for.

~~⚠ **THE PHASE CLOSES WITH TWO ROWS NOT DRIVEN, AND THAT IS A DECISION, NOT A CLAIM THAT EVERYTHING
RAN.** **U5 first** when someone next opens the composer (thirty seconds). **U2 cannot be scheduled**
— it waits on an interactive step appearing, which is precisely the thing this phase reduced to 0/20.~~

⚠ **AMENDED 2026-08-16 — U5 WAS DRIVEN, AND IT COST MORE THAN THIRTY SECONDS BECAUSE IT WAS NOT THE
ROW IT LOOKED LIKE.** The instruction above (*"U5 first … thirty seconds"*) presumed a picker to open.
There is none. Driving the row meant enumerating every consumer of `GET /workflows/published` and
measuring each for order-sensitivity — which returned a **stronger** answer than the look would have:
the reorder is user-visible in exactly ONE surface, and U4 had already driven it. **One row remains
NOT DRIVEN — U2 — and it still cannot be scheduled**, waiting on an interactive step appearing, which
is precisely what this phase reduced to 0/20. ⚠ *A UAT row that asks you to look at something is worth
checking exists before it is scheduled as a thirty-second task.*

⚠ **Three of this phase's four ROADMAP success criteria were driven by U1; the fourth was too.** No
criterion is ticked that this run did not drive, and neither NOT-DRIVEN row underwrites any of them.

**Findings filed during the session — referenced, not re-derived here:** `BUG-260815-05` (blocking —
a provider outage reported as *"nothing was retrieved (0 sources)"*), `BUG-260815-06` (major — the
structural-gate refusal names a stage, not a cause), `BUG-260815-07` (major — a delete failed once,
**not reproducible**, stays open; two runs stuck `active` since 2026-08-01 and 2026-06-14 are a
permanent delete blocker), `BUG-260815-08` (minor — Workflows header polish; **G-2 fires**, sketch it
with `SEED-155`'s card density), **`SEED-159` — trigger FIRED, and its prediction was WRONG** (not a
silent blank but a verbose internal disclaimer, *"Not explicitly stated in the KB."*, rendered in the
front-page header of a document footed *"Commercial in confidence"*; the model behaved correctly and
the defect is that a **deliverable has no register distinct from an internal answer**), `SEED-165`
(52 out-of-gate backend failures, triaged 34 stale / 18 undiagnosed), `SEED-166`
(settings/operator/admin IA).

**Gates at close** (re-run after the code-review fix pass): count gate **`OK` · 3918 · failed 0 ·
75/75** · backend workflow suites **210 / 0** · full `backend/tests/unit` **62 failed / 2221 passed**
(the 62 is the recorded baseline rot set) · `tsc -p tsconfig.app.json` **33**, unmoved all phase.
**Code review: 0 Critical / 0 Warning / 5 Info**, all six Warnings fixed post-review. **G-7 CLEAR** —
10 plans, 0 gap-closure rounds. **No guardrail override recorded.**
