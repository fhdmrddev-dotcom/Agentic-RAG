---
phase: 067.2
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - STREAM-04-correctness
must_haves:
  truths:
    - "Verification-only plan — no production code changes are proposed by Plan 05 itself; if a regression is observed, log it as a follow-up phase candidate, do NOT patch SYSTEM_PROMPT or agent behavior in Plan 05 (CONTEXT.md § domain Phase Boundary: out-of-scope)."
    - "Provider model-id verification runs FIRST against `backend/.env` (OPENAI_MODELS, ANTHROPIC_MODELS, plus OpenRouter config). If a provider is unreachable or unconfigured, mark its UAT row as `n/a — provider unconfigured` and proceed (CONTEXT.md § risks)."
    - "Each of the 4 target models runs the synthetic-timeout protocol (Phase 067.1 Plan 01 / Phase 066 SC#6 protocol) and the Fahed-Mrad multi-step prompt (Phase 067.1 Plan 03 protocol)."
    - "Acceptance is per-model: synthetic-timeout produces a clean LangSmith trace exception (NOT `GeneratorExit at run_helpers.py:1680`) AND multi-step pipeline executes the full search→analyze→load_skill→execute_code chain without mid-pipeline confirmation."
    - "Output is a `MULTIPROVIDER-EVIDENCE.md` artifact with run_id, banner DOM screenshot, runs row JSON, and LangSmith trace exception column for each (model × scenario)."
  artifacts:
    - path: ".planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md"
      provides: "Per-model evidence for synthetic-timeout + multi-step protocols (or n/a-with-reason for unreachable providers)"
      contains: "claude-opus-4-6"
  key_links:
    - from: "Provider model-id check (Task 1)"
      to: "Synthetic-timeout protocol per model (Task 2)"
      via: "LLM_CALL_TIMEOUT_OVERRIDES=<model>=10 in backend/.env, restart uvicorn"
      pattern: "LLM_CALL_TIMEOUT_OVERRIDES"
    - from: "Synthetic-timeout protocol per model"
      to: "MULTIPROVIDER-EVIDENCE.md row green/red"
      via: "LangSmith trace exception column captured"
      pattern: "TimeoutError|CancelledError|RemoteProtocolError"
---

<objective>
Verify that Phase 067.1 Plan 01 Track A's drain-into-queue helper (`_drain_stream_with_close_on_cancel` at `backend/app/api/threads.py:155-249`) produces a clean LangSmith trace exception under synthetic-timeout for the three providers NOT live-verified in Phase 067.1 (Phase 067.1 Plan 05 only covered gpt-5.4). Additionally, verify Phase 067.1 Plan 03's multi-step SYSTEM_PROMPT executes the full pipeline (search → analyze → load_skill → execute_code) on those same providers without mid-pipeline confirmation prompts.

Models in scope (per CONTEXT.md `### D-067.2-06`):
1. **Anthropic claude-opus-4-6** (native SDK branch — Plan 01 Track A applied at threads.py:1297-1332)
2. **Anthropic claude-sonnet-4-6** (native SDK branch — same code path as #1)
3. **OpenRouter Kimi 2.5** (verify exact model id against env config — likely `moonshotai/kimi-k2.5` per PATTERNS.md / CONTEXT.md, but env is the source of truth)
4. **OpenRouter MiniMax 2.7** (verify exact model id against env config)

This is a verification-only plan. No production code changes ship from Plan 05. If a regression surfaces (e.g. Anthropic SDK doesn't honor stream.close() on cancellation), log it as a Gap candidate for a future phase (analogous to how Phase 066 Gap-007 became Phase 067.1).

Purpose: close the multi-provider coverage gap from Phase 067.1's "OpenAI-only" verification.

Output: `MULTIPROVIDER-EVIDENCE.md` artifact and 4 user-UAT rows in Plan 06's scoreboard updated with green/red/n-a status.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-CONTEXT.md
@.planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-CONTEXT.md
@.planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-HUMAN-UAT.md
@.planning/phases/066-adaptive-run-timeouts-lifecycle-states/066-HUMAN-UAT.md
@backend/app/api/threads.py
@backend/.env

<interfaces>
<!-- Protocol references — Plan 05 does NOT modify these; it RUNS them. -->

Synthetic-timeout protocol (from Phase 067 Plan 05 Task 4 / Phase 067.1 Plan 01 / Phase 066 D-066-11):
1. Set `LLM_CALL_TIMEOUT_OVERRIDES=<model>=10` in `backend/.env`.
2. Restart uvicorn.
3. Submit a slow long-form prompt (e.g. "Write a 5000-word essay on quantum computing").
4. Per-call timer fires at iteration 0 after 10s.
5. Capture: run_id, supabase `runs` row state (status='timed_out', error containing 'timed_out: 10s per-call deadline exceeded'), LangSmith ChatOpenAI sub-trace exception column.
6. Pass criterion: trace exception is one of {`TimeoutError`, `CancelledError`, `RemoteProtocolError`} — NOT `GeneratorExit at run_helpers.py:1680`.

Multi-step Fahed-Mrad prompt (from Phase 067.1 Plan 03):
- Verbatim user prompt: "search for research authored by Fahed Mrad → professional short report → charts/diagrams → docx"
- Pass criterion: agent executes full pipeline (search_documents → analyze_document → execute_code → load_skill('docx') → execute_code with docx) without intermediate "If you want, I can also..." / "Shall I proceed to..." confirmation prompts. All 4 deliverables produced.

Track A helper (the substrate being verified — read-only):
```python
# backend/app/api/threads.py:155-249
async def _drain_stream_with_close_on_cancel(stream, queue, *, model: str, on_token, ...):
    """D-067-Track-A: ensure stream.close() runs cleanly BEFORE cancellation
    propagates into the LangSmith @traceable async-generator wrapper.
    Symmetric across OpenAI + Anthropic timeout branches."""
```

Anthropic timeout branch (where Track A is applied — read-only):
```python
# backend/app/api/threads.py:1297-1332
# (Anthropic native SDK; uses _drain_stream_with_close_on_cancel)
```

OpenAI timeout branch (Track A applied; live-verified Phase 067.1):
```python
# backend/app/api/threads.py:1419-1474
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provider model-id + reachability verification (env config + minimal smoke per provider)</name>
  <files>(read-only — backend/.env, no source modification)</files>
  <read_first>
    - backend/.env (READ ONLY — do not echo secrets to console; use name-only extraction. CLAUDE.md memory: never paste full .env. Specifically need: OPENAI_MODELS, ANTHROPIC_MODELS, OPENROUTER_MODELS or equivalent OpenRouter config, plus the API key NAMES — confirm presence, never the values)
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-CONTEXT.md § specifics (D-067.2-06 model-id verification — confirms `ANTHROPIC_MODELS=claude-opus-4-6,claude-sonnet-4-6,...`)
    - backend/app/services or backend/app/utils — search `grep -rln "OPENROUTER" backend/app/` for the OpenRouter integration file (likely a model-routing module that lists the configured OpenRouter models)
  </read_first>
  <action>
    Build a provider availability map.

    **Step A — read environment names only (do NOT paste values):**

    Run a name-only listing:
    ```bash
    grep -E "^(OPENAI_MODELS|ANTHROPIC_MODELS|OPENROUTER|ANTHROPIC_API_KEY|OPENROUTER_API_KEY|OPENAI_API_KEY)=" backend/.env | sed 's/=.*$/=<set>/'
    ```
    (PowerShell equivalent if running on Windows: use `Select-String` with the same regex; emit `<set>` placeholder so values never enter logs.)

    Record which env vars are present. Cross-reference with project memory `feedback_env_secrets_handling.md` (NEVER expose live key values).

    **Step B — record exact model ids configured:**

    For each provider, capture the exact model strings from the env (the names, not the keys). Examples:
    - OPENAI_MODELS values include: `gpt-5.4` (verified Phase 067.1)
    - ANTHROPIC_MODELS values per CONTEXT.md: `claude-opus-4-6,claude-sonnet-4-6,claude-haiku-4-5-20251001`
    - OpenRouter: parse whatever env var the codebase uses; identify the exact id strings for "Kimi 2.5" and "MiniMax 2.7" (likely `moonshotai/kimi-k2.5` and `minimax/minimax-2.7` — verify against env or against `backend/app/services/agent_service.py` MODEL_CAPABILITIES registry).

    If OpenRouter config is missing (OPENROUTER_API_KEY not set, or no Kimi/MiniMax model in OPENROUTER_MODELS), record `provider unreachable: openrouter — env missing`.

    **Step C — minimal smoke per provider (1 token, 1 prompt):**

    Via Chrome MCP or via direct `curl` to the local backend, send a minimal prompt to each target model. The prompt: `"reply with the single word OK"`. Verify a successful streaming response.

    For each model:
    - claude-opus-4-6: smoke OK / failed
    - claude-sonnet-4-6: smoke OK / failed
    - OpenRouter Kimi 2.5: smoke OK / failed / unreachable
    - OpenRouter MiniMax 2.7: smoke OK / failed / unreachable

    **Step D — initialize MULTIPROVIDER-EVIDENCE.md** at `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md` with the table:

    ```markdown
    # Phase 067.2 Plan 05 — Multi-provider evidence (D-067.2-06)

    ## Provider availability (Task 1)

    | Model | Provider | Env reachable? | Smoke 'OK'? | UAT eligible? |
    |---|---|---|---|---|
    | claude-opus-4-6 | Anthropic native | yes/no | yes/no/n/a | yes/no |
    | claude-sonnet-4-6 | Anthropic native | yes/no | yes/no/n/a | yes/no |
    | <kimi-id> | OpenRouter | yes/no | yes/no/n/a | yes/no |
    | <minimax-id> | OpenRouter | yes/no | yes/no/n/a | yes/no |

    ## Per-model UAT scoreboard (Tasks 2-3)

    (filled in by Tasks 2-3)
    ```

    Only models with `UAT eligible: yes` proceed to Tasks 2-3. Unreachable / unconfigured providers are marked `n/a — provider unconfigured` and the corresponding Plan 06 user-UAT row gets the same `n/a` status (CONTEXT.md § risks).
  </action>
  <verify>
    <automated>test -f .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md &amp;&amp; grep -c "claude-opus-4-6\\|claude-sonnet-4-6" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md</automated>
    Plus:
    1. `grep -c "Provider availability" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md` → must be 1 (Task 1 section header).
    2. `grep -c "UAT eligible" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md` → must be ≥ 1.
    3. The artifact does NOT contain any string that matches an API key shape (`sk-`, `or-`, `pk_`, etc.) — `grep -nE "sk-[A-Za-z0-9_-]{10,}|or-[A-Za-z0-9_-]{10,}" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md` → must be EMPTY (security gate per feedback_env_secrets_handling.md).
  </verify>
  <done>
    Provider availability table populated; 4-model eligibility decided. MULTIPROVIDER-EVIDENCE.md initialized; no API keys leaked into the artifact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthetic-timeout protocol per eligible model — capture LangSmith trace exception</name>
  <files>(read-only on source — only modifies backend/.env LLM_CALL_TIMEOUT_OVERRIDES temporarily; final state restored)</files>
  <read_first>
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md (Task 1's UAT-eligible list)
    - .planning/phases/066-adaptive-run-timeouts-lifecycle-states/066-HUMAN-UAT.md (SC#6 row — synthetic-timeout protocol reference)
    - .planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-CONTEXT.md (Plan 01 Track A — what's being verified)
    - backend/app/api/threads.py:155-249 (the helper under test — read for context, do not modify)
  </read_first>
  <action>
    For EACH model marked `UAT eligible: yes` in Task 1:

    1. Edit `backend/.env`: set `LLM_CALL_TIMEOUT_OVERRIDES=<exact-model-id>=10` (replace any prior override). Save. Restart uvicorn (`Ctrl-C` and re-run `uvicorn` per project convention; if the dev server is run via a script, use that script).
    2. Open the local frontend at `http://localhost:5173/`. Log in as `fhdmrd@gmail.com` / `123456` per memory `reference_local_dev_app.md`.
    3. In a NEW thread, select the target model from the model dropdown. Submit a slow long-form prompt: `"Write a detailed 5000-word essay on the history of quantum computing, including the contributions of every major researcher from 1900 to present."` (or any prompt long enough to exceed 10s of model output streaming).
    4. Wait ~12s — per-call timer fires at iteration 0.
    5. Capture (paste-into-MULTIPROVIDER-EVIDENCE.md):
       - `run_id` (visible in DevTools Network tab `POST /threads/.../messages` response, or in Supabase `runs` table)
       - Supabase `runs` row: `status`, `error` (query: `SELECT status, error FROM runs WHERE run_id='<run_id>';`)
       - LangSmith trace URL for the run, AND the ChatOpenAI/Anthropic sub-trace exception-column text. Specifically: navigate to https://smith.langchain.com/, find the run by run_id, drill into the model-call sub-trace, and capture the exception column verbatim.
    6. Pass criterion per model:
       - `runs.status == 'timed_out'` AND `runs.error` contains `'timed_out: 10s per-call deadline'`
       - LangSmith sub-trace exception is one of `TimeoutError`, `CancelledError`, `RemoteProtocolError`, OR a clean exception type — NOT `GeneratorExit at run_helpers.py:1680`.
    7. Restore `backend/.env`: remove or reset the LLM_CALL_TIMEOUT_OVERRIDES line; restart uvicorn.

    Loop through the eligible models in order: Anthropic claude-opus-4-6, claude-sonnet-4-6, OpenRouter Kimi, OpenRouter MiniMax.

    Append to MULTIPROVIDER-EVIDENCE.md per-model:

    ```markdown
    ### claude-opus-4-6 — synthetic-timeout
    - run_id: <uuid>
    - runs.status: timed_out
    - runs.error: "timed_out: 10s per-call deadline exceeded at iteration 0 (model=claude-opus-4-6)"
    - LangSmith trace: https://smith.langchain.com/...
    - Sub-trace exception column: TimeoutError() / CancelledError() / RemoteProtocolError(...) / GeneratorExit (RED)
    - Verdict: GREEN / RED
    ```

    If any model produces `GeneratorExit at run_helpers.py:1680` in the sub-trace, mark RED and write a Gap candidate note in MULTIPROVIDER-EVIDENCE.md `## Gaps` section. Do NOT attempt to patch SYSTEM_PROMPT or stream-close behavior in Plan 05 — that's out of scope per CONTEXT.md § domain Phase Boundary. The gap escalates to a follow-on phase.
  </action>
  <verify>
    <automated>grep -c "synthetic-timeout" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md</automated>
    Plus:
    1. For each UAT-eligible model from Task 1: a corresponding `### <model-id> — synthetic-timeout` section exists in MULTIPROVIDER-EVIDENCE.md with `run_id`, `runs.status`, `Sub-trace exception column`, and `Verdict` lines.
    2. `grep -c "GREEN\\|RED\\|n/a" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md` → must be ≥ N where N is the count of UAT-eligible models.
    3. backend/.env final state has NO `LLM_CALL_TIMEOUT_OVERRIDES=` line set to a 10s override (restoration confirmed). Run: `grep -c "LLM_CALL_TIMEOUT_OVERRIDES" backend/.env` → 0 OR the line exists but is commented out / set to empty.
  </verify>
  <done>
    Per-model synthetic-timeout evidence captured. Each row has GREEN/RED/n-a verdict with concrete LangSmith trace evidence. backend/.env restored to pre-test state. No SYSTEM_PROMPT or threads.py source change.
  </done>
</task>

<task type="auto">
  <name>Task 3: Multi-step Fahed-Mrad pipeline per eligible model — verify full pipeline executes without confirmation</name>
  <files>(read-only on source — UAT only)</files>
  <read_first>
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md (Task 2's per-model verdicts)
    - .planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-CONTEXT.md § Plan 03 (the multi-step prompt protocol — verbatim wording)
    - backend/app/services/agent_service.py SYSTEM_PROMPT (read-only — context for what the agent should do; do NOT modify)
  </read_first>
  <action>
    For EACH model marked `UAT eligible: yes` in Task 1 (regardless of Task 2's RED/GREEN verdict — multi-step verification is independent of the timeout test):

    1. Open the local frontend at `http://localhost:5173/`. Log in.
    2. New thread. Select target model.
    3. Submit verbatim prompt (Phase 067.1 Plan 03 reference): `"search for research authored by Fahed Mrad → professional short report → charts/diagrams → docx"`
    4. Watch the agent run end-to-end. Capture:
       - run_id
       - tool sequence: which tools fired in what order? Expected sequence: `search_documents` (or equivalent) → `analyze_document` (one or more) → `execute_code` (charts) → `load_skill('docx')` → `execute_code` (docx generation).
       - Any "If you want, I can also..." / "Shall I proceed..." / "Would you like me to also..." confirmation prompts mid-pipeline (capture verbatim if observed).
       - Final deliverables: themes report (text), chart PNG (in chat outputs), docx file (downloadable).
    5. Pass criterion: full pipeline executes; all 4 deliverables produced; ZERO mid-pipeline confirmation prompts.
    6. RED criteria: agent stops after step 2 to ask "Would you like me to..." — record verbatim text.

    Append to MULTIPROVIDER-EVIDENCE.md per-model:

    ```markdown
    ### claude-opus-4-6 — multi-step Fahed-Mrad
    - run_id: <uuid>
    - Tool sequence observed: search_documents → analyze_document → execute_code → load_skill('docx') → execute_code
    - Mid-pipeline confirmation prompts: NONE / "Would you like me to also..." (verbatim if observed)
    - Deliverables: report ✓, chart PNG ✓, docx ✓
    - Verdict: GREEN / RED
    ```

    Loop through eligible models. If a model produces mid-pipeline confirmation, mark RED and write a Gap candidate note in MULTIPROVIDER-EVIDENCE.md. Do NOT modify SYSTEM_PROMPT to fix it (out of scope per CONTEXT.md § domain Phase Boundary).
  </action>
  <verify>
    <automated>grep -c "multi-step Fahed-Mrad" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md</automated>
    Plus:
    1. For each UAT-eligible model from Task 1: a corresponding `### <model-id> — multi-step Fahed-Mrad` section exists with `run_id`, `Tool sequence observed`, `Mid-pipeline confirmation prompts`, `Deliverables`, `Verdict` lines.
    2. `grep -nE "Verdict: (GREEN|RED|n/a)" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md | wc -l` → must be ≥ 2 × N (synthetic-timeout verdict + multi-step verdict per eligible model).
    3. No source file is modified by Plan 05 (verification-only).
  </verify>
  <done>
    Per-model multi-step pipeline evidence captured. RED entries (if any) escalated to Gap candidates in MULTIPROVIDER-EVIDENCE.md `## Gaps` section. No production code modified. Plan 06's 4 D-067.2-06 user-UAT rows can now be filled with GREEN / RED / n/a per the per-model verdicts gathered here.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan — runs existing prompts against existing endpoints. No new endpoints, no new code paths. The only file modification is a temporary `backend/.env` `LLM_CALL_TIMEOUT_OVERRIDES` toggle (restored at end of Task 2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067.2-05-01 | Information disclosure / Secrets in artifacts | MULTIPROVIDER-EVIDENCE.md may inadvertently capture API keys from copy-paste of error messages or .env grep output | mitigate | Task 1's `<verify>` block includes a key-pattern grep that fails the task if anything matching `sk-...`/`or-...` appears in the artifact. Task 1's `<action>` explicitly uses name-only extraction (`sed 's/=.*$/=<set>/'`) per memory `feedback_env_secrets_handling.md`. |
| T-067.2-05-02 | Tampering / Test artifact pollution | LLM_CALL_TIMEOUT_OVERRIDES set to 10s lingering in dev .env after Task 2 | mitigate | Task 2 step 7 + Task 2 verify gate explicitly require restoration. Final-state grep confirms no `=10` override remains. |
| T-067.2-05-03 | Repudiation / Evidence trail | Captured run_ids / LangSmith URLs may be invalid by phase close | accept | run_ids and LangSmith trace URLs are immutable-ish (LangSmith retains traces); mid-test capture is sufficient. Plan 06's user UAT can re-run any failing model independently if evidence becomes stale. |
</threat_model>

<verification>
1. MULTIPROVIDER-EVIDENCE.md exists with: provider availability table + per-model synthetic-timeout sections + per-model multi-step sections.
2. backend/.env LLM_CALL_TIMEOUT_OVERRIDES restored to pre-test state.
3. No source file outside .planning/ has been modified by Plan 05.
4. Plan 06's 4 D-067.2-06 rows are populated based on this evidence.
</verification>

<success_criteria>
- MULTIPROVIDER-EVIDENCE.md exists, committed, and contains:
  - Task 1 provider-availability table (4 rows).
  - Task 2 synthetic-timeout sections per UAT-eligible model.
  - Task 3 multi-step Fahed-Mrad sections per UAT-eligible model.
  - `## Gaps` section listing RED outcomes (if any) as follow-on phase candidates.
- No API keys / secrets in the artifact.
- backend/.env has no leftover `LLM_CALL_TIMEOUT_OVERRIDES=...=10` line.
- No source file outside `.planning/` was modified by Plan 05.
- 4 user-UAT row decisions (GREEN / RED / n/a — provider unconfigured) feed into Plan 06.
</success_criteria>

<output>
After completion, create `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-SUMMARY.md` documenting: per-model verdicts, any escalated gaps, and the explicit confirmation that no production code was changed. Plan 06 references this artifact when filling its D-067.2-06 rows.
</output>
