---
phase: 067.2
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified: []
autonomous: false
requirements:
  - STREAM-04-correctness
must_haves:
  truths:
    - "Phase 067.2 closure gate is **user-driven UAT** with 12 actionable checkboxes — every checkbox MUST be ticked GREEN by the user (NOT by an automated DOM observer or test) for the phase to be marked complete (CONTEXT.md § verification_protocol)."
    - "Partial-with-approval is EXPLICITLY REJECTED for this phase — CONTEXT.md states the diagnosis was that previous partial-with-approval patterns let lived-experience failures escape verification."
    - "Plan 06 tasks are USER-INSTRUCTION tasks, not implementation tasks — the executor's job is to coordinate the UAT session and capture evidence; the user does the verification."
    - "Plan 06 reads MULTIPROVIDER-EVIDENCE.md (from Plan 05) and INVESTIGATION-NOTES.md (from Plan 04) to pre-fill rows 9-12 (D-067.2-06 multi-provider) and row 6 (D-067.2-04 confidence/suggestions)."
    - "If a provider was marked `n/a — provider unconfigured` in Plan 05, the corresponding D-067.2-06 row is allowed to be `n/a` — this does NOT block phase closure (CONTEXT.md § risks)."
    - "The 12 checkboxes are listed verbatim from CONTEXT.md `<verification_protocol>` § User UAT Scoreboard — Plan 06 reproduces them exactly so the user clicks/types from a stable list."
  artifacts:
    - path: ".planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md"
      provides: "12-row user-UAT scoreboard, 2 no-regression checks, completion-gate statement, evidence column per row"
      contains: "D-067.2-01 verified"
  key_links:
    - from: "User UAT scoreboard 12/12 GREEN"
      to: "Phase 067.2 marked complete"
      via: "Plan 06 SUMMARY closes phase; STATE.md updated by executor"
      pattern: "12/12"
    - from: "Plan 05 MULTIPROVIDER-EVIDENCE.md"
      to: "Plan 06 rows 9-12 (D-067.2-06 multi-provider)"
      via: "Pre-fill GREEN/RED/n/a from Task 5's verdicts"
      pattern: "MULTIPROVIDER-EVIDENCE"
    - from: "Plan 04 INVESTIGATION-NOTES.md"
      to: "Plan 06 row 6 (D-067.2-04 confidence + suggestions)"
      via: "Pre-fill verdict (a/b/c) into row evidence"
      pattern: "INVESTIGATION-NOTES"
---

<objective>
Coordinate the user-driven UAT session that gates Phase 067.2 closure. Construct `067.2-HUMAN-UAT.md` with the 12 user-actionable checkboxes verbatim from CONTEXT.md `<verification_protocol>` § User UAT Scoreboard, plus 2 no-regression checks and the completion-gate statement. Pre-fill rows where Plan 04 / Plan 05 already produced evidence. Walk the user through each remaining row, capture evidence, mark each row GREEN/RED/n-a. Phase 067.2 ships ONLY when 12/12 are GREEN-or-n/a (with `n/a` allowed only for unconfigured-provider rows under D-067.2-06).

Purpose: enforce the lived-experience verification gate that CONTEXT.md mandates. Phase 067.2 exists precisely because Phase 067.1's DOM-observer-based UAT missed the 6 lived-experience failures the user reported. Plan 06 is the corrective fence: a real human, in a real browser, ticks each row.

Output: `067.2-HUMAN-UAT.md` with all 12 rows ticked (or n/a-with-reason for unconfigured providers), plus a closure statement. STATE.md updated to reflect phase completion (or staying open if any row is RED).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-CONTEXT.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-PATTERNS.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md
@.planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-HUMAN-UAT.md
@.planning/phases/066-adaptive-run-timeouts-lifecycle-states/066-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 067.2-HUMAN-UAT.md scaffold with verbatim 12-checkbox scoreboard + pre-fill from Plans 04/05</name>
  <files>.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-CONTEXT.md `<verification_protocol>` § User UAT Scoreboard (verbatim 12 checkboxes — copy these EXACTLY into the scaffold; do NOT paraphrase, reword, or shorten)
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md (pre-fill row 6 evidence based on verdict)
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md (pre-fill rows 9-12 with GREEN/RED/n-a per model)
    - .planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-HUMAN-UAT.md (format precedent for HUMAN-UAT.md scoreboard layout)
  </read_first>
  <action>
    Create `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` with the following structure. Copy the 12 checkbox texts VERBATIM from CONTEXT.md `<verification_protocol>` § User UAT Scoreboard:

    ```markdown
    ---
    phase: 067.2
    plan: 06
    status: in_progress
    created: <today's date>
    gate: 12/12 user-ticked GREEN required for phase close — partial-with-approval REJECTED per CONTEXT.md
    ---

    # Phase 067.2 — Human UAT Scoreboard

    > **Closure gate:** Phase 067.2 cannot be marked complete until every checkbox below is ticked GREEN by the user (NOT by an automated DOM observer or test). `n/a — provider unconfigured` is allowed ONLY for rows 9-12 (D-067.2-06) where Plan 05 verified the provider env was missing (CONTEXT.md § risks).

    ## User UAT Scoreboard

    Pre-conditions before starting:
    - Plan 01, 02, 03 source changes are landed and the dev frontend at `localhost:5173` serves the new bundle (verify by `grep useLayoutEffect` against `ChatArea.tsx` in DevTools Sources).
    - Backend uvicorn restarted with the new sandbox-outputs router registered.
    - Logged in as `fhdmrd@gmail.com` / `123456`.

    ### Row 1 — D-067.2-01
    - [ ] **D-067.2-01 verified:** Submit Fahed-Mrad multi-step prompt on a NEW thread (no existing thread). Assistant content streams progressively (visible delta-by-delta) — NOT empty until end-of-run.
    - **How to test:** Click "+ New Chat" → type the verbatim Fahed-Mrad prompt → Send → watch the assistant message render delta-by-delta from the first ~100ms.
    - **Pass:** Tokens stream visibly into the placeholder as they arrive; no empty-then-lump-replace at end.
    - **Evidence captured:** screenshot / GIF / verbal confirmation; run_id: ____
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 2 — D-067.2-02a
    - [ ] **D-067.2-02a verified:** During a multi-step run, press F5 mid-stream. Page reloads; assistant content continues streaming with NO blank-state period.
    - **How to test:** Submit a multi-step prompt; mid-stream (within first 5s after first delta) press F5 → wait for page to reload → observe whether the in-progress assistant content is visible immediately (replay-from-offset-0 lands).
    - **Pass:** No blank-state gap; content visible immediately on reload and continues streaming.
    - **Evidence:** screenshot / video; run_id: ____
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 3 — D-067.2-02b
    - [ ] **D-067.2-02b verified:** During a multi-step run on Thread A, click Thread B; click back to Thread A. Content present, continuing if still streaming.
    - **How to test:** Submit a multi-step prompt on Thread A; create / select Thread B mid-stream; click back to Thread A.
    - **Pass:** Thread A's assistant content is present and (if still mid-run) continues streaming.
    - **Evidence:** verbal confirmation / screenshot
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 4 — D-067.2-03a
    - [ ] **D-067.2-03a verified:** Submit a docx-generating prompt → click download immediately → file downloads.
    - **How to test:** Submit a prompt that triggers a docx skill load + execute_code → wait for the download link to render in the chat → click → confirm the docx file downloads.
    - **Pass:** docx downloads successfully; no `InvalidJWT` error.
    - **Evidence:** downloaded file path / browser download confirmation
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 5 — D-067.2-03b
    - [ ] **D-067.2-03b verified:** Wait 90 minutes (or use short-TTL test override) → click same download link in chat history → file STILL downloads, no `InvalidJWT`.
    - **How to test:**
      - **Slow path (real wait):** complete Row 4, then wait 90 minutes; revisit the chat thread; click the same download link.
      - **Fast path (Plan 03 short-TTL test):** Plan 03's re-sign endpoint uses 60s TTL on the *redirect target*, but the URL in the message body is permanent. To simulate the 1h failure mode, the user can leave the dev tab idle for 60+ minutes OR (developer-friendly) restart Supabase Storage which invalidates legacy long-form URLs immediately. The lived-experience pass is the 90-minute wait.
    - **Pass:** Download link still works; the new GET /sandbox-outputs/{path} endpoint re-signs and 302-redirects.
    - **Evidence:** timestamp of original generation + timestamp of re-click + downloaded file confirmation
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 6 — D-067.2-04
    - [ ] **D-067.2-04 verified:** Submit a Fahed-Mrad search-only prompt. Confidence indicator (low/medium/high or numeric) appears in assistant message UI; follow-up suggestions block appears with 2-4 clickable examples.
    - **How to test:** New thread → submit "search for any document about fahed mrad and summarize" → wait for run completion → look for confidence badge AND suggestion pills below the assistant message.
    - **Pass:** Both visible in DOM.
    - **Plan 04 INVESTIGATION-NOTES.md verdict:** [pre-fill: a/b/c] (executor copies from .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md "Verdict:" line)
    - **Evidence:** screenshot of confidence + suggestions in DOM
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 7 — D-067.2-05a
    - [ ] **D-067.2-05a verified:** Submit any prompt → mid-stream click Stop → sidebar shows a generated title (NOT "New Chat").
    - **How to test:** New thread → submit any non-trivial prompt → after ~2s, click Stop button → look at the sidebar entry for this thread.
    - **Pass:** Sidebar title is something LLM-generated (3-6 words), NOT "New Chat".
    - **Evidence:** screenshot of sidebar; thread_id: ____
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 8 — D-067.2-05b
    - [ ] **D-067.2-05b verified:** Submit a prompt with synthetic timeout active → sidebar shows generated title even though run failed.
    - **How to test:** Set `LLM_CALL_TIMEOUT_OVERRIDES=gpt-5.4=5` in `backend/.env`; restart uvicorn. New thread → submit a long-form prompt → wait for the synthetic timeout to fire (~6s). Confirm the runs row hits `status='timed_out'`. Then check the sidebar.
    - **Pass:** Sidebar title is generated despite the run failing.
    - **Note:** restore .env after this row.
    - **Evidence:** screenshot of sidebar after timeout; runs row status confirmation; thread_id: ____
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 9 — D-067.2-06 — Anthropic claude-sonnet-4-6
    - [ ] **D-067.2-06 verified — Anthropic claude-sonnet-4-6:** synthetic-timeout protocol → clean LangSmith trace + Fahed-Mrad multi-step → full pipeline executes.
    - **Plan 05 MULTIPROVIDER-EVIDENCE.md verdict:** [pre-fill from .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-05-MULTIPROVIDER-EVIDENCE.md row]
    - **Evidence:** Plan 05 captures synthetic-timeout LangSmith trace + multi-step run_id
    - **Verdict:** ⬜ GREEN ⬜ RED ⬜ n/a — provider unconfigured

    ### Row 10 — D-067.2-06 — Anthropic claude-opus-4-6
    - [ ] **D-067.2-06 verified — Anthropic claude-opus-4-6:** same as above.
    - **Plan 05 verdict:** [pre-fill]
    - **Verdict:** ⬜ GREEN ⬜ RED ⬜ n/a — provider unconfigured

    ### Row 11 — D-067.2-06 — OpenRouter Kimi 2.5
    - [ ] **D-067.2-06 verified — OpenRouter Kimi 2.5:** same as above.
    - **Plan 05 verdict:** [pre-fill]
    - **Verdict:** ⬜ GREEN ⬜ RED ⬜ n/a — provider unconfigured

    ### Row 12 — D-067.2-06 — OpenRouter MiniMax 2.7
    - [ ] **D-067.2-06 verified — OpenRouter MiniMax 2.7:** same as above.
    - **Plan 05 verdict:** [pre-fill]
    - **Verdict:** ⬜ GREEN ⬜ RED ⬜ n/a — provider unconfigured

    ## No-regression checks

    ### NR-1
    - [ ] **Phase 067.1 SC#1-#6 still hold:** spot-check 067.1's HUMAN-UAT scoreboard items remain green (Track A trace clean, in-flight copy phases visible, skill-load card present, ambiguous prompt asks clarification, multi-step pipeline executes for gpt-5.4).
    - **How to test:** open .planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-HUMAN-UAT.md; pick 2 representative rows; manually re-run.
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### NR-2
    - [ ] **Phase 066 SC#6 still green:** synthetic-timeout protocol on gpt-5.4 still produces clean LangSmith trace.
    - **How to test:** set `LLM_CALL_TIMEOUT_OVERRIDES=gpt-5.4=10` → submit a slow prompt → check LangSmith sub-trace exception column. Restore .env.
    - **Verdict:** ⬜ GREEN ⬜ RED

    ## Completion gate

    Phase 067.2 is marked complete ONLY when:
    - Rows 1-8 are GREEN.
    - Rows 9-12 are GREEN OR `n/a — provider unconfigured` (per Plan 05).
    - NR-1 and NR-2 are GREEN.

    Partial closures with project-level approval are NOT acceptable for this phase. If any non-`n/a` row is RED, the phase stays open and a follow-on plan is created to address the failure.

    ## Final closure

    - [ ] All 12 rows + 2 NR rows resolved as above.
    - [ ] Phase status updated in STATE.md.
    - [ ] Plan 06 SUMMARY written.
    ```

    Pre-fill the bracketed `[pre-fill: ...]` placeholders by reading the actual verdicts from Plan 04's INVESTIGATION-NOTES.md and Plan 05's MULTIPROVIDER-EVIDENCE.md. If those files don't exist yet (running this task before Plans 04/05 land), leave the placeholders as `[pre-fill: pending Plan 04]` / `[pre-fill: pending Plan 05]` and document in the SUMMARY.

    Replace `<today's date>` with the actual date the file is created.

    Do NOT modify the 12 checkbox texts — they MUST be verbatim from CONTEXT.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md &amp;&amp; grep -c "D-067.2-0" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md</automated>
    Plus:
    1. `grep -c "verified:" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` → must be ≥ 12 (one "verified:" line per checkbox).
    2. `grep -c "D-067.2-06 verified" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` → must be ≥ 4 (the 4 multi-provider rows).
    3. `grep -c "Partial closures with project-level approval are NOT acceptable" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` → must be 1 (the strict-gate language present verbatim).
    4. `grep -c "Phase 067.1 SC#1-#6 still hold" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` → must be 1 (NR-1 present).
    5. `grep -c "Phase 066 SC#6 still green" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` → must be 1 (NR-2 present).
  </verify>
  <done>
    067.2-HUMAN-UAT.md exists with all 12 verbatim checkbox rows, 2 NR rows, completion gate language, and pre-fills (or `[pre-fill: pending]` placeholders) where Plan 04 / Plan 05 evidence is referenced.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User UAT session — user ticks 12 + 2 = 14 rows; capture evidence per row</name>
  <what-built>
    Plans 01-05 are landed. 067.2-HUMAN-UAT.md scaffold from Task 1 is ready. The user now drives a real-browser UAT session to tick each row GREEN, RED, or n/a (n/a allowed only for rows 9-12).
  </what-built>
  <how-to-verify>
    The executor's role here is to coordinate, not to verify. Drive the user through the 14 rows in 067.2-HUMAN-UAT.md.

    For each row:
    1. The executor announces the row number and the test instructions verbatim from the scaffold.
    2. The user runs the test in the browser at `localhost:5173`.
    3. The executor (with Chrome MCP if available) can OPTIONALLY assist by automating browser navigation, but the **verdict is the user's** — the executor does not auto-tick rows.
    4. The user reports the verdict (GREEN / RED / n/a).
    5. The executor updates 067.2-HUMAN-UAT.md by editing the Verdict line for that row to fill in the chosen radio button (e.g. change `⬜ GREEN ⬜ RED` to `✅ GREEN ⬜ RED`) AND fills in the Evidence line with whatever the user provided (run_id, screenshot path, brief verbal note).
    6. If the user says RED, the executor records the failure mode verbatim in the Evidence line and proceeds to the next row (do NOT block the session on a single RED — record all 14 outcomes first).

    Order of rows: run 1, 2, 3 (the streaming-race rows — these need a multi-step prompt) before 4, 5 (download — needs a docx prompt) before 6 (search-only) before 7, 8 (Stop / synthetic timeout). Then rows 9-12 are mostly evidence-pulls from MULTIPROVIDER-EVIDENCE.md (the user re-confirms by reading that artifact, optionally re-running one model). Finally NR-1 and NR-2.

    Pre-fill rows 9-12 from MULTIPROVIDER-EVIDENCE.md if Plan 05 has run; rows 9-12 may already be `n/a — provider unconfigured` if Plan 05's Task 1 found a provider missing.

    For Row 5 (D-067.2-03b 90-minute wait): if the user does NOT want to wait 90 minutes during this session, mark Row 5 as `pending: 90-minute wait` and document the deferral; either complete it asynchronously (user re-clicks the link 90+ minutes later) OR escalate as a known carry-forward. Phase 067.2 cannot fully close until Row 5 is GREEN; explicitly NOT acceptable to close as `partial — approved`.

    Output: at the end of this task, 067.2-HUMAN-UAT.md has every Verdict line filled in for all 14 rows.
  </how-to-verify>
  <resume-signal>
    User types one of:
    - `12/12 + 2/2 GREEN — close phase` (or with up-to-3 `n/a` for rows 9-12 — phase still closes)
    - `partial: <list RED row numbers>` — escalate each RED to a follow-on plan; phase stays OPEN
    - `pending: <list pending row numbers>` — partial completion, not acceptable per CONTEXT.md; phase stays OPEN
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close phase based on UAT outcome — write SUMMARY, update STATE.md</name>
  <files>
    .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md (final verdicts captured in Task 2)
    - .planning/STATE.md (current schema; the "Current Position" + "Recent Completed Phases" sections need updating)
    - .planning/ROADMAP.md (the Phase 067.2 entry checkbox; should flip from `[ ]` to `[x]` only on full closure)
  </read_first>
  <action>
    Read 067.2-HUMAN-UAT.md and determine the closure state:

    **Branch A — full closure (all rows GREEN, or rows 9-12 mix of GREEN + n/a-only-for-unconfigured-providers, NR-1 and NR-2 GREEN):**
    1. Edit 067.2-HUMAN-UAT.md frontmatter: `status: complete` and add `closed: <today>`. Add a `## Closure summary` section recording 12/12 + 2/2 outcome.
    2. Create `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md` with:
       - Phase 067.2 closure date.
       - 12-row verdict summary (GREEN counts, n/a-with-reason rows, NR rows).
       - Plan 04 verdict summary (no-op / patch / escalation).
       - Plan 05 multi-provider summary (per-model GREEN/RED/n-a).
       - Pointer to 067.2-HUMAN-UAT.md as the authoritative evidence trail.
       - Statement: "Phase 067.2 closed with full user-driven UAT — 12/12 GREEN-or-n-a; lived-experience contract verified."
    3. Edit `.planning/ROADMAP.md`: flip the Phase 067.2 entry from `[ ]` to `[x]` and add a closure note (e.g. `*(Closed <date> — 12/12 user-UAT GREEN; lived-experience contract verified.)*`).
    4. Edit `.planning/STATE.md`: update `Current Position` (last completed phase = 067.2; next action = TBD per user). Update `Recent Completed Phases` section to include a 067.2 entry mirroring the 056.1 / 057 / 067.1 stub format.

    **Branch B — partial / RED outcomes:**
    1. Do NOT mark Phase 067.2 complete. Edit 067.2-HUMAN-UAT.md frontmatter: `status: in_progress` (or `status: blocked`) and add a `## Open issues` section listing each RED row + a brief failure description.
    2. Create `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md` with:
       - Phase 067.2 NOT closed.
       - List of RED rows + failure modes.
       - Recommended follow-on plan(s) (e.g. "Row 1 RED → race fix incomplete; create Plan 067.2-07 to investigate why useLayoutEffect didn't fully close the race in browser X").
    3. Do NOT modify ROADMAP.md (entry stays `[ ]`).
    4. Edit `.planning/STATE.md` to record the partial state: `Current Position: Phase 067.2 — UAT partial (N/14 GREEN); blockers: <list>`.

    **Branch C — pending (e.g. Row 5's 90-minute wait):**
    1. Same as Branch B but with the pending row noted as "deferred for time-bounded re-test" rather than "RED — failed".
    2. STATE.md note: `Current Position: Phase 067.2 — UAT 13.5/14 (Row 5 deferred 90min); resume with /gsd:verify-work`.
    3. Phase stays OPEN.

    All three branches: never silently flip ROADMAP.md `[x]` if any non-`n/a` row is unresolved.
  </action>
  <verify>
    <automated>test -f .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md &amp;&amp; grep -c "Phase 067.2" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md</automated>
    Plus (branch-dependent):
    1. **Branch A only:** `grep -c "12/12" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md` → must be ≥ 1 (closure outcome stated). Also: `grep -c "\\[x\\] \\*\\*Phase 067.2" .planning/ROADMAP.md` → must be 1 (the entry is checked).
    2. **Branch B/C only:** `grep -c "RED\\|deferred" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md` → must be ≥ 1 (failure / deferral cited). Also: `grep -c "\\[x\\] \\*\\*Phase 067.2" .planning/ROADMAP.md` → must be 0 (the entry is NOT checked).
    3. STATE.md updated: `grep -c "067.2" .planning/STATE.md` → must be ≥ 1 (entry exists in current state).
  </verify>
  <done>
    Branch-correct artifacts produced:
    - 067.2-06-SUMMARY.md exists with the right closure / open / pending outcome.
    - 067.2-HUMAN-UAT.md frontmatter status reflects branch.
    - ROADMAP.md flipped to `[x]` ONLY on Branch A.
    - STATE.md reflects current position correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 06 is purely coordination + documentation. No code changes, no new endpoints, no data flow modification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067.2-06-01 | Repudiation / Evidence integrity | UAT verdicts captured by editing 067.2-HUMAN-UAT.md without per-row screenshot | accept | The HUMAN-UAT artifact is the canonical record; user-supplied evidence per row (run_id, screenshot link, verbal confirmation) provides the audit trail. Phase planning artifacts are git-committed, so verdict-line edits are versioned. |
| T-067.2-06-02 | Tampering / scope-reduction-via-partial-approval | Future maintainer marks phase complete despite RED rows | mitigate | Task 3 explicitly enforces three branches: Branch A flips ROADMAP only on full closure; Branches B/C explicitly do NOT flip ROADMAP. Strict-gate language ("Partial closures with project-level approval are NOT acceptable for this phase") appears verbatim in 067.2-HUMAN-UAT.md, in this PLAN.md, and is enforced by the Task 3 verify gate (`[x] **Phase 067.2` grep must be 0 on Branch B/C). |
</threat_model>

<verification>
1. 067.2-HUMAN-UAT.md exists with 12 + 2 = 14 rows, each filled with a verdict.
2. 067.2-06-SUMMARY.md correctly reflects the UAT outcome (closure / open / pending).
3. ROADMAP.md status matches: `[x]` only on full closure.
4. STATE.md current position is updated.
</verification>

<success_criteria>
- 067.2-HUMAN-UAT.md scoreboard exists with all 12 D-067.2-NN rows verbatim from CONTEXT.md, plus 2 NR rows, plus the completion gate.
- Every row has a Verdict line filled in (GREEN / RED / n/a-with-reason).
- 067.2-06-SUMMARY.md correctly summarizes the outcome on the right branch.
- ROADMAP.md Phase 067.2 entry is `[x]` ONLY if 12/12 + 2/2 GREEN-or-n-a.
- STATE.md `Current Position` reflects the actual state (closed / partial / pending).
- No source code outside `.planning/` is modified by Plan 06.
- The strict-gate language ("Partial closures... NOT acceptable for this phase") appears verbatim in 067.2-HUMAN-UAT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md` per the branch-appropriate template in Task 3. The SUMMARY is the canonical phase-close record.
</output>
