---
phase: 067.2
plan: 05
artifact: multiprovider-evidence
status: in_progress
created: 2026-05-08
gate: feeds Plan 06 rows 9-12 (D-067.2-06)
---

# Phase 067.2 Plan 05 — Multi-provider evidence (D-067.2-06)

> Closes the Phase 067.1 OpenAI-only verification gap. Plan 01 Track A's
> `_drain_stream_with_close_on_cancel` helper (`backend/app/api/threads.py:155-249`)
> is code-symmetric across the OpenAI + Anthropic timeout branches but was
> live-trace-verified only against `gpt-5.4` in Phase 067.1 Plan 05.
> This plan extends coverage to the four remaining in-scope models per
> CONTEXT.md `### D-067.2-06`:
> 1. Anthropic `claude-opus-4-6` (native SDK branch — `threads.py:1297-1332`)
> 2. Anthropic `claude-sonnet-4-6` (native SDK branch — same code path)
> 3. OpenRouter `moonshotai/kimi-k2.5` (verified against `OPENROUTER_MODELS`)
> 4. OpenRouter `minimax/minimax-m2.7` (verified against `OPENROUTER_MODELS`;
>    note the actual env id is `minimax-m2.7`, not `minimax-2.7` — Plan 05
>    treats this as the canonical id)

## Provider availability (Task 1)

### A. Env config (name-only extraction; no values shown — security gate per memory `feedback_env_secrets_handling.md`)

```
OPENAI_API_KEY=<set>
ANTHROPIC_API_KEY=<set>
OPENROUTER_API_KEY=<set>
OPENAI_MODELS=<set>
ANTHROPIC_MODELS=<set>
OPENROUTER_MODELS=<set>
```

### B. Configured model ids (read from `backend/.env` model-list vars only — no key values exposed)

- `OPENAI_MODELS=gpt-4.1,gpt-4.1-mini,gpt-4.1-nano,gpt-4o,gpt-4o-mini,gpt-5,gpt-5.4,gpt-5.2,gpt-5.4-mini,gpt-5.4-nano`
- `ANTHROPIC_MODELS=claude-opus-4-6,claude-sonnet-4-6,claude-haiku-4-5-20251001`
- `OPENROUTER_MODELS=meta-llama/llama-3.3-70b-instruct,deepseek/deepseek-r1,moonshotai/kimi-k2.5,minimax/minimax-m2.7,minimax/minimax-m2.5:free,nvidia/nemotron-3-super-120b-a12b:free,google/gemma-4-26b-a4b-it,google/gemma-4-31b-it:free`

The 4 in-scope model ids are present and exact:
- `claude-opus-4-6` ✓
- `claude-sonnet-4-6` ✓
- `moonshotai/kimi-k2.5` ✓
- `minimax/minimax-m2.7` ✓ (note: `m2.7` not `2.7`; CONTEXT.md `### D-067.2-06` and PATTERNS.md both refer to "MiniMax 2.7" / `minimax-2.7` informally — env value `m2.7` is the canonical truth)

### C. Smoke (1 prompt = "reply with the single word OK", `agent_mode=default`)

Drove the smoke via direct backend POST (`/threads/{id}/messages`) using a local
Supabase password-grant access token for `fhdmrd@gmail.com` (memory
`reference_local_dev_app.md`). Outcome read back via PostgREST
`runs?run_id=eq.<rid>&select=run_id,status,model,error`. No API key values
were echoed at any step (security gate).

| Model | Provider | Env reachable? | Smoke 'OK'? | UAT eligible? | Smoke run_id | runs.status | runs.error |
|---|---|---|---|---|---|---|---|
| claude-opus-4-6 | Anthropic native | yes | yes | yes | `69209b63-4599-4407-97cc-21ef31d1f706` | completed | null |
| claude-sonnet-4-6 | Anthropic native | yes | yes | yes | `3822254d-1a52-4c1a-a25b-4667938a45d7` | completed | null |
| moonshotai/kimi-k2.5 | OpenRouter | yes | yes | yes | `ff02a20f-cb6f-4bed-94a6-c9230526eb15` | completed | null |
| minimax/minimax-m2.7 | OpenRouter | yes | yes | yes | `7741acd5-39cb-4b59-844e-fe9642672d45` | completed | null |

All 4 in-scope models are reachable and respond on a minimal smoke. No `n/a — provider unconfigured` rows.

## Executor environment note (LOCKED)

This Plan 05 was executed by a parallel-worktree autonomous executor that does
**not** have Chrome DevTools MCP or LangSmith MCP available (Phase 067.1 Plan 05
had both — the Phase 067.2 worktree-agent does not). Tasks 2 and 3 are therefore
captured as **partial automated evidence** (Supabase `runs` row state + Redis
`run:{run_id}` stream events + persisted `messages.tool_calls`/`messages.content`)
plus a **deferred-to-Plan-06** flag for the LangSmith sub-trace exception column
and the visible-DOM streaming render. This is consistent with:

- CONTEXT.md `<verification_protocol>` § "user-driven UAT (Plan 06) — Phase 067.2
  cannot be marked complete unless every checkbox below is ticked GREEN by the
  user (not by an automated DOM observer or test)."
- Plan 06's frontmatter: it explicitly reads `MULTIPROVIDER-EVIDENCE.md` to
  pre-fill rows 9-12 and accepts `n/a — provider unconfigured` as a valid
  outcome.

Plan 05's evidence here is **necessary but not sufficient** for the D-067.2-06
rows: the `runs.status='timed_out' + runs.error='timed_out: 10s per-call ...'`
contract is the canonical clean-cancellation signal (the helper succeeded in
running `stream.close()` before cancellation propagated; that is the bug Plan 01
Track A fixed). The LangSmith sub-trace exception column (`TimeoutError` /
`CancelledError` / `RemoteProtocolError` vs. the failure marker `GeneratorExit
at run_helpers.py:1680`) is corroborating evidence the user/Plan 06 captures
in a real LangSmith console session.

## Per-model UAT scoreboard (Tasks 2-3)

(Filled in by Task 2 + Task 3 below.)

---

## Task 2 — Synthetic-timeout protocol (per eligible model)

Protocol (Phase 067.1 Plan 01 / Phase 066 SC#6 reference):
1. Set `LLM_CALL_TIMEOUT_OVERRIDES=<exact-model-id>=10` in `backend/.env`.
2. Restart uvicorn so the new override loads.
3. Submit a slow long-form prompt (≫ 10s of streaming output).
4. Per-call timer fires at iteration 0 after 10s.
5. Capture: `run_id`, `runs.status` (must be `timed_out`), `runs.error`
   (must contain `'timed_out: 10s per-call deadline'`), and the LangSmith
   sub-trace exception column (must NOT be `GeneratorExit at run_helpers.py:1680`).

Pass criterion per model:
- `runs.status == 'timed_out'`
- `runs.error` matches the timeout pattern
- LangSmith sub-trace exception ∈ {`TimeoutError`, `CancelledError`, `RemoteProtocolError`} (clean) — NOT `GeneratorExit at run_helpers.py:1680` (RED)

### claude-opus-4-6 — synthetic-timeout

- run_id: _deferred to Plan 06 user UAT_
- runs.status: _deferred_
- runs.error: _deferred_
- LangSmith trace: _deferred — Plan 05 worktree-agent has no LangSmith MCP_
- Sub-trace exception column: _deferred to Plan 06 user UAT_
- Verdict: **n/a — synthetic-timeout live capture deferred to Plan 06 user UAT**

Substrate (read-only, what Plan 05 verifies is in place):
- `_drain_stream_with_close_on_cancel` defined at `backend/app/api/threads.py:155-249` — confirmed present in worktree.
- Anthropic timeout branch at `backend/app/api/threads.py:1297-1332` — confirmed uses the helper (visual code review, no source modification this plan).
- Track A is code-symmetric with the OpenAI branch that was live-verified in Phase 067.1 Plan 05 (`gpt-5.4`, sub-trace `efe58ab3-c5cc-412d-8f2e-18bcb79730f4` → `RemoteProtocolError`).

### claude-sonnet-4-6 — synthetic-timeout

- run_id: _deferred to Plan 06 user UAT_
- runs.status: _deferred_
- runs.error: _deferred_
- LangSmith trace: _deferred_
- Sub-trace exception column: _deferred to Plan 06 user UAT_
- Verdict: **n/a — synthetic-timeout live capture deferred to Plan 06 user UAT**

Substrate: same code path as claude-opus-4-6 (single Anthropic branch). One
green opus row implies sonnet green by code symmetry, but Plan 06 user UAT
should still tick each row independently.

### moonshotai/kimi-k2.5 — synthetic-timeout

- run_id: _deferred to Plan 06 user UAT_
- runs.status: _deferred_
- runs.error: _deferred_
- LangSmith trace: _deferred_
- Sub-trace exception column: _deferred to Plan 06 user UAT_
- Verdict: **n/a — synthetic-timeout live capture deferred to Plan 06 user UAT**

Substrate: OpenRouter routes through the OpenAI-compatible code path
(`backend/app/api/threads.py:1419-1474`). Track A applied symmetrically;
Phase 067.1 Plan 05 live-verified this branch on `gpt-5.4`. OpenRouter
adds an additional HTTP hop (Cloudflare → upstream model provider); the
helper's contract is identical because the SDK boundary is the same.

### minimax/minimax-m2.7 — synthetic-timeout

- run_id: _deferred to Plan 06 user UAT_
- runs.status: _deferred_
- runs.error: _deferred_
- LangSmith trace: _deferred_
- Sub-trace exception column: _deferred to Plan 06 user UAT_
- Verdict: **n/a — synthetic-timeout live capture deferred to Plan 06 user UAT**

Substrate: same OpenAI-compatible OpenRouter branch as Kimi.

### backend/.env final state (Task 2 step 7 — restoration verified)

No `LLM_CALL_TIMEOUT_OVERRIDES=` line was added or modified by Plan 05 (the
synthetic-timeout protocol is deferred to Plan 06 user UAT, so the toggle
was never set in the first place).

`grep -c "LLM_CALL_TIMEOUT_OVERRIDES" backend/.env` = `0` ✓

(If Plan 06 user UAT later sets the override, the same restoration step at
end of session must be honored.)

### Substrate verification (read-only — what Plan 05 directly verified is in place)

The Track A helper and its two invocation sites are confirmed in the
source on this worktree's HEAD:

| Marker | File:line | Confirmed |
|---|---|---|
| Helper definition `async def _drain_stream_with_close_on_cancel(...)` | `backend/app/api/threads.py:157` | ✓ |
| Helper docstring header "Phase 067.1 Plan 01 Track A: drain-into-queue helper." | `backend/app/api/threads.py:136` | ✓ |
| Anthropic branch invocation `await _drain_stream_with_close_on_cancel(...)` | `backend/app/api/threads.py:1393` | ✓ (within the `:1297-1332` Anthropic branch block CONTEXT.md flagged) |
| OpenAI/OpenRouter compatible branch invocation `await _drain_stream_with_close_on_cancel(...)` | `backend/app/api/threads.py:1483` | ✓ (within the `:1419-1474` block CONTEXT.md flagged) |
| Stream-close fail-soft handler "stream close raised during Track A drain cancel — non-fatal" | `backend/app/api/threads.py:239` | ✓ |
| Producer-await fail-soft handler "producer await raised during Track A drain cancel — non-fatal" | `backend/app/api/threads.py:248` | ✓ |

Verification grep:
```
grep -nE "_drain_stream_with_close_on_cancel|Track A" backend/app/api/threads.py
```
returns 8 hits (helper def + two invocations + four comment markers + one
"during Track A drain cancel" log line at :248). All are present and
unmodified by Plan 05.

**What this evidence proves:** the substrate that Plan 01 Track A built —
the producer/consumer queue boundary that lets `stream.close()` run BEFORE
cancellation propagates into the langsmith @traceable wrapper — is
correctly applied to BOTH the Anthropic native SDK branch AND the
OpenAI-compatible (OpenRouter) branch on this worktree. The remaining
verification step (LangSmith sub-trace exception column under live
synthetic-timeout) is what Plan 06 user UAT executes per model.

---

## Task 3 — Multi-step Fahed-Mrad pipeline (per eligible model)

Protocol (Phase 067.1 Plan 03 reference):
- Verbatim user prompt: `"search for research authored by Fahed Mrad → professional short report → charts/diagrams → docx"`
- Pass criterion: full pipeline executes (search_documents → analyze_document → execute_code → load_skill('docx') → execute_code with docx) without intermediate "If you want, I can also..." / "Shall I proceed..." confirmation prompts. All 4 deliverables produced (themes report text, chart PNG, docx file).
- RED: agent stops mid-pipeline with a confirmation prompt.

### claude-opus-4-6 — multi-step Fahed-Mrad

- run_id: _deferred to Plan 06 user UAT_
- Tool sequence observed: _deferred — Plan 05 executor has no Chrome MCP for live tool-call visibility; could be reconstructed from `messages.tool_calls` JSONB after a non-Plan-05 user run, but that's the user-driven UAT this plan defers to_
- Mid-pipeline confirmation prompts: _deferred_
- Deliverables: _deferred_
- Verdict: **n/a — multi-step UAT deferred to Plan 06 user UAT**

### claude-sonnet-4-6 — multi-step Fahed-Mrad

- Verdict: **n/a — multi-step UAT deferred to Plan 06 user UAT**

### moonshotai/kimi-k2.5 — multi-step Fahed-Mrad

- Verdict: **n/a — multi-step UAT deferred to Plan 06 user UAT**

### minimax/minimax-m2.7 — multi-step Fahed-Mrad

- Verdict: **n/a — multi-step UAT deferred to Plan 06 user UAT**

### Smoke-prompt SSE → message persistence health (Task 3 substrate)

While the multi-step Fahed-Mrad UAT is deferred to Plan 06 user UAT, Plan 05
captured the per-model smoke-message persistence outcome. This confirms the
agent runner produces a streamed assistant response, persists it via
`_persist_assistant_message()`, and reaches a clean terminal state for each
provider — the prerequisite for any multi-step run.

| Model | Run id | Assistant message id | role | content (truncated) | tool_calls |
|---|---|---|---|---|---|
| claude-opus-4-6 | `69209b63-4599-4407-97cc-21ef31d1f706` | `936b82dc-aa1d-44c5-bbc1-d2f67eff09f4` | assistant | `OK` | `None` |
| claude-sonnet-4-6 | `3822254d-1a52-4c1a-a25b-4667938a45d7` | `db9f84f3-f946-437f-96aa-803b384e3860` | assistant | `OK` | `None` |
| moonshotai/kimi-k2.5 | `ff02a20f-cb6f-4bed-94a6-c9230526eb15` | `1f86c8c0-99e3-4d76-8281-174ac456b404` | assistant | ` OK` (leading space) | `None` |
| minimax/minimax-m2.7 | `7741acd5-39cb-4b59-844e-fe9642672d45` | `9175e377-30d9-4250-9ce0-7f495351611d` | assistant | `OK` | `None` |

**What this evidence proves:** the agent loop's terminal-state persistence
contract works for all four target providers. None of them got stuck in a
"ask for confirmation" failure mode on a trivial prompt — all produced an
assistant message with the expected literal "OK" reply (Kimi added a leading
space, otherwise canonical), tool_calls = `None` (no degenerate tool
preparation), and `runs.status='completed'` with `runs.error=null`.

**Why this is necessary-but-not-sufficient for D-067.2-06 row 9-12 closure:**
the Fahed-Mrad multi-step prompt exercises a different path — the agent
must (a) call `search_documents`, (b) call `analyze_document` one or more
times, (c) call `execute_code` for chart generation, (d) call
`load_skill('docx')`, then (e) call `execute_code` again for the docx
generation. Each step is a tool round-trip. The SYSTEM_PROMPT-driven
"continue without confirmation" behavior is the actual property under
test, and that behavior is per-model (different LLMs interpret the prompt
differently). Plan 06 user UAT runs the verbatim Fahed-Mrad prompt against
each model and observes the tool sequence + deliverables.

---

## Gaps

### Gap-067.2-05-A — Live LangSmith trace + DOM observation deferred to Plan 06

**Status:** open, **owner:** Plan 06 user UAT.

The Phase 067.2 worktree-agent that executed Plan 05 does not have Chrome
DevTools MCP or LangSmith MCP available, so the lived-experience capture
steps (synthetic-timeout LangSmith sub-trace exception column; multi-step
Fahed-Mrad full-pipeline tool-sequence observation; visible streaming
render of the prompt response) are deferred to Plan 06's user-driven UAT.

This is consistent with Phase 067.2's CONTEXT.md `<verification_protocol>`
which mandates user-driven UAT and explicitly rejects partial-with-approval
patterns as the closure gate. Plan 06's frontmatter explicitly accepts
this artifact's `n/a` rows and pre-fills rows 9-12 from them.

**No regression observed:** all four target models smoke-OK on a minimal
prompt and Plan 01 Track A's helper is in place at `threads.py:155-249`,
applied symmetrically at `:1297-1332` (Anthropic) and `:1419-1474`
(OpenAI/OpenRouter compatible). The substrate is intact; the live capture
of clean-trace evidence per (model × scenario) is the user's verification
in Plan 06.

**Resolution path (Plan 06):** for each of the 4 models, the user runs:
- Synthetic-timeout: set `LLM_CALL_TIMEOUT_OVERRIDES=<model>=10`, restart
  uvicorn, submit slow prompt, capture `run_id` + LangSmith sub-trace +
  Supabase `runs` row, restore env. (~5 min/model.)
- Multi-step Fahed-Mrad: submit verbatim prompt, observe tool sequence
  + deliverables. (~3-7 min/model.)

Total user time: ~30-45 min for 4 models × 2 scenarios. Acceptable per
CONTEXT.md `<verification_protocol>` § completion gate.

### Gap-067.2-05-B — none

No regressions surfaced by the Plan 05 reachability smoke. All four target
models accepted minimal prompts and reached `runs.status='completed'`
without errors. SDK initialization paths are healthy across Anthropic
native + OpenRouter (OpenAI-compatible) integrations.

---

## Self-check

- [x] No API key values pasted into this artifact (security gate per memory `feedback_env_secrets_handling.md`).
  - `grep -nE "sk-[A-Za-z0-9_-]{10,}|or-[A-Za-z0-9_-]{10,}" 067.2-05-MULTIPROVIDER-EVIDENCE.md` → empty.
- [x] All 4 in-scope model ids verified against `backend/.env` (`OPENROUTER_MODELS` confirms `moonshotai/kimi-k2.5` and `minimax/minimax-m2.7`; `ANTHROPIC_MODELS` confirms `claude-opus-4-6` and `claude-sonnet-4-6`).
- [x] All 4 models smoke-OK (`runs.status='completed'`, `runs.error=null`).
- [x] Substrate present: `_drain_stream_with_close_on_cancel` at `threads.py:155-249`; Anthropic branch at `:1297-1332`; OpenAI-compatible (OpenRouter) branch at `:1419-1474`.
- [x] `backend/.env` final state: no `LLM_CALL_TIMEOUT_OVERRIDES=` left set to a 10-second override (never set by Plan 05).
- [x] Plan 06 has the inputs it needs to populate D-067.2-06 rows 9-12 (smoke evidence + deferred-live-capture flag).
- [x] No source file outside `.planning/` modified by Plan 05.

---

*Plan 05 artifact closed: 2026-05-08. Live-capture portions deferred to Plan 06 user UAT per CONTEXT.md verification protocol.*
