---
phase: 067.2
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified: []
autonomous: true
requirements:
  - STREAM-04-correctness
must_haves:
  truths:
    - "Investigation runs AFTER Plan 01 lands — depends_on:[01] ensures the guardedSetMessages no-op race is closed before this audit, so any remaining 'no confidence/suggestions in UI' is observable as a TRUE render-side gap (not a side effect of D-067.2-01)."
    - "Investigation produces an evidence-backed verdict: (a) state populated AND UI renders → no-op outcome (close as resolved by Plan 01), or (b) state populated but UI does NOT render → render-side gap (add render code), or (c) state NOT populated → backend emit gap or SSE-parser regression (escalate)."
    - "If the verdict is no-op, this plan ships an INVESTIGATION-NOTES.md artifact recording the React DevTools $r.confidence + $r.suggestions evidence and closes without code change."
    - "If the verdict requires render-side patches, the patches are line-anchored against MessageItem.tsx:89-98 (existing render block — already present; the gap is upstream)."
    - "The plan does NOT pre-write blind render-side patches — every code change is gated on the investigation verdict."
  artifacts:
    - path: ".planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md"
      provides: "Evidence trail: DevTools/Network capture of confidence + suggestions state, verdict, code-change scope (or no-op closure)"
      contains: "Verdict:"
  key_links:
    - from: "Plan 01 ChatArea.tsx fix landed"
      to: "Investigation: re-run Fahed-Mrad search-only prompt"
      via: "Chrome MCP or manual DevTools session against localhost:5173"
      pattern: "Verdict:"
    - from: "Investigation evidence (state populated + UI renders)"
      to: "Plan 04 closes no-op"
      via: "INVESTIGATION-NOTES.md verdict 'a) no-op'"
      pattern: "no-op"
---

<objective>
Per CONTEXT.md `### D-067.2-04`, the full render path for `message.confidence` + `message.suggestions` already exists end-to-end:
- Backend emits at `threads.py:2387` (confidence) and `:2428` (suggestions).
- Frontend SSE callbacks update state at `useMessages.ts:303-310` (onConfidence) and `:313-316` (onSuggestions).
- Type fields exist at `frontend/src/types/index.ts:98-99`.
- Render blocks exist at `MessageItem.tsx:89-98`.

This strongly suggests D-067.2-04 is a SIDE EFFECT of D-067.2-01 — the confidence/suggestions events arrive during the `guardedSetMessages` no-op window and are silently dropped. After Plan 01 lands, the events should land in state and render correctly with NO additional code change.

This plan is **investigation-first** per CONTEXT.md `### D-067.2-04` and PATTERNS.md § 3 + § 4. It depends on Plan 01 (which closes the upstream race). Its goal is to produce an evidence-backed verdict and either close as a no-op or surgically patch a confirmed render-side gap.

Purpose: avoid the anti-pattern of writing speculative render-side patches before observing live state. Every code change must be gated on a reproducible failure mode in the post-Plan-01 build.

Output: `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md` with the verdict, evidence, and (if applicable) the line-anchored patches landed.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-CONTEXT.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-PATTERNS.md
@frontend/src/components/chat/MessageItem.tsx
@frontend/src/hooks/useMessages.ts
@frontend/src/types/index.ts
@frontend/src/lib/api.ts
@backend/app/api/threads.py

<interfaces>
<!-- Existing render path — already present; investigation verifies whether each layer fires. -->

Backend emits (threads.py:2387 + :2428):
```python
await _emit(redis, run_id, 'confidence', level=level, avg_similarity=round(final_avg, 4), disclaimer=disclaimer)
# ...
await _emit(redis, run_id, 'suggestions', questions=questions[:3])
```

Frontend SSE callbacks (useMessages.ts:303-316):
```ts
onConfidence: (level, avgSimilarity, disclaimer) => {
  setMessages((prev) =>
    prev.map((m) =>
      m.id === assistantId
        ? { ...m, confidence: { level, avg_similarity: avgSimilarity, disclaimer } }
        : m,
    ),
  )
},
onSuggestions: (questions: string[]) => {
  setMessages((prev) =>
    prev.map((m) => (m.id === assistantId ? { ...m, suggestions: questions } : m)),
  )
},
```

Type fields (types/index.ts:98-99):
```ts
confidence?: ConfidenceResult
suggestions?: string[]
```

Render blocks (MessageItem.tsx:89-98 — ALREADY PRESENT):
```tsx
{message.confidence && <ConfidenceBadge confidence={message.confidence} />}
{message.citations && message.citations.length > 0 && (
  <CitationList citations={message.citations} />
)}
{!isStreaming && message.suggestions && message.suggestions.length > 0 && onSendMessage && (
  <SuggestionPills questions={message.suggestions} onSelect={onSendMessage} />
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the full render path is intact (read-only audit) — confirm no-op hypothesis or identify gap</name>
  <files>(read-only — no file modifications)</files>
  <read_first>
    - frontend/src/components/chat/MessageItem.tsx (entire file — confirm the render blocks at lines 89-98 are still present after Plan 01 lands; line numbers may have shifted slightly)
    - frontend/src/hooks/useMessages.ts (search for `onConfidence` and `onSuggestions` callbacks — confirm they fire `setMessages` not `guardedSetMessages` for the confidence/suggestions branches; PATTERNS.md confirms they use `setMessages` directly which is the right idiom)
    - frontend/src/types/index.ts (confirm `Message.confidence` and `Message.suggestions` fields are still declared at lines 98-99 with the shapes documented in PATTERNS.md § 4)
    - frontend/src/lib/api.ts — search `subscribeToRun` event-name dispatch table (find the lines that map SSE event names → callback invocations; confirm both `'confidence'` and `'suggestions'` are dispatched)
    - backend/app/api/threads.py — `grep -n "_emit(redis, run_id, 'confidence'" backend/app/api/threads.py` AND `grep -n "_emit(redis, run_id, 'suggestions'" backend/app/api/threads.py` to confirm both emits are still present
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-CONTEXT.md § D-067.2-04 (locked investigation-first decision)
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-PATTERNS.md § 3 + § 4 (full render-path inventory)
  </read_first>
  <action>
    Read-only audit. For each layer of the render path, confirm presence/absence and record evidence:

    **Layer 1 — Backend emit:** run `grep -nE "_emit\\(redis, run_id, '(confidence|suggestions)'" backend/app/api/threads.py`. Both event names must appear at least once. Record line numbers.

    **Layer 2 — SSE-parser dispatch:** read `frontend/src/lib/api.ts` for the `subscribeToRun` function (around lines 100-300; find by `grep -n "subscribeToRun" frontend/src/lib/api.ts`). Locate the event-name dispatch table or switch — confirm both `'confidence'` and `'suggestions'` route to their respective callbacks. If either is missing or routed to a no-op, that's a confirmed regression — record as "Layer 2 gap".

    **Layer 3 — useMessages callback shape:** confirm onConfidence + onSuggestions callbacks at useMessages.ts:303-316 still call `setMessages` (NOT `guardedSetMessages`). PATTERNS.md says they use `setMessages` directly, which is correct — those are terminal-status-flip-style updates that must run regardless of viewing thread. Record verification.

    **Layer 4 — Type fields:** confirm `Message.confidence?: ConfidenceResult` and `Message.suggestions?: string[]` at types/index.ts:98-99 (or wherever they live now). Record line numbers.

    **Layer 5 — Render blocks:** confirm the three blocks at MessageItem.tsx:89-98 — ConfidenceBadge, CitationList (already shipped), SuggestionPills with `!isStreaming` + length-check + onSendMessage gate. Record line numbers.

    **Side observation about suggestions emit gating:** PATTERNS.md § 3 step 4 flags that `_emit(redis, run_id, 'suggestions', ...)` at threads.py:2428 lives in the "post-`done`-event block" — meaning it only fires on a fully successful run. If the user reports "no suggestions on a successful run", that's a render-side or SSE-parser issue. If the user reports "no suggestions on a failed/cancelled run", that's expected behavior under current code (the emit doesn't fire on failure paths). The investigation should distinguish.

    Output: write `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md` with:
    - Layer-by-layer audit table (Layer | File | Line | Present? | Notes)
    - Suggested verdict based on static audit (pre-runtime test): "all layers present → predicted no-op" / "Layer N gap identified → predicted patch needed"
  </action>
  <verify>
    <automated>test -f .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md && grep -c "Layer" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md</automated>
    Plus:
    1. `grep -nE "_emit\\(redis, run_id, '(confidence|suggestions)'" backend/app/api/threads.py | wc -l` → must be ≥ 2 (both emits still present).
    2. `grep -c "ConfidenceBadge" frontend/src/components/chat/MessageItem.tsx` → must be ≥ 1.
    3. `grep -c "SuggestionPills" frontend/src/components/chat/MessageItem.tsx` → must be ≥ 1.
    4. INVESTIGATION-NOTES.md must contain a "Layer-by-layer audit" section AND a "Predicted verdict" line.
  </verify>
  <done>
    INVESTIGATION-NOTES.md drafted with full layer audit. Predicted verdict recorded based on static evidence. All grep gates pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-driven runtime DevTools investigation — capture $r.confidence + $r.suggestions evidence; lock verdict</name>
  <what-built>
    Plan 01's ChatArea.tsx race fix is live in the dev build. Task 1 produced a static-audit verdict in INVESTIGATION-NOTES.md predicting whether confidence/suggestions render correctly. This checkpoint asks the user (or an executor with Chrome MCP) to confirm the runtime behavior and lock the actual verdict.
  </what-built>
  <how-to-verify>
    Pre-conditions:
    - Plan 01 (067.2-01-PLAN.md) is fully landed and the dev frontend at `localhost:5173` serves the new bundle (verify by `grep useLayoutEffect` in DevTools Sources tab on `ChatArea.tsx`).
    - Backend running. Logged in as test user `fhdmrd@gmail.com` / `123456`.

    Procedure (the executor or user runs this; if executor has Chrome MCP, drive the browser; otherwise this is a user-checkpoint):
    1. Open `localhost:5173`. Open DevTools → Components tab (React DevTools).
    2. Submit a Fahed-Mrad SEARCH-ONLY prompt (e.g. "search for any document about fahed mrad and summarize").
    3. Wait for the run to complete (`done` event fires server-side; assistant message turns from streaming to final).
    4. Select the latest assistant message in React DevTools (so it becomes the `$r` selected fiber).
    5. In the Console tab, run:
       ```js
       JSON.stringify($r.props.message.confidence)
       JSON.stringify($r.props.message.suggestions)
       ```
       (Adapt: if MessageItem accesses `confidence` differently, navigate the props tree; the shape is `message.confidence` per types/index.ts:98.)
    6. Visually inspect the chat UI: is there a confidence badge (ConfidenceBadge component) visible? Are there clickable follow-up suggestion pills (SuggestionPills) visible?

    Capture the evidence in INVESTIGATION-NOTES.md under a "Runtime evidence" section:
    - `confidence` state populated? yes/no, paste the JSON
    - `suggestions` state populated? yes/no, paste the JSON
    - ConfidenceBadge visible in DOM? yes/no
    - SuggestionPills visible in DOM? yes/no

    Determine verdict:
    - **Verdict (a) NO-OP:** state populated for both AND UI renders both → close Plan 04 with NO code change. INVESTIGATION-NOTES.md final section: "Verdict: (a) no-op — D-067.2-04 was a side effect of D-067.2-01; render path was always intact; the upstream race fix from Plan 01 closed it."
    - **Verdict (b) RENDER-SIDE GAP:** state populated for at least one field BUT UI does NOT render that field → file `frontend/src/components/chat/MessageItem.tsx` needs a render-side patch. INVESTIGATION-NOTES.md captures the populated state JSON + the missing DOM element. Proceed to Task 3.
    - **Verdict (c) STATE GAP:** state NOT populated for at least one field → SSE-parser regression OR backend-emit gap. INVESTIGATION-NOTES.md captures absence; escalate via Plan 04 SUMMARY documenting the new failure mode (do NOT attempt to patch this in Plan 04 — its scope is render-side; if state isn't populated, it's a different root cause that needs a new plan or extension).
  </how-to-verify>
  <resume-signal>Type one of:
    - `verdict: a` (no-op — close Plan 04 without code change)
    - `verdict: b confidence` (only confidence has render-side gap)
    - `verdict: b suggestions` (only suggestions has render-side gap)
    - `verdict: b both` (both fields have render-side gaps)
    - `verdict: c <details>` (state not populated; escalate)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: (Conditional — only if Task 2 verdict is 'b') Add the missing render block(s) to MessageItem.tsx; commit</name>
  <files>frontend/src/components/chat/MessageItem.tsx</files>
  <read_first>
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md (Task 2's verdict + the populated-state JSON; required to know which field needs a patch)
    - frontend/src/components/chat/MessageItem.tsx lines 1-120 (re-locate the render blocks at MessageItem.tsx:89-98; confirm which is missing)
    - frontend/src/components/chat/ConfidenceBadge.tsx (if confidence is the missing one — confirm component still imports correctly)
    - frontend/src/components/chat/SuggestionPills.tsx (if suggestions is the missing one — confirm component still imports correctly)
  </read_first>
  <action>
    **Skip this task entirely if Task 2 verdict is `(a) no-op` or `(c) state gap`.** Mark Task 3 as `n/a` in the SUMMARY in those cases.

    If Task 2 verdict is `(b) render-side gap`, apply the minimal patch needed:

    **If `confidence` is missing render block:**
    Locate MessageItem.tsx line where the citations/suggestions render blocks live (around :89-98). Confirm the existing pattern:
    ```tsx
    {message.confidence && <ConfidenceBadge confidence={message.confidence} />}
    ```
    If this block is ALREADY there (verifiable via `grep -c "ConfidenceBadge" MessageItem.tsx` returning ≥ 1), the issue is something else (e.g. ConfidenceBadge component itself broken, conditional too restrictive); read `ConfidenceBadge.tsx` to find the actual render gap. Likely fix: ensure `<ConfidenceBadge>` doesn't gate on `!isStreaming` (it shouldn't — confidence shows during streaming once emitted), and that the `message.confidence` truthy check correctly handles the `ConfidenceResult` shape.

    **If `suggestions` is missing render block:**
    Confirm via `grep -c "SuggestionPills" MessageItem.tsx`. The existing pattern at :89-98 already gates on `!isStreaming && message.suggestions && message.suggestions.length > 0 && onSendMessage`. If suggestions are populated but UI doesn't render, the most likely cause is the `onSendMessage` prop not being threaded through to MessageItem in the parent — read `MessageList.tsx` for how it passes `onSendMessage` to `MessageItem`. Fix is likely a one-line prop-threading addition, NOT a render-block addition.

    **Constraint — minimal patch:** the diff in this task should be very small (1-5 lines). If the investigation reveals a structural issue (e.g. `<MessageItem>` is missing the `onSendMessage` prop entirely from a parent component), document the cascading change in INVESTIGATION-NOTES.md but apply only the minimum needed. If the cascade is large (>10 lines across 3+ files), update INVESTIGATION-NOTES.md with the full scope and escalate to a separate plan rather than expanding Plan 04 mid-flight.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "MessageItem\\|SuggestionPills\\|ConfidenceBadge" || echo "NO TS ERRORS"</automated>
    Plus:
    1. If verdict was `(b) confidence`: `grep -c "ConfidenceBadge confidence={message.confidence}" frontend/src/components/chat/MessageItem.tsx` → must be ≥ 1.
    2. If verdict was `(b) suggestions`: `grep -c "SuggestionPills" frontend/src/components/chat/MessageItem.tsx` → must be ≥ 1.
    3. INVESTIGATION-NOTES.md "Patch applied" section documents: which field, the line range, the diff summary.
  </verify>
  <done>
    Conditional outcomes:
    - Verdict (a) no-op or (c) state gap → Task 3 marked n/a, no file modified, SUMMARY explains.
    - Verdict (b) → MessageItem.tsx (or surrounding component) patched minimally, TypeScript compiles, INVESTIGATION-NOTES.md updated with patch evidence.
    Plan 06 user-UAT D-067.2-04 checkbox revalidates the live behavior in either case.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Investigation-first plan; no new endpoints, no new auth surface. Conditional Task 3 may add a single conditional render block, which is React text-children rendering of LLM-generated `confidence` (object with level/avg_similarity/disclaimer) or `suggestions` (string[]). Pre-existing trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067.2-04-01 | Tampering / Injection | If verdict (b) leads to a render patch, the patch renders LLM-emitted strings (`suggestions[i]`, `confidence.disclaimer`) | accept | React text-children auto-escape applies. The existing render pattern at MessageItem.tsx:89-98 already renders these via React components (`<SuggestionPills questions={...}>`, `<ConfidenceBadge confidence={...}>`). No `dangerouslySetInnerHTML` involved. Verified by reading the existing render path — Task 3 patches preserve this pattern. |
| T-067.2-04-02 | (no other applicable category) | Investigation evidence captured in INVESTIGATION-NOTES.md | accept | INVESTIGATION-NOTES.md is a developer-only artifact in `.planning/`. May contain JSON dumps of `confidence` / `suggestions` from a test run on user fhdmrd@gmail.com — these are non-sensitive (LLM-generated metadata); INVESTIGATION-NOTES.md is committed to the same repo as the rest of the planning artifacts. Not a new disclosure surface. |
</threat_model>

<verification>
1. INVESTIGATION-NOTES.md exists with Layer audit + Runtime evidence + Verdict.
2. If verdict (b): TypeScript compiles cleanly with the minimal patch.
3. Plan 06 user-UAT D-067.2-04 checkbox revalidates.
</verification>

<success_criteria>
- INVESTIGATION-NOTES.md exists, is committed, and contains:
  - Static audit: layer-by-layer table.
  - Runtime evidence: $r.confidence + $r.suggestions JSON capture (or n/a-with-reason).
  - Verdict (a / b / c).
  - If verdict (b): patch description + line-anchored diff summary.
  - If verdict (c): escalation note describing the new failure mode and recommendation for a follow-on plan.
- If verdict was (a): no source file is modified by this plan.
- If verdict was (b): exactly the minimum patch is applied; TypeScript compiles.
- If verdict was (c): no source file is modified by Plan 04; the SUMMARY explicitly recommends a follow-on plan.
- The plan does NOT pre-write speculative render code that turned out to be unnecessary.
</success_criteria>

<output>
After completion, create `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-SUMMARY.md` documenting: the verdict, the evidence, whether code changed, and (if verdict (b)) the exact diff applied.
</output>
