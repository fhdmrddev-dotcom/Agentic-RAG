---
phase: 089-agent-loop-extraction-g-5-kickoff-uat
plan: 03
type: execute
wave: 2
depends_on: ["089-01"]
files_modified:
  - backend/app/services/agent_loop.py
  - backend/app/api/threads.py
  - backend/tests/test_provider_router.py
  - backend/tests/unit/test_explorer_agent.py
  - backend/tests/integration/test_058_concurrency.py
  - backend/tests/integration/test_059_disconnect.py
  - backend/tests/integration/test_061_producer_survives_disconnect.py
  - backend/tests/integration/test_061_ttl.py
  - backend/tests/integration/test_061_runs_table.py
  - backend/tests/integration/test_062_delete_happy.py
  - backend/tests/integration/test_062_multi_consumer_fanout.py
  - backend/tests/integration/test_062_stream_replay.py
  - backend/tests/integration/test_063_post_then_subscribe.py
  - backend/tests/integration/test_063_post_contract.py
  - backend/tests/integration/test_066_per_call_timer.py
  - backend/tests/integration/test_066_langsmith_clean.py
  - backend/tests/integration/test_066_sse_terminal.py
  - backend/tests/integration/test_066_terminal_classification.py
  - backend/tests/integration/test_067_4_suggestion_emit.py
  - backend/tests/integration/test_075_code_stdout_progressive.py
  - backend/tests/integration/test_075_tool_args_progress.py
  - backend/tests/integration/test_threads_skills.py
  - backend/tests/integration/test_089_agent_loop_result_seam.py
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "run_agent_loop in agent_loop.py owns the iteration loop, the three _on_chunk_* handlers, _persist_assistant_message, _persist_system_messages, the tool-dispatch round, and the post-loop emits — moved VERBATIM (no logic change)"
    - "threads.py agent_runner calls run_agent_loop(ctx, emit=_emit, emit_terminal=_emit_terminal, spawn=_spawn) and consumes the returned AgentLoopResult"
    - "_shielded_finalize (STAYS in threads.py) still receives a valid persisted message id + token totals + warnings via AgentLoopResult"
    - "All ~37 monkeypatch targets across 20 test files point at app.services.agent_loop.create_adaptive_streaming_chat; generate_thread_title patches STAY at app.api.threads"
    - "The full pytest suite is GREEN (no real-network calls, no connection-refused)"
    - "All 14 per-provider invariants (I1-I14) appear verbatim in agent_loop.py; the .neq('role','system') filter is UNTOUCHED in threads.py route handlers"
  artifacts:
    - path: "backend/app/services/agent_loop.py"
      provides: "The full run_agent_loop body + the three _on_chunk_* + persist functions + tool-dispatch round, moved verbatim"
      contains: "for iteration in range"
    - path: "backend/tests/integration/test_089_agent_loop_result_seam.py"
      provides: "Asserts the finalizer still gets a valid message id + token totals + warnings from the moved loop"
      contains: "AgentLoopResult"
  key_links:
    - from: "backend/app/api/threads.py"
      to: "backend/app/services/agent_loop.py"
      via: "agent_runner calls run_agent_loop(ctx, emit=_emit, ...)"
      pattern: "run_agent_loop\\("
    - from: "backend/app/api/threads.py"
      to: "AgentLoopResult"
      via: "_shielded_finalize reads result.persist / result.input_tokens_total / result.persisted_system_warnings"
      pattern: "AgentLoopResult|result\\.persist"
---

<objective>
The verbatim move (D-089-01..03): lift the iteration loop, the three provider chunk-handlers,
`_persist_assistant_message`/`_persist_system_messages`, the tool-dispatch round, the post-loop
emits, and the inner try/except OUT of `agent_runner` in `threads.py` INTO
`run_agent_loop(ctx)` in `agent_loop.py` — BYTE-IDENTICALLY. Wire the seam in `agent_runner`,
update every monkeypatch target so the suite stays GREEN, and add the `AgentLoopResult` seam test.

Purpose: FOUND-03 / SC#1 — this is G-5. The extraction changes file LOCATION only, never
behavior. This is the highest-risk-if-done-wrong task of v2.8: a careless "while-I'm-in-here"
cleanup re-opens the entire 075.x cross-provider cascade. The seam is operator-approved (Plan 01
checkpoint) before this plan runs.

Output: a clean run_agent_loop module + a GREEN suite + the finalizer seam intact. No behavior change.

GATE: This plan depends on the Plan 01 seam-review checkpoint. Do NOT start the move until the
operator has approved SEAM.md (the run_agent_loop signature + the B1-vs-B2 Category-B boundary).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-CONTEXT.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-RESEARCH.md
@backend/app/services/SEAM.md

<interfaces>
<!-- The EXACT seam-map line ranges to move (089-RESEARCH.md §"Extraction Seam Map" L391-420). -->
<!-- VERIFIED against the live threads.py 2026-05-30 (re-confirmed anchors below). -->
<!-- The body is lifted VERBATIM — copy these byte ranges from threads.py, do NOT rewrite. -->

MOVES to agent_loop.py (into run_agent_loop, D-089-01):
- iteration loop: `for iteration in range(max_iterations):` threads.py L1818 → end of loop body ~L2738
- _on_chunk_anthropic: L1944-2022 (anthropic native path L1897-2023)
- _on_chunk_google:    L2055-2143 (google native path L2025-2144)
- _on_chunk_openai:    L2245-2403 (openai/openrouter/ollama path L2146-2452)
- tool-dispatch round: L2583-2738 (ToolContext construction L2632-2660; dispatch_tool L2677; result append L2704-2737)
- _persist_assistant_message: L1659-1724 (returns msg id; idempotent via _message_persisted + fn-attr cache)
- _persist_system_messages:   L1736-1778 (role='system' warning rows, migration 048)
- post-loop emits: final_output_files L2753-2762, sources/citations/confidence L2866-2889, fallback-empty L2764-2780
- inner try/except (provider-error/exception handlers): L2782-2864
- suggestion-gen + stream_end: L2911-2984

STAYS in threads.py (D-089-02):
- send_message route shell L1224-3185; provider/model resolution L1293-1331; runs INSERT L1337; title-gen L1383-1421
- agent_runner producer shell L1423 → finally L3151 (outer try/except sets _terminal_status)
- _emit/_emit_terminal/_spawn L109-140 / L69-74 → passed into run_agent_loop as callables
- _shielded_finalize L3035-3142 (finalize_run → sentinel → expire → zrem order) — calls result.persist
- _reconstruct_history L1135-1221 — KEEP in threads.py, import into agent_loop.py (pure fn, no cycle) per SEAM.md
- get_snapshot/get_messages L763-874 / L1087-1132 — carry .neq("role","system") (Phase 086 landmine) — UNTOUCHED

Category-B setup (L1470-1627): per the operator-approved SEAM.md — B1 moves it into run_agent_loop,
B2 keeps it in agent_runner. Follow whichever the operator approved. Variables: thread_folder_id,
folder_subtree_ids, scoped_folder_path, active_system_prompt, active_tools, max_iterations, messages.

Per-provider invariants I1-I14 (089-RESEARCH.md L476-491) — each MUST survive verbatim:
I1 Anthropic end_turn L2562-2565 · I2/I3 Google thought_signature L2127-2136 + L2604-2608 + L1184-1188
I4/I5 DeepSeek reasoning_content L2313-2317 + L2620 · I6 Moonshot <think> filter L2280-2308
I7 empty-retry L2574-2580 · I8 force_no_tools L1868-1869 · I9 iteration_cap_dropped_tool_calls L2506-2520
I10 terminal race L3094-3118 (in _shielded_finalize, STAYS) · I11 transient retry L2476-2488
I12 TPM 429 L2460-2474 · I13 length-recovery L2530-2559 · I14 OpenRouter XML pre-inject L1876-1885 + L2191/L2364

GLM/MiniMax: ZERO provider-specific branches — they route through _on_chunk_openai (shared OpenAI-compat).
No extra invariant rows (089-RESEARCH L493-494).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the loop body + three _on_chunk_* + persist functions + tool-dispatch round into run_agent_loop VERBATIM, wire the seam in agent_runner</name>
  <read_first>
    - backend/app/services/SEAM.md — the operator-approved signature + B1/B2 boundary decision. FOLLOW IT EXACTLY.
    - backend/app/api/threads.py L1423-3151 (the whole agent_runner) — the source bytes; pay attention to the nested structure (run helpers nested inside agent_runner) and the Category-B setup L1470-1627.
    - 089-RESEARCH.md §"Extraction Seam Map" (L391-420) + §"Closure-Capture Inventory" (L422-472) — Categories A (RunContext), B (setup boundary), C (callables), D (loop-locals re-declared inside run_agent_loop), E (AgentLoopResult).
    - 089-RESEARCH.md §"Common Pitfalls" #2 (active_provider_name double-compute — do NOT dedupe), #3 (mutable accumulators stay loop-local), #5 (terminal-status race order must not move).
  </read_first>
  <files>backend/app/services/agent_loop.py, backend/app/api/threads.py</files>
  <action>
    Replace the run_agent_loop STUB (from Plan 01) with the VERBATIM-moved loop body. Move from threads.py into agent_loop.py, byte-for-byte (no logic change, D-089-03):
    1. `_persist_assistant_message` (L1659-1724), `_persist_system_messages` (L1736-1778) — move into run_agent_loop as nested functions (they close over loop-locals).
    2. The Category-B setup (L1470-1627) per the operator-approved B1/B2 decision in SEAM.md. If B1: move it into run_agent_loop's top. If B2: keep in agent_runner and add the computed values to the call (extend the seam exactly as SEAM.md specifies). Call `_reconstruct_history` via `from app.api.threads import _reconstruct_history` (pure fn, no cycle).
    3. The iteration loop `for iteration in range(max_iterations):` (L1818) through the end of the loop body (~L2738), INCLUDING the three `_on_chunk_anthropic`/`_on_chunk_google`/`_on_chunk_openai` handlers (nested inside the loop, they `nonlocal` the accumulators), the provider branch, the tool-dispatch round (ToolContext construction + dispatch_tool + result append), all the I1-I14 invariant code, the inner try/except (L2782-2864), the post-loop emits (L2753-2762, L2866-2889), and suggestion-gen + stream_end (L2911-2984).
    4. Re-declare ALL Category-D mutable accumulators (full_content, full_reasoning_content, persisted_tool_calls, input_tokens_total, output_tokens_total, _previous_files_in_run, _empty_retries, etc. — 089-RESEARCH L459) as LOCALS at the top of run_agent_loop (exactly where they're declared today at L1629-1657). The `_on_chunk_*` handlers `nonlocal` them — this keeps working because they stay nested inside run_agent_loop.
    5. Build and return an `AgentLoopResult` carrying: the bound `_persist_assistant_message` callable (or the cached id), input_tokens_total, output_tokens_total, _persisted_system_warnings, full_content (as full_content_final).
    6. In threads.py `agent_runner`: construct the frozen `RunContext` (the 9 Category-A fields, from the already-resolved values), call `result = await run_agent_loop(ctx, emit=_emit, emit_terminal=_emit_terminal, spawn=_spawn)`, and rewire `_shielded_finalize` to read `result.persist` / `result.input_tokens_total` / `result.output_tokens_total` / `result.persisted_system_warnings` instead of the old closure variables. The finalizer step ORDER (persist → finalize_run → sentinel → expire → zrem) stays byte-identical (Pitfall 5 / I10).
    FORBIDDEN (D-089-03): collapsing the three chunk handlers; deduping the L1889/L2231 active_provider_name double-compute (Pitfall 2); fixing the two deferred Anthropic bugs (D-089-15); touching dispatch_tool/ToolContext; touching the .neq("role","system") filter; re-resolving provider/model inside the loop. Move bytes only.
    Add to agent_loop.py's import block every symbol the moved code reads from threads.py-imported modules (stream_anthropic, stream_google, create_adaptive_streaming_chat, dispatch_tool/ToolContext/ToolResult, trim_messages_to_fit, get_per_call_timeout_async, settings, EXPLORER_SYSTEM_PROMPT/get_explorer_tools/SYSTEM_PROMPT/get_tools, etc.) — import them from their ORIGINAL source modules (openai_service, anthropic_service, google_service, tool_dispatcher, context_window, config), NOT re-exported through threads.py.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import app.services.agent_loop as a, app.api.threads as t; import inspect; src=inspect.getsource(a); assert 'for iteration in range' in src and 'async def _on_chunk_anthropic' in src and 'async def _on_chunk_google' in src and 'async def _on_chunk_openai' in src and 'def _persist_assistant_message' in src; tsrc=inspect.getsource(t); assert 'run_agent_loop(' in tsrc and 'for iteration in range' not in tsrc, 'loop must be gone from threads.py'; print('OK')" && backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_chunk_handler_provider_aware.py backend/tests/integration/test_075_4_terminal_race.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `agent_loop.py` contains `for iteration in range`, `async def _on_chunk_anthropic`, `async def _on_chunk_google`, `async def _on_chunk_openai`, `def _persist_assistant_message`, `def _persist_system_messages` (all inside/near run_agent_loop).
    - `threads.py` no longer contains `for iteration in range` (the loop moved) but DOES contain `run_agent_loop(` and constructs a `RunContext`.
    - All 14 invariant grep-anchors survive in agent_loop.py: `end_turn`, `thought_signature`, `reasoning_content`, the moonshot/deepseek `<think>` branch, `_empty_retries`, `force_no_tools`, `iteration_cap_dropped_tool_calls`, the transient-retry + 429 + length-recovery branches, the OpenRouter XML pre-inject (`_emit_boundary_openrouter`). `kind="iteration_cap_dropped_tool_calls"` is present in agent_loop.py.
    - The L1889 AND L2231 `active_provider_name` computations BOTH survive (grep shows ≥2 `active_provider_name =` assignments — NOT deduped).
    - `.neq("role", "system")` still appears exactly in threads.py get_snapshot + get_messages (UNTOUCHED — 086 landmine).
    - The `<automated>` import + structural check + the quick-run pytest both exit 0.
    - A `git diff` of the moved symbols shows pure relocation (the function bodies are byte-identical between the old threads.py location and the new agent_loop.py location).
  </acceptance_criteria>
  <done>run_agent_loop owns the full loop verbatim; threads.py agent_runner calls it and consumes AgentLoopResult; the finalizer order is intact; all I1-I14 invariants + both active_provider_name computes + the 086 filter preserved; quick tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Update all monkeypatch targets from app.api.threads.create_adaptive_streaming_chat to app.services.agent_loop.create_adaptive_streaming_chat</name>
  <read_first>
    - 089-RESEARCH.md §"Pitfall 1" (L250-254) — the #1 silent failure: Python `from X import Y` binds in the importer's namespace, so after the move the loop reads `agent_loop.create_adaptive_streaming_chat`; patching `app.api.threads.*` no longer intercepts it.
    - The grep inventory: `app.api.threads.create_adaptive_streaming_chat` appears in 20 test files (37 occurrences; some are docstring comments, only the `patch(...)`/`setattr(...)` calls matter).
  </read_first>
  <files>backend/tests/test_provider_router.py, backend/tests/unit/test_explorer_agent.py, backend/tests/integration/test_058_concurrency.py, backend/tests/integration/test_059_disconnect.py, backend/tests/integration/test_061_producer_survives_disconnect.py, backend/tests/integration/test_061_ttl.py, backend/tests/integration/test_061_runs_table.py, backend/tests/integration/test_062_delete_happy.py, backend/tests/integration/test_062_multi_consumer_fanout.py, backend/tests/integration/test_062_stream_replay.py, backend/tests/integration/test_063_post_then_subscribe.py, backend/tests/integration/test_063_post_contract.py, backend/tests/integration/test_066_per_call_timer.py, backend/tests/integration/test_066_langsmith_clean.py, backend/tests/integration/test_066_sse_terminal.py, backend/tests/integration/test_066_terminal_classification.py, backend/tests/integration/test_067_4_suggestion_emit.py, backend/tests/integration/test_075_code_stdout_progressive.py, backend/tests/integration/test_075_tool_args_progress.py, backend/tests/integration/test_threads_skills.py</files>
  <action>
    Across the 20 listed test files, replace every patch/setattr target string `app.api.threads.create_adaptive_streaming_chat` → `app.services.agent_loop.create_adaptive_streaming_chat` (the call site moved into agent_loop.py, so the patch must target the importer's binding — Pitfall 1). This is a test-side edit (tests are not "behavior" — allowed).
    Update BOTH the `patch("app.api.threads.create_adaptive_streaming_chat", ...)` / `monkeypatch.setattr("app.api.threads.create_adaptive_streaming_chat", ...)` call strings AND the explanatory docstring/comment references to the same path (e.g. test_058 L22, test_063 L59, test_075_tool_args_progress L340) so the comments stay accurate.
    DO NOT touch `generate_thread_title` patches — title-gen STAYS in threads.py, so `app.api.threads.generate_thread_title` patch targets are correct and must remain. (Verify by grep that no `generate_thread_title` patch path was changed.)
    DO NOT change any other symbol path.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import subprocess,sys; from pathlib import Path; bad=[p for p in Path('backend/tests').rglob('*.py') if 'app.api.threads.create_adaptive_streaming_chat' in p.read_text(encoding='utf-8')]; print('REMAINING OLD TARGETS:', bad); sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Grep for `app.api.threads.create_adaptive_streaming_chat` across `backend/tests/` returns ZERO occurrences (all updated to `app.services.agent_loop.create_adaptive_streaming_chat`).
    - Grep for `app.api.threads.generate_thread_title` is UNCHANGED (title-gen patch targets preserved — grep still finds them where they were).
    - No test file imports or asserts were altered beyond the patch-target string + its matching comment.
    - The `<automated>` check exits 0 (no old targets remain).
  </acceptance_criteria>
  <done>All create_adaptive_streaming_chat monkeypatch targets point at agent_loop; generate_thread_title patches stay at threads; no stray old targets remain.</done>
</task>

<task type="auto">
  <name>Task 3: Add the AgentLoopResult seam test + run the full suite GREEN</name>
  <read_first>
    - 089-RESEARCH.md §"Wave 0 Gaps" (L548) + §"Pitfall 5" (L274-278) — the finalizer (_shielded_finalize, STAYS) needs the loop to return the persist callable + token totals + warnings; no existing test covers this seam.
    - backend/tests/integration/test_075_4_terminal_race.py — the existing finalizer-order test (house style for asserting the persist → finalize → sentinel order); mirror its fixtures/mocks.
    - backend/app/services/agent_loop.py (post-move) — the AgentLoopResult shape the loop returns.
  </read_first>
  <files>backend/tests/integration/test_089_agent_loop_result_seam.py</files>
  <action>
    Create `backend/tests/integration/test_089_agent_loop_result_seam.py` — a NEW integration test asserting the AgentLoopResult seam (the finalizer still gets what it needs after the move):
    1. Drive a minimal send_message run (mocking `app.services.agent_loop.create_adaptive_streaming_chat` to emit one assistant turn + terminal — mirror the fixtures in test_075_4_terminal_race.py).
    2. Assert run_agent_loop returns an `AgentLoopResult` whose `persist` callable yields a valid (non-None) persisted message id, and whose `input_tokens_total`/`output_tokens_total` are populated (or correctly None when the mock omits usage), and whose `persisted_system_warnings` is a list.
    3. Assert the finalizer path still persists the assistant message (the message row exists / the persist callable was invoked) AND the terminal sentinel is emitted AFTER finalize_run (the I10 / Pitfall 5 order — reuse the assertion shape from test_075_4_terminal_race.py).
    Keep it fast (mock the LLM + DB as the existing integration tests do; no live provider). This is the Wave-0-gap seam test from the validation contract.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/integration/test_089_agent_loop_result_seam.py -x -q && backend/venv/Scripts/python.exe -m pytest backend/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/integration/test_089_agent_loop_result_seam.py` exists and references `AgentLoopResult`.
    - The test asserts the finalizer receives a valid (non-None) persisted message id via the result, and that the terminal sentinel is emitted after finalize_run (I10 order).
    - The new seam test passes in isolation.
    - The FULL pytest suite (`backend/tests/ -q`) is GREEN — no real-network/connection-refused failures (proves the monkeypatch sweep in Task 2 is complete) and no regression from the move.
  </acceptance_criteria>
  <done>The AgentLoopResult seam is test-covered (finalizer gets a valid message id + token totals + warnings; terminal order intact); the full suite is GREEN after the verbatim move + monkeypatch sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → loop (user content) | Untrusted user input flows into the moved loop; validation must survive verbatim |
| loop → Postgres (messages/runs) | Every DB write carries the user_id predicate (RLS) — must be preserved through the move |
| loop → provider APIs | Per-provider chunk handlers translate native streams to the shared SSE vocabulary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-089-08 | Elevation of privilege / Info-disclosure | cross-user data leak via RLS bypass | mitigate | Every moved loop DB write KEEPS the `user_id` predicate verbatim — persist L1683/L1711, history read L1515 (089-RESEARCH §Security V4). Acceptance: `git diff` shows the user_id filters byte-identical post-move. |
| T-089-09 | Tampering | PG-illegal null bytes in persisted content | mitigate | `_strip_nul` (moved Plan 01) + `json.loads(tc["arguments"])` guarded by `except json.JSONDecodeError` (L2690) preserved verbatim (V5 input validation). Acceptance: the JSONDecodeError guard survives in agent_loop.py. |
| T-089-10 | Tampering | monkeypatch mis-target → tests hit real providers | mitigate | Task 2 sweep updates all 37 targets; the full-suite-GREEN gate (Task 3) fails loudly on any connection-refused, catching a missed target. |
| T-089-11 | Repudiation / Info-disclosure | terminal-status race (done SSE before persisted) | mitigate | The finalizer order (persist → finalize_run → sentinel → expire → zrem) stays byte-identical (I10/Pitfall 5); test_089_agent_loop_result_seam + test_075_4_terminal_race assert it. |
| T-089-12 | Info-disclosure | 086 snapshot filter regression (system rows 500) | mitigate | The `.neq("role","system")` filter in get_snapshot/get_messages is UNTOUCHED (route handlers stay); the loop's history query is a SEPARATE path, NOT consolidated (089-RESEARCH §086 landmine). Acceptance verifies the filter string survives. |

Controls preserved verbatim through the move: V4 `user_id` predicate on every DB write; V5 `_strip_nul` + JSONDecodeError guard; the I10 terminal-race order; the 086 system-row filter. No NEW attack surface — pure relocation.
</threat_model>

<verification>
- Full pytest suite GREEN: `backend/venv/Scripts/python.exe -m pytest backend/tests/ -q`.
- `git diff` review of the seam: the moved symbols are byte-identical relocations (SC#1 structural check).
- All 14 invariant grep-anchors present in agent_loop.py (SC#2).
- Zero `app.api.threads.create_adaptive_streaming_chat` targets remain in tests; `generate_thread_title` patches unchanged.
- The 086 `.neq("role","system")` filter intact in threads.py route handlers.
- Operator runs the AFTER half of the SSE-diff runbook (Plan 02) + the eval — handled at the phase gate / Plan 04 (live keys).
</verification>

<success_criteria>
- The loop + handlers + persist + tool-dispatch round live in run_agent_loop verbatim; threads.py retains only the shell (SC#1).
- All I1-I14 invariants carried verbatim (SC#2).
- Full suite GREEN; AgentLoopResult seam test passes.
- No forbidden cleanup: three chunk handlers separate, both active_provider_name computes intact, deferred Anthropic bugs untouched, dispatch_tool/ToolContext/086-filter untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-03-SUMMARY.md`
</output>
