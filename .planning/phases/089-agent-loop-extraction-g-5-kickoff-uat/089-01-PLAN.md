---
phase: 089-agent-loop-extraction-g-5-kickoff-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/agent_loop.py
  - backend/app/api/threads.py
  - backend/app/services/SEAM.md
autonomous: false
requirements: [FOUND-03]

must_haves:
  truths:
    - "backend/app/services/agent_loop.py exists with the G-5 provenance docstring and a frozen RunContext + plain AgentLoopResult dataclass"
    - "The three pure helpers drain_step, _drain_stream_with_close_on_cancel, _strip_nul live in agent_loop.py (moved verbatim), not duplicated"
    - "agent_loop.py imports run cleanly (no circular import) — `python -c 'import app.services.agent_loop'` exits 0"
    - "The proposed run_agent_loop(ctx, *, emit, emit_terminal, spawn) signature + the Category-B boundary call (B1 vs B2) are written to SEAM.md and reported to the operator for sign-off BEFORE any loop-body move"
    - "The full pytest suite is still GREEN (helper move does not change behavior)"
  artifacts:
    - path: "backend/app/services/agent_loop.py"
      provides: "Module skeleton: header, RunContext (frozen), AgentLoopResult, moved pure helpers, run_agent_loop stub (NotImplementedError or pass — no body yet)"
      contains: "class RunContext"
    - path: "backend/app/services/SEAM.md"
      provides: "The proposed seam: run_agent_loop signature, RunContext field list, AgentLoopResult field list, the B1-vs-B2 Category-B boundary decision"
      contains: "run_agent_loop"
  key_links:
    - from: "backend/app/api/threads.py"
      to: "backend/app/services/agent_loop.py"
      via: "from app.services.agent_loop import drain_step, _drain_stream_with_close_on_cancel"
      pattern: "from app.services.agent_loop import"
---

<objective>
Lay the extraction foundation WITHOUT moving the loop body: create the `agent_loop.py`
module skeleton (G-5 house-style header + `RunContext`/`AgentLoopResult` dataclasses +
the three zero-dependency pure helpers moved verbatim), then STOP at an operator
seam-review checkpoint that confirms the exact `run_agent_loop` signature and the
Category-B boundary BEFORE the verbatim loop move (Plan 03) proceeds.

Purpose: D-089-04 — the operator was explicit: confirm the seam before any code moves.
This plan de-risks the highest-risk-if-done-wrong phase of v2.8 by separating the
mechanical-but-safe prep (pure helpers have no closure deps) from the loop move, and
by gating the move on a human sign-off.

Output: `agent_loop.py` skeleton + `SEAM.md` proposal + a passing suite. No behavior change.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-CONTEXT.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-RESEARCH.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-PATTERNS.md

<interfaces>
<!-- House-style header + dataclass conventions the skeleton MUST copy (089-PATTERNS.md). -->
<!-- Source: backend/app/services/tool_dispatcher.py L1-101. Executor uses these directly. -->

tool_dispatcher.py module header convention (copy the shape, not the words):
```python
"""<one-line purpose>.

Extracted from threads.py (Phase 083, G-5 mandated refactor). ...
"""
from __future__ import annotations

import asyncio
import json
import logging
...
from dataclasses import dataclass, field
from typing import TYPE_CHECKING, Any, Callable, Awaitable
from uuid import UUID

if TYPE_CHECKING:
    import asyncpg
    import redis.asyncio as aioredis
    from supabase import Client
    from app.models.user_settings import UserEffectiveSettings

logger = logging.getLogger(__name__)
```

ToolContext / ToolResult dataclass house style (tool_dispatcher.py L59-100):
```python
@dataclass
class ToolContext:
    """Carries all dependencies a tool handler needs from the agent_runner scope."""
    redis: Any  # redis.asyncio client
    run_id: UUID
    thread_id: str
    supabase: Any  # supabase Client
    user_settings: Any  # UserEffectiveSettings
    current_user: dict  # {"id": str, ...}
    emit: Callable[..., Awaitable[None]]  # reference to _emit
    spawn: Callable  # reference to _spawn
    ...

@dataclass
class ToolResult:
    """Structured return from a tool handler."""
    result: str
    source_refs: list[dict] = field(default_factory=list)
```

RESEARCH's PROPOSED run_agent_loop signature (089-RESEARCH.md L465-472 — to be confirmed at checkpoint):
```python
async def run_agent_loop(ctx: RunContext, *, emit, emit_terminal, spawn) -> AgentLoopResult
# RunContext (frozen): run_id, thread_id, current_user, user_settings, body,
#                      redis, supabase, resolved_model, resolved_provider
# AgentLoopResult: persist (callable), input_tokens_total, output_tokens_total,
#                  persisted_system_warnings, full_content_final
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent_loop.py skeleton with header, RunContext, AgentLoopResult, and a run_agent_loop stub</name>
  <read_first>
    - 089-PATTERNS.md (entire) — the G-5 module-header convention and the ToolContext/ToolResult dataclass house style.
    - backend/app/services/tool_dispatcher.py L1-101 — the exact header + dataclass bytes to mirror.
    - 089-RESEARCH.md §"Closure-Capture Inventory" (L422-472) — Category A (RunContext fields) and Category E (AgentLoopResult fields).
  </read_first>
  <files>backend/app/services/agent_loop.py</files>
  <action>
    Create `backend/app/services/agent_loop.py` (NEW). Mirror the tool_dispatcher.py header VERBATIM in shape:
    1. Module docstring opening with one-line purpose + provenance line: "Extracted from threads.py (Phase 089, G-5 mandated refactor)."
    2. `from __future__ import annotations` as the FIRST line after the docstring.
    3. stdlib imports (asyncio, json, logging, etc.), then `from dataclasses import dataclass, field`, `from typing import TYPE_CHECKING, Any, Callable, Awaitable`, `from uuid import UUID`.
    4. An `if TYPE_CHECKING:` block importing `asyncpg`, `redis.asyncio as aioredis`, `from supabase import Client`, `from app.models.user_settings import UserEffectiveSettings`.
    5. `logger = logging.getLogger(__name__)` at module scope after imports.
    6. A `# Data classes` section banner, then:
       - `@dataclass(frozen=True) class RunContext:` with a one-line docstring and the Category-A INPUT fields ONLY (089-RESEARCH.md L426-438): `run_id: UUID`, `thread_id: str`, `current_user: dict`, `user_settings: Any  # UserEffectiveSettings`, `body: Any  # MessageCreate`, `redis: Any  # redis.asyncio client`, `supabase: Any  # supabase Client`, `resolved_model: str`, `resolved_provider: str`. Use the `Any + # real-type` house style for opaque externals.
       - `@dataclass class AgentLoopResult:` (PLAIN, not frozen — it is a return bag like ToolResult) with the Category-E fields (089-RESEARCH.md L461-463): `persist: Callable[..., Awaitable[Any]]  # reference to the bound _persist_assistant_message`, `input_tokens_total: int | None = None`, `output_tokens_total: int | None = None`, `persisted_system_warnings: list[dict] = field(default_factory=list)`, `full_content_final: str = ""`.
    7. A `run_agent_loop` STUB ONLY (no body yet): `async def run_agent_loop(ctx: RunContext, *, emit, emit_terminal, spawn) -> AgentLoopResult:` with a one-line docstring and `raise NotImplementedError("loop body lands in Plan 03 after seam sign-off")`. DO NOT write the loop body in this plan.
    NO module-level mutable state (WORKER_COUNT=2 — RESEARCH anti-pattern). RunContext is per-call.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import app.services.agent_loop as m; assert hasattr(m,'RunContext') and hasattr(m,'AgentLoopResult') and hasattr(m,'run_agent_loop'); from dataclasses import fields; assert m.RunContext.__dataclass_params__.frozen is True; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `agent_loop.py` contains the string `Extracted from threads.py (Phase 089, G-5 mandated refactor)`.
    - `agent_loop.py` contains `from __future__ import annotations` as the first non-docstring line.
    - `agent_loop.py` contains `class RunContext` decorated `@dataclass(frozen=True)` and `class AgentLoopResult` decorated `@dataclass` (NOT frozen).
    - `RunContext` declares exactly the 9 Category-A fields listed in the action (run_id, thread_id, current_user, user_settings, body, redis, supabase, resolved_model, resolved_provider).
    - `run_agent_loop` is a stub raising `NotImplementedError` — grep confirms NO `for iteration in range` appears in agent_loop.py yet.
    - The import-check `<automated>` command exits 0.
  </acceptance_criteria>
  <done>agent_loop.py module skeleton exists, imports cleanly, RunContext is frozen, run_agent_loop is an unimplemented stub. No loop body present.</done>
</task>

<task type="auto">
  <name>Task 2: Move the three pure helpers (drain_step, _drain_stream_with_close_on_cancel, _strip_nul) verbatim into agent_loop.py and re-import in threads.py</name>
  <read_first>
    - backend/app/api/threads.py L166-192 (`drain_step`), L216-315 (`_drain_stream_with_close_on_cancel`), L1726-1734 (`_strip_nul`) — the exact bytes to move.
    - 089-RESEARCH.md §"Pitfall 4" (L268-272) — these are pure (zero closure deps, verified L166/L216); moving them breaks the import cycle.
    - 089-RESEARCH.md §"Extraction Seam Map" (L418) — confirms drain_step/_drain_stream_with_close_on_cancel are pure helpers recommended to move.
  </read_first>
  <files>backend/app/services/agent_loop.py, backend/app/api/threads.py</files>
  <action>
    Move three PURE helper functions from threads.py into agent_loop.py BYTE-IDENTICALLY (no logic change):
    1. `drain_step` (threads.py L166-192).
    2. `_drain_stream_with_close_on_cancel` (threads.py L216-315).
    3. `_strip_nul` (threads.py L1726-1734) — currently nested inside agent_runner; un-nest it to module scope in agent_loop.py (it is a pure helper used by both persist functions which also move in Plan 03).
    In agent_loop.py, place them after the dataclasses. Add any imports they need (asyncio, etc.) to agent_loop.py's import block.
    In threads.py: DELETE the three moved definitions and add `from app.services.agent_loop import drain_step, _drain_stream_with_close_on_cancel` at the top-of-file import block so any remaining threads.py callers still resolve. `_strip_nul` was nested-only inside agent_runner; its callers (`_persist_assistant_message`, `_persist_system_messages`) move in Plan 03, so it does NOT need re-import into threads.py — confirm by grep that no module-scope threads.py code calls `_strip_nul`.
    DO NOT alter the bodies. This is a relocation only: `git diff` must show pure deletion from threads.py + pure addition to agent_loop.py for these three symbols.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import app.services.agent_loop as a, app.api.threads as t; assert hasattr(a,'drain_step') and hasattr(a,'_drain_stream_with_close_on_cancel') and hasattr(a,'_strip_nul'); assert t.drain_step is a.drain_step; print('OK')" && backend/venv/Scripts/python.exe -m pytest backend/tests/integration/test_075_4_terminal_race.py backend/tests/unit/test_chunk_handler_provider_aware.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `agent_loop.py` contains `def drain_step`, `async def _drain_stream_with_close_on_cancel`, and `def _strip_nul` (all three, at module scope).
    - `threads.py` no longer contains `def drain_step` or `async def _drain_stream_with_close_on_cancel` (grep returns 0 definitions) but DOES contain `from app.services.agent_loop import drain_step, _drain_stream_with_close_on_cancel`.
    - `t.drain_step is a.drain_step` is True (threads.py re-imports the moved symbol, no duplicate definition).
    - The moved bodies are byte-identical: a `git diff` of the three functions shows pure relocation (no character change inside the function bodies).
    - The quick-run pytest (`test_075_4_terminal_race.py` + `test_chunk_handler_provider_aware.py`) exits 0.
  </acceptance_criteria>
  <done>The three pure helpers live in agent_loop.py verbatim; threads.py re-imports the two still-referenced ones; no circular import; quick tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Write the proposed seam (run_agent_loop signature + RunContext/AgentLoopResult + B1-vs-B2 boundary) to SEAM.md</name>
  <read_first>
    - 089-RESEARCH.md §"Closure-Capture Inventory" (L422-472) — Categories A (RunContext), B (the L1470-1627 setup boundary), C (callables), D (loop-locals), E (AgentLoopResult).
    - 089-RESEARCH.md §"Open Questions" (L599-611) — Q1 (Category-B boundary: B1 move-into-loop vs B2 keep-in-runner), Q3 (_reconstruct_history move/stay).
    - 089-CONTEXT.md D-089-04 — report the exact seam + signature to the operator BEFORE executing.
  </read_first>
  <files>backend/app/services/SEAM.md</files>
  <action>
    Write `backend/app/services/SEAM.md` documenting the EXACT proposed seam for operator sign-off (D-089-04). Include verbatim:
    1. The final `run_agent_loop` signature: `async def run_agent_loop(ctx: RunContext, *, emit, emit_terminal, spawn) -> AgentLoopResult`.
    2. The `RunContext` field list (the 9 Category-A fields, with origins from the closure inventory).
    3. The `AgentLoopResult` field list (Category-E: the bound persist callable, input/output token totals, persisted_system_warnings, full_content_final) and WHY a result object exists (the finalizer `_shielded_finalize` STAYS in threads.py and reads these — 089-RESEARCH.md L207).
    4. The Category-B boundary RECOMMENDATION: B1 (move the L1470-1627 setup — folder-scope, mode/prompt/tool selection, skills/memory injection, `_reconstruct_history` call + trim — INTO run_agent_loop, giving a thinner ctx) vs B2 (keep it in agent_runner, pass computed values via an extended ctx). State the RESEARCH recommendation (B1) and the trade-off, and present it as an explicit DECISION the operator confirms.
    5. The `_reconstruct_history` disposition (Q3): RESEARCH recommends KEEP in threads.py, import into agent_loop.py (pure function, no cycle). State this.
    6. A one-line summary of what Plan 03 will move (the loop body L1818-2738, the three `_on_chunk_*`, `_persist_assistant_message`, `_persist_system_messages`, post-loop emits, the inner try/except) so the operator sees the full move scope before signing off.
    This is a documentation artifact for the checkpoint — no code change.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import pathlib,sys; t=pathlib.Path('backend/app/services/SEAM.md').read_text(encoding='utf-8'); assert 'run_agent_loop' in t and 'RunContext' in t and 'AgentLoopResult' in t and ('B1' in t and 'B2' in t); sys.exit(0 if all(k in t for k in ('emit','emit_terminal','spawn','_reconstruct_history')) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/SEAM.md` exists and contains the literal string `async def run_agent_loop(ctx: RunContext, *, emit, emit_terminal, spawn) -> AgentLoopResult`.
    - SEAM.md lists all 9 RunContext fields and all 5 AgentLoopResult fields.
    - SEAM.md contains both `B1` and `B2` and states the recommended Category-B boundary decision.
    - SEAM.md states the `_reconstruct_history` keep-and-import disposition.
    - The `<automated>` check exits 0.
  </acceptance_criteria>
  <done>SEAM.md documents the full proposed seam (signature, both dataclass field lists, the B1/B2 boundary decision, the _reconstruct_history call) ready for operator review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Seam-review checkpoint — operator confirms run_agent_loop signature + Category-B boundary BEFORE the verbatim move</name>
  <what-built>
    A clean `agent_loop.py` skeleton (frozen `RunContext`, plain `AgentLoopResult`, the three pure helpers moved verbatim, a `run_agent_loop` stub) and `backend/app/services/SEAM.md` proposing the exact seam. NO loop body has moved yet. The full suite is still GREEN.
  </what-built>
  <how-to-verify>
    1. Open `backend/app/services/SEAM.md` and review:
       - The `run_agent_loop(ctx: RunContext, *, emit, emit_terminal, spawn) -> AgentLoopResult` signature.
       - The `RunContext` field list (9 frozen INPUT fields).
       - The `AgentLoopResult` field list (the finalizer-needed outputs).
       - The Category-B boundary recommendation: B1 (move the L1470-1627 setup into `run_agent_loop`) vs B2 (keep it in `agent_runner`). Confirm or override B1.
       - The `_reconstruct_history` keep-in-threads.py-and-import disposition.
    2. Open `backend/app/services/agent_loop.py` and confirm the skeleton matches the proposal.
    3. Confirm the scope of what Plan 03 will move (the loop body L1818-2738 + the three `_on_chunk_*` + persist functions + tool-dispatch round + post-loop emits + inner try/except) is exactly the D-089-01 boundary and contains zero "while-I'm-in-here" cleanup.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the seam (Plan 03 proceeds with the verbatim move as-proposed), OR specify changes to the signature / RunContext fields / B1-vs-B2 boundary before the move proceeds.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /threads/{id}/messages route | Untrusted user input enters at send_message (UNCHANGED — route stays in threads.py) |
| loop → Postgres (messages/runs) | DB writes carry the user_id predicate — this plan moves only pure helpers, no DB writes yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-089-01 | Information disclosure | `agent_loop.py` skeleton | accept | No new attack surface — skeleton has no I/O, no secrets, no DB. Pure helpers move verbatim; `_strip_nul` (V5 input validation — strips PG-illegal null bytes) preserved byte-identically (089-RESEARCH §Security V5). |
| T-089-02 | Tampering | circular-import / wrong-binding | mitigate | Pure helpers verified zero-closure-dependency (RESEARCH Pitfall 4); `from app.services.agent_loop import ...` re-import in threads.py keeps `t.drain_step is a.drain_step` (acceptance criterion) — no behavior drift. |
| T-089-03 | Elevation of privilege | module-level mutable state | mitigate | `RunContext` is frozen + per-call; NO module-level mutable singletons in agent_loop.py (WORKER_COUNT=2 hazard — RESEARCH anti-pattern). Verified by acceptance: no module-global accumulators. |

Controls preserved verbatim through the move: V5 `_strip_nul` (null-byte strip) moves byte-identically; no V4/V2 surface touched (no DB writes, no auth in this plan).
</threat_model>

<verification>
- `python -c "import app.services.agent_loop"` exits 0 (no circular import).
- Full pytest suite GREEN after the helper move (no behavior change): `backend/venv/Scripts/python.exe -m pytest backend/tests/ -q`.
- SEAM.md reviewed and approved by the operator (Task 4 checkpoint) before Plan 03 runs.
- `git diff` of the three moved helpers shows pure relocation (byte-identical bodies).
</verification>

<success_criteria>
- agent_loop.py exists with G-5 header, frozen RunContext, plain AgentLoopResult, and the three pure helpers moved verbatim.
- run_agent_loop is an unimplemented stub (no loop body moved in this plan).
- SEAM.md documents the exact proposed seam and is operator-approved.
- Full suite GREEN; no circular import.
</success_criteria>

<output>
After completion, create `.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-01-SUMMARY.md`
</output>
