---
phase: 075.6
slug: live-streaming-ux-cross-provider-parity
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-23
---

# Phase 075.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Sourced from RESEARCH.md §Validation Architecture (file `075.6-RESEARCH.md`, lines 694-766). 4-axis UAT bandwidth per CLAUDE.md SC#10 + D-075.6-C1..C3.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (frontend)** | Vitest (existing `frontend/src/__tests__/`) + React Testing Library + `vi.hoisted` mock pattern |
| **Framework (backend)** | pytest + pytest-asyncio + asyncpg + httpx ASGITransport |
| **Config files** | `frontend/vitest.config.ts` (existing), `backend/pyproject.toml` `[tool.pytest.ini_options]` (existing) |
| **Quick run command (frontend)** | `cd frontend && npx vitest run src/__tests__/providers/streamsProvider.test.tsx src/__tests__/providers/streamsProvider_067_5_regression.test.tsx` |
| **Quick run command (backend)** | `cd backend && ./venv/Scripts/python -m pytest tests/integration/test_075_tool_args_progress.py -x` |
| **Full suite (frontend)** | `cd frontend && npx vitest run` |
| **Full suite (backend)** | `cd backend && ./venv/Scripts/python -m pytest tests/ -x` |
| **Chrome MCP UAT runner** | `http://localhost:5173/` authenticated as `fhdmrd@gmail.com / 123456` (per `reference_local_dev_app`) |
| **Estimated runtime (per-task quick)** | ~5s frontend + ~30s backend (with PG_AVAILABLE) |

---

## Sampling Rate

- **After every task commit:** Run quick frontend `vitest -t "argsCodeText|L-068-01"` + backend `pytest -x test_075_tool_args_progress.py`
- **After every plan wave:** Run full file suites — `vitest run src/__tests__/providers/` + `pytest tests/integration/test_075_6*` + `pytest test_075_tool_args_progress.py`
- **Before `/gsd:verify-work`:** Full frontend (`vitest run`) + full backend integration (`pytest tests/integration/`) AND the manual 4-axis Chrome MCP UAT recorded below
- **Max feedback latency:** ~60 seconds (full integration suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 075.6-01-* | 01 | 1 | Req #1 (Anthropic `code_so_far`) | — | additive field; no PII leak (code is user-owned) | unit (backend) | `pytest backend/tests/integration/test_075_tool_args_progress.py::test_anthropic_path_emits_code_so_far -x` | ❌ Wave 0: extend existing file | ⬜ pending |
| 075.6-01-* | 01 | 1 | Req #1 (Google `code_so_far`) | — | additive field | unit (backend) | `pytest -x ::test_google_path_emits_code_so_far` | ❌ Wave 0: extend existing file | ⬜ pending |
| 075.6-01-* | 01 | 1 | Req #2 (OpenAI emits for `execute_code`, skip removed) | — | additive field | unit (backend) | `pytest -x ::test_openai_path_emits_for_execute_code` | ❌ Wave 0: extend existing file | ⬜ pending |
| 075.6-01-* | 01 | 1 | Req #3 (OpenRouter independent buffer state) | — | provider buffers isolated; no cross-talk | unit (backend) | `pytest -x ::test_openrouter_independent_buffer_from_openai` | ❌ Wave 0: extend existing file | ⬜ pending |
| 075.6-01-* | 01 | 1 | Req #1 acceptance (monotonic prefix growth on Redis stream) | — | n/a | integration | `pytest -x backend/tests/integration/test_075_6_code_so_far_monotonic.py` | ❌ Wave 0: NEW file | ⬜ pending |
| 075.6-01-* | 01 | 1 | L2 regression (existing 075 suite still green) | — | existing 6 tests stay green | regression (backend) | `pytest backend/tests/integration/test_075_tool_args_progress.py -x` | ✅ existing | ⬜ pending |
| 075.6-02-* | 02 | 2 | Req #5 (longer-string-wins reducer) | — | reducer never overwrites with shorter value | unit (frontend) | `vitest run src/__tests__/providers/streamsProvider.test.tsx -t "argsCodeText longer wins"` | ❌ Wave 0: add describe block | ⬜ pending |
| 075.6-02-* | 02 | 2 | Req #5 (reverse-order idempotency) | — | n/a | unit (frontend) | `vitest run -t "argsCodeText reverse order"` | ❌ Wave 0 | ⬜ pending |
| 075.6-02-* | 02 | 2 | Req #5 (`onToolStart` clears `argsCodeText`) | — | n/a | unit (frontend) | `vitest run -t "onToolStart clears argsCodeText"` | ❌ Wave 0 | ⬜ pending |
| 075.6-02-* | 02 | 2 | Req #5 (late `tool_args_progress` is no-op post-tool_start; Pitfall 7) | — | reducer race closed by status filter | unit (frontend) | `vitest run -t "late tool_args_progress no-op"` | ❌ Wave 0 | ⬜ pending |
| 075.6-02-* | 02 | 2 | Req #4 (`<ToolArgsLivePanel>` default state) | — | n/a | unit (frontend) | `vitest run frontend/src/__tests__/components/ToolArgsLivePanel.test.tsx` | ❌ Wave 0: NEW file | ⬜ pending |
| 075.6-02-* | 02 | 2 | Req #4 (auto-collapse on `tool_start`) | — | n/a | unit (frontend) | Same file, second it() block | ❌ Wave 0 | ⬜ pending |
| 075.6-02-* | 02 | 2 | 067.5 D-3 invariant (`clearThreadBucket` not called by new code paths) | — | bucket-wipe never racing streams | unit (frontend) | `vitest run streamsProvider_067_5_regression.test.tsx -t "075.6 paths don't call clearThreadBucket"` | ❌ Wave 0: add assertion | ⬜ pending |
| 075.6-03-* | 03 | 2 | Req #6 (sub-agent gate drop for non-`analyze_document`) | — | n/a | UAT (Chrome MCP) | Manual UAT below | ❌ Manual | ⬜ pending |
| 075.6-03-* | 03 | 2 | Req #7 (5 done + 1 active → 3 rows) | — | n/a | unit (frontend) | `vitest run frontend/src/__tests__/components/ToolCallPanel.test.tsx -t "step-list collapse"` | ❌ Wave 0: NEW file | ⬜ pending |
| 075.6-03-* | 03 | 2 | Req #7 (expand chevron restores 6 rows) | — | n/a | unit (frontend) | Same file, second it() block | ❌ Wave 0 | ⬜ pending |
| 075.6-03-* | 03 | 2 | Req #7 (iteration divider count = 2 across iter-1→iter-2→iter-3) | — | n/a | unit (frontend) | Same file, third it() block | ❌ Wave 0 | ⬜ pending |
| 075.6-03-* | 03 | 2 | Req #8 (Working badge re-render gate, Pitfall 5) | — | n/a | unit (frontend) | `vitest run frontend/src/__tests__/components/WorkingBadge.test.tsx` | ❌ Wave 0: NEW file | ⬜ pending |
| 075.6-03-* | 03 | 2 | Req #9 (zero provider-conditional branches) | — | provider-uniform invariant | static (grep) | `cd frontend && (! git grep -E "provider\\s*===\\s*['\\\"]" src/components/chat/ src/providers/StreamsProvider.tsx)` returns no matches | ✅ baseline clean | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/tests/integration/test_075_tool_args_progress.py` — extend with 4 new tests (`test_anthropic_path_emits_code_so_far`, `test_google_path_emits_code_so_far`, `test_openai_path_emits_for_execute_code`, `test_openrouter_independent_buffer_from_openai`) covering Reqs #1/#2/#3
- [ ] `backend/tests/integration/test_075_6_code_so_far_monotonic.py` — NEW file; Redis stream inspection asserting event N's `code_so_far` is a string prefix of event N+1's per Req #1 acceptance
- [ ] `frontend/src/__tests__/providers/streamsProvider.test.tsx` — extend with `argsCodeText` reducer describe block (4 new it() blocks for Req #5)
- [ ] `frontend/src/__tests__/providers/streamsProvider_067_5_regression.test.tsx` — extend with `vi.spyOn(actions, 'clearThreadBucket')` zero-call assertion under 075.6-introduced reducer paths (SPEC §Constraints invariant L6)
- [ ] `frontend/src/__tests__/components/ToolCallPanel.test.tsx` — NEW file; covers Req #7 step-list collapse + iteration divider preservation across collapsed/expanded boundary (Pitfall 6 / L5)
- [ ] `frontend/src/__tests__/components/WorkingBadge.test.tsx` — NEW file; covers Req #8 visibility + Pitfall 5 re-render-thrash regression (`React.memo` equality check)
- [ ] `frontend/src/__tests__/components/ToolArgsLivePanel.test.tsx` — NEW file; covers Req #4 (default expanded for active preparing, collapsed for past, auto-collapse on `tool_start`)
- [ ] No framework install needed — Vitest + pytest already in tree

---

## Manual-Only Verifications (4-axis UAT bandwidth — CLAUDE.md SC#10)

UAT execution order per **D-075.6-C1..C3**: (1) cross-provider FIRST in order Anthropic → Google → OpenAI → OpenRouter; (2) multi-tool; (3) parallel-thread; (4) long-message.

| UAT ID | Axis | Behavior | Requirement | Why Manual | Test Instructions |
|--------|------|----------|-------------|------------|-------------------|
| UAT-1A | Cross-provider | Anthropic panel renders during preparing on gallery prompt | Req #1 + #4 | Visual streaming UX | Chrome MCP: open `localhost:5173` as `fhdmrd@gmail.com`, switch model to Anthropic Claude default, submit "12-page matplotlib gallery prompt". Screenshot during preparing window (~5-15s in) showing panel expanded with >5KB code text. Screenshot ≥500ms after `tool_start` shows panel collapsed. Inspect Redis: `XLEN run:{rid}` ≥ 2 `tool_args_progress` events for the execute_code call. |
| UAT-1B | Cross-provider | Google panel renders | Req #1 + #4 | Visual | Same prompt, model = Gemini-3 default; same screenshots + Redis check |
| UAT-1C | Cross-provider | OpenAI panel renders (**new emit path — critical**) | Req #2 + #4 | Visual + Critical | Same prompt, model = OpenAI gpt-4.1 default; same screenshots + Redis check. PRIMARY ACCEPTANCE for Plan 01 |
| UAT-1D | Cross-provider | OpenRouter panel renders (**new emit path + independent buffer — critical**) | Req #3 + #4 | Visual + Critical | Same prompt, model = OpenRouter routing `openai/gpt-4.1`; same screenshots + Redis check. Confirms independent buffer doesn't break |
| UAT-2 | Multi-tool | `search_documents` + `execute_code` in one prompt | Req #4 (live panel) + Req #6 (sub-agent gate if invoked) | Coordination between tools | Anthropic only (per D-075.6-C3). Prompt: "Search my docs for 'machine learning' then write a Python script that plots the distribution of result counts". Assert live panel renders for execute_code window; `search_documents` row renders normally; no cross-talk |
| UAT-3 | Parallel-thread | Thread A streaming + Thread B accepting new prompt | Req #4 + D-075.4-A1 invariant | Per-thread state surface | Open 2 Chrome tabs same user (`fhdmrd@gmail.com / 123456`). Tab A: submit gallery prompt (long stream). Tab B: while A streaming, submit any new prompt. Assert: Tab A's panel state unchanged on Tab B's submission; Tab B's panel renders independently; Working badge stays pinned per-thread |
| UAT-4 | Long-message | ≥50 prior messages + new prompt | Req #4 + #8 (badge under virtualization) | Edge case for badge + panel render | Per D-075.6-C3: pre-seed 50 prior message pairs via Supabase Studio SQL editor (`INSERT INTO messages (thread_id, role, content, ...)` block) into a test thread; open the thread; submit gallery prompt; assert MessageItem-list virtualization doesn't break panel render; assert Working badge stays pinned at top of new assistant bubble even with long scrollback |
| UAT-5 | Sub-agent gate drop | Sub-agent text streams for non-`analyze_document` tool | Req #6 | Visual confirmation | Trigger any tool that delegates to `sub_agent_service.py` other than `analyze_document` (manual UAT trigger). Screenshot showing >0 chars of sub-agent text inside that tool's row during the sub-agent run |
| UAT-6 | Working badge lifecycle | Badge appears during stream, disappears on terminal | Req #8 | Visual | Submit any prompt. Screenshot during active assistant turn showing `✦ Working` at top of bubble. Screenshot ≥500ms after terminal SSE shows badge gone |
| UAT-7 | Iteration divider preservation | D-067-03 divider fires correctly across collapse boundary | Req #7 + L5 | Visual confirmation Vitest is correct end-to-end | Submit a prompt that produces ≥6 tool calls spanning ≥2 iterations. With step-list collapsed: expect 3 rows (1 summary + iteration divider + active step). Expand chevron: expect 6 rows with exactly 2 iteration dividers in correct positions |

---

## Static Analysis Gates

- `cd frontend && (! git grep -E "provider\s*===\s*['\"]" src/components/chat/ src/providers/StreamsProvider.tsx)` — returns no new matches added by 075.6 plans (Req #9 acceptance)
- `git grep "_tool_name != \"execute_code\"" backend/app/api/threads.py` — returns ZERO matches after Plan 01 (proves the skip was removed per Pitfall 2)
- `git grep "openrouter_service" backend/` — returns ZERO matches (proves no fictional file was created per L1)

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (7 file gaps listed above)
- [ ] No watch-mode flags in any `<automated>` block
- [ ] Feedback latency < 60s (full integration suite)
- [ ] 4-axis UAT bandwidth all 7 rows logged with screenshots + Redis inspection (UAT-1A through UAT-7)
- [ ] `nyquist_compliant: true` set in frontmatter after Wave 0 lands

**Approval:** pending
