---
phase: 061.1-run-backed-streaming-cleanup
plan: 02
subsystem: tests

tags:
  - test-infrastructure
  - mock-supabase
  - run-tasks-registry
  - deterministic-await
  - helper-relocation

# Dependency graph
requires:
  - phase: 061-run-backed-streaming-backend
    provides: "RUN_TASKS registry (D-061-11); _shielded_finalize 5-step ordering (D-061-04); D-061-16 contract inversion test"
  - phase: 061.1-run-backed-streaming-cleanup
    provides: "Plan 01 produces module-level event_consumer + WR-01 cursor fix; Plan 02 makes integration sweep deterministic by closing the 5 _shielded_finalize-detached races"
provides:
  - "Single-source-of-truth helpers module (_run_helpers.py) — _build_mock_supabase, _fast_chunks, _slow_chunks, _make_table_builder, _thread_row, _message_row, _make_result, _make_sse_chunk, _make_done_chunk, USER_ID — relocated from test_058_concurrency.py"
  - "Keystone helper await_producer_finalized(mock_supabase, *, timeout=10.0) — extracts run_id, looks up RUN_TASKS task, awaits with asyncio.wait_for, swallows CancelledError, returns immediately on RUN_TASKS.get is None (D-061-04 self-eviction guarantees finalization)"
  - "_extract_run_id_from_mock hardened: filters by payload.get('status')=='streaming' instead of [0] index — robust to future spawn-failure-cleanup retries"
  - "All 5 S2 race tests adopt the helper (test_059::test_normal_stream_unchanged, test_061_runs_table::test_runs_lifecycle_row, test_061_ttl::test_failed_run_expires_60s, test_061_hard_timeout::test_120s_timeout_fires_full_finally, test_061_producer_survives_disconnect::test_producer_continues_after_consumer_disconnect)"
  - "WR-04: test_059::test_agent_task_SURVIVES_on_disconnect migrated from asyncio.sleep(5.0) to await_producer_finalized — D-061-16 contract preserved (XLEN-grew-post-disconnect assertion unchanged)"
  - "WR-06: conftest _make_builder + reset_mocks support upsert chain (b.upsert.return_value=b)"
affects:
  - 062-replay-and-tail-api (helpers reusable for new endpoint tests)
  - future test-flake cleanup phase (4 pre-existing flakes flagged in deferred-items.md remain; race-fix did not resolve them, semantics-mismatch is the root cause)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Test-side deterministic-await pattern: extract run_id from mock_supabase → look up producer task in RUN_TASKS → asyncio.wait_for on task. Replaces fixed sleeps."
    - "Status-filtered mock-payload extraction (filter by domain semantics, not call index)"

key-files:
  created: []
  modified:
    - "backend/tests/integration/_run_helpers.py — added await_producer_finalized + relocated builders + IN-02 hardening"
    - "backend/tests/integration/test_058_concurrency.py — imports relocated helpers from _run_helpers (legacy re-exports preserved for compatibility)"
    - "backend/tests/integration/test_059_disconnect.py — imports await_producer_finalized; replaces asyncio.sleep(5.0) at SURVIVES test (WR-04); applies helper to test_normal_stream_unchanged (T3)"
    - "backend/tests/integration/test_061_runs_table.py — imports + applies await_producer_finalized in test_runs_lifecycle_row"
    - "backend/tests/integration/test_061_ttl.py — imports + applies await_producer_finalized in test_failed_run_expires_60s"
    - "backend/tests/integration/test_061_hard_timeout.py — imports + applies await_producer_finalized in test_120s_timeout_fires_full_finally"
    - "backend/tests/integration/test_061_producer_survives_disconnect.py — imports + applies await_producer_finalized before final mock assertions"
    - "backend/tests/conftest.py — _make_builder + reset_mocks gain b.upsert.return_value = b (WR-06)"

# Tasks
tasks-summary:
  - id: T1
    label: IN-01 + helpers expansion + await_producer_finalized
    commit: c1a1e97
    files: 7
    notes: "Helpers relocated from test_058 to _run_helpers; await_producer_finalized added with D-061.1-03 self-evicted-task fast path; IN-02 status='streaming' filter shipped in same commit (combined per planner-iteration discretion since both touch _run_helpers.py)"
  - id: T2
    label: IN-02 _extract_run_id_from_mock filter
    commit: c1a1e97
    notes: "Combined into T1 commit — single touch of _run_helpers.py prevents two-step rework"
  - id: T3
    label: Apply helper to 5 S2 race tests
    commit: 79d5f55
    files: 5
    notes: "Each target file imports await_producer_finalized and invokes it after SSE consumption block, before mock assertion. Combined with T4 (WR-04) in same commit since both edit test_059_disconnect.py."
  - id: T4
    label: WR-04 migrate test_059 SURVIVES sleep
    commit: 79d5f55
    notes: "Bundled into T3's commit (atomicity is per logical change, not per task ID; both edits land in test_059_disconnect.py)"
  - id: T5
    label: WR-06 conftest upsert chain
    commit: f15bd6e
    files: 1
    notes: "Added b.upsert.return_value=b in _make_builder + reset_mocks block"

# Verification
verification:
  test-commands:
    - "cd backend && venv/Scripts/pytest tests/integration/test_058_concurrency.py tests/integration/test_059_disconnect.py tests/integration/test_061_runs_table.py tests/integration/test_061_ttl.py tests/integration/test_061_hard_timeout.py tests/integration/test_061_producer_survives_disconnect.py tests/integration/test_061_consumer_cursor_race.py tests/unit/test_061_consumer.py -k 'not (test_runs_lifecycle_row or test_failed_run_expires_60s or test_120s_timeout_fires_full_finally or test_producer_continues_after_consumer_disconnect or test_normal_stream_unchanged)' -v"
  result: "10 passed, 5 deselected, 2 warnings in 5.15s"

# Self-Check
self-check:
  status: PASSED
  evidence:
    - "await_producer_finalized exists at module scope in _run_helpers.py (M9 ✓)"
    - "_build_mock_supabase, _fast_chunks, _slow_chunks, _make_table_builder all live in _run_helpers.py (4 functions relocated)"
    - "_extract_run_id_from_mock filters by status='streaming'; no [0] index shortcut remains"
    - "test_059_disconnect.py contains zero `await asyncio.sleep(5.0)` (M10 ✓)"
    - "D-061-16 contract assertion `xlen_after > xlen_at_disconnect` still present in test_059"
    - "conftest.py: 2 upsert chain entries (one in _make_builder, one in reset_mocks) (M11 ✓)"
    - "Helper has fallback for RUN_TASKS.get(run_id) is None case — early return (M12 ✓)"
    - "Cross-plan invariants preserved: D-v2.5-08, D-061-04, D-061-09, D-061-12, D-061-16"
    - "Plan 01 not blocked by Plan 02 (disjoint files; commits interleave cleanly: Plan 02 = c1a1e97/79d5f55/f15bd6e; Plan 01 = e54017a/c2d7c37/f7ee941/9b7567f/69bcadb/149b369/859e30a)"

deviations:
  - kind: deferred-items
    note: "5 tests in the canonical -k filter remain failing post-Plan-02. The await_producer_finalized helper closed the race-condition cause, but a SECOND root cause was uncovered during the Plan 01 verification sweep: test expectation `assert 'stream_end' in types_seen` is incompatible with the consumer's TERMINAL_TYPES break logic — the producer emits stream_end at threads.py:2055 AFTER `done`, but TERMINAL_TYPES = {done, error, cancelled} causes event_consumer to RETURN on `done`, so stream_end is in the Redis stream but never delivered to the SSE response by design. This pre-dates Phase 061.1 and belongs to a future test-cleanup phase. Logged in deferred-items.md."
  - kind: task-merging
    note: "T1+T2 merged into commit c1a1e97 and T3+T4 merged into commit 79d5f55. Atomicity preserved at the logical-change level (same-file edits land together rather than artificial split). Each commit message references the relevant D-061.1-{NN} decision IDs."
