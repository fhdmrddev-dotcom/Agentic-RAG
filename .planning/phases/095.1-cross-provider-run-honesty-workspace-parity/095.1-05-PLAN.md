---
phase: 095.1-cross-provider-run-honesty-workspace-parity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/OutputFileCard.tsx
  - frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx
autonomous: true
requirements: [WORKSPACE-PARITY, RUN-HONESTY]
nyquist_compliant: true
user_setup: []

must_haves:
  truths:
    - "Output files render as ONE flat 'Generated files' list — no '★ Your file' hero crown, no hero/working split"
    - "Every file is present and downloadable; a url-less file still shows the honest 'Download unavailable' affordance"
    - "A run that produced its deliverables before later failing/timing-out does NOT falsely offer Resume"
    - "A failed/timed_out run with NO deliverables still offers Resume; completed/cancelled/streaming unchanged"
  artifacts:
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "Flat FinalOutputsPanel (one OutputFileCard list) + deliverable-aware Resume gate"
      contains: "final-outputs-panel"
    - path: "frontend/src/components/chat/OutputFileCard.tsx"
      provides: "Hero variant + '★ Your file' caption removed; url-less dead-state KEPT"
      contains: "Download unavailable"
  key_links:
    - from: "frontend/src/components/chat/MessageItem.tsx"
      to: "message.finalOutputFiles"
      via: "flat map of OutputFileCard + Resume gate predicate producedDeliverables"
      pattern: "finalOutputFiles"
---

<objective>
Two MessageItem honesty fixes that share the file (so they ship together):

- D-095.1-06 (flat "Generated files" list, WORKSPACE-PARITY): drop the "★ Your file" hero
  crown and the hero/working split. Collapse `FinalOutputsPanel` into ONE equal "Generated
  files" list — every file an equal `OutputFileCard` row, all visible, all downloadable. KEEP
  the url-less "Download unavailable" affordance (orthogonal honesty fix). Reverses the visual
  part of 095-05/08. The backend `is_hero` flag stays WRITTEN-BUT-UNREAD (no backend change).

- D-095.1-07 (deliverable-aware Resume, RUN-HONESTY): the Resume gate (currently
  `runStatus ∈ {failed, timed_out}` at MessageItem.tsx:425) becomes deliverable-aware — a run
  that already produced output files before a later iteration failed/timed_out does NOT falsely
  offer Resume. Closes BUG-260518-01 (resume-after-success genuine-terminal case 075 didn't cover).

Purpose: no dishonest "primary file" guess; no false Resume after a successful-then-failed run.
Frontend-only, all-provider.

NOTE (skill contract): the sketch-findings SKILL.md describes a hero/working split as the design
contract — D-095.1-06 DELIBERATELY REVERSES it per the operator-approved CONTEXT.md decision.
CONTEXT.md is authoritative over the skill for this surface; the skill is now stale on the
output-files split (a Skill update is a follow-on note, out of scope here).

Output: flat FinalOutputsPanel + OutputFileCard hero-removal + Resume gate predicate + updated test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-CONTEXT.md
@.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-RESEARCH.md
@.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-PATTERNS.md
@frontend/src/components/chat/MessageItem.tsx
@frontend/src/components/chat/OutputFileCard.tsx

<interfaces>
<!-- The exact edit points the executor needs. -->

MessageItem.tsx:76-119 — FinalOutputsPanel (the hero/working split to COLLAPSE):
```tsx
function FinalOutputsPanel({ files }: { files: FinalOutputFile[] }) {
  const [workingOpen, setWorkingOpen] = useState(true)
  const heroes = files.filter((f) => f.is_hero)
  const working = heroes.length > 0 ? files.filter((f) => !f.is_hero) : files
  return (
    <div className="mt-3 border-t border-border/60 pt-3.5" data-testid="final-outputs-panel">
      <div className="text-[10px] font-mono uppercase tracking-[0.1em] text-muted-foreground mb-2.5">Generated files</div>
      {/* heroes block (variant="hero") + collapsible "Working files (N)" group (variant="working") */}
    </div>
  )
}
```
Guard at MessageItem.tsx:577 — renders FinalOutputsPanel when `message.finalOutputFiles?.length > 0`.

MessageItem.tsx:425 — the Resume gate (the predicate to extend):
```tsx
{!isStreaming && message.role === "assistant" && (message.runStatus === "failed" || message.runStatus === "timed_out") && (
  <Button ... onClick={() => onResume?.(message)}>Resume</Button>
)}
```

OutputFileCard.tsx:73,79-82,90-127 — the variant prop + hero branch + the url-less dead-state (KEEP):
```tsx
export function OutputFileCard({ file, variant = "working" }: OutputFileCardProps) {
  const isHeroVariant = variant === "hero"
  const icon = fileIcon(file.filename, isHeroVariant ? 48 : 30)
  if (!file.url) { /* "Download unavailable" dead-state — KEEP; the isHeroVariant "★ Your file" caption inside it is the part to drop */ }
  ...
}
```
finalOutputFiles entry shape: `{ filename: string; url?: string; size?: number; is_hero?: boolean }`
(types/index.ts:166). `finalOutputFiles` is reload-reconstructed from persisted execute_code output_files
(api.ts:114) — so D-07's deliverable signal works on reopened threads too.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flatten FinalOutputsPanel + drop the hero variant in OutputFileCard (D-06)</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/components/chat/OutputFileCard.tsx, frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageItem.tsx:72-119 (FinalOutputsPanel — the hero/working split + the data-testids + the "Generated files" eyebrow to keep)
    - frontend/src/components/chat/OutputFileCard.tsx:50-127 (the variant prop, isHeroVariant branches, the "★ Your file" caption, and the url-less dead-state to KEEP verbatim)
    - frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx (the existing hero/working assertions to update)
    - 095.1-RESEARCH.md §"D-095.1-06" (the concrete change + the SKILL.md contract-tension note) + §"Don't Hand-Roll" (fileIcon reuse)
    - 095.1-PATTERNS.md §"MessageItem.tsx (MOD)" + §"OutputFileCard.tsx (MOD)" + §"Pattern B"
  </read_first>
  <behavior>
    - FinalOutputsPanel renders ALL files as one flat list of equal OutputFileCard rows (no hero block, no "Working files (N)" group, no is_hero filter, no collapse toggle)
    - The "Generated files" eyebrow + data-testid="final-outputs-panel" are preserved
    - No "★ Your file" caption appears anywhere
    - A url-less file still renders the "Download unavailable" dead-state affordance
    - All files render with the uniform 30px fileIcon
    - The backend is_hero flag is ignored by the frontend (no read), no crash if present or absent
  </behavior>
  <action>
    1. `frontend/src/components/chat/MessageItem.tsx` — rewrite `FinalOutputsPanel` to a flat list:
       remove the `useState(workingOpen)`, the `heroes`/`working` filters, the hero block, and the
       collapsible "Working files (N)" group. Keep the container `<div className="mt-3 border-t
       border-border/60 pt-3.5" data-testid="final-outputs-panel">` + the "Generated files" eyebrow.
       Render `files.map((f, i) => <OutputFileCard key={`gen-${i}`} file={f} variant="working" />)` in a
       single `<div className="space-y-1.5">`. Do NOT read `f.is_hero` anywhere. The guard at :577
       (`message.finalOutputFiles?.length > 0`) is unchanged.

    2. `frontend/src/components/chat/OutputFileCard.tsx` — remove the hero presentation path:
       - Drop the `"★ Your file"` caption rendering (both in the dead-state branch at :102-106 and in
         the live branch wherever the hero caption renders). Grep the file for "★ Your file" and remove
         every occurrence.
       - Simplify the icon to the uniform 30px: `const icon = fileIcon(file.filename, 30)` (drop the
         `isHeroVariant ? 48 : 30` conditional). You MAY keep the `variant` prop accepting "hero"|"working"
         for call-site back-compat (TodosSection-style minimal churn) but it must no longer change the
         rendered shape — OR remove the prop entirely and update the FinalOutputsPanel call site. Prefer
         removing the hero-specific branches while keeping the prop optional+inert to avoid touching other
         call sites; document the choice. The `isHeroVariant` glow/gradient/large-icon/prominent-Download
         styling is removed so every row renders the quiet uniform style.
       - KEEP the url-less "Download unavailable" dead-state block (the `if (!file.url)` branch) VERBATIM
         except for removing the "★ Your file" caption inside it. This is an orthogonal honesty fix that
         CONTEXT explicitly KEEPS (D-095.1-06).

    3. Update `frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx`: replace the
       hero/working-split assertions with: (a) a multi-file fixture (some with `is_hero: true`) renders
       exactly N flat rows; (b) NO "★ Your file" caption present; (c) NO "Working files" group/toggle;
       (d) all rows downloadable; (e) a url-less entry renders "Download unavailable". Keep the
       data-testid="final-outputs-panel" assertion.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx` passes
    - `grep -c "★ Your file" frontend/src/components/chat/OutputFileCard.tsx` == 0
    - `grep -c "★ Your file" frontend/src/components/chat/MessageItem.tsx` == 0
    - `grep -c "Working files" frontend/src/components/chat/MessageItem.tsx` == 0 (the split group is gone)
    - `grep -c "f.is_hero\|is_hero)" frontend/src/components/chat/MessageItem.tsx` == 0 (frontend no longer reads is_hero in FinalOutputsPanel)
    - `grep -c "Download unavailable" frontend/src/components/chat/OutputFileCard.tsx` >= 1 (dead-state KEPT)
    - `grep -c "data-testid=\"final-outputs-panel\"" frontend/src/components/chat/MessageItem.tsx` >= 1
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/MessageItem.tsx` == 0 AND in OutputFileCard.tsx == 0 (T-095.1-05-01)
    - `cd frontend && npx tsc -b` zero net-new vs baseline 37
  </acceptance_criteria>
  <done>FinalOutputsPanel is one flat list; no hero crown/caption/split; url-less dead-state kept; is_hero unread by FE; test green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliverable-aware Resume gate (D-07)</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx, frontend/src/__tests__/components/MessageItem.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageItem.tsx:415-436 (the Resume gate + its mutual-exclusivity with streaming/cancelled/completed)
    - frontend/src/lib/api.ts:111-135 (how finalOutputFiles is reconstructed on reload — confirms the deliverable signal is reload-safe)
    - frontend/src/__tests__/components/MessageItem.test.tsx (the MessageItem render-test analog to extend, if Resume is covered there) OR add the Resume cases to MessageItem.finalOutputs.test.tsx
    - 095.1-RESEARCH.md §"D-095.1-07" + §"Code Examples" (the producedDeliverables predicate)
    - 095.1-PATTERNS.md §"MessageItem.tsx (MOD)" (the Resume gate extension)
  </read_first>
  <behavior>
    - failed run WITH finalOutputFiles (length > 0) → NO Resume button
    - failed run with NO finalOutputFiles → Resume button shown (unchanged)
    - timed_out run WITH deliverables → NO Resume
    - completed / cancelled / streaming → unchanged (no Resume, as today)
  </behavior>
  <action>
    `frontend/src/components/chat/MessageItem.tsx` — extend the Resume gate at :425. Define a local
    predicate near the gate:
    ```tsx
    const producedDeliverables = (message.finalOutputFiles?.length ?? 0) > 0
    ```
    Add `&& !producedDeliverables` to the existing condition:
    ```tsx
    {!isStreaming && message.role === "assistant" &&
      (message.runStatus === "failed" || message.runStatus === "timed_out") &&
      !producedDeliverables && (
        <Button ...>Resume</Button>
    )}
    ```
    Do NOT change the Continue card (workflowLock?.capPaused) sibling or any other branch. `finalOutputFiles`
    is reload-reconstructed (api.ts:114), so this works on reopened threads. "Deliverables" = execute_code
    output files specifically (CONTEXT D-095.1-07); do NOT additionally count workspace_write files (out of
    scope).

    Add render tests (in MessageItem.finalOutputs.test.tsx or MessageItem.test.tsx — whichever already
    renders the Resume button): (a) `runStatus: "failed"` + finalOutputFiles non-empty → Resume NOT in the
    document (`queryByRole("button", { name: /resume/i })` is null); (b) `runStatus: "failed"` + no
    finalOutputFiles → Resume present; (c) `runStatus: "timed_out"` + finalOutputFiles → Resume absent;
    (d) `runStatus: "completed"` → Resume absent (unchanged). Use `onResume` as a no-op prop.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx src/__tests__/components/MessageItem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx src/__tests__/components/MessageItem.test.tsx` passes (incl. the failed+deliverables→no-Resume case)
    - `grep -c "producedDeliverables" frontend/src/components/chat/MessageItem.tsx` >= 2 (declared + used in the gate)
    - the gate includes the predicate: `grep -c "!producedDeliverables" frontend/src/components/chat/MessageItem.tsx` >= 1
    - the Continue card branch is untouched: `grep -c "workflowLock?.capPaused" frontend/src/components/chat/MessageItem.tsx` unchanged from baseline
    - `cd frontend && npx tsc -b` zero net-new vs baseline 37
  </acceptance_criteria>
  <done>Resume gate is deliverable-aware (failed/timed_out + finalOutputFiles → no Resume); failed-without-deliverables still resumes; completed/cancelled/streaming unchanged; Continue card untouched; tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox/model → file rows | filenames + the is_hero flag are model/sandbox-derived strings rendered in the file list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095.1-05-01 | Tampering/XSS (V5 Output Encoding) | FinalOutputsPanel / OutputFileCard | mitigate | Filenames render as React text children via the shared `fileIcon` (parsed-ext-only) + plain text; never dangerouslySetInnerHTML. Acceptance greps `dangerouslySetInnerHTML`==0 in both files. |
| T-095.1-05-02 | Repudiation (false Resume) | Resume gate | mitigate | A run that produced deliverables never offers Resume; the predicate reads the reload-safe finalOutputFiles. Test proves failed+deliverables→no-Resume and failed+none→Resume. |
| T-095.1-05-03 | Information Disclosure (dishonest crown) | flat file list | accept→mitigate | Dropping the hero crown removes a dishonest "primary file" guess; the url-less "Download unavailable" affordance is KEPT so a dead link is never silently rendered as downloadable. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx src/__tests__/components/MessageItem.test.tsx` green
- `cd frontend && npx tsc -b` zero net-new vs baseline 37; `npx vite build` exit 0
- Manual UAT (VALIDATION.md, /gsd:verify-work): flat file list visual (no crown) + deliverable-aware Resume on a real successful-then-failed run, across providers
</verification>

<success_criteria>
- Output files render as one flat "Generated files" list, no crown, all downloadable, url-less → "Download unavailable"
- A failed/timed_out run with deliverables shows NO Resume; without deliverables still resumes; completed/cancelled/streaming unchanged
- is_hero unread by frontend; tsc/build clean
</success_criteria>

<output>
After completion, create `.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-05-SUMMARY.md`
</output>
