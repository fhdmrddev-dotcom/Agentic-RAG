---
phase: 095.1-cross-provider-run-honesty-workspace-parity
verified: 2026-06-06T17:15:00Z
status: human_needed
score: 6/7 must-haves statically verified
overrides_applied: 0
human_verification:
  - test: "Live 8-provider cross-provider UAT — SC#7 / SC#10 4-axis"
    expected: "Panel fills for ALL 8 providers on genuine multi-step work; 429 reads as rate-limit (not billing) on each provider; timer shows TRUE duration on day-old reload; model attribution visible at rest; flat files list with no crown; deliverable-aware Resume behaves correctly"
    why_human: "Real LLM streams required; timer honesty depends on actual DB completedAt; 429 forcing, parallel-thread isolation, and long-message axis require a live browser. Automated tests prove the mechanism is correct; live UAT proves it fires correctly across all providers."
---

# Phase 095.1: Cross-Provider Run Honesty & Workspace Parity — Verification Report

**Phase Goal:** Every provider's runs are honest and legible — the workspace todos/tasks panel fills consistently (deterministically, never forced), provider errors read truthfully, finished runs never falsely offer Resume, the timer never lies on reload, and the user can always see which model answered — verified live across all 8 providers.
**Verified:** 2026-06-06T17:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Summary

All 6 code requirements are statically ACHIEVED. Every implementing module exists, is substantive (well above line minimums), is wired into its consumer, and is test-green. The 7th success criterion (SC#7 — all-8-provider live UAT) is explicitly manual-only per VALIDATION.md and SC#10's 4-axis mandate. This report is a prerequisite input to `/gsd:verify-work 095.1`.

---

## Goal Achievement

### Observable Truths (Roadmap Success Criteria SC#1–SC#7)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC#1 | Workspace panel fills deterministically for ALL providers via smart-gate + activity-derivation; stays clean for simple chats | VERIFIED | `workspacePanel.ts` (223 lines): `shouldPopulate` (write_todos OR ≥2 MEANINGFUL deduped) + `deriveWorkspacePanel` (label precedence chain). `useDerivedPanel` wired into `StreamsProvider.tsx` + `TodosSection.tsx`. 31/31 unit tests green; 19/19 + 12/12 render tests green. |
| SC#2 | A 429 rate-limit reads as rate-limit with retry guidance, NEVER "billing" — at the per-provider gateway boundary | VERIFIED | `errors.py` (213 lines): `classify_provider_error` branches on `provider=="google"` using `.code` (int) not `.status_code`; 429 → `rate_limit` precedes ANY billing check. `message_for_kind("rate_limit")` never contains "billing". 30/30 unit tests green including the BUG-260606-01 headline case. Wired into `agent_loop.py` line 2271. |
| SC#3 | A run that produced deliverables but ended failed/timed_out does NOT falsely offer Resume | VERIFIED | `MessageItem.tsx` line 405: `!producedDeliverables` guard added to Resume gate (`producedDeliverables = (message.finalOutputFiles?.length ?? 0) > 0`). 11/11 render tests green (5 deliverable-aware Resume cases). |
| SC#4 | Reloaded completed run shows TRUE duration (completedAt − startedAt), never time-since-creation | VERIFIED | `RunCard.tsx` lines 121–161: `wasStreamingRef` gate distinguishes live→terminal (gets `Date.now()` frozen end) from reloaded already-terminal (must not fabricate). Terminal + no `completedAt` → no duration shown (honesty rule). 34/34 RunCard tests green (6 new timer cases including the day-old BUG-260606-02 guard). |
| SC#5 | Every assistant response surfaces which model/provider generated it (`{provider} · {model} · turn N`) | VERIFIED | `RunCard.tsx` lines 236–237: `runSub = message.provider && message.model ? \`${message.provider} · ${message.model} · turn ${turnNumber}\` : \`turn ${turnNumber}\``. Threaded through: `threads.py` additive SELECT → `MessageResponse` Pydantic → `api.ts` mapper (snake→camel, null→undefined) → `types/index.ts` `model?`/`provider?` optional fields. Backend enrich tests 5/5 green. |
| SC#6 | Output files render as ONE flat "Generated files" list — no hero crown, no hero/working split, all downloadable | VERIFIED | `MessageItem.tsx` `FinalOutputsPanel` is a flat `space-y-1.5` map of equal `OutputFileCard` rows. `OutputFileCard.tsx`: hero glow/gradient/48px/caption removed; uniform 30px icon; `variant` prop inert; "Download unavailable" affordance KEPT. `is_hero` written-but-unread (no backend change). 11/11 flat-list tests green. |
| SC#7 | All-8-provider live verification (SC#10 4-axis: multi-tool, parallel-thread, cross-provider, long-message) | HUMAN NEEDED | Requires live browser Chrome-MCP UAT per VALIDATION.md. Automated backstop is green; live confirmation is the remaining step. |

**Score:** 6/7 truths statically verified. SC#7 is manual-only by design (VALIDATION.md explicitly routes this to `/gsd:verify-work`).

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/lib/workspacePanel.ts` | MEANINGFUL_TOOLS, GATE_MIN_STEPS, shouldPopulate, inferLabel, deriveWorkspacePanel | VERIFIED | 223 lines; all 5 named exports present; uses `task` not `sub_agent`; imports `dedupToolCalls` from stepCount; no JSX/React/innerHTML |
| `frontend/src/lib/__tests__/workspacePanel.test.ts` | Gate + derive + label-precedence test matrix | VERIFIED | 31 tests green; covers gate matrix, derive precedence, inferLabel cases, write_todos supremacy |
| `backend/app/services/provider_gateway/errors.py` | ErrorKind, classify_provider_error, message_for_kind | VERIFIED | 213 lines; SDK imports guarded in try/except ImportError; no import of agent_loop or threads |
| `backend/app/services/provider_gateway/test_errors.py` | Classifier matrix including Google-429-not-billing headline | VERIFIED | 30 tests green; 3 wiring-guard tests added in Plan 04; BUG-260606-01 case explicitly present |
| `backend/app/services/provider_gateway/__init__.py` | Re-exports the 3 new symbols | VERIFIED | `from .errors import ErrorKind, classify_provider_error, message_for_kind` + `__all__` entries |
| `backend/app/services/agent_loop.py` | classify_provider_error wired at line 2271 | VERIFIED | `kind = classify_provider_error(_resolved_provider, e)` + `user_msg = message_for_kind(kind, err_str)`; billing-first keyword ladder fully replaced |
| `frontend/src/providers/StreamsProvider.tsx` | useDerivedPanel export (PANEL-06-safe read selector) | VERIFIED | Imports shouldPopulate + deriveWorkspacePanel; EMPTY_DERIVED stable ref; pure read, never writes todosByThread |
| `frontend/src/components/panel/TodosSection.tsx` | 3-way precedence: real todos → derived rows + marker → null | VERIFIED | DerivedRow component; "derived from activity" marker; real write_todos always wins; no onClick on derived rows |
| `backend/app/api/threads.py` | Additive SELECT + 4 null stamps in _enrich_messages_with_runs | VERIFIED | +model, +provider, +started_at, +completed_at in SELECT; WHERE byte-identical; 4 m[] stamps null-graceful |
| `backend/app/models/message.py` | MessageResponse += model/provider/started_at/completed_at (nullable) | VERIFIED | All 4 fields added as Optional |
| `frontend/src/lib/api.ts` | Mapper: snake→camel, null→undefined for 4 new fields | VERIFIED | `startedAt: started_at ?? undefined`, `completedAt: completed_at ?? undefined` explicitly destructured (not leaked through `...rest`) |
| `frontend/src/types/index.ts` | Message += model?/provider?/startedAt?/completedAt? | VERIFIED | All 4 optional fields present with doc-comments |
| `frontend/src/components/chat/RunCard.tsx` | run-sub `{provider}·{model}·turn N` + wasStreamingRef timer honesty | VERIFIED | Lines 236–237 (run-sub); lines 121–161 (wasStreamingRef + completedAt − startedAt); showElapsed gates the duration segment |
| `frontend/src/components/chat/RunCard.timer.test.tsx` | 6 timer tests (true duration, no-duration, live-tick, day-old guard) | VERIFIED | Created in Plan 03; 6 cases all green |
| `frontend/src/components/chat/MessageItem.tsx` | FinalOutputsPanel flat list + producedDeliverables Resume gate | VERIFIED | FinalOutputsPanel is a flat map; `producedDeliverables` predicate + `&& !producedDeliverables` in Resume gate |
| `frontend/src/components/chat/OutputFileCard.tsx` | Hero presentation removed; variant inert; dead-state kept | VERIFIED | 30px uniform icon; no glow/gradient/caption; "Download unavailable" preserved |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `workspacePanel.ts` | `stepCount.ts` | `import { dedupToolCalls }` | WIRED | Line 2: `import { dedupToolCalls } from "./stepCount"` |
| `StreamsProvider.tsx` | `workspacePanel.ts` | `import { shouldPopulate, deriveWorkspacePanel }` | WIRED | Lines 99–103 |
| `TodosSection.tsx` | `StreamsProvider.tsx` | `import { useDerivedPanel }` | WIRED | Line 20 |
| `provider_gateway/__init__.py` | `errors.py` | re-exports ErrorKind/classify_provider_error/message_for_kind | WIRED | Line 17 + __all__ lines 46–48 |
| `agent_loop.py` | `provider_gateway` | `from app.services.provider_gateway import classify_provider_error, message_for_kind` | WIRED | Line 67; consumed at lines 2268–2272 |
| `threads.py` _enrich | `runs` table | additive SELECT `model, provider, started_at, completed_at` | WIRED | Line 260; stamps at lines 283–286 |
| `api.ts` mapper | `types/index.ts` Message | `startedAt`/`completedAt`/`model`/`provider` coercion | WIRED | Lines 117–120 |
| `RunCard.tsx` | `message.provider`/`message.model` | `runSub` derivation at line 236–237 | WIRED | Rendered as text at line 296 |
| `MessageItem.tsx` | `message.finalOutputFiles` | `producedDeliverables` predicate gate | WIRED | Line 228 (predicate) + line 405 (gate) |

---

## Data-Flow Trace (Level 4)

### WORKSPACE-PARITY panel derivation

| Stage | What flows | Source produces real data |
|-------|-----------|--------------------------|
| `tool_calls` in messages | Persisted DB truth, reconstructed by `_mapMessageResponse` on reload | Real — `095` invariant proven (reload-reconstruction) |
| `dedupToolCalls` | Deduped tool call array | Real — reads the persisted array, no static defaults |
| `shouldPopulate` | Gate result (boolean) | Real — computed from actual tool names |
| `deriveWorkspacePanel` | DerivedPanelItem[] with real labels | Real — reads `tc.args.description` / `tc.args.code` from the persisted call |
| `useDerivedPanel` in StreamsProvider | Memoized derivation over stable `Message[]` ref | Real — wraps deriveWorkspacePanel over the chat bucket |
| `TodosSection` DerivedRow | Renders item.label as text child | Real — plain string, no innerHTML |

**Status: FLOWING**

### RUN-HONESTY model attribution + timer

| Stage | What flows | Source produces real data |
|-------|-----------|--------------------------|
| `runs` table | `model`, `provider`, `started_at`, `completed_at` | Real — NOT NULL at insertion; `completed_at` written on finalize |
| `_enrich_messages_with_runs` SELECT | 4 new columns via join | Real — additive SELECT, same WHERE; null for no-run messages (graceful) |
| `MessageResponse` Pydantic | 4 optional fields | Real — nullable passthrough from query |
| `api.ts` mapper | `startedAt`/`completedAt`/`model`/`provider` | Real — explicit destructure, null→undefined |
| `RunCard` `runSub` | `{provider} · {model} · turn N` | Real — reads message.provider/model from DB |
| `RunCard` timer | `completedAt − startedAt` (terminal) or live tick (streaming) | Real — `wasStreamingRef` prevents fabrication on reload |

**Status: FLOWING**

---

## Behavioral Spot-Checks

Step 7b skipped for live streaming/browser paths (requires uvicorn + Chrome MCP). Backend and frontend unit/integration tests serve as the automated backstop.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| workspacePanel gate + derive | `vitest run workspacePanel.test.ts` | 31/31 | PASS |
| Google 429 → rate_limit (not billing) | `pytest test_errors.py` | 30/30 | PASS |
| Agent-loop wiring guards | `pytest test_errors.py -k wiring` | 3/3 | PASS |
| Backend enrich SELECT shape | `pytest test_threads.py -k "enrich or runs"` | 5/5 | PASS |
| RunCard timer honesty | `vitest run RunCard.timer.test.tsx RunCard.test.tsx` | 34/34 | PASS |
| TodosSection precedence | `vitest run TodosSection.test.tsx` | 12/12 | PASS |
| panelHooks PANEL-06 isolation | `vitest run panelHooks.test.tsx` | 19/19 | PASS |
| Flat files + deliverable-aware Resume | `vitest run MessageItem.finalOutputs.test.tsx` | 11/11 | PASS |
| TypeScript baseline | `tsc -b` | 37 errors = documented baseline, zero net-new | PASS |
| Backend import smoke | `python -c "from app.services.provider_gateway import classify_provider_error"` | exit 0 | PASS |

---

## Cross-Provider Invariant Audit

This phase was specifically instructed to maintain provider-agnostic shared paths. Audit result:

### threads.py (G-5 hot file)

The diff is confined to `_enrich_messages_with_runs` only:
- **Removed:** `.select("run_id, message_id, status")` — 1 line
- **Added:** `.select("run_id, message_id, status, model, provider, started_at, completed_at")` + 4 null stamps — 5 lines

No change to: `_emit`, `event_consumer`, `send_message`, `stream_run`, `list_active_runs`, or any SSE event vocabulary. The WHERE clause (`thread_id` + `user_id` + RLS `runs_select_own`) is byte-identical — no IDOR surface widened. The shared chunk path is untouched.

**Verdict: ADDITIVE-ONLY, provider-agnostic. Invariant HELD.**

### agent_loop.py

The diff at the error-catch site:
- **Removed:** The billing-first keyword if-ladder (~15 lines). The old first branch checked "billing"/"quota" before anything else, making Google RESOURCE_EXHAUSTED (text contains "quota") read as billing.
- **Added:** 3 lines — context-overflow narrow pre-check (retained for the one case with no reliable structured status code), `kind = classify_provider_error(_resolved_provider, e)`, `user_msg = message_for_kind(kind, err_str)`.
- **Unchanged:** `full_content += user_msg`, `await _emit(redis, run_id, 'delta', content=user_msg)`, `await _emit(redis, run_id, 'error', message=err_str)`, `raise`. The SSE event types (`delta`, `error`), the `_is_transient_provider_error` retry path (line 1838), and the request-too-large 429 branch are all byte-identical.
- **No adapter touched:** `provider_gateway/anthropic.py`, `google.py`, `openai_compat.py` are unchanged. The classifier lives in a pure helper called at the consumer catch site, not inside the byte-identical generator streams. This matches the D-095.1-03 / RESEARCH Pitfall 5 invariant.

**Verdict: PROVIDER-SCOPED FIX at consumer catch only. Shared SSE path byte-identical. Adapters untouched. Invariant HELD.**

### StreamsProvider.tsx / TodosSection.tsx

Changes are additive panel-side derivations:
- `useDerivedPanel` is a READ-ONLY selector; it never writes `todosByThread` (verified: `replaceTodosForThread` not called within the hook). PANEL-06 / FC#1 isolation confirmed by the panelHooks render-count test.
- No modification to the existing streaming event handling, no `onDelta`/`onToolStart`/`onToolEnd`/`onDone` changes.

**Verdict: PANEL-SIDE PURE READ ONLY. Streaming path unchanged. Invariant HELD.**

---

## Requirements Coverage

| Requirement | Plans | Description | Status | Evidence |
|-------------|-------|-------------|--------|----------|
| WORKSPACE-PARITY | 01, 02, 05 | Deterministic panel fill (D-01/02) + flat files list (D-06) | SATISFIED | workspacePanel.ts + useDerivedPanel + TodosSection + FinalOutputsPanel flatten |
| RUN-HONESTY | 03, 05 | Model attribution (D-04) + true timer (D-05) + deliverable-aware Resume (D-07) | SATISFIED | threads.py additive SELECT + RunCard run-sub + wasStreamingRef timer + producedDeliverables gate |
| PROVIDER-ERR | 01, 04 | Gateway-boundary error classification (D-03), Google 429 → rate_limit | SATISFIED | errors.py classify_provider_error + message_for_kind + agent_loop wiring at line 2271 |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None detected in the 12 touched source files. The url-less "Download unavailable" affordance and the no-duration-when-no-completedAt behavior are INTENTIONAL honesty rules, not stubs. The context-overflow narrow pre-check in agent_loop.py is a DOCUMENTED intentional retention (the one case with no reliable structured status code). | | | | |

---

## Test Baseline Confirmation

| Suite | Before (baseline) | After | Net-new failures |
|-------|-------------------|-------|-----------------|
| Frontend vitest | 17 failed / 497 passed (documented) | 17 failed / 548 passed | **ZERO** — same 7-file pre-existing cluster; 51 more passing (new tests) |
| Backend test_threads.py | 2 failed (pre-existing `create_streaming_chat` attribute error) | 2 failed | **ZERO** — confirmed identical via git stash |
| Frontend tsc -b | 37 errors (documented baseline) | 37 errors | **ZERO** |

---

## Statically Proven vs Needs-Live-UAT

### Statically proven (this report)

- The smart-gate algorithm (`shouldPopulate`) correctly implements the spike-006 logic with the CONTEXT-authoritative MEANINGFUL_TOOLS set (10 tools, `task` not `sub_agent`).
- The label derivation (`deriveWorkspacePanel` + `inferLabel`) produces the correct spike-007 output for all fixture cases (print, csv, chart, leading-comment, description precedence, write_todos supremacy).
- `useDerivedPanel` is PANEL-06-safe (read-only selector, never writes the panel store, stable EMPTY ref).
- `classify_provider_error("google", exc_with_code_429)` returns `"rate_limit"` — not `"billing"` — for any Google exception with HTTP code 429, regardless of whether the message text contains "quota".
- `message_for_kind("rate_limit")` never contains the word "billing".
- The billing-first keyword ladder in `agent_loop.py` is fully replaced; the context-overflow narrow pre-check is the only retained keyword case.
- The `_enrich_messages_with_runs` SELECT is additive-columns-only with byte-identical WHERE; no IDOR surface widened.
- `RunCard` derives `completedAt − startedAt` for terminal runs; `wasStreamingRef` prevents fabricating an end-time on reload; the honesty rule (no completedAt → no duration) is enforced and test-green.
- `producedDeliverables` predicate correctly gates the Resume button off when `finalOutputFiles.length > 0`.
- `FinalOutputsPanel` is a flat equal list; `OutputFileCard` no longer reads `is_hero`; "Download unavailable" is kept.
- All SSE event types, `_emit` signatures, adapters, and the transient-retry path are byte-identical.

### Requires live `/gsd:verify-work 095.1` UAT (SC#7 / SC#10 4-axis)

Per VALIDATION.md and SC#10, the following require a real browser with Chrome-MCP against the running app:

1. **Cross-provider panel fill/clean (8 providers):** real LLM streams required to confirm panel fills for every provider on genuine multi-step work and stays clean for Q&A. Includes OpenRouter/Ollama (best-effort).
2. **Multi-tool derived items:** a prompt exercising `search_documents` + `execute_code` → derived panel shows both as semantic items, neither labeled "Run code".
3. **Flat file list visual:** a multi-file run → ONE flat "Generated files" list, no "★ Your file", all downloadable.
4. **Parallel-thread timer/attribution isolation:** Thread A streaming while Thread B accepts a prompt → neither run-sub/timer corrupts the other.
5. **True reload duration (≥50-msg / ≥5 KB):** reopen a day-old completed run → timer shows TRUE `completedAt − startedAt`, not inflated (BUG-260606-02 live guard).
6. **Model attribution at rest (8 providers):** each provider response → run-sub shows `{provider} · {model}` without expanding.
7. **Deliverable-aware Resume:** a run that produced files then failed/timed_out → NO Resume; a run that failed with no deliverables → Resume present.
8. **429 truthful error (force real rate-limit):** Google over-quota → reads as rate-limit-with-retry, NOT "billing / insufficient credits".

---

## Human Verification Required

### 1. All-8-provider cross-provider + 4-axis UAT (SC#7 / SC#10)

**Test:** For each of the 8 providers (openai, anthropic, google, deepseek, moonshot, glm/zhipu, minimax, openrouter), run a genuine multi-step prompt (≥2 meaningful tools) and verify the workspace todos panel fills. Then run a one-shot Q&A and verify the panel stays clean.
**Expected:** Every provider's panel reflects the real tasks it performed. The parity bar is the OpenAI gpt-5.4-mini run showing "print the numbers / create the CSV / make the chart".
**Why human:** Real LLM streams required; panel fill cannot be asserted in Vitest against a fixture because the live stream is the data source.

### 2. Multi-tool derived items

**Test:** A prompt on each provider exercising `search_documents` + `execute_code` in one run.
**Expected:** Derived panel shows both items with semantic labels (e.g. "Search documents" and either the description or code-inferred label), neither labeled "Run code".
**Why human:** Requires live tool dispatch and panel observation.

### 3. Flat file list visual (no crown)

**Test:** A run that produces multiple output files.
**Expected:** ONE flat "Generated files" section; all files shown as equal rows; no "★ Your file" header or caption; all present with their download links (or "Download unavailable" if no URL).
**Why human:** Visual confirmation required.

### 4. Parallel-thread timer/attribution isolation

**Test:** Thread A streaming (live-ticking timer) while Thread B accepts a new prompt.
**Expected:** Thread A's timer and run-sub are unaffected by Thread B's activity; neither shows stale/corrupted attribution or timer.
**Why human:** Requires two concurrent live runs.

### 5. True reload duration (long-message / day-old reopen)

**Test:** Reopen a day-old completed run on any provider. Observe the timer. Also run with a ≥50-message or ≥5 KB prompt and reload.
**Expected:** Timer shows `completedAt − startedAt` (typically seconds/minutes), never "1440m" (BUG-260606-02 guard).
**Why human:** Day-old DB data required; live clock delta test not automatable.

### 6. Model attribution at rest (8 providers)

**Test:** Send a message on each provider, observe the run-sub under the RunCard title.
**Expected:** Reads `{provider} · {model} · turn N` (e.g. `google · gemini-3.5-flash · turn 1`) without expanding the card.
**Why human:** Requires visual confirmation per provider.

### 7. Deliverable-aware Resume

**Test:** Engineer a run that produces files (execute_code output) then fails/times out. Separately, engineer a run that fails with NO output files.
**Expected:** First case → NO Resume button. Second case → Resume button present.
**Why human:** Requires triggering a real failed run with/without deliverables.

### 8. 429 truthful error (Google over-quota)

**Test:** Force a real 429 from Google (trigger quota exhaustion or use a rate-limited key).
**Expected:** Error message reads as rate-limit-with-retry ("Rate limited by the provider — please retry in a moment."), NOT "billing / insufficient credits".
**Why human:** Real provider 429 required; the unit test proves the mechanism, but live confirmation proves the wiring fires correctly end-to-end.

---

## Gaps Summary

No blocking gaps. All 6 code-deliverable success criteria are verified. SC#7 is manual-only by design (live Chrome-MCP UAT per VALIDATION.md). No anti-patterns or stubs found in any of the 12 touched source files. The phase is structurally complete and ready for `/gsd:verify-work 095.1`.

---

_Verified: 2026-06-06T17:15:00Z_
_Verifier: Claude (gsd-verifier)_
