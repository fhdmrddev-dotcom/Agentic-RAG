---
phase: 095.1-cross-provider-run-honesty-workspace-parity
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backend/app/services/agent_loop.py
autonomous: true
requirements: [PROVIDER-ERR]
nyquist_compliant: true
user_setup: []

must_haves:
  truths:
    - "A 429 rate-limit (any provider, incl. Google RESOURCE_EXHAUSTED) reads as a rate-limit-with-retry message, NEVER 'billing / insufficient credits'"
    - "Error classification uses the per-provider structured-status classifier, not the keyword-soup ladder"
    - "An uncertain/unknown error shows a neutral truthful message (the provider returned an error) + bounded raw detail, never a guessed cause"
    - "The shared chat path (delta + error emit + raise) stays byte-identical — no adapter generator touched, no new SSE event"
  artifacts:
    - path: "backend/app/services/agent_loop.py"
      provides: "The keyword if-ladder replaced by classify_provider_error + message_for_kind"
      contains: "classify_provider_error"
  key_links:
    - from: "backend/app/services/agent_loop.py"
      to: "backend/app/services/provider_gateway/errors.py"
      via: "classify_provider_error(_resolved_provider, e) + message_for_kind(kind, err_str)"
      pattern: "classify_provider_error"
---

<objective>
Replace the billing-first keyword classifier at `agent_loop.py:2251-2280` with the Plan-01
gateway-boundary classifier (D-095.1-03), so a 429 — including Google's `RESOURCE_EXHAUSTED`
whose text contains "quota" — is classified as a rate-limit, NEVER billing (closes
BUG-260606-01).

The classification logic lives in the gateway package (`provider_gateway/errors.py`, Plan 01);
this plan WIRES it into the consumer's existing catch block, keyed on the in-scope resolved
provider. The adapters stay untouched (no try/except added to the byte-identical clean-move
generators — RESEARCH Pitfall 5), no new SSE event, and the `full_content += user_msg;
_emit('delta'); _emit('error'); raise` flow stays byte-identical.

Purpose: honest provider errors across all 8 providers from reliable structured signals
instead of keyword soup. Provider-knowledge moves into the gateway package; the shared chat
path is rendered uniformly.

Output: 1 swap of the keyword if-ladder for the classifier call in agent_loop.py.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-CONTEXT.md
@.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-RESEARCH.md
@.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-PATTERNS.md
@backend/app/services/provider_gateway/errors.py

<interfaces>
<!-- The exact edit point + the in-scope provider variable (CONFIRMED by the planner). -->

From backend/app/services/provider_gateway/errors.py (Plan 01 — the helper to call):
```python
def classify_provider_error(provider: str, exc: Exception) -> ErrorKind
def message_for_kind(kind: ErrorKind, raw_detail: str = "") -> str
```
Importable as: `from app.services.provider_gateway import classify_provider_error, message_for_kind`

The in-scope provider variable (CONFIRMED by the planner reading agent_loop.py):
`_resolved_provider = ctx.resolved_provider` is set at agent_loop.py:956, inside `run_agent_loop`
(defined at :893). The catch block at :2242 is in the SAME function (no intervening top-level def),
so `_resolved_provider` IS in scope at the catch — use it as the provider key. (Resolves RESEARCH
Open Question 2 / Assumption A1.)

backend/app/services/agent_loop.py:2242-2295 — the catch block (the keyword ladder is the ONLY part replaced):
```python
except (APIError, anthropic.APIError, google_errors.APIError) as e:
    logger.error("LLM API error in event stream (thread %s): %s", thread_id, e)
    err_str = str(e)
    err_lower = err_str.lower()
    if any(kw in err_lower for kw in ("credit balance", "billing", "quota", ...)):   # ← BUG: billing-first, "quota" trips Google 429
        user_msg = ("*API billing or rate-limit error: ...*")
    elif ... (auth / unsupported param / context / transient / else)                  # ← the whole if-ladder, :2251-2280, is replaced
    # KEEP byte-identical (:2288-2295):
    full_content += user_msg
    await _emit(redis, run_id, 'delta', content=user_msg)
    await _emit(redis, run_id, 'error', message=err_str)
    raise
```
The special-case "request too large" 429 branch (:1812-1831) and the transient-retry path
(`_is_transient_provider_error`, :436/:1833) are DISTINCT retry logic — DO NOT touch them; this
plan replaces ONLY the terminal-classification keyword ladder in this outer catch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire classify_provider_error + message_for_kind into the agent_loop catch block</name>
  <files>backend/app/services/agent_loop.py, backend/app/services/provider_gateway/test_errors.py</files>
  <read_first>
    - backend/app/services/agent_loop.py:2242-2295 (the full catch block — the keyword ladder to replace + the byte-identical emit/raise flow to preserve)
    - backend/app/services/agent_loop.py:893,956 (confirm run_agent_loop scope + `_resolved_provider = ctx.resolved_provider`)
    - backend/app/services/agent_loop.py:1812-1833 + :436 (the request-too-large 429 + transient-retry paths — DO NOT touch)
    - backend/app/services/provider_gateway/errors.py (Plan 01 — classify_provider_error + message_for_kind + the ErrorKind values)
    - backend/app/services/provider_gateway/test_errors.py (Plan 01 — the classifier matrix this wiring relies on)
    - 095.1-RESEARCH.md §"D-095.1-03" the concrete-change step 3 + Pitfall 5 + Assumption A1/Open-Q2
    - 095.1-PATTERNS.md §"agent_loop.py (MOD)"
  </read_first>
  <behavior>
    - The keyword if-ladder (:2251-2280) is replaced by: kind = classify_provider_error(_resolved_provider, e); user_msg = message_for_kind(kind, err_str)
    - A Google 429/RESOURCE_EXHAUSTED no longer maps to the billing message (it maps to rate_limit)
    - The context-window overflow case is preserved (classifier returns context_overflow OR the message_for_kind copy covers it; if the keyword "context"/"too long" detection is dropped, map it via the classifier's context_overflow kind OR keep a minimal pre-check — see action)
    - The emit/raise flow (full_content += user_msg; _emit delta; _emit error; raise) is byte-identical
    - No adapter generator is touched; no new SSE event; the 'final_output_files'/Deep stream contract is unchanged
  </behavior>
  <action>
    Edit ONLY the keyword if-ladder at `backend/app/services/agent_loop.py:2251-2280`. At the top of the
    file's imports add `from app.services.provider_gateway import classify_provider_error, message_for_kind`
    (place beside the existing provider_gateway imports if present, else with the other app.services imports).

    Replace the `if any(kw in err_lower ...) ... elif ... else: user_msg = ...` ladder with:
    ```python
    kind = classify_provider_error(_resolved_provider, e)
    user_msg = message_for_kind(kind, err_str)
    ```
    Keep the `logger.error(...)` line and `err_str = str(e)` above it. Remove the now-unused `err_lower`
    if nothing else references it in the block (grep first — if `err_lower` is used elsewhere in scope,
    leave it).

    CONTEXT-WINDOW PRESERVATION: the old ladder mapped context/overflow keywords to a "conversation too
    long" message. `classify_provider_error` returns `context_overflow` only if a structured signal proves
    it (usually it won't — overflow often arrives as a 400 bad_request). To avoid regressing the helpful
    context-overflow copy, do ONE of:
    - (preferred) keep a SINGLE minimal pre-check BEFORE the classifier call: if the error text clearly
      indicates context overflow (`any(kw in err_lower for kw in ("context", "maximum context", "too long",
      "token limit"))`), set `kind = "context_overflow"` and skip the classifier; else call the classifier.
      This is a targeted retention of the one keyword case that has no reliable structured code, NOT the
      billing-first soup. Document it inline as "context-overflow has no reliable structured status; retained
      as a narrow text pre-check — all other classification is structured (D-095.1-03)."
    Keep the request-too-large 429 branch (:1812-1831) and `_is_transient_provider_error` retry path
    (:436/:1833) BYTE-IDENTICAL — do not touch them.

    Preserve the emit/raise tail (:2288-2295) BYTE-IDENTICAL: `full_content += user_msg`, `await _emit(redis,
    run_id, 'delta', content=user_msg)`, `await _emit(redis, run_id, 'error', message=err_str)`, `raise`.

    Add a regression test (extend test_errors.py from Plan 01 is the wrong home — add a focused agent-loop
    wiring test). Create/extend a small test asserting the WIRING: that the catch-block path, given a Google
    429-shaped exception, produces a user_msg that does NOT contain "billing" and DOES read as rate-limit.
    If a full agent_loop catch-block test is too heavy (it requires a streaming harness), instead add an
    assertion in `backend/app/services/provider_gateway/test_errors.py` that exercises the exact composition
    used here — `message_for_kind(classify_provider_error("google", google_429_exc), str(google_429_exc))`
    contains no "billing" and reads as rate-limit — and reference it as the wiring guard. Document which
    surface the wiring test lives on in the SUMMARY.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest app/services/provider_gateway/test_errors.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest app/services/provider_gateway/test_errors.py -x` passes (incl. the google-429→rate_limit + the no-"billing"-in-rate_limit-copy assertions)
    - `grep -c "classify_provider_error(_resolved_provider" backend/app/services/agent_loop.py` == 1
    - `grep -c "message_for_kind(" backend/app/services/agent_loop.py` >= 1
    - the billing-first keyword check is GONE: `grep -c "credit balance.*billing.*quota" backend/app/services/agent_loop.py` == 0 AND the old `if any(kw in err_lower for kw in ("credit balance"` ladder line is removed
    - the byte-identical emit/raise tail is intact: `grep -c "await _emit(redis, run_id, 'error', message=err_str)" backend/app/services/agent_loop.py` >= 1
    - `grep -c "from app.services.provider_gateway import" backend/app/services/agent_loop.py` >= 1 (or the classifier symbols appear in an existing gateway import)
    - `cd backend && venv/Scripts/python -c "import app.services.agent_loop"` exits 0 (module imports clean)
    - the request-too-large + transient paths untouched: `grep -c "_is_transient_provider_error" backend/app/services/agent_loop.py` unchanged from baseline
  </acceptance_criteria>
  <done>Keyword ladder replaced by classify_provider_error(_resolved_provider, e) + message_for_kind; Google-429→rate_limit (not billing); context-overflow retained as a narrow pre-check; emit/raise byte-identical; adapters untouched; module imports clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider SDK exception → user-facing error copy | a provider exception's raw message flows into the rendered error message |
| gateway classifier → shared chat emit path | the classification must not perturb the byte-identical Deep stream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095.1-04-01 | Information Disclosure | agent_loop error message | mitigate | `message_for_kind` (Plan 01) bounds the raw detail to 300 chars and only on the `unknown` kind; the specific-kind copies don't interpolate raw detail. No stack trace / key surfaced. |
| T-095.1-04-02 | Repudiation (dishonest cause) | error classification | mitigate | Classification is structured-status only; billing is claimed ONLY when structurally proven; 429→rate_limit always precedes billing; uncertain→neutral message. Google-429→rate_limit is the regression-tested headline. |
| T-095.1-04-03 | Tampering (Deep byte-identical regression) | shared stream path | mitigate | The classifier is a PURE helper called from the EXISTING consumer catch; NO adapter generator touched, NO new SSE event, the emit/raise tail is byte-identical. Acceptance greps confirm the emit tail + transient paths are intact. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest app/services/provider_gateway/test_errors.py -x` green
- `cd backend && venv/Scripts/python -c "import app.services.agent_loop"` exits 0
- Backend touched-suite sanity: the agent_loop-importing test files still pass (run the existing 089-seam / continue / registry-sweep suites — zero net-new failures)
- Manual UAT (VALIDATION.md, /gsd:verify-work): force a real 429 (Google over quota) → reads as rate-limit-with-retry, NOT billing — across providers
</verification>

<success_criteria>
- A 429 (incl. Google RESOURCE_EXHAUSTED) reads as rate-limit-with-retry, never billing
- Classification is structured per-provider via the gateway classifier; uncertain→neutral
- Deep stream byte-identical (no adapter/SSE-event change); module imports clean; emit/raise tail intact
</success_criteria>

<output>
After completion, create `.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-04-SUMMARY.md`
</output>
