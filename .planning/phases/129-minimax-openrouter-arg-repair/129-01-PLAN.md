---
phase: 129-minimax-openrouter-arg-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/openai_service.py
  - backend/app/config.py
  - backend/tests/test_129_openrouter_require_params.py
autonomous: true
requirements: [MP-04]

must_haves:
  truths:
    - "D-02 / MP-04: an OpenRouter request on openrouter_tool_strategy=='quality' carries extra_body['provider']={'require_parameters': True} alongside the existing response-healing plugin and :exacto model suffix"
    - "D-02 / MP-04: an OpenRouter request on the 'native' or 'xml' strategy carries NO 'provider' key in extra_body (opt-in via the strategy the user already selected — no always-on behavior change)"
    - "D-02 / MP-04: a non-OpenRouter provider (openai/anthropic/google) request is byte-identical — the require_parameters injection is inside the existing provider=='openrouter' gate and never fires for the shared path (D-14 RED LINE)"
    - "D-02 / MP-04: the config.py:342-346 D-15 directive comment is reconciled to reflect that require_parameters is now actually wired (no longer documented-but-unwired)"
  artifacts:
    - path: "backend/app/services/openai_service.py"
      provides: "OpenRouter require_parameters injection inside the quality-strategy extra_body block"
      contains: "require_parameters"
    - path: "backend/tests/test_129_openrouter_require_params.py"
      provides: "Unit assertions on assembled extra_body shape (quality-only, absent on native/xml and non-openrouter)"
      exports: ["test_require_parameters_quality_only"]
  key_links:
    - from: "backend/app/services/openai_service.py"
      to: "extra_body['provider']"
      via: "kwargs['extra_body']['provider'] = {'require_parameters': True} inside if provider == 'openrouter'"
      pattern: "extra_body.*provider.*require_parameters"
---

<objective>
Wire the OpenRouter `require_parameters` routing flag (D-02 / MP-04) into the EXISTING `openrouter_tool_strategy=="quality"` request-build block, so OpenRouter excludes upstream providers that would silently drop the tool schema. This implements the already-documented-but-never-wired D-15 directive (config.py:344). Additive, opt-in via the quality strategy the user already selects — no new Settings surface, no always-on behavior change, no shared-path fork.

Purpose: Broader tool-schema honoring for routed OpenRouter models (the second half of MP-04). Net security gain — `require_parameters` is itself a hardening (excludes non-compliant upstreams).
Output: One-line injection in `openai_service.py`, a reconciled D-15 comment in `config.py`, and a unit test asserting the assembled `extra_body` shape.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-RESEARCH.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-PATTERNS.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md

<interfaces>
<!-- The EXACT block to extend. Use these directly — no codebase exploration needed. -->

From backend/app/services/openai_service.py:1610-1625 (the quality-strategy block — D-02 injects one line here):
```
# OpenRouter quality strategy enhancements
if user_settings and getattr(user_settings, "openrouter_tool_strategy", "quality") == "quality":
    # D-09 #3 (BUG-260616-01): gate on the RESOLVED provider, not the model string ...
    if provider == "openrouter":
        if ":exacto" not in effective_model:
            kwargs["model"] = f"{effective_model}:exacto"
        kwargs.setdefault("extra_body", {})
        kwargs["extra_body"]["plugins"] = [{"id": "response-healing"}]
        # <-- D-02 NEW LINE GOES HERE, immediately after the plugins line
```

Confirmed OpenRouter request shape (official docs, RESEARCH §"Code Examples"):
`extra_body["provider"] = {"require_parameters": True}` → produces a top-level `"provider": {"require_parameters": true}` request-body object.

From backend/app/config.py:342-346 (the D-15 directive comment — reconcile, do not delete):
```
# Phase 101.1 D-15: OpenRouter is TIER-FORCE *conditional* ...
# The forcing adapter MUST send provider.require_parameters=true so OpenRouter does
# NOT silently downgrade. ...
```

Test scaffold analog — backend/tests/test_provider_router.py (mocked request-build shape assert, no live call): uses SimpleNamespace user_settings, builds kwargs, asserts on kwargs["extra_body"].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject OpenRouter require_parameters into the quality-strategy extra_body block (D-02)</name>
  <files>backend/app/services/openai_service.py, backend/app/config.py</files>
  <read_first>
    - backend/app/services/openai_service.py:1605-1632 (the file being modified — the quality block + the surrounding kwargs assembly and `stream = client.chat.completions.create(**kwargs)` at :1631)
    - backend/app/config.py:342-346 (the D-15 directive comment to reconcile)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-PATTERNS.md §"openai_service.py — D-02" (the exact analog block + Pitfall 4/5 anti-patterns)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-RESEARCH.md §"Code Examples" (confirmed request shape) + §"Common Pitfalls" Pitfall 4 + Pitfall 5
  </read_first>
  <action>
    In `backend/app/services/openai_service.py`, inside the EXISTING `if provider == "openrouter":` branch at ~:1619-1625 (already guarded by `openrouter_tool_strategy == "quality"` at :1611), add ONE statement immediately AFTER the `kwargs["extra_body"]["plugins"] = [{"id": "response-healing"}]` line (:1625): set `kwargs["extra_body"]["provider"] = {"require_parameters": True}`. The `kwargs.setdefault("extra_body", {})` on the preceding line guarantees `extra_body` exists. Add a brief inline comment citing `D-02 / config.py:344 D-15` and the rationale (exclude upstreams that silently drop the tool schema).

    Pitfall 4 (RED LINE): the injection MUST stay inside BOTH the `strategy == "quality"` guard (:1611) and the `provider == "openrouter"` gate (:1619). Do NOT hoist it to a shared `extra_body` line — that would narrow routing for `native`/`xml`-strategy users who did not opt in, and would touch the shared path for non-OpenRouter providers (D-14). Pitfall 5: this stacks a THIRD OpenRouter routing modifier next to `:exacto` and `plugins:[response-healing]`; the combined behavior is confirm-at-execution (Plan 03 SC#10 OpenRouter before/after row) — do not attempt to special-case the interaction here.

    In `backend/app/config.py:342-346`, reconcile the D-15 comment: it currently implies the forcing adapter "MUST send provider.require_parameters=true" as an unwired directive. Update the wording so it reads as now-wired (point at the `openai_service.py` quality block), or trim the "but doesn't" implication. Do NOT remove the D-15 reference or change any registry row values — comment-only edit.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import ast; ast.parse(open('app/services/openai_service.py').read()); ast.parse(open('app/config.py').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `openai_service.py` sets `kwargs["extra_body"]["provider"] = {"require_parameters": True}` ONLY inside the `provider == "openrouter"` branch that is itself inside the `openrouter_tool_strategy == "quality"` guard (grep: the new `require_parameters` literal appears exactly once and within those two gates).
    - The new statement is positioned AFTER the existing `plugins` assignment (so `extra_body` already exists / does not stomp `plugins`).
    - No edit outside the quality+openrouter double-gate; the `else:` structured-mode branch and the non-openrouter path are byte-identical.
    - `config.py` D-15 comment (:342-346) reconciled to reflect wired state; no registry row value changed; both files parse (ast verify exits 0).
  </acceptance_criteria>
  <done>OpenRouter quality-strategy requests carry `provider.require_parameters=true`; native/xml and non-OpenRouter requests unchanged; config D-15 note accurate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test the require_parameters injection shape (quality-only, shared-path safe)</name>
  <files>backend/tests/test_129_openrouter_require_params.py</files>
  <behavior>
    - test_require_parameters_quality_only: when provider=="openrouter" AND openrouter_tool_strategy=="quality", the assembled request kwargs contain extra_body["provider"] == {"require_parameters": True} (alongside extra_body["plugins"] == [{"id":"response-healing"}]).
    - native/xml strategy: extra_body has NO "provider" key (the quality enhancements block is skipped).
    - non-openrouter provider (e.g. openai) with quality strategy: no "provider":{"require_parameters"} injected (the provider=="openrouter" gate blocks it) — shared-path byte-identical.
  </behavior>
  <read_first>
    - backend/tests/test_provider_router.py:1-60 (the established mocked request-build unit scaffold — SimpleNamespace settings, kwargs assembly, extra_body shape asserts)
    - backend/app/services/openai_service.py:1395-1632 (the request-build function under test — how `provider`, `effective_model`, `user_settings`, `openrouter_tool_strategy`, and `kwargs["extra_body"]` are assembled; pick the smallest entry point that lets you assert on the assembled kwargs without a live `client.chat.completions.create` call)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-PATTERNS.md §"test_129_openrouter_require_params.py" (role-match analog) + .planning/research memory note feedback_mock_completeness.md guidance (mock ALL network deps)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-RESEARCH.md §"Validation Architecture" → the `test_require_parameters_quality_only` test-id row
  </read_first>
  <action>
    Create `backend/tests/test_129_openrouter_require_params.py` following the `test_provider_router.py` mocked-scaffold pattern. Mock the OpenAI client so `client.chat.completions.create(**kwargs)` is a no-op spy (capture `kwargs`, return a MagicMock stream) — NO live API call (follow feedback_mock_completeness.md: mock ALL network deps; explicit MagicMock attrs). Drive the request-build path for three scenarios and assert on the captured `kwargs["extra_body"]`:
    (1) provider="openrouter" + openrouter_tool_strategy="quality" → `extra_body["provider"] == {"require_parameters": True}` AND `extra_body["plugins"] == [{"id": "response-healing"}]`;
    (2) provider="openrouter" + strategy="native" (and a separate "xml" case) → `"provider"` key absent from `extra_body` (or `extra_body` absent entirely);
    (3) provider="openai" + strategy="quality" → no `require_parameters` injection (shared-path safety).
    Use the exact test function name `test_require_parameters_quality_only` for the quality case (RESEARCH test-map id). If the request-build entry point requires more fixtures than the spy (supabase/redis), reuse the `tests/integration/_run_helpers` mock builders the analog imports rather than re-scaffolding.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_129_openrouter_require_params.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_129_openrouter_require_params.py -x` exits 0.
    - The file contains `test_require_parameters_quality_only` asserting `extra_body["provider"] == {"require_parameters": True}` for the quality+openrouter case.
    - A native AND an xml strategy case assert the `provider` key is ABSENT (no always-on injection).
    - A non-openrouter (openai) case asserts no injection (shared-path proof).
    - No live network call (the OpenAI client create is mocked/spied).
  </acceptance_criteria>
  <done>The injection shape is unit-pinned: quality+openrouter gets require_parameters, every other path does not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| our adapter → OpenRouter routing | Our request-build sets routing modifiers; OpenRouter selects an upstream provider. `require_parameters` constrains which upstreams are eligible (excludes those that drop the tool schema). |
| OpenRouter-routed upstream → our app | Untrusted routed-model behavior crosses back; unchanged by this plan (handled on the response side, not here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering | OpenRouter routing to a non-compliant upstream that silently drops the tool schema | mitigate | `require_parameters: true` excludes such upstreams from routing — this IS the hardening (RESEARCH Security Domain: net security gain). |
| T-129-02 | Elevation/Behavior bleed | The flag leaking onto `native`/`xml` strategies or non-OpenRouter providers | mitigate | Injection lives strictly inside the `strategy=="quality"` + `provider=="openrouter"` double-gate; Task 2 unit-asserts absence on native/xml and openai (D-14 RED LINE). |
| T-129-03 | Information Disclosure | New request field exposing data | accept | `require_parameters` is a boolean routing flag — no PII, no new field carrying user data, no new endpoint. |
| T-129-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (stdlib + existing OpenAI SDK only) — RESEARCH §"Package Legitimacy Audit": N/A. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_129_openrouter_require_params.py -x -q` green.
- Grep `require_parameters` in `openai_service.py` → exactly one occurrence, inside the quality+openrouter block.
- No diff to the non-openrouter / structured-mode branches.
- Full-suite spot-check (run at wave merge): `cd backend && venv/Scripts/python -m pytest -q` — watch for regressions in `test_provider_router.py` and any `-k openrouter` tests.
</verification>

<success_criteria>
- D-02 wired: OpenRouter quality-strategy requests carry `provider.require_parameters=true`.
- Opt-in preserved: native/xml strategies and all non-OpenRouter providers are byte-identical (no shared-path fork — D-14).
- config.py D-15 directive comment reconciled to wired state.
- Unit coverage green (`test_require_parameters_quality_only` + native/xml/openai negatives).
</success_criteria>

<output>
Create `.planning/phases/129-minimax-openrouter-arg-repair/129-01-SUMMARY.md` when done.
</output>
