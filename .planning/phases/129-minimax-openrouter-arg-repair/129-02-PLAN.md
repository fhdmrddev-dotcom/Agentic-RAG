---
phase: 129-minimax-openrouter-arg-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/agent_loop.py
  - backend/tests/test_129_minimax_argrepair.py
  - .planning/reported-bugs/minimax-m3-invalid-tool-args-400.md
autonomous: true
requirements: [MP-04]

must_haves:
  truths:
    - "D-01 / D-03 / MP-04: a MiniMax tool-call round-trip whose buffered arguments string is invalid JSON is DETECTED at the assistant-message build seam (agent_loop.py ~2037, before messages.append) via json.loads in a try/except — independent of finish_reason (MiniMax reports 'tool_calls' even when truncated)"
    - "D-03 / MP-04: the validity guard is provider-gated to MiniMax (gated on the resolved provider, not the model string) — a non-MiniMax round-trip (openai/anthropic/google) is byte-identical and the guard never fires (D-14 RED LINE)"
    - "D-01 / MP-04: on invalid MiniMax args the bad assistant tool_calls turn is NOT appended; instead a single bounded re-ask fires (drop the bad turn, inject a corrective nudge, continue) using a SEPARATE single-shot counter that does NOT consume _provider_retries"
    - "D-01 / MP-04: a still-malformed re-ask reaches the existing honest bad_request copy (provider_gateway message_for_kind) — never a silent swallow, never a fabricated/partial dispatch (no brace-balancing / re-escaping of truncated JSON)"
    - "D-01 / MP-04: a SUCCESSFUL re-ask emits a quiet 'recovered' honesty signal on the Deep agent-loop SSE channel via _emit(redis, run_id, ...) using a Phase-122-family event name (tool_args_recovered) — NOT the harness forced_emit substrate the Deep loop bypasses"
    - "MP-04: BUG-260607-03 (minimax-m3-invalid-tool-args-400) frontmatter is set to status: folded, folded_into: 129, and this plan's guard addresses that bug class"
  artifacts:
    - path: "backend/app/services/agent_loop.py"
      provides: "MiniMax-gated arg-validity guard + bounded one-shot re-ask + honest-fail + quiet recovered signal at the tool-call round-trip seam"
      contains: "json.loads"
    - path: "backend/tests/test_129_minimax_argrepair.py"
      provides: "Unit coverage of valid pass-through, truncation detect, non-minimax no-op, bounded re-ask, honest-fail, recovered signal"
      exports: ["test_truncated_args_detected", "test_non_minimax_unaffected", "test_still_malformed_honest_fail", "test_recovered_signal_emitted"]
    - path: ".planning/reported-bugs/minimax-m3-invalid-tool-args-400.md"
      provides: "Folded bug record"
      contains: "folded_into: 129"
  key_links:
    - from: "backend/app/services/agent_loop.py"
      to: "json.loads(tc['arguments'])"
      via: "provider==minimax validity guard before messages.append at ~2037"
      pattern: "minimax.*json\\.loads|json\\.loads.*arguments"
    - from: "backend/app/services/agent_loop.py"
      to: "_emit(redis, run_id, 'tool_args_recovered', ...)"
      via: "quiet recovered signal on successful re-ask"
      pattern: "_emit.*recovered"
    - from: "backend/app/services/agent_loop.py"
      to: "message_for_kind('bad_request')"
      via: "honest-fail on exhausted re-ask"
      pattern: "message_for_kind|bad_request"
---

<objective>
Harden the MiniMax tool-call round-trip (D-01 + D-03 / MP-04): detect that a buffered MiniMax tool-call `arguments` string is invalid JSON BEFORE it is re-sent to the model, run a single bounded re-ask (the model re-emits the tool call), surface a quiet "recovered" signal on success, and honest-fail with the existing `bad_request` copy if still malformed. The root cause is CONFIRMED truncation (run `2c711ee4`, `output_tokens=8192` = the cap), so the args cannot be coerced into validity — only a fresh emission is honest. The guard is provider-gated to MiniMax; the shared round-trip for OpenAI/Anthropic/Google stays byte-identical (D-14 RED LINE). Closes BUG-260607-03 (`minimax-m3-invalid-tool-args-400`).

Purpose: A code-heavy MiniMax-M3 run that truncates its `execute_code.code` arg once recovers instead of dying with a 400 — the headline half of MP-04, the only STRETCH phase closing a live open bug.
Output: A MiniMax-gated guard + bounded re-ask + recovered signal + honest-fail in `agent_loop.py`, unit tests, and the bug frontmatter flipped to folded.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-RESEARCH.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-PATTERNS.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md
@.planning/reported-bugs/minimax-m3-invalid-tool-args-400.md

<interfaces>
<!-- The EXACT seams + in-file analogs. Use these directly — no codebase exploration needed. -->

THE TARGET SEAM — backend/app/services/agent_loop.py:2036-2074 (the round-trip assistant-message build; the validity check goes at :2037, BEFORE the messages.append at :2039):
```
# --- Tool execution round ---
tool_calls = list(tool_calls_buffer.values())          # :2037  ← guard goes immediately after this
messages.append({                                       # :2039  ← do NOT append the bad turn on invalid MiniMax args
    "role": "assistant",
    "tool_calls": [ {"id": tc["id"], "type": "function",
                     "function": {"name": tc["name"], "arguments": tc["arguments"]}, ... } for tc in tool_calls ],
    ...
})
```

PROVIDER GATE — the resolved provider is in scope as `_resolved_provider = ctx.resolved_provider` (agent_loop.py:1023). Gate on the RESOLVED provider, NOT the model string (BUG-260616-01 / D-09 #3 lesson — slash-gating on the model id mis-fired for every `org/model` id).

SEPARATE SINGLE-SHOT COUNTER ANALOG — agent_loop.py:1224 (`_empty_retries = 0`, run-scoped init) + :2027-2033 (use: `if not full_content and _empty_retries < 1: _empty_retries += 1; ... continue`). Mirror THIS, NOT `_provider_retries` (:1508-1510, which resets per-iteration and is the transient-error budget — Pitfall 3).

CORRECTIVE-NUDGE + CONTINUE ANALOG — agent_loop.py:1998-2008 (prose-before-code recovery): drop the bad turn, append a corrective `user` message, `logger.warning(...)`, `continue`. The re-ask mirrors this shape but is triggered PROACTIVELY by the validity check (not by an exception — the 400 only happens on the NEXT request).

WRITE_TODOS COERCION PRECEDENT (D-03) — backend/app/services/tool_dispatcher.py:2446-2456: `try: json.loads(...) except (ValueError, TypeError): <self-correcting result>`. Mirror the stdlib `json.loads` in `try/except (ValueError, TypeError)`. DIFFERENCE: MiniMax's failure is truncation → the except branch triggers a re-ask, NOT a self-correcting error string (a truncated arg cannot be coerced — DO NOT brace-balance / re-escape; that fabricates a partial dispatch, violating D-01).

HONEST-FAIL COPY — `message_for_kind` is already imported (agent_loop.py:67: `from app.services.provider_gateway import classify_provider_error, message_for_kind`) and used at :2339 (`message_for_kind(kind, err_str)`). The `bad_request` copy lives at provider_gateway/errors.py:187-190: `"*Model parameter error — this model may not support the current configuration.*"`. Reuse via `message_for_kind("bad_request")`. Do NOT interpolate raw 400 detail for this known kind (T-095.1-01-02 Information-Disclosure control).

QUIET RECOVERED SIGNAL — the Deep loop emitter is `_emit = emit` (agent_loop.py:1026); call shape `await _emit(redis, run_id, '<event>', ...)` (e.g. :2121 `await _emit(redis, run_id, 'tool_start', ...)`). It resolves to threads.py:152 `_emit(redis, run_id, type, **fields)` → one canonical XADD on `run:{run_id}`. Phase 122 uses `emit_recovered`/`recovered_from_narration` in the harness forced_emit substrate (phase_types.py:1199-1205) — the Deep loop BYPASSES that substrate (forced_emit.py:74 docstring). So emit a Deep-side equivalent named `tool_args_recovered` (Open Q1 — quiet audit signal, NOT a user-facing error `delta`; the FE can ignore unknown event types).

ANTI-PATTERN — the existing length guards at :1975 (`finish_reason == "length" and tool_calls_buffer`) and :1983 will NOT catch this (Pitfall 2): MiniMax reports `finish_reason: "tool_calls"` even when truncated at the cap. The guard MUST validate the args JSON directly, independent of finish_reason.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MiniMax-gated arg-validity guard + bounded re-ask + recovered signal + honest-fail (D-01 / D-03)</name>
  <files>backend/app/services/agent_loop.py</files>
  <behavior>
    - Valid MiniMax args (well-formed JSON string): guard is a no-op; the assistant tool_calls turn is appended exactly as today; run proceeds. (No behavior change on the happy path.)
    - Invalid/truncated MiniMax args (json.loads raises) + a re-ask budget remaining: the bad assistant tool_calls turn is NOT appended; a corrective user nudge is appended; the single-shot counter increments; `continue` re-asks the model turn. Independent of finish_reason.
    - Successful re-ask (next iteration's args are valid): a quiet `tool_args_recovered` signal is emitted on the run SSE channel via `_emit(redis, run_id, ...)`; the run proceeds.
    - Still-malformed after the one re-ask (budget exhausted): honest-fail via the existing `message_for_kind("bad_request")` copy on the run's error/failure path — never a silent swallow, never a fabricated/partial dispatch.
    - Non-MiniMax provider (openai/anthropic/google) with the same invalid-args shape: guard never fires; the round-trip is byte-identical to today (D-14 RED LINE).
  </behavior>
  <read_first>
    - backend/app/services/agent_loop.py:2020-2090 (THE FILE being modified — the round-trip seam at :2037-2074; the accumulator resets at :2076-2081 to match conventions)
    - backend/app/services/agent_loop.py:1224 (`_empty_retries` init analog), :2027-2033 (its single-shot use), :1998-2008 (the corrective-nudge + continue recovery analog), :1023 (`_resolved_provider`), :1026 (`_emit`), :67 + :2330-2345 (`message_for_kind` import + existing honest-fail usage)
    - backend/app/services/tool_dispatcher.py:2446-2456 (the write_todos json.loads coercion precedent — D-03 pattern to mirror)
    - backend/app/services/provider_gateway/errors.py:180-213 (the `bad_request` copy + `message_for_kind` — the honest-fail surface to reuse; do not add raw-detail for known kinds)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-PATTERNS.md §"agent_loop.py — D-01 MiniMax arg-repair guard" + §"D-01 honest-fail + recovered signal" (Analogs 1-3 + the Deep-side _emit mechanism)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-RESEARCH.md §"Architecture Patterns" Patterns 1-3 + §"Common Pitfalls" 1-3 + §"Anti-Patterns to Avoid" + §"Open Questions" Q1/Q3
  </read_first>
  <action>
    Edit `backend/app/services/agent_loop.py` at the round-trip seam (:2036-2074). Add a run-scoped single-shot counter `_minimax_argrepair_retries = 0` at the top-of-run init alongside `_empty_retries` (:1224) — NOT inside the per-iteration block where `_provider_retries` resets (Pitfall 3: do not steal the transient-error budget).

    At :2037, immediately AFTER `tool_calls = list(tool_calls_buffer.values())` and BEFORE the `messages.append({...})` at :2039, insert a guard gated on `_resolved_provider == "minimax"` (the resolved provider from ctx, NOT the model string — D-09 #3 / BUG-260616-01). For each `tc` in `tool_calls`, validate `json.loads(tc["arguments"])` inside `try/except (ValueError, TypeError)` (mirror tool_dispatcher.py:2450; use the already-imported stdlib `json`). On any invalid args:
      - If `_minimax_argrepair_retries < 1`: do the corrective-nudge-and-continue shape from :1998-2008 — do NOT append the bad assistant tool_calls turn; append a corrective `user` message (e.g. "Your previous tool call's arguments were truncated/invalid JSON — re-emit the tool call with complete arguments; if the code is large, split it across multiple smaller execute_code calls."); `logger.warning(...)` with iteration + thread_id; increment `_minimax_argrepair_retries`; `continue` the loop. Per Open Q3 start with this corrective nudge (not a bare re-ask) so a re-truncation is less likely.
      - Else (budget exhausted, still malformed): honest-fail via the existing error path using `message_for_kind("bad_request")` — route it to the SAME failure surface the loop already uses for a classified provider error (see :2330-2345), so the run ends with the existing honest copy. Never silently swallow; never fabricate a partial dispatch (do NOT brace-balance / re-escape the truncated string).

    Track per-iteration whether a re-ask just succeeded: when the guard runs and finds the MiniMax args VALID *after* a prior `_minimax_argrepair_retries` increment (i.e. the re-ask recovered), emit a quiet `await _emit(redis, run_id, "tool_args_recovered", ...)` (Open Q1 — Phase-122-family name; quiet audit signal, NOT a user-facing error delta) BEFORE the normal `messages.append`. Keep the event minimal (e.g. provider + iteration metadata); the FE can ignore unknown event types — do not add a new FE handler.

    RED LINE: the entire guard MUST be inside the `_resolved_provider == "minimax"` gate; the `messages.append` for non-MiniMax providers must be byte-identical to today. Do NOT touch `_is_transient_provider_error` (:445-462) — the re-ask is a SEPARATE proactive path. Do NOT key anything on `finish_reason` (Pitfall 2).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import ast; ast.parse(open('app/services/agent_loop.py').read()); print('parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `agent_loop.py` validates `json.loads(tc["arguments"])` for each tool_call inside a `_resolved_provider == "minimax"` gate, positioned after `tool_calls = list(tool_calls_buffer.values())` (:2037) and before the `messages.append` (:2039).
    - A NEW run-scoped counter (e.g. `_minimax_argrepair_retries`) is initialized alongside `_empty_retries` (~:1224), capped at 1, and is NOT `_provider_retries` (grep: the new counter name is distinct and not reset inside the per-iteration block).
    - On invalid args with budget: the code does the drop-bad-turn + corrective-user-nudge + `continue` shape (mirrors :1998-2008); the bad assistant tool_calls turn is not appended.
    - On exhausted budget: the code reaches `message_for_kind("bad_request")` on the existing failure surface; no raw 400 detail interpolated for this known kind; no silent swallow.
    - On a recovered re-ask: `await _emit(redis, run_id, "tool_args_recovered", ...)` fires once before the append; the event name is in the Phase-122 family and quiet (not an error delta).
    - No edit outside the MiniMax gate; the non-MiniMax `messages.append` is byte-identical; `finish_reason` is not used as the trigger; `_is_transient_provider_error` untouched; file parses (ast verify exits 0).
  </acceptance_criteria>
  <done>MiniMax truncated-args round-trips detect-then-re-ask-then-honest-fail with a quiet recovered signal; other providers byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit coverage of the guard + fold BUG-260607-03</name>
  <files>backend/tests/test_129_minimax_argrepair.py, .planning/reported-bugs/minimax-m3-invalid-tool-args-400.md</files>
  <behavior>
    - test_valid_args_pass_through: well-formed MiniMax args → guard is a no-op, the round-trip append proceeds, no re-ask, no recovered signal.
    - test_truncated_args_detected: a truncated/invalid args string under provider=="minimax" is detected (json.loads fails) and triggers the re-ask path (bad turn not appended, counter incremented, continue).
    - test_non_minimax_unaffected: the SAME invalid-args shape under provider in {openai, anthropic, google} does NOT trigger the guard — round-trip byte-identical.
    - test_reask_bounded_separate_counter: the re-ask is bounded to exactly 1 and does NOT decrement/consume `_provider_retries`.
    - test_still_malformed_honest_fail: invalid → re-ask → still invalid → honest `message_for_kind("bad_request")` copy surfaced; no silent swallow, no partial dispatch.
    - test_recovered_signal_emitted: invalid → re-ask → valid → exactly one quiet `tool_args_recovered` `_emit` on the run channel.
  </behavior>
  <read_first>
    - backend/tests/test_provider_router.py:1-60 (the established mocked agent-loop/stream unit scaffold — SimpleNamespace settings, mocked supabase/redis via tests/integration/_run_helpers, _fast_chunks)
    - backend/app/services/agent_loop.py (the function under test, as edited in Task 1 — the round-trip seam, the counter, the _emit call, the honest-fail branch)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-PATTERNS.md §"test_129_minimax_argrepair.py (NEW)" (the role-match scaffold + mock-completeness guidance)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-RESEARCH.md §"Validation Architecture" → the 6 MiniMax test-id rows (reuse the exact ::test_ names) + §"Wave 0 Gaps"
    - .planning/reported-bugs/minimax-m3-invalid-tool-args-400.md (frontmatter to fold) + .planning/reported-bugs/TEMPLATE.md (frontmatter convention)
  </read_first>
  <action>
    Create `backend/tests/test_129_minimax_argrepair.py` following the `test_provider_router.py` mocked scaffold. Mock ALL network deps (the OpenAI-compat stream/buffer, supabase, redis) per feedback_mock_completeness.md — explicit MagicMock attrs, no live API. Drive the agent-loop round-trip seam with a mocked `tool_calls_buffer` that yields a truncated `arguments` string, and cover the 6 mapped behaviors using the EXACT test-id names from RESEARCH: `test_valid_args_pass_through`, `test_truncated_args_detected`, `test_non_minimax_unaffected`, `test_reask_bounded_separate_counter`, `test_still_malformed_honest_fail`, `test_recovered_signal_emitted`. Assert: re-ask fires at most once and leaves `_provider_retries` untouched (capture/inspect, or assert the transient path is not entered); the honest-fail branch surfaces the `bad_request` copy from `message_for_kind`; the recovered case produces exactly one `_emit(..., "tool_args_recovered", ...)` (spy on the `_emit` callable). For `test_non_minimax_unaffected`, parametrize over openai/anthropic/google and assert the malformed turn is appended unchanged (the guard is gated out).

    Then fold the bug: in `.planning/reported-bugs/minimax-m3-invalid-tool-args-400.md` frontmatter, set `status: folded` and `folded_into: 129` (leave `re_open_trigger` and history intact; this plan's guard is the addressing task). Add a one-line note in the body pointing at Phase 129 Plan 02 as the fix.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_129_minimax_argrepair.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_129_minimax_argrepair.py -x` exits 0 with all 6 named tests present and passing.
    - `test_non_minimax_unaffected` covers openai/anthropic/google and proves the guard is gated out (byte-identical append).
    - `test_reask_bounded_separate_counter` proves max-1 re-ask AND no consumption of `_provider_retries`.
    - `test_still_malformed_honest_fail` asserts the `message_for_kind("bad_request")` copy is surfaced (no silent swallow).
    - `test_recovered_signal_emitted` asserts exactly one `tool_args_recovered` `_emit`.
    - `minimax-m3-invalid-tool-args-400.md` frontmatter now reads `status: folded` + `folded_into: 129` (grep confirms both literals).
  </acceptance_criteria>
  <done>The guard is unit-pinned across all six behaviors and the live open bug is folded into Phase 129.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MiniMax model output → our agent loop | Untrusted, possibly truncated/malformed tool-call `arguments` JSON crosses into the round-trip build and would be re-sent to the API. This is the core boundary the guard defends. |
| agent loop (per-run) → run SSE channel (`run:{run_id}`) | The recovered signal + honest-fail must stay scoped to the originating run_id; no cross-run/thread bleed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-04 | Denial of Service | Malformed/truncated MiniMax args crashing the round-trip (the folded bug) | mitigate | `json.loads` validity guard at the round-trip seam + bounded one-shot re-ask + honest-fail (D-01) — the run recovers or fails cleanly instead of 400-dying. |
| T-129-05 | Spoofing / Integrity | Coercing truncated args into a fabricated/partial tool dispatch | mitigate | Never brace-balance / re-escape truncated JSON (D-01 forbids fabricated/partial dispatch); only a fresh re-ask is honest; still-malformed → honest bad_request, not a guessed dispatch. |
| T-129-06 | Information Disclosure | Raw 400 detail leaking into user-facing copy | mitigate | Reuse `message_for_kind("bad_request")` fixed copy (errors.py:187) — no raw-detail interpolation for known kinds (existing T-095.1-01-02 control). |
| T-129-07 | Tampering / Elevation | Cross-run/thread bleed of the re-ask counter or recovered signal | mitigate | Counter is run-scoped (init alongside `_empty_retries`, per-run); the recovered `_emit` targets `run:{run_id}` only — covered by the Plan 03 parallel-thread UAT axis. |
| T-129-08 | Elevation / Behavior bleed | The guard firing for non-MiniMax providers (shared-path fork) | mitigate | Guard strictly inside the `_resolved_provider == "minimax"` gate; `test_non_minimax_unaffected` proves openai/anthropic/google are byte-identical (D-14 RED LINE). |
| T-129-SC | Tampering | npm/pip/cargo installs | accept | No package installs (stdlib `json` + existing OpenAI SDK) — RESEARCH §"Package Legitimacy Audit": N/A. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_129_minimax_argrepair.py -x -q` green (all 6 named tests).
- Grep `_resolved_provider == "minimax"` near the round-trip seam → present; grep the non-MiniMax append → unchanged.
- Grep `folded_into: 129` in the bug report → present.
- Wave-merge full suite: `cd backend && venv/Scripts/python -m pytest -q` — watch for regressions in agent-loop tests, `test_errors.py`, and any `-k minimax` / `-k tuner` tests (prior regression-gate lessons: a touched-files-only run can miss a broken cross-phase unit test).
</verification>

<success_criteria>
- D-01 ladder live: invalid MiniMax args → detect → one bounded re-ask → recovered (quiet signal) OR honest bad_request fail; never silent swallow, never fabricated dispatch.
- D-03 scope held: MiniMax-gated on the resolved provider; non-MiniMax round-trips byte-identical (D-14 RED LINE).
- Recovered signal is a Deep-side `_emit("tool_args_recovered")`, NOT the harness forced_emit substrate.
- BUG-260607-03 folded into 129 and addressed by the guard.
- Unit coverage green across all 6 mapped behaviors.
</success_criteria>

<output>
Create `.planning/phases/129-minimax-openrouter-arg-repair/129-02-SUMMARY.md` when done.
</output>
