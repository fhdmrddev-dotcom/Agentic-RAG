---
phase: 129-minimax-openrouter-arg-repair
plan: 03
type: execute
wave: 2
depends_on: ["129-01", "129-02"]
files_modified:
  - .planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md
autonomous: false

requirements: [MP-04]

must_haves:
  truths:
    - "MP-04 / SC#10: the VALIDATION.md scoreboard authors all 4 axes (cross-provider OpenAI+Anthropic+Google+MiniMax+OpenRouter, multi-tool on MiniMax, parallel-thread isolation, long-message reproduction) — UAT rows live in VALIDATION.md, NOT in PLAN tasks"
    - "D-01 / MP-04: both rungs of the repair ladder are exercised live — the RECOVERED rung (MiniMax truncates once → re-ask succeeds → run continues → quiet tool_args_recovered signal visible) AND the HONEST-FAIL rung (re-ask still malformed → bad_request copy, run fails cleanly, no silent swallow / no partial dispatch)"
    - "D-02 / MP-04: an OpenRouter quality-strategy before/after row confirms require_parameters improves tool-schema honoring with no :exacto/plugins routing regression (RESEARCH Pitfall 5 three-way interaction)"
    - "D-14 / MP-04: the regression rows prove OpenAI/Anthropic/Google tool-call round-trips are byte-identical to pre-129 — the MiniMax guard + OpenRouter flag are provider-gated and never fire on the shared path"
  artifacts:
    - path: ".planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md"
      provides: "Filled SC#10 4-axis scoreboard with recorded live results; nyquist_compliant + Per-Task map completed"
      contains: "tool_args_recovered"
  key_links:
    - from: ".planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md"
      to: "live MiniMax-M3 + OpenRouter + OpenAI/Anthropic/Google runs"
      via: "operator-driven 4-axis scoreboard with recorded outcomes"
      pattern: "recovered|honest|byte-identical"
---

<objective>
Author and run the SC#10 4-axis cross-provider live scoreboard for Phase 129 under `VALIDATION.md` (UAT rows belong there, not in PLAN tasks — per the CLAUDE.md SC#10 recipe). This is the LOAD-BEARING verification for MP-04: the unit tests (Plans 01/02) pin the request-build and guard shape, but only a live run proves (a) the MiniMax repair ladder recovers-or-honest-fails on a real truncating prompt, (b) OpenRouter `require_parameters` improves tool-schema honoring without a `:exacto`/`plugins` routing regression, and (c) OpenAI/Anthropic/Google are byte-identical (the D-14 RED LINE). Depends on both implementations (Plans 01 + 02) landing.

Purpose: Prove the phase actually closes BUG-260607-03 in lived experience and regresses nothing on the shared path — the recurring lesson that structural/mocked tests false-green cross-provider streaming defects.
Output: A completed `129-VALIDATION.md` scoreboard with recorded outcomes (recovered + honest-fail rungs both observed, OpenRouter before/after, regression-clean), `nyquist_compliant: true`, and the Per-Task verification map filled.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-RESEARCH.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-01-SUMMARY.md
@.planning/phases/129-minimax-openrouter-arg-repair/129-02-SUMMARY.md

<interfaces>
<!-- Evidence tools + the scoreboard contract. -->

CLAUDE.md SC#10 4-axis recipe: cross-provider (OpenAI/Anthropic/Google + MiniMax + OpenRouter) × multi-tool (>=1 row 2+ tools) × parallel-thread (Thread A streaming while Thread B accepts a prompt) × long-message (>=5KB code payload to push MiniMax-M3 toward its output-token cap).

Evidence tools (reference_evidence_tools_inventory): live app http://localhost:5173/ (login fhdmrd@gmail.com / 123456); Supabase `runs`/`messages` via psycopg2 at localhost:54322 (the run `2c711ee4` was confirmed this way — `output_tokens`, error text, status); backend uvicorn console logs (the operator starts uvicorn themselves — give plain steps, never run_in_background); Chrome MCP for live UI (can HANG — fallback = operator-drives-clicks + Claude DB-cross-checks). LangSmith NOT emitting since 2026-06-20 → use Supabase + logs.

The repair ladder to observe (from 129-02): RECOVERED = quiet `tool_args_recovered` event on `run:{run_id}` + run continues; HONEST-FAIL = the `bad_request` copy "*Model parameter error — this model may not support the current configuration.*" + run fails cleanly.

RESEARCH Pitfall 5: OpenRouter quality stacks `:exacto` + `plugins:[response-healing]` + `provider.require_parameters` — confirm-at-execution that the three-way interaction does not 404/422 on routing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SC#10 4-axis scoreboard rows + fill the Per-Task verification map in VALIDATION.md</name>
  <files>.planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md</files>
  <read_first>
    - .planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md (THE FILE being completed — the existing "Manual-Only Verifications" section + the empty Per-Task map row + the Wave 0 list)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-RESEARCH.md §"SC#10 4-Axis Cross-Provider Scoreboard" (the 4-axis table + the repair-specific rungs + the OpenRouter before/after + the regression-proof rows)
    - CLAUDE.md §"UAT scoreboard recipe (MANDATORY)" (the 4-axis bandwidth table)
    - .planning/phases/129-minimax-openrouter-arg-repair/129-01-SUMMARY.md + 129-02-SUMMARY.md (the as-built event name for the recovered signal + the exact honest-fail copy + the test ids — so the scoreboard references the real artifacts)
  </read_first>
  <action>
    Complete `129-VALIDATION.md`: turn the prose "Manual-Only Verifications" block into a concrete, runnable SC#10 scoreboard with explicit per-row instructions and an empty Result/Outcome column for the operator to fill. Author at least these rows (per RESEARCH §"SC#10 4-Axis"):
      - Cross-provider regression (3 rows): OpenAI, Anthropic, Google — run a normal tool-using prompt; expected = byte-identical tool-call round-trip, the MiniMax guard never fires, no `tool_args_recovered` event (cross-check Supabase `runs` + the run SSE).
      - MiniMax recovered rung (1 row): a heavy `execute_code` prompt (>=5KB code payload — the long-message axis) on MiniMax-M3 that pushes toward the output-token cap; expected = one truncation → re-ask → run continues → quiet `tool_args_recovered` visible (use the as-built event name from 129-02-SUMMARY).
      - MiniMax honest-fail rung (1 row): force a still-malformed re-ask; expected = the `bad_request` copy surfaced, run fails cleanly, NO silent swallow / NO partial dispatch.
      - MiniMax multi-tool (1 row): a prompt exercising 2+ tools (e.g. `search_documents` + `execute_code`) on MiniMax — confirm the per-tool-call guard validates each tool_call independently.
      - Parallel-thread (1 row): Thread A (MiniMax heavy execute_code triggering repair) streaming while Thread B accepts a new prompt — confirm the re-ask counter + recovered signal stay run-scoped (no cross-thread bleed; cross-check `run:{run_id}` scoping).
      - OpenRouter require_parameters before/after (2 rows): one quality-strategy row WITHOUT the flag (baseline routing) and one WITH — confirm routing still succeeds, tool-schema honoring improves, and no `:exacto`/`plugins`/`require_parameters` three-way conflict (Pitfall 5).
    For each row give: provider/model, the prompt shape, the exact expected observable (event name / copy / run status), and the evidence source (Supabase query, backend log line, or UI). Reference the Plan 01/02 unit tests as the automated backstop. Then fill the "Per-Task Verification Map" table (replace the `TBD (planner fills)` row) with the real task ids from Plans 01 and 02, their requirement (MP-04), threat refs (T-129-0x), and the automated commands. Leave the Result/Outcome cells empty for Task 2 to record. Do NOT flip `nyquist_compliant` yet (Task 2 does that after the live run).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -iE "recovered|honest|require_parameters|parallel|multi-tool|byte-identical" .planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `129-VALIDATION.md` "Manual-Only Verifications" section now contains an explicit scoreboard with >= 9 rows covering: 3 regression (OpenAI/Anthropic/Google), MiniMax recovered, MiniMax honest-fail, MiniMax multi-tool, parallel-thread, OpenRouter before, OpenRouter after — each with an empty Result/Outcome cell.
    - All 4 SC#10 axes are explicitly named (cross-provider, multi-tool, parallel-thread, long-message) and the long-message axis is the >=5KB MiniMax reproduction.
    - The scoreboard references the as-built `tool_args_recovered` event name and the `bad_request` honest-fail copy from the Plan 02 SUMMARY (not invented).
    - The Per-Task Verification Map table has the placeholder row replaced with the real Plan 01/02 task ids + MP-04 + T-129-0x threat refs + the pytest commands.
    - The grep verify returns a count > 0 (the axis/rung keywords are present).
  </acceptance_criteria>
  <done>The SC#10 scoreboard is authored and runnable; the Per-Task map is filled; awaiting the live run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator-run the SC#10 live scoreboard + record outcomes + flip nyquist_compliant</name>
  <files>.planning/phases/129-minimax-openrouter-arg-repair/129-VALIDATION.md</files>
  <action>
    This is a blocking human-verify checkpoint — the assistant orchestrates the live SC#10 run and records outcomes; it does NOT write production code here. See `<what-built>` / `<how-to-verify>` for the operator-facing steps. The assistant's actions: confirm the backend is up (ask the operator to start uvicorn in their visible terminal if needed — never run_in_background), drive each scoreboard row one plain instruction at a time, cross-check each run in Supabase (`runs`/`messages` via psycopg2 at localhost:54322) for `output_tokens` / status / error text / the `tool_args_recovered` event, record PASS/FAIL + evidence into the `129-VALIDATION.md` Result/Outcome cells, and on a fully clean run set `nyquist_compliant: true` + `wave_0_complete: true` in the VALIDATION.md frontmatter. On any failure, capture it in VALIDATION.md and route to a gap-closure follow-up instead of flipping the flags.
  </action>
  <what-built>
    Plans 01 + 02 landed: OpenRouter `require_parameters` is injected on the quality strategy (D-02), and the MiniMax-gated arg-repair ladder (detect → bounded re-ask → recovered signal / honest-fail) is live (D-01/D-03). The unit suites (`test_129_openrouter_require_params.py`, `test_129_minimax_argrepair.py`) are green. Task 1 authored the SC#10 scoreboard in `129-VALIDATION.md`. This checkpoint runs that scoreboard live — the load-bearing proof that the phase closes BUG-260607-03 and regresses nothing on the shared path.
  </what-built>
  <how-to-verify>
    Pre-flight (assistant does these): confirm the backend is running (ask the operator to start uvicorn in their visible terminal if not — never run_in_background), confirm the app is at http://localhost:5173/, and have the Supabase `runs`/`messages` query ready (psycopg2 at localhost:54322) to cross-check `output_tokens` / status / error text per run.

    Operator runs each scoreboard row (assistant gives one plain instruction at a time and cross-checks the DB after each):
    1. Regression — OpenAI, Anthropic, Google: send a normal tool-using prompt (e.g. search_documents + a short execute_code). Expected: tool call round-trips and completes; assistant confirms via Supabase there is NO `tool_args_recovered` event and the round-trip looks byte-identical to pre-129. (D-14 RED LINE.)
    2. MiniMax recovered rung: send the heavy >=5KB `execute_code` prompt on MiniMax-M3 (the reproduction class of run `2c711ee4`). Expected: the run does NOT 400-die; the assistant confirms exactly one `tool_args_recovered` event on `run:{run_id}` and the run continues. If MiniMax happens to not truncate this attempt, escalate the payload size until it does (the cap is ~8192 output tokens per the evidence).
    3. MiniMax honest-fail rung: drive a prompt where the re-ask still truncates (or simulate via an even larger payload). Expected: the `bad_request` copy "*Model parameter error — this model may not support the current configuration.*" is surfaced, the run fails cleanly, and the assistant confirms NO silent swallow and NO partial/fabricated tool dispatch in the DB.
    4. MiniMax multi-tool: a 2+ tool prompt on MiniMax. Expected: each tool_call is validated independently; the guard fires only on the malformed one.
    5. Parallel-thread: start Thread A (MiniMax heavy execute_code triggering repair), then in Thread B submit a new prompt while A streams. Expected: the recovered signal + re-ask counter are scoped to A's run_id only; the assistant cross-checks `run:{run_id}` shows no cross-thread bleed.
    6. OpenRouter before/after: on `openrouter_tool_strategy="quality"`, run a tool prompt. Expected: routing still succeeds with `require_parameters` set (no 404/422 from the `:exacto` + `plugins` + `require_parameters` three-way — Pitfall 5); tool-schema honoring is at least as good as the baseline.

    Record each row's outcome (PASS/FAIL + the observed evidence) in the `129-VALIDATION.md` Result/Outcome cells. If all rows pass, the assistant sets `nyquist_compliant: true` and `wave_0_complete: true` in the VALIDATION.md frontmatter and marks the approval line. If any row fails, capture the failure in VALIDATION.md and route to a gap-closure follow-up rather than flipping the flags.
  </how-to-verify>
  <verify>
    <human-check>All 9+ scoreboard rows recorded with PASS + evidence in 129-VALIDATION.md; recovered AND honest-fail rungs both observed; OpenRouter before/after clean; OpenAI/Anthropic/Google byte-identical; nyquist_compliant flipped only on a fully clean run.</human-check>
  </verify>
  <done>The live SC#10 scoreboard is run, every row recorded, both repair rungs observed, no shared-path regression, and `nyquist_compliant: true` set (or gaps captured and routed).</done>
  <resume-signal>Type "approved" once all scoreboard rows are recorded (recovered + honest-fail rungs both observed, OpenRouter before/after clean, OpenAI/Anthropic/Google byte-identical), or describe which row(s) failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live provider responses → agent loop | The live scoreboard exercises the same untrusted-provider-output boundary the implementation plans defend; this plan VERIFIES the mitigations hold under real traffic (no code change). |
| per-run SSE channel (`run:{run_id}`) → other threads | The parallel-thread row verifies the re-ask state + recovered signal stay run-scoped (no cross-run/thread bleed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-07 | Tampering / Elevation | Cross-run/thread bleed of the re-ask counter or recovered signal | mitigate | The parallel-thread scoreboard row (Task 2 step 5) explicitly verifies run-scoping via the `run:{run_id}` keyspace — no bleed from Thread A into Thread B. |
| T-129-09 | Denial of Service (verification escape) | A mocked-only sign-off false-greening a live cross-provider streaming defect (the recurring 075.x lesson) | mitigate | This plan makes the LIVE 4-axis scoreboard the load-bearing gate; both repair rungs + OpenRouter before/after + 3-provider regression must be observed before `nyquist_compliant` flips. |
| T-129-10 | Information Disclosure | Operator pasting raw secrets/keys while running live UAT | accept | Evidence is run rows / event types / status — name-only; no .env paste required (feedback_env_secrets_handling). |
| T-129-SC | Tampering | npm/pip/cargo installs | accept | No installs — this plan only edits a `.md` and runs the existing app. RESEARCH §"Package Legitimacy Audit": N/A. |
</threat_model>

<verification>
- Task 1 automated: the scoreboard keyword grep returns > 0 (axes + rungs present in VALIDATION.md).
- Task 2 (human-verify, blocking): all 9+ scoreboard rows recorded with PASS + evidence; recovered AND honest-fail rungs both observed; OpenRouter before/after clean (no Pitfall-5 routing regression); OpenAI/Anthropic/Google byte-identical.
- On full PASS: `nyquist_compliant: true` + `wave_0_complete: true` set in `129-VALIDATION.md`.
- Backstop already green from Wave 1: `cd backend && venv/Scripts/python -m pytest tests/test_129_*.py -x -q`.
</verification>

<success_criteria>
- SC#10 4-axis scoreboard authored in VALIDATION.md (not in PLAN tasks) and run live.
- D-01 ladder proven both ways: recovered rung (quiet `tool_args_recovered` + run continues) AND honest-fail rung (`bad_request` copy + clean failure, no silent swallow / no partial dispatch).
- D-02 proven: OpenRouter `require_parameters` improves tool-schema honoring with no `:exacto`/`plugins` routing regression.
- D-14 proven: OpenAI/Anthropic/Google byte-identical (the guard + flag never fire on the shared path).
- `nyquist_compliant: true` recorded after a clean live run.
</success_criteria>

<output>
Create `.planning/phases/129-minimax-openrouter-arg-repair/129-03-SUMMARY.md` when done.
</output>
