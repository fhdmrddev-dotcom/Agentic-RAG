---
status: testing
phase: 075-seed-008-tool-args-progress-polish-bundle
source:
  - 075-01-SUMMARY.md
  - 075-02-SUMMARY.md
  - 075-03-SUMMARY.md
started: 2026-05-18
updated: 2026-05-18
---

## Current Test

[testing complete — 5 issues found, 1 blocked, advancing to diagnose phase]

## Tests

### 1. Cold Start Smoke Test
expected: Backend boots without errors. Frontend loads. Logging in and opening an existing thread shows message history. No 500s/console errors against /snapshot, /messages, or /active-runs.
result: pass

### 2. Snapshot Endpoint — Cold-Cache Thread Switch (SC #1)
expected: After F5 (hard reload), clicking a thread with prior history loads the messages noticeably faster than before Phase 075. In Network tab you should see one `GET /threads/{id}/snapshot` call instead of three (no separate `/messages` + `/active-runs` + `/stream?since=` chain on first load). Optional: capture Chrome DevTools Performance trace and compare; target is ≥50% reduction in time-to-paint vs the old 3-call chain.
result: issue
reported: "Chrome MCP at http://localhost:5173/ + F5 reload + click thread (4641c433-...): /threads/{tid}/snapshot fires (reqid=271, 74257 bytes, returns full messages+active_runs+since_cursors as expected) BUT /threads/{tid}/messages ALSO fires immediately after (reqid=272, 74208 bytes — same message payload as snapshot.messages). Snapshot's messages field is being thrown away. The atomic-swap intent of D-075-02 is incomplete: there's a second loadMessages caller somewhere outside StreamsProvider.reconcile that still issues /messages. End result: 2 calls instead of the planned 1 (and definitely not 3 like before), so SC #1's directional benefit (fewer calls) holds but the magnitude is reduced."
severity: major

### 3. BUG-260518-01 — Resume Button Stays Hidden During Long Sandbox Cell
expected: Drive a long sandbox cell — e.g., prompt the agent to "build a 30-slide deck on this 50-page PDF" or just run `import time; time.sleep(45)` in a code block. While the cell is executing (>30s), the Resume button does NOT appear on the assistant message. The bottom indicator stays animated. When the sandbox returns, the result paints cleanly without any "stream failed" / Resume flicker.
result: issue
reported: "Drove `import time; print('starting', flush=True); time.sleep(45); print('done', flush=True)` via Chrome MCP. PARTIAL PASS for the literal Resume-button check: button never appeared during 45s sleep (run a41b9006-38f3-4734-8920-91ce850b2ec8, thread e6e8d53e-...). BUT serious side effect: SSE stream (reqid=281) net::ERR_ABORTED at some point during/after the run. Backend correctly marked run_status=completed and saved output, but the FRONTEND remained permanently stuck on 'Running code…' indefinitely (≥90s past completion) with NO Resume button to recover. The previous behavior emitted a Resume button on any stream end so users could click to reconnect; the new BUG-260518-01 fix removed the false-positive but ALSO removed the recovery path for genuine stream-end events. Only a hard reload + thread re-click via /snapshot recovered the UI. Net effect: BUG-260518-01 fix is a Pyrrhic victory — eliminated the spurious Resume button at the cost of UI freezes on any SSE disconnect after a long cell. Plan 01 reconcile-fetch wiring in onTerminal only triggers on `buffer_expired_*` error events, NOT on plain stream-end / ERR_ABORTED."
severity: major

### 4. Line-by-Line Stdout (SC #2) — 5-Step Printer
expected: In a code block run `for i in range(5): print(i); import time; time.sleep(1)`. Each number (0, 1, 2, 3, 4) appears in the streamed output ROUGHLY ONE SECOND APART — not all at once at the end. Total observable streaming time ≥4-5 seconds with progressive output, not a single dump at completion.
result: issue
reported: "Chrome MCP: submitted `for i in range(5): print(i, flush=True); time.sleep(1)` in a fresh chat. Backend confirms run dd54c8ae-... completed in 5065ms with stdout='0\\n1\\n2\\n3\\n4\\n' captured correctly. Frontend NEVER showed any of 0/1/2/3/4 in the output area at any sampling point. Polled main.innerText at multiple intervals (T+~0.5s after cell start: textLen=428 no numbers; T+~14s: textLen=448 still no numbers, indicator gone; T+~30s: still stuck at 448 with 'Executing code' shown). Cannot confirm whether line-by-line emit (D-075-05/06) is actually firing the per-line code_stdout events — same SSE-stream-break pattern as Test 3 occurs, just faster. This means EITHER (a) the line-by-line emit isn't working AND the post-completion emit was correctly deleted (D-075-07) — so frontend gets NO stdout events at all, OR (b) the events fire but the SSE stream breaks before they reach the frontend. Either way, the user observes no progressive output. SC #2 fails for the lived-experience check. The backend integration tests (test_075_code_stdout_progressive.py) are gated on SANDBOX_ENABLED env var and may have skipped silently in CI, meaning this regression wasn't caught at test time."
severity: blocker

### 5. BUG-260514-03 — Bottom Indicator Stays Animated Through Silent Renders
expected: During a silent cell (time.sleep or long matplotlib render), the bottom indicator shows "Running code…" the entire time, not clearing to blank.
result: issue
reported: "Observed during Test 3 via Chrome MCP. At T+0s during a `time.sleep(45)` silent cell: bottom indicator showed 'Running code…' (uid=7_1 in a11y tree). At T+20s: indicator was GONE from the DOM (no element matching 'Running code…' anywhere — confirmed via grep on innerText). The top-level step indicator ('Step 1 — Running code — Testing long sleep…' + 'Python / Executing code / Testing long sleep in Python code execution.') stayed visible the entire time, but the BOTTOM indicator that should stay sticky cleared. This is BUG-260514-03 still reproducing despite Plan 02 D-075-14's sticky-text + code_stdout-subscription fix. Two hypotheses: (a) the sticky logic in MessageItem.tsx only triggers from `code_stdout` events but a silent `time.sleep(45)` cell emits zero code_stdout events, so the sticky-fallback path may not be wired correctly; (b) the heartbeat (`code_executing`) was supposed to be the silent-window keepalive but the sticky-text logic doesn't re-anchor from heartbeat events. Top/bottom desync is still reproducible."
severity: major

### 6. tool_args_progress (SC #3) — Anthropic Long-Form Prompt
expected: Switch the active provider to Anthropic (model `claude-sonnet-4-6`). Drive a prompt that produces a large `analyze_document` tool call — open browser DevTools Network tab → find the SSE stream → between `tool_preparing` and `tool_start` you should see one or more `tool_args_progress` events with `tool_index`, `args_so_far` (≤5KB), `total_args_bytes_so_far`.
result: blocked
blocked_by: prior-phase
reason: "Tests 3, 4, 7 all confirmed the SSE stream for code-execute runs breaks mid-flight and the frontend never receives the terminal frame. The tool_args_progress event would land on the same SSE infrastructure; running this test before the SSE-break regression is fixed would either reproduce the same freeze or produce unreliable readings on whether the new events fire correctly. Run /gsd:execute-phase 075 --gaps-only first to land the SSE recovery fixes, then revisit Test 6."

### 7. SC #4 Regression — code_executing Heartbeat on Silent Workload
expected: Run a code cell that's silent for a while — e.g., `import time; time.sleep(8); print("done")`. During the 8-second silent window, the bottom indicator stays animated (heartbeats are still firing under the hood). After 8 seconds, "done" prints once. No "stream went dead" flicker; no Resume button.
result: issue
reported: "Chrome MCP: submitted `import time; time.sleep(8); print('done', flush=True)`. T+0: bottom indicator 'Running code…' visible. T+~3s: indicator CLEARED. T+~11s past start: backend confirms run completed in 8043ms with stdout='done\\n', frontend STILL stuck showing 'Executing code' and 'done' never appeared in output. THIRD REPRODUCTION of the same SSE-break + frontend-freeze pattern (Tests 3, 4, 7 all hit it). The D-075-08 silent-window-only heartbeat invariant is violated TWICE OVER: (a) the bottom indicator cleared during a silent window despite the 1-Hz code_executing heartbeat supposedly firing, and (b) sse-starlette's 10s keepalive (D-061-10) also didn't keep the stream alive long enough to deliver the terminal frame. Same blocker as Test 4 — entire code-execution SSE flow is broken in Phase 075."
severity: blocker

## Summary

---
status: complete
total: 7
passed: 1
issues: 5
pending: 0
skipped: 0
blocked: 1
---

## Gaps

- truth: "After F5 + thread click, the cold-cache reconcile makes ONE /snapshot call (not /snapshot + /messages). The snapshot's messages field is the source of truth for the initial render — there should be no separate /messages fetch on the reconcile hot path."
  status: failed
  reason: "User reported: Chrome MCP captured both /threads/{tid}/snapshot (reqid=271, 74257 bytes) and /threads/{tid}/messages (reqid=272, 74208 bytes) firing back-to-back after a single thread-click. Both return identical message arrays. The snapshot's messages payload is being thrown away because something is still calling loadMessages outside StreamsProvider.reconcile. Atomic-swap (D-075-02) is incomplete."
  severity: major
  test: 2
  artifacts: []
  missing: []

- truth: "When the SSE stream ends for any reason during code execution (including genuine network disconnect, not just buffer_expired_*), the frontend recovers — either by reconcile-fetching /snapshot to confirm run status, or by showing a Resume button so the user can reconnect."
  status: failed
  reason: "Live UAT on a 45-second time.sleep() cell. Backend ran the cell successfully (run_status=completed, output saved). Frontend SSE stream (reqid=281) hit net::ERR_ABORTED at some point during/after execution. Plan 01's onTerminal reconcile-fetch wiring only catches buffer_expired_during_tail / buffer_expired_after_eviction error events — it doesn't handle plain stream-end without an error payload (or with a generic abort). Frontend stayed stuck on 'Running code…' for 90+ seconds past completion. Resume button stayed hidden (per Plan 01's literal goal — passes that check) but ALSO no other recovery mechanism kicked in. User had to F5 reload + click thread to recover via /snapshot. This is arguably WORSE than the original BUG-260518-01 because the old false-positive Resume button at least gave users a recovery path; the new behavior is silent UI freeze."
  severity: major
  test: 3
  artifacts: []
  missing: []

- truth: "During a silent sandbox cell (time.sleep, matplotlib renders, etc.), the bottom indicator stays visible/animated the entire time — the sticky-text fix from Plan 02 D-075-14 preserves the last 'Running code…' label even when no code_stdout events fire."
  status: failed
  reason: "Observed during Test 3 via Chrome MCP at T+0 vs T+20s into `time.sleep(45)`. At T+0: 'Running code…' visible in a11y tree (uid=7_1). At T+20s: completely gone from DOM. BUG-260514-03 is still reproducing despite Plan 02's sticky-text + code_stdout-subscription fix. Likely the sticky logic only re-anchors from code_stdout events (which never fire for a silent cell), and the code_executing heartbeat path doesn't feed the sticky-text branch."
  severity: major
  test: 5
  artifacts: []
  missing: []

- truth: "When a 5-second `for i in range(5): print(i, flush=True); time.sleep(1)` cell executes, the frontend shows each of 0,1,2,3,4 appearing ~1s apart in the output area via per-line code_stdout SSE events (D-075-05/06)."
  status: failed
  reason: "Chrome MCP submitted the cell in a fresh thread (19625583-79f3-4bfe-a969-7bcabe0fe671). Backend captured stdout='0\\n1\\n2\\n3\\n4\\n' over 5065ms (run dd54c8ae-... completed). Frontend NEVER displayed any of the numbers progressively — main.innerText was 428 chars at T+~0.5s and 448 chars at T+~14s with no numeric content. Frontend remained on 'Executing code' state indefinitely (same SSE-stream-break regression as Test 3 — but now on a SHORT 5-second cell, not just a 45-second sleep). SC #2 fails the lived-experience check. Because Plan 02 D-075-07 deleted the post-completion emit, if line-by-line emit is broken AND the stream breaks before completion, the user sees nothing. The backend tests gated on SANDBOX_ENABLED may have skipped silently and failed to catch this in CI."
  severity: blocker
  test: 4
  artifacts: []
  missing: []

- truth: "During an 8-second silent `time.sleep(8)` cell, the 1-Hz code_executing heartbeat (D-075-08 silent-window-only path) keeps the bottom indicator visible the entire time AND the SSE stream stays alive long enough to deliver the terminal frame at T+8s, surfacing 'done' in the output."
  status: failed
  reason: "Chrome MCP: T+~3s into 8s silent cell — bottom indicator already cleared. T+11s — backend confirms run completed (duration_ms=8043, stdout='done\\n', run_status=completed) but frontend still on 'Executing code'. Three independent SSE-break reproductions (Tests 3, 4, 7) across run durations of 5s, 8s, and 45s. Heartbeat (D-075-08) appears not to fire OR not to reach the frontend OR not to keep the SSE alive. Could be a subtle bug in the silent-window heartbeat reset logic (_last_output_at) interacting with sse-starlette's keepalive, or the line-buffer accumulator producing zero output when there are no '\\n' delimited lines mid-flight, or the session.execute_command swap subtly changing how callbacks fire."
  severity: blocker
  test: 7
  artifacts: []
  missing: []
