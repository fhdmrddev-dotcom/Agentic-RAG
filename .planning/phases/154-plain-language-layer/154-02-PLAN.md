---
phase: 154-plain-language-layer
plan: 02
type: execute
wave: 2
depends_on: [154-01]
files_modified:
  - frontend/src/components/ingestion/DocumentStatusBadge.tsx
  - frontend/src/__tests__/components/DocumentStatusBadge.test.tsx
  - frontend/src/components/metadata/DocumentDetailPanel.tsx
autonomous: true
requirements: [LANG-01]

must_haves:
  truths:
    - "Every uploader sees plain ingestion labels by default (Surface A / D-04) — 'Splitting into sections', 'Making it searchable' — with today's technical words ('Chunking', 'Embedding') revealed only under the toggle."
    - "The document-detail 'Metadata' header reads plainly ('Details') by default (D-04) with the technical word behind the reveal."
    - "Relabels are display-only (D-05a): DocumentStatusBadge's style lookup stays keyed on the raw `status` enum, and no wire/enum/audit value is renamed (D-02a)."
  artifacts:
    - path: "frontend/src/components/ingestion/DocumentStatusBadge.tsx"
      provides: "Ingestion status/step labels routed through usePlainLabel (plain default, technical on reveal)"
      contains: "usePlainLabel"
    - path: "frontend/src/components/metadata/DocumentDetailPanel.tsx"
      provides: "Document-detail section header relabeled via the term-map (Metadata → Details)"
      contains: "usePlainLabel"
  key_links:
    - from: "frontend/src/components/ingestion/DocumentStatusBadge.tsx"
      to: "frontend/src/lib/termMap.ts"
      via: "usePlainLabel(`ingest.${step}` / `status.${status}`)"
      pattern: "usePlainLabel"
    - from: "frontend/src/components/metadata/DocumentDetailPanel.tsx"
      to: "frontend/src/lib/termMap.ts"
      via: "usePlainLabel('doc.metadata_section')"
      pattern: "usePlainLabel"
---

<objective>
Relabel the two highest-jargon DOCUMENT surfaces (RESEARCH ranks 1 & 2) to read plainly by default, routing every label through the Wave-1 term-map.

Purpose: Surface A (the ingestion status badge) is the single biggest win — every uploader sees it and it currently shows RAW technical steps by default. Surface B (the document-detail "Metadata" header) is high-traffic jargon. Both flip plain⇄technical off the shared context with the underlying enums untouched.
Output: `DocumentStatusBadge.tsx` + its extended test, and the `DocumentDetailPanel.tsx` header relabel.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/154-plain-language-layer/154-CONTEXT.md
@.planning/phases/154-plain-language-layer/154-RESEARCH.md
@.planning/phases/154-plain-language-layer/154-PATTERNS.md

# Wave-1 spine this plan consumes
@frontend/src/lib/termMap.ts
@frontend/src/providers/TechnicalNamesProvider.tsx

<interfaces>
<!-- From Wave 1 (Plan 01). Use directly — no exploration needed. -->
// frontend/src/lib/termMap.ts
export function usePlainLabel(key: TermKey): string   // plain when reveal OFF, technical when ON; unknown key → String(key)
export type TermKey = keyof typeof TERM_MAP
// Relevant keys already authored in Wave 1:
//   ingest.extracting | ingest.extracting_tables | ingest.extracting_images | ingest.chunking | ingest.embedding | ingest.metadata
//   status.pending | status.processing | status.completed | status.failed
//   doc.metadata_section  ({ plain:"Details", technical:"Metadata", helper:"Facts about this document." })

// frontend/src/providers/TechnicalNamesProvider.tsx
export function TechnicalNamesProvider(props: { children: ReactNode }): JSX.Element   // wrap render(...) in tests
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DocumentStatusBadge ⭐ — plain-by-default ingestion labels via term-map</name>
  <files>frontend/src/components/ingestion/DocumentStatusBadge.tsx, frontend/src/__tests__/components/DocumentStatusBadge.test.tsx</files>
  <read_first>
    - frontend/src/components/ingestion/DocumentStatusBadge.tsx:1-41 — the whole component: `ingestionStepLabel` switch (:16-24), the label pick `status === "processing" ? ingestionStepLabel(...) : status` (:27), and `styles[status]` (:32) keyed on the raw enum.
    - 154-RESEARCH.md §Surface A + §Code Examples — the term keys + the "keep `styles[status]` enum-keyed" instruction.
    - frontend/src/__tests__/components/DocumentStatusBadge.test.tsx — the existing suite to extend (wrap new reveal-cases in `<TechnicalNamesProvider>`).
    - frontend/src/lib/termMap.ts (Wave 1) — `usePlainLabel`.
  </read_first>
  <behavior>
    - Reveal OFF (default, or rendered inside a provider with showTechnical=false): a `processing` badge with `ingestionStep="chunking"` shows "Splitting into sections"; `embedding` shows "Making it searchable"; a `pending` badge shows "Waiting"; `completed` shows "Ready".
    - Reveal ON (provider with showTechnical=true): the SAME badges show today's strings — "Chunking", "Embedding", "pending", "completed".
    - Enum-keyed styling is unchanged: `styles[status]` still uses the raw `status` value (a `completed` badge keeps its green class regardless of the displayed label).
    - Unknown/absent `ingestionStep` while processing falls back to the `status.processing` plain label ("Working…"), never a raw key.
  </behavior>
  <action>
    Route the DISPLAY through `usePlainLabel` while leaving the `status` enum driving `styles[status]`. Compute ONE term key unconditionally (hooks can't be conditional): `const termKey = status === "processing" ? (has TERM_MAP entry for `ingest.${ingestionStep}` ? `ingest.${ingestionStep}` : "status.processing") : `status.${status}`` then `const label = usePlainLabel(termKey as TermKey)`. The backend keeps emitting `"chunking"`/`"embedding"` and the `status` enum — this is DISPLAY-only (D-02a). Do NOT swap the `styles[status]` key to the plain label (Pitfall 15). You may delete the now-unused `ingestionStepLabel` switch (its strings now live in TERM_MAP as the `technical` side). Extend the existing test: keep current cases (they render plain by default — usePlainLabel's optional accessor returns plain with no provider) and ADD reveal cases wrapped in `<TechnicalNamesProvider>` proving plain-default vs technical-on-reveal and that `styles[status]` (the class) is unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/DocumentStatusBadge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "usePlainLabel" frontend/src/components/ingestion/DocumentStatusBadge.tsx` ≥ 1.
    - `grep -c "styles\[status\]" frontend/src/components/ingestion/DocumentStatusBadge.tsx` ≥ 1 (style key still the raw enum, NOT the label).
    - Test asserts: plain "Splitting into sections"/"Making it searchable" by default AND "Chunking"/"Embedding" when wrapped with reveal ON.
    - `npx vitest run src/__tests__/components/DocumentStatusBadge.test.tsx` — all cases green.
  </acceptance_criteria>
  <done>Ingestion badge reads plainly by default, reveals today's technical steps under the toggle, and keeps its enum-keyed styling; extended suite green.</done>
</task>

<task type="auto">
  <name>Task 2: DocumentDetailPanel — relabel the "Metadata" section header</name>
  <files>frontend/src/components/metadata/DocumentDetailPanel.tsx</files>
  <read_first>
    - frontend/src/components/metadata/DocumentDetailPanel.tsx:225-235 — the `<PanelSection title="Metadata" …>` at :230 (RESEARCH §Surface B).
    - 154-PATTERNS.md §DocumentDetailPanel — the light-touch instruction: relabel the header only; do NOT relabel ConfidenceChip words (already plain/honest, Phase 112).
    - frontend/src/lib/termMap.ts (Wave 1) — `usePlainLabel("doc.metadata_section")`.
  </read_first>
  <action>
    Replace the literal `title="Metadata"` at `DocumentDetailPanel.tsx:230` with `title={usePlainLabel("doc.metadata_section")}` (renders "Details" by default, "Metadata" under the reveal). Add the `usePlainLabel` import from `@/lib/termMap`. DocumentDetailPanel already renders inside the app-wide provider (documents view under ChatLayout), so the hook resolves; if `PanelSection.title` is a string prop, pass the hook result string (do not force a node). Do NOT relabel `ConfidenceChip` words ("Extracted"/"High/Med/Low") — they are already honest + plain (Phase 112); at most this phase leaves them as-is. No enum/API/audit change.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -cE 'error TS' && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'usePlainLabel("doc.metadata_section")' frontend/src/components/metadata/DocumentDetailPanel.tsx` ≥ 1.
    - `grep -c 'title="Metadata"' frontend/src/components/metadata/DocumentDetailPanel.tsx` == 0 (literal replaced).
    - No `ConfidenceChip` word relabel in the diff (git diff shows only the header line + import changed in this file).
    - `npx tsc -b` prints exactly 30 `error TS` lines (0 net-new); `npx vite build` exits 0.
  </acceptance_criteria>
  <done>The document-detail section header reads "Details" by default and "Metadata" under the reveal, via the term-map; ConfidenceChip untouched; tsc 0 net-new; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| display string → wire value | The relabels change DISPLAY strings only; the `status`/`ingestionStep` enums the backend emits and the style-key lookups are untouched. No client→server / auth / data-access boundary is crossed or added (display-only, D-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-01 | Tampering | DocumentStatusBadge / DocumentDetailPanel | mitigate | Labels route through `usePlainLabel`; `styles[status]` stays keyed on the raw `status` enum (never the plain label); backend keeps emitting `chunking`/`embedding`/status enums. Contract-Safety Recipe (D-05a) at phase end shows zero backend files changed. |
| T-154-02 | Tampering (XSS) | rendered labels | mitigate | Labels are static term-map literals rendered as auto-escaped React text; no `dangerouslySetInnerHTML`, no user/model-supplied string reaches the label sink. |
| T-154-03 | Information Disclosure | technical reveal | accept | Step/status names are not secret (D-01); the reveal exposes only field wording. No packages installed. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/components/DocumentStatusBadge.test.tsx` — green.
- `cd frontend && npm run build` — `tsc -b` exactly 30 `error TS` lines (0 net-new), `vite build` exit 0.
- Diff review: only DISPLAY strings + `usePlainLabel` wiring changed in the two source files; `status`/`ingestionStep` enum literals unchanged at any style/call site.
</verification>

<success_criteria>
- The ingestion badge and the document-detail "Metadata" header read plainly by default and reveal today's technical words under the toggle.
- Style/enum behavior is byte-identical; zero backend files touched; tsc 0 net-new; suites green.
</success_criteria>

<output>
Create `.planning/phases/154-plain-language-layer/154-02-SUMMARY.md` when done.
</output>
