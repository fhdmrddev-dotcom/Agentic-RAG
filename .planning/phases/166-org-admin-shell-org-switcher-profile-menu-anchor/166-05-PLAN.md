---
phase: 166-org-admin-shell-org-switcher-profile-menu-anchor
plan: 05
type: execute
wave: 4
depends_on: ["166-02", "166-04"]
files_modified:
  - frontend/src/components/layout/ProfileMenu.tsx
  - frontend/src/components/layout/NavPanel.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/components/layout/ProfileMenu.test.tsx
  - frontend/src/components/layout/NavPanel.test.tsx
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-05]
must_haves:
  truths:
    - "D-166-05 / ADMIN-03: the 079-C merged rail-footer ProfileMenu popover holds identity (name/email) + the ◆ Org-admin / Member role badge + the org-switcher section + theme + Sign out (Sign out moves INTO the menu) — the user-side mirror of the Phase-146 operator shield"
    - "D-166-02 / ADMIN-02: the org switcher renders ONLY at 2+ orgs; a solo user sees a quiet name button with no switcher chrome"
    - "D-166-05 / ADMIN-01: the org-admin entry is an INDIGO rail Shield-mirror rendered OUTSIDE NAV_ITEMS, parallel to the amber operator shield, shown ONLY when canManage; honestly ABSENT (never disabled) for a member"
    - "ADMIN-01 reachability triad: the ActiveView union has org-admin (Plan 02), ChatLayout mounts OrgAdminShell on the org-admin branch, and the NavPanel shield fires onNavigate('org-admin') — all owned in-phase (the Phase-118 built-but-unreachable lesson)"
    - "D-166-08 / ADMIN-02: picking an org calls OrgProvider.switchOrg() (which owns the teardown) — the menu is presentational and does not tear down streams itself"
    - "ADMIN-05: the personal identity sliver (name/email/theme/sign-out) lives in the ProfileMenu — the three-homes split's personal home"
  artifacts:
    - path: "frontend/src/components/layout/ProfileMenu.tsx"
      provides: "the 079-C merged rail-footer popover (identity + role badge + org switcher + theme + sign out)"
    - path: "frontend/src/components/layout/NavPanel.tsx"
      provides: "indigo org-admin Shield-mirror (canManage-gated, outside NAV_ITEMS) + the ProfileMenu anchor replacing the bare Sign out"
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "the org-admin view branch mounting OrgAdminShell"
      contains: "OrgAdminShell"
  key_links:
    - from: "frontend/src/components/layout/NavPanel.tsx"
      to: "onNavigate('org-admin')"
      via: "the indigo Shield-mirror click, gated on canManage"
      pattern: "org-admin"
    - from: "frontend/src/components/layout/ProfileMenu.tsx"
      to: "OrgProvider.switchOrg"
      via: "the org-switcher section pick action"
      pattern: "switchOrg"
---

<objective>
Wire the human-facing entry: the 079-C merged ProfileMenu popover (identity + role badge + org switcher + theme + sign out) in the rail footer, the indigo org-admin Shield-mirror parallel to the operator shield, and the ChatLayout `org-admin` view branch that mounts OrgAdminShell — closing the reachability triad.

Purpose: ADMIN-03 (identity anchor), ADMIN-02 (switcher UI + switchOrg), ADMIN-01 (shell entry point + reachability), ADMIN-05 (personal sliver home). This is the last mile — after it, a manager can reach the shell and any user has a real identity anchor.
Output: ProfileMenu.tsx (new), NavPanel.tsx + ChatLayout.tsx (modified), tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-org-admin-shell-org-switcher-profile-menu-anchor/166-CONTEXT.md
@.planning/phases/166-org-admin-shell-org-switcher-profile-menu-anchor/166-PATTERNS.md
@.planning/sketches/079-identity-anchor-and-org-switcher/README.md

<interfaces>
<!-- Self-analogs + the Plan 02/04 consumers. -->

frontend/src/components/layout/NavPanel.tsx — self:
  RailItem collapsed/expanded dual-render (:56-93) — reuse so both new elements work icon-only at 58px.
  the probe-gated AMBER operator shield (:181-198), rendered OUTSIDE navItems, active-highlights in Control Room — the pattern to MIRROR in indigo, gated on canManage.
  the bare "Sign out" RailItem (:200-206) — REPLACED by the ProfileMenu anchor (sign-out moves into the menu).
  a regression test locks "shield lives OUTSIDE NAV_ITEMS" (:35-38) — keep the indigo shield out of the shared array too.

frontend/src/components/layout/ChatLayout.tsx — self:
  ControlRoomPage: import (:18), the view branch (:575), the mount (:583 `<ControlRoomPage identity={operatorIdentity} onBack={…}/>`).
  NavPanel props flow (:299-310). The mobile-drawer operator shield (:471-478).
  ActiveView union already has "org-admin" (Plan 02, App.tsx:82).

frontend/src/hooks/useAuth.ts:15-18 — user.email / user.id (identity source; do NOT re-fetch — read via useAuth / passed context).
frontend/src/providers/OrgProvider.tsx (Plan 02): useOrg()/useOrgOptional() → {activeOrgId, orgs, role, canManage, canAuditView, switchOrg}. NavPanel/ProfileMenu are inside OrgProvider, so read canManage + orgs + switchOrg via useOrgOptional() — NO new App→ChatLayout prop threading needed.
frontend/src/components/org/OrgAdminShell.tsx (Plan 04): <OrgAdminShell onBack={…}/>.
frontend/src/components/ui/{popover,sheet} — the shadcn primitives already in the repo (UsersAndAccess.tsx:39-44 imports sheet); a Popover is the lighter fit for a rail-footer menu.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProfileMenu — the 079-C merged rail-footer popover</name>
  <files>frontend/src/components/layout/ProfileMenu.tsx, frontend/src/components/layout/ProfileMenu.test.tsx</files>
  <read_first>
    - frontend/src/components/layout/NavPanel.tsx:56-93 (RailItem dual-render), :171-207 (footer where Sign out lives)
    - frontend/src/hooks/useAuth.ts:15-18 (identity)
    - frontend/src/providers/OrgProvider.tsx (Plan 02 — useOrg/useOrgOptional, switchOrg)
    - .planning/sketches/079-identity-anchor-and-org-switcher/README.md (the merged menu spec + "What to Look For")
  </read_first>
  <behavior>
    - Test: the menu header shows the user's name/email + the ◆ Org-admin / Member role badge (from the org role).
    - Test: with 2+ orgs, the org-switcher section renders and lists the orgs; picking a non-active org calls switchOrg(id).
    - Test: with exactly 1 org, NO switcher chrome renders (solo = a quiet name button) — D-166-02.
    - Test: the menu contains theme toggle + Sign out (sign-out lives INSIDE the menu now).
    - Test: the anchor renders icon-only when the rail is collapsed (dual-render), and opens the popover on click.
  </behavior>
  <action>
    Create ProfileMenu.tsx: a rail-footer identity button (mirroring RailItem's collapsed/expanded dual-render so it works at 58px) that opens a shadcn Popover holding, top-to-bottom: identity (name/email from useAuth), the `◆ Org-admin` / `Member` role badge (indigo `admin` / muted `member`, D-166-05), the org-switcher section (render ONLY when `orgs.length >= 2` per D-166-02 — solo shows just the identity, no switcher chrome), the theme toggle, and Sign out. Props `{ theme, onToggleTheme, onSignOut, collapsed }`; read org data via `useOrgOptional()` (activeOrgId, orgs, role, switchOrg). Picking an org calls `switchOrg(id)` — the menu is presentational; OrgProvider owns the D-166-08 teardown (the menu must NOT tear down streams itself). Close the popover after a switch.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/components/layout/ProfileMenu.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>ProfileMenu renders identity + role badge + (2+ orgs) switcher + theme + Sign out; solo hides the switcher; picking an org calls switchOrg; collapsed-rail dual-render works; tests green.</done>
  <acceptance_criteria>
    - The 1-org test asserts NO switcher chrome; the 2+-org test asserts switchOrg(id) is called on pick.
    - `grep -n "switchOrg" frontend/src/components/layout/ProfileMenu.tsx` matches; `grep -vi '^ *//' frontend/src/components/layout/ProfileMenu.tsx | grep -ci "clearThreadBucket"` == 0 (the menu never tears down streams itself).
    - `grep -n "Org-admin\|Member" frontend/src/components/layout/ProfileMenu.tsx` shows the role-badge copy.
    - `npx vitest run src/components/layout/ProfileMenu.test.tsx` passes; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NavPanel indigo Shield-mirror + ProfileMenu anchor + ChatLayout org-admin mount</name>
  <files>frontend/src/components/layout/NavPanel.tsx, frontend/src/components/layout/ChatLayout.tsx, frontend/src/components/layout/NavPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/layout/NavPanel.tsx:35-38 (the OUTSIDE-NAV_ITEMS regression anchor), :181-207 (amber operator shield + bare Sign out footer)
    - frontend/src/components/layout/ChatLayout.tsx:18,575,583 (ControlRoomPage import + view branch + mount), :471-478 (mobile-drawer operator shield), :299-310 (NavPanel props flow)
    - frontend/src/components/org/OrgAdminShell.tsx (Plan 04)
    - frontend/src/providers/OrgProvider.tsx (Plan 02 — useOrgOptional)
  </read_first>
  <behavior>
    - Test: when canManage is true, an indigo org-admin Shield-mirror renders in the footer (parallel to the amber operator shield) and clicking it fires onNavigate('org-admin').
    - Test: when canManage is false, the indigo shield does NOT render (absent, not disabled) — D-166-05.
    - Test: the indigo shield is NOT a member of the shared NAV_ITEMS array (the OUTSIDE-NAV_ITEMS contract holds for the org shield too).
    - Test: the footer renders the ProfileMenu anchor in place of the bare Sign out RailItem.
  </behavior>
  <action>
    NavPanel.tsx: add the indigo org-admin Shield-mirror in the footer block, directly parallel to the amber operator shield (copy the `{isOperator && (<RailItem icon={Shield} …/>)}` pattern at :185-198), gate it on `canManage` (read via `useOrgOptional()` — NavPanel is inside OrgProvider), re-tint amber → indigo, active-highlight when `activeView === "org-admin"`, and fire `onNavigate("org-admin")`. Render ONLY when canManage; honestly ABSENT for a member. Keep it OUTSIDE the shared NAV_ITEMS array (the :35-38 regression contract). Replace the bare "Sign out" RailItem (:200-206) with `<ProfileMenu theme={theme} onToggleTheme={onToggleTheme} onSignOut={onSignOut} collapsed={!expanded}/>`. Reuse the RailItem dual-render so both work at 58px.

    ChatLayout.tsx: add the `org-admin` view branch mounting `<OrgAdminShell onBack={() => onNavigate("chat")}/>` exactly as the `control-room` branch mounts ControlRoomPage (:575/583) — an additive branch before the trailing else. Mirror the mobile-drawer shield (:471-478) with a canManage-gated indigo entry so the shell is reachable on mobile too. The ActiveView union already carries "org-admin" (Plan 02). This closes the reachability triad (union + ChatLayout mount + NavPanel entry — all in-phase).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/components/layout/NavPanel.test.tsx && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>The indigo shield renders only when canManage (outside NAV_ITEMS) and navigates to org-admin; the ProfileMenu anchor replaces the bare Sign out; ChatLayout mounts OrgAdminShell on the org-admin branch (desktop + mobile drawer); full suite green.</done>
  <acceptance_criteria>
    - The canManage=false test asserts the indigo shield is ABSENT (not a disabled button).
    - `grep -n "org-admin" frontend/src/components/layout/ChatLayout.tsx` shows the view branch mounting OrgAdminShell.
    - `grep -n "OrgAdminShell" frontend/src/components/layout/ChatLayout.tsx` matches the import + mount.
    - The indigo shield is not added to NAV_ITEMS (the OUTSIDE-NAV_ITEMS test still passes).
    - `grep -ci "amber" frontend/src/components/layout/NavPanel.tsx` count is UNCHANGED from baseline (the org shield adds indigo, never amber — the existing operator shield's amber lines remain).
    - `npx vitest run` (full suite) passes; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav render → navigation | The shield/menu decide RENDERING only; the backend gate (Plan 01) is the authority for any /org data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-14 | Elevation | canManage-gated indigo shield | mitigate | Render-only gate on the fail-closed probe; a forged canManage shows the shield but the shell's fetches (/org/members, /org/audit) 403 server-side. The shield stays OUTSIDE NAV_ITEMS so the shared array never leaks the surface (test-locked). |
| T-166-15 | Information Disclosure | ProfileMenu identity leak across user switch | mitigate | Identity reads from the live useAuth/OrgProvider (keyed to userId); a sign-out clears the probe (fail-closed default) so no prior user's org/role leaks — mirrors the useOperatorProbe WR-01 same-tab-switch fix. |
| T-166-16 | Tampering | switch performed without teardown | mitigate | The menu delegates to OrgProvider.switchOrg (Plan 02) which owns the 067.5 guarded teardown; the menu itself never wipes buckets (grep-locked absence of clearThreadBucket in ProfileMenu). |
| T-166-SC | Tampering | npm installs | accept | No new packages — reuses shipped shadcn Popover/Sheet + RailItem. No install task. |
</threat_model>

<verification>
- `npx vitest run` (full suite) green — includes the ProfileMenu + NavPanel tests and the existing OUTSIDE-NAV_ITEMS regression.
- `npx tsc --noEmit` exits 0.
- The reachability triad is complete: ActiveView union (Plan 02) + ChatLayout mount + NavPanel entry.
</verification>

<success_criteria>
- ProfileMenu is the 079-C merged anchor (ADMIN-03) with the switcher only at 2+ orgs (ADMIN-02, D-166-02) and the personal sliver home (ADMIN-05).
- The indigo Shield-mirror gates the shell entry on canManage, absent for members (ADMIN-01, D-166-05); switching delegates to switchOrg (D-166-08).
</success_criteria>

<output>
Create `.planning/phases/166-org-admin-shell-org-switcher-profile-menu-anchor/166-05-SUMMARY.md` when done.
</output>
