---
phase: 162.5-threads-py-producer-extraction
plan: 03
type: execute
wave: 3
depends_on: [162.5-02]
files_modified:
  - backend/app/api/threads.py
  - backend/app/services/run_producer.py
  - backend/app/api/runs.py
autonomous: true
requirements: []          # G-5 refactor — no own REQ-ID (enables TEN-02). Intentionally empty.

must_haves:
  truths:
    - "D-A3: run_producer.py preserves ALL 8 finalize-ordering invariants byte-identical — (1) asyncio.shielded assistant persist -> (2) system_warnings persist -> (3) RUN-01b two-clause gate (completed AND cap_disposition != cap_paused) -> (4) finalize_run_terminal atomic status+ZREM co-write -> (5) terminal sentinel XADD AFTER finalize / BEFORE EXPIRE -> (6) EXPIRE (600 completed / 60 else) -> (7) harness F2 terminalize (gated) -> (8) RUN_TASKS.pop in the outer finally."
    - "D-A3 unification: `agent_runner`'s `_shielded_finalize` and `spawn_continuation_run`'s near-duplicate `_finalize` collapse onto ONE shared finalize routine — neither path diverges from the ordering; the producer/continuation differences (harness-F2 present only when active_workflow_run_id is set; cap_paused re-pause keeping runs:active only on the continuation) are handled by PARAMETERS, not a forked order."
    - "D-A4: additive-then-repoint with late imports — run_producer.py has NO top-level `import app.api.threads`; it late-imports the SSE-transport names (_emit/_emit_terminal/_spawn/RUN_TASKS/_RUN_STATUS_TO_TERMINAL_TYPE) AND the test-patched names (run_agent_loop/get_pg_pool/finalize_run_terminal/finalize_run/run_workflow) from `app.api.threads`, so the existing `patch(\"app.api.threads.*\")` surface still intercepts and the threads.py<->service cycle stays broken."
    - "D-A7: zero org_id/RLS/client-swap content — behavior-preserving only; agent_loop.py::run_agent_loop is NOT modified (the Deep red line stays where Phase 089 put it)."
  artifacts:
    - path: "backend/app/services/run_producer.py"
      provides: "The unified Deep/continuation producer shell + shared 8-invariant finalizer"
      contains: "async def run_producer"
    - path: "backend/app/services/run_producer.py"
      provides: "The extracted Deep-run continuation spawner (was threads.py spawn_continuation_run)"
      contains: "async def spawn_continuation_run"
    - path: "backend/app/api/runs.py"
      provides: "continue endpoint repointed to run_producer.spawn_continuation_run"
      contains: "from app.services.run_producer import spawn_continuation_run"
  key_links:
    - from: "backend/app/api/threads.py"
      to: "backend/app/services/run_producer.py"
      via: "asyncio.create_task(run_producer(run_id, ...)) in send_message"
      pattern: "run_producer\\("
    - from: "backend/app/api/runs.py"
      to: "backend/app/services/run_producer.py"
      via: "spawn_continuation_run late import in the continue verb"
      pattern: "run_producer import spawn_continuation_run"
    - from: "backend/app/services/run_producer.py"
      to: "backend/app/api/threads.py"
      via: "late-import of _emit/_emit_terminal/RUN_TASKS/run_agent_loop (cycle break + patch surface)"
      pattern: "from app.api.threads import"
---

<objective>
Extract the byte-identical HEART: the inline `agent_runner` producer shell + its
`_shielded_finalize` out of `send_message`, and the near-duplicate module-level
`spawn_continuation_run`, into ONE new `backend/app/services/run_producer.py` — UNIFIED
onto a single shared finalize routine so the two finalize orderings can never drift again.

This is the load-bearing move of Phase 162.5. All 8 finalize-ordering invariants (D-A3) MUST
survive byte-identical, on BOTH the producer path and the continuation path. The unification
handles their two genuine differences (harness-F2 terminalize; cap_paused re-pause) with
parameters, NEVER by forking the step order.

Purpose: give the Phase-163 crux a clean producer seam to thread `org_id` through, instead of
the ~1,027-LOC inline handler (enables TEN-02). Discipline is the `run_lifecycle.py` precedent
(D-A4): additive/dead-until-wired, late imports to break the cycle, docstring-states-invariant.

Output: `run_producer.py` with `run_producer(...)` + `spawn_continuation_run(...)` + the shared
finalizer; `send_message` spawns the extracted producer; `runs.py`'s continue verb repoints;
`agent_loop.py::run_agent_loop` untouched; the full suite green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162.5-threads-py-producer-extraction/162.5-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md

# The extraction discipline + the atomic terminal co-writer both paths call:
@backend/app/services/run_lifecycle.py

<interfaces>
<!-- RE-VERIFY every anchor live — threads.py has shrunk across Plans 01-02; re-locate before editing. -->

PRODUCER source in backend/app/api/threads.py (move to run_producer.py):
  - `async def agent_runner(run_id)` — nested closure in send_message           (~:1410-2003)
      classifier locals (_terminal_status/_terminal_error/_last_iteration/...), the middle
      try/except (Harness branch build+run_workflow / Deep branch RunContext+run_agent_loop),
      the TimeoutError/CancelledError/Exception classifier arms, and `_shielded_finalize` (~:1770-1993).
      CLOSURE VARS captured (become explicit params): _user_settings, _resolved_model,
      _resolved_provider, body, _active_workflow_run_id, _kickoff_definition, _kickoff_definition_id,
      thread_id, current_user, supabase, redis.
  - the spawn + RUN_TASKS registration + _evict callback in send_message         (~:2005-2015) — the
      create_task/RUN_TASKS[run_id]/add_done_callback STAY in send_message; only the coroutine moves.
  - `async def spawn_continuation_run(*, run_id, thread_id, current_user, redis, supabase,
     dropped_tool_calls)` — module-level                                          (~:2279-2444)
      its inner `_continuation` + `_finalize` (~:2335-2437). Comment ~:2274 admits it "Mirrors
      agent_runner's _shielded_finalize ordering" — THIS is the near-duplication the unification kills.

THE 8 FINALIZE INVARIANTS (from _shielded_finalize ~:1770-1993 — preserve byte-identical):
  1. asyncio.shielded `_persist()` assistant-message persist (exceptions logged, not raised)   (~:1792-1798)
  2. system_warnings persist — best-effort                                                      (~:1800-1809)
  3. RUN-01b reconcile — gate `_terminal_status == "completed" AND cap_disposition != "cap_paused"` (~:1826)
  4. finalize_run_terminal (status UPDATE + both ZREMs, ONE atomic co-write); status lands FIRST  (~:1882-1893)
  5. terminal sentinel XADD via _emit_terminal — AFTER finalize / BEFORE EXPIRE; catch BaseException (~:1897-1910)
  6. EXPIRE — 600 completed / 60 else                                                            (~:1912-1917)
  7. harness F2 terminalize — gate `_active_workflow_run_id is not None AND != "completed" AND not is_app_shutting_down()` (~:1947-1985)
  8. whole block under asyncio.shield; `RUN_TASKS.pop(run_id, None)` in the outer finally         (~:1988-1993)

THE TWO DIVERGENCES the unification must PARAMETERIZE (not fork):
  - Harness F2 (step 7): producer runs it when active_workflow_run_id is set; continuation passes
    active_workflow_run_id=None -> the same gate skips it naturally (byte-identical to today).
  - Step 4 finalize + step 5 sentinel: continuation, when `_terminal_status == "cap_paused"`, must
    call the SHARED finalize with a flag that (a) uses plain `finalize_run` (KEEPS runs:active — NO
    ZREM) and (b) skips the terminal sentinel (cap_paused not in _RUN_STATUS_TO_TERMINAL_TYPE). The
    producer's Deep first-turn is never "cap_paused" so it always takes the terminal branch — identical.
    NOTE: the shared step-3 gate `completed AND cap_disposition != cap_paused` is a safe superset that
    reproduces BOTH the producer two-clause gate AND the continuation plain `== completed` gate.

MONKEYPATCH surface that must keep working (test_dual_mode_wiring / test_089 / test_provider_router
patch these at app.api.threads.*): run_agent_loop, get_pg_pool, finalize_run, create_workflow_run,
insert_run, load_user_settings, override_provider, generate_thread_title. STAY-in-threads.py SSE
names (imported widely): _emit, _emit_terminal, _spawn, RUN_TASKS, TERMINAL_TYPES, _RUN_STATUS_TO_TERMINAL_TYPE.
runs.py already late-imports: _emit (:617), RUN_TASKS (:811), _spawn (:815), spawn_continuation_run (:1048).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the agent_runner producer shell + the shared finalizer into run_producer.py</name>
  <files>backend/app/services/run_producer.py, backend/app/api/threads.py</files>
  <read_first>
    - backend/app/api/threads.py — `agent_runner` `~:1410-2003` (incl. `_shielded_finalize` `~:1770-1993`) and the spawn/registration tail `~:2005-2015` (RE-VERIFY anchors — file has shrunk).
    - backend/app/services/run_lifecycle.py — the late-import cycle-break (`from app.api.threads import RUN_TASKS` `:202`, `_emit_terminal` `:278`) + `finalize_run_terminal` (the atomic co-writer step 4 calls).
    - This plan's `<interfaces>` — the 8 invariants + the two parameterized divergences.
  </read_first>
  <action>
    Create `backend/app/services/run_producer.py` with a module docstring that ENUMERATES the 8
    finalize invariants (copy the list from `<interfaces>`) and states the additive/dead-until-wired
    + late-import discipline. NO top-level `import app.api.threads`.

    Add `async def run_producer(run_id, *, thread_id, current_user, supabase, redis, user_settings,
    body, resolved_model, resolved_provider, active_workflow_run_id, kickoff_definition,
    kickoff_definition_id)` holding the `agent_runner` body VERBATIM (the classifier locals; the
    middle try with the Harness branch — now `wf_ctx = await build_harness_run_context(...)` then
    `run_workflow(...)` from Plan 02 — and the Deep branch `RunContext(...)` + `run_agent_loop(...)`;
    the TimeoutError/CancelledError/Exception classifier arms with their exact `_terminal_error`
    strings and the CancelledError re-raise). Convert every captured closure var to the explicit
    parameter of the same name (drop the leading underscores as you like, but keep values identical).

    Extract `_shielded_finalize` into a shared `async def _finalize_producer_run(*, run_id,
    thread_id, redis, result_sink, terminal_status, terminal_error, active_workflow_run_id,
    resolved_provider, resolved_model)` that performs steps 1-7 in EXACT order, with:
      - step 3 gate = `terminal_status == "completed" AND result_sink.get("cap_disposition") != "cap_paused"`;
      - step 4 branch on `terminal_status == "cap_paused"` -> plain `finalize_run` (keep runs:active),
        else `finalize_run_terminal` (atomic status+ZREM); producer always hits the terminal branch;
      - step 5 sentinel gated `terminal_status in _RUN_STATUS_TO_TERMINAL_TYPE` (skips cap_paused);
      - step 6 EXPIRE 600/60; step 7 harness F2 gated on `active_workflow_run_id is not None`.
    `run_producer` calls `await asyncio.shield(_finalize_producer_run(...))` inside the OUTER
    finally, and does `RUN_TASKS.pop(run_id, None)` in that finally's `finally` (step 8).

    LATE-IMPORT inside run_producer / _finalize_producer_run (function-local, `# noqa: PLC0415`)
    from `app.api.threads`: `_emit, _emit_terminal, _spawn, RUN_TASKS, _RUN_STATUS_TO_TERMINAL_TYPE,
    run_agent_loop, RunContext, get_pg_pool, finalize_run_terminal, finalize_run, create_workflow_run`,
    and from `app.services.workflow_kickoff` `build_harness_run_context`. This BOTH breaks the cycle
    AND keeps the `patch("app.api.threads.*")` surface live (threads.py re-exports those names).

    In `threads.py`: DELETE the nested `agent_runner` def; KEEP the spawn tail but repoint it to
    `task = asyncio.create_task(run_producer(run_id, thread_id=thread_id, current_user=current_user,
    supabase=supabase, redis=redis, user_settings=_user_settings, body=body,
    resolved_model=_resolved_model, resolved_provider=_resolved_provider,
    active_workflow_run_id=_active_workflow_run_id, kickoff_definition=_kickoff_definition,
    kickoff_definition_id=_kickoff_definition_id))`. Late-import `run_producer` inside `send_message`
    (`from app.services.run_producer import run_producer  # noqa: PLC0415`) for maximal cycle safety.
    Leave `RUN_TASKS[run_id] = task` + the `_evict` done-callback in send_message. Do NOT modify
    `agent_loop.py::run_agent_loop`.
  </action>
  <acceptance_criteria>
    - `grep -c "async def run_producer" backend/app/services/run_producer.py` returns 1; `grep -c "def _finalize_producer_run" backend/app/services/run_producer.py` returns 1.
    - run_producer.py has NO module-level threads import: `grep -c "^from app.api.threads import\|^import app.api.threads" backend/app/services/run_producer.py` returns 0; the late imports exist: `grep -c "from app.api.threads import" backend/app/services/run_producer.py` is >= 1.
    - The finalizer step order is intact (markers appear in source order): `finalize_run_terminal` before the `_emit_terminal` sentinel before `redis.expire` before the F2 `finish_run` — confirm by reading `_finalize_producer_run` top-to-bottom.
    - `agent_runner` is gone from threads.py: `grep -v '^[[:space:]]*#' backend/app/api/threads.py | grep -c "async def agent_runner"` returns 0; `grep -c "run_producer(" backend/app/api/threads.py` returns 1.
    - `agent_loop.py::run_agent_loop` is byte-unchanged: `git diff --stat backend/app/services/agent_loop.py` shows no change.
    - Producer/finalize/SSE-order tests green: `cd backend && python -m pytest tests/test_dual_mode_wiring.py tests/integration/test_089_agent_loop_result_seam.py tests/integration/test_075_4_terminal_race.py tests/integration/test_066_terminal_classification.py tests/test_provider_router.py -q` exits 0 (retarget any producer-path patch that a test can no longer reach to `app.services.run_producer.*`, per the Phase-089 Pitfall-1 precedent, ONLY if the late-import-from-threads path did not already preserve it).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_dual_mode_wiring.py tests/integration/test_089_agent_loop_result_seam.py tests/test_provider_router.py -q</automated>
  </verify>
  <done>run_producer.py owns the producer shell + the shared 8-invariant finalizer; send_message spawns it; the 8 invariants are in exact order; run_agent_loop untouched; producer/SSE-order tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Fold spawn_continuation_run onto the shared finalizer + repoint runs.py</name>
  <files>backend/app/services/run_producer.py, backend/app/api/threads.py, backend/app/api/runs.py</files>
  <read_first>
    - backend/app/api/threads.py — `spawn_continuation_run` `~:2279-2444` incl. its `_continuation` + `_finalize` and the cap_disposition override (RE-VERIFY anchors).
    - backend/app/api/runs.py — the continue verb's late import `from app.api.threads import spawn_continuation_run` (~:1048) + call (~:1054).
    - This plan Task 1's `_finalize_producer_run` — the shared routine spawn_continuation_run now reuses.
  </read_first>
  <action>
    Move `spawn_continuation_run` (and its inner `_continuation`) VERBATIM into `run_producer.py`.
    Then UNIFY its finalize: DELETE the near-duplicate inner `_finalize` and instead call the SAME
    `_finalize_producer_run(...)` from Task 1, passing `active_workflow_run_id=None` (a continuation
    is Deep-only -> step 7 F2 skips), the continuation's `result_sink`, and its `terminal_status`
    (which its own `finally` already set to `cap_paused` when the cap re-fired). The shared finalizer's
    step-4 `cap_paused` branch (plain `finalize_run`, keeps runs:active) and step-5 sentinel skip
    reproduce the continuation's original cap_paused semantics EXACTLY — verify against the old
    `_finalize` line-by-line that the persist -> sys-warnings -> RUN-01b -> finalize/keep-active ->
    sentinel-or-skip -> EXPIRE -> RUN_TASKS.pop order is preserved. Keep the outer
    `asyncio.shield(...)` + `RUN_TASKS.pop` in `_continuation`'s finally. Preserve the MessageCreate
    carrier + RunContext(resume_dropped_tool_calls=True, dropped_tool_calls=...) build verbatim.
    Reuse the same late-import block from Task 1.

    In `threads.py`: DELETE the moved `spawn_continuation_run`. (No re-export needed — the only
    importer is runs.py.) In `runs.py`: change the continue verb's late import to
    `from app.services.run_producer import spawn_continuation_run  # noqa: PLC0415`; the call args
    are unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "async def spawn_continuation_run" backend/app/services/run_producer.py` returns 1; it is GONE from threads.py: `grep -v '^[[:space:]]*#' backend/app/api/threads.py | grep -c "async def spawn_continuation_run"` returns 0.
    - spawn_continuation_run reuses the shared finalizer (no second finalize copy): `grep -c "def _finalize_producer_run\|def _finalize\b" backend/app/services/run_producer.py` returns 1 (only the shared one).
    - `grep -c "from app.services.run_producer import spawn_continuation_run" backend/app/api/runs.py` returns 1; the old `from app.api.threads import spawn_continuation_run` is gone.
    - Continuation semantics hold (cap_paused stays non-terminal + keeps runs:active; true terminal ZREMs + sentinels): `cd backend && python -m pytest tests/test_continue.py -q` exits 0.
    - `wc -l backend/app/api/threads.py` is dramatically smaller than 2444 (the ~1,027-LOC producer + continuation are gone).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_continue.py tests/test_dual_mode_wiring.py -q</automated>
  </verify>
  <done>spawn_continuation_run lives in run_producer.py and reuses the ONE shared finalizer; runs.py repointed; the two finalize orderings are now a single routine that cannot drift; continue tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer task ↔ Redis/Postgres | Unchanged — the producer/finalizer move verbatim; the same shielded terminal co-write + SSE sentinel ordering, the same best-effort framing, the same run-owner scoping. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162.5-03-NNS | (none) | run_producer.py | accept | No new threat surface — pure behavior-preserving extraction (D-A7). Zero org_id/RLS/client-swap content. The finalize-ordering invariants (incl. the 075.4-03 sentinel-after-finalize race fix and the 096-09 resume-safety F2 gate) are preserved byte-identical. Correctness control = the Plan-04 Deep byte-identical gate (Phase-089 harness + full suite). |
| T-162.5-SC | Tampering | package installs | accept | N/A — no package installs; no new dependency, no new runtime (D-A7 / D-14). |
</threat_model>

<verification>
- Full backend suite green: `cd backend && python -m pytest -q` — with special attention to the
  producer/SSE/finalize/continue/dual-mode families (test_089, test_075_4_terminal_race,
  test_066_*, test_dual_mode_wiring, test_continue, test_062_*, test_085_lifespan_shutdown).
- `git diff --stat backend/app/services/agent_loop.py` shows NO change (Deep red line intact).
- The 8 invariants appear in exact order in `_finalize_producer_run`; the continuation reuses it
  with `active_workflow_run_id=None` and the cap_paused keep-active branch.
- `wc -l backend/app/api/threads.py` is dramatically reduced from 2444.
</verification>

<success_criteria>
run_producer.py owns a unified Deep/continuation producer shell over a single shared finalizer
that preserves all 8 finalize-ordering invariants byte-identical on both paths; send_message and
runs.py repoint; `agent_loop.py::run_agent_loop` is untouched; the full suite is green (the
executable byte-identical proof, sealed by Plan 04's live gate).
</success_criteria>

<output>
Create `.planning/phases/162.5-threads-py-producer-extraction/162.5-03-SUMMARY.md` when done.
</output>
