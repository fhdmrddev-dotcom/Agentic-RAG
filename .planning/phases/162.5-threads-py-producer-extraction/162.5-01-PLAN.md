---
phase: 162.5-threads-py-producer-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/threads.py
  - backend/app/services/thread_title.py
  - backend/app/services/run_model_resolution.py
autonomous: true
requirements: []          # G-5 refactor — no own REQ-ID (enables TEN-02). Intentionally empty.

must_haves:
  truths:
    - "D-A2: thread_title.py owns the title subsystem (generate_thread_title + the 3 title helpers + _SINGLE_MODEL_PROVIDERS) and the auto-title orchestration; run_model_resolution.py owns the disabled-model / provider-resolution transform — the two lowest-risk leaf modules land FIRST (D-A5)."
    - "D-A4: the extraction is additive-then-repoint — the moved names are re-imported into threads.py at module scope so every existing `patch(\"app.api.threads.generate_thread_title\")` / `app.api.threads._resolve_enabled_model` still intercepts, and send_message calls the module-global title_fn/emit; the full backend suite stays green at each commit."
    - "Deep-Mode behavior is unchanged (D-A6 leaves slice) — title generation, the disabled-model fallback notice, and provider resolution produce byte-identical results; threads.py shrinks below its 2,444-LOC starting size."
  artifacts:
    - path: "backend/app/services/thread_title.py"
      provides: "Title subsystem + maybe_autotitle_thread orchestration"
      contains: "def generate_thread_title"
    - path: "backend/app/services/run_model_resolution.py"
      provides: "Disabled-model fallback + provider resolution transform"
      contains: "def resolve_run_model"
    - path: "backend/app/api/threads.py"
      provides: "send_message repointed to the two leaf modules; backward-compat re-imports"
      contains: "from app.services.thread_title import"
  key_links:
    - from: "backend/app/api/threads.py"
      to: "backend/app/services/thread_title.py"
      via: "maybe_autotitle_thread(title_fn=generate_thread_title, emit=_emit)"
      pattern: "maybe_autotitle_thread"
    - from: "backend/app/api/threads.py"
      to: "backend/app/services/run_model_resolution.py"
      via: "resolve_run_model(body, user_settings)"
      pattern: "resolve_run_model"
---

<objective>
Extract the two lowest-risk, self-contained leaf subsystems out of the 2,444-LOC
`backend/app/api/threads.py` into new service modules, keeping Deep-Mode behavior
byte-identical:

1. **`thread_title.py`** — the auto-title subsystem (`generate_thread_title` + the
   three title helpers + `_SINGLE_MODEL_PROVIDERS`) and the inline auto-title emit
   block that currently lives in `send_message`.
2. **`run_model_resolution.py`** — the disabled-model fallback + provider-resolution
   transform (`_resolve_enabled_model` / `_reresolve_fallback_provider` /
   `_apply_fallback_to_request`) and the inline model/provider resolution block.

Purpose: leaves-first sequencing (D-A5) — these have no producer coupling and no
finalize-ordering invariants, so they de-risk the hot file before the producer heart
moves in Plan 03. This is the established `run_lifecycle.py` extraction discipline (D-A4).

Output: two new modules under `backend/app/services/`, `send_message` repointed, and
the full backend test suite green (the byte-identical proof for this slice).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162.5-threads-py-producer-extraction/162.5-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md

# The extraction-discipline analog (docstring-states-invariant, additive/dead-until-wired,
# late imports to break the threads.py <-> service cycle):
@backend/app/services/run_lifecycle.py

<interfaces>
<!-- Contracts the executor works against. Extracted live; RE-VERIFY line anchors before editing
     (threads.py is 2,444 LOC and drifts as plans land). -->

CURRENT title subsystem in backend/app/api/threads.py (move verbatim to thread_title.py):
  - `_SINGLE_MODEL_PROVIDERS = frozenset({...})`                          (~:751)
  - `def _strip_think_blocks(text: str) -> str`                          (~:754)
  - `def _derive_title_from_message(msg: str) -> str`                    (~:774)
  - `def _clean_llm_title(raw: str, first_user_message: str) -> str`     (~:786)
  - `def generate_thread_title(first_user_message, user_settings=None, chat_model="") -> tuple[str, dict | None]`  (~:803)
  - inline auto-title emit block inside send_message                     (~:1360-1408)
      (title-check SELECT == "New Chat" -> run_in_threadpool(generate_thread_title,...)
       -> fallback_model emit BEFORE title emit -> threads.update(title) -> title emit;
       outer try swallows setup errors with logger.warning)

CURRENT resolution subsystem in backend/app/api/threads.py (move to run_model_resolution.py):
  - `async def _resolve_enabled_model(resolved_model, org_default) -> tuple[str, dict | None]` (~:196)
  - `async def _reresolve_fallback_provider(effective_model, current_provider) -> str`        (~:244)
  - `async def _apply_fallback_to_request(body, resolved_model, resolved_provider, user_settings)` (~:278)
  - inline model/provider resolution block inside send_message           (~:1224-1282)
      (_resolved_model = body.model or _user_settings.llm_model
       -> _resolve_enabled_model -> provider if/else using get_model_capability_async +
       override_provider -> if _model_fallback_notice: _apply_fallback_to_request)
      NOTE: `_user_settings = load_user_settings(...)` (~:1219) is SHARED with the producer
      (captured as user_settings) — it STAYS in send_message; resolve_run_model receives it.

MODULE-GLOBAL names in threads.py that MUST remain patchable at app.api.threads.*
(dozens of tests patch these — do NOT let the extraction move the patch surface):
  - generate_thread_title      (patched in ~30 test files as app.api.threads.generate_thread_title)
  - _resolve_enabled_model / _reresolve_fallback_provider / _apply_fallback_to_request
    (test_149_fallback_notice.py imports these via app.api.threads / threads_mod)
  - load_user_settings, override_provider, get_model_capability_async, _emit
    (patched at app.api.threads.* by test_threads.py / test_provider_router.py / test_dual_mode_wiring.py)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract thread_title.py (title subsystem + auto-title orchestration)</name>
  <files>backend/app/services/thread_title.py, backend/app/api/threads.py</files>
  <read_first>
    - backend/app/api/threads.py — the title subsystem `~:751-896` and the inline auto-title emit block `~:1360-1408` (RE-VERIFY anchors live).
    - backend/app/services/run_lifecycle.py — the extraction discipline (module docstring states the invariant; additive/dead-until-wired; late imports).
  </read_first>
  <action>
    Create `backend/app/services/thread_title.py` with a module docstring naming this as the
    Phase-162.5 G-5 title extraction (behavior-preserving). Move VERBATIM into it:
    `_SINGLE_MODEL_PROVIDERS`, `_strip_think_blocks`, `_derive_title_from_message`,
    `_clean_llm_title`, `generate_thread_title` (bring their imports: `openai`, `settings`,
    `_SUB_AGENT_MODEL_DEFAULTS`, `get_llm_client`, `_uses_max_completion_tokens`, `logger`).
    Zero "while-I'm-in-here" edits — copy the bodies unchanged (bugs included).

    Add an orchestration function `async def maybe_autotitle_thread(*, supabase, redis,
    thread_id, first_message, user_settings, chat_model, run_id, title_fn, emit)` holding the
    logic of the inline `~:1360-1408` block: the `title == "New Chat"` guard, the
    `run_in_threadpool(title_fn, first_message, user_settings, chat_model)` call, the ordering
    invariant `fallback_model` emit BEFORE `title` emit, the `threads.update(title)` write, and
    the two nested try/except with the identical `logger.warning` shapes. Take `title_fn` and
    `emit` as PARAMETERS (dependency injection) so patching `app.api.threads.generate_thread_title`
    / `app.api.threads._emit` still intercepts (D-A4).

    In `threads.py`: DELETE the moved definitions; add `from app.services.thread_title import
    generate_thread_title, maybe_autotitle_thread` at module scope (re-import keeps
    `app.api.threads.generate_thread_title` present + patchable). Replace the inline `~:1360-1408`
    block with a single `await maybe_autotitle_thread(supabase=supabase, redis=redis,
    thread_id=thread_id, first_message=body.content, user_settings=_user_settings,
    chat_model=body.model or "", run_id=run_id, title_fn=generate_thread_title, emit=_emit)`
    — `generate_thread_title`/`_emit` resolved as threads.py module globals so patches apply.
    Do NOT touch `agent_loop.py::run_agent_loop`.
  </action>
  <acceptance_criteria>
    - `grep -c "def generate_thread_title" backend/app/services/thread_title.py` returns 1; `grep -c "def maybe_autotitle_thread" backend/app/services/thread_title.py` returns 1.
    - `grep -c "from app.services.thread_title import" backend/app/api/threads.py` returns 1 (backward-compat re-import present).
    - The `def generate_thread_title` body no longer lives in threads.py: `grep -v '^[[:space:]]*#' backend/app/api/threads.py | grep -c "def generate_thread_title"` returns 0.
    - `wc -l backend/app/api/threads.py` is strictly less than 2444.
    - Title-path tests pass unchanged (patch surface preserved): `cd backend && python -m pytest tests/unit/test_threads_title_gen.py tests/test_mdl_verification.py tests/unit/test_threads_suggestions_threadpool.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_threads_title_gen.py tests/test_mdl_verification.py -q</automated>
  </verify>
  <done>thread_title.py owns the title subsystem + orchestration; send_message calls maybe_autotitle_thread with injected title_fn/emit; all app.api.threads.generate_thread_title patches still intercept; threads.py shrinks.</done>
</task>

<task type="auto">
  <name>Task 2: Extract run_model_resolution.py (disabled-model fallback + provider resolution)</name>
  <files>backend/app/services/run_model_resolution.py, backend/app/api/threads.py</files>
  <read_first>
    - backend/app/api/threads.py — the three resolution helpers `~:196-322` and the inline model/provider resolution block `~:1224-1282` (RE-VERIFY anchors live; note `_user_settings` load at ~:1219 STAYS in send_message).
    - backend/app/services/run_lifecycle.py — the additive-then-repoint discipline.
  </read_first>
  <action>
    Create `backend/app/services/run_model_resolution.py` with a docstring naming the
    Phase-162.5 G-5 model-resolution extraction. Move VERBATIM: `_resolve_enabled_model`,
    `_reresolve_fallback_provider`, `_apply_fallback_to_request` (bring imports:
    `load_all_model_overrides`, `override_provider`, `get_model_capability_async`, `logger`).

    Add `async def resolve_run_model(*, body, user_settings)` holding the inline `~:1224-1282`
    transform: compute `resolved_model = body.model or user_settings.llm_model`; call
    `_resolve_enabled_model(resolved_model, user_settings.llm_model)`; run the provider if/else
    (explicit `body.provider` branch vs `get_model_capability_async` + the D-075.3-08
    `capability_source == "registry"` gate + `override_provider` alignment); and, when
    `model_fallback_notice` is truthy, apply `_apply_fallback_to_request`. Return the tuple
    `(resolved_model, resolved_provider, model_fallback_notice, body, user_settings)` so the
    caller keeps the (possibly mutated) `body` and `user_settings`.

    In `threads.py`: DELETE the three moved helpers; add `from app.services.run_model_resolution
    import _resolve_enabled_model, _reresolve_fallback_provider, _apply_fallback_to_request,
    resolve_run_model` at module scope (re-import keeps them patchable/importable for
    test_149_fallback_notice.py). Replace the inline `~:1224-1282` block with
    `_resolved_model, _resolved_provider, _model_fallback_notice, body, _user_settings =
    await resolve_run_model(body=body, user_settings=_user_settings)`. Leave the
    `_user_settings = load_user_settings(...)` load (~:1219), the `run_id` generation, and the
    subsequent `register_run_start` / fallback-notice `_emit` exactly where they are.
  </action>
  <acceptance_criteria>
    - `grep -c "def resolve_run_model" backend/app/services/run_model_resolution.py` returns 1; the file also contains `def _resolve_enabled_model`, `def _reresolve_fallback_provider`, `def _apply_fallback_to_request`.
    - `grep -c "from app.services.run_model_resolution import" backend/app/api/threads.py` returns 1.
    - The three helper bodies no longer live in threads.py: `grep -v '^[[:space:]]*#' backend/app/api/threads.py | grep -c "async def _resolve_enabled_model"` returns 0.
    - `cd backend && python -m pytest tests/test_149_fallback_notice.py -q` exits 0 (the app.api.threads / threads_mod imports of the three helpers still resolve; behavior byte-identical).
    - The full send_message wiring holds: `cd backend && python -m pytest tests/test_provider_router.py tests/integration/test_threads.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_149_fallback_notice.py tests/test_provider_router.py -q</automated>
  </verify>
  <done>run_model_resolution.py owns the disabled-model fallback + provider resolution; send_message calls resolve_run_model; test_149 + provider-router tests pass; threads.py shrinks further.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → send_message | Unchanged by this plan — no new boundary; the extracted leaves run inside the same authenticated handler with the same inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162.5-01-NNS | (none) | thread_title.py / run_model_resolution.py | accept | No new threat surface — pure behavior-preserving extraction (D-A7). Zero org_id/RLS/client-swap content. The correctness control is the full backend test suite staying green + the Plan-04 Deep byte-identical gate. |
| T-162.5-SC | Tampering | package installs | accept | N/A — no npm/pip/cargo installs in this phase (no new dependency, D-A7). No package-legitimacy gate required. |
</threat_model>

<verification>
- Full backend suite green after both tasks: `cd backend && python -m pytest -q` (integration rows self-skip when DB/Redis is absent — the live green run is the Plan-04 operator gate).
- `wc -l backend/app/api/threads.py` strictly less than 2444.
- No `app.api.threads.generate_thread_title` / `_resolve_enabled_model` patch or import broke (title + fallback + provider-router tests green without test-side retargeting).
</verification>

<success_criteria>
thread_title.py and run_model_resolution.py exist and own their subsystems; send_message
delegates to them; every pre-existing patch/import against `app.api.threads.*` for the moved
names still resolves; threads.py is smaller; Deep-Mode title/fallback/provider behavior is
byte-identical (suite green).
</success_criteria>

<output>
Create `.planning/phases/162.5-threads-py-producer-extraction/162.5-01-SUMMARY.md` when done.
</output>
