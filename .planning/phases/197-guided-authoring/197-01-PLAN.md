---
phase: 197-guided-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "The pre-draft describe screen renders exactly what it rendered before this phase, on BOTH doors"
    - "A later wave can tell a real regression from a SEED-171 flake, because the pre-change gate verdict is written down"
    - "The D-05 red line is a mechanical criterion any wave can run, not a judgement anyone has to make"
  artifacts:
    - path: ".planning/phases/197-guided-authoring/197-01-SUMMARY.md"
      provides: "The phase base SHA, the three baseline verdicts, the four gate baselines and the four numstat criteria, all recorded verbatim"
      contains: "phase base SHA"
  key_links:
    - from: "197-01-SUMMARY.md"
      to: "every later plan's wave-merge check"
      via: "the four numstat deletion criteria and the recorded gate baselines"
      pattern: "git diff --numstat"
---

<objective>
Land the D-05 red line as a **mechanical criterion** before a single source byte changes, and
record the pre-change measurements every later wave will be judged against.

Purpose: 188.1's binding lesson — *a characterization baseline only proves something if it
PREDATES the change.* The baseline this phase needs **already exists and already predates it by
four phases**; what does not exist is a written record that it was green on this tree, and a
criterion later waves can run. This plan supplies both. It also removes the phase's single
largest triage hazard: `WorkflowBuilderPage.canvas.test.tsx` is BOTH a SEED-171 flaky suite AND a
suite this phase extends, so a red run on it is only interpretable against a verdict taken now.

Output: `197-01-SUMMARY.md` carrying the phase base SHA, three baseline verdicts, four gate
baselines, six re-derived ledger triples and the four standing numstat criteria.

⚠ **THIS PLAN MODIFIES NO SOURCE FILE.** `files_modified` is empty by design. A task here that
edits source has misread the plan.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-guided-authoring/197-CONTEXT.md
@.planning/phases/197-guided-authoring/197-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the three shipped pre-draft baselines green at the phase base SHA and record the verdict verbatim</name>

  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx` — read `:1-77` (the
      docblock, including the predate proof at `:19-31` and the zero-deletions rule at `:60-67`)
      and `:250-300` (the single `capture` helper). This file is the D-05 fence. **Do not author a
      second one** — a capture taken now postdates this phase's own context and would create the
      exact *"a capture and the assertion that guards it drift apart"* condition its docblock names.
    - `frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` — the LOOSE door's
      half. ⚠ There are TWO near-identical pre-draft screens; both contain the literal splice anchor
      `<div className="flex flex-col items-center gap-3">`.
    - `.planning/phases/197-guided-authoring/197-VALIDATION.md` §"How to read a RED gate"
    - `CLAUDE.md` §"Parallel execution — worktrees are ENABLED" (the cap rule and its 2026-08-17
      correction)
  </read_first>

  <files>none — this task runs commands and records their output</files>

  <action>
    Record `git rev-parse HEAD` as the **phase base SHA**. Every numstat criterion in this phase is
    expressed against this SHA; write it into the SUMMARY as a literal, not as a description.

    Run the three shipped baseline suites in ONE invocation, from `frontend/`, with
    `GSD_VITEST_MAX_WORKERS=2`:
    `src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx`,
    `src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx`,
    `src/pages/WorkflowBuilderPage.describe.test.tsx`.

    Copy the verdict lines into the SUMMARY **verbatim** — files, tests, passed, failed — never a
    paraphrase. If any suite is red at the base SHA, STOP and report: a red pre-change baseline is a
    finding about the tree, not a licence to proceed, and no later plan's D-05 claim would mean
    anything.

    Also record `git log --numstat` for `WorkflowBuilderPage.preDraft.baseline.test.tsx` restricted
    to its five commits, so the "deletions have happened exactly once, deliberately" history is in
    the SUMMARY rather than only in RESEARCH.md.
  </action>

  <acceptance_criteria>
    - The SUMMARY contains the phase base SHA as a 40-character hex literal.
    - The SUMMARY quotes the vitest verdict line for the run, showing `failed 0` (or the equivalent
      `Tests  N passed`), covering all three named files.
    - The SUMMARY names all three suite paths explicitly.
    - No file under `frontend/`, `backend/` or `scripts/` appears in `git status --short` as
      modified by this task.
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx</automated>
  </verify>

  <done>All three baseline suites green at the base SHA, and the SHA plus the verbatim verdict are in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the four gate baselines and the six G-5 ledger triples — never inherit a number</name>

  <read_first>
    - `CLAUDE.md` §"Parallel execution" — the three recorded rots of the gate total, and the rule
      that a GROWING total is the gate WORKING (its contract is *no per-file decrease* + *zero
      failing*, never a fixed grand total).
    - `CLAUDE.md` §"Hot-file ledger" — the re-derivation recipe, including the rule that six-digit
      buckets (`260814`, `260809`, `260529`) are DATED QUICK TASKS and are subtracted from the
      phase count.
    - `.planning/phases/197-guided-authoring/197-RESEARCH.md` §"How to read the count gate" — the
      last measured verdict (`total 4291 · failed 0 · pinned 4217 · 89/89`, tsc baseline 33, backend
      211 failed / 4046 passed) is the figure to **compare against**, never to copy forward.
  </read_first>

  <files>none — this task runs commands and records their output</files>

  <action>
    Re-derive and record, each verbatim:

    1. `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — quote the **verdict line**,
       not a summary of it (total, failed, pinned total, pinned-file fraction).
    2. `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — the error count. ⚠ The `-p` is
       load-bearing; a bare `--noEmit` checks ZERO files and would record a meaningless 0.
    3. `backend/venv/Scripts/python.exe -m pytest backend/tests/unit -q` — the passed/failed summary
       line.
    4. The six G-5-firing files this phase touches, each re-derived with the ledger's own three
       commands (`commits`, `phases`, `lines`): `frontend/src/lib/api.ts`,
       `frontend/src/pages/WorkflowBuilderPage.tsx`, `backend/app/api/workflows.py`,
       `backend/app/services/harness/grounding.py`,
       `frontend/src/components/workflows/builderStore.ts`,
       `backend/app/services/workflow_authoring.py`.

    If the gate is RED, follow VALIDATION.md's non-negotiable procedure: take failing filenames from
    the gate's **own persisted JSON before any re-run**, check each against `git status --short`
    (which must be clean of source changes at this point), and record them as the phase's inherited
    flake set. **Do not touch the cap.** A red gate at the base SHA is a recorded baseline
    condition, not a blocker for this plan — but it MUST be written down, because every later wave's
    triage depends on knowing which suites were already red before anyone touched anything.
  </action>

  <acceptance_criteria>
    - The SUMMARY quotes the count-gate verdict line verbatim, including the grand total, the failed
      count, the pinned total and the `N/N pinned files present` fraction.
    - The SUMMARY records the tsc error count produced by a command that includes
      `-p tsconfig.app.json`.
    - The SUMMARY records the backend pytest summary line.
    - The SUMMARY carries a six-row table of `commits / phases / lines` for the six named files, each
      derived by the ledger recipe, with the CONTEXT.md figure beside it where it differs.
    - If the gate was red, the SUMMARY names every failing file taken from the persisted JSON and
      states explicitly that no source file was modified when they were captured.
  </acceptance_criteria>

  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>

  <done>All four gate baselines and all six ledger triples are recorded as measured values with their commands beside them.</done>
</task>

<task type="auto">
  <name>Task 3: Declare the four standing numstat criteria that fence this phase's four untouchable files</name>

  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx:60-67` — the rule this
      criterion is LIFTED from, verbatim: *"a diff against these strings is a BEHAVIOUR CHANGE TO
      EXPLAIN, never a test to update … `git diff --numstat` on this file must show ZERO DELETIONS
      … the first wave that intentionally changes a rendered node re-captures ONCE, DECLARED in its
      own plan with a date and a reason."*
    - `.planning/phases/197-guided-authoring/197-CONTEXT.md` — D-05 (the red line), D-02 (SeedReceipt
      is not widened), D-11 and D-20 (the publish gate is untouched, and D-11's reach is row 3 only).
    - `.planning/phases/197-guided-authoring/197-RESEARCH.md` §D-05 — ⚠ the paragraph naming the ONE
      thing this fence does NOT cover: the **drafted** header's byte pin
      (`FLAG_OFF_HEADER_MARKUP`), which plan `197-10` deliberately re-captures. **Two different
      literals, two different dispositions.**
  </read_first>

  <files>none — this task writes the criteria into the SUMMARY</files>

  <action>
    Write the four criteria into the SUMMARY as runnable commands against the recorded base SHA,
    each with the decision it defends and the disposition it carries:

    - **D-05 / SC#2 / SC#3** — deletions must be `0` for
      `frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx` and
      `frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx`.
    - **D-02** — `frontend/src/components/workflows/SeedReceipt.tsx` must show `0 0` (no insertions
      AND no deletions). This phase composes that component; it does not widen it.
    - **D-11 / D-20** — `backend/app/services/harness/publish_service.py` must show `0 0`. The
      publish gauntlet learns nothing new in this phase.

    Then state, in the SUMMARY and in one sentence each, the two **exclusions** so a later executor
    cannot mistake one for the other:
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx` is **NOT** under a zero-deletion
      criterion. Plan `197-10` performs a declared, dated, one-time re-capture of
      `FLAG_OFF_HEADER_MARKUP` band 3 under D-19. ⚠ That band's own note says the phase that wrote
      it expects **no third** re-capture; this phase is the third and says so.
    - `scripts/vitest-count-gate.cjs` is **NOT** under a zero-deletion criterion. Raising a pin
      necessarily deletes one line, so a `grep -c '^-[^-]'` expecting 0 on that file is wrong.

    State the standing instruction: these four commands run **at every wave merge and at phase
    close**, and a non-zero deletion on any of the four is a behaviour change to explain, never a
    test to update.
  </action>

  <acceptance_criteria>
    - The SUMMARY contains four `git diff --numstat <base-sha> HEAD -- <path>` commands with the
      literal base SHA substituted, covering exactly:
      `WorkflowBuilderPage.preDraft.baseline.test.tsx`, `WorkflowDoorSwitch.baseline.test.tsx`,
      `SeedReceipt.tsx`, `publish_service.py`.
    - Each command is annotated with the decision id it defends (`D-05`, `D-02`, `D-11`).
    - The SUMMARY names `WorkflowBuilderPage.header.test.tsx` and `scripts/vitest-count-gate.cjs`
      as explicit EXCLUSIONS, each with its reason.
    - Running all four commands now returns `0` deletions on every row (trivially true — nothing has
      been edited — and recorded as the starting state).
  </acceptance_criteria>

  <verify>
    <automated>git diff --numstat HEAD -- frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx frontend/src/components/workflows/SeedReceipt.tsx backend/app/services/harness/publish_service.py</automated>
  </verify>

  <done>The four criteria and the two exclusions are written into the SUMMARY as runnable commands against the literal base SHA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed by this plan | This plan runs read-only commands and writes one planning document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-BASE | Repudiation | the phase's own evidence trail | mitigate | The base SHA, the three baseline verdicts and the four gate baselines are recorded as literals with their commands beside them, so a later wave cannot silently inherit a number that has rotted — this project's gate totals have rotted three times, once in a single day |
| T-197-SC | Tampering | npm/pip/cargo installs | accept | **No package is installed by this phase.** `197-RESEARCH.md` §"Package Legitimacy Audit" records the gate as vacuous-with-reason: every file in scope is existing source or a new first-party module. No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
- All three shipped baseline suites green at the base SHA, verdict quoted verbatim.
- Count gate, tsc (`-p tsconfig.app.json`), and backend pytest baselines recorded as measured
  values, not inherited ones.
- Six G-5 ledger triples re-derived with the ledger's own recipe.
- Four numstat criteria written as runnable commands against the literal base SHA, plus the two
  named exclusions.
- `git status --short` shows **no** source file modified by this plan.
</verification>

<success_criteria>
- `197-01-SUMMARY.md` exists and carries: the 40-char base SHA; the three baseline verdicts; the
  four gate baselines; the six ledger triples; the four numstat criteria; the two exclusions.
- `files_modified` for this plan is genuinely empty — verified with `git status --short`.
- Every later plan in this phase can now run the D-05 criterion without re-deriving anything.
</success_criteria>

<output>
Create `.planning/phases/197-guided-authoring/197-01-SUMMARY.md` when done.
</output>
