---
phase: 148-governance-audit-users-feature-visibility
plan: 02
type: execute
wave: 2
depends_on: ["148-01"]
files_modified:
  - supabase/migrations/098_feature_visibility.sql
  - backend/app/models/user_settings.py
  - backend/app/dependencies.py
autonomous: true
requirements: [VIS-01, ADMIN-03]

must_haves:
  truths:
    - "D-03: require_visible returns 403 with body 'This feature is available to administrators only.' (NOT 404) for a non-operator on an Operators-only feature; no-op for operators and Everyone features"
    - "D-06: cold-read / DB-blip / missing-key resolves each feature to its per-feature hardcoded default — deny skill_studio + model_management, allow workflow_authoring + governance_health"
    - "A disabled user (auth.users.banned_until in the future) is refused on ANY authed route via an app-layer check inside get_current_user, even with a still-valid JWT"
    - "The ban check FAILS OPEN on DB read error (a blip never locks out every user)"
    - "Audience is stored as an enum-shaped record {\"audience\":\"operators|everyone\"} on app_settings.feature_visibility — NEVER a boolean (SEED-115 forward-compat)"
    - "set_feature_visibility writes via an atomic JSONB || merge per key (no lost-update clobber)"
  artifacts:
    - path: "supabase/migrations/098_feature_visibility.sql"
      provides: "app_settings.feature_visibility jsonb column + D-05 day-one seed"
      contains: "feature_visibility jsonb"
    - path: "backend/app/models/user_settings.py"
      provides: "feature_audience resolver + _GOVERNED_FEATURES cold-default map + set_feature_visibility JSONB-merge writer"
      contains: "_GOVERNED_FEATURES"
    - path: "backend/app/dependencies.py"
      provides: "require_visible(feature) factory + _is_banned + ban check in get_current_user"
      contains: "require_visible"
  key_links:
    - from: "backend/app/dependencies.py require_visible"
      to: "backend/app/models/user_settings.py feature_audience"
      via: "audience resolution via the ONE swappable is_operator boundary"
      pattern: "feature_audience"
    - from: "backend/app/dependencies.py get_current_user"
      to: "auth.users.banned_until"
      via: "asyncpg fetchrow after supabase.auth.get_user validates"
      pattern: "banned_until"
---

<objective>
Build the shared substrate every downstream 148 slice depends on: migration 098 (the `feature_visibility` JSONB column + day-one seed), the audience resolver/writer in `user_settings.py`, and the two gates in `dependencies.py` — `require_visible(feature)` (VIS-01 enforcement) and the app-layer ban check inside `get_current_user` (closes the JWT window). This is the ONLY plan that edits the shared auth path.

Purpose: One swappable audience boundary + one by-construction ban seam — everything else composes these.
Output: 1 migration file + 2 modified backend modules.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md
@.planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md
@supabase/migrations/097_operator_flags.sql
@backend/app/models/user_settings.py
@backend/app/dependencies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 098 — app_settings.feature_visibility JSONB column + day-one seed</name>
  <files>supabase/migrations/098_feature_visibility.sql</files>
  <read_first>
    - supabase/migrations/097_operator_flags.sql (copy the header verbatim: APPLY-via-SQL-editor + regenerate-full-schema + CLOUD-PARITY note; and the `INSERT ... ('global') ON CONFLICT DO NOTHING` idempotency line)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 4 (exact SQL)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md (migration section — next free number 098; feature_visibility does NOT join main._DIRECT_COLUMNS)
  </read_first>
  <action>
    Create `supabase/migrations/098_feature_visibility.sql`. Filename must match `<digits>_name.sql` (no letter suffix). Body: `ALTER TABLE public.app_settings ADD COLUMN IF NOT EXISTS feature_visibility jsonb NOT NULL DEFAULT '{}'::jsonb;`, then `INSERT INTO public.app_settings (id) VALUES ('global') ON CONFLICT DO NOTHING;`, then the D-05 day-one seed `UPDATE public.app_settings SET feature_visibility = jsonb_build_object('skill_studio', jsonb_build_object('audience','operators'), 'model_management', jsonb_build_object('audience','operators'), 'workflow_authoring', jsonb_build_object('audience','everyone'), 'governance_health', jsonb_build_object('audience','everyone')) WHERE id = 'global';`. Copy 097's comment header verbatim (apply-order + cloud-parity). IMPORTANT: audience values are enum-shaped records, NEVER booleans. Do NOT extend any CHECK constraint — the new audit action codes (`user.disable`, `operator.grant`, `visibility.set`, `audit.export`, `audit.view_platform`) are OPERATOR-ledger actions written to `operator_audit_log.action`, which is FREE-TEXT (no CHECK — verified mig 095); the platform `audit_log` 19-action CHECK is READ-ONLY here and is not touched. This migration is metadata-only.
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/098_feature_visibility.sql` exists; filename matches `^[0-9]+_.*\.sql$`.
    - `grep -c "feature_visibility jsonb" supabase/migrations/098_feature_visibility.sql` ≥ 1.
    - Seed contains all four feature keys with `jsonb_build_object('audience', ...)` values (no bare boolean literal `true`/`false` as an audience).
    - No `ALTER ... CHECK` / `DROP CONSTRAINT` on any audit table in this migration.
    - Header carries the CLOUD-PARITY note (grep "cloud" or "parity").
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/migrations/098_feature_visibility.sql && grep -c "feature_visibility jsonb" supabase/migrations/098_feature_visibility.sql && ! grep -Eiq "add constraint|drop constraint|check \(" supabase/migrations/098_feature_visibility.sql && echo OK</automated>
  </verify>
  <done>Migration 098 exists (metadata-only column + enum-record seed), no CHECK changes, cloud-parity header present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audience resolver + JSONB-merge writer in user_settings.py</name>
  <files>backend/app/models/user_settings.py</files>
  <read_first>
    - backend/app/models/user_settings.py (the 147 flag fields ~:141-143; provider_model_lists double-serialization guard ~:403-413; _build_settings_from_row ~:493-541; TTL cache + save_app_settings ~:224-315; invalidate_settings_cache ~:253-256; sync load_app_settings ~:602-609)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 4 (verbatim resolver + writer)
    - backend/tests/test_148_visibility_cold_default.py (the target this task turns GREEN)
  </read_first>
  <behavior>
    - feature_audience("skill_studio") with a cold/empty cache -> "operators" (deny); ("workflow_authoring") -> "everyone" (allow) — from _GOVERNED_FEATURES.
    - feature_audience reading a stored record {"audience":"operators"} -> "operators"; a malformed/missing key -> the per-feature default; an unknown feature -> "operators" (safe-deny).
    - feature_audience NEVER reads or returns a boolean; on any exception it returns the hardcoded default (never raises).
    - set_feature_visibility("skill_studio","everyone") issues an atomic `feature_visibility = coalesce(feature_visibility,'{}'::jsonb) || $1::jsonb` merge (only that key changes) then invalidates the cache.
  </behavior>
  <action>
    Add to `user_settings.py`: (1) a `feature_visibility: dict = {}` field on the effective-settings model + a builder read `feature_visibility=row.get("feature_visibility") or {}` with the same `isinstance(x, str): json.loads` double-serialization guard `provider_model_lists` uses. (2) `_GOVERNED_FEATURES = {"skill_studio":"operators","model_management":"operators","workflow_authoring":"everyone","governance_health":"everyone"}` — the ONLY place D-06 polarity lives. (3) `feature_audience(feature) -> str` reading the sync `load_app_settings().feature_visibility`, returning the stored `audience` only if in `("everyone","operators")`, else `_GOVERNED_FEATURES.get(feature,"operators")`; wrap in try/except -> default (mirrors maintenance_mode's no-raise posture). (4) `async def set_feature_visibility(feature, audience) -> bool` doing the atomic JSONB `||` merge via `get_pg_pool()` then `invalidate_settings_cache()`. Do NOT route the write through `save_app_settings` (its whole-column `SET` causes the Pitfall-4 lost-update clobber). The caller validates feature/audience against code allowlists — this function assumes validated input but still only ever serializes `{feature: {"audience": audience}}`.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_148_visibility_cold_default.py -x` exits 0 (cold-default polarity proven).
    - `grep -n "_GOVERNED_FEATURES" backend/app/models/user_settings.py` shows exactly the four keys with the D-06 polarity.
    - `set_feature_visibility` SQL contains `|| $1::jsonb` and does NOT call `save_app_settings`.
    - `feature_audience` has a try/except that returns a `_GOVERNED_FEATURES` default (never raises, never returns bool).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_visibility_cold_default.py -x</automated>
  </verify>
  <done>Resolver + JSONB-merge writer land; cold-default test GREEN; polarity lives in exactly one map.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: require_visible factory + app-layer ban check in dependencies.py</name>
  <files>backend/app/dependencies.py</files>
  <read_first>
    - backend/app/dependencies.py (require_operator ~:201-218 for gate shape; operator_audit_floor ~:221-257 for the yield idiom; get_current_user ~:107-118 — the seam to modify; get_pg_pool ~:78-104 for the asyncpg read)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 1 (require_visible) + §Pattern 2 (_is_banned, fail-open)
    - backend/tests/test_148_require_visible.py, backend/tests/test_148_ban_enforcement.py, backend/tests/test_148_ban_fail_open.py (targets this task turns GREEN)
  </read_first>
  <behavior>
    - require_visible("skill_studio") as a dependency: operator -> no-op (None); non-operator with audience "everyone" -> no-op; non-operator with audience "operators" -> HTTPException(403, "This feature is available to administrators only.") — NOT 404 (D-03).
    - get_current_user, after supabase.auth.get_user validates a token, calls _is_banned(user_id); if banned_until is in the future -> HTTPException(403, "This account is disabled — contact your administrator.").
    - _is_banned returns False (fail-OPEN) on any DB read exception; returns True only when banned_until is a real future timestamp.
    - authenticate_operator_request / require_operator are UNCHANGED — /admin keeps its byte-identical 404.
  </behavior>
  <action>
    Add `require_visible(feature: str)` per RESEARCH §Pattern 1: a closure returning an async `_dep(current_user = Depends(get_current_user))` — `if await is_operator(current_user["id"]): return`; lazy-import `feature_audience`; `if feature_audience(feature) == "everyone": return`; else raise `HTTPException(status.HTTP_403_FORBIDDEN, detail="This feature is available to administrators only.")`. `is_operator` is the ONE swappable boundary (SEED-115). Add `async def _is_banned(user_id) -> bool` per RESEARCH §Pattern 2: `SELECT banned_until FROM auth.users WHERE id = $1` via `get_pg_pool()`, return `bu is not None and bu > datetime.now(timezone.utc)`, wrapped in try/except returning False (fail-open). Insert the ban check INSIDE `get_current_user` AFTER `supabase.auth.get_user(token)` resolves and BEFORE returning the identity dict: `if await _is_banned(response.user.id): raise HTTPException(403, "This account is disabled — contact your administrator.")`. Keep it to one asyncpg fetchrow (~1ms). Do NOT trust `supabase.auth.get_user` to reject the ban (it does not on a live token). Do NOT touch `authenticate_operator_request` or `require_operator`.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_148_require_visible.py tests/test_148_ban_enforcement.py tests/test_148_ban_fail_open.py -x` exits 0.
    - `require_visible` raises 403 (not 404) with the exact plain body for the operators-only non-operator case; returns None for operator and everyone cases.
    - `_is_banned` has a try/except returning False on error; the ban check sits inside `get_current_user` after token validation.
    - `git diff` shows NO change to `authenticate_operator_request` or `require_operator` (the /admin 404 contract is untouched).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_require_visible.py tests/test_148_ban_enforcement.py tests/test_148_ban_fail_open.py -x</automated>
  </verify>
  <done>require_visible (403/no-op) + fail-open ban check in the shared auth seam land; three VIS-01/ban tests GREEN; /admin 404 contract unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| end-user JWT -> get_current_user | Untrusted bearer token crosses here; a banned user's still-valid token must be rejected app-side. |
| non-operator -> governed feature router | require_visible is the API-layer authority (VIS-01); UI hiding is not a control. |
| operator input -> feature_visibility JSONB | feature/audience strings must be code-constant allowlisted before they reach SQL. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-02 | Elevation/Spoofing | get_current_user ban check | mitigate | App-layer `banned_until` fetchrow after token validation; closes the ~1h stateless-JWT window (Pitfall 1). |
| T-148-04 | Denial of Service | _is_banned DB read | mitigate | Fail-OPEN on read error — a DB blip must never lock out every user (maintenance_mode "no self-inflicted outage" polarity). |
| T-148-05 | Elevation | feature_audience cold read | mitigate | _GOVERNED_FEATURES per-feature default = deny for skill_studio/model_management (D-06); DB seed is belt-and-suspenders. |
| T-148-06 | Tampering/Integrity | set_feature_visibility write | mitigate | Atomic JSONB `||` merge per key — no read-modify-write lost-update clobber (Pitfall 4). |
| T-148-03 | Tampering | feature/audience -> SQL | mitigate | Caller allowlists feature/audience against code constants; only `{feature:{"audience":audience}}` is ever serialized (JSONB codec, asyncpg `$N`). |
| T-148-07 | Information Disclosure | require_visible 403 | accept | Deliberate 403-not-404 (D-03): these are known product features; /admin keeps its 404. |
| T-148-SC | Tampering | pip installs | accept | Zero new packages (RESEARCH audit). |
</threat_model>

<verification>
`cd backend && venv/Scripts/python -m pytest tests/test_148_require_visible.py tests/test_148_visibility_cold_default.py tests/test_148_ban_enforcement.py tests/test_148_ban_fail_open.py -x` GREEN. Full suite `pytest -q` stays green (146/147 regression backstop). Migration 098 file present (live apply is 148-03).
</verification>

<success_criteria>
require_visible (403 D-03), feature_audience (D-06 cold-default), set_feature_visibility (atomic merge), and the fail-open ban check exist; the five substrate tests are GREEN; migration 098 written (metadata-only, no CHECK change); the /admin 404 contract is byte-identical.
</success_criteria>

<output>
Create `.planning/phases/148-governance-audit-users-feature-visibility/148-02-SUMMARY.md` when done.
</output>
