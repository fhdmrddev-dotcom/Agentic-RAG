---
phase: 148-governance-audit-users-feature-visibility
plan: 05
type: execute
wave: 3
depends_on: ["148-02"]
files_modified:
  - backend/app/api/features.py
  - backend/app/main.py
  - backend/app/api/evals.py
  - backend/app/api/skill_tuner.py
  - backend/app/api/skill_test_cases.py
  - backend/app/api/settings.py
  - backend/app/api/workflows.py
  - backend/app/api/document_governance.py
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "GET /features returns a per-user effective map: operator -> all four keys True; end user -> True only for Everyone features (workflow_authoring, governance_health day-one); non-operators MUST reach it (authed, NOT operator-gated)"
    - "require_visible('skill_studio') gates evals.py + skill_tuner.py + skill_test_cases.py (all endpoints)"
    - "require_visible('model_management') gates settings.py GET/PUT/reembed — but GET /settings/providers stays ungated (chat model picker Run carve-out)"
    - "require_visible('workflow_authoring') gates workflows.py create/drafts/patch/delete/publish/generate — but GET /published, GET /starters, and the threads.py workflow launch stay ungated (Run stays for everyone — D-05)"
    - "require_visible('governance_health') gates document_governance.py; knowledge_health.py (Library Health) stays ungated (A4)"
    - "Gating is PER-ENDPOINT, never router-level on settings.py/workflows.py (router-gating would 403 the carve-outs — Pitfall 3)"
  artifacts:
    - path: "backend/app/api/features.py"
      provides: "GET /features effective-map endpoint (authed, per-user)"
      exports: ["get_effective_features"]
    - path: "backend/app/main.py"
      provides: "features router mounted alongside the other feature routers (not under /admin)"
      contains: "features"
  key_links:
    - from: "backend/app/api/settings.py"
      to: "require_visible('model_management')"
      via: "per-endpoint Depends, with GET /settings/providers carved out"
      pattern: "require_visible"
    - from: "backend/app/api/features.py"
      to: "feature_audience + is_operator"
      via: "per-user map comprehension over _GOVERNED_FEATURES"
      pattern: "feature_audience"
---

<objective>
Wire VIS-01 API-layer enforcement: the tiny authenticated `GET /features` map endpoint, and a per-endpoint `require_visible(feature)` dependency on each governed router — with the hard Run carve-outs (chat model picker, workflow published/starters + launch) left ungated so end-user chat/run never breaks. This is the security wall; 148-07's frontend only hides + bounces.

Purpose: The API is the authority (Pitfall 13) — a non-operator's call is refused server-side, not merely UI-hidden.
Output: 1 new router + main.py mount + one Depends line per governed endpoint across 6 routers.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md
@.planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md
@backend/app/api/admin.py
@backend/app/api/settings.py
@backend/app/api/workflows.py
</context>

<interfaces>
<!-- Built in 148-02, consumed here — no exploration needed -->
From backend/app/dependencies.py:
  require_visible(feature: str) -> Depends-able async dependency (no-op for operator + Everyone; 403 D-03 otherwise)
  is_operator(user_id: str) -> bool  (existing)
From backend/app/models/user_settings.py:
  _GOVERNED_FEATURES = {"skill_studio":"operators","model_management":"operators","workflow_authoring":"everyone","governance_health":"everyone"}
  feature_audience(feature: str) -> "everyone" | "operators"
Feature keys (exact): skill_studio, model_management, workflow_authoring, governance_health
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /features effective-map endpoint + main.py mount</name>
  <files>backend/app/api/features.py, backend/app/main.py</files>
  <read_first>
    - backend/app/api/admin.py (~:518-531 get_operator_me — a thin authed GET; but /features is gated by get_current_user NOT require_operator)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 5 (verbatim endpoint) + the anti-pattern "require_operator on GET /features is forbidden"
    - backend/app/main.py (where the feature routers are include_router'd — mount /features alongside them, NOT under /admin)
    - backend/tests/test_148_effective_features.py (target)
  </read_first>
  <behavior>
    - GET /features (Depends get_current_user, NOT require_operator): returns {"features": {f: (op or feature_audience(f)=="everyone") for f in _GOVERNED_FEATURES}}.
    - operator -> all four True; end user -> skill_studio False, model_management False, workflow_authoring True, governance_health True (day-one).
    - a non-operator can reach it (200), never 403/404 — they must learn their own map.
  </behavior>
  <action>
    Create `backend/app/api/features.py` with a router carrying `GET /features` per RESEARCH §Pattern 5 — authed via `Depends(get_current_user)`, resolving `is_operator(current_user["id"])` and mapping over `_GOVERNED_FEATURES` via `feature_audience`. Do NOT gate it with require_operator or require_visible (non-operators must reach it). Mount the router in `main.py` via `include_router` alongside the other feature routers (a top-level path, not under `/admin`).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_148_effective_features.py -x` exits 0.
    - features.py uses `Depends(get_current_user)` and NOT require_operator/require_visible (grep confirms neither gate on the route).
    - main.py includes the features router (grep "features" in an include_router call).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_effective_features.py -x</automated>
  </verify>
  <done>GET /features returns the per-user map; mounted top-level; effective-features test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: require_visible on Skill Studio + model_management routers (with the providers carve-out)</name>
  <files>backend/app/api/evals.py, backend/app/api/skill_tuner.py, backend/app/api/skill_test_cases.py, backend/app/api/settings.py</files>
  <read_first>
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 1 wiring table (exact endpoints + the GET /settings/providers carve-out) + Pitfall 3
    - .planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md (governed-routers section — attach per-endpoint alongside the existing Depends(get_current_user), NOT router-level)
    - backend/app/api/settings.py (identify GET "", PUT "", GET /reembed-progress, POST /reembed vs GET /providers)
  </read_first>
  <action>
    Add `Depends(require_visible("skill_studio"))` to EVERY endpoint of `evals.py` (both the `/skills/{id}/evals/*` router and the `/evals/*` router), `skill_tuner.py` (`/skills/{id}/tuner/*`), and `skill_test_cases.py`. Add `Depends(require_visible("model_management"))` to `settings.py` `GET ""`, `PUT ""`, `GET /reembed-progress`, `POST /reembed` — but LEAVE `GET /settings/providers` UNGATED (it feeds the chat model picker `ChatArea.tsx:135 getProviders()`; end users need it — Pitfall 3). Attach per-endpoint as a second dependency alongside each endpoint's existing `Depends(get_current_user)`. Do NOT add require_visible at the settings.py router level (that would 403 the providers carve-out).
  </action>
  <acceptance_criteria>
    - `grep -c "require_visible(\"skill_studio\")" backend/app/api/evals.py backend/app/api/skill_tuner.py backend/app/api/skill_test_cases.py` shows a gate on each router's endpoints.
    - `settings.py`: GET ""/PUT ""/reembed endpoints carry `require_visible("model_management")`; `GET /settings/providers` does NOT (verify the providers route has no require_visible line).
    - No router-level `dependencies=[... require_visible ...]` on settings.py.
    - `pytest tests/test_148_carveouts.py -x` exits 0 (providers stays ungated).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_carveouts.py -x</automated>
  </verify>
  <done>Skill Studio (3 routers) fully gated; model_management gated with the providers carve-out; carve-out test GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: require_visible on workflows (authoring, Run carve-out) + governance_health</name>
  <files>backend/app/api/workflows.py, backend/app/api/document_governance.py</files>
  <read_first>
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 1 wiring table (workflows authoring endpoints vs the GET /published, GET /starters + threads.py:937 launch carve-outs) + A4 (governance_health = document_governance.py; knowledge_health.py stays ungated)
    - backend/app/api/workflows.py (identify POST "" create, GET /drafts, PATCH /{id}, DELETE /{id}, POST /{id}/publish, POST /generate vs GET /published, GET /starters)
    - backend/tests/test_148_carveouts.py (asserts published/starters + launch stay ungated)
  </read_first>
  <action>
    Add `Depends(require_visible("workflow_authoring"))` to `workflows.py` `POST ""` (create draft), `GET /drafts`, `PATCH /{id}`, `DELETE /{id}`, `POST /{id}/publish`, `POST /generate`. LEAVE `GET /published` and `GET /starters` UNGATED (Run picker feeds), and do NOT touch `threads.py` (the workflow launch at `:937` stays for everyone — D-05 "Run stays for everyone"). Add `Depends(require_visible("governance_health"))` to ALL endpoints of `document_governance.py`. Do NOT gate `knowledge_health.py` (Library Health is a sibling read-only surface not in VIS-01's day-one map — A4; it stays Everyone). Attach per-endpoint, never router-level on workflows.py.
  </action>
  <acceptance_criteria>
    - workflows.py authoring endpoints (create/drafts/patch/delete/publish/generate) carry `require_visible("workflow_authoring")`; `GET /published` + `GET /starters` do NOT.
    - `threads.py` is unchanged in this plan (`git diff --stat` shows no threads.py entry) — the launch carve-out is preserved by NOT touching it.
    - document_governance.py endpoints carry `require_visible("governance_health")`; knowledge_health.py is unchanged.
    - `pytest tests/test_148_carveouts.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_carveouts.py -x && git diff --stat backend/app/api/threads.py | grep -q . && echo "THREADS TOUCHED (FAIL)" || echo "threads.py untouched OK"</automated>
  </verify>
  <done>Workflow authoring gated with the Run carve-outs intact (threads.py untouched); governance_health gated; carve-out test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| non-operator -> governed feature endpoint | require_visible is the API-layer VIS-01 authority; the frontend hide (148-07) is not a control. |
| end user -> Run carve-outs | Chat model picker + workflow published/starters/launch MUST stay reachable — over-gating is a self-inflicted end-user outage. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-07 | Information Disclosure | require_visible 403 | accept | Deliberate 403-not-404 (D-03): governed features are known products; /admin keeps its 404. |
| T-148-08 | Denial of Service | Run carve-out over-gating | mitigate | Per-endpoint gating only; GET /settings/providers, GET /workflows/published|starters, and threads.py launch left ungated (Pitfall 3); test_148_carveouts is the guard. |
| T-148-05 | Elevation | GET /features / feature_audience | mitigate | Map derives from the D-06 cold-default resolver (148-02); tightened features never resolve to allow on a blip. |
| T-148-SC | Tampering | pip installs | accept | Zero new packages. |
</threat_model>

<verification>
`cd backend && venv/Scripts/python -m pytest tests/test_148_effective_features.py tests/test_148_carveouts.py -x` GREEN. Full `pytest -q` stays green (no governed-router regressions). `git diff --stat` shows threads.py untouched.
</verification>

<success_criteria>
GET /features returns the per-user map; require_visible gates every governed endpoint with the three Run carve-outs intact; effective-features + carve-out tests GREEN; threads.py untouched.
</success_criteria>

<output>
Create `.planning/phases/148-governance-audit-users-feature-visibility/148-05-SUMMARY.md` when done.
</output>
