---
phase: 148-governance-audit-users-feature-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_148_require_visible.py
  - backend/tests/test_148_effective_features.py
  - backend/tests/test_148_visibility_cold_default.py
  - backend/tests/test_148_carveouts.py
  - backend/tests/test_148_platform_audit_filters.py
  - backend/tests/test_148_platform_audit_scope.py
  - backend/tests/test_148_csv_export.py
  - backend/tests/test_148_view_platform_recorded.py
  - backend/tests/test_148_roster.py
  - backend/tests/test_148_disable.py
  - backend/tests/test_148_ban_enforcement.py
  - backend/tests/test_148_ban_fail_open.py
  - backend/tests/test_148_enable.py
  - backend/tests/test_148_operator_grant.py
  - backend/tests/conftest.py
autonomous: true
requirements: [ADMIN-03, VIS-01]

must_haves:
  truths:
    - "Every 148-VALIDATION.md Per-Task entry has a matching pytest node that FAILS (RED) before implementation exists"
    - "pytest can COLLECT all test_148_*.py files (imports guarded inside test bodies so a missing target does not break collection)"
    - "conftest carries a banned_user asyncpg-pool fixture and a feature_visibility app_settings-row fixture (Nyquist Wave 0)"
  artifacts:
    - path: "backend/tests/test_148_require_visible.py"
      provides: "VIS-01 403-not-404 + operator no-op + everyone no-op stubs"
      contains: "test_non_operator_403"
    - path: "backend/tests/test_148_ban_enforcement.py"
      provides: "app-layer ban enforcement stub (disabled live token -> 403)"
      contains: "banned"
    - path: "backend/tests/conftest.py"
      provides: "banned_user + feature_visibility fixtures"
      contains: "feature_visibility"
  key_links:
    - from: "backend/tests/test_148_*.py"
      to: "148-VALIDATION.md Per-Task Verification Map"
      via: "one pytest node per validation row"
      pattern: "def test_"
---

<objective>
Create the Nyquist Wave 0 test scaffold for Phase 148 — one failing (RED) pytest node per row of `148-VALIDATION.md`, plus the two shared fixtures every downstream backend plan depends on. This plan writes NO production code; it encodes the acceptance contract so every later task has a green target.

Purpose: Nyquist compliance — tests exist before implementation. Every backend plan (148-02, 148-04, 148-06) turns these RED stubs GREEN.
Output: 14 `test_148_*.py` files + extended `conftest.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/148-governance-audit-users-feature-visibility/148-VALIDATION.md
@.planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md
@backend/tests/conftest.py
@backend/pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: VIS-01 test stubs (require_visible, effective-features, cold-default, carve-outs)</name>
  <files>backend/tests/test_148_require_visible.py, backend/tests/test_148_effective_features.py, backend/tests/test_148_visibility_cold_default.py, backend/tests/test_148_carveouts.py</files>
  <read_first>
    - backend/tests/conftest.py (existing fixtures: operator_override, mock_asyncpg_pool, _supabase, authenticate_operator_request, client, auth_headers)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-VALIDATION.md (rows: require_visible 403/operator-noop/everyone-noop, cold-read default, carve-outs, effective-features)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 1 + §Pattern 5 + the Run carve-out table
  </read_first>
  <action>
    Create 4 test files. Each test imports its target INSIDE the test body (e.g. `from app.dependencies import require_visible`) so `pytest --collect-only` succeeds even though 148-02 has not built the target yet — the test then FAILS at runtime (RED), not at collection.
    - `test_148_require_visible.py`: `test_non_operator_403` (non-operator hitting an Operators-only governed feature -> HTTP 403 with body "This feature is available to administrators only." — assert status 403 AND detail text, per D-03, NOT 404), `test_operator_noop` (operator -> dependency returns/None pass-through), `test_everyone_noop` (feature whose audience resolves "everyone" -> no-op for a non-operator).
    - `test_148_effective_features.py`: `GET /features` for an operator returns all four keys True; for an end user returns True only for `workflow_authoring` + `governance_health`, False for `skill_studio` + `model_management` (per D-05 day-one map).
    - `test_148_visibility_cold_default.py`: with the settings cache cold / DB unreachable, `feature_audience("skill_studio")` and `feature_audience("model_management")` resolve to "operators" (deny), `feature_audience("workflow_authoring")` and `feature_audience("governance_health")` resolve to "everyone" (allow) — D-06 per-feature polarity.
    - `test_148_carveouts.py`: assert `GET /settings/providers`, `GET /workflows/published`, `GET /workflows/starters`, and the `threads.py` workflow-launch path carry NO require_visible dependency (introspect the route dependencies or hit them as a non-operator and expect NOT 403).
    Use exact feature keys `skill_studio`, `model_management`, `workflow_authoring`, `governance_health`. Do NOT invent helper production code — stubs assert against the real target names 148-02/148-04/148-05/148-06 will create.
  </action>
  <acceptance_criteria>
    - `pytest backend/tests/test_148_require_visible.py backend/tests/test_148_effective_features.py backend/tests/test_148_visibility_cold_default.py backend/tests/test_148_carveouts.py --collect-only -q` lists ≥ 6 test node IDs and exits 0 (collection succeeds).
    - Running those tests now exits non-zero (RED — targets unbuilt), never "0 collected".
    - `test_non_operator_403` asserts BOTH `status_code == 403` and the exact plain-body string (guards against a 404 regression).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_require_visible.py tests/test_148_effective_features.py tests/test_148_visibility_cold_default.py tests/test_148_carveouts.py --collect-only -q</automated>
  </verify>
  <done>Four VIS-01 stub files exist; collection succeeds; tests are RED.</done>
</task>

<task type="auto">
  <name>Task 2: Audit-browse test stubs (filters, scope, CSV cap, view_platform)</name>
  <files>backend/tests/test_148_platform_audit_filters.py, backend/tests/test_148_platform_audit_scope.py, backend/tests/test_148_csv_export.py, backend/tests/test_148_view_platform_recorded.py</files>
  <read_first>
    - backend/tests/conftest.py (mock_asyncpg_pool, operator_override)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 3 (parameterized/scoped/paginated SQL) + §Code Examples "_ACTIONS"
    - .planning/phases/148-governance-audit-users-feature-visibility/148-VALIDATION.md (audit-browse rows)
  </read_first>
  <action>
    Create 4 test files asserting ADMIN-03 audit-browse behavior against `services/governance_service.py` + the `admin.py` platform endpoints (built later):
    - `test_148_platform_audit_filters.py`: an `action_type` array filter + a half-open date window pass through to SQL params and only matching rows return (drive via mock_asyncpg_pool capturing the query args — assert `action_type = ANY($N)` and the `created_at >= $N AND created_at < $N` params are present).
    - `test_148_platform_audit_scope.py`: the browse enforces pagination (page_size le=100 — a request for page_size 5000 is clamped/rejected) AND a NULL user_id param means "all users" while a value means single-user scope (no unbounded `SELECT *`). This is the T-148-01 no-full-tenant-leak guard.
    - `test_148_csv_export.py`: export streams EXACTLY the filtered set; when the count exceeds the cap (~50000) the endpoint REFUSES (413/422 "narrow the filter") instead of truncating; a successful export records `audit.export` with the exact row count in the label; a REFUSED export records NO audit.export row.
    - `test_148_view_platform_recorded.py`: hitting the platform-source browse records an `audit.view_platform` operator-ledger row (floor with audit_is_write=False).
    Guard imports inside test bodies. Use asyncpg-mock capture, not a live DB.
  </action>
  <acceptance_criteria>
    - `pytest backend/tests/test_148_platform_audit_filters.py backend/tests/test_148_platform_audit_scope.py backend/tests/test_148_csv_export.py backend/tests/test_148_view_platform_recorded.py --collect-only -q` exits 0.
    - `test_148_csv_export.py` contains both an over-cap-refuse assertion and a "refused export writes no audit.export" assertion.
    - Tests are RED (targets unbuilt).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_platform_audit_filters.py tests/test_148_platform_audit_scope.py tests/test_148_csv_export.py tests/test_148_view_platform_recorded.py --collect-only -q</automated>
  </verify>
  <done>Four audit-browse stub files exist; collection succeeds; tests are RED.</done>
</task>

<task type="auto">
  <name>Task 3: Users test stubs + conftest fixtures (roster, disable/enable, ban, grant)</name>
  <files>backend/tests/test_148_roster.py, backend/tests/test_148_disable.py, backend/tests/test_148_ban_enforcement.py, backend/tests/test_148_ban_fail_open.py, backend/tests/test_148_enable.py, backend/tests/test_148_operator_grant.py, backend/tests/conftest.py</files>
  <read_first>
    - backend/tests/conftest.py (extend, do not rewrite — reuse mock_asyncpg_pool + _supabase GoTrue-admin mock)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 2 (ban + app-layer check, fail-open) + §Code Examples "Roster read" + "Grant / revoke operator"
    - .planning/phases/148-governance-audit-users-feature-visibility/148-VALIDATION.md (users rows) + Wave 0 Requirements
  </read_first>
  <action>
    Create 6 users test files + extend conftest.
    - `test_148_roster.py::test_last_active_honesty`: a user with `last_sign_in_at` NULL surfaces as "never signed in" (never fabricated); a recent value surfaces as recent — assert the roster serializer preserves NULL, does not backfill.
    - `test_148_disable.py`: disable calls GoTrue admin `update_user_by_id(uid, {"ban_duration": "876600h"})` AND cancels the victim's in-flight runs via `run_lifecycle._cancel_run_internals` (assert the mock kill helper is called per active run) AND records `user.disable`.
    - `test_148_ban_enforcement.py`: a user whose `auth.users.banned_until` is in the future gets 403 "This account is disabled — contact your administrator." from `get_current_user` on ANY authed route, even with a still-valid token (closes the JWT window).
    - `test_148_ban_fail_open.py`: when the ban-check DB read raises, `get_current_user` does NOT lock the user out (fail-OPEN) — the request proceeds (T-148-04).
    - `test_148_enable.py`: enable calls GoTrue admin with `{"ban_duration": "none"}` and records `user.enable`.
    - `test_148_operator_grant.py`: grant INSERTs into `operator_users` populating `granted_by` = acting operator; self-revoke (DELETE own row) and self-disable are REFUSED server-side (409) BEFORE any mutation (T-148-04 lockout-proof).
    - conftest: add a `banned_user` fixture (mock_asyncpg_pool returns a future `banned_until` for a target id) and a `feature_visibility` fixture (an app_settings row carrying `{"skill_studio":{"audience":"operators"}, ...}`). Reuse existing fixture wiring; append only.
    Guard imports inside test bodies. Ban-duration literal must be exactly `"876600h"` / `"none"`.
  </action>
  <acceptance_criteria>
    - `pytest backend/tests/test_148_roster.py backend/tests/test_148_disable.py backend/tests/test_148_ban_enforcement.py backend/tests/test_148_ban_fail_open.py backend/tests/test_148_enable.py backend/tests/test_148_operator_grant.py --collect-only -q` exits 0.
    - `conftest.py` defines `banned_user` and `feature_visibility` fixtures (grep-visible) and the existing fixtures still import (`pytest --collect-only -q` on the whole suite exits 0).
    - `test_148_disable.py` asserts the ban literal `"876600h"` AND a `_cancel_run_internals` call; `test_148_ban_fail_open.py` asserts NO lockout on DB error.
    - Tests are RED (targets unbuilt).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_roster.py tests/test_148_disable.py tests/test_148_ban_enforcement.py tests/test_148_ban_fail_open.py tests/test_148_enable.py tests/test_148_operator_grant.py --collect-only -q && venv/Scripts/python -m pytest --collect-only -q 2>&1 | tail -3</automated>
  </verify>
  <done>Six users stub files + two new conftest fixtures exist; whole-suite collection succeeds; the new tests are RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| n/a (test-only plan) | This plan writes no production code and no request handlers; it introduces no new trust boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-SC | Tampering | pip installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit — no install task). pytest already present. |
| T-148-TEST | Repudiation | test scaffold fidelity | mitigate | Every 148-VALIDATION row maps 1:1 to a RED pytest node; the security-critical tests (403-not-404, no-full-tenant-leak, ban-window, fail-open, self-lockout, CSV-cap) are authored here so later plans cannot silently skip them. |
</threat_model>

<verification>
Run `cd backend && venv/Scripts/python -m pytest tests/test_148_*.py --collect-only -q` — all 14 files collect, ≥ 16 test nodes present, whole-suite collection unbroken. Tests are RED (expected until 148-02/04/06 implement).
</verification>

<success_criteria>
14 `test_148_*.py` files + extended `conftest.py` exist; every 148-VALIDATION Per-Task row has a matching pytest node; collection succeeds suite-wide; no production code written.
</success_criteria>

<output>
Create `.planning/phases/148-governance-audit-users-feature-visibility/148-01-SUMMARY.md` when done.
</output>
