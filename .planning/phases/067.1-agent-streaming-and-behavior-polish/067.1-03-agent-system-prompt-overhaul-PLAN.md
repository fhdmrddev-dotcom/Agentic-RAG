---
phase: 067.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/threads.py
autonomous: true
requirements: [STREAM-04-polish]

must_haves:
  truths:
    - "SC#3: When a user submits a clear multi-step intent (e.g. 'search for X → write a report → make charts → make a docx'), the agent executes the FULL pipeline end-to-end without intermediate 'If you want, I can also…' / 'Shall I proceed to…' / 'Let me know if you'd like me to…' confirmation prompts."
    - "SC#3 guardrail: When the user submits a genuinely ambiguous prompt (e.g. 'make me a report' without specifying topic, document, or format), the agent STILL asks for clarification — the change is autonomy-default, NOT autonomy-always."
    - "SC#3 destructive-action guardrail: The new section explicitly preserves confirmation for irreversible/destructive actions (V11 business-logic mitigation per VALIDATION.md security domain)."
    - "Existing `## Rules` block (threads.py:214-237) is preserved verbatim — including the load-bearing 'Tool call brevity' rule at lines 226-227."
    - "Existing `## Confidence & hedging` section (threads.py:239-243) is preserved verbatim — the new section inserts BEFORE it."
    - "The system prompt continues to be a single immutable module-level constant with concatenated string literals (no f-strings, no runtime substitution) — pattern preserved per PATTERNS.md."
  artifacts:
    - path: "backend/app/api/threads.py"
      provides: "New `## Multi-step intent` section inserted BEFORE the existing `## Confidence & hedging` header at line 239. Preserves the `## Rules` block (lines 214-237) verbatim. Includes explicit `EXCEPTIONS` sub-section for ambiguous / missing-information / irreversible-destructive cases."
      preserves: "Module-level constant SYSTEM_PROMPT shape (line 156-257); Python implicit string concatenation pattern; markdown `## Section\\n` headers; `\\n\\n` double-newline separators between sections; the load-bearing 'Tool call brevity' rule at line 226-227."
  key_links:
    - from: "backend/app/api/threads.py SYSTEM_PROMPT constant (line 156-257)"
      to: "threads.py:1027 messages list construction (`{\"role\": \"system\", \"content\": active_system_prompt}`)"
      via: "active_system_prompt = SYSTEM_PROMPT (or user_settings override) — sent on every chat completion call (CLAUDE.md stateless completions)"
      pattern: "active_system_prompt"
    - from: "Multi-step intent recognition section (new)"
      to: "Agent behavior on Fahed-Mrad-style multi-tool prompts"
      via: "Static prompt instruction — propagates automatically to OpenAI / Anthropic / OpenRouter / Google completions"
      pattern: "## Multi-step intent"
---

<objective>
Surgically extend `backend/app/api/threads.py` SYSTEM_PROMPT (line 156-257) with a new `## Multi-step intent` section addressing CONTEXT.md's failure mode: gpt-5.4 (and likely other models) defer multi-step pipelines to user confirmation. From the 2026-05-07 deep UAT Run 1 evidence, the user submitted "search for research authored by Fahed Mrad → professional short report → charts/diagrams → docx" intent; the agent did search + analyze, then offered "If you want, I can also turn these into a one-page thematic map or a chapter-by-chapter theme matrix" instead of executing the full pipeline.

Purpose: Tune the system prompt to instruct the agent: when the user provides a clear multi-step intent (e.g. "search → analyze → chart → docx"), execute the FULL pipeline without intermediate confirmation. Use POSITIVE framing ("Execute the FULL pipeline") not negative ("Don't ask") per Anthropic's published prompt-engineering guidance. Preserve safety for genuinely-ambiguous and irreversible/destructive paths via an explicit `EXCEPTIONS` sub-section.

Output: A single-section insertion BEFORE the existing `## Confidence & hedging` header at line 239, written in the same Python implicit-string-concatenation shape as the rest of the constant. No changes to messages-list construction at threads.py:1027 (the constant flows through automatically). No `tool_choice` tuning (RESEARCH.md: keep "auto"; system-prompt fix is the right surface). No `user_settings` toggle (RESEARCH.md anti-pattern: premature configurability).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-CONTEXT.md
@.planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-RESEARCH.md
@.planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-PATTERNS.md
@.planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing SYSTEM_PROMPT structure from threads.py:156-257. Key boundary lines: -->

Line 156: `SYSTEM_PROMPT = (`
Lines 214-237: `## Rules` block (PRESERVE VERBATIM — includes "Tool call brevity" load-bearing rule at lines 226-227)
Line 237: closing rule `"If the task requires more than 200 lines, split into multiple execute_code calls.\n\n"`
Line 239: existing `"## Confidence & hedging\n"` — INSERTION POINT IS THE LINE BEFORE THIS
Line 243: closing of confidence section `"... Do not fabricate an answer from weak matches.\n\n"`
Line 245-249: `## Citation format` section
Line 251-256: `## execute_code output` section
Line 257: `)` — closes the constant

The pattern shape is Python implicit string concatenation:
```python
SYSTEM_PROMPT = (
    "First section content\n\n"
    "## Section name\n"
    "Body line 1...\n"
    "Body line 2...\n\n"

    "## Next section\n"
    ...
)
```

Each section ends with `\\n\\n` double-newline before the next section's `##` header. No f-strings, no runtime values, no runtime composition. Comments/annotations are NEVER embedded in the string (they would ship to the LLM).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert `## Multi-step intent` section in SYSTEM_PROMPT before `## Confidence & hedging` (line 239)</name>
  <files>backend/app/api/threads.py</files>
  <read_first>
    - backend/app/api/threads.py lines 156-257 (full SYSTEM_PROMPT constant — pattern source)
    - .planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-CONTEXT.md § Plan 03 (Failure mode + Approach + Acceptance)
    - .planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-RESEARCH.md § Pattern 3 (Recommended SYSTEM_PROMPT insertion — verbatim text below)
    - .planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-PATTERNS.md § threads.py SYSTEM_PROMPT (line 156-257)
    - .planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-VALIDATION.md § Per-Task Verification Map (T-067.1-03 EXCEPTIONS sub-section requirement)
  </read_first>
  <behavior>
    - SYSTEM_PROMPT contains the literal substring `## Multi-step intent`.
    - SYSTEM_PROMPT contains the literal substring `Execute the FULL pipeline` (positive framing per Anthropic guidance).
    - SYSTEM_PROMPT contains the literal substring `EXCEPTIONS` (the destructive-action guardrail sub-section).
    - SYSTEM_PROMPT contains the literal substring `irreversible` or `destructive` (V11 mitigation marker).
    - SYSTEM_PROMPT contains the literal substring `genuinely ambiguous` (clarification gate preserved).
    - The new section is positioned BEFORE the existing `## Confidence & hedging` header.
    - The existing `## Rules` block (lines 214-237) is preserved character-by-character — including the load-bearing "Tool call brevity" rule at lines 226-227.
    - The existing `## Confidence & hedging` section is preserved verbatim.
    - SYSTEM_PROMPT remains a single immutable module-level constant — no f-string conversion, no runtime values introduced.
  </behavior>
  <action>
    Insert the following section into the SYSTEM_PROMPT constant in `backend/app/api/threads.py`. The insertion goes AFTER the closing of the existing `## Rules` block (line 237 — the line ending `"If the task requires more than 200 lines, split into multiple execute_code calls.\n\n"`) and BEFORE the existing `## Confidence & hedging` header (line 239 — `"## Confidence & hedging\n"`).

    Concretely, INSERT these lines verbatim between current line 237 and current line 239 (preserve the blank line separator and the pattern of one blank line between sections):

    ```python
        "## Multi-step intent\n"
        "When the user requests a multi-step pipeline — for example:\n"
        "- 'search for X and write a report'\n"
        "- 'find Y, analyze it, then make a chart and a docx'\n"
        "- 'research Z and produce a one-pager'\n"
        "Execute the FULL pipeline end-to-end. The user named the deliverables; deliver them. "
        "After each tool call, continue to the next step in the user's named sequence rather than "
        "asking 'If you want, I can also...', 'Shall I proceed to...', or 'Let me know if you'd "
        "like me to...'. When the pipeline completes, summarize what you produced in your final "
        "text response.\n\n"

        "EXCEPTIONS (still ask for clarification):\n"
        "- The intent is genuinely ambiguous (e.g. 'make me a report' — about what? from which "
        "documents? what format?).\n"
        "- The next step requires information the user did not provide and you cannot infer "
        "from the documents (e.g. specific names, date ranges, file format preferences when "
        "the document corpus has many).\n"
        "- The action would be irreversible or destructive in a way the user might not have "
        "intended (e.g. overwriting / deleting existing artifacts when an alternative path "
        "exists).\n\n"
    ```

    PRESERVE VERBATIM (do NOT modify, reorder, or delete):
    - Lines 156-213 (everything BEFORE the `## Rules` block).
    - Lines 214-237 (the entire `## Rules` block — every rule, including the load-bearing "Tool call brevity" at lines 226-227, the "Never call execute_code in same response" rule at lines 232-234, and the "Keep execute_code scripts under 200 lines" rule at lines 235-237).
    - Lines 239-257 (the `## Confidence & hedging`, `## Citation format`, and `## execute_code output` sections).
    - The closing `)` at line 257.

    Style invariants (PATTERNS.md):
    - Use Python implicit string concatenation: each line is a separate `"..."` string literal with `\n` for newlines and `\n\n` to separate paragraphs/sections.
    - DO NOT use f-strings — every line must be a plain string literal.
    - DO NOT introduce runtime variables or function calls inside the constant.
    - DO NOT add inline comments or `# ...` lines inside the parenthesized expression — they break the pattern and (more importantly) would be invisible to the LLM, which is the point.
    - Section header is `"## Multi-step intent\n"` — single-newline after the header (matches existing pattern at line 214 `"## Rules\n"`).
    - Section ends with `\n\n` double-newline (matches existing pattern at line 213 closing the previous section, line 237 closing `## Rules`).
    - Token budget consideration (RESEARCH § Pitfall 3): the inserted section is ~150-180 tokens. Tighten if you find redundant phrasing during write — keep the canonical phrases ("Execute the FULL pipeline", "If you want, I can also...", "Shall I proceed to...", "Let me know if you'd like me to...", "EXCEPTIONS", "genuinely ambiguous", "irreversible or destructive") since these are the SC#3 acceptance markers.

    DO NOT change the `tool_choice` parameter on chat completions (RESEARCH.md: "Do not change `tool_choice` settings; current 'auto' mode is correct"). DO NOT introduce a `user_settings` toggle (RESEARCH.md anti-pattern: premature configurability). The change is hard-coded by design.

    Verification fast path: after writing, confirm the file still imports cleanly:
    ```
    cd backend && venv/Scripts/python.exe -c "from app.api.threads import SYSTEM_PROMPT; print('len=', len(SYSTEM_PROMPT)); assert '## Multi-step intent' in SYSTEM_PROMPT; assert 'EXCEPTIONS' in SYSTEM_PROMPT; assert '## Rules' in SYSTEM_PROMPT; assert '## Confidence & hedging' in SYSTEM_PROMPT; print('OK')"
    ```
  </action>
  <acceptance_criteria>
    - Grep `-c '## Multi-step intent' backend/app/api/threads.py` returns ≥1 match.
    - Grep `-c 'Execute the FULL pipeline' backend/app/api/threads.py` returns ≥1 match.
    - Grep `-c 'EXCEPTIONS' backend/app/api/threads.py` returns ≥1 match.
    - Grep `-c 'genuinely ambiguous' backend/app/api/threads.py` returns ≥1 match.
    - Grep `-cE 'irreversible or destructive|irreversible / destructive' backend/app/api/threads.py` returns ≥1 match.
    - Grep `-c "If you want, I can also" backend/app/api/threads.py` returns ≥1 match (the literal phrase the agent must NOT emit, present in the prompt as an explicit anti-example).
    - Grep `-c '## Rules' backend/app/api/threads.py` returns ≥1 match (existing block preserved).
    - Grep `-c '## Confidence & hedging' backend/app/api/threads.py` returns ≥1 match (existing section preserved and downstream of the new section).
    - Grep `-c 'Tool call brevity' backend/app/api/threads.py` returns ≥1 match (load-bearing rule preserved).
    - Grep `-c 'Never call execute_code in the same response' backend/app/api/threads.py` returns ≥1 match (existing rule preserved).
    - Module imports cleanly: `cd backend && venv/Scripts/python.exe -c "from app.api.threads import SYSTEM_PROMPT; assert '## Multi-step intent' in SYSTEM_PROMPT and 'EXCEPTIONS' in SYSTEM_PROMPT and '## Rules' in SYSTEM_PROMPT and '## Confidence' in SYSTEM_PROMPT and '## Citation format' in SYSTEM_PROMPT; print('OK')"` exits 0 with `OK` printed. (Note: the Confidence-section assertion uses prefix-only `'## Confidence'` rather than `'## Confidence & hedging'` to avoid HTML-entity ambiguity inside the <automated> verify block — see <verify> command rationale below.)
    - Section ordering check: in the SYSTEM_PROMPT string, `## Rules` appears before `## Multi-step intent`, which appears before `## Confidence` (and `## Citation format` further downstream — used as an unambiguous post-Confidence anchor that contains no `&` character). Verify via:
      `cd backend && venv/Scripts/python.exe -c "from app.api.threads import SYSTEM_PROMPT as s; assert s.index('## Rules') < s.index('## Multi-step intent') < s.index('## Confidence') < s.index('## Citation format'); print('order OK')"`.
    - Rationale (revision 2026-05-07): the <verify> automated command intentionally avoids the literal `&` character inside Python string assertions because XML `<automated>` content is not entity-decoded in this project's convention (no other PLAN.md files use `&amp;` inside Python strings — confirmed via Grep across `.planning/phases/`). Asserting on `'## Confidence'` (prefix) plus `'## Citation format'` (next downstream section) provides equivalent ordering coverage without the entity hazard. The Grep-based assertions above (which run via Bash, where literal `&` is unambiguous in single-quoted strings) continue to use the full `## Confidence & hedging` literal for completeness.
    - No f-string conversion: Grep `-c 'f"' backend/app/api/threads.py | head -1` count not increased over baseline (no new f-strings introduced inside SYSTEM_PROMPT). Spot-check the inserted lines visually — every inserted string starts with plain `"`.
    - No `## Multi-step intent` content embedded in any other constant or function — Grep `-c '## Multi-step intent' backend/` returns 1 (or however many match the literal once across the codebase, but only inside threads.py).
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -c "from app.api.threads import SYSTEM_PROMPT as s; assert '## Multi-step intent' in s and 'EXCEPTIONS' in s and 'Execute the FULL pipeline' in s and 'genuinely ambiguous' in s and 'irreversible' in s and '## Rules' in s and '## Confidence' in s and '## Citation format' in s and 'Tool call brevity' in s and s.index('## Rules') &lt; s.index('## Multi-step intent') &lt; s.index('## Confidence') &lt; s.index('## Citation format'); print('OK')"</automated>
  </verify>
  <done>
    SYSTEM_PROMPT now contains the new `## Multi-step intent` section between `## Rules` and `## Confidence & hedging` with all required substrings; module imports cleanly; ordering verified; existing rules preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User intent → agent autonomy decision | The system prompt is the canonical surface for autonomy-default-vs-clarification gating. Static prompt is sent every iteration (CLAUDE.md "stateless chat completions"). |
| LLM provider → tool execution | Existing tool gating via `Rules` section (e.g. "Never call execute_code in the same response as search_documents") — preserved verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067.1-03 | Elevation of privilege (V11 — business logic guardrail; agent autonomy outpacing user intent on irreversible actions) | SYSTEM_PROMPT new `## Multi-step intent` section | mitigate | The `EXCEPTIONS` sub-section explicitly preserves clarification for "irreversible or destructive" actions. The current toolbelt has no destructive tools (no delete, no overwrite-without-confirm). The clause is forward-looking — if a destructive tool is added later, the prompt-level guard is in place. |
| T-067.1-07 | Information disclosure (excessive autonomy on ambiguous prompts producing unwanted outputs) | SYSTEM_PROMPT — agent behavior on under-specified prompts | mitigate | The `EXCEPTIONS` sub-section preserves clarification for "genuinely ambiguous" prompts (e.g. 'make me a report' without topic/document/format). Plan 05 includes a regression check (1 ambiguous-prompt run alongside 5 multi-step runs). |
| T-067.1-08 | Repudiation (system prompt drift over time) | SYSTEM_PROMPT module-level constant | mitigate | Single immutable constant with explicit section markers (`## Multi-step intent`); future PR diffs are easy to audit. The new section uses positive framing per Anthropic guidance, so future tuning is additive (add to EXCEPTIONS list) rather than re-flipping autonomy default. |
</threat_model>

<verification>
- Module-import gate: `cd backend && venv/Scripts/python.exe -c "from app.api.threads import SYSTEM_PROMPT; ..."` exits 0 (already in <verify> automated command for Task 1).
- Existing-rules preservation: Grep counts for "Tool call brevity", "Never call execute_code in the same response", "Keep execute_code scripts under 200 lines", "Always cite which document", "Never call the same tool twice" each return ≥1 — all load-bearing rules from the existing `## Rules` block preserved.
- Section-order invariant: `## Rules` < `## Multi-step intent` < `## Confidence & hedging` (verified via `s.index(...)` triple comparison in <verify> command).
- No `tool_choice` change: Grep `-n 'tool_choice' backend/app/api/threads.py` count unchanged from baseline (Plan 03 does not modify tool_choice handling — RESEARCH § Pattern 3).
- No `user_settings` toggle introduced: Grep for new `user_settings.agentic_mode` or similar field returns 0 matches (RESEARCH § Anti-Patterns: premature configurability avoided).
- Plan 05 will provide runtime verification via Chrome MCP: 5 consecutive Fahed-Mrad runs producing all 4 deliverables with zero "If you want…" / "Shall I…" hedges, plus 1 ambiguous-prompt regression run that DOES produce a clarification.
</verification>

<success_criteria>
- ROADMAP SC#3 (multi-step pipelines execute without intermediate confirmation): The architectural prompt change required is in place. Plan 05 will validate behaviorally.
- ROADMAP SC#3 guardrail (genuinely-ambiguous prompts still get clarification): The `EXCEPTIONS` sub-section preserves this path. Plan 05 will validate via 1 ambiguous-prompt regression run.
- Pattern preservation: SYSTEM_PROMPT remains a single Python implicit-string-concat constant; existing `## Rules` block unchanged; existing `## Confidence & hedging` and downstream sections unchanged.
- Token budget (RESEARCH § Pitfall 3): inserted section ≤200 tokens; sent on every iteration via stateless chat completions per CLAUDE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/067.1-agent-streaming-and-behavior-polish/067.1-03-SUMMARY.md` documenting:
- Exact line numbers where the new section was inserted (post-insertion).
- Token count of the inserted block (approximate via `len(inserted_text.split())`).
- Confirmation that all PATTERNS.md preservation invariants were honored.
- Note that runtime verification of SC#3 is owned by Plan 05 Task 2 (5 Fahed-Mrad runs) + Plan 05 Task 6 (1 ambiguous-prompt regression).
</output>
