---
phase: 067.1
plan: 02
subsystem: chat-streaming-ui
tags: [frontend, ux-polish, streaming, copy, toolMeta, MessageItem]
requires:
  - "ToolCall type from frontend/src/types/index.ts"
  - "Existing toolMeta.ts module with toolLabel/toolSummary/taskPhaseLabel exports"
  - "Already-emitted SSE events: iteration_start, tool_preparing, tool_start, code_execution_start, skill_activated"
provides:
  - "frontend/src/lib/toolMeta.ts:outerBannerLabel — pure function deriving outer-banner copy from in-flight state"
  - "MessageItem.tsx outer-banner copy now context-aware across 5 latency phases"
affects:
  - "User-perceived chat streaming feel during pre-first-delta, tool-preparing, inter-iteration, and synthesis windows"
  - "ROADMAP SC#2 (context-aware in-flight copy) — frontend deliverable shipped"
tech-stack:
  added: []
  patterns:
    - "Pure-function sibling export in toolMeta.ts following toolLabel/taskPhaseLabel shape (if-chain + default fallback + per-function docblock)"
    - "Pre-computed activeTool ternary branching matching ToolCallPanel.tsx:540 (running OR preparing)"
key-files:
  created: []
  modified:
    - "frontend/src/lib/toolMeta.ts"
    - "frontend/src/components/chat/MessageItem.tsx"
decisions:
  - "Plan 02 derives outer-banner copy from already-emitted SSE state — no new backend events; Plan 04 separately extends api.ts for the skill_loaded follow-up event."
  - "activeTool detection extended from status==='running' only to status==='running' || status==='preparing' — required so outerBannerLabel can render 'Preparing code…' during the ~2s sandbox warmup before tool_start fires."
metrics:
  duration: "~10 min"
  completed: "2026-05-07"
  tasks: 2
  files: 2
  loc-added: 45
  loc-removed: 4
---

# Phase 067.1 Plan 02: Context-Aware In-Flight Copy Summary

Replaced generic "Thinking" / "Working" / "Synthesizing answer" placeholder copy in `MessageItem.tsx` with five context-aware variants ("Setting up agent…", "Thinking…", "Searching knowledge base…", "Preparing code…" / "Running code…", `Loading skill "X"…`) derived from already-emitted SSE state via a new `outerBannerLabel` sibling export in `toolMeta.ts`.

## Lines Changed

**`frontend/src/lib/toolMeta.ts`** (+36 lines):
- Added `import type { ToolCall } from "@/types"` at module top (line 1).
- Added new `outerBannerLabel(activeTool, hasAnyTools, isPlanning): string` export at end of file (lines 55-90) following the same shape as existing `toolLabel` / `taskPhaseLabel` (if-chain, default fallback, per-function docblock).
- Existing `toolLabel`, `toolSummary`, `taskPhaseLabel` exports UNCHANGED.

**`frontend/src/components/chat/MessageItem.tsx`** (+9 / −4 lines):
- Line 10: extended import to include `outerBannerLabel` alongside existing `toolLabel`, `toolSummary`.
- Lines 41-46: extended `activeTool` to detect both `"running"` AND `"preparing"` statuses (was `"running"`-only); changed return type from `ToolCall | undefined` to `ToolCall | null` via `?? null` — works with downstream truthy checks at line 49.
- Line 126: replaced literal `<span className="italic">Thinking</span>` with `<span className="italic">{outerBannerLabel(null, false, message.isPlanning ?? false)}</span>`.
- Line 140: replaced chained ternary `(allToolsDone ? "Synthesizing answer" : "Working")` with `outerBannerLabel(activeTool, hasAnyTools, message.isPlanning ?? false)`.

## Behavior Cases Covered (11 total — Task 1 docstring)

| # | Input                                                     | Output                       |
|---|-----------------------------------------------------------|------------------------------|
| 1 | `(null, false, false)`                                    | `"Setting up agent…"`        |
| 2 | `(null, false, true)`                                     | `"Thinking…"`                |
| 3 | `(null, true, false)`                                     | `"Synthesizing answer…"`     |
| 4 | `({name:"search_documents",...}, true, false)`            | `"Searching knowledge base…"`|
| 5 | `({name:"query_documents",...}, true, false)`             | `"Querying document metadata…"`|
| 6 | `({name:"web_search",...}, true, false)`                  | `"Searching the web…"`       |
| 7 | `({name:"analyze_document",...}, true, false)`            | `"Analyzing document…"`      |
| 8 | `({name:"execute_code", status:"preparing",...}, ...)`    | `"Preparing code…"`          |
| 9 | `({name:"execute_code", status:"running",...}, ...)`      | `"Running code…"`            |
| 10| `({name:"load_skill", args:{skill_name:"docx"},...}, ...)`| `Loading skill "docx"…`      |
| 11| `({name:"unknown_tool",...}, true, false)`                | `"Working…"` (default)       |

Graceful fallback: `load_skill` with empty/missing `args.skill_name` returns `Loading skill ""…`.

## Invariants Preserved (D-066-10, D-067-02, Pitfall 2)

- `: null /* D-067-02: no mid-stream chrome — terminal states only carry text. Match Claude/ChatGPT. */` at MessageItem.tsx line 145 (formerly 140) PRESERVED — D-067-02 carry-forward from Phase 067 NOT reverted to `"Saving response…"`. Repository-wide grep across `frontend/src/` confirms zero `Saving response` occurrences.
- `animate-fadeSlideUp` Tailwind class on both placeholder spans PRESERVED — RESEARCH § Pitfall 2 needs this for smooth crossfades between state transitions (7 occurrences across the file).
- `<Loader2 className="w-4 h-4 animate-spin text-primary" />` icon at line 125 + `<Loader2 className="w-3.5 h-3.5 animate-spin text-primary flex-shrink-0" />` icon at line 137 PRESERVED.
- 3-dot bouncer span block at lines 127-131 + 147-153 PRESERVED verbatim.
- Terminal-state copy "Agent reached time limit" (line 142) and "Response stopped" (line 144) PRESERVED — Phase 066 D-066-10 contract markers.
- Bottom-of-message Square indicator block at lines 165-170 PRESERVED unchanged.
- ToolCallPanel.tsx UNTOUCHED — D-067-03 iteration-divider invariant intact.

## Static Verification Gates Passed

| Gate | Result |
|------|--------|
| `cd frontend && tsc --noEmit -p tsconfig.json` (Task 1) | exit 0 |
| `cd frontend && tsc --noEmit -p tsconfig.json` (Task 2) | exit 0 |
| `grep -c 'export function outerBannerLabel' toolMeta.ts` | 1 (== 1 ✓) |
| `grep -c 'import type { ToolCall } from' toolMeta.ts` | 1 (== 1 ✓) |
| `grep -E 'export function (toolLabel\|toolSummary\|taskPhaseLabel)' toolMeta.ts` | 3 (== 3 ✓) |
| `grep -c 'outerBannerLabel' MessageItem.tsx` | 4 (≥ 3 ✓) |
| `grep '<span className="italic">Thinking</span>' MessageItem.tsx` | 0 (== 0 ✓) |
| `grep '"Working"' MessageItem.tsx` | 0 (== 0 ✓) |
| `grep '"Synthesizing answer"' MessageItem.tsx` | 0 (== 0 ✓) |
| `grep 'Saving response' frontend/src/` | 0 (== 0 ✓) |
| `grep 'D-067-02' MessageItem.tsx` | ≥1 ✓ |
| `grep 'animate-fadeSlideUp' MessageItem.tsx` | 7 (≥ 2 ✓) |
| `grep 'Agent reached time limit' MessageItem.tsx` | ≥1 ✓ |
| `grep 'Response stopped' MessageItem.tsx` | ≥1 ✓ |
| `grep 'tc.status === "running" \|\| tc.status === "preparing"' MessageItem.tsx` | 1 (≥ 1 ✓) |

**Note on tsc invocation:** the worktree's `frontend/node_modules` is not populated (worktrees do not share node_modules with the parent). The compile gate was run via the parent project's installed tsc binary (`C:/Vibe Apps/Agentic RAG/frontend/node_modules/.bin/tsc.cmd`) against the worktree's `tsconfig.json`. This is equivalent to the plan-prescribed `npx tsc` since the parent ships the same TypeScript ~5.9.3 declared in the worktree's `package.json`. Both runs returned exit 0 with no diagnostics.

## Deviations from Plan

None — plan executed exactly as written. The one minor variance ("changed `activeTool` from `find()` `T | undefined` to `?? null` `T | null`") was explicitly required by the plan's Task 2 step 2 (verbatim `... ?? null`) and is consistent with the `outerBannerLabel(activeTool: ToolCall | null, ...)` parameter type from Task 1.

## Threat Surface Scan

No new trust boundaries introduced. T-067.1-05 (skill_name disclosure via `outerBannerLabel(load_skill)`) and T-067.1-06 (XSS via skill_name template literal — React JSX text-node auto-escapes) both remain `accept` per plan threat model. No new endpoints, auth paths, or schema changes.

## Auth Gates

None — fully autonomous frontend-only execution.

## Runtime Verification — Owned by Plan 05

Per plan §verification + §output, this plan ships only static verification (tsc + grep). Plan 05 owns runtime verification via Chrome MCP `evaluate_script` against the Fahed-Mrad multi-tool prompt (UX-067.1-01..05), which will live-validate:
- Pre-first-delta silence renders "Setting up agent…" (1-3s window).
- `tool_preparing(search_documents)` → "Searching knowledge base…".
- `tool_preparing(execute_code)` → "Preparing code…" → `tool_start` → "Running code…".
- `skill_activated` → `Loading skill "X"…`.
- All-tools-done → "Synthesizing answer…".

## Commits

| Task | Commit  | Description                                              |
|------|---------|----------------------------------------------------------|
| 1    | fdd343a | feat(067.1-02): add outerBannerLabel sibling export to toolMeta.ts |
| 2    | 6044590 | feat(067.1-02): wire outerBannerLabel into MessageItem.tsx        |

## Self-Check: PASSED

- ✓ `frontend/src/lib/toolMeta.ts` exists and contains `outerBannerLabel` export
- ✓ `frontend/src/components/chat/MessageItem.tsx` exists and imports/calls `outerBannerLabel` at 2 sites
- ✓ Commit fdd343a present in `git log`
- ✓ Commit 6044590 present in `git log`
- ✓ tsc exit 0 (both tasks)
- ✓ All acceptance grep checks satisfied
- ✓ D-067-02 + D-066-10 + animate-fadeSlideUp invariants preserved verbatim
