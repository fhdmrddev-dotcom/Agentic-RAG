---
phase: 136-skill-publish-gate-gate-01
plan: 04
type: execute
wave: 3
depends_on: [136-01, 136-03]
files_modified:
  - frontend/src/components/skills/SkillEvalSection.tsx
  - frontend/src/components/skills/SkillEvalSection.test.tsx
autonomous: true
requirements: [GATE-01]
user_setup: []

must_haves:
  truths:
    - "SkillEvalSection shows a plain gate-status line (publish readiness at a glance): satisfied 'Eval passed X/N on current version' when met, or the honest unmet state (never evaled / latest run failed / passed on an older version) when not (D-06)"
    - "When a skill was force-published, SkillEvalSection shows the owner-visible override record — an honest 'published without a passing eval on <date>' status from PublishGate.last_override (D-02)"
    - "The line renders server-computed gate state from getPublishGate — no client-side gate math (D-07); it is plain/undesigned, reusing the existing readout style, no new chrome (137 fence)"
  artifacts:
    - path: "frontend/src/components/skills/SkillEvalSection.tsx"
      provides: "Gate-status line + owner-visible override record hydrated from getPublishGate"
      contains: "getPublishGate"
  key_links:
    - from: "frontend/src/components/skills/SkillEvalSection.tsx"
      to: "/skills/{id}/publish-gate"
      via: "getPublishGate on mount (useEffect [skillId])"
      pattern: "getPublishGate"
---

<objective>
Add the D-06 gate-status line to the existing thin eval surface: publish readiness at a glance
(satisfied X/N vs honest unmet) plus the owner-visible override record ("published without a
passing eval" — D-02) when the skill was force-published. Both are rendered from the server
`PublishGate` (incl `last_override`), reusing the surface's own honest-counts style. Do NOT
redesign the section — Phase 137 (PANEL-01, G-2) owns the designed panel.

Purpose: D-06 gives the owner publish readiness + the honest override trail in the one place they
already read eval state. Isolated in its own plan because SkillEvalSection is the largest surface
file and its gate-line is additive-only (137 fence).

Output: the gate-status line + override record in SkillEvalSection + a focused component test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-skill-publish-gate-gate-01/136-CONTEXT.md
@.planning/phases/136-skill-publish-gate-gate-01/136-PATTERNS.md
@.planning/phases/136-skill-publish-gate-gate-01/136-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 (already merged): -->
<!--   api.ts getPublishGate(skillId): Promise<PublishGate> -->
<!--   types PublishGate { met, state, measured, passed, passing_run_id, reason,
         last_override: { gate_state, created_at } | null } -->
<!-- SkillEvalSection.tsx already: Props { skillId: string }; renderGateCounts helper (:96-111);
     per-run verdict numeric readout (:566-572); proposal hydrate on mount useEffect [skillId] (:288-353)
     + refetchProposal refetch-not-optimistic (:263-272). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate-status line + override record in SkillEvalSection + test</name>
  <files>frontend/src/components/skills/SkillEvalSection.tsx, frontend/src/components/skills/SkillEvalSection.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.tsx:50-52 (Props), :96-111 (renderGateCounts — the plain honest-counts render style), :263-272 (refetchProposal — refetch-not-optimistic), :288-353 (the useEffect [skillId] mount-hydration block to mirror for the gate fetch), :560-575 (the per-run verdict readout to echo)
    - frontend/src/components/skills/SkillFormDialog.test.tsx (the same-directory test that mocks @/lib/api — the analog for mounting a skills component with a mocked api; MEMORY project_frontend_vitest_rot: author fresh, don't lean on rotted siblings)
    - .planning/phases/136-skill-publish-gate-gate-01/136-PATTERNS.md (SkillEvalSection.tsx section — D-06 gate-status line + override record)
  </read_first>
  <action>
    In frontend/src/components/skills/SkillEvalSection.tsx add `publishGate` local state (`PublishGate | null`). Hydrate it on mount by calling `getPublishGate(skillId)` inside the existing `useEffect [skillId]` init block (same place proposals load), refetch-not-optimistic. Render a PLAIN gate-status line near the top of the section (reuse the renderGateCounts / verdict-line text style — `text-xs`, muted, no new chrome, 137 fence):
    - `publishGate.met` → "Publish ready — eval passed {passed}/{measured} on the current version".
    - `!publishGate.met` → the honest state text keyed off `publishGate.state` ("never_evaled" → "Not publishable yet — run an eval on the current version"; "latest_failed" → "Not publishable — the latest eval on the current version failed"; "passed_on_older_version" → "Not publishable — the last passing eval was on an older version; re-eval to publish"), echoing `publishGate.reason`.
    - If `publishGate.last_override` is present → an owner-visible override record line "Published without a passing eval on {new Date(last_override.created_at).toLocaleDateString()}" (D-02 honest "published without passing eval" status). Plain text.
    Do NOT redesign the section, add tabs, or introduce design-system chrome (137 fence). Additive only.
    Create frontend/src/components/skills/SkillEvalSection.test.tsx (vitest + @testing-library/react, `vi.mock("@/lib/api")` mocking getProviders/listEvalRuns/listProposals to return empty + subscribeToRun as a no-op unsubscribe + getPublishGate as the unit under test): (a) met → getPublishGate resolves met=true passed=2 measured=2 → assert "2/2" publish-ready line renders; (b) unmet never_evaled → assert the "Not publishable yet — run an eval" line; (c) last_override present → assert the "Published without a passing eval" override line renders.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- SkillEvalSection --run</automated>
  </verify>
  <acceptance_criteria>
    - Test (a): met render shows a "2/2" publish-ready line
    - Test (b): unmet never_evaled render shows the "Not publishable yet — run an eval" pointer line
    - Test (c): with last_override set, the "Published without a passing eval" override-record line renders (D-02)
    - grep confirms `getPublishGate` is called inside the `useEffect [skillId]` hydration (not on every render) and no client-side met/pass recomputation exists (renders server fields only)
    - `npm run test -- SkillEvalSection --run` green; no new design-system component imported (137 fence — grep shows only existing plain-text elements added)
  </acceptance_criteria>
  <done>SkillEvalSection shows the gate-status line + owner-visible override record from getPublishGate, with 3 green tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser eval surface → skills API | The gate-status line reads server-computed state; it must not recompute the gate client-side, and the override record is owner-only (server-enforced) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-03 | Tampering (forged gate) | SkillEvalSection gate line | mitigate | Renders server PublishGate fields only — no client-side met/pass math (Task 1) |
| T-136-04 | Information Disclosure (IDOR) | override record display | mitigate | last_override comes from the owner-scoped getPublishGate (server `.eq(user_id)` + owner-only RLS on skill_publish_overrides — Plans 01/02); the client only renders what the owner is entitled to see (Task 1) |
| T-136-SC | Tampering (supply chain) | npm installs | accept | Zero new packages — RESEARCH Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `cd frontend && npm run test -- SkillEvalSection --run` green
- `cd frontend && npx tsc --noEmit -p tsconfig.json` clean
- Frontend-only diff (no backend/agent-loop/threads.py touch — D-10); section is additive (137 fence — no redesign)
- G-4 lived UAT of the status line + override record is a phase-gate manual check (136-VALIDATION.md Manual-Only)
</verification>

<success_criteria>
- SkillEvalSection shows a plain publish-readiness gate line (satisfied X/N vs honest unmet) (D-06)
- The owner-visible override record renders honestly when the skill was force-published (D-02)
- Server-computed state only, no client-side gate math (D-07); plain/undesigned (137 fence)
</success_criteria>

<output>
Create `.planning/phases/136-skill-publish-gate-gate-01/136-04-SUMMARY.md` when done.
</output>
