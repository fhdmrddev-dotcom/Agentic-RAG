---
phase: 149-model-registry-discovery
plan: 07
type: execute
wave: 3
depends_on: ["149-04", "149-05"]
files_modified:
  - frontend/src/components/admin/ModelRegistryTab.tsx
  - frontend/src/components/admin/ModelDiscoveryPanel.tsx
  - frontend/src/components/admin/ControlRoomPage.tsx
  - frontend/src/components/admin/__tests__/ModelRegistryTab.test.tsx
  - frontend/src/components/admin/__tests__/ModelDiscoveryPanel.test.tsx
  - .planning/phases/149-model-registry-discovery/149-VALIDATION.md
autonomous: true
requirements: [MODEL-01, MODEL-02]

must_haves:
  truths:
    - "D-149-02: the Model Registry tab renders the 070-A instrument table — provider-grouped collapsible sections, click-to-edit numeric cells, OVR-vs-dim/italic-DEF with Reset, 🔓/🔒 per-row lock, and the derived `✓ in picker / ✕ hidden` coupling chip from `enabled` (the two-layer pattern made visible, D-149-01)"
    - "D-149-02 + SC#3: the discovery panel renders the 071-A propose→confirm flow — per-provider run cards (timers, verbatim error = excluded-not-failed, no-key = skipped, capabilities ✓ vs IDs-only badge), ✚New/±Changed/⊘Vanished groups, and amber 'unknown — you set it' inputs for un-returned capabilities; a new model is NEVER auto-enabled, vanished models are flagged (deprecate/disable/keep) never auto-deleted"
    - "D-149-09: disabling the org-default/locked model surfaces the server's 409 plain-language refusal in the UI (no silent failure); every write flashes a ✎ receipt"
    - "D-149-06: advanced routing facets (emit_tier, max_tools, parallel, prefill, strict_json) stay code-only in 149 and are optionally rendered read-only behind the ⌥ Technical names reveal — never editable this phase"
    - "The locked Model Registry tab (146 placeholder) is unlocked and renders both surfaces via the shell's write-then-refetch + pulseRecording wiring"
    - "SC#10: 149-VALIDATION.md carries the 4-axis cross-provider UAT rows (incl. the D-149-16 gpt-5.6 native_tools flip = the 'no restart' proof) under Manual-Only Verifications"
  artifacts:
    - path: "frontend/src/components/admin/ModelRegistryTab.tsx"
      provides: "070-A capability instrument table (inline edit, OVR/DEF, lock, coupling chip)"
      min_lines: 80
    - path: "frontend/src/components/admin/ModelDiscoveryPanel.tsx"
      provides: "071-A propose→confirm diff (run cards, unknown-you-set-it, vanished-flagged)"
      min_lines: 80
    - path: ".planning/phases/149-model-registry-discovery/149-VALIDATION.md"
      provides: "SC#10 4-axis cross-provider UAT rows"
      contains: "gpt-5.6"
  key_links:
    - from: "frontend/src/components/admin/ControlRoomPage.tsx"
      to: "getModelRegistry / setModelCapability / runModelDiscovery"
      via: "shell fetch-then-refetch + pulseRecording on recorded writes"
      pattern: "ModelRegistry"
---

<objective>
Deliver the operator-facing Model Registry surfaces (D-149-02, the LOCKED build contract): the 070-A capability instrument table and the 071-A discovery propose→confirm panel, mounted by unlocking the Phase-146 Model Registry tab. Then author the phase's SC#10 cross-provider UAT rows into `149-VALIDATION.md`.

Purpose: This is where MODEL-01 + MODEL-02 become visible to the operator — reading/editing capabilities with OVR/DEF honesty + the enabled→picker coupling (the two-layer pattern made visible), and running discovery with the propose-only "unknown — you set it" SC#3 hero. The sketches are locked; do NOT re-litigate the UI. Runtime completeness of the discovery panel + the 409 refusal depends on Plan 06 (same wave); the component tests mock the api so this plan's own gate does not.
Output: `ModelRegistryTab.tsx` (070-A), `ModelDiscoveryPanel.tsx` (071-A), the unlocked + wired `ControlRoomPage.tsx`, and the SC#10 UAT rows in `149-VALIDATION.md`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/149-model-registry-discovery/149-CONTEXT.md
@.planning/phases/149-model-registry-discovery/149-PATTERNS.md
@.planning/phases/149-model-registry-discovery/149-RESEARCH.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@.planning/sketches/070-model-capability-editor/README.md
@.planning/sketches/071-model-discovery-propose-confirm/README.md

<interfaces>
frontend/src/lib/api.ts (Plan 04): getModelRegistry(): Promise<ModelRegistryRow[]>,
  setModelCapability(modelId, patch): Promise<void> (409 → ApiError with server detail),
  runModelDiscovery(): Promise<DiscoveryResult>; types ModelRegistryRow / ModelCapabilityPatch / DiscoveryResult.

Analogs (PATTERNS.md):
  frontend/src/components/admin/FeatureVisibility.tsx — pure-leaf props-in/DOM-out; per-card busy/failed/receipt state;
    the amber styling (border-amber-500/30 bg-amber-500/[0.05], text-amber-400) for the "unknown — you set it" inputs;
    the ⌥ Technical-names conditional reveal (:262-267).
  frontend/src/components/admin/UsersAndAccess.tsx — Sheet side="bottom" confirm + runWrite busy wrapper (for "enable now" opt-in ticks + vanished flag actions).
  frontend/src/components/admin/CapabilityGrid.tsx — toggle grid + its test (mirror for the component tests).
  frontend/src/components/admin/ControlRoomPage.tsx — the users-access tab-def flip (:112-117 locked→false),
    lazy tab-open fetch + alive.current guard (:257-264), write-then-refetch + pulseRecording (:427-458),
    and the users-access render branch (:639-663) to mirror for the model-registry branch.
  Inline numeric-cell edit: the 112/FolderNode pattern (RDD 21).
  Provider logos: frontend/src/lib/providerLogo.tsx (deep subpath imports only).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ModelRegistryTab.tsx — the 070-A capability instrument table</name>
  <files>frontend/src/components/admin/ModelRegistryTab.tsx, frontend/src/components/admin/__tests__/ModelRegistryTab.test.tsx</files>
  <read_first>
    - .planning/sketches/070-model-capability-editor/README.md + index.html (the LOCKED contract — variant A)
    - frontend/src/components/admin/FeatureVisibility.tsx (the pure-leaf + per-card busy/receipt + ⌥ Technical-names pattern)
    - frontend/src/components/admin/CapabilityGrid.tsx + its __tests__ (the toggle-grid analog + test mirror)
    - frontend/src/lib/providerLogo.tsx (provider marks)
  </read_first>
  <behavior>
    - Renders provider-grouped collapsible sections of `ModelRegistryRow[]` (props-in; the shell owns fetch + write).
    - Numeric cells (context_window_tokens, max_output_tokens, llm_call_timeout_seconds) are click-to-edit inline; editing calls `onSetCapability(modelId, patch)` and shows a per-row busy → ✎ receipt flash → or failure state; no optimistic flip (the shell re-fetches).
    - Each row shows the derived coupling chip `✓ in picker` when enabled, `✕ hidden` when disabled; a 🔓/🔒 lock affordance (locked row shows 🔒 + "org default").
    - An OVR (db_override) value reads distinct from a dim+italic DEF (inherited) value, with a Reset on overridden fields.
    - A 409 from `onSetCapability` (disabling the org-default/locked model) surfaces the server's plain-language detail in-row (no silent failure).
    - `showTechnical` reveals raw column names (e.g. `max_output_tokens`); optionally the read-only advanced facets (D-149-06).
  </behavior>
  <action>
    Create `ModelRegistryTab.tsx` as a pure presentational leaf (FeatureVisibility pattern): props `rows: ModelRegistryRow[]`, `onSetCapability(modelId, patch): Promise<void>`, `onLock(modelId, locked): Promise<void>`, `showTechnical: boolean`. Group rows by provider (collapsible sections) with `providerLogo(provider)`. Render the real columns (context / max-out / native-tools toggle / timeout / enabled) as inline click-to-edit cells (112/FolderNode inline-edit); wire each edit through `onSetCapability` with per-row busy/receipt/failed transient state (mirror FeatureVisibility's FeatureCard). Derive the `✓ in picker / ✕ hidden` coupling chip from `enabled`; render the 🔓/🔒 lock (calls `onLock`) and mark the locked row as the org default. Distinguish OVR (stored) from DEF (dim+italic inherited) and show a Reset on overridden fields (Reset routes through `onSetCapability` with the agreed reset semantics — clear the override for that field). Catch a rejected `onSetCapability` and, when it is an ApiError(409), show its `detail` inline (the plain refusal). Gate raw column names behind `showTechnical`. Add `ModelRegistryTab.test.tsx` (mirror CapabilityGrid.test.tsx): a disabled row shows `✕ hidden`; an enabled row shows `✓ in picker`; editing a numeric cell calls `onSetCapability` with the patch; a 409 rejection surfaces the server detail; a DEF field shows Reset only when overridden.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- ModelRegistryTab 2>&1 | tail -8 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `ModelRegistryTab.tsx` is a props-in/DOM-out leaf (no direct fetch/api import — the shell owns writes)
    - Renders the coupling chip (`✓ in picker` / `✕ hidden`) from `enabled` and the 🔓/🔒 lock
    - Numeric cells are inline-editable and call `onSetCapability`; OVR vs DEF is visually distinct with a Reset on overridden fields
    - A 409 rejection surfaces the server `detail` inline (test asserts the plain refusal text renders)
    - `npm run test -- ModelRegistryTab` passes; `npm run build` exits 0 (zero new tsc errors)
  </acceptance_criteria>
  <done>The 070-A instrument table renders + edits capabilities with OVR/DEF honesty, the enabled→picker coupling, lock, and graceful 409 handling; tests + build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ModelDiscoveryPanel.tsx — the 071-A propose→confirm panel (SC#3 hero)</name>
  <files>frontend/src/components/admin/ModelDiscoveryPanel.tsx, frontend/src/components/admin/__tests__/ModelDiscoveryPanel.test.tsx</files>
  <read_first>
    - .planning/sketches/071-model-discovery-propose-confirm/README.md + index.html (the LOCKED contract — variant A)
    - frontend/src/components/admin/FeatureVisibility.tsx (the amber "you set it" styling to reuse; the per-card receipt)
    - frontend/src/components/admin/UsersAndAccess.tsx (Sheet side="bottom" + runWrite busy wrapper for the enable-now ticks / vanished flag actions)
  </read_first>
  <behavior>
    - "Run discovery" triggers `onRunDiscovery()` and shows per-provider run cards: an in-flight timer, then per provider a verbatim error (excluded, not failed), a "no key — skipped", or a "capabilities ✓ / IDs only" badge.
    - Results render as ✚New / ±Changed / ⊘Vanished groups. New/changed capability fields the provider did NOT return render as amber "unknown — you set it" inputs (never a guessed value).
    - A new model's "enable now" tick is available ONLY when its capabilities are complete (provider-returned or hand-filled); un-fillable rows cannot be enabled (SC#3).
    - Vanished models are flagged with mark-deprecated / disable / keep actions — never auto-deleted.
    - Confirming applies the chosen changes via `onConfirm(...)` (which the shell routes to setModelCapability per model) with ✎ receipts; navigating away discards (ephemeral — no local persistence).
  </behavior>
  <action>
    Create `ModelDiscoveryPanel.tsx` as a leaf: props `onRunDiscovery(): Promise<DiscoveryResult>`, `onConfirm(changes): Promise<void>`. Render the per-provider run cards (client-side timers while in flight; then the verbatim-error / no-key / "capabilities ✓ vs IDs only" badge from the result's per-provider outcomes). Render ✚New / ±Changed / ⊘Vanished groups; for any un-returned capability field render the amber "unknown — you set it" input (reuse FeatureVisibility's amber classes). Gate each new model's "enable now" tick on capability completeness (disabled tick + tooltip when incomplete — SC#3). Give vanished models mark-deprecated / disable / keep actions (never a delete). A sticky confirm bar names the count and calls `onConfirm` (busy wrapper like UsersAndAccess.runWrite); the diff lives only in component state (ephemeral, D-149-12). Add `ModelDiscoveryPanel.test.tsx`: a Google new model → native_tools renders as the amber "unknown" input (not a value); an OpenAI provider card shows the "IDs only" badge; an un-fillable new model's "enable now" tick is disabled; a vanished model shows flag actions and NO delete; confirming calls `onConfirm`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- ModelDiscoveryPanel 2>&1 | tail -8 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Un-returned capability fields render as amber "unknown — you set it" inputs (test asserts the amber input, not a value, for a Google native_tools + an OpenAI capability)
    - A new model's "enable now" tick is disabled unless capabilities are complete (SC#3 — test asserts an incomplete row cannot be enabled)
    - Vanished models show mark-deprecated/disable/keep actions and NO delete action
    - Per-provider cards show verbatim-error / no-key / "capabilities ✓ vs IDs only" states
    - `npm run test -- ModelDiscoveryPanel` passes; `npm run build` exits 0
  </acceptance_criteria>
  <done>The 071-A panel makes propose-only the visible hero — nothing un-returned is auto-enabled, vanished is flagged-not-deleted, proposals are ephemeral; tests + build green.</done>
</task>

<task type="auto">
  <name>Task 3: Unlock + wire the Model Registry tab, and author the SC#10 VALIDATION rows</name>
  <files>frontend/src/components/admin/ControlRoomPage.tsx, .planning/phases/149-model-registry-discovery/149-VALIDATION.md</files>
  <read_first>
    - frontend/src/components/admin/ControlRoomPage.tsx (the model-registry tab def :112-117 to flip locked→false; the lazy fetch + alive.current :257-264; the write-then-refetch + pulseRecording :427-458; the users-access render branch :639-663 to mirror)
    - frontend/src/lib/api.ts (getModelRegistry / setModelCapability / runModelDiscovery + lock seam)
    - .planning/phases/149-model-registry-discovery/149-VALIDATION.md (the stub to fill — Manual-Only Verifications + Per-Task map)
    - CLAUDE.md (the MANDATORY UAT scoreboard recipe — the 4-axis bandwidth)
    - .planning/phases/149-model-registry-discovery/149-RESEARCH.md (§"SC#10 Cross-Provider UAT" — the exact axis rows + the D-149-16 gpt-5.6 flip)
  </read_first>
  <action>
    In `ControlRoomPage.tsx`: flip the `model-registry` tab def from `locked: true` to `locked: false` (remove `lockedDescription`) and render `<ModelRegistryTab .../>` + `<ModelDiscoveryPanel .../>` in the model-registry branch (mirror the users-access branch). Add a lazy `fetchRegistry` on model-registry tab-open (guarded by `alive.current`), and `handleSetCapability` / `handleLock` / `handleRunDiscovery` callbacks that call the api.ts seams, `pulseRecording()` after a recorded write, and re-fetch the registry (no optimistic flip — server is source of truth). Co-commit the ControlRoomPage state + the leaf props seam so the per-task `npm run build` is green (noUnusedLocals). Then author the SC#10 UAT rows into `149-VALIDATION.md` under Manual-Only Verifications — all 4 axes: (Cross-provider >=4) edit a capability on an OpenAI, Anthropic, Google, and OpenRouter model and verify the effect on the next request, INCLUDING the D-149-16 row: flip gpt-5.6 native_tools OFF in the new UI -> a tool-carrying chat then works via the prompt-injected path (doubles as the SC#1 "capability change affects routing live, no restart" proof); (Multi-tool) >=1 row exercising 2+ tools in one prompt after a capability edit; (Parallel-thread) >=1 row — Thread A streaming while Thread B sends a prompt on a just-disabled model (the D-149-10 fallback notice fires, no mid-conversation break); (Long-message) >=1 row >=50 prior messages or >=5 KB prompt on an edited model. Also add a propose-only UAT row: discovery shows Google/OpenRouter "capabilities ✓" auto-fill vs OpenAI/Anthropic "IDs only" amber "you set it" inputs, and a new model cannot be enabled without complete caps (SC#3). Fill the Per-Task Verification Map with the 149 test files and set `nyquist_compliant: true` when the map is complete.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3 && grep -qi "gpt-5.6" ../.planning/phases/149-model-registry-discovery/149-VALIDATION.md && grep -qiE "parallel|long-message|multi-tool" ../.planning/phases/149-model-registry-discovery/149-VALIDATION.md && echo VALIDATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ControlRoomPage.tsx` model-registry tab def is `locked: false` and renders `<ModelRegistryTab>` + `<ModelDiscoveryPanel>`
    - The shell fetches the registry lazily on tab-open (alive.current guard) and re-fetches after each recorded write with `pulseRecording()`
    - `npm run build` exits 0 (the ControlRoomPage state + leaf props seam are co-committed — zero new tsc errors)
    - `149-VALIDATION.md` Manual-Only Verifications contains all 4 SC#10 axes AND the D-149-16 gpt-5.6 native_tools flip row AND the propose-only SC#3 row
    - `149-VALIDATION.md` Per-Task Verification Map lists the 149 test files; `nyquist_compliant: true` is set
  </acceptance_criteria>
  <done>The Model Registry tab is unlocked, wired write-then-refetch, and the SC#10 4-axis UAT rows (incl. the gpt-5.6 no-restart proof) are authored in VALIDATION.md; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → operator surfaces | The tab renders only for operators (the shell is 404-gated server-side); the leaves add no authority |
| operator input → capability writes | Inline edits + confirm actions flow through the api.ts seams to the server allowlist (Plan 05/06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-18 | Elevation of Privilege | Model Registry tab | mitigate | The tab is inside the operator-only Control Room shell (server 404-gates every /admin route); the leaves render API-supplied data only |
| T-149-19 | Tampering (routing integrity, SC#3) | discovery "enable now" | mitigate | The "enable now" tick is disabled unless capabilities are complete; un-returned fields are amber "unknown" inputs — the UI cannot auto-enable a guessed capability |
| T-149-20 | Repudiation | inline capability writes | mitigate | Every write flashes a ✎ receipt; the shell pulses the 062-A recording marker; the server records the ledger row (Plan 05/06) |
| T-149-SC | Tampering | package installs | accept | Zero new packages (@lobehub/icons + testing libs already present) |
</threat_model>

<verification>
- `npm run test -- ModelRegistryTab` and `npm run test -- ModelDiscoveryPanel` green; `npm run build` exits 0.
- The Model Registry tab is unlocked and renders both surfaces; writes flash ✎ receipts and re-fetch.
- `149-VALIDATION.md` carries all 4 SC#10 axes + the D-149-16 gpt-5.6 flip + the propose-only SC#3 row; `nyquist_compliant: true`.
</verification>

<success_criteria>
- The operator can read/edit capabilities (070-A) and run discovery (071-A) from the Control Room; propose-only is the visible hero; no un-returned capability can be auto-enabled.
- The SC#10 4-axis UAT contract is authored for verify-work; the gpt-5.6 flip proves "capability change affects routing live, no restart."
- Build green within the known baseline; zero new packages.
</success_criteria>

<output>
Create `.planning/phases/149-model-registry-discovery/149-07-SUMMARY.md` when done.
</output>
