---
phase: 149-model-registry-discovery
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/settings/ModelPillRow.tsx
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/components/settings/__tests__/SettingsModelBadge.test.tsx
autonomous: true
requirements: [MODEL-01, MODEL-02]

must_haves:
  truths:
    - "D-149-17: the model picker (Settings + chat) groups models by provider with @lobehub provider logos (RDD 48), the model name is primary, and context/output info is demoted to a subtle secondary line/tooltip — a bounded visual pass, NO redesign, same component + interaction"
    - "D-149-05: a deprecated model shows a small informational `deprecated` badge in the picker (mirroring the existing amber `unverified` chip) and stays selectable — badge only, no refusal"
    - "The api.ts client seams (getModelRegistry / setModelCapability / runModelDiscovery) exist as the typed contract the admin tabs (Plan 07) consume, unwrapping the response envelope in the client (never in the component)"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "getModelRegistry / setModelCapability / runModelDiscovery + ModelRegistryRow / ModelCapabilityPatch / DiscoveryResult types"
      exports: ["getModelRegistry", "setModelCapability", "runModelDiscovery"]
    - path: "frontend/src/components/settings/ModelPillRow.tsx"
      provides: "provider-logo + deprecated-badge picker polish"
      contains: "deprecated"
  key_links:
    - from: "frontend/src/components/settings/ModelPillRow.tsx"
      to: "frontend/src/lib/providerLogo.tsx"
      via: "single-source @lobehub provider marks (RDD 48)"
      pattern: "providerLogo"
---

<objective>
Ship the frontend data contract + the bounded picker visual polish (D-149-17 + D-149-05). Two independent, Wave-1-safe pieces: (1) the typed `api.ts` client seams the operator Model Registry tab (Plan 07) will call — defined now as the interface-first contract; (2) the user-facing model picker polish — provider logos, provider grouping, demoted capability info, and the deprecated badge — in both the Settings picker (`ModelPillRow`) and the chat picker (`MessageInput`).

Purpose: The picker's data source is becoming the registry (D-149-08), so its presentation gets the operator's requested cleanup ("we added a lot of models under each provider and did not include icons… the context info makes it not very good"). Defined as a visual-only pass on the same components — no redesign. The api.ts seams are the contract Plan 07 builds against.
Output: `api.ts` registry seams + types; `ModelPillRow` + `MessageInput` grouped-by-provider with logos, demoted info, and a deprecated badge that lights up when the payload carries `deprecated_models` (supplied by Plan 05).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/149-model-registry-discovery/149-CONTEXT.md
@.planning/phases/149-model-registry-discovery/149-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- api.ts existing seams to mirror (PATTERNS.md): -->
frontend/src/lib/api.ts:
  setFlag(key, value) (:3996)               # PUT + getAuthHeaders + !res.ok → ApiError — the write-seam shape
  getAdminActiveRuns() (:3971)              # backend returns {"runs":[...]}; UNWRAP .runs in the client (CR-01) — the envelope-unwrap shape
  → ADD: getModelRegistry(): Promise<ModelRegistryRow[]>      // GET /admin/models, unwrap .models
         setModelCapability(modelId, patch): Promise<void>    // PATCH /admin/models/{id}
         runModelDiscovery(): Promise<DiscoveryResult>        // POST /admin/models/discover (ephemeral diff)
  → ADD types: ModelRegistryRow (model_id, provider, capability_source "registry"|"db_override",
               enabled, deprecated, context_window_tokens, max_output_tokens, native_tools,
               llm_call_timeout_seconds, is_default, is_locked),
               ModelCapabilityPatch (partial of the editable columns),
               DiscoveryResult (per-provider run outcomes + new/changed/vanished groups)

<!-- Picker components: -->
frontend/src/components/settings/ModelPillRow.tsx — models: string[] + verifiedModels: Set + inferredProviderFor: Record;
  renders name + an amber `unverified` chip (the styling template for the deprecated badge).
frontend/src/components/chat/MessageInput.tsx (:246-261) — the chat picker inlines the model list + context info (demote this).
frontend/src/lib/providerLogo.tsx (:41-53) — providerLogo(providerId) → brand mark or null;
  DEEP subpath imports ONLY (`@lobehub/icons/es/OpenAI/components/Mono`) — never the barrel/brand index (drags antd, throws under Vite).
FullAppSettings — carries providers[] (each {id, models}), verified_models, inferred_provider_for;
  Plan 05 adds `deprecated_models: string[]` — consume it defensively (absent → empty set → no badge).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts registry client seams + types (the Plan-07 contract)</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (setFlag :3996 for the write-seam shape; getAdminActiveRuns :3971 for the envelope-unwrap shape; the ApiError + getAuthHeaders helpers)
  </read_first>
  <action>
    Add three client seams beside the existing admin seams, each using `await getAuthHeaders()` + `!res.ok → throw new ApiError(...)`:
    `getModelRegistry(): Promise<ModelRegistryRow[]>` — GET `${API_BASE}/admin/models`, parse the envelope and return `.models` (unwrap in the client, never in the component — CR-01 precedent);
    `setModelCapability(modelId: string, patch: ModelCapabilityPatch): Promise<void>` — PATCH `${API_BASE}/admin/models/{modelId}` with the patch body; a 409 (default/locked guard) must surface as `ApiError` with the server `detail` preserved so the tab can show the plain-language refusal;
    `runModelDiscovery(): Promise<DiscoveryResult>` — POST `${API_BASE}/admin/models/discover`, returns the ephemeral diff.
    Add the TS types `ModelRegistryRow`, `ModelCapabilityPatch`, `DiscoveryResult` (fields per the <interfaces> block) near the seams. These are the interface-first contract Plan 07 consumes — keep field names aligned with the backend GET/PATCH/discover payloads (Plan 05/06).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "api\.ts" ; echo "exit=$?" ; npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `api.ts` exports `getModelRegistry`, `setModelCapability`, `runModelDiscovery`
    - `getModelRegistry` returns `.models` (grep the function unwraps the envelope, not the raw response)
    - `ModelRegistryRow`, `ModelCapabilityPatch`, `DiscoveryResult` types are declared/exported
    - `npm run build` (`tsc -b` + `vite build`) exits 0 (no NEW tsc errors beyond the known 30-error baseline; `api.ts` contributes zero)
  </acceptance_criteria>
  <done>The three registry seams + types compile and build green; they are the contract Plan 07 imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Picker visual polish — logos, grouping, demoted info, deprecated badge</name>
  <files>frontend/src/components/settings/ModelPillRow.tsx, frontend/src/components/chat/MessageInput.tsx, frontend/src/components/settings/__tests__/SettingsModelBadge.test.tsx</files>
  <read_first>
    - frontend/src/components/settings/ModelPillRow.tsx (the current pill render + the amber `unverified` chip styling to mirror for the deprecated badge)
    - frontend/src/components/chat/MessageInput.tsx (~lines 246-261 — the chat picker's inline model + context info to demote)
    - frontend/src/lib/providerLogo.tsx (providerLogo() usage + the deep-subpath import rule)
    - frontend/src/components/settings/__tests__/SettingsModelBadge.test.tsx (the existing badge test to extend)
  </read_first>
  <behavior>
    - A model in `deprecatedModels` renders a small `deprecated` badge (amber-chip styling, mirroring `unverified`) AND remains a clickable/selectable pill (no disable, no refusal).
    - A model NOT in `deprecatedModels` renders no deprecated badge.
    - When `deprecatedModels` is absent/undefined, the picker renders exactly as before (defensive default — no crash, no badge).
    - Each provider group renders its `providerLogo(providerId)` mark; the model name is the primary text; context/output info is demoted to a secondary line or `title` tooltip (not inline-primary).
  </behavior>
  <action>
    Visual-only pass, NO redesign, same components + same click interaction (D-149-17). In `ModelPillRow`: add an optional `deprecatedModels?: Set<string>` prop (default empty) and render a `deprecated` badge for members, styled like the existing amber `unverified` chip (reuse the same chip classes); keep the pill selectable. Group the pills by provider (use the provider association available from the settings payload — `providers[].models` / `inferred_provider_for`) and prefix each group with `providerLogo(providerId)` (import per the deep-subpath rule — never the barrel). Demote any context/output capability text to a secondary line or a `title` tooltip so the model name is primary. Apply the same logo-grouping + demoted-info + deprecated-badge treatment to the chat picker in `MessageInput.tsx:246-261`. Update the ModelPillRow caller (SettingsPage) to pass `deprecatedModels` from `FullAppSettings.deprecated_models` defensively (`new Set(settings.deprecated_models ?? [])`) so the badge lights up once Plan 05 ships the field, and the per-task build stays green now (optional prop). Extend `SettingsModelBadge.test.tsx`: a model in `deprecatedModels` shows the `deprecated` badge and stays clickable; a model not in the set shows none; an undefined `deprecatedModels` renders without error.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- SettingsModelBadge 2>&1 | tail -8 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `ModelPillRow` accepts an optional `deprecatedModels?: Set<string>` and renders a `deprecated` badge for its members (grep: the component references `deprecated`)
    - The deprecated pill remains a `<button>` with the `onSelect` handler (still selectable — no `disabled`)
    - `providerLogo` is imported and rendered per provider group in both `ModelPillRow` and `MessageInput`
    - `SettingsModelBadge.test.tsx` asserts: deprecated member → badge + clickable; non-member → no badge; undefined set → no error
    - `npm run test -- SettingsModelBadge` passes; `npm run build` exits 0 (zero new tsc errors; coupled SettingsPage caller co-committed)
  </acceptance_criteria>
  <done>Both pickers show provider logos, grouped models, demoted capability info, and a selectable deprecated badge; tests + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /admin/models* | The api.ts seams call operator-gated routes; the client never bypasses the server gate (a non-operator simply gets a 404 → ApiError) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-08 | Elevation of Privilege | api.ts registry seams | mitigate | Client seams add NO authority — the backend `require_operator` 404 gate (Plan 05) is the sole authority; the seams only surface `ApiError` on non-2xx, including 404 for non-operators |
| T-149-09 | Information Disclosure | picker deprecated badge | accept | `deprecated_models` is global config (not user data); the badge is informational only |
| T-149-SC | Tampering | package installs | accept | Zero new packages (`@lobehub/icons` already in package.json) |
</threat_model>

<verification>
- `npm run build` green; the three api.ts seams + types compile.
- `npm run test -- SettingsModelBadge` green; deprecated badge selectable + defensive-absent behavior verified.
- Provider logos + grouping + demoted info present in both pickers (no redesign — same components).
</verification>

<success_criteria>
- The api.ts registry contract exists for Plan 07 to consume.
- The picker shows logos + grouping + demoted info + a selectable deprecated badge; the badge lights up when Plan 05 supplies `deprecated_models`.
- Zero new packages; build green within the known baseline.
</success_criteria>

<output>
Create `.planning/phases/149-model-registry-discovery/149-04-SUMMARY.md` when done.
</output>
