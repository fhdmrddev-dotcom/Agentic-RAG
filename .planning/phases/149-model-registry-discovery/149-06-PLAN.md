---
phase: 149-model-registry-discovery
plan: 06
type: execute
wave: 3
depends_on: ["149-05", "149-02"]
files_modified:
  - backend/app/api/admin.py
  - backend/app/main.py
  - backend/app/api/threads.py
  - backend/tests/test_149_default_guard.py
  - backend/tests/test_149_discover_endpoint.py
  - backend/tests/test_149_fallback_notice.py
autonomous: true
requirements: [MODEL-01, MODEL-02]

must_haves:
  truths:
    - "D-149-09: disabling the current org-default (or the locked) model is REFUSED with a 409 + plain explanation ('this is the org default; pick a new default first'; locked ⇒ 'unlock first') — no dead default can ever exist; ordinary disables stay a direct flip + ✎ receipt"
    - "D-149-07: locking a model via the dedicated PUT /admin/models/{id}/lock endpoint (body {locked}; SEPARATE from PATCH, matching the setModelLock seam) pins the org default (writes app_settings.llm_model + llm_model_locked=true) with at most ONE lock; unlocking clears llm_model_locked; llm_model_locked is added to the code-constant save_app_settings allowlist"
    - "D-149-09 (lock path): locking a currently-disabled model is REFUSED with 409 ('model is disabled — enable it first') so the pinned org default is always enabled — the lock guard + the disable guard TOGETHER guarantee no dead default can ever exist (symmetric with D-149-09's REFUSED vocabulary; honest consequence, no hidden auto-enable side-effect write)"
    - "D-149-10: a user whose selected model was just disabled has their next message run on the org default with an honest inline SSE notice naming BOTH models — never breaks mid-conversation, never silent"
    - "D-149-11: POST /admin/models/discover fans out to all keyed providers server-side, returns the ephemeral diff, records ✎ model.discover; provider selection (if any) is validated against the PROVIDER_ENDPOINTS code-constant set (no client URL — SSRF-safe)"
    - "ROADMAP red line: each NEW non-GET /admin model route (PUT .../lock, POST .../discover) carries its OWN non-operator 404 regression test — the 146 gate auto-enumerates GET only, so 147/148 precedent gives every non-GET route an explicit 404 test"
  artifacts:
    - path: "backend/app/api/admin.py"
      provides: "default/locked-disable 409 guard + PUT .../lock lock/unlock (with lock-a-disabled-model 409 guard) + POST /admin/models/discover"
      contains: "409"
    - path: "backend/app/api/threads.py"
      provides: "enabled-enforcement + fallback notice at the shared model-resolution seam"
      contains: "llm_model"
  key_links:
    - from: "backend/app/api/threads.py (resolution seam ~1089-1123)"
      to: "app_settings.llm_model (org default)"
      via: "disabled resolved model → fallback + honest SSE notice"
      pattern: "disabled"
    - from: "backend/app/api/admin.py:discover"
      to: "model_discovery_service.discover_all"
      via: "keyed providers → compute_diff vs current registry union"
      pattern: "discover"
---

<objective>
Make the registry govern the request path and complete MODEL-02's operator flow: the graded-guard that forbids a dead org default (D-149-09) — spanning BOTH the disable path (can't disable the current default/locked model) and the lock path (can't lock a disabled model) — the lock/unlock that pins the single org default via the dedicated `PUT /admin/models/{id}/lock` endpoint (D-149-07), the enabled-enforcement + honest fallback notice when a user's model was disabled (D-149-10), and the operator-gated `POST /admin/models/discover` endpoint that wraps the Plan-02 service (D-149-11).

Purpose: Plan 05 built the read + base write; this plan adds the governance semantics (both halves of the no-dead-default guard, lock), turns `enabled` into real request-path behavior, and wires discovery behind the operator gate. It touches the shared `threads.py` model-resolution seam — a MINIMAL in-place enforcement guard (no new endpoint, no file growth beyond the guard), mirroring the accepted Phase-147 G-5 override rationale for threads.py; the enforcement lives at the ONE shared resolution point and never forks per provider (D-14 red line).
Output: the two-part 409 no-dead-default guard (disable-path + lock-path) + `PUT .../lock` lock/unlock in `admin.py`, `POST /admin/models/discover`, and the disabled-model fallback notice in `threads.py` — each new non-GET route with its own non-operator 404 test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/149-model-registry-discovery/149-CONTEXT.md
@.planning/phases/149-model-registry-discovery/149-RESEARCH.md
@.planning/phases/149-model-registry-discovery/149-PATTERNS.md

<interfaces>
backend/app/api/admin.py:
  PATCH /admin/models/{model_id} (Plan 05)   # the base write — ADD the disable-guard check here
  disable_user 409 precedent (:401-408 / :793-797)   # HTTPException(409, detail="<plain sentence>") BEFORE any mutation
  operator_audit_floor + ✎ audit_action/label   # the receipt plumbing
  # the router already carries Depends(require_operator) (:96) → new routes inherit the byte-identical 404 gate
  # 404 test precedent: test_146_operator_gate.py (auto-enumerates GET); test_147_operator_kill.py::test_kill_non_operator_404 + test_148_disable/enable (each NEW non-GET route gets its own explicit 404 test)

backend/app/main.py:
  _DIRECT_COLUMNS (:98-111)   # save_app_settings' code-constant column allowlist; llm_model is present, llm_model_locked is NOT
                              # → ADD "llm_model_locked" (SQLi-safe: column names are code constants, values parameterized)

backend/app/models/user_settings.py:
  save_app_settings(updates) (:266)   # parameterized UPDATE over _DIRECT_COLUMNS + invalidates the settings cache
  load_all_model_overrides() (Plan 05)   # all-rows read → the disabled-id set for BOTH the request-path check AND the lock-path guard
  _load_settings_from_db / the app_settings row   # source of llm_model + llm_model_locked

backend/app/api/threads.py:
  resolution seam (:1089-1123)   # _resolved_model computed here; get_model_capability_async already read here
  scope_violation emit (~:1399-1405 region; also runs.py / tool_dispatcher.py / harness/scope.py)   # the informational-SSE event shape to REUSE for the fallback notice

backend/app/services/model_discovery_service.py (Plan 02):
  PROVIDER_ENDPOINTS, discover_all(keyed), compute_diff(current, discovered)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-part no-dead-default 409 guard (disable-path + lock-path) + PUT .../lock lock/unlock semantics (+ its 404 test)</name>
  <files>backend/app/api/admin.py, backend/app/main.py, backend/tests/test_149_default_guard.py</files>
  <read_first>
    - backend/app/api/admin.py (the Plan-05 PATCH handler; the 409-refuse precedent at :401-408 / disable_user self-guard :793-797; the router's require_operator gate :96)
    - backend/app/main.py (_DIRECT_COLUMNS :98-111)
    - backend/app/models/user_settings.py (save_app_settings :266 — how a parameterized app_settings write works; how the app_settings row exposes llm_model + llm_model_locked; load_all_model_overrides from Plan 05 Task 1 — the all-rows read that yields the disabled-id set for the lock-path guard)
    - backend/tests/test_147_operator_kill.py (test_kill_non_operator_404 — the per-route non-operator-404 mirror for a non-GET route)
  </read_first>
  <behavior>
    - PATCH that sets `enabled=false` on the model equal to app_settings.llm_model → 409 with a plain "this is the org default; pick a new default first" (no DB write).
    - PATCH that sets `enabled=false` on the locked model → 409 "unlock it first" (no DB write).
    - An ordinary disable of a non-default, non-locked model → succeeds (direct flip + ✎ receipt, reversible).
    - Locking model X via `PUT /admin/models/{X}/lock` with body `{"locked": true}` when X's effective `enabled` is TRUE → app_settings.llm_model = X AND llm_model_locked = true; any previously-locked model is no longer locked (one lock max — the single llm_model_locked flag + the single llm_model pin enforce it).
    - Locking model X when X's effective `enabled` is FALSE (a `✕ hidden` row) → 409 "model is disabled — enable it first" (no DB write). This closes the dead-default gap: a lock must never pin a disabled model as the org default. 409-refuse (not a hidden auto-enable) keeps it symmetric with the D-149-09 disable guard — honest consequence, no side-effect write.
    - Unlocking (`{"locked": false}`) → llm_model_locked = false (llm_model unchanged).
    - A non-operator JWT gets a byte-identical 404 on `PUT /admin/models/{id}/lock` (the new non-GET route carries its OWN regression test — the 146 gate covers GET only).
  </behavior>
  <action>
    In `main.py`, add `"llm_model_locked"` to `_DIRECT_COLUMNS` (code-constant column; value parameterized in save_app_settings — SQLi-safe). In `admin.py`, add the disable-guard to the Plan-05 PATCH handler: BEFORE the upsert, if the patch would set `enabled=false` AND `model_id == app_settings.llm_model`, raise 409 with the plain org-default explanation; if `model_id` is the locked model (llm_model_locked AND model_id == llm_model), raise 409 "unlock first". Add the lock/unlock endpoint as the dedicated `PUT /admin/models/{model_id}/lock` route (body `{locked: bool}` — this is the settled shape the Plan-04 `setModelLock(modelId, locked)` seam calls; it is SEPARATE from PATCH and must NOT be a model_capabilities_overrides column): with `locked=true`, FIRST resolve the target model's effective `enabled` (a model is disabled only when its `load_all_model_overrides()` row has `enabled=false`; an absent override defaults enabled) — if the target is disabled, raise 409 "model is disabled — enable it first" BEFORE any write (this is what keeps a lock from ever pinning a dead default; refuse rather than silently auto-enable, mirroring the disable-guard's honest-consequence shape); otherwise write `save_app_settings({"llm_model": model_id, "llm_model_locked": True})` and stamp ✎ `model.lock` (plain label naming the model as the org default); with `locked=false`, write `{"llm_model_locked": False}` and stamp ✎ `model.unlock`. The single `llm_model_locked` flag + single `llm_model` pin guarantee "at most one lock" structurally. Add `test_149_default_guard.py`: disable-default → 409 no-write; disable-locked → 409 no-write; disable-ordinary → success; lock X (enabled) → llm_model==X & llm_model_locked True; `test_lock_disabled_model_409` — lock a DISABLED model → 409 with NO save_app_settings write (assert the write path is not reached); unlock → llm_model_locked False; AND `test_lock_non_operator_404` — a non-operator JWT gets a 404 on `PUT /admin/models/{id}/lock` (mirror test_147_operator_kill.py::test_kill_non_operator_404).
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_149_default_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `main._DIRECT_COLUMNS` contains `"llm_model_locked"`
    - Lock/unlock is a dedicated `PUT /admin/models/{model_id}/lock` endpoint (body `{locked}`) — separate from PATCH, matching the Plan-04 setModelLock seam
    - Disabling the org-default model → 409 with a plain-language detail, and NO upsert occurs (test asserts the write path is not reached)
    - Disabling the locked model → 409 "unlock first"; an ordinary disable succeeds
    - Locking a currently-disabled model → 409 "model is disabled — enable it first" with NO save_app_settings write (test `test_lock_disabled_model_409` asserts the write path is not reached) — the lock guard + the disable guard TOGETHER guarantee no dead default can exist
    - Lock writes `llm_model` + `llm_model_locked=true` via the code-constant allowlist (no free-text column); unlock clears `llm_model_locked`
    - Lock/unlock stamp ✎ `model.lock` / `model.unlock` receipts
    - `test_149_default_guard.py` asserts a non-operator JWT gets 404 on `PUT /admin/models/{id}/lock` (the new non-GET route's own regression test)
    - `pytest tests/test_149_default_guard.py` exits 0
  </acceptance_criteria>
  <done>No dead default is possible — the disable guard AND the lock guard both refuse (409) rather than create one; the single lock pins an ENABLED org default via the dedicated PUT .../lock endpoint + the app_settings allowlist; guards + lock are audited; the lock route carries its own non-operator 404 test; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /admin/models/discover — operator-gated discovery run (+ its 404 test)</name>
  <files>backend/app/api/admin.py, backend/tests/test_149_discover_endpoint.py</files>
  <read_first>
    - backend/app/api/admin.py (the router gate :96; the recorded-write receipt shape; the run_in_threadpool discipline for any supabase-py write)
    - backend/app/services/model_discovery_service.py (Plan 02 — discover_all, compute_diff, PROVIDER_ENDPOINTS)
    - backend/app/models/user_settings.py (load_all_model_overrides — for the current registry union)
    - backend/app/config.py (MODEL_CAPABILITIES — the built-in half of the current registry union)
    - backend/tests/test_147_operator_kill.py (test_kill_non_operator_404 — the per-route non-operator-404 mirror for a non-GET route)
  </read_first>
  <behavior>
    - `POST /admin/models/discover` resolves the keyed providers from settings (a no-key provider is skipped, honest), calls `discover_all`, builds the current registry union (MODEL_CAPABILITIES ∪ load_all_model_overrides), calls `compute_diff`, and returns the ephemeral diff (no proposals table).
    - If the request body includes a provider-selection list, any value not in `set(PROVIDER_ENDPOINTS)` → 422 BEFORE any fan-out (no client URL ever reaches the HTTP client — SSRF-safe).
    - The run stamps a ✎ `model.discover` receipt.
    - A non-operator JWT gets a byte-identical 404 on `POST /admin/models/discover` (its OWN regression test — the new non-GET route; the 146 gate covers GET only).
  </behavior>
  <action>
    Add `POST /admin/models/discover` to the `admin.py` router with `_floor: None = Depends(operator_audit_floor)`. Resolve the keyed provider map from `settings` (provider → key-or-None). If the (optional) body carries a `providers` selection, validate each against `set(PROVIDER_ENDPOINTS)` and 422 on any unknown BEFORE calling the service (mirror the `_MODEL_CAP_COLUMNS` allowlist discipline — no client-supplied URL/base). Call `discover_all(keyed)`; assemble the current registry union from `MODEL_CAPABILITIES` keys + `load_all_model_overrides()`; call `compute_diff(current, discovered)`; return the diff (JSON-serializable, ephemeral). Stamp `request.state.audit_action = "model.discover"` + a plain label ("Ran model discovery"). Do NOT persist proposals (D-149-12). Add `test_149_discover_endpoint.py`: a bad provider selection → 422 with no fan-out (mock the service and assert it is never called); a happy path (mock discover_all + a synthetic current) → returns new/changed/vanished groups + stamps `audit_action == "model.discover"`; AND `test_discover_non_operator_404` — a non-operator JWT gets a 404 on `POST /admin/models/discover` (mirror test_147_operator_kill.py::test_kill_non_operator_404).
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_149_discover_endpoint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `POST /admin/models/discover` exists on the operator router and returns the diff (new/changed/vanished)
    - A provider selection value not in `set(PROVIDER_ENDPOINTS)` → 422 BEFORE `discover_all` is called (test asserts the service is not invoked on the reject path)
    - The endpoint stamps `audit_action == "model.discover"`
    - No `discovery_proposals`/persistence table is written (the diff lives only in the response)
    - `test_149_discover_endpoint.py` asserts a non-operator JWT gets 404 on `POST /admin/models/discover` (the new non-GET route's own regression test)
    - `pytest tests/test_149_discover_endpoint.py` exits 0
  </acceptance_criteria>
  <done>Discovery runs behind the operator gate, validates provider selection against the code-constant allowlist, returns the ephemeral diff, records a ✎ receipt, and carries its own non-operator 404 test; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enabled-enforcement + fallback notice at the shared resolution seam (D-149-10)</name>
  <files>backend/app/api/threads.py, backend/tests/test_149_fallback_notice.py</files>
  <read_first>
    - backend/app/api/threads.py (the resolution seam :1089-1123 where `_resolved_model` is computed + get_model_capability_async is already called; the scope_violation informational-SSE emit region ~:1399-1405 for the event shape to reuse)
    - backend/app/models/user_settings.py (load_all_model_overrides — the cached disabled-id set; the app_settings.llm_model org default)
  </read_first>
  <behavior>
    - If the resolved model has a disabled override (enabled=false), the run uses the org default (app_settings.llm_model) instead, and an honest inline SSE notice is emitted naming BOTH models ("modelX was disabled by your administrator — this reply used modelY").
    - If the resolved model is enabled or has no override, resolution is byte-identical to today (no notice, no behavior change — the Deep/shared path is untouched).
    - The check reads the cached all-rows/disabled set (30s TTL — no per-request DB read); it does NOT alter the enabled-only hot cache.
    - The enforcement lives at the ONE shared resolution point — no per-provider fork.
  </behavior>
  <action>
    At the `threads.py` resolution seam (~:1090, right after `_resolved_model` is determined), add a minimal in-place enforcement guard: if `_resolved_model` is in the cached disabled set (from `load_all_model_overrides()`), set `_resolved_model` to the org default `app_settings.llm_model` (which the Plan-06 Task-1 guards guarantee is always enabled — BOTH the disable guard, which refuses disabling the current org default/locked model, AND the lock guard, which refuses locking a disabled model, so the pinned org default is never disabled — no dead default) and emit an informational SSE notice naming both models, REUSING the existing informational-event shape (grep the `scope_violation` emit path and mirror its event/channel shape rather than inventing a new event; Claude's discretion on the exact event name if an existing one fits). Keep the change additive and single-seam — do NOT branch per provider, do NOT grow the endpoint, do NOT touch the shared chunk/SSE emitter (D-14 red line). Add `test_149_fallback_notice.py`: a disabled resolved model → resolution falls back to the org default AND an SSE notice naming both models is emitted; an enabled model → no fallback, no notice (byte-identical path).
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_149_fallback_notice.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The resolution seam falls back to `app_settings.llm_model` when the resolved model is disabled (test asserts the effective model becomes the org default)
    - An informational SSE notice naming BOTH models is emitted on the fallback path (test asserts both model names appear in the emitted notice)
    - An enabled/no-override model produces NO notice and NO fallback (the shared path is unchanged)
    - The disabled check uses the cached all-rows set (no new per-request DB query; the enabled-only hot cache is not modified)
    - `pytest tests/test_149_fallback_notice.py` exits 0
  </acceptance_criteria>
  <done>A disabled model never breaks a conversation — the next message runs on the org default with an honest two-model notice; the shared path stays byte-identical otherwise; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /admin/models/discover | Operator-authed; provider selection must be a code-constant, never a URL |
| client → /admin/models/{id}/lock | Operator-authed non-GET route; a non-operator must get a byte-identical 404 (own regression test) |
| request path → model resolution | A disabled model must not route; the fallback must be honest and non-silent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-14 | Tampering / Info-disclosure (SSRF) | discover endpoint | mitigate | Provider selection validated against `set(PROVIDER_ENDPOINTS)`; no client URL reaches the HTTP client (Pitfall 7) |
| T-149-15 | Availability (dead default) | disable guard + lock guard | mitigate | 409 refuses (a) disabling the org-default/locked model AND (b) locking a currently-disabled model, both BEFORE any write — the two guards together guarantee no dead default can exist (D-149-09); the fallback target is therefore always enabled |
| T-149-16 | Repudiation / integrity (silent routing change) | fallback notice | mitigate | The fallback is honest + non-silent (names both models via an existing informational SSE event); never breaks mid-conversation |
| T-149-17 | Repudiation | lock/discover writes | mitigate | ✎ `model.lock` / `model.unlock` / `model.discover` receipts via operator_audit_floor |
| T-149-21 | Elevation of Privilege (SC#4) | PUT .../lock + POST .../discover | mitigate | Both new non-GET routes inherit the router `require_operator` 404 gate; each carries its OWN non-operator 404 regression test (147/148 precedent — the 146 auto-gate covers GET only) |
| T-149-SC | Tampering | package installs | accept | Zero new packages |
</threat_model>

<verification>
- `pytest tests/test_149_default_guard.py tests/test_149_discover_endpoint.py tests/test_149_fallback_notice.py` green.
- No dead default possible (the disable guard refuses disabling the default/locked model; the lock guard refuses locking a disabled one); single lock pins an ENABLED org default via the dedicated PUT .../lock; discovery SSRF-gated + audited; disabled-model fallback honest.
- Each new non-GET route (PUT .../lock, POST .../discover) carries its own non-operator 404 regression test.
- threads.py touch is a single-seam additive guard (no per-provider fork; shared SSE emitter untouched).
</verification>

<success_criteria>
- Disabling the org-default/locked model is refused AND locking a disabled model is refused (the two-part no-dead-default guard); locking pins the single ENABLED org default via `PUT .../lock`; discovery runs operator-gated + audited; a disabled model falls back with an honest notice.
- The two new non-GET routes each have an explicit non-operator 404 test (ROADMAP red line satisfied).
- The shared model-resolution path stays byte-identical for enabled models (Deep untouched).
</success_criteria>

<output>
Create `.planning/phases/149-model-registry-discovery/149-06-SUMMARY.md` when done.
</output>
</output>
</output>
