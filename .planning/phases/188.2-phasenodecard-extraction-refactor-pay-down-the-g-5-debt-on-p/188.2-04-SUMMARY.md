---
phase: 188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p
plan: 04
subsystem: frontend
tags: [typescript, verbatim-move, leaf-module, types-only, esm-cycle, wr-04, prototype-pollution, react]

# Dependency graph
requires:
  - plan: 188.2-01
    provides: "`CARD_SUBTREE_PATHS`, whose path list already named `./phaseNodeCardContract.ts` and `./ownProperty.ts` before either existed — so the 17 negative fences follow the code to its new home, and the ESM-cycle fence is in place before the modules it guards"
  - plan: 188.2-03
    provides: "`CARD_HTML_BASELINE` + `CARD_SHAPE_BASELINE` / `CARD_READING_SHAPES` / `CARD_VERDICT_SHAPES` / `CARD_BORDER_SHAPES` — the pre-move rendered shape this plan had to leave untouched, and did (zero re-capture, zero production edits)"
  - phase: 188.1
    provides: "the proven split of an extraction into *create additively*, then *cut*; the leaf-header shape; and the rule that a misplaced docblock moves misplaced"
provides:
  - "`frontend/src/components/workflows/phaseNodeCardContract.ts` — the six slot-contract types, moved verbatim (empty span `diff`), 214 L, ZERO runtime exports"
  - "`frontend/src/components/workflows/ownProperty.ts` — `own<T>()` as a zero-import leaf, function body byte-identical (empty span `diff`), 86 L"
  - "D-05 recorded IN PLACE: the superseded two-copies decision, the third option, and the constraint that still holds"
  - "first live resolution of two of the five destination paths in the 188.2-01 subtree fence — observed RED twice, from inside each new file"
affects: [188.2-05, 188.2-06, 188.2-07, 189]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "assemble a verbatim move BY SCRIPT (`git show HEAD:<file> | sed -n 'A,Bp'` piped straight into the destination), never by re-typing — the span `diff` is then empty by construction rather than by care"
    - "a types-only leaf under `verbatimModuleSyntax: true`: every import is `import type`, `noUnusedLocals` still applies, and the module emits no runtime code at all"
    - "prose must not quote its own acceptance needle — a docblock that spells the guard an acceptance grep counts makes that count lie"

key-files:
  created:
    - frontend/src/components/workflows/phaseNodeCardContract.ts
    - frontend/src/components/workflows/ownProperty.ts
  modified: []

key-decisions:
  - "The STATE OF THE EXTRACTION paragraphs say what is TRUE AT THIS COMMIT — the card still declares its own copies — rather than the plan's literal 'were CUT … it is a HARD CUT'. At an additive commit that wording would have shipped a false docblock, which is the exact defect the phase's verbatim-move rule exists to prevent. The hard-cut contract is stated as 188.2-06's, explicitly."
  - "Neither header spells `Object.prototype.hasOwnProperty.call` or the ES2022 static alternative, and the reason is written inline: an acceptance grep counts the guard's occurrences in `ownProperty.ts`, so quoting the needle in prose would have made the file read 2 where the contract is 1."
  - "The `:139` section separator moved verbatim, in place, as the new file's own in-body separator (the plan allowed either that or folding it into the header). Keeping it in place is what makes the 147-line span `diff` exactly empty."

patterns-established:
  - "Prove a re-scoped fence really REACHES a newly created file by planting a violation INSIDE that file and observing the named fence RED — a fence that merely lists a path is not known to be reading it."
  - "On Windows with `core.autocrlf=true`, a script-assembled file arrives LF while its siblings are CRLF. Normalise the working tree (`sed -i 's/$/\\r/'`) and re-`git add`: the blob is unchanged, so the tree stops being cosmetically inconsistent without touching history."

requirements-completed: []  # EMPTY BY DESIGN — ROADMAP §Phase 188.2: "Requirements: none — refactor phase."

# Metrics
duration: 25min
completed: 2026-08-07
---

# Phase 188.2 Plan 04: Create the two camelCase leaves Summary

**The card's slot contract and its WR-04 own-guard now exist as their own leaves — 300 lines moved with an EMPTY span `diff` on both, `PhaseNodeCard.tsx` byte-unchanged at 797 L, and the 188.2-01 subtree fence proven to reach each new file by planting a violation inside it and watching the named guard go RED.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-08-06T22:32:00Z
- **Completed:** 2026-08-06T22:57:00Z
- **Tasks:** 2
- **Files created:** 2 · **Files modified:** 0

## Accomplishments

- `phaseNodeCardContract.ts` — **214 L**, holding `PhaseNodeCard.tsx:139-285` (147 L) moved verbatim. **6** exported type names, **0** runtime exports, **5** `import type` statements, **0** value imports.
- `ownProperty.ts` — **86 L**, holding `PhaseNodeCard.tsx:289-310` (22 L) moved verbatim with `export` added to the signature. **0** imports of any kind.
- **Both span `diff`s are EMPTY.** Neither file was re-typed: both were assembled by piping `git show HEAD:… | sed -n 'A,Bp'` straight into the destination, so verbatim-ness is a property of the construction rather than of care taken.
- **The subtree fence was observed RED from inside each new file** — proof that naming a path in `CARD_SUBTREE_PATHS` and actually reading that path are different claims, and that this plan closed the gap for two of the five destinations.
- **`PhaseNodeCard.tsx`, `PhaseNode.tsx` and `runVocabulary.ts` are all byte-unchanged.** `git diff --stat` reports 0 files changed for all three at both commits.
- **Plan 03's baselines stayed green with ZERO re-capture** — `CARD_HTML_BASELINE`, `CARD_SHAPE_BASELINE`, `CARD_READING_SHAPES`, `CARD_VERDICT_SHAPES` and `CARD_BORDER_SHAPES` were not touched, and the suite is 124/124 at both commits.

## Task Commits

1. **Task 1: the slot contract as a types-only leaf** — `82dd29a7` (refactor)
2. **Task 2: `own<T>()` as a zero-import leaf (D-05)** — `155b3832` (refactor)

## Recorded measurements (verbatim, as `<output>` requires)

### The two span `diff`s proving the moves

**Task 1 — `PhaseNodeCard.tsx:139-285` (147 L) against `phaseNodeCardContract.ts:68-214`:**

```
$ git show HEAD:frontend/src/components/workflows/PhaseNodeCard.tsx | sed -n '139,285p' > $SCR/contract-src.txt
$ sed -n '68,214p' frontend/src/components/workflows/phaseNodeCardContract.ts > $SCR/contract-dst.txt
$ diff $SCR/contract-src.txt $SCR/contract-dst.txt
<<<EMPTY DIFF — VERBATIM>>>
```

The `diff` output is **empty**. The only authorised changes therefore live strictly ABOVE line 68: the 66-line header docblock and the five `import type` lines. Line 68 is the moved `// ── The slot contract ──…` separator, kept in place verbatim.

**Task 2 — the function body, `PhaseNodeCard.tsx:307-310` against `ownProperty.ts:83-86`:**

```
$ git show HEAD:frontend/src/components/workflows/PhaseNodeCard.tsx | sed -n '307,310p' > $SCR/own-body-src.txt
$ sed -n '83,86p' frontend/src/components/workflows/ownProperty.ts | sed 's/\r$//' > $SCR/own-body-dst4.txt
$ diff $SCR/own-body-src.txt $SCR/own-body-dst4.txt
<<<EMPTY — BODY BYTE-IDENTICAL>>>
```

**Task 2 — the moved docblock's unedited head, `:289-300` against `ownProperty.ts:55-66`:**

```
$ diff $SCR/own-doc-src.txt $SCR/own-doc-dst.txt
<<<EMPTY — VERBATIM>>>
```

**Task 2 — the ONE authorised difference, `:305-306` against `ownProperty.ts:81-82`:**

```
2c2
< function own<T>(table: Record<string, T>, key: string): T | undefined {
---
> export function own<T>(table: Record<string, T>, key: string): T | undefined {
```

That is the whole delta on the declaration: the `export` keyword, which is what a module-private helper needs in order to become a shared one.

### Measured file sizes (Plan 06's before/after table reads these)

| File | `wc -l` | Plan floor | Composition |
|---|---|---|---|
| `phaseNodeCardContract.ts` | **214** | ≥ 150 | 66 header + 1 blank + 147 moved |
| `ownProperty.ts` | **86** | ≥ 25 | 54 header + 12 moved doc head + 15 D-05 + 5 closer/signature/body |
| `PhaseNodeCard.tsx` | **797** | — | **unchanged**, at both commits |

### The rewritten D-05 paragraph, quoted verbatim

```
 * ⚠ D-05 — THE ONE PARAGRAPH THAT DID NOT MOVE UNEDITED, AND WHY. What stood here read
 * *"WRITTEN LOCALLY RATHER THAN IMPORTED, deliberately … One concept, two small private
 * copies, is cheaper here than one shared function with a blast radius."* That decision
 * is SUPERSEDED rather than deleted, and it is recorded here so the reasoning it replaces
 * stays readable. It weighed exactly TWO options — a second private copy in the card, or
 * exporting and WIDENING `runVocabulary.ts`'s `own()`, which is module-private, typed to
 * `CanvasReading` rather than to a bare key, and whose signature that file's own `?raw`
 * fences constrain. Both premises still hold. What changed is the arithmetic: 188.2 splits
 * the card's two sinks across two DIFFERENT destination modules, so "two small private
 * copies" became THREE, and a small dedicated leaf is a third option the paragraph above
 * never had to consider. Three copies is what it loses to. THE CONSTRAINT IT NAMED IS
 * UNTOUCHED: `runVocabulary.ts:84-88` keeps its own private copy with its different
 * second-parameter type, called at its four sites, and its fences stay exactly as shipped
 * — this module does not import it, widen it, or ask it to change.
```

It names all three things the criterion requires: the two-copies decision it supersedes (quoted, not paraphrased), the third option and why it exists (two sinks, two destinations, three copies avoided), and the fact that `runVocabulary`'s differently-signed copy and its fences stay put.

### Acceptance greps

| Grep | Expected | Measured |
|---|---|---|
| `grep -c "^export type\|^export interface" phaseNodeCardContract.ts` | 6 | **6** |
| `grep -c "^export const\|^export function\|^export let" phaseNodeCardContract.ts` | 0 | **0** |
| `grep -c "import type" phaseNodeCardContract.ts` | 5 | **6** — see corrections below; `grep -c "^import type"` is **5** |
| `grep -Ec "^import \{" phaseNodeCardContract.ts` | 0 | **0** |
| `grep -Ec "^import" ownProperty.ts` | 0 | **0** |
| `grep -c "Object.prototype.hasOwnProperty.call" ownProperty.ts` | 1 | **1** |
| `grep -c own runVocabulary.ts` | unchanged | **23 → 23** |
| `grep -c "phaseNodeCardContract" PhaseNodeCard.test.tsx` | ≥ 1 (key_links) | **3** |

### Gates

| Gate | Command | Task 1 | Task 2 |
|---|---|---|---|
| Type | `npx tsc --noEmit -p tsconfig.app.json \| grep -c "error TS"` | **33** | **33** |
| Type (scope) | `… \| grep -c "src/components/workflows"` | **0** | **0** |
| Type (new file) | `… \| grep -c "phaseNodeCardContract"` | **0** | — |
| Lint | `npx eslint src/components/workflows/` | **5 errors, 0 warnings** | **5 errors, 0 warnings** |
| Suite | `npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` | **124 passed (124)**, 0 failed | **124 passed (124)**, 0 failed |
| Count gate | `node scripts/vitest-count-gate.cjs` | — | **`count gate OK`** — 45/45 pinned files present, no per-file decrease, **failed 0**; `total 2482 2502 +20` |
| Additivity | `git diff --stat -- PhaseNodeCard.tsx PhaseNode.tsx runVocabulary.ts` | **0 files changed** | **0 files changed** |
| T-188.2-SC | `git diff --stat -- frontend/package.json frontend/package-lock.json backend/ supabase/migrations/ scripts/` | **0 files changed** | **0 files changed** |
| Scope | `git status --porcelain -- frontend/` | one `??` (the new file) | one `??` (the new file) |

The suite count is **unchanged at 124** across both commits — this plan adds no test and changes no behaviour, exactly as its acceptance criterion requires. The count-gate total is **2502**, identical to Plan 03's exit.

## RED observations (full failure text)

Neither new file's membership in `cardSubtreeSource` was taken on trust. A fence that merely LISTS a path is not known to be reading it — `import.meta.glob` contributes the empty string for a path with no file, silently — so one violation was planted inside each new file and the named guard observed RED, then reverted.

### Task 1 — N5, the zero-graph-library property (D-184-06), planted in `phaseNodeCardContract.ts`

```
× the card names the canvas graph package NOWHERE (D-184-06)
FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard — the scope fences
     (source guard) > the card names the canvas graph package NOWHERE (D-184-06)
AssertionError: expected '/**\r\n * Phase 184-03 Task 2 (D-184-…' not to contain '@xyflow'
    474|   it("the card names the canvas graph package NOWHERE (D-184-06)", () …
```

### Task 2 — N10, the `dangerouslySetInnerHTML` XSS ban, planted in `ownProperty.ts`

```
× the card renders authored strings as text children — never the HTML sink
FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard — the scope fences
     (source guard) > the card renders authored strings as text children — never the HTML sink
AssertionError: expected '/**\r\n * Phase 184-03 Task 2 (D-184-…' not to match /dangerouslySetInnerHTML=/
Tests  1 failed | 123 passed (124)
```

Both plants were reverted from a byte-copy backup (`grep -c "PLANTED"` → **0**, `wc -l` back to 214 / 86) and the suite re-verified at **124 passed (124)** before either commit. **These are the first two of the five destination paths in `CARD_SUBTREE_PATHS` to resolve to a real file**, so the non-vacuity line Plan 01 authored (`if (path in CARD_MODULES) expect(CARD_MODULES[path].length).toBeGreaterThan(0)`) fires for real for the first time here.

## Files Created/Modified

- **Created** `frontend/src/components/workflows/phaseNodeCardContract.ts` (214 L) — the six slot-contract types with their prose, plus a header recording the extraction state, the deliberately-stale `:NNN` references, the fences that read this file, and the measurement that it is the directory's first types-only module.
- **Created** `frontend/src/components/workflows/ownProperty.ts` (86 L) — `own<T>()` exported from a zero-import leaf, its docblock moved unedited except for the D-05 paragraph.
- **Modified: nothing.** No existing file in the repository was changed by either commit.

## Decisions Made

1. **The extraction paragraphs state what is TRUE AT THIS COMMIT.** The plan's literal wording was *"these six types were CUT out of `PhaseNodeCard.tsx` … it is a HARD CUT with NO re-export shim, and the card will import them back from here."* At an ADDITIVE commit the first clause is false — the card still declares all six and imports none — so both headers say so in bold and attribute the hard cut to `188.2-06` as its contract. See Deviations, Rule 1.
2. **Neither header spells its own acceptance needle.** `ownProperty.ts`'s criterion counts `Object.prototype.hasOwnProperty.call` occurrences in the file and requires **1**; a header sentence quoting the guard would have made it read 2. The same applies to the ES2022 static alternative, whose "zero occurrences in `frontend/src`" claim a prose mention would falsify at the moment of writing it. Both are referred to obliquely, with the reason recorded inline so a later author does not "fix" the omission.
3. **The `:139` separator moved in place** rather than being folded into the header — the plan allowed either. In place is what makes the 147-line span `diff` exactly empty rather than "empty except one line".
4. **Both files were assembled by script, never re-typed.** `git show HEAD:<file> | sed -n 'A,Bp'` piped directly into the destination. The empty `diff`s are therefore a property of the construction; re-typing 169 lines of prose and then checking would have been the same evidence with a worse failure mode.
5. **Working-tree line endings normalised to CRLF after assembly.** `core.autocrlf=true` and every sibling in the directory ships CRLF in the working tree; the script-assembled files arrived LF. `sed -i 's/$/\r/'` + `git add` left the blob byte-identical (`git status` clean, `git diff` empty), so the tree is consistent without a second commit.

## Deviations from Plan

### Auto-fixed / auto-added

**1. [Rule 1 — Bug] The plan's literal STATE OF THE EXTRACTION wording would have shipped a false docblock**

- **Found during:** Task 1, while writing the header.
- **Issue:** The plan instructs both headers to say the types "were CUT out of `PhaseNodeCard.tsx`" and that "it is a HARD CUT with NO re-export shim". Neither is true at this commit: this plan is ADDITIVE BY DESIGN (its own `must_haves` say so), `PhaseNodeCard.tsx` still declares every one of the seven moved declarations, and `git diff --stat` on it reports 0 files changed. A docblock that runs ahead of its own tree is precisely the drift these headers exist to prevent — 188.1's own rule is *"read this literally, it is not a claim about the future."*
- **Fix:** Both headers state the additive position in bold (**"AT THIS COMMIT THE CARD STILL DECLARES ITS OWN COPIES AND IMPORTS NONE OF THESE"**), then attribute the hard-cut contract to `188.2-06` explicitly, and `phaseNodeCardContract.ts` adds a sentence saying why no completed-cut sentence is written. Every other element the plan required of the paragraph — the `:139-285` / `:289-310` declaration sites, the 797 L pre-move measurement at `42b42cb7`, the no-shim contract, the stale-`:NNN` warning, the fences paragraph — is present verbatim.
- **Files:** both new files. **Commits:** `82dd29a7`, `155b3832`.

**2. [Rule 2 — Missing critical functionality] The subtree fence's reach into each new file was PROVEN, not assumed**

- **Found during:** both tasks.
- **Issue:** Plan 01 shipped `CARD_SUBTREE_PATHS` naming five files that did not exist, and a `?? ""` that makes an absent path contribute the empty string **in silence**. Creating a file whose path is listed does not by itself prove the glob matched it — and a fence reading an empty string looks exactly like a fence that holds. The plan asked for no such proof.
- **Fix:** One violation planted inside each new file, the named guard observed RED (full text above), then reverted from a byte-copy backup and the suite re-verified green. Two of the seventeen negative fences are now known to read the new files rather than assumed to.
- **Files:** none committed — both plants reverted before commit.

**Total deviations:** 2 — one Rule 1, one Rule 2. None architectural. Neither touched a production file, an existing test, a package manifest or a baseline.

## Inherited claims re-measured (per the standing "don't inherit unmeasured claims" rule)

Nineteen claims were re-derived before being relied on. **Sixteen held exactly. Three are corrected.**

| # | Claim | Verdict |
|---|---|---|
| 1 | `phaseNodeCardContract.ts` ← `PhaseNodeCard.tsx:139-285`, 147 L | **TRUE** — `sed -n '139,285p' \| wc -l` = 147 exactly, `:139` is the separator, `:215` is `export interface PhaseNodeCardProps {`, `:285` is its `}`. |
| 2 | `ownProperty.ts` ← `:289-310`, 22 L | **TRUE**, exactly. `:289` is `/**`, `:306` the signature, `:310` the closing brace. |
| 3 | `:287` is the RING separator and does NOT travel here (RESEARCH's `287-311` is imprecise) | **TRUE** — `:287` is `// ── The status ring's constants (188-06 · sketch 153-A) ──`. Excluded, as the plan corrects. |
| 4 | `const RING_RADIUS` is at `:315` (Plans 01 and 03 both corrected `:313`) | **TRUE at `:315`.** The plan did not re-inherit the stale figure. |
| 5 | `PhaseNodeCard.tsx` measured **797 L** at HEAD `42b42cb7` | **TRUE** — and still 797 at `6680295e`, the commit this plan started from. |
| 6 | The card's `:113` is a VALUE import `import { StatusChip, type ChipTone }` | **TRUE**, exactly at `:113`. |
| 7 | `verbatimModuleSyntax: true` in `tsconfig.app.json` | **TRUE** (`:15`). Also `noUnusedLocals: true`, so all five type imports had to be reachable — all five are. |
| 8 | **11 of 11** `.ts` modules in the directory are camelCase | **TRUE** — exactly the eleven the plan lists, no twelfth, no exception. |
| 9 | No exact analog exists: all 11 export at least one runtime value | **TRUE**, measured per file (2–43 runtime exports each). `phaseNodeCardContract.ts` really is the directory's first types-only module. |
| 10 | `runVocabulary.ts`'s `own()` is module-private, `CanvasReading`-typed, called at **four** sites | **TRUE** — `:84`, called at `:95`, `:156`, `:248`, `:338`. Untouched by this plan. |
| 11 | `Object.hasOwn` has **zero** occurrences in `frontend/src` | **TRUE** — and this plan introduces no second spelling (the header deliberately does not spell it either). |
| 12 | Baselines: `tsc` 33 with 0 in `src/components/workflows/`; `eslint` 5 errors / 0 warnings | **TRUE**, and both unmoved at both commits. |
| 13 | Entering suite count 124 (Plan 03's exit) | **TRUE**, and unchanged leaving. |
| 14 | The five type-only imports the contract needs | **TRUE** — all five resolve, all five are used, and none of the four target modules imports `PhaseNodeCard` (`StatusChip.tsx` 0, `lib/phaseState.ts` 0, `types/index.ts` 0; `nodePresentation.ts` names it 4× **in prose only**, never in an import). No cycle is reachable. |
| 15 | `PhaseNodeCard.test.tsx`'s path list already names both new files | **TRUE** — `CARD_SUBTREE_PATHS:95-96`, and the glob `./*.{ts,tsx}` matches both, now proven by RED rather than by reading. |
| 16 | The plan's `must_haves` leaf rules (camelCase `.ts`, zero imports, no `PhaseNodeCard` specifier) | **TRUE** — all three hold by construction and are measured above. |
| 17 | `grep -c "import type" phaseNodeCardContract.ts` returns **5** | **FALSE — measured 6.** Five are the import statements; the sixth is the header sentence explaining why every import below is `import type`. `grep -c "^import type"` is **5** and `grep -Ec "^import"` is **5**, which is the property the criterion is actually about. The plan counted statements, not lines — the same class of arithmetic slip Plan 01 recorded for `phaseNodeCardSource`. |
| 18 | The two shipped WR-04 falsifications are at `PhaseNodeCard.test.tsx:2000-2063` | **FALSE — they are at `:2214-2288`** (block comment `:2214`, docblock `:2216-2231`, `describe(` at `:2232`, site 4 at `:2233`, site 5 at `:2252`). `:2000-2063` is the 188-06 card-body-budget block. The plan's figure was measured against the pre-188.2-01 file, which was 2063 lines TOTAL; Plans 01 and 03 added +225 and +1387, taking it to 3675. Content matched the description exactly — only the anchors rotted. Both falsifications are green and untouched. |
| 19 | `nodePresentation.ts:145-146` carries the sentence that goes stale | **FALSE — it is at `:144-145`.** Off by one. See the OWED note below, which quotes it from the measured lines. |

Also a near-miss worth recording rather than counting: the plan cites `editAffordance.ts:42-44` for the "collapsing a duplication is a behaviour-shaped change owed its own phase" rule. Measured, the sentence spans **`:41-44`** — `:42-44` truncates its first clause. Content exactly as described.

## OWED TO PLAN 06 (recorded here so it cannot be forgotten between waves)

**`nodePresentation.ts:144-145` will be FALSE after the 188.2-06 cut.** It currently reads, verbatim:

```
 * rather than of any finding. `PhaseNodeCard.NodeVerdictMark` is an alias of this type,
 * kept so every existing caller's name still resolves.
```

`NodeVerdictMark` now lives in `phaseNodeCardContract.ts`, not on `PhaseNodeCard`. **This plan deliberately did NOT edit it** — `nodePresentation.ts` is outside this plan's `files_modified`, and D-01 forbids folding a correction into a move commit. The sentence is still literally true at this commit (the card also still declares the alias), so it goes false only when Plan 06 cuts. Plan 06 owns the one-line correction: the qualified name becomes `phaseNodeCardContract.NodeVerdictMark`.

Also owed forward, unchanged from Plan 01's list and neither advanced nor blocked by this plan:

- **Plan 05:** the ESM-cycle fence's second `it(` still skips **three** of five paths (`NodeCornerMarks.tsx`, `NodeRunOverlay.tsx`, `NodeIconWell.tsx`); it now runs for real on the two created here, both trivially green (zero and five type-only imports, none naming the card). C-5's RED against a planted back-import is still owed.
- **Plan 05:** N10's `dangerouslySetInnerHTML=` ban must still be observed RED at the seal's authored-string render site in `NodeCornerMarks.tsx` (T-188.2-02). The RED recorded above is from `ownProperty.ts` and is a fence-reach proof, not that plant.
- **Plan 06:** the one-direction fence is still green by vacuity; the matching `import` assertions are its.
- **Plan 07:** the count-gate pin move — `PhaseNodeCard.test.tsx` 107 → 124, total 2482 → 2502. Unchanged by this plan, which adds no test.

## Threat model disposition

| Threat ID | Disposition | Evidence |
|---|---|---|
| T-188.2-14 (WR-04 own-guard moved) | **mitigated** | The four body lines `diff` EMPTY against `PhaseNodeCard.tsx:307-310`; the guard's spelling is the tree's only one and no second was introduced; both shipped falsifications (`:2233`, `:2252`) still drive through the CARD, whose copy is untouched, and both are green in the 124/124 run. Re-verification that they reach the MOVED code is Plan 06's, by construction — nothing imports this module yet. |
| T-188.2-15 (ESM cycle into either leaf) | **mitigated** | `ownProperty.ts` has **0** imports — un-cyclable by construction. `phaseNodeCardContract.ts` imports only types, from four modules none of which imports `PhaseNodeCard` at all (measured; `nodePresentation`'s four mentions are prose). Both are covered by the Plan 01 cycle fence, which now resolves both paths. |
| T-188.2-16 (the move reading as a rewrite) | **mitigated** | Span-level `diff`s pasted above, both empty. The single authorised prose edit is named in the file, named in the plan, and quoted in full here. |
| T-188.2-17 (`emitFailure` docblock) | **accepted, preserved** | Moved inside the verbatim span; the constraint travels with the type. No new surface. |
| T-188.2-SC (package installs) | **mitigated** | `git diff --stat -- frontend/package.json frontend/package-lock.json backend/ supabase/migrations/ scripts/` → **0 files changed** at both commits. Zero packages installed. |

## Issues Encountered

**None blocking.** The count gate reported `failed 0` on its single run here — the flake the orchestrator warned about (measured range 1 → 50 today, all `Axe is already running` or 5 s timeouts, SEED-056) did not recur.

One environment note carried forward rather than fixed: script-assembled files arrive **LF** under `core.autocrlf=true` while every sibling in the working tree is **CRLF**. Normalising with `sed -i 's/$/\r/'` and re-`git add`ing produces a byte-identical blob (`git diff` empty), but `git status --porcelain` reports a stale ` M` until the index stat cache is refreshed — `git add` is the cheapest refresh. Worth knowing for Plan 05, which creates three more files the same way.

## User Setup Required

None.

## Next Phase Readiness

- **Plan 05 receives the contracts rather than going looking for them.** `NodeCornerMarks` and `NodeRunOverlay` can both `import { own } from "@/components/workflows/ownProperty"` and type against `PhaseNodeCardProps` from `phaseNodeCardContract`, and neither import can close a cycle.
- **Plan 06 inherits a clean cut surface:** the card's `:139-285` and `:289-310` are now duplicated, not moved, so the cut is a pure deletion plus two import lines — and the phase's diff still reads as a MOVE.
- **Nothing was re-captured.** Plan 03's five baseline literals are byte-untouched, which is the property the wave ordering exists to protect.
- **No blockers.**

## Self-Check: PASSED

- `FOUND: .planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-04-SUMMARY.md`
- `FOUND: frontend/src/components/workflows/phaseNodeCardContract.ts` (214 L)
- `FOUND: frontend/src/components/workflows/ownProperty.ts` (86 L)
- `FOUND: 82dd29a7` · `FOUND: 155b3832`
- `UNCHANGED (as required): frontend/src/components/workflows/PhaseNodeCard.tsx` (797 L, 0 files changed)
- `UNCHANGED (as required): PhaseNode.tsx · runVocabulary.ts · PhaseNodeCard.test.tsx · scripts/vitest-count-gate.cjs`
- `ABSENT (as required, Plan 05's): NodeCornerMarks.tsx · NodeRunOverlay.tsx · NodeIconWell.tsx`

---
*Phase: 188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p*
*Completed: 2026-08-07*
