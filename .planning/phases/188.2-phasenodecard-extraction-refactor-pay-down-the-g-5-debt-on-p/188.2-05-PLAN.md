---
phase: 188.2
plan: 05
type: execute
wave: 4
depends_on: ["188.2-04"]
files_modified:
  - frontend/src/components/workflows/NodeCornerMarks.tsx
  - frontend/src/components/workflows/NodeIconWell.tsx
  - frontend/src/components/workflows/NodeRunOverlay.tsx
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
autonomous: true
requirements: []
# EMPTY BY DESIGN — refactor phase, no REQ- ids (ROADMAP §188.2).
success_criteria: ["SC#3", "SC#4"]
gap_closure: false

must_haves:
  truths:
    - "D-04: the three absolutely-positioned mark blocks land in three component modules grouped by ONE reason to change each — `NodeCornerMarks` (184's verdict mark + 185's ⛨ seal), `NodeRunOverlay` (188's status ring + pause chip), `NodeIconWell` (the 3D mark) — never one omnibus module that mixes three phases' ownership"
    - "D-01: every JSX block moves with its comments intact, and the seal keeps its `{grounded ? ( … ) : null}` TERNARY shape — `sealJsxBlock` anchors on those two literal strings and THROWS on a miss, so an early-return rewrite is a hard failure rather than a style choice"
    - "R2 holds by construction: each `.tsx` module exports the component and its props type and NO runtime value of any kind — `RING_RADIUS`, `RING_STROKE`, `RING_BOX_CLASSES` and `RING_TRACK_STROKE` stay module-private exactly as they are in the card today, because one `export const` in a `.tsx` file puts the directory's eslint count back to 6 and undoes 188.1's only measurable lint win"
    - "D-14 holds: the new modules take ALREADY-DERIVED props and derive no run state. The `arc` computation moves with the ring because `ringDash(ringSpecFor(reading), C)` is pure geometry over a reading that arrived derived — not a claim about what state a step is in — and this is said out loud so a reviewer does not read it as a breach"
    - "C-2, C-5 and C-8 are each observed RED against a violation planted in a real destination file: the subtree fence bites for all THREE of the fences reachable at this wave — N1 (`onClick=`), N5 (the graph-package scope) and N10 (`dangerouslySetInnerHTML=`, planted at a REAL authored-string render site, because N10 is the one re-scoped negative that carries a security property) — the cycle fence bites on both the value and the type-only back-import, and the no-focusable-control walk reaches the moved JSX"
    - "This plan is ADDITIVE ONLY — three files created, nothing deleted from `PhaseNodeCard.tsx`, and the card still renders through its own inline copies"
  artifacts:
    - path: "frontend/src/components/workflows/NodeCornerMarks.tsx"
      provides: "the verdict mark (left edge) and the ⛨ governance seal (top-right), with own()'s verdict sink"
      contains: "function NodeCornerMarks("
      min_lines: 120
    - path: "frontend/src/components/workflows/NodeRunOverlay.tsx"
      provides: "the status ring, its arc geometry and the pause chip, with own()'s ring-stroke sink"
      contains: "function NodeRunOverlay("
      min_lines: 160
    - path: "frontend/src/components/workflows/NodeIconWell.tsx"
      provides: "the 3D mark floating above the card's top edge, and the clipping-ban prose"
      contains: "function NodeIconWell("
      min_lines: 35
  key_links:
    - from: "frontend/src/components/workflows/NodeCornerMarks.tsx"
      to: "frontend/src/components/workflows/ownProperty.ts"
      via: "the shared own-property read for VERDICT_MARK (D-05 sink 1 of 2)"
      pattern: "own\\(VERDICT_MARK"
    - from: "frontend/src/components/workflows/NodeRunOverlay.tsx"
      to: "frontend/src/components/workflows/ownProperty.ts"
      via: "the shared own-property read for RING_STROKE (D-05 sink 2 of 2)"
      pattern: "own\\(RING_STROKE"
---

<objective>
Create the three presentational modules by moving their JSX blocks verbatim out of
`PhaseNodeCard.tsx`, and prove the three fences that could only be proven once real destination
files existed.

Purpose: D-02 is the reason these three exist. Cutting only the slot contract and the ring
constants would drop the file 797 → 566 and leave the 427-line BODY untouched — satisfying the
ledger while missing the goal, because 189 writes in the body (badge slot 1, the
capability-derived subtitle). The four absolutely-positioned mark blocks are therefore in scope,
and so is their fence risk.

Two constraints bind the shape and neither is negotiable. `sealJsxBlock`
(`PhaseNodeCard.test.tsx:1046-1064`) carves the seal's block by two literal string anchors and
THROWS on a miss, so `NodeCornerMarks` must keep the shipped ternary rather than an early return.
And `react-refresh/only-export-components` means these three modules may export the component and
its props type and nothing else — type-only exports are exempt (measured: `PhaseNodeCard.tsx`
exports six types and contributes zero of the directory's five eslint errors), runtime values are
not, and there are four live violations in this directory to learn from.

This plan is additive. The card keeps its inline copies until Plan 06 cuts them, so at the end of
this plan the card still renders exactly as it did and `CARD_HTML_BASELINE` still holds.

Output: three modules; C-2 observed RED THREE times — N1 (`onClick=`), N5 (the graph-package
scope) and N10 (`dangerouslySetInnerHTML=`), one plant per fence, each in a real destination
file — plus C-5 and C-8 resolved against their own planted violations.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-CONTEXT.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-RESEARCH.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-PATTERNS.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-VALIDATION.md

<interfaces>
<!-- RE-MEASURED at HEAD `42b42cb7` by the planner, 2026-08-06, by printing every boundary line
     below. Re-derive before cutting; Plan 04 added no lines to `PhaseNodeCard.tsx`, so these
     ranges are still valid, but verify that rather than assuming it. -->

THE L1 SOURCE RANGES, by destination. Every range moves VERBATIM — docblocks, inline comments,
`⚠` paragraphs and all.

    NodeCornerMarks.tsx   ← 115 L across three ranges
      :410-434   the verdict derivation — a 24-line WR-04 docblock plus
                 `const mark = verdict ? (own(VERDICT_MARK, verdict) ?? VERDICT_MARK.unknown) : null`
                 (`:434`; this is WR-04 SINK 4)
      :567-599   the verdict-mark JSX  (`-left-2 top-1.5 z-[8]`, 22×22, testid canvas-node-verdict)
      :601-656   the ⛨ governance-seal JSX (`right-[17px] top-[11px] z-[6]`, 21×21,
                 testid canvas-node-seal)
      ⚠ KEEP THE TWO BLOCKS IN THEIR SHIPPED ORDER — verdict ABOVE seal. `sealJsxBlock` does
        `lastIndexOf(SEAL_GUARD_OPEN, indexOf("canvas-node-seal"))`, and its docblock's phrase
        "so it cannot latch onto the verdict mark's structurally identical block above it" is
        only true while that order holds.

    NodeRunOverlay.tsx    ← 153 L across four ranges
      :287        the separator `// ── The status ring's constants (188-06 · sketch 153-A) ──`
                  ⚠ It names the RING, so it travels HERE — not with `own()`, which Plan 04
                  already moved. RESEARCH's cut table folds it into the `own()` range; that is
                  the one place its table is imprecise.
      :312-355    RING_RADIUS (34) · RING_CIRCUMFERENCE · RING_BOX_CLASSES · RING_STROKE ·
                  RING_TRACK_STROKE, each with its docblock — MODULE-PRIVATE today and they
                  STAY module-private (R2)
      :440-443    the `arc` derivation and its 3-line comment
      :658-739    the status-ring JSX (testids canvas-node-ring, canvas-node-ring-arc; carries
                  the `canvas-ring-spin` class at `:734` and WR-04 SINK 5, `own(RING_STROKE,
                  reading)`, at `:731`)
      :741-762    the pause-chip JSX (testid canvas-node-pause-chip)

    NodeIconWell.tsx      ← 31 L, one range
      :764-794    the JSX comment (`:764-772`) plus the four-`<span>` well (`:773-794`)
      ⚠ `:772` IS THE TREE'S ONLY MENTION OF THE CLIPPING UTILITY and it moves with this block.
        Plan 01 already re-anchored the count fence to the subtree, so the count stays 1. Do not
        re-word that sentence and do not add a second mention anywhere in the subtree.

WHAT STAYS IN THE CARD — do not move these, they are Plan 06's problem and they belong to the
card, not to an overlay:
      :357-369   RUN_MODE_NODE_MIN_HEIGHT   feeds the card's own minHeight at :444, :453, :498
      :438-439   `reading` and `runBorder`  runBorder drives the card's border ternary :490-496
      :444       `minHeight`                the node box
      :446-565   the node box and the 137-B card div

D-14, ANSWERED OUT LOUD (RESEARCH Open Q3) — moving the `arc` derivation is NOT a red-line
breach. `ringDash(ringSpecFor(reading), RING_CIRCUMFERENCE)` is a pure geometry computation over
an ALREADY-DERIVED reading; D-14 forbids deriving *what state a step is in*, and the one
derivation that does that lives in `frontend/src/lib/phaseState.ts` and is fenced by
`phaseState.test.ts`. Say this in `NodeRunOverlay`'s docblock so a reviewer does not read it as a
breach. The warning sign to stay clear of: any new import of `@/lib/phaseState`'s FUNCTIONS (as
opposed to its `CanvasReading` type) into this directory.

THE PROPS EACH MODULE TAKES — already-derived values only:
    NodeCornerMarks   verdict?: NodeVerdictMark        grounded?: boolean
    NodeRunOverlay    reading: CanvasReading | null    status-derived nothing else
    NodeIconWell      icon: ReactNode                  tint: string
    (both of `NodeIconWell`'s arrive already resolved at the card — `PhaseNodeCard.tsx:220-222`,
     `:232-234` — and the well performs NO lookup. That is the TOTALITY contract, `:97-101`,
     fenced at `PhaseNodeCard.test.tsx:334-351`.)

THE IMPORT LISTS, and the fence each one has to satisfy:
    NodeCornerMarks:  { VERDICT_MARK, type VerdictMarkKind } from ".../nodePresentation"
                      { GOVERNANCE_SEAL_LABEL } from ".../definitionOps"
                      { cn } from "@/lib/utils"
                      { own } from ".../ownProperty"
      ⚠ Fence N2 bans importing ICON_TINT/DEFAULT_TINT FROM `nodePresentation` — it is anchored
        on the two TINT identifiers, NOT on the module path, precisely so `VERDICT_MARK` may be
        imported from there. That narrowing is recorded at `PhaseNodeCard.test.tsx:359-366` and
        transfers unchanged.
    NodeRunOverlay:   { ringDash, ringSpecFor } from ".../runVocabulary"
                      type { CanvasReading } from "@/lib/phaseState"
                      { cn } from "@/lib/utils"
                      { own } from ".../ownProperty"
    NodeIconWell:     type { ReactNode } from "react"     { cn } from "@/lib/utils" (if used)
    ⚠ NONE of the three may import from `@xyflow/*` — fence N5, the zero-graph-library property
      D-184-06 exists for. `PlaneEditingLayer.tsx` DOES import it (it is a portal layer); it is
      the container analog for shape, not for imports.

THE L2 CONTAINER ANALOG — `PlaneEditingLayer.tsx` (268 L), 188.1's own output. Copy seven things:
    a) TWO docblocks, not one: the file header (`:1-52` — what the module IS, the extraction
       state, the export budget, the fences) and a SECOND docblock immediately above the function
       (`:84-108`) carrying the DESIGN argument. `NodeRunOverlay`'s second docblock is where the
       ring prose from `PhaseNodeCard.tsx:658-701` lands (the 62px-well-becomes-dial argument,
       the `z-[5]` ordering, the `⚠` dashoffset-not-rotation rule, the clipping-ban extension)
       plus `:741-749`'s pause-chip prose.
    b) the props interface EXPORTED immediately above the component (`:66-82`).
    c) the destructure IN THE SIGNATURE, not in the body (`:109-120`).
       ⚠ DIVERGENCE TO STATE EXPLICITLY: `PhaseNodeCard` itself uses `props` plus a body
         destructure (`:378`, `:393-408`) because a 14-line comment sits between them. The new
         modules have no such comment and follow `PlaneEditingLayer`'s signature form. Say so in
         the SUMMARY so a reviewer does not read it as drift.
    d) NOT MEMOIZED (`:32-36`) — the recorded reason transfers verbatim: a render-skipping
       wrapper changes WHEN this subtree renders, which is an unmeasured behaviour change on the
       surface whose whole promise is "renders identically". `FlowEdge.tsx:134`'s `memo` import is
       explicitly named as the analog NOT to copy.
    e) the export budget paragraph (`:23-30`) — quoted in R2 above.
    f) the fences paragraph (`:38-51`) — the 188.2 analog names `PhaseNodeCard.test.tsx`, the 17
       re-scoped fences and the `PhaseNodeCard` cycle fence.
    g) the `⚠` that moved `:NNN` references point at the PRE-MOVE file (797 L at `42b42cb7`) and
       are kept stale on purpose.

THE FOUR LIVE `react-refresh` VIOLATIONS to learn from, measured: `FlowEdge.tsx:147`
(`export const DETOUR`), `BuilderStoreProvider.tsx:45, 53, 78`. The directory's count is 5 and
must stay 5.

MEASURED GATES: `tsc --noEmit -p tsconfig.app.json` → 33. `eslint src/components/workflows/` → 5.
`PhaseNodeCard.test.tsx` → the count Plan 03 recorded. Do NOT edit `scripts/vitest-count-gate.cjs`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NodeCornerMarks.tsx and NodeIconWell.tsx — the two marks that carry the hardest constraints</name>

  <files>
frontend/src/components/workflows/NodeCornerMarks.tsx
frontend/src/components/workflows/NodeIconWell.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.tsx:410-434` and `:567-656` and `:764-794` —
  the three source ranges, read in full including every comment. They are the payload.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:1040-1120` — `SEAL_GUARD_OPEN`,
  `SEAL_GUARD_CLOSE`, `sealJsxBlock` and its docblock. Read why it throws rather than returning
  `""`; that is the reason the ternary shape is a hard constraint and not a preference.
- `frontend/src/components/workflows/PlaneEditingLayer.tsx:1-130` — the container analog: two
  docblocks, exported props interface, signature destructure, the not-memoized paragraph, the
  export budget and the fences paragraph.
- `frontend/src/components/workflows/nodePresentation.ts:140-216` — `VerdictMarkKind` and
  `VERDICT_MARK`, and the `PhaseNodeCard.test.tsx:359-366` narrowing that lets this module import
  from here while fence N2 still holds.
- `frontend/src/components/workflows/ownProperty.ts` — Plan 04's leaf, for the exact export name.
  </read_first>

  <action>
Create `frontend/src/components/workflows/NodeCornerMarks.tsx` holding
`PhaseNodeCard.tsx:410-434`, `:567-599` and `:601-656` moved VERBATIM, in that order — verdict
derivation, then verdict JSX, then seal JSX. Keeping verdict ABOVE seal is load-bearing:
`sealJsxBlock` walks backwards from the seal's test id, and its docblock's claim about not
latching onto "the verdict mark's structurally identical block above it" is only true in that
order.

**The seal keeps its shipped `{grounded ? ( … ) : null}` ternary.** An early
`if (!grounded) return null` makes `sealJsxBlock` throw, which is a loud failure and therefore a
good one, but it hard-constrains the move to be verbatim. Record that constraint in the module's
own docblock so the next editor knows before they try it.

Export exactly two names: `NodeCornerMarks` and `NodeCornerMarksProps`. Take `verdict` and
`grounded` as props — already-derived values, no lookup, no run-state derivation. The
`own(VERDICT_MARK, verdict) ?? VERDICT_MARK.unknown` expression and its 24-line WR-04 docblock
move together and unchanged: that is WR-04 sink 4, its falsification was observed RED at 188.1-04,
and the docblock explains why the guard exists.

Then create `frontend/src/components/workflows/NodeIconWell.tsx` holding `:764-794` verbatim —
the JSX comment plus the four-`<span>` well. Props: `icon` and `tint`, both already resolved by
the adapter before they reach the card, so this module performs NO lookup. That is the TOTALITY
contract and it is fenced; say so in the docblock. **Do not re-word the sentence at `:772`** — it
is the tree's only mention of the clipping utility and the count fence pins it at exactly 1
across the whole subtree.

Give both files the seven-point `PlaneEditingLayer` shape from `<interfaces>`: two docblocks
(header plus a design docblock above the function), the exported props interface immediately
above the component, the destructure in the signature, no `memo`, the export budget paragraph,
the fences paragraph naming `PhaseNodeCard.test.tsx` and the 17 re-scoped fences, and the `⚠`
that the moved `:NNN` references point at the pre-move 797-line file at HEAD `42b42cb7` and are
deliberately stale.

`NodeIconWell.tsx`'s docblock will be larger than its body. That is correct and matches
`editAffordance.ts`, whose 52-line header wraps a 13-line table — say so rather than trimming the
prose to make the ratio look better.

Export NO runtime value from either file. Import nothing from `@xyflow/*`. Delete nothing from
`PhaseNodeCard.tsx`.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx eslint src/components/workflows/</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors, 0 warnings** — unmoved. A 6th means a runtime value was exported from a `.tsx` module, which undoes 188.1's only measurable lint win.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` returns exactly **33**.
- `grep -Ec "^export (const|let|var|function [a-z])" frontend/src/components/workflows/NodeCornerMarks.tsx frontend/src/components/workflows/NodeIconWell.tsx` returns **0** for each. Only the component and its props type are exported.
- `grep -c "@xyflow" frontend/src/components/workflows/NodeCornerMarks.tsx frontend/src/components/workflows/NodeIconWell.tsx` returns **0** for each (fence N5, D-184-06).
- **THE SEAL'S TERNARY SURVIVED**: `grep -c "{grounded ? (" frontend/src/components/workflows/NodeCornerMarks.tsx` returns **1** and `grep -c ") : null}" ...` returns **≥ 2**. Then prove the extractor still works against the new file's contribution: run the suite and confirm `sealJsxBlock(cardSubtreeSource)` does not throw.
- **THE CLIP COUNT IS STILL EXACTLY 1 ACROSS THE SUBTREE**: `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with **0 failed**, which includes fence N15 at its re-anchored subtree scope. At this point the mention exists in BOTH the card and `NodeIconWell.tsx`, so N15 MUST report 2 and go RED — **that is expected, and it is the proof the fence is live.** Record the failure text, then confirm the count returns to 1 only after Plan 06 deletes the card's copy. If N15 does NOT go red here, the fence is vacuous and that is a finding to report before proceeding.
- **THE MOVE IS VERBATIM**: `diff` each moved span against `git show HEAD:frontend/src/components/workflows/PhaseNodeCard.tsx | sed -n 'A,Bp'` (writing the temp files to the session scratchpad, never into the watched tree) and paste the output. Differences are limited to the component wrapper, the props destructure and the props type — the JSX element bodies and their comments are byte-identical.
- `git status --porcelain` shows the two files as NEW and **no `M`** on `PhaseNodeCard.tsx`.
  </acceptance_criteria>

  <done>Both mark modules exist with their JSX and prose moved verbatim, the seal's ternary and the verdict-above-seal order intact, zero runtime exports, zero graph-library imports, and the duplicated clip mention observed making fence N15 go RED — which is what proves the fence reaches the new file.</done>
</task>

<task type="auto">
  <name>Task 2: Create NodeRunOverlay.tsx — the ring, its constants, its arc geometry and the pause chip</name>

  <files>
frontend/src/components/workflows/NodeRunOverlay.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.tsx:287-356` — the separator and all five
  `RING_*` constants with their docblocks. Confirm for yourself that none of them is exported
  today; that is why moving them module-private is legal as-is.
- `frontend/src/components/workflows/PhaseNodeCard.tsx:436-445` — the run derivations. Only
  `arc` (`:443`) moves; `reading`, `runBorder` and `minHeight` stay with the card.
- `frontend/src/components/workflows/PhaseNodeCard.tsx:658-762` — the ring JSX and the pause-chip
  JSX in full, including the ring's ~48 lines of prose which become this module's design docblock.
- `frontend/src/components/workflows/runVocabulary.ts:80-104` — `ringDash`, `ringSpecFor` and the
  private `own()` that must NOT be imported or touched.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:1676-1700` and `:1755-1790` — fence
  N13 (the ring decimals must appear in NEITHER module that produces them), N16/N17 (no
  `transform=` and no `rotate(` — the ring animates by dashoffset, never by rotation), and P4
  (`canvas-ring-spin`). All four now read the subtree, so this file is inside them.
- `frontend/src/components/workflows/PlaneEditingLayer.tsx:84-120` — the second-docblock shape.
  </read_first>

  <action>
Create `frontend/src/components/workflows/NodeRunOverlay.tsx` holding, in this order and moved
verbatim: `PhaseNodeCard.tsx:287` (the ring separator), `:312-355` (the five `RING_*` constants
with their docblocks), `:440-443` (the `arc` derivation and its comment), `:658-739` (the
status-ring JSX) and `:741-762` (the pause-chip JSX).

Keep all five `RING_*` constants **module-private**. They are private in the card today, so the
move is legal exactly as written. The trap is exporting one "so the test can read it" — the tests
read `?raw` source and rendered DOM instead, and one `export const` in a `.tsx` file takes the
directory's eslint count from 5 to 6.

Export exactly two names: `NodeRunOverlay` and `NodeRunOverlayProps`. The component takes an
already-derived `reading` and computes only geometry from it. Do NOT add a `status`-to-reading
mapping, a severity derivation, or any other run-state inference — that is red line D-14, and the
one derivation that answers "what state is this step in" lives in `frontend/src/lib/phaseState.ts`
and is fenced there.

Write the design docblock above the function from `PhaseNodeCard.tsx:658-701`'s ring prose plus
`:741-749`'s pause-chip prose, moved with the code that it explains (D-01). Add one paragraph
those did not need: state plainly that moving the `arc` derivation here is NOT a D-14 breach,
because `ringDash(ringSpecFor(reading), RING_CIRCUMFERENCE)` is pure geometry over a reading that
arrived already derived, and name the warning sign — an import of `@/lib/phaseState`'s FUNCTIONS
rather than its `CanvasReading` type would be the breach.

Give the file the same seven-point `PlaneEditingLayer` shape as Task 1: header docblock with the
extraction state, the `⚠` about deliberately-stale `:NNN` references pointing at the pre-move
797-line card at HEAD `42b42cb7`, the export budget paragraph, the fences paragraph, the exported
props interface above the component, the signature destructure, and no `memo`.

The `own(RING_STROKE, reading) ?? RING_STROKE.unknown` expression at `:731` and its four-line
WR-04 comment move together and unchanged — that is sink 5, and its falsification drives through
the card, so it must keep reaching this code.

Import nothing from `@xyflow/*`. Delete nothing from `PhaseNodeCard.tsx`.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx eslint src/components/workflows/</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors, 0 warnings**.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` returns exactly **33**.
- `grep -Ec "^export (const|let|var)" frontend/src/components/workflows/NodeRunOverlay.tsx` returns **0**, and `grep -c "RING_RADIUS\|RING_STROKE\|RING_BOX_CLASSES\|RING_TRACK_STROKE\|RING_CIRCUMFERENCE" ...` is **> 0** — the constants are present and private.
- `grep -c "@xyflow" frontend/src/components/workflows/NodeRunOverlay.tsx` returns **0**.
- `grep -c "from \"@/lib/phaseState\"" frontend/src/components/workflows/NodeRunOverlay.tsx` returns **1** and that line is an `import type` — the type, never the derivation functions (D-14).
- `grep -c "own(RING_STROKE" frontend/src/components/workflows/NodeRunOverlay.tsx` returns **1** and the WR-04 comment above it moved with it.
- **THE VERBATIM PROOF**: `diff` each of the five moved spans against `git show HEAD:` output, writing temp files to the session scratchpad. Paste the output; differences are limited to the wrapper, the destructure and the props type.
- `wc -l frontend/src/components/workflows/NodeRunOverlay.tsx` is **≥ 160**. Record the exact figure.
- `git status --porcelain` shows the file as NEW and **no `M`** on `PhaseNodeCard.tsx`.
- The D-14 paragraph is present and is quoted in the SUMMARY.
  </acceptance_criteria>

  <done>`NodeRunOverlay.tsx` exists holding the ring separator, all five private `RING_*` constants, the `arc` geometry, the ring JSX and the pause chip, moved verbatim; it exports only the component and its props type, imports no graph library and no derivation function, and its docblock states why the `arc` move is not a D-14 breach.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the three fences that could only be proven once real destination files existed — C-2, C-5, C-8</name>

  <files>
frontend/src/components/workflows/PhaseNodeCard.test.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — the `CARD_SUBTREE_PATHS` block and
  the non-vacuity control Plan 01 added, and the ESM-cycle `describe(` block with its
  `if (path in CARD_MODULES)` guards. Those guards skipped every path at Plan 01; three of the
  five now resolve, and this task is where that is confirmed rather than assumed.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:195-216` — the no-focusable-control
  walk (C-8's subject) and the `onClick=` source fence beside it.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:390-435` — the five purity fences,
  each with its positive control, so the planted violations are placed where they will be seen.
  Read `:423-428` closely before planting: fence N10 is anchored on the JSX PROP FORM
  (`not.toMatch(/dangerouslySetInnerHTML=/)` at `:426`) with its positive control on the very
  next line. That anchoring is why the card's own docblock may name the identifier in prose at
  `PhaseNodeCard.tsx:104` without tripping it — and it is why a plant that omits the `=` proves
  nothing at all. Re-derive both line numbers rather than trusting these.
- `frontend/src/components/workflows/PhaseNodeCard.tsx:596-597` and `:654` — the three authored
  strings the marks render as React text children (`{mark.glyph}`, `{mark.label}` and
  `{GOVERNANCE_SEAL_LABEL}`). These are the sites N10 exists to protect, and they are the
  measured reason the XSS plant lands in `NodeCornerMarks.tsx` and nowhere else: `NodeIconWell`'s
  only authored string is `tint`, which is interpolated into an inline `style` object
  (`PhaseNodeCard.tsx:780`) and never into markup, and `NodeRunOverlay` renders no authored
  string at all.
- `.planning/phases/188.2-.../188.2-VALIDATION.md` § Falsification Controls — C-2, C-5, C-8 in
  their authored form.
  </read_first>

  <action>
Confirm the fences bite against real files, and make the cycle fence's resolved-module assertions
non-vacuous now that three destinations exist.

Update the non-vacuity control's resolved-module loop so the three new `.tsx` paths are asserted
non-empty unconditionally rather than under the `if (path in CARD_MODULES)` guard, keeping the
guard only for `phaseNodeCardContract.ts` and `ownProperty.ts` if the glob has not picked them up
— then record which paths are guarded and which are not, because a guard left in place over a
file that now exists is a fence that reads an empty string in silence.

Then observe all three controls RED, each against a violation planted in a REAL destination file
and each reverted immediately:

  · **C-2 — the subtree fence covers the moved code. THREE plants, one per fence.** Plant
    `onClick=` in `NodeCornerMarks.tsx` and confirm fence N1 fails. Revert. Then plant the
    graph-package scope literal in `NodeRunOverlay.tsx` and confirm fence N5 fails. Revert.
    Then plant the XSS form and confirm fence N10 fails: temporarily rewrite the seal's
    authored-string render site in `NodeCornerMarks.tsx` from
    `<span className="sr-only">{GOVERNANCE_SEAL_LABEL}</span>` to
    `<span className="sr-only" dangerouslySetInnerHTML={{ __html: GOVERNANCE_SEAL_LABEL }} />`.
    Revert.

    One plant proves one path resolves, not that the sweep covers the subtree — and it proves
    nothing whatever about a DIFFERENT fence, which is the reason there are three rather than
    two. **N10 is not optional here.** It is the only fence in the vacuity register carrying a
    security property (ASVS V5, T-124-01 / T-184-03-01), and `188.2-01`'s threat register
    (T-188.2-02) rests its mitigation on it; until N10 is seen catching a real violation inside
    a file it did not read before this phase, that mitigation is a re-scope asserted, not a guard
    observed — exactly the unmeasured-claim shape this phase exists to stop shipping.

    ⚠ Plant the PROP FORM. The fence is anchored on `dangerouslySetInnerHTML=`, so a prose
    mention in a comment does NOT trip it (that is deliberate: `PhaseNodeCard.tsx:104` names the
    identifier in exactly that way today). A plant that produces no red has not proven the fence
    vacuous — check the `=` first, then report.

    ⚠ `NodeCornerMarks.tsx` is the plant site on evidence, not convenience: it is the earliest
    module in the phase that renders an authored string into markup, it renders three of them
    (`PhaseNodeCard.tsx:596-597`, `:654`), and Task 1 of THIS plan creates it. Waiting for Plan
    06 would delay the observation without strengthening it.

  · **C-5 — the cycle fence.** Plant `import { PhaseNodeCard } from "./PhaseNodeCard"` in
    `NodeIconWell.tsx` and confirm the cycle fence's negative loop fails. Revert. Then plant the
    TYPE-ONLY form, `import type { PhaseNodeCardProps } from "./PhaseNodeCard"`, and confirm it
    ALSO fails — `verbatimModuleSyntax: true` is exactly why the type-only form must be forbidden
    too, and a fence that catches only the value form would let the cycle back in under a later
    edit. Revert.
  · **C-8 — no focusable control inside the card.** Plant a `<button>` inside
    `NodeCornerMarks.tsx`'s verdict JSX and confirm the `:201-209` walk fails. Revert. ⚠ At this
    point in the phase the card still renders its OWN inline copies, so the new module is not
    yet mounted — if the walk does NOT go red, that is the expected result and the honest thing
    to record: the source-side half of C-8 (the `onClick=` fence, already proven under C-2) is
    what covers the module today, and the RENDERED half becomes meaningful only after Plan 06
    wires the call sites. Say which of the two happened; do not report a red that was not
    observed, and do not silently drop the control.

Do not change any assertion's meaning while doing this — no bound is loosened, no `it(` is
renamed. Do not edit `scripts/vitest-count-gate.cjs`.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/PhaseNodeCard.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- After every plant is reverted, `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` fails ONLY on fence N15 (the clip count reading 2 while both the card and `NodeIconWell.tsx` hold the mention). Record that as the single expected red and state that Plan 06's cut is what returns it to 1. Any OTHER failure is a real regression and must be named.
- **C-2 RED OBSERVED THREE TIMES.** Each plant is run under `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx`, and the SUMMARY carries all three FAILURE TEXTS verbatim — the failing `it(` name, the assertion line and the expected/received. Recording that a plant was made does NOT satisfy this criterion; the recorded red does. The three: (1) `onClick=` planted in `NodeCornerMarks.tsx` fails N1; (2) the graph-package scope literal planted in `NodeRunOverlay.tsx` fails N5; (3) `dangerouslySetInnerHTML=` planted at the seal's authored-string render site in `NodeCornerMarks.tsx` fails N10, the fence named `the card renders authored strings as text children — never the HTML sink`.
- **EACH of those three runs shows EXACTLY TWO failures**: the planted fence plus the expected N15 clip-count red. A run showing only N15 means the plant did not land and the fence is NOT known to reach that file — for N10 confirm the `=` is present (the prop form, not a prose mention), re-plant, and report the outcome either way rather than moving on. A run showing a third failure is a real regression and must be named. After each revert, `git status --porcelain` is clean of that file.
- **C-5 RED OBSERVED TWICE**, both failure texts recorded: the value back-import and the TYPE-ONLY back-import each fail the cycle fence. If the type-only form does NOT fail, the regex is too narrow — fix the regex, re-observe, and record both attempts.
- **C-8 RESOLVED HONESTLY**: either the planted `<button>` turned the walk RED (record the failure text) or it did not (record that, name the reason — the module is not yet mounted at this wave — and carry the rendered half forward to Plan 06 as an explicit owed observation). Do not report a red that was not observed.
- `grep -n "if (path in CARD_MODULES)" frontend/src/components/workflows/PhaseNodeCard.test.tsx` shows the guard remaining ONLY over paths whose files do not yet exist. Record which paths those are.
- `git status --porcelain` at the end of the task shows **no modification** to any of the three new `.tsx` files or to `PhaseNodeCard.tsx` — every plant was reverted.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` returns exactly **33** and `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors**.
  </acceptance_criteria>

  <done>The subtree fence, the cycle fence and the focusable-control walk have each been exercised against a violation planted in a real destination file; the cycle fence was proven to catch the type-only back-import as well as the value form; the one expected red (the duplicated clip mention) is recorded with the reason it resolves in Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a fence's path list → the files it actually resolves | `import.meta.glob` silently contributes the empty string for a path it cannot resolve. A guard left over a file that now exists reads nothing and reports success. |
| a component module → the shared runtime values it must not export | One `export const` in a `.tsx` file re-fires `react-refresh/only-export-components`, and the only signal is a lint count nobody re-reads. |
| a projection → a deriver | A presentational module that "helpfully" maps a raw status to a reading becomes a second source of truth for run state, and the divergence surfaces as two surfaces disagreeing at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188.2-18 | Tampering | XSS via authored strings rendering in the new modules — measured, the sites are `NodeCornerMarks`' three (`{mark.glyph}`, `{mark.label}`, `{GOVERNANCE_SEAL_LABEL}`; `PhaseNodeCard.tsx:596-597`, `:654`). `NodeIconWell`'s `tint` reaches an inline `style` object only (`:780`) and `NodeRunOverlay` renders no authored string | mitigate | Fence N10 (`dangerouslySetInnerHTML=`, T-124-01 / T-184-03-01) was re-scoped to the subtree in Plan 01 and now reads all three files; the move is verbatim React text children throughout; and **N10 ITSELF is observed RED in Task 3** against a `dangerouslySetInnerHTML=` plant at the seal's render site in `NodeCornerMarks.tsx`. This row therefore rests on an observation of THIS fence, not on a plant of a different one. ASVS V5 |
| T-188.2-19 | Tampering | WR-04 prototype-pollution sinks 4 and 5, now in two different modules (V3 in the vacuity register) | mitigate | Both `own(...) ?? TABLE.unknown` expressions move verbatim with their WR-04 docblocks; both falsifications (`PhaseNodeCard.test.tsx:2007-2050`) drive through the CARD, so Plan 06 must re-verify they keep passing AND keep reaching the moved code — carried forward as an explicit acceptance criterion there |
| T-188.2-20 | Denial of Service | an ESM cycle from any of the three components back into `PhaseNodeCard` | mitigate | C-5 observed RED against BOTH the value and the type-only back-import form; `verbatimModuleSyntax: true` makes the type-only form a live hazard rather than a theoretical one |
| T-188.2-21 | Elevation of Privilege | red line D-14 — a new module acquiring run-state derivation during the move | mitigate | The new modules take already-derived props; `arc` is named as pure geometry with the reasoning written into `NodeRunOverlay`'s docblock; the acceptance criterion asserts `@/lib/phaseState` is imported as a TYPE only |
| T-188.2-22 | Spoofing | the zero-graph-library property (D-184-06, fence N5) | mitigate | Asserted per file by grep AND by the re-scoped fence, and observed RED under a planted literal in `NodeRunOverlay.tsx` |
| T-188.2-SC | Tampering | npm / pip / cargo installs | mitigate | Zero packages installed. Hard gate: `git diff --stat -- frontend/package.json frontend/package-lock.json` reports 0 files changed |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → exactly 33
- `cd frontend && npx eslint src/components/workflows/` → exactly 5 errors, 0 warnings
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` → the ONLY failure is fence N15 (clip count 2, resolved by Plan 06's cut); every other test passes
- `grep -c "@xyflow" frontend/src/components/workflows/Node{CornerMarks,IconWell,RunOverlay}.tsx` → 0 each
- `git status --porcelain` → three NEW files; no `M` on `PhaseNodeCard.tsx`
- `git diff --stat -- frontend/package.json frontend/package-lock.json backend/ supabase/migrations/ scripts/` → 0 files changed
</verification>

<success_criteria>
1. Three component modules exist, grouped per D-04 with one reason to change each, holding their source ranges moved verbatim with all prose intact.
2. The seal keeps its ternary and the verdict block stays above it, so `sealJsxBlock` still carves rather than throwing (D-01).
3. Each `.tsx` module exports the component and its props type and no runtime value; the directory's eslint count is unmoved at 5 (R2).
4. `NodeRunOverlay` imports `@/lib/phaseState` as a TYPE only and its docblock states why moving `arc` is not a D-14 breach.
5. C-2 was observed RED three times — N1, N5 and the XSS fence N10 — and C-5 twice, against violations planted in real destination files, with the type-only back-import proven caught. N10's red is what converts `188.2-01`'s security mitigation (T-188.2-02) from a re-scope asserted into a guard observed.
6. C-8 was resolved honestly — either observed RED, or recorded as not-yet-mountable with the rendered half carried to Plan 06.
7. Nothing was deleted from `PhaseNodeCard.tsx`; the single expected red (fence N15 at 2) is recorded with the reason it resolves in Plan 06.
</success_criteria>

<output>
Create `.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-05-SUMMARY.md` when done. Record verbatim: every span `diff` proving the three verbatim moves; `wc -l` for all three files; the failure text of all FIVE C-2/C-5 plants (N1's `onClick=`, N5's graph-package literal, N10's `dangerouslySetInnerHTML=`, the value back-import and the type-only back-import); the C-8 outcome and which half of it is owed to Plan 06; the N15 failure text and why it is expected; and the `tsc`/`eslint` numbers after every revert.
</output>
