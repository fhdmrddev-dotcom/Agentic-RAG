---
phase: 188.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
autonomous: true
requirements: []
# EMPTY BY DESIGN. ROADMAP §Phase 188.2: "**Requirements**: none — refactor phase."
# Traceability is to the phase success criteria and to the CONTEXT decision ids, never to a
# REQ- id. Do NOT call `requirements.mark-complete`.
success_criteria: ["SC#3", "SC#5"]
gap_closure: false

must_haves:
  truths:
    - "D-07: every NEGATIVE `?raw` assertion in `PhaseNodeCard.test.tsx` reads a SUBTREE source whose path list already names all five destination modules BEFORE they exist, so the 471 lines the cut moves cannot leave a fence's scope silently — the 17 fences are enumerated by line, not handled implicitly"
    - "D-07: the subtree fence carries all three of 188.1's non-vacuity controls — a length pin, EACH destination path named individually (a length pin catches TRUNCATION but not SUBSTITUTION), and resolved-module-non-empty once the file exists"
    - "D-08: the clip-utility count fence is re-anchored to the joined subtree source, so it stays at exactly 1 wherever the rule lives and cannot be satisfied by shuffling prose between files — the measured landmine (`PhaseNodeCard.tsx:772` is the tree's only mention and it moves to `NodeIconWell.tsx`)"
    - "P4, which CONTEXT does not name: `PhaseNodeCard.test.tsx:1776`'s `toContain(\"canvas-ring-spin\")` is a POSITIVE assertion that goes RED on the move, and it is re-anchored in the same commit as D-08's twin"
    - "D-09: 'a third badge is a typecheck error' is asserted for the first time — measured, it is asserted NOWHERE today (`BadgeSlot2Tuple` has zero callers, `@ts-expect-error` appears 0 times in the whole workflows directory) — and it was observed RED against a deliberately widened union before it was trusted"
    - "This plan is behaviour-neutral on the shipped tree: it touches ONE test file, no production source at all, and every re-scoped fence stays green against the unchanged card"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseNodeCard.test.tsx"
      provides: "the CARD_SUBTREE_PATHS block, 17 re-pointed negatives, 4 re-pointed haystacks, the D-09 third-badge control and the PhaseNodeCard ESM-cycle fence"
      contains: "CARD_SUBTREE_PATHS"
  key_links:
    - from: "frontend/src/components/workflows/PhaseNodeCard.test.tsx"
      to: "./phaseNodeCardContract.ts + ./ownProperty.ts + ./NodeCornerMarks.tsx + ./NodeRunOverlay.tsx + ./NodeIconWell.tsx (none of which exist at this commit)"
      via: "import.meta.glob path list — an absent path has no key in the record and contributes the empty string, never a throw"
      pattern: "CARD_SUBTREE_PATHS"
---

<objective>
Re-scope every negative `?raw` fence in `PhaseNodeCard.test.tsx` to a subtree source that
already names the five destination modules, and add the two guards this file has never had —
BEFORE one line of the card moves.

Purpose: this is the phase's highest-value plan, and the number that justifies it was
measured, not estimated. **There are 17 negative fences over `phaseNodeCardSource`, not the 2
CONTEXT names by hand. Two of them go RED on the move; FIFTEEN stay green while covering
nothing** — including `:399`, the zero-graph-library fence D-184-06 exists for, and `:426`,
the XSS fence. Neither the count gate nor `tsc` can see that loss, and a reviewer reading a
green suite cannot tell a guard that guards from one that stopped. That is Phase 188's F7
lesson at 8× the estimated scale. Doing it FIRST, against the unchanged tree, makes the
re-scope provably behaviour-neutral: every fence must stay green with zero production edits.

The two guards that do not exist yet: **D-09's third-badge typecheck invariant is asserted
NOWHERE** (measured — the two-badge budget block at `:218-267` tests 0/1/2 badges *rendering*
and nothing tests the TYPE), so widening `BadgeSlots` to `readonly BadgeSlot[]` during the move
would leave every test green; and the ESM-cycle fence, because `PhaseNodeCard` will import the
new components' VALUES at module scope and a back-import would close a live cycle that
typechecks clean, lints clean and fails only at runtime as a TDZ `ReferenceError`.

Output: `CARD_SUBTREE_PATHS`/`CARD_MODULES`/`cardSubtreeSource`, 17 re-pointed negatives, 4
re-pointed haystacks, three non-vacuity controls, the D-09 `@ts-expect-error` control, the
cycle fence, an updated `stripComments` scoping docblock, and four RED observations (C-1, C-3,
C-4, C-7).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-CONTEXT.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-RESEARCH.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-PATTERNS.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-VALIDATION.md

<interfaces>
<!-- RE-MEASURED at HEAD `42b42cb7` by the planner, 2026-08-06. Every line number below was
     confirmed by printing that exact line. Re-derive anyway before editing — this repository's
     numbers are live (`feedback_dont_inherit_unmeasured_claims`; 188.1 found NINE inherited
     claims false across five plans, and this phase's own researcher found SEVEN more). -->

THE COMPLETE NEGATIVE-FENCE INVENTORY over `phaseNodeCardSource`. All 17 re-point to
`cardSubtreeSource`. ⚠ means it goes RED (rather than green-vacuous) if left alone.

    N1   :214        not.toMatch(/onClick=/)
    N2   :367-369    not.toMatch(... import {ICON_TINT|DEFAULT_TINT} ... nodePresentation)
    N3   :370        not.toMatch(/ICON_TINT\[/)
    N4   :371        not.toMatch(/DEFAULT_TINT\s*[,)\]]/)
    N5   :399        not.toContain(GRAPH_PKG_SCOPE)     ← the zero-graph-library property (D-184-06)
    N6   :409        not.toMatch(/<Handle[\s/>]/)
    N7   :410        not.toMatch(/<ReactFlow/)
    N8   :411        not.toMatch(/:\s*NodeProps</)
    N9   :418        not.toMatch(/from ["']@\/lib\/api["']/)
    N10  :426        not.toMatch(/dangerouslySetInnerHTML=/)   ← XSS (T-124-01 / T-184-03-01)
    N11  :431-433    not.toMatch(/getBoundingClientRect|offsetHeight|offsetWidth|document\.|
                                  window\.|Date\.now|Math\.random/)
    N12  :1101   ⚠   sealJsxBlock(phaseNodeCardSource).not.toMatch(statusIdentifier())
    N13  :1684       for (const source of [phaseNodeCardSource, runVocabularySource])
                     — ONLY the card element re-points; `runVocabularySource` stays
    N14  :1735       stripComments(phaseNodeCardSource).not.toContain(CLIP_UTILITY)
    N15  :1745   ⚠   phaseNodeCardSource.split(CLIP_UTILITY).length - 1 === 1   ← D-08's landmine
    N16  :1763       not.toContain(SVG_TRANSFORM_ATTR)
    N17  :1764       not.toContain(CSS_ROTATE)

THE FOUR HAYSTACKS that also re-point (positives / extractor inputs, NOT negatives):

    P1   :1089   ⚠   const block = sealJsxBlock(phaseNodeCardSource)
                     `sealJsxBlock` does `source.indexOf("canvas-node-seal")` and THROWS on a
                     miss — so it is a hard error after the move, not a vacuous pass.
    P2   :1097   ⚠   expect(block.length).toBeLessThan(phaseNodeCardSource.length / 4)
                     ⚠⚠ THE HAYSTACK re-points to `cardSubtreeSource`, NEVER to
                     `NodeCornerMarks?raw`: a 56-line seal block inside a ~145-line file is
                     ~39 %, well over the ¼ bound ⇒ RED for the wrong reason. Any proposal to
                     change `/ 4` to `/ 2` is the wrong fix.
    P3   :1114       expect(phaseNodeCardSource).toMatch(statusIdentifier())
                     Stays GREEN either way (`status` survives in the destructure) but its
                     MEANING silently shifts from "the docblock explains the seal" to "the
                     destructure mentions a prop". Re-point to restore the meaning.
    P4   :1776   ⚠   expect(phaseNodeCardSource).toContain("canvas-ring-spin")
                     ⚠⚠ NOT NAMED IN CONTEXT. A second landmine of exactly D-08's shape: the
                     spin class is at `PhaseNodeCard.tsx:734` and moves to `NodeRunOverlay`.

TWO THAT DO NOT MOVE (per-file by design — say so in an inline note at each):

    P5   :1695       expect(phaseNodeCardSource.length).toBeGreaterThan(1000)
    P6   :1729       expect(stripComments(phaseNodeCardSource).length)
                       .toBeLessThan(phaseNodeCardSource.length)
                     — a CONTROL on the stripper itself; widening it changes what it controls.

THE HOUSE `?raw` / `import.meta.glob` IDIOM to copy (`PhaseFormPanel.rails.test.tsx:422-426`,
cited by `governanceVocabulary.test.ts:65` as "the house idiom"), narrowed by 188.1 at
`WorkflowCanvas.test.tsx:51-79`:

    const CANVAS_SUBTREE_PATHS = [
      "./WorkflowCanvas.tsx",
      "./PlaneEditingLayer.tsx",
      "./editAffordance.ts",
    ] as const
    const CANVAS_MODULES = import.meta.glob("./*.{ts,tsx}", {
      query: "?raw", eager: true, import: "default",
    }) as Record<string, string>
    const canvasSubtreeSource =
      CANVAS_SUBTREE_PATHS.map((path) => CANVAS_MODULES[path] ?? "").join("\n")

THE `?? ""` IS WHAT MAKES IT NEVER THROW, and the property is PROVEN BY COMMIT ANCESTRY rather
than by the comment claiming it: `004a6486` shipped that block naming two non-existent files;
`14917821` created them; `git merge-base --is-ancestor 004a6486 14917821` is true and the suite
was green between them.

THE THREE NON-VACUITY CONTROLS to copy (`WorkflowCanvas.test.tsx:591-615`) — a length pin, each
destination named individually (measured RED under a SUBSTITUTION that a length pin missed), and
resolved-module-non-empty once the file exists.

THE SIX 188.2 SUBTREE PATHS (exactly the file this phase cuts, plus the five it creates — do NOT
pull in `nodePresentation.ts` or `runVocabulary.ts`, which have their own fences and would make a
negative go red for reasons unrelated to the card; RESEARCH Open Q2):

    "./PhaseNodeCard.tsx"
    "./phaseNodeCardContract.ts"
    "./ownProperty.ts"
    "./NodeCornerMarks.tsx"
    "./NodeRunOverlay.tsx"
    "./NodeIconWell.tsx"

MOVE-INVARIANT LITERALS for the non-vacuity control — each verified by the planner to occur
EXACTLY ONCE in `PhaseNodeCard.tsx` today, and each lands in a named destination after the cut:

    "export interface PhaseNodeCardProps"   :215   → phaseNodeCardContract.ts
    "function own<T>("                      :306   → ownProperty.ts
    "canvas-node-seal"                      :645   → NodeCornerMarks.tsx
    "const RING_RADIUS"                     :313   → NodeRunOverlay.tsx
    "canvas-node-pause-chip"                :753   → NodeRunOverlay.tsx
    "h-[62px]"                              :775   → NodeIconWell.tsx

    ⚠ Do NOT use `"function NodeRunOverlay("` or its siblings as the move-invariant literal —
    they are FALSE today, so the control would be red at Wave 0 for the wrong reason. 188.1's
    control worked precisely because its two literals were true BEFORE and AFTER the move.

THE ESM-CYCLE FENCE to copy (`WorkflowCanvas.test.tsx:618-691`), with its inline positive
controls FIRST (a fence whose matcher is broken passes vacuously and looks exactly like a fence
that holds). The 188.2 regexes:

    /from\s+["'][^"']*PhaseNodeCard["']/          — static and type-only import forms
    /import\s*\(\s*["'][^"']*PhaseNodeCard["']\s*\)/   — the dynamic form

    ⚠ Both are anchored so the quote CLOSES right after `PhaseNodeCard`, which is why
    `from "./PhaseNodeCard?raw"` (this file's own line :37) and
    `from "./phaseNodeCardContract"` do NOT match. Verify that empirically with an inline
    positive/negative control pair rather than trusting this sentence.
    `verbatimModuleSyntax: true` is on in `tsconfig.app.json`, which is exactly why the
    type-only back-import must be forbidden too.

D-09 CONTROL SHAPE. `noUnusedLocals: true` in `tsconfig.app.json` means an unused
`const three` is its own error — the control would then read 34 whether or not the union is
narrow (Pitfall 5). The house idiom for a deliberately-unused local is `deriveTier.ts:124-126`:

    const _never: never = citationPolicy
    void _never

`BadgeSlot` and `BadgeSlots` are already imported at `PhaseNodeCard.test.tsx:45`.

MEASURED TYPE-GATE BASELINE (re-run it; do not inherit):
    npx tsc --noEmit                      → exit 0, ZERO output. Checks nothing. Never use it.
    npx tsc --noEmit -p tsconfig.app.json → 33 error lines, ZERO of them in
                                            `src/components/workflows/`.
    ⇒ 33 → 34 is a CLEAN red signal for the D-09 control and 33 unmoved is a clean green.

MEASURED SUITE BASELINE:
    npx vitest run src/components/workflows/PhaseNodeCard.test.tsx → 107 passed / 107, ~3.4 s

COUNT GATE — do NOT touch `scripts/vitest-count-gate.cjs` in this plan. `TARGETS[0]` is the bare
directory `src/components/workflows` (`:474`) so nothing needs registering, an INCREASE over a
pin is legal and printed as `+N`, and RESEARCH's recommended shape is one upward pin move at the
END of the phase (Plan 07). `"PhaseNodeCard.test.tsx": 107` sits at `:294`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the subtree source that names five files that do not exist yet, with all three non-vacuity controls</name>

  <files>
frontend/src/components/workflows/PhaseNodeCard.test.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:25-135` — the whole import block
  (note `:37` `phaseNodeCardSource`, `:39` `phaseNodeSource`, `:41` `nodePresentationSource`,
  `:44` `runVocabularySource`, `:45` the value import) and `ALL_READINGS_TABLE` at `:123-132`.
- `frontend/src/components/workflows/WorkflowCanvas.test.tsx:51-79` — the shipped
  `CANVAS_SUBTREE_PATHS` block and its recorded rationale for naming files before they exist.
  Copy the SHAPE and the reasoning voice, not the canvas's path list.
- `frontend/src/components/workflows/WorkflowCanvas.test.tsx:591-615` — the three non-vacuity
  controls, including the substitution guard that was measured RED.
- `frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx:415-445` — the upstream
  house idiom the above narrows, and its "the glob is not empty" control at `:440-443`.
- `frontend/src/components/workflows/PhaseNodeCard.tsx:215, 306, 313, 645, 753, 775` — the six
  move-invariant literals, so the control is written against lines that were read, not quoted.
  </read_first>

  <action>
Add a module-scope subtree source to `PhaseNodeCard.test.tsx`, immediately after the existing
`?raw` import block. Do NOT delete `phaseNodeCardSource` — P5, P6 and every positive assertion
still need it, and an additive diff is the house rule (`canvasModel.purity.test.ts:18-21`).

Declare `CARD_SUBTREE_PATHS` as an `as const` array of exactly the six literals listed in
`<interfaces>`, `CARD_MODULES` via `import.meta.glob("./*.{ts,tsx}", { query: "?raw", eager:
true, import: "default" })` cast to `Record<string, string>`, and `cardSubtreeSource` as
`CARD_SUBTREE_PATHS.map((path) => CARD_MODULES[path] ?? "").join("\n")`.

Carry a `//` comment block above it in the 188.1 voice naming: the idiom and its precedent
(`PhaseFormPanel.rails.test.tsx:422-426`); that FIVE of the six paths do not exist at this
commit and that this is the point; that `import.meta.glob` expands at build time over files that
EXIST, so a path with no file simply has no key and contributes the empty string; and the
measured scale — 17 negative fences, of which fifteen would go green while covering nothing and
two would go red, if this block did not exist.

Then add ONE new `it(` carrying all three non-vacuity controls in one place:
  · the six move-invariant `toContain` literals from `<interfaces>`, asserted against
    `cardSubtreeSource` — every one is true TODAY (they are in the card) and stays true AFTER
    the cut (they are in the destinations), which is what makes the control prove coverage
    rather than presence;
  · `expect(CARD_SUBTREE_PATHS).toHaveLength(6)` and
    `expect(Object.keys(CARD_MODULES).length).toBeGreaterThan(5)` — the workflows directory
    holds 66 `.ts`/`.tsx` files, so `> 5` is a safe non-brittle floor;
  · a loop over the FIVE destination paths asserting each is individually present in
    `CARD_SUBTREE_PATHS` — because a length pin catches a TRUNCATION but not a SUBSTITUTION —
    and, guarded by `if (path in CARD_MODULES)`, that the resolved module is non-empty, so a
    fence naming a module the glob no longer matches cannot read an empty string in silence.

Do NOT re-point any assertion in this task; that is Task 2. Do NOT touch any production file.
Do NOT rename, delete or re-describe any existing `it(` or `describe(`.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/PhaseNodeCard.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with **0 failed** and a test count of **108** (107 + the one new control). Record both numbers verbatim.
- `grep -c "CARD_SUBTREE_PATHS" frontend/src/components/workflows/PhaseNodeCard.test.tsx` returns **≥ 4** (the declaration, the length pin, the `toContain` loop subject, the destination loop).
- `grep -n "\"\\./phaseNodeCardContract.ts\"\|\"\\./ownProperty.ts\"\|\"\\./NodeCornerMarks.tsx\"\|\"\\./NodeRunOverlay.tsx\"\|\"\\./NodeIconWell.tsx\"" frontend/src/components/workflows/PhaseNodeCard.test.tsx` returns **10 lines** — each of the five destinations named twice (once in the path list, once in the individual-name loop). A destination named only once is a substitution the length pin cannot see.
- `ls frontend/src/components/workflows/phaseNodeCardContract.ts` returns **no such file** at the end of this task. The fence names files that do not exist, and that is the property being shipped.
- `git diff --name-only` lists **exactly one file**: `frontend/src/components/workflows/PhaseNodeCard.test.tsx`. Zero production source touched.
- **C-1 RED OBSERVED**, failure text recorded in the SUMMARY: (a) temporarily truncate `CARD_SUBTREE_PATHS` to five entries and confirm the `toHaveLength(6)` assertion FAILS; (b) revert, then temporarily SUBSTITUTE `"./NodeRunOverlay.tsx"` for `"./nodePresentation.ts"` (a real file) and confirm the individual-name loop FAILS while `toHaveLength(6)` still passes — that asymmetry is the whole reason the loop exists. Revert both. Record both failure texts.
  </acceptance_criteria>

  <done>`cardSubtreeSource` exists, names all five destination modules before any of them exists, resolves without throwing, and its three non-vacuity controls are green and were each observed failing under the specific edit they are built to catch.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point all 17 negative fences and the 4 haystacks, and re-anchor both landmines (D-08 and P4)</name>

  <files>
frontend/src/components/workflows/PhaseNodeCard.test.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:195-275` — the tab-stop / badge-budget
  region containing N1 (`:214`).
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:355-440` — the purity fence set
  (N2-N11), including `GRAPH_PKG_SCOPE`'s assembled-from-halves idiom and the positive control
  each fence carries.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:1040-1120` — `SEAL_GUARD_OPEN`,
  `SEAL_GUARD_CLOSE`, `sealJsxBlock`, `statusIdentifier`, the `PLANTED_SEAL_SOURCE` control and
  P1/P2/P3. Read `sealJsxBlock`'s docblock: it THROWS rather than returning `""` on a miss,
  deliberately, and that is why N12 is a hard error rather than a vacuous pass.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:1676-1790` — N13-N17, P4, P5, P6,
  `stripComments` (`:1710-1720`) and the assembled needles `CLIP_UTILITY`/`SVG_TRANSFORM_ATTR`/
  `CSS_ROTATE` (`:1722-1725`).
- `frontend/src/components/workflows/PhaseNodeCard.tsx:772` — the tree's ONLY mention of the
  clipping utility, inside the 3D-mark JSX comment that D-04 moves to `NodeIconWell.tsx`. This
  is D-08's landmine; read the sentence before re-anchoring the count that pins it.
- `frontend/src/components/workflows/PhaseNodeCard.tsx:734` — the `canvas-ring-spin` class P4
  pins, which moves to `NodeRunOverlay.tsx`.
  </read_first>

  <action>
Re-point the SUBJECT of every one of the 17 negative fences enumerated in `<interfaces>` from
`phaseNodeCardSource` to `cardSubtreeSource`: N1 `:214`, N2 `:367-369`, N3 `:370`, N4 `:371`,
N5 `:399`, N6 `:409`, N7 `:410`, N8 `:411`, N9 `:418`, N10 `:426`, N11 `:431-433`, N12 `:1101`,
N13 `:1684` (ONLY the `phaseNodeCardSource` element of the array — `runVocabularySource` stays),
N14 `:1735`, N15 `:1745`, N16 `:1763`, N17 `:1764`.

Re-point the four HAYSTACKS: P1 `:1089` (`sealJsxBlock`'s argument), P2 `:1097` (the `/ 4`
bound's divisor operand — the BOUND itself is untouched; changing `/ 4` to `/ 2` is the wrong
fix and must not appear in the diff), P3 `:1114`, P4 `:1776`.

Leave P5 `:1695` and P6 `:1729` on `phaseNodeCardSource` and add a one-line `//` note at each
saying they are per-FILE by design — P5 asserts this file is still a real file, and P6 is a
CONTROL on the stripper, so widening either changes what it measures.

Add a `//` note at N15 recording D-08 in the plan's own voice: the count is re-anchored to the
subtree because it stays 1 wherever the rule lives and cannot be satisfied by shuffling prose
between files — the exact failure mode a per-file count invites during a refactor. Add the twin
note at P4 recording that it is the same landmine shape and that CONTEXT does not name it.

Update `stripComments`'s scoping docblock at `:1716-1717`. Its body needs NO change (two global
unanchored regexes, no single-file assumption, and block comments are balanced within each file
so the lazy match cannot straddle a boundary). The SENTENCE "Scoped to this file's needs: the
card contains no regex literal and no string containing `//`" must name the six-file subtree
instead, or it is a docblock asserting a scope it no longer has — the defect
`PhaseNodeCard.tsx:33-35` names in the card's own words.

This is a SUBJECT change throughout. Do not add, delete, rename or re-describe any `it(` or
`describe(`. Do not touch any production file.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/PhaseNodeCard.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with **0 failed** and the test count still **108** — every re-scoped fence is green against the UNCHANGED production tree, which is the proof the re-scope is behaviour-neutral.
- `grep -c "cardSubtreeSource" frontend/src/components/workflows/PhaseNodeCard.test.tsx` returns **≥ 24** (17 negatives + 4 haystacks + the declaration + the control's subjects). Record the exact number.
- `grep -n "phaseNodeCardSource" frontend/src/components/workflows/PhaseNodeCard.test.tsx` returns **exactly 4 lines**: the `?raw` import at `:37`, P5, and P6's two operands. Any fifth line is a fence that was missed — name it in the SUMMARY rather than leaving it.
- `git diff -U0 -- frontend/src/components/workflows/PhaseNodeCard.test.tsx | grep -c "^+.*length / 2"` returns **0**. The `/ 4` bound was not loosened; the haystack was widened instead.
- **C-4 RED OBSERVED** (the clip count), failure text recorded: temporarily add a second prose mention of the clipping utility inside a comment in `frontend/src/components/workflows/PhaseNodeCard.tsx`, re-run, confirm N15 fails with `expected 2 to be 1`, then `git checkout -- frontend/src/components/workflows/PhaseNodeCard.tsx`.
- **N5 RED OBSERVED** (the fence that matters most — the zero-graph-library property D-184-06 exists for), failure text recorded: temporarily add the graph package scope as a literal inside a comment in `PhaseNodeCard.tsx`, confirm N5 fails, revert. Do this against the CARD; the destinations do not exist yet.
- **C-7 RE-VERIFIED**: the shipped `PLANTED_SEAL_SOURCE` control near `:1104-1108` still passes after the re-scope, and `sealJsxBlock(cardSubtreeSource)` still carves a slice whose length is under `cardSubtreeSource.length / 4`. Record the carved block's length and the bound.
- `git diff --name-only` lists **exactly one file**. Zero production source touched.
  </acceptance_criteria>

  <done>All 17 negative fences and all 4 haystacks read the joined subtree source; both landmines (D-08's clip count and P4's spin-class positive) are re-anchored with their reasons recorded inline; `stripComments`' scoping sentence names the subtree; the suite is green against an unchanged card and two fences were each observed RED.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the third-badge typecheck invariant for the first time (D-09), and fence the ESM cycle before the modules exist</name>

  <files>
frontend/src/components/workflows/PhaseNodeCard.test.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.tsx:165-175` — `BadgeSlot2Tuple` and the
  `BadgeSlots` max-2 tuple union. Measured: the type is referenced by ZERO callers anywhere in
  the tree outside its own declaration, so the invariant it encodes is exercised by nobody.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:218-267` — the two-badge budget
  block. Read it and confirm for yourself that it tests badges RENDERING (0, 1, 2) and asserts
  nothing about the TYPE. That measurement is the reason this task exists.
- `frontend/src/components/workflows/deriveTier.ts:118-129` — the house idiom for a
  deliberately-unused local (`void _never`), which is what keeps `noUnusedLocals: true` from
  making the control fail for the wrong reason.
- `frontend/src/components/workflows/WorkflowCanvas.test.tsx:618-691` — the ESM-cycle fence,
  its inline-positive-controls-first structure, and the rationale paragraph at `:618-633`.
- `frontend/tsconfig.app.json` — confirm `noUnusedLocals: true`, `verbatimModuleSyntax: true`
  and `"include": ["src"]` (which is why test files are typechecked at all).
  </read_first>

  <action>
Add the D-09 control inside a live `it(` in the two-badge budget region. Declare a legal
`const two: BadgeSlots` holding two `BadgeSlot` values and assert `toHaveLength(2)` so it is
consumed. Then declare `const three: BadgeSlots` holding THREE values, preceded by a
`// @ts-expect-error` line whose comment text names the invariant ("a third badge is a typecheck
error"), and consume it with the house `void` idiom so `noUnusedLocals` cannot make the control
red for the wrong reason. Add a `//` note recording the measurement: this invariant is named in
`CLAUDE.md`, in the card's own docblock and in Phase 189's D-12, and until this line it was
asserted nowhere — `@ts-expect-error` occurs 0 times in the entire workflows directory.

Add the ESM-cycle fence as its own `describe(` block, copying `WorkflowCanvas.test.tsx:618-691`'s
structure exactly:
  · declare the two regexes named in `<interfaces>`;
  · FIRST `it(` — inline positive controls, before any negative: assert each of the four
    forbidden import shapes (static named, type-only, re-export, dynamic) matches its regex when
    given as a plain string literal, AND assert two NEGATIVE controls that must NOT match —
    `from "./PhaseNodeCard?raw"` and `from "./phaseNodeCardContract"` — because those two are
    exactly the false positives that would make this fence unusable in this file; also assert
    `CARD_MODULES["./PhaseNodeCard.tsx"].length` is greater than zero so the subject is real;
  · SECOND `it(` — loop over the five destination paths, and for each one guarded by
    `if (path in CARD_MODULES)` assert its source matches NEITHER regex. At this commit the
    guard skips every path, which is correct and must be SAID in a `//` note: the loop is
    authored here so the fence exists before the modules do, and Plan 05 is where it is observed
    RED against a deliberately planted back-import (C-5);
  · THIRD `it(` — the one-direction half, authored now and green now by vacuity, with the same
    `//` note: assert `phaseNodeCardSource` does not contain `"function NodeRunOverlay("`,
    `"function NodeCornerMarks("` or `"function NodeIconWell("`. Plan 06 makes it meaningful by
    adding the matching `import` assertions once the imports exist.

Carry the rationale paragraph in the 188.1 voice with the one substitution that makes it true
here: `PhaseNodeCard` will render `<NodeRunOverlay>`, `<NodeCornerMarks>` and `<NodeIconWell>`
as JSX elements, which is a VALUE reference at module scope, so a back-import closes a live ESM
cycle that typechecks clean, lints clean and fails only at RUNTIME as a TDZ `ReferenceError` in
whichever module the first caller reaches. `verbatimModuleSyntax: true` is why the type-only
form is forbidden too.

Touch no production file.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json 2&gt;&amp;1 | grep -c "error TS"</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` returns exactly **33**, and `... | grep -c "src/components/workflows"` returns **0**. Never `tsc -b`; never a bare `npx tsc --noEmit` (which resolves no files and exits 0 having checked nothing).
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with **0 failed** and a test count of **112** (108 + the D-09 control + the three cycle-fence blocks). Record the actual number; if it differs, say why rather than adjusting the claim.
- **C-3 RED OBSERVED**, both directions, failure text recorded: temporarily widen `BadgeSlots` in `frontend/src/components/workflows/PhaseNodeCard.tsx` to `readonly BadgeSlot[]`, re-run the type gate, and confirm it reports exactly **34** with the extra error being `Unused '@ts-expect-error' directive` on the D-09 line. Then `git checkout --` the card and confirm the gate returns to exactly **33**. A control that only ever reads 33 is not known to be a control.
- The `@ts-expect-error` line is not suppressed by an eslint disable and `cd frontend && npx eslint src/components/workflows/` still reports exactly **5 errors, 0 warnings**, none of them in `PhaseNodeCard.test.tsx`.
- The cycle fence's inline NEGATIVE controls are green: `'import type { X } from "./PhaseNodeCard?raw"'` and `'import { own } from "./phaseNodeCardContract"'` each fail to match both regexes. Record that both were asserted — an over-broad regex here would make Plan 05's fence unfixable.
- `git diff --name-only` lists **exactly one file**. Zero production source touched (the two temporary edits above are reverted and `git status` is clean of them).
  </acceptance_criteria>

  <done>The third-badge invariant is asserted for the first time and was observed RED against a widened union at exactly 34 errors; the ESM-cycle fence exists with inline positive AND negative controls before the modules it guards; the type gate reads 33 and the lint gate reads 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped guard → refactor | A source fence is the only mechanical record that a scope promise still holds. A refactor that moves code out from under a fence crosses this boundary in total silence: nothing fails, no gate reports it, and a green suite is indistinguishable from a suite that stopped asking. |
| type system → runtime | `BadgeSlots`' max-2 tuple is a compile-time guarantee with zero callers. Widening it is invisible to every runtime test in the estate. |
| module graph → runtime | An ESM cycle typechecks clean and lints clean. No build tool in this repo can see it; it surfaces only as a TDZ `ReferenceError` at first import. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188.2-01 | Repudiation | the 17 negative `?raw` fences in `PhaseNodeCard.test.tsx` | mitigate | Re-point all 17 plus the 4 haystacks at `cardSubtreeSource`, whose path list names all five destinations before they exist; carry all three 188.1 non-vacuity controls; observe C-1 RED under BOTH a truncation and a substitution, and N5 RED under a planted graph-package literal |
| T-188.2-02 | Tampering | fence N10 `:426` — the `dangerouslySetInnerHTML` ban (T-124-01 / T-184-03-01) | mitigate | N10 is one of the fifteen that go green-vacuous. Re-scoped in Task 2 so it covers all five modules, three of which will render authored strings (titles, subtitles, badge labels). ASVS V5 |
| T-188.2-03 | Spoofing | the `BadgeSlots` max-2 tuple invariant (D-09) | mitigate | Asserted for the first time with an `@ts-expect-error` control, observed RED at exactly 34 errors against a deliberately widened union, with the `noUnusedLocals` trap closed by the house `void` idiom |
| T-188.2-04 | Denial of Service | an ESM cycle between `PhaseNodeCard` and any of its five new modules | mitigate | The `?raw` cycle fence with inline positive AND negative controls, authored in Task 3 before the modules exist; the negative controls specifically prove the regex cannot false-positive on `?raw` or on `phaseNodeCardContract` |
| T-188.2-05 | Repudiation | fence N15 `:1745` and positive P4 `:1776` — the two whose single needle moves | mitigate | Both re-anchored to the subtree source in Task 2, each with its reason recorded inline. P4 is a landmine CONTEXT does not name and would otherwise be discovered at execution as a "test to fix" |
| T-188.2-SC | Tampering | npm / pip / cargo installs | mitigate | **Zero packages are installed by this phase.** `188.2-RESEARCH.md` § Package Legitimacy Audit records "not applicable — this phase installs no external packages". Hard gate at plan close: `git diff --stat -- frontend/package.json frontend/package-lock.json` reports **0 files changed**. Any package appearing here is out of scope by construction and requires a blocking human checkpoint before install |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` → 0 failed
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → exactly 33
- `cd frontend && npx eslint src/components/workflows/` → exactly 5 errors, 0 warnings
- `node scripts/vitest-count-gate.cjs` → `count gate OK`, failed 0, no `[count-decrease]`, no `[missing-file]`
- `git diff --name-only` → exactly `frontend/src/components/workflows/PhaseNodeCard.test.tsx`
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json scripts/` → 0 files changed
</verification>

<success_criteria>
1. All 17 negative fences and all 4 haystacks read `cardSubtreeSource`, whose path list names all five destination modules before any of them exists (D-07). The two per-file assertions that must NOT widen are annotated as such.
2. Both landmines are re-anchored with their reasons recorded inline: D-08's clip count and P4's `canvas-ring-spin` positive.
3. The three non-vacuity controls are green and were each observed failing under the specific edit they exist to catch — including the SUBSTITUTION a length pin cannot see (C-1).
4. `stripComments`' scoping docblock names the six-file subtree.
5. D-09's third-badge invariant is asserted and was observed RED at exactly 34 type errors against a widened union, returning to exactly 33 on revert (C-3).
6. The ESM-cycle fence exists with inline positive AND negative controls, before the modules it guards.
7. Zero production files changed — this plan cannot alter behaviour. The type gate reads 33, the lint gate reads 5, the count gate is green.
</success_criteria>

<output>
Create `.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-01-SUMMARY.md` when done. Record verbatim: the vitest test count after each task, the `grep -c` numbers for `cardSubtreeSource` and `phaseNodeCardSource`, the exact `tsc` counts at 33 / 34 / 33, and the full failure text of every RED observation (C-1 truncation, C-1 substitution, C-4, N5, C-3).
</output>
