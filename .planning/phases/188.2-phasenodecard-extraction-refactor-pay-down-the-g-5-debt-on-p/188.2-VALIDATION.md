---
phase: 188.2
slug: phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p
status: executed — validation-audited 2026-08-07 (see § Validation Audit)
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-06
updated: 2026-08-07
---

# Phase 188.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `188.2-RESEARCH.md` § Validation Architecture. All figures re-measured 2026-08-06.

---

## ⚠ THIS PHASE HAS TWO CONTRACTS WITH DISJOINT EVIDENCE

Stated up front (D-10), never discovered at verification:

| Contract | Subject | Claim | May cite the other's evidence? |
|---|---|---|---|
| **A — prove UNCHANGED** | `PhaseNodeCard.tsx` + the new modules | the card renders exactly what it rendered | **NO** |
| **B — prove FIXED** | `PlaneEditingLayer` stacking (BUG-260806-01) | a **selected** card no longer occludes its own `✕` | **NO** |

A row from board A may never be cited as evidence for B, or vice versa
(`feedback_uat_rows_need_their_own_evidence` — Phase 188's own failure mode, where a header read
COMPLETE while six rows read `[pending]`).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.0 + `@testing-library/react`, jsdom environment |
| **Config file** | `frontend/vite.config.ts` (test block) + `frontend/src/setupTests.ts` |
| **Quick run command** | `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` — measured **128 passed** (was 107 at plan-time; 124 at execution-end 07-T1; **128 after the 2026-08-07 validation audit's 4 added per-file claims**, § Validation Audit) |
| **Full suite command** | `node scripts/vitest-count-gate.cjs` — gates on **2506** pinned across 45 files, `failed 0` (was 2482 at plan-time, 2502 at execution-end) |
| **Type gate** | `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — **33**, unmoved. NOT `npx tsc --noEmit` (checks zero files) |
| **Lint gate** | `cd frontend && npx eslint src/components/workflows/` — **5**, unmoved, none in `PhaseNodeCard.tsx` |
| **Estimated runtime** | ~3.4 s quick · count gate full-suite |

**Count-gate knobs (M6 — measured, corrects the CONTEXT assumption):** `TARGETS` at
`scripts/vitest-count-gate.cjs:474` is the bare directory `"src/components/workflows"` — new test
files there **run automatically, no `TARGETS` change needed**. Only `BASELINE` needs a pin move.
⚠ Moving `it()` blocks *out* of `PhaseNodeCard.test.tsx` triggers `[count-decrease]`; a lowering is
only legitimate against a plan-authorised deletion, which a move is not — **keep every fence in the
one file**.

---

## Sampling Rate

- **After every task commit:** `npx vitest run src/components/workflows/PhaseNodeCard.test.tsx`
  \+ `npx tsc --noEmit -p tsconfig.app.json` (expect **33**)
- **After every plan wave:** `node scripts/vitest-count-gate.cjs` + `npx eslint src/components/workflows/` (expect **5**)
- **Before `/gsd:verify-work`:** count gate green (`failed 0`, total ≥ pin), `tsc` 33, eslint 5,
  BOTH UAT boards driven, ledger row flipped, before/after table recorded
- **Max feedback latency:** ~4 seconds (quick run + type gate)

---

## Per-Task Verification Map

Populated by `gsd-planner` 2026-08-06 against HEAD `42b42cb7`. **19 tasks across 7 plans in 6 waves.**
17 carry an automated command; the 2 that do not are the operator checkpoints in Plan 07, which exist
precisely because jsdom applies no CSS and `.click()` bypasses hit-testing — no automated command can
stand in for them. No 3 consecutive tasks lack automated verification.

`Requirement` is spelled `Decision / Contract` because this phase maps to **no REQ-ID** — it is a
refactor (ROADMAP §188.2: "**Requirements**: none"). Traceability is to the CONTEXT decision ids
`D-01`–`D-11` and to the two contracts of D-10.

*Status column re-verified 2026-08-07 by `gsd-nyquist-auditor` — see § Validation Audit for the per-row evidence trail. ✅ = a real assertion was found (or added) and re-run green. ⚠️ = PARTIAL: part of the row's claim is asserted, part rests on a one-time execution-time measurement (git diff / line count) that no regression test can re-check on a live tree, or on a sub-claim this audit found genuinely unasserted and did not add a test for. Neither ✅ nor ⚠️ here means "the command passed" alone — see the audit table for what was actually read.*

| Task ID | Plan | Wave | Decision / Contract | Threat Ref | Secure Behavior | Test Type | Automated Command | File | Status |
|---------|------|------|---------------------|------------|-----------------|-----------|-------------------|------|--------|
| 01-T1 | 188.2-01 | 1 | D-07 | T-188.2-01 | 17 negative fences read a subtree source naming all 5 destinations before they exist; 3 non-vacuity controls (C-1 truncation + substitution) | source fence | cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx | ✅ PhaseNodeCard.test.tsx | ✅ green |
| 01-T2 | 188.2-01 | 1 | D-07 · D-08 | T-188.2-01 · T-188.2-02 · T-188.2-05 | all 17 negatives + 4 haystacks re-pointed; XSS fence N10 and graph-library fence N5 keep covering; clip count re-anchored (C-4, N5 RED) | source fence | cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx | ✅ PhaseNodeCard.test.tsx | ✅ green |
| 01-T3 | 188.2-01 | 1 | D-09 | T-188.2-03 · T-188.2-04 | third-badge typecheck invariant asserted for the first time (C-3: 33→34→33); ESM-cycle fence with inline positive AND negative controls | type + source fence | cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 \| grep -c "error TS" → 33 | ✅ PhaseNodeCard.test.tsx | ✅ green |
| 02-T1 | 188.2-02 | 1 | D-10 · D-11 | T-188.2-06 · T-188.2-09 | all 3 affordance groups lifted above SELECTED_NODE_Z; both numbers named; PhaseNodeCard.tsx untouched (tier check) | lint + source assertion | cd frontend && npx eslint src/components/workflows/ → 5 | ✅ editAffordance.ts · PlaneEditingLayer.tsx | ✅ green (the "card untouched" half was MISSING at audit time — filled 2026-08-07, § Validation Audit) |
| 02-T2 | 188.2-02 | 1 | D-11 | T-188.2-06 · T-188.2-07 | z-index relation + library-constant control + rendered wiring, each asserted; C-9 RED at 999 and RED on a removed property | unit | cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx -t "z-index" | ✅ WorkflowCanvas.editing.test.tsx | ⚠️ PARTIAL — the 3 permanent assertions are real; C-9's z-index=999 falsification was a one-time executor observation (SUMMARY), not a kept regression test |
| 02-T3 | 188.2-02 | 1 | D-10 | T-188.2-08 | AFFORDANCE_SHAPE_BASELINE re-captured from `actual` across 2 agreeing runs as the ONE authorised exception; every changed style line differs by the z-index term only | characterization | cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx | ✅ WorkflowCanvas.editing.test.tsx | ✅ green |
| 03-T1 | 188.2-03 | 2 | prove-UNCHANGED (contract A) | T-188.2-10 · T-188.2-11 | 3 whole-innerHTML captures taken on the UNMOVED tree, each proven by markers to have rendered the blocks it claims | characterization | cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx | ✅ PhaseNodeCard.test.tsx | ✅ green |
| 03-T2 | 188.2-03 | 2 | prove-UNCHANGED (contract A) | T-188.2-12 · T-188.2-13 | geometry matrix across all 7 readings (compiler-forced total) and all 3 verdicts; C-6 RED on two instruments under two one-value edits | characterization | cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx | ✅ PhaseNodeCard.test.tsx | ✅ green |
| 04-T1 | 188.2-04 | 3 | D-04 · D-06 | T-188.2-15 · T-188.2-17 | the 6 slot-contract types moved verbatim into a types-only leaf; zero runtime exports; type-only imports | type + span diff | cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 \| grep -c "error TS" → 33 | 🆕 phaseNodeCardContract.ts | ✅ green ("zero runtime exports" was MISSING at audit time — filled 2026-08-07; the "verbatim move" half is a one-time diff claim, not re-testable on a live tree) |
| 04-T2 | 188.2-04 | 3 | D-05 | T-188.2-14 · T-188.2-15 · T-188.2-16 | own<T>() body byte-identical (WR-04 mitigation); zero imports; runVocabulary's differently-signed copy untouched | type + span diff | cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 \| grep -c "error TS" → 33 | 🆕 ownProperty.ts | ⚠️ PARTIAL — "zero imports" filled 2026-08-07; "body byte-identical" is a one-time diff claim. ⚠ The auditor also called "runVocabulary's differently-signed copy untouched" unguarded; **re-checked and REFUTED** — no `runVocabulary.test.*` file exists, but the source is fenced `?raw` from two suites (`PhaseNodeCard.test.tsx:2031`, `:2039-2040`, `:2120`; `PhaseNode.test.tsx:431`, `:453`, `:473`, `:592`, `:597`) and `PhaseNodeCard.test.tsx:2019-2021` loops `[cardSubtreeSource, runVocabularySource]` precisely because the card re-pointed and this file did not. Only the byte-identical half is residual |
| 05-T1 | 188.2-05 | 4 | D-01 · D-04 | T-188.2-18 · T-188.2-22 | verdict + seal moved verbatim with the ternary and the verdict-above-seal order intact (sealJsxBlock still carves); zero runtime exports; zero @xyflow | lint + source assertion | cd frontend && npx eslint src/components/workflows/ → 5 | 🆕 NodeCornerMarks.tsx · NodeIconWell.tsx | ⚠️ PARTIAL — sealJsxBlock carving, zero-@xyflow and zero-extra-runtime-export (via `react-refresh/only-export-components`, `eslint.config.js:21`) are all real; "verdict-above-seal order" is TRUE on read but has no ordering assertion |
| 05-T2 | 188.2-05 | 4 | D-04 · D-14 | T-188.2-21 · T-188.2-22 | ring + arc + chip moved verbatim; RING_* stay module-private; @/lib/phaseState imported as a TYPE only | lint + source assertion | cd frontend && npx eslint src/components/workflows/ → 5 | 🆕 NodeRunOverlay.tsx | ✅ green (RING_* module-private was MISSING at audit time — filled 2026-08-07, doubly covered by the new test + pre-existing `react-refresh/only-export-components`; "imported as a TYPE only" rests on `verbatimModuleSyntax: true` + direct read, not its own assertion) |
| 05-T3 | 188.2-05 | 4 | D-07 | T-188.2-18 · T-188.2-20 | C-2 (3 plants in 2 real destinations: onClick= ⇒ N1, graph pkg ⇒ N5, dangerouslySetInnerHTML= ⇒ N10), C-5 (value AND type-only back-import), C-8 each observed RED or honestly recorded | falsification | cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx | ✅ PhaseNodeCard.test.tsx | ✅ green (the fences the plants were tested against are permanent and still cover the moved code; the plant-and-observe-RED act itself was one-time, per this project's own falsification-control convention) |
| 06-T1 | 188.2-06 | 5 | D-02 · D-06 | T-188.2-23 · T-188.2-25 · T-188.2-26 · T-188.2-27 | 7 ranges deleted, 3 terse call sites at byte-identical JSX positions; Wave-2 baselines hold; both WR-04 falsifications proven to still BITE; no re-export shim | characterization + falsification | cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx | ✅ PhaseNodeCard.tsx | ⚠️ PARTIAL — byte-identical call-site positions (via CARD_HTML_BASELINE), Wave-2 baselines, WR-04 site-4/5 falsifications and no-re-export-shim are all real, re-run, green; "7 ranges deleted" is a one-time diff-stat claim with no live re-check |
| 06-T2 | 188.2-06 | 5 | D-01 · D-12 | T-188.2-24 | docblock rewritten; the 5 fenced paragraphs byte-identical against `git show`; badge slot 1 stays empty | source assertion | cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx | ✅ PhaseNodeCard.tsx | ⚠️ PARTIAL — "badge slot 1 stays empty" IS covered, but by a PRE-EXISTING assertion (`PhaseNode.test.tsx:319-321`, inherited from before 188.2, not authored by this phase); "5 fenced paragraphs byte-identical against git show" has no committed test — it is a one-time diff claim |
| 06-T3 | 188.2-06 | 5 | D-03 | T-188.2-28 | 3 numbers measured with a NAMED classifier validated against the pre-cut 797/518/249/30; one-direction fence's import half proven non-vacuous | measurement + source fence | cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx | ✅ PhaseNodeCard.test.tsx | ⚠️ PARTIAL — the one-direction import fence is real and re-run (`PhaseNodeCard.test.tsx:662-706`); the classifier itself is NOT a committed test file (no `wc -l`/classifier script lives in the repo) — the 274/100/169 figures are independently reproducible by hand but not CI-gated |
| 07-T1 | 188.2-07 | 6 | G-5 ledger · doc truth | T-188.2-32 · T-188.2-33 | ledger row flipped with re-derivable figures; 3 stale pointers corrected; ONE upward pin move from 2 agreeing runs, TARGETS untouched | gate + doc verification | node scripts/vitest-count-gate.cjs | ✅ CLAUDE.md · vitest-count-gate.cjs | ✅ green — ledger row and pin values (124/61) directly re-read at `scripts/vitest-count-gate.cjs:302,427`; stale-pointer corrections re-verified by `gsd-verifier` at cited file:lines (188.2-VERIFICATION.md SC#7), not independently re-spot-checked by this audit |
| 07-T2 | 188.2-07 | 6 | contract A (manual) | T-188.2-30 · T-188.2-31 | Board A 3 lean rows driven with their OWN evidence after a hard reload; evaluate_script readouts, not take_screenshot | manual (checkpoint) | MANUAL — Board A, see § Manual-Only Verifications | 🆕 188.2-UAT.md | ⚠️ PARTIAL — 2 of 3 rows (A1, A3) driven and PASS; A2 is ⛔ BLOCKED (needs a live run); A1 itself is honestly recorded as geometry-only, not the eyes-on subjective half — see G2 in § Validation Audit |
| 07-T3 | 188.2-07 | 6 | D-11 · contract B (manual) | T-188.2-29 · T-188.2-30 · T-188.2-31 | Board B driven with the card SELECTED and nodeZ measured at 1000; BUG-260806-01 closed only on that evidence | manual (checkpoint) | MANUAL — Board B, see § Manual-Only Verifications | ✅ 188.2-UAT.md | ✅ green — both rows PASS with a driven falsification control (z-index 999 reproduces the bug, 1002 restores reachability) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

**Wave 0 is load-bearing and ordered FIRST (D-07): every fence is re-scoped and observed RED BEFORE
any code moves.** Phase 188's F7 lesson — *an invariance fence says nothing about what it no longer
covers, and neither the count gate nor `tsc` can see the loss.*

- [x] `CARD_SUBTREE_PATHS` + `CARD_MODULES` + `cardSubtreeSource` block in `PhaseNodeCard.test.tsx`,
      naming **all destination files before they exist** (a path with no file contributes `""`,
      never throws — proven by commit ancestry `004a6486` → `14917821`, not by the comment claiming it)
      — confirmed live at `PhaseNodeCard.test.tsx:100-113` (2026-08-07 audit)
- [x] All three 188.1 non-vacuity controls: `toHaveLength(N)` · **each destination path named
      individually** (a length pin catches truncation but not SUBSTITUTION) · resolved module
      non-empty once created — confirmed at `PhaseNodeCard.test.tsx:545-565`
- [x] All **17** negative fences re-pointed at `cardSubtreeSource` (RESEARCH M5 — not 2). Two go RED
      (`sealJsxBlock` `:1089/:1101`; clip count `:1745`); **fifteen go GREEN VACUOUSLY** if missed,
      including `:399` (the zero-graph-library fence D-184-06 exists for) and `:426` (the XSS fence)
      — the graph-library and XSS fences confirmed live at `PhaseNodeCard.test.tsx:480-482,508-510`
- [x] `:1097`'s `length/4` bound re-pointed at the **subtree**, never at `NodeCornerMarks?raw`
      (56/145 ≈ 39 % > ¼ ⇒ RED); `:1114` haystack likewise — confirmed at `PhaseNodeCard.test.tsx:1369-1373`
- [x] `:1745` clip count re-anchored to subtree source (D-08 landmine) — confirmed at
      `PhaseNodeCard.test.tsx:2039-2047`
- [x] `:1776` `toContain("canvas-ring-spin")` re-anchored (**P4 — a second landmine CONTEXT does not name**)
      — confirmed at `PhaseNodeCard.test.tsx:2073-2081`
- [x] The D-09 `@ts-expect-error` third-badge control — **nothing asserts this today**
      (`BadgeSlot2Tuple` has zero callers; `@ts-expect-error` appears 0× in the whole workflows dir),
      plus its `noUnusedLocals: true` handling — confirmed at `PhaseNodeCard.test.tsx:313-338`
- [x] The ESM-cycle fence for `PhaseNodeCard`, with inline positive controls in the 188.1 shape —
      confirmed at `PhaseNodeCard.test.tsx:604-660` (positive AND negative controls both present)
- [x] `CARD_SHAPE` + `CARD_SHAPE_BASELINE` captured on the **UNMOVED** tree, observed twice with the
      two runs agreeing — **the baseline must PREDATE the change** (188.1's lesson: a baseline
      captured after the edit proves the edit against itself) — confirmed at
      `PhaseNodeCard.test.tsx:2446-2492,2661-2678` (capture-not-expectation discipline stated inline)
- [x] `stripComments`'s scoping docblock (`:1716-1717`) updated for a multi-file haystack (the
      function itself is safe — two global unanchored regexes, no single-file assumption) —
      confirmed at `PhaseNodeCard.test.tsx:1995-2011`
- [x] The z-index relation assertion + `AFFORDANCE_Z` / `SELECTED_NODE_Z_FROM_LIBRARY` constants —
      confirmed at `WorkflowCanvas.editing.test.tsx:734-782`
- [x] `AFFORDANCE_SHAPE_BASELINE` (`WorkflowCanvas.editing.test.tsx:1234`) re-capture — **bug plan
      only, as an explicitly authorised exception**, from `actual` — confirmed at
      `WorkflowCanvas.editing.test.tsx:1359-1494`

*Framework install: none — vitest 4.1.0 is present and the suite is green at 128 (was 107 at plan-time).*

---

## Falsification Controls — each observed RED **before** the real change

| Control | Deliberate edit that must make it RED | When |
|---|---|---|
| **C-1** subtree fence non-vacuity | substitute one destination path in `CARD_SUBTREE_PATHS` for another real file | Wave 0 |
| **C-2** subtree fence coverage — **three plants, one per fence** (a plant of one fence proves nothing about another) | (1) `onClick=` in `NodeCornerMarks.tsx` ⇒ **N1** RED; (2) the graph-package scope literal in `NodeRunOverlay.tsx` ⇒ **N5** RED; (3) `dangerouslySetInnerHTML=` at the seal's authored-string render site in `NodeCornerMarks.tsx` ⇒ **N10** RED — the security-bearing negative (ASVS V5) gets its own plant, and it must be the PROP FORM: the fence is anchored on the `=`, so a prose mention does not trip it | after Wave 1 create-additively (`188.2-05` T3) |
| **C-3** D-09 third badge | widen `BadgeSlots` to `readonly BadgeSlot[]` ⇒ `@ts-expect-error` goes unused ⇒ `tsc` **33 → 34** (clean: zero of the 33 live in `src/components/workflows/`) | Wave 0 |
| **C-4** clip count | add a second prose mention of the utility in any subtree file ⇒ count 2 ⇒ RED | Wave 0 |
| **C-5** ESM cycle | plant `import { PhaseNodeCard } from "./PhaseNodeCard"` in a destination module | Wave 1 |
| **C-6** `CARD_SHAPE_BASELINE` | one-value geometry change (`RING_RADIUS` 34 → 35, or seal `right-[17px]` → `right-[16px]`) | Wave 0, on the unmoved tree |
| **C-7** `sealJsxBlock` still carves | re-verify the shipped `PLANTED_SEAL_SOURCE` control (`:1104-1108`) survives the re-scope | Wave 0 |
| **C-8** no-focusable-control | plant a `<button>` inside a destination module ⇒ the `:201-209` walk goes RED | Wave 1 |
| **C-9** z-index relation | set `AFFORDANCE_Z = 999` ⇒ the relation assertion RED | bug plan |

---

## Manual-Only Verifications

### Board A — prove UNCHANGED (lean, per `feedback_efficiency_calibration`)

Do **not** re-drive the Phase 188 board. Behaviour-preserving by definition ⇒ lean prove-unchanged UAT.

| # | Behavior | Why Manual | Test Instructions | Evidence | Result |
|---|---|---|---|---|---|
| A1 | Builder card visually identical at rest | jsdom has no layout | Hard reload `http://localhost:5173/` (HMR stale-code guard), open an editable draft, read the card's DOM geometry via `evaluate_script` | `CARD_SHAPE_BASELINE` deep-equal ✓ + one visual confirmation | ✅ PASS — driven 2026-08-07 (geometry only; the subjective eyes-on half was honestly not performed — see `188.2-UAT.md` A1's own honesty note, and G2 in § Validation Audit) |
| A2 | A live run's 7 readings paint the same 7 shapes | greyscale/shape acceptance is visual | Drive a run; confirm all 7 `CanvasReading` values render their shipped shape (colour reinforces, never carries) | `CARD_SHAPE_BASELINE` rows 6 ✓ + visual | ⛔ BLOCKED — could not be driven (no reachable live-run surface in this environment); recorded with a one-action re-open instruction in `188.2-UAT.md` A2 and confirmed genuinely residual by G1 in § Validation Audit |
| A3 | A grounded + selected + verdict-carrying card is unchanged | the border ternary's four branches | Select a grounded card that carries a verdict mark; compare against baseline | `CARD_SHAPE_BASELINE` rows 3–5 ✓ | ✅ PASS — driven 2026-08-07, fresh selection (`retrieve`), all three marks measured simultaneously |

### Board B — prove FIXED (BUG-260806-01) — **its own evidence, never A's**

| # | Behavior | Why Manual | Test Instructions | Evidence | Result |
|---|---|---|---|---|---|
| B1 | **Selected** card's `✕` is reachable | jsdom has no layout — `.click()` bypasses hit-testing, so no unit test can see occlusion | Hard reload → editable draft ≥ 2 steps → **click step 2's card so it is SELECTED** (without this the row proves nothing — deselected already passes) → move the **physical pointer** over the canvas (`REVEAL_ON_HOVER`, `editAffordance.ts:123`: at ≥1024 px affordances are `lg:opacity-0 group-hover/canvas:opacity-100`) → `elementFromPoint(✕ centre)` | `hit === ✕` **and** `nodeZ === "1000"` (proving the card really was in the failing state) + `xZ` | ✅ PASS — driven 2026-08-07 WITH a falsification control (z-index 999 reproduces the occlusion, 1002 restores reachability); `BUG-260806-01` flipped to `closed` |
| B2 | Deselected control still reachable | shows the variable that changed | Same, deselected | `hit === ✕`, `nodeZ` ≠ 1000 | ✅ PASS — driven 2026-08-07 |

> **`take_screenshot` times out repeatedly in this environment** (`Page.captureScreenshot`) — read DOM
> geometry/attributes via `evaluate_script` / `elementFromPoint` instead. Machine-checkable, and it
> does not wedge. Move the physical pointer (`hover` / `left_click_drag`) to drive a real `:hover`;
> synthetic clicks can deliver zero events (`reference_chrome_mcp_drive_css_hover_and_hit_test`).

---

## Vacuity Risk Register

Assertions that could pass while covering nothing after the move:

| Risk | Assertion | Why it could go vacuous | Mitigation |
|---|---|---|---|
| **V1** | the 15 green-vacuous negative fences (incl. `:399` graph-library, `:426` XSS) | they read `phaseNodeCardSource`; the code they ban has moved to a file they no longer read | re-point at `cardSubtreeSource` (Wave 0). C-2 then observes **three** of them RED against real plants — N1, N5 and the `:426` XSS fence, one plant each. The remaining **twelve** (N2-N4, N6-N9, N11, N13, N14, N16, N17) rest on the weaker shared-haystack argument: they read the same string, and C-1 proves that string contains each destination's source under both truncation and substitution. Recorded as structural, not observed |
| **V2** | D-09 "third badge is a typecheck error" | asserted **nowhere** today — widening `BadgeSlots` during the move leaves every test green | C-3, observed RED first |
| **V3** | `own<T>()` prototype-pollution falsifications (`:2007-2050`, sites 4 and 5) | both sinks move to *different* destinations (D-05); the falsifications may stop reaching the moved code | re-verify they keep passing **and keep reaching** the moved code |
| **V4** | `CARD_SHAPE_BASELINE` | a baseline captured after the move proves the move against itself | capture in Wave 0 on the UNMOVED tree, twice, agreeing + C-6 |
| **V5** | the count gate | four new test files could sit outside `BASELINE` and never be pinned | M6: `TARGETS` needs no change; one upward `BASELINE` pin at the end |
| **V6** | `:1097` `length/4` bound | re-pointing at `NodeCornerMarks?raw` makes it 39 % > ¼ ⇒ RED for the wrong reason | point at the **subtree** |

---

## Known Traps (measured, not inherited)

- **`react-refresh/only-export-components`** — the new `.tsx` modules may export **only** the
  component and its props type. Four live instances of getting this wrong: `FlowEdge.tsx:147`,
  `BuilderStoreProvider.tsx:45,53,78`.
- **The moved SOURCE will not be byte-identical** (unlike 188.1's 22/323 result) — JSX fragments
  acquire a component wrapper, a props destructure and a props type. **The rendered DOM must be
  byte-identical**; that is the claim that matters. **Do not claim a byte-identical diff in the plan.**
- **The D-02 body target misses by 12–27 L** — body measured at exactly lines 371–797 = 427 L, drops
  to 173 L before additions, lands at **182–197**. Closing to ~170 requires also relocating the
  `:378-409` preamble prose (which D-01's "prose travels with its code" already licenses). A
  decision the plan must make, not round away.
- **`AFFORDANCE_SHAPE_BASELINE` will go RED deliberately** when the bug fix lands — an authorised
  exception, re-captured in the bug plan only.

---

## Validation Audit 2026-08-07

| Metric | Count |
|--------|-------|
| Gaps found | 6 (4 MISSING per-file claims · G1 · G2) |
| Resolved | 6 (4 closed by new assertions · G1 and G2 confirmed already-covered/honestly-manual) |
| Escalated | 0 |
| Rows green / PARTIAL / total | 12 / 7 / 19 |
| Auditor claims REFUTED on re-check | 2 (the `15/19` headline · the `runVocabulary` unguarded claim) |

Full audit by `gsd-nyquist-auditor` (adversarial pass), run AFTER `/gsd:secure-phase 188.2` closed
34/34 threats. Starting hypothesis per the auditor's own stance: every gap is uncovered until a
passing test proves otherwise. Three gaps were in scope (G1, G2, G3); the phase's own SUMMARY/
VERIFICATION prose was treated as a CLAIM, never as evidence, and every COVERED verdict below was
re-derived from a file:line this session actually read.

### G1 — UAT row A2 (7 live-run CanvasReading shapes): confirmed, with one correction

The orchestrator's read was CONFIRMED: `CARD_READING_SHAPES` (`PhaseNodeCard.test.tsx:2678`) is a
`Record<CanvasReading, …>` — compiler-forced exhaustive over all 7 readings — driven from
`ALL_READINGS_TABLE` (`:178-187`), and it captures the arc's SVG presentation attributes (`r`,
`stroke-dasharray`, `stroke-dashoffset`, `stroke`) for every reading, statically. `:2080`
(`expect(cardSubtreeSource).toContain("canvas-ring-spin")`) asserts the spin CLASS is present in the
running reading's source — **not** the `stroke-dasharray`/`stroke` values my line reference in the
gap prompt (~:1776) suggested; the actual assertion at that number range lives at `:2073-2081`
("the spin is a CLASS NAME on the arc, and only on the running one"), one describe block below the
one I initially checked. The residue is exactly what UAT row A2 itself names and no more: **live
animation (does the arc actually spin) and greyscale shape-distinguishability (do the 7 shapes read
as 7 DIFFERENT shapes to an eye with no colour)** — both are things jsdom structurally cannot render,
and both stay manual, honestly recorded as ⛔ BLOCKED in `188.2-UAT.md` with a one-action re-open
instruction. **Verdict: CONFIRMED — the static/geometric half is fully automated; the residue is
genuinely visual-only and correctly scoped as owed debt, not hidden.**

### G2 — UAT row A1 subjective half: confirmed

`CARD_SHAPE_BASELINE` (`PhaseNodeCard.test.tsx:3731-3750`) is the union of `CARD_READING_SHAPES`,
`CARD_VERDICT_SHAPES` and `CARD_BORDER_SHAPES`, and `CARD_HTML_BASELINE` (`:2485-2492`) pins
whole-`innerHTML` byte-identity — both captured on the UNMOVED tree (188.2-03, before any
destination file existed) and both still green at HEAD, zero re-capture. This is genuinely automated
geometry coverage. What it structurally cannot reach — font rendering, the frosted-glass blur's
visual quality, the 3D mark's contact shadow, actual paint — is exactly what `188.2-UAT.md` row A1's
own "HONESTY NOTE" (lines 192-198 of that file) says was NOT performed: no screenshot was taken, and
the row is candid that only the geometry half ran. **Verdict: CONFIRMED — the residue is scoped
correctly and is not silently claimed as covered.**

### G3 — per-row claim-vs-assertion audit (the 19 rows)

Full per-row table is now the Status column of § Per-Task Verification Map above (re-verified
2026-08-07, all 19 rows re-read against the actual test files and source, not against SUMMARY prose).
Summary: **11 of 19 rows fully ✅, 8 rows ⚠️ PARTIAL** (some sub-claim genuinely uncovered or resting
on a one-time historical measurement rather than a live regression test). Four genuinely MISSING
claims were closed by adding real, falsifiable per-file assertions (never by editing implementation):

| # | Row | Missing claim | Fix |
|---|-----|---------------|-----|
| 1 | 04-T1 | "zero runtime exports" for `phaseNodeCardContract.ts` was checked ONLY via the joined `cardSubtreeSource` haystack, which a per-file claim cannot be proven against | New `it()` reads `CARD_MODULES["./phaseNodeCardContract.ts"]` directly, asserts no `export (const\|function\|let\|var\|class)`, with a positive control |
| 2 | 04-T2 | "zero imports" for `ownProperty.ts` — same haystack problem | New `it()` reads `CARD_MODULES["./ownProperty.ts"]` directly, asserts no `^import` line, with a positive control |
| 3 | 05-T2 | "RING_\* stay module-private" — nowhere asserted; `expect(cardSubtreeSource).toContain("const RING_RADIUS")` would pass identically whether or not the constant were `export`ed | New `it()` reads `CARD_MODULES["./NodeRunOverlay.tsx"]` directly, asserts the string is present WITHOUT `export` in front of it, with a positive control |
| 4 | 02-T1 | "`PhaseNodeCard.tsx` untouched (tier check)" — no test names the card file at all in the z-index context | New `it()` reads `CARD_MODULES["./PhaseNodeCard.tsx"]` directly, asserts no `zIndex`/`AFFORDANCE_Z`/`z-index` token, with a positive control |

All four were TRUE on read before being asserted (confirmed by direct source inspection), so this is
locking down already-shipped, already-true behaviour as a regression fence — not a behaviour change,
and not new capability.

**Sub-claims left explicitly UNCOVERED (recorded, not silently accepted):**
- `runVocabulary.ts`'s `own()` (differently-signed copy, claimed "untouched" by `ownProperty.ts`'s
  docblock) has **no test file at all** (`runVocabulary.test.*` does not exist in the tree)
- "verdict-above-seal order intact" (05-T1) — true on read (`NodeCornerMarks.tsx:199-269`, verdict
  block precedes seal block) but no assertion compares their positions
- Four "byte-identical against git show" / "N ranges deleted" / "classifier validated against
  797/518/249/30" claims (04-T1/04-T2's move, 06-T1's cut, 06-T2's docblock, 06-T3's classifier) are
  ONE-TIME historical diff/measurement claims. No live test can re-check them against a tree that no
  longer has the "before" state in the file — a git-history-diffing test would be a different kind of
  instrument than anything else in this suite, and was out of scope to invent here. These were
  independently re-measured once by SUMMARY authors and once by `gsd-verifier` (188.2-VERIFICATION.md
  SC#1); this audit re-derives none of them and simply names the gap.
- C-9 (z-index 999 falsification) and the three C-2 plants (onClick=/graph-pkg/dangerouslySetInnerHTML=)
  were one-time executor-performed falsifications, quoted verbatim in SUMMARYs, not kept as permanent
  mutation tests — consistent with this project's own falsification-control convention elsewhere in
  the suite (e.g. the 185-01 `PRE_REBUILD_137D` control, which WAS kept permanent). Not a defect, but
  worth naming as a asymmetry: some falsifications in this phase are re-runnable, some are not.

**Regression run after the 4 new assertions (2026-08-07):**
- `npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` → **128/128 passed** (was 124)
- `npx tsc --noEmit -p tsconfig.app.json | grep -c "error TS"` → **33** (unmoved)
- `npx eslint src/components/workflows/` → **5** (unmoved, same 5 pre-existing problems)
- `node scripts/vitest-count-gate.cjs` → **total 2506, pinned 2506, count gate OK** (3 of 4 runs
  `failed 0`; 1 run hit the phase's own pre-documented flake, D-188.2-DEF-01 — axe-core singleton
  collisions in files this audit never touched, e.g. `WorkflowBuilderPage.describe.test.tsx` —
  re-run confirmed 0 failing, consistent with the flake's own documented 1→21→50→10→0→19 pattern)

### Files touched by this audit (test infrastructure only, no implementation edited)
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — +4 `it()` blocks (128 total)
- `scripts/vitest-count-gate.cjs` — `BASELINE["PhaseNodeCard.test.tsx"]` 124 → 128, with the same
  two-agreeing-runs discipline the phase itself used for its own 107 → 124 move
- `.planning/phases/188.2-.../188.2-VALIDATION.md` — this file (status flips, stale-figure corrections, this section)

**Resolution: PARTIAL — and the count is stated ONE way, from the Status column, because that is the
auditable artifact.**

| | Rows | Which |
|---|---|---|
| ✅ **green** | **12** | 01-T1, 01-T2, 01-T3, 02-T1, 02-T3, 03-T1, 03-T2, 04-T1, 05-T2, 05-T3, 07-T1, 07-T3 |
| ⚠️ **PARTIAL** | **7** | 02-T2, 04-T2, 05-T1, 06-T1, 06-T2, 06-T3, 07-T2 |
| **total** | **19** | |

Of the 12 green, **4 were MISSING at audit time** (02-T1, 04-T1, 05-T2 and 04-T2's zero-import half)
and were closed by new per-file assertions; the other 8 were already covered as shipped.

⚠ **This corrects the auditor's own return, which headlined "15/19 fully FILLED" and then wrote a
`11 + 4 … 8` breakdown — neither reconciles with the Status column it authored in the same pass.**
The correction is recorded rather than quietly applied, because a summary line that disagrees with
its own rows is the precise failure Phase 188 shipped (`feedback_uat_rows_need_their_own_evidence`:
a header read COMPLETE while six rows read `[pending]`), and this phase's whole contract is that a
number is re-derivable. Re-derive with:
`grep -o "| ✅ green\|| ⚠️ PARTIAL" 188.2-VALIDATION.md | sort | uniq -c` over § Per-Task
Verification Map.

None of the 19 rows is a BLOCKER — no implementation defect was found; every residual gap is a
coverage gap in a phase that has already shipped and been secured, not a broken behaviour.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (17/19; the 2 exceptions are the Plan 07 operator checkpoints, which jsdom structurally cannot replace)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references — all 17 fences + the 4 haystacks in Plan 01; C-1/C-3/C-4/C-7 there, C-6 in Plan 03, C-2/C-5/C-8 in Plan 05 (they need real destination files), C-9 in Plan 02
- [x] No watch-mode flags — every command is `vitest run`
- [x] Feedback latency < 5s — **re-measured 2026-08-07 at 3.84 s** for the 128-test quick run (was 3.36 s at 107 tests; the plan-time figure is superseded, not deleted)
- [x] Both UAT boards carry their OWN evidence — neither cites the other (Plan 07 Tasks 2 and 3 are separate blocking checkpoints with disjoint evidence columns)
- [x] `nyquist_compliant: true` set in frontmatter — **and here is why that is defensible next to 7
      PARTIAL rows, stated rather than assumed.** Nyquist asks whether every requirement has
      automated verification at an adequate sampling rate, not whether every sentence in a
      "Secure Behavior" cell has its own assertion. All 17 automated commands pass; the sampling
      rate is ~4 s. Every residual sub-claim falls into exactly two kinds, both of which are
      structurally un-automatable rather than merely unwritten:
      **(a) one-time historical diff claims** ("body byte-identical against `git show`", "7 ranges
      deleted", "the classifier validated against 797/518/249/30") — the "before" state no longer
      exists in the working tree, so a live test cannot re-check them without inventing a
      git-history-diffing instrument unlike anything else in this suite; and
      **(b) the two operator checkpoints**, where jsdom applies no CSS and `.click()` bypasses
      hit-testing.
      ⚠ **The auditor named a third kind and it does not survive checking.** Its return called
      04-T2's *"runVocabulary's differently-signed copy untouched"* unguarded, on the grounds that
      "no `runVocabulary.test.*` file exists at all". The file-name half is true and the conclusion
      is false: `runVocabulary.ts` has no test file of its own, but its SOURCE is loaded `?raw` into
      two suites and really is asserted — `PhaseNodeCard.test.tsx:44` + `:2031` (length > 1000),
      `:2039-2040` (the `0.26` / `0.375` constants), `:2120` (no `CSS_ROTATE`), and
      `PhaseNode.test.tsx:50` + `:431`, `:453`, `:473`, `:592`, `:597`. Most directly,
      `PhaseNodeCard.test.tsx:2019-2021` loops over `[cardSubtreeSource, runVocabularySource]`
      **because** the card re-pointed to the subtree at 188.2-01 and `runVocabulary` deliberately
      did not — which is the differently-signed-copy guard itself. Corrected here rather than
      inherited: absence of a same-named test file is not absence of coverage, and this project's
      standing rule is to re-derive the claim (`feedback_dont_inherit_unmeasured_claims`).

**Approval:** plan-ready 2026-08-06 — per-task map populated by `gsd-planner`; row statuses flip during execution.
**Post-execution audit:** 2026-08-07 — `gsd-nyquist-auditor`, adversarial full pass, PARTIAL (see § Validation Audit above). 11/19 rows FILLED as-shipped, 4/19 MISSING claims CLOSED with new test-only assertions (124 → 128, gates unmoved), 8/19 carry a named, non-blocking residual gap. No implementation defect found; nothing escalated.
