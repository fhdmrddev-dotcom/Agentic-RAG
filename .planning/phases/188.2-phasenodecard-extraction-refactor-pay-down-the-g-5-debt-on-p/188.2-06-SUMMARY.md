---
phase: 188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p
plan: 06
subsystem: frontend
tags: [react, tsx, hard-cut, extraction, esm-cycle, wr-04, docblock, line-classifier, d-06]

# Dependency graph
requires:
  - plan: 188.2-01
    provides: "`CARD_SUBTREE_PATHS` + the 17 re-scoped negative fences + the ESM-cycle fence whose one-direction `it(` was authored green-by-vacuity and is completed here"
  - plan: 188.2-03
    provides: "`CARD_HTML_BASELINE` + `CARD_SHAPE_BASELINE` / `CARD_READING_SHAPES` / `CARD_VERDICT_SHAPES` / `CARD_BORDER_SHAPES` — the pre-move rendered shape this cut had to reproduce with ZERO re-capture, and did"
  - plan: 188.2-04
    provides: "`phaseNodeCardContract.ts` + `ownProperty.ts` — the two leaves the card's deleted declarations already lived in"
  - plan: 188.2-05
    provides: "`NodeCornerMarks.tsx`, `NodeRunOverlay.tsx`, `NodeIconWell.tsx` — the three modules the four mark blocks already lived in, and fence N15 deliberately RED at 2"
provides:
  - "`PhaseNodeCard.tsx` at 274 L / 100 CODE / a 169-line body — the hard cut, 16 insertions against 499 deletions"
  - "three terse call sites at byte-identical JSX positions: every Wave-2 baseline green with ZERO re-capture"
  - "both importers re-pointed at `phaseNodeCardContract` with NO re-export shim, enforced mechanically"
  - "the one-direction fence completed — four import assertions + the no-shim half, both observed RED"
  - "fence N15 back to exactly 1; the WR-04 falsifications proven to still BITE their now-separated sinks; C-8's rendered half discharged"
affects: [188.2-07, 189]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "assemble a hard cut BY SCRIPT from explicit 1-based spans, with a line-anchor assertion table that THROWS on drift before a single byte is written — the deletion is then a property of the construction rather than of care"
    - "a named line classifier, written to the session scratchpad and VALIDATED against the pre-change file's known figures before any after-number is trusted"
    - "prove a moved fenced paragraph is byte-identical by searching the post-edit file for the pre-edit block as ONE contiguous string — a set-membership diff test cannot distinguish a shared separator line"

key-files:
  created: []
  modified:
    - frontend/src/components/workflows/PhaseNodeCard.tsx
    - frontend/src/components/workflows/PhaseNode.tsx
    - frontend/src/components/workflows/PhaseNodeCard.test.tsx
    - frontend/src/components/workflows/nodePresentation.ts
    - frontend/src/components/workflows/NodeCornerMarks.tsx
    - frontend/src/components/workflows/NodeRunOverlay.tsx
    - frontend/src/components/workflows/NodeIconWell.tsx

key-decisions:
  - "The `:54-57` badge-tuple paragraph was DELETED rather than relocated, because its destination already holds it: `phaseNodeCardContract.ts:97` carries the same argument, moved verbatim by 188.2-04. Relocating it would have created the second copy these headers exist to prevent."
  - "Task 1 left the preamble comment untouched and Task 2 did door (b), so the two commits are separable: one reads as a pure MOVE (16/499), the other as pure prose."
  - "The one-direction fence's no-shim half is anchored on the export FORM (`export * from`, `export {…} from`) rather than on any one type name, so a shim for a slot nobody has thought of yet is caught too. `WorkflowCanvas.test.tsx` names its one constant; this file cannot, because the contract has six names and 189 will add more."

patterns-established:
  - "A phase whose headline is 'the file got smaller' must state the SUBTREE total in the file's own docblock, measured, so the growth cannot later be read as a regression — and it must not inherit the estimate."
  - "When a plan gives a target and the measurement misses it, report the shortfall in lines; when it BEATS it, report the margin. Both are the same discipline."

requirements-completed: []  # EMPTY BY DESIGN — ROADMAP §Phase 188.2: "Requirements: none — refactor phase."

# Metrics
duration: 33min
completed: 2026-08-07
---

# Phase 188.2 Plan 06: THE CUT Summary

**The 797-line card is now 274 lines with a 169-line body and 100 lines of code, cut by deleting seven ranges and rendering three terse call sites — and the phase's first contract held on the first run: `CARD_HTML_BASELINE`'s three whole-`innerHTML` comparisons and all three Wave-2 geometry matrices are green with ZERO re-capture, so the rendered DOM is byte-identical to the tree as it shipped.**

## Performance

- **Duration:** ~33 min
- **Started:** 2026-08-07T03:20:00Z
- **Completed:** 2026-08-07T03:53:00Z
- **Tasks:** 3
- **Files modified:** 7 · **Files created:** 0

## Accomplishments

- **All three D-03 targets MET, none by rounding** — total **274** ≤ 380, CODE **100** ≤ 120, BODY **169** ≤ 175. The classifier reproduced the pre-cut file's known `797/518/249/30` exactly, on the first run, before any after-number was trusted.
- **`git diff --numstat` on the card reads `16  499`** at the cut commit — the diff reads as a MOVE.
- **Every Wave-2 baseline green with ZERO re-capture.** Not one character of `CARD_HTML_BASELINE`, `CARD_SHAPE_BASELINE`, `CARD_READING_SHAPES`, `CARD_VERDICT_SHAPES` or `CARD_BORDER_SHAPES` was touched.
- **Fence N15 returned 2 → 1**, measured per file: the clip-utility rule now exists only in `NodeIconWell.tsx`.
- **Both WR-04 falsifications proven to still BITE their now-separated sinks** — each guard temporarily removed from its NEW home, the corresponding falsification observed RED, then reverted.
- **C-8's rendered half, owed by 188.2-05, is DISCHARGED**: a planted `<button>` inside `NodeCornerMarks.tsx` drove **five** failures including three no-focusable-control walks. Plan 05 could not observe this because the module was not mounted; it is now.
- **The one-direction fence is no longer vacuous** — four import assertions plus the D-06 no-shim half, each observed RED under its own falsification.
- **`nodePresentation.ts`'s alias sentence corrected in the SAME COMMIT that made it false.**
- Gates unmoved: `tsc` **33** (0 in `src/components/workflows/`), `eslint` **5 errors / 0 warnings**, `count gate OK` **failed 0**, `PhaseNodeCard.test.tsx` **124/124**, `PhaseNode` + `WorkflowCanvas` + `WorkflowCanvas.editing` **139/139**.

## Task Commits

1. **Task 1: the hard cut, three call sites, two re-pointed importers** — `7ca0ec1b` (refactor)
2. **Task 2: settle the prose — docblock 110 → 75 L, door (b)** — `a4e60a04` (docs)
3. **Task 3: complete the one-direction fence** — `579504de` (test)

## Recorded measurements (verbatim, as `<output>` requires)

### The classifier's VALIDATION on the PRE-cut file

Written to the session scratchpad (never into the watched tree), implementing exactly the three rules the plan names:

```
$ git show 95a4c915:frontend/src/components/workflows/PhaseNodeCard.tsx | node classify.cjs --stdin
total=797  comment=518  code=249  blank=30
    expected: total=797  comment=518  code=249  blank=30
```

Reproduced on the first run. The region sums verify too: `110 + 28 + 147 + 1 + 84 + 427 = 797`, and the pre-cut body measured `total=427 comment=226 code=187 blank=14`.

### THE D-03 TABLE — the three numbers this phase is judged on

| Number | Before | After | Target | Verdict |
|---|---|---|---|---|
| **total lines** | 797 | **274** | ≤ 380 | **PASS — 106 lines under** |
| **CODE lines** | 249 | **100** | ≤ 120 | **PASS — 20 lines under** |
| **BODY (separator → EOF)** | 427 | **169** | ≤ 175 | **PASS — 6 lines under** |

Commands, one script run twice:

```
# before
$ git show 95a4c915:frontend/src/components/workflows/PhaseNodeCard.tsx | node classify.cjs --stdin
total=797  comment=518  code=249  blank=30
$ node classify.cjs precut.tsx --from 371          # the body
total=427  comment=226  code=187  blank=14

# after
$ node classify.cjs frontend/src/components/workflows/PhaseNodeCard.tsx
total=274  comment=161  code=100  blank=13
$ node classify.cjs frontend/src/components/workflows/PhaseNodeCard.tsx --from 106
total=169  comment=67  code=91  blank=11
```

**THE BODY RANGE, pinned as a RANGE and not as a number:**

```
$ grep -n "── The card ──" frontend/src/components/workflows/PhaseNodeCard.tsx
106:// ── The card ────────────────────────────────────────────────────────────────────
$ wc -l < frontend/src/components/workflows/PhaseNodeCard.tsx
274
```

⇒ **separator line 106 through EOF at line 274 = 169 lines.** (Before: separator line 371 through EOF at line 797 = 427.)

Door (b) is what closed the gap. The plan predicted the extraction ALONE would land the body at ~182 and miss by 12, and that door (b) — terse call sites plus relocating the `:379-392` preamble — would land it at ~162-167. Measured: **169**, three lines above the top of that predicted band and six under the target. Recorded as measured rather than rounded to the prediction.

### THE SUBTREE TOTAL — the tree got bigger while the file got smaller

| File | `wc -l` |
|---|---|
| `PhaseNodeCard.tsx` | 274 |
| `phaseNodeCardContract.ts` | 214 |
| `ownProperty.ts` | 86 |
| `NodeCornerMarks.tsx` | 272 |
| `NodeRunOverlay.tsx` | 319 |
| `NodeIconWell.tsx` | 167 |
| **SUBTREE TOTAL** | **1332** |

**1332 against 797 pre-cut = +67.1 %.** The growth is expected — five new files carry their own headers, imports, props types and wrappers — and it is stated in the card's own docblock so nobody later reads the sum as a regression.

⚠ **It is NOT "comparable to 188.1's +8 %", and the plan's `~940 L / about +18 %` estimate is measured FALSE.** See the inherited-claims table. The measurable reason is proportional: 188.1's two destinations came out of a 1593-line file, these five come out of a 797-line one whose prose was already 65 % of it.

### `git diff --numstat`

```
$ git diff --numstat 95a4c915..7ca0ec1b -- frontend/src/components/workflows/PhaseNodeCard.tsx
16	499	frontend/src/components/workflows/PhaseNodeCard.tsx
```

**499 deletions ≥ 440 ✓ · 16 insertions ≤ 40 ✓.** Cumulative across all three commits:

```
$ git diff --numstat 95a4c915..HEAD -- frontend/src/components/workflows/
25	0	NodeCornerMarks.tsx
21	0	NodeIconWell.tsx
18	0	NodeRunOverlay.tsx
7	5	PhaseNode.tsx
48	6	PhaseNodeCard.test.tsx
52	575	PhaseNodeCard.tsx
5	2	nodePresentation.ts
```

**The moved SOURCE is NOT byte-identical and this plan does not claim it is** — the deleted spans acquired component wrappers, props destructures and props types in 188.2-04/05. The RENDERED DOM is byte-identical, and that is the claim the baselines hold.

### FENCE N15 — the clip-utility count, 2 → 1

Entering (HEAD `95a4c915`), the one expected red 188.2-05 handed forward:

```
FAIL  src/components/workflows/PhaseNodeCard.test.tsx > 188-06 — no clipping anywhere in
      the node subtree (the mark overhangs) > the card's PROSE names it exactly once —
      the rule, and only the rule
AssertionError: expected 2 to be 1 // Object.is equality
 ❯ src/components/workflows/PhaseNodeCard.test.tsx:1979:25
Tests  1 failed | 123 passed (124)
```

Leaving, measured per file rather than only through the green suite:

```
PhaseNodeCard.tsx 0
phaseNodeCardContract.ts 0
ownProperty.ts 0
NodeCornerMarks.tsx 0
NodeRunOverlay.tsx 0
NodeIconWell.tsx 1
SUBTREE TOTAL = 1
```

**The pin was NOT edited.** The count returned to 1 because the card's copy was deleted, which is exactly what the fence was re-anchored to observe.

### V3 / T-188.2-23 — THE WR-04 FALSIFICATIONS STILL REACH THE MOVED CODE

Both sinks now live in different files while both falsifications drive through the CARD. Passing is not evidence of reaching, so each guard was temporarily removed from its NEW home and the corresponding falsification observed RED.

**Site 4 — `own(...)` removed from `NodeCornerMarks.tsx`:**

```
     × site 4 — a prototype-key verdict renders the DECLARED unknown mark, not an inherited member
FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard 188.1-04 — WR-04
     sites 4 and 5: the lookups are total > site 4 — a prototype-key verdict renders the
     DECLARED unknown mark, not an inherited member
AssertionError: expected '' to contain '?'
 ❯ src/components/workflows/PhaseNodeCard.test.tsx:2264:31
Tests  1 failed | 123 passed (124)
```

**Site 5 — `own(...)` removed from `NodeRunOverlay.tsx`:**

```
     × site 5 — a prototype-key reading paints a COLOUR TOKEN stroke, never a stringified function
FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard 188.1-04 — WR-04
     sites 4 and 5: the lookups are total > site 5 — a prototype-key reading paints a
     COLOUR TOKEN stroke, never a stringified function
AssertionError: expected 'object' to be 'string' // Object.is equality
Expected: "string"
Received: "object"
 ❯ src/components/workflows/PhaseNodeCard.test.tsx:2299:27
Tests  1 failed | 123 passed (124)
```

Each run showed **exactly one** failure — the guarded one. Both reverted from byte copies; `git diff --stat` on all three destination modules reports **0 files changed** afterwards.

### C-8 — THE RENDERED HALF, owed by 188.2-05 and DISCHARGED HERE

`<button type="button">planted</button>` inserted inside `NodeCornerMarks.tsx`'s verdict JSX, immediately above `{mark.glyph}`:

```
     × a card carrying a verdict mark still contains NO focusable control
     × a grounded card still contains NO focusable control (one tab stop per node)
     × keeps ONE TAB STOP PER NODE at every reading — the run line is a <p>
     × MAXIMAL_RUNNING reproduces the DOM CAPTURED from the unmoved tree, byte for byte
     × MAXIMAL_WAITING reproduces the DOM CAPTURED from the unmoved tree, byte for byte
      Tests  5 failed | 119 passed (124)

AssertionError: expected <button type="button"></button> to have a length of +0 but got 1
 ❯ src/components/workflows/PhaseNodeCard.test.tsx:780:67
```

**Three no-focusable-control walks went RED, plus both maximal HTML baselines.** 188.2-05 observed only N15 here and said so honestly; the walk could not go red then because the card still rendered its own inline copies and `NodeCornerMarks` was not mounted by anything. It is mounted now, and the walk reaches it.

⚠ Recorded precisely rather than generously: the **leaf-level walk at `:256`** (`contains NO focusable control of any kind`) did **not** fire, because `renderCard()`'s default props pass no `verdict` and no `grounded`, so `NodeCornerMarks` renders an empty fragment on that row. The three that fired are the rows that supply one or both. Reverted; `git diff --stat` clean.

### THE FIVE FENCED PARAGRAPHS ARE PROVABLY UNTOUCHED

Each pre-Task-2 span extracted from `git show`, then searched for in the post-edit file as ONE contiguous block:

```
VERBATIM: p1 (pre-cut :5-9)     found as ONE contiguous block at post-edit line 5,  5 lines, 404 chars
VERBATIM: p2 (pre-cut :11-16)   found as ONE contiguous block at post-edit line 11, 6 lines, 418 chars
VERBATIM: p3 (pre-cut :59-70)   found as ONE contiguous block at post-edit line 42, 12 lines, 963 chars
VERBATIM: p4 (pre-cut :97-101)  found as ONE contiguous block at post-edit line 62, 5 lines, 356 chars
VERBATIM: p5 (pre-cut :103-109) found as ONE contiguous block at post-edit line 68, 7 lines, 505 chars
```

And the `git diff -U0` form the criterion asks for:

```
p1 (:5-9):     0 of 5 lines appear as a `-` line — NONE, as required
p2 (:11-16):   0 of 6 lines appear as a `-` line — NONE, as required
p3 (:59-70):   0 of 12 lines appear as a `-` line — NONE, as required
p4 (:97-101):  0 of 5 lines appear as a `-` line — NONE, as required
p5 (:103-109): 1 of 7 lines appear as a `-` line  <<< " *"
```

⚠ **That single p5 "hit" is an artefact of the test, not a drift, and it is worth recording because it is the exact shape of a false positive.** The line is the bare comment separator ` *`, which occurs dozens of times in this docblock; a *set-membership* test cannot tell which one the diff removed. The contiguous-block search above already proves p5 is present verbatim, and the criterion's own stronger form settles it:

```
$ git show HEAD:frontend/.../PhaseNodeCard.tsx | sed -n '103,109p' > xss-head.txt
$ sed 's/\r$//' frontend/.../PhaseNodeCard.tsx | sed -n '68,74p'  > xss-work.txt
$ diff xss-head.txt xss-work.txt
<<<EMPTY DIFF — the XSS paragraph is BYTE-IDENTICAL (HEAD :103-109 == working :68-74)>>>
```

### THE DOCBLOCK REGION

```
$ awk '/^import /{print NR-1; exit}' frontend/src/components/workflows/PhaseNodeCard.tsx
75
```

**110 → 75 lines**, at the ≤ 75 bound exactly. Of those 75, **35 are the five fenced paragraphs** carried byte-for-byte.

Badge slot 1 is still pinned in the card:

```
$ grep -c "badge slot 1\|slot 1" frontend/src/components/workflows/PhaseNodeCard.tsx
1
39: * `technicalLine` is DECLINED and `stepNumber` declared-and-unread. Badge slot 1 stays
```

…and no badge is rendered in slot 1 (D-12) — the badge row is unchanged and the card still renders only what the caller passes.

### THE SIX RELOCATED PARAGRAPHS — one destination each

| # | Pre-cut span | Distinctive phrase | `grep -rlF` inside the six-file subtree |
|---|---|---|---|
| 1 | `:27-32` 185-09's corner seal | `WHAT 185 FILLED` | `NodeCornerMarks.tsx` ✓ |
| 2 | `:34-41` 188-06's ring / chip / mode (**SPLIT**) | `WHAT 188 FILLED` | `NodeRunOverlay.tsx` ✓ |
| 3 | `:43-52` 184-08's verdict mark | `WHAT 184-08 CHANGED` | `NodeCornerMarks.tsx` ✓ |
| 4 | `:54-57` the max-2 badge tuple | `max-2 tuple union` | `phaseNodeCardContract.ts` ✓ (see the decision below) |
| 5 | `:72-87` 137-B geometry (**SPLIT**) | `PER-STEP-TYPE COLOUR IS A TINT BEHIND THE ICON ONLY` + the 137-D rebuild history | `NodeIconWell.tsx` ✓ |
| 6 | `:89-95` the arc and the spin (**SPLIT**) | `MOTION KEYS OFF RUN STATE` | `NodeRunOverlay.tsx` ✓ |

Two matches fall OUTSIDE the six-file subtree and are pre-existing, unrelated sentences that predate this phase — recorded rather than glossed: `WorkflowCanvas.tsx:181` has its own *"MOTION KEYS OFF RUN STATE, NEVER OFF SELECTION — and 184 has no run state, so…"* paragraph, and `phaseVocabulary.ts:629` + `PhaseNodeCard.test.tsx:331` both refer to the card's max-2 tuple union. Neither is a duplicate created here.

**Three of the six were SPLIT rather than moved whole**, because each described BOTH something that left and something that stayed — the ring left while the run line, the border branch and the min-height stayed; the 3D mark left while `pt-[42px]` and `NODE_MIN_HEIGHT: 104` stayed; the arc's motion left while the card's colour policy is now stated in the two files that spend it.

### D-06 — NO SECOND NAME FOR THE SAME TYPE

```
$ grep -Ec "^export (type|interface) \{?(BadgeSlot|BadgeSlots|BadgeSlot2Tuple|NodeVerdictMark|NodeRunStatus|PhaseNodeCardProps)" PhaseNodeCard.tsx
0
$ grep -c "export \*\|export type {.*} from" PhaseNodeCard.tsx
0
$ grep -rn "phaseNodeCardContract" frontend/src | grep import
PhaseNode.tsx:94:       import type { BadgeSlot, BadgeSlots } from "@/components/workflows/phaseNodeCardContract"
PhaseNodeCard.test.tsx:51: import type { BadgeSlot, BadgeSlots } from "./phaseNodeCardContract"
PhaseNodeCard.tsx:123:  import type { PhaseNodeCardProps } from "@/components/workflows/phaseNodeCardContract"
```

Both importers re-pointed as SEPARATE statements with their own `188.2-06` comment, per the `FlowEdge.test.tsx:80-85` convention — so the change reads as added lines and no shipped line had to be edited to accommodate it (`PhaseNode.tsx` 7/5, `PhaseNodeCard.test.tsx` 7/1 at the cut commit).

### THE ONE-DIRECTION FENCE, proven non-vacuous TWICE

**(a) the import half — `NodeIconWell` renamed to `NodeIconWellRenamed` at its import and its call site:**

```
     × the cut has ONE direction — the card IMPORTS the three modules and declares none
FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard 188.2 — the extracted
     modules cannot import back (SC#3) > the cut has ONE direction — the card IMPORTS the
     three modules and declares none
AssertionError: expected '/**\n * Phase 184-03 Task 2 (D-184-06…' to match
                /import \{ NodeIconWell \} from ["']@\/components\/workflows\/NodeIconWell["']/
Tests  1 failed | 123 passed (124)
```

**(b) the no-shim half — a real `export type { BadgeSlot, BadgeSlots } from "…/phaseNodeCardContract"` planted in the card:**

```
FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard 188.2 — the extracted
     modules cannot import back (SC#3) > the cut has ONE direction — the card IMPORTS the
     three modules and declares none
AssertionError: expected '/**\n * Phase 184-03 Task 2 (D-184-06…' not to match
                /export\s+(type\s+)?\{[^}]*\}\s*from/
```

Both reverted from a byte copy; `git diff --stat -- PhaseNodeCard.tsx` reports 0 files changed against HEAD afterwards.

### The vacuity guard, and the resolved-source floor

```
$ grep -c "if (path in CARD_MODULES)" frontend/src/components/workflows/PhaseNodeCard.test.tsx
0
```

None remained — 188.2-05 removed both. The **unconditional** non-empty assertion covering all five destinations is the subtree fence's loop (`expect(Object.keys(CARD_MODULES)).toContain(path)` then `expect(CARD_MODULES[path].length).toBeGreaterThan(0)`), and the cycle-fence loop asserts membership for all five before its two negatives. No path is guarded and no path is skipped.

### Gates

| Gate | Command | Entering | Leaving |
|---|---|---|---|
| Card suite | `npx vitest run …/PhaseNodeCard.test.tsx` | 123 passed, **1 failed (N15)** | **124 passed (124)**, 0 failed |
| Adapter + canvas | `npx vitest run PhaseNode.test.tsx WorkflowCanvas.test.tsx WorkflowCanvas.editing.test.tsx` | — | **139 passed (139)**, 0 failed |
| Type | `npx tsc --noEmit -p tsconfig.app.json \| grep -c "error TS"` | 33 | **33** (and **0** in `src/components/workflows/`) |
| Lint | `npx eslint src/components/workflows/` | 5 errors, 0 warnings | **✖ 5 problems (5 errors, 0 warnings)** |
| Count gate | `node scripts/vitest-count-gate.cjs` | — | **`count gate OK` — 45/45 pinned files present, no per-file decrease, failed 0**, exit 0. `PhaseNodeCard.test.tsx 107 124 +17`; `WorkflowCanvas.editing.test.tsx 58 61 +3`; `total 2482 2502 +20`. No `[count-decrease]`, no `[missing-file]` |
| Pin file | `git diff --stat -- scripts/vitest-count-gate.cjs` | — | **0 files changed** |
| T-188.2-SC | `git diff --stat -- frontend/package.json frontend/package-lock.json backend/ supabase/migrations/ scripts/` | — | **0 files changed** — zero packages installed |
| Scope | `git status --porcelain -- frontend/` | — | only the seven files this plan declares, at each commit |
| Deletions | `git diff --diff-filter=D --name-only 95a4c915..HEAD` | — | **empty** — no file deleted |

**On the count gate's `failed` line.** It read **0**. Per the orchestrator's standing instruction the full-suite `failed N` line is not this plan's acceptance instrument (measured flake range 1 → 50 today, all `Axe is already running` or 5 s timeouts, SEED-056); the four suites this plan touches were run in isolation and are the instrument, and every one is green.

**On the scratch-file criterion.** The classifier and every temp file were written to the session scratchpad outside the repository. `git status --porcelain` DOES list pre-existing untracked `.py`/`.json` files under `scripts/_uat111/`, `scripts/pm-pack/out/` and `backend/scripts/` — those predate this plan (they are in the tree snapshot at plan start) and this plan added none. The measurable form of the criterion: `git status --porcelain -- frontend/` shows no untracked file at any commit.

## Files Created/Modified

- **`PhaseNodeCard.tsx`** — 797 → **274 L**. Seven ranges deleted; three terse call sites added at byte-identical JSX positions; the import block re-cut (`VERDICT_MARK`, `VerdictMarkKind`, `GOVERNANCE_SEAL_LABEL`, `ringDash`, `ringSpecFor`, `ReactNode`, `ChipTone`, `CanvasReading` and `EmitFailure` all removed — `runReadingBorder`, `runReadingLabel`, `StatusChip`, `CANVAS_LAYOUT` and `cn` all still used and all kept); docblock rewritten to 75 L; preamble comment reduced to the two slots the card still owns.
- **`PhaseNode.tsx`** — the type import re-pointed as its own statement (+7/−5).
- **`PhaseNodeCard.test.tsx`** — the type import re-pointed as its own statement; the one-direction fence completed with four import assertions, the D-06 no-shim half and three inline positive controls. **No `it(` deleted, no `describe(` re-described, no bound loosened, no baseline touched.** One `it(` TITLE was widened (`the card declares none of the three components` → `the card IMPORTS the three modules and declares none`) because the old title had become an understatement of what the block asserts; the test count is unchanged at 124.
- **`nodePresentation.ts`** — the `VerdictMarkKind` alias sentence corrected in the same commit that made it false; the qualified name is now `phaseNodeCardContract.NodeVerdictMark`. Nothing else in the file touched, so its four `ICON_TINT` / `DEFAULT_TINT` / `renderPhaseMark` / `GROUNDING_TONE` fences are unaffected.
- **`NodeCornerMarks.tsx`** (247 → 272 L), **`NodeRunOverlay.tsx`** (301 → 319 L), **`NodeIconWell.tsx`** (146 → 167 L) — each received the relocated paragraph(s) describing its own code, plus a `⚠` note recording where each came from and, where a paragraph was SPLIT, what stayed behind and why. All three additions are **insertions only** (25/0, 18/0, 21/0) — not one existing line in any of the three was edited.

## Decisions Made

1. **The `:54-57` badge-tuple paragraph was DELETED, not relocated.** Its destination already holds the argument: `phaseNodeCardContract.ts:97` carries *"A max-2 tuple union. Zero, one or two badges are representable; a third is a TYPECHECK ERROR…"*, moved verbatim by 188.2-04 as part of the `BadgeSlots` declaration. The card's version was a *summary* of that same paragraph. Relocating it would have shipped a second copy of a decision that already has one home — the exact drift these headers exist to prevent, and the one thing `phaseNodeCardContract.ts` was created to stop. The card keeps a one-clause pointer (`including the max-2 badge tuple`) in its where-it-went list. `phaseNodeCardContract.ts` was therefore not edited, which also keeps this plan inside its declared `files_modified`.
2. **Task 1 did not touch the preamble comment; Task 2 did.** The plan puts door (b) in Task 2, and keeping the split makes the two commits separately readable: `7ca0ec1b` is 16/499 and reads as a pure MOVE, `a4e60a04` is 100/76 and is entirely prose.
3. **The no-shim half is anchored on the export FORM, not on a type name.** `WorkflowCanvas.test.tsx` can assert `not.toContain("EDIT_AFFORDANCE")` because 188.1 extracted exactly one constant. This contract has six type names and 189 will add more, so a name-list would rot into a fence that permits the next shim. `not.toMatch(/export\s+\*\s+from/)` and `not.toMatch(/export\s+(type\s+)?\{[^}]*\}\s*from/)` catch every form, with three inline positive controls (`export *`, `export type {…} from`, `export { type … } from`).
4. **The cut was assembled by script from explicit 1-based spans, behind a 27-entry line-anchor assertion table** that throws on drift before a byte is written. It caught its own bug immediately: the first assembly kept `:410-434` (the verdict derivation) because a span boundary was wrong. The file was restored with `git checkout --` and the span corrected — which is precisely what the anchor table plus a read-back is for.
5. **The three call sites are one to two lines each with no pointer prose** (door a). The explanation lives in each destination module's docblock, which is where D-01 puts it, and a single three-line import comment covers why the positions are byte-identical.
6. **The `// ── The status ring's constants ──` separator was NOT replaced with a new one for `RUN_MODE_NODE_MIN_HEIGHT`.** That constant now sits directly under the imports with its own docblock. A replacement separator would have cost a line against a target the plan explicitly forbids rounding.

## Deviations from Plan

### Auto-fixed / auto-added

**1. [Rule 2 — Missing critical functionality] C-8's RENDERED half was discharged here, and it is recorded as an owed item met rather than as a new test**

- **Found during:** Task 1 verification.
- **Issue:** 188.2-05 reported honestly that its `<button>` plant could not turn the no-focusable-control walk red, because nothing mounted `NodeCornerMarks`. It named the re-plant as owed to this plan. The plan's acceptance criteria do not mention it.
- **Fix:** Re-planted after the call sites existed; **five** failures observed including three no-focusable-control walks. Full text above, including the honest note that the leaf-level walk at `:256` does not fire because its default props render an empty fragment.
- **Files:** none committed — the plant was reverted.

**2. [Rule 1 — Bug] The `:54-57` paragraph would have been duplicated, not relocated** — see Decision 1. Following the plan's `<interfaces>` literally would have created a second copy of an argument that already has exactly one home.

**Total deviations:** 2 — one Rule 2, one Rule 1. None architectural. No package installed, no baseline re-captured, no `it(` deleted, no assertion weakened.

## Inherited claims re-measured (per the standing "don't inherit unmeasured claims" rule)

Twenty-three claims were re-derived before being relied on. **Sixteen held exactly. Seven are corrected.**

| # | Claim | Verdict |
|---|---|---|
| 1 | `PhaseNodeCard.tsx` is still **797 L** on entry (Plans 04/05 were additive) | **TRUE**, `wc -l` = 797 at `95a4c915`. |
| 2 | The eleven source ranges: `:139-285`, `:289-310`, `:287`, `:312-355`, `:410-434`, `:440-443`, `:567-599`, `:601-656`, `:658-739`, `:741-762`, `:764-794` | **ALL TRUE**, verified by a 27-entry `startsWith` anchor table that throws on drift. |
| 3 | WHAT STAYS: `:357-369`, `:436-439`, `:444`, `:446-565` | **ALL TRUE**, same table. |
| 4 | Body is `:371-797` = **427 L**, separator at `:371` | **TRUE**, and the classifier measured `total=427` on that exact range. |
| 5 | `code = 249`; the sanity figures `797/518/249/30`; the region sums `110+28+147+1+84+427=797` | **ALL TRUE** — the classifier reproduced every one on its first run. |
| 6 | `PhaseNode.tsx:88-92` is the four-line braced import of the card + two types | **TRUE**, exactly at `:88-92`. |
| 7 | `PhaseNodeCard.test.tsx:45` is the single-line import of the card + two types | **TRUE**, exactly at `:45`. |
| 8 | `nodePresentation.ts:145-146` carries the alias sentence that goes false | **FALSE — it is at `:144-145`.** Off by one, and 188.2-04 had ALREADY measured and recorded this correction; the plan re-typed the stale figure anyway. |
| 9 | `WorkflowCanvas.test.tsx:680-690` is the one-direction fence to copy | **TRUE**, exactly — `it(` at `:680`, the no-shim assertion at `:689`, closing `})` at `:690`. |
| 10 | The subtree total will be **~940 L against 797, about +18 %, comparable to 188.1's +8 %** | **FALSE, and by a wide margin — measured 1332 L, +67.1 %.** The five new files alone are 1058 L. It is roughly EIGHT TIMES 188.1's relative growth, and the card's docblock now says so with the measured numbers rather than the estimate. This is the single most consequential correction in this plan: a phase whose headline is "the file shrank" would have shipped a docblock understating the tree's growth by 49 percentage points. |
| 11 | `grep -rn 'from "@/components/workflows/PhaseNodeCard"\|from "./PhaseNodeCard"' frontend/src` returns **exactly 2 lines** | **FALSE — measured 5.** Three are the ESM-cycle fence's own POSITIVE-CONTROL string literals inside `PhaseNodeCard.test.tsx` (`:599`, `:600`, `:603`), which 188.2-01 authored deliberately. The property the criterion is about — **exactly two real import statements**, in `PhaseNode.tsx:88` and `PhaseNodeCard.test.tsx:45` — holds exactly. |
| 12 | The five must-not-touch paragraphs are at `:5-9`, `:12-16`, `:59-70`, `:97-101`, `:103-109` | **Near-miss on one: the second paragraph starts at `:11`, not `:12`.** `:11` is *"The graph-library boundary lives one file away…"*. The whole `:11-16` block was preserved byte-identically rather than the plan's `:12-16`, which would have orphaned its opening clause. The other four are exact. |
| 13 | The card's `:33-35` says *"a docblock that names a slot the component no longer holds is the same defect as a false one"* | **Near-miss, twice.** The sentence is at **`:34-35`**, and it reads *"a docblock that still names a slot the component now FILLS is the same defect as a false one"* — about a slot that got filled, not one that moved away. The plan's paraphrase extends it correctly (a docblock narrating a MOVED block is the same defect again), but the extension is this plan's argument and not a quotation, and it is written as such in the new docblock. |
| 14 | Task 2's `read_first` fence regions `:135-147`, `:200-216`, `:334-351`, `:390-435` | **ALL FOUR FALSE.** Measured today: the no-provider `describe(` is at `:189`, the ONE-TAB-STOP `describe(` at `:255-265`, the scope-fences `describe(` at `:472-558`, N10 at `:505`. The plan's four spans land on a render fixture, an anchors assertion, a `@ts-expect-error` comment and a tint assertion respectively. Same mechanism 188.2-05 recorded: the test file grew 2063 → 3687 and the figures were measured against an earlier tree. |
| 15 | The two WR-04 falsifications are near `PhaseNodeCard.test.tsx:2007-2050` | **FALSE — they are at `:2287` (site 4) and `:2306` (site 5)**, `describe(` at `:2286`. `:2007-2050` is the ring-spin block. Third time this same anchor has rotted in this phase. |
| 16 | The plan's arithmetic: extraction ALONE lands the body at ~182 (missing ~170 by 12); door (b) lands it at ~162-167 | **Near-miss, and the direction is right.** Measured **169** — three lines above the top of the predicted band, six under the 175 target. The prediction's mechanism was correct; only its precision was optimistic. |
| 17 | Deletions ≥ 440 and insertions ≤ 40 on the card | **TRUE** — 499 / 16. |
| 18 | `tsc` 33 with 0 in `src/components/workflows/`; `eslint` 5 errors / 0 warnings | **TRUE**, entering and leaving. |
| 19 | Fence N15 reads 2 entering and must return to 1 | **TRUE**, both halves observed. |
| 20 | `grep -c "if (path in CARD_MODULES)"` returns 0 | **TRUE** — 188.2-05 already removed both guards. |
| 21 | The three destination props signatures (`{ verdict, grounded }`, `{ reading }`, `{ icon, tint }`) | **TRUE**, all three read off the real exported interfaces. |
| 22 | `:54-57`'s destination is `phaseNodeCardContract` | **TRUE as a destination, but the move was already done** — see Decision 1. |
| 23 | Do not edit `scripts/vitest-count-gate.cjs`; the pin move is Plan 07's | **Honoured** — `git diff --stat` reports 0 files changed. |

## Threat model disposition

| Threat ID | Disposition | Evidence |
|---|---|---|
| T-188.2-23 (WR-04 sinks 4 and 5, split across two modules) | **mitigated — OBSERVED, not asserted** | Both falsifications pass in the 124/124 run AND both were driven RED by removing each guard from its NEW home, one at a time, each run showing exactly one failure. Full text above. ASVS V5 |
| T-188.2-24 (the XSS clause and fence N10) | **mitigated** | The `:103-109` paragraph proved byte-identical by an EMPTY `diff` against `git show` output; fence N10 reads the six-file subtree and is green. ASVS V5 |
| T-188.2-25 (the fifteen re-scoped fences at the moment the code leaves) | **mitigated** | The full card suite is green after the cut and **N15 moved 2 → 1**, measured per file — the clip fence is live rather than assumed, and it is the one fence a naive cut would have taken to 0. |
| T-188.2-26 (a second name for the same type, D-06) | **mitigated** | Two greps return 0; the one-direction fence's no-shim half was driven RED against a REAL planted `export type {…} from`; both importers re-pointed as separate auditable statements. |
| T-188.2-27 (D-14 — the card acquiring run-state derivation during the cut) | **mitigated** | Only `arc` left. `reading`, `runBorder` and `minHeight` stay as the card's own already-derived values and no new derivation was added; the card no longer imports `@/lib/phaseState` at all, and `NodeRunOverlay` takes its `CanvasReading` as a TYPE only. |
| T-188.2-28 (a rounded D-03 figure) | **mitigated** | The classifier is named, lives in the scratchpad, and reproduced `797/518/249/30` on the pre-cut file BEFORE any after-number was used. All three targets were beaten, and the margins (106 / 20 / 6 lines) are stated rather than the numbers being described as "about". |
| T-188.2-SC (package installs) | **mitigated** | `git diff --stat -- frontend/package.json frontend/package-lock.json backend/ supabase/migrations/ scripts/` → **0 files changed**. Zero packages installed. |

## Issues Encountered

**None blocking.**

Two worth carrying forward rather than fixing:

1. **A set-membership diff test cannot prove a paragraph untouched.** The `git diff -U0` form the plan specifies reports a false positive on any paragraph containing a bare ` *` separator, because that line occurs dozens of times in the same docblock. The contiguous-block search and the span `diff` are the forms that actually answer the question. Recorded because the same criterion is likely to be re-used.
2. **The count gate's `failed` line read 0 in every run here** — the machine flake the orchestrator warned about (1 → 50 today, `Axe is already running` / 5 s timeouts, SEED-056) did not recur, and 188.2-05's `failed 1` was its own expected N15 red, now resolved.

## User Setup Required

None.

## Next Phase Readiness

- **THE CUT IS DONE AND THE PHASE'S FIRST CONTRACT HELD.** `PhaseNodeCard.tsx` is 274 L with a 169-line body and 100 lines of code; the rendered DOM is byte-identical to the tree as it shipped, machine-checked by three whole-`innerHTML` comparisons and three geometry matrices captured before the move and re-captured never.
- **Owed to Plan 07 (the close), recorded so nothing is lost between waves:**
  - **The count-gate pin move** — `PhaseNodeCard.test.tsx` 107 → **124**, `WorkflowCanvas.editing.test.tsx` 58 → **61**, total 2482 → **2502**. Unchanged by this plan, which adds no test. The gate prints `+20` and passes.
  - **The CLAUDE.md hot-file ledger row** flips from *G-5 fires — extraction due* to *satisfied (188.2)*. The re-derivable figures it should carry: **797 → 274 L** (−65.6 %), **body 427 → 169**, **CODE 249 → 100**, **subtree 1332 L (+67.1 % over 797)**, commands `wc -l`, `grep -n "── The card ──"` and the named classifier.
  - **Three stale doc pointers** — this plan corrected the fourth (`nodePresentation.ts:144-145`) in the commit that made it false.
  - **Board B's driven `elementFromPoint` row** for `BUG-260806-01`, still owed from 188.2-02.
  - **⚠ Do NOT re-state the subtree growth as `~940 L / +18 %` or as "comparable to 188.1's +8 %".** Both figures are measured FALSE — see inherited claim 10. The true figures are 1332 L and +67.1 %.
- **Owed to Phase 189:** the seams are open. Badge slot 1 is still EMPTY and reserved; the third-badge typecheck invariant and the no-focusable-control invariant are both still enforced; the capability-derived subtitle and the 7th `phase_type`'s face land in a 169-line body rather than a 427-line one, and the contract they extend is now its own file with exactly one name per type.
- **No blockers.**

## Self-Check: PASSED

- `FOUND: .planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-06-SUMMARY.md`
- `FOUND: frontend/src/components/workflows/PhaseNodeCard.tsx` (274 L)
- `FOUND: frontend/src/components/workflows/PhaseNode.tsx` · `PhaseNodeCard.test.tsx` · `nodePresentation.ts`
- `FOUND: frontend/src/components/workflows/NodeCornerMarks.tsx` (272 L) · `NodeRunOverlay.tsx` (319 L) · `NodeIconWell.tsx` (167 L)
- `FOUND: 7ca0ec1b` · `FOUND: a4e60a04` · `FOUND: 579504de`
- `UNCHANGED (as required): scripts/vitest-count-gate.cjs · frontend/package.json · frontend/package-lock.json · phaseNodeCardContract.ts · ownProperty.ts`
- `NO FILE DELETED: git diff --diff-filter=D --name-only 95a4c915..HEAD → empty`

---
*Phase: 188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p*
*Completed: 2026-08-07*
