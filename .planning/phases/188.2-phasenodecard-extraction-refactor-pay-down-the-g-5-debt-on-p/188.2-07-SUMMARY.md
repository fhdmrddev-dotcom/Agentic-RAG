---
phase: 188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p
plan: 07
status: PARTIAL — CHECKPOINT-BLOCKED (Task 1 of 3 complete; Tasks 2 and 3 await the operator)
subsystem: documentation
tags: [g-5-ledger, doc-truth, count-gate-pin, deferred-register, uat-board, checkpoint]

# Dependency graph
requires:
  - plan: 188.2-02
    provides: "the `AFFORDANCE_Z` = 1002 fix and its three-part unit guard — the UNIT half of BUG-260806-01, whose DRIVEN half this plan owes and could not obtain"
  - plan: 188.2-06
    provides: "THE CUT and its three measured D-03 numbers (797→274 / 249→100 / 427→169), the 1332 L subtree total, and the explicit correction of the plan's FALSE ~940 L / +18 % estimate"
provides:
  - "the `PhaseNodeCard.tsx` hot-file ledger row flipped to *satisfied (188.2 — 2026-08-07)* with all three D-03 numbers as before → after, the classifier re-validated in THIS plan, the five destination modules and the commands to re-derive"
  - "four corrected documentation pointers (three named by the plan + `GOVERNANCE_SEAL_LABEL`), each verified by a grep against the file that now holds what it names"
  - "one upward count-gate pin move on two files, read from the gate's own `actual` column across two agreeing runs, `TARGETS` untouched, no pin lowered"
  - "`188.2-DEFERRED.md` — six deferrals, each with an observable re-open trigger"
  - "`188.2-UAT.md` — both boards authored with disjoint evidence columns, SC#10's non-application recorded as a decision, and all five rows explicitly NOT DRIVEN"
affects: [189]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "re-derive a prior plan's classifier from its OUTPUT rather than inheriting the tool: reproduce the known validation figures first, and treat a mismatch as a specification gap to close (the JSX `{/* … */}` rule was recovered exactly this way)"
    - "prove a served dev bundle is the post-change tree by GETting the Vite module URLs directly — stronger than asserting a hard reload, and explicitly NOT evidence about layout, stacking or reachability"

key-files:
  created:
    - .planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-DEFERRED.md
    - .planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-UAT.md
  modified:
    - CLAUDE.md
    - scripts/vitest-count-gate.cjs
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md
    - .claude/skills/sketch-findings-agentic-rag/references/icon-convention.md

key-decisions:
  - "`188.2-UAT.md` was AUTHORED but not driven, and every row says so. Creating the board does not depend on the checkpoint; approving a row does. The frontmatter reads `status: AWAITING-OPERATOR / rows_passed: 0` so the file can never be mistaken for a completed board — the specific defect Phase 188 shipped."
  - "`BUG-260806-01` stays `status: folded`. RESEARCH rates the fix's sufficiency MEDIUM until B1 is driven, and closing it on the stacking analysis is exactly the substitution the report itself warns against."
  - "A fourth stale pointer was corrected beyond the plan's three: `GOVERNANCE_SEAL_LABEL` was cited at a root-level `definitionOps.ts:388` that does not exist. It lives in the same table cell as pointer 1 and was measured while verifying it."

patterns-established:
  - "A phase finding about tooling belongs in the DEFERRED register with an observable trigger AND in the tool's own source comment, so the next reader of the gate meets it without opening a planning file."

requirements-completed: []  # EMPTY BY DESIGN — ROADMAP §Phase 188.2: "Requirements: none — refactor phase."

# Metrics
duration: 48min
completed: null  # NOT COMPLETE — blocked at Task 2
---

# Phase 188.2 Plan 07: Close the phase — PARTIAL Summary

**Task 1 is complete and verified: the `PhaseNodeCard.tsx` G-5 ledger row now reads *satisfied
(188.2)* carrying three independently re-derived numbers, four stale documentation pointers resolve
to the files that now hold what they name, and one upward pin move landed from two agreeing gate runs
with `TARGETS` untouched. Tasks 2 and 3 — the two UAT boards — are BLOCKING OPERATOR CHECKPOINTS and
have NOT been driven: this session has no browser-driving tool, and no unit test, served-bundle read
or green gate was substituted for a hit test. `BUG-260806-01` therefore stays `folded`.**

## ⛔ PLAN STATUS: 1 of 3 tasks complete

| Task | Type | Status |
|---|---|---|
| 1. Flip the ledger, correct the pointers, move the pin, register the deferrals | `auto` | ✅ **COMPLETE** — committed `615b2a06` |
| 2. Drive Board A (prove UNCHANGED, 3 rows) | `checkpoint:human-verify` (blocking) | ⛔ **AWAITING OPERATOR** — board authored `360399b2`, 3 rows NOT DRIVEN |
| 3. Drive Board B (prove FIXED, 2 rows) | `checkpoint:human-verify` (blocking) | ⛔ **AWAITING OPERATOR** — board authored `360399b2`, 2 rows NOT DRIVEN |

**Why the checkpoint was returned rather than resolved.** The executor has `Read`, `Write`, `Edit`,
`Bash`, `Grep` and `Glob` — and no Chrome MCP, no `evaluate_script`, no `elementFromPoint`. Board B's
single claim is that a **selected** card no longer occludes its own `✕`, and the plan, the research
and the bug report all state the same thing three ways: jsdom applies no CSS, computes no stacking
contexts, and `.click()` bypasses hit-testing, so **no instrument in this repository can see that
defect class**. Substituting one would be the exact substitution that let the bug ship from `184-12`
until an operator found it by hand.

## Performance

- **Duration:** ~48 min (Task 1 + board authoring)
- **Started:** 2026-08-07T04:00:00Z
- **Stopped at checkpoint:** 2026-08-07T04:48:00Z
- **Tasks:** 1 of 3
- **Files modified:** 4 · **Files created:** 2

## Task Commits

1. **Task 1: flip the ledger row, correct four pointers, move the pin once, register the deferrals** — `615b2a06` (docs)
2. **Board scaffold (does not depend on the checkpoint): `188.2-UAT.md`, five rows, NONE DRIVEN** — `360399b2` (docs)

---

## Recorded measurements (verbatim, as `<output>` requires)

### 1. BOTH COUNT-GATE RUNS, AND THE PINS WRITTEN FROM THEM

The two runs **agreed on every per-file `actual` and on the total** BEFORE either pin was written:

```
RUN 1 (2026-08-07, report vitest-count-gate-66708-1786060797902.json)
  PhaseNodeCard.test.tsx                      107     124     +17
  WorkflowCanvas.editing.test.tsx              58      61      +3
  total                                      2482    2502     +20
  total 2502  ·  failed 1  ·  pinned total 2482
  RESULT: COUNT GATE VIOLATED (1 reason(s))  [failing-tests]

RUN 2 (2026-08-07, report vitest-count-gate-64476-1786060924857.json)
  PhaseNodeCard.test.tsx                      107     124     +17
  WorkflowCanvas.editing.test.tsx              58      61      +3
  total                                      2482    2502     +20
  total 2502  ·  failed 0  ·  pinned total 2482
```

**Pins written from that agreed `actual` column** — never hand-counted, never taken from a planning
document (the house rule in the script's own header):

```
scripts/vitest-count-gate.cjs:302   "PhaseNodeCard.test.tsx": 124,          (was 107, +17)
scripts/vitest-count-gate.cjs:427   "WorkflowCanvas.editing.test.tsx": 61,  (was  58,  +3)
BASELINE_TOTAL trailing note        2482 → 2502
```

**EXTENSIONS ONLY.** The only two removed pin lines in `git diff` are the two being raised:

```
$ git diff -U0 scripts/vitest-count-gate.cjs | grep -E '^-\s+"'
-  "PhaseNodeCard.test.tsx": 107,
-  "WorkflowCanvas.editing.test.tsx": 58,
```

Nothing in this phase was a deletion — Plans 01, 03, 05 and 06 only added tests, and no `it()` was
moved out of `PhaseNodeCard.test.tsx`. `TARGETS` is untouched (`git diff` contains no `+`/`-` line
inside the array; the one `TARGETS` string in the diff output is a context line).

**Verification run AFTER the pins were written — pinned == actual for both edited entries:**

```
  PhaseNodeCard.test.tsx                      124     124       0
  WorkflowCanvas.editing.test.tsx              61      61       0
  total                                      2502    2502       0
  total 2502  ·  failed 0  ·  pinned total 2502
count gate OK — 45/45 pinned files present, no per-file decrease, 0 failing.
```

`count gate OK` · **failed 0** · no `[count-decrease]` · no `[missing-file]`.

### 2. ⚠ THE `failed 1` IN RUN 1 — NAMED, NOT WAVED THROUGH

Per the orchestrator's standing instruction, the failing file was identified from the gate's own
JSON report rather than assumed, and checked against this phase's file set:

```
FILE: frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
  - WorkflowBuilderPage 184-11 — with the flag OFF the panel receives NO rails key (D-14)
    POSITIVE CONTROL — with the flag ON the very same read finds the key
    AssertionError: expected 0 to be greater than 0
numFailedTests 1
```

**It is NOT a 188.2 file.** It appears in no `files_modified` of any of the seven plans in this
phase. Driven in isolation immediately afterwards:

```
$ npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx
 Test Files  1 passed (1)
      Tests  128 passed (128)
```

**Stated explicitly, as required:** the one failure observed while gating this plan is in a file this
phase never touched, and it passes 128/128 alone. It matches the machine flake the orchestrator
recorded (`failed` varying 1 → 21 → 50 → 10 → 0 → 0 across eight runs today with per-file counts
intact; all `Axe is already running` or 5 s timeouts; pre-existing rot, SEED-056, at a higher count
than that seed records). It is recorded as a **phase finding** in `188.2-DEFERRED.md`
**D-188.2-DEF-01** with an observable re-open trigger, and in the gate script's own `BASELINE_TOTAL`
comment so the next reader of the tool meets it without opening a planning file.

### 3. THE FLIPPED LEDGER ROW — its text, and the numbers re-derived rather than inherited

`grep -n "satisfied (188.2" CLAUDE.md` returns exactly **1** match — the flipped row — and
`grep -n "G-5 fires" CLAUDE.md` now returns exactly **two** rows, both legitimately still firing:

```
191:| `backend/app/api/threads.py` … | G-5 fires — extraction due |
194:| `backend/app/services/anthropic_service.py` … | G-5 fires — adapter pattern audit due |
```

`PhaseNodeCard.tsx` is no longer among them.

**Column 2 (phases touched), measured:**

> 184 (184-03, 184-08) / 185 (185-01 …, 185-08 …, 185-09 …) / 188 (188-06 …) / 188.1 (188.1-04 …) /
> **188.2 (188.2-06 THE CUT, 16 insertions / 499 deletions)** — **8 plans across 5 phases**,
> **797 → 274 lines**. ⚠ Two corrections on measurement (2026-08-07, `git log --oneline -- <file>` +
> `wc -l`): the cell read *7 plans across 4 phases, 797 lines* and did not yet name 188.2; and
> `git log` reports **10 commits** against those 8 plans, because 188-06 and 188.2-06 each landed
> twice. Both figures are defensible — the PLAN count is what this cell quotes, and the commit count
> is how it was derived.

The derivation, run this session:

```
$ git log --oneline -- frontend/src/components/workflows/PhaseNodeCard.tsx
a4e60a04 docs(188.2-06)  7ca0ec1b refactor(188.2-06)  b018b20b fix(188.1-04)
60cddc78 test(188-06)    c635e39f feat(188-06)        6bdc658a feat(185-09)
30cb77f9 feat(185-08)    c31c3811 refactor(185-01)    296350b4 feat(184-08)
b7603244 refactor(184-03)
⇒ 10 commits · 8 plans · 5 phases
```

**Column 3 (the flipped status), verbatim:**

> **satisfied (188.2 — 2026-08-07).** The debt named at the 188.1-05 checkpoint was paid by a
> dedicated refactor phase, and it is recorded here with re-derivable figures rather than an
> assertion. **All three D-03 numbers, before → after: total `797 → 274` (−65.6 %) · CODE
> `249 → 100` · BODY (separator → EOF) `427 → 169`.** They come from a named line classifier (blank /
> comment / code, counting `{/* … */}` JSX comment blocks as comment) which was VALIDATED against the
> pre-cut file's known `797/518/249/30` before any after-number was trusted — re-run independently in
> `188.2-07` and reproduced exactly. Re-derive with: `git log --oneline -- <file>`; `wc -l <file>`;
> `grep -n "── The card ──" <file>` → **106**, so the body is lines 106–274; and the classifier over
> `git show 95a4c915:<file>` for the before column. **Where the code went — five sibling modules:**
> `phaseNodeCardContract.ts` (214 L …), `ownProperty.ts` (86 L …), `NodeCornerMarks.tsx` (272 L …),
> `NodeRunOverlay.tsx` (319 L …) and `NodeIconWell.tsx` (167 L …). **⚠ The SUBTREE GREW: 797 → 1332 L,
> +67.1 % — roughly EIGHT times 188.1's +8 %, and it is stated rather than smoothed** … **PROVED, not
> asserted:** the rendered DOM is byte-identical … all seventeen negative source fences were
> re-scoped … three driven RED against real plants … **a third badge is a typecheck error** — asserted
> NOWHERE before 188.2-01, now guarded by an `@ts-expect-error` control observed RED at 34 and back at
> 33; **no focusable control may live inside the card** …; and **badge slot 1 is still EMPTY and
> reserved for 189** (D-12). Deferred with its own trigger in `188.2-DEFERRED.md` …

### 4. THE CLASSIFIER WAS RE-DERIVED, NOT INHERITED — and the re-derivation found its own spec gap

The plan requires the row to name the classifier. Rather than copy 188.2-06's three numbers, the
classifier was **rebuilt from its published output** in the session scratchpad and validated against
the pre-cut file's known figures. The **first** attempt (blank / `/* */` and `//` comments / code)
reproduced the totals but **not** the composition:

```
attempt 1:  total=797  comment=356  code=392  blank=49     ✗ (expected 518/249/30)
```

The 143-line code and 19-line blank discrepancies were **entirely inside the render body**, which
identified the missing rule exactly: **JSX `{/* … */}` comment blocks count as comment, and blank
lines inside any block comment count as comment.** With that rule added:

```
$ git show 95a4c915:.../PhaseNodeCard.tsx | node classify.cjs --stdin
total=797  comment=518  code=249  blank=30            ✓ EXACT
```

Only then were the after-numbers taken. **All three D-03 numbers reproduce 188.2-06 exactly:**

| Number | Before (`95a4c915`) | After (HEAD) | Command |
|---|---|---|---|
| **total lines** | **797** | **274** | `classify.cjs <file>` |
| **CODE lines** | **249** | **100** | same |
| **BODY (separator → EOF)** | **427** (`:371-797`) | **169** (`:106-274`) | `classify.cjs <file> --from 106` |

```
$ node classify.cjs .../PhaseNodeCard.tsx              → total=274  comment=161  code=100  blank=13
$ node classify.cjs .../PhaseNodeCard.tsx --from 106   → total=169  comment=67   code=91   blank=11
$ grep -n "── The card ──" .../PhaseNodeCard.tsx       → 106:// ── The card ──…
$ wc -l < .../PhaseNodeCard.tsx                        → 274
```

**Subtree total, re-measured (NOT the plan's FALSE `~940 L / +18 %`):**

```
$ wc -l PhaseNodeCard.tsx phaseNodeCardContract.ts ownProperty.ts \
        NodeCornerMarks.tsx NodeRunOverlay.tsx NodeIconWell.tsx
  274 + 214 + 86 + 272 + 319 + 167  =  1332
```

**1332 / 797 = +67.1 %**, carried into the ledger row as growth, not smoothed into a favourable
sentence. The classifier lives in the session scratchpad, never in the watched tree.

### 5. THE POINTER-VERIFICATION GREPS — all four resolve

```
### POINTER 1 — icon-convention.md:66 → NodeCornerMarks.tsx:266
$ sed -n '266p' frontend/src/components/workflows/NodeCornerMarks.tsx
          <span aria-hidden="true">⛨</span>

### POINTER 1b — GOVERNANCE_SEAL_LABEL → components/workflows/definitionOps.ts:466
$ sed -n '466p' frontend/src/components/workflows/definitionOps.ts
export const GOVERNANCE_SEAL_LABEL = "Must prove it"

### POINTER 2 — icon-convention.md:69 → PlaneEditingLayer.tsx:186 / :234 (testids :150 / :194)
$ sed -n '150p;186p;194p;234p' frontend/src/components/workflows/PlaneEditingLayer.tsx
          data-testid={`canvas-insert-${index}`}
          <span aria-hidden="true">＋</span>
          data-testid={`canvas-remove-${slug}`}
          <span aria-hidden="true">✕</span>

### POINTER 3 — SKILL.md's per-fact geometry pointers
$ sed -n '65p;70p' .../canvasModel.ts        →   NODE_WIDTH: 260,   /   NODE_MIN_HEIGHT: 104,
$ sed -n '176p'    .../PhaseNodeCard.tsx     →   "mx-auto block w-[248px] rounded-[22px] border pb-5 pt-[42px] px-5 text-center",
$ sed -n '145p'    .../NodeIconWell.tsx      →   className="pointer-events-none absolute left-1/2 top-[-26px] grid h-[62px] w-[62px] -translate-x-1/2 place-items-center"
$ sed -n '204p'    .../NodeCornerMarks.tsx   →   "pointer-events-none absolute -left-2 top-1.5 z-[8] grid h-[22px] w-[22px]",
$ sed -n '261p'    .../NodeCornerMarks.tsx   →   "pointer-events-none absolute right-[17px] top-[11px] z-[6] grid h-[21px] w-[21px]",
$ sed -n '142p'    .../NodeIconWell.tsx      →   the node wrapper around it — may ever take `overflow-hidden`. */}
```

**⚠ How stale each pointer actually was — measured, because "it moved in 188.2" was only half true
in every case:**

| Pointer | What it said | What was TRUE before 188.2 | Now |
|---|---|---|---|
| `icon-convention.md:66` (⛨) | `PhaseNodeCard.tsx:440` | **`:653`** — stale by **213 lines**; `:440` is a comment about `ringDash` | `NodeCornerMarks.tsx:266` |
| `icon-convention.md:66` (label) | `definitionOps.ts:388` | **no root-level `definitionOps.ts` exists**; it is `components/workflows/definitionOps.ts:466` | corrected |
| `icon-convention.md:69` (＋/✕) | `WorkflowCanvas.tsx:637, :680` | the pre-188.1 render sites were **`:641`, `:684`** — off by 4 even then — and **188.1** moved both out | `PlaneEditingLayer.tsx:186` / `:234` |
| `SKILL.md` "always read `PhaseNodeCard.tsx`" | 4 geometry facts | all four were in the card | 2 in `NodeCornerMarks`, 1 in `NodeIconWell`, the clipping ban subtree-wide; the 248/22/42-20-20 card facts **stayed** |

So three of the four are corrections of pointers that were **already wrong before this phase moved
anything** — which is the argument for the same-commit doc-truth rule, not merely a move-update.

### 6. GATES

| Gate | Command | Result |
|---|---|---|
| Count gate | `node scripts/vitest-count-gate.cjs` | **`count gate OK`**, failed 0, pinned == actual on both edited entries, no `[count-decrease]`, no `[missing-file]` |
| Type | `npx tsc --noEmit -p tsconfig.app.json \| grep -c "error TS"` | **33** — unmoved |
| Lint | `npx eslint src/components/workflows/` | **✖ 5 problems (5 errors, 0 warnings)** — unmoved |
| Scope | `git diff --stat -- frontend/src backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json` | **0 files changed** |
| T-188.2-SC | same, `package.json` / `package-lock.json` | **0 files changed** — zero packages installed |
| Deletions | `git diff --diff-filter=D --name-only HEAD~1 HEAD` | **empty** |
| Untracked | `git status --porcelain -- frontend/ \| grep '^??'` | **empty** |

### 7. THE SERVED-BUNDLE READING — recorded, and explicitly NOT evidence for any row

Both dev servers were found already running (frontend `:5173` HTTP 200, backend `:8000` HTTP 200 —
neither was started by this executor, per the standing rule). The served Vite modules were fetched to
establish the freshness premise mechanically:

```
card_still_declares_RING_RADIUS            false   <- THE CUT landed in the served tree
runoverlay_declares_RING_RADIUS            true
iconwell_declares_the_62x62_mark           true
card_renders_{NodeCornerMarks,NodeRunOverlay,NodeIconWell}_callsite   true / true / true
no_module_imports_back_into_card           true    <- no ESM cycle, AS SERVED
served card contains the ⛨ GLYPH            1      <- prose pointer only
served NodeCornerMarks contains ⛨           2
affordance_module_defines_AFFORDANCE_Z     true    (= 1002)
layer_wires_zIndex_AFFORDANCE_Z            3       <- all three affordance groups
```

**⚠ This proves the served bundle is the post-cut, post-fix tree. It proves NOTHING about layout,
stacking or reachability** — reading a served module is exactly as blind to those as jsdom is. It is
recorded as a freshness precondition in `188.2-UAT.md` and is **not** cited as evidence for A1, A2,
A3, B1 or B2.

### 8. THE UAT BOARDS — authored, and the state of every row

```
$ grep -c "NOT YET DRIVEN" .../188.2-UAT.md   →  5
$ grep -n "^rows_passed\|^status:\|^driven_by" .../188.2-UAT.md
status: AWAITING-OPERATOR
driven_by: NOBODY YET
rows_passed: 0
```

| Row | Board | Result |
|---|---|---|
| A1 the Builder card is visually identical at rest | A | ⬜ **NOT YET DRIVEN** |
| A2 a live run's seven readings paint the same seven shapes | A | ⬜ **NOT YET DRIVEN** |
| A3 a grounded + selected + verdict-carrying card is unchanged | A | ⬜ **NOT YET DRIVEN** |
| B1 the **SELECTED** card's `✕` is reachable (`elementFromPoint`, node z-index 1000) | B | ⬜ **NOT YET DRIVEN** |
| B2 the DESELECTED control is still reachable | B | ⬜ **NOT YET DRIVEN** |

The board carries: the disjoint-evidence rule in its header; **SC#10's non-application stated as a
DECISION with its reason** (no streaming, no agent loop, no provider routing, no UI state machine;
`git diff --stat -- backend/` is 0 files across the phase; the canvas makes no model call, so there
is no per-provider variable to vary); Board A's leanness stated as a decision; the two-step
select-then-hover drive B1 requires; and a per-row list of exactly which readings each row must
carry.

### 9. `BUG-260806-01` — FINAL FRONTMATTER STATE, AND THE REASON

```
status: folded              ← UNCHANGED
folded_into: 188.2
verified_closed_by: null    ← UNCHANGED
```

**Not flipped, deliberately.** The plan's own condition is *"ONLY IF both rows passed"*, and neither
was driven. `188.2-RESEARCH.md` rates the fix's SUFFICIENCY at **MEDIUM** until B1 exists, and the
bug report itself records why: `.click()` bypasses hit-testing and jsdom computes no stacking
contexts, which is how this defect survived from `184-12` with every unit test green. Closing it on
the strength of the stacking analysis would repeat the substitution that shipped it.

---

## Files Created/Modified

- **`CLAUDE.md`** — the `PhaseNodeCard.tsx` hot-file ledger row flipped, in the `WorkflowCanvas.tsx`
  row's house style: measured before/after for all three D-03 numbers, the classifier, the five
  destination modules, the commands, the honest +67.1 % subtree growth, what was PROVED rather than
  asserted, the three surviving invariants (one sharpened from *implied* to *mechanically asserted*),
  and the deferral pointer. Touch history corrected to 8 plans / 5 phases / 10 commits.
- **`scripts/vitest-count-gate.cjs`** — two pins raised with inline reasons in the file's own
  annotation style; `BASELINE_TOTAL`'s trailing note corrected 2482 → 2502 as the **fifth** recorded
  `⚠` correction of that note; the `failed`-line flake finding recorded at the same site. `TARGETS`
  untouched; no pin lowered.
- **`.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md`** — §4 rows for `⛨`
  and `＋`/`✕` re-pointed, each with a `⚠` recording that the previous pointer was already wrong
  before 188.2 moved anything.
- **`.claude/skills/sketch-findings-agentic-rag/SKILL.md`** — the *Canvas Frame & Node Anatomy* row's
  *"always read `PhaseNodeCard.tsx`"* replaced with per-fact pointers across the six-file subtree,
  every value grepped rather than restated from memory, plus the warning that
  `NodeIconWell.tsx:142`'s clipping sentence is pinned by a count fence and must not be re-worded.
- **`188.2-DEFERRED.md`** (created) — six deferrals, each with an observable re-open trigger.
- **`188.2-UAT.md`** (created) — both boards, five undriven rows, two recorded decisions.

## Decisions Made

1. **`188.2-UAT.md` was authored but not driven, and the file says so in three places** (a header
   banner, the frontmatter, and every row's `result:`). Authoring the board — the header note, the
   disjoint-evidence rule, the SC#10 decision, the per-row instructions and the evidence slots — does
   not depend on the checkpoint. Approving a row does, and nothing was approved.
2. **`BUG-260806-01` stays `folded`.** See §9.
3. **A fourth pointer was corrected beyond the plan's three.** `GOVERNANCE_SEAL_LABEL` was cited at
   `definitionOps.ts:388`; there is no root-level `definitionOps.ts`, and the constant is at
   `components/workflows/definitionOps.ts:466`. It sits in the same table cell as pointer 1 and was
   measured while verifying it — leaving it would have shipped a known-false pointer in the same
   edit that fixed its neighbour.
4. **The count-gate flake was recorded in TWO places** — `188.2-DEFERRED.md` D-188.2-DEF-01 and the
   gate script's own `BASELINE_TOTAL` comment — so a future reader of the tool meets the finding
   without opening a planning file, and the deferral carries the observable trigger.
5. **The classifier was rebuilt and validated rather than inherited.** The first build reproduced the
   totals but not the composition; the mismatch identified the missing JSX-comment rule precisely.
   Had the numbers simply been copied, that specification gap would have been invisible.

## Deviations from Plan

**1. [Rule 2 — Missing critical functionality] The `GOVERNANCE_SEAL_LABEL` pointer was corrected too**

- **Found during:** Task 1, verifying pointer 1.
- **Issue:** the plan names three stale pointers. Measuring the first one's table cell showed a
  fourth false claim in the same cell (`definitionOps.ts:388` — no such file at that path).
- **Fix:** re-pointed to `components/workflows/definitionOps.ts:466`, verified by `sed -n '466p'`.
- **Files:** `icon-convention.md` · **Commit:** `615b2a06`

**2. [Rule 3 — Blocking] The plan's `PhaseNode.tsx:165` anchor is wrong; it is `:167`**

- **Found during:** Task 1, writing the deferred register's `hasOwnProperty` item.
- **Issue:** `188.2-07-PLAN.md` names four guard sites; three are exact, `PhaseNode.tsx` is at
  **`:167`**. Writing the plan's figure would have shipped a deferral pointing at the wrong line.
- **Fix:** re-derived all four by grep; the register carries the measured anchors and the correction.
- **Files:** `188.2-DEFERRED.md` · **Commit:** `615b2a06`

**Total deviations:** 2 — one Rule 2, one Rule 3. None architectural. No package installed, no
production file touched, no test deleted, no pin lowered, no UAT row approved.

## Inherited claims re-measured (per the standing "don't inherit unmeasured claims" rule)

Fourteen claims were re-derived before being relied on. **Ten held exactly. Four are corrected.**

| # | Claim | Verdict |
|---|---|---|
| 1 | 188.2-06's three D-03 numbers: 797→274, 249→100, 427→169 | **ALL TRUE** — reproduced with an independently rebuilt classifier, itself validated against `797/518/249/30` first. |
| 2 | The subtree total is **1332 L / +67.1 %** (and the plan's `~940 L / +18 %` is FALSE) | **TRUE** — `wc -l` sums to 1332; 1332/797 = +67.1 %. The false estimate was not restated anywhere. |
| 3 | Body separator at `:106`, EOF at `:274` | **TRUE**, exactly. |
| 4 | Pins to move: `PhaseNodeCard.test.tsx` 107→124, `WorkflowCanvas.editing.test.tsx` 58→61, total 2482→2502 | **ALL TRUE** — read from the gate's own `actual` across two agreeing runs. |
| 5 | `scripts/vitest-count-gate.cjs:294` / `:411` / `:470` are the two pins and `BASELINE_TOTAL` | **TRUE at plan time**; after this plan's own insertions they are at `:302` / `:427` / `:479`. Recorded because the next reader will inherit the plan's figures. |
| 6 | `TARGETS[0]` is the bare directory `src/components/workflows`, so no `TARGETS` edit is needed | **TRUE** — untouched. |
| 7 | `icon-convention.md:66` says `PhaseNodeCard.tsx:440` and the true pre-cut home was `:653` | **TRUE, both halves** — `:440` is a `ringDash` comment; the glyph was at `:653`. |
| 8 | `icon-convention.md:69` says `WorkflowCanvas.tsx:637, :680` and 188.1 moved them | **Content TRUE, anchors FALSE** — the pre-188.1 render sites were **`:641`** and **`:684`**, so that pointer was *already* off by 4 before 188.1 moved anything. |
| 9 | `GOVERNANCE_SEAL_LABEL` at `definitionOps.ts:388` | **FALSE** — no root-level `definitionOps.ts`; it is `components/workflows/definitionOps.ts:466`. Corrected (deviation 1). |
| 10 | The `SKILL.md` row's four geometry facts all left `PhaseNodeCard.tsx` | **TRUE, all four** — mark → `NodeIconWell.tsx:145`, seal → `NodeCornerMarks.tsx:261`, verdict → `NodeCornerMarks.tsx:204`, clipping ban → `NodeIconWell.tsx:142`. ⚠ But the row's **other** facts (248px card / radius 22 / padding 42-20-20) **stayed** in the card at `:176`, and the 260×104 box was never in it (`canvasModel.ts:65`/`:70`) — so a blanket "read the new modules" would have been wrong too. Per-fact pointers were written for that reason. |
| 11 | The four inline guards at `phaseState.ts:77`, `phaseGlyph.tsx:92`, `PhaseNode.tsx:165`, `nodePresentation.ts:128` | **THREE TRUE, ONE FALSE** — `PhaseNode.tsx` is at **`:167`** (deviation 2). |
| 12 | Baselines `tsc` 33 (0 in `src/components/workflows/`), eslint 5 errors / 0 warnings | **TRUE**, unmoved. |
| 13 | The card row previously read "7 plans across 4 phases, 797 lines" | **TRUE as the prior text**; measured today it is 8 plans / 5 phases / 10 commits / 274 lines. |
| 14 | `188.1-DEFERRED.md` is the house shape for a deferred register (every item a concrete trigger) | **TRUE** — followed, including its `⚠`-correction convention. |

## Threat model disposition

| Threat ID | Disposition | Evidence |
|---|---|---|
| T-188.2-29 (`BUG-260806-01`'s closure) | **NOT DISCHARGED — deliberately open** | The report stays `status: folded`, `verified_closed_by: null`. B1 was not driven, so the only evidence that could close it does not exist. Recorded rather than worked around. |
| T-188.2-30 (the two boards' evidence) | **mitigated structurally, pending the rows** | Disjoint evidence columns, a header note naming the rule, per-row `own evidence` slots, and frontmatter (`status: AWAITING-OPERATOR`, `rows_passed: 0`) that cannot read as completion while rows are undriven. |
| T-188.2-31 (a row driven against a stale bundle) | **mitigated for the operator's benefit** | The hard reload is required and stated before the first row of each board, AND the served bundle was proven post-cut mechanically — with an explicit note that this is a precondition, not evidence. |
| T-188.2-32 (the count-gate pin) | **mitigated** | EXTENSIONS ONLY, both values read from the gate's own `actual` across two agreeing runs, `TARGETS` untouched, the only removed pin lines being the two raised, and a verification run showing pinned == actual. |
| T-188.2-33 (the flipped ledger row) | **mitigated** | Carries all three D-03 numbers as before → after, the classifier (re-validated in THIS plan against the pre-cut figures), the five destinations and the commands — a future reader re-derives rather than inherits. |
| T-188.2-SC (package installs) | **mitigated** | `git diff --stat -- frontend/package.json frontend/package-lock.json` → 0 files changed. Zero packages installed. |

## Issues Encountered

**One blocking, by design: Tasks 2 and 3 are operator checkpoints and this session cannot drive a
browser.** See the checkpoint returned to the orchestrator.

**One finding, recorded rather than fixed:** the count gate's `failed` line is unreliable on this
machine today (varying 0/1 across two runs with identical per-file columns; the failure is in
`WorkflowBuilderPage.canvas.test.tsx`, which passes 128/128 alone and which no 188.2 plan touches).
Recorded in `188.2-DEFERRED.md` D-188.2-DEF-01 and in the gate script's own comment, per the
operator's decision to continue on scoped per-file gates rather than fix the flakiness in this phase.

## User Setup Required

**Yes — this plan cannot finish without the operator.** Drive the five rows in
`188.2-UAT.md`, paste each row's own evidence, and (only if both Board B rows pass) flip
`BUG-260806-01` to `closed` with `verified_closed_by: 188.2`. The dev server is already running at
`http://localhost:5173/`; backend, Supabase and Redis are not needed for Board A/B.

## Next Phase Readiness

- **The G-5 gate on `PhaseNodeCard.tsx` is retired.** `CLAUDE.md`'s ledger row reads *satisfied
  (188.2 — 2026-08-07)*, so **Phase 189's discuss-phase no longer owes a refactor recommendation as
  its first option** on that file. The seams 189 needs are open: badge slot 1 is still EMPTY and
  reserved (D-12), the third-badge typecheck invariant and the no-focusable-control invariant are
  both still enforced, and the contract they extend is now its own file with exactly one name per
  type.
- **⚠ Still owed before the phase can be verified:** the five UAT rows, and `BUG-260806-01`'s closure
  — which SC#6 names explicitly (*"proved by a driven `elementFromPoint` row with the card selected
  and the node measured at z-index 1000 … the analysis alone does not close it"*). SC#7 likewise
  requires both boards to carry their own evidence with no row reading `[pending]` under a completed
  header; that header currently reads `AWAITING-OPERATOR`, which is the honest state.
- **Not owed:** ROADMAP's 188.2 plan checkbox for `188.2-07` stays **unchecked** and STATE's
  `completed_plans` stays at **124**. Nothing here claims a completion it does not have.

## Self-Check: PASSED

- `FOUND: .planning/phases/188.2-…/188.2-07-SUMMARY.md`
- `FOUND: .planning/phases/188.2-…/188.2-DEFERRED.md`
- `FOUND: .planning/phases/188.2-…/188.2-UAT.md`
- `FOUND: 615b2a06` · `FOUND: 360399b2`
- `VERIFIED: grep -c "satisfied (188.2" CLAUDE.md → 1`
- `VERIFIED: "PhaseNodeCard.test.tsx": 124 · "WorkflowCanvas.editing.test.tsx": 61 · count gate OK, failed 0, pinned == actual`
- `UNCHANGED (as required): frontend/src · backend/ · supabase/migrations/ · frontend/package.json · frontend/package-lock.json → 0 files changed`
- `UNCHANGED (as required): BUG-260806-01 → status: folded, verified_closed_by: null`
- `NO FILE DELETED: git diff --diff-filter=D --name-only HEAD~2..HEAD → empty`

---
*Phase: 188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p*
*Status: PARTIAL — blocked at Task 2 (blocking operator checkpoint), 2026-08-07*
