---
phase: 227-the-run-frame-has-one-owner
plan: 03
type: execute
wave: 3
depends_on:
  - 227-02
files_modified:
  - frontend/src/components/chat/RunCard.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md
autonomous: true
requirements:
  - SC#1
  - SC#2
  - SC#3
  - SC#4
user_setup: []

must_haves:
  truths:
    - "RunCard.tsx owns the run frame: header, rail, steps, terminal status, footer"
    - "RunTerminalStatus is co-located in RunCard.tsx and encapsulates terminal status copy and conditions"
    - "MessageItem.tsx delegates terminal status rendering to RunTerminalStatus, rendering a run rather than pieces of one"
    - "Moving the terminal status line inside the frame (SC#3) is localized entirely within RunCard.tsx as a clean 1-file edit"
    - "MessageItem.tsx and ToolCallPanel.tsx leave 'extraction due' honestly in CLAUDE.md and docs/HOT-FILE-LEDGER.md in the same commit by having lost the responsibilities"
    - "227-VALIDATION.md is authored detailing the 8 run states (streaming, settled, failed, timed-out, cancelled, paused-on-approval, no-tools, sub-agent) with reproduction instructions"
    - "All gates pass: Vitest count gate OK with 0 failures, tsc 66 (holding baseline), and backend unit tests 71 (holding baseline)"
  artifacts:
    - frontend/src/components/chat/RunCard.tsx
    - frontend/src/components/chat/MessageItem.tsx
    - CLAUDE.md
    - docs/HOT-FILE-LEDGER.md
    - .planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md
  key_links:
    - "MessageItem.tsx mounts RunTerminalStatus exported from RunCard.tsx"
    - "CLAUDE.md and docs/HOT-FILE-LEDGER.md rows for MessageItem.tsx and ToolCallPanel.tsx are updated to honoured by extraction"
---

<objective>
Consolidate Run Frame ownership into `RunCard.tsx`, delegate terminal status rendering from `MessageItem.tsx`, update hot-file ledger rows, and author `227-VALIDATION.md`:
1. In `RunCard.tsx`: Extract and export `RunTerminalStatus`, encapsulating terminal state checking and copy. Provide the internal slot/structure so that moving the status line inside the frame in a future phase is a 1-file edit in `RunCard.tsx` (SC#3).
2. In `MessageItem.tsx`: Replace lines 752-761 with `<RunTerminalStatus message={message} isStreaming={isStreaming} />`. `MessageItem.tsx` renders *a run* without owning terminal status derivation (SC#1, SC#2).
3. Update `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` in the same commit: Re-derive triples for `MessageItem.tsx`, `ToolCallPanel.tsx`, and `RunCard.tsx`. Record legitimate extraction discharge (SC#4).
4. Author `.planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md` with explicit reproduction steps across all 8 run states for the operator's before/after browser drive.
5. Run full gate checks (`vitest-count-gate.cjs`, `tsc`, `pytest`).

Output: Updated `RunCard.tsx`, `MessageItem.tsx`, `CLAUDE.md`, `docs/HOT-FILE-LEDGER.md`, and `227-VALIDATION.md`.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-the-run-frame-has-one-owner/227-CONTEXT.md
@frontend/src/components/chat/RunCard.tsx
@frontend/src/components/chat/MessageItem.tsx
@CLAUDE.md
@docs/HOT-FILE-LEDGER.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extract RunTerminalStatus in RunCard.tsx with SC#3 1-file edit readiness</name>
  <files>frontend/src/components/chat/RunCard.tsx</files>
  <read_first>
    frontend/src/components/chat/RunCard.tsx
    frontend/src/components/chat/MessageItem.tsx
  </read_first>
  <action>
    1. In `frontend/src/components/chat/RunCard.tsx`:
       - Define and export `RunTerminalStatus({ message, isStreaming }: { message: Message; isStreaming: boolean })`:
         ```tsx
         export function RunTerminalStatus({
           message,
           isStreaming,
           className,
         }: {
           message: Message
           isStreaming: boolean
           className?: string
         }) {
           if (isStreaming) return null
           const showTerminal =
             message.stopped ||
             message.runStatus === "timed_out" ||
             (message.runStatus === "cancelled" && !!message.content)
           if (!showTerminal) return null

           const copy =
             message.runStatus === "timed_out"
               ? "Agent reached time limit"
               : "Response stopped"

           return (
             <div className={cn("flex items-center gap-1.5 mt-1 text-xs text-muted-foreground", className)}>
               <span className="italic">{copy}</span>
             </div>
           )
         }
         ```
       - SC#3 preparation: Document clearly with a comment in `RunCard.tsx` that moving `RunTerminalStatus` inside the card frame (e.g. at the bottom of the expanded body or below the Next-up footer) is a 1-file edit inside `RunCard.tsx`.
  </action>
  <verify>
    Verify TypeScript compilation in `frontend/`: `npx tsc -p tsconfig.app.json --noEmit`.
  </verify>
  <acceptance_criteria>
    - `RunTerminalStatus` is exported from `RunCard.tsx`.
    - Terminal condition derivation and copy are encapsulated.
    - Zero TypeScript compilation errors.
  </acceptance_criteria>
  <done>Created RunTerminalStatus in RunCard.tsx ready for delegation and 1-file move.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MessageItem.tsx to delegate terminal status to RunTerminalStatus</name>
  <files>frontend/src/components/chat/MessageItem.tsx</files>
  <read_first>
    frontend/src/components/chat/MessageItem.tsx
    frontend/src/components/chat/RunCard.tsx
  </read_first>
  <action>
    1. In `frontend/src/components/chat/MessageItem.tsx`:
       - Import `RunTerminalStatus` from `./RunCard`.
       - Replace lines 752-761:
         ```tsx
         {(message.stopped ||
           message.runStatus === "timed_out" ||
           (message.runStatus === "cancelled" && !!message.content)) &&
           !isStreaming && (
           <div className="flex items-center gap-1.5 mt-1 text-xs text-muted-foreground">
             <span className="italic">
               {message.runStatus === "timed_out" ? "Agent reached time limit" : "Response stopped"}
             </span>
           </div>
         )}
         ```
         with:
         ```tsx
         <RunTerminalStatus message={message} isStreaming={isStreaming} />
         ```
       - `MessageItem.tsx` now renders the run status via `RunTerminalStatus` rather than deriving the terminal sentence itself.
       - Layout, class names, and DOM output remain 100% byte-identical (SC#2).
    2. Run vitest suites:
       - `npx vitest run src/__tests__/components/MessageItem.test.tsx`
       - `npx vitest run src/components/chat/__tests__/MessageItem.test.tsx`
  </action>
  <verify>
    Run `npx vitest run src/__tests__/components/MessageItem.test.tsx src/components/chat/__tests__/MessageItem.test.tsx` in `frontend/`.
  </verify>
  <acceptance_criteria>
    - `MessageItem.tsx` mounts `<RunTerminalStatus>`.
    - Existing tests pass 100%.
    - Visual DOM output is byte-identical.
  </acceptance_criteria>
  <done>MessageItem.tsx delegates terminal status to RunTerminalStatus.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md and docs/HOT-FILE-LEDGER.md in the same commit (SC#4)</name>
  <files>
    CLAUDE.md
    docs/HOT-FILE-LEDGER.md
  </files>
  <read_first>
    CLAUDE.md
    docs/HOT-FILE-LEDGER.md
  </read_first>
  <action>
    1. Re-derive triples for all touched hot files via git (`commits / phases / lines`):
       - `frontend/src/components/chat/MessageItem.tsx`
       - `frontend/src/components/chat/ToolCallPanel.tsx`
       - `frontend/src/components/chat/RunCard.tsx`
    2. In `CLAUDE.md` line 535-537:
       - Update `ToolCallPanel.tsx`: triple updated, status updated from `extraction due` to `honoured by extraction (227: ToolCallDetails + StepRow extracted)`.
       - Update `MessageItem.tsx`: triple updated, status updated from `extraction due` to `honoured by extraction (227: RunTerminalStatus extracted)`.
       - Update `RunCard.tsx`: triple updated, status noted for Phase 227 run frame ownership.
    3. In `docs/HOT-FILE-LEDGER.md`:
       - Append extraction discharge records under `## frontend/src/components/chat/ToolCallPanel.tsx` and `## frontend/src/components/chat/MessageItem.tsx`.
       - Record that both files left "extraction due" legitimately by shedding responsibilities, not by re-wording the row.
    4. Run hot file size check: `bash scripts/check-hot-file-sizes.sh` (or powershell equivalent).
  </action>
  <verify>
    Verify git diff on `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` to confirm honest triple derivation and extraction discharge.
  </verify>
  <acceptance_criteria>
    - Both `MessageItem.tsx` and `ToolCallPanel.tsx` rows leave "extraction due".
    - Triples match current HEAD state.
    - `docs/HOT-FILE-LEDGER.md` details the extractions.
  </acceptance_criteria>
  <done>Updated CLAUDE.md and docs/HOT-FILE-LEDGER.md discharging extraction due in the same commit.</done>
</task>

<task type="auto">
  <name>Task 4: Author 227-VALIDATION.md and run full gate suite</name>
  <files>.planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md</files>
  <read_first>
    .planning/phases/227-the-run-frame-has-one-owner/227-MEASUREMENT-PACK.md
    .planning/phases/227-the-run-frame-has-one-owner/227-PROPOSAL.md
  </read_first>
  <action>
    1. Author `.planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md`:
       - Define the 8-state test matrix:
         1. `streaming`: Live run in progress with active tool spinner and timer.
         2. `settled`: Completed run with collapsed row `Run · N steps`.
         3. `failed`: Run ending with error; status pill in error state.
         4. `timed-out`: Run stopped at timeout; terminal copy `"Agent reached time limit"`.
         5. `cancelled`: User stopped run mid-flight; terminal copy `"Response stopped"`.
         6. `paused-on-approval`: Paused run waiting for tool grant.
         7. `no-tools`: Assistant response without tool calls.
         8. `sub-agent`: Run involving subagent delegation.
       - Provide reproduction steps and triggers for each state for the operator's before/after browser drive.
       - Detail SC#3 verification proving both deferred changes (terminal status line inside frame, right-aligned result column) are now 1-file edits.
    2. Run full gate checks:
       - Vitest count gate: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` (must pass 0 failing, all pinned files present).
       - TypeScript: `npx tsc -p tsconfig.app.json --noEmit` in `frontend/` (must hold baseline 66).
       - Pytest: `pytest tests/unit -q` in `backend/` (must hold baseline 71).
  </action>
  <verify>
    Run all three gates and record exact readings in `227-VALIDATION.md`.
  </verify>
  <acceptance_criteria>
    - `227-VALIDATION.md` exists with 8 run state reproduction rows.
    - All three gates verified and holding baselines.
  </acceptance_criteria>
  <done>Authored 227-VALIDATION.md and verified all gates.</done>
</task>

</tasks>
