---
phase: 227-the-run-frame-has-one-owner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/__tests__/components/ToolCallPanel.test.tsx
  - frontend/src/__tests__/components/MessageItem.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements:
  - SC#5
user_setup: []

must_haves:
  truths:
    - "All 15 covering test suites for the chat run frame (the 14 suites with 0 mentions in measurement pack §4 plus RunCard.logo) are in TARGETS and BASELINE of scripts/vitest-count-gate.cjs"
    - "Pre-existing test assertions in ToolCallPanel.test.tsx and MessageItem.test.tsx are aligned with shipped behavior (2026-08-31 operator noise audit and Phase 174/194 'Setting up agent…' contract)"
    - "scripts/vitest-count-gate.cjs runs green with 0 failing tests and no unpinned test decrease before any refactoring code is touched"
    - "The count gate verdict line is re-derived from actual run output and committed as baseline infrastructure"
  artifacts:
    - scripts/vitest-count-gate.cjs
    - frontend/src/__tests__/components/ToolCallPanel.test.tsx
    - frontend/src/__tests__/components/MessageItem.test.tsx
  key_links:
    - "vitest-count-gate.cjs TARGETS array includes all 15 covering suite paths"
    - "vitest-count-gate.cjs BASELINE map includes all 15 covering suite bare filenames with their measured test counts"
---

<objective>
Pin all covering suites in `scripts/vitest-count-gate.cjs` BEFORE any refactoring begins (SC#5):
1. Align two pre-existing failing assertions in previously-unpinned suites with shipped project contracts:
   - `frontend/src/__tests__/components/ToolCallPanel.test.tsx`: Align with the 2026-08-31 operator noise audit (`ToolCallPanel.tsx:420-430`) which removed redundant green `DONE` status-pills from finished steps.
   - `frontend/src/__tests__/components/MessageItem.test.tsx`: Align the empty-content streaming indicator test with Phase 174/194's `"Setting up agent…"` contract (`MessageItem.tsx:689`).
2. Add all 15 covering suites to `TARGETS` and `BASELINE` in `scripts/vitest-count-gate.cjs`.
3. Run the gate, re-derive the verdict line and new `BASELINE_TOTAL`, and verify 0 failures.

Output: Updated `ToolCallPanel.test.tsx`, `MessageItem.test.tsx`, and `scripts/vitest-count-gate.cjs`.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-the-run-frame-has-one-owner/227-CONTEXT.md
@.planning/phases/227-the-run-frame-has-one-owner/227-MEASUREMENT-PACK.md
@scripts/vitest-count-gate.cjs
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Align pre-existing assertions in ToolCallPanel.test.tsx and MessageItem.test.tsx</name>
  <files>
    frontend/src/__tests__/components/ToolCallPanel.test.tsx
    frontend/src/__tests__/components/MessageItem.test.tsx
  </files>
  <read_first>
    frontend/src/__tests__/components/ToolCallPanel.test.tsx
    frontend/src/components/chat/ToolCallPanel.tsx
    frontend/src/__tests__/components/MessageItem.test.tsx
    frontend/src/components/chat/MessageItem.tsx
  </read_first>
  <action>
    1. In `frontend/src/__tests__/components/ToolCallPanel.test.tsx`:
       - Lines 75-79 assert:
         ```tsx
         essences.forEach((row) => {
           expect(row.querySelector("[data-testid='status-pill']")).toBeTruthy()
         })
         ```
       - The 2026-08-31 operator noise audit (`ToolCallPanel.tsx:420-430`) explicitly removed `status-pill` for plain `done` steps because the run header already carries `✓ done`.
       - Update the test to reflect this shipped contract: on done essence cards, the text summary and tool icon render cleanly without a redundant done pill, while non-done/interrupted/failed steps preserve their status pill.
    2. In `frontend/src/__tests__/components/MessageItem.test.tsx`:
       - Line 106 asserts:
         ```tsx
         expect(screen.getByText(/thinking/i)).toBeInTheDocument()
         ```
       - `MessageItem.tsx:684-694` (Phase 174 / 194) renders `"Setting up agent…"` (or `"Reasoning…"`) when streaming with empty content and no tools yet.
       - Update the test assertion to expect `/setting up agent/i` (or `/reasoning/i`).
    3. Run `npx vitest run src/__tests__/components/ToolCallPanel.test.tsx src/__tests__/components/MessageItem.test.tsx` to verify both suites pass with 0 failures.
  </action>
  <verify>
    Run `npx vitest run src/__tests__/components/ToolCallPanel.test.tsx src/__tests__/components/MessageItem.test.tsx` in `frontend/`. Both suites must pass 100%.
  </verify>
  <acceptance_criteria>
    - `ToolCallPanel.test.tsx` passes with 0 failures, honoring the 2026-08-31 noise audit.
    - `MessageItem.test.tsx` passes with 0 failures, honoring the Phase 174/194 setup-copy contract.
  </acceptance_criteria>
  <done>Pre-existing failing assertions in ToolCallPanel.test.tsx and MessageItem.test.tsx aligned and passing green.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt all 15 covering suites into TARGETS and BASELINE in scripts/vitest-count-gate.cjs</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    scripts/vitest-count-gate.cjs
    .planning/phases/227-the-run-frame-has-one-owner/227-MEASUREMENT-PACK.md
  </read_first>
  <action>
    1. Read `227-MEASUREMENT-PACK.md` §4 listing the covering suites with zero mentions:
       - `src/components/chat/__tests__/MessageItem.test.tsx`
       - `src/components/chat/__tests__/MessageItem.blockedNotice.test.tsx`
       - `src/components/chat/__tests__/MessageItem.harnessBanner.test.tsx`
       - `src/components/chat/__tests__/ChatAreaBanner.test.tsx`
       - `src/components/chat/__tests__/ChatAreaMode.test.tsx`
       - `src/__tests__/components/MessageItem.test.tsx`
       - `src/__tests__/components/MessageItem.clamp.test.tsx`
       - `src/__tests__/components/MessageItem.fallbackNotice.test.tsx`
       - `src/__tests__/components/MessageItem.memo.test.tsx`
       - `src/__tests__/components/MessageItem.sticky.test.tsx`
       - `src/__tests__/components/RunCard.logo.test.tsx`
       - `src/__tests__/components/ToolCallPanel.test.tsx`
       - `src/__tests__/components/chat/MessageList.test.tsx`
       - `src/__tests__/components/chat/MessageList.dedup.test.tsx`
       - `src/__tests__/components/chat/MessageList.runline.baseline.test.tsx`
    2. Add all 15 paths to the `TARGETS` array in `scripts/vitest-count-gate.cjs` under a dedicated Phase 227 block.
    3. Run `npx vitest run <15 suites> --reporter=json` (or `node scripts/vitest-count-gate.cjs`) to capture the exact assertion count for each bare filename from the `actual` output.
    4. Add each of the 15 bare filenames and its measured count to the `BASELINE` object in `scripts/vitest-count-gate.cjs`.
    5. Run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root to verify that the gate passes with all pinned suites present and 0 failures.
  </action>
  <verify>
    Run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from repo root. The verdict line must read `count gate OK — N/N pinned files present, no per-file decrease, 0 failing.` (or only the pre-existing cap-independent flake in WorkflowBuilderPage.canvas.test.tsx if triggered).
  </verify>
  <acceptance_criteria>
    - All 15 covering suites are listed in `TARGETS`.
    - All 15 bare filenames are pinned in `BASELINE` with counts read from actual execution.
    - `vitest-count-gate.cjs` successfully executes all adopted suites.
  </acceptance_criteria>
  <done>Wave 1 gate pinning complete: covering suites are guarded by vitest-count-gate.cjs before refactoring starts.</done>
</task>

</tasks>
