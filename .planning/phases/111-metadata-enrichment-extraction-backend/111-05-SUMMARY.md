---
phase: 111-metadata-enrichment-extraction-backend
plan: 05
subsystem: database
tags: [migration-apply, live-db, full-schema-regen, psycopg2-direct, blocking-checkpoint, no-reset, dev-data-preserved]

# Dependency graph
requires:
  - phase: 111-01
    provides: "migration 072 file (3 app_settings extraction columns + metadata_field_definitions.options jsonb) — authored un-applied"
  - phase: 111-03
    provides: "the /metadata-fields CRUD router whose integration tests need the live options column"
  - phase: 111-04
    provides: "the ingest_document enriched branch whose settings reads need the live extraction columns"
provides:
  - "migration 072 LIVE on :54322 — the 3 app_settings extraction columns + metadata_field_definitions.options jsonb now exist with the right defaults"
  - "supabase/full-schema.sql regenerated (script-generated, no --reset) reflecting all 4 new columns"
  - "the test_111 integration suite is now truly GREEN against the live DB (no longer in a false-positive build-passes-without-apply state)"
affects: [111-verification, 112-confidence-display, 113-119-dm-read-paths]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Operator-authorized psycopg2-direct apply in ONE transaction (ADD COLUMN IF NOT EXISTS, idempotent) — the 100/102/110 precedent; NEVER supabase db push / db reset"
    - "Read-back assertion BEFORE marking done: assert all 4 columns + defaults AND documents count unchanged (>0) to prove no reset happened"
    - "full-schema.sql regenerated by scripts/regenerate-full-schema.sh (live-DB dump, no --reset), never hand-edited"
  removed: []

# Files
key-files:
  created:
    - "scripts/apply_migration_072.py — throwaway operator-authorized apply+read-back script (kept as the apply record; idempotent, NOT in the uvicorn-watched backend/ tree)"
  modified:
    - "supabase/full-schema.sql — regenerated from the live DB (2935 lines); now carries extraction_model / extraction_window_cap / metadata_enrichment_mode (app_settings) + options jsonb (metadata_field_definitions)"
  note: "migration file supabase/migrations/072_app_settings_extraction_model.sql was authored + committed in 111-01 (c17a9662); this plan crossed it into the live DB and committed the regenerated artifact."

commits:
  - "0438eace — feat(111-05): apply migration 072 to live :54322 + regenerate full-schema.sql"
---

# Plan 111-05 Summary — [BLOCKING] Apply migration 072 live + regenerate full-schema.sql

## What was built

The operator-gated gate the phase could not pass without. Migration 072 (authored un-applied in Plan 01) was crossed into the live local Supabase DB (:54322), making the 3 `app_settings` extraction columns + the `metadata_field_definitions.options` jsonb carrier actually resolvable on the live DB instead of reading column-defaults forever / erroring on a missing column.

## Tasks

- **Task 1 (auto) — Apply migration 072 + read-back.** The local stack was UP, so the autonomous psycopg2-direct path was used (operator-authorized at the Wave-4 checkpoint — the 100/102/110 precedent). `scripts/apply_migration_072.py` connected to `postgresql://postgres:postgres@127.0.0.1:54322/postgres`, executed `supabase/migrations/072_app_settings_extraction_model.sql` verbatim in ONE transaction, then read back `information_schema.columns`. **Result printed `OK migration 072 live`:** `extraction_model` (default NULL), `extraction_window_cap` (default 32000), `metadata_enrichment_mode` (default `'enriched'`), `metadata_field_definitions.options` (jsonb, default NULL). **Dev data preserved: documents 19 → 19** (proves no reset). Idempotent (`ADD COLUMN IF NOT EXISTS` — safe to re-run).

- **Task 2 (checkpoint:human-verify, gate=blocking) — Operator confirmed + regenerate + live tests + commit.** Operator authorized the psycopg2-direct apply via the Wave-4 checkpoint (chose "psycopg2-direct" over self-paste). `bash scripts/regenerate-full-schema.sh` (NO `--reset`) rebuilt `supabase/full-schema.sql` (2935 lines, live-DB dump) — confirmed it now contains `extraction_model`, `extraction_window_cap integer DEFAULT 32000`, `metadata_enrichment_mode text DEFAULT 'enriched'`, and `options jsonb` (line 611, on metadata_field_definitions). The live `test_111` integration suite passed: **7 passed / 2 xpassed / 0 failures** (`test_111_settings_readback`, `test_111_flat_filter_compat`, `test_111_metadata_fields_crud`, `test_111_audit_field_create`, `test_111_field_def_scoping`). Committed `full-schema.sql` + the apply-record script (migration file was already committed in 111-01).

## Acceptance criteria — all met

- ✅ `grep "extraction_model" / "metadata_enrichment_mode" / "options jsonb" supabase/full-schema.sql` — all match.
- ✅ `pytest tests/integration/test_111_*.py -q` exits 0 against the live DB (7 passed / 2 xpassed).
- ✅ `full-schema.sql` script-regenerated (no `--reset`), not hand-edited.
- ✅ Migration 072 live on :54322 — 4 columns + correct defaults; dev data preserved (19 docs); never `db push`/`db reset`.

## Threat model — all mitigated

- **T-111-05-01 (partial apply):** one transaction + idempotent `ADD COLUMN IF NOT EXISTS` + read-back-before-done. A failed apply would roll back, leaving existing columns untouched.
- **T-111-05-02 (db push/reset wiping data):** psycopg2-direct only; `regenerate-full-schema.sh` ran with NO `--reset`; the read-back asserted documents count unchanged (19 → 19).
- **T-111-05-03 (hand-edited full-schema drift):** regenerated by the script from the live dump, never hand-edited.

## Deviations

**None.** Plan executed exactly as written. The local stack was up, so the autonomous psycopg2-direct path was used (no SQL-editor-paste fallback needed). The throwaway apply script was committed (kept as the apply record, mirroring `scripts/repair_dirty_workflow_phases.py` from 101.1) rather than deleted. STATE.md / ROADMAP.md were left for the orchestrator (sole writer for this phase).

## Self-Check: PASSED

- ✅ Migration 072 live on :54322 (read-back: 4 columns + defaults; documents 19→19).
- ✅ `supabase/full-schema.sql` regenerated + committed (2935 lines, contains all 4 columns).
- ✅ Live `test_111` integration suite GREEN (7 passed / 2 xpassed / 0 failures).
- ✅ Commit `0438eace` present in git log.
- ✅ No `db push`/`db reset` run; dev data preserved.
