---
phase: 111-metadata-enrichment-extraction-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_111_dynamic_model.py
  - backend/tests/unit/test_111_confidence_survives_exclude_none.py
  - backend/tests/unit/test_111_exclude_none_nonregression.py
  - backend/tests/unit/test_111_window_sampling.py
  - backend/tests/unit/test_111_extraction_model_resolve.py
  - backend/tests/unit/test_111_lmstudio_provider.py
  - backend/tests/integration/test_111_metadata_fields_crud.py
  - backend/tests/integration/test_111_field_def_scoping.py
  - backend/tests/integration/test_111_settings_readback.py
  - backend/tests/integration/test_111_flat_filter_compat.py
  - backend/tests/integration/test_111_audit_field_create.py
  - supabase/migrations/072_app_settings_extraction_model.sql
  - backend/app/config.py
  - backend/app/models/user_settings.py
  - backend/.env.example
autonomous: true
requirements: [META-03]
user_setup: []

must_haves:
  truths:
    - "Every Wave-0 test_111_* file exists and runs (RED for not-yet-built behavior, never collection-errors)"
    - "Migration 072 file exists with the 3 app_settings ALTERs + the metadata_field_definitions.options ALTER, un-applied (Plan 05 crosses it live)"
    - "A `lmstudio` provider is registered in config so a non-impersonation local route resolves"
    - "extraction_model / extraction_window_cap / metadata_enrichment_mode read back off app_settings via _build_settings_from_row (env_attr=None, app_settings-only)"
  artifacts:
    - path: "supabase/migrations/072_app_settings_extraction_model.sql"
      provides: "3 app_settings columns + options jsonb on metadata_field_definitions"
      contains: "ADD COLUMN IF NOT EXISTS extraction_model"
    - path: "backend/app/config.py"
      provides: "lmstudio provider base-url + key + resolve branch (no /v1 append)"
      contains: "lmstudio"
    - path: "backend/app/models/user_settings.py"
      provides: "3 UserEffectiveSettings fields + 3 _build_settings_from_row lines (env_attr=None)"
      contains: "metadata_enrichment_mode"
    - path: "backend/tests/unit/test_111_confidence_survives_exclude_none.py"
      provides: "the BLOCKING A1 underscore-field caveat proof scaffold"
      min_lines: 10
  key_links:
    - from: "backend/app/models/user_settings.py _build_settings_from_row"
      to: "app_settings columns (migration 072)"
      via: "_val(row, <col>, None, <default>)"
      pattern: "_val\\(row, \"extraction_model\""
    - from: "backend/app/config.py resolve_llm_provider"
      to: "self.lmstudio_base_url"
      via: "elif provider == lmstudio branch (no /v1 append)"
      pattern: "lmstudio"
---

<objective>
Land the Wave-0 test scaffolds and the additive foundations for Phase 111: the migration 072 FILE (un-applied), the first-class `lmstudio` provider, and the 3 `app_settings`-backed `UserEffectiveSettings` fields. This is the substrate every later plan builds on — settings the engine reads, the provider the local UAT row uses, and the failing-first test files that turn GREEN as Plans 02-04 deliver behavior.

Purpose: Front-load the cross-plan test files (RED) and the zero-risk additive config/model/migration changes so Plans 02/03 (engine + CRUD) and Plan 04 (wiring) have their settings + provider + schema contract already in place. Migration 072 is AUTHORED here but APPLIED in Plan 05 (the [BLOCKING] live-apply gate) — the FILE existing does not change the live DB.
Output: 11 test_111_* files, migration 072 SQL, lmstudio provider registration, 3 new settings fields.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-metadata-enrichment-extraction-backend/111-RESEARCH.md
@.planning/phases/111-metadata-enrichment-extraction-backend/111-PATTERNS.md
@.planning/phases/111-metadata-enrichment-extraction-backend/111-VALIDATION.md

<interfaces>
<!-- The closed field_type vocabulary the Create model + dynamic-model builder share (RESEARCH Pattern 2 / Q4). -->
<!-- Built-in 7 fields (models/document.py:8-15) the dynamic model mirrors as always-on: -->
title, author, date, document_type, topics, language, summary

<!-- field_type closed Literal vocabulary (D-111-5; Q4 gap (a) — enum SHIPS, options jsonb added in migration 072): -->
{string, date, number, boolean, enum}

<!-- The exact migration 072 column set (RESEARCH Q9 / PATTERNS migration block): -->
app_settings.extraction_model         text DEFAULT NULL    (unset → env settings.llm_model = gpt-4o)
app_settings.extraction_window_cap    integer DEFAULT 32000
app_settings.metadata_enrichment_mode text DEFAULT 'enriched'   (enriched | legacy)
metadata_field_definitions.options    jsonb DEFAULT NULL   (carries enum options)

<!-- The _build_settings_from_row env_attr=None precedent (user_settings.py:478-480 document_management_enabled, :489-496 extraction_*): -->
extraction_model=str(_val(row, "extraction_model", None, "")),
extraction_window_cap=int(_val(row, "extraction_window_cap", None, 32000)),
metadata_enrichment_mode=str(_val(row, "metadata_enrichment_mode", None, "enriched")),

<!-- config.py live _PROVIDER_BASE_URLS (verified this session): ollama="" resolved dynamically; lmstudio mirrors it minus the /v1 append. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author all 11 Wave-0 test_111_* scaffolds (RED)</name>
  <read_first>
    - backend/tests/integration/test_110_dm_audit_live.py (the live-DB :54322 header — _POSTGRES_TEST_DSN, _pg_reachable, pytestmark skipif, function-scoped pg_pool fixture; copy verbatim — PATTERNS.md §Wave-0 tests lines 392-433)
    - backend/tests/integration/test_110_dm_schema.py (the two_users fixture + _select_count_as_user SET LOCAL ROLE incantation + test_rls_global_metadata_field_visible_to_other_user at :270-287 — PATTERNS.md lines 435-455)
    - .planning/phases/111-metadata-enrichment-extraction-backend/111-VALIDATION.md (the Per-Task Verification Map + Wave 0 Requirements — the exact 11 filenames + test names)
    - .planning/phases/111-metadata-enrichment-extraction-backend/111-RESEARCH.md §"Validation Architecture" + Pattern 2 (the A1 underscore-field caveat the confidence test must prove)
  </read_first>
  <action>
    Create all 11 Wave-0 test files matching the VALIDATION.md filenames EXACTLY. Follow the Phase-110 RED convention: import the not-yet-built symbols INSIDE each test body (or guard module-level imports) so collection never errors; mark not-yet-built behavior `@pytest.mark.xfail(strict=False)` so the suite exits 0 today and the test flips to a real assertion when its owning plan lands.

    UNIT files (plain pytest, no DB — backend/tests/unit/):
    - `test_111_dynamic_model.py`: `test_custom_fields_in_schema` (build_metadata_model([{field_key:"contract_value",field_type:"number"}]) → schema has the 7 built-ins + contract_value + confidence) and `test_field_type_vocabulary` (unknown field_type rejected by the closed Literal {string,date,number,boolean,enum}; enum with options → Literal members). Import `build_metadata_model` from `app.services.embedding_service` inside the body.
    - `test_111_confidence_survives_exclude_none.py` (BLOCKING — A1): build the dynamic model with `confidence={"title":0.9}`, assert `model_dump(exclude_none=True)` does NOT contain a `confidence`-as-private-attr drop, then assert the documented post-dump attach yields `metadata_dict["_confidence"] == {"title":0.9}`; ALSO assert that naming the field `confidence` (no leading underscore) is the chosen design (a `_confidence`-named Pydantic field would be excluded as a private attr).
    - `test_111_exclude_none_nonregression.py`: build the model with `author=None`, assert `model_dump(exclude_none=True)` has NO `author` key (dropped, never `""`); assert flat fields stay top-level.
    - `test_111_window_sampling.py`: `sample_for_extraction(text, cap)` returns full text when len ≤ cap; returns head(70%)+elision+tail(30%) when len > cap; the elision marker `[...document body elided for metadata extraction...]` is present; tail contains the document's final chars.
    - `test_111_extraction_model_resolve.py`: resolve helper returns `app_settings.extraction_model` when set; falls back to `settings.llm_model` (gpt-4o env default) when unset/empty — NOT `app_settings.llm_model`.
    - `test_111_lmstudio_provider.py`: `"lmstudio" in config._PROVIDER_BASE_URLS`; after `resolve_llm_provider("lmstudio")` the base_url == the configured lmstudio_base_url with NO extra `/v1` appended, and the key is the dummy `lm-studio`.

    INTEGRATION files (live-DB :54322 — backend/tests/integration/ — copy the test_110 header verbatim):
    - `test_111_metadata_fields_crud.py`: create via the CRUD endpoint forces `user_id=caller` + `is_global=false`; list returns own+global enabled. (xfail until Plan 03.)
    - `test_111_field_def_scoping.py`: seed user A private + a global metadata_field_definitions row; the app-code `.or_(user_id.eq.{B},is_global.eq.true)` read returns only B-own + global, NOT A's private (2-user cross-leak negative). (xfail until Plan 02.)
    - `test_111_settings_readback.py`: after migration 072 (Plan 05) the 3 new columns read back through `_build_settings_from_row` with the right defaults. (xfail until Plan 05 applies.)
    - `test_111_flat_filter_compat.py`: a documents.metadata row carrying a nested `_confidence` still matches a flat `@>` containment filter on `document_type`. (xfail until Plan 04 produces such a row, OR seed a synthetic row.)
    - `test_111_audit_field_create.py`: model on test_110_dm_audit_live.py — drive `metadata.field.create` via the CRUD create endpoint (or write_audit_entry) then SELECT-back via raw asyncpg. (xfail until Plan 03.)

    Do NOT implement any production behavior in this task — tests only. Every test that asserts unbuilt behavior is `xfail(strict=False)`; the suite MUST exit 0.
  </action>
  <verify>
    <automated>cd backend && venv\Scripts\python -m pytest tests/unit/test_111_dynamic_model.py tests/unit/test_111_confidence_survives_exclude_none.py tests/unit/test_111_exclude_none_nonregression.py tests/unit/test_111_window_sampling.py tests/unit/test_111_extraction_model_resolve.py tests/unit/test_111_lmstudio_provider.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 6 unit files + 5 integration files exist under backend/tests/{unit,integration}/ with the EXACT names in VALIDATION.md
    - `cd backend && venv\Scripts\python -m pytest tests/unit/test_111_*.py -q` exits 0 (xfails/xpasses, NO collection errors, NO hard failures)
    - `cd backend && venv\Scripts\python -m pytest tests/integration/test_111_*.py -q` exits 0 (xfail or skip when :54322 unreachable — never collection-errors)
    - `grep -c "xfail" backend/tests/unit/test_111_confidence_survives_exclude_none.py` returns ≥ 1
  </acceptance_criteria>
  <done>All 11 test_111_* files exist; the unit subset exits 0; integration files skip-or-xfail cleanly; the confidence test encodes the A1 no-underscore design.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 072 (un-applied) — 3 app_settings columns + options jsonb</name>
  <read_first>
    - supabase/migrations/045_app_settings_extraction_aspects.sql (the ADD COLUMN IF NOT EXISTS … idiom, lines 11-18 — PATTERNS.md migration block lines 126-145)
    - supabase/migrations/071_dm_foundations.sql lines 88-104 (metadata_field_definitions DDL — confirm it has NO options column today)
    - .planning/phases/111-metadata-enrichment-extraction-backend/111-RESEARCH.md §Q9 (the exact column set + the enum/options decision A4)
  </read_first>
  <action>
    Create `supabase/migrations/072_app_settings_extraction_model.sql` with EXACTLY (per RESEARCH Q9 — Q4 gap resolved to option (a): enum SHIPS, so the options column is included; this is the planner decision — one additive migration):

    ```sql
    -- Phase 111: configurable metadata extraction model + window cap + enrichment reversibility (META-03)
    -- + enum field_type options carrier on metadata_field_definitions (META-01, Q4 gap (a))
    -- Apply via Supabase SQL editor OR psycopg2-direct to :54322 — NEVER db push/db reset (Plan 05 [BLOCKING]).
    ALTER TABLE public.app_settings
      ADD COLUMN IF NOT EXISTS extraction_model            text DEFAULT NULL,
      ADD COLUMN IF NOT EXISTS extraction_window_cap       integer DEFAULT 32000,
      ADD COLUMN IF NOT EXISTS metadata_enrichment_mode    text DEFAULT 'enriched';

    ALTER TABLE public.metadata_field_definitions
      ADD COLUMN IF NOT EXISTS options jsonb DEFAULT NULL;
    ```

    Idempotent ALTER … IF NOT EXISTS (re-runnable, mirroring 045). Do NOT apply it — this task authors the FILE only; Plan 05 crosses it into the live DB + regenerates full-schema.sql. Do NOT touch supabase/full-schema.sql in this task.
  </action>
  <verify>
    <automated>cd backend && python -c "import pathlib,sys; t=pathlib.Path('../supabase/migrations/072_app_settings_extraction_model.sql').read_text(); sys.exit(0 if all(s in t for s in ['extraction_model','extraction_window_cap','metadata_enrichment_mode','ADD COLUMN IF NOT EXISTS options jsonb']) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/072_app_settings_extraction_model.sql` exists
    - `grep "extraction_model" supabase/migrations/072_app_settings_extraction_model.sql` matches; same for extraction_window_cap (integer DEFAULT 32000), metadata_enrichment_mode (text DEFAULT 'enriched'), and `options jsonb DEFAULT NULL`
    - File uses `ADD COLUMN IF NOT EXISTS` (idempotent)
    - `supabase/full-schema.sql` is NOT modified in this plan (git diff shows no change to it)
  </acceptance_criteria>
  <done>Migration 072 SQL file exists with all 4 ADD COLUMN IF NOT EXISTS statements; not applied; full-schema untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Register the lmstudio provider + add the 3 app_settings-backed settings fields</name>
  <read_first>
    - backend/app/config.py lines 10-20 (_PROVIDER_BASE_URLS — ollama="" line), and resolve_llm_provider's key_map + base_url branch around lines 720-755 (the ollama key + `/v1` append branch — PATTERNS.md lines 343-384)
    - backend/app/config.py around line 691 (ollama_base_url env field — the env-field shape to mirror)
    - backend/app/models/user_settings.py lines 437-518 (_build_settings_from_row — the document_management_enabled DMF-03 env_attr=None line at :478-480 + the extraction_* block at :489-496) and the UserEffectiveSettings field declarations (the extraction_* fields)
    - backend/.env.example around line 103-104 (the OLLAMA_BASE_URL line to add LMSTUDIO_BASE_URL next to)
  </read_first>
  <action>
    Three additive edits. None modify a shared/hot path; all mirror live precedents verbatim.

    (a) config.py — register lmstudio (PATTERNS.md §config.py, RESEARCH Q1):
      - In `_PROVIDER_BASE_URLS` (line 10-20) add: `"lmstudio": "",  # resolved dynamically from lmstudio_base_url`
      - Add an env field on Settings next to ollama_base_url: `lmstudio_base_url: str = "http://localhost:1234/v1"` (env `LMSTUDIO_BASE_URL`).
      - In `resolve_llm_provider`'s `key_map` add `"lmstudio": "lm-studio"` (dummy key, like ollama's keyless `"ollama"`).
      - Add a base_url branch — CRITICAL: LM Studio's URL ALREADY includes `/v1`, so NO append (unlike ollama which appends `/v1`):
        ```python
        elif provider == "lmstudio":
            self.llm_base_url = self.lmstudio_base_url.rstrip("/")   # URL already includes /v1 — NO append
        ```
      Do NOT add lmstudio to `_NATIVE_TOOL_PROVIDERS` — a registry-miss model infers to the ollama fallback bucket (`_INFERENCE_FALLBACK_PROVIDER`) → forced_emission=False, native_tools=False → TIER-COERCE by construction (the desired safe local default). Membership validation flows through `_PROVIDER_BASE_URLS.keys()` automatically, so the dict entry is the only registration the unknown-provider guard needs.

    (b) user_settings.py — add the 3 fields (PATTERNS.md §user_settings, Shared Pattern D):
      - Add 3 field declarations on the `UserEffectiveSettings` model alongside the existing `extraction_*` fields:
        `extraction_model: str = ""`, `extraction_window_cap: int = 32000`, `metadata_enrichment_mode: str = "enriched"`.
      - Add 3 lines to `_build_settings_from_row` (env_attr=None → app_settings-only, the DMF-03 precedent at :480):
        ```python
        extraction_model=str(_val(row, "extraction_model", None, "")),
        extraction_window_cap=int(_val(row, "extraction_window_cap", None, 32000)),
        metadata_enrichment_mode=str(_val(row, "metadata_enrichment_mode", None, "enriched")),
        ```
      Do NOT add these to `SettingsUpdate` — DB-only, no UI (D-111-2; mirrors extraction_* / harness_judge_model).

    (c) backend/.env.example — add `LMSTUDIO_BASE_URL=http://localhost:1234/v1` next to the OLLAMA_BASE_URL line (non-secret).

    Then un-xfail (flip to real assertions) `test_111_lmstudio_provider.py` since its behavior now exists. (Leave the settings-readback integration test xfail — that depends on Plan 05's live apply.)
  </action>
  <verify>
    <automated>cd backend && venv\Scripts\python -m pytest tests/unit/test_111_lmstudio_provider.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "lmstudio" backend/app/config.py` matches in _PROVIDER_BASE_URLS, key_map, the base_url branch, and the lmstudio_base_url env field
    - The lmstudio base_url branch does NOT append `/v1` (the URL already carries it) — confirm `rstrip("/")` with no `/v1` concat
    - `grep "metadata_enrichment_mode" backend/app/models/user_settings.py` matches both a field declaration AND a `_build_settings_from_row` line with `None` as the env_attr arg
    - `grep "LMSTUDIO_BASE_URL" backend/.env.example` matches
    - `cd backend && venv\Scripts\python -m pytest tests/unit/test_111_lmstudio_provider.py -x -q` exits 0 (now GREEN, no longer xfail)
    - `grep -c "extraction_model" backend/app/models/user_settings.py` ≥ 2 (declaration + readback line); these fields NOT present in SettingsUpdate
  </acceptance_criteria>
  <done>lmstudio resolves via config with no /v1 double-append; the 3 settings fields declare + read back app_settings-only; lmstudio provider unit test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → app_settings | The extraction_model / window_cap / enrichment_mode are admin-set DB values (no UI, no end-user write path) |
| backend → LM Studio endpoint | The lmstudio base_url is an admin-set env (LMSTUDIO_BASE_URL), not user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01-01 | Tampering / Info Disclosure | LM Studio endpoint as an SSRF/exfil vector | mitigate | `LMSTUDIO_BASE_URL` is an admin-set env, NOT user-controlled — identical trust model to the existing `OLLAMA_BASE_URL`. No user input reaches the base_url. Verify: the env field defaults to localhost and is read only from Settings, never from a request body. |
| T-111-01-02 | Elevation of Privilege | extraction_model / mode settings exposed to non-admins | accept (mitigated by design) | The 3 fields are DB-only `app_settings`, deliberately NOT in `SettingsUpdate` — no end-user write path exists (the harness_judge_model / extraction_* precedent). RLS on `app_settings` already governs admin-only write. No new surface. |
| T-111-01-03 | Tampering | metadata_enrichment_mode accepting an out-of-vocabulary value silently routing nowhere | mitigate | `_build_settings_from_row` defaults to `'enriched'` on missing/null; the engine (Plan 02) treats any non-`'legacy'` value as `'enriched'` (fail-safe to the default-on path), never an unrouted branch. Verify: settings-readback integration test (Plan 05) asserts the default. |
| T-111-01-04 | Denial of Service | a partial migration apply leaving app_settings half-altered | mitigate | Migration 072 uses idempotent `ADD COLUMN IF NOT EXISTS` (re-runnable); the apply itself + read-back is the Plan 05 [BLOCKING] gate, not this plan. This plan only authors the FILE. |
</threat_model>

<verification>
- `cd backend && venv\Scripts\python -m pytest tests/unit/test_111_*.py -q` exits 0 (all scaffolds collect; lmstudio test GREEN; rest xfail).
- Migration 072 file exists and contains all 4 ADD COLUMN statements; full-schema.sql untouched.
- `grep "lmstudio"` and `grep "metadata_enrichment_mode"` hit config.py + user_settings.py respectively.
- SEED-056 net-new proof: this plan adds only test files + additive config/model lines + a migration file — run `cd backend && venv\Scripts\python -m pytest tests/unit -q` and confirm net-new failures = 0 vs base (the new xfail/GREEN test_111 files are the only delta).
</verification>

<success_criteria>
- All 11 Wave-0 test_111_* files exist; unit subset exits 0; the A1 confidence test encodes the no-underscore `confidence`→`_confidence` design.
- Migration 072 SQL file authored (un-applied), idempotent, with the 3 app_settings columns + options jsonb.
- lmstudio is a first-class provider (no Ollama-impersonation needed); resolves base_url with no /v1 double-append.
- extraction_model / extraction_window_cap / metadata_enrichment_mode read back off app_settings via _build_settings_from_row (env_attr=None), absent from SettingsUpdate.
- META-03 (un-pin, settings half) foundation in place; Plans 02-04 consume it.
</success_criteria>

<output>
After completion, create `.planning/phases/111-metadata-enrichment-extraction-backend/111-01-SUMMARY.md`
</output>
