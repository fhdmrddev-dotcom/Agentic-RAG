---
phase: 254-independent-review-of-249-253
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agent-bus/OPEN.md
autonomous: true
requirements: [DEBT-06]
user_setup: []

must_haves:
  truths:
    - "Each of the five review asks states a date by which it must be answered, and what happens if it is not"
    - "Each of the five asks states where it sits in the risk rank, so a reviewer with capacity for one knows which one"
    - "Gemini's mailbox still works after the edit — list, count and the SessionStart hook all behave exactly as before"
    - "No new bus item exists; the queue is still nine to:gemini items"
    - "Claude opened, answered, closed and archived nothing"
  artifacts:
    - path: ".agent-bus/OPEN.md"
      provides: "the five amended item bodies (BUS-249, BUS-250, BUS-251, BUS-256, BUS-257)"
      contains: "2026-09-24"
  key_links:
    - from: ".agent-bus/OPEN.md"
      to: "scripts/agent-bus.sh list --to gemini"
      via: "the ### header line, untouched"
      pattern: "^### \\[OPEN\\] BUS-(249|250|251|256|257) · to:gemini · from:claude · 2026-09-16$"
    - from: ".agent-bus/OPEN.md"
      to: "scripts/agent-bus.sh answer"
      via: "the bare **Answer:** line, untouched"
      pattern: "^\\*\\*Answer:\\*\\*$"
---

<objective>
The five §6.3 review asks for phases 249-253 are already filed and unanswered. This plan gives each of
them the two things they lack: a **date** (2026-09-24) after which the phase closes on a written
refusal instead, and a **rank** (`251 → 253 → 252 → 249 → 250`) so a reviewer with capacity for one
item knows which one.

Purpose: D-04 + D-06 + D-07. A deferral with no deadline is a deletion that looks like a decision, and
a nine-item queue with no rank is answered in filing order rather than risk order.
Output: five amended item BODIES in `.agent-bus/OPEN.md`. ⛔ Zero new items, zero answers, zero closes.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/254-independent-review-of-249-253/254-CONTEXT.md
@.planning/phases/254-independent-review-of-249-253/254-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- Everything the executor needs about the bus. Do not go exploring. -->

`scripts/agent-bus.sh` dispatch (`:166-173`) is exactly: `open | list | answer | close | archive`.
⛔ **There is no `amend` verb.** An amend is a hand `Edit` of the item BODY, and it is legal only
because `.agent-bus/OPEN.md:4-5` restricts its prohibition to the `###` header lines.

Item anchors, measured 2026-09-17 (re-derive with `grep -n '^### \[OPEN\]' .agent-bus/OPEN.md` —
line numbers shift as you edit, so locate each item by its id, never by its line):

| rank | phase | bus id | header line at plan time |
|---|---|---|---|
| 1 | 251 Register Integrity | `BUS-249` | `:3051` |
| 2 | 253 Bootstrap artifact  | `BUS-257` | `:3099` |
| 3 | 252 v4.2 audit gaps     | `BUS-256` | `:3093` |
| 4 | 249 The model you run   | `BUS-251` | `:3063` |
| 5 | 250 Run honesty         | `BUS-250` | `:3057` |

⛔ **`BUS-252`, `BUS-253`, `BUS-254`, `BUS-255` are the OLDER owed arm (245/241/242/244) and are OUT
OF SCOPE by D-01.** Touch none of them. `BUS-246` / `BUS-248` are `to:operator` — likewise untouched.

Baseline captured 2026-09-17 on a clean tree — these are the numbers the fences compare against, and
the executor MUST re-capture them itself before editing rather than inherit them:
```
grep -c '^\*\*Answer:\*\*$' .agent-bus/OPEN.md          -> 20
grep -cE '^### \[OPEN\].*to:gemini' .agent-bus/OPEN.md   -> 9
grep -cE '^### \[OPEN\].*to:operator' .agent-bus/OPEN.md -> 2
grep -cE '^### \[OPEN\].*to:claude' .agent-bus/OPEN.md   -> 0
md5sum .agent-bus/OPEN.md                                -> b0c01746eea5cdb174a77bdc89682a11
```

Body shape to imitate — `BUS-249`'s own (`:3053`) is the richest in the file and is the section order
CONTEXT's discretion block points at: a `⛔ WHY IT IS YOU` opener, `ARTIFACTS:`, numbered
`HIGHEST-VALUE TARGETS`, `⛔ BOUNDARIES THAT WERE DELIBERATE, so you do not raise them as defects`,
then `RECORD:`. ⭐ The **boundaries** section is what stops a reviewer re-litigating a decision.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the bus baseline, then amend the two highest-ranked asks (BUS-249 / 251 and BUS-257 / 253)</name>
  <files>.agent-bus/OPEN.md</files>
  <read_first>
    - `.agent-bus/OPEN.md` — read lines 1-10 (the hand-edit prohibition) and the full bodies of
      `BUS-249` and `BUS-257`. Locate each by id with `grep -n`, never by the line numbers above.
    - `scripts/agent-bus.sh` — `:110-175` (`cmd_answer`, `cmd_close`, the dispatch). Read it so the
      two invariants below are understood rather than obeyed blindly.
    - `.planning/phases/254-independent-review-of-249-253/254-CONTEXT.md` — D-04, D-06, D-07.
  </read_first>
  <action>
    FIRST, before any edit, write the baseline into a scratch file OUTSIDE the repo
    (`BASE="${TMPDIR:-${TMP:-/tmp}}/254-01"; mkdir -p "$BASE"`): the four `grep -c` counts above, the
    full list of `^### \[OPEN\]` header lines, and `md5sum`. These are what Task 3 compares against.

    Then APPEND a deadline-and-rank block to the END of the BODY of `BUS-249` and `BUS-257` —
    immediately BEFORE the bare `**Answer:**` line of each item, separated by a blank line. Do not
    restructure or rewrite what is already there; D-06 is an amend, not a re-file.

    Each appended block carries, in this order:
    (a) `⏳ AMENDED 2026-09-17 by Phase 254 — this ask now has a DEADLINE and a RANK.` and states
        that the item itself was NOT re-filed and no duplicate exists.
    (b) The deadline, as the literal string `2026-09-24`, and what happens at it, stated plainly:
        the phase closes on a **written refusal** drafted by claude and ruled on by the operator
        (per D-04 / D-05), naming the decider and the reason — which is the door `DEBT-06`'s own
        wording offers. ⭐ State that a refusal is **not a pass** and **not a criticism of the
        reviewer**: it is the record that the debt stopped being silent.
      ⭐ State the two-door close explicitly so answering after the date is still worth doing: if this
        ask is answered at any time, the verdict artifact is `<phase>-REVIEW-IND.md` in the REVIEWED
        phase's own directory and the drafted refusal is void.
    (c) The rank, as the literal string `251 → 253 → 252 → 249 → 250`, with THIS item's position named
        (`rank 1 of 5` for BUS-249, `rank 2 of 5` for BUS-257) and the one-line reason from D-07:
        251 because it is the only one of the five with **no review file of any kind**; 253 because it
        is security-bearing (credential boundary / greenfield ACLs) and `AGENTS.md` §3.1's critical
        tests apply to it.
    (d) `⛔ BOUNDARIES THAT WERE DELIBERATE` — for BUS-257 add the ones the existing body lacks and
        that a reviewer would otherwise raise as defects: `status: gaps_found` in `253-VERIFICATION.md`
        sitting beside a ROADMAP Progress row reading COMPLETE is the gap-closure round landing after
        the verification and **neither register refutes the other** (M-4); the registers disagree on
        whether 253 is the FIFTH or SIXTH consecutive self-verified close and that disagreement is
        left visible on purpose; `schema-acl-parity.yml` has never executed and the phase says so
        itself. For BUS-249 the existing body already has a boundaries section — do not duplicate it,
        reference it.
    (e) `⛔ NOTHING IS BEING ASKED OF YOU THAT CLAUDE COULD DO ITSELF.` One sentence: a claude pass
        over a claude build is a self-assessment under §6.3 and is labelled `review_type: self-assessed`;
        it improves quality and discharges nothing. For BUS-249 name that such a floor pass is being
        run on 251 in this same phase and lands at `251-REVIEW.md` — so the reviewer knows a file will
        be there and knows it is not a peer review.

    ⛔ THREE MECHANICAL INVARIANTS, each load-bearing:
    1. NEVER touch the `### [OPEN] BUS-NNN · to:x · from:y · YYYY-MM-DD` line. `cmd_close` (`:145-160`)
       matches `^### \[(OPEN|ANSWERED)\] $id ` and dies on a malformed header; `cmd_list` and
       `.claude/hooks/agent-bus-check.sh` parse `to:` / `from:` / the ISO date out of it by regex.
    2. Leave the trailing `**Answer:**` line EXACTLY as-is — one per item, nothing after the colon,
       nothing appended to that line. `cmd_answer` (`:120-125`) requires `$0 == "**Answer:**"` exactly;
       appending to it makes the item permanently unanswerable by the script (exit 3, "already
       answered?").
    3. ⛔ Do NOT run `agent-bus.sh open`, `answer`, `close` or `archive`. `close` is the operator's
       verb (`REG-03` / D-05).
  </action>
  <verify>
    <automated>bash -c 'B="${TMPDIR:-${TMP:-/tmp}}/254-01"; for id in BUS-249 BUS-257; do awk -v id="$id" "/^### \\[OPEN\\] $id /{f=1} f&&/^\\*\\*Answer:\\*\\*\$/{exit} f" .agent-bus/OPEN.md | grep -q "2026-09-24" || { echo "FAIL $id missing deadline"; exit 1; }; awk -v id="$id" "/^### \\[OPEN\\] $id /{f=1} f&&/^\\*\\*Answer:\\*\\*\$/{exit} f" .agent-bus/OPEN.md | grep -q "251 → 253 → 252 → 249 → 250" || { echo "FAIL $id missing rank"; exit 1; }; done; diff <(grep "^### \[OPEN\]" .agent-bus/OPEN.md) "$B/headers.txt" && echo HEADERS-IDENTICAL'</automated>
  </verify>
  <acceptance_criteria>
    - The body of `BUS-249` (between its header and its `**Answer:**` line) contains the literal
      `2026-09-24` and the literal `251 → 253 → 252 → 249 → 250`. Same for `BUS-257`.
    - `diff` of `grep '^### \[OPEN\]' .agent-bus/OPEN.md` against the baseline capture is EMPTY —
      not one header line added, removed or altered.
    - `grep -c '^\*\*Answer:\*\*$' .agent-bus/OPEN.md` still reads the baseline value (20).
    - `git diff -U0 .agent-bus/OPEN.md | grep -c '^[-+]### '` is `0`.
    - `git diff --stat .agent-bus/OPEN.md` shows `0` deletions — this is an append-only amend.
    - `BUS-252`, `BUS-253`, `BUS-254`, `BUS-255`, `BUS-246`, `BUS-248` are byte-unchanged:
      `git diff -U0 .agent-bus/OPEN.md` contains no added line inside any of their bodies.
  </acceptance_criteria>
  <done>BUS-249 and BUS-257 each carry a dated, ranked, boundaried amendment; every header line and every `**Answer:**` line in the file is byte-identical to the baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the remaining three asks (BUS-256 / 252, BUS-251 / 249, BUS-250 / 250)</name>
  <files>.agent-bus/OPEN.md</files>
  <read_first>
    - `.agent-bus/OPEN.md` — the bodies of `BUS-256`, `BUS-251`, `BUS-250`, located by id.
    - `.planning/phases/249-the-model-you-actually-run/249-VERIFICATION.md` frontmatter (`:1-12`) —
      the `independent_review_waiver` string is the reason 249 is ranked 4th rather than 1st.
    - `.planning/phases/250-run-honesty-the-residue/250-VERIFICATION.md` frontmatter (`:1-14`) —
      its `owed:` list is a 5-entry list and the 4th entry names DEBT-06.
    - `.planning/phases/254-independent-review-of-249-253/254-CONTEXT.md` — D-07 and the `<deferred>`
      block (`BUG-260916-01`).
  </read_first>
  <action>
    Same append-before-`**Answer:**` amendment as Task 1, with the same (a)-(e) block structure and the
    same three mechanical invariants, for `BUS-256` (rank 3 of 5), `BUS-251` (rank 4 of 5) and
    `BUS-250` (rank 5 of 5).

    The per-item rank reason, from D-07:
    - `BUS-256` / 252 — security-bearing (privilege model, greenfield ACLs); §3.1's critical tests apply.
    - `BUS-251` / 249 and `BUS-250` / 250 — each already carries a claude code-review pass, so what is
      missing is **independence**, not a first reading. Say that plainly: it is why they rank last, and
      it is not a statement that they are safe.

    Two per-item facts to carry, because a reviewer cannot be expected to find them:
    - On `BUS-251` (249): name `BUG-260916-01` (`llm-call-timeout above 600s is silently ineffective`,
      `affected_areas: backend/provider-routing, admin/model-registry, local-models`) as an **INPUT to
      the review** — an open, operator-reported defect squarely in this phase's reviewed surface.
      ⛔ State that Phase 254 fixes nothing and is not asking for a fix: it is evidence the reviewer
      should weigh, per D-11 and the `<deferred>` block.
    - On `BUS-250` (250): name that `250-VERIFICATION.md` carries `reviewer: null` and a five-entry
      `owed:` list whose fourth entry reads that the independent review was **waived by instruction,
      not satisfied** — so the ask is the operator's own recorded intention catching up, not a
      second-guess of the close.

    ⛔ Do NOT add a `RECORD:` instruction that tells the reviewer to flip `verification_mode`. The
    existing `BUS-249` body already says "flip `verification_mode` only if it earns it"; leave that
    wording where it is and do not extend it to the other four — what 254 asks for is a verdict
    artifact, and the register move is Plan 254-04's and the operator's.
  </action>
  <verify>
    <automated>bash -c 'for id in BUS-250 BUS-251 BUS-256; do b=$(awk -v id="$id" "/^### \\[OPEN\\] $id /{f=1} f&&/^\\*\\*Answer:\\*\\*\$/{exit} f" .agent-bus/OPEN.md); echo "$b" | grep -q "2026-09-24" && echo "$b" | grep -q "251 → 253 → 252 → 249 → 250" || { echo "FAIL $id"; exit 1; }; done; echo "$(awk "/^### \\[OPEN\\] BUS-251 /{f=1} f&&/^\\*\\*Answer:\\*\\*\$/{exit} f" .agent-bus/OPEN.md)" | grep -q "BUG-260916-01" && echo ALL-FIVE-AMENDED'</automated>
  </verify>
  <acceptance_criteria>
    - All five bodies (`BUS-249`, `BUS-250`, `BUS-251`, `BUS-256`, `BUS-257`) contain the literal
      `2026-09-24` and the literal `251 → 253 → 252 → 249 → 250`.
    - `BUS-251`'s body contains the literal `BUG-260916-01` and a sentence stating Phase 254 fixes
      nothing — assert the rendered sentence, not merely the id.
    - `BUS-250`'s body contains the literal `reviewer: null`.
    - `grep -c '^\*\*Answer:\*\*$' .agent-bus/OPEN.md` still reads the baseline value.
    - `git diff --numstat .agent-bus/OPEN.md` reports `0` in the deletions column.
  </acceptance_criteria>
  <done>All five in-scope asks carry the deadline, the rank, their own rank reason and their own boundary set; the four out-of-scope `to:gemini` asks and both `to:operator` asks are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Drive the mailbox after the edit — prove the bus still works, and prove claude wrote no item</name>
  <files>.agent-bus/OPEN.md</files>
  <read_first>
    - `scripts/agent-bus.sh` — `:110-175`.
    - `.claude/hooks/agent-bus-check.sh` — `:70-90` (the `awk` that prints the first non-empty body
      line of each `to:claude` item).
    - the baseline capture written by Task 1 at `${TMPDIR:-${TMP:-/tmp}}/254-01/`.
  </read_first>
  <action>
    Run the bus's own consumers against the edited file and compare every reading to the Task 1
    baseline. ⚠ Capture each exit code WITHOUT a pipe — `… | tail` returns tail's status, not the
    gate's, and that exact mistake published a wrong backend baseline in this repo.

    Run, and record each verbatim in the SUMMARY:
    1. `bash scripts/agent-bus.sh list` and `bash scripts/agent-bus.sh list --to gemini` — exit code
       and the item count each reports.
    2. `bash .claude/hooks/agent-bus-check.sh` — exit code and output (it should stay silent: there
       are 0 `to:claude` items).
    3. The four `grep -c` counts, diffed against the baseline.
    4. `git diff -U0 .agent-bus/OPEN.md | grep -c '^[-+]### '` and `git diff --numstat .agent-bus/OPEN.md`.

    ⭐ THEN DRIVE THE ONE INVARIANT THAT MATTERS AND CANNOT BE PROVEN BY READING — that `cmd_answer`
    still recognises each amended item. Do it WITHOUT writing to the real bus: copy
    `.agent-bus/OPEN.md` to the scratch dir, run the `answer` path against the COPY by pointing the
    script at it (`BUS_FILE`/`OPEN_FILE` env var if the script exposes one — read `:1-40` to find the
    path variable; if it is hard-coded, reproduce `cmd_answer`'s awk condition `$0 == "**Answer:**"`
    against the copy instead and state in the SUMMARY that the script itself was not driven and why).
    Assert the answer path resolves for all five ids and exits 0 on the copy, then DELETE the copy.
    ⛔ The real `.agent-bus/OPEN.md` must not be written by any script invocation.

    Record in the SUMMARY the honest accounting line this phase reuses everywhere:
    **bus items opened, answered, closed or archived by claude: 0 · bus item bodies amended: 5.**
    ⛔ Do NOT write `bus_writes: 0` — this plan DID write to the file, and copying `251-BUS-TRIAGE.md`'s
    phrase verbatim here would be a false claim. The honest assertion is the header-diff of `0`.
  </action>
  <verify>
    <automated>bash -c 'bash scripts/agent-bus.sh list --to gemini >/dev/null; echo "list-exit:$?"; bash .claude/hooks/agent-bus-check.sh >/dev/null; echo "hook-exit:$?"; echo "gemini:$(grep -cE "^### \[OPEN\].*to:gemini" .agent-bus/OPEN.md) operator:$(grep -cE "^### \[OPEN\].*to:operator" .agent-bus/OPEN.md) claude:$(grep -cE "^### \[OPEN\].*to:claude" .agent-bus/OPEN.md) answers:$(grep -c "^\*\*Answer:\*\*$" .agent-bus/OPEN.md) hdrdiff:$(git diff -U0 .agent-bus/OPEN.md | grep -c "^[-+]### ")"'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/agent-bus.sh list --to gemini` exits `0` and reports `9` items — the same count as
      the baseline. An amend that changed the queue size would mean a duplicate ask was filed.
    - `bash .claude/hooks/agent-bus-check.sh` exits `0`.
    - `to:gemini` = 9, `to:operator` = 2, `to:claude` = 0, bare `**Answer:**` lines = 20 — every one
      identical to the Task 1 baseline.
    - `git diff -U0 .agent-bus/OPEN.md | grep -c '^[-+]### '` is `0`.
    - `git diff --numstat .agent-bus/OPEN.md` deletions column is `0`.
    - The answer path resolves for all five ids against the scratch COPY, and the copy is deleted;
      OR the SUMMARY states the script was not driven and gives the reason.
    - The SUMMARY records the accounting line verbatim: `bus items opened, answered, closed or
      archived by claude: 0 · bus item bodies amended: 5`.
  </acceptance_criteria>
  <done>Every consumer of the bus file behaves identically before and after the amend, the answer path still resolves for all five ids, and the accounting line is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claude → `.agent-bus/OPEN.md` | the only channel between two agents that cannot call each other; a corrupted file silently stops coordination rather than failing loudly |
| claude → the operator's verbs | `close` (and `answer` on another agent's behalf) belong to the operator under `REG-03` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-254-01 | Tampering | `### [OPEN] BUS-NNN` header lines | mitigate | `git diff -U0 \| grep -c '^[-+]### '` must be `0`, asserted on every task; `cmd_close` dies on a malformed header |
| T-254-02 | Denial of service | the bare `**Answer:**` line | mitigate | `grep -c '^\*\*Answer:\*\*$'` fenced against a pre-captured baseline; the answer path driven against a scratch COPY. Appending to that line makes an item permanently unanswerable |
| T-254-03 | Elevation of privilege | `agent-bus.sh close` / `answer` | mitigate | no task invokes either against the real file; `git diff --numstat` deletions column `0` proves no status flip |
| T-254-04 | Repudiation | a duplicate ask making a 9-item queue a 14-item queue | mitigate | `list --to gemini` must report `9`, the pre-captured baseline; D-06 is amend-in-place |
| T-254-05 | Spoofing | an amendment implying claude may act as the §6.3 reviewer | mitigate | block (e) states a claude pass is `review_type: self-assessed` and discharges nothing |
| T-254-SC | Tampering | npm/pip/cargo installs | accept | this plan runs **zero** package-manager installs; no `RESEARCH.md` legitimacy table is required and none is cited |
</threat_model>

<verification>
- `bash scripts/agent-bus.sh list --to gemini` → exit 0, 9 items (baseline).
- `bash .claude/hooks/agent-bus-check.sh` → exit 0, silent.
- Header lines byte-identical to baseline; `**Answer:**` count 20; deletions `0`.
- Each of the five bodies carries `2026-09-24` and `251 → 253 → 252 → 249 → 250`.
</verification>

<success_criteria>
Five asks, five deadlines, five ranks, one queue of nine, zero items opened / answered / closed /
archived by claude, and a mailbox that behaves byte-for-byte as it did before the edit.
</success_criteria>

<output>
Create `.planning/phases/254-independent-review-of-249-253/254-01-SUMMARY.md` when done.
</output>
