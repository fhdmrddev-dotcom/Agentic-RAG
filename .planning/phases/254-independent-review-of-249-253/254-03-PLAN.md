---
phase: 254-independent-review-of-249-253
plan: 03
type: execute
wave: 2
depends_on: ["254-02"]
files_modified:
  - .planning/phases/249-the-model-you-actually-run/249-REVIEW-REFUSAL.md
  - .planning/phases/250-run-honesty-the-residue/250-REVIEW-REFUSAL.md
  - .planning/phases/251-register-integrity/251-REVIEW-REFUSAL.md
  - .planning/phases/252-close-the-v42-audit-gaps/252-REVIEW-REFUSAL.md
  - .planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW-REFUSAL.md
autonomous: true
requirements: [DEBT-06]
user_setup: []

must_haves:
  truths:
    - "Each of the five phases has a written document naming who would decide the refusal, why, and on what evidence"
    - "Not one of the five drafts claims the phase was reviewed, or flips any register"
    - "Each draft names the one thing a reviewer would have tested that a measurement cannot"
    - "Each draft voids itself if the bus ask is answered — the door stays open after the deadline"
    - "All five state the SAME reading of the two registers that disagree, so 254 does not re-create the conflict"
  artifacts:
    - path: ".planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW-REFUSAL.md"
      provides: "the weakest-case refusal draft — security-bearing, and it says so"
      contains: "status: draft-pending-operator-ruling"
    - path: ".planning/phases/251-register-integrity/251-REVIEW-REFUSAL.md"
      provides: "the draft that cites the 254-02 floor pass and states it discharges nothing"
      contains: "251-REVIEW.md"
  key_links:
    - from: "each *-REVIEW-REFUSAL.md"
      to: ".planning/DEBT-06-REFUSALS.md"
      via: "the 2026-09-14 operator-ruled format and its mechanical-evidence-vs-judgement test"
      pattern: "kind:[ ]*recorded-decision-not-review"
    - from: "each *-REVIEW-REFUSAL.md"
      to: ".agent-bus/OPEN.md"
      via: "the bus item the draft is void against"
      pattern: "BUS-(249|250|251|256|257)"
---

<objective>
Five drafted refusals — one per phase, in the reviewed phase's own directory — each naming who would
decide, why, on what evidence, what risk is being accepted, and what re-opens it.

Purpose: D-04 gives the phase a completion condition it controls. The five asks were filed 2026-09-16
and none has moved; Gemini cannot be driven from here (M-5). ⛔ **The alternative is not "wait" — it is
a phase with no completion condition it owns.**
Output: five `<phase>-REVIEW-REFUSAL.md` files. ⛔ Existing `*-REVIEW.md` / `*-REVIEW-R2.md` files are
not touched (D-09).

⛔ **A DRAFT IS NOT A REFUSAL AND A REFUSAL IS NOT A PASS.** Claude prepares; only the operator's
ruling makes it real (D-05). A refusal written by the builder about its own work is exactly the
self-assessment §6.3 exists to prevent — so every file states its own status as `draft`, names the
operator as the decider it is waiting on, and flips nothing.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/254-independent-review-of-249-253/254-CONTEXT.md
@.planning/phases/254-independent-review-of-249-253/254-PATTERNS.md
@.planning/DEBT-06-REFUSALS.md
@.planning/DEBT-06-AUDIT.md

<interfaces>
<!-- The format, the test, the adopted reading, and the per-phase evidence anchors. -->

**The format analog** is `.planning/DEBT-06-REFUSALS.md` (183 lines, `decided_by: operator`,
2026-09-14). ⚠ It is **one** file holding five phase sections; D-09 wants one file per phase — so lift
the SECTION shape as the file body and scope the frontmatter block (`:1-9`) to a single phase.

**Frontmatter key set** (from `DEBT-06-REFUSALS.md:1-9`, scoped down and with four keys added):
`type: debt-refusal-draft` · `requirement: DEBT-06` · `phase: <NNN-slug>` · `written: 2026-09-17` ·
`author: claude (the BUILDER)` · `decided_by: pending — operator (D-05)` ·
`kind: recorded-decision-not-review` · `review_type: self-assessed` ·
`status: draft-pending-operator-ruling` · `effective_from: 2026-09-24` · `bus_item: BUS-NNN` ·
`independent_review_after_ruling: refused` (⛔ the value it would take *if ruled*, never written into
the phase's own `*-VERIFICATION.md` by this plan).

⭐ **`review_type: self-assessed` is THE token for this whole phase** and appears exactly once in every
artifact 254 writes. ⛔ Nothing 254 writes may contain the string `review_type: independent`.

**Body section order** (from `DEBT-06-REFUSALS.md:45-67`):
```
# Phase NNN — <name> · **DRAFT REFUSAL — <strongest|weakest> case of the five**
| | |                                  <- a 2-column facts table: dispatched review(s), trust boundary,
|---|---|                                  verification_mode, bus item, what the reviewer would have tested
## The test this refusal applies         <- quoted, not reinvented
## ADOPTED READING                       <- identical across all five, see below
## The evidence standing in for a review <- mechanical, re-runnable, named by file and command
## What a reviewer would have tested that this cannot
**Residual risk accepted:** <low | low-moderate | moderate | HIGH>, with the reason
**Re-open trigger:**
## ⛔ What this draft does not do
```

**The test, quoted rather than reinvented** (`DEBT-06-REFUSALS.md:24-38`):
> *"how much of this phase's claim rests on mechanical evidence a third party can re-run, versus on a
> judgement only a reviewer could test?"*
and its ground, from the v4.0 close:
> *"What a self-verification does NOT weaken is the mechanical evidence — a hash, a byte-identical
> file, a driven function. What it weakens is every judgement call about whether an owed item was
> acceptable."*

**⛔ THE ADOPTED READING — identical wording in all five files, because M-8 is a conflict 254 inherits
and must not re-create one milestone on.** `ROADMAP.md:271` records that `DEBT-06-AUDIT.md` calls four
rows *"genuinely unmet"* while `DEBT-06-REFUSALS.md` covers exactly those four, and the audit never
reads it — *"Two registers disagree and the ruling is the operator's."* The reading these five drafts
adopt, stated in one paragraph repeated byte-identically:

> **254 adopts BOTH rules, because they answer different questions.** `DEBT-06-AUDIT.md`'s counting
> rule — *a review file whose frontmatter does not assert `review_type: independent` is a code-review
> pass* — is the test for whether a row was **REVIEWED**. `DEBT-06-REFUSALS.md`'s rule — *the refusal
> is the record, not a discharge* — is the test for whether a row is **ACCOUNTED FOR**. A row can be
> accounted-for and unmet at the same time, and every one of these five drafts produces
> **accounted-for, never reviewed**. ⛔ 254 does not settle which of the two registers governs
> `/gsd:complete-milestone`; that ruling is the operator's and it is `BUS-247`.

**Per-phase evidence anchors** — re-derive each from the named file; do not trust this table alone:

| Phase | bus | mechanical evidence a third party can re-run | the judgement only a reviewer could test |
|---|---|---|---|
| 253 Bootstrap artifact | `BUS-257` | the greenfield harness on a scratch DB; ACL parity `133/133`; `--self-test` 37 arms; two RED drives re-driven by the orchestrator with `full-schema.sql` md5 `a4f570396a44035102567ec1e3b7ff62` restored identical | whether `SEED-290`'s **13 unfixed findings** were correctly dispositioned — notably `WR-05` (`E'…'` defeats BOTH lexers, permissive direction, latent and **unfenced**) and `WR-04`, a **false docstring this phase itself added** which is the stated reason another finding was deferred |
| 252 v4.2 audit gaps | `BUS-256` | `base: 53e2435b7` / `head: 1ee7903f0` in its own frontmatter; identical failing backend node-id set; the parity gate firing for real on run one over three unmirrored function ACLs incl. `resize_embedding_column` | whether the **seven refuted inherited claims** were refuted correctly, and whether the two warnings judged to need no work (W-5, W-9) genuinely needed none |
| 251 Register Integrity | `BUS-249` | `293/293 parsed · 0 duplicate ids`, exit 0; `--self-test` arms; D-11's body md5s (blob-vs-blob); **plus `251-REVIEW.md` from plan 254-02 — cite its verdict triple and finding counts** | the D-07 date rule on the 8 renumbers where `seedDate()` is not total, and D-17's decision to leave product-source references pointing at stubs |
| 249 The model you run | `BUS-251` | `MODEL-09` closed by a fresh 8/8 sweep; the 2 blockers `249-REVIEW.md` found, both fixed and driven | whether `MODEL-09` being *closed by measurement* rather than built was the right call, and whether `BUG-260916-01` (open, in this phase's surface) belongs to it |
| 250 Run honesty | `BUS-250` | `250-MEASUREMENT.md` — the blocking measurement WAS taken and the report's dichotomy was measured FALSE (both arms true of different rows) | the `NOT TICKED` badge wording (claude's call from the operator's vocabulary), the declined G-2 sketch (D-250-12), and the 3 remaining entries of the `owed:` list |

⛔ **252 and 253 are the SECURITY-BEARING pair and their refusals are the WEAKEST of the five — say so
in the file rather than treating all five uniformly.** `AGENTS.md` §3.1's critical tests apply to them
and to no other phase in this set; `DEBT-06-REFUSALS.md` framed its own five as *"strongest case"* /
weaker, and flattening that is how a refusal set stops carrying information.

**The closing disclaimer** (`DEBT-06-REFUSALS.md:174-182`, the shape D-03 needs):
> ⛔ **What this does not do:** it does not flip any `independent_review` field to `done`, and it does
> not claim the phase was reviewed. The phase keeps `verification_mode: self-verified` and
> `independent_review: owed`. **The refusal is the record, not a discharge.**

**The void clause — required in all five, because M-5 means the door must stay open:**
> ⭐ **VOID IF ANSWERED.** If `BUS-NNN` is answered at any time, before or after 2026-09-24, the verdict
> artifact is `<phase>-REVIEW-IND.md` written by the reviewer, this draft is void, and the row is
> `independent_review: done` rather than `refused`. **A deadline is a completion condition, not a
> closed door.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the two security-bearing refusals — 253 and 252 — as the weakest cases of the five</name>
  <files>.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW-REFUSAL.md, .planning/phases/252-close-the-v42-audit-gaps/252-REVIEW-REFUSAL.md</files>
  <read_first>
    - `.planning/DEBT-06-REFUSALS.md` — the whole file (183 lines); it is the format and the precedent.
    - `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-VERIFICATION.md` frontmatter
      and `253-REVIEW-R2.md` frontmatter + its `## 4. What this round did NOT do`.
    - `.planning/phases/252-close-the-v42-audit-gaps/252-VERIFICATION.md` frontmatter (`base:`/`head:`).
    - `.planning/seeds/SEED-290*.md` — the 13 unfixed findings and their re-open triggers.
    - `AGENTS.md` §3.1 (`### Is this phase critical? A test, not a list`).
  </read_first>
  <action>
    Write the two files to the format, body order, frontmatter key set, adopted-reading paragraph,
    void clause and closing disclaimer given in `<interfaces>`.

    Both headline as the **weakest** cases of the five and say why in one sentence: they are the two
    phases `AGENTS.md` §3.1's critical-phase test catches (credential boundary, privilege model,
    greenfield ACLs), and §3.1 is the rule that swaps the seats rather than breaking them. ⭐ A refusal
    on a security-bearing phase is a **higher** residual risk than a refusal on a bookkeeping one, and
    the residual-risk line must reflect that rather than reading `low` out of habit.

    For 253, the facts table must carry — without resolving them, because neither refutes the other:
    - M-4: `253-VERIFICATION.md` reads `status: gaps_found` while its ROADMAP Progress row reads
      **COMPLETE**; the gap-closure round landed after the verification. **Both are defensible.**
    - The registers disagree on whether it is the FIFTH or the SIXTH consecutive self-verified close,
      and that disagreement is left visible on purpose.
    - `schema-acl-parity.yml` has never executed (`gh run list` → HTTP 404, not on `master`), so
      `--self-test`'s only runner has never run — an owed item, named rather than implied.

    For 252, the facts table must carry its own `base:` / `head:` SHAs verbatim from its frontmatter —
    ⭐ they are the one thing in this set that hands a future reviewer a ready-made range, and naming
    them is what makes the refusal cheap to reverse.

    ⛔ Do not open, read into, or write to any `*-REVIEW.md` or `*-REVIEW-R2.md` file for writing. They
    are inputs only (D-09).
  </action>
  <verify>
    <automated>bash -c 'for f in .planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW-REFUSAL.md .planning/phases/252-close-the-v42-audit-gaps/252-REVIEW-REFUSAL.md; do test -f "$f" || { echo "FAIL missing $f"; exit 1; }; grep -q "^status: draft-pending-operator-ruling$" "$f" || { echo "FAIL status $f"; exit 1; }; grep -q "^effective_from: 2026-09-24$" "$f" || { echo "FAIL date $f"; exit 1; }; grep -q "^review_type: self-assessed$" "$f" || { echo "FAIL token $f"; exit 1; }; grep -q "VOID IF ANSWERED" "$f" || { echo "FAIL void $f"; exit 1; }; done; grep -q "gaps_found" .planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW-REFUSAL.md && grep -q "53e2435b7" .planning/phases/252-close-the-v42-audit-gaps/252-REVIEW-REFUSAL.md && echo TWO-DRAFTS-OK'</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at the exact paths in `files_modified`.
    - Each carries `status: draft-pending-operator-ruling`, `effective_from: 2026-09-24`,
      `decided_by: pending — operator (D-05)`, `kind: recorded-decision-not-review`,
      `review_type: self-assessed` — each exactly once.
    - `grep -c 'review_type: independent'` over both files is `0`.
    - `grep -c 'independent_review: done'` over both files is `0`.
    - 253's file contains the literals `gaps_found` and `COMPLETE` together with a sentence stating
      neither refutes the other — assert the rendered sentence, not just the two tokens.
    - 252's file contains the literal SHAs `53e2435b7` and `1ee7903f0`.
    - Each file's `**Residual risk accepted:**` line is **not** `low` — both are security-bearing and
      the line states that as the reason.
    - `git status --short` names no `*-REVIEW.md` or `*-REVIEW-R2.md` file as modified.
  </acceptance_criteria>
  <done>The two phases a reviewer most needs to read have a written draft that says so, names what a measurement cannot cover, and is cheap for a reviewer to pick up later.</done>
</task>

<task type="auto">
  <name>Task 2: Draft the remaining three refusals — 251, 249 and 250</name>
  <files>.planning/phases/251-register-integrity/251-REVIEW-REFUSAL.md, .planning/phases/249-the-model-you-actually-run/249-REVIEW-REFUSAL.md, .planning/phases/250-run-honesty-the-residue/250-REVIEW-REFUSAL.md</files>
  <read_first>
    - `.planning/phases/251-register-integrity/251-REVIEW.md` — written by plan 254-02; read its
      frontmatter `verdicts:` triple, `findings:` counts and `review_type`.
    - `.planning/phases/249-the-model-you-actually-run/249-VERIFICATION.md` frontmatter — the
      `independent_review_waiver` string, verbatim.
    - `.planning/phases/250-run-honesty-the-residue/250-VERIFICATION.md` frontmatter — `reviewer: null`
      and the 5-entry `owed:` list.
    - `.planning/DEBT-06-REFUSALS.md` — the format (again, if not still in context).
  </read_first>
  <action>
    Write the three files to the same format, body order, adopted-reading paragraph, void clause and
    closing disclaimer.

    **251 is the one that must be most careful.** It is the only phase in the set that now has a
    review file that did not exist when this phase was scoped. Its refusal must:
    - cite `251-REVIEW.md` by name, with its `verdicts:` triple and per-severity counts re-read from
      the file rather than recalled;
    - state in its own words that this file is `review_type: self-assessed`, that
      `DEBT-06-AUDIT.md`'s counting rule therefore makes it a **code-review pass and not a §6.3
      review**, and that **it ticks nothing** (D-08, verbatim in spirit);
    - name that a floor pass raises the floor and changes no register — ⛔ so the presence of a review
      file must NOT be used anywhere as the reason the refusal is low-risk.

    **249** carries the `independent_review_waiver` string VERBATIM in its facts table: it is an
    operator instruction of 2026-09-15 that **waived** the separation rather than satisfying it, and
    the phase's own frontmatter says so. ⭐ That is the strongest kind of evidence a refusal can rest
    on — a decision already named, dated and recorded by the party entitled to make it.

    **250** carries `reviewer: null` and the `owed:` list's fourth entry verbatim
    (*independent review (DEBT-06) — WAIVED BY INSTRUCTION, NOT SATISFIED*). ⭐ The refusal is that
    recorded intention catching up, not a second-guess of the close.

    ⛔ Do not modify `251-REVIEW.md`, and do not modify any `*-VERIFICATION.md` — Plan 254-04 owns the
    register writes and doing them here would split one edit across two plans.
  </action>
  <verify>
    <automated>bash -c 'for f in .planning/phases/251-register-integrity/251-REVIEW-REFUSAL.md .planning/phases/249-the-model-you-actually-run/249-REVIEW-REFUSAL.md .planning/phases/250-run-honesty-the-residue/250-REVIEW-REFUSAL.md; do test -f "$f" || { echo "FAIL missing $f"; exit 1; }; grep -q "^status: draft-pending-operator-ruling$" "$f" && grep -q "^effective_from: 2026-09-24$" "$f" && grep -q "^review_type: self-assessed$" "$f" && grep -q "VOID IF ANSWERED" "$f" || { echo "FAIL keys $f"; exit 1; }; done; grep -q "251-REVIEW.md" .planning/phases/251-register-integrity/251-REVIEW-REFUSAL.md && grep -q "reviewer: null" .planning/phases/250-run-honesty-the-residue/250-REVIEW-REFUSAL.md && git diff --name-only | grep -qE "VERIFICATION.md|251-REVIEW.md$" && { echo "FAIL touched a file this plan does not own"; exit 1; }; echo THREE-DRAFTS-OK'</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist at the exact paths in `files_modified`.
    - Each carries the same five frontmatter keys as Task 1's pair, each exactly once.
    - 251's file names `251-REVIEW.md`, reproduces its `verdicts:` triple and its per-severity counts,
      and contains a sentence stating the floor pass ticks nothing — assert the rendered sentence.
    - 249's file contains a verbatim quotation from `independent_review_waiver`.
    - 250's file contains the literals `reviewer: null` and `WAIVED BY INSTRUCTION, NOT SATISFIED`.
    - `git diff --name-only` names **no** `*-VERIFICATION.md` and not `251-REVIEW.md`.
    - `grep -c 'review_type: independent'` over all three is `0`.
  </acceptance_criteria>
  <done>All five phases have a draft; the one with a fresh review file is the one most explicit that having one changes nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Fence the five drafts against each other — one adopted reading, one token, zero discharges</name>
  <files>.planning/phases/249-the-model-you-actually-run/249-REVIEW-REFUSAL.md, .planning/phases/250-run-honesty-the-residue/250-REVIEW-REFUSAL.md, .planning/phases/251-register-integrity/251-REVIEW-REFUSAL.md, .planning/phases/252-close-the-v42-audit-gaps/252-REVIEW-REFUSAL.md, .planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW-REFUSAL.md</files>
  <read_first>
    - all five `*-REVIEW-REFUSAL.md` files just written.
    - `.planning/phases/254-independent-review-of-249-253/254-CONTEXT.md` — M-8.
  </read_first>
  <action>
    ⚠ **This project's own recurring finding is that presence assertions cannot see content drift.**
    Five files written in two tasks will drift in the paragraph that matters most unless the drift is
    measured. The adopted-reading paragraph is the WORDS, not a block — so fence the words.

    Extract the `## ADOPTED READING` section body from each of the five files, normalise trailing
    whitespace only, and assert all five are **byte-identical to one another**. A `sort -u | wc -l` of
    `1` over the five extracted bodies is the assertion. If they differ, fix the outliers to match
    rather than relaxing the fence.

    Do the same for the closing `## ⛔ What this draft does not do` disclaimer body: all five identical.

    Then run the phase-wide token fences across the five files plus `251-REVIEW.md`:
    - `review_type: self-assessed` appears exactly once per file;
    - `review_type: independent` appears **zero** times across all six;
    - `independent_review: done` appears **zero** times across all six;
    - `independent_review: refused` appears **zero** times in any `*-VERIFICATION.md` (nothing has been
      ruled yet — the drafts carry `independent_review_after_ruling`, which is a different key).

    Finally, assert the five phases' own registers are untouched by this plan: the five
    `*-VERIFICATION.md` files and the six pre-existing `*-REVIEW*.md` files are byte-unchanged.
    Prove it with `git status --short` and with `md5sum` against `git show HEAD:<path>` for each of the
    six pre-existing review files — ⚠ `git status` alone cannot see line-ending damage in content, and
    `git checkout -- <dir>` is not a restore on this box.
  </action>
  <verify>
    <automated>bash -c 'set -e; D=".planning/phases"; F="$D/249-the-model-you-actually-run/249-REVIEW-REFUSAL.md $D/250-run-honesty-the-residue/250-REVIEW-REFUSAL.md $D/251-register-integrity/251-REVIEW-REFUSAL.md $D/252-close-the-v42-audit-gaps/252-REVIEW-REFUSAL.md $D/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW-REFUSAL.md"; n=0; for f in $F; do awk "/^## ADOPTED READING/{g=1;next} g&&/^## /{exit} g" "$f" | sed "s/[ \t]*$//" | md5sum | cut -d" " -f1; n=$((n+1)); done | sort -u | wc -l | grep -qx 1 || { echo "FAIL adopted-reading drift"; exit 1; }; echo "adopted-reading: identical across $n files"; grep -l "review_type: independent" $F && { echo "FAIL independence claim"; exit 1; }; grep -l "independent_review: done" $F && { echo "FAIL discharge claim"; exit 1; }; echo FENCED'</automated>
  </verify>
  <acceptance_criteria>
    - The md5 of the `## ADOPTED READING` section body is **identical across all five** files
      (`sort -u | wc -l` == 1).
    - The md5 of the `## ⛔ What this draft does not do` section body is identical across all five.
    - `grep -c 'review_type: self-assessed'` is exactly `1` in each of the five files.
    - `grep -l 'review_type: independent'` over the five refusals **and** `251-REVIEW.md` returns
      nothing.
    - `grep -l 'independent_review: done'` over the same six returns nothing.
    - Each of the five `*-VERIFICATION.md` files has an `md5sum` identical to
      `git show HEAD:<path> | md5sum`, and so does each of the six pre-existing `*-REVIEW*.md` files
      (`249-REVIEW.md`, `250-REVIEW.md`, `252-REVIEW.md`, `252-REVIEW-R2.md`, `253-REVIEW.md`,
      `253-REVIEW-R2.md`).
    - The fence was driven RED at least once: temporarily alter one word of one file's ADOPTED READING,
      confirm the fence FAILS, restore, and confirm the restored file's md5 matches its pre-drive md5.
      ⭐ A fence nobody has seen fail is not a fence.
  </acceptance_criteria>
  <done>The five drafts say the same thing about the two registers that disagree, in the same words, and nothing in the phase claims a review or a discharge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a draft refusal → the `DEBT-06` ledger | a document whose status is ambiguous can be read later as a ruling that never happened |
| claude → the operator's decision | only the operator can make a refusal real (D-05 / `REG-03`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-254-11 | Spoofing | a draft read as an operator ruling | mitigate | `status: draft-pending-operator-ruling` and `decided_by: pending — operator (D-05)` fenced as exactly-once keys in all five |
| T-254-12 | Repudiation | the M-8 register conflict re-created one milestone on | mitigate | the ADOPTED READING body fenced byte-identical across all five by md5, and the fence driven RED |
| T-254-13 | Tampering | an existing `*-REVIEW.md` / `*-REVIEW-R2.md` overwritten (D-09) | mitigate | md5 of each of the six against `git show HEAD:<path>`; `git status --short` clean for them |
| T-254-14 | Elevation of privilege | a draft flipping a register it does not own | mitigate | `independent_review_after_ruling` is a distinct key; `grep` for `independent_review: done` returns nothing; no `*-VERIFICATION.md` in this plan's `files_modified` |
| T-254-15 | Information disclosure | a uniform `low` residual risk hiding that two phases are security-bearing | mitigate | 252 and 253 must not read `low`, asserted; §3.1's test is named in each |
| T-254-SC | Tampering | npm/pip/cargo installs | accept | zero package-manager installs in this plan |
</threat_model>

<verification>
- five files exist; five frontmatter key sets complete and exactly-once.
- ADOPTED READING and the closing disclaimer byte-identical across all five (md5, `sort -u` == 1).
- zero `review_type: independent`, zero `independent_review: done` across the six review-class files.
- eleven pre-existing register/review files md5-identical to `git show HEAD:`.
</verification>

<success_criteria>
Every one of 249-253 has a written refusal draft naming the decider, the reason, the mechanical
evidence, the judgement a measurement cannot replace, the risk accepted and the re-open trigger — and
none of the five claims the phase was reviewed or moves a register.
</success_criteria>

<output>
Create `.planning/phases/254-independent-review-of-249-253/254-03-SUMMARY.md` when done.
</output>
