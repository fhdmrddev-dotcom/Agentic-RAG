---
phase: 254-independent-review-of-249-253
plan: 04
type: execute
wave: 3
depends_on: ["254-01", "254-02", "254-03"]
files_modified:
  - .planning/phases/249-the-model-you-actually-run/249-VERIFICATION.md
  - .planning/phases/250-run-honesty-the-residue/250-VERIFICATION.md
  - .planning/phases/251-register-integrity/251-VERIFICATION.md
  - .planning/phases/252-close-the-v42-audit-gaps/252-VERIFICATION.md
  - .planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/254-independent-review-of-249-253/254-REVIEW-INDEX.md
autonomous: true
requirements: [DEBT-06]
user_setup: []

must_haves:
  truths:
    - "A sweep that counts `independent_review` finds five rows, not four — 251 stopped being invisible"
    - "Not one of the five rows reads `done` or `refused`, because neither is true yet"
    - "`DEBT-06`'s text names every phase that actually carries the debt, and its box is still unticked"
    - "Every ROADMAP row that changed kept its prior verdict verbatim beside the new one"
    - "The operator has one document, with the evidence, the dispositions and the commands, and claude ran none of them"
  artifacts:
    - path: ".planning/phases/251-register-integrity/251-VERIFICATION.md"
      provides: "the `independent_review` key that was absent for the phase's whole life"
      contains: "independent_review:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the amended DEBT-06 text naming both arms, still unticked"
      contains: "DEBT-06"
    - path: ".planning/phases/254-independent-review-of-249-253/254-REVIEW-INDEX.md"
      provides: "the list the operator rules on, with pre-filled commands claude did not run"
      contains: "bus_items_opened_answered_or_closed: 0"
  key_links:
    - from: ".planning/REQUIREMENTS.md DEBT-06"
      to: "the five `*-VERIFICATION.md` files"
      via: "re-derived from the files, never re-typed (D-02)"
      pattern: "^- \\[ \\] \\*\\*DEBT-06\\*\\*"
    - from: ".planning/phases/254-independent-review-of-249-253/254-REVIEW-INDEX.md"
      to: ".agent-bus/OPEN.md"
      via: "pre-filled `answer` then `close`, for the operator to run"
      pattern: "agent-bus\\.sh (answer|close)"
---

<objective>
Make the registers say what is true, and hand the operator one document to rule on.

Purpose: D-10 names four registers that must move before a row stops reading `owed`. This plan moves
the three claude owns, in the honest direction, and prepares the fourth for the operator.
Output: five `*-VERIFICATION.md` frontmatter writes, the ROADMAP Progress rows, the `DEBT-06`
requirement text, and `254-REVIEW-INDEX.md`.

⛔ **NOTHING IS FLIPPED TO `done` OR `refused` BY THIS PLAN, AND THAT IS THE POINT.** No §6.3 review has
run and no refusal has been ruled on. What changes is that the debt stops being **silent** and stops
being **invisible**: 251's `independent_review` key has been absent for the phase's entire life, so a
sweep counting `independent_review: owed` returns **4** and omits it — the same class of invisibility
as a hot file with no ledger row.

⛔ **`DEBT-06` IS AMENDED, NOT TICKED** (D-02 / D-03). Once amended it covers ten phases; 254 closes
only the **249-253 arm**. Ticking it here would be exactly the claim `ROADMAP.md:271` forbids.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/254-independent-review-of-249-253/254-CONTEXT.md
@.planning/phases/254-independent-review-of-249-253/254-PATTERNS.md
@.planning/DEBT-06-AUDIT.md
@.planning/DEBT-06-REFUSALS.md

<interfaces>
<!-- Every anchor measured 2026-09-17. Line numbers shift as you edit — locate by the literal, never
     by the number. -->

**The five `*-VERIFICATION.md` key sets are NOT uniform. Measured, one `grep -n` per file:**

| File | anchors | keys present | what this plan does |
|---|---|---|---|
| `249-.../249-VERIFICATION.md` | `:6` mode · `:9` key | `phase, phase_name, verified, status, verification_mode, builder, reviewer, independent_review, independent_review_waiver, score` | amend the `independent_review:` line's inline comment. Value stays `owed`. The only file with `builder:`/`reviewer:` **and** a `_waiver` string |
| `250-.../250-VERIFICATION.md` | `:4` mode · `:5` key | `phase, verified, verification_mode, independent_review, reviewer, verdict, owed[]` | amend the comment. `reviewer: null` stays null; the `owed:` LIST gains one entry naming the 254 apparatus |
| `251-.../251-VERIFICATION.md` | `:4` mode · **no key** | `phase, verified, verification_mode, status, score, overrides_applied` | ⛔ **ADD** `independent_review: owed` **and** `builder: claude` / `reviewer: claude`, all three absent |
| `252-.../252-VERIFICATION.md` | `:4` mode · `:5` key | `phase, status, verification_mode, independent_review, verified_by, verified_at, base, head, requirements[], success_criteria` | amend the comment. `base:`/`head:` SHAs stay — they are a reviewer's ready-made range |
| `253-.../253-VERIFICATION.md` | `:7` mode · `:8` key | `phase, verified, status, score, overrides_applied, verification_mode, independent_review, re_verification, re_verification_detail{}, gaps[], deferred, human_verification[]` | amend the comment. `status: gaps_found` is **left alone** (M-4) |

**The inline-comment pattern to imitate** — `.planning/milestones/v4.1-phases/243-…/243-VERIFICATION.md:5`:
one key, one inline comment carrying **who decided · why · the evidence verbatim · the re-open trigger ·
a pointer to the full audit**. ⭐ `check-verification-honesty.cjs:158-163` records why the state goes in
a KEY and the reasons in the comment: *"the eventual retirement line will contain both tokens forever"*
— **prose is invisible to every scan.**

**⛔ THE HOOK THAT FIRES ON THESE EDITS, measured — expect it, do not plan around it.**
`.claude/settings.json:133-141` registers a PostToolUse `Write|Edit` hook →
`.claude/hooks/verification-honesty-guard.js` → `scripts/check-verification-honesty.cjs --files <path>`.
It reads **`verification_mode` only** (`:376-388`), never `independent_review`, and it **never blocks**
(exit 0). Current clean-tree reading: `subject: 16 files (9 archived + 7 live)` ·
`OV-SOLO-01-status: retired-2026-09-13 — claims-review arm SKIPPED` · `honesty gate OK`.
⛔ **Do NOT change any `verification_mode` value.** All five stay `self-verified` — a refusal is not a
pass, and no review has run.

**ROADMAP Progress rows — locate by these literals, not by line number:**

| Phase | distinctive literal in the row (also the preservation assertion) | line at plan time |
|---|---|---|
| 249 | `closed by measurement` | `:410` |
| 250 | `the report's dichotomy was FALSE` | `:411` |
| 251 | `THE REGISTER IS GREEN` | `:412` |
| 252 | `refuted SEVEN inherited claims` | `:414` |
| 253 | `BELOW IS PRESERVED RATHER THAN OVERWRITTEN` | `:415` |
| 254 | `added 2026-09-17` | `:416` |

**The house rule, and it is in row 415 itself:** the new verdict goes FIRST, then the literal
`— SUPERSEDED VERDICT FOLLOWS:`, then the prior cell **verbatim**. ⛔ A correction sits BESIDE its
original, never over it.

⛔ **Do NOT call `gsd-sdk query phase.add` or any `state.*` verb.** `phase.add` wrote ROADMAP entries
into the wrong section twice in two days (252 on 2026-09-16, 254 on 2026-09-17); `state.*` has
corrupted `STATE.md` seven times. Hand-edit, then read the diff — never the return value.

**`REQUIREMENTS.md` anchors:** `:185` the `DEBT-06` requirement text (a `- [ ]` bullet) · `:303` the
coverage row · `:304` the already-present *"LEFT UNTICKED at the 251-04 sweep, deliberately"* row,
which is the row to EXTEND rather than replace. The tick pattern to imitate **if an arm ever closes**
is `:175-181` (REG-03: `⭐ TICKED <date> (<plan>), evidence: <artifact>`). ⛔ Not applicable here.
The header at `:273` reads `26 requirements` — 254 mints no ids, so that number must not move.

**The index analog (exact):** `.planning/phases/251-register-integrity/251-BUS-TRIAGE.md` (376 lines).
Its skeleton: `## The queue, measured` (counts + how counted) → `## The five items` (each with a
`| finding | held by | verdict |` table) → `## The commands — ⛔ YOURS TO RUN, NOT CLAUDE'S` (fenced,
pre-filled, `answer` **then** `close`, with `<angle-bracket placeholders>` for anything claude may not
decide) → `## What this document changed on disk` (a 3-row self-accounting table).

⚠ **ONE THING MUST NOT BE COPIED VERBATIM FROM IT: `bus_writes: 0`.** Plan 254-01 **did** write to
`.agent-bus/OPEN.md` — five item bodies were amended in place. Copying that phrase here would be a
false claim. Use instead, as two separate keys:
`bus_items_opened_answered_or_closed: 0` and `bus_item_bodies_amended: 5`,
and back the first with `git diff -U0 .agent-bus/OPEN.md | grep -c '^[-+]### '` → `0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the debt visible in all five verification registers — ADD on 251, amend on four, flip nothing</name>
  <files>.planning/phases/249-the-model-you-actually-run/249-VERIFICATION.md, .planning/phases/250-run-honesty-the-residue/250-VERIFICATION.md, .planning/phases/251-register-integrity/251-VERIFICATION.md, .planning/phases/252-close-the-v42-audit-gaps/252-VERIFICATION.md, .planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-VERIFICATION.md</files>
  <read_first>
    - all five `*-VERIFICATION.md` frontmatter blocks (the first ~20 lines of each).
    - `.planning/milestones/v4.1-phases/243-the-thinking-block-and-the-follow-scroll-seam/243-VERIFICATION.md`
      `:1-10` — the inline-comment pattern, verbatim.
    - `scripts/check-verification-honesty.cjs` `:150-200` and `:370-395` — what it reads and what it
      does not.
    - the five `*-REVIEW-REFUSAL.md` drafts from plan 254-03 — each is the `full audit` pointer the
      comment must name.
  </read_first>
  <action>
    Capture `md5sum` of all five files and of `git show HEAD:<path>` for each BEFORE editing, into a
    scratch dir outside the repo. ⚠ `git status` cannot see line-ending damage in content, so md5 is
    the instrument, not `git status`.

    **251 — an ADD, not a flip.** Insert three keys into the frontmatter: `independent_review: owed`,
    `builder: claude`, `reviewer: claude`. All three are absent today and their absence is the finding:
    a sweep that counts `independent_review: owed` returns 4 of 5 and **silently omits 251**.

    **249, 250, 252, 253 — amend the inline comment on the existing `independent_review:` line.** The
    value stays `owed` on all four.

    Every one of the five `independent_review:` lines carries an inline comment in the `243` pattern,
    naming, in this order:
    - **WHO DECIDED / WHO IS OWED IT** — the ask is `BUS-NNN`, filed 2026-09-16, amended 2026-09-17 by
      Phase 254 with a deadline and a rank. ⛔ It is unanswered as of this write.
    - **THE DEADLINE** — the literal `2026-09-24`, and that after it a drafted refusal awaits the
      operator's ruling (D-04 / D-05).
    - **THE DRAFT** — the exact path of this phase's `<phase>-REVIEW-REFUSAL.md`.
    - **WHAT THIS MARKER IS NOT** — it says the row is accounted for, never that the work was done.
      A refusal, if ruled, would make it `refused`; only a §6.3 review makes it `done`.
    - **RE-OPEN TRIGGER** — `BUS-NNN` being answered at any time, at which point the verdict artifact
      is `<phase>-REVIEW-IND.md` and the draft is void.
    - **FULL AUDIT** — `.planning/phases/254-independent-review-of-249-253/254-REVIEW-INDEX.md`.

    For 250 only, append ONE entry to its existing `owed:` list naming the 254 apparatus and its
    deadline. ⛔ Do not rewrite the four entries already there.

    ⛔ Do NOT touch `verification_mode` on any of the five — all stay `self-verified`. ⛔ Do not touch
    253's `status: gaps_found` (M-4: its ROADMAP row reading COMPLETE does not refute it).
    ⛔ Do not write `independent_review: done` or `independent_review: refused` anywhere.
    ⚠ The PostToolUse honesty hook will fire on each edit and report in-turn; it never blocks. Record
    its output verbatim in the SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'set -e; D=".planning/phases"; F="$D/249-the-model-you-actually-run/249-VERIFICATION.md $D/250-run-honesty-the-residue/250-VERIFICATION.md $D/251-register-integrity/251-VERIFICATION.md $D/252-close-the-v42-audit-gaps/252-VERIFICATION.md $D/253-the-bootstrap-artifact-tells-the-whole-truth/253-VERIFICATION.md"; k=$(grep -h "^independent_review:" $F | wc -l); o=$(grep -h "^independent_review:" $F | grep -c "owed"); m=$(grep -h "^verification_mode:" $F | grep -c "self-verified"); d=$(grep -h "^independent_review:" $F | grep -cE "(done|refused)" || true); echo "keys:$k owed:$o self-verified:$m flipped:$d"; test "$k" = "5" -a "$o" = "5" -a "$m" = "5" -a "$d" = "0" || { echo FAIL; exit 1; }; for f in $F; do grep "^independent_review:" "$f" | grep -q "2026-09-24" || { echo "FAIL no deadline in $f"; exit 1; }; done; node scripts/check-verification-honesty.cjs; echo "honesty-exit:$?"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -h '^independent_review:' <the five>` returns **5** lines — it returned 4 before this task.
      That delta, 4 → 5, is the deliverable.
    - All 5 values are `owed`; `grep -cE 'independent_review:.*(done|refused)'` across the five is `0`.
    - All 5 `verification_mode:` values are still `self-verified` — count is 5, unchanged.
    - `251-VERIFICATION.md` now carries `builder:` and `reviewer:`, both absent before.
    - Every one of the 5 `independent_review:` lines contains the literal `2026-09-24`, the literal
      `BUS-`, and the path of its own `*-REVIEW-REFUSAL.md` — assert the rendered comment content,
      not merely that a comment exists.
    - `253-VERIFICATION.md` still reads `status: gaps_found`; `250-VERIFICATION.md`'s `owed:` list has
      exactly one more entry than before and the original four are byte-unchanged.
    - `node scripts/check-verification-honesty.cjs` exits `0` (captured WITHOUT a pipe) and its
      `subject:` count is ≥ the pre-edit reading of 16.
    - `git diff --numstat` on the five files shows deletions only on the lines genuinely rewritten
      (the `independent_review:` line of four files); no other line was removed.
  </acceptance_criteria>
  <done>A sweep of the five registers finds five rows where it found four, every one still honestly reading `owed`, every one naming its deadline, its draft and its re-open trigger.</done>
</task>

<task type="auto">
  <name>Task 2: Append to the ROADMAP Progress rows and amend DEBT-06's text — preserve every prior verdict, tick nothing</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — the Progress rows located by the six literals in `<interfaces>`, plus
      the `⚠ **7 / 8 v4.2 phases closed …` counts line directly below the table, and `:271` (the
      *"must not be quietly ticked"* sentence).
    - `.planning/REQUIREMENTS.md` `:175-192` (REG-03's tick pattern and DEBT-06's text) and
      `:295-310` (the coverage rows and the table's own footer rule).
    - the five `*-VERIFICATION.md` files as edited by Task 1 — the source D-02 says to re-derive from.
  </read_first>
  <action>
    **A · Re-derive the owed set from the files, never re-type it (D-02).** Run a derivation over
    every `*-VERIFICATION.md` in `.planning/phases/` **and** `.planning/milestones/`, and report BOTH
    halves separately — the phases whose `independent_review` is present-and-not-done, and the phases
    where the key is **ABSENT entirely**. ⭐ The absent set is the half a `grep 'independent_review: owed'`
    cannot see, and it is why 251 was invisible. Publish the derivation command and its verbatim
    output in the SUMMARY.

    **B · ROADMAP Progress rows.** For each of the five rows (249, 250, 251, 252, 253), put the new
    note FIRST in the Status cell, then the literal `— SUPERSEDED VERDICT FOLLOWS:`, then the prior
    cell **verbatim**. The new note states: the ask (`BUS-NNN`), the deadline `2026-09-24`, the rank
    position, the path of the drafted refusal, and — ⛔ explicitly — that `independent_review` still
    reads `owed` and that Phase 254 discharged **nothing** for this row yet.
    For 251's row ONLY, also name `251-REVIEW.md` and state it is `review_type: self-assessed` and
    ticks nothing.
    Then update **254's own row** (`added 2026-09-17`) from `⬜ NOT STARTED` to its in-flight status,
    naming the four plans and preserving the existing cell text after the same `— SUPERSEDED VERDICT
    FOLLOWS:` marker.
    ⛔ **Leave the `⚠ **7 / 8 v4.2 phases closed …` counts line byte-unchanged.** The numerator moves
    when a phase CLOSES, and that is `/gsd:verify-work`'s call, not a plan's. Moving it here would be a
    phase claiming its own close.

    **C · `REQUIREMENTS.md` `DEBT-06` text (`:185`).** Amend the sentence so it names every phase that
    actually carries the debt, from the derivation in step A, split into its two arms:
    the **249-253 arm** (this phase's) and the **238 / 240 / 241 + 242-246 arm** (older, out of D-01's
    scope, filed as `BUS-252`..`BUS-255`). State in the same bullet, in its own words:
    - that the amendment WIDENS the requirement to ten phases;
    - that 254 closes only the 249-253 arm, so `DEBT-06` becomes tickable when **both** arms are, not
      at this close (D-03);
    - ⛔ that the box stays `- [ ]`, quoting `ROADMAP.md:271` — *"`DEBT-06` is NOT in this phase's
      scope and must not be quietly ticked by it."*
    Keep the existing `OV-SOLO-01` re-arm paragraph and the `/code-review ultra` ruled-out sentence.

    **D · `REQUIREMENTS.md` coverage rows (`:303`, `:304`).** Append to the `:303` `DEBT-06` row a
    sentence naming the 254 apparatus and the amendment. **EXTEND** the `:304` LEFT-UNTICKED row —
    keep its existing text verbatim and add, after it, the 254-specific reason it is still unticked.
    ⛔ Do not tick the box; ⛔ do not move the `26 requirements` count at `:273`.
  </action>
  <verify>
    <automated>bash -c 'set -e; R=.planning/ROADMAP.md; Q=.planning/REQUIREMENTS.md; for s in "closed by measurement" "the report.s dichotomy was FALSE" "THE REGISTER IS GREEN" "refuted SEVEN inherited claims" "BELOW IS PRESERVED RATHER THAN OVERWRITTEN" "added 2026-09-17"; do grep -qE "$s" "$R" || { echo "FAIL prior verdict lost: $s"; exit 1; }; done; test "$(grep -c "2026-09-24" "$R")" -ge 5 || { echo "FAIL deadline not on all rows"; exit 1; }; test "$(grep -c "^- \[x\] \*\*DEBT-06\*\*" "$Q")" = "0" || { echo "FAIL DEBT-06 TICKED"; exit 1; }; test "$(grep -c "^- \[ \] \*\*DEBT-06\*\*" "$Q")" = "1" || { echo "FAIL DEBT-06 bullet"; exit 1; }; grep -q "26 requirements" "$Q" || { echo "FAIL requirement count moved"; exit 1; }; grep -q "LEFT UNTICKED at the 251-04 sweep" "$Q" || { echo "FAIL :304 row clobbered"; exit 1; }; echo REGISTERS-APPENDED'</automated>
  </verify>
  <acceptance_criteria>
    - All six distinctive row literals are still present in `ROADMAP.md` — every prior verdict survived
      verbatim, asserted by content rather than by a diff eyeball.
    - `grep -c '— SUPERSEDED VERDICT FOLLOWS:' .planning/ROADMAP.md` increased by exactly `6`.
    - `grep -c '2026-09-24' .planning/ROADMAP.md` is `≥ 5` and each of the five rows contains it.
    - The `⚠ **7 / 8 v4.2 phases closed` line is **byte-identical** to `git show HEAD:` — proven by md5,
      not by reading.
    - The Progress table has the same number of `^| ` rows before and after (capture the count first).
    - `REQUIREMENTS.md`: `- [ ] **DEBT-06**` count is `1`, `- [x] **DEBT-06**` count is `0`.
    - The amended `DEBT-06` bullet contains all ten phase numbers (`249 250 251 252 253 238 240 241`
      and the literal `242-246`) and both arm names.
    - `LEFT UNTICKED at the 251-04 sweep` is still present verbatim at `:304`, with new text AFTER it.
    - `26 requirements` is unchanged.
    - The derivation in step A is reproduced in the SUMMARY with its command and verbatim output, and
      reports the present-but-owed set and the ABSENT-key set as **two separate figures, never summed**.
  </acceptance_criteria>
  <done>Three registers name the debt, name both arms, name the deadline — and the box is still empty, for a reason written next to it.</done>
</task>

<task type="auto">
  <name>Task 3: Write 254-REVIEW-INDEX.md — the list the operator rules on — and run the phase's own hygiene sweeps</name>
  <files>.planning/phases/254-independent-review-of-249-253/254-REVIEW-INDEX.md</files>
  <read_first>
    - `.planning/phases/251-register-integrity/251-BUS-TRIAGE.md` — the exact analog; read its
      frontmatter, its opening disclaimer, its command block and its self-accounting table.
    - `.planning/phases/251-register-integrity/251-REVIEW.md` — the findings and their dispositions,
      written by plan 254-02.
    - the five `*-REVIEW-REFUSAL.md` drafts — the ADOPTED READING paragraph to reproduce identically.
    - `.planning/DEBT-06-AUDIT.md` `:60-74` and `:170-182` — the structural finding and its decay rate.
    - `CLAUDE.md` § *Seeds register cross-check* and § *Reported bugs cross-check*.
  </read_first>
  <action>
    Write `254-REVIEW-INDEX.md` in **254's own** directory (D-09: 254 holds the index and the routing,
    nothing else), to the `251-BUS-TRIAGE.md` skeleton, with these sections:

    1. **Frontmatter** — `type: review-index`, `phase: 254`, `requirement: DEBT-06`, `derived`,
       `derived_by`, `scope`, `review_type: self-assessed`, `debt_06_ticked: false`,
       `bus_items_opened_answered_or_closed: 0`, `bus_item_bodies_amended: 5`.
       ⛔ NOT `bus_writes: 0` — see `<interfaces>`; plan 254-01 wrote to that file and the honest
       assertion is the header-diff, not the phrase.
    2. **Opening disclaimer** — what claude did NOT do: closed no bus item, answered none, ruled on no
       refusal, fixed nothing, flipped no `independent_review`, ticked no box. Each claim paired with
       its command and reading.
    3. **`## The five rows, measured`** — one row per phase:
       `| Phase | verification_mode | independent_review | review artifacts on disk | bus item + age | draft refusal | what is still owed |`.
       Re-derive each cell from the file; ⛔ do not copy from this plan.
    4. **`## The findings, triaged`** — every finding from `251-REVIEW.md` with its severity, its
       one-line claim, whether it was DRIVEN (and the verdict triple), and its single recommended
       disposition (`fast-fix` / `next phase` / `accept`). ⛔ Recommended. The operator rules (D-11);
       nothing is fixed in this phase.
    5. **`## ⛔ The structural finding: nothing reads `independent_review`** — re-derive, do not quote:
       `grep -rn "independent_review" scripts/ .claude/ .github/ docs/ AGENTS.md CLAUDE.md` and publish
       the verbatim result. State the consequence plainly: **the register flip is documentation, not an
       enforced state**, and `DEBT-06-AUDIT.md` measured the decay rate — **Phase 240's marker went
       stale in ten hours.** ⛔ State that building a gate over the field was **considered and rejected
       here** as new capability inside a review phase (D-11 / G-7), and that this is therefore recorded
       as an open finding with a re-open trigger, not closed.
    6. **`## ADOPTED READING`** — the M-8 paragraph, byte-identical to the one in the five refusal
       drafts. Fence it the same way (md5 against one of them).
    7. **`## The flip recipe — what must be true before a row stops reading `owed`** — D-10's four
       registers, each with the exact edit: the `*-VERIFICATION.md` key and its comment, the ROADMAP
       Progress row, the `REQUIREMENTS.md` line, and the bus item. ⭐ Written so the next session can
       apply it without re-deriving anything, because that is what 240's ten-hour decay costs.
    8. **`## The commands — ⛔ YOURS TO RUN, NOT CLAUDE'S`** — a fenced block, pre-filled per bus item,
       `answer` **then** `close`, with `<angle-bracket placeholders>` for the ruling claude may not
       make. Five items.
    9. **`## What this document changed on disk`** — the self-accounting table, every row backed by a
       command and its reading.
    10. **`## Gate readings`** — the sweeps below, verbatim, with their vacuity caveats.

    **Then run the phase's own hygiene sweeps.** ⚠ Capture every exit code WITHOUT a pipe.
    - `node scripts/check-seeds-register.cjs --phase 254` — ⛔ **assert it reports a NON-ZERO plan-file
      count and a non-zero `files_modified` path count.** At discuss time it ran `exit 0` over
      `0 plan file(s), 0 path(s)` and that is **not a clean sweep**; now that PLAN.md files exist it
      has something to match. Report its two unswept figures **separately and never summed**.
    - `node scripts/check-gap-closure-rounds.cjs 254` — expect `G-7 clear`, `0 gap-closure` plans.
    - `node scripts/check-hot-file-ledger.cjs .planning/phases/254-independent-review-of-249-253` —
      ⛔ **assert it reports a non-zero number of PARSED plan files before reading its verdict**: it was
      measured printing `subject: 0` over a CRLF plan and exiting 0, which is a green over nothing. Then
      record M-10 explicitly: its `WATCHED` set is `backend/app` + `frontend/src` only, so it watches
      **0** of this phase's files and **a green here means *nothing to see*, never *clear*.**
    - **Reported-bugs cross-check** — assert no report carries `folded_into: 254`
      (`grep -l 'folded_into: 254' .planning/reported-bugs/` returns nothing) and record that
      `BUG-260916-01` stays `status: open`, named as an INPUT to 249's review and deliberately not
      folded, because 254 fixes nothing.

    ⚠ **THE SEEDS SWEEP ALREADY FIRED AT PLAN TIME AND ITS ROUTING IS DECIDED — re-run it and record
    the same routing, or correct it if the reading changed.** Measured 2026-09-17 once the four PLAN.md
    files existed: `register: 297 · parsed: 297 · duplicate ids: 0`, exit 0, `4 plan file(s), 15
    path(s) in files_modified`, **1 seed matched**:
    `[trigger-fires] SEED-177 (status: partially-answered)` — matched on the path `.planning/ROADMAP.md`.
    ⛔ **ROUTING: NOT FOLDED — a path collision, not a trigger.** `SEED-177` is about MCP connections
    usable in chat and workflows; 254 touches `ROADMAP.md` only to append register bookkeeping to six
    Progress rows and ships no capability at all. Folding it would widen a review phase into a build
    phase, which D-11 / G-7 forbid. ⛔ **Leave `SEED-177` byte-unchanged** — its own re-open trigger
    (`SEED-013` / Open Platform getting a phase number) is unaffected by this phase and overwriting it
    with a false routing is worse than leaving it. Record the match, the reading and the reason in the
    index; do not edit the seed.
    ⚠ Also record the gate's own caveat verbatim: *"the phase declares NO surfaces, so
    `trigger_surfaces` matched nothing here — that is a fact about the PHASE, not about the register"*,
    and its two unswept figures **separately, never summed**: `134 carry no trigger_when at all` ·
    `114 carry prose but no structured trigger`.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=.planning/phases/254-independent-review-of-249-253/254-REVIEW-INDEX.md; test -f "$f"; grep -q "^bus_items_opened_answered_or_closed: 0$" "$f"; grep -q "^bus_item_bodies_amended: 5$" "$f"; grep -q "^debt_06_ticked: false$" "$f"; test "$(grep -c "bus_writes: 0" "$f")" = "0" || { echo "FAIL false bus_writes claim"; exit 1; }; test "$(grep -c "agent-bus.sh answer BUS-" "$f")" = "5"; test "$(grep -c "agent-bus.sh close" "$f")" = "5"; test "$(grep -c "review_type: independent" "$f")" = "0"; node scripts/check-seeds-register.cjs --phase 254 > /tmp/254seeds.txt 2>&1; echo "seeds-exit:$?"; grep -E "plan file" /tmp/254seeds.txt; node scripts/check-gap-closure-rounds.cjs 254 >/dev/null; echo "g7-exit:$?"; test -z "$(grep -rl "folded_into: 254" .planning/reported-bugs/ 2>/dev/null)" && echo INDEX-OK'</automated>
  </verify>
  <acceptance_criteria>
    - `254-REVIEW-INDEX.md` exists in 254's own directory and carries
      `bus_items_opened_answered_or_closed: 0`, `bus_item_bodies_amended: 5`, `debt_06_ticked: false`.
    - `grep -c 'bus_writes: 0'` in it is `0` — the analog's phrase was deliberately NOT copied, because
      plan 254-01 wrote to that file.
    - `git diff -U0 .agent-bus/OPEN.md | grep -c '^[-+]### '` is `0`, and that reading is reproduced in
      the document.
    - The command block contains exactly `5` `agent-bus.sh answer BUS-` lines and `5` `close` lines,
      and `git log -1 --stat` shows no bus status change — claude ran none of them.
    - Every finding in `251-REVIEW.md` appears in `## The findings, triaged` with exactly one
      disposition; the count matches `grep -cE '^### (CR|WR|IN)-[0-9]' 251-REVIEW.md`.
    - The `## ADOPTED READING` body's md5 equals that of the same section in
      `253-REVIEW-REFUSAL.md` — the reading is one reading, not six.
    - The structural-finding section contains the **re-derived** grep output (run in this task, quoted
      verbatim) and the literal `ten hours`.
    - `node scripts/check-seeds-register.cjs --phase 254` exits `0` **and** its output reports a plan
      file count `> 0` and a `files_modified` path count `> 0`. ⛔ A run reporting `0 plan file(s)` is a
      FAILED acceptance criterion even at exit 0.
    - `node scripts/check-gap-closure-rounds.cjs 254` exits `0` with `0 gap-closure` plans.
    - `node scripts/check-hot-file-ledger.cjs <254 dir>` reports a parsed-plan count `> 0`, and the
      document records that its green verdict means nothing was checked (M-10).
    - `grep -rl 'folded_into: 254' .planning/reported-bugs/` returns nothing, and the document names
      `BUG-260916-01` as an un-folded input.
    - The document records the `SEED-177` match, its **NOT FOLDED — path collision** routing and the
      reason; and `.planning/seeds/` is byte-unchanged — `git status --short .planning/seeds/` is empty.
    - The two unswept seed figures appear in the document as two numbers, never as one sum.
    - `grep -c 'review_type: independent'` across every file Phase 254 authored is `0`.
  </acceptance_criteria>
  <done>The operator has one document: what is owed, what was found, what it costs to leave it, the exact edits that discharge it, and five commands that are theirs to run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a register edit → `/gsd:complete-milestone` | the milestone-close gate reads these rows; a row that reads discharged when it is owed lets a requirement ship unnoticed |
| an append-only register → its own history | `ROADMAP.md` rows carry superseded verdicts on purpose; an overwrite destroys the finding, not just the text |
| claude → the operator's ruling | only the operator closes a bus item or makes a refusal real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-254-16 | Spoofing | `independent_review` reading `done`/`refused` before either is true | mitigate | `grep -cE 'independent_review:.*(done\|refused)'` across the five must be `0`; all five values asserted `owed` |
| T-254-17 | Repudiation | `DEBT-06` quietly ticked by a phase that closes one of two arms | mitigate | `- [x] **DEBT-06**` count `0` and `- [ ] **DEBT-06**` count `1`, asserted; `ROADMAP.md:271` quoted in the amended bullet |
| T-254-18 | Tampering | a ROADMAP prior verdict overwritten instead of preserved | mitigate | six distinctive literals asserted still present; `— SUPERSEDED VERDICT FOLLOWS:` count `+6`; the counts line md5-identical to `git show HEAD:` |
| T-254-19 | Elevation of privilege | claude answering or closing a bus item | mitigate | pre-filled commands only, `<angle-bracket placeholders>` for the ruling; `git diff -U0 .agent-bus/OPEN.md \| grep -c '^[-+]### '` is `0` |
| T-254-20 | Information disclosure | a green gate reported as coverage when it watched nothing | mitigate | the ledger gate's parsed count and `watched: 0` both published; the seeds gate must report `> 0` plan files or the criterion FAILS at exit 0 |
| T-254-21 | Denial of service | `state.*` / `phase.add` corrupting `STATE.md` or writing to the wrong ROADMAP section | mitigate | no SDK write verb is called; hand-edit, then read the diff |
| T-254-SC | Tampering | npm/pip/cargo installs | accept | zero package-manager installs in this plan |
</threat_model>

<verification>
- 5 `independent_review` keys, all `owed`; 5 `verification_mode: self-verified`; 0 flips.
- honesty gate exit 0; six ROADMAP literals intact; `+6` superseded markers; counts line md5-identical.
- `DEBT-06` bullet names ten phases and two arms; box unticked; `26 requirements` unmoved.
- index exists with the three accounting keys, five pre-filled `answer`+`close` pairs, zero run.
- seeds gate exit 0 **with** a non-zero plan-file count; G-7 clear; ledger gate's vacuity recorded.
</verification>

<success_criteria>
Every register that can honestly move has moved, in the direction that makes the debt louder rather
than smaller; nothing reads discharged; and the one decision claude may not make is sitting in front
of the operator with the commands pre-filled.
</success_criteria>

<output>
Create `.planning/phases/254-independent-review-of-249-253/254-04-SUMMARY.md` when done.
</output>
