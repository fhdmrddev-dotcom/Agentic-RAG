---
phase: 111.1-configurable-multi-provider-embeddings-incl-local-ollama-and
plan: 04
type: execute
wave: 3
depends_on: ["111.1-01", "111.1-03"]
files_modified:
  - supabase/full-schema.sql
  - backend/tests/integration/test_111_1_match_filters_stale.py
  - backend/tests/integration/test_111_1_cross_embedder_roundtrip.py
  - backend/tests/integration/test_111_1_resize_column.py
  - .planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-PROVIDER-PRESETS.md
autonomous: false
requirements: [EMBED-01, EMBED-03]
user_setup:
  - service: local-embedders
    why: "SC#4 + curation need at least one local OpenAI-compatible /v1/embeddings server running"
    dashboard_config:
      - task: "Have Ollama (:11434) and/or LM Studio (:1234) running with an embedding model loaded for the D-07 curation + live read-back"
        location: "Operator's local machine"
must_haves:
  truths:
    - "Migration 073 is applied LIVE to :54322 via the SQL-editor/psycopg2-direct protocol (NEVER db push/reset); the 4 app_settings cols + 2 document_chunks tag cols exist; existing rows are backfilled (no NULL embedding_model)"
    - "match_document_chunks live signature carries p_embedding_model; resize_embedding_column is callable live"
    - "full-schema.sql is regenerated (no --reset) and reflects the new columns + function signatures"
    - "The D-07 per-provider default embedding-model IDs are curated against live /models and recorded in 111.1-PROVIDER-PRESETS.md"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "Regenerated bootstrap artifact reflecting migration 073"
      contains: "p_embedding_model"
    - path: ".planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-PROVIDER-PRESETS.md"
      provides: "Curated per-provider default model IDs + base_urls + default dims + recommended threshold (the picker's preset map source of truth)"
      contains: "text-embedding-3-small"
  key_links:
    - from: "supabase/migrations/073_embedding_provider_and_chunk_tags.sql"
      to: "live :54322 schema"
      via: "operator paste into Supabase SQL editor / psycopg2-direct — NEVER db push"
      pattern: "embedding_provider"
---

<objective>
Cross migration 073 into the live local DB and curate the provider preset defaults — the
[BLOCKING] gate that turns this phase out of its build-passes-without-apply false-positive state.

Build/type checks pass without the live apply (the columns are referenced in code but only exist
in the DB after this step), so this task is mandatory before verification. It mirrors Phase 111
Plan 05 exactly: apply via the SQL editor (or psycopg2-direct) — NEVER `supabase db push` / `db
reset` (preserves the ~19 dev docs) — read back the columns + backfill + function signatures live,
then regenerate `full-schema.sql` (no `--reset`). It also runs the D-07 curation: validate each
provider's default embedding-model ID against its live `/models` and record the preset map.

Purpose: the live columns + functions are the substrate the re-embed job (Plan 05) and the picker
(Plan 06) depend on; the curated preset map is the source of truth the picker auto-fills from.
Output: applied migration, regenerated full-schema.sql, three un-marked live integration tests,
and 111.1-PROVIDER-PRESETS.md.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-PATTERNS.md
@.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-RESEARCH.md
@.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-01-SUMMARY.md
@supabase/migrations/073_embedding_provider_and_chunk_tags.sql
@CLAUDE.md

<interfaces>
<!-- The project migration-apply protocol (CLAUDE.md + Phase 111 Plan 05 precedent). -->

Apply protocol (NEVER db push/reset):
1. Paste supabase/migrations/073_embedding_provider_and_chunk_tags.sql into the Supabase SQL editor
   against the live local DB (:54322). (If the stack is up + operator authorizes, psycopg2-direct
   to postgresql://postgres:postgres@127.0.0.1:54322/postgres is the autonomous equivalent — Phase
   111 Plan 05 / Phase 110 Plan 02 precedent.)
2. Read back LIVE: the 4 app_settings cols + 2 document_chunks tag cols exist; SELECT count(*)
   FROM document_chunks WHERE embedding_model IS NULL == 0 (backfill complete); the
   match_document_chunks signature includes p_embedding_model; resize_embedding_column exists.
3. Regenerate: bash scripts/regenerate-full-schema.sh   (NO --reset — destructive).

D-07 curation candidates (RESEARCH § Provider Reference — MEDIUM-LOW confidence, MUST validate):
| Provider | base_url | candidate default | dims |
| OpenAI | (SDK default) | text-embedding-3-small (default, D-08) | 1536 |
| Google | OpenAI-compat | gemini-embedding-001 | 3072 (768/1536/3072) |
| Ollama | http://localhost:11434/v1 | nomic-embed-text / mxbai-embed-large | model-dependent |
| LM Studio | http://localhost:1234/v1 | operator-loaded GGUF embedder | model-dependent |
| Cohere | OpenAI-compat | embed-v4 | 256-1536 |
| Jina | OpenAI-compat | jina-embeddings-v3 | 1024 |
| Mistral | OpenAI-compat | mistral-embed | 1024 |
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration 073 live + read-back</name>
  <read_first>
    - supabase/migrations/073_embedding_provider_and_chunk_tags.sql (the SQL to apply)
    - CLAUDE.md (the migration apply protocol — NEVER db push/reset)
    - .planning/phases/111-metadata-enrichment-extraction-backend/111-05-SUMMARY.md (the Phase 111 Plan 05 apply precedent to mirror)
  </read_first>
  <files>supabase/migrations/073_embedding_provider_and_chunk_tags.sql, supabase/full-schema.sql</files>
  <action>
    Apply migration 073 to the live local DB (:54322) using the project protocol — NEVER
    `supabase db push` / `db reset`. Paste the SQL file into the Supabase SQL editor (OR, if the
    stack is up and the operator authorizes, psycopg2-direct in one transaction — the Phase 111
    Plan 05 autonomous equivalent). Then read back live (queries in how-to-verify) and regenerate
    full-schema.sql with `bash scripts/regenerate-full-schema.sh` (NO --reset). This is a human-action
    checkpoint because the operator owns the SQL-editor paste + the destructive-protocol guarantee.
  </action>
  <what-built>
    Migration 073 FILE (Plan 01) carrying the app_settings provider/bucket columns, the
    document_chunks embedding_model/embedding_dimensions tag columns + backfill, and the
    match_document_chunks (+ p_embedding_model) / resize_embedding_column function replacements.
  </what-built>
  <how-to-verify>
    Apply the migration to the live local DB (:54322) using the project protocol — NEVER
    `supabase db push` / `db reset`:
    1. Paste `supabase/migrations/073_embedding_provider_and_chunk_tags.sql` into the Supabase SQL
       editor (OR, if the stack is up and you authorize it, psycopg2-direct in one transaction —
       the Phase 111 Plan 05 autonomous equivalent).
    2. Read back LIVE and confirm:
       - `SELECT column_name FROM information_schema.columns WHERE table_name='app_settings' AND
         column_name IN ('embedding_provider','extraction_provider','confidence_bucket_high',
         'confidence_bucket_medium')` returns 4 rows.
       - `SELECT column_name FROM information_schema.columns WHERE table_name='document_chunks' AND
         column_name IN ('embedding_model','embedding_dimensions')` returns 2 rows.
       - `SELECT count(*) FROM document_chunks WHERE embedding_model IS NULL` == 0 (backfill done).
       - `SELECT count(*) FROM documents` is unchanged from before (no reset; ~19 docs preserved).
       - `match_document_chunks` signature includes `p_embedding_model` (check pg_proc /
         pg_get_function_arguments).
       - `SELECT 'resize_embedding_column'::regproc` resolves (function callable).
    3. Regenerate the bootstrap artifact: `bash scripts/regenerate-full-schema.sh` (NO `--reset`).
    Report the read-back numbers (4 cols / 2 cols / 0 NULL / doc count preserved) + that
    full-schema.sql was regenerated.
  </how-to-verify>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "p_embedding_model" supabase/full-schema.sql && echo "FULL_SCHEMA_REGENERATED"</automated>
  </verify>
  <resume-signal>Type "applied" with the read-back numbers, or describe the failure</resume-signal>
  <acceptance_criteria>
    - All four read-back checks pass (4 app_settings cols, 2 chunk cols, 0 NULL embedding_model, doc count preserved).
    - match_document_chunks live signature contains p_embedding_model; resize_embedding_column resolves.
    - `git diff --stat supabase/full-schema.sql` shows it was regenerated and contains `p_embedding_model`.
    - The migration was applied WITHOUT `supabase db push` / `db reset` (operator confirms the SQL-editor/psycopg2 path).
  </acceptance_criteria>
  <done>Migration 073 is live on :54322; columns + backfill + function signatures verified by read-back; full-schema.sql regenerated; dev docs preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Un-mark the live D-10 integration tests</name>
  <read_first>
    - backend/tests/integration/test_111_settings_readback.py:25-37 (the live _pg_reachable harness)
    - backend/tests/integration/test_111_1_match_filters_stale.py, test_111_1_cross_embedder_roundtrip.py, test_111_1_resize_column.py (the Plan-01 scaffolds)
    - 111.1-03-SUMMARY.md (confirms the chunk-tag write + retrieval filter caller are wired)
  </read_first>
  <files>backend/tests/integration/test_111_1_match_filters_stale.py, backend/tests/integration/test_111_1_cross_embedder_roundtrip.py, backend/tests/integration/test_111_1_resize_column.py</files>
  <action>
    With the columns + functions now live, un-mark (remove xfail) and flesh out three live
    integration tests against :54322 (skip-guard when unreachable):
    - `test_111_1_match_filters_stale.py` (D-10): insert two chunk rows for one user — one tagged
      `embedding_model='current'`, one tagged `embedding_model='stale'` — call
      `match_document_chunks(..., p_embedding_model='current')` and assert only the current-model
      chunk is returned. Rollback / clean up the test rows.
    - `test_111_1_cross_embedder_roundtrip.py` (EMBED-04 SC#2): with a configured non-default
      embedder (or, if only OpenAI keys are available, assert the cred-resolution parity at the
      client-build boundary — chunk-path and query-path build an identical client given the same
      user_settings; the full second-embedder roundtrip is the manual UAT axis). NO mocking of the
      cred resolution ("static would false-green").
    - `test_111_1_resize_column.py` (EMBED-05): call `resize_embedding_column(N)` against a
      scratch/copy context and assert the column type changed + the HNSW index exists; guard so it
      does NOT destroy the live corpus (operate on a throwaway dim round-trip or assert via a
      transaction that rolls back). Honor Assumption A4 (the function has never been called live).
    Keep `test_111_1_reembed_resume.py` + `test_111_1_reembed_rls.py` xfail — Plan 05 builds the job.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_111_1_match_filters_stale.py tests/integration/test_111_1_cross_embedder_roundtrip.py tests/integration/test_111_1_resize_column.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three integration tests GREEN against live :54322 (or skip-guarded if unreachable — record which).
    - `grep -L "xfail" backend/tests/integration/test_111_1_match_filters_stale.py` shows xfail removed (test is real).
    - `test_111_1_match_filters_stale.py` asserts the stale-model chunk is EXCLUDED (D-10).
    - `test_111_1_resize_column.py` does NOT destroy the live corpus (no permanent vector loss — rollback/scratch path).
    - `cd backend && venv/Scripts/python -m pytest tests/ -q` net-new failures = 0 (baseline checkout, SEED-056).
  </acceptance_criteria>
  <done>The three live D-10 integration tests pass against :54322 (or skip-guard honestly); stale-model filtering proven live, not mocked.</done>
</task>

<task type="auto">
  <name>Task 3: D-07 — curate provider preset defaults against live /models</name>
  <read_first>
    - 111.1-RESEARCH.md § "Provider Reference (D-06 presets — IDs need curation per D-07)"
    - 111.1-CONTEXT.md D-07 (lock the SHAPE now; curate exact IDs as a phase task) + D-08 (default stays text-embedding-3-small/1536)
  </read_first>
  <files>.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-PROVIDER-PRESETS.md</files>
  <action>
    Validate the candidate per-provider default embedding-model IDs against each provider's live
    `/models` (and the operator's running Ollama/LM Studio), then record the preset map as the
    source of truth the picker (Plan 06) auto-fills from. For each provider record: preset key,
    `base_url`, validated default model id, default dimensions, whether `dimensions=` truncation is
    supported, recommended `retrieval_match_threshold` (D-11), and confidence-bucket defaults
    (D-12) if known.

    Hard rules: OpenAI default stays `text-embedding-3-small` / 1536 (D-08 — no surprise re-embed).
    Local presets (Ollama `:11434/v1`, LM Studio `:1234`) use dummy keys; LM Studio base_url is NOT
    double-appended. Any provider that turns out NOT to be a DIRECT OpenAI-compatible
    `/v1/embeddings` (Assumption A1 — verify Cohere/Jina/Mistral) becomes a "Custom /
    OpenAI-compatible" entry only — note it. Do NOT pass `dimensions=` as a forced global cap (D-01).

    Use Context7 / WebFetch / the provider's live `/models` as available; where a key/endpoint is
    unavailable, mark the entry `[UNVERIFIED — pin shape, curate at availability]` rather than
    blindly pinning (D-07). The picker SHAPE ships regardless.
  </action>
  <verify>
    <automated>test -f .planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-PROVIDER-PRESETS.md && grep -q "text-embedding-3-small" .planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-PROVIDER-PRESETS.md && grep -qi "11434" .planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-PROVIDER-PRESETS.md && echo "PRESETS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - 111.1-PROVIDER-PRESETS.md exists with a row per preset (OpenAI, Google, Ollama, LM Studio, Cohere, Jina, Mistral, Custom).
    - OpenAI default is `text-embedding-3-small` / 1536 (D-08); the file contains `11434` and `1234` (local base_urls).
    - Each row marks whether the ID was live-validated or `[UNVERIFIED]`; no blind pinning of an unvalidated ID as "confirmed".
    - LM Studio base_url is documented as NOT double-appended; local presets use dummy keys.
  </acceptance_criteria>
  <done>The per-provider preset defaults are curated (or honestly marked UNVERIFIED) and recorded as the picker's source of truth; D-08 default preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → live DB schema | A destructive apply (db reset/push) would wipe the ~19 dev docs |
| provider /models curation → preset map | A blindly-pinned wrong model id could mis-route or fail at embed time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111.1-04-01 | DoS / Data loss | migration apply | mitigate | SQL-editor/psycopg2-direct only, NEVER db push/reset; read-back asserts doc count preserved (Phase 111 Plan 05 precedent) |
| T-111.1-04-02 | Tampering (integrity) | resize_embedding_column live (A4, never called) | mitigate | Test against a scratch/rollback path; do NOT run the live resize on the real corpus in the test; the job (Plan 05) only resizes on a true dims change |
| T-111.1-04-03 | Tampering | half-re-embed mixed space | mitigate | Live `test_111_1_match_filters_stale.py` proves the p_embedding_model filter excludes stale-model chunks (not mocked) |
| T-111.1-04-04 | Information Disclosure | wrong-provider preset default | accept | Unvalidated IDs are marked `[UNVERIFIED]` not pinned (D-07); the egress structural cure (Plan 02) makes a wrong id fail honestly, not leak |
</threat_model>

<verification>
- The [BLOCKING] apply read-back confirms 4+2 columns, 0 NULL embedding_model, doc count preserved, p_embedding_model in the live signature.
- full-schema.sql regenerated (no --reset) and contains p_embedding_model.
- The three live D-10 integration tests pass (or skip-guard); full suite net-new failures = 0.
- 111.1-PROVIDER-PRESETS.md records the curated/UNVERIFIED preset map.
</verification>

<success_criteria>
- Migration 073 live on :54322 via the project protocol (NEVER db push/reset); columns + backfill + function signatures verified by read-back; dev docs preserved.
- full-schema.sql regenerated.
- D-10 stale-model filtering proven live; resize_embedding_column callable without destroying the corpus.
- Provider preset defaults curated (or honestly UNVERIFIED) in 111.1-PROVIDER-PRESETS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-04-SUMMARY.md`
</output>
