---
phase: 111.1-configurable-multi-provider-embeddings-incl-local-ollama-and
plan: 01
subsystem: database
tags: [embeddings, supabase, migration, pydantic, fastapi, settings, pgvector, rls]

# Dependency graph
requires:
  - phase: 111-metadata-enrichment-extraction-backend
    provides: "migration 072 + env_attr=None app_settings readback pattern + extraction_model resolver + Wave-0 scaffold convention (commit 11c453ef)"
  - phase: 110-dm-foundations
    provides: "live-DB integration harness (_POSTGRES_TEST_DSN / _pg_reachable) + document_management_enabled env_attr=None precedent"
provides:
  - "migration 073 FILE (un-applied): app_settings provider/bucket cols, document_chunks embedding_model/embedding_dimensions tag cols + backfill, match_document_chunks p_embedding_model filter (D-10), resize_embedding_column re-declared as source of truth (EMBED-05)"
  - "11 Wave-0 RED/xfail test scaffolds matching VALIDATION.md filenames (7 unit + 5 integration, live-DB skip-guarded)"
  - "4 new app_settings fields end-to-end: embedding_provider / extraction_provider / confidence_bucket_high / confidence_bucket_medium (schema readback env_attr=None + SettingsUpdate accept + save_app_settings persist + response surface)"
  - "_validate_confidence_buckets V5 guard ([0,1] clamp + medium<=high coherence -> 422)"
affects: [111.1-02 provider-routing, 111.1-03 embed_chunks-cred-fix, 111.1-04 migration-apply-blocking, 111.1-05 reembed-job, 111.1-06 picker-ui]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "env_attr=None app-config readback (4 new settings, NO env fallback per CLAUDE.md secrets-only rule)"
    - "migration-as-source-of-truth: SQL function bodies re-declared via CREATE OR REPLACE in the migration, NOT hand-edited into full-schema.sql"
    - "Wave-0 RED scaffold: xfail(strict=False) + imports-inside-body so the suite exits 0 with zero collection errors; live integration files skip-guard on :54322"
    - "SEED-056 net-new proof via baseline stash/checkout (identical 59-failure set at base and after)"

key-files:
  created:
    - "supabase/migrations/073_embedding_provider_and_chunk_tags.sql"
    - "backend/tests/unit/test_111_1_embedding_presets.py"
    - "backend/tests/unit/test_111_1_local_embedding_resolve.py"
    - "backend/tests/unit/test_111_1_embed_chunks_creds.py"
    - "backend/tests/unit/test_111_1_explicit_provider_routing.py"
    - "backend/tests/unit/test_111_1_forced_emit_local.py"
    - "backend/tests/unit/test_111_1_no_mangle_local.py"
    - "backend/tests/unit/test_111_1_threshold_portable.py"
    - "backend/tests/integration/test_111_1_cross_embedder_roundtrip.py"
    - "backend/tests/integration/test_111_1_match_filters_stale.py"
    - "backend/tests/integration/test_111_1_reembed_resume.py"
    - "backend/tests/integration/test_111_1_resize_column.py"
    - "backend/tests/integration/test_111_1_reembed_rls.py"
  modified:
    - "backend/app/models/user_settings.py"
    - "backend/app/api/settings.py"

key-decisions:
  - "Migration 073 re-declares match_document_chunks AND resize_embedding_column via CREATE OR REPLACE so the migration is the source of truth (full-schema.sql untouched; Plan 04 regenerates it)"
  - "p_embedding_model added as the LAST, defaulted (NULL) param to match_document_chunks so existing callers stay compatible (D-10)"
  - "V5 bucket guard lives in a reusable settings.py helper (_validate_confidence_buckets) called before building updates; rejects out-of-range AND incoherent medium>high with 422"
  - "Two non-xfail anchor tests document current pre-fix behavior (embed_chunks empty short-circuit + slashed-id->openrouter inference) so the RED scaffold has a live baseline"

patterns-established:
  - "Wave-0 scaffold: not-yet-built symbol imports inside test bodies + xfail(strict=False); schema-layer assertions un-marked in the SAME plan's later task, consumption-layer stays xfail until the owning downstream plan"
  - "Confidence-bucket portability: 0.54/0.38 kept as defaults everywhere (D-08 back-compat) while becoming configurable app_settings columns (D-12)"

requirements-completed: [EMBED-01, EMBED-02, EMBED-03]

# Metrics
duration: ~35min
completed: 2026-06-16
---

# Phase 111.1 Plan 01: Embeddings Substrate Summary

**Migration 073 FILE (provider/bucket app_settings cols + per-chunk embedding-model tags + backfill + match_document_chunks model filter + resize_embedding_column), 11 Wave-0 RED scaffolds, and four new configurable-embedding settings wired end-to-end through Pydantic schema readback + the settings API.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-06-16
- **Completed:** 2026-06-16
- **Tasks:** 3
- **Files modified:** 15 (13 created, 2 modified)

## Accomplishments
- Migration 073 file authored (un-applied — Plan 04 is the [BLOCKING] live-apply gate): 4 app_settings columns (embedding_provider, extraction_provider, confidence_bucket_high/medium), 2 document_chunks tag columns (embedding_model, embedding_dimensions) + truthful backfill to text-embedding-3-small/1536, the D-10 `p_embedding_model` stale-model filter on match_document_chunks (RLS clause preserved byte-for-byte), and resize_embedding_column re-declared as the migration's source of truth.
- 11 Wave-0 test scaffolds (7 unit + 5 integration) matching VALIDATION.md filenames exactly; the suite exits 0 (xfail/xpass, zero collection errors), integration files skip-guard cleanly when :54322 is unreachable.
- Four new settings resolve on both Pydantic models (UserEffectiveSettings + SettingsUpdate), read back via the env_attr=None app-config pattern, persist through the existing save_app_settings SET-clause path, and surface embedding_provider/extraction_provider in the API response for the picker.
- V5 input validation (`_validate_confidence_buckets`): clamps each bucket to [0,1] and rejects an incoherent medium>high order with a 422.

## Task Commits

Each task was committed atomically:

1. **Task 1: Wave-0 test scaffolds (11 RED/xfail files)** - `257347e1` (test)
2. **Task 2: Migration 073 FILE (un-applied) — all phase DDL** - `eaed1736` (feat)
3. **Task 3: Settings schema readback + API accept/persist/respond** - `cc83bac4` (feat)

_Note: STATE.md / ROADMAP.md updates and the metadata commit are owned by the orchestrator (worktree mode)._

## Files Created/Modified
- `supabase/migrations/073_embedding_provider_and_chunk_tags.sql` - All phase DDL (app_settings cols, chunk tag cols + backfill, match_document_chunks + resize_embedding_column CREATE OR REPLACE); idempotent, un-applied.
- `backend/app/models/user_settings.py` - 4 new UserEffectiveSettings fields + env_attr=None readback in _build_settings_from_row.
- `backend/app/api/settings.py` - SettingsUpdate fields, save-block persistence, FullSettingsResponse + _build_response surface, _validate_confidence_buckets V5 helper.
- `backend/tests/unit/test_111_1_*.py` (7 files) - preset resolution, local-endpoint resolve, embed_chunks creds, explicit provider routing, forced_emit local, no-mangle local, threshold portability.
- `backend/tests/integration/test_111_1_*.py` (5 files) - live cross-embedder roundtrip, stale-model filter, re-embed resume, resize column, re-embed RLS.

## Decisions Made
- **Migration is the source of truth for SQL functions:** both match_document_chunks and resize_embedding_column are re-declared via CREATE OR REPLACE in 073 rather than hand-editing full-schema.sql (CLAUDE.md rule; Plan 04 regenerates full-schema.sql post-apply). full-schema.sql left untouched by this plan.
- **p_embedding_model is the last, NULL-defaulted parameter** so no existing caller signature breaks; the D-10 filter is a single additive WHERE clause and the RLS `dc.user_id = match_user_id` scope is preserved byte-for-byte (V4).
- **Two non-xfail anchor tests** (`embed_chunks([]) == []` and `_infer_provider_for('mistral/mistral-small') == 'openrouter'`) intentionally pass against the current tree to give the RED scaffold a live behavioral baseline the downstream fixes will flip.

## Deviations from Plan

None - plan executed exactly as written.

The plan explicitly directed un-marking the `test_111_1_threshold_portable.py` schema-read assertion once Task 3 made it pass; this was done (the two schema reads + the bucket-order validation case are now GREEN, while the `_compute_confidence` consumption assertion stays xfail until Plan 03 — exactly as the plan specified). This is planned behavior, not a deviation.

## Issues Encountered
- **Worktree has no venv and no `.env`** (both gitignored / live only in the shared checkout). Resolved by running the shared-checkout venv's `python.exe` against the worktree source with `PYTHONPATH=<worktree>/backend`, and copying the gitignored `backend/.env` into the worktree for the test run (verified `git check-ignore` confirms it can never be committed). This is the documented "venv-relative verify" worktree caveat (Phase 101 memory).
- **59 pre-existing unit failures** (retrieval_service / sandbox_service / sql_service / streaming_reliability) are SEED-056 rot, NOT net-new. Proven via a baseline stash/checkout: the failing node-ID set is byte-identical at the worktree base commit and after all three task commits (`diff` empty). Net-new failures from this plan = 0.

## Known Stubs

The 11 Wave-0 scaffolds are intentional RED/xfail stubs — that is the plan's purpose (Wave-0 contract). They reference symbols built in downstream plans:
- `resolve_embedding_preset` (Plan 03/06), `resolve_extraction_provider` (Plan 02), `_inject_local_provider_creds` / `_apply_openrouter_quality_strategy` (Plan 02), `reembed_service.reembed_job` (Plan 05), the bucket-aware `_compute_confidence` signature (Plan 03), the applied migration 073 (Plan 04 [BLOCKING]).
Each is xfail(strict=False) with a reason naming its owning plan, so the suite stays green until that plan lands. No stub blocks this plan's own goal (substrate + settings plumbing), which is fully functional.

## Threat Flags

None — the only security-relevant surface added (the confidence-bucket input boundary) is covered by threat T-111.1-01-01 in the plan's threat_model and mitigated by `_validate_confidence_buckets` (V5). No new endpoints, auth paths, or trust-boundary schema beyond what the threat register anticipates.

## User Setup Required
None for this plan. **Plan 04 is the [BLOCKING] live-apply gate**: migration 073 must be applied to the live local DB via the Supabase SQL editor (or psycopg2-direct to :54322) — NEVER `db push`/`db reset` — then `bash scripts/regenerate-full-schema.sh` (no reset). This plan only authors the FILE.

## Next Phase Readiness
- The four app_settings columns are DEFINED in the model + the migration FILE, so Plan 02 (provider routing), Plan 03 (embed_chunks cred fix + threshold consumption), and Plan 06 (picker UI) can wire against them.
- Migration 073 is ready to apply in Plan 04; full-schema.sql regeneration follows the apply.
- Blocker: the live integration scaffolds (cross-embedder roundtrip, stale-model filter, resize, re-embed resume/RLS) remain xfail until migration 073 is applied (Plan 04) and the downstream services exist (Plans 03/05).

## Self-Check: PASSED

- All 16 claimed files exist on disk (13 created + 2 modified + this SUMMARY).
- All 3 task commits exist in git history (257347e1, eaed1736, cc83bac4).
- 111.1 test subset exits 0 (5 passed / 20 xfailed / 3 xpassed / 0 errors / 0 unexpected failures).
- Net-new full-unit-suite failures = 0 (SEED-056 baseline stash/checkout: identical 59-failure set).
- No file deletions across the 3 task commits.

---
*Phase: 111.1-configurable-multi-provider-embeddings-incl-local-ollama-and*
*Completed: 2026-06-16*
