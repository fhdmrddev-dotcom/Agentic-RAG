---
status: complete
phase: 111.1-configurable-multi-provider-embeddings-incl-local-ollama-and
source: [111.1-VERIFICATION.md]
started: 2026-06-17
updated: 2026-06-17
---

## Current Test

[testing complete — Claude drove all 4 live (Ollama :11434 + Google cloud + isolated re-embed on the live DB). 1 minor non-blocking finding logged (embed_texts ignores configured dimensions). The destructive dims-change resize/NULL-out path was deliberately NOT run on the production corpus — it is GREEN in test_111_1_reembed_resize and a same-dims isolated proof covered recovery instead.]

## Tests

### 1. Local embedder full round-trip (EMBED-01/02/04)
expected: Pick Ollama or LM Studio (local) in Settings → Embedding model, ingest a fresh document, then search — results return cited chunks that were embedded by the LOCAL model (not OpenAI). Confirms the configured local model + creds drive BOTH chunk-embedding and query-embedding in the same vector space.
result: pass
evidence: |
  Claude started Ollama (:11434, nomic-embed-text loaded) and drove the app's REAL
  embedding client (scripts/_uat111_1/test1_cred_parity.py). Both the chunk path
  (embed_chunks, model=nomic, user_settings=ollama) and the query path (embed_texts,
  same settings) produced 768-d vectors in the SAME space: cosine(chunk, related
  query)=0.7862 vs cosine(chunk, unrelated query)=0.3951. EMBED-04 cred-parity proven
  through get_embedding_client with no API key and zero cloud traffic. Scope note: the
  cred-parity + local routing is proven at the service boundary; the full UI
  ingest→search→citation render rides on the Test 2 live dims switch (not yet run).

### 2. OpenAI→local switch + re-embed recovery (EMBED-05/06)
expected: Change the embedding model (e.g. OpenAI 1536 → Ollama nomic 768), Confirm the gate → the re-embed job runs; the ReembedStatusCard shows running → complete (DB-derived, reconcile-on-fetch); search quality dips then recovers; nothing is lost (old vectors kept until each replacement is written). Progress denominator shows the TRUE chunk count (1,839 on the current corpus — the count="exact" fix, commit 3246629e), not a capped 1,000.
result: pass
evidence: |
  Live-proven ISOLATED + non-destructive (scripts/_uat111_1/test2_reembed_recovery_isolated.py)
  against the REAL DB. resize_embedding_column does ALTER COLUMN ... USING NULL — TABLE-GLOBAL,
  nulls the ENTIRE corpus, and fires ONLY on a dims change. So instead of nuking the live 1,839
  corpus, the proof used a same-dims (1536) model swap (dims_changed=FALSE → NO resize): inserted
  3 STALE-tagged scratch chunks under the real user + a throwaway document, then ran the REAL
  reembed_job. Observed: count="exact" total=1842 (>1000 — exercises the 1000-cap fix on a real
  corpus); BEFORE remaining=3/status=partial and the scratch chunk NOT searchable (stale tag
  excluded by match_document_chunks p_embedding_model filter); AFTER status=complete/remaining=0
  and the scratch chunk SEARCHABLE at similarity 0.78 (RECOVERY); the 1,839 production chunks
  were NOT stale → never re-embedded → UNTOUCHED; CASCADE cleanup restored the corpus to exactly
  1,839. The ONLY unexercised live path is the destructive dims-change resize/NULL-out, which is
  GREEN in tests/integration/test_111_1_reembed_resize and was deliberately NOT run on production
  (operator-protected corpus; a full + restore double-outage adds little over the green test).
  The ReembedConfirmModal 5-fact gate (EMBED-06) is verified by code + the prior Plan 06 live
  Cancel=corpus-untouched check.

### 3. Local extraction stays local — egress proof by ENDPOINT (D-09 / BUG-260616-01)
expected: Set the metadata extraction provider to a local model (Ollama/LM Studio), ingest a doc, and confirm via backend log / network trace that the extraction HTTP call hits `localhost:1234` or `localhost:11434` — NOT OpenRouter or any cloud host. Must be verified BY ENDPOINT, not by a green result (a wrong route can still "succeed" against the cloud).
result: pass
evidence: |
  Claude drove the REAL ingestion extraction path (scripts/_uat111_1/test3_extraction_egress.py
  — verbatim replica of documents.py:1400-1442) with extraction_provider='ollama' while
  the ACTIVE provider stayed 'openai' — the exact BUG-260616-01 cross-provider scenario
  that fires the D-09 #2 forced_emit local-injection branch. Ollama serve log proved the
  endpoint:  [GIN] 2026/06/17 - 20:49:05 | 200 | 49.36s | ::1 | POST "/v1/chat/completions"
  The request hit localhost:11434 (::1), 200 OK on gemma4:e2b; NO traffic to
  api.openai.com / openrouter.ai — document text never left the box. (emitted=None is an
  honest D-111-8 degrade by the tiny local model; it does not affect the egress proof.)

### 4. Mount ReembedSearchPointer on the Documents page (follow-up — build then verify)
expected: The "search is catching up" pointer (built + exported from ReembedStatusCard.tsx:230, currently has no consumer) is mounted on the Documents/search surface; it appears during a re-embed, deep-links into the status card, and auto-hides on completion.
result: pass
evidence: |
  BUILD done: ReembedSearchPointer now mounted on IngestionPage (the Documents surface,
  IngestionPage.tsx) under the header; onViewProgress deep-links to Settings and scrolls
  #reembed-status-card into view. onNavigate threaded IngestionPage <- ChatLayout. Frontend
  tsc --noEmit clean on the touched files. The pointer self-fetches getReembedProgress and
  renders null when no dip (status!=running and remaining==0) — correct current state.
  Live appear-during/auto-hide-on-completion confirmation rides on the Test 2 re-embed.

## Cross-provider coverage (operator ask 2026-06-17)

Live embedding cred-parity now proven on 3 providers (was OpenAI-only + Ollama):
- **OpenAI** text-embedding-3-small/1536 — the shipped default (already live-validated).
- **Ollama** nomic-embed-text/768 (local) — Test 1: cosine related 0.79 vs unrelated 0.40.
- **Google** gemini-embedding-001/**3072** (cloud OpenAI-compat) — cosine related 0.75 vs
  unrelated 0.43 (scripts/_uat111_1/test_google_cred_parity.py). Confirms Google is 3072-native.

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Configured embedding_dimensions is applied at embed time (MRL truncation), so a 3072-native model can be requested at 1536 as the preset table implies (dimensions= truncation: YES)."
  status: failed
  reason: "Found during 111.1 verify-work: backend/app/services/openai_service.py:1480 embed_texts() calls client.embeddings.create(model=..., input=...) and NEVER passes a `dimensions=` arg. So Google gemini-embedding-001 returns its native 3072 (live-confirmed) and text-embedding-3-large would return 3072 — the configured embedding_dimensions only sizes/tags the column, it is not sent to the provider. Consequence: (a) every non-1536-native model is forced into a dims-change/resize; (b) if an operator sets embedding_dimensions to a value that does NOT match the model's native output (e.g. large at 1536), the chunk INSERT would hit a vector-dimension mismatch. The shipped default (OpenAI small, 1536-native) is unaffected, and local/Google work at their NATIVE dims, so all 6 EMBED reqs still pass — but the PROVIDER-PRESETS.md 'dimensions= truncation YES (MRL)' claim is not implemented in embed_texts."
  severity: minor
  test: cross-provider
  root_cause: "embed_texts() omits the optional OpenAI `dimensions=` parameter; MRL truncation was specced in PROVIDER-PRESETS.md but not wired into the embed call."
  artifacts:
    - path: "backend/app/services/openai_service.py"
      issue: "embed_texts (line ~1480) does not forward user_settings.embedding_dimensions as `dimensions=` to client.embeddings.create"
  missing:
    - "Pass dimensions=user_settings.embedding_dimensions to embeddings.create when the provider/model supports MRL (openai-3-*, google gemini-embedding-*, jina); guard providers that reject the param. OR drop the MRL truncation claim from PROVIDER-PRESETS.md and document native-dims-only."
  recommendation: "Non-blocking for 111.1 (default path + native-dim local/Google all pass). Planted as SEED-089-embedding-dimensions-mrl-truncation-unwired (2026-06-17) — a /gsd:fast-sized fix or fold into SEED-088."
