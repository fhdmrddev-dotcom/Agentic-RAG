---
phase: 111.1
slug: configurable-multi-provider-embeddings-incl-local-ollama-and
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-16
validated: 2026-06-17
---

# Phase 111.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `111.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest + `asyncio_mode=auto` (`backend/pytest.ini`) |
| **Config file** | `backend/pytest.ini` (`testpaths=tests`) |
| **Quick run command** | `cd backend && venv/Scripts/python -m pytest tests/unit/ -x -q` |
| **Full suite command** | `cd backend && venv/Scripts/python -m pytest tests/ -q` |
| **Estimated runtime** | ~30–90 seconds (unit); integration adds live `:54322` round-trips |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && venv/Scripts/python -m pytest tests/unit/ -x -q`
- **After every plan wave:** Run `cd backend && venv/Scripts/python -m pytest tests/ -q` (prove net-new failures = 0 via baseline checkout, NOT raw count — SEED-056 / frontend-vitest-rot caveat)
- **Before `/gsd-verify-work`:** Full suite green **+** the live cross-embedder / local-route UAT matrix below
- **Max feedback latency:** ~90 seconds (unit)

---

## Per-Task Verification Map

> **Post-execution audit 2026-06-17:** statuses flipped from plan-time `⬜ pending` to live-run results. Full 111.1 suite re-run on `:54322` = **30 unit + 12 integration = 42 passed, 0 xfail/0 xpass, 0 net-new failures.** Two stale-premise test sets were RETIRED (operator-approved) — see the row notes + audit trail.

| Req / SC | Behavior | Threat Ref | Test Type | Automated Command | Status |
|----------|----------|------------|-----------|-------------------|--------|
| EMBED-01/02/03 | Preset selection resolves correct base_url + dummy key + default model/dims | T-V5 | manual (frontend + UAT) | — (see note) | 🔵 manual — RECLASSIFIED |
| EMBED-02 | Ollama appends `/v1`; LM Studio does NOT double-append (embedding-client base_url) | — | unit | `pytest tests/unit/test_111_1_local_embedding_resolve.py -x` | ✅ green (2/2) |
| EMBED-02 | Local embedder uses a dummy key | — | manual (frontend + UAT) | — (see note) | 🔵 manual — RECLASSIFIED |
| EMBED-04 (D-13) | `embed_chunks` threads `user_settings`; chunk + query resolve identical client | — | unit | `pytest tests/unit/test_111_1_embed_chunks_creds.py -x` | ✅ green |
| EMBED-05 | Re-embed kickoff fires on confirmed model/dim change | T-consent | unit | `pytest tests/unit/test_111_1_reembed_kickoff.py -x` | ✅ green |
| EMBED-04 (SC#2) | Chunk + query paths build identical client for the configured embedder | — | integration (live) | `pytest tests/integration/test_111_1_cross_embedder_roundtrip.py -x` | ✅ green (client-build parity; live cross-provider roundtrip stays manual) |
| EMBED-05 (D-10) | `match_document_chunks` excludes chunks whose `embedding_model != current` | T-integrity | integration (live) | `pytest tests/integration/test_111_1_match_filters_stale.py -x` | ✅ green |
| EMBED-05 (D-05) | Re-embed job resumable + non-destructive (old vector kept until replacement written) | — | integration (live) | `pytest tests/integration/test_111_1_reembed_resume.py -x` | ✅ green |
| EMBED-05 | `resize_embedding_column(N)` resizes col + rebuilds HNSW (scratch/rollback) | — | integration (live) | `pytest tests/integration/test_111_1_resize_column.py -x` | ✅ green |
| EMBED-05 (V4) | Re-embed job stays RLS-scoped (`eq(user_id)`); never touches another user's chunks | T-access | integration (live) | `pytest tests/integration/test_111_1_reembed_rls.py -x` | ✅ green |
| D-09 #1 | Stored `extraction_provider`/`embedding_provider` short-circuits name-inference | T-egress | unit | `pytest tests/unit/test_111_1_explicit_provider_routing.py -x` | ✅ green |
| D-09 #2 | `forced_emit` resolves local base_url + dummy key for lmstudio/ollama targets | T-egress | unit | `pytest tests/unit/test_111_1_forced_emit_local.py -x` | ✅ green |
| D-09 #3 | OpenRouter `:exacto`/plugin mangling does NOT fire for local slashed ids | T-egress | unit | `pytest tests/unit/test_111_1_no_mangle_local.py -x` | ✅ green |
| D-11/D-12 | Threshold + confidence buckets read from settings, not hardcoded | — | unit | `pytest tests/unit/test_111_1_threshold_portable.py -x` | ✅ green |
| EMBED-06 (SC) | Save with a changed model fires the confirm gate before commit | T-consent | manual (Chrome MCP) | — (sketch 025 attention contract) | 🔵 manual |
| SC#4 | Local embedder embeds+serves search; OpenAI→local re-embed; relevant cited results | T-egress | manual (live UAT) | — (see UAT matrix) | 🔵 manual |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky · 🔵 manual-only*

**Retired tests (operator-approved 2026-06-17 — stale-premise, no behavior gap):**
- `tests/unit/test_111_1_embedding_presets.py` — DELETED. Imported a backend `resolve_embedding_preset` that was **never built by design**: preset auto-fill (base_url/key/model/dims) lives in the frontend `ProviderPicker.tsx` preset map, and SEED-088 (next phase) replaces the hardcoded presets entirely. EMBED-01/02/03 preset resolution is COVERED by the frontend component + the live UAT matrix below.
- `test_111_1_local_embedding_resolve.py::test_local_embedder_uses_dummy_key` — DELETED. Asserted the backend `get_embedding_client` auto-injects a dummy local key; the shipped design has the frontend supply it. The two surviving base_url tests had their stale `xfail` markers removed (they were XPASS) and now assert as plain green.

---

## Cross-Provider / Local UAT Matrix (project UAT scoreboard recipe)

> This phase touches retrieval + provider routing, so the 4-axis bandwidth applies, adapted to embedders. Authored here in VALIDATION.md, NOT in PLAN.md tasks.

| Axis | Required coverage for 111.1 |
|------|------------------------------|
| Cross-embedder (cloud) | OpenAI (default) **+** one non-OpenAI cloud embedder (e.g. Google `gemini-embedding-001`) embed + serve search with relevant cited results |
| Local-embedder | At least one of Ollama `:11434` / LM Studio `:1234` embeds chunks + queries + serves search (privacy axis; closes the SEED-048 SPOF live) |
| Switch + re-embed | OpenAI → local switch fires D-03 confirm → background re-embed → search recovers (graceful-dip honesty visible per sketch 026) |
| Local extraction routing (BUG-260616-01) | A local model selected for extraction stays local (NOT shipped to OpenRouter) — **verified by endpoint, not just a green result** (data-egress regression guard) |
| Long-doc / many-chunk | A ≥ 50-chunk document fully re-embeds via the batched job; search returns correct cited results after completion |

---

## Wave 0 Requirements (complete — `wave_0_complete: true`)

- [x] ~~`tests/unit/test_111_1_embedding_presets.py`~~ — RETIRED (backend resolver never built by design; EMBED-01/02/03 covered by frontend `ProviderPicker` + UAT)
- [x] `tests/unit/test_111_1_local_embedding_resolve.py` — Ollama/LM Studio embedding-client base_url (EMBED-02) — ✅ 2/2 green (dummy-key sub-test retired)
- [x] `tests/unit/test_111_1_embed_chunks_creds.py` — `embed_chunks` threads `user_settings` (EMBED-04) — ✅ green
- [x] `tests/unit/test_111_1_explicit_provider_routing.py` — D-09 #1 — ✅ green
- [x] `tests/unit/test_111_1_forced_emit_local.py` — D-09 #2 — ✅ green
- [x] `tests/unit/test_111_1_no_mangle_local.py` — D-09 #3 — ✅ green
- [x] `tests/unit/test_111_1_threshold_portable.py` — D-11/D-12 — ✅ green
- [x] `tests/unit/test_111_1_reembed_kickoff.py` — EMBED-05 kickoff — ✅ green
- [x] `tests/integration/test_111_1_cross_embedder_roundtrip.py` — EMBED-04 client-build parity (live) — ✅ green
- [x] `tests/integration/test_111_1_match_filters_stale.py` — D-10 search filter (live) — ✅ green
- [x] `tests/integration/test_111_1_reembed_resume.py` + `test_111_1_resize_column.py` + `test_111_1_reembed_rls.py` — EMBED-05 (live) — ✅ green
- [x] Migration 073 applied live to :54322 + read-back (docs 30→30, 0 NULL embedding_model) — verified in Plan 04 SUMMARY + VERIFICATION truth #6
- [x] Framework install: none — pytest infra already present

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirm-gate fires before commit, reads as serious, all 5 facts legible | EMBED-06 / SC#3 | UI attention contract (sketch 025 D-03) — can't assert "felt seriousness" via wire | In Settings, change embedding model, hit Save → gate appears naming chunk count · target model · recall dip · ETA · irreversibility; Cancel = no change; Confirm = job kicks |
| Local embedder embeds + serves search end-to-end | SC#4 / EMBED-01/02 | Requires a running Ollama/LM Studio server + live ingest+search | Pick local preset, ingest a doc, run a chat search, confirm cited results from the local-embedded chunks |
| OpenAI→local switch re-embeds, search recovers | SC#4 / EMBED-05 | Live background job + recall-recovery over time | After switch + Confirm, watch sketch-026 status card; search degrades then recovers as job completes |
| Local extraction model stays local (no cloud egress) | BUG-260616-01 / D-09 | Must verify by endpoint/log, not green result | Select local extraction model, ingest; confirm request hit `:1234`/`:11434`, NOT OpenRouter |
| Re-embed in-progress honesty surface | EMBED-05 / D-04/D-05 | Visual graceful-dip + re-kick control (sketch 026) | Trigger re-embed; confirm "search is catching up" pointer + progress + re-kick for a failed/partial run |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (orphaned/stale-premise scaffolds retired, operator-approved)
- [x] No watch-mode flags
- [x] Feedback latency < 90s (unit 0.82s; integration 1.64s)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-06-16 (plan-time contract ratified by gsd-plan-checker) → **VALIDATED 2026-06-17** (post-execution audit: 42/42 tests green live on :54322, `wave_0_complete: true`, 2 stale-premise test sets retired with operator approval).

---

## Validation Audit 2026-06-17

| Metric | Count |
|--------|-------|
| Gaps found | 3 (bookkeeping only — no missing behavior) |
| Resolved | 3 (2 stale xfail markers → plain green; 4 orphaned tests retired) |
| Escalated | 0 |
| Automated tests green (live) | 42 (30 unit + 12 integration) |
| Manual-only items | EMBED-01/02/03 preset map + local dummy key (frontend `ProviderPicker` + UAT); EMBED-06 confirm gate; SC#4 local e2e; local-extraction endpoint; ReembedSearchPointer mount |

**Method:** State A audit. Plan-time per-task map (all `⬜ pending`) reconciled against the executed test suite, run live on `:54322`. gsd-nyquist-auditor applied the operator-approved disposition (fix 2 stale xfail markers → real green; retire 4 orphaned tests targeting backend symbols intentionally not built); orchestrator independently re-ran the full suite (42 passed, 0 xfail/0 xpass, 0 net-new failures) and confirmed the git state (presets file staged-deleted, resolve file modified to 2 clean green tests). No implementation/source file was modified. The live cross-embedder / local-route / switch→re-embed axes remain the MANUAL UAT matrix above (verified by endpoint, not a green unit result — by design; carried as verify-work truth #10 + the 4 HUMAN-UAT items).
