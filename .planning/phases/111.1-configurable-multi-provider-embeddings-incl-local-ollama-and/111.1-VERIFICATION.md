---
phase: 111.1-configurable-multi-provider-embeddings-incl-local-ollama-and
verified: 2026-06-17T08:00:00Z
status: human_needed
score: 9/10
overrides_applied: 0
human_verification:
  - test: "Local embedder full round-trip — pick Ollama (localhost:11434/v1) or LM Studio (localhost:1234/v1) in Settings, ingest a document, run a search in chat, confirm cited results come from local-embedded chunks."
    expected: "Chunks embedded via the local server, search returns cited results scoped to that vector space, no traffic to OpenAI or OpenRouter during the ingest/query."
    why_human: "Requires a running Ollama/LM Studio server with an embedding model loaded. Cannot test without live local infrastructure."
  - test: "OpenAI-to-local switch triggers re-embed and search recovers — in Settings change the embedding provider from openai to ollama, hit Save, confirm the gate (sketch 025), then watch the status card (sketch 026) show running → complete; verify search returns cited results after completion."
    expected: "Status card shows running then complete; 'nothing was lost' D-05 promise; search catches up; the slim ReembedSearchPointer (if mounted on Documents page) auto-hides on completion."
    why_human: "Requires a live local server plus a real corpus re-embed. The status-card running/complete state was deferred at the Plan 06 checkpoint (operator preserved the live corpus). Also validates the full D-04 graceful-dip honesty surface."
  - test: "Local extraction model stays local — select a local model (LM Studio/Ollama) as the extraction provider in Settings, ingest a document, confirm the extraction request hit localhost:1234 or localhost:11434, NOT OpenRouter."
    expected: "Backend log / network trace shows the extraction HTTP call went to the local endpoint, document text was never sent to a cloud provider."
    why_human: "D-09 BUG-260616-01 egress cure must be verified by endpoint, not just a green result. Cannot assert network destination without running infra."
  - test: "ReembedSearchPointer mounted on the Documents page — confirm that after a re-embed is triggered the slim 'search is catching up' pointer appears on the Documents search surface and auto-hides when the job completes."
    expected: "The pointer appears during the re-embed and disappears when remaining == 0. Deep-links to the status card in Settings."
    why_human: "ReembedSearchPointer is exported from ReembedStatusCard.tsx but is not yet mounted on the Documents page (a known follow-up from Plan 06). This is a 1-line consumer add but is not yet wired."
---

# Phase 111.1: Configurable / Multi-Provider Embeddings — Verification Report

**Phase Goal:** Retire the OpenAI embedding SPOF (SEED-048): admin can pick any embedding provider (incl. local Ollama / LM Studio), chunk-embedding and query-embedding always use the same configured creds, an existing-chunk re-embed fires on a confirmed model/dimension change, and local LLM/extractor routing no longer leaks document text to cloud providers.
**Verified:** 2026-06-17T08:00:00Z
**Status:** human_needed — all automated checks and 9/10 truths pass; 4 items require live local infrastructure or mounted UI component.
**Re-verification:** No — initial verification.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Admin can select an embedding provider (incl. Ollama, LM Studio) from Settings; local presets auto-fill base_url + dummy key + show green LOCAL tag | VERIFIED | `ProviderPicker.tsx` exists; preset map at lines 50-58 has ollama (`11434/v1`) + lmstudio (`1234/v1`) + `local: true` + `dummyKey`. Always-on endpoint footer at component root. Live UI approved by operator via Chrome MCP (Plan 06 Task 3). |
| 2 | Chunk-embedding and query-embedding use the SAME configured model + creds (EMBED-04 fixed) | VERIFIED | `embedding_service.py:112` signature `embed_chunks(chunks, model=None, user_settings=None)` threads user_settings to embed_texts. `documents.py:1491` calls `embed_chunks(..., user_settings=app_settings)`. Retrieval path at `retrieval_service.py:58` already correct. Unit tests `test_111_1_embed_chunks_creds.py` GREEN. Live cred-parity at client-build boundary proven in `test_111_1_cross_embedder_roundtrip.py` (commit a47f44d3). |
| 3 | A stored extraction_provider short-circuits model-name inference — slashed local id no longer mis-routes to openrouter (D-09 #1) | VERIFIED | `documents.py:1408`: `provider = (getattr(app_settings, "extraction_provider", "") or "").strip().lower() or (get_model_capability(model) or {}).get("provider")`. `test_111_1_explicit_provider_routing.py` GREEN (commit b60f3224). |
| 4 | forced_emit injects local base_url + dummy key for ollama/lmstudio targets (D-09 #2) | VERIFIED | `forced_emit.py:270-274`: local branch injects `lm-studio`/`ollama` dummy key and correct base_url. `test_111_1_forced_emit_local.py` GREEN (commit 5e628ddf). |
| 5 | OpenRouter :exacto / response-healing mangling fires ONLY when provider==openrouter, not on any slashed local model id (D-09 #3) | VERIFIED | `openai_service.py:1457`: gate changed to `if provider == "openrouter"`. `test_111_1_no_mangle_local.py` GREEN (commit 496d2569). |
| 6 | Migration 073 applied live to :54322; 4 app_settings cols + 2 document_chunks tag cols exist; backfill complete; match_document_chunks carries p_embedding_model; full-schema.sql regenerated | VERIFIED | Commit 8c4adece applied psycopg2-direct. `full-schema.sql:120` contains `p_embedding_model text DEFAULT NULL::text` and D-10 WHERE clause. `full-schema.sql:310-313` shows all 4 new app_settings cols. 7 ADD COLUMN IF NOT EXISTS in migration 073. Read-back: docs 30→30, 0 NULL embedding_model (Plan 04 Summary). |
| 7 | Batched, RLS-scoped, resumable, non-destructive re-embed job exists; resize_embedding_column gated on dims change; run_in_threadpool on all blocking calls; progress endpoints wired (EMBED-05) | VERIFIED | `reembed_service.py` exists with `reembed_job` + `reembed_progress` + `start_reembed`. `grep run_in_threadpool` = 8 occurrences. `eq("user_id", user_id)` on both read (line 119) and write (line 150). `resize_embedding_column` inside `if dims_changed:` only. `settings.py` has `background_tasks.add_task(start_reembed, ...)` at line 364, GET /settings/reembed-progress at line 386, POST /settings/reembed at line 402. `count="exact"` (lines 196, 206) — the live 1000-cap bug found and fixed (commit 3246629e). |
| 8 | D-03 destructive-change confirm gate fires before any embedding model/dim change commits; names all 5 facts; Cancel = no change (EMBED-06) | VERIFIED | `ReembedConfirmModal.tsx` exists with `role="alertdialog"`, 5 facts listed in file header (lines 16-20) and rendered in JSX (countLabel + eta + targetModel + recall-dip + reversibility). `SettingsPage.tsx:703` guard fires gate only on model/dim change. Live UI approval: Cancel left corpus at 1,839 chunks all `text-embedding-3-small`, 0 NULL, DB-verified (Plan 06 Summary). |
| 9 | Confidence buckets read from settings (confidence_bucket_high/medium), not hardcoded 0.54/0.38; back-compat when unset (D-12) | VERIFIED | `agent_loop.py:692-695`: reads `settings_obj.confidence_bucket_high` / `settings_obj.confidence_bucket_medium`; `getattr` with 0.54/0.38 defaults. `test_111_1_threshold_portable.py` GREEN. Call site at agent_loop:2346 passes `user_settings`. |
| 10 | Local embedder embeds + searches end-to-end; OpenAI→local switch re-embeds and search recovers; local extraction stays local (verified by endpoint) | NEEDS HUMAN | Requires running Ollama/LM Studio server. Automated unit/integration tests cover the code paths and cred resolution, but live end-to-end local embedding + the switch→re-embed→recovery flow require live local infrastructure (VALIDATION.md UAT axes SC#4 + Local extraction routing). ReembedSearchPointer built but not yet mounted on Documents page (Plan 06 follow-up #1). |

**Score:** 9/10 truths verified (1 needs human — live local infrastructure dependent)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/073_embedding_provider_and_chunk_tags.sql` | All phase DDL: 4 app_settings cols, 2 document_chunks tag cols, backfill, match_document_chunks p_embedding_model, resize_embedding_column | VERIFIED | Exists; 7 ADD COLUMN IF NOT EXISTS; DROP FUNCTION + CREATE OR REPLACE for match_document_chunks; RLS clause preserved; backfill of text-embedding-3-small/1536 |
| `supabase/full-schema.sql` | Regenerated from live DB; contains p_embedding_model | VERIFIED | 2942 lines; p_embedding_model at line 120; single match_document_chunks definition; 4 new app_settings cols at lines 310-313 |
| `backend/app/models/user_settings.py` | 4 new fields with env_attr=None readback | VERIFIED | Lines 164-167: typed fields; lines 520-523: `_build_settings_from_row` env_attr=None pattern |
| `backend/app/api/settings.py` | SettingsUpdate fields + save wiring + kickoff + progress + re-kick endpoints | VERIFIED | Lines 100-103: SettingsUpdate; lines 268-275: save block; line 364: `background_tasks.add_task(start_reembed,...)`; lines 386-417: GET + POST endpoints |
| `backend/app/api/documents.py` | extraction_provider short-circuit + embed_chunks user_settings pass + D-10 chunk tags | VERIFIED | Line 1408: explicit provider wins; line 1491: `user_settings=app_settings`; lines 1507-1508: embedding_model + embedding_dimensions in chunk_rows |
| `backend/app/services/embedding_service.py` | embed_chunks(user_settings=...) threading | VERIFIED | Line 112: `def embed_chunks(chunks, model=None, user_settings=None)` — forwards to embed_texts |
| `backend/app/services/retrieval_service.py` | p_embedding_model passed to match_document_chunks | VERIFIED | Line 58: `"p_embedding_model": current_model` in RPC args dict |
| `backend/app/services/agent_loop.py` | _compute_confidence reads buckets from settings | VERIFIED | Lines 692-695: reads confidence_bucket_high / medium from settings_obj; call site line 2346 passes user_settings |
| `backend/app/services/forced_emit.py` | Local-aware injection branch | VERIFIED | Lines 270-274: `if provider in ("ollama", "lmstudio"):` branch injects dummy key + local base_url |
| `backend/app/services/openai_service.py` | OpenRouter mangling gated on provider==openrouter | VERIFIED | Line 1457: `if provider == "openrouter":`; old slash-gate removed |
| `backend/app/services/reembed_service.py` | reembed_job + reembed_progress + start_reembed | VERIFIED | Created by Plan 05; 8x run_in_threadpool; eq(user_id) on read + write; dims_changed gates resize; count="exact" for accurate total (1000-cap bug fixed) |
| `frontend/src/components/settings/ProviderPicker.tsx` | Reusable picker, always-on endpoint footer, local presets | VERIFIED | Exists; ollama (11434) + lmstudio (1234) in preset map; local:true + dummyKey; endpoint footer in component |
| `frontend/src/components/settings/ReembedConfirmModal.tsx` | D-03 5-fact confirm gate | VERIFIED | Exists; role="alertdialog"; 5 facts enumerated; chunkCount prop + estimateEta; deep midnight tokens |
| `frontend/src/components/settings/ReembedStatusCard.tsx` | Running/partial/failed/complete states + Re-embed-now + reconcile-on-fetch | VERIFIED | Exists; getReembedProgress + kickReembed called; running/complete/isFailedish states; reconcile-on-fetch light poll |
| `frontend/src/pages/SettingsPage.tsx` | Two ProviderPicker instances + modal + status card wired | VERIFIED | Lines 1023 + 1047: two ProviderPicker instances (embedding + extraction); line 772: ReembedConfirmModal; line 1069: ReembedStatusCard; line 703: confirm-on-save guard |
| `frontend/src/lib/api.ts` | embedding_provider / extraction_provider types + getReembedProgress + kickReembed | VERIFIED | Lines 1549-1550, 1603-1604: provider fields; lines 1671-1679: getReembedProgress() + kickReembed() |
| `.planning/phases/111.1-.../111.1-PROVIDER-PRESETS.md` | Curated preset map; D-08 default preserved; honest UNVERIFIED marks | VERIFIED | Exists; text-embedding-3-small/1536 D-08 default; ollama 11434 + lmstudio 1234; UNVERIFIED marks on local/Jina/Cohere/Mistral/Voyage |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `backend/app/api/documents.py extraction routing` | `app_settings.extraction_provider` | `getattr(app_settings, "extraction_provider", "")` wins over name-inference | WIRED | Line 1408 confirmed |
| `backend/app/api/documents.py chunk-embed` | `embedding_service.embed_chunks` | `user_settings=app_settings` kwarg (mirrors retrieval_service.py) | WIRED | Line 1491 confirmed |
| `backend/app/api/documents.py chunk_rows` | `document_chunks.embedding_model / .embedding_dimensions` | Explicit dict keys at lines 1507-1508 | WIRED | D-10 write path |
| `backend/app/services/retrieval_service.py _vector_search` | `match_document_chunks(p_embedding_model=...)` | Line 58 adds to RPC args dict | WIRED | D-10 stale-model filter |
| `backend/app/api/settings.py update_settings` | `reembed_service.start_reembed` | `background_tasks.add_task(start_reembed, ...)` line 364-365 | WIRED | Kickoff on confirmed model/dim change |
| `frontend/src/pages/SettingsPage.tsx Save` | `ReembedConfirmModal` | Guard at line 703; modal fires before PUT on model/dim change | WIRED | D-02/D-03 consent gate |
| `frontend/src/components/settings/ReembedStatusCard.tsx` | `GET /settings/reembed-progress` + `POST /settings/reembed` | `getReembedProgress()` + `kickReembed()` from api.ts | WIRED | Reconcile-on-fetch |
| `frontend/src/components/settings/ReembedStatusCard.tsx ReembedSearchPointer` | Documents page | Export at line 230 — NOT yet imported/used on Documents page | ORPHANED | Known follow-up; the component exists but is not mounted. Non-blocking per the phase's own tracking. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `ProviderPicker.tsx` | `preset` / `value.base_url + model + dims` | Hardcoded preset map read from PROVIDER-PRESETS.md curation; populated at select onChange | Yes — preset map has real validated IDs | VERIFIED |
| `ReembedStatusCard.tsx` | `progress` (total / re_embedded / remaining / status) | `getReembedProgress()` → GET /settings/reembed-progress → `reembed_progress()` → live `document_chunks` DB queries (count="exact") | Yes — DB-derived counts; count=exact fix verified (commit 3246629e, was 1000, now 1839) | VERIFIED |
| `ReembedConfirmModal.tsx` | `chunkCount` | Prop from SettingsPage; SettingsPage calls `getReembedProgress()` to read live total | Yes — same live DB count endpoint | VERIFIED |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| migration 073 p_embedding_model in full-schema.sql | `grep -q "p_embedding_model" supabase/full-schema.sql` | match confirmed | PASS |
| embed_chunks has user_settings param | `grep -n "def embed_chunks.*user_settings" embedding_service.py` | line 112 match | PASS |
| reembed_service has start_reembed | file + function grep | `start_reembed` at line 238 | PASS |
| ProviderPicker has ollama (11434) and lmstudio (1234) | grep on component | lines 53-54 confirmed | PASS |
| settings.py has background_tasks.add_task(start_reembed) | grep | line 365 confirmed | PASS |
| ReembedConfirmModal wired into SettingsPage | grep on SettingsPage imports | line 20 + 772 | PASS |
| run_in_threadpool count in reembed_service >= 3 | `grep -c run_in_threadpool reembed_service.py` | 8 | PASS |
| count="exact" in reembed_service (not len()) | `grep -n 'count="exact"' reembed_service.py` | lines 196, 206 | PASS |
| All 21 task commits exist in git log | `git log --oneline <hashes>` | 21/21 confirmed | PASS |
| Local embedder full round-trip | Requires running Ollama/LM Studio | Cannot test without local server | SKIP (human) |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| EMBED-01 | Plans 01, 02, 04, 06 | Admin can select embedding provider from Settings (incl. local) | SATISFIED | ProviderPicker.tsx with preset select; local presets auto-fill; always-on endpoint footer; live UI approved |
| EMBED-02 | Plans 01, 06 | Local preset auto-fills base_url + dummy key | SATISFIED | ollama (11434/v1 + dummy "ollama"), lmstudio (1234/v1 + dummy "lm-studio") in preset map; LM Studio not double-appended |
| EMBED-03 | Plans 01, 04, 06 | Advanced overrides (model/dims/base_url/key) remain editable; preset auto-fills map | SATISFIED | ProviderPicker keeps editable advanced fields; PROVIDER-PRESETS.md is the auto-fill source |
| EMBED-04 | Plans 01, 03, 04 | embed_chunks and query-embedding use the SAME creds | SATISFIED | embed_chunks(user_settings=...) fix; documents.py passes user_settings=app_settings; live cred-parity test GREEN |
| EMBED-05 | Plans 01, 03, 04, 05 | Changing embedding model/dim triggers guarded RLS-scoped re-embed; resize_embedding_column wired | SATISFIED | reembed_service.py; kickoff in settings.py; 3 live integration tests GREEN (resume + RLS + resize); count=exact fix |
| EMBED-06 | Plans 01, 06 | Destructive-change confirm names the re-embed before any commit | SATISFIED | ReembedConfirmModal with role="alertdialog"; 5 facts; Cancel=corpus-untouched DB-verified |
| UX-01 (cross-cutting) | Plan 06 | Deep Midnight / Aether tokens; mobile-responsive; WCAG 2.1 AA | SATISFIED (partial) | Deep Midnight tokens + alertdialog role + focus-to-action confirmed at live checkpoint; local-embedder and switch+re-embed surface need live UAT to fully satisfy the "lived experience" UX bar (G-4) |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `frontend/src/components/settings/ProviderPicker.tsx` | 147, 237, 261, 272 | `placeholder=...` HTML input attribute | Info | These are HTML input placeholders, NOT code stubs. The values flow to UI as hint text, not to rendering logic. Not a stub. |
| `frontend/src/components/settings/ReembedStatusCard.tsx` | 230 | `export function ReembedSearchPointer` | Warning | Exported but not imported on the Documents page. The pointer exists and works — it's simply unmounted. Plan 06 Follow-up #1 explicitly tracks this. Non-blocking. |

No blocker anti-patterns found. No TODO/FIXME/placeholder in the implementation logic of any phase artifact.

---

### Human Verification Required

#### 1. Local Embedder Full Round-Trip (SC#4 / EMBED-01/02)

**Test:** With Ollama or LM Studio running and an embedding model loaded, in Settings pick the Ollama (or LM Studio) preset, confirm the endpoint footer shows `localhost:11434/v1` (or `:1234/v1`) with the LOCAL tag. Ingest a document. Run a chat query that searches the knowledge base. Confirm cited results come from chunks embedded by the local server.

**Expected:** Local embedding runs without an API key; results are cited; no traffic to OpenAI or OpenRouter during the ingest/query cycle.

**Why human:** Requires a running local server with a loaded embedding model — cannot automate without live local infrastructure.

#### 2. OpenAI-to-Local Switch + Re-embed Recovery (SC#4 / EMBED-05 full)

**Test:** With Ollama/LM Studio running, switch the embedding provider from OpenAI to Ollama in Settings, hit Save, pass the confirm gate (verify all 5 facts are legible), and watch the ReembedStatusCard (sketch 026) in Settings show the job running then complete. Verify search returns relevant cited results after the job finishes. Also verify that "search is catching up" graceful-dip honesty surface is visible during the job.

**Expected:** Status card shows running → complete; "nothing was lost" D-05 promise visible; search degrades during re-embed (recall dip is honest) and recovers to full recall on completion.

**Why human:** Requires a live local server plus a real corpus re-embed. The status-card running/complete states require a real job; operator explicitly deferred this at the Plan 06 G-4 checkpoint to protect the live corpus.

#### 3. Local Extraction Stays Local — Endpoint Verification (BUG-260616-01 / D-09)

**Test:** In Settings set the extraction provider to Ollama or LM Studio (a local model). Ingest a document. Examine the backend uvicorn logs or network trace to confirm the extraction HTTP request went to `localhost:1234` or `localhost:11434`, NOT to `api.openai.com` or `openrouter.ai`.

**Expected:** Extraction request visible in local server logs. No document text shipped to any cloud provider.

**Why human:** D-09 data-egress cure must be verified by endpoint (not just a green result). Unit tests confirm the code path; endpoint-level proof requires live extraction against a local server.

#### 4. ReembedSearchPointer Mounted on Documents Page

**Test:** After mounting `ReembedSearchPointer` on the Documents/search surface (Plan 06 Follow-up #1 — 1-line consumer add), trigger a re-embed. Confirm the slim "search is catching up" pointer appears on the Documents page and auto-hides when the job completes.

**Expected:** Pointer appears during the re-embed and disappears when `remaining == 0`. Deep-links to the ReembedStatusCard in Settings.

**Why human:** The component exists and is exported but is not yet imported on the Documents page. This is a known follow-up that requires the 1-line mounting and a live re-embed to verify.

---

### Gaps Summary

No gaps blocking goal achievement. All 9 automated/verifiable truths pass. The 4 human verification items are live-infrastructure-dependent UAT axes (SC#4 cross-local-embedder, switch+re-embed, extraction egress proof) and one known unmounted component (ReembedSearchPointer). These are explicitly tracked in the phase's own follow-up list and VALIDATION.md manual-only matrix — they are not implementation omissions.

The one known deviation: `ReembedSearchPointer` is built and exported but not yet mounted on the Documents page. This is non-blocking by the phase's own tracking (Plan 06 Follow-up #1). The pointer works when mounted; the mounting is a 1-line consumer add.

---

### Per-Requirement EMBED Verdicts

| Requirement | Verdict | Key Evidence |
|-------------|---------|-------------|
| EMBED-01 | SATISFIED | ProviderPicker with preset select + always-on endpoint footer; live UI approved |
| EMBED-02 | SATISFIED | Local presets auto-fill base_url + dummy key; LM Studio not double-appended; local tag rendered |
| EMBED-03 | SATISFIED | Advanced fields remain editable; PROVIDER-PRESETS.md is the auto-fill source |
| EMBED-04 | SATISFIED | embed_chunks threads user_settings; unit tests GREEN; live cred-parity proven at client-build boundary |
| EMBED-05 | SATISFIED | reembed_service.py: resumable + RLS-scoped + non-destructive; 3 live integration tests GREEN; count=exact fix; settings kickoff + progress/re-kick endpoints |
| EMBED-06 | SATISFIED | ReembedConfirmModal: alertdialog + 5 facts + Cancel=corpus-untouched DB-verified |

---

_Verified: 2026-06-17T08:00:00Z_
_Verifier: Claude (gsd-verifier)_
