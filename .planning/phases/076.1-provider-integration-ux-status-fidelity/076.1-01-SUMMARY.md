---
phase: 076.1-provider-integration-ux-status-fidelity
plan: 01
subsystem: api
tags: [deepseek, moonshot, minimax, zhipu, provider-routing, openai-compat, config]

# Dependency graph
requires:
  - phase: 075.4-cross-provider-cleanup-per-thread-state
    provides: "MODEL_CAPABILITIES registry-or-inference pattern, _INFERENCE_PATTERNS, _PROVIDER_BASE_URLS single source of truth, UnknownProviderError"
provides:
  - "_PROVIDER_BASE_URLS entries for deepseek, moonshot, minimax, zhipu"
  - "MODEL_CAPABILITIES entries for 8 models across 4 new direct providers"
  - "_INFERENCE_PATTERNS for ^deepseek-, ^kimi-, ^moonshot-, ^minimax-, ^glm- prefixes"
  - "_SUB_AGENT_MODEL_DEFAULTS for 4 new providers"
  - "KNOWN_PROVIDERS entries for Settings UI provider list"
  - "Settings class fields: 4 API keys + 4 model lists + key_map entries"
  - ".env.example documentation for all 4 new providers"
affects: [076.1-02 (frontend Settings UI), 076.1-03 (UX status fidelity), future provider phases]

# Tech tracking
tech-stack:
  added: []
  patterns: ["OpenAI-compat base_url switching for 4 additional providers — same pattern as existing 5 providers"]

key-files:
  created: []
  modified:
    - "backend/app/config.py"
    - "backend/app/models/user_settings.py"
    - "backend/.env.example"

key-decisions:
  - "All 4 providers use existing openai_service.py via base_url switching — zero new service files"
  - "Existing OpenRouter-routed entries preserved — users can still route deepseek/minimax/etc via OpenRouter"
  - "New providers NOT added to _BIG_3_PROVIDERS — unknown model IDs get native_tools=False conservatively"
  - "_build_providers registry merge extended to include 4 new providers for auto-model-list population"

patterns-established:
  - "Extensible provider registration: add _PROVIDER_BASE_URLS + MODEL_CAPABILITIES + _SUB_AGENT_MODEL_DEFAULTS + Settings field + key_map = done"

requirements-completed: [UX-STATUS-01, SEED-031]

# Metrics
duration: 3min
completed: 2026-05-25
---

# Phase 076.1 Plan 01: Backend Provider Config Registration Summary

**Registered 4 curated direct providers (DeepSeek, Moonshot, MiniMax, Zhipu) across all backend config surfaces — 8 model entries, inference patterns, sub-agent defaults, and Settings fields — enabling direct API routing without OpenRouter**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-25T17:12:48Z
- **Completed:** 2026-05-25T17:16:07Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- All 4 providers registered in `_PROVIDER_BASE_URLS` with their direct API endpoints
- 8 model entries in `MODEL_CAPABILITIES` with verified timeouts and max_output_tokens (deepseek-v4-pro, deepseek-chat, deepseek-reasoner, kimi-k2.6, moonshot-v1-8k, minimax-m2.7, glm-4-plus, glm-4-flash)
- Inference pattern matching works: unknown `deepseek-*`, `kimi-*`, `moonshot-*`, `minimax-*`, `glm-*` model IDs resolve to correct providers
- Sub-agent defaults set: cheap provider-specific models instead of falling back to main model
- KNOWN_PROVIDERS extended with display names for Settings UI consumption
- `.env.example` documented with all API key and model list entries

## Task Commits

Each task was committed atomically:

1. **Task 1: Register 4 providers in backend/app/config.py** - `ce05de9` (feat)
2. **Task 2: Register 4 providers in user_settings.py + .env.example** - `cc4c056` (feat)

## Files Created/Modified
- `backend/app/config.py` - Added 4 providers to _PROVIDER_BASE_URLS, PROVIDER_CONTEXT_DEFAULTS, MODEL_CONTEXT_DEFAULTS, MODEL_CAPABILITIES (8 models), _INFERENCE_PATTERNS (5 new patterns), _INFERRED_DEFAULT_MAX_TOKENS, _SUB_AGENT_MODEL_DEFAULTS; added Settings fields (4 API keys + 4 model lists + key_map entries)
- `backend/app/models/user_settings.py` - Added 4 providers to KNOWN_PROVIDERS dict; extended _build_providers registry merge to include new providers
- `backend/.env.example` - Added DEEPSEEK_API_KEY, MOONSHOT_API_KEY, MINIMAX_API_KEY, ZHIPU_API_KEY + model list entries

## Decisions Made
- All 4 providers use existing `openai_service.py` via base_url switching — zero new service code, consistent with D-05b from CONTEXT.md
- Existing OpenRouter-routed entries (`deepseek/deepseek-chat`, `moonshotai/kimi-k2.6`, etc.) preserved unchanged — no regression for OpenRouter users
- New providers NOT added to `_BIG_3_PROVIDERS` — unknown model IDs for these providers conservatively get `native_tools=False` via inference defaults; registered models explicitly set `native_tools=True`
- `_build_providers` registry merge extended from 3 to 7 providers so auto-model-list population works for new providers too

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None - all config entries are fully wired with real values.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Users set their own API keys via `.env` or the Settings UI when Plan 02 (frontend) ships.

## Next Phase Readiness
- Backend fully wired: selecting any of the 4 new providers routes through their direct APIs
- Plan 02 (frontend Settings UI entries) can now read KNOWN_PROVIDERS and render provider tabs
- Plan 03 (UX status fidelity) is independent of this plan

## Self-Check: PASSED

- All 3 modified files exist on disk
- Both task commits (ce05de9, cc4c056) verified in git log
- SUMMARY.md exists at expected path

---
*Phase: 076.1-provider-integration-ux-status-fidelity*
*Completed: 2026-05-25*
