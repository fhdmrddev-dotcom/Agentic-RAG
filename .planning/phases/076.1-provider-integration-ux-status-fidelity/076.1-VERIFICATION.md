---
phase: 076.1-provider-integration-ux-status-fidelity
verified: 2026-05-25T22:45:00Z
status: human_needed
score: 12/13 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Auto-scroll to active tool panel during TTFT gap"
    expected: "During execute_code tool arg generation, viewport auto-scrolls to show streaming code panel within 2s of tool_preparing firing. No jitter from competing scrollIntoView calls (WR-03)."
    why_human: "Requires a live streaming run to observe scroll behavior. Code review flagged two competing scrollIntoView calls (WR-03) that may cause visual jitter — only observable in a real browser."
  - test: "Google Gemini atomic args honest UX"
    expected: "During a Google Gemini run, the preparing state shows 'Waiting for model...' with spinner — not an empty code panel or '0.0 KB' badge."
    why_human: "Requires a Google Gemini API key and a live streaming run to verify atomic delivery handling."
  - test: "Text dedup for repeated narration (Anthropic/DeepSeek)"
    expected: "An Anthropic Sonnet or DeepSeek run that produces 4x repeated narration renders each paragraph exactly once."
    why_human: "Requires triggering the specific 4x narration pattern with a live provider — only reproducible with actual LLM output."
  - test: "Failure reason surfacing for failed runs"
    expected: "Failed run shows 'failed - timed out' or similar categorized reason (live run), not just 'failed'. After page reload, shows graceful 'failed' fallback."
    why_human: "Requires triggering a real failure (timeout, error) and observing the RunCard badge. Then reload to verify degradation."
  - test: "Sticky timer bar visibility during long sub-agent panels"
    expected: "While scrolled to bottom of a long sub-agent extraction panel, the sticky timer bar remains visible above the input box showing elapsed time, step count, and tool description."
    why_human: "Requires a live multi-tool run with enough content to scroll. Only verifiable by observing the fixed-position bar."
  - test: "Cross-provider Chrome MCP UAT (SC13)"
    expected: "DBA PPTX generation across at least 3 providers (OpenAI + Anthropic + one direct-integrated): no 10s+ visible stall, status labels match backend state, failure reasons surface."
    why_human: "ROADMAP SC13 explicitly requires Chrome MCP UAT across 3 providers. Cannot be verified programmatically."
  - test: "Direct provider routing verification"
    expected: "With DEEPSEEK_API_KEY set, selecting deepseek-chat routes through api.deepseek.com (verified by backend log). Same for each provider."
    why_human: "Requires real API keys and a running backend to verify HTTP routing via logs."
---

# Phase 076.1: Provider Integration + UX Status Fidelity Verification Report

**Phase Goal:** Two-scope phase: (1) UX Status Fidelity -- make the agent run always feel alive (auto-scroll, status labels, failure reasons, text dedup, sticky timer, Google atomic args, file count), and (2) Provider Architecture -- add 4 direct OpenAI-compatible provider integrations (DeepSeek, Moonshot, MiniMax, Zhipu) with per-provider sub-agent defaults and timeout profiles.
**Verified:** 2026-05-25T22:45:00Z
**Status:** human_needed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | During execute_code TTFT gap, viewport auto-scrolls to show tool panel with streaming code | VERIFIED (code) | MessageList.tsx:90-106 useEffect queries `[data-tool-status="preparing"]` and calls `scrollIntoView({ behavior: 'smooth', block: 'nearest' })`. Guard: `isNearBottomRef.current` (D-01). ToolCallPanel.tsx:549 sets `data-tool-status={tc.status}`. WR-03 flags competing scroll calls -- needs human visual check. |
| 2 | Status labels: "Generating code" during preparing, "Executing code" during running, "queued" replaced with elapsed | VERIFIED | ExecuteCodeBody.tsx:268 has `isPreparing ? "Generating code" : isRunning ? "Executing code"`. Generic tools show "Running {toolLabel}" at ToolCallPanel.tsx:620. RunCard.tsx: zero matches for "queued" -- replaced with elapsed counter `Math.floor(elapsedMs / 1000)` at line 303. |
| 3 | Failed runs surface error category (not just "failed") | VERIFIED (code) | errorCategories.ts exports `categorizeError()` with 6 regex patterns. StreamsProvider.tsx captures `errorPayload` as `runError` in both onTerminal handlers (lines 960-964, 1161-1165). RunCard.tsx imports `categorizeError` and calls `statusWord(runStatus, runError)` at lines 126-127 and 235. Title tooltip at line 234 shows raw error. |
| 4 | Consecutive identical text blocks deduplicated (Anthropic/DeepSeek 4x pattern) | VERIFIED (code) | MessageItem.tsx:30-43 `dedupParagraphs()` splits by `\n\n`, collapses consecutive identical paragraphs >20 chars. Applied only to assistant messages at line 195. Raw data in StreamsProvider unchanged. |
| 5 | Google atomic args: "Waiting for model..." with spinner, not empty panel | VERIFIED (code) | ToolCallPanel.tsx:676-681 renders "Waiting for model..." with Loader2 spinner when `tc.status === "preparing" && !argsCodeText && !argsBytesStreamed`. Correctly handles atomic delivery pattern. |
| 6 | Sticky elapsed timer visible regardless of scroll position | VERIFIED | ChatArea.tsx:416-419 renders `StickyTimerBar` between MessageList and input during streaming. Component at lines 428-478 shows elapsed time (M:SS or Ns), step count, file count, tool description. Outside ScrollArea so always visible. |
| 7 | Running file count visible during multi-batch runs | VERIFIED | RunCard.tsx:97-106 computes `fileCount` from `tc.result` JSON parsing of `output_files`. Rendered at line 199-202 in header. Also in StickyTimerBar (ChatArea.tsx:436-444). |
| 8 | No new SSE event types or fields added | VERIFIED | `git diff HEAD~8..HEAD` on backend SSE emission code (threads.py, openai_service.py, anthropic_service.py, google_service.py) shows zero new event types and zero new fields. |
| 9 | 4 direct providers integrated: DeepSeek, Moonshot, MiniMax, Zhipu | VERIFIED | config.py: `_PROVIDER_BASE_URLS` has all 4 with correct URLs (lines 16-19). `MODEL_CAPABILITIES` has 8 model entries across 4 providers (lines 212-223). `_INFERENCE_PATTERNS` has 5 new patterns for ^deepseek-, ^kimi-, ^moonshot-, ^minimax-, ^glm- (lines 258-262). Settings class has 4 API key fields + 4 model list fields + key_map entries (lines 495-544). |
| 10 | OpenRouter kept as generic fallback; existing models unchanged | VERIFIED | Existing OpenRouter model entries at config.py:226-237 unchanged. `_INFERENCE_PATTERNS` catch-all `*/*` pattern at line 264 still routes unknown org/model patterns to OpenRouter. |
| 11 | Per-provider sub-agent defaults set | VERIFIED | `_SUB_AGENT_MODEL_DEFAULTS` at config.py:469-472: deepseek->deepseek-chat, moonshot->moonshot-v1-8k, minimax->minimax-m2.7, zhipu->glm-4-flash. |
| 12 | Per-provider timeout profiles in MODEL_CAPABILITIES | VERIFIED | deepseek-v4-pro: 900s, deepseek-chat: 120s, kimi-k2.6: 600s, moonshot-v1-8k: 120s, minimax-m2.7: 300s, glm-4-plus: 300s, glm-4-flash: 120s. |
| 13 | Chrome MCP UAT across 3 providers: no 10s+ stall, labels match state, failure reasons surface | ? NEEDS HUMAN | ROADMAP SC13 requires live UAT across at least 3 providers. Cannot be verified programmatically. |

**Score:** 12/13 truths verified (1 needs human UAT)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/app/config.py` | 4 providers in _PROVIDER_BASE_URLS, MODEL_CAPABILITIES, _INFERENCE_PATTERNS, _SUB_AGENT_MODEL_DEFAULTS, Settings fields | VERIFIED | All registries populated. 8 model entries, 5 inference patterns, 4 sub-agent defaults, 4 API keys, 4 model lists, key_map entries. |
| `backend/app/models/user_settings.py` | KNOWN_PROVIDERS entries + _build_providers merge for 4 new providers | VERIFIED | Lines 38-41 have all 4 KNOWN_PROVIDERS entries. Line 277 extends merge to 7 providers. |
| `backend/.env.example` | API key + model entries for 4 providers | VERIFIED | Lines 91-94 (API keys), lines 108-111 (model lists). |
| `frontend/src/pages/SettingsPage.tsx` | PROVIDER_META entries + OpenRouter relabel + isNativeProvider | VERIFIED | Lines 21-25 (4 new PROVIDER_META entries + OpenRouter experimental). Line 492 (isNativeProvider includes 7 providers). |
| `frontend/src/components/chat/MessageList.tsx` | Auto-scroll useEffect for tool_preparing | VERIFIED | Lines 90-106. Queries `[data-tool-status="preparing"]`, calls scrollIntoView with smooth/nearest. |
| `frontend/src/components/chat/ToolCallPanel.tsx` | data-tool-status attribute + status labels + Google atomic UX | VERIFIED | Line 549 (data-tool-status). Line 620 (Running {toolLabel}). Lines 676-681 (Waiting for model...). |
| `frontend/src/components/chat/RunCard.tsx` | Elapsed counter replacing "queued" + failure reason + file count | VERIFIED | Line 303 (elapsed counter). Lines 347-356 (statusWord with categorizeError). Lines 97-106 + 199-202 (file count). |
| `frontend/src/components/chat/ChatArea.tsx` | StickyTimerBar between message list and input | VERIFIED | Lines 416-419 (rendering). Lines 428-478 (component implementation with elapsed, step, files, description). |
| `frontend/src/lib/errorCategories.ts` | categorizeError utility | VERIFIED | New file. 6 regex patterns + default fallback. Exports categorizeError function. |
| `frontend/src/types/index.ts` | runError field on Message type | VERIFIED | Line 152: `runError?: string`. |
| `frontend/src/providers/StreamsProvider.tsx` | runError captured from SSE terminal errorPayload | VERIFIED | Both onTerminal handlers capture errorPayload (lines 960-964 reconcile path, lines 1161-1165 sendMessage path). |
| `frontend/src/components/chat/MessageItem.tsx` | dedupParagraphs utility applied to assistant messages | VERIFIED | Lines 30-43 (function). Line 195 (applied to assistant messages only). |
| `frontend/src/components/chat/ToolArgsLivePanel.tsx` | Title with description injection | VERIFIED | Title prop consumed from ToolCallPanel.tsx:718-720 which injects `tc.args.description`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| config.py::_PROVIDER_BASE_URLS | openai_service.py::get_llm_client | Settings.resolve_llm_provider reads base_url | WIRED | key_map at config.py:535-544 maps all 9 providers. get_llm_client at openai_service.py:529 consumes Settings. |
| config.py::MODEL_CAPABILITIES | threads.py | get_model_capability lookup | WIRED | 8 new model entries with provider fields. get_model_capability resolves registered or inferred capability. |
| user_settings.py::KNOWN_PROVIDERS | settings.py::_build_response | _build_providers iterates KNOWN_PROVIDERS | WIRED | Line 277 extends registry merge to include 4 new providers. |
| SettingsPage.tsx::PROVIDER_META | backend settings API | provider.id maps to PROVIDER_META labels | WIRED | Frontend PROVIDER_META has all 9 entries matching backend KNOWN_PROVIDERS. |
| MessageList.tsx auto-scroll | ToolCallPanel.tsx data-tool-status | querySelector targets preparing elements | WIRED | MessageList.tsx:101 queries `[data-tool-status="preparing"]`. ToolCallPanel.tsx:549 sets attribute. |
| StickyTimerBar in ChatArea | MessageList/input layout | Rendered between message list and input | WIRED | Lines 416-419 insert StickyTimerBar conditionally between MessageList and inputBar. |
| errorCategories.ts::categorizeError | RunCard.tsx | Import and call in statusWord | WIRED | RunCard.tsx:7 imports. Lines 350,354 call categorizeError(runError).shortLabel. |
| StreamsProvider.tsx::runError | RunCard.tsx | Message.runError read by RunCard | WIRED | StreamsProvider stores errorPayload as runError. RunCard passes message.runError to statusWord at lines 126-127, 235. |
| MessageItem.tsx::dedupParagraphs | MarkdownRenderer | Applied before rendering content | WIRED | Line 195: `dedupParagraphs(message.content)` passed to MarkdownRenderer for assistant messages. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| StickyTimerBar | message.tool_calls | StreamsProvider SSE events | Yes -- tool_calls populated from SSE tool_preparing/tool_start/tool_end | FLOWING |
| RunCard fileCount | tc.result (JSON) | SSE tool_end populates result | Yes -- JSON parsed from completed tool results | FLOWING |
| RunCard runError | errorPayload from SSE terminal | StreamsProvider onTerminal handlers | Yes -- captured from real SSE error/timed_out events | FLOWING (live runs only; historical shows fallback) |
| errorCategories | error string | SSE errorPayload | Yes -- regex matches on real error strings | FLOWING |
| dedupParagraphs | message.content | SSE delta concatenation | Yes -- applied to accumulated assistant content | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript compilation | `cd frontend && npx tsc --noEmit` | Zero errors | PASS |
| Backend config imports cleanly | Verified via grep: all 4 providers in _PROVIDER_BASE_URLS, MODEL_CAPABILITIES, _SUB_AGENT_MODEL_DEFAULTS, _INFERENCE_PATTERNS | All present | PASS |
| errorCategories.ts exists and exports categorizeError | File exists at frontend/src/lib/errorCategories.ts, exports function | Confirmed | PASS |
| "queued" removed from RunCard | `grep "queued" RunCard.tsx` | Zero matches | PASS |
| No new SSE events in backend | git diff on SSE emission code | Zero new events/fields | PASS |
| All commits present | git log shows ce05de9, cc4c056, 78ac846, c217a76, a2b6999, 2691c7c, 74062c5, dbc3a24 | All 8 task commits verified | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| UX-STATUS-01 | Plans 01, 03, 04 | UX status fidelity features | SATISFIED | Auto-scroll, status labels, sticky timer, failure reasons, text dedup, Google atomic args, file count -- all implemented across Plans 03-04. |
| SEED-031 | Plans 01, 02 | Direct provider SDK integrations | SATISFIED | 4 providers (DeepSeek, Moonshot, MiniMax, Zhipu) registered in backend config + Settings UI. All use existing openai_service.py via base_url switching. |
| SEED-030 | Plans 03, 04 | Streaming silence gap UX | SATISFIED | Auto-scroll surfaces streaming code during TTFT gaps. Sticky timer provides continuous alive signal. Status labels differentiate preparing/running states. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| RunCard.tsx | 100 | `tc.status === "running"` in fileCount filter -- running tools have no result yet | Warning | Non-blocking: JSON.parse catch handles gracefully. Inconsistent with StickyTimerBar filter. |
| ChatArea.tsx | 430 | `tc.status === "completed"` -- dead code, "completed" is not a valid ToolCall.status | Warning | Non-blocking: dead filter arm, completedCount still correct via "done" match. |
| MessageList.tsx | 70-106 | Two useEffects with identical `[messages, isStreaming]` dependencies -- competing scrollIntoView calls | Warning | Potential UX jitter: "instant" bottom-scroll vs "smooth" preparing-scroll on same render cycle. Human verification needed. |
| user_settings.py | 32-42 | KNOWN_PROVIDERS duplicates _PROVIDER_BASE_URLS base URLs | Warning | Maintenance debt: D-075.4-B2 mandated single source of truth. Adding a provider now requires two file edits. Not a functional bug. |

### Human Verification Required

### 1. Auto-scroll to active tool panel during TTFT gap

**Test:** Select a model (e.g., GPT 5.4 or Claude Sonnet), send a prompt triggering execute_code (e.g., "Write a Python script that prints the first 20 prime numbers"). During code generation (preparing state), observe whether the viewport scrolls to show the code panel with streaming code.
**Expected:** The tool panel with streaming code is visible within 2s of tool_preparing firing. No visual jitter from competing scrollIntoView calls.
**Why human:** Requires a live streaming run to observe scroll behavior. Code review WR-03 flagged two competing scrollIntoView calls that may cause visual jitter.

### 2. Google Gemini atomic args honest UX

**Test:** Switch to a Google Gemini model, send a code generation prompt.
**Expected:** During preparing state, shows "Waiting for model..." with spinner -- not an empty code panel or "0.0 KB" badge.
**Why human:** Requires a Google Gemini API key and a live streaming run. Atomic delivery pattern only reproducible with real Google API.

### 3. Text dedup for repeated narration

**Test:** With Anthropic or DeepSeek configured, send a multi-step prompt (e.g., "Create a PPTX presentation about climate change with 5 slides").
**Expected:** If the model repeats narration paragraphs 4x, only single occurrence renders.
**Why human:** Requires triggering the specific 4x narration pattern with actual LLM output.

### 4. Failure reason surfacing

**Test:** Trigger a timeout or error (e.g., tight timeout model, impossible sandbox task).
**Expected:** Failure badge shows "failed - timed out" or similar. Reload shows graceful "failed" fallback.
**Why human:** Requires triggering a real failure and observing the RunCard badge live.

### 5. Sticky timer visibility during long runs

**Test:** During a multi-tool run, scroll to the bottom of a long sub-agent extraction panel.
**Expected:** Sticky timer bar remains visible above the input box showing elapsed time, step count, file count, tool description.
**Why human:** Requires enough content to scroll and a running stream to observe the fixed-position bar.

### 6. Cross-provider Chrome MCP UAT (ROADMAP SC13)

**Test:** Run the DBA PPTX generation task across at least 3 providers (OpenAI + Anthropic + one direct-integrated like DeepSeek).
**Expected:** At no point during the run does the visible screen stay unchanged for >10s. Status labels match backend state. Failure reasons surface on failed runs.
**Why human:** ROADMAP SC13 explicitly requires Chrome MCP UAT. Requires real API keys, running backend, and browser-level observation.

### 7. Direct provider routing verification

**Test:** With DEEPSEEK_API_KEY set, select deepseek-chat model and send a message.
**Expected:** Backend log shows routing through api.deepseek.com. Sub-agent uses deepseek-chat (not main model).
**Why human:** Requires real API keys and a running backend to verify HTTP routing via server logs.

### Gaps Summary

No code-level gaps were found. All 12 programmatically-verifiable truths are VERIFIED with artifacts existing, substantive, wired, and data flowing. The 4 code review warnings (WR-01 through WR-04) are non-blocking: WR-01/WR-02 are dead-code filter inconsistencies with no functional impact, WR-03 (competing scrollIntoView) is a potential UX concern requiring human visual validation, and WR-04 (KNOWN_PROVIDERS duplication) is maintenance debt. TypeScript compiles cleanly. All 8 task commits are present in git history.

The remaining unverified truth (SC13: Chrome MCP UAT across 3 providers) inherently requires human testing with real API keys and live provider interactions.

---

_Verified: 2026-05-25T22:45:00Z_
_Verifier: Claude (gsd-verifier)_
