---
phase: 070-docling-httpx-spike
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/phases/070-docling-httpx-spike/070-SUMMARY.md
  - .planning/PROJECT.md
  - backend/requirements.txt
  - backend/.env.example
autonomous: true
requirements:
  - RAG-DOCLING-02
user_setup: []

must_haves:
  truths:
    - "`070-SUMMARY.md` contains a `## Rejected Paths` section with one `### Path (b)` and one `### Path (c)` subsection (D-070-04 / D-070-13)."
    - "Each rejected-path subsection records all five D-070-04 fields: path identifier, pip commands attempted (or 'not attempted — pro-forma'), verbatim error output (or 'N/A'), wall-clock time, one-paragraph rationale."
    - "`.planning/PROJECT.md` Key Decisions table has a new `**D-v2.6-01**:` row appended that closes Q-v2.6-01 with the chosen path (per D-070-11 #4 / SC#1)."
    - "`backend/requirements.txt` contains a multi-line `# Pinned by Phase 070` comment block immediately above the supabase / httpx / docling lines (D-070-14 regression guardrail)."
    - "If path (c) won instead of (a): `backend/.env.example` documents `EXTRACTOR_DOCLING_ISOLATION=subprocess|in-process` AND `070-SUMMARY.md` references it (SC#3 contingency). If path (a) won (expected): this artifact is skipped explicitly with a one-line note in the SUMMARY."
  artifacts:
    - path: ".planning/phases/070-docling-httpx-spike/070-SUMMARY.md"
      provides: "Phase 070 spike narrative + Rejected Paths matrix + SC#4 audit trail"
      contains: "## Rejected Paths"
      contains_also: "### Path (b)"
      contains_also_2: "### Path (c)"
    - path: ".planning/PROJECT.md"
      provides: "Q-v2.6-01 closure row in the Key Decisions table"
      contains: "**D-v2.6-01**:"
    - path: "backend/requirements.txt"
      provides: "D-070-14 regression-guardrail comment block above pinned lines"
      contains: "# Pinned by Phase 070"
  key_links:
    - from: ".planning/PROJECT.md Key Decisions row"
      to: ".planning/phases/070-docling-httpx-spike/070-SUMMARY.md"
      via: "rationale-cell reference to the matrix"
      pattern: "070-SUMMARY\\.md"
    - from: "backend/requirements.txt guardrail comment"
      to: "backend/tests/integration/test_pdf_extractor_docling_compat.py"
      via: "comment names the test as the regression gate"
      pattern: "test_pdf_extractor_docling_compat\\.py"
---

<objective>
Package the empirical outcome of Plan 01 into the three durable artifacts that close Phase 070:

1. **`070-SUMMARY.md`** — narrative + `## Rejected Paths` matrix (D-070-04 / D-070-13). Five fields per rejected path. This is SC#4: the audit trail so future maintainers don't relitigate.
2. **`.planning/PROJECT.md` Key Decisions row** — `**D-v2.6-01**:` row closing Q-v2.6-01, matching the existing D-v2.5-NN / D-PRD-NN format. This is SC#1: chosen path recorded in PROJECT.md.
3. **`backend/requirements.txt` guardrail comment block** — multi-line `# Pinned by Phase 070 (Q-v2.6-01 resolution)` block above the supabase / httpx / docling lines, naming the regression test (D-070-14).
4. **`backend/.env.example`** entry for `EXTRACTOR_DOCLING_ISOLATION` — ONLY if path (c) won. Per Plan 01's expected outcome (path (a) green), this becomes a one-line skip-with-justification note in the SUMMARY.

Purpose: Phase 070 SC#1 (PROJECT.md update), SC#3 (env var doc if path-c), and SC#4 (matrix). Plan 01 already delivered the empirical work; Plan 02 turns it into durable, searchable, regression-protected documentation. The matrix is for future maintainers; the PROJECT.md row is for the active decision audit; the guardrail comment is for the next developer who tries to bump supabase past 2.29.x.

Output: three modified files (`070-SUMMARY.md`, `PROJECT.md`, `requirements.txt`) + conditionally `.env.example`. No code changes. No test changes. No off-limits-file changes.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/070-docling-httpx-spike/070-CONTEXT.md
@.planning/phases/070-docling-httpx-spike/070-PATTERNS.md
@.planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/seeds/SEED-006-multimodal-extraction-quality.md
@.planning/prd-reset/DECISIONS.md
@.planning/PRDs/v2.6.md
@backend/requirements.txt

# Plan 01's execution log + 070-01-SUMMARY.md are the load-bearing inputs:
# - chosen path (a vs c)
# - concrete pin versions
# - RED→GREEN cycle output
# - full-suite summary line
# - the gotrue/supafunc grep output (recorded as "not-attempted-because-no-source-hits" rationale for paths b/c)

<interfaces>
<!-- Existing format precedents — extracted from codebase. -->

From `.planning/PROJECT.md` lines 289-292 — the verbatim format for a Key Decisions table row:

```markdown
| **D-v2.5-08**: Run-backed streaming via Redis Streams (not pgmq, not LISTEN/NOTIFY) | Redis Streams provide native replay-from-offset + live-tail... | New — locked 2026-05-02 by user before /gsd:discuss-phase 061 |
```

Three columns: `**<bold-decision-id>**: <decision-name>` | `<rationale paragraph>` | `New — <status, date, gating context>`.

From `backend/requirements.txt` line 24 — the existing in-file comment precedent:

```
reportlab>=4.0.0  # Phase 069 test fixtures (PDF synth for golden-extraction tests)
```

Single-line trailing comment with `# Phase NNN <reason>` prefix. The D-070-14 guardrail is a multi-line block — both forms coexist (PATTERNS.md line 169-172 confirms both are acceptable).

From `.planning/seeds/SEED-006-multimodal-extraction-quality.md` §Docling Evaluation (lines 79-92) — the historical-rationale framing the SUMMARY should reference (NOT verbatim-quote in full; reference + paraphrase to keep the SUMMARY tight).
</interfaces>

<source_of_truth>
- **Decisions:** `.planning/phases/070-docling-httpx-spike/070-CONTEXT.md` D-070-04, D-070-05, D-070-11 #4, D-070-13, D-070-14 — authoritative for this plan's artifact contracts.
- **Empirical inputs:** `.planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md` (produced by Plan 01) — chosen path, pin versions, cycle output. This plan READS those values and WRITES them into durable artifacts.
- **Format precedent:** `.planning/PROJECT.md` lines 289-293 for the Key Decisions row format. `backend/requirements.txt` line 24 for the trailing-comment style.
- **Success criteria:** ROADMAP.md Phase 070 SC#1 (PROJECT.md update), SC#3 (env var doc if path-c), SC#4 (matrix in 070-SUMMARY.md).
</source_of_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add D-070-14 regression-guardrail comment block to requirements.txt</name>
  <files>backend/requirements.txt</files>

  <read_first>
    - `backend/requirements.txt` (current state — Plan 01 has already bumped lines 4, 23, and appended `docling>=2.x` at the end; this task adds the comment block above those lines).
    - `.planning/phases/070-docling-httpx-spike/070-CONTEXT.md` D-070-14 (guardrail spec — names the test as the regression gate).
    - `.planning/phases/070-docling-httpx-spike/070-PATTERNS.md` lines 169-185 (comment-style precedent + recommended hybrid format).
    - `.planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md` (for the actual pinned version numbers Plan 01 chose).
  </read_first>

  <action>
    Edit `backend/requirements.txt` to insert a D-070-14 regression-guardrail comment block immediately above the `supabase==` line. Use this exact text (substituting `<SUPABASE_VER>` and `<DOCLING_VER>` with the actual pin versions Plan 01 recorded in `070-01-SUMMARY.md`):

    ```
    # Pinned by Phase 070 (Q-v2.6-01 resolution): supabase 2.10 → 2.29.x closes the
    # docling httpx<0.28 conflict. Bumping past supabase 2.29.x or relaxing the
    # httpx<0.29 cap re-introduces the conflict. Re-run
    # backend/tests/integration/test_pdf_extractor_docling_compat.py before changing.
    supabase==<SUPABASE_VER>
    ```

    The block replaces the bare `supabase==<SUPABASE_VER>` line that Plan 01 wrote. Do NOT add an analogous block above the `httpx` line or the `docling` line — the supabase block names both other pins in the comment body, so one block guards all three.

    Resulting file shape (after Plan 01 + Plan 02 Task 1):

    ```
    fastapi==0.115.6
    sse-starlette==2.4.1
    uvicorn[standard]==0.32.1
    # Pinned by Phase 070 (Q-v2.6-01 resolution): supabase 2.10 → 2.29.x closes the
    # docling httpx<0.28 conflict. Bumping past supabase 2.29.x or relaxing the
    # httpx<0.29 cap re-introduces the conflict. Re-run
    # backend/tests/integration/test_pdf_extractor_docling_compat.py before changing.
    supabase==<SUPABASE_VER>
    openai>=2.0.0
    ...
    httpx>=0.28.0,<0.29.0
    ...
    docling>=2.<x>,<3.0.0
    ```

    Path-(c) contingency (only if Plan 01's `070-01-SUMMARY.md` records path (c) as winner instead of (a)): the comment block changes to reference the subprocess-isolation contract instead of the in-process upgrade. In that case, also do NOT include `docling` in the main requirements.txt; per CONTEXT.md `<specifics>` bullet 7, docling moves to a separate `backend/requirements-extractor-subprocess.txt`. Plan 01 already handles this divergence; Plan 02 just mirrors whichever shape was committed.

    Verify the edit:

    ```bash
    grep -A 4 '# Pinned by Phase 070' backend/requirements.txt
    ```

    Expected: four comment lines + the `supabase==` pin line.
  </action>

  <verify>
    <automated>grep -c '# Pinned by Phase 070' backend/requirements.txt</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '# Pinned by Phase 070' backend/requirements.txt` returns at least 1 (the block exists).
    - `grep -c 'test_pdf_extractor_docling_compat.py' backend/requirements.txt` returns at least 1 (the comment names the regression test).
    - The comment block appears IMMEDIATELY above the `supabase==` line (no blank line between, by convention with line 24's existing comment style). `grep -B 1 '^supabase==' backend/requirements.txt` shows a `#`-prefixed line as the previous line.
    - The supabase / httpx / docling pin VALUES themselves are unchanged from Plan 01's commit (use `git diff backend/requirements.txt` — only added lines should be the 4 comment lines; no pin-line modifications).
    - `pip install -r backend/requirements.txt` still completes cleanly (pip ignores comments — this verifies the edit didn't accidentally break the file format).
    - `pytest backend/tests/integration/test_pdf_extractor_docling_compat.py -v` still exits 0 (sanity check that the comment block didn't disturb anything).
  </acceptance_criteria>

  <done>
    The regression-guardrail comment block sits above the three coordinated pins; any future PR that bumps them inherits the documented warning + the test as the binding gate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 070-SUMMARY.md with ## Rejected Paths matrix + spike narrative (SC#4)</name>
  <files>.planning/phases/070-docling-httpx-spike/070-SUMMARY.md</files>

  <read_first>
    - `.planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md` — Plan 01's execution log (chosen path, pin versions, RED→GREEN cycle output, full-suite summary line).
    - `.planning/phases/070-docling-httpx-spike/070-CONTEXT.md` D-070-04, D-070-05, D-070-13 (matrix contract — five fields per rejected path).
    - `.planning/phases/070-docling-httpx-spike/070-PATTERNS.md` lines 208-232 (matrix structure example).
    - `.planning/seeds/SEED-006-multimodal-extraction-quality.md` §Docling Evaluation (lines 79-92) — historical context for the narrative section.
    - `.planning/prd-reset/DECISIONS.md` D-PRD-07 Appendix (subprocess-fence precedent for path-(c) rationale).
    - `.planning/PRDs/v2.6.md` §13 Q-v2.6-01 (line 431) — three options + PRD-recommended attempt order (a)→(c)→(b).
  </read_first>

  <action>
    Create `.planning/phases/070-docling-httpx-spike/070-SUMMARY.md` (NEW). The file is the SC#4 audit-trail artifact. Use this structure (substitute actual values from Plan 01 — chosen path, pin versions, wall-clock, full-suite line):

    ```markdown
    # Phase 070: Docling httpx Spike — Summary

    **Closed:** 2026-05-14
    **Goal:** Resolve Q-v2.6-01 (docling 2.x httpx>=0.28 ↔ supabase 2.10 httpx<0.28 conflict) with a CI-verified path and a documented rejection matrix.
    **Chosen path:** Path (a) — supabase-py 2.10 → 2.29.x upgrade, docling 2.x added in-process, httpx pin widened to `>=0.28.0,<0.29.0`.
    **CI gate:** `backend/tests/integration/test_pdf_extractor_docling_compat.py` — 2 tests GREEN under the venv.

    ## Outcome

    - `supabase==<SUPABASE_VER>` pinned in `backend/requirements.txt` (was `2.10.0`).
    - `httpx>=0.28.0,<0.29.0` pinned (was `>=0.27.0` — upper-bound cap added to make future regressions detectable at install time).
    - `docling>=2.<x>,<3.0.0` added.
    - Full backend pytest suite green: `<paste verbatim summary line from Plan 01 log, e.g., '===== 247 passed, 1 xfailed, 3 skipped in 184.32s =====' >`.
    - `gotrue` / `supafunc` import-sweep verified no-op via `grep -rn --include='*.py' backend/`: ZERO source hits (matches PATTERNS.md prediction).
    - D-070-14 regression-guardrail comment block in `requirements.txt` above the three pinned lines, naming the test as the gate.

    ## Historical context

    The conflict was first surfaced in SEED-006 §Docling Evaluation (lines 79-92): supabase-py 2.10 ships an `httpx<0.28` constraint inherited from `gotrue` (deprecated in favor of `supabase-auth`); docling 2.x publishes wheels that pin `httpx>=0.28` for HTTP/2 support. PRD v2.6 §13 Q-v2.6-01 (line 431) enumerated three resolution paths:

    - **(a)** Upgrade supabase-py to 2.29.x (which dropped the `gotrue` constraint).
    - **(b)** Pin docling back to an `httpx<0.28`-compatible version.
    - **(c)** Subprocess-isolate docling so the FastAPI process never imports it.

    PRD recommendation: attempt (a) first, fall back to (c). This spike followed that order; (a) succeeded; (b) and (c) were not empirically attempted (D-070-05 pro-forma rejection).

    ## Rejected Paths

    ### Path (b) — Pin docling back to an httpx<0.28-compatible version

    - **Pip commands attempted:** `pip index versions docling` (one-shot PyPI lookup, D-070-05).
    - **Verbatim output:** `<paste verbatim output of `pip index versions docling` from Plan 01's execution log — should be a one-line listing of all 2.x versions>`.
    - **Wall-clock:** ~30 seconds (single PyPI roundtrip; no install attempted).
    - **Rationale:** No docling 2.x version publishes wheels with an `httpx<0.28` constraint (verified via the PyPI metadata listing above). Path (b) is structurally impossible at the current docling release set. Falsified cheaply via PyPI lookup; no install attempted. Marked **not empirically attempted — falsified by PyPI metadata** per D-070-05.

    ### Path (c) — Subprocess isolation

    - **Pip commands attempted:** (none — pro-forma rejection per D-070-05.)
    - **Verbatim output:** N/A.
    - **Wall-clock:** 0 (not attempted).
    - **Rationale:** Path (a) succeeded at the spike test in `<wall-clock from Plan 01 log>`; subprocess overhead (process spawn + pickled/JSON IPC per extract) is unjustified once in-process coexistence works. The PyMuPDF subprocess-fence precedent (D-PRD-07 Appendix, Phase 069 Plan 02) remains available as a fallback if path (a) regresses in a future supabase or docling release — re-applying it for docling would require a new `EXTRACTOR_DOCLING_ISOLATION=subprocess|in-process` env var (documented in `.env.example` only if/when path (c) becomes the active path; deliberately NOT added to `.env.example` in this spike to avoid documenting an unused config knob). Marked **not empirically attempted — pro-forma per D-070-05** because (a) succeeded.

    ## Non-regression posture (D-070-15 / D-070-03 verification)

    - `backend/app/services/extraction_service.py` byte-identical to its pre-Plan-01 state (`git diff` empty). Phase 069's `PdfExtractor` ABC + `LegacyExtractor` + `get_extractor(mime)` dispatcher untouched.
    - `backend/app/api/documents.py` byte-identical to its pre-Plan-01 state. The `BackgroundTasks` ingestion-decoupling flow (line 269) is intact; navigate-away / refresh resilience during ingestion is preserved.
    - No `DoclingExtractor` class created (`grep -rn 'class DoclingExtractor' backend/` returns zero). Phase 071 owns that class against the 069 ABC.

    ## Regression gate

    Future PRs that bump `supabase` past `<SUPABASE_VER>` or relax the `httpx<0.29` cap will trip `backend/tests/integration/test_pdf_extractor_docling_compat.py`. The D-070-14 guardrail comment block in `requirements.txt` makes the rationale visible at the point of change. The capped upper bounds (`httpx<0.29.0`, `docling<3.0.0`) ensure the conflict is detectable at `pip install` resolver time before it ever reaches runtime.

    ## Files changed

    - `backend/requirements.txt` — supabase pin bump (2.10.0 → <SUPABASE_VER>); httpx pin widened (`>=0.28.0,<0.29.0`); docling pin added (`>=2.<x>,<3.0.0`); D-070-14 guardrail comment block added.
    - `backend/tests/integration/test_pdf_extractor_docling_compat.py` — NEW; two test fns: `test_docling_supabase_coexist`, `test_supabase_smoke_post_resolution`.
    - `.planning/PROJECT.md` — Key Decisions row appended (`**D-v2.6-01**:` closure entry, see Plan 02 Task 3).
    - `.planning/phases/070-docling-httpx-spike/070-SUMMARY.md` — THIS FILE.

    ## Linked artifacts

    - Plan 01 execution log: `.planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md`
    - Plan 02 execution log: `.planning/phases/070-docling-httpx-spike/070-02-SUMMARY.md`
    - Requirement closed: RAG-DOCLING-02 (`.planning/REQUIREMENTS.md` line 17).
    - Phase enabled: Phase 071 — Docling Primary Path (`.planning/ROADMAP.md` lines 277-287; depends_on 070).
    ```

    Important: every `<SUPABASE_VER>`, `<DOCLING_VER>`, `<x>`, `<wall-clock from Plan 01 log>`, and `<paste verbatim output ...>` placeholder MUST be filled in with the actual value from `070-01-SUMMARY.md`. The executor reads Plan 01's log, extracts the concrete values, and substitutes them. No placeholders remain in the committed file.

    Path-(c) divergence: if Plan 01 recorded path (c) as winner (unexpected per the D-070-05/06 attempt order), this Task's SUMMARY content shifts — the `## Outcome` section names path (c), the `### Path (a)` subsection captures the empirical failure that triggered the escalation, and Task 4 (env var doc) activates. The skeleton above assumes the expected path-(a)-wins case; if (c) wins, mirror the structure with (a) and (b) as rejected and (c) as chosen.
  </action>

  <verify>
    <automated>grep -c '## Rejected Paths' .planning/phases/070-docling-httpx-spike/070-SUMMARY.md ; grep -c '### Path (b)' .planning/phases/070-docling-httpx-spike/070-SUMMARY.md ; grep -c '### Path (c)' .planning/phases/070-docling-httpx-spike/070-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `.planning/phases/070-docling-httpx-spike/070-SUMMARY.md`.
    - File contains `## Rejected Paths` heading (grep `^## Rejected Paths` returns 1).
    - File contains `### Path (b)` subsection (grep `^### Path \(b\)` returns 1).
    - File contains `### Path (c)` subsection (grep `^### Path \(c\)` returns 1).
    - Each rejected-path subsection contains all five D-070-04 fields. Check via grep: `Pip command` (or `Pip commands`), `output:`, `Wall-clock:`, `Rationale:` — all four labels present under both `### Path (b)` and `### Path (c)`. Plus the path identifier (b / c) is in the heading itself, satisfying field (i).
    - File contains no placeholder strings: `grep -E '<SUPABASE_VER>|<DOCLING_VER>|<paste verbatim|<wall-clock' .planning/phases/070-docling-httpx-spike/070-SUMMARY.md` returns ZERO matches (all placeholders substituted with actual values from Plan 01 log).
    - File contains a reference to `backend/tests/integration/test_pdf_extractor_docling_compat.py` (the CI gate name — grep returns at least 1 match).
    - File contains "byte-identical" or equivalent attestation for the off-limits files (D-070-15 / D-070-03 invariant captured in writing).
    - File length is between ~80 and ~250 lines — not a 5-line stub, not a 500-line essay. The matrix is for future maintainers; concision is a feature.
  </acceptance_criteria>

  <done>
    The SC#4 audit-trail artifact exists, contains the five-field matrix for both unattempted paths, and references the regression test + the off-limits-files attestation. Future maintainers have a single source of truth for "why didn't we just pin docling back?" and "why didn't we subprocess-isolate?".
  </done>
</task>

<task type="auto">
  <name>Task 3: Append Q-v2.6-01 closure row to PROJECT.md Key Decisions table (SC#1)</name>
  <files>.planning/PROJECT.md</files>

  <read_first>
    - `.planning/PROJECT.md` lines 280-293 (Key Decisions table — read enough rows above and below line 293 to confirm where the table ends, so the new row appends at the correct location).
    - `.planning/phases/070-docling-httpx-spike/070-PATTERNS.md` lines 188-204 (exact row format + pitfall: append to existing table, do NOT start new section).
    - `.planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md` (chosen path, pin versions, date — to fill the rationale cell).
    - `.planning/PRDs/v2.6.md` §13 Q-v2.6-01 line 431 (canonical Q-ID — confirms the closure ID is `D-v2.6-01` and the question being closed is Q-v2.6-01).
  </read_first>

  <action>
    Append a new row to the existing Key Decisions table in `.planning/PROJECT.md`. Use the Edit tool, NOT Write (the file is large; targeted append-after-line-293 is the correct surgery).

    The exact row to append (substituting `<SUPABASE_VER>`, `<DOCLING_VER>` with the actual Plan 01 values, and `<HTTPX_PIN>` with `>=0.28.0,<0.29.0`):

    ```markdown
    | **D-v2.6-01**: Q-v2.6-01 resolution — path (a): supabase-py 2.10 → 2.29.x in-process upgrade | The httpx<0.28 (supabase 2.10) vs httpx>=0.28 (docling 2.x) conflict is resolved by upgrading supabase-py to 2.29.x, which drops the `gotrue`-inherited httpx constraint. Path (a) succeeded on the first attempt; paths (b) (pin docling back) and (c) (subprocess isolate) rejected — (b) falsified by PyPI metadata (no docling 2.x publishes httpx<0.28 wheels), (c) pro-forma per D-070-05 (subprocess overhead unjustified once in-process works; D-PRD-07 Appendix subprocess-fence precedent remains available as a future fallback). Pins locked: `supabase==<SUPABASE_VER>`, `httpx<HTTPX_PIN>`, `docling>=2.<x>,<3.0.0`. Full backend pytest suite green post-upgrade; D-070-14 regression-guardrail comment block in `requirements.txt` names `backend/tests/integration/test_pdf_extractor_docling_compat.py` as the binding gate. Full matrix in `.planning/phases/070-docling-httpx-spike/070-SUMMARY.md`. | New — locked 2026-05-14 by Phase 070 spike; CI test green; closes RAG-DOCLING-02 |
    ```

    **Append position:** the new row goes IMMEDIATELY after line 293 (`**D-v2.5-12**` row) — i.e., as the last row of the table before the `## Constraints` heading at line 295. Read line 294 first to confirm what's there (likely blank or close-of-table); insert the new row above the blank/heading.

    **Format precedent check** (PATTERNS.md line 204 pitfall): the row uses three pipe-separated cells, bold `**D-v2.6-01**:` prefix, full-paragraph rationale in the middle cell, and `New — ...` status in the third cell — matching the D-v2.5-08..12 rows verbatim. Do NOT start a new section heading; the table already exists; just append a row.

    Verify the edit:

    ```bash
    grep -c '\*\*D-v2.6-01\*\*' .planning/PROJECT.md
    ```

    Expected: 1 (the new row exists, exactly once).
  </action>

  <verify>
    <automated>grep -c '\*\*D-v2.6-01\*\*:' .planning/PROJECT.md ; grep -c 'Q-v2.6-01 resolution' .planning/PROJECT.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '\*\*D-v2.6-01\*\*:' .planning/PROJECT.md` returns exactly 1 (the row was added exactly once — no duplicates).
    - `grep -c 'Q-v2.6-01 resolution' .planning/PROJECT.md` returns at least 1.
    - The new row appears in the Key Decisions table (not at end-of-file, not as a new section). Verify via `grep -A 3 '\*\*D-v2.5-12\*\*' .planning/PROJECT.md` — the D-v2.6-01 row should appear within ~5 lines after D-v2.5-12.
    - The new row references `070-SUMMARY.md` (the matrix file). Grep `D-v2.6-01.*070-SUMMARY.md` matches in a multiline-aware check, OR the single line containing `**D-v2.6-01**` itself contains `070-SUMMARY.md`.
    - The new row contains the date `2026-05-14` in the third cell.
    - The new row contains the chosen-path identifier `path (a)` (or `path (c)` if Plan 01 escalated).
    - No other PROJECT.md content was modified (`git diff .planning/PROJECT.md` shows only the single appended-row hunk).
  </acceptance_criteria>

  <done>
    Q-v2.6-01 has an auditable closure row in PROJECT.md's Key Decisions table; future readers of PROJECT.md see the chosen path, the rejected alternatives summarized, and the test name that gates regressions, with a pointer to the full matrix.
  </done>
</task>

<task type="auto">
  <name>Task 4: Conditional — document EXTRACTOR_DOCLING_ISOLATION in .env.example (only if path (c) won)</name>
  <files>backend/.env.example</files>

  <read_first>
    - `.planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md` — read the chosen path. If `path (a)`, skip this entire task with a one-line note in `070-02-SUMMARY.md`. If `path (c)`, proceed.
    - `.planning/ROADMAP.md` Phase 070 SC#3 ("`EXTRACTOR_DOCLING_ISOLATION=subprocess|in-process` env var documented if (c) is chosen").
    - `.planning/PRDs/v2.6.md` §5 line 163 (PRD reference to the env var for path-c contingency).
    - `backend/.env.example` (current state — to find the right section to append the new variable; group with extractor-related vars if any exist).
  </read_first>

  <action>
    **First check: read `070-01-SUMMARY.md`'s "Chosen path" line.**

    - If "Chosen path: path (a)" or similar — SKIP this task. Record in `070-02-SUMMARY.md` (Task 5's output): "Task 4 skipped — path (a) won; `EXTRACTOR_DOCLING_ISOLATION` env var deliberately NOT added to `.env.example` to avoid documenting an unused config knob (SC#3 condition `if (c) is chosen` not met)."
    - If "Chosen path: path (c)" — proceed below.

    **Path-(c) branch (only if SC#3 condition activates):**

    Append the following block to `backend/.env.example` (group near the existing `SANDBOX_*` or extraction-related env vars if any; otherwise append at end):

    ```
    # Phase 070 (D-070-06 path-c fallback): controls whether Docling runs in-process or subprocess-isolated.
    # Default: subprocess (matches the path-c resolution path locked in Phase 070).
    # Set to 'in-process' ONLY if a future supabase / docling release reconciles the httpx pin and the
    # in-process upgrade (path a) becomes viable again. Re-run
    # backend/tests/integration/test_pdf_extractor_docling_compat.py before flipping.
    EXTRACTOR_DOCLING_ISOLATION=subprocess
    ```

    Then add a corresponding entry to `070-SUMMARY.md`'s `## Files changed` section, naming `backend/.env.example` and referencing the contingency.

    Verify (only meaningful in path-(c) branch):

    ```bash
    grep -c 'EXTRACTOR_DOCLING_ISOLATION' backend/.env.example
    ```

    Expected (path c): 1. Expected (path a, task skipped): 0.
  </action>

  <verify>
    <automated>grep -c 'EXTRACTOR_DOCLING_ISOLATION' backend/.env.example 2>/dev/null ; echo "Plan-01 chosen path:" ; grep -i 'chosen path' .planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - IF `grep -i 'chosen path.*(a)' .planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md` matches → `grep -c 'EXTRACTOR_DOCLING_ISOLATION' backend/.env.example` returns 0 AND `070-02-SUMMARY.md` records the explicit skip rationale (a one-line note naming SC#3's `if (c)` condition).
    - IF `grep -i 'chosen path.*(c)' .planning/phases/070-docling-httpx-spike/070-01-SUMMARY.md` matches → `grep -c 'EXTRACTOR_DOCLING_ISOLATION' backend/.env.example` returns at least 1, AND the entry's surrounding comment names `test_pdf_extractor_docling_compat.py` as the regression gate, AND `070-SUMMARY.md` records `backend/.env.example` under `## Files changed`.
    - No other `backend/.env.example` content is modified (`git diff backend/.env.example` shows ONLY the new env var block, OR is empty if Task 4 was skipped).
  </acceptance_criteria>

  <done>
    SC#3's conditional env-var contract is satisfied. Either the var is documented (path c) or its absence is recorded with rationale (path a). No silent gap.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation files (PROJECT.md, 070-SUMMARY.md) ↔ future maintainers | The matrix and the Key Decisions row are the audit trail; if they're wrong or incomplete, future Phase 071+ work proceeds on bad assumptions. |
| requirements.txt comment block ↔ future dependency bumps | The guardrail comment is the warning sign; if it's missing or misleading, a future PR may silently re-introduce the httpx conflict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-070-06 | Repudiation | Future PR bumps `supabase` past 2.29.x or relaxes `httpx<0.29` cap without re-running the spike test, silently re-introducing the conflict | mitigate | D-070-14 guardrail comment block in `requirements.txt` (Task 1) names the regression test explicitly. The CI spike test trips on any conflict-re-introducing bump. The capped upper bounds (`httpx<0.29.0`, `docling<3.0.0`) ensure the conflict surfaces at pip-install resolver time. PROJECT.md row (Task 3) provides the audit trail for "when was this last verified". |
| T-070-07 | Information disclosure | `070-SUMMARY.md` matrix records `pip index versions docling` output verbatim — could leak internal infra details if the PyPI metadata format ever embeds environment-specific markers | accept | `pip index versions` output contains only public PyPI metadata (version strings). No internal info disclosed. Low-value, no PII, no secrets. |
| T-070-08 | Tampering | Future planner edits PROJECT.md and accidentally deletes the D-v2.6-01 row, dropping the audit trail | accept | Git history preserves the row. Sweep audits (e.g., `gsd-tools.cjs audit-open`) would surface the gap at next milestone close. Low-likelihood; mitigated by version control + retrospective protocol. |

**block_on: high** — no High-severity findings. T-070-06 is the highest, mitigated by the test + the cap + the comment block working together.

</threat_model>

<verification>
After Plan 02 completes, the following must all hold:

1. `backend/requirements.txt` contains the multi-line `# Pinned by Phase 070` comment block immediately above the `supabase==` line, naming the regression test.
2. `.planning/phases/070-docling-httpx-spike/070-SUMMARY.md` exists, contains `## Rejected Paths` + `### Path (b)` + `### Path (c)` subsections, each with five D-070-04 fields. No `<placeholder>` strings remain.
3. `.planning/PROJECT.md` Key Decisions table contains exactly one `**D-v2.6-01**:` row that references `070-SUMMARY.md` and dates `2026-05-14`.
4. `backend/.env.example` either contains `EXTRACTOR_DOCLING_ISOLATION` (path c) or doesn't (path a — with the absence recorded in `070-02-SUMMARY.md` as an intentional skip).
5. No backend code files were modified (`git diff backend/app/` is empty; `git diff backend/tests/` is empty modulo what Plan 01 created — Plan 02 touches NO `.py` files).
6. `pytest backend/tests/integration/test_pdf_extractor_docling_compat.py -v` still exits 0 (sanity gate that Plan 02's edits didn't disturb the CI test).
</verification>

<success_criteria>
- SC#1 (PROJECT.md update): Key Decisions table closes Q-v2.6-01 via the new `**D-v2.6-01**:` row.
- SC#3 (env var doc if path-c): `.env.example` documents `EXTRACTOR_DOCLING_ISOLATION` OR its absence is explicitly justified in `070-02-SUMMARY.md`.
- SC#4 (matrix): `070-SUMMARY.md` contains the `## Rejected Paths` matrix with five fields per rejected path, plus the historical-context narrative + the non-regression attestation + the regression-gate description.
- D-070-14: requirements.txt has the guardrail comment block above the pinned lines.
- All four ROADMAP SCs (#1, #2, #3, #4) for Phase 070 are now satisfied across Plans 01 + 02.
</success_criteria>

<output>
After completion, create `.planning/phases/070-docling-httpx-spike/070-02-SUMMARY.md` capturing: the four edits made (requirements.txt comment block, 070-SUMMARY.md, PROJECT.md row, and either the .env.example edit or the explicit-skip note), the actual chosen-path string from Plan 01, and a one-line attestation that no backend code or test files were modified by Plan 02. This SUMMARY plus `070-01-SUMMARY.md` plus `070-SUMMARY.md` together form the Phase 070 close-out record.
</output>
</content>
</invoke>