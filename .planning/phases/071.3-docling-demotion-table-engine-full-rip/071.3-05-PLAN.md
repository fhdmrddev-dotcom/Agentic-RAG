---
phase: 071.3
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md
  - .planning/phases/071.3-docling-demotion-table-engine-full-rip/uat_scratch.json
  - .planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md
  - .planning/seeds/SEED-020-retrieval-quality-audit.md
  - .planning/seeds/SEED-021-table-image-recall-lift.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - RAG-DOCLING-01
notes: |
  autonomous: false because Task 1 (live re-extract via /upload or /reextract on
  user's thesis) is operator-driven. After Task 1 the executor records counts via
  SQL and continues autonomously through seed-plant + doc updates. SEED-021 is
  CONDITIONAL — planted only if thesis tables < 25 OR thesis images < 45
  (D-071.3-16). SEED-022 may already be planted by Plan 04 Phase G FAIL — Plan 05
  references it without re-planting. SEED-023 may already be planted by Plan 04
  Phase F FAIL — Plan 05 references it without re-planting, but if Phase F failed
  AND user chose "skip Plan 04" path, Plan 05 should NOT run (orchestrator gate).

  **TWO DISTINCT FLOORS — DO NOT CONFLATE.** This plan records BOTH:
    - D-071.3-02 engine acceptance floor (≥20 tables) — set at Plan 01 PICK time;
      drives SEED-021 conditional plant per D-071.3-16 (< 25 tables → plant).
    - ROADMAP SC#6 phase ship floor (≥15 tables if winner is gmft, ≥12 if PyMuPDF) —
      the actual pass/fail gate for shipping Phase 071.3.
  The UAT status field (green/partial/red) keys off SC#6, NOT D-071.3-02.
  D-071.3-02 becomes a stretch indicator that informs SEED-021 plant.

must_haves:
  truths:
    - "Live re-extract on thesis PDF returns ingestion success (documents.status='completed')"
    - "document_tables row count for thesis recorded; SC#6 floor (≥15 gmft / ≥12 PyMuPDF) drives ship/no-ship; D-071.3-02 floor (≥20) drives SEED-021 plant"
    - "document_images row count for thesis recorded (no assertion — D-071.3-04 measures only)"
    - "Live re-extract on friendly_real.pdf returns ingestion success; counts recorded"
    - "Live re-extract on the smallest DOCX fixture with inline image + table returns success; counts recorded; chunks > 0 AND document_images > 0"
    - "uat_scratch.json exists at .planning/phases/071.3-*/uat_scratch.json with all 3 fixture results (thesis, friendly_real, docx_sibling) in the required shape"
    - "071.3-HUMAN-UAT.md exists with BOTH floors recorded (engine_acceptance_floor_D_071_3_02 = 20, phase_ship_floor_SC_6 = 15 or 12 depending on winner) + acceptance verdict keyed off SC#6"
    - "SEED-019 frontmatter status flipped from planted to closed"
    - "SEED-020 (retrieval-quality audit) planted with concrete re-open trigger per D-071.3-14/15"
    - "SEED-021 planted IFF thesis tables < 25 OR thesis images < 45 (D-071.3-16)"
    - "PROJECT.md Key Decisions table updated with D-PRD-NN codifying Docling demotion + in-process PyMuPDF posture (or supersedes D-PRD-07); a new D-PRD-* row count is present"
    - "ROADMAP.md Phase 076 + Phase 082 SC#1 wording updated per D-071.3 Deferred Ideas"
  artifacts:
    - path: .planning/phases/071.3-docling-demotion-table-engine-full-rip/uat_scratch.json
      provides: "Machine-readable per-fixture counts for Task 3 to consume; gitignored (temporary scratch)"
      contains: "document_tables_count"
    - path: .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md
      provides: "Live UAT scoreboard with 3 fixture results + BOTH floors + acceptance verdict + seed-plant decisions"
      contains: "thesis|friendly_real|DOCX"
    - path: .planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md
      provides: "Status flipped to closed; closing note added"
      contains: "status: closed"
    - path: .planning/seeds/SEED-020-retrieval-quality-audit.md
      provides: "NEW seed planted for embedding-model + re-ranker + hybrid retrieval audit"
      contains: "seed_id: SEED-020"
    - path: .planning/PROJECT.md
      provides: "Key Decisions table records the Docling demotion + in-process PyMuPDF posture as a new ADR or supersedes D-PRD-07"
      contains: "Docling"
  key_links:
    - from: .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md
      to: .planning/seeds/SEED-021-table-image-recall-lift.md
      via: "UAT count thresholds trigger conditional seed plant"
      pattern: "table-image-recall-lift"
    - from: .planning/seeds/SEED-019-*.md
      to: .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-05-SUMMARY.md
      via: "SEED-019 closing note references the closing phase + UAT outcome"
      pattern: "closed.*071.3"
---

<objective>
Implement RAG-DOCLING-01 Plan 05: Live UAT on three fixtures (thesis PDF + friendly_real.pdf + DOCX sibling), close SEED-019, plant SEED-020 (always) + SEED-021 (conditional), update PROJECT.md + ROADMAP.md to codify the Docling demotion direction. Per D-071.3-13 (3-fixture UAT scope), D-071.3-14/15 (SEED-020 re-open trigger + scope), D-071.3-16 (SEED-021 conditional plant), Deferred Ideas in CONTEXT.md (PROJECT.md + ROADMAP.md doc patches).

Purpose: Prove on the user's real content (thesis + arXiv fixture + DOCX) that the post-rip default-set produces acceptable counts. Plant follow-up seeds for the gaps this phase intentionally did NOT close (image recall, embedding-model quality). Update project-level docs to reflect new architectural posture.

**Two distinct floors — record both, gate ship on SC#6:**
- **Engine acceptance floor (D-071.3-02):** ≥20 tables on thesis. Set at Plan 01 PICK time. Drives **SEED-021 conditional plant** per D-071.3-16 (< 25 tables OR < 45 images → plant). NOT the phase ship gate.
- **Phase ship floor (ROADMAP SC#6):** ≥15 tables if winner is gmft, ≥12 if PyMuPDF-only. This is the binary pass/fail for whether Phase 071.3 ships. UAT status (green/partial/red) is computed against this floor.

Output: 071.3-HUMAN-UAT.md scoreboard recording BOTH floors + uat_scratch.json + closed SEED-019 + planted SEED-020 + conditionally planted SEED-021 + PROJECT.md ADR entry + ROADMAP.md wording updates. Phase 071.3 ready to close.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-CONTEXT.md
@.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-PATTERNS.md
@.planning/phases/071.3-docling-demotion-table-engine-full-rip/WINNER.md
@.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-04-SUMMARY.md
@.planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md
@.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Plan 05 reads UAT analog format + seed templates; writes structured artifacts. -->

UAT artifact analog: `.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-HUMAN-UAT.md` (sections + table headers + parking-artifact format).

Seed template structure (from SEED-019):
```yaml
---
seed_id: SEED-NNN
title: <short title>
status: planted | closed | deferred | superseded
planted: <ISO date>
phase_origin: <phase-id>
related_seeds: [...]
re_open_trigger: |
  <concrete condition>
suggested_phase: <hint>
---

## Why this seed exists
<body>
```

D-071.3-13 UAT scope (verbatim from CONTEXT.md):
1. Re-extract user's reference thesis under new defaults — assert document_tables count vs SC#6 floor (≥15 gmft / ≥12 PyMuPDF) AND record document_images count for SEED-021 evidence.
2. Re-extract friendly_real.pdf (arXiv) — record both counts for generalization signal.
3. Re-extract a DOCX sibling fixture (any committed DOCX from prior phase fixtures) — assert chunks > 0 AND document_images > 0 (verifies zip_xpath_docx adapter still works post-Docling-rip).

**Floor sources — DO NOT CONFLATE:**
- D-071.3-02 (CONTEXT.md): ≥20 tables on thesis — engine acceptance floor at PICK time. Drives SEED-021 conditional plant.
- ROADMAP SC#6: count ≥15 (gmft target) OR ≥12 (PyMuPDF-only target). Phase ship floor. UAT status keys off this.
- D-071.3-16: SEED-021 plant condition — thesis tables < 25 OR thesis images < 45.

D-071.3-16 SEED-021 conditional plant:
- IF thesis post-ship table count < 25 (gap > 10 vs ground truth 35) → plant SEED-021
- OR IF thesis post-ship image count < 45 (gap > 14 vs ground truth 59) → plant SEED-021
- Re-open trigger: "Next academic-PDF upload misses tables/figures observed by user, OR Phase 072 vision-cap raise verification shows persistent recall gap."

D-071.3-14 SEED-020 re-open triggers (BOTH independent):
1. User reports retrieval-quality complaint
2. v2.7 milestone opens

D-071.3-15 SEED-020 scope:
- Embedding model bench (current default vs BGE-M3 vs voyage-2 vs nomic-embed-text vs Cohere embed-v3)
- Re-ranker integration (cross-encoder, Cohere Rerank, or bge-reranker-large)
- Hybrid retrieval architecture (BM25 sparse + dense vector fusion, RRF or weighted)

Deferred Ideas from CONTEXT.md (Plan 05 acts on these):
- PROJECT.md Key Decisions update — add D-PRD-NN entry codifying Docling demotion OR supersede D-PRD-07
- ROADMAP Phase 076 SC#1 wording: "Docling-extracted chunks" → "new default engine output"
- ROADMAP Phase 082 SC#1: "Validate Docling output" → "Validate final default-set output"

Committed DOCX fixtures available (verified via Glob):
- backend/tests/fixtures/extraction/reference.docx
- backend/tests/fixtures/extraction/academic_synth.docx
- backend/tests/fixtures/extraction/friendly_real.docx

Plan 05 picks smallest of the three (or whichever has confirmed inline image + table) — record choice in UAT artifact.

API surfaces for live UAT:
- `POST /documents/{document_id}/reextract` — re-extracts an existing document under new defaults
- `POST /upload` — uploads a new document
- For SQL: `SELECT COUNT(*) FROM document_tables WHERE document_id = '<id>'` (+ document_images, document_chunks)

uat_scratch.json shape (Task 2 writes this; Task 3 reads it):
```json
{
  "thesis": {
    "document_id": "<uuid>",
    "document_tables_count": 22,
    "document_images_count": 37,
    "document_chunks_count": 240
  },
  "friendly_real": {
    "document_id": "<uuid>",
    "document_tables_count": 5,
    "document_images_count": 8,
    "document_chunks_count": 65
  },
  "docx_sibling": {
    "fixture_name": "reference.docx",
    "document_id": "<uuid>",
    "document_tables_count": 2,
    "document_images_count": 3,
    "document_chunks_count": 15
  }
}
```
Path: `.planning/phases/071.3-docling-demotion-table-engine-full-rip/uat_scratch.json` (gitignored — temporary scratch consumed by Task 3 and deleted at phase close).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User uploads (or re-extracts) thesis PDF; executor records counts</name>
  <what-built>
    Post-Plan-04 codebase ships with the winner as the default table engine, EXTRACTOR_DOCLING_* env vars gone, httpx unpinned (or revert documented via SEED-023), PyMuPDF either in-process (if Plan 04 Phase G passed) or still fenced. Now we verify with real content.
  </what-built>
  <how-to-verify>
    1. Restart uvicorn (Docling singleton was held by some routes pre-rip; fresh process ensures clean state):
       ```
       cd backend && venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
       ```
    2. Decide one of:
       a. **If thesis is already in the documents table:** find its document_id via SQL, then call:
          ```
          curl -X POST "http://localhost:8000/documents/<thesis-doc-id>/reextract" \
            -H "Authorization: Bearer <user-jwt>" \
            -H "Content-Type: application/json" \
            -d '{}'
          ```
          (No `?engines=` hint — uses migration 047 defaults.)
       b. **If thesis is not yet uploaded:** upload via the frontend at http://localhost:5173/ (test login fhdmrd@gmail.com / 123456) using the Upload button on the library page.
    3. Wait for ingestion to complete. Poll:
       ```sql
       SELECT id, status, ingestion_step, extractor
       FROM documents
       WHERE id = '<thesis-doc-id>';
       ```
       Expected end state: `status='completed'`, `ingestion_step` NULL or 'completed'.
    4. Once `status='completed'`, run the count queries:
       ```sql
       SELECT
         (SELECT COUNT(*) FROM document_tables WHERE document_id = '<thesis-doc-id>') AS tables,
         (SELECT COUNT(*) FROM document_images WHERE document_id = '<thesis-doc-id>') AS images,
         (SELECT COUNT(*) FROM document_chunks WHERE document_id = '<thesis-doc-id>') AS chunks;
       ```
    5. Reply in chat with the document_id + the three counts, e.g.:
       `thesis: doc_id=<uuid> tables=22 images=37 chunks=240 status=completed`
       If ingestion failed: reply with `thesis: FAILED status=<status> error=<msg>` and include backend logs around the failure point.
  </how-to-verify>
  <resume-signal>Reply: `thesis: doc_id=<uuid> tables=<N> images=<N> chunks=<N> status=<status>`</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Re-extract friendly_real.pdf + DOCX sibling; write uat_scratch.json with all 3 fixture counts</name>
  <read_first>
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-CONTEXT.md (D-071.3-13 UAT scope)
    - Glob result for backend/tests/fixtures/extraction/*.docx (reference.docx, academic_synth.docx, friendly_real.docx — pick smallest with inline image + table)
    - backend/tests/fixtures/extraction/friendly_real.pdf (arXiv 2605.15184v1 — always committed)
    - Task 1 chat reply (thesis doc_id + counts)
  </read_first>
  <action>
    1. **friendly_real.pdf:**
       a. Upload via the API:
          ```
          curl -X POST "http://localhost:8000/upload" \
            -H "Authorization: Bearer <user-jwt>" \
            -F "file=@backend/tests/fixtures/extraction/friendly_real.pdf"
          ```
          (No `?engines=` hint.)
       b. Wait for `documents.status='completed'`.
       c. Record `friendly_doc_id`, then run the same count query as Task 1.

    2. **DOCX sibling:**
       a. Decide which committed DOCX to use. Default order: `reference.docx` (likely smallest; verify) → `academic_synth.docx` → `friendly_real.docx`. Whichever has confirmed inline image + table.
       b. Run a quick inspection in venv:
          ```
          cd backend && venv\Scripts\python.exe -c "from docx import Document; d = Document('tests/fixtures/extraction/reference.docx'); print(f'paragraphs={len(d.paragraphs)} inline_shapes={len(d.inline_shapes)} tables={len(d.tables)}')"
          ```
          Repeat for other DOCX files. Pick the one with `inline_shapes >= 1 AND tables >= 1` AND smallest file size.
       c. Upload via the API:
          ```
          curl -X POST "http://localhost:8000/upload" \
            -H "Authorization: Bearer <user-jwt>" \
            -F "file=@backend/tests/fixtures/extraction/<chosen>.docx"
          ```
          (No `?engines=` hint — uses migration 047 + 046 defaults, which routes DOCX images through zip_xpath_docx.)
       d. Wait for `documents.status='completed'`.
       e. Record `docx_doc_id`, then run the count query.

    3. Verify D-071.3-13 floors for the DOCX:
       - `document_chunks > 0`
       - `document_images > 0` (proves zip_xpath_docx adapter still works post-Docling-rip — RAG-MM-LIFT-02 continuity check)

    4. **Write `uat_scratch.json`** (machine-readable scratch file consumed by Task 3). Path: `.planning/phases/071.3-docling-demotion-table-engine-full-rip/uat_scratch.json`. Shape MUST match exactly:
       ```json
       {
         "thesis": {
           "document_id": "<thesis-doc-id from Task 1>",
           "document_tables_count": <int from Task 1 SQL>,
           "document_images_count": <int from Task 1 SQL>,
           "document_chunks_count": <int from Task 1 SQL>
         },
         "friendly_real": {
           "document_id": "<friendly-doc-id from step 1c>",
           "document_tables_count": <int>,
           "document_images_count": <int>,
           "document_chunks_count": <int>
         },
         "docx_sibling": {
           "fixture_name": "<reference.docx | academic_synth.docx | friendly_real.docx>",
           "document_id": "<docx-doc-id from step 2e>",
           "document_tables_count": <int>,
           "document_images_count": <int>,
           "document_chunks_count": <int>
         }
       }
       ```
       Write the file via Python or `Path.write_text` — emit valid JSON (no trailing commas, no comments). NOTE: This file is gitignored (temporary scratch). Add it to `.gitignore` if not already covered by a wildcard.

    5. Confirm in chat to user: "uat_scratch.json written with all 3 fixture counts. Proceeding to Task 3."
  </action>
  <verify>
    <automated>test -f .planning/phases/071.3-docling-demotion-table-engine-full-rip/uat_scratch.json &amp;&amp; jq '.thesis.document_tables_count, .friendly_real.document_tables_count, .docx_sibling.document_chunks_count' .planning/phases/071.3-docling-demotion-table-engine-full-rip/uat_scratch.json | grep -E "^[0-9]+$" | wc -l | grep -q "^3$"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/071.3-docling-demotion-table-engine-full-rip/uat_scratch.json` exists
    - File is valid JSON parseable by `jq` (no syntax errors)
    - JSON contains the 3 top-level keys: `thesis`, `friendly_real`, `docx_sibling`
    - Each fixture object contains `document_id` (string) and `document_tables_count`, `document_images_count`, `document_chunks_count` (integers ≥ 0)
    - `docx_sibling` ALSO contains `fixture_name` (string, one of: `reference.docx`, `academic_synth.docx`, `friendly_real.docx`)
    - `documents.status='completed'` for friendly_real.pdf upload (via SQL inspect)
    - `documents.status='completed'` for DOCX upload (via SQL inspect)
    - `document_chunks` count > 0 for DOCX (`jq '.docx_sibling.document_chunks_count' uat_scratch.json` > 0)
    - `document_images` count > 0 for DOCX (proves zip_xpath_docx adapter survives post-Docling-rip)
    - `jq '.thesis.document_tables_count, .friendly_real.document_tables_count, .docx_sibling.document_chunks_count' uat_scratch.json` returns 3 non-empty numeric lines (verify gate)
  </acceptance_criteria>
  <done>Two additional fixtures re-extracted; uat_scratch.json written with all 3 fixture counts in canonical shape.</done>
</task>

<task type="auto">
  <name>Task 3: Write 071.3-HUMAN-UAT.md with BOTH floors recorded + acceptance verdict keyed off SC#6</name>
  <read_first>
    - .planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-HUMAN-UAT.md (analog format — section structure, table headers, parking-artifact format) — if exists; otherwise structure freshly
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/uat_scratch.json (Task 2 output — read all 3 fixture counts)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/WINNER.md (winner name + bench rationale)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-04-SUMMARY.md (Phase G outcome — fence delete or SEED-022 plant; Phase F outcome — httpx unpin or SEED-023 plant)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-CONTEXT.md (D-071.3-02 engine floor 20; D-071.3-13 UAT scope; D-071.3-16 seed conditional)
    - .planning/ROADMAP.md (Phase 071.3 SC#6 wording — `≥15 (gmft target) OR ≥12 (PyMuPDF-only target)`)
  </read_first>
  <action>
    Read `uat_scratch.json` to get fixture counts. Read `WINNER.md` to determine which SC#6 floor applies (15 for gmft, 12 for PyMuPDF, default to 15 for camelot/pdfplumber).

    **Determine SC#6 floor (phase ship floor):**
    - If `winner == "gmft"`: `phase_ship_floor_SC_6 = 15`
    - If `winner == "pymupdf"`: `phase_ship_floor_SC_6 = 12`
    - If `winner == "camelot"` or `winner == "pdfplumber"` (fallback per D-071.3-03): `phase_ship_floor_SC_6 = 15` (default to the gmft-target wording; the user accepted shipping best-available)

    **Determine status (computed against SC#6 floor, NOT D-071.3-02):**
    - `green`: thesis_tables_count >= phase_ship_floor_SC_6 AND all 3 fixtures completed (status='completed')
    - `partial`: thesis_tables_count < phase_ship_floor_SC_6 BUT > 0 (extractor returned non-zero); OR exactly one fixture failed but the others passed
    - `red`: thesis_tables_count == 0 (extraction returned zero tables) OR all fixtures failed

    **Determine SEED-021 plant (D-071.3-16 — uses the OTHER floor, D-071.3-02-adjacent):**
    - Plant if `thesis_tables_count < 25` OR `thesis_images_count < 45`
    - Both thresholds are independent of the SC#6 ship gate

    Create `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md` with this structure (substitute literal values for `<>`):

    ```markdown
    ---
    phase: 071.3-docling-demotion-table-engine-full-rip
    uat_type: live
    fixtures_tested: 3
    status: <green | partial | red>
    completed: <ISO date>
    engine_acceptance_floor_D_071_3_02: 20  # CONTEXT.md — drives SEED-021 plant, NOT ship gate
    phase_ship_floor_SC_6: <15 | 12>        # ROADMAP SC#6 — drives ship/no-ship verdict
    winner_engine: <winner from WINNER.md>
    project_level_approval: <pending | approved>
    ---

    # Phase 071.3 — Live UAT Scoreboard

    Winner engine (from Plan 01): **<winner>** — bench rationale at `.planning/research/071.3-bench-results.md`.
    Post-rip codebase: docling removed, httpx <unpinned | pinned-with-SEED-023>, PyMuPDF <in-process | subprocess-fenced>.

    ## Floor Sources — DO NOT CONFLATE

    | Floor | Source | Value | Drives |
    |---|---|---|---|
    | Engine acceptance | D-071.3-02 (CONTEXT.md) | ≥20 tables on thesis | SEED-021 conditional plant (D-071.3-16: < 25 → plant) |
    | Phase ship | ROADMAP SC#6 | ≥15 (gmft) OR ≥12 (PyMuPDF-only) | Binary pass/fail for whether Phase 071.3 ships |

    **This UAT's status field is computed against ROADMAP SC#6, NOT D-071.3-02.** D-071.3-02 is a stretch indicator informing SEED-021.

    ## Fixture Results

    | Fixture | Engine | Tables (GT) | Images (GT) | Chunks | Status | Notes |
    |---|---|---|---|---|---|---|
    | User thesis PDF | <winner> | <thesis_tables_count> (35) | <thesis_images_count> (59) | <thesis_chunks_count> | <completed|failed> | tables_recall=<pct>% vs SC#6 floor=<15 or 12>; D-071.3-02 floor=20 |
    | friendly_real.pdf (arXiv 2605.15184v1) | <winner> | <friendly_tables_count> (?) | <friendly_images_count> (?) | <friendly_chunks_count> | <completed|failed> | generalization signal |
    | <docx_fixture_name> | zip_xpath / pdfplumber | <docx_tables_count> | <docx_images_count> | <docx_chunks_count> | completed | RAG-MM-LIFT-02 continuity check |

    ## Acceptance Verdict (ROADMAP SC#6)

    - SC#6 floor for winner `<winner>`: **<15 | 12>**
    - Thesis table count: **<thesis_tables_count>**
    - SC#6 cleared: **<YES | NO>**
    - DOCX chunks > 0 AND DOCX images > 0: **<YES | NO>**
    - friendly_real.pdf completed: **<YES | NO>**

    **Status: <green | partial | red>**

    ## Stretch Indicator (D-071.3-02 Engine Acceptance Floor)

    - D-071.3-02 floor: **≥20 tables on thesis**
    - Actual thesis table count: **<thesis_tables_count>**
    - D-071.3-02 cleared: **<YES | NO>**

    This is NOT the phase ship gate; it is the floor used at Plan 01 PICK time and informs the SEED-021 plant condition below.

    ## D-071.3-16 SEED-021 Decision

    SEED-021 plant condition (independent of SC#6 / D-071.3-02 — uses D-071.3-16's own thresholds):

    - Thesis tables = **<thesis_tables_count>**; SEED-021 plant threshold = `tables < 25`. Triggered: **<YES | NO>**
    - Thesis images = **<thesis_images_count>**; SEED-021 plant threshold = `images < 45`. Triggered: **<YES | NO>**
    - **Plant SEED-021: <YES | NO>** (plant if ANY trigger above is YES).
    - Reason: <one-line explanation>.

    ## Plan 04 Phase F + G Reference

    - PyMuPDF in-process smoke (D-071.3-11): **<PASS | FAIL>**. Fence retained: <NO | YES>. SEED-022 planted: <NO | YES (path)>.
    - httpx unpin (D-071.3-10): **<SHIPPED | HELD-SEED-023>**. SEED-023 planted: <NO | YES (path)>.

    ## Overall Verdict

    Acceptance criteria status: **<met | partial | not-met>** (keyed off SC#6).
    Project-level approval: <pending | approved> by <user> on <date>.

    ## Carry-forward observations

    - <any unexpected findings during live UAT, e.g., first-call latency on gmft TATR model download>
    - <any backend log warnings worth a separate seed>
    ```

    Fill in EVERY `<>` placeholder with literal values from `uat_scratch.json` + Plan 04 Phase F/G outcome. Do NOT leave angle-bracket placeholders.
  </action>
  <verify>
    <automated>test -f .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md &amp;&amp; grep -q "Fixture Results" .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md &amp;&amp; grep -q "D-071.3-16" .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md &amp;&amp; grep -q "engine_acceptance_floor_D_071_3_02" .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md &amp;&amp; grep -q "phase_ship_floor_SC_6" .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md` exists
    - File has valid YAML frontmatter with keys: phase, uat_type, fixtures_tested, status, completed, **engine_acceptance_floor_D_071_3_02** (set to 20), **phase_ship_floor_SC_6** (set to 15 or 12), winner_engine
    - File contains BOTH floor sources documented in the body (the "Floor Sources — DO NOT CONFLATE" table)
    - File contains the literal heading `## Fixture Results` with a 3-row table (thesis + friendly_real + DOCX)
    - File contains the literal heading `## Acceptance Verdict (ROADMAP SC#6)` showing SC#6 as the ship gate
    - File contains the literal heading `## Stretch Indicator (D-071.3-02 Engine Acceptance Floor)` documenting D-071.3-02 as a stretch indicator, NOT the ship gate
    - File contains the literal heading `## D-071.3-16 SEED-021 Decision` with a YES/NO outcome
    - File contains the literal heading `## Plan 04 Phase F + G Reference` documenting BOTH Plan 04 conditional outcomes (httpx unpin AND PyMuPDF smoke)
    - File `status` value (in frontmatter) is computed against SC#6 (15 for gmft / 12 for PyMuPDF), NOT against D-071.3-02 (20)
    - No `<>` placeholder remains in the file (`grep -q "<.*>" file && exit 1`)
  </acceptance_criteria>
  <done>UAT scoreboard written with BOTH floors recorded distinctly. Status keys off SC#6. SEED-021 decision determined.</done>
</task>

<task type="auto">
  <name>Task 4: Close SEED-019; plant SEED-020; conditionally plant SEED-021</name>
  <read_first>
    - .planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md (frontmatter)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md (SEED-021 decision)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-CONTEXT.md (D-071.3-14/15 SEED-020 spec; D-071.3-16 SEED-021 spec)
  </read_first>
  <action>
    **Step 1 — Close SEED-019:**

    Edit `.planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md`:
    - Change frontmatter `status: planted` → `status: closed`.
    - Add frontmatter `closed: <ISO date>` and `closed_by: 071.3-docling-demotion-table-engine-full-rip`.
    - Append to bottom of file a `## Closing Note (2026-05-NN)` section briefly describing how Phase 071.3 closed this seed (winner engine + Docling rip + post-rip UAT counts).

    **Step 2 — Plant SEED-020 (always per D-071.3-14):**

    Create `.planning/seeds/SEED-020-retrieval-quality-audit.md`:
    ```yaml
    ---
    seed_id: SEED-020
    title: Retrieval Quality Audit — embedding model + re-ranker + hybrid retrieval
    status: planted
    planted: <ISO date>
    phase_origin: 071.3-docling-demotion-table-engine-full-rip
    related_seeds: [SEED-006, SEED-019]
    re_open_trigger: |
      Either of these (whichever fires first) per D-071.3-14:
      1. User reports a retrieval-quality complaint (e.g., chat returns wrong /
         irrelevant chunks for a clear query — verifiable via specific failing
         query examples)
      2. v2.7 milestone opens (proactive audit before next major)
    suggested_phase: post-v2.6 (likely early v2.7 — paired with Agent Workspace
      or as a dedicated retrieval phase)
    ---

    ## Why this seed exists

    Phase 071.3 fixed the extraction-quality bottleneck (4 tables → ~20+ on
    thesis after winner engine swap). Once raw text + tables are in the database,
    the next quality bottleneck is RETRIEVAL — choosing which chunks to surface
    to the LLM for a given user query.

    The current retrieval stack is:
    - Embedding model: <current default — record from settings>
    - Re-ranker: <currently enabled? what model?>
    - Hybrid retrieval: <RRF? weighted? off?>

    Limitations to audit (D-071.3-15 scope):
    1. **Embedding model bench** — compare current default vs:
       - BGE-M3 (BAAI, multi-lingual, multi-functionality)
       - voyage-2 (Voyage AI)
       - nomic-embed-text (Nomic, fully open + local-OK)
       - Cohere embed-v3
    2. **Re-ranker integration** — evaluate:
       - Cross-encoder (sentence-transformers/ms-marco-MiniLM-L-6-v2)
       - Cohere Rerank
       - bge-reranker-large (BAAI, open-source)
    3. **Hybrid retrieval architecture** — evaluate:
       - BM25 sparse (tantivy or postgres-trgm) + dense vector fusion
       - RRF fusion vs weighted score combination

    ## Future-phase success criteria template

    - [ ] At least 2 retrieval configs benched on a fixed query set + golden answers
    - [ ] Best config beats current by >= 15% on top-3 hit rate
    - [ ] Production deployment with rollback path (admin UI / settings toggle)
    - [ ] Cost analysis (embedding API spend, re-ranker latency)

    ## Related work

    - Phase 071.3: extraction quality (closed)
    - SEED-006: multimodal extraction quality (planted)
    - SEED-019: PyMuPDF subprocess OOM + non-Docling engine eval (closed)
    ```

    **Step 3 — Conditionally plant SEED-021 (only if UAT thresholds tripped):**

    Read 071.3-HUMAN-UAT.md `## D-071.3-16 SEED-021 Decision` — if "YES":
    Create `.planning/seeds/SEED-021-table-image-recall-lift.md`:
    ```yaml
    ---
    seed_id: SEED-021
    title: Table + Image Recall Lift — GPU-fenced or alternative engines
    status: planted
    planted: <ISO date>
    phase_origin: 071.3-docling-demotion-table-engine-full-rip
    related_seeds: [SEED-006, SEED-018, SEED-019]
    re_open_trigger: |
      Either fires (whichever first) per D-071.3-16:
      1. Next academic-PDF upload misses tables/figures observed by user
         (concrete query: user uploads new academic PDF + reports specific
         tables/figures that didn't surface in retrieval)
      2. Phase 072 vision-cap raise verification shows persistent recall gap
         (RAG-MM-LIFT-01 floor not met on a representative PDF)
    suggested_phase: post-v2.6 (likely v2.7 or v3.0 — depends on whether GPU
      compute becomes routine for the project, see SEED-018 for GPU candidates)
    ---

    ## Why this seed exists

    Phase 071.3 shipped winner=<winner> as the default table engine. Post-rip UAT
    on user's thesis (35 tables / 59 figures ground truth):
    - Tables found: <N> (recall <pct>%)
    - Images found: <N> (recall <pct>%)

    Gap analysis:
    - Tables: <N>/35 = <pct>% — <commentary on remaining gap>
    - Images: <N>/59 = <pct>% — <commentary on Form-XObject / vector-figure / chart-tile gap>

    Investigation candidates (do NOT enable before re-open trigger):
    1. **Table-recall lift:**
       - Marker GPU (SEED-018) — best-of-class on FinTabNet with --use_llm
       - MinerU AGPL — strong on OmniDocBench
       - PaddleOCR PP-StructureV3 — borderless table strength
       - Tabula-py (JVM) — strong on academic per arXiv 2410.09871
    2. **Image-recall lift:**
       - Form XObject deep-walk via fitz.Document.xref_object
       - page.get_drawings() + bbox merging for vector figures
       - Embedded chart-tile reassembly via fitz.Pixmap
       - PDFLib PageGraphics structure walk

    ## Future-phase success criteria template

    - [ ] >= <stretch>% table recall on user thesis
    - [ ] >= <stretch>% image recall on user thesis
    - [ ] No regression on friendly_real.pdf
    - [ ] CPU-OK if no GPU available; else GPU subprocess fence per D-PRD-07

    ## Related work

    - Phase 071.3: replaced Docling with <winner> — table baseline established
    - SEED-006: multimodal extraction umbrella
    - SEED-018: Marker GPL-fenced GPU tables — candidate engine
    ```

    If UAT decision = "NO" — do NOT create SEED-021. Note in summary "SEED-021 not planted — UAT counts cleared threshold."

    Commit all seed changes atomically: `docs(071.3-05): close SEED-019, plant SEED-020 (always), plant SEED-021 (conditional)`.
  </action>
  <verify>
    <automated>grep -q "status: closed" .planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md &amp;&amp; test -f .planning/seeds/SEED-020-retrieval-quality-audit.md &amp;&amp; grep -q "seed_id: SEED-020" .planning/seeds/SEED-020-retrieval-quality-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md` frontmatter contains `status: closed` and `closed:` field
    - File contains `## Closing Note` section referencing Phase 071.3
    - `.planning/seeds/SEED-020-retrieval-quality-audit.md` exists with `seed_id: SEED-020`, `status: planted`, `re_open_trigger` referencing the 2 conditions from D-071.3-14
    - File contains D-071.3-15 scope items: embedding bench (4 models), re-ranker (3 options), hybrid retrieval (BM25 + RRF)
    - IF 071.3-HUMAN-UAT.md says SEED-021 = YES: `.planning/seeds/SEED-021-table-image-recall-lift.md` exists with concrete recall numbers from the UAT
    - IF 071.3-HUMAN-UAT.md says SEED-021 = NO: SEED-021 file does NOT exist
    - Atomic commit exists with message starting `docs(071.3-05):`
  </acceptance_criteria>
  <done>Seeds reconciled: SEED-019 closed, SEED-020 planted always, SEED-021 planted conditionally.</done>
</task>

<task type="auto">
  <name>Task 5: Update PROJECT.md Key Decisions + ROADMAP.md Phase 076/082 wording</name>
  <read_first>
    - .planning/PROJECT.md (full file — locate Key Decisions table)
    - .planning/ROADMAP.md (Phase 076 section + Phase 082 section)
    - .planning/prd-reset/DECISIONS.md (D-PRD-07 — current PyMuPDF AGPL subprocess fence policy)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-04-SUMMARY.md (Phase G outcome — did fence get deleted?)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-CONTEXT.md (Deferred Ideas — D-PRD-NN entry suggested)
  </read_first>
  <action>
    **Step 0 — Verify PROJECT.md heading exists (pre-edit guard):**

    Run a heading discovery grep BEFORE editing — the section is called variably "Key Decisions" or "Locked Decisions" or just "Decisions" across GSD projects. Confirm exact heading text:
    ```
    grep -n "## .*Decisions" .planning/PROJECT.md
    ```

    If grep returns a match, note the exact heading text (e.g., `## Key Decisions` or `## Locked Decisions`) — use that literal heading to anchor the edit. If grep returns ZERO matches, surface a checkpoint message: "PROJECT.md has no decisions heading — please confirm where the new D-PRD-* row should be inserted." Do NOT guess.

    Also capture the BEFORE count of D-PRD entries for the post-edit acceptance check:
    ```
    grep -c "D-PRD-" .planning/PROJECT.md > /tmp/d_prd_count_before.txt
    cat /tmp/d_prd_count_before.txt   # remember this number
    ```

    **Step 1 — Update PROJECT.md Key Decisions:**

    Locate the decisions section using the heading discovered in Step 0. Add a NEW row (D-PRD-NN with NN = next available number, or a new row if numbering is informal):

    | Decision | Locked | Rationale | Supersedes |
    | --- | --- | --- | --- |
    | **Docling demotion + PyMuPDF in-process posture (post-071.3)** | <ISO date> | Docling consistently failed to deliver on its table/image recall promise (Phases 070, 071, 071.1, 071.2 — 4-min stalls, OOM, ~750 MB install, httpx<0.29 pin). Phase 071.3 replaced it with `<winner>` (Plan 01 bench winner) on the per-aspect dispatcher's tables registry; equation engine reduced to `none`. PyMuPDF subprocess fence <deleted (smoke pass, D-071.3-11) | retained (smoke fail, SEED-022 planted)>. httpx unpinned to `>=0.28.0` <(D-071.3-10) | (HELD per SEED-023 — Phase F joint-incompat)>. | D-PRD-07 Appendix (PyMuPDF AGPL fence) — <fully superseded if smoke passed | partially superseded — fence retained while in-process retry deferred> |

    Adapt to the actual table format in PROJECT.md. The KEY data points:
    - Winner engine name (from WINNER.md)
    - PyMuPDF posture (in-process or fenced — from Plan 04 Phase G outcome)
    - httpx unpin status (shipped or HELD per SEED-023 — from Plan 04 Phase F outcome)
    - Relation to D-PRD-07 (fully supersede or partial)

    After editing, capture AFTER count:
    ```
    grep -c "D-PRD-" .planning/PROJECT.md > /tmp/d_prd_count_after.txt
    ```

    **Step 2 — Update ROADMAP.md Phase 076 wording:**

    Locate Phase 076 (Confidence Recalibration) section in `.planning/ROADMAP.md`. Find any text that says "Docling-extracted chunks" or similar. Replace with "new default engine output" or "post-071.3 default-set output". Example sed-style change:

    Before:
    > **Phase 076: Confidence Recalibration** — Re-run Phase 32.5 calibration on Docling-extracted chunks (or reuse, per Q-v2.6-03).

    After:
    > **Phase 076: Confidence Recalibration** — Re-run Phase 32.5 calibration on post-071.3 default-set chunks (the `<winner>` table engine + `none` equations + non-Docling text/image defaults — Q-v2.6-03 answer locked at 071.3 close to "re-run on new defaults").

    Also update Phase 076 SC#1 if it mentions Docling specifically.

    **Step 3 — Update ROADMAP.md Phase 082 SC#1 wording:**

    Locate Phase 082 (Cross-cutting Verification) section. Find SC#1. Replace "Validate Docling output against the user's reference 5/4 vs 50+/0 PDF/DOCX baseline" with "Validate final default-set output against the user's reference baseline (the post-071.3 winner table engine + non-Docling text/image defaults; ground truth: 35 tables / 59 figures on user thesis)".

    **Step 4 — Atomic commit:**

    `docs(071.3-05): PROJECT.md ADR + ROADMAP Phase 076/082 wording — codify Docling demotion`.
  </action>
  <verify>
    <automated>grep -q "071.3" .planning/PROJECT.md &amp;&amp; ! grep -q "Docling-extracted chunks" .planning/ROADMAP.md &amp;&amp; ! grep -q "Validate Docling output" .planning/ROADMAP.md &amp;&amp; test "$(grep -c 'D-PRD-' .planning/PROJECT.md)" -gt "$(cat /tmp/d_prd_count_before.txt 2>/dev/null || echo 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Step 0 heading discovery succeeded: `grep -n "## .*Decisions" .planning/PROJECT.md` returned at least one match (or the executor surfaced a checkpoint message if zero matches)
    - **AFTER count > BEFORE count**: `grep -c "D-PRD-" .planning/PROJECT.md` returns a value strictly greater than the value captured in `/tmp/d_prd_count_before.txt` (proves a new D-PRD-* entry was added, not just text rephrased)
    - `.planning/PROJECT.md` Key Decisions section contains a new entry referencing Phase 071.3, the winner engine name, the PyMuPDF posture, and D-071.3-10 httpx unpin (or SEED-023 hold)
    - `.planning/PROJECT.md` references either "supersedes D-PRD-07" or "supplements D-PRD-07"
    - `.planning/ROADMAP.md` Phase 076 no longer contains the literal string "Docling-extracted chunks"
    - `.planning/ROADMAP.md` Phase 082 SC#1 no longer contains the literal string "Validate Docling output"
    - `grep "071.3" .planning/ROADMAP.md` returns at least 1 match (Phase 076 OR Phase 082 references the post-071.3 reality)
    - Atomic commit exists starting with `docs(071.3-05):` and includes PROJECT.md + ROADMAP.md
  </acceptance_criteria>
  <done>Project-level docs reflect post-Docling-rip reality. New D-PRD-* entry verifiably added (count delta proves it).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: User reviews HUMAN-UAT.md + seed plant decisions; confirms phase ship</name>
  <what-built>
    - Live UAT scoreboard at `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md` (records BOTH floors: D-071.3-02 engine acceptance + ROADMAP SC#6 phase ship)
    - uat_scratch.json (machine-readable scratch, Task 2 output, Task 3 input)
    - SEED-019 closed
    - SEED-020 planted (retrieval-quality audit)
    - SEED-021 planted IF thesis_tables < 25 OR thesis_images < 45 (NOT planted otherwise)
    - PROJECT.md Key Decisions row added codifying Docling demotion (new D-PRD-* entry, count verified)
    - ROADMAP.md Phase 076 + 082 wording updated
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md`. Verify:
       - Counts match what you saw in Task 1+2 SQL queries
       - Status field aligns with SC#6 floor (15 for gmft / 12 for PyMuPDF) — NOT D-071.3-02 (20)
       - D-071.3-02 is documented as a stretch indicator (drives SEED-021), NOT the phase ship gate
       - SEED-021 decision is consistent with D-071.3-16 thresholds (tables < 25 OR images < 45)
    2. Open `.planning/seeds/SEED-019-*.md`. Verify status=closed and closing note references 071.3 + UAT counts.
    3. Open `.planning/seeds/SEED-020-*.md`. Verify scope (embedding bench + re-ranker + hybrid retrieval) and re-open triggers (retrieval-quality complaint OR v2.7 milestone).
    4. If SEED-021 was planted: open `.planning/seeds/SEED-021-*.md` and verify the recall numbers match Task 1 SQL.
    5. Open `.planning/PROJECT.md` Key Decisions table. Verify new row references 071.3 + winner + httpx unpin (or SEED-023 hold) + PyMuPDF posture.
    6. Open `.planning/ROADMAP.md`. Verify Phase 076 + 082 SC#1 wording is updated (no "Docling-extracted chunks" / "Validate Docling output" anywhere).
    7. Reply in chat with one of:
       - `approve 071.3 — phase ready to close`: executor flips ROADMAP checkbox `[x] Phase 071.3` and updates STATE.md
       - `gaps — <specific items>`: executor logs gaps in 071.3-HUMAN-UAT.md and stops; phase remains open until gaps closed
  </how-to-verify>
  <resume-signal>Reply: `approve 071.3 — phase ready to close` OR `gaps — <list>`.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Final commit summary; flip ROADMAP checkbox; update STATE.md</name>
  <read_first>
    - .planning/STATE.md (Current Position section — update phase from 071.3 to next phase, likely 072)
    - .planning/ROADMAP.md (Phase 071.3 plan checkboxes; 5 plans expected — flip all to [x] if approved)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md (verdict)
  </read_first>
  <action>
    Only run if Task 6 returned `approve 071.3 — phase ready to close`.

    1. Edit `.planning/ROADMAP.md`:
       - Flip all 5 plan checkboxes for Phase 071.3 from `[ ]` to `[x]`.
       - Update the Phase 071.3 line summary if needed (mark phase as completed with date).

    2. Edit `.planning/STATE.md`:
       - Update `## Current Position`:
         - Phase: change from 071.3 to next planned (072 per ROADMAP wave 1).
         - Plan: Not started
         - Status: Ready to plan
         - Last activity: <today's ISO date>
       - Append to `### Recent Completed Phases` a new section for Phase 071.3 with UAT verdict + key outcomes (winner engine, Docling rip, PyMuPDF posture, seeds reconciled).
       - Update `Last session` and `stopped_at` frontmatter fields.

    3. Create `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-05-SUMMARY.md` describing Plan 05 outcomes (UAT counts per fixture, seed decisions made, PROJECT.md + ROADMAP.md edits, user approval timestamp).

    4. Final atomic commit: `chore(071.3): close phase — flip ROADMAP checkboxes + STATE update + Plan 05 summary`.

    5. (Optional cleanup) Delete `uat_scratch.json` if you want — it's gitignored and no longer needed once HUMAN-UAT.md exists. The phase summary can keep the counts in narrative form.
  </action>
  <verify>
    <automated>grep -E "071.3-0[1-5]-PLAN.md" .planning/ROADMAP.md | grep -c "\[x\]" | grep -q "^5$" &amp;&amp; grep -q "Phase 071.3" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - 5 `[x]` checkboxes for plans 01-05 in .planning/ROADMAP.md under Phase 071.3
    - `.planning/STATE.md` Current Position now points to the next phase (072 or whatever ROADMAP wave 1 next is)
    - `.planning/STATE.md` Recent Completed Phases includes Phase 071.3 with UAT verdict
    - `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-05-SUMMARY.md` exists
    - Final commit exists starting with `chore(071.3): close phase`
  </acceptance_criteria>
  <done>Phase 071.3 fully closed. ROADMAP and STATE reflect completion. Next phase ready to start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied thesis -> backend ingestion | Live UAT exposes the real Docling-replacement engine to real PDFs |
| live-DB SQL counts -> UAT scoreboard | Numbers must match SQL — operator-driven entry; uat_scratch.json is the audit trail |
| seed plant decisions -> future phase scope | Wrong threshold judgments mis-route future work |
| floor confusion (D-071.3-02 vs SC#6) -> phase ship verdict | Conflating the two floors would gate ship on the wrong number; explicit separation in UAT artifact prevents this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-071.3-05-01 | T (Tampering) | UAT scoreboard counts don't match live DB | mitigate | Task 1 is a checkpoint:human-action — user replies with literal SQL output in chat. Task 2 records counts in `uat_scratch.json` (machine-readable; verified by `jq` in the Task 2 verify gate). Task 3 reads scratch JSON and writes HUMAN-UAT.md. Task 6 has user review the final scoreboard against original SQL output. 3-step double-check. |
| T-071.3-05-02 | I (Info disclosure) | committing user's thesis path or PDF contents to .planning/ | mitigate | UAT scoreboard records COUNTS only; never file contents or absolute paths. Document IDs are UUIDs — safe to commit. uat_scratch.json is gitignored. User may redact path strings if sensitive. |
| T-071.3-05-03 | E (EoP) | misjudged SEED-021 plant decision causes future work to skip a real gap | mitigate | D-071.3-16 threshold is concrete (tables<25 OR images<45). Task 3 records explicit threshold + actual values + Plant=YES/NO in a dedicated section; Task 6 user review catches mistakes. |
| T-071.3-05-04 | D (DoS) | live UAT on thesis triggers OOM in the new winner engine | mitigate | Task 1 is a checkpoint — user observes ingestion outcome. If status='failed' the user replies with the failure mode and Plan 05 stops there; gap escalated to a Plan 06 or carry-forward seed. |
| T-071.3-05-05 | T | ROADMAP wording update accidentally breaks the markdown structure | mitigate | Task 5 only edits text within existing sections; no structural changes. Acceptance criteria verify removal of specific strings ("Docling-extracted chunks" / "Validate Docling output") AND that the file still parses (implicit via downstream tools reading ROADMAP.md). |
| T-071.3-05-06 | T | UAT status field keyed off wrong floor (D-071.3-02 instead of SC#6) | mitigate | Plan 05 explicitly documents BOTH floors and their distinct roles in the UAT artifact (frontmatter keys: `engine_acceptance_floor_D_071_3_02` + `phase_ship_floor_SC_6`). Status calculation is keyed off SC#6 per Task 3 action. Task 6 user review verifies the verdict aligns with SC#6. |
| T-071.3-05-07 | T | PROJECT.md edit missing the heading (heading-name drift) | mitigate | Task 5 Step 0 runs `grep -n "## .*Decisions" .planning/PROJECT.md` BEFORE editing to confirm the exact heading text. If zero matches, surfaces a checkpoint instead of guessing. Step 0 also captures D-PRD-* count BEFORE, and acceptance gates require AFTER > BEFORE (proves new entry added). |

**Severity:** LOW-to-MEDIUM. Highest impact threat is mis-counted UAT triggering wrong seed-plant decision; mitigated by 3-step double-check (chat reply → uat_scratch.json → HUMAN-UAT.md → user review).
</threat_model>

<verification>
- All 3 fixtures re-extracted with `documents.status='completed'` (or failures documented).
- `uat_scratch.json` exists with 3-fixture canonical shape (3 non-empty numeric counts assertable via jq).
- 071.3-HUMAN-UAT.md exists with BOTH floors documented + counts + decisions + Plan 04 Phase F/G reference. Status field keys off ROADMAP SC#6.
- SEED-019 closed; SEED-020 planted; SEED-021 conditionally planted.
- PROJECT.md Key Decisions table contains the new Docling-demotion ADR row (count delta verified).
- ROADMAP.md Phase 076 + 082 wording updated.
- User approval recorded in 071.3-HUMAN-UAT.md.
- ROADMAP.md 071.3 plan checkboxes all flipped to [x] (5 of 5).
- STATE.md Current Position advanced to next phase.
</verification>

<success_criteria>
- ROADMAP SC#6 satisfied: "Live UAT on the user's reference thesis PDF: `/upload` (or `/reextract`) returns; SQL on `document_tables` returns count >= 15 (gmft target) OR >= 12 (PyMuPDF-only target if gmft underperforms). `document_images` count >= 30 (preserved from prior baseline post-migration 046). Chunks >= 200. Recorded in `071.3-HUMAN-UAT.md`."
- ROADMAP SC#7 satisfied: "SEED-019 frontmatter status flipped from `planted` → `closed`. SEED-020 (embedding-model audit) planted with concrete re-open trigger."
- Phase 071.3 closed; next phase (072) ready to start.
</success_criteria>

<output>
After completion, create `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-05-SUMMARY.md` describing: per-fixture UAT outcomes (counts + status), BOTH floor verdicts (SC#6 ship status + D-071.3-02 stretch indicator), SEED decisions made (closed/planted/conditional), PROJECT.md + ROADMAP.md edits committed, user approval timestamp, any carry-forward observations (e.g., gmft cold-start latency, unexpected DOCX behavior, frontend regressions noted during the live UAT).
</output>
</output>
