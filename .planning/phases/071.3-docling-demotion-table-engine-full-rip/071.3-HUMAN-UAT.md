---
phase: 071.3-docling-demotion-table-engine-full-rip
uat_type: live
fixtures_tested: 3
status: green
completed: 2026-05-16
engine_acceptance_floor_D_071_3_02: 20   # CONTEXT.md — drives SEED-021 plant, NOT ship gate
phase_ship_floor_SC_6: 15                # ROADMAP SC#6 — drives ship/no-ship verdict (camelot winner → 15-table target)
winner_engine: camelot
project_level_approval: pending
---

# Phase 071.3 — Live UAT Scoreboard

Winner engine (from Plan 01): **camelot** — bench rationale at `.planning/research/071.3-bench-results.md` + WINNER.md.

Post-rip codebase (Plan 04 outcomes):
- Docling fully removed (adapters, source, env knobs, EXTRACTOR_PRIMARY)
- httpx **unpinned** (Plan 04 Phase F PASS — no SEED-023 plant)
- PyMuPDF **in-process** (subprocess fence deleted in Plan 04 Phase G PASS — no SEED-022 plant)
- TABLE_ENGINES = `{camelot, pdfplumber}`; EQUATION_ENGINES = `{none}`

## Floor Sources — DO NOT CONFLATE

| Floor | Source | Value | Drives |
|---|---|---|---|
| Engine acceptance | D-071.3-02 (CONTEXT.md) | >=20 tables on thesis | SEED-021 conditional plant (D-071.3-16: < 25 tables OR < 45 images -> plant) |
| Phase ship | ROADMAP SC#6 | >=15 (camelot/gmft target) OR >=12 (PyMuPDF-only target) | Binary pass/fail for whether Phase 071.3 ships |

**This UAT's status field is computed against ROADMAP SC#6, NOT D-071.3-02.** D-071.3-02 is a stretch indicator informing SEED-021 (and was cleared at Plan 01 PICK time by camelot's bench).

## Fixture Results

| Fixture | Engine | Tables (GT) | Images (GT) | Chunks | Status | Notes |
|---|---|---|---|---|---|---|
| User thesis PDF | composable[legacy/camelot/pymupdf_full/none] | **214** (35) | **20** (59) | 461 | completed | tables_recall_vs_GT=611% (raw camelot output — includes false positives per WINNER.md); SC#6 floor=15; D-071.3-02 floor=20 |
| friendly_real.pdf (arXiv 2605.15184v1) | composable[legacy/camelot/pymupdf_full/none] | **15** (n/a) | **1** (n/a) | 65 | completed | generalization signal — same camelot 15-table read as Plan 01 bench (consistent) |
| reference.docx | composable[legacy/camelot/zip_xpath/none] | 1 | 1 | 2 | completed | RAG-MM-LIFT-02 continuity check — zip_xpath_docx works post-Docling-rip (images>0 confirmed) |

**Document IDs (audit trail):**
- thesis PDF: `171fb532-26ae-4d30-84a4-fde5c1959515`
- friendly_real.pdf: `9c77bd7b-4002-4a91-ba63-62f010052eca`
- reference.docx: `5d06405e-3d9b-48ce-946e-0fe121484232`

## Acceptance Verdict (ROADMAP SC#6)

- SC#6 floor for winner `camelot`: **>=15 tables on thesis**
- Thesis table count: **214**
- SC#6 cleared: **YES** (14.3x the floor)
- DOCX chunks > 0 AND DOCX images > 0: **YES** (chunks=2, images=1)
- friendly_real.pdf completed: **YES** (status=completed; tables=15, images=1, chunks=65)

**Status: green**

## Stretch Indicator (D-071.3-02 Engine Acceptance Floor)

- D-071.3-02 floor: **>=20 tables on thesis**
- Actual thesis table count: **214**
- D-071.3-02 cleared: **YES** (10.7x the floor)

This is NOT the phase ship gate; it is the floor used at Plan 01 PICK time and informs the SEED-021 plant condition below. Camelot cleared it at PICK time and the post-rip extraction matches Plan 01's raw bench count exactly (214), confirming the storage layer faithfully persists what camelot detects.

## D-071.3-16 SEED-021 Decision

SEED-021 plant condition (independent of SC#6 / D-071.3-02 — uses D-071.3-16's own thresholds):

- Thesis tables = **214**; SEED-021 plant threshold = `tables < 25`. Triggered: **NO** (214 >> 25)
- Thesis images = **20**; SEED-021 plant threshold = `images < 45`. Triggered: **YES** (20 < 45)
- **Plant SEED-021: YES** (image threshold tripped — table threshold cleared)
- Reason: Camelot delivered overwhelming table recall on the thesis (raw 214 vs ground-truth 35 means false positives, not gaps), but the image side stayed at 20/59 = 34% recall — a 39-figure gap vs ground truth. Image-recall lift is the next bottleneck per D-071.3-04 / SEED-006 / SEED-018 framing. Investigation surface: Form XObject deep-walk via `fitz.Document.xref_object`, `page.get_drawings()` + bbox merging for vector figures (architecture diagrams), embedded chart-tile reassembly via `fitz.Pixmap`.

Note on raw-vs-distinct tables: the 214 count is **post-storage** (every row in `document_tables` for the thesis document). Raw count includes per-page detections camelot found at low confidence; structural fidelity is recorded per row but not gating extraction (per D-071.3-17 — "any detection above bbox-area threshold counts"). Downstream chat retrieval will surface the highest-quality matches per `query_tables` ranking. If post-ship user reports surface specific *missing* tables (not just noise), SEED-021's "Next academic-PDF upload misses tables/figures observed by user" re-open trigger fires.

## Plan 04 Phase F + G Reference

- **PyMuPDF in-process smoke (D-071.3-11):** PASS. Fence retained: NO (deleted). SEED-022 planted: NO. (Plan 04 commit `1482f46` confirmed pages=2, tables=1, images=1 on `reference.pdf` with in-process `fitz`.)
- **httpx unpin (D-071.3-10):** SHIPPED. SEED-023 planted: NO. (Plan 04 commit `5317d4b` — `httpx>=0.28.0` resolves to 0.28.1 because supabase-py's transitive `postgrest==2.29.0` still pins `httpx[http2]<0.29,>=0.26`; the cap-removal policy landed even though the resolved version is unchanged.)

## Overall Verdict

Acceptance criteria status: **met** (keyed off SC#6 — 214 tables on thesis >>> 15-table floor; all three fixtures completed; DOCX continuity confirmed; SEED-021 image-recall plant decision recorded).

Project-level approval: pending — user reviews this scoreboard + seed plant decisions in Task 6 checkpoint.

## Carry-forward observations

- **Camelot raw output may need post-processing.** The 214 raw tables vs 35 ground-truth on the thesis suggests camelot's `Stream` mode is detecting tabular-looking page regions (figure captions, multi-column text blocks, narrow text columns) as tables. This is acceptable for now per D-071.3-17 (bbox-area threshold is the only gate, not structural strictness), and the user accepted shipping best-available per D-071.3-03. A future SEED-021 follow-on may add a low-confidence filter or switch to `lattice` mode for bordered-only tables.
- **Image recall stuck at 20 across both PDF fixtures.** The `pymupdf_full` adapter detects 20 images on the thesis and 1 image on the small arXiv fixture. The 20-image count on a 150+ page academic PDF is suspicious — same `pymupdf_full` engine output as before the Docling rip suggests no regression but also no progress on the 30-figure baseline (pre-046 baseline was 30 images). SEED-021 absorbs this.
- **Camelot first-call latency:** none observed in this UAT — friendly_real.pdf completed almost instantly after upload (status='completed' on first poll at t+0s after `_upload_pipeline` BackgroundTask runs). No warm-up gate needed.
- **DOCX chunks=2 on reference.docx (4 paragraphs source).** This matches the chunker's behavior on small inputs (1 chunk per ~512 tokens or paragraph cluster); not a regression, just a small input. The signal is `chunks > 0 AND images > 0` per D-071.3-13, which is met.
- **Frontend behavior during UAT:** uploads + completion tracked via the documents-list API; the user (operator) running these via the dev frontend at http://localhost:5173/ would see the same `pending -> processing -> completed` transitions backed by Realtime + reconcile. UI-side validation deferred to ongoing manual testing.
