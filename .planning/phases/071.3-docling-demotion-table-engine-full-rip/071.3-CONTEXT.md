# Phase 071.3: Docling Demotion — Table Engine + Full Rip - Context

**Gathered:** 2026-05-16
**Status:** Ready for planning

<domain>
## Phase Boundary

Replace the interim `pdfplumber` table engine (4 of 35 visible tables on the user's reference thesis) with a non-Docling engine that hits at least ≥20 tables (~57% recall, 5× pdfplumber), then complete the Docling demotion started by migration 046 (commit `315f307`, 2026-05-16) by **deleting the Docling runtime dependency end-to-end** — adapters, source files, AGPL fence, env knobs, and the `httpx<0.29` pin that exists only because of Docling.

**Source of demand:** SEED-019 (planted 2026-05-15 during Phase 071.2 close). Promoted here. User's verbatim direction: *"I believe we should neglect Docling completely... we still need to extract the correct tables and images... the whole point of previous phases."*

**Already shipped (do NOT re-do):**

1. **Migration 046** (`supabase/migrations/046_app_settings_disable_docling_aspects.sql`, commit `315f307`, 2026-05-16) flipped defaults: `extraction_table_engine_pdf` `docling_tf` → `pdfplumber`, `extraction_equation_engine` `docling_formula` → `none`. `user_settings.py` defaults match.
2. **Per-aspect dispatcher** (Phase 071.2 Plan 05): `aspects/__init__.py` registries, `extract_composable()` composer, `?engines=` hint on `/upload` + `/reextract`. 071.3 swaps engines INSIDE this dispatcher — does not modify the dispatcher itself.
3. **PyMuPDF subprocess fence** (Phase 071 Plan 03): `backend/extractors/pymupdf_isolated.py` child + `backend/app/services/extractors/pymupdf.py` parent wrapper. 071.3 Plan 04 DELETES this fence if the in-process PyMuPDF smoke test passes post-httpx-unpin (per D-071.3-11).
4. **Research brief** (`.planning/research/071.3-table-engine-comparison.md`, 2026-05-16) — arXiv 2410.09871-backed 3-engine comparison. Top recommendation gmft. PRIMARY SOURCE for Plan 01 bench.
5. **Threadpool sweep + per-call timeout discipline** (Phase 071.1 + 071.2): `run_in_threadpool` wrap for blocking I/O in async handlers (D-v2.5-01). 071.3 Plan 02 follows this pattern when wiring the winner adapter.

**Phase 071.3 owns:**

- **Plan 01** — Bench script + spike on 2 fixtures, pick winning table engine.
- **Plan 02** — New adapter for winner; register in `TABLE_ENGINES`; lazy-import; unit tests.
- **Plan 03** — Migration 047 + default flip + full-schema regen.
- **Plan 04** — Docling rip (hard delete adapters, drop `docling` from `requirements.txt`, unpin `httpx`, delete PyMuPDF subprocess fence if smoke test passes, delete EXTRACTOR_DOCLING_* env knobs).
- **Plan 05** — Live UAT on thesis + DOCX sibling + 2nd academic fixture; close SEED-019; plant SEED-020 (embedding-model audit) + SEED-021 (table/image recall lift, conditional plant).

**What this phase does NOT do:**

- Investigate or fix the 30 / 59 figure-recall gap on the thesis — Plan 05 MEASURES this and feeds SEED-021 if needed (per D-071.3-04). Image-engine swap is a different problem class.
- Add GPU-based engines (Marker, MinerU). SEED-018 (Marker GPL-fenced GPU) stays planted; only escalates if bench shows all 3 candidates below threshold AND `D-071.3-03` doesn't ship best-available — which it does.
- Touch the `?engines=` hint plumbing on `/upload` + `/reextract` — already shipped in 071.2 Plan 05.
- Modify `multimodal_service.extract_and_store_tables/images` — Phase 071.2 Plan 04 already wired `extracted_doc.tables/images` consumption; 071.3 just changes what produces them.
- Re-litigate the engine-selection of text / equation / DOCX-image adapters — defaults already correct post-046.
- Touch frontend — admin UI for engine choice is v3.1 (D-PRD-09).
- Address embedding-model quality — SEED-020 is planted at close, not built.
- Touch Phase 072 scope (vision-call cap raise, DOCX completeness) — already absorbed via 071.2 Plan 05 (DOCX) or unchanged (vision cap).

</domain>

<decisions>
## Implementation Decisions

### Bench coverage + acceptance + fallback (Area A)

- **D-071.3-01 — Bench fixture set:** Plan 01 benches PyMuPDF `find_tables(strategy="text"|"lines_strict")` + gmft (TATR) + Camelot 1.0 Stream against TWO fixtures: (a) the user's reference thesis (ground truth: **35 tables, 59 figures** — illustrative content, NOT a benchmark dataset) and (b) `backend/tests/fixtures/extraction/friendly_real.pdf` (arXiv 2605.15184v1 CC-BY 4.0, already committed from Phase 071.1). No new PDFs uploaded for this bench.
- **D-071.3-02 — Acceptance threshold:** Winning engine must recover **≥ 20 tables** on the thesis (~57% recall, 5× pdfplumber's 4). If the top performer clears this on the thesis, it ships even if recall on `friendly_real.pdf` is lower — the thesis is the user's actual content. Bench script records both fixture scores in `.planning/research/071.3-bench-results.md` for audit.
- **D-071.3-03 — Fallback if all 3 underperform:** Ship best-available. **Do NOT** open Plan 06 for Marker GPU in this phase. Plant SEED-021 (table-recall lift) with re-open trigger "next academic PDF upload misses tables, or v2.7 retrieval-quality complaint." Document the gap in 071.3-HUMAN-UAT.md and 071.3-SUMMARY.md.
- **D-071.3-04 — Image recall scope:** 071.3 is tables-focused. Plan 05 UAT MEASURES `document_images` count vs the 59 ground truth and records the delta, but does NOT fix the image-recall gap. If the gap stays wide (e.g., < 45 / 59), plant SEED-021 (image-recall lift) with concrete re-open trigger.
- **D-071.3-17 — Bench recall metric:** "Any detection above bbox-area threshold (≥ 25 px² total area, configurable in bench script)" counts as a found table. Recall = engine_count / 35 on thesis. Structural fidelity (rows × cols, sample cell content) recorded in bench output for human review but NOT used to gate winner selection. Reasoning: layout-aligned tables sometimes have ambiguous row counts; structural-strictness disqualifies real recoveries.

### Adapter registry shape + failure policy (Area B)

- **D-071.3-05 — Registry shape:** After Plan 04 lands, `TABLE_ENGINES = {<winner>: <adapter>, "pdfplumber": pdfplumber_tables}`. Other benched engines NOT registered. The bench script + research brief document why the winner won — runtime swapping to a benched-but-not-shipped engine requires re-running the bench (intentional friction).
- **D-071.3-06 — Failure policy:** No auto-fallback chain inside the dispatcher. If the winner adapter raises (timeout, parse error, OOM), the exception bubbles to `ingest_document`, which marks `ingestion_step='extracting_failed'` (or equivalent). User recovers via `/reextract?engines=tables:pdfplumber`. Matches Phase 071.2 dispatcher philosophy — no silent quality drops.
- **D-071.3-07 — Migration 047 constraint:** Allowed values for `extraction_table_engine_pdf` reduced to `[<winner>, 'pdfplumber']`. `'docling_tf'` removed from the constraint entirely (was already off the default after migration 046). Migration body: `ALTER TABLE app_settings ALTER COLUMN extraction_table_engine_pdf DROP DEFAULT, ADD CONSTRAINT app_settings_extraction_table_engine_pdf_check CHECK (...)`.
- **D-071.3-08 — Bench script disposition:** Plan 01 produces `backend/scripts/bench_table_engines.py` (committed). Future re-bench friendly. Acceptable LOC ceiling: ~250. CLI shape Claude's discretion (argparse vs typer vs raw — argparse recommended for stdlib-only).

### Cleanup boldness (Area C)

- **D-071.3-09 — Docling file hard delete:** Plan 04 deletes:
  - `backend/app/services/extractors/docling.py` — entire file
  - `backend/app/services/extractors/aspects/text.py::docling_text` — function
  - `backend/app/services/extractors/aspects/tables.py::docling_tf_tables` — function
  - `backend/app/services/extractors/aspects/images_pdf.py::docling_pictures_pdf` — function
  - `backend/app/services/extractors/aspects/equations.py::docling_formula_equations` — function (leaves `none_equations` as the only equation engine)
  - `backend/app/services/extractors/aspects/__init__.py` imports + registry entries for the 4 above
  - All `from app.services.extractors.docling import ...` references in tests + code
  - `backend/tests/integration/test_pdf_extractor_docling_compat.py` — entire file (Phase 070 spike artifact)
  - Any other `test_docling*.py` files in `backend/tests/`
  - No deprecation breadcrumbs. Git history is sufficient.
- **D-071.3-10 — httpx unpin scope:** Plan 04 changes `httpx>=0.28.0,<0.29.0` to `httpx>=0.28.0` (no upper bound) in `backend/requirements.txt`. Comment block above the line updated to reflect Docling removal as the rationale. CI integration test for supabase compatibility runs once before commit to verify no joint-incompatibility surface.
- **D-071.3-11 — PyMuPDF subprocess fence delete:** Plan 04 runs the smoke test `backend/tests/integration/test_pymupdf_in_process.py` (Plan-04 authored): import `fitz` directly in the parent process AFTER httpx unpin, exercise `fitz.Document.find_tables()` + `.get_images()` on a known-good PDF, assert no crash + counts non-zero. If smoke test passes, Plan 04 deletes:
  - `backend/extractors/pymupdf_isolated.py` (child entrypoint)
  - `backend/app/services/extractors/pymupdf.py` (parent wrapper)
  - `backend/tests/integration/test_pymupdf_fence.py` (AGPL invariant test)
  - The fence-related requirements.txt comment block above the PyMuPDF line
  - `pymupdf_full_images_pdf` adapter at `aspects/images_pdf.py` rewritten to call `import fitz` directly with lazy import
  - The AGPL note in `requirements.txt` for PyMuPDF stays (legal disclosure) but the fence-enforcement note is removed.
  - Per D-PRD-07, AGPL in-process is permitted for PyMuPDF specifically — the legal posture is already established.
  - If smoke test FAILS (PyMuPDF + new httpx + supabase combination breaks), Plan 04 stops the rip there — keeps fence + reports the gap. SEED-022 plants the in-process retry trigger.
- **D-071.3-12 — EXTRACTOR_DOCLING_* env knobs:** Plan 04 deletes from `backend/app/config.py`:
  - `EXTRACTOR_DOCLING_TIMEOUT_S` field
  - `EXTRACTOR_DOCLING_DISABLE_TABLE_STRUCTURE` field
  - `EXTRACTOR_DOCLING_IMAGES_SCALE` field
  - All references in tests + adapters (already deleted in D-071.3-09 cascade)
  - `EXTRACTOR_PRIMARY` env var: AUDIT — if only Docling consumed it, delete; otherwise leave. (Plan 04 verifies.)

### Live UAT + seed planting (Area D)

- **D-071.3-13 — Plan 05 UAT scope:** Three test surfaces:
  1. Re-extract user's reference thesis under new defaults — assert `document_tables` count ≥ 20 (D-071.3-02 floor) AND record `document_images` count for SEED-021 evidence.
  2. Re-extract `friendly_real.pdf` (arXiv) — record both counts for generalization signal.
  3. Re-extract a DOCX sibling fixture (any committed DOCX from prior phase fixtures) — assert chunks > 0 AND `document_images` > 0 (verifies `zip_xpath_docx` adapter still works post-Docling-rip). The DOCX path doesn't depend on Docling runtime (zip_xpath was a verbatim XPath port, not a wrapper), but Plan 04's deletions could break imports if not careful.
  All results recorded in `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md`.
- **D-071.3-14 — SEED-020 trigger conditions:** Re-open trigger has TWO independent conditions (whichever fires first):
  1. User reports retrieval-quality complaint (e.g., chat returns wrong/irrelevant chunks for a clear query)
  2. v2.7 milestone opens (regardless of complaints — proactive audit before next major)
- **D-071.3-15 — SEED-020 scope:** Comprehensive retrieval-quality audit, NOT just embedding swap:
  - Embedding model bench (current default vs BGE-M3 vs voyage-2 vs nomic-embed-text vs Cohere embed-v3)
  - Re-ranker integration (cross-encoder, Cohere Rerank, or `bge-reranker-large` open-source)
  - Hybrid retrieval architecture (BM25 sparse + dense vector fusion, RRF or weighted)
  Includes concrete success criteria template for the future phase.
- **D-071.3-16 — SEED-021 plant conditions:** Plan 05 plants SEED-021 (table + image recall lift) IF AND ONLY IF either:
  - Thesis post-ship table count stays below 25 (i.e., > 10-table gap from ground truth 35), OR
  - Thesis post-ship image count stays below 45 (i.e., > 14-figure gap from ground truth 59)
  Re-open trigger: "Next academic-PDF upload misses tables/figures observed by user, OR Phase 072 vision-cap raise verification shows persistent recall gap." Connects to SEED-018 (Marker GPU as one of the candidate engines for the lift).

### Claude's Discretion

- **gmft model download policy** — first-call lazy download (~120 MB TATR weights) vs FastAPI startup pre-warm. Recommend lazy first-call to keep cold-start fast; document expected first-extract latency (+15–30s) in the adapter docstring.
- **gmft thread-safety wrapping** — known issue #55 (thread safety) is moot under single-uvicorn-worker (D-v2.5-02) + `run_in_threadpool` discipline. Plan 02 wraps gmft calls in `run_in_threadpool` as a precaution. Phase 077 multi-worker re-evaluates.
- **Bench script CLI** — argparse vs typer vs raw `sys.argv`. Recommend argparse (stdlib, no new dep).
- **Bench output format** — markdown table in `.planning/research/071.3-bench-results.md` is the canonical; CSV/JSON optional if useful for re-running.
- **DOCX regression fixture choice** — pick the smallest DOCX committed in `backend/tests/fixtures/` that has at least one inline image + one table. Plan 05 selects.
- **Migration 047 → full-schema regen timing** — Plan 03 applies migration via Supabase SQL editor (per CLAUDE.md) THEN runs `bash scripts/regenerate-full-schema.sh` THEN commits regenerated file. Standard discipline.
- **In-process PyMuPDF AGPL note wording** — Plan 04 picks the exact `requirements.txt` comment.
- **gmft import naming** — function lives in `aspects/tables.py` as `gmft_tables` (matches existing naming convention `<engine>_tables`).
- **Test fixture for Plan 02 unit tests** — smallest possible PDF with 2-3 known tables. Plan 02 either reuses an existing fixture or hand-crafts one.

### Folded Todos

None. `gsd-sdk query todo.match-phase 071.3` returned zero matches.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase 071.3's direct upstream artifacts (PRIMARY)

- `.planning/research/071.3-table-engine-comparison.md` — **RESEARCH BRIEF** (2026-05-16, 1900 words, arXiv 2410.09871-backed). 3-engine comparison: PyMuPDF `find_tables`, gmft (TATR), Camelot 1.0. Top recommendation gmft. Honest losers documented. PRIMARY SOURCE for Plan 01 bench selection.
- `.planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md` — **PARENT SEED** promoted to this phase. Q1-Q5 are the questions this phase answers (or escalates).
- `.planning/seeds/SEED-006-multimodal-extraction-quality.md` — umbrella seed (multimodal lift family). Informs image-recall reasoning if SEED-021 plants.
- `.planning/seeds/SEED-018-marker-gpl-fenced-gpu-tables.md` — escalation candidate. NOT activated per D-071.3-03 (ship best-available). Re-open trigger: SEED-021's table-recall lift in a follow-on phase.
- `.planning/seeds/SEED-017-pymupdf4llm-agpl-fenced-text.md` — sibling seed (text-aspect). Not consumed unless 071.3 surfaces text-extract issues during rip.

### Prior phase context (locked decisions to inherit)

- `.planning/phases/071-docling-primary-path/071-CONTEXT.md` — D-071-01..16. Specifically D-071-03 (PyMuPDF subprocess fence) — superseded by D-071.3-11. D-071-04 (PyMuPDF requirements pin) — preserved (no change). AGPL fence invariant test `test_fitz_not_imported_by_parent` deleted in Plan 04 per D-071.3-11.
- `.planning/phases/071.1-docling-sc-1-retry-threadpool-timeouts-pymupdf-fallback/071.1-SUMMARY.md` — threadpool sweep pattern. Plan 02 follows for gmft wiring.
- `.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-CONTEXT.md` — D-071.2-01..16. Specifically D-071.2-02 (per-aspect dispatcher) — Plan 02 consumes `TABLE_ENGINES` registry. D-071.2-15/16 (SEED-017/018 plant rationale) — informs D-071.3-03 fallback policy. D-071.2-12 (thesis floor) — referenced for D-071.3-13 UAT.
- `.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-HUMAN-UAT.md` — UAT parking artifacts. Reference for D-071.2-12 floor.

### Project-level locked decisions

- `.planning/PROJECT.md` Key Decisions table — **D-v2.6-01** (supabase 2.29.0 + httpx pin). D-071.3-10 modifies the httpx half.
- `.planning/prd-reset/DECISIONS.md` — **D-PRD-07 + Appendix** (PyMuPDF AGPL subprocess fence policy → D-071.3-11 supersedes when in-process smoke test passes; AGPL in-process posture is established).
- `CLAUDE.md` — venv mandatory; no LangChain; **Migrations applied via SQL editor + regen full schema** (D-071.3-09/10 follows for migration 047); **Reported Bugs cross-check rule** (applied 2026-05-16 — none folded into 071.3).

### Code surfaces touched by this phase

- `backend/app/services/extractors/aspects/__init__.py` — TABLE_ENGINES registry (Plan 02 adds winner, Plan 04 removes Docling entries)
- `backend/app/services/extractors/aspects/tables.py` — Plan 02 adds `<winner>_tables` adapter
- `backend/app/services/extractors/aspects/text.py` — Plan 04 deletes `docling_text`
- `backend/app/services/extractors/aspects/images_pdf.py` — Plan 04 deletes `docling_pictures_pdf`; if D-071.3-11 fence-delete proceeds, rewrites `pymupdf_full_images_pdf` to in-process `fitz` import
- `backend/app/services/extractors/aspects/equations.py` — Plan 04 deletes `docling_formula_equations`
- `backend/app/services/extractors/docling.py` — **Plan 04 deletes entirely**
- `backend/app/services/extractors/pymupdf.py` — **Plan 04 deletes (parent wrapper) IF D-071.3-11 fence-delete proceeds**
- `backend/extractors/pymupdf_isolated.py` — **Plan 04 deletes (child entrypoint) IF D-071.3-11 fence-delete proceeds**
- `backend/app/services/extraction_service.py` — may need import cleanup; Plan 04 audits
- `backend/app/models/user_settings.py` — Plan 03 updates `extraction_table_engine_pdf` default
- `backend/app/config.py` — Plan 04 deletes EXTRACTOR_DOCLING_* fields + EXTRACTOR_PRIMARY (conditional audit)
- `backend/requirements.txt` — Plan 04 removes `docling>=2.93.0,<3.0.0`, unpins `httpx` to `>=0.28.0`, updates comment blocks
- `supabase/migrations/047_*.sql` — Plan 03 ships (constraint update + default flip)
- `supabase/full-schema.sql` — Plan 03 regenerates via `scripts/regenerate-full-schema.sh`
- `backend/scripts/bench_table_engines.py` — **Plan 01 creates** (~150-250 LOC)
- `.planning/research/071.3-bench-results.md` — **Plan 01 creates** (winner pick rationale)
- `backend/tests/unit/test_aspect_engines_tables.py` — Plan 02 extends (unit tests for winner adapter)
- `backend/tests/integration/test_pymupdf_in_process.py` — **Plan 04 creates** (smoke test for D-071.3-11)
- `backend/tests/integration/test_pdf_extractor_docling_compat.py` — **Plan 04 deletes**
- `backend/tests/integration/test_pymupdf_fence.py` — **Plan 04 deletes IF D-071.3-11 fence-delete proceeds**
- `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md` — Plan 05 creates

### Reference docs (external)

- **arXiv 2410.09871** — "A Comparative Study of PDF Parsing Tools Across Diverse Document Categories" (Oct 2024). Cited in research brief. Ranks TATR #1 on Scientific/Patent/Financial/Law — informs Plan 01 expected winner.
- **gmft GitHub** (https://github.com/conjuncts/gmft) — MIT, v0.4.3 (Feb 2026), AutoTableDetector/AutoFormatter API
- **PyMuPDF docs** (https://pymupdf.readthedocs.io/en/latest/page.html) — `find_tables(strategy="text"|"lines"|"lines_strict"|"explicit")`
- **Camelot 1.0 docs** (https://camelot-py.readthedocs.io/en/master/) — pdfium-backed, lattice/stream modes

### Reported bugs cross-check (CLAUDE.md MANDATORY rule)

Four open `surface: Agentic-RAG` bugs reviewed 2026-05-16:

- `anthropic-end-of-cycle-shows-actions-not-summary.md` — agent-loop / chat-surface / system-prompts. **Does NOT touch extraction.** Leave open.
- `streaming-indicator-top-bottom-desync.md` — frontend/streaming/typing-indicator. **Does NOT touch extraction.** Leave open.
- `tool-output-download-bloat-intermediate-artifacts.md` — frontend/tool-card/code-output/UX. **Does NOT touch extraction.** Leave open.
- `thread-switch-blank-state-load-latency.md` — frontend/chat-surface (status: `folded` into Phase 068.5). **Not 071.3 scope.**

**Routing decision: NONE folded into Phase 071.3.**

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **`TABLE_ENGINES` registry** at `backend/app/services/extractors/aspects/__init__.py:61-64` — Plan 02 extends; Plan 04 trims.
- **`pdfplumber_tables` adapter** at `aspects/tables.py:20-37` — STAYS as fallback in `TABLE_ENGINES`. Don't delete.
- **`zip_xpath_docx` adapter** at `aspects/images_docx.py` — verbatim XPath port of Docling `MsWordDocumentBackend` (Phase 071.2 Plan 05). **Has NO Docling runtime dep** — survives the Docling rip cleanly. Plan 05 UAT verifies.
- **`ExtractedDocument` + `TableData` dataclasses** at `extraction_service.py` — Plan 02 winner adapter returns `list[TableData]`. Existing shape preserved.
- **`run_in_threadpool` discipline** (D-v2.5-01) — Plan 02 wraps gmft + Camelot calls per Phase 071.1 + 071.2 pattern.
- **Lazy-import Pattern SP-4** — Plan 02 follows: heavy deps imported INSIDE adapter function body, not at module top.
- **`load_app_settings()` + `UserEffectiveSettings`** at `models/user_settings.py:91, 290` — Plan 03 updates `extraction_table_engine_pdf` default to winner.
- **Migration apply discipline** (CLAUDE.md) — apply via Supabase SQL editor, then `bash scripts/regenerate-full-schema.sh`, commit both. Plan 03 follows.
- **`bench_table_engines.py` shape inspiration** — Plan 01 may mirror Phase 071.2 probe harness style (probe_docling_timeout.py, if it exists in git history) for CLI consistency.
- **`friendly_real.pdf` fixture** at `backend/tests/fixtures/extraction/friendly_real.pdf` (arXiv 2605.15184v1 CC-BY 4.0) — Plan 01 bench fixture #2.
- **`backend/tests/integration/test_extraction_dispatcher.py`** — Plan 02 adds winner-adapter integration test alongside existing dispatcher tests.

### Established Patterns

- **Per-aspect dispatcher** (Phase 071.2 D-071.2-01..04) — Plan 02/03/04 stay within the registry-swap pattern. Don't introduce a new dispatch mechanism.
- **Lazy heavy-dep imports** (Pattern SP-4) — gmft, Camelot, fitz all imported inside adapter function bodies. Zero FastAPI startup cost.
- **Migration → SQL editor → regen full-schema → commit** (CLAUDE.md) — Plan 03 follows verbatim.
- **`?engines=` hint plumbing** on `/upload` + `/reextract` (Phase 071.2 Plan 05) — no changes needed; winner becomes a valid value once migration 047 lands.
- **Bubble-on-failure** for adapter exceptions — matches D-071.3-06.

### Integration Points

- **`extract_composable(raw, mime, engines)`** at `extraction_service.py` — calls `TABLE_ENGINES[engines["tables"]](raw, mime)`. Plan 02 winner adapter is invoked here.
- **`/upload` + `/reextract` routes** at `backend/app/api/documents.py` — already accept `?engines=tables:<name>` per 071.2 Plan 05. Winner name flows through.
- **`multimodal_service.extract_and_store_tables(..., extracted_doc=ExtractedDocument)`** — already reads `extracted_doc.tables` per 071.2 Plan 04. Plan 02 winner populates these.
- **`documents.extractor` column + `pdf_extraction_runs` telemetry** — populated by the active adapter. Plan 02 winner sets engine name correctly.
- **`app_settings.extraction_table_engine_pdf`** — read at runtime via `load_app_settings()`. Plan 03 migration 047 + Plan 04 user_settings.py update align.

### Phase 071.3 risk surfaces

- **gmft cold-start (TATR model download ~120 MB on first call)** — Mitigation: lazy first-call; document expected first-extract +15–30s latency in adapter docstring. Phase 077 multi-worker may revisit pre-warm strategy.
- **httpx unpin: surprise incompatibility with supabase 2.29 or new winner deps** — Mitigation: Plan 04 runs CI integration test BEFORE final commit. If fail, stops the unpin step there, reports gap.
- **PyMuPDF in-process AGPL exposure** — Mitigation: D-PRD-07 documented this is OK for PyMuPDF specifically; legal posture already established. requirements.txt comment retains AGPL disclosure.
- **Docling rip cascade — accidentally breaking imports in tests** — Mitigation: Plan 04 runs full test suite locally before commit. Any deletion-induced ImportError fixes inline.
- **gmft thread safety (issue #55)** — Mitigation: single-uvicorn-worker (D-v2.5-02) + `run_in_threadpool` covers. Phase 077 multi-worker re-evaluates.
- **Bench result variance** — different runs of gmft on CPU may have ±1-2 table variance. Mitigation: bench script runs each engine 3× and reports median + range. Plan 01 picks winner based on median.
- **D-071.3-11 in-process smoke test fails** — Plan 04 stops the fence-delete step, keeps fence + reports gap via SEED-022 (auto-planted). Other Plan 04 deletions (Docling files, env knobs, requirements line) STILL proceed — the fence is the only conditional element.

</code_context>

<specifics>
## Specific Ideas

- **Thesis ground truth (user-stated 2026-05-16):** 35 tables, 59 figures. Illustrative content used for development verification, NOT a benchmark dataset. The "real" production goal is accurate extraction at this scale on similar academic documents — not optimizing to one specific PDF's quirks.
- **Current baseline (after migration 046):** 4 tables (pdfplumber) / 30 figures (pymupdf_full) / chunks ≥ 200. Gap: 31 tables, 29 figures.
- **Acceptance target on thesis:** ≥ 20 tables (D-071.3-02). 5× pdfplumber. Realistic for CPU-only ML on layout-heavy academic content per arXiv 2410.09871 (TATR / gmft).
- **Stretch target:** ≥ 25 tables triggers no follow-up; ≥ 20 ships winner + plants SEED-021 conditionally; < 20 ships best-available + always plants SEED-021.
- **Why no Docling fallback:** user-stated direction "neglect Docling completely." `feedback_docling_skepticism` + `project_docling_quality_unproven` in memory. The dispatcher pattern is preserved (engine choice still swappable), but Docling adapters do not survive Plan 04.
- **Why no GPU-engine inclusion:** Marker / MinerU are GPL/AGPL + multi-GB + GPU-first. CPU-OK gmft beats them on Scientific category per arXiv 2410.09871. GPU-engine work is genuinely a different phase (SEED-018 stays planted for that future trigger).
- **Vibe-coder plain summary:** Phase 071.3 picks a smarter table-finder, swaps it in, then deletes Docling (the thing that kept timing out and crashing). After this lands the system finds 5× more tables on your thesis and the install is ~750 MB smaller. We also measure the figure count to surface a separate follow-up if needed, and plant a "look at the embedding model later" reminder.

</specifics>

<deferred>
## Deferred Ideas

- **Plan 06 — Marker GPU table engine** — escalation if D-071.3-03 had said "halt and open Plan 06". It didn't (ship best-available). **Re-open trigger:** SEED-021 fires (table recall stays < 25 on the thesis), AND a follow-on phase explicitly requests GPU-fenced extractor evaluation.
- **Image recall lift (29 / 59 figures missed)** — out of 071.3 scope per D-071.3-04. **Re-open trigger:** SEED-021 plants if Plan 05 UAT confirms post-ship image gap > 14. Investigation candidates: Form XObject deep-walk, `page.get_drawings()` + bbox merging for vector figures (architecture diagrams), embedded chart-tile reassembly.
- **gmft model pre-warm in FastAPI startup** — Claude's discretion in Plan 02. **Re-open trigger:** ingest UX complaints about first-document latency.
- **gmft subprocess fence (defense against thread-safety issue #55 in multi-worker future)** — out of 071.3 scope. **Re-open trigger:** Phase 077 multi-worker validation harness flags gmft thread-safety issue in practice.
- **Re-evaluation of all Docling-adjacent migrations (039-046)** for column simplification — these are engine-agnostic telemetry/schema additions; no cleanup needed. **Re-open trigger:** schema audit milestone.
- **Updating Phase 076 (Confidence Recalibration) ROADMAP entry** — wording shift from "Docling-extracted chunks" to "new default engine output". Cosmetic. **Re-open trigger:** Phase 076 discuss-phase opens (we'll edit then with clean diff).
- **Updating Phase 082 (Cross-cutting Verify) SC#1** — rephrase from "validate Docling output" to "validate final default-set output". Cosmetic. **Re-open trigger:** Phase 082 discuss-phase opens.
- **PROJECT.md Key Decisions table update** — add D-PRD-NN entry codifying the Docling demotion direction (or supersede D-PRD-07 explicitly). **Re-open trigger:** Phase 071.3 close (Plan 05) — small doc patch.
- **SEED-020 (embedding-model + re-ranking + hybrid retrieval audit)** — planted at Plan 05 close per D-071.3-14/15. NOT built in 071.3.
- **SEED-021 (table + image recall lift)** — conditional plant at Plan 05 per D-071.3-16.
- **SEED-022 (in-process PyMuPDF retry — if D-071.3-11 smoke test fails)** — auto-planted by Plan 04 if smoke test fails. NOT planted in advance.
- **Vector-figure clustering** (SEED-006 mentions `page.get_drawings()` + bbox merging) — out of 071.3 scope. **Re-open trigger:** SEED-021 image lift.
- **OCR for scanned/image-only PDFs** — out of 071.3 scope. **Re-open trigger:** Skill Studio milestone or user request for scanned-PDF support.
- **Frontend admin UI for table engine choice** — v3.1 admin shell milestone (D-PRD-09).
- **`do_ocr` user-tunable flag** — Phase 072 / Skill Studio carry-forward, NOT 071.3.

### Reviewed Todos (not folded)

None. `gsd-sdk query todo.match-phase 071.3` returned zero matches — no backlog items relevant to this phase.

### Reported bugs reviewed (not folded — none touch extraction)

- `anthropic-end-of-cycle-shows-actions-not-summary.md` — agent-loop; leave open
- `streaming-indicator-top-bottom-desync.md` — frontend/streaming; leave open
- `tool-output-download-bloat-intermediate-artifacts.md` — frontend/tool-card; leave open
- `thread-switch-blank-state-load-latency.md` — already folded into Phase 068.5

</deferred>

---

*Phase: 071.3-docling-demotion-table-engine-full-rip*
*Context gathered: 2026-05-16*
