---
phase: 071.3-docling-demotion-table-engine-full-rip
plan: 04
subsystem: extraction
tags: [docling-rip, httpx-unpin, pymupdf-in-process, agpl-fence, camelot, per-aspect-dispatcher]

# Dependency graph
requires:
  - phase: 071.3-docling-demotion-table-engine-full-rip
    provides: Plan 03 — migration 047 + camelot default sealed in app_settings + full-schema regen
  - phase: 071.3-docling-demotion-table-engine-full-rip
    provides: Plan 02 — camelot_tables adapter registered in TABLE_ENGINES
provides:
  - "Docling rip complete — runtime code, deps, env knobs, fence cost all removed"
  - "PyMuPDF in-process import via aspect adapters (text + images); subprocess fence retired"
  - "httpx upper-bound pin removed from requirements.txt (D-071.3-10 LOCKED unpin honored)"
  - "Per-aspect composer is the sole primary extraction path; EXTRACTOR_PRIMARY dispatcher cruft gone"
  - "ReextractRequest.engine Literal narrowed to {'pymupdf','legacy'}; docling no longer routable"
affects:
  - "Plan 05 (live UAT) — clean codebase, no Docling references"
  - "Phase 072 (vision-cap raise) — extraction surface stable"
  - "Future supabase major bump — httpx pin no longer a blocker"

# Tech tracking
tech-stack:
  added: []
  removed:
    - "docling>=2.93.0,<3.0.0 (production dep)"
    - "EXTRACTOR_DOCLING_TIMEOUT_S env var"
    - "EXTRACTOR_DOCLING_DISABLE_TABLE_STRUCTURE env var"
    - "EXTRACTOR_DOCLING_IMAGES_SCALE env var"
    - "EXTRACTOR_PRIMARY env var"
    - "backend/extractors/ package (AGPL fence subprocess)"
  patterns:
    - "In-process AGPL imports (PyMuPDF) per D-PRD-07 — no fence required when smoke test verifies stability"
    - "Atomic per-phase commits in oversized plans (8 sub-phases A-H, ~21 files)"
    - "Locked-decision halting: a LOCKED user decision (D-071.3-10) cannot be silently retracted by plan logic — failure plants SEED + surfaces human-action"

key-files:
  created:
    - backend/tests/integration/test_pymupdf_in_process.py
  modified:
    - backend/requirements.txt
    - backend/.env.example
    - backend/README.md
    - backend/app/main.py
    - backend/app/api/documents.py
    - backend/app/services/extraction_service.py
    - backend/app/services/extractors/aspects/__init__.py
    - backend/app/services/extractors/aspects/text.py
    - backend/app/services/extractors/aspects/tables.py
    - backend/app/services/extractors/aspects/images_pdf.py
    - backend/app/services/extractors/aspects/equations.py
    - backend/tests/conftest.py
    - backend/tests/unit/test_aspect_engines_tables.py
    - backend/tests/unit/test_aspect_engines_equations.py
    - backend/tests/unit/test_aspect_engines_images_pdf.py
    - backend/tests/unit/test_extract_composable.py
    - backend/tests/unit/test_extraction_service.py
    - backend/tests/integration/test_documents.py
    - backend/tests/integration/test_extraction_dispatcher.py
  deleted:
    - backend/app/services/extractors/docling.py
    - backend/app/services/extractors/pymupdf.py
    - backend/extractors/pymupdf_isolated.py
    - backend/extractors/__init__.py
    - backend/scripts/probe_docling_timeout.py
    - backend/tests/integration/test_pdf_extractor_docling_compat.py
    - backend/tests/integration/test_docling_extractor.py
    - backend/tests/integration/test_pymupdf_fence.py

key-decisions:
  - "D-071.3-09 hard delete honored — no breadcrumbs, no compat shims, git history is the record"
  - "D-071.3-10 httpx unpin LOCKED applied — requirements.txt no longer carries <0.29; transitive pin still comes from postgrest==2.29.0 (supabase-py's problem to relax)"
  - "D-071.3-11 fence delete proceeded — Phase G smoke test passed (pages=2, tables=1, images=1, no crash) on the committed reference.pdf"
  - "D-071.3-12 env knob deletion: PATTERNS.md #1 finding honored — EXTRACTOR_DOCLING_* fields were NEVER in config.py; deletions at call sites + .env.example + README"
  - "Option A (PATTERNS.md): EXTRACTOR_PRIMARY deleted entirely — per-aspect composer is now the sole primary path"
  - "ReextractRequest.engine Literal narrowed from {'docling','pymupdf','legacy'} to {'pymupdf','legacy'} — Pydantic 422s docling submissions"

patterns-established:
  - "AGPL-in-process pattern: PyMuPDF import lives inline in aspect adapters; AGPL disclosure in requirements.txt is the legal stance (D-PRD-07)"
  - "Smoke-test-gated deletion: Phase G ran D-071.3-11 smoke BEFORE deleting fence files; pass branch proceeded with delete + adapter rewrite"
  - "Locked-decision halting pattern: failure on D-071.3-10 unpin would have HALTED Plan 04 + planted SEED-023 + surfaced human-action (did not fire — joint compat OK because no httpx 0.29.x exists yet)"

requirements-completed:
  - RAG-DOCLING-01

# Metrics
duration: ~80min
completed: 2026-05-16
---

# Phase 071.3 Plan 04: Docling Rip + httpx Unpin + PyMuPDF In-Process Migration Summary

**Docling runtime fully evicted from the codebase; PyMuPDF subprocess fence retired in favor of in-process `import fitz`; httpx upper-bound pin lifted; EXTRACTOR_PRIMARY/EXTRACTOR_DOCLING_* env knobs deleted. Per-aspect composer is now the sole primary extraction path with TABLE_ENGINES=`{camelot,pdfplumber}` and EQUATION_ENGINES=`{none}`.**

## Performance

- **Duration:** ~80 minutes
- **Started:** 2026-05-16T08:30:00Z (approximate — worktree creation time)
- **Completed:** 2026-05-16T09:47:00Z
- **Tasks:** 6 plan tasks executed as 6 atomic per-phase commits (A+B, C+D, E, F, G-PASS, H)
- **Files modified:** 19 modified + 1 created + 8 deleted = 28 file changes total

## Accomplishments

- **Docling end-to-end deletion** — `docling.py`, 4 aspect adapter functions, registry entries, source file, 2 integration tests, 1 probe script, AGPL-fence parent wrapper, AGPL-fence child entrypoint, AGPL-fence invariant test. No deprecation breadcrumbs. Git history is the record (D-071.3-09).
- **httpx unpinned** — `httpx>=0.28.0,<0.29.0` → `httpx>=0.28.0` in `backend/requirements.txt`. The `<0.29` cap existed solely because docling 2.x required `httpx<0.28`; with Docling gone, the cap is gone (D-071.3-10 LOCKED unpin).
- **PyMuPDF subprocess fence retired** — D-071.3-11 smoke test passed (pages=2, tables=1, images=1, no crash on `reference.pdf`). Fence files deleted, `pymupdf_full_images_pdf` and `pymupdf_text` rewritten to in-process `import fitz` (AGPL permitted per D-PRD-07).
- **EXTRACTOR_PRIMARY dispatcher cruft deleted** — PATTERNS.md Option A executed. `_read_primary()`, `_PRIMARY_CACHED`, `_DOCLING`, `_PYMUPDF` caches all removed. `get_extractor()` simplified to LegacyExtractor for any supported mime; per-aspect composer is now the sole extraction path.
- **Env knob hygiene** — `EXTRACTOR_DOCLING_TIMEOUT_S`, `EXTRACTOR_DOCLING_DISABLE_TABLE_STRUCTURE`, `EXTRACTOR_DOCLING_IMAGES_SCALE`, and `EXTRACTOR_PRIMARY` all evicted from `.env.example`, `documents.py`, `main.py`, `README.md` (D-071.3-12).
- **Plan 04-touched test modules clean** — 21/21 passing (aspect engines + extract_composable + extraction_dispatcher + pymupdf_in_process). Pre-existing failures in unrelated modules logged in `deferred-items.md`.

## Task Commits

Each phase was committed atomically per the `<execution_discipline>` block:

1. **Phase A+B (Task 1):** Delete Docling adapters + registry entries + source file + tests — `9b6fabb`
2. **Phase C+D (Task 2):** Drop docling from requirements + delete EXTRACTOR_DOCLING_* env knobs — `fc7cbd1`
3. **Phase E (Task 3):** Delete EXTRACTOR_PRIMARY env var + dispatcher cache (Option A — composer is the primary path) — `c39d31f`
4. **Phase F (Task 4):** Unpin httpx upper bound (D-071.3-10) — Docling rip removed the only constraint — `5317d4b`
5. **Phase G-PASS (Task 5):** Delete PyMuPDF subprocess fence + rewrite to in-process (D-071.3-11) — `1482f46`
6. **Phase H (Task 6):** Final orphan-reference cleanup + verification gate — `2d32829`

## Files Created/Modified

### Created
- `backend/tests/integration/test_pymupdf_in_process.py` — D-071.3-11 smoke test gating the fence-delete decision (committed regardless of branch; re-runnable verifier)
- `.planning/phases/071.3-docling-demotion-table-engine-full-rip/deferred-items.md` — pre-existing failures logged per SCOPE BOUNDARY rule

### Modified — production code
- `backend/requirements.txt` — drop `docling>=2.93.0,<3.0.0`; relax `supabase==2.29.0` → `supabase>=2.29.0`; drop `httpx<0.29` upper bound; rewrite AGPL fence comment block to in-process posture
- `backend/.env.example` — drop the entire `PDF/DOCX extraction (Phase 071)` env-knob block (kept `PYMUPDF_TIMEOUT_S` — still consumed by adapters)
- `backend/app/main.py` — rewrite the line-12 docstring to drop EXTRACTOR_DOCLING_*/EXTRACTOR_PRIMARY mentions
- `backend/app/api/documents.py`:
  - `ReextractRequest.engine: Literal["pymupdf", "legacy"]` (dropped "docling")
  - `/upload`: delete docling_timeout_s try/except + the Docling-timeout-fallback dance
  - `/reextract`: same — timeout now surfaces as a clean 422
  - `ingest_document` lineage tag: literal `"legacy"` default instead of `os.getenv("EXTRACTOR_PRIMARY")`
- `backend/app/services/extraction_service.py`:
  - Drop `_read_primary()`, `_PRIMARY_CACHED`, `_DOCLING`, `_PYMUPDF` module-level caches
  - Simplify `get_extractor()` to return LegacyExtractor for any supported mime
  - Module docstring updated for post-rip state
- `backend/app/services/extractors/aspects/__init__.py`:
  - TABLE_ENGINES = `{camelot, pdfplumber}` (2 entries; docling_tf gone)
  - TEXT_ENGINES = `{legacy, pymupdf}` (docling gone)
  - IMAGE_ENGINES_PDF = `{pdfplumber, pymupdf_full}` (docling_pictures gone)
  - EQUATION_ENGINES = `{none}` (1 entry; docling_formula gone)
- `backend/app/services/extractors/aspects/text.py` — drop `docling_text`; rewrite `pymupdf_text` to in-process `import fitz`
- `backend/app/services/extractors/aspects/tables.py` — drop `docling_tf_tables` + TYPE_CHECKING TableItem import
- `backend/app/services/extractors/aspects/images_pdf.py` — drop `docling_pictures_pdf`; rewrite `pymupdf_full_images_pdf` to in-process `import fitz` + `page.get_images(full=True)` + `doc.extract_image` walking (preserves per-image try/except-skip-on-failure)
- `backend/app/services/extractors/aspects/equations.py` — drop `docling_formula_equations` + Docling helper references; `none_equations` sole engine
- `backend/README.md` — drop Docling pre-pull, troubleshooting steps, AGPL fence test reference; rewrite extraction section to describe the per-aspect dispatcher

### Modified — tests
- `backend/tests/conftest.py` — drop `reset_docling_singleton` fixture
- `backend/tests/unit/test_aspect_engines_tables.py` — drop `test_docling_tf_tables_returns_table_data_with_bbox`
- `backend/tests/unit/test_aspect_engines_equations.py` — drop `test_docling_formula_routes_through_get_converter`
- `backend/tests/unit/test_aspect_engines_images_pdf.py` — rewrite `_run_pymupdf_subprocess` mock to mock `fitz.Document` directly
- `backend/tests/unit/test_extract_composable.py` — swap docling_tf → camelot, docling_formula → stub_formula in monkeypatch fixtures
- `backend/tests/unit/test_extraction_service.py` — rewrite docstrings (no more EXTRACTOR_PRIMARY)
- `backend/tests/integration/test_documents.py`:
  - happy_path: body.engine='pymupdf' instead of 'docling'
  - owner/is_latest 404 tests: same swap
  - Delete the 3 D-071.1-04 Docling-fallback tests
  - Add `test_reextract_timeout_returns_422_no_fallback` (new loud-failure assertion)
  - Replace mock extractor_name strings: `composable[legacy/camelot/pymupdf_full/none]`
- `backend/tests/integration/test_extraction_dispatcher.py` — swap body.engine='docling' → 'pymupdf'; parser-test inputs text:docling → text:legacy

### Deleted
- `backend/app/services/extractors/docling.py` (Phase A+B — UNCONDITIONAL)
- `backend/scripts/probe_docling_timeout.py` (Phase A+B — UNCONDITIONAL, PATTERNS.md #4)
- `backend/tests/integration/test_pdf_extractor_docling_compat.py` (Phase A+B — UNCONDITIONAL)
- `backend/tests/integration/test_docling_extractor.py` (Phase A+B — UNCONDITIONAL)
- `backend/app/services/extractors/pymupdf.py` (Phase G-PASS — fence retired per D-071.3-11)
- `backend/extractors/pymupdf_isolated.py` (Phase G-PASS — fence retired)
- `backend/extractors/__init__.py` + the entire `backend/extractors/` directory (Phase G-PASS — now-empty fence package)
- `backend/tests/integration/test_pymupdf_fence.py` (Phase G-PASS — AGPL fence invariant no longer applies)

## Decisions Made

- **D-071.3-09 hard delete honored** — no breadcrumbs, no compat shims, no "removed for X" comments anywhere in code. Git history (commits `9b6fabb` and `1482f46`) is the trail.
- **D-071.3-10 httpx unpin LOCKED branch A success** — Phase F PASS. The unpin landed; effective resolved version is still 0.28.1 because (a) no httpx 0.29.x stable release exists yet on PyPI, and (b) supabase-py's transitive dep `postgrest==2.29.0` pins `httpx[http2]<0.29,>=0.26`. Our requirements.txt no longer carries the cap — when a 0.29.x stable lands AND supabase-py relaxes its postgrest pin, the bump will happen without any further change here. **No SEED-023 plant needed** (Phase F PASS, not FAIL).
- **D-071.3-11 PyMuPDF in-process — Phase G-PASS branch** — smoke test on the committed `tests/fixtures/extraction/reference.pdf` returned pages=2, tables=1, images=1 with no exception. Fence retired, adapters rewritten to in-process `import fitz`. **No SEED-022 plant needed** (Phase G PASS, not FAIL).
- **D-071.3-12 env knob deletion** — PATTERNS.md #1 audit finding honored: `EXTRACTOR_DOCLING_*` were NEVER in `config.py` (no pydantic_settings.Field declaration); deletions happened at call sites in `documents.py` + the `.env.example` docs block + README.
- **PATTERNS.md Option A applied** — EXTRACTOR_PRIMARY deleted entirely (not reduced to `("pymupdf","legacy")`). Per-aspect composer is the locked dispatcher direction per `feedback_preserve_engine_optionality`; future engines plug in via the per-aspect registry, not via EXTRACTOR_PRIMARY.
- **ReextractRequest.engine Literal narrowed** — `Literal["pymupdf", "legacy"]` (dropped "docling"). Submitting `"docling"` now returns FastAPI auto-422 via Pydantic Literal validation. T-071-04-02 mitigation preserved.

## Conditional-phase outcomes (Phase F + Phase G)

### Phase F outcome: **PASS (Branch A)** — httpx unpin landed cleanly

- `pip install -r requirements.txt` resolved without conflict.
- `import httpx, supabase; get_supabase()` constructs the Client cleanly (httpx=0.28.1, supabase=2.29.0).
- Supabase-touching integration tests (`pytest -k "supabase or auth or threads"`) — 32 passed, 3 pre-existing failures in `test_threads.py` unrelated to httpx (logged in `deferred-items.md` #6: `create_streaming_chat` symbol rename from Phase 061+ refactor).
- **Why no httpx 0.29 even after unpin:** no httpx 0.29.x stable release exists yet on PyPI; the latest stable is 0.28.1, then `1.0.devN` pre-releases. The unpin lands the policy ("no upper bound") even when pip's resolution doesn't immediately change the version. The transitive cap also comes from `postgrest==2.29.0 -> httpx[http2]<0.29,>=0.26`, which is supabase-py's internal concern. Phase F's deliverable is the *policy change* in our requirements.txt, not the resolved version.
- **SEED-023 NOT planted** — Phase F PASS branch.
- **Commit:** `5317d4b refactor(071.3-04-phase-F): unpin httpx upper bound (D-071.3-10) — Docling rip removed the only constraint`

### Phase G outcome: **PASS** — PyMuPDF in-process works post-unpin

- Smoke test result on `tests/fixtures/extraction/reference.pdf`: `[D-071.3-11 smoke] pages=2 tables=1 images=1`. No crash on `fitz.Document(stream=...)`, `page.find_tables()`, or `page.get_images(full=True)`.
- Fence files deleted: `pymupdf_isolated.py` (child), `pymupdf.py` (parent wrapper), `test_pymupdf_fence.py` (AGPL invariant test). The `backend/extractors/` package directory removed (was empty after deletions).
- Aspect adapters rewritten: `pymupdf_full_images_pdf` and `pymupdf_text` now `import fitz` directly with lazy imports. Per-image try/except-skip-on-failure preserved.
- `get_extractor()` simplified — pymupdf branch + `_PYMUPDF` cache removed; the engine is now exclusively consumed via the per-aspect composer.
- AGPL note in `requirements.txt` updated to describe in-process posture per D-PRD-07.
- **SEED-022 NOT planted** — Phase G PASS branch.
- **Commit:** `1482f46 refactor(071.3-04-phase-G-PASS): delete PyMuPDF subprocess fence + rewrite to in-process (D-071.3-11)`

## Deviations from Plan

None substantive — plan executed in exact phase order A→B→C→D→E→F→G-PASS→H, atomic commits per `<execution_discipline>` block.

### Minor in-scope adjustments

1. **Phase C+D**: Plan called out cleaning `backend/app/main.py:12` comment as part of Phase E (EXTRACTOR_PRIMARY removal). I cleaned it during Phase D since the same line referenced both EXTRACTOR_DOCLING_* AND EXTRACTOR_PRIMARY in a single sentence — splitting the edit across two commits would have been more invasive than a single rewrite during Phase D. The Phase E commit later confirmed no orphan EXTRACTOR_PRIMARY references anywhere.

2. **Phase E**: The plan's PATTERNS.md Option A delete left `_PYMUPDF` cache + the `engine == "pymupdf"` branch in `get_extractor()` intentionally (pending Phase G's decision). When Phase G PASS branch ran and deleted the parent wrapper module, that branch became dead code referring to a deleted module — I deleted it as part of the Phase G-PASS commit. Net result is what the plan intended: `get_extractor()` returns LegacyExtractor for any supported mime, no PyMuPDF branch.

3. **Phase E**: The 3 D-071.1-04 fallback tests in `test_documents.py` had to be deleted (not migrated) because the fallback path itself is gone. Replaced with a single `test_reextract_timeout_returns_422_no_fallback` test asserting the new loud-failure behavior. The pre-existing test `test_reextract_explicit_pymupdf_timeout_does_NOT_fallback` also had a long-standing NameError bug (referenced undefined `mock_get_extractor`) — deleting it alongside the others avoided dragging that bug forward.

## Issues Encountered

### Pre-existing test failures (out-of-scope per SCOPE BOUNDARY)

Cataloged in `.planning/phases/071.3-docling-demotion-table-engine-full-rip/deferred-items.md`:

- 2 test_documents.py mock-fixture / golden-fixture issues
- 2 test_extraction_service.py legacy-golden tests (missing `equations` field — Phase 071.2 added the field but golden fixtures weren't refreshed)
- 3 test_threads.py `create_streaming_chat` symbol rename (Phase 061+ refactor stale mock patch points)
- ~40 retrieval/sql/sandbox/streaming async/sync test issues (coroutine never-awaited warnings; sync tests on async functions)
- ~20 frontend TypeScript errors in test files, hooks, components, and stores (zero references to Docling/extractor — confirmed unrelated to Plan 04)

All verified pre-existing via `git stash` baseline before Plan 04 changes. These belong in separate hygiene phases.

### Context utilization

Plan 04's `<execution_discipline>` block mandated `/clear` between Phase E and Phase F if context utilization exceeded 60%. Executor stayed well under 60% throughout — `/clear` not invoked. Total of ~80 minutes wall clock for all 8 sub-phases executed in a single session.

## Threat Flags

None — the rip removed surfaces (Docling runtime, subprocess fence, env knobs); no new network endpoints / auth paths / file access patterns / schema changes were introduced.

## Known Stubs

None introduced. The composer still routes the `equations` aspect through `none_equations` which returns `[]` — this is the *current shipping behavior post-046* (migration 046 flipped the default to `none`), not a Plan 04 stub. Equation extraction is a future-phase concern, documented in PROJECT.md as deferred.

## User Setup Required

None — no env-var changes operators need to make. `EXTRACTOR_DOCLING_*` and `EXTRACTOR_PRIMARY` set in any operator `.env` are now silently ignored (no code path reads them), which is harmless. The documented `.env.example` no longer lists them, so fresh setups won't see them either.

## Next Phase Readiness

- **Plan 05 ready** — clean codebase, no Docling references, in-process PyMuPDF, camelot as table default. Plan 05 runs live UAT on the thesis + arXiv friendly_real.pdf + a DOCX sibling fixture and decides SEED-020 / SEED-021 plants per D-071.3-13/14/15/16.
- **No blockers.**

## Self-Check: PASSED

### Files created/exist:
- FOUND: backend/tests/integration/test_pymupdf_in_process.py
- FOUND: .planning/phases/071.3-docling-demotion-table-engine-full-rip/deferred-items.md
- FOUND: .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-04-SUMMARY.md

### Files deleted (verified absent):
- MISSING (intentional): backend/app/services/extractors/docling.py
- MISSING (intentional): backend/app/services/extractors/pymupdf.py
- MISSING (intentional): backend/extractors/pymupdf_isolated.py
- MISSING (intentional): backend/extractors/__init__.py
- MISSING (intentional): backend/scripts/probe_docling_timeout.py
- MISSING (intentional): backend/tests/integration/test_pdf_extractor_docling_compat.py
- MISSING (intentional): backend/tests/integration/test_docling_extractor.py
- MISSING (intentional): backend/tests/integration/test_pymupdf_fence.py

### Commits exist:
- FOUND: 9b6fabb (Phase A+B)
- FOUND: fc7cbd1 (Phase C+D)
- FOUND: c39d31f (Phase E)
- FOUND: 5317d4b (Phase F)
- FOUND: 1482f46 (Phase G-PASS)
- FOUND: 2d32829 (Phase H)

### Registry assertions:
- TABLE_ENGINES = ['camelot', 'pdfplumber'] — 2 entries — PASS
- TEXT_ENGINES = ['legacy', 'pymupdf'] — 2 entries — PASS
- IMAGE_ENGINES_PDF = ['pdfplumber', 'pymupdf_full'] — 2 entries — PASS
- IMAGE_ENGINES_DOCX = ['inline_shapes', 'zip_xpath'] — 2 entries — PASS
- EQUATION_ENGINES = ['none'] — 1 entry — PASS

### Grep verification:
- `from app.services.extractors.docling` in backend/ — 0 matches — PASS
- `^import docling` / `^from docling` in backend/ — 0 matches — PASS
- `docling` in `backend/requirements.txt` — 0 matches — PASS
- `<0.29` in `backend/requirements.txt` — 1 match (explanatory comment only) — PASS
- `EXTRACTOR_DOCLING_` consumed in code — 0 matches (only commit-trail comments remain) — PASS
- `EXTRACTOR_PRIMARY` consumed in code — 0 matches (only commit-trail comments remain) — PASS

---
*Phase: 071.3-docling-demotion-table-engine-full-rip*
*Plan: 04*
*Completed: 2026-05-16*
