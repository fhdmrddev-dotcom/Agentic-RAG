# Phase 071.3: Docling Demotion — Table Engine + Full Rip - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-16
**Phase:** 071.3-docling-demotion-table-engine-full-rip
**Areas discussed:** Bench coverage + fallback + image scope; Adapter shelf + failure policy; Cleanup boldness; Live UAT + SEED framing

---

## Pre-discussion: scope agreed before discuss-phase opened

Before `/gsd:discuss-phase 071.3` was invoked, the following was locked through prior conversation (2026-05-16):

- **5-plan split:** spike → adapter → migration 047 → Docling rip → live UAT
- **Bench candidates:** PyMuPDF `find_tables(strategy="text")` + gmft (TATR-backed, MIT) + Camelot 1.0 Stream
- **Direction:** Full Docling rip (not opt-in fallback), preserve per-aspect dispatcher, plant SEED-020 (embedding audit) at close
- **Expected winner:** gmft per arXiv 2410.09871 (TATR ranks #1 on Scientific/Patent/Financial/Law)
- **Migration 046 (commit `315f307`, 2026-05-16) demoted Docling defaults at the column level** — Phase 071.3 supersedes the interim `pdfplumber` default

User clarification mid-discussion: the reference thesis contains **35 tables and 59 figures** (illustrative content for development, not a benchmark). Current state: 4 tables (pdfplumber) / 30 figures (pymupdf_full). Phase 071.3 owns the table-recall gap; the image-recall gap is measured but not fixed (per D-071.3-04).

---

## Area A: Bench coverage + fallback plan + image scope

### Q1: Bench fixture set

| Option | Description | Selected |
|--------|-------------|----------|
| Just the user's thesis (35 tables ground truth) | Fastest. Risk: winner might overfit to thesis layout style. Acceptable if you don't plan to load drastically different document types soon. | |
| Thesis + `friendly_real.pdf` (arXiv from 071.1) (Recommended) | Adds a second academic PDF as generalization signal. Already in fixtures. ~5 min extra to bench, no new files committed. | ✓ |
| Thesis + 2-3 additional academic PDFs you'll upload | You provide 2-3 more PDFs. Slowest, highest confidence the winner generalizes. | |

**User's choice:** Thesis + `friendly_real.pdf` (arXiv from 071.1). Locked as **D-071.3-01**.

### Q2: Acceptance threshold for the winner

| Option | Description | Selected |
|--------|-------------|----------|
| Strict — ≥ 25 tables (~71% recall) | High bar. If gmft hits this, ship it; if not, escalate to Plan 06 (Marker GPU). | |
| Moderate — ≥ 20 tables (~57% recall) (Recommended) | 5× pdfplumber. Realistic for CPU-only ML on layout-heavy academic content. Documents the gap, plants a seed for further work. | ✓ |
| Permissive — best-of-3 wins, no floor | Whichever engine finds the most wins, even if it's only 8. Ships fastest, locks in known-low-recall until follow-up. | |

**User's choice:** ≥ 20 tables (~57% recall). Locked as **D-071.3-02**.

### Q3: Fallback if all 3 candidates fall below threshold

| Option | Description | Selected |
|--------|-------------|----------|
| Promote SEED-018 (Marker GPU) as Plan 06 same phase | Bench Marker as 4th candidate. Lengthens phase but doesn't open a new one. Pulls in GPU dependency story. | |
| Ship best-available + plant SEED for next iteration (Recommended) | Accept the gap as documented. Ship the highest-recall engine. Plant SEED-021 with re-open trigger. Keeps 071.3 focused. | ✓ |
| Halt + open separate Phase 071.4 for GPU/heavy-ML engines | Stop 071.3 at Plan 03 (no rip yet), open 071.4 for Marker GPU + MinerU subprocess-fenced. Slowest, but isolates failure-mode handling. | |

**User's choice:** Ship best-available + plant SEED-021. Locked as **D-071.3-03**.

### Q4: Image recall scope (30 / 59 figures)

| Option | Description | Selected |
|--------|-------------|----------|
| Keep 071.3 narrow — measure images in Plan 05 UAT, plant seed (Recommended) | Plan 05 reports image count alongside table count. If 30/59 stays bad, plant SEED-021. Phase 072's vision-cap raise is a different problem class. | ✓ |
| Expand 071.3 — add Plan 06 for image-extraction lift | Investigate why pymupdf_full misses 29 figures. Add a 6th plan. Inflates phase ~30%. | |
| Rescope Phase 072 to include image lift + vision-cap raise | Move image-extraction lift into Phase 072. 072 grows from 3 → 4-5 plans. | |

**User's choice:** Keep 071.3 narrow — measure-only in UAT. Locked as **D-071.3-04**.

**Notes:** User confirmed mid-discussion that the thesis contains **35 tables, 59 figures** (ground truth, illustrative). This raised the question of whether image-recall lift should fold into 071.3. Decision: keep tables-focused, measure images, plant SEED-021 if gap stays wide.

---

## Area B: Adapter shelf + failure policy

### Q1: How many engines in `TABLE_ENGINES` registry after Plan 04

| Option | Description | Selected |
|--------|-------------|----------|
| Just the winner — trim the registry (Recommended) | Cleanest. Registry: [winner, pdfplumber-fallback]. Less surface area. If swap needed later, bench results document why winner won. | ✓ |
| Top-2 from bench — both available via `?engines=` hint | Winner default, runner-up opt-in. More optionality, more maintenance. | |
| All 3 benched engines — winner default, others opt-in | Maximum flexibility, maximum maintenance burden. | |

**User's choice:** Winner-only registry. Locked as **D-071.3-05**.

### Q2: Default engine failure → dispatcher behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Bubble the error — no auto-fallback (Recommended) | Fail loud. ingestion_step marked; user /reextract with `?engines=` hint. Predictable; matches dispatcher philosophy. | ✓ |
| Auto-fallback to pdfplumber on failure | Wrap in try/except; on failure run pdfplumber as backstop. Hides errors but guarantees SOMETHING. Risk: silent recall degradation. | |
| Auto-fallback chain (gmft → Camelot → pdfplumber) | Each engine tried in order. Highest robustness, highest latency on failures, hardest to debug. | |

**User's choice:** Bubble the error. Locked as **D-071.3-06**.

### Q3: Migration 047 constraint

| Option | Description | Selected |
|--------|-------------|----------|
| Only the new winner + pdfplumber (Recommended if Q1 = winner-only) | Cleanest. Drops `docling_tf` from allowed values entirely. | ✓ |
| All registered engines from Q1 | Whatever Q1 picks gets accepted. | |
| All 3 benched engines (even if Q1 = winner-only) | Keep constraint permissive for future flexibility. | |

**User's choice:** Only winner + pdfplumber. Locked as **D-071.3-07**.

### Q4: Bench script disposition

| Option | Description | Selected |
|--------|-------------|----------|
| Commit as `backend/scripts/bench_table_engines.py` (Recommended) | Future re-bench friendly. ~150-250 LOC. | ✓ |
| Throwaway — delete after winner picked | Lean codebase. Re-bench requires rewrite. | |
| Commit to `.planning/research/bench_*` as documentation | Halfway. | |

**User's choice:** Commit as `backend/scripts/bench_table_engines.py`. Locked as **D-071.3-08**.

---

## Area C: Cleanup boldness

### Q1: Docling files — hard delete or soft deprecation?

| Option | Description | Selected |
|--------|-------------|----------|
| Hard delete (Recommended) | Delete `docling.py` + 4 adapters. No breadcrumbs. Git history preserves it. | ✓ |
| Rename to `_deprecated_docling.py` for one milestone | Keeps file marked. Adapters raise NotImplementedError. Drops in v2.7. | |
| Hard delete only `docling.py`, leave adapter stubs raising NotImplementedError | Half-measure. Stubs keep registry shape consistent. | |

**User's choice:** Hard delete. Locked as **D-071.3-09**.

### Q2: httpx unpin scope

| Option | Description | Selected |
|--------|-------------|----------|
| Relax to `httpx>=0.28.0` (no upper bound) (Recommended) | Maximum flexibility. CI integration test catches future supabase incompatibility. | ✓ |
| Keep one major cushion: `httpx>=0.28.0,<0.30.0` | Most cautious. | |
| Pin to exact `httpx==0.28.x` | Locked. Highest maintenance. | |

**User's choice:** Relax to `httpx>=0.28.0` (no upper bound). Locked as **D-071.3-10**.

### Q3: PyMuPDF subprocess fence

| Option | Description | Selected |
|--------|-------------|----------|
| Delete fence + parent wrapper entirely (Recommended) | Cleanest. Adapters call `import fitz` directly (lazy). ~150 LOC dropped. Per D-PRD-07 AGPL in-process is OK for PyMuPDF specifically. | ✓ |
| Delete child entrypoint, keep parent wrapper as thin in-process pass-through | Wrapper stays as API seam; body becomes direct `fitz` call. ~50 LOC kept. | |
| Keep fence as-is — in-process is opt-in via env knob | Conservative. `PYMUPDF_IN_PROCESS=1` opt-in. | |

**User's choice:** Delete fence + parent wrapper entirely (conditional on smoke test passing — see D-071.3-11). Locked as **D-071.3-11**.

### Q4: EXTRACTOR_DOCLING_* env knobs

| Option | Description | Selected |
|--------|-------------|----------|
| Delete entirely from `config.py` + all references (Recommended) | Hard delete with the rip. If users have them in `.env`, silently ignored. Cleanest. | ✓ |
| Keep as no-ops with deprecation warning on startup | Soft transition. Drops in v2.7. | |
| Keep them — they affect docling text adapter if re-enabled | Only matters if Q1 was soft deprecation. Dead. | |

**User's choice:** Delete entirely. Locked as **D-071.3-12**.

---

## Area D: Live UAT scope + SEED-020 framing

### Q1: Plan 05 live UAT scope

| Option | Description | Selected |
|--------|-------------|----------|
| Thesis + DOCX regression spot-check (Recommended) | Re-extract thesis + extract DOCX sibling to verify zip_xpath_docx still works post-rip. Catches DOCX-side regressions. | ✓ |
| Thesis only | Fastest. Doesn't catch DOCX-side regression. | |
| Thesis + DOCX + friendly_real.pdf (arXiv) end-to-end | Most thorough. ~10-15 min extra UAT. | |

**User's choice:** Thesis + DOCX regression spot-check. Locked as **D-071.3-13** (extended in CONTEXT.md to also include `friendly_real.pdf` since it's already in fixtures from D-071.3-01 — minor scope expansion at no extra fixture cost).

### Q2: Image recall measurement in UAT

| Option | Description | Selected |
|--------|-------------|----------|
| Measure and report alongside tables (Recommended) | Plan 05 records both counts vs ground truth (35 / 59). Documents the gap concretely. Feeds SEED-021 with real numbers. | ✓ |
| Skip image measurement — 071.3 is tables-focused | UAT only measures tables. Cleaner phase boundary but loses SEED-021 evidence. | |

**User's choice:** Measure and report alongside tables. Locked as **D-071.3-13** (combined with Q1).

### Q3: SEED-020 trigger conditions

| Option | Description | Selected |
|--------|-------------|----------|
| Both — retrieval-quality complaint OR v2.7 milestone opens (Recommended) | Two independent triggers. Whichever fires first. Belt-and-suspenders. | ✓ |
| Only retrieval-quality complaint | Event-driven. May sit dormant. | |
| Only v2.7 milestone opens | Time-driven. Forces review at next milestone. | |

**User's choice:** Both triggers. Locked as **D-071.3-14**.

### Q4: SEED-020 scope

| Option | Description | Selected |
|--------|-------------|----------|
| Embeddings + re-ranking + hybrid retrieval (BM25 + dense) (Recommended) | Full retrieval pipeline audit. Comprehensive future audit. | ✓ |
| Just embedding models (current vs BGE-M3 / voyage-2 / nomic-embed-text) | Narrow. Re-ranking deferred to a separate seed. | |
| Embeddings + chunking strategy revisit | Broader; overlaps with Phase 32.5 territory. | |

**User's choice:** Comprehensive (embeddings + re-ranking + hybrid). Locked as **D-071.3-15**.

---

## Closing area: Bench recall metric

### Q1: What counts as "a table found"

| Option | Description | Selected |
|--------|-------------|----------|
| Any detection above bbox-area threshold (Recommended) | Count any region the engine emits. Matches industry benchmarks. Structural fidelity recorded but not gated. | ✓ |
| Detection + minimum structural fidelity (≥2 rows AND ≥2 cols) | Filter out 1-cell or single-row 'fake tables'. Tighter recall numbers. | |
| Detection + per-cell content sample check | Engine output must include actual cell text. Strictest. | |

**User's choice:** Any detection above bbox-area threshold. Locked as **D-071.3-17**.

---

## Claude's Discretion

- gmft model download policy — lazy first-call vs FastAPI startup pre-warm (recommend lazy)
- gmft thread-safety wrapping — `run_in_threadpool` is sufficient under D-v2.5-02 single-worker
- Bench script CLI shape — argparse recommended (stdlib)
- Bench output format — markdown table canonical in `.planning/research/071.3-bench-results.md`
- DOCX regression fixture choice — pick smallest committed DOCX with at least one image + table
- Migration 047 → full-schema regen ordering — apply via SQL editor, then regen, then commit
- gmft adapter naming — `gmft_tables` matches existing `<engine>_tables` convention
- Plan 02 unit test fixture — reuse existing OR hand-craft smallest possible PDF with 2-3 known tables
- requirements.txt comment wording for AGPL in-process disclosure

## Deferred Ideas

- Plan 06 Marker GPU — escalation only if SEED-021 fires in a follow-on phase
- Image recall lift (29 / 59 missed) — SEED-021 conditional plant at Plan 05
- gmft model pre-warm — Phase 077 multi-worker may revisit
- gmft subprocess fence — out of scope; Phase 077 re-evaluates if thread-safety issue surfaces in practice
- Re-evaluation of migrations 039-046 — schema-audit milestone
- Phase 076 ROADMAP wording update — cosmetic, when 076 discuss-phase opens
- Phase 082 ROADMAP wording update — cosmetic, when 082 discuss-phase opens
- PROJECT.md Key Decisions table update — small patch at 071.3 close
- SEED-020 embedding-model + re-ranking + hybrid retrieval audit — planted at close, not built
- SEED-021 table + image recall lift — conditional plant at Plan 05
- SEED-022 in-process PyMuPDF retry — auto-planted by Plan 04 if D-071.3-11 smoke test fails
- Vector figure clustering — out of 071.3; SEED-021 lift territory
- OCR for scanned/image-only PDFs — Skill Studio milestone
- Frontend admin UI for table engine choice — v3.1 admin shell
- `do_ocr` user-tunable flag — Phase 072 / Skill Studio carry-forward
