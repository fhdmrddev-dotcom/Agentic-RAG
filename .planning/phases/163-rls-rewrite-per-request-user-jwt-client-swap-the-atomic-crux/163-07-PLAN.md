---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 07
type: execute
wave: 4
depends_on: ["163-01", "163-05"]
files_modified:
  - backend/app/api/documents.py
  - backend/app/api/folders.py
  - backend/app/api/document_views.py
  - backend/app/api/document_relationships.py
  - backend/app/api/classification_rules.py
  - backend/app/api/metadata_fields.py
  - backend/app/api/document_governance.py
  - backend/app/api/knowledge_health.py
  - backend/app/api/kb.py
  - backend/app/services/governance_service.py
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "D-03: request-scoped Depends(get_supabase) sites across the documents/DM cluster routers are swapped to the per-request user-JWT client get_user_supabase — RLS becomes the primary gate on document/folder CRUD"
    - "D-02: request-scoped connectionless pool.*() calls in the documents/DM cluster (incl services/governance_service.py) become explicit get_user_pg_connection blocks carrying SET LOCAL"
    - "The preserved global-folder visibility still works end-to-end: a globally-visible folder/document remains readable for a non-owner through the user-JWT client (RLS folder_is_globally_visible branch fires under role authenticated)"
    - "Retrieval is NOT claimed org-isolated by this plan: the SECDEF retrieval RPCs (match_document_chunks/keyword_search_chunks) bypass RLS and remain Phase 164's job — this plan only swaps the direct-table document/folder request paths"
    - "D-14: .eq('user_id') filters KEPT; D-v2.5-01: run_in_threadpool preserved on blocking supabase-py calls"
  artifacts:
    - path: "backend/app/api/documents.py"
      provides: "document CRUD request handlers on the user-JWT client"
      contains: "get_user_supabase"
  key_links:
    - from: "documents/folders request handlers"
      to: "get_user_supabase / get_user_pg_connection"
      via: "Depends swap + async-with connection"
      pattern: "get_user_supabase|get_user_pg_connection"
    - from: "folder global-visibility"
      to: "RLS folder_is_globally_visible branch"
      via: "user-JWT client under role authenticated"
      pattern: "folder"
---

<objective>
Wave 4, part 2 — the flip on the DOCUMENTS / DM (document-management) cluster. Swap the request-scoped handlers across documents, folders, document_views, document_relationships, classification_rules, metadata_fields, document_governance, knowledge_health, and kb to the user-JWT clients so RLS (with the preserved global-folder branch) becomes the real gate on document/folder CRUD. Retrieval org-isolation is explicitly NOT in scope here — the SECDEF RPCs bypass RLS and are Phase 164; this plan only swaps the direct-table request paths.

Purpose: this cluster carries the folder-global-visibility branch that must keep working for non-owners under the new membership predicates — the swap proves it end-to-end on the live client.

Output: the documents/DM cluster running on the user-JWT clients on both DB paths.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-RESEARCH.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md

<interfaces>
<!-- Consumed from plan 01 (dependencies.py). -->
get_user_supabase(request, current_user, token) -> Client         # per-request anon-key+Bearer client (keep run_in_threadpool)
get_user_pg_connection(request, current_user) -> async CM         # SET LOCAL ROLE authenticated + both GUC forms

Swap rule per call site:
  request-scoped handler → user-JWT client / get_user_pg_connection
  background/cross-tenant → get_service_role_supabase(org_id) (rare in this cluster)
  Retrieval RPC calls (match_document_chunks/keyword_search_chunks) → LEAVE AS-IS (SECDEF, Phase 164)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap documents/folders/document_views/document_relationships request handlers + convert their pool calls</name>
  <files>backend/app/api/documents.py, backend/app/api/folders.py, backend/app/api/document_views.py, backend/app/api/document_relationships.py</files>
  <read_first>
    - backend/app/api/documents.py (11 Depends(get_supabase) — the doc CRUD + any retrieval-RPC call sites; the retrieval RPC calls stay as-is)
    - backend/app/api/folders.py (7 Depends — folder CRUD + the global-folder visibility surface)
    - backend/app/api/document_views.py (6 Depends), backend/app/api/document_relationships.py (3 Depends)
    - backend/app/dependencies.py (the 3 factories from plan 01)
    - backend/app/services/kb.py or the retrieval seam (confirm which calls are SECDEF RPCs to LEAVE untouched — do not "org-isolate retrieval" here)
  </read_first>
  <action>
    Swap request-scoped `Depends(get_supabase)` → `Depends(get_user_supabase)` across these four routers; convert any request-scoped connectionless pool.* calls to `async with get_user_pg_connection(...)`. Do NOT modify calls into the SECDEF retrieval RPCs (match_document_chunks/keyword_search_chunks) — leave the retrieval path exactly as-is (Phase 164 owns its org-scoping). KEEP .eq("user_id") filters (D-14) + run_in_threadpool (D-v2.5-01). Verify the folder-global-visibility path still returns globally-visible folders/documents for a non-owner under the user-JWT client (the RLS folder_is_globally_visible branch).
  </action>
  <acceptance_criteria>
    - request-scoped Depends(get_supabase) in documents/folders/document_views/document_relationships == 0 remaining (any left has a comment: retrieval-RPC-adjacent or background)
    - Retrieval RPC call sites unchanged (grep match_document_chunks / keyword_search_chunks shows no edit)
    - Global-folder visibility still works for a non-owner (existing folders/documents test suite passes)
    - .eq("user_id") filters + run_in_threadpool intact
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_documents.py tests/test_folders.py -x</automated>
  </verify>
  <done>documents/folders/document_views/document_relationships request handlers enforce RLS via the user-JWT clients; retrieval RPC path untouched; global-folder visibility preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Swap classification_rules/metadata_fields/document_governance/knowledge_health/kb + governance_service pool calls</name>
  <files>backend/app/api/classification_rules.py, backend/app/api/metadata_fields.py, backend/app/api/document_governance.py, backend/app/api/knowledge_health.py, backend/app/api/kb.py, backend/app/services/governance_service.py</files>
  <read_first>
    - backend/app/api/classification_rules.py (4 Depends — is_global-branch table), backend/app/api/metadata_fields.py (4 Depends — is_global-branch)
    - backend/app/api/document_governance.py (3 Depends), backend/app/api/knowledge_health.py (8 Depends), backend/app/api/kb.py (5 Depends)
    - backend/app/services/governance_service.py (4 pool.* calls — classify request-scoped vs background)
    - backend/app/dependencies.py (the 3 factories)
  </read_first>
  <action>
    Swap request-scoped Depends(get_supabase) → Depends(get_user_supabase) across these five routers; convert request-scoped connectionless pool.* calls (incl governance_service.py) to get_user_pg_connection; leave any background/aggregate pool calls that legitimately need cross-user scope on the hardened service-role via get_service_role_supabase(org_id). classification_rules + metadata_fields carry the is_global global-read branch — confirm a global rule/field still renders for a non-owner under the user-JWT client. KEEP .eq("user_id") + run_in_threadpool.
  </action>
  <acceptance_criteria>
    - request-scoped Depends(get_supabase) across these five files == 0 remaining (commented exceptions only)
    - governance_service.py request-scoped pool.* calls use get_user_pg_connection; any background aggregate uses get_service_role_supabase(org_id)
    - is_global classification rules / metadata fields still visible to a non-owner (existing suites pass)
    - .eq("user_id") + run_in_threadpool intact
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_classification_rules.py tests/test_metadata_fields.py tests/test_knowledge_health.py -x</automated>
  </verify>
  <done>The rest of the documents/DM cluster runs on the user-JWT clients; global-read branches preserved; belt-and-suspenders + threadpool intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document/folder request → DB | now the user-JWT client (RLS-enforced with the global-folder branch) |
| retrieval RPC → chunks | SECDEF, bypasses RLS — remains Phase 164's isolation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-07a | Information Disclosure | a doc/folder handler left on service-role | mitigate | grep gate: request-scoped Depends(get_supabase) in the cluster == 0 (commented exceptions only) |
| T-163-07b | Availability | global-folder branch regression | mitigate | non-owner global-visibility test passes under the user-JWT client (the RLS OR-branch fires) |
| T-163-07c | Information Disclosure | accidentally "org-isolating" retrieval in 163 | mitigate | retrieval RPC calls untouched — 163 lays document_chunks.org_id substrate only; isolation is Phase 164 (Pitfall 5) |
| T-163-06b | Information Disclosure | connectionless pool.* stays BYPASSRLS | mitigate | request-scoped pool.* converted to get_user_pg_connection |
| T-163-SC | Tampering | dependency installs | accept | no new package |
</threat_model>

<verification>
- request-scoped Depends(get_supabase) across the 9 documents/DM routers == 0 remaining.
- Retrieval RPC call sites unchanged (Phase 164 boundary held).
- Global-folder + is_global visibility preserved for non-owners; document/folder suites pass.
</verification>

<success_criteria>
The documents/DM cluster enforces RLS via the user-JWT clients on both DB paths, the preserved global branches still serve non-owners, retrieval RPCs are untouched (Phase 164 boundary), and belt-and-suspenders filters + threadpool discipline are intact.
</success_criteria>

<output>
Create `.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-07-SUMMARY.md` when done.
</output>
