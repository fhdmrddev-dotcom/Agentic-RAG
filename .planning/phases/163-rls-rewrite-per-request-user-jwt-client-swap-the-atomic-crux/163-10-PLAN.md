---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 10
type: execute
wave: 5
depends_on: ["163-06", "163-07", "163-08", "163-09"]
files_modified:
  - backend/tests/integration/test_163_leak_asyncpg.py
  - backend/tests/integration/test_163_leak_supabase.py
autonomous: false
requirements: [TEN-01, TEN-02, TEN-04]
must_haves:
  truths:
    - "D-07 perf gate: CONCUR-01 <1s cross-tab-GET-during-streaming stays GREEN with the client swap live — the benchmark measures the per-request SET-LOCAL round-trip overhead (NOT a pgvector recall cliff — the retrieval RPCs are SECDEF and bypass RLS); GREEN BEFORE merge"
    - "D-08 go/no-go: the operator runs the live two-user leak test once and reads a pass/fail scoreboard — user B in Org Y provably reads/updates/deletes 0 of user A's (Org X) rows across BOTH DB paths, with the fail-loud auth.uid() preflight + positive control + role-swap-noop diff + spoof/fail-closed cases + GUC-variant arbitration"
    - "D-09 SC#10: the 4-axis UAT (cross-provider x multi-tool x parallel-thread x long-message) confirms Deep Mode stays byte-identical on the native-7 with the swap live — org context rides the request seam only, no shared-path fork"
    - "D-02 GUC-variant is ARBITRATED by the live test (not docs): the shipped form is whichever set makes auth.uid() resolve on this DB — both is the D-11-portable default"
    - "D-11 tier-parity: the swap behaves identically in an isolated single-org deploy and a co-tenant deploy — pure env-var, no hardcoded URL/key/role; local setup unbroken"
    - "The milestone's atomic crux is COMPLETE: membership RLS + the per-request user-JWT context landed together, proven by a live leak test — RLS is now enforced, not decorative"
  artifacts:
    - path: "backend/tests/integration/test_163_leak_asyncpg.py"
      provides: "the operator-run D-08 harness finalized to emit a single pass/fail scoreboard"
  key_links:
    - from: "the swapped app (Wave 4)"
      to: "the D-08 live leak proof + CONCUR-01 + SC#10"
      via: "operator-run go/no-go before merge"
      pattern: "auth.uid()"
---

<objective>
The [BLOCKING] crux go/no-go. With all four Wave-4 swaps live (RLS is now the real gate), run the three acceptance gates before merge: (1) the CONCUR-01 <1s perf benchmark (measures the SET-LOCAL RTT the swap adds); (2) the operator-run D-08 live two-user leak test (the definitive cross-org isolation proof + GUC-variant arbitration); (3) the SC#10 4-axis UAT proving Deep Mode stays byte-identical. Passing all three means the milestone's single load-bearing security transition is complete — RLS enforced, not decorative.

Purpose: the research base is MEDIUM-confidence on the exact SET-LOCAL/GUC semantics — the live two-user test is the arbiter, not documentation (D-08). And a perf regression on the pgvector-adjacent hot path is the phase's highest risk (D-07) — it must be measured on the live DB before merge.

Output: CONCUR-01 GREEN, D-08 pass/fail scoreboard, SC#10 4-axis result; the crux merged only on all-green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-RESEARCH.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] CONCUR-01 perf benchmark with the swap live</name>
  <files>backend/tests/integration/test_163_leak_asyncpg.py</files>
  <read_first>
    - backend/tests/integration/test_058_concurrency.py (the CONCUR-01 gate — the `assert elapsed < 1.0` at :270; REUSE, do not rewrite)
    - .planning/phases/163-.../163-RESEARCH.md § "pgvector + RLS Performance" (the benchmark measures SET-LOCAL RTT overhead, NOT a pgvector recall cliff — the retrieval RPCs are SECDEF and bypass RLS; mitigations if it regresses: batch reads within one txn, the composite (org_id,user_id) index, or the D-12 JWT-membership escape valve)
    - .planning/feedback_user_starts_backend.md (operator starts the backend; do not run_in_background)
  </read_first>
  <action>
    With the Wave-4 swap live, run `test_058_concurrency.py` against the live local DB and confirm the cross-tab-GET-during-streaming path stays <1s (the CONCUR-01 binding gate). If it regresses: first confirm the cause is the per-request SET-LOCAL round-trip (not a pgvector cliff — retrieval RPCs bypass RLS), then apply a mitigation in order of least change — (a) batch multiple reads within one get_user_pg_connection transaction rather than one txn per query, (b) switch the document_chunks index to the composite `(org_id, user_id)` (plan 02 left btree(org_id) as the default), (c) the D-12 escape valve (bake the membership set into the JWT to drop the org_members join — only if the join is proven the cost). Re-run until GREEN before merge.
  </action>
  <acceptance_criteria>
    - `test_058_concurrency.py` passes with the swap live (elapsed < 1.0s) — captured in the plan summary as the pre-merge benchmark number
    - If a mitigation was applied, the summary records which one and the before/after timing
    - The benchmark is confirmed to measure SET-LOCAL RTT (retrieval RPC latency is unchanged — SECDEF bypass)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_058_concurrency.py -x</automated>
  </verify>
  <done>CONCUR-01 <1s is GREEN with the swap live; any regression was root-caused to SET-LOCAL RTT and mitigated; the benchmark number is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator runs the D-08 live two-user leak test — the crux go/no-go</name>
  <what-built>
    All predicates are applied + inert-then-flipped; the app now runs on the per-request user-JWT clients on both DB paths. The authored leak harness (test_163_leak_asyncpg / test_163_leak_supabase / test_163_role_swap) proves cross-org isolation at the mechanism level. This checkpoint is the operator-run LIVE ceremony: one command, a pass/fail scoreboard, and the GUC-variant arbitration that documentation could not settle.
  </what-built>
  <read_first>
    - .planning/phases/163-.../163-RESEARCH.md § "The Live Two-User Leak Test Design (D-08)" (the 7-step runbook)
    - .planning/phases/163-.../163-VALIDATION.md § "Manual-Only Verifications" (the D-08 row)
    - backend/tests/integration/test_163_leak_asyncpg.py + test_163_leak_supabase.py + test_163_role_swap.py
  </read_first>
  <how-to-verify>
    1. Start the backend (operator).
    2. Run the leak harness: `cd backend && python -m pytest tests/integration/test_163_leak_asyncpg.py tests/integration/test_163_leak_supabase.py tests/integration/test_163_role_swap.py -v` — it seeds two users in two orgs and prints a per-assertion scoreboard.
    3. Confirm on the scoreboard: fail-loud auth.uid() preflight PASSED (auth.uid() == the user's UUID); positive control PASSED (user B sees own rows); BOTH-path isolation PASSED (user B reads/updates/deletes 0 of user A's rows via asyncpg AND supabase-py); role-swap-noop diff PASSED (WITHOUT the swap → A's rows visible; WITH → 0); spoof/fail-closed PASSED (no-claims/non-member → 0 rows); GUC-variant arbitration recorded (which forms resolve on this DB — both shipped).
    4. Reply "leak test passed" (or paste the failing assertions).
  </how-to-verify>
  <action>
    Operator-run go/no-go. Claude presents the command + the scoreboard checklist and waits. On pass, the GUC-variant result is recorded in the summary. On any leak, STOP — the crux does not merge; the failing predicate/GUC form is fixed (loop back to the relevant plan) before re-running.
  </action>
  <acceptance_criteria>
    - Operator confirms every scoreboard line PASSED across BOTH DB paths (0 cross-org rows; role-swap diff proven; fail-closed proven)
    - The GUC-variant that resolves auth.uid() on this DB is recorded (both is the shipped default)
    - Any leak blocks merge and is fixed before re-running
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_leak_asyncpg.py tests/integration/test_163_leak_supabase.py tests/integration/test_163_role_swap.py -v</automated>
  </verify>
  <resume-signal>Type "leak test passed" or paste the failing assertions</resume-signal>
  <done>The live two-user leak test passes on both DB paths; cross-org isolation is proven; the GUC variant is arbitrated and recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] SC#10 4-axis UAT + tier-parity — Deep byte-identical with the swap live</name>
  <what-built>
    The org context now rides the request seam (the injected user-JWT client/connection) on every swapped path. agent_loop.py and the provider/gateway path were never touched. This checkpoint confirms the swap did not regress the shared streaming path across providers, and that the deployment-tier contract (D-11) holds.
  </what-built>
  <read_first>
    - .planning/phases/163-.../163-VALIDATION.md § "Manual-Only Verifications" (the SC#10 4-axis row + the CONCUR-01 row)
    - CLAUDE.md § "UAT scoreboard recipe" (the 4-axis bandwidth: cross-provider / multi-tool / parallel-thread / long-message)
    - .planning/feedback_cross_provider_full_native_roster.md + .planning/reference_local_dev_app.md (login fhdmrd@gmail.com / 123456)
  </read_first>
  <how-to-verify>
    With the swap live, the operator exercises the 4 axes and confirms no regression:
    1. Cross-provider: send a normal chat on OpenAI, Anthropic, Google, and OpenRouter (one representative model each) — all complete, stream cleanly, cite correctly.
    2. Multi-tool: one prompt exercising 2+ tools (e.g. search_documents + execute_code) — both fire, results render.
    3. Parallel-thread: Thread A streaming while Thread B accepts a new prompt — no cross-leak, no stall.
    4. Long-message: a >=50-message thread OR a >=5KB prompt — completes without truncation regression.
    5. Confirm the user still sees ONLY their own rows in the live UI (documents/threads/skills) and that a globally-shared folder/skill still renders — RLS + preserved global branches both work end-to-end.
    6. Tier-parity sanity (D-11): confirm nothing hardcoded a URL/key/role — the swap reads only env vars (SUPABASE_URL / SUPABASE_ANON_KEY / SERVICE_ROLE_KEY / POSTGRES_DSN).
    7. Reply "SC#10 passed" (or describe the regression).
  </how-to-verify>
  <action>
    Operator-run UAT. Claude presents the 4-axis + isolation + tier-parity checklist and waits. On pass, record the per-axis result in the summary + VALIDATION.md scoreboard. On regression, STOP — identify whether the swap forked the shared path (it must not — D-09) and fix before merge.
  </action>
  <acceptance_criteria>
    - All 4 SC#10 axes PASS with the swap live (cross-provider full-native + OpenRouter, multi-tool, parallel-thread, long-message)
    - Live UI confirms per-org isolation AND preserved global-share visibility
    - No hardcoded URL/key/role — tier-parity (D-11) holds; local setup unbroken
    - Deep Mode byte-identical (no shared-path fork; agent_loop.py + provider path untouched across the phase diff)
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms the 4-axis + isolation + tier-parity scoreboard PASS</human-check>
  </verify>
  <resume-signal>Type "SC#10 passed" or describe the regression</resume-signal>
  <done>The SC#10 4-axis UAT passes with the swap live; per-org isolation + global-share both work in the live UI; Deep stays byte-identical; tier-parity holds. The atomic crux is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| swapped app → DB | the live isolation boundary; the D-08 test is the adversary proving it holds |
| deployment tier | the swap must not make any tier harder (D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-01 | Elevation of Privilege | role-swap no-op in the live app | mitigate | D-08 role-swap-noop diff on the live app; auth.uid() preflight |
| T-163-02 | Information Disclosure | any residual predicate leak post-swap | mitigate | D-08 both-path two-user isolation across a representative table set is the go/no-go; leak blocks merge |
| T-163-03 | Spoofing | wrong GUC variant false-pass | mitigate | fail-loud auth.uid() preflight; live GUC-variant arbitration; both forms shipped |
| T-163-07 (perf DoS) | Denial of Service | SET-LOCAL RTT regresses CONCUR-01 | mitigate | benchmark <1s before merge; mitigations (batch/composite-index/JWT-escape-valve) if regressed |
| T-163-09 | Tampering | shared-path fork breaks Deep | mitigate | SC#10 4-axis + agent_loop.py/provider path byte-unchanged across the phase diff |
| T-163-SC | Tampering | dependency installs | accept | no new package this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- CONCUR-01 <1s GREEN with the swap live (benchmark number recorded).
- D-08 live two-user leak test PASS on both DB paths (operator scoreboard); GUC variant arbitrated + recorded.
- SC#10 4-axis PASS; live UI shows per-org isolation + preserved global-share; Deep byte-identical; tier-parity holds.
- Phase-wide: `git diff` across the phase shows agent_loop.py + provider/gateway files unchanged.
</verification>

<success_criteria>
All three gates pass: CONCUR-01 <1s, the operator-run D-08 live two-user leak proof (both paths, GUC-arbitrated), and the SC#10 4-axis UAT (Deep byte-identical, per-org isolation + global-share verified live, tier-parity). The milestone's atomic crux — membership RLS + the per-request user-JWT DB context, landed together — is complete and merged only on all-green.
</success_criteria>

<output>
Create `.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-10-SUMMARY.md` when done.
</output>
