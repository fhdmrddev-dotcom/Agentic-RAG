---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 06
type: execute
wave: 4
depends_on: ["163-01", "163-05"]
files_modified:
  - backend/app/api/threads.py
  - backend/app/api/runs.py
  - backend/app/api/panel.py
  - backend/app/api/feedback.py
  - backend/app/api/sandbox_outputs.py
  - backend/app/api/workspace.py
  - backend/app/db/workspace.py
  - backend/app/db/runs.py
  - backend/app/services/todos_service.py
  - backend/app/services/workspace_service.py
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "D-03: request-scoped Depends(get_supabase) sites in the chat cluster (threads.py, runs.py, panel.py, feedback.py, sandbox_outputs.py, workspace.py) are swapped to the per-request user-JWT client get_user_supabase — so RLS becomes the primary gate on the chat hot path"
    - "D-02: request-scoped connectionless pool.*() calls in the chat cluster become explicit `async with get_user_pg_connection(request, current_user) as conn` blocks that carry SET LOCAL — they no longer silently run as postgres/BYPASSRLS"
    - "D-05: the agent-loop async writer (db/runs.py + the run_producer/agent_loop path) KEEPS service-role via get_service_role_supabase(org_id) and WIDENS its ownership reads to org-aware — it is NOT converted to the user-JWT client (no auth.uid() on that async path)"
    - "D-09 (RED LINE): org context rides the REQUEST seam only (the injected user-JWT client / connection) — the provider/gateway path is untouched, agent_loop.py::run_agent_loop stays byte-unchanged, Deep Mode stays byte-identical on the native-7"
    - "D-14: the .eq('user_id') filters in these files are KEPT as belt-and-suspenders (not deleted this milestone); run_in_threadpool stays on every blocking supabase-py call (D-v2.5-01)"
    - "D-01 (precondition honored): org_id now threads through the CLEAN post-162.5 send_message seam via the request-injected client — the producer shell already lives in run_producer.py, so send_message stays thin"
  artifacts:
    - path: "backend/app/api/threads.py"
      provides: "chat/streaming request handlers on the user-JWT client; producer path still service-role (widened)"
      contains: "get_user_supabase"
  key_links:
    - from: "backend/app/api/threads.py request handlers"
      to: "get_user_supabase / get_user_pg_connection"
      via: "Depends swap + async-with connection"
      pattern: "get_user_supabase|get_user_pg_connection"
    - from: "backend/app/db/runs.py (agent-loop writer)"
      to: "get_service_role_supabase(org_id)"
      via: "org-aware widened service-role"
      pattern: "org_id"
---

<objective>
Wave 4, part 1 — the flip on the CHAT/STREAMING hot path. Now that the predicates are applied and inert (plan 05), swap the chat cluster's request-scoped handlers to the user-JWT clients so RLS becomes the real gate, while the agent-loop/producer async-writer path stays service-role (widened to org-aware). This is the CONCUR-01- and SC#10-critical file group (threads.py is the streaming core, just extracted clean in 162.5). The org context rides the request seam ONLY — the provider/gateway path and `agent_loop.py::run_agent_loop` are untouched, so Deep Mode stays byte-identical.

Purpose: threads.py is the milestone's hottest file and the one that touches streaming + the agent loop. Isolating its swap into its own plan keeps the Deep red line and the CONCUR-01 benchmark focused here.

Output: the chat cluster running on the user-JWT clients on both DB paths, with the agent-loop writer widened.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-RESEARCH.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md

<interfaces>
<!-- Consumed from plan 01 (dependencies.py). No re-derivation. -->
get_user_supabase(request, current_user, token) -> Client         # per-request anon-key+Bearer client (keep run_in_threadpool around blocking calls)
get_user_pg_connection(request, current_user) -> async CM         # SET LOCAL ROLE authenticated + both GUC forms on the existing pool
get_service_role_supabase(org_id) -> Client                       # raises without org_id; for the agent-loop async writer ONLY

Swap rule per call site:
  request-scoped handler (has current_user + token in scope) → user-JWT client / get_user_pg_connection
  agent-loop / producer / background writer (no auth.uid()) → get_service_role_supabase(org_id) + widen .eq to org-aware
  NEVER touch agent_loop.py::run_agent_loop or the provider/gateway path (D-09 Deep red line)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap threads.py + runs.py request handlers to the user-JWT clients (both DB paths); keep the producer/agent-loop path service-role</name>
  <files>backend/app/api/threads.py, backend/app/api/runs.py</files>
  <read_first>
    - backend/app/api/threads.py (FULL — the 9 Depends(get_supabase) handlers + the 6 request-scoped pool.* sites; the send_message seam is now clean post-162.5, delegating the producer to run_producer.py)
    - backend/app/services/run_producer.py (the extracted producer shell — this is on the agent-loop async-writer side; it does NOT get the user-JWT client)
    - backend/app/services/agent_loop.py (run_agent_loop — the Deep byte-identical red line; DO NOT modify; confirm the swap does not touch it)
    - backend/app/api/runs.py + backend/app/db/runs.py (insert_run omits org_id — the mig-106 runs_autofill_org_id trigger fills it; reads/finalize by run_id)
    - backend/app/dependencies.py (the 3 factories from plan 01)
    - .planning/phases/163-.../163-PATTERNS.md § "The 4 async service-role writers to widen" (agent loop row)
  </read_first>
  <action>
    In threads.py + runs.py: for each REQUEST-scoped handler that injects `Depends(get_supabase)`, swap to `Depends(get_user_supabase)` (the handler already has current_user + the bearer token in scope; thread the token through). Convert each request-scoped connectionless `pool.fetch/fetchval/fetchrow/execute` call to `async with get_user_pg_connection(request, current_user) as conn:` so it carries SET LOCAL. KEEP run_in_threadpool around blocking supabase-py calls (D-v2.5-01). KEEP the .eq("user_id") filters (D-14). Do NOT convert the producer/agent-loop path: run_producer.py + the run-finalize writes stay on the service-role writer — if they touch db/runs.py inserts/reads, leave them for Task 2's widening. Do NOT modify agent_loop.py or any provider/gateway code (D-09). Preserve the SSE vocabulary + streaming behavior exactly.
  </action>
  <acceptance_criteria>
    - Every request-scoped Depends(get_supabase) in threads.py + runs.py is now Depends(get_user_supabase) (grep: `Depends(get_supabase)` in these two files == only non-request/operator sites, if any, with a comment why)
    - Request-scoped pool.* calls in threads.py are wrapped in `get_user_pg_connection` (grep confirms the async-with usage)
    - agent_loop.py is byte-unchanged (git diff == empty for that file); no provider/gateway file in the diff
    - `.eq("user_id"` count in threads.py is unchanged (belt-and-suspenders kept); run_in_threadpool still wraps blocking supabase-py calls
    - `pytest tests/integration/test_058_concurrency.py -x` still passes (CONCUR-01 smoke — full benchmark is plan 09) and the existing threads/streaming tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_058_concurrency.py tests/test_threads.py tests/test_continue.py -x</automated>
  </verify>
  <done>threads.py + runs.py request handlers enforce RLS via the user-JWT clients on both DB paths; the producer/agent-loop path + provider path are untouched; streaming behavior + Deep red line preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Swap workspace/panel/feedback/sandbox handlers + convert their db/service pool calls; widen the agent-loop writer (db/runs.py) to org-aware</name>
  <files>backend/app/api/panel.py, backend/app/api/feedback.py, backend/app/api/sandbox_outputs.py, backend/app/api/workspace.py, backend/app/db/workspace.py, backend/app/db/runs.py, backend/app/services/todos_service.py, backend/app/services/workspace_service.py</files>
  <read_first>
    - backend/app/api/workspace.py + backend/app/db/workspace.py (the 6 Depends + 14 pool.* sites — the workspace_files/workspace_file_versions CRUD paths)
    - backend/app/api/panel.py (3 Depends + 4 pool), backend/app/api/feedback.py (2 Depends), backend/app/api/sandbox_outputs.py (1 Depends)
    - backend/app/db/runs.py (the agent-loop writer — insert_run :52-66 + reads/finalize; STAYS service-role, widen ownership reads to org-aware via get_service_role_supabase(org_id))
    - backend/app/services/todos_service.py (2 pool) + backend/app/services/workspace_service.py (1 pool) — classify each call: request-scoped → get_user_pg_connection; producer/background → service-role
    - .planning/phases/163-.../163-PATTERNS.md § "The 4 async service-role writers to widen"
  </read_first>
  <action>
    Swap the request-scoped Depends(get_supabase) → Depends(get_user_supabase) in panel.py, feedback.py, sandbox_outputs.py, workspace.py. Convert their request-scoped connectionless pool.* calls (incl db/workspace.py) to `async with get_user_pg_connection(...)`. For db/runs.py (the agent-loop async writer, no auth.uid()): KEEP it service-role but route it through get_service_role_supabase(org_id) and WIDEN its ownership reads to add an org_id predicate (D-05) — insert_run continues to omit org_id (the mig-106 trigger fills it). For todos_service.py + workspace_service.py: convert request-scoped pool calls; leave any producer/background-invoked pool calls on the service-role path. KEEP all .eq("user_id") filters + run_in_threadpool.
  </action>
  <acceptance_criteria>
    - panel/feedback/sandbox_outputs/workspace request handlers use get_user_supabase; their request-scoped pool.* calls use get_user_pg_connection
    - db/runs.py stays service-role via get_service_role_supabase(org_id) and its ownership reads carry an org_id predicate (grep `org_id` present); insert_run still omits org_id (trigger-filled)
    - No .eq("user_id") filter deleted (D-14); run_in_threadpool intact
    - `pytest tests/integration/test_058_concurrency.py -x` + the workspace/panel test suites pass
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_058_concurrency.py tests/test_workspace.py -x</automated>
  </verify>
  <done>The rest of the chat cluster runs on the user-JWT clients; the agent-loop writer is widened to org-aware service-role; belt-and-suspenders filters + threadpool discipline intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat request → DB | now crosses the user-JWT client (RLS-enforced) instead of the service-role singleton |
| agent-loop async writer → DB | no auth.uid(); org-scoping is app-enforced behind get_service_role_supabase(org_id) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-06a | Information Disclosure | a chat handler left on service-role | mitigate | grep gate: request-scoped Depends(get_supabase) in the chat files == 0 (or commented operator/writer exceptions) |
| T-163-06b | Information Disclosure | connectionless pool.* stays BYPASSRLS (Pitfall 6) | mitigate | request-scoped pool.* converted to get_user_pg_connection; grep confirms no bare pool.fetch/execute on a request path |
| T-163-09 (Deep regression) | Tampering | provider/gateway path fork | mitigate | agent_loop.py byte-unchanged; no provider file in the diff; CONCUR-01 smoke + streaming tests pass; full SC#10 in plan 09 |
| T-163-05 | Information Disclosure | agent-loop writer under-scoped | mitigate | db/runs.py widened to org-aware via get_service_role_supabase(org_id); .eq belt-and-suspenders retained |
| T-163-SC | Tampering | dependency installs | accept | no new package |
</threat_model>

<verification>
- `git diff backend/app/services/agent_loop.py` is EMPTY (Deep red line held); no provider/gateway file in the plan diff.
- Chat request handlers use get_user_supabase / get_user_pg_connection; db/runs.py stays org-aware service-role.
- CONCUR-01 smoke (test_058) + streaming/continue/workspace suites pass.
</verification>

<success_criteria>
The chat/streaming hot path enforces RLS via the user-JWT clients on both DB paths; the agent-loop async writer is widened to org-aware service-role; agent_loop.py and the provider path are untouched (Deep byte-identical); belt-and-suspenders filters + threadpool discipline preserved.
</success_criteria>

<output>
Create `.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-06-SUMMARY.md` when done.
</output>
