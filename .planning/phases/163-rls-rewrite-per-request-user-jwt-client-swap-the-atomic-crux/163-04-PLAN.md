---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 04
type: execute
wave: 2
depends_on: ["163-01"]
files_modified:
  - backend/tests/integration/test_163_leak_asyncpg.py
  - backend/tests/integration/test_163_leak_supabase.py
  - backend/tests/integration/test_163_role_swap.py
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "D-08: an asyncpg two-user leak test proves user B (Org Y) reads/updates/deletes 0 of user A's (Org X) rows across a representative table set, with a fail-loud SELECT auth.uid() preflight asserting the expected UUID BEFORE any isolation assertion"
    - "D-08: a supabase-py two-user leak test proves the per-request user-JWT client (anon key + user B's Bearer, no singleton mutation) reads 0 of user A's rows"
    - "T-163-01 detector: the role-swap-noop test proves that WITHOUT SET LOCAL ROLE the same query returns ALL rows (BYPASSRLS default), and WITH it returns own rows only — the diff proves the role swap is the thing turning RLS on"
    - "Spoof / fail-closed: no-claims (auth.uid() NULL) returns 0 rows (fail-closed); a non-member sub returns 0 rows; membership (not just ownership) is the gate"
    - "GUC-variant arbitration (D-02 deliverable): the harness is parameterized over legacy-only / JSON-only / both, and records which variants make auth.uid() resolve on THIS local DB — both is the shipped D-11-portable default"
    - "Positive control: user B CAN read user B's own rows (>0) so a harness that over-restricts fails loudly rather than false-greening at 0"
  artifacts:
    - path: "backend/tests/integration/test_163_leak_asyncpg.py"
      provides: "the D-08 asyncpg two-user leak core (RED until plan 05 applies the migrations)"
    - path: "backend/tests/integration/test_163_leak_supabase.py"
      provides: "the D-08 supabase-py two-user leak core"
    - path: "backend/tests/integration/test_163_role_swap.py"
      provides: "the role-swap-noop / claims-spoof / fail-closed detector"
  key_links:
    - from: "test_163_leak_asyncpg"
      to: "get_user_pg_connection (via _rls_harness.as_user_asyncpg)"
      via: "SET LOCAL ROLE + both GUC forms as user B against user A's rows"
      pattern: "auth.uid()"
    - from: "test_163_role_swap"
      to: "the BYPASSRLS-vs-authenticated diff"
      via: "same SELECT with and without SET LOCAL ROLE"
      pattern: "SET LOCAL ROLE authenticated"
---

<objective>
Author the three automated tests that ARE the D-08 crux proof at the mechanism level: the asyncpg two-user leak, the supabase-py two-user leak, and the role-swap-noop / spoof / fail-closed detector. These exercise the Front-B factories + the (soon-to-be-applied) membership predicates directly against seeded two-user/two-org rows — proving RLS isolates BEFORE the app-wide router swap. They are RED until plan 05 applies migrations 107+108; the operator-run LIVE runbook version (with the pass/fail scoreboard) is plan 09.

Purpose: catch a wrong GUC variant, a role-swap no-op, or a predicate leak at the earliest possible gate (right after apply), before any router-swap work. Fail fast, cheaply.

Output: `test_163_leak_asyncpg.py`, `test_163_leak_supabase.py`, `test_163_role_swap.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-RESEARCH.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_163_leak_asyncpg.py + test_163_leak_supabase.py — the two-user, two-path leak core</name>
  <files>backend/tests/integration/test_163_leak_asyncpg.py, backend/tests/integration/test_163_leak_supabase.py</files>
  <read_first>
    - backend/tests/integration/_rls_harness.py (as_user_asyncpg, as_user_supabase_txn, assert_auth_uid from plan 01)
    - backend/tests/integration/conftest.py (two_orgs_two_users + auth_uid_variant fixtures from plan 01)
    - backend/tests/integration/test_119_leak.py (the two-user non-vacuity seed + cross-user assertion pattern) + test_110_dm_schema.py:302-314 (SET-LOCAL-as-user)
    - backend/tests/integration/_reembed_adapter.py (SupabaseTxnAdapter — supabase-py surface over a real asyncpg txn so the leak test hits real RLS on real rows, not a mock)
    - .planning/phases/163-.../163-RESEARCH.md § "The Live Two-User Leak Test Design (D-08)" (the 7-step harness: seed → fail-loud preflight → positive control → isolation both paths → role-swap-noop → spoof/fail-closed → GUC-variant arbitration)
  </read_first>
  <action>
    `test_163_leak_asyncpg.py`: using two_orgs_two_users, for a representative table set (documents, folders, threads, messages, skills, user_memory): (1) fail-loud preflight — as user B, after SET LOCAL ROLE + both GUC forms, `assert_auth_uid(conn, user_B_id)` (STOP if NULL — a wrong GUC variant would false-pass isolation at 0 rows); (2) positive control — user B reads >0 of user B's own rows; (3) isolation — as user B, `SELECT count(*) WHERE id IN (<A's row ids>)` == 0, and `UPDATE`/`DELETE` of A's rows affects 0. Parameterize the GUC form over (legacy-only, JSON-only, both) and record which resolve auth.uid() on this DB.
    `test_163_leak_supabase.py`: the same isolation assertions via the per-request supabase-py client (anon key + user B's Bearer through the SupabaseTxnAdapter bridge) — user B reads 0 of A's rows on the PostgREST path. Assert the client is per-request (no mutation of the get_supabase singleton).
    Both RED until plan 05 applies the migrations. Skip-guard on Postgres :54322.
  </action>
  <acceptance_criteria>
    - Both tests assert user B reads 0 of user A's rows across ≥4 representative tables on their respective DB path
    - The asyncpg test's FIRST assertion per user is `assert_auth_uid` (fail-loud) — an isolation assert can never run with a NULL auth.uid()
    - The positive control (user B sees own rows >0) is present so an over-restricting harness fails loudly
    - The GUC form is parameterized (legacy/JSON/both) and the resolved variant is recorded
    - Both `pytest ... --collect-only` clean; RED now, GREEN after plan 05
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_leak_asyncpg.py tests/integration/test_163_leak_supabase.py --collect-only</automated>
  </verify>
  <done>The two-user leak proof exists for BOTH DB paths, fail-loud-preflighted and positive-controlled, GUC-variant parameterized; RED until apply.</done>
</task>

<task type="auto">
  <name>Task 2: test_163_role_swap.py — role-swap-noop + spoof + fail-closed detector</name>
  <files>backend/tests/integration/test_163_role_swap.py</files>
  <read_first>
    - backend/tests/integration/_rls_harness.py (as_user_asyncpg + a variant that OMITS the role swap)
    - backend/tests/integration/conftest.py (two_orgs_two_users)
    - .planning/phases/163-.../163-RESEARCH.md § "Pitfall 1" (the role-swap-noop detector) + § "The Live Two-User Leak Test Design" steps 5-6 (role-swap-noop diff + spoof/fail-closed cases)
  </read_first>
  <action>
    Author `test_163_role_swap.py` (RED until apply): (a) role-swap-noop detector — run one representative SELECT WITHOUT `SET LOCAL ROLE authenticated` (connection stays postgres) and assert it returns user A's rows (proves BYPASSRLS is the default / RLS is inert without the swap); then WITH the swap assert it returns own rows only — the diff proves the swap is load-bearing (T-163-01). (b) fail-closed — a fresh transaction with role authenticated but NO claims set (auth.uid() NULL) returns 0 rows. (c) spoof — a claims sub for a NON-member of Org X cannot reach Org X rows (membership, not just ownership, is the gate); a random/non-existent sub returns 0 rows. Skip-guard on Postgres :54322.
  </action>
  <acceptance_criteria>
    - The role-swap-noop test asserts BOTH sides of the diff: WITHOUT the swap → A's rows visible (BYPASSRLS); WITH the swap → 0 of A's rows
    - The fail-closed case (no claims → auth.uid() NULL → 0 rows) is asserted
    - The spoof case (non-member / random sub → 0 rows) is asserted
    - `pytest tests/integration/test_163_role_swap.py --collect-only` clean; RED now, GREEN after plan 05
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_role_swap.py --collect-only</automated>
  </verify>
  <done>The role-swap-noop / spoof / fail-closed detector exists and encodes the BYPASSRLS-vs-authenticated diff that proves the role swap is what turns RLS on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live RLS | The leak tests are the adversary — they attempt cross-org reads as user B |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-01 | Elevation of Privilege | role-swap no-op | mitigate | the role_swap test's WITHOUT/WITH diff is the detector; a claims-only connection is caught returning ALL rows |
| T-163-03 | Spoofing | GUC-variant false-pass | mitigate | fail-loud assert_auth_uid preflight BEFORE isolation asserts; GUC form parameterized + resolved variant recorded |
| T-163-02 | Information Disclosure | predicate leak | mitigate | two-user, both-path isolation asserts across a representative table set with non-vacuity + positive control |
| T-163-SPOOF | Spoofing | non-member sub | mitigate | spoof case asserts membership (not just ownership) is the gate; random sub → 0 rows |
| T-163-SC | Tampering | dependency installs | accept | test-only; no new package |
</threat_model>

<verification>
- All three test files collect cleanly (RED until plan 05 applies migrations 107+108).
- The asyncpg leak test's first per-user assertion is the fail-loud auth.uid() preflight.
- The role-swap test asserts both sides of the BYPASSRLS-vs-authenticated diff.
</verification>

<success_criteria>
The automated D-08 core exists: two-user isolation on both DB paths, a role-swap-noop detector, spoof/fail-closed cases, fail-loud preflight, positive control, and GUC-variant arbitration — RED until the apply gate flips them GREEN.
</success_criteria>

<output>
Create `.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-04-SUMMARY.md` when done.
</output>
