---
status: testing
phase: 099-workflow-skill-composition
source: [099-VALIDATION.md (rows L1-L10), 099-01..06-SUMMARY.md]
started: 2026-06-10T08:30:00Z
updated: 2026-06-10T08:30:00Z
---

## Current Test

number: 1
name: L1 — OpenAI: skill composes + snapshot read round-trips
expected: |
  With the thread model set to an OpenAI model, running the skill-bearing workflow
  produces phase output that reflects the skill's instructions, and the run log
  shows a read_skill_file call returning the snapshot file's contents.
awaiting: user response

## Tests

### 1. L1 — OpenAI: skill composes + snapshot read round-trips
expected: Set thread model to an OpenAI model; run the skill-bearing workflow. Phase output reflects the skill's instructions AND the run log shows a `read_skill_file` call returning the snapshot file's contents.
result: pass (after 099-07 fix)
evidence: "Re-run on OpenAI gpt-5.4-mini (screenshots 2026-06-10 121743/121806): full Risk Lens assessment — 5 Greek criteria with AMBER/JADE band scores, KB evidence quotes (5 sources via search_documents), rubric Rule R1 escalation logic (only in rubric.md → snapshot read proven), ends RL-MARKER-OMEGA. Second kickoff exercised the idempotent graft path (snapshot already persisted) cleanly."
reported: "ORIGINAL FAILURE (pre-fix): Setting up agent then directly blank response; on refresh the thread page is empty and the chat title stays 'New Chat'. Backend: 500 on POST /threads/{id}/messages — postgrest APIError code 23514 'workflow_definitions row 8a11b1b1-859c-46e9-9db3-ce87091fe749 is published and immutable; create a new version instead', raised from skill_snapshot.py:243-247 persist-back UPDATE inside materialize_skill_snapshots, called via threads.py:931 _ensure_skill_snapshots."
severity: blocker

### 2. L2 — Anthropic: same as L1
expected: Set model to an Anthropic model; repeat L1. Skill instructions compose into framing; `read_skill_file` round-trips from the snapshot.
result: pass
evidence: "Post-fix run 19c11850 (claude-sonnet-4-6) completed. Output: AMBER band first line (bands exist ONLY in rubric.md → snapshot read round-tripped), all 5 Greek criteria + UNSCORED rule (instructions composed), ends RL-MARKER-OMEGA. skill_snapshots persisted with files:['rubric.md']; Storage snapshot object exists. DB-verified 2026-06-10."

### 3. L3 — Google: same as L1
expected: Set model to a Google model; repeat L1. Skill instructions compose into framing; `read_skill_file` round-trips from the snapshot.
result: pass
evidence: "gemini-3.5-flash run (screenshots 2026-06-10 122245/122304): full Risk Lens assessment — OBSIDIAN headline correctly forced by rubric Rule R1 (Omega scored 5), Greek criteria with KB quotes from kb_doc1_project_updates.md, 13 sources, ends RL-MARKER-OMEGA. Slow + low-confidence badge are provider/retrieval-grading behavior, not 099 defects."
side_observations: "Nav-during-run display glitches (timer resets on thread nav + duplicated empty assistant avatar) filed as BUG-260610-01 — display-only, run completed correctly; same family as the open 098 run-honesty trio."

### 4. L4 — OpenRouter: same as L1
expected: Set model to an OpenRouter model; repeat L1. Skill instructions compose into framing; `read_skill_file` round-trips from the snapshot.
result: pass
evidence: "Operator-confirmed pass 2026-06-10. Same nav-during-run display glitches observed (timer reset + duplicate avatar) — BUG-260610-01 reproduces on OpenRouter too, confirming the glitch is provider-agnostic frontend reconcile behavior, not provider-specific."

### 5. L5 — Multi-tool: read_skill_file + search_documents in ONE phase
expected: A skill-bearing phase whose prompt requires both reading the skill file AND searching the project KB. Both tool calls fire and both results inform the output.
result: pass
evidence: "Same run 19c11850: output quotes {{executive_summary}}/{{metric_1_name}} placeholders from the scoped KB doc (search_documents) AND applies rubric.md band system (read_skill_file from snapshot). Both results demonstrably inform one phase output."

### 6. L6 — Parallel-thread: workflow in Thread A, Deep in Thread B
expected: Start the workflow in Thread A; while it streams, send a Deep prompt in Thread B calling `load_skill` + `read_skill_file`. Thread B behaves normally (live skill, NOT the snapshot); Thread A's whitelist does not leak into Thread B.
result: pass
evidence: "Operator-confirmed 2026-06-10: Thread B (Deep, load_skill + read_skill_file on pptx skill) worked normally with correct tool panel while Thread A streamed the kimi-k2.6 workflow run. No whitelist bleed, no snapshot routing in Deep."
side_observations: "Thread A on kimi-k2.6 (Moonshot): very slow (~8.5 min) but COMPLETED clean — monitored via Redis stream (11 events frozen ~3.5 min, then one massive final generation; output 49.8 KB with full rubric compliance: all 5 criteria, bands, RL-MARKER-OMEGA). Counts as a BONUS 5th-provider pass (Moonshot, beyond representative-4). Worse duplicate-avatar variant on Thread A — two bare avatars with NO 'Setting up agent...' text (screenshot 123038). Timer reset reproduced on both threads. All appended to BUG-260610-01."

### 7. L7 — Long-message: ≥5 KB skill instructions compose without truncation
expected: A `_099uat` skill with instructions ≥ 5 KB; run the workflow; the phase completes and the output reflects late-in-the-instructions content (proves no truncation).
result: pass
evidence: "risk-lens_099uat instructions are 6,981 chars (≥5 KB); the RL-MARKER-OMEGA terminator is the LAST line of the instructions and appeared in both the Anthropic (run 19c11850) and OpenAI (gpt-5.4-mini) outputs — late-in-instructions content reflected, no truncation, both phases completed."

### 8. L8 — Deep red-line live: load_skill + read_skill_file in Deep unchanged
expected: In a Deep thread (no workflow), call `load_skill` + `read_skill_file` on a real skill across the representative-4 providers. Behavior matches pre-099: file bytes returned correctly, normal SSE stream, no snapshot routing, no errors.
result: pass
evidence: "EXCEEDED representative-4: DB-verified 7 providers (openai/gpt-5.4-mini, anthropic/claude-haiku-4-5, google/gemini-3.5-flash, deepseek/deepseek-v4-flash, zhipu/glm-5.1, moonshot/kimi-k2.6, openrouter/llama-3.3-70b) all completed with exactly 1 load_skill + 1 read_skill_file tool message each, normal Deep behavior, live skill content. Automated layer: test_deep_noop + 096/085 suites green."
side_observations: "Slow providers (OpenRouter, kimi) show a blank assistant avatar until the first tool call — empty-bubble family, appended to BUG-260610-01. Reasoning providers (kimi/deepseek/glm) got fallback message-derived titles — KNOWN designed behavior (title-gen fallback from 2026-05-30 fix), not a new bug."

### 9. L9 — Immutability live: edit/delete live skill after snapshot, re-run unchanged
expected: After L1 materialized the snapshot, edit the skill's instructions (and delete one attached file) via the Skills UI; re-run the SAME published workflow. The phase still uses the ORIGINAL snapshotted instructions and the deleted file is still readable from the snapshot copy.
result: pass
evidence: "Operator 2026-06-10 on deepseek v4 flash (BONUS 6th provider): live skill instructions replaced with pirate-speak + rubric.md deleted from the live skill → re-run completely ignored the edits. Output = ORIGINAL Risk Lens assessment (band headline, Greek criteria, RL-MARKER-OMEGA); rubric content still round-tripped from the snapshot Storage copy despite live-file deletion. No pirate speak. D-01 immutability proven live."
side_observations: "Same timer reset + duplicate avatar on DeepSeek — appended to BUG-260610-01 (now confirmed on 4 providers)."

### 10. L10 — Publish gate live: disabled skill refused with 400 at kickoff
expected: Disable the referenced skill, then kick off the workflow. Kickoff is refused with a 400 and a descriptive message (gate fired) — NOT a silent run on a resurrected skill.
result: issue
reported: "Disabled the skill, ran the workflow in 3 threads/providers (glm, minimax, gpt-5.4-mini). UI shows nothing (only the assistant avatar) which disappears on navigation; all 3 threads left completely blank with 'New Chat' title — no error, nothing. Screenshot 2026-06-10 124716."
severity: major
backend_half: "PASS — DB-verified: zero workflow_runs created for the 3 attempt threads (all 0 messages, fail-closed before insert); the gate refused all 3 kickoffs. No silent run on a disabled skill."
frontend_half: "FAIL — the descriptive 400 detail never surfaces in the UI: no toast/error bubble, prompt lost, dead empty threads left in the sidebar."

## Summary

total: 10
passed: 9
issues: 1
pending: 0
skipped: 0
blocked: 0

> Test 1's original blocker (23514 trigger collision) was diagnosed, fixed by gap plan 099-07, and the re-run passed — counted under passed.

## Gaps

```yaml
- truth: "A skill_ref kickoff refusal (400) surfaces a descriptive error to the user in the chat UI"
  status: failed
  reason: "User reported: disabled skill + 3 kickoff attempts (glm/minimax/gpt-5.4-mini) → UI shows nothing, avatar disappears on nav, 3 dead blank 'New Chat' threads, no error anywhere. Backend half verified correct (DB: zero workflow_runs created, descriptive 400 returned). The frontend send path swallows non-2xx responses from POST /threads/{id}/messages in harness kickoff: no toast/error bubble, prompt lost, dead thread left behind. Same family as the silent-send-drop class (BUG-260603-01 / SEED-055)."
  severity: major
  test: 10
  artifacts:
    - path: "frontend (send path: useMessages / ChatArea / streams store)"
      issue: "POST /threads/{id}/messages non-2xx during workflow kickoff produces no user-visible feedback; optimistic UI cleans up to blank"
  missing:
    - "Surface kickoff HTTP errors in the chat UI: error bubble or toast with the response detail; keep the typed prompt recoverable (composer retains text or error bubble offers retry)"
    - "Avoid leaving dead empty threads on kickoff refusal (or render the error in-thread so the thread isn't mysteriously blank)"

- truth: "Running a published skill-bearing workflow materializes the snapshot at first kickoff and the phase output reflects the skill's instructions with read_skill_file round-tripping from the snapshot"
  status: failed
  reason: "User reported: blank response, empty thread after refresh, title stuck on 'New Chat'. Backend 500 on POST /messages — DB trigger rejection: 'workflow_definitions row is published and immutable; create a new version instead' (code 23514) raised by the Phase 098 published-row immutability lock when materialize_skill_snapshots attempts its persist-back UPDATE of the definition JSONB (skill_snapshot.py:243-247). The D-03a first-kickoff persist-back collides with the 098 schema lock; mock-based tests never enforced the trigger. Kickoff dies BEFORE the user-message insert, so the thread is left empty (500 instead of a graceful error)."
  severity: blocker
  test: 1
  resolution: "FIXED by gap plan 099-07 (commits 17b3e47d/9dd578c9/a6a2a656): migration 067 sibling column + amended trigger; persist CAS; read-point grafts; structured 500. VERIFIED LIVE 2026-06-10: run 19c11850 (skill_compose_099uat, Anthropic claude-sonnet-4-6) completed; snapshot persisted to skill_snapshots column; rubric content round-tripped from the snapshot copy."
  artifacts:
    - path: "supabase/migrations/056_workflow_definitions.sql"
      issue: "Lines 81-99: workflow_definitions_block_published_update() raises 23514 on ALL updates when OLD.status='published' — no column exemption. Shipped in Phase 091 (HARNESS-02/D-04)."
    - path: "backend/app/services/harness/skill_snapshot.py"
      issue: "Lines 242-248: persist-back does .update({'definition': ...}) on the published row — structurally guaranteed to hit the trigger on every real published definition. Offline fakes don't model the trigger (third mock-masking instance this phase, DB-constraint layer)."
    - path: "backend/app/api/threads.py"
      issue: "Line 931 region: _ensure_skill_snapshots maps ValueError→400 but lets postgrest.APIError escape as a naked 500; kickoff dies before the user-message insert → blank response, empty thread, title stuck on 'New Chat'."
  missing:
    - "Migration 067: ADD COLUMN workflow_definitions.skill_snapshots jsonb (nullable) + amend the 056 trigger to allow published-row updates ONLY when all authored columns are unchanged (only skill_snapshots may change) — operator-approved sibling-column design"
    - "skill_snapshot.py: persist snapshots map {phase_slug: snapshot} to skill_snapshots column with .is_('skill_snapshots','null') CAS guard (also closes the IN-03 double-kickoff race); graft helper to re-attach snapshots onto parsed definitions"
    - "Read-point grafts: kickoff definition load (threads.py:884 region) + _load_run_definition (harness_engine.py:1048) must SELECT skill_snapshots and graft onto phase.config.skill_snapshot"
    - "threads.py: map unexpected materializer errors to a structured HTTPException instead of a naked 500"
    - "De-mock: fake workflow_definitions table must raise APIError 23514 on published-row updates that touch authored columns, so this collision class can never pass tests again"
```
