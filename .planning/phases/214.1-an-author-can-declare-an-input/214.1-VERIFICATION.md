---
phase: 214.1-an-author-can-declare-an-input
verified: 2026-08-28T20:35:00Z
status: human_needed
score: 5/5 truths code-verified; 1 carries an unproven bug-closure claim and 1 UI has no operator veto exercised
overrides_applied: 0
overrides: []
human_verification:
  - test: "Operator visually reviews DeclaredInputsEditor.tsx (the declared-input door and its dialog) on a live build — G-2 was OVERRIDDEN, not satisfied, per D-214.1-06. No sketch was ever approved."
    expected: "The affordance/dialog either passes the operator's visual bar, or is flagged for a re-skin. Per the recorded mitigation, a rejected visual only touches this component's JSX — the store, save path, describe-door emission and reachability test all survive untouched."
    why_human: "G-2 is a visual-design gate; no automated check can approve or reject a UI's look. The override in STATE.md → Guardrail overrides names this exact re-open trigger: 'the operator's first look at the shipped surface.'"
  - test: "Drive the full loop live end-to-end: build a workflow with an external_action send_email step, set its 'to' argument to Asked when this runs, open the declared-input editor, declare the matching key, publish, then LAUNCH a run from (a) the library, (b) Test Run, and (c) a schedule — and confirm workflow_runs.inputs carries the declared key on all three doors and the step actually receives the value."
    expected: "All three launch doors ask for the declared input and the run's inputs (verified in Postgres) carry the declared key, not just kickoff_prompt — closing BUG-260826-01 for real."
    why_human: "This is exactly the G4-3 drive BUG-260826-01's own closure record requires ('verified_closed_by: null... flip to closed only when G4-3 reads a driven verdict for library, thread AND schedule'). 214.1 built the missing DECLARE half and proved it reaches the wire in an automated test, but no test in this phase (or any prior one) exercises declare→publish→launch→run as one live sequence against a real backend. The launch-form/wire mechanism itself is unmodified by 214.1 (confirmed: no changes to threads.py, args.py, or phase_types.py's _adapter_args in this phase's diff) — it was built and partially driven in Phase 214, where the same gap was already recorded and left open."
  - test: "Update the reported-bugs register: BUG-260828-02 and BUG-260828-03 both still read status: open / folded_into: null despite being this phase's namesake fixes; BUG-260826-01 still reads folded_into: 214 (not 214.1)."
    expected: "BUG-260828-02 and BUG-260828-03 flip to folded / folded_into: 214.1 (or closed, if the operator's live drive above confirms it). BUG-260826-01's folded_into stays accurate to whichever phase most recently touched its blast radius."
    why_human: "CLAUDE.md's MANDATORY reported-bugs cross-check states a bug's status: frontmatter IS the index — prose elsewhere is invisible to the sweep. Neither an automated check nor this verifier can decide the bug is truly closed (that needs the live drive above); a human/orchestrator must reconcile the register regardless of how the drive turns out."
---

## ⚠ CORRECTION 2026-08-28, AFTER THE OPERATOR DROVE IT — SC#2 IS NOT MET

This report was written before the operator drove a real publish. **They then drove one, and it
blocked.** The correction is recorded here rather than by editing the findings above, because what
the report VERIFIED is still true — what it could not see is what a live drive found.

**What the drive proved worked:** the declaration persists (`definition.inputs` carries
`{key: "to", label: "to", type: "text", required: true}`), the golden run completed all three
phases, `citations_required` passed, and the external step correctly recorded rather than sent.

**What blocked:** the STRUCTURAL GATE, on the very argument the author was told to ask for —
`no_source` / phase `act` / argument `to` / *“resolved to nothing when the workflow actually ran”*.

**Cause:** `publish_service.py:1320,1368` start the golden run with `inputs={"kickoff_prompt": …}`
and nothing else. Declared inputs are never fabricated, so an argument sourced *Asked when this
runs* can NEVER resolve during validation. Filed as **`BUG-260828-10`** (blocking).

⚠ **`BUG-260826-01` therefore does NOT close** and its `verified_closed_by` stays `null`: a launch
cannot be driven because the workflow cannot be published. Phase 214 SC#2 remains **UNMET**.

⚠ **Phase 214.1 still delivered its own goal** — the authoring half. It did not, and could not,
close the launch half; the gate it exposed had never been reached by any author before, because
before 214.1 nobody could get past the earlier refusal.


# Phase 214.1: An Author Can Declare an Input — Verification Report

**Phase Goal:** An author can declare a workflow input and source a step's argument from it — so
that *Asked when this runs* stops being a dead end, and a `send_email` step actually receives the
recipient a person typed at launch.

**Verified:** 2026-08-28
**Status:** human_needed
**Re-verification:** No — initial verification

## Method

This was NOT a SUMMARY-trusting pass. Every artifact named below was read directly from the
repository at HEAD, and every test suite claimed by the three SUMMARYs was independently
re-executed in this session (not inherited from the SUMMARY's own numbers):

| Suite | Independently re-run | Result |
|---|---|---|
| `frontend/src/pages/WorkflowBuilderPage.declaredInputs.test.tsx` (the reachability property) | ✅ | **9/9 passed** |
| `backend/tests/unit/test_2141_describe_declares_inputs.py` | ✅ | **13/13 passed** |
| `backend/tests/unit/test_2141_no_false_capability_refusal.py` | ✅ | **13/13 passed** |
| `PublishGauntlet.test.tsx` + `PublishRefusalList.test.tsx` + `publishRefusalVocabulary.test.ts` | ✅ | **149/149 passed** |
| `declaredInputs.test.ts` + `DeclaredInputsEditor.test.tsx` + `builderStore.test.ts` + `soulData.test.ts` + `LaunchInputFields.test.tsx` | ✅ | **196/196 passed** |
| `backend/tests/unit` (full) | ✅ | **68 failed / 3081 passed** — exact baseline match, no regression |
| `tsc --noEmit -p tsconfig.app.json` | ✅ | **34 errors** — exact baseline match |
| `node scripts/vitest-count-gate.cjs` (repo root, cap 2) | ✅ (×2) | Run 1: `failed 1` (`WorkflowRunPage.test.tsx`, provably unmodified by this phase — `git diff --numstat` empty — matching SEED-171's known flake). Run 2: **`count gate OK — 135/135 pinned files present, no per-file decrease, 0 failing`**, matching the SUMMARY's quoted verdict line exactly (`5707 → 6435`) |
| `node scripts/check-claude-md-size.cjs` | ✅ | exit 0, 97,749 chars / 65.2% |

Source was also read directly (not grepped from the SUMMARY's claims) for: `builderStore.ts`
(`setDeclaredInputs`), `workflow_authoring.py` (`_declare_asked_arguments`, its call site),
`phase_types.py` (`_wired_services_block`, all 4 composition sites), `PublishGauntlet.tsx` /
`PublishRefusalList.tsx` (the `onGoToStep` finding), `CLAUDE.md` / `docs/HOT-FILE-LEDGER.md` (the
new/corrected ledger rows), and the full text of
`frontend/src/pages/WorkflowBuilderPage.declaredInputs.test.tsx` (all 613 lines).

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | An author declares an input, sources a step argument from it, and publishes — the gate that refused before now passes, no hand-editing JSON | ✓ VERIFIED (composed evidence; live drive still owed — see human items) | `DeclaredInputsEditor.tsx` + `setDeclaredInputs` (`builderStore.ts:884-887`) give a real UI path with no JSON editing. `DeclaredInputsEditor.test.tsx` proves the offer→declare flow for a step whose `arg_sources.to = {source:"ask", ask_key:"recipient"}` already exists (lines 285-298), and proves the DOM-typed key reaches the parsed PATCH body. The **actual gate** (`reachability.lint_workflow` / `args.unsatisfiable_arguments`, the same predicate `publish_service.py:195` calls) is proven in `test_2141_describe_declares_inputs.py`'s gate-agreement case: 0 `ask_undeclared` with the declared entry present, 1 without it — real backend code, not mocked. No single test drives UI-declare → real-backend-publish in one live sequence; the two halves are independently proven and structurally compose (same `InputFieldSpec` shape, same gate predicate on both paths). |
| 2 | A run launched from library/Test Run asks for the input and the step receives it — `workflow_runs.inputs` carries the key (closes `BUG-260828-02`, and with it `BUG-260826-01`) | ⚠ PARTIAL / UNCERTAIN — the DECLARE half is verified; the "closes BUG-260826-01" claim is not | The reachability test proves the declared key reaches the **saved definition** (`inputs[]` in the CREATE/PATCH body — confirmed 9/9 passing, independently re-run). It does **not** launch a run afterward. The launch-form/run-wire mechanism itself is **unmodified by 214.1** (confirmed: `git diff` for this phase touches no file in `threads.py`, `args.py`'s adapter resolution, or `phase_types.py`'s `_adapter_args`) — it was built in Phase 214 and its own bug record (`BUG-260826-01`) still reads `verified_closed_by: null`, explicitly requiring a G4-3 drive across library/thread/schedule that has not happened. 214.1 does not perform or claim that drive. |
| 3 | A publish refusal is readable in full on screen and names its next action — never the raw diagnostic (closes `BUG-260828-04`) | ✓ VERIFIED | Diagnosed against the byte-exact live payload (`code=ask_undeclared, phase=act, step_name=act, argument=to, upstream=null`) rendered through the real `PublishGauntlet` — confirmed by re-reading the test and re-running it (149/149 green). The publish surface is measured innocent (arm c): the composed sentence renders in full, the server diagnostic string never appears (`git diff` for `PublishGauntlet.tsx` shows **0 clamp-pattern hits**, confirmed by direct grep). The raw diagnostic the operator saw is `ProblemsTray`'s own designed verbatim render, fed by a different route (`/workflows/validate`) — correctly re-scoped rather than "fixed" against the wrong surface. Its next action IS textually present (`{nextAction}` rendered as button text, confirmed by reading `PublishRefusalList.tsx:114-124`), though the control is `disabled` (`onGoToStep` never passed from `PublishGauntlet.tsx:810-814`, confirmed by direct read) — an honestly-reported, deliberately-out-of-scope pre-existing gap with a named re-open trigger, not a new defect. |
| 4 | The describe door emits `inputs[]` for an asked argument, so an AI-drafted workflow publishes without hand-repair | ✓ VERIFIED | `_declare_asked_arguments` read directly at `workflow_authoring.py:458-524`: `model_copy` (not in-place), skips `RESERVED_RUN_INPUT_KEYS` (imported, not re-typed), runs once on the single success path (`:809`, after the vocabulary refusal, before the provenance stamp). 13/13 backend cases independently re-run and green, including the gate-agreement case against real `lint_workflow` with a positive control (1 `ask_undeclared` when stripped). |
| 5 | The agent never states a service is unconnected when a connection exists — a lookup, not a generation | ✓ VERIFIED, under the phase's own declared scope limit | `_wired_services_block` confirmed at all 4 composition sites by direct grep (`phase_types.py:809, 887, 989, 1499` — `llm_single`, `llm_agent`, `llm_batch_agents`, `llm_emit`; the 4th site was a deviation the plan's own AST assertion caught, correctly fixed rather than left at 3). 13/13 backend cases independently re-run and green. This is explicitly an **awareness** fix (a fact handed to the model), not a runtime guarantee — the SUMMARY states this outright and D-214.1-05's scope limit forbids a post-hoc censor. That is the scope the phase's own CONTEXT committed to, not an overclaim. |

**Score:** 4/5 fully VERIFIED by code + independent test execution; 1/5 (#2) VERIFIED for its own mechanism but carries an unproven "closes BUG-260826-01" claim that requires a live drive.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `frontend/src/components/workflows/declaredInputs.ts` | pure leaf: type, 3 refusals, `undeclaredAskKeys` | ✓ VERIFIED | exists, imports `RESERVED_LAUNCH_INPUT_KEYS` from `soulData.ts` rather than re-typing (confirmed) |
| `frontend/src/components/workflows/DeclaredInputsEditor.tsx` | definition-level authoring surface | ✓ VERIFIED, WIRED | mounted in `WorkflowBuilderPage.tsx` behind the `canvasEnabled` gate; drives real store writes (confirmed via passing `DeclaredInputsEditor.test.tsx`) |
| `frontend/src/components/workflows/builderStore.ts` — `setDeclaredInputs` | 6th `meta` writer | ✓ VERIFIED | read directly at `:884-887`; bails outside `drafted`, writes `meta.inputs` + `dirty` in one `set()`, matches all five sibling writers' shape |
| `frontend/src/pages/WorkflowBuilderPage.declaredInputs.test.tsx` | the reachability property | ✓ VERIFIED | 613 lines read in full; genuinely mocks only `@/lib/supabase` (auth) + `fetch` (network); real store/hook/API client throughout; 3 self-fences + non-vacuity control all present and independently confirmed passing |
| `backend/app/services/workflow_authoring.py` — `_declare_asked_arguments` | server-side derivation | ✓ VERIFIED | read directly, matches every claim (model_copy, reserved-key skip, single success path, `ask_key or property` rule) |
| `backend/app/services/harness/phase_types.py` — `_wired_services_block` | connected-service awareness | ✓ VERIFIED | read directly, 4 composition sites confirmed by grep |
| `frontend/src/lib/api/workflows.ts` | save path sends `inputs` | ✓ VERIFIED (docblock only, no behaviour change) | measured that `updateWorkflowDraft`'s whole-object body already carried `inputs` before this phase; no field whitelist invented (correctly declined per the plan's own guidance) |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `DeclaredInputsEditor.tsx` | `builderStore.ts` | `setDeclaredInputs` | ✓ WIRED | confirmed by passing DOM test asserting the store call |
| `builderStore.ts` | `useDraftPersistence.ts` | `selectDefinition` spreading `meta` into PATCH body | ✓ WIRED | confirmed by the reachability test's parsed-body assertions (both CREATE and PATCH) |
| `workflow_authoring.py` derivation | `reachability.py` gate | same `ask_key or name` rule; gate-agreement case | ✓ WIRED | confirmed: real `lint_workflow` run in the backend test, 0 vs 1 `ask_undeclared` |
| declared-input's stored shape | `LaunchInputFields.tsx` | `required` mark, `label===key` absence rule | ✓ WIRED | confirmed by passing `soulData.test.ts` / `LaunchInputFields.test.tsx`, and the 6 `RunModal` captures remained byte-identical (an a11y regression was caught and fixed in-flight per the SUMMARY, consistent with real TDD rather than post-hoc narrative) |
| declared input → **published workflow → a launched run** | `workflow_runs.inputs` | (none — unbuilt by this phase) | ✗ NOT WIRED BY THIS PHASE (pre-existing, Phase 214's territory) | no file in this phase's diff touches the launch/run path; the link exists only insofar as Phase 214 already built it, which itself remains only partially driven per `BUG-260826-01` |

### Data-Flow Trace (Level 4)

The declared-input value is genuinely dynamic and traced end to end on the AUTHORING side: DOM
input → `refuseDeclaredInputKey`/`declaredInputFor` → `setDeclaredInputs` → `meta.inputs` (real
store state, not hardcoded) → `selectDefinition` → real `updateWorkflowDraft` → parsed request body
(asserted non-empty, keyed by the DOM-typed value, not a fixture constant). This is FLOWING, not
STATIC — independently confirmed by re-running the test and reading its assertions rather than
trusting the SUMMARY's narrative.

The RUN side (does a declared input's value actually reach a step's adapter at execution) was
**not re-traced in this verification** because 214.1 touches none of that code; it was Phase 214's
territory and that phase's own bug record already flags it as un-driven.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Declared-input reachability property | `vitest run WorkflowBuilderPage.declaredInputs.test.tsx` | 9/9 passed | ✓ PASS |
| Describe-door derivation + gate agreement | `pytest test_2141_describe_declares_inputs.py` | 13/13 passed | ✓ PASS |
| No false capability refusal | `pytest test_2141_no_false_capability_refusal.py` | 13/13 passed | ✓ PASS |
| Publish refusal rendering (arm-c diagnosis) | `vitest run PublishGauntlet.test.tsx PublishRefusalList.test.tsx publishRefusalVocabulary.test.ts` | 149/149 passed | ✓ PASS |
| Full backend regression | `pytest tests/unit -q` | 68 failed / 3081 passed (exact baseline) | ✓ PASS (no regression) |
| Frontend type-check | `tsc --noEmit -p tsconfig.app.json` | 34 errors (exact baseline) | ✓ PASS (no regression) |
| Count gate | `node scripts/vitest-count-gate.cjs` ×2 | run 1: 1 flaky pre-existing fail (unmodified file); run 2: clean, matches SUMMARY's quoted verdict exactly | ✓ PASS |
| Live UI declare→publish→launch→run against a running backend | — | not run | ? SKIP — routed to human verification |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist for this phase or referenced by its PLAN/SUMMARY files.
Skipped — not a migration/tooling phase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| STEP-02 | 214.1-01, 214.1-02 | Required arguments author-fillable from every launch path, a run supplies them | ✓ SATISFIED (declare mechanism); ⚠ NEEDS HUMAN (full loop closure, `BUG-260826-01`) | See Truth #1/#2 above |
| STEP-03 | 214.1-03 | Publish refuses an unsatisfiable step, and the refusal is readable/actionable | ✓ SATISFIED | See Truth #3 above |

⚠ **`.planning/REQUIREMENTS.md` itself is stale for both rows** — both still show unchecked `[ ]`
and are tabulated against "Phase 214" rather than 214.1 (lines 94-100, 213-214). This is a
tracking-table hygiene gap parallel to the reported-bugs staleness below; it does not affect the
code-level verification above but should be corrected alongside it.

### Anti-Patterns Found

None. Swept every non-test source file this phase touched for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER`
and for hardcoded-empty-data patterns — zero hits. No debt markers, no stub returns, no
console.log-only implementations.

### Reported-Bugs Register — NOT reconciled by this phase

`CONTEXT.md`'s frontmatter declares `closes_bugs: [BUG-260828-02, BUG-260826-01, BUG-260828-03,
BUG-260828-04]`, but reading the actual bug files at HEAD:

| Bug | Frontmatter at HEAD | Expected after this phase |
|---|---|---|
| `BUG-260828-02` | `status: open`, `folded_into: null` | should read `folded` / `folded_into: 214.1` at minimum |
| `BUG-260828-03` | `status: open`, `folded_into: null` | should read `folded` / `folded_into: 214.1` at minimum |
| `BUG-260828-04` | `status: folded`, `folded_into: 214.1` | ✓ correctly updated |
| `BUG-260826-01` | `status: folded`, `folded_into: 214` (not 214.1), `verified_closed_by: null` | correctly NOT closed (the drive it needs did not happen here), but its own bug record's "un-driven door" language is now inherited by 214.1 rather than 214, and nothing routes that forward |

Per CLAUDE.md's MANDATORY reported-bugs cross-check: *"a bug's `status:` frontmatter IS the
index — prose inside a folded record is invisible to the scan."* Two of the four bugs this phase
exists to answer are invisible to that scan as it stands. This is a process-hygiene gap, not a
code defect — flagged as a human-verification item above rather than a BLOCKER, because the
underlying fixes are real and independently verified.

### Human Verification Required

### 1. Operator visual review of `DeclaredInputsEditor.tsx`

**Test:** Open the Workflow Builder, view the declared-input authoring surface (affordance +
dialog) as shipped.
**Expected:** Either it passes the operator's visual bar, or specific rejected elements are named.
**Why human:** G-2 is OVERRIDDEN, not satisfied (D-214.1-06). No sketch was ever approved; the
operator was absent for the entire build and explicitly retained the visual veto. STATE.md's own
Guardrail-overrides entry names *"the operator's first look at the shipped surface"* as the
re-open trigger.

### 2. Live end-to-end drive: declare → publish → launch → run

**Test:** Build a workflow with an `external_action` `send_email` step, set `to` to *Asked when
this runs*, declare the matching input via the new editor, publish, then launch a run from the
library, from Test Run, and from a schedule.
**Expected:** All three doors ask for the declared input; `workflow_runs.inputs` (read from
Postgres) carries the declared key on each; the email step actually receives and uses the value.
**Why human:** This is precisely the G4-3 drive `BUG-260826-01`'s own closure record requires
and has never received. 214.1 proves the DECLARE half reaches the wire (automated, independently
re-confirmed); it does not and cannot prove the launch/run half without a live backend and a real
send — no file on that path was touched by this phase.

### 3. Reconcile the reported-bugs register

**Test:** Update `BUG-260828-02` and `BUG-260828-03` frontmatter to reflect this phase's fixes
(at minimum `folded` / `folded_into: 214.1`; `closed` if item 2's drive confirms the fix holds
live).
**Expected:** The register accurately reflects what shipped, so the mandatory sweep at the next
`/gsd:new-milestone` or `/gsd:discuss-phase` doesn't re-surface already-addressed bugs as open.
**Why human:** Frontmatter is the index; no code check can decide when a bug is "closed" versus
merely "fixed in code but unconfirmed live" — that's exactly what item 2 exists to resolve first.

## Gaps Summary

There is no BLOCKER-class gap: every artifact this phase's plans committed to exists, is
substantive, is wired, and is independently re-verified by test execution (not SUMMARY narrative)
in this session — count-gate, tsc, and the full backend suite all match their stated baselines
exactly, with zero regression. The `_declare_asked_arguments` and `_wired_services_block`
mechanisms were read directly from source and match every claim about their shape and call sites.
The reachability test — the phase's own stated headline artifact — was read in full and
independently re-run, and it genuinely does what it claims: an author-driven DOM walk from an
empty builder to a published workflow with a non-empty `inputs[]`, mocking neither the store nor
any `@/lib/api` module.

What remains open is not a code defect but an unclosed loop: (1) no operator has seen the new UI
(G-2 override, honestly recorded, not silently bypassed), and (2) no one has driven the full
declare→publish→launch→run sequence against a real backend, which is the exact gap
`BUG-260826-01` was filed to close and which this phase's own plans correctly declined to fake
(the CONTEXT explicitly forbade a test that seeds the store or fixtures the loop). Both are
scoped as human-verification items rather than as returned gaps, because the code that would make
that drive succeed is real, tested, and independently confirmed here — the missing piece is the
drive itself, not the mechanism.

---

_Verified: 2026-08-28_
_Verifier: Claude (gsd-verifier)_
