---
phase: 17-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/threads.py
  - .planning/REQUIREMENTS.md
  - .planning/phases/15-code-output-ui/15-VERIFICATION.md
autonomous: true
requirements: [SKIL-08, SAND-01]

must_haves:
  truths:
    - "System prompt advertises thirteen tools (matching actual tool count when fully configured)"
    - "SKIL-08 and SAND-01 are marked complete in REQUIREMENTS.md"
    - "Phase 15 has a VERIFICATION.md confirming SAND-12 is satisfied"
  artifacts:
    - path: "backend/app/api/threads.py"
      provides: "Corrected system prompt tool count"
      contains: "thirteen tools"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes"
      contains: "[x] **SKIL-08**"
    - path: ".planning/phases/15-code-output-ui/15-VERIFICATION.md"
      provides: "Phase 15 verification artifact"
      contains: "SAND-12"
  key_links: []
---

<objective>
Close three procedural and cosmetic gaps flagged in the v2.0 audit: fix the stale tool count in the system prompt, mark two already-implemented requirements as complete, and create the missing Phase 15 verification artifact.

Purpose: Ensure project artifacts accurately reflect the implemented state before closing v2.0 milestone.
Output: One corrected source file, one updated requirements doc, one new verification document.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-code-output-ui/15-01-SUMMARY.md
@.planning/phases/15-code-output-ui/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix system prompt tool count from twelve to thirteen</name>
  <files>backend/app/api/threads.py</files>
  <read_first>backend/app/api/threads.py (lines 30-31 — the SYSTEM_PROMPT string)</read_first>
  <action>
On line 31 of backend/app/api/threads.py, replace the string "You have twelve tools" with "You have thirteen tools".

The exact change: find the substring `"You have twelve tools"` in the SYSTEM_PROMPT constant and change `twelve` to `thirteen`.

Context: Phase 14 added execute_code as the 13th tool (11 base tools + web_search + execute_code = 13 when fully configured). The system prompt count was not updated at that time.

Do NOT change anything else in the file. Single word replacement only.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -c "thirteen tools" backend/app/api/threads.py</automated>
  </verify>
  <acceptance_criteria>
- `grep "thirteen tools" backend/app/api/threads.py` returns exactly 1 match
- `grep "twelve tools" backend/app/api/threads.py` returns 0 matches
- No other lines in the file are modified
  </acceptance_criteria>
  <done>SYSTEM_PROMPT on line 31 says "thirteen tools" instead of "twelve tools"</done>
</task>

<task type="auto">
  <name>Task 2: Mark SKIL-08 and SAND-01 as complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md (lines 19 and 40 — the SKIL-08 and SAND-01 checkbox lines)</read_first>
  <action>
In .planning/REQUIREMENTS.md, make two checkbox replacements:

1. Line 19: Change `- [ ] **SKIL-08**:` to `- [x] **SKIL-08**:`
   (skill-creator seed skill was implemented in Phase 12 via migration 018_seed_skill_creator.sql)

2. Line 40: Change `- [ ] **SAND-01**:` to `- [x] **SAND-01**:`
   (execute_code tool conditional registration was implemented in Phase 14)

Also update the "Pending" line near bottom (line 162):
- Change `Pending (gap closure phases 16-17): FILE-01, FILE-02, SKIL-08, SAND-01` to `Pending: none`

And update the traceability table entries:
- SKIL-08 row (line 137): Change `Pending` to `Complete`
- SAND-01 row (line 144): Change `Pending` to `Complete`

Do NOT change anything else.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -c "\- \[ \]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
- `grep "\- \[ \]" .planning/REQUIREMENTS.md` returns 0 matches (no unchecked boxes remain in v2.0 section)
- `grep "\[x\] \*\*SKIL-08\*\*" .planning/REQUIREMENTS.md` returns 1 match
- `grep "\[x\] \*\*SAND-01\*\*" .planning/REQUIREMENTS.md` returns 1 match
- Traceability table shows SKIL-08 and SAND-01 as Complete
  </acceptance_criteria>
  <done>Both SKIL-08 and SAND-01 checkboxes are [x] and traceability table shows Complete</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 15 VERIFICATION.md confirming SAND-12</name>
  <files>.planning/phases/15-code-output-ui/15-VERIFICATION.md</files>
  <read_first>
- .planning/phases/15-code-output-ui/15-01-SUMMARY.md (what Plan 01 built)
- .planning/phases/15-code-output-ui/15-02-SUMMARY.md (what Plan 02 built)
- frontend/src/components/chat/ExecuteCodeBlock.tsx (component implementing SAND-12)
- frontend/src/components/chat/ToolCallPanel.tsx (dispatch wiring)
- frontend/src/hooks/useMessages.ts (SSE callback wiring)
- frontend/src/lib/api.ts (SSE parsing for code execution events)
  </read_first>
  <action>
Create `.planning/phases/15-code-output-ui/15-VERIFICATION.md` that verifies SAND-12 requirement:

> SAND-12: Frontend displays a Code Output panel that renders stdout/stderr stream and shows download links for output files

Structure the verification as a standard VERIFICATION.md with:

1. **Frontmatter**: phase, status (passed), requirement (SAND-12), verified date
2. **Requirement statement**: Quote SAND-12 text
3. **Evidence by sub-criterion** — for each SAND-12 success criterion from the ROADMAP (Phase 15 Success Criteria 1-5), cite the specific file, component, or code pattern that satisfies it:
   - SC1 (Code Output panel renders): ExecuteCodeBlock.tsx exists and is dispatched from ToolCallPanel.tsx for execute_code tool calls
   - SC2 (Python badge, status indicator, execution time): ExecuteCodeBlock.tsx renders Python badge, Loader2/CheckCircle2/XCircle status icons, and formatted execution duration
   - SC3 (Streaming stdout/stderr): api.ts parses code_stdout/code_stderr SSE events; useMessages.ts accumulates them as interleaved outputLines; TerminalOutput renders green stdout and red stderr
   - SC4 (Download cards): OutputFileCard sub-component renders filename, formatted size, and download link for each output file
   - SC5 (Error state): ExecuteCodeBlock renders red XCircle icon and errorMessage text when exitCode !== 0
4. **Verdict**: SAND-12 PASSED — all 5 success criteria satisfied via code inspection

Base all evidence on actual file contents read during this task. Do not fabricate line numbers or code that does not exist.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && test -f .planning/phases/15-code-output-ui/15-VERIFICATION.md && grep -c "SAND-12" .planning/phases/15-code-output-ui/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
- File .planning/phases/15-code-output-ui/15-VERIFICATION.md exists
- File contains "SAND-12" at least 3 times (header, requirement statement, verdict)
- File contains "PASSED" or "passed" verdict
- File references all 4 key source files: ExecuteCodeBlock.tsx, ToolCallPanel.tsx, useMessages.ts, api.ts
  </acceptance_criteria>
  <done>Phase 15 VERIFICATION.md exists confirming SAND-12 is fully satisfied with evidence from code inspection</done>
</task>

</tasks>

<verification>
All three success criteria from the phase definition are met:
1. `grep "thirteen tools" backend/app/api/threads.py` returns a match
2. `grep "\- \[ \]" .planning/REQUIREMENTS.md` returns 0 (all v2.0 requirements checked)
3. `test -f .planning/phases/15-code-output-ui/15-VERIFICATION.md` succeeds
</verification>

<success_criteria>
- System prompt says "thirteen tools" (not "twelve")
- REQUIREMENTS.md has zero unchecked v2.0 requirement boxes
- Phase 15 VERIFICATION.md exists and confirms SAND-12 passed
</success_criteria>

<output>
After completion, create `.planning/phases/17-tech-debt-cleanup/17-01-SUMMARY.md`
</output>
