---
phase: 155-accessibility-sweep-wcag-aa
plan: 06
type: execute
wave: 3
depends_on: ["155-03"]
files_modified:
  - frontend/src/pages/__tests__/RunModal.a11y.test.tsx
  - frontend/src/components/chat/__tests__/CitationUI.a11y.test.tsx
  - frontend/src/pages/__tests__/SettingsPage.a11y.test.tsx
  - frontend/src/components/chat/__tests__/MessageInput.a11y.test.tsx
  - frontend/src/components/ingestion/__tests__/DocumentStatusBadge.a11y.test.tsx
autonomous: true
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "D-01: the net-new 152 Run modal + run-input controls + workflow-delete confirm, the 153 citation UI (CitedMarkdown/CitationPeek/CitationList/CitationCard/AbsenceHint — verify not rebuild), and the 154 surfaces (Settings toggle row, relabeled DocumentStatusBadge, composer mode helpers) each have a `*.a11y.test.tsx` vitest-axe suite asserting zero STRUCTURAL violations"
    - "D-12: this covers the remaining net-new v3.3 surface inventory (152 + 153 + 154); the Phase-095 chat tool-cards are explicitly NOT included (SEED-092 remainder)"
    - "D-08: the four G-4 keyboard scenarios are Claude-driven via Chrome MCP then operator-confirmed — scenarios 1 (workflow launch, this Run modal), 2 (cited answer, this citation UI), and 4 (Settings + nav toggle, this Settings/composer) are wired as VALIDATION.md rows the operator personally re-runs"
    - "D-09: scenario 1's Run-modal file input (the classic keyboard-trap spot) exposes the accessible hidden-input + proxy-button pattern; scenario 2's citation peek is role=dialog aria-modal=false with Esc-to-close and no focus trap — the vitest suites assert these roles/names so the live keyboard drive can succeed"
    - "D-10: the three invariants (visible focus indicator, no keyboard trap, logical focus order) are the pass bar for every scenario — asserted structurally here (roles/names/Esc wiring) and confirmed live by the operator in VALIDATION.md"
    - "D-13: the citation suite is a VERIFY of the already-shipped 153 a11y contracts (role=dialog aria-modal=false, state-toggled aria-pressed labels, reduced-motion, no dangerouslySetInnerHTML) inside MessageItem — G-5 additive-only, no render/stream-logic edits"
  artifacts:
    - path: "frontend/src/pages/__tests__/RunModal.a11y.test.tsx"
      provides: "Run modal + file input + delete-confirm a11y contract"
      contains: "toHaveNoViolations"
    - path: "frontend/src/components/chat/__tests__/CitationUI.a11y.test.tsx"
      provides: "citation cluster a11y contract (verify not rebuild)"
      contains: "aria-modal"
  key_links:
    - from: "*.a11y.test.tsx"
      to: "vitest-axe axe()"
      via: "render then settle then expect(await axe(container)).toHaveNoViolations()"
      pattern: "toHaveNoViolations"
---

<objective>
Lock the D-01 regression gate on the remaining net-new v3.3 surfaces: the 152 Run modal (with the file-input keyboard-trap spot + workflow-delete confirm), the 153 citation UI cluster (verify the already-shipped a11y contracts hold — do NOT rebuild), and the 154 surfaces (Settings "Show technical names" toggle row, relabeled DocumentStatusBadge, composer mode helpers in MessageInput). Each gets a `*.a11y.test.tsx` vitest-axe suite; the suites also assert the ROLES/NAMES that the D-09 keyboard scenarios depend on so the live operator drive can succeed.

Purpose: These three clusters complete the D-12 net-new surface inventory. The citation cluster is a VERIFY (153 shipped its contracts: role=dialog aria-modal=false, state-toggled aria-pressed pin labels, Esc-to-close, reduced-motion, no innerHTML) — the suite proves they still hold after the Plan 03 sweep, especially since the markers live inside MessageItem (G-5 additive-only).
Output: Five a11y suites (run modal, citation cluster, settings, composer, doc status badge), all green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-CONTEXT.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md

<interfaces>
<!-- THE reference suites to copy (structure, mocks, settle-then-scan): -->
frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx  (dialog + combobox states — closest to RunModal)
frontend/src/components/metadata/DocumentDetailPanel.a11y.test.tsx        (role/aria contract + matchMedia stub)

<!-- 152 Run modal (verify, do NOT rebuild): RunModal defined in frontend/src/pages/WorkflowsPage.tsx (function RunModal). -->
Template file-input already uses the accessible hidden-input pattern: type="file" tabIndex={-1} className="hidden"
  aria-label="Upload template file" + a visible proxy <button> that calls fileInputRef.current?.click().
Remove-template button has aria-label="Remove template". launchError is role="alert".
Existing non-a11y tests: frontend/src/pages/__tests__/RunModal.test.tsx, ChatLayoutLaunch.test.tsx (reuse mocks).

<!-- 153 citation cluster (verify shipped contracts): frontend/src/components/chat/{CitedMarkdown,CitationPeek,CitationList,CitationCard,AbsenceHint}.tsx -->
Shipped a11y contracts: role="dialog" aria-modal="false" (peek); aria-pressed + state-toggled aria-label (`Pin citation {n}` / `Unpin citation {n}`);
  Esc-to-close restoring marker focus; reduced-motion honored; never color-alone (a visible WORD present); NO dangerouslySetInnerHTML.
Markers render INSIDE MessageItem (G-5) — the suite renders CitedMarkdown/CitationPeek directly; do not edit MessageItem render/stream logic.

<!-- 154 surfaces: frontend/src/pages/SettingsPage.tsx (Show technical names toggle row + relabeled tabs);
     frontend/src/components/chat/MessageInput.tsx (composer mode helpers); frontend/src/components/ingestion/DocumentStatusBadge.tsx (relabels). -->
<!-- DocumentDetailPanel already has its a11y suite (metadata/DocumentDetailPanel.a11y.test.tsx) — do NOT duplicate it. -->

<!-- Shared setup (global): setupTests.ts extends the axe matcher; import { axe } from "vitest-axe". Mock supabase/api; wrap useTechnicalNames() consumers in <TechnicalNamesProvider>; jsdom matchMedia stub for reduced-motion. -->
<!-- CRITICAL: color-contrast is a NO-OP in jsdom - STRUCTURAL rules only. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run modal a11y suite (152 — file input trap spot + delete confirm)</name>
  <files>frontend/src/pages/__tests__/RunModal.a11y.test.tsx (+ additive aria fix to frontend/src/pages/WorkflowsPage.tsx RunModal or a shared ui/* primitive only if axe surfaces a real violation, per D-13)</files>
  <read_first>
    - frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx (dialog + combobox pattern to copy)
    - frontend/src/pages/WorkflowsPage.tsx (the RunModal function — file input, KB-folder scope select, launch, workflow-delete confirm, launchError role="alert")
    - frontend/src/pages/__tests__/RunModal.test.tsx, ChatLayoutLaunch.test.tsx (existing mocks to reuse)
  </read_first>
  <action>
    Create `frontend/src/pages/__tests__/RunModal.a11y.test.tsx`. Render the Run modal open, settle, then `expect(await axe(container)).toHaveNoViolations()` (STRUCTURAL only). Assert the D-09 scenario-1 contract so the live keyboard drive can succeed: the template file input is queryable and uses the accessible hidden-input + proxy-button pattern (the proxy `<button>` has an accessible name; the hidden `<input type="file">` carries `aria-label="Upload template file"` and `tabIndex={-1}` so Tab passes THROUGH it — not a trap); the remove-template button has `aria-label="Remove template"`; the KB-folder scope control is reachable by role+name; `launchError` renders as `role="alert"`. Also cover the workflow-delete confirm dialog (152) — render it and assert it has a role + accessible name + a named confirm button (never color-alone). VERIFY the shipped pattern; do NOT rebuild the file input. If a real violation surfaces, additive-fix at the owning component (D-13) and note it; document any false positive per D-14.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/RunModal.a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/__tests__/RunModal.a11y.test.tsx` exists and the vitest command above exits 0.
    - The suite asserts the hidden file input carries `tabIndex={-1}` + `aria-label="Upload template file"` and the proxy button has an accessible name (Tab-through, not a trap).
    - The suite asserts the remove-template button (aria-label="Remove template"), a role+name KB-folder scope control, and `launchError` as `role="alert"`.
    - The workflow-delete confirm is asserted to have a role + accessible name + a named confirm button (not color-alone).
    - The suite calls `toHaveNoViolations()`; no contrast assertion; the file input is not rebuilt.
  </acceptance_criteria>
  <done>RunModal.a11y.test.tsx green; file-input Tab-through pattern + labeled controls + role=alert + delete-confirm role/name asserted; no rebuild.</done>
</task>

<task type="auto">
  <name>Task 2: Citation UI a11y suite (153 — verify shipped contracts hold)</name>
  <files>frontend/src/components/chat/__tests__/CitationUI.a11y.test.tsx (+ additive aria fix to the citation components frontend/src/components/chat/{CitedMarkdown,CitationPeek,CitationList,CitationCard,AbsenceHint}.tsx only if a contract regressed, per D-13 — NEVER MessageItem.tsx/StreamsProvider.tsx)</files>
  <read_first>
    - frontend/src/components/metadata/DocumentDetailPanel.a11y.test.tsx (role/aria contract + matchMedia reduced-motion stub)
    - frontend/src/components/chat/CitedMarkdown.tsx, CitationPeek.tsx, CitationList.tsx, CitationCard.tsx, AbsenceHint.tsx (the shipped a11y contracts to verify)
    - frontend/src/components/chat/__tests__/ (existing CitationPeek/CitationList/CitedMarkdown tests to reuse mocks)
  </read_first>
  <action>
    Create `frontend/src/components/chat/__tests__/CitationUI.a11y.test.tsx` — one file with a `describe` per citation component. For each: render with representative props, settle, `expect(await axe(container)).toHaveNoViolations()` (STRUCTURAL only). VERIFY (do NOT rebuild) the shipped 153 contracts: CitationPeek pinned = `role="dialog" aria-modal="false"` with an accessible name (filename) and NO focus trap; the pin toggle exposes `aria-pressed` + the state-toggled `aria-label` (`Pin citation {n}` ↔ `Unpin citation {n}`); Esc closes the peek; reduced-motion (stub matchMedia reduce=true → element STILL renders); never-color-alone (a visible WORD present); the citation markers are `role="button"` with an accessible name and NO `dangerouslySetInnerHTML` in the tree. This is a VERIFY suite — it must NOT edit MessageItem/StreamsProvider (G-5); render the citation components directly. If a contract regressed under the Plan 03 sweep, additive-fix at the citation component (D-13, not MessageItem) and note it. Document any false positive per D-14.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__/CitationUI.a11y.test.tsx && npx vitest run src/components/chat/__tests__/MessageItem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/chat/__tests__/CitationUI.a11y.test.tsx` exists and the vitest command above exits 0.
    - The suite asserts CitationPeek pinned is `role="dialog" aria-modal="false"` with an accessible name and the pin toggle's state-toggled `aria-label` + `aria-pressed`.
    - The suite asserts Esc-to-close, reduced-motion (element still renders), never-color-alone (visible WORD), and NO `dangerouslySetInnerHTML` in the tree.
    - `git diff --name-only` does NOT include MessageItem.tsx or StreamsProvider.tsx; MessageItem.test replay suite is green.
    - The suite calls `toHaveNoViolations()`; no contrast assertion.
  </acceptance_criteria>
  <done>CitationUI.a11y.test.tsx green (contracts verified); MessageItem replay suite green (G-5 tripwire); no G-5 file edits; no innerHTML.</done>
</task>

<task type="auto">
  <name>Task 3: 154-surface a11y suites (Settings toggle row, composer mode helpers, DocumentStatusBadge)</name>
  <files>frontend/src/pages/__tests__/SettingsPage.a11y.test.tsx, frontend/src/components/chat/__tests__/MessageInput.a11y.test.tsx, frontend/src/components/ingestion/__tests__/DocumentStatusBadge.a11y.test.tsx (+ additive aria fix to the owning component or shared ui/* primitive only if axe surfaces a real violation, per D-13)</files>
  <read_first>
    - frontend/src/components/metadata/DocumentDetailPanel.a11y.test.tsx (copy this shape)
    - frontend/src/pages/SettingsPage.tsx (the "Show technical names" toggle row + relabeled tabs)
    - frontend/src/components/chat/MessageInput.tsx (composer General/Explorer mode helpers)
    - frontend/src/components/ingestion/DocumentStatusBadge.tsx (relabeled status badge; never color-alone)
    - frontend/src/pages/__tests__/SettingsPage.test.tsx (existing renderSettings() provider wrapper to reuse)
  </read_first>
  <action>
    Create `frontend/src/pages/__tests__/SettingsPage.a11y.test.tsx`, `frontend/src/components/chat/__tests__/MessageInput.a11y.test.tsx`, and `frontend/src/components/ingestion/__tests__/DocumentStatusBadge.a11y.test.tsx`. Same settle-then-scan pattern (STRUCTURAL only), each wrapped in `<TechnicalNamesProvider>` where the component consumes `useTechnicalNames()` (SettingsPage does — reuse the existing renderSettings() wrapper). Contract asserts: the Settings "Show technical names" toggle row exposes an accessible name + switch/`aria-pressed` reachable by role (D-09 scenario 4's toggle); the relabeled Settings tabs keep their tablist/tab roles with accessible names; MessageInput composer mode items are reachable by role+name and any helper text is associated/announced (not orphaned); DocumentStatusBadge status is conveyed by a visible WORD, never color-alone (assert the label text present for each state). Do NOT duplicate DocumentDetailPanel's existing a11y suite. Apply D-13/D-14 as needed. No contrast assertion.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/SettingsPage.a11y.test.tsx src/components/chat/__tests__/MessageInput.a11y.test.tsx src/components/ingestion/__tests__/DocumentStatusBadge.a11y.test.tsx && npx vitest run src/pages/__tests__/SettingsPage.test.tsx src/__tests__/components/DocumentStatusBadge.test.tsx src/components/chat/__tests__/MessageItem.test.tsx >/dev/null 2>&1 && echo NETNEW_SUITES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - The three named `*.a11y.test.tsx` files exist and the vitest command above exits 0.
    - SettingsPage suite asserts the "Show technical names" toggle exposes an accessible name + switch/`aria-pressed` by role, and the relabeled tabs keep tablist/tab roles with accessible names.
    - MessageInput suite asserts composer mode items reachable by role+name with helper text associated/announced.
    - DocumentStatusBadge suite asserts each status is conveyed by a visible WORD (never color-alone).
    - Targeted non-regression suites (`npx vitest run src/pages/__tests__/SettingsPage.test.tsx src/__tests__/components/DocumentStatusBadge.test.tsx src/components/chat/__tests__/MessageItem.test.tsx`) green — full `npm test` runs at the wave boundary per 155-VALIDATION.md Sampling Rate; each suite calls `toHaveNoViolations()`; no contrast assertion; DocumentDetailPanel's existing suite not duplicated.
  </acceptance_criteria>
  <done>Three 154-surface a11y suites green; Settings toggle/tabs + composer + badge roles/names asserted; targeted non-regression suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test render → axe scan | Additive test files. The citation cluster renders G-5-adjacent components (markers live in MessageItem) — the suite must not edit MessageItem/StreamsProvider. |
| model text → citation markup | Verify no XSS regression (V5) — citation markers must stay `document.createElement`/auto-escaped, never raw-HTML injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-06-XSS | Tampering | CitedMarkdown / citation markers | mitigate | The VERIFY suite asserts NO `dangerouslySetInnerHTML` in the tree; must not reintroduce raw-HTML injection while checking aria (153 dropped innerHTML deliberately) |
| T-155-06-G5 | Tampering | `MessageItem.tsx` / `StreamsProvider.tsx` | mitigate | D-13 RED LINE — the citation suite renders components directly and edits none of MessageItem's render/stream logic; MessageItem.test replay suite is the tripwire |
| T-155-06-FALSEGREEN | Repudiation (false-green) | jsdom color-contrast no-op | mitigate | STRUCTURAL rules only; contrast + the keyboard walkthrough are proven live (D-03/D-08/D-09) in VALIDATION.md; no "verify contrast" assertion (G-6 #1) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/__tests__/RunModal.a11y.test.tsx src/components/chat/__tests__/CitationUI.a11y.test.tsx src/pages/__tests__/SettingsPage.a11y.test.tsx src/components/chat/__tests__/MessageInput.a11y.test.tsx src/components/ingestion/__tests__/DocumentStatusBadge.a11y.test.tsx` → all green.
- `npx vitest run src/components/chat/__tests__/MessageItem.test.tsx` → green (G-5 non-regression).
- `git diff --name-only` → MessageItem.tsx / StreamsProvider.tsx NOT modified.
- `npx tsc -b` → 30 baseline, 0 net-new; `npx vite build` → 0; `npm test` → full suite green.
- Live (VALIDATION.md, D-08/D-09/D-10): operator keyboard-drives scenario 1 (workflow launch), scenario 2 (cited answer), scenario 4 (Settings + nav toggle) — task + 3 invariants each.
</verification>

<success_criteria>
The remaining net-new v3.3 surfaces (152 Run modal, 153 citation UI, 154 surfaces) each have a passing vitest-axe a11y suite asserting zero structural violations and the roles/names the D-09 keyboard scenarios depend on, with the citation cluster verified (not rebuilt) and G-5 hot files untouched.
</success_criteria>

<output>
Create `.planning/phases/155-accessibility-sweep-wcag-aa/155-06-SUMMARY.md` when done. Record: suites created, whether the 153 contracts still held (or needed an additive citation-component fix), the Run-modal file-input trap-spot result, and any D-14 exclusions.
</output>
