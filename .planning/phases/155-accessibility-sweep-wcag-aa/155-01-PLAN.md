---
phase: 155-accessibility-sweep-wcag-aa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/eslint.config.js
  - .github/workflows/frontend-tests.yml
  - .planning/phases/155-accessibility-sweep-wcag-aa/155-lint-inventory.txt
autonomous: true
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "D-02: eslint-plugin-jsx-a11y is installed as a dev dependency and its recommended rule-set runs app-wide at ERROR severity"
    - "D-01a: @axe-core/playwright is NOT added (the rotted Playwright harness stays untouched per SEED-049)"
    - "D-02: the jsx-a11y lint gate runs in CI (a `npm run lint` step exists in frontend-tests.yml) so an a11y regression cannot silently merge"
    - "The complete set of jsx-a11y violations against current source is captured as an inventory artifact for the Plan 03 fix sweep"
  artifacts:
    - path: "frontend/eslint.config.js"
      provides: "jsx-a11y recommended flat config spread into the ESLint 9 array at error severity"
      contains: "jsxA11y.flatConfigs.recommended"
    - path: "frontend/package.json"
      provides: "eslint-plugin-jsx-a11y dev dependency pinned"
      contains: "eslint-plugin-jsx-a11y"
    - path: ".github/workflows/frontend-tests.yml"
      provides: "CI lint enforcement step"
      contains: "npm run lint"
    - path: ".planning/phases/155-accessibility-sweep-wcag-aa/155-lint-inventory.txt"
      provides: "captured jsx-a11y violation list Plan 03 consumes"
  key_links:
    - from: "frontend/eslint.config.js"
      to: "eslint-plugin-jsx-a11y"
      via: "import + flatConfigs.recommended spread"
      pattern: "eslint-plugin-jsx-a11y"
    - from: ".github/workflows/frontend-tests.yml"
      to: "frontend/package.json scripts.lint"
      via: "npm run lint step in the vitest job"
      pattern: "npm run lint"
---

<objective>
Install and wire the jsx-a11y regression gate that makes the rest of Phase 155 permanent: add `eslint-plugin-jsx-a11y` (the ONE new dev dep, D-02), enable its recommended rule-set as ERRORS app-wide in the ESLint 9 flat config, add a `npm run lint` step to CI so the "regressions cannot merge" promise is structurally true, and capture the full current violation inventory so Plan 03 can fix every one.

Purpose: This is the foundation plan — it defines exactly WHAT must be fixed (the lint inventory) and locks the gate so no future PR can reintroduce an unlabeled icon button or missing-role violation. Without the CI step (RESEARCH "CRITICAL CI gap"), jsx-a11y would be local-only convenience and D-02's promise would be hollow.
Output: A wired eslint config, a pinned dev dep, a CI lint step, and `155-lint-inventory.txt` listing every violation.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-CONTEXT.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md

<interfaces>
<!-- Current ESLint 9 flat config (frontend/eslint.config.js) — the executor spreads jsx-a11y into this array. -->
Current file (verbatim): react-hooks + react-refresh only, no a11y.
  import js from '@eslint/js'
  import globals from 'globals'
  import reactHooks from 'eslint-plugin-react-hooks'
  import reactRefresh from 'eslint-plugin-react-refresh'
  import tseslint from 'typescript-eslint'
  import { defineConfig, globalIgnores } from 'eslint/config'
  export default defineConfig([
    globalIgnores(['dist']),
    { files: ['**/*.{ts,tsx}'], extends: [ js.configs.recommended, tseslint.configs.recommended,
        reactHooks.configs.flat.recommended, reactRefresh.configs.vite ],
      languageOptions: { ecmaVersion: 2020, globals: globals.browser } },
  ])

<!-- frontend/package.json already has the lint script — do NOT add it. -->
scripts.lint === "eslint ."   (present, line 9)

<!-- Package Legitimacy Audit (RESEARCH.md) — eslint-plugin-jsx-a11y@6.10.2 -->
Registry npm, ~10yr old, jsx-eslint org, no postinstall, slopcheck [OK] → Approved. Pin exactly 6.10.2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install eslint-plugin-jsx-a11y, wire recommended-as-errors, capture the violation inventory</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/eslint.config.js, .planning/phases/155-accessibility-sweep-wcag-aa/155-lint-inventory.txt</files>
  <read_first>
    - frontend/eslint.config.js (current flat config — you spread into this array)
    - frontend/package.json (confirm scripts.lint === "eslint ." already exists; devDependencies block for placement)
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md ("ESLint flat-config wiring (D-02)" + "Package Legitimacy Audit" + "Code Examples → ESLint flat-config wiring")
  </read_first>
  <action>
    From `frontend/`, install the ONE new dev dep pinned exactly: `npm install --save-dev eslint-plugin-jsx-a11y@6.10.2` (records it in package.json + package-lock.json). Do NOT add `@axe-core/playwright` (D-01a). In `frontend/eslint.config.js`, add `import jsxA11y from 'eslint-plugin-jsx-a11y'` and spread `jsxA11y.flatConfigs.recommended` into the `defineConfig([...])` array BEFORE the `files: ['**/*.{ts,tsx}']` block. Use `recommended`, NOT `strict` (strict balloons the sweep past the D-06 scope line). `recommended` already sets its rules at error severity — after wiring, run `npx eslint --print-config src/main.tsx` and confirm jsx-a11y rules resolve to `"error"` / `2`; if ANY jsx-a11y rule resolves to `"warn"` / `1`, pin it to `'error'` explicitly in the `files` block's rules. Then capture the full current violation list to the inventory artifact: `npx eslint . -f stylish > ../.planning/phases/155-accessibility-sweep-wcag-aa/155-lint-inventory.txt 2>&1 || true` (the `|| true` is required — lint WILL exit non-zero because violations are not fixed until Plan 03; that is expected here). Do NOT fix any violations in this plan and do NOT add any `eslint-disable` comments.
  </action>
  <verify>
    <automated>cd frontend && grep -q "eslint-plugin-jsx-a11y" package.json && grep -q "jsxA11y.flatConfigs.recommended" eslint.config.js && npx eslint --print-config src/main.tsx | grep -q "jsx-a11y/" && test -s ../.planning/phases/155-accessibility-sweep-wcag-aa/155-lint-inventory.txt && echo GATE_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` devDependencies contains `"eslint-plugin-jsx-a11y": "6.10.2"` (exact pin).
    - `frontend/eslint.config.js` contains `import jsxA11y from 'eslint-plugin-jsx-a11y'` AND `jsxA11y.flatConfigs.recommended` spread into the array.
    - `cd frontend && npx eslint --print-config src/main.tsx | grep "jsx-a11y/"` prints jsx-a11y rules, all at `2` (error) — none at `1` (warn).
    - `grep "@axe-core/playwright" frontend/package.json` returns nothing (D-01a).
    - `.planning/phases/155-accessibility-sweep-wcag-aa/155-lint-inventory.txt` exists and is non-empty.
    - `grep -rn "eslint-disable" frontend/src` shows no NEW jsx-a11y disables added by this task.
  </acceptance_criteria>
  <done>Gate installed + wired at error severity; violation inventory captured; @axe-core/playwright absent; no suppressions added.</done>
</task>

<task type="auto">
  <name>Task 2: Add the npm run lint step to CI (frontend-tests.yml vitest job)</name>
  <files>.github/workflows/frontend-tests.yml</files>
  <read_first>
    - .github/workflows/frontend-tests.yml (the `vitest` job's `Install + test` step at L38-42 — you mirror its shape)
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md ("frontend-tests.yml (config, CI)" section — the exact step to add)
  </read_first>
  <action>
    In `.github/workflows/frontend-tests.yml`, inside the existing `vitest` job, add a new step named "Lint (jsx-a11y as errors)" that runs `cd frontend && npm run lint`. Place it AFTER the `Install + test` step's `npm ci` has run (either a new step right after `Install + test` reusing the installed deps, or split install into its own step then lint then test). Do NOT touch the `playwright` job, do NOT add new secrets, do NOT add any third-party marketplace actions. The step reuses `npm run lint` (= `eslint .`, already in package.json scripts — do not redefine). Add a comment noting this enforces D-02.
  </action>
  <verify>
    <automated>grep -q "npm run lint" .github/workflows/frontend-tests.yml && echo CI_LINT_STEP_ADDED</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/frontend-tests.yml` contains a step running `npm run lint` inside the `vitest` job.
    - The `playwright` job block is byte-identical to before (no diff).
    - No new `secrets.*` references and no new `uses:` third-party actions were added (`git diff` shows only the additive lint step).
  </acceptance_criteria>
  <done>CI vitest job runs `npm run lint`; playwright job unchanged; no new secrets or marketplace actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev toolchain | A new dev dependency (`eslint-plugin-jsx-a11y`) is installed and executed by developers/CI |
| repo → CI runner | A GitHub Actions workflow file is edited (supply-chain / CI-config surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-01-SC | Tampering | `eslint-plugin-jsx-a11y` npm install | mitigate | Pin exact `6.10.2`; RESEARCH "Package Legitimacy Audit" verdict is slopcheck `[OK]` (Approved), jsx-eslint org, no postinstall script — legitimate, no blocking-human gate required for an `[OK]` package |
| T-155-01-CI | Tampering | `.github/workflows/frontend-tests.yml` edit | mitigate | Additive `npm run lint` step only; no new secrets, no third-party actions, no change to the `playwright` job or existing secret handling |
</threat_model>

<verification>
- `cd frontend && grep "eslint-plugin-jsx-a11y" package.json` → present in devDependencies.
- `cd frontend && npx eslint --print-config src/main.tsx | grep "jsx-a11y/"` → jsx-a11y rules present at error.
- `grep "@axe-core/playwright" frontend/package.json` → ABSENT (D-01a).
- `155-lint-inventory.txt` exists, non-empty (feeds Plan 03).
- `grep "npm run lint" .github/workflows/frontend-tests.yml` → present.
- Note: `cd frontend && npm run lint` intentionally FAILS at this plan's end (violations unfixed until Plan 03) — expected; the whole phase lands as a unit before any deploy.
</verification>

<success_criteria>
The jsx-a11y regression gate is installed, wired at error severity, enforced in CI, and the full violation inventory is captured for the Plan 03 fix sweep — with @axe-core/playwright deliberately not added.
</success_criteria>

<output>
Create `.planning/phases/155-accessibility-sweep-wcag-aa/155-01-SUMMARY.md` when done. Record the violation count from 155-lint-inventory.txt so Plan 03 has a target.
</output>
