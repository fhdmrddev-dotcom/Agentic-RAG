---
phase: 155-accessibility-sweep-wcag-aa
plan: 03
type: execute
wave: 2
depends_on: ["155-01", "155-02"]
files_modified:
  - frontend/src/components/layout/NavPanel.tsx
  - "frontend/src/**  (every file the jsx-a11y lint inventory flags — app-wide fix sweep)"
  - .planning/seeds/SEED-092-remainder.md
autonomous: true
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "D-02: every jsx-a11y lint error from the Plan 01 inventory is fixed at the source (real fixes — aria-label adds, static-element→button conversions); `npm run lint` exits 0 with ZERO per-file or per-rule eslint-disable suppressions"
    - "D-05: every icon-only button app-wide carries an accessible name (aria-label), closing the button-name offender category permanently; the NavPanel `<span onClick>` row-menu trigger becomes a real `<button aria-label>`, and title-only icon buttons (New Chat, Choose folder) gain aria-label"
    - "D-13: a violation that traces to a shared shadcn/Radix primitive is fixed once at the primitive (additive aria/focus attributes); inside MessageItem.tsx / StreamsProvider.tsx only display-additive attribute changes are allowed — never render/stream-logic edits (G-5)"
    - "D-06: every pre-existing finding OUTSIDE the scope line (focus-visible gaps on old pages, heading order, missing alt, full screen-reader UX pass, the Phase-095 chat tool-cards) is logged to SEED-092-remainder.md — visible, explicitly NOT fixed this phase, with a re_open_trigger"
    - "D-11: accessible NAMES are the bar (verified by axe scan + the DevTools accessibility tree); no live NVDA/JAWS pass is done this phase"
  artifacts:
    - path: ".planning/seeds/SEED-092-remainder.md"
      provides: "documented follow-up list of out-of-scope a11y findings (D-06)"
      contains: "re_open_trigger"
  key_links:
    - from: "frontend/src/components/layout/NavPanel.tsx"
      to: "accessible name"
      via: "aria-label on every icon-only button + <button> conversion of the row-menu trigger"
      pattern: "aria-label"
    - from: "frontend package scripts.lint"
      to: "zero jsx-a11y errors"
      via: "npm run lint exits 0 with no eslint-disable suppressions"
      pattern: "aria-label"
---

<objective>
Eat the whole a11y backlog the lint gate surfaced: fix EVERY jsx-a11y error from the Plan 01 inventory at the source (D-02, real fixes only — no suppressions), sweep an `aria-label` onto every icon-only button app-wide so the button-name category closes permanently (D-05), and log every out-of-scope finding to a documented SEED-092-remainder follow-up list (D-06).

Purpose: This is the one natural moment to fix the whole button-name backlog — the lint rule and the icon-button sweep are the same work done once, and after it lands regressions cannot merge (the CI gate from Plan 01). Most fixes are mechanical aria-label adds; the notable non-mechanical one is converting NavPanel's `<span onClick>` row-menu trigger into a real keyboard-operable `<button>`.
Output: `npm run lint` green, every icon button labeled, and a visible follow-up list for the exhaustive future sweep.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-CONTEXT.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-lint-inventory.txt

<interfaces>
<!-- Icon-button aria-label patterns already in the repo (RESEARCH/PATTERNS) — replicate, do not invent. -->
State-toggled (gold pattern, CitationPeek.tsx L148-149):
  aria-pressed={pinned}
  aria-label={pinned ? `Unpin citation ${n}` : `Pin citation ${n}`}
Static icon-only (Run-modal remove, WorkflowsPage.tsx; Stop, NavPanel):
  <button type="button" aria-label="Remove template" onClick={...}>
    <X className="h-3 w-3" aria-hidden="true" />   {/* glyph decorative; label on the button */}
  </button>
~199 aria-labels already exist app-wide — the sweep closes the gap, it does not create a new pattern.

<!-- Named NavPanel offenders (verify against the fresh live scan; counts shifted since 2026-06-20) -->
- `<span onClick>` row-menu trigger (~L214) → convert to `<button type="button" aria-label="Thread options">` (jsx-a11y no-static-element-interactions / click-events-have-key-events will force this)
- "New Chat" (~L353) + "Choose folder" (~L363) use title= only → axe accepts title as a name, but prefer aria-label (announced consistently) per D-05

<!-- aria-label copy: plain-language, consistent with frontend/src/lib/termMap.ts where a term exists (D-05, Claude's Discretion). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix every jsx-a11y lint error to zero — real fixes, no suppressions</name>
  <files>frontend/src/components/layout/NavPanel.tsx, every file flagged in .planning/phases/155-accessibility-sweep-wcag-aa/155-lint-inventory.txt (app-wide fix sweep), and shared shadcn/ui primitives (frontend/src/components/ui/*.tsx) where a violation traces to the primitive (D-13)</files>
  <read_first>
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-lint-inventory.txt (the exact violation list from Plan 01 — your worklist)
    - frontend/src/components/layout/NavPanel.tsx (the named `<span onClick>` row-menu offender + title-only buttons)
    - frontend/src/lib/termMap.ts (plain-language vocabulary for label copy)
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md ("Anti-Patterns to Avoid" — never a whole-file eslint-disable; "Pitfall 2" title vs aria-label)
  </read_first>
  <action>
    Walk `155-lint-inventory.txt` and fix every jsx-a11y error at the source. Common fixes: add `aria-label` to icon-only `<button>`s (glyph marked `aria-hidden="true"`); convert interactive `<span onClick>` / `<div onClick>` into a real `<button type="button">` with an accessible name (NavPanel row-menu trigger → `<button aria-label="Thread options">`, keeping its dropdown wiring); add `htmlFor`/`id` label associations; add `alt` to images. Label copy: plain-language, consistent with `termMap.ts` where a term exists. FORBIDDEN: any `/* eslint-disable jsx-a11y/... */` (whole-file OR line) to hit zero — that is D-02 violated in spirit (G-6 #5); if a rule genuinely cannot be satisfied because it is a false positive on a shared Radix internal, that is a D-14 case → document it (defer to Task 3's remainder list) rather than blanket-disabling. Per D-13: if the violation traces to a shared shadcn/ui or Radix wrapper (e.g. `ui/button.tsx`), fix it once at the primitive (additive attributes) and let consumers inherit; inside MessageItem.tsx / StreamsProvider.tsx apply ONLY additive aria attributes, never render/stream-logic edits (G-5 RED LINE). Re-run `npm run lint` iteratively until it exits 0.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && grep -rn "eslint-disable.*jsx-a11y" src | wc -l | grep -qx 0 && echo LINT_ZERO_NO_SUPPRESSIONS</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint` exits 0.
    - `grep -rn "eslint-disable.*jsx-a11y" frontend/src | wc -l` → 0.
    - `frontend/src/components/layout/NavPanel.tsx` row-menu trigger is a `<button` with an accessible name (no interactive `<span onClick>` / `<div onClick>` remains for that control).
    - `git diff` on MessageItem.tsx / StreamsProvider.tsx (if touched) shows only additive `aria-*` attribute lines — no logic/render/stream lines changed.
    - Any shared-primitive fix lives in `frontend/src/components/ui/*.tsx` (D-13), not duplicated per consumer.
  </acceptance_criteria>
  <done>Lint green via real fixes; zero jsx-a11y suppressions; NavPanel trigger is a labeled button; G-5 files logic-untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Icon-button completeness pass — close the button-name category (D-05)</name>
  <files>frontend/src/components/layout/NavPanel.tsx, plus any component with a still-unlabeled icon-only button surfaced by the live button-name scan (app-wide)</files>
  <read_first>
    - frontend/src/components/layout/NavPanel.tsx (title-only "New Chat" / "Choose folder" buttons)
    - frontend/src/components/chat/CitationPeek.tsx (L148-149 state-toggled aria-label gold pattern)
    - frontend/src/lib/termMap.ts (label vocabulary)
  </read_first>
  <action>
    Beyond what lint forced in Task 1, ensure EVERY icon-only button app-wide has an `aria-label` so the button-name category is permanently closed (D-05) — including buttons that pass axe via `title=` but should use an announced `aria-label` (NavPanel "New Chat", "Choose folder"). Prefer `aria-label` over `title` for the accessible name; a `title` tooltip may remain for hover UX but the accessible name is the `aria-label`. For toggle icon buttons, use the state-toggled `aria-label` + `aria-pressed` pattern (CitationPeek) so the label reflects state. Enumerate remaining candidates with a grep for icon-only `<button>` elements, but rely on the live button-name scan (VALIDATION.md, D-03) as the source of truth for the residual set — do NOT hard-code a "46 buttons" target (the count shifted since the 2026-06-20 baseline). Keep the glyph `aria-hidden="true"` and the label on the button. Do NOT put IDs or secrets in labels (they are UI verbs — T-155-03-INFO).
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test >/dev/null 2>&1 && grep -c "aria-label" src/components/layout/NavPanel.tsx | awk '$1>=1{print "NAVPANEL_LABELED"; exit} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/layout/NavPanel.tsx` "New Chat", "Choose folder", and "Thread options" controls each carry an `aria-label`.
    - `cd frontend && npm run lint` still exits 0; `cd frontend && npm test` → full suite green (non-regression).
    - Every icon-only button's glyph is `aria-hidden="true"` with the accessible name on the button (spot-check the files touched).
    - No aria-label contains a raw ID, UUID, email, or secret (labels are UI verbs).
  </acceptance_criteria>
  <done>Every icon-only button labeled (button-name category closed); lint + tests green; labels are safe UI verbs.</done>
</task>

<task type="auto">
  <name>Task 3: Create the SEED-092-remainder follow-up list (D-06)</name>
  <files>.planning/seeds/SEED-092-remainder.md</files>
  <read_first>
    - .planning/seeds/SEED-092-app-wide-wcag-aa-contrast-and-icon-button-labels.md (the parent seed — format + the offender baseline)
    - .planning/seeds/SEED-049-e2e-suite-revival.md (seed-file frontmatter/structure reference)
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-CONTEXT.md (D-06 scope line — what is explicitly deferred)
  </read_first>
  <action>
    Create `.planning/seeds/SEED-092-remainder.md` following the existing seed-file template (frontmatter with a `re_open_trigger`). Seed it with the KNOWN out-of-scope categories D-06 defers: focus-visible gaps on OLD (non-net-new) pages, heading-order issues on legacy pages, missing `alt` on legacy images, the FULL screen-reader UX pass / one-off NVDA run (D-11 kept names-only), the Phase-095 chat tool-cards frame (explicitly excluded from D-12), and any light-theme-only deep audit beyond the sweep. Add a placeholder section "Discovered during the 155 sweep / live scan — append here" for findings the Task 1/2 sweep and the D-03 live scan surface that fall outside the scope line (e.g. any documented decorative/disabled contrast exemptions from Plan 02, any D-14 false-positive exclusions). Set `re_open_trigger` to the future exhaustive-WCAG-AA polish slot (REQUIREMENTS.md "Future Requirements → Full app-wide WCAG AA sweep"). This list must be VISIBLE, not lost — nothing gets silently swallowed (G-6 #7).
  </action>
  <verify>
    <automated>test -f "C:/Vibe Apps/Agentic RAG/.planning/seeds/SEED-092-remainder.md" && grep -q "re_open_trigger" "C:/Vibe Apps/Agentic RAG/.planning/seeds/SEED-092-remainder.md" && echo REMAINDER_SEEDED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/seeds/SEED-092-remainder.md` exists with seed-file frontmatter including a `re_open_trigger`.
    - The file lists the known out-of-scope categories (legacy focus-visible, heading order, missing alt, full SR pass, Phase-095 tool-cards, light-theme deep audit).
    - The file has an "append during execution" section for sweep/live-scan discoveries.
    - `re_open_trigger` references the future exhaustive-WCAG-AA polish slot.
  </acceptance_criteria>
  <done>SEED-092-remainder.md exists — visible follow-up list with a re_open_trigger and an append section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author markup → a11y tree | aria-label copy becomes the announced accessible name (could leak data if careless) |
| shared primitive → all consumers | a fix at a shadcn/Radix wrapper propagates app-wide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-03-INFO | Information Disclosure | icon-button `aria-label` copy | mitigate | Labels are UI verbs ("Thread options", "Stop run", "New chat") consistent with termMap.ts — never document IDs, user IDs, or secrets |
| T-155-03-G5 | Tampering | `MessageItem.tsx` / `StreamsProvider.tsx` | mitigate | D-13 RED LINE — additive aria attributes ONLY; citation replay/render suites are the non-regression tripwire; `git diff` shows no logic-line changes |
| T-155-03-SUPPRESS | Repudiation (false-green) | eslint-disable to hit lint-zero | mitigate | FORBIDDEN — `grep -rn "eslint-disable.*jsx-a11y" src` must be 0; genuine false positives go to SEED-092-remainder / D-14 documented exclusions, never a blanket disable (G-6 #5) |
</threat_model>

<verification>
- `cd frontend && npm run lint` → exit 0.
- `grep -rn "eslint-disable.*jsx-a11y" frontend/src | wc -l` → 0.
- `cd frontend && npm test` → full suite green; `npx tsc -b` → 30 baseline, 0 net-new; `npx vite build` → 0.
- `git diff --name-only` on MessageItem.tsx/StreamsProvider.tsx (if present) shows only additive aria attribute lines.
- `.planning/seeds/SEED-092-remainder.md` exists with a re_open_trigger.
- Live (VALIDATION.md, D-03): Chrome DevTools/Lighthouse button-name audit on the SEED-092 pages → 0 failing nodes.
</verification>

<success_criteria>
The jsx-a11y lint gate is green with zero suppressions, every icon-only button app-wide is labeled (button-name category permanently closed), G-5 hot files are untouched at the logic level, and every out-of-scope finding is documented in SEED-092-remainder.md.
</success_criteria>

<output>
Create `.planning/phases/155-accessibility-sweep-wcag-aa/155-03-SUMMARY.md` when done. Record: the count of lint errors fixed, the NavPanel conversions, any shared-primitive fixes (D-13), any D-14 false-positive candidates, and the SEED-092-remainder categories logged.
</output>
