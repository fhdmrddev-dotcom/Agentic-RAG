---
phase: 155-accessibility-sweep-wcag-aa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/components/admin/AuditTab.tsx
  - frontend/src/components/admin/UsersAndAccess.tsx
  - frontend/src/components/admin/ModelRegistryTab.tsx
  - frontend/src/components/admin/HealthSignals.tsx
  - frontend/src/components/admin/RecentActionsCard.tsx
  - frontend/src/components/admin/FeatureVisibility.tsx
  - frontend/src/components/chat/ToolCallPanel.tsx
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/components/chat/OutputFileCard.tsx
  - frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx
  - frontend/src/components/skills/studio/RunHistory.tsx
  - frontend/src/components/skills/studio/VersionsTab.tsx
  - frontend/src/components/skills/studio/RunCaseDetail.tsx
  - frontend/src/components/classification/RuleBuilderPanel.tsx
  - frontend/src/components/settings/ReembedConfirmModal.tsx
  - frontend/src/components/settings/ReembedStatusCard.tsx
  - frontend/src/components/settings/ProviderPicker.tsx
  - frontend/src/components/ingestion/ConditionPopover.tsx
  - frontend/src/components/ingestion/AutomationGroup.tsx
  - frontend/src/pages/SettingsPage.tsx
  - "frontend/src/**  (remaining text-muted-foreground/{40,50,60,70} occurrences across the full 42-file set)"
autonomous: false
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "D-04: the global dark `--muted-foreground-dim` token is lifted from 220 16% 45% (fails, ~3.6:1) to 220 16% 70% (~8:1 on bg), keeping the blue-gray muted read while passing WCAG 2.1 AA 1.4.3"
    - "D-04: every opacity-modified muted-text offender (text-muted-foreground/{40,50,60,70}) on MEANINGFUL text is swept onto a full-opacity AA-passing token; only truly decorative/disabled elements stay exempt (WCAG-allowed)"
    - "D-04: the base `--muted-foreground` token (already ~7.7:1) is NOT rewritten — only the DIM token and the opacity modifiers are changed"
    - "D-07: the operator personally eyeballs the retuned tokens on chat, documents, and Control Room (before/after) and approves that the Deep Midnight look is preserved before the sweep is considered final"
    - "D-03: the color-contrast category reaches zero failing nodes app-wide on the SEED-092 baseline pages, provable only by the live Chrome DevTools scan (jsdom cannot compute contrast)"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "AA-passing global --muted-foreground-dim token + extended contrast-math documentation"
      contains: "--muted-foreground-dim: 220 16% 70%"
  key_links:
    - from: "frontend/src/**/*.tsx"
      to: "frontend/src/index.css tokens"
      via: "className text-muted-foreground / text-muted-foreground-dim (no opacity modifier on meaningful text)"
      pattern: "text-muted-foreground"
---

<objective>
Fix the #1 pre-existing offender class — low-contrast muted text — at the SOURCE (D-04). Lift the global dark `--muted-foreground-dim` token to an AA-passing value (mirroring the already-shipped Phase 088-05 panel token), then sweep every opacity-modified muted-text className (`text-muted-foreground/{40,50,60,70}`, 133 occurrences across 42 files) onto full-opacity AA-passing tokens. A mid-execution operator eyeball (D-07) confirms the Deep Midnight look survives before the sweep is final.

Purpose: One decision, app-wide — no page can regress by using the old dim token because the token itself becomes safe. The opacity modifier is the real killer (65% L @ 0.6 alpha ≈ 3.4:1). This half is contrast-only; because jsdom cannot compute color-contrast, the actual pass/fail bar is the live scan (D-03), captured in VALIDATION.md.
Output: A retuned index.css token block + a clean opacity sweep + operator sign-off on the look.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-CONTEXT.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md

<interfaces>
<!-- index.css token facts (verified this session) — the executor edits ONE token + documents the math. -->
Dark theme (.dark), against --background 216 45% 4% (#060a0f):
  --muted-foreground:      220 16% 65% (#97a1b4)  → ~7.7:1  PASSES  → DO NOT CHANGE (index.css L94)
  --muted-foreground-dim:  220 16% 45% (#606d85)  → ~3.6:1  FAILS   → LIFT to 220 16% 70% (index.css L107-109)
Proven precedent already in-file (Phase 088-05, L123-132):
  --panel-muted-foreground-dim: 220 16% 70% (#a6aebf) → 8.42:1  ← the D-04 target template
Global focus floor + reduced-motion blocks already ship (L177 / L375 / L504 / L757) — do NOT add new ones.

<!-- Opacity offender scope (grep-verified): 133 occurrences / 42 files. Highest-density files: -->
AuditTab(13) ToolCallPanel(9) RunHistory(8) UsersAndAccess(8) VersionsTab(6) RuleBuilderPanel(6)
ModelRegistryTab(6) HealthSignals(6) ExecuteCodeBody(5) MessageInput(5) ...  (see grep in Task 2)
Confirmed: MessageItem.tsx and StreamsProvider.tsx have ZERO opacity offenders → the G-5 red line is not crossed by this sweep.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift the global dark --muted-foreground-dim token to AA and extend the contrast-math doc</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (L88-140 — the .dark token block, the Phase 088-05 documented panel-token math at L123-132, and the offender token at L107-109)
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md ("Contrast Token Retune (D-04) — computed values")
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md ("frontend/src/index.css (config, design tokens — D-04)")
  </read_first>
  <action>
    In `frontend/src/index.css`, change the dark-theme global `--muted-foreground-dim` from `220 16% 45%` to `220 16% 70%` (same H/S as the shipped `--panel-muted-foreground-dim`; ~8:1 on the dark background, still reads muted). Extend the EXISTING Phase 087 dim-label comment above the token with the AA math: note the old 45% L = ~3.6:1 FAILED and the new 70% L mirrors the 088-05 panel token (8.42:1) — this satisfies D-04's "extend it". Do NOT touch the base `--muted-foreground` (L94, already ~7.7:1) — rewriting it would wash out the whole app (G-6 #4). Do NOT add new focus-visible or prefers-reduced-motion rules (they already ship at L177/L375/L504/L757). Light theme: the global `--muted-foreground-dim` only exists in the dark block; if a light `--muted-foreground-dim` is absent, leave light as-is (Task 2's opacity sweep fixes light too) and note it in the SUMMARY for the live scan.
  </action>
  <verify>
    <automated>cd frontend && grep -q "muted-foreground-dim: 220 16% 70%" src/index.css && ! grep -q "muted-foreground-dim: 220 16% 45%" src/index.css && npx vite build >/dev/null 2>&1 && echo TOKEN_LIFTED_BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "muted-foreground-dim: 220 16% 70%" frontend/src/index.css` → present.
    - `grep "muted-foreground-dim: 220 16% 45%" frontend/src/index.css` → returns nothing.
    - `grep "muted-foreground: 220 16% 65%" frontend/src/index.css` → still present (base token unchanged).
    - The token comment mentions the new AA ratio (~8:1) and the 088-05 precedent.
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <done>Dark `--muted-foreground-dim` = 220 16% 70% with documented AA math; base token unchanged; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the 133 text-muted-foreground/{40,50,60,70} opacity offenders onto real tokens</name>
  <files>frontend/src/components/admin/AuditTab.tsx, frontend/src/components/admin/UsersAndAccess.tsx, frontend/src/components/admin/ModelRegistryTab.tsx, frontend/src/components/admin/HealthSignals.tsx, frontend/src/components/admin/RecentActionsCard.tsx, frontend/src/components/admin/FeatureVisibility.tsx, frontend/src/components/chat/ToolCallPanel.tsx, frontend/src/components/chat/MessageInput.tsx, frontend/src/components/chat/OutputFileCard.tsx, frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx, frontend/src/components/skills/studio/RunHistory.tsx, frontend/src/components/skills/studio/VersionsTab.tsx, frontend/src/components/skills/studio/RunCaseDetail.tsx, frontend/src/components/classification/RuleBuilderPanel.tsx, frontend/src/components/settings/ReembedConfirmModal.tsx, frontend/src/components/settings/ReembedStatusCard.tsx, frontend/src/components/settings/ProviderPicker.tsx, frontend/src/components/ingestion/ConditionPopover.tsx, frontend/src/components/ingestion/AutomationGroup.tsx, frontend/src/pages/SettingsPage.tsx, and the remaining files from the grep enumeration (~42 total)</files>
  <read_first>
    - frontend/src/index.css (L671 citation-CSS note — the in-file authority: "meaningful muted text uses --muted-foreground, never the --muted-foreground-dim ~3.6:1 trap")
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md ("Opacity sweep" section — the transform rule + highest-density files)
    - The specific files as you edit them (do NOT read all 42 fully up front — use the grep below to jump straight to the offending lines per file)
  </read_first>
  <action>
    Enumerate the offenders first: `cd frontend && grep -rnE 'text-muted-foreground/(40|50|60|70)' src` (expect ~133 hits across 42 files). Apply this decision rule per occurrence:
    (a) MEANINGFUL text (labels, values, meta a user reads — the common case) → drop the opacity modifier, use `text-muted-foreground` (full opacity, already ~7.7:1); for the very quietest meta that must stay a step dimmer, use `text-muted-foreground-dim` (now lifted to AA in Task 1).
    (b) TRULY DECORATIVE or DISABLED elements (a decorative graphic icon, a disabled-state control, placeholder-only affordance) → leave the opacity as-is (WCAG-allowed exemption). When you exempt one, note WHY in the SUMMARY so the live scan (D-03) and the D-06 remainder list can see it.
    Work file-by-file using the grep line numbers; edit only the className, never surrounding logic. `ToolCallPanel.tsx` is a G-5-ADJACENT hot file but NOT a G-5 red-line file (only MessageItem/StreamsProvider are) — a className-only change there is allowed. MessageItem.tsx / StreamsProvider.tsx have no offenders and MUST NOT be touched. Do NOT introduce any NEW `text-muted-foreground-dim` on text that needs full contrast; do NOT convert a decorative `opacity-50` on a graphic (not a `text-muted-foreground/*` class, out of scope). After the sweep, re-run the grep and record the residual count + the exempt list in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npm test >/dev/null 2>&1 && npx vite build >/dev/null 2>&1 && echo "residual offenders:" && grep -rcE 'text-muted-foreground/(40|50|60|70)' src | grep -vE ':0$' | wc -l && echo SWEEP_TESTS_BUILD_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` does NOT include `frontend/src/components/chat/MessageItem.tsx` or `frontend/src/providers/StreamsProvider.tsx`.
    - Every removed offender was a className-only edit (no logic/JSX-structure lines changed — verify via `git diff`).
    - Residual `text-muted-foreground/{40,50,60,70}` occurrences (from `grep -rnE`) are ALL documented as decorative/disabled exemptions in 155-02-SUMMARY.md.
    - `cd frontend && npm test` → full suite green (non-regression).
    - `cd frontend && npx vite build` exits 0; `npx tsc -b` → 30 baseline errors, 0 net-new.
  </acceptance_criteria>
  <done>Meaningful-text opacity offenders swept onto full-opacity tokens; residuals are documented exemptions; tests + build green; G-5 files untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-07 operator eyeball on the retuned tokens (chat / documents / Control Room)</name>
  <files>N/A — verification checkpoint (no files modified)</files>
  <action>
    Claude has already lifted the global dim token (Task 1) and swept the opacity offenders (Task 2). Present the running app to the operator for the D-07 look check: point them at Chat, Documents, and the Control Room `/admin`, and ask for a 2-minute before/after eyeball to confirm the Deep Midnight muted read is preserved (not washed-out). Wait for approval or a lightness nudge before treating the retune as final. If the operator requests a nudge (e.g. 68% instead of 70%), apply it to `frontend/src/index.css` and re-present.
  </action>
  <what-built>The global dark `--muted-foreground-dim` token lifted to AA (220 16% 70%) plus the app-wide opacity-offender sweep. Faint gray helper text is now brighter/AA-passing across chat, documents, Settings, and the Control Room, while the Deep Midnight "muted" read is intended to be preserved (the Phase 088 panel lift is the taste reference).</what-built>
  <how-to-verify>
    1. Ensure the dev app is running (operator starts uvicorn + `npm run dev`; app at http://localhost:5173/).
    2. Look at 2-3 representative pages for a ~2-minute before/after eyeball (D-07): the Chat page (message meta / helper text), the Documents page (document detail meta, status badges), and the Control Room `/admin` (audit rows, health signals, user roster meta).
    3. Confirm: previously-dim helper text is now clearly readable, AND the overall app still reads as quiet/muted Deep Midnight (not washed-out or too bright).
    4. If any value looks too bright or breaks the muted feel, name the surface and nudge the lightness (e.g. 68% instead of 70%) — otherwise approve.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the retuned muted text is AA-readable AND the Deep Midnight look is preserved on chat, documents, and Control Room.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator types "approved" (or requests a specific lightness nudge, which is applied and re-presented until approved).
    - The final `--muted-foreground-dim` value the operator approved is recorded in 155-02-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" to lock the retuned tokens, or describe which surface/value to nudge.</resume-signal>
  <done>Operator has approved the retuned token look (Deep Midnight preserved); the approved value is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design tokens → all consumers | A global CSS custom property change propagates to every surface that reads `--muted-foreground-dim` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-02-VIS | Tampering (visual regression) | `index.css` global token lift | mitigate | Mirror the operator-approved 088-05 panel value (8.42:1); D-07 operator eyeball is the blocking approval gate before the sweep is final (guards G-6 #4 "washed-out") |
| T-155-02-G5 | Tampering | `MessageItem.tsx` / `StreamsProvider.tsx` | mitigate | Confirmed zero opacity offenders in those files; the sweep MUST NOT touch them (D-13 RED LINE); `git diff --name-only` is the tripwire |
| T-155-02-FALSEGREEN | Repudiation | contrast SC claimed from vitest | mitigate | jsdom cannot compute contrast; the color-contrast bar is proven ONLY by the live Chrome scan (D-03) recorded in VALIDATION.md — never claimed from a green vitest run (G-6 #1) |
</threat_model>

<verification>
- `grep "muted-foreground-dim: 220 16% 70%" frontend/src/index.css` → present; `220 16% 45%` gone.
- `grep "muted-foreground: 220 16% 65%" frontend/src/index.css` → base token unchanged.
- `cd frontend && npm test` → full suite green (non-regression).
- `cd frontend && npx vite build` → exit 0; `npx tsc -b` → 30 baseline errors, 0 net-new (SEED-056/049).
- `git diff --name-only` → MessageItem.tsx / StreamsProvider.tsx NOT listed.
- Live (VALIDATION.md, D-03): Chrome DevTools color-contrast scan on the SEED-092 pages → 0 failing nodes (the authoritative bar).
- Operator D-07 checkpoint: approved.
</verification>

<success_criteria>
The muted-text contrast offender class is fixed at the token source and swept app-wide, the Deep Midnight look is operator-approved (D-07), and the color-contrast category is ready to prove zero on the live scan (D-03).
</success_criteria>

<output>
Create `.planning/phases/155-accessibility-sweep-wcag-aa/155-02-SUMMARY.md` when done. Record: the final token value, the residual opacity-offender count + the documented exempt list, whether a light-theme dim token exists, and the D-07 approval outcome.
</output>
