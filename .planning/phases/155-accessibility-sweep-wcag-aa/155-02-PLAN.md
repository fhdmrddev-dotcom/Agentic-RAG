---
phase: 155-accessibility-sweep-wcag-aa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/components/admin/AuditTab.tsx
  - frontend/src/components/admin/UsersAndAccess.tsx
  - frontend/src/components/admin/ModelRegistryTab.tsx
  - frontend/src/components/admin/HealthSignals.tsx
  - frontend/src/components/admin/RecentActionsCard.tsx
  - frontend/src/components/admin/FeatureVisibility.tsx
  - frontend/src/components/admin/ModelDiscoveryPanel.tsx
  - frontend/src/components/admin/LockedTab.tsx
  - frontend/src/components/admin/ControlRoomPage.tsx
  - frontend/src/components/admin/CapabilityGrid.tsx
  - frontend/src/components/admin/ActiveRunsSection.tsx
autonomous: false
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "D-04: the global dark `--muted-foreground-dim` token is lifted from 220 16% 45% (fails, ~3.6:1) to 220 16% 70% (~8:1 on bg), keeping the blue-gray muted read while passing WCAG 2.1 AA 1.4.3"
    - "D-04: the ADMIN-cluster opacity-modified muted-text offenders (11 files, 48 occurrences — the densest SEED-092 cluster) on MEANINGFUL text are swept onto full-opacity AA-passing tokens; only truly decorative/disabled elements stay exempt (WCAG-allowed); the remaining clusters are swept by plan 155-07"
    - "D-04: the base `--muted-foreground` token (already ~7.7:1) is NOT rewritten — only the DIM token and the opacity modifiers are changed"
    - "D-07: the operator personally eyeballs the retuned tokens on chat, documents, and Control Room (before/after) and approves that the Deep Midnight look is preserved — then the remaining sweep (155-07) proceeds, exactly per D-07's 'approve, then the sweep proceeds'"
    - "D-03: the color-contrast category reaches zero failing nodes app-wide on the SEED-092 baseline pages, provable only by the live Chrome DevTools scan (jsdom cannot compute contrast)"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "AA-passing global --muted-foreground-dim token + extended contrast-math documentation"
      contains: "--muted-foreground-dim: 220 16% 70%"
  key_links:
    - from: "frontend/src/components/admin/*.tsx"
      to: "frontend/src/index.css tokens"
      via: "className text-muted-foreground / text-muted-foreground-dim (no opacity modifier on meaningful text)"
      pattern: "text-muted-foreground"
---

<objective>
Fix the #1 pre-existing offender class — low-contrast muted text — at the SOURCE (D-04), starting with the token and the densest cluster. Lift the global dark `--muted-foreground-dim` token to an AA-passing value (mirroring the already-shipped Phase 088-05 panel token), then sweep the ADMIN cluster's opacity-modified muted-text classNames (`text-muted-foreground/{40,50,60,70}` — 11 files / 48 occurrences, the densest SEED-092 cluster). A mid-execution operator eyeball (D-07) confirms the Deep Midnight look survives; then the remaining clusters proceed in plan 155-07 (same wave, disjoint files).

Purpose: One decision, app-wide — no page can regress by using the old dim token because the token itself becomes safe. The opacity modifier is the real killer (65% L @ 0.6 alpha ≈ 3.4:1). This half is contrast-only; because jsdom cannot compute color-contrast, the actual pass/fail bar is the live scan (D-03), captured in VALIDATION.md.
Output: A retuned index.css token block + a clean admin-cluster sweep + operator sign-off on the look.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-CONTEXT.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md

<interfaces>
<!-- index.css token facts (verified this session) — the executor edits ONE token + documents the math. -->
Dark theme (.dark), against --background 216 45% 4% (#060a0f):
  --muted-foreground:      220 16% 65% (#97a1b4)  → ~7.7:1  PASSES  → DO NOT CHANGE (index.css L94)
  --muted-foreground-dim:  220 16% 45% (#606d85)  → ~3.6:1  FAILS   → LIFT to 220 16% 70% (index.css L107-109)
Proven precedent already in-file (Phase 088-05, L123-132):
  --panel-muted-foreground-dim: 220 16% 70% (#a6aebf) → 8.42:1  ← the D-04 target template
Global focus floor + reduced-motion blocks already ship (L177 / L375 / L504 / L757) — do NOT add new ones.

<!-- Cluster split (scope-sanity fix): total offender scope = 133 occurrences / 42 files, split across two disjoint wave-1 plans. -->
THIS plan (cluster A — admin/, 11 files, 48 occurrences):
  AuditTab(13) UsersAndAccess(8) ModelRegistryTab(6) HealthSignals(6) RecentActionsCard(3) FeatureVisibility(3)
  ModelDiscoveryPanel(2) LockedTab(2) ControlRoomPage(2) CapabilityGrid(2) ActiveRunsSection(1)
Plan 155-07 (clusters B — chat/studio/classification/settings/ingestion/layout/pages/workflows/panel, 31 files, 85 occurrences).
Confirmed: MessageItem.tsx and StreamsProvider.tsx have ZERO opacity offenders → the G-5 red line is not crossed by either sweep.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift the global dark --muted-foreground-dim token to AA and extend the contrast-math doc</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (L88-140 — the .dark token block, the Phase 088-05 documented panel-token math at L123-132, and the offender token at L107-109)
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md ("Contrast Token Retune (D-04) — computed values")
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md ("frontend/src/index.css (config, design tokens — D-04)")
  </read_first>
  <action>
    In `frontend/src/index.css`, change the dark-theme global `--muted-foreground-dim` from `220 16% 45%` to `220 16% 70%` (same H/S as the shipped `--panel-muted-foreground-dim`; ~8:1 on the dark background, still reads muted). Extend the EXISTING Phase 087 dim-label comment above the token with the AA math: note the old 45% L = ~3.6:1 FAILED and the new 70% L mirrors the 088-05 panel token (8.42:1) — this satisfies D-04's "extend it". Do NOT touch the base `--muted-foreground` (L94, already ~7.7:1) — rewriting it would wash out the whole app (G-6 #4). Do NOT add new focus-visible or prefers-reduced-motion rules (they already ship at L177/L375/L504/L757). Light theme: the global `--muted-foreground-dim` only exists in the dark block; if a light `--muted-foreground-dim` is absent, leave light as-is (the opacity sweeps fix light too) and note it in the SUMMARY for the live scan.
  </action>
  <verify>
    <automated>cd frontend && grep -q "muted-foreground-dim: 220 16% 70%" src/index.css && ! grep -q "muted-foreground-dim: 220 16% 45%" src/index.css && npx vite build >/dev/null 2>&1 && echo TOKEN_LIFTED_BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "muted-foreground-dim: 220 16% 70%" frontend/src/index.css` → present.
    - `grep "muted-foreground-dim: 220 16% 45%" frontend/src/index.css` → returns nothing.
    - `grep "muted-foreground: 220 16% 65%" frontend/src/index.css` → still present (base token unchanged).
    - The token comment mentions the new AA ratio (~8:1) and the 088-05 precedent.
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <done>Dark `--muted-foreground-dim` = 220 16% 70% with documented AA math; base token unchanged; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the ADMIN-cluster opacity offenders (11 files, 48 occurrences) onto real tokens</name>
  <files>frontend/src/components/admin/AuditTab.tsx, frontend/src/components/admin/UsersAndAccess.tsx, frontend/src/components/admin/ModelRegistryTab.tsx, frontend/src/components/admin/HealthSignals.tsx, frontend/src/components/admin/RecentActionsCard.tsx, frontend/src/components/admin/FeatureVisibility.tsx, frontend/src/components/admin/ModelDiscoveryPanel.tsx, frontend/src/components/admin/LockedTab.tsx, frontend/src/components/admin/ControlRoomPage.tsx, frontend/src/components/admin/CapabilityGrid.tsx, frontend/src/components/admin/ActiveRunsSection.tsx</files>
  <read_first>
    - frontend/src/index.css (L671 citation-CSS note — the in-file authority: "meaningful muted text uses --muted-foreground, never the --muted-foreground-dim ~3.6:1 trap")
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md ("Opacity sweep" section — the transform rule + highest-density files)
    - The 11 admin files as you edit them (use the grep below to jump straight to the offending lines per file)
  </read_first>
  <action>
    Enumerate this cluster's offenders: `cd frontend && grep -rnE 'text-muted-foreground/(40|50|60|70)' src/components/admin` (expect ~48 hits across 11 files). Apply this decision rule per occurrence:
    (a) MEANINGFUL text (labels, values, meta a user reads — the common case) → drop the opacity modifier, use `text-muted-foreground` (full opacity, already ~7.7:1); for the very quietest meta that must stay a step dimmer, use `text-muted-foreground-dim` (now lifted to AA in Task 1).
    (b) TRULY DECORATIVE or DISABLED elements (a decorative graphic icon, a disabled-state control, placeholder-only affordance) → leave the opacity as-is (WCAG-allowed exemption). When you exempt one, note WHY in the SUMMARY so the live scan (D-03) and the D-06 remainder list can see it.
    Work file-by-file using the grep line numbers; edit only the className, never surrounding logic. Files OUTSIDE `src/components/admin/` are NOT this plan's scope — plan 155-07 sweeps the remaining clusters (chat/studio/classification/settings/ingestion/layout/pages/workflows/panel). Do NOT introduce any NEW `text-muted-foreground-dim` on text that needs full contrast. After the sweep, re-run the admin-scoped grep and record the residual count + the exempt list in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/__tests__ >/dev/null 2>&1 && npx vite build >/dev/null 2>&1 && echo "admin residuals:" && (grep -rcE 'text-muted-foreground/(40|50|60|70)' src/components/admin | grep -vE ':0$' | wc -l) && echo ADMIN_SWEEP_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` shows ONLY `frontend/src/index.css` + files under `frontend/src/components/admin/` (no chat/, no MessageItem.tsx, no StreamsProvider.tsx).
    - Every removed offender was a className-only edit (no logic/JSX-structure lines changed — verify via `git diff`).
    - Residual `text-muted-foreground/{40,50,60,70}` occurrences under `src/components/admin/` are ALL documented as decorative/disabled exemptions in 155-02-SUMMARY.md.
    - `cd frontend && npx vitest run src/components/admin/__tests__` → green (targeted non-regression; full `npm test` runs at the wave boundary per 155-VALIDATION.md Sampling Rate).
    - `cd frontend && npx vite build` exits 0; `npx tsc -b` → 30 baseline errors, 0 net-new.
  </acceptance_criteria>
  <done>Admin-cluster meaningful-text opacity offenders swept onto full-opacity tokens; residuals are documented exemptions; targeted admin suites + build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-07 operator eyeball on the retuned tokens (chat / documents / Control Room)</name>
  <files>N/A — verification checkpoint (no files modified)</files>
  <action>
    Claude has already lifted the global dim token (Task 1 — a GLOBAL change visible on every page including chat and documents) and swept the admin cluster (Task 2). Present the running app to the operator for the D-07 look check: point them at Chat, Documents, and the Control Room `/admin`, and ask for a 2-minute before/after eyeball to confirm the Deep Midnight muted read is preserved (not washed-out). Wait for approval or a lightness nudge before treating the retune as final. If the operator requests a nudge (e.g. 68% instead of 70%), apply it to `frontend/src/index.css` and re-present. After approval, the remaining opacity clusters proceed in plan 155-07 (D-07: "approve or nudge the values, then the sweep proceeds").
  </action>
  <what-built>The global dark `--muted-foreground-dim` token lifted to AA (220 16% 70% — visible app-wide, since every page reads the token) plus the admin-cluster opacity sweep. Faint gray helper text is now brighter/AA-passing, most densely across the Control Room, while the Deep Midnight "muted" read is intended to be preserved (the Phase 088 panel lift is the taste reference).</what-built>
  <how-to-verify>
    1. Ensure the dev app is running (operator starts uvicorn + `npm run dev`; app at http://localhost:5173/).
    2. Look at 2-3 representative pages for a ~2-minute before/after eyeball (D-07): the Chat page (message meta / helper text — token-lift effect), the Documents page (document detail meta — token-lift effect), and the Control Room `/admin` (audit rows, health signals, user roster meta — token lift + full cluster sweep).
    3. Confirm: previously-dim helper text is now clearly readable, AND the overall app still reads as quiet/muted Deep Midnight (not washed-out or too bright).
    4. If any value looks too bright or breaks the muted feel, name the surface and nudge the lightness (e.g. 68% instead of 70%) — otherwise approve. The remaining clusters (chat/studio/settings/ingestion/layout — plan 155-07) sweep onto these SAME approved tokens.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the retuned muted text is AA-readable AND the Deep Midnight look is preserved on chat, documents, and Control Room.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator types "approved" (or requests a specific lightness nudge, which is applied and re-presented until approved).
    - The final `--muted-foreground-dim` value the operator approved is recorded in 155-02-SUMMARY.md (plan 155-07 sweeps onto the approved value).
  </acceptance_criteria>
  <resume-signal>Type "approved" to lock the retuned tokens, or describe which surface/value to nudge.</resume-signal>
  <done>Operator has approved the retuned token look (Deep Midnight preserved); the approved value is recorded for the 155-07 sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design tokens → all consumers | A global CSS custom property change propagates to every surface that reads `--muted-foreground-dim` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-02-VIS | Tampering (visual regression) | `index.css` global token lift | mitigate | Mirror the operator-approved 088-05 panel value (8.42:1); D-07 operator eyeball is the blocking approval gate before the remaining sweep (155-07) proceeds (guards G-6 #4 "washed-out") |
| T-155-02-G5 | Tampering | `MessageItem.tsx` / `StreamsProvider.tsx` | mitigate | This plan touches ONLY index.css + admin/ files; `git diff --name-only` is the tripwire (D-13 RED LINE) |
| T-155-02-FALSEGREEN | Repudiation | contrast SC claimed from vitest | mitigate | jsdom cannot compute contrast; the color-contrast bar is proven ONLY by the live Chrome scan (D-03) recorded in VALIDATION.md — never claimed from a green vitest run (G-6 #1) |
</threat_model>

<verification>
- `grep "muted-foreground-dim: 220 16% 70%" frontend/src/index.css` → present; `220 16% 45%` gone.
- `grep "muted-foreground: 220 16% 65%" frontend/src/index.css` → base token unchanged.
- `cd frontend && npx vitest run src/components/admin/__tests__` → green (full `npm test` at the wave boundary per 155-VALIDATION.md).
- `cd frontend && npx vite build` → exit 0; `npx tsc -b` → 30 baseline errors, 0 net-new (SEED-056/049).
- `git diff --name-only` → only index.css + admin/ files; MessageItem.tsx / StreamsProvider.tsx NOT listed.
- Live (VALIDATION.md, D-03): Chrome DevTools color-contrast scan on the SEED-092 pages → 0 failing nodes (the authoritative bar, after 155-07 completes the remaining clusters).
- Operator D-07 checkpoint: approved.
</verification>

<success_criteria>
The muted-text contrast offender class is fixed at the token source, the densest (admin) cluster is swept, and the Deep Midnight look is operator-approved (D-07) — clearing the way for plan 155-07 to sweep the remaining clusters onto the approved tokens.
</success_criteria>

<output>
Create `.planning/phases/155-accessibility-sweep-wcag-aa/155-02-SUMMARY.md` when done. Record: the final approved token value, the admin-cluster residual count + the documented exempt list, whether a light-theme dim token exists, and the D-07 approval outcome.
</output>
