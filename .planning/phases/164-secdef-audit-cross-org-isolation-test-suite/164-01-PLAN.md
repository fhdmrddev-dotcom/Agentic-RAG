---
phase: 164-secdef-audit-cross-org-isolation-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/test_v3_4_org_isolation.py
autonomous: true
requirements: [TEN-05, TEN-03, PRAG-01]
must_haves:
  truths:
    - "D-164-06: test_v3_4_org_isolation.py exists as the milestone exit gate — data-driven matrix extending the 163 two_orgs_two_users fixture + _rls_harness (open_user_conn / assert_auth_uid / as_user_supabase_txn)"
    - "D-164-06: the red-anchor test_definer_ignores_spoofed_match_user_id FAILS (RED) against the pre-164 DB — the fixture seeds user A a real document_chunk so the spoofed match_user_id leaks A's row pre-164 (non-vacuous)"
    - "TEN-05: every user-facing table (driven from information_schema) asserts user B reads 0 of user A's rows across BOTH DB paths (asyncpg open_user_conn + supabase-py as_user_supabase_txn), fail-loud-preflighted + positive-controlled"
    - "D-164-01: all four DEFINER functions (match_document_chunks / keyword_search_chunks / match_skills / folder_is_globally_visible) return 0 cross-org rows when called as a non-member (fail-closed on absent auth.uid())"
    - "D-164-06: a spoofed X-Org-Id header does not widen access — org derives from membership via current_user_org_ids(), never the header"
    - "PRAG-01: a two-user live retrieval leg proves user B never receives user A's private chunks and receives shared-folder chunks only per folder-ACL"
  artifacts:
    - path: "backend/tests/integration/test_v3_4_org_isolation.py"
      provides: "two-org adversarial exit-gate matrix + pg_proc audit helper + red-anchor + PRAG-01 leg"
      contains: "test_definer_ignores_spoofed_match_user_id"
  key_links:
    - from: "backend/tests/integration/test_v3_4_org_isolation.py"
      to: "backend/tests/integration/_rls_harness.py"
      via: "import open_user_conn / assert_auth_uid / as_user_supabase_txn / requires_pg"
      pattern: "from tests.integration._rls_harness import"
    - from: "backend/tests/integration/test_v3_4_org_isolation.py"
      to: "public.match_document_chunks"
      via: "asyncpg conn.fetch over open_user_conn"
      pattern: "match_document_chunks"
---

<objective>
Author `test_v3_4_org_isolation.py` — the two-org adversarial exit-gate suite that is the v3.4 milestone's verifiable isolation gate (TEN-05). It extends the shipped Phase-163 fixtures + `_rls_harness` into an exhaustive, data-driven matrix over every user-facing table, both DB access paths, all four `SECURITY DEFINER` functions, `X-Org-Id` header-spoof rejection, and a PRAG-01 live-retrieval leg.

This plan is the **Wave-0 scaffold** (mirrors the 163 leak-test-first sequencing): it is authored FIRST, against the **pre-164 DB**, so the red-anchor (`test_definer_ignores_spoofed_match_user_id`) is provably **RED** before migration 110 applies (Plan 03) and the client-swap lands (Plan 04). Downstream plans turn it GREEN.

Purpose: Prove "zero cross-org leakage" is a measured property, not a claim (D-164-06). The red-then-green anchor proves the gate tests the NEW behavior, not a tautology.
Output: `backend/tests/integration/test_v3_4_org_isolation.py` (imports resolve; red-anchor RED against pre-164; positive controls behave).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-CONTEXT.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-RESEARCH.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-PATTERNS.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-VALIDATION.md

<interfaces>
<!-- Harness + fixtures the suite imports directly — no codebase exploration needed. -->

From backend/tests/integration/_rls_harness.py (import verbatim, do NOT re-derive):
- `requires_pg` — pytest skipif marker (local Postgres :54322 reachable)
- `open_user_conn(pool, uid)` — async ctx manager: acquire + txn + SET LOCAL ROLE authenticated + both-GUC-forms; COMMIT auto-reverts
- `as_user_asyncpg(pool, uid, sql, *args)` / `as_user_fetchval(...)` — run one SELECT under RLS as uid
- `as_user_supabase_txn(pool, uid)` — yields a SupabaseTxnAdapter (supabase-py fluent surface over the asyncpg RLS txn) — the supabase-py leak-path leg
- `assert_auth_uid(conn, expected_uid)` — FAIL-LOUD preflight; MUST be the first assertion on every user conn (a NULL auth.uid() false-passes isolation at 0 rows)

From backend/tests/integration/conftest.py (fixtures already in scope for tests/integration):
- `pg_pool` — dedicated asyncpg pool (jsonb codec registered) @ :54322
- `two_orgs_two_users` — {"a":{uid,org_id,doc_id,folder_id,thread_id,skill_id}, "b":{...}} two disjoint orgs, one owned row per table (documents/folders/threads/skills). DO NOT MODIFY this shared fixture (163 leak tests count its rows exactly).
- `auth_uid_variant` — records which GUC form THIS DB's auth.uid() reads

From backend/tests/integration/test_163_leak_asyncpg.py (clone the shape):
- `_table_exists(pool, table)` — information_schema probe (drive the every-table matrix)
- `_assert_isolated(pool, viewer, other)` — (2) fail-loud preflight → (3) positive control (own row == 1) → (4) isolation (0 of other's row) per table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pg_proc DEFINER audit helper + the red-anchor (spoofed match_user_id) + a non-vacuous chunk fixture</name>
  <files>backend/tests/integration/test_v3_4_org_isolation.py</files>
  <read_first>
    - backend/tests/integration/test_163_leak_asyncpg.py:1-120 (module docstring "RED now → GREEN after migration" state contract; `_table_exists`; `_assert_isolated` shape; `assert_auth_uid`-first invariant)
    - backend/tests/integration/_rls_harness.py:78-141 (open_user_conn / as_user_asyncpg / assert_auth_uid)
    - backend/tests/integration/conftest.py:97-197 (two_orgs_two_users seed + FK-safe teardown — the shape a new fixture must mirror)
    - 164-PATTERNS.md §test_v3_4_org_isolation.py (the red-then-green anchor code + the pg_proc-introspection helper) + §Shared Pattern B
    - 164-RESEARCH.md §Validation Architecture (Test→Req map) + Code Example "The red-then-green anchor"
  </read_first>
  <action>
    Create `test_v3_4_org_isolation.py` with `pytestmark = requires_pg` and `@pytest.mark.asyncio`. Import the harness helpers + `_table_exists` shape (copy the information_schema probe locally, matching test_163_leak_asyncpg.py). Mirror the 163 module-docstring STATE CONTRACT: RED now → GREEN after Plan 03 (migration 110) + Plan 04 (client-swap); the authoring bar HERE is imports-resolve + red-anchor demonstrably RED.

    Add a NEW fixture `two_orgs_chunks_and_shared_folder(pg_pool, two_orgs_two_users)` that EXTENDS (never mutates) the shared fixture: for each user seed exactly one `public.document_chunks` row referencing that user's seeded `doc_id` with a FIXED unit embedding literal (dimension = the live `document_chunks.embedding` column dim — confirm via a `SELECT ... information_schema`/`\d` probe at author time; text-embedding-3-small = 1536) and `org_id` auto-filled by the mig-106 trigger (or set explicitly from the user's `org_id`). Non-vacuity is load-bearing: without A owning a real chunk the red-anchor would false-green at 0 rows. FK-safe teardown deletes the chunks before the parent fixture tears down.

    Add the pg_proc audit helper `_assert_definer_pinned(pool, fn_name)`: fetch `prosecdef, proconfig FROM pg_proc WHERE proname=$1 AND pronamespace='public'::regnamespace`; assert `prosecdef is True` AND `any(c.startswith("search_path=") for c in (proconfig or []))`. Parametrize over the four function names. (This leg is RED pre-164 for the two un-pinned fns — match_document_chunks, keyword_search_chunks — proving it audits real state.)

    Add the RED-ANCHOR `test_definer_ignores_spoofed_match_user_id(pg_pool, two_orgs_chunks_and_shared_folder)`: as user B (`open_user_conn(pg_pool, b["uid"])`), `assert_auth_uid` FIRST, then `conn.fetch("SELECT id FROM public.match_document_chunks($1::public.vector, $2, 50, 0.0)", <fixed unit vector literal matching the seed embedding>, a["uid"])` — spoofed `match_user_id = A`. Assert `len(rows) == 0` with message "cross-org leak: DEFINER trusted the spoofed match_user_id". Per D-164-06 this MUST fail against the pre-164 body (which filters `dc.user_id = match_user_id` → returns A's chunk) and pass post-164 (org gate keys on auth.uid()=B).
  </action>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py --collect-only -q` exits 0 (all imports resolve; fixtures discovered).
    - `venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py::test_definer_ignores_spoofed_match_user_id -x` FAILS against the pre-164 DB with the "cross-org leak" assertion message (RED proof captured in the run log). If it PASSES pre-164, the chunk fixture is vacuous — fix the seed (A must own a chunk the spoof returns).
    - `_assert_definer_pinned` parametrized cases exist for all four function names; the two un-pinned fns (match_document_chunks, keyword_search_chunks) are RED pre-164.
    - `assert_auth_uid` is the FIRST assertion on every user connection in the file (grep: no `conn.fetch`/`conn.fetchval` isolation assertion precedes an `assert_auth_uid` on the same conn).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py --collect-only -q</automated>
  </verify>
  <done>Suite file collects; red-anchor demonstrably RED against pre-164; chunk fixture non-vacuous; pg_proc helper present.</done>
</task>

<task type="auto">
  <name>Task 2: Every-user-facing-table matrix (both DB paths) + all-four-DEFINER-fns 0-cross-org legs + X-Org-Id spoof rejection</name>
  <files>backend/tests/integration/test_v3_4_org_isolation.py</files>
  <read_first>
    - backend/tests/integration/test_163_leak_asyncpg.py:42-190 (`_SEEDED_TABLES`/`_UNSEEDED_TABLES`, `_assert_isolated`, write-leak + unseeded-table extension)
    - backend/tests/integration/test_163_leak_supabase.py (the supabase-py path leg via as_user_supabase_txn — clone its adapter usage)
    - backend/tests/integration/_rls_harness.py:108-141 (as_user_supabase_txn + assert_auth_uid)
    - 164-RESEARCH.md §Architecture Pattern 5 (matrix structure) + Open Question 3 (X-Org-Id grep — expected unread in 164)
  </read_first>
  <action>
    Add `test_table_matrix_asyncpg` and `test_table_matrix_supabase` (the `k -k table_matrix` legs). Drive the table set from `information_schema.tables` (public schema, guarded by `_table_exists`), asserting for every user-facing table user B reads 0 of user A's rows, with the fail-loud preflight + positive control on the fixture-seeded tables (documents/folders/threads/skills) and owner-scoped `WHERE user_id = A.uid` count==0 on the rest. asyncpg leg via `open_user_conn`; supabase-py leg via `as_user_supabase_txn` (SupabaseTxnAdapter) — mirroring the two 163 leak files so both paths are proven.

    Add `test_definer_zero_cross_org[fn]` parametrized over the four DEFINER functions: as user B, call each with parameters that would surface user A's rows (spoofed `match_user_id=A` for the three retrieval fns; `folder_is_globally_visible(A's folder_id)` for the fourth) and assert 0 A-owned rows. Add the supabase-path variant where the function is reachable via `.rpc(...)` through the adapter.

    Add `test_org_header_spoof_does_not_widen` (the `-k org_header_spoof` leg): grep the codebase for `X-Org-Id`/`x_org_id` at author time (RESEARCH Open Q3 — expected unread in 164). As user B, issue the same B-scoped read/RPC WITH the header set to A's org (via the API client or by asserting the header is not consulted at the DB seam); assert access is unchanged (0 A-rows) — org derives from `current_user_org_ids()`, never the header. If no path reads the header, assert "header present ≠ access change" explicitly and document the 166 hand-off in a comment.
  </action>
  <acceptance_criteria>
    - `venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "table_matrix or definer or org_header_spoof" --collect-only -q` exits 0.
    - The table matrix is driven from `information_schema` (grep: `information_schema.tables` present), NOT a hardcoded ~4-table list — it must cover every user-facing table.
    - Both `test_table_matrix_asyncpg` (open_user_conn) and `test_table_matrix_supabase` (as_user_supabase_txn) exist — both DB paths.
    - `test_definer_zero_cross_org` is parametrized over all four function names.
    - Pre-164: the definer/matrix legs are RED (migration not applied) — consistent with the STATE CONTRACT; the file still COLLECTS clean.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "table_matrix or definer or org_header_spoof" --collect-only -q</automated>
  </verify>
  <done>Table matrix (both paths), four-DEFINER legs, and X-Org-Id spoof rejection authored and collecting.</done>
</task>

<task type="auto">
  <name>Task 3: PRAG-01 live-retrieval isolation leg + platform-universal / over-widening guards</name>
  <files>backend/tests/integration/test_v3_4_org_isolation.py</files>
  <read_first>
    - backend/tests/integration/test_163_rls_platform_universal.py (is_system universal @ :104-121; user is_global stays org-scoped @ :148-217; badge-spoof block @ :222-267 — clone these against the 4 DEFINER fns)
    - 164-RESEARCH.md §Architecture Pattern 5 (PRAG-01 leg) + §Pitfall 5 (is_system OUTSIDE the org gate)
    - 164-PATTERNS.md §match_skills org-gate transform (is_system escape shape from mig 109:70-73)
  </read_first>
  <action>
    Extend the `two_orgs_chunks_and_shared_folder` fixture from Task 1 to seed a SHARED-FOLDER document for user A: an `is_global` folder owned by A + a document in it + one `document_chunks` row (fixed embedding, org_id). Add `test_prag01_retrieval_isolation` (the `-k prag01_retrieval` leg): as user B call `match_document_chunks` (over `open_user_conn`) with a query vector matching the seeds; assert B never receives A's PRIVATE chunk and receives A's SHARED-folder chunk ONLY per the folder-ACL predicate (`folder_is_globally_visible`) — proving org- AND folder-ACL scoping. Post-164 this is the live PRAG-01 proof; pre-164 it is RED.

    Add `test_is_system_stays_universal` and `test_user_is_global_stays_org_scoped` cloning test_163_rls_platform_universal against `match_skills`: an `is_system=true` skill (the built-in skill-creator shape) must remain visible to a non-owner in a disjoint org (mirrors mig 109 FIX-A — the Test-7 regression guard), while a user `is_global` skill must STAY org-scoped (0 for a disjoint-org reader). Add a `test_badge_spoof_blocked` note that an authenticated user cannot self-set `is_system=true` (mig 109 WITH-CHECK) — assert via the write path if reachable at the DB seam.
  </action>
  <acceptance_criteria>
    - `venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "prag01_retrieval or is_system or is_global" --collect-only -q` exits 0.
    - The fixture seeds A a PRIVATE chunk AND a SHARED-folder chunk (grep: an `is_global` folder insert + two chunk inserts for A) so the PRAG-01 leg can distinguish private-vs-shared.
    - `test_is_system_stays_universal` asserts a non-owner in a disjoint org SEES the is_system skill (guards the mig-109 FIX-A / Test-7 regression); `test_user_is_global_stays_org_scoped` asserts a disjoint-org reader sees 0 of a user `is_global` skill.
    - Full-file collect: `venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py --collect-only -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py --collect-only -q</automated>
  </verify>
  <done>PRAG-01 private-vs-shared retrieval leg + platform-universal/over-widening guards authored; whole file collects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated caller (user B) → DB (asyncpg user-context / supabase-py JWT) | untrusted `match_user_id` param + `X-Org-Id` header cross here; the suite is the adversary that proves they cannot widen access |

## STRIDE Threat Register (this plan = the PROOF instrument for the phase register)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-EoP-01 | Elevation | DEFINER bodies trusting spoofed `match_user_id` | mitigate | Red-anchor `test_definer_ignores_spoofed_match_user_id` is the RED-then-GREEN proof (mitigation authored in Plan 03) |
| T-164-Spoof-07 | Spoofing/EoP | `X-Org-Id` header | mitigate | `test_org_header_spoof_does_not_widen` asserts the header is inert (org from membership) |
| T-164-Avail-06 | Availability | `is_system` platform content trapped in org gate | mitigate | `test_is_system_stays_universal` guards the mig-109 FIX-A / Test-7 regression |
| T-164-SC | Tampering | npm/pip/cargo installs | accept (N/A) | No package installs this phase (RESEARCH §Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py --collect-only -q` exits 0 (imports/fixtures resolve).
- Red-anchor RED against pre-164 (`::test_definer_ignores_spoofed_match_user_id` FAILS with the leak message) — the RED-then-GREEN proof.
- No regression on the crux: `venv/Scripts/python.exe -m pytest tests/integration -k "163" -q` unchanged from HEAD.
</verification>

<success_criteria>
- `test_v3_4_org_isolation.py` exists as the named milestone exit gate, extending `_rls_harness` + `two_orgs_two_users` (D-164-06).
- The exhaustive matrix is authored: every user-facing table (information_schema-driven) × both DB paths × all four DEFINER fns × X-Org-Id spoof × PRAG-01 private-vs-shared retrieval.
- The red-anchor is provably RED against the pre-164 DB (non-vacuous chunk fixture); the file collects clean.
</success_criteria>

<output>
Create `.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-01-SUMMARY.md` when done.
</output>
