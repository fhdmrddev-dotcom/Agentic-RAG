---
phase: 164-secdef-audit-cross-org-isolation-test-suite
plan: 04
type: execute
wave: 3
depends_on: ["164-03", "164-01"]
files_modified:
  - backend/app/services/retrieval_service.py
  - backend/app/services/agent_loop.py
  - backend/app/services/tool_dispatcher.py
  - backend/app/services/sql_service.py
  - backend/app/api/kb.py
autonomous: true
requirements: [TEN-03, PRAG-01]
must_haves:
  truths:
    - "D-164-02: the producer's match_document_chunks / keyword_search_chunks / match_skills calls run over the Phase-163 asyncpg get_user_pg_connection user-context (uid-synthesized request.jwt.claims, no token → no mid-run expiry) — NOT over ctx.supabase service-role and NOT over a captured request JWT"
    - "D-164-02: the retrieval RPCs format the embedding as a pgvector literal cast `$1::public.vector` (asyncpg registers no vector codec — only jsonb)"
    - "D-164-04: _inject_user_id (sql_service.py) and _inject_user_id_for_grep (kb.py) are DELETED; query_user_documents runs on the user-context so RLS (INVOKER) auto-scopes the text-to-SQL / grep path"
    - "D-164-04: _inject_folder_scope (feature-narrowing) and the SELECT-only + no-semicolon single-statement guards are RETAINED; query_user_documents is NOT made DEFINER"
    - "TEN-03 fail-closed: if a retrieval RPC is ever called on a service-role/owner connection, auth.uid() is NULL → current_user_org_ids() empty → 0 rows (over-restrict, never over-share)"
  artifacts:
    - path: "backend/app/services/retrieval_service.py"
      provides: "shared _call_as_user asyncpg helper + vector/keyword RPCs over the user-context"
      contains: "get_user_pg_connection"
    - path: "backend/app/services/sql_service.py"
      provides: "query_documents with the regex deleted, RLS-scoped via the user-context"
    - path: "backend/app/api/kb.py"
      provides: "grep_path with _inject_user_id_for_grep deleted, RLS-scoped"
  key_links:
    - from: "backend/app/services/retrieval_service.py"
      to: "app.dependencies.get_user_pg_connection"
      via: "asyncpg user-context for the retrieval RPCs"
      pattern: "get_user_pg_connection|open_user_conn"
    - from: "backend/app/services/sql_service.py + backend/app/api/kb.py"
      to: "public.query_user_documents (INVOKER)"
      via: "run over the asyncpg user-context so RLS scopes the arbitrary SELECT"
      pattern: "query_user_documents"
---

<objective>
Land the Python half of the two-halves-that-must-land-together phase: move the PRODUCER's retrieval RPCs (`match_document_chunks`, `keyword_search_chunks`, `match_skills`) and its text-to-SQL / grep DB calls off the service-role `ctx.supabase` (where `auth.uid()` is NULL → the migration-110 org predicate returns 0 rows for everyone) and onto the Phase-163 asyncpg `get_user_pg_connection` user-context (uid-synthesized claims, no token, no mid-run expiry). Only then delete the two fragile security-injection regexes — RLS now owns cross-user isolation on the INVOKER `query_user_documents` path (D-164-02 / D-164-04).

Purpose: This is the connection-identity half that makes migration 110's in-body org predicate LIVE. The 163 red line stands — do NOT carry the request's user-JWT supabase client into the detached producer (it expires mid-run, `threads.py:747-749`); the uid-synthesized asyncpg context is the expiry-safe realization.
Output: retrieval + text-to-SQL/grep producer DB calls on the user-context; `_inject_user_id` + `_inject_user_id_for_grep` deleted; `_inject_folder_scope` + SELECT-only/no-`;` guards retained.

Depends on 164-03 (the org-scoped DEFINER bodies must be applied so the swapped calls resolve `auth.uid()` and scope correctly) and 164-01 (the exit-gate suite legs that verify this — text_to_sql, prag01_retrieval — go GREEN here).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-CONTEXT.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-RESEARCH.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-PATTERNS.md

<interfaces>
<!-- The user-context factory + the exact call sites to swap. -->

From backend/app/dependencies.py:125-175 (Shared Pattern A — the ONLY plumbing needed; already shipped in 163):
- `_apply_rls_user_context(conn, uid)` — SET LOCAL ROLE authenticated (turns RLS on) + both GUC forms, is_local=true
- `get_user_pg_connection(request, current_user)` — async ctx manager: pool.acquire + txn + _apply_rls_user_context + yield conn; claims from current_user["id"] ALONE (no token). Passing `request=None` is fine — it is only used for the router Depends contract.
- asyncpg pool registers ONLY a jsonb codec (dependencies.py:74) — NO vector codec (Pitfall 3).

Call sites to swap (currently on a passed-in service-role `supabase` client):
- retrieval_service.py:65 `_vector_search` → supabase.rpc("match_document_chunks", params); :87 `_keyword_search` → supabase.rpc("keyword_search_chunks", params); public entry `search_documents` @ :257 threads `supabase` in.
- agent_loop.py:1166 `supabase = ctx.supabase` (service-role in producer); :1306-1318 supabase.rpc("match_skills", {...}); ToolContext build @ :2484-2491 already carries `pool=await get_pg_pool()` + `current_user=current_user`.
- tool_dispatcher.py:682 `_handle_search_documents` → search_documents(..., ctx.supabase); :756 `_handle_query_documents` → query_documents(..., ctx.supabase); :235 `grep_path(..., ctx.supabase)`.
- sql_service.py:90 `query_documents(sql_query, user_id, supabase, folder_ids)` — DELETE `_inject_user_id` (:31-68) + its call (:106) + the `get_globally_visible_folder_ids` call (:105, only feeds the regex); KEEP `_inject_folder_scope` (:71-87) + the SELECT-only/no-`;` guards (:99-102).
- kb.py:241 `grep_path(pattern, path, user_id, supabase)` — DELETE `_inject_user_id_for_grep` (:233-238) + its wrap (:263); route the query_user_documents RPC over the user-context.

Anti-pattern (163 red line): NEVER pass the request user-JWT supabase client into the producer (expires mid-run). NEVER leave ctx.supabase service-role AND add the org predicate (breaks retrieval → 0 rows for everyone).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared _call_as_user asyncpg helper + swap the vector/keyword retrieval RPCs onto the user-context</name>
  <files>backend/app/services/retrieval_service.py</files>
  <read_first>
    - backend/app/services/retrieval_service.py:25-88 (_vector_search / _keyword_search — the two supabase.rpc sites + the params dicts) + :257-298 (search_documents entry threading `supabase`) + :216-256 (fetch_full_document direct-read fallback)
    - backend/app/dependencies.py:125-175 (get_user_pg_connection / _apply_rls_user_context) + :74 (jsonb-only codec)
    - 164-PATTERNS.md §retrieval_service.py + §Shared Pattern A + 164-RESEARCH.md §Architecture Pattern 2 (the vector-literal wrinkle) + §Pitfall 3
  </read_first>
  <action>
    Add a shared helper `_call_as_user(user_id, fn_sql, *args)` in retrieval_service.py that opens `get_user_pg_connection(None, {"id": user_id})` and `conn.fetch(fn_sql, *args)`, returning `[dict(r) for r in rows]`. Rewrite `_vector_search` and `_keyword_search` to call the DEFINER functions through it instead of `supabase.rpc(...)`. Format the embedding as a pgvector literal — `vec_literal = "[" + ",".join(repr(float(x)) for x in query_embedding) + "]"` — and pass it as `$1::public.vector` in the positional SELECT `FROM public.match_document_chunks($1::public.vector, $2, $3, $4, $5, $6, $7)` (map the existing params dict to positional args in the migration-073 signature order; keyword fn uses its own signature). Keep the `run_in_threadpool(embed_texts, …)` at :42-44 (blocking HTTP embedding call, orthogonal to the DB path). The `supabase` parameter stays on the function signatures for the non-RPC helpers (`_enrich_with_filenames`, direct reads) — thread `user_id` to `_call_as_user`, not the client.

    Confirm the `fetch_full_document` direct-read fallback (:216-256) behavior under the new model: it reads `document_chunks` directly; note in the SUMMARY whether it runs on the user-context (RLS-scoped by the mig-110 widened policy) or the service-role client — do NOT regress owner reads; if it must be user-scoped, route it through the user-context too. Fail-closed property must hold: a call with an absent auth.uid() returns 0 rows.
  </action>
  <acceptance_criteria>
    - `grep -c "supabase.rpc(\"match_document_chunks\"" backend/app/services/retrieval_service.py` == 0 and `...keyword_search_chunks...` == 0 (both swapped off supabase.rpc).
    - `_call_as_user` exists and calls `get_user_pg_connection` (grep: `get_user_pg_connection`); the embedding is passed as `$1::public.vector` with a `[...]` literal (grep: `::public.vector` and the join-repr literal builder).
    - `cd backend && venv/Scripts/python.exe -c "import app.services.retrieval_service"` exits 0.
    - `venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "prag01_retrieval" -x` PASSES (B's retrieval returns 0 of A's private chunks, shared per folder-ACL) — the retrieval half is now live over the user-context.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "prag01_retrieval" -x</automated>
  </verify>
  <done>Vector + keyword RPCs run over the asyncpg user-context with the vector-literal cast; PRAG-01 retrieval leg green; imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: Swap match_skills onto the same user-context seam + wire the search_documents tool ctx</name>
  <files>backend/app/services/agent_loop.py, backend/app/services/tool_dispatcher.py</files>
  <read_first>
    - backend/app/services/agent_loop.py:1160-1180 (supabase = ctx.supabase) + :1262-1318 (the match_skills RPC site + the id-SELECT) + :2484-2491 (ToolContext build carrying pool + current_user)
    - backend/app/services/tool_dispatcher.py:680-760 (_handle_search_documents / _handle_query_documents wiring ctx.supabase)
    - backend/app/services/retrieval_service.py (the `_call_as_user` seam added in Task 1)
    - 164-PATTERNS.md §agent_loop.py / tool_dispatcher.py assignment (ToolContext already resolves pool + current_user — the user-context is one get_user_pg_connection away; no new ToolContext field)
  </read_first>
  <action>
    Route the `match_skills` RPC (agent_loop.py:1306-1318) onto the asyncpg user-context via the same seam (reuse retrieval_service `_call_as_user`, or a sibling helper). Build the positional call `SELECT id, name, description, similarity FROM public.match_skills(...)` in the mig-091 signature order, formatting any embedding arg as the `$n::public.vector` literal (same wrinkle). Derive uid from `ctx.current_user["id"]` (ToolContext already carries `current_user` + `pool` at :2484-2491 — no new field needed). Ensure the `search_documents` handler (tool_dispatcher.py:682) reaches `search_documents` such that the retrieval RPCs self-acquire the user-context (the dispatcher may only need to stop relying on `ctx.supabase` for the RPC leg while keeping it for the non-RPC `_enrich_with_filenames` reads). Preserve the Phase-140 id-SELECT behavior on match_skills.

    Do NOT capture or thread the request JWT into the producer closure (163 red line). Keep the shared SSE/agent-loop path byte-compatible — this is a DB-connection swap at the retrieval seam, not a provider-path fork (D-14).
  </action>
  <acceptance_criteria>
    - `grep -c "supabase.rpc(\"match_skills\"" backend/app/services/agent_loop.py` == 0 (swapped off supabase.rpc); the match_skills call goes through the user-context seam (grep: `get_user_pg_connection` or the shared `_call_as_user`).
    - No new field added to ToolContext (grep the ToolContext definition — uid comes from the existing `current_user`); the request JWT is not captured into the producer (grep: no user-JWT supabase client constructed in the producer path).
    - `cd backend && venv/Scripts/python.exe -c "import app.services.agent_loop, app.services.tool_dispatcher"` exits 0.
    - `venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "definer" -x` PASSES (all four DEFINER legs incl. match_skills 0-cross-org).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "definer" -x</automated>
  </verify>
  <done>match_skills runs over the user-context (no request JWT captured, no new ctx field); DEFINER legs green; imports clean.</done>
</task>

<task type="auto">
  <name>Task 3: DELETE the two security-injection regexes; route text-to-SQL + grep through the user-context (RLS scopes them)</name>
  <files>backend/app/services/sql_service.py, backend/app/api/kb.py</files>
  <read_first>
    - backend/app/services/sql_service.py:15-128 (_detect_alias, _inject_user_id DELETE, _inject_folder_scope KEEP, query_documents flow + the SELECT-only/no-`;` guards @ :99-102)
    - backend/app/api/kb.py:233-269 (_inject_user_id_for_grep DELETE + grep_path)
    - backend/app/dependencies.py:125-175 (get_user_pg_connection) — the INVOKER query_user_documents must run over THIS so RLS auto-scopes the arbitrary SELECT
    - 164-PATTERNS.md §sql_service.py + kb.py + 164-RESEARCH.md §Pitfall 4 (deleting the regex before the connection is user-context = full leak) + Open Q1 (KEEP _inject_folder_scope)
  </read_first>
  <action>
    In `sql_service.query_documents`: DELETE `_inject_user_id` (:31-68) and its call (:106) and the `get_globally_visible_folder_ids(...)` call (:105 — it only fed the regex; audit for other callers before removing the import). KEEP `_inject_folder_scope` (:71-87 — feature-narrowing to a chosen folder subtree, NOT a cross-user gate) and its conditional call, and KEEP the SELECT-only + single-statement (no `;`) guards (:99-102). Route the `query_user_documents` RPC over the asyncpg user-context (`get_user_pg_connection(None, {"id": user_id})` → `conn.fetch("SELECT * FROM public.query_user_documents($1)", scoped)`) so the INVOKER function's dynamic EXECUTE runs as `authenticated` and RLS scopes every base-table read. Do NOT make `query_user_documents` DEFINER. Preserve the markdown/JSON result formatting.

    In `kb.grep_path`: DELETE `_inject_user_id_for_grep` (:233-238) and its wrap at the RPC call (:263); route the same `query_user_documents` RPC over the user-context (same seam). Keep the folder-subtree scoping (`folder_ids IN (...)`) and the pattern-escaping. The producer's `ctx.supabase` is service-role — Pitfall 4 is exactly this path, so the deletion is ONLY safe because the RPC now runs user-context.
  </action>
  <acceptance_criteria>
    - `grep -c "_inject_user_id" backend/app/services/sql_service.py` == 0 (def + all calls gone); `grep -c "_inject_user_id_for_grep" backend/app/api/kb.py` == 0.
    - `grep -c "_inject_folder_scope" backend/app/services/sql_service.py` ≥ 1 (RETAINED); the SELECT-only + no-`;` guards remain (grep: `startswith("select")` and `";" in`).
    - `query_user_documents` runs over `get_user_pg_connection` in both sql_service and kb.py (grep: `get_user_pg_connection` present in both); `query_user_documents` is NOT altered to DEFINER (no migration touched here).
    - `cd backend && venv/Scripts/python.exe -c "import app.services.sql_service, app.api.kb"` exits 0.
    - `venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "text_to_sql" -x` PASSES (text-to-SQL/grep cannot cross orgs); `venv/Scripts/python.exe -m pytest tests/integration -k "163" -q` shows no NEW failures vs HEAD (crux not regressed).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "text_to_sql" -x</automated>
  </verify>
  <done>Both security regexes deleted; text-to-SQL + grep run RLS-scoped over the user-context; _inject_folder_scope + SELECT-only/no-`;` guards retained; suite text_to_sql leg green; crux not regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detached producer (service-role by default) → retrieval RPCs / INVOKER query_user_documents | the identity seam: unless the DB call carries the caller's uid, org scoping is inert (0-for-everyone) or bypassed (BYPASSRLS full leak) |
| arbitrary text-to-SQL string → INVOKER query_user_documents EXECUTE | untrusted SELECT text; RLS under `authenticated` is the primary boundary (regex deleted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-ID-03 | Information Disclosure | INVOKER query_user_documents run on the producer's BYPASSRLS service-role connection after the regex is deleted | mitigate | route the text-to-SQL / grep DB path onto the asyncpg user-context (RLS enforced) in the SAME change; delete the regex only after; suite `text_to_sql` leg + 163 no-regress prove it |
| T-164-DoS-04 | Denial of Service (self) | token expiry mid-run silently degrading retrieval to 0 rows | mitigate | uid-synthesized asyncpg claims (no token) via get_user_pg_connection — never carry the request JWT into the producer (163 red line) |
| T-164-EoP-01 | Elevation | producer retrieval still on service-role → migration-110 org predicate returns 0 for everyone (broken app, fail-closed) | mitigate | move all three retrieval RPCs onto the user-context so auth.uid()=caller resolves; fail-closed property asserted by the suite |
| T-164-SC | Tampering | npm/pip/cargo installs | accept (N/A) | No package installs this phase (RESEARCH §Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -x` — the WHOLE exit-gate suite GREEN (definer, search_path, table_matrix both paths, org_header_spoof, text_to_sql, prag01_retrieval, is_system universal).
- Red-anchor RED-then-GREEN re-proven: revert `match_document_chunks` to its pre-164 body (function snapshot) → `::test_definer_ignores_spoofed_match_user_id` FAILS → restore (re-apply 110's function) → passes. (VALIDATION manual-only row elevated to an explicit phase-gate criterion.)
- `venv/Scripts/python.exe -m pytest tests/integration -k "163" -q` — crux not regressed.
- CONCUR-01 re-run <1s (test_058_concurrency.py).
- SC#10 4-axis live operator UAT (cross-provider × multi-tool × parallel-thread × long-message) is authored in 164-VALIDATION.md — reference it; run post-suite-green before /gsd:verify-work. Do NOT author SC#10 rows as PLAN tasks.
</verification>

<success_criteria>
- All three producer retrieval RPCs + the text-to-SQL/grep DB path run over the asyncpg user-context (D-164-02); the two security regexes are deleted with RLS as the replacement (D-164-04); _inject_folder_scope + SELECT-only/no-`;` guards retained.
- The full `test_v3_4_org_isolation.py` exit gate is GREEN and the red-anchor is proven RED-then-GREEN; the crux is not regressed; CONCUR-01 <1s.
</success_criteria>

<output>
Create `.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-04-SUMMARY.md` when done.
</output>
