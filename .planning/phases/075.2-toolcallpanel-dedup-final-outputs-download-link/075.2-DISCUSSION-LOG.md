# Phase 075.2: ToolCallPanel Dedup + Final Outputs Download Link - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-21
**Phase:** 075.2-toolcallpanel-dedup-final-outputs-download-link
**Areas discussed:** Dedup root-cause depth, WR-01/WR-02 pickup + Final Outputs UX, Acceptance bar for transient duplicate, Plan split + test strategy

---

## Dedup root-cause depth (BUG-260521-01)

### Q1: How deep should the fix for BUG-260521-01 go?

| Option | Description | Selected |
|--------|-------------|----------|
| Root cause + dedup hardening (Recommended) | Fix streaming reducer (update-in-place on id finalization, key by stable position/name when id changes) AND tighten dedup-key fallback per WR-03. Defense-in-depth. | ✓ |
| Root cause only | Fix reducer's id-finalization path; skip WR-03 (dead code if root cause is fixed anyway). | |
| Dedup key only (WR-03) | Cheapest visual patch; transient duplicate still exists in state but never renders. | |

**User's choice:** Root cause + dedup hardening.
**Notes:** Confirms the framing that "fixed" means the streaming reducer produces ONE entry from the first SSE event, with the dedup hardening shipping as defense for the next neighborhood edge case.

### Q2: Is WR-02 (two-probe getSnapshot race) in-scope as part of the root cause investigation?

| Option | Description | Selected |
|--------|-------------|----------|
| In scope (Recommended) | Bug report names WR-02 as a likely contributor. Thread snapshot through `_isTransientStreamEnd` so `_reattachAfterTransient` reuses it. | ✓ |
| Defer | Treat WR-02 as separate. Risk: dedup fix might look fine in UAT but silently re-trigger on poor network. | |

**User's choice:** In scope.
**Notes:** Lives in the same StreamsProvider reducer block, so folding it in keeps Plan 01 as a coherent reducer-quality pass.

---

## WR-01 pickup + Final Outputs UX

### Q3: Pick up WR-01 (onToolEnd id matching) in this phase?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — fold into same wave (Recommended) | Small change (thread `tool_call_id` through callback signature). Same reducer block being edited. Keeps WR-01 + WR-02 + root cause as one quality pass. | ✓ |
| Defer | Mitigated by `parallel_tool_calls: false` today; defer until parallel-tool support is re-enabled. | |

**User's choice:** Fold in.
**Notes:** Same neighborhood, near-zero marginal cost, removes a non-deterministic "done" stamp risk for future parallel-tool support.

### Q4: What shape should the Final Outputs download link take?

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse per-cell OutputFileCard verbatim (Recommended) | Stack of identical cards used in per-cell delta panels. Free icon + size badge + spinner + error states. Visual consistency. | ✓ |
| Compact text-link variant | Underline + filename + click. Minimal pinned-panel visual weight; less affordance. | |
| Extract shared component, use in both | Refactor OutputFileCard into a shared file consumed by per-cell AND pinned. Cleaner long-term, adds refactor scope. | |

**User's choice:** Reuse OutputFileCard verbatim.
**Notes:** Planner picks the exact reuse mechanism (export from ExecuteCodeBlock vs extract to shared file) based on import-graph / circular-dep inspection — both deliver visual consistency. Documented as D-075.2-06.

---

## Acceptance bar for transient duplicate

### Q5: What's the acceptance bar for BUG-260521-01?

| Option | Description | Selected |
|--------|-------------|----------|
| Zero duplicate, ever (Recommended) | First tool of a run renders exactly ONE card from the first SSE event. Verified via Chrome MCP screenshots at t=0/2s/5s on all 3 providers. | ✓ |
| No permanent duplicate + transient window <2s | Tolerate brief flicker; require collapse within 2s instead of 10-15s. | |
| No permanent duplicate (today's behavior) | Accept the 10-15s transient. Lowest bar; effectively says the bug is non-actionable. | |

**User's choice:** Zero duplicate, ever.
**Notes:** Sets the verification target consistent with the "root-cause fix" framing from Q1. Anything less means the reducer isn't actually fixed.

---

## Plan split + test strategy

### Q6: How should the 2 plans split?

| Option | Description | Selected |
|--------|-------------|----------|
| Split by bug (Recommended) | Plan 01 = BUG-260521-01 reducer-quality pass (root cause + WR-01 + WR-02 + dedup harden). Plan 02 = BUG-260521-02 Final Outputs link. Independent surfaces, ship/verify independently. | ✓ |
| Split by surface (StreamsProvider vs presentation) | Plan 01 = reducer changes only. Plan 02 = visual layer (ToolCallPanel + MessageItem). Harder to verify per-bug. | |

**User's choice:** Split by bug.
**Notes:** Wave-1 parallel is acceptable; plans touch different files (Plan 01 = StreamsProvider.tsx + ToolCallPanel.tsx, Plan 02 = MessageItem.tsx + optional shared OutputFileCard.tsx).

### Q7: What test mix should this phase ship with?

| Option | Description | Selected |
|--------|-------------|----------|
| Vitest unit + Chrome MCP cross-provider UAT (Recommended) | Match 075.1 precedent (it's what surfaced these bugs). Unit covers reducer invariants + dedup + WR-01/02. MCP screenshots confirm zero-duplicate live on all 3 providers. | ✓ |
| Vitest unit + single-provider smoke | Unit + manual smoke on Anthropic. Faster verify, weaker cross-provider confidence. | |
| Unit-only | Fastest. Risk: cross-provider SSE timing differences (which caused these bugs) won't be caught. | |

**User's choice:** Vitest unit + Chrome MCP cross-provider UAT.
**Notes:** Cross-provider Chrome MCP is what caught both bugs in 075.1 UAT — same recipe (sleep test + primes-matplotlib test, dev login fhdmrd@gmail.com / 123456 at http://localhost:5173/) makes UAT setup near-zero.

---

## Claude's Discretion

- Exact reuse mechanism for `OutputFileCard` (export-from-ExecuteCodeBlock vs extract-to-shared) — planner decides based on import-graph inspection (D-075.2-06).
- Exact root-cause diff site inside `StreamsProvider.tsx` (which event path creates the second entry for the first tool) — planner verifies via SSE wire log + reducer-state trace before locking the diff (D-075.2-01).

## Deferred Ideas

- IN-01 (`sub_agent_service.py` client reuse) — backend, comment-only, no concrete trigger.
- IN-02 (`harvest_output_files` double-upload) — wasteful but correct; sandbox-efficiency pass.
- IN-04 (`stoppedByUserRef` stale on reconcile) — harmless today; defer until a new consumer is added.
- BUG-260514-02 (Anthropic narration vs summary) — already deferred to v2.7 Agent Workspace; not re-routed here.
- Shared component refactor scope expansion — single component extracted, two consumers updated, stop.
- Frontend UI consumption of `tool_args_progress` SSE — deferred to v3.0 Skill Studio.
