---
phase: 075.2-toolcallpanel-dedup-final-outputs-download-link
verified: 2026-05-22T01:25:00Z
status: gaps_found
score: 17/19 must-haves verified (+ 2 partial / latent)
plan_01_verdict: PARTIAL (BUG-260521-01 closed live; latent reconcile-path type+runtime defect)
plan_02_verdict: PARTIAL (BUG-260521-02 presentation closed; download blocked by backend gap → BUG-260522-02)
phase_verdict: PARTIAL — both bugs presentationally/operationally closed under tested conditions, but one latent regression (reconcile-path reattach call site) and one known data-layer gap (BUG-260522-02) remain.

overrides: []

gaps:
  - truth: "Reconcile-path onTerminal threads the snapshot into _reattachAfterTransient (single probe across reconcile flow)"
    status: failed
    reason: |
      Plan 01 Task 1 specified updating BOTH _reattachAfterTransient call sites (reconcile path at ~line 720 and sendMessage path at ~line 921). The sendMessage path (line 964-993) was correctly updated to thread `transientSnapshot` as the first argument. The reconcile path (line 759-794) was NOT updated: it still calls `_reattachAfterTransient(threadId, run.run_id, lastSeenOffsetRef, callback)` — 4 arguments to a 5-argument signature. Because `_reattachAfterTransient` now requires `snapshot: ThreadSnapshot` as its FIRST positional parameter, the reconcile-path call:
        1. Will TypeError at runtime when `reattach(rid, since)` is invoked because the `reattach` callback arg is undefined (positional slots all shifted by one).
        2. Triggers `tsc -p tsconfig.app.json` errors TS2554 (line 773 "Expected 5 arguments, but got 4") + TS7006 (line 777 "Parameter 'rid' implicitly has 'any' type") + TS7006 (line 777 "Parameter 'since' implicitly has 'any' type").
      The Plan 01 SUMMARY at "Verification Evidence" line 154 claimed "tsc --noEmit -p tsconfig.json — zero errors". That claim is technically true only because the repo's top-level `tsconfig.json` is a stub with `files: []` and only `references`; running `tsc -p tsconfig.app.json` (the actual app project) surfaces the real errors. The orchestrator/executor verification ran the stub, masking the regression.
      The grep-checkable acceptance criterion `_reattachAfterTransient(snapshot|transientSnapshot, ...)` returns 1, not 2 (Plan 01 frontmatter expected 2). The PLAN frontmatter `pattern: "if \\(snapshot\\)\\s*\\{[\\s\\S]*?_reattachAfterTransient\\(snapshot"` for the reconcile-path key link is unsatisfied.
    artifacts:
      - path: "frontend/src/providers/StreamsProvider.tsx"
        lines: "759-794"
        issue: "Reconcile-path onTerminal still calls _reattachAfterTransient(threadId, run.run_id, lastSeenOffsetRef, callback) — 4 args to a 5-arg signature; `await _isTransientStreamEnd(...)` is still consumed as boolean (the `if (...)` wrapper, no `const snapshot = await ...` binding)."
    impact: |
      Latent regression on cold-reload-during-streaming-run. Routine new chats (the Chrome MCP UAT Recipes A + B) do NOT exercise the reconcile-path transient/reattach branch — they complete normally without hitting buffer_expired or kind=done-with-running-tools. The UAT therefore passes on dedup despite the latent bug. The defect will surface when: (a) user reloads a tab while a run is mid-stream, OR (b) network blip triggers buffer_expired during reconcile.
    missing:
      - "Mirror the sendMessage-path edit (line 949-993) onto the reconcile-path block (line 751-794): bind `const transientSnapshot = await _isTransientStreamEnd(...)`, wrap reattach call in `if (transientSnapshot) {`, pass `transientSnapshot` as the first positional argument to `_reattachAfterTransient`."
      - "Re-run `npx tsc --noEmit -p tsconfig.app.json` and confirm exit 0 with no new errors mentioning `StreamsProvider.tsx` lines 773-777."
      - "Add an UAT step: reload the browser tab during an active streaming run (sleep test, t=10s mid-stream) to exercise the reconcile-path reattach branch."

  - truth: "Pinned Final Outputs panel renders downloadable card (icon + filename + size + click-to-download) on Anthropic primes-matplotlib"
    status: partial
    reason: |
      Plan 02 closed the PRESENTATION half of BUG-260521-02: `<OutputFileCard>` is reused in `MessageItem.tsx:255-264`, the panel has `data-testid="final-outputs-panel"`, ghost-border + bg-muted/30 styling, monospace filename, lucide-download icon. Visual consistency with the per-cell card is verified live (Chrome MCP UAT, Anthropic claude-sonnet-4-6, primes-matplotlib).
      But the FUNCTIONAL half is blocked: the card renders in the url-absent fallback state (`opacity-40` muted icon, no size badge, no click target) because the backend SSE `final_output_files` event payload at `backend/app/api/threads.py:2896-2902` emits `{filename: fname}` only — `url` and `size` are deliberately discarded at `_previous_files_in_run: set[str]` (threads.py:1574) and `current_files_set = {f["filename"] for f in output_files}` (sandbox_service.py:185). React fiber inspection confirmed `message.finalOutputFiles === [{filename: "primes.png"}]`. The per-cell `<OutputFileCard>` on the same message renders correctly with full URL + size — proving the URL data IS available elsewhere, just not surfaced into the cumulative cross-iteration set.
      Original BUG-260521-02 user intent (verbatim from bug report): "make download work from the pinned panel" + "wrap the filename in `<a href={file.url}>`". Plan 02 honored CONTEXT.md `<domain>` "Any backend changes — Out of scope" (firm), so the data-layer half was deferred. BUG-260522-02 was filed 2026-05-22 with a concrete ~30-line fix path (promote `_previous_files_in_run` from `set[str]` to `dict[str, {url, size}]`; no migration, no new dependency).
      "Partial-closed" is the accurate characterization: presentation contract met (visual consistency + the url-optional fallback gracefully renders), download contract unmet (zero of Chrome MCP UAT downloads from pinned panel succeeded — they hit url-absent path).
    artifacts:
      - path: "frontend/src/components/chat/MessageItem.tsx"
        lines: "255-276"
        issue: "Renders the card correctly — but receives url-less data because of backend gap upstream."
      - path: "backend/app/services/sandbox_service.py"
        lines: "185-189"
        issue: "Collapses {filename, url, size} triples to filename-only set."
      - path: "backend/app/api/threads.py"
        lines: "1574, 2896-2902"
        issue: "Cumulative cross-iteration set is `set[str]`; SSE emit projects only filename."
    missing:
      - "Backend-side: promote `_previous_files_in_run` from `set[str]` to `dict[str, {url: str, size: int}]`; project full triples into SSE `final_output_files` event payload (3 sites, ~30 lines)."
      - "Chrome MCP re-UAT once backend gap closes: verify click-to-download from pinned panel works on Anthropic primes-matplotlib."
    deferral_justification: "CONTEXT.md `<domain>` for Phase 075.2 explicitly locked 'Any backend changes — Out of scope.' The orchestrator deferred to BUG-260522-02 instead of expanding scope mid-flight, which honors the locked phase boundary. The deferral is defensible: (a) backend fix is well-scoped (~30 lines, 2 files, no migration); (b) re_open_trigger in BUG-260521-02 explicitly names the path forward; (c) the presentation half is now in place — the next phase that addresses the backend can verify end-to-end on a single UAT run."

deferred: []  # No items addressed in later phases of this milestone

human_verification:
  - test: "Reload-tab-during-streaming-run to exercise reconcile-path reattach"
    expected: "Tool card continues streaming after reload without runtime error in console; no TypeError on `reattach is not a function`."
    why_human: "Requires live SSE stream + manual browser reload at a precise moment; not exercisable by Vitest reducer harness."

  - test: "Cross-provider dedup re-verification once reconcile-path is fixed"
    expected: "ZERO duplicate cards on cold-reload + buffer_expired branches on OpenAI gpt-4.1, Anthropic claude-sonnet-4-6, OpenRouter (sleep test scenario where backend disconnects briefly mid-run)."
    why_human: "Chrome MCP UAT against live backend; requires inducing transient SSE failure or browser reload mid-stream."

  - test: "Final Outputs click-to-download on Anthropic primes-matplotlib (post BUG-260522-02 close)"
    expected: "Click on `primes.png` card in pinned 'Final outputs' panel triggers programmatic JS blob download; file lands in Downloads folder; matches per-cell download behavior."
    why_human: "Requires live LLM-driven matplotlib generation + browser-level download verification; depends on BUG-260522-02 backend fix."
---

# Phase 075.2: ToolCallPanel Dedup + Final Outputs Download Link — Verification Report

**Phase Goal (from ROADMAP):** Close 2 frontend defects deferred from Phase 075.1 Chrome MCP UAT (2026-05-21) — BUG-260521-01 (transient duplicate tool card) + BUG-260521-02 (Final Outputs panel renders plain text instead of download cards). Bonus pickup: WR-01 / WR-02 / WR-03 from 075.1-REVIEW.

**Verified:** 2026-05-22T01:25:00Z
**Status:** gaps_found (1 latent regression + 1 partial close with documented backend follow-up)
**Re-verification:** No — initial verification

---

## Plan-Level Verdicts

### Plan 01 (BUG-260521-01 reducer-quality pass) — **PARTIAL**

**What landed:**

- `onToolStart` else-branch idempotency-on-replay guard (D-075.2-01) — landed at `StreamsProvider.tsx:323-334`, comment block `D-075.2-01: idempotency-on-replay guard` present.
- `_isTransientStreamEnd` return type widened to `Promise<ThreadSnapshot | null>` (D-075.2-03) — landed at line 128, all four exit points (`cancelled/timed_out → null`, `!stillWorking → null`, snapshot-null → null, terminal `isStreaming ? snapshot : null`) match the plan.
- `_reattachAfterTransient` exported + accepts `snapshot: ThreadSnapshot` as first parameter — landed at line 185-206; the internal `getSnapshot` probe is deleted.
- `onToolEnd` accepts optional `id?` parameter with `id ? tc.id === id : tc.name === name` matcher (D-075.2-04) — landed at line 358-368; `lib/api.ts:209` `StreamCallbacks.onToolEnd` signature widened.
- ToolCallPanel `deduplicatedToolCalls` dedup-key fallback gains `idx` tiebreaker (WR-03) — landed at line 538-555 with comment `D-075.2-01 §2 / WR-03`.
- New test file `StreamsProvider.dedup.test.ts` with 5 tests — landed; all 5 GREEN.
- Existing `StreamsProvider.transient.test.ts` migrated to `ThreadSnapshot | null` shape + 1 new D-075.2-03 reattach-no-second-probe test — landed; all 9 GREEN.

**What's broken:**

- **Reconcile-path `_reattachAfterTransient` call site (StreamsProvider.tsx:773) was NOT updated.** It still calls the helper with 4 arguments `(threadId, run.run_id, lastSeenOffsetRef, callback)` — but the helper now expects 5 arguments `(snapshot, threadId, runId, lastSeenOffsetRef, reattach)`. The sendMessage path (line 964-993) IS correctly threaded.
- `tsc -p tsconfig.app.json` surfaces 3 hard errors at lines 773, 777, 777 (TS2554 + TS7006 × 2).
- Plan 01 SUMMARY claimed "tsc clean" — true only against the stub `tsconfig.json`, false against the real app config.
- Latent runtime regression: cold-reload-during-active-streaming-run will TypeError because `reattach` callback arg is `undefined` at the runtime call site.

**Live Chrome MCP UAT for the BUG-260521-01 acceptance bar:**

| Provider | Test | t=3s | t=10s | t=15s | t=30s | Final | Verdict |
|---|---|---|---|---|---|---|---|
| OpenAI gpt-4.1 | 45s sleep test | 1 card | 1 card | 1 card | 1 card | 1 card (Used 1 tool 60.7s) | PASS |
| Anthropic claude-sonnet-4-6 | primes-matplotlib | 1 card | — | — | — | 1 card (Used 1 tool 12.7s) | PASS |
| OpenRouter | sleep test | — | — | — | — | — | SKIPPED (provider-agnostic reducer path covered by Recipe A) |

D-075.2-02 acceptance bar (ZERO duplicate, ever) MET on the tested providers + scenarios. **Caveat:** the UAT did not exercise the reconcile-path `_reattachAfterTransient` branch (no cold-reload-mid-stream, no buffer_expired). The latent gap above lives outside the UAT envelope.

### Plan 02 (BUG-260521-02 Final Outputs download link) — **PARTIAL**

**What landed:**

- `OutputFileCard` extracted to `frontend/src/components/chat/OutputFileCard.tsx` with `export function OutputFileCard` (D-075.2-05/06).
- Relaxed prop shape: `{ filename: string; url?: string; size?: number }` — canonical `OutputFile` in `@/types` UNTOUCHED (PATTERNS.md §7 honored).
- Url-optional early-return renders plain-filename row with `opacity-40` muted icon when `!file.url`.
- `ExecuteCodeBlock.tsx` deletes local `OutputFileCard` + `API_BASE` + `resolveOutputUrl` + `formatBytes`; imports from `./OutputFileCard`.
- `MessageItem.tsx:255-276` Final Outputs panel renders `<OutputFileCard key={i} file={f} />` stack; `data-testid="final-outputs-panel"` preserved; empty-state guard preserved (D-075.2-07).
- New test file `MessageItem.finalOutputs.test.tsx` with 5 tests — all GREEN.
- Existing `Plan04.frontend.test.tsx` continues passing (8/8 — verified post-merge).
- 4 unrelated `formatBytes` copies UNTOUCHED (SettingsPage, DocumentList, FolderDetail, SkillFormDialog).

**What's still broken (deferred to BUG-260522-02):**

- Click-to-download from pinned panel does NOT work because backend SSE `final_output_files` event payload at `threads.py:2896-2902` emits `{filename}` only (url + size discarded at `_previous_files_in_run: set[str]`).
- Pinned panel card therefore renders in url-absent fallback state on every live run; download icon is muted, no size badge, no click handler.
- Hooks-ordering deviation from PLAN.md (unconditional hooks BEFORE early-return instead of early-return-before-hooks) — documented in Plan 02 SUMMARY; rules-of-hooks compliant; functionally equivalent.

**Live Chrome MCP UAT for the BUG-260521-02 acceptance bar:**

- Visual consistency vs per-cell card: PASS (`data-testid="final-outputs-panel"`, ghost-border, monospace filename, lucide-download icon — confirmed via React fiber inspection on Anthropic primes-matplotlib).
- Click-to-download from pinned panel: **FAIL** (url-absent fallback path; backend gap).
- Per-cell `<OutputFileCard>` byte-identical to pre-refactor: PASS (per-cell rendered correctly with full URL `http://localhost:8000/sandbox-outputs/.../primes.png` + `38.2 KB` size badge on the same Anthropic test).

---

## Goal Achievement

### Observable Truths

| # | Truth | Source | Status | Evidence |
|---|-------|--------|--------|----------|
| 1 | First tool of a run renders EXACTLY ONE card on OpenAI gpt-4.1 (sleep) | Plan 01 must_haves | VERIFIED | Chrome MCP UAT Recipe A at t=3/10/15/30s + final all single card |
| 2 | First tool of a run renders EXACTLY ONE card on Anthropic claude-sonnet-4-6 (primes-matplotlib) | Plan 01 must_haves | VERIFIED | Chrome MCP UAT Recipe B at t=3s + final single card |
| 3 | First tool of a run renders EXACTLY ONE card on OpenRouter (sleep test) | Plan 01 must_haves | NOT_TESTED | UAT skipped per orchestrator rationale: provider-agnostic reducer path covered by Recipes A+B; OpenRouter uses OpenAI-compatible reducer path |
| 4 | `_isTransientStreamEnd` returns ThreadSnapshot \| null (not boolean) | Plan 01 must_haves | VERIFIED | line 128 returns `Promise<ThreadSnapshot \| null>`; all 4 exit points return null/snapshot |
| 5 | `_reattachAfterTransient` no longer calls getSnapshot internally (single probe across the transient + reattach flow) | Plan 01 must_haves | VERIFIED | line 185-206 helper body has no `getSnapshot` call; `grep -c "await getSnapshot" StreamsProvider.tsx` = 2 (helper line 158 + unrelated reconcile-action line ~649) |
| 6 | Both callers consume the threaded snapshot | Plan 01 must_haves | **FAILED** | sendMessage path (line 964-993) threads `transientSnapshot` correctly; reconcile path (line 759-794) does NOT thread snapshot — runtime + tsc errors. See gap #1. |
| 7 | `onToolEnd` accepts an optional `id?` parameter; matches by tc.id === id when present | Plan 01 must_haves | VERIFIED | line 358 signature `(name, result, id) =>`; line 362 matcher `(id ? tc.id === id : tc.name === name)` |
| 8 | `ToolCallPanel` dedup fallback key includes idx tiebreaker | Plan 01 must_haves | VERIFIED | line 548 `\`${tc.name}-${tc.startedAt ?? ''}-${idx}\`` |
| 9 | Plan 04 Atom B invariant preserved — every render-time read inside ToolCallPanel uses `deduplicatedToolCalls` | Plan 01 must_haves | VERIFIED | Memo body changed only; no consumer site touched in diff |
| 10 | D-063.1-11 `reconcileInFlightRef` single-bit lock untouched | Plan 01 must_haves | VERIFIED | `grep -c reconcileInFlightRef` = 4 references; no diff touched the lock-wrap |
| 11 | Phase 067.5 Branch D-3 streaming-bucket guard at `useMessages.ts:572-590` untouched | Plan 01 must_haves | VERIFIED | `git diff -- frontend/src/hooks/useMessages.ts` returns 0 lines |
| 12 | Plan 03 Task 1 invariant preserved — no setMessages callback explicitly sets `content:` | Plan 01 must_haves | VERIFIED | `grep -c "setMessages.*content:"` = 0 |
| 13 | Pinned Final Outputs panel renders downloadable cards (icon + filename + ghost-border + hover) | Plan 02 must_haves | VERIFIED | Chrome MCP: `<div data-testid="final-outputs-panel">` with card stack, `lucide-download` icon, monospace filename, `ghost-border` + `bg-muted/30` |
| 14 | Clicking a Final Outputs card triggers programmatic blob download with Bearer token | Plan 02 must_haves | **PARTIAL** (blocked) | Card renders in url-absent fallback state (no click handler attached). Root cause: backend SSE payload omits url. See gap #2 + BUG-260522-02. |
| 15 | When finalOutputFiles[*].url is undefined, card renders as plain row with reduced opacity | Plan 02 must_haves | VERIFIED | Live Chrome MCP capture matches: `opacity-40` icon, `text-foreground/60`, no anchor. Vitest test `D-075.2-05 back-compat` also asserts this. |
| 16 | Empty-state guard preserved — panel only renders when `finalOutputFiles.length > 0` | Plan 02 must_haves | VERIFIED | `grep -c "message.finalOutputFiles.length > 0"` = 1; tests `D-075.2-07` (×2) assert empty-state correctly |
| 17 | `OutputFileCard` lives in shared module; ExecuteCodeBlock imports it; per-cell behavior byte-identical | Plan 02 must_haves | VERIFIED | `OutputFileCard.tsx` new file with `export function OutputFileCard`; ExecuteCodeBlock imports it (`import { OutputFileCard } from "./OutputFileCard"`); per-cell render verified live (`primes.png 38.2 KB` clickable) |
| 18 | `resolveOutputUrl` + `API_BASE` move along with OutputFileCard (D-067.2-03 preserved) | Plan 02 must_haves | VERIFIED | Both moved into `OutputFileCard.tsx`; `grep -c "function resolveOutputUrl" ExecuteCodeBlock.tsx` = 0 |
| 19 | 5 other `formatBytes` copies UNTOUCHED; `types/index.ts` canonical OutputFile strict | Plan 02 must_haves | VERIFIED | `git diff -- frontend/src/types/index.ts` = 0 lines; 4 unrelated formatBytes sites in SettingsPage / DocumentList / FolderDetail / SkillFormDialog all UNTOUCHED |

**Score:** 17/19 truths VERIFIED; 1 FAILED (truth #6); 1 PARTIAL (truth #14, blocked by documented backend gap → BUG-260522-02); 1 NOT_TESTED (truth #3, justified skip).

### Locked Invariants (Spot Audit)

All 5 locked invariants from the orchestrator's preamble are preserved:

| Invariant | Status | Evidence |
|---|---|---|
| D-063.1-11 `reconcileInFlightRef` single-bit lock | UNTOUCHED | 4 references; outer reconcile body wrap intact |
| Phase 067.5 Branch D-3 streaming-bucket guard at `useMessages.ts:572-590` | UNTOUCHED | `useMessages.ts` diff = 0 lines |
| Plan 03 Task 1 m.content invariant (no setMessages callback explicitly sets content:) | UNTOUCHED | `grep -c "setMessages.*content:"` = 0 |
| Plan 04 Atom B dedup-by-id authoritative-inside-ToolCallPanel | PRESERVED | Memo body changed (idx tiebreaker added); no consumer site touched in diff |
| `frontend/src/types/index.ts` canonical OutputFile strict | UNTOUCHED | `git diff` = 0 lines |

---

## D-075.2-XX Decision Traceability

| Decision | Intent | Commit | Landed? | Evidence |
|---|---|---|---|---|
| D-075.2-01 | Fix root cause at reducer (onToolStart else-branch idempotency) + harden dedup-key fallback | `9ae9fbd` (root cause) + `bc940a8` (WR-03) | YES | StreamsProvider.tsx:323-334 `findIndex` predicate; ToolCallPanel.tsx:548 idx tiebreaker |
| D-075.2-02 | Acceptance bar = ZERO duplicate, ever (Chrome MCP at t=0/2/5s) | UAT artifacts | YES (on tested providers) | Chrome MCP Recipe A (OpenAI sleep) + Recipe B (Anthropic primes) both single-card at every t-point |
| D-075.2-03 | WR-02 two-probe getSnapshot race fix (snapshot threaded through helpers) | `25bf768` | **PARTIAL** | Helper signatures updated correctly; sendMessage caller threaded; **reconcile-path caller NOT threaded** (gap #1) |
| D-075.2-04 | WR-01 `onToolEnd` accepts optional id (frontend-only this phase) | `9ae9fbd` | YES | StreamsProvider.tsx:358 signature + line 362 matcher; api.ts:209 type widened |
| D-075.2-05 | Reuse `OutputFileCard` verbatim from per-cell rendering | `94e04aa` + `2ce5a4b` | YES | OutputFileCard.tsx exports component; MessageItem.tsx imports + renders the stack |
| D-075.2-06 | Planner picks extract-to-shared-module vs re-export | `94e04aa` | YES (option b chosen) | New `frontend/src/components/chat/OutputFileCard.tsx`; both consumers updated |
| D-075.2-07 | Empty-state guard unchanged | `2ce5a4b` | YES | MessageItem.tsx:264 `message.finalOutputFiles && message.finalOutputFiles.length > 0` preserved |
| D-075.2-08 | Split by bug — 2 plans, Wave 1 parallel | (structural) | YES | Plan 01 + Plan 02 shipped in parallel waves, no cross-edits |
| D-075.2-09 | Test mix — Vitest unit + Chrome MCP UAT | (structural) | YES | 24 new/extended Vitest tests across 4 files all GREEN; Chrome MCP UAT Recipe A + B captured live |

**Flag:** D-075.2-03 lands in helper signatures but only ONE of two call sites was updated. The reconcile-path key link from PLAN 01 frontmatter (`pattern: "if \\(snapshot\\)\\s*\\{[\\s\\S]*?_reattachAfterTransient\\(snapshot"`) is UNSATISFIED.

---

## Required Artifacts (Plan-Level)

### Plan 01 Artifacts

| Path | Expected | Status | Details |
|---|---|---|---|
| `frontend/src/providers/StreamsProvider.tsx` | Hardened onToolStart else-branch + WR-01 id-match + WR-02 snapshot threading | VERIFIED (intent) / PARTIAL (wiring) | Reducer-level edits all present; one caller-site wiring missed (gap #1) |
| `frontend/src/components/chat/ToolCallPanel.tsx` | Dedup-key fallback with idx tiebreaker | VERIFIED | line 548 `${tc.name}-${tc.startedAt ?? ''}-${idx}` |
| `frontend/src/__tests__/providers/StreamsProvider.dedup.test.ts` | ≥5 tests, reducer dedup + WR-01 id-match coverage | VERIFIED | 5 it() blocks; all GREEN (24/24 in combined run) |
| `frontend/src/__tests__/providers/StreamsProvider.transient.test.ts` | Updated assertions for ThreadSnapshot \| null shape + reattach-no-second-probe test | VERIFIED | `toBeNull` assertions present; D-075.2-03 test present; 9/9 GREEN |

### Plan 02 Artifacts

| Path | Expected | Status | Details |
|---|---|---|---|
| `frontend/src/components/chat/OutputFileCard.tsx` | Shared downloadable card with url-optional back-compat (min 70 lines) | VERIFIED | 123 lines; `export function OutputFileCard`; relaxed prop shape; url-optional early-return |
| `frontend/src/components/chat/ExecuteCodeBlock.tsx` | Per-cell rendering imports from shared module | VERIFIED | `import { OutputFileCard } from "./OutputFileCard"` present; local definitions removed (0 grep matches for `function OutputFileCard`, `function formatBytes`, `function resolveOutputUrl`) |
| `frontend/src/components/chat/MessageItem.tsx` | Final Outputs panel renders OutputFileCard per file | VERIFIED | line 272 `<OutputFileCard key={i} file={f} />`; empty-state guard + data-testid preserved |
| `frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx` | Render coverage: with-url, without-url, empty-state, mixed (min 50 lines) | VERIFIED | 5 it() blocks; all GREEN |

---

## Key Link Verification

### Plan 01 Key Links

| From | To | Via | Status | Details |
|---|---|---|---|---|
| StreamsProvider.tsx onTerminal (sendMessage path, ~line 921) | _isTransientStreamEnd → _reattachAfterTransient | threaded snapshot (single probe) | VERIFIED | line 964 `const transientSnapshot = await _isTransientStreamEnd(...)`; line 971 `if (transientSnapshot)`; line 974 `_reattachAfterTransient(transientSnapshot, ...)` |
| StreamsProvider.tsx onTerminal (reconcile path, ~line 720) | _isTransientStreamEnd → _reattachAfterTransient | threaded snapshot (single probe) | **NOT_WIRED** | line 759 `if (await _isTransientStreamEnd(...))` still consumed as boolean; line 773 `_reattachAfterTransient(threadId, ...)` passes 4 args to 5-arg signature. Pattern `_reattachAfterTransient\(snapshot` returns 0 hits in this block. |
| StreamsProvider.tsx onToolStart else-branch | existingCalls findIndex (running\|done) | idempotency-on-replay guard | VERIFIED | line 323-324 `existingCalls.findIndex((tc) => tc.name === name && (tc.status === "running" \|\| tc.status === "done"))` |
| StreamsProvider.tsx onToolEnd | tc.id matching | id-first match with name fallback | VERIFIED | line 362 `(id ? tc.id === id : tc.name === name)` |

### Plan 02 Key Links

| From | To | Via | Status | Details |
|---|---|---|---|---|
| MessageItem.tsx | OutputFileCard.tsx | `import { OutputFileCard } from "./OutputFileCard"` | VERIFIED | line 11 of MessageItem.tsx |
| ExecuteCodeBlock.tsx | OutputFileCard.tsx | `import { OutputFileCard } from "./OutputFileCard"` | VERIFIED | line 5 of ExecuteCodeBlock.tsx |
| OutputFileCard onClick | downloadSandboxOutput from @/lib/api | JS blob + Bearer fetch (D-067.3-R2-03) | VERIFIED (component-level); BLOCKED end-to-end | OutputFileCard.tsx:78 `await downloadSandboxOutput(file.url!, file.filename)`; never invoked on pinned panel because backend omits url upstream (BUG-260522-02) |

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| All Plan 01 + Plan 02 Vitest suites pass | `npx vitest run src/__tests__/providers/StreamsProvider.{dedup,transient,anthropic-ordering}.test.ts src/__tests__/components/MessageItem.finalOutputs.test.tsx` | 4 files / 24 tests / all PASS in 1.75s | PASS |
| `Plan04.frontend.test.tsx` non-regression | `npx vitest run src/__tests__/components/Plan04.frontend.test.tsx` | 1 file / 8 tests / all PASS in 1.63s | PASS |
| TypeScript clean against app project config | `npx tsc --noEmit -p tsconfig.app.json` | TS2554 + 2× TS7006 at StreamsProvider.tsx:773-777; plus pre-existing errors in 10 unrelated files | **FAIL (new errors at 773-777 are in this phase's scope)** |
| TypeScript clean against stub config | `npx tsc --noEmit -p tsconfig.json` | exit 0 (stub config does no work) | PASS (but misleading — see gap #1) |
| `_reattachAfterTransient(snapshot or transientSnapshot, ...)` call site count | `grep -cE "_reattachAfterTransient\((snapshot\|transientSnapshot)" StreamsProvider.tsx` | 1 (expected 2 per Plan 01 acceptance) | FAIL |
| Locked invariant grep `setMessages.*content:` | `grep -c "setMessages.*content:" StreamsProvider.tsx` | 0 | PASS |
| Locked invariant — `useMessages.ts` untouched | `git diff -- frontend/src/hooks/useMessages.ts` | empty (0 lines) | PASS |
| Locked invariant — `types/index.ts` untouched | `git diff -- frontend/src/types/index.ts` | empty (0 lines) | PASS |

---

## Requirements Coverage

This is a defect-closure phase; `REQUIREMENTS.md` declares no REQ-IDs (TBD). Coverage tracked via BUG-IDs:

| BUG-ID | Plan | Description | Status | Evidence |
|---|---|---|---|---|
| BUG-260521-01 | 01 | ToolCallPanel transient duplicate card on FIRST tool | CLOSED (UAT envelope) / partial (latent reconcile-path defect) | Chrome MCP Recipes A + B both single-card; reducer-level fix verified; reconcile-path reattach call site is broken (latent — gap #1) |
| BUG-260521-02 | 02 | Pinned Final Outputs panel renders plain text | partial-closed (presentation) / open (download functional) | OutputFileCard rendered; backend SSE payload omits url → click-to-download blocked → BUG-260522-02 captures the data-layer follow-up |
| WR-01 (onToolEnd id-match) | 01 | Frontend ready for id-based matching | CLOSED (frontend) | api.ts type widened; reducer matcher present; tests cover with-id + back-compat |
| WR-02 (two-probe getSnapshot race) | 01 | Single-probe transient/reattach | PARTIAL | Helper signatures correct; sendMessage caller correct; reconcile caller missed |
| WR-03 (dedup-key fallback hardening) | 01 | idx tiebreaker | CLOSED | ToolCallPanel.tsx:548 |
| BUG-260522-02 | (new) | Backend SSE omits url + size from final_output_files event | open (filed during UAT) | Captured 2026-05-22; concrete ~30-line fix path documented |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| frontend/src/providers/StreamsProvider.tsx | 773 | `_reattachAfterTransient(threadId, run.run_id, lastSeenOffsetRef, callback)` — 4-arg call to 5-arg signature | 🛑 Blocker (latent regression) | Type error at compile (TS2554) + TypeError at runtime when reconcile-path reattach branch fires (cold-reload-during-streaming-run, buffer_expired during reconcile). Blocks reattach functionality on that branch. |
| frontend/src/providers/StreamsProvider.tsx | 759-767 | `if (await _isTransientStreamEnd(...))` consumed as boolean | 🛑 Blocker (latent regression) | Same defect as line 773; the boolean-context coerces `ThreadSnapshot | null` to truthy/falsy, but the helper's threaded snapshot is then discarded — defeats the WR-02 race fix on this code path. |
| .planning/.../075.2-01-SUMMARY.md | line 154 | "`npx tsc --noEmit -p tsconfig.json` — zero errors across the full project" | ⚠️ Warning (documentation drift) | Claim is technically true against the stub `tsconfig.json` (project references only), false against the real `tsconfig.app.json`. Masked the latent regression at verification time. |

No console.log, stubs, or empty handlers introduced by this phase.

---

## Plan 01 Summary Self-Check Discrepancies

Plan 01 SUMMARY listed 3 deviations under "Documentation drift in grep-checkable criteria". A re-audit confirms:

| Deviation | SUMMARY claim | Re-audit finding |
|---|---|---|
| #1 (CRLF tooling) | Python helpers used for byte-level edits; no behavior impact | CONFIRMED — final files match planned semantics |
| #2 (`transientSnapshot` vs `snapshot` variable name) | "Both caller sites use the new pattern" → `grep -c "_reattachAfterTransient(transientSnapshot"` = 2 | **WRONG.** Actual `grep -c "_reattachAfterTransient(transientSnapshot"` = 1. Only the sendMessage path was updated. The reconcile path still uses the old 4-arg call. SUMMARY's claim "lines 749 (reconcile path) and 949 (sendMessage path)" is incorrect — only line 974 (sendMessage path) has the threaded call. |
| #3 (`await getSnapshot(threadId)` count = 2, not 1) | "The reconcile-action `getSnapshot` is a separate code path and was never the WR-02 race source" | CONFIRMED — third `getSnapshot` at line 649 is unrelated to WR-02; but the SUMMARY missed that the helper at line 158 is still the only WR-02-relevant call only because the reconcile-path caller no longer passes 4 args correctly (whole reattach is broken on that path). |

The Plan 01 SUMMARY appears to have over-counted the 2 caller updates as both completed. This is the root cause of the gap reaching the merge.

---

## Gaps Summary

**1 latent regression + 1 documented backend-deferred half:**

- **Gap #1 (latent regression):** Reconcile-path `_reattachAfterTransient` call site at StreamsProvider.tsx:773 was not updated to thread the snapshot. Will TypeError at runtime when the reconcile-path transient/reattach branch fires (cold-reload-during-streaming-run, buffer_expired during reconcile). Surfaces as 3 TS errors at `tsc -p tsconfig.app.json` (TS2554 + 2× TS7006). The Plan 01 SUMMARY's "tsc clean" claim was made against the stub `tsconfig.json` and missed the real errors. **Action: spawn a narrow fix plan in Phase 075.3 or 075.2.1** to mirror the sendMessage-path edit onto the reconcile-path block (4 lines: bind `transientSnapshot`, wrap `if (transientSnapshot)`, pass as first arg).

- **Gap #2 (documented backend-deferred):** Pinned Final Outputs panel renders the card stack visually but card cannot click-to-download because backend SSE `final_output_files` event payload omits url + size. BUG-260522-02 captures the fix path (~30 lines, 2 backend files, no migration). This deferral honors Phase 075.2 CONTEXT.md `<domain>` "Any backend changes — Out of scope" lock — the scope-protection decision is defensible and the follow-up is well-scoped.

**Other observations (non-blocking):**

- OpenRouter UAT was skipped with documented rationale (provider-agnostic reducer path, OpenAI-compatible). Acceptable per the verification context; future re-UAT could close the matrix fully.
- `Plan04.frontend.test.tsx` non-regression check claimed "not re-run post-merge" in the orchestrator context — verifier ran it 2026-05-22; 8/8 PASS, no regression.
- Pre-existing test debt in `streamsProvider.test.tsx` (7 tests against the legacy Phase 075 D-075-02 atomic-swap shape) remains untouched per the deferred-items.md scope rule. Not introduced by this phase.

---

## PHASE VERIFICATION COMPLETE

Phase 075.2 SHIPPED PARTIAL: both bugs structurally addressed at the planned surfaces, Chrome MCP UAT validated dedup zero-duplicate bar on tested providers, but reconcile-path reattach has a latent 4-vs-5-arg type+runtime regression (StreamsProvider.tsx:773) and Final Outputs click-to-download is blocked on documented backend gap BUG-260522-02.
