# Phase 214 — UAT scoreboard

**Authored:** 2026-08-28, at `/gsd:plan-phase 214`.
**Owner of the drive:** the operator, at plan `214-14`'s blocking checkpoint (Task 4).

> ⚠ **This is NOT a `VALIDATION.md`.** D-214-24 (operator decision, 2026-08-28) records that no
> `RESEARCH.md` and no `VALIDATION.md` are owed for this phase; the acceptance bars are the four
> generated `BUILD-CONTRACT.generated.md` files and CONTEXT.md's `<failure_modes>` list. This file is
> the **SC#10 scoreboard and the G-4 lived-experience record** — the two things CLAUDE.md requires of
> any phase touching streaming, the agent loop, provider routing, UI state, or user-visible UI.
>
> ⚠ **UAT rows are recorded HERE, never as PLAN.md tasks.**

---

## 0 · Flag state — every row must declare it

Two governed flags bear on this phase and they are **not** in the same state.

| flag | cold default AFTER this phase | how a row must record it |
|---|---|---|
| `visual_workflow_canvas` | **`everyone`** (flipped in `214-14`, D-214-19) | say whether the row ran against the **cold default** or a flipped `feature_visibility` row |
| `live_connectors` | **`off`** — deliberately **not** flipped | ⚠ a row that performs a **real outbound send** required a **deliberate local arming**; say so and say it was reverted |

⚠ **Any live drive of SC#2 or SC#4 needs BOTH flags.** A row driven with `live_connectors` off exercises
the *record, do not send* path — which is a real and useful row, but it is **not** a send.

⚠ **Phase 209's 16/16 browser drive ran against a flag-flipped database and shipped nothing to anyone.**
A row that does not name its flag state is not a row.

---

## 1 · SC#10 — the cross-provider roster

**The FULL native roster plus OpenRouter — 8 rows, not 4** (CLAUDE.md, amended 2026-07-31 by the
operator during Phase 185).

⚠ **Derived from `MODEL_CAPABILITIES`, not transcribed.** Re-derive at drive time — this table was
generated on 2026-08-28 by grouping `backend/app/config.py`'s registry by `provider` and taking the
newest registry-backed id per group. **An id absent from `MODEL_CAPABILITIES` resolves
`capability_source=inferred` and silently loses `emit_tier`**, so it would measure a weaker
configuration than the one that ships (SEED-040 §2026-07-31, SEED-135).

**Cheapest honest method** (proven in Phase 185): drive each row as a real run with a **per-request**
`model` + `provider` on `POST /threads/{id}/messages`, and read verdicts from `workflow_runs` /
`workflow_phases` / `harness_audit`. That scores the whole board **without mutating any global
setting**, so the operator's environment is untouched and rows cannot contaminate each other.

⚠ **Rows may be ⛔ BLOCKED with a named reason and a blocking id — never silently omitted.**
A scoreboard that lists only what passed is not a scoreboard.

The scenario for every row is the same, so the rows are comparable: **author-and-run a two-phase
workflow whose second phase is an `external_action` `send_email` with `to` = *Asked when this runs*,
`subject` = *Set here*, `body` = *From an earlier step*, armed for approval.** Record what the pause
said and what the step received.

| # | provider | model (re-derive) | `emit_tier` | `native_tools` | flag state | pause named the service? | all 3 args in the pause? | step received `to`? | verdict |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `openai` | `gpt-5.6-terra` | `force_strict` | True | | | | | |
| 2 | `anthropic` | `claude-sonnet-5` | `force` | True | | | | | |
| 3 | `google` | `gemini-3.5-flash` | `force` | True | | | | | |
| 4 | `deepseek` | `deepseek-v4-pro` | `force` | True | | | | | |
| 5 | `zhipu` | `glm-5.2` | `force` | True | | | | | |
| 6 | `minimax` | `MiniMax-M3` | `force` | True | | | | | |
| 7 | `moonshot` | `kimi-k2.6` | **`coerce`** | True | | | | | |
| 8 | `openrouter` | `minimax/minimax-m2.7` | `force` | **False** | | | | | |

⚠ **Row 7 is the weakest emission guarantee in the registry** — `moonshot` is the only native
`emit_tier: coerce` family. If any row is going to disagree about a tool call, expect this one.
⚠ **Row 8 is the NON-NATIVE tool path**, not a ninth flavour of the native one: every OpenRouter row
is `native_tools: False`. ⚠ And `deepseek`'s `strict_json_schema` is **inert** (no `/beta` base_url,
D-122-04) — do not read row 4 as a strict-schema row.

⚠ **Google has historically been the highest-risk row for tool-call emission.** Drive it early.

---

## 2 · SC#10 — the other three axes

| axis | required | row | flag state | verdict |
|---|---|---|---|---|
| **Multi-tool** | ≥ 1 row exercising 2+ tools in one prompt (e.g. `search_documents` + `execute_code`) alongside an `external_action` step | | | |
| **Parallel-thread** | ≥ 1 row with Thread A streaming while Thread B accepts a new prompt — ⚠ **and one of them holding an armed approval pause**, since a pause suspends a live run | | | |
| **Long-message** | ≥ 1 row with ≥ 50 prior messages OR a ≥ 5 KB user prompt, launched through the **new chat launch form** (`214-12`) | | | |

---

## 3 · G-4 — the operator's "I'd recognise failure here" checks

Defined at scope time, driven with Chrome MCP at verification. ⚠ **Wire format and screenshots are
insufficient** — that is the whole content of G-4.

| # | check | what failure looks like | verdict |
|---|---|---|---|
| G4-1 | **Author a `send_email` step.** The form shows exactly `to`, `subject`, `body`. | A `Cc` or `Reply to` row appears — the backend refuses every submission with `SmtpArgumentsInvalid` while the form typechecks and every frontend test passes. Or a JSON box appears in **any** state, including "for advanced tools". | |
| G4-2 | **Set the three sources.** `to` = *Asked when this runs*, `subject` = *Set here*, `body` = *From an earlier step* — and the body row arrived **pre-set** with a note, still changeable. | Nothing states where an argument comes from; or the body was filled silently. **An invisible rule is what `BUG-260826-01` was.** | |
| G4-3 | **Launch three ways** — library, thread, schedule — and confirm each asks for `to` and the run receives it. | A door that renders a field and sends nothing. ⛔ This is the milestone's blocking defect; if any door fails, SC#2 is unmet. | |
| G4-4 | **Break it and publish.** Remove `to`'s source; then set it to *Asked when this runs* without declaring the key. | One generic *"this step is invalid"* headline for both, or a headline naming a gauntlet stage, or a headline naming the slug instead of your step's name. | |
| G4-5 | **Watch it fail.** The panel shows the adapter's own sentence and agrees with chat at the same moment. | The panel reads *"Failure reason not captured by the backend"* while chat shows the reason. ⚠ Drive this against a **reloaded** run, not only a live one — the defect is on the reconcile path. | |
| G4-6 | **Watch it pause.** The pause names the **service** and lists **all three** arguments. | `It will run "post_message" through post_message.` on a capability row; no service at all on an MCP row; or a pause listing only the fixed value and silently omitting the two that vary. | |
| G4-7 | **The panel and the lane.** The builder panel is visibly wider than before; a sourced and an unsourced argument row have **identical** label offsets. | The lane is inserted rather than widened, so the form reads as two layouts — variant B has quietly become variant C on a panel the phase already paid to widen. | |
| G4-8 | **The describe door.** Name an unconnected service in the box. | A Slack step is drafted for an author with no Slack connection; or a refusal with no next action; or a refusal that fires **at rest**, before you typed anything; or an anchor on the wrong word. | |

---

## 4 · What a verdict may say

- **✅ pass** — driven, with the observation recorded, not inferred.
- **⚠ partial** — driven, and something specific was wrong. **Name it.**
- **⛔ blocked** — could not be driven. **Name the reason and the blocking id.** A missing provider key,
  a missing connection row and a known defect are all legitimate blocks; *"did not get to it"* is
  recorded as **not run**, which is a different word and must not be written as a pass.
- **not run** — honest, and a legitimate way to close a phase **as a decision**. Record the owed rows
  in the ROADMAP progress row and in `STATE.md`, and name which row to run first.

⚠ **Closing this phase with owed rows is legitimate. Claiming they ran is not.**

⚠ **A UAT fix needs its own UAT row.** A defect found here and fixed is re-driven, in a new row, with
its own verdict — a fix verified only by the test written for it has been verified by its author.
