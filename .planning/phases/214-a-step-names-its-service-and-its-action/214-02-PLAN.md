---
phase: 214-a-step-names-its-service-and-its-action
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/workflows.py
  - backend/app/api/workflow_runs.py
  - backend/app/models/thread.py
  - backend/app/api/threads.py
  - frontend/src/types/index.ts
  - frontend/src/lib/api/workflows.ts
  - frontend/src/lib/apiRunFields.fences.test.ts
  - backend/tests/unit/test_214_failure_reason_seam.py
autonomous: true
requirements: [STEP-04, STEP-05]

must_haves:
  truths:
    - "D-214-23: a failed phase's reason reaches BOTH run wire models, parsed through the ONE shared unwrap, and reads correctly on a jsonb STRING-SCALAR row — not only on the 2-of-43 object rows."
    - "D-214-18: the measurement was taken and its binary was refuted; this plan fixes the read/serialize seam, not the emitter and not the executor's failure path."
    - "D-214-16: a run surface receives the service and the action as data, so the shared identity element can render them without any surface resolving anything for itself."
    - "An EMPTY failure reason and an ABSENT one remain different facts on the wire — they must not collapse, or the `reason_unknown` sentinel loses its meaning."
    - "`WorkflowRunPhaseRead` and `WorkflowPhaseState` are widened in the SAME commit — the two halves of one contract, never a model and its copy."
  artifacts:
    - path: "backend/app/db/workflows.py"
      provides: "load_run_phases projecting the parsed failure reason"
      contains: "phase_output_object"
    - path: "backend/app/api/workflow_runs.py"
      provides: "WorkflowRunPhaseRead carrying failure_reason + step identity"
      contains: "failure_reason"
    - path: "backend/app/models/thread.py"
      provides: "WorkflowPhaseState carrying the same facts for the chat panel"
      contains: "failure_reason"
    - path: "frontend/src/types/index.ts"
      provides: "Phase gains the DB-backed failure reason, additively"
      contains: "failureReason"
  key_links:
    - from: "backend/app/db/workflows.py:1835"
      to: "backend/app/api/workflow_runs.py"
      via: "output._failure_reason parsed through phase_output_object"
      pattern: "_failure_reason"
    - from: "backend/app/models/thread.py"
      to: "frontend/src/types/index.ts"
      via: "the chat panel's ungated wire"
      pattern: "failure_reason"
---

<objective>
Carry the failure reason and the step's service/action identity from the `workflow_phases` row to
**both** run wire models, through the one shared unwrap, and widen the two client types to match.

Purpose: `BUG-260826-05`'s mechanism is measured and it is neither the emitter nor the executor
(D-214-23). The reason **is** written — `db/workflows.py:1835` puts it at `output._failure_reason`
on every `fail_phase` — and **nothing projects it**: `grep -rn "_failure_reason" frontend/src`
returns zero, so `PhaseCard.classifyFailure` reads `phase.error`, which `types/index.ts:188`
documents as live-SSE-only, finds it empty on any reconciled run, and fires `reason_unknown` **while
the reason sits in the row**. That is D-v2.5-03 exactly.

⚠ **And 38 of 43 failed rows store `output` as a jsonb STRING SCALAR**, so a reader written as
`output["_failure_reason"]` finds it on 2 of 43. The parse is the load-bearing half.

Output: one projection, two widened wire models, two widened client types, and a suite driven RED
against a string-scalar row.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-CONTEXT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-PATTERNS.md
@.planning/reported-bugs/BUG-260826-05-run-failed-reason-empty-for-external-action-failure.md

**PATTERNS.md §3 is this plan's brief** — §3a the parsing idiom (including why `RecursionError` is in
the catch tuple), §3b the parse-once consumer loop, §3c the writer whose shape must be parsed, §3d
the read to widen, §3e the two-wire-model rule and why its one recorded exception does NOT apply
here, §3f where it is tested.

⚠ **Do NOT re-open D-214-23 as research.** It is measured. The reproduction, if a reader wants one,
is `select jsonb_typeof(output), count(*) from workflow_phases where status='failed' group by 1`.
</context>

<interfaces>
<!-- The wire contract this plan DEFINES. Plan 214-11 renders it; plan 214-08's element receives it. -->

Both `WorkflowRunPhaseRead` (`api/workflow_runs.py`) and `WorkflowPhaseState` (`models/thread.py`)
gain the SAME four additive, nullable fields:

- `failure_reason: str | None` — the adapter's / gate's own sentence, from `output._failure_reason`.
  ⚠ `None` means NOT RECORDED. `""` is impossible by construction (`fail_phase` always writes a
  non-empty `reason`); if one is ever read, it is normalised to `None` so the sentinel stays honest.
- `tool_name: str | None` — the action's wire name, from the definition's phase config.
- `capability: str | None` — the native capability, from the same config. Feeds the mark map's
  capability keys.
- `service_name: str | None` — the connection row's display `name`, resolved at read time.
  ⚠ **Computed, never stored** (D-213-02 / D-214-14). Unresolvable ⇒ `None` ⇒ the identity renders
  the action alone. **Never "Unknown service".**

Client mirror:
- `frontend/src/types/index.ts` → `Phase` gains `failureReason?: string | null`, `toolName?`,
  `capability?`, `serviceName?`.
- `frontend/src/lib/api/workflows.ts` → `WorkflowRunPhase` gains the same four in snake_case.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The projection — one parse per row, three facts off it</name>
  <files>backend/app/db/workflows.py, backend/app/api/workflow_runs.py, backend/tests/unit/test_214_failure_reason_seam.py</files>
  <read_first>
    - `backend/app/models/thread.py:48-140` — `phase_output_object`, the ONE read-side unwrap, and
      its verbatim contract at `:76-79`. ⚠ `RecursionError` is in the catch tuple because it inherits
      from `RuntimeError`, not `ValueError`, and the input is model-influenced.
    - `backend/app/api/workflow_runs.py:69-76` — the module import comment stating there is ONE door
      and this module reads through it and never beside it.
    - `backend/app/api/workflow_runs.py:750-778` — the BYTE-PINNED `.select()` string (a `.select("*")`
      is asserted at zero occurrences) and the parse-once loop to extend.
    - `backend/app/db/workflows.py:1370-1386` — `load_run_phases`. ⚠ `output` is ALREADY selected on
      all three readers; **no SELECT widens, no column exists, no migration.**
    - `backend/app/db/workflows.py:1822-1842` — `fail_phase`, the writer whose payload shape is parsed.
    - `backend/app/db/workflows.py:1447-1466` — the WEAKER inline unwrap in
      `get_latest_completed_workflow_run` (bare `except Exception`, a `{"text": raw_output}` fallback).
      ⚠ **Do not copy it and do not extend it.** Note it in the summary as an existing divergence.
    - `backend/tests/unit/test_200_1_phase_output_shape.py:1-60` — the acceptance shape to reproduce,
      including the driven counterfactual rule and the string-scalar fixture builder at `:57-60`.
  </read_first>
  <action>
In `api/workflow_runs.py`'s `:764-778` loop, read `_failure_reason` as the THIRD fact off the
existing single `obj = phase_output_object(row.get("output"))` parse. Do not add a second door and
do not add a second `isinstance(raw, dict)` test anywhere in the module — the import comment at
`:69-76` states why. Normalise a non-`str` or whitespace-only value to `None`.

Widen `WorkflowRunPhaseRead` with the four fields from `<interfaces>`, each carrying a field
description in the same register as `step_count`'s `0`-vs-`null` doc at `lib/api/workflows.ts:554-563`:
say explicitly that `None` means NOT RECORDED and that an empty string is not a synonym for it.

In `db/workflows.py`, leave `load_run_phases`' SQL **byte-unchanged** — the gap is projection, not
selection. If the resumability substrate needs the parsed reason, parse it in the same
`phase_output_object` idiom at the point of consumption, never by widening the SELECT.

Create `backend/tests/unit/test_214_failure_reason_seam.py` in `test_200_1_phase_output_shape.py`'s
two-part shape:
  1. **The RED driver D-214-23(b) demands**: a `failed` row whose `output` is a jsonb STRING SCALAR
     carrying `_failure_reason`. Assert the serializer surfaces the reason. ⚠ **Drive it against the
     pre-fix behaviour first** — a fence that was never driven RED is a fence that could not fire —
     and state the counterfactual as a LOCAL re-statement, never an import of the shipped function.
  2. The object arm, the absent arm (`None`, not `""`), a `None` output, and an unparseable string.
  3. A SQL byte-identity assertion over `load_run_phases`' `.select`/`SELECT` literal against the
     base SHA, in the shape `test_200_1_phase_output_shape.py:41-56` already uses.
  </action>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_214_failure_reason_seam.py -q` exits 0.
    - `grep -c "phase_output_object" backend/app/api/workflow_runs.py` is unchanged from HEAD +0 new call sites (one parse per row is preserved) — verify with `grep -n "phase_output_object(" backend/app/api/workflow_runs.py` returning exactly the pre-existing call sites plus zero.
    - `git diff -- backend/app/db/workflows.py | grep -c "^[-+].*SELECT id, slug"` is 0 — the SELECT moved not one byte.
    - `grep -c 'select("\*")' backend/app/api/workflow_runs.py` is 0.
    - The suite contains a case whose fixture sets `output` to a Python `str` (not a dict) and asserts a non-empty `failure_reason`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_214_failure_reason_seam.py tests/unit/test_200_1_phase_output_shape.py -q</automated>
  </verify>
  <done>The reason reaches `WorkflowRunPhaseRead` from a string-scalar row, driven RED first, with the SELECT byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: The second wire model, in the same commit — and the step's identity on both</name>
  <files>backend/app/models/thread.py, backend/app/api/threads.py, backend/app/api/workflow_runs.py, backend/tests/unit/test_214_failure_reason_seam.py</files>
  <read_first>
    - `backend/app/models/thread.py:196-243` — `WorkflowPhaseState` and, at `:210-218`, the
      TWO-WIRE-MODEL RULE verbatim: *"Widening only the other model would ship a run page with
      durations and a chat panel without them."*
    - `backend/app/models/thread.py:220-234` — `D-200.1-02-A`, the ONE recorded exception, and why it
      does not apply here: nothing renders the failure reason on the chat panel today —
      `PhaseCard.tsx:253` fires `reason_unknown` instead.
    - `backend/app/api/threads.py:1204-1247` — the builder, which today reads
      `declared_phase_measure(r["output"])` directly off the RAW value. Port it to the
      `obj = phase_output_object(...)` parse-once shape FIRST, then read both facts off `obj`.
    - `backend/app/services/connector_service.py:211-245, 455-470` — `ResolvedConnection` (it carries
      `name`, `capability`, `mcp_server_url`; it does NOT carry `service_id`) and `resolve_connection`,
      whose `org_id` has no default.
    - `backend/app/services/harness/grounding.py:1255-1294` — the *"never draw a name the system
      cannot know"* rule this resolution must preserve.
  </read_first>
  <action>
Widen `WorkflowPhaseState` with the identical four fields, in the same commit, and add a comment
beside them recording that `D-200.1-02-A`'s exception was CONSIDERED and does NOT apply, with its
one-sentence reason (nothing on the chat panel renders the reason today; the sentinel fires
instead). ⚠ **Do not paraphrase the rule at `:210-218` — extend the block, keep its words.**

Populate the new fields in `api/threads.py:1234-1247`'s builder. `tool_name` and `capability` come
free from the definition's phase config (the builder already derives `slug_to_type` from it —
derive a `slug_to_config` beside it in the same pass; do not add a second definition read).

`service_name` requires the connection row. Resolve it with **one batched lookup per run**: collect
the distinct `connection_id`s across the definition's `external_action` phases (typically 1–3),
resolve each once, and build a `connection_id → name` map before the row loop. Do NOT resolve inside
the loop. ⚠ **Blocking I/O must not run bare in an async handler** (D-v2.5-01) — if the lookup path
is `supabase-py`, wrap it in `run_in_threadpool` exactly as the sibling capability path already does.

⚠ **An unresolvable connection yields `service_name = None`, and that is correct, not a fallback to
fill in.** Never emit `"Unknown service"`, never emit the capability id, never emit the connection
id. This preserves `grounding.py`'s shipped rule at full strength on a new surface.

Add cases to `test_214_failure_reason_seam.py`: both models carry the same four field names
(assert the two field-name sets agree **mechanically** — PATTERNS §G names the prose rule as the
weakest of the five same-commit pairs and asks for exactly this); an `external_action` phase with a
resolvable connection carries `service_name`; one with a deleted connection carries `None` and no
substitute string; and a non-`external_action` phase carries `None` for all three identity fields.
  </action>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_214_failure_reason_seam.py -q` exits 0.
    - A case asserts `set(WorkflowRunPhaseRead.model_fields) >= {"failure_reason","tool_name","capability","service_name"}` AND `set(WorkflowPhaseState.model_fields) >= {"failure_reason","tool_name","capability","service_name"}`, in one test that fails if either side is widened alone.
    - `grep -rn "Unknown service" backend/app | wc -l` is 0.
    - `grep -c "run_in_threadpool" backend/app/api/threads.py` is ≥ 1 if the connection lookup is a blocking client; otherwise a comment beside the lookup states it is already async.
    - The connection lookup appears exactly once per run: `grep -c "resolve_connection\|_fetch_connection_row" backend/app/api/threads.py` shows the call outside any `for r in phase_rows` block (verify by reading the diff, not by count alone).
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_214_failure_reason_seam.py -q</automated>
  </verify>
  <done>Both wire models carry the same four fields, asserted mechanically; an unresolvable service is `None` with no substitute.</done>
</task>

<task type="auto">
  <name>Task 3: The client types, additively — and the file's OTHER two widenings, up front</name>
  <files>frontend/src/types/index.ts, frontend/src/lib/api/workflows.ts, frontend/src/lib/apiRunFields.fences.test.ts</files>
  <read_first>
    - `frontend/src/types/index.ts:180-195` — `Phase.error`'s doc at `:188`: *"error string from SSE
      terminal errorPayload … Only available for live-streamed runs (not backfilled from DB)"*.
      ⚠ **That sentence stops being the whole truth and must be corrected in place, beside the new
      field, not deleted.**
    - `frontend/src/types/index.ts:1018-1062` — the additive-union comment discipline: state WHY the
      existing members cannot express the new fact and name the compiler-enforced consequence.
    - `frontend/src/lib/api/workflows.ts:534-564` — `WorkflowRunPhase` and `step_count`'s
      `0`-vs-`null` field doc. ⚠ Its sibling rule transfers directly: an EMPTY reason and an ABSENT
      one are different facts and must never collapse under a `?? ""`.
    - `frontend/src/lib/apiRunFields.fences.test.ts` — the shipped, count-gate-PINNED fence suite this
      task extends rather than replaces (a new file under `src/lib` would need a `TARGETS` entry;
      extending a pinned suite needs neither).
  </read_first>
  <action>
Add `failureReason`, `toolName`, `capability` and `serviceName` to `Phase` in `types/index.ts`, and
the four snake_case siblings to `WorkflowRunPhase` in `lib/api/workflows.ts`.

Correct `Phase.error`'s doc **in place, keeping the original sentence beside the correction** in this
repo's recorded style: the SSE field is still live-only; `failureReason` is the DB-backed sibling
that reconciles. Say which one a reader should prefer and why (D-v2.5-03: Realtime is a hint,
reconcile by fetch).

⚠ **This file is `frontend/src/lib/api/workflows.ts`, and TWO LATER PLANS ALSO NEED IT WIDENED.**
Do both here, in this wave, so the file has ONE owner and waves 3 and 4 never touch it:

- `named_failures`' entry type gains `step_name?: string`, `argument?: string | null`,
  `upstream?: string | null` — consumed by plan `214-10`. ⚠ They are OPTIONAL because
  `named_failures` is **POLYMORPHIC across stages** (`PublishGauntlet.tsx`'s docblock rule 4: key
  detection per ENTRY, never a switch on `blocked_stage`); an entry lacking them is a pre-214 or
  other-stage entry and must still render.
- `generateWorkflow`'s request gains `allowed_connection_ids?: string[]` — consumed by plan `214-13`.
  ⚠ Its field doc must state that **absent and `[]` are DIFFERENT**: absent is today's
  unconstrained behaviour; `[]` is the author's decision that no external step may be emitted. An
  `?? []` or `|| undefined` anywhere on that path collapses two facts into one, which is the
  `0`-vs-`null` family of bug this same file already documents at `:554-563`.

⚠ **Type-only additions — no call site changes here.** Plans `214-10` and `214-13` supply the values.

Extend `frontend/src/lib/apiRunFields.fences.test.ts` with: a type-level assertion that
`failureReason` is `string | null | undefined` and not `string` (an absent reason must stay
expressible); a fence asserting no source file in `src/` spells `failureReason ?? ""` or
`failure_reason ?? ""` (the collapse the `0`-vs-`null` rule forbids), assembled at runtime as
`"failureReason " + "?? \"\""` so the fence's own prose cannot satisfy its grep — the 187-24 trap
has fired on this surface twice; and a non-vacuity control asserting the glob resolved > 200 modules.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` produces **no more than 34** errors (the recorded baseline). A higher count is this plan's.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/apiRunFields.fences.test.ts` exits 0 and reports a HIGHER case count than its pinned baseline.
    - `grep -rn 'failureReason ?? ""' frontend/src --include=*.ts --include=*.tsx | wc -l` is 0.
    - `grep -c "failureReason" frontend/src/types/index.ts` is ≥ 2 (field + the corrected `error` doc's cross-reference).
    - The `Phase.error` doc still contains its original sentence verbatim (`grep -c "Only available for live-streamed runs" frontend/src/types/index.ts` is 1).
    - `grep -c "step_name" frontend/src/lib/api/workflows.ts` is ≥ 1 and the three `named_failures` keys are OPTIONAL (`?`), verified by reading the type.
    - `grep -c "allowed_connection_ids" frontend/src/lib/api/workflows.ts` is ≥ 1, and its field doc contains the words `absent` and `empty` (the two-arm rule).
    - `grep -rnE "allowed_connection_ids (\?\?|\|\|)" frontend/src | wc -l` is 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/apiRunFields.fences.test.ts</automated>
  </verify>
  <done>Both client types carry the four fields; `named_failures` and `generateWorkflow` are widened here so no later wave touches this file; the collapse fence is in place with a non-vacuity control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter / gate error text → `output._failure_reason` → two wire models → two UI surfaces | Text that may originate from a third-party server now reaches two client surfaces that never rendered it. |
| `connector_connections.name` → `service_name` on an UNGATED route | `GET /threads/{id}/workflow` carries no canvas gate; the run read does. A connection display name crosses the narrower door for the first time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-02-01 | Information Disclosure | `service_name` on the ungated `GET /threads/{id}/workflow` | mitigate | Only the connection's display `name` crosses — never `mcp_server_url`, never `connection_id`, never any part of `config` or `secret_ciphertext`. Tested by an assertion over the serialized payload's key set AND a value fence asserting no `http`/`@`/`:` -bearing string appears in `service_name` for a URL-shaped connection fixture (Task 2). |
| T-214-02-02 | Tampering | third-party error text rendered as `failure_reason` | mitigate | The value is carried as data and rendered as text by React (no `dangerouslySetInnerHTML` anywhere on the consuming surfaces). Plan `214-11` owns the render; this plan asserts the wire is `str \| None` and never HTML-typed. Tested by a case asserting a `<script>`-bearing reason round-trips as an escaped string, not as markup. |
| T-214-02-03 | Denial of Service | `json.loads` on a model-influenced `output` string | mitigate | `phase_output_object` already catches `RecursionError` alongside `(ValueError, TypeError)` — a deeply nested `"[[[[…]]]]"` blows the stack and escapes a `ValueError`-only catch. This plan reads THROUGH that helper and adds no second parser. Tested by a nesting-depth case (Task 1). |
| T-214-02-04 | Spoofing | a fabricated service name | mitigate | An unresolvable connection yields `None`, never a substitute string. Asserted by `grep -rn "Unknown service" backend/app` returning 0 and by the deleted-connection case (Task 2). |
| T-214-02-05 | Repudiation | the two models silently disagreeing | mitigate | The field-set agreement is asserted MECHANICALLY rather than left to `models/thread.py:210-218`'s prose rule — PATTERNS §G names that pair as the weakest of the five and asks for exactly this. |
| T-214-02-SC | Tampering | package installs | mitigate | This plan installs nothing. Any dependency request stops the plan and raises a blocking human checkpoint. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit/test_214_failure_reason_seam.py tests/unit/test_200_1_phase_output_shape.py -q` — green.
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` — no more than 68 failed.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/apiRunFields.fences.test.ts` — green, count up.
- Whole-gate reading, **paired with the per-file delta above rather than standing alone**:
  `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root, verdict line
  verbatim. ⚠ `count gate OK` is not reliably reachable on demand; if it reds, capture failing
  filenames from the gate's persisted JSON **before** re-running anything and check each against
  `git diff --numstat`.
</verification>

<how_wed_know_this_failed>
From CONTEXT.md's `<failure_modes>` — this plan owns #6:

6. **The panel says "Failure reason not captured by the backend" while chat shows the reason** — the
   two surfaces disagreeing at the same moment. SC#5 unmet. This plan's half is the wire; the render
   is `214-11`. The mechanical guard is the two-model field-set agreement test in Task 2 — the
   disagreement `models/thread.py:210-218` was written to prevent.

Additionally, and specific to this plan: **the fix ships green and reads empty in production**
because it was written as `output["_failure_reason"]` and driven only against an object fixture.
38 of 43 failed rows are string scalars. The RED driver in Task 1 is the only thing that catches it.
</how_wed_know_this_failed>

<g5_disposition>
Re-derived from git 2026-08-28 with CLAUDE.md's recipe (six-digit dated-quick-task buckets subtracted):

| File | Ledger cell | **Measured** | Verdict |
|---|---|---|---|
| `backend/app/db/workflows.py` | 43 / 21 / 2194 | **47 / 24 / 2564** | ⚠ **STALE — G-5 FIRES** |
| `backend/app/api/workflow_runs.py` | 7 / 5 / 776 | **9 / 7 / 888** | ⚠ **STALE — G-5 FIRES** |
| `backend/app/models/thread.py` | 14 / 9 / 319 | **(re-derive at execute time)** | fires |
| `frontend/src/types/index.ts` | 70 / 56 / 1154 | **70 / 57 / 1154** | ⚠ **STALE — fires; absent at 56 phases when its row was added** |

**Disposition — ARGUED, not asserted.** All four are *honoured by construction*, and the argument is
the same in each case and is checkable rather than rhetorical: **this plan's diff on every one of
them is purely ADDITIVE and structurally bounded.** `db/workflows.py`'s SQL moves not one byte (a
byte-identity assertion enforces it); `workflow_runs.py` gains reads off an EXISTING single parse and
adds no second door (asserted by the `phase_output_object(` call-site count); `models/thread.py` and
`types/index.ts` gain nullable fields under the shipped additive-field discipline. **No new
responsibility enters any of the four**, so the extraction each is owed is not made harder by this
plan — which is the only honest form of that claim.

⚠ `api/workflow_runs.py` and `models/thread.py` cross their thresholds here; their rows and their
`docs/HOT-FILE-LEDGER.md` sections are written in plan `214-15`'s single ledger commit, which carries
both halves of the same-commit sync rule.
</g5_disposition>

<success_criteria>
- A `failed` phase whose `output` is a jsonb string scalar surfaces its reason on BOTH wire models.
- The RED driver was observed failing against the pre-fix source before the fix landed.
- `load_run_phases`' SQL is byte-identical to HEAD.
- The two models' new field sets are asserted equal by a test that fails if either is widened alone.
- No file in `frontend/src` collapses an absent reason to `""`.
- An unresolvable connection yields `service_name: null` with no substitute string anywhere in `backend/app`.
</success_criteria>

<output>
Create `.planning/phases/214-a-step-names-its-service-and-its-action/214-02-SUMMARY.md` when done.
Record the exact four wire field names and their null semantics — plans `214-08` and `214-11` read
them, and plan `214-14`'s seam audit checks both sides against this list.
</output>
