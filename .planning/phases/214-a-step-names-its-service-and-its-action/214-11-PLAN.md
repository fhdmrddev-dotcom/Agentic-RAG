---
phase: 214-a-step-names-its-service-and-its-action
plan: 11
type: execute
wave: 3
depends_on: ["214-02", "214-08"]
files_modified:
  - frontend/src/components/panel/PhaseCard.tsx
  - frontend/src/components/panel/PhaseTimeline.tsx
  - frontend/src/components/chat/RunCard.tsx
  - frontend/src/components/panel/PendingAskCard.tsx
  - frontend/src/components/workflows/RunSpine.tsx
  - frontend/src/components/workflows/RunStepList.tsx
  - frontend/src/components/workflows/RunTranscript.tsx
  - frontend/src/pages/WorkflowRunPage.tsx
  - frontend/src/components/panel/PhaseCard.test.tsx
  - frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
  - frontend/src/components/workflows/StepIdentity.coverage.test.tsx
autonomous: true
requirements: [STEP-04, STEP-05]

must_haves:
  truths:
    - "D-214-16: EVERY surface a run appears on shows the service and the action — the panel's `PhaseCard`/`PhaseTimeline`, `WorkflowRunPage`'s `RunSpine` + `RunStepList` + `RunTranscript`, the chat `RunCard`, and the approval pause. Coverage is asserted PER SURFACE, not once."
    - "D-214-23: `classifyFailure`'s CONDITION narrows to read the DB-backed reason; its WORDS do not change, and the `reason_unknown` sentinel is not weakened."
    - "A failed step shows the adapter's OWN sentence, not a generic one — and the panel and chat no longer disagree at the same moment."
    - "`SEED-206` is closed by coverage of all five surfaces; a partial answer would leave the seed live for the next phase to rediscover."
    - "No wire id reaches any run surface — `send_email` / `create_ticket` / `post_message` / `ask_question` / `external_action`."
  artifacts:
    - path: "frontend/src/components/panel/PhaseCard.tsx"
      provides: "the identity + the narrowed sentinel condition"
      contains: "failureReason"
    - path: "frontend/src/components/workflows/StepIdentity.coverage.test.tsx"
      provides: "the per-surface coverage assertion SEED-206 requires"
      contains: "RunSpine"
  key_links:
    - from: "frontend/src/components/panel/PhaseCard.tsx"
      to: "frontend/src/components/workflows/StepIdentity.tsx"
      via: "the shared element, mounted with props the surface already holds"
      pattern: "StepIdentity"
    - from: "frontend/src/pages/WorkflowRunPage.tsx"
      to: "frontend/src/components/workflows/RunSpine.tsx"
      via: "the existing titleOf prop seam, extended with the service"
      pattern: "titleOf"
---

<objective>
Mount the shared step-identity element on all five run surfaces, and narrow the panel's
unknown-reason sentinel so it stops firing when the reason is known.

Purpose: SC#4's second half and SC#5. `SEED-206`'s own warning is that **a partial answer leaves the
seed live for the next phase to rediscover** — *"do NOT wait for a catalog; the spine gap is live
TODAY on shipped surfaces."* And `BUG-260826-05`'s panel/chat disagreement is closed here, on the
render side of the wire plan `214-02` built.

⚠ **`PhaseCard.tsx:246-256`'s `reason_unknown` block is CORRECT and must not be weakened.** It is an
honesty mechanism. The fix is to stop it firing when the reason is known, by carrying the reason —
**never by removing the sentinel.** Its condition narrows; its words do not change.

Output: five mounted surfaces, one narrowed condition, and a coverage suite that asserts per surface.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
⚠ **This plan does NOT edit `scripts/vitest-count-gate.cjs`.** Every `BASELINE` pin for this phase is
collected in plan `214-15`; record this plan's printed `— N new` figures in its SUMMARY instead.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-CONTEXT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-PATTERNS.md
@.planning/sketches/216-the-mark-and-the-action-everywhere/BUILD-CONTRACT.generated.md
@.planning/seeds/SEED-206-the-service-mark-belongs-on-every-step-surface-not-only-the-canvas.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-02-SUMMARY.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-08-SUMMARY.md

**PATTERNS.md §3g and §4d are this plan's brief** — §3g the ONE line in `classifyFailure` that
changes, §4d the `titleOf` prop seam and the measured warning that `PhaseCard`/`PhaseTimeline` have
**no `titleOf`**, so the element must take props or the surfaces will disagree.
</context>

<interfaces>
<!-- Consumed, from plans 214-02 and 214-08. Do not re-derive either. -->

From `214-02`: both wire models carry `failure_reason`, `tool_name`, `capability`, `service_name`,
all nullable, with `null` meaning NOT RECORDED. Client mirrors: `Phase.failureReason` / `.toolName` /
`.capability` / `.serviceName`, and `WorkflowRunPhase`'s snake_case siblings.

From `214-08`: `<StepIdentity shape action service size />`, where `size` is one of
`"row" | "chip" | "canvas" | "spine"` and `service: null` renders the action alone.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The sentinel's condition narrows — its words do not</name>
  <files>frontend/src/components/panel/PhaseCard.tsx, frontend/src/components/panel/PhaseCard.test.tsx</files>
  <read_first>
    - `frontend/src/components/panel/PhaseCard.tsx:236-256` — `classifyFailure`. ⚠ **`raw` at `:237` is
      the ONE line that changes**: `(phase.error ?? "").trim()` becomes
      `(phase.error ?? phase.failureReason ?? "").trim()` or equivalent. The `reason_unknown` block at
      `:246-256` is **untouched**.
    - `frontend/src/components/panel/PhaseCard.tsx:246-256` — the sentinel's exact sentence and its
      `where: \`phase: ${slug} · error field was empty\`` clause. ⚠ **That clause becomes literally
      false once a second source exists** — decide whether it is re-worded, and if it is, state the
      new wording's reason. PATTERNS §3g raises this explicitly; do not leave it undecided.
    - `frontend/src/components/workflows/stepIdentityVocabulary.ts` (plan `214-03`) —
      `FAILED_REASON_LABEL` and `FAILED_REASON_UNKNOWN`, the latter character-identical to the shipped
      sentence and bound to it by a `?raw` assertion.
    - `frontend/src/types/index.ts` — `Phase.error`'s corrected doc (plan `214-02`) explaining which
      of the two fields to prefer and why (D-v2.5-03).
  </read_first>
  <action>
Narrow `classifyFailure`'s condition to read `phase.error` first (the live SSE value, freshest during
a stream) and fall back to `phase.failureReason` (the DB-backed value, present on any reconciled or
reloaded run). ⚠ **Order matters and must be commented**: a live run has the SSE value first; a
reconciled one has only the DB value. Reversing them would show a stale reason mid-stream.

⛔ **Change nothing else in `classifyFailure`.** The `reason_unknown` branch, its sentence and its
classification order stay. Sketch 216 §4: *"Its condition narrows; its words do not change."*

Take the `where`-clause decision explicitly. Recommended: re-word it to name both sources
(`error field and failure reason were both empty`) so the diagnostic stops asserting something false,
and record in a comment that the SENTENCE the user reads is unchanged — only the `where` diagnostic
moved. ⚠ If the re-word touches a character-identity assertion, that assertion must be updated in the
SAME commit and the change recorded, not absorbed.

Mount `StepIdentity` on `PhaseCard` for an `external_action` phase, passing `shape` from the phase's
`capability` / `toolName`, `action` from `toolName`, `service` from `serviceName`, `size="row"`.
Render `FAILED_REASON_LABEL` above the reason on a failed step.

Update `PhaseCard.test.tsx`: a case seeding `failureReason` and `error: undefined` asserting the
verbatim reason renders and the sentinel does **not** (sketch 216 #12, #13); a case seeding neither
asserting the sentinel renders with its **character-identical** sentence; a case seeding BOTH
asserting `error` wins; and a case asserting an `external_action` phase renders the identity while a
`llm_single` phase renders none.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/PhaseCard.test.tsx` exits 0 and its per-file count does **not decrease** from its pin of 41.
    - `grep -c "Failure reason not captured by the backend" frontend/src/components/panel/PhaseCard.tsx` is 1 and the sentence is byte-identical to HEAD (`git diff` shows zero changes on that string).
    - `git diff -- frontend/src/components/panel/PhaseCard.tsx | grep -c "^[-+].*reason_unknown"` is 0 — the branch itself was not edited.
    - A case asserts that with `failureReason` set and `error` unset, `FAILED_REASON_UNKNOWN` appears 0 times and the seeded reason appears ≥ 1 time.
    - A case asserts that with both unset, `FAILED_REASON_UNKNOWN` appears exactly once.
    - The `where`-clause decision is recorded in a comment naming both sources.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/PhaseCard.test.tsx</automated>
  </verify>
  <done>The sentinel fires only when both sources are empty, its sentence is byte-identical, and the panel now shows the adapter's own reason.</done>
</task>

<task type="auto">
  <name>Task 2: Four more surfaces, each through the same element</name>
  <files>frontend/src/components/panel/PhaseTimeline.tsx, frontend/src/components/chat/RunCard.tsx, frontend/src/components/panel/PendingAskCard.tsx, frontend/src/components/workflows/RunSpine.tsx, frontend/src/components/workflows/RunStepList.tsx, frontend/src/components/workflows/RunTranscript.tsx, frontend/src/pages/WorkflowRunPage.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowRunPage.tsx:983-991` — `titleBySlug` / `titleOf`, and its passes to
      `RunSpine` / `RunStepList` / `RunTranscript` at `:1476, :1506, :1515, :1557`.
      ⭐ **This is the seam to extend**: add a sibling `serviceOf(slug)` built the same way, from the
      run-phase wire's `service_name`. The caller resolves; the component renders (D-214-14's rule).
    - `frontend/src/components/workflows/RunSpine.tsx:30, 184, 312-319` — the `titleOf` prop and its
      docblock: *"the step's HUMAN NAME — the page's `titleOf`, the same `nodeTitle` the canvas
      paints."*
    - `frontend/src/components/workflows/RunStepList.tsx:105, 164, 221` and
      `frontend/src/components/workflows/RunTranscript.tsx:349, 616` — the same seam.
    - `frontend/src/components/panel/PhaseTimeline.tsx` — mounts `PhaseCard`; it reads `Phase` from
      `StreamsProvider` and has **no `titleOf`**. ⚠ PATTERNS §4d: this is exactly why the element takes
      props — the two surfaces must not resolve differently.
    - `frontend/src/components/chat/RunCard.tsx:1-20` — the `providerLogo` mount at `:4`, the shape a
      cross-surface mark import takes on this file.
    - `frontend/src/components/panel/PendingAskCard.tsx` — the approval pause, sketch 216's **fifth**
      surface. ⚠ D-213-14: it shows and never records; this plan adds identity, not a receipt field.
    - `.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md` §1 and §4 — one source
      for marks; nothing net-new drawn; the top-right corner is claimed.
  </read_first>
  <action>
Extend `WorkflowRunPage`'s resolver seam with `serviceOf(slug)` and `actionOf(slug)`, built from the
run-phase wire exactly as `titleBySlug` is built from `specs`. Pass them to `RunSpine`,
`RunStepList` and `RunTranscript` beside `titleOf`, and mount `StepIdentity` in each — `size="spine"`
on the spine, `size="row"` on the list, `size="chip"` in the transcript.

Mount `StepIdentity` on `PhaseTimeline` (via `PhaseCard`, already done in Task 1 — verify the timeline
renders it rather than assuming the child does), on chat `RunCard`, and on `PendingAskCard`.

⚠ **Every mount passes props; no surface resolves anything.** A surface that reached for the
connection list on its own would give a different answer than the wire, and the two would disagree at
the same moment — the exact failure `models/thread.py:210-218` exists to prevent, one layer up.

⚠ **`service === null` renders the action alone** on every surface. No placeholder, no id, no slug.

⚠ **Draw no new mark and claim no new corner.** `icon-convention.md` §4: the top-right is the
governance seal's; the word-badge carries no glyph. If a surface seems to need a net-new mark, **flag
it as a proposal in the summary** rather than drawing it.
  </action>
  <acceptance_criteria>
    - `grep -c "StepIdentity" frontend/src/components/panel/PhaseCard.tsx frontend/src/components/chat/RunCard.tsx frontend/src/components/panel/PendingAskCard.tsx frontend/src/components/workflows/RunSpine.tsx frontend/src/components/workflows/RunStepList.tsx frontend/src/components/workflows/RunTranscript.tsx` returns ≥ 1 for **every** file.
    - `grep -rn "connectionMark\|CONNECTION_MARK" frontend/src/components/panel frontend/src/components/chat frontend/src/pages/WorkflowRunPage.tsx | wc -l` is 0 — no surface reaches past the element to the map.
    - `grep -rnE "fetch\(|useQuery|listConnections" frontend/src/components/workflows/RunSpine.tsx frontend/src/components/workflows/RunStepList.tsx frontend/src/components/workflows/RunTranscript.tsx frontend/src/components/panel/PhaseCard.tsx | wc -l` is 0 — no surface resolves anything.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowRunPage.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx` exits 0 with no per-file decrease.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowRunPage.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx</automated>
  </verify>
  <done>All five surfaces mount the one element with props, none resolves anything for itself, and no new mark was drawn.</done>
</task>

<task type="auto">
  <name>Task 3: The coverage assertion SEED-206 asks for — per surface, not once</name>
  <files>frontend/src/components/workflows/StepIdentity.coverage.test.tsx</files>
  <read_first>
    - `.planning/sketches/216-the-mark-and-the-action-everywhere/BUILD-CONTRACT.generated.md` §2 —
      invariant **#3**: *"⭐ all five surfaces present — panel `PhaseCard`, chat `RunCard`, `RunSpine`,
      `RunStepList`, the approval pause. ⚠ `SEED-206`'s warning: a **partial** answer leaves the seed
      live. Assert per surface, not once."* Plus #4, #7, #12, #13.
    - `.planning/seeds/SEED-206-the-service-mark-belongs-on-every-step-surface-not-only-the-canvas.md`
      — the measured ClickUp hole and the seed's own closure condition.
    - `frontend/src/components/workflows/ExternalActionSection.test.tsx:296-334` — the tree-wide `?raw`
      glob sweep **with its non-vacuity control first**, and the runtime-assembled-needle remedy for
      the 187-24 trap.
  </read_first>
  <action>
Create a dedicated coverage suite. ⚠ **It lives in `src/components/workflows/` deliberately** — that
directory is a `TARGETS` entry, so the suite runs the moment it exists without any gate edit.

Its cases:
1. **Per-surface presence.** Render each of the five surfaces with an `external_action` fixture and
   assert `StepIdentity`'s marker node is present. ⚠ **Five separate assertions, not a loop over a
   list of four** — a list is the thing that goes out of sync, which is what D-214-16 means by
   *"coverage should be a consequence of one component existing, not a list kept in sync."* Write the
   five explicitly so a sixth surface's absence is visible as an absence.
2. **A structural companion to #1**: a `?raw` glob over `src/components/{panel,chat,workflows}` and
   `src/pages`, asserting that every module which renders a phase's failure or a phase's title AND
   matches `external_action` also imports `StepIdentity`. With a **non-vacuity control** asserting the
   glob resolved > 200 modules.
3. **#4** — the rendered `innerHTML` of each surface contains none of `send_email`, `create_ticket`,
   `post_message`, `ask_question`, `external_action`.
4. **#7** — an SMTP shape and an unmapped service (ClickUp) both take the neutral mark on a RUN
   surface, and no Gmail mark appears.
5. **#12/#13** — a failed step shows the adapter's own sentence verbatim; a step with no reason shows
   the sentinel, quieter and exactly once.
6. **#8's run-surface half** — the approval pause lists arguments from all three sources. ⚠ The
   backend half is plan `214-06`'s; this asserts the RENDER does not drop any of the three.
7. **#9 and #11, both absence assertions on the pause** — the argument list carries **no per-argument
   source annotation** (rejected under D-214-15 as copy weight: the `dd` carries text only), and the
   pause contains **no countdown, no timer and no `role="progressbar"`**. ⚠ The second is Phase 213's
   rule and its reason is not cosmetic: **a person's decision time is unknowable**, so a timer on an
   approval surface either lies or pressures. Assert both with `queryBy*` returning null, and pair
   them with a positive control that the pause itself rendered — an absence assertion over an
   unmounted component is free.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/StepIdentity.coverage.test.tsx` exits 0 with ≥ 12 cases.
    - The per-surface block contains five separately-named `it(...)` cases, one per surface — verify with `grep -c "^\s*it(" ` on that describe block returning ≥ 5 and by reading the names.
    - The structural sweep's non-vacuity control asserts `> 200` modules and appears before its absence assertions.
    - The wire-id needles are assembled at runtime; `grep -c "send_email" frontend/src/components/workflows/StepIdentity.coverage.test.tsx` counts only occurrences inside runtime concatenations (read the file to confirm; no bare literal in prose).
    - The ClickUp case asserts a non-empty neutral `svg.innerHTML` and no vendor mark.
    - The approval-pause case asserts three distinct argument names in one render, AND asserts zero `progressbar` roles, zero timer nodes and no per-argument source annotation — each paired with a positive control that the pause mounted.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/StepIdentity.coverage.test.tsx</automated>
  </verify>
  <done>Five surfaces asserted individually, a structural sweep backs the list, and the neutral-mark and three-source cases are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter / MCP-server failure text → five rendered surfaces | Text that may originate from a third-party server is now displayed verbatim on the panel, the run page and chat. |
| a connection display name → five rendered surfaces | Same, for admin-supplied text. |
| the `reason_unknown` sentinel | ⚠ **An honesty mechanism.** Weakening it — or letting it fire when the reason is known — trains readers to distrust it, which costs more than the bug it hides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-11-01 | Tampering | third-party failure text rendered as markup | mitigate | React text rendering throughout; a `?raw` fence asserts `dangerouslySetInnerHTML` appears zero times across the six touched components. Tested by a case seeding a `<script>`-bearing reason and asserting it renders escaped. |
| T-214-11-02 | Spoofing | a weakened sentinel implying a reason exists when none does | mitigate | The `reason_unknown` branch is untouched (`git diff` asserted at 0 changed lines) and its sentence is byte-identical, bound to the vocabulary by plan `214-03`'s `?raw` assertion. Both the fires and does-not-fire arms are tested. |
| T-214-11-03 | Information Disclosure | a wire id or a slug on a run surface | mitigate | Invariant #4 asserted per surface, with runtime-assembled needles. |
| T-214-11-04 | Repudiation | two surfaces disagreeing about the same step at the same moment | mitigate | Every surface takes PROPS from the same wire; no surface resolves. Asserted by the zero-fetch and zero-`connectionMark`-import criteria in Task 2. ⚠ This is `models/thread.py:210-218`'s rule applied at the render layer. |
| T-214-11-05 | Spoofing | a borrowed vendor mark for an unmapped service | mitigate | The ClickUp case on a RUN surface (Task 3 #7) — `SEED-206`'s measured hole, closed by the neutral mark rather than by adding an icon. |
| T-214-11-06 | Information Disclosure | the approval pause's arguments reaching a record | accept, unchanged | D-213-14 stands: shown once, recorded never. This plan adds identity to `PendingAskCard` and **no receipt field**; plan `214-06` carries the backend assertion. |
| T-214-11-SC | Tampering | package installs | mitigate | This plan installs nothing. ⛔ **Adding an icon package to cover an unmapped service is out of scope.** |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel src/pages/WorkflowRunPage.test.tsx src/components/workflows/StepIdentity.coverage.test.tsx` — green.
- Per-file deltas recorded for `PhaseCard.test.tsx` (pin 41), `PhaseTimeline.test.tsx` (pin 35) and
  `WorkflowRunPage.test.tsx` — **none may decrease**.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root, verdict line
  verbatim, **paired with those deltas rather than standing alone**.
  ⚠ Three of SEED-171's five known-flaky suites sit in this blast radius. **If the gate reds: capture
  failing filenames from its persisted JSON BEFORE re-running anything**, check each against
  `git diff --numstat`, and if a named file is byte-unchanged and on SEED-171's list, record it as an
  observation — *"provably unmodified"*, never *"fine"*. **Do not reach for the worker cap**; that
  remedy is measured not to fix these.
- ⚠ **This plan does not edit `scripts/vitest-count-gate.cjs`.** Its new suite lives under a `TARGETS`
  directory entry and runs as `new`; record the printed figure for plan `214-15` to pin.
</verification>

<how_wed_know_this_failed>
From CONTEXT.md's `<failure_modes>` — this plan owns #6 and the render half of #5:

6. **The panel says "Failure reason not captured by the backend" while chat shows the reason** — the
   two surfaces disagreeing at the same moment. SC#5 unmet. Guarded by Task 1's three-arm case set,
   and by the fact that both surfaces read the same wire fields.
5. **The pause omits the service on an MCP row when the connection has a name** — this plan's half is
   that `PendingAskCard` renders the identity it is given; `214-06` supplies it.

Also specific to this plan: **`SEED-206` survives its own closure.** Four surfaces covered and one
missed is a partial answer, and the seed says so in its own words. The five separately-named cases —
rather than a loop over a list — are what make an absence visible as an absence.

And: **the sentinel is weakened rather than narrowed.** If `reason_unknown` is deleted, softened, or
made conditional on something other than emptiness, the surface stops being able to say *the backend
recorded nothing*, and every future silent failure reads as a success. The `git diff` criterion on
that branch is the guard.
</how_wed_know_this_failed>

<g5_disposition>
Re-derived from git 2026-08-28 with CLAUDE.md's recipe (dated buckets subtracted):

| File | Ledger cell | **Measured** | Verdict |
|---|---|---|---|
| `frontend/src/components/panel/PhaseCard.tsx` | 13 / 9 / 623 | **13 / 9 / 623** | fires — cell CURRENT |
| `frontend/src/components/panel/PhaseTimeline.tsx` | 8 / 6 / 370 | **8 / 6 / 370** | fires — ⚠ ledger says *absent from BOTH for its ENTIRE LIFE* |
| `frontend/src/components/chat/RunCard.tsx` | 21 / 9 / 608 | **21 / 9 / 608** | fires |
| `frontend/src/pages/WorkflowRunPage.tsx` | 25 / 8 / 1601 | **26 / 8 / 1601** | ⚠ **STALE — fires** |
| `frontend/src/components/workflows/RunTranscript.tsx` | 6 / 2 / 652 | **7 / 3 / 652** | ⚠ **STALE — now FIRES; its cell reads *young (2 phases)*** |
| `frontend/src/components/panel/PendingAskCard.tsx` | 10 / 5 / 629 | **(re-derive at execute time)** | fires |
| `frontend/src/components/workflows/RunSpine.tsx` | 3 / 2 / 372 | **4 / 2 / 372** | not firing (2 phases) — cell stale in commits |
| `frontend/src/components/workflows/RunStepList.tsx` | 1 / 1 / 298 | **1 / 1 / 298** | young |

⚠ **`RunTranscript.tsx` crossing from *young* to *firing* is a finding, not a footnote** — its cell
says `no (2 phases)` and the measurement says 3. That is the ledger's own repeated lesson: a row that
is present and WRONG answers the auditor with a pass and stops the audit.

**Disposition for all eight: honoured by construction, and the argument is the same one and is
structural.** This plan's diff on every surface is **one import and one mounted element**, plus — on
`PhaseCard` alone — **one changed expression** and, on `WorkflowRunPage`, two `useMemo`/`useCallback`
resolvers built beside the shipped `titleBySlug`/`titleOf` pair in its own established shape. **No
surface gains a fetch, a store subscription, a context read or a branch on connection shape** — all of
which are asserted by the zero-fetch and zero-`connectionMark`-import acceptance criteria, so the
claim is falsifiable rather than rhetorical. ⭐ **That is the whole point of D-214-16**: the identity
becomes one component's responsibility instead of six surfaces' worth of new code, which makes these
files *less* likely to need an extraction than before, not more.

All eight rows and their `docs/HOT-FILE-LEDGER.md` sections are synced in plan `214-15`'s single
ledger commit.
</g5_disposition>

<success_criteria>
- All five run surfaces render the shared element, asserted per surface in five separately-named cases.
- No surface resolves the service for itself — zero fetches, zero mark-map imports outside the element.
- A failed step shows the adapter's own sentence; the sentinel fires only when BOTH sources are empty.
- The sentinel's sentence is byte-identical and its branch shows zero changed lines.
- An unmapped service takes the neutral mark on a run surface.
- No wire id and no slug appears on any run surface.
- The approval pause renders arguments from all three sources.
</success_criteria>

<output>
Create `.planning/phases/214-a-step-names-its-service-and-its-action/214-11-SUMMARY.md` when done.
Record: the printed `— N new` figure for the coverage suite (plan `214-15` pins it), the
`where`-clause decision taken, the per-file deltas for the three pinned suites, and any SEED-171
flake observed with the filenames captured **before** any re-run.
</output>
