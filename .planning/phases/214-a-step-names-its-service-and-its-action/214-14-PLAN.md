---
phase: 214-a-step-names-its-service-and-its-action
plan: 14
type: execute
wave: 4
depends_on: ["214-01", "214-02", "214-03", "214-04", "214-05", "214-06", "214-07", "214-08", "214-09", "214-10", "214-11", "214-12", "214-13", "214-16"]
files_modified:
  - backend/app/models/user_settings.py
  - docs/OPERATOR.md
  - backend/tests/integration/test_214_argument_seams.py
  - backend/tests/unit/test_214_flag_cold_default.py
autonomous: true
requirements: [STEP-01, STEP-02, STEP-03, STEP-04, STEP-05, STEP-06]

must_haves:
  truths:
    - "D-214-19: `visual_workflow_canvas` COLD-READS as `everyone`. The criterion is verified against the cold default in `_GOVERNED_FEATURES`, not against a flipped database — Phase 209's exact failure."
    - "D-214-19's second half: `live_connectors` does NOT flip. It stays a separately armed decision."
    - "D-214-24: no RESEARCH.md and no VALIDATION.md are owed; the acceptance bars are the four generated BUILD-CONTRACTs and CONTEXT.md's failure-mode list."
    - "Every value one plan WRITES and another READS is named on BOTH sides and proven by a test that mocks NEITHER side."
    - "⭐ THE SEAM LIST IS DERIVED MECHANICALLY FROM THE TREE, NEVER RECALLED FROM THE PLANS. For every field name the phase introduces or widens, grep the producer and every consumer, and require that EVERY FILE ON THE PATH BETWEEN THEM appears in some plan's `files_modified`. A path file no plan owns is named as a finding even when it needs no change."
    - "⚠ A HAND-BUILT OBJECT CONSTRUCTION ON A SEAM PATH IS WHERE THIS DEFECT HIDES. A widened type propagates NOTHING through a field-by-field constructor, and the build stays green — `StreamsProvider.tsx::reconcilePhases` is the measured instance."
    - "⚠ TWO PREDICATES SHARING A SCHEMA ARGUMENT PROVE `f(s) == g(s)` AND ARE BLIND TO `s_gate != s_executor`. Schema PROVENANCE is its own seam and gets its own case."
    - "D-214-12: an already-published workflow with an unsatisfiable step still RUNS — the gate binds the next publish, nothing retroactive."
  artifacts:
    - path: "backend/tests/integration/test_214_argument_seams.py"
      provides: "the seven cross-plan seam tests, each mocking neither side"
      contains: "_failure_reason"
      min_lines: 260
    - path: "backend/app/models/user_settings.py"
      provides: "the cold default flipped on exactly one key"
      contains: "visual_workflow_canvas"
    - path: "backend/tests/unit/test_214_flag_cold_default.py"
      provides: "the COLD read assertion Phase 209 did not have"
      contains: "live_connectors"
  key_links:
    — from: "backend/app/services/connectors/args.py"
      to: "backend/app/services/harness/reachability.py"
      via: "the gate and the executor agreeing on one config, nothing mocked"
      pattern: "unsatisfiable_arguments"
    — from: "backend/app/services/connectors/args.py::schema_for_bound_tool"
      to: "phase_types.py + publish_service.py + harness_engine.py"
      via: "one accessor, three callers — provenance identical by construction"
      pattern: "schema_for_bound_tool"
    - from: "backend/app/models/user_settings.py"
      to: "docs/OPERATOR.md"
      via: "deployment-artifact parity in the same commit"
      pattern: "visual_workflow_canvas"
---

<objective>
Flip the one flag against its **cold default**, and prove the four cross-plan seams with tests that
mock neither side.

Purpose: neither of these is housekeeping and the phase fails without them.

1. ⚠ **Phase 209's 16/16 browser drive ran against a flag-flipped database and shipped nothing to
   anyone.** D-214-19 exists because of that, and the test must assert the **cold read**.
2. ⚠ **Phase 204 shipped a disarmed spend cap because one plan wrote `workflow_runs.inputs` and
   another read `workflow_runs.metadata`.** The read failed open **silently** and **106 tests were
   green**, because the two waves ran in parallel and each mocked the other's side. **This phase has
   at least four such seams and they are its highest risk.**

⚠ **The ledger sync, the gate pins, the registers and the G-4 checkpoint are plan `214-15`** — a
checkpoint and its implementation do not share a plan, and the ledger pass must run after this plan's
own suite exists.

3. ⭐ **AND THE FIRST VERSION OF THIS PLAN'S AUDIT MISSED THREE SEAMS, WHICH IS THE FINDING THAT
   SHAPED IT.** The audit was written from the seams the planner already knew about, and a checker
   found three more by grepping the tree: the launcher `inputs` wire (no `inputs` option on
   `postMessage`, no field on `MessageCreate`, no merge in `threads.py`, and a **second** un-owned
   copy of the same dict in `workflow_kickoff.py`); the failure-reason wire's client mirror
   (`lib/api/threads.ts::WorkflowPhaseState`) and its field-by-field mapper
   (`StreamsProvider.tsx::reconcilePhases`); and the executor's / gate's **schema provenance**.
   ⚠ **All three sat between two plans that were each individually correct, and each side's suite
   supplied the other side's half.** So the audit is now DERIVED, not recalled — Task 2a is that
   derivation, and it runs before any test is written.

Output: one flipped cold default, one artifact update, one **derived** seam table, and one seam suite.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
⚠ **SERIALIZE THIS PLAN.** Its integration suite drives the local Postgres. Worktrees isolate files,
not Postgres, and no `files_modified` check can see that interference. It is alone in wave 4 for that
reason.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-CONTEXT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-PATTERNS.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-UAT.md

**All fourteen prior SUMMARYs are required reading** (plans `214-01`..`214-13` and `214-16`) — this plan's seam audit checks the field names
each of them recorded against the other side's reader. **Lay them side by side before writing a test.**

**PATTERNS.md §8 is the flag's brief** — `_GOVERNED_FEATURES` is *the ONE authoritative cold default*,
the flip route is an atomic per-key JSONB `||` merge, and ⚠ **`scripts/check-deploy-drift.sh` cannot
see `feature_visibility` at all** (`grep` returns ZERO), so the artifact half is a documentation
update **enforced by nothing**. Say that in the plan rather than relying on the script.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The cold default flips — on one key, asserted cold, with its artifact</name>
  <files>backend/app/models/user_settings.py, docs/OPERATOR.md, backend/tests/unit/test_214_flag_cold_default.py</files>
  <read_first>
    - `backend/app/models/user_settings.py:1191-1214` — `_GOVERNED_FEATURES`, *"the ONE authoritative
      cold default (an unseeded `feature_visibility` key falls through to it), which is why NO
      migration is needed"*. ⚠ The `visual_workflow_canvas` comment currently **describes the off
      default** and becomes false; the `live_connectors` comment must stop being a copy of it.
    - `backend/app/models/user_settings.py:1295` — `set_feature_visibility`'s atomic per-key JSONB `||`
      merge, and `backend/app/api/admin.py:1055-1077` — the flip route, *"no lost-update clobber"*.
    - `backend/app/dependencies.py:616-695` — `require_canvas`, the 404-before-auth gate this flag
      controls.
    - `docs/OPERATOR.md:137` — Step 3's seed list.
    - `scripts/check-deploy-drift.sh:32-38` — it checks the Step-3 **migration filename** table and
      hard-fails on a rename. ⚠ It does **not** see `feature_visibility`; measured
      `grep -n "feature_visibility\|visual_workflow_canvas" docs/OPERATOR.md scripts/check-deploy-drift.sh`
      returns **ZERO**.
  </read_first>
  <action>
Change `"visual_workflow_canvas": "off"` to `"everyone"` in `_GOVERNED_FEATURES`. ⛔ **`live_connectors`
does NOT change** — D-214-19 is explicit: arming real outbound sending for every user is a bigger
decision than these six criteria describe.

Rewrite the `visual_workflow_canvas` comment so it stops describing an off default and instead records
**why it flipped** (the canvas layer has shipped through 199, 200, 209, 211 and 213 and is the surface
this milestone governs; left off, SC#1/#3/#4 render for nobody). ⚠ **Rewrite `live_connectors`'
comment too** — the two are currently near-copies, and after this change one of them is a lie by
adjacency.

Add `docs/OPERATOR.md` Step-3 coverage for the feature-visibility defaults: a short table naming each
governed feature and its cold default, so a greenfield operator can see what is on before they touch
anything. ⚠ **State in the plan and in the summary that `scripts/check-deploy-drift.sh` cannot enforce
this** — it reads migration filenames only. **A parity obligation enforced by nothing must be named,
or it is a hope.**

Write `backend/tests/unit/test_214_flag_cold_default.py`:
  - ⭐ **the COLD read** — with **no** `feature_visibility` row and no seed, resolve
    `visual_workflow_canvas` and assert `everyone`. This is the assertion Phase 209 did not have.
  - `live_connectors` cold-reads `off` — the second half of D-214-19, asserted so a future flip is
    deliberate.
  - a case asserting `_GOVERNED_FEATURES` has exactly the six known keys (a seventh appearing without
    a decision is the thing to catch).
  - a case asserting `docs/OPERATOR.md` names every key in `_GOVERNED_FEATURES` — the parity the drift
    script cannot check, checked here instead.
  </action>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_214_flag_cold_default.py -q` exits 0.
    - `grep -c '"visual_workflow_canvas": "everyone"' backend/app/models/user_settings.py` is 1 and `grep -c '"live_connectors": "off"' backend/app/models/user_settings.py` is 1.
    - The cold-read case constructs its resolver with **no** `app_settings` row and asserts `everyone` — verify by reading the fixture; a case that seeds the DB first has tested the wrong thing.
    - The OPERATOR.md parity case reads the real file and fails if a `_GOVERNED_FEATURES` key is missing from it.
    - `bash scripts/check-deploy-drift.sh` exits 0 (it is unaffected — record that it is unaffected, not that it passed as evidence).
    - `git diff -- backend/app/models/user_settings.py | grep -c "^[-+].*live_connectors.*everyone"` is 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_214_flag_cold_default.py -q</automated>
  </verify>
  <done>The canvas flag cold-reads `everyone`, `live_connectors` cold-reads `off`, both comments tell the truth, and the artifact gap is named rather than assumed covered.</done>
</task>

<task type="auto">
  <name>Task 2: The cross-plan seam audit — DERIVED from the tree, then proven by tests that mock neither side</name>
  <files>backend/tests/integration/test_214_argument_seams.py</files>
  <read_first>
    - All fourteen `214-NN-SUMMARY.md` files (`214-01`..`214-13` and `214-16`) — each records the exact field/key/prop names its plan
      wrote or read. **This task's first action is to lay them side by side.**
    - `backend/app/db/workflows.py:1835` (`fail_phase`'s `_failure_reason` write) and
      `backend/app/api/workflow_runs.py` + `backend/app/models/thread.py` (the two readers).
    - `backend/app/services/connectors/args.py` (`resolve_arguments` / `unsatisfiable_arguments`) and
      its two callers: `backend/app/services/harness/phase_types.py` and
      `backend/app/services/harness/reachability.py`.
    - `backend/app/services/harness/grounding.py::_external_action_clause` and
      `backend/app/services/harness_engine.py`'s armed branch.
    - `backend/app/services/harness/reachability.py::LINT_CODES` and
      `backend/app/api/workflows.py`'s severity classifier, plus
      `backend/tests/unit/test_182_severity_codes.py`.
    - `backend/tests/unit/test_213_gate55_execution.py` — its second half is the shipped example of
      *"the test that mocks neither side of the seam"*.
    - ⚠ Phase 212's D-1/D-2: **a test that MOCKS THE THING UNDER TEST proves only that the caller is
      self-consistent.** One stub even invented a parameter the real function lacked. 2,796 passing
      tests saw none of it.
  </read_first>
  <action>
**(2a) FIRST, DERIVE THE SEAM LIST MECHANICALLY. This is the first action of the task and it is not
optional.** ⛔ **Do not start from the table below and do not start from the summaries.** For every
field name this phase introduces or widens — at minimum `_failure_reason` / `failure_reason` /
`failureReason`, `tool_name` / `toolName`, `service_name` / `serviceName`, `capability`, `inputs` /
`run_inputs`, `arg_sources`, `tool_args`, `step_name`, `argument`, `upstream`, the five gap-kind
codes, `tool_schemas`, `allowed_connection_ids`, and any other the fourteen summaries name — run:

```
grep -rln "<field>" backend/app frontend/src | grep -v __pycache__
```

and walk producer → every consumer. **Require that EVERY FILE ON THE PATH BETWEEN THEM appears in
some plan's `files_modified`.** Record the derived list in the SUMMARY as a table with, per field: the
producer (file:line), every path file, every consumer, and the owning plan for each. ⭐ **Name any
path file that no plan owns — even if you then decide it needs no change.** An unowned path file
that needed no change is a recorded decision; an unowned path file nobody looked at is Phase 204.

⚠ **PAY PARTICULAR ATTENTION TO HAND-BUILT OBJECT CONSTRUCTIONS.** A widened type propagates
**nothing** through a field-by-field constructor, and the build stays green:
`StreamsProvider.tsx::reconcilePhases` (`:3898-3916` and `:3932-3953`) builds every `Phase`
key-by-key with no spread, which is why a widened wire model reached the panel as `undefined` for a
whole phase in 200-02. **A field-by-field constructor anywhere on a seam path is a place this class of
defect hides** — find them (`grep -n "): Phase =>\|return {" ` over the path files) and say in the
summary which ones you checked.

**(2b) THEN write the audit table**, one row per seam, naming the **exact** identifier on **both**
sides and the file:line of each. The rows below are the ones known at revision time and must all
appear; **the derivation in 2a decides whether there are more.**

| # | value | WRITER (plan · symbol) | READER (plan · symbol) |
|---|---|---|---|
| S-1 | the failure reason | `214-02` · `output["_failure_reason"]` via `fail_phase` | `214-02` serializers → `214-11` · `Phase.failureReason` |
| S-2 | argument source arms | `214-07` · `config.arg_sources` / `config.tool_args` | `214-01` · `resolve_arguments`; `214-05` · `unsatisfiable_arguments` |
| S-3 | service + action strings | `214-02` · `service_name` / `tool_name` on both wire models; `214-06` · the composer's `service_name` param | `214-08`/`214-11` · `StepIdentity` props; `214-06` · the pause |
| S-4 | the five refusal kinds | `214-01` · `ArgumentGapKind` → `214-05` · `LINT_CODES` + `named_failures.code` | `214-03` · `REFUSAL_FOR_KIND` → `214-10` · the headline |
| S-5 | launcher inputs | `214-09` · `onRun`/`onLaunch` → `214-12` · `doRun` → **`214-16` · `postMessage.inputs` → `MessageCreate.inputs` → `threads.py:917` AND `workflow_kickoff.py:500`** | `214-01` · `resolve_arguments`' `ask` arm, via `ctx.inputs` / `workflow_runs.inputs` |
| S-6 | tool `inputSchema` **provenance** | `214-01` · `args.schema_for_bound_tool` | `214-01` executor (2 call sites) · `214-05` `tool_schemas` builder · `214-06` the pause |
| S-7 | the `/workflows/generate` refusal code | `214-13` · `workflow_authoring.py`'s post-emit refusal, returned as `{"ok": False, "error": <code>}` | `useTemplateFirstDraft.ts:580` → `builderStore.setErrorState` → `WorkflowBuilderPage.tsx:2151` |

⚠ **S-5's middle four hops did not exist when this table was first written**, and the two
predicates in S-6 were each given a `schema` by their callers with nobody checking the callers agreed.
Both were found by 2a's derivation. **That is the argument for doing 2a before reading this table.**

**Then prove each one with a test that mocks NEITHER side.** Create
`backend/tests/integration/test_214_argument_seams.py`:

- **S-1** — write a real `failed` phase row through `fail_phase` (both an object `output` and a
  **string-scalar** one), read it back through **both** serializers — `WorkflowRunPhaseRead` **and**
  the `GET /threads/{id}/workflow` builder — and assert the reason arrives on each.
  ⛔ No stubbed pool on either side. ⭐ **The chat-panel transport is the half that was
  missing**: `BUG-260826-05`'s symptom is the PANEL saying *"Failure reason not captured by the
  backend"*, and the panel reads `GET /threads/{id}/workflow`, not the run read.
  ⚠ **This seam does not end at the backend, and its remaining two hops are cross-language, so
  they are asserted where they live rather than duplicated here**: the client mirror and both
  `reconcilePhases` literals are plan `214-02` Task 4's RED-driven cases, and the rendered `PhaseCard`
  built from `reconcilePhases`' real output (not a literal) is plan `214-11` Task 1's. **Assert here
  that those cases exist and pass**, and record the full five-hop chain in this plan's seam table —
  a seam whose two ends are green in two suites that never meet is the Phase 204 shape.
- **S-2/S-5** — ⭐ **the end-to-end argument test the phase cannot ship without.** Author a definition
  whose `send_email` step has `to` = `ask` (declared in `inputs[]`), `subject` = `fixed`, `body` =
  `upstream`; run `unsatisfiable_arguments` (the gate's call) and `resolve_arguments` (the executor's
  call) over the same config with real run inputs and real accumulated outputs, and assert the gate
  returns `[]` **and** the resolver produces all three values. Then flip `to`'s `ask_key` to an
  undeclared name and assert the gate returns exactly `ask_undeclared` **and** the resolver omits `to`
  — the two must agree in both directions. ⚠ **This is CONTEXT failure mode #3 and Phase 204's defect
  class, stated as a test.**
  ⚠ **AND STATE ITS LIMIT IN THE CASE'S OWN DOCSTRING**: it hands ONE schema object to both
  functions, so it proves `f(s) == g(s)` and is **structurally incapable** of detecting
  `s_gate != s_executor`. That half is S-6.
  ⚠ **S-5's launcher half is NOT satisfied by handing `run_inputs` to `resolve_arguments`
  in-process.** Doing so supplies the launcher's side from the test and passes green over a wire that
  does not exist — which is exactly what the first version of this plan did. The launcher half is
  plan `214-16`'s integration suite, which drives a REAL `POST /threads/{id}/messages` and reads
  `workflow_runs.inputs` back out of Postgres. **Cross-reference it here by name and assert its
  existence** (`test_214_launch_inputs_wire.py`, >= 6 cases); do not re-implement it.
- ⭐ **S-6 — THE SCHEMA PROVENANCE CASE.** Obtain the `inputSchema` for one **native** and one
  **MCP** bound tool through **each side's own production path** — the gate's `tool_schemas` builder
  in `publish_service.py`, the executor's obtain in `phase_types.py`, and the pause's in
  `harness_engine.py` — and assert all three are `==` for the same bound tool. Add the native
  control: `descriptors.descriptor_for(cap)["inputSchema"] == _plain_json(get_adapter(cap).INPUT_SCHEMA)`.
  ⛔ Nothing patched, no schema hand-typed. ⭐ **This is the case that catches a
  published-then-fails-at-the-send workflow**, which the S-2 agreement case cannot see.
- ⭐ **S-7 — the generate refusal reaches a screen.** Plan `214-13` Task 3b returns a NEW
  `{"ok": False, "error": <code>}` code and no plan's `files_modified` names a renderer for it. It is
  measured to need none: `useTemplateFirstDraft.ts:580` calls
  `store.getState().setErrorState(result.error, result.detail)` for **any** `ok: false`, and
  `WorkflowBuilderPage.tsx:2151` renders that state. **Assert that path rather than assuming it**:
  drive the refusal through the real client, assert `setErrorState` received the new code, and assert
  the builder renders it. ⚠ If it turns out a component must change, that is a finding for the
  summary and a plan owner must be named — not absorbed here.
- **S-3** — compose the approval sentence through the real `_approval_sentence` with a real
  `ResolvedConnection` (via the injected `fetch_row`, not a patched resolver) and assert the service
  name in the sentence equals the row's `name`; then serialize the same phase through the real run
  read and assert `service_name` equals the same string. **One value, two surfaces, one assertion.**
- **S-4** — assert `set(ArgumentGapKind.__args__) == (LINT_CODES & the five) == set(REFUSAL_FOR_KIND
  keys read from the TS module via a source parse)`. ⚠ A cross-language assertion; if that is
  impractical in pytest, put the TS half in `publishRefusalVocabulary.test.ts` reading `args.py?raw`
  and say so — but **one of the two must exist**, or the pairing is three separate lists.

⛔ **No test in this file may patch `resolve_arguments`, `unsatisfiable_arguments`,
`phase_output_object`, `_external_action_clause` or `resolve_connection`.** Those are the things under
test. Assert that mechanically: a case that reads this file's own source and fails if any of the five
names appears inside a `patch(` call.
  </action>
  <acceptance_criteria>
    - The DERIVED seam list from 2a appears in the SUMMARY before the audit table, with the grep commands that produced it, and names every unowned path file found (or states that none was).
    - The summary names which field-by-field object constructions on the seam paths were checked.
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_214_argument_seams.py -q` exits 0 with ≥ 12 cases.
    - The self-check case exists and passes: no `patch(` in this file names any of the five functions.
    - S-2's agreement case runs BOTH directions (satisfiable ⇒ gate empty AND resolver complete; unsatisfiable ⇒ gate names the kind AND resolver omits the key).
    - S-1 has a string-scalar arm and an object arm, both writing through the real `fail_phase`, and reads back through BOTH the run serializer and the `GET /threads/{id}/workflow` builder.
    - S-1's cross-language hops are cited by name and asserted to pass: `214-02` Task 4's `PhaseReconcile.test.tsx` cases and `214-11` Task 1's `reconcilePhases`-sourced `PhaseCard` case. ⚠ Cited and RUN — not assumed from a summary.
    - S-3 asserts one string equal on two independent code paths.
    - S-4 exists on one side or the other and the SUMMARY says which, and it parses the TS union from its **declaring module** (`publishRefusalVocabulary.ts`, plan `214-03`) rather than from a locally-retyped list.
    - S-5's launcher half is satisfied by asserting `backend/tests/integration/test_214_launch_inputs_wire.py` exists and passes (plan `214-16`), NOT by handing `run_inputs` to `resolve_arguments` in-process.
    - S-6 asserts three production paths agree on one schema per shape, plus the native `descriptor_for` == `_plain_json(INPUT_SCHEMA)` control.
    - S-7 drives a `/workflows/generate` refusal to `setErrorState` and to a rendered builder error, or names the owner of a needed change.
    - The S-2 agreement case's docstring states the limit it cannot cover.
    - The seam table appears in `214-14-SUMMARY.md` with a file:line on both sides of every row, and carries **at least seven** rows (S-1..S-7) plus anything 2a's derivation adds.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/integration/test_214_argument_seams.py -q</automated>
  </verify>
  <done>The seam list was DERIVED by grep from the tree with its commands recorded and every unowned path file named; at least seven seams named on both sides with file:line, each proven by a test that patches neither half.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the cold default → every install | ⚠ Flipping a governed feature's cold default changes what **every** deployment shows, including ones nobody will re-configure. |
| a value one plan writes and another reads | ⚠ Four such seams cross wave boundaries in this phase, and each was built by an agent that could not see the other side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-14-01 | Elevation of Privilege | flipping `live_connectors` by accident alongside the canvas flag | mitigate | Two explicit assertions — the canvas cold-reads `everyone` **and** `live_connectors` cold-reads `off` — plus a `git diff` grep asserting no line pairs `live_connectors` with `everyone`. ⚠ That flag arms real outbound sending for every user. |
| T-214-14-02 | Spoofing | a criterion verified against a flipped database | mitigate | The cold-read case constructs its resolver with **no** `app_settings` row. Phase 209's 16/16 drive is the recorded precedent for why a seeded fixture tests the wrong thing. |
| T-214-14-03 | Repudiation | a seam that fails open silently | mitigate | Task 2's five seam tests, each mocking neither side, plus a self-check case that fails if any of the five functions under test appears inside a `patch(`. ⚠ Phase 204: 106 green tests over a disarmed spend cap. |
| T-214-14-04 | Information Disclosure | a deploy-artifact obligation enforced by nothing | accept, NAMED | `scripts/check-deploy-drift.sh` cannot see `feature_visibility` — measured, `grep` returns zero. The OPERATOR.md parity is asserted by a unit test in Task 1 instead, and the script's blindness is stated in the summary rather than assumed covered. |
| T-214-14-05 | Tampering | the seam suite mutating the operator's local database | mitigate | The suite writes only rows it created, under ids it generated, and reverts them; the plan is **alone in its wave** so no sibling agent is writing the same Postgres. ⚠ Worktrees isolate files, not Postgres, and no `files_modified` check can see that interference. |
| T-214-14-SC | Tampering | package installs | mitigate | This plan installs nothing. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit/test_214_flag_cold_default.py tests/integration/test_214_argument_seams.py -q` — green.
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` — no more than 68 failed, and the number recorded.
- `bash scripts/check-deploy-drift.sh` — exit 0, **recorded as unaffected rather than as evidence**.
- ⚠ **No frontend file is touched**, so the count gate is not this plan's criterion. Plan `214-15`
  runs it once, at the phase's close, after every new suite exists.
</verification>

<how_wed_know_this_failed>
From CONTEXT.md's `<failure_modes>` — this plan owns #7 and is the last line of defence on #3:

7. **A criterion is verified only with `visual_workflow_canvas` flipped by hand and the cold default
   left `off`** — Phase 209's exact failure. D-214-19 unmet. The cold-read case is the guard, and it
   only guards if its fixture seeds nothing. ⚠ A case that seeds `feature_visibility` first and then
   asserts `everyone` has tested the seeding, not the default.
3. **The publish gate and the executor disagree.** Task 2's S-2 case is the only test in the phase that
   runs both predicates over one config with nothing mocked. Every other test of either predicate has
   the other side stubbed, by construction — which is exactly the condition under which Phase 204's
   106 tests stayed green over a disarmed cap. ⭐ **And S-2 alone is NOT enough**: it hands one
   schema to both, so it cannot see the two sides holding different schemas. S-6 is that half, and a
   phase that ships S-2 without S-6 has tested the predicates and not the provenance.

Also, and it is the failure this plan's own first version had:

⭐ **THE AUDIT IS SCOPED TO THE SEAMS ALREADY KNOWN.** Written from the plans, it found four
seams and missed three, and every one of the three had green tests on both sides. The guard is 2a:
derive from the tree, require an owner for every file on every path, and name the unowned ones. ⚠
A seam audit that starts by reading its own table has assumed the answer.

Also specific to this plan: **a seam test that patches the thing under test.** Phase 212's D-1 stub
monkeypatched `_post` away and asserted only that `server_hostname` was *handed* to it; D-2's stub
**invented the `timeout` parameter the real function lacked**. Both passed while the wire was wrong.
The self-check case — this file's own source, grepped for `patch(` naming the five functions — is what
makes that failure impossible to reintroduce quietly.
</how_wed_know_this_failed>

<g5_disposition>
This plan touches one application source file: `backend/app/models/user_settings.py` — **one string in
a dict literal, plus two comments.** No control flow, no new function, no new field.

⚠ **`user_settings.py` has NO hot-file ledger row.** Re-derive its triple in plan `214-15`'s batch pass
and add a row if G-5 fires. An absent row is precisely how `ChatLayout.tsx` stayed invisible to its own
guardrail for twenty-one phases and `config.py` for forty-two — and both were absences, not stale cells.

`backend/tests/integration/` carries no ledger rows by convention (the ledger tracks source, not
suites); recorded here so the omission is a decision rather than an oversight.
</g5_disposition>

<success_criteria>
- `visual_workflow_canvas` cold-reads `everyone` with no seeded row; `live_connectors` cold-reads `off`.
- Both flags' comments tell the truth and are no longer copies of each other.
- `docs/OPERATOR.md` names every governed feature and its cold default, asserted by a test rather than by the drift script.
- The seam list is DERIVED mechanically (2a) before the table is written, with the grep commands recorded and every unowned path file named.
- The seam audit is written in the summary with a file:line on both sides of every row, S-1 through S-7 plus anything the derivation adds.
- S-6 proves schema PROVENANCE across three production paths — the thing the S-2 agreement case is structurally blind to.
- S-5's launcher half cites plan `214-16`'s integration suite rather than supplying the launcher's side in-process.
- S-7's refusal render path is driven, not assumed.
- Each of the five seams has a test that patches neither half, and the self-check case proves it.
- S-2 runs in BOTH directions: satisfiable ⇒ gate empty and resolver complete; unsatisfiable ⇒ gate names the kind and resolver omits the key.
- S-1 covers the jsonb string-scalar arm, written through the real `fail_phase`.
</success_criteria>

<output>
Create `.planning/phases/214-a-step-names-its-service-and-its-action/214-14-SUMMARY.md` when done.
It carries: **the seam-audit table with file:line on both sides**, the cold-read assertion's fixture
shape (so a reader can see it seeded nothing), the backend suite's failure count against the 68
baseline, and the explicit statement that `check-deploy-drift.sh` cannot see `feature_visibility`.
Plan `214-15` reads this summary to build the mitigation→test map.
</output>
