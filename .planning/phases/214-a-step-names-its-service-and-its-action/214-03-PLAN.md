---
phase: 214-a-step-names-its-service-and-its-action
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/argumentVocabulary.ts
  - frontend/src/components/workflows/publishRefusalVocabulary.ts
  - frontend/src/components/workflows/stepIdentityVocabulary.ts
  - frontend/src/components/workflows/doorVocabulary.ts
  - frontend/src/components/workflows/argumentVocabulary.test.ts
  - frontend/src/components/workflows/publishRefusalVocabulary.test.ts
  - frontend/src/components/workflows/stepIdentityVocabulary.test.ts
  - frontend/src/components/workflows/doorVocabulary.test.ts
autonomous: true
requirements: [STEP-01, STEP-02, STEP-03, STEP-04, STEP-05, STEP-06]

must_haves:
  truths:
    - "Every user-visible sentence this phase ships is a named export from a vocabulary module, asserted for character identity against the generated BUILD-CONTRACT it came from."
    - "D-214-06: the vocabulary contains no string offering a JSON, textarea, key/value or 'Advanced' escape hatch — the absence is asserted, not assumed."
    - "D-214-21: the describe refusal is a SECOND ARM on the shipped `DESCRIBE_REFUSAL` mechanism, in the same module, not a second mechanism beside it."
    - "The three shipped door strings are reproduced, never reworded (sketch 217 invariant #2)."
    - "`FAILED_REASON_UNKNOWN` is character-identical to the sentence `PhaseCard.classifyFailure` ships today — the sentinel's words do not change; only its condition will (D-214-23)."
  artifacts:
    - path: "frontend/src/components/workflows/argumentVocabulary.ts"
      provides: "sketch 214's 20 ids + 5 composed"
      contains: "ARG_SOURCE_UPSTREAM"
    - path: "frontend/src/components/workflows/publishRefusalVocabulary.ts"
      provides: "sketch 215's 11 ids + 7 composed"
      contains: "REFUSE_ASK_UNDECLARED"
    - path: "frontend/src/components/workflows/stepIdentityVocabulary.ts"
      provides: "sketch 216's 8 ids + 5 composed"
      contains: "FAILED_REASON_UNKNOWN"
    - path: "frontend/src/components/workflows/doorVocabulary.ts"
      provides: "sketch 217's 12 ids + 3 composed, as ADDITIONS"
      contains: "DOOR_REFUSAL"
  key_links:
    - from: "frontend/src/components/workflows/argumentVocabulary.test.ts"
      to: ".planning/sketches/214-argument-form-and-its-source/BUILD-CONTRACT.generated.md"
      via: "the contract is RE-PARSED at test time and its value column asserted equal"
      pattern: "BUILD-CONTRACT"
    - from: "frontend/src/components/workflows/stepIdentityVocabulary.ts"
      to: "frontend/src/components/panel/PhaseCard.tsx"
      via: "FAILED_REASON_UNKNOWN copied from the shipped sentinel, character-identical"
      pattern: "Failure reason not captured by the backend"
---

<objective>
Create the three new governed vocabulary modules and extend the shipped `doorVocabulary.ts`, binding
each to the sketch BUILD-CONTRACT it came from by re-parsing that contract at test time.

Purpose: this is the **contracts-first wave**. Four downstream plans (`214-05`, `214-07`, `214-13`,
`214-11`) import these identifiers rather than typing sentences, and each is unblocked the moment
this lands. The repo's own rule: *a sentence that lives inside a component is a sentence nobody can
test for drift.*

Output: four vocabulary modules and four suites, each asserting whole-table properties and each
re-reading its generated contract so the sketch is bound to the build rather than merely quoted.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-CONTEXT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-PATTERNS.md
@.planning/sketches/214-argument-form-and-its-source/BUILD-CONTRACT.generated.md
@.planning/sketches/215-publish-refuses-by-name/BUILD-CONTRACT.generated.md
@.planning/sketches/216-the-mark-and-the-action-everywhere/BUILD-CONTRACT.generated.md
@.planning/sketches/217-the-door-that-knows-your-services/BUILD-CONTRACT.generated.md

**PATTERNS.md § Shared Patterns A is this plan's brief** — the five rules from
`doorVocabulary.test.ts`'s own header, including the 187-24 trap that has fired on this exact
surface twice.
</context>

<interfaces>
<!-- The identifier surface plans 05, 07, 10 and 11 import. Values are the four generated
     BUILD-CONTRACT tables VERBATIM — copy them from the contracts, do not retype them from here. -->

`argumentVocabulary.ts` — sketch 214 §1: the 20 flat ids `ARG_SOURCE_FIXED` … `ARG_ASK_KEY_HINT`,
plus the 5 composed functions `ARG_READING_ASK`, `ARG_READING_UPSTREAM`, `ARG_UNRENDERABLE`,
`ARG_LEFTOVER`, `ARG_STEP_IDENTITY`.

`publishRefusalVocabulary.ts` — sketch 215 §1: the 11 flat ids `REFUSE_NEXT` …
`ALREADY_PUBLISHED_NOTE`, plus the 7 composed `REFUSE_NO_SOURCE`, `REFUSE_ASK_UNDECLARED`,
`REFUSE_UPSTREAM_UNREACHABLE`, `REFUSE_SHAPE_UNKNOWN`, `REFUSE_UNRENDERABLE`, `REFUSE_COUNT_MANY`,
`REFUSE_REST_OK`.
⚠ The five composed refusal ids pair 1:1 with plan `214-01`'s `ArgumentGapKind` values —
`no_source` / `ask_undeclared` / `upstream_unreachable` / `shape_unknown` / `unrenderable`. Export a
`REFUSAL_FOR_KIND: Record<ArgumentGapKind, …>` map so the pairing is a compiler-enforced Record and
not a `switch` a sixth kind can silently fall out of.

`stepIdentityVocabulary.ts` — sketch 216 §1: the 8 flat ids `FAILED_REASON_LABEL` …
`ASK_NOT_RECORDED`, plus the 5 composed `STEP_IDENTITY`, `STEP_IDENTITY_SERVICE_UNKNOWN`,
`ASK_WILL_RUN`, `RECEIPT_SENT`, `RECEIPT_REFUSED`.

`doorVocabulary.ts` — sketch 217 §1: **ADDITIONS to the shipped module**, the 12 flat ids
`SERVICES_LABEL` … `SHIPPED_DESCRIBE_REFUSAL` plus the 3 composed `SERVICE_ACTIONS`, `DOOR_REFUSAL`,
`DOOR_REFUSAL_CONNECT`. ⚠ Three of those twelve (`SHIPPED_DESCRIBE_CTA`,
`SHIPPED_DESCRIBE_CTA_REFUSED`, `SHIPPED_DESCRIBE_REFUSAL`) name strings the module **already
exports**; re-export or alias them, never re-declare a second literal.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The three new modules — sentences only, imports none</name>
  <files>frontend/src/components/workflows/argumentVocabulary.ts, frontend/src/components/workflows/publishRefusalVocabulary.ts, frontend/src/components/workflows/stepIdentityVocabulary.ts</files>
  <read_first>
    - `frontend/src/components/workflows/doorVocabulary.ts` in full — the module shape being copied:
      the leaf claim (it imports NOTHING), the docblock register, and `DESCRIBE_REFUSAL` at `:265`
      with its five rules at `:240-264` (a refusal SENTENCE is presentation, never a rule; no
      severity word, no exclamation, no mechanism; **the dash is an EM DASH U+2014**).
    - `frontend/src/components/workflows/phaseVocabulary.ts:156-231` — `branchConditionOf` /
      `EXTERNAL_CAPABILITY_SENTENCES`, the shipped precedent for a COMPOSED value id.
    - `frontend/src/components/panel/PhaseCard.tsx:236-256` — the `reason_unknown` block.
      ⚠ **Copy `FAILED_REASON_UNKNOWN` from this source, character for character. Do not retype it.**
    - `frontend/src/components/workflows/ExternalActionSection.tsx:64-77` — the measured
      `react-refresh/only-export-components` rule: **no runtime `export const` beside a component**.
      Derived constants stay module-private and suites read them from their ONE vocabulary home.
    - The four generated `BUILD-CONTRACT.generated.md` §1 tables — the values, verbatim.
  </read_first>
  <action>
Create the three modules with the exact ids and values from their contracts' §1 tables. Each module
imports NOTHING (the leaf claim, which Task 3 asserts by AST rather than documents).

Each module's header states, in the shipped register: what surface it serves, that the values are
generated-contract-bound, and the one rule specific to it —
  - `argumentVocabulary.ts`: **there is no escape-hatch sentence in this table and there will not be
    one.** Name the reason (D-214-06: a JSON box for the hard cases is the surface SC#1 forbids
    under a different name, and once it exists every hard case routes to it).
    ⚠ **Do NOT spell `Tool Arguments (JSON)` or `MCP_TOOL_ARGS_LABEL` in this prose** — the 187-24
    trap: a docblock that spells a forbidden identifier makes a `?raw`-swept grep count its own
    prose, and it has fired on this exact surface twice.
  - `publishRefusalVocabulary.ts`: five refusals rather than one, because each source arm is proved
    on its own terms (D-214-09) so each failure is a different fact about the world; and
    ⚠ `REFUSE_SHAPE_UNKNOWN` is the ONE refusal that must not name an argument.
  - `stepIdentityVocabulary.ts`: most of this surface is NOT copy — it is a mark plus two names the
    system already holds (D-214-17). And `FAILED_REASON_UNKNOWN` is a **copy of a shipped sentence**;
    its condition narrows in plan `214-11`, its words never change.

Export `REFUSAL_FOR_KIND` from `publishRefusalVocabulary.ts` as a `Record` keyed by the five gap
kinds, so adding a sixth kind is a compile error rather than a silent fall-through.

Every composed id is a function taking named arguments (`{ step, arg }`, `{ action, service }`),
never positional, so a mis-ordered call is a type error rather than a swapped sentence.

⚠ **The em dash is U+2014 in every sentence that carries one.** `doorVocabulary.test.ts` already
asserts the codepoint over its whole table; the new suites do the same.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
    - `grep -c "^import" frontend/src/components/workflows/argumentVocabulary.ts` is 0, and the same for the other two modules **except** `publishRefusalVocabulary.ts`, which may carry exactly one `import type` for the gap-kind union.
    - `grep -c "Failure reason not captured by the backend" frontend/src/components/workflows/stepIdentityVocabulary.ts` is 1, and the string is byte-identical to the one at `frontend/src/components/panel/PhaseCard.tsx` (assert with `diff <(grep -o "Failure reason not captured[^\"]*" …)`).
    - `grep -riEc "json|textarea|advanced|key/value|key-value" frontend/src/components/workflows/argumentVocabulary.ts` is 0.
    - `grep -c "REFUSAL_FOR_KIND" frontend/src/components/workflows/publishRefusalVocabulary.ts` is ≥ 1 and its type is a `Record<…>`, not a function with a `default:` branch.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>Three leaf modules exist carrying the contracts' values verbatim, with no escape-hatch string and no forbidden identifier spelled in prose.</done>
</task>

<task type="auto">
  <name>Task 2: The door's second arm — additions, not a second mechanism</name>
  <files>frontend/src/components/workflows/doorVocabulary.ts</files>
  <read_first>
    - `frontend/src/components/workflows/doorVocabulary.ts:240-265` — `DESCRIBE_REFUSAL` and its five
      rules, verbatim. The new `DOOR_REFUSAL(...)` inherits every one of them.
    - `.planning/sketches/217-the-door-that-knows-your-services/BUILD-CONTRACT.generated.md` §1 and §3
      — §3 records that the door ALREADY draws its disabled control carrying its reason
      (`DESCRIBE_CTA_REFUSED`), so this is **a second arm on one mechanism**; and that with an empty
      box AND nothing connected the CTA reads the **thinness** reason, because a workflow with no
      external step is perfectly legitimate.
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx:246, 428-431, 462-471, 493-521` — the
      shipped render site plan `214-13` extends, and the SPREAD-CONDITIONAL idiom that keeps the
      non-refusing DOM character-identical.
  </read_first>
  <action>
Add sketch 217 §1's twelve ids and three composed functions to `doorVocabulary.ts`. For
`SHIPPED_DESCRIBE_CTA`, `SHIPPED_DESCRIBE_CTA_REFUSED` and `SHIPPED_DESCRIBE_REFUSAL`, alias the
module's existing exports — **one literal per sentence in the repository**; a second copy is the drift
this module exists to prevent.

Add a docblock above the additions recording the precedence rule from contract §3 in the module's own
voice: **the two reasons do not compete.** With an empty description AND nothing connected, the CTA
reads the thinness reason, because naming the connection there would refuse for a reason that is not
binding. Plan `214-13` implements that precedence; this module states it.

⚠ **RESOLVE THE COLOUR CONFLICT HERE, in this module's docblock, and state the ruling.** PATTERNS §7
measures it: the shipped refusal renders `text-destructive` / `border-destructive`
(`WorkflowDoorSwitch.tsx:462-471`), while sketch 217 invariant #11 says the anchor *spends warning,
never destructive*, and sketch 214 #14 / 215 #9 assert `--destructive` is **absent** from the new
surfaces. **Two arms of one mechanism must not mean two colour budgets.** The ruling this phase
takes: **the refusal block's tone token is unified to the WARNING token for BOTH arms**, because a
refusal that names a next action is not a destructive event and the three newest contracts agree —
and because leaving the shipped arm destructive while the new arm is warning would make the same
mechanism read as two severities. ⚠ **Changing the shipped arm's colour re-baselines
`WorkflowDoorSwitch.baseline.test.tsx`, which pins all six resting states BYTE FOR BYTE.** Name that
here; plan `214-13` owns the re-baseline and must record the before/after captures rather than
regenerate them silently.
  </action>
  <acceptance_criteria>
    - `grep -c "There is nothing here to draft from yet" frontend/src/components/workflows/doorVocabulary.ts` is 1 — the alias did not duplicate the literal.
    - `grep -c "DOOR_REFUSAL\b" frontend/src/components/workflows/doorVocabulary.ts` is ≥ 1 and the exported function's body contains a U+2014 em dash (`grep -c $'\u2014'`).
    - The module's added docblock contains the words `thinness` and `not binding` (the precedence rule) — `grep -c "not binding" …` is ≥ 1.
    - The module's added docblock names the colour ruling and names `WorkflowDoorSwitch.baseline.test.tsx` as the pin it re-baselines — `grep -c "baseline.test.tsx" …` is ≥ 1.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>The door's twelve additions land as a second arm on one mechanism, with the precedence rule and the colour ruling recorded and the baseline pin named.</done>
</task>

<task type="auto">
  <name>Task 3: Four suites that FALSIFY the tables — and re-read the generated contracts</name>
  <files>frontend/src/components/workflows/argumentVocabulary.test.ts, frontend/src/components/workflows/publishRefusalVocabulary.test.ts, frontend/src/components/workflows/stepIdentityVocabulary.test.ts, frontend/src/components/workflows/doorVocabulary.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/doorVocabulary.test.ts:1-55` — its header states the four
      rules verbatim: whole-table properties never row assertions; **exact match (`toBe`), never
      containment**; the literal lives in the TEST exactly once in the repository (`COLUMN_D`), which
      makes the assertion a FALSIFICATION of the table rather than a copy of it; and a further case
      RE-READS `BUILD-CONTRACT.generated.md` at test time and asserts the parsed column equals
      `COLUMN_D`. ⭐ **That last case is the mechanism binding the sketch to the build — copy it per
      module.**
    - `frontend/src/components/workflows/ExternalActionSection.test.tsx:296-334` — the tree-wide
      `import.meta.glob("/src/**/*.{ts,tsx}", {query:"?raw"})` sweep with its **non-vacuity control
      first** (`expect(Object.keys(modules).length).toBeGreaterThan(200)`).
    - `frontend/src/components/workflows/ExternalActionSection.test.tsx:333-334` — the 187-24 trap
      remedy: forbidden needles are ASSEMBLED AT RUNTIME (`"EXTERNAL_SHAPE_" + "CAPABILITY_LABEL"`)
      so a docblock cannot satisfy its own sweep.
  </read_first>
  <action>
Write one suite per module. Each carries, at minimum:

1. **A whole-table property pass** over `Object.entries(module)` — nothing hand-lists an identifier.
   Assert: no sentence contains `!`; no sentence contains a severity word (`error`, `invalid`,
   `failed`, `warning`, `critical`) except where the contract's own value does; every dash is
   U+2014; no sentence is empty; no two flat ids share a value.
2. **A `COLUMN_D`-style exact-match block** — the contract's values written once, in the test,
   asserted with `toBe`. ⚠ **Never `toContain`**: on these tables collisions are already the case.
3. **The contract re-parse case** — read the module's own
   `.planning/sketches/<n>-*/BUILD-CONTRACT.generated.md` at test time via `?raw`, parse its §1
   markdown table, and assert the parsed id→value map equals the test's own `COLUMN_D`. This is the
   case that fails when someone edits a sentence in one place and not the other.
4. **A leaf assertion** — the module's source, read via `?raw`, contains no `import` statement
   (except `publishRefusalVocabulary.ts`'s single `import type`).

`argumentVocabulary.test.ts` additionally carries the **escape-hatch absence sweep**: a tree-wide
`?raw` glob asserting no module under `src/components/workflows` exports a string matching
`/json/i` + `textarea` + `advanced` in an argument-authoring context — with the **non-vacuity control
first** and every forbidden needle **assembled at runtime**, never spelled in the file.

`stepIdentityVocabulary.test.ts` additionally asserts `FAILED_REASON_UNKNOWN` equals the sentence
read via `?raw` out of `frontend/src/components/panel/PhaseCard.tsx` — so the copy cannot drift from
its source while `214-11` narrows the sentinel's condition.

`publishRefusalVocabulary.test.ts` additionally asserts `Object.keys(REFUSAL_FOR_KIND)` is exactly
the five gap kinds, sorted — the pairing plan `214-05` depends on.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/argumentVocabulary.test.ts src/components/workflows/publishRefusalVocabulary.test.ts src/components/workflows/stepIdentityVocabulary.test.ts src/components/workflows/doorVocabulary.test.ts` exits 0.
    - Each of the four suites contains a case reading a `BUILD-CONTRACT.generated.md` — `grep -lc "BUILD-CONTRACT" frontend/src/components/workflows/*Vocabulary.test.ts | wc -l` is 4.
    - `grep -c "toContain(" frontend/src/components/workflows/argumentVocabulary.test.ts` is 0 for the value-identity block (containment is forbidden on these tables).
    - The escape-hatch sweep's non-vacuity control asserts `> 200` modules and appears BEFORE the absence assertions in file order.
    - `grep -c "MCP_TOOL_ARGS_LABEL" frontend/src/components/workflows/argumentVocabulary.test.ts` is 0 — the needle is assembled at runtime.
    - The whole-gate reading is HIGHER than its prior total by at least the four new suites' case counts, and no pinned file DECREASES: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root, verdict line read verbatim.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/argumentVocabulary.test.ts src/components/workflows/publishRefusalVocabulary.test.ts src/components/workflows/stepIdentityVocabulary.test.ts src/components/workflows/doorVocabulary.test.ts</automated>
  </verify>
  <done>Four suites falsify their tables, each re-parses its generated contract, and the escape-hatch sweep runs with a non-vacuity control and runtime-assembled needles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author prose → `DOOR_REFUSAL(service)` | ⚠ The service name in the refusal comes from **what the author wrote** (D-214-21), so untrusted free text is interpolated into a rendered sentence. |
| sketch contract file → test assertion | A generated file on disk is read at test time; a tampered contract would silently weaken the assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-03-01 | Tampering | `DOOR_REFUSAL(service)` interpolating author prose | mitigate | The composed function returns a plain `string` rendered by React as text — no `dangerouslySetInnerHTML` anywhere on the door. Tested by a case passing a `<img onerror>`-shaped service name and asserting the returned value is the escaped literal, plus a `?raw` fence asserting `dangerouslySetInnerHTML` appears zero times under `src/components/workflows`. Plan `214-13` additionally caps the matched span to a catalog whole-word match. |
| T-214-03-02 | Information Disclosure | a vocabulary string leaking a wire id | mitigate | The whole-table property pass asserts no value contains `send_email`, `create_ticket`, `post_message`, `external_action` or an id-shaped token — sketch 214 #13 / 215 #13 / 216 #4, applied at the vocabulary layer where every sentence is in one place. |
| T-214-03-03 | Repudiation | a sentence edited in the module but not the contract | mitigate | The contract re-parse case in every suite. Without it the "generated contract" is decoration; with it, drift is a red test. |
| T-214-03-04 | Elevation of Privilege | an escape-hatch string re-appearing under another name | mitigate | The tree-wide `?raw` sweep with runtime-assembled needles and a non-vacuity control. ⚠ The control is load-bearing: an empty glob makes every absence assertion free. |
| T-214-03-SC | Tampering | package installs | mitigate | This plan installs nothing; all four modules are string tables. Any dependency request stops the plan and raises a blocking human checkpoint. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows` — the four new
  suites green; **per-file counts recorded** for `doorVocabulary.test.ts` (an existing pinned suite
  whose count rises) and the three new ones.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — verdict line
  verbatim. The three new suites live under `src/components/workflows`, already a **directory** entry
  in `TARGETS`, so they RUN the moment they exist and report as `new`. ⚠ **This plan does NOT edit `scripts/vitest-count-gate.cjs`.** Every `BASELINE` pin for this
  phase is collected in plan `214-15`, in one commit, once every new file exists — a `BASELINE`
  key naming a path that does not yet exist makes the gate **ERROR**, and distributing the pins
  across parallel worktrees is what would create that. **Record the gate's printed `— N new`
  figures verbatim in this plan's SUMMARY**; `214-15` pins those exact numbers, never a
  hand-derived one. The one exception in the whole phase is plan `214-08`'s `TARGETS` PATH edit
  for the moved `connectionMark.test.tsx`, which cannot wait because a pinned file the gate
  cannot find fails `N/N pinned files present` immediately.
</verification>

<how_wed_know_this_failed>
From CONTEXT.md's `<failure_modes>` — this plan owns #2:

2. **A JSON textarea survives anywhere in the authoring flow** — including as a "for advanced tools"
   fallback. SC#1 unmet by its own words. This plan's half is the *vocabulary* half: no string in the
   table offers one, and the tree-wide sweep asserts none appears anywhere under
   `src/components/workflows`. Plan `214-07` owns the deletion itself.

Also specific to this plan: **the sweep passes vacuously.** An `import.meta.glob` that resolved zero
modules makes every absence assertion free, and the failure is invisible. The `> 200` non-vacuity
control asserted FIRST is the only guard.

And: **a needle spelled in the file's own prose satisfies its own sweep.** That is the 187-24 trap and
it has fired on this exact surface twice. Runtime assembly is the remedy, and a reviewer should grep
the test file for the literal to confirm it is absent.
</how_wed_know_this_failed>

<g5_disposition>
**`frontend/src/components/workflows/doorVocabulary.ts` — re-derived 2026-08-28: `5 commits / 3 phases / 364 lines`. G-5 FIRES, exactly at threshold.** The ledger cell reads `4 / 3 / 341` — ⚠ **STALE**.

**Disposition: no seam is proposed, and that is the argued verdict rather than a deferral.** This
module is a vocabulary — a table doing one thing many times — and it is the same verdict the ledger
already carries for `connectionsCopy.ts` and `connectionFormCopy.ts` on identical reasoning:
splitting a governed string table by topic would create two homes for the property *"every sentence
in this surface's vocabulary obeys these five rules"*, and the whole-table assertions in
`doorVocabulary.test.ts` are the thing that property rests on. **A split would weaken the guard it
exists to enable.** The row and its `docs/HOT-FILE-LEDGER.md` section are synced in plan `214-15`.

The three NEW modules are young (0 phases) and each owes a detail section at its third phase; plan
`214-15` adds their rows so they are never invisible to G-5 the way `libraryFilter.ts` was for four
phases and `ChatLayout.tsx` was for twenty-one.
</g5_disposition>

<success_criteria>
- Four vocabulary modules exist carrying their contracts' §1 tables verbatim.
- Each of the four suites re-parses its generated contract and asserts equality with its own `COLUMN_D`.
- `FAILED_REASON_UNKNOWN` is byte-identical to the shipped sentinel sentence, asserted by `?raw`.
- `REFUSAL_FOR_KIND` is a `Record` over exactly plan `214-01`'s five gap kinds.
- The escape-hatch sweep runs with a non-vacuity control first and needles assembled at runtime.
- Three new `BASELINE` pins recorded from the gate's own printed `— N new` figures.
</success_criteria>

<output>
Create `.planning/phases/214-a-step-names-its-service-and-its-action/214-03-SUMMARY.md` when done.
Record the exact exported identifier list per module — plans `214-05`, `214-07`, `214-13` and
`214-11` import from it — and the three `BASELINE` numbers as printed.
</output>
