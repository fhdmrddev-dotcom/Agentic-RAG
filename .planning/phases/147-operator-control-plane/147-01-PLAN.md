---
phase: 147-operator-control-plane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/097_operator_flags.sql
  - backend/app/main.py
  - backend/app/models/user_settings.py
  - backend/app/api/settings.py
  - backend/tests/test_147_flag_failure_semantics.py
autonomous: false
requirements: [FLAG-01]
user_setup: []

must_haves:
  truths:
    - "Three new app_settings booleans (self_improve_enabled, workflows_enabled, maintenance_mode) exist and read through the 30s TTL cache with no server restart (FLAG-01 substrate)"
    - "A transient DB read failure returns last-known-good, never flips a flag (FLAG-01 fail-closed)"
    - "maintenance_mode on a truly-cold cache reads False (platform OPEN) per D-Q4; capability *_enabled cold-cache reads the migration default true (never silently disables a capability)"
    - "Defaults keep existing runtime byte-identical until an operator flips a flag (D-06 substrate ready)"
  artifacts:
    - path: "supabase/migrations/097_operator_flags.sql"
      provides: "3 additive app_settings boolean columns"
      contains: "ADD COLUMN IF NOT EXISTS maintenance_mode"
    - path: "backend/app/models/user_settings.py"
      provides: "fail-closed read helpers + settings-row wiring for the 3 flags"
      contains: "maintenance_mode"
    - path: "backend/app/main.py"
      provides: "_DIRECT_COLUMNS write-allowlist extended with the 3 flags"
      contains: "self_improve_enabled"
    - path: "backend/app/api/settings.py"
      provides: "GET /settings exposure of the 3 flags (FullSettingsResponse — the Control Room grid's flag-read source)"
      contains: "workflows_enabled"
  key_links:
    - from: "backend/app/models/user_settings.py"
      to: "app_settings columns"
      via: "_build_settings_from_row + _val_bool"
      pattern: "self_improve_enabled|workflows_enabled|maintenance_mode"
---

<objective>
Lay the FLAG-01 substrate: add three boolean columns to `app_settings`
(`self_improve_enabled`, `workflows_enabled`, `maintenance_mode`) via migration 097,
wire them through the existing per-worker 30s TTL settings cache, and add fail-closed
read helpers with the correct polarity per D-Q4. This is the foundation every later
FLAG-01 enforcement seam (tool gates, workflow-launch block, maintenance middleware)
reads from. Zero new flag infrastructure — the `app_settings` substrate suffices.

Purpose: A single, correct, last-known-good flag read surface so a transient DB blip
can never flip a flag into a platform outage, and a cold cache defaults to the SAFE
polarity for each flag's meaning.
Output: migration 097 + settings-model wiring + read helpers + failure-semantics tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-operator-control-plane/147-CONTEXT.md
@.planning/phases/147-operator-control-plane/147-RESEARCH.md
@.planning/phases/147-operator-control-plane/147-PATTERNS.md

<interfaces>
<!-- Extracted contracts the executor implements against. No codebase exploration needed. -->

Existing pattern to mirror (VERIFIED file:line):
- `053_settings_unification.sql:14-29` — the web_search_enabled / sandbox_enabled boolean-column DDL pattern.
- `backend/app/main.py:97-106` — `_DIRECT_COLUMNS: set[str]` write-allowlist (already contains "web_search_enabled", "sandbox_enabled"). Column names are code CONSTANTS (SQLi-safe).
- `backend/app/models/user_settings.py:215-241` — the TTL cache + last-known-good-on-DB-failure (`except Exception: ... if _settings_cache is None: _settings_cache = {}`).
- `backend/app/models/user_settings.py:355-366` — `_val_bool(row, key, env_attr, default)` DB→env→default resolver.
- `backend/app/models/user_settings.py:471+` — `_build_settings_from_row` (maps a row → the Pydantic settings object).
- `backend/app/models/user_settings.py:639-650` — `document_management_enabled()` = the default-ON polarity template (cold-cache/exception → True).

Target column defaults (A5 — keeps runtime byte-identical):
- self_improve_enabled boolean DEFAULT true
- workflows_enabled    boolean DEFAULT true
- maintenance_mode     boolean DEFAULT false
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 097 + wire the 3 flags into the settings model with D-Q4 polarity helpers</name>
  <files>supabase/migrations/097_operator_flags.sql, backend/app/main.py, backend/app/models/user_settings.py, backend/app/api/settings.py, backend/tests/test_147_flag_failure_semantics.py</files>
  <read_first>
    - backend/app/models/user_settings.py (the WHOLE file's cache/read/write region: lines 204-666 — the TTL cache 215-241, _val_bool 355-366, _build_settings_from_row 471+, document_management_enabled 639-650, save_app_settings 250-288)
    - backend/app/main.py (lines 97-121 — the _DIRECT_COLUMNS allowlist)
    - supabase/migrations/053_settings_unification.sql (the boolean-column DDL analog, lines 14-29)
    - backend/app/api/settings.py (lines 32-70 FullSettingsResponse with web_search_enabled/sandbox_enabled at :62/:66; lines 200-215 the _build_response mapping — the GET /settings read contract to extend)
    - .planning/phases/147-operator-control-plane/147-PATTERNS.md (section "backend/app/models/user_settings.py — flag read + failure semantics")
  </read_first>
  <behavior>
    - self_improve_enabled() / workflows_enabled() helpers: normal read returns the DB value; on last-known-good cache hit returns the prior value; on a TRULY-cold cache / exception returns True (the migration default — never silently disables a capability on a blip).
    - maintenance_mode() helper INVERTS (D-Q4): on a truly-cold cache / exception returns False (platform OPEN — failing "closed" here would be a self-inflicted outage).
    - A simulated transient DB read failure (patch the loader to raise once) returns last-known-good, NOT the cold default (proves the blip does not flip the flag).
    - After a save_app_settings write of one flag, invalidate_settings_cache is called so the next read reflects the new value within the current worker.
  </behavior>
  <action>
Create `supabase/migrations/097_operator_flags.sql` following the `053_settings_unification.sql:14-29` pattern: `ALTER TABLE public.app_settings ADD COLUMN IF NOT EXISTS self_improve_enabled boolean DEFAULT true`, `workflows_enabled boolean DEFAULT true`, `maintenance_mode boolean DEFAULT false`. Add a header comment block naming: apply via Supabase SQL editor (never db push/reset), then `bash scripts/regenerate-full-schema.sh`, and CLOUD PARITY (paste into cloud SQL editor at promotion — docs/DEPLOYMENT-WORKFLOW.md). Filename digits-only per CLAUDE.md (097_operator_flags.sql).

In `backend/app/main.py`, extend the `_DIRECT_COLUMNS` set (currently containing `web_search_enabled`, `sandbox_enabled`) to also include `self_improve_enabled`, `workflows_enabled`, `maintenance_mode` — these are the code-constant write allowlist that keeps `save_app_settings` SQLi-safe (T-147-01).

In `backend/app/models/user_settings.py`: add the three fields to the Pydantic effective-settings model and to `_build_settings_from_row` using `_val_bool(row, "<col>", "<env_attr_or_None>", default=<True|False>)` mirroring how `web_search_enabled` is mapped. Then add three module-level read helpers modeled on `document_management_enabled()` (639-650): `self_improve_enabled()` and `workflows_enabled()` fall back to True on exception; `maintenance_mode()` falls back to False on exception (D-Q4 inversion — cite the decision in a code comment). Do NOT add Redis pub/sub or any cross-worker cache-bust (Pitfall 5 — a ≤30s per-worker skew is expected and honest).

Expose the three booleans on the EXISTING settings read contract so the Control Room capability grid has a flag-read source with no new endpoint: add `self_improve_enabled`, `workflows_enabled`, `maintenance_mode` fields to `FullSettingsResponse` (backend/app/api/settings.py:32 — alongside `web_search_enabled` :62 / `sandbox_enabled` :66) and populate them in `_build_response` (the :207-210 mapping) from the effective settings. `GET /settings` (settings.py:270) then serves them to any authed user — consistent with the two existing capability booleans already exposed there.

Create `backend/tests/test_147_flag_failure_semantics.py` proving: (1) cold-cache polarity for each helper (capability→True, maintenance→False), (2) a patched one-shot DB read failure returns last-known-good not the cold default, (3) a write invalidates the cache. Mock at the data-access layer the same way `test_146_*` mocks (do NOT mock the supabase query-builder — match the asyncpg/pool access the settings loader actually uses; see MEMORY lesson from 146).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_147_flag_failure_semantics.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/097_operator_flags.sql` returns 3.
    - `backend/app/main.py` `_DIRECT_COLUMNS` contains all three literal strings `self_improve_enabled`, `workflows_enabled`, `maintenance_mode` (grep the 3 tokens, each present).
    - `user_settings.py` defines `def maintenance_mode(` whose exception branch returns `False` and `def self_improve_enabled(` / `def workflows_enabled(` whose exception branches return `True`.
    - `pytest tests/test_147_flag_failure_semantics.py -x` passes; the last-known-good test fails if the helper returns the cold default on a transient blip.
    - `FullSettingsResponse` (settings.py) declares the three new boolean fields and `_build_response` maps them (grep all three tokens in settings.py) — the GET /settings contract carries them.
  </acceptance_criteria>
  <done>Migration file written; the three flags are read through the TTL cache with correct D-Q4 polarity and last-known-good semantics; unit tests green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 097 to the live local DB + regenerate the bootstrap schema</name>
  <files>supabase/migrations/097_operator_flags.sql, supabase/full-schema.sql, docs/DEPLOYMENT-WORKFLOW.md</files>
  <read_first>
    - supabase/migrations/097_operator_flags.sql (the file Task 1 wrote — the exact SQL to paste)
    - supabase/SETUP.md (the local migration-apply story)
    - docs/DEPLOYMENT-WORKFLOW.md (the cloud deploy-parity checklist to append the 097 note to)
    - scripts/regenerate-full-schema.sh (the no-reset live-DB dump the operator runs after applying)
  </read_first>
  <action>[HUMAN-ACTION — Claude cannot paste into the Supabase SQL editor.] The operator pastes the full contents of supabase/migrations/097_operator_flags.sql into the LOCAL Supabase SQL editor and runs it (idempotent ADD COLUMN IF NOT EXISTS — safe to re-run; never supabase db push / db reset, which wipe dev data per CLAUDE.md). Then the operator runs bash scripts/regenerate-full-schema.sh (no --reset — live-DB dump preserving data) to rebuild supabase/full-schema.sql, appends a one-line 097 note to the deploy-parity checklist in docs/DEPLOYMENT-WORKFLOW.md (new app_settings rows must be pasted into cloud Supabase at promotion), and commits supabase/migrations/097_operator_flags.sql + supabase/full-schema.sql + the doc edit.</action>
  <what-built>Task 1 wrote `supabase/migrations/097_operator_flags.sql` (three additive `app_settings` booleans). The schema change is not yet on the live local DB — migrations in this project are applied by hand via the Supabase SQL editor (CLAUDE.md: never `supabase db push`/`db reset`, which would wipe dev data).</what-built>
  <how-to-verify>
    1. Open the Supabase SQL editor for the LOCAL project and paste the full contents of `supabase/migrations/097_operator_flags.sql`; run it (idempotent `ADD COLUMN IF NOT EXISTS` — safe to re-run).
    2. Confirm the three columns exist: run `SELECT column_name FROM information_schema.columns WHERE table_name='app_settings' AND column_name IN ('self_improve_enabled','workflows_enabled','maintenance_mode');` — expect 3 rows.
    3. From the repo root run `bash scripts/regenerate-full-schema.sh` (no --reset — live-DB dump, preserves data). Confirm `supabase/full-schema.sql` now contains the three columns.
    4. Add a one-line entry to the deploy-parity checklist in `docs/DEPLOYMENT-WORKFLOW.md` noting migration 097 must be pasted into cloud Supabase at promotion (new `app_settings` rows).
    5. Commit both `supabase/migrations/097_operator_flags.sql` and `supabase/full-schema.sql` (and the doc edit).
  </how-to-verify>
  <resume-signal>Type "applied" once the three columns are live and `full-schema.sql` is regenerated + committed, or describe any error.</resume-signal>
  <verify>
    <automated>PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='app_settings' AND column_name IN ('self_improve_enabled','workflows_enabled','maintenance_mode');" | grep -qx 3 && echo OK || echo "MISSING — operator must apply migration 097 in the Supabase SQL editor first"</automated>
  </verify>
  <acceptance_criteria>
    - The three columns exist on the live local app_settings table (the psql count returns 3).
    - supabase/full-schema.sql contains all three column names (grep each present) after regen.
    - docs/DEPLOYMENT-WORKFLOW.md deploy-parity checklist references migration 097.
    - Both supabase/migrations/097_operator_flags.sql and supabase/full-schema.sql are committed.
  </acceptance_criteria>
  <done>Migration 097 is live on the local DB, full-schema.sql regenerated + committed, and the cloud-parity note recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator write → app_settings | Flag values persist to a shared platform-wide table (no per-user RLS scope — these are global switches). |
| DB read → runtime flag | A transient failure must NEVER be interpreted as "flag = unknown". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-01 | Tampering | `save_app_settings` column write | mitigate | Column names are a code-constant allowlist (`_DIRECT_COLUMNS`, main.py:97-121); values parameterized (user_settings.py:274-287). Adding the 3 columns to the allowlist keeps writes SQLi-safe. |
| T-147-02 | Denial of Service | flag read on DB blip | mitigate | Last-known-good TTL cache (user_settings.py:233-241) returns the prior value; test proves a transient failure does not flip a flag. |
| T-147-COLD | Denial of Service (self-inflicted) | `maintenance_mode()` cold-cache polarity | mitigate | D-Q4: a truly-cold read returns False (platform OPEN); capability flags cold-read the migration default True (never silently disable). |
| T-147-SC | Tampering (supply chain) | npm/pip installs | accept | Zero new dependencies this phase (RESEARCH Package Legitimacy Audit: N/A — no install tasks). |
</threat_model>

<verification>
- Backend unit suite for failure semantics passes: `cd backend && venv/Scripts/python -m pytest tests/test_147_flag_failure_semantics.py -x`.
- Live columns present after the blocking apply (Task 2 step 2).
- Existing settings tests unaffected: `cd backend && venv/Scripts/python -m pytest tests/ -k settings -x` (no regression).
</verification>

<success_criteria>
- Migration 097 written, applied to the live local DB, and `full-schema.sql` regenerated + committed.
- The 3 flags read through the TTL cache with D-Q4 polarity and last-known-good semantics.
- Runtime is byte-identical until an operator flips a flag (defaults preserve current behavior).
</success_criteria>

<output>
Create `.planning/phases/147-operator-control-plane/147-01-SUMMARY.md` when done.
</output>
