---
phase: 147-operator-control-plane
plan: 09
type: execute
wave: 3
depends_on: [147-06, 147-07, 147-08]
files_modified:
  - frontend/src/components/admin/ControlRoomPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/admin/__tests__/ControlRoomPage.test.tsx
autonomous: true
requirements: [ADMIN-02, FLAG-01]
must_haves:
  truths:
    - "The 146 Overview tab becomes the live Control Plane landing tab (D-08 promote); the band tabs read Control Plane · Users & Access(🔒148) · Model Registry(🔒149) · Secrets(🔒150) · Audit log; System Controls dissolves INTO Control Plane; health lives in exactly ONE place"
    - "The Control Plane tab composes the 063-B scroll: pinned vitals → Health detail → Active runs (064-B) → Controls (065-A grid + separate maintenance) → Activity (062-A ledger); Activity 'View all ›' lands on the Audit log tab"
    - "The surface auto-polls read-only data ~10s while open and PAUSES when the tab is hidden (D-07); polls are silent (floor-exempt); a single 'Opened the Control Plane' visit row is recorded on mount and the manual ↻ records 'Viewed system health'"
    - "End users see a persistent app-wide maintenance banner when maintenance is ON (read from the public /health flag) and can still browse/read — the banner lives OUTSIDE the /admin surface"
  artifacts:
    - path: "frontend/src/components/admin/ControlRoomPage.tsx"
      provides: "recomposed Control Plane landing + auto-poll + visit-row"
      contains: "Control Plane"
    - path: "frontend/src/App.tsx"
      provides: "end-user maintenance banner mount (outside admin)"
      contains: "maintenance"
  key_links:
    - from: "frontend/src/components/admin/ControlRoomPage.tsx"
      to: "getAdminActiveRuns + getBackpressure (poll) + recordControlPlaneEvent"
      via: "setInterval ~10s + visibilitychange pause + visit-row on mount"
      pattern: "setInterval|visibilitychange"
---

<objective>
Assemble the locked Control Plane: recompose `ControlRoomPage` to promote the 146 Overview
into the live Control Plane landing tab (D-08), re-author the band tabs, and compose the
063-B scroll (pinned vitals → Health → Active runs → Controls → Activity) from the plan-07
and plan-08 leaves. Replace the 146 one-shot entry read with the D-07 auto-poll (~10s,
hidden-tab pause, silent polls, one visit-row on mount, manual ↻ still records). Mount a
persistent end-user maintenance banner outside the admin surface, reading the public
/health flag.

Purpose: The three sketch winners read as one instrument (sketch 066), and end users get
an honest read-only banner without touching the operator-only /admin surface.
Output: recomposed `ControlRoomPage` + end-user banner + tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-operator-control-plane/147-CONTEXT.md
@.planning/phases/147-operator-control-plane/147-PATTERNS.md
@.planning/sketches/066-control-plane-assembled-and-linkage/README.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- The shell owns fetch/poll/state and threads it to the plan-07/08 leaves. -->

From lib/api.ts (plan 06): getBackpressure(), getAdminActiveRuns(), killRun(runId), setFlag(key,value), recordControlPlaneEvent("visit"|"refresh"), getMaintenanceStatus(), types BackpressureSignals / ActiveRun / FlagKey.
From plan 07: `HealthSignals` (extended), `ActiveRunsSection` (props {runs, onKill}).
From plan 08: `CapabilityGrid` (props {flags, impactCounts, onToggle, showTechnical}), `MaintenancePanel` (props {maintenanceOn, onSetMaintenance}).

Current shell (VERIFIED ControlRoomPage.tsx):
- TABS array 68-95 (Overview / Audit log / System Controls / Users & Access / AI Models / API Keys). RE-AUTHOR to: Control Plane (live) · Users & Access (locked→148) · Model Registry (locked→149) · Secrets (locked→150) · Audit log (live). System Controls / AI Models / API Keys are REPLACED (System Controls dissolves into Control Plane; AI Models→Model Registry 149; API Keys→Secrets 150). Keep the TabDef/lockedDescription shape + the LockedTab refusal (146 D-07 rule: name the arriving phase, no phase numbers in the body copy).
- One-shot entry load 112-133 + manual refresh 139-158 (records via /backpressure floor today). SUPERSEDE with D-07: silent auto-poll; the visit + manual-refresh recording now go through recordControlPlaneEvent (the data GETs became floor-exempt in plan 02). Keep the `alive = useRef(true)` unmount guard.

D-08 / 066 composition: the Control Plane tab body = pinned vitals (063-B header that stays pinned on scroll) → Health detail (HealthSignals) → Active runs (ActiveRunsSection) → Controls (CapabilityGrid + MaintenancePanel) → Activity (RecentActionsCard/ledger with "View all ›" → the Audit log tab). Health lives ONLY here (no duplicate Overview health).

End-user banner (VERIFIED App.tsx:64-70 mounts StreamsProvider > TooltipProvider > ChatLayout): the banner wraps above/around ChatLayout, OUTSIDE the admin surface; it reads getMaintenanceStatus() (public /health) — end users are 404 on /admin so it must NOT call an /admin route.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose ControlRoomPage — promote to Control Plane, compose the 063-B scroll, D-07 auto-poll + visit-row</name>
  <files>frontend/src/components/admin/ControlRoomPage.tsx, frontend/src/components/admin/__tests__/ControlRoomPage.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/ControlRoomPage.tsx (the WHOLE file — TABS 68-95, the entry-load useEffect 112-133, handleRefresh 139-158, the Overview body 204-235, the alive-guard idiom)
    - frontend/src/components/admin/ActiveRunsSection.tsx + CapabilityGrid.tsx + MaintenancePanel.tsx (the plan-07/08 leaves + their prop shapes)
    - frontend/src/lib/api.ts (the plan-06 client fns + types)
    - .planning/sketches/066-control-plane-assembled-and-linkage/README.md (the assembled composition + the band-tab IA + button→destination contract)
    - .planning/phases/147-operator-control-plane/147-PATTERNS.md (section "ControlRoomPage.tsx — RECOMPOSE")
  </read_first>
  <behavior>
    - The landing tab is "Control Plane" (was Overview); the tab bar reads Control Plane · Users & Access(🔒) · Model Registry(🔒) · Secrets(🔒) · Audit log; locked tabs render the honest LockedTab refusal naming the arriving phase (no phase numbers in body copy).
    - The Control Plane body renders, in order: pinned vitals (stays pinned on scroll) → HealthSignals → ActiveRunsSection → CapabilityGrid + MaintenancePanel → the Activity ledger with "View all ›" switching to the Audit log tab.
    - On mount: fetch backpressure + active-runs once AND record ONE visit row (recordControlPlaneEvent("visit")).
    - While mounted + visible: auto-poll backpressure + active-runs every ~10s SILENTLY (no recording). When the browser tab is hidden (visibilitychange), the poll pauses; it resumes when visible. The interval is cleared on unmount (alive guard preserved).
    - Manual ↻ Refresh re-fetches AND records "Viewed system health" (recordControlPlaneEvent("refresh")) + pulses the band recording marker — the deliberate read, distinct from the silent polls.
    - Kill / flag / maintenance callbacks: onKill→killRun, onToggle→setFlag, onSetMaintenance→setFlag("maintenance_mode", …); after a write, re-fetch active-runs/flags so the surface reflects the new state (writes still record via the backend floor).
  </behavior>
  <action>
Re-author the TABS array to the five-tab band-tab IA (Control Plane live; Users & Access / Model Registry / Secrets locked with LockedTab refusals naming the arriving phase without numbers; Audit log live) — remove the System Controls / AI Models / API Keys entries (System Controls content moves INTO the Control Plane body; the other two become the renamed locked tabs). Compose the Control Plane tab body as the 063-B scroll using the plan-07/08 leaves: a pinned vitals header (sticky) → HealthSignals → ActiveRunsSection (pass `runs` + an `onKill` that calls killRun then re-fetches) → CapabilityGrid + MaintenancePanel (pass the current flags, an impactCounts derived from the active-runs list, `onToggle`→setFlag, `onSetMaintenance`→setFlag) → the Activity ledger (RecentActionsCard) with a "View all ›" that setActiveTab("audit"). Replace the one-shot entry `useEffect` with a `setInterval` (~10s) auto-poll of backpressure + active-runs that PAUSES on `document.visibilitychange` (hidden) and clears on unmount (keep the `alive` ref guard). On mount call recordControlPlaneEvent("visit") exactly once. Change handleRefresh to re-fetch + recordControlPlaneEvent("refresh") + pulse the marker (the data GETs no longer auto-record — plan 02 made them floor-exempt). Fetch the current flag values for the grid (read them from the settings the app already exposes to the operator, or add a small operator flags GET if none exists — reuse plan 03's PUT surface's read counterpart; if a read endpoint is needed, note it in the SUMMARY). Add `__tests__/ControlRoomPage.test.tsx`: (1) the landing tab label is "Control Plane" and the tab bar has the five tabs; (2) mount records exactly one "visit" (mock recordControlPlaneEvent, assert called once with "visit"); (3) a hidden-tab visibility event pauses polling (no further fetches after hidden); (4) "View all ›" switches to the Audit tab; (5) a locked tab renders the LockedTab refusal.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- ControlRoomPage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The landing tab renders "Control Plane" and the tab bar contains exactly: Control Plane, Users & Access, Model Registry, Secrets, Audit log (test assertion); "System Controls"/"AI Models"/"API Keys" no longer appear (grep the TABS array — those labels are gone).
    - On mount `recordControlPlaneEvent` is called once with "visit" (mock assertion); it is NOT called on each poll.
    - A `visibilitychange` to hidden stops further poll fetches (assert no additional getBackpressure calls after hidden) and unmount clears the interval.
    - "View all ›" switches the active tab to Audit (assertion); a locked tab shows the LockedTab refusal naming the arriving phase.
    - The Control Plane body renders HealthSignals, ActiveRunsSection, CapabilityGrid, and MaintenancePanel in one scroll (component presence assertions).
  </acceptance_criteria>
  <done>The three winners compose into one Control Plane instrument with D-07 poll/visit discipline; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: End-user maintenance banner (outside admin, public /health flag)</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (lines 60-80 — the StreamsProvider > TooltipProvider > ChatLayout mount region)
    - frontend/src/components/admin/OperatorBand.tsx (the full-width persistent band shape to mirror, 38-85)
    - frontend/src/lib/api.ts (getMaintenanceStatus from plan 06 — the public /health flag source)
    - .planning/phases/147-operator-control-plane/147-PATTERNS.md (section "Frontend app-shell maintenance banner")
  </read_first>
  <behavior>
    - When maintenance is ON, a persistent full-width app-wide banner ("Maintenance mode — the platform is read-only. You can read everything; nothing new runs right now.") renders above/around ChatLayout for ALL users (not just operators).
    - The banner reads the PUBLIC /health maintenance flag (getMaintenanceStatus) — it never calls an /admin route (end users are 404 there). It refreshes on an interval and/or reacts to a 503 maintenance response.
    - When maintenance is OFF, no banner renders (byte-identical to today).
  </behavior>
  <action>
Add a lightweight maintenance banner at the App/ChatLayout seam (App.tsx:64-70), wrapping above ChatLayout so it is visible on every surface OUTSIDE the /admin room. Read the flag via `getMaintenanceStatus()` (public /health) on mount and on a modest interval (e.g. ~30s, matching the TTL window — do NOT hammer it); render a persistent full-width banner (mirror the OperatorBand full-width band shape, amber) with plain-language read-only copy when true, and nothing when false. Optionally also flip the banner on if any API call returns the maintenance 503 (belt-and-suspenders), but the /health poll is the primary source. Keep it presentational + resilient (a failed /health read → assume not-in-maintenance, do not block the app).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "App.tsx" || echo "App.tsx typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx renders a maintenance banner conditioned on the public maintenance flag (grep: `getMaintenanceStatus` used in App.tsx; the banner does NOT call any `/admin` client fn).
    - The banner copy is plain-language read-only (grep the "read-only" copy) and renders above ChatLayout (outside the admin surface).
    - A false/failed flag read renders no banner and does not block the app (resilient default).
    - `npx tsc --noEmit` reports no new App.tsx error.
  </acceptance_criteria>
  <done>End users see an honest persistent read-only banner during maintenance, sourced from the public flag (never /admin).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| end user → maintenance flag source | Non-operators are 404 on /admin; the banner must read a public source only. |
| operator poll → audit ledger | Silent polls must NOT spam the ledger; only deliberate reads record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-15 | Information Disclosure | end-user banner flag source | mitigate | The banner reads only the public /health `maintenance` boolean — it never calls an /admin route (which would 404 for end users and could leak surface existence). |
| T-147-17 | Repudiation / audit noise | silent polls recording | mitigate | D-07: poll GETs are floor-exempt (plan 02); only the mount visit-row and the manual ↻ record — the ledger stays a record of deliberate human actions. |
| T-147-12 | Elevation of Privilege | operator affordances in shell | mitigate | Every write goes through the server-gated client fns; a forged operator flag yields an empty shell that fetches nothing (146 Pitfall 13 inherited). |
| T-147-SC | Tampering (supply chain) | npm installs | accept | Zero new dependencies (RESEARCH audit N/A). |
</threat_model>

<verification>
- `cd frontend && npm run test -- ControlRoomPage` green; `npx tsc --noEmit` no new ControlRoomPage/App errors.
- Manual: mount the Control Plane → exactly one "Opened the Control Plane" ledger row; scroll → vitals stay pinned and can go amber/red on a poll; hide the tab → polling pauses.
</verification>

<success_criteria>
- Overview promoted to the live Control Plane landing tab (D-08); five-tab IA; one-place health.
- 063-B scroll composed from the plan-07/08 leaves; Activity "View all ›" → Audit.
- D-07 silent auto-poll + hidden-tab pause + one visit-row + manual-refresh recording.
- End-user maintenance banner outside admin, sourced from the public flag.
</success_criteria>

<output>
Create `.planning/phases/147-operator-control-plane/147-09-SUMMARY.md` when done.
</output>
