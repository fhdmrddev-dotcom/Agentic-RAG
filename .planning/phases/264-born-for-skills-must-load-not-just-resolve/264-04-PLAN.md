---
phase: 264-born-for-skills-must-load-not-just-resolve
plan: 04
type: execute
wave: 4
depends_on: ["264-03"]
files_modified:
  - backend/tests/integration/test_v3_4_org_isolation.py
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [PACK-17]

must_haves:
  truths:
    - "On a REAL database, with the real service-role client, a same-org non-author loads the instruction body of a private born-for skill — and a disjoint-org caller still cannot"
    - "Every hot file this phase touched carries a re-derived triple and a disposition sentence, in BOTH registers, in one commit"
    - "The phase's closing evidence is a set-diff in both directions, not a count"
  artifacts:
    - path: "backend/tests/integration/test_v3_4_org_isolation.py"
      provides: "the real-DB same-org non-author born-for load, beside the existing SEED-125 cross-org refusal"
      contains: "born_for_expert_bundle_id"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "re-derived triples + dispositions for the six touched files; the skill_visibility.py section updated from 'row added at planning' to its shipped disposition"
    - path: "CLAUDE.md"
      provides: "the same six scan rows, same commit"
  key_links:
    - from: "CLAUDE.md scan table"
      to: "docs/HOT-FILE-LEDGER.md sections"
      via: "the same-commit sync rule — a row without a section, or a section without a row, is drift"
      pattern: "skill_visibility"
---

<objective>
Close the phase with the two things a mocked unit test cannot give: evidence from a real database,
and registers that tell the truth about the files this phase made hotter.

Purpose: 264-03's `load_skill` proof is honest but bounded — it proves the query we would send and
the rows that query would admit, evaluated in-process. It does not prove what Postgres returns. The
one existing test that drives this predicate against a real DB
(`test_load_skill_cross_org_refused_seed125`) drives the **disjoint-org** axis, which is the case
that must keep failing; 264's case is the **same-org non-author** one, which must now pass. Inverting
that scenario is a one-fixture variant of a test that already has every fixture it needs.

⛔ This test sits **outside** `pytest tests/unit`, so it does not defend the ceiling. Name it as
UAT-adjacent evidence for D-264-09, never as a gate.

Output: a real-DB driver beside its cross-org sibling, six honest ledger rows in two registers, and
a closing set-diff.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-CONTEXT.md
@.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-RESEARCH.md
@.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-PATTERNS.md
@CLAUDE.md

**Worktree, base and the FIRST command.** Isolated git worktree based on
`f04d9c406b50671a2df723b767d95d0dd38401c7` after 264-01, 264-02 and 264-03 have merged. ⛔ First
command: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.

⚠ **The integration suite needs local Supabase up, and Windows can refuse its ports.** If
`_service_role_supabase_or_skip()` skips, that is a real result and must be reported as ⛔ with its
reason — never as a pass. Bring infra up with
`powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1` (idempotent). ⚠ A container
reporting `Up (healthy)` whose port accepts nothing is the Windows port-reservation trap; diagnose
with `netsh int ipv4 show excludedportrange protocol=tcp` and read CLAUDE.md's entry before
concluding anything about the test.

⛔ **This plan mutates the local database.** Per CLAUDE.md's worktree rule 4, it is serialised: it is
the only plan in wave 4 and no other plan runs concurrently with it. Worktrees isolate files, not
Postgres.

⛔ **The Supabase MCP points at PRODUCTION. Reads are free; every WRITE needs explicit per-action
operator approval.** This plan writes only to the LOCAL Supabase via the integration fixtures. Do not
use the MCP to seed anything.

<interfaces>
`backend/tests/integration/test_v3_4_org_isolation.py` `:918-996` — the scaffold this task inverts,
all of it already present:
- fixture `two_orgs_two_users` (`a` and `b`, each with `uid` and `org_id`)
- `_seed_org_shared_skill(pool, owner_uid, org_id)` → `(skill_id, name, body)`
- `_make_seed125_tool_ctx(sb, caller_uid)` → a `ToolContext` on the service-role client
- `_service_role_supabase_or_skip()`
- `test_load_skill_cross_org_refused_seed125` (`:967-995`) — the positive-control-then-refusal shape

264's variant needs: a skill seeded **private** (`is_org_shared=False`) with
`born_for_expert_bundle_id=<bundle>`, owned by user A; a SECOND user inside **A's own org** (the
existing fixture gives two users in two DIFFERENT orgs, so a same-org second member must be created
— follow whatever the fixture module already does for org membership rows); and a ctx built with
`born_for_bundle_id=<bundle>`.

**The ledger obligations, measured 2026-09-22 (RESEARCH §7) — RE-DERIVE, do not copy:**

| File | measured at research time | CLAUDE.md row said |
|---|---|---|
| `backend/app/services/tool_dispatcher.py` | 87 / 37 / 5082 | 85 / 35 / 5048 (STALE) |
| `backend/app/services/agent_loop.py` | 51 / 25 / 3473 | 48 / 22 / 3441 (STALE) |
| `backend/app/services/run_producer.py` | 9 / 5 / 899 | 5 / 3 / 749 (STALE) |
| `backend/app/services/expert_service.py` | 7 / 4 / 515 | 6 / 4 / 507 (STALE) |
| `backend/app/services/task_service.py` | 19 / 10 / 958 | accurate at research time |
| `backend/app/utils/skill_visibility.py` | 1 / 1 / 83 | row ADDED at 264 planning (both registers) |

The CLAUDE.md re-derivation recipe (subtract six-digit dated quick-task buckets like `260814`; they
are not phases):
`git log --oneline -- <file> | wc -l` ·
`git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | grep -E '^[0-9]+(\.[0-9]+)?$' | sort -u | wc -l` ·
`wc -l <file>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive SC#1 against a real database — same-org non-author, private born-for skill</name>
  <files>backend/tests/integration/test_v3_4_org_isolation.py</files>
  <read_first>
    - `.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-RESEARCH.md` §6.3 option 2 (the exact one-line variant and its cost), §2.6 (why the existing test drives the wrong axis), §8.11 (the two `[ASSUMED]` items this row can settle)
    - `.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-PATTERNS.md` §3d — the positive-control-then-target-assertion shape
    - `backend/tests/integration/test_v3_4_org_isolation.py` `:900-1010` — the fixtures, the helpers and the existing cross-org test
    - `supabase/migrations/191_skill_expert_provenance.sql`
    - `backend/app/services/tool_dispatcher.py` `:1314-1400` as widened by 264-03
  </read_first>
  <behavior>
    - Positive control: user A (the author) loads its own private born-for skill and gets the body.
    - Target: a SECOND member of A's org, with `born_for_bundle_id` set to the stamped bundle, loads the same skill and gets the SAME body from a real Postgres round-trip.
    - Narrowness on the real DB: the same second member with `born_for_bundle_id=None` is refused; with a WRONG bundle is refused.
    - The existing `test_load_skill_cross_org_refused_seed125` still passes, unmodified — the cross-org fence is untouched by this widening, on the real DB and not only in the predicate.
    - RED: the new test fails against the pre-264-03 dispatcher. Drive it by reverting the three `born_for=True` keywords, capture, restore, confirm md5.
  </behavior>
  <action>
    Add the new test beside `test_load_skill_cross_org_refused_seed125`, reusing
    `two_orgs_two_users`, `_service_role_supabase_or_skip()` and `_make_seed125_tool_ctx` exactly as
    the neighbour does. Add a seeding helper for the 264 case — a **private** skill
    (`is_org_shared=False`, `is_enabled=True`) owned by A, stamped with
    `born_for_expert_bundle_id=<a fresh UUID>` — and a second member of A's org, created the same way
    the fixture module already creates membership rows. Extend `_make_seed125_tool_ctx` (or add a
    sibling) so the ctx can carry `born_for_bundle_id`; ⛔ do not change the existing helper's
    default, or the neighbouring cross-org test starts measuring a different configuration than it
    did before.

    Drive the four cases in order: author (positive control), same-org non-author WITH the bundle
    (the target), same-org non-author with `None`, same-org non-author with a WRONG bundle. Assert on
    the returned `instructions` body string, never on a status word. Clean up every seeded row in a
    teardown, and verify the cleanup — `263-UAT.md`'s "Data left behind, deliberately" section is the
    standard: anything kept is named and justified, anything else is deleted and the deletion
    verified.

    Write the docstring to say what this green means that 264-03's could not: real RLS-bypassing
    service-role client, real PostgREST, real `public.skills` rows — the predicate is not just
    syntactically right, Postgres agrees with it. And say what it still does not defend: it is
    **outside** `pytest tests/unit`, so it will not hold the ceiling and cannot be quoted as a gate.

    ⚠ Settle RESEARCH §8.11's first `[ASSUMED]` while the fixtures are up: inspect whether any
    shipped born-for skill actually bundles files. Record the answer either way — it is the evidence
    behind widening `read_skill_file` and `execute_code`, and an assumption named in RESEARCH must
    not close as an assumption.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -q -k "seed125 or born_for"</automated>
  </verify>
  <acceptance_criteria>
    - The new test passes against a real local Supabase, and the RED against the reverted dispatcher is quoted in the SUMMARY with a matching md5 pair for `tool_dispatcher.py`.
    - `test_load_skill_cross_org_refused_seed125` is byte-unchanged: `git diff backend/tests/integration/test_v3_4_org_isolation.py` shows additions only within that function's neighbourhood, none inside it.
    - The four cases assert on the instruction body string; no case asserts only a boolean or a status word.
    - Teardown verified: a post-run count of the seeded rows is `0`, quoted in the SUMMARY.
    - If the suite SKIPS for want of a service-role client or a reachable local Supabase, that is recorded as ⛔ with the reason and the blocking condition — never reported as a pass.
    - The `[ASSUMED]` item about bundled files is answered with a measured yes or no.
  </acceptance_criteria>
  <done>The same-org non-author case is proven on a real database, and the cross-org refusal it sits beside is proven untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive every ledger triple and write both registers in one commit</name>
  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>
  <read_first>
    - `CLAUDE.md` § "Hot-file ledger" — the re-derivation recipe, the 200-char disposition cap, the same-commit sync rule
    - `.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-RESEARCH.md` §7 (the measured triples and the named seams)
    - `.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-PATTERNS.md` Appendix — the three required row shapes, quoted from live precedents
    - `docs/HOT-FILE-LEDGER.md` — the sections for `tool_dispatcher.py`, `agent_loop.py`, `run_producer.py`, `expert_service.py`, `task_service.py`, and the `skill_visibility.py` section added at 264 planning
  </read_first>
  <action>
    Re-derive the triple for each of the six files this phase touched, with the CLAUDE.md recipe,
    **after** all four plans have merged — a triple measured mid-phase is stale by the close, which
    is this ledger's own most-repeated finding. Subtract six-digit dated quick-task buckets; they are
    not phases.

    Update the scan row in BOTH registers for each file, in ONE commit. ⛔ Record every correction
    **beside** its original, never over it — the prose convention in this repo is that a stale figure
    is struck through or quoted, not deleted, because the drift is the finding. Four of the six rows
    were already measured STALE at research time (`tool_dispatcher.py`, `agent_loop.py`,
    `run_producer.py`, `expert_service.py`) — say so explicitly in each disposition.

    ⛔ The disposition cell is capped at **200 characters** and `scripts/check-claude-md-size.cjs`
    FAILS above it. Put the VERDICT in the cell and every reason, invariant and named seam in that
    file's own section in `docs/HOT-FILE-LEDGER.md`, same commit. Each disposition should be
    "honoured by construction (264-0N): <what was actually added>" plus the ⛔ invariant a future
    reader must not break.

    Update the `skill_visibility.py` section from "row added at 264 PLANNING, before the first edit"
    to its shipped disposition: the file now carries the optional-keyword widening, and its binding
    invariants gain the new ones this phase established — the default arm is pinned byte-identical by
    `==` against a frozen literal; the born-for arm carries its own enablement term because two of
    the four dispatcher sites filter enablement nowhere; the module still imports nothing under
    `app.services.`.

    ⛔ Do NOT claim any named seam is discharged. `agent_loop.py`'s standing seam (the prompt-assembly
    block, six conditional appends deep inside one 400-line branch) is **untouched** by this phase —
    say so, so the seam stays owed rather than looking closed. `tool_dispatcher.py`'s seam (a
    `skills`-resolution module, if a fifth call site ever appears) is likewise still owed.
    `expert_service.py` got tighter, not extracted.
  </action>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs &amp;&amp; node scripts/check-hot-file-ledger.cjs 264</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-claude-md-size.cjs` exits 0 — no `[disposition-too-long]`, no `[duplicate-row]`, no `[malformed-row]`.
    - `node scripts/check-hot-file-ledger.cjs 264` exits 0.
    - All six files have a row in BOTH registers whose triple was re-derived after the last merge; the derivation commands and their outputs are quoted in the SUMMARY.
    - Each of the four previously-stale rows names its old figure beside the new one.
    - `grep -c "seam" docs/HOT-FILE-LEDGER.md` in the `agent_loop.py` section still shows the prompt-assembly seam as OWED, not discharged.
    - The CLAUDE.md row and the `docs/HOT-FILE-LEDGER.md` section for every one of the six files changed in the same commit (`git show --stat HEAD` names both files).
  </acceptance_criteria>
  <done>Six honest rows, two registers, one commit, no discharged seams claimed.</done>
</task>

<task type="auto">
  <name>Task 3: Closing gate sweep — set-diff both directions, and every guardrail run for real</name>
  <files>backend/tests/integration/test_v3_4_org_isolation.py</files>
  <read_first>
    - `CLAUDE.md` § "Backend unit testing baseline gate", § "Parallel execution" (the `GSD_VITEST_MAX_WORKERS=2` rule and the count-gate contract), § "Workflow guardrails"
    - `.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-RESEARCH.md` §6.1 (the captured base FAILED set), §6.5 (the false-RED fence)
    - `.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-CONTEXT.md` § cross_checks — the seeds sweep that must be RE-RUN now that `files_modified` exists
  </read_first>
  <action>
    Run the full backend unit gate from `backend/` with the venv and capture the FAILED SET, not the
    count: `pytest tests/unit -q --continue-on-collection-errors 2>&1 | grep "^FAILED" | sort`.
    Set-diff it against the base capture in BOTH directions. ⛔ A plan that fixes one inherited red
    and introduces one new red reads `71` and is broken. Report the two diffs explicitly, even when
    both are empty.

    Run the frontend count gate once, from the repo ROOT, with `GSD_VITEST_MAX_WORKERS=2`, and read
    the verdict line VERBATIM rather than summarising it. This phase should touch no frontend source
    — state that as a measured fact (`git diff --stat` over `frontend/`), and report the gate's
    current totals as the re-derivation this repo asks for rather than quoting CLAUDE.md's figures,
    which have rotted seven times.

    ⚠ `backend/tests/test_eval_runner.py::test_deep_mode_byte_identical_guard` reads RED on an
    uncommitted `agent_loop.py`. Everything is committed by now, so confirm it is GREEN and say so —
    that confirmation is what closes the warning 264-01 raised.

    Re-run the seeds sweep now that PLAN.md files exist:
    `node scripts/check-seeds-register.cjs --phase 264`. The context-time reading matched 0 seeds
    ONLY because the phase declared no surfaces yet. Route anything it surfaces — fold, defer with a
    trigger, or leave open — and write the routing back into the seed's frontmatter. ⚠ Pay particular
    attention to `SEED-129`: its third re-open trigger fires on any phase touching
    `agent_loop.py`'s skill-catalog block at `:1435`. This phase deliberately did NOT touch those
    lines; confirm that with `git diff` over the phase's whole range and record the confirmation, so
    the seed stays open on its own terms rather than being silently assumed untouched.

    Run `node scripts/check-gap-closure-rounds.cjs 264` and `node scripts/check-hot-file-ledger.cjs 264`
    and record both verdicts.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit -q --continue-on-collection-errors 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The backend FAILED set-diff is reported in BOTH directions and both are empty; the raw base and post sets are attached to the SUMMARY.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` run from the repo root; the verdict line is quoted VERBATIM and `git diff --stat` over `frontend/` is empty.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_eval_runner.py::test_deep_mode_byte_identical_guard -q` exits 0 on the committed tree.
    - `node scripts/check-seeds-register.cjs --phase 264` verdict recorded; every surfaced seed routed with its frontmatter updated.
    - `SEED-129`'s non-trigger is PROVEN: `git diff f04d9c406..HEAD -- backend/app/services/agent_loop.py` shows no change within the skill-catalog block, quoted in the SUMMARY.
    - `node scripts/check-gap-closure-rounds.cjs 264` and `node scripts/check-hot-file-ledger.cjs 264` both exit 0.
  </acceptance_criteria>
  <done>Every gate run for real, every diff reported as a set, and SEED-129 proven untriggered rather than assumed so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration fixtures → local Postgres | this plan WRITES rows to the local database; worktrees isolate files, not Postgres |
| the phase's registers → every future phase | a stale or absent ledger row makes G-5 structurally unable to fire on that file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-264-19 | Information disclosure | the real-DB driver | mitigate | The cross-org refusal test sits beside the new one and must stay byte-unchanged and green — the widening is proven narrow on the real DB, not only in the predicate. |
| T-264-20 | Tampering | local database state | mitigate | Every seeded row is torn down and the teardown VERIFIED with a post-run count; anything deliberately kept is named. Serialised as the only wave-4 plan (CLAUDE.md worktree rule 4). |
| T-264-21 | Repudiation | the closing evidence | mitigate | Set-diffs in both directions, verdict lines quoted verbatim, derivation commands recorded — a count alone is not evidence here and the SUMMARY may not present one as such. |
| T-264-22 | Tampering | production Supabase via MCP | accept | Not used by this plan. Reads are free; WRITES need explicit per-action operator approval, and none is requested. |
| T-264-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `bash scripts/bootstrap-worktree.sh "$(pwd)"` was the first command run.
- Backend unit gate: `71 failed`, set-diff empty in BOTH directions, both raw sets attached.
- `cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -q -k "seed125 or born_for"` → exits 0, or a recorded ⛔ SKIP with its reason.
- `node scripts/check-claude-md-size.cjs` → exit 0.
- `node scripts/check-hot-file-ledger.cjs 264` → exit 0.
- `node scripts/check-seeds-register.cjs --phase 264` → verdict recorded, seeds routed.
- `node scripts/check-gap-closure-rounds.cjs 264` → exit 0.
- Frontend count gate run from the repo root at `GSD_VITEST_MAX_WORKERS=2`, verdict line verbatim; `git diff --stat` over `frontend/` empty.
</verification>

<success_criteria>
- SC#1's real-DB half is driven: a same-org non-author gets the body from Postgres, and the disjoint-org caller still does not.
- Six ledger rows re-derived post-merge and written to both registers in one commit, with every stale figure recorded beside its replacement.
- No named seam claimed as discharged.
- Every guardrail run for real, and the closing evidence is a set-diff.
</success_criteria>

<output>
Create `.planning/phases/264-born-for-skills-must-load-not-just-resolve/264-04-SUMMARY.md` when done.
Record: the real-DB RED capture and md5 pair, the teardown verification count, the answer to
RESEARCH §8.11's bundled-files `[ASSUMED]`, the six re-derived triples with their commands, all four
gate verdicts, the frontend verdict line verbatim, and the SEED-129 non-trigger proof.
</output>
