---
phase: 068.5-chat-surface-persistent-rendering-in-flight-pulse
plan: 02
type: verification
status: pending
created: 2026-05-14
binding_bug: BUG-260513-01
binding_requirement: CHAT-RESILIENCE-01
---

# Phase 068.5 — VERIFICATION (SC#1..SC#6 Scoreboard)

This file is the single-source-of-truth for BUG-260513-01 closure progress. Code halves of SC#3 + SC#4 land in Plan 02 commits (`c8ce3c6` pulse, `6ee6f65` retry banner). UAT halves (SC#5 cold-cache F5 paint timing + SC#6 5/5 lived-experience cycles) are USER-DRIVEN — the orchestrator collaborates with the user to execute the Chrome MCP runbook and 5/5 cycle protocol from `068.5-02-PLAN.md` Task 4 and records verdicts directly into this file.

**Status legend:** `green` = verified; `red` = failed (escalate to Plan 03 gap closure); `pending` = awaiting UAT.

---

## SC Scoreboard

| SC | Description | Expected behavior | Status | Notes |
|---|---|---|---|---|
| **SC#1** | No blank window on thread switch / in-app nav / page refresh | Cached content paints first; in-app switch never blanks the surface; F5 paints cache within ~100ms then reconciles | green | Closed in Plan 01 — hydrate in store factory + `ChatArea.tsx:134` `clearMessages()` deletion + MessageSkeleton on true cold-load. |
| **SC#2** | Phase 067.5 Branch D-3 guard preserved verbatim | `streamsStore.clearThreadBucket` refuses to wipe a bucket whose thread is `streamingThreadIdRef.current`. Hydrate-from-cache path does NOT bypass the guard. | green | Closed in Plan 01 — `L-068.5-01` grep gate verified; existing Phase 068 Vitest regression unchanged; Plan 02 retry wrap does not touch `clearThreadBucket`. |
| **SC#3** | In-flight pulse on assistant messages with `runStatus === 'streaming'` | Bot icon container carries `animate-brandPulse` ONLY when `runStatus === 'streaming'`; never on `completed` / `failed` / `cancelled` / `timed_out` / `undefined`. Mutual exclusivity with Resume button is structural (enum is one value at a time). | green | Closed in Plan 02 Task 2 (commit `c8ce3c6`). `@keyframes brandPulse` + `.animate-brandPulse` registered in `index.css`; `brandPulse` registered in `tailwind.config.js` `extend.keyframes` + `extend.animation`. MessageItem.tsx Bot icon carries the conditional class. 7/7 pulse-class-gating Vitest tests GREEN. Enum-mismatch grep gate (`running\|queued`) returns 0. |
| **SC#4** | Inline retry banner over cached content (no blank-on-failure) | First fetch failure → silent retry at 1s. Second failure → amber-400 sticky banner above MessageList (sibling of `fallbackNotice`) with Retry + Dismiss buttons. Cached content remains visible during all retry attempts. AbortError early-returns (no retry, no banner). | green (Vitest) / pending (browser UX cycle) | Code half closed in Plan 02 Task 3 (commit `6ee6f65`). 5/5 retry-banner Vitest tests GREEN. Browser UX exercise (Network → block `/threads/*/messages` → banner appears after 1s, dismiss works, manual Retry re-fires reconcile) recorded below under "SC#4: Retry Banner Behavior". |
| **SC#5** | Chrome MCP UAT: cold-cache F5 paint timing | Cold-cache F5 → re-navigate to a 38KB thread → cached snapshot paints within 100ms of route-render; reconciled fresh data within 1s; no visible blank intermediate state. | pending | Awaiting UAT collaboration. See Runbook + table below. |
| **SC#6** | 5/5 lived-experience UAT cycles (BUG-260513-01 closure gate) | Cycle protocol: open Thread A → send long prompt to A → mid-stream click Thread X (cached paints, no blank, no pulse on X's static messages) → click Thread A again (streaming still active, brand pulse visible, accumulated content rendered, pulse stops on terminal). 5/5 cycles GREEN consecutively. | pending | Awaiting UAT collaboration. See Runbook + table below. |

---

## SC#5: Chrome MCP Cold-Cache F5 Paint Timing

**Runbook** (from `068.5-02-PLAN.md` Task 4 — SC#5 section, do not modify):

**Preconditions:**
1. Backend running locally (`cd backend && uvicorn app.main:app --reload`).
2. Frontend dev server running (`cd frontend && npm run dev`).
3. Test login `fhdmrd@gmail.com / 123456` at `http://localhost:5173/`.
4. Thread X exists with ≥25 KB of message content (≥30 messages, ideally the 38KB dissertation thread from BUG-260513-01 evidence).

**Steps:**
1. Log into the app fresh; navigate to Thread X. Wait for it to fully load.
2. Chrome DevTools → Application → Local Storage → `http://localhost:5173/`. Confirm `agentic-rag.streams.v1` key exists and contains Thread X's messages.
3. Chrome DevTools → Performance → Record → press F5 → wait for page settle → stop recording.
4. In the trace, identify timestamps:
   - `T_route` — first Paint after JS load (route-render commit).
   - `T_cached_paint` — first frame where Thread X's message text is visible.
   - `T_reconcile` — frame at which `GET /threads/X/messages` resolves AND a re-render commits.
5. Scrub the screenshot strip from `T_route` to `T_cached_paint` to verify no frame shows a blank message-list region.

**Result Scoreboard:**

| Condition | Expected | Observed | Verdict |
|---|---|---|---|
| `T_cached_paint - T_route` ≤ 100 ms | ≤ 100 ms | _pending_ | pending |
| `T_reconcile - T_route` ≤ 1000 ms | ≤ 1000 ms | _pending_ | pending |
| No fully-blank message-list frame between `T_route` and `T_cached_paint` | no blank frame | _pending_ | pending |

**Overall SC#5 verdict:** pending

---

## SC#6: 5/5 Lived-Experience UAT Cycles

**Cycle protocol** (from `068.5-02-PLAN.md` Task 4 — SC#6 section, do not modify; mirrors Phase 067.5 precedent that closed STREAM-04-correctness-round3):

a. Open Thread A; wait for load.
b. Send a longer-form prompt to Thread A (one that takes >3 seconds to stream — e.g., "Explain the history of supabase realtime in 5 paragraphs"). Wait for streaming to begin (assistant Bot icon appears with `animate-brandPulse` visible).
c. Mid-stream (BEFORE the terminal event arrives — typically within the first 1-2 seconds), CLICK Thread X in the sidebar.
d. Verify: Thread X loads with cached content visible immediately (no blank window). Bot icon pulses are NOT visible on Thread X's messages (X has no active stream).
e. CLICK Thread A again. Verify: Thread A's streaming is STILL active; Bot icon pulses are visible on the in-flight assistant turn; the accumulated streamed content is rendered (not blank). When the terminal event arrives, the pulse stops; Resume button is absent (run completed successfully).
f. Record cycle result: GREEN if all of (b/c/d/e) match expectations; RED otherwise (note the failure mode).

**5/5 means 5 consecutive GREEN cycles.** A flaky failure breaks the streak; restart at cycle 1. Structural failures (blank window re-appears, pulse doesn't stop, retry banner shows when it shouldn't) → STOP, reproduce 2 more times, document in `## Gaps`, escalate to Plan 03.

**Result Scoreboard:**

| Cycle | A→X switch render | X→A switch render | Pulse during stream (on A) | Pulse stops on terminal | Verdict |
|---|---|---|---|---|---|
| 1 | _pending_ | _pending_ | _pending_ | _pending_ | pending |
| 2 | _pending_ | _pending_ | _pending_ | _pending_ | pending |
| 3 | _pending_ | _pending_ | _pending_ | _pending_ | pending |
| 4 | _pending_ | _pending_ | _pending_ | _pending_ | pending |
| 5 | _pending_ | _pending_ | _pending_ | _pending_ | pending |

**Overall SC#6 verdict:** pending (5/5 GREEN required to flip BUG-260513-01 `folded → closed` at `/gsd:complete-milestone`)

---

## SC#4: Retry Banner Behavior (Browser UX exercise, non-gating)

**Runbook** (from `068.5-02-PLAN.md` Task 4 — SC#4 sanity section):

1. Open Chrome DevTools → Network tab → right-click any `/threads/*/messages` request and "Block request URL". This blocks the next 2 reconcile attempts.
2. Navigate to a thread; observe: no banner appears for ~1s (silent retry); after the second failure, the amber banner appears above MessageList with copy "Couldn't load latest messages. Showing cached version." + Retry + × buttons.
3. Click `×`. Banner disappears; cached content remains visible.
4. Reproduce; click "Retry". Banner clears momentarily; if blocking still active, banner reappears after the 1s silent retry. If blocking removed mid-retry, banner clears permanently and fresh content loads.

**Observation log:**

_pending — to be filled by orchestrator during UAT_

---

## Gaps

_None recorded — all SCs pending UAT. If any UAT cycle goes RED, gap entry format below:_

```
### Gap NN — {short description}
- Cycle / SC: {which cycle or condition}
- Failure mode: {observed behavior}
- Reproduction: {step-by-step}
- Escalation: {Plan 03 gap closure / revise Plan 02 / etc.}
```

---

## Cross-references

- **Plan:** `.planning/phases/068.5-chat-surface-persistent-rendering-in-flight-pulse/068.5-02-PLAN.md`
- **Plan 01 SUMMARY:** `.planning/phases/068.5-chat-surface-persistent-rendering-in-flight-pulse/068.5-01-SUMMARY.md`
- **Bug report:** `.planning/reported-bugs/thread-switch-blank-state-load-latency.md` (BUG-260513-01; `folded_into: 068.5`; flips to `closed` at `/gsd:complete-milestone v2.6` when SC#6 5/5 GREEN)
- **Precedent for 5/5 lived-experience cycles:** Phase 067.5 closed STREAM-04-correctness-round3 with literal 5/5 user-driven cycles. The pattern repeats here.

*Phase: 068.5-chat-surface-persistent-rendering-in-flight-pulse*
*Plan: 02 (code-complete; SC#5 + SC#6 UAT pending)*
