---
phase: 253-the-bootstrap-artifact-tells-the-whole-truth
plan: 03
type: tdd
wave: 3
depends_on: [253-02]
gap_closure: true
gap_closure_round: 1
autonomous: false
requirements: [CRED-03, CRED-04]
files_modified:
  - scripts/check-schema-acl-parity.cjs
  - scripts/check-greenfield-privileges.py
  - .claude/hooks/schema-acl-parity-guard.js
  - .claude/settings.json
  - .github/workflows/backend-tests.yml
  - backend/tests/unit/test_253_greenfield_sql_lexer.py
  - backend/tests/unit/test_253_ci_path_coverage.py
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md

must_haves:
  truths:
    - "Deleting an ACL from `supabase/full-schema.sql` — the artifact an operator pastes into a greenfield project — makes `node scripts/check-schema-acl-parity.cjs` exit non-zero. Today it exits 0: the gate never opens that file (CR-01, driven by the reviewer on this tree)."
    - "An ACL statement that follows `COMMENT ON COLUMN … IS '… -- …';` is present in `check-greenfield-privileges.py`'s expectation model. Today it is invisible, and the harness then agrees with a greenfield database that is missing the same REVOKE — exit 0, in the permissive direction (CR-02)."
    - "A MultiEdit to a migration or to `scripts/full-schema-supplement.sql` runs the parity gate. Today the PostToolUse matcher is `Write|Edit` and MultiEdit fires nothing (WR-02)."
    - "A change to `scripts/full-schema-supplement.sql` or to `scripts/check-greenfield-privileges.py` triggers the CI job that runs their fences. Today `backend-tests.yml` triggers on `backend/**` only, so the §5 column fence does not run in CI for the change that breaks it (WR-08 / WR-06)."
    - "Each of the four above is proven by an arm that lives in the repository and was SEEN to fail before the fix — not by a source-presence assertion and not by prose in a SUMMARY."
  artifacts:
    - path: "scripts/check-schema-acl-parity.cjs"
      provides: "tail-identity read of supabase/full-schema.sql + --self-test arms for it"
      contains: "full-schema.sql"
      exports: ["analyse", "report", "aclsIn", "statements", "normaliseSignature"]
    - path: "scripts/check-greenfield-privileges.py"
      provides: "a literal-, dollar- and block-comment-aware statement splitter feeding derive_expectations"
      contains: "_statements"
    - path: "backend/tests/unit/test_253_greenfield_sql_lexer.py"
      provides: "the CR-02 RED: a REVOKE after a `--`-bearing literal IS parsed; a dollar-quoted phantom is NOT"
      min_lines: 60
    - path: "backend/tests/unit/test_253_ci_path_coverage.py"
      provides: "the WR-08 RED: every non-backend file a backend unit test reads is inside backend-tests.yml's push AND pull_request path filters"
      min_lines: 50
    - path: ".claude/settings.json"
      provides: "PostToolUse registration of the parity guard for Write|Edit|MultiEdit"
      contains: "MultiEdit"
    - path: ".github/workflows/backend-tests.yml"
      provides: "path filters covering the two scripts/ files backend unit tests read"
      contains: "scripts/full-schema-supplement.sql"
  key_links:
    - from: "scripts/check-schema-acl-parity.cjs"
      to: "supabase/full-schema.sql"
      via: "byte-level tail comparison against scripts/full-schema-supplement.sql (D-11)"
      pattern: "full-schema\\.sql"
    - from: ".claude/settings.json"
      to: ".claude/hooks/schema-acl-parity-guard.js"
      via: "PostToolUse matcher Write|Edit|MultiEdit"
      pattern: "Write\\|Edit\\|MultiEdit"
    - from: ".claude/hooks/schema-acl-parity-guard.js"
      to: "tool_input.edits[].file_path"
      via: "path-candidate extraction for the MultiEdit payload shape"
      pattern: "edits"
    - from: ".github/workflows/backend-tests.yml"
      to: "backend/tests/unit/test_253_supplement_column_parity.py"
      via: "paths: scripts/full-schema-supplement.sql"
      pattern: "full-schema-supplement"
    - from: "scripts/check-greenfield-privileges.py::derive_expectations"
      to: "_statements"
      via: "statement iteration replacing _strip_sql_comments(...).split(';')"
      pattern: "_statements\\("
---

<objective>
Close the five findings the operator locked out of `253-REVIEW.md`: **CR-01, CR-02, WR-02, WR-06, WR-08**.

Every one of them is the same shape — **a guard that cannot fail**. The phase's own VERIFICATION
passed 14/14 with zero gaps; the code review found these anyway, which is the point of this round.

Purpose: Phase 253 exists so a database born from `supabase/full-schema.sql` has the same privileges
as one built by replaying migrations, **and so the gate that claims to prove it can actually fail.**
The review measured that the gate never opens the artifact it protects, that the harness measuring
the same property reproduces the very comment-stripping defect its sibling fixed this same phase,
that a MultiEdit bypasses the guard entirely, and that two fences never run in CI for the change
that breaks them.

Output: four driven RED→GREEN proofs living in the repository — new `--self-test` arms, two new
pytest fences, and a driven hook invocation — plus the register updates the same-commit rule owes.

⛔ **This closes ROADMAP SC#3 and SC#4 against the phase GOAL, not against their literal wording.**
SC#3 ("the parity gate fails on a PARTIAL revoke") and SC#4 ("a comment cannot hide an ACL from the
gate") were both verified true of `check-schema-acl-parity.cjs` — and both are still FALSE one file
over, in `check-greenfield-privileges.py`, and FALSE of the artifact `full-schema.sql` itself.
</objective>

<scope_lock>
**Exactly five findings. Nothing else from `253-REVIEW.md`.** The other 8 warnings and 5 info items
are deferred with a concrete re-open trigger each — see `<deferred>` at the bottom of this plan.
⛔ **Three of those deferrals (IN-02, IN-04, IN-05) sit in files this plan edits.** They are left
alone deliberately, by operator lock, and that is recorded rather than quietly taken.
</scope_lock>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW.md
@.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-CONTEXT.md
@.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-02-SUMMARY.md
@scripts/check-schema-acl-parity.cjs
@scripts/check-greenfield-privileges.py
@.claude/hooks/schema-acl-parity-guard.js

<interfaces>
<!-- Extracted from the tree at plan time. Use these directly — no codebase exploration needed. -->

`scripts/check-schema-acl-parity.cjs` (883 lines) — the shapes this plan extends:

  - `const MIGRATIONS_DIR = path.join(root, 'supabase', 'migrations')` (:90)
  - `const SUPPLEMENT = path.join(root, 'scripts', 'full-schema-supplement.sql')` (:91)
    ⛔ there is **no** `FULL_SCHEMA` constant — that absence IS CR-01.
  - `const MIN_MIGRATION_FILES = 120` (:108) · `class VacuousScanError` (:110) · `fail(msg)` → exit 2 (:113)
  - `analyse({ migrationsDir, supplementPath, minFiles = MIN_MIGRATION_FILES })` (:463) →
    `{ migrationCount, expected: Map<key,{label,kind,files}>, missing, mirrored, fn, tbl }`
  - `report(result, log = console.log)` (:487) → `0 | 1`; `log` is injectable, which is how
    `--self-test` reads the failure text.
  - `runSelfTest()` (:669) — `check(name, ok, detail)` pushes into `results`; fixtures are built in
    `fs.mkdtempSync(os.tmpdir())` and guarded by `assertOutsideWatchedTree(:539)`; `padToFloor(:662)`
    writes `MIN_MIGRATION_FILES` pad files so the fixture exercises the shipped constant.
  - `main()` (:864) accepts exactly one flag, `--self-test`; anything else is `fail()` → exit 2.
  - `module.exports = { normaliseSignature, statements, aclsIn, analyse, report, MIN_MIGRATION_FILES }`
  - `--self-test` closes at **29/29** arms today.

`scripts/check-greenfield-privileges.py` (1157 lines):

  - `REPO_ROOT = parents[1]` · `FULL_SCHEMA` · `SUPPLEMENT` · `MIGRATIONS_DIR` (:99-102)
  - `_md5(data: bytes)` (:423) · `_tail_lines(data: bytes, n: int) -> bytes` (:427) —
    `data.splitlines(keepends=True)` then `b"".join(lines[-n:])`. ⭐ **This is the semantics CR-01
    must port** — a byte tail, not a text split-and-rejoin.
  - `assert_supplement_is_the_artifact_tail() -> tuple[str, int]` (:433) — the ONLY executable
    enforcement of D-11 today, and it needs a live Postgres run to be reached (WR-06).
  - `_strip_sql_comments(sql)` (:564) — `line.find("--")`, the CR-02 defect. **Two callers:**
    `derive_expectations` (:733, followed by `sql.split(";")` at :735) and
    `supplement_section5_columns` (:905).
  - `_split_top_level(text)` (:578) — splits a privilege list on top-level commas. Unrelated to CR-02;
    do not touch.
  - `_ACL_RE` (:598) · `_NOT_A_TABLE_ACL` (:607) · `_PRIV_RE` (:609) · `_parse_statement(chunk)` (:612)
  - `derive_expectations(...)` (:704) → `(model, files, statement_count, per_file)`
  - Module level imports are `asyncio, hashlib, os, pathlib, re, sys, time, urllib.parse` only —
    **asyncpg is imported lazily inside `_require_asyncpg()` (:391), so this module is importable in a
    pure-Python unit test with no database and no driver.**
  - `main(argv)` (:1118) takes no DSN and reads no env var (S-5); the only flag is `--self-test-skip`.
  - Exit codes: `EXIT_CLEAR=0` · `EXIT_VIOLATION=1` · `EXIT_HARNESS=2`. ⛔ **A SKIP is exit 2.**

`.claude/hooks/schema-acl-parity-guard.js` (114 lines):

  - `SUBJECTS` = three regexes: `supabase/migrations/\d+_[^/]*\.sql$`,
    `scripts/full-schema-supplement\.sql$`, `supabase/full-schema\.sql$`.
    ⚠ The third already fires on `full-schema.sql` while the gate cannot read it — CR-01's second half.
  - Path extraction today: `input.tool_input && (input.tool_input.file_path || input.tool_input.filePath)`
    — **no `edits[]` arm**, so a MultiEdit payload yields `''` and the hook exits 0 silently.
  - Normalises `path.sep` → `/` before matching; runs the gate with
    `execFileSync(process.execPath, [gate], { cwd: root, timeout: 15000 })`; emits
    `{ hookSpecificOutput: { hookEventName, additionalContext, acl_parity_gate_exit, file_path } }`.
  - `root = process.env.CLAUDE_PROJECT_DIR || process.cwd()` — this is what makes an isolated drive possible.

`.claude/settings.json`:
  - `:114-122` registers this guard with `"matcher": "Write|Edit"`.
  - `:144` registers `react-hooks-rules-guard.js` with `"matcher": "Write|Edit|MultiEdit"` — **the
    proof MultiEdit is a live tool in this configuration, and the exact string to match.**

`.github/workflows/backend-tests.yml`:
  - `push.paths` = `backend/**`, `supabase/migrations/**`, `.github/workflows/backend-tests.yml`
  - `pull_request.paths` = `backend/**`, `supabase/migrations/**` (⚠ the workflow's own file is
    absent from the PR arm — that asymmetry is pre-existing; do not "fix" it silently, see Task 4).
  - The job runs `pytest tests -q` in `backend/` after booting Redis.

`backend/tests/unit/test_253_supplement_column_parity.py`:
  - `_REPO_ROOT = Path(__file__).resolve().parents[3]` · `_SUPPLEMENT = _REPO_ROOT / "scripts" / "full-schema-supplement.sql"`
  - 7 cases, all passing, no database. **This is the fence that does not run in CI (WR-08).**
  - Precedent for loading a `scripts/` file into a unit test: `backend/tests/unit/test_241_bench_safety.py`
    uses `importlib.util.spec_from_file_location` from `parents[3]` — the house idiom, cited in its own docstring.
</interfaces>
</context>

<execution_rules>
⛔ **NEVER hand-edit `supabase/full-schema.sql`.** CR-01 is a **read-side** change: the gate learns to
read that file. A test may build or mutate a **temp copy** under the scratchpad; the tracked file is
never written. Assert its md5 unchanged at the end of any task that reads it.

⛔ **A guard nobody has seen fire is not a guard.** Every acceptance criterion below is a driven
RED→GREEN against a **named planted defect**. A source-presence grep is not evidence, and a
presence assertion cannot see content drift — assert the parsed/rendered CONTENT.

⛔ **Plant, then restore, then PROVE the restore.** Any defect planted in a real tracked file is
restored and the restore proven by md5 (the method `253-02-SUMMARY.md` §"The hook — the DRIVEN half"
used). Prefer a scratchpad copy where the fixed-path design allows one.

⛔ **A SKIP is exit 2 and is not a pass.** If the local Postgres is unreachable, the greenfield
harness's end-to-end arm is recorded as **owed**, naming `scripts/start-local-infra.ps1` — never
reported as green.

⚠ **Serialisation (CLAUDE.md worktree rule 4), stated rather than left implicit.** Task 2's optional
end-to-end harness run CREATEs and DROPs a scratch database on the shared local cluster, which no
`files_modified` check can see. **This plan is the only plan in wave 3**, so the rule is satisfied by
construction — but if this plan is ever split, the harness task must stay serial.

⚠ **Worktree bootstrap is the FIRST action** — `bash scripts/bootstrap-worktree.sh "$(pwd)"` before
the HEAD assertion and before reading anything. A worktree that skipped it reports green typechecks
and red tests for reasons that look like this plan's fault.

⚠ **The frontend diff of this plan is EMPTY.** `vitest-count-gate` is therefore **out of scope** and
is deliberately not run — recorded here so its absence reads as a decision, not an omission.

⚠ **Backend ceiling: `71 failed / 3497 passed / 0 collection errors`, zero headroom.** Both new test
files must PASS, so the ceiling is unchanged. Capture the failing node-id **SET** and diff it against
253-02's — `| tail` keeps a count and throws away the set, which has already published a wrong
baseline in this project once.

⚠ **Python work uses the venv**: `backend/venv/Scripts/python`. Canonical command, run in `backend/`:
`pytest tests/unit -q --continue-on-collection-errors`.

⚠ **`.claude/settings.json` is an operator config file.** Task 3 edits it additively, re-validates
with `JSON.parse`, and pastes the `git diff` into the SUMMARY. The executor will need approval.

⚠ **G-5 / ledger:** `scripts/` is EXEMPT in `check-hot-file-ledger.cjs` (`WATCHED` is
`backend/app/` + `frontend/src/` only), and nothing this plan touches is under either. **No gate can
demand a row here, and the gate's green must NOT be cited as clearance** — Task 5 carries its own
re-derived evidence, exactly as D-23 required of `253-02`.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — the parity gate reads the artifact it exists to protect</name>
  <files>scripts/check-schema-acl-parity.cjs</files>
  <read_first>
    - `scripts/check-schema-acl-parity.cjs` — the whole file. Note `SUPPLEMENT` (:91), the absent
      `FULL_SCHEMA`, `analyse()` (:463), `report()` (:487), `runSelfTest()` (:669), `main()` (:864).
    - `scripts/check-greenfield-privileges.py:423-460` — `_md5`, `_tail_lines`,
      `assert_supplement_is_the_artifact_tail`. **This is the source of truth for the semantics being
      ported**: a byte tail via `splitlines(keepends=True)[-n:]`, compared by md5.
    - `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW.md` §CR-01 —
      including its proposed JS snippet. ⛔ **Evaluate it, do not paste it.** Its
      `split('\n').slice(-n).join('\n')` round-trips through text and drops the distinction between a
      file that ends with a newline and one that does not; on this box `core.autocrlf=true` leaves
      `\r` inside every line, so a re-encode is a second place for bytes to change. Port
      `_tail_lines`' byte semantics instead.
    - `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-CONTEXT.md` D-11
      (same-commit rule), D-13 (ordering + reverse drift OUT), D-14 (the failure text MUST state what
      is and is not checked), D-18 (every planted defect lives inside `--self-test`).
  </read_first>
  <behavior>
    - RED 1 (the reviewer's reproduction, re-driven without touching the tracked file): a fixture
      artifact built as `<arbitrary head>` + the REAL supplement, with the four
      `resize_embedding_column` ACL lines stripped from the **copy**, makes `analyse`/`report` exit
      **1**. Against the shipped gate this arm is impossible to even express — that impossibility is
      the RED, and the arm must be seen to fail once with the check stubbed out before it is trusted.
    - RED 2: a fixture artifact whose tail differs from its supplement by a single byte exits 1, and
      the failure text names `supabase/full-schema.sql`, the line count, and both md5s.
    - RED 3: a MISSING artifact file is a harness error (exit 2 / `VacuousScanError`), never a pass.
    - COUNTERFACTUAL 1: an artifact whose tail IS the supplement, byte for byte, stays exit 0 — so the
      arms fail for the planted defect and not for the fixture.
    - COUNTERFACTUAL 2 (CRLF): the same identical pair written with `\r\n` line endings also stays
      exit 0. The gate already pins CRLF parity for the lexer; this extends it to the tail read.
    - REAL-TREE: `node scripts/check-schema-acl-parity.cjs` on the untouched repo still exits 0, and
      its scan line now prints the tail line count and md5 it verified.
  </behavior>
  <action>
    Add `const FULL_SCHEMA = path.join(root, 'supabase', 'full-schema.sql')` beside `SUPPLEMENT`.

    Add `assertTailIdentity(supplementPath, artifactPath)` returning `{ ok, lines, md5, detail }`.
    Read BOTH files as Buffers. Derive `n` as the supplement's line count and slice the artifact's
    last `n` lines **as bytes** — split the artifact Buffer on line terminators keeping the
    terminators (the Node equivalent of Python's `splitlines(keepends=True)`), join the last `n`, and
    compare with `Buffer.equals` plus an md5 via `crypto.createHash('md5')` for the message. Do NOT
    decode to a string, slice, and re-encode. A missing or unreadable file throws `VacuousScanError`
    (exit 2), matching `scanSupplement`'s existing shape.

    Thread `artifactPath = FULL_SCHEMA` through `analyse()`'s options object so `--self-test` can
    inject a fixture, exactly as `supplementPath` already is. `analyse()` returns the tail result on
    the result object (`tail`); `report()` prints `tail: N lines · md5 <hex>` in the scan line and, on
    mismatch, returns **1** with a failure block that names both paths, both md5s, the line count, and
    the remedy — that `scripts/regenerate-full-schema.sh` appends the supplement verbatim, so the same
    text goes into both files in the SAME commit (D-11). Report the tail mismatch **before** the tuple
    verdict: an artifact that has lost its tail makes every tuple statement about it meaningless.

    Update the gate's header docstring and the failure-text "WHAT THIS GATE DOES NOT CHECK" block
    (D-14): the D-11 tail identity is now CHECKED and must move out of the "does not" list wherever it
    is implied there, and the `INVOKED BY` / coverage prose must not claim more or less than the code
    does. `docs/HOT-FILE-LEDGER.md`'s admission *"There is no gate behind that rule"* is now false and
    is corrected in Task 5.

    Add the five arms above to `runSelfTest()` using the existing `check(name, ok, detail)` helper and
    the existing `mkdtempSync` + `assertOutsideWatchedTree` fixture discipline (D-18). Build the
    fixture artifacts from the **real** `scripts/full-schema-supplement.sql` read at runtime (never a
    typed copy of it), inside the temp dir. Record the arm total in the final self-test line.

    ⛔ Do not add a `--strict` flag, a reverse-drift check, or a tuple floor here — WR-01 and WR-03 are
    deferred (see `<deferred>`).
  </action>
  <acceptance_criteria>
    - Before the fix, with the new check stubbed to a no-op, the two RED arms are SEEN to fail — the
      self-test prints `FAIL` for them. Paste that output into the SUMMARY. A RED that was never
      observed red is the vacuous-guard defect this round exists to close.
    - `node scripts/check-schema-acl-parity.cjs --self-test` exits **0** and reports **≥ 34** arms
      (29 today + the 5 new). The printed total is quoted verbatim in the SUMMARY, not summarised.
    - The reviewer's own reproduction, re-driven on a **scratchpad copy** of `supabase/full-schema.sql`
      with `resize_embedding_column` lines stripped: `grep -c resize_embedding_column <copy>` → `0`,
      and the gate pointed at that copy exits **1** naming `full-schema.sql`. Against `HEAD~` it
      exits 0. Both exit codes recorded.
    - `node scripts/check-schema-acl-parity.cjs` on the untouched tree exits **0**, prints
      `mirrored: 133/133` (or a larger figure, re-derived — a bigger number is the gate working), and
      prints a tail md5 equal to the md5 of `scripts/full-schema-supplement.sql` computed
      independently in the shell.
    - `git status --short` shows `supabase/full-schema.sql` unmodified, and its md5 is byte-identical
      to the value measured before the task started.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-schema-acl-parity.cjs --self-test &amp;&amp; node scripts/check-schema-acl-parity.cjs</automated>
  </verify>
  <done>The gate opens `supabase/full-schema.sql`, fails on a tail divergence, refuses a missing artifact with exit 2, stays green on an identical pair under both LF and CRLF, and the two RED arms were observed failing before the fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-02 — the greenfield harness stops losing an ACL behind a `--` inside a literal</name>
  <files>scripts/check-greenfield-privileges.py, backend/tests/unit/test_253_greenfield_sql_lexer.py</files>
  <read_first>
    - `scripts/check-greenfield-privileges.py` — `_strip_sql_comments` (:564), `_split_top_level`
      (:578), `_ACL_RE`/`_NOT_A_TABLE_ACL`/`_PRIV_RE` (:598-609), `_parse_statement` (:612),
      `derive_expectations` (:704-747, note `sql.split(";")` at :735),
      `supplement_section5_columns` (:892-911, the SECOND caller of the stripper).
    - **`scripts/check-schema-acl-parity.cjs:150-283` — `statements()`, the lexer being ported FROM,
      and its docstring recording WHY** (`--` inside a literal eats the closing quote and the
      semicolon; the "drop the `^\s*` anchor" patch was rejected as the sole remedy).
      `scripts/check-schema-acl-parity.cjs:718-760` — the `--self-test` arms for comment-swallow, the
      dollar-quoted phantom **false-positive control**, and the doubled-quote escape. These arms are
      the specification for the Python ones; ⚠ note that 253-02 found its first dollar-quote arm
      **vacuous** (it passed against its own planted defect) and replaced it with a false-positive
      control. Port the REPLACED arm, not the vacuous one.
    - `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW.md` §CR-02,
      including the proposed `_statements` Python snippet — ⛔ evaluate it: it drops block-comment
      NESTING depth handling for `/* /* */ */` correctness in the same way the `.cjs` does not, and it
      returns comment text as a bare space. Reconcile with the `.cjs` behaviour and say which you kept.
    - `backend/tests/unit/test_241_bench_safety.py:26-60` — the house idiom for importing a `scripts/`
      module into a unit test (`importlib.util.spec_from_file_location` from `parents[3]`).
    - `supabase/migrations/180_app_settings_self_hosted_endpoints.sql:29-32` — the live construct.
  </read_first>
  <behavior>
    - RED 1 (COMMENT-SWALLOW, the review's reproduction): the fixture
      `COMMENT ON COLUMN public.t.c IS 'a value -- with a dash';` followed by
      `REVOKE ALL ON public.secrets FROM anon;` yields a parsed statement for the REVOKE —
      `_parse_statement` returns `("REVOKE", …, "public.secrets", ["anon"])`. Against the shipped
      splitter it returns `None` for every chunk. **That None is the RED.**
    - RED 2 (the same shape with `FROM PUBLIC`): a `REVOKE ALL ON public.secrets FROM PUBLIC;` after
      a `--`-bearing literal is likewise **split out as its own statement**. ⚠ Assert the SPLIT, not
      the model — `ROLES` still drops the `public` grantee (WR-05, deferred): assert the chunk exists
      and its text is intact, and do not write an assertion that would go green if WR-05 were fixed
      or stay red because it is not.
    - RED 3 (dollar-quote FALSE-POSITIVE CONTROL): an ACL-shaped line inside a `DO $$ … $$;` body is a
      phantom and is **NOT** returned as a top-level statement. This arm must be seen to fail against
      a deliberately reverted lexer, or it is vacuous — 253-02 shipped a vacuous one of exactly this
      shape and caught it only by falsifying the arm itself.
    - RED 4: a doubled single-quote escape (`'it''s -- fine'`) does not end the literal, so the ACL
      after it is still found.
    - PRESERVED (the property the old stripper existed for): an entirely-commented VERIFY block
      quoting a GRANT contributes **no** statement. A false red is how a guard gets switched off.
    - REAL-CORPUS COUNTERFACTUAL: `derive_expectations()` over the real `supabase/migrations/` yields a
      statement count and per-file map. The review measured **32 statements across 12 files**
      (`118 126 127 128 129 150 151 156 168 169 172 177`). Re-derive before and after. If the number
      MOVES, the newly-visible statement is named by file and line in the SUMMARY — never absorbed.
    - PRESERVED: `supplement_section5_columns()` returns the identical tuple before and after
      (20 columns, `created_by` included per MC-3) — proven by capturing both.
  </behavior>
  <action>
    Write `backend/tests/unit/test_253_greenfield_sql_lexer.py` FIRST and run it against the unmodified
    harness so RED 1, 3 and 4 are observed failing. Load the script with the `test_241_bench_safety.py`
    importlib idiom (it is not on the import path); the module imports no asyncpg at module level, so
    the test needs **no database, no driver and no environment variable** — state that in its docstring,
    with the same reasoning `test_253_supplement_column_parity.py` gives for why it touches no DB.
    Include a non-vacuity control: assert the loaded module actually exposes `_parse_statement` and the
    new splitter, so a rename cannot turn this fence into a silent skip.

    Then, in `scripts/check-greenfield-privileges.py`, add `_statements(sql: str) -> list[str]`: a
    single-pass lexer splitting on **top-level** `;` while tracking single-quoted literals (with the
    doubled-quote escape), double-quoted identifiers, dollar-quoted bodies (`$$…$$` and `$tag$…$tag$`),
    line comments (`--` to end of line) and block comments (`/* … */`). A `--` opens a comment and a
    `;` ends a statement **only at depth zero**. Mirror the `.cjs` `statements()` semantics; where you
    diverge (block-comment nesting, what a stripped comment leaves behind), write the divergence into
    the docstring rather than leaving it to be discovered — WR-11 is deferred precisely because the two
    parsers already disagree in three measured ways, and a fourth undocumented one is worse than a
    documented one.

    Point `derive_expectations` at `_statements(raw_sql)` and delete the `_strip_sql_comments(...)` +
    `sql.split(";")` pair at :733-735. Keep `supplement_section5_columns` (:905) working: either feed
    it from `_statements` or rebuild its comment-strip on the same lexer. ⛔ Its output must be
    byte-identical before and after — capture both and compare.

    `_strip_sql_comments` is removed only if it has no remaining caller; if it survives, its
    `line.find("--")` body is replaced so the defect does not live on behind a second door.
    ⛔ Do NOT touch `_split_top_level` (privilege-list commas — unrelated), `_NOT_A_TABLE_ACL`
    (WR-09, deferred), `ROLES` (WR-05, deferred), or `assert_derived_set` (WR-04, deferred).

    Finally run the harness end to end once — `backend/venv/Scripts/python scripts/check-greenfield-privileges.py`
    — to prove the lexer change did not collapse the expectation model against a real database. ⛔ If
    the local Postgres is down the script SKIPs at **exit 2**; record that as OWED with
    `scripts/start-local-infra.ps1` as the remedy and do not call the task green on the fixture arms
    alone. ⚠ This is the only DB-touching step in the plan; it is serial by construction (wave 3, one plan).
  </action>
  <acceptance_criteria>
    - The new pytest file is run against the **unmodified** harness and RED 1, 3 and 4 FAIL. The
      failure output is pasted into the SUMMARY, naming the planted/real defect each arm caught.
    - After the fix: `cd backend && venv/Scripts/python -m pytest tests/unit/test_253_greenfield_sql_lexer.py -q`
      exits 0 with every arm passing, and the dollar-quote control is separately shown to FAIL when the
      lexer is temporarily reverted (the arm fails for the defect, not for the fixture).
    - `derive_expectations()` statement count and per-file map are printed before and after. Either
      they are identical to the review's `32 / 12`, or every difference is named by file and line.
    - `supplement_section5_columns()` output is identical before and after — both tuples printed,
      length 20, `secret_ciphertext` absent, `created_by` present.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_253_supplement_column_parity.py -q`
      still passes 7/7 (the sibling fence over the same artifact is unaffected).
    - `backend/venv/Scripts/python scripts/check-greenfield-privileges.py` exits **0** on a real
      scratch database, OR exits **2** as a SKIP recorded as owed with the reason. ⛔ Exit 2 is never
      written as a pass. If it runs, the scratch DB teardown is confirmed (`0` `greenfield_acl_*`
      databases left behind) and the operator's data is confirmed intact.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_253_greenfield_sql_lexer.py tests/unit/test_253_supplement_column_parity.py -q</automated>
  </verify>
  <done>An ACL following a `--`-bearing string literal is parsed by the harness; a dollar-quoted phantom is not; the real-corpus statement count is unchanged or every delta is named; and the §5 column extraction is byte-identical.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: WR-02 — a MultiEdit fires the parity guard (hook + operator config)</name>
  <files>.claude/hooks/schema-acl-parity-guard.js, .claude/settings.json</files>
  <read_first>
    - `.claude/hooks/schema-acl-parity-guard.js` — the whole file; especially `SUBJECTS` and the
      `tool_input.file_path || tool_input.filePath` extraction that yields `''` for a MultiEdit.
    - `.claude/settings.json:100-135` — the three adjacent `"matcher": "Write|Edit"` PostToolUse
      entries, and `:144` — `react-hooks-rules-guard.js` at `"matcher": "Write|Edit|MultiEdit"`,
      **the sibling that proves MultiEdit is live in this configuration and the exact string to copy**.
    - `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-02-SUMMARY.md`
      §"The hook — the DRIVEN half" — the five-case drive table, the md5-verified restore, and the
      rule that **the negative case is driven with the defect STILL PLANTED**.
    - `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW.md` §WR-02.
  </read_first>
  <behavior>
    - RED: a MultiEdit-shaped payload — `{"tool_name":"MultiEdit","tool_input":{"file_path":"…/scripts/full-schema-supplement.sql","edits":[{…}]}}`
      AND the harsher shape where `file_path` is absent and only `edits[].file_path` carries the path —
      piped to the hook **with a defect planted**, produces **zero bytes** of stdout today. That silence
      is the RED.
    - GREEN: the same two payloads produce the `hookSpecificOutput` JSON with `acl_parity_gate_exit: 1`
      and `additionalContext` naming the unmirrored tuple.
    - NEGATIVE CONTROL, **defect still planted**: a MultiEdit payload naming only non-subject files
      (`frontend/src/App.tsx`, `supabase/seed.sql`) stays silent — so the silence proves the path
      filter and not a clean tree.
    - CONFIG: the parity-guard PostToolUse entry's matcher string equals the `react-hooks-rules-guard`
      entry's matcher string, read out of the parsed JSON — a derived comparison, not a typed literal.
  </behavior>
  <action>
    In the hook, widen path extraction to a CANDIDATE LIST: `tool_input.file_path`,
    `tool_input.filePath`, `tool_input.notebook_path`, and every `file_path` in
    `Array.isArray(tool_input.edits) ? tool_input.edits : []`. Filter falsy, normalise each with the
    existing `path.sep` → `/` rewrite, and exit 0 only when **no** candidate matches `SUBJECTS`.
    Everything downstream (gate invocation, exit-code handling, `additionalContext` text) is
    unchanged; report the matched path in `file_path` so the payload still names what triggered it.
    ⛔ Do not strip ANSI codes here (IN-01, deferred) and do not add a `--self-test` pre-run
    (WR-01, deferred).

    In `.claude/settings.json`, change **only** this guard's matcher from `Write|Edit` to
    `Write|Edit|MultiEdit`. ⛔ Additive/surgical: the two neighbouring `Write|Edit` entries
    (`hot-file-ledger-guard`, `landing-drift-guard`) are NOT touched, and the dead top-level
    `"PostToolUse": []` at `:201` is left alone (IN-02, deferred — its re-open trigger technically
    fires here and it is deliberately not taken; record that in the SUMMARY). Re-validate the file
    with `node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8'))"` and paste
    the `git diff` into the SUMMARY.

    Drive the hook from a scratchpad script that: records the real supplement's md5; runs the clean
    case; plants the `resize_embedding_column` REVOKE deletion; runs the four payload cases; restores;
    and re-asserts the md5. Mirror `253-02-SUMMARY.md`'s table format so the two drives are comparable.
  </action>
  <acceptance_criteria>
    - A before/after table in the SUMMARY with **stdout byte counts**, in the 253-02 format: the two
      MultiEdit payloads read `0 bytes` against `HEAD~` (RED, observed) and `>0 bytes` with
      `acl_parity_gate_exit: 1` after — including the payload whose ONLY path source is `edits[0].file_path`.
    - The negative control is driven **with the defect still planted** and reads `0 bytes`.
    - `node -e` prints the two matcher strings side by side and they are equal; the JSON re-parses.
    - The supplement's md5 after the drive is byte-identical to the value recorded before it, printed
      in the SUMMARY.
    - `node scripts/check-schema-acl-parity.cjs` exits 0 on the restored tree.
  </acceptance_criteria>
  <what-built>
    The hook now extracts paths from a MultiEdit payload, and the PostToolUse registration matches
    `Write|Edit|MultiEdit`. Both halves were driven against a planted defect with an md5-verified restore.
  </what-built>
  <how-to-verify>
    1. Read the `git diff` of `.claude/settings.json` in the SUMMARY — exactly one matcher string changed,
       on the `schema-acl-parity-guard.js` entry. Nothing else in that file moved.
    2. Confirm you accept an edit to your operator config file.
    3. Optional: restart the session and edit `scripts/full-schema-supplement.sql` with MultiEdit; the
       guard should stay silent on a clean tree (it only speaks when the gate fails).
  </how-to-verify>
  <resume-signal>Type "approved" or name what you want changed in `.claude/settings.json`.</resume-signal>
  <verify>
    <automated>node -e "const s=JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8'));const e=s.hooks.PostToolUse.filter(h=>JSON.stringify(h).includes('schema-acl-parity-guard'));if(e.length!==1||e[0].matcher!=='Write|Edit|MultiEdit')process.exit(1);console.log('matcher OK: '+e[0].matcher)"</automated>
  </verify>
  <done>A MultiEdit to a migration, the supplement or the artifact runs the parity gate; a MultiEdit to anything else does not; both proven with the defect planted, and the operator approved the config edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: WR-08 + WR-06 — the fences run in CI for the change that breaks them</name>
  <files>.github/workflows/backend-tests.yml, backend/tests/unit/test_253_ci_path_coverage.py</files>
  <read_first>
    - `.github/workflows/backend-tests.yml` — the `push.paths` and `pull_request.paths` lists, and the
      `pytest tests -q` step. ⚠ Note the pre-existing asymmetry: the workflow's own file is in the
      push arm and not the PR arm.
    - `.github/workflows/schema-acl-parity.yml` — the wiring precedent this phase already shipped
      (paths, `--self-test` first, verdict second, and its header explaining the CI-is-the-backstop split).
    - `backend/tests/unit/test_253_supplement_column_parity.py:50-60` — `_REPO_ROOT = parents[3]`,
      `_SUPPLEMENT`; the module-level `Path` constants this fence derives its set from.
    - `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-REVIEW.md` §WR-08 and
      §WR-06 (its two-part fix: move the no-DB half into the `.cjs` gate — that is Task 1 — and give
      the harness a real trigger).
  </read_first>
  <behavior>
    - RED: the new fence, run against the **unmodified** `backend-tests.yml`, FAILS naming
      `scripts/full-schema-supplement.sql` as read by a backend unit test and matched by no path
      filter in either trigger arm. After the fix it passes.
    - The fence DERIVES its subject set: it imports the two `test_253_*` modules, collects their
      module-level `pathlib.Path` constants that resolve under the repo root and **outside**
      `backend/`, and asserts each is matched by at least one glob in `on.push.paths` AND in
      `on.pull_request.paths`.
    - NON-VACUITY: the derived set must contain **≥ 2** entries and must include
      `scripts/full-schema-supplement.sql`; an empty derived set is a FAILURE, never a pass. This is
      the Phase 242 defect (two guards exiting 0 over zero parsed files) and the fence must not repeat it.
    - FALSE-POSITIVE CONTROL: a synthetic path known to be uncovered (e.g. `docs/OPERATOR.md`) is
      asserted **not** matched by the same matcher, so a matcher that returns True for everything reds.
    - ⚠ YAML TRAP, named so it is not rediscovered: PyYAML parses the bare key `on` as the **boolean
      True** (YAML 1.1). Read the trigger mapping as `doc.get("on") or doc.get(True)` and assert it
      was found, or the fence silently tests nothing.
  </behavior>
  <action>
    Write `backend/tests/unit/test_253_ci_path_coverage.py` FIRST and observe it RED. Parse
    `.github/workflows/backend-tests.yml` with `yaml.safe_load` (PyYAML is already a backend
    dependency — `backend/requirements.txt:63`). Implement a small glob matcher for the GitHub
    `paths:` syntax used here (`prefix/**`, exact path) — keep it narrow and say in the docstring that
    it covers the forms this repo uses, rather than claiming general `fnmatch` compatibility it does
    not have.

    Then add to **both** the `push.paths` and `pull_request.paths` lists of `backend-tests.yml`:
    `scripts/full-schema-supplement.sql` (read by `test_253_supplement_column_parity.py`) and
    `scripts/check-greenfield-privileges.py` (loaded by `test_253_greenfield_sql_lexer.py` from
    Task 2). Give each a trailing comment naming the test that reads it, the way
    `schema-acl-parity.yml` names its own reasons. ⛔ Do not "fix" the pre-existing missing
    `.github/workflows/backend-tests.yml` entry in the PR arm — it is out of the locked scope; note it
    in the SUMMARY instead.

    **WR-06, stated as a decision rather than left implicit.** This does not put the greenfield
    harness's DATABASE half into CI, and that is deliberate: it hard-codes `127.0.0.1:54322`
    (`ALLOWED_PORTS`), needs the `vector` extension, and mutates a cluster. What this round wires is:
    (1) the harness's **no-database** enforcement of D-11 now lives in `check-schema-acl-parity.cjs`,
    which the PostToolUse hook and the `schema-acl-parity` CI job both already run (Task 1); (2) the
    harness's **parsing** half is now executed by CI through `test_253_greenfield_sql_lexer.py`, and
    the path filter above is what makes a change to the harness trigger that run. Write the residue
    into the SUMMARY plainly: **the end-to-end DB run remains operator-invoked via
    `docs/DEPLOYMENT-WORKFLOW.md`**, and its re-open trigger is a CI runner that can host a
    pgvector-capable Postgres on port 54322, or the next promotion where `get_advisors(security)`
    disagrees with a local run.
  </action>
  <acceptance_criteria>
    - Run the new fence against `HEAD~` (or with the workflow's `paths:` reverted): it FAILS and its
      message names `scripts/full-schema-supplement.sql`. Paste that failure into the SUMMARY.
    - After the change: `cd backend && venv/Scripts/python -m pytest tests/unit/test_253_ci_path_coverage.py -q`
      passes, and its output/assertion messages show the derived set (≥ 2 paths, both named).
    - The non-vacuity arm is proven by temporarily emptying the derived set and observing the fence
      go RED — a derived-set fence that passes over nothing is the exact defect it guards against.
    - `yaml.safe_load` finds the trigger mapping under either `"on"` or `True`, asserted explicitly.
    - `git diff .github/workflows/backend-tests.yml` shows exactly two added entries in each of the
      two `paths:` lists and nothing else.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_253_ci_path_coverage.py -q</automated>
  </verify>
  <done>Both `scripts/` files that backend unit tests read are inside both trigger arms of `backend-tests.yml`, a derived fence holds them there, and WR-06's remaining DB-half residue is written down with a re-open trigger rather than implied closed.</done>
</task>

<task type="auto">
  <name>Task 5: the registers tell the truth about this round, and the gates are re-run on the merged tree</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - `docs/HOT-FILE-LEDGER.md` — the scan-list rows at `:10647-10649` and the three detail sections
      `## scripts/full-schema-supplement.sql` (:14385), `## scripts/check-schema-acl-parity.cjs`
      (:14443), `## scripts/check-greenfield-privileges.py` (:14508). ⚠ `:14415` currently reads
      *"There is no gate behind that rule"* about D-11 — **Task 1 makes that false**, and a row that
      is present and WRONG stops an audit, which is worse than an absent row.
    - `CLAUDE.md` — the G-5-FIRING abridged table; only `scripts/full-schema-supplement.sql` has a row
      there, and this plan does not modify that file. The 200-char disposition cap and the same-commit
      sync rule bind.
    - `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-CONTEXT.md` D-23 — the
      ledger work is MANUAL and is forbidden from citing `check-hot-file-ledger.cjs` as proof.
  </read_first>
  <action>
    Re-derive the triple for every file this plan touched, with the CLAUDE.md recipe and nothing else —
    `git log --oneline -- <file> | wc -l`, the phase-bucket `sed`/`grep` pipeline (⚠ six-digit dated
    quick-task buckets are NOT phases — subtract them), and `wc -l <file>`. Update the scan-list rows
    for `scripts/check-schema-acl-parity.cjs` and `scripts/check-greenfield-privileges.py`, and their
    detail sections, in the SAME commit. Correct the `:14415` claim that D-11 has no gate behind it,
    naming `assertTailIdentity` and the two runners that now carry it.

    Add a row **only** where one is owed: `.claude/hooks/schema-acl-parity-guard.js`,
    `.claude/settings.json`, `.github/workflows/backend-tests.yml` and the two new test files are all
    outside `WATCHED`, so no gate demands rows for them — decide explicitly and record the decision
    either way rather than leaving silence. Touch `CLAUDE.md` **only** if a touched file's abridged row
    exists or newly FIRES at ≥ 3 phases; if it is touched, run `node scripts/check-claude-md-size.cjs`
    and quote the char count.

    Then run the full gate set on the merged tree and publish the numbers verbatim, never summarised:
    `node scripts/check-schema-acl-parity.cjs --self-test` · `node scripts/check-schema-acl-parity.cjs` ·
    `node scripts/check-gap-closure-rounds.cjs 253` (it must now count **round 1** from this plan's
    `gap_closure_round` frontmatter and stay under the cap) ·
    `node scripts/check-seeds-register.cjs --phase 253` ·
    `cd backend && venv/Scripts/python -m pytest tests/unit -q --continue-on-collection-errors`.

    ⛔ For the backend run, capture the failing node-id **SET** with `grep -c` / a written-out list —
    not `| tail`. Diff it against 253-02's published set. A count that matches with a different set is
    a regression wearing a green hat.

    ⚠ `vitest-count-gate` is NOT run: the frontend diff of this plan is empty. Say so.
  </action>
  <acceptance_criteria>
    - Every touched file's ledger triple equals a freshly-derived one, with both the command and its
      output pasted into the SUMMARY. ⛔ No triple is copied forward from a previous row.
    - `docs/HOT-FILE-LEDGER.md:14415`'s "no gate behind that rule" sentence is corrected, and the
      correction names `assertTailIdentity` plus the hook and CI job that run it.
    - `node scripts/check-gap-closure-rounds.cjs 253` prints `1` gap-closure plan and `rounds
      completed: 1`, exits 0, and reports **no** `[new-capability-in-closure]` — verbatim.
    - `node scripts/check-schema-acl-parity.cjs --self-test` exits 0 at ≥ 34 arms; the scan run exits 0.
    - Backend: `71 failed` (or fewer) and `0 collection errors`, with the failing SET byte-identical to
      `253-02-SUMMARY.md`'s. If the set differs at all, the delta is named node-id by node-id.
    - If `CLAUDE.md` was edited, `node scripts/check-claude-md-size.cjs` exits 0 and the char count is quoted.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-gap-closure-rounds.cjs 253 &amp;&amp; node scripts/check-schema-acl-parity.cjs --self-test &amp;&amp; node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <done>Both ledger registers agree with a freshly-derived measurement, the D-11 "no gate" claim is corrected in the same commit, and every gate figure in the SUMMARY is a verbatim verdict line rather than a summary of one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `supabase/full-schema.sql` → a brand-new production database | The artifact an operator pastes into a greenfield Supabase project. Whatever it omits is a **default** — and Postgres defaults are permissive. |
| `supabase/migrations/` → `scripts/full-schema-supplement.sql` | A hand-mirror that has failed three times with the instruction in plain sight. |
| PostToolUse payload (`tool_input`) → gate execution | An untrusted-shaped JSON decides whether the guard runs at all. |
| A pull request → CI path filters | The filters decide which fences a change is measured by. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-03-01 | Elevation of Privilege | `supabase/full-schema.sql` losing an ACL while the supplement keeps it | mitigate | Task 1 — `assertTailIdentity` compares the artifact's byte tail to the supplement inside the gate the hook and CI both run. Driven RED by stripping `resize_embedding_column` from a copy (today: exit 0). |
| T-253-03-02 | Tampering (parser) | `check-greenfield-privileges.py::_strip_sql_comments` dropping a REVOKE after a `--`-bearing literal | mitigate | Task 2 — literal/dollar/block-comment-aware `_statements`, fenced by `test_253_greenfield_sql_lexer.py`. ⛔ The failure direction is **permissive**: the model believes the un-revoked state and a greenfield DB missing the same REVOKE then MATCHES. |
| T-253-03-03 | Repudiation | A MultiEdit to a migration or the supplement leaving no guard record | mitigate | Task 3 — matcher `Write|Edit|MultiEdit` plus `edits[].file_path` extraction, driven with the defect still planted so the negative proves the filter. |
| T-253-03-04 | Elevation of Privilege | The §5 column fence and the harness fences never running in CI for the change that breaks them | mitigate | Task 4 — both `scripts/` paths added to both trigger arms, held by a derived fence with a non-vacuity floor. |
| T-253-03-05 | Information Disclosure | Reverse drift — a supplement MORE permissive than the migrations, shipped to every bootstrap | **accept** | D-13, an explicit prior decision; WR-03 deferred with its own re-open trigger below. ⛔ Accepted **knowing the permissive direction is the dangerous one**, and knowing the only detector (`check-greenfield-privileges.py`) still needs a local Postgres. |
| T-253-03-06 | Elevation of Privilege | `ROLES` drops the `public` grantee, so a table-level `REVOKE … FROM PUBLIC` models nothing | **accept (latent)** | WR-05 deferred; measured: no migration writes that shape today. ⛔ **This is the project's own thrice-fired PUBLIC trap**, so the trigger is mechanical, not a judgement call — see `<deferred>`. |
| T-253-03-SC | Tampering | npm / pip / cargo installs | **n/a** | This plan installs **no** package. PyYAML, pytest, asyncpg and Node built-ins are all already present; the Package Legitimacy Gate has no subject here. |
</threat_model>

<deferred>
**The 13 findings the operator's lock excluded.** None is built. Each carries a concrete re-open
trigger, because a deferral with no trigger is a deletion that looks like a decision.

| ID | Finding | Re-open trigger |
|---|---|---|
| WR-01 | The non-vacuity floor guards file COUNT, not parsed statements — both gates can still pass over a collapsed PARSE. The hook does not run `--self-test`. | The **next edit to any extractor** — `aclsIn`, `FUNC_ACL_RE`, `TABLE_ACL_RE`, `statements()`, `_ACL_RE`, `_statements` — or any run whose tuple total drops below the 133 measured 2026-09-17. ⭐ Highest-value of the deferred set: it is the defect class Phase 242 already measured twice. |
| WR-03 | Reverse drift — a supplement that GRANTS what the migrations REVOKE — is detected by nothing automated. | D-13's own trigger, unchanged: **any phase that edits the supplement's grants by hand**. Escalate as a phase, not a round: it needs a `--strict` mode and a CI split. |
| WR-04 | `assert_derived_set` pairs measured rows to expectations by array position over an `unnest()` with no `ORDER BY`. | The first harness violation naming a tuple the operator cannot find in the migrations; or any Postgres major-version upgrade / parallel-plan change on the local cluster. |
| WR-05 | The Python model drops the `PUBLIC` grantee entirely (`ROLES` has three members; `_parse_statement` lowercases `PUBLIC` to `public` and it is filtered out). | **Mechanical:** the first migration containing a table-level `REVOKE … FROM PUBLIC` or `GRANT … TO PUBLIC` — `grep -rn "FROM PUBLIC\|TO PUBLIC" supabase/migrations/ \| grep -v "ON FUNCTION"` is non-empty. Re-check at every migration review. |
| WR-07 | `assert_greenfield_target` guards an irreversible `DROP DATABASE … WITH (FORCE)` against the operator's live cluster with no regression fence, while its own docstring cites the fenced analog as precedent. | Any edit to `create_scratch_database`, `teardown_scratch_database`, `generate_db_name`, `GREENFIELD_DB_PATTERN`, `ALLOWED_PORTS` or `MAINTENANCE_DSN` — or the first caller passing a name it did not generate. The fix is a port of `test_241_bench_safety.py` and is ~30 lines with no database. |
| WR-09 | The two parsers disagree on `ON ALL TABLES IN SCHEMA`: the `.cjs` skips it (written down), the Python invents `public.public` and emits a spurious violation → a false RED over a correct artifact. | The first migration containing `ON ALL TABLES IN SCHEMA` (grep-able), or the first `[missing-table] public.public` line in a harness run. |
| WR-10 | The default-privilege preamble is derived from the operator's live `pg_default_acl` while `PrivilegeModel._seed` hardcodes `TABLE_PRIVILEGES` — the two are never reconciled, so the verdict is machine-dependent. | Any attempt to run the harness on a second machine or in CI (see WR-06's residue), or the first `ALTER DEFAULT PRIVILEGES` anywhere in `supabase/migrations/`. |
| WR-11 | Three independent SQL-ACL extractors over one corpus, already divergent in three measured ways. | ⚠ **This round is itself evidence for it** — CR-02 is a port of a fix already made in the `.cjs`, applied a second time by hand. Trigger: the **next** SQL-shape fix that must be applied in more than one parser. The named seam is `--emit-json` from the `.cjs` consumed by the Python side, already proposed in `docs/HOT-FILE-LEDGER.md`. |
| IN-01 | The hook injects raw ANSI escapes into `additionalContext`. | The next edit of the hook's output path, or the first transcript where the escapes obstruct a grep. |
| IN-02 | `.claude/settings.json` carries a dead top-level `"PostToolUse": []` outside `"hooks"`. | ⚠ **Its trigger fires in Task 3 and is deliberately not taken** — the operator's lock is five findings. Re-trigger: the next `.claude/settings.json` edit that is not scope-locked. |
| IN-03 | `.github/workflows/schema-acl-parity.yml` declares no `permissions:` block. | The next edit of that workflow, or a repo-wide decision to declare least-privilege tokens. |
| IN-04 | `check-greenfield-privileges.py:16`'s md5 constant is already stale (`6a58a476…` vs the live `da9c5616…`). | ⚠ **Task 2 edits this file and leaves the docstring alone by lock.** Re-trigger: the next non-scope-locked edit to that file's header, or the next time the pair's md5 changes (which is every supplement edit). |
| IN-05 | Stale cross-file line reference at `check-greenfield-privileges.py:137` (`…cjs:63` → the symbol is at `:108`). | Same as IN-04. |

⛔ **None of these is "too hard" and none is being judged as such.** They are excluded by an operator
scope decision taken on 2026-09-17, recorded here so the exclusion is auditable and re-openable.
</deferred>

<seeds_sweep>
**REG-02 ran at plan time, and its routing is written down rather than left silent.**
`node scripts/check-seeds-register.cjs --phase 253` → `296/296 parsed · 0 duplicate ids · gate OK`,
**3 seeds fired** — the same three the 253 plan-time sweep fired.

| Seed | Match | Routing for THIS round |
|---|---|---|
| `SEED-188` (open) — anti-prompt-injection discipline verified by nobody | path-only, `backend/tests/**` vs the three `test_253_*` files | **NOT folded — false positive**, same ruling as `253-CONTEXT.md`. This round adds no untrusted-content channel and no injection surface; the match is the glob, not the subject. |
| `SEED-266` (partially-answered) — `full-schema.sql` leaks `SET row_security = off` | `**/full-schema.sql` (from 253-01/02's `files_modified`) | **NOT folded.** Arm (a) was answered by `253-02`; arms (b)/(c) are outside the operator's five-finding lock. ⛔ Status left byte-unchanged. ⚠ Task 1 makes the gate READ `full-schema.sql` for the first time — if a later round widens that read beyond the tail, arm (b) is its natural home. |
| `SEED-284` (planted) — three file-local elapsed formatters, one home owed | path-only, `docs/HOT-FILE-LEDGER.md` | **NOT folded — false positive**, same ruling as `253-CONTEXT.md`. Task 5 edits ledger ROWS; it touches no formatter. |

⚠ The sweep reported *"the phase declares NO surfaces"*, so it was **path-only** — recorded, never
passed off as a clean sweep.
</seeds_sweep>

<verification>
Run on the merged tree, in this order, reading **verdict lines verbatim** rather than summarising them:

1. `node scripts/check-schema-acl-parity.cjs --self-test` → exit 0, **≥ 34** arms (29 before this round).
2. `node scripts/check-schema-acl-parity.cjs` → exit 0, `mirrored: 133/133` or higher, **plus the new
   tail line: line count + md5 matching `scripts/full-schema-supplement.sql`**.
3. The CR-01 reproduction against a scratchpad copy of `full-schema.sql`: exit **1** after, exit **0**
   against `HEAD~`. `supabase/full-schema.sql` md5 unchanged; `git status --short` clean for it.
4. `cd backend && venv/Scripts/python -m pytest tests/unit/test_253_greenfield_sql_lexer.py tests/unit/test_253_ci_path_coverage.py tests/unit/test_253_supplement_column_parity.py -q` → all pass.
5. `backend/venv/Scripts/python scripts/check-greenfield-privileges.py` → exit 0 on a real scratch DB,
   **or exit 2 recorded as an owed SKIP with its reason**. Never exit 2 written as a pass.
6. The hook drive table: two MultiEdit payloads loud with the defect planted, two non-subject payloads
   silent **with the defect still planted**, md5-verified restore.
7. `cd backend && venv/Scripts/python -m pytest tests/unit -q --continue-on-collection-errors` →
   `71 failed` or fewer, `0 collection errors`, failing **SET** diffed against 253-02's.
8. `node scripts/check-gap-closure-rounds.cjs 253` → exit 0, `rounds completed: 1`, no
   `[new-capability-in-closure]`.
9. `node scripts/check-claude-md-size.cjs` → exit 0 (run whether or not CLAUDE.md moved).
10. `node scripts/check-seeds-register.cjs --phase 253` → exit 0; route anything it fires in writing.

⛔ **NOT run, by decision:** `vitest-count-gate` — the frontend diff is empty.
⛔ **NOT driveable from here:** the `schema-acl-parity` and `backend-tests` GitHub jobs. Say so
plainly; the local hook is the driven half, and the CI half is confirmed on the next push touching a
matching path (this is the same residue `253-02-SUMMARY.md` recorded and it is not silently inherited).
</verification>

<success_criteria>
- **CR-01:** stripping an ACL from a copy of `supabase/full-schema.sql` makes the gate exit non-zero;
  it exits 0 against `HEAD~`. The tracked artifact is byte-unchanged throughout.
- **CR-02:** a REVOKE following `COMMENT … IS '… -- …';` is parsed by the harness; a dollar-quoted
  ACL-shaped phantom is not; the real-corpus statement count is unchanged or every delta is named by
  file and line; `supplement_section5_columns()` is byte-identical.
- **WR-02:** both MultiEdit payload shapes drive the guard loud with a defect planted; a non-subject
  MultiEdit stays silent with the same defect still planted; the matcher equals its sibling's.
- **WR-06:** the D-11 tail rule is enforced by something the hook and CI already run, the harness's
  parsing half executes in CI, and the DB half's residue is written down with a re-open trigger.
- **WR-08:** both `scripts/` files backend unit tests read sit in both trigger arms of
  `backend-tests.yml`, held there by a derived, non-vacuous fence.
- **Every one of the five** has an executable proof in the repository that was SEEN to fail before the
  fix. ⛔ A fix whose RED was never observed red does not count as closed in this round.
- Backend ceiling intact (71 / 0 collection errors) with an identical failing SET; ROADMAP SC#3 and
  SC#4 re-closed against the phase goal; `CRED-03` and `CRED-04` re-closed.
</success_criteria>

<output>
Create `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-03-SUMMARY.md` when done.

⛔ **The SUMMARY must carry the RED evidence, not a claim that it happened** — the self-test `FAIL`
lines, the pytest failure output, the hook byte-count table, the two exit codes of the CR-01
reproduction, and the failing-SET diff. ⛔ Name every deviation rather than quietly satisfying a
neighbouring property; `253-01` did that correctly and it is the house standard.
⛔ `independent_review: owed` — **DEBT-06 / BUS-257** stands for this round too. This would be the
sixth consecutive self-verified close; say so in writing rather than letting it pass unremarked.
</output>
