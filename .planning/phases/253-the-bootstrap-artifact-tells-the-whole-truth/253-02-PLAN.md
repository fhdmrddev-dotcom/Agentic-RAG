---
phase: 253-the-bootstrap-artifact-tells-the-whole-truth
plan: 02
type: tdd
wave: 2
depends_on: [253-01]
autonomous: true
requirements: [CRED-04]
gap_closure: false

files_modified:
  - scripts/check-schema-acl-parity.cjs
  - scripts/full-schema-supplement.sql
  - supabase/full-schema.sql
  - .github/workflows/schema-acl-parity.yml
  - .claude/hooks/schema-acl-parity-guard.js
  - .claude/settings.json
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/seeds/SEED-266-full-schema-sql-carries-no-acls-and-leaks-row-security-off.md

user_setup: []

must_haves:
  truths:
    - "SC#3 — deleting the single line REVOKE EXECUTE ON FUNCTION public.resize_embedding_column(integer) FROM PUBLIC from a copy of the supplement makes the gate exit NON-ZERO. It prints missing 0 and exits 0 today (D-12)."
    - "SC#4 — a COMMENT ON column whose literal contains a double-dash, followed by a REVOKE EXECUTE ON FUNCTION public.danger(integer) FROM PUBLIC, still yields that signature. Migration 180 lines 30 and 32 already ship that construct (D-15)."
    - "The gate sees TABLE and COLUMN privileges, compared as (table, grantee, verb, column-set) tuples beside the function tuples, making the supplement's existing 'table OR function' header TRUE rather than aspirational (D-07)."
    - "Ordering assertion and reverse-drift detection are OUT of scope BY DECISION, and the gate's prose says so instead of claiming coverage it lacks (D-13, D-14)."
    - "--self-test carries four arms — partial-revoke omission, comment-swallow, table/column omission, and the counterfactual — each driven RED against its planted defect BEFORE the fix and GREEN after (D-18, D-19)."
    - "The gate is INVOKED by something other than a human typing it: a CI workflow and a PostToolUse hook, the hook driven against a planted defect (MC-4)."
    - "SC#5 — three absent hot-file ledger rows exist with their detail sections in the same commit, and the twelve triples CR-08 measured stale are re-derived (D-23)."
    - "SEED-266 records arm (a) as answered by Phase 253 in its status_note, with status still partially-answered and arms (b) and (c) left open."
  artifacts:
    - path: "scripts/check-schema-acl-parity.cjs"
      provides: "the tuple-keyed, literal-aware, table-and-function ACL parity gate"
      exports: ["normaliseSignature", "statements", "aclsIn", "analyse", "report", "MIN_MIGRATION_FILES"]
    - path: ".github/workflows/schema-acl-parity.yml"
      provides: "the CI backstop that makes the gate able to fail a build"
    - path: ".claude/hooks/schema-acl-parity-guard.js"
      provides: "the in-turn PostToolUse guard — the primary half, per CLAUDE.md's own two-guards rule"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "three new rows, three detail sections, twelve re-derived triples"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/schema-acl-parity-guard.js"
      via: "PostToolUse Write|Edit matcher entry"
      pattern: "schema-acl-parity-guard"
    - from: ".claude/hooks/schema-acl-parity-guard.js"
      to: "scripts/check-schema-acl-parity.cjs"
      via: "execFileSync(process.execPath, [gate])"
      pattern: "check-schema-acl-parity"
    - from: "CLAUDE.md"
      to: "docs/HOT-FILE-LEDGER.md"
      via: "same-commit sync rule — a row here, a section there"
      pattern: "full-schema-supplement"
---

<objective>
Make `scripts/check-schema-acl-parity.cjs` structurally able to FAIL on the mutations it exists to
catch, and make something other than a human actually RUN it.

Purpose: CR-02 was **driven, not reasoned** — one `REVOKE ... FROM PUBLIC` line was deleted from a
copy of the supplement, the shipped `analyse()` was run, and it printed `missing: 0` and exit 0. The
function it passed over is `resize_embedding_column`, the RPC `BUG-260911-01` found callable
unauthenticated in production, which NULLs every vector in `document_chunks` and `skill_embeddings`.
So the one gate written to stop that class re-shipping passes over precisely the edit that re-ships
it. CR-03 is the same failure one layer down: a double-dash inside a string literal destroys
statement boundaries, and migration 180 already ships that construct. This closes `CRED-04`.

⛔ **And the finding that decides whether any of it is a guard: the gate is invoked by NOTHING.**
See MC-4 below.

Output: a tuple-keyed, literal-aware gate that sees TABLE and COLUMN privileges; four RED-driven
`--self-test` arms; a CI job and a PostToolUse hook that run it; three absent ledger rows with their
sections; and `SEED-266`'s arm (a) recorded as answered.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-CONTEXT.md
@.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-PATTERNS.md
@.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-01-SUMMARY.md
@CLAUDE.md
</context>

<scope_fences>

⛔ **D-13 is a NEGATIVE decision and it is recorded here so a future reader sees it was DECIDED, not
forgotten.** TWO things are explicitly OUT of scope:

1. **Ordering assertion.** The gate does NOT check that `REVOKE ... FROM PUBLIC` precedes
   `REVOKE ... FROM anon`. Deferred, with a concrete re-open trigger: *the next migration that
   revokes a role privilege without revoking PUBLIC first* — the migration 181 Group B class, which
   252's SUMMARY records as having been inert on greenfield.
2. **Reverse-drift detection.** The gate does NOT detect the supplement GRANTING something the
   migrations REVOKE, i.e. the bootstrap being MORE permissive than the migration history. Re-open
   trigger: *any phase that edits the supplement's grants by hand.*

⛔ **D-14 is DERIVED from D-13 and is an OBLIGATION, not a note.** Because ordering is not checked,
the gate's failure text must stop lecturing the reader about it. **Either check it or stop saying
it** — a guard whose prose claims more than its code is the exact defect CR-01 is. Task 3 (part D of
Task 2) removes or re-words the claim in all THREE places it appears.

⛔ **Scope source is FIXED** to `252-REVIEW.md` CR-01, CR-02, CR-03, CR-08. The nine behavioural
findings (CR-04..CR-07, CR-09..CR-11) are `/gsd:fast` sized under G-3 and are NOT this phase.

⛔ **Widening `check-hot-file-ledger.cjs`'s WATCHED / EXEMPT lists is OUT of scope** — that is a
change to a shared guardrail affecting every phase. Task 4 plants it as a named finding instead.

</scope_fences>

<measured_corrections>

**MC-4 — NOTHING INVOKES `scripts/check-schema-acl-parity.cjs`, and this is the single most
consequential finding of the phase's pattern pass.** Verified at plan time:
`grep -rln "check-schema-acl-parity" .claude .github package.json scripts docs supabase` returns
**only the gate itself plus two files that mention it in a comment** (`full-schema-supplement.sql`
and `supabase/full-schema.sql`). There is no hook entry, no CI workflow, no npm script. The five CI
workflows present are `backend-tests`, `claude-md-size`, `deploy-artifacts`, `frontend-tests`,
`landing-drift`; none names it.

**This falsifies two decisions as written.** D-07 says *"one gate, one home, ONE HOOK ENTRY"* and
D-18 says `--self-test` *"ALREADY RUNS WHEREVER THE GATE RUNS"*. Neither is true today — the gate
runs only when a human types it.

⭐ **It decides whether D-18's four new RED arms are a guard or a file nobody runs**, so this plan
WIRES the gate (Task 3) and the wiring itself is an acceptance criterion, **driven against a planted
defect** rather than satisfied by the presence of a YAML line. This is squarely inside CR-02's scope
— *"the gate that claims to prove it can actually fail"*, and a gate nobody runs cannot fail — so it
is **not** scope creep, and that judgement is recorded here in writing rather than left implicit.

**MC-5 — the ledger gate is structurally blind to every file D-23 names** (carried forward from
`253-01-PLAN.md`). `scripts/check-hot-file-ledger.cjs:67` sets
`WATCHED = [/^backend\/app\//, /^frontend\/src\//]`, and `:51`'s `EXEMPT` additionally drops
`/^scripts\//` and the `.md` / `.sql` extensions. So `node scripts/check-hot-file-ledger.cjs 253`
exits **0** whether the three rows exist or not. ⭐ That is the STRUCTURAL CAUSE of CR-08's finding
that `full-schema-supplement.sql` had no ledger row for its entire life at 5 phases — the guardrail
was blind by construction, not the author careless, which is the same class of finding as `App.tsx`
at 23 phases, one directory over. **D-23 is therefore a MANUAL task carrying its own evidence, and
it must NOT claim the ledger gate as proof.** Task 4 says so and plants the widening as a
seed-shaped finding rather than doing it.

**MC-6 — the CONTEXT's ACL statement count does not reproduce.** Re-derived at plan time: **32**
GRANT/REVOKE statement starts excluding `EXECUTE ON FUNCTION`, across **12** migration files —
`118 (4)`, `126 (1)`, `127 (1)`, `128 (1)`, `129 (3)`, `150 (1)`, `151 (2)`, `156 (6)`, `168 (3)`,
`169 (3)`, `172 (3)`, `177 (4)`. D-16 says *"25 statements"* and `253-PATTERNS.md` says *"11 files"*
and then lists twelve. The table-half regex in Task 2 must therefore find **at least 32** statements
against the real migrations directory, and the gate must PRINT the number it measured. ⛔ A
hardcoded expected total anywhere outside a comment is a defect — `check-schema-acl-parity.cjs:139-141`
already says so about `migrationCount`.

</measured_corrections>

<interfaces>

The seams Tasks 1-3 land inside, extracted so the executor does not go hunting.

`scripts/check-schema-acl-parity.cjs` exports today, at line 363:
`module.exports = { normaliseSignature, aclsIn, analyse, report, MIN_MIGRATION_FILES }`. The
`require.main === module` guard at `:365` is what makes those importable, and
`check-seeds-register.cjs:43-48` records that this export seam was a deliberate departure from every
other `check-*.cjs`. **Keep it, and ADD `statements`** so the RED drives can call the lexer directly.

The three functions Task 2 rewrites:

- `statements(sql)` at `:112-121` — splits lines, truncates each at `line.indexOf('--')`, rejoins,
  splits on `;`. **This is CR-03.**
- `ACL_RE` at `:123` — matches only `(REVOKE|GRANT) EXECUTE ON FUNCTION <sig> (FROM|TO) <grantees>`.
  **It sees no table or column privilege at all. This is CR-01's half of the gate.**
- `aclsIn(sql, file)` at `:126-134` — pushes `{ signature: normaliseSignature(m[2]), file }` and
  **discards `m[1]` (the verb) and `m[3]` (the grantee) entirely. This is CR-02.**

`analyse()` at `:177-190` keys `expected` on `signature` ALONE and computes
`missing = [...expected.keys()].filter(s => !mirroredSet.has(s))`. `report(result, log)` at
`:193-223` takes an injectable `log`, which is how `--self-test` reads its output — **that seam is
already right; do not disturb it.**

⛔ `normaliseSignature` at `:84-103` must stay **byte-unchanged**, along with its three `--self-test`
normaliser arms at `:296-301`. Those three arms are the only proof that an argument list is part of
a function's identity, and a normaliser that collapsed `f(uuid, text)` and `f(uuid)` would make the
gate pass over a real gap.

`VacuousScanError` at `:66` plus `MIN_MIGRATION_FILES = 120` at `:63` plus `scanMigrations` at
`:142-163` — the collapsed-scan-set refusal. **The table half inherits this for free; do not
duplicate it.**

`assertOutsideWatchedTree(resolved)` at `:230-236` — refuses to build a fixture inside `frontend/`,
`backend/` or the repo root. Every new `--self-test` fixture goes through it.

The hook registration shape in `.claude/settings.json`, copied from the `hot-file-ledger-guard.js`
entry: one element of the `PostToolUse` array, with `matcher` set to `Write|Edit` and a single
`hooks` entry of `type: command` whose command is the node executable path followed by
`"$CLAUDE_PROJECT_DIR"/.claude/hooks/<name>.js`, plus a `timeout`.

</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — drive all three proven defects against the SHIPPED gate and capture the green</name>

  <files>scripts/check-schema-acl-parity.cjs</files>

  <behavior>
    - RED-1 (CR-02 / D-12): deleting the single line REVOKE EXECUTE ON FUNCTION
      public.resize_embedding_column(integer) FROM PUBLIC from a COPY of the supplement and running
      the SHIPPED analyse() prints missing 0 and returns exit code 0. That green IS the defect.
    - RED-2 (CR-03 / D-15): aclsIn over a COMMENT ON COLUMN whose literal contains a double-dash,
      followed by REVOKE EXECUTE ON FUNCTION public.danger(integer) FROM PUBLIC, returns an empty
      array. The same SQL with the double-dash removed from inside the literal returns one entry
      whose signature is public.danger(integer).
    - RED-3 (CR-01 / D-07): the shipped aclsIn over the twelve real ACL-bearing migrations returns
      ZERO table or column entries, while the directory contains 32 such statements (MC-6).
  </behavior>

  <read_first>
    - `scripts/check-schema-acl-parity.cjs` in full — it is 373 lines and every seam matters.
    - `.planning/phases/252-close-the-v42-audit-gaps/252-REVIEW.md:137-183` (CR-02, including the
      driven transcript) and `:186-216` (CR-03, including the two-line driven counterexample).
    - `supabase/migrations/180_app_settings_self_hosted_endpoints.sql:28-34` — the live
      COMMENT-with-a-double-dash-inside-a-literal pair.
    - `scripts/full-schema-supplement.sql:400-433` — the `resize_embedding_column` block, whose
      `FROM PUBLIC` line is RED-1's subject.
    - `backend/tests/unit/test_211_closed_set_agreement.py:208-243` — the
      falsify-the-reader-before-trusting-it control this task is the gate-side equivalent of.
    - This plan's `<interfaces>` and MC-6.
  </read_first>

  <action>
⛔ **This is its own task because D-19 says every arm is driven RED against its planted defect BEFORE
the fix, and because a guard nobody has seen fire is not a guard.** The point is to reproduce
`252-REVIEW.md`'s three findings on THIS machine at THIS commit, so that Task 2's GREEN means
something. ⛔ Change no source file.

**RED-1 — the CR-02 partial-revoke.** Copy `scripts/full-schema-supplement.sql` into the session
scratchpad. ⛔ NOT into the repo — `assertOutsideWatchedTree` exists because a fixture inside
`frontend/`, `backend/` or the repo root is refused. Delete exactly the one line
`REVOKE EXECUTE ON FUNCTION public.resize_embedding_column(integer) FROM PUBLIC;` from the copy,
leaving the `FROM anon` and `FROM authenticated` revokes and the `TO service_role` grant in place.
Then `require` the gate and call `analyse` with `migrationsDir` set to the repo's
`supabase/migrations` and `supplementPath` set to the copy, pass the result to `report` with a
collector function as the `log` argument, and record the returned exit code and the `missing` count.
**It must be exit 0 with `missing: 0` — that is the shipped defect, reproduced.** Record the
verbatim output. ⛔ If it exits 1 here, STOP and investigate: either the supplement changed under you
(plan `253-01` edited it) or the reproduction is wrong, and Task 2 would then be fixing something
unmeasured.

**RED-2 — the CR-03 comment-swallow.** Call the exported `aclsIn` directly on the two strings named
in `<behavior>`: the swallowed case must return an empty array and the control must return one entry
with signature `public.danger(integer)`. Record both. Then show it is LIVE rather than hypothetical:
run the shipped `statements` over
`supabase/migrations/180_app_settings_self_hosted_endpoints.sql` and record that the chunk boundary
after line 30 is wrong — truncating at the double-dash leaves an unterminated single-quoted literal
whose closing quote and semicolon land on a later line, so every subsequent split is misaligned.

**RED-3 — the CR-01 blindness.** Run the shipped `aclsIn` over each of the twelve ACL-bearing
migrations (118, 126, 127, 128, 129, 150, 151, 156, 168, 169, 172, 177) and record that it returns
ZERO table or column entries from any of them, while
`grep -rn -E "^[[:space:]]*(GRANT|REVOKE)[[:space:]]+" supabase/migrations/ | grep -viE "EXECUTE ON FUNCTION" | wc -l`
returns 32 (MC-6 — re-derive it, do not type it). That difference is what makes the supplement's
section 6 header claim of *"table OR function"* aspirational rather than true.

⛔ Delete the scratchpad copies afterwards and confirm `md5sum scripts/full-schema-supplement.sql`
is unchanged from the value plan `253-01` recorded. `git checkout -- <dir>` is NOT a restore on this
box — `core.autocrlf` moved 224 body digests while `git status` read clean.
  </action>

  <verify>
    <automated>node scripts/check-schema-acl-parity.cjs --self-test; echo "selftest exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - The RED-1 transcript shows `missing: 0` and exit code `0` from the SHIPPED `analyse` against a
      supplement copy with the `resize_embedding_column ... FROM PUBLIC` line deleted; the exact
      deleted line is quoted in the SUMMARY.
    - The RED-2 transcript shows `aclsIn` returning an empty array for the swallowed case and one
      entry with signature `public.danger(integer)` for the control.
    - The RED-3 transcript shows `0` table/column entries from the shipped `aclsIn` over the twelve
      migrations, beside a re-derived count of `32` GRANT/REVOKE statements, and names the twelve
      files it read.
    - `git status --short` shows NO modified file at the end of this task.
    - `md5sum scripts/full-schema-supplement.sql` equals the value recorded at the end of plan
      `253-01`.
    - No fixture was written inside `frontend/`, `backend/` or the repo root.
  </acceptance_criteria>

  <done>
All three of `252-REVIEW.md`'s structural findings are reproduced on this machine at this commit,
with verbatim transcripts, and no source file was touched.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — literal-aware lexer (D-15), tuple key (D-12), TABLE/COLUMN regex (D-07), narrowed prose (D-14)</name>

  <files>scripts/check-schema-acl-parity.cjs, scripts/full-schema-supplement.sql</files>

  <behavior>
    - After the rewrite, RED-1 exits 1 and the failure output NAMES
      public.resize_embedding_column(integer) together with the grantee public.
    - After the rewrite, RED-2's swallowed case returns one entry with signature
      public.danger(integer).
    - After the rewrite, analyse over the real migrations directory reports at least 32 table/column
      ACL statements across 12 files, re-derived at run time.
    - The three existing normaliser self-test arms still PASS byte-unchanged, and the GREEN arm still
      passes against the real tree as plan 253-01 left it.
    - A dollar-quoted body containing a double-dash does not lose its statement boundary, and a
      doubled single-quote escape inside a literal does not end the literal.
    - A file with CRLF line endings parses identically to the same file with LF.
  </behavior>

  <read_first>
    - `scripts/check-schema-acl-parity.cjs:105-134` (the stripper, `ACL_RE`, `aclsIn`), `:165-223`
      (`scanSupplement`, `analyse`, `report`), `:296-301` (the normaliser arms).
    - `.planning/phases/252-close-the-v42-audit-gaps/252-REVIEW.md:169-183` — the reviewer's own
      suggested `aclsIn` shape, returning verb, signature and grantees and keying on
      verb-pipe-signature-pipe-grantee. ⚠ A suggested fix is a CLAIM to verify, not an instruction
      to paste.
    - `scripts/check-seeds-register.cjs:1-56` — the house docstring style and the ⚠ CRLF hazard note
      at `:50-54`: `core.autocrlf=true` with no `.gitattributes` means a normalising rewrite changes
      every working-tree line while producing no `git diff`.
    - `supabase/migrations/129_connector_oauth_tokens.sql:83-106` and
      `156_grant_default_ingest_visibility_column.sql:28-40` — real multi-line `GRANT SELECT ( ... )`
      blocks the TABLE regex must match across newlines.
    - `scripts/full-schema-supplement.sql` as plan `253-01` left it, especially `:284-286` (the
      third home of the ordering claim) and the new sections 5b / 5c / 5d.
    - This plan's `<interfaces>`, `<scope_fences>` and MC-6.
  </read_first>

  <action>
**A. Replace `statements()` with a single-pass, string-literal-aware walk (D-15).** Walk the SQL once
tracking three states: inside a single-quoted literal (with the doubled-quote escape), inside a
double-quoted identifier, and inside a dollar-quoted body whose tag may be empty. A double-dash
begins a comment ONLY when none of those states is active; a semicolon terminates a statement ONLY
when none of those states is active. ⛔ **The cheaper *drop the `^\s*` anchor and scan globally* fix
was REJECTED as the sole remedy and must not be substituted:** it leaves a stripper that is still
wrong about SQL, so the next thing built on statement boundaries inherits the bug — and D-12's tuple
rewrite depends on semicolon-splitting being meaningful. Keep the existing *COMMENTS MUST GO FIRST*
reasoning in the docstring (migration 181's tail carries an entirely-commented VERIFY block quoting
function names; counting those would inflate the expected set and make the gate fail against a
CORRECT supplement — a false red is how a guard gets switched off), and add the CR-03 reason beside
it. ⛔ Treat a carriage return as ordinary whitespace and NEVER rewrite input — this gate only reads.
Export `statements` so the RED drives and the self-test can call it directly.

**B. Key on tuples, one entry per grantee (D-12).** Change `aclsIn` to return verb, signature,
grantees and file, where verb is the upper-cased GRANT or REVOKE, signature is
`normaliseSignature(m[2])` (⛔ **`normaliseSignature` stays byte-unchanged** — it already strips the
trailing FROM/TO clause, so it is unaffected), and grantees is the third capture split on commas,
trimmed, lower-cased, with a trailing semicolon and any CASCADE or RESTRICT word removed. A
`REVOKE ... FROM anon, authenticated` therefore becomes TWO entries. Key both `expected` and the
mirrored set on verb-pipe-signature-pipe-grantee. ⚠ Decide the `^\s*` anchor EXPLICITLY rather than
leaving it implicit: with a correct lexer each chunk is a whole statement, so the anchor is now safe
— but pin that decision in a `--self-test` arm (a chunk with a leading newline or leading comment
must still match) rather than in prose, because the anchor plus a correct lexer is a real
interaction.

**C. Add the TABLE/COLUMN half (D-07).** A second regex for GRANT or REVOKE of a privilege list,
optionally with a parenthesised column list, ON an optionally-TABLE-qualified schema.table, FROM or
TO a grantee list. It MUST match across newlines because every `GRANT SELECT ( ... )` block in the
migrations is multi-line. Normalise each table ACL to a `(table, grantee, verb, column-set)` tuple:
lower-case and `public.`-qualify the table name exactly as `normaliseSignature` does for functions;
split the privilege list (SELECT, INSERT, UPDATE, DELETE, or ALL) into ONE tuple per privilege so a
partial revoke is visible; sort the column-set so hand-ordering differences do not produce a false
red; and use a distinguished EMPTY column-set for a table-level statement. ⛔ A table-level
`REVOKE ALL` and a column-level `REVOKE ALL (c)` are DIFFERENT facts and must not collapse into one
key. Compare the table tuples BESIDE the function tuples in the same expected/mirrored maps,
prefixed so a table key can never collide with a function key. ⭐ This is what makes the supplement's
section 6 header claim of *"table OR function"* TRUE rather than aspirational, and it is why a
separate sibling gate was rejected: a gate missing from a scan list is invisible to its own
guardrail. The `VacuousScanError` plus `MIN_MIGRATION_FILES` floor is inherited for free — do not
duplicate it.

**D. Narrow the failure text (D-14).** Remove or re-word the two lines at `:220-221` that lecture the
reader that *REVOKE ... FROM PUBLIC must precede REVOKE ... FROM anon*. Replace them with an explicit
statement of what this gate does and does not check, naming ordering and reverse-drift as OUT of
scope BY DECISION (D-13) together with their re-open triggers from `<scope_fences>`. ⚠ **The same
claim appears in two more places and the same-commit honesty applies:**
`check-schema-acl-parity.cjs:26-28` (*"Ordering (PUBLIC before anon) is asserted by the plan's own
checks"*) and `scripts/full-schema-supplement.sql:284-286`. Correct all THREE in this task. ⛔ Either
check it or stop saying it — a guard whose prose claims more than its code is the exact defect CR-01
is.

**E. Update the header docstring and the verdict line.** The verdict line at `:195-200` says
*"function ACLs found"*; it must now report function and table/column counts SEPARATELY, both
re-derived at run time. Update the `WHY THIS EXISTS` block to name CR-01, CR-02 and CR-03 and what
each one changed. ⛔ No hardcoded expected total outside a comment — the gate's own rule at
`:139-141`.

**F. Re-drive RED-1, RED-2 and RED-3 and prove they are now GREEN**, using the identical commands and
fixtures Task 1 used. RED-1 must exit 1 naming `public.resize_embedding_column(integer)`; RED-2 must
return `public.danger(integer)`; RED-3 must report at least 32 table/column statements across 12
files. Then run the gate against the real tree unmodified: it must exit **0**, because plan `253-01`
mirrored all seven tables. ⚠ If it exits 1, READ the missing list before changing anything — a
genuine gap found by a newly-sighted gate is a FINDING to carry back to `253-01`'s mirror, never a
reason to loosen the regex.
  </action>

  <verify>
    <automated>node scripts/check-schema-acl-parity.cjs --self-test; echo "selftest exit=$?"; node scripts/check-schema-acl-parity.cjs; echo "scan exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/check-schema-acl-parity.cjs` exits NON-ZERO when the single line
      `REVOKE EXECUTE ON FUNCTION public.resize_embedding_column(integer) FROM PUBLIC;` is deleted
      from a scratchpad COPY of the supplement and that copy is passed as `supplementPath`
      (SC#3 / D-12). The failure output names the signature and the grantee `public`.
    - Calling the exported `aclsIn` on the CR-03 swallowed-case string returns exactly one entry
      whose signature is `public.danger(integer)` (SC#4 / D-15).
    - A dollar-quoted body containing a double-dash and a doubled-single-quote escape each preserve
      their statement boundary; both are asserted inside `--self-test`.
    - `analyse` over the real migrations directory and the real supplement reports at least **32**
      table/column ACL statements across **12** files, re-derived at run time (MC-6).
    - `grep -nE "(=|\s)32\b" scripts/check-schema-acl-parity.cjs` returns only comment lines — no
      hardcoded expected total in executable code.
    - `node scripts/check-schema-acl-parity.cjs` exits **0** against the tree as plan `253-01` left
      it.
    - `git diff scripts/check-schema-acl-parity.cjs` shows `normaliseSignature` (lines 84-103)
      byte-unchanged, and its three normaliser `--self-test` arms still PASS.
    - `grep -c "must precede" scripts/check-schema-acl-parity.cjs` returns `0`;
      `grep -c "must precede" scripts/full-schema-supplement.sql` returns `0`; and
      `grep -c "asserted by the plan" scripts/check-schema-acl-parity.cjs` returns `0` (D-14, all
      three homes).
    - The gate still has zero runtime dependencies — the only `require` calls name `fs`, `path`,
      `os`.
    - `tail -n $(wc -l < scripts/full-schema-supplement.sql) supabase/full-schema.sql | md5sum`
      still equals `md5sum scripts/full-schema-supplement.sql` after the part-D prose edit, and
      `supabase/full-schema.sql`'s tail is updated in the SAME commit (D-11 still binds).
  </acceptance_criteria>

  <done>
The gate compares verb-signature-grantee tuples for functions and (table, grantee, verb, column-set)
tuples for tables, splits statements with a literal-aware lexer, and its failure text claims exactly
what its code checks in all three homes. All three Task 1 RED drives are now GREEN, with both
transcripts recorded side by side.
  </done>
</task>

<task type="auto">
  <name>Task 3: The four --self-test arms (D-18) and WIRING the gate so it can actually fail (MC-4)</name>

  <files>scripts/check-schema-acl-parity.cjs, .github/workflows/schema-acl-parity.yml, .claude/hooks/schema-acl-parity-guard.js, .claude/settings.json</files>

  <read_first>
    - `scripts/check-schema-acl-parity.cjs:225-351` — the existing `--self-test`:
      `assertOutsideWatchedTree`, `FIXTURE_MIGRATION`, `FIXTURE_SUPPLEMENT_COMPLETE`,
      `FIXTURE_SUPPLEMENT_MISSING_ONE`, the pad-to-the-REAL-floor loop, the `check()` reporter, and
      the existing `RED arm 2 (COUNTERFACTUAL)`.
    - `.claude/hooks/hot-file-ledger-guard.js` in full — the PostToolUse analog: the stdin JSON
      parse, the *never break the turn on a parse failure* exit 0, the path filter, the
      `execFileSync(process.execPath, [gate, ...])` call, and the *Deliberately NOT a PreToolUse
      blocker* reasoning.
    - `.claude/hooks/claude-md-size-guard.js` — the second PostToolUse precedent.
    - `.claude/settings.json` — the `PostToolUse` array, specifically the `hot-file-ledger-guard.js`
      element (matcher `Write|Edit`, timeout 20), which is the registration shape to copy.
    - `.github/workflows/claude-md-size.yml` in full — including its header comment explaining why
      CI is the BACKSTOP and the local hook is PRIMARY (`develop` ran 30 commits without a push
      while CLAUDE.md grew from 43k to 195k chars).
    - `.planning/seeds/SEED-287-targets-names-files-not-directories-so-a-new-suite-runs-nowhere.md`
      — why D-18 refuses a new vitest suite.
    - This plan's MC-4.
  </read_first>

  <action>
**A. Extend `--self-test` to four arms (D-18).** Today it plants only a *whole-function* omission
(`FIXTURE_SUPPLEMENT_MISSING_ONE` filters every `public.alpha` line) and therefore never exercises
the partial case. Keep every existing arm and ADD:

1. **Partial-revoke omission (CR-02's proven defect).** A fixture supplement identical to
   `FIXTURE_SUPPLEMENT_COMPLETE` except that the single line
   `REVOKE EXECUTE ON FUNCTION public.alpha() FROM PUBLIC;` is deleted while the `FROM anon` revoke
   and the `TO service_role` grant REMAIN. `analyse` must exit 1 and the failure must name the
   REVOKE-public.alpha()-public tuple. ⛔ Name the real-world instance in the arm's own label so a
   future reader knows what it stands for: `resize_embedding_column ... FROM PUBLIC`.
2. **Comment-swallow (CR-03).** A fixture migration whose ACL follows a `COMMENT ON COLUMN` whose
   literal contains a double-dash, plus a dollar-quoted variant and a doubled-single-quote variant.
   All three must still yield their signatures. Keep the control: the entirely-commented
   `public.never_real()` line must still NOT be counted.
3. **Table/column omission (CR-01).** A fixture migration carrying
   `REVOKE ALL ON TABLE public.widgets FROM anon, authenticated;` and a multi-line
   `GRANT SELECT ( a, b ) ON public.widgets TO authenticated;`, against a supplement that mirrors the
   GRANT but not the REVOKE. `analyse` must exit 1 and NAME the missing table tuple. Add a
   column-set arm too: a supplement granting `(a)` where the migration granted `(a, b)` must ALSO
   fail.
4. **The counterfactual.** A correct supplement stays GREEN, and the failure output of the RED arms
   does NOT print the mirrored tuples — the gate does not print everything it knows. Extend the
   existing `RED arm 2` shape to cover the new table tuples.

Keep both existing idioms: **pad to the REAL `MIN_MIGRATION_FILES`** so the fixture exercises the
shipped constant rather than a stand-in, and route every fixture path through
`assertOutsideWatchedTree`. ⛔ **Rejected and must not be substituted: a new vitest suite that shells
out.** `SEED-287` measured that `vitest-count-gate.cjs`'s `TARGETS` names FILES, not directories, so
a suite under `scripts/` would run NOWHERE unless both count-gate knobs are edited — which is how a
green gate ends up guarding nothing.

**B. Wire the gate — the CI backstop.** Create `.github/workflows/schema-acl-parity.yml` modelled
line for line on `.github/workflows/claude-md-size.yml`: `on: push` to the branches
`master`, `main`, `develop`, `production` and `release/**`, plus `on: pull_request`, both filtered to
the paths `supabase/migrations/**`, `scripts/full-schema-supplement.sql`, `supabase/full-schema.sql`,
`scripts/check-schema-acl-parity.cjs` and the workflow file itself; `runs-on: ubuntu-latest`; a
single `actions/checkout@v4` step; then TWO run steps —
`node scripts/check-schema-acl-parity.cjs --self-test` and
`node scripts/check-schema-acl-parity.cjs`. No secrets, no `setup-node` (node is preinstalled on
`ubuntu-latest`), matching the sibling exactly. Write the same kind of header comment the sibling
carries, naming MC-4 in plain words: this gate was invoked by NOTHING for its entire life; CR-02
proved it could not fail, and a gate nobody runs cannot fail either way.

**C. Wire the gate — the PRIMARY local hook.** CLAUDE.md's own two-guards rule says the local hook
is primary and CI is the backstop, because `develop` ran 634 commits over 8 days without a push.
Create `.claude/hooks/schema-acl-parity-guard.js` as a sibling of `hot-file-ledger-guard.js`: read
the PostToolUse JSON from stdin, exit 0 on any parse failure (never break the turn), normalise
`tool_input.file_path` to forward slashes, and return immediately unless the path matches
`supabase/migrations/<digits>_*.sql`, `scripts/full-schema-supplement.sql` or
`supabase/full-schema.sql`. Otherwise run `execFileSync(process.execPath, [<repo>/scripts/check-schema-acl-parity.cjs])`
with `cwd` set to `CLAUDE_PROJECT_DIR` or `process.cwd()` and a timeout, and print the gate's output
LOUDLY when it exits non-zero. ⛔ **Deliberately NOT a PreToolUse blocker**, matching both
precedents: a migration that narrows a privilege is a correct edit that simply owes a mirror, and a
hook must never deny that write. It reports in-turn, while the author still holds the reason. Carry
the sibling's *Not prefixed `gsd-` on purpose — it is not GSD-managed and must survive `/gsd:update`*
note, and add the CLAUDE.md vendored-framework caveat by name.

**D. Register the hook.** Add ONE element to the `PostToolUse` array in `.claude/settings.json`,
copying the `hot-file-ledger-guard.js` element's exact shape: matcher `Write|Edit`, one command
entry naming `"$CLAUDE_PROJECT_DIR"/.claude/hooks/schema-acl-parity-guard.js`, timeout 20. ⚠ **This
is a configuration file — the edit must be ADDITIVE ONLY.** Paste the `git diff` into the SUMMARY and
confirm that no existing entry, permission, matcher or timeout changed and that the file still
parses as JSON.

**E. DRIVE THE WIRING; do not assert that it exists.** ⛔ A YAML line and a settings entry are not
evidence — MC-4 exists precisely because a gate's PRESENCE was mistaken for the gate RUNNING. Drive
both halves:

- **The hook (the driven half).** Pipe a synthetic PostToolUse payload naming
  `supabase/migrations/181_revoke_public_secdef_functions.sql` into the hook on stdin and confirm it
  invokes the gate and reports. Then plant the CR-02 defect IN THE REAL SUPPLEMENT — delete the
  `resize_embedding_column ... FROM PUBLIC` line — re-run the hook with the same payload, and confirm
  it reports the failure LOUDLY with a non-zero gate exit. ⛔ **Then restore the supplement and verify
  by md5 that it is byte-identical to its pre-drive state**; `git checkout -- <dir>` is NOT a restore
  on this box. Also drive the NEGATIVE: a payload naming an unrelated file such as
  `frontend/src/App.tsx` exits 0 WITHOUT invoking the gate — a hook that fires on everything gets
  switched off.
- **The workflow (the undrivable half, stated plainly).** Confirm the YAML parses and that the exact
  command strings it runs reproduce locally: `--self-test` exits 0, and the scan exits 0 on a clean
  tree and 1 against the planted defect. ⛔ Record in the SUMMARY that CI itself cannot be driven from
  here, and that the local hook is therefore the driven half — say it plainly rather than implying
  the workflow was tested.
  </action>

  <verify>
    <automated>node scripts/check-schema-acl-parity.cjs --self-test; echo "selftest exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/check-schema-acl-parity.cjs --self-test` exits `0` and its output names at least
      FOUR new arms: the partial-revoke omission, the comment-swallow, the table/column omission,
      and the counterfactual — plus a column-set arm and the three preserved normaliser arms.
    - Commenting out the tuple-key change in a scratchpad COPY of the gate makes the partial-revoke
      arm FAIL; the copy is deleted and the real gate is md5-unchanged. (The self-test arms are
      themselves falsified, not merely present.)
    - `grep -rn "check-schema-acl-parity" .github .claude | wc -l` returns `>= 2` — at plan time it
      returned `0` for both directories (MC-4).
    - Piping a PostToolUse payload naming `supabase/migrations/181_revoke_public_secdef_functions.sql`
      into `.claude/hooks/schema-acl-parity-guard.js` invokes the gate; the transcript is in the
      SUMMARY.
    - With the `resize_embedding_column ... FROM PUBLIC` line deleted from the REAL supplement, the
      same hook invocation reports the failure loudly; afterwards
      `md5sum scripts/full-schema-supplement.sql` matches its pre-drive value exactly.
    - A payload naming `frontend/src/App.tsx` makes the hook exit `0` without invoking the gate.
    - `node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8'))"` succeeds,
      and `git diff .claude/settings.json` is purely additive — one new `PostToolUse` element and
      nothing else.
    - `.github/workflows/schema-acl-parity.yml` contains `actions/checkout@v4`, the string
      `check-schema-acl-parity.cjs --self-test`, and path filters naming `supabase/migrations/**`
      and `scripts/full-schema-supplement.sql`.
    - The SUMMARY states in writing that CI was NOT driven from here and that the hook is the driven
      half.
  </acceptance_criteria>

  <done>
`--self-test` carries four new arms covering CR-01, CR-02, CR-03 and the counterfactual, each shown
to fail against its planted defect. The gate is invoked by a CI workflow and by a registered
PostToolUse hook, and the hook has been driven against a real planted defect with the artifact
restored md5-identical.
  </done>
</task>

<task type="auto">
  <name>Task 4: SC#5 / CR-08 — three absent ledger rows, twelve stale triples, and SEED-266's status_note (D-23)</name>

  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md, .planning/seeds/SEED-266-full-schema-sql-carries-no-acls-and-leaks-row-security-off.md</files>

  <read_first>
    - `.planning/phases/252-close-the-v42-audit-gaps/252-REVIEW.md:339-377` — CR-08 in full,
      including the six-row measured table and the six further files named in its parenthetical.
    - `docs/HOT-FILE-LEDGER.md:10364` — the scan-table header
      (`| File | commits / phases / lines | G-5 | Disposition |`) and the rows immediately below it;
      and `:11876-11898` — the `frontend/src/lib/workspaceAllowedExt.ts` detail section, which is the
      row-at-creation precedent to copy.
    - `scripts/check-claude-md-size.cjs:55-70` (`LEDGER_CELL_LIMIT = 200`) and `:230-240`
      (`LEDGER_FILES`, and the three failure codes `[disposition-too-long]`, `[duplicate-row]`,
      `[malformed-row] ... expected 6 cells`).
    - `scripts/check-hot-file-ledger.cjs:45-70` — `EXEMPT` and `WATCHED`, which is MC-5's evidence.
    - `CLAUDE.md` — the *hot-file ledger* section, the FIRING-shortlist table's THREE-column header
      (`| Hot file (FIRING) | commits / phases / lines | Verdict (abridged) |`), and the triple
      re-derivation recipe.
    - `.planning/seeds/SEED-266-full-schema-sql-carries-no-acls-and-leaks-row-security-off.md` —
      its frontmatter and the existing 2026-09-16 `status_note` block, which is the format the 253
      entry copies.
    - `.planning/seeds/TEMPLATE.md:1-20` and `:50-62` — the `status` enum and the TRIPLE rule.
  </read_first>

  <action>
⛔ **MC-5 binds this whole task: `node scripts/check-hot-file-ledger.cjs 253` exits 0 whether these
rows exist or not**, because `WATCHED` is `backend/app` plus `frontend/src` only and `EXEMPT` drops
`^scripts/` and the `.md` / `.sql` extensions. **So this task is MANUAL and must carry its own
evidence — it must NOT cite the ledger gate as proof of anything.** Paste the re-derived triples.

**A. Re-derive every triple with the CLAUDE.md recipe.** For each file below run
`git log --oneline -- <file> | wc -l` for commits, the `git log --format=%s` / `sed` / `grep -E
'^[0-9]+(\.[0-9]+)?$'` pipeline for phases, and `wc -l <file>` for lines. ⚠ Six-digit buckets such as
`260814` or `260912` are DATED QUICK TASKS, not phases — subtract them, and non-numeric buckets are
likewise not phases. ⛔ Do not copy a cell forward; a row that is present and WRONG answers the
auditor with *satisfied* and stops the audit, which is worse than an absent row.

**B. Add three rows to `docs/HOT-FILE-LEDGER.md`'s scan table, with their detail sections in the
SAME COMMIT (D-23).**

1. `scripts/full-schema-supplement.sql` — G-5 **FIRING** (it measured 8 commits / 5 phases with NO
   row for its entire life, and this phase is its next touch). Re-derive the triple. The disposition
   cell carries the VERDICT only and is capped at **200 characters**; everything else goes in the
   detail section.
2. `scripts/check-schema-acl-parity.cjs` — row **AT CREATION**, the `settingsSearchPayload.ts`
   precedent: an absent row makes G-5 absent forever, silently, at any count.
3. `scripts/check-greenfield-privileges.py` — row **AT CREATION**, created by plan `253-01`.

Each detail section states: what the file is the ONE home of; the invariants that bind it (for the
supplement: every statement is idempotent, section 5's column list is hand-maintained one per line
so the ABSENT column is visible in a diff, and its tail is byte-identical to `supabase/full-schema.sql`
— the same-commit rule with no gate behind it; for the gate: `normaliseSignature` must never drop an
argument list, the `MIN_MIGRATION_FILES` floor, and the D-13 scope fence; for the harness: the
anchored target-name grammar, the `pg_default_acl`-derived preamble, and that a SKIP is exit 2 and
never 0); the named seam the next refactor should take; and ⭐ **MC-5 stated plainly — this file's
row could never have been demanded by the ledger gate, because `scripts/` is exempt by construction.
That is the same class of finding as `App.tsx` at 23 phases, one directory over.**

**C. Add the FIRING row to CLAUDE.md's shortlist.** Only `scripts/full-schema-supplement.sql` is
FIRING, so only it joins CLAUDE.md's THREE-column FIRING table; the other two live in
`docs/HOT-FILE-LEDGER.md` only. ⛔ Abridge the verdict — the 200-char cap is machine-enforced and
this column reaching 60,558 chars is what forced the 2026-08-25 split. Then run
`node scripts/check-claude-md-size.cjs` and read its verdict: it must report no
`[disposition-too-long]`, no `[duplicate-row]`, no `[malformed-row]`, and CLAUDE.md must stay under
the 150,000-character hard limit (warn band 120,000).

**D. Re-derive the twelve stale triples CR-08 named.** `frontend/src/components/panel/PhaseCard.tsx`,
`frontend/src/components/panel/PhaseTimeline.tsx`,
`frontend/src/components/panel/phaseStatusMeta.ts`,
`frontend/src/components/sources/sourceHealthVocabulary.ts`, `backend/app/api/connectors.py`,
`frontend/src/stores/streamsStore.ts`, `frontend/src/providers/StreamsProvider.tsx`,
`frontend/src/components/panel/TodosSection.tsx`,
`frontend/src/components/sources/WatchRowCard.tsx`,
`frontend/src/components/sources/WatchedFoldersSection.tsx`,
`frontend/src/components/chat/ThinkingBlock.tsx`, `backend/app/services/connector_service.py`.
Update both the `docs/HOT-FILE-LEDGER.md` row and the CLAUDE.md shortlist row where one exists, and
write the measured figure BESIDE the stale one rather than over it, per this project's
corrected-claim rule. ⛔ **Record `sourceHealthVocabulary.ts`'s G-5 disposition EXPLICITLY** — its
row reads *"row added at its THIRD phase, so G-5 FIRES on the next touch"*, Phase 252-03 WAS that
next touch, and no refactor proposal or honoured-by-construction disposition was recorded anywhere.
A reader of that row today would believe G-5 has not fired yet and would be wrong by two phases.

**E. Plant MC-5 as a named finding, do not act on it.** CR-08's own *"separately worth considering"*
sentence proposes teaching `check-hot-file-ledger.cjs` to fail on a row whose line count differs from
`wc -l` — presence-only is the same class of guard as CR-02. Combine that with MC-5's `WATCHED` /
`EXEMPT` blindness and record BOTH in the SUMMARY as a seed-shaped finding with a concrete re-open
trigger (*the next phase whose `files_modified` names a `scripts/` file*). ⛔ Do not widen the lists
and do not write the line-count check — that is a change to a shared guardrail affecting every phase
and is outside this phase's fixed scope.

**F. Edit `SEED-266`'s `status_note` only.** Append a new dated block inside the existing
`status_note: |` literal, in the same `── 2026-09-XX · ...` format as its 2026-09-16 entry, naming
arm **(a)** — TABLE and COLUMN privileges — as ANSWERED by Phase 253 (SC#1 and SC#2), citing
`253-01-SUMMARY.md` and the greenfield harness, and re-stating that arms **(b)**
(`SET row_security = off`, byte-unchanged) and **(c)** (`--no-privileges` itself, whose reason is
recorded in `252-01-PLAN.md`) stay OPEN with their existing triggers. ⛔ `status:` stays
`partially-answered` and `partial:` stays `true` — obey `.planning/seeds/TEMPLATE.md`'s enum, and do
not touch any other key. ⚠ A seed that shipped but still reads `planted` is re-proposed forever, and
`status:` frontmatter IS the index — prose in the body is invisible to the scan. Verify with
`node scripts/check-seeds-register.cjs --files .planning/seeds/SEED-266-*.md` (per-file mode, no
count floor).

**G. Close out the process decisions.** Record in the SUMMARY that **D-20**'s two-plan serial
structure held (waves 1 and 2, `depends_on: [253-01]`, required by CLAUDE.md worktree rule 4 because
plan `253-01`'s harness creates and drops a database on the shared local cluster that no
`files_modified` check can see); that **D-22**'s `security_enforcement` and `code_review` were
treated as non-optional and both plans carry a `<threat_model>`; and that **D-21**'s independent
review was queued to gemini on the bus AT PLAN TIME rather than at close. ⚠ If the bus item does not
exist, say so plainly — this phase would otherwise close `self-verified` for the FIFTH consecutive
time (249, 250, 251, 252), and `DEBT-06`'s structural half is unmet regardless:
`grep -rln "independent_review" scripts/ .claude/hooks/` returns nothing, so the field its close
condition is written against has no gate behind it.
  </action>

  <verify>
    <automated>node scripts/check-claude-md-size.cjs; echo "size exit=$?"; node scripts/check-seeds-register.cjs --files .planning/seeds/SEED-266-full-schema-sql-carries-no-acls-and-leaks-row-security-off.md; echo "seed exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "full-schema-supplement.sql" docs/HOT-FILE-LEDGER.md` returns `>= 2` (a scan row and a
      detail-section heading); the same for `check-schema-acl-parity.cjs` and
      `check-greenfield-privileges.py`.
    - `grep -c "full-schema-supplement.sql" CLAUDE.md` returns `>= 1` in the FIRING shortlist table.
    - `node scripts/check-claude-md-size.cjs` exits `0` — no `[disposition-too-long]`, no
      `[duplicate-row]`, no `[malformed-row]`; CLAUDE.md is under 150,000 characters and the
      measured figure is recorded in the SUMMARY.
    - Every one of the three new disposition cells is `<= 200` characters, and every new scan row has
      exactly 6 pipe-delimited cells.
    - All twelve CR-08 triples are re-derived with the CLAUDE.md recipe, with the stale figure
      preserved BESIDE the measured one, and the commands used are pasted into the SUMMARY.
    - `sourceHealthVocabulary.ts`'s G-5 disposition is recorded explicitly, naming Phase 252-03 as
      the touch that fired it.
    - `node scripts/check-seeds-register.cjs --files .planning/seeds/SEED-266-*.md` exits `0`, and
      `grep -c "^status: partially-answered" .planning/seeds/SEED-266-*.md` returns `1` and
      `grep -c "^partial: true"` returns `1` — the seed edit is `status_note` only.
    - `git diff .planning/seeds/SEED-266-*.md` touches ONLY the `status_note` block; no other
      frontmatter key and no body line changed.
    - `node scripts/check-seeds-register.cjs --phase 253` runs (the PLAN.md files now exist) and its
      output is recorded — REG-02's sweep, which could not run at discuss time.
    - The SUMMARY states plainly that `node scripts/check-hot-file-ledger.cjs 253` exits `0`
      REGARDLESS of these rows (MC-5), and does NOT cite it as evidence.
  </acceptance_criteria>

  <done>
Three absent ledger rows exist with their detail sections in the same commit and within the 200-char
cap; twelve stale triples are re-derived with the stale figures preserved beside them;
`sourceHealthVocabulary.ts`'s fired G-5 is dispositioned; MC-5 and CR-08's line-count proposal are
planted as a named finding rather than acted on; and `SEED-266` records arm (a) as answered with its
status and enum untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a migration author's edit → `supabase/full-schema.sql`'s privilege posture | The ONLY thing standing between a narrowed privilege and its silent absence from every greenfield deploy is this gate plus a hand-mirror. `pg_dump --no-privileges` means a dump can never carry a grant. |
| the gate's PROSE → the reader's belief about coverage | A failure text that lectures about PUBLIC-before-anon ordering reads as something it checks. This is the same trust failure CR-01 is, one register over. |
| the gate's EXISTENCE → the belief that it runs | MC-4: measured, nothing invokes it. A guard's presence was mistaken for a guard's execution for its entire life. |
| a `--self-test` fixture → the working tree | `assertOutsideWatchedTree` refuses a fixture inside `frontend/`, `backend/` or the repo root; a RED drive plants a defect in the REAL supplement and must restore it md5-identical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-09 | Tampering | `analyse()` keyed on signature alone | mitigate | D-12: key on verb-signature-grantee tuples, one entry per grantee. Driven RED in Task 1 against the exact `resize_embedding_column ... FROM PUBLIC` deletion that printed `missing: 0`, GREEN in Task 2, and pinned as `--self-test` arm 1 in Task 3. |
| T-253-10 | Tampering | `statements()` truncating at a double-dash inside a literal | mitigate | D-15: a single-pass lexer tracking single-quote (with doubled-quote escape), double-quote and dollar-quote state. Live case, not hypothetical: migration 180 lines 30 and 32 already ship it. Pinned as `--self-test` arm 2. |
| T-253-11 | Information Disclosure | table and column ACLs invisible to the gate | mitigate | D-07: a second regex compared as `(table, grantee, verb, column-set)` tuples, matched across newlines because every real `GRANT SELECT ( ... )` block is multi-line; a table-level and a column-level statement may never collapse into one key. Pinned as `--self-test` arm 3. |
| T-253-12 | Repudiation (false assurance) | the failure text and two sibling docstrings | mitigate | D-14: remove or re-word the ordering claim in all THREE homes and state what is NOT checked, with D-13's re-open triggers. Acceptance greps for `must precede` returning 0 in both files. |
| T-253-13 | Elevation of Privilege | a gate that nothing invokes | mitigate | MC-4: a CI workflow modelled on `claude-md-size.yml` AND a registered PostToolUse hook modelled on `hot-file-ledger-guard.js`. The hook is DRIVEN against a planted defect and against the negative case; the workflow is stated as undriven, plainly. |
| T-253-14 | Denial of Service (vacuity) | the table half over a collapsed scan set | accept (inherited) | `VacuousScanError` + `MIN_MIGRATION_FILES = 120` already refuse a verdict over a collapsed directory, and the table half inherits it. Not duplicated — a second floor is a second thing to rot. |
| T-253-15 | Tampering | the RED drive planting a defect in the REAL supplement (Task 3E) | mitigate | Restore explicit paths and verify by md5; `git checkout -- <dir>` is forbidden here because `core.autocrlf` moved 224 body digests while `git status` read clean. The pre-drive md5 is recorded before the plant. |
| T-253-16 | Tampering | editing `.claude/settings.json`, a configuration file | mitigate | ADDITIVE ONLY — one new `PostToolUse` element copying the `hot-file-ledger-guard.js` shape. The `git diff` is pasted into the SUMMARY and the file is re-parsed as JSON. No permission, matcher or timeout of any existing entry changes. |
| T-253-17 | Repudiation | the ledger row claiming a guard it does not have | mitigate | MC-5: the SUMMARY states that `check-hot-file-ledger.cjs 253` exits 0 regardless of these rows, so the rows are recorded as MANUAL with their own re-derived evidence rather than as gate output. |
| T-253-SC | Tampering | npm/pip installs | accept | **No package is installed by this plan.** The gate is zero-dependency by rule (`fs`, `path`, `os` only), the hook uses `child_process` and `fs`, and the CI job uses only `actions/checkout@v4` with node preinstalled on `ubuntu-latest`. No `RESEARCH.md` Package Legitimacy Audit is owed because there is nothing to audit. If an executor finds itself adding a dependency, that is a scope change and it stops instead. |
</threat_model>

<verification>
- `node scripts/check-schema-acl-parity.cjs --self-test` exits **0** and names at least four new
  arms plus the three preserved normaliser arms.
- `node scripts/check-schema-acl-parity.cjs` exits **0** against the tree as plan `253-01` left it,
  and exits **1** against a scratchpad copy missing the `resize_embedding_column ... FROM PUBLIC`
  line (SC#3).
- The exported `aclsIn` returns `public.danger(integer)` for the CR-03 swallowed case (SC#4).
- `grep -rn "check-schema-acl-parity" .github .claude` returns at least two hits; it returned zero
  at plan time (MC-4).
- `node scripts/check-claude-md-size.cjs` exits **0**.
- `node scripts/check-seeds-register.cjs --files .planning/seeds/SEED-266-*.md` exits **0**, and
  `node scripts/check-seeds-register.cjs --phase 253` runs and its output is recorded (REG-02).
- `backend/venv/Scripts/python scripts/check-greenfield-privileges.py` still exits **0** — plan
  `253-02` must not regress plan `253-01`'s artifact half.
- `cd backend && venv/Scripts/python -m pytest tests/unit -q --continue-on-collection-errors`
  reports at most **71 failed** with **0 collection errors**; the failing SET is recorded.
- `git status --short` shows exactly the nine files in `files_modified` and nothing else.

⚠ `scripts/full-schema-supplement.sql` and `supabase/full-schema.sql` appear in BOTH plans'
`files_modified`. That overlap is precisely why the waves are SERIAL and is NOT a conflict: plan
`253-01` mirrors the seven tables into them, and plan `253-02` Task 2 part D only corrects the
ordering claim at `full-schema-supplement.sql:284-286`. ⛔ D-11's same-commit tail identity binds on
that second edit too — re-assert the `tail | md5sum` pair after it.
</verification>

<success_criteria>
- ROADMAP SC#3 is TRUE: the gate fails on a PARTIAL revoke, driven against the exact planted defect
  before the fix and again after.
- ROADMAP SC#4 is TRUE: a comment cannot hide an ACL from the gate, including the dollar-quoted and
  doubled-quote variants.
- ROADMAP SC#5 is TRUE: three absent ledger rows exist with their sections, twelve stale triples are
  re-derived, and `sourceHealthVocabulary.ts`'s fired G-5 is dispositioned.
- The gate is RUN by a CI workflow and by a registered PostToolUse hook, and the hook has been seen
  to fire (MC-4 closed).
- D-13's two out-of-scope decisions are visible in the gate's own prose with re-open triggers, and
  D-14's claim is removed from all three homes.
- MC-4, MC-5 and MC-6 are each recorded in the SUMMARY beside the CONTEXT claim they correct, never
  over it.
</success_criteria>

<output>
Create `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-02-SUMMARY.md` when
done. It MUST contain: Task 1's three RED transcripts and Task 2's three GREEN transcripts side by
side; the hook-driven transcript with its pre- and post-drive md5 pair; the twelve re-derived
triples with their stale figures preserved beside them; the plain statement that CI was not driven
from here and that `check-hot-file-ledger.cjs 253` exits 0 regardless of the new rows (MC-5); the
`check-seeds-register.cjs --phase 253` sweep output (REG-02); and the D-20 / D-21 / D-22 close-out
from Task 4G.
</output>
