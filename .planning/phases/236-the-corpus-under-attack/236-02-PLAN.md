---
phase: 236-the-corpus-under-attack
plan: 02
type: execute
wave: 2
depends_on:
  - "236-01"
files_modified:
  - scripts/mutants/01_tool_dispatcher_trifecta.patch
  - scripts/mutants/02_chat_tools_envelope.patch
  - scripts/mutants/03_service_tools_param_fence.patch
  - scripts/mutants/04_embedding_metadata_prompt.patch
  - scripts/mutants/05_eval_runner_evidence_prompt.patch
  - scripts/mutants/06_phase_types_grounding_tag.patch
  - scripts/mutants/07_validator_kinds_grounded.patch
  - scripts/mutants/08_skill_proposer_evidence.patch
  - scripts/run-defense-mutations.sh
  - .planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md
autonomous: true
requirements:
  - TRUST-02
user_setup: []

must_haves:
  truths:
    - "SC#2 GA Gate is proven by actual source deletion: removing ANY ONE named defense turns the suite RED and names the removed defense"
    - "Zero runtime kill-switches or environment bypass flags in production code"
    - "scripts/mutants/ holds the 8 canonical patch files representing single-defense deletions"
    - "scripts/run-defense-mutations.sh applies each patch, runs the adversarial suite, asserts failure with named defense, restores byte-identically, and asserts git status is clean"
    - "Generates .planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md with the 8-row audit outcome"
    - "Unmutated baseline remains 100% green with zero new backend failures (<= 71 ceiling)"
  artifacts:
    - scripts/run-defense-mutations.sh
    - scripts/mutants/01_tool_dispatcher_trifecta.patch
    - scripts/mutants/02_chat_tools_envelope.patch
    - scripts/mutants/03_service_tools_param_fence.patch
    - scripts/mutants/04_embedding_metadata_prompt.patch
    - scripts/mutants/05_eval_runner_evidence_prompt.patch
    - scripts/mutants/06_phase_types_grounding_tag.patch
    - scripts/mutants/07_validator_kinds_grounded.patch
    - scripts/mutants/08_skill_proposer_evidence.patch
    - .planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md
  key_links:
    - "Resolves BLOCKING 1 by mutating source out-of-process and restoring, completely eliminating production kill-switches"
    - "Provides counterfactual proof for SC#2 that the reviewer (Claude) can independently execute and verify"
---

<objective>
Deliver the GA Gate mutation harness for Phase 236 (SC#2):
1. Author 8 mutant patch files in `scripts/mutants/` corresponding to targeted deletions of each defense:
   - `01_tool_dispatcher_trifecta.patch`: removes forced `ask` posture in `tool_dispatcher.py`.
   - `02_chat_tools_envelope.patch`: removes `<external_tool_result>` wrapper in `chat_tools.py`.
   - `03_service_tools_param_fence.patch`: removes `_ISSUE_KEY` regex validation in `service_tools.py`.
   - `04_embedding_metadata_prompt.patch`: removes data-not-instruction clause in `embedding_service.py:331`.
   - `05_eval_runner_evidence_prompt.patch`: removes DATA-not-command clause in `eval_runner_service.py:185/193-194`.
   - `06_phase_types_grounding_tag.patch`: allows injected `<doc id=...>` tags into `valid_ids` in `phase_types.py`.
   - `07_validator_kinds_grounded.patch`: bypasses `grounded_in_evidence` check in `validator_kinds.py`.
   - `08_skill_proposer_evidence.patch`: removes `EVIDENCE (DATA)` delimiters in `skill_proposer_service.py:302`.
2. Implement `scripts/run-defense-mutations.sh`:
   - Runs baseline suite on untouched tree -> confirms green.
   - For each of the 8 mutant patches:
     - Applies patch with `git apply`.
     - Runs `pytest backend/tests/unit/security/test_adversarial_corpus.py`.
     - Asserts pytest fails (non-zero) and failure output names the mutated defense.
     - Restores file via `git restore .`.
     - Confirms working tree is clean (`git diff --exit-code`).
   - Generates `.planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md` documenting the 8 mutation outcomes.
3. Fully satisfies SC#2 without touching or weakening any production code.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/236-the-corpus-under-attack/BASELINES.md
@.planning/phases/236-the-corpus-under-attack/236-CONTEXT.md
@.planning/phases/236-the-corpus-under-attack/236-THREAT-MODEL.md
@.planning/phases/236-the-corpus-under-attack/236-PREFLIGHT.md
</execution_context>

<threat_model>
<trust_boundaries>
Out-of-process Mutation Script -> Source File (temporary deletion) -> Test Runner -> Git Restore
</trust_boundaries>
<threats>
- threat_id: TM-236-07
  category: Regression / Silent Defense Deletion (SC#2)
  component: scripts/run-defense-mutations.sh
  disposition: mitigated
  mitigation_plan: Assert that deleting any of the 8 defenses produces a loud test failure naming the defense.
</threats>
</threat_model>

<tasks>
<task>
<task_id>236-02-01</task_id>
<description>Create 8 mutant patch files in scripts/mutants/</description>
<action>
Create directory `scripts/mutants/` and author the 8 patch files against HEAD:
1. `01_tool_dispatcher_trifecta.patch`: removes the `if ctx.has_connection_retrieval ... posture = 'ask'` block in `tool_dispatcher.py`.
2. `02_chat_tools_envelope.patch`: modifies `wrap_untrusted_tool_result` in `chat_tools.py` to return raw `raw_output` without `<external_tool_result>` envelope.
3. `03_service_tools_param_fence.patch`: comments out `_ISSUE_KEY` regex validation in `service_tools.py`.
4. `04_embedding_metadata_prompt.patch`: removes line 331 anti-injection prompt clause from `embedding_service.py`.
5. `05_eval_runner_evidence_prompt.patch`: removes line 193-194 anti-injection prompt clause from `eval_runner_service.py`.
6. `06_phase_types_grounding_tag.patch`: removes tag sanitization from `_emit_evidence` in `phase_types.py`.
7. `07_validator_kinds_grounded.patch`: removes citation existence check in `validator_kinds.py`.
8. `08_skill_proposer_evidence.patch`: removes `=== SKILL SELF-IMPROVEMENT EVIDENCE ===` wrapper from `skill_proposer_service.py`.

Verify each patch can be cleanly applied with `git apply --check scripts/mutants/<patch>`.
</action>
<verify>
Run `for p in scripts/mutants/*.patch; do git apply --check "$p" || exit 1; done` and verify all 8 patches check cleanly.
</verify>
</task>

<task>
<task_id>236-02-02</task_id>
<description>Implement run-defense-mutations.sh runner and 236-MUTATION-REPORT.md generator</description>
<action>
Create `scripts/run-defense-mutations.sh`:
1. Check that git working tree is clean before starting.
2. Step 0 (Baseline): Run `pytest backend/tests/unit/security/test_adversarial_corpus.py -q`. Assert 0 failures.
3. Loop through patches in `scripts/mutants/`:
   - Extract defense name and description from patch header.
   - Run `git apply $patch`.
   - Run `pytest backend/tests/unit/security/test_adversarial_corpus.py -q`.
   - Assert exit code != 0 (failed loudly).
   - Capture failed test name and ensure it matches the targeted defense.
   - Run `git restore backend/app`.
   - Verify `git diff --exit-code` is 0 (working tree restored).
4. Render summary table to stdout and write `.planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md`:
   - Columns: `Defense ID`, `Target Module`, `Mutant Patch`, `Result`, `Failure Caught`.
   - Final line: `SC#2 MUTATION AUDIT: PASS (8/8 mutations caught loudly)`.
5. Make script executable with `chmod +x scripts/run-defense-mutations.sh`.
</action>
<verify>
Run `bash scripts/run-defense-mutations.sh` and verify:
- All 8 mutations fail loudly and name the missing defense.
- Tree restores byte-identically between each run and at completion.
- `.planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md` is populated.
</verify>
</task>
</tasks>
