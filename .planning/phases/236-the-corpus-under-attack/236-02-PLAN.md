---
phase: 236-the-corpus-under-attack
plan: 02
type: execute
wave: 2
depends_on:
  - "236-01"
files_modified:
  - backend/app/services/embedding_service.py
  - backend/app/services/skill_proposer_service.py
  - backend/tests/unit/security/conftest.py
  - scripts/run-defense-mutations.sh
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md
autonomous: true
requirements:
  - TRUST-02
user_setup: []

must_haves:
  truths:
    - "SC#2 GA Gate is proven by runtime monkeypatch mutation: disabling ANY ONE named defense turns the suite RED and names the disabled defense"
    - "Zero runtime kill-switches or environment bypass flags in production code"
    - "2 inline literals in embedding_service.py and skill_proposer_service.py hoisted to module constants with G-5 ledger rows and sections updated in the same commit"
    - "conftest.py implements pytest --disable-defense=<name> with strict pre-patch target existence assertions"
    - "scripts/run-defense-mutations.sh iterates all 8 defenses and asserts loud failure on each"
    - "Generates .planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md with the 8-row audit outcome"
    - "Unmutated baseline remains 100% green with zero new backend failures (<= 71 ceiling)"
  artifacts:
    - backend/tests/unit/security/conftest.py
    - scripts/run-defense-mutations.sh
    - .planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md
  key_links:
    - "Ratified by Operator Ruling on BUS-163 (Option A pytest test-harness fixture, not an env var)"
    - "Provides counterfactual proof for SC#2 that the reviewer (Claude) can independently execute and verify without dirtying the working tree"
---

<objective>
Deliver the GA Gate mutation harness for Phase 236 (SC#2):
1. Hoist 2 inline string literals to module-level constants in production code:
   - `embedding_service.py`: `METADATA_EXTRACTION_ANTI_INJECTION`
   - `skill_proposer_service.py`: `SKILL_PROPOSER_EVIDENCE_DELIMITER`
   - Update `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md` in the same commit per same-commit sync rule.
2. Implement pytest mutation fixture in `backend/tests/unit/security/conftest.py`:
   - Adds `--disable-defense=<name>` CLI option.
   - Enforces pre-patch target symbol existence assertion (`assert hasattr(mod, symbol)`).
   - Monkeypatches the targeted module and test namespace to disable defense behavior.
3. Implement `scripts/run-defense-mutations.sh`:
   - Runs baseline suite -> confirms 11/11 green.
   - Loops over the 8 defenses with `pytest --disable-defense=<name>`.
   - Asserts each run fails loudly (non-zero exit code) and failure names the targeted defense test.
   - Generates `.planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md` documenting the 8 mutation outcomes.
4. Fully satisfies SC#2 without dirtying the working tree with patch files.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/236-the-corpus-under-attack/BASELINES.md
@.planning/phases/236-the-corpus-under-attack/236-CONTEXT.md
@.planning/phases/236-the-corpus-under-attack/236-THREAT-MODEL.md
@.planning/phases/236-the-corpus-under-attack/236-PREFLIGHT.md
</execution_context>

<threat_model>
<trust_boundaries>
Pytest Test Harness -> Monkeypatched Defense Symbol -> In-Memory Test Execution
</trust_boundaries>
<threats>
- threat_id: TM-236-07
  category: Regression / Silent Defense Deletion (SC#2)
  component: scripts/run-defense-mutations.sh
  disposition: mitigated
  mitigation_plan: Assert that disabling any of the 8 defenses produces a loud test failure naming the defense.
</threats>
</threat_model>

<tasks>
<task>
<task_id>236-02-01</task_id>
<description>Hoist inline string literals to module-level constants and sync hot-file ledger</description>
<action>
1. Hoist `METADATA_EXTRACTION_ANTI_INJECTION` in `embedding_service.py:27`.
2. Hoist `SKILL_PROPOSER_EVIDENCE_DELIMITER` in `skill_proposer_service.py:56`.
3. Update `docs/HOT-FILE-LEDGER.md` (narrative sections + scan list rows).
4. Update `CLAUDE.md` (G-5-FIRING row for embedding_service.py).
5. Verify `node scripts/check-hot-file-ledger.cjs` and `node scripts/check-claude-md-size.cjs`.
</action>
<verify>
`node scripts/check-hot-file-ledger.cjs --files backend/app/services/embedding_service.py backend/app/services/skill_proposer_service.py` exits 0.
</verify>
</task>

<task>
<task_id>236-02-02</task_id>
<description>Implement pytest conftest.py mutation fixture and run-defense-mutations.sh runner</description>
<action>
1. Create `backend/tests/unit/security/conftest.py` with `--disable-defense=<name>` CLI option.
2. Assert target symbol exists on target module before patching.
3. Create `scripts/run-defense-mutations.sh` iterating all 8 defenses.
4. Generate `.planning/phases/236-the-corpus-under-attack/236-MUTATION-REPORT.md`.
</action>
<verify>
Run `bash scripts/run-defense-mutations.sh` and verify 8/8 mutations caught loudly.
</verify>
</task>
</tasks>
