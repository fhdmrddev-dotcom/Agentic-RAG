# Phase 076.2: Provider Streaming Parity + Full Integration - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-26
**Phase:** 076.2-provider-streaming-parity-deepseek-full-integration
**Areas discussed:** DeepSeek thinking UX, Streaming delta investigation, Provider model resolution, Per-provider UAT scope

---

## Bug Routing (BUG-260524-01)

| Option | Description | Selected |
|--------|-------------|----------|
| Fold into 076.2 | Add a task to investigate + fix Google skill registration. Adjacent to the provider parity work. | ✓ |
| Defer to Phase 078 | Route to the Code-Quality Bundle phase. It's a backend wiring bug, not a streaming parity issue. | |
| Leave open | Keep as-is. Users can switch providers for skill-driven prompts. | |

**User's choice:** Fold into 076.2
**Notes:** BUG-260524-01 (Google Skills not loading) is directly related to provider parity and should be fixed alongside the other provider-specific work.

---

## DeepSeek Thinking UX

### Q1: How should reasoning_content appear in the chat UI?

| Option | Description | Selected |
|--------|-------------|----------|
| Collapsible block (Recommended) | Same pattern as Anthropic thinking blocks — collapsed by default, expandable. Consistent cross-provider UX. | ✓ |
| Always visible | Show reasoning_content inline above the response, styled differently (muted/smaller). | |
| Hidden entirely | Don't render reasoning_content in the UI at all. Stored in DB but invisible. | |

**User's choice:** Collapsible block (Recommended)

### Q2: Should thinking mode be configurable?

| Option | Description | Selected |
|--------|-------------|----------|
| Follow defaults (Recommended) | Enable thinking by default for all DeepSeek models. No user toggle. reasoning_effort='high'. | ✓ |
| Per-model capability flag | Add supports_thinking flag in MODEL_CAPABILITIES. Let users disable in Settings. | |
| User toggle in Settings | Add 'Enable thinking mode' toggle per provider in Settings. | |

**User's choice:** Follow defaults (Recommended)

---

## Streaming Delta Investigation

### Q1: How deep should we go on streaming investigation?

| Option | Description | Selected |
|--------|-------------|----------|
| Fix what we can, document what we can't (Recommended) | Streaming trace comparison per provider. Fix our chunk handler issues. Document provider-side limitations as known issues. | ✓ |
| Full parity at any cost | Build workarounds for provider bugs (polling, synthetic events, etc.). | |
| Minimal investigation | Only fix DeepSeek. Other providers ship as-is. | |

**User's choice:** Fix what we can, document what we can't

### Q2: Kimi routing — direct API vs OpenRouter?

| Option | Description | Selected |
|--------|-------------|----------|
| Direct API preferred (Recommended) | Prioritize moonshot direct API (already registered). OpenRouter stays as fallback. | ✓ |
| OpenRouter only for now | Don't invest in direct Kimi testing. | |
| Both routes tested | Test direct AND OpenRouter Kimi in UAT. | |

**User's choice:** Direct API preferred

---

## Provider Model Resolution

### Q1: Is there a real gap in provider model resolution?

| Option | Description | Selected |
|--------|-------------|----------|
| Verify during UAT (Recommended) | Include provider resolution as a UAT checkpoint. Don't allocate a dedicated plan. | ✓ |
| Dedicated investigation | Trace model resolution end-to-end for all 4 providers. | |
| Skip — it works | Trust the code. override_provider already handles this. | |

**User's choice:** Verify during UAT

---

## Per-Provider UAT Scope

### Q1: UAT priority order for 4 curated providers?

| Option | Description | Selected |
|--------|-------------|----------|
| DeepSeek deep, others smoke (Recommended) | DeepSeek: full multi-tool + thinking + streaming trace. Others: quick 2-3 turn smoke test. | ✓ |
| All 4 equal depth | Same test matrix for every provider. 4x UAT time. | |
| DeepSeek + Kimi deep, others smoke | DeepSeek and Kimi get deep testing. MiniMax and GLM get smoke tests. | |

**User's choice:** DeepSeek deep, others smoke

### Q2: Google Skills bug UAT approach?

| Option | Description | Selected |
|--------|-------------|----------|
| Part of cross-provider UAT (Recommended) | Add 'Skills load correctly' row to UAT matrix for ALL providers. | ✓ |
| Standalone checkpoint | Dedicated investigation task with its own fix + test. | |

**User's choice:** Part of cross-provider UAT

---

## Claude's Discretion

- Frontend collapsible block component reuse vs new component for DeepSeek thinking
- Streaming trace logging approach
- Kimi direct API testing scope beyond kimi-k2.6
- GLM/Zhipu testing depth (minimal monitoring data)

## Deferred Ideas

- Per-provider system prompt tuning
- Backend text dedup (SSE-level)
- Adaptive iteration limits per model
- Resume vs Retry UX rework (SEED-029)
- Native Google SDK split (SEED-028)
- Kimi/MiniMax/GLM thinking models (future)
