---
phase: 124-workflow-studio-ux-soul-strict-loose
plan: 03
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - frontend/src/components/panel/WorkspacePanel.tsx
  - frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx
  - frontend/src/components/workflows/PublishGauntlet.tsx
  - frontend/src/components/workflows/PublishGauntlet.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.tsx
autonomous: true
requirements: [WUX-01]

must_haves:
  truths:
    - "D-08: The run surface shows a soul header (purpose · tier · glyph-dot spine · needs · output) as a new panel section above the live timeline, gated to harness runs (showTimeline) so Deep Mode / no-run threads never see it — Deep stays byte-identical, no shared-path fork"
    - "D-07: The run soul header is an ADDITIVE SIBLING of PhaseTimeline.tsx / PhaseCard.tsx — their internals are byte-identical (zero diff) and no soul atoms are threaded into PhaseCard internals"
    - "The run soul sources the published definition by id (a sibling-only read), never via PhaseCard / usePhases internals, never an elapsed timer or per-phase slug"
    - "The publish summary prepends the soul block above the existing 8-stage gauntlet ladder; the ladder and verdict render unchanged (D-06)"
  artifacts:
    - path: "frontend/src/components/panel/WorkspacePanel.tsx"
      provides: "A new <PanelSection> sibling rendering <WorkflowSoul scale='run'> above the Workflow timeline section"
      contains: "WorkflowSoul"
    - path: "frontend/src/components/workflows/PublishGauntlet.tsx"
      provides: "The prepended <WorkflowSoul scale='pub'> soul block above the gauntlet content"
      contains: "WorkflowSoul"
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "Threads the definition prop into renderPublish for the PublishGauntlet soul block (additive only — zero behavior change to the describe/draft/publish flow)"
      contains: "renderPublish"
  key_links:
    - from: "frontend/src/components/panel/WorkspacePanel.tsx"
      to: "frontend/src/components/workflows/WorkflowSoul.tsx"
      via: "new sibling PanelSection at scale=run"
      pattern: "WorkflowSoul"
    - from: "frontend/src/components/workflows/PublishGauntlet.tsx"
      to: "frontend/src/components/workflows/WorkflowSoul.tsx"
      via: "prepended pub-scale soul block"
      pattern: "WorkflowSoul"
    - from: "frontend/src/components/panel/PhaseTimeline.tsx"
      to: "(unchanged)"
      via: "byte-identical — G-5 red line"
      pattern: "9bf88c9bf41780153b267a95947643174059b46f"
---

<objective>
Mount the soul at run scale and publish scale — the two remaining sizes of the
SC#1+SC#2 consistency story. On the run surface the soul header mounts as a NEW
`<PanelSection>` sibling above the existing live `title="Workflow"` timeline section
in `WorkspacePanel.tsx` (the G-5 additive-sibling seam, D-07). On the publish summary
the soul block prepends above the existing 8-stage gauntlet ladder in
`PublishGauntlet.tsx` — soul block ONLY (D-06); the ladder + verdict are untouched
(that re-skin is WUX-03 / Phase 127). The run soul sources the published definition
additively by id (A2), never by reading PhaseCard / usePhases internals. The new run
section is gated to harness runs so Deep Mode stays byte-identical, no shared-path
fork (D-08).

Purpose: WUX-01 run header + publish summary soul (the 2nd and 3rd sizes). The
G-5 red line (PhaseTimeline/PhaseCard byte-identical) is the load-bearing discipline.
Output: the run-surface soul section + the publish soul block.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/124-workflow-studio-ux-soul-strict-loose/124-CONTEXT.md
@.planning/phases/124-workflow-studio-ux-soul-strict-loose/124-RESEARCH.md
@.planning/phases/124-workflow-studio-ux-soul-strict-loose/124-PATTERNS.md
@.planning/sketches/046-workflow-soul-object/README.md
@.planning/sketches/046-workflow-soul-object/index.html

<interfaces>
<!-- From Plan 01 (wave 1) — already built and tested: -->
From frontend/src/components/workflows/WorkflowSoul.tsx:
```typescript
export function WorkflowSoul(props: { def: DefShape | null | undefined; scale: "card" | "run" | "pub" }): JSX.Element;
```
From frontend/src/components/workflows/soulData.ts:
```typescript
export type DefShape = { /* loose read-shape over the definition JSONB */ };
```

<!-- The G-5 additive-sibling mount seam (WorkspacePanel.tsx:184-191) — add ABOVE this, do NOT edit PhaseTimeline -->
```tsx
{showTimeline && (
  <PanelSection title="Workflow" count={phases.length || undefined}>
    <PhaseTimeline threadId={threadId} />   {/* ← UNCHANGED. Soul header is a separate section. */}
  </PanelSection>
)}
```

<!-- A2 (RESOLVED): the run-frame ThreadWorkflowState carries only run-state phases ({slug,phase_index,status}) + definition_name — NOT the authored definition.
     The run soul must source the published WorkflowDefinitionJSON additively by id — the SAME JSONB the card gets via PublishedWorkflow.definition (api.ts:1121-1126).
     A sibling-only read; never consume usePhases / PhaseCard internals; never render an elapsed timer or per-phase slug (would re-open BUG-260610-01 / BUG-260609-04). -->

<!-- The publish prepend point (PublishGauntlet.tsx GauntletContent return, ~line 386-419): -->
```tsx
return (
  <div className="w-full">
    {/* ← PREPEND <WorkflowSoul scale="pub"> HERE, above this resting publish form */}
    <div className="rounded-lg border border-border bg-card p-4"> ... golden_input form ... </div>
    <GauntletSpine .../>   {/* ← 8-stage ladder — UNCHANGED (D-06 / WUX-03 = Phase 127) */}
```
<!-- GauntletContent has definitionId but NOT the definition object. The Builder's renderPublish already passes the full def:
     WorkflowBuilderPage.tsx:409 → renderPublish(state.definition, draftId). -->
<!-- Thread the definition into PublishGauntlet via a new optional `definition` prop. -->

<!-- G-5 HEAD blob hashes for the byte-identical assertion (baseline clean): -->
<!-- PhaseTimeline.tsx = 9bf88c9bf41780153b267a95947643174059b46f -->
<!-- PhaseCard.tsx     = a489492fc3c7367e442e4b18ad3e934ed247d8aa -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the run-surface soul as an additive sibling PanelSection (G-5)</name>
  <files>frontend/src/components/panel/WorkspacePanel.tsx, frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx</files>
  <read_first>
    - frontend/src/components/panel/WorkspacePanel.tsx (lines 150-208 — the PanelSection idiom; the showTimeline gate; the existing title="Workflow" section at 187-191 that stays UNCHANGED)
    - frontend/src/components/panel/PhaseTimeline.tsx (read ONLY to confirm the RunFrame/getThreadWorkflow read shape — DO NOT edit; it carries definition_name + run-state phases, not the authored definition — A2)
    - frontend/src/lib/api.ts (the ThreadWorkflowState type ~1069-1112 and the published-definition read used by the card — PublishedWorkflow.definition ~1121-1126; pick the existing additive read to source the full definition by id)
    - frontend/src/components/workflows/WorkflowSoul.tsx (the scale-keyed soul; render at scale="run")
    - frontend/src/components/workflows/PhaseSpineGraph.test.tsx (the ?raw G-5 source-grep idiom — lines 16-19, 153-159)
    - .planning/sketches/046-workflow-soul-object/README.md (the run-header (②) build-handover row: a NEW strip above the existing meaningful spine; draft → "draft · test run" honest label; do NOT thread soul state into PhaseCard)
    - .planning/phases/124-workflow-studio-ux-soul-strict-loose/124-PATTERNS.md (the WorkspacePanel additive-sibling section + the A2 resolution: source the published definition by id, sibling-only read)
  </read_first>
  <action>
    In `WorkspacePanel.tsx` add a NEW `<PanelSection>` ABOVE the existing `title="Workflow"` section (line 187) that renders `<WorkflowSoul scale="run" def={...} />`. Per D-07 this is an ADDITIVE SIBLING: do NOT edit the `PhaseTimeline` section, do NOT add any prop to `PhaseTimeline`/`PhaseCard`, do NOT thread any soul atom into PhaseCard internals, do NOT touch `panel/PhaseTimeline.tsx` or `panel/PhaseCard.tsx`. Source the soul's definition ADDITIVELY (A2): the run frame carries only `definition_name` + run-state phases, so fetch/select the published `WorkflowDefinitionJSON` by id (reuse the existing read the card uses — `PublishedWorkflow.definition` / the listing already loaded, or a small by-id read) — a sibling-only read. The soul header must read the DEFINITION (purpose/tier/spine/needs/output) only; it must NOT consume `usePhases(threadId)` for live phase state, must NOT render an elapsed timer, and must NOT render a per-phase slug (those belong to the live timeline and would re-open BUG-260610-01 / BUG-260609-04). Gate the new section to the same harness-run/phases-exist condition the timeline uses (`showTimeline`) so Deep/no-run threads never see it — Deep Mode stays byte-identical, no shared-path fork (D-08). For a draft test-run where `business_requirement` is null, the soul's honest empty-state ("draft · purpose not declared yet") already covers it. Update `WorkspacePanel.test.tsx`: assert the run soul section mounts as a sibling (a WorkflowSoul renders when showTimeline is true) and that the WorkspacePanel source still renders the unchanged PhaseTimeline section. Add a ?raw source-grep test asserting the WorkspacePanel's new soul wiring does not import or thread into `PhaseCard`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx && git diff --stat -- frontend/src/components/panel/PhaseTimeline.tsx frontend/src/components/panel/PhaseCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- frontend/src/components/panel/PhaseTimeline.tsx frontend/src/components/panel/PhaseCard.tsx` is EMPTY (zero lines) — the G-5 byte-identical proof (D-07). Equivalently `git rev-parse :frontend/src/components/panel/PhaseTimeline.tsx` still equals `9bf88c9bf41780153b267a95947643174059b46f` and PhaseCard.tsx equals `a489492fc3c7367e442e4b18ad3e934ed247d8aa`.
    - The new soul `<PanelSection>` renders `<WorkflowSoul scale="run">` and mounts ABOVE the existing `title="Workflow"` PhaseTimeline section (DOM order asserted).
    - The new run-soul section is gated to `showTimeline` (harness runs only) — a Deep/no-run thread renders no soul section (D-08).
    - The WorkspacePanel run-soul wiring does NOT add a prop to PhaseCard/PhaseTimeline and does NOT call `usePhases` to drive the soul; it renders no elapsed timer and no per-phase slug in the soul section.
    - The soul definition is sourced via an existing/by-id read, not by reading live run-state phases.
  </acceptance_criteria>
  <done>The run surface shows a soul header as an additive sibling section; both G-5 hot files are byte-identical; the soul reads the definition, not the live timeline; the section is gated to harness runs so Deep is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prepend the soul block on the publish summary (D-06 soul-only)</name>
  <files>frontend/src/components/workflows/PublishGauntlet.tsx, frontend/src/components/workflows/PublishGauntlet.test.tsx, frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PublishGauntlet.tsx (the GauntletContent return ~386-419 — the prepend point above the resting form; the 8-stage STAGES ladder + GauntletSpine + verdict that stay UNCHANGED, D-06; the existing props at 46+ — definitionId)
    - frontend/src/pages/WorkflowBuilderPage.tsx (line 64 renderPublish signature + line 409 renderPublish(state.definition, draftId) — the full definition is already available to pass into the gauntlet)
    - frontend/src/pages/WorkflowsPage.tsx (the builder host's renderPublish={(_def, draftId) => <PublishGauntlet .../>} seam at 377-398 — _def is the full definition to thread through)
    - frontend/src/components/workflows/WorkflowSoul.tsx (render at scale="pub")
    - .planning/sketches/046-workflow-soul-object/README.md (the PublishGauntlet build-handover row: prepend the soul block above the existing ladder; the ladder + verdict stay as-is)
    - .planning/phases/124-workflow-studio-ux-soul-strict-loose/124-PATTERNS.md (the PublishGauntlet prepend-only section, D-06 anti-pattern)
  </read_first>
  <action>
    Add a new OPTIONAL `definition?: DefShape` (or the existing definition JSONB type) prop to `PublishGauntlet` and thread it down to `GauntletContent`. In `GauntletContent`'s return, PREPEND `<WorkflowSoul scale="pub" def={definition} />` as the FIRST child inside the top-level `<div className="w-full">`, above the resting `golden_input` publish form. Do NOT modify the `GauntletSpine` 8-stage ladder, the `PublishingNotice`, the verdict/outcome rendering, the HTTP-outcome badge, or any of the stage logic — D-06: soul block PREPEND only; the ladder re-skin (pip-strip + worded verdict + quiet idle cards) is WUX-03 / Phase 127. Thread the definition from the call site: in `WorkflowsPage.tsx`'s builder host, the `renderPublish={(_def, draftId) => <PublishGauntlet definitionId={draftId} definition={_def} ... />}` — `_def` is already the full definition passed by the Builder's `renderPublish(state.definition, draftId)`. `WorkflowBuilderPage.tsx` is touched ONLY to thread the `definition` type cleanly through the `renderPublish` prop signature (e.g. widening/forwarding the `BuilderDefinition` type so the call site can pass it) — this is ADDITIVE: zero behavior change to the describe/draft/publish flow, no edit to the gauntlet invocation logic, no new state. Render all authored strings as escaped React children (inherited from WorkflowSoul). Extend `PublishGauntlet.test.tsx`: assert the soul block (a WorkflowSoul / its tier chip + purpose) renders ABOVE the gauntlet content, AND assert the existing 8-stage gauntlet-spine + verdict assertions still pass unchanged (the ladder is byte-behavior-identical).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `PublishGauntlet.test.tsx` asserts a `<WorkflowSoul scale="pub">` (purpose + tier chip) renders, and that it appears BEFORE the `gauntlet-spine` (DOM order: soul block first).
    - The existing PublishGauntlet tests (the 8-stage spine, the 4 distinct HTTP outcomes, the publish-success/publish-block verdicts) still pass UNCHANGED — no ladder/verdict markup edited (D-06).
    - `PublishGauntlet` accepts an optional `definition` prop; passing it absent does not crash (the soul renders the honest draft empty-states).
    - `WorkflowBuilderPage.tsx` is touched ONLY to thread the `definition` type through `renderPublish` (additive forwarding) — ZERO behavior change, confirmed by the UNCHANGED PublishGauntlet/Builder integration assertions (the describe/draft/publish flow tests stay green; no new behavior added).
    - The `GauntletSpine`/`STAGES`/verdict code paths show no diff beyond the prepend + the new prop plumbing.
  </acceptance_criteria>
  <done>The publish summary prepends the pub-scale soul block; the 8-stage ladder and verdict are untouched; the definition is threaded from the Builder's renderPublish (WorkflowBuilderPage.tsx touched additively for the prop type only — zero behavior change).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored workflow strings → React render | The run soul + publish soul render `business_requirement`, phase names, input keys, deliverable label — all user/LLM-authored. |
| run-frame / published-definition read → soul | The run soul sources the published definition by id; the boundary is the existing owner-scoped read (no new endpoint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-09 | Tampering / Elevation (XSS) | run soul + publish soul blocks | mitigate | All authored strings render as escaped React children (inherited from WorkflowSoul/PhaseSpine, Plan 01). No `dangerouslySetInnerHTML`; no authored text in raw HTML attributes. |
| T-124-10 | Tampering (G-5 red-line creep) | `WorkspacePanel.tsx` run soul mount | mitigate | The soul is an additive sibling `<PanelSection>` (D-07); PhaseTimeline.tsx/PhaseCard.tsx internals byte-identical (asserted by an EMPTY `git diff --stat` and the pinned HEAD blob hashes). No new PhaseCard prop, no usePhases-driven soul. |
| T-124-11 | Repudiation (re-opening BUG-260610-01 / BUG-260609-04) | run soul header | mitigate | The soul reads the DEFINITION only — no elapsed timer, no per-phase slug. The run strip timer and phase-card slug render are not touched, so the two OPEN bugs' re_open_triggers do not fire. |
| T-124-12 | Information disclosure (new data exposure) | the published-definition read on the run surface | accept | The run soul reads the SAME owner-scoped published definition the library card already reads (`PublishedWorkflow.definition`); no new endpoint, no widening of the fetched fields, RLS unchanged. |
| T-124-13 | Tampering (D-06 scope creep into the gauntlet ladder) | `PublishGauntlet.tsx` | mitigate | Soul block PREPEND only; the 8-stage ladder + verdict markup are byte-behavior-identical (existing gauntlet tests stay green). The ladder re-skin is explicitly Phase 127 / WUX-03. |
| T-124-14 | Tampering (Deep-path / shared-path fork) | `WorkspacePanel.tsx` run soul gate | mitigate | The new run-soul section is gated to `showTimeline` (harness runs only); Deep Mode / no-run threads render no soul — Deep stays byte-identical, no shared-path fork (D-08). |

No high-severity threats remain after the T-124-09 XSS and T-124-10 G-5 mitigations: FE-only chrome over read-only owner-scoped data, no new endpoint, no run-strip / launch-path / Deep-path change.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx src/components/workflows/PublishGauntlet.test.tsx` — all green.
- `git diff --stat -- frontend/src/components/panel/PhaseTimeline.tsx frontend/src/components/panel/PhaseCard.tsx` — EMPTY (G-5 byte-identical proof).
- `cd frontend && npx vitest run` (full suite at the wave/phase gate) — no regression in panel/ or workflows/.
- Manual sketch-match UAT (phase gate, before /gsd:verify-work): the run header + publish summary match 046-A across all 3 soul sizes; switching workflow changes tier/spine/needs/output in lockstep across card/run/pub.
</verification>

<success_criteria>
- The run surface shows a soul header as an additive sibling section; PhaseTimeline/PhaseCard are byte-identical.
- The publish summary prepends the pub-scale soul block; the gauntlet ladder + verdict are untouched (D-06).
- The run soul sources the published definition by id (A2), reads no live phase state, renders no timer/slug.
- The run-soul section is gated to harness runs; Deep Mode is byte-identical (D-08).
- All three soul sizes (card from Plan 02, run + pub here) render a consistent tier/spine/needs/output for one workflow.
</success_criteria>

<output>
Create `.planning/phases/124-workflow-studio-ux-soul-strict-loose/124-03-SUMMARY.md` when done.
Note in the summary: the A1 deliverable-label string + the full 3-size sketch-match are flagged for the phase-gate manual UAT against 046-A.
</output>
</content>
</invoke>
