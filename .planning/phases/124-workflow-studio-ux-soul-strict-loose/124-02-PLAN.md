---
phase: 124-workflow-studio-ux-soul-strict-loose
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - frontend/src/components/workflows/WorkflowDoorSwitch.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
  - frontend/src/pages/WorkflowsPage.tsx
  - frontend/src/pages/WorkflowsPage.test.tsx
autonomous: true
requirements: [WUX-01, WUX-02]

must_haves:
  truths:
    - "The library card (published + draft) shows the workflow soul at card scale — the same essence the run header and publish summary show"
    - "Entering the Studio to create or shape a workflow opens a two-door fork: 'Describe & run' (loose) and 'Author & govern' (strict)"
    - "The describe door shows a soul preview + a 'switch to Author & govern ›' strip; a persistent '‹ both doors' returns from either door"
    - "The 'Author & govern' door opens the EXISTING Builder with all advanced controls; the tier recomputes live and the judge stays locked-on"
    - "The library-card Run path (doRun → createThread → postMessage → create_workflow_run) is NOT wrapped by the fork and still launches into a thread"
  artifacts:
    - path: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      provides: "The two-door disclosure shell wrapping the describe box + the existing Builder"
      exports: ["WorkflowDoorSwitch"]
    - path: "frontend/src/pages/WorkflowsPage.tsx"
      provides: "Card-scale soul on library cards + the door-fork entry on New / Author & govern"
      contains: "WorkflowSoul"
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "frontend/src/components/workflows/WorkflowSoul.tsx"
      via: "card-scale soul render replacing TierBadge + PhaseChain + entry-needs"
      pattern: "WorkflowSoul"
    - from: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      to: "frontend/src/pages/WorkflowBuilderPage.tsx"
      via: "the Author & govern door mounts the existing Builder"
      pattern: "WorkflowBuilderPage"
    - from: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      to: "frontend/src/components/workflows/WorkflowSoul.tsx"
      via: "describe-door soul preview"
      pattern: "WorkflowSoul"
---

<objective>
Surface the soul at card scale on every library card and fork the Studio authoring
ENTRY into the two-door disclosure (047-A). The card soul replaces the current
ad-hoc TierBadge + PhaseChain + "entry needs" trio with the shared
`<WorkflowSoul scale="card">`. The `WorkflowDoorSwitch` shell offers "Describe & run"
(loose — describe box + soul preview + one CTA, with a one-click "switch to Author &
govern ›" strip) and "Author & govern" (strict — the existing Builder with all
advanced controls, live tier recompute, locked judge). The fork lives at the Studio
entry ONLY (New / Author & govern a draft); the library-card Run stays the Phase-121
one-click launch-into-thread, byte-identical.

Purpose: WUX-01 (card-scale soul, SC#1+2 consistency) + WUX-02 (the strict↔loose two
doors, nothing removed, advanced one click away — D-01, D-05).
Output: `WorkflowDoorSwitch.tsx` + the restyled `WorkflowsPage.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/124-workflow-studio-ux-soul-strict-loose/124-CONTEXT.md
@.planning/phases/124-workflow-studio-ux-soul-strict-loose/124-RESEARCH.md
@.planning/phases/124-workflow-studio-ux-soul-strict-loose/124-PATTERNS.md
@.planning/sketches/047-strict-loose-two-doors/README.md
@.planning/sketches/047-strict-loose-two-doors/index.html
@.planning/sketches/046-workflow-soul-object/index.html

<interfaces>
<!-- From Plan 01 (wave 1) — already built and tested: -->
From frontend/src/components/workflows/WorkflowSoul.tsx:
```typescript
export function WorkflowSoul(props: { def: DefShape | null | undefined; scale: "card" | "run" | "pub" }): JSX.Element;
```
From frontend/src/components/workflows/soulData.ts:
```typescript
export type DefShape = { /* loose read-shape over the definition JSONB */ };
export function tierForDefinition(def: DefShape | null | undefined): Tier;
export function entryInputKeys(def: DefShape | null | undefined): string[];
```

<!-- The Studio entry seam this plan forks (WorkflowsPage.tsx) -->
<!-- pageView state "library" | "builder"; openBuilderFresh() (New card), onOpenDraft()/onTweak() (Author & govern an existing draft) all setPageView("builder"). -->
<!-- The builder host block renders <WorkflowBuilderPage initial=... renderPublish=... /> wrapped in a header with a "← Workflows" back button (backToLibrary). -->
<!-- Library cards: DraftCard + PublishedCard currently render <TierBadge def/> + <PhaseChain def/> + an "entry needs <keys>" span — these three are the card soul to replace. -->

<!-- The preserved launch path (D-01 — DO NOT wrap in the fork) -->
From frontend/src/components/layout/ChatLayout.tsx:91-100:
```typescript
const doRun = async (def, kickoff) => { const t = await createThread(def.name);
  await postMessage(t.id, kickoff, { workflowDefinitionId: def.id }); /* → create_workflow_run */ }
```

<!-- The describe-box lineage (Door A) — reuse its shape/copy -->
From frontend/src/pages/WorkflowBuilderPage.tsx:270-336:
<!-- the EMPTY-state screen: one <textarea aria-label business requirement>, a disabled "Draft the workflow" CTA, an honest hint. Door A descends from this 018-A lineage. -->

<!-- The Builder accepts initial + renderPublish; the drafted view already owns the read-only PhaseSpineGraph + 400px PhaseFormPanel (citation_policy / gates / folder_scope / model → deriveTier live; llm_judge_rubric locked). -->
From frontend/src/pages/WorkflowBuilderPage.tsx:64,76:
```typescript
renderPublish?: (def: BuilderDefinition, draftId: string | null) => React.ReactNode
export function WorkflowBuilderPage({ renderPublish, initial }: WorkflowBuilderPageProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the card-scale soul on library cards</name>
  <files>frontend/src/pages/WorkflowsPage.tsx, frontend/src/pages/WorkflowsPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowsPage.tsx (DraftCard 589-639 and PublishedCard 641-702 — the TierBadge + PhaseChain + "entry needs" trio to replace; keep the card actions/buttons unchanged)
    - frontend/src/components/workflows/WorkflowSoul.tsx (the scale-keyed soul from Plan 01)
    - frontend/src/components/workflows/soulData.ts (DefShape, entryInputKeys, tierForDefinition — now imported from here, not page-local)
    - .planning/sketches/046-workflow-soul-object/index.html (the card-size acceptance bar — purpose HERO, glyph-dot spine, tier chip, needs, output)
    - .planning/phases/124-workflow-studio-ux-soul-strict-loose/124-PATTERNS.md (the WorkflowsPage edit section — restyle card to scale-keyed soul; the State-of-the-Art note that PhaseChain's ribbon/label render is dropped)
  </read_first>
  <action>
    In `WorkflowsPage.tsx` replace the page-local `tierForDefinition`, `entryInputKeys`, `PHASE_GLYPHS`, `POLICY_ORDER`, `stricterPolicy`, `ALL_VALIDATOR_KINDS`, and the `DefShape` interface with imports from `@/components/workflows/soulData` (Plan 01 extracted them — delete the now-duplicate definitions to honor the single-source-of-truth rule). In `DraftCard` and `PublishedCard`, replace the `<TierBadge def={def} />` + `<PhaseChain def={def} />` + the `entry needs <keys>` span with a single `<WorkflowSoul def={def} scale="card" />`. Keep the card chrome that is NOT a soul atom: the name/version header row, the draft/published status pill, the folder chip, and the action buttons (Open/Publish for drafts; Tweak/Run for published) — these are card affordances, not soul atoms, and must stay. Preserve the `data-testid="published-card"` / `data-testid="draft-card"` / `data-testid="published-run"` / `data-testid="published-tweak"` hooks unchanged. Render all authored strings as escaped React children. Update `WorkflowsPage.test.tsx` (or create it) to assert: a published card renders a WorkflowSoul (the tier chip + glyph-dot spine appear), and the Run button (`published-run`) still calls `onLaunch` — i.e. the launch path is NOT wrapped or removed (D-01).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Library cards render `<WorkflowSoul ... scale="card" />`; the old `TierBadge`/`PhaseChain`/"entry needs" trio is removed from the card bodies.
    - `WorkflowsPage.tsx` no longer declares its own `PHASE_GLYPHS` or `tierForDefinition` (grep: no local `const PHASE_GLYPHS` / `function tierForDefinition` in the page; they import from `soulData`).
    - A test asserts the published card's Run button still invokes `onLaunch` (D-01 launch path not wrapped) and the soul tier chip renders.
    - The `published-run`/`published-tweak`/`draft-open` testids are intact.
  </acceptance_criteria>
  <done>Both card variants render the shared card-scale soul; the page consumes the extracted soulData; the Run launch path is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Build the two-door fork at the Studio authoring entry</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.tsx, frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx, frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowsPage.tsx (the builder host block 347-403 — openBuilderFresh/onOpenDraft/onTweak all setPageView("builder"); the back button backToLibrary; the WorkflowBuilderPage mount with initial + renderPublish)
    - frontend/src/pages/WorkflowBuilderPage.tsx (the EMPTY describe screen 270-336 — the 018-A describe box lineage for Door A; the drafted view it renders for Door B; the renderPublish/initial props)
    - frontend/src/components/workflows/WorkflowSoul.tsx (the soul preview shown in the describe door)
    - .planning/sketches/047-strict-loose-two-doors/README.md (variant A explicit fork — two big door cards; "‹ both doors" return; "switch to Author & govern ›" strip; judge locked; nothing removed)
    - .planning/sketches/047-strict-loose-two-doors/index.html (the rendered two-door acceptance bar)
    - .planning/phases/124-workflow-studio-ux-soul-strict-loose/124-PATTERNS.md (the WorkflowDoorSwitch section — intra-view fork analog, Door A describe lineage, Door B = existing Builder, locked door behaviors)
  </read_first>
  <action>
    Create `WorkflowDoorSwitch.tsx` — the explicit-fork shell (047-A variant A). It holds local door state (`"both" | "describe" | "govern"`, default `"both"`). At `"both"` it renders two big side-by-side door cards: "Describe & run" (loose) and "Author & govern" (strict). Selecting "Describe & run" opens the describe content inline — reuse the describe-box shape/copy descended from WorkflowBuilderPage's empty screen (a textarea labeled for the business requirement, a draft CTA, an honest hint) PLUS a `<WorkflowSoul scale="card">` soul preview of the current draft/definition PLUS a visible "switch to Author & govern ›" strip. Selecting "Author & govern" mounts the EXISTING `WorkflowBuilderPage` (drafted/govern view) — do NOT rebuild the advanced controls; the Builder's PhaseFormPanel already owns citation_policy / gate chips / folder_scope / model with live deriveTier recompute, and llm_judge_rubric is locked always-on. A persistent "‹ both doors" control returns from either open door to the `"both"` chooser. Accept props that let the shell drive both the fresh-build and the open/tweak flows: an `initial` (the existing definition for Author & govern a draft, or undefined for a fresh build), a `renderBuilder` (or pass through the Builder's `initial`/`renderPublish`), an `onDescribeDraft` handler (the describe-CTA → draft action), and the current `def` for the soul preview. The shell adds NO API call and NO tier re-derivation of its own — it delegates governance to the Builder and tier display to WorkflowSoul. Then wire it into `WorkflowsPage.tsx`: the builder host block (entered via openBuilderFresh / onOpenDraft / onTweak) now renders `<WorkflowDoorSwitch>` instead of mounting `WorkflowBuilderPage` directly — for a FRESH build the shell opens at `"both"`; for "Author & govern an existing draft" (Open/Tweak) the shell may open straight into the govern door with the loaded `initial` (per D-01/D-05 the fork is the Studio entry — Open/Tweak land in govern). The library-card Run path (doRun via onLaunch) is untouched and is NOT routed through the shell. Write `WorkflowDoorSwitch.test.tsx`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowDoorSwitch.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `WorkflowDoorSwitch.test.tsx` asserts: at the `"both"` state both door cards ("Describe & run" and "Author & govern") render side by side.
    - A test asserts the describe door shows a "switch to Author & govern ›" strip and a WorkflowSoul preview, and that a "‹ both doors" control returns to the chooser.
    - A test asserts the "Author & govern" door mounts the existing `WorkflowBuilderPage` (the read-only spine/govern view) — the shell does not re-implement citation_policy/gate editors.
    - A test asserts `llm_judge_rubric` renders LOCKED / non-removable wherever the door surfaces it (delegated to the Builder/TIERS judgeAlwaysOn invariant — assert the locked affordance is present, not a removable toggle).
    - `WorkflowDoorSwitch` adds no API import (grep: no `from "@/lib/api"` introducing a new fetch path) and no local `deriveTier`/`tierForDefinition` re-derivation.
  </acceptance_criteria>
  <done>The Studio entry forks into two doors; the describe door carries a soul preview + the one-click switch strip; the govern door is the existing Builder with live tier recompute + locked judge; the library-card Run path is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored workflow strings → React render | The card soul + the describe-door soul preview render `business_requirement`, phase names, input keys, deliverable label — all user/LLM-authored. |
| describe-box free text → draft action | The describe-door textarea is user input forwarded to the existing draft/generate path (no new sink in this plan). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-05 | Tampering / Elevation (XSS) | card soul + describe-door soul preview in `WorkflowsPage.tsx` / `WorkflowDoorSwitch.tsx` | mitigate | All authored strings render as escaped React children (inherited from WorkflowSoul/PhaseSpine, Plan 01). No `dangerouslySetInnerHTML`; no authored text interpolated into raw HTML attributes. |
| T-124-06 | Spoofing / Elevation (D-01 launch bypass) | the door fork vs the library-card Run | mitigate | The fork lives at the Studio authoring entry ONLY; the library-card Run (`doRun`/`onLaunch`) is NOT wrapped — preserved byte-identical, asserted by a test that Run still calls `onLaunch`. No new launch route is introduced. |
| T-124-07 | Repudiation (silent governance downgrade) | the "Author & govern" door | mitigate | The govern door delegates to the existing Builder; the judge (`llm_judge_rubric`) renders locked always-on (TIERS.judgeAlwaysOn + gauntlet hard wall). The shell never re-derives the tier or forks the gate logic — a STRICT workflow cannot be silently downgraded. |
| T-124-08 | Information disclosure (new data exposure) | the whole plan | accept | No new endpoint, no new fetch, no widening: cards read the same owner-scoped definitions already listed; the describe box reuses the existing draft/generate path. |

No high-severity threats remain after the T-124-05 XSS and T-124-06 launch-preservation mitigations: pure FE chrome over read-only owner-scoped data, the only write path (draft/generate) and launch path unchanged.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/WorkflowsPage.test.tsx src/components/workflows/WorkflowDoorSwitch.test.tsx` — all green.
- `cd frontend && npx vitest run src/components/workflows src/pages` — no regression in workflow/page tests.
- The D-01 launch-not-wrapped test and the judge-locked test are present and green.
- Manual sketch-match (deferred to the phase-gate UAT, Plan 03 owns the run/pub UAT rows): the card soul + both doors match 046-A/047-A.
</verification>

<success_criteria>
- Library cards render the shared card-scale soul; the page no longer duplicates the tier/glyph helpers.
- The Studio entry forks into "Describe & run" and "Author & govern" with a one-click switch strip and a "‹ both doors" return.
- The govern door is the existing Builder (live tier recompute, locked judge) — no advanced control removed.
- The library-card Run launch path is byte-identical (not wrapped by the fork).
</success_criteria>

<output>
Create `.planning/phases/124-workflow-studio-ux-soul-strict-loose/124-02-SUMMARY.md` when done.
</output>
