---
phase: 115-virtual-folders-agent-tool
plan: 03
type: execute
wave: 3
depends_on: ["115-02"]
files_modified:
  - backend/app/services/openai_service.py
  - backend/app/services/tool_dispatcher.py
  - backend/tests/unit/test_115_tool_wiring.py
  - backend/tests/unit/test_115_tool_schema.py
  - backend/tests/unit/test_115_whitelist_guard.py
  - backend/tests/integration/test_115_tool_global_leak.py
autonomous: true
requirements: [VIEW-07]

must_haves:
  truths:
    - "query_documents_by_view is registered in _TOOL_REGISTRY AND advertised in get_tools() output (the inverse-Phase-101 dual-wiring — SC#1)"
    - "The advertised tool schema has NO anyOf/oneOf (Gemini-safe); view and filter are genuinely optional; the inline-filter op enums match ViewCondition.op exactly"
    - "A phase_whitelist excluding the tool refuses it via the dispatch_tool guard; None (Deep) dispatches it (SC#2 free — no special-casing)"
    - "Two users + ONE seeded global view → per-viewer disjoint result sets driving the actual tool handler (the live leak proof)"
  artifacts:
    - path: "backend/app/services/openai_service.py"
      provides: "QUERY_DOCUMENTS_BY_VIEW_TOOL schema constant + the get_tools() assembly entry (SC#1 second wiring site)"
      contains: "QUERY_DOCUMENTS_BY_VIEW_TOOL"
    - path: "backend/app/services/tool_dispatcher.py"
      provides: "the _TOOL_REGISTRY entry mapping query_documents_by_view → _handle_query_documents_by_view (SC#1 first wiring site)"
      contains: "\"query_documents_by_view\": _handle_query_documents_by_view"
    - path: "backend/tests/integration/test_115_tool_global_leak.py"
      provides: "the live two-user leak proof driving the tool handler (run live in secure-phase)"
      contains: "_handle_query_documents_by_view"
  key_links:
    - from: "backend/app/services/openai_service.py get_tools()"
      to: "QUERY_DOCUMENTS_BY_VIEW_TOOL"
      via: "the default tool-schema assembly list (Deep-visible)"
      pattern: "QUERY_DOCUMENTS_BY_VIEW_TOOL\\]"
    - from: "backend/app/services/tool_dispatcher.py _TOOL_REGISTRY"
      to: "_handle_query_documents_by_view"
      via: "the one-line registry mapping (G-5 contract; threads.py untouched)"
      pattern: "\"query_documents_by_view\": _handle_query_documents_by_view"
    - from: "backend/tests/integration/test_115_tool_global_leak.py"
      to: "_handle_query_documents_by_view"
      via: "driving the agent path (not the route) for the live leak proof"
      pattern: "_handle_query_documents_by_view"
---

<objective>
Land the SC#1 dual-wiring that guards the inverse-Phase-101 visibility bug: define the
cross-provider-safe `QUERY_DOCUMENTS_BY_VIEW_TOOL` schema in `openai_service.py` and add it to
BOTH `_TOOL_REGISTRY` (`tool_dispatcher.py:2353`) AND the `get_tools()` default assembly
(`openai_service.py:876-882`), so the Deep-mode model actually SEES and CALLS the tool. Author
the schema with NO `anyOf`/`oneOf` (Gemini function-calling rejects them — RESEARCH Pitfall 1):
two flat optional fields `view: string` and `filter: object` + an optional `limit: integer`, the
either/or invariant in PROSE, the nested-`filter` `op` enums matching `ViewCondition.op` EXACTLY.
Then prove SC#2 (the `phase_whitelist` guard is free) with a unit test and SC#2-leak-safety with
the LIVE two-user leak integration test driving the actual handler.

Purpose: "registered" is not the acceptance bar — the model must ACTUALLY invoke it live (SC#1).
This is the INVERSE of `render_template` (registered but deliberately NOT in `get_tools` →
harness-only); this tool MUST be in both. SC#2 is free: never special-case the tool around the
`dispatch_tool` guard. The live leak test (run in secure-phase) is the non-vacuous proof that the
agent PATH itself is leak-safe — the unit/wiring tests can pass while the boundary is broken (the
"static would false-green → verify leak-safety LIVE" lesson).

Output: the dual-wired, cross-provider-safe tool schema; the SC#2 whitelist-guard test; and the
live two-user leak proof scaffold completed (drives the handler, not the route).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-virtual-folders-agent-tool/115-CONTEXT.md
@.planning/phases/115-virtual-folders-agent-tool/115-RESEARCH.md
@.planning/phases/115-virtual-folders-agent-tool/115-PATTERNS.md
@.planning/phases/115-virtual-folders-agent-tool/115-02-SUMMARY.md

<interfaces>
<!-- The two wiring sites + the schema shape. Exact line anchors verified in source. -->

The advertised-schema analogs (openai_service.py):
- `SEARCH_DOCUMENTS_TOOL` (:17-53) — optional nested object, only `query` in `required` → `metadata_filter` genuinely optional. The shape to mirror for OPTIONAL fields.
- `QUERY_DOCUMENTS_TOOL` (:56) — the description does the routing work / contrast (D-115-5).
- `RENDER_TEMPLATE_TOOL` / `_build_render_template_tool` (:528) — a nested object whose own sub-fields are `required` (the precedent for the inline `filter` shape, production-proven cross-provider incl. DeepSeek's nested-object rule).

The SECOND wiring site (SC#1) — get_tools() assembly (openai_service.py:873-889):
```
def get_tools(user_settings=None) -> list[dict]:
    ...
    tools = [SEARCH_DOCUMENTS_TOOL, QUERY_DOCUMENTS_TOOL, LS_TOOL, ..., ASK_USER_TOOL]   # ADD QUERY_DOCUMENTS_BY_VIEW_TOOL here
```
NOTE: `apply_tool_budget` (:892) only filters on the HARNESS path (Google max_tools:16); Deep stays byte-identical. This tool is Deep-visible by default.

The FIRST wiring site (SC#1) — _TOOL_REGISTRY (tool_dispatcher.py:2353-2382):
```
_TOOL_REGISTRY: dict[str, Callable] = {
    "ls": _handle_ls,
    ...
    "render_template": _handle_render_template,   # ← registered but NOT in get_tools (harness-only) — the INVERSE
}
```
Add ONE line: `"query_documents_by_view": _handle_query_documents_by_view,` (mirror the G-5 comment at :2380-2381; threads.py untouched).

The dispatch guard — SC#2 is FREE (tool_dispatcher.py:2417-2439):
```
async def dispatch_tool(tool_name, args, ctx) -> ToolResult:
    if ctx.phase_whitelist is not None and tool_name not in ctx.phase_whitelist:
        ... return ToolResult(result=json.dumps({"error":"tool_not_available_in_phase", ...}))
    handler = _TOOL_REGISTRY.get(tool_name)
    ...
```
NEVER special-case the new tool around this guard. `phase_whitelist=None` (Deep) → no-op → dispatches.

The op-enum parity source (models/document_view.py:42-64):
`ViewCondition.op = Literal["eq","gte","lte","one_of","contains","is_empty","within_next","older_than","before","after","between"]`; the relative-date `unit = Literal["days","weeks","months"]`. The schema's `filter.properties.conditions.items.properties.op.enum` MUST equal these exactly.

The full drop-in schema: 115-RESEARCH.md:144-216 (QUERY_DOCUMENTS_BY_VIEW_TOOL — copy it verbatim; description contrasts search_documents/query_documents, prose either/or invariant, `value` uses a JSON-Schema type-array NOT anyOf).

The leak-test analog (clone, drive the handler): backend/tests/integration/test_113_view_global_leak.py — `_pg_reachable`, `_seed_user`, `_seed_doc`, `_seed_global_view`, the two-user assertions (:226-275). Plan 01 created the test_115_tool_global_leak.py scaffold; this plan completes it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define QUERY_DOCUMENTS_BY_VIEW_TOOL + dual-wire it (registry + get_tools)</name>
  <read_first>
    - backend/app/services/openai_service.py:17-96 (`SEARCH_DOCUMENTS_TOOL`, `QUERY_DOCUMENTS_TOOL` — optional-field shape + description-as-router)
    - backend/app/services/openai_service.py:520-593 (`_build_render_template_tool` — nested-object-with-required exemplar)
    - backend/app/services/openai_service.py:873-889 (`get_tools()` assembly — the SECOND wiring site)
    - backend/app/services/tool_dispatcher.py:2353-2382 (`_TOOL_REGISTRY` — the FIRST wiring site + the render_template G-5 comment)
    - backend/app/models/document_view.py:34-69 (`ViewCondition.op` / `unit` Literals — the enum-parity source)
    - .planning/phases/115-virtual-folders-agent-tool/115-RESEARCH.md:144-216 (the verbatim drop-in schema)
  </read_first>
  <files>backend/app/services/openai_service.py, backend/app/services/tool_dispatcher.py</files>
  <action>
    In `openai_service.py`, add the module-level constant `QUERY_DOCUMENTS_BY_VIEW_TOOL` (copy the verbatim schema from 115-RESEARCH.md:144-216) near `SEARCH_DOCUMENTS_TOOL`. Required exact properties: `function.name = "query_documents_by_view"` (final name per D-115-12); a description that (a) frames it as "list ALL documents matching a saved view or exact metadata criteria — complete, deterministic, newest-first, NO semantic ranking and NO search query string", (b) gives example prompts ("show me all X", "how many X", "open my Invoices view", "which docs expire within 90 days"), (c) explicitly contrasts it with `search_documents` (NL query + ranked top-K) and `query_documents` (free SQL) (D-115-5), and (d) states the either/or invariant in prose ("Provide EXACTLY ONE of `view` OR `filter`; provide NEITHER to get a catalog; never both"). `parameters` = an object with `view` (string, optional), `filter` (object, optional — nested `op:{enum:["and"]}` + `conditions:array` whose item `op.enum` EXACTLY equals the `ViewCondition.op` Literal values and a `unit.enum` of `["days","weeks","months"]`; `value` typed as the JSON-Schema type-array `["string","number","boolean","null"]`, NOT `anyOf`), and `limit` (integer, optional, "default 20, hard cap 50; TRUE total always reported"). NO top-level `required` (so catalog mode is reachable). NO `anyOf`/`oneOf` ANYWHERE. Add `QUERY_DOCUMENTS_BY_VIEW_TOOL` to the `get_tools()` assembly list (openai_service.py:882) with a `# Phase 115 (VIEW-07) — D-115-8: Deep-visible so the model actually SEES it (SC#1)` comment. In `tool_dispatcher.py`, add ONE line to `_TOOL_REGISTRY` (after the render_template line at :2381): `"query_documents_by_view": _handle_query_documents_by_view,` with a `# Phase 115 (VIEW-07) — registry + get_tools BOTH (the inverse of render_template); threads.py untouched (G-5)` comment. Do NOT touch `threads.py`, `agent_loop.py`, or any provider service. Un-xfail test_115_tool_wiring.py + test_115_tool_schema.py.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_115_tool_wiring.py tests/unit/test_115_tool_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"query_documents_by_view": _handle_query_documents_by_view' backend/app/services/tool_dispatcher.py` is 1 (registry wiring)
    - `grep -c "QUERY_DOCUMENTS_BY_VIEW_TOOL" backend/app/services/openai_service.py` is ≥ 2 (the constant definition + the get_tools entry)
    - `python -c "from app.services.openai_service import get_tools; assert any(t['function']['name']=='query_documents_by_view' for t in get_tools())"` passes (advertised — SC#1)
    - `python -c "import json; from app.services.openai_service import QUERY_DOCUMENTS_BY_VIEW_TOOL as t; s=json.dumps(t); assert 'anyOf' not in s and 'oneOf' not in s"` passes (Gemini-safe)
    - `tests/unit/test_115_tool_schema.py` passes: `view`/`filter` NOT in `parameters.required`; the inline-filter `op.enum` equals the `ViewCondition.op` Literal values exactly
    - `git diff --stat backend/app/api/threads.py` is empty (G-5 — threads.py byte-untouched)
  </acceptance_criteria>
  <done>The tool is dual-wired (registry + advertised schema), the schema is Gemini-safe (no anyOf/oneOf, optional fields, enum parity), and threads.py is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: SC#2 whitelist-guard unit test + complete the live two-user leak proof</name>
  <read_first>
    - backend/app/services/tool_dispatcher.py:2417-2439 (`dispatch_tool` + the `phase_whitelist` guard — SC#2 is free)
    - backend/tests/unit/test_tool_budget.py (the whitelist/budget test pattern to extend)
    - backend/tests/integration/test_113_view_global_leak.py:142-309 (the two-user live harness: `_seed_user`, `_seed_doc`, `_seed_global_view`, the per-viewer disjoint assertions, the zero-match + 404-not-403 cases)
    - backend/tests/integration/test_115_tool_global_leak.py (the Plan-01 scaffold to complete — drive the handler, not the route)
    - backend/app/services/tool_dispatcher.py:59-114 (`ToolContext` — build a stub ctx with `current_user`, `supabase`, `spawn`, `folder_subtree_ids=None`, `phase_whitelist`)
  </read_first>
  <files>backend/tests/unit/test_115_whitelist_guard.py, backend/tests/integration/test_115_tool_global_leak.py</files>
  <action>
    In `test_115_whitelist_guard.py` (SC#2): build a minimal `ToolContext` stub and assert via `dispatch_tool("query_documents_by_view", {}, ctx)` — (a) with `ctx.phase_whitelist = frozenset({"search_documents"})` (EXCLUDES the tool) → the returned `ToolResult.result` JSON has `error == "tool_not_available_in_phase"`; (b) with `ctx.phase_whitelist = frozenset({"query_documents_by_view"})` → it dispatches to the handler (catalog mode, since args={}); (c) with `ctx.phase_whitelist = None` (Deep) → it dispatches. This proves the tool is NOT special-cased around the guard (SC#2 free). In `test_115_tool_global_leak.py` (SC#2 leak-safety, the secure-phase proof): complete the Plan-01 scaffold by cloning the test_113 two-user harness verbatim but DRIVE `_handle_query_documents_by_view(args, ctx)` (NOT `resolve_view`) — seed two users + ONE `is_global=true` view (`field="document_type", value="invoice"`) + per-user matching/non-matching docs; build a per-caller ToolContext stub (`current_user={"id": str(user_a)}`, real `supabase`, `spawn=lambda c: None`, `folder_subtree_ids=None`, `phase_whitelist=None`); call the handler with `{"view": "Global Invoices"}` for each caller; parse `ToolResult.result` JSON and assert: A's `documents` ids ⟂ B's (disjoint), the `total`s differ, no filename leaks across callers, and an unknown view name `{"view":"NoSuchViewXYZ"}` returns `mode == "catalog"` (404-equivalent, never 403, never another user's data). Un-xfail both files. Keep the `:54322` skip-guard (skips cleanly when unreachable — secure-phase runs it live).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_115_whitelist_guard.py tests/integration/test_115_tool_global_leak.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_115_whitelist_guard.py` passes: excluded whitelist → `tool_not_available_in_phase`; included → dispatches; None → dispatches (SC#2 free, no special-casing)
    - `tests/integration/test_115_tool_global_leak.py` drives `_handle_query_documents_by_view` (NOT `resolve_view`) — `grep -c "_handle_query_documents_by_view" backend/tests/integration/test_115_tool_global_leak.py` is ≥ 1
    - The leak test asserts: disjoint id sets across the two callers, differing totals, no filename leak, and unknown-view → `mode:"catalog"` (never 403)
    - The leak test skip-guards on `:54322` unreachable (clones the test_113 `_pg_reachable` pattern) so it runs live in secure-phase
  </acceptance_criteria>
  <done>SC#2 is proven free (the guard refuses an excluded tool, dispatches otherwise) and the live two-user leak proof drives the actual agent handler — non-vacuous, ready to run live in secure-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → advertised tool schema | The schema is the only surface the model fills; a non-cross-provider-safe shape (anyOf/oneOf) breaks Gemini and mis-fills MiniMax |
| workflow phase → dispatch | A locked harness phase's `phase_whitelist` is the EoP boundary; the tool must obey it for free |
| agent path → resolve core | The tool handler is the leak boundary; the live two-user test (driving the handler) is the only non-vacuous proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03-01 | Information Disclosure | cross-user leak via the agent tool path | mitigate | The LIVE two-user leak test drives `_handle_query_documents_by_view` (not the route) → disjoint per-caller result sets proven on `:54322` in secure-phase; the RLS/caller-scope label is NOT proof (the "static would false-green" lesson) |
| T-115-03-02 | Elevation of Privilege | the tool callable in a workflow phase that didn't allow it | mitigate | `dispatch_tool` `phase_whitelist` guard refuses a non-whitelisted tool with a `tool_refused` audit (SC#2 free); the unit test proves the tool is NOT special-cased around the guard |
| T-115-03-03 | Tampering | a cross-provider-unsafe schema (anyOf/oneOf) breaking Gemini / worsening MiniMax | mitigate | The schema uses flat optional fields + JSON-Schema type-arrays, NO anyOf/oneOf (RESEARCH Pitfall 1); the unit schema test asserts their absence; the MiniMax `minimax-m3-invalid-tool-args-400` watch row is observed in secure-phase UAT (not fixed here — out of scope per D-115-13) |
| T-115-03-04 | Information Disclosure | "registered but not advertised" → the model never calls it (the inverse Phase-101 bug, a silent SC#1 miss) | mitigate | Dual-wiring asserted: `_TOOL_REGISTRY` AND `get_tools()` output both contain the tool; acceptance is the model ACTUALLY invoking it live (the SC#10 UAT in VALIDATION.md) |
| T-115-03-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH §"Package Legitimacy Audit": not applicable) |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit/test_115_*.py tests/integration/test_115_*.py -q` — the FULL phase suite green (skip live integration cleanly when :54322 unreachable)
- `python -c "from app.services.openai_service import get_tools; assert any(t['function']['name']=='query_documents_by_view' for t in get_tools())"` (SC#1 advertised)
- `python -c "import json; from app.services.openai_service import QUERY_DOCUMENTS_BY_VIEW_TOOL as t; assert 'anyOf' not in json.dumps(t) and 'oneOf' not in json.dumps(t)"` (Gemini-safe)
- `git diff --stat backend/app/api/threads.py` empty (G-5)
- No new SQL migration created (D-115-11)
</verification>

<success_criteria>
- The tool is registered AND advertised (SC#1 dual-wiring; the inverse-Phase-101 bug guarded)
- The schema is cross-provider-safe (no anyOf/oneOf; optional fields; op-enum parity with ViewCondition.op)
- SC#2 is free (the dispatch guard refuses an excluded tool; Deep dispatches)
- The live two-user leak proof drives the actual handler and is ready to run live in secure-phase
- threads.py byte-untouched; zero new migration; zero new audit enum
</success_criteria>

<output>
Create `.planning/phases/115-virtual-folders-agent-tool/115-03-SUMMARY.md` when done
</output>
