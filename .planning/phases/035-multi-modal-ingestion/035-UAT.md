---
status: complete
phase: 035-multi-modal-ingestion
source: 035-01-SUMMARY.md, 035-02-SUMMARY.md, 035-03-SUMMARY.md
started: 2026-04-18T00:00:00Z
updated: 2026-04-18T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running backend server. Start the backend fresh (venv active, uvicorn or equivalent). Server boots without errors. The /api/health endpoint (or any basic API call) returns a live response. No import errors related to multimodal_service or pdfplumber.
result: pass

### 2. DB Migrations Applied
expected: The tables `document_tables` and `document_images` exist in your Supabase database. You can verify in the Supabase dashboard → Table Editor, or run `SELECT * FROM document_tables LIMIT 1` in the SQL Editor — it should return empty (not error).
result: pass

### 3. PDF Table Extraction
expected: Upload a PDF that contains at least one table (any PDF with grid-style data). After ingestion completes, query `SELECT * FROM document_tables WHERE document_id = '<id>'` in Supabase — you should see rows with headers and rows populated as JSON.
result: pass

### 4. DOCX Table Extraction
expected: Upload a .docx file that contains at least one table. After ingestion, query `document_tables` for that document — rows appear with `page = NULL` (DOCX has no page boundaries) and headers/rows populated.
result: pass

### 5. PDF Image Extraction + Vision Description
expected: Upload a PDF that contains at least one embedded image larger than 50×50 px. After ingestion, query `SELECT * FROM document_images WHERE document_id = '<id>'` — rows appear with a non-empty `description` text (the OpenAI vision LLM's description of the image).
result: issue
reported: "No rows returned in document_images despite PDF containing embedded images (confirmed Figure 2.3 visible in PDF)"
severity: major

### 6. Small Images Are Skipped
expected: Upload a document with only tiny/icon-sized images (under 50px in either dimension). After ingestion, `document_images` has no rows for that document — small images are silently skipped.
result: skipped
reason: Cannot meaningfully test — image extraction pipeline broken (Test 5 issue); zero images extract regardless of size

### 7. Realtime Status Events During Ingestion
expected: While uploading a document and watching the ingestion status in the UI, you should see the status progress through `extracting_tables` and then `extracting_images` stages before completing. (Requires watching the UI during upload, not just checking after.)
result: pass

### 8. Graceful Failure — Ingestion Continues on Extraction Error
expected: Even if table or image extraction fails internally (e.g., corrupt PDF, unsupported format), the document still gets ingested — it appears in the document list with status `complete` or similar, not stuck or errored. Extraction failures are silent (don't abort ingestion).
result: pass

## Summary

total: 8
passed: 6
issues: 1
pending: 0
skipped: 1
blocked: 0

## Gaps

- truth: "PDF images are extracted, described via vision LLM, and stored in document_images"
  status: failed
  reason: "User reported: No rows returned in document_images despite PDF containing embedded images (confirmed Figure 2.3 visible in PDF)"
  severity: major
  test: 5
  root_cause: "extract_pdf_images uses img_obj.get('stream', b'') but pdfplumber image dicts do not contain raw bytes at the 'stream' key — either the key is absent (returns b'', skipped) or it is a pdfminer PDFStream object (io.BytesIO call fails, caught silently). No images ever pass through."
  artifacts:
    - path: "backend/app/services/multimodal_service.py"
      issue: "Line 142: img_bytes = img_obj.get('stream', b'') never yields usable image bytes from pdfplumber image dicts"
  missing:
    - "Fix extract_pdf_images to decode image bytes from pdfminer PDFStream (call stream.get_data()) or use pdfplumber page.crop() to render image region as PNG"
  debug_session: ""
