---
phase: 242-ship-it-and-prove-what-already-shipped
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/pages/SettingsPage.test.tsx
  - frontend/src/pages/__tests__/SettingsPage.changedFields.test.tsx
  - backend/app/api/settings.py
  - backend/tests/unit/test_242_stored_value_refusal.py
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [SHIP-01]

must_haves:
  truths:
    - "D-242-02: the Search tab sends CHANGED FIELDS ONLY. Not 'report every failing field' — that makes the failure legible and leaves it possible. A value the operator never typed must become UNABLE to block the save, not merely easier to diagnose."
    - "D-242-02's deciding second reason: `hnsw_ef_search` and `hnsw_iterative_scan` ride this same payload (Phase 241 D-09). Under today's shape Phase 246's remedy is hostage to ANY unrelated stored value drifting out of range. Changed-fields-only unblocks 246 permanently — that is why this is the shape, not merely the nicer one."
    - "⛔ THE DANGEROUS DIRECTION IS SILENT DROP, NOT SPURIOUS SEND. If the baseline disagrees with what `hydrate` actually put in the fields, a REAL edit can look unchanged and never be sent — a save that reports success and changes nothing, which is Phase 240's 'screen that discards its own answer'. The fence against that is §1: load settings, press Save with NO edits, and assert the body is `{}` — one case that checks every field's baseline at once."
    - "The API-key fields are baselined as `KEY_PLACEHOLDER` on BOTH sides, so an untouched key is dropped rather than sent as '***'. `save_app_settings` already skips `KEY_PLACEHOLDER` (`user_settings.py:542`), so behaviour at the database is byte-identical either way — the change is that the key stops riding the payload at all."
    - "D-242-03: a refusal names the STORED value as the cause when the refused value is the one already in the database — `'Images read per document' was already set to 1001 …  nothing you just changed is at fault` — and keeps today's sentence when the operator genuinely typed an out-of-range number."
    - "D-242-03 is applied at ALL FOUR numeric bound sites through one helper, not only at `multimodal_max_vision_calls`. SC#3's 'the class fix, not the instance' applies to the sentence as much as to the constraint."
    - "⛔ The stored-value lookup must NEVER be able to turn a 400 into a 500: the helper wraps `load_app_settings_async()` in try/except and falls back to today's sentence. A nicer error message that can crash is worse than a blunt one."
    - "⚠ SC#1 is verified by reading the NETWORK RESPONSE, never the banner. `241-HUMAN-UAT.md` row 3 recorded that a red banner is exactly what a PASSING refusal looks like — 'presence of an error is not evidence of the RIGHT error'. Every frontend case here asserts the `updateSettings` argument or the thrown detail, never a rendered colour."
    - "The new suite is registered in BOTH `TARGETS` and `BASELINE` in the SAME commit — a `BASELINE` key naming a file that does not exist makes the gate exit 2 and halts every agent."
    - "⭐ Two EXISTING SettingsPage suites — `__tests__/SettingsPage.a11y.test.tsx` and `__tests__/SettingsPage.sourceCeiling.test.tsx` — are in NEITHER knob and have never been gated. `src/pages` is not a directory entry in TARGETS. They are ADOPTED in this commit. Adoption raises the total; that is the desirable direction and is not drift."
    - "G-5 fires on `settings.py` (36/19/854) and `SettingsPage.tsx` (45/23/1751) — both honoured BY CONSTRUCTION (a payload shape and a sentence; no new surface). Row AND section updated in the SAME commit, and the triples RE-DERIVED, never copied from CONTEXT."
  artifacts:
    - path: "frontend/src/pages/__tests__/SettingsPage.changedFields.test.tsx"
      provides: "The payload-shape fence, including the silent-drop guard"
      contains: "toHaveBeenCalledWith({})"
    - path: "backend/tests/unit/test_242_stored_value_refusal.py"
      provides: "The refusal-sentence fence for all four bounded fields"
      contains: "was already set to"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "Three SettingsPage suites in both knobs — one new, two adopted"
      contains: "SettingsPage.changedFields.test.tsx"
  key_links:
    - from: "frontend/src/pages/SettingsPage.tsx"
      to: "frontend/src/pages/SettingsPage.tsx"
      via: "the hydrate-time baseline consumed by handleSaveSearch"
      pattern: "searchBaseline"
    - from: "backend/app/api/settings.py"
      to: "backend/app/models/user_settings.py"
      via: "load_app_settings_async() read inside the refusal helper, wrapped so it can never 500"
      pattern: "load_app_settings_async"
---

<objective>
Make it impossible for a field the operator never touched to take the whole Search tab down — and
make the one refusal that survives say what is actually wrong.

Purpose: `SettingsPage.tsx:873-899` sends **all 22 Search fields unconditionally** on every save. So
one stored value out of range refuses the entire tab, including the reranker, the embedding model,
the retrieval threshold, `rrf_k` — and, since Phase 241 D-09, `hnsw_ef_search`, which is the knob
Phase 246's whole remedy is delivered on. The operator sees *"Images read per document must be
between 1 and 1000"* on a **Search** tab where they changed a threshold, pointing at a field they
never edited, on a value they never typed.

Two changes, in that causal order: the payload stops carrying what did not change, and the refusal
that remains reachable names the stored value as the cause.

Output: a diffed payload with a silent-drop fence over it; one shared refusal helper at all four
bound sites; three SettingsPage suites gated where one was.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/242-ship-it-and-prove-what-already-shipped/242-CONTEXT.md
@.planning/phases/242-ship-it-and-prove-what-already-shipped/242-BASELINE.md
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
<!-- Measured at HEAD on 2026-09-11. The executor should not re-discover these. -->

**`frontend/src/pages/SettingsPage.tsx:872-899` — the payload literal, verbatim keys, in order:**

`embedding_model` · `embedding_api_key` (`embeddingApiKey || KEY_PLACEHOLDER`) · `embedding_base_url` ·
`embedding_dimensions` · `embedding_provider` · `extraction_provider` · `extraction_model` ·
`rerank_enabled` · `rerank_provider` · `rerank_api_key` (`|| KEY_PLACEHOLDER`) · `rerank_model` ·
`rerank_top_n` · `multimodal_max_vision_calls` · `vision_model` · `vision_max_pages` ·
`retrieval_top_k` · `retrieval_match_threshold` · `hybrid_search_enabled` ·
`hybrid_candidate_count` · `vector_search_weight` · `keyword_search_weight` · `rrf_k` ·
`hnsw_ef_search` · `hnsw_iterative_scan`  — **24 keys.**

**`hydrate(data: FullAppSettings)` at `:718-796` — the fallbacks that make a naive diff unsafe.**
These state values are NOT raw column values:

| state | hydrate expression |
|---|---|
| `embeddingApiKey` / `rerankApiKey` | `data.*_has_api_key ? KEY_PLACEHOLDER : ""` |
| `embeddingProvider` | `data.embedding_provider \|\| "openai"` |
| `extractionProvider` | `data.extraction_provider \|\| "openai"` |
| `extractionModel` | `data.extraction_model \|\| exPreset?.model \|\| ""` |
| `visionModel` | `data.vision_model ?? ""` |
| `visionMaxPages` | `data.vision_max_pages ?? 50` |
| `hnswEfSearch` | `data.hnsw_ef_search ?? 40` |
| `hnswIterativeScan` | `data.hnsw_iterative_scan ?? "off"` |

⛔ **A diff against the RAW `s` would therefore mark `vision_model` "changed" on a database holding
NULL, every single save.** The baseline must be the HYDRATED shape, which is why it is captured
inside `hydrate` rather than derived from `s` at save time.

**`KEY_PLACEHOLDER = "***"` (`:58`) means "keep existing"** — `save_app_settings`
(`backend/app/models/user_settings.py:542`) `continue`s on it. So baselining both key fields as
`KEY_PLACEHOLDER` and dropping them when unchanged is **behaviour-preserving at the database**.

**The confirm-on-save gate at `:903-921`** computes `modelChanged` / `dimsChanged` against `s`
independently of the payload and stores the body in `pendingSearchSave`. It is unaffected: when
either changed, that key is in the diffed body by definition.

**The four bound sites in `backend/app/api/settings.py`** (all four get the D-242-03 sentence):

| line | field | label for the sentence | bound |
|---|---|---|---|
| 466-475 | `multimodal_max_vision_calls` | *Images read per document* | `1 .. 1000` |
| 491-500 | `vision_max_pages` | *Pages read per document* | `1 .. 500` |
| 514-532 | `source_max_file_size_mb` | *Largest file a connected source may import* | `SOURCE_MAX_FILE_SIZE_MB_FLOOR .. _CEILING` |
| 601-616 | `hnsw_ef_search` | *Search breadth* | `HNSW_EF_SEARCH_FLOOR .. _CEILING` |

⭐ **Each existing `detail` string is GOOD and must survive as the typed-value branch.** The SEED-227,
SEED-226, SEED-258 and Phase 241 comment blocks explain what each bound buys; none of them is
deleted or reworded. The helper adds a SECOND sentence for a different cause, it does not replace
the first.

`load_app_settings_async` is already imported in `settings.py` (used at `:692`). No new import.

**The gate knobs** (`scripts/vitest-count-gate.cjs`): `BASELINE` is keyed by BARE FILENAME
(`"SettingsPage.test.tsx": 23` at `:1495`), `TARGETS` by PATH
(`"src/pages/SettingsPage.test.tsx"` at `:3978`). ⚠ **Bare-name collisions are real** — the comment
at `:3977` already warns about the `SettingsPage.a11y.test.tsx` sibling. The three names here
(`SettingsPage.test.tsx`, `SettingsPage.a11y.test.tsx`, `SettingsPage.sourceCeiling.test.tsx`,
`SettingsPage.changedFields.test.tsx`) are all distinct.
</interfaces>
</context>

<tasks>

<!-- ===========================================================================================
     PLAN REVISION 2026-09-11 - the plan-checker's findings, accepted and folded in BEFORE
     execution. The original task text below stands; these SUPERSEDE it where they conflict, and
     each conflict is named so the change is auditable rather than silent.
     ===========================================================================================

  R-1 (checker B-2) [BLOCKER] `frontend/src/pages/SettingsPage.test.tsx:454-465` WILL GO RED, and
      the original plan promised it would stay green. That case - Phase 241 D-09's guarantee that
      "the two knobs ride the payload this tab already sends" - edits `hnsw_iterative_scan` ONLY
      and then asserts the body has `hnsw_ef_search: 40`, an UNCHANGED field. After D-242-02 that
      field is dropped. The file is pinned at 23 (`vitest-count-gate.cjs:1495`) and gated
      (`:3978`), so the count gate reds with it - on a phase whose BASELINE is GREEN, which is
      exactly when a real failure gets misread as SEED-171 flake.
      DO NOT delete the assertion - that erases 241's guarantee silently. REPLACE it with the
      post-242 contract: an EDITED `hnsw_ef_search` rides, an UNTOUCHED one does not, with a
      comment stating that Phase 242 changed the contract and HOW 241's intent survives it.
      The file is now in `files_modified`.

  R-2 (checker B-3) [BLOCKER] THE SILENT-DROP HOLE SS1 WAS MEANT TO CLOSE, AND DOES NOT WITH ONE
      FIXTURE. SS1's all-NULL fixture cannot tell a DATA-DERIVED baseline from a HARD-CODED one.
      Worked example, on the field Phase 246 depends on: if `searchPayloadFrom` is written
      `hnsw_ef_search: 40` (a constant, matching the `useState(40)` at `:681`) instead of
      `data.hnsw_ef_search ?? 40`, then with the fixture's NULL the state is 40, the baseline is
      40, the body is `{}` and SS1 IS GREEN - while in production, where the column holds 200, an
      operator dragging it 200 -> 40 produces `Object.is(40, 40)` and the key is DROPPED. The save
      reports success and the database still holds 200. That is Phase 240's "screen that discards
      its own answer".
      The same blindness covers every `??` / `||` field: `hnsw_iterative_scan` (:774),
      `vision_max_pages` (:752), `vision_model` (:751), `extraction_model` (:742, which also needs
      the EXTRACTION_PRESETS lookup replicated), `embedding_provider` (:734),
      `extraction_provider` (:735-736). And no case in the original plan edits a `??`-fallback
      field at all - SS5 and SS2 both edit raw passthroughs.
      THREE FIXES, all taken:
        (a) SS1 runs against TWO fixtures - the all-NULL one AND one where every
            nullable/defaulted column holds a distinctive NON-DEFAULT value. The second fixture is
            the only thing that separates "mirrors hydrate" from "returns the constants hydrate's
            fallbacks happen to produce".
        (b) NEW SS2b - stored `hnsw_ef_search: 200`, operator edits it to the default `40`, assert
            the body is exactly `{ hnsw_ef_search: 40 }`. This is the worked example as a case,
            and it is also R-1's replacement assertion.
        (c) EXPORT `searchPayloadFrom` and `onlyChanged` and unit-test them directly against both
            fixtures - cheaper and stronger than driving the DOM, and it is what makes SS7
            implementable at all (see R-6).

  R-3 (checker W-9) `onlyChanged` iterates `Object.keys(next)`, SPECIFIED not left to the
      implementer. Iterating the BASELINE instead would silently drop forever any key present in
      `full` and absent from `searchPayloadFrom`. A case pins it: a key in `full` with no baseline
      entry must be SENT.

  R-4 (checker W-7) THE `Object.is` RATIONALE IN THE ORIGINAL PLAN IS FACTUALLY WRONG and must not
      ship in a source comment. `Object.is(NaN, NaN)` is true only if the BASELINE is also `NaN` -
      and the baseline comes from `searchPayloadFrom(data)`, always a real number. So
      `Object.is(NaN, 100)` is false and a transient `NaN` IS sent, exactly as with `===`. The
      only real difference is `+0` / `-0`, where `Object.is` sends and `===` drops. Keep
      `Object.is` (it errs toward SENDING, the safe direction) and write the true reason.

  R-5 (checker W-5) SS5's `hnsw_ef_search` case can be SHORT-CIRCUITED before its bound is
      reached: `settings.py:577-600`'s CR-01 gate calls `app_settings_has_hnsw_columns()` and,
      when False, sets `body.hnsw_ef_search = None` so `:602` never runs. The backend case must
      pin that gate True (monkeypatch) or it passes for the wrong reason.

  R-6 (checker W-8) SS7 as written is NOT DRIVABLE. Making `getSettings` reject leaves `s === null`
      and `SettingsPage.tsx:807-817` renders a full-page error branch - there is no Save button and
      `handleSaveSearch` is unreachable. SS7 becomes a UNIT assertion on the exported `onlyChanged`
      with a null baseline (R-2c).

  R-7 (checker W-6) `settings.py:572-573` says "The frontend sends both keys UNCONDITIONALLY
      (SettingsPage.tsx), so 'the operator did not touch them' is indistinguishable here from 'the
      operator set them'". D-242-02 MAKES THAT FALSE. The original plan's "not one comment block is
      deleted or reworded" would ship exactly the prose-rot CLAUDE.md pays for repeatedly. CARVE
      OUT THIS ONE SENTENCE: correct it BESIDE the original (strike-through convention), noting the
      gate's stored-comparison is now belt-and-braces rather than load-bearing. Every other comment
      block - SEED-226, SEED-227, SEED-258, the Phase 241 cost sentence - stays untouched.

  R-8 (checker W-10) ADD a backend case: `PUT /settings` with an EMPTY body returns 200 and writes
      nothing. `user_settings.py`'s `if not clean: return True` makes this safe today - but "I
      checked once" is not a fence, and `{}` becomes the MOST COMMON request shape on this tab
      after this change.

  R-9 (checker W-11) A REAL BEHAVIOUR CHANGE AT THE DATABASE, to be NAMED rather than discovered
      later: today pressing Save MATERIALISES hydrate's fallbacks into columns - `extraction_model`
      (preset-derived), `vision_model` "", `hnsw_ef_search` 40, `hnsw_iterative_scan` "off",
      `vision_max_pages` 50. After the diff, a NULL column STAYS NULL. Resolution is equivalent
      (`_val()` falls back) and it is arguably the better behaviour - but the plan claimed
      database-equivalence only for the API-key fields. State it in the SUMMARY.

  R-10 (checker W-12) Task 4's verify gates exit 0 on COPIED OR INVENTED triples. Paste the three
      derivation commands AND their raw output into the SUMMARY. This ledger has eleven recorded
      rots; a mandate with no evidence is how each one happened.

  R-11 (checker W-4) `depends_on: [01]` IS NOT a file dependency - the two plans' `files_modified`
      are disjoint and 02 consumes no artifact of 01. THE REAL REASON IS CLAUDE.md parallel rule 4:
      242-01 Task 3 WRITES TO THE LOCAL DATABASE, and worktrees isolate files, not Postgres.
      AND A CONSEQUENCE FOR THIS PLAN: after 178 lands, the local database CANNOT HOLD 1001. Every
      backend case here must MONKEYPATCH `load_app_settings_async`, never seed a row.

  R-12 (checker W-13) SHIP-02 / SHIP-03 / SHIP-04 appear in no plan's `requirements:` field because
      SC#4 and SC#5 are G-3 fast tasks. Record the IDs against those tasks in the phase record so
      the milestone close can find them.
-->


<task type="auto" tdd="true">
  <name>Task 1: The payload fence — RED against today's unconditional body</name>
  <files>frontend/src/pages/__tests__/SettingsPage.changedFields.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/pages/__tests__/SettingsPage.sourceCeiling.test.tsx:1-140` — the api-mock idiom, the `EffectiveFeaturesProvider` + `TechnicalNamesProvider` render wrapper, the `mkSettings` fixture builder, and `:227-260` where it drives a Save and asserts the `updateSettings` argument. **This suite is the direct template.**
    - `frontend/src/pages/SettingsPage.tsx:718-796` (hydrate) and `:872-921` (the payload + the confirm gate).
    - `scripts/vitest-count-gate.cjs:1490-1500` and `:3970-3985` — the two knobs and the bare-name-collision comment.
  </read_first>
  <behavior>
    A new suite `frontend/src/pages/__tests__/SettingsPage.changedFields.test.tsx`. Its docblock
    states D-242-02 in two sentences, and states the silent-drop risk §1 exists to catch.

    ⛔ **Every case asserts the ARGUMENT `updateSettings` was called with — the wire payload — never
    a banner, a colour or a toast.** `241-HUMAN-UAT.md` row 3: *"presence of an error is not evidence
    of the RIGHT error."*

    §1 — **the silent-drop fence, and the most important case in this plan.**
    Load settings, switch to the Search tab, press Save with **no edits at all**, and assert
    `mockUpdateSettings` was called with exactly `{}`. ⭐ One assertion that checks all 24 baselines
    against all 24 hydrated states simultaneously: any field whose baseline disagrees with what
    `hydrate` actually set appears as a stray key and reds here.
    Run this against a fixture whose nullable columns are **NULL** (`vision_model: null`,
    `hnsw_ef_search: null`, `hnsw_iterative_scan: null`, `extraction_model: null`,
    `extraction_provider: null`) — the exact shape a naive `s`-diff gets wrong.

    §2 — **one edit, one key.** Change `rrf_k` only; assert the body is exactly `{ rrf_k: <new> }` —
    an equality on the whole object, not a `toMatchObject`. ⚠ A superset assertion would pass over
    the bug this plan fixes.

    §3 — **the field that caused the outage can no longer ride along.** Fixture holds
    `multimodal_max_vision_calls: 1001` (the operator's real local value). Change `retrieval_top_k`
    only; assert `multimodal_max_vision_calls` is **absent** from the body and the call resolves.
    ⭐ This is SHIP-01 in one case: the save the operator could not perform.

    §4 — **an untouched API key does not ride.** With `embedding_has_api_key: true`, a save that
    changes only `rerank_top_n` carries neither `embedding_api_key` nor `rerank_api_key`. Its
    mirror: typing a real key into the embedding field DOES carry `embedding_api_key` with that
    value.

    §5 — **the offending field, edited.** Change `multimodal_max_vision_calls` to `2000`; assert it
    IS in the body (the operator asked for it, so it must travel and be refused by the API).

    §6 — **the re-embed confirm gate still sees its change.** Change `embedding_model`; assert the
    modal opens and `updateSettings` has NOT yet been called; confirm it; assert the committed body
    contains `embedding_model`. ⚠ The gate reads `s`, not the baseline — this case proves the two
    did not get crossed.

    §7 — **a no-baseline save is not a silent no-op.** If `searchBaseline` is null (settings never
    loaded), the full payload is sent. Drive it by making `getSettings` reject, or assert the
    fallback branch directly; the point is that the diff never turns "we don't know" into "nothing
    changed".
  </behavior>
  <action>
    1. Write the suite.
    2. Register it in **both** knobs in the same commit: `TARGETS` gets
       `"src/pages/__tests__/SettingsPage.changedFields.test.tsx"`; `BASELINE` gets
       `"SettingsPage.changedFields.test.tsx": <N>` where **N is read from the gate's own `— N new`
       column**, never hand-counted.
    3. ⭐ **Adopt the two orphans in the same edit**, with a comment naming the finding: add
       `"src/pages/__tests__/SettingsPage.a11y.test.tsx"` and
       `"src/pages/__tests__/SettingsPage.sourceCeiling.test.tsx"` to `TARGETS` and their measured
       counts to `BASELINE`. They exist, they pass, and they have been gated by nothing —
       `src/pages` is not a directory entry.
    4. **Drive RED**: run the suite against the UNCHANGED `SettingsPage.tsx`. §1, §2, §3 and §4 must
       fail with the full 24-key body. Record the failing case names and the stray-key count.
  </action>
  <verify>
    `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/__tests__/SettingsPage.changedFields.test.tsx`
    — §1-§4 red before Task 2. Capture the failing SET before re-running anything (SEED-171 rule).
  </verify>
</task>

<task type="auto">
  <name>Task 2: Changed-fields-only, with the baseline captured where hydrate already runs</name>
  <files>frontend/src/pages/SettingsPage.tsx</files>
  <read_first>
    - `frontend/src/pages/SettingsPage.tsx:718-796` — every hydrate fallback, which the baseline must mirror EXACTLY.
    - `242-CONTEXT.md` D-242-02 — the decision and the rejected alternative.
  </read_first>
  <behavior>
    Three additions, all inside this one file.

    **(a) A module-level pure function** `searchPayloadFrom(data: FullAppSettings): SettingsUpdate`
    returning the 24 keys **using the identical expressions `hydrate` uses**, with the two API-key
    fields set to `KEY_PLACEHOLDER`. Its comment says: *"this must mirror `hydrate` exactly; §1 of
    `SettingsPage.changedFields.test.tsx` is the fence that proves it does, and it fails loudly if
    the two drift."*

    **(b) One new state**, `searchBaseline`, set inside `hydrate` from that function. `hydrate` is
    already called on load AND after every successful save, so the baseline re-arms for free — a
    second save of the same edit correctly sends `{}`.

    **(c) In `handleSaveSearch`**: build today's literal unchanged as `full`, then

    ```ts
    // Phase 242 (D-242-02) — send only what CHANGED. A field the operator never touched can no
    // longer refuse the whole tab, and Phase 246's hnsw_* remedy stops being hostage to an
    // unrelated stored value drifting out of range.
    // ⚠ `searchBaseline` null means "we never loaded settings" — send everything, because
    //    "we don't know" must never be collapsed into "nothing changed".
    const body: SettingsUpdate = searchBaseline ? onlyChanged(full, searchBaseline) : full
    ```

    with a tiny module-level `onlyChanged(next, baseline)` keeping keys where
    `!Object.is(next[k], baseline[k])`. ⚠ **`Object.is`, not `===`** — `NaN` from an emptied number
    input must compare equal to itself so a transient `NaN` is not sent as a change.

    ⛔ **Nothing else moves.** The confirm-on-save gate keeps reading `s`. The `full` literal keeps
    all 24 keys and their existing expressions. No field is renamed, no default is changed.
  </behavior>
  <verify>
    The new suite is green including §1 with the all-NULL fixture. `SettingsPage.test.tsx` (23
    pinned) and the two newly-adopted suites are green and did not decrease.
    `npx tsc -p tsconfig.app.json --noEmit` — **set diff against the 67 base errors is empty both
    ways** (`tsconfig.json` type-checks ZERO files; never quote it).
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The refusal names the stored value — one helper, four sites, RED first</name>
  <files>backend/tests/unit/test_242_stored_value_refusal.py, backend/app/api/settings.py</files>
  <read_first>
    - `backend/app/api/settings.py:461-475`, `:487-500`, `:505-533`, `:595-616` — the four bound sites and the four comment blocks that must survive.
    - `backend/tests/unit/test_241_cr01_settings_write_without_migration.py:1-60` — the docblock convention: state what the test catches and why it was invisible.
    - `242-CONTEXT.md` D-242-03 — the sentence's shape (not its final copy).
  </read_first>
  <behavior>
    **The fence first.** `backend/tests/unit/test_242_stored_value_refusal.py`:

    §1 — the helper exists and is a single shared seam (`hasattr` on the module). ⛔ RED before the fix.
    §2 — **stored == submitted, out of range** → the detail contains the stored number, the phrase
      *"was already set to"*, the field's human label, and the phrase *"nothing you just changed"*.
      Assert on CONTENT, not on `status_code == 400` alone — a 400 with the wrong sentence is exactly
      the failure `241-HUMAN-UAT.md` row 3 warns about.
    §3 — **stored != submitted, out of range** → the detail is byte-identical to today's sentence.
      ⭐ The negative control: the new branch must not swallow the old one.
    §4 — **the lookup cannot 500.** Monkeypatch `load_app_settings_async` to raise; assert the helper
      still returns today's sentence and the endpoint still refuses with 400.
    §5 — **all four fields**, parameterised: each of `multimodal_max_vision_calls`,
      `vision_max_pages`, `source_max_file_size_mb`, `hnsw_ef_search` produces a stored-value
      sentence naming its own label. ⚠ An assertion that only covers the first field would let the
      class fix ship as an instance fix.

    **Then the fix.** One module-level async helper in `settings.py`, placed above
    `update_settings`:

    ```python
    async def _range_refusal_detail(
        *, field: str, label: str, submitted: int, lo, hi, typed_detail: str
    ) -> str:
        """D-242-03 — when the value being refused is the one ALREADY STORED, say so.

        Today's sentence is truthful about the RULE and actively misleading about the CAUSE: it
        reads as a rejection of what the operator just typed, on a field they may never have
        opened. ⚠ After D-242-02 the untouched-field path is unreachable from the UI — but it is
        still reachable when the operator edits the offending field itself, and by any other client.

        ⛔ This must never turn a 400 into a 500: a failed lookup falls back to `typed_detail`.
        """
    ```

    It loads `load_app_settings_async()` inside `try/except Exception`, compares
    `getattr(stored, field, None)` to `submitted`, and returns either the stored-value sentence or
    `typed_detail` unchanged. Each of the four sites becomes
    `raise HTTPException(status_code=400, detail=await _range_refusal_detail(...))` with its
    existing string passed in as `typed_detail`.

    ⛔ **Not one comment block is deleted or reworded.** SEED-226, SEED-227, SEED-258 and the
    Phase 241 cost sentence all stay exactly as they are.
  </behavior>
  <action>
    Drive §1-§5 RED against unmodified `settings.py` and record the failing names, THEN implement.
  </action>
  <verify>
    `backend/venv/Scripts/python.exe -m pytest tests/unit/test_242_stored_value_refusal.py -q` from
    `backend/` — green. Then the full `pytest tests/unit -q --continue-on-collection-errors`: the
    failing SET is diffed against `242-BASELINE.md`'s 71. ⛔ **Zero headroom — 72 breaks the gate.**
  </verify>
</task>

<task type="auto">
  <name>Task 4: The two ledger rows, and their sections, in this commit</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` → the hot-file ledger table rows for `backend/app/api/settings.py` and `frontend/src/pages/SettingsPage.tsx`.
    - `docs/HOT-FILE-LEDGER.md` → both files' sections.
    - `CLAUDE.md` → the ledger's own rule: the disposition cell is capped at **200 chars**; reasons go in the detail file's section, in the SAME commit.
  </read_first>
  <behavior>
    **RE-DERIVE both triples with the recipe** (commits / phases / lines, six-digit dated-quick-task
    buckets subtracted) — never copy CONTEXT's figures, and never copy the row's own. Both rows read
    stale at plan time (`35/19/814` and `44/23/1738`); the measured values at plan authoring were
    `36/19/854` and `45/23/1751`, and this plan's own commits move them again.

    Update the CLAUDE.md row to the post-commit triple with a ≤200-char disposition naming
    *honoured by construction (242)*; put the reasons — what changed, why a payload diff is not new
    surface, and that the `settings.py` tab/bound seam and the `SettingsPage.tsx` tab seam both stay
    **OWED** — in each file's section in `docs/HOT-FILE-LEDGER.md`.

    ⚠ `backend/app/models/user_settings.py` is named by `242-CONTEXT.md` as firing G-5 — **this plan
    does not modify it**, so its row is not touched. Say so in the SUMMARY rather than leaving it
    ambiguous.
  </behavior>
  <verify>
    `node scripts/check-claude-md-size.cjs` exits 0 (no `[disposition-too-long]`, no
    `[duplicate-row]`, no `[malformed-row]`).
    `node scripts/check-hot-file-ledger.cjs 242` exits 0 — every non-test source file in this
    phase's `files_modified` has a row.
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `PUT /settings` | The payload shape decides which stored values can refuse a save |
| API refusal → operator | A refusal that misattributes the cause sends the operator to the wrong field |
| refusal helper → database | A settings read on the error path can itself fail |
| plan → `scripts/vitest-count-gate.cjs` | The shared gate for every frontend agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-02-01 | Tampering | the Search tab's payload | mitigate | A stale/incorrect baseline could DROP a real edit — a save that reports success and changes nothing. §1 (no edits ⇒ `{}`) plus §2 (one edit ⇒ exactly one key) fence both directions, and §7 keeps the unknown-baseline case sending everything. |
| T-242-02-02 | Information disclosure | API keys in the payload | mitigate | Untouched keys stop being transmitted at all (baselined as `KEY_PLACEHOLDER`, dropped by the diff). Strictly less secret material on the wire than today. §4 pins both directions. |
| T-242-02-03 | Denial of service | `PUT /settings` error path | mitigate | `_range_refusal_detail` wraps the settings load in `try/except` and falls back to today's sentence; §4 drives it with a raising monkeypatch. A 400 can never become a 500 via this path. |
| T-242-02-04 | Repudiation | the refusal sentence | mitigate | §2 asserts CONTENT (the stored number, the label, *"nothing you just changed"*) and §3 asserts the typed-value sentence is byte-identical — a 400 with the wrong sentence fails. |
| T-242-02-05 | Elevation of privilege | `require_visible("model_management")` | accept | Untouched. The diff happens client-side and the endpoint's gate, its 422 provider check and `save_app_settings`'s `_VALID_COLUMN_NAME` guard are all unchanged. A client that sends everything anyway is refused exactly as today — **the server-side bound is NOT weakened by the client sending less.** |
| T-242-02-06 | Denial of service | `scripts/vitest-count-gate.cjs` | mitigate | Suite and both knob entries land in ONE commit; `TARGETS` entries are appended, never substituted. |
| T-242-02-SC | Tampering | package installs | mitigate | **No package is installed.** `vitest`, `@testing-library/react`, `userEvent`, `pytest`, `pytest-asyncio` all ship. A task that believes it needs a package STOPS and reports. |
</threat_model>

<verification>
- The frontend fence was seen RED against the unconditional payload (§1-§4), with the stray-key count recorded, and is green after Task 2.
- The backend fence was seen RED (§1-§5) before the helper existed.
- Backend unit suite: failing SET diffed against `242-BASELINE.md`. ⛔ **71 is a ceiling with zero headroom.**
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root: no per-file decrease; the failing SET diffed against `242-BASELINE.md`, never a colour. ⚠ `sourceComposition.test.tsx` is a KNOWN standing red in neither knob (Phase 235 decision) — if it appears, it is inherited.
- `npx tsc -p tsconfig.app.json --noEmit`: set diff against base **empty both ways**. ⛔ Never `npx tsc --noEmit` — `tsconfig.json` is solution-style and checks zero files.
- `node scripts/check-claude-md-size.cjs` and `node scripts/check-hot-file-ledger.cjs 242` both exit 0.
- `git diff -- backend/app/api/settings.py` shows the four `detail=` expressions changed and the helper added — and **no comment block deleted** (`grep -c "SEED-227\|SEED-226\|SEED-258"` unchanged).
</verification>

<success_criteria>
An operator who changes one Search field sends one field — so a value they never typed cannot refuse
the save — and the refusal that remains reachable tells them the value was already stored that way.
</success_criteria>

<output>
Create `.planning/phases/242-ship-it-and-prove-what-already-shipped/242-02-SUMMARY.md` when done.
</output>
