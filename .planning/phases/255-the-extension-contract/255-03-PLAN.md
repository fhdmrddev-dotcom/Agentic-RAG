---
phase: 255-the-extension-contract
plan: 03
type: execute
wave: 3
depends_on: ["255-01", "255-02"]
autonomous: true
requirements: [EXT-01, EXT-02, EXT-03]
gap_closure: false

files_modified:
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .planning/seeds/SEED-291-the-extension-contract-plugin-is-data-process-or-sandbox-never-engine.md
  - .planning/STATE.md
  - .planning/ROADMAP.md

user_setup: []

must_haves:
  truths:
    - "`node scripts/check-hot-file-ledger.cjs 255` exits 0 with all hot files accounted for and zero missing rows."
    - "`node scripts/check-seeds-register.cjs --phase 255` exits 0 with SEED-291 marked answered by Phase 255."
    - "Hot-file ledger rows for `programmatic.py` and `emitters.py` are present in both `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` in the same commit."
    - "Stale ledger rows for `phase_types.py`, `agent_loop.py`, and `validator_kinds.py` are re-derived and updated."
    - "CLAUDE.md context size is under the 150,000 char hard limit (`node scripts/check-claude-md-size.cjs` exits 0)."
  artifacts:
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "detail sections and scan-list entries for programmatic.py and emitters.py"
      contains: "programmatic.py"
    - path: ".planning/seeds/SEED-291-the-extension-contract-plugin-is-data-process-or-sandbox-never-engine.md"
      provides: "answered seed record pointing to Phase 255"
      contains: "status: answered"
  key_links:
    - from: "CLAUDE.md"
      to: "docs/HOT-FILE-LEDGER.md"
      via: "same-commit ledger row sync"
      pattern: "programmatic.py"
    - from: ".planning/seeds/SEED-291-the-extension-contract-plugin-is-data-process-or-sandbox-never-engine.md"
      to: ".planning/ROADMAP.md"
      via: "status answered by Phase 255"
      pattern: "folded_into: 255"
---

<objective>
Update the hot-file ledger in `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` (adding `programmatic.py` and
`emitters.py`, refreshing stale triples), update `SEED-291` frontmatter to answered, update phase
state in `STATE.md` and `ROADMAP.md`, and verify all governance gates pass.
</objective>

<tasks>
<task name="update_hot_file_ledger" wave="3">
Re-derive git triples for the trigger paths:
1. `backend/app/services/harness/phase_types.py` (53/26/2937 -> re-derive exact)
2. `backend/app/services/tool_dispatcher.py` (85/35/5048 -> re-derive exact)
3. `backend/app/services/agent_loop.py` (48/22/3441 -> re-derive exact)
4. `backend/app/services/harness/validator_kinds.py` (14/6/762 -> re-derive exact)
5. `backend/app/services/harness/programmatic.py` (add new row + detail section)
6. `backend/app/services/harness/emitters.py` (add new row + detail section)
Update both `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` simultaneously under the same-commit rule.
Verify `node scripts/check-hot-file-ledger.cjs 255` exits 0.
</task>

<task name="update_seed_and_state" wave="3">
1. Update `SEED-291` frontmatter:
   - `status: answered`
   - `folded_into: 255`
   - `status_note: "ANSWERED by Phase 255 (2026-09-18). Extension contract written to docs/EXTENSION-CONTRACT.md and guarded by scripts/check-extension-contract.cjs and unit tests."`
2. Update `.planning/ROADMAP.md` Phase 255 checklist to completed.
3. Update `.planning/STATE.md` to reflect Phase 255 completion and ready for Phase 256.
</task>

<task name="verify_all_gates" wave="3">
Run repository validation gates:
1. `node scripts/check-claude-md-size.cjs`
2. `node scripts/check-seeds-register.cjs --phase 255`
3. `node scripts/check-hot-file-ledger.cjs 255`
4. `node scripts/check-extension-contract.cjs`
5. Backend unit tests baseline check.
</task>
</tasks>

<verify>
```bash
node scripts/check-claude-md-size.cjs
node scripts/check-seeds-register.cjs --phase 255
node scripts/check-hot-file-ledger.cjs 255
node scripts/check-extension-contract.cjs
```
</verify>
