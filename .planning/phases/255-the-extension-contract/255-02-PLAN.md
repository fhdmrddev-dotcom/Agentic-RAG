---
phase: 255-the-extension-contract
plan: 02
type: execute
wave: 2
depends_on: ["255-01"]
autonomous: true
requirements: [EXT-02]
gap_closure: false

files_modified:
  - scripts/check-extension-contract.cjs
  - backend/tests/unit/test_255_extension_contract_guard.py
  - package.json
  - .claude/hooks/extension-contract-guard.js
  - .claude/settings.json

user_setup: []

must_haves:
  truths:
    - "SC#2 — Planting a violation (an executor, emitter, validator, programmatic function or agent tool made resolvable from data, config, a database row or a user-supplied name) on any of SEED-291's six trigger_paths makes the guard exit non-zero; removing the plant makes it pass (EXT-02)."
    - "The guard runs from a place that invokes it in order rather than only by hand — wired into `package.json` (`npm run check:extension-contract`), into a Claude PostToolUse hook, and as a pytest unit test in `backend/tests/unit/` (EXT-02)."
    - "Every one of the six trigger paths is driven RED in isolation against a planted violation and the failure output is captured and verified before restoring clean state (EXT-02)."
    - "The backend unit test suite remains within the locked ceiling of 71 failing tests (diff against 255-BASELINE-backend-failing-set.txt shows 0 new failures)."
  artifacts:
    - path: "scripts/check-extension-contract.cjs"
      provides: "the mechanical AST/pattern scanner for closed-core extension violations"
      exports: ["checkExtensionContract", "main"]
    - path: "backend/tests/unit/test_255_extension_contract_guard.py"
      provides: "the pytest unit test verifying closed-core registries and lack of dynamic dispatch"
      contains: "test_phase_type_registry_is_closed"
    - path: ".claude/hooks/extension-contract-guard.js"
      provides: "PostToolUse hook executing check-extension-contract.cjs when trigger paths are edited"
      contains: "check-extension-contract.cjs"
  key_links:
    - from: "scripts/check-extension-contract.cjs"
      to: "backend/app/services/harness/phase_types.py"
      via: "ast scan for dynamic phase type registration"
      pattern: "PHASE_TYPE_REGISTRY"
    - from: "scripts/check-extension-contract.cjs"
      to: "backend/app/services/tool_dispatcher.py"
      via: "ast scan for dynamic tool registration or eval"
      pattern: "_TOOL_REGISTRY"
    - from: "backend/tests/unit/test_255_extension_contract_guard.py"
      to: "backend/app/services/harness/programmatic.py"
      via: "asserting PROGRAMMATIC_PHASE_REGISTRY is closed"
      pattern: "PROGRAMMATIC_PHASE_REGISTRY"
---

<objective>
Build and wire the mechanical guard enforcing the closed-core extension contract over the six
trigger paths named in `SEED-291`, prove it by driving it RED against planted violations on all six
files, and integrate it into automated test passes and hooks so it executes in order.
</objective>

<tasks>
<task name="create_script_guard" wave="2">
Author `scripts/check-extension-contract.cjs`:
1. Audits all six trigger paths:
   - `backend/app/services/harness/phase_types.py`
   - `backend/app/services/harness/validator_kinds.py`
   - `backend/app/services/harness/emitters.py`
   - `backend/app/services/harness/programmatic.py`
   - `backend/app/services/tool_dispatcher.py`
   - `backend/app/services/agent_loop.py`
2. Detects illegal dynamic dispatch patterns:
   - Dynamic registration APIs taking runtime names from config/data/DB.
   - `importlib`, `eval(`, `exec(` used to resolve executors, emitters, validators, programmatic functions, or tools.
   - Dynamic attribute lookups (`getattr(..., var)`) for callable dispatch.
   - Unauthorized additions to registry dicts outside of verified hardcoded manifests.
3. Exits with code 0 if all clean, code 1 with descriptive diagnostics and file/line references if violated.
4. Support `--files <list>` for incremental checking and full mode for repository verification.
</task>

<task name="create_pytest_guard" wave="2">
Author `backend/tests/unit/test_255_extension_contract_guard.py`:
1. `test_phase_type_registry_is_closed`: Asserts `PHASE_TYPE_REGISTRY_ENTRIES` contains exactly the 7 verified executors.
2. `test_programmatic_phase_registry_is_closed`: Asserts `PROGRAMMATIC_PHASE_REGISTRY` contains only the allowed functions (`split_topic`, `eval_slow_step`).
3. `test_emitter_registry_is_closed`: Asserts `EMITTER_REGISTRY` contains only registered entries (default `render_template`).
4. `test_tool_registry_is_closed`: Asserts `_TOOL_REGISTRY` contains only the fixed manifest of built-in tools.
5. `test_validator_registry_is_closed`: Asserts `VALIDATOR_REGISTRY` and `PROGRAMMATIC_VALIDATOR_REGISTRY` contain only explicit system validators.
6. `test_no_dynamic_code_execution_in_trigger_paths`: Asserts zero occurrences of `importlib`, `eval(`, or `exec(` across the six trigger files.
</task>

<task name="wire_in_order_execution" wave="2">
Wire the guard into the development workflow:
1. Add `"check:extension-contract": "node scripts/check-extension-contract.cjs"` to `package.json`.
2. Create `.claude/hooks/extension-contract-guard.js` (or wire into PostToolUse hooks in `.claude/settings.json`) to trigger on edits to any of the six files.
</task>

<task name="drive_red_against_six_paths" wave="2">
Execute the mandatory RED drive against each of the six trigger files:
1. Plant a dynamic resolution violation in `phase_types.py` -> verify guard exits non-zero -> revert.
2. Plant a dynamic resolution violation in `validator_kinds.py` -> verify guard exits non-zero -> revert.
3. Plant a dynamic resolution violation in `emitters.py` -> verify guard exits non-zero -> revert.
4. Plant a dynamic resolution violation in `programmatic.py` -> verify guard exits non-zero -> revert.
5. Plant a dynamic resolution violation in `tool_dispatcher.py` -> verify guard exits non-zero -> revert.
6. Plant a dynamic resolution violation in `agent_loop.py` -> verify guard exits non-zero -> revert.
Record the exact RED failure outputs and subsequent GREEN restores in the plan summary.
</task>
</tasks>

<verify>
```bash
# Verify the Node guard passes cleanly
node scripts/check-extension-contract.cjs

# Verify the pytest suite passes and doesn't increase baseline failures
pytest backend/tests/unit/test_255_extension_contract_guard.py -q
```
</verify>
