---
phase: 192-workflow-library-ia
plan: 10
type: execute
wave: 6
depends_on: ["192-02", "192-06", "192-07", "192-08", "192-09"]
files_modified:
  - frontend/src/pages/WorkflowsPage.tsx
  - frontend/src/pages/WorkflowsPage.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]

must_haves:
  truths:
    - "D-02: the three shelves are gone. One flat result list sits under one persistent toolbar, and the create affordance leads that toolbar — SC#4 is fixed structurally, not by promotion."
    - "D-16: the list is a client-side merge of three feeds fetched with `allSettled`, so a 403 on the gated `/drafts` feed degrades to zero drafts and NEVER to an empty library."
    - "D-05: the 200px project rail is gone; the project filter is one toolbar control and stays a server-side re-query."
    - "D-17: published rows narrow server-side, drafts narrow client-side on `definition.project_folder_id`, and starters are held OUT of the project filter and stay visible with a stated reason."
    - "D-11: no user-visible string on the library surface is developer vocabulary. The `GET /workflows/published` chip and the D14 honesty banner are both gone — the banner removed LOUDLY, because its claim was measured FALSE at HEAD, and a disclosure of a falsehood cannot be honestly re-worded."
    - "D-01: `WorkflowsPage.tsx` is composition — it owns fetches, merge and filter state, and declares no card, no filter item and no modal."
    - "The three latest-wins race guards survive the merge, per source, because only the published feed re-queries on project change."
    - "`NetNewFlag` and its Builder-band render at `:382` are untouched — they belong to Phase 193, and a pinned suite asserts that band by byte-exact `innerHTML`."
  artifacts:
    - path: "frontend/src/pages/WorkflowsPage.tsx"
      provides: "the composition page — merged feed, filter state, one flat list"
      contains: "allSettled"
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "library/libraryFilter.ts"
      via: "mergeLibrary + filterLibrary + chipCounts — the page computes no predicate itself"
      pattern: "mergeLibrary"
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "library/LibraryToolbar.tsx + library/WorkflowCard.tsx"
      via: "named imports; the page declares neither"
      pattern: "from \"@/components/workflows/library/"
---

<objective>
Turn `WorkflowsPage.tsx` into composition: one merged feed, one toolbar, one flat list of cards — and
delete the three shelves, the project rail, the three card components, `FilterItem`, the dashed
build-card, the honesty banner and the two library `NetNewFlag` renders that they carried.

Purpose: this is where 157-B lands. It is also the single riskiest commit of the phase — the page's four
existing behavioural contracts (project re-query, latest-wins, the fork mechanics, the Builder handoff)
must survive a frame that replaces everything around them.

Output: a composition page, a realigned suite, and the one **authorized** pin lowering of this phase,
riding in the same commit as the deletions that justify it.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-RESEARCH.md
@.planning/phases/192-workflow-library-ia/192-PATTERNS.md
@.planning/phases/192-workflow-library-ia/192-CONTEXT.md
@.planning/sketches/157-the-library-at-scale/README.md

<interfaces>
The three feeds, their gating, and the one that must never take the others down:

| Source | Client fn | Endpoint | Gated? |
|---|---|---|---|
| Published (mine) | `listPublishedWorkflows(projectArg, undefined, {scope:"mine"})` (`api.ts:1379`) | `GET /workflows/published?scope=mine` | **NO** — RUN CARVE-OUT (`workflows.py:175`) |
| Starters | `listStarterWorkflows()` (`api.ts:1411`) | `GET /workflows/starters` | **NO** — RUN CARVE-OUT (`workflows.py:223`) |
| Drafts | `listDraftWorkflows()` (`api.ts:3454`) | `GET /workflows/drafts` | **YES** — `require_visible("workflow_authoring")` (`workflows.py:1012`) |

`workflows.py:171-174` states the rule verbatim: *"Phase 148 (VIS-01) — RUN CARVE-OUT: DO NOT gate
/published or /starters. They are the Run picker feeds … end users need them so Run stays for everyone."*
</interfaces>

**Single-plan wave.** Every other plan's output converges here.

**⚠ The one authorized pin lowering of this phase rides in Task 3**, in the same commit as the two
deletions that justify it. The rule is explicit and is not negotiable: *a pin is lowered only alongside a
deliberate, plan-authorized deletion, in the same commit — never to make a red gate go quiet.*
</context>

<tasks>

<task type="auto">
  <name>Task 1: The merged feed — allSettled, per-source isolation, latest-wins preserved, D-17 semantics</name>
  <files>frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx:149-207` — the three latest-wins tickets and the comment
      explaining WHY (the live symptom: *"All projects showed 7, a specific project showed 16, but the
      rendered list lagged the selection"*), plus the three `useEffect`s. Only `refetchPublished` has
      `[selectedProjectId]` deps (`:177`); the other two are `[]` (`:187`, `:195`). **That asymmetry is
      D-17's whole cause and must be preserved, not collapsed.**
    - `frontend/src/pages/WorkflowsPage.tsx:67` and `:163`, `:172-173` — the `UNBOUND` sentinel and the
      existing client-side narrow it drives.
    - `.planning/phases/192-workflow-library-ia/192-RESEARCH.md` § "The D-16 merge" (why the feeds cannot
      collide today, and why the `Map` guard must exist anyway) and § "The unnamed IA defect".
    - `frontend/src/components/workflows/library/libraryFilter.ts` — `mergeLibrary`, `filterLibrary`,
      `chipCounts` from `192-05`. The page calls them; it writes no predicate of its own.
    - `frontend/src/components/workflows/DescribeKbPicker.tsx:160-195` — the honest-state rule: *"there are
      none"* and *"we could not ask"* are DIFFERENT FACTS and must be held apart.
  </read_first>
  <action>
    Replace the three sequential feed effects with a single `Promise.allSettled` over the three fetches,
    **keeping all three latest-wins tickets** and committing each source's result behind its OWN ticket.
    Do not collapse them into one guard: only `published` re-queries on project change, and a single
    ticket would either over- or under-invalidate.

    ⚠ **`allSettled`, never `all`.** `/drafts` is gated; `/published` and `/starters` are the RUN
    CARVE-OUT. A `Promise.all` plus one shared error path means a user without `workflow_authoring`
    visibility gets a 403 on drafts and an **empty library** — the gate re-introduced client-side on the
    two feeds the carve-out protects. RESEARCH names this the single highest-risk defect the merge can
    ship. Isolate failure per source and keep a per-source error state; never one shared `error` for the
    whole page.

    Normalize and merge through `mergeLibrary`, which dedupes by **`id`** and assigns `provenance` from
    feed origin. Keep the ORIGINAL row on each `LibraryRow` — `onOpenDraft` needs `token` (`:304`), and
    `WorkflowBuilderPage.session.test.tsx:750` exists specifically because dropping `token` from a
    hand-built object ships a silent clobber rather than a type error.

    Implement D-17's project semantics: published narrows **server-side** via the existing
    `?project_folder_id=` re-query (unchanged — only the instrument changed); drafts narrow
    **client-side** on `definition.project_folder_id`; **starters are held OUT** and stay visible. Keep
    the `UNBOUND` sentinel behaviour. Drive `LibraryToolbar`'s `updating` prop from the in-flight
    published re-query so the D-17 companion rule holds: previously-committed rows stay rendered with
    correct counts, counts are never zeroed and never pre-computed for rows that have not arrived.

    Hold the four states apart, per the honest-empty-state rule: **loading** (no counts rendered at all —
    a count of 0 during load is a lie); **genuinely empty**; **filtered to empty** (with the toolbar's
    *Clear search & filters*, and chips still showing their real recounted totals); and **a source
    failed** (say which one, keep the others rendered — never present a partial library as a complete one).
  </action>
  <acceptance_criteria>
    - `grep -c "allSettled" frontend/src/pages/WorkflowsPage.tsx` ≥ 1 and
      `grep -c "Promise.all(" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - All three tickets survive: `grep -c "publishedSeqRef" ` ≥ 2, `grep -c "startersSeqRef" ` ≥ 2,
      `grep -c "draftsSeqRef" ` ≥ 2.
    - `grep -c "mergeLibrary" ` ≥ 1; `grep -Ec "soulDeliverable|tierForDefinition" frontend/src/pages/WorkflowsPage.tsx` equals **0** (the page computes no chip predicate).
    - `grep -c "UNBOUND" ` ≥ 1.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowsPage.test.tsx -t "latest-wins"`
      passes — the stale-response guard is load-bearing and survives.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowsPage.test.tsx</automated>
  </verify>
  <done>Three feeds settle independently behind their own tickets, merge by id with provenance, and the project filter has honest semantics for all three row kinds.</done>
</task>

<task type="auto">
  <name>Task 2: The frame swap — one toolbar, one flat list, and the deletions that follow</name>
  <files>frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx:476-673` — the whole library render: header (`:479-486`),
      honesty banner (`:488-493`), run-cta (`:495-516`), the `grid-cols-[200px_1fr]` container (`:518`),
      the project rail (`:520-538`) and the three shelves (`:544`, `:568`, `:604`) with the build-card
      first cell (`:613`).
    - `frontend/src/pages/WorkflowsPage.tsx:675-691` (`FilterItem`), `:693-744` (`DraftCard`),
      `:745-1003` (`PublishedCard`, now without the Sheet after `192-08`), and the `StarterCard` block —
      all four are deleted here.
    - `frontend/src/pages/WorkflowsPage.tsx:80-90` (`NetNewFlag`) and its **three** render sites: `:382`
      (the **Builder breadcrumb** — Phase 193's surface, OUT of scope), `:492` (the banner) and `:609`
      (the Drafts shelf header). ⚠ `frontend/src/pages/WorkflowBuilderPage.header.test.tsx:303` asserts
      the flag-off breadcrumb band by **byte-exact `innerHTML` including the flag's full `title=` string
      and complete Tailwind class list**, and `:363` / `:794` require `getByTestId("net-new-flag")` to be
      present. That suite is pinned at 32.
    - `.planning/phases/192-workflow-library-ia/192-RESEARCH.md` § "D-11 plan-time check — ANSWERED".
    - `frontend/src/pages/WorkflowsPage.tsx:347-355` and `:473-474` — the post-publish Run CTA's set and
      lookup.
  </read_first>
  <action>
    Replace the library render with: header, the post-publish Run CTA, `<LibraryToolbar …/>`, then ONE
    flat grid of `<WorkflowCard …/>` over the filtered rows. Remove the `grid-cols-[200px_1fr]` split —
    the 200px returns to the grid, which is a measurable win at 200 workflows.

    Delete, in this commit: the three `<section>` shelves, the project rail `<nav>`, `FilterItem`,
    `DraftCard`, `PublishedCard`, `StarterCard`, and the dashed build-card at `:613` (its job moves to the
    toolbar's create control; its two-door handoff `openBuilderFresh` → `setBuilderInitial(null)` +
    `setPageView("builder")` is UNCHANGED — 192 must not change the seam to Phase 193's authoring doors).

    ⚠ `noUnusedLocals` and `noUnusedParameters` are on: removing the last render of a helper without
    removing the function is a **type error**, not a lint warning. Remove function and last usage in the
    same commit, and re-check every import the deletions orphan.

    **D-11, both halves:**
    - Delete the Published-shelf chip carrying the literal `GET /workflows/published` (`:577`) with the
      shelf it sat on.
    - **Delete the honesty banner (`:488-493`) loudly, not silently re-word it.** D-11 asked for a
      plan-time check; RESEARCH performed it and the answer is that the banner's claims are **FALSE at
      HEAD**: all four draft-CRUD routes are live and gated (`POST /workflows` `:979`,
      `GET /workflows/drafts` `:1012`, `PATCH /workflows/{id}` `:1044`, `DELETE /workflows/{id}` `:1141`),
      and `createWorkflowDraft` / `listDraftWorkflows` / `updateWorkflowDraft` are all called from this
      very page. A disclosure of a falsehood cannot be honestly re-worded — re-wording it would ship a new
      false statement in plain language. Record the measurement as the reason in a code comment and in the
      SUMMARY: the banner was removed because its claim was verified false, not because it was inconvenient.

    **⚠ Do NOT delete `NetNewFlag`.** Remove only its two **library** renders (`:492` with the banner and
    `:609` with the shelf). The function and its `:382` Builder-band render stay untouched — that band is
    Phase 193's surface and is asserted byte-exactly by a suite pinned at 32. Record the surviving `:84`
    `title=` as a Builder-band residual, deferred to Phase 193 with a named trigger, so a later reviewer
    cannot read it as 192's.

    **The post-publish Run CTA survives, unchanged, above the toolbar** — recorded here as an explicit
    decision (RESEARCH OQ1) so a reviewer does not read its survival as an oversight. It is a page-level
    banner, not a card verb. **Retarget its lookup to the merged list** so a just-published row is found
    regardless of which feed it arrived in.
  </action>
  <acceptance_criteria>
    - `grep -c "starters-shelf\|published-shelf\|drafts-shelf" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - `grep -c "function FilterItem\|function DraftCard\|function PublishedCard\|function StarterCard" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - `grep -c "build-card" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - `grep -c "GET /workflows/published" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - `grep -c "grid-cols-\[200px_1fr\]" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - **`grep -c "function NetNewFlag" frontend/src/pages/WorkflowsPage.tsx` equals `1`** and
      `grep -c "<NetNewFlag" frontend/src/pages/WorkflowsPage.tsx` equals **1** (the `:382` Builder render only).
    - `grep -c "run-cta" frontend/src/pages/WorkflowsPage.tsx` ≥ 1 (the CTA survives).
    - `grep -c "openBuilderFresh" frontend/src/pages/WorkflowsPage.tsx` ≥ 1 (the Phase-193 seam is unchanged).
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowBuilderPage.header.test.tsx`
      passes with **0 failures** and reports **32** tests — the byte-exact breadcrumb band is unmoved.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**.
    - `cd frontend && npx eslint src/pages/WorkflowsPage.tsx` → 0; `-c eslint.a11y.config.js` → 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx</automated>
  </verify>
  <done>One toolbar over one flat list; shelves, rail, three cards, FilterItem, build-card, banner and the two library NetNewFlag renders are gone; the Builder band and the Phase-193 seam are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Realign the page suite — four rewrites, two authorized deletions, one pin lowering</name>
  <files>frontend/src/pages/WorkflowsPage.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.test.tsx` in full — the 23 tests, the `vi.hoisted` +
      `vi.mock("@/lib/api")` seam (`:21-57`) and the four literal fixtures (`:75-152`).
    - `.planning/phases/192-workflow-library-ia/192-RESEARCH.md` § "Which `WorkflowsPage.test.tsx` tests
      break BY DESIGN vs must stay green" — the classification is **two deletions, four rewrites,
      seventeen that must stay green**, and it should appear in the plan's commit verbatim.
    - `scripts/vitest-count-gate.cjs` header — the lowering rule, and `:972-974` on reading the number
      from the script's own `actual` column rather than computing it.
  </read_first>
  <action>
    **Rewrite four tests, keeping their assertions and changing only their interaction:**
    - `:174` *selecting a project re-queries with that `project_folder_id`* — drive the toolbar `<select>`
      instead of a `FilterItem` click. The assertion on `listPublishedWorkflows`' arguments is unchanged.
    - `:224` *the dashed build-card … opens the two-door chooser* — the build-card half dies with D-02;
      the **opens the two-door chooser** half is the seam to Phase 193 and must stay green. Rewrite as
      "the toolbar create affordance opens the two-door chooser".
    - `:237` *NO draft card exposes a Run affordance; published cards DO* — the contract survives D-09
      exactly (a draft gets `✎ Open`, never `▶ Run`); the testids change and `draft-publish` disappears
      with D-10. **Rewrite, do not delete** — this is the only page-level mechanical guard on a
      load-bearing contract stated in the page's own docblock (`:14`).
    - `:434` *renders the Starters shelf with the curated card, its chip, and a Use-this-starter control*
      — rewrite as "a starter row renders with its provenance mark and the fork verb".

    **Delete exactly two tests, and say so out loud in the commit message:** `:212` *the Published shelf
    renders ABOVE the Drafts shelf* and `:460` *section order is Starters → Published → Drafts*. Both
    assert a shelf order D-02 deliberately removes. A deletion is an **authorized act** here, not a
    convenience: it rides in the same commit as the pin lowering that permits it, with the reason stated.

    **Seventeen tests must stay green untouched** — in particular the latest-wins guard (`:187`), the soul
    and derived-tier renders (`:251`, `:265`), the strictest-emit-policy order-independence (`:279`), the
    five Run-modal tests (`:306`-`:374`), the three Tweak tests (`:394`-`:421`), the starter fresh-copy
    fork (`:446`), the open-a-draft and true-fresh-build tests (`:476`, `:488`) and the back-button refetch
    (`:499`). If any of those reds, the restructure broke a contract — fix the source, not the test.

    ⚠ **Add `deleteWorkflowDraft` to the `vi.mock("@/lib/api")` factory** in this file. That factory is
    **exhaustive**: every api symbol the page, its hosted Builder or the Gauntlet imports must be listed,
    or the mount throws. D-18 introduces a new one. Check every other suite that mounts the page
    (`RunModal.test.tsx`, `RunModal.a11y.test.tsx`, `PublishedCardDelete.test.tsx`,
    `WorkflowBuilderPage.header.test.tsx`, `.session.test.tsx`) and add it wherever the mount would
    otherwise throw — read the failure, do not guess.

    **Lower the `WorkflowsPage.test.tsx` pin to the post-restructure number, in THIS commit**, read from
    the gate's own `actual` column across two agreeing runs. Write the reason into the comment: two
    deliberate, plan-authorized deletions, named. Never lower any other pin here.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowsPage.test.tsx`
      passes with 0 failures.
    - `grep -c "shelf" frontend/src/pages/WorkflowsPage.test.tsx` equals **0**.
    - `grep -c "deleteWorkflowDraft" frontend/src/pages/WorkflowsPage.test.tsx` ≥ 1.
    - `node scripts/vitest-count-gate.cjs` exits 0 on **two** consecutive runs with identical per-file
      numbers, and the `WorkflowsPage.test.tsx` row shows `delta 0`.
    - The gate diff lowers **exactly one** pin: `git diff scripts/vitest-count-gate.cjs` changes one
      numeric value downward, and its adjacent comment names the two deleted tests.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages src/components/workflows/library`
      passes with 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Four tests rewritten, two deleted with their authorization stated, seventeen still green, and one pin lowered in the same commit as the deletions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a feature-gated endpoint → an ungated surface | `/drafts` carries `require_visible("workflow_authoring")`; `/published` and `/starters` are the documented RUN CARVE-OUT. A client-side composition can re-introduce the gate the server deliberately withheld. |
| server-side project filter → client-rendered list | `?project_folder_id=` narrows only `/published`. Applying it uniformly client-side silently deletes every starter from view. |
| shipped cross-phase assertions → this restructure | A pinned suite holds the Builder breadcrumb band by byte-exact `innerHTML`, including `NetNewFlag`'s full `title=`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-03 | Denial of Service (availability) | the merged feed | mitigate | `Promise.allSettled` with per-source failure isolation and per-source error state. A rejected `/drafts` degrades to zero drafts, never to zero library. Asserted by a test in `192-11` that stubs `listDraftWorkflows` to reject and requires published + starter rows to still render. This is the highest-risk defect the merge can ship. |
| T-192-26 | Spoofing (of completeness) | the rendered list | mitigate | Four states held apart — loading · genuinely empty · filtered to empty · a source failed. Never present a partial library as a complete one; never render a count of 0 during load. |
| T-192-27 | Spoofing (of filter behaviour) | the project filter | mitigate | D-17: published server-side, drafts client-side, starters held out **with a stated reason** rendered in the toolbar. Silence is a FAIL (UAT U6). |
| T-192-18 | Tampering | the pinned Builder-band assertion | mitigate | `NetNewFlag` is not deleted; only its two library renders are removed. `WorkflowBuilderPage.header.test.tsx` must report exactly 32 tests, 0 failures. |
| T-192-06 | Repudiation | the count gate | mitigate | Exactly one pin lowered, in the same commit as the two named deletions, at a number read from the gate's own `actual` column across two agreeing runs. |
| T-192-28 | Tampering | the Phase-193 seam | accept | `setPageView("builder")` + `setBuilderInitial` are unchanged; the create control merely calls `openBuilderFresh`. Asserted by the rewritten `:224` test. |
| T-192-SC | Tampering | package installs | accept | None. Zero new dependencies. |
</threat_model>

<verification>
- `node scripts/vitest-count-gate.cjs` exits 0 on two agreeing runs.
- `npx tsc -p tsconfig.app.json --noEmit` → **33**; `npx eslint src/pages/WorkflowsPage.tsx` → 0 and
  a11y-lint → 0.
- `WorkflowBuilderPage.header.test.tsx` reports **32** tests, 0 failures.
- `grep -c "function NetNewFlag" frontend/src/pages/WorkflowsPage.tsx` → 1;
  `grep -c "<NetNewFlag" frontend/src/pages/WorkflowsPage.tsx` → 1.
- All seven `192-04` and six `192-03` baselines still green, with no baseline literal edited.
</verification>

<success_criteria>
- One flat list under one toolbar; shelves, rail, three card components, `FilterItem`, build-card,
  banner and both library `NetNewFlag` renders removed.
- `allSettled` with per-source isolation; three latest-wins tickets intact.
- D-17 semantics implemented for all three row kinds.
- Four tests rewritten, two deleted with authorization, seventeen green; one pin lowered in the same commit.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-10-SUMMARY.md` when done. Record: the measured
`wc -l frontend/src/pages/WorkflowsPage.tsx` after this plan (it feeds `192-12`'s subtree delta); the
verbatim D-11 measurement that justified removing the banner rather than re-wording it; the two deleted
test names and the new pinned number read from the gate; and the Phase-193 residual (`NetNewFlag`'s
surviving `:84` `title=` in the Builder band) with its named trigger.
</output>
