---
phase: 192-workflow-library-ia
plan: 12
type: execute
wave: 8
depends_on: ["192-11"]
files_modified:
  - frontend/src/components/workflows/library/librarySubtree.fences.test.ts
  - frontend/src/components/workflows/library/WorkflowCard.tsx
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
autonomous: false
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]

must_haves:
  truths:
    - "Every one of the five negative fences has been observed RED against a real plant in production source, and the planted file was restored md5-identical — a fence never driven RED is a claim, not a guard."
    - "D-14 / D-08 / D-01: F1, F5 and F4 are proved to fire, including F4 — the ESM-cycle fence whose violation typechecks and lints clean, which is exactly why prose could never enforce it."
    - "`lib/threadGroups.tsx` is byte-unchanged by Phase 192, proved by a content hash rather than asserted — which resolves 158-B's re-open trigger #3 mechanically instead of by judgment."
    - "The subtree delta is MEASURED and RECORDED — before and after line counts and the real percentage — never quoted from RESEARCH's estimate."
    - "Every suite this phase created or grew is PINNED at a number read from the gate's own `actual` column across two agreeing runs. An unpinned suite is an unguarded one."
    - "G-5 on `WorkflowsPage.tsx` is recorded as SATISFIED in the CLAUDE.md hot-file ledger, with re-derivable figures and the commands that produced them."
  artifacts:
    - path: "frontend/src/components/workflows/library/librarySubtree.fences.test.ts"
      provides: "the five fences plus the one-direction import assertions, each proved to fire"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "pins for every new and grown suite of this phase"
    - path: "CLAUDE.md"
      provides: "the updated G-5 hot-file ledger row for WorkflowsPage.tsx"
  key_links:
    - from: "each fence"
      to: "a real plant in production source"
      via: "observed RED, then the file restored md5-identical, both hashes recorded"
      pattern: "md5"
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "every module under library/"
      via: "one-direction import edge, asserted with no re-export shim"
      pattern: "import .* from \"@/components/workflows/library/"
---

<objective>
Close the phase: prove every fence fires, measure what the restructure actually cost, pin what this phase
created, record G-5 as satisfied, and drive the eleven lived-experience UAT rows.

Purpose: 188.2's binding lesson is that **a negative fence must be driven RED against a real plant before
it is trusted** — and 190-15 found two fences that were green for an entire plan while the feature they
guarded was absent from the running app. 188.2's other lesson is that a pure extraction measurably GREW
its subtree by +67.1%, and that the honest thing is to state the figure rather than smooth it. RESEARCH
estimates +5% to +16% here and **labels it an estimate**; this plan measures it.

Output: five proved fences, one measured delta, a fully pinned gate, an updated ledger row, and the G-4
operator verification.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-VALIDATION.md
@.planning/phases/192-workflow-library-ia/192-RESEARCH.md
@.planning/phases/192-workflow-library-ia/192-PATTERNS.md

**Single-plan wave.** This plan is `autonomous: false` — Task 3 is a blocking operator checkpoint.

**Chrome MCP constraints, recorded and binding:** `take_screenshot` **times out repeatedly** — read DOM
geometry via `evaluate_script` instead; `computer` CLICKS can deliver **zero events** while `hover` and
`left_click_drag` work; `elementFromPoint` is machine-checkable reachability.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive all five fences RED against real plants, and restore md5-identical</name>
  <files>frontend/src/components/workflows/library/librarySubtree.fences.test.ts</files>
  <read_first>
    - `.planning/phases/192-workflow-library-ia/192-VALIDATION.md` § "Negative fences" — the table of five
      fences and their named plants.
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` — F1, F4, F5 and the
      threadGroups hash fence as written in `192-05`, now covering a subtree where all seven modules exist.
    - `frontend/src/pages/WorkflowsPage.test.tsx` — F2 and F3 as written in `192-11`.
    - `frontend/src/components/workflows/WorkflowCanvas.test.tsx:756-763` — the one-direction assertion,
      and its third clause: **no re-export shim**, without which the two negatives are also satisfied by
      two modules nobody uses.
    - The 190-16 precedent for recording absence proofs: the SUMMARY carries the md5 before and after each
      plant, so a waved-through finding can never read as an absent one.
  </read_first>
  <action>
    For each fence, in turn: plant the violation **in production source** (never in a test), run the fence,
    **observe it RED**, restore the file, and verify `md5sum` matches the pre-plant value exactly. Record
    both hashes and the observed failure message.

    - **F1** — add `title="x"` to one button in `library/WorkflowCard.tsx`.
    - **F2** — re-insert the `GET /workflows/published` chip text into `library/LibraryToolbar.tsx`.
    - **F3** — add a `Publish…` button to the draft branch of `library/WorkflowCard.tsx`. Also demonstrate
      F3's **positive control** from `192-11` still passes — the selector finds a planted `Publish…`.
    - **F4** — add `import { WorkflowsPage } from "@/pages/WorkflowsPage"` to `library/RunModal.tsx`.
      ⚠ Note in the SUMMARY that this plant **typechecks and lints clean** — run `tsc` and `eslint`
      against the planted tree and record that both pass — because that is the entire justification for
      the fence existing. Then repeat with the extension form (`from "@/pages/WorkflowsPage.tsx"`) to prove
      the `(\.[jt]sx?)?` group is doing its job, and with the dynamic `import()` form.
    - **F5** — set the search `placeholder` in `library/LibraryToolbar.tsx` to a meaning-search phrasing.
      The plant must live in the **toolbar**, not the vocabulary module: that is PATTERNS correction C-3,
      and a fence scoped to `libraryVocabulary.ts` alone would be evaded by exactly this plant.
    - **The threadGroups hash fence** — make a one-character change to `frontend/src/lib/threadGroups.tsx`,
      observe RED, restore, and confirm `md5sum` is identical. This is what converts 158-B's re-open
      trigger #3 from an ambiguity into a fact.

    Then add the **one-direction assertions** to the fences file: `WorkflowsPage.tsx`'s source imports each
    library module it consumes; declares none of them (`function RunModal(`, `type DeletePhase`,
    `function FilterItem(`, `function PublishedCard(`, `function DraftCard(`, `function StarterCard(` all
    absent); and leaves **no re-export shim** (`export { … }` / `export * from` pointing at `library/`).

    ⚠ **Do not simulate any of this.** A synthetic positive control (which `192-05` already added) proves
    the detector works; only a real plant proves the fence is pointed at the real files.
  </action>
  <acceptance_criteria>
    - The SUMMARY records, for each of the six plants: the file, the exact planted text, the fence's
      observed failure message, and `md5sum` before and after — with before == after.
    - For F4, the SUMMARY records that `npx tsc -p tsconfig.app.json --noEmit` and
      `npx eslint src/components/workflows/library` **both passed** on the planted tree, at 33 errors and
      0 errors respectively.
    - After all restores: `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages src/components/workflows/library`
      passes with 0 failures.
    - `git status --porcelain` is clean apart from `librarySubtree.fences.test.ts` — every planted file is
      byte-identical to its pre-plant state.
    - The one-direction assertions exist: `grep -c "export \\* from" ` and the six `function …(` absence
      checks are all present in the fences file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages src/components/workflows/library</automated>
  </verify>
  <done>All five fences plus the threadGroups hash fence observed RED against real plants and restored md5-identical; the one-direction edge is asserted with its no-shim clause.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the subtree delta, pin every suite, and record G-5 as satisfied</name>
  <files>frontend/src/components/workflows/library/WorkflowCard.tsx, scripts/vitest-count-gate.cjs, CLAUDE.md</files>
  <read_first>
    - `.planning/phases/192-workflow-library-ia/192-RESEARCH.md` § "The subtree delta — stated, not
      smoothed" — the estimate table, explicitly labelled an estimate (assumption A2).
    - `CLAUDE.md` § "Workflow guardrails" hot-file ledger — the `WorkflowCanvas.tsx` row (188.1) and the
      `PhaseNodeCard.tsx` row (188.2) are the shape to copy: **every figure re-derivable, with the command
      that produced it, and corrections stated on measurement rather than smoothed.** The 188.2 row states
      its subtree GREW `797 → 1332 L, +67.1%` in the same cell as the win.
    - `CLAUDE.md` hot-file ledger row for `frontend/src/pages/WorkflowsPage.tsx` (added 2026-08-10) — the
      row this plan updates. It currently reads **G-5 FIRES — extraction due**.
    - `scripts/vitest-count-gate.cjs:968-974` and `:1005-1011` — read the pinned number from the script's
      own printed `actual`, never by adding to a figure in a comment. That note has gone stale eight times
      by being computed rather than read.
    - `.planning/phases/192-workflow-library-ia/192-0*-SUMMARY.md` — each plan recorded its module's
      measured line count for exactly this step.
  </read_first>
  <action>
    **Measure the delta. Do not quote the estimate.** Re-derive:
    `git show <phase-start-sha>:frontend/src/pages/WorkflowsPage.tsx | wc -l` for the before (expected
    1407 — falsify it, do not assume it), `wc -l frontend/src/pages/WorkflowsPage.tsx` for the after, and
    `wc -l` over every file in `frontend/src/components/workflows/library/` for the subtree. Compute the
    real percentage. If it lands outside RESEARCH's +5%…+16% estimate, **state that plainly** — RESEARCH
    labelled it an estimate precisely so the measurement could contradict it without anyone having to
    smooth the record.

    Record the measured before/after in **two** places: the SUMMARY, and `WorkflowCard.tsx`'s own docblock
    — so a later reader cannot mistake growth for regression, which is the reason 188.2 put the figure in
    the card's docblock rather than only in its summary.

    **Pin every suite this phase created or grew**, in one commit, at numbers read from the gate's own
    printed `actual` column across **two agreeing runs**: `libraryFilter.test.ts`,
    `librarySubtree.fences.test.ts`, `LibraryToolbar.test.tsx`, `WorkflowCard.test.tsx` — four new suites,
    and no more: the delete Sheet is covered by `PublishedCardDelete.test.tsx`'s seven baselines rather
    than by a suite of its own, so do not invent a pin for a file that does not exist (a `TARGETS` entry
    pointing at a missing path makes the gate ERROR, exit 2, not fail) — plus the RAISES owed by
    `RunModal.test.tsx`,
    `RunModal.a11y.test.tsx`, `PublishedCardDelete.test.tsx` and `WorkflowsPage.test.tsx`. Write the
    adoption comment in the house shape, citing the 188-12 precedent for why they were left unpinned while
    growing — and stating that an unpinned suite is an unguarded one, so the exemption expires here.
    Update the trailing `BASELINE_TOTAL` marker comment to the printed `pinned total`, read not computed.

    **Update the CLAUDE.md hot-file ledger row for `frontend/src/pages/WorkflowsPage.tsx`** from
    *G-5 FIRES — extraction due* to **satisfied (192 — the date)**, in the 188.1/188.2 row shape: the
    measured before/after line counts, the measured subtree total and its real percentage, the modules the
    code went to, the commands to re-derive each figure, and the constraints that now bind (nothing under
    `library/` may import `@/pages/WorkflowsPage`; `WorkflowSoul` and `PhaseSpine` are consumed unchanged
    and carry the inherited `title=`). Correct the touch count and line count on measurement if they have
    moved. If any part of the concern stack named in that row is still unextracted — the builder host, the
    project rail's replacement, the Gauntlet mount — say so and name the seam rather than implying the
    file is finished.
  </action>
  <acceptance_criteria>
    - `node scripts/vitest-count-gate.cjs` exits 0 on **two** consecutive runs with identical per-file
      numbers, and every suite this phase created or grew shows `delta 0`.
    - No file in the gate's output prints as `new` (unpinned) under `src/components/workflows/library/`.
    - The printed `pinned total` equals the value written into the trailing marker comment.
    - `grep -c "192" CLAUDE.md` — the `WorkflowsPage.tsx` ledger row names Phase 192 and no longer contains
      the string `G-5 FIRES` in that row.
    - `WorkflowCard.tsx`'s docblock contains the measured before and after totals and the computed
      percentage.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**;
      `npx eslint src/components/workflows/library` → 0; `-c eslint.a11y.config.js` → 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The delta is measured and recorded in two places, every suite is pinned from a read number, and the ledger row states G-5 satisfied with re-derivable figures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: G-4 lived-experience UAT — the eleven rows, driven at 200 workflows</name>
  <files>.planning/phases/192-workflow-library-ia/192-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/192-workflow-library-ia/192-VALIDATION.md` § "Manual-Only Verifications" — the
      eleven rows, their pass bars, and the U4 correction (the DOM sweep excludes the `workflow-soul`
      subtree, because `WorkflowSoul.tsx:99` and `PhaseSpine.tsx:77` carry inherited `title=` attributes
      two prior phases shipped).
    - `CLAUDE.md` § "UAT scoreboard recipe" — the rule that a blocked row is recorded ⛔ with its reason
      and never dropped, and the reason the 8-row cross-provider roster does not apply here.
  </read_first>
  <action>
    Start the app locally, seed or generate ~200 workflows, and drive all eleven rows below with Chrome
    MCP, reading DOM geometry through `evaluate_script`. Record each row's outcome — pass, fail, or ⛔ with
    a named reason and blocking issue id — into `192-VALIDATION.md`'s manual table and into the SUMMARY.
    Do not mark a row from another row's evidence: an unrecognised state renders "state unknown", not
    "complete", and a PASS inferred from a neighbouring row is not a driven row.
  </action>
  <what-built>
    The Workflows library is now one flat list under one persistent toolbar: create leads, an always-on
    search over name + purpose with the hit highlighted, six chips that recount against the live search, a
    project select that states what it does to starters, and one card with one primary verb, a `⋯`
    overflow, and one consequence sentence wired as real DOM text. `RunModal` and the victim-naming delete
    Sheet moved to their own modules with byte-identical rendering; the backend now returns `is_mine` and
    `is_system_global`; `Publish…` and the developer-vocabulary strings are gone.
  </what-built>
  <how-to-verify>
    Drive all eleven rows from `.planning/phases/192-workflow-library-ia/192-VALIDATION.md`
    § "Manual-Only Verifications", **at 200 workflows, never at 12**. Use `evaluate_script` for DOM
    geometry — `take_screenshot` times out repeatedly on this setup, and `computer` clicks can deliver zero
    events while `hover` and `left_click_drag` work.

    1. **U1** — type three characters; the target row is visible **without scrolling**, and
       `elementFromPoint` on its centre returns a node inside that row.
    2. **U2** — activate two chips at once; every chip's number **equals** the rows it produces.
    3. **U3** — measure `getBoundingClientRect().top` of the create control at 200 rows:
       `top < window.innerHeight` at first paint, and it is the **first** interactive element in DOM order.
    4. **U4** — count `[title]` inside the library subtree **excluding the `workflow-soul` subtree**; the
       count must be **0** in this phase's own chrome. The two remaining `title=`s belong to
       `WorkflowSoul.tsx:99` and `PhaseSpine.tsx:77`, are inherited, and are recorded in CONTEXT.md as a
       carried defect out of scope — left unstated, this row would fail the phase for a defect two prior
       phases shipped. Then read the `aria-describedby` target's `textContent` and confirm it names **both**
       halves.
    5. **U5** — read a card and state aloud what the fork verb will do **before** clicking it; then click.
       The stated expectation must match what happens.
    6. **U6** — pick a project. Starters remain, **with a stated reason**. **Silence is a FAIL.**
    7. **U7** — type *"the thing that checks vendors"*: zero results, an honest empty state, a one-click
       *Clear search & filters*, and **no copy implying meaning-search**.
    8. **U8** — open ⋯ → `Delete workflow…` on a published row: exact server counts render, and `Escape`
       during `deleting` does **not** close.
    9. **U9** — open Run → confirm: it lands on the run surface (canvas flag on) or a new chat thread
       (flag off), matching the `run-destination` copy shown.
    10. **U10** — emulate touch and re-read every action: every explanation is still legible.
    11. **U11** — open a draft's ⋯ → `Delete`: the row disappears, and the published victim-naming Sheet
        is **not** what appeared.

    Record each row's outcome. A row that cannot be driven is recorded as ⛔ with its reason and the
    blocking issue id — **never silently omitted**. A scoreboard that lists only what passed is not a
    scoreboard.

    ⚠ The 8-row cross-provider roster is **NOT triggered** by this phase and that is stated, not omitted:
    `WorkflowsPage.tsx` imports no provider symbol, reads no model registry and opens no SSE stream; the
    only backend change is two projected columns on two read endpoints; the launch path
    (`onLaunch` → `ChatLayout.doRun`) is unchanged and is already covered by `ChatLayout.launch.test.tsx`,
    pinned at 17.
  </how-to-verify>
  <acceptance_criteria>
    - All eleven rows are recorded with an outcome — pass, fail, or ⛔ with a named reason.
    - U6's outcome quotes the **exact text** the toolbar rendered, so "explained, not silent" is evidenced
      rather than judged.
    - U4's `[title]` count inside this phase's chrome is **0**, and the two inherited `title=` sources are
      named in the record.
    - U3's measurement includes the numeric `top` and `window.innerHeight`.
    - Any owed row is recorded in the SUMMARY and in STATE.md with the row to run first — closing with owed
      manual UAT is legitimate, but it is stated as a DECISION, never as a claim that everything ran.
  </acceptance_criteria>
  <verify>
    <human-check>All eleven rows recorded with an outcome; U6 quotes the exact rendered text; U4's
    `[title]` count inside this phase's chrome is 0; U3 records the numeric `top` and `window.innerHeight`.</human-check>
  </verify>
  <done>The eleven-row scoreboard is complete — every row passed, failed with a named defect, or is
  recorded ⛔ with its reason and the row to run first.</done>
  <resume-signal>Type "approved" with the eleven-row record, or describe the failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a written fence → an enforced invariant | A fence never observed RED is a claim. 190-15 found two that were green for a whole plan while the guarded feature was absent from the running app. |
| an estimate → a recorded result | RESEARCH's +5%…+16% subtree figure is labelled an estimate; quoting it as a result would be the exact "unmeasured claim" this project has recorded nine falsifications of. |
| structural tests → lived experience | G-4 exists because structural tests passed on every one of the 075.x phases while the lived experience was defective. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-06 | Repudiation | the five negative fences | mitigate | Each driven RED against a real plant in production source, restored md5-identical, hashes and failure messages recorded. F4's plant additionally demonstrated to typecheck and lint clean, which is the whole reason it exists. |
| T-192-30 | Repudiation | the subtree-delta claim | mitigate | Measured with named commands, recorded in the SUMMARY and in the card's docblock, and stated plainly if it falls outside the estimate. |
| T-192-31 | Repudiation | unpinned new suites | mitigate | Every suite created or grown by this phase is pinned here from the gate's own `actual` across two agreeing runs; the 188-12 growing-suite exemption expires in this commit. |
| T-192-21 | Denial of Service (accessibility) | touch users | mitigate | UAT rows U4 and U10 drive the explanation without hover, with the inherited `WorkflowSoul` / `PhaseSpine` `title=`s excluded and named. |
| T-192-32 | Spoofing (of verification) | the UAT record | mitigate | A blocked row is recorded ⛔ with its reason, never dropped; U6 quotes the exact rendered text. |
| T-192-SC | Tampering | package installs | accept | None. Phase 192 installs zero packages; the audit is not applicable and no plan may add one. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages src/components/workflows/library` — 0 failures.
- `node scripts/vitest-count-gate.cjs` exits 0 on two agreeing runs, with no `library/` suite printing `new`.
- `npx tsc -p tsconfig.app.json --noEmit` → **33**; `npx eslint src` a11y config → no new errors in this
  phase's files.
- `git status --porcelain` shows no residue from any plant.
- Eleven UAT rows recorded.
</verification>

<success_criteria>
- Five fences + the threadGroups hash fence proved to fire against real plants, all files restored md5-identical.
- The subtree delta measured, recorded in two places, and stated honestly against the estimate.
- Every suite of this phase pinned; the gate green twice.
- The CLAUDE.md ledger row records G-5 satisfied with re-derivable figures.
- Eleven G-4 rows driven, or explicitly recorded as owed with the row to run first.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-12-SUMMARY.md` when done. It carries the phase's
closing record: the six plant/restore md5 pairs, the F4 typecheck-and-lint-clean note, the measured
subtree before/after and percentage, the full pin table, the updated ledger row, and the eleven-row UAT
scoreboard including any ⛔.
</output>
