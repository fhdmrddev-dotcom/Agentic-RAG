---
phase: 200-the-workflow-journey
plan: 06
type: execute
wave: 6
depends_on: ["200-01", "200-02", "200-05"]
files_modified:
  - frontend/src/components/workflows/FlowEdge.tsx
  - frontend/src/components/workflows/FlowEdge.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.tsx
  - frontend/src/components/workflows/WorkflowCanvas.test.tsx
  - frontend/src/providers/ThemeProvider.tsx
  - frontend/src/hooks/useTheme.ts
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/main.tsx
  - scripts/seed-constructor-slug-workflow.py
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .claude/skills/sketch-findings-agentic-rag/references/icon-convention.md
autonomous: true
requirements: [DES-02]

must_haves:
  truths:
    - "D-01 — the `builder-canvas` screen reports N/N atoms against `200-CHECKLIST.md` §3 or NAMES the miss"
    - "D-08 — the canvas edge label IS the upstream step's declared count, read through the existing `runState` seam: ONE mechanism, no second counting path, no new fetch; an edge whose upstream declared nothing renders NO label"
    - "D-07 — the edge never renders `0` as a stand-in for absence, and never a dash; a declared `0` renders as the fact it is"
    - "`BUG-260813-01` is closed by a `ThemeProvider`, not a second `useTheme()` call — a forked hook gives each consumer its own state and neither re-renders the other"
    - "⚠ The theme fix is a CROSS-SURFACE change that lands in CHAT first: `ChatLayout.tsx` is `useTheme`'s ONE consumer and `WorkspacePanel`'s only production mount"
    - "`BUG-260807-01` and `BUG-260808-01` are already CODE-FIXED; what this plan ships is the seeded fixture and the driven row that flip BOTH — a UAT row, not a repair"
  artifacts:
    - path: "frontend/src/providers/ThemeProvider.tsx"
      provides: "One theme state broadcast by context, with a non-throwing accessor for leaf reads"
      contains: "createContext"
    - path: "scripts/seed-constructor-slug-workflow.py"
      provides: "A workflow_definitions fixture whose middle phase is slugged `constructor` — the UI cannot author this slug"
  key_links:
    - from: "frontend/src/components/workflows/FlowEdge.tsx"
      to: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      via: "the existing runState seam supplies the upstream node's declared count; the edge counts nothing itself"
      pattern: "runState"
    - from: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      to: "frontend/src/providers/ThemeProvider.tsx"
      via: "a non-throwing context read replacing the hardcoded colorMode"
      pattern: "colorMode"
---

<objective>
Build the `builder-canvas` screen to `200-CHECKLIST.md` §3: connections that carry the upstream step's
declared count, the four unexpressed connection states, a branch node that shows its own condition,
and node marks drawn in the line vocabulary rather than as large filled circles. Close
`BUG-260813-01` (the canvas stays dark in light mode) while the surface is open.

Purpose: BC-1 is `199-05`'s flagship CANNOT-EXPRESS and one of the two amber atoms that justify
lifting the presentation-only fence. BC-3's half is frontend-only and was never built —
`on_failure: skip_to_phase:<slug>` **is already in the definition**.

Output: an edge that labels a fact it did not invent, a canvas that follows the theme, and the one
driven row that closes two open prototype-key bug reports.

⚠ **`WorkflowCanvas.tsx` is mounted on BOTH pages** (`WorkflowBuilderPage.tsx:2106` editable;
`WorkflowRunPage.tsx:1091` `editable={false}` + `runState`). **Any change here is a change to the run
surface too**, so a regression lands on `200-07`'s screen. This plan is the ROADMAP's `200-05`,
shifted by one.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-the-workflow-journey/200-CHECKLIST.md
@.planning/phases/200-the-workflow-journey/200-PATTERNS.md
@.planning/phases/200-the-workflow-journey/200-RESEARCH.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md
@.planning/sketches/200-journey-interactive/screens/builder-canvas.html
@.planning/reported-bugs/BUG-260813-01.md
@.planning/reported-bugs/BUG-260807-01.md
@.planning/reported-bugs/BUG-260808-01.md
@frontend/src/providers/TechnicalNamesProvider.tsx
@frontend/src/components/workflows/ownProperty.ts

<interfaces>
<!-- The seam D-08 rides. WorkflowCanvas.tsx:555-567, VERBATIM — and its docblock is the rule: -->
<!-- the PAGE owns the join onto the definition, the reading and the words, so the canvas       -->
<!-- derives no run state, reads no step ordinal and imports no vocabulary. Its own suite greps -->
<!-- for all three. Optional and inert when absent.                                             -->
  runState?: (slug: string) => NodeRunState | undefined

<!-- FlowEdge ALREADY renders labels — the payload label has a home (FlowEdge.tsx:196-204): -->
export const DETOUR_ARMED_LABEL = "you say yes"
export const DETOUR_OPEN_LABEL  = "nobody is asked"

<!-- The provider shape to copy — TechnicalNamesProvider.tsx, whose own docblock names        -->
<!-- useTheme.ts as the anti-pattern it deliberately did NOT copy:                            -->
<!--   createContext<T | null>(null) + a THROWING useX() for writers                          -->
<!--                                 + a NON-THROWING useXOptional() for leaf reads.          -->
<!--   Persistence: a `typeof window` guard + localStorage get/set.                           -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BC-1 and BC-2 — the edge carries the upstream's DECLARED count, and the four connection states</name>
  <read_first>
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:555-567` (`runState?` and its docblock: the canvas derives no run state, reads no step ordinal and imports no vocabulary — **its own suite greps for all three**) and `:1091-1099` in `WorkflowRunPage.tsx` (the mount that supplies it)
    - `frontend/src/components/workflows/FlowEdge.tsx:147` (`DETOUR`), `:178` (`ARC`), `:185` (`LINE`), `:196-204` (the two shipped label constants — the payload label's home)
    - `frontend/src/components/workflows/FlowEdge.test.tsx` (pinned 22) and `WorkflowCanvas.test.tsx` (pinned 53) — ⚠ the latter carries a **`?raw` ESM-cycle fence with inline positive controls, observed RED against a deliberate back-import**: `WorkflowCanvas` imports `FlowEdge`'s VALUE at module scope for the `edgeTypes` map, **so an extracted module must not import back**
    - `.planning/sketches/200-journey-interactive/screens/builder-canvas.html` (the four states drawn as a legend: `at rest` · `selected` · `hovered` · `not taken`; ⚠ it draws **NO** payload label — BC-1 is ledger-derived per §0)
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §3, and §0's LEDGER-DERIVED paragraph
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 18 (`WorkflowCanvas` / `FlowEdge`), IDIOM-1, IDIOM-3
  </read_first>
  <files>frontend/src/components/workflows/FlowEdge.tsx, frontend/src/components/workflows/FlowEdge.test.tsx, frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/components/workflows/WorkflowCanvas.test.tsx</files>
  <action>
    **BC-1 — ONE mechanism, not two (D-08).** Sheet c1's `312 contracts → 48 extracted → 12 flagged` is
    the **upstream step's already-declared count rendered on the connection**. The canvas gets **no
    second counting path and no new fetch**: the edge reads the upstream node's facts through the
    existing `runState?: (slug) => NodeRunState | undefined` seam, and `FlowEdge` already renders labels,
    so the label has a home.

    ⚠ **The canvas must keep deriving nothing.** Its docblock and its own suite fence three things: it
    derives no run state, reads no step ordinal and imports no vocabulary. **The PAGE owns the join, the
    reading and the words.** So the count and its noun arrive through `runState`'s payload; the edge
    renders what it is handed.

    ⚠ **An edge whose upstream step declared NO count renders NO label** (D-07). Not `0`, not a dash, not
    a placeholder. The arm is `typeof count === "number"`, never `count ?? …` and never `if (count)` —
    because a declared `0` (a step that searched and found nothing) is a REAL fact and must render.
    ⚠ **Any slug-keyed lookup you add routes through `own()`** from `./ownProperty` — `workflow_phases.slug`
    is unconstrained `text`, so an author-supplied `constructor` reaches these tables.

    **BC-2 — the four connection states.** Only the shipped one exists today (`199-05`, measured). Express
    `at rest` · `selected` · `hovered` · `not taken` as the sketch's legend draws them. Keep the shipped
    `DETOUR_ARMED_LABEL` / `DETOUR_OPEN_LABEL` wording byte-identical — those two are governed strings
    whose reasons are recorded in their docblocks (`"nobody is asked"` was deliberately reworded from the
    sketch's `"nobody asked"`, which read as a verdict on the author).

    ⚠ **The ESM-cycle constraint is FENCED, not documented.** Do not create a module that `FlowEdge`
    imports and that imports `WorkflowCanvas` back; the fence has been observed RED against exactly that.
  </action>
  <acceptance_criteria>
    - A `FlowEdge.test.tsx` case asserts an edge whose upstream declared a count renders `"<n> <noun>"`, and a second asserts an edge whose upstream declared **nothing** renders **no label element at all** (a DOM absence, not an empty string).
    - A third case asserts a declared `0` renders — `grep -cE '\?\?\s*0|if \(count\)' frontend/src/components/workflows/FlowEdge.tsx` returns 0.
    - `grep -c "own(" frontend/src/components/workflows/FlowEdge.tsx` ≥ 1 if the edge indexes any table by slug; a case asserts a `constructor`-keyed lookup yields no function.
    - `WorkflowCanvas.test.tsx`'s three "derives nothing" greps still pass, and its ESM-cycle fence is green.
    - `DETOUR_ARMED_LABEL` and `DETOUR_OPEN_LABEL` are byte-unchanged: `git diff -U0 -- frontend/src/components/workflows/FlowEdge.tsx | grep -c 'DETOUR_\(ARMED\|OPEN\)_LABEL ='` returns 0.
    - Cases exist for all four connection states.
    - `cd frontend && npx vitest run src/components/workflows/FlowEdge.test.tsx src/components/workflows/WorkflowCanvas.test.tsx --reporter=basic` is green.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test $(grep -cE "\?\?\s*0|if \(count\)" frontend/src/components/workflows/FlowEdge.tsx) -eq 0; test $(git diff -U0 -- frontend/src/components/workflows/FlowEdge.tsx | grep -c "DETOUR_\(ARMED\|OPEN\)_LABEL =") -eq 0; cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/FlowEdge.test.tsx src/components/workflows/WorkflowCanvas.test.tsx --reporter=basic'</automated>
  </verify>
  <done>The connection carries a fact it did not invent, renders nothing when there is no fact, renders `0` when `0` is the fact, and all four states are expressed.</done>
</task>

<task type="auto">
  <name>Task 2: BC-3 and BC-4 — a branch shows its own condition, and the node marks join the line vocabulary</name>
  <read_first>
    - `frontend/src/components/workflows/WorkflowCanvas.tsx` (node rendering; ⚠ `colorMode="dark"` is at **`:1317`**, not `:1250` — the bug report is stale by 67 lines) and `frontend/src/components/workflows/PhaseNodeCard.tsx` (⚠ **the badge budget is a max-2 tuple and a third badge is a TYPECHECK ERROR, not a preference**)
    - `frontend/src/components/workflows/soulData.ts:57-65` (`PHASE_GLYPHS` — **SEVEN keys, TOTAL over the seven shipped phase types**; see §0's X-2 refutation before looking for an eighth)
    - `.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md` §2 and **§4** (the canvas mark table — ⚠ its last row reads *"the **6** workflow phase types"*, stale since 189 added `external_action`: **X-15**)
    - `.planning/sketches/200-journey-interactive/screens/builder-canvas.html` (the branch `Over £2m?` → `Escalate to the risk committee` / `High risk path`, `File as routine` / `Standard path`; small inline line marks)
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §3 and §0 (N-5 — Material Symbols ligatures are not shippable atoms)
    - `frontend/src/components/workflows/definitionOps.ts` / the phase vocabulary (where `on_failure: skip_to_phase:<slug>` is read)
  </read_first>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/components/workflows/WorkflowCanvas.test.tsx, .claude/skills/sketch-findings-agentic-rag/references/icon-convention.md</files>
  <action>
    **BC-3 — a branch node shows its own condition.** `on_failure: skip_to_phase:<slug>` **is already in
    the definition**; this half is frontend-only and was never built. Render the condition on the node in
    the product's business vocabulary — the slug is an identifier, not a sentence, so resolve it to the
    target step's name through the page-owned name context rather than printing a slug.
    ⚠ Route the slug→name lookup through `own()`.
    ⚠ **Respect the node's badge budget** — it is a max-2 tuple and a third badge is a typecheck error.
    If the condition needs a third slot, put it in the node's body, not in a badge.

    **BC-4 — node marks in the line vocabulary.** The sketch uses small inline line marks; the shipped
    canvas draws large filled circles. `icon-convention.md` §1/§2/§4 is the authority:
    - provider/model marks are single-sourced `@lobehub/icons` (§1),
    - phase-type marks are the ONE shared `PHASE_GLYPHS` map (§2) — **which is TOTAL over the seven
      shipped phase types; there is no eighth and no missing `llm_judge_rubric` glyph** (§0's X-2),
    - the canvas mark table is §4.
    ⚠ **No Material Symbols ligature name is a shippable atom** (N-5) — the sketch draws `account_tree`,
    `psychology`, `fit_screen` and seventeen more, and every one is sketch scaffolding.
    ⚠ **A net-new mark is FLAGGED as a proposal in the summary, never passed off as shipped vocabulary.**
    `199-05` recorded that §4 has no row for three shipped marks; if this plan draws a mark §4 does not
    list, add the row — and while §4 is open, **correct its last row from `6` to `7` workflow phase
    types** (X-15).
  </action>
  <acceptance_criteria>
    - A case asserts a phase with `on_failure: skip_to_phase:<slug>` renders the TARGET STEP'S NAME (not the slug) as its condition, and a case asserts a phase with no `on_failure` renders no condition affordance.
    - A case asserts a `constructor`-slugged `skip_to_phase` target resolves to no function and renders no `[object …]`/`function` text.
    - `grep -cE '"(description|bolt|search|check_circle|chevron_right|folder|lock|shield|add|close|info|error|priority_high|sync|psychology|account_tree|summarize|save_as|fit_screen|widgets)"' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0 — no Material Symbols ligature reached the source.
    - `PhaseNodeCard`'s badge tuple is unchanged in arity: `npx tsc -p tsconfig.app.json --noEmit` exits 0.
    - `icon-convention.md` §4's phase-type row reads `7`: `grep -c "the 6 workflow phase types" .claude/skills/sketch-findings-agentic-rag/references/icon-convention.md` returns 0.
    - Any net-new canvas mark has a §4 row added in this commit, and the summary flags it as a proposal.
    - `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx --reporter=basic` is green.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test $(grep -c "the 6 workflow phase types" .claude/skills/sketch-findings-agentic-rag/references/icon-convention.md) -eq 0; test $(grep -cE "\"(account_tree|psychology|fit_screen|priority_high|check_circle)\"" frontend/src/components/workflows/WorkflowCanvas.tsx) -eq 0; cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx --reporter=basic && npx tsc -p tsconfig.app.json --noEmit'</automated>
  </verify>
  <done>A branch names its own condition in business words, the node marks follow the line vocabulary, and §4's stale `6` is now `7`.</done>
</task>

<task type="auto">
  <name>Task 3: `BUG-260813-01` by ThemeProvider (it lands in CHAT first), the canvas fence, the 807/808 fixture, the pins and the ledger</name>
  <read_first>
    - `frontend/src/providers/TechnicalNamesProvider.tsx:11-50` — the shipped provider-shaped precedent, whose own docblock says copying `useTheme.ts` verbatim would be wrong: *"This is NOT a bare per-consumer hook — copying `useTheme.ts` verbatim would give each consumer its OWN useState and the toggles would drift."*
    - `frontend/src/hooks/useTheme.ts` (12 lines of state **per consumer**) and `frontend/src/components/layout/ChatLayout.tsx:31,:111` — ⚠ **`grep -rn "useTheme" frontend/src` returns exactly TWO sites: the def and this ONE consumer. There is no `ThemeProvider`.**
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:1317` (`colorMode="dark"`, hardcoded)
    - `.planning/reported-bugs/BUG-260807-01.md` and `BUG-260808-01.md` — ⚠ **both tails say the code fix LANDED** (`own()` at `editAffordance.ts:540,543` and `WorkflowCanvas.tsx:699,738,942`) and that what is owed is **ONE driven row with a seeded fixture, flipping BOTH**; and *"the UI cannot author this slug"* (`D-184-11`: there is no slug field)
    - `frontend/src/components/workflows/PublishGauntlet.test.tsx:1095-1334` (the role-SET scan proved able to fire)
    - `scripts/vitest-count-gate.cjs:2540-2546` (TARGETS and BASELINE are TWO knobs — ⚠ a file under `src/providers/` needs **BOTH**) and `200-CHECKLIST.md` §6
    - `docs/HOT-FILE-LEDGER.md` (any `##` section — the shape) and CLAUDE.md's scan-list table
    - `.planning/phases/200-the-workflow-journey/200-VALIDATION.md` § Manual-Only Verifications (the two driven rows this task prepares)
  </read_first>
  <files>frontend/src/providers/ThemeProvider.tsx, frontend/src/hooks/useTheme.ts, frontend/src/components/layout/ChatLayout.tsx, frontend/src/main.tsx, frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/components/workflows/WorkflowCanvas.test.tsx, scripts/seed-constructor-slug-workflow.py, scripts/vitest-count-gate.cjs, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>
  <action>
    **A — `BUG-260813-01`, and the honest fix is a provider.** ⚠ **The canvas does NOT consume `useTheme`
    today**, and adding a second `useTheme()` call would fork the state: two `useState`s, both effects
    writing `localStorage` and toggling the root class, and **neither re-rendering the other**. That is
    the report's own analysis. Create `frontend/src/providers/ThemeProvider.tsx` on
    `TechnicalNamesProvider`'s shape — `createContext<T | null>(null)`, a **throwing** `useTheme()` for
    writers and a **non-throwing** `useThemeOptional()` for leaf reads, with persistence kept exactly as
    `hooks/useTheme.ts` does it (a `typeof window` guard plus `localStorage` get/set, and the root
    `dark` class toggle). Convert `ChatLayout.tsx` to consume the provider, mount the provider at the app
    root, and replace `colorMode="dark"` at `WorkflowCanvas.tsx:1317` with the context read.
    - **The non-throwing accessor is REQUIRED** so the canvas still renders in isolation — its four
      suites mount it without any provider, and a throwing hook would red every one of them.
    - ⚠ **STATE IT IN THE SUMMARY: this is a CROSS-SURFACE change that lands in CHAT first.**
      `ChatLayout.tsx` is `useTheme`'s sole consumer and `WorkspacePanel`'s only production mount, so the
      chat shell is touched before any workflow surface. UAT on the workflow surface alone will miss it.

    **B — the `builder-canvas` MUST NOT RENDER fence.** Cover §3's `BC-MNR-*` rows — no payload label on
    an edge whose upstream declared no count; no Material Symbols ligature as visible text; no `0` or dash
    standing in for absence. Use the **role-SET + rendered-text** shape with **two permanent non-vacuity
    controls**: a planted violation asserting the predicate finds every plant, and a deliberate-absence
    case proving it does not fire on honest copy. Assert non-vacuity **before** contents. ⚠ `199-03`
    measured a `?raw` regex AND a `queryAllByRole("button")` filter both passing GREEN against a live
    planted violation — **only the role-SET scan went red**.

    **C — `BUG-260807-01` + `BUG-260808-01`: the driven row, not a repair.** ⚠ Both are **already
    code-fixed** (X-6); CONTEXT's *"budget for it rather than discover it"* over-states the cost. Ship
    `scripts/seed-constructor-slug-workflow.py`, a small seed that inserts a `workflow_definitions` row
    whose **middle phase is slugged `constructor`** — ⚠ **the UI cannot author this slug** (`D-184-11`:
    there is no slug field), which is exactly why a fixture is required. Document in the script's
    docstring the driven row it enables: open the canvas on that definition, read `node.style.transform`
    and the affordance's **computed** transform with the slug swung both ways, and **flip BOTH reports to
    `closed` on that one row**. Also add a jsdom-level case asserting a `constructor`-slugged node renders
    a finite transform (no `NaN`, no `function`), so the class has an automated floor beneath the manual
    row. ⚠ Note the deliberate decline recorded in `200-02`: **`SEED-143` (constraining `slug` at the
    boundary) is NOT taken this phase**; its trigger is *the next phase that opens a `workflow_phases`
    migration for another reason.*

    **D — the pins and the ledger.**
    - Pin every new suite in the commit that creates it. ⚠ `ThemeProvider.tsx` lives under
      `src/providers/`, which the `src/components/workflows` directory entry does **not** sweep — it needs
      **BOTH** a `TARGETS` entry and a `BASELINE` entry.
    - `FlowEdge.tsx` **OWES a hot-file-ledger row** (X-16: `2 / 2 / 378`, below threshold). Add the
      CLAUDE.md row and the `docs/HOT-FILE-LEDGER.md` section in the SAME COMMIT that modifies it, and
      mark it *"listed BELOW the threshold on purpose"* — the ledger's own precedent (`fileIcon.tsx`,
      `useTemplateFirstDraft.ts`). Refresh `WorkflowCanvas.tsx`'s existing row with §6's re-derived triple.
      *A row without a section, or a section without a row, is drift.* Then run
      `node scripts/check-claude-md-size.cjs`.

    Run the phase gate and compare **per file** against §6. ⚠ **`WorkflowBuilderPage.canvas.test.tsx` is
    one of SEED-171's five flaky suites and is this plan's primary page suite.** If the gate reds:
    capture failing filenames from its **own persisted JSON before re-running anything**, check each
    against `git diff --numstat <base> HEAD` and `git status --short`, and if a named file is
    byte-unchanged and one of the five, record it as an observation — say *"provably unmodified"*, never
    *"fine"*. **Never reach for the cap.** ⚠ **Red is sometimes REAL** — `196-08` hit `failed 249` and
    every one was genuine.
  </action>
  <acceptance_criteria>
    - `grep -rn "useTheme(" frontend/src | grep -v providers/ThemeProvider | grep -c "useState"` returns 0 — no consumer forks the state.
    - `grep -c 'colorMode="dark"' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0.
    - `ThemeProvider.tsx` exports both a throwing and a non-throwing accessor; a case mounts `WorkflowCanvas` **without** a provider and asserts it still renders (the four canvas suites do exactly this).
    - A case asserts a theme change broadcast by the provider re-renders the canvas — the property a forked `useTheme` cannot have.
    - The canvas fence ships a planted-violation case asserting a count ≥ 2 plus a deliberate-absence case, both permanent.
    - `scripts/seed-constructor-slug-workflow.py` exists, inserts a middle phase slugged `constructor`, and its docstring names the driven row and the two report ids it flips.
    - `scripts/vitest-count-gate.cjs` gains a `TARGETS` **and** a `BASELINE` entry for the provider suite; every other new suite gains a `BASELINE` entry.
    - `docs/HOT-FILE-LEDGER.md` has a `FlowEdge.tsx` section and CLAUDE.md has a matching row, both in this commit; `node scripts/check-claude-md-size.cjs` exits 0.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`: no per-file decrease against §6; any red is triaged by the procedure above, never by the cap.
    - The summary reports `builder-canvas: N/N atoms` or **names the miss**, states that the theme change lands in CHAT first, and records that 807/808 are owed a driven row rather than a repair.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test $(grep -c "colorMode=\"dark\"" frontend/src/components/workflows/WorkflowCanvas.tsx) -eq 0; test -f scripts/seed-constructor-slug-workflow.py; grep -q "FlowEdge.tsx" docs/HOT-FILE-LEDGER.md; grep -q "FlowEdge.tsx" CLAUDE.md; node scripts/check-claude-md-size.cjs; (cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/FlowEdge.test.tsx --reporter=basic && npx tsc -p tsconfig.app.json --noEmit); GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs'</automated>
  </verify>
  <done>One theme state broadcast to both surfaces, a canvas fence that can fire, a fixture the UI cannot author, and `FlowEdge.tsx`'s row and section landed together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-supplied phase `slug` → canvas geometry and label maps | `workflow_phases.slug` / `PhaseSpec.slug` are unconstrained `text` (`058:18`; `harness.py:202`), so `constructor` / `__proto__` reach object indices that decide transforms |
| run facts → an edge label | A number rendered on a connection reads as a business claim |
| localStorage → theme state | Shared mutable state across two surfaces; a forked hook makes two writers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-06-01 | Tampering | prototype-key slugs on the canvas | mitigate | The class is **already fixed** through `own()` at `WorkflowCanvas.tsx:699,738,942` and `editAffordance.ts:540,543`; this plan adds the **seeded fixture the UI cannot author** plus a jsdom floor case, so `BUG-260807-01` and `BUG-260808-01` can finally be closed on evidence. Every NEW slug-keyed lookup this plan adds also routes through `own()` |
| T-200-06-02 | Spoofing (of fact) | the edge payload label | mitigate | D-08: the label IS the upstream's declared count read through `runState` — the canvas invents no number and opens no fetch. No declaration ⇒ no label, so an absent fact cannot render as `0` |
| T-200-06-03 | Tampering | `SEED-143` (unconstrained `slug` at the boundary) | accept | **Deliberately declined this phase** — a schema + API-surface change stacked on a phase already carrying a behaviour change. Trigger recorded: *the next phase that opens a `workflow_phases` migration for another reason.* The client-side guard (`own()`) is the compensating control |
| T-200-06-04 | Denial of Service | a forked `useTheme` | mitigate | One provider, one state. A second `useState` per consumer means two writers to `localStorage` and the root class with neither re-rendering the other — the measured cause of `BUG-260813-01` |
| T-200-06-05 | Repudiation | an unfired fence | mitigate | Role-SET scan + permanent planted-violation control. `199-03` measured a `?raw` regex and a button scan **both green** against a live violation; only the role-SET scan went red |
| T-200-06-SC | Tampering | npm/pip/cargo installs | mitigate | **No package installs.** `@lobehub/icons` is already a dependency and `icon-convention.md` §1 mandates it; no `package.json` or lockfile is in `files_modified`. RESEARCH carries no `## Package Legitimacy Audit`, so any new package would be `[ASSUMED]` and need a blocking human checkpoint |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/FlowEdge.test.tsx src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.editing.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx --reporter=basic` — green, with SEED-171 triage applied if red.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` exits 0.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — per-file deltas against `200-CHECKLIST.md` §6.
- `node scripts/check-claude-md-size.cjs` exits 0.
- **Owed as driven rows** (recorded in `200-VALIDATION.md`, not claimed here): the light-mode toggle on **both** the builder and the run page including a toggle made in `ChatLayout`; and the `constructor`-slug row that flips `BUG-260807-01` and `BUG-260808-01`.
</verification>

<success_criteria>
- ROADMAP SC#1 for `builder-canvas`: N/N atoms against §3 or a named miss.
- ROADMAP SC#3 (canvas half): the edge label is the **same** declared count, not a second mechanism, and an edge with no declaration renders no label.
- `BUG-260813-01` is closed by one theme state, not by a second hook call.
- `BUG-260807-01` / `BUG-260808-01` have the fixture and the automated floor they need; the driven row is scheduled rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/200-the-workflow-journey/200-06-SUMMARY.md` when done.
</output>
