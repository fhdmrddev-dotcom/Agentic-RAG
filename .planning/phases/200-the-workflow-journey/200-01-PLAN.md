---
phase: 200-the-workflow-journey
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/200-the-workflow-journey/200-CHECKLIST.md
autonomous: true
requirements: [DES-02]

must_haves:
  truths:
    - "D-01 — every in-scope screen (step-panel, builder-spine, builder-canvas, run-surface) has an explicit MUST RENDER / MUST NOT RENDER atom inventory with stable row ids"
    - "D-02 — every atom carries a mechanical verdict derived from the sketch ledger's own colour: blue BUILD, amber-in-slice BUILD, amber-outside REPORT+trigger, green VERIFY"
    - "D-03 — the checklist is committed in a commit whose `git diff --name-only` contains ZERO source files; later plans cite row ids and may not edit it"
    - "D-04 — §0 KNOWN SKETCH DEFECTS opens the document, each defect named with the correction written in, including the five defects RESEARCH added (N-5..N-8, X-14)"
    - "D-14 — the ten G-5 target files' triples are re-derived from git in this plan and published, not read from a ledger cell"
    - "RESEARCH R2 is discharged: BS-4 and BC-1 are recorded as LEDGER-DERIVED, not screen-derived, and each of those two screens carries at least one MUST RENDER atom sourced from its amber row"
    - "RESEARCH R3 is discharged: RS-3 is split into RS-3a (in slice) and RS-3b (REPORT, with a named re-open trigger)"
    - "RESEARCH X-2 is recorded as a REFUTATION: there are seven phase types and seven PHASE_GLYPHS keys, so no later plan hunts a non-existent llm_judge_rubric glyph gap"
    - "The phase's own count-gate + backend baselines are re-derived and published as THE phase baseline that every later plan compares against"
  artifacts:
    - path: ".planning/phases/200-the-workflow-journey/200-CHECKLIST.md"
      provides: "The acceptance bar: §0 defects, four per-screen atom inventories, the REPORT register, the phase baselines"
      contains: "MUST NOT RENDER"
      min_lines: 200
  key_links:
    - from: ".planning/phases/200-the-workflow-journey/200-CHECKLIST.md"
      to: ".planning/sketches/200-journey-interactive/index.html"
      via: "every atom cites the JOURNEY row id (SP-n / BS-n / BC-n / RS-n) it derives from"
      pattern: "(SP|BS|BC|RS)-[0-9]"
---

<objective>
Derive the acceptance bar for Phase 200 — a per-screen `MUST RENDER` / `MUST NOT RENDER` atom
inventory for the four in-scope screens — and commit it **before any source changes exist**.

Purpose: Phase 199 verified 5/5 on its own criteria while the operator's verdict was *"nothing
changed from a UI perspective"*. Both were true, because the criteria were derived alongside the
work. D-03 fixes that by taking the characterization-baseline discipline this repo already trusts
(`WorkflowDoorSwitch.baseline.test.tsx`: *"A baseline taken after the edit proves the edit against
itself"*) and applying it to the acceptance criteria themselves.

Output: `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md`, in a commit that modifies
**zero source files** — `git diff --name-only` over that commit is the proof rather than a promise.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/200-the-workflow-journey/200-CONTEXT.md
@.planning/phases/200-the-workflow-journey/200-RESEARCH.md
@.planning/phases/200-the-workflow-journey/200-VALIDATION.md
@.planning/sketches/200-journey-interactive/README.md
@.planning/sketches/200-journey-interactive/index.html
@.planning/sketches/200-journey-interactive/FORWARD-CHECK.md
@.planning/sketches/200-journey-interactive/COVERAGE.md
@.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md

<interfaces>
<!-- The ledger's own colour map, verbatim from `index.html` immediately after the JOURNEY array. -->
<!-- This IS D-02's resolution table. Executor: do not re-derive it, do not re-word it. -->

var NEEDS = {
  none:     {label:"already shipped", color:"#21C45D"},   // green  → VERIFY, do not rebuild
  frontend: {label:"frontend only",   color:"#A3A5FF"},   // blue   → BUILD
  data:     {label:"NEEDS BACKEND",   color:"#F5A524"},   // amber  → BUILD if inside the slice, else REPORT
  capture:  {label:"not captured",    color:"#6B7383"}    // grey   → no shipped half to compare against
};

<!-- The 16 in-scope JOURNEY rows, already transcribed with their verdicts in
     200-RESEARCH.md §A1. Read them from there rather than re-parsing the 105 KB of HTML. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write §0 KNOWN SKETCH DEFECTS and the phase's REFUTATION register</name>
  <read_first>
    - `.planning/sketches/200-journey-interactive/README.md` (§ "Known nits, carried rather than re-run" — N-1, N-2, N-3)
    - `.planning/phases/200-the-workflow-journey/200-RESEARCH.md` §A2 (the exhaustive eight-defect table), §A3 (X-14), §C16 (X-2), §E-R2, §E-R3, §E-R5 (X-13)
    - `.planning/phases/200-the-workflow-journey/200-CONTEXT.md` D-04 (the four-row source table)
    - `.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md` §1, §2, §4 (the icon authority N-5 defers to)
  </read_first>
  <files>.planning/phases/200-the-workflow-journey/200-CHECKLIST.md</files>
  <action>
    Create the file with frontmatter (`phase: 200`, `slug: the-workflow-journey`, `kind: acceptance-checklist`,
    `derived_from: .planning/sketches/200-journey-interactive/index.html`, `locked: true`) and then §0.

    §0 lists every known defect in the sketch with the correction written in, so that no later plan
    promotes a recorded nit to an acceptance criterion. Use exactly these ids and cover all eight from
    RESEARCH §A2 plus the four refutations below:

    - **N-1** spine fork lanes name the PRE-RENAME steps (`Confirm the QBR before rendering` /
      `Fill the QBR template`) → correction: the current step names.
    - **N-2** draft-arrival footer reads `How long it looks back` twice → correction
      `Open in the builder` / `See the steps`, and mark **NOT APPLICABLE** (that screen is deferred).
    - **N-3** the run surface has **no NOW capture** (`JOURNEY.run-surface.now === null`) → the screen's
      acceptance is the proposal alone; there is no shipped half to compare against.
    - **N-4** connections sheet's nine gstatic `stitch-placeholder-300x300.svg` marks → `@lobehub/icons`
      via `providerLogo.tsx`; mark **NOT APPLICABLE** (connections is a milestone, not a screen).
    - **N-5** ⚠ **every in-scope screen draws Material Symbols ligature names as its icon vocabulary**
      (`description`, `bolt`, `search`, `check_circle`, `chevron_right`, `folder`, `lock`, `shield`,
      `add`, `close`, `info`, `error`, `priority_high`, `sync`, `psychology`, `account_tree`,
      `summarize`, `save_as`, `fit_screen`, `widgets`). Correction: the product's icon authority is
      `icon-convention.md` §1 (`@lobehub/icons` brand marks), §2 (`PHASE_GLYPHS`) and §4 (the canvas
      mark table). **No Material Symbols ligature is a shippable atom** — a checklist row naming one
      would be a defect promoted to a criterion.
    - **N-6** the step-panel screen names three stale model literals (`GPT-4o`, `Claude 3.5 Sonnet`,
      `Llama 3 Instruct`) → models come from the registry (`useModelRegistry` / `ModelField`), never a
      literal. Placeholder text, not atoms.
    - **N-7** the step-panel screen renders `Locked — only the person who locked it can release it`
      with **no person named**, while ledger row SP-4 says `Locked by Alex M.` → derive from the SCREEN
      (which already ships, via 199-06's one-way grounding dial) and REPORT SP-4 as an atom the screen
      itself does not draw.
    - **N-8** the run-surface screen puts the sentence `Summarized meeting notes` in the same slot as the
      count `Found 12 contracts` → under D-07 a step with no real number renders **nothing** in that
      slot. Drawing prose there re-introduces the fabricated-figure failure D-07 exists to prevent.

    Then a **REFUTATIONS** subsection — claims carried by CONTEXT / ROADMAP / FORWARD-CHECK that
    measurement disproved, recorded here so no later plan chases them:

    - **X-2 — the `llm_judge_rubric` "missing glyph" gap DOES NOT EXIST.** `PHASE_GLYPHS`
      (`soulData.ts:57-65`) has SEVEN keys; `PHASE_TYPE_REGISTRY_ENTRIES` (`phase_types.py:2398-2410`)
      and `PhaseConfig`'s discriminated union (`models/harness.py:68,77,93,110,129,154,242`) each have
      SEVEN members. The map is TOTAL. `llm_judge_rubric` is a **`ValidatorSpec.kind`**
      (`models/harness.py:350`, `validator_kinds.py:500`) — a gate attached to a phase, with no node
      and therefore no glyph. The assertion appears in CONTEXT `<specifics>`, in the ROADMAP Phase 200
      entry and in the sketch's FORWARD-CHECK; record all three locations and state that **no later
      plan may open work against it**. Where a judge DOES surface is the step panel's GATES rail, and
      the word for it belongs to `phaseVocabulary` / `definitionOps`, never `PHASE_GLYPHS`.
    - **X-13 — SP-1's "3 of 27 named" is stale.** Re-measure it in this task: read
      `friendlyToolName` in `PhaseFormPanel.tsx` (count the map entries) and `rails.toolOptions`
      (count the offered tool ids), and publish the measured pair as SP-1's real numbers.
    - **X-14 — the sketch README says "the six real captures"; there are SEVEN files** in
      `.planning/sketches/200-journey-now/`. `now-07-chat-panel.png` is referenced by no `JOURNEY`
      row's `now:` field. Note it so nobody hunts a seventh pairing.
    - **X-6 — `BUG-260807-01` and `BUG-260808-01` are ALREADY CODE-FIXED** (`editAffordance.ts:540,543`;
      `WorkflowCanvas.tsx:699,738,942`, all through `own()`). What is owed is **one driven browser row
      with a seeded `constructor`-slugged fixture**, flipping BOTH reports — not a repair.

    Finally an explicit **DERIVATION PROVENANCE** paragraph discharging RESEARCH R2: state that
    **BS-4 (per-step timings + total runtime) and BC-1 (the connection payload label) are
    LEDGER-DERIVED, not screen-derived** — the proposed screens `builder-spine.html` and
    `builder-canvas.html` draw neither — and that their drawing source is sketch **178**'s sheet c3
    col 3 and sheet c1 respectively. These are the two atoms that justify lifting the
    presentation-only fence; a checklist that omits them has dropped the phase's whole argument.
  </action>
  <acceptance_criteria>
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` exists and its first `##` heading is `## §0 KNOWN SKETCH DEFECTS`.
    - `grep -c '^| \*\*N-[1-8]\*\*\|^| N-[1-8]' 200-CHECKLIST.md` ≥ 8 — all eight defects present.
    - The literal tokens `X-2`, `X-6`, `X-13`, `X-14` each appear at least once.
    - The tokens `BS-4` and `BC-1` each appear within 10 lines of the string `LEDGER-DERIVED`.
    - The measured SP-1 pair is written as two integers, not as the inherited `3 of 27`; `grep -c '3 of 27' 200-CHECKLIST.md` returns 0 outside a line that also contains `stale`.
    - `grep -c 'Material Symbols' 200-CHECKLIST.md` ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=.planning/phases/200-the-workflow-journey/200-CHECKLIST.md; test -f "$f" && for t in "§0 KNOWN SKETCH DEFECTS" X-2 X-6 X-13 X-14 "LEDGER-DERIVED" "Material Symbols"; do grep -qF "$t" "$f" || { echo "MISSING: $t"; exit 1; }; done; echo OK'</automated>
  </verify>
  <done>§0 exists with all eight named defects, four refutations, and the R2 provenance paragraph naming BS-4 and BC-1 as ledger-derived.</done>
</task>

<task type="auto">
  <name>Task 2: Write the four per-screen MUST RENDER / MUST NOT RENDER inventories and the REPORT register</name>
  <read_first>
    - `.planning/phases/200-the-workflow-journey/200-RESEARCH.md` §A1 (the 16 in-scope JOURNEY rows transcribed verbatim with their D-02 verdicts), §A4 (what each proposed screen actually draws), §C11 (which component renders each screen)
    - `.planning/sketches/200-journey-interactive/screens/step-panel.html`, `builder-spine.html`, `builder-canvas.html`, `run-surface.html`
    - `.planning/phases/200-the-workflow-journey/200-CONTEXT.md` D-01 (the worked `builder-spine` example), D-02 (the colour→verdict table)
    - `frontend/src/components/workflows/PhaseSpineGraph.tsx:71` (`READ_ONLY_LEGEND` — the exact string BS-1 forbids as a RENDER, while the identifier stays exported)
  </read_first>
  <files>.planning/phases/200-the-workflow-journey/200-CHECKLIST.md</files>
  <action>
    Append §1–§4, one section per screen, each with a stable row id per atom
    (`SP-MR-01`, `SP-MNR-01`, `BS-MR-01`, `BC-MNR-01`, `RS-MR-01`, …). Every row carries:
    `id | atom | source (JOURNEY row id or screen text) | verdict (BUILD/REPORT/VERIFY) | owning plan`.

    **§1 `step-panel` → `PhaseFormPanel.tsx`.** MUST RENDER: the human-named tool phrases (SP-1, BUILD,
    plan `200-04`); the composed `MODEL` / `WHAT IT CAN REACH` / `WHAT IT CHANGES OUTSIDE THIS
    WORKFLOW` card sections (SP-2, BUILD, `200-04`); the `We couldn't load the list of models.`
    absent-registry arm (SP-3, VERIFY — 199-06 already built it, do not rebuild). MUST NOT RENDER:
    raw `snake_case` tool ids in the tool list; any of N-6's three stale model literals; any Material
    Symbols ligature name as visible text.

    **§2 `builder-spine` → `PhaseSpineGraph.tsx`.** MUST RENDER: per-step model name, type badge and
    branch `TRAVERSED`/`SKIPPED` (BS-3, BUILD, `200-05`); **per-step duration and total runtime
    (BS-4, BUILD, amber-in-slice, `200-05`) — flagged LEDGER-DERIVED per §0**. MUST NOT RENDER: the
    `READ_ONLY_LEGEND` string (BS-1) — ⚠ **scan the RENDERED DOM, not the source**, because the
    identifier stays `export`ed at `PhaseSpineGraph.tsx:71` and a source scan would red on the export;
    the per-step `llm_agent` chip and the `phase_index N` line (BS-2), whose replacement is the
    canvas's own words (`AI agent step`); N-1's pre-rename step names.

    **§3 `builder-canvas` → `WorkflowCanvas.tsx` + `FlowEdge.tsx`.** MUST RENDER: the connection
    payload label — the upstream step's declared count (BC-1, BUILD, amber-in-slice, `200-06`),
    **flagged LEDGER-DERIVED**; the four unexpressed connection states (BC-2, BUILD); a branch node's
    own condition, sourced from `on_failure: skip_to_phase:<slug>` which is already in the definition
    (BC-3, BUILD); line-vocabulary node marks rather than large filled circles (BC-4, BUILD).
    MUST NOT RENDER: a payload label on an edge whose upstream step declared **no** count (D-07/D-08
    — an absent count renders no label, never `0` and never a dash); any Material Symbols ligature.

    **§4 `run-surface` → `WorkflowRunPage.tsx` + `panel/PhaseTimeline.tsx` + `PhaseCard.tsx`.**
    MUST RENDER: per-step counts in the live column (RS-2, BUILD); **RS-3a — total runtime plus
    per-step start/finish (BUILD, in slice)**. MUST NOT RENDER: prose in the count slot (N-8); a
    live-ticking clock on a step that never ran; `0` or a dash where a step declared no count.
    Record **RS-1 (no NOW capture)** as grey — the screen has no shipped half, so its acceptance is
    the proposal alone.

    Then §5 **THE REPORT REGISTER** — every atom whose verdict is REPORT, each with a **named re-open
    trigger**, so nothing is faked and nothing is silently dropped:
    - `SP-4` `Locked by Alex M.` — lock-holder attribution is not on the wire. Trigger: *a phase that
      scopes lock ownership.* (See N-7: the screen itself draws the person-less form, which ships.)
    - `SP-5` `Will overwrite 1,200 records` — no row count is computed anywhere; a preflight count is
      a capability, not a label. Trigger: *the connections / approval milestone (`SEED-146`).*
    - **`RS-3b` — the sub-step execution trace.** Discharges RESEARCH R3: the proposed screen draws
      **eight** trace lines for a **five**-step run and several (`Connecting to Northwind CRM
      instance…`, `Analyzing risk factors`) are sub-step events, while this phase's slice is
      phase-level only. Trigger: *the phase that scopes `harness_audit` / `EmitSubStep` as a client
      transport* — a second backend concern, deliberately not stacked here.
    - The **fan-out router** — drawn dashed and tagged `NOT BUILT`; the spine is LINEAR by recorded
      decision. Trigger: *a deliberate revisit of the linear commitment.*
    - `RS-1` the NOW capture — trigger: *a run that can be driven for capture.*

    Close §5 with the **AUTHORED COPY** subsection recording the three per-step count nouns this phase
    ships — `sources` (`llm_agent`), `agents` (`llm_batch_agents`), `fields` (`llm_emit`) — and stating
    that each is authored at exactly one executor site and pinned by one test case, so a later wording
    change is a one-line edit rather than a re-derivation.
  </action>
  <acceptance_criteria>
    - Four `## §` screen sections exist, one per in-scope screen, each containing both the literal strings `MUST RENDER` and `MUST NOT RENDER`.
    - Every atom row carries an id matching `^(SP|BS|BC|RS)-(MR|MNR)-[0-9]{2}$` and an owning plan id matching `200-0[4-7]`.
    - `§2` contains the token `READ_ONLY_LEGEND` on a line that also contains `RENDERED DOM` (the scan-the-DOM-not-the-source instruction).
    - `§3` contains a MUST NOT RENDER row forbidding a label on an edge whose upstream declared no count.
    - §5 contains at least five REPORT rows, and **every one** has a line containing `Trigger:`.
    - `RS-3a` and `RS-3b` both appear, in different verdict columns (BUILD vs REPORT).
    - The three nouns `sources`, `agents`, `fields` all appear in the AUTHORED COPY subsection.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=.planning/phases/200-the-workflow-journey/200-CHECKLIST.md; n=$(grep -oE "(SP|BS|BC|RS)-(MR|MNR)-[0-9]{2}" "$f" | sort -u | wc -l); test "$n" -ge 12 || { echo "only $n atom ids"; exit 1; }; test $(grep -c "MUST NOT RENDER" "$f") -ge 4 || exit 1; test $(grep -c "Trigger:" "$f") -ge 5 || exit 1; for t in RS-3a RS-3b READ_ONLY_LEGEND sources agents fields; do grep -qF "$t" "$f" || { echo "MISSING: $t"; exit 1; }; done; echo OK'</automated>
  </verify>
  <done>Four screen inventories with ≥12 uniquely-id'd atoms, and a REPORT register where every row names its re-open trigger.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive and publish THE phase baselines, then prove the source diff is empty</name>
  <read_first>
    - `.planning/phases/200-the-workflow-journey/200-RESEARCH.md` §D17 (the count-gate figures measured 2026-08-19), §D19 (backend baselines and the one named pre-existing failure), §B5 (the next free migration number), X-16 (the four files owing ledger rows), D-14's ten-file G-5 table
    - `CLAUDE.md` § *Parallel execution* (the `GSD_VITEST_MAX_WORKERS=2` rule and the rot history), § *Hot-file ledger*
    - `scripts/vitest-count-gate.cjs` (TARGETS and BASELINE are TWO knobs)
  </read_first>
  <files>.planning/phases/200-the-workflow-journey/200-CHECKLIST.md</files>
  <action>
    Append §6 **PHASE BASELINES — re-derived, not inherited**. Run each command and paste its verdict
    line **verbatim**; a summary is not a baseline.

    1. From the repo root: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`. Record the
       three-figure verdict line and the `count gate OK — N/N pinned files present` line. RESEARCH
       measured `total 4970 · failed 0 · pinned total 4543 · 96/96` on 2026-08-19; **CLAUDE.md's
       published `4594 · 4328 · 92/92` is the FIFTH rot of this constant and it happened the same
       day the fourth was written.** State explicitly: *every later plan in this phase compares
       against THIS number, not against CLAUDE.md's*, and a **growing** number is the gate working —
       its contract is *no per-file DECREASE* and *zero failing*, never a fixed total.
    2. `backend/venv/Scripts/python.exe -m pytest tests/unit -q` — record the failed/passed line
       (RESEARCH measured `62 failed, 2350 passed`; the 62 are known SEED-era rot, none in the
       workflow cluster).
    3. The wire-slice suite set: `backend/venv/Scripts/python.exe -m pytest tests/test_188_workflow_run_read.py tests/test_workflow_phase_cancel.py tests/test_l01_finish_run_terminal_guard.py tests/test_thread_workflow_endpoint.py tests/test_harness_engine.py tests/test_harness_resume.py -q`.
       ⚠ **Name the one pre-existing failure so it is never attributed to this phase:**
       `test_thread_workflow_endpoint.py::test_thread_workflow_state_shape` (`assert body["locked"] is True`).
       It is IN the blast radius — it asserts the very `ThreadWorkflowState` shape `200-02` widens.
    4. `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — record the exit status.
       ⚠ Note that a bare `tsc --noEmit` checks ZERO files; the `-p tsconfig.app.json` form is the one.
    5. `ls supabase/migrations/ | sort -V | tail -1` — record the highest migration on disk and
       therefore the number `200-02` claims. Note that letter suffixes (`121b`) are **silently skipped**
       by the Supabase CLI.
    6. Re-derive the G-5 triple for each of D-14's ten target files **plus** the four that owe rows
       (X-16: `backend/app/api/workflow_runs.py`, `frontend/src/components/panel/PhaseTimeline.tsx`,
       `frontend/src/components/panel/phaseStatusMeta.ts`, `frontend/src/components/workflows/FlowEdge.tsx`),
       using CLAUDE.md's published recipe (`git log --oneline -- <f> | wc -l`; the phase-bucket sed
       pipeline; `wc -l`). ⚠ **Six-digit buckets are DATED QUICK TASKS, not phases — subtract them.**
       Publish a table and state which plan owns each row's same-commit addition:
       `workflow_runs.py` → `200-02` · `FlowEdge.tsx` → `200-06` · `PhaseTimeline.tsx` +
       `phaseStatusMeta.ts` → `200-07`.
    7. Record the SEED-171 triage procedure verbatim in substance, because **two of its five flaky
       suites (`WorkflowRunPage.test.tsx`, `WorkflowBuilderPage.canvas.test.tsx`) are the primary
       suites of `200-06` and `200-07`**: capture failing filenames from the gate's own persisted JSON
       BEFORE re-running anything; check each against `git diff --numstat <base> HEAD`; **never reach
       for the cap**; and red is sometimes REAL (`196-08` hit `failed 249`, all genuine).

    Then commit. ⚠ **This plan's commit must contain ONLY `.planning/**` paths** (D-03). Before
    committing, run the proof command in the acceptance criteria; if it names any path outside
    `.planning/`, stop and remove that change rather than committing it.
  </action>
  <acceptance_criteria>
    - §6 contains a line matching `count gate OK` and a line matching `pinned total [0-9]+`, both pasted verbatim from the command output.
    - §6 names `test_thread_workflow_endpoint.py::test_thread_workflow_state_shape` as the pre-existing RED-before baseline.
    - §6 contains a fourteen-row (or more) G-5 table and assigns an owning plan to each of the four ledger rows owed.
    - The literal string `never reach for the cap` (or an unambiguous restatement plus the token `SEED-171`) appears in §6.
    - **THE D-03 PROOF:** `git show --name-only --format= HEAD | grep -v '^\.planning/' | grep -c .` returns `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=.planning/phases/200-the-workflow-journey/200-CHECKLIST.md; grep -qF "count gate OK" "$f" || { echo "no gate verdict"; exit 1; }; grep -qF "test_thread_workflow_state_shape" "$f" || { echo "no RED-before baseline"; exit 1; }; grep -qF "SEED-171" "$f" || exit 1; n=$(git show --name-only --format= HEAD | grep -v "^\.planning/" | grep -c . || true); test "$n" -eq 0 || { echo "D-03 VIOLATED: $n non-planning path(s) in HEAD"; git show --name-only --format= HEAD | grep -v "^\.planning/"; exit 1; }; echo OK'</automated>
  </verify>
  <done>The phase baselines are published verbatim, the four ledger rows are assigned to owning plans, and `git show --name-only HEAD` contains zero paths outside `.planning/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sketch HTML → acceptance criteria | Untrusted prose (placeholder copy, stale literals, foreign icon vocabulary) crosses into a document that later plans treat as binding |
| planner → later plans | A wrong atom here is executed faithfully by four downstream plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-01-01 | Spoofing (of provenance) | `200-CHECKLIST.md` atoms | mitigate | Every atom cites its `JOURNEY` row id or the screen text it was extracted from; BS-4 and BC-1 are explicitly marked LEDGER-DERIVED so a reader cannot mistake a ledger note for a drawn atom (R2) |
| T-200-01-02 | Tampering | the checklist after the fact | mitigate | D-03: committed before any source change; `git show --name-only HEAD` proves the empty source diff; later plans cite row ids and may not edit it — a moved atom is a deviation record, never a quiet edit |
| T-200-01-03 | Repudiation | the phase baselines | mitigate | Verdict lines pasted **verbatim** from command output, never summarised; the pre-existing RED backend case is named so it can never be re-attributed |
| T-200-01-04 | Information Disclosure | none | accept | This plan writes only planning documentation; it reads no secrets and touches no runtime surface |
| T-200-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No package installs in this plan.** No `package.json`, `requirements.txt` or lockfile is modified; the acceptance criterion `git show --name-only HEAD` outside `.planning/` = 0 makes an install structurally impossible here |
</threat_model>

<verification>
- `git show --name-only --format= HEAD` lists only `.planning/**` paths (D-03, the plan's headline proof).
- The checklist carries ≥12 uniquely-id'd atoms across four screens, each with a BUILD/REPORT/VERIFY verdict traceable to the ledger's own colour (D-02).
- Every REPORT row names a re-open trigger (D-02's *"never faked, never silently dropped"*).
- §0 records all eight sketch defects and the four refutations; `llm_judge_rubric` is recorded as a NON-gap.
- §6 publishes this phase's own count-gate and backend baselines, re-derived rather than inherited.
</verification>

<success_criteria>
- `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` exists, is committed, and its commit modifies zero source files.
- ROADMAP SC#1's instrument exists: every in-scope screen can now report `N/N atoms` or **name the miss**, and the `MUST NOT RENDER` half is as enumerable as the `MUST RENDER` half.
- Plans `200-02`…`200-07` can cite row ids without re-reading 105 KB of sketch HTML.
</success_criteria>

<output>
Create `.planning/phases/200-the-workflow-journey/200-01-SUMMARY.md` when done.
</output>
