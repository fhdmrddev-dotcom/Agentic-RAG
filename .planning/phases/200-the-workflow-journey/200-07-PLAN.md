---
phase: 200-the-workflow-journey
plan: 07
type: execute
wave: 7
depends_on: ["200-01", "200-02", "200-05", "200-06"]
files_modified:
  - frontend/src/components/panel/phaseStatusMeta.ts
  - frontend/src/components/panel/phaseStatusMeta.test.ts
  - frontend/src/components/panel/PhaseCard.tsx
  - frontend/src/components/panel/PhaseCard.test.tsx
  - frontend/src/components/panel/PhaseTimeline.tsx
  - frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx
  - frontend/src/pages/WorkflowRunPage.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [DES-02]

must_haves:
  truths:
    - "D-01 — the `run-surface` screen reports N/N atoms against `200-CHECKLIST.md` §4 or NAMES the miss"
    - "D-06 — the panel and the run page render the SIX distinct arms; `skipped` (never ran) and a historic row (time not recorded) are provably different renders, and a boolean cannot express the set"
    - "D-07 — a per-step count renders only where the phase type declared one; a type that declared nothing renders NOTHING in that slot — never `0`, never a dash, and never prose (N-8)"
    - "D-09 — the receipt built in `200-05` mounts HERE, on the run page, never on the builder — which keeps 199-02's refusal intact by construction"
    - "D-17 — the no-previewer fence on the run page STANDS, and Phase 195's shipped deliverable listing is a GREEN row: verified, not rebuilt"
    - "`BUG-260610-01`'s timer half is closed STRUCTURALLY: the elapsed reading derives from server timestamps, so it cannot reset on navigation. ⚠ Its duplicate-avatar half is NOT taken and the report's `status: open` must NOT be flipped"
    - "The run page and the chat panel agree on every duration, because `200-02` widened both transports"
  artifacts:
    - path: "frontend/src/components/panel/phaseStatusMeta.test.ts"
      provides: "The first dedicated suite for the panel's status vocabulary — previously UNPINNED while being D-06's natural home"
    - path: "frontend/src/pages/WorkflowRunPage.tsx"
      provides: "Total runtime + per-step start/finish (RS-3a), the mounted receipt, and a navigation-proof elapsed"
  key_links:
    - from: "frontend/src/pages/WorkflowRunPage.tsx"
      to: "frontend/src/components/workflows/RunReceipt.tsx"
      via: "the mount — the page already holds run + definition in one fetch"
      pattern: "RunReceipt"
    - from: "frontend/src/components/panel/PhaseCard.tsx"
      to: "frontend/src/components/workflows/phaseDuration.ts"
      via: "the shared resolver — the panel grows no second duration path"
      pattern: "phaseDuration"
---

<objective>
Build the `run-surface` screen to `200-CHECKLIST.md` §4: per-step counts in the live column, a real
total runtime and real per-step start/finish, D-06's six distinct renders, and the receipt from
`200-05` mounted where it belongs.

Purpose: this is the screen the whole phase exists for. It is also the only in-scope screen with
**no NOW capture** — `JOURNEY.run-surface.now` is literally `null`, so its acceptance is the proposal
alone. And it is where the phase's honesty rules are most easily broken: a live-ticking clock on a
run that ended, prose in a count slot, or a `0` standing in for an absence.

Output: the panel half (`PhaseTimeline` / `PhaseCard` / `phaseStatusMeta`) and the page half agreeing
on the same facts, the receipt mounted, and the two ledger rows those panel files owe.

⚠ `claimed_at` is **null on 100% of completed runs** (149 rows, 0 with `claimed_at` —
`WorkflowRunPage.tsx:849-851`), so the phase timestamps are not a nicety here: they are the first
honest total runtime this product has ever had. This plan is the ROADMAP's `200-06`, shifted by one.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-the-workflow-journey/200-CHECKLIST.md
@.planning/phases/200-the-workflow-journey/200-PATTERNS.md
@.planning/phases/200-the-workflow-journey/200-RESEARCH.md
@.planning/phases/200-the-workflow-journey/200-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@.planning/sketches/200-journey-interactive/screens/run-surface.html
@.planning/reported-bugs/BUG-260610-01.md
@frontend/src/components/workflows/phaseDuration.ts
@frontend/src/components/workflows/receiptVocabulary.ts
@frontend/src/components/workflows/RunReceipt.tsx

<interfaces>
<!-- Both transports now carry these (200-02). The page reads one, the panel reads the other,
     and they must agree. FETCH IS AUTHORITATIVE; the SSE emit is the live tick's anchor only. -->

WorkflowRunPhase   (api.ts, from GET /workflow-runs/{id})
WorkflowPhaseState (models/thread.py, from GET /threads/{id}/workflow)
  slug · phase_index · status · phase_type
  started_at?: string | null
  completed_at?: string | null
  step_count?: number | null    // a REAL integer INCLUDING 0; null ⇒ the type declares no count
  step_noun?: string | null

<!-- The ONE resolver (200-05). The panel and the page both import it; neither re-derives. -->
import { phaseDurationFacts } from "@/components/workflows/phaseDuration"
<!-- The ONE elapsed formatter. The tree carried THREE before 194.1-06 hoisted it. -->
import { fmtElapsed } from "@/lib/fmtElapsed"

<!-- The TOTAL status lookup already in the panel — phaseStatusMeta.ts:177-180 -->
export function statusMeta(status: Phase["status"]): StatusMeta {
  if (!Object.prototype.hasOwnProperty.call(STATUS_META, status)) return STATUS_META.unknown
  return STATUS_META[status]
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The panel half — D-06's arms and RS-2's counts in `phaseStatusMeta` / `PhaseCard` / `PhaseTimeline`, and the suite that module never had</name>
  <read_first>
    - `frontend/src/components/panel/phaseStatusMeta.ts` — the **9-member** status vocabulary (`pending/running/done/failed/retrying/skipped/recorded-not-sent/unknown/cancelled`), `statusMeta()`'s `hasOwnProperty` guard at `:177-180`, `statusWord()` at `:204`. ⚠ **It has NO dedicated suite and is UNPINNED**, while being D-06's natural home
    - `frontend/src/components/panel/PhaseCard.tsx` (pinned 27) and `frontend/src/components/panel/PhaseTimeline.tsx` (pinned 17; `<ol>/<li>/<PhaseCard>` at `:258`, aria-busy, the doing-now line; **sole mount `WorkspacePanel.tsx:542`**)
    - `frontend/src/components/workflows/phaseDuration.ts` (`200-05`'s resolver — the panel imports it and derives nothing new)
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §4 and §0 (N-8 — prose must not occupy the count slot)
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 18 (the panel trio), IDIOM-2, IDIOM-3
    - `.planning/phases/200-the-workflow-journey/200-RESEARCH.md` §B4 (the inherited residual: an `active` phase under a run that ends `failed`)
  </read_first>
  <files>frontend/src/components/panel/phaseStatusMeta.ts, frontend/src/components/panel/phaseStatusMeta.test.ts, frontend/src/components/panel/PhaseCard.tsx, frontend/src/components/panel/PhaseCard.test.tsx, frontend/src/components/panel/PhaseTimeline.tsx, frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx</files>
  <action>
    Render D-06's arms and RS-2's counts on the panel, importing `phaseDuration.ts` from `200-05` rather
    than growing a second derivation. ⚠ **The panel keeps the split it already has:** the vocabulary
    module owns WORDS and holds no derivation; the resolver owns the DECISION and holds no words.

    The six arms, plus the seventh `200-05` added:
    `pending` → nothing · `skipped` → nothing (**never ran** — correct silence) · `active` → ticks from
    `started_at` · `completed` → a duration · `cancelled` → ran Ns, interrupted · **historic
    (`completed`, both timestamps NULL) → `time not recorded`** — a DIFFERENT render from *never ran* ·
    and `active` on a **terminal** run → `did not finish`, because `harness_engine.py:1698-1706` records
    verbatim that such a row is *"PRE-EXISTING and inherited"* and a live clock on an ended run
    re-creates the `BUG-260610-01` symptom on the very surface this phase is building.

    ⚠ **`statusWord`'s WR-05 lesson binds any new word:** never interpolate an internal status member
    into user-visible copy — route through the vocabulary table. ⚠ **Keep `statusMeta()` TOTAL** — its
    `hasOwnProperty` guard with an explicit `unknown` row is the pattern; do not replace it with
    `STATUS_META[status] ?? …`, which returns a function for `constructor`.

    **RS-2 — per-step counts in the live column.** Render `step_count` + `step_noun` where the type
    declared them. ⚠ **A step that declared nothing renders NOTHING in that slot — never `0`, never a
    dash, and never prose.** N-8 is the trap: the sketch's own run surface puts the sentence
    `Summarized meeting notes` in the same slot as `Found 12 contracts`, and drawing prose there
    re-introduces the fabricated-figure failure D-07 exists to prevent. The arm is
    `typeof step_count === "number"`.

    **Give `phaseStatusMeta.ts` its first dedicated suite.** It is the module in this phase's blast
    radius with no direct guard, and the count gate's own §187-29 correction says why that matters:
    *"a pinned TOTAL rising proves nothing about the NEW cases, because slack inside an already-listed
    file absorbs them."* Cover: totality over every one of the nine members, the `unknown` fallback, a
    `constructor` key resolving no function, and that no word is interpolated from a raw status member.

    ⚠ **`WorkspacePanel` is a CROSS-SURFACE shell with exactly one production mount —
    `ChatLayout.tsx:673`.** No workflow page mounts it, so **a change to the panel lands in CHAT first**
    and UAT on the workflow surface alone will miss it. Say so in the summary.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/panel/phaseStatusMeta.test.ts` exists with ≥ 6 cases, including a totality case over all nine members and a `constructor`-key case.
    - `grep -cE 'STATUS_META\[[^\]]+\]\s*\?\?' frontend/src/components/panel/phaseStatusMeta.ts` returns 0 — the `hasOwnProperty` guard is intact.
    - `PhaseCard.test.tsx` gains cases asserting: `skipped` and a historic `completed` produce **different** renders; a no-count step renders **no** count slot; a declared `0` renders; and an `active` phase under a terminal run does **not** render a ticking clock.
    - `grep -cE '\?\?\s*0|if \(step_count\)' frontend/src/components/panel/PhaseCard.tsx` returns 0.
    - `PhaseCard.tsx` and `PhaseTimeline.tsx` import `phaseDuration` and define no local duration derivation (`grep -cE 'new Date\(.*\)\s*-\s*new Date\(' ` on both returns 0).
    - `cd frontend && npx vitest run src/components/panel/phaseStatusMeta.test.ts src/components/panel/PhaseCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx --reporter=basic` is green.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test -f frontend/src/components/panel/phaseStatusMeta.test.ts; test $(grep -cE "STATUS_META\[[^]]+\]\s*\?\?" frontend/src/components/panel/phaseStatusMeta.ts) -eq 0; test $(grep -cE "\?\?\s*0|if \(step_count\)" frontend/src/components/panel/PhaseCard.tsx) -eq 0; cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/phaseStatusMeta.test.ts src/components/panel/PhaseCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx --reporter=basic'</automated>
  </verify>
  <done>The panel renders all seven arms with `never ran` and `time not recorded` provably distinct, counts appear only where declared, and the status vocabulary finally has its own suite.</done>
</task>

<task type="auto">
  <name>Task 2: The page half — RS-3a's total runtime and per-step start/finish, the mounted receipt, and `BUG-260610-01` closed structurally</name>
  <read_first>
    - `frontend/src/pages/WorkflowRunPage.tsx:735-889` — the run band and ⚠ **the mount-anchored timer at `:845-877`** (`const elapsed = useMemo(...)`, `fmtElapsed(nowMs - anchorMs)`), which is `BUG-260610-01`'s cause; `:849-851` (the measured `claimed_at` figures); `:68` (the ONE `@/lib/fmtElapsed` import); `:1091-1099` (the canvas mount with `runState`); `:1143-1177` (the `FileRow` deliverables at `density="run"`)
    - `frontend/src/pages/WorkflowRunPage.test.tsx` — pinned 108, ⚠ **one of SEED-171's five flaky suites**, and it carries **the active no-previewer fence** (*"promises no preview: the previewer is neither imported nor named"*) with its reason recorded verbatim
    - `frontend/src/components/workflows/RunReceipt.tsx` + `phaseDuration.ts` + `receiptVocabulary.ts` (`200-05`'s outputs)
    - `.planning/reported-bugs/BUG-260610-01.md` — ⚠ its frontmatter says `status: open` **DELIBERATELY**: the duplicate-avatar half is live, and a `folded` status would hide it from the routing scan. **Do not flip it.**
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §4 (RS-3a) and §5 (RS-3b, RS-1)
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 18 (`WorkflowRunPage`)
  </read_first>
  <files>frontend/src/pages/WorkflowRunPage.tsx, frontend/src/pages/WorkflowRunPage.test.tsx</files>
  <action>
    **RS-3a — total runtime and per-step start/finish.** Derive the run span from the phase rows —
    `min(started_at) → max(completed_at)` — rather than from `claimed_at`, which is **null on 100% of
    completed runs**. Use `phaseDuration.ts` for the arms and `@/lib/fmtElapsed` for the formatting;
    **no fourth elapsed formatter** — the tree carried three before 194.1-06 hoisted this one.

    **`BUG-260610-01`, the timer half — closed STRUCTURALLY, not patched.** The shipped elapsed is
    anchored at component MOUNT (`:845-877`), which is exactly why navigating away and back restarts it.
    Re-anchor it on the server-supplied `started_at` so **a duration derived from a server timestamp
    cannot reset**. ⚠ **Do NOT flip the report's `status: open`** — its duplicate-avatar half is live and
    a `folded` status would hide it from the reported-bugs routing scan. Record the split in the summary
    instead. ⚠ The driven confirmation (navigate away from a running workflow and back; the elapsed
    **continues**) is a manual row already recorded in `200-VALIDATION.md`.

    **Mount the receipt (D-09).** `RunReceipt` was built in `200-05` and deliberately mounted nowhere.
    Mount it here — the page already holds the run and the definition in one fetch — and **never on
    `WorkflowBuilderPage`**, which is what keeps `199-02`'s refusal intact by construction.

    **FETCH IS AUTHORITATIVE.** Reconcile the phase rows on (re)connect and on mount; treat the SSE
    `phase_started` / `phase_completed` payloads as the live tick's anchor, not as truth. Reasons, each
    measured: CLAUDE.md's D-v2.5-03; `196`'s live-SSE-lock failure, where a gate with no fetch reconcile
    **hid a shipped control**; and `PhaseTimeline`'s reconcile floor, which exists precisely because a
    terminal run has no stream.

    **D-17 — VERIFY, do not rebuild.** ⚠ **The no-previewer fence STANDS.** Its recorded reason:
    *"DOCX/PPTX/XLSX/PDF are download-only by decision and the template engine emits .docx, so the
    flagship deliverable is exactly the artefact that cannot be shown in place. Req 7 asks that it be
    listed and downloadable — not that it be rendered."* **Mounting `FilePreview` would break a tested
    decision.** Phase 195 already shipped the listing and the download (`:63`, `:625`, `:1143-1177`, both
    empty-state strings), so under D-02 this is a **green row: assert it still renders and move on.**
    ⚠ **The two empty-state strings must stay byte-identical**, each pinned at exactly one occurrence.
    ⚠ `SEED-148`'s frontmatter correction is **already done** (X-9) — do not re-plan it.
  </action>
  <acceptance_criteria>
    - A case asserts the run's total runtime is computed from the phase timestamps and is **unchanged across a remount** (render, unmount, re-render with the same props ⇒ the same elapsed), which is `BUG-260610-01`'s timer half.
    - `grep -cE 'function fmt|padStart\(2' frontend/src/pages/WorkflowRunPage.tsx` returns 0 — no fourth formatter.
    - `grep -c "RunReceipt" frontend/src/pages/WorkflowRunPage.tsx` ≥ 1 and `grep -c "RunReceipt" frontend/src/pages/WorkflowBuilderPage.tsx` returns 0.
    - The **no-previewer fence passes unedited**: `git diff -U0 -- frontend/src/pages/WorkflowRunPage.test.tsx | grep -c 'promises no preview'` returns 0, and `grep -c "FilePreview" frontend/src/pages/WorkflowRunPage.tsx` returns 0.
    - The two Phase-195 empty-state strings are byte-unchanged and each still occurs exactly once.
    - A case asserts a fetch reconcile overwrites a stale SSE-derived phase state (fetch authoritative).
    - `.planning/reported-bugs/BUG-260610-01.md` is **not** in this plan's diff.
    - `cd frontend && npx vitest run src/pages/WorkflowRunPage.test.tsx --reporter=basic` is green and `npx tsc -p tsconfig.app.json --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test $(grep -c "RunReceipt" frontend/src/pages/WorkflowBuilderPage.tsx) -eq 0; test $(grep -c "FilePreview" frontend/src/pages/WorkflowRunPage.tsx) -eq 0; test -z "$(git diff --numstat -- .planning/reported-bugs/BUG-260610-01.md)"; cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowRunPage.test.tsx --reporter=basic && npx tsc -p tsconfig.app.json --noEmit'</automated>
  </verify>
  <done>The run span comes from the server, the elapsed survives a remount, the receipt is mounted on the run page only, and the no-previewer fence stands unedited.</done>
</task>

<task type="auto">
  <name>Task 3: The `run-surface` fence, the two owed ledger rows, the pins, and the phase's final N/N report</name>
  <read_first>
    - `frontend/src/components/workflows/PublishGauntlet.test.tsx:1095-1334` (the role-SET scan **proved able to fire**, with its two permanent non-vacuity controls)
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §4 (the `RS-MNR-*` rows), §5 (RS-3b and RS-1 with their triggers), §6 (this phase's baselines and the row-ownership assignments)
    - `scripts/vitest-count-gate.cjs:2540-2546` (TARGETS and BASELINE are TWO knobs; `PhaseTimeline.test.tsx` pinned 17, `PhaseCard.test.tsx` 27, `WorkflowRunPage.test.tsx` 108)
    - `docs/HOT-FILE-LEDGER.md` (any `##` section — the shape: a re-derived triple, `### Phases touched`, `### G-5 status`) and CLAUDE.md's scan-list table
    - `.planning/phases/200-the-workflow-journey/200-RESEARCH.md` X-16 (⚠ **`PhaseTimeline.tsx` measures `7 / 5 / 267` — G-5 FIRES and it has NO ROW**; `phaseStatusMeta.ts` is `2 / 2 / 206`, below threshold and also unlisted) and §D20 (SEED-171)
    - `.planning/phases/200-the-workflow-journey/200-VALIDATION.md` (the manual rows and the four UAT axes)
  </read_first>
  <files>frontend/src/pages/WorkflowRunPage.test.tsx, frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx, scripts/vitest-count-gate.cjs, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>
  <action>
    **A — the `run-surface` MUST NOT RENDER fence.** Cover §4's `RS-MNR-*` rows: **no prose in the count
    slot** (N-8); **no live-ticking clock on a step that never ran** and none on a terminal run; **no `0`
    and no dash** where a step declared no count; no Material Symbols ligature as visible text. Use the
    role-SET + rendered-text shape with **two permanent non-vacuity controls** — a planted violation that
    renders a prose count, a ticking clock on a `skipped` step and a bare dash, asserting the predicate
    finds **all three**; and a deliberate-absence case proving it does not fire on the honest shipped
    copy. Assert non-vacuity **before** contents. ⚠ `199-03` measured a `?raw` regex AND a
    `queryAllByRole("button")` filter both passing GREEN against a live planted violation — **only the
    role-SET scan went red.**

    **B — the REPORT rows.** Record **RS-3b** (the sub-step execution trace) and **RS-1** (no NOW capture)
    as REPORTED, each with the trigger `200-CHECKLIST.md` §5 already names — the trace's substrate would
    be `harness_audit` events / `EmitSubStep`, a second backend concern deliberately not stacked here;
    the capture needs a live run to photograph honestly. ⚠ The proposed screen draws **eight** trace lines
    for a **five**-step run and several are sub-step events, so a plan that built RS-3 whole would be
    building a second backend concern under a UI atom's name.

    **C — the two owed ledger rows (X-16).** ⚠ **`PhaseTimeline.tsx` measures `7 / 5 / 267` — G-5 FIRES
    and it has NO ROW AT ALL**, which is the `WorkflowsPage.tsx`-for-ten-phases failure recurring: *a hot
    file missing from the scan list is permanently invisible to its own guardrail.* Add its CLAUDE.md row
    and its `docs/HOT-FILE-LEDGER.md` section **in the SAME COMMIT** that modifies it, with the triple
    re-derived in §6 and its binding invariants (sole mount `WorkspacePanel.tsx:542`; the cross-surface
    property — a change here lands in CHAT first; the reconcile floor that exists because a terminal run
    has no stream). Do the same for **`phaseStatusMeta.ts`** (`2 / 2 / 206`) and mark it *"listed BELOW
    the threshold on purpose"* — the ledger's own precedent — with its invariant: **`statusMeta()` must
    stay TOTAL over the nine members and must never become `TABLE[key] ?? …`**. Refresh
    `WorkflowRunPage.tsx`'s existing row and section with §6's triple. Then run
    `node scripts/check-claude-md-size.cjs`.

    **D — the pins.** Add a `BASELINE` entry for `phaseStatusMeta.test.ts` **in this commit** and check
    **both knobs** — panel files are listed individually in `TARGETS`, not swept by a directory entry, so
    a new panel suite needs both. Raise the pins for `PhaseCard.test.tsx`, `PhaseTimeline.test.tsx` and
    `WorkflowRunPage.test.tsx` to their new actuals. ⚠ **A raise necessarily DELETES a line; an ADD is
    `+n / -0`** — a `grep -c '^-[^-]'` expecting 0 is wrong for the raises and right for the add.

    **E — the phase's closing report.** Run the full gate and the backend suite, compare **per file**
    against §6, and report `run-surface: N/N atoms` against §4 — or **name the miss**. Then state, across
    all four screens, the phase-level totals: atoms BUILT, atoms VERIFIED, atoms REPORTED with triggers,
    and any atom that moved (which is a **deviation record**, never a quiet edit).
    ⚠ **`WorkflowRunPage.test.tsx` is one of SEED-171's five flaky suites and is this plan's primary
    suite.** If the gate reds: capture failing filenames from the gate's **own persisted JSON before
    re-running anything**, check each against `git diff --numstat <base> HEAD` and `git status --short`,
    and if a named file is byte-unchanged and one of the five, record it as an observation — say
    *"provably unmodified"*, never *"fine"*. **Never reach for the cap.** ⚠ **Red is sometimes REAL** —
    `196-08` hit `failed 249` because nine `@/lib/api` mock factories did not declare a newly-added
    export; `200-02`'s type-only grep is what keeps that unreachable here, and it should be re-run once
    at the phase close.
  </action>
  <acceptance_criteria>
    - The fence's planted-violation case asserts a count ≥ 3 and is committed permanently; the deliberate-absence case stays green.
    - Cases exist recording RS-3b and RS-1 as REPORTED, each naming its re-open trigger.
    - `docs/HOT-FILE-LEDGER.md` contains `## \`frontend/src/components/panel/PhaseTimeline.tsx\`` and `## \`frontend/src/components/panel/phaseStatusMeta.ts\`` sections, and CLAUDE.md's scan-list table contains a row for each — all in this commit.
    - `node scripts/check-claude-md-size.cjs` exits 0.
    - `scripts/vitest-count-gate.cjs` has a `TARGETS` and a `BASELINE` entry for `phaseStatusMeta.test.ts`, and the three raised pins equal their suites' actual counts.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root: **no per-file decrease** against §6 and `failed 0`, or a triaged observation naming provably-unmodified SEED-171 suites.
    - `backend/venv/Scripts/python.exe -m pytest tests/unit -q` shows no new failures against §6.
    - `git diff -U0 -- frontend/src/lib/api.ts | grep -E '^\+' | grep -E '^\+\s*export\s+(const|function|class|let|var|enum)\b'` over the whole phase prints nothing (D-15's trigger still unfired).
    - The summary reports `run-surface: N/N atoms` **and** the phase-level BUILT / VERIFIED / REPORTED totals across all four screens.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; grep -q "PhaseTimeline.tsx" docs/HOT-FILE-LEDGER.md; grep -q "phaseStatusMeta.ts" docs/HOT-FILE-LEDGER.md; grep -q "PhaseTimeline.tsx" CLAUDE.md; node scripts/check-claude-md-size.cjs; GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs; (cd backend && venv/Scripts/python.exe -m pytest tests/unit -q --no-header)'</automated>
  </verify>
  <done>The run-surface fence can fire, both owed ledger rows landed with their sections, every pin is current, and the phase closes with an N/N (or named-miss) report across all four screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server phase rows → rendered claims about a run | Durations and counts become statements a person acts on |
| SSE stream vs fetch reconcile | Two sources for the same rows; one must be authoritative or the UI can show a stale truth |
| unconstrained `slug` → panel status/label maps | `workflow_phases.slug` is bare `text`; a `constructor` slug reaches keyed lookups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-07-01 | Spoofing (of fact) | the live count slot | mitigate | D-07 + N-8: a step that declared no count renders **nothing** — never `0`, never a dash, never prose. The arm is `typeof step_count === "number"`, and a fence plants all three violations and watches them go red |
| T-200-07-02 | Spoofing (of fact) | a ticking clock on a dead run | mitigate | The seventh D-06 arm: `active` on a **terminal** run renders `did not finish`. The residual is inherited (`harness_engine.py:1698-1706`, verbatim) and would otherwise re-create `BUG-260610-01`'s symptom on this screen |
| T-200-07-03 | Tampering | `statusMeta()` and any new slug-keyed map | mitigate | The shipped `hasOwnProperty` guard with an explicit `unknown` row stays; `STATUS_META[key] ?? …` is forbidden and asserted, because `constructor` returns a function |
| T-200-07-04 | Information Disclosure | the deliverables list | mitigate | **D-17: the no-previewer fence STANDS** — `FilePreview` is neither imported nor named, and the shipped listing/download from Phase 195 is verified, not rebuilt. No new file-content path is opened on this surface |
| T-200-07-05 | Repudiation | a stale UI state | mitigate | **FETCH AUTHORITATIVE** (D-v2.5-03), with a case asserting a reconcile overwrites a stale SSE-derived state. `196` measured a live-SSE lock with no reconcile **hiding a shipped control** |
| T-200-07-06 | Repudiation | an invisible hot file | mitigate | `PhaseTimeline.tsx` FIRES G-5 with **no ledger row at all**; the row and the section land in this commit, and `phaseStatusMeta.ts` is listed below threshold on purpose |
| T-200-07-SC | Tampering | npm/pip/cargo installs | mitigate | **No package installs.** No dependency manifest in `files_modified`; every atom composes shipped modules and `200-05`'s leaves. RESEARCH carries no `## Package Legitimacy Audit`, so any package would be `[ASSUMED]` and need a blocking human checkpoint |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/phaseStatusMeta.test.ts src/components/panel/PhaseCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx src/pages/WorkflowRunPage.test.tsx --reporter=basic` — green.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` exits 0.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` and `backend/venv/Scripts/python.exe -m pytest tests/unit -q` — both compared **per file** against `200-CHECKLIST.md` §6.
- `node scripts/check-claude-md-size.cjs` exits 0.
- **Owed as driven rows** (`200-VALIDATION.md`, not claimed here): the elapsed continuing across a real route change; the answer-after-pause resume; the light-mode toggle on both surfaces; the `constructor`-slug canvas row. Plus the four UAT axes — the **full native roster + OpenRouter (8 rows, derived from `MODEL_CAPABILITIES`, blocked rows recorded ⛔ with their reason and blocking id, never omitted)**, multi-tool, parallel-thread and long-message.
</verification>

<success_criteria>
- ROADMAP SC#1 for `run-surface`: N/N atoms against §4 or a named miss, and the phase-level totals reported across all four screens.
- ROADMAP SC#2 (render half): the run surface and the spine render a real per-step duration and a real total runtime, with `never ran` and `time not recorded` provably distinct.
- ROADMAP SC#3 (render half): a count appears only where the type declared one; nothing renders where none was declared.
- `BUG-260610-01`'s timer half is closed structurally; its duplicate-avatar half is explicitly NOT taken and the report stays `open`.
- D-17 is honoured: the shipped deliverable listing is verified as a green row and the no-previewer fence stands.
</success_criteria>

<output>
Create `.planning/phases/200-the-workflow-journey/200-07-SUMMARY.md` when done.
</output>
