---
phase: 200-the-workflow-journey
plan: 05
type: execute
wave: 5
depends_on: ["200-01", "200-02", "200-04"]
files_modified:
  - frontend/src/components/workflows/phaseDuration.ts
  - frontend/src/components/workflows/phaseDuration.test.ts
  - frontend/src/components/workflows/receiptVocabulary.ts
  - frontend/src/components/workflows/receiptVocabulary.test.ts
  - frontend/src/components/workflows/PhaseSpineGraph.tsx
  - frontend/src/components/workflows/PhaseSpineGraph.test.tsx
  - frontend/src/components/workflows/RunReceipt.tsx
  - frontend/src/components/workflows/RunReceipt.test.tsx
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [DES-02]

must_haves:
  truths:
    - "D-01 — the `builder-spine` screen reports N/N atoms against `200-CHECKLIST.md` §2 or NAMES the miss"
    - "D-02 — BS-1 and BS-2 are subtractions provable by a fence that can FIRE; BS-3 and BS-4 are BUILT"
    - "D-06 — SIX distinct renders exist and a boolean cannot express them: `pending` nothing · `skipped` nothing (never ran) · `active` ticks from `started_at` · `completed` a duration · `cancelled` ran Ns interrupted · historic (`completed` with both timestamps NULL) `time not recorded` — a DIFFERENT render from never ran"
    - "D-07 — a per-step count renders only where the type declared one; a type that declared nothing renders NOTHING, never `0` and never a dash, while a declared `0` renders as the fact it is"
    - "D-09 — the receipt is the SAME spine re-read in the PAST TENSE: one component, two tenses, reached through an OPTIONAL run prop whose ABSENCE leaves the authoring render byte-identical"
    - "`PhaseSpineGraph` reads a DRAFT definition and has NO run, so no run-tense word may appear on the builder mount — 199-02 refused exactly that and the refusal stays intact BY CONSTRUCTION"
    - "The receipt's words live in a NEW string home; `libraryVocabulary.ts` supplies the REGISTER, never the module — that module is fenced to `library/**` by a 14-path list"
  artifacts:
    - path: "frontend/src/components/workflows/phaseDuration.ts"
      provides: "D-06's discriminated arms — the ONE place a wire truth becomes a rendered duration fact"
      contains: "hasOwnProperty"
    - path: "frontend/src/components/workflows/receiptVocabulary.ts"
      provides: "The ONE past-tense string home for the receipt, whole table not a subset"
    - path: "frontend/src/components/workflows/RunReceipt.tsx"
      provides: "The past-tense spine: total runtime, per-step outcome, duration, declared count, deliverable"
  key_links:
    - from: "frontend/src/components/workflows/RunReceipt.tsx"
      to: "frontend/src/components/workflows/phaseDuration.ts"
      via: "the decision lives in the resolver; the words live in the vocabulary"
      pattern: "from \"./phaseDuration\""
    - from: "frontend/src/components/workflows/PhaseSpineGraph.tsx"
      to: "the run"
      via: "an OPTIONAL run-tense prop, absent ⇒ byte-identical authoring render"
      pattern: "\\?:"
---

<objective>
Build the `builder-spine` screen to `200-CHECKLIST.md` §2, and create the receipt D-09 describes —
**the same spine re-read in the past tense**, where every row is now a measured fact rather than a
claim, which is precisely what `200-02`'s slice bought.

Purpose: BS-1's read-only legend is *"the noisiest string in the product"*; BS-2 makes the same step
read `llm_agent` on the spine and `AI agent step` on the canvas — two views, two languages. BS-3's
four atoms are already in the definition the client holds. And BS-4, the per-step timings, is one of
the **two amber atoms that justify lifting the presentation-only fence**.

Output: `phaseDuration.ts` (D-06's arms), `receiptVocabulary.ts` (a NEW string home),
`RunReceipt.tsx`, and a spine that gains a run tense without losing its authoring honesty.

⚠ **THE RECEIPT IS CREATED HERE AND MOUNTED IN `200-07`.** That is the deliberate resolution of the
`WorkflowRunPage.tsx` overlap the orchestrator flagged: this plan does not touch that page, so no two
plans edit it. This plan is the ROADMAP's `200-04`, shifted by one.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-the-workflow-journey/200-CHECKLIST.md
@.planning/phases/200-the-workflow-journey/200-PATTERNS.md
@.planning/phases/200-the-workflow-journey/200-RESEARCH.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@.planning/sketches/200-journey-interactive/screens/builder-spine.html
@frontend/src/components/workflows/library/runFacts.ts
@frontend/src/components/workflows/doorVocabulary.ts
@frontend/src/components/workflows/ownProperty.ts

<interfaces>
<!-- The wire fields `200-02` put on BOTH transports. Consume THESE; derive nothing new. -->
export interface WorkflowRunPhase {
  slug: string
  phase_index: number
  status: string
  phase_type: string | null
  started_at?: string | null
  completed_at?: string | null
  step_count?: number | null      // a REAL integer, INCLUDING 0. null ⇒ this type declares no count.
  step_noun?: string | null       // null iff step_count is null
}

<!-- The house seam this plan rides — an OPTIONAL prop, absent ⇒ byte-identical.
     Three shipped instances: WorkflowCanvas.runState, WorkflowCanvas.editable, PhaseFormPanel.rails.
     PhaseSpineGraph's own instance is `nameContext?`. `PhaseFormPanel.rails.test.tsx:125` even pins
     that such a prop is LOAD-BEARING ("the absent and present renders are NOT the same DOM"). -->
export interface PhaseSpineGraphProps {
  phases: PhaseSpecJSON[]
  selectedSlug: string | null
  onSelectNode: (slug: string) => void
  nameContext?: NameContext
  runTense?: (slug: string) => PhaseRunFacts | undefined   // NEW — absent on the builder mount
}

<!-- The ONE shared elapsed formatter. The tree carried THREE before 194.1-06 hoisted this one. -->
import { fmtElapsed } from "@/lib/fmtElapsed"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `phaseDuration.ts` — D-06's arms, RED-first — and `receiptVocabulary.ts`, a NEW string home</name>
  <read_first>
    - `frontend/src/components/workflows/library/runFacts.ts` — **the four-arm precedent**: `hasOwnProperty.call` never `?? fallback`; `now` a PARAMETER with a `Date.now()` default; and ⚠ its CR-01 correction, where folding an absence together with a negative printed a false claim
    - `frontend/src/components/panel/phaseStatusMeta.ts:177-180` (the in-file TOTAL-lookup spelling, for a module that must stay leaf-pure) — ⚠ **note it is UNPINNED**, which is why D-06's arms get their own leaf
    - `frontend/src/components/workflows/doorVocabulary.ts` (the ONE-string-home module shape and its header rules) and `frontend/src/components/workflows/decisionsVocabulary.ts` (a TRUE leaf — imports nothing)
    - `frontend/src/components/workflows/runVocabulary.ts` (its EIGHT LOCKED canvas words — the receipt must duplicate none of them) and `frontend/src/components/workflows/library/libraryVocabulary.ts` (the REGISTER to copy — ⚠ **the MODULE is fenced to `library/**` by an explicit 14-path list with `toHaveLength(14)`; do not import it**)
    - `frontend/src/lib/fmtElapsed.ts` (the ONE formatter — no fourth)
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 6, § 7, IDIOM-2, IDIOM-3
    - `.planning/phases/200-the-workflow-journey/200-RESEARCH.md` §B4 (the residual: an `active` phase under a TERMINAL run — *"PRE-EXISTING and inherited"*) and A3
  </read_first>
  <behavior>
    - `pending` ⇒ renders nothing.
    - `skipped` ⇒ renders nothing — **never ran**, which is correct silence, not absence.
    - `active` with a `started_at` ⇒ a live tick derived from `started_at` and the injected `now`.
    - `completed` with both timestamps ⇒ a duration.
    - `cancelled` with both timestamps ⇒ ran Ns, interrupted.
    - **historic**: `completed` with BOTH timestamps NULL ⇒ `time not recorded` — a **DIFFERENT** render from *never ran*, and the case a boolean cannot express.
    - ⚠ **SEVENTH ARM (additive, RESEARCH §B4 / A3):** `active` on a **terminal** run ⇒ `did not finish`. Without it a crashed run shows a live-ticking clock forever — the `BUG-260610-01` symptom re-created on the surface this phase is building.
    - `own()`/`hasOwnProperty` on every status-keyed table: a `constructor`-slugged phase must not resolve a function.
    - `now` is a PARAMETER with a `Date.now()` default, so a suite injects a fixed instant with no clock mock.
  </behavior>
  <files>frontend/src/components/workflows/phaseDuration.ts, frontend/src/components/workflows/phaseDuration.test.ts, frontend/src/components/workflows/receiptVocabulary.ts, frontend/src/components/workflows/receiptVocabulary.test.ts</files>
  <action>
    **A — `phaseDuration.ts`, modelled on `runFacts.ts`.** Write the arms as failing cases first, then
    implement. This is the highest-consequence logic in the phase and it lands in a NEW leaf rather than
    in `phaseStatusMeta.ts` precisely because that module is **unpinned** (no dedicated suite; exercised
    only transitively) — a new leaf is pinned in the commit that creates it.

    ⚠ **The lesson this repo has now learned three times: absence gets its OWN arm.** `runFacts.ts` needed
    four arms after CR-01, because folding an absence together with a negative printed *"Never run"*
    about workflows that had really run. `DecisionsList` needed three under D-20 for the same reason.
    Here the twin cases are `skipped` (**never ran** — correct silence) and a historic row (**time not
    recorded** — we do not know). Compare `=== undefined` separately from `=== null`; a truthiness test
    folds `false` / `null` / `undefined` into one.

    ⚠ **A count of `0` is a FACT.** `step_count === 0` renders `0 sources`; `step_count == null` renders
    **nothing at all** — never `0`, never a dash (SEED-159 / D-07). The arm must be
    `typeof step_count === "number"`, never `step_count ?? …` and never `if (step_count)`.

    **No fourth elapsed formatter** — import `@/lib/fmtElapsed`. The decision lives here; the WORDS live
    in `receiptVocabulary.ts` (the same split `cardFace.ts` made for the state axis, one field over).

    **B — `receiptVocabulary.ts`, a NEW string home.** ⚠ **D-09 says the past-tense words come from
    `libraryVocabulary.ts`'s REGISTER — the tone, not the module.** That module is fenced to `library/**`
    by an explicit 14-path list (`librarySubtree.fences.test.ts:82-131`, `:169` asserts
    `toHaveLength(14)`, `:202` asserts the glob equals the list); importing it from outside the subtree
    would put library words on a non-library surface AND make `LIBRARY_SUBTREE_PATHS` stop describing the
    subtree's blast radius. **Copy the tone, import nothing.** Model the module on `doorVocabulary.ts`:
    one `export const` per governed string, each with a docblock, **EXACT-MATCH ASSERTIONS ONLY**.
    Rules: the WHOLE receipt table lives here, never a subset (a module holding a subset means the module
    and the checklist describe different things); it duplicates **none** of `runVocabulary.ts`'s eight
    locked canvas words (different audience — a whole PAST run versus one step being watched); it exports
    **zero glyph strings**; and it names the three count nouns recorded in `200-CHECKLIST.md` §5's
    AUTHORED COPY subsection.
    ⚠ Build tokens, never spell them, in any prose a `?raw` fence will read.
  </action>
  <acceptance_criteria>
    - `phaseDuration.test.ts` contains **seven** discriminated-arm cases (the six of D-06 plus the terminal-`active` arm), and a case asserting `skipped` and a historic `completed` produce **different** renders.
    - A case asserts `step_count === 0` renders the count, and a separate case asserts `step_count == null` renders **nothing** — `grep -cE '\?\?\s*0|if \(count\)' frontend/src/components/workflows/phaseDuration.ts` returns 0.
    - `grep -c "hasOwnProperty\|own(" frontend/src/components/workflows/phaseDuration.ts` ≥ 1, and a case asserts a `constructor`-slugged phase resolves no function.
    - `phaseDuration.ts` imports `@/lib/fmtElapsed` and defines no local duration formatter (`grep -cE 'function fmt|padStart\(2' phaseDuration.ts` returns 0).
    - `grep -c "libraryVocabulary" frontend/src/components/workflows/receiptVocabulary.ts` returns 0, and `receiptVocabulary.test.ts` asserts no exported string equals any of `runVocabulary.ts`'s eight locked words.
    - `cd frontend && npx vitest run src/components/workflows/phaseDuration.test.ts src/components/workflows/receiptVocabulary.test.ts --reporter=basic` is green.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test $(grep -cE "\?\?\s*0|if \(count\)" frontend/src/components/workflows/phaseDuration.ts) -eq 0; test $(grep -c "libraryVocabulary" frontend/src/components/workflows/receiptVocabulary.ts) -eq 0; cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/phaseDuration.test.ts src/components/workflows/receiptVocabulary.test.ts --reporter=basic'</automated>
  </verify>
  <done>Seven discriminated arms exist with `never ran` and `time not recorded` provably distinct, `0` is renderable and distinct from absence, and the receipt has one string home that imports no library words.</done>
</task>

<task type="auto">
  <name>Task 2: The spine — BS-3 built, BS-4 through an OPTIONAL run prop, BS-1 and BS-2 subtracted provably</name>
  <read_first>
    - `frontend/src/components/workflows/PhaseSpineGraph.tsx` — **`:71` `READ_ONLY_LEGEND`** (the exact string BS-1 forbids as a RENDER, while the identifier stays exported) and **`:75-85` the props: `phases: PhaseSpecJSON[]`, `selectedSlug`, `onSelectNode`, `nameContext?` — NO RUN DATA AT ALL**
    - `frontend/src/pages/WorkflowBuilderPage.tsx:2136` (the sole authoring mount — it passes nothing new and must stay unchanged)
    - `frontend/src/components/workflows/PhaseSpineGraph.test.tsx` (⚠ **pinned at 20 while running ~24 — four cases of SLACK**; *"a pinned TOTAL rising proves nothing about the NEW cases, because slack inside an already-listed file absorbs them"*)
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:555-567` (`runState?` — the house seam, and its docblock's rule that the PAGE owns the join, the reading and the words)
    - `.planning/sketches/200-journey-interactive/screens/builder-spine.html` (the drawn type sentences and badges; ⚠ it draws **NO** duration and **NO** total runtime — BS-4 is ledger-derived, per §0)
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §2 and §0 (N-1, N-5)
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 18 (`PhaseSpineGraph` — the structural problem stated in full)
  </read_first>
  <files>frontend/src/components/workflows/PhaseSpineGraph.tsx, frontend/src/components/workflows/PhaseSpineGraph.test.tsx</files>
  <action>
    ⚠ **READ THIS BEFORE TOUCHING THE FILE, or this task will re-litigate a refusal that was correct.**
    `PhaseSpineGraph` takes a **DRAFT definition** and has **no run** (`:75-85`). Any run-time word on the
    authoring mount — `TRAVERSED`/`SKIPPED` as a *run* claim, a duration, an elapsed — is a **fabricated
    claim**, and `199-02` refused exactly that. CONTEXT contradicts itself here (`<canonical_refs>` says
    the spine has no run; `<specifics>` says the slice *"clears the spine's per-step timings"*). **The
    reconciliation is D-09: one component, two tenses.**

    Give the component an **OPTIONAL run-tense prop, absent ⇒ byte-identical authoring render** — the
    house pattern three times over (`WorkflowCanvas.runState`, `WorkflowCanvas.editable`,
    `PhaseFormPanel.rails`), and `nameContext?` is this file's own instance of it. The authoring mount at
    `WorkflowBuilderPage.tsx:2136` passes nothing and stays unchanged; the run mount is `200-07`'s.
    ⚠ **The prop must be LOAD-BEARING and asserted so** — `PhaseFormPanel.rails.test.tsx:125` pins that
    the absent and present renders are NOT the same DOM; copy that assertion.

    **BS-3 — BUILD (all four atoms are already in the definition the client holds):** per-step model
    name, type badge, and the branch's `TRAVERSED` / `SKIPPED` marking. ⚠ On the AUTHORING mount the
    branch marks describe the DEFINITION's on-fail routing (`skip_to_phase`), not a run — word them so
    they cannot be read as a run claim, or gate them behind the run-tense prop.

    **BS-4 — BUILD, in run tense only:** per-step duration and total runtime, computed by
    `phaseDuration.ts` and worded by `receiptVocabulary.ts`. **Ledger-derived** per §0 — the proposed
    screen does not draw them, and a `builder-spine` checklist with no duration atom has dropped the
    phase's argument.

    **BS-1 — MUST NOT RENDER:** the `READ_ONLY_LEGEND` string. ⚠ **Scan the RENDERED DOM, not the
    source.** The identifier stays `export`ed at `:71`, so a source-level scan for those words would red
    on the export itself, and a deliberate absence must not trip its own fence. If a source scan is also
    wanted, scan for the JSX **mount**, not the constant.

    **BS-2 — MUST NOT RENDER:** the per-step `llm_agent` chip and the `phase_index N` line. Their
    replacement is the canvas's own words (`AI agent step`) so the same step reads one language in both
    views. ⚠ Do not delete `phase_index` from the DATA — it is the ordering key; forbid it as a rendered
    LABEL.

    **N-1:** use the CURRENT step names; the sketch's fork lanes carry the pre-rename ones.
    **N-5:** no Material Symbols ligature is a shippable atom; `icon-convention.md` §1/§2/§4 is the
    authority and a net-new mark is flagged as a proposal.

    **De-slack the suite.** `PhaseSpineGraph.test.tsx` is pinned at 20 while running ~24. Raise the pin to
    the ACTUAL count in this commit, so this plan's new cases are visible rather than absorbed.
    ⚠ **A raise necessarily DELETES a line**, so a `grep -c '^-[^-]'` expecting 0 is wrong here.
  </action>
  <acceptance_criteria>
    - `PhaseSpineGraphProps` gains exactly one new **optional** member; `grep -c "runTense" frontend/src/pages/WorkflowBuilderPage.tsx` returns 0 — the authoring mount is untouched.
    - A case asserts the absent-prop render and the present-prop render are **NOT** the same DOM (the prop is load-bearing).
    - A case renders **without** the run prop and asserts **no** duration string, **no** elapsed and **no** run-tense word appears — the 199-02 refusal, now enforced.
    - A case renders **with** the run prop and asserts a real duration and the total runtime appear (BS-4).
    - A DOM-level case asserts `READ_ONLY_LEGEND`'s text does not render, while `grep -c "export const READ_ONLY_LEGEND" frontend/src/components/workflows/PhaseSpineGraph.tsx` still returns 1 — the export survives, the render does not.
    - A DOM-level case asserts neither `llm_agent` nor a `phase_index N` label renders.
    - `scripts/vitest-count-gate.cjs`'s `PhaseSpineGraph.test.tsx` pin equals the suite's ACTUAL case count.
    - `cd frontend && npx vitest run src/components/workflows/PhaseSpineGraph.test.tsx --reporter=basic` is green and `npx tsc -p tsconfig.app.json --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test $(grep -c "runTense" frontend/src/pages/WorkflowBuilderPage.tsx) -eq 0; test $(grep -c "export const READ_ONLY_LEGEND" frontend/src/components/workflows/PhaseSpineGraph.tsx) -eq 1; cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PhaseSpineGraph.test.tsx --reporter=basic && npx tsc -p tsconfig.app.json --noEmit'</automated>
  </verify>
  <done>The spine gains a run tense without gaining a fabricated claim: absent prop ⇒ byte-identical authoring render, present prop ⇒ real durations; the legend and the `phase_index` label no longer render; the suite's slack is gone.</done>
</task>

<task type="auto">
  <name>Task 3: `RunReceipt.tsx` — the same spine in the past tense — plus the fence, the pins and the ledger</name>
  <read_first>
    - `.planning/phases/200-the-workflow-journey/200-CONTEXT.md` D-09 (the receipt's worked example, verbatim) and `.planning/sketches/200-journey-interactive/COVERAGE.md` (the *"c3's receipt column is missing"* gap this closes)
    - `frontend/src/components/workflows/PublishGauntlet.test.tsx:1095-1334` (the role-SET scan **proved able to fire**) and `library/librarySubtree.fences.test.ts:82-210` (non-vacuity BEFORE contents)
    - `frontend/src/components/workflows/receiptVocabulary.ts` and `phaseDuration.ts` (Task 1's outputs — the receipt spells no string and makes no decision of its own)
    - `scripts/vitest-count-gate.cjs:2540-2546` (TARGETS and BASELINE are TWO knobs) and `200-CHECKLIST.md` §6
    - `docs/HOT-FILE-LEDGER.md` → `frontend/src/components/workflows/PhaseSpineGraph.tsx` (its existing section) and CLAUDE.md's row for it
  </read_first>
  <files>frontend/src/components/workflows/RunReceipt.tsx, frontend/src/components/workflows/RunReceipt.test.tsx, scripts/vitest-count-gate.cjs, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>
  <action>
    **A — the receipt.** One component, past tense. Header: total runtime, step count and finish time
    (`Ran 4m 12s · 6 steps · finished 14:22`). Each row: the step, its outcome, its duration, its count
    **if it declared one**, and its deliverable **if it produced one**. Everything on it is a measured
    fact — that is exactly what `200-02`'s slice bought, and it is why D-09 calls this *"not a third
    surface"*.

    Compose, do not re-derive: the decision comes from `phaseDuration.ts`, the words from
    `receiptVocabulary.ts`, the formatter from `@/lib/fmtElapsed`. **The receipt itself spells no
    user-visible string** — one literal left in JSX is a second home, and a second home cannot be
    re-worded by a one-line diff.

    ⚠ **A step that declared no count renders NOTHING in that slot** — never `0`, never a dash, and never
    prose. N-8 records that the sketch's own run-surface puts the sentence `Summarized meeting notes` in
    the same slot as the count `Found 12 contracts`; drawing prose there would re-introduce the
    fabricated-figure failure D-07 exists to prevent. ⚠ And the noun is the STEP's own, never the
    contract's — `312 docs matched` / `48 fields extracted` are DOMAIN sentences (SEED-168).

    ⚠ **This plan does NOT mount the receipt.** `WorkflowRunPage.tsx` is `200-07`'s primary file, and
    mounting it there from here would put two plans in one file. Export it; `200-07` mounts it — which
    also keeps 199-02's refusal intact **by construction**, since the receipt never reaches the builder.

    **B — the `builder-spine` MUST NOT RENDER fence.** Cover §2's `BS-MNR-*` rows with a **role-SET +
    rendered-text** scan and **two permanent non-vacuity controls**: a planted violation that renders the
    legend text, an `llm_agent` chip and a `phase_index 3` label and asserts the predicate finds **all
    three**; and a deliberate-absence case proving the predicate does not fire on the honest shipped copy.
    ⚠ `199-03` measured a `?raw` source regex AND a `queryAllByRole("button")` filter **both passing
    GREEN** against a live planted violation — only a role-SET scan went red. Assert non-vacuity **before**
    contents, or every negative passes against the empty string.

    **C — the pins.** Add a `BASELINE` entry for **every** file this plan created — `phaseDuration.test.ts`,
    `receiptVocabulary.test.ts`, `RunReceipt.test.tsx` — **in this same commit**. All three sit under
    `src/components/workflows`, which the directory entry in `TARGETS` already RUNS, so what is owed is the
    `BASELINE` entry; check both knobs rather than assuming.

    **D — the ledger.** `PhaseSpineGraph.tsx` already has a row and a section: refresh both with the triple
    re-derived in `200-CHECKLIST.md` §6, record this phase's edit as *honoured by construction*, and state
    the binding invariant that this plan hardened — **it reads a DRAFT definition and has no run, so a
    run-tense word is reachable only through an optional prop the builder mount does not pass.** Then run
    `node scripts/check-claude-md-size.cjs`.

    Run the phase gate and compare **per file** against §6. ⚠ **If it reds, do NOT reach for the cap** —
    capture failing filenames from the gate's own persisted JSON first, check each against
    `git diff --numstat <base> HEAD`, and say *"provably unmodified"*, never *"fine"*. Red is sometimes REAL.
  </action>
  <acceptance_criteria>
    - `grep -cE '"[A-Z][a-z].*"' frontend/src/components/workflows/RunReceipt.tsx` shows no user-visible sentence literal; every displayed string traces to a `receiptVocabulary.ts` import.
    - `grep -c "RunReceipt" frontend/src/pages/WorkflowRunPage.tsx` returns 0 and `grep -c "RunReceipt" frontend/src/pages/WorkflowBuilderPage.tsx` returns 0 — **this plan mounts nothing**.
    - `RunReceipt.test.tsx` contains: a case asserting a no-count step renders no count slot at all; a case asserting a declared `0` renders; a case asserting a `skipped` step reads *never ran* while a historic row reads *time not recorded*; and a case asserting the total runtime is derived from the phase timestamps, not from a client clock.
    - The fence's planted-violation case asserts a count ≥ 3 and is committed permanently; the deliberate-absence case stays green.
    - `scripts/vitest-count-gate.cjs` gains BASELINE entries for all three new suites in this commit.
    - `docs/HOT-FILE-LEDGER.md`'s `PhaseSpineGraph.tsx` section and CLAUDE.md's row both carry the re-derived triple; `node scripts/check-claude-md-size.cjs` exits 0.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root: no per-file decrease against §6.
    - The plan summary reports `builder-spine: N/N atoms` against §2, or **names the miss**.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test $(grep -c "RunReceipt" frontend/src/pages/WorkflowRunPage.tsx) -eq 0; node scripts/check-claude-md-size.cjs; (cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/RunReceipt.test.tsx src/components/workflows/PhaseSpineGraph.test.tsx --reporter=basic); GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs'</automated>
  </verify>
  <done>The receipt exists, spells no string of its own, mounts nowhere yet, and the spine's subtractions are guarded by a fence proved able to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire phase rows → rendered facts | Server-supplied timestamps, counts and an unconstrained `slug` cross into keyed client lookups and user-visible claims |
| draft definition → authoring render | A definition the author is still editing must never be described in run tense |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-05-01 | Spoofing (of fact) | the spine's run-tense words | mitigate | `PhaseSpineGraph` has no run; the run tense is reachable only through an OPTIONAL prop the builder mount does not pass, and a case asserts the propless render carries no duration, no elapsed and no run-tense word (199-02's refusal, now enforced rather than remembered) |
| T-200-05-02 | Spoofing (of fact) | the receipt's count slot | mitigate | D-07: a step that declared no count renders **nothing** — never `0`, never a dash, never prose (N-8). The noun is the step's own, authored at one executor site (SEED-168) |
| T-200-05-03 | Tampering | slug-keyed tables in `phaseDuration.ts` | mitigate | `own()` / `hasOwnProperty.call` only. `workflow_phases.slug` is unconstrained `text` (`058:18`), so a `constructor` slug reaches these tables; a case asserts it resolves no function |
| T-200-05-04 | Repudiation | `never ran` vs `time not recorded` | mitigate | Separate arms with separate words and separate cases; `=== undefined` tested separately from `=== null`, because a truthiness test folds three states into one |
| T-200-05-05 | Information Disclosure | none | accept | The receipt renders only fields already on the wire to this owner; no new fetch, no new endpoint, no id exposure |
| T-200-05-SC | Tampering | npm/pip/cargo installs | mitigate | **No package installs.** No dependency manifest in `files_modified`; every new file is first-party. RESEARCH carries no `## Package Legitimacy Audit`, so any package would be `[ASSUMED]` and need a blocking human checkpoint |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/phaseDuration.test.ts src/components/workflows/receiptVocabulary.test.ts src/components/workflows/RunReceipt.test.tsx src/components/workflows/PhaseSpineGraph.test.tsx --reporter=basic` — green.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` exits 0.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — per-file deltas against `200-CHECKLIST.md` §6; SEED-171 triage if red, never the cap.
- `node scripts/check-claude-md-size.cjs` exits 0.
- The summary reports `builder-spine: N/N atoms` or names the miss, and states that the receipt is exported but not yet mounted.
</verification>

<success_criteria>
- ROADMAP SC#1 for `builder-spine`: N/N atoms or a named miss, with BS-1 and BS-2 proved as subtractions.
- ROADMAP SC#2 (render half, spine): a real per-step duration and a real total runtime, with `never ran` and `time not recorded` provably distinct renders.
- ROADMAP SC#3 (render half): a count renders only where one was declared; `0` renders as the fact it is.
- D-09 is satisfied by ONE component in two tenses, not by a third surface.
</success_criteria>

<output>
Create `.planning/phases/200-the-workflow-journey/200-05-SUMMARY.md` when done.
</output>
