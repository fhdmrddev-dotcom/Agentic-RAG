---
phase: 247-sources-and-watches
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/sources/adapters/google_drive.py
  - backend/app/services/sources/adapters/microsoft_graph.py
  - backend/app/services/sources/ingest_enrich.py
  - backend/tests/unit/test_boundary_fence.py
  - backend/tests/unit/test_247_source_paths.py
autonomous: true
requirements: [WATCH-01, WATCH-02, WATCH-08]

must_haves:
  truths:
    - "A document ingested from Google Drive carries full folder hierarchy in `metadata.source.path` (e.g. `/Finance/2026/Q1_Tax.xlsx`) relative to watched root (WATCH-01)."
    - "A document ingested from OneDrive/SharePoint carries full folder hierarchy without truncation or prefix corruption (WATCH-02)."
    - "Classification rules matching `path contains '/Finance/'` or `path eq '/Finance/2026/Q1_Tax.xlsx'` match cleanly against watched files."
    - "WR-04 fix: User Gmail label in `google_drive.py` resolves human label name instead of `label_id` (`/Label_9`)."
    - "WR-07 fix: `test_boundary_fence.py` uses recursive traversal so `sources/mail/` modules cannot escape the fence."
    - "FENCE: `backend/app/api/connectors.py` is byte-unchanged (0 lines modified)."
  artifacts:
    - path: "backend/app/services/sources/adapters/google_drive.py"
      provides: "Full relative path hierarchy resolution on SourceFile.path + human label name resolution"
      contains: "path="
    - path: "backend/app/services/sources/adapters/microsoft_graph.py"
      provides: "parentReference.path parsing with prefix stripping"
      contains: "parentReference"
    - path: "backend/app/services/sources/ingest_enrich.py"
      provides: "metadata.source.path population from SourceFile.path"
      contains: "source.path"
    - path: "backend/tests/unit/test_247_source_paths.py"
      provides: "Unit tests verifying path construction for Drive and Graph and classification rule matching"
      contains: "test_google_drive_source_path_hierarchy"
  key_links:
    - from: "backend/app/services/sources/adapters/google_drive.py"
      to: "backend/app/services/sources/base.py"
      via: "SourceFile(..., path=relative_path)"
      pattern: "SourceFile\\(.*path="
    - from: "backend/app/services/sources/ingest_enrich.py"
      to: "backend/app/services/classification_rules.py"
      via: "metadata.source.path match"
      pattern: "metadata\\[\"source\"\\]\\[\"path\"\\]"
---

<objective>
**WATCH-01 & WATCH-02: Remote Path Hierarchy Propagation in Drive & Graph Adapters, plus WR-04 and WR-07 fixes.**

Ensure documents ingested via Google Drive and OneDrive/SharePoint adapters carry the complete, accurate folder hierarchy in `metadata.source.path` rather than falling back to `/{filename}` or truncating folder components, enabling path-based classification rules to match. Fix WR-04 (human label name for Gmail user labels) and WR-07 (recursive boundary fence glob).

Strict Boundary: `backend/app/api/connectors.py` is strictly fenced (0 lines touched).
</objective>

<threat_model>
### Security & Reliability Invariants (ASVS L1)
1. **Path Traversal & Absolute Path Boundary:**
   - Invariant: `path` must remain normalized and strictly relative to the watched root (e.g. `/Accounting/Q3.pdf`). Any `..`, `./`, or double slashes are normalized out to prevent path traversal confusion in classification or virtual folder projection.
2. **Missing Folder / Root Fallback:**
   - Invariant: If a file sits directly in the watched root folder or if parent folder resolution fails, `path` falls back safely to `/{f.name}` without throwing exceptions or generating null pointer errors.
3. **Fence Integrity (WR-07):**
   - Invariant: `test_boundary_fence.py` must recursively inspect all subpackages under `sources/` so mail and connector subpackages remain within boundary constraints.
</threat_model>

<context>
- `backend/app/services/sources/base.py:42` defines `SourceFile` with `path: str | None = None`.
- `google_drive.py:249` currently constructs `SourceFile` without passing `path=`, leaving it `None`.
- `microsoft_graph.py` extracts `driveItem` but strips or truncates `parentReference.path` inconsistently.
- `ingest_enrich.py` reads `source_info.get("path")` and defaults to `f"/{filename}"`.
- `google_drive.py:239` passes `label_name=label_id` for user labels (WR-04).
</context>

<tasks>

<task type="auto" id="247-01-01">
<name>Fix Google Drive Adapter Path Construction & WR-04 Label Name</name>
<files>backend/app/services/sources/adapters/google_drive.py</files>
<action>
1. In `google_drive.py`:
   - When listing folder items, build a parent-to-path cache during the traversal from the watched root folder `source_folder_id`.
   - For each file item, resolve its relative path hierarchy (e.g. `/Finance/2026/Q1_Tax.xlsx`) and pass it as `path=resolved_path` when instantiating `SourceFile`.
   - In the mail arm (`mailbox:` labels): For WR-04, when the label is a user label (e.g. `Label_9`), query Gmail labels API or label mapping cache to resolve the human-readable display name (`label.get("name")`) rather than passing raw `label_id`.
</action>
<verify>
<command>python -m pytest backend/tests/unit/test_247_source_paths.py -k "test_google_drive" -v</command>
</verify>
</task>

<task type="auto" id="247-01-02">
<name>Fix Microsoft Graph Adapter Path Parsing (OneDrive / SharePoint)</name>
<files>backend/app/services/sources/adapters/microsoft_graph.py</files>
<action>
1. In `microsoft_graph.py`:
   - Cleanly extract `parentReference.path` from Graph `driveItem`.
   - Strip `/drive/root:` (or `/drive/root`) prefix and decode URL entities cleanly.
   - Combine with `item.name` to produce the clean relative path `/Folder/Subfolder/filename.ext`.
   - Pass `path=clean_path` when instantiating `SourceFile`.
</action>
<verify>
<command>python -m pytest backend/tests/unit/test_247_source_paths.py -k "test_microsoft_graph" -v</command>
</verify>
</task>

<task type="auto" id="247-01-03">
<name>Propagate Source Path in Ingestion Enrichment & Fix WR-07 Fence Glob</name>
<files>backend/app/services/sources/ingest_enrich.py, backend/tests/unit/test_boundary_fence.py, backend/tests/unit/test_247_source_paths.py</files>
<action>
1. In `ingest_enrich.py`:
   - Ensure `metadata["source"]["path"]` is populated from `source_file.path` whenever available.
   - Verify classification rule evaluator matches `path contains ...` against this enriched field.
2. In `backend/tests/unit/test_boundary_fence.py`:
   - Fix WR-07: Change `Path.glob("*.py")` to `Path.rglob("*.py")` to recursively check `services/sources/mail/` and other subpackages.
3. Write `backend/tests/unit/test_247_source_paths.py`:
   - Test Drive folder hierarchy resolution.
   - Test Graph parentReference parsing.
   - Test WR-04 human label name resolution.
   - Test rule evaluation on `metadata.source.path`.
   - Assert `backend/app/api/connectors.py` is byte-unchanged.
</action>
<verify>
<command>python -m pytest backend/tests/unit/test_247_source_paths.py backend/tests/unit/test_boundary_fence.py -v</command>
</verify>
</task>

</tasks>
