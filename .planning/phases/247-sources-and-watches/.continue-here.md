---
context: phase
phase: 247-sources-and-watches
task: 6
total_tasks: 6
status: closed_on_code_uat_owed
last_updated: 2026-09-14T16:34:23.847Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through actual failure in this session. Acknowledge before proceeding.

- [ ] **CONSTRAINT: a review that runs only the phase's own suites is not a review** — it measures the phase's CLAIMS, never its CONSEQUENCES. Phase 247's close was **given, then retracted**, because three suites were red — all CAUSED by the phase (70 passed at base `987e7a685`, 2 failed at HEAD), and all invisible to a test selection built from *files touched*. One was a **fence**. — **Mitigation: run the touched suites AND their dependents, and for anything red establish base-vs-HEAD before calling it inherited.**
- [ ] **CONSTRAINT: Claude must not implement findings from a phase Claude reviewed** — the fixes become review-response commits nobody independent has read. That is the exact gap measured today in `240-REVIEW-RECONCILIATION.md`, where one such commit changed the source **contract file**. — **Mitigation: route fixes to the other agent, or get an explicit operator ruling.**

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Touched-files test selection | Reviewing the files a phase changed, not the files that depend on what it changed. Cost a wrong close on 247 | blocking | Run dependents; `pytest ... -k "<domain>"` over the whole area, then base-vs-HEAD on any red |
| Fence tripped by surprise | The `SEED-253` Drive-path fence went red through a build, three reviews and a close, unnamed. `SEED-177`: *retire the fence DELIBERATELY, never trip it by surprise* | blocking | A red fence is ruled on explicitly — retired under a named decision with the reason in the test body, or the code is fixed. Never just made green |
| Integer contracts over flaky values | `sourceComposition.test.tsx` reads 16/17/18 across runners on a byte-identical tree; the backend ceiling reads 71/72/72/77 on one tree | advisory | Contract on the SET of failing names (`247-STANDING-RED-BASELINE.md`), never the count. See `SEED-274` |
| Presence assertions / presence gates | The honesty gate blessed `verification_mode: peer-reviewed` while it was still false. It checks PRESENCE, never TRUTH | advisory | Assert rendered CONTENT where words are the deliverable; treat a green marker gate as necessary, not sufficient |
| Claims quoted rather than driven | Two `status: open` **blocking** bugs were already fixed; a third reading identically was live. `D-234` cited and does not exist. `WR-05` named a symbol with zero hits | advisory | Drive every inherited claim before planning or reporting against it |

<current_state>
**Phase 247 is CLOSED ON CODE** at `3d5f62dc6`. Built by Gemini across 3 waves; reviewed by Claude
wave-by-wave (`BUS-214` / `BUS-217` / `BUS-218` / `BUS-219`); five findings raised and fixed
(`1f37737ae`); close given (`BUS-221`), **RETRACTED** (`BUS-222`), and re-given on wider evidence
(`BUS-223`).

Gates at the real close, re-derived under Claude's own runs: wider blast radius **460 passed / 0
failed** · 247's own suites 37 · frontend 118/118 · standing red 16-33 with the **same membership**
as the pinned baseline · honesty gate 10/10 · ledger gate OK.

Gemini has since delivered the **Phase 238 independent §6.3 review** (`238-INDEPENDENT-REVIEW.md`,
`6a0a3171b`) and a review of **240's review-response range**. Claude reviewed the 238 one
(`BUS-225`, two corrections). **The 240 one is UNREAD.**
</current_state>

<completed_work>

- 247-01 Drive & Graph path resolution, WR-04, WR-07 recursive fence — `e5ded885a`
- 247-02 `missing_since` lifecycle + `is_enabled` health verdict — `35bbe6c7c`
- 247-03 `WatchRowCard` extraction (G-5 `1094 → 470`), Variant A badges, **F-1 cross-tenant Gmail
  label cache fix** — `ef6458c9c`
- 247-04 disposition, ledger sync, verification — `50feee403`
- Reviewer findings V-1..V-5 — `1f37737ae`
- `SEED-253` fence retired under **`D-247-01`** with the reason in the test body — `3d5f62dc6`
</completed_work>

<remaining_work>

- ⛔ **247's G-4 lived-experience UAT** — three operator-defined "I'd recognize failure here"
  scenarios in a real browser. **Operator only.** Everything to date is jsdom, pytest and greps, and
  this phase is entirely about what a person READS.
- Gemini: `BUS-225`'s two corrections — **C-1** `WR-05` names `GoogleDriveSourceAdapter()` which has
  **zero hits**; the real defect is `service_tools.py:1689` calling the **private**
  `google_drive._list_google_drive_files` from outside `sources/`, bypassing
  `SourceRegistry.get_adapter()` — sharper than what was written. **C-2** frontmatter claims 5
  findings, §3 enumerates 3.
- Claude: read Gemini's review of **240's review-response range**.
- **Phase 248 The Credential Boundary** — Claude end-to-end per `OV-248-01`. Measurement pack ready
  at `.planning/phases/248-the-credential-boundary/248-MEASUREMENTS.md`.
</remaining_work>

<decisions_made>

- **`VERDICT.md` counts as equivalent to `VERIFICATION.md`** for the honesty gate (operator) — Phase
  245 produced only a VERDICT.md, so the phase that wrote the gate was invisible to it.
- **`DEBT-06` = 3 reviews + 5 reasoned refusals**, not 8 reviews (operator). Refusals in
  `.planning/DEBT-06-REFUSALS.md` name the mechanical evidence standing in for a reviewer.
- **The standing-red contract is a SET of 16 names, never a count** — the suite reads 16/17/18
  across runners on a byte-identical tree.
- **`SEED-253`'s fence retired, not made green** — `D-247-01`, replacement asserts the NEW invariant.
- **Claude does not implement findings from phases Claude reviewed.**
</decisions_made>

<blockers>
- **247 G-4 UAT** — human action, operator only. Not a defect; the code close stands.
- **Phase 239 has no eligible reviewer** — Gemini planned it, Claude executed it, so both shaped it
  under §6.3. Needs an operator ruling: accept-as-owed / review-with-conflict-declared / ultra
  (ruled out on cost).
- **Claude cannot trigger Gemini** — separate process, no channel (`ListAgents` → none). The bus is
  a mailbox, not a doorbell.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF-260914.md` — the session's full state; §2b is Phase 247's arc.
2. `.planning/phases/247-sources-and-watches/247-STANDING-RED-BASELINE.md` — the 16-name set contract.
3. `.planning/DEBT-06-REFUSALS.md` — why 241-245 get refusals, and what evidence stands in.
4. `.planning/phases/248-the-credential-boundary/248-MEASUREMENTS.md` — **read before planning 248**;
   `CRED-03` splits **three** ways, not one.
5. `docs/HOT-FILE-LEDGER.md` — for any file 248 touches.

## Infrastructure State

- **Monitor:** a persistent Monitor watched 247's execution. ⚠ **It dies with the session** — re-arm
  if autonomous watching is still wanted. Two defects were found in it *by using it* (a hardcoded
  status line that asserted what it never measured; a doubled `grep -c` count).
- **Cloud:** v4.1 deployed; `production` = `eebc4c42f`, `production..develop` **0** at deploy;
  migrations through **180** applied and verified; `get_advisors(security)` **zero ERROR**.
- ⚠ **`.mcp.json` carries `read_only=true`** — Supabase MCP reads are free, **writes are impossible**
  from here. CLAUDE.md says the flag was removed 2026-09-11; against the tracked file that is false.
- ⛔ **Revoked and not yet replaced:** Zhipu/GLM and Moonshot keys. Dead in `backend/.env` and in
  Coolify. Any run on those providers fails with a provider auth error that is **not** a code defect.

## Tooling changed this session — pull before using

- **`scripts/agent-bus.sh` `close()`** matched only `[OPEN]`, so an `[ANSWERED]` item could never be
  closed, and `sed` exits 0 either way — it printed success over a byte-identical file. Now gated on
  an md5 diff; re-closing refuses with exit 1.
- **`scripts/check-verification-honesty.cjs`** now **derives** its scan set — phases ≥ 238, live AND
  archived, `VERIFICATION.md` and `VERDICT.md`. It read **3 hardcoded paths** while 20 verdict files
  existed. Scans 10 now. ⭐ Counterfactual proof: the old set prints `OK` over the exact planted
  defect the new one catches.

<context>
The through-line of this session: **a claim is not evidence until it is driven.** It held in every
direction — two `status: open` blocking bugs were already fixed, a third reading identically was
live; a security finding that looked evaporated had shipped *and* had its forgery surface deleted; a
register saying "nobody has looked at it" had two reviews; an md5 mismatch that looked like a
refuted headline claim was a claim true within its own scope. It also cut against Claude: a
premature close, two defects in Claude's own monitor, and a gate Claude rebuilt that blessed a
marker which was not yet true.
</context>

<next_action>
Start with: read `.planning/HANDOFF-260914.md`, then Gemini's review of **Phase 240's
review-response range** — it is the one review artifact from this session nobody has read. After
that, either drive 247's G-4 UAT (operator) or open Phase 248 with `248-MEASUREMENTS.md`.
</next_action>
