---
phase: 247-sources-and-watches
plan: 04
type: execute
wave: 3
depends_on: [247-01, 247-02, 247-03]
files_modified:
  - .planning/phases/247-sources-and-watches/247-DISPOSITION.md
  - .planning/reported-bugs/BUG-260910-02-phase-240-build-review-open-warnings.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [WATCH-08]

must_haves:
  truths:
    - "Phase 240's seven open build-review warnings are formally dispositioned in `247-DISPOSITION.md` (WATCH-08)."
    - "WR-04, WR-07, and WR-09 are documented with their code fixes and test assertions."
    - "WR-02, WR-05, WR-06, and WR-08 are accepted as debt with explicit technical justification and risk analysis."
    - "`BUG-260910-02` status is updated to closed with verified_closed_by referencing Phase 247."
    - "`docs/HOT-FILE-LEDGER.md` is updated with re-derived triples for the 5 hot files touched in Phase 247, including written G-5 safe-as-is rationale for `sourceHealthVocabulary.ts`."
    - "FENCE: `backend/app/api/connectors.py` is byte-unchanged (0 lines modified)."
    - "Standing red baseline for `sourceComposition.test.tsx` satisfies set contract in `247-STANDING-RED-BASELINE.md` (no failure outside the documented 16-test set; readings of 16-18 acceptable provided all failures belong to the set)."
  artifacts:
    - path: ".planning/phases/247-sources-and-watches/247-DISPOSITION.md"
      provides: "Formal disposition document for Phase 240 build-review warnings"
      contains: "WR-02"
    - path: ".planning/reported-bugs/BUG-260910-02-phase-240-build-review-open-warnings.md"
      provides: "Closed bug report for Phase 240 build review"
      contains: "status: closed"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "Re-derived hot-file ledger entries for Phase 247 touched files"
      contains: "WatchedFoldersSection.tsx"
  key_links:
    - from: ".planning/phases/247-sources-and-watches/247-DISPOSITION.md"
      to: ".planning/reported-bugs/BUG-260910-02-phase-240-build-review-open-warnings.md"
      via: "Disposition cross-reference"
      pattern: "BUG-260910-02"
---

<objective>
**WATCH-08: Formal Disposition of Phase 240 Build-Review Warnings, Hot-File Ledger Synchronization, and Full Phase Verification.**

Deliver the formal paperwork deliverable for WATCH-08:
1. Author `.planning/phases/247-sources-and-watches/247-DISPOSITION.md` detailing the resolution of all seven warnings left open from Phase 240 (WR-04, WR-07, WR-09 fixed in code; WR-02, WR-05, WR-06, WR-08 accepted as debt).
2. Close `BUG-260910-02`.
3. Synchronize `docs/HOT-FILE-LEDGER.md` with re-derived triples for touched files (`WatchedFoldersSection.tsx`, `sourceHealthVocabulary.ts`, `watch_service.py`, `google_drive.py`, `microsoft_graph.py`).
4. Re-assert strict fence on `backend/app/api/connectors.py` (0 lines touched).
5. Run full verification across frontend and backend suites, confirming standing red baseline `sourceComposition.test.tsx` remains strictly at 17 failed | 32 passed.
</objective>

<threat_model>
### Security & Reliability Invariants (ASVS L1)
1. **Audit Honesty & No Silent Rot:**
   - Invariant: A dispositioned warning must document why it does not violate security or operational integrity, rather than brushing past it. The four accepted debts (WR-02, WR-05, WR-06, WR-08) must clearly explain why they are safe to defer.
2. **Fence Invariant:**
   - Invariant: `backend/app/api/connectors.py` must remain completely unmodified (0 lines touched), preserving its owed extraction status.
</threat_model>

<context>
- `BUG-260910-02`: Phase 240 build review left seven warnings open.
- WR-04, WR-07, WR-09 fixed in plans 247-01 and 247-03.
- WR-02 (listing growth / H-5 suppression in mail), WR-05 (conversation test greps), WR-06 (ingest metadata rewrite), WR-08 (gmail URL path interpolation) are accepted debt.
- `docs/HOT-FILE-LEDGER.md` tracks files at $\ge 3$ phases or $\ge 1,000$ lines.
</context>

<tasks>

<task type="auto" id="247-04-01">
<name>Author 247-DISPOSITION.md & Close BUG-260910-02</name>
<files>.planning/phases/247-sources-and-watches/247-DISPOSITION.md, .planning/reported-bugs/BUG-260910-02-phase-240-build-review-open-warnings.md</files>
<action>
1. Create `.planning/phases/247-sources-and-watches/247-DISPOSITION.md`:
   - Document the status and rationale for each of the seven Phase 240 warnings:
     - WR-04: Fixed in `google_drive.py` (Plan 247-01). User Gmail labels resolve display name rather than `label_id`.
     - WR-07: Fixed in `test_boundary_fence.py` (Plan 247-01). Replaced non-recursive glob with `rglob` to enclose `sources/mail/`.
     - WR-09: Fixed in `watchProductMark.ts` (Plan 247-03). Microsoft Graph/OneDrive/SharePoint map to `'microsoft'`.
     - WR-02: Accepted as debt. Mail listing suppresses deletion detection wholesale (WR-03 fix); permanently incomplete listing does not cause false deletion. Growth bounded by `SEED-264`.
     - WR-05: Accepted as debt. Conversation endpoint is fully functional; grep-based tests will be deepened when endpoint next expands.
     - WR-06: Accepted as debt. Metadata clobbering in `ingest_splice.py` is benign under current MIME branch usage.
     - WR-08: Accepted as debt. Uvicorn decodes `%2F` safely before routing, preventing injection; low risk.
2. In `.planning/reported-bugs/BUG-260910-02-phase-240-build-review-open-warnings.md`:
   - Set `status: closed`.
   - Set `verified_closed_by: Phase 247 (247-DISPOSITION.md)`.
</action>
<verify>
<command>python -c "import os; assert os.path.exists('.planning/phases/247-sources-and-watches/247-DISPOSITION.md'); print('DISPOSITION exists')"</command>
</verify>
</task>

<task type="auto" id="247-04-02">
<name>Sync Hot-File Ledger Entries & Assert connectors.py Fence</name>
<files>docs/HOT-FILE-LEDGER.md</files>
<action>
1. Update `docs/HOT-FILE-LEDGER.md`:
    - Record re-derived triples (commits / phases / lines) for files touched in Phase 247:
      - `backend/app/services/sources/adapters/google_drive.py`
      - `backend/app/services/sources/adapters/microsoft_graph.py`
      - `backend/app/services/watch_service.py`
      - `frontend/src/components/sources/WatchedFoldersSection.tsx` (record extraction of `WatchRowCard.tsx`, bringing line count under 1,000 lines)
      - `frontend/src/components/sources/sourceHealthVocabulary.ts` (record G-5 safe-as-is disposition: leaf presentation dictionary, zero imports, pure copy/action maps, no state/mutation)
    - Record strict fence assertion: `backend/app/api/connectors.py` at 44 commits / 21 phases (per CLAUDE.md recipe) / 2,140 lines — byte-identical, 0 lines modified.
</action>
<verify>
<command>git diff backend/app/api/connectors.py</command>
</verify>
</task>

<task type="auto" id="247-04-03">
<name>Full Verification & Standing Red Baseline Check</name>
<files></files>
<action>
1. Run backend unit tests for sources and watches:
   - `python -m pytest backend/tests/unit/test_247_source_paths.py backend/tests/unit/test_247_watch_missing_lifecycle.py backend/tests/unit/test_boundary_fence.py -v`
2. Run frontend unit tests for sources and watches:
   - `$env:CI="1"; node frontend/node_modules/vitest/vitest.mjs run frontend/src/components/sources/watchProductMark.test.ts frontend/src/components/sources/sourceHealthVocabulary.test.ts frontend/src/components/sources/WatchedFoldersSection.test.tsx`
3. Run standing red baseline check:
   - `$env:CI="1"; node frontend/node_modules/vitest/vitest.mjs run frontend/src/components/sources/sourceComposition.test.tsx`
   - Capture failing test names and assert no test outside the documented 16-test set in `247-STANDING-RED-BASELINE.md` fails (readings of 16-18 acceptable provided all failures belong to the set).
4. Run frontend typecheck:
   - `npm --prefix frontend run typecheck`
</action>
<verify>
<command>git status</command>
</verify>
</task>

</tasks>
