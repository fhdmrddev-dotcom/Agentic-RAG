---
phase: 247-sources-and-watches
plan: 03
type: execute
wave: 2
depends_on: [247-01, 247-02]
files_modified:
  - frontend/src/components/sources/watchProductMark.ts
  - frontend/src/components/sources/watchProductMark.test.ts
  - frontend/src/components/sources/sourceHealthVocabulary.ts
  - frontend/src/components/sources/sourceHealthVocabulary.test.ts
  - frontend/src/components/sources/WatchedFoldersSection.tsx
  - frontend/src/components/sources/WatchRowCard.tsx
  - frontend/src/components/sources/WatchedFoldersSection.test.tsx
autonomous: true
requirements: [WATCH-03, WATCH-04, WATCH-05, WATCH-06, WATCH-07, WATCH-08]

must_haves:
  truths:
    - "Variant A ratified two-tier status badge renders on watch card: connection status pill on left (Connected vs Connection Off) and run outcome on right (WATCH-03)."
    - "Clicking 'Sync now' triggers row-level spinner and returns inline outcome ('✓ Synced just now (0 changes)') without unmounting or collapsing the accordion (WATCH-04)."
    - "Missing items in accordion display 'Missing at source since [timestamp] · Retained in Library' using item.missing_since (WATCH-05)."
    - "WR-09 fix: Microsoft Graph / OneDrive / SharePoint watches resolve to 'microsoft' in `watchProductMark.ts` and draw the official Microsoft mark."
    - "WATCH-06 fix: Disabled connection action button is labelled 'Open [Connection Name] in Settings ↗' / 'Open Connection Settings ↗' rather than an imperative verb that only navigates."
    - "WATCH-07 fix: `COPY.lastGood` drops preposition 'on' so relative and absolute timestamps format cleanly without collisions (e.g. 'Last read successfully 8m ago')."
    - "G-5 Hot File Extraction: `WatchRowCard.tsx` is extracted, bringing `WatchedFoldersSection.tsx` under 1,000 lines (from 1,095 down to ~650 lines)."
    - "Standing red baseline for `sourceComposition.test.tsx` remains strictly at 17 failed | 32 passed (no regressions)."
    - "FENCE: `backend/app/api/connectors.py` is byte-unchanged (0 lines modified)."
  artifacts:
    - path: "frontend/src/components/sources/WatchRowCard.tsx"
      provides: "Extracted row card component with Variant A dual pills, row-level sync, and missing items display"
      contains: "export function WatchRowCard"
    - path: "frontend/src/components/sources/WatchedFoldersSection.tsx"
      provides: "Watched folders list container (< 1,000 lines) with non-collapsing background refresh"
      contains: "WatchRowCard"
    - path: "frontend/src/components/sources/watchProductMark.ts"
      provides: "Product mark resolver supporting Microsoft Graph and OneDrive"
      contains: "microsoft"
    - path: "frontend/src/components/sources/sourceHealthVocabulary.ts"
      provides: "Clean time vocabulary without preposition collision and honest navigation label"
      contains: "Settings ↗"
  key_links:
    - from: "frontend/src/components/sources/WatchedFoldersSection.tsx"
      to: "frontend/src/components/sources/WatchRowCard.tsx"
      via: "WatchRowCard component render"
      pattern: "<WatchRowCard"
    - from: "frontend/src/components/sources/WatchRowCard.tsx"
      to: "frontend/src/components/sources/sourceHealthVocabulary.ts"
      via: "CONTROL_FOR_CAUSE and COPY.lastGood"
      pattern: "CONTROL_FOR_CAUSE"
---

<objective>
**WATCH-03, WATCH-04, WATCH-05 (UI), WATCH-06, WATCH-07, and WR-09: Watched Folders Surface & Component Extraction.**

Refactor the Watched Folders frontend surface to deliver ratified Variant A:
1. Extract `WatchRowCard.tsx` from `WatchedFoldersSection.tsx` to satisfy G-5 and bring `WatchedFoldersSection.tsx` from 1,095 lines down under 1,000 lines.
2. Implement Variant A Two-Tier Status Pill (`● Connected` vs `⊙ Connection Off` + Run Outcome).
3. Prevent row unmounting on Sync Now and background refresh, displaying inline feedback directly adjacent to the control (`✓ Synced just now (0 changes)`).
4. Display `missing_since` timestamps on remote-vanished files in the tracked files accordion.
5. Fix WR-09 (Microsoft Graph product marks in `watchProductMark.ts`).
6. Fix WATCH-06 (honest action navigation label: "Open Connection Settings ↗").
7. Fix WATCH-07 (clean time vocabulary without preposition collision in `COPY.lastGood`).

Strict Boundaries:
- `backend/app/api/connectors.py` is strictly fenced (0 lines touched).
- Inherited test `sourceComposition.test.tsx` remains exactly 17 failed | 32 passed.
</objective>

<threat_model>
### Security & Reliability Invariants (ASVS L1)
1. **Consequence != Receipt (Action Honesty):**
   - Invariant: A button that navigates must never be labelled with an imperative mutation verb ("Turn on", "Fix", "Resolve"). It must clearly state "Open ... in Settings ↗".
2. **State Transition Stability (No Layout Shifts / Collapse Trashing):**
   - Invariant: Background polling and row-level sync operations must never toggle global container `loading=true`, which would unmount child cards, drop open accordions, or trash local DOM state.
3. **Fail-Quiet Per-File Error Reporting:**
   - Invariant: Missing or failed items must never crash or block rendering of the parent watch row card.
</threat_model>

<context>
- Ratified Sketch: `.planning/sketches/247-sources-and-watches/index.html` (Variant A approved by Operator).
- `WatchedFoldersSection.tsx:1095` lines (G-5 violation > 1,000 lines). Extraction of `WatchRowCard.tsx` reduces size to ~650 lines.
- `BUG-260909-03`: Watch health conflates connection health with sync run outcome.
- `BUG-260909-04`: "Sync now" unmounts rows due to global `setLoading(true)` in `loadWatches()`.
- `BUG-260909-05`: `missing_since` timestamp present in database item payload, needed in UI file failure/missing list.
- `BUG-260909-06`: Fix button only navigates to Settings tab, needs honest navigation label.
- `BUG-260909-07`: `COPY.lastGood` prepends `"on "` colliding with relative phrases ("on 8m ago").
- `WR-09`: `watchProductMark.ts` returns `null` for Microsoft Graph.
</context>

<tasks>

<task type="auto" id="247-03-01">
<name>Fix WR-09 Microsoft Graph Product Marks</name>
<files>frontend/src/components/sources/watchProductMark.ts, frontend/src/components/sources/watchProductMark.test.ts</files>
<action>
1. In `watchProductMark.ts`:
   - Extend `watchProductMarkKey`:
     - When `service` starts with `"microsoft"`, or includes `"graph"`, `"onedrive"`, or `"sharepoint"`:
       - If `folder.startsWith("mailbox:") || folder === "mailbox_root"`: return `null` (no borrowed mail mark).
       - Otherwise: return `"microsoft"` (resolves to Microsoft mark via `connectionMark.tsx`).
2. In `watchProductMark.test.ts`:
   - Add unit tests verifying Microsoft Graph folder IDs resolve to `"microsoft"` and resolve to the Microsoft brand mark.
   - Verify mail addresses for Microsoft still return `null` (no borrowed mark).
</action>
<verify>
<command>$env:CI="1"; node frontend/node_modules/vitest/vitest.mjs run frontend/src/components/sources/watchProductMark.test.ts</command>
</verify>
</task>

<task type="auto" id="247-03-02">
<name>Fix WATCH-06 Action Honesty & WATCH-07 Time Vocabulary</name>
<files>frontend/src/components/sources/sourceHealthVocabulary.ts, frontend/src/components/sources/sourceHealthVocabulary.test.ts</files>
<action>
1. In `sourceHealthVocabulary.ts`:
   - WATCH-06: Update `CONTROL_FOR_CAUSE.connection_disabled.label`:
     - Change from `"Turn ${named(connectionName)} back on"` to:
       `(connectionName) => { const n = named(connectionName); return n === UNNAMED_CONNECTION ? "Open Connection Settings ↗" : `Open ${n} in Settings ↗` }`
   - WATCH-07: Update `COPY.lastGood`:
     - Drop preposition `"on"`:
       `lastGood: (when: string) => `Last read successfully ${when}`,`
2. In `sourceHealthVocabulary.test.ts`:
   - Update assertions for `CONTROL_FOR_CAUSE.connection_disabled.label` to expect `"Open Marketing Drive in Settings ↗"` and `"Open Connection Settings ↗"`.
   - Update assertions for `COPY.lastGood` to expect `"Last read successfully 2 September, 09:14"` (without "on") and verify relative inputs like `"Last read successfully 8m ago"`.
   - Keep all 69 tests green.
</action>
<verify>
<command>$env:CI="1"; node frontend/node_modules/vitest/vitest.mjs run frontend/src/components/sources/sourceHealthVocabulary.test.ts</command>
</verify>
</task>

<task type="auto" id="247-03-03">
<name>Extract WatchRowCard.tsx & Implement Variant A Dual Pills, Non-Collapsing Sync & missing_since</name>
<files>frontend/src/components/sources/WatchRowCard.tsx, frontend/src/components/sources/WatchedFoldersSection.tsx</files>
<action>
1. Create `frontend/src/components/sources/WatchRowCard.tsx`:
   - Extract `WatchRow` and its related types/props (`WatchRowProps`) from `WatchedFoldersSection.tsx`.
   - Implement Variant A Two-Tier Status Badge:
     - Tier 1 (Connection Pill):
       - If connection is disabled (`stopped?.cause === "connection_disabled"` or connection disabled flag): Render `⊙ Connection Off` with neutral/amber badge styling (`border-amber-500/30 bg-amber-500/10 text-amber-500`).
       - If connection is active/healthy: Render `● Connected` (`border-emerald-500/30 bg-emerald-500/10 text-emerald-500`).
     - Tier 2 (Run Outcome Pill / Badge):
       - If last run succeeded: `✓ Healthy`.
       - If last run failed with transient status (e.g. 429 rate limit or timeout): Render secondary indicator `⚠ Run failed (429)` alongside connection pill without branding the entire connection as dead.
   - Row-level Sync Feedback (WATCH-04):
     - Track `isSyncing` and `syncResult` inline on the row:
       - When `onSyncNow` is in progress for this row: Display `<Loader2 className="animate-spin h-3.5 w-3.5" /> Syncing...`.
       - When sync completes: Display inline tag `✓ Synced just now (0 changes)` without collapsing the card.
   - Missing Items Display (WATCH-05):
     - In the tracked files section / failure list, render missing items (`item.state === "missing"`):
       `{item.name} · Missing at source since {formatDate(item.missing_since)} · Retained in Library`.
2. In `frontend/src/components/sources/WatchedFoldersSection.tsx`:
   - Import and render `WatchRowCard`.
   - In `loadWatches(initial = false)`:
     - Set `loading = true` ONLY when `initial` is true.
     - For subsequent polling / refreshes (e.g. interval or post-sync), do NOT set `loading = true`. This prevents the entire section from unmounting, maintaining scroll position and open accordions.
   - Assert `WatchedFoldersSection.tsx` is now < 1,000 lines (G-5 compliant).
</action>
<verify>
<command>$env:CI="1"; node frontend/node_modules/vitest/vitest.mjs run frontend/src/components/sources/WatchedFoldersSection.test.tsx</command>
</verify>
</task>

<task type="auto" id="247-03-04">
<name>Update WatchedFoldersSection.test.tsx & Verify Standing Red Baseline</name>
<files>frontend/src/components/sources/WatchedFoldersSection.test.tsx</files>
<action>
1. In `WatchedFoldersSection.test.tsx`:
   - Update tests to reflect new navigation label ("Open ... in Settings ↗").
   - Update tests to verify Variant A two-tier status badges (`Connected` vs `Connection Off`).
   - Add test ensuring row does not unmount when `Sync now` is triggered.
   - Verify all tests pass cleanly.
2. Run standing red baseline check:
   - Run `node node_modules/vitest/vitest.mjs run src/components/sources/sourceComposition.test.tsx`.
   - Verify it reproduces exactly 17 failed | 32 passed (no regression, inherited baseline preserved).
</action>
<verify>
<command>$env:CI="1"; node frontend/node_modules/vitest/vitest.mjs run frontend/src/components/sources/WatchedFoldersSection.test.tsx</command>
</verify>
</task>

</tasks>
