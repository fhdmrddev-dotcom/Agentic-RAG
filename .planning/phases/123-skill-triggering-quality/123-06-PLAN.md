---
phase: 123-skill-triggering-quality
plan: 06
type: execute
wave: 5
depends_on: ["123-01", "123-03", "123-05"]
files_modified:
  - frontend/src/components/skills/SkillFormDialog.tsx
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/pages/SkillsPage.tsx
  - frontend/src/types/index.ts
  - frontend/src/components/skills/SkillFormDialog.test.tsx
autonomous: true
requirements: [TRIG-03, TRIG-01]
must_haves:
  truths:
    - "A weak-description lint warning renders inline directly under the Description textarea with the specific reason codes; the save always proceeds — never blocks (044-A / D-09)."
    - "The warning offers a one-click 'Tune this' that opens the Trigger Tuner for that skill (D-12 → TRIG-03→TRIG-01 one flow)."
    - "When the description is healthy, no warning renders (silent-when-healthy)."
    - "The builder-model knob (D-08) is a Settings picker selectable across the FULL provider list incl. local (Ollama / LM Studio / OpenAI-compat / DeepSeek-on-own-infra) with a strong default and an always-on cloud/local footer; it is decoupled from the benchmark targets (builder writes, targets measure) — no paid-provider SPOF (044-A)."
  artifacts:
    - path: "frontend/src/components/skills/SkillFormDialog.tsx"
      provides: "inline lint warning under Description + 'Tune this' handoff (renders once in the shared SkillForm)"
      contains: "Tune this"
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "the skill_builder_model picker reusing the provider-picker + cloud/local footer pattern"
      contains: "skill_builder_model"
  key_links:
    - from: "frontend/src/components/skills/SkillFormDialog.tsx (lint render)"
      to: "the lint_warnings from POST/PATCH /skills (Plan 01)"
      via: "render the {code, message} warnings under the Description Textarea"
      pattern: "lint_warnings"
    - from: "frontend/src/components/skills/SkillFormDialog.tsx ('Tune this' button)"
      to: "the skill-tuner ActiveView (Plan 05 onTuneSkill seam)"
      via: "onTuneSkill(skillId) navigates to the Tuner for this skill"
      pattern: "onTuneSkill"
    - from: "frontend/src/pages/SettingsPage.tsx (builder-model picker)"
      to: "the skill_builder_model app setting (Plan 03)"
      via: "read/write skill_builder_model via the settings api"
      pattern: "skill_builder_model"
---

<objective>
Close the TRIG-03 authoring loop in the UI (sketch 044-A) and surface the D-08 builder-model knob: (1) render the never-block lint warning inline directly under the Description textarea in the shared `SkillForm` (covers both the modal `SkillFormDialog` and the 3-pane `SkillDetailPanel`), with the specific reason codes and a one-click "Tune this" handoff into the Trigger Tuner (D-12); (2) add the `skill_builder_model` picker to Settings — selectable across the full provider list incl. local, strong default, always-on cloud/local footer, decoupled from the benchmark targets, no SPOF (mirrors the Phase-111.1 provider-picker footer).

Purpose: This ties the save-time lint (Plan 01) to the Tuner (Plan 05) as one authoring flow, and exposes the configurable builder model the operator explicitly asked for (no paid-provider lock).
Output: the inline lint warning + "Tune this" in SkillFormDialog, the Settings builder-model picker, the types addition (lint_warnings), the SkillsPage prop thread, and a frontend test.
Depends on Plan 01 (the lint_warnings response field), Plan 03 (the skill_builder_model setting), and Plan 05 (the onTuneSkill seam + skill-tuner ActiveView already threaded through App/ChatLayout/SkillsPage). Binding contract = sketch 044-A + MANIFEST decision 35.

Dependency note: wave 5 (after Plan 05) because the "Tune this" button consumes the `onTuneSkill` prop Plan 05 threads through App/ChatLayout/SkillsPage, and to avoid a same-wave conflict on shared frontend files. This plan does NOT edit `frontend/src/lib/api.ts` — it consumes the existing skill create/update + settings api calls (only `types/index.ts` gains the `lint_warnings` typing).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/123-skill-triggering-quality/123-CONTEXT.md
@.planning/phases/123-skill-triggering-quality/123-PATTERNS.md
@.planning/sketches/044-lint-warning-and-handoff/README.md
@.planning/sketches/MANIFEST.md

<interfaces>
<!-- Verified at source this session. -->

SkillFormDialog.tsx:69-78 (the shared SkillForm Description block — the lint warning mounts DIRECTLY under this Textarea, variant A): a flex-col div with a Description label + a Textarea (value=description, onChange=setDescription, placeholder "One sentence describing what this skill does", rows=2). Both SkillFormDialog (modal) and SkillDetailPanel (3-pane, :291) share the SkillForm inner component → the warning renders in ONE place. handleSave (:220 / :347) always proceeds.

lint_warnings: from Plan 01, POST/PATCH /skills returns SkillResponse with an optional lint_warnings: list[{code, message}] field. (The save already succeeded — the warning is advisory; the existing createSkill/updateSkill api calls already return this payload — only the TS type needs the field.)

onTuneSkill seam (Plan 05): App threads onTuneSkill(skillId) → setTunerSkillId + setActiveView("skill-tuner") down through ChatLayout → SkillsPage. This plan threads it the last hop SkillsPage → SkillForm so the "Tune this" button can fire it.

SettingsPage.tsx: the Phase-111.1 provider-picker (sketch 024 synthesis) + the embedding/extraction model-id select + the always-on cloud/local endpoint footer (SettingsPage.tsx ~:934-959). Mirror that picker shape for skill_builder_model. The app_settings carry the skill_builder_model field (Plan 03, user_settings.py); the existing settings read/write api handles it.

Real builder default label: claude-haiku-4-5 (A7) — but the picker MUST offer the full provider list incl. local (Ollama/LM Studio/OpenAI-compat/DeepSeek-on-own-infra). Decoupled from the benchmark targets (the "builder writes, targets measure" note).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline lint warning under Description + "Tune this" handoff (TDD on the render)</name>
  <files>frontend/src/components/skills/SkillFormDialog.tsx, frontend/src/pages/SkillsPage.tsx, frontend/src/types/index.ts, frontend/src/components/skills/SkillFormDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillFormDialog.tsx:60-90 (the shared SkillForm Description block — mount point) + the handleSave region (the save flow that must keep proceeding) + the SkillDetailPanel host
    - .planning/sketches/044-lint-warning-and-handoff/README.md (variant A — inline under Description, specific reasons, "Tune this", never block, never silent)
    - .planning/phases/123-skill-triggering-quality/123-PATTERNS.md §"SkillFormDialog.tsx" (the 044-A mount + the shared-component note)
    - frontend/src/types/index.ts (Skill / SkillResponse types — add lint_warnings)
    - frontend/src/pages/SkillsPage.tsx (where onTuneSkill arrives from Plan 05 — thread it into SkillForm)
  </read_first>
  <behavior>
    - Test: given a save that returns lint_warnings (e.g. name_echo + no_trigger_verb), the form renders the specific reason text under the Description field AND a "Tune this" button.
    - Test: given lint_warnings empty/absent, NO warning renders (silent-when-healthy).
    - Test: the save still completes when warnings are present (the warning is advisory — never blocks the save flow).
    - Test: clicking "Tune this" fires onTuneSkill with the skill's id (handoff to the Tuner).
  </behavior>
  <action>
    Add `lint_warnings?: { code: string; message: string }[]` to the Skill/SkillResponse type in `types/index.ts` (the existing createSkill/updateSkill api calls already return this payload — no api.ts edit needed). In the shared `SkillForm` (SkillFormDialog.tsx), after a save resolves, hold the returned `lint_warnings` in local state and render them DIRECTLY under the Description `<Textarea>` (variant A — closest to the field): each warning's specific message + a single "Tune this" button. The warning is advisory — `handleSave` ALWAYS proceeds (never block — D-09); render nothing when warnings are empty (silent-when-healthy). Thread the `onTuneSkill?: (skillId: string) => void` prop the last hop from SkillsPage (which received it from Plan 05) into SkillForm so "Tune this" navigates to the Trigger Tuner for this skill. Write `SkillFormDialog.test.tsx` covering the four behaviors FIRST (RED) then implement.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/components/skills/SkillFormDialog.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lint_warnings" frontend/src/components/skills/SkillFormDialog.tsx frontend/src/types/index.ts` shows >=1 in each.
    - `grep -c "Tune this\|onTuneSkill" frontend/src/components/skills/SkillFormDialog.tsx` >= 2 (the button + the handler).
    - The warning renders under the Description Textarea (variant A) and is silent when warnings are empty (tests green).
    - The save proceeds with warnings present (test green — never blocks).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The never-block lint warning renders inline under Description with specific reasons + a working "Tune this" handoff into the Tuner; silent when healthy; the save never blocks.</done>
</task>

<task type="auto">
  <name>Task 2: The D-08 builder-model picker in Settings (full provider list incl. local, no SPOF)</name>
  <files>frontend/src/pages/SettingsPage.tsx</files>
  <read_first>
    - frontend/src/pages/SettingsPage.tsx (~:934-959 — the Phase-111.1 provider-picker + model-id select + the always-on cloud/local endpoint footer to mirror for skill_builder_model)
    - .planning/sketches/044-lint-warning-and-handoff/README.md (the builder-model knob: strong default claude-haiku-4-5, full provider list incl. local, always-on cloud/local tag, "decoupled from the benchmark targets" note)
    - .planning/sketches/MANIFEST.md decision 16.1 (the embedding/extraction picker + always-on endpoint footer — the reuse target)
  </read_first>
  <action>
    Add a `skill_builder_model` control to Settings, mirroring the Phase-111.1 provider-picker shape (the embedding/extraction select + the always-on cloud/local endpoint footer, decision 16.1). It MUST offer the full provider list including local options (Ollama / LM Studio / OpenAI-compat / DeepSeek-on-own-infra) — never a paid-provider-only list (no SPOF; mirrors the 111.1 embedding-SPOF removal). Show the strong default (`claude-haiku-4-5`) when unset, an always-on cloud/local tag in the footer, and a short "decoupled from the benchmark targets — the builder writes candidates, the targets measure firing" note. Read/write the value through the existing settings api (the app_settings field exists from Plan 03). Do NOT couple this control to the Tuner's target selection.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "skill_builder_model" frontend/src/pages/SettingsPage.tsx` >= 1.
    - The picker offers local options (Ollama / LM Studio / OpenAI-compat) — not a paid-only list (source assertion against the picker options).
    - An always-on cloud/local footer + the "decoupled from benchmark targets" note are present.
    - The control reads/writes the app_settings skill_builder_model field; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The builder-model picker in Settings is provider-agnostic (full list incl. local, no SPOF), shows the strong default + cloud/local footer + the decoupled-from-targets note, and persists via app_settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill form -> save response | The lint_warnings render is advisory display of a server result |
| Settings picker -> app_settings | The builder-model id is written to settings (a value, not a secret) |
| "Tune this" -> Tuner | Navigation to a skill-scoped focused surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-06-01 | Repudiation / honesty | The lint warning is silent or non-specific, letting a weak description ship unnoticed | mitigate | Variant A renders the warning inline under Description with the SPECIFIC reason codes; silent ONLY when healthy. Tests assert specific-reason render + silent-when-healthy. Never-block is by design (D-09). |
| T-123-06-02 | Tampering / availability | Hardcoding a paid provider as the only builder option re-introduces a SPOF | mitigate | The picker offers the full provider list incl. local (Ollama/LM Studio/OpenAI-compat/DeepSeek-on-own-infra); the resolver (Plan 03) has an honest-None floor. Source assertion confirms local options present. |
| T-123-06-03 | Information Disclosure | The builder-model id treated as a secret / echoed where keys live | mitigate | A model id is a VALUE not a secret -> app_settings/Settings UI (CLAUDE.md). No API key is rendered or echoed by this control. |
| T-123-06-SC | Tampering | npm/pip installs | accept | No packages installed this plan (zero new deps per RESEARCH). |
</threat_model>

<verification>
- `npx vitest run src/components/skills/SkillFormDialog.test.tsx` green.
- `npx tsc --noEmit` clean.
- grep gates pass (lint_warnings rendered; Tune-this wired; skill_builder_model picker with local options).
- SC#10 UAT (VALIDATION.md): the lint warning surfaces a real weak description on save and "Tune this" reaches the Tuner; the builder-model picker lists local options.
</verification>

<success_criteria>
- TRIG-03 authoring loop is complete in the UI: inline never-block warning + "Tune this" -> Tuner (sketch 044-A).
- The D-08 builder-model picker is provider-agnostic with no SPOF, decoupled from the targets.
- No DB schema change; no new external package.
</success_criteria>

<output>
Create `.planning/phases/123-skill-triggering-quality/123-06-SUMMARY.md` when done.
</output>
