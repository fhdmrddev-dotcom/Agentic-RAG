---
phase: 123-skill-triggering-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/skill_lint.py
  - backend/app/services/agent_loop.py
  - backend/app/services/openai_service.py
  - backend/app/api/skills.py
  - backend/app/services/tool_dispatcher.py
  - backend/app/models/skill.py
  - backend/tests/unit/test_skill_lint.py
  - backend/tests/unit/test_skill_catalog_note.py
  - backend/tests/integration/test_skills_lint.py
autonomous: true
requirements: [TRIG-03, TRIG-01]
must_haves:
  truths:
    - "Saving a skill with a weak description (name-echo / no verb / too short / generic / duplicate / >1024) returns a never-silent warning naming the specific reason, and the save still succeeds (TRIG-03 / D-09)."
    - "The same lint fires on POST /skills, PATCH /skills, AND the agent save_skill tool path — one shared utility, all three surfaces (D-10)."
    - "The agent's save_skill never aborts on a weak description — the warning is a non-fatal note in the tool result (D-09 / Pitfall 6)."
    - "The agent_loop.py catalog note now tells the model to call load_skill when the request clearly matches a catalog description, reconciled with LOAD_SKILL_TOOL — both surfaces tell one story (D-01)."
    - "The relaxed catalog-note policy text lives in a single shared constant the Tuner classifier can import (Pitfall 1 fidelity guard)."
  artifacts:
    - path: "backend/app/services/skill_lint.py"
      provides: "Deterministic, no-LLM, never-raises lint_description() returning a list of {code, message} warnings"
      contains: "def lint_description"
    - path: "backend/app/services/skill_lint.py"
      provides: "The relaxed D-01 catalog-note policy string as a shared module constant"
      contains: "LOAD_SKILL_POLICY"
    - path: "backend/tests/unit/test_skill_lint.py"
      provides: "Coverage of every warning code + never-raises contract"
    - path: "backend/tests/unit/test_skill_catalog_note.py"
      provides: "Structural assertion that the catalog note reconciles with LOAD_SKILL_TOOL"
  key_links:
    - from: "backend/app/api/skills.py:create_skill / update_skill"
      to: "backend/app/services/skill_lint.py:lint_description"
      via: "pre-persist call, warnings attached to response, save always proceeds"
      pattern: "lint_description"
    - from: "backend/app/services/tool_dispatcher.py:_handle_save_skill"
      to: "backend/app/services/skill_lint.py:lint_description"
      via: "non-fatal note in ToolResult, never raises"
      pattern: "lint_description"
    - from: "backend/app/services/agent_loop.py catalog note"
      to: "backend/app/services/skill_lint.py:LOAD_SKILL_POLICY"
      via: "import of the shared policy constant"
      pattern: "LOAD_SKILL_POLICY"
---

<objective>
Deliver TRIG-03 (the deterministic save-time description lint) and the load-bearing D-01 runtime relaxation that makes the whole phase non-decorative. The lint is a pure, no-LLM, warn-never-block heuristic living in one shared service so it covers the human Skills form (POST/PATCH) AND the agent's save_skill tool from a single place. D-01 replaces the over-conservative `## Available Skills` catalog note in `agent_loop.py` so a skill's description actually drives `load_skill` firing — reconciling the existing contradiction with `LOAD_SKILL_TOOL`. The relaxed policy text is extracted to a shared constant in `skill_lint.py` so the Tuner's classifier (Plan 03) measures the REAL production policy (Pitfall 1 fidelity guard).

Purpose: TRIG-03 ensures new skills start with descriptions that fire; D-01 is the runtime change that gives the Trigger Tuner something real to measure. Both are independent backend changes with no DB schema impact.
Output: `skill_lint.py` (utility + shared policy constant), 3 lint hook points, the relaxed catalog note, and 3 test files.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/123-skill-triggering-quality/123-CONTEXT.md
@.planning/phases/123-skill-triggering-quality/123-RESEARCH.md
@.planning/phases/123-skill-triggering-quality/123-PATTERNS.md

<interfaces>
<!-- Verified at source this session. The executor uses these directly. -->

The conservative catalog note to RELAX (backend/app/services/agent_loop.py:1099-1108):
  catalog_note = f"\n\n## Available Skills\nThe following skills are available. ONLY call `load_skill(skill_name)` when the user explicitly names a skill or says 'use [skill name]'. Never auto-load based on description similarity — wait for an explicit request:\n{catalog_lines}"
The owner-scoped enabled-skills query at agent_loop.py:1090-1094 builds catalog_lines — PRESERVE it untouched.

The already-D-01-aligned tool description (backend/app/services/openai_service.py:434-437):
  LOAD_SKILL_TOOL.description ≈ "Use when the user's request matches a skill in the catalog."

skills.py create_skill (:127) inserts {user_id, name, description, instructions, is_global} and returns result.data[0] (response_model=SkillResponse).
skills.py update_skill (:260) does body.model_dump(exclude_none=True), .eq("id", skill_id).eq("user_id", current_user["id"]), returns result.data[0]. The owner-scoped sibling query for lint context is the list query at :111-116 (.or_(user_id.eq.{id},is_global.eq.true)).

tool_dispatcher.py _handle_save_skill (:705-739): resolves name/description/instructions, owner-scoped existing-name check, insert OR update, returns ToolResult(result=json.dumps({"status": "...", "name": name})).

SkillResponse / SkillCreate / SkillUpdate live in backend/app/models/skill.py — read before adding a lint_warnings field.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the deterministic skill_lint utility + the shared D-01 policy constant (TDD)</name>
  <files>backend/app/services/skill_lint.py, backend/tests/unit/test_skill_lint.py</files>
  <read_first>
    - backend/app/services/context_window.py:45-68 (the `_parse_model_limits` pure-parse + `_TRIM_MARKER` module-constant idioms to mirror — pure, swallows bad input, never raises)
    - .planning/phases/123-skill-triggering-quality/123-RESEARCH.md §"Code Examples" lines 399-422 (the lint heuristic body + Claude's-discretion thresholds) and §"Common Pitfalls" Pitfall 6 (never-block)
    - .planning/phases/123-skill-triggering-quality/123-PATTERNS.md §"skill_lint.py" (module-constants + pure-fn pattern, the D-09/D-11 hard rule)
  </read_first>
  <behavior>
    - Test: empty/whitespace description → warning code "empty"
    - Test: description shorter than MIN_DESCRIPTION_CHARS (25) and non-empty → "too_short"
    - Test: description longer than MAX_DESCRIPTION_CHARS (1024) → "too_long" (the free STD-01 hint)
    - Test: >60% of description tokens are just the skill name → "name_echo"
    - Test: no trigger verb present (none of TRIGGER_VERBS) → "no_trigger_verb"
    - Test: description matches a GENERIC_PHRASES bucket → "generic"
    - Test: description duplicates a sibling description (case-insensitive normalize) → "duplicate"
    - Test: a healthy description ("Use to generate a quarterly risk register from KB documents") → EMPTY list
    - Test: lint_description NEVER raises — pass None, non-str, huge input, weird unicode → always returns a list
  </behavior>
  <action>
    Create `backend/app/services/skill_lint.py` as a PURE module: no I/O, no LLM, no DB, NEVER raises (D-09/D-11). Define module-level tunable constants `MIN_DESCRIPTION_CHARS=25`, `MAX_DESCRIPTION_CHARS=1024`, `NAME_ECHO_RATIO=0.6`, a `TRIGGER_VERBS` set, and a `GENERIC_PHRASES` set (use the RESEARCH defaults; you may extend the sets sensibly). Implement `lint_description(name: str, description: str, sibling_descriptions: list[str]) -> list[dict]` returning a list of `{"code": str, "message": str}` warnings — EMPTY list means healthy. Each message names the specific reason (never a generic "weak description"). Wrap the body defensively so malformed input returns `[]` or partial warnings rather than raising. Also define the shared D-01 policy string as a module constant `LOAD_SKILL_POLICY` (the relaxed catalog-note text — e.g. "Call `load_skill(skill_name)` when the user's request clearly matches one of these skill descriptions. Match on intent, not just exact names. Do not load a skill for an unrelated request.") so Task 2 and the Plan 03 classifier import ONE source of truth (Pitfall 1). Write `test_skill_lint.py` covering every behavior above FIRST (RED), then implement to GREEN.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_skill_lint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_skill_lint.py` exercises all 7 warning codes + the healthy-empty case + the never-raises contract, all green.
    - `skill_lint.py` exports `lint_description` and `LOAD_SKILL_POLICY`; grep confirms: `grep -c "def lint_description\|LOAD_SKILL_POLICY" backend/app/services/skill_lint.py` ≥ 2.
    - `lint_description` performs zero imports of supabase / forced_emit / any LLM client (pure): `grep -v '^#' backend/app/services/skill_lint.py | grep -c "forced_emit\|supabase\|httpx\|openai" == 0`.
  </acceptance_criteria>
  <done>The deterministic lint exists, returns specific warning codes, never raises, and exposes the shared D-01 policy constant; its unit suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Relax the D-01 catalog note + wire the lint into all three save surfaces</name>
  <files>backend/app/services/agent_loop.py, backend/app/services/openai_service.py, backend/app/api/skills.py, backend/app/services/tool_dispatcher.py, backend/app/models/skill.py, backend/tests/unit/test_skill_catalog_note.py, backend/tests/integration/test_skills_lint.py</files>
  <read_first>
    - backend/app/services/agent_loop.py:1085-1115 (the catalog query at :1090-1094 to PRESERVE + the catalog_note at :1099-1108 to relax)
    - backend/app/services/openai_service.py:430-467 (LOAD_SKILL_TOOL / SAVE_SKILL_TOOL — confirm one story, do not change behavior)
    - backend/app/api/skills.py:105-145 (list/create — siblings come from the :111-116 owner-scoped query) and :260-283 (update_skill owner-scoping to PRESERVE)
    - backend/app/services/tool_dispatcher.py:705-739 (_handle_save_skill — the insert/update branches + ToolResult shape)
    - backend/app/models/skill.py (SkillResponse/SkillCreate/SkillUpdate — to add an optional lint_warnings response field)
    - .planning/phases/123-skill-triggering-quality/123-RESEARCH.md §"Code Examples" lines 369-384 (the D-01 contradiction) + Pitfall 6 (agent path never blocks)
  </read_first>
  <action>
    (1) D-01: in `agent_loop.py`, replace the over-conservative `catalog_note` body (the "ONLY call ... Never auto-load based on description similarity" text) with the relaxed policy by importing and interpolating `skill_lint.LOAD_SKILL_POLICY` — keep the `## Available Skills` header + `{catalog_lines}` and PRESERVE the owner-scoped query at :1090-1094 byte-for-byte. Do NOT change the `trim_messages_to_fit` call sites in this file (Plan 02 owns the trim path). (2) Add an optional `lint_warnings: list[dict] | None = None` field to `SkillResponse` in `skill.py`. (3) In `skills.py` `create_skill` and `update_skill`, call `lint_description(name, description, sibling_descriptions)` PRE-persist — fetch siblings via the existing owner-scoped `.or_(...)` list query (exclude the skill being edited on PATCH), then ALWAYS proceed with insert/update and attach the warnings to the returned row's `lint_warnings` (D-09 — never raise, never block). (4) In `tool_dispatcher.py` `_handle_save_skill`, after resolving name/description/instructions, call `lint_description(...)` and append the warnings as a non-fatal `"lint_warnings"` key in the `ToolResult(result=json.dumps(...))` — the insert/update still ALWAYS proceeds, NEVER raises (Pitfall 6). (5) Write `test_skill_catalog_note.py` asserting the relaxed note no longer contains the "ONLY call" / "Never auto-load" strings and DOES embed `LOAD_SKILL_POLICY` (structural reconciliation with LOAD_SKILL_TOOL). (6) Write `test_skills_lint.py` integration test: POST/PATCH a weak-description skill returns `lint_warnings` non-empty AND HTTP 201/200 (save proceeded); the agent save_skill path returns warnings in the tool result without raising.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_skill_catalog_note.py backend/tests/integration/test_skills_lint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The relaxed note: `grep -c "ONLY call\|Never auto-load" backend/app/services/agent_loop.py == 0` AND `agent_loop.py` imports `LOAD_SKILL_POLICY` from `skill_lint`.
    - All three save surfaces call `lint_description`: `grep -rc "lint_description" backend/app/api/skills.py backend/app/services/tool_dispatcher.py` shows ≥1 in each file.
    - `update_skill` still carries `.eq("id", skill_id).eq("user_id", current_user["id"])` (owner-scoping preserved).
    - The agent-path save still returns a `status` (created/updated) — the lint did NOT block it; integration test asserts save proceeded for a weak description.
    - The owner-scoped catalog query at agent_loop.py:1090-1094 is unchanged (diff shows only the note string + an import).
  </acceptance_criteria>
  <done>D-01 is live (note reconciled with LOAD_SKILL_TOOL, one shared policy constant), the lint surfaces on all three save paths warn-never-block, and the structural + integration tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/agent → POST/PATCH /skills | Author-typed description crosses into persisted skill state |
| agent loop → save_skill tool | The model's own authored description crosses into persisted skill state |
| model → load_skill firing decision | The relaxed catalog note influences which skill the model loads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01-01 | Tampering / Elevation | D-01 relaxation causes a previously-conservative skill to false-fire on unrelated prompts, loading the wrong instructions | mitigate | The relaxed wording explicitly says "Do not load a skill for an unrelated request"; load_skill still only loads the author's own/global enabled skills (owner-scoped query preserved). The measured guard is the Tuner's should-NOT benchmark axis (Plan 03/04) + the SC#10 cross-provider UAT (VALIDATION.md) — MANDATORY for this phase. Contingency per-skill `auto_trigger` flag is a documented fast-follow only if UAT shows regression. |
| T-123-01-02 | Information Disclosure | The PATCH/create lint fetches sibling descriptions for the duplicate check | mitigate | Siblings fetched via the EXISTING owner-scoped `.or_(user_id.eq.{id}, is_global.eq.true)` query — only the author's own + global skills, never another user's private skills. |
| T-123-01-03 | Denial of Service | Lint on the agent save_skill path hard-fails and aborts the agent's task | mitigate | `lint_description` is pure and NEVER raises (D-09/D-11/Pitfall 6); the agent save always proceeds and the warning is a non-fatal note. Unit test asserts the never-raises contract. |
| T-123-01-SC | Tampering | npm/pip installs | accept | No packages installed this plan (RESEARCH §Package Legitimacy Audit: zero new deps). No install task exists. |
</threat_model>

<verification>
- `backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_skill_lint.py backend/tests/unit/test_skill_catalog_note.py backend/tests/integration/test_skills_lint.py -x -q` green.
- G-5 regression (this plan touches agent_loop.py): `backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_context_window.py backend/tests/integration/test_075_4_subagent_truncation.py -q` still green (the note change must not regress the trim path).
- `grep` gates in each task's acceptance_criteria pass.
</verification>

<success_criteria>
- TRIG-03 lint deterministic, never-blocks, surfaces on POST /skills + PATCH /skills + agent save_skill (D-09/D-10/D-11).
- D-01 catalog note relaxed and reconciled with LOAD_SKILL_TOOL via one shared `LOAD_SKILL_POLICY` constant.
- Owner-scoped skill reads preserved; no DB schema change.
</success_criteria>

<output>
Create `.planning/phases/123-skill-triggering-quality/123-01-SUMMARY.md` when done.
</output>
