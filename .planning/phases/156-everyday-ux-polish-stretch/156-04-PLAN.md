---
phase: 156-everyday-ux-polish-stretch
plan: 04
type: execute
wave: 3
depends_on: ["156-01", "156-02", "156-03"]
use_worktrees: false
autonomous: true
requirements: [POLISH-01]
files_modified:
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/lib/threadGroups.ts
  - frontend/src/lib/__tests__/threadGroups.test.ts
  - frontend/src/components/layout/ChatHistoryColumn.tsx
  - frontend/src/components/layout/__tests__/ChatHistoryColumn.test.tsx

must_haves:
  truths:
    - "The mobile drawer's thread list gains a 'Search chats…' title-search box reusing the SHARED matchesTitle predicate (Plan 01) — SC#2 reaches mobile (D-08); the drawer's flat list, New Chat, folder picker, bottom nav-icon row, and probe-gated operator shield stay otherwise byte-identical."
    - "The desktop rail/history split stays desktop-only (hidden md:flex) and the mobile drawer keeps its structure with NO ⌘K — mobile reaches search via the drawer box (D-08, desktop-keyboard-first)."
    - "[OPTIONAL / CUT-ABLE per D-04 & D-08 discretion] A Date⇄Folder segmented toggle (default Date) on the desktop column and mobile date grouping land ONLY if trivial; they gate NO Success Criterion — SC#3 is already satisfied by the Wave-1 date grouping, so this task may be deferred as a fast-follow without touching the acceptance bar."
  artifacts:
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "mobile drawer title search over the shared matchesTitle predicate"
      contains: "matchesTitle"
  key_links:
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "frontend/src/lib/threadGroups.ts"
      via: "mobile drawer filter reuses matchesTitle"
      pattern: "matchesTitle"
---

<objective>
Wave 3 — the cut-able tail (D-06): bring the SC#2 search to the mobile drawer (D-08), then OPTIONALLY add the Date⇄Folder toggle + mobile date grouping if they land trivially (D-04/D-08 discretion). None of this gates the three Success Criteria — they all shipped in Wave 1.

Purpose: Keep the mental model consistent on mobile (the drawer filters its list the same way the desktop column does) by reusing the ONE tested `matchesTitle` predicate. The optional Task 2 is explicitly deferrable: it exists only to opportunistically realize the sketch's `groupByFolder` toggle if it is a few lines — SC#3 does not depend on it.
Output: the mobile drawer search in `ChatLayout.tsx`; optionally a `groupByFolder` helper + a Date⇄Folder toggle in the column.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-everyday-ux-polish-stretch/156-CONTEXT.md
@.planning/phases/156-everyday-ux-polish-stretch/156-RESEARCH.md
@.planning/sketches/078-chat-history-home/index.html
@frontend/src/components/layout/ChatLayout.tsx
@frontend/src/lib/threadGroups.ts
@frontend/src/components/layout/ChatHistoryColumn.tsx

<interfaces>
<!-- threadGroups (Plan 01): matchesTitle(t,q), groupByDate(threads), HighlightTitle({title,query}),
     folderLabel(folders, folderId). Task 2 (optional) adds groupByFolder(threads, folders). -->

<!-- ChatLayout mobile drawer (frontend/src/components/layout/ChatLayout.tsx:234-340): the flat threads.map (:264-292),
     the New Chat button + folder picker (:242-263), and the bottom nav-icon row + probe-gated shield (:294-338)
     — the shield MUST stay (D-07). The rail/history split is desktop-only (hidden md:flex); this drawer is md:hidden. -->

<!-- Sketch (index.html): groupByFolder at :248-249; the `.seg`/`.seg-btn` Date⇄Folder toggle at :120-122,307;
     rowCompact mode-swap (folder chip vs relative-date meta) at :251-255. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a title-search box to the mobile drawer thread list (D-08)</name>
  <files>frontend/src/components/layout/ChatLayout.tsx</files>
  <read_first>
    - frontend/src/components/layout/ChatLayout.tsx:234-293 — the mobile drawer: the flat `threads.map` (:264-292), the New Chat button + folder `<select>` (:242-263), and the empty-state (:265-267)
    - frontend/src/lib/threadGroups.ts — `matchesTitle` (and `HighlightTitle` if you choose to highlight)
    - 156-CONTEXT.md D-08 (keep the drawer structure; add title search; the split stays desktop-only; no mobile ⌘K)
  </read_first>
  <action>
    In the mobile drawer, add a controlled `<input>` (placeholder "Search chats…", a `mobileQuery` state, a lucide `Search` icon) directly ABOVE the `threads.map` (after the New Chat + folder picker block, before the list). Filter the mapped list with `matchesTitle(thread, mobileQuery)`; optionally wrap the row title in `<HighlightTitle title={thread.title} query={mobileQuery} />` for parity with the column. Render the honest empty-state "No chats match your search." when the filtered list is empty (reuse the existing italic empty-state styling). Keep EVERYTHING else in the drawer byte-identical — the New Chat button, the folder `<select>`, the row markup, the bottom nav-icon row, and the probe-gated operator shield (:323-337) must not change (D-07/D-08). Do NOT add any ⌘K handler on mobile (desktop-keyboard-first, D-08). Do not touch the desktop composition (Plan 02) or the palette (Plan 03).
  </action>
  <acceptance_criteria>
    - [test-command] `cd frontend && npx vitest run src/components/layout/__tests__/ChatLayoutLaunch.test.tsx` → green (no regression; the drawer is not open in that test), AND `cd frontend && npm run build` → **0 net-new tsc errors beyond the pre-existing baseline** (30 errors / 17 files as of 2026-07-16 HEAD `98c77718`, all unrelated to this phase; do NOT edit unrelated baseline files to force a literal exit 0).
    - [source] the mobile drawer renders a "Search chats…" input above `threads.map`, and the map is narrowed by `matchesTitle(thread, mobileQuery)`.
    - [source] the drawer's bottom nav row still renders the operator shield (`grep -c "Control Room" ChatLayout.tsx` unchanged; the `isOperator &&` drawer shield block intact) and no ⌘K handler was added to the drawer.
    - [behavior] the mobile search behavior rides the Plan-01 unit-tested `matchesTitle`; felt behavior is confirmed in the live mobile-drawer UAT.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/__tests__/ChatLayoutLaunch.test.tsx</automated>
    <!-- Machine-gate is the reliably-green vitest run. The `npm run build` type-check is the caveated acceptance-criteria [test-command] above — executor runs it and interprets vs. the documented 30-error baseline (do NOT chain `&& npm run build` here: it hard-exits non-zero on the pre-existing baseline and would false-fail the gate on an unattended run). -->
  </verify>
  <done>The mobile drawer renders a 'Search chats...' box filtering its list via matchesTitle; the bottom nav row + isOperator-gated shield stay byte-identical; no mobile Ctrl+K; ChatLayoutLaunch green + npm run build shows no net-new tsc errors beyond the documented 30-error baseline (do not fix unrelated files).</done>
</task>

<task type="auto">
  <name>Task 2 (OPTIONAL — cut-able): Date⇄Folder segmented toggle + mobile date grouping</name>
  <files>frontend/src/lib/threadGroups.ts, frontend/src/lib/__tests__/threadGroups.test.ts, frontend/src/components/layout/ChatHistoryColumn.tsx, frontend/src/components/layout/__tests__/ChatHistoryColumn.test.tsx</files>
  <read_first>
    - 156-CONTEXT.md "Claude's Discretion" + D-04/D-08 — this toggle + mobile grouping are IN-SCOPE-IF-CHEAP; the locked default is DATE and SC#3 does not depend on this
    - .planning/sketches/078-chat-history-home/index.html — `groupByFolder` (:248-249), the `.seg` Date⇄Folder toggle (:120-122,307), and the rowCompact mode-swap (:251-255)
    - frontend/src/lib/threadGroups.ts (add `groupByFolder`) and frontend/src/components/layout/ChatHistoryColumn.tsx (the header where the toggle sits + the group iteration)
  </read_first>
  <action>
    CUT-LINE FIRST: if adding this introduces new state complexity beyond a small `groupMode` state + one helper (i.e., more than a few lines), DEFER it — record it as a fast-follow in the SUMMARY and do NOT ship a half-measure (D-04: "only if cheap"). If it is trivial: add `groupByFolder(threads: Thread[], folders: Folder[]): {label: string; items: Thread[]}[]` to threadGroups.ts (group by folder name, "Unfiled" last, empty groups dropped, within-group updated_at DESC) with unit coverage in threadGroups.test.ts. In ChatHistoryColumn, add a `groupMode: "date" | "folder"` state DEFAULTING to "date", render a small segmented toggle (the sketch `.seg`) in the header, and branch the group iteration between `groupByDate` and `groupByFolder(threads, folders)` — in folder mode show the relative-date as the per-row meta instead of the folder chip (mirror the sketch rowCompact swap). Extend ChatHistoryColumn.test.tsx with a toggle test (default is Date; switching to Folder groups by folder name). Keep DATE the default so SC#3 is unaffected. Optionally mirror date grouping into the mobile drawer if equally trivial.
  </action>
  <acceptance_criteria>
    - [test-command] (if built) `cd frontend && npx vitest run src/lib/__tests__/threadGroups.test.ts src/components/layout/__tests__/ChatHistoryColumn.test.tsx` → green including the groupByFolder + toggle tests.
    - [behavior] (if built) the toggle DEFAULTS to Date; SC#3 date grouping is unchanged when the toggle is untouched; switching to Folder groups rows by folder name with "Unfiled" last.
    - [behavior] (if cut) the SUMMARY records the deferral as a fast-follow; no partial toggle is shipped; SC#3 remains satisfied by Wave-1 date grouping.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/threadGroups.test.ts src/components/layout/__tests__/ChatHistoryColumn.test.tsx</automated>
  </verify>
  <done>Either the Date-Folder toggle (default Date) + groupByFolder ship with green tests, or the deferral is recorded in the SUMMARY as a fast-follow (never a half-measure); SC#3 date grouping is unaffected either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-controlled thread title → mobile drawer rows / optional highlight | The drawer renders every thread title + its filtered highlight |

Pure-client (Architectural Responsibility Map: Browser/Client only). No endpoint, query, or backend change. The mobile filter reuses the same RLS-scoped `threads`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01 | Tampering (stored XSS) | mobile drawer title + optional highlight | mitigate | Titles render as JSX (`{thread.title}` / `HighlightTitle` from Plan 01) — never dangerouslySetInnerHTML. |
| T-156-02 | Info Disclosure | mobile filter set | accept | Filters only the caller's own RLS-scoped `threads`; no new fetch (ASVS V4 N/A — client not a trust boundary). |
| T-156-05 | Elevation of Privilege | mobile drawer operator shield | mitigate | The `isOperator`-gated drawer shield stays OUTSIDE `NAV_ITEMS` and byte-identical (D-07/Pitfall 4). |
| T-156-SC | Tampering (supply chain) | npm installs | mitigate | No package installed; supply-chain surface unchanged. No install task. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/layout/__tests__/ChatLayoutLaunch.test.tsx src/lib/__tests__/threadGroups.test.ts src/components/layout/__tests__/ChatHistoryColumn.test.tsx` — all green.
- `cd frontend && npm run build` → no net-new tsc errors beyond the documented 30-error baseline (do not fix unrelated files); `npm run lint:a11y` exit 0.
- Live Chrome-MCP: the mobile drawer shows the new "Search chats…" box and narrows its list; the bottom nav row + operator shield are unchanged; no ⌘K on mobile. (If Task 2 shipped: the Date⇄Folder toggle defaults to Date and switches cleanly.)
</verification>

<success_criteria>
- SC#2 reaches mobile: the drawer thread list is searchable via the shared predicate; the drawer otherwise stays byte-identical (D-08, D-07 shield intact).
- Optional Task 2 either ships trivially (default Date, SC#3 unaffected) or is cleanly deferred as a fast-follow — never a half-measure.
</success_criteria>

<output>
Create `.planning/phases/156-everyday-ux-polish-stretch/156-04-SUMMARY.md` when done.
</output>
