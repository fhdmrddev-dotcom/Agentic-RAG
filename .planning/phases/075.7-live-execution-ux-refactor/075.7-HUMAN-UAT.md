---
status: partial
phase: 075.7-live-execution-ux-refactor
source: [075.7-VERIFICATION.md]
started: 2026-05-24T00:00:00Z
updated: 2026-05-24T00:00:00Z
---

## Current Test

number: 2
name: 6 Playwright specs runtime execution
expected: |
  `cd frontend && npx playwright test --grep "@075.7"` runs 9 tests (scenario-09 fans
  out to 4 sub-tests for OpenAI/Anthropic/Google/OpenRouter) and all pass. Requires:
  backend dev server running, frontend dev server at localhost:5173, valid provider
  API keys, Docker sandbox running, supabase local stack up.
awaiting: user response

## Tests

### 1. Long-message axis Chrome MCP UAT (CLAUDE.md SC#10 Axis 4)
expected: With ≥50 prior messages OR ≥5 KB user prompt: sticky RunCard header pins during scroll; no auto-collapse mid-stream; brand-pulse avatar visible end-to-end; auto-collapse fires on terminal+tools>0; click collapsed row → body expands with `→ {summary}` rows on past tools. Auto-approved at Plan 03 Task 4 checkpoint per AUTO_MODE chain. Operator-driven verification per VALIDATION.md §Manual-Only Verifications.
result: pass
verified: 2026-05-24
resolution: "Diagnosed via 075.7-DEBUG-runstatus-transition.md as two RunCard-internal bugs (A: outerBannerLabel called unconditionally, B: wasStreamingRef stale-snapshot one-shot) plus two additional duplications surfaced during verification (C: ToolResultBlock rendering both chevron-button summary AND new T1 → summary; D: ToolCallPanel still rendering its outer frame + header inside RunCard's frame). Original runStatus-never-flips hypothesis was falsified — runStatus does transition correctly. All four bugs fixed in commits cb5899b (Bug A+B header gating + pure-derivation collapse) and 2c8e5b7 (Bug C+D ToolResultBlock dedup + ToolCallPanel body-only). User-confirmed pass after browser refresh on the same code-execution + 5-tool flow that produced the original failing screenshot."
resolving_commits: [cb5899b, 2c8e5b7]

### 2. 6 Playwright specs runtime execution
expected: `cd frontend && npx playwright test --grep "@075.7"` runs 9 tests (scenario-09 fans out to 4 sub-tests for OpenAI/Anthropic/Google/OpenRouter) and all pass.
result: partial
unblocked_fixes_committed:
  - b6bad76 — db-teardown schema (id→run_id), auth fixture content-assertion, in-spec URL checks removed, composer Send/Stop aria-label
  - 6dea591 — DevTwoPaneMock pointer-events-none, scenario-07 pressSequentially+Enter probe
deferred_blockers:
  - "scenario-09 Settings flow expects combobox/button shape that differs from production /settings UI — needs Chrome MCP exploration + rewrite."
  - "Live-message-render anomaly observed in Chrome MCP probe at 075.7 HEAD: chat round-trip succeeds at network layer (POST /threads/{id}/messages 201, GET /runs/{id}/stream 200) but messages don't render in active chat surface during streaming; render WORKS after reload + thread-click. Could be a regression from 2c8e5b7 OR pre-existing. Rollback experiment to cb5899b was inconclusive (Chrome MCP click+Enter didn't fire send on rolled-back tree). Needs dedicated debug session at 075.7.1 polish."
  - "scenarios 10/11/12 typing flow may need same pressSequentially+Enter pattern from scenario-07."
runtime_exec_deferred_reason: |
  6 inline fixes landed (cb5899b + 2c8e5b7 = 075.7 UI bugs; b6bad76 + 6dea591 = test-infra unblocks).
  Remaining work is either (a) per-spec rewrites against current UI taxonomy or (b) a live-stream
  render bug that needs its own debug session. Scoped to a follow-up test-infra polish phase.
note: |
  Despite Test 2 not reaching full green, the commits above are independently valuable
  codebase wins: schema correction, a11y improvements, fixture modernization, and the
  actual 075.7 UI bugs A+B+C+D are FIXED and verified visually via Test 1 pass.

### 3. Regression spot-check — Phase 067.5 Branch D-3 clearMessages guard
expected: Cross-thread switching during streaming does NOT wipe streaming-bucket thread. Guard lives at `StreamsProvider.tsx:711-726` (corrected ref per RESEARCH §10.1; original CONTEXT D-15 ref `useMessages.ts:572-590` is stale). Verify by: open Thread A, send long-running tool prompt, mid-stream click Thread B in sidebar, then return to Thread A — confirm Thread A's messages array still has the in-flight assistant turn.
result: [pending]

### 4. Regression spot-check — Phase 075.4-01 BUG-260523-01 per-thread composer
expected: Thread B composer remains enabled while Thread A is streaming. Read pattern at `ChatArea.tsx:222` uses `useStreamingForThread(thread?.id)` not global `useIsStreaming`. Verify by: trigger long stream on Thread A, navigate to Thread B, confirm Thread B composer accepts input + sends successfully without "Wait for current response" gate.
result: [pending]

### 5. Regression spot-check — Anthropic terminal-frame DOM order (BUG-260514-02 re-litigability)
expected: Anthropic-provider assistant turns at end-of-cycle still render the actions block in current DOM order so the deferred bug `anthropic-end-of-cycle-shows-actions-not-summary` (deferred 3×) remains re-litigable later. Auto-collapse from R-6 must NOT mask the terminal frame — expand-on-click restores full visibility per CONTEXT D-08. Verify by: Anthropic-provider tool-using turn → on terminal, expand collapsed RunCard → confirm the action-vs-summary block ordering is unchanged from pre-075.7 baseline.
result: [pending]

## Summary

total: 5
passed: 1
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

- truth: "Auto-collapse-on-completion (R-6) fires when runStatus !== 'streaming' AND tool_calls.length > 0; collapsed-row JSX appears; past-tool rows render via summarizeToolCall (R-4); streaming-phase copy ('Synthesizing answer…', 'Used 5 tools') is replaced with terminal-phase copy ('Run · N tool calls · ✓ done · {duration}s ▸')."
  status: resolved
  resolved: 2026-05-24
  resolving_commits: [cb5899b, 2c8e5b7]
  reason: "User-captured screenshot (Recordings/screencapture-localhost-5173-2026-05-24-12_17_52.png) showed a clearly terminal run (composer enabled, file outputs rendered, final assistant text rendered, 5/5 tools green-checked) where: (a) 'Synthesizing answer…' label still pinned at top; (b) RunCard body still fully expanded with all 5 tools in full ToolResultBlock form; (c) no '→ {summarizeToolCall(tc)}' one-liner rows on completed past tools; (d) header reads 'Used 5 tools' instead of the terminal-state. Plus two adjacent duplications: (e) each past tool rendered both '> {summary}' chevron-button AND '→ {summary}' new T1 row; (f) outer RunCard frame + inner ToolCallPanel frame stacked with two competing headers."
  resolution: "Diagnosed in 075.7-DEBUG-runstatus-transition.md. Four root causes: Bug A — outerBannerLabel (streaming-only helper) called unconditionally in RunCard.tsx:113. Bug B — wasStreamingRef + useEffect one-shot stale-snapshot couldn't reliably observe streaming→terminal edge under React 18 batching or temp-id→DB-id remount. Bug C — Plan 03 T1 added new `→ summary` row without removing pre-existing chevron-button summary row. Bug D — Plan 02 mounted RunCard wrapper but didn't strip ToolCallPanel's own outer frame + header + expanded state machinery. All four fixed via commits cb5899b (RunCard.tsx header gating + pure-derivation collapse via useState) and 2c8e5b7 (ToolResultBlock chevron-vs-→ XOR + ToolCallPanel body-only refactor; -84 LOC net). User confirmed `pass` on the same code-execution + 5-tool flow after browser refresh."
  severity: major
  test: 1
  affected_requirements: [R-4, R-5, R-6]
  symptoms_resolved: 6
  verification_gaps_to_close:
    - "No grep gate asserted header copy is bound to terminal state — would have caught Bug A"
    - "Vitest only tested lazy initializer, not the streaming→terminal rerender — would have caught Bug B"
    - "scenario-08-focus-mode-collapse.spec.ts authored but never executed during phase verification — would have caught Bug B"
    - "VERIFICATION.md grep gates pass while structural duplication (Bugs C+D) visible at first glance — felt-experience UAT must precede phase-close per CLAUDE.md G-4"
  artifacts: []
  missing: []
