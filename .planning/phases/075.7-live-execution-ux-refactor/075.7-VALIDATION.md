---
phase: 075.7
slug: live-execution-ux-refactor
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-24
---

# Phase 075.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 1.x (unit) + Playwright 1.x (e2e) |
| **Config file** | `frontend/vitest.config.ts` + `tests/e2e/playwright.config.ts` |
| **Quick run command** | `cd frontend && npm run build && npm run lint` (TS-gate per CONTEXT D-13) |
| **Full suite command** | `cd frontend && npm run build && npm run lint && npx playwright test --config tests/e2e/playwright.config.ts` |
| **Estimated runtime** | ~90 seconds (TS-gate ~25s + Playwright ~60s) |

> **Note (CONTEXT D-13):** Vitest specs ship alongside source files but execution is "local-vitest-blocked" — see `075.7-deferred-items.md` (will be created during execution if needed; same pattern as 063.1/075.4). Phase passes verification on TS-gate + Playwright + Chrome MCP.

---

## Sampling Rate

- **After every task commit:** Run `cd frontend && npm run build` (TypeScript gate; ~10s)
- **After every plan wave:** Run quick command (full TS + lint; ~25s)
- **Before `/gsd-verify-work`:** Full suite must be green (TS-gate + Playwright + manual Chrome MCP per axis)
- **Max feedback latency:** 25 seconds (TS-gate is the primary in-loop signal)

---

## Per-Task Verification Map

> Filled during plan-phase task breakdown. Reference table — each PLAN.md task adds rows when authored.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 075.7-01-XX | 01 | 1 | R-2 (LOC ≤707) | — | N/A | static-grep | `wc -l frontend/src/components/chat/ToolCallPanel.tsx` | ✅ W0 | ⬜ pending |
| 075.7-01-XX | 01 | 1 | R-1 (extraction) | — | N/A | static-grep | `ls frontend/src/components/chat/tool-bodies/*.tsx \| wc -l` (≥10) | ✅ W0 | ⬜ pending |
| 075.7-02-XX | 02 | 2 | R-3 (RunCard) | — | N/A | playwright | `npx playwright test tests/e2e/scenario-07-run-card-frame.spec.ts` | ❌ W0 | ⬜ pending |
| 075.7-03-XX | 03 | 3 | R-5 (auto-collapse) | — | N/A | playwright | `npx playwright test tests/e2e/scenario-08-focus-mode-collapse.spec.ts` | ❌ W0 | ⬜ pending |

> Planner fills full per-task matrix during plan-phase. Each row maps to one SPEC.md requirement (R-1…R-7).

---

## Wave 0 Requirements

**4-axis UAT bandwidth (MANDATORY per CLAUDE.md SC#10):**

Per CONTEXT D-14 + RESEARCH §7: 3-of-4 axes automated via Playwright; long-message stays manual Chrome MCP.

- [ ] `tests/e2e/scenario-07-run-card-frame.spec.ts` — RunCard mounts on tool-bearing turns; sticky header pins; brand-pulse avatar present. Covers R-3.
- [ ] `tests/e2e/scenario-08-focus-mode-collapse.spec.ts` — past tool cards collapse to `→ {summary}`; auto-collapse on terminal+tools>0. Covers R-4, R-5.
- [ ] `tests/e2e/scenario-09-cross-provider-tool-bodies.spec.ts` — **cross-provider axis** (OpenAI + Anthropic + Google + OpenRouter, one model each) — exercises new tool-bodies through each provider. Covers R-1.
- [ ] `tests/e2e/scenario-10-multi-tool-runcard.spec.ts` — **multi-tool axis** — single prompt invokes `search_documents` + `execute_code` in one turn; both bodies render correctly inside one RunCard.
- [ ] `tests/e2e/scenario-11-parallel-thread-runcard.spec.ts` — **parallel-thread axis** — Thread A streaming while Thread B accepts a new prompt; per-thread composer isolation (075.4-01 BUG-260523-01) verified; RunCard renders correctly on both.
- [ ] `tests/e2e/scenario-12-execute-code-body-rename.spec.ts` — ExecuteCodeBody renders identically to ExecuteCodeBlock baseline; STDOUT/STDERR/output-files preserved (R-1 + sketch D2 verification).

**Manual Chrome MCP scenario:**

- [ ] **Long-message axis** — at least one assistant turn with ≥50 prior messages or ≥5KB user prompt while RunCard is mid-stream. Verify sticky header survives long scroll; auto-collapse doesn't fire mid-stream; brand-pulse stays visible. Run via Chrome MCP at `localhost:5173`.

**Vitest specs (locally-blocked, ship to CI):**

- [ ] `frontend/src/components/chat/RunCard.test.tsx` — render snapshot, memo predicate, expand/collapse state ownership (CONTEXT D-05/D-06)
- [ ] `frontend/src/components/chat/tool-bodies/*Body.test.tsx` — one per Body file (10 total); default export renders; named `summarize(tc)` returns expected string per sketch findings examples
- [ ] `frontend/src/components/chat/tool-bodies/__registry__.test.tsx` — registry exhaustiveness (all 9 tools + execute_code mapped); summarize() coverage

**Regression invariants from CONTEXT D-15 (must stay GREEN):**

- [ ] Existing test: `streamsProvider_067_5_regression.test.tsx` (clearMessages guard — RESEARCH §3 corrected the path; original CONTEXT D-15 ref `useMessages.ts:572-590` is stale, lives at `StreamsProvider.tsx:711-726` post-068)
- [ ] Existing fixture extension: `tests/e2e/scenario-01-parallel-composers.spec.ts` — add RunCard assertions per CONTEXT D-15

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Long-message axis (≥50 prior msgs or ≥5KB prompt) | SC#10 axis 4 | Long history fixtures not yet plumbed into Playwright; one-off Chrome MCP scenario per provider | Open Chrome MCP at `localhost:5173`; load thread with ≥50 msgs OR paste ≥5KB prompt; trigger tool-using assistant turn; verify sticky header survives scroll, no auto-collapse mid-stream, brand-pulse visible end-to-end |
| Anthropic terminal-frame preservation | (deferred bug `anthropic-end-of-cycle-shows-actions-not-summary`) | Bug deferred 3× — needs rendering visible for future re-litigation | Run an Anthropic assistant turn that goes 5+ iterations; expand collapsed RunCard at terminal; confirm whatever the agent renders at end-of-cycle today is still visible (auto-collapse hides but expand-on-click restores per CONTEXT D-08) |
| Sticky-bottom indicator (BUG-260514-03 / B-260519-07) | CONTEXT D-12 two-sticky pattern | Visual interaction; Playwright doesn't reliably assert sticky positioning under scroll | Scroll up while a different thread is mid-stream; confirm BOTTOM sticky indicator at MessageList foot still appears; confirm TOP RunCard sticky header is independent (doesn't collide) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (6 scenario files + vitest specs)
- [ ] No watch-mode flags
- [ ] Feedback latency < 25s (TS-gate)
- [ ] 4-axis bandwidth covered: cross-provider ✓ (scenario-09), multi-tool ✓ (scenario-10), parallel-thread ✓ (scenario-11), long-message ✓ (manual Chrome MCP)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — fills during plan-phase task-row authoring; set to `approved 2026-MM-DD` after plan-checker passes.
