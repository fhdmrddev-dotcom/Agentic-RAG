---
phase: 135-self-improvement-loop-si-01
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/evals.py
  - backend/tests/test_skill_proposals_router.py
  - backend/tests/test_skill_proposals.py
autonomous: true
requirements: [SI-01]
gap_closure: true

must_haves:
  truths:
    - "Force-promoting a not_promoted proposal over HTTP with an empty/absent request body returns 200 (not 422) and records override_forced=true (CR-01 backend half — verification Truth 4)."
    - "A running re-eval whose task is absent from the local RUN_TASKS but whose eval_inflight Redis claim still names its run_id is left re_evaling — NOT falsely marked interrupted under WORKER_COUNT=2 (CR-02 — verification Truth 3)."
    - "A _launch_reeval failure after status='approved' reverts the proposal to 'proposed' (CAS-guarded), and a stale approved row with no re_eval_run_id self-heals to interrupted on read (CR-03 backend half — verification Truth 2)."
    - "The re-eval WITH-arm override map is keyed on the draft version name the catalog actually injects, so a rename between propose and approve still measures the DRAFT (WR-02)."
  artifacts:
    - path: "backend/app/api/evals.py"
      provides: "force-promote optional body, reconcile cross-worker liveness claim check, approve except/revert, reconcile stale-approved self-heal, override map keyed on draft version name"
      contains: "ForcePromoteBody | None"
    - path: "backend/tests/test_skill_proposals_router.py"
      provides: "HTTP-level (body-less) force-promote wire-contract test"
      contains: "force-promote"
    - path: "backend/tests/test_skill_proposals.py"
      provides: "reconcile running-branch cross-worker test + approve launch-failure revert test"
  key_links:
    - from: "reconcile_proposal running branch"
      to: "eval_inflight:{skill_id} Redis claim"
      via: "redis.get(_inflight_key(proposal['skill_id'])) compared to str(re_eval_run_id) before declaring orphaned"
      pattern: "_inflight_key\\(proposal"
    - from: "approve_skill_proposal step 8"
      to: "status='proposed' revert"
      via: "try/except around _launch_reeval with a CAS .eq('status','approved') UPDATE, then re-raise"
      pattern: "isinstance\\(claim, bytes\\)|_APPROVED_STALE_GRACE_S"
    - from: "_launch_reeval override map"
      to: "draft version name"
      via: "skill_instructions_override keyed on both skill.name and draft_version.name"
      pattern: "draft_version.get\\(\"name\"\\)"
---

<objective>
Close the three backend BLOCKER gaps and the verification-recommended WR-02 warning in the shared
self-improvement proposal/eval path — all in `backend/app/api/evals.py` — and lock them with backend
tests so re-verification can flip Truths 2, 3, and 4 of `135-VERIFICATION.md` from FAILED to VERIFIED.

Gap coverage (source = `135-VERIFICATION.md` gaps + `135-REVIEW.md` suggested fixes):
- **CR-01 (backend half)** — force-promote route requires a non-optional empty Pydantic body → make it optional. (Truth 4)
- **CR-02** — reconcile's `running` orphan check reads only the per-process `RUN_TASKS`; add a cross-worker Redis-claim liveness signal. (Truth 3)
- **CR-03 (backend half)** — approve commits `status='approved'` before `_launch_reeval` with no recovery → except/revert, plus reconcile self-heals a stale `approved` row. (Truth 2)
- **WR-02** — the re-eval override map is keyed only on the live skill name → key it on the draft version name the catalog injects too.

Every item above maps to a task below; no VERIFICATION gap is left unplanned. The frontend halves of
CR-01/CR-03 and WR-04 ship in the parallel plan `135-09` (no file overlap → same wave).

Purpose: restore the auto-re-eval gate and the human-override escape hatch that SI-01 promises.
Output: patched `evals.py` + a body-less HTTP force-promote test + a reconcile-running-branch test + an approve-launch-failure-revert test.

Cross-provider red line (CLAUDE.md / D-16): all edits are in the shared, provider-agnostic proposal/eval
path — NO provider-specific forks; the re-eval reuses the existing gateway routing verbatim.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/135-self-improvement-loop-si-01/135-VERIFICATION.md
@.planning/phases/135-self-improvement-loop-si-01/135-REVIEW.md
@.planning/phases/135-self-improvement-loop-si-01/135-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase this planning pass — use directly. -->

backend/app/api/evals.py (the ONE file for tasks 1-2):
- `_inflight_key(skill_id: str) -> str`  (:78)  -> returns `f"eval_inflight:{skill_id}"`
- `async def reconcile_proposal(supabase, redis, *, proposal_id, user_id) -> dict | None`  (:1121)
    · early-return: `if proposal.get("status") != "re_evaling": return proposal`  (:1155)
    · running branch: `elif run_status == "running":` reads `from app.api.threads import RUN_TASKS`; sets `new_status="interrupted"` iff `run_key not in RUN_TASKS`  (:1208-1219)
    · `_persist()` does the owner-scoped status UPDATE  (:1227-1234)
- `async def _launch_reeval(*, skill, draft_version, proposed_instructions, cases, provider, model, proposal_id, user_id, current_user, supabase, redis, pool) -> UUID`  (:1240)
    · override map (WR-02 target): `skill_instructions_override={(skill.get("name") or ""): proposed_instructions}`  (:1369)
    · CAS-releases the inflight claim on pre-spawn failure  (:1386)
- `async def approve_skill_proposal(skill_id, proposal_id, ...)`  (:1394)
    · step 7 `_mark_approved()` commits `status='approved'`  (:1539-1551)
    · step 8 `re_eval_run_id = await _launch_reeval(...)`  (:1555)  <- wrap this
    · step 9 `_mark_reevaling()` sets `re_eval_run_id` + `status='re_evaling'`  (:1571-1580)
- `async def force_promote_skill_proposal(skill_id, proposal_id, body: ForcePromoteBody, ...)`  (:1746)
    · CR-01 target: `body: ForcePromoteBody`  (:1749) has NO default -> FastAPI requires a JSON body
    · owner-verify + `status=='not_promoted'` 409 guard + single `skills.instructions` UPDATE + `override_forced=true` (do NOT relax these)
- read paths that trigger reconcile ONLY for `re_evaling`: `list_skill_proposals` (:876), `get_skill_proposal` (:939) — extend both to also fire on `"approved"`

backend/app/services/eval_runner_service.py:
- WITH-arm catalog injects the DRAFT version's name: `with_override = ({"name": skill_version.get("name") or "", ...},)`  (:627-632). The agent calls `load_skill` with THIS name.

backend/app/services/tool_dispatcher.py:
- `_handle_load_skill` looks the override up by the agent-supplied catalog name: `if override is not None and skill_name in override:`  (:713) — so the override map MUST contain that name (WR-02).

backend/app/models/eval_run.py:
- `ForcePromoteBody` is an EMPTY Pydantic model (carries nothing the server reads).

Test analogs:
- backend/tests/test_skill_proposals_router.py — `_seed(...)`, `_patch_service(...)`, `_override/_clear_overrides`, `_FilterSupabase` (honors `.eq()/.in_()/insert/update/delete`), `OWNER/OTHER_USER`, `_H` headers.
- backend/tests/test_skill_proposals.py — `_make(*, proposal_status, draft, re_eval_status, source_verdicts, reeval_verdicts, override_forced)`, `_result(...)`, drives route functions DIRECTLY over `_FilterSupabase`, patches `evals._launch_reeval` with `AsyncMock`.
- backend/tests/test_eval_runner.py — `_FakeRedis` (async `.get()/.set()`), the model for the redis fake the CR-02 test needs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force-promote optional body (CR-01) + override-map draft-name key (WR-02)</name>
  <files>backend/app/api/evals.py</files>
  <read_first>
    - backend/app/api/evals.py — `force_promote_skill_proposal` (~1746-1830) and `_launch_reeval` (~1240-1389), to see current param + override-map lines in context.
    - backend/app/services/eval_runner_service.py — WITH-arm catalog build (~610-655): confirm `skill_version.get("name")` is the injected key.
    - backend/app/services/tool_dispatcher.py — `_handle_load_skill` override lookup (~700-716): confirm the lookup is by the agent-supplied `skill_name` (the catalog name).
    - backend/app/models/eval_run.py — `ForcePromoteBody` definition (empty model).
    - .planning/phases/135-self-improvement-loop-si-01/135-REVIEW.md — CR-01 and WR-02 suggested fixes.
  </read_first>
  <action>
    Two surgical edits in `backend/app/api/evals.py`, both provider-agnostic.

    (a) CR-01 — On the `force_promote_skill_proposal` signature at line 1749, change `body: ForcePromoteBody`
    to `body: ForcePromoteBody | None = None`. `ForcePromoteBody` is an empty model the server never reads,
    so making the JSON body optional is safe; a body-less `POST .../force-promote` currently 422s with
    `{"detail":[{"type":"missing","loc":["body"],"msg":"Field required"}]}`. Do NOT touch the owner-verify,
    the `status=='not_promoted'` 409 guard, the single `skills.instructions` UPDATE, the `override_forced=True`
    write, or the `_compute_gate(...)` attach — only the parameter default changes.

    (b) WR-02 — In `_launch_reeval`, replace the single-name override map at line 1369
    (`skill_instructions_override={(skill.get("name") or ""): proposed_instructions}` inside the
    `run_eval_job(...)` spawn) so the map is keyed on BOTH `skill.get("name")` AND `draft_version.get("name")`.
    Build a set of the non-empty names from `skill.get("name")` and `draft_version.get("name")`, then map each
    name to `proposed_instructions`. Rationale: the WITH-arm catalog (eval_runner_service.py:627-632) injects
    `skill_version["name"]` = the DRAFT version's name, and `_handle_load_skill` (tool_dispatcher.py:713) looks
    the override up by that catalog name — so keying only on the live `skill.get("name")` silently misses when
    the skill was renamed between propose and approve (the exact "silent no-op gate" Pitfall #1 targets).
    `draft_version` is already a parameter of `_launch_reeval` (used at the `eval_runs` INSERT and the spawn) —
    no new DB read is required. Keying on both names covers the no-rename case (identical names collapse) and
    the rename case. This one edit in the shared `_launch_reeval` helper fixes BOTH the approve and rerun paths.
  </action>
  <verify>
    <automated>cd backend && grep -q 'ForcePromoteBody | None = None' app/api/evals.py && grep -q 'draft_version.get("name")' app/api/evals.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ForcePromoteBody | None = None' backend/app/api/evals.py` returns 1.
    - The `_launch_reeval` `skill_instructions_override=` argument references `draft_version.get("name")` (grep hit inside evals.py).
    - No change to the `status=='not_promoted'` 409 guard or the owner-verify in `force_promote_skill_proposal` (the guard string `Only a not_promoted proposal can be force-promoted` still present).
    - `cd backend && python -m pytest tests/test_skill_proposals.py::test_approve_creates_self_improve_version tests/test_skill_proposals.py::test_force_promote_records_override -q` passes (existing lifecycle unbroken).
  </acceptance_criteria>
  <done>A body-less force-promote is accepted by the route signature, and the re-eval override map contains the draft version name the catalog injects.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile cross-worker liveness (CR-02) + approve failure recovery (CR-03 backend)</name>
  <files>backend/app/api/evals.py</files>
  <read_first>
    - backend/app/api/evals.py — `reconcile_proposal` (1121-1238) incl. the early-return (:1155) and the `running` branch (:1208-1219); `approve_skill_proposal` (1394-1590) incl. `_mark_approved` (:1539) -> `_launch_reeval` (:1555) -> `_mark_reevaling` (:1571); `list_skill_proposals` (:844-902, reconcile trigger :876); `get_skill_proposal` (:907-954, reconcile trigger :939); `_inflight_key` (:78).
    - .planning/phases/135-self-improvement-loop-si-01/135-REVIEW.md — CR-02 and CR-03 suggested fixes (the Redis-claim liveness snippet and the except/revert snippet).
    - .planning/phases/135-self-improvement-loop-si-01/135-VERIFICATION.md — gaps 1 (truth 2) and 2 (truth 3) `missing` items.
  </read_first>
  <action>
    Three logical edits in `backend/app/api/evals.py`, all provider-agnostic. Add a module constant
    `_APPROVED_STALE_GRACE_S = 120` (seconds; discretionary value, ~2 min is well beyond a normal approve
    request) near the other module constants for use in (c).

    (a) CR-02 — In `reconcile_proposal`, the `elif run_status == "running":` branch (1208-1219) currently sets
    `new_status="interrupted"` purely when `run_key not in RUN_TASKS` (a per-process dict defeated by the
    documented `WORKER_COUNT=2` default). Add a cross-worker liveness check BEFORE declaring orphaned: read the
    Redis in-flight claim with `redis.get(_inflight_key(proposal["skill_id"]))` (the SAME key `_launch_reeval`
    SET-NX'd with value `str(re_eval_run_id)`, CAS-released in `run_eval_job`'s `finally`; `redis` is already a
    param, `_inflight_key` is at :78, `proposal` is the full `select("*")` row so `proposal["skill_id"]` exists).
    Decode bytes if needed via `claim.decode() if isinstance(claim, bytes) else claim`. Only set
    `new_status="interrupted"` when `run_key not in RUN_TASKS` AND the decoded claim value `!= str(re_eval_run_id)`.
    A claim still naming this run_id means a healthy in-flight re-eval on another worker -> leave `re_evaling`
    (no transition). This stops the false `interrupted` that silently skips the promotion gate.

    (b) CR-03 part 1 — In `approve_skill_proposal`, wrap the step-8 `re_eval_run_id = await _launch_reeval(...)`
    call (line 1555) in try/except. On any exception, revert the proposal with a CAS-guarded owner-scoped UPDATE:
    set `status='proposed'` filtered by `.eq("id", str(proposal_id)).eq("user_id", user_id).eq("status", "approved")`
    (define an inner `def _revert(): ...` and `await run_in_threadpool(_revert)`), then re-raise the original
    exception. This returns the proposal to an actionable `proposed` state when `_launch_reeval` fails after
    step 7 committed `approved` — the reachable case being the shared `eval_inflight:{skill_id}` 409 raised when
    approving while a normal eval is already running for the skill. The `.eq("status","approved")` predicate is a
    compare-and-swap so a concurrent transition is not clobbered. The already-inserted `self_improve` draft
    version row is harmless and may remain (optionally also set `new_skill_version_id=None` in the revert —
    discretionary). Leave the step-9 `_mark_reevaling` success path unchanged.

    (c) CR-03 part 2 — Make the reconcile early-return (line 1155, currently
    `if proposal.get("status") != "re_evaling": return proposal`) ALSO self-heal a stale `approved` row: when
    `status=='approved'` AND `re_eval_run_id` is falsy AND the row's `updated_at` is older than
    `_APPROVED_STALE_GRACE_S` (parse the stored ISO `updated_at` — it may end in `Z` — against current UTC using
    `datetime`/`timezone`; if `updated_at` is missing/unparseable, treat as stale), transition to `interrupted`
    via the existing `_persist` path (D-14 — matches reconcile's own docstring about "approved but no re-eval
    linked -> honestly interrupted"). A fresh `approved` row still within the grace window (mid-request) is left
    untouched. THEN make this reachable from reads: extend the reconcile trigger condition in BOTH
    `list_skill_proposals` (line 876, `if r.get("status") == "re_evaling":`) and `get_skill_proposal`
    (line 939, `if row.get("status") == "re_evaling":`) to also fire for `"approved"` rows (e.g. membership in a
    `("re_evaling", "approved")` tuple), so a wedged `approved` proposal is reconciled on the next GET.

    Do NOT touch the promotion math, the `completed`/`failed`/`cancelled`/`interrupted` reconcile branches, the
    `_compute_gate` attach, or any provider routing.
  </action>
  <verify>
    <automated>cd backend && grep -q 'isinstance(claim, bytes)' app/api/evals.py && grep -q '_APPROVED_STALE_GRACE_S' app/api/evals.py && grep -q '_inflight_key(proposal' app/api/evals.py</automated>
  </verify>
  <acceptance_criteria>
    - The `reconcile_proposal` `running` branch references `_inflight_key(proposal[` and `isinstance(claim, bytes)` (grep hits in evals.py).
    - `_APPROVED_STALE_GRACE_S` is defined as a module constant and referenced in the reconcile early-return path.
    - `approve_skill_proposal` contains a try/except around `_launch_reeval` whose except sets `status` to `proposed` with a `.eq("status", "approved")` CAS predicate, then re-raises (source read confirms).
    - Both `list_skill_proposals` (~:876) and `get_skill_proposal` (~:939) invoke `reconcile_proposal` for `approved` rows as well as `re_evaling` rows.
    - `cd backend && python -m pytest tests/test_skill_proposals.py::test_interrupted_state tests/test_skill_proposals.py::test_promotion_paths -q` passes (existing reconcile/promotion behavior unbroken).
  </acceptance_criteria>
  <done>A healthy in-flight re-eval survives a reconcile on the other worker; a launch failure returns the proposal to `proposed`; a stale `approved` row self-heals to `interrupted` on read.</done>
</task>

<task type="auto">
  <name>Task 3: Backend regression tests — HTTP force-promote, reconcile running-branch, approve revert, stale-approved self-heal</name>
  <files>backend/tests/test_skill_proposals_router.py, backend/tests/test_skill_proposals.py</files>
  <read_first>
    - backend/tests/test_skill_proposals_router.py — full file: `_seed`, `_patch_service`, `_override/_clear_overrides`, the `_FilterSupabase` import, `OWNER/OTHER_USER`, `_H`, and the existing route-driving pattern.
    - backend/tests/test_skill_proposals.py — full file: `_make(...)` (note `re_eval_status`, `draft`, `source_verdicts`, `reeval_verdicts`), `_result(...)`, the direct route-function call style, and `test_interrupted_state` (the closest reconcile analog) + `test_force_promote_records_override` (the direct-call force-promote analog this HTTP test complements).
    - backend/tests/test_eval_runner.py — `_FakeRedis` (async `.get()`), the model for the redis fake the CR-02 test needs.
    - backend/app/api/evals.py — the functions edited in Tasks 1-2 (to mirror exact statuses/fields).
  </read_first>
  <action>
    Add four tests. No live DB, no live LLM, no live provider — reuse the in-memory `_FilterSupabase` fake and
    the existing seed/make helpers.

    (a) `test_skill_proposals_router.py` -> add `test_force_promote_over_http` (CR-01 wire contract). Seed a store
    with a `not_promoted` proposal that has a `self_improve` draft version, a completed re-eval `eval_runs` row,
    and source-run graded `eval_results` (extend `_seed` or build the store inline mirroring the service test's
    `_make(proposal_status="not_promoted", draft=True, re_eval_status="completed", source_verdicts=..., reeval_verdicts=...)`
    shape so `_compute_gate` has rows). `_override(app, user=OWNER, supabase=sb)`, then
    `await c.post(f"/skills/{skill_id}/proposals/{pid}/force-promote", headers=_H)` with NO `json=` argument
    (the exact body-less shape the shipped frontend sent). Assert `resp.status_code == 200` (explicitly NOT 422),
    assert `resp.json()["override_forced"] is True` and `["status"] == "promoted"`, and assert the live
    `skills` row's `instructions` was updated to the proposed body in `sb.store`. This exercises the wire contract
    the direct-call `test_force_promote_records_override` cannot.

    (b) `test_skill_proposals.py` -> add `test_reconcile_running_cross_worker` (CR-02 / IN-05). Extend `_make` to
    accept `re_eval_status="running"` (it already threads `re_eval_status` into the `eval_runs` insert). Provide a
    minimal redis fake exposing an async `get(key)` (model on `test_eval_runner._FakeRedis`). Two cases, each
    driving `evals.get_skill_proposal(...)` (or `evals.reconcile_proposal(...)` directly) with the fake redis and
    an EMPTY `app.api.threads.RUN_TASKS`:
      (i) the fake returns the claim value `str(re_eval_run_id)` for key `eval_inflight:{skill_id}` ->
          assert the proposal status stays `"re_evaling"` and the gate is NOT prematurely applied (healthy
          in-flight, multi-worker case — the run is absent from RUN_TASKS yet the claim proves liveness);
      (ii) the fake returns `None` (no claim) with RUN_TASKS still empty ->
          assert the proposal status becomes `"interrupted"`.
    Do NOT register anything in RUN_TASKS (the default empty dict is the multi-worker "other-worker" condition).

    (c) `test_skill_proposals.py` -> add `test_approve_reverts_on_launch_failure` (CR-03 backend). Build a
    `proposed` proposal via `_make(proposal_status="proposed")`, `monkeypatch.setattr(evals, "_launch_reeval",
    AsyncMock(side_effect=HTTPException(status_code=409, detail="An eval run is already in progress for this skill")))`,
    then call `evals.approve_skill_proposal(...)` inside `pytest.raises(HTTPException)`. After the raise, assert the
    proposal row in `sb.store` was reverted to `status == "proposed"` (NOT left `"approved"`), so the user retains
    an actionable state; the inserted `self_improve` draft version row may remain (assert it is harmless — either
    present or `new_skill_version_id` cleared, matching the chosen revert).

    (d) `test_skill_proposals.py` -> add `test_reconcile_self_heals_stale_approved` (CR-03 self-heal half — Truth 2b).
    Two cases, both built via `_make(proposal_status="approved")` with NO `re_eval_run_id` on the proposal row:
      (i) STALE: set the proposal's `updated_at` to an ISO timestamp older than `evals._APPROVED_STALE_GRACE_S`
          (e.g. `(datetime.now(timezone.utc) - timedelta(seconds=evals._APPROVED_STALE_GRACE_S + 60)).isoformat().replace("+00:00", "Z")`
          — use the `Z`-suffix form to exercise the ISO-`Z` parsing path Task 2c specifies), then call
          `evals.get_skill_proposal(...)` -> assert the returned/persisted proposal status is `"interrupted"`
          (the self-heal fired on read);
      (ii) FRESH: set `updated_at` to now (within the grace window), call `evals.get_skill_proposal(...)` ->
          assert the proposal status is STILL `"approved"` (the grace-window guard prevents clobbering a
          just-approved proposal whose launch is in flight).
    Keep `app.api.threads.RUN_TASKS` empty in both cases; no redis claim is needed (an `approved` row has no
    run yet — this path must not depend on the claim).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_skill_proposals_router.py tests/test_skill_proposals.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_force_promote_over_http` exists, POSTs force-promote with no JSON body, and asserts `status_code == 200` + `override_forced is True` + the live skill instructions updated.
    - `test_reconcile_running_cross_worker` exists and asserts case (i) stays `re_evaling` (claim present) and case (ii) becomes `interrupted` (claim absent), with RUN_TASKS empty in both.
    - `test_approve_reverts_on_launch_failure` exists and asserts the proposal reverts to `proposed` after a `_launch_reeval` HTTPException.
    - `test_reconcile_self_heals_stale_approved` exists and asserts case (i) a stale `approved` row (Z-suffix `updated_at` older than `_APPROVED_STALE_GRACE_S`, no `re_eval_run_id`) transitions to `interrupted` on `get_skill_proposal`, and case (ii) a fresh `approved` row (within the grace window) stays `approved`.
    - `cd backend && python -m pytest tests/test_skill_proposals_router.py tests/test_skill_proposals.py -q` reports all tests passed (including the 4 new ones and the pre-existing 14).
  </acceptance_criteria>
  <done>The wire contract, the cross-worker liveness path, the approve-failure revert, and the stale-approved self-heal (both sides of the grace window) are each covered by a green automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> force-promote / approve / rerun routes | `skill_id` + `proposal_id` path params are attacker-controlled untrusted input |
| backend -> Redis (`eval_inflight:{skill_id}`) | internal channel; reconcile now reads the claim as a cross-worker liveness signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-08 | Elevation of Privilege / Tampering (IDOR) | `force_promote_skill_proposal` (CR-01 body-optional) + `approve_skill_proposal` except/revert | mitigate | The body-optional change and the revert do NOT relax the owner gate: `_verify_owned_skill` + the proposal read on `id` AND `user_id` AND `skill_id` (404-not-403) stay intact; the revert UPDATE keeps `.eq("user_id", user_id)` and adds `.eq("status","approved")`. No route path can now mutate another user's proposal. |
| T-135-09 | Spoofing | reconcile `running`-branch Redis-claim read | accept | `eval_inflight:{skill_id}` is server-internal (Redis is not client-reachable); the claim VALUE is compared to the specific `str(re_eval_run_id)` (not mere key presence), so a stale/foreign value cannot mask an orphaned run as alive. TTL (1800s) bounds staleness after a hard crash. |
| T-135-10 | Tampering (status-transition race) | approve revert + reconcile self-heal | mitigate | The revert-to-`proposed` UPDATE carries a compare-and-swap `.eq("status","approved")` predicate; the stale-approved self-heal only fires past `_APPROVED_STALE_GRACE_S`, so a concurrent legitimate transition is never clobbered (WR-03-adjacent hardening on the touched path). |
| T-135-11 | Denial of Service | reconcile extra `redis.get` per running-branch read | accept | one O(1) GET per read of a `running` re-eval; bounded and negligible versus the existing per-read `_compute_gate` DB work. |
| T-135-SC | Tampering (supply chain) | npm/pip/cargo installs | mitigate | Zero new packages — a pure in-repo code fix; no install task in this plan, so the supply-chain surface is unchanged (no `[ASSUMED]`/`[SUS]` packages to gate). |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_skill_proposals_router.py tests/test_skill_proposals.py tests/test_promotion_gate.py -q` — all green (baseline phase tests + 3 new).
- Source assertions: `ForcePromoteBody | None = None` present; `_launch_reeval` override map references `draft_version.get("name")`; reconcile running branch references `_inflight_key(proposal[` + `isinstance(claim, bytes)`; `_APPROVED_STALE_GRACE_S` defined; approve has a try/except revert with a `.eq("status","approved")` CAS.
- Cross-provider: no provider-specific branches introduced (the re-eval still routes via the shared gateway override).
</verification>

<success_criteria>
- A body-less HTTP `POST .../force-promote` returns 200 and records `override_forced=true` (Truth 4 re-verifiable).
- A `running` re-eval whose Redis claim still names its run_id is NOT marked `interrupted` on a cross-worker reconcile; a claim-absent orphan IS (Truth 3 re-verifiable).
- An approve whose `_launch_reeval` raises leaves the proposal at `proposed` (not wedged `approved`); a stale `approved` self-heals to `interrupted` on read (Truth 2 re-verifiable).
- A rename between propose and approve still measures the DRAFT (WR-02 closed) — proven by the draft-name key in the override map.
</success_criteria>

<output>
Create `.planning/phases/135-self-improvement-loop-si-01/135-08-SUMMARY.md` when done.
</output>
