---
phase: 135-self-improvement-loop-si-01
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/skills/SkillEvalSection.tsx
autonomous: true
requirements: [SI-01]
gap_closure: true

must_haves:
  truths:
    - "Clicking 'Force promote anyway' sends an explicit empty JSON body so the force-promote succeeds instead of 422ing on every click (CR-01 frontend half — verification Truth 4)."
    - "A FastAPI 422 validation error renders a readable message rather than '[object Object]' (WR-04)."
    - "An approved proposal renders a user escape action (Reject), not just a spinner, preserving the human-always-in-the-loop guarantee (CR-03 frontend half — verification Truth 2)."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "force-promote empty-body POST + string-guarded proposalError"
      contains: "JSON.stringify({})"
    - path: "frontend/src/components/skills/SkillEvalSection.tsx"
      provides: "escape (Reject) action rendered for the approved proposal status"
  key_links:
    - from: "forcePromoteProposal fetch"
      to: "POST .../force-promote route"
      via: "body: JSON.stringify({}) on the request options"
      pattern: "JSON.stringify\\(\\{\\}\\)"
    - from: "proposal.status === 'approved' branch"
      to: "handleReject"
      via: "an escape Reject button in the approved card render"
      pattern: "handleReject"
---

<objective>
Close the frontend halves of the two BLOCKER gaps and the adjacent WR-04 warning on the self-improvement
proposal card — in `frontend/src/lib/api.ts` and `frontend/src/components/skills/SkillEvalSection.tsx` —
so re-verification can flip Truths 2 and 4 of `135-VERIFICATION.md` from FAILED to VERIFIED.

Gap coverage (source = `135-VERIFICATION.md` gaps + `135-REVIEW.md` suggested fixes):
- **CR-01 (frontend half)** — `forcePromoteProposal` sends no request body -> always 422s against the current backend route. Send `JSON.stringify({})`. (Truth 4)
- **CR-03 (frontend half)** — the `approved` proposal status renders ONLY a spinner, leaving no escape when a wedged approve blocks the loop. Render a Reject escape. (Truth 2)
- **WR-04** — `proposalError` assumes `detail` is a string; FastAPI 422 returns `detail` as an array, so the message renders `"[object Object]"` — directly reachable today via the CR-01 422. Guard to string-only. (rides the CR-01 fix in the same file/function neighborhood)

Every item maps to a task below; no VERIFICATION gap is left unplanned. The backend halves of CR-01/CR-03,
CR-02, and WR-02 ship in the parallel plan `135-08` (no file overlap -> same wave). The CR-01 fix is
belt-and-suspenders (REVIEW.md recommends BOTH sides): sending `{}` works whether or not `135-08` lands.

Purpose: make the human-override escape hatch and the human-in-the-loop reject actually reachable from the UI.
Output: patched `api.ts` (force-promote body + error guard) + patched `SkillEvalSection.tsx` (approved escape).

Cross-provider red line (CLAUDE.md / D-16): the surface is provider-agnostic; these are pure request-shape,
error-render, and UI-affordance changes with NO provider forks and NO new dependency.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/135-self-improvement-loop-si-01/135-VERIFICATION.md
@.planning/phases/135-self-improvement-loop-si-01/135-REVIEW.md
@.planning/phases/135-self-improvement-loop-si-01/135-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase this planning pass — use directly. -->

frontend/src/lib/api.ts:
- `async function proposalError(res, fallback): Promise<Error>`  (:1739-1748)
    · WR-04 target: `const j = (await res.json()) as { detail?: string }` then `if (j?.detail) detail = j.detail`  (:1742-1743) — coerces a 422 detail ARRAY to "[object Object]".
- `export async function forcePromoteProposal(skillId, proposalId): Promise<SkillProposal>`  (:1836-1847)
    · CR-01 target: fetch options are `{ method: "POST", headers }`  (:1843) — NO body. `getAuthHeaders()` already sets `Content-Type: application/json`.
- sibling helpers already sending a JSON body via `body: JSON.stringify(...)`: `proposeImprovement` (:1757-1761), `startEvalRun` (the copy-from shape).

frontend/src/components/skills/SkillEvalSection.tsx:
- handlers (all follow "call owner-gated endpoint -> refetch DB, never optimistic"):
    · `handleApprove` (:418), `handleReject` (:435) -> `rejectProposal`, `handleRerun` (:449), `handleForcePromote` (:466)
- proposal-card render (status-driven action row) (:661-741):
    · `proposal.status === "proposed"` -> Approve + Reject buttons  (:663-683) — the Reject Button idiom to reuse (variant="outline", `<X/>` icon, `onClick={() => void handleReject()}`)
    · CR-03 target: `(proposal.status === "approved" || proposal.status === "re_evaling")` -> spinner-only, NO action  (:685-690)
    · `proposal.status === "not_promoted"` -> gate counts + "Force promote anyway" -> `handleForcePromote`  (:702-721)
    · `proposal.status === "interrupted"` -> "Re-run re-eval" -> `handleRerun`  (:723-740)

backend/app/api/evals.py (read-only confirm):
- `reject_skill_proposal` (:959-1009) has NO status guard -> reject works from ANY status, so it reliably frees a wedged `approved` proposal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force-promote empty body (CR-01) + string-guarded error detail (WR-04)</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts — `proposalError` (~1739-1748) and `forcePromoteProposal` (~1836-1847) in context; also glance at `proposeImprovement` (~1757-1761) for the existing `body: JSON.stringify(...)` idiom.
    - .planning/phases/135-self-improvement-loop-si-01/135-REVIEW.md — CR-01 and WR-04 suggested fixes.
  </read_first>
  <action>
    Two edits in `frontend/src/lib/api.ts`, both provider-agnostic, no new dependency.

    (a) CR-01 — In `forcePromoteProposal`, add `body: JSON.stringify({})` to the `fetch` options at line 1843
    (currently `{ method: "POST", headers }`). The backend `force-promote` route required a JSON body (a
    body-less POST 422s); sending an explicit empty object is the belt-and-suspenders fix REVIEW.md CR-01
    recommends on BOTH sides and works whether or not the parallel `135-08` backend change lands. Keep
    `getAuthHeaders()` (which already sets `Content-Type: application/json`) unchanged; do not alter the URL,
    method, or the `if (!res.ok) throw await proposalError(...)` handling.

    (b) WR-04 — In `proposalError`, the current parse `const j = (await res.json()) as { detail?: string }` +
    `if (j?.detail) detail = j.detail` assumes `detail` is a string, but FastAPI 422 responses return `detail`
    as an ARRAY of objects, so `new Error(j.detail)` renders `"[object Object]"`. Widen the parsed type so
    `detail` is `unknown` (e.g. `as { detail?: unknown }`) and only assign `detail = j.detail` when
    `typeof j?.detail === "string"`; otherwise keep the existing generic fallback message (`${fallback} (status ${res.status}).`).
    This is directly reachable today on every force-promote 422 and lives one function above the CR-01 fix.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'JSON.stringify({})' src/lib/api.ts && grep -q 'typeof j' src/lib/api.ts && ! npx tsc --noEmit 2>&1 | grep -E 'src/lib/api'</automated>
  </verify>
  <acceptance_criteria>
    - `forcePromoteProposal`'s `fetch` options include `body: JSON.stringify({})` (grep hit in api.ts).
    - `proposalError` only assigns `detail` from `j.detail` when `typeof j?.detail === "string"` (grep hit for `typeof j`); the generic fallback string is retained for non-string / array detail.
    - `npx tsc --noEmit` produces NO error lines mentioning `src/lib/api` (the inverted-grep verify above succeeds).
  </acceptance_criteria>
  <done>The force-promote request carries an empty JSON body and validation errors render a readable message, not "[object Object]".</done>
</task>

<task type="auto">
  <name>Task 2: Escape action for the wedged approved proposal (CR-03 frontend)</name>
  <files>frontend/src/components/skills/SkillEvalSection.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillEvalSection.tsx — the proposal-card status action row (:661-741), especially the `proposed` Reject Button idiom (:673-681), the combined `approved`/`re_evaling` spinner branch (:685-690), and the `handleReject` handler (:435-447).
    - .planning/phases/135-self-improvement-loop-si-01/135-VERIFICATION.md — gap 1 `missing` item 3 ("Frontend: an escape action ... for the 'approved' status, not just a spinner").
    - backend/app/api/evals.py — `reject_skill_proposal` (:959-1009): confirm it has NO status guard (so reject works from `approved`).
  </read_first>
  <action>
    One edit in `frontend/src/components/skills/SkillEvalSection.tsx`, provider-agnostic, no new dependency.

    Split the combined branch at lines 685-690 that renders ONLY a spinner for both `approved` and `re_evaling`.
    Keep the existing spinner-only render for `proposal.status === "re_evaling"` (a genuinely in-flight re-eval —
    "Re-evaluating the proposed skill — live progress shows above."). For `proposal.status === "approved"`, render
    the same spinner text PLUS an escape action: a Reject button wired to the existing `handleReject` (:435),
    reusing the exact Button idiom from the `proposed`-branch Reject at lines 673-681 (`size="sm"`,
    `variant="outline"`, `className="text-xs gap-1"`, the `<X className="h-3 w-3" />` icon, `onClick={() => void handleReject()}`).
    Rationale: `approved` is transient in the happy path (it becomes `re_evaling` in the same approve response),
    so a persistent `approved` on refetch means the re-eval launch failed and the loop is wedged with no user
    action — this restores the human-in-the-loop escape. `rejectProposal` accepts ANY status server-side
    (`reject_skill_proposal` has no state guard), so it reliably frees a wedged `approved` proposal. Do NOT change
    the `proposed`, `promoted`, `not_promoted`, or `interrupted` branches, and do NOT add optimistic state
    (`handleReject` already refetches the DB row).
  </action>
  <verify>
    <automated>cd frontend && grep -q 'proposal.status === "approved"' src/components/skills/SkillEvalSection.tsx && grep -q 'handleReject' src/components/skills/SkillEvalSection.tsx && ! npx tsc --noEmit 2>&1 | grep -E 'SkillEvalSection'</automated>
  </verify>
  <acceptance_criteria>
    - A dedicated `proposal.status === "approved"` render branch exists and includes a Reject Button wired to `handleReject`.
    - The `proposal.status === "re_evaling"` branch still renders the spinner-only "Re-evaluating…" text with no reject.
    - `npx tsc --noEmit` produces NO error lines mentioning `SkillEvalSection` (the inverted-grep verify above succeeds).
  </acceptance_criteria>
  <done>An approved proposal offers a Reject escape (not just a spinner); a genuinely re-evaling proposal keeps the spinner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> force-promote / reject routes | the request the button sends is client-controlled; the real gate is server-side owner-scoping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-12 | Tampering / Elevation (IDOR) | `forcePromoteProposal` empty-body POST + the new `approved` Reject escape | mitigate | The frontend adds NO client-trusted authority — both force-promote and the escape Reject hit owner-gated server routes (owner-verify on `id` AND `user_id` AND `skill_id`, 404-not-403); the button only changes which request is sent, never the authorization. |
| T-135-13 | Information disclosure | `proposalError` render (WR-04) | mitigate | `proposalError` now renders `detail` ONLY when it is a string and falls back to a generic `${fallback} (status ${res.status})` otherwise, so a FastAPI 422 `detail` array (which can echo request-shape internals) is not dumped verbatim into the user-visible error. |
| T-135-SC | Tampering (supply chain) | npm installs | mitigate | Zero new dependencies — no `jsdiff`/no `package.json` change; pure source edits, so the supply-chain surface is unchanged (no `[ASSUMED]`/`[SUS]` packages to gate). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` produces no error lines mentioning `src/lib/api` or `SkillEvalSection` (touched-file cleanliness — the shipped verification's own check).
- Source assertions: `forcePromoteProposal` includes `body: JSON.stringify({})`; `proposalError` guards on `typeof j?.detail === "string"`; a `proposal.status === "approved"` branch renders a `handleReject` Reject button.
- Cross-provider: no provider-specific branches introduced (pure request-shape / render / affordance changes).
- Live confirmation of the button click (U10 force-promote path) is covered by the SC#10 4-axis UAT in `135-VALIDATION.md`, run via `/gsd:verify-work` after both gap plans ship — NOT a task here.
</verification>

<success_criteria>
- The "Force promote anyway" button sends `{}` and succeeds instead of 422ing on every click (Truth 4 re-verifiable, frontend half).
- A validation error surfaces a readable message, never "[object Object]" (WR-04 closed).
- An `approved` proposal offers a Reject escape so the human-in-the-loop guarantee holds even when a re-eval launch fails (Truth 2 re-verifiable, frontend half).
</success_criteria>

<output>
Create `.planning/phases/135-self-improvement-loop-si-01/135-09-SUMMARY.md` when done.
</output>
