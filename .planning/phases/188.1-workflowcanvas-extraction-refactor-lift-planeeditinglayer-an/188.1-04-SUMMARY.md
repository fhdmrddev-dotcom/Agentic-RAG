---
phase: 188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an
plan: 04
subsystem: frontend
tags: [security, wr-04, wr-07, prototype-pollution, own-property-guard, falsification, count-gate, docblock-correction]

# Dependency graph
requires:
  - phase: 188.1
    plan: 03
    provides: "the settled tree the six falsifications were measured RED against — gate 2470/2470, tsc 33, eslint 5"
provides:
  - "Five WR-04 prototype-key lookups made total in the tree's only shipped spelling"
  - "TWO MORE lookups on the same author-supplied value — `phaseGlyph`'s and `renderPhaseMark`'s — closed because they BLOCKED two of the five (a hard render crash reached first)"
  - "`getWorkflowRun` encodes its run-id path segment (WR-07); the `signal` half stays deferred (D4)"
  - "Six falsifications, every one observed RED against the shipped code before any fix"
  - "Four false-totality docblocks corrected in the house ⚠ register"
  - "Four count-gate pins at twice-measured actuals; whole-gate total 2470 → 2476"
  - "D-188.1-DEF-01 (the WorkflowRunPage gate flake) CLOSED; D-188.1-DEF-02 (providerLogo) logged, not fixed"
affects: [188.1-05, 189]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A falsification that drives the real component through a prototype key finds the FIRST consequence, not the intended one — site 1's was a render crash two modules away, which is how the property proved wider than the patch it was written for"
    - "Before writing a falsification into a suite that drives a fixture through the real normalizer, check whether an ALREADY-SHIPPED guard sanitizes the input first — otherwise the test is green against the unguarded tree and has falsified nothing"
    - "When an acceptance grep asks the tree whether a false claim still exists, the ⚠ correction must PARAPHRASE the claim, never quote it"

key-files:
  created: []
  modified:
    - frontend/src/components/workflows/PhaseNode.tsx
    - frontend/src/components/workflows/PhaseNodeCard.tsx
    - frontend/src/components/workflows/nodePresentation.ts
    - frontend/src/components/panel/PhaseCard.tsx
    - frontend/src/lib/phaseGlyph.tsx
    - frontend/src/lib/api.ts
    - frontend/src/components/workflows/PhaseNode.test.tsx
    - frontend/src/components/workflows/PhaseNodeCard.test.tsx
    - frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx
    - frontend/src/pages/WorkflowRunPage.test.tsx
    - scripts/vitest-count-gate.cjs
    - .planning/phases/188.1-.../deferred-items.md

key-decisions:
  - "`phaseGlyph.tsx` and `nodePresentation.renderPhaseMark` were fixed as BLOCKERS (Rule 3), not as a sweep — `ICON_TINT`'s consequence is unobservable while `phaseGlyph` crashes the render first. `providerLogo.tsx`, the identical defect on no path this plan drives, was logged and left alone."
  - "Sites 2/3's falsifications drive `panel/PhaseCard` DIRECTLY rather than a `PhaseTimeline` fixture: the fixture path is normalised by the already-guarded `phaseStatusFromDb`, so a fixture-driven site-3 test would have been GREEN against the unguarded tree."
  - "Sites 4/5 share ONE module-private `own()` helper (the plan's own Form B), which makes the plan's `hasOwnProperty.call ≥ 5 across three files` criterion arithmetically unreachable — measured 4 across the three named files, 6 across all five guarded files."
  - "The site-4 ⚠ correction PARAPHRASES the false claim instead of quoting it, because the acceptance grep asks whether the literal still exists in the file."
  - "D-188.1-DEF-01 was closed rather than deferred again: its own re-open trigger named 'the next phase touching this suite', and this plan extends it."

patterns-established:
  - "Pattern 1: assert the prototype-key render is byte-equal to an ORDINARY unrecognised value's render, rather than against a hand-typed literal copied out of a module-private table — the fallback can then be renamed without the guard going stale"
  - "Pattern 2: a function-valued DOM attribute is OMITTED by React, so the unguarded consequence of a poisoned lookup at an attribute sink is ABSENCE, not corruption — assert the attribute's TYPE, not only its value"

requirements-completed: []  # ROADMAP §188.1: "none — this is a refactor phase." Traceability is to SC#6 only.

# Metrics
duration: 70min
completed: 2026-08-06
---

# Phase 188.1 Plan 04: The bounded hardening fixes — Summary

Closed the five WR-04 prototype-key lookups and WR-07's path-segment encode, each with a
falsification observed RED against the shipped code first — and the RED observation found **two
more** lookups on the same author-supplied value whose consequence is strictly worse than any of
the five: `"constructor"` reached `createElement(Object, …)` and **crashed the whole node** before
a wrong colour could ever be painted.

## What Was Built

### Task 1 — six falsifications, every one observed RED (commit `358a4c34`, 333 insertions / **0 deletions**)

Written as pure extensions to four already-pinned suites. No file created, no test renamed,
deleted or re-described.

| # | Site | Suite | What it asserts (the CONSEQUENCE, never the expression) |
|---|---|---|---|
| 1 | `PhaseNode.tsx` `ICON_TINT` | `PhaseNode.test.tsx` | the painted face for `"constructor"` is byte-equal to an ordinary unknown type's — well style AND card text — and no rendered `style` attribute contains `function` |
| 2 | `panel/PhaseCard.tsx` `PHASE_TYPE_LABEL` | `PhaseTimeline.test.tsx` | the rendered header row equals an ordinary unrecognised type's; controls pin that the ordinary row really says *Step* and `•` |
| 3 | `panel/PhaseCard.tsx` `STATUS_META` | `PhaseTimeline.test.tsx` | the status atom equals the DECLARED `unknown` row's; controls pin *Unknown* and `?` |
| 4 | `PhaseNodeCard.tsx` `VERDICT_MARK` | `PhaseNodeCard.test.tsx` | the mark carries `VERDICT_MARK.unknown`'s glyph **and** its accessible label |
| 5 | `PhaseNodeCard.tsx` `RING_STROKE` | `PhaseNodeCard.test.tsx` | the arc's `stroke` is a `hsl(` token, and its TYPE is `string` |
| WR-07 | `api.getWorkflowRun` | `WorkflowRunPage.test.tsx` | the URL the mocked `fetch` RECEIVED for `runId = "a/b"` contains `a%2Fb` and not `workflow-runs/a/b` |

### Task 2 — the guards (commit `b018b20b`, 160 insertions / 11 deletions)

All in `Object.prototype.hasOwnProperty.call(...)`, the tree's only shipped spelling.
**`Object.hasOwn` still has ZERO occurrences in `frontend/src`** — no second spelling entered.

| Site | Form | Why that form |
|---|---|---|
| 1 `PhaseNode.tsx` | **ternary** | preserves the literal `ICON_TINT[` that `PhaseNodeCard.test.tsx:373` keeps as a positive control; `own(ICON_TINT, …)` would red a shipped guard |
| 2 `panel/PhaseCard.tsx` | Form A guard clause | `phaseTypeMeta`'s whole body IS the lookup |
| 3 `panel/PhaseCard.tsx` | Form A guard clause, wrapped as `statusMeta()` | had **no fallback at all**; now lands on the table's OWN declared `unknown` row |
| 4 `PhaseNodeCard.tsx` | Form B, local `own()` | a `?? fallback` already read naturally there |
| 5 `PhaseNodeCard.tsx` | Form B, same local `own()` | same |
| **6 `lib/phaseGlyph.tsx`** | guard clause | **BLOCKER** — see Deviation 1 |
| **7 `nodePresentation.renderPhaseMark`** | inline ternary | **BLOCKER** — see Deviation 1 |

`runVocabulary.ts`'s `own()` was **not** exported or generalised (it is `CanvasReading`-typed and
its file carries its own `?raw` fences); a local copy was written instead, with a docblock saying
why one concept has two small private copies here.

**Four false-totality docblocks corrected**, all in the house `⚠` register:

1. `PhaseNodeCard.tsx` site 4 — the claim that the lookup was total *"by construction"*.
2. `lib/phaseGlyph.tsx` — *"total over any key"*, twice (the header `@returns` and the inline comment).
3. `nodePresentation.ts` header — *"TOTALITY contract: both resolvers are total … rather than throwing"*, the sharpest of the four: the file promised the exact thing it did.
4. `panel/PhaseCard.tsx` — the header's *"the renderer NEVER crashes on an unrecognized discriminator"* is now qualified inside `phaseTypeMeta`'s new docblock (true of a MISS, false of an inherited key).

### Task 3 — WR-07 and the pins (commit `d17151bd`, 56 insertions / 6 deletions)

`getWorkflowRun` wraps `runId` in `encodeURIComponent`, in the `listRelationships` register (the
defensive encode WITH a two-line rationale naming the finding id). The docblock was **appended
to**, not rewritten. **`signal` untouched (D4)** — the deferral and its re-open trigger are now
recorded in the docblock rather than only in a plan.

### Post-task — a deferred item closed and a new one logged (commit `23046860`)

## Measured Evidence (verbatim)

### THE SIX RED OBSERVATIONS — run against the UNFIXED production tree

```
 × a runId carrying a slash cannot reshape the request path
 × site 4 — a prototype-key verdict renders the DECLARED unknown mark, not an inherited member
 × site 5 — a prototype-key reading paints a COLOUR TOKEN stroke, never a stringified function
 × site 2 — a prototype-key phaseType renders the ORDINARY unknown row, never an inherited member
 × site 3 — a prototype-key status renders the DECLARED unknown atom, never an inherited member
 × a prototype-key phase type renders the ORDINARY unknown face, never an inherited member
⎯⎯⎯ Failed Tests 6 ⎯⎯⎯

FAIL src/pages/WorkflowRunPage.test.tsx > api.getWorkflowRun 188.1-04 — WR-07: the run-id path segment is encoded > a runId carrying a slash cannot reshape the request path
AssertionError: expected 'http://localhost:8000/workflow-runs/a…' to contain 'a%2Fb'
  Expected: "a%2Fb"
  Received: "http://localhost:8000/workflow-runs/a/b"

FAIL src/components/workflows/PhaseNode.test.tsx > PhaseNode 188.1-04 — WR-04 site 1: the phase-type face is total > a prototype-key phase type renders the ORDINARY unknown face, never an inherited member
Error: Objects are not valid as a React child (found: object with keys {className}). If you meant to render a collection of children, use an array instead.
 ❯ throwOnInvalidObjectTypeImpl node_modules/react-dom/cjs/react-dom-client.development.js:6099:13

FAIL src/components/panel/__tests__/PhaseTimeline.test.tsx > panel/PhaseCard 188.1-04 — WR-04 sites 2 and 3: the lookups are total > site 2 — a prototype-key phaseType renders the ORDINARY unknown row, never an inherited member
Error: Objects are not valid as a React child (found: object with keys {className}). If you meant to render a collection of children, use an array instead.

FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard 188.1-04 — WR-04 sites 4 and 5: the lookups are total > site 4 — a prototype-key verdict renders the DECLARED unknown mark, not an inherited member
AssertionError: expected '' to contain '?'

FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard 188.1-04 — WR-04 sites 4 and 5: the lookups are total > site 5 — a prototype-key reading paints a COLOUR TOKEN stroke, never a stringified function
AssertionError: expected 'object' to be 'string' // Object.is equality
  (preceded by, on stderr) Invalid value for prop `stroke` on <circle> tag. Either remove it
  from the element, or pass a string or number value to keep it in the DOM.

FAIL src/components/panel/__tests__/PhaseTimeline.test.tsx > panel/PhaseCard 188.1-04 — WR-04 sites 2 and 3: the lookups are total > site 3 — a prototype-key status renders the DECLARED unknown atom, never an inherited member
AssertionError: expected 'draft-the-summary' to be 'draft-the-summary?Unknown' // Object.is equality
  Expected: "draft-the-summary?Unknown"
  Received: "draft-the-summary"

 Test Files  4 failed (4)
      Tests  6 failed | 225 passed (231)
```

**225 is the EXACT pre-change total** (25 + 105 + 8 + 87, measured on the clean tree before any
edit) — so not one shipped assertion moved to make room for the six.

Read the two crash rows carefully: sites 1 and 2 did **not** fail on a colour or a label. They
failed on `Objects are not valid as a React child`, out of `phaseGlyph`, two modules away. That is
the finding this plan would not have made if the falsifications had asserted the lookup expression
instead of the render.

Read site 5's stderr too: React **omits** a function-valued DOM attribute rather than stringifying
it, so the unguarded consequence at an attribute sink is an arc with **no stroke at all**.

### GREEN after the guards

```
npx vitest run PhaseNode.test.tsx PhaseNodeCard.test.tsx PhaseTimeline.test.tsx
 Test Files  3 passed (3)
      Tests  143 passed (143)        (26 + 107 + 10)

npx vitest run src/pages/WorkflowRunPage.test.tsx
 Test Files  1 passed (1)
      Tests  88 passed (88)
```

Including `PhaseNodeCard.test.tsx`'s *"paints the resolved tint it is handed, never one it looked
up"* block with all three positive controls intact.

### The A7 audit — all five sites, each STATED

| Site | Table | Result |
|---|---|---|
| 1 | `ICON_TINT` | **audited — FENCES FOUND.** `PhaseNodeCard.test.tsx:373` (`expect(phaseNodeSource).toMatch(/ICON_TINT\[/)`, the positive control that constrains the fix's SHAPE), plus `:364`, `:370-371` (negatives on the card's source), `:438`, `:444`, `:448`. The ternary preserves all three controls; `own(ICON_TINT, …)` would have reddened `:373`. |
| 2 | `PHASE_TYPE_LABEL` | **audited — none found.** The only tree-wide test hit is `PhaseSpineGraph.test.tsx:210`, `not.toMatch(/const PHASE_TYPE_LABELS/)` — a DIFFERENT table (plural, `phaseVocabulary.ts`) fenced against a DIFFERENT file. No `?raw` fence reads `panel/PhaseCard.tsx` anywhere in the tree (`grep -rn "PhaseCard?raw\|phaseCardSource"` → no import). |
| 3 | `STATUS_META` | **audited — none found.** Three tree-wide hits, all PROSE in `lib/phaseState.test.ts` (`:80-81`, `:166`, `:290`). `:81` is load-bearing context rather than a fence: it records that `STATUS_META` *"is NOT exported, so it cannot be imported here"* and that `Record<Phase["status"], StatusMeta>` makes the COMPILER the panel's parity guarantee — which is exactly the argument the new guard qualifies (the compiler covers every DECLARED key and says nothing about an inherited one). |
| 4 | `VERDICT_MARK` | **audited — none found that constrains the SPELLING.** Sixteen hits in `PhaseNodeCard.test.tsx` (`:482-519`, `:625-627`) and one in `WorkflowCanvas.editing.test.tsx:471`, every one a VALUE assertion against `nodePresentation`'s export. The card's `?raw` fences (`:214`, `:367-371`, `:399-431`, `:1089-1114`) name `onClick=`, the tint identifiers, the graph-library scope and the seal block — none names `VERDICT_MARK`. Form B is free. |
| 5 | `RING_STROKE` | **audited — none found.** ZERO occurrences of the identifier anywhere in the test tree. |

### The house spelling, measured at HEAD after the fix

```
grep -rn "Object.hasOwn" frontend/src                       →  0
grep -c "Object.prototype.hasOwnProperty.call" <named 3>    →  PhaseNode.tsx 1 · PhaseNodeCard.tsx 1 · panel/PhaseCard.tsx 2   = 4
grep -c "Object.prototype.hasOwnProperty.call" <blockers>   →  lib/phaseGlyph.tsx 1 · nodePresentation.ts 1                    = 2
                                                               TOTAL GUARDS IN THE PHASE = 6
grep -n "ICON_TINT\[" PhaseNode.tsx                         →  :166 (plus :161, the comment explaining why it must survive)
grep -n "Total by construction" PhaseNodeCard.tsx           →  NO MATCH        (⚠ count in that file: 5)
grep -n "encodeURIComponent(runId)" lib/api.ts              →  :3768 (getWorkflowRun) · :4661 (the pre-existing admin twin)
```

The `≥ 5 across three files` criterion measures **4** — see Deviation 3; the plan's own prescribed
Form B (one shared helper for sites 4 and 5) makes 5 arithmetically unreachable.

### `PhaseNodeCard.tsx` diff scope — no hunk near the badge tuple

```
git diff -U0 -- PhaseNodeCard.tsx | grep "^@@"
@@ -288,0 +289,23 @@   the own() helper
@@ -387,3 +410,18 @@   site 4's ⚠ correction
@@ -396   +434   @@   site 4's expression
@@ -689   +727,5 @@   site 5's stroke
```

`BadgeSlots` lives at `:175` / `:235-236`. Untouched — SC#4's "a third badge is a typecheck error"
tuple is not in any hunk, and `tsc` is unmoved.

### Both gate runs BEFORE the pin was written — the two `actual` columns agree

```
  PhaseNodeCard.test.tsx                      105     107      +2
  WorkflowRunPage.test.tsx                     87      88      +1
  PhaseNode.test.tsx                           25      26      +1
  PhaseTimeline.test.tsx                        8      10      +2
  total                                      2470    2476      +6
  total 2476  ·  failed 0  ·  pinned total 2470
count gate OK — 45/45 pinned files present, no per-file decrease, 0 failing.
```

(identical `actual` column on run 2)

### Gate AFTER the extension — pinned equals actual, zero slack

```
  PhaseNodeCard.test.tsx                      107     107       0
  WorkflowRunPage.test.tsx                     88      88       0
  PhaseNode.test.tsx                           26      26       0
  PhaseTimeline.test.tsx                       10      10       0
  total                                      2476    2476       0
  total 2476  ·  failed 0  ·  pinned total 2476
count gate OK — 45/45 pinned files present, no per-file decrease, 0 failing.
```

`git diff -U0` on the gate script shows **four `-` pin lines, every one raised** (105→107, 25→26,
8→10, 87→88) and **no `TARGETS` array line changed** — the two `TARGETS` tokens in the diff are
inside added comment prose.

### THE DEAD-ZONE QUESTION, answered by measurement rather than by assumption

Two of the four suites this plan extends live in directories that are outside both count-gate
knobs *by default* — `src/components/panel/__tests__/` and `src/pages/`. **Both are nevertheless
inside `TARGETS` by NAMED FILE** (`vitest-count-gate.cjs:471`, `:515`), adopted deliberately in
188-01 and 188-12. That they genuinely RUN is not inferred from the entry, it is read off the
gate's own output twice: `PhaseTimeline.test.tsx` moved `8 → 10` and `WorkflowRunPage.test.tsx`
moved `87 → 88`, and the printed `running:` command lists both paths. A green gate alone would
NOT have been evidence.

### The rest of the battery

```
npx tsc --noEmit -p tsconfig.app.json          →  33            (33 at baseline, 33 after Task 1, 33 after Task 2, 33 final)
npx eslint src/components/workflows/           →  5 problems (5 errors, 0 warnings) — the same five:
                                                  BuilderStoreProvider.tsx:45,:53,:78 · FlowEdge.tsx:147 · StepTypePicker.test.tsx:561
npx vite build                                 →  exit 0, built in 4.32s
grep -c INEFFECTIVE_DYNAMIC_IMPORT             →  2             (the PINNED figure — see Deviation 4)
git diff --stat -- backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json
                                               →  0 files changed
bash scripts/check-deploy-drift.sh             →  exit 0
git log --diff-filter=D across all four commits →  no deletions
```

Zero packages installed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — BLOCKING, and the headline finding] Two MORE prototype-key lookups on the same value had to be closed, because they crash the render before site 1's or site 2's consequence exists**

- **Found during:** Task 1, the very first RED run
- **Issue:** `data.phaseType` does not feed one lookup, it feeds **three**. The adapter hands the
  same author-supplied discriminator to `ICON_TINT` (WR-04 site 1) **and** to
  `nodePresentation.renderPhaseMark`, which indexes `lib/phaseGlyph`'s `PHASE_GLYPH_MARKS` and
  then `soulData.PHASE_GLYPHS`. Measured: `phaseGlyph("constructor")` returns the `Object`
  FUNCTION (truthy, so its `?? null` never fires), the caller does
  `createElement(Object, { className: "h-8 w-8" })`, and React throws **`Objects are not valid as
  a React child`**. The whole node crashes. Site 1's falsification — *"no rendered `style`
  attribute contains `function`"*, the plan's own must_have — **cannot pass while nothing renders
  at all**, and site 2's hits the identical crash through `panel/PhaseCard`'s `Glyph`.
- **Why it is a blocker rather than a sweep:** the plan's stated truth is *"a key inherited from
  `Object.prototype` yields the declared fallback"*. Guarding one of three lookups on one value
  leaves that property false and leaves two falsifications red. `188.1-CONTEXT.md`'s deferral of
  "sites beyond the five" is about sites this plan does not have to touch — these two are
  literally in the way.
- **Severity, stated plainly:** these are **worse** than any of the five named sites. Four of the
  five mis-paint; these two produce an unrecoverable render error on an author-supplied string.
  Both also shipped with docblocks asserting the totality they did not have — `phaseGlyph`'s
  *"total over any key"* and `nodePresentation`'s *"TOTALITY contract: both resolvers are total …
  rather than throwing"*.
- **Fix:** the same house guard at both, plus `⚠` corrections to both false claims.
- **Files modified:** `frontend/src/lib/phaseGlyph.tsx`, `frontend/src/components/workflows/nodePresentation.ts`
- **Commit:** `b018b20b`

**2. [Rule 1 — a falsification would have been VACUOUS as specified] Sites 2/3 drive `PhaseCard` directly, not a `PhaseTimeline` fixture**

- **Found during:** Task 1, reading the suite's harness before writing into it
- **Issue:** The plan says to *"render a `PhaseTimeline` fixture whose phase carries `phaseType:
  "constructor"` and `status: "toString"`"*. That fixture path reaches the store through the REAL
  normalizer, and `StreamsProvider.tsx:3393`/`:3464` map every wire status through
  `lib/phaseState.phaseStatusFromDb` — **which is already an own-property guard**. A
  `status: "toString"` handed to `replayFixture` is resolved to `"unknown"` before it can reach
  site 3. The prescribed test would have been **green against the unguarded tree**: never observed
  RED, and therefore not known to test anything — the exact failure the plan's own acceptance
  criterion exists to prevent.
- **Fix:** drive the exported `PhaseCard` directly. It is the real component `PhaseTimeline.tsx:217`
  renders, so this satisfies *"through the real component rather than by calling an unexported
  function"*; the reference in each case is an ORDINARY unrecognised value's render rather than a
  literal copied out of a module-private table. Both were then observed RED.
- **Files modified:** `frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx`
- **Commit:** `358a4c34`

**3. [Rule 3 — plan-internal contradiction] `hasOwnProperty.call ≥ 5 across the three named files` is unreachable given the plan's own prescribed Form B**

- **Found during:** Task 2, checking the acceptance criteria
- **Issue:** The plan instructs sites 4 and 5 to use *"Form B with a LOCAL guard (a small
  module-private helper in `PhaseNodeCard.tsx`…)"* — one helper, therefore **one** occurrence of
  the spelling — and then requires the three named files to sum to **≥ 5**. One helper for two
  sites yields 4 (1 + 1 + 2). Only inlining the ternary at both sites reaches 5.
- **Fix:** kept the shared helper. It is the shape the plan recommends first and the one
  `phaseState.ts` argues for (*"the fallback lives inside this function rather than at each call
  site precisely so that there is exactly ONE place it can be got wrong"*). Discharged the
  criterion at its intent — *are all five sites guarded* — with the per-site table above.
  Measured: **4** across the three named files, **6** across all five guarded files, **0**
  occurrences of `Object.hasOwn`.
- **Files modified:** none (a verification-method correction)
- **Commit:** `b018b20b`

**4. [Rule 1 — an inherited claim was false, for the second consecutive plan] The `[INEFFECTIVE_DYNAMIC_IMPORT]` criterion is TWO warnings, not one**

- **Found during:** Task 3, the battery
- **Issue:** The plan's acceptance criterion says `vite build` must exit 0 *"with exactly one
  `[INEFFECTIVE_DYNAMIC_IMPORT]` warning"*. Measured: **two**.
- **Fix:** none needed — `188.1-03` already measured this baseline empirically (its Deviation 3
  restored the pre-move tree and counted **2 before, 2 after**) and its closing note to this plan
  says so verbatim. The second warning is `ShikiCode.tsx`, in the chat subtree, untouched by this
  phase. Recorded rather than silently ignored because the same stale figure has now survived into
  two consecutive plans.
- **Files modified:** none
- **Commit:** `d17151bd` (recorded in the commit body)

**5. [Rule 2 — an acceptance grep and the house `⚠` rule are in direct conflict] The site-4 correction paraphrases the false claim instead of quoting it**

- **Found during:** Task 2
- **Issue:** The house rule (`WorkflowCanvas.tsx:313`, `soulData.ts:21-23`) is that a `⚠`
  correction states *what was previously claimed*. The plan's acceptance criterion is
  `grep -n "Total by construction" PhaseNodeCard.tsx` → **no match**. Quoting the claim satisfies
  the rule and fails the grep. This is the third occurrence of this exact shape in the phase
  (188.1-02 and 188.1-03 each hit it once).
- **Fix:** the correction opens *"USED TO OPEN BY CLAIMING THE LOOKUP WAS TOTAL 'by
  construction'"* and says, in place, why it paraphrases: the grep asks the tree whether the false
  claim still exists, and a quotation would answer yes. The argument survives; the literal does
  not.
- **Files modified:** `frontend/src/components/workflows/PhaseNodeCard.tsx`
- **Commit:** `b018b20b`

**6. [Rule 1 — a pre-existing flake inside this plan's declared blast radius] D-188.1-DEF-01 closed**

- **Found during:** post-Task-3, reviewing the phase's `deferred-items.md`
- **Issue:** `WorkflowRunPage.test.tsx:504` asserted `toHaveBeenCalled()` **synchronously** after
  a `visibilitychange` dispatch, alone in a describe where every other assertion awaits. Measured
  by `188.1-02` at roughly **1 failing run in 6**. The suite is pinned in the gate and the gate
  requires `failed 0`, so it can red CI for any plan in any phase.
- **Fix:** `await waitFor(() => expect(reconcile).toHaveBeenCalled())`. `waitFor` does not weaken
  the claim — the call must still happen, it may merely land a tick later. Fixed rather than
  deferred again because the item's OWN re-open trigger reads *"the next phase that touches
  `WorkflowRunPage.tsx` or its suite"*, and this plan extends that suite. It did **not** fire in
  any of the four gate runs this plan performed, so the fix is preventive.
- **Files modified:** `frontend/src/pages/WorkflowRunPage.test.tsx`, phase `deferred-items.md`
- **Commit:** `23046860`

### Not deviations, recorded so the next reader does not re-derive them

- **The plan's `<interfaces>` inventory was accurate at every site it named**, including the
  warning that site 3 has no fallback at all and the instruction to read `STATUS_META`'s keys
  before choosing one. It does declare an `unknown` row (Phase 188 Plan 02), so nothing was
  invented — recorded because the plan explicitly asked for the finding either way.
- **The `PhaseNodeCard.test.tsx:373` collision analysis was correct at both ends.** `DEFAULT_TINT`
  is satisfied by the import line at `PhaseNode.tsx:83` regardless of how the expression is
  written; only `/ICON_TINT\[/` was at risk, and the ternary preserves it.
- **`STATUS_META` is `Record<Phase["status"], StatusMeta>`, a typed union, not `Record<string, …>`.**
  The compiler is a total guarantee over DECLARED keys and no guarantee at all over inherited
  ones — which is the distinction the guard adds, and is worth stating because
  `lib/phaseState.test.ts:80-85` leans on the first half.
- **`PhaseNodeCard.tsx` carries a `?raw` fence at `:214` forbidding `onClick=`** (the "no focusable
  control inside the card" invariant) and one at `:1101` forbidding the run-state prop name inside
  the seal's JSX block. Neither was touched; both stayed green.

## Threat Model Dispositions

| Threat ID | Disposition | Outcome |
|---|---|---|
| T-188.1-01 | mitigate | **Closed, and WIDER than registered.** All five named lookups guarded in the house spelling, site 1 in the control-preserving ternary. Plus two unregistered lookups on the same value (`phaseGlyph`, `renderPhaseMark`) whose disposition should have been `mitigate` and was not written — they were found by driving the property rather than the patch. |
| T-188.1-01b | mitigate | **Closed.** The site-1 falsification asserts no rendered `style` attribute contains `function`, and separately that the painted well is byte-equal to an ordinary unknown type's. The equality is the load-bearing half: it holds regardless of how jsdom's CSS parser treats an invalid value. |
| T-188.1-02 | mitigate | **Closed.** `encodeURIComponent(runId)` at `api.ts:3768`; the test asserts the URL the mock RECEIVED. |
| T-188.1-02b | transfer (deferred, D4) | **Deferred as planned.** `signal` untouched; `git diff -- lib/api.ts` shows two hunks, both inside `getWorkflowRun`, neither touching the parameter list. The re-open trigger now lives in the shipped docblock, not only in a plan. |
| T-188.1-10 | mitigate | **Closed, ×4.** Four false-totality docblocks corrected in the `⚠` register, each naming the falsification that now keeps it honest. `soulData.ts:21-23`'s lesson — a drift that survived two phases — applied. |
| T-188.1-SC | mitigate | **Clean.** `git diff --stat -- frontend/package.json frontend/package-lock.json` → 0 files changed. `check-deploy-drift.sh` exit 0. Zero packages installed. |

## Verification Results

| Check | Required | Measured | ✔ |
|---|---|---|---|
| four suites, UNFIXED tree | exactly 6 failing, all others pass | **6 failed / 225 passed**, 225 == the exact pre-change total | ✔ |
| four suites, after the fixes | 0 failed | **231 passed / 0 failed** (143 + 88) | ✔ |
| `node scripts/vitest-count-gate.cjs` | `count gate OK`, failed 0, total ≥ 2462, no `[count-decrease]`, no `[missing-file]`, pinned == actual ×4 | `count gate OK`, failed 0, **2476/2476**, 45/45, all four at 0 | ✔ |
| both pre-pin runs agree | yes | identical `actual` columns (107 / 88 / 26 / 10, total 2476) | ✔ |
| no pin lowered, `TARGETS` unedited | yes | four `-` lines, all raised; no `TARGETS` array line in the diff | ✔ |
| `npx tsc --noEmit -p tsconfig.app.json` | exactly 33 | **33** at every stage | ✔ |
| `npx eslint src/components/workflows/` | exactly 5 errors | **5**, exactly the same five | ✔ |
| `npx vite build` | exit 0 | exit 0, `INEFFECTIVE_DYNAMIC_IMPORT` **2** (the pinned figure — Deviation 4) | ⚠ criterion said 1; the 1 is a stale inherited claim, corrected in 188.1-03 |
| `grep -rn "Object.hasOwn" frontend/src` | no match | **0** | ✔ |
| `grep -n "ICON_TINT\[" PhaseNode.tsx` | ≥ 1 | **2** (`:166` code, `:161` its explanation) | ✔ |
| `grep -n "Total by construction"` | no match, replacement contains `⚠` | **no match**; 5 `⚠` in the file | ✔ |
| `grep -n "encodeURIComponent(runId)"` | ≥ 1 inside `getWorkflowRun` | `:3768` | ✔ |
| `PhaseNodeCard.tsx` hunks | none near `BadgeSlots` | four hunks, at `:289`, `:410`, `:434`, `:727`; tuple at `:175`/`:235` | ✔ |
| `git diff --stat -- backend/ supabase/migrations/ frontend/package*.json` | 0 files | **0 files** | ✔ |
| `bash scripts/check-deploy-drift.sh` | exit 0 | **exit 0** | ✔ |
| `hasOwnProperty.call` sum, three named files | ≥ 5 | **4** (6 across all guarded files) | ⚠ Deviation 3 — unreachable given the plan's own Form B |

## Plan Success Criteria

1. Five prototype-key lookups total in `Object.prototype.hasOwnProperty.call` form; no `Object.hasOwn` — **met** (and two more, as blockers)
2. Six falsifications observed RED before any fix, green after — **met**, all six texts recorded verbatim
3. `PhaseNodeCard.test.tsx:373`'s `ICON_TINT[` positive control still holds — **met**
4. `getWorkflowRun` encodes its path segment; `signal` stays deferred — **met**
5. Two false-totality docblocks carry `⚠` corrections — **met, exceeded** (four)
6. Four pins at twice-measured actuals; `tsc` 33; eslint 5; zero backend/migration/package change — **met**

## Known Stubs

None. Nothing was stubbed — this plan guarded shipped lookups and added no surface.

## Deferred Issues

- **D-188.1-DEF-02 (new, logged not fixed):** `frontend/src/lib/providerLogo.tsx:107-108` is
  byte-for-byte the `phaseGlyph` defect, false *"total over any key"* comment included, because one
  file was written by copying the other. It blocks nothing this plan drives and `188.1-CONTEXT.md`
  defers beyond-the-five sites. Re-open trigger recorded in the phase's `deferred-items.md`, along
  with a note to re-measure `fileIcon`'s `?? DEFAULT_SPEC` (which providerLogo's comment cites as
  its model) in the same pass.
- **D4 / T-188.1-02b:** `getWorkflowRun`'s unused `signal` parameter. Unchanged by decision; the
  re-open trigger now ships inside the function's docblock.

## Threat Flags

None. This plan created no network endpoint, no auth path, no file-access pattern and no schema
change. It narrowed one existing client request path and made six client-side lookups total.

## For the Next Plan (188.1-05)

- **The count gate is at ZERO SLACK on all four extended suites** (107/107, 88/88, 26/26, 10/10)
  and a whole-gate total of **2476/2476**. Re-measure from the gate's own `actual` column across
  two agreeing runs; never hand-count `it(` literals.
- **`frontend/src/lib/` is still outside BOTH gate knobs.** The WR-07 falsification lives in
  `pages/WorkflowRunPage.test.tsx` for exactly that reason, and the reasoning is now recorded in
  the pin comment. Any future `lib/` test must either be adopted into `TARGETS` by named file or
  written into a suite that already is.
- **`FlowEdge.tsx`'s eslint error is at `:147`**, and the `[INEFFECTIVE_DYNAMIC_IMPORT]` count is
  **2**. Both are restated because both have already been wrong in an inherited document once.
- **UF-1 (`last_workflow_run_id` on the un-gated `GET /threads/{id}/workflow`) is untouched**, as
  planned — it is Plan 05's documentation deliverable. No endpoint was re-gated here; the backend
  diff is 0 files.
- **The A7 audit found a genuinely wider defect class than SC#6 names.** Three of the eight
  prototype-key lookups discovered in this subtree crash the render rather than mis-paint it, and
  the falsification only found them because it asserted the rendered consequence. If Plan 05 or
  189 writes another lookup guard, assert what the DOM does, not what the expression returns.

## Commits

| Task | Commit | Message |
|---|---|---|
| 1 | `358a4c34` | `test(188.1-04): six WR-04/WR-07 falsifications, all observed RED first` |
| 2 | `b018b20b` | `fix(188.1-04): close the five WR-04 prototype-key lookups, plus the two that blocked them` |
| 3 | `d17151bd` | `fix(188.1-04): encode the run-id path segment (WR-07) and extend four count-gate pins` |
| post | `23046860` | `fix(188.1-04): de-flake the visibilitychange assertion, log the eighth prototype-key site` |
