---
phase: 188.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/WorkflowCanvas.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: []
# NOTE: `requirements` is deliberately EMPTY. ROADMAP §Phase 188.1: "none — this is a
# refactor phase. It delivers no new user-facing capability." Traceability is to the six
# success criteria, never to a REQ- id. Do NOT call `requirements.mark-complete`.
success_criteria: ["SC#3", "SC#4", "SC#5"]
gap_closure: false

must_haves:
  truths:
    - "Every negative `?raw` assertion in the two canvas suites reads a SUBTREE source that automatically covers `PlaneEditingLayer.tsx` and `editAffordance.ts` — the two modules D-01 creates — the moment those files exist, so the 311 moved lines cannot leave a fence's scope silently"
    - "The subtree fence carries a MOVE-INVARIANT non-vacuity control: it asserts the moved code's own signatures are inside the fenced source, which is true BEFORE the move (they are in WorkflowCanvas.tsx) and after (they are in the new modules) — a later narrowing of the path list turns it red"
    - "SC#4's no-focusable-control-inside-a-node assertion runs against a render where `PlaneEditingLayer` is actually mounted, proved by a non-vacuity control, so it is not vacuous with respect to the moved code"
    - "This plan is behaviour-neutral on the shipped tree: no production file is touched, and every re-scoped fence stays green against the unchanged source"
  artifacts:
    - path: "frontend/src/components/workflows/WorkflowCanvas.test.tsx"
      provides: "subtree-scoped negative fences + a non-vacuous editable SC#4 assertion"
      contains: "import.meta.glob"
    - path: "frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx"
      provides: "subtree-scoped negative fences"
      contains: "import.meta.glob"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "BASELINE pins extended to the measured actuals for both edited suites"
      contains: "WorkflowCanvas.test.tsx"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowCanvas.test.tsx"
      to: "./PlaneEditingLayer.tsx + ./editAffordance.ts (not yet existing)"
      via: "import.meta.glob path list — an absent path contributes an empty string, never a throw"
      pattern: "CANVAS_SUBTREE_PATHS"
---

<objective>
Re-scope every narrowed `?raw` source fence in the two canvas suites BEFORE the code moves,
and make SC#4's focusable-control assertion non-vacuous.

Purpose: this is the phase's single highest-value task. Twelve negative assertions across
`WorkflowCanvas.test.tsx` and `WorkflowCanvas.composition.test.tsx` grep `WorkflowCanvas?raw`.
When 311 lines leave that file, **every one of them stays GREEN while covering 311 fewer
lines.** The count gate will not see it, `tsc` will not see it, and a reviewer reading a
green suite cannot tell a guard that guards from one that stopped. That is Phase 188's F7
lesson in a new dress: *an invariance fence says nothing about what it no longer covers.*
Doing this FIRST — against the unchanged tree — means the re-scope is provably behaviour-
neutral (every fence must stay green with zero production edits), and the move in Plan 03
lands into fences that already reach it.

Output: a subtree-scoped source constant in each suite, twelve re-pointed negatives, a
move-invariant non-vacuity control, an editable-mode SC#4 assertion, and two extended
`BASELINE` pins.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-CONTEXT.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-PATTERNS.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-VALIDATION.md

<interfaces>
<!-- Measured at HEAD `18267202` this session. Re-derive rather than trust — every number
     in this repository is live (`feedback_dont_inherit_unmeasured_claims`). -->

The house `?raw` directory-sweep idiom already shipped in this tree
(`PhaseFormPanel.rails.test.tsx:422-426`, cited by `governanceVocabulary.test.ts:65` as
"the house `?raw` / `import.meta.glob` idiom"):

    const WORKFLOW_MODULES = import.meta.glob("./*.{ts,tsx}", {
      query: "?raw",
      eager: true,
      import: "default",
    }) as Record<string, string>

`import.meta.glob` expands at build time over files that EXIST. A path named in a filter
list that does not exist yet simply has no key in the record — it does not throw. That is
what makes this fence authorable before Plan 03 creates the two modules.

MEASURED assertion inventory (subjects to re-point are marked ▶):

  frontend/src/components/workflows/WorkflowCanvas.test.tsx
    :32   import workflowCanvasSource from "./WorkflowCanvas?raw"
    :503  toContain("showInteractive={false}")            POSITIVE — stays
  ▶ :507  not.toMatch(/MiniMap/)
  ▶ :508  not.toMatch(/hideAttribution/)
  ▶ :512  not.toMatch(/workflows\/validate/)
  ▶ :513  not.toMatch(/grounding_mode/)
    :531  toMatch(/change\.type !== "position"/)          POSITIVE — stays
    :532  toMatch(/change\.type !== "dimensions"/)        POSITIVE — stays
    :537  toMatch(/\{ measured \}/)                       POSITIVE — stays
    :633  stripComments(...).length < ...length           CONTROL on the stripper — stays
  ▶ :643  not.toContain(word) for 7 READING_WORDS
    :649  toMatch(... runVocabulary ...)                  POSITIVE — stays
  ▶ :653  not.toMatch(... runVocabulary import ...)
  ▶ :658  not.toMatch(/from\s+["']@\/lib\/phaseState["']/)   ← D-14 relevant
  ▶ :674  const code = stripComments(workflowCanvasSource) — the phase_index/phaseIndex negative
    :677  match(ORDINAL_FIELD, "g") toHaveLength(1)       HARD COUNT PIN — see the trap below
    :685-:696  settledNodes / nodes memo slices           POSITIVE — stay

  frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx
    :44   import workflowCanvasSource from "./WorkflowCanvas?raw"
  ▶ :370  not.toMatch(/workflows\/validate/)
  ▶ :371  not.toMatch(/grounding_mode/)
  ▶ :375  not.toMatch(/MiniMap/)
  ▶ :376  not.toMatch(/hideAttribution/)
    :377  toContain("showInteractive={false}")            POSITIVE — stays
  ▶ :385  const code = workflowCanvasSource... — the `severity` negative at :392
    :397  toMatch(/severity/)                             POSITIVE CONTROL — stays

⚠ THE COUNT-PIN TRAP at `WorkflowCanvas.test.tsx:677`. That assertion pins the number of
`phase_index` occurrences at EXACTLY 1 (not ≥1, not ≤1). The single occurrence is
`WorkflowCanvas.tsx:845`, inside the `onInsertAt` prop docblock, which STAYS. Widening a
count pin to three files changes what the number means. RULE: negatives widen to the
subtree; POSITIVES and COUNT PINS stay on `workflowCanvasSource`.

MEASURED SC#4 vacuity (this corrects `188.1-RESEARCH.md` Open Question 3, which left it
open): BOTH shipped assertions render NON-editable —
    :257  const { container } = renderCanvas(evalCoverage)
    :265  const { container } = renderCanvas(evalCoverage)
    :274  const { container } = renderCanvas(docQaHumanGrounded, { kbTools: [KB_TOOL] })
`PlaneEditingLayer` renders only under `editable`, so today the SC#4 walk never mounts it.
It is vacuous with respect to the moved code, and the answer is an added editable variant.

MEASURED affordance testids in `WorkflowCanvas.tsx` (the layer's own DOM):
    :609  data-testid={`canvas-insert-${index}`}
    :649  data-testid={`canvas-remove-${slug}`}
    :690  data-testid="canvas-insert-picker"

MEASURED gate pins (`scripts/vitest-count-gate.cjs`):
    :153  "WorkflowCanvas.test.tsx": 46
    :382  "WorkflowCanvas.composition.test.tsx": 19
    :399  const BASELINE_TOTAL = ... // 2421   ← stale comment; the reduce computes 2462
    :403  TARGETS[0] === "src/components/workflows"   ← a BARE DIRECTORY. Do NOT edit TARGETS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point every negative `?raw` fence at a subtree source that self-includes the two D-01 modules</name>

  <files>
frontend/src/components/workflows/WorkflowCanvas.test.tsx
frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/WorkflowCanvas.test.tsx` — the whole `:490-700` fence
  region plus the import block at `:25-40`. Classify EVERY `workflowCanvasSource` assertion
  as negative / positive / count-pin before editing one of them.
- `frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx` — `:40-50` and
  `:360-400`.
- `frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx:415-445` — the shipped
  `import.meta.glob` + `?raw` sweep and its "the glob is not empty" control. This is the
  house form; match it.
- `frontend/src/components/workflows/canvasModel.purity.test.ts:14-21` — the `?raw` import
  idiom every other suite cites, and the shipped statement of the additive-diff rule.
- `.planning/phases/188.1-.../188.1-RESEARCH.md` § Pitfall 1 — the full hazard write-up.
  </read_first>

  <action>
In BOTH suites, add a module-scope subtree source beside the existing `workflowCanvasSource`
import. Do NOT delete the existing import — positives and the count pin still need it, and
an additive diff is the house rule (`canvasModel.purity.test.ts:18-21`).

Build it with the shipped `import.meta.glob("./*.{ts,tsx}", { query: "?raw", eager: true,
import: "default" })` sweep, narrowed by an explicit path list named
`CANVAS_SUBTREE_PATHS` holding exactly three literals — `./WorkflowCanvas.tsx`,
`./PlaneEditingLayer.tsx`, `./editAffordance.ts` — mapped through the glob record with an
empty-string default for a path that does not exist yet, then joined with a newline into
`canvasSubtreeSource`. Carry a `//` comment above it naming the idiom, its precedent
(`PhaseFormPanel.rails.test.tsx:422-426`), and — the load-bearing part — WHY the list names
two files that do not exist at this commit: they arrive in Plan 03, and a fence authored to
receive them cannot be narrowed by the extraction that follows.

Re-point ONLY the negative assertions listed with ▶ in `<interfaces>` from
`workflowCanvasSource` to `canvasSubtreeSource`. This is a SUBJECT change, not a count
change — do not add, delete, rename or re-describe any `it(` or `describe(`. Leave every
positive assertion and the `:677` count pin on `workflowCanvasSource`; add a short `//`
note at the count pin stating that it is a per-file count and deliberately not widened.

Add ONE new `it(` per suite carrying the move-invariant non-vacuity control: assert
`canvasSubtreeSource` contains the literal `EDIT_AFFORDANCE = {` AND the literal
`function PlaneEditingLayer(`. Both are true TODAY (they are in `WorkflowCanvas.tsx:365`
and `:569`) and remain true after Plan 03 (they are in the new modules) — so the control
proves the fence covers the moved code wherever it lives, and goes red if a later edit
narrows `CANVAS_SUBTREE_PATHS`. In the same `it(`, assert `CANVAS_SUBTREE_PATHS` has
length 3 and that the glob record is non-empty, so a broken glob cannot pass vacuously.

Do NOT touch any production file in this task. Do NOT introduce the words `grounding_mode`,
`workflows/validate`, `MiniMap`, `hideAttribution`, `severity`, `phase_index` or any of the
seven `READING_WORDS` into a file the fences now read — the test files are not fenced, but
`WorkflowCanvas.tsx` is, and it must not be edited here at all.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx` exits 0 with **0 failed**. Every re-scoped fence is green against the UNCHANGED production tree — that is the proof the re-scope is behaviour-neutral.
- `grep -c "canvasSubtreeSource" frontend/src/components/workflows/WorkflowCanvas.test.tsx` returns **≥ 9** (8 re-pointed subjects + the control), and the same grep on `WorkflowCanvas.composition.test.tsx` returns **≥ 6**.
- `grep -n "workflowCanvasSource" frontend/src/components/workflows/WorkflowCanvas.test.tsx` still shows the positives at the shipped lines (`showInteractive={false}`, `change.type !== "position"`, `change.type !== "dimensions"`, `{ measured }`, the `runVocabulary` positive, the `settledNodes`/`nodes` slices) and the `ORDINAL_FIELD` count pin — none of them was re-pointed.
- `git diff -- frontend/src/components/workflows/` shows **zero** changes to any non-test file.
- RED OBSERVED, recorded in the SUMMARY with the failure text: temporarily append the literal `grounding_mode` inside a comment in `frontend/src/components/workflows/WorkflowCanvas.tsx`, re-run the two suites, confirm the `grounding_mode` fence FAILS, then `git checkout -- frontend/src/components/workflows/WorkflowCanvas.tsx`. A fence never observed failing is not known to be a fence.
- RED OBSERVED for the control: temporarily reduce `CANVAS_SUBTREE_PATHS` to `["./WorkflowCanvas.tsx"]` and confirm the new control still PASSES (it must — the symbols are still in the canvas today), then temporarily change one literal to `"./NotAFile.ts"` and confirm the length-3 assertion FAILS. Revert. Record both observations.
  </acceptance_criteria>

  <done>Every negative `?raw` assertion in both canvas suites reads `canvasSubtreeSource`; the path list names the two D-01 modules that Plan 03 creates; the move-invariant control is green and was observed failing under a narrowed list; no production file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Make SC#4's no-focusable-control assertion non-vacuous, and extend the two BASELINE pins</name>

  <files>
frontend/src/components/workflows/WorkflowCanvas.test.tsx
scripts/vitest-count-gate.cjs
  </files>

  <read_first>
- `frontend/src/components/workflows/WorkflowCanvas.test.tsx:248-300` — the shipped
  `describe("WorkflowCanvas — one tab stop per node")` block. Both existing renders are
  NON-editable (`renderCanvas(evalCoverage)` / `renderCanvas(docQaHumanGrounded, { kbTools })`),
  which is why the walk never mounts `PlaneEditingLayer` today.
- The `renderCanvas` helper in the same file — read its options object to learn the exact
  spelling of the editable flag and any props the layer needs (`lanes`, `nudges`,
  `onOpenPicker`, `onRequestRemove`) before writing the render call.
- `frontend/src/components/workflows/WorkflowCanvas.tsx:600-700` — the affordance JSX, for
  the `data-testid` spellings the non-vacuity control queries.
- `scripts/vitest-count-gate.cjs:40-120` (the LOWERED/EXTENDED doctrine in the header),
  `:150-160`, `:375-405` (the pins and `BASELINE_TOTAL`).
  </read_first>

  <action>
Add ONE new `it(` to the shipped `one tab stop per node` describe block: the same walk —
every `.react-flow__node` contains zero `button, a, [tabindex]` — but run against an
**editable** canvas, so `PlaneEditingLayer` is actually mounted and the `＋`/`✕` controls
it renders are in the DOM.

Guard it with a non-vacuity control ABOVE the walk, in the `FlowEdge.test.tsx:294-301`
house shape (control first, so an empty result below is evidence rather than a broken
matcher): assert the container has at least one element matching
`[data-testid^="canvas-insert-"]` AND at least one matching `[data-testid^="canvas-remove-"]`.
Without that control the new test is exactly as vacuous as the two it supplements. Also
assert the tabbable-node count still equals the phase count in editable mode, so the
invariant under test is "the affordances are on the LANE, never inside a card" rather than
"there are no affordances".

Add a `//` comment recording the measured finding this test exists for: the two shipped
assertions render non-editable, so before this commit SC#4's mechanical enforcement did not
reach the layer at all — a correction to the prior reading, in the house `⚠` register.

Then extend the count-gate pins. Run `node scripts/vitest-count-gate.cjs` TWICE and read
`WorkflowCanvas.test.tsx` and `WorkflowCanvas.composition.test.tsx` from the script's own
printed `actual` column; the two runs must agree. Raise each pin to that measured actual.
EXTENSIONS ONLY — never lower a pin, never edit `TARGETS` (`TARGETS[0]` is the bare
directory `src/components/workflows`, which already covers everything this phase adds).
While the file is open, free-ride D9: correct the stale `// 2421` trailing comment at
`BASELINE_TOTAL` to the value the reduce actually computes, and mark it with the house `⚠`
so the correction reads as a correction rather than a silent rewrite.
  </action>

  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>

  <acceptance_criteria>
- `node scripts/vitest-count-gate.cjs` prints `count gate OK`, **failed 0**, total **≥ 2462**, and reports **no `[count-decrease]`** and **no `[missing-file]`**.
- The pinned and actual values for `WorkflowCanvas.test.tsx` and `WorkflowCanvas.composition.test.tsx` are EQUAL in the gate's own output, and the two gate runs agreed on the actual before the pin was written. Both numbers are recorded verbatim in the SUMMARY.
- `WorkflowCanvas.test.tsx`'s pin is **> 46** and `WorkflowCanvas.composition.test.tsx`'s is **≥ 19**; neither was lowered. `git diff scripts/vitest-count-gate.cjs` shows **no** edit to `TARGETS`.
- VACUITY PROVEN AND CLOSED, recorded in the SUMMARY: run the new editable test with the non-vacuity control temporarily inverted (`toHaveLength(0)` on `[data-testid^="canvas-insert-"]`) and confirm it FAILS — i.e. the affordances really are in the DOM in this render. Revert. Then confirm the two SHIPPED assertions at `:257`/`:265` still pass unchanged.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports exactly **33** errors, with **0** lines naming `WorkflowCanvas`, `PlaneEditingLayer` or `editAffordance`. Never `tsc -b`, never a bare `--noEmit`.
  </acceptance_criteria>

  <done>SC#4's invariant is mechanically enforced against a render where the extracted layer is mounted, proved non-vacuous by an inverted control; both suite pins sit at their twice-measured actuals; the gate is green with zero slack violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped guard → refactor | A source fence is the only mechanical record that a scope promise still holds. A refactor that moves code out from under a fence crosses this boundary silently: nothing fails. |
| test estate → count gate | A suite that stops running, or a pin that stops matching its actual, is indistinguishable from a passing one unless the gate is read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188.1-04 | Repudiation | the twelve `?raw` negative fences in `WorkflowCanvas.test.tsx` + `WorkflowCanvas.composition.test.tsx` | mitigate | Re-point every negative at `canvasSubtreeSource`, whose path list names the two D-01 modules before they exist; carry a move-invariant control asserting the moved code's own signatures are inside the fenced source; observe the `grounding_mode` fence RED against a deliberate violation before trusting it |
| T-188.1-04b | Repudiation | `WorkflowCanvas.test.tsx:677` `phase_index` count pin | accept | A per-FILE count pin is not widenable without changing its meaning. Left on `workflowCanvasSource` with an explicit in-file note; the single occurrence (`WorkflowCanvas.tsx:845`, the `onInsertAt` docblock) is outside the moved regions and survives Plan 03 unchanged |
| T-188.1-07 | Spoofing | SC#4's "no focusable control inside the card" walk | mitigate | The two shipped assertions render non-editable, so the invariant was never enforced against `PlaneEditingLayer`. An editable variant with a `[data-testid^="canvas-insert-"]` non-vacuity control closes it; the control is proven by inversion |
| T-188.1-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages are installed by this phase.** `188.1-RESEARCH.md` § Package Legitimacy Audit records "not applicable — nothing to check". Hard gate at every plan close: `git diff --stat -- frontend/package.json frontend/package-lock.json` reports **0 files changed**. Any package appearing in this phase is out of scope by construction and requires a blocking human checkpoint before install |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx` → 0 failed
- `node scripts/vitest-count-gate.cjs` → `count gate OK`, failed 0, total ≥ 2462, no `[count-decrease]`, no `[missing-file]`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → exactly 33
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json` → 0 files changed
- `git diff --name-only -- frontend/src` → test files only; no production source touched
</verification>

<success_criteria>
1. All twelve negative `?raw` assertions read a subtree source that will include `PlaneEditingLayer.tsx` and `editAffordance.ts` automatically once Plan 03 creates them (D-01).
2. The move-invariant control is green and was observed red under a narrowed path list.
3. The `grounding_mode` fence was observed RED against a deliberate violation of the unchanged tree.
4. SC#4's walk runs against an editable render with a proven-non-vacuous affordance control.
5. Both BASELINE pins equal their twice-measured actuals; `TARGETS` unedited; gate green.
6. Zero production files changed — this plan cannot alter behaviour.
</success_criteria>

<output>
Create `.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-01-SUMMARY.md` when done. Record verbatim: both gate runs' actual columns, the two pin values written, the RED-observation failure texts, and the inverted-control failure text.
</output>
