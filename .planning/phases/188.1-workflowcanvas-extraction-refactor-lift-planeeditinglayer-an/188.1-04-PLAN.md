---
phase: 188.1
plan: 04
type: execute
wave: 4
depends_on: ["188.1-03"]
files_modified:
  - frontend/src/components/workflows/PhaseNode.tsx
  - frontend/src/components/workflows/PhaseNodeCard.tsx
  - frontend/src/components/panel/PhaseCard.tsx
  - frontend/src/lib/api.ts
  - frontend/src/components/workflows/PhaseNode.test.tsx
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
  - frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: []
# EMPTY BY DESIGN — refactor phase, no REQ- ids (ROADMAP §188.1).
success_criteria: ["SC#6"]
gap_closure: false

must_haves:
  truths:
    - "All five WR-04 prototype-key lookups are total: a key inherited from `Object.prototype` yields the declared fallback, never an inherited function — and the one site with a real sink can no longer interpolate a function into a CSS `radial-gradient`"
    - "Each of the five guards was observed RED first — the falsification failed against the shipped code before the guard was written, so the guard is known to test the property rather than the patch"
    - "The own-guard is spelled `Object.prototype.hasOwnProperty.call(...)`, the tree's ONLY shipped spelling — `Object.hasOwn` has zero occurrences in `frontend/src` and this phase does not introduce a second spelling"
    - "`PhaseNodeCard.test.tsx`'s positive control survives: the literal `ICON_TINT[` form still exists in `PhaseNode.tsx`, so the sibling negative fence stays non-vacuous"
    - "`getWorkflowRun` encodes its path segment, so a `runId` containing `/`, `?` or `#` can no longer reshape the request path"
    - "Two docblocks that claimed a totality the expression did not provide are corrected — the correction is part of the fix, not optional prose"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseNode.tsx"
      provides: "the guarded tint lookup, in the ternary shape that preserves the shipped control"
      contains: "Object.prototype.hasOwnProperty.call"
    - path: "frontend/src/components/panel/PhaseCard.tsx"
      provides: "guarded phase-type and status lookups"
      contains: "Object.prototype.hasOwnProperty.call"
    - path: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      provides: "guarded verdict-mark and ring-stroke lookups"
      contains: "Object.prototype.hasOwnProperty.call"
    - path: "frontend/src/lib/api.ts"
      provides: "the encoded run-id path segment in getWorkflowRun"
      contains: "encodeURIComponent(runId)"
  key_links:
    - from: "frontend/src/components/workflows/PhaseNode.tsx"
      to: "frontend/src/components/workflows/PhaseNodeCard.tsx CSS radial-gradient sink"
      via: "the resolved `tint` prop, interpolated into a style string"
      pattern: "radial-gradient"
---

<objective>
Land the three bounded hardening fixes Phase 188's security audit owed to the next phase
touching these files: the five WR-04 prototype-key sites, WR-07's `encodeURIComponent`, and
their falsifications.

Purpose: SC#6 makes security a first-class deliverable rather than a side-check. WR-04 and
WR-07 were deferred from 188 as WARNINGs under a non-security label; the ROADMAP names them
here because they sit in exactly this subtree. Each is a few lines — and each is a property,
not a patch: `phaseState.ts:73-74` states the house rule that *totality is a property of the
function, not of its current callers*, which is precisely the reachability argument the 188
deferral rested on and this phase rejects.

Output: five guarded lookups in the tree's own spelling, one encoded path segment, six
falsifications each observed RED first, two corrected docblocks, and extended pins.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-CONTEXT.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-PATTERNS.md
@.planning/phases/188-non-technical-run-observability/188-SECURITY.md

<interfaces>
<!-- MEASURED at HEAD `18267202` this session. Re-derive; line numbers move. -->

THE FIVE WR-04 SITES, read at source:

  1  frontend/src/components/workflows/PhaseNode.tsx:149
       const tint = ICON_TINT[data.phaseType] ?? DEFAULT_TINT
       SINK: the resolved value is interpolated into CSS at
       `PhaseNodeCard.tsx:738` — `radial-gradient(circle, ${tint}, …)`.
       This is the ONLY one of the five with a sink.
  2  frontend/src/components/panel/PhaseCard.tsx:53
       function phaseTypeMeta(phaseType: string): PhaseTypeMeta {
         return PHASE_TYPE_LABEL[phaseType] ?? UNKNOWN_PHASE_META
       }
       Consumers read `.glyph` / `.label` off the result.
  3  frontend/src/components/panel/PhaseCard.tsx:257
       const status = STATUS_META[phase.status]
       NO FALLBACK AT ALL. Read `STATUS_META`'s declared keys before choosing a fallback —
       if it already carries an unknown/degraded entry, use that; if it does not, record
       that finding explicitly rather than inventing a new visual state.
  4  frontend/src/components/workflows/PhaseNodeCard.tsx:396
       const mark = verdict ? (VERDICT_MARK[verdict] ?? VERDICT_MARK.unknown) : null
       Sits under a comment claiming "Total by construction" — a claim the expression does
       not provide. Correcting that docblock is part of the fix (SC#6), not optional prose.
  5  frontend/src/components/workflows/PhaseNodeCard.tsx:689
       stroke={RING_STROKE[reading]}     (`RING_STROKE` declared at :321)
       Sink: an SVG `stroke` attribute.

WHY `??` DOES NOT SAVE ANY OF THEM: a plain object literal INHERITS `constructor`,
`toString`, `__proto__` and friends. `TABLE["constructor"]` returns a FUNCTION — truthy, so
the coalesce never fires — and site 1 then interpolates that function into a CSS string.

THE HOUSE SPELLING — measured: `Object.hasOwn` occurs **ZERO** times in `frontend/src`.
Both shipped guards use `Object.prototype.hasOwnProperty.call(...)`. `188.1-RESEARCH.md`
Pitfall 3 recommends `Object.hasOwn`; `188.1-PATTERNS.md` § 5 overturns that on measurement.
**Use the house spelling.** Two shipped forms to choose between:

  Form A — the guard clause (`frontend/src/lib/phaseState.ts:76-79`), best when the whole
  function IS the lookup (fits sites 2 and 3):
      if (!Object.prototype.hasOwnProperty.call(DB_PHASE_STATUS, raw)) return "unknown"
      return DB_PHASE_STATUS[raw]

  Form B — a local `own()` helper + `??` (`frontend/src/components/workflows/runVocabulary.ts:84-88`),
  best where a `?? fallback` already reads naturally (fits sites 4 and 5):
      function own<T>(table: Record<string, T>, reading: CanvasReading): T | undefined {
        return Object.prototype.hasOwnProperty.call(table, reading)
          ? (table as Record<string, T>)[reading] : undefined
      }
  ⚠ LANDMINE: that `own()` is NOT exported and is typed to `CanvasReading`. SC#6's "route
  through `own()`" is NOT a drop-in for `phaseType: string` or `verdict`. Do not export and
  generalise it — `runVocabulary.ts` has its own `?raw` fences at `PhaseNode.test.tsx:50`
  and `PhaseNodeCard.test.tsx:44`, so widening its signature has a blast radius. Write a
  local guard at each site instead.

⚠ THE POSITIVE-CONTROL COLLISION AT SITE 1 — verified at both ends.
`PhaseNodeCard.test.tsx:368-374` requires THREE literal forms to survive in `PhaseNode.tsx`:
      toMatch(/import[^;]*\b(ICON_TINT|DEFAULT_TINT)\b[^;]*from ... nodePresentation/)
      toMatch(/ICON_TINT\[/)
      toMatch(/DEFAULT_TINT\s*[,)\]]/)
Measured: `DEFAULT_TINT` appears at `PhaseNode.tsx:83` (in the import list, followed by a
comma), so the third control is satisfied by the IMPORT LINE regardless of how `:149` is
rewritten. Only `/ICON_TINT\[/` is at risk. `own(ICON_TINT, …)` REMOVES the bracket form and
turns the control red; a ternary preserves it:
      const tint = Object.prototype.hasOwnProperty.call(ICON_TINT, data.phaseType)
        ? ICON_TINT[data.phaseType]
        : DEFAULT_TINT

⚠ A7 (research assumption, MEDIUM confidence): sites 2-5 were NOT individually audited for
their own `?raw` controls. Before fixing each, grep that table's identifier across the test
tree for a source fence that constrains the spelling — the same way site 1's collision was
found. Record the result per site; "audited, none found" and "not audited" must be
distinguishable in the SUMMARY.

WR-07 — the whole fix, at `frontend/src/lib/api.ts:3751`:
      BEFORE: const res = await fetch(`${API_BASE}/workflow-runs/${runId}`, { headers, signal })
      AFTER:  encodeURIComponent(runId) in the path segment
`encodeURIComponent` appears 15× in this file already; `api.ts:3053-3060` (`listRelationships`)
is the form to copy — the defensive encode WITH a two-line rationale comment naming the
finding id. `getWorkflowRun`'s docblock at `:3733-3745` already documents the ownership gate;
APPEND to it, do not rewrite (house `⚠`/additive docblock rule).
⚠ SCOPE BOUNDARY: the same review paragraph also proposes threading an `AbortController`
into the unused `signal` parameter. That half is OUT OF SCOPE (D4) — it changes request-
cancellation behaviour on a live polling surface. SC#6 names only `encodeURIComponent`.

WHERE THE FALSIFICATIONS GO (measured — there is NO `PhaseCard.test.tsx` in the tree):
  site 1  →  frontend/src/components/workflows/PhaseNode.test.tsx          pin 25
  sites 2,3 → frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx  pin 8
             (`PhaseTimeline.tsx:217` renders `<PhaseCard phase={phase} position={i} />`,
             and this suite is inside BOTH `TARGETS` (`:440`) and `BASELINE` (`:266`))
  sites 4,5 → frontend/src/components/workflows/PhaseNodeCard.test.tsx      pin 105
  WR-07   →  frontend/src/pages/WorkflowRunPage.test.tsx                    pin 87
             (in `TARGETS` at `:484`, in `BASELINE` at `:337`)
⚠ Do NOT create a new suite in `src/components/panel/__tests__/` — that directory is covered
by NAMED FILES only (`vitest-count-gate.cjs:423-446`), so a new file there silently never
runs. Extend the two named suites that already exist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write one falsification per WR-04 site and per WR-07, and observe every one of them RED</name>

  <files>
frontend/src/components/workflows/PhaseNode.test.tsx
frontend/src/components/workflows/PhaseNodeCard.test.tsx
frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx
frontend/src/pages/WorkflowRunPage.test.tsx
  </files>

  <read_first>
- Each of the five sites at source: `PhaseNode.tsx:140-155`, `panel/PhaseCard.tsx:44-60` and
  `:250-265`, `PhaseNodeCard.tsx:315-330` and `:388-400` and `:680-695`. Read what each
  resolved value is USED for — the falsification must assert on the consequence, not on the
  lookup, or it tests the patch instead of the property.
- `frontend/src/lib/phaseState.ts:60-85` — the shipped guard AND its docblock, which records
  that the guard "was measured RED before it was written". That is the standard this task
  meets.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:326-385` — the tint fence and
  its three positive controls, so the site-1 falsification does not collide with them.
- `frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx` — its render helper and
  fixtures, so a phase with a prototype-key `phaseType`/`status` can be driven through the
  real component rather than by calling an unexported function.
- `frontend/src/pages/WorkflowRunPage.test.tsx` — how `fetch` is mocked in this suite, so the
  WR-07 assertion reads the URL the mock actually received.
  </read_first>

  <action>
Write six falsifications as EXTENSIONS to the four named suites — no new file, no test
renamed, deleted or re-described.

For each WR-04 site, drive the real component with a prototype key (`constructor` is the
sharpest: it resolves to a FUNCTION, so a `??` fallback provably does not fire) and assert
the CONSEQUENCE:
  · site 1 — render `PhaseNode` with `data.phaseType = "constructor"` and assert that no
    rendered `style` attribute anywhere in the container contains the substring `function`,
    and that the tint-bearing element renders the same value it renders for an ordinary
    unknown type. The CSS sink is what makes this the serious one.
  · sites 2/3 — render a `PhaseTimeline` fixture whose phase carries `phaseType:
    "constructor"` and `status: "toString"`; assert the render does not throw, the label and
    glyph are the declared unknown meta, and no rendered text or attribute contains
    `function`.
  · sites 4/5 — render `PhaseNodeCard` with `verdict = "constructor"` and, separately,
    `reading = "constructor"`; assert the verdict mark is the declared unknown mark and the
    SVG `stroke` attribute is a colour token, never a stringified function.
For WR-07, assert the URL the mocked `fetch` receives when `getWorkflowRun` is called with a
`runId` of `"a/b"` — it must contain the encoded `a%2Fb` and must NOT contain a bare
`workflow-runs/a/b` path.

Every falsification carries a `//` comment naming the finding id (WR-04 site N / WR-07), the
sink, and the fact that it was observed RED first — the `phaseState.ts` docblock register.

Run them BEFORE any production edit and confirm each one FAILS. Record every failure text
verbatim in the SUMMARY. Per `project_185_secured`: verify the PROPERTY, not the PATCH, and
observe falsification RED first. A falsification that never failed is not known to test
anything.

While you are in each site's suite, perform the A7 audit: grep the table identifier
(`ICON_TINT`, `PHASE_TYPE_LABEL`, `STATUS_META`, `VERDICT_MARK`, `RING_STROKE`) across the
test tree for any `?raw` source fence that constrains its spelling, exactly as site 1's
collision was found. Record per site: audited + fence found (with file:line), or audited +
none found. Do not leave a site's audit result unstated.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/PhaseNode.test.tsx src/components/workflows/PhaseNodeCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- Run against the UNFIXED production tree, the command above reports **exactly 6 failing tests** — one per site plus WR-07 — and every other test in those four suites still passes. Each failure text is recorded verbatim in the SUMMARY. This is the RED observation; it is the deliverable of this task.
- Each falsification asserts on a CONSEQUENCE (a rendered `style`, `stroke`, label, or the URL a mock received), not on the lookup expression. A test that greps source for `hasOwnProperty` tests the patch and does not satisfy this criterion.
- The A7 audit is recorded for all five sites, each as "audited — fence at file:line" or "audited — none found". No site is left unstated.
- No new test file was created. `git status --short frontend/src` lists only the four named suites as modified.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports exactly **33** (a failing test still typechecks).
  </acceptance_criteria>

  <done>Six falsifications exist as extensions to four already-pinned suites, all six were observed failing against the shipped code, and every site's own-fence audit result is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Close the five WR-04 sites in the tree's own spelling, and correct the docblocks that claimed a totality they did not have</name>

  <files>
frontend/src/components/workflows/PhaseNode.tsx
frontend/src/components/workflows/PhaseNodeCard.tsx
frontend/src/components/panel/PhaseCard.tsx
  </files>

  <read_first>
- `frontend/src/lib/phaseState.ts:60-85` — Form A and the shared docblock rationale (*"THE
  OWN-PROPERTY GUARD IS NOT CEREMONY, and it was measured RED before it was written … totality
  is a property of the function, not of its current callers"*). Reuse that wording; it is the
  house argument.
- `frontend/src/components/workflows/runVocabulary.ts:72-95` — Form B and its docblock. Note
  `own()` is module-private and `CanvasReading`-typed: read it for the SHAPE, do not import it.
- `frontend/src/components/workflows/PhaseNodeCard.tsx:388-400` — the "Total by construction"
  comment at site 4, which is the false claim SC#6 requires corrected.
- `frontend/src/components/workflows/PhaseNodeCard.tsx:730-745` — the `radial-gradient` sink,
  so the site-1 fix is written with its consequence in view.
- `.planning/phases/188.1-.../188.1-PATTERNS.md` § 5 — the measured overturn of research's
  `Object.hasOwn` recommendation and the verified site-1 control constraint.
  </read_first>

  <action>
Apply an own-property guard at all five sites using `Object.prototype.hasOwnProperty.call(...)`
— the tree's only shipped spelling. Do not introduce `Object.hasOwn`; it has zero occurrences
in `frontend/src`, and a second spelling for one concept is a drift this phase must not start.

Site 1 MUST use the ternary shape that preserves the literal `ICON_TINT[` form, because
`PhaseNodeCard.test.tsx:373` is a positive control keeping the sibling negative fence
non-vacuous. Any shape that removes the bracket index turns a shipped guard red.

Sites 2 and 3 fit Form A (the guard clause), since each is or can be wrapped in a function
whose whole body is the lookup. For site 3, use the fallback the table already declares —
read `STATUS_META`'s keys first. If no unknown-ish entry exists, do NOT invent a new visual
state: record the finding, choose the least-surprising existing entry, and say so in the
docblock. Note that this makes an unrecognised status render an honest unknown rather than
crash, which is the Phase 188 lesson (*an unrecognised wire status renders "State unknown",
never "Complete"*) — it changes no shipped state, because no shipped status is a prototype key.

Sites 4 and 5 fit Form B with a LOCAL guard (a small module-private helper in
`PhaseNodeCard.tsx`, or an inline `hasOwnProperty.call` ternary). Do not export or generalise
`runVocabulary.ts`'s `own()` — that file carries its own `?raw` fences.

Correct the two docblocks that claim a totality the expression did not provide — site 4's
"Total by construction" and any sibling comment making the same claim at site 5 or site 2 —
in the house `⚠` register: state what was claimed, state the measured truth, name the
falsification that now keeps the claim honest. This is part of the fix, not optional prose.

Change nothing else in these three files. No badge code, no geometry, no styling. A hunk near
`PhaseNodeCard.tsx`'s max-2 `BadgeSlots` tuple is a red flag — SC#4 requires a third badge to
remain a typecheck error, and this phase does not touch that tuple.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/PhaseNode.test.tsx src/components/workflows/PhaseNodeCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- The command above exits 0 with **0 failed**: the five WR-04 falsifications flipped RED → GREEN, and every previously-passing test in those suites still passes — including `PhaseNodeCard.test.tsx`'s "paints the resolved tint it is handed, never one it looked up" block with all three positive controls.
- `grep -c "Object.prototype.hasOwnProperty.call" frontend/src/components/workflows/PhaseNode.tsx frontend/src/components/workflows/PhaseNodeCard.tsx frontend/src/components/panel/PhaseCard.tsx` sums to **≥ 5**.
- `grep -rn "Object.hasOwn" frontend/src` returns **no match** — no second spelling entered the tree.
- `grep -n "ICON_TINT\[" frontend/src/components/workflows/PhaseNode.tsx` returns **≥ 1 match** — the shipped positive control survives.
- `grep -n "Total by construction" frontend/src/components/workflows/PhaseNodeCard.tsx` returns **no match**, and the replacing text contains the literal `⚠`.
- `git diff --stat -- frontend/src/components/workflows/PhaseNodeCard.tsx` shows hunks only at the two WR-04 sites and their docblocks — no hunk near the `BadgeSlots` tuple.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports exactly **33**, preserving SC#4's "a third badge is a typecheck error" tuple untouched.
  </acceptance_criteria>

  <done>All five prototype-key lookups are total in the tree's own spelling, every falsification is green, the site-1 positive control still holds, and the false-totality docblocks carry `⚠` corrections.</done>
</task>

<task type="auto">
  <name>Task 3: Encode the run-id path segment, extend the pins, and run the phase gate battery</name>

  <files>
frontend/src/lib/api.ts
scripts/vitest-count-gate.cjs
  </files>

  <read_first>
- `frontend/src/lib/api.ts:3733-3755` — `getWorkflowRun`, its docblock and the exact fetch
  line being changed.
- `frontend/src/lib/api.ts:3053-3060` — `listRelationships`, the shipped defensive-encode
  form WITH its two-line rationale comment. This is the register to copy for a security fix.
- `frontend/src/lib/api.ts:4641` — the terser admin-route twin using the same `runId`
  variable, for comparison.
- `scripts/vitest-count-gate.cjs:40-120`, `:255-345`, `:415-460` — the LOWERED/EXTENDED
  doctrine, the pins for the four suites this plan extends, and the recorded reasoning for
  why `src/components/panel/__tests__` and `src/pages` are covered by NAMED FILES only.
  </read_first>

  <action>
Wrap `runId` in `encodeURIComponent` inside `getWorkflowRun`'s fetch path segment, and add a
two-line rationale comment above it in the `listRelationships` register naming the finding id
(WR-07) and the reason: defensive URL construction keeps a non-UUID value from reshaping the
request path, paired with the route's ownership gate. Append to the existing docblock rather
than rewriting it.

Do NOT touch the unused `signal` parameter or thread an `AbortController` — that is D4,
deferred, and it changes cancellation behaviour on a live polling surface.

Then extend the count-gate pins for all four suites this plan grew. Read each number from the
gate script's own printed `actual` column across TWO agreeing runs — never a hand count of
`it(` literals, which is unsound under `it.each` (`definitionOps.test.ts` declares ~122 and
runs 232). EXTENSIONS ONLY: raise `PhaseNode.test.tsx` from 25, `PhaseNodeCard.test.tsx` from
105, `PhaseTimeline.test.tsx` from 8 and `WorkflowRunPage.test.tsx` from 87 to their measured
actuals. Lower nothing. Edit `TARGETS` not at all — all four suites are already inside it.

Finish with the full phase gate battery and record every number.
  </action>

  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx vitest run src/pages/WorkflowRunPage.test.tsx` exits 0 with **0 failed**: the WR-07 falsification flipped RED → GREEN, and the assertion reads the URL the mock received rather than grepping source.
- `node scripts/vitest-count-gate.cjs` prints `count gate OK`, **failed 0**, total **≥ 2462**, no `[count-decrease]`, no `[missing-file]`, and pinned == actual for all four extended suites. Both runs' actual columns are recorded and agree.
- No pin was lowered and `TARGETS` is unedited: `git diff scripts/vitest-count-gate.cjs` shows only pin increases and comments.
- `grep -n "encodeURIComponent(runId)" frontend/src/lib/api.ts` returns **≥ 1 match** inside `getWorkflowRun`; `git diff -- frontend/src/lib/api.ts` shows **no** change to the `signal` parameter or to any other function.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports exactly **33**; `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors**; `cd frontend && npx vite build` exits **0** with exactly one `[INEFFECTIVE_DYNAMIC_IMPORT]` warning.
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json` reports **0 files changed** — UF-1 is documentation only and lands in Plan 05; no endpoint was re-gated.
- `bash scripts/check-deploy-drift.sh` exits **0** (no env var, seed migration, bundled service or sandbox tag moved).
  </acceptance_criteria>

  <done>`getWorkflowRun` encodes its path segment with an in-register rationale, all six falsifications are green, four pins sit at their twice-measured actuals, and the full battery is green with zero backend or package churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-supplied JSONB discriminator → client lookup table | `phaseType`, `status`, `verdict` and `reading` arrive from workflow-definition / run JSONB. A plain object literal inherits `Object.prototype`, so an author-chosen key can name an inherited member. |
| resolved lookup value → CSS / SVG sink | Site 1's value is interpolated into `radial-gradient(circle, ${tint}, …)`; site 5's into an SVG `stroke`. A function reaching either is a string-injection into a style context. |
| client-built path segment → API route | `getWorkflowRun` interpolates a caller-supplied `runId` directly into the URL path, where `/`, `?` and `#` reshape the request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188.1-01 | Tampering | the five WR-04 lookups — `PhaseNode.tsx:149`, `panel/PhaseCard.tsx:53` and `:257`, `PhaseNodeCard.tsx:396` and `:689` | mitigate | `Object.prototype.hasOwnProperty.call` guard at each (house spelling; `Object.hasOwn` has zero in-tree occurrences), site 1 in the ternary shape that preserves the `PhaseNodeCard.test.tsx:373` positive control; one falsification per site driving a `constructor` key through the real component and asserting the CONSEQUENCE, each observed RED first |
| T-188.1-01b | Information disclosure | the CSS `radial-gradient` sink at `PhaseNodeCard.tsx:738` | mitigate | Site 1's falsification asserts no rendered `style` attribute contains the substring `function` — the sink, not the lookup, is the subject |
| T-188.1-02 | Tampering | `getWorkflowRun` (`frontend/src/lib/api.ts:3751`) | mitigate | `encodeURIComponent(runId)` in the path segment, matching the 15 existing call sites; a test asserting the URL a mocked `fetch` receives for `runId = "a/b"` contains `a%2Fb` and not a bare `/a/b` |
| T-188.1-02b | Denial of service | `getWorkflowRun`'s unused `signal` parameter | transfer (deferred, D4) | Threading an `AbortController` changes request-cancellation behaviour on a live polling surface — a behaviour change under SC#2. SC#6 names only the encode. Re-open trigger: the next phase touching `WorkflowRunPage`'s fetch lifecycle, or a superseded read causing a visible defect |
| T-188.1-10 | Repudiation | a docblock claiming a totality the expression does not provide (`PhaseNodeCard.tsx:396` "Total by construction") | mitigate | Corrected in the house `⚠` register, naming the falsification that now keeps the claim honest. `soulData.ts:21-23` records what an unguarded in-code claim of a property costs: that drift survived two phases |
| T-188.1-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. Gate: `git diff --stat -- frontend/package.json frontend/package-lock.json` = 0 files changed; `bash scripts/check-deploy-drift.sh` exit 0 |

*T-188.1-03 (UF-1, `last_workflow_run_id` on the un-gated `GET /threads/{id}/workflow`) is
`accept + register`: a DOCUMENTATION deliverable, carried in Plan 05. No endpoint changes
here — re-gating it would be a backend behaviour change and is deferred as D7.*
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/PhaseNode.test.tsx src/components/workflows/PhaseNodeCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx src/pages/WorkflowRunPage.test.tsx` → 0 failed after the fixes, 6 failed before them
- `node scripts/vitest-count-gate.cjs` → `count gate OK`, failed 0, total ≥ 2462
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → exactly 33
- `cd frontend && npx eslint src/components/workflows/` → exactly 5 errors
- `cd frontend && npx vite build` → exit 0, one `[INEFFECTIVE_DYNAMIC_IMPORT]`
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json` → 0 files changed
- `bash scripts/check-deploy-drift.sh` → exit 0
</verification>

<success_criteria>
1. Five prototype-key lookups are total, in `Object.prototype.hasOwnProperty.call` form; no `Object.hasOwn` entered the tree.
2. Six falsifications were observed RED against the shipped code before any fix, and are green after.
3. `PhaseNodeCard.test.tsx:373`'s `ICON_TINT[` positive control still holds.
4. `getWorkflowRun` encodes its path segment; the `signal` half stays deferred (D4).
5. Two false-totality docblocks carry `⚠` corrections.
6. Four pins at twice-measured actuals; `tsc` 33; eslint 5; zero backend, migration or package change.
</success_criteria>

<output>
Create `.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-04-SUMMARY.md` when done. Record verbatim: the six RED failure texts, the per-site A7 audit results, the chosen guard form per site with its rationale, and both gate runs' actual columns for the four extended suites.
</output>
