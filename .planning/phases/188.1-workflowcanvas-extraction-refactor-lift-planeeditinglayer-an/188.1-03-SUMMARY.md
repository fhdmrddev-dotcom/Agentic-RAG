---
phase: 188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an
plan: 03
subsystem: frontend
tags: [refactor, g-5-extraction, workflow-canvas, esm-cycle-fence, raw-source-fence, count-gate, react-refresh]

# Dependency graph
requires:
  - phase: 188.1
    plan: 01
    provides: "the move-invariant `canvasSubtreeSource` fence whose path list already named both destination modules — the only reason this 311-line move cannot narrow a guard"
  - phase: 188.1
    plan: 02
    provides: "`AFFORDANCE_SHAPE_BASELINE`, the 12-entry captured DOM literal this move had to stay deep-equal to"
provides:
  - "`frontend/src/components/workflows/editAffordance.ts` — the geometry table + three pure helpers, as a leaf module"
  - "`frontend/src/components/workflows/PlaneEditingLayer.tsx` — the `＋`/`✕`/picker layer, exporting the component and its props TYPE and no runtime value"
  - "A `WorkflowCanvas.tsx` shell 301 lines lighter (1593 → 1292), with the moved code imported rather than declared"
  - "A machine-enforced ESM-cycle fence with inline positive controls, observed RED against a deliberate back-import"
  - "The 188.1-01 subtree fence observed RED against a violation planted INSIDE the moved code — the measurement that closes SC#5"
  - "A criterion-24 focusable-token control that reads the extracted layer as well as the canvas"
affects: [188.1-04, 188.1-05, 189]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two-commit extraction: create the modules additively FIRST, so the second commit's diff is a pure deletion and `git diff --numstat` alone proves 'move, not rewrite'"
    - "Split a component extraction into `.tsx` (component + types) and `.ts` (runtime constants) so `react-refresh/only-export-components` is eliminated rather than relocated"
    - "State the ESM-cycle constraint as ONE regex over `from \"…<specifier>\"` — it covers static, re-export and `import type` in a single line; dynamic `import()` needs its own"

key-files:
  created:
    - frontend/src/components/workflows/editAffordance.ts
    - frontend/src/components/workflows/PlaneEditingLayer.tsx
  modified:
    - frontend/src/components/workflows/WorkflowCanvas.tsx
    - frontend/src/components/workflows/FlowEdge.tsx
    - frontend/src/components/workflows/FlowEdge.test.tsx
    - frontend/src/components/workflows/WorkflowCanvas.test.tsx
    - scripts/vitest-count-gate.cjs

key-decisions:
  - "The `PlaneEditingLayer` import was placed between `phaseVocabulary` and `ProblemsTray`, not between `PhaseNode` and `phaseVocabulary` as PATTERNS.md suggested — the shipped block is strictly case-insensitive alphabetical by path and `phasevocabulary` < `planeeditinglayer` < `problemstray`"
  - "The G-5 flag block at `WorkflowCanvas.tsx:33` was REWRITTEN IN PLACE (⚠ register) rather than appended to, because the plan's own acceptance criterion requires zero `EDIT_AFFORDANCE` occurrences in the file and the flag spelled the token"
  - "The mis-placed `insertPointX` docblock moved misplaced (D8), and every `:NNN` reference inside the moved docblocks was left pointing at the pre-move canvas — both recorded in a ⚠ paragraph in each new module rather than silently corrected"
  - "`FlowEdge.tsx`'s duplication of `GAP`/`INSERT_Y` and its drift pin STAY (D2); only the stated cycle REASON was marked superseded"

patterns-established:
  - "Pattern 1: prove an extraction did not narrow a fence by planting a forbidden token in the NEW file and watching the OLD fence go red"
  - "Pattern 2: a cycle fence needs a fourth assertion nobody asks for — that the edge EXISTS and points one way — or three green negatives are also satisfied by two modules nobody imports"

requirements-completed: []  # ROADMAP §188.1: "none — this is a refactor phase." Traceability is to SC#1-#5 only.

# Metrics
duration: 55min
completed: 2026-08-06
---

# Phase 188.1 Plan 03: The extraction — Summary

Lifted `PlaneEditingLayer` and the `EDIT_AFFORDANCE` geometry table out of the largest file in the
workflow tree into two new modules as a **verbatim move** — `WorkflowCanvas.tsx` 1593 → 1292 L at
22 insertions / 323 deletions — and proved it changed nothing by keeping Plan 02's captured DOM
baseline deep-equal, keeping `tsc` unmoved at 33, and observing BOTH guards that protect the moved
code go RED against deliberate violations.

## What Was Built

**Task 1 — the two modules, additively** (commit `14917821`, 432 insertions / **0 deletions**)

`editAffordance.ts` (167 L) and `PlaneEditingLayer.tsx` (265 L) were created with the moved code
pasted byte-for-byte and `WorkflowCanvas.tsx` **untouched** — so the tree briefly held the code
twice, which is what makes Task 2's diff a pure deletion.

Fidelity was checked mechanically, not by eye: `sed -n '346,413p;486,524p' WorkflowCanvas.tsx`
piped through the four `export` additions diffs against the new file with **only** those four lines
and one blank separator differing; `sed -n '526,728p'` against `PlaneEditingLayer.tsx` is
**byte-identical, zero diff lines**.

| Symbol | From | To | Export |
|---|---|---|---|
| `EDIT_AFFORDANCE` | `WorkflowCanvas.tsx:365` | `editAffordance.ts` | already exported |
| `REVEAL_ON_HOVER` | `:412` | `editAffordance.ts` | added |
| `verticalOffsetFor` | `:506` | `editAffordance.ts` | added |
| `insertPointX` | `:518` | `editAffordance.ts` | added |
| `PlaneEditingLayerProps` | `:526` | `PlaneEditingLayer.tsx` | added (a TYPE) |
| `PlaneEditingLayer` | `:569` | `PlaneEditingLayer.tsx` | added |

D-01's mechanical corollary holds: `grep "export const|export let|export var"` on the `.tsx`
returns **no match**. Both modules carry the house `STATE OF THE EXTRACTION — read this literally`
paragraph naming the origin, the hard cut, the absent shim and the guard that keeps the claim
honest. The mis-placed `insertPointX` docblock moved **misplaced** (D8), and a ⚠ paragraph in each
module records that the `:NNN` references inside the moved docblocks address the PRE-MOVE canvas.

**Task 2 — the cut** (commit `718ccd48`, 41 insertions / 324 deletions across three files)

Regions A (`:346-413`) and B (`:486-728`) deleted in full — 313 lines including the two blank
separators. Exactly three import specifiers dropped (`ViewportPortal`, `useStore as useFlowStore`,
`CANVAS_LAYOUT`) after re-deriving every usage with a grep; exactly one added. The JSX site and its
`184-12` comment are byte-identical. `FlowEdge.test.tsx`'s import was re-pointed as a **separate
added statement**, no assertion touched. Two ⚠ prose corrections landed: the G-5 flag marked
DISCHARGED with the measured before/after, and `FlowEdge.tsx`'s cycle REASON marked superseded while
the duplication and its drift pin stay by decision (D2).

**Task 3 — the fences and the battery** (commit `e92224c2`, 102 insertions / 2 deletions)

A new `describe` (4 cases, an extension — no shipped test touched) in `WorkflowCanvas.test.tsx`
forbids either new module from naming a `WorkflowCanvas` specifier in any import form, asserts
`editAffordance` is a leaf (no `react`, no PascalCase sibling), and pins the one-way edge. Every
regex carries an inline positive control. A fourth case exists because three negatives are also
satisfied by two modules nobody imports.

`FlowEdge.test.tsx`'s criterion-24 focusable-token control now reads `planeEditingLayerSource` in
addition to the canvas. **Measured:** the click-handler spelling fell **4 → 2** in
`WorkflowCanvas.tsx` and appears **twice** in the new file — the canvas half was about to pass over
less code, which is Phase 188's F7 lesson in a new dress.

## Measured Evidence (verbatim)

### Line count and the numstat that proves it is a MOVE

```
before: 1593  (measured at HEAD eff79154, not the ROADMAP's stale 1574)
after:  1292
git diff --numstat -- frontend/src/components/workflows/WorkflowCanvas.tsx
22      323     frontend/src/components/workflows/WorkflowCanvas.tsx
```

22 insertions (< 25 ✔), 323 deletions (≥ 300 ✔), 1292 < 1350 ✔.

### Typecheck — unmoved at 33, and the TS6133 set is byte-identical to baseline

```
npx tsc --noEmit -p tsconfig.app.json  →  33 errors, at every stage:
  baseline (HEAD eff79154)      33
  after Task 1 (both modules)   33
  after Task 2 (the cut)        33
  after Task 3 (final)          33
lines naming WorkflowCanvas / PlaneEditingLayer / editAffordance:  0
```

### eslint — D-01's win, 6 → 5

```
BEFORE (HEAD):                          AFTER:
BuilderStoreProvider.tsx:45,:53,:78     BuilderStoreProvider.tsx:45,:53,:78
FlowEdge.tsx:135                        FlowEdge.tsx:147   (same error, shifted +12 by the ⚠ para)
StepTypePicker.test.tsx:561             StepTypePicker.test.tsx:561
WorkflowCanvas.tsx:365                  — GONE
✖ 6 problems (6 errors, 0 warnings)     ✖ 5 problems (5 errors, 0 warnings)
```

### The three canvas suites, including Plan 02's baseline — 128/128

```
 Test Files  3 passed (3)
      Tests  128 passed (128)
```

`AFFORDANCE_SHAPE_BASELINE` was **not edited** — `git diff` on
`WorkflowCanvas.editing.test.tsx` across this whole plan is empty. SC#2 is a measurement.

### RED OBSERVED #1 — the subtree fence bites on the MOVED code (T-188.1-04, the headline)

Appended `// 188.1-03 TEMPORARY RED PROBE — grounding_mode` to `PlaneEditingLayer.tsx`:

```
 × the canvas still names no validation seam and no Phase-185 authoring field
 × makes no network call and reads no Phase-185 authoring field
FAIL src/components/workflows/WorkflowCanvas.composition.test.tsx > WorkflowCanvas composition — the scope fences still hold > the canvas still names no validation seam and no Phase-185 authoring field
AssertionError: expected '/**\n * Phase 183-06 Task 2 (CANVAS-0…' not to match /grounding_mode/
FAIL src/components/workflows/WorkflowCanvas.test.tsx > WorkflowCanvas — the scope fences (source guard) > makes no network call and reads no Phase-185 authoring field
AssertionError: expected '/**\n * Phase 183-06 Task 2 (CANVAS-0…' not to match /grounding_mode/
      Tests  2 failed | 70 passed (72)
```

Reverted with `git checkout`; `grep -c grounding_mode PlaneEditingLayer.tsx` → **0**, `git status`
clean. **This is the observation that converts "no loss of coverage" from a claim into a
measurement** — before Plan 01 the fence read one file, and it is now KNOWN to read the moved 311
lines.

### RED OBSERVED #2 — the ESM-cycle fence, against a deliberate back-import (T-188.1-05)

Appended `import { WorkflowCanvas } from "./WorkflowCanvas"` to `editAffordance.ts`:

```
 × neither extracted module names a WorkflowCanvas specifier in ANY import form
FAIL src/components/workflows/WorkflowCanvas.test.tsx > WorkflowCanvas 188.1-03 — the extracted modules cannot import back (SC#3) > neither extracted module names a WorkflowCanvas specifier in ANY import form
AssertionError: expected '/**\n * Phase 188.1-03 Task 1 (D-01) …' not to match /from\s+["'][^"']*WorkflowCanvas["']/
      Tests  1 failed | 51 passed (52)
```

Reverted; `git diff --stat HEAD` for the four source files is empty afterwards.

### `vite build` — exit 0, no circular-dependency warning, warning count UNCHANGED

```
✓ built in 4.79s          (exit 0)
grep -c "INEFFECTIVE_DYNAMIC_IMPORT"  →  2   BEFORE (pre-move tree, measured this session)
grep -c "INEFFECTIVE_DYNAMIC_IMPORT"  →  2   AFTER
grep -ci "circular"                   →  0   AFTER
```

Both warnings are the same two at both ends: `ShikiCode.tsx` (chat subtree, untouched by this
phase) and `WorkflowCanvas.tsx` (the `WorkflowRunPage` static import D-02 deliberately leaves
alone). See Deviation 3 — the plan's "exactly ONE" was an inherited, unmeasured figure.

### Both gate runs BEFORE the pin was written — the two `actual` columns agree

```
  WorkflowCanvas.editing.test.tsx              58      58       0
  WorkflowCanvas.test.tsx                      48      52      +4
  FlowEdge.test.tsx                            22      22       0
  WorkflowCanvas.composition.test.tsx          20      20       0
  total                                      2466    2470      +4
  total 2470  ·  failed 0  ·  pinned total 2466
count gate OK — 45/45 pinned files present, no per-file decrease, 0 failing.
```

(identical output on run 2)

**Pin written:** `"WorkflowCanvas.test.tsx": 52` (was 48, **> 48** ✔). Never lowered.

### Gate AFTER the extension — pinned equals actual, zero slack

```
  WorkflowCanvas.editing.test.tsx              58      58       0
  WorkflowCanvas.test.tsx                      52      52       0
  FlowEdge.test.tsx                            22      22       0
  WorkflowCanvas.composition.test.tsx          20      20       0
  total                                      2470    2470       0
  total 2470  ·  failed 0  ·  pinned total 2470
count gate OK — 45/45 pinned files present, no per-file decrease, 0 failing.
```

No `[count-decrease]`, no `[missing-file]`, `failed 0`, total 2470 ≥ 2462 ✔. `git diff` on the gate
script shows **no `TARGETS` edit** (the single `TARGETS` match in the diff is a context line, ` const
TARGETS = [`, with a leading space) and **no pin lowered** — the one `-` pin line is 48 being raised
to 52.

### Scope

```
git diff --stat -- backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json
→ 0 files changed
```

Zero packages installed. `git diff --diff-filter=D` on all three commits → empty.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — blocking, plan-internal contradiction] `grep -c "ViewportPortal\|useFlowStore\|CANVAS_LAYOUT" WorkflowCanvas.tsx` cannot return 0 while the JSX-site comment stays byte-identical**

- **Found during:** Task 2, checking the acceptance criteria
- **Issue:** The plan requires that grep to return **0**, and — three paragraphs earlier — that the
  JSX site *"and its `184-12` comment"* be left **byte-identical**. That comment reads *"drawn
  through `<ViewportPortal>` so it lives on the plane"*. A second prose mention of `CANVAS_LAYOUT`
  sits in the `resolveDrop` docblock at `:684`, which is not in either moved region. The two
  instructions cannot both be satisfied.
- **Fix:** Read the criterion at its intent — **no CODE use remains** — and discharged it with a
  comment-stripping check rather than a raw grep. Measured, with `/* */`, `//` and `{/* */}`
  stripped: `ViewportPortal` **0**, `useFlowStore` **0**, `CANVAS_LAYOUT` **0**, and also
  `EDIT_AFFORDANCE` / `REVEAL_ON_HOVER` / `insertPointX` / `verticalOffsetFor` **0**. The two raw
  matches are prose, one of them the comment the plan orders preserved. The stronger property the
  criterion was reaching for — *"a leftover becomes a NEW typecheck error under `noUnusedLocals`"* —
  is independently green: `tsc` is unmoved at 33 with a TS6133 set byte-identical to baseline.
- **Files modified:** none (a verification-method correction)
- **Commit:** `718ccd48`

**2. [Rule 3 — blocking, plan-internal contradiction] The G-5 flag block could not be both "kept and marked done" and free of the literal `EDIT_AFFORDANCE`**

- **Found during:** Task 2
- **Issue:** The plan says to mark `WorkflowCanvas.tsx:33-37` DISCHARGED (the house rule being a
  correction kept in place, never a silent rewrite), and separately requires
  `grep -n "EDIT_AFFORDANCE" WorkflowCanvas.tsx` → **no match**. The flag block spells the token.
  Appending a correction beneath the untouched paragraph satisfies the first and fails the second.
- **Fix:** The same resolution Plan 02 reached for the identical shape — the shipped
  `⚠ REWRITTEN BY` register at `WorkflowCanvas.tsx:313`. The block is REWRITTEN IN PLACE, opens by
  stating what it used to claim in paraphrase ("named `PlaneEditingLayer` and the editing-geometry
  table as *the natural extraction … already self-contained*", flagged for Phase 188), says why the
  reasoning is kept rather than deleted, and then records the result with the measured 1593 → 1292.
  The literal is gone; the flag's argument is not.
- **Files modified:** `frontend/src/components/workflows/WorkflowCanvas.tsx`
- **Commit:** `718ccd48`

**3. [Rule 1 — an inherited claim was false] The `[INEFFECTIVE_DYNAMIC_IMPORT]` baseline is TWO warnings, not one**

- **Found during:** Task 3
- **Issue:** The plan's gate baseline and acceptance criterion both state *"exactly ONE
  `[INEFFECTIVE_DYNAMIC_IMPORT]` warning — the pre-existing `WorkflowRunPage` one"*. Measured after
  the move: **two**. Taken literally that reads as a regression this plan introduced.
- **Fix:** Measured the baseline instead of arguing it. Restored `WorkflowCanvas.tsx` and
  `FlowEdge.tsx` from `eff79154`, removed the two new modules, ran `vite build`: **2 warnings**, the
  same two (`ShikiCode.tsx` and `WorkflowCanvas.tsx`). Restored the tree exactly — `git diff --stat
  HEAD` for all four files empty afterwards. So the count is **2 before, 2 after, unchanged**, which
  is the property D-02 actually wants pinned; the plan's figure came from research, which counted
  only the `WorkflowCanvas` warning it cared about. The `ShikiCode` warning lives entirely in
  `components/chat/` + `components/panel/`, subtrees this phase does not touch.
- **Files modified:** none
- **Commit:** `e92224c2` (recorded in the commit body)

**4. [Rule 2 — a fence would have been satisfiable by dead code] The cycle fence needed a fourth case asserting the edge EXISTS**

- **Found during:** Task 3, writing the fence
- **Issue:** The plan specifies three negatives (no static import back, no dynamic import back,
  `editAffordance` is a leaf). All three are equally satisfied by two modules that nobody imports —
  including by a `WorkflowCanvas.tsx` that kept its own copy and a re-export shim.
- **Fix:** Added a fourth case pinning the direction: the canvas imports `{ PlaneEditingLayer }`
  from the alias path, does **not** contain `function PlaneEditingLayer(`, and does **not** contain
  `EDIT_AFFORDANCE` (the no-shim assertion, now machine-checked rather than left to the SUMMARY).
- **Files modified:** `frontend/src/components/workflows/WorkflowCanvas.test.tsx`
- **Commit:** `e92224c2`

### Not deviations, recorded so the next reader does not re-derive them

- **The plan's `<interfaces>` inventory was accurate at every line it named** — regions, symbol line
  numbers, the three removable imports, the five that must survive, and the `CANVAS_LAYOUT`
  correction about `PlaneEditingLayer`'s import list. Nothing in it was stale.
- **`PATTERNS.md`'s suggested import slot is wrong.** It says to place the `PlaneEditingLayer`
  import *"between `PhaseNode` at `:211` and `phaseVocabulary` at `:212`"*. The shipped alias block
  is strictly case-insensitive alphabetical by path, and `phasevocabulary` < `planeeditinglayer` <
  `problemstray`, so the correct slot is between `phaseVocabulary` and `ProblemsTray`. The plan's
  own wording (*"placed to keep the alias block alphabetised by path"*) is what was followed.
- **`FlowEdge.tsx`'s eslint error moved `:135` → `:147`.** Same error, same rule, same export; the
  ⚠ correction paragraph above it is 12 lines long. Not a new finding.
- **The tree is LF on disk**, not CRLF (`cat -A` shows no `^M`), with `core.autocrlf=true` and no
  `.gitattributes`. The new modules were written LF to match.

## Threat Model Dispositions

| Threat ID | Disposition | Outcome |
|---|---|---|
| T-188.1-05 | mitigate | **Closed.** A `?raw` fence forbids both new modules from naming a `WorkflowCanvas` specifier in any static, re-export, type-only or dynamic form; four inline positive controls; observed RED against a deliberate back-import. `vite build` exits 0 with zero circular-dependency warnings. |
| T-188.1-04 | mitigate | **Closed by measurement.** `grounding_mode` planted in `PlaneEditingLayer.tsx` turned BOTH subtree fences red. The Plan 01 fence provably reads the moved 311 lines. |
| T-188.1-06 | mitigate | **Closed.** `AFFORDANCE_SHAPE_BASELINE` still deep-equals with the file untouched; 128/128 across the three canvas suites; the whole 2470-test gate green. Operator rows M1-M3 (Plan 05) still cover what jsdom cannot see. |
| T-188.1-08 | mitigate | **Closed.** `grep -nE "grounding_mode\|workflows/validate\|MiniMap\|hideAttribution\|severity\|phase_index\|phaseIndex"` over both new modules → no match; the seven `READING_WORDS` → no match; `phase_index` in `WorkflowCanvas.tsx` still exactly **1**. |
| T-188.1-09 | accept | **Accepted as planned.** The move is verbatim and the layer's ten props are unchanged; the re-scoped `@/lib/phaseState` and `runVocabulary` negatives now cover both new modules, and `editAffordance`'s leaf assertion adds a second, stronger fence on that module. |
| T-188.1-SC | mitigate | **Clean.** `git diff --stat -- frontend/package.json frontend/package-lock.json` → **0 files changed**. Zero packages installed. |

## Verification Results

| Check | Required | Measured | ✔ |
|---|---|---|---|
| `npx tsc --noEmit -p tsconfig.app.json` | exactly 33, no NEW TS6133/TS6192 | **33**, TS6133 set byte-identical to baseline (4, all pre-existing, all outside the canvas) | ✔ |
| `node scripts/vitest-count-gate.cjs` | `count gate OK`, failed 0, total ≥ 2462, no `[count-decrease]`, no `[missing-file]` | `count gate OK`, failed 0, **2470/2470**, 45/45 | ✔ |
| `npx eslint src/components/workflows/` | exactly 5 errors, the five named | **5**, exactly those five | ✔ |
| `npx vite build` | exit 0, no circular warning, warning count pinned | exit 0, 0 circular, `INEFFECTIVE_DYNAMIC_IMPORT` **2 → 2** | ✔ |
| `wc -l WorkflowCanvas.tsx` | < 1350 | **1292** (before **1593**) | ✔ |
| `git diff --numstat` on that file | ≥ 300 del, < 25 ins | **22 / 323** | ✔ |
| Three canvas suites | 0 failed, baseline unedited | **128 passed / 0 failed**, `WorkflowCanvas.editing.test.tsx` diff empty | ✔ |
| `grep -c "^export" editAffordance.ts` | 4 | **4** | ✔ |
| `grep "export const\|let\|var" PlaneEditingLayer.tsx` | no match | no match | ✔ |
| `grep "EDIT_AFFORDANCE" WorkflowCanvas.tsx` | no match | no match | ✔ |
| `grep -c "phase_index" WorkflowCanvas.tsx` | exactly 1 | **1** | ✔ |
| `grep -c "StepTypePicker" WorkflowCanvas.tsx` | ≥ 2 | **2** | ✔ |
| `git diff --stat -- backend/ supabase/migrations/ frontend/package.json` | 0 files | **0 files** | ✔ |
| `git diff scripts/vitest-count-gate.cjs` | no `TARGETS` edit, nothing lowered | no `TARGETS` line changed; the one `-` pin is 48 raised to 52 | ✔ |

## Plan Success Criteria

1. Two modules per D-01; the `.tsx` exports no runtime constant; eslint 6 → 5 — **met**
2. `WorkflowCanvas.tsx` measurably smaller and the diff reads as a move — **met** (1593 → 1292, 22/323)
3. ESM-cycle constraint is a test with positive controls, observed RED — **met** (RED #2)
4. The subtree fence observed RED against a violation inside the moved code — **met** (RED #1)
5. `tsc` unmoved at 33; gate green; captured baseline still equal; `vite build` clean — **met**
6. No re-export shim; no relocated test; no package churn; no backend/migration change — **met**

## Known Stubs

None. Nothing was stubbed — this plan moved shipped code and added no surface.

## Deferred Issues

None new. Plan 02's pre-existing `WorkflowRunPage.test.tsx:504` flake (logged in the phase's
`deferred-items.md`) did **not** fire in any of the four gate runs this plan performed.

## Threat Flags

None. This plan created no network endpoint, no auth path, no file-access pattern and no schema
change; both new modules are pure client-side presentation and geometry.

## For the Next Plan (188.1-04 / 188.1-05)

- **The count gate is at zero slack on all four canvas suites** (52/52, 58/58, 20/20, 22/22) and a
  whole-gate total of **2470/2470**. Any movement is a real change and must be re-measured from the
  gate's own `actual` column across two agreeing runs.
- **`AFFORDANCE_SHAPE_BASELINE` remains the SC#2 gate** and remains un-recaptured. It survived the
  move untouched, which is the single strongest piece of evidence this phase has.
- **`FlowEdge.tsx`'s eslint error is now at `:147`**, not `:135`. Any document naming `:135` is
  stale from this commit forward.
- **The `[INEFFECTIVE_DYNAMIC_IMPORT]` pin is TWO, not one** — see Deviation 3. A plan or
  verification that asserts one will fail on a tree that is behaving correctly.
- **WR-04 (Plan 04) must use `Object.prototype.hasOwnProperty.call(...)`**, not `Object.hasOwn`, and
  must preserve the literal `ICON_TINT[` form in `PhaseNode.tsx` — `PhaseNodeCard.test.tsx:373` is a
  positive control that the obvious fix turns red. Unchanged by this plan, restated because it is
  the next landmine.

## Commits

| Task | Commit | Message |
|---|---|---|
| 1 | `14917821` | `refactor(188.1-03): add editAffordance.ts and PlaneEditingLayer.tsx additively` |
| 2 | `718ccd48` | `refactor(188.1-03): cut both regions out of the canvas shell and re-point the one importer` |
| 3 | `e92224c2` | `test(188.1-03): fence the ESM cycle, widen the focusable control, extend the pin` |

## Self-Check: PASSED

- `frontend/src/components/workflows/editAffordance.ts` — FOUND
- `frontend/src/components/workflows/PlaneEditingLayer.tsx` — FOUND
- `frontend/src/components/workflows/WorkflowCanvas.tsx` — FOUND (1292 L)
- `frontend/src/components/workflows/FlowEdge.tsx` — FOUND
- `frontend/src/components/workflows/FlowEdge.test.tsx` — FOUND
- `frontend/src/components/workflows/WorkflowCanvas.test.tsx` — FOUND
- `scripts/vitest-count-gate.cjs` — FOUND
- commit `14917821` — FOUND
- commit `718ccd48` — FOUND
- commit `e92224c2` — FOUND
