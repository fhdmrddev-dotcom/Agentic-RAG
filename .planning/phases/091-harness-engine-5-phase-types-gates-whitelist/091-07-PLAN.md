---
phase: 091-harness-engine-5-phase-types-gates-whitelist
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - supabase/migrations/061_harness_seed_templates.sql
  - supabase/full-schema.sql
  - backend/tests/test_harness_templates.py
  - backend/tests/test_harness_reachability.py
autonomous: false
requirements: [HARNESS-07]
must_haves:
  truths:
    - "4 seed workflow templates ship as a numbered SQL seed migration (061), is_global=true, status='published', created_by the seed system user"
    - "The 4 definitions collectively exercise all 5 phase types end-to-end (SC#1): Research→Summarize (llm_agent+llm_single), Plan→Execute→Verify (llm_single+llm_agent+gate), Literature review (programmatic+llm_batch_agents+llm_single), Doc Q&A (llm_agent+llm_human_input+llm_single)"
    - "Each seed's definition JSONB parses via WorkflowDefinition.model_validate and lints clean (reachability)"
    - "Each seed runs end-to-end through the engine with mocked LLM substrate (integration), proving the dispatch + transition + gate + completion path"
    - "Migration 061 is applied via the Supabase SQL editor (operator checkpoint — NEVER db push/reset) and full-schema.sql is regenerated"
  artifacts:
    - path: "supabase/migrations/061_harness_seed_templates.sql"
      provides: "4 INSERT INTO workflow_definitions seed rows (idempotent, ON CONFLICT DO NOTHING)"
      contains: "INSERT INTO workflow_definitions"
    - path: "backend/tests/test_harness_templates.py"
      provides: "end-to-end-per-seed (mocked LLM) + parse + 5-type-coverage assertions"
      contains: "test_each_seed_runs_end_to_end"
  key_links:
    - from: "supabase/migrations/061_harness_seed_templates.sql"
      to: "backend/app/models/harness.py (WorkflowDefinition)"
      via: "the definition JSONB parses via model_validate"
      pattern: "phase_type"
---

<objective>
Ship the 4 seed workflow templates (HARNESS-07) as migration 061 — global, published, immutable — that double as the UAT fixtures for SC#1/#6 and collectively exercise all 5 phase types end-to-end. Prove each runs through the now-complete engine with mocked LLM substrate, apply the migration via the operator SQL-editor checkpoint, and regenerate the bootstrap artifact.

Purpose: HARNESS-07 (2-3+ seed templates as the end-to-end exercisers/UAT fixtures). D-01 ships all 4; D-02 keeps the 2 PRIMARY showcases must-ship; D-03 is_global=true + seed/JSONB. This is the integration capstone that proves Plans 02-06 compose.

Output: `061_harness_seed_templates.sql` (4 seeds), regenerated `full-schema.sql`, the end-to-end integration tests flipped live.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-CONTEXT.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-02-SUMMARY.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-03-SUMMARY.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-04-SUMMARY.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-05-SUMMARY.md

<interfaces>
<!-- workflow_definitions row shape (migration 056 seed precedent at :127-151, RESEARCH-verified):
     INSERT INTO workflow_definitions (id, slug, version, name, status, definition, created_by, is_global)
     status='published'  (block-published trigger then freezes it — 090 D-04)
     created_by = '00000000-0000-0000-0000-000000000001'  (seed system user, created idempotently in 056)
     is_global = true  (RLS forbids users self-setting; only seed migrations — 056:55-57 + 060)
     definition = full WorkflowDefinition JSONB: {slug, version, name, status, phases:[{slug, phase_index, config:{phase_type,...}, validators:[...]}]}
     ON CONFLICT (id) DO NOTHING  (idempotent re-apply) -->
<!-- Next migration number = 061 (head = 060_harness_update_with_check.sql; VERIFIED) -->
<!-- Migration discipline (CLAUDE.md): author SQL → operator pastes into Supabase SQL editor (NEVER db push/reset)
     → bash scripts/regenerate-full-schema.sh (live-DB dump, no reset) → commit both. -->
<!-- The 4 seeds (D-01) and their phase types:
     research_summarize:  [llm_agent(search KB+web), llm_single(cited summary)]                    -> llm_agent, llm_single
     plan_execute_verify: [llm_single(plan), llm_agent(execute), llm_single(verify behind a gate)] -> + gates
     literature_review:   [programmatic(split_topic), llm_batch_agents(parallel), llm_single(merge)]-> programmatic, llm_batch_agents
     doc_qa_human:        [llm_agent(draft), llm_human_input(pause), llm_single(finalize)]          -> llm_human_input
   Union across all 4 = all 5 phase types (SC#1). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 4 seed WorkflowDefinitions + the migration SQL (061)</name>
  <read_first>
    - supabase/migrations/056_*.sql (the seed-row precedent at :127-151 — the exact INSERT column set, the seed system user creation, is_global, ON CONFLICT)
    - backend/app/models/harness.py (Plan 01 — the FINALIZED WorkflowDefinition/PhaseSpec/PhaseConfig/ValidatorSpec field names the definition JSONB must match exactly so model_validate parses)
    - backend/app/services/harness/programmatic.py (Plan 03 — the `split_topic` fn name the literature_review programmatic phase references)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Pattern 8 (seed migration shape) + decisions D-01/D-02/D-03/D-10 (terminal phase is user-facing)
  </read_first>
  <action>
    First author the 4 definition JSONB blobs as Python dicts in a fixture (reuse the conftest `four_seed_defs()` from Plan 01 — make it the SINGLE source so the migration SQL and the tests share identical definitions). Each MUST parse via `WorkflowDefinition.model_validate` and lint clean. Author terminal-by-design (D-10 — the last phase output is the chat answer):

    1. `research_summarize` (PRIMARY) — phase 0 `llm_agent` (prompt: search KB+web; available_tools: ["search_documents","web_search"]); phase 1 `llm_single` (prompt: write a cited summary of the prior findings) — TERMINAL, user-facing.
    2. `plan_execute_verify` (PRIMARY) — phase 0 `llm_single` (plan); phase 1 `llm_agent` (execute; available_tools include execute_code/write tools); phase 2 `llm_single` (verify) with a `regex_match` or `json_schema` validator gate (on_failure: "retry", max_retries: 2) — TERMINAL.
    3. `literature_review` (COVERAGE) — phase 0 `programmatic` (fn: "split_topic", input_keys: ["topic"]); phase 1 `llm_batch_agents` (available_tools: ["search_documents"], max_parallel_agents: 5, merge_strategy: "concat_numbered"); phase 2 `llm_single` (merge into a review) — TERMINAL. Sole exerciser of programmatic + llm_batch_agents.
    4. `doc_qa_human` (COVERAGE) — phase 0 `llm_agent` (draft an answer; available_tools: ["search_documents"]); phase 1 `llm_human_input` (prompt: "Does this draft answer your question? Add corrections."; options: ["Looks good","Needs changes"]); phase 2 `llm_single` (finalize using the human input) — TERMINAL. Sole exerciser of llm_human_input.

    NO v1 seed uses an auto-fallback recovery `skip_to_phase` (deferred — the mechanism ships but seeds don't use it). NO `final_output` knob (D-11).

    Then create `supabase/migrations/061_harness_seed_templates.sql`:
    - A header comment: migration intent + the CLAUDE.md apply rule (SQL editor, never db push/reset).
    - Re-assert the seed system user `'00000000-0000-0000-0000-000000000001'` with `ON CONFLICT DO NOTHING` (mirror 056) so 061 is self-contained.
    - 4 `INSERT INTO workflow_definitions (id, slug, version, name, status, definition, created_by, is_global) VALUES (...)` rows, each with a FIXED uuid (so ON CONFLICT (id) DO NOTHING is idempotent), `status='published'`, `is_global=true`, `created_by` the seed user, and the `definition` as the JSONB blob (use `'<json>'::jsonb`). The JSONB MUST be byte-equal in shape to the Python `four_seed_defs()` (the test asserts parity).
    - `ON CONFLICT (id) DO NOTHING` on each.
    Do NOT author a `supabase db push` step anywhere. Do NOT hand-edit full-schema.sql in this task.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_templates.py -k "parse or 5_types or lint" tests/test_harness_reachability.py -k seeds -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ls supabase/migrations/061_harness_seed_templates.sql` exists
    - `grep -c "INSERT INTO workflow_definitions" supabase/migrations/061_harness_seed_templates.sql` is ≥ 4
    - `grep -c "ON CONFLICT" supabase/migrations/061_harness_seed_templates.sql` is ≥ 4 (idempotent)
    - `grep -n "is_global" supabase/migrations/061_harness_seed_templates.sql` shows true on each
    - `grep -n "00000000-0000-0000-0000-000000000001" supabase/migrations/061_harness_seed_templates.sql` matches (seed user)
    - `grep -n "db push\|db reset" supabase/migrations/061_harness_seed_templates.sql` returns NOTHING
    - `grep -cE "programmatic|llm_single|llm_agent|llm_batch_agents|llm_human_input" supabase/migrations/061_harness_seed_templates.sql` covers ALL 5 phase types
    - test_harness_templates.py `test_each_seed_parses_via_model_validate` + `test_seeds_cover_all_5_phase_types` (live) pass; test_harness_reachability.py `test_4_seeds_lint_clean` passes
  </acceptance_criteria>
  <done>4 seed definitions authored (shared with the test fixture), collectively covering all 5 phase types, parsing via model_validate and linting clean; migration 061 inserts them idempotently as global+published seed rows; no db push/reset anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration test per seed (mocked LLM substrate)</name>
  <read_first>
    - backend/app/services/harness_engine.py (Plan 02/05 — run_workflow + retry + completion)
    - backend/app/services/harness/phase_types.py (Plan 03 — the 5 executors; mock their substrate calls: _stream_one_iteration, run_task_sub_agent, ask_user block)
    - backend/tests/conftest.py (Plan 01 — four_seed_defs, mock_asyncpg_pool, fake_redis, make_run_context)
    - backend/tests/test_harness_templates.py (Plan 01 skeleton — the end-to-end contract to flip live)
  </read_first>
  <action>
    Flip `test_each_seed_runs_end_to_end_mocked_llm` live in `test_harness_templates.py`. For each of the 4 seeds:
    - Build the `workflow_runs` + pre-seeded `workflow_phases` (all `pending`) rows in the `mock_asyncpg_pool` (per Plan 02 batch-insert-at-run-start expectation — confirm against the migration 058 column shape).
    - Mock the LLM substrate at the executor boundary: `_stream_one_iteration` returns a canned `(content, [])`; `run_task_sub_agent` returns `{"sub_run_id": "...", "summary": "...", "status": "completed"}`; the ask_user block returns a canned answer immediately (no real pub/sub). `split_topic` runs for real (it's pure Python).
    - Run `await run_workflow(run_id, seed_def, ctx, pool=pool, redis=fake_redis)`.
    - Assert: every phase reached `status='completed'` in order (via `mock_asyncpg_pool.calls` showing the active→completed UPDATE sequence per phase_index); the final phase's output became the chat message (D-10); `fake_redis.xadds` contains `phase_started`/`phase_completed`/`phase_transition`/`run_completed` events for each phase; for `plan_execute_verify` the gate ran (gate_passed audit). 
    - For `literature_review`, assert the programmatic split_topic produced sub_questions and the batch phase fanned out (gather called N times, bounded by max_parallel_agents). For `doc_qa_human`, assert the ask_user phase blocked-then-resumed with the canned answer feeding the finalize phase.
    This is the proof that Plans 02-06 compose end-to-end on the real seed definitions.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_templates.py -x -q && cd backend && venv/Scripts/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "test_each_seed_runs_end_to_end" backend/tests/test_harness_templates.py` matches and is NOT skipped (no `pytest.mark.skip`)
    - the test parametrizes over all 4 seeds (grep shows `four_seed_defs` or a parametrize with 4 ids)
    - test asserts active→completed UPDATE ordering per phase via `mock_asyncpg_pool.calls`
    - test asserts `run_completed` in `fake_redis.xadds`
    - full suite green: `cd backend && venv/Scripts/python -m pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>All 4 seeds run end-to-end through the engine with mocked LLM substrate; ordered completion + completion-is-chat-message + SSE events + gate + batch fan-out + human-input resume all asserted; full backend suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator applies migration 061 via Supabase SQL editor + regenerate full-schema</name>
  <what-built>
    `supabase/migrations/061_harness_seed_templates.sql` — 4 global, published seed workflow templates (Research→Summarize, Plan→Execute→Verify, Literature review, Doc Q&A) authored as idempotent INSERTs. Per CLAUDE.md migration discipline, schema/seed changes are applied to the live local DB by pasting into the Supabase SQL editor — NEVER `supabase db push` / `db reset` (preserves dev data).
  </what-built>
  <how-to-verify>
    1. Open the Supabase SQL editor for the local project.
    2. Paste the entire contents of `supabase/migrations/061_harness_seed_templates.sql` and run it. It is idempotent (ON CONFLICT DO NOTHING) — safe to re-run.
    3. Verify the 4 rows landed: run `SELECT slug, version, status, is_global FROM workflow_definitions WHERE slug IN ('research_summarize','plan_execute_verify','literature_review','doc_qa_human');` — expect 4 rows, all `status='published'`, `is_global=true`.
    4. Back in the repo, regenerate the bootstrap artifact (live-DB dump, no reset):
       `bash scripts/regenerate-full-schema.sh`
       (Do NOT pass --reset.) This rewrites `supabase/full-schema.sql`.
    5. Confirm `supabase/full-schema.sql` now contains the 4 seed rows: `grep -c "research_summarize\|plan_execute_verify\|literature_review\|doc_qa_human" supabase/full-schema.sql` ≥ 4.
    6. Type "approved" when the 4 rows are present in the DB and full-schema.sql is regenerated, or describe any error.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed migration → global rows | Seed rows are is_global=true (visible to all users); only seed migrations may set is_global (RLS forbids self-setting). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-24 | Tampering | a user self-sets is_global to publish a global workflow | mitigate | RLS forbids self-setting is_global (056:55-57 + 060); only the SQL-editor superuser seed migration sets it. The published rows are then frozen by the block-published trigger (090 D-04). |
| T-091-25 | Integrity | seed JSONB drifts from the model | mitigate | the 4 definitions are a single source (conftest four_seed_defs) shared by the migration and the tests; test asserts model_validate parity + reachability lint-clean. |
| T-091-26 | Availability | accidental db reset wipes dev data | mitigate | apply is an operator SQL-editor checkpoint (autonomous:false); the migration contains no db push/reset; regen runs without --reset. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/ -q` fully green (all 091 contracts live across Plans 01-07).
- The 4 seed rows present in the live DB (operator checkpoint step 3) and in `supabase/full-schema.sql` (step 5).
- `grep -n "db push\|db reset" supabase/migrations/061_harness_seed_templates.sql` returns nothing.
</verification>

<success_criteria>
- HARNESS-07: 4 seed templates ship (global, published, immutable), collectively exercise all 5 phase types end-to-end, double as UAT fixtures.
- Each seed parses + lints clean + runs end-to-end through the engine (mocked LLM).
- Migration 061 applied via SQL editor; full-schema.sql regenerated; no db push/reset.
</success_criteria>

<output>
After completion, create `.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-07-SUMMARY.md`.
</output>
