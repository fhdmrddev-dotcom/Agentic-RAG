---
phase: 091-harness-engine-5-phase-types-gates-whitelist
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/services/tool_dispatcher.py
  - backend/app/services/openai_service.py
  - backend/app/config.py
  - backend/tests/test_harness_whitelist.py
  - backend/tests/test_tool_budget.py
autonomous: true
requirements: [HARNESS-05, TOOL-05]
must_haves:
  truths:
    - "A tool call outside the active phase whitelist is refused at dispatch_tool() with a clean tool_result carrying the matching tool_call_id and the D-04 guiding message listing available tools"
    - "When ctx.phase_whitelist is None (Deep Mode) dispatch_tool() is a literal no-op — byte-identical to pre-091 dispatch (Explorer/General untouched)"
    - "Every refusal writes a harness_audit tool_refused row (D-06)"
    - "get_tools() output filtered to the phase whitelist, then capped at the per-provider max_tools soft ceiling — whitelist tools are ALWAYS retained even if over budget"
    - "max_tools is a new ModelCapability field; absent = no cap (soft ceiling fires only for registered low-limit models, e.g. Google per SEED-035)"
  artifacts:
    - path: "backend/app/services/tool_dispatcher.py"
      provides: "ToolContext.phase_whitelist field + one guard at the top of dispatch_tool()"
      contains: "phase_whitelist"
    - path: "backend/app/services/openai_service.py"
      provides: "phase-whitelist filter + TOOL-05 budget guard at the get_tools composition site"
      contains: "max_tools"
    - path: "backend/app/config.py"
      provides: "max_tools key on ModelCapability TypedDict + Google rows populated"
      contains: "max_tools"
  key_links:
    - from: "backend/app/services/tool_dispatcher.py"
      to: "harness_audit (tool_refused)"
      via: "fire-and-forget audit write on refusal"
      pattern: "tool_refused"
    - from: "backend/app/services/openai_service.py"
      to: "MODEL_CAPABILITIES[model].max_tools"
      via: "budget guard reads the per-provider ceiling"
      pattern: "max_tools"
---

<objective>
Land the per-phase tool-whitelist enforcement (HARNESS-05) and the tool-count budget guard (TOOL-05). Both are additive and live at the ONLY two provider-agnostic composition sites: one guard at the top of `dispatch_tool()` (the single dispatch entry, below all provider streaming branches) and one filter+budget block at the `get_tools()` composition site. In Deep Mode (no active workflow → `phase_whitelist is None`) both are literal no-ops, keeping Explorer/General byte-identical (the Phase 089 invariant — 075.x cascade prevention).

Purpose: HARNESS-05 (locked workflow: model can't reach blocked tools; refusals are clean cross-provider tool_results, no 400) + TOOL-05 (protect providers, esp. Google, from accuracy degradation past their tool limit). D-05 two-layer: schema filter (model only SEES allowed) + dispatch backstop (catches hallucinated names). D-04 guiding refusal so workflows feel smart, not stuck.

Output: `ToolContext.phase_whitelist` + the dispatch guard; the get_tools filter+budget; `max_tools` on the ModelCapability registry + Google rows.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-CONTEXT.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-01-SUMMARY.md

<interfaces>
<!-- tool_dispatcher.py (RESEARCH-verified anchors) -->
ToolContext             # dataclass at :60-89 — already has available_tools, parent_run_id, tool_call_id; ADD phase_whitelist
ToolResult              # has .result (str)
async def dispatch_tool(tool_name, args, ctx) -> ToolResult   # at :1495 — ADD ONE guard at the TOP
_TOOL_REGISTRY          # dict at :1465 — name -> handler
# Precedent refusal (_handle_task subset gate at :1091-1115) — a ToolResult.result string already crosses all 6 providers cleanly.
# Loop attaches tool_call_id for EVERY tool call (task_service.py:359-364) → refusal needs no provider branch (Pitfall 4).

<!-- openai_service.py:768-789 (RESEARCH-verified) -->
def get_tools(user_settings) -> list[dict]   # returns hardcoded ~24 tool schemas; conditionally appends WEB_SEARCH/EXECUTE_CODE.
# NO existing priority field; "priority" = list assembly (registry) order (A3 VERIFIED).
# Schema shape: {"type":"function","function":{"name": "...", ...}}
# Precedent phase filter (task_service.py:304-308): [t for t in get_tools(...) if t["function"]["name"] in allowed_tools]

<!-- config.py (RESEARCH-verified) -->
class ModelCapability(TypedDict, total=False):   # at :129 — NO max_tools yet; ADD it
    ...
MODEL_CAPABILITIES: dict[str, ModelCapability]   # at :179
DEFAULT_LLM_CALL_TIMEOUT_SECONDS = 300
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ToolContext.phase_whitelist + the single dispatch_tool guard (HARNESS-05 / D-04 / D-06)</name>
  <files>backend/app/services/tool_dispatcher.py, backend/tests/test_harness_whitelist.py</files>
  <read_first>
    - backend/app/services/tool_dispatcher.py (the file being modified — read ToolContext at :60-89, dispatch_tool at :1495, the _handle_task subset-refusal precedent at :1091-1115, and how handlers fire-and-forget audit writes)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Pattern 1 (the exact guard code) + § Pitfall 3 (never touch a provider branch) + § Pitfall 4 (matching tool_call_id) + decisions D-04/D-05/D-06
    - backend/tests/test_harness_whitelist.py (Plan 01 skeleton — the contracts to flip live)
  </read_first>
  <behavior>
    - Test: `dispatch_tool("execute_code", {}, ctx(phase_whitelist=frozenset({"search_documents"})))` returns a ToolResult whose `.result` JSON-parses to `{"error":"tool_not_available_in_phase","tool":"execute_code","allowed":["search_documents"],"message": "...Available tools here: ['search_documents']..."}` and raises NO exception
    - Test: `dispatch_tool("search_documents", {...}, ctx(phase_whitelist=frozenset({"search_documents"})))` dispatches normally (not refused)
    - Test: `dispatch_tool("execute_code", {}, ctx(phase_whitelist=None))` dispatches exactly as pre-091 (the guard branch is skipped) — Deep Mode no-op
    - Test: a refusal triggers a `tool_refused` audit write (assert via the fire-and-forget hook / spy)
  </behavior>
  <action>
    1. Add `phase_whitelist: frozenset[str] | None = None` to the `ToolContext` dataclass (purely additive — mirror how `available_tools`/`tool_call_id` were added; put it last with a default so no caller breaks).
    2. At the TOP of `async def dispatch_tool(tool_name, args, ctx)` (before the `_TOOL_REGISTRY.get`), add ONE guard:
       ```python
       if ctx.phase_whitelist is not None and tool_name not in ctx.phase_whitelist:
           allowed = sorted(ctx.phase_whitelist)
           # D-06: audit the refusal (fire-and-forget, mirroring the existing handler audit pattern — do NOT block dispatch on the write)
           _spawn_tool_refused_audit(ctx, tool_name, allowed)
           return ToolResult(result=json.dumps({
               "error": "tool_not_available_in_phase",
               "tool": tool_name,
               "message": f"Tool `{tool_name}` is not available in this phase. Available tools here: {allowed}",
               "allowed": allowed,
           }))
       ```
       The refusal is a normal `ToolResult.result` string — the most provider-agnostic surface. The loop attaches the matching `tool_call_id` itself (Pitfall 4); do NOT edit any provider branch. NEVER add this check inside a provider service or chunk handler (Pitfall 3).
    3. Add `_spawn_tool_refused_audit(ctx, tool_name, allowed)` — a small helper that fire-and-forgets `write_audit(pool, ctx.parent_run_id or ctx.run_id, "tool_refused", {"tool": tool_name, "allowed": allowed})` via the existing spawn mechanism (`ctx.spawn(...)` if present, else `asyncio.create_task`). Import `write_audit` from `app.db.workflows`. If `ctx` carries no pool/run_id (pure Deep Mode call), the guard never fires so this is never reached with phase_whitelist=None.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_whitelist.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "phase_whitelist" backend/app/services/tool_dispatcher.py` matches the ToolContext field AND the guard
    - `grep -n "tool_not_available_in_phase" backend/app/services/tool_dispatcher.py` matches
    - `grep -n "Available tools here:" backend/app/services/tool_dispatcher.py` matches (D-04 wording)
    - `grep -n "_spawn_tool_refused_audit\|tool_refused" backend/app/services/tool_dispatcher.py` matches (D-06)
    - the guard appears ABOVE the `_TOOL_REGISTRY.get(tool_name)` line in dispatch_tool (grep line numbers: guard line < registry-get line)
    - NO edit appears in any `*_service.py` provider file or agent_loop.py provider branch (the only files this plan modifies are tool_dispatcher.py, openai_service.py, config.py)
    - test_harness_whitelist.py all 4 live contracts pass (refused-clean, in-whitelist-passes, None-no-op, refusal-audited)
  </acceptance_criteria>
  <done>ToolContext gains phase_whitelist; dispatch_tool refuses out-of-whitelist calls with the D-04 guiding tool_result + tool_refused audit; phase_whitelist=None is a literal no-op; zero provider-branch edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_tools phase-whitelist filter + TOOL-05 budget guard at the composition site (D-05 layer 1 + TOOL-05)</name>
  <files>backend/app/services/openai_service.py, backend/app/config.py, backend/tests/test_tool_budget.py</files>
  <read_first>
    - backend/app/services/openai_service.py (the file being modified — read get_tools at :768-789, the hardcoded ~24-tool assembly order)
    - backend/app/config.py (the file being modified — read ModelCapability TypedDict at :129 + MODEL_CAPABILITIES at :179 + DEFAULT_LLM_CALL_TIMEOUT_SECONDS)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Pattern 6 (the two surfaces; whitelist tools always retained) + Open Questions 4 (max_tools rollout: Google rows; default-when-absent = no cap) + SEED-035 reference
    - backend/tests/test_tool_budget.py (Plan 01 skeleton — contracts to flip live)
  </read_first>
  <behavior>
    - Test: `apply_tool_budget(schemas, model="gemini-3-...", whitelist={"search_documents","execute_code"})` with MODEL_CAPABILITIES[gemini].max_tools=8 and 24 input schemas → returns exactly 8 schemas, and BOTH whitelist tools are present (retained even if registry-order would have dropped them)
    - Test: a model with NO max_tools in MODEL_CAPABILITIES → returns all input schemas unchanged (no cap)
    - Test: whitelist filter alone: `[t for t in get_tools(...) if name in whitelist]` reduces 24 → len(whitelist) before budget
  </behavior>
  <action>
    1. In `config.py`: add `max_tools: int` to the `ModelCapability` TypedDict (it is `total=False` so the key stays optional — absent = no cap). Populate the Google/Gemini model rows in `MODEL_CAPABILITIES` with an explicit `max_tools` (Google is the SEED-035 priority target — set a conservative ceiling, e.g. `max_tools: 16`; size from the Gemini tool-count guidance, document the chosen value in a comment referencing SEED-035). Do NOT add max_tools to other providers (default-when-absent = no cap; OQ4).
    2. In `openai_service.py`: add a pure helper `apply_tool_budget(schemas: list[dict], model: str, whitelist: frozenset[str] | None) -> list[dict]`:
       - First, if `whitelist is not None`, filter `schemas` to those whose `t["function"]["name"]` is in `whitelist` (D-05 layer 1 — the model only SEES allowed tools).
       - Then read `cap = MODEL_CAPABILITIES.get(model, {}).get("max_tools")`. If `cap is None or len(schemas) <= cap`, return as-is. Else drop schemas from the LOW-priority end (registry/assembly order — last appended = lowest priority, A3) UNTIL `len <= cap`, but NEVER drop a tool whose name is in `whitelist` (whitelist tools are the point of the phase — Pattern 6). If the whitelist alone exceeds cap, keep all whitelist tools (the soft ceiling yields to the structural requirement) and log a warning.
       - Document that Google caps tool count, so the budget is the deterministic enforcement of that cap WHEN it is invoked from the harness executor path. IMPORTANT: this function is NOT wired into the Deep-Mode/default `get_tools()` call sites in this phase — it is invoked only from the harness executor (Plan 03, phase config present), so Explorer/General/Deep-Mode (including Google) stay byte-identical (SC#2). Wiring it into the Deep-Mode tool path is out of scope for 091.
       - Return the result preserving order. Pure function, no side effects.
    3. Expose `apply_tool_budget` so the harness phase executor (Plan 03) calls `apply_tool_budget(get_tools(user_settings), model, phase_whitelist)` to build the per-phase `tools_override`. Do NOT change `get_tools`'s own signature/behavior. Do NOT rewire any existing Deep-Mode `get_tools()` call site to `apply_tool_budget` — the ONLY caller in 091 is the harness executor (phase_whitelist present). Existing Deep-Mode callers continue calling `get_tools` directly with no cap, so non-Google AND Google Deep-Mode tool sets remain byte-identical.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_tool_budget.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "max_tools" backend/app/config.py` matches the TypedDict key AND at least one Google/Gemini row
    - `grep -n "def apply_tool_budget" backend/app/services/openai_service.py` matches
    - `grep -n "max_tools is None" backend/app/services/openai_service.py` matches
    - `grep -n "SEED-035" backend/app/config.py` matches (documents the Google ceiling rationale)
    - test_tool_budget.py live: cap-at-max_tools, whitelist-always-retained, no-cap-when-absent all pass
    - a non-Google model with no max_tools key + whitelist=None returns the original get_tools list unchanged (Deep-Mode byte-identical for non-capped providers) — asserted in test
    - Deep-Mode byte-identical scope guard (SC#2 / CLAUDE.md — ALL providers incl. Google): NO existing Deep-Mode `get_tools()` call site is rewired to `apply_tool_budget` in THIS phase, so Explorer/General/Deep-Mode tool sets (including Google) stay byte-identical. The TOOL-05 `max_tools` field + `apply_tool_budget` function ship in 091, but the Google `max_tools` ceiling only applies through the harness executor path (phase config present); wiring `apply_tool_budget` into the Deep-Mode get_tools() path is explicitly OUT OF SCOPE here. Verify: `grep -rn "apply_tool_budget" backend/app/services/*_service.py backend/app/api/*.py | grep -v "openai_service.py"` returns NOTHING (the budget is invoked only from the harness executor in Plan 03, never the Deep-Mode/default get_tools path).
  </acceptance_criteria>
  <done>apply_tool_budget filters by whitelist then caps at the per-provider max_tools, always retaining whitelist tools; max_tools added to ModelCapability with Google rows populated; absent = no cap; the function is NOT wired into any Deep-Mode get_tools() call site this phase, so Deep Mode stays byte-identical for ALL providers including Google.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool call → dispatch_tool | The model may emit a tool name outside the active phase's allowed set (stale/hallucinated). Untrusted. |
| LLM → tool schema list | The schemas the model SEES gate what it can attempt; filtered+capped server-side before the provider call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-08 | Elevation of Privilege | LLM escapes locked workflow via out-of-phase tool | mitigate | Two layers (D-05): get_tools schema filter (model never sees the tool) + dispatch_tool hard backstop (catches hallucinated names). Refusal is a clean tool_result, never an exception. |
| T-091-09 | DoS / accuracy degradation | provider overloaded past tool limit (Google) | mitigate | TOOL-05 budget guard caps the schema count at the per-provider max_tools soft ceiling (SEED-035); whitelist tools always retained. Invoked only via the harness executor path (phase config present); Deep Mode untouched. |
| T-091-10 | Integrity (cross-provider regression) | guard placed in a provider branch | mitigate | Guard lives ONLY at the top of dispatch_tool() (provider-agnostic); refusal is a ToolResult.result string carried on the existing tool_result append with the matching tool_call_id (Pitfall 3/4). Deep Mode (phase_whitelist=None) is a literal no-op (Phase 089 byte-identical invariant); apply_tool_budget is NOT wired into any Deep-Mode get_tools call site. |
| T-091-11 | Repudiation | hidden tool refusal | mitigate | every refusal writes a harness_audit `tool_refused` row (INSERT-only, D-06). |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_harness_whitelist.py tests/test_tool_budget.py -q` green.
- `cd backend && venv/Scripts/python -c "from app.services.tool_dispatcher import ToolContext; assert 'phase_whitelist' in ToolContext.__dataclass_fields__; from app.services.openai_service import apply_tool_budget; print('ok')"` prints ok.
- Diff touches ONLY tool_dispatcher.py, openai_service.py, config.py (+ the 2 test files). Zero edits to any other *_service.py or agent_loop.py provider branch. No existing Deep-Mode get_tools() call site is rewired to apply_tool_budget.
</verification>

<success_criteria>
- HARNESS-05: out-of-whitelist refused with clean tool_result + matching tool_call_id + tool_refused audit; Deep Mode literal no-op.
- TOOL-05: get_tools filtered+capped at the composition site; whitelist tools always retained; max_tools on the registry with Google populated; absent = no cap; budget invoked only via the harness executor path so Deep Mode is byte-identical for ALL providers (SC#2).
- Zero provider-branch edits (075.x cascade prevention).
</success_criteria>

<output>
After completion, create `.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-06-SUMMARY.md`.
</output>
</content>
