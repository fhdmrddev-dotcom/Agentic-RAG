---
phase: 091-harness-engine-5-phase-types-gates-whitelist
plan: 03
type: execute
wave: 3
depends_on: [01, 02, 06]
files_modified:
  - backend/app/services/harness/phase_types.py
  - backend/app/services/harness/programmatic.py
  - backend/app/services/task_service.py
  - backend/tests/test_harness_engine.py
autonomous: true
requirements: [HARNESS-01]
must_haves:
  truths:
    - "All 5 phase types execute by wrapping shipped substrate: programmatic (registry fn), llm_single (_stream_one_iteration), llm_agent (run_task_sub_agent), llm_batch_agents (gather of run_task_sub_agent + merge), llm_human_input (ask_user pause/resume)"
    - "Each executor is registered in PHASE_TYPE_REGISTRY so the engine's dispatch seam (Plan 02) resolves all 5"
    - "llm_agent/llm_batch_agents pass the phase whitelist as allowed_tools AND set ToolContext.phase_whitelist (both D-05 layers); the phase.prompt reaches the model verbatim via an additive system_prompt_override"
    - "llm_agent step cap aligns with the existing agent-loop Explorer=8 convention; llm_batch_agents bounded by max_parallel_agents (default 5) composing the shipped Semaphore(3)/Redis-Lua(20) caps"
    - "PROGRAMMATIC_PHASE_REGISTRY is a closed dict (an unknown fn fails fast, never eval'd); the split_topic fn for the Literature-review seed is registered"
  artifacts:
    - path: "backend/app/services/harness/phase_types.py"
      provides: "PHASE_TYPE_REGISTRY with 5 executors wired to substrate"
      contains: "PHASE_TYPE_REGISTRY"
    - path: "backend/app/services/harness/programmatic.py"
      provides: "PROGRAMMATIC_PHASE_REGISTRY + register_programmatic decorator + split_topic"
      contains: "PROGRAMMATIC_PHASE_REGISTRY"
    - path: "backend/app/services/task_service.py"
      provides: "additive system_prompt_override param on run_task_sub_agent"
      contains: "system_prompt_override"
  key_links:
    - from: "backend/app/services/harness/phase_types.py"
      to: "task_service.run_task_sub_agent"
      via: "llm_agent/llm_batch_agents call with allowed_tools + system_prompt_override + max_steps"
      pattern: "run_task_sub_agent"
    - from: "backend/app/services/harness/phase_types.py"
      to: "harness_engine.PHASE_TYPE_REGISTRY"
      via: "register all 5 executors so the dispatch seam resolves"
      pattern: "PHASE_TYPE_REGISTRY\\["
---

<objective>
Implement the 5 phase-type executors as thin wrappers over already-shipped, cross-provider-tested substrate, and register them in the engine's `PHASE_TYPE_REGISTRY` dispatch seam (Plan 02). This is the ~80% composition — the executors CALL the substrate, they never reimplement loops or edit byte-frozen paths.

Purpose: HARNESS-01 — all 5 phase types drive end-to-end (`programmatic`, `llm_single`, `llm_agent`, `llm_batch_agents`, `llm_human_input`). Each maps to its verified substrate (RESEARCH Pattern 2). The whitelist (Plan 06) is wired through both layers for the LLM phases. The phase.prompt reaches the model via an additive `system_prompt_override` (OQ1).

Output: `harness/phase_types.py` (5 executors + registration), `harness/programmatic.py` (registry + decorator + split_topic), an additive `system_prompt_override` param on `run_task_sub_agent`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-CONTEXT.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-02-SUMMARY.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-06-SUMMARY.md

<interfaces>
<!-- Substrate (RESEARCH-verified file:line anchors) — CALL these, do NOT edit byte-frozen paths -->
# task_service.py:162-193
async def _stream_one_iteration(messages, tools, model, user_settings) -> tuple[str, list]  # (content, tool_calls); drains in threadpool
# task_service.py:196-436 — complete bounded loop (own run row, own run:{sub_run_id} stream, dispatch_tool per iteration, finalize+sentinel)
async def run_task_sub_agent(parent_ctx, description, instructions, allowed_tools, max_steps) -> dict  # {"sub_run_id","summary","status"}
#   - sets sub_ctx.available_tools=list(allowed_tools) (:300) + schema filter (:304-308)
#   - builds its own sub-agent system prompt via _build_sub_agent_system_prompt (:97-118) — hardcodes "focused sub-agent"
#   - CAVEAT: resolve_sub_agent_model_safely REPLICATED not imported (D-085-16) — CALL run_task_sub_agent, never edit it
# task_service.py:58-91 — acquire_global_task_slot Redis-Lua cap (20) + per-run Semaphore(3); already binds batch concurrency

# ask_user substrate (RESEARCH-verified): _handle_ask_user 5-step flow (tool_dispatcher.py:1273-1458),
#   ask_user_service.subscribe_for_response / publish_response; channels ask_user:{run_id}:{tool_call_id} + ask_user:channels:{run_id}
#   Settings.ask_user_max_timeout_seconds = 1800 (hard cap); per-call default 300

# Engine seam (Plan 02): PHASE_TYPE_REGISTRY: dict[str, Callable]  in harness_engine.py
# Budget (Plan 06): apply_tool_budget(get_tools(user_settings), model, phase_whitelist) in openai_service.py
# Models (Plan 01): LlmAgentPhaseConfig{prompt, available_tools, max_steps, wall_clock_seconds, model}, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROGRAMMATIC_PHASE_REGISTRY + register_programmatic decorator + split_topic (programmatic phase)</name>
  <read_first>
    - backend/app/services/tool_dispatcher.py (the `_TOOL_REGISTRY` dict at :1465 — the closed-registry pattern to MIRROR)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Pattern 2 (programmatic registry surface, the split_topic consumer) + § Pattern 3 idempotency row (programmatic fns MUST be pure/idempotent)
    - backend/app/models/harness.py (Plan 01 — ProgrammaticPhaseConfig{fn, input_keys})
  </read_first>
  <action>
    Create `backend/app/services/harness/programmatic.py`:
    - `PROGRAMMATIC_PHASE_REGISTRY: dict[str, Callable[[dict, "PhaseRunCtx"], Awaitable[dict]]] = {}` — a CLOSED dict (an unknown fn name raises, never eval'd — T-091 tampering mitigation).
    - `def register_programmatic(name: str)` decorator that registers `fn` under `name` and returns it.
    - Register `@register_programmatic("split_topic")` — a pure async fn that reads the run's topic from the accumulated-outputs context (keys named by `config.input_keys`), splits it into N sub-questions (deterministic: split on the topic's natural clauses / a fixed prompt-free heuristic for v1, or a single bounded `_stream_one_iteration` call IF a deterministic split is impossible — but prefer pure-Python so it's idempotent on re-run per Pattern 3). Return `{"sub_questions": [...]}` for the Literature-review batch phase to fan out over. Document the idempotency requirement in the module docstring.
    - Export `PROGRAMMATIC_PHASE_REGISTRY`, `register_programmatic`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py -k "programmatic or split_topic" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PROGRAMMATIC_PHASE_REGISTRY" backend/app/services/harness/programmatic.py` matches
    - `grep -n "def register_programmatic" backend/app/services/harness/programmatic.py` matches
    - `grep -n "split_topic" backend/app/services/harness/programmatic.py` matches
    - `cd backend && venv/Scripts/python -c "from app.services.harness.programmatic import PROGRAMMATIC_PHASE_REGISTRY; assert 'split_topic' in PROGRAMMATIC_PHASE_REGISTRY; print('ok')"` prints ok
    - calling split_topic twice with the same input returns the same sub_questions (idempotency asserted in test)
  </acceptance_criteria>
  <done>A closed programmatic registry + decorator + an idempotent split_topic fn for the Literature-review seed.</done>
</task>

<task type="auto">
  <name>Task 2: Additive system_prompt_override on run_task_sub_agent (OQ1 — phase.prompt reaches the model)</name>
  <read_first>
    - backend/app/services/task_service.py (the file being modified — read run_task_sub_agent :196-436 signature + _build_sub_agent_system_prompt :97-118; understand where the system prompt is assembled)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md Open Questions 1 (the mapping recommendation) + § Pattern 2 CAVEAT (byte-frozen-adjacent — additive only)
  </read_first>
  <action>
    Make ONE additive, backward-compatible change to `run_task_sub_agent`: add a keyword-only param `system_prompt_override: str | None = None` (default None preserves byte-identical behavior for every existing caller — tasks/sub-agents). When provided, it REPLACES the hardcoded "focused sub-agent" framing from `_build_sub_agent_system_prompt` for this call only (the harness phase needs the phase.prompt to be the system framing, not the generic sub-agent text). Do NOT modify `_build_sub_agent_system_prompt` itself, do NOT touch `resolve_sub_agent_model_safely` (D-085-16 replicated footgun), do NOT change any other behavior. Guard: if `system_prompt_override is None`, the code path is identical to today (assert this in a test).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/ -k "sub_agent and (override or prompt)" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "system_prompt_override" backend/app/services/task_service.py` matches (param + usage)
    - the param is keyword-only with default None (grep the signature shows `*,` before it or `system_prompt_override: str | None = None`)
    - `grep -n "def _build_sub_agent_system_prompt" backend/app/services/task_service.py` body is UNCHANGED (no edits inside that fn)
    - `grep -n "resolve_sub_agent_model_safely" backend/app/services/task_service.py` body unchanged
    - a test asserts: calling run_task_sub_agent without system_prompt_override produces the same assembled system prompt as before (None path byte-identical)
  </acceptance_criteria>
  <done>run_task_sub_agent accepts an optional system_prompt_override (None = unchanged); byte-frozen helpers untouched.</done>
</task>

<task type="auto">
  <name>Task 3: The 5 phase-type executors + PHASE_TYPE_REGISTRY registration (phase_types.py)</name>
  <read_first>
    - backend/app/services/harness_engine.py (Plan 02 — PHASE_TYPE_REGISTRY seam + _execute_phase dispatch + accumulated_outputs shape)
    - backend/app/services/task_service.py (the substrate: _stream_one_iteration, run_task_sub_agent with the new system_prompt_override, the Semaphore/Redis-Lua caps)
    - backend/app/services/harness/programmatic.py (Task 1 — PROGRAMMATIC_PHASE_REGISTRY)
    - backend/app/services/openai_service.py (Plan 06 — apply_tool_budget for the per-phase tools_override)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Pattern 2 (the full map + Code Examples 471-494) + decisions D-10 (final phase output IS chat msg) + D-12 (caps: llm_agent ≈ Explorer=8) + § Pitfall 5 (retry idempotency)
  </read_first>
  <action>
    Create `backend/app/services/harness/phase_types.py` with 5 async executors, each `async def _exec_<type>(phase, accumulated_outputs, ctx) -> dict` returning the phase output (with a `text` key carrying the chat-ready string — the final phase's `text` becomes the assistant message per D-10):

    1. `_exec_programmatic` — resolve `PROGRAMMATIC_PHASE_REGISTRY[phase.config.fn]` (raise a clear error if absent — closed dict), build the fn input from `phase.config.input_keys` against `accumulated_outputs`, `await fn(input, ctx)`. Pure Python, no LLM.
    2. `_exec_llm_single` — `content, _ = await _stream_one_iteration(messages=[{"role":"system","content":phase.config.prompt},{"role":"user","content": _prior_output_text(accumulated_outputs)}], tools=[], model=(phase.config.model or ctx.model), user_settings=ctx.user_settings)`. Return `{"text": content}`. One call (re-run is one paid call — Pitfall 5, acceptable v1).
    3. `_exec_llm_agent` — build `tools_override = apply_tool_budget(get_tools(ctx.user_settings), model, frozenset(phase.config.available_tools))`; build a per-phase ToolContext with `phase_whitelist=frozenset(phase.config.available_tools)` (D-05 layer 2). Call `result = await run_task_sub_agent(parent_ctx=phase_ctx, description=f"Phase: {phase.slug}", instructions=None, allowed_tools=phase.config.available_tools, max_steps=phase.config.max_steps, system_prompt_override=phase.config.prompt)`. Pin `max_steps` default alignment: if `phase.config.max_steps` is the model default, clamp to the Explorer=8 convention (D-12) — document the choice. Return `{"text": result["summary"], "sub_run_id": result["sub_run_id"]}`.
    4. `_exec_llm_batch_agents` — derive the N inputs from `accumulated_outputs` (the split_topic sub_questions); `await asyncio.gather(*[run_task_sub_agent(...) for q in subqs])` bounded by `phase.config.max_parallel_agents` (default 5) using an `asyncio.Semaphore(phase.config.max_parallel_agents)` wrapper that COMPOSES with the shipped per-run Semaphore(3)/Redis-Lua(20) caps (do NOT replace them — fair-share sizing is Phase 096/CONC-01, deferred). Merge by `phase.config.merge_strategy`: `"concat"` joins summaries with `\n\n`; `"concat_numbered"` prefixes `## Result N`. Return `{"text": merged, "sub_run_ids": [...]}`. Each gather branch sets phase_whitelist + tools_override identically to llm_agent.
    5. `_exec_llm_human_input` — emit the ask_user prompt and block on the ask_user pub/sub channel, reusing the `_handle_ask_user` 5-step ordered flow / `ask_user_service` (subscribe → sadd → durable prompt row → emit → block), with `timeout_seconds = min(phase.config.timeout_seconds, settings.ask_user_max_timeout_seconds)` (the 1800s hard cap). Return `{"text": phase.config.prompt, "answer": <response_text>, "tool_call_id": <id>}`. The pending-prompt durable row + tool_call_id are what Plan 04's resume re-subscribes against — store the tool_call_id in the output so resume can find it.

    Register all 5 in the engine: at module import, `from app.services.harness_engine import PHASE_TYPE_REGISTRY` then `PHASE_TYPE_REGISTRY.update({"programmatic": _exec_programmatic, "llm_single": _exec_llm_single, "llm_agent": _exec_llm_agent, "llm_batch_agents": _exec_llm_batch_agents, "llm_human_input": _exec_llm_human_input})`. Ensure `harness/__init__.py` imports `phase_types` so registration happens when the engine is used. Add a `_prior_output_text(accumulated_outputs) -> str` helper (the latest phase's `text`, or all concatenated for context).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py -k "phase_dispatch or 5_types or ordered" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PHASE_TYPE_REGISTRY" backend/app/services/harness/phase_types.py` shows registration of all 5 keys
    - `grep -cE "_exec_(programmatic|llm_single|llm_agent|llm_batch_agents|llm_human_input)" backend/app/services/harness/phase_types.py` is ≥ 5
    - `grep -n "run_task_sub_agent" backend/app/services/harness/phase_types.py` matches in both _exec_llm_agent and _exec_llm_batch_agents
    - `grep -n "phase_whitelist=frozenset" backend/app/services/harness/phase_types.py` matches (D-05 layer 2 wired)
    - `grep -n "system_prompt_override=phase.config.prompt" backend/app/services/harness/phase_types.py` matches (OQ1)
    - `grep -n "ask_user_max_timeout_seconds\|min(phase.config.timeout_seconds" backend/app/services/harness/phase_types.py` matches (hard-cap clamp)
    - `grep -n "max_parallel_agents" backend/app/services/harness/phase_types.py` matches (batch cap)
    - test_harness_engine.py `test_phase_dispatch_routes_each_of_5_types` (live) passes: each phase_type resolves to its executor; `test_engine_drives_ordered_transitions` end-to-end with mocked substrate passes
    - NO edit to run_agent_loop provider branches or any provider service (only files: phase_types.py, programmatic.py, task_service.py additive param)
  </acceptance_criteria>
  <done>All 5 executors wrap their shipped substrate and are registered in PHASE_TYPE_REGISTRY; LLM phases wire both whitelist layers + the phase prompt + caps; the engine resolves all 5 phase types end-to-end with mocked substrate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase config → substrate call | phase.config.fn / available_tools / prompt cross into the substrate; fn is resolved against a closed registry, tools against the whitelist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-12 | Tampering | programmatic fn name injection | mitigate | PROGRAMMATIC_PHASE_REGISTRY is a closed dict; unknown fn raises, never eval'd. |
| T-091-13 | Integrity | byte-frozen sub-agent path edited | mitigate | system_prompt_override is additive keyword-only (None = byte-identical); _build_sub_agent_system_prompt + resolve_sub_agent_model_safely untouched (D-085-16). |
| T-091-14 | DoS | batch fan-out starves the app | accept (this phase) / transfer (096) | max_parallel_agents (default 5) composes with the shipped Semaphore(3)/Redis-Lua(20) caps; fair-share sizing deferred to Phase 096/CONC-01 (documented). |
| T-091-15 | Elevation of Privilege | LLM phase reaches a blocked tool | mitigate | both D-05 layers wired per phase (apply_tool_budget filter + ToolContext.phase_whitelist backstop from Plan 06). |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py -q` green for dispatch + ordered-transition contracts.
- `cd backend && venv/Scripts/python -c "import app.services.harness; from app.services.harness_engine import PHASE_TYPE_REGISTRY; assert set(PHASE_TYPE_REGISTRY) == {'programmatic','llm_single','llm_agent','llm_batch_agents','llm_human_input'}; print('ok')"` prints ok.
- Diff to task_service.py is the single additive system_prompt_override param only.
</verification>

<success_criteria>
- HARNESS-01: all 5 phase types execute by wrapping shipped substrate and are registered in the engine seam.
- Whitelist (both D-05 layers), the phase prompt (OQ1), and caps (D-12) are wired for the LLM phases.
- Zero byte-frozen-path / provider-branch edits.
</success_criteria>

<output>
After completion, create `.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-03-SUMMARY.md`.
</output>
