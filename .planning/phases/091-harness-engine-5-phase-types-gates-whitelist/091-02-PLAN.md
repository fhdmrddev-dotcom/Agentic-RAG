---
phase: 091-harness-engine-5-phase-types-gates-whitelist
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/db/workflows.py
  - backend/app/services/harness_engine.py
  - backend/app/services/harness/__init__.py
  - backend/app/services/harness/reachability.py
  - backend/tests/test_harness_engine.py
  - backend/tests/test_harness_resume.py
  - backend/tests/test_harness_reachability.py
autonomous: true
requirements: [HARNESS-01, HARNESS-03]
must_haves:
  truths:
    - "The engine steps a published WorkflowDefinition through its phases by phase_index in order — the dispatch-by-phase_type seam exists and the LLM never picks the next phase"
    - "Each phase is marked status='active' (durable) BEFORE its work runs and status='completed' with its output ONLY after the output is durable — a single atomic UPDATE per flip"
    - "A phase whose execution raises mid-work is left status='active' (never 'completed', output empty) so a later sweep re-runs it"
    - "On successful completion the final phase's output IS the assistant chat message verbatim — no extra synthesis LLM call (D-10)"
    - "publish-time reachability lint (pure function) flags orphan phases, unsatisfiable skip_to_phase targets, and missing terminal; the 4 seed shapes lint clean"
  artifacts:
    - path: "backend/app/services/harness_engine.py"
      provides: "run_workflow() transition loop + 2-phase write + audit emit + completion semantics"
      contains: "async def run_workflow"
    - path: "backend/app/db/workflows.py"
      provides: "typed asyncpg helpers: mark_phase_active, complete_phase, advance_current_phase, fail_run, load_run_phases, claim_run"
      contains: "async def mark_phase_active"
    - path: "backend/app/services/harness/reachability.py"
      provides: "lint_workflow(WorkflowDefinition) -> list[LintError] pure function"
      contains: "def lint_workflow"
  key_links:
    - from: "backend/app/services/harness_engine.py"
      to: "backend/app/db/workflows.py"
      via: "mark_phase_active before work, complete_phase after durable output"
      pattern: "mark_phase_active|complete_phase"
    - from: "backend/app/services/harness_engine.py"
      to: "run:{run_id} (_emit)"
      via: "phase_started / phase_completed / phase_transition / run_completed events"
      pattern: "_emit\\(.*phase_"
---

<objective>
Build the harness state-machine core: a hand-rolled async transition loop (`harness_engine.run_workflow`) that drives a published `WorkflowDefinition` through its phases in `phase_index` order over the Phase 090 tables, with the strict 2-phase write (the highest-risk correctness surface) and the publish-time reachability lint. This plan establishes the dispatch-by-phase_type SEAM (a registry call) that Plan 03 fills with the 5 real executors, and the gate seam that Plan 05 fills.

Purpose: HARNESS-01 (backend drives ordered transitions; LLM cannot reorder/skip) + HARNESS-03 (2-phase-write durability). Correctness here is the whole milestone — a backwards 2-phase write loses output on restart (Pitfall 1). All harness logic lives ABOVE the agent loop; zero provider-branch edits.

Output: `harness_engine.py` (run_workflow loop + completion semantics), `db/workflows.py` (typed asyncpg helpers mirroring `db/runs.py`), `harness/reachability.py` (pure lint), `harness/__init__.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-CONTEXT.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-01-SUMMARY.md

<interfaces>
<!-- DB column shapes (migrations 056-059, RESEARCH-verified file:line anchors). -->
workflow_runs:        status (active|paused|completed|failed|cancelled), current_phase_id (uuid), thread_id, user_id, workflow_definition_id
workflow_phases:      id (uuid), run_id, slug, phase_index, status CHECK = pending|active|completed|failed|skipped, output jsonb, updated_at
harness_audit:        run_id (plain uuid, no FK), event_type text+CHECK, metadata jsonb, created_at — INSERT-only RLS (no UPDATE/DELETE)
harness_audit.event_type allowed (059 CHECK, 9 kinds): phase_started, phase_completed, phase_transition, gate_passed, gate_failed, tool_refused, run_started, run_completed, run_failed
threads.active_workflow_run_id

<!-- _emit signature (threads.py:139, RESEARCH-verified) — XADD to run:{run_id}, maxlen=10000 -->
async def _emit(redis, run_id, type: str, **fields) -> None   # ONE XADD; every event rides this; zero new namespace

<!-- db/runs.py is the asyncpg-helper precedent to mirror (D-073 hot-path on the pool). -->
<!-- Finalized models from Plan 01 — backend/app/models/harness.py:
     WorkflowDefinition{slug,version,name,status,phases:[PhaseSpec]}
     PhaseSpec{slug, phase_index, config: PhaseConfig (discriminated), validators:[ValidatorSpec]}
     ValidatorSpec{kind, config, on_failure, max_retries} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typed asyncpg DB helpers for the 2-phase write (db/workflows.py)</name>
  <read_first>
    - backend/app/db/runs.py (the asyncpg-helper precedent to MIRROR — pool acquisition, parameterized SQL, return shapes)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Pattern 3 (the exact 2-phase write UPDATE sequence) + § Pitfall 7 (multi-worker claim)
    - backend/app/models/harness.py (Plan 01 — the parsed types these helpers return-shape against)
  </read_first>
  <action>
    Create `backend/app/db/workflows.py` mirroring `db/runs.py` (asyncpg pool, parameterized queries, no supabase-py in the hot path per D-073). Implement these async helpers, each scoping by the run's owner where it reads/writes (V4 access control — the engine runs as service role, so it MUST filter by user_id/run_id, never trust the LLM):

    1. `async def load_run_phases(pool, run_id) -> list[dict]` — `SELECT id, slug, phase_index, status, output FROM workflow_phases WHERE run_id=$1 ORDER BY phase_index`.
    2. `async def mark_phase_active(pool, phase_id) -> None` — `UPDATE workflow_phases SET status='active', updated_at=now() WHERE id=$1`. DURABLE-before-work (Pitfall 1).
    3. `async def complete_phase(pool, phase_id, output: dict) -> None` — `UPDATE workflow_phases SET status='completed', output=$2::jsonb, updated_at=now() WHERE id=$1`. The status flip + output write are ONE atomic UPDATE (never two statements).
    4. `async def fail_phase(pool, phase_id, reason: str) -> None` — `UPDATE workflow_phases SET status='failed', output=$2::jsonb WHERE id=$1` (reason in output as `{"_failure_reason": reason}`).
    5. `async def skip_phase(pool, phase_id) -> None` — `UPDATE workflow_phases SET status='skipped' WHERE id=$1` (for skip_to_phase routing in Plan 05).
    6. `async def advance_current_phase(pool, run_id, next_phase_id) -> None` — `UPDATE workflow_runs SET current_phase_id=$2 WHERE id=$1`.
    7. `async def finish_run(pool, run_id, status: str) -> None` — `UPDATE workflow_runs SET status=$2 WHERE id=$1` for terminal `completed`/`failed`.
    8. `async def claim_run(pool, run_id) -> bool` — CAS: `UPDATE workflow_runs SET status='active' WHERE id=$1 AND status IN ('active','paused') RETURNING id`; return True iff a row came back. Multi-worker single-producer discipline (Pitfall 7); Plan 04 calls this before a resume sweep re-runs.
    9. `async def write_audit(pool, run_id, event_type: str, metadata: dict) -> None` — `INSERT INTO harness_audit (run_id, event_type, metadata) VALUES ($1,$2,$3::jsonb)`. event_type MUST be one of the 9 in the 059 CHECK (assert at the top against a module-level `_AUDIT_EVENT_TYPES` frozenset so a typo fails fast in tests, not as a Postgres 23514 mid-run — Pitfall 6).

    Add a module-level `_AUDIT_EVENT_TYPES = frozenset({"phase_started","phase_completed","phase_transition","gate_passed","gate_failed","tool_refused","run_started","run_completed","run_failed"})`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_resume.py -k "two_phase_write" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def mark_phase_active" backend/app/db/workflows.py` matches
    - `grep -n "async def complete_phase" backend/app/db/workflows.py` matches
    - `grep -n "async def claim_run" backend/app/db/workflows.py` matches
    - `grep -n "_AUDIT_EVENT_TYPES" backend/app/db/workflows.py` matches and lists exactly 9 event types
    - `grep -n "status='completed'" backend/app/db/workflows.py` shows the flip and output write in ONE UPDATE statement (no separate output-only UPDATE)
    - the test_harness_resume.py `two_phase_write` test (flipped live) asserts via `mock_asyncpg_pool.calls` that the `status='active'` UPDATE index precedes the `status='completed'` UPDATE index — exits 0
  </acceptance_criteria>
  <done>db/workflows.py exposes the 9 typed helpers; the 2-phase write is two ordered atomic UPDATEs (active then completed-with-output); audit event types are validated against the 9-kind CHECK in code.</done>
</task>

<task type="auto">
  <name>Task 2: run_workflow transition loop + 2-phase write + completion semantics (harness_engine.py)</name>
  <read_first>
    - backend/app/db/workflows.py (Task 1 — the helpers this loop calls)
    - backend/app/api/threads.py (the `_emit` definition at ~:139 + `_shielded_finalize` at ~:1122 — the clean-termination model to mirror: durable status write BEFORE terminal sentinel)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § System Architecture Diagram (the full loop) + § Pattern 3 + Code Examples lines 495-521 + § Pattern 9 (event vocabulary) + Open Questions 3 (retry state NOT persisted — re-run from attempt 0)
    - backend/app/models/harness.py (Plan 01 — PhaseSpec/PhaseConfig)
  </read_first>
  <action>
    Create `backend/app/services/harness/__init__.py` (empty package marker) and `backend/app/services/harness_engine.py`. Implement the hand-rolled async state machine (no LangGraph — raw asyncio per CLAUDE.md):

    `async def run_workflow(run_id, definition: WorkflowDefinition, ctx, *, pool, redis) -> None`:
    - Load phases via `load_run_phases(pool, run_id)`; build an `accumulated_outputs: dict[slug -> output]` from any already-`completed` rows (so a resumed run sees prior outputs — Plan 04 reuses this).
    - Iterate phases in `phase_index` order, skipping rows already `completed` or `skipped` (idempotent resume). For each to-run phase:
      1. `await mark_phase_active(pool, phase.id)` — DURABLE before any work.
      2. `await write_audit(pool, run_id, "phase_started", {...})` then `await _emit(redis, run_id, "phase_started", phase=phase.slug, phase_index=phase.phase_index, phase_type=phase.config.phase_type)`. WRITE-before-emit (D-v2.5-03).
      3. Run the phase under a wall-clock cap: `output = await asyncio.wait_for(_execute_phase(phase, accumulated_outputs, ctx), timeout=wall_clock)`. `_execute_phase` dispatches via a `PHASE_TYPE_REGISTRY` dict (the SEAM — Plan 03 registers the 5 executors; for THIS plan, the registry is a module-level `PHASE_TYPE_REGISTRY: dict[str, Callable] = {}` that raises `KeyError`-derived `PhaseTypeNotRegistered` if empty — Plan 03 fills it and flips the dispatch tests live). Pin the wall_clock source: `phase.config.wall_clock_seconds or _DEFAULT_PHASE_WALL_CLOCK` (Plan 05 defines the default; here import a placeholder constant `_DEFAULT_PHASE_WALL_CLOCK = 1800`).
      4. Run gates via a `_run_gates(phase, output, ctx)` SEAM (Plan 05 implements; here a no-op returning pass so the loop is testable). Plan 05 wires the bounded retry around the execute call.
      5. `await complete_phase(pool, phase.id, output)` — ONLY after output durable. Spill: if `len(json.dumps(output)) > _OUTPUT_INLINE_LIMIT` (set 64*1024), write the blob to the `workspace-files` bucket and store `{"_spilled_path": path}` in output jsonb (path-only — Pattern 3). Add a `_persist_output(output) -> dict` helper that returns the inline dict or the spilled-path dict.
      6. `accumulated_outputs[phase.slug] = output`; `await advance_current_phase(pool, run_id, next.id)` (or None on last); `await write_audit(... "phase_completed" ...)` + `_emit phase_completed`; on transition `_emit phase_transition` + audit.
    - On loop completion: the FINAL phase's output IS the assistant chat message verbatim (D-10) — persist it as the assistant message via the existing message-insert path the executors return (Plan 03 returns the chat-ready string; here, set `ctx.final_output = accumulated_outputs[last.slug]`). NO extra synthesis LLM call. `await finish_run(pool, run_id, "completed")`; `await write_audit(... "run_completed" ...)`; `_emit run_completed`. Mirror `_shielded_finalize` ordering: durable run-status UPDATE BEFORE the terminal `_emit`.
    - Wrap the whole loop so a raised exception mid-phase leaves that phase `active` (do NOT mark completed/failed inside the generic except — let it propagate after emitting nothing that flips status; Plan 05 owns the deliberate `fail_run` path). The crash-leaves-active invariant is the resume contract.

    Add `__all__` exporting `run_workflow`, `PHASE_TYPE_REGISTRY`, `PhaseTypeNotRegistered`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py tests/test_harness_resume.py -k "transition or two_phase_write or crash_leaves or completion" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def run_workflow" backend/app/services/harness_engine.py` matches
    - `grep -n "PHASE_TYPE_REGISTRY" backend/app/services/harness_engine.py` matches (the dispatch seam)
    - `grep -n "mark_phase_active" backend/app/services/harness_engine.py` appears BEFORE `complete_phase` in the file (active-before-completed)
    - `grep -n "asyncio.wait_for" backend/app/services/harness_engine.py` matches (wall-clock cap seam)
    - `grep -n "_spilled_path\|workspace-files\|_persist_output" backend/app/services/harness_engine.py` matches (large-output spill)
    - `grep -n "synthes\|final_output" backend/app/services/harness_engine.py` shows NO extra-LLM-call synthesis (D-10/D-11) — final output is accumulated_outputs[last.slug]
    - test_harness_resume.py `crash_leaves_phase_active_not_completed` (live) passes: simulate execute raising → assert no `status='completed'` UPDATE for that phase
    - test_harness_engine.py `engine_drives_ordered_transitions` (live, with PHASE_TYPE_REGISTRY stubbed in-test) passes
  </acceptance_criteria>
  <done>run_workflow steps phases in order via the registry seam, marks active-before-work and completed-after-durable-output as ordered atomic UPDATEs, spills large outputs path-only, sets the final phase output as the chat message with no extra LLM call, and leaves a crashed phase `active`.</done>
</task>

<task type="auto">
  <name>Task 3: Publish-time reachability lint as a pure function (harness/reachability.py)</name>
  <read_first>
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Pattern 7 (reachability lint: orphans / unsatisfiable transitions / terminal exists) + Open Questions 5 (pure function this phase; HTTP publish endpoint deferred to 092)
    - backend/app/models/harness.py (Plan 01 — WorkflowDefinition/PhaseSpec/ValidatorSpec; skip_to_phase lives in ValidatorSpec.on_failure as `skip_to_phase:<slug>`)
    - backend/tests/test_harness_reachability.py (Plan 01 skeleton — the contracts to flip live)
  </read_first>
  <action>
    Create `backend/app/services/harness/reachability.py` with a pure function `lint_workflow(definition: WorkflowDefinition) -> list[LintError]` (no I/O, no DB). Define `LintError` as a small dataclass/namedtuple `(code: str, phase_slug: str|None, message: str)`. Build the phase graph: nodes = phases; edges = sequential (phase_index i → i+1) PLUS every `skip_to_phase:<slug>` parsed out of each phase's `validators[].on_failure`. Checks:

    1. ORPHAN — every phase except the entry (`phase_index == 0`) must be reachable from the entry via sequential+skip edges. Emit `LintError("orphan_phase", slug, ...)` for unreachable phases.
    2. UNSATISFIABLE_SKIP — every `skip_to_phase:<slug>` target must be an existing phase slug. Emit `LintError("unsatisfiable_skip", slug, ...)` for dangling targets.
    3. NO_TERMINAL — there must be at least one phase from which completion is reachable (the last phase by index, with no skip edge that loops back forever). Detect a cycle that can never reach the terminal index; emit `LintError("no_terminal", None, ...)`.
    4. DUPLICATE_SLUG / NON_CONTIGUOUS_INDEX — slugs unique; phase_index is 0..N-1 contiguous (defensive — emit `LintError("bad_index", ...)`).

    Add a helper `parse_skip_target(on_failure: str) -> str | None` returning the slug after `skip_to_phase:` or None. Export `lint_workflow`, `LintError`, `parse_skip_target` from `harness/__init__.py`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_reachability.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def lint_workflow" backend/app/services/harness/reachability.py` matches
    - `grep -n "orphan_phase\|unsatisfiable_skip\|no_terminal" backend/app/services/harness/reachability.py` matches all three codes
    - `grep -n "def parse_skip_target" backend/app/services/harness/reachability.py` matches
    - test_harness_reachability.py live tests pass: orphan fixture → ["orphan_phase"]; dangling skip → ["unsatisfiable_skip"]; cycle-no-terminal → ["no_terminal"]; the 4 seed defs (from conftest `four_seed_defs()`) → `lint_workflow` returns `[]`
  </acceptance_criteria>
  <done>lint_workflow is a pure function flagging orphans, unsatisfiable skip targets, and missing terminals; the 4 seed definitions lint clean; no HTTP publish endpoint is added (deferred to 092 per OQ5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-role engine → workflow_* rows | The engine runs as service role (bypasses RLS) and MUST scope every read/write by run_id/owner — it can otherwise leak across users. |
| workflow author → publish | A draft definition graph crosses into publish; an unsatisfiable graph could hang a run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-03 | Information Disclosure | engine DB reads (service role) | mitigate | Every `db/workflows.py` query is keyed by `run_id` (which is owner-scoped via the workflow_runs→threads FK chain); the engine never accepts a run_id it did not receive from the owning thread's producer spawn. |
| T-091-04 | Denial of Service | unsatisfiable phase graph | mitigate | `lint_workflow` rejects orphans / dangling skip_to_phase / no-terminal at publish (pure function, unit-tested on broken fixtures); runtime skip to an unknown slug routes to fail_run (Plan 05). |
| T-091-05 | Integrity | 2-phase write done backwards | mitigate | active-before-work + completed-only-after-durable, two ordered atomic UPDATEs; crash leaves phase `active` (deterministic unit proof via mock_asyncpg_pool.calls ordering). |
| T-091-06 | Repudiation | audit gaps | mitigate | every transition writes a `harness_audit` row (INSERT-only RLS, 9-kind CHECK validated in code before INSERT). |
| T-091-07 | DoS | output blob bloat | mitigate | outputs > 64KB spill to workspace-files bucket, path-only in `output jsonb`. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py tests/test_harness_resume.py tests/test_harness_reachability.py -q` green for this plan's flipped contracts.
- `cd backend && venv/Scripts/python -c "from app.services.harness_engine import run_workflow, PHASE_TYPE_REGISTRY; from app.services.harness import lint_workflow; from app.db.workflows import mark_phase_active, complete_phase, claim_run; print('ok')"` prints ok.
- No edit to any provider service file or agent_loop.py provider branch (grep the diff: zero changes under backend/app/services/*_service.py and zero changes to agent_loop.py chunk handlers).
</verification>

<success_criteria>
- HARNESS-01: run_workflow drives ordered transitions via the PHASE_TYPE_REGISTRY seam; LLM never selects the next phase.
- HARNESS-03: the 2-phase write is two ordered atomic UPDATEs; a crashed phase stays `active`; resume re-run is correct-by-construction.
- Reachability lint (pure function) flags the three failure classes; the 4 seeds lint clean.
- All harness logic is above the loop / in new modules; zero provider-branch edits.
</success_criteria>

<output>
After completion, create `.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-02-SUMMARY.md`.
</output>
