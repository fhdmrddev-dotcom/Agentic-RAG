---
phase: 153-inline-citations
plan: 03
type: execute
wave: 2
depends_on: ["153-02"]
files_modified:
  - frontend/src/components/chat/CitationList.tsx
  - frontend/src/components/chat/CitationCard.tsx
  - frontend/src/components/chat/__tests__/CitationList.test.tsx
autonomous: true
requirements: [CITE-01]

must_haves:
  truths:
    - "D-06: the References footer ALWAYS renders when a retrieval set exists — independent of whether the model emitted any inline markers"
    - "D-07: the footer opens by default when valid inline markers exist on the message; otherwise it keeps today's collapsed default (weaker model degrades to footer-only, no provider worse than today)"
    - "D-03: footer rows are numbered [n] sharing ONE canonical numbering with the inline markers (the backend-finalized set order)"
    - "D-09: both chunk and full-doc citations render as footer rows"
    - "D-10: a full-doc row shows '· Full document' with no chunk index and no similarity score (reuse the existing is_full_doc branch)"
    - "The row↔marker link is bidirectional: activating a row flashes its marker and activating a marker flashes its row, via the shared citationNav flash contract"
  artifacts:
    - path: "frontend/src/components/chat/CitationCard.tsx"
      provides: "numbered [n] row + ↗ Open document + full-doc branch + data-citation-row + keyboard-operable row"
      contains: "data-citation-row"
    - path: "frontend/src/components/chat/CitationList.tsx"
      provides: "References · N sources header + open-by-default-when-markers + numbering thread"
      contains: "References"
    - path: "frontend/src/components/chat/__tests__/CitationList.test.tsx"
      provides: "numbering / open-by-default / full-doc row / marker-flash coverage"
      contains: "open-by-default"
  key_links:
    - from: "frontend/src/components/chat/CitationCard.tsx (row)"
      to: "citationNav flashCitationMarker + useCitationNav.openDocument"
      via: "row activate → flash marker; Open document → openDocument(document_id)"
      pattern: "flashCitationMarker|openDocument"
    - from: "frontend/src/components/chat/CitationList.tsx"
      to: "CitationCard with n = i+1"
      via: "numbered map preserving citations order"
      pattern: "i \\+ 1"
---

<objective>
Restructure the existing References footer into the numbered, click-through form the
sketch locked (075-A): `CitationList` gains a `References · {N} source(s)` header,
opens by default when valid inline markers exist on the message (D-06/D-07), threads a
1-based number `[n]` into each row, and wires the row→marker direction of the shared
bidirectional flash. `CitationCard` gains the mono `[n]` number, an `↗ Open document`
primary link, a `data-citation-row={n}` flash target, keyboard operability, and reuses
the existing `is_full_doc` branch for full-doc rows (D-09/D-10). The footer is the
cross-provider floor: it renders whenever retrieval ran, even when no markers exist.

Purpose: SC#1/SC#3 footer half + the "no provider worse than today" floor (D-06/D-07).
Output: modified `CitationList.tsx` + `CitationCard.tsx` + a new `CitationList.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/153-inline-citations/153-CONTEXT.md
@.planning/phases/153-inline-citations/153-UI-SPEC.md
@.planning/phases/153-inline-citations/153-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- From 153-02 (Wave 1) — consume these; do NOT redefine. -->
citationNav.tsx exports: `useCitationNav()` → { openDocument(documentId) }; `flashCitationMarker(n, container)`;
constants CITATION_ROW_ATTR (data-citation-row), CITATION_MARKER_ATTR (data-citation-marker), CITATION_FLASH_CLASS, CITATION_ACTIVE_CLASS.
Citation CSS (.citation-ref-row, flash class, --primary reserved) lives in index.css.

<!-- Current footer state (verified HEAD 2026-07-15) -->
CitationList.tsx: `const [open, setOpen] = useState(false)` (L12); `if (!citations.length) return null` (L13);
Collapsible + CollapsibleTrigger button showing `{citations.length} source{...}` (L27); maps CitationCard (L34–39).
CitationCard.tsx: version suffix `(v{version_number})` when >1 (L51–54); is_full_doc branch
`{is_full_doc ? "Full document" : chunk_index != null ? \`Chunk ${chunk_index + 1}\` : ""}` (L55–61);
passage line-clamp-2 + Show more (L64–73). Citation type: {document_id, filename, chunk_index, passage, similarity, is_full_doc, version_number}.
UI-SPEC §Color: --primary reserved for [n], active/flash, Open-document ONLY; muted text uses --muted-foreground (AA), NOT --muted-foreground-dim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CitationCard — [n] number, Open document, full-doc branch, flash target</name>
  <files>frontend/src/components/chat/CitationCard.tsx</files>
  <read_first>
    - frontend/src/components/chat/CitationCard.tsx (whole file — the version suffix L51–54, is_full_doc branch L55–61, passage L64–73)
    - frontend/src/lib/citationNav.tsx (useCitationNav, flashCitationMarker, the ROW/MARKER attr + class constants — from 153-02)
    - .planning/phases/153-inline-citations/153-UI-SPEC.md §"References Footer Contract" + §"Copywriting Contract" + §"Accessibility" (row keyboard-operable, aria-label, aria-current)
  </read_first>
  <behavior>
    - A card given `n` renders a leading mono `[n]` in `--primary` (weight 600).
    - A chunk card renders `· Chunk {chunk_index+1} · {similarity 2dp}`; a full-doc card (is_full_doc) renders `· Full document` with NO chunk index and NO score (D-09/D-10).
    - Each card renders an `↗ Open document` link that calls `openDocument(citation.document_id)`.
    - The card root carries `data-citation-row={n}` and is a keyboard-operable `<button>`/focusable with `aria-label` (filename + location); activating it calls `flashCitationMarker(n, container)`.
    - The version suffix `(v{n})` still shows only when `version_number > 1`; the passage line-clamp/Show more still works.
  </behavior>
  <action>
    Extend `CitationCard` to accept a `n: number` prop (the 1-based footer number) and an optional container ref for scoping
    flash. Add the leading mono `[n]` chip using `--font-mono` weight 600 `--primary` (accent-reserved). Reuse the EXISTING
    `is_full_doc` branch verbatim for the location line (do not add a new conditional). Add the `↗ Open document` primary link
    calling `useCitationNav().openDocument(citation.document_id)`. Make the row a keyboard-operable button/focusable with
    `aria-label` (filename + location) and `data-citation-row={n}` (use `CITATION_ROW_ATTR`); on activate, call
    `flashCitationMarker(n, container)` and set `aria-current` while active. Keep the version suffix + passage behavior byte-identical.
    Do NOT use `--primary` for filenames/body/hover (those stay `--accent`/`--muted-foreground`); meaningful muted text uses
    `--muted-foreground` (never `--muted-foreground-dim`). No new package.
  </action>
  <verify>
    <automated>cd frontend && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vite build` → exit 0, 0 net-new tsc errors vs baseline.
    - `grep -c "data-citation-row\|CITATION_ROW_ATTR" frontend/src/components/chat/CitationCard.tsx` ≥ 1.
    - `grep -c "openDocument\|Open document" frontend/src/components/chat/CitationCard.tsx` ≥ 1.
    - The full-doc branch is the reused existing `is_full_doc` conditional (no new similarity/chunk render for full-doc rows) — verify by inspecting the diff.
    - `grep -c "muted-foreground-dim" frontend/src/components/chat/CitationCard.tsx` shows no new usage.
  </acceptance_criteria>
  <done>CitationCard renders a numbered, keyboard-operable, flash-linked row with Open document and the reused full-doc branch; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CitationList — References header, open-by-default-when-markers, numbering + tests</name>
  <files>frontend/src/components/chat/CitationList.tsx, frontend/src/components/chat/__tests__/CitationList.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/CitationList.tsx (whole file — useState(false) L12, return-null guard L13, Collapsible + count header L16–27, map L34–39)
    - frontend/src/components/chat/CitationCard.tsx (Task 1's new `n` prop + data-citation-row)
    - .planning/phases/153-inline-citations/153-UI-SPEC.md §"References Footer Contract" (open-by-default rule, header copy) + §"Interaction States" (footer-only degradation row)
    - .planning/phases/153-inline-citations/153-PATTERNS.md §"CitationList.tsx"
  </read_first>
  <behavior>
    - With markers present on the message, the footer renders OPEN by default; with no markers it renders collapsed (today's `useState(false)`) — D-06/D-07. Accept the "has valid markers" signal as a prop (e.g. `hasMarkers`/`defaultOpen`).
    - Header copy reads `References · {N} source` / `References · {N} sources` (pluralized), reusing today's count logic.
    - `citations.length === 0` still returns null; a non-empty set always renders (D-06).
    - Each row gets `n = i + 1`, preserving the citations array order (D-03); rows are `CitationCard` instances.
    - Activating a marker (simulated by calling the exposed flash on a row's `data-citation-row`) scrolls+blooms the matching row and auto-clears (~1700ms) — assert the flash class toggles.
  </behavior>
  <action>
    Change the footer default-open to open WHEN valid markers exist (a `defaultOpen`/`hasMarkers` prop wired from MessageItem
    in a later plan; default false preserves today's collapsed behavior — the ONE default-state change, D-06/D-07). Update the
    header copy to `References · {N} source(s)` reusing the existing pluralization. Thread `n = i + 1` into each `CitationCard`.
    Keep the existing `Collapsible`/`CollapsibleTrigger` (do NOT hand-roll disclosure) and the `aria-expanded` already present.
    Keep the `citations.length` return-null guard (D-06 footer renders only when a set exists). Author `CitationList.test.tsx`
    FIRST (RED) — numbering, open-by-default-with-markers vs collapsed-without, full-doc row copy, and the row-flash toggle — then GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__/CitationList.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/chat/__tests__/CitationList.test.tsx` → all pass.
    - A test asserts open-by-default WHEN markers exist and collapsed WHEN they do not (D-06/D-07).
    - A test asserts `References · {N} source(s)` header copy and 1-based `[n]` numbering matching citations order.
    - A test asserts a full-doc citation row shows `· Full document` with no chunk/score (D-10).
    - `cd frontend && npx vite build` → exit 0, 0 net-new tsc errors vs baseline.
  </acceptance_criteria>
  <done>The footer is numbered, opens-by-default when markers exist, always renders when a set exists, and the test file is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| citation fields (filename, passage) → footer render | citation strings originate from retrieved docs (attacker-influenceable) and are rendered in the footer |
| Open-document link → documents view | the row's document_id opens a doc — must respect visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-03-01 | Tampering / XSS | CitationCard filename/passage render | mitigate | rendered as React text nodes (auto-escaped); no `dangerouslySetInnerHTML` in the footer; additive `[n]`/Open are inert markup |
| T-153-03-02 | Information Disclosure | row "Open document" | mitigate | routes through `useCitationNav().openDocument` → the existing owner-scoped panel fetch (T-153-02-01); no raw unscoped fetch here |
| T-153-03-03 | Spoofing (trust story) | footer numbering vs backend set | mitigate | `n = i + 1` over the `citations` array (the backend-finalized set); no client-side re-derivation of membership (D-03) |
| T-153-03-SC | Tampering | npm installs | accept | NO package installs (reuses vendored Collapsible/lucide) — Legitimacy Gate not triggered |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/chat/__tests__/CitationList.test.tsx` → green.
- `cd frontend && npx vite build` → exit 0, 0 net-new tsc errors vs baseline.
- Footer renders whenever a citation set exists (D-06); opens-by-default only with markers (D-07); full-doc rows show `· Full document` (D-10).
- No `dangerouslySetInnerHTML` added; Open-document routes through the owner-scoped nav.
</verification>

<success_criteria>
- Numbered `[n]` footer sharing the backend numbering (D-03), always-on when retrieval ran (D-06), open-by-default with markers (D-07), full-doc rows correct (D-09/D-10), bidirectional row→marker flash wired.
</success_criteria>

<output>
Create `.planning/phases/153-inline-citations/153-03-SUMMARY.md` when done.
</output>
