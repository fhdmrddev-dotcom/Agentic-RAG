---
phase: 153-inline-citations
plan: 05
type: execute
wave: 3
depends_on: ["153-03", "153-04"]
files_modified:
  - frontend/src/components/chat/CitedMarkdown.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/__tests__/CitedMarkdown.test.tsx
  - frontend/src/components/chat/__tests__/MessageItem.test.tsx
autonomous: true
requirements: [CITE-01]

must_haves:
  truths:
    - "D-05: markers attach on SETTLE — CitedMarkdown parses the settled/persisted content (never the streaming body); the StreamingNarration path is NEVER given markers (body streams calm & unmarked)"
    - "D-07: only valid in-range markers render — CitedMarkdown upgrades [n] to an interactive <sup> ONLY where n ∈ [1, citations.length], keyed 1:1 to citations[n-1]"
    - "D-11: always-on — the cited branch renders whenever message.citations?.length, with no toggle"
    - "D-12/D-14 + G-5: non-cited assistant messages and ALL user messages stay byte-identical on the shared MarkdownRenderer; dedupParagraphs and the streaming-narration path are unchanged"
    - "V5: the <sup> marker is created as an OWNED document.createElement node (tabindex/role/aria-label set programmatically), never re-parsed from model text; [n] inside code/pre/a is skipped (Pitfall 2/3)"
    - "The marker↔row link is bidirectional: activating a marker flashes+scrolls its footer row and opens the peek; the peek pins on click/Enter"
  artifacts:
    - path: "frontend/src/components/chat/CitedMarkdown.tsx"
      provides: "marked+DOMPurify pipeline (reused) + post-mount TreeWalker marker upgrade + CitationPeek portal + flash wiring"
      min_lines: 80
      exports: ["CitedMarkdown"]
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "additive cited branch — CitedMarkdown when message.citations?.length, else byte-identical MarkdownRenderer"
      contains: "CitedMarkdown"
    - path: "frontend/src/components/chat/__tests__/CitedMarkdown.test.tsx"
      provides: "marker parse/range-check/code-span-skip/createElement-node coverage"
      contains: "range"
  key_links:
    - from: "frontend/src/components/chat/MessageItem.tsx (settled assistant path ~L451)"
      to: "CitedMarkdown when message.role === assistant && message.citations?.length"
      via: "additive ternary; else MarkdownRenderer (byte-identical)"
      pattern: "message.citations\\?\\.length"
    - from: "frontend/src/components/chat/CitedMarkdown.tsx marker"
      to: "CitationPeek + flashCitationRow"
      via: "marker hover/focus → peek portal; click/Enter → pin + flash matching footer row"
      pattern: "CitationPeek|flashCitationRow"
---

<objective>
Assemble the inline-marker experience on the G-5 hot files. `CitedMarkdown` reuses the
existing `marked` + `DOMPurify` pipeline verbatim, then in a post-mount effect walks the
sanitized HTML's text nodes (TreeWalker), skips `code`/`pre`/`a` ancestors, and upgrades
each `[n]` where `n ∈ [1, citations.length]` into a TRUSTED `<sup>` node created via
`document.createElement` (tabindex/role/aria-label + delegated listeners set
programmatically — Pitfall 3), keyed 1:1 to `citations[n-1]`. Hover/focus surfaces the
`CitationPeek` (from 153-04); click/Enter pins it and flashes the matching footer row via
the shared flash contract (from 153-02). `MessageItem` gains the ONE additive branch:
`CitedMarkdown` only when `message.role === "assistant" && message.citations?.length`,
else the byte-identical shared `MarkdownRenderer`. The `StreamingNarration` path never
sees markers (D-05 attach-on-settle) and the streaming/reducer path is verified unchanged.

Purpose: SC#1 (inline markers keyed to the real set) + SC#2 (marker click-through) +
SC#4/D-14/G-5 (byte-identical shared path). This is the assembly of every prior plan.
Output: `CitedMarkdown.tsx`, the additive `MessageItem` branch, `CitedMarkdown.test.tsx`,
and the extended G-5 non-regression `MessageItem.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/153-inline-citations/153-CONTEXT.md
@.planning/phases/153-inline-citations/153-UI-SPEC.md
@.planning/phases/153-inline-citations/153-RESEARCH.md
@.planning/phases/153-inline-citations/153-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- Reused pipeline (MarkdownRenderer.tsx:1–33, verified HEAD) — copy verbatim into CitedMarkdown: -->
  marked.setOptions({ gfm: true, breaks: true })
  const html = useMemo(() => DOMPurify.sanitize(marked.parse(content) as string), [content])
  <div className={cn("markdown text-sm text-foreground leading-relaxed", className)} dangerouslySetInnerHTML={{ __html: html }} />

<!-- MessageItem settled-assistant render (verified HEAD 2026-07-15, L442–459): -->
  {message.content ? (<div className="text-sm text-foreground">
    {isMessageStreaming && (message.tool_calls?.length ?? 0) > 0 && message.role === "assistant"
      ? <StreamingNarration content={dedupParagraphs(message.content)} />         // NEVER give markers
      : <MarkdownRenderer content={message.role === "assistant" ? dedupParagraphs(message.content) : message.content} /> }
    ... {message.citations?.length > 0 && <CitationList citations={message.citations} />} )}
dedupParagraphs is MessageItem.tsx:220 (runs first — markers are mid-sentence, untouched).

<!-- From 153-02 / 153-03 / 153-04 — consume: -->
citationNav.tsx: flashCitationRow(n, container), flashCitationMarker(n, container), useCitationNav().openDocument,
  CITATION_MARKER_ATTR (data-citation-marker), CITATION_ACTIVE_CLASS.
CitationList (153-03): now numbered, accepts a `defaultOpen`/`hasMarkers` signal + renders rows with data-citation-row.
CitationPeek (153-04): { citation, n, anchorRect, pinned, onPin, onClose }.

<!-- StreamsProvider seams to VERIFY unchanged (do NOT edit; RESEARCH: no code change expected): -->
Terminal reconcile StreamsProvider.tsx:1988–2011 already swaps raw streamed content → persisted normalized content on
`done`/`reader_done` (this delivers the backend-normalized markers live — the attach-on-settle seam). onCitations L839
maps citations onto the message. Reducer invariant L367–373: only onDelta may mutate content — do NOT move the swap.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CitedMarkdown — reused pipeline + post-mount marker upgrade + peek/flash wiring (TDD)</name>
  <files>frontend/src/components/chat/CitedMarkdown.tsx, frontend/src/components/chat/__tests__/CitedMarkdown.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MarkdownRenderer.tsx (whole file — the marked+DOMPurify+dangerouslySetInnerHTML pipeline to reuse verbatim)
    - frontend/src/components/chat/CitationPeek.tsx (153-04 — props contract) + frontend/src/lib/citationNav.tsx (153-02 — flashCitationRow, constants, useCitationNav)
    - .planning/phases/153-inline-citations/153-RESEARCH.md §"Pattern 2: Marker injection into sanitized HTML via post-mount DOM upgrade" + Pitfalls 2/3/4/6
    - .planning/phases/153-inline-citations/153-UI-SPEC.md §"Marker Contract" + §"Streaming → Settle" + §"Accessibility"
  </read_first>
  <behavior>
    - Reused pipeline: given plain markdown with no markers, CitedMarkdown output equals the MarkdownRenderer output (same sanitized HTML) — the base render is unchanged.
    - Marker upgrade: `"Revenue rose [1] sharply [2]."` with 2 citations upgrades `[1]` and `[2]` to `<sup>` nodes carrying `role="button"`, `tabindex="0"`, `aria-label="Citation {n}: {filename}"`, and `data-citation-marker={n}`.
    - Range-check: `[9]` with only 2 citations stays LITERAL text (not upgraded) — D-07; and `[0]` stays literal.
    - Code-span skip: `[1]` inside a fenced code block or inline `code` is NOT upgraded (Pitfall 2); a markdown link `[label](url)` is not corrupted.
    - Owned node (V5/Pitfall 3): the `<sup>` is created via `document.createElement` and its interactive attributes are present after mount (proving they are not stripped by DOMPurify because they were never in the sanitized string).
    - Interaction: activating a marker calls the peek open + `flashCitationRow(n, container)`; the marker gets the active class; Esc/onClose unpins.
  </behavior>
  <action>
    Create `CitedMarkdown({ content, citations, className? })`. REUSE the `MarkdownRenderer` pipeline verbatim (memoized
    `DOMPurify.sanitize(marked.parse(content))` into a `ref`'d div via `dangerouslySetInnerHTML`). In a `useLayoutEffect`,
    walk the container's text nodes with a `TreeWalker`, SKIP nodes whose ancestor is `code`/`pre`/`a`, and for each `[n]`
    token where `n ∈ [1, citations.length]` replace it with a TRUSTED `<sup>` created via `document.createElement` (class
    `.citation-marker`, `data-citation-marker={n}` using `CITATION_MARKER_ATTR`); set `role="button"`, `tabindex="0"`,
    `aria-label="Citation {n}: {filename}"` and attach delegated hover/focus/click/keydown listeners PROGRAMMATICALLY (never
    via the sanitized string — Pitfall 3). Out-of-range/`[0]` tokens stay literal (D-07). On hover/focus open a `CitationPeek`
    (render via its portal) anchored to the marker's `getBoundingClientRect()`; on click/Enter pin the peek and call
    `flashCitationRow(n, containerRef)`; wire `useCitationNav().openDocument` through the peek. Apply the `markerPop` attach
    animation class (staggered) from 153-02's CSS; honor reduced-motion (CSS-owned). Re-run the marker upgrade when
    `content`/`citations` change; clean up listeners on unmount. Author `CitedMarkdown.test.tsx` FIRST (RED) then GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__/CitedMarkdown.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/chat/__tests__/CitedMarkdown.test.tsx` → all pass.
    - A test asserts an out-of-range `[9]` (2 citations) and `[0]` stay literal text; in-range `[1]`/`[2]` become `<sup>` with `role="button"`, `tabindex="0"`, `aria-label` containing the filename (D-07/V5/Pitfall 3).
    - A test asserts `[1]` inside a fenced code block stays literal and a `[label](url)` link is not corrupted (Pitfall 2).
    - A test asserts activating a marker triggers the peek + a flash on the matching `data-citation-row`.
    - `grep -c "createElement" frontend/src/components/chat/CitedMarkdown.tsx` ≥ 1 (owned node, not string-injected attributes).
    - `cd frontend && npx vite build` → exit 0, 0 net-new tsc errors vs baseline.
  </acceptance_criteria>
  <done>CitedMarkdown reuses the pipeline, upgrades only in-range non-code [n] to owned interactive <sup> nodes, wires peek + flash, and the test file is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: MessageItem cited branch (G-5 additive) + G-5 non-regression tests</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/components/chat/__tests__/MessageItem.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageItem.tsx L200–240 (dedupParagraphs) + L440–474 (the render branches, StreamingNarration vs MarkdownRenderer, CitationList mount)
    - frontend/src/components/chat/CitedMarkdown.tsx (Task 1) + frontend/src/components/chat/CitationList.tsx (153-03 — the defaultOpen/hasMarkers signal)
    - frontend/src/components/chat/__tests__/MessageItem.test.tsx (existing — extend, do not rewrite)
    - .planning/phases/153-inline-citations/153-PATTERNS.md §"MessageItem.tsx (G-5 HOT)" + §"StreamsProvider.tsx"
    - .planning/phases/153-inline-citations/153-RESEARCH.md §"Code Examples: cited-path gate in MessageItem"
  </read_first>
  <action>
    Add the ONE additive branch at the settled-assistant render (~L451): when `message.role === "assistant" && message.citations?.length`,
    render `CitedMarkdown content={dedupParagraphs(message.content)} citations={message.citations}`; ELSE keep the EXISTING
    `MarkdownRenderer` call byte-identical (assistant → dedupParagraphs, user → raw). The `StreamingNarration` branch (the
    `isMessageStreaming && tool_calls && assistant` case) stays UNTOUCHED — it never receives markers (D-05, body streams calm).
    Pass a `hasMarkers`/`defaultOpen` signal (true when `message.citations?.length` AND the settled content actually contains a
    valid in-range marker) into `CitationList` so the footer opens-by-default only when markers exist (D-06/D-07). Do NOT touch
    `dedupParagraphs`, the streaming path, or any unrelated MessageItem behavior (G-5 additive-only). Extend `MessageItem.test.tsx`
    with the non-regression assertions: a non-cited assistant message and a user message render through `MarkdownRenderer`
    byte-identically (no `<sup>` markers, no CitedMarkdown), and a cited assistant message routes to CitedMarkdown.
    VERIFY (no edit) `StreamsProvider.tsx`: the terminal reconcile (L1988–2011) and reducer invariant (L367–373) are unchanged —
    re-run the existing provider tests; do NOT move the content swap into onCitations.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__/MessageItem.test.tsx src/providers/__tests__</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/chat/__tests__/MessageItem.test.tsx src/providers/__tests__` → all pass (existing provider tests stay green = StreamsProvider non-regression).
    - A new MessageItem test asserts a non-cited assistant message and a user message render via MarkdownRenderer with NO `<sup>`/CitedMarkdown (G-5 byte-identical, D-14).
    - A new MessageItem test asserts a message with `citations?.length` routes to CitedMarkdown.
    - `git diff` shows `StreamsProvider.tsx` is NOT modified; the StreamingNarration branch in MessageItem is unchanged.
    - `cd frontend && npx vite build` → exit 0, 0 net-new tsc errors vs baseline.
  </acceptance_criteria>
  <done>MessageItem routes cited assistant messages to CitedMarkdown and everything else to the byte-identical MarkdownRenderer; the streaming/reducer path is verified unchanged; G-5 non-regression tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model answer HTML → marker host DOM | the sanitized answer HTML hosts injected marker nodes; injection must not open an XSS or attribute-strip hole |
| shared render path (MarkdownRenderer / StreamingNarration / dedupParagraphs / StreamsProvider) | G-5 hot files under active churn — must stay byte-identical where unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-05-01 | Tampering / XSS (V5) | marker host (model emits `<script>`/HTML in a claim) | mitigate | `DOMPurify.sanitize` already runs on the parsed HTML; markers are OWNED `document.createElement` `<sup>` nodes with attributes set programmatically — never re-parsed from model text (Pitfall 3) |
| T-153-05-02 | Spoofing (fabricated attribution) | marker range-check | mitigate | only `[n]` with `n ∈ [1, citations.length]` upgrade, keyed 1:1 to `citations[n-1]`; backend already stripped non-members (D-02) — belt-and-suspenders (Pitfall 6/14) |
| T-153-05-03 | Tampering (Pitfall 2) | `[n]` in code/links | mitigate | TreeWalker skips `code`/`pre`/`a` ancestors; `[label](url)` links are untouched |
| T-153-05-04 | Tampering (G-5 shared-path regression) | MarkdownRenderer / StreamingNarration / dedupParagraphs / StreamsProvider | mitigate | additive branch only; non-cited assistant + user messages byte-identical; streaming path never given markers; StreamsProvider unedited; non-regression tests re-run (D-14/G-5) |
| T-153-05-05 | Information Disclosure | marker/peek "Open document" | mitigate | routes through `useCitationNav().openDocument` → existing owner-scoped fetch (T-153-02-01) |
| T-153-05-SC | Tampering | npm installs | accept | NO package installs (reuses vendored marked/dompurify/react-dom) — Legitimacy Gate not triggered |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/chat/__tests__/CitedMarkdown.test.tsx src/components/chat/__tests__/MessageItem.test.tsx src/providers/__tests__` → all green.
- `cd frontend && npx vite build` → exit 0, 0 net-new tsc errors vs baseline.
- `git diff` for this plan touches only the four declared files; `StreamsProvider.tsx` NOT modified.
- Non-cited assistant + user messages render byte-identical (G-5); markers are owned createElement nodes; out-of-range/code-span `[n]` stay literal.
- (Live, post-merge) Operator restarts uvicorn; the SC#10 4-axis UAT in 153-VALIDATION.md is executed before /gsd:verify-work.
</verification>

<success_criteria>
- Inline markers render keyed 1:1 to the real retrieval set, attach on settle, click through to the peek + flash the footer row (SC#1/SC#2), only valid in-range markers render (D-07), and the shared render/streaming path stays byte-identical (SC#4/D-14/G-5).
</success_criteria>

<output>
Create `.planning/phases/153-inline-citations/153-05-SUMMARY.md` when done.
</output>
