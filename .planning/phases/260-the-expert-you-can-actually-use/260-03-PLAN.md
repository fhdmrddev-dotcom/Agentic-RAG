---
phase: 260-the-expert-you-can-actually-use
plan: 03
type: execute
wave: 3
depends_on: ["260-01", "260-02"]
files_modified:
  - frontend/src/components/chat/ExpertSpotlightCard.tsx
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/chat/__tests__/ExpertSpotlightCard.test.tsx
  - backend/tests/unit/test_260_financial_analyzer_conversation.py
autonomous: true
requirements: [PACK-03, PACK-05]

must_haves:
  truths:
    - "Stream announcement renders ExpertSpotlightCard with glowing icon gem, identity badges, and zero lecturing prose (D-260-06, 260-UI-SPEC §2.3)."
    - "ExpertSpotlightCard displays 3 large, visual Action Tiles (📈 Q3 Revenue Growth YoY, ⚖️ Gross Margin Comparison, 💵 Operating Cash Flow) (PACK-03, 260-UI-SPEC §2.4)."
    - "Clicking an Action Tile immediately dispatches the prompt text and triggers an agent run with 1-click execution (D-260-07, PACK-03)."
    - "End-to-end conversation test in test_260_financial_analyzer_conversation.py verifies Financial Analyzer answers earnings/margin query from seeded 10-K document with citations (PACK-05)."
    - "Financial Analyzer calculation skill executes ratio calculations accurately based on document figures (PACK-05)."
    - "Financial Analyzer honestly refuses an out-of-scope query (e.g. employee vacation policy) without hallucinating (PACK-05, Phase 185 graded governance)."
  artifacts:
    - path: "frontend/src/components/chat/ExpertSpotlightCard.tsx"
      provides: "Hero visual spotlight card with Action Tiles grid"
      exports: ["ExpertSpotlightCard"]
    - path: "frontend/src/components/chat/__tests__/ExpertSpotlightCard.test.tsx"
      provides: "Vitest tests for spotlight card rendering, action tile formatting, and 1-click prompt dispatch"
    - path: "backend/tests/unit/test_260_financial_analyzer_conversation.py"
      provides: "Live conversation proof for PACK-05 (answers from seeded documents, calculates ratios, refuses out of scope)"
  key_links:
    - from: "frontend/src/components/chat/ChatArea.tsx"
      to: "frontend/src/components/chat/ExpertSpotlightCard.tsx"
      via: "rendered in message stream when expert is active"
      pattern: "<ExpertSpotlightCard"
    - from: "backend/tests/unit/test_260_financial_analyzer_conversation.py"
      to: "supabase/migrations/188_expert_chat_scoping.sql"
      via: "reads seeded 10-K document and folder"
      pattern: "00000000-0000-0000-0000-000000000260"
---

<objective>
Deliver the onboarding affordance (`ExpertSpotlightCard.tsx` with 3 visual Action Tiles) in `ChatArea.tsx`, 1-click prompt execution, and the end-to-end live conversation proof slice for the Financial Analyzer (`PACK-03`, `PACK-05`).

Purpose: PACK-03 (visual onboarding affordance) and PACK-05 (first-party Financial Analyzer answering from real documents or refusing, proven end-to-end).
Output: `ExpertSpotlightCard.tsx`, `ChatArea.tsx` integration, Vitest component tests, and `test_260_financial_analyzer_conversation.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

Base SHA to assert: commit produced by plan 260-02.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/260-the-expert-you-can-actually-use/260-CONTEXT.md
@.planning/phases/260-the-expert-you-can-actually-use/260-UI-SPEC.md
@frontend/src/components/chat/ChatArea.tsx
@backend/app/services/run_producer.py
@backend/app/services/agent_loop.py
</context>

<tasks>

<task type="auto" name="1. Author ExpertSpotlightCard leaf component">
  <action>
    Create `frontend/src/components/chat/ExpertSpotlightCard.tsx` per 260-UI-SPEC §2.3 and §2.4:
    - Glassmorphic card styling:
      `bg-gradient-to-br from-violet-950/40 to-slate-900/60 border border-violet-500/25 rounded-2xl p-5 shadow-xl shadow-black/40 animate-in fade-in slide-in-from-bottom-2 duration-300`
    - Top row:
      - Icon gem: luminous violet/indigo gradient square (`w-10 h-10 rounded-xl`) with icon `📊`
      - Name: `text-base font-bold text-foreground`
      - Scope tags:
        - `[📁 SEC Filings & Reports]`
        - `[🧰 ratio_calculator]`
        - `[Restricted]` (`bg-rose-500/10 border border-rose-500/30 text-rose-300 text-[11px] rounded-full px-2 py-0.5 font-semibold`)
      - Top-right dismiss `✕` button
    - Action Tiles Grid:
      - 3-column responsive grid (`grid grid-cols-1 md:grid-cols-3 gap-2.5 mt-3`)
      - Tiles populated from `expert.prompt_suggestions`:
        1. `📈 Q3 Revenue Growth YoY`
        2. `⚖️ Gross Margin Comparison`
        3. `💵 Operating Cash Flow`
      - Each tile has hover effect (`hover:-translate-y-0.5 hover:border-violet-400/40 hover:bg-violet-500/10`)
      - Tapping triggers `onSelectPrompt(tile.prompt)`.
  </action>
  <verify>
    <command>npx tsc -p frontend/tsconfig.app.json --noEmit</command>
  </verify>
</task>

<task type="auto" name="2. Mount in ChatArea.tsx with 1-click execution">
  <action>
    In `frontend/src/components/chat/ChatArea.tsx`:
    - When `activeExpert` is present on the thread:
      - If thread messages are empty, or if an expert invitation event occurred, render `<ExpertSpotlightCard expert={activeExpert} onSelectPrompt={handlePromptSelect} onDismiss={handleDismissExpert} />`.
      - In `handlePromptSelect(prompt: string)`:
        Immediately call `sendMessage(prompt)` to send the user message and start the agent run without opening or editing composer (`D-260-07`, 1-click execution).
  </action>
  <verify>
    <command>npx tsc -p frontend/tsconfig.app.json --noEmit</command>
  </verify>
</task>

<task type="auto" name="3. Deliver Vitest component tests">
  <action>
    Create `frontend/src/components/chat/__tests__/ExpertSpotlightCard.test.tsx`:
    1. Verify `ExpertSpotlightCard` renders expert name and scope tags without didactic lecturing text.
    2. Verify 3 Action Tiles render with correct titles and prompt previews.
    3. Verify clicking an Action Tile invokes `onSelectPrompt` with the exact prompt string.
    4. Verify clicking dismiss `✕` invokes `onDismiss`.
  </action>
  <verify>
    <command>npx vitest run frontend/src/components/chat/__tests__/ExpertSpotlightCard.test.tsx</command>
  </verify>
</task>

<task type="auto" name="4. Deliver end-to-end conversation proof (PACK-05)">
  <action>
    Create `backend/tests/unit/test_260_financial_analyzer_conversation.py`:
    Drive a live conversation against the seeded 10-K document:
    1. **Turn 1 (Document Citation Grounding)**:
       - User asks: "What was our Q3 revenue and YoY growth?"
       - Verify agent loop queries the scoped financial folder (`00000000-0000-0000-0000-000000000260`).
       - Verify response content contains "$124.5M" and "+18.2%" with citation to the seeded document fixture.
    2. **Turn 2 (Ratio Calculation)**:
       - User asks: "Calculate gross margin and EBITDA margin."
       - Verify response computes 64.2% and 30.8% with arithmetic steps.
    3. **Turn 3 (Honest Out-of-Scope Refusal)**:
       - User asks: "What is the employee vacation rollover policy?"
       - Verify search in scoped folder returns zero matches and the Financial Analyzer refuses honestly ("I am restricted to financial documents and cannot find vacation policy in the scoped filings") without hallucinating general policy.
  </action>
  <verify>
    <command>pytest backend/tests/unit/test_260_financial_analyzer_conversation.py -v</command>
  </verify>
</task>

</tasks>

<threat_model>
ASVS Level 1 Threat Assessment:
- **T-260-06 (Hallucination Fail-Open on Out-of-Scope Queries)**: When asked about non-financial topics, a restricted expert hallucinates answers rather than refusing.
  *Mitigation*: Tested in Turn 3 of `test_260_financial_analyzer_conversation.py`; restricted scope bounds folder retrieval to financial documents only and prompt priming mandates explicit refusal when documents lack answers.
- **T-260-07 (Prompt Injection via Action Tile Title)**: Custom expert prompt suggestions containing prompt injection attacks.
  *Mitigation*: Prompts are passed as standard user messages through existing message sanitize and validation pipeline.
</threat_model>
