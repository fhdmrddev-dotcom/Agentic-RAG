---
phase: 121-one-front-door-for-workflows-ia
plan: 02
type: execute
wave: 2
depends_on: [121-01]
files_modified:
  - frontend/src/components/chat/__tests__/ChatAreaMode.test.tsx
  - frontend/src/components/chat/__tests__/ChatAreaBanner.test.tsx
  - frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx
autonomous: true
requirements: [IA-01]
must_haves:
  truths:
    - "An automated test asserts the composer has exactly 2 pills: workflow-mode-selector is null, workflow-picker is null, agent-mode-selector present, Model pill present (SC#1)."
    - "An automated test asserts the reachable Cancel survives: with disabled/streaming, composer-stop renders and clicking it calls onStop (Cancel-reachability, D-01)."
    - "An automated test asserts the SC#3 preserve: with workflowLocked, the composer textarea is disabled + shows the 'Workflow running — Cancel to switch back' placeholder and Send is gated."
    - "The existing 409 lock-banner test (ChatAreaBanner test b) stays GREEN byte-unchanged (SC#3 — the 409 signal)."
    - "An automated reconcile-lock test asserts getThreadWorkflow → locked:true disables the composer on mount and locked:false leaves it enabled (SC#3 reconcile-on-mount, server truth)."
    - "An automated SC#2 launch test asserts the Workflows-page Run → ChatLayout.doRun calls createThread + postMessage with workflowDefinitionId + navigates to chat (launch capability NOT removed, D-02/D-03 always-new-thread)."
    - "RunCard.timer.test.tsx and RunCard.test.tsx are NOT edited and stay GREEN as no-regression evidence."
  artifacts:
    - path: "frontend/src/components/chat/__tests__/ChatAreaMode.test.tsx"
      provides: "rewritten 2-pill / Cancel-reachability / workflowLocked-preserve assertions (replaces the 3 finding-#5 pill-label tests)"
      contains: "queryByTestId(\"workflow-mode-selector\")"
    - path: "frontend/src/components/chat/__tests__/ChatAreaBanner.test.tsx"
      provides: "extended with a reconcile-lock case (getThreadWorkflow locked:true → composer disabled); the 409 test b kept unchanged"
      contains: "workflow-lock-error-banner"
    - path: "frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx"
      provides: "new SC#2 integration test — page Run → doRun → createThread + postMessage({workflowDefinitionId}) + onNavigate('chat')"
      contains: "workflowDefinitionId"
  key_links:
    - from: "frontend/src/components/chat/__tests__/ChatAreaMode.test.tsx"
      to: "frontend/src/components/chat/MessageInput.tsx"
      via: "render(<MessageInput/>) in isolation, query composer testids"
      pattern: "queryByTestId\\(\"workflow-picker\"\\)"
    - from: "frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx"
      to: "frontend/src/components/layout/ChatLayout.tsx"
      via: "render(<ChatLayout activeView='workflows'/>) → click Run → assert createThread+postMessage"
      pattern: "postMessage"
---

<objective>
Make the Phase 121 success criteria observable in CI: rewrite the three stale `ChatAreaMode.test.tsx` tests (which currently assert the removed Deep/Harness pill EXISTS) into SC#1 (2-pill) + Cancel-reachability (D-01) + SC#3-preserve assertions; extend `ChatAreaBanner.test.tsx` with a reconcile-lock case (the 409 test stays byte-unchanged); and add a new `ChatLayout` integration test proving the surviving launch path (SC#2). After this plan, `ChatAreaMode.test.tsx` flips from RED (post-Plan-01) to GREEN.

Purpose: bind every Phase 121 SC to an automated Vitest oracle (per VALIDATION.md) so verify-work has machine-checkable evidence; leave the SC#10 4-axis cross-provider UAT to VALIDATION.md (manual).
Output: rewritten `ChatAreaMode.test.tsx`, extended `ChatAreaBanner.test.tsx`, new `frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — the test plan and the oracles
@.planning/phases/121-one-front-door-for-workflows-ia/121-VALIDATION.md
@.planning/phases/121-one-front-door-for-workflows-ia/121-PATTERNS.md
@.planning/phases/121-one-front-door-for-workflows-ia/121-RESEARCH.md

<interfaces>
<!-- Test harness contracts extracted from live code this session. Use these directly. -->

ChatAreaMode.test.tsx (CURRENT — to REWRITE): renders MessageInput IN ISOLATION
  (`import { MessageInput } from "../MessageInput"`, `render(<MessageInput .../>)`, `afterEach(cleanup)`).
  Current helper `modePill()` = `screen.getByTestId("workflow-mode-selector")` — DELETE it (the testid is gone).
  The 3 current tests assert the Deep/Harness label — all 3 are obsolete (finding #5 died by construction).
  Rewrite render props must NOT include displayedMode/workflowMode/onWorkflowModeChange/selectedWorkflowId/
  onWorkflowSelect/publishedWorkflows (removed from Props in Plan 01 — TS will error). KEEP onAgentModeChange
  so agent-mode-selector renders. A minimal Model-pill render needs providers (length>1 to show the selector,
  OR assert the single-provider static label) — see WorkflowsPage.test.tsx provider mock shape:
    providers={[{id:"openai",name:"OpenAI",models:["gpt-test"],is_active:true}]} selectedProvider="openai"
    models={["gpt-test"]} selectedModel="gpt-test".
  Surviving testids to assert: agent-mode-selector (present), composer-stop (present when disabled),
    workflow-mode-selector (null), workflow-picker (null). Locked-preserve: pass workflowLocked → the textarea
    placeholder is "Workflow running — Cancel to switch back" and the textarea is disabled.

ChatAreaBanner.test.tsx (EXISTING — to EXTEND, do NOT rewrite): mounts the REAL ChatArea inside the REAL
  StreamsProvider; mocks @/hooks/useMessages + @/lib/api (getProviders, listPublishedWorkflows→[],
  getThreadWorkflow→{mode:"deep",active_workflow_run_id:null}) + @/lib/supabase auth.
  Test "b" (L130-141) asserts a 409 ApiError → workflow-lock-error-banner + no Retry — MUST stay byte-unchanged.
  ADD a case that overrides getThreadWorkflow to resolve
    { locked:true, lock_is_stale:false, active_workflow_run_id:"run-1", cap_paused:false, continues_remaining:0 }
  then after mount-reconcile the composer textarea is `disabled` + shows the running placeholder; inverse
  (locked:false) leaves it enabled. NOTE: the `listPublishedWorkflows` mock (L45) becomes dead after Plan 01
  removed ChatArea's fetch effect — leaving it is harmless; trimming it is optional hygiene. If you trim it,
  do so AFTER confirming ChatArea no longer imports the symbol (it doesn't, post-Plan-01).

ChatLayout.tsx (for the NEW SC#2 test — render, do NOT edit): the launch route is doRun (L83-92):
    createThread(def.name) → postMessage(thread.id, kickoff, { workflowDefinitionId: def.id })
    → loadThreads() → selectThread(thread) → onNavigate("chat").
  doRun is passed to WorkflowsPage as `onLaunch`. ChatLayout consumes:
    - @/lib/api: createThread, postMessage  (mock these — assert the call shapes)
    - @/hooks/useThreads (loadThreads/selectThread/selectedThread/threads/...), @/hooks/useFolders, @/hooks/useTheme (mock)
    - props: activeView (set to "workflows" to render WorkflowsPage), onNavigate (a vi.fn() — assert "chat")
  WorkflowsPage already proves onLaunch(def, kickoff) fires from the page (WorkflowsPage.test.tsx:269-279):
    click published-run → fill run-kickoff textarea → click run-confirm. Reuse that exact interaction to drive doRun.
  WorkflowsPage's own api seam (listPublishedWorkflows/listDraftWorkflows/listFolders/listSkills) must also be
  mocked so the page mounts and renders a published-card (mirror WorkflowsPage.test.tsx beforeEach).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ChatAreaMode.test.tsx (SC#1 + Cancel-reachability + SC#3-preserve) and extend ChatAreaBanner.test.tsx (reconcile-lock)</name>
  <files>frontend/src/components/chat/__tests__/ChatAreaMode.test.tsx, frontend/src/components/chat/__tests__/ChatAreaBanner.test.tsx</files>
  <behavior>
    ChatAreaMode.test.tsx (render MessageInput in isolation):
    - Test: with a normal (unlocked) composer render, `queryByTestId("workflow-mode-selector")` is null AND `queryByTestId("workflow-picker")` is null AND `getByTestId("agent-mode-selector")` is in the document; the Model pill renders (provider/model label or selector present).
    - Test: with `disabled` (streaming), `getByTestId("composer-stop")` is present; `fireEvent.click` on it calls the `onStop` spy (the post-removal Cancel — D-01).
    - Test: with `workflowLocked`, the textarea placeholder is "Workflow running — Cancel to switch back" and the textarea is `disabled`.
    ChatAreaBanner.test.tsx (real ChatArea in real StreamsProvider):
    - Existing test b (409 → workflow-lock-error-banner + no Retry) stays GREEN unchanged.
    - New test: getThreadWorkflow mock resolves locked:true (active_workflow_run_id:"run-1") → after mount-reconcile the composer textarea is disabled + running placeholder; a paired locked:false render leaves it enabled.
  </behavior>
  <read_first>
    - frontend/src/components/chat/__tests__/ChatAreaMode.test.tsx (full — the modePill helper + 3 tests to replace, the render-prop shape)
    - frontend/src/components/chat/__tests__/ChatAreaBanner.test.tsx (full — the mock seam L36-51, renderChatArea L79-89, the 4 banner tests incl. test b)
    - frontend/src/components/chat/MessageInput.tsx (post-Plan-01: the surviving agent-mode-selector, composer-stop, the locked placeholder L210-212 — confirm the exact placeholder string)
    - .planning/phases/121-one-front-door-for-workflows-ia/121-PATTERNS.md § "Edit Op 4 (a)" rewrite target + "(b)" extend seam (the getThreadWorkflow locked:true mock) + "(c)" RunCard DO-NOT-TOUCH
    - .planning/phases/121-one-front-door-for-workflows-ia/121-VALIDATION.md § "Oracles" (verbatim assertions)
  </read_first>
  <action>
    Rewrite `ChatAreaMode.test.tsx`: delete the `modePill()` helper and all 3 finding-#5 tests; rewrite the file header doc-comment to describe the 2-pill removal (not finding #5). Add three tests per the behavior block, rendering `<MessageInput>` in isolation (mirror the existing harness — `afterEach(cleanup)`). Use the provider mock shape from the interfaces block so the Model pill renders; include `onAgentModeChange` so `agent-mode-selector` renders. Do NOT pass any removed prop (`displayedMode`/`workflowMode`/`onWorkflowModeChange`/`selectedWorkflowId`/`onWorkflowSelect`/`publishedWorkflows`) — they no longer exist on `Props`. For the locked-preserve test, query the textarea by its placeholder/role and assert `disabled` + the exact "Workflow running — Cancel to switch back" string.
    Extend `ChatAreaBanner.test.tsx`: leave test b (the 409 case) byte-unchanged. Add ONE new `it(...)` that overrides `getThreadWorkflow` (use `vi.mocked(getThreadWorkflow).mockResolvedValueOnce({ locked:true, lock_is_stale:false, active_workflow_run_id:"run-1", cap_paused:false, continues_remaining:0 })` or re-mock per the file's pattern) → render → `waitFor` the composer textarea to be `disabled` with the running placeholder; then a paired assertion that the default `locked:false` mock leaves the textarea enabled with "Ask anything…". Do not modify the `useMessages`/`supabase` mocks. (The `listPublishedWorkflows` mock is now dead but harmless — leave it unless you confirm ChatArea no longer imports it, then trimming is optional.)
    Do NOT touch `RunCard.timer.test.tsx` or `RunCard.test.tsx`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__/ChatAreaMode.test.tsx src/components/chat/__tests__/ChatAreaBanner.test.tsx</automated>
  </verify>
  <done>`ChatAreaMode.test.tsx` and `ChatAreaBanner.test.tsx` both exit 0. `ChatAreaMode` asserts `workflow-mode-selector`/`workflow-picker` are null, `agent-mode-selector` present, `composer-stop` reachable + clickable, and the `workflowLocked` disabled+placeholder preserve. `ChatAreaBanner` test b is unchanged and green; the new reconcile-lock case passes. `RunCard.timer.test.tsx`/`RunCard.test.tsx` not modified (`git diff --quiet` on both).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New SC#2 launch integration test — ChatLayoutLaunch.test.tsx (page Run → doRun → createThread + postMessage + navigate)</name>
  <files>frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx</files>
  <behavior>
    Render `<ChatLayout activeView="workflows" onNavigate={spy} ... />` with the api + hook seams mocked.
    Drive the WorkflowsPage launch: click `published-run`, fill `run-kickoff`, click `run-confirm`.
    Assert the surviving launch wiring fired:
    - `createThread` called with the published def's name.
    - `postMessage` called with the new thread id, the kickoff text, and an options object containing `workflowDefinitionId` equal to the published def's id (D-02 live route; D-03 always a NEW thread).
    - `onNavigate` called with "chat".
    No assertion on the removed in-chat picker (it is gone — covered by Task 1 / SC#1).
  </behavior>
  <read_first>
    - frontend/src/components/layout/ChatLayout.tsx (L1-104 — imports L28 createThread/postMessage, useThreads L39-49, doRun L83-92, the activeView render switch for "workflows" → <WorkflowsPage onLaunch={doRun} .../>)
    - frontend/src/pages/WorkflowsPage.test.tsx (full — the api mock via vi.hoisted, the beforeEach published/draft/folders/skills mocks, and the Run interaction at L269-279 + the in-flight guard pattern; reuse the mock shapes and the click→fill→confirm flow)
    - frontend/src/pages/WorkflowsPage.tsx (only as needed to confirm the published-run / run-kickoff / run-confirm testids and that onLaunch is the Run handler)
    - .planning/phases/121-one-front-door-for-workflows-ia/121-VALIDATION.md § "SC#2" oracle + "Wave 0 Requirements" (the ChatLayout doRun integration-test option)
    - .planning/phases/121-one-front-door-for-workflows-ia/121-RESEARCH.md § "Pattern 2: The launch path is page/panel-driven and already complete"
  </read_first>
  <action>
    Create `frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx`. Mock `@/lib/api` to expose `createThread` (resolves a thread `{id:"thread-new", title:...}`), `postMessage` (resolves), AND the symbols WorkflowsPage needs to mount (`listPublishedWorkflows` → one published def, `listDraftWorkflows` → [], `createWorkflowDraft`, `generateWorkflow`, `updateWorkflowDraft`, `publishWorkflow`, `listFolders` → [], `listSkills` → []) — mirror `WorkflowsPage.test.tsx`'s vi.hoisted mock block. Mock `@/hooks/useThreads` (return `loadThreads`, `selectThread`, `selectedThread:null`, `threads:[]`, and the other fns ChatLayout destructures — `newThread`/`deleteThread`/`renameThread`/`updateThreadTitle`/`loading`), `@/hooks/useFolders` (`folders:[]`), and `@/hooks/useTheme` (`theme`/`toggleTheme`). Render `<ChatLayout onSignOut={vi.fn()} activeView="workflows" onNavigate={onNavigate} prefillMessage={null} onSetPrefillMessage={vi.fn()} />`. Find a `published-card`, click its `published-run`, fill the `run-kickoff` textarea with a probe string, click `run-confirm`. Assert: `createThread` called with the def name; `postMessage` called with `("thread-new", <probe>, { workflowDefinitionId: <def.id> })`; `onNavigate` called with `"chat"`. Do NOT edit `ChatLayout.tsx`, `WorkflowsPage.tsx`, or any production file. If the WorkspacePanel/NavPanel children make the render heavy or hit un-mocked seams, mock those child modules to trivial stubs — keep the test scoped to the doRun wiring (the assertion is on the api/navigate calls, not panel render).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/__tests__/ChatLayoutLaunch.test.tsx</automated>
  </verify>
  <done>`ChatLayoutLaunch.test.tsx` exits 0. It proves the page Run path calls `createThread` + `postMessage` with `{ workflowDefinitionId }` + `onNavigate("chat")` (SC#2 launch capability preserved). No production file edited (`git diff --name-only` shows only the new test file for this task).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → component under test | Tests render real components / mock the api+store seams; no real network, no real auth. No production trust boundary is crossed by tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | Elevation of Privilege | 409 lock-banner test (ChatAreaBanner b) | mitigate (preserve) | The SC#3 409 signal is locked by keeping test b byte-unchanged GREEN — the server-enforced lock authority remains the asserted contract (client UI removal cannot weaken it). |
| T-121-02 | Tampering | reconcile-lock test (per-thread lock) | mitigate (preserve) | The new reconcile-lock case asserts the owner-scoped `getThreadWorkflow` → per-thread disable; combined with the parallel-thread SC#10 manual axis it guards against a cross-thread lock leak being introduced by the removal. |
| T-121-03 | Injection / XSS | N/A | accept | Tests add no render path; the removed pills' (already-nil) XSS surface is gone. No new innerHTML. |
| T-121-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added (vitest/RTL already present — RESEARCH § Environment Availability). No supply-chain surface. |

**ASVS level:** L1. **Block on:** `high` severity — none open. This plan is test-only; it asserts the preserved server authority rather than changing it.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/chat/__tests__/ChatAreaMode.test.tsx src/components/chat/__tests__/ChatAreaBanner.test.tsx src/components/layout/__tests__/ChatLayoutLaunch.test.tsx` all GREEN.
- `cd frontend && npm run test` (full Vitest suite) GREEN — including the now-rewritten `ChatAreaMode.test.tsx` (RED after Plan 01, GREEN after this plan) and the untouched `RunCard.timer.test.tsx`/`RunCard.test.tsx`.
- `git diff --stat` shows changes ONLY under `frontend/src/components/chat/__tests__/` and `frontend/src/components/layout/__tests__/` — ZERO production-source edits, ZERO `backend/`, ZERO migration (D-06/G-5).
- `cd frontend && git diff --quiet -- src/components/chat/RunCard.timer.test.tsx src/components/chat/RunCard.test.tsx && echo "RUNCARD-UNTOUCHED"` prints the marker.
</verification>

<success_criteria>
- SC#1 oracle GREEN: composer has exactly 2 pills, both removed testids null (`ChatAreaMode.test.tsx`).
- Cancel-reachability (D-01) GREEN: `composer-stop` renders + click calls `onStop`.
- SC#3 GREEN: 409 banner test b unchanged; the new reconcile-lock case + the `workflowLocked` disabled+placeholder preserve all pass.
- SC#2 GREEN: the new `ChatLayoutLaunch.test.tsx` proves Run → `createThread` + `postMessage({workflowDefinitionId})` + `onNavigate("chat")` (launch capability preserved; D-02/D-03).
- `RunCard` tests untouched + GREEN (no-regression evidence). Test-only; no production/backend/migration change.
</success_criteria>

<output>
Create `.planning/phases/121-one-front-door-for-workflows-ia/121-02-SUMMARY.md` when done.
</output>
