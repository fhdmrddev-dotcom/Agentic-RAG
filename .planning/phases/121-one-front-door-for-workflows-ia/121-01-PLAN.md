---
phase: 121-one-front-door-for-workflows-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/components/chat/ChatArea.tsx
autonomous: true
requirements: [IA-01]
must_haves:
  truths:
    - "The composer renders exactly two interactive mode pills (Model + General/Explorer); the Deep/Harness toggle and the in-chat workflow picker are gone (SC#1, D-02)."
    - "The General/Explorer pill (agent-mode-selector) and the Model/Provider pill survive the removal unchanged and render independently of any removed symbol (SC#1)."
    - "A workflow-locked thread still disables the composer textarea + shows the 'Workflow running — Cancel to switch back' placeholder + gates Send (SC#3, D-01 — workflowLocked KEPT)."
    - "While a locked thread streams, the composer-stop red Stop button still renders and routes onStop → stopStreaming → cancelRun (the reachable Cancel; D-01 — no new composer chrome added)."
    - "The mount reconcile, the 409 lock banner, and the workflowLocked derivation are byte-unchanged behavior (SC#3 preserve list)."
    - "The Workflows-page launch path (ChatLayout.doRun → postMessage with workflowDefinitionId) is untouched and remains the live launch route — only the in-chat kickoffWorkflowId staging in ChatArea.handleSend dies as harmless dead code (SC#2, D-02)."
    - "No backend file and no migration is touched — frontend-only (D-06 / G-5)."
  artifacts:
    - path: "frontend/src/components/chat/MessageInput.tsx"
      provides: "2-pill composer (workflow-mode-selector + workflow-picker + their 6 props + WorkflowOption + labelMode removed; agent-mode-selector, Model pill, workflowLocked gating, composer-stop KEPT)"
      contains: "data-testid=\"agent-mode-selector\""
    - path: "frontend/src/components/chat/ChatArea.tsx"
      provides: "composer wiring with the 6 removed props no longer passed, the picker-feed useState + listPublishedWorkflows fetch effect removed, the dead kickoffWorkflowId branch removed; reconcile/lock/409 banner KEPT"
      contains: "useWorkflowLockForThread"
  key_links:
    - from: "frontend/src/components/chat/ChatArea.tsx"
      to: "frontend/src/components/chat/MessageInput.tsx"
      via: "prop pass-through (workflowLocked KEEP; 6 mode/picker props REMOVED)"
      pattern: "workflowLocked=\\{workflowLocked\\}"
    - from: "frontend/src/components/chat/ChatArea.tsx"
      to: "GET /threads/{id}/workflow"
      via: "getThreadWorkflow mount reconcile (server-truth lock, preserved)"
      pattern: "getThreadWorkflow"
---

<objective>
Remove the two composer mode affordances — the Deep/Harness toggle (`data-testid="workflow-mode-selector"`) and the in-chat published-workflow picker (`data-testid="workflow-picker"`) — and their now-orphaned props/handlers/state, leaving a **2-pill composer** (Model + General/Explorer). Preserve the per-thread workflow lock, the 409 banner, the disabled-composer placeholder, the mount reconcile, and the `composer-stop` Stop button exactly (SC#1 + SC#3 + Cancel-reachability; D-01/D-02/D-03/D-04/D-06).

Purpose: make the Workflows page the single front door for launching workflows (IA-01) by deleting the redundant in-chat launch surface, while the Harness↔Deep lock / 409 / reconcile behavior stays byte-identical and the server launch route stays live.
Output: edited `MessageInput.tsx` (2 controls + 6 props + 1 interface + `labelMode` + 2 icon imports removed) and `ChatArea.tsx` (3 useState + 1 fetch effect + the dead `kickoffWorkflowId` branch + 6 prop pass-throughs removed; reconcile/lock/banner kept).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — read the seams before editing
@.planning/phases/121-one-front-door-for-workflows-ia/121-RESEARCH.md
@.planning/phases/121-one-front-door-for-workflows-ia/121-PATTERNS.md
@.planning/phases/121-one-front-door-for-workflows-ia/121-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from live code this session (line numbers verified). -->
<!-- DO NOT explore the codebase to rediscover these — they are the removal seams. -->

MessageInput.tsx — the composer (props interface L28-73):
  REMOVE these 6 props (interleaved with the workflowLocked KEEP prop):
    workflowMode?: "deep" | "harness"                         (L54)
    onWorkflowModeChange?: (mode: "deep" | "harness") => void (L55)
    displayedMode?: "deep" | "harness"                        (L62)
    publishedWorkflows?: WorkflowOption[]                     (L63-64)
    selectedWorkflowId?: string | null                       (L66)
    onWorkflowSelect?: (workflowId: string) => void          (L67)
  KEEP (do NOT cut):
    workflowLocked?: boolean                                 (L68-72) — gates disable+placeholder+Send
    agentMode / onAgentModeChange                            (L43-44) — drives the surviving General/Explorer pill
  REMOVE the WorkflowOption interface (L21-26) — only used by publishedWorkflows.
  REMOVE in the destructure (L95-117): workflowMode (L110), onWorkflowModeChange (L111),
    displayedMode (L112), publishedWorkflows (L113), selectedWorkflowId (L114), onWorkflowSelect (L115).
    KEEP workflowLocked (L116).
  REMOVE the labelMode derivation (L120: `const labelMode = displayedMode ?? workflowMode`).
  REMOVE the Deep/Harness toggle block (L374-425, gated `{onWorkflowModeChange && (...)}`, testid workflow-mode-selector).
  REMOVE the workflow picker block (L427-477, gated `{onWorkflowModeChange && workflowMode === "harness" && !workflowLocked && (...)}`, testid workflow-picker + workflow-option-${slug}).
  Icon import line L4: `import { ArrowUp, ChevronDown, Compass, Cpu, Layers, Square, Sparkles, Workflow } from "lucide-react"`.
    REMOVE `Workflow` and `Sparkles` (only the removed controls use them). KEEP Layers (Provider selector L443/...), Compass/Cpu/ChevronDown/ArrowUp/Square.
  KEEP byte-identical: agent-mode-selector (L329-372), Provider+Model selectors (L224-324),
    handleSend lock-gate (L161 `if (!trimmed || disabled || workflowLocked) return`), canSend (L179),
    locked placeholder/title/disabled (L210-212), composer-stop (L487-497), per-thread drafts (L83-93,L124-140).

ChatArea.tsx — the thread container (composer wiring):
  REMOVE the 3 picker/toggle useState (L70-72):
    const [workflowMode, setWorkflowMode] = useState<"deep" | "harness">("deep")   (L70)
    const [publishedWorkflows, setPublishedWorkflows] = useState<PublishedWorkflow[]>([])  (L71)
    const [selectedWorkflowId, setSelectedWorkflowId] = useState<string | null>(null)      (L72)
  REMOVE in the thread-switch effect (L140-147): the `setWorkflowMode("deep")` (L145) + `setSelectedWorkflowId(null)` (L146) lines.
    KEEP setAgentMode("default") (L141) + setScopeFolderId(null) (L142).
  REMOVE the listPublishedWorkflows mount fetch effect entirely (L166-171).
  REMOVE the dead kickoff branch in handleSend (L328-356):
    - the `kickoffWorkflowId` const (L331-332),
    - the `if (kickoffWorkflowId) { requestOpenPanel() }` block (L333-340),
    - the 7th positional arg `kickoffWorkflowId,` (L348) → drop it; sendMessage's 7th param becomes `undefined` (omit it),
    - the `if (kickoffWorkflowId) { setSelectedWorkflowId(null) }` block (L350-355),
    - the deps `workflowMode, selectedWorkflowId` from the useCallback deps array (L356).
  REMOVE the 6 prop pass-throughs to <MessageInput> (L409-420):
    workflowMode (L409), onWorkflowModeChange (L410), displayedMode (L417), publishedWorkflows (L418),
    selectedWorkflowId (L419), onWorkflowSelect (L420). KEEP workflowLocked={workflowLocked} (L421).
  REMOVE now-orphaned imports (L18,L20,L25): `listPublishedWorkflows`, `type PublishedWorkflow`, and
    `import { requestOpenPanel } from "@/components/panel/panelOpenSignal"` — ONLY after confirming by grep
    that no other line in the file still references each symbol (requestOpenPanel's sole caller is the deleted L339).
  KEEP byte-identical (the PRESERVE LIST — SC#2/SC#3 proof set):
    workflowLock/workflowLocked derivation (L95-96), the FULL mount reconcile (L178-204),
    the 409 banner + NON_RETRYABLE (L84-86, L519-561), isStreaming (L78), stopStreaming + onStop (L54,L384),
    the normal handleSend thread-create + sendMessage (minus the kickoff branch), workflowLocked pass-through (L421).

ChatLayout.tsx — DO NOT EDIT in this plan. doRun (L83-92) is the surviving launch route
  (createThread → postMessage(..., {workflowDefinitionId}) → loadThreads → selectThread → onNavigate("chat")).
  It does NOT call requestOpenPanel (confirmed) — that import lived only in ChatArea's deleted branch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the two mode controls + their props/state from MessageInput.tsx (the 2-pill composer)</name>
  <files>frontend/src/components/chat/MessageInput.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageInput.tsx (full — the props interface L28-73, destructure L95-117, labelMode L120, agent-mode-selector L329-372, Deep/Harness toggle L374-425, picker L427-477, composer-stop L487-497, handleSend/canSend/placeholder L156-215, icon import L4)
    - .planning/phases/121-one-front-door-for-workflows-ia/121-PATTERNS.md § "Edit Op 1" (the prop-seam classification table) + § "Edit Op 2" (the two control blocks + lint gotcha) + § "Edit Op 3" (the KEEP idiom proving independence)
    - .planning/phases/121-one-front-door-for-workflows-ia/121-RESEARCH.md § "The Removal Seams → MessageInput.tsx" (DELETE/KEEP tables) + § "Cancel-Reachability Verdict"
  </read_first>
  <action>
    Delete from `MessageInput.tsx`, in this top-down order so no symbol dies before its last reader:
    (1) the two render blocks first — the Deep/Harness toggle (the whole `{onWorkflowModeChange && (...)}` block, testid `workflow-mode-selector`, label `labelMode === "harness" ? "Harness" : "Deep"`) and the workflow picker (the whole `{onWorkflowModeChange && workflowMode === "harness" && !workflowLocked && (...)}` block, testid `workflow-picker` + `workflow-option-${w.slug}`);
    (2) the `labelMode` derivation (`const labelMode = displayedMode ?? workflowMode`);
    (3) the 6 props from the `Props` interface — `workflowMode`, `onWorkflowModeChange`, `displayedMode`, `publishedWorkflows`, `selectedWorkflowId`, `onWorkflowSelect` — and the `WorkflowOption` interface (only `publishedWorkflows` referenced it);
    (4) the same 6 names from the function-signature destructure, leaving `workflowLocked = false` in place;
    (5) the now-unused `Workflow` and `Sparkles` lucide-react imports on the import line (KEEP `Layers`/`Compass`/`Cpu`/`ChevronDown`/`ArrowUp`/`Square` — `Layers` is still used by the Provider selector).
    Do NOT touch `workflowLocked` (it sits in the same interface block but gates the textarea disable at L212, the placeholder/title at L210-211, the `handleSend` lock-gate at L161, and `canSend` at L179 — Pitfall 1). Do NOT touch the `agent-mode-selector` block, the Provider/Model selectors, or `composer-stop` (it is the post-removal Cancel per D-01 — adding a new status chip is forbidden). Rewrite the file header doc-comment (currently describes the 2-pill removal is the goal, not finding #5).
  </action>
  <verify>
    <automated>cd frontend && rg -c 'data-testid="workflow-mode-selector"|data-testid="workflow-picker"|workflowMode|onWorkflowModeChange|publishedWorkflows|selectedWorkflowId|onWorkflowSelect|displayedMode|WorkflowOption|labelMode' src/components/chat/MessageInput.tsx; test $? -eq 1 && echo "REMOVED-CLEAN"</automated>
  </verify>
  <done>`MessageInput.tsx` contains no `workflow-mode-selector`, `workflow-picker`, `workflowMode`, `onWorkflowModeChange`, `publishedWorkflows`, `selectedWorkflowId`, `onWorkflowSelect`, `displayedMode`, `WorkflowOption`, or `labelMode` (rg exits 1 = zero matches). `data-testid="agent-mode-selector"` and `data-testid="composer-stop"` still present; `workflowLocked` still consumed at the placeholder/disable/Send sites. No `Workflow`/`Sparkles` import remains.</done>
</task>

<task type="auto">
  <name>Task 2: Stop passing the removed props + delete the picker-feed state and the dead kickoff branch in ChatArea.tsx</name>
  <files>frontend/src/components/chat/ChatArea.tsx</files>
  <read_first>
    - frontend/src/components/chat/ChatArea.tsx (full — imports L1-28, picker/toggle useState L70-72, thread-switch effect L140-147, providers effect L149-164, listPublishedWorkflows effect L166-171, mount reconcile L178-204, handleSend L312-356, MessageInput pass-through L382-422, 409 banner L519-561)
    - frontend/src/components/chat/MessageInput.tsx (the now-edited Props shape from Task 1 — the 6 props no longer exist, so passing them would be a TypeScript error; this is why Task 1 is done first)
    - .planning/phases/121-one-front-door-for-workflows-ia/121-PATTERNS.md § "Edit Op 1" passing-site excerpt (the workflowLocked-is-last trap) + § "Edit Op 2" ChatArea delete table + the OQ-2 gotcha (requestOpenPanel)
    - .planning/phases/121-one-front-door-for-workflows-ia/121-RESEARCH.md § "The Removal Seams → ChatArea.tsx" + § "The Preserve List"
  </read_first>
  <action>
    Edit `ChatArea.tsx`:
    (1) In the `<MessageInput .../>` block, stop passing the 6 removed props — `workflowMode`, `onWorkflowModeChange`, `displayedMode`, `publishedWorkflows`, `selectedWorkflowId`, `onWorkflowSelect`. KEEP `workflowLocked={workflowLocked}` (it is the LAST prop in the block, immediately after the removed cluster — do not sweep it; it gates the disable).
    (2) Delete the 3 useState declarations `workflowMode`/`setWorkflowMode`, `publishedWorkflows`/`setPublishedWorkflows`, `selectedWorkflowId`/`setSelectedWorkflowId`.
    (3) In the thread-switch effect, delete the `setWorkflowMode("deep")` and `setSelectedWorkflowId(null)` lines; keep `setAgentMode("default")` and `setScopeFolderId(null)`.
    (4) Delete the entire `listPublishedWorkflows()` mount fetch effect.
    (5) In `handleSend`, delete the dead in-chat kickoff staging: the `kickoffWorkflowId` const, the `if (kickoffWorkflowId) { requestOpenPanel() }` block, the trailing `if (kickoffWorkflowId) { setSelectedWorkflowId(null) }` block, and drop the 7th positional `kickoffWorkflowId` argument to `sendMessage(...)` (omit it so the param defaults to undefined). Remove `workflowMode, selectedWorkflowId` from the `useCallback` deps array. Per D-02 this is the only dead code created — do NOT touch the `postMessage` api function or `ChatLayout.doRun` (those are the live launch route, SC#2); the Deep send path stays byte-identical (red line / D-14).
    (6) Remove the now-orphaned imports — `listPublishedWorkflows`, `type PublishedWorkflow` (from `@/lib/api`), and the `requestOpenPanel` import from `@/components/panel/panelOpenSignal` — but ONLY after a grep of the file confirms no surviving line references each (requestOpenPanel's only caller was the deleted block; verify before deleting the import).
    Do NOT alter the PRESERVE LIST: `workflowLock`/`workflowLocked` (L95-96), the FULL mount reconcile (L178-204), the 409 banner + `NON_RETRYABLE` (L84-86, L519-561), `isStreaming` (L78), `stopStreaming`/`onStop` (L54/L384). Do NOT touch `backend/` or any migration (D-06/G-5). Do NOT edit `MessageItem.tsx` (G-5 hot file; the cap_paused Continue card stays — preserve item 7).
  </action>
  <verify>
    <automated>cd frontend && rg -c 'workflowMode|setWorkflowMode|publishedWorkflows|setPublishedWorkflows|selectedWorkflowId|setSelectedWorkflowId|onWorkflowSelect|displayedMode|listPublishedWorkflows|requestOpenPanel|kickoffWorkflowId' src/components/chat/ChatArea.tsx; test $? -eq 1 && npx tsc --noEmit -p tsconfig.json && echo "WIRING-CLEAN+TYPES-OK"</automated>
  </verify>
  <done>`ChatArea.tsx` references none of the removed symbols (rg exits 1). `npx tsc --noEmit` passes (proves `MessageInput`'s narrowed Props is satisfied and no removed import is dangling). `useWorkflowLockForThread`, `getThreadWorkflow`, `workflow-lock-error-banner`, and `workflowLocked={workflowLocked}` are all still present. No edit under `backend/` and no migration file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (composer/React) → API (`threads.py`) | Mode-switch + send requests cross here; the server is the authority. This phase only removes client UI on the client side of this boundary. |
| Thread A run state → Thread B composer | The per-thread lock must never leak across threads (owner-scoped). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | Elevation of Privilege | composer mode control → `threads.py` 409 | mitigate (preserve) | Client-side mode bypass (forge "Deep" to escape a Harness lock) is already mitigated by the server-enforced 409; removing the pill REDUCES the client surface and cannot weaken the server. The 409 path (`ChatArea.tsx:519-561` + the backend route) stays byte-identical — in the PRESERVE LIST, not edited here. |
| T-121-02 | Tampering | `useWorkflowLockForThread(thread?.id)` | mitigate (preserve) | Cross-thread lock leak (Thread A's run locks Thread B) is already mitigated — the lock is keyed by the OWNING thread id, never global. Untouched by this removal (L95-96 KEEP). The parallel-thread SC#10 axis (VALIDATION.md) is the binding gate. |
| T-121-03 | Injection / XSS | removed pill labels | mitigate (eliminated) | The removed pills rendered enum-derived labels (`"Deep"`/`"Harness"`, workflow names) as React text children, never innerHTML. Removing them eliminates the (already-nil) surface entirely. No new render path is added (D-01 — no `.runchip`). |
| T-121-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added/removed (RESEARCH § Package Legitimacy Audit: N/A). No supply-chain surface in this phase. |

**ASVS level:** L1. **Block on:** `high` severity — none open (net impact neutral-to-positive; server authority untouched per D-06).
</threat_model>

<verification>
- `cd frontend && rg -l 'workflow-mode-selector|workflow-picker|workflowMode|onWorkflowModeChange|publishedWorkflows|selectedWorkflowId|onWorkflowSelect|displayedMode' src` returns ONLY `src/components/chat/__tests__/ChatAreaMode.test.tsx` (the test rewritten in Plan 02). `MessageInput.tsx` and `ChatArea.tsx` must NOT appear.
- `cd frontend && npx tsc --noEmit` passes.
- `cd frontend && npx vitest run src/components/chat/RunCard.timer.test.tsx src/components/chat/RunCard.test.tsx` stays GREEN, untouched (zero coupling to the removed symbols — no-regression evidence).
- `git diff --stat` shows changes ONLY under `frontend/src/components/chat/` — ZERO changes under `backend/`, ZERO migration files (D-06/G-5 hard gate).
- **Expected transient RED:** `ChatAreaMode.test.tsx` currently asserts `workflow-mode-selector` EXISTS, so it goes RED the instant this plan lands. That is by design — Plan 02 (same phase, next wave) rewrites it to assert the pill is GONE. Do NOT "fix" it by re-adding the control. Running the FULL `npm run test` here will show that one file red and everything else green; that is the correct post-Plan-01 state.
</verification>

<success_criteria>
- The composer source renders exactly two interactive mode pills (Model + General/Explorer); `workflow-mode-selector` and `workflow-picker` are gone from `MessageInput.tsx` (SC#1).
- `workflowLocked` still disables the textarea + swaps the "Workflow running — Cancel to switch back" placeholder + gates Send; `composer-stop` still renders while streaming (SC#3 + Cancel-reachability / D-01).
- The mount reconcile, the 409 banner, and the lock derivation are unchanged; the Workflows-page launch route (`doRun` → `postMessage` with `workflowDefinitionId`) is untouched (SC#2/SC#3 preserve list).
- TypeScript compiles; no backend/migration touched (D-06/G-5).
</success_criteria>

<output>
Create `.planning/phases/121-one-front-door-for-workflows-ia/121-01-SUMMARY.md` when done.
</output>
