---
phase: 185-graded-governance-per-node-grounding-mode-action-risk-dial
plan: 07
type: execute
wave: 3
depends_on: ["185-06"]
files_modified:
  - frontend/src/components/workflows/GovernanceSection.tsx
  - frontend/src/components/workflows/GovernanceSection.test.tsx
  - frontend/src/components/workflows/PhaseFormPanel.tsx
  - frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx
autonomous: true
requirements: [GOVERN-01, GOVERN-03]

must_haves:
  truths:
    - "D-185-02 — the panel's what-this-gate-does sentence is agent-step copy and never reuses the emit-path every-value-traceable wording"
    - "D-185-15 — the dial exists ONLY on llm_agent and llm_batch_agents; llm_emit shows read-only text and llm_single, llm_human_input and programmatic render no dial element at all"
    - "D-185-16 — a step type with no dial still renders the governance section, carrying the locked phrase Nothing to prove here"
    - "Pressing the refused side prints the reason as real DOM text wired by aria-describedby; getByTitle finds no refusal text anywhere"
    - "The refusal sentence is character-identical to the shared exported constant — the component authors no sentence of its own"
    - "PhaseFormPanel.tsx grows by a mount point only; every new branch is gated on rails so a rails-absent render stays byte-identical"
  artifacts:
    - path: "frontend/src/components/workflows/GovernanceSection.tsx"
      provides: "the two-position dial that visibly refuses, the attached-gate statement, and the arming switch"
      min_lines: 120
    - path: "frontend/src/components/workflows/GovernanceSection.test.tsx"
      provides: "Acceptance criteria 12, 13 and the G-5 mount-point proof"
    - path: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      provides: "the mount point only — one import and one gated JSX line"
  key_links:
    - from: "frontend/src/components/workflows/GovernanceSection.tsx"
      to: "frontend/src/components/workflows/definitionOps.ts"
      via: "every user-visible sentence imported as a shared const"
      pattern: "GROUNDING_LOCK_REFUSAL"
---

<objective>
Build the governance section — the two-position dial that visibly refuses, the plain-language statement
of the gate it attaches, and the action-risk arming switch — as a self-contained component, and mount it
in `PhaseFormPanel` with one import and one gated JSX line.

Purpose: SPEC Req 5. The panel is where you SET; the canvas is where you SEE (sketch 147, operator). Both
governance dials live in one place because `PhaseNodeCard.tsx:37-48` forbids any focusable control inside
a node card — a clickable canvas mark is not representable.

G-5 is honoured BY CONSTRUCTION here, and it is checkable: `PhaseFormPanel.tsx` is 1078 lines and gains a
**mount point**, not another 200 lines. `git diff --stat` on it must be ≤ ~4 lines (plus the prop and its
docblock). The section ships as its own file.

Output: `GovernanceSection.tsx`, its suite, one optional caller-owned prop on the panel, and one
`{rails && <GovernanceSection … />}` line.

This plan wires no page state. `WorkflowBuilderPage` passes the new prop in plan 185-08; the prop is
optional, so the panel typechecks and renders correctly without it.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-SPEC.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-CONTEXT.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-PATTERNS.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-06-SUMMARY.md
@.claude/skills/sketch-findings-agentic-rag/references/graded-governance.md
@.planning/sketches/147-the-armed-mark/README.md

Invoke `Skill("sketch-findings-agentic-rag")`. 142-B (the dial) and the one-way lock table are LOCKS.

<interfaces>
`frontend/src/components/workflows/PhaseFormPanel.tsx:83-108` — the rails and the gate-row union that
already makes "a locked gate with a remove button" unrepresentable:

  export type PhaseGateRow =
    | { label: string; locked: true }
    | { label: string; locked: false; onRemove: () => void }

  export interface PhaseFormRails {
    order: { index: number; total: number }
    toolOptions: string[] | "degraded"
    gates: PhaseGateRow[]
  }

The panel's only write seam (`:60`, `:120`) — config-only, and NOT to be widened (D-185-10):

  export type PhaseConfigPatch = Record<string, unknown>
  onChange: (patch: PhaseConfigPatch) => void

The mount point (`:1032`, the LAST child of the scroll body):

          {rails && <GatesRail gates={rails.gates} />}
        </div>
      </aside>

The refusal idiom to copy — `StepTypePicker.tsx:152-206`, trimmed to the load-bearing lines:

  const reason = choice.disabledReason
  const refused = typeof reason === "string" && reason.length > 0
  const reasonId = `${baseId}-why-${choice.type}`
  <button type="button" data-refused={refused ? "true" : "false"} disabled={refused}
          aria-disabled={refused ? "true" : undefined}
          aria-describedby={refused ? reasonId : undefined}
          onClick={() => { if (refused) return; onChoose(choice.type) }}>
    <small id={refused ? reasonId : undefined}
           data-testid={refused ? `step-type-reason-${choice.type}` : undefined}>
      {refused ? reason : subtitle}
    </small>
  </button>

The in-panel section frame to copy — `GatesRail` (`PhaseFormPanel.tsx:617-657`), whose footnote is the
refusal VOICE to reuse: *"A locked check came with a choice above it — change what made it apply and it
goes. There is no switch."*

  <section data-rail="gates" data-testid="rail-gates"
           className="mt-3 rounded border border-border bg-muted/40 px-2.5 py-2">
    <h3 className="text-[11px] font-medium text-foreground">Checks that run on this step</h3>

The twelve governance constants exported from `definitionOps.ts` by plan 185-06 (import them; author no
sentence in this file).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GovernanceSection — the dial that visibly refuses</name>
  <files>frontend/src/components/workflows/GovernanceSection.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/StepTypePicker.tsx` (whole file, 218 lines — the refusal docblock at :16-31, the `useId()` reason-id wiring, and the "this component AUTHORS NO REASON OF ITS OWN" rule)
    - `frontend/src/components/workflows/PhaseFormPanel.tsx:60-140, 617-657` (the props contract, `PhaseGateRow`, `PhaseFormRails`, and the `GatesRail` section frame + footnote)
    - `frontend/src/components/workflows/definitionOps.ts` (the twelve Phase-185 governance constants added in plan 185-06)
    - `.claude/skills/sketch-findings-agentic-rag/references/graded-governance.md` §142-B, §"The tool list IS the dial", §VOCABULARY, §"CSS patterns", §"HTML structure"
    - `.planning/phases/185-.../185-PATTERNS.md` §7 and §S2
    - `.planning/phases/185-.../185-RESEARCH.md` §Landmines L-14
  </read_first>
  <action>
Create `frontend/src/components/workflows/GovernanceSection.tsx`. It is presentational and
caller-driven: it reads no context, fetches nothing, imports nothing from the API client, and authors no
sentence — every user-visible string comes from the `definitionOps` constants plan 185-06 exported.

Props (a narrow, total contract — no `PhaseSpecJSON` spread):

    phaseType: string
    /** The step's currently selected tools, already resolved by the caller. */
    availableTools: readonly string[]
    /** The server's KB tool list (D-185-09). The caller reads it off useGroundingBundle. */
    kbTools: readonly string[]
    /** Author intent only (D-185-07). */
    groundingEscalated: boolean
    actionRiskArmed: boolean
    /** D-185-10 — the PhaseSpec-level write, caller-owned. */
    onGovernanceChange: (patch: { grounding_escalated?: boolean; action_risk_armed?: boolean }) => void
    /** llm_emit only: its shipped citation_policy, so the already-set cause can be read. */
    citationPolicy?: string

Derive the cause LOCALLY, at render time, in the same total order the server uses — `detected` first,
then `already-set`, then `escalated`, else none. Compute the intersection inline (`availableTools.filter(
(t) => kbTools.includes(t))`) as a derived value during render, NOT in state and NOT in an effect: that
is what makes the dial, the strike-through and the seal move on the same commit as a tool chip, which is
G-4 scenario 1's whole test.

Render, in this order:

1. A `<section data-testid="rail-governance">` in the `GatesRail` frame (same classes, same `<h3>`
   sizing). The heading is "How strictly this step is held" (sketch 142-B's `.sechd`).

2. **The dial — ONLY when `phaseType` is `llm_agent` or `llm_batch_agents`** (D-185-15). Two
   `<button type="button">`s inside a `role="group"` with `aria-label` naming the heading:
   `GROUNDING_DIAL_LOOSE_LABEL` and `GROUNDING_DIAL_STRICT_LABEL`.
   - free-to-think step (no cause): the loose side is ON, the strict side is pressable and calls
     `onGovernanceChange({ grounding_escalated: true })`.
   - `escalated` step: the strict side is ON, and the loose side IS pressable — this is the ONE cause the
     author may undo (`onGovernanceChange({ grounding_escalated: false })`).
   - `detected` step: the strict side is ON and the loose side is REFUSED — struck through
     (`line-through`), `disabled`, `aria-disabled="true"`, `data-refused="true"`, and
     `aria-describedby` pointing at a rendered element containing `GROUNDING_LOCK_REFUSAL` as real DOM
     text. NEVER a `title` attribute. Pressing it must not call `onGovernanceChange`. The stored
     `grounding_escalated` bit, if any, is INERT here — detection wins (SPEC Req 3), so the undo
     affordance must be absent regardless of it.
   Use `useId()` for the reason element id in the `StepTypePicker` `${baseId}-why-…` form.

3. **The why-line**: `GROUNDING_WHY_DETECTED` when detected, `GROUNDING_WHY_ESCALATED` when escalated,
   nothing otherwise.

4. **The attached-gate statement**, rendered only when the cause is `detected` or `escalated`:
   `🔒` + `GROUNDING_ATTACHED_GATE`. This sentence is D-185-02 copy and must not be substituted with the
   emit-path wording.

5. **The tool-list line**, rendered only when the dial is rendered: `GROUNDING_TOOL_LIST_IS_THE_CONTROL`
   — the panel's tool chips are the real control (142-B).

6. **`llm_emit`**: no dial. Render `GROUNDING_ALREADY_SET_NOTE` as READ-ONLY text when
   `citationPolicy === "strict"` (L-14 — do NOT render a second control for a value the shipped
   `citation_policy` dial one scroll up already owns; two surfaces making opposite claims about the same
   stored value is the exact defect `phaseVocabulary.ts:208-213` documents for `"partial"`).

7. **Every other step type** (`llm_single`, `llm_human_input`, `programmatic`): the section STILL
   renders (D-185-16), carrying `GROUNDING_NOTHING_TO_PROVE` and **no dial element at all** — query
   returns `null`, not a disabled node (Req 5: a control that could never do anything is removed, not
   disabled).

8. **The action-risk switch — on EVERY step type** (SPEC Req 8: any step may carry it). A single
   `<button type="button" role="switch" aria-checked={actionRiskArmed}>` labelled
   `ACTION_RISK_ARM_LABEL`, calling `onGovernanceChange({ action_risk_armed: !actionRiskArmed })`.
   Default OFF in 185 — no engine step type performs outbound egress yet; Phase 189's external-action
   node arrives armed-on per its SC#2. When armed, render `ACTION_RISK_ARMED_NOTE` as real DOM text.
   That note MAY say the run waits (it does, per plan 185-04); the unarmed state must say nothing of the
   kind.

Write a module docblock in the `StepTypePicker` voice stating: this component authors no reason of its
own and consults no server; the refusal is a SHAPE rule decided locally from the tools already in hand;
the KB list is server-supplied and the client only intersects, so a wrong client read is a display bug
by construction, never a safety hole. Add a `?raw` source fence in the house idiom (with a positive
control) proving this module imports nothing from `@/lib/api`.
  </action>
  <acceptance_criteria>
    - `grep -c "from \"@/lib/api\"\|from '@/lib/api'" frontend/src/components/workflows/GovernanceSection.tsx` returns 0.
    - `grep -c "title=" frontend/src/components/workflows/GovernanceSection.tsx` returns 0 — no refusal (or anything else) rides a `title` attribute.
    - `grep -c "aria-describedby" frontend/src/components/workflows/GovernanceSection.tsx` returns at least 1.
    - Every user-visible sentence is an imported identifier: `grep -cE '>[A-Z][a-z]+ [a-z]+ [a-z]+' frontend/src/components/workflows/GovernanceSection.tsx` finds no prose literal in JSX other than the section heading (verify by reading; the heading may be inline).
    - `grep -c "GROUNDING_\|ACTION_RISK_" frontend/src/components/workflows/GovernanceSection.tsx` returns at least 10.
    - `grep -riE "\b(Proven|Ungoverned|Unchecked|Not applicable|N/A)\b" frontend/src/components/workflows/GovernanceSection.tsx` returns 0.
    - `cd frontend && npx tsc -b` reports no NEW error naming `GovernanceSection.tsx`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/workflows/GovernanceSection.test.tsx</automated>
  </verify>
  <done>The dial refuses in readable DOM text, exists only where it could ever be satisfied, and the section always says something honest.</done>
</task>

<task type="auto">
  <name>Task 2: The suite — refusal is text, absence is absence</name>
  <files>frontend/src/components/workflows/GovernanceSection.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/StepTypePicker.test.tsx:40-60, 155-181` (the `?raw` fence with its positive control, the character-identity assertion, and the `aria-describedby` round trip through `document.getElementById`)
    - `frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx:1-90` (the suite header explaining why a net-new file rather than an absorption — the Phase-177 lesson — and the `phaseOf` / `railsOf` / `AGENT` fixtures)
    - `frontend/src/components/workflows/definitionOps.ts` (the constants to assert against)
    - `.planning/phases/185-.../185-VALIDATION.md` (criteria 12, 13)
  </read_first>
  <action>
Create `GovernanceSection.test.tsx`. Copy `PhaseFormPanel.rails.test.tsx`'s fixture idiom (`phaseOf`,
`railsOf`, the `AGENT` config constant) and its suite header explaining why this is a net-new file rather
than an absorption into an existing suite.

Cover, at minimum:

  - **Criterion 12 — the refusal is text, not a title.** With `phaseType: "llm_agent"`,
    `availableTools: ["search_documents"]` and the 5-name `kbTools`, press the loose side and assert:
    (a) `screen.getByText(GROUNDING_LOCK_REFUSAL)` finds it — imported, so the assertion is
    character-identical and the component cannot drift; (b)
    `expect(() => screen.getByTitle(GROUNDING_LOCK_REFUSAL)).toThrow()`; (c) the round trip — read the
    loose button's `aria-describedby`, resolve it with `document.getElementById`, and assert
    `textContent` equals the constant; (d) `onGovernanceChange` was NOT called.

  - **Criterion 13 — absent, not disabled.** For each of `llm_single`, `llm_human_input`,
    `programmatic`: `queryByRole("button", { name: new RegExp(GROUNDING_DIAL_LOOSE_LABEL) })` is `null`
    — assert `null`, not `toBeDisabled()`. Assert in the same cases that the SECTION still renders and
    contains `GROUNDING_NOTHING_TO_PROVE` (D-185-16).

  - **The one-way lock (criterion 7, DOM half).** A phase with `groundingEscalated: true` and NO KB tool
    renders a PRESSABLE loose side that calls `onGovernanceChange({ grounding_escalated: false })`. The
    SAME phase with `search_documents` added renders the loose side REFUSED and calls nothing — the undo
    affordance disappears and the stored bit is inert.

  - **D-185-15 / L-14 — `llm_emit`.** No dial element; with `citationPolicy: "strict"` the read-only
    `GROUNDING_ALREADY_SET_NOTE` renders; there is no second control (`queryAllByRole("button")` within
    the section contains only the action-risk switch).

  - **The action-risk switch.** Present on every step type; default off; toggling calls
    `onGovernanceChange({ action_risk_armed: true })`; `ACTION_RISK_ARMED_NOTE` renders only when armed
    and is absent when not (the Req 9 honesty control — the surface never claims "the run waits" on an
    unarmed step).

  - **D-185-02 — no emit copy.** Assert the rendered section's `textContent` does not contain
    `"every value traceable"` or `"everything it says is checked"`.

  - **Req 7 vocabulary.** Assert the rendered `textContent` matches `/Must prove it/` and, for a
    non-groundable type, `/Nothing to prove here/`; and that it does not match
    `/\b(Proven|Ungoverned|Unchecked|Not applicable|N\/A)\b/`.

Add a FALSIFICATION for the criterion-13 assertion and record its observed output in the SUMMARY: render
the dial disabled instead of absent for `llm_single` and observe the `queryByRole(...) === null`
assertion turn red.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/GovernanceSection.test.tsx` exits 0 with every listed case green.
    - The refusal assertion imports `GROUNDING_LOCK_REFUSAL` from `definitionOps` — `grep -c "GROUNDING_LOCK_REFUSAL" frontend/src/components/workflows/GovernanceSection.test.tsx` returns at least 2.
    - `grep -c "toBeDisabled" frontend/src/components/workflows/GovernanceSection.test.tsx` returns 0 for the non-groundable step types (they assert `null`).
    - The criterion-13 falsification was observed RED and its output is quoted in the SUMMARY.
    - `node scripts/vitest-count-gate.cjs` exits 0 — this file is NEW, postdates the 424 pin, reports as `new`, and must NOT be added to `BASELINE` (the `WorkflowBuilderPage.header.test.tsx` precedent at `:85-90`). It is already inside the `src/components/workflows` target glob, so `TARGETS` needs no edit.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/GovernanceSection.test.tsx && cd .. && node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Criteria 12 and 13 have green, falsified automated proofs, and the vocabulary rules are machine-checked at the component level.</done>
</task>

<task type="auto">
  <name>Task 3: The mount point — G-5 honoured by construction, and provably so</name>
  <files>frontend/src/components/workflows/PhaseFormPanel.tsx, frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseFormPanel.tsx:1-140` (the module docblock incl. the two Phase-185 mentions at :47 and :97, the props interface, and the `rails`-absent byte-identity contract at :129-138)
    - `frontend/src/components/workflows/PhaseFormPanel.tsx:1020-1035` (the mount point — `{rails && <GatesRail gates={rails.gates} />}` as the last child of the scroll body)
    - `frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx` (whole file — the rails-absent guard suite and the D-14 marker-list technique)
    - `.planning/phases/185-.../185-SPEC.md` §Constraints ("G-5 honoured by construction")
  </read_first>
  <action>
Add ONE optional prop to `PhaseFormPanelProps`, with the docblock naming the `onRemove` precedent
verbatim (`PhaseFormPanel.tsx:76-79`: *"`onRemove` belongs to the CALLER because a gate is a `validators`
entry, and this panel's only write seam patches `config`"*):

    /** D-185-10 — the PhaseSpec-level governance write. Caller-owned for the same reason
     *  `PhaseGateRow.onRemove` is: these fields are siblings of `validators`, and this
     *  panel's only write seam (`onChange`) patches `config`. ABSENT ⇒ the section renders
     *  read-only rather than disappearing, so a dropped wiring is visible, not silent. */
    onGovernanceChange?: (patch: { grounding_escalated?: boolean; action_risk_armed?: boolean }) => void

Add the mount as a single gated JSX line immediately BEFORE the gates rail, so the reading runs
"how strictly this step is held" → "the checks that run on it":

    {rails && <GovernanceSection … />}
    {rails && <GatesRail gates={rails.gates} />}

Every new branch MUST be gated on `rails`. The panel is ONE instance serving both the flag-off Spine view
and the flagged Canvas view; a rails-absent render must stay byte-identical (D-181-01 / D-14). Pass the
props the section needs from `phase.config` (`available_tools`, `citation_policy`, `phase_type`) and from
`phase` (`grounding_escalated`, `action_risk_armed`), plus `kbTools` — which must arrive from the caller.
Add `kbTools: string[]` to `PhaseFormRails` rather than as a second loose prop, so the panel keeps ONE
rails object as its server-sourced input; document it as server-supplied (D-185-09) with an empty array
meaning "the palette could not be read", which marks nothing rather than un-marking something.

Rewrite the two docblock mentions at `:47` and `:97` that name Phase 185 as a future replacer. Do not
delete their reasoning — restate it in the present tense (the gates rail's intended consumer has arrived;
the derivation it described is gone as of plan 185-08).

**Do not add anything else to this file.** No handler bodies, no derivations, no copy. `git diff --stat`
on `PhaseFormPanel.tsx` must show a small line count, and the SUMMARY must quote it.

In `PhaseFormPanel.rails.test.tsx`, add: (a) a rails-ABSENT render still contains none of the governance
markers (extend the shipped `RAIL_MARKERS` list technique with the governance section's `data-testid`);
(b) a rails-PRESENT render contains `rail-governance` positioned BEFORE `rail-gates` in document order;
(c) with `onGovernanceChange` omitted the panel still renders and the section is read-only (no crash,
no thrown handler).
  </action>
  <acceptance_criteria>
    - `git diff --stat frontend/src/components/workflows/PhaseFormPanel.tsx` shows ≤ 14 changed lines total, of which the RENDER BODY accounts for ≤ 4 (one import, one JSX line, and at most two prop lines) — the remainder being the prop declaration, its docblock, the `PhaseFormRails.kbTools` field and the two rewritten docblock mentions. Quote the exact `--stat` output in the SUMMARY.
    - `grep -c "GovernanceSection" frontend/src/components/workflows/PhaseFormPanel.tsx` returns exactly 2 (the import and the JSX element).
    - `grep -c "GROUNDING_\|ACTION_RISK_\|Must prove it\|Nothing to prove here" frontend/src/components/workflows/PhaseFormPanel.tsx` returns 0 — the panel authors no governance copy.
    - `cd frontend && npx vitest run src/components/workflows/PhaseFormPanel.rails.test.tsx src/components/workflows/PhaseFormPanel.test.tsx` exits 0 with `PhaseFormPanel.test.tsx` at its pinned count of 19.
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 pinned files at delta ≥ 0 and none decreased.
    - `cd frontend && npx tsc -b` reports no NEW error naming `PhaseFormPanel.tsx`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows && cd .. && node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The 1078-line hot file grew by a mount point, the flag-off surface is unchanged, and the diff-stat proves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author input (tool chips) → local derivation | The dial's state is derived from author-selected tools intersected with the server's list. Nothing here enforces. |
| component → caller (`onGovernanceChange`) | The only write this component can make is one of two booleans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-07-01 | Elevation of privilege | the refused loose side | mitigate | A `disabled` button alone is bypassable by a determined DOM edit — which is exactly why enforcement does not live here. The refusal is a DISPLAY rule; the run-time gate (plan 185-03) reads the server's own detection and cannot be reached from this component. Additionally the escalated bit is the ONLY value this component can write, and it is safe in both directions (setting it strengthens; clearing it only ever restores a state the server would derive anyway). |
| T-185-07-02 | Information disclosure | `title` attributes | mitigate | Refusal reasons are real DOM text wired by `aria-describedby` — never a `title` (the 184-07 lesson). Greps for `title=` returning 0 and `getByTitle` throwing are both acceptance criteria. |
| T-185-07-03 | Information disclosure | authored strings | accept | Every string is an imported static const; the component interpolates no user, org or document value into markup and uses no `dangerouslySetInnerHTML`. |
| T-185-07-04 | Tampering | flag-off surface | mitigate | Every new branch is gated on `rails`, which `WorkflowBuilderPage` spreads conditionally on the canvas flag. `PhaseFormPanel.rails.test.tsx`'s rails-absent guard is extended with the governance markers. |

No package installs. No migration. Frontend-only plan.
</threat_model>

<verification>
- `cd frontend && npx tsc -b` — no NEW error naming any file in `files_modified`.
- `cd frontend && npx vitest run src/components/workflows` — green.
- `node scripts/vitest-count-gate.cjs` — exit 0, no pinned file decreased, the two new suites report as `new`.
- `cd frontend && npx vite build` — exit 0.
- `git diff --stat frontend/src/components/workflows/PhaseFormPanel.tsx` — quoted in the SUMMARY, render body ≤ 4 lines.
- `git diff --stat -- backend/ supabase/migrations` — 0 files.
</verification>

<success_criteria>
Pressing the refused side prints a reason a person can read and a screen reader can speak. A step type
that can never be grounded has no dial at all and still says something honest. The 1078-line panel gained
a mount point.
</success_criteria>

<output>
Create `.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-07-SUMMARY.md` when done.
</output>
