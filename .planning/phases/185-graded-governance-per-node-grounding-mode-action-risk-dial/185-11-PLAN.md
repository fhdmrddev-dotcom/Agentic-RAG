---
phase: 185-graded-governance-per-node-grounding-mode-action-risk-dial
plan: 11
type: execute
wave: 6
depends_on: ["185-05", "185-09", "185-10"]
files_modified:
  - .planning/STATE.md
  - CLAUDE.md
  - .planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-VALIDATION.md
  - frontend/src/components/workflows/governanceVocabulary.test.ts
autonomous: false
requirements: [GOVERN-01, GOVERN-02, GOVERN-03]

must_haves:
  truths:
    - "D-185-05 — not author-loosenable-away is enforced in two independent places: the state is unrepresentable and a weak declared gate cannot displace the engine's"
    - "D-185-07 — the unrepresentable-state half of that property is verified in-phase, by grep and by test"
    - "git diff -- supabase/migrations is 0 lines and the live migration head is still 113"
    - "The Deep chat path is byte-identical — the git-diff fence over agent_loop, tool_dispatcher, openai_service and anthropic_service reports 0 files"
    - "A grep over the workflow tree returns 0 for each banned term in user-visible strings and at least 1 for each required term"
    - "The G-5 fire on PhaseFormPanel.tsx is recorded as honoured-by-construction, with the measured diff-stat as its evidence"
  artifacts:
    - path: "frontend/src/components/workflows/governanceVocabulary.test.ts"
      provides: "the banned-term and required-term sweep as an automated, falsifiable test rather than a one-off grep"
    - path: ".planning/STATE.md"
      provides: "the G-5 honoured-by-construction record and the phase's carried decisions"
  key_links:
    - from: "frontend/src/components/workflows/governanceVocabulary.test.ts"
      to: "frontend/src/components/workflows"
      via: "a source sweep over the shipped component tree"
      pattern: "Must prove it"
---

<objective>
Close the phase: turn the cross-cutting acceptance criteria into automated, falsifiable checks rather
than one-off greps, and record the guardrail dispositions this phase owes.

Purpose: five of SPEC's criteria are cross-cutting and can only be honestly checked once every wave has
landed — the vocabulary sweep (14, 15), the zero-migration check (2), the D-14 byte-identity fence (11),
and the G-5 mount-point evidence (21). A grep run once by hand and reported in a SUMMARY rots; a test
does not.

Output: one vocabulary sweep suite, the VALIDATION per-task map filled in, and the STATE.md + CLAUDE.md
records this phase's guardrails require.

This plan changes NO application source. If any check fails, the fix belongs in the plan that owns the
file, not here.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-SPEC.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-VALIDATION.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-RESEARCH.md
@CLAUDE.md

All ten plan SUMMARYs from this phase are inputs to task 2's ledger.

<interfaces>
Req 7's binding vocabulary (`references/graded-governance.md:108-116`):

  Say:   Must prove it (canvas, at rest) · Free to think · Nothing to prove here
  Never: Proven · Ungoverned · Unchecked · Not applicable · N/A
  traceable is legitimate ONLY at the review moment — Phase 188, not here.

Req 6's retired strings, which must appear 0 times:
  "Must cite its sources" · "Flags uncited claims" · "No sources needed"

The D-14 fence (RESEARCH §L-10) — the concrete, greppable proof:
  git diff --stat backend/app/services/agent_loop.py backend/app/services/tool_dispatcher.py \
                  backend/app/services/openai_service.py backend/app/services/anthropic_service.py
must report 0 files. Every other backend file this phase touches was audited as having NO Deep exposure,
with one exception: `ask_user_service.py` IS shared with the Deep dispatcher's `ask_user` tool, and the
fence there is that the `timeout_seconds` widening is a type-only change on the default path
(`tool_dispatcher.py:3824` documents that Deep does not call `subscribe_for_response`).

The G-5 ledger row this phase owes (`CLAUDE.md` §"Workflow guardrails"): `PhaseFormPanel.tsx` at 1078
lines was the real hot file; 185 adds a mount point, not another 200 lines. RESEARCH also flags
`WorkflowCanvas.tsx` (1505 L, grown again by plan 185-10) as worth a G-5 note for Phase 188.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The vocabulary sweep, as a test</name>
  <files>frontend/src/components/workflows/governanceVocabulary.test.ts</files>
  <read_first>
    - `.claude/skills/sketch-findings-agentic-rag/references/graded-governance.md` §VOCABULARY (the binding table)
    - `frontend/src/components/workflows/canvasModel.purity.test.ts:1-40` (the `?raw` source-sweep idiom and its positive-control discipline)
    - `frontend/src/components/workflows/definitionOps.ts` (the twelve governance constants — the required terms' home)
    - `.planning/phases/185-.../185-VALIDATION.md` (criteria 14, 15)
  </read_first>
  <action>
Create `frontend/src/components/workflows/governanceVocabulary.test.ts`, a source sweep over the shipped
workflow component tree (`frontend/src/components/workflows` plus `frontend/src/pages/WorkflowBuilderPage.tsx`)
using the house `?raw` / `import.meta.glob` idiom. Exclude `*.test.ts(x)` files from the sweep and say
why in a comment: a test that asserts a banned string is absent must be allowed to NAME it.

Assert:

  - **Criterion 14** — each of `"Must cite its sources"`, `"Flags uncited claims"`, `"No sources needed"`
    appears 0 times across the swept sources.
  - **Criterion 15, banned half** — each of `Proven`, `Ungoverned`, `Unchecked`, `Not applicable`, `N/A`
    appears 0 times in USER-VISIBLE strings. Anchor on WORD BOUNDARIES and case, or the sweep
    false-positives: `Must prove it` must not trip `Proven`, and `waitsForYou` must not trip anything.
    Scope the match to string literals and JSX text rather than to comments and docblocks, and state that
    scoping decision in a comment — a phase whose deliverable is honest wording is allowed to DISCUSS the
    banned words in its own reasoning.
  - **Criterion 15, required half** — each of `Must prove it`, `Free to think`, `Nothing to prove here`
    appears at least once. Assert against the exported constants from `definitionOps`, not against
    re-typed literals, so the test cannot drift from the copy it guards.
  - **The D-185-02 overclaim guard** — `every value traceable` and `everything it says is checked` appear
    0 times in the swept sources. That is emit-path copy and must never be transplanted onto a detected
    agent step.
  - **`traceable`** appears 0 times in user-visible strings in this tree — it is legitimate only at the
    review moment, which is Phase 188.

Include a POSITIVE CONTROL for each of the three assertion families (banned-absent, required-present,
overclaim-absent), proving the sweep can go red and that the swept file set is non-empty
(`expect(files.length).toBeGreaterThan(10)` — the non-vacuity guard that makes the whole suite mean
something). Avoid the D-ITEM-183-02 self-matching trap: assemble the searched tokens from parts where a
literal in this file would otherwise be found by its own sweep.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/governanceVocabulary.test.ts` exits 0.
    - The non-vacuity guard asserts more than 10 swept files, so an empty glob cannot pass silently.
    - All three positive controls were observed RED and their outputs are quoted in the SUMMARY.
    - The banned-term assertion passes with `Must prove it` present in the tree — i.e. the word-boundary anchoring is proven not to false-positive on `prove`/`proves`.
    - `node scripts/vitest-count-gate.cjs` exits 0 — this file is NEW and must NOT be added to `BASELINE`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/governanceVocabulary.test.ts && cd .. && node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Criteria 14 and 15 are enforced by a falsifiable test that a future phase cannot silently violate.</done>
</task>

<task type="auto">
  <name>Task 2: The phase-level fences and the guardrail records</name>
  <files>.planning/STATE.md, CLAUDE.md, .planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-VALIDATION.md</files>
  <read_first>
    - All ten `185-*-SUMMARY.md` files produced by this phase (the diff-stats, measured pins and falsification outputs they quote are this task's evidence)
    - `.planning/phases/185-.../185-VALIDATION.md` (the empty Per-Task Verification Map and the sign-off checklist)
    - `CLAUDE.md` §"Workflow guardrails (MANDATORY)" and its hot-file ledger table
    - `.planning/STATE.md` §"Current Position"
    - `.planning/phases/185-.../185-RESEARCH.md` §Landmines L-3 and L-10
  </read_first>
  <action>
Run and RECORD the four phase-level fences, quoting the actual command output in the SUMMARY — a fence
whose output is not quoted has not been run:

  1. `git diff --stat -- supabase/migrations` — must report 0 files (SPEC criterion 2). Also confirm the
     live migration head is still 113 and state how it was confirmed.
  2. `git diff --stat backend/app/services/agent_loop.py backend/app/services/tool_dispatcher.py backend/app/services/openai_service.py backend/app/services/anthropic_service.py`
     — must report 0 files (SPEC criterion 11 / D-14). Additionally quote
     `git diff --stat backend/app/services/harness/phase_types.py`, which must also be 0 (criterion 20).
  3. `git diff --stat frontend/src/components/workflows/PhaseFormPanel.tsx` — the G-5 evidence
     (criterion 21). Quote the number and state the split between the mount point and the prop
     declaration.
  4. `grep -rn "groundingFor\|GROUNDINGS" frontend/src` — must return 0 lines.

Fill in `185-VALIDATION.md`'s **Per-Task Verification Map** — one row per task across all eleven plans,
each naming its plan, wave, requirement, criterion number, test type, automated command and status. Tick
the Wave-0 Requirements checklist (the four new test files plus the count-gate baseline update) and the
Validation Sign-Off items that are now true. Set `nyquist_compliant: true` in the frontmatter ONLY if
every task in the map carries an `<automated>` command or an explicit Wave-0 dependency; otherwise leave
it false and name what is missing.

Add to the SC#10 scoreboard's preamble a sentence stating the expectation up front so a Google failure is
not read as a regression (L-3): `citations` are built by the dispatcher's tool handlers and harvested off
`ToolResult` — a provider-independent path — and `apply_tool_budget` never drops a whitelisted tool
(`openai_service.py:1193-1196`, a verified negative). The divergence lives one layer up: whether the MODEL
emits a tool call at all. A model that answers from the prompt without searching produces
`citations == []`, so the gate fails — **which is the gate working as designed**. Google is the
highest-risk row, and the negative row is what exposes it. Per the provider-docs-first rule, research that
provider's own tool-use documentation before attributing a failure to our code, and keep any fix at the
service boundary.

Update `.planning/STATE.md`:
  - record the **G-5 fire on `PhaseFormPanel.tsx` as HONOURED-BY-CONSTRUCTION**, with the measured
    diff-stat from fence 3 as its evidence and `GovernanceSection.tsx` named as the own-component half.
  - record the **D-185-17 scope decision** and its reversibility note verbatim: it is the one scope
    decision taken without the operator in the room, and it is the single most reversible thing in the
    phase — drop the Wave-1 plan file and Wave 2+ still build, with acceptance criterion 23 failing until
    the rebuild lands separately.
  - record the **publish behaviour change** (D-185-11's publish half): the gauntlet gains no new stage,
    but its golden run drives the enforcement seam, so a detected step whose golden run retrieves nothing
    now blocks publish. Every `workflow_definitions` row is throwaway test data, so this is accepted.
  - record that **`_exec_llm_human_input` is unchanged** and the global timeout-advances-silently
    behaviour remains open, deliberately, scoped out by SPEC.

Update `CLAUDE.md`'s hot-file ledger: add `frontend/src/components/workflows/WorkflowCanvas.tsx` (now the
largest workflow file, grown again by plan 185-10) with a G-5 note for Phase 188, and update
`frontend/src/components/workflows/PhaseNodeCard.tsx`'s row for the 137-B rebuild + seal. Do not change
any guardrail rule text — only the ledger rows.
  </action>
  <acceptance_criteria>
    - The SUMMARY quotes the verbatim output of all four fences, and fences 1, 2 and 4 report 0.
    - Fence 3's number is quoted and the render-body portion is ≤ 4 lines.
    - `185-VALIDATION.md`'s Per-Task Verification Map has one row per task across all eleven plans, with no `*(pending planner output)*` placeholder remaining.
    - `185-VALIDATION.md`'s `nyquist_compliant` frontmatter value is set deliberately, and if `false` the SUMMARY names exactly what is missing.
    - `grep -c "WorkflowCanvas.tsx" CLAUDE.md` returns at least 1 (the new ledger row).
    - `grep -c "honoured-by-construction\|honoured by construction" .planning/STATE.md` returns at least 1.
    - `git diff --stat -- frontend/src backend/app` reports 0 files for this task — this plan changes no application source.
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat -- supabase/migrations backend/app/services/agent_loop.py backend/app/services/tool_dispatcher.py backend/app/services/openai_service.py backend/app/services/anthropic_service.py backend/app/services/harness/phase_types.py</automated>
  </verify>
  <done>Every cross-cutting criterion has a recorded, quoted result, and the guardrails this phase fired are auditable rather than assumed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The G-4 lived-experience gate and the SC#10 scoreboard</name>
  <what-built>
The full phase: detection and the engine-attached citation gate (plans 02-03), the fail-closed armed
checkpoint and its honest wait (04-05), the governance section and its refusal (06-07), the word-badge
deletion and the client-synthesized locked row (08), the 137-B card with its corner seal (01, 09), and
the detour edge (10). Every automated gate is green and every fence is recorded.
  </what-built>
  <how-to-verify>
Drive all four G-4 scenarios and the SC#10 scoreboard **from `185-VALIDATION.md`**, which is where the
rows are authored — do not re-author them here. Chrome MCP drives the G-4 rows at phase verification;
wire format and screenshots alone are insufficient. Local app: http://localhost:5173/.

The four G-4 scenarios, in `185-VALIDATION.md` §"Manual-Only Verifications":
  1. Watch a step lock in front of you — switch on *Search your documents* and watch three things move at
     once with no reload.
  2. The seal survives a live run — idle → running → needs-you → failed.
  3. Arm it and walk away — arm an outbound step, launch, close the tab, come back much later.
  4. The detour reads as a detour — armed vs unarmed at a glance, no collision with the ✕ or the ＋.

Plus the two other manual-only rows in the same section: the greyscale render (criterion 17) and the
before/after screenshot diff on ordinary flow edges (D-185-18).

Then the SC#10 scoreboard: OpenAI / Anthropic / Google / OpenRouter, plus the multi-tool, parallel-thread
and long-message rows, and the negative row (a detected step where the model answers without searching —
the gate must FAIL honestly and the retry feedback must name what was missing). Google is the
highest-risk row and a failure there is most likely the gate working as designed.

Record every result in `185-VALIDATION.md`'s tables. Redis and local Supabase must both be up for
scenario 3 and the parallel-thread row; `WORKER_COUNT=2` for the parallel row.
  </how-to-verify>
  <read_first>
    - `.planning/phases/185-.../185-VALIDATION.md` (the G-4 table, the other-manual table and the SC#10 scoreboard — the authored rows)
    - `CLAUDE.md` §"UAT scoreboard recipe (MANDATORY)" and §"Workflow guardrails" G-4
    - `.planning/phases/185-.../185-RESEARCH.md` §Landmines L-3 (why a Google failure may not be a regression)
  </read_first>
  <files>.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-VALIDATION.md</files>
  <action>
Pause for the operator. Drive the four G-4 lived-experience scenarios and the two other manual-only rows
via Chrome MCP (fallback: operator-clicks plus psycopg2 on port 54322 if Chrome MCP hangs), then the
eight-row SC#10 scoreboard across OpenAI / Anthropic / Google / OpenRouter. Record every observed result
in `185-VALIDATION.md`'s existing tables — the rows are authored there and must not be re-authored in
this plan. Do not mark any row passed from wire format or a screenshot alone; G-4 requires the lived
behaviour. Route any failure with its evidence rather than absorbing it here.
  </action>
  <verify>
    <human-check>Every G-4 row and every SC#10 row in `185-VALIDATION.md` carries a recorded result.</human-check>
  </verify>
  <done>All four G-4 scenarios and all eight SC#10 rows are recorded in `185-VALIDATION.md` with observed behaviour, and any failure is routed.</done>
  <acceptance_criteria>
    - All four G-4 rows in `185-VALIDATION.md` carry a recorded pass/fail with the observed behaviour, not a blank.
    - Both other-manual rows (greyscale, ordinary-edge screenshot diff) are recorded.
    - The SC#10 scoreboard has a result in every row including the negative row, with the model actually used named per provider.
    - Any failure is recorded with its evidence and routed — to a gap-closure plan, or, if it is a provider behaviour, researched against that provider's own tool-use docs first (provider-docs-first).
  </acceptance_criteria>
  <resume-signal>Type "approved" once every G-4 row and every SC#10 row is recorded, or describe the failures observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan changes no application source. It runs checks and writes planning records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-11-01 | Repudiation | the phase's own evidence | mitigate | A fence run by hand and reported in prose rots. The vocabulary sweep becomes a test with positive controls and a non-vacuity guard; the four git fences have their verbatim output quoted in the SUMMARY rather than being asserted to have passed. |
| T-185-11-02 | Tampering | the "not author-loosenable-away" property | mitigate | The phase's one real security property is verified in TWO independent places on purpose: the state is unrepresentable (D-185-07, proven in plan 185-02's source guard) and a weak declared gate cannot displace the engine's (D-185-05, proven in plan 185-03's index assertion). Either alone degrades to a rule someone must keep defending; this plan records that both are green. |
| T-185-11-03 | Information disclosure | Deep chat path | mitigate | The D-14 fence is a git-diff over the four Deep-path files reporting 0, quoted verbatim. `ask_user_service.py` is the one shared file and its change is type-only on the default path, with no existing caller passing `None`. |

No package installs — RESEARCH's Package Legitimacy Audit found this phase installs nothing; every
mechanism reuses shipped code (`pydantic`, `redis.asyncio`, `@xyflow/react` v12, `vitest`). The Package
Legitimacy Gate is therefore not applicable and no legitimacy checkpoint is required. No migration.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows src/pages src/hooks src/stores src/components/panel` — green.
- `cd frontend && npx tsc -b` — no NEW errors; `npx vite build` exit 0.
- `node scripts/vitest-count-gate.cjs` — exit 0, no pinned file decreased.
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` — green.
- All four git fences quoted, three of them reporting 0.
</verification>

<success_criteria>
Every cross-cutting SPEC criterion has a recorded result. The vocabulary rules are machine-enforced. The
G-5 fire, the D-185-17 scope decision, the publish behaviour change and the deliberately-unclosed global
timeout behaviour are all recorded where the next phase will read them.
</success_criteria>

<output>
Create `.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-11-SUMMARY.md` when done.
</output>
