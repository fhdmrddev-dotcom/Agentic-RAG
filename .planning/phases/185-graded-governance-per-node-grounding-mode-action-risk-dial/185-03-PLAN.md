---
phase: 185-graded-governance-per-node-grounding-mode-action-risk-dial
plan: 03
type: execute
wave: 2
depends_on: ["185-02"]
files_modified:
  - backend/app/services/harness/validator_kinds.py
  - backend/app/services/harness/grounding.py
  - backend/app/services/harness_engine.py
  - backend/tests/unit/test_185_engine_attachment.py
  - backend/tests/unit/test_validator_kinds.py
autonomous: true
requirements: [GOVERN-01, GOVERN-03]

must_haves:
  truths:
    - "D-185-01 — the synthesized citations_required gate uses a new deterministic mode that FAILS on an empty citations list AND requires at least one citation marker in the text"
    - "D-185-03 — attachment happens at the parse-time effective phase, never inside run_gates"
    - "D-185-04 — disposition is retry with feedback then fail_run: on_failure fail_run, max_retries 2"
    - "D-185-05 — when the author already declared a citations_required validator the engine's gate runs TOO; both specs run and the first failure wins"
    - "D-185-12 — the armed checkpoint is a synthesized PRE-gate validator attached by the same parse-time mechanism, so arming changes neither len(phases) nor any phase_index"
    - "A run of a published definition carrying a KB tool and NO declared citations_required validator fails on uncited output"
    - "Deleting the citations_required validator from the JSONB does not remove enforcement"
    - "An ungoverned phase is returned BY REFERENCE — byte-identical when unset"
  artifacts:
    - path: "backend/app/services/harness/validator_kinds.py"
      provides: "the retrieved_and_cited citations_required mode + the action_risk_approval kind"
      contains: "retrieved_and_cited"
    - path: "backend/app/services/harness/grounding.py"
      provides: "effective_phase() — the one synthesis home, never persisted"
      contains: "effective_phase"
    - path: "backend/app/services/harness_engine.py"
      provides: "the spec_by_slug seam swapped to the effective phase"
      contains: "effective_phase"
    - path: "backend/tests/unit/test_185_engine_attachment.py"
      provides: "Acceptance criteria 9, 10, 18"
  key_links:
    - from: "backend/app/services/harness_engine.py"
      to: "backend/app/services/harness/grounding.py"
      via: "function-local import at the spec_by_slug seam inside run_workflow"
      pattern: "effective_phase\\("
---

<objective>
Attach both synthesized gates at the one seam every run passes through, and register the two validator
behaviours they need.

Purpose: SPEC Req 4 says the `citations_required` coverage gate fires **whether or not the definition
JSONB declares it, including on already-published definitions**. `harness_engine.py:1118` builds
`spec_by_slug = {p.slug: p for p in definition.phases}` — the single chokepoint downstream of every
`WorkflowDefinition.model_validate()` (fresh kickoff AND boot-time resume AND the publish golden run,
which drives `run_workflow` directly from `_drive_golden_run`) and upstream of `_run_phase_with_gates`.
Swapping it for the effective phase satisfies Req 4 without touching the save path, the publish path, or
Deep.

The headline landmine: the shipped default `deterministic` mode reads `output["field_map"]` and returns
`GateResult(False, "citations_required: no field_map on output")` when absent
(`validator_kinds.py:224-226`). Detection only ever fires on `llm_agent` / `llm_batch_agents`, whose
output is `{text, sub_run_id(s), source_refs, citations, similarity_scores}` — **no `field_map` in
exactly zero places**. Attaching the shipped mode unchanged would fail 100% of detected steps.

Output: a `retrieved_and_cited` mode, an `action_risk_approval` kind, one pure `effective_phase()`, and
a one-line seam swap.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-SPEC.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-CONTEXT.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-RESEARCH.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-PATTERNS.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-02-SUMMARY.md

<interfaces>
`ValidatorSpec` (`backend/app/models/harness.py:170-187`) — the field is `config: dict`, NOT `params`:

  kind: Literal[...]                       # gained "action_risk_approval" in plan 185-02
  config: dict = Field(default_factory=dict)
  on_failure: str = "fail_run"             # fail_run | retry | skip_to_phase:<slug> | ask_user
  max_retries: int = 2
  timing: Literal["pre", "post"] = "post"

`run_gates` (`backend/app/services/harness/validators.py:214-249`) — `idx` is the index into the FULL
`phase.validators` list even when `timing` filters, and the loop returns on the FIRST failure:

  async def run_gates(phase, output: dict, ctx, *, timing: str | None = None) -> GateResult:
      for idx, spec in enumerate(getattr(phase, "validators", None) or []):
          if timing is not None and getattr(spec, "timing", "post") != timing: continue
          validator = VALIDATOR_REGISTRY.get(spec.kind)
          if validator is None: return GateResult(False, f"unknown validator kind {spec.kind!r} ...", idx)
          result = await validator(output, spec.config, ctx)
          if not result.passed: return result._replace(validator_index=idx)
      return GateResult(True, None)

The detected-agent output shapes (`harness/phase_types.py:489-495` and `:584-590`):
  {"text", "sub_run_id",  "source_refs", "citations", "similarity_scores"}
  {"text", "sub_run_ids", "source_refs", "citations", "similarity_scores"}

The shipped `presence` branch of `citations_required` (`validator_kinds.py:209-221`) — the marker half
already exists, regex-configurable, default pattern `r"\[\d+\]|\(doc[^)]*\)"`, `min_markers` default 1.

The seam (`backend/app/services/harness_engine.py:1116-1118`), consumed once at `:1149`:

  # Map the parsed definition's PhaseSpec by slug so we dispatch on the typed
  # config while iterating the durable rows in phase_index order.
  spec_by_slug = {p.slug: p for p in definition.phases}

The shipped precedent for a post-parse / pre-consumption transform: `graft_skill_snapshots`
(`harness/skill_snapshot.py:263-289`), called with a function-local import at `harness_engine.py:1455-1461`.
It MUTATES in place — that is the one part NOT to copy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The retrieved_and_cited mode and the action_risk_approval kind</name>
  <files>backend/app/services/harness/validator_kinds.py, backend/tests/unit/test_validator_kinds.py</files>
  <read_first>
    - `backend/app/services/harness/validator_kinds.py:1-280` (the DISCIPLINE block at :16-25, the `citations_required` registration and its three current modes, the `field_map` blocker at :224-226)
    - `backend/app/services/harness/validator_kinds.py:511-580` (`freshness` — the ONLY shipped `timing="pre"` kind, and its `"<kind>:<subkind>|<payload>"` structured-finding message form)
    - `backend/app/services/harness/validators.py` (whole file, 250 lines — the registry, `register_validator`, `GateResult`, `run_gates`)
    - `.planning/phases/185-.../185-PATTERNS.md` §2 and §3
    - `.planning/phases/185-.../185-RESEARCH.md` §Landmines L-1 and §"Discretion Items" D-2
  </read_first>
  <action>
In `_validate_citations_required`, add a THIRD `if mode == "retrieved_and_cited":` branch ABOVE the
deterministic fall-through, sitting exactly where `presence` sits. Two halves, BOTH required:

  (a) real retrieval evidence — `output.get("citations")` must be a non-empty list. Read **`citations`**,
      NEVER `source_refs`: `citations` are the deduped, passage-bearing objects the retrieval TOOL
      produces via the sub-agent loop (`task_service.py:765`, folded at `harness_engine.py:296-302`), so
      the model cannot fake them; `source_refs` can be populated by non-KB tools and reading it would
      weaken the gate (L-1).
  (b) at least one citation marker in the text — reuse the SAME code path as the `presence` branch (same
      default pattern literal, same `re.error` guard, same `min_markers` default of 1), not a re-derived
      copy.

Failure messages follow the file's universal `"<kind>: <reason>"` prefix and must name what was missing
so the engine's retry-feedback loop shows the model something actionable:
  - `"citations_required: nothing was retrieved (0 sources) — this step reads your documents and must show where its answer came from"`
  - `"citations_required: {n}/{need} citation markers in the answer"`

Add the third entry to the function docstring's `mode` list at `:203-207` in the existing two-space
bullet style, naming D-185-01/D-185-02 and stating explicitly that this mode is NOT `check_coverage`:
coverage is computable only over a structured leaf set (a `field_map`), which no agent step produces, so
the honest claim on an agent step is *retrieved-and-pointed-at*, not *every value traceable*.

Add a new registered kind in the same file, under a `# ── N. action_risk_approval (timing=pre; …) ──`
section header copied from `freshness`:

    @register_validator("action_risk_approval")
    async def _validate_action_risk_approval(output: dict, config: dict, ctx) -> GateResult:

It **ALWAYS returns `GateResult(False, "action_risk:approval|" + (config.get("prompt") or ""))`** and
never inspects `output`. It never blocks and never raises — the validators.py contract is preserved
verbatim; the pause is owned entirely by the `on_failure: "ask_user"` disposition machinery
(`_resolve_failure_with_ask_user`), which is where plan 185-04 makes the wait indefinite. Say that in
the docstring, and say why: putting an indefinite pub/sub block inside a gate whose contract says it
never blocks would break the abstraction, while the shipped `ask_user` disposition already pauses the
same way. The `"action_risk:approval|"` prefix must follow the `freshness:staleness|` shape
character-for-character in structure — `_ask_user_choices_from_finding` branches on exactly it.

Registration is by import side-effect (`harness/__init__.py:27` does `from . import validator_kinds`);
nothing else is needed.

Extend `test_validator_kinds.py` with: the three `retrieved_and_cited` outcomes (empty `citations` →
fail with the retrieval message; non-empty `citations` but zero markers → fail with the marker message;
both present → pass), a case proving the mode does NOT read `field_map` (an agent-shaped output with no
`field_map` at all passes when both halves are satisfied), and one asserting
`_validate_action_risk_approval` returns `passed=False` with the exact prefix, for any output including
`{}`.
  </action>
  <acceptance_criteria>
    - `grep -c '"retrieved_and_cited"' backend/app/services/harness/validator_kinds.py` returns at least 1.
    - `grep -c 'register_validator("action_risk_approval")' backend/app/services/harness/validator_kinds.py` returns 1.
    - `grep -c 'action_risk:approval|' backend/app/services/harness/validator_kinds.py` returns at least 1.
    - The `retrieved_and_cited` branch does not name `field_map` or `source_refs`: within that branch's body `grep -c "field_map\|source_refs"` returns 0 (verify by reading the branch).
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_validator_kinds.py -q` exits 0 with the 5 new cases green and every pre-existing case unchanged.
    - `git diff backend/app/services/harness/validator_kinds.py` shows ZERO deletions inside the existing `presence` and `deterministic` branches.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_validator_kinds.py -x -q</automated>
  </verify>
  <done>Both new validator behaviours are registered, the field_map blocker is routed around, and the structured finding is in the shape the disposition machinery parses.</done>
</task>

<task type="auto">
  <name>Task 2: effective_phase() — the one synthesis home</name>
  <files>backend/app/services/harness/grounding.py</files>
  <read_first>
    - `backend/app/services/harness/grounding.py` (the Phase-185 section added in plan 185-02: `KB_TOOLS`, `KB_TOOLS_SORTED`, `grounding_cause`; plus the RED LINE docblock at :13-15)
    - `backend/app/services/harness/skill_snapshot.py:263-289` (`graft_skill_snapshots` — the analog for WHERE and WHEN, and the wrong analog for HOW)
    - `backend/app/services/harness_engine.py:670-700` (the WR-03 retry-bound seed `validators[0].max_retries` at :684-686 — the reason APPEND is mandatory)
    - `.planning/phases/185-.../185-PATTERNS.md` §4 and §5
    - `.planning/phases/185-.../185-RESEARCH.md` §"The Attachment Seam" and §Landmines L-2
  </read_first>
  <action>
Add two functions to `harness/grounding.py`, in the Phase-185 section beside `grounding_cause`.

`_approval_sentence(phase, total_phases: int) -> str` — the D-185-14 engine-generated approval prompt.
There is NO new authored-message field; the sentence is composed at synthesis time, where
`len(definition.phases)` is in hand, and carried in the spec's `config["prompt"]`. Use the step's
`phase.name or phase.slug` as the label and its 1-based position:

    Step {phase.phase_index + 1} of {total_phases}, "{label}", is about to run.
    This step is marked as needing your approval first.
    The run is waiting here and will not continue until you answer.

(one string, spaces not newlines; ASCII double quotes around the label). Honesty rules to state in the
docstring: the ARMED copy MAY say the run waits — with plan 185-04's indefinite subscribe it is true —
and it never says *approved*, *safe* or *proven*; it states position, identity and consequence only. The
unarmed `llm_human_input` copy is untouched and must NOT say the run waits (SPEC Req 9).

`effective_phase(phase, *, total_phases: int)` — the phase the ENGINE runs: authored validators plus any
synthesized gates. Build a local `extra: list[ValidatorSpec]`:

  - if `getattr(phase, "action_risk_armed", False)`:
        `ValidatorSpec(kind="action_risk_approval", timing="pre", on_failure="ask_user",
                       max_retries=0, config={"prompt": _approval_sentence(phase, total_phases)})`
  - if `grounding_cause(phase) == "detected"`:
        `ValidatorSpec(kind="citations_required", timing="post", on_failure="fail_run",
                       max_retries=2, config={"mode": "retrieved_and_cited"})`   # D-185-04

Then:
  - `if not extra: return phase` — the IDENTITY early return, copied from `graft_skill_snapshots`'s
    `if not snapshots_map: return definition`. An ungoverned phase is handed back BY REFERENCE, which is
    what makes "byte-identical when unset" a structural property rather than a claim (D-14).
  - otherwise `return phase.model_copy(update={"validators": [*phase.validators, *extra]})`.

**APPEND, never prepend.** `harness_engine.py:684-686` seeds the retry bound from
`validators[0].max_retries`; prepending would change that seed for every phase that already has authored
validators. Appending keeps `validators[0]` the author's first spec, so the seed is byte-identical.

**Do NOT mutate.** `graft_skill_snapshots` writes `phase.config.skill_snapshot = ...` in place; that is
the right analog for where and when and the WRONG one for how. A synthesized spec must never be
observable to any other reader holding the same parsed object, so use `model_copy`.

**Never implement this as a Pydantic `model_validator`** (L-2). `db/workflows.py:349-356` persists
`json.dumps(definition.model_dump(mode="json"))`, so a `@model_validator(mode="after")` on `PhaseSpec`
would bake the synthesized gate permanently into the JSONB on the next save — after which removing the
KB tool would leave the gate attached forever, breaking Req 3's "removing the tool is the only exit" and
contradicting D-185-07.

The docstring must carry the contract sentences in `graft_skill_snapshots`' style: **NEVER persisted;
called from `harness_engine.py`'s `spec_by_slug` seam ONLY**; pure, no I/O, no pool. Also state D-185-05
explicitly: when the author already declared a `citations_required` validator, BOTH specs run — the
author's at its index and the engine's at index N — and `run_gates`' first-failure-wins loop means an
author's deliberately weak spec (`mode: presence, min_markers: 0`) passes and is then followed by the
real one. The author's spec cannot displace the engine's. That is what makes "not author-loosenable-away"
structural rather than trusted.

Import `ValidatorSpec` under the module's existing `if TYPE_CHECKING:` for typing and function-locally
for construction, matching the module's import-light posture.
  </action>
  <acceptance_criteria>
    - `grep -c "def effective_phase" backend/app/services/harness/grounding.py` returns 1.
    - `grep -c "model_copy" backend/app/services/harness/grounding.py` returns at least 1 and `grep -c "phase.validators.append\|\.validators +=" backend/app/services/harness/grounding.py` returns 0.
    - `grep -c "model_validator" backend/app/models/harness.py backend/app/services/harness/grounding.py` returns 0 for both files.
    - The synthesized citation spec is APPENDED: the returned list's element 0 is the author's first spec — asserted by a unit test in plan task 3.
    - `grep -c "retry\b" backend/app/services/harness/grounding.py` — the `on_failure="fail_run"` / `max_retries=2` pair from D-185-04 is present verbatim in the citation spec (verify by reading).
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_185_detection.py -q` still exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_185_detection.py tests/unit/test_validator_kinds.py -x -q</automated>
  </verify>
  <done>One pure synthesis function exists, appends, copies rather than mutates, and returns the identity for every ungoverned phase.</done>
</task>

<task type="auto">
  <name>Task 3: Swap the spec_by_slug seam and prove enforcement survives a deleted validator</name>
  <files>backend/app/services/harness_engine.py, backend/tests/unit/test_185_engine_attachment.py</files>
  <read_first>
    - `backend/app/services/harness_engine.py:1110-1180` (the `spec_by_slug` seam at :1118, `mark_phase_active` at :1156, the consumption at :1149, the `_run_phase_with_gates` call at :1173)
    - `backend/app/services/harness_engine.py:1440-1465` (`_load_run_definition` and the `graft_skill_snapshots` function-local-import call site to copy)
    - `backend/app/services/harness_engine.py:637-700, 750-800` (`_run_phase_with_gates`, the WR-03 rebinding at :755-757, `_route_on_failure`)
    - `backend/tests/unit/test_ask_user_disposition.py:29-95` (the `_phase()` / `_ctx()` helpers, `AsyncMock` + `patch.object` pairing, `asyncio.run` at the call)
    - `backend/app/services/harness/publish_service.py:688-835` (`_drive_golden_run` — confirm it is the ONLY publish execution entry and that it reaches the engine through `run_workflow`, not a parallel path. Verified 2026-07-29: the function-local import sits at `:717` and the call at `:830`. Re-confirm before recording the publish consequence — line numbers drift.)
    - `.planning/phases/185-.../185-PATTERNS.md` §4 and §T2
  </read_first>
  <action>
Replace `spec_by_slug = {p.slug: p for p in definition.phases}` at `harness_engine.py:1118` with a
comprehension over `effective_phase`, using a FUNCTION-LOCAL import in the `graft_skill_snapshots` house
style (`harness_engine` does not import these helpers at module top):

    from app.services.harness.grounding import effective_phase
    _total = len(definition.phases)
    spec_by_slug = {p.slug: effective_phase(p, total_phases=_total) for p in definition.phases}

Extend the existing comment above it to state: this is the ONE enforcement seam (GOVERN-01 Req 4); it is
downstream of every `WorkflowDefinition.model_validate()` (fresh kickoff, boot-time resume, AND the
publish golden run, which drives `run_workflow` from `_drive_golden_run`), and upstream of
`_run_phase_with_gates`; it never touches the save path, so no synthesized `ValidatorSpec` can ever be
persisted; and it leaves `workflow_phases` rows untouched.

Record the publish consequence in the same comment, because it must be stated rather than discovered in
UAT: the publish gauntlet gains NO new stage (D-185-11's letter holds), but publish BEHAVIOUR changes
for detected steps — a detected step whose golden run retrieves nothing now fails the golden-run stage
and blocks publish. Every `workflow_definitions` row is throwaway test data, so this is acceptable.

Create `backend/tests/unit/test_185_engine_attachment.py`, copying `test_ask_user_disposition.py`'s
posture (a header docstring naming which mocks are set and why; `AsyncMock`; `patch.object`;
`asyncio.run`; imports inside test bodies; all network deps mocked with explicit MagicMock attrs).
Drive `effective_phase` DIRECTLY rather than mocking the whole run loop:

  - criterion 9: a definition whose `llm_agent` phase carries `available_tools: ["search_documents"]`
    and `validators: []` yields an effective phase whose `validators` contains exactly one
    `citations_required` spec with `config == {"mode": "retrieved_and_cited"}`,
    `on_failure == "fail_run"`, `max_retries == 2`, `timing == "post"`. Then run `run_gates` over that
    effective phase with an agent-shaped output carrying `citations: []` and assert `passed is False`
    with the retrieval message.
  - criterion 10: the SAME fixture with the validator explicitly present in the JSONB and then removed
    produces an IDENTICAL effective validator list — deleting the declared validator does not remove
    enforcement.
  - D-185-05: a fixture whose author declared `{"kind": "citations_required", "config": {"mode":
    "presence", "min_markers": 0}}` yields an effective list of length 2 with the AUTHOR'S spec at index
    0 (the APPEND proof, which is also the WR-03 retry-seed proof) and the engine's at index 1; running
    `run_gates` over an output with `citations: []` and no markers still FAILS, at `validator_index == 1`.
  - criterion 18: `effective_phase` applied across a 5-phase definition returns 5 phases, every
    `phase_index` unchanged, and `len(definition.phases) == 5` — arming adds no phase.
  - the identity return: a phase with neither governance field set is returned by reference
    (`assert effective_phase(p, total_phases=1) is p`).

Add a FALSIFICATION control and record its observed output in the SUMMARY: temporarily change
`effective_phase` to PREPEND and observe the D-185-05 index assertion turn red naming index 0.
  </action>
  <acceptance_criteria>
    - `grep -n "effective_phase" backend/app/services/harness_engine.py` returns exactly 2 lines (the function-local import and the comprehension) and both are inside `run_workflow`.
    - `grep -c "spec_by_slug = {p.slug: p for p in definition.phases}" backend/app/services/harness_engine.py` returns 0.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_185_engine_attachment.py -q` exits 0 with all five cases green.
    - The identity assertion `effective_phase(p, total_phases=1) is p` passes for an ungoverned phase (proves the byte-identical-when-unset property structurally).
    - The prepend falsification was observed RED and its output is quoted in the SUMMARY.
    - `grep -n "run_workflow" backend/app/services/harness/publish_service.py` returns ≥ 1 line inside `_drive_golden_run`, and `grep -c "harness_engine" backend/app/services/harness/publish_service.py` shows no second engine entry point — the SUMMARY quotes both outputs. This closes RESEARCH open question 3 / assumption A6 with evidence BEFORE the publish consequence is recorded as fact.
    - `git diff --stat -- backend/app/services/agent_loop.py backend/app/services/tool_dispatcher.py backend/app/services/openai_service.py backend/app/services/anthropic_service.py` reports 0 files (D-14).
    - `git diff --stat -- supabase/migrations` reports 0 files.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_185_engine_attachment.py tests/unit/test_ask_user_disposition.py -x -q</automated>
  </verify>
  <done>Every run — including a published definition's next run and the publish golden run — carries the engine's gate, and a deleted validator cannot remove it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored definition JSONB → engine | An author-controlled (or hand-edited) definition is parsed and executed. This plan is where "the author cannot loosen the gate" becomes enforceable. |
| model output → gate | The LLM's own output is the input to `retrieved_and_cited`. Half (a) reads objects the model does not author. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-03-01 | Tampering | author-declared `citations_required` spec | mitigate | D-185-05: the engine's spec is APPENDED, never substituted. `run_gates` returns on first failure, so a deliberately weak author spec (`mode: presence, min_markers: 0`) passes at index 0 and the real gate still runs at index N. Asserted in `test_185_engine_attachment.py`. |
| T-185-03-02 | Spoofing | the model fabricating citations | mitigate | Half (a) reads `output["citations"]`, which the retrieval TOOL builds off `ToolResult` (`task_service.py:765`) — not text the model emits. Reading `source_refs` instead would be spoofable by a non-KB tool; the plan forbids it (L-1). |
| T-185-03-03 | Repudiation | gate audit trail | accept | The synthesized spec rides the shipped `gate_failed` audit row and SSE emit unchanged; `validator_index` stays correct end-to-end because the spec is appended and `run_gates` enumerates the full list. Accepted cost: a doubled gate row on hand-strict steps. |
| T-185-03-04 | Denial of service | retry loop on a permanently uncited step | accept | `max_retries: 2` bounds it, and the shipped consecutive-identical short-circuit (T-091-16) already stops a pointless loop. |
| T-185-03-05 | Tampering | persistence of a synthesized gate | mitigate | L-2: attachment is at the run seam, never a `model_validator`. Acceptance criterion greps that `model_validator` appears in neither `harness.py` nor `grounding.py`. A baked-in gate would make removing the KB tool fail to remove the gate. |

No package installs. No migration. Deep chat path untouched — the D-14 git-diff fence is an acceptance criterion.
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` — green.
- `git diff --stat -- supabase/migrations` — 0 files.
- `git diff --stat -- backend/app/services/agent_loop.py backend/app/services/tool_dispatcher.py backend/app/services/openai_service.py backend/app/services/anthropic_service.py` — 0 files.
- `git diff --stat -- frontend/` — 0 files.
</verification>

<success_criteria>
A published definition carrying a KB tool and no declared validator fails on uncited output. Deleting the
validator from the JSONB does not remove enforcement. An ungoverned phase is returned by reference.
</success_criteria>

<output>
Create `.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-03-SUMMARY.md` when done.
</output>
