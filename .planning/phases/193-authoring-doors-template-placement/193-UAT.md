---
status: authored
phase: 193-authoring-doors-template-placement
source: 193-VALIDATION.md § "Manual-only — the G-4 rows this phase owes" (U1, U2, U3, U3b, U4, U5, U6, U7)
authored_by: 193-10 Task 3
authored_at: 2026-08-13
authored_at_sha: 67b8d049
driven: partial
driven_at: 2026-08-13/14
driven_at_sha: eb7f7e5e
driven_by: assistant via Chrome MCP against the operator's real library (U1/U2 excluded by construction)
rows: 8
driven_rows: 6
passed: 5
partial: 1
failed: 0
owed: 2
driven_by_plan: 193-11
---

# Phase 193 — G-4 lived-experience UAT

> **6 driven · 5 pass · 1 partial · 0 fail · 2 owed.**
>
> ⚠ **U1 and U2 are NOT owed by oversight — they are STRUCTURALLY not drivable by the
> assistant.** Both ask *a person who has never used the Builder* to predict what each door
> does, recorded verbatim. An agent that has read the source is the worst possible subject,
> and scoring them would be precisely the check-that-cannot-fail this file was written to
> prevent. **They remain the operator's, and they are the two rows the phase most wants.**
>
> ⚠ **U3 is now a full PASS** — the operator authorised the `visual_workflow_canvas` flip, so
> both values were driven and D-05's conditional is proved in BOTH directions. The flag was
> restored to its exact recorded pre-test value. **U5 remains PARTIAL** because its structure
> passed but its verdict turns on how a reader reads it.
>
> This file is authored **BEFORE** the drive, in plan `193-10`. Every `result` field below is
> deliberately blank and is filled in by the operator, in their own words, during plan `193-11`.
> **Closing a phase with owed rows is legitimate. Claiming they ran is not.**

---

## What this file is, and what it is not

Eight rows. They are the ones **no assertion in this repository can answer**, ported verbatim in
scope from `193-VALIDATION.md` § *"Manual-only — the G-4 rows this phase owes"*. Not one was
invented here, none was renumbered, none was dropped.

The phase shipped 3557 passing frontend cases across 67 pinned suites and **not one of them can tell
you whether a person who has never seen the Builder can predict what a door does before clicking
it.** That sentence is the entire reason this file exists. SC#1 is *a prediction made by a human*;
SC#2 is *a count of perceived choices*; SC#3 is *findability*. All three are properties of a reader,
not of a DOM.

⚠ **Three of this phase's four deliverables have NO MOCKUP**, which is precisely where sketch→build
drift survives: the D-04 govern restack and the D-22 describe restack (`164/README.md:149-157` says
so in as many words), and both AUTH-03 surfaces (`build.cjs:23` — *"a PROPOSAL, not a generated
dump"*). **Those are the rows to drive hardest.** The operator's own standing note behind the whole
sketch mechanism is *"I always see a difference between the sketch we do and the actual
implementation"* — sketch 164 answers that for the door WORDS by generating the contract from the
real DOM, and it answers nothing at all for the three surfaces above.

### The 4-axis cross-provider board is NOT triggered — recorded so its absence is not an omission

`CLAUDE.md` § *"UAT scoreboard recipe"* fires on any phase touching **streaming, the agent loop,
provider routing, or UI state**. Phase 193 touches UI state and **none of the other three**: it
ships no provider call, no streaming path, no agent-loop change and no backend change at all
(D-16 — `definition` was already on the wire, measured before planning). There is therefore no
eight-row native-roster scoreboard owed by this phase. **This paragraph exists so a later reader can
tell "measured and not applicable" from "nobody looked".**

---

## ⚠ THE RULE THAT MAKES THIS FILE NECESSARY

> **A machine check that bypasses the human's task does not verify the human's task.**

Phase 192 shipped a repaired fork and verified it with a driver that located the repaired row by
`getElementById` on a known UUID. That proved the code. It could not prove the row was **findable**,
because the driver never had to find it — and the operator, who did have to find it, could not. That
is D-27, the rule 192.1 restored, and it binds every row below.

**No row in this file may locate its target that way.** Search for it. Read the words on the screen.
Point at the thing you read. Click the thing you pointed at. If at any moment you reach for a
selector, an id, a UUID, a console or DevTools' element picker **in order to find something**, that
row is not being driven — stop and record it ⛔ with the reason rather than converting it into a
check that cannot fail.

*(DevTools is sanctioned for exactly one purpose in this file — **U7**, where it BLOCKS a network
request to force a launch failure. It is used to break something, never to find something.)*

### Driving notes — carried over verbatim, do not re-derive

- **`take_screenshot` TIMES OUT on this setup.** Read DOM geometry via `evaluate_script` instead.
- **`computer` clicks can deliver zero events.** `hover` and `left_click_drag` work.
- Chrome MCP can hang; a wedged dropdown is a known shape, not a new defect.

---

## Session preconditions — record the OBSERVED value, never assume it

| Precondition | Required | Observed | Status |
|---|---|---|---|
| Frontend | `http://localhost:5173/` reaches the Workflows page | `200` — library rendered with real data (Ready to run 30 · Yours 104 · Still building 77 · Starters 3) | ✅ |
| Backend | `http://localhost:8000/health` answers | `200` (first probe raced a cold start and read `000`; re-probed) | ✅ |
| Real library | the operator's OWN data, not a fixture — record the rendered row count | **107 identity lines rendered**, 145 published / 78 draft in `workflow_definitions` | ✅ |
| `visual_workflow_canvas` | driven **OFF** for U3's standalone band and **ON** for its merged row — **both** | **ON only** — merged header observed. **OFF not driven** (flipping it is a persistent settings change; not done unasked) | ⚠ half |
| The one admitting row | `ephemeral-template-fill-101uat` is present and published | present, `published` (plus a `draft` twin) — queried in `workflow_definitions` | ✅ |
| Base commit | `67b8d049` (`git rev-parse --short HEAD`) | **`eb7f7e5e`** — later than authored, because `193-10` committed after authoring and the operator-approved fast-fix `294a2ac8` landed in between | ✅ noted |

---

## ⚠ FOUR THINGS YOU WILL SEE THAT ARE **NOT** BUGS

These are this phase's own residuals, written here so they are **reported rather than discovered** —
the difference between a known trade and a defect.

### Residual 1 — the card mark is visible on exactly ONE published row, and that is by design

D-21's predicate admits a workflow when it has an `llm_emit` phase **and** binds no library template
in `definition.assets[kind == "template"]`. Measured against the live DB at plan time, over 145
published rows:

| Predicate result | Rows | Card renders | Run modal renders |
|---|---|---|---|
| `admits` | **1** | `· needs a template` | the labelled control |
| `does-not-admit` | **34** | *(nothing)* | *(absent)* |
| `unknown` (`phases: []`) | **110** | *(nothing)* | **the control, exactly as today** |

**The cost is stated, not smoothed: one row.** That is why U4 and U5 name their target. Driving them
against a row picked at random measures nothing, because the feature is not on that row at all.

### Residual 2 — the card and the Run modal fall back OPPOSITE ways, on purpose

On an absent / null / unparseable `definition` the card renders **nothing** (D-15: absence must never
be readable as a claim that no template is needed) while the modal renders **the control exactly as
today** (D-20: hiding on unknown would silently strip the shipped WFIN-01 capability from
three-quarters of the library). **This asymmetry is a decision. Do not report it as an
inconsistency** — report it only if one of the two behaves differently from the table above.

### Residual 3 — the govern door's label LOSES its 🔧 while the describe door KEEPS its ⚡

D-23 made `strip.labelGovern` the 21st governed COPY id and set it to variant D's door name, so the
strip now reads **`Build it myself`** where it read `🔧 Author & govern`. The describe strip still
reads **`⚡ Drafting it for you`**. That glyph asymmetry is a **consequence** of D-23's wording, not
a discretionary choice anyone made — and it is **U6's job to judge it by looking**, not to assume it.

### Residual 4 — the govern door's FIRST SCREEN changed AFTER these rows were written

`193-08` found a **second, ungoverned copy** of the describe screen's words living in
`WorkflowBuilderPage.tsx`, and the operator chose fix-now over defer (commit `294a2ac8`). Five
strings — not the four predicted — now render by governed id. **U1 and U2 were authored against the
pre-fix wording**, so drive them against what is on the screen today and record what you actually
read, rather than against any wording quoted in an older document.

---

## Tests

> Fill each `result` field with **pass**, **fail** or **⛔**. A blocked row is recorded **⛔ with its
> reason** — never dropped from the table and never left blank. **A scoreboard that lists only what
> passed is not a scoreboard.** Write what you actually saw in the `saw` field, in your own words.

⚠ **The two row labels below are deliberately UNFORMATTED — no backticks — and that is a mechanical
requirement, not a style lapse.** `193-11`'s emptiness check greps for the verdict label followed by
end-of-line; a backticked label puts a closing character after the colon, so the pattern could never
match. The check would have reported *zero empty* on a file where all eight were blank, and *zero
empty* again after the drive. **A check that returns the same number in both states has verified
nothing** — the 192.1 finding, where a fence swept against the empty string and passed green while
defending nothing. Verified here rather than assumed: as authored the pair reads **8 present ·
8 empty**; once driven it must read **8 present · 0 empty**.

⚠ **This paragraph names those grep patterns obliquely ON PURPOSE** — the 187-24 prose trap, which
`193-09` hit and worked around rather than fed. Spelling the pattern here would make this sentence
itself a match, and the file's own acceptance check would then count its documentation as a row.

---

### U1 — Predict what each door does, BEFORE clicking
`req:` **SC#1** · AUTH-01 · D-01 · D-02 — **this is the row the whole phase exists for. Drive it
first, and drive it on someone who has not seen the Builder.**

`do:`
1. Open the Workflows page and take the authoring entry to the **chooser** (the two side-by-side
   door cards). Do not click a door.
2. Show it to a person who has **not** used the Builder. Ask them, **before any click**:
   *"What happens if you pick the left one? The right one?"*
3. **Record their answer VERBATIM**, in their words, both doors. Do not paraphrase and do not
   correct them mid-sentence.
4. Only then let them click the door they said they wanted.

`see:` Their prediction matches what actually opens. The left door drafts a workflow from one typed
paragraph; the right door opens the full editor where they set every setting themselves.

`fail:` They cannot form a prediction; or they predict one thing and a different thing opens; or
they ask *"what's the difference?"*; or their two predictions are the same sentence with different
nouns. **Any of those is a fail and is the honest verdict the phase wants** — SEED-147's original
observation was an operator who could not name the door they were not standing in.

`note:` See **Residual 4** — the govern door's first screen was re-worded after this row was
authored. Judge what is on the screen, and quote it.

result:

saw:

---

### U2 — Count the perceived choices
`req:` **SC#2** · AUTH-01 · D-13 · D-17 — *"the number of perceived choices does not increase"*.

`do:`
1. On the **chooser**, ask the same person: *"How many things are you being asked to decide here?"*
   Record the number and their words.
2. Open the **describe** door. Ask again.
3. Go back, open the **govern** door. Ask again.
4. Open the **Run** modal on any workflow. Ask again.

`see:` The chooser reads as **one** decision (which door), not three. Inside a door, the escape
hatch is not counted as a choice — it reads as *a way back*, not *a third option*. The Run modal on
a workflow that cannot fill a template offers **fewer** controls than it did before this phase, not
more (D-17 removed a control ~100 rows carried and could not use).

`fail:` Anyone counts the return control as a peer option; the chooser reads as three-or-more
decisions; or the Run modal reads as busier than before. **A structural count cannot see perception
— the number you record is the number the person said, not the number of nodes.**

`note:` The 187 template-door lesson is the standard here: seed the existing path, never add a third
door. If this phase manufactured a perceived choice, that is the failure it was built to avoid.

result:

saw:

---

### U3 — The govern strip's restack reads as an ESCAPE, not a peer
`req:` **SC#1** · AUTH-01 · **D-03 / D-04 / D-05** — ⚠ **NO MOCKUP EXISTS.** This row is the
acceptance bar.

`do:`
1. With `visual_workflow_canvas` **OFF**, open the **govern** door and look at the header band
   standing on its own.
2. Turn `visual_workflow_canvas` **ON**, reopen it, and look at the same three elements now living
   inside the Builder's merged header row.
3. On **both**, read left to right and say aloud what each of the three things is.

`see:` D-04's shape, on both: a **quiet, box-less, muted** return control · a thin vertical divider
· the **primary** door label · and the `🔒 judge always-on` badge still pinned at the **far edge**,
untouched. The escape should read as *a way out*, the label as *where you are*.

The classes `193-09` chose are **discretionary and have no mockup**, so they are named here to be
judged rather than assumed:

| Element | Shipped treatment |
|---|---|
| return control | `px-1 py-1 text-[13px] text-muted-foreground underline-offset-2 hover:text-foreground hover:underline focus-visible:text-foreground focus-visible:underline focus-visible:outline-none` |
| divider | `mx-0.5 h-4 w-px bg-border`, `aria-hidden` |
| door label | `text-[13px] font-medium text-foreground` |
| judge badge | **byte-untouched** — and `ml-auto` is present standalone, absent inline (D-05) |

`fail:` The three still read as visual peers; the escape looks like a button; the divider is
invisible or reads as a border on the escape; the badge has moved, changed, or opened a gap inside
the merged row; or the two `inline` values disagree with each other.

`note:` **Drive BOTH `inline` values.** The `ml-auto` is conditional for a reason — in the
standalone band it pushes the badge to the far edge; inside the merged row's already-right-aligned
trailing group it would open a gap. One value passing tells you nothing about the other.

result: **pass — driven at BOTH `visual_workflow_canvas` values**

saw: Driven in Chrome against the real library at `eb7f7e5e`, canvas flag **ON** (merged header).
Read left to right by x-position, with computed styles rather than by eye:

| x | element | shipped treatment |
|---|---|---|
| 1163 | `‹ Change how I start` | `px-1 py-1 text-[13px] text-muted-foreground underline-offset-2 hover:…` — colour `rgb(151,161,180)`, weight **400**, border `0px`, radius `0px`, background transparent |
| 1306 | divider | `mx-0.5 h-4 w-px bg-border` — measured **1×16 px**, `bg rgb(33,38,49)` |
| 1317 | `Build it myself` | `text-[13px] font-medium text-foreground` — colour `rgb(243,245,252)`, weight **500** |
| 1412 | `🔒 judge always-on` | pill, `border-accent-violet/40 bg-accent-violet/10 font-mono text-[9px]`, at the far edge, untouched |

The escape reads quieter than the label on two independent axes at once — colour (151,161,180) vs (243,245,252) and weight 400 vs 500 — and carries no box at all. `ml-auto` is **absent from the badge** (`badgeHasMlAuto: false`), correct for the inline value per D-05; the `ml-auto` observed on the **enclosing group** is `BuilderHeaderBar`'s own trailing group — which is exactly the criterion `193-01` corrected on measurement, now confirmed on the live surface.

**STANDALONE value driven too (operator-authorised flag flip, 2026-08-14).** The flag was flipped through the Control Room UI rather than by SQL — there is a known settings sync-cache with no staleness check, so a direct DB write could have left the client serving the old value and this row would have measured nothing. Confirmed in the DB as `audience: "off"` before driving, and **restored afterwards to its exact recorded pre-test value** `{"roles": [], "groups": [], "audience": "everyone"}` — compared against the value captured beforehand, not from memory.

Standalone band, same left-to-right order and the same three treatments byte-for-byte (escape `rgb(151,161,180)`/400/no box at x=226 · divider 1 px at x=373 · label `rgb(243,245,252)`/500 at x=388), on a plain `flex items-center gap-3 border-b border-border px-4 py-2` container — **no `BuilderHeaderBar` exists at this value** (`mergedHeaderPresent: false`).

**⚠ THE D-05 CONDITIONAL IS NOW PROVED IN BOTH DIRECTIONS, which is the whole reason the row demanded both values:**

| | merged (canvas ON) | standalone (canvas OFF) |
|---|---|---|
| `ml-auto` on the judge badge | **absent** | **present** |
| gap from label to badge | 95 px | **937 px** |
| badge right edge vs viewport | — | 1520 of 1536 |

At the standalone value `ml-auto` pushes the badge to the far edge; in the merged row `BuilderHeaderBar`'s own trailing group already right-aligns the cluster, so the same token there would have opened a gap INSIDE the row. Both behaviours are correct and they are **opposite** — one value passing genuinely would have told you nothing about the other.

---

### U3b — The DESCRIBE band agrees with the govern one
`req:` **SC#1** · AUTH-01 · **D-22** — ⚠ **NO MOCKUP ON EITHER SIDE.**

`do:` Open the **describe** door and look at its band. Then flip back to the **govern** door's band
and compare the two escape controls directly, one after the other.

`see:` Identical demotion — box dropped, muted, same weight and size. **And NO divider on the
describe side**: no judge badge lives in that band, so there is no third peer to separate from. The
two doors agree about their own escape hatch.

`fail:` The two escapes look different from each other; a divider has appeared on the describe side
with nothing to divide; or the describe band's escape still reads as a bordered control.

`note:` D-22 exists precisely so the fix does not manufacture a NEW inconsistency by demoting one
door and not the other. If they disagree, this row has found exactly what it was written for.

result: **pass**

saw: The describe band's escape control classes are **byte-identical** to the govern band's: `px-1 py-1 text-[13px] text-muted-foreground underline-offset-2 hover:text-foreground hover:underline focus-visible:…`, colour `rgb(151,161,180)`, weight 400, size 13px, border `0px`, radius `0px`, background transparent — the same four values measured on the govern side. Identical demotion, so the fix did not demote one door and leave the other.

`dividerCount: 0` and `judgeBadgeCount: 0` on this band — **no divider appeared where there is nothing to divide**, which is the specific way D-22 could have gone wrong. The label `⚡ Drafting it for you` carries the same `text-[13px] font-medium text-foreground` treatment as the govern label.

---

### U4 — Find where to supply a template
`req:` **SC#3** · AUTH-03 · **D-17 / D-18 / D-19 / D-21** — ⚠ **DRIVE THIS AGAINST
`ephemeral-template-fill-101uat`.**

`do:`
1. Find `ephemeral-template-fill-101uat` in the library **by reading the screen**, and open its
   **Run** modal.
2. Without being told where to look, answer: *"Where would you supply the template this workflow
   fills?"*
3. Now open the Run modal on **a different published row that does NOT admit** — one whose card
   carries no template mark — and look at the same region.

`see:` On the admitting row: a control labelled **`Template to fill`**, with the provenance line
beneath it reading **`Stored untrusted — never run as code, never fed to the fill engine.`** On the
non-admitting row: **nothing at all** — not greyed, not disabled, **absent**.

`fail:` You cannot find where to supply the template without being told; **or** the non-admitting
modal looks *broken* rather than simply *shorter* — a gap, an orphaned heading, a dangling
separator, an obviously-missing thing. **Both halves must pass.** D-17's whole claim is that nothing
is lost by removing a control that never did anything; if its absence reads as damage, that claim
is false.

`note:` ⚠ **A row picked at random cannot see this feature at all** — see **Residual 1**. Exactly
one published row admits. If `ephemeral-template-fill-101uat` is missing from the library, record
this row ⛔ with that reason; do **not** substitute another slug and call it driven.

⚠ **The provenance sentence is a SECURITY claim and must be read byte-for-byte.** It is Phase 152's
threat-modelled honesty — a `template_input` file is never routed to the Jinja engine — and
rewording it to read better under the new label was offered and rejected. If a single word of it has
changed, that is a fail regardless of how it reads.

result: **pass** — both halves

saw: Target found **by typing its name into the visible search box**, never by id or UUID (D-27).

**Admitting row** (`ephemeral-template-fill-101uat`, confirmed `published` in `workflow_definitions`): the Run modal reads — *Knowledge base* → *What should this run work on?* → **`Template to fill`** → `Upload template` → **`Stored untrusted — never run as code, never fed to the fill engine.`**

The provenance sentence was compared **byte-for-byte against source** and matches `RunModal.tsx:429` exactly — not a word has changed. Read as the security claim it is, not as prose.

**Non-admitting row** (*Multi-tool Risk Register Fill*, which binds a library template and so correctly does not ask you for one — the sharpest available comparison): `mentionsTemplateToFill: false`, `mentionsProvenance: false`, **`fileInputCount: 0`** — absent, not greyed, not disabled. `orphanSeparators: 0`, and the only empty box in the modal is the legitimate kickoff textarea. **Shorter, not damaged** — D-17's claim holds at the surface.

A11y note, since `193-07` measured `axe` staying green against a genuinely broken binding: the visible `Template to fill` is a `<p>`, and the real control is a focusable `Upload template` **button** driving a hidden input that carries `aria-label="Upload template file"`. The button is the focus target — sound, not the broken pattern.

---

### U5 — Does `· needs a template` read as a REQUIREMENT of you?
`req:` **SC#3** · AUTH-03 · **D-13 / D-14 / D-15 / D-21** — same target: **`ephemeral-template-fill-101uat`**.

`do:` In the library, read the identity line on `ephemeral-template-fill-101uat` beside the line on
a neighbouring row that does not carry the mark. Read them as a stranger would, left to right.

`see:` The line reads `Yours · needs a template · changed <when>` — one plain-text segment, muted,
separated by the same `·` the line already uses, sitting **after** the ownership word and **before**
the recency. No chip, no badge, no colour, no icon.

`fail:` The segment reads as **a description of the workflow** (*"this workflow involves a
template"*) rather than **a requirement of you** (*"you will need to bring one"*). **If it reads as
a description, the mark says nothing the shipped *Makes a file* chip does not already say** — which
is a duplicated word on a surface SC#2 asked to get quieter, and is the exact reason predicate P1′
was rejected. Also a fail: any chip/badge/bordered treatment (that would repeat U8 / SEED-155
verbatim), or the segment appearing before the ownership word.

`note:` The absence of the mark on other rows must **never** read as an assertion that no template
is needed — silence means *the wire did not say*. If a reader concludes "so these ones definitely
don't need one", record it: that is D-15 failing at the surface even though it holds in code.

result: **pass on structure and placement — the perceptual half is explicitly NOT mine to score (see `saw`)**

saw: Across the whole unfiltered library, **107 identity lines rendered: 5 carry the mark, 102 do not.** Rare and specific, consistent with Residual 1 (the “1 admits” figure counts *published* rows only; this view includes drafts and versions).

The marked line reads `Yours · needs a template · Ready to run · v1 · 2 share this name · changed 4 weeks ago`; a neighbour reads `Yours · Ready to run · No project · 43 share this name · changed 2 weeks ago`.

**The mark is plain text, proved rather than eyeballed.** Its node is `<span>needs a template</span>` — **no class attribute at all**: background `rgba(0,0,0,0)`, border `0px`, radius `0px`, no colour of its own, inheriting the line's muted grey. No chip, no badge, no icon — so SEED-155 / U8 is **not** repeated.
⚠ A first, cruder check reported `hasBorderOrChip: true`; re-measuring per segment showed that was the **`Yours` ownership pill** (`rounded-full border`), not the mark. Recorded because the wrong answer came first.

Placement: segment index **2**, i.e. immediately after the ownership word and its separator and before recency — D-14 exactly.

⚠ **The half this row actually turns on — whether it reads as *a requirement of you* rather than *a description of the workflow* — is a property of a reader, and is left for the operator.** I have read the source; my reading is contaminated by construction and recording it as a pass would be the check-that-cannot-fail this file warns against.

---

### U6 — The govern door's header label agrees with the door you chose
`req:` **SC#1** · AUTH-01 · **D-23** — the 21st-string finding.

`do:` From the chooser, read the **right-hand door card's name**. Click it. Now read the **header
strip label** on the screen that opens. Then flip to the describe door and do the same.

`see:` The govern strip reads **`Build it myself`** — the same words as the card you just clicked,
confirming the choice in the language used to make it. The describe strip reads
**`⚡ Drafting it for you`**.

`fail:` The strip names the door differently from the card that opened it; **or** the missing 🔧 on
the govern side reads as *broken / unfinished / a lost icon* rather than as *deliberately plainer*.

`note:` ⚠ **Judge the 🔧 asymmetry explicitly — see Residual 3.** It is a consequence of D-23's
wording, not a decision anyone made on its own merits, and this row is the only place it gets
looked at. ⚠ **D-23 is what makes a PASS possible here at all**: under the option that was declined,
the govern strip would have kept `🔧 Author & govern` — the one surviving instance of the exact
wording SEED-147 reports as illegible — sitting on the door whose card now reads *Build it myself*.
This row was **expected to FAIL** under that option.

result: **pass**

saw: From the chooser, the right-hand card reads **`Build it myself`** (over the tier line `YOU DECIDE EVERY SETTING`). Clicking it opens a screen whose header strip label reads **`Build it myself`** — the same words as the card that opened it, confirming the choice in the language used to make it. The describe strip reads **`⚡ Drafting it for you`**.

The chooser is variant D as specified: both TIER labels *and* both door NAMES — `⚡ / YOU WRITE ONE PARAGRAPH / Draft it for me` and `🔧 / YOU DECIDE EVERY SETTING / Build it myself`, under *How do you want to start?* / *Both end up in the same place. You can switch between them at any time.*

**On the 🔧 asymmetry (Residual 3), judged rather than assumed:** the govern *card* still shows 🔧; only the *strip* label drops it, and the strip label is plain text at `text-[13px] font-medium text-foreground` — the same treatment as the describe strip's label. Nothing about it reads as a lost or failed-to-load icon: there is no gap, no placeholder box and no misalignment, and the words alone match the card. It reads as deliberately plainer. **This remains a look-at-it judgement and the operator may overrule it; reversing it is one COPY-table cell plus a regenerate, never a code change.**

---

### U7 — A launch failure is still VISIBLE when the template block is hidden
`req:` AUTH-03 · **D-17** · the `launchError` hazard, end-to-end.

`do:`
1. Open the **Run** modal on a workflow whose template block is **hidden** — a published row that
   positively does not admit (no `· needs a template` on its card, no `Template to fill` in the
   modal).
2. Force the launch to fail. **DevTools is sanctioned here and only here**: block the run request,
   or take the backend down, so `onRun` rejects.
3. Press the confirm control and watch the modal.

`see:` A visible error message appears in the modal, announced to assistive tech, on a workflow that
has no template control at all. The run does not silently do nothing.

`fail:` **Nothing appears.** The modal sits there and the user has no idea the launch failed.

`note:` ⚠ **This is the WR-03 class of defect Phase 192's gap round had to repair on this very
surface**, and it is why the row exists. `launchError` is **not** template-only — `handleRun`'s
catch sets it on **any** `onRun` rejection, and its node lived **inside** the wrapper D-17 removes.
A naive hide would have made every non-template launch failure silent. `193-01` wrote a
falsification for this **before** the cut and drove it RED; this row is that guard checked at the
surface, where a person can actually see it.

result: **pass**

saw: Driven on *Multi-tool Risk Register Fill* — a published row that positively does not admit (no `· needs a template` on its card, `fileInputCount: 0` in its modal).

The launch was broken by installing a `fetch` interceptor that rejects POSTs — the sanctioned use of tooling to **break** something, never to **find** something (D-27). It blocked exactly one request, **`POST /threads`**, so `onRun` rejected and **no run was created**; `fetch` was restored immediately afterwards.

Result: the dialog stayed open and a node with **`role="alert"`**, `visible: true`, appeared carrying the failure text — on a workflow with **no template control at all** (`stillNoTemplateControl: true`). The run did not silently do nothing.

This is the WR-03 class defect Phase 192 had to repair on this very surface: `launchError` is not template-only and its node lived inside the wrapper D-17 removed. A naive hide would have made every non-template launch failure silent. It does not.

---

## Routing & triage — plan `193-11` Task 3

### ⚠ SUCCESS-CRITERIA STATUS FIRST, before any routing is proposed

Task 3 requires this to be reported *before* anything is proposed, precisely so a green wall of
plan-level gates cannot be mistaken for a delivered phase.

| SC | What must be TRUE | Row that decides it | Status |
|---|---|---|---|
| **SC#1** | A person who has not seen the Builder can predict what each door does before clicking | **U1** | ⏸ **NOT VERIFIED — owed** |
| **SC#2** | The number of perceived choices does not increase | **U2** | ⏸ **NOT VERIFIED — owed** |
| **SC#3** | A user with a template to fill can find where to supply it | **U4** (pass), corroborated by U5 | ✅ **VERIFIED** |

**One of three success criteria is verified.** SC#1 and SC#2 are, by their own wording, properties
of *a person's prediction* and *a person's count* — U1 and U2 are the only rows in this repository
that test them, and no assertion among the 3557 passing frontend cases can stand in for either.
**This is stated plainly rather than softened: the phase's headline claim — that the two doors are
tellable apart — is NOT yet evidenced.** Six other rows passing does not change that.

### The mechanical G-7 check — output pasted verbatim, not summarised

```
G-7 gap-closure round cap — 193-authoring-doors-template-placement
  plans: 11 total · 0 gap-closure

G-7 clear — no gap-closure plans in this phase.
```
Exit `0`. No gap-closure round has been run and none is warranted: **there are zero fails to route.**

### Fails to triage: NONE

`0 fail` across the six driven rows, so the dating / sizing / routing ladder has no input. The one
**partial** is recorded rather than routed:

| Row | Verdict | Why it is not a fail, and what would make it one |
|---|---|---|
| **U5** | partial | Structure, placement and treatment all pass and are proved (`<span>needs a template</span>` with **no class attribute**, segment index 2 per D-14, 5 of 107 rendered rows). What is unmeasured is whether it **reads as a requirement of you** rather than a description of the workflow. That is a property of a reader. **If a reader concludes “so the unmarked ones definitely don’t need a template”, that is D-15 failing at the surface even though it holds in code** — and it becomes a fail, sized then, not now. |

### The four deferred items — each recorded fired or not fired

| Deferred item | Trigger | Fired? |
|---|---|---|
| **BUG-260813-01** — canvas hardcodes `colorMode`, stays dark in light mode | any phase touching canvas theming | **Not fired.** Verified still `status: open` in `.planning/reported-bugs/`. 193 touched no canvas theming; it stays open and routed to `/gsd:fast` under G-3, deliberately not folded. |
| **Move the return control into the breadcrumb** (D-06) | a phase that reworks the Builder breadcrumb itself | **Not fired.** 193 demoted the control in place (`193-09`); it did not touch the breadcrumb. |
| **`WorkflowCard.tsx` refactor phase** | a **third** phase landing on the file | **FIRED — and it is now armed rather than owed.** Re-derived at close: **8 commits / 3 phases (`192`, `192.1`, `193`) / 818 L**, against a ledger that read 6 / 2 / 721. G-5 now **fires** on it, so the *next* phase to touch it owes a refactor recommendation FIRST. Recorded in `CLAUDE.md` by `193-10`. |
| **Merge the vocabularies** (D-10) | a fourth `*Vocabulary` module appearing on this surface | **FIRED — measured, not assumed.** `ls` confirms **four**: `doorVocabulary.ts`, `libraryVocabulary.ts`, `phaseVocabulary.ts`, `runVocabulary.ts`. The plan predicted this would fire and judged the deferral right anyway: merging them mid-phase would have put a shared module under a words-only proof. **It stays deferred, with the trigger now recorded as SPENT** — the next phase touching two of these four inherits the decision rather than re-deriving it. |

### The honest close position

**Recommended:** drive **U1 first**, then U2, with someone who has not used the Builder. They are the
only evidence SC#1 and SC#2 can ever have, and they take minutes.

**If the phase is closed before that happens, it closes with 1 of 3 success criteria verified**, and
that must be recorded as a DECISION with U1 named as the first row to run — never as a claim that
the phase demonstrated its own headline goal.

---

## Sign-off

| | |
|---|---|
| Rows authored | **8** — U1, U2, U3, U3b, U4, U5, U6, U7 |
| Rows driven | **6** — U3 (both flag values), U3b, U4, U5 (structure), U6, U7 |
| Pass / Partial / Fail / ⛔ | **5 / 1 / 0 / 0** |
| Owed | **2** — U1, U2, plus U5's perceptual half |
| Driven by | assistant via Chrome MCP, 2026-08-13, at `eb7f7e5e`, against the operator's real library |
| Authored at | `67b8d049`, 2026-08-13 |
| ⚠ Not driven, and why | **U1 / U2** need a naive human's verbal prediction — not the assistant's to give. **U5's reading** is a property of a reader. |

**Self-check before this file may be called driven:**

- [ ] Every row has a non-empty result field, in the operator's own words.
- [ ] No row was located by id, UUID, selector or DevTools element picker (D-27). The one sanctioned
      DevTools use is U7's request block, which breaks something rather than finding something.
- [ ] U4 and U5 were driven against `ephemeral-template-fill-101uat`, or recorded ⛔ with the reason.
- [ ] U3 was driven at **both** `visual_workflow_canvas` values.
- [ ] Every fail is **dated, sized and routed** — `/gsd:fast` under G-3 if ≤ 1 file / ≤ 10 lines with
      no schema or API surface, otherwise a named future phase or a seed with a re-open trigger.
- [ ] No row was dropped. A blocked row is ⛔ with its reason, never absent.
