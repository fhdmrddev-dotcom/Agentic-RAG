---
phase: 193-authoring-doors-template-placement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx
  - frontend/src/pages/__tests__/RunModal.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-01, AUTH-03]

must_haves:
  truths:
    - "D-08's capture wave ships FIRST: every characterization baseline this phase will be judged against exists in a commit that modifies NO source file, and the destination modules the later waves create answer 'does not exist in HEAD' at that commit"
    - "The six-state whole-innerHTML capture of WorkflowDoorSwitch includes BOTH govern states, so the D-05 `ml-auto` conditional is inside the baseline rather than outside it"
    - "A launch failure on a workflow that does NOT admit a template is proved visible on the SHIPPED tree, so D-17's later cut cannot make it silent without reddening a test that predates the cut"
    - "Every baseline row is captured from the tree as it ships, never hand-typed, and carries a marker row so byte-identity cannot ratify a blank render"
  artifacts:
    - path: "frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx"
      provides: "Six whole-innerHTML captures (chooser standalone/inline, describe standalone/inline, govern standalone/inline) + marker rows + the capture-SHA proof"
      min_lines: 120
      contains: "CAPTURE_SHA"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "BASELINE pin for the new suite, added in the commit that creates it"
      contains: "WorkflowDoorSwitch.baseline.test.tsx"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx"
      to: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      via: "rendering the real component and capturing container.innerHTML"
      pattern: "from \"\\./WorkflowDoorSwitch\""
    - from: "frontend/src/pages/__tests__/RunModal.test.tsx"
      to: "frontend/src/components/workflows/library/RunModal.tsx"
      via: "an onRun rejection on a non-admitting definition asserting run-upload-error"
      pattern: "run-upload-error"
---

<objective>
Capture every characterization baseline this phase will be judged against, on the **UNMOVED, UNREWORDED** tree.

Purpose: **D-08's wave order is load-bearing, not cosmetic.** A baseline only proves something if it
PREDATES the change (the 188.1 lesson, re-proved in 188.2, 192-06 and 192-08). Extracting or
rewording in the same commit as the capture leaves the move unprovable.

⚠ **This is D-08's "Wave 0".** It carries frontmatter `wave: 1` because GSD execution waves are
1-indexed; the property D-08 actually requires — *the capture commits modify no source file* — is an
acceptance criterion on both tasks below, mechanically checked, not a numbering convention.

Output: `WorkflowDoorSwitch.baseline.test.tsx` (six whole-`innerHTML` captures), one new
`RunModal.test.tsx` case pinning launch-failure visibility on a non-admitting workflow, and the
count-gate `BASELINE` entry for the new suite.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_protocol>
1. **FIRST action in the worktree, before the HEAD assertion and before reading anything:**
   `bash scripts/bootstrap-worktree.sh "$(pwd)"`
2. **Assert the dispatched base SHA.** 12 of 12 worktrees in Phase 192 forked from the wrong base
   (a `master` merge commit). Run `git merge-base --is-ancestor <dispatched-base> HEAD` and
   `git rev-parse HEAD`; if HEAD is not the dispatched base, `git reset --hard <dispatched-base>`
   before doing anything else. A baseline captured on the wrong base proves nothing.
3. `GSD_VITEST_MAX_WORKERS=4` on every vitest invocation. The cap holds at **two** concurrent runs.
4. Never `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.
</worktree_protocol>

<context>
@.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md
@.planning/phases/193-authoring-doors-template-placement/193-RESEARCH.md
@.planning/phases/193-authoring-doors-template-placement/193-PATTERNS.md
@.planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md
@frontend/src/components/workflows/WorkflowDoorSwitch.tsx
@frontend/src/pages/__tests__/RunModal.test.tsx
@.planning/sketches/164-telling-the-doors-apart/emit.test.tsx.src
</context>

<interfaces>
<!-- Extracted from the tree at HEAD f2087341. RE-DERIVE BY CONTENT, NOT BY NUMBER — this project
     has corrected line numbers in five of six recent waves. -->

`WorkflowDoorSwitch.tsx` exports:
```ts
export interface WorkflowDoorSwitchProps {
  def?: DefShape | null
  initial?: BuilderInitial
  renderPublish?: (def, draftId, blockedReason?, onPublishRunning?) => React.ReactNode
  onDescribeDraft?: (describe: string) => void
  initialDoor?: "both" | "describe" | "govern"
  registerCanLeave?: (canLeave: (() => boolean) | null) => void
  inline?: boolean
  headerLead?: React.ReactNode
}
export function WorkflowDoorSwitch(props: WorkflowDoorSwitchProps): JSX.Element
export default WorkflowDoorSwitch
```

Door roots (the capture subjects), by testid:
`workflow-doors` (chooser) · `door-describe` · `door-govern`.

The mock seam the govern door needs (it mounts the real `WorkflowBuilderPage`) — already shipped
verbatim in BOTH `WorkflowDoorSwitch.test.tsx` and `.planning/sketches/164-.../emit.test.tsx.src`:
```ts
const { mockGenerateWorkflow, mockListFolders, mockListSkills } = vi.hoisted(() => ({
  mockGenerateWorkflow: vi.fn(), mockListFolders: vi.fn(), mockListSkills: vi.fn(),
}))
vi.mock("@/lib/api", () => ({
  generateWorkflow: mockGenerateWorkflow, createWorkflowDraft: vi.fn(),
  updateWorkflowDraft: vi.fn(), listFolders: mockListFolders, listSkills: mockListSkills,
}))
```

`RunModal` props (from `library/RunModal.tsx`, already exercised in `RunModal.test.tsx`):
`wf` · `folders` · `authorDefaultFolderId` · `kickoff` · `submitting` · `onKickoffChange` ·
`onCancel` · `onRun`. `handleRun`'s catch sets `launchError` on **any** `onRun` rejection and the
node it renders is `data-testid="run-upload-error"` `role="alert"`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The six-state WorkflowDoorSwitch characterization baseline, captured on the unmoved tree</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` (385 L — the capture subject; read it whole)
    - `frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx` (the shipped suite: its `vi.hoisted` mock block and its `?raw` idiom are the conventions to match)
    - `frontend/src/pages/__tests__/RunModal.test.tsx` — the **canonical capture template**: the "WHY A CAPTURE AND NOT AN EXPECTATION" block, the `CAPTURE_SHA` constant, `RUN_MODAL_HTML_BASELINE`, the driver loop's non-vacuity guard, and the MARKER ROWS. Copy the SHAPE of all five.
    - `.planning/sketches/164-telling-the-doors-apart/emit.test.tsx.src` — the ready-made harness (mock set + render/`innerHTML` dump). Reuse its mocks; do not reinvent them.
    - `193-PATTERNS.md` § "Wave 0 — the `WorkflowDoorSwitch` characterization baseline"
    - `193-RESEARCH.md` § F.2 (the six states) and § F.1 (the mechanics)
    - `scripts/vitest-count-gate.cjs` — the `BASELINE` map and the two-knob comment block
  </read_first>
  <action>
Create `frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx`.

Capture SIX whole-`innerHTML` states of the REAL component, keyed exactly:
`CHOOSER_STANDALONE` (no props beyond `onDescribeDraft`), `CHOOSER_INLINE` (`inline` + a
`headerLead` node — exercises the chooser breadcrumb band), `DESCRIBE_STANDALONE`
(`initialDoor="describe"`), `DESCRIBE_INLINE` (`initialDoor="describe"` + `inline` + `headerLead` —
exercises `{inline && headerLead}` in the describe band), `GOVERN_STANDALONE`
(`initialDoor="govern"`, no `inline` — **the `ml-auto` PRESENT branch**), and `GOVERN_INLINE`
(`initialDoor="govern"` + `inline` + `headerLead` — **the `ml-auto` ABSENT branch, via `headerTrail`**).

Rows 5 and 6 are the load-bearing pair: they are the only two that differ by exactly the D-05 class
conditional, and a baseline missing either cannot detect the `ml-auto` regression D-05 warns about.
Capture the whole `container.innerHTML` (the 192-03 default `(m) => m.innerHTML`), never a subtree
and never a hand-typed expectation.

`headerLead` must be a fixed, deterministic node (e.g. a `<nav data-testid="host-lead">` carrying one
static text child) so the capture is stable across runs.

Port these five constructs from `RunModal.test.tsx` by shape:
  1. The "WHY A CAPTURE AND NOT AN EXPECTATION" rationale block, retargeted at D-08 and this phase's
     two moves — plus the "WHAT IS AND IS NOT CLAIMED TO BE BYTE-IDENTICAL" paragraph (the rendered
     DOM must be byte-identical; the moved SOURCE need not be, because the destination acquires a
     docblock and import lines).
  2. A `CAPTURE_SHA` constant holding `git rev-parse HEAD` at the capture commit, with the proof
     recorded in a comment: `git show HEAD:frontend/src/components/workflows/DoorHeaderStrip.tsx`
     and `git show HEAD:frontend/src/components/workflows/doorVocabulary.ts` must BOTH answer
     `fatal: path '…' does not exist in 'HEAD'` (exit 128). Paste the observed output.
  3. A `DOOR_HTML_BASELINE: Record<string, string>` of the six captured strings.
  4. The driver loop with its non-vacuity guard FIRST
     (`expect(DOOR_HTML_BASELINE[row].length).toBeGreaterThan(0)`) before the `toBe`.
  5. MARKER ROWS that read the COMMITTED baseline strings (never a fresh render) and assert what each
     capture CONTAINS, so a state that stopped painting is a failure rather than a pass:
     - `CHOOSER_*` contains `door-card-describe`, `door-card-govern`, and both shipped door names.
     - `DESCRIBE_*` contains `both-doors`, `describe-box`, `describe-draft`, `describe-hint`,
       `switch-strip`, `switch-to-govern`, `describe-soul-preview`.
     - `GOVERN_*` contains `both-doors`, `judge-locked`, and the shipped govern label.
     - `GOVERN_STANDALONE` contains `ml-auto` and `GOVERN_INLINE` does **not** — the D-05 pair,
       asserted off the committed strings.

Then add `"WorkflowDoorSwitch.baseline.test.tsx": <n>` to `BASELINE` in `scripts/vitest-count-gate.cjs`
in THIS SAME commit. A `BASELINE` entry pointing at a path that does not yet exist makes the gate
ERROR (exit 2), so it can only land here. **Read `<n>` off the gate's own printed `actual` column —
never hand-count `it(` blocks** (the house rule recorded five times in `librarySubtree.fences.test.ts`).
Correct the stale `BASELINE_TOTAL` marker comment in passing (it reads `⚠ 3384 (192.1-06)`; the
reduce computes a different number) — state the correction in the comment rather than overwriting it,
the file's own habit.

⚠ **This commit must modify NO source file.** `git diff --name-only` over it must list only
`WorkflowDoorSwitch.baseline.test.tsx` and `scripts/vitest-count-gate.cjs`.
  </action>
  <acceptance_criteria>
    - `git show <capture-sha>:frontend/src/components/workflows/DoorHeaderStrip.tsx` exits 128 with `fatal: path … does not exist in 'HEAD'`, and the observed output is pasted into the suite's comment block.
    - `git show <capture-sha>:frontend/src/components/workflows/doorVocabulary.ts` exits 128, same treatment.
    - `git diff --name-only <capture-sha>~1 <capture-sha>` lists exactly two paths, neither under `frontend/src/**` outside `*.test.tsx`.
    - `Object.keys(DOOR_HTML_BASELINE)` has length 6 and contains exactly `CHOOSER_STANDALONE`, `CHOOSER_INLINE`, `DESCRIBE_STANDALONE`, `DESCRIBE_INLINE`, `GOVERN_STANDALONE`, `GOVERN_INLINE`.
    - `DOOR_HTML_BASELINE.GOVERN_STANDALONE` contains the substring `ml-auto`; `DOOR_HTML_BASELINE.GOVERN_INLINE` does not. Both assertions are in the suite.
    - Every capture string is non-empty and every state has at least one marker row naming ≥3 testids.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` → all pass, 0 failed.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, no `[count-decrease]`, and the new file appears in the pinned map.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**, unmoved.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx</automated>
  </verify>
  <done>Six byte-exact captures of the unmoved tree exist, pinned in the gate, in a commit that touched no source file — and the two destination modules provably did not exist when they were taken.</done>
</task>

<task type="auto">
  <name>Task 2: Pin launch-failure visibility on a NON-admitting workflow, before D-17 can hide it</name>
  <files>frontend/src/pages/__tests__/RunModal.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/library/RunModal.tsx` — `handleRun`'s catch (`setLaunchError(e instanceof Error ? e.message : "Run failed")`), the `run-upload-error` node, and the whole `<div className="flex flex-col gap-1.5">` template-block wrapper D-17 will remove. Re-derive the spans by content.
    - `frontend/src/pages/__tests__/RunModal.test.tsx` — its fixtures (`boundPublished` / `unboundPublished`, which declare `config: { phase_type: "llm_emit", citation_policy: "draft" }` and **no `emitter` key**), its render helper, and `RUN_MODAL_HTML_BASELINE`.
    - `193-RESEARCH.md` § D.1 — the `launchError` hazard, measured.
    - `193-CONTEXT.md` § "Two inherited claims measured FALSE" — the second one is this hazard.
  </read_first>
  <action>
Add ONE case to `frontend/src/pages/__tests__/RunModal.test.tsx` that pins launch-failure visibility
on a workflow whose definition will read `does-not-admit` under D-21 once the predicate ships.

Build a fixture whose `definition.phases` contains **no** `llm_emit` phase — e.g. a single
`{ config: { phase_type: "programmatic" } }` phase. Render the modal with it, drive the launch
(`run-confirm`), have `onRun` reject with a distinct message (e.g. `"scope unavailable"`), and assert
that `run-upload-error` is present, has `role="alert"`, and carries that exact message.

⚠ Write the case docblock so a later reader knows WHY it exists rather than what it does:
`launchError` is set by `handleRun` on **any** `onRun` rejection, not only on the upload 422 its
neighbouring comment describes, and its node lives INSIDE the wrapper D-17 removes. Hiding that
wrapper naively makes launch failures silent on every non-template workflow — the WR-03 class of
defect Phase 192's gap round had to repair on this same surface. This case is captured NOW, on the
shipped tree where the block always renders, so that D-17's cut reds a test that predates it.

Do NOT touch `RUN_MODAL_HTML_BASELINE` — no capture string may be edited in this commit. Do NOT
change any source file. Do NOT raise `RunModal.test.tsx`'s count-gate pin here: a count INCREASE is
drift, not a gate failure, and plan `193-11` owns the pin sweep.
  </action>
  <acceptance_criteria>
    - The new case's fixture definition contains zero phases with `phase_type === "llm_emit"` (assert it inside the test, so the fixture cannot silently drift into admitting).
    - The case asserts `getByTestId("run-upload-error")` is in the document, `toHaveAttribute("role", "alert")`, and `toHaveTextContent("scope unavailable")` (or the chosen distinct message).
    - `git diff --numstat -- frontend/src/pages/__tests__/RunModal.test.tsx` shows **0 deletions** — insertions only. A deletion here would mean a baseline literal was edited.
    - `git diff --name-only` for this commit lists exactly one path, under `__tests__/`.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/__tests__/RunModal.test.tsx` → all pass, 0 failed.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/__tests__/RunModal.test.tsx</automated>
  </verify>
  <done>A non-template launch failure is proved visible on the shipped tree, in a commit with zero deletions and zero source edits — so D-17 cannot make it silent in silence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server `definition` JSON → jsdom fixture | The fixtures in these suites stand in for server-supplied JSON; a fixture that does not match a real wire shape produces a baseline that pins a fiction. |
| captured DOM string → committed literal | Once committed, a baseline string is treated as truth by every later wave; a wrong capture is indistinguishable from a right one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-01 | Repudiation | The capture's claim to predate the change | mitigate | `CAPTURE_SHA` is recorded in the suite and the `git show HEAD:<destination>` → exit 128 output is pasted verbatim for BOTH destination modules. Re-runnable, not asserted. |
| T-193-02 | Tampering | A baseline edited later to agree with a change it was meant to falsify | mitigate | `git diff --numstat` on the baseline files must show **0 deletions** through waves 2–3; any deletion is a re-capture and must be stated. Acceptance criterion on every later plan that touches them. |
| T-193-03 | Spoofing (of fact) | A byte-identical capture of a blank render | mitigate | Non-vacuity guard (`length > 0`) runs BEFORE each `toBe`, plus MARKER ROWS naming ≥3 testids per state, read off the committed string rather than a fresh render. |
| T-193-04 | Denial of Service | Oversubscribed vitest workers producing bare timeouts mistaken for real failures | mitigate | `GSD_VITEST_MAX_WORKERS=4` on every run; this wave dispatches at most 2 concurrent vitest runs (measured non-deterministic at 3, `192-05`). |
| T-193-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** No test-utility, no snapshot library, no serializer. `193-RESEARCH.md` § Environment records no missing dependency. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, `failed 0`, no `[count-decrease]`.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
- `cd frontend && npx eslint src/components/workflows/` → **≤ 10 errors**, and **0** in any file this plan touched.
- `git log --name-only <base>..HEAD` over this plan's commits lists no path under `frontend/src/**` that is not a `*.test.tsx`.
</verification>

<success_criteria>
- Six whole-`innerHTML` captures of `WorkflowDoorSwitch` exist, including both govern states, with marker rows and a recorded `CAPTURE_SHA`.
- Both destination modules (`DoorHeaderStrip.tsx`, `doorVocabulary.ts`) are proved absent at the capture commit.
- A non-admitting launch failure is proved visible on the shipped tree.
- No source file was modified by this plan.
- `tsc` 33, count gate exit 0.
</success_criteria>

<output>
Create `.planning/phases/193-authoring-doors-template-placement/193-01-SUMMARY.md` when done.
Record: the `CAPTURE_SHA`, the two `git show` exit-128 outputs verbatim, the six capture byte-lengths,
the gate's printed pin number for the new suite, and the corrected `BASELINE_TOTAL` figure.
</output>
