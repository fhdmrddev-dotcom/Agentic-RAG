---
phase: 193-authoring-doors-template-placement
plan: 11
type: execute
wave: 7
depends_on: ["193-10"]
files_modified:
  - .planning/phases/193-authoring-doors-template-placement/193-UAT.md
  - .planning/STATE.md
autonomous: false
requirements: [AUTH-01, AUTH-03]

must_haves:
  truths:
    - "The eight G-4 rows are DRIVEN by a human looking at the running app, and each carries a recorded `result:` — a scoreboard that lists only what passed is not a scoreboard"
    - "SC#1 is decided by a person's prediction before they click, recorded verbatim — no assertion in this repo can measure it (D-01's whole purpose)"
    - "SC#3 is driven against `ephemeral-template-fill-101uat`, the ONLY published row that admits under D-21, and against a non-admitting row beside it"
    - "The three surfaces with NO mockup — the D-04 govern restack, the D-22 describe restack, and both AUTH-03 surfaces (D-15 / D-17 / D-18) — are the ones driven hardest, because that is where sketch→build drift survives (D-03's own warning, and the SEED-155 lesson)"
    - "D-23's `🔧` asymmetry and D-19's untouched security sentence are both judged by looking, not inferred from a green suite"
    - "A row that cannot be driven is recorded ⛔ with its reason and the blocking issue id, never dropped"
  artifacts:
    - path: ".planning/phases/193-authoring-doors-template-placement/193-UAT.md"
      provides: "Eight rows each carrying a recorded `result:` — pass, fail with the operator's words, or ⛔ with a reason"
      contains: "result:"
  key_links:
    - from: ".planning/phases/193-authoring-doors-template-placement/193-UAT.md"
      to: "the running app at localhost:5173"
      via: "a human driving each row by looking, never by getElementById on a known id"
      pattern: "result:"
---

<objective>
Drive the eight owed G-4 rows and record what a person actually saw.

Purpose: **this phase's headline requirement cannot be tested.** AUTH-01 is *"a user can tell the two
authoring doors apart and predict what each will do, **before choosing**"* — a prediction by a human.
And three of the four deliverables have **no mockup at all**: the sketch says so for the restack, and
the AUTH-03 panel is marked a PROPOSAL rather than a generated dump. Those are exactly the places
sketch→build drift survives, and the only instrument that reaches them is a person looking.

⚠ This plan is **not autonomous**. It ends in a blocking checkpoint. Everything before the checkpoint
is preparation the operator should not have to do.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-authoring-doors-template-placement/193-UAT.md
@.planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md
@.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md
@.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md
@.planning/sketches/164-telling-the-doors-apart/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage the drive — confirm the targets exist and the acceptance bar is open</name>
  <files>.planning/phases/193-authoring-doors-template-placement/193-UAT.md</files>
  <read_first>
    - `.planning/phases/193-authoring-doors-template-placement/193-UAT.md` — the eight rows authored by `193-10`, with their recipes and pre-written `fail:` conditions
    - `.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md` **D-21** — why `ephemeral-template-fill-101uat` is the only row that can see AUTH-03
    - `.planning/sketches/164-telling-the-doors-apart/index.html` — the rendered comparison, tab **D · THE PICK**, which is U1/U2's reference
    - Every prior SUMMARY in this phase — for the discretionary Tailwind choices U3/U3b must judge and the `🔧` asymmetry U6 must judge
  </read_first>
  <action>
Prepare everything the operator would otherwise have to work out, and write it INTO `193-UAT.md`
beside each row so the drive is a sequence of looks rather than a research task.

1. **Confirm the AUTH-03 target still exists and still admits.** Query the local Supabase
   (`127.0.0.1:54322`, via `backend/venv/Scripts/python.exe` + psycopg2 — the system `python` on PATH
   lacks it) for `ephemeral-template-fill-101uat` and score `templateAdmission`'s three arms across
   published rows. ⚠ **Normalize the jsonb string-scalar trap first**: 194 of 223 `definition` values
   are JSON **string scalars**, so `definition->'phases'` silently returns nothing — use
   `(definition #>> '{}')::jsonb`. Record the counts beside U4 and U5.
   ⚠ If the slug is gone or no longer admits, **say so and name a replacement slug from the query**;
   do not let U4/U5 be driven against a row that cannot see the feature.
2. **Name a non-admitting published slug** for U4's second half and U7's launch failure, from the same
   query.
3. **Record how to toggle `visual_workflow_canvas`** both ways, since U3 must be driven at both
   `inline` values, and confirm the toggle's current state.
4. **Quote the exact Tailwind classes `193-09` chose** for the demoted return control and the divider,
   on both bands, beside U3 and U3b — they are discretionary, have no mockup, and are what the
   operator is judging.
5. **Quote the shipped govern label and the door-B card name** beside U6, so the agreement question is
   answerable by reading two strings; and state the `🔧` asymmetry explicitly as a second judgement.
6. **Quote D-19's provenance sentence** beside U4, so the operator can confirm by eye that it is
   byte-unchanged where the control renders.
7. **Confirm the app runs and the acceptance bar opens** — `.planning/sketches/164-telling-the-doors-apart/index.html`,
   tab **D · THE PICK**, is U1/U2's reference. Note that the doors panel is the real component's DOM
   while the template panel is a PROPOSAL.

Driving notes to restate at the top, verbatim, so they are not re-derived under time pressure:
`take_screenshot` **times out** on this setup — read DOM geometry via `evaluate_script`; `computer`
clicks can deliver zero events while `hover` / `left_click_drag` work; **no row may locate its target
by `getElementById` on a known id** (D-27 of Phase 192.1 — the rule Phase 192's re-drive broke).

⚠ **The operator starts the backend.** Do not start it; state what must be running and stop.
  </action>
  <acceptance_criteria>
    - The DB query is run with `(definition #>> '{}')::jsonb` normalization and its output pasted into `193-UAT.md`, including the three-arm counts across published rows.
    - `ephemeral-template-fill-101uat` is confirmed present and admitting — or a named replacement is recorded with its query evidence.
    - A non-admitting published slug is named for U4's second half and U7.
    - U3/U3b carry the exact Tailwind class strings from `193-09`, quoted.
    - U6 carries both strings to compare and names the `🔧` asymmetry as a second judgement.
    - U4 carries D-19's provenance sentence quoted byte-exactly.
    - The driving notes (`take_screenshot` timeout, `computer`-click caveat, the no-`getElementById` prohibition) appear at the top of `193-UAT.md`.
    - No row's `result:` field is filled by this task — it is preparation, not a drive.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "result:" .planning/phases/193-authoring-doors-template-placement/193-UAT.md</automated>
  </verify>
  <done>Every row is drivable from the file alone: its target named, its comparison strings quoted, its toggle described, and its evidence attached.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The operator drives the eight rows</name>
  <files>.planning/phases/193-authoring-doors-template-placement/193-UAT.md</files>
  <read_first>
    - `.planning/phases/193-authoring-doors-template-placement/193-UAT.md` — the eight rows as staged by Task 1, with their targets, comparison strings and pre-written `fail:` conditions
    - `.planning/sketches/164-telling-the-doors-apart/index.html` — tab **D · THE PICK**, the visual bar for U1/U2
  </read_first>
  <what-built>
**AUTH-01 — the doors.** Variant D's wording ships on all 21 governed strings, ported from the
generated build contract rather than typed (D-01 / D-02 / D-23). The door names now answer *who does
the work* (`Draft it for me` / `Build it myself`) and the two tier labels state *what it costs you*
(`you write one paragraph` / `you decide every setting`). The header strip is restacked on **both**
doors: the return control dropped its border/box and is now plain muted text, a divider separates it
from the current-door label on the govern side, and the `🔒 judge always-on` badge is byte-untouched
at the far edge (D-03 / D-04 / D-22). All of it is served by one extracted component across both
header variants (D-05), with the return control still in the strip rather than the breadcrumb (D-06).

**AUTH-03 — the template.** The library card now carries `· needs a template` as a plain text segment
in its identity line, immediately after provenance (D-13 / D-14), and renders **nothing** when the
wire does not positively say (D-15). The Run modal's upload control gained the label
`Template to fill` and is **absent** — not greyed, not disabled — on a workflow that positively does
not fill a template (D-17 / D-18). It still renders exactly as today when the definition is unknown
(D-20), the security provenance sentence is byte-unchanged (D-19), and a launch failure is still
visible when the block is hidden.

All automated gates are green and their numbers are quoted in the plan SUMMARIES: `tsc -p
tsconfig.app.json` at its 33 baseline, eslint 0 on every touched file, the count gate exit 0 with no
`[count-decrease]`, and every fence driven RED against a real plant and restored md5-identical.

⚠ **Three of these surfaces have NO mockup** — the two restacks and both AUTH-03 surfaces. That is
what these rows are for.

⚠ **The card mark is visible on exactly ONE published row locally** — `ephemeral-template-fill-101uat`.
Driving U4/U5 against any other row cannot see the feature at all.
  </what-built>
  <how-to-verify>
Open `.planning/phases/193-authoring-doors-template-placement/193-UAT.md` and drive **all eight rows**
in order. Each row names its target, its steps, and the `fail:` conditions that were written down
BEFORE the drive. Record a `result:` for every one — **in your own words** for the judgement rows.

1. **U1 (SC#1)** — the row the whole phase exists for. Show the chooser to someone who has not seen
   the Builder. **Before they click**, ask what each door will do. Record their answer **verbatim**.
2. **U2 (SC#2)** — count the perceived choices on the chooser and inside each open door. Did the
   number go up?
3. **U3** — the govern strip's restack, with `visual_workflow_canvas` **OFF** and again **ON**. Does
   the return control read as an *escape* rather than a peer of the two doors?
4. **U3b** — the same judgement on the **describe** door's band. Do the two doors agree?
5. **U4 (SC#3)** — open the Run modal on `ephemeral-template-fill-101uat` and on the named
   non-admitting row. Is `Template to fill` findable without being told? Is the control's absence on
   the second one *unremarkable* rather than *broken-looking*? And is the provenance sentence beneath
   it word-for-word the one quoted in the file?
6. **U5** — read a card carrying `· needs a template` beside one that does not. Does the mark read as
   *a requirement of you*, or as *a description of the workflow*? If it reads as a description, it
   says nothing the shipped *Makes a file* chip does not.
7. **U6** — open the **govern** door. Does its header label agree with the door card that opened it?
   And is losing the `🔧` glyph, while the describe side keeps `⚡`, right or wrong?
8. **U7** — trigger a launch failure on a workflow whose template block is hidden. **Is the error
   visible?**

**Rules that make the result mean something:**
- Drive **by looking**. No row may locate its target by `getElementById` on a known id.
- `take_screenshot` times out on this setup — read DOM geometry via `evaluate_script` if you need it.
- A row you cannot drive is recorded **⛔ with its reason and the blocking issue id** — never dropped.
- **Silence is a FAIL** on the judgement rows. "Looks fine" is not a result; the outcome must quote
  what you actually read on screen.
  </how-to-verify>
  <action>
Hand `193-UAT.md` to the operator and WAIT. Do not drive the rows on their behalf, do not mark any row
from a structural assertion, and do not summarise an outcome that has not been observed.

When the operator returns, transcribe each row's `result:` **in their words**. A paraphrase is a lost
measurement — this project has a recorded UAT fail whose entire value was the exact sentence the
operator used, and a recorded incident where a summary of finished work manufactured a request to redo
it. Where a row is blocked, record ⛔ with the reason and the blocking issue id rather than dropping
it, and update the header tally to the true count.
  </action>
  <acceptance_criteria>
    - All eight rows carry a non-empty `result:` — `pass`, `fail` with the operator's words, or ⛔ with a reason and a blocking id.
    - U1's `result:` contains the tester's prediction **verbatim**, recorded before any click.
    - U4 and U5 record which slugs they were driven against, and U4 covers BOTH the admitting and the non-admitting row.
    - U3 records both `visual_workflow_canvas` states.
    - U6 records a judgement on the `🔧` asymmetry, not only on the label agreement.
    - The header count is updated from `0 driven · 8 owed` to the true tally.
    - `grep -c "result: *$" .planning/phases/193-authoring-doors-template-placement/193-UAT.md` → **0** — no empty result field survives.
  </acceptance_criteria>
  <verify>
    <human-check>All eight rows carry a non-empty `result:` recorded in the operator's own words, and the header tally matches what was driven.</human-check>
    <automated>grep -c "result: *$" .planning/phases/193-authoring-doors-template-placement/193-UAT.md</automated>
  </verify>
  <done>Eight rows driven by a person looking at the running app, each with its own evidence, each result recorded verbatim — including any ⛔.</done>
  <resume-signal>Type "approved" when all eight rows carry a result — or describe what failed and it will be triaged under G-3 / G-7 rather than routed straight to another round.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the outcome honestly, and route every failure</name>
  <files>.planning/phases/193-authoring-doors-template-placement/193-UAT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/193-authoring-doors-template-placement/193-UAT.md` — the eight recorded results
    - `.planning/STATE.md` — the hand-edit warning at the top of the file and the § "Current Position" shape
    - `CLAUDE.md` § "Workflow guardrails" — **G-7** (the gap-closure round cap) and **G-3** (lightweight commands for small work)
    - `193-CONTEXT.md` `<deferred>` — the four deferred items whose re-open triggers may now have fired
    - `.planning/ROADMAP.md` § Phase 193 — the three success criteria
  </read_first>
  <action>
Write the outcome down in the form that survives a stale-state incident.

1. **Update `193-UAT.md`'s header tally** to the true `N driven · N passed · N failed · N owed`.
2. **Triage every fail, before proposing anything.** For each:
   - **Date the offending code** with `git log --diff-filter=A -- <file>`. A complaint about code
     written in this phase's own output is a signal to fast-fix, not to open a round. A complaint
     about code from an earlier phase changes only WHERE the fix goes, never whether the complaint is
     valid.
   - **Size it.** ≤ 1 file / ≤ 10 lines with no schema or API surface is `/gsd:fast` under **G-3** —
     never a gap-closure round.
   - **Run the mechanical check**: `node scripts/check-gap-closure-rounds.cjs 193`. Do not eyeball the
     round count. Record its output verbatim.
   - **Report success-criteria status FIRST.** If all three ROADMAP success criteria are verified, say
     so plainly and present *stopping* as a real option. A closure round may **never** introduce a new
     user-facing capability — that is a phase, not a gap.
   - A taste fail with no mechanical breakage may be **accepted and seeded** (the U8 / SEED-155
     precedent), provided the acceptance is recorded as a decision with its reason.
3. **Hand-edit `.planning/STATE.md`** — ⚠ **never call the `state.*` SDK verbs; seven of them write
   false records and corrupted this file five times in Phase 190 alone.** Record: the phase's
   position, the UAT tally, every fail with its routing, the G-7 check output, the fact that
   `WorkflowDoorSwitch.tsx` now has a hot-file ledger row and that **no G-5 override was recorded
   (D-09)**, and that `WorkflowCard.tsx` is now armed for G-5 in the phase after this one.
   ⚠ **Write what is TRUE at the moment of writing.** A state file that describes work already done
   manufactures a request to redo it — that is a measured incident in this project, not a caution.
4. **Check the four deferred items' re-open triggers** and record whether any fired: BUG-260813-01
   (the canvas `colorMode`, routed to `/gsd:fast`), moving the return control into the breadcrumb
   (D-06's re-open condition), the `WorkflowCard.tsx` refactor phase, and merging the vocabularies
   (D-10's re-open condition — note that `doorVocabulary.ts` is the FOURTH `*Vocabulary` module on
   this surface, so that trigger has already fired and the deferral was still judged right).
  </action>
  <acceptance_criteria>
    - `193-UAT.md`'s header tally matches the eight recorded results exactly.
    - Every fail carries: the `git log --diff-filter=A` date of the offending code, a size verdict, and a routing (fast-fix / defer with a named trigger / accept with a reason / gap round with a named unmet success criterion).
    - `node scripts/check-gap-closure-rounds.cjs 193` was run and its output is pasted verbatim into both `193-UAT.md` and the SUMMARY.
    - `.planning/STATE.md` was edited by hand — the SUMMARY states that no `state.*` SDK verb was called — and its frontmatter still parses (no unquoted multi-line string, the corruption shape that ballooned the file to 562 KB once).
    - The three ROADMAP success criteria are each reported as verified / unverified with the row that decides them, BEFORE any routing is proposed.
    - Each of the four deferred items is recorded as fired or not fired.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-gap-closure-rounds.cjs 193</automated>
  </verify>
  <done>The scoreboard says what happened, every fail has a route chosen on evidence rather than reflex, and STATE.md describes the present rather than a plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human observation → recorded result | The only instrument that reaches SC#1 and the three un-mockup'd surfaces. A paraphrased observation is a lost measurement. |
| recorded state → the next session's proposal | A stale or optimistic STATE.md manufactures a request to redo finished work — measured, not hypothetical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-46 | Spoofing (of fact) | A row marked pass on another row's evidence | mitigate | Each row records its own target slug, its own toggle state and its own observed text. U4 must record BOTH slugs; U3 must record BOTH `visual_workflow_canvas` states. |
| T-193-47 | Spoofing (of fact) | A row driven against a target that cannot see the feature | mitigate | Task 1 re-queries the live DB with the string-scalar normalization and confirms `ephemeral-template-fill-101uat` still admits, naming a replacement if it does not. Under D-21 it is the ONLY published row that admits. |
| T-193-48 | Repudiation | A phase closed claiming UAT it did not run | mitigate | The header tally is mechanical and `grep -c "result: *$"` must be 0; a row that cannot be driven is ⛔ with a reason and a blocking id, never dropped. Closing with rows owed is legitimate **as a stated decision**, never as a claim. |
| T-193-49 | Tampering | STATE.md corrupted by an SDK verb writing a false record | mitigate | STATE.md is HAND-EDITED; the SUMMARY asserts no `state.*` verb was called; the frontmatter is checked to still parse. Seven verbs write false records and corrupted this file five times in Phase 190. |
| T-193-50 | Elevation of Privilege | A closure round smuggling in a new capability | mitigate | `node scripts/check-gap-closure-rounds.cjs 193` is run and its output pasted; G-7's rule that a closure round may never add a user-facing capability is applied before any routing is proposed, and success-criteria status is reported FIRST. |
| T-193-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** The DB query uses the existing `backend/venv` psycopg2; the drive uses the shipped browser tooling. |
</threat_model>

<verification>
- All eight rows in `193-UAT.md` carry a recorded `result:`; `grep -c "result: *$"` → 0.
- The header tally matches the recorded results.
- `node scripts/check-gap-closure-rounds.cjs 193` output is recorded verbatim.
- `.planning/STATE.md` frontmatter parses and describes the present state.
- The three ROADMAP success criteria are each reported with the row that decides them.
</verification>

<success_criteria>
- Eight rows driven by a human looking at the running app, each with its own evidence.
- SC#1's prediction is recorded verbatim, before the click.
- SC#3 is driven against the one slug that can see it, and against a non-admitting row beside it.
- Every fail is dated, sized and routed on evidence; stopping is presented as a real option when the success criteria are met.
- STATE.md is hand-edited and true at the moment of writing.
</success_criteria>

<output>
Create `.planning/phases/193-authoring-doors-template-placement/193-11-SUMMARY.md` when done.
Record: the eight results in a table; U1's verbatim prediction; the slugs U4/U5 were driven against
and the live three-arm counts; the G-7 check output; every fail's date/size/routing; the statement
that STATE.md was hand-edited with no `state.*` verb called; and the four deferred triggers' status.
</output>
