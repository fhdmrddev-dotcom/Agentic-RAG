---
phase: 145-run-lifecycle-honesty-threads-py-extraction-stretch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-REPRO.md
autonomous: false
requirements: [FND-01]

must_haves:
  truths:
    - "D-145-11: the LIVE repro is captured BEFORE any fix lands — it reproduces Direction A WITHOUT a backend restart (pure missed-terminal SSE on an open connection, per U7) and records, with psycopg2 (:54322) + Redis evidence, WHY runs:active was empty during Direction B (restart-swept vs ZADD race vs reconciler _drop_stream)"
    - "D-145-11: the repro confirms (or corrects) research assumption A2 — whether a Deep run waiting on ask_user keeps runs.status='streaming' (not cap_paused) — and A3 — that no hidden frontend derive already clears streamingThreads (only the send-path finally does today)"
  artifacts:
    - path: ".planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-REPRO.md"
      provides: "Recorded live-repro observations for both directions + A2/A3 confirmation — the evidence gate that precedes every 145 fix"
      contains: "Direction A"
  key_links:
    - from: "145-REPRO.md"
      to: "BUG-260709-01"
      via: "reference repro cases (thread f308d617 / run 9f69ddf1 Dir A; DeepSeek 2075b364 + MiniMax 3bd13487 Dir B)"
      pattern: "9f69ddf1"
---

<objective>
Reproduce the run-state desync LIVE, on the current code, BEFORE any fix — the
locked "repro-first" gate (D-145-11). Two questions must be answered with real
DB + Redis evidence, not assumed:

1. **Direction A** — does the phantom-running / dead-Stop bug reproduce WITHOUT a
   backend restart (pure missed-terminal SSE with the connection still open, per
   RESEARCH U7), or only after a restart? This decides whether the frontend
   watchdog TIMER (Plan 05) is strictly necessary (U7 says it is).
2. **Direction B** — WHY was `runs:active` empty while `runs.status='streaming'`?
   (restart-swept by the boot reconciler `_drop_stream`, a ZADD/ZREM race, or a
   producer death). This confirms the co-write anti-drift design (D-145-02).

Also confirm two research assumptions the fix design rests on: A2 (an `ask_user`
wait keeps `runs.status='streaming'`, not `cap_paused`) and A3 (no hidden frontend
code already clears `streamingThreads` — grep says only the send-path `finally`
does).

Purpose: never ship a fix against a hypothesised bug. The corrected root-cause
trace (`c12ff264`) already reshaped this phase once — the live repro is the last
check that the reshaped model holds on the running system.
Output: `145-REPRO.md` recording the observations. NO code changes in this plan.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-CONTEXT.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-RESEARCH.md
@.planning/reported-bugs/BUG-260709-01-run-state-stop-button-desync.md
@backend/app/api/threads.py
@backend/app/api/runs.py
@frontend/src/providers/StreamsProvider.tsx

<evidence_tools>
<!-- The operator drives the browser; Claude captures DB/Redis state. Reference-only. -->
- Postgres (local): psycopg2 to `localhost:54322` (dev DB). Query `runs` for
  `run_id, thread_id, status, started_at, completed_at, error`.
- Redis (local, docker-compose.dev.yml): `ZSCORE runs:active <run_id>`,
  `ZRANGE runs:active 0 -1 WITHSCORES`, `XINFO STREAM run:{run_id}` →
  `last-generated-id` (`<ms>-<seq>`), `XLEN run:{run_id}`.
- The user starts the backend uvicorn themselves (never run_in_background). Wait
  for their confirmation before assuming the backend is up.
- Dev app: http://localhost:5173/ · test login fhdmrd@gmail.com / 123456.
- Reference repro cases (from CONTEXT <specifics>): Direction A = OpenAI thread
  `f308d617-04a6-4c59-b4c2-30d381b64a86` run `9f69ddf1` (completed 20:22:22, UI
  stuck "running" 16 min); Direction B = DeepSeek `ef317508…` run `2075b364`
  (xlen 4391) + MiniMax `e086075e…` run `3bd13487` (both `status='streaming'` in
  DB while `runs:active` empty).
</evidence_tools>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Reproduce both directions live and record the evidence in 145-REPRO.md</name>
  <files>.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-REPRO.md</files>
  <read_first>
    - .planning/reported-bugs/BUG-260709-01-run-state-stop-button-desync.md (the corrected static root-cause trace — three drifting signals, two directions with DIFFERENT causes, and the finding that the frontend does NOT read runs:active)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-RESEARCH.md §"U7 — Direction A repro shape" (why onTerminal never fired: missed terminal + open connection) and §"Assumptions Log" (A2, A3, A4)
    - backend/app/api/threads.py:364-489 (get_snapshot — the SELECT status='streaming' reconcile read the browser refresh already self-heals off) and :1697-1724 (Deep terminal sentinel + EXPIRE + ZREM ordering)
    - frontend/src/providers/StreamsProvider.tsx:194-236 (_isTransientStreamEnd probe), :1713-1715 / :2011-2023 (streamingThreads add on send / delete-in-finally), :2591-2616 (visibility/focus reconcile belt)
  </read_first>
  <action>
    This is the evidence gate — no source code is changed. Claude prepares the
    psycopg2 + Redis query helpers and writes a `145-REPRO.md` skeleton with three
    sections: "## Direction A", "## Direction B", "## A2/A3 confirmation". The
    OPERATOR drives the browser to trigger each scenario; Claude runs the DB/Redis
    queries at each step and records the readings verbatim (identifier-only — run_id,
    thread_id, status, timestamps, ZRANGE output, XINFO last-generated-id — never
    message content).

    Direction A (no restart): (1) operator sends a short prompt on an OpenAI model and
    lets it finish; (2) operator does NOT reload the tab; (3) Claude queries
    `runs.status` (expect `completed`) + `ZSCORE runs:active <run_id>` + the SSE
    stream state; (4) operator reports whether the Stop button / "running" persists
    with the tab open. Record whether the phantom reproduces WITHOUT a reload
    (confirms U7 → the Plan 05 watchdog timer is load-bearing) or only after a
    restart, and note the deviation if so.

    Direction B (producer death): (1) operator starts a stream on DeepSeek, then
    repeats on MiniMax (off the openai_compat path); (2) mid-stream, restart the
    backend (`--reload` save or kill/restart); (3) Claude queries `runs.status` (note
    if still `streaming`), `ZRANGE runs:active 0 -1` (note if empty), and
    `XINFO STREAM run:{run_id}` last-generated-id age. Record WHICH mechanism emptied
    `runs:active` (boot reconciler `_drop_stream` vs ZADD/ZREM race vs never-registered)
    and whether the Stop is absent.

    A2/A3: with a Deep `ask_user`-waiting run, Claude records the run's `status`
    (expect `streaming`, not `cap_paused` — A2). Claude greps StreamsProvider for any
    `streamingThreads` write outside the send path (expect none — A3). Write both
    findings into the A2/A3 section.
  </action>
  <verify>
    <automated>test -f ".planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-REPRO.md" && grep -q "Direction A" ".planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-REPRO.md" && git -C "C:/Vibe Apps/Agentic RAG" diff --name-only | grep -vqE '^backend/|^frontend/' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `145-REPRO.md` exists with a Direction A section stating, with a psycopg2 row + Redis reading, whether the phantom reproduces WITHOUT a restart.
    - A Direction B section names the concrete mechanism that emptied `runs:active` (with the ZRANGE output + XINFO last-generated-id age captured).
    - An A2/A3 section records the observed `ask_user`-wait `runs.status` value and the grep result for out-of-send-path `streamingThreads` writes.
    - No source file under `backend/` or `frontend/` is modified (`git diff --name-only` shows only the 145-REPRO.md doc).
  </acceptance_criteria>
  <resume-signal>Operator types "repro recorded" once 145-REPRO.md captures both directions + A2/A3. If Direction A does NOT reproduce without a restart, note it — the watchdog design still stands (it also covers the restart-flavored path) but flag the deviation for Plan 05.</resume-signal>
  <done>145-REPRO.md records both directions with real DB/Redis evidence + the A2/A3 confirmations; no code touched; the fix plans (02-05) are unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/dev tools → live dev DB + Redis | Read-only observation of `runs` + Redis keys via psycopg2/redis-cli on localhost dev instances. No production, no request-path code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01-01 | Information Disclosure | 145-REPRO.md capturing run rows | mitigate | Record identifier-only evidence (run_id / thread_id / status / timestamps / stream age). Do NOT paste message content, prompts, or secrets into the doc (T-073-04 logging discipline). |
| T-145-01-SC | Tampering (supply chain) | package installs | accept | No package-manager installs in this plan — T-145-SC not applicable. |
</threat_model>

<verification>
- `145-REPRO.md` present and answers the two direction questions + A2/A3 with real DB/Redis readings.
- `git diff --name-only` lists only the 145-REPRO.md doc (no code touched — repro precedes every fix).
</verification>

<success_criteria>
- Direction A reproduction (with/without restart) is settled with evidence — the watchdog-timer necessity (Plan 05) is confirmed or the deviation is flagged.
- The `runs:active`-empty mechanism for Direction B is named — the co-write anti-drift design (Plan 02) is grounded in the observed failure, not the hypothesis.
- A2 (ask_user status) and A3 (no hidden streamingThreads derive) are confirmed, unblocking the reconciler cap_paused decision (Plan 04) and the frontend derive (Plan 05).
</success_criteria>

<output>
Create `.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-01-SUMMARY.md` when done.
</output>
