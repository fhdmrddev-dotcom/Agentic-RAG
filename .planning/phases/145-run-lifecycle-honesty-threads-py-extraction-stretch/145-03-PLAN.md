---
phase: 145-run-lifecycle-honesty-threads-py-extraction-stretch
plan: 03
type: execute
wave: 3
depends_on: [145-02]
files_modified:
  - backend/app/api/threads.py
  - backend/app/api/runs.py
  - backend/tests/test_cancel_run.py
autonomous: true
requirements: [FND-01]

must_haves:
  truths:
    - "D-145-09: threads.py no longer owns run-lifecycle state transitions — the START register, SPAWN-FAIL cleanup, Deep TERMINAL, and continuation TERMINAL blocks all route through run_lifecycle.register_run_start / finalize_run_terminal; threads.py contains NO literal `zadd(\"runs:active\"` or standalone `zrem(\"runs:active\"` for chat lifecycle (G-5 extraction proof)"
    - "D-145-09: the SSE transport STAYS in threads.py — _emit/_emit_terminal XADDs, the terminal sentinel + EXPIRE run:{id}, get_snapshot, and RUN_TASKS are untouched; the owner is called for status+ZREM only, BEFORE the sentinel/EXPIRE, preserving the 075.4-03 status-first ordering"
    - "D-145-09: the cap_paused gate is preserved — finalize_run_terminal is called ONLY for true terminals; a cap_paused continuation still skips the ZREM (stays re-attachable), mirroring the existing threads.py:2205-2212 gate"
    - "D-145-14: the runs.py cancel zombie-heal terminal is routed through finalize_run_terminal(status='cancelled') for writer parity — NO new cancel logic is added; the ownership SELECT (404-not-403, T-062-01), already-terminal 204 short-circuit, happy-path task.cancel()+publish_cancel_sentinel, cancel_lock SETNX, and EXPIRE are all preserved verbatim"
    - "D-145-13: cancel correctness is proven — test_cancel_run.py asserts the cancel terminal co-writes runs.status='cancelled' AND ZREM runs:active for the same run_id"
  artifacts:
    - path: "backend/app/api/threads.py"
      provides: "producer wired onto the run_lifecycle owner; SSE transport intact"
      contains: "register_run_start"
    - path: "backend/app/api/runs.py"
      provides: "cancel zombie-heal terminal routed through finalize_run_terminal('cancelled') with a pool threaded in"
      contains: "finalize_run_terminal"
    - path: "backend/tests/test_cancel_run.py"
      provides: "cancel → finalize_run_terminal parity (status='cancelled' + ZREM ×2)"
      contains: "test_cancel_finalizes_and_zrems"
  key_links:
    - from: "threads.py producer"
      to: "run_lifecycle.finalize_run_terminal"
      via: "Deep + continuation terminal transitions"
      pattern: "finalize_run_terminal"
    - from: "runs.py cancel zombie-heal"
      to: "run_lifecycle.finalize_run_terminal"
      via: "cancelled terminal write (parity, D-145-14)"
      pattern: "finalize_run_terminal"
---

<objective>
Re-point the threads.py chat-run producer and the runs.py cancel zombie-heal onto
the atomic owner from Plan 02 — the actual G-5 extraction (D-145-09). After this
plan, threads.py no longer owns run-lifecycle status⇄mirror transitions: every
chat-run start and terminal goes through `run_lifecycle.register_run_start` /
`finalize_run_terminal`, so status and `runs:active` can no longer drift.

Behavior is preserved exactly — this is a mechanical re-wire, not a semantics
change (D-145-14: no new cancel logic; the cancel path already works per RESEARCH
U4). The SSE transport (sentinel/EXPIRE/get_snapshot/RUN_TASKS) stays put; only the
status+mirror writes move. The cap_paused re-attachability gate and every ownership
/ lock / short-circuit control is preserved verbatim.

Purpose: collapse the drift by construction — one owner, one atomic unit.
Output: threads.py + runs.py wired onto the owner + a cancel-parity test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-CONTEXT.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-RESEARCH.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-PATTERNS.md
@backend/app/services/run_lifecycle.py
@backend/app/api/threads.py
@backend/app/api/runs.py

<extraction_map>
<!-- PATTERNS §"The Extraction Seam" — VERIFIED line refs this session. Replace each
     inline block with the owner call; KEEP everything marked "stays". -->
| Replace in threads.py | With owner call | Keep verbatim |
|---|---|---|
| :1108-1127 insert_run + ZADD ×2 | register_run_start(pool=…, redis=redis, run_id=…, thread_id=…, user_id=…, model=_resolved_model, provider=_resolved_provider, spawned_by_worker=str(os.getpid())) | — |
| :1128-1142 spawn-fail UPDATE + ZREM ×2 | finalize_run_terminal(…, status="failed", error="spawn_failed", completed_at=now) | — |
| :1684-1693 Deep finalize + :1719-1724 ZREM ×2 | finalize_run_terminal(…) | sentinel XADD :1706-1710 + EXPIRE :1712-1717 stay AFTER the call |
| :2176-2188 continuation finalize + :2205-2212 ZREM ×2 | finalize_run_terminal(…) ONLY when _terminal_status != "cap_paused" | sentinel/EXPIRE :2189-2204 stay; cap_paused skip preserved |

Ordering constraint (075.4-03): status lands FIRST, THEN sentinel, THEN EXPIRE, THEN
(now inside the owner) ZREM. The owner does step-1(status)+step-5(ZREM); steps 2-4
(sentinel/EXPIRE) remain in the producer AFTER the owner call. Do NOT reorder.

runs.py cancel zombie-heal (:1176-1183 supabase UPDATE + :1246-1257 ZREM ×2) →
finalize_run_terminal(pool=pool, redis=redis, run_id=run_id, thread_id=<from the
ownership-SELECT row>, status="cancelled", error="cancelled_by_user",
completed_at=now). The handler has supabase+redis deps at :1097-1101 but NO pool —
thread one in (Depends(get_pg_pool) or `await get_pg_pool()`). The happy path
already reaches the owner via the producer's _shielded_finalize — only the
zombie-heal branch needs the swap.
</extraction_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the threads.py producer onto register_run_start / finalize_run_terminal</name>
  <files>backend/app/api/threads.py</files>
  <read_first>
    - backend/app/services/run_lifecycle.py (the owner signatures from Plan 02 — the exact keyword args to pass)
    - backend/app/api/threads.py:1100-1159 (START register + SPAWN-FAIL), :1680-1729 (Deep terminal + sentinel + EXPIRE + ZREM), :2170-2219 (continuation terminal + cap_paused ZREM gate), :152-183 (_emit/_emit_terminal — MUST stay), :364-489 (get_snapshot — MUST stay)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-PATTERNS.md §"backend/app/api/threads.py (EXTRACTION SOURCE)" (the per-block replace table + the cap_paused trap + "preserve verbatim" list)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-RESEARCH.md §"U5" (what moves vs stays)
  </read_first>
  <action>
    Import `from app.services.run_lifecycle import register_run_start,
    finalize_run_terminal`. Replace the four inline lifecycle blocks per the
    `<extraction_map>`: (1) START register :1108-1127 → one `await register_run_start(
    pool=..., redis=redis, run_id=..., thread_id=..., user_id=..., model=<the
    resolved model var>, provider=<the resolved provider var>,
    spawned_by_worker=str(os.getpid()))` call, deleting the inline insert_run + both
    ZADDs; (2) SPAWN-FAIL :1128-1142 → `await finalize_run_terminal(pool=..., redis=
    redis, run_id=..., thread_id=..., status="failed", error="spawn_failed",
    completed_at=<now>, ...)`, deleting the inline UPDATE + both ZREMs; (3) Deep
    TERMINAL :1684-1693 + :1719-1724 → `await finalize_run_terminal(...)` carrying the
    computed terminal status/error/completed_at/message_id/tokens, deleting the inline
    finalize_run + both ZREMs — but KEEP the terminal sentinel XADD (:1706-1710) and
    `EXPIRE run:{id}` (:1712-1717) in the producer AFTER the owner call, in the same
    order; (4) continuation TERMINAL :2176-2188 + :2205-2212 → `await
    finalize_run_terminal(...)` GATED exactly like today: call it ONLY when
    `_terminal_status != "cap_paused"` (a cap_paused run stays in the active sets,
    re-attachable) — preserve the existing skip branch and the sentinel/EXPIRE
    (:2189-2204). Do NOT move `_emit`/`_emit_terminal`, the sentinel/EXPIRE,
    get_snapshot, RUN_TASKS, or the `_RUN_STATUS_TO_TERMINAL_TYPE` enum mapping. Keep
    each Redis op's existing best-effort try/except discipline at the call site
    (owner stays thin). Preserve the ownership SELECT at :379-388 (get_snapshot).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/ -q -k "thread or run_reconciler or run_lifecycle" && grep -nc 'zadd("runs:active"\|zadd(\x27runs:active\x27\|zrem("runs:active"\|zrem(\x27runs:active\x27' backend/app/api/threads.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*#' backend/app/api/threads.py | grep -c 'zadd("runs:active"'` returns 0 — the ZADD moved into the owner (extraction proof).
    - `grep -vE '^\s*#' backend/app/api/threads.py | grep -c 'zrem("runs:active"'` returns 0 — the terminal ZREM also moved into the owner (fully machine-checks the G-5 "NO literal zadd/zrem for chat lifecycle" truth, not just the ZADD).
    - `grep -nE "register_run_start|finalize_run_terminal" backend/app/api/threads.py` shows both owner calls present.
    - The terminal sentinel XADD + `EXPIRE run:` still exist in threads.py (`grep -n "EXPIRE\|expire(" ...` around the Deep + continuation terminals) — transport preserved.
    - The cap_paused skip is intact: `finalize_run_terminal` in the continuation path is guarded by a `!= "cap_paused"` condition.
    - Existing thread/run tests remain green (no behavior change).
  </acceptance_criteria>
  <done>threads.py chat-run start + all terminals route through the owner; SSE transport + cap_paused gate + get_snapshot preserved; ZADD/ZREM("runs:active") gone from threads.py chat lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route the runs.py cancel zombie-heal through finalize_run_terminal + prove parity</name>
  <files>backend/app/api/runs.py, backend/tests/test_cancel_run.py</files>
  <behavior>
    - Cancelling a run whose producer task is missing but status='streaming' (zombie-heal) writes runs.status='cancelled' via finalize_run_terminal AND ZREMs runs:active + runs_by_thread for the same run_id — one owner call replacing the supabase UPDATE + separate ZREM ×2.
    - The happy path (task alive in RUN_TASKS) is unchanged — it already reaches the owner via the producer's _shielded_finalize.
    - Cross-user cancel still returns 404 (not 403); an already-terminal run still short-circuits 204; the cancel_lock SETNX + synthetic sentinel + EXPIRE are unchanged.
  </behavior>
  <read_first>
    - backend/app/api/runs.py:1090-1264 (the whole cancel_run DELETE: ownership SELECT :1107-1119, already-terminal 204 :1124, happy-path task.cancel()+publish_cancel_sentinel :1133-1155, zombie-heal UPDATE :1176-1183, cancel_lock SETNX :1208-1241, ZREM ×2 :1246-1257, EXPIRE :1259-1262, and the handler deps :1097-1101 — note there is NO pool dep to thread in)
    - backend/app/services/run_lifecycle.py (finalize_run_terminal signature)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-PATTERNS.md §"backend/app/api/runs.py (cancel parity)" (the "preserve" list + the pool-threading note)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-RESEARCH.md §"U4" (cancel path is correct; Open Q1 cross-worker is FLAGGED not fixed) + backend/tests/test_run_reconciler.py:34-136 (the _FakePool/_FakeRedis to reuse for the new test)
  </read_first>
  <action>
    First author `backend/tests/test_cancel_run.py` (grep first — it does not exist
    today). Reuse the `_FakePool`/`_FakeRedis` fakes (import or copy from
    test_run_reconciler.py / test_run_lifecycle.py). Test
    `test_cancel_finalizes_and_zrems`: exercise the zombie-heal terminal path (or, if
    the handler is hard to unit-drive, directly assert `finalize_run_terminal(...,
    status="cancelled", ...)` co-writes status + ZREM ×2 — the same atomicity recipe
    as test_run_lifecycle.py) — assert pool.executed grew with status "cancelled" AND
    the run_id left `runs:active`. Then in `runs.py`: thread a pool into the cancel
    handler (add a `pool = Depends(get_pg_pool)` param, matching how other asyncpg
    handlers obtain it, or `await get_pg_pool()` inside), and replace the zombie-heal
    supabase UPDATE (:1176-1183) + the subsequent ZREM ×2 (:1246-1257) with a single
    `await finalize_run_terminal(pool=pool, redis=redis, run_id=run_id,
    thread_id=<thread_id from the ownership-SELECT row>, status="cancelled",
    error="cancelled_by_user", completed_at=datetime.now(timezone.utc))`. PRESERVE
    verbatim: the ownership SELECT (:1107-1119, 404-not-403), the already-terminal 204
    short-circuit (:1124), the happy-path task.cancel() + publish_cancel_sentinel
    (:1133-1155), the synthetic sentinel + cancel_lock SETNX (:1208-1241), and the
    `EXPIRE run:{id}` (:1259-1262). Do NOT add cross-worker cancel logic (D-145-14 /
    Open Q1 is flagged in Plan 06, not fixed here). Keep the per-op best-effort
    try/except.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_cancel_run.py tests/test_run_lifecycle.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_cancel_run.py::test_cancel_finalizes_and_zrems` passes GREEN (status='cancelled' + ZREM proven together).
    - `grep -n "finalize_run_terminal" backend/app/api/runs.py` shows the zombie-heal now uses the owner.
    - `grep -vE '^\s*#' backend/app/api/runs.py | grep -c 'update({.*"status": "cancelled"'` shows the zombie-heal supabase UPDATE-to-cancelled is gone (replaced by the owner).
    - The ownership SELECT (404) + cancel_lock SETNX + happy-path task.cancel() are still present (`grep -n "publish_cancel_sentinel\|cancel_lock\|task.cancel" ...`).
  </acceptance_criteria>
  <done>runs.py zombie-heal cancel uses finalize_run_terminal('cancelled') for parity; every ownership/lock/short-circuit control preserved; cancel parity unit-proven; no new cancel logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DELETE /runs/{id} (cancel) | Untrusted run_id crosses into the owner-scoped cancel handler. |
| client → SSE producer (threads.py) | The producer runs under an already-authenticated request; the owner is called from inside it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-03-01 | Info Disclosure / EoP | cross-user cancel/snapshot | mitigate | PRESERVE the ownership SELECT (404-not-403, T-062-01) at runs.py:1107-1119 and threads.py:379-388 — the re-wire must NOT move or weaken them. Verified by grep in acceptance criteria. |
| T-145-03-02 | Tampering | cancel side-effects (lock / sentinel) | mitigate | PRESERVE cancel_lock SETNX (:1208-1241), synthetic sentinel, and EXPIRE — only the terminal WRITER is swapped to the owner; the cancel semantics are unchanged (D-145-14). |
| T-145-03-03 | Tampering (cross-worker cancel) | task.cancel in-process under WORKER_COUNT=2 | accept | Pre-existing gap (Open Q1): a Stop on the non-owning worker falls to zombie-heal while the live producer keeps running. NOT fixed here (honesty/extraction scope). Flagged for UAT + recorded in Plan 06's follow-up notes; re-open trigger = "a live run is not cancellable when Stop lands on the non-owning worker." |
| T-145-03-04 | Info Disclosure | error strings into RLS-readable runs.error | mitigate | The cancel/spawn-fail errors are short identifiers ("cancelled_by_user", "spawn_failed") — never tracebacks (T-073-04). |
| T-145-03-SC | Tampering (supply chain) | package installs | accept | No package-manager installs — T-145-SC not applicable. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/ -q -k "thread or cancel or run_lifecycle or run_reconciler"` → green.
- Extraction proof: `grep -vE '^\s*#' backend/app/api/threads.py | grep -c 'zadd("runs:active"'` == 0.
- `git diff --name-only` == threads.py + runs.py + test_cancel_run.py.
</verification>

<success_criteria>
- threads.py no longer owns chat-run status⇄mirror transitions (D-145-09 G-5) — all four blocks route through the owner; SSE transport + cap_paused gate intact.
- The cancel zombie-heal uses finalize_run_terminal('cancelled') for writer parity (D-145-14); no new cancel logic; every access-control gate preserved.
- Cancel correctness is unit-proven (D-145-13 cancel axis); the cross-worker gap is flagged, not fixed.
</success_criteria>

<output>
Create `.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-03-SUMMARY.md` when done.
</output>
