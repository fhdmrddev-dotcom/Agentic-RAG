---
phase: 116-document-relationships-backend-agent-tool
plan: 03
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - backend/app/services/tool_dispatcher.py
  - backend/app/services/openai_service.py
  - backend/tests/unit/test_116_tool_schema.py
  - backend/tests/unit/test_116_tool_wiring.py
  - backend/tests/unit/test_116_handler.py
  - backend/tests/unit/test_116_whitelist_guard.py
  - backend/tests/integration/test_116_tool_read.py
  - backend/tests/integration/test_116_tool_leak.py
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "get_related_documents is registered in _TOOL_REGISTRY AND advertised in get_tools() (the Deep-mode model actually sees it — SC#1, guards the Phase 101 harness-only-visibility bug)"
    - "The tool schema is Gemini-safe: NO anyOf, NO oneOf, NO multi-type `type` arrays — two flat optional scalar-string fields (document_id, filename); the either/or is stated in prose"
    - "The handler returns BOTH outgoing and incoming edges, incoming surfaced with the inverse label (superseded_by/amended_by/referenced_by/has_attachment)"
    - "Each edge's OTHER endpoint is re-checked for caller-readability; an unseeable target renders as 'linked document (no access)' — never the target's real title/metadata (D-116-9, SC#2)"
    - "An unresolvable subject (bad document_id / unknown filename / neither provided) returns a calm ToolResult JSON string — NEVER a raise into the agent loop"
    - "Compact citable rows + source_refs (seeable endpoints only); the dispatch_tool whitelist guard refuses the tool when excluded and dispatches when phase_whitelist is None (byte-identical Deep)"
  artifacts:
    - path: "backend/app/services/tool_dispatcher.py"
      provides: "_handle_get_related_documents handler + one _TOOL_REGISTRY line"
      contains: "_handle_get_related_documents"
    - path: "backend/app/services/openai_service.py"
      provides: "GET_RELATED_DOCUMENTS_TOOL schema + one get_tools() entry"
      contains: "GET_RELATED_DOCUMENTS_TOOL"
  key_links:
    - from: "backend/app/services/tool_dispatcher.py"
      to: "_resolve_readable_latest (shared resolver, Plan 01)"
      via: "subject + per-endpoint readability resolution"
      pattern: "_resolve_readable_latest"
    - from: "backend/app/services/openai_service.py"
      to: "get_tools() assembly"
      via: "GET_RELATED_DOCUMENTS_TOOL in the tools list"
      pattern: "GET_RELATED_DOCUMENTS_TOOL"
    - from: "backend/app/services/tool_dispatcher.py"
      to: "_TOOL_REGISTRY"
      via: "get_related_documents handler entry"
      pattern: "\"get_related_documents\""
---

<objective>
Ship the read-only agent tool `get_related_documents` (REL-04): a leak-safe handler over the directional edges, dual-wired into `_TOOL_REGISTRY` AND `get_tools()` so the Deep-mode model sees and calls it. Returns both directions (inverse-labeled), masks unseeable targets as "linked document (no access)", and never raises into the loop.

Purpose: The agent traverses the human-curated graph (D-116-8); SC#2 (dual-wiring + Gemini-safe schema + leak-safe masking) lands here. The masking is the ONE genuinely net-new behavior — proven LIVE two-user, non-vacuous (the "static would false-green" lesson).
Output: `tool_dispatcher.py` (+1 handler, +1 registry line), `openai_service.py` (+1 schema, +1 get_tools entry), 6 tool tests un-marked incl. the live leak proof.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-CONTEXT.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-RESEARCH.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-PATTERNS.md

<interfaces>
<!-- From Plan 01 + the codebase. Build against these directly. -->

From `backend/app/services/document_relationship_service.py` (Plan 01):
- `_resolve_readable_latest(doc_id_or_filename, caller, *, by_filename=False, supabase=None)` → latest readable doc row or None. Use for BOTH the subject resolution AND each edge's other-endpoint readability re-check.

The handler clone target — `backend/app/services/tool_dispatcher.py:302-467` (`_handle_query_documents_by_view`):
- Compact-rows + source_refs shape (`:425-467`): `compact = [{...} for d in rows]`; `source_refs = [{"document_id": d["id"], "filename": d["filename"]} for d in rows]`; `return ToolResult(result=json.dumps({...}), source_refs=source_refs)`.
- Calm-error contract (`:352-360, 418-423`): every failure path returns `ToolResult(result=json.dumps({...}))` — NEVER raises (the 115 WR-01/WR-03 lesson). `agent_loop.py:2097` catches ValueError as a backstop but the handler must NOT depend on it.
- Fire-and-forget audit getattr-guard (`:439-449`): `_spawn = getattr(ctx, "spawn", None); if callable(_spawn): ...`.
- `ToolContext` fields: `current_user` (`{"id": str}`), `supabase`, `spawn`, `model`. Do NOT thread `folder_subtree_ids` (whole-KB like the 115 view path). `ToolResult(result, source_refs, citations, similarity_score)`.

The `_TOOL_REGISTRY` (`tool_dispatcher.py:2567-2598`) — the 115 line is `"query_documents_by_view": _handle_query_documents_by_view,` at `:2596-2597`. Add ONE line after it.

The dispatch whitelist guard (`tool_dispatcher.py:2640-2655`): `if ctx.phase_whitelist is not None and tool_name not in ctx.phase_whitelist: ... return refusal`. `phase_whitelist is None` in Deep → byte-identical. The new tool inherits this — NO change here (SC#2 free).

The schema clone target — `backend/app/services/openai_service.py:17-53` (`SEARCH_DOCUMENTS_TOOL` — flat scalar). THE LANDMINE: `QUERY_DOCUMENTS_BY_VIEW_TOOL:159` has `"value": {"type": ["string","number","boolean","null"]}` — the a5b0b917 Gemini-breaker. The 116 schema MUST use flat scalar strings only.

The `get_tools()` assembly — `openai_service.py:967-988`: `tools = [SEARCH_DOCUMENTS_TOOL, QUERY_DOCUMENTS_TOOL, QUERY_DOCUMENTS_BY_VIEW_TOOL, LS_TOOL, ...]`. Add `GET_RELATED_DOCUMENTS_TOOL` to this list. The `:978-981` comment documents the dual-wiring contract (registry AND get_tools = Deep-visible).

The Google boundary (defense-in-depth, do NOT rely on it) — `google_service.py:256-257` (anyOf/oneOf stripped silently → constraint vanishes), `:270` (`_translate_nullable_type` collapses multi-type arrays). 116 avoids both by construction.

Inverse-label map (RESEARCH / D-116-2): `{"supersedes":"superseded_by","amends":"amended_by","references":"referenced_by","attached_to":"has_attachment"}`.

The leak-proof clone target — `backend/tests/integration/test_115_tool_global_leak.py` (drives the HANDLER, not the RLS label; `_pg_reachable` 2s guard, imports in test bodies).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET_RELATED_DOCUMENTS_TOOL schema (Gemini-safe) + get_tools() entry; _handle_get_related_documents handler + _TOOL_REGISTRY line</name>
  <files>backend/app/services/openai_service.py, backend/app/services/tool_dispatcher.py, backend/tests/unit/test_116_tool_schema.py, backend/tests/unit/test_116_tool_wiring.py, backend/tests/unit/test_116_handler.py, backend/tests/unit/test_116_whitelist_guard.py</files>
  <read_first>
    - backend/app/services/tool_dispatcher.py:302-467 (the leak-safe handler clone target), :2567-2598 (_TOOL_REGISTRY), :2633-2655 (dispatch_tool whitelist guard)
    - backend/app/services/openai_service.py:17-53 (SEARCH_DOCUMENTS_TOOL shape), :107-189 (QUERY_DOCUMENTS_BY_VIEW_TOOL + the :159 multi-type landmine), :967-988 (get_tools assembly + the dual-wiring comment)
    - backend/app/services/document_relationship_service.py (Plan 01 — _resolve_readable_latest)
    - backend/app/services/google_service.py:256-257,270-309 (the anyOf/oneOf strip + nullable-type collapse — why scalar-only matters)
    - backend/app/services/agent_loop.py:2080-2108 (handler dispatch + source_refs accumulation + the ValueError calm-catch backstop)
    - supabase/migrations/071_dm_foundations.sql:135-150 (user-scoped RLS — the edges returned are the caller's own; masking applies to TARGET DOC access)
  </read_first>
  <behavior>
    - The schema params contain NO anyOf, NO oneOf, and NO property whose `type` is a list; `document_id` and `filename` are each `type: "string"`; no top-level `required` (the either/or is prose-only).
    - `get_related_documents` is present in BOTH `_TOOL_REGISTRY` AND the `get_tools()` returned list.
    - Handler with a valid subject document_id holding outgoing+incoming edges → returns both, each incoming row carrying the inverse label.
    - Handler where an edge's other endpoint is unseeable to the caller → that row is `{"document_id": None, "filename": "linked document (no access)", "rel_type": ..., "direction": ...}` (never the real filename/metadata); source_refs excludes it.
    - Handler with a bad document_id / unknown filename / neither arg → a calm ToolResult JSON string (status + hint), NOT a raise.
    - dispatch_tool refuses `get_related_documents` when `phase_whitelist` excludes it; dispatches when `phase_whitelist is None`.
  </behavior>
  <action>
    In `backend/app/services/openai_service.py`: add `GET_RELATED_DOCUMENTS_TOOL` (mirror `SEARCH_DOCUMENTS_TOOL` shape) — `name="get_related_documents"`, a description that names BOTH directions + the four inverse labels (superseded_by/amended_by/referenced_by/has_attachment) + example prompts + "Identify by `document_id` (preferred) OR exact `filename`. Provide exactly one." Params = two flat scalar-string properties (`document_id`, `filename`), NO top-level `required`, NO anyOf/oneOf, NO multi-type `type` arrays (the either/or in PROSE only — the a5b0b917 discipline). Add `GET_RELATED_DOCUMENTS_TOOL` to the `tools = [...]` list in `get_tools()` (after `QUERY_DOCUMENTS_BY_VIEW_TOOL`), with a Phase 116 REL-04 / SC#1 / D-116-10 comment mirroring the 115 dual-wiring comment at `:978-981`.
    In `backend/app/services/tool_dispatcher.py`: add `_handle_get_related_documents(args, ctx)` cloning `_handle_query_documents_by_view`:
    1. Resolve the subject → its latest version id via `_resolve_readable_latest` (document_id arg preferred; else exact filename `by_filename=True`; neither/unresolvable → a calm "not found, here's what to do" ToolResult string — NEVER raise).
    2. Two queries on the subject's LATEST id (own-scoped, the table is user-scoped): outgoing `source_doc_id = subject`, incoming `target_doc_id = subject` (wrap every `.execute()` in `aexec`).
    3. For EACH edge's OTHER endpoint: re-check caller-readability via `_resolve_readable_latest` + resolve to latest; if unseeable, emit the `"linked document (no access)"` masked row (D-116-9) — never the real title/metadata. This is the SC#2 net-new behavior.
    4. Compact rows: `{document_id, filename, rel_type, direction ("outgoing"|"incoming"), label (rel_type for outgoing, _INVERSE_LABEL[rel_type] for incoming)}`; `source_refs` = the SEEABLE endpoints only (masked rows contribute no citable ref). Define `_INVERSE_LABEL` per D-116-2.
    5. Read-audit policy (RESEARCH Open Question 1 / A2 — Claude's discretion): DEFAULT to NO read audit (D-116-12 mandates audit only for create/remove; no SC requires a read audit; do NOT add a new enum value). If a read receipt is wanted, reuse `search.query` tagged `via:"relationship"` via the getattr-guarded `ctx.spawn` pattern — but default to skipping it.
    Add EXACTLY ONE line to `_TOOL_REGISTRY` (after the 115 `query_documents_by_view` line): `"get_related_documents": _handle_get_related_documents,` with a Phase 116 REL-04 / G-5 comment. Do NOT modify `threads.py`, the `dispatch_tool` guard, or any provider service. Un-xfail `test_116_tool_schema.py`, `test_116_tool_wiring.py`, `test_116_handler.py`, `test_116_whitelist_guard.py`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_116_tool_schema.py tests/unit/test_116_tool_wiring.py tests/unit/test_116_handler.py tests/unit/test_116_whitelist_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_116_tool_schema.py` passes: the params dict has NO `anyOf`/`oneOf` key anywhere and NO property `type` that is a list; `document_id` and `filename` are `type:"string"`.
    - `test_116_tool_wiring.py` passes: `get_related_documents` is in `_TOOL_REGISTRY` AND in `get_tools()` output (SC#1, D-116-10).
    - `grep -n "\"get_related_documents\": _handle_get_related_documents" backend/app/services/tool_dispatcher.py` → exactly one line.
    - `grep -n "GET_RELATED_DOCUMENTS_TOOL" backend/app/services/openai_service.py` → the schema constant AND the get_tools list entry.
    - `test_116_handler.py` passes: both directions + inverse labels + masked-row shape + calm-error-on-unresolvable (no raise).
    - `test_116_whitelist_guard.py` passes: refused when excluded, dispatched when `phase_whitelist is None`.
    - `backend/app/api/threads.py` NOT in the diff; `dispatch_tool` guard unchanged; no provider service (`google_service.py`/`anthropic_service.py`/etc.) modified (G-5 / no cross-provider regression).
  </acceptance_criteria>
  <done>Gemini-safe schema dual-wired (registry + get_tools), handler returns both directions with inverse labels + leak-safe masking + calm errors; threads.py / dispatch guard / provider services untouched.</done>
</task>

<task type="auto">
  <name>Task 2: LIVE non-vacuous two-user leak proof + handler integration test on :54322</name>
  <files>backend/tests/integration/test_116_tool_leak.py, backend/tests/integration/test_116_tool_read.py</files>
  <read_first>
    - backend/tests/integration/test_115_tool_global_leak.py (the leak harness to clone VERBATIM — _pg_reachable guard, two-user seed, handler-driven assertions, imports in test bodies)
    - backend/app/services/tool_dispatcher.py (the _handle_get_related_documents shipped in Task 1)
    - .planning/phases/116-document-relationships-backend-agent-tool/116-VALIDATION.md (the two-user leak-proof requirement: seed a target visible to A, link it, make it unseeable to B, assert B sees the mask)
    - supabase/migrations/071_dm_foundations.sql:135-150 (RLS user-scoped — the edges are the caller's; masking is on TARGET DOC access)
  </read_first>
  <action>
    Finalize `test_116_tool_leak.py` (clone `test_115_tool_global_leak.py`) as the NON-vacuous two-user proof that drives the HANDLER (not the RLS label — the "static would false-green" lesson, Pitfall 2). Seed: a target doc visible to user A (in a global folder), user A links the subject → target; then make the target UNSEEABLE to user B (move it out of the global folder, or seed B with no access). Assert: (a) user A sees the target's REAL filename in the related rows; (b) user B sees `"linked document (no access)"` and NEVER the target's real filename/metadata for that edge; (c) the assertion is non-vacuous — guard against both users owning everything (the masking must actually trigger for B). Un-xfail `test_116_tool_read.py` (handler returns both directions + source_refs against live data). Both gate on `_pg_reachable` and skip cleanly when `:54322` is unreachable. Make NO source changes — this task is test-only (the handler shipped in Task 1).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_116_tool_leak.py tests/integration/test_116_tool_read.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_116_tool_leak.py` passes LIVE on `:54322` (or skips cleanly when unreachable) and is NON-vacuous: it asserts B sees `"linked document (no access)"` AND that A sees the real filename for the same edge (so the mask demonstrably triggers for B but not A).
    - `test_116_tool_read.py` passes: the handler returns both outgoing and incoming rows with `source_refs` for the seeable endpoints.
    - No source files changed in this task (`git diff --stat` shows only test files).
    - Net-new failures in the wider integration suite = 0 (base-checkout proof against the wave base).
  </acceptance_criteria>
  <done>The live two-user leak proof drives the handler, is non-vacuous (B sees the mask, A sees the real filename), and is green on :54322; handler read integration green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deep-mode model → tool args (document_id / filename) | Untrusted model-emitted strings cross here |
| handler → service-role supabase (edge queries + per-endpoint readability) | RLS bypassed; the handler's per-viewer readability re-check is the leak gate |
| handler → agent loop | a raised exception would leak a stack trace to the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03-01 | Information Disclosure | cross-viewer target leak (a related-doc title/metadata of a doc the caller can't see) | mitigate | per-viewer readability re-check on EACH endpoint at read; mask as `"linked document (no access)"` (D-116-9); **verified LIVE two-user, non-vacuous** (Task 2) — the RLS label is NOT the proof |
| T-116-03-02 | Information Disclosure | Gemini 400 / silent constraint loss from a bad arg schema | mitigate | two flat scalar-string fields, NO anyOf/oneOf, NO multi-type `type` arrays; either/or in prose, validated in the handler (the a5b0b917 lesson; `test_116_tool_schema.py` asserts it) |
| T-116-03-03 | Denial of Service / Error Handling | a handler raise leaking a stack trace into the agent loop | mitigate | calm-string contract — every failure path (unresolvable subject, DB error, bad arg) returns a calm `ToolResult` JSON string, never a raise (the 115 WR-01/WR-03 lesson) |
| T-116-03-04 | Elevation of Privilege | a hallucinated/whitelisted-out tool name dispatched in a workflow phase | mitigate | inherits the existing `dispatch_tool` `phase_whitelist`-None guard (byte-identical Deep, SC#2 free) — unchanged, `test_116_whitelist_guard.py` proves it |
| T-116-03-05 | Tampering | `user_id` interpolation in the edge queries | mitigate | own-scoped `.eq("user_id", ...)` with the `_uid` guard (Plan 01) on any `.or_()`/`.eq()` runtime UUID |
| T-116-03-SC | Tampering | npm/pip installs | accept | No package installs (zero net-new deps — RESEARCH §"Package Legitimacy Audit": N/A) |
</threat_model>

<verification>
- Schema is Gemini-safe (no anyOf/oneOf/multi-type arrays); dual-wired (registry + get_tools).
- Handler returns both directions with inverse labels; masks unseeable targets; never raises.
- LIVE two-user leak proof is non-vacuous and green.
- threads.py / dispatch guard / provider services untouched (G-5, no cross-provider regression).
</verification>

<success_criteria>
- `get_related_documents` dual-wired and Deep-visible.
- Schema has two flat scalar-string fields, no anyOf/oneOf/multi-type arrays.
- Handler: both directions + inverse labels + leak-safe masking + calm errors.
- Live two-user leak proof green and non-vacuous.
</success_criteria>

<output>
Create `.planning/phases/116-document-relationships-backend-agent-tool/116-03-SUMMARY.md` when done.
</output>
