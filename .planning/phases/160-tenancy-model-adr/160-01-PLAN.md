---
phase: 160-tenancy-model-adr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/160-tenancy-model-adr/160-ADR.md
  - .planning/prd-reset/DECISIONS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [ADR-01]

must_haves:
  truths:
    - "ADR-01 is satisfied: a written Tenancy-Model ADR at 160-ADR.md ratifies D-PRD-02 (co-tenant org_id + membership RLS default; isolation-via-deployment for enterprise) and reads as settled, not re-opened (SC#1)"
    - "The ADR locks the binding naming/renumbering decisions for phases 161-168: is_system_global reuse, is_global -> is_org_shared value-preserving RENAME, migrations continuing at slot 104+ (SC#2, D-02)"
    - "The ADR records the ratify-not-relitigate scope: v3.3's shipped org_id stubs + Solo/Team/Enterprise deploy presets already ~80% pre-decided the posture, and no code changes ship in this phase (SC#3, D-04)"
    - "The ADR ratifies the 4-tier deployment-flexibility contract as binding and non-negotiable with a per-phase enforcement clause and the SEED-120 forward-compat substrate (SC#4, D-01)"
    - "A short D-v3.4-01 pointer entry in DECISIONS.md references 160-ADR.md and the already-accepted D-PRD-02, and a matching pointer row exists in PROJECT.md's Key Decisions table (D-02 register home)"
    - "The ADR carries a short Consequences + reversal section recording the one-way-door cost (co-tenant -> isolated = redeploy on customer-owned Supabase, a deploy-time switch not a code fork) and the named escape valve (a paying customer forcing isolation) (D-03)"
  artifacts:
    - path: ".planning/phases/160-tenancy-model-adr/160-ADR.md"
      provides: "The Tenancy-Model ADR ratifying D-PRD-02 + the 4-tier deployment-flexibility contract; the stable canonical path phases 161-168 cite"
      contains: "is_system_global"
      min_lines: 60
    - path: ".planning/prd-reset/DECISIONS.md"
      provides: "The D-v3.4-01 pointer entry (first per-milestone ADR in this register)"
      contains: "## D-v3.4-01"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table row pointing to the ADR (belt-and-suspenders per the file's footer convention)"
      contains: "D-v3.4-01"
  key_links:
    - from: ".planning/prd-reset/DECISIONS.md"
      to: ".planning/phases/160-tenancy-model-adr/160-ADR.md"
      via: "D-v3.4-01 pointer reference"
      pattern: "160-ADR\\.md"
    - from: ".planning/phases/160-tenancy-model-adr/160-ADR.md"
      to: "D-PRD-02"
      via: "ratification citation"
      pattern: "D-PRD-02"
---

<objective>
Write the Tenancy-Model ADR (ADR-01) that ratifies the v3.4 tenancy posture, locks the binding naming/renumbering decisions, and ratifies the 4-tier deployment-flexibility contract — then record it in the two decision registers so the milestone's biggest posture call is complete and non-relitigated.

Purpose: Give every downstream v3.4 phase (161-168, and STRETCH 169-173) a settled foundation to build on. This is a ratify-not-relitigate phase: D-PRD-02 was ACCEPTED 2026-05-10 and confirmed unchanged (D-04); ~80% of the posture already shipped in v3.3. The one genuinely-new, binding commitment is SC#4's 4-tier deployment-flexibility contract with per-phase enforcement.

Output: `.planning/phases/160-tenancy-model-adr/160-ADR.md` (the deliverable) + a `D-v3.4-01` pointer entry appended to `.planning/prd-reset/DECISIONS.md` + a one-line pointer row in `.planning/PROJECT.md`'s Key Decisions table.

Scope guard: This phase writes documents only. No code, no schema, no migration, no test, no threat model (no code -> no attack surface this phase). Do NOT invent any of those tasks.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-tenancy-model-adr/160-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/prd-reset/DECISIONS.md
@.planning/seeds/SEED-120-per-org-provider-config-byo-keys.md

Source-of-truth anchors (the executor reads the full files above; these line pointers cut the scan):
- ROADMAP.md lines 82-91 — Phase 160 detail with SC#1-4 VERBATIM. The ADR must satisfy each of the four criteria exactly as written there.
- DECISIONS.md lines 101-183 — D-PRD-02 (the decision being ratified). Lines 27-30 — the ADR-ID prefix convention (`D-PRD-NN` = business-shape; `D-vX.Y-NN` = per-milestone). Lines 1400-1408 — the Cross-references footer (the convention nuance for Task 2).
- REQUIREMENTS.md line 16 — ADR-01 text; line 115 — its traceability row.
- Version-slot fact (do NOT re-derive): D-PRD-02's Consequences still name the ORIGINAL v3.1/v3.2/v3.3 implementation slots (DECISIONS.md lines 148/154/160/162), written before the 2026-06-15 SEQUENCE.md re-sequencing moved multi-tenancy to v3.4. The posture is unchanged; only the version slot shifted. The ADR notes this so a reader is not confused by the older slots.
- Reported-bugs cross-check (MANDATORY touchpoint): 160-CONTEXT.md confirms NO open `surface: Agentic-RAG` bug folds into Phase 160 (all open reports are chat/streaming/provider-surface, kept OUT of v3.4). Nothing to cover here.

No `<interfaces>` block: this phase is self-contained document authoring with no code interfaces.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Tenancy-Model ADR (160-ADR.md)</name>
  <files>.planning/phases/160-tenancy-model-adr/160-ADR.md</files>
  <read_first>
    - .planning/phases/160-tenancy-model-adr/160-CONTEXT.md — the LOCKED framing decisions D-01..D-04 (honor exactly): D-01 = the 4-tier hard contract + per-phase enforcement + SEED-120 forward-compat; D-02 = naming/renumbering locks; D-03 = Consequences + reversal; D-04 = clean ratify, no changes.
    - .planning/ROADMAP.md lines 82-91 — Phase 160 SC#1-4 VERBATIM. Every criterion must be observably satisfied by the ADR.
    - .planning/prd-reset/DECISIONS.md lines 101-183 — D-PRD-02 (the posture being ratified) + lines 27-30 (ADR-ID convention). NOTE the version-slot staleness: D-PRD-02 Consequences reference v3.1/v3.2/v3.3 (pre-2026-06-15 re-sequencing); posture unchanged, slot shifted to v3.4.
    - .planning/REQUIREMENTS.md line 16 — the ADR-01 requirement text.
    - .planning/seeds/SEED-120-per-org-provider-config-byo-keys.md — the three forward-compat capabilities (per-org provider config / BYO keys / per-org incl. local model selection), the "no schema rewrite in v3.5" bar, and the SEC-01 `enc:v1:` MultiFernet + DB>env `_val` precedence reuse.
    - .planning/phases/160-tenancy-model-adr/160-ADR.md — the file being created (does not exist yet).
  </read_first>
  <action>
Create `160-ADR.md` as a structured Architecture Decision Record. Frame it throughout as ratify-not-relitigate (settled, not a fresh debate). Include these sections with the exact load-bearing tokens named below:

1. Header — title "Tenancy-Model ADR", status "Ratified" (or "Accepted"), date 2026-07-18, requirement ADR-01, phase 160, and the register id D-v3.4-01 (the pointer Task 2 appends).

2. "## Context" — state that D-PRD-02 was ACCEPTED 2026-05-10 and is confirmed unchanged (D-04); note the version-slot shift (originally slotted v3.2, re-sequenced to v3.4 on 2026-06-15 per SEQUENCE.md) and that the posture is unchanged even though the slot moved — so a reader is not thrown by the older v3.1/v3.2/v3.3 references still in D-PRD-02's Consequences.

3. "## Decision — Ratified Tenancy Posture" (SC#1) — ratify D-PRD-02: co-tenant `org_id` + membership RLS by default; isolation-via-deployment (customer-owned Supabase) for enterprise; one codebase, the co-tenant-vs-isolated difference is configuration not a fork. Must contain the tokens `D-PRD-02`, `co-tenant`, `isolation-via-deployment`.

4. "## Binding Naming & Renumbering Locks (phases 161-168)" (SC#2, D-02) — lock, as binding for phases 161-168: `skills.is_system` -> `is_system_global` reuse; `is_global` -> `is_org_shared` value-preserving RENAME (never drop+add); v3.4 migrations continue at slot 104+. Must contain the tokens `is_system_global`, `is_org_shared`, `is_global`, `104`, and reference the `161-168` phase span.

5. "## Ratify-Not-Relitigate Scope" (SC#3, D-04) — record that v3.3's shipped `org_id` stubs (migrations 095/096 on the 4 owned roots + org-agnostic `operator_users`) + the Solo/Team/Enterprise deploy presets (Phase 157) already ~80% pre-decided this posture; state plainly that this phase makes no code changes. Must contain `80%`, `org_id`, and `no code`.

6. "## 4-Tier Deployment-Flexibility Contract (BINDING · NON-NEGOTIABLE)" (SC#4 / D-01) — the one genuinely-new binding commitment. Enumerate the FOUR named tiers verbatim: `solo-local`, `small-team-VPS`, `medium-SaaS` (co-tenant), and `enterprise` / on-prem / BYO (isolation-via-deployment, customer-owned Supabase). State that all four remain a `pure env-var` switch with `no hardcoded` URLs/keys/models/ports; the local setup never breaks; the co-tenant-vs-isolated choice is a deploy-time decision, never a code fork. Then the forward-compat substrate half: the org-settings + encrypted-secrets substrate stays forward-compatible with the three SEED-120 capabilities (per-org provider config, BYO keys, per-org incl. local model selection), reusing the v3.3 SEC-01 `enc:v1:` MultiFernet envelope + the DB>env `_val` precedence chain, so v3.5 adds them with `no schema rewrite`. Include the per-phase enforcement clause: every subsequent v3.4 phase (161-173) is verified against this contract; nothing a phase ships may make any deployment tier harder. Reference the D-14 red line (Deep Mode byte-identical, provider differences at the gateway/adapter boundary, no new runtime — the contract must not imply any shared-path fork). Must contain `solo-local`, `small-team-VPS`, `medium-SaaS`, `SEED-120`, `enc:v1:`, `no schema rewrite`, `customer-owned Supabase`.

7. "## Consequences + Reversal" (D-03) — keep to a short paragraph, not a re-argument: the one-way-door cost is that co-tenant -> isolated is a redeploy on a customer-owned Supabase (a deploy-time switch, not a code fork); the expensive/unlikely direction is isolated -> co-tenant. Name the escape valve: a `paying customer` forcing isolation / integrations-first re-orders priorities. Must contain `customer-owned Supabase` and `paying customer`.

8. "## Success Criteria Coverage" — an explicit mapping so verification is mechanical: SC#1 -> Decision section; SC#2 -> Naming/Renumbering Locks section; SC#3 -> Ratify-Not-Relitigate Scope section; SC#4 -> 4-Tier Deployment-Flexibility Contract section. Must contain the literal tokens `SC#1`, `SC#2`, `SC#3`, `SC#4`.

9. "## References" — D-PRD-02, the D-14 red line, ADR-01, SEED-120, and a note that the D-v3.4-01 pointer entry lives in DECISIONS.md + PROJECT.md (added by Task 2).

Prose, headings, and exact wording beyond the mandated tokens are your discretion (per CONTEXT "Claude's Discretion"), as long as SC#1-4 are each observably satisfied and D-01..D-04 are honored. Do NOT reduce any commitment to a "v1" / "for now" / "future" simplification — the contract is binding as written.
  </action>
  <acceptance_criteria>
    - `.planning/phases/160-tenancy-model-adr/160-ADR.md` exists and is >= 60 lines.
    - (SC#1) It contains `D-PRD-02`, `co-tenant`, and `isolation-via-deployment`.
    - (SC#2) It contains `is_system_global`, `is_org_shared`, `is_global`, and `104`, and references the `161-168` phase span as binding.
    - (SC#3) It contains `80%`, `org_id`, and `no code`.
    - (SC#4) It contains the four tier tokens `solo-local`, `small-team-VPS`, `medium-SaaS`, `enterprise`; the contract tokens `pure env-var` and `no hardcoded`; the forward-compat tokens `SEED-120`, `enc:v1:`, and `no schema rewrite`; and the per-phase enforcement wording (`161-173` verified against the contract).
    - (D-03) It contains a "Consequences" + reversal section with `customer-owned Supabase` and `paying customer`.
    - It contains a "Success Criteria" coverage section with all of `SC#1`, `SC#2`, `SC#3`, `SC#4`.
  </acceptance_criteria>
  <verify>
    <automated>f=.planning/phases/160-tenancy-model-adr/160-ADR.md; test -f "$f" && for t in "D-PRD-02" "co-tenant" "isolation-via-deployment" "is_system_global" "is_org_shared" "is_global" "104" "80%" "no code" "solo-local" "small-team-VPS" "medium-SaaS" "pure env-var" "no hardcoded" "SEED-120" "enc:v1:" "no schema rewrite" "customer-owned Supabase" "paying customer" "SC#1" "SC#2" "SC#3" "SC#4"; do grep -qF -- "$t" "$f" || { echo "MISSING: $t"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>160-ADR.md exists ratifying D-PRD-02, locking the naming/renumbering decisions, recording the ~80% ratify-not-relitigate scope, ratifying the 4-tier deployment-flexibility contract with per-phase enforcement + SEED-120 forward-compat, carrying a Consequences+reversal paragraph, and mapping SC#1-4 in a coverage section. The automated token gate prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Record the ADR in both decision registers (D-v3.4-01)</name>
  <files>.planning/prd-reset/DECISIONS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/prd-reset/DECISIONS.md lines 27-30 — the ADR-ID convention (`D-vX.Y-NN` = per-milestone architectural decision) -> this new entry is `D-v3.4-01`. Lines 1400-1408 — the "## Cross-references" footer. CONVENTION NUANCE: the footer states per-milestone `D-vN.N-NN` ADRs live in PROJECT.md while "this file is for cross-milestone D-PRD-NN ADRs", but LOCKED decision D-02 explicitly puts the D-v3.4-01 pointer HERE. Resolution: honor D-02 — append it to DECISIONS.md (this is the FIRST per-milestone entry in this file) AND also add a PROJECT.md pointer row so the footer convention is satisfied too (belt-and-suspenders).
    - .planning/PROJECT.md line 462+ — the "## Key Decisions" table (rows shaped `| **D-vX.Y-NN**: <title> | <rationale> | <status> |`). Add one matching row.
    - .planning/phases/160-tenancy-model-adr/160-ADR.md — the file Task 1 created; the pointer must cite its exact path.
    - .planning/phases/160-tenancy-model-adr/160-CONTEXT.md — D-02 (the register-home decision) for the exact intent of the pointer.
  </read_first>
  <action>
Append a NEW top-level section `## D-v3.4-01` to `.planning/prd-reset/DECISIONS.md`, inserted immediately BEFORE the existing `## Cross-references` footer (so that footer stays the last heading). Keep it SHORT (a pointer, not a duplicate of the ADR): a title line, Status (Ratified 2026-07-18), and 2-4 sentences stating that the standalone Tenancy-Model ADR at `.planning/phases/160-tenancy-model-adr/160-ADR.md` ratifies the already-accepted `D-PRD-02` (co-tenant + isolation-via-deployment) and locks the binding 4-tier deployment-flexibility contract + the `is_system_global` / `is_org_shared` / slot-104+ naming/renumbering decisions for phases 161-168. The entry MUST contain both the token `160-ADR.md` and the token `D-PRD-02`.

Then add ONE row to the "## Key Decisions" table in `.planning/PROJECT.md` following the existing `| **D-vX.Y-NN**: ... | ... | ... |` row shape: id `D-v3.4-01`, a one-line title (Tenancy-Model ADR ratifies D-PRD-02 co-tenant + isolation-via-deployment posture + the 4-tier deploy-flexibility contract), a rationale pointing to the full ADR at `.planning/phases/160-tenancy-model-adr/160-ADR.md`, and a status marking it ratified in Phase 160. The row MUST contain the token `D-v3.4-01`.

Do NOT duplicate the full ADR content into either register — both are pointers. Do NOT modify D-PRD-02 itself (it is ACCEPTED and non-relitigated).
  </action>
  <acceptance_criteria>
    - `.planning/prd-reset/DECISIONS.md` contains a new top-level heading `## D-v3.4-01`.
    - Within that section (the lines following the `## D-v3.4-01` heading), the text references both `160-ADR.md` and `D-PRD-02`.
    - The `## Cross-references` footer is still present and remains the final top-level heading in DECISIONS.md.
    - `.planning/PROJECT.md`'s Key Decisions table contains a row bearing the token `D-v3.4-01` that points to `160-ADR.md`.
  </acceptance_criteria>
  <verify>
    <automated>d=.planning/prd-reset/DECISIONS.md; sec=$(grep -A 40 "^## D-v3.4-01" "$d"); echo "$sec" | grep -qF "160-ADR.md" && echo "$sec" | grep -qF "D-PRD-02" && grep -qF "## Cross-references" "$d" && grep -qF "D-v3.4-01" .planning/PROJECT.md && echo OK</automated>
  </verify>
  <done>DECISIONS.md carries a short `## D-v3.4-01` pointer entry (before the Cross-references footer) referencing 160-ADR.md and D-PRD-02, and PROJECT.md's Key Decisions table has a matching D-v3.4-01 row. Both registers point to the ADR without duplicating it; D-PRD-02 is untouched. The automated gate prints OK.</done>
</task>

</tasks>

<verification>
Overall phase checks (all mechanical — this is a documentation-only phase):

1. The deliverable exists: `.planning/phases/160-tenancy-model-adr/160-ADR.md` is present and passes the Task 1 token gate (all SC#1-4 tokens + Consequences/reversal + Success Criteria coverage present).
2. Both registers point to it: `## D-v3.4-01` in DECISIONS.md references 160-ADR.md + D-PRD-02; a D-v3.4-01 row exists in PROJECT.md.
3. Scope integrity: `git status` shows ONLY the three doc files changed (160-ADR.md new; DECISIONS.md + PROJECT.md edited). No code/schema/migration/test file touched. D-PRD-02's own section is unmodified (`git diff` on DECISIONS.md shows only the appended D-v3.4-01 section + no edits inside lines 101-183).
4. No threat model applies (no code -> no attack surface); this omission is intentional per the phase's documentation-only scope, not a gap.
</verification>

<success_criteria>
- SC#1: 160-ADR.md ratifies D-PRD-02 (co-tenant `org_id` + membership RLS default; isolation-via-deployment for enterprise) and reads as settled, not re-opened.
- SC#2: 160-ADR.md locks `is_system_global` reuse, the `is_global` -> `is_org_shared` value-preserving RENAME, and the slot-104+ migration renumbering as binding for phases 161-168.
- SC#3: 160-ADR.md records the ~80% pre-decided ratify-not-relitigate scope (v3.3 org_id stubs + Solo/Team/Enterprise presets) and that no code changes ship this phase.
- SC#4: 160-ADR.md ratifies the binding, non-negotiable 4-tier deployment-flexibility contract (solo-local / small-team-VPS / medium-SaaS / enterprise-BYO, pure env-var switch, no hardcoded URLs/keys/models/ports, local never breaks, deploy-time-not-code-fork) with the per-phase enforcement clause and the SEED-120 forward-compat substrate (per-org provider config / BYO keys / per-org incl. local model selection; no schema rewrite in v3.5).
- Register home (D-02): a short D-v3.4-01 pointer entry exists in DECISIONS.md (referencing 160-ADR.md + D-PRD-02) and a matching row in PROJECT.md's Key Decisions table.
</success_criteria>

<output>
Create `.planning/phases/160-tenancy-model-adr/160-01-SUMMARY.md` when done.
</output>
