---
phase: 241-recall-at-corpus-scale
plan: 04
type: execute
wave: 2
depends_on: [241-01, 241-02, 241-03]
files_modified:
  - supabase/full-schema.sql
  - .planning/phases/241-recall-at-corpus-scale/241-VALIDATION.md
  - .planning/phases/241-recall-at-corpus-scale/reports/
  - .planning/seeds/SEED-265-narrow-a-search-by-connection-or-by-saved-view.md
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [QUEUE-06]

user_setup:
  - service: supabase-local
    why: "Migration 176 must be applied by hand — CLAUDE.md forbids db push / db reset because they destroy dev data"
    dashboard_config:
      - task: "Paste supabase/migrations/176_app_settings_hnsw_knobs.sql into the SQL editor and run it"
        location: "Local Supabase Studio → SQL editor (http://127.0.0.1:54323)"
  - service: supabase-cloud
    why: "SC#3 requires a reproducible number on cloud, and D-14 gates iterative_scan on VERIFIED live pgvector parity"
    dashboard_config:
      - task: "Run the parity query and report pgvector + PostgreSQL versions verbatim"
        location: "Cloud Supabase project → SQL editor"
      - task: "Supply a read-capable cloud connection string, or decline and have the row recorded ⛔ blocked with its reason"
        location: "Cloud Supabase project → Settings → Database → Connection string"

must_haves:
  truths:
    - "D-12: migration 176 is applied by pasting it into the Supabase SQL editor — never `db push`, never `db reset` — and `supabase/full-schema.sql` is regenerated with `scripts/regenerate-full-schema.sh` (no `--reset`) BEFORE the bench that reads it is built."
    - "D-14: cloud pgvector parity is VERIFIED LIVE before `hnsw.iterative_scan` is claimed as the remedy anywhere; if cloud is below 0.8 the verdict says `ef_search` alone carries production."
    - "D-15: cloud is measured AS-IS at whatever scale it holds, and the knobs are proven to change cloud behaviour — no bench corpus is ever built in cloud Supabase."
    - "D-08: VALIDATION.md SAYS we measured selectivity-driven collapse at a feasible size — it never claims a 10M-chunk install was reproduced on a laptop."
    - "D-13: SC#2 is scored HONESTLY-PARTIAL with the reason — three axes measured (`p_folder_ids`, arbitrary `metadata @>`, `source.system` by nested jsonb containment), two named as not existing, and NEITHER is built. A filter exercised only by the harness is presence-without-behaviour."
    - "D-17: the verdict is a SELF-verification and says so in its own first paragraph — Claude built and Claude verified, so AGENTS.md §6.3's independent reviewer does not exist for this phase."
    - "D-11: the ledger re-derivation happens LAST, and `retrieval_service.py`'s disposition still reads *extraction still OWED*."
  artifacts:
    - path: ".planning/phases/241-recall-at-corpus-scale/241-VALIDATION.md"
      provides: "The verdict — before/after, local and cloud, per-axis, with the honest framing and the SC scoring"
      contains: "SELF-verification"
    - path: ".planning/seeds/SEED-265-narrow-a-search-by-connection-or-by-saved-view.md"
      provides: "SC#2's unbuilt half carried forward with a concrete trigger"
  key_links:
    - from: "241-VALIDATION.md"
      to: ".planning/phases/241-recall-at-corpus-scale/reports/*.json"
      via: "every quoted number cites the report file it came from"
      pattern: "reports/"
    - from: "supabase/full-schema.sql"
      to: "supabase/migrations/176_app_settings_hnsw_knobs.sql"
      via: "scripts/regenerate-full-schema.sh dumping the live schema after the paste"
      pattern: "hnsw_ef_search"
---

<objective>
Take the verdict: recall@k and under-fill, before and after, local and cloud — and write down what
was actually measured, including the parts that were not.

Purpose: SC#3 asks for a number anyone can re-run *"so a later regression is detectable rather than
anecdotal"*. SC#1 asks for the before/after on record. SC#2 asks whether a narrowed search returns
what an unnarrowed one would have found within that scope. All three become true here or they do
not become true at all — 241-01 built the instrument, 241-02 built the subject, 241-03 built the
remedy, and none of them measured anything.

Output: migration 176 applied and `full-schema.sql` regenerated; the bench built at full scale from
that artifact; before/after runs at three tenant-selectivity points; a cloud parity verdict and a
cloud AS-IS number; `241-VALIDATION.md`; `SEED-265`; and the ledger re-derived last.

⚠ **The UAT scoreboard's 4-axis cross-provider recipe does NOT fire here.** This phase touches
neither streaming, the agent loop, provider routing nor UI state, and exactly one embedding model
is in play (`text-embedding-3-small`). Do not manufacture an 8-row provider roster for a retrieval
measurement.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/241-recall-at-corpus-scale/241-CONTEXT.md
@.planning/phases/241-recall-at-corpus-scale/241-01-SUMMARY.md
@.planning/phases/241-recall-at-corpus-scale/241-02-SUMMARY.md
@.planning/phases/241-recall-at-corpus-scale/241-03-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Measured 2026-09-10. -->

Local server: pgvector **0.8.0**, PostgreSQL **17.6**, `hnsw.ef_search = 40`,
`hnsw.iterative_scan = off`. `hybrid_candidate_count = 20` (`config.py:930`).
Local DSN shape: `postgresql://postgres:postgres@127.0.0.1:54322/postgres`.

⛔ **Cloud pgvector is UNVERIFIED.** `hnsw.iterative_scan` does not exist below pgvector 0.8.

The parity query, to be pasted into the CLOUD SQL editor verbatim:
  SELECT extname, extversion FROM pg_extension WHERE extname = 'vector';
  SELECT version();
  SELECT current_setting('hnsw.ef_search', true) AS ef_search,
         current_setting('hnsw.iterative_scan', true) AS iterative_scan;
⚠ On pgvector < 0.8 the third statement returns NULL for `iterative_scan` rather than erroring
(`current_setting(..., true)` is the missing_ok form) — a NULL there IS the parity answer.

`scripts/regenerate-full-schema.sh` — dumps the LIVE local DB schema with **no reset** by default.
`--reset` is destructive (wipes the local DB) and is NOT used here.

`.planning/reported-bugs/TEMPLATE.md` and `.planning/seeds/` — the seed frontmatter shape.
The next free seed number is **265** (`.planning/seeds/` ends at `SEED-264`).

CLAUDE.md's hot-file ledger recipe (run it, never copy a figure):
  git log --oneline -- <file> | wc -l
  git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' \
    | grep -E '^[0-9]+(\.[0-9]+)?$' | sort -u | wc -l
  wc -l <file>
⚠ Six-digit buckets are DATED QUICK TASKS, not phases — subtract them.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator — apply migration 176 locally, and report cloud pgvector parity</name>
  <files>(operator action — no file is written by this task)</files>
  <what-built>
    241-03 authored `supabase/migrations/176_app_settings_hnsw_knobs.sql` and deliberately did NOT
    apply it — CLAUDE.md forbids `supabase db push` and `db reset` because they destroy dev data, so a
    migration reaches the local database only by an operator pasting it. Everything else in this phase
    is already built: the honest harness, the bench builder, and the two settings end to end.

    Nothing has been measured yet, and the settings are fail-soft — until the paste, `_val` reads the
    `config.py` default and the product behaves exactly as it does today.
  </what-built>
  <how-to-verify>
    1. **Apply migration 176 locally.** Open local Supabase Studio (http://127.0.0.1:54323) → SQL
       editor. Paste the entire contents of `supabase/migrations/176_app_settings_hnsw_knobs.sql` and
       run it. ⛔ Do NOT run `supabase db push` or `supabase db reset`.
       Then confirm with:
       `SELECT column_name FROM information_schema.columns WHERE table_name = 'app_settings' AND column_name LIKE 'hnsw%';`
       Expect two rows: `hnsw_ef_search`, `hnsw_iterative_scan`.

    2. **Report cloud pgvector parity.** Open the CLOUD Supabase project → SQL editor and run the
       three statements in this plan's `<interfaces>` block. Paste the output back **verbatim** — the
       `extversion`, the `version()` string, and both `current_setting` values.
       ⚠ This gates D-14: `hnsw.iterative_scan` may not be claimed as the SEED-076 remedy on cloud
       until parity is verified live. A NULL `iterative_scan` means cloud is below pgvector 0.8 and
       `ef_search` alone carries production — that is a legitimate answer, not a failure.

    3. **Supply a read-capable cloud connection string**, or decline. Declining is fine and is
       recorded as ⛔ blocked with the reason — never silently dropped from the table.
       ⚠ Paste it where it will not be committed. The harness takes it on `--dsn` and prints only host
       and database; it must not appear in any report, SUMMARY or VALIDATION row.

    4. **Local infrastructure.** If the local stack is not up, run
       `powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1` first — it verifies
       54322 accepts a real TCP connection, which is the property the next task needs.

    ⚠ **The full bench build in Task 2 writes several gigabytes to the local Docker volume** — the
    20k smoke build in 241-02 measured the real per-chunk cost, and its extrapolation is in
    `241-02-SUMMARY.md`. Confirm you have that headroom, or say what `--chunks` to use instead.
  </how-to-verify>
  <resume-signal>Reply with the two column names, the verbatim cloud parity output, and either the cloud DSN or "decline". Then type "approved".</resume-signal>
  <action>
    ⛔ **This task performs NO automated work — it is a blocking operator gate, and it is here rather
    than earlier because D-17 batches operator-owed items to the end.** Present the three items in
    `<how-to-verify>` to the operator, wait for the resume signal, and record their verbatim replies
    in the SUMMARY: the two `information_schema` column names, the cloud `extversion` /
    `version()` / `current_setting` output, and whether a cloud DSN was supplied or declined.

    ⛔ Do NOT attempt `supabase db push`, `supabase db reset`, or any programmatic application of
    migration 176. CLAUDE.md forbids both because they destroy dev data, and a migration reaches the
    database only by an operator pasting it into the SQL editor.

    ⛔ Do NOT write the cloud DSN into any file. Hold it for the duration of Task 3's cloud run only,
    pass it on `--dsn`, and record host + database in the SUMMARY — never the credential.

    If the operator declines the cloud DSN, record the cloud row ⛔ **blocked** with the reason and the
    unblocking action, and carry that into `241-VALIDATION.md` in Task 3. A blocked row is never
    silently dropped from a scoreboard.
  </action>
  <verify>
    <automated>MISSING — operator gate by construction; the automated proof is Task 2's first step, which asserts `hnsw_ef_search` and `hnsw_iterative_scan` are present in the regenerated `supabase/full-schema.sql`. That assertion CANNOT pass unless this checkpoint really happened, so the gate is verified downstream rather than unverified.</automated>
  </verify>
  <acceptance_criteria>
    - `information_schema.columns` reports both `hnsw_ef_search` and `hnsw_iterative_scan` on `app_settings`.
    - The cloud `extversion` and `version()` strings are recorded verbatim in the SUMMARY.
    - The cloud DSN, if supplied, appears in NO committed file.
  </acceptance_criteria>
  <done>The local column exists, cloud's pgvector version is known rather than assumed, and the cloud row is either drivable or explicitly blocked.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the schema, build the bench at scale, and take the local verdict</name>
  <files>supabase/full-schema.sql, .planning/phases/241-recall-at-corpus-scale/reports/</files>
  <read_first>
    - `.planning/phases/241-recall-at-corpus-scale/241-01-SUMMARY.md`, `241-02-SUMMARY.md`, `241-03-SUMMARY.md` — the three instruments and their measured costs.
    - `scripts/measure-recall.py --help` and `scripts/build-recall-bench.py --help` — the flags as built, not as planned.
    - `scripts/regenerate-full-schema.sh` — confirm the no-reset default before running it.
    - `.planning/phases/241-recall-at-corpus-scale/241-CONTEXT.md` — D-05, D-08, D-12, D-13, D-14.
    - `.planning/ROADMAP.md` → Phase 241 `## How we'd know this failed` — the five conditions this task must be able to answer against.
  </read_first>
  <action>
    **Step 1 — regenerate, and prove it folded 176 in.** Run `bash scripts/regenerate-full-schema.sh`
    (no `--reset`). Then assert `supabase/full-schema.sql` now contains `hnsw_ef_search` and
    `hnsw_iterative_scan`. ⚠ This is not bookkeeping: the bench reads this artifact as its build input
    (D-12), so a stale artifact would build a corpus whose schema disagrees with the product's.
    Never hand-edit the file.

    **Step 2 — build the bench at full scale.** Run `scripts/build-recall-bench.py` against the local
    Docker Postgres at the `--chunks` the operator confirmed in Task 1 (target ~250,000, or the
    smaller figure they named), with the default seed and the three-point skew ladder. Record the
    stage timings, the achieved per-tenant shares and the measured table and index sizes.

    **Step 3 — take the BEFORE number.** Run `scripts/measure-recall.py --dsn <bench>
    --label before-<tenant-share>` for each tenant in the skew ladder, `--layer both`, `--k 20`
    (matching `hybrid_candidate_count`), with NO `--ef-search` and NO `--iterative-scan` — i.e. the
    shipped starved configuration, `ef_search = 40`, `iterative_scan = off`. Write one JSON report per
    run under `.planning/phases/241-recall-at-corpus-scale/reports/`.

    **Step 4 — take the AFTER numbers.** Re-run the same commands with `--ef-search` raised, and again
    with `--iterative-scan relaxed_order`, and again with both. That is a small matrix: three tenant
    shares × three filter shapes × the configuration ladder. Choose the `ef_search` ladder to make the
    curve legible rather than to prove a point — start at the shipped 40 and climb until either recall
    plateaus or latency becomes the binding cost, and report the latency alongside the recall so the
    trade is visible. **Report whatever the ladder shows, including a flat curve.**

    **Step 5 — take the SMALL-CORPUS control.** Run the same Layer-2 probes against the operator's
    REAL local database (7,953 chunks) with the shipped configuration. SC#1's sentence is *"the same
    questions that worked at small scale still work"* — without the small-scale number there is no
    "still". ⛔ Read-only: `measure-recall.py` issues only `SELECT` and `set_config`, and this run must
    not touch `recall_bench`.

    **Step 6 — answer the five ROADMAP failure conditions explicitly**, each with the report file that
    settles it. In particular: *"a filter silently truncates results and the only symptom is answers
    that are slightly worse"* is precisely Layer 1's `underfill` column, and it either shows the
    truncation or it does not.

    ⚠ If any run raises `RecallUnmeasurable`, record the refusal and the reason and STOP that row —
    do not substitute an estimate. *Could not measure* and *measured, and it was fine* never share an
    exit code, and they must never share a table cell either.
  </action>
  <verify>
    <automated>ls .planning/phases/241-recall-at-corpus-scale/reports/*.json &amp;&amp; grep -l "recall_at_k" .planning/phases/241-recall-at-corpus-scale/reports/*.json | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hnsw_ef_search" supabase/full-schema.sql` is at least 1, and `git diff --stat supabase/full-schema.sql` shows a regeneration, not a hand edit.
    - `scripts/regenerate-full-schema.sh` was run WITHOUT `--reset`; the SUMMARY states so and the local `documents` / `document_chunks` counts are unchanged across the whole task.
    - One JSON report exists per (tenant share × configuration) run, each carrying its label, timestamp, pgvector version, live `hnsw.*` values, corpus counts, query-vector sample size and seed.
    - The before/after tables in the SUMMARY quote `recall_at_k` AND `underfill` per filter shape (`none`, `folder`, `metadata`, `source_system`), never a single headline number.
    - The small-corpus control ran against the real local database and its Layer-2 metric is recorded beside the bench's, so "still work" has a "still".
    - Every number in the SUMMARY names the report file it came from.
    - Backend baseline gate holds: `pytest tests/unit -q --continue-on-collection-errors` from `backend/` — 0 collection errors, at most 71 failures, failing NAME SET diffed with `comm -13` against the phase base and empty. Never derive the count from `| tail`.
  </acceptance_criteria>
  <done>There is a reproducible local number, at three selectivities, before and after, with the small-corpus control beside it.</done>
</task>

<task type="auto">
  <name>Task 3: Cloud, the verdict document, the seed, and the ledger — last</name>
  <files>.planning/phases/241-recall-at-corpus-scale/241-VALIDATION.md, .planning/seeds/SEED-265-narrow-a-search-by-connection-or-by-saved-view.md, .planning/seeds/SEED-076-filtered-vector-search-recall-pgvector-index-scale.md, .planning/seeds/SEED-197-embed-texts-sends-every-chunk-in-one-request.md, docs/HOT-FILE-LEDGER.md, CLAUDE.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - Task 2's reports and SUMMARY.
    - `.planning/phases/240-*/240-VALIDATION.md` (or the most recent VALIDATION.md in `.planning/phases/`) — the house format for a scoreboard with blocked rows.
    - `.planning/seeds/SEED-076-filtered-vector-search-recall-pgvector-index-scale.md` — the seed this phase folds; its status must be answered by EDITING it, not by leaving it `planted`.
    - `.planning/seeds/SEED-197-embed-texts-sends-every-chunk-in-one-request.md` — folded by the ROADMAP; D-08 REMOVED the exposure rather than fixing it, and the seed must say so with its trigger intact.
    - Any recent seed file for the frontmatter shape.
    - `docs/HOT-FILE-LEDGER.md` → the sections for `retrieval_service.py`, `recall_eval.py`, `retrieval_tuning.py`, `config.py`, `user_settings.py`, `api/settings.py`, `SettingsPage.tsx`.
    - `CLAUDE.md` → the G-5 FIRING table and the ledger recipe.
  </read_first>
  <action>
    **Step 1 — the cloud arm.** If the operator supplied a DSN: run `scripts/measure-recall.py --dsn
    <cloud> --label cloud-as-is` at whatever scale cloud holds, then re-run with `--ef-search` raised
    to prove the knob CHANGES cloud behaviour (D-15). If Task 1's parity output showed pgvector below
    0.8, run the `iterative_scan` arm anyway and record that the knob was refused/ignored — that is the
    evidence D-14 wants, and 241-03's per-knob `try` is what makes it safe to attempt. ⛔ **No bench
    corpus is built in cloud Supabase, at any scale, for any reason.** If the operator declined the
    DSN, record the row ⛔ blocked with the reason and the unblocking action — never drop it.

    ⚠ Cloud application of migration 176 is NOT this phase's work and must not be attempted. It is a
    deploy-parity item under CLAUDE.md's standing rule that every prod push walks the DB + non-code
    checklist. Record it in VALIDATION.md as OWED at the next operator-triggered deploy.

    **Step 2 — write `241-VALIDATION.md`.** Its FIRST paragraph states it is a **SELF-verification**:
    Claude planned, built and measured this phase, so AGENTS.md §6.3's reviewer-who-did-not-shape-the-
    build does not exist for it (D-17). Then:

    · **The honest framing, before any number.** State that we measured **selectivity-driven collapse
      at a feasible size** — a tenant owning 0.2% of N chunks starves `ef_search = 40` the same way at
      N = 250k as at N = 20M — and that **no 10M-chunk install was reproduced on a laptop** (D-08).
      Give the measured per-chunk storage basis so a reader can size their own install.
    · **The before/after tables**, per tenant share × filter shape, `recall_at_k` and `underfill`,
      with the latency beside them, each citing its report file.
    · **SC#1** — the small-corpus control against the bench, scored plainly.
    · **SC#2 — scored HONESTLY-PARTIAL, with the reason.** Three axes measured: `p_folder_ids`,
      arbitrary `metadata @>`, and `source.system` via nested jsonb containment on
      `metadata->'source'`. Two axes SC#2 names — **connection-by-id** and **saved View** — DO NOT
      EXIST: `search_documents` accepts only `metadata_filter` and `folder_ids`, and the live metadata
      keys (measured: `title, summary, document_type, topics, language, author, date, source,
      email_*`) contain neither `source_connection_id` nor `source_system`. ⛔ **They are not built,
      deliberately.** Nothing today — no UI, no tool schema, no caller — would invoke them, and a
      filter exercised only by the harness is the presence-without-behaviour shape this project has
      paid for repeatedly. Say partial, say why, and point at the seed.
    · **SC#3** — the reproduction command, verbatim, for local and for cloud, plus the cloud parity
      verdict and what it means for `iterative_scan` in production.
    · **The five ROADMAP failure conditions**, each answered with its evidence.
    · **What is OWED**, stated as a decision rather than absorbed: cloud application of migration 176
      at the next deploy; any blocked row and its unblocking action; and the fact that the review is a
      self-verification.

    **Step 3 — plant `SEED-265`.** File:
    `.planning/seeds/SEED-265-narrow-a-search-by-connection-or-by-saved-view.md`, `status: planted`,
    `surface: Agentic-RAG`, `relates_to` naming `backend/app/services/retrieval_service.py`,
    `supabase/migrations/154_connection_scoped_visibility.sql` and
    `backend/app/services/tool_dispatcher.py`. Its `trigger_when` is the concrete one from
    241-CONTEXT `<deferred>`: **the first surface — a UI control, a tool-schema parameter, or a
    workflow binding — that would actually narrow a search by connection or by saved View.** Record
    what was measured (the two missing metadata keys, the two-parameter tool signature) so the next
    reader does not re-derive it.

    **Step 4 — answer the folded seeds by EDITING them.** `SEED-076`: flip status and record where
    each of its four levers went — the harness is built, `ef_search` and `iterative_scan` are shipped
    as settings, and per-tenant partial indexes / partitioning stay deferred **with the re-open
    trigger being the measured curve showing the two shipped levers insufficient at the measured
    skew** (which this phase's numbers now settle one way or the other, so write the answer, not a
    placeholder). `SEED-197`: record that D-08 REMOVED the exposure rather than fixing it — no bench
    path calls `embed_texts` — and leave its own trigger intact. ⚠ `status:` frontmatter IS the index;
    prose inside a body saying "still open" is invisible to the scan.

    **Step 5 — the ledger, LAST.** Re-derive with the CLAUDE.md recipe (run it, never copy a figure)
    the triples for every file this phase touched: `retrieval_service.py`, `recall_eval.py`,
    `retrieval_tuning.py`, `config.py`, `user_settings.py`, `api/settings.py`, `SettingsPage.tsx`.
    Update each scan-list row AND its `docs/HOT-FILE-LEDGER.md` section in the same commit, and mirror
    the FIRING rows into CLAUDE.md's table. ⭐ **This is deliberately the last step**, because the
    ledger's own `retrieval_service.py` section records the lesson: *"a cell written MID-PHASE is a
    claim about a moving file — a phase that touches a file twice will falsify its own ledger note
    unless the note is written last."*

    ⛔ **`retrieval_service.py`'s disposition must still read `extraction still OWED`.** D-11's landing
    is the SECOND, not a discharge; a THIRD must propose the extraction first. Write the phase number
    into the cell so the next reader can count.

    **Step 6 — the gates and the record.** Run the four gates and quote each verdict line verbatim:
    `node scripts/check-hot-file-ledger.cjs 241`, `node scripts/check-claude-md-size.cjs`,
    `node scripts/check-gap-closure-rounds.cjs 241`, and
    `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root. Update the
    ROADMAP's Phase 241 progress row (including its two measured-STALE Flags — F-7's SEED-224 hook was
    discharged at Phase 217.1, and the G-5 triple was `17/9/362` and is not) and hand-edit `STATE.md`.
    ⛔ Do NOT call the `state.*` SDK verbs; seven of them write false records.
  </action>
  <verify>
    <automated>node scripts/check-hot-file-ledger.cjs 241 &amp;&amp; node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `241-VALIDATION.md` exists and its first paragraph contains the words `SELF-verification`.
    - It contains the sentence that we measured selectivity-driven collapse at a feasible size and did NOT reproduce a 10M-chunk install — quote it in the SUMMARY.
    - SC#2 is scored partial with the two unbuilt axes named and the reason stated; `grep -c "connection_id\|saved View" 241-VALIDATION.md` is non-zero.
    - No plan in this phase built a connection-by-id or saved-View filter: `git log <phase-base>..HEAD -- backend/app/services/retrieval_service.py backend/app/services/tool_dispatcher.py supabase/migrations/` shows no such parameter added. State this in the SUMMARY.
    - `SEED-265` exists with `status: planted` and a `trigger_when` naming a concrete surface.
    - `SEED-076` and `SEED-197` frontmatter `status:` fields are EDITED, not left `planted` while the body claims otherwise.
    - `docs/HOT-FILE-LEDGER.md`'s `retrieval_service.py` row still contains the words `extraction still OWED`.
    - `node scripts/check-hot-file-ledger.cjs 241` exits 0; `node scripts/check-claude-md-size.cjs` exits 0; `node scripts/check-gap-closure-rounds.cjs 241` exits 0.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` verdict line quoted VERBATIM. A growing total is the gate working. If it reds, capture the failing filenames from the gate's own persisted JSON BEFORE re-running, check each against `git diff --numstat`, and do not touch the cap.
    - The cloud DSN appears in no committed file — `git grep -c "postgres://\|postgresql://" -- .planning docs` names only shapes, never a credential.
  </acceptance_criteria>
  <done>The verdict is on record with its framing, the two unbuilt axes are carried forward with a trigger, and the ledger is re-derived last with the extraction still owed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → local Supabase SQL editor | Hand-applied DDL on a database holding real dev data |
| harness → the operator's LIVE CLOUD database | A measurement runs against production data |
| cloud DSN → shell history, reports, VALIDATION.md, git | A production credential could be committed |
| `regenerate-full-schema.sh` → `supabase/full-schema.sql` | A generated deploy artifact is rewritten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-241-20 | Denial of service (data loss) | `supabase db push` / `db reset` | mitigate | Neither is ever run. The migration reaches the database ONLY through the operator's paste in Task 1, and `regenerate-full-schema.sh` is run WITHOUT `--reset` (the `--reset` path wipes the local DB and is for CI/release verification only). Acceptance asserts the local `documents` / `document_chunks` counts are unchanged across the whole plan. |
| T-241-21 | Information disclosure | The cloud DSN | mitigate | It is passed on `--dsn` only. `measure-recall.py` prints host and database and never the password (241-01 T-241-01). Acceptance greps `.planning` and `docs` for a credential shape. The operator is told in Task 1 to paste it where it will not be committed. |
| T-241-22 | Tampering | The harness writing to the LIVE cloud database | mitigate | The harness issues only `SELECT`, `set_config` and planner `SET LOCAL` — no DDL, no DML, no `CREATE`. Open the cloud connection read-only (`SET TRANSACTION READ ONLY`) so a write fails at the server rather than by convention, and state the assertion in VALIDATION.md. ⛔ No bench corpus is built in cloud Supabase at any scale. |
| T-241-23 | Denial of service | The exact arm's forced sequential scan against CLOUD | mitigate | Cloud runs Layer 2 and a bounded Layer 1 sample only, at whatever scale cloud holds. Cap `--query-vectors` for the cloud arm and say what cap was used. D-15 asks for a reproducible cloud number, not a cloud bench. |
| T-241-24 | Repudiation | A verdict nobody can re-run | mitigate | VALIDATION.md carries the verbatim reproduction command for both environments, and every quoted number cites its JSON report, which carries the seed, the sample size, the pgvector version and the live `hnsw.*` values. That is the difference SC#3 asks for between a number and an anecdote. |
| T-241-25 | Repudiation | A self-verification read as an independent review | mitigate | D-17: the FIRST paragraph of VALIDATION.md says SELF-verification, and VERIFICATION.md says the same. `/gsd:code-review` runs over the diff and its result is labelled accordingly. ⛔ No bus item is routed `--to gemini`. Two phases already owe an independent §6.3 review (238, 240) and this one joins them rather than pretending otherwise. |
| T-241-26 | Tampering | Hand-editing `supabase/full-schema.sql` | mitigate | It is GENERATED — its own header says so. It is only ever produced by `scripts/regenerate-full-schema.sh`; acceptance checks the diff is a regeneration and not a targeted edit. |
| T-241-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** ROADMAP: ⛔ no new Python package. Any step that believes it needs one STOPS and reports — no legitimacy checkpoint is authorised here. |
</threat_model>

<verification>
- Migration 176's two columns exist on `app_settings` and appear in the regenerated `full-schema.sql`.
- One JSON report per run under `.planning/phases/241-recall-at-corpus-scale/reports/`, each fully attributed.
- `241-VALIDATION.md` exists, opens with SELF-verification, carries the honest framing, scores SC#1/SC#2/SC#3 and answers the five ROADMAP failure conditions.
- `SEED-265` planted; `SEED-076` and `SEED-197` answered in their frontmatter.
- Ledger re-derived LAST; `retrieval_service.py` still reads *extraction still OWED*.
- Four gates green with verdict lines quoted verbatim; backend ceiling 71 held by NAME-SET diff.
- ROADMAP progress row and `STATE.md` hand-edited (never the `state.*` SDK verbs).
</verification>

<success_criteria>
Anyone can run one command and get a number, on local and on cloud; the before/after is on record
at three selectivities with the filter shapes broken out; and everything that was not measured —
the two filter axes that do not exist, cloud's migration parity, and the absence of an independent
reviewer — is written down as a decision rather than left as an impression.
</success_criteria>

<output>
Create `.planning/phases/241-recall-at-corpus-scale/241-04-SUMMARY.md` when done.
</output>
