---
phase: 067.3-streaming-render-and-storage-fixes-round-2
plan: 05
subsystem: closing-uat
tags: [uat, closing-gate, multi-phase, chrome-mcp, gate-not-met, escalation, gap-escalation-branch, autonomous-false]

# Dependency graph
requires:
  - phase: 067.3-streaming-render-and-storage-fixes-round-2
    provides: Plans 01-04 (N-01 router fix, R-2 sandbox download, R-1 per-thread store, R-3 observability) — Plan 05 is the closing UAT that verifies the cumulative effect against the strict 12/12 GREEN gate (D-067.3-WAVE-03)
  - phase: 067.2-streaming-render-and-storage-fixes
    provides: 067.2-HUMAN-UAT.md scoreboard with deferred Rows 5/11/12 + RED Rows 3/4/6 — Plan 05 mirrors Phase 067.3 verdicts back into 067.2-HUMAN-UAT.md per D-067.3-WAVE-03 cross-phase closure rule
provides:
  - "067.3-HUMAN-UAT.md scoreboard with 9 GREEN / 2 RED / 2 deferred verdicts captured by user (Chrome MCP-driven UAT, lived browser, real run_ids)"
  - "Cross-phase mirror appended to 067.2-HUMAN-UAT.md as `## Phase 067.3 verdict — 9/12 GREEN (gate not met)` section — preserves existing audit trail; documents that Phase 067.2 stays OPEN alongside Phase 067.3"
  - "STATE.md update: status reflects Phase 067.3 OPEN with strict UAT gate not met; new Blockers (escalated to Phase 067.4) section appended mirroring the 067.2 → 067.3 escalation format"
  - "Phase 067.4 scope recommendation: (a) PRIMARY R-3 suggestion-emit fix [gate-blocking]; (b) SECONDARY OpenRouter kimi-k2.5 + minimax-m2.7 timeout regression checks [carry-forward]"
affects:
  - 067.4 (next phase — primary scope = R-3 fix; secondary scope = carry-forward 067.2-11/12)
  - 067.2 (stays OPEN; closure deferred to 067.4 closing UAT)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cross-phase closure rule honored in gap branch: when the strict 12/12 GREEN gate is not met, the cross-phase mirror does NOT flip checkboxes; instead, append a documented gap section preserving audit trail. Both phases stay OPEN; the next phase (067.4) takes both gate-blocking + carry-forward rows."
    - "Hypothesis-disconfirmation logged inline in evidence: the `max_tokens` 200→800 hypothesis (suggestion_service.py:75/92) was tested live with retest run_id `00dcf270-3773-4ae2-8181-6ee36169dc03` — SSE replay showed no `suggestions` event again. Disconfirmed-hypothesis evidence captured in 067.3-HUMAN-UAT.md Row R-3 + STATE.md Blockers + this SUMMARY so 067.4 doesn't re-explore the same dead-end."

key-files:
  created:
    - ".planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-05-SUMMARY.md"
  modified:
    - ".planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md (Task 1 + Task 2 — scoreboard authored + verdicts captured by predecessor commits 4eba168 + ee75f13)"
    - ".planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md (Task 3 — `## Phase 067.3 verdict — 9/12 GREEN (gate not met)` section appended; +48 insertions; commit e6b2b06)"
    - ".planning/STATE.md (Task 4 — frontmatter status, Current Position, Blockers (escalated to Phase 067.4), Session Continuity all updated)"

key-decisions:
  - "Gap-escalation branch selected per Plan 05's conditional `<action>` block — `gaps` resume signal from user (not `approved`). Cross-phase mirror appends documented gap section; does NOT flip checkboxes on Phase 067.2 rows."
  - "Phase 067.4 takes BOTH the gate-blocking R-3 fix AND the secondary OpenRouter timeout carry-forward rows (067.2-11, 067.2-12) — combined into one follow-on phase rather than two, to amortize the closing-UAT cost across both unfinished surfaces."
  - "Phase 067.2 frontmatter `status:` field NOT changed (remains `blocked`) — accurately reflects that 067.2 is still blocked by Row 6 RED, even though Rows 3/4/5 are observably resolved by 067.3 in practice. Strict-gate semantics: row checkboxes do not tick GREEN until the full phase can close."
  - "Hypothesis-disconfirmation captured inline in evidence (max_tokens 200→800 retest run_id `00dcf270-3773-…` SSE still missing `suggestions` event) so Phase 067.4 doesn't re-explore the same dead-end."

patterns-established:
  - "Strict-gate gap-escalation: when 12/12 GREEN gate is not met, the cross-phase mirror documents the gap as a new section (not as flipped checkboxes); BOTH phases stay OPEN; next phase takes the union of {gate-blocking RED rows + carry-forward deferred rows}."
  - "Disconfirmed-hypothesis logging in UAT evidence: any retest that disconfirms a hypothesis is logged with the retest run_id + the SSE replay observation, so the next phase doesn't waste time re-exploring."

requirements-completed: []  # STREAM-04-correctness-round2 NOT closed — gate-blocking R-3 still RED; will close via 067.4

# Metrics
duration: ~15min (Tasks 3 + 4 — Tasks 1 + 2 were predecessor commits 4eba168 + ee75f13)
completed: 2026-05-09
---

# Phase 067.3 Plan 05: Closing UAT — Gate Not Met, Escalating to Phase 067.4 Summary

**Plan 05 closing UAT executed via Chrome MCP — 9 GREEN / 2 RED / 2 deferred against the strict 12/12 GREEN gate. Two RED rows are mirrors of the same root cause: suggestion pills are not emitted to SSE for Fahed-Mrad search-only prompts. The `max_tokens` 200→800 hypothesis was tested live and disconfirmed (retest run_id `00dcf270-3773-4ae2-8181-6ee36169dc03` SSE still missing `suggestions` event). Phase 067.3 OPEN; Phase 067.2 stays OPEN alongside (cross-phase D-067.3-WAVE-03 rule). Both phases close together via Phase 067.4.**

## Final UAT counts

| Group | Count | Rows |
|-------|-------|------|
| GREEN | 9 | R-1, R-2, R-2-IDOR, N-01, 067.2-3, 067.2-4, 067.2-5, 067.3-NR (and bundled re-ticks for the same protocols) |
| RED   | 2 | **R-3, 067.2-6** — same root cause: suggestion pills not emitted to SSE; `uniqueKinds` of SSE replay shows `[title, iteration_start, tool_preparing, tool_start, tool_end, planning, delta, sources, citations, confidence, done]` — NO `suggestions` event, NO `fallback_model` event |
| Deferred | 2 | **067.2-11, 067.2-12** — OpenRouter Kimi 2.5 + MiniMax 2.7 live timeout. `OPENROUTER_API_KEY` IS present in `backend/.env`, but synthetic-timeout protocol was not exercised during 067.3 UAT (out of plan-set scope per D-067.3-WAVE-03 multi-provider availability rule) |

**Gate met:** ❌ NO (9/12 GREEN; strict gate is 12/12)

## Cross-phase status

- **Phase 067.3 (this phase):** OPEN — 5/5 plans landed (Plans 01/02/03/04 fixed N-01/R-2/R-1; Plan 04 shipped observability-only deliverable; Plan 05 closing UAT showed R-3 still RED). Strict UAT gate not met. Escalating to Phase 067.4.
- **Phase 067.2 (parent phase):** OPEN — Rows 3/4/5 are observably resolved by Phase 067.3 in practice (cross-thread switch fixed by Plan 03; sandbox download fixed by Plan 02; re-sign architecture fixed by Plan 02). However, no checkbox flips because Row 6 (suggestions render) remains RED — same root cause as 067.3 Row R-3. Strict-gate semantics: rows tick GREEN together when the phase closes, not piecewise. 067.2 closure deferred to Phase 067.4 closing UAT.
- **067.2-HUMAN-UAT.md:** preserved verbatim as audit trail; new `## Phase 067.3 verdict — 9/12 GREEN (gate not met)` section appended after `## Final closure` documenting the cross-phase outcome.

## Hypotheses already disconfirmed (do NOT re-explore in 067.4)

- **`max_tokens` budget bump 200→800 in `backend/app/services/suggestion_service.py:75/92`** — disconfirmed live. Retest run_id `00dcf270-3773-4ae2-8181-6ee36169dc03` after the budget bump ALSO produced no `suggestions` event in SSE. Root cause is elsewhere; 067.4 should NOT spend time on token-budget tuning.

## Recommendation for Phase 067.4

**Scope:**

1. **PRIMARY (gate-blocking) — R-3 suggestion-emit fix.** Investigation start points:
   - `backend/app/services/suggestion_service.py` — Pydantic-structured output path with `gpt-5.4-mini`. Likely candidate causes:
     - **(a) Exception swallowed in broad `except Exception:`** at `backend/app/api/threads.py:2509`. The Plan 04 instrumentation `logger.warning("suggestion generation failed", exc_info=True)` is now in place — Phase 067.4 can capture uvicorn stdout during a Fahed-Mrad search-only run to disambiguate this hypothesis. If the warning fires with a stack trace → exception-swallow path; fix the exception class.
     - **(b) `gpt-5.4-mini` reasoning-token consumption** with even the 800-token budget. Consider 2000 tokens or model swap (e.g., `gpt-4.1-mini` or `gpt-4.6-mini` if reasoning-token budgets are smaller). The Plan 04 paired `logger.info "suggestions empty for run X"` would fire if `questions=[]` — captures this branch.
     - **(c) JSON output parser miscount** — `response.choices[0].message.content` could be empty due to function-call-style response wrapping that's not flowing to the structured-output path. Inspect the Pydantic schema's deserialization fallback.
   - SSE stream evidence to capture for the next investigation: replay `/runs/{id}/stream?since=0` for the run AND uvicorn stdout for the same `run_id` correlation.

2. **SECONDARY (carry-forward) — OpenRouter timeout regression checks** for Rows 067.2-11 and 067.2-12. `OPENROUTER_API_KEY` is present; just exercise the synthetic-timeout protocol:
   - Set `LLM_CALL_TIMEOUT_OVERRIDES=moonshotai/kimi-k2.5=10` (and separately `=minimax/minimax-m2.7=10`).
   - Restart uvicorn.
   - Submit long-form prompts; confirm `runs.status='timed_out'` + `runs.error='timed_out: 10s per-call deadline exceeded at iteration N (model=<id>)'`.
   - Clean cancellation format (NOT `GeneratorExit`).

**Closing UAT:**

- 067.4 closing UAT must mirror verdicts back into BOTH `067.2-HUMAN-UAT.md` AND `067.3-HUMAN-UAT.md` AND its own `067.4-HUMAN-UAT.md` — closes all three phases together when strict gate met.

## Pointers

- **Canonical verdict store** (Plan 05 Task 1 + Task 2 output): `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md` (frontmatter `status: gaps_blocking`, `gate_met: false`, `escalation: 067.4`).
- **Phase 067.2 cross-phase mirror** (Plan 05 Task 3 output): `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` § `## Phase 067.3 verdict — 9/12 GREEN (gate not met)`.
- **Plan 04 observability + INVESTIGATION-NOTES** (no-op deferred-evidence variant; routes branch selection to this UAT's uvicorn stdout capture): `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-INVESTIGATION-NOTES.md` + `067.3-04-SUMMARY.md`.
- **File under suspicion** for the R-3 gate-blocking fix: `backend/app/services/suggestion_service.py` + emit path at `backend/app/api/threads.py:2466-2526` (specifically the `except Exception:` at 2509 and the `if questions:` guard at 2476).
- **STATE.md** Blockers (escalated to Phase 067.4) section: documents the 3 carry-forward items (R-3, NR-deferred-row-067.2-11, NR-deferred-row-067.2-12) with the format mirroring the prior 067.2 → 067.3 escalation.

## Self-Check: PASSED

Files verified:
- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md` — exists (created in commits 4eba168 + ee75f13)
- `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` — `## Phase 067.3 verdict — 9/12 GREEN (gate not met)` section appended (commit e6b2b06)
- `.planning/STATE.md` — frontmatter status updated, Current Position updated, Blockers (escalated to Phase 067.4) section added, Session Continuity updated
- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-05-SUMMARY.md` — this file

Commits verified (in `git log --oneline -10`):
- 4eba168 docs(067.3-05): author 067.3-HUMAN-UAT.md scoreboard (Task 1) — Task 1 predecessor
- ee75f13 test(067.3-05): UAT verdicts captured — 9 GREEN, 2 RED (R-3 family), 2 deferred → escalate to 067.4 — Task 2 predecessor
- e6b2b06 docs(067.3-05): append Phase 067.3 gate-not-met verdict to 067.2-HUMAN-UAT.md (R-3 escalated to 067.4) — Task 3 (this session)
- (Task 4 — STATE + SUMMARY commit pending below)

Plan 05 self-check PASSED — all artifacts referenced exist; cross-phase mirror is in place; STATE.md reflects the actual phase outcome (OPEN, escalating to 067.4); SUMMARY documents the gap and the 067.4 scope recommendation with disconfirmed-hypothesis evidence preserved.
