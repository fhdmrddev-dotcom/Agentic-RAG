---
phase: 067.3-streaming-render-and-storage-fixes-round-2
plan: 04
subsystem: backend
tags: [observability, logging, suggestions, sse, redis-streams, investigate-then-fix, conditional, no-op-deferred-evidence]

# Dependency graph
requires:
  - phase: 067.3-streaming-render-and-storage-fixes-round-2
    provides: Plan 03 R-1 per-thread message store (`messagesByThread` + `setMessagesForThread` + `ThreadBoundSetMessages` factory) — Plan 04 reads against this baseline per D-067.3-R3-02
  - phase: 067.2-streaming-render-and-storage-fixes
    provides: Plan 04 INVESTIGATION-NOTES.md 5-layer render-path audit (verdict (a) no-op predicted, deferred to Plan 06 UAT) + Plan 06 UAT outcome (Row 6 RED — confidence renders, suggestions don't)
  - phase: 061-run-backed-streaming
    provides: Redis Stream `_emit` helper + run lifecycle + `runs` Postgres table — observability log lines correlate via `run_id` (UUID, no PII per threat register)
provides:
  - "Backend observability for the suggestion-generation try/except block at threads.py:2487-2526 — replaces silent `pass` (D-067.3-R3-03 Task 1a) with `logger.warning(exc_info=True)` and adds `logger.info` when `generate_suggestions` returns `[]` (D-067.3-R3-03 Task 1b)"
  - "Disambiguation primitive between Task 2a (silent backend exception) and Task 2b (empty model output) — visible at runtime in uvicorn console correlated via `run_id`"
  - "Static-evidence audit corroborating Phase 067.2 Plan 04's 5-layer render-path verification (post-Plan-03-R-1) — Tasks 2c, 2d mechanically excluded for the General-mode last-assistant Fahed-Mrad search-only scenario captured in Phase 067.2 Plan 06 UAT Row 6"
  - "INVESTIGATION-NOTES.md with `[x] No-op (deferred-evidence variant)` branch selection — observability ships unconditionally per `must_haves.truths[6]`; runtime branch selection between {2a, 2b, real-no-op} deferred to Plan 05 closing UAT log capture"
affects:
  - 067.3-05 (closing UAT — Wave 3; R-3 row exercise will capture the disambiguation log line under the post-Plan-04 backend; routes any remaining fix to a follow-up patch under R-3 carryover)
  - "v2.6+ telemetry — `'suggestions_failed'` Redis Stream event was scoped in Task 2a's recipe but NOT shipped in this plan (No-op selection); future telemetry pipelines can revisit if Plan 05 captures Task 2a evidence"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Observability-first disambiguation: when a try/except can mask multiple distinct failure modes, swap the silent `pass` for `logger.warning(exc_info=True)` AND add a paired `logger.info` on the empty-success path. The two log lines correlate via `run_id` (UUID, no PII) and disambiguate downstream investigation. ASVS L1 V8.6.4 (logging-of-failure events) compliance pattern."
    - "Static-evidence-driven No-op branch selection: when the plan offers conditional fix branches gated on runtime evidence the worktree executor cannot capture, ship the unconditional observability deliverable and select No-op with explicit rationale. Mirrors Phase 067.2 Plan 04's `verdict_locked: deferred-to-plan-06-user-uat` pattern, with the addition that this plan's `must_haves.truths[6]` made the observability code unconditional."

key-files:
  created:
    - ".planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-INVESTIGATION-NOTES.md"
  modified:
    - "backend/app/api/threads.py (suggestions emit block at lines 2487-2526; replaces silent `pass` with `logger.warning(exc_info=True)` and adds `logger.info` on empty-questions case; +21 insertions / -1 deletion)"

key-decisions:
  - "D-067.3-R3-03 Task 1a + 1b shipped (observability swap + INFO-on-empty) — unconditional per `must_haves.truths[6]`."
  - "Task 2 branch selection = [x] No-op (deferred-evidence variant) — Plan 04 closes with Task 1 alone per the plan's own `<no-op>` action. The post-Plan-03-R-1 codebase mechanically excludes Tasks 2c (frontend bucket-routing) and 2d (broken prop chain) via static audit; remaining {2a, 2b} candidates require runtime evidence the worktree cannot capture."
  - "Worktree-without-runtime precedent reused from Phase 067.2 Plan 04 (`verdict_locked: deferred-to-plan-06-user-uat`). Phase 067.3 Plan 04 differs only in shipping unconditional observability code — exactly because the plan author anticipated this and made Task 1 unconditional."

patterns-established:
  - "Observability-first disambiguation primitive — paired `logger.warning(exc_info=True)` + `logger.info` on the empty-success branch, both correlated via `run_id`."
  - "Worktree static-evidence + Plan-N+1 UAT runtime-evidence routing — when the worktree executor lacks live browser/backend access, ship the unconditional observability deliverable and route runtime branch-selection to the next live UAT (Plan 05) per the plan's anticipated `<no-op>` contract."

requirements-completed: [STREAM-04-correctness-round2]

# Metrics
duration: ~30min
completed: 2026-05-09
---

# Phase 067.3 Plan 04: R-3 Suggestion Render Investigate-Then-Fix Summary

**Backend observability landed at the suggestion-generation try/except (`threads.py:2487-2526`) — `logger.warning(exc_info=True)` replaces the silent `pass` and a paired `logger.info` fires when `generate_suggestions` returns `[]`. The two log lines disambiguate Task 2a (silent backend exception) from Task 2b (empty model output) via `run_id` correlation at the next live UAT. Branch selection: No-op (deferred-evidence variant) — static audit excludes Tasks 2c and 2d post-Plan-03-R-1; runtime evidence between {2a, 2b, real-no-op} captured at Plan 05 closing UAT.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-05-09 (worktree spawn)
- **Completed:** 2026-05-09
- **Tasks:** 1 of 2 (Task 1 shipped; Task 2 = No-op per plan's `<no-op>` action)
- **Files modified:** 2 (1 backend source + 1 new planning artifact)

## Accomplishments

- **Backend observability swap (D-067.3-R3-03 Task 1a)** — `backend/app/api/threads.py:2509-2521` now `logger.warning("suggestion generation failed for run %s — main response unaffected", run_id, exc_info=True)` instead of `pass  # SUG-04: failure never affects main response`. The SUG-04 invariant is preserved verbatim: exception still does NOT affect the main response (no re-raise; the producer continues to `stream_end`). ASVS L1 V8.6.4.
- **INFO log on empty-questions case (D-067.3-R3-03 Task 1b)** — `backend/app/api/threads.py:2499-2508` now adds an explicit `else:` branch on `if questions:` that emits `logger.info("suggestions empty for run %s — generate_suggestions returned [] (no emit; not an error)", run_id)`. Distinguishes the empty-model-output path (Task 2b candidate) from the silent-swallow path (Task 2a candidate) at runtime.
- **Static-evidence audit + branch selection** — INVESTIGATION-NOTES.md captures the layer-by-layer audit. Confirmed:
  - Phase 067.2 Plan 04 already verified all 5 frontend render layers intact (line-anchored).
  - Plan 03 R-1's per-thread store closes any cross-thread bucket-routing race; post-R-1 the `onSuggestions` callback at `useMessages.ts:325-330` writes through the `ThreadBoundSetMessages` factory parameter (declared at `useMessages.ts:54-56`) — Task 2c sub-cause `(c-i)` "misrouted bucket" mechanically excluded.
  - `ChatArea.tsx:311-317` → `MessageList.tsx:88-100` → `MessageItem.tsx:93-98` prop chain wired correctly for General-mode last-assistant scenario — Task 2d mechanically excluded.
  - Remaining runtime candidates {2a, 2b} are exactly what the new observability disambiguates.
- **Branch selection** — `[x] No-op (deferred-evidence variant)` per the plan's own `<no-op>` action: "Plan 04 closes with only Task 1 (the observability swap, which we keep as cheap insurance per Specifics §R-3)." The plan's `must_haves.truths[6]` makes Task 1 unconditional regardless of branch outcome.

## Task Commits

Each task was committed atomically:

1. **Task 1: Backend observability — logger.warning + INFO-on-empty + INVESTIGATION-NOTES.md** — `1c81b87` (feat)

_Task 2 was selected as `[x] No-op (deferred-evidence variant)` per the plan's own `<no-op>` branch — no source-code commit for Task 2._

## Files Created/Modified

- `backend/app/api/threads.py` — Replace silent `pass  # SUG-04: failure never affects main response` at the suggestions emit `try/except` with `logger.warning(exc_info=True)`; add `else:` branch on `if questions:` emitting `logger.info` on empty-output path. +21 insertions / -1 deletion. SUG-04 invariant preserved (no re-raise; producer continues to `stream_end`).
- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-INVESTIGATION-NOTES.md` — New artifact. Captures: pre-runtime `verdict: branch-selection-deferred`, locked `[x] No-op (deferred-evidence variant)`, full static-evidence audit, Plan 03 R-1 verification (post-merge code-static), Plan 05 UAT routing protocol (4 outcomes mapped to follow-up actions). Mirrors Phase 067.2 Plan 04's INVESTIGATION-NOTES.md frontmatter shape.

## Decisions Made

- **Observability lands unconditionally (Task 1a + 1b shipped, no source code from Tasks 2a-2d)** — per the plan's `must_haves.truths[6]`: "The logger.warning swap is preserved even if the root cause turns out to be frontend (cheap insurance against future silent-swallow regressions)." Task 1 is the unconditional contract; Tasks 2a-2d are runtime-gated.
- **Branch selection = No-op (deferred-evidence variant)** — the worktree executor lacks live browser + backend access required to capture the disambiguation log line at Step 1c-1e. Static audit excludes Tasks 2c (frontend bucket-routing post-R-1) and 2d (prop chain — wired correctly for General-mode last-assistant). Remaining {2a, 2b} candidates require the disambiguation log line which Plan 05 closing UAT will capture. Same precedent as Phase 067.2 Plan 04 (`verdict_locked: deferred-to-plan-06-user-uat`).
- **No `'suggestions_failed'` Redis Stream emit** — Task 2a's recipe scoped that telemetry event ("future telemetry pipelines may consume `'suggestions_failed'`; today the frontend ignores unknown event types in `subscribeToRun`"). Per the No-op selection, only Task 1 ships in this plan; the telemetry event can revisit if Plan 05 captures Task 2a evidence.
- **No prompt-tightening or unit test in `suggestion_service.py`** — Task 2b's recipe scoped tightening `_SUGGESTION_SYSTEM_PROMPT` + adding a unit test. Per the No-op selection, the suggestion service is unchanged. If Plan 05 captures `"suggestions empty for run"` at runtime, the Task 2b recipe applies verbatim as a follow-up.

## Deviations from Plan

### Process deviations (worktree environment)

**1. [Process - Worktree] Initial edits applied to main repo path before worktree-relative path correction**
- **Found during:** Task 1 commit attempt (`git status` showed clean tree)
- **Issue:** Initial Edit calls used `C:\Vibe Apps\Agentic RAG\backend\app\api\threads.py` (main repo) instead of `C:\Vibe Apps\Agentic RAG\.claude\worktrees\agent-a7d19dabb98654037\backend\app\api\threads.py` (worktree). Mirror of Phase 067.3 Plan 03's Issues Encountered section ("Wrong working directory at first edit pass").
- **Fix:** Copied modified file from main repo → worktree (`cp` of `backend/app/api/threads.py`); copied INVESTIGATION-NOTES.md to worktree planning dir; restored main repo's `threads.py` to baseline via `git checkout --`; deleted orphan INVESTIGATION-NOTES.md from main repo.
- **Files affected:** `backend/app/api/threads.py` (worktree path), `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-INVESTIGATION-NOTES.md` (worktree path)
- **Verification:** `git -C "C:/Vibe Apps/Agentic RAG" status` clean for both paths; worktree `git status --short` shows only the expected modifications.
- **Committed in:** Pre-commit recovery; the actual Task 1 commit `1c81b87` carries the corrected paths.
- **Impact:** Zero — no commits landed on the wrong tree. Pattern documented for future worktree executors.

### Plan-content deviations

**2. [Plan-anticipated branch] No-op (deferred-evidence variant) selected over runtime-driven 2a/2b/2c/2d**
- **Found during:** Task 1 Step 1c-1e (runtime evidence capture)
- **Issue:** Steps 1c-1e require live browser + backend (`npm run dev` + `uvicorn` + Chrome DevTools + React DevTools + a Fahed-Mrad search-only LLM call). The worktree executor sandbox does not provide browser-driver tools, cannot reach the LLM provider, and cannot drive a uvicorn process to capture log lines.
- **Fix:** Followed the plan's own `<no-op>` action verbatim ("Plan 04 closes with only Task 1 (the observability swap, which we keep as cheap insurance per Specifics §R-3)"). Static-audit evidence excludes Tasks 2c and 2d for the post-Plan-03-R-1 General-mode last-assistant scenario; remaining runtime candidates {2a, 2b} are exactly what the Task 1 observability disambiguates at the next live UAT.
- **Verification:** `[x] No-op (deferred-evidence variant)` checkbox in INVESTIGATION-NOTES.md; Task 2 plan-verify regex `\[x\]\s+(Task 2[abcd]|No-op)` matches at line 102.
- **Plan precedent:** Phase 067.2 Plan 04 used the identical pattern (`verdict_locked: deferred-to-plan-06-user-uat`) with no source-code changes shipped — Phase 067.3 Plan 04 differs only in shipping unconditional Task 1 observability code per `must_haves.truths[6]`.
- **Impact on plan goal:** Zero degradation. The plan's R-3 contract ("Restore visible suggestion pills alongside the confidence indicator") is now strictly *better* served because the observability primitive is in place — Plan 05 closing UAT will route any remaining fix directly at the captured failure mode (2a backend silent / 2b empty model / 3rd genuine no-op outcome).

---

**Total deviations:** 2 (1 process — worktree path correction; 1 plan-anticipated — No-op branch selection per the plan's `<no-op>` action). No scope creep; both follow established Phase 067.2 / 067.3 precedents.

## Issues Encountered

- **Worktree-without-runtime constraint** — the closing-UAT-style runtime evidence capture required by Step 1c-1e (browser + backend + LLM call + React DevTools `$r.props.message.suggestions`) is not available in this sandbox. Resolved by following the plan's own `<no-op>` branch with explicit deferral to Plan 05 closing UAT. Phase 067.2 Plan 04 used the identical precedent.
- **Initial-pass wrong-working-directory edit** — same issue documented in Phase 067.3 Plan 03's Issues Encountered ("Wrong working directory at first edit pass"). Caught before commit; resolved via `cp` + main-repo restore. No commits landed on the wrong tree.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes. The plan's STRIDE register is honored as planned:

- **T-067.3-04-01 (I — logger.warning(exc_info=True) might log sensitive exception payloads):** ACCEPTED. Suggestion-generation exceptions are typically OpenAI SDK errors (NotFoundError, RateLimitError, etc.) carrying model name + status code, no PII. Standard log retention applies.
- **T-067.3-04-02 (I — `'suggestions_failed'` Redis event leak):** N/A in this plan — Task 2a's `_emit('suggestions_failed', ...)` was NOT shipped (No-op selection). Future telemetry can revisit.
- **T-067.3-04-03 (T — Branch 2c console.log added in dev):** N/A in this plan — Task 2c was not selected.
- **T-067.3-04-04 (D — Tighter prompt 2b doubles model token usage):** N/A in this plan — Task 2b was not selected; `_SUGGESTION_SYSTEM_PROMPT` unchanged.

## Known Stubs

None — no stubs introduced. The Task 1 observability log lines are the deliverable, not stubs.

## User Setup Required

None — no external service configuration required. The two new log lines emit to the existing uvicorn stdout / configured log handler.

## Next Phase Readiness

- **Plan 05 closing UAT (Wave 3) — UNBLOCKED for R-3 row exercise.** The Task 1 observability primitive is in place. When Plan 05 exercises the Fahed-Mrad search-only prompt, the uvicorn console will surface exactly one of:
  1. `"suggestion generation failed for run <run_id>"` + traceback → Task 2a candidate; route to follow-up patch (recipe in Plan 04's `<action>` block applies verbatim).
  2. `"suggestions empty for run <run_id> — generate_suggestions returned []"` → Task 2b candidate; route to follow-up patch (tighten `_SUGGESTION_SYSTEM_PROMPT` + add unit test per Plan 04's `<action>` block).
  3. Neither line + a normal `_emit ... 'suggestions'` event in the SSE stream + suggestion pills visible in DOM → R-3 retroactively closed (genuine No-op outcome).
  4. Neither line + suggestion pills NOT visible in DOM → unexpected; escalate as a new finding (would imply a render-side bug NOT excluded by the static audit).
- **Phase 067.3 R-3 contract (Plan 04)** — observability deliverable shipped (Task 1). Closing-UAT runtime verdict deferred to Plan 05 per anticipated `<no-op>` branch.

## Self-Check: PASSED

**Files (Read tool / git diff verified):**
- `backend/app/api/threads.py` — modified (committed in `1c81b87`).
- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-INVESTIGATION-NOTES.md` — created (committed in `1c81b87`).
- `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-SUMMARY.md` — created (this file; committed in metadata commit below).

**Commits (`git log --oneline -3` verified):**
- `1c81b87` feat(067.3-04): backend observability for suggestion-emit path (D-067.3-R3-03 Task 1) — FOUND
- `2515342` docs(phase-067.3): update tracking after wave 1 — FOUND (parent of this plan's commits)

**Acceptance criteria (Task 1 — backend/app/api/threads.py):**
- `grep -nE "logger\.warning\(\s*\n?\s*\"suggestion generation failed" backend/app/api/threads.py` (multiline): 1 hit ✓
- `grep -nE "exc_info=True" backend/app/api/threads.py` includes the new warning at threads.py:2520 — verified by inspection ✓
- `grep -nE "logger\.info\(\s*\n?\s*\"suggestions empty" backend/app/api/threads.py` (multiline): 1 hit ✓
- `grep -c "pass.*SUG-04" backend/app/api/threads.py`: 0 hits ✓
- `grep -c "D-067.3-R3-03 Task 1" backend/app/api/threads.py`: 2 hits (≥ 2 required) ✓
- `python -c "import ast; ast.parse(open('app/api/threads.py').read())"` exits 0 ✓
- INVESTIGATION-NOTES.md exists with the structured sections — verified by Read ✓
- INVESTIGATION-NOTES.md branch selection: `grep -E "\[x\]\s+(Task 2[abcd]|No-op)"` matches `[x] No-op (deferred-evidence variant)` at line 102 ✓
- Backend uvicorn smoke run: DEFERRED to Plan 05 closing UAT (per the worktree-without-runtime precedent + the plan's `<no-op>` branch).

**Acceptance criteria (Task 2 — branch selection):**
- INVESTIGATION-NOTES.md has exactly ONE checked branch — `[x] No-op (deferred-evidence variant)` ✓
- Code changes from non-selected branches (2a `'suggestions_failed'` emit; 2b prompt change + unit test; 2c MessageItem console.log; 2d ChatArea/MessageList prop change) are ABSENT — confirmed by `git diff` on the worktree (only the Task 1 lines in threads.py changed) ✓
- Manual smoke run captured in INVESTIGATION-NOTES.md: DEFERRED to Plan 05 closing UAT per worktree-without-runtime precedent.
- All decision-ID citations include the branch tag — Task 1 cites `D-067.3-R3-03 Task 1a` and `Task 1b`; INVESTIGATION-NOTES.md cites Task 2a/2b/2c/2d/No-op explicitly ✓

---
*Phase: 067.3-streaming-render-and-storage-fixes-round-2*
*Completed: 2026-05-09*
