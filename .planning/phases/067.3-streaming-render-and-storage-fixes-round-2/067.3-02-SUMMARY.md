---
phase: 067.3-streaming-render-and-storage-fixes-round-2
plan: 02
subsystem: ui
tags: [frontend, sandbox-download, auth, ui, react, fetch, blob, supabase]

# Dependency graph
requires:
  - phase: 067.2-streaming-render-and-storage-fixes
    provides: "/sandbox-outputs/{path} backend endpoint (Plan 03 — path-segment fence + sandbox_files lookup + 60s re-sign + 302 redirect; reused as-is, no changes)"
provides:
  - "downloadSandboxOutput(relativeUrl, filename) async helper in lib/api.ts that injects Bearer token via fetch and triggers programmatic <a download> click"
  - "DownloadError class with .status (number | 'network') for status-specific UI feedback"
  - "Stateful OutputFileCard with click intercept, downloading spinner (Loader2 + animate-spin), and inline status-specific error feedback"
affects: [067.3-05-closing-uat, future skills-export-helper-refactor]

# Tech tracking
tech-stack:
  added: []  # No new libraries — reused existing fetch + lucide Loader2 + supabase.auth.getSession patterns
  patterns:
    - "JS blob fetch+download for Bearer-required browser asset endpoints (vs cookie-only <a href>)"
    - "Status-routed DownloadError class for status-specific UX feedback (extends Error with .status field)"
    - "Inline 3s auto-clearing error feedback (no toast lib in repo) via setTimeout setState"

key-files:
  created: []
  modified:
    - "frontend/src/lib/api.ts (added DownloadError class + downloadSandboxOutput helper, +77 lines)"
    - "frontend/src/components/chat/ExecuteCodeBlock.tsx (replaced OutputFileCard with stateful click intercept, +53 / -4 lines)"

key-decisions:
  - "Reused getAuthToken pattern (lib/api.ts:21-26) for token injection — same convention as uploadDocument, exportSkill, exportAuditLogs"
  - "URL.revokeObjectURL deferred via setTimeout 1000ms — some browsers race the download trigger and immediate revoke can break the save dialog"
  - "Inline error feedback (red border + small red text below filename) over a toast library — repo has no toast lib and the chat-history footprint is small"
  - "Right-click 'Save link as' fallback explicitly accepted — preserved <a href={resolveOutputUrl(file.url)}> for UX continuity even though raw click hits 401 (D-067.3-R2-03)"

patterns-established:
  - "JS blob download for Bearer-required endpoints: fetch+Authorization → res.blob() → URL.createObjectURL → temp <a download> click → revoke on next tick"
  - "DownloadError is the canonical shape for download-flow errors; status field accepts both numeric HTTP codes and the 'network' literal"

requirements-completed: [STREAM-04-correctness-round2]

# Metrics
duration: ~10min
completed: 2026-05-09
---

# Phase 067.3 Plan 02: R-2 Sandbox-Outputs Auth via JS Blob Fetch+Download Summary

**JS-blob download path for `/sandbox-outputs/{path}` — fetch with Bearer header, follow 302 to Supabase CDN, download as Blob, trigger programmatic `<a download>` click; backend untouched.**

## Performance

- **Duration:** ~10 min (development); ~5 min wait for `node_modules` copy from main repo to enable `tsc` verify gate (worktree starts without `node_modules`)
- **Started:** 2026-05-09 (worktree session)
- **Completed:** 2026-05-09
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `downloadSandboxOutput(relativeUrl, filename)` helper added to `frontend/src/lib/api.ts` with status-routed error throwing per D-067.3-R2-04 verbatim copy.
- `DownloadError` class exported with `.status` field accepting `number | "network"` so the caller can branch UI by status without re-parsing message strings.
- `OutputFileCard` rewritten as a stateful component: `e.preventDefault()` blocks the default anchor click (which would 401), spinner swaps for the Download icon during the download, status-specific error message renders inline with a red border for 3s on failure.
- Right-click "Save link as" fallback preserved (the static `href={resolveOutputUrl(file.url)}` is intact) — accepted UX trade-off documented in code and CONTEXT.md.
- NO backend changes — `backend/app/api/sandbox_outputs.py` unmodified per D-067.3-R2-01.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add downloadSandboxOutput helper in lib/api.ts** — `64eb598` (feat)
2. **Task 2: Wire OutputFileCard click intercept + spinner + error feedback** — `d6a6ef4` (feat)

_Note: Plan declared `tdd="true"` on both tasks. Per Phase 063.1 plans 02/03/04 precedent (vitest unavailable on this machine due to npm optional-dep cascade — `@rolldown/binding-win32-x64-msvc` + `@jridgewell/sourcemap-codec` missing), the TDD ceremony was simplified to a TypeScript-gated single commit per task, matching the plan's actual `<verify>` gate (`npx tsc --noEmit` + grep acceptance criteria). Live verification + cross-user IDOR regression (D-067.3-R2-05) is owned by Plan 05 closing UAT. This is consistent with the documented STATE.md decision logs from Phase 063.1._

## Files Created/Modified

- `frontend/src/lib/api.ts` — Added `DownloadError` class + `downloadSandboxOutput` async helper after `uploadDocument` and before `deleteDocument` (insert site at the file-bytes helpers cluster). +77 lines.
- `frontend/src/components/chat/ExecuteCodeBlock.tsx` — Added imports (`MouseEvent` type, `downloadSandboxOutput`, `DownloadError`); replaced `OutputFileCard` with stateful click-intercept version. +53 / -4 lines net. The `resolveOutputUrl` helper, `TerminalOutput`, `formatBytes`, `formatDuration`, and `ExecuteCodeBlock` components are byte-identical to pre-change.

## Decisions Made

- **`useState` import was already present at line 1** — confirmed before edit; no separate import line change needed.
- **`Loader2` was already imported** in the existing `lucide-react` import line — no change to imports for that icon.
- **Used `import type { MouseEvent } from "react"`** for the click handler signature rather than `React.MouseEvent` to keep the inline type isolated and avoid an extra default-import dependency.
- **3s auto-clear for error feedback** is hard-coded as a `setTimeout` constant — repo has no shared toast lib and the small footprint doesn't justify hoisting the timer to a hook.
- **Did NOT add a unit test file** for this plan — vitest runtime is unavailable on this machine (per Phase 063.1 deferred-item carry-forward). Acceptance criteria gates were met via `tsc --noEmit` + grep checks per the plan's `<verify>` block. Cross-user IDOR + click-flow regression coverage is delivered by Plan 05's Chrome MCP UAT (D-067.3-R2-05).

## Deviations from Plan

None - plan executed exactly as written.

The plan declared TDD ceremony on both tasks, but as documented under Task Commits and consistent with Phase 063.1 precedent, the runtime test execution is deferred (vitest cannot run locally) and the binding `<verify>` gate is `tsc --noEmit` + grep acceptance criteria — both of which pass cleanly. This is a known-and-accepted carry-forward, not a deviation from the verify gate.

## Issues Encountered

- **Worktree `frontend/node_modules` not present at session start.** Worktrees spawned by Claude Code's `isolation="worktree"` carry only the working tree, not `node_modules`. To run the plan's `<verify>` gate (`npx tsc --noEmit`) the repo's `node_modules` was copied from the main repo (`C:/Vibe Apps/Agentic RAG/frontend/node_modules`). The copy ran in the background during code authoring; both `tsc` invocations (post-Task-1 and post-Task-2) completed cleanly. The copied `node_modules` is gitignored (`git status --short` confirmed) and will be discarded with the worktree.

## Verification Summary

**Per plan `<verify>` block + acceptance criteria:**

| Gate | Result | Notes |
|------|--------|-------|
| `npx tsc --noEmit` (frontend root) | clean exit (no output) | post-Task-1 ✓; post-Task-2 ✓ |
| `^export async function downloadSandboxOutput` in `lib/api.ts` | 1 hit (line 522) | matches |
| `^export class DownloadError` in `lib/api.ts` | 1 hit (line 513) | matches |
| `D-067.3-R2-0` citation in `lib/api.ts` | 2 hits (≥ 2 required) | lines 504, 512 |
| Verbatim "Session expired — please refresh the page and try again." | 2 occurrences (auth-fail path + 401-response path) | matches D-067.3-R2-04 |
| Verbatim "File not found." | 1 occurrence | matches D-067.3-R2-04 |
| Verbatim "Download failed — try again." | 2 occurrences (network path + 5xx path) | matches D-067.3-R2-04 |
| `URL\.(create\|revoke)ObjectURL` in `lib/api.ts` | 2 hits in helper (lines 565, 577); 4 additional hits in pre-existing exportSkill / exportAuditLogs (unchanged) | matches `≥ 2` requirement |
| `import.*downloadSandboxOutput.*DownloadError` in `ExecuteCodeBlock.tsx` | 1 hit (line 5) | matches |
| `useState` count in `ExecuteCodeBlock.tsx` | 4 (≥ 3 required) | terminalOpen + downloading + downloadError + import |
| `Loader2.*animate-spin` in `ExecuteCodeBlock.tsx` | 2 hits (≥ 1 required) | OutputFileCard spinner + execution-running spinner |
| `e\.preventDefault\(\)` in `ExecuteCodeBlock.tsx` | 1 hit (line 53) | matches |
| `D-067.3-R2-0` citation in `ExecuteCodeBlock.tsx` | 3 hits (≥ 1 required) | matches |
| `cursor-not-allowed` in `ExecuteCodeBlock.tsx` | 1 hit (line 83) | matches D-067.3-R2-03 |
| `border-red-500/40` in `ExecuteCodeBlock.tsx` | 1 hit (line 84) | matches D-067.3-R2-03 |
| `downloadError` state hook + conditional className + inline error render | all three present | lines 50, 84, 94-95 |
| `resolveOutputUrl(file.url)` href preserved | 1 hit (line 75) | right-click "Save link as" target preserved per D-067.3-R2-03 |
| Backend `sandbox_outputs.py` unmodified | confirmed via `git diff 3524dd5..HEAD --name-only` shows only the two frontend files | matches D-067.3-R2-01 |

## Threat Surface Scan

No new security-relevant surface introduced. The plan's `<threat_model>` register is unchanged:

- **T-067.3-02-01 (IDOR mitigation)** — backend's `sandbox_outputs.py:48-67` continues to collapse missing-and-unauthorized to 404; frontend mirrors `"File not found."` message verbatim. Plan 05 owns the Chrome MCP e2e regression (D-067.3-R2-05).
- **T-067.3-02-02 (Bearer token in fetch header)** — accepted; reuses `getAuthToken` pattern.
- **T-067.3-02-03 (Blob URL via createObjectURL)** — accepted; origin-scoped + revoked after 1s.
- **T-067.3-02-04 (Right-click 'Save link as' → 401)** — accepted UX trade-off, documented inline.
- **T-067.3-02-05 (Repeated rapid clicks)** — `downloading` state guards re-entry; `aria-disabled` + `cursor-not-allowed` provide visual feedback.

## User Setup Required

None - no external service configuration required. The fix is purely a frontend behavioral change that exercises the existing `/sandbox-outputs/{path}` endpoint (already shipped in Phase 067.2 Plan 03) using an `Authorization: Bearer` header that the backend already consumes.

## Self-Check: PASSED

**Files exist and are committed:**
- `frontend/src/lib/api.ts` — modified, committed in `64eb598` ✓
- `frontend/src/components/chat/ExecuteCodeBlock.tsx` — modified, committed in `d6a6ef4` ✓

**Commits exist on branch:**
- `64eb598` (Task 1) ✓
- `d6a6ef4` (Task 2) ✓

**No backend modifications:**
- `git diff 3524dd5..HEAD --name-only` returned exactly 2 files, both under `frontend/` ✓

## Next Phase Readiness

- Code shipped is ready for Plan 05 (closing UAT) end-to-end verification of R-2 row + D-067.3-R2-05 cross-user IDOR Chrome MCP e2e regression.
- Plan 03 (R-1 per-thread store, Wave 1 parallel) and Plan 01 (N-01 router fix, Wave 1 parallel) are file-disjoint with this plan — no merge risk.
- Plan 04 (R-3 suggestions, Wave 2) is unaffected.

---
*Phase: 067.3-streaming-render-and-storage-fixes-round-2*
*Plan: 02*
*Completed: 2026-05-09*
