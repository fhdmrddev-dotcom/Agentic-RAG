---
phase: 067.3
plan: 01
subsystem: api
tags: [backend, routing, multi-provider, surgical-fix, model-capabilities]

requires:
  - phase: 067.2
    provides: "Phase 067.2 Plan 06 Branch B closure — N-01 HIGH finding (model→provider router auto-resolution gap at threads.py:949) escalated to Phase 067.3."
  - phase: 052
    provides: "Multi-provider model routing via MODEL_CAPABILITIES registry (config.py:92-129) + get_model_capability helper (config.py:132-134)."
provides:
  - "Auto-resolution from MODEL_CAPABILITIES at threads.py:949 — Anthropic / Google / OpenRouter models route through their correct SDK even when user_settings.active_provider='openai'."
  - "4 unit tests in backend/tests/test_provider_router.py locking the resolution order (explicit body.provider > MODEL_CAPABILITIES[model] > active_provider fallback)."
  - "runs.provider populated correctly for downstream replay-and-tail observability and audit (D-067.3-N01-04)."
affects: [067.3-05-uat, 067.2-closing-rerun, agent_runner-sdk-selection]

tech-stack:
  added: []
  patterns:
    - "3-tier provider resolution chain (explicit body.provider > capability registry > active_provider fallback) — comments cite D-067.3-N01-01/02/04 for future-greppability."
    - "Mock-based POST-handler unit testing pattern: load_user_settings + override_provider + get_redis dependency overrides + producer-task cleanup in finally (test_provider_router.py)."

key-files:
  created:
    - "backend/tests/test_provider_router.py — 4 unit tests for N-01 router auto-resolution + repro run_id 6eab949f-78da-4ea4-ac01-f04b16c9be7d traceability."
  modified:
    - "backend/app/api/threads.py — patched _resolved_provider at line 949 with branched resolution; added get_model_capability to imports at line 32."

key-decisions:
  - "Used override_provider() to mutate _user_settings on the registry-resolved branch — preserves the canonical mutation path so the inner-shadowed user_settings = _user_settings at line 1084 picks up the resolved provider for free, with no second mutation needed downstream."
  - "Patched override_provider with a pass-through stub in tests rather than wiring real api_key fixtures — Plan 01 acceptance is the value written to runs.insert(...,'provider'), not the helper's return shape; test stays decoupled from credential validation."
  - "Test 3 hand-sets body.provider='openai' (the bug's WRONG value), NOT 'anthropic' — sets up a precedence assertion that actually exercises the precedence path; setting it to the correct value would mask the bug rather than test it."

patterns-established:
  - "3-tier resolution chain pattern: explicit override > registry lookup > settings fallback. Reusable for other model-attribute routing (e.g., per-model timeout overrides already follow this shape via get_per_call_timeout)."
  - "Producer-task cleanup in test finally: _RUN_TASKS.get(UUID(run_id)).cancel() + asyncio.wait_for(timeout=2) — prevents Pitfall 6 (closed-loop XADD on next test). Mirrors test_063_post_contract.py:280-289."

requirements-completed: [STREAM-04-correctness-round2]

duration: ~10min
completed: 2026-05-09
---

# Phase 067.3 Plan 01: Model→Provider Router Fix (N-01 HIGH) Summary

**Replaced the bare `_resolved_provider = _user_settings.active_provider` at threads.py:949 with a 3-tier resolution chain that consults `MODEL_CAPABILITIES[model]['provider']` first, restoring correct provider routing for Anthropic / Google / OpenRouter models when active_provider='openai' and no explicit body.provider override is set.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-08T23:55:00Z (worktree spawn)
- **Completed:** 2026-05-08T20:05:09Z (Task 2 commit)
- **Tasks:** 2 (Task 1 implementation, Task 2 TDD-flagged unit tests)
- **Files modified:** 2 (`backend/app/api/threads.py`, `backend/tests/test_provider_router.py`)

## Accomplishments

- **Surgical 5-line core fix at threads.py:949** with the existing `get_model_capability(model_id)` helper from config.py:132-134 — no new helpers invented, no side-effect on the explicit-`body.provider` path.
- **Downstream audit per D-067.3-N01-04** — confirmed that the 6 `user_settings.active_provider` reads inside `agent_runner` (lines 1225, 1316, 1334, 1366, 1368, 1576) automatically pick up the resolved provider through the closure-captured `_user_settings` reassignment, with no second mutation required.
- **4 passing unit tests** covering all four resolution paths with the canonical repro run_id `6eab949f-78da-4ea4-ac01-f04b16c9be7d` cited in the docstring.
- **runs.provider write site preserved** — line 980 still reads `_resolved_provider` (the value now correctly set), so the audit trail and replay-and-tail observability stay aligned.

## Task Commits

1. **Task 1: Patch _resolved_provider at threads.py:949 + audit downstream reads** — `6f6d1ff` (`fix`)
2. **Task 2: Add unit test for N-01 auto-resolution (TDD-flagged, GREEN-only)** — `e0e5321` (`test`)

_Note: Plan 01 is `tdd="true"` on Task 2 only. Because Task 1's implementation landed atomically before Task 2's tests were authored, the RED ceremony was waived per the plan's acceptance-criteria language ("No actual revert needed; the assertion semantics suffice"). The bug-repro test (Test 1) asserts `provider == 'anthropic'` and would fail with `provider == 'openai'` under the unfixed code, satisfying the lock-against-regression invariant._

## Files Created/Modified

- **`backend/app/api/threads.py`** (modified, +23 -2):
  - Line 32 — added `get_model_capability` to the `from app.config import …` line.
  - Lines 949-970 — replaced the bare `_resolved_provider = _user_settings.active_provider` with a branched resolution citing D-067.3-N01-01/02/04 in comments.
- **`backend/tests/test_provider_router.py`** (created, 328 lines):
  - 4 async pytest tests + shared `_run_post_and_capture` helper.
  - Mocks `load_user_settings`, `override_provider`, redis dependency, `create_adaptive_streaming_chat`, `stream_anthropic`, `generate_thread_title`, `generate_suggestions` to keep the producer task neutral.
  - Producer-task cleanup in finally to avoid leaking into subsequent tests (Pitfall 6 mirror).

## Decisions Made

- **Use `override_provider()` to mutate `_user_settings` on the registry-resolved branch** — keeps the canonical mutation path. The inner-shadowed `user_settings = _user_settings` at line 1084 then sees the resolved provider for free; no second mutation in agent_runner needed (D-067.3-N01-04).
- **Test stub for `override_provider`** — the real helper returns `effective` unchanged when the matched provider has no `api_key`, which would couple the test to credential validation rather than the resolution-order assertion. Stub mutates `active_provider` unconditionally; Plan 01 acceptance is the value written to `runs.insert(...,'provider')`, which is `_resolved_provider` (set by the routing logic, not the helper's return value).
- **Plan's "second `load_user_settings` call" comment is stale** — the inner agent_runner does `user_settings = _user_settings` at line 1084 (a reference assignment), not a second `load_user_settings(...)` call. The audit confirmed this and the plan's Step 3 fallback ("ONE additional reconciliation right after that inner load_user_settings call") is therefore unnecessary; reassigning `_user_settings` before producer spawn is sufficient because Python closures capture by reference.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Workflow Bug] Initial commit landed on wrong branch (v2.5-dev) due to bash `cd` cwd-reset escaping the worktree**
- **Found during:** Task 1 commit
- **Issue:** The bash tool's per-command cwd reset combined with my `cd "C:/Vibe Apps/Agentic RAG"` in the commit pipeline took the commit out of the worktree (`agent-a3dca6487a8e832a9`) and into the main repo on `v2.5-dev`. The pre-commit HEAD assertion at the start of execution passed (it ran in the worktree), but the commit pipeline ran in the main repo.
- **Fix:** Recovered cleanly: `git reset --soft HEAD~1` on `v2.5-dev` to undo the commit, `git reset HEAD <file>` + `git checkout -- backend/app/api/threads.py` to restore the file, then re-applied the same edits to the worktree-path file and committed in the worktree. v2.5-dev was rolled back to `3524dd5` (its pre-mistake state); no concurrent work was clobbered (the protected-ref policy was honored — `v2.5-dev` is not in the deny-list, and the commit being rolled back was my own from <30s prior).
- **Files modified (recovery):** `backend/app/api/threads.py` (restored in main, then re-edited in worktree).
- **Verification:** `git log --oneline -2` on worktree shows `e0e5321` (test) → `6f6d1ff` (fix) → `3524dd5` (Phase 067.3 plan); `git log --oneline -2` on main repo (`v2.5-dev`) shows `3524dd5` (back to pre-execution state).
- **Committed in:** Task 1 commit (`6f6d1ff`) is the corrected commit on the worktree branch; the v2.5-dev rollback is not committed (it was a soft reset + checkout).

**2. [Rule 3 - Blocking] Worktree has no `backend/venv/` — import smoke-check needed PYTHONPATH + main-repo venv**
- **Found during:** Task 1 verification (smoke import after Edit)
- **Issue:** Worktrees spawned by Claude Code's `isolation="worktree"` are bare git checkouts with no `backend/venv/`; the verify step's `python -c "from app.api import threads"` would fail with `ModuleNotFoundError: No module named 'app'`.
- **Fix:** Used `PYTHONPATH="$(pwd)" SUPABASE_URL=… SUPABASE_SERVICE_ROLE_KEY=… "C:/Vibe Apps/Agentic RAG/backend/venv/Scripts/python.exe"` to run the smoke check from the worktree's `backend/` directory using the main-repo's venv interpreter. Pytest run used the same pattern.
- **Files modified:** None (test-environment-only workaround).
- **Verification:** `IMPORT OK` on smoke check; pytest reported `4 passed, 1 warning in 6.51s`.
- **Committed in:** N/A (workflow-only).

---

**Total deviations:** 2 auto-fixed (1 workflow bug recovered, 1 environment workaround).
**Impact on plan:** Zero impact on the deliverable — both deviations are workflow-level and didn't change the code or tests. Cost ~1 minute of recovery time.

## Issues Encountered

- **Worktree commit-routing trap.** Documented in deviation #1 above. Future executor agents should be aware that `cd <main-repo-path>` from inside a worktree's bash tool will ESCAPE the worktree because each Bash call resets cwd. Use absolute paths to the worktree directory (`cd "C:/Vibe Apps/Agentic RAG/.claude/worktrees/agent-…"`) or invoke git with `--git-dir`/`-C` flags when committing. The pre-commit HEAD assertion in `task_commit_protocol` step 0 passes when run inside the worktree, but won't fire if a `cd` has already escaped the worktree before the assertion runs (because the main repo's `.git` is a directory, not a file, so `if [ -f .git ]; then …` is false).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 02 (R-2 sandbox download), Plan 03 (R-1 per-thread store) ready to merge in parallel** — D-067.3-WAVE-02 confirmed no file overlap with Plan 01.
- **Plan 04 (R-3 suggestions) ready to start in Wave 2** — reads `useMessages.ts` post-Plan-03; does not touch threads.py routing.
- **Plan 05 (closing UAT) needs row R-N-01 evidence** — a manual repro run with `model='claude-sonnet-4-6'` + `active_provider='openai'` (no body.provider) should now return 200 (not 404) and `runs.provider='anthropic'` in the row.

## Self-Check: PASSED

**Files exist:**
- `C:/Vibe Apps/Agentic RAG/.claude/worktrees/agent-a3dca6487a8e832a9/backend/app/api/threads.py` — FOUND (modified)
- `C:/Vibe Apps/Agentic RAG/.claude/worktrees/agent-a3dca6487a8e832a9/backend/tests/test_provider_router.py` — FOUND (created)

**Commits exist on `worktree-agent-a3dca6487a8e832a9`:**
- `6f6d1ff fix(067.3-01): route by MODEL_CAPABILITIES when body.provider absent (N-01)` — FOUND
- `e0e5321 test(067.3-01): lock N-01 router auto-resolution against MODEL_CAPABILITIES` — FOUND

**Plan acceptance criteria:**
- `get_model_capability` import: PRESENT at threads.py:32
- `if body.provider:` hits ≥ 2: PRESENT (lines 944, 956)
- `get_model_capability(_resolved_model)` exactly 1 hit: PRESENT (line 960)
- Bare `_resolved_provider = _user_settings.active_provider` outside guards: 0 hits (lines 958 + 970 are inside guarded `if/else` branches)
- `runs.insert` dict still references `_resolved_provider`: PRESENT at line 980
- `python -c "from app.api import threads"`: IMPORT OK
- D-067.3-N01-01 / -02 / -04 cited in code comments: PRESENT (lines 949-955, 963-967)
- `grep -c "6eab949f-78da-4ea4-ac01-f04b16c9be7d" backend/tests/test_provider_router.py`: 3 (≥ 1)
- `grep -c "D-067.3-N01" backend/tests/test_provider_router.py`: 8 (≥ 2)
- 4 test functions: PRESENT
- `pytest tests/test_provider_router.py -v`: 4 passed in 6.51s

---
*Phase: 067.3-streaming-render-and-storage-fixes-round-2*
*Plan: 01 — Model→Provider Router Fix (N-01 HIGH)*
*Completed: 2026-05-09*
