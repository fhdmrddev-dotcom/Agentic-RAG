---
phase: 248-the-credential-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/settings/grantsVocabulary.ts
  - frontend/src/components/settings/ActionRow.tsx
  - frontend/src/components/settings/__tests__/ConnectionGrantsList.test.tsx
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [CRED-02]

must_haves:
  truths:
    - "The grant override reset affordance is reworded to a forward action ('Follow the default instead') in `grantsVocabulary.ts` (CRED-02, D-248-06)."
    - "The affordance renders 'Follow the default instead' on an overridden row and is absent on a default row."
    - "The retired phrasing 'Use the default' is absent from the rendered surface."
    - "The rendered content fence lives in `src/components/settings/__tests__/ConnectionGrantsList.test.tsx`, which is pinned in `scripts/vitest-count-gate.cjs` TARGETS and BASELINE, ensuring it executes under the count gate."
    - "Hot-file ledger row and section for `ActionRow.tsx` are maintained in `docs/HOT-FILE-LEDGER.md`."
  artifacts:
    - path: "frontend/src/components/settings/grantsVocabulary.ts"
      provides: "Updated OVERRIDDEN_RESET copy with forward-action phrasing"
      contains: "Follow the default instead"
    - path: "frontend/src/components/settings/ActionRow.tsx"
      provides: "ActionRow rendering forward action affordance"
      contains: "OVERRIDDEN_RESET"
    - path: "frontend/src/components/settings/__tests__/ConnectionGrantsList.test.tsx"
      provides: "Component test asserting rendered content of ActionRow in default and overridden states"
      contains: "Follow the default instead"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "Scan list row and section for ActionRow.tsx"
      contains: "ActionRow.tsx"
  key_links:
    - from: "frontend/src/components/settings/ActionRow.tsx"
      to: "frontend/src/components/settings/grantsVocabulary.ts"
      via: "GRANTS_COPY.OVERRIDDEN_RESET"
      pattern: "GRANTS_COPY\\.OVERRIDDEN_RESET"
    - from: "frontend/src/components/settings/__tests__/ConnectionGrantsList.test.tsx"
      to: "frontend/src/components/settings/ActionRow.tsx"
      via: "Rendered text assertions in pinned suite"
      pattern: "Follow the default instead"
---

<objective>
**CRED-02: Reword the grant override affordance to forward action and assert rendered content in a pinned gate suite.**

Ensure that tool grant rows that differ from default do not overclaim human authorship or frame resetting as undoing a human choice. Update `GRANTS_COPY.OVERRIDDEN_RESET` to `"Follow the default instead"`, verify through rendered content assertions in `ConnectionGrantsList.test.tsx`, and track `ActionRow.tsx` in the hot-file ledger.
</objective>

<threat_model>
### Security & Reliability Invariants (ASVS L1)
1. **Attribution Integrity (TM-248-02 / `BUG-260828-11`, written here as `BUG-260828-02` — re-pointed at Phase 252 / D-36, because that id was shared by two different bugs and resolved by filename to the OTHER one, a closed workflow-input report):**
   - Invariant: Grant settings UI must not attribute default postures or system backfills to operator actions.
   - The affordance must indicate forward convergence ("Follow the default instead") rather than an undo of human choice ("Use the default").
2. **Content vs Presence Fencing (D-248-07):**
   - Invariant: Vitest assertions must test visible text content (`screen.getByText("Follow the default instead")` and query for `"Use the default"` returns null), preventing regressions caused by structural markup surviving text drift.
3. **Execution Under Count Gate:**
   - Invariant: The fence lives in `ConnectionGrantsList.test.tsx`, an existing suite already in `scripts/vitest-count-gate.cjs` TARGETS and BASELINE, ensuring it executes reliably and moves the gate legitimately.
</threat_model>

<context>
- `frontend/src/components/settings/grantsVocabulary.ts`: line 39 (`OVERRIDDEN_RESET: "Use the default"`).
- `frontend/src/components/settings/ActionRow.tsx`: line 95 (`data-testid="grant-reset"`).
- `frontend/src/components/settings/__tests__/ConnectionGrantsList.test.tsx`: line 212 (`getByTestId("grant-reset")`).
- `docs/HOT-FILE-LEDGER.md`: row added at 10391, detail section added at 13813.
</context>

<tasks>
<task>
<action>
Verify `ActionRow.tsx` entry in `docs/HOT-FILE-LEDGER.md`:
- Confirm metrics: `1 / 1 / 153`, `no (1 phase)`, scan list row and detail section.
- Run `node scripts/check-hot-file-ledger.cjs 248`.
</action>
<verify>
`node scripts/check-hot-file-ledger.cjs 248` outputs `ledger gate OK`.
</verify>
<done>
ActionRow.tsx is tracked in the hot-file ledger.
</done>
</task>

<task>
<action>
Update `frontend/src/components/settings/grantsVocabulary.ts`:
- Change `OVERRIDDEN_RESET: "Use the default"` to `OVERRIDDEN_RESET: "Follow the default instead"`.
- Document in the comment block the rationale from D-248-06 (forward action, not an undo of a choice no person made).
</action>
<verify>
Grep `OVERRIDDEN_RESET` in `frontend/src/components/settings/` to ensure clean resolution.
</verify>
<done>
Vocabulary updated to forward action.
</done>
</task>

<task>
<action>
Update `frontend/src/components/settings/__tests__/ConnectionGrantsList.test.tsx`:
- In the test asserting overridden rows:
  - Assert `within(overriddenRow).getByText("Follow the default instead")` is present and visible.
  - Assert `within(overriddenRow).queryByText("Use the default")` is null.
  - Assert `within(inheritedRow).queryByText("Follow the default instead")` is null.
</action>
<verify>
Run `npx vitest run frontend/src/components/settings/__tests__/ConnectionGrantsList.test.tsx`.
</verify>
<done>
Rendered text assertions pass in the count-gate pinned suite.
</done>
</task>
</tasks>
