---
phase: 175-cross-provider-streaming-fidelity
plan: 04
type: execute
wave: 2
depends_on: [175-01]
files_modified:
  - backend/app/services/thread_title.py
  - backend/app/services/suggestion_service.py
  - backend/tests/unit/test_title_reasoning_off.py
  - backend/tests/unit/test_threads_title_gen.py
autonomous: true
requirements: [XPROV-03, XPROV-04]

must_haves:
  truths:
    - "D-03: title-gen and suggestion apply provider_safe_utility_model so a cross-provider sub_agent_model override is dropped BEFORE the call → no 404 → no fallback_model emit → no misleading banner (suppress-when-fine)"
    - "D-04: no fallback banner when the primary utility model would succeed; a genuine substitution still emits fallback_model honestly"
    - "D-05: a SAFE model's title call injects the reasoning-off param (deepseek/kimi-k2.6/glm → extra_body thinking disabled; Google 2.5 → reasoning_effort none) so the tiny budget yields a real 4-6 word title; an UNSAFE model (MiniMax-M2.7-highspeed, gemini-3.5-flash) injects NOTHING and keeps the derived fallback"
    - "D-05/D-14: the title budget (30 / Google 160) and the inline-await-before-producer ordering (maybe_autotitle_thread) stay byte-identical; empty/refusal on a SAFE provider still derives (no regression of the closed title-generation-broken fix)"
  artifacts:
    - path: backend/app/services/thread_title.py
      provides: "D-03 guard on the multi-model override + D-05 reasoning-off injection on the title call"
      contains: "provider_safe_utility_model"
    - path: backend/app/services/suggestion_service.py
      provides: "D-03 guard on the suggestion override"
      contains: "provider_safe_utility_model"
    - path: backend/tests/unit/test_title_reasoning_off.py
      provides: "SAFE injects / UNSAFE omits param matrix + budget/ordering invariance + empty-response derive"
  key_links:
    - from: backend/app/services/sub_agent_models.py
      to: backend/app/services/thread_title.py
      via: "provider_safe_utility_model (Plan 01) wraps the raw override before the provider call"
      pattern: "provider_safe_utility_model"
    - from: backend/app/config.py
      to: backend/app/services/thread_title.py
      via: "reasoning_off marker (Plan 01) read via get_model_capability at the title-call request build"
      pattern: "reasoning_off"
---

<objective>
Fix cross-provider utility-model honesty AND reasoning-provider title quality at the title-gen seam: apply the shared provider-safe guard so no misleading fallback banner fires (XPROV-03 / D-03), and inject a per-MODEL reasoning-off param on the title call so SAFE reasoning providers produce a real title instead of the degenerate first-few-words fallback (XPROV-04 / D-05) — without enlarging the tiny budget or changing the inline-await ordering.

Purpose: closes BUG-260623-01 (misleading fallback banner) at its root and BUG-260722-01 (degenerate reasoning-provider titles), both provider-docs-first and default-inert.
Output: the D-03 guard applied at thread_title + suggestion, the D-05 reasoning-off injection on the title call for SAFE models only, and two unit proofs (plus a no-regression extension).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/175-cross-provider-streaming-fidelity/175-CONTEXT.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-RESEARCH.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-PATTERNS.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-VALIDATION.md

<interfaces>
<!-- Verified against live source 2026-07-22. Depends on Plan 01 (provider_safe_utility_model + reasoning_off markers). -->
thread_title.py:
  generate_thread_title(first_user_message, user_settings=None, chat_model="") -> (title, fallback_info)  (body from :100)
  multi-model override (UNGUARDED — root of BUG-260623-01): :125-137  (`override = (user_settings.sub_agent_model ...) or settings.sub_agent_model`; `if override: model = override`)
  budget (BYTE-IDENTICAL): `_title_max_tokens = 160 if provider=="google" else 30`  :145 ; token_param :146 ; the title call :154-159
  derived fallback (MUST stay intact for UNSAFE/empty): _clean_llm_title :82-96 → _derive_title_from_message ; _strip_think_blocks :50-67
  inline-await ordering (BYTE-IDENTICAL): maybe_autotitle_thread :247 ; fallback_model emit BEFORE title emit :256-262
  patch surface (162.5 late-import): tests patch `app.api.threads.get_llm_client` / `app.api.threads.generate_thread_title`
suggestion_service.py: override block :74-87 (twin of thread_title's unguarded pattern)
config.py: get_model_capability(model_id) :515 → row.get("reasoning_off") ∈ {"thinking_disabled","effort_none", absent}
Helper (Plan 01): provider_safe_utility_model(user_settings, override_candidate) -> str | None  (sub_agent_models.py)
Analog tests: backend/tests/unit/test_threads_title_gen.py (patches app.api.threads.get_llm_client; drives generate_thread_title).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider-safe guard at title-gen + suggestion (XPROV-03 D-03)</name>
  <files>backend/app/services/thread_title.py, backend/app/services/suggestion_service.py, backend/tests/unit/test_threads_title_gen.py</files>
  <read_first>
    - backend/app/services/thread_title.py :125-137 (the unguarded multi-model override) + :163-190 (the 404 fallback branch that emits `fallback_model`) + :256-262 (the ordering invariant).
    - backend/app/services/suggestion_service.py :74-87 (twin unguarded override).
    - backend/app/services/sub_agent_models.py — `provider_safe_utility_model` (from Plan 01).
    - 175-PATTERNS.md § "thread_title.py (XPROV-03 seam)" + § "suggestion_service.py" + 175-RESEARCH.md § "XPROV-03 Finding 6" (suppress-when-fine is automatic once the cross-provider override is dropped before the call).
    - Analog test: backend/tests/unit/test_threads_title_gen.py.
  </read_first>
  <behavior>
    - Active provider X + a stale sub_agent_model that infers to provider Y (Y != X) → the override is dropped; generate_thread_title falls through to _SUB_AGENT_MODEL_DEFAULTS[X]; NO 404 retry runs and NO fallback_model is emitted (no banner).
    - Active provider X + a sub_agent_model that infers to X → the override is used unchanged (byte-identical).
    - A GENUINE substitution (e.g. a real 404 on a same-provider model) still emits fallback_model honestly (D-04 keep-the-honest-case).
    - suggestion_service applies the same guard to its override.
  </behavior>
  <action>
    In `thread_title.py` at the multi-model else branch (:125-137), wrap the raw override acceptance with the shared guard: `override = provider_safe_utility_model(user_settings, (user_settings.sub_agent_model if user_settings else "") or settings.sub_agent_model)`. When the guard returns None (cross-provider mismatch), keep the existing fall-through to `_SUB_AGENT_MODEL_DEFAULTS.get(provider, ...)` (:134-137). Import `provider_safe_utility_model` from `app.services.sub_agent_models`. Because the cross-provider override is dropped BEFORE the call, no 404 is raised → no `fallback_model` emit → no banner (D-04 suppress-when-fine is automatic). Leave the 404 fallback branch (:163-190) and the ordering invariant (:256-262) untouched — a genuine same-provider 404 still emits `fallback_model` honestly.
    In `suggestion_service.py` (:74-87), apply the identical wrap to `override_model` via `provider_safe_utility_model`, falling through to the existing provider-default resolution when the guard returns None.
    Extend `backend/tests/unit/test_threads_title_gen.py` (mirror its `patch("app.api.threads.get_llm_client")` surface): a cross-provider `sub_agent_model` override → generate_thread_title returns `fallback_info is None` (no fallback_model emit) and uses the provider default; a same-provider override → used unchanged; a genuine same-provider fallback still returns non-None `fallback_info`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_threads_title_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Cross-provider override dropped → `fallback_info is None` (no `fallback_model` emit → no banner) (D-03/D-04).
    - Same-provider override preserved; genuine same-provider substitution still emits `fallback_model` (D-04 honest case kept).
    - suggestion_service applies `provider_safe_utility_model` (source assertion).
    - D-14: same-provider / single-model-provider / empty-override paths byte-identical; ordering invariant untouched.
  </acceptance_criteria>
  <done>The misleading cross-provider fallback banner is suppressed at its root at both sites; genuine substitutions stay honest; title-gen test extended and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-MODEL reasoning-off title call (XPROV-04 D-05)</name>
  <files>backend/app/services/thread_title.py, backend/tests/unit/test_title_reasoning_off.py</files>
  <read_first>
    - backend/app/services/thread_title.py :139-159 (budget comment + `_title_max_tokens` :145 + token_param :146 + the title call :154-159) + :82-96 (`_clean_llm_title` → `_derive_title_from_message`, the fallback that MUST stay intact for UNSAFE/empty) + :247-262 (inline-await ordering, byte-identical).
    - backend/app/config.py :515 (`get_model_capability`) — the `reasoning_off` marker from Plan 01.
    - 175-PATTERNS.md § "thread_title.py (XPROV-04 seam)" + 175-RESEARCH.md § "XPROV-04 Findings 7-8" (the per-provider verdict table; the enable-form proof at openai_service.py:1812-1817; budget/ordering must stay byte-identical) + § "Anti-Patterns" (do NOT enlarge the budget).
    - Analog test: backend/tests/unit/test_threads_title_gen.py (patch surface + mock client).
  </read_first>
  <behavior>
    - SAFE model with reasoning_off=="thinking_disabled" (deepseek-v4-flash/kimi-k2.6/glm-5-turbo) → the title call kwargs carry `extra_body={"thinking":{"type":"disabled"}}`.
    - SAFE model with reasoning_off=="effort_none" (Google 2.5, if configured) → kwargs carry `reasoning_effort="none"`.
    - UNSAFE model (no reasoning_off key: MiniMax-M2.7-highspeed, gemini-3.5-flash) → NO reasoning-off param in the kwargs (identical call to today).
    - Budget stays `160 if google else 30`; inline-await ordering unchanged; an empty/refusal response on a SAFE provider still returns a derived title (no regression).
  </behavior>
  <action>
    In `generate_thread_title`, after `model`/`_title_max_tokens`/`token_param` are resolved (:145-146) and before the `client.chat.completions.create` call (:154-159), compute `_reasoning_off = get_model_capability(model).get("reasoning_off")` and build an additive kwargs dict: when `_reasoning_off == "thinking_disabled"` → `{"extra_body": {"thinking": {"type": "disabled"}}}` (the DISABLE mirror of the proven DeepSeek ENABLE block at openai_service.py:1812-1817); when `_reasoning_off == "effort_none"` → `{"reasoning_effort": "none"}`; otherwise `{}`. Spread these into the existing create call via `**_reasoning_off_kwargs`. Do NOT change `_title_max_tokens` (30 / Google 160), the token_param, the messages, `stream=False`, or the inline-await ordering. Import `get_model_capability` from `app.config`. For UNSAFE models the dict is empty → the call is byte-identical → `_clean_llm_title` still derives on empty content (no regression of the closed `title-generation-broken-*` fix).
    Create `backend/tests/unit/test_title_reasoning_off.py` (mirror the `patch("app.api.threads.get_llm_client")` surface; assert on the mock client's recorded `create(...)` kwargs): SAFE thinking_disabled model → kwargs contain `extra_body={"thinking":{"type":"disabled"}}`; SAFE effort_none model → kwargs contain `reasoning_effort="none"`; UNSAFE model → kwargs contain NO `extra_body` reasoning key AND no `reasoning_effort`; assert `_title_max_tokens` is 30 for a non-google SAFE model and 160 for google (budget invariance); an empty-content response on a SAFE provider still returns a derived (non-empty, non-"New Chat") title.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_title_reasoning_off.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - SAFE model title call injects the correct provider param (extra_body thinking disabled OR reasoning_effort none); UNSAFE model injects NOTHING (per-MODEL, not per-provider) (D-05).
    - Budget (30 / 160) and inline-await ordering are byte-identical (source assertion on `_title_max_tokens` + unchanged create signature) (D-05/D-14).
    - Empty/refusal on a SAFE provider still derives (no regression of the closed title fix).
    - No package install; no shared-path fork.
  </acceptance_criteria>
  <done>SAFE reasoning providers produce real titles from the existing fast call; UNSAFE providers keep the honest derived fallback; the reasoning-off test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| stale utility-model setting → provider call | A cross-provider `sub_agent_model` value could route a utility call to the wrong provider — a routing-correctness concern, not a new external input surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-04-01 | Tampering | utility-model override routing | mitigate | `provider_safe_utility_model` drops a cross-provider override before any call; the guard changes only WHICH model id is sent, never the caller's RLS/auth context (request-scoped per Phase-163 D-03). |
| T-175-04-02 | Information Disclosure | fallback banner honesty | accept | Suppressing the false banner reduces misleading UI; a genuine substitution still emits `fallback_model` honestly. No secret/detail exposure. |
| T-175-04-03 | — | reasoning-off title param | accept | Additive `extra_body`/`reasoning_effort` kwarg on a SAFE-marked model's own title call; no new input surface, no authz change. |
| T-175-04-SC | Tampering | package installs | accept | No installs (RESEARCH Package Legitimacy Gate N/A); `git diff` shows zero requirements.txt / Dockerfile.sandbox changes. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` green.
- Assumption A2 (representative model names map to the researched generations) confirmed at UAT — the capability marker makes each verdict a per-row edit, no code change.
- SC#10 cross-provider axis (a SAFE reasoning provider produces a real title; Google/MiniMax UNSAFE controls still derive; parallel-thread title-gen isolation) is authored in 175-VALIDATION.md (manual — real per-provider title quality can't be mocked). Do not duplicate.
</verification>

<success_criteria>
- Cross-provider fallback banner suppressed at its root; genuine substitutions stay honest.
- SAFE reasoning providers emit real titles via the reasoning-off param; UNSAFE providers keep the derived fallback.
- Budget + inline-await ordering byte-identical; both unit proofs green; full unit suite green.
</success_criteria>

<output>
Create `.planning/phases/175-cross-provider-streaming-fidelity/175-04-SUMMARY.md` when done.
</output>
