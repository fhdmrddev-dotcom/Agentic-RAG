---
phase: 128-chat-tool-card-unification-chat-area-reclaim
plan: 04
type: execute
wave: 3
depends_on: ['128-03']
files_modified: [frontend/src/components/chat/RunCard.tsx, frontend/src/components/chat/ToolCallPanel.tsx, frontend/src/__tests__/components/RunCard.logo.test.tsx]
autonomous: true
requirements: [CTC-01, CTC-02, TDP-02]

must_haves:
  truths:
    - "D-01: the tool-card header avatar shows the REAL per-provider @lobehub/icons mark (via providerLogo(message.provider)) on the existing gradient-primary tinted backing; the animate-brandPulse ring STAYS while streaming; the card reads byte-identically across providers — only the logo differs"
    - "D-01: a tool's description surfaces DURING the preparing window (before tool_start) via preparingDescription(tc), reading the partial-JSON tc.argsCodeText (NOT tc.args, which is {} during preparing)"
    - "D-04: this closes the TDP-02 description-window slice of setting-up-agent-hides-model-activity (BUG-260607-02); the timer-reseed of BUG-260610-01 is left OPEN per the research finding (the reseed lives in the harness/workflow strip = Phase 127's surface, NOT the Deep RunStatusStrip 128 makes the sole timer); the banner-honesty + title-gen + dup-avatar causes are NOT folded"
    - "TDP-02 honest fallback: the description shows only WHEN preparingDescription(tc) is non-null; when null, the existing quiet 'Preparing {toolLabel}…' copy stays — never fabricate. The card always renders logo + status regardless; the description is additive"
    - "CTC-02: RunCard + ToolCallPanel both import the ONE providerLogo.tsx helper (no duplicate map/parse); the already-uniform RunStatusStrip + unifiedStepCount + fileCount carry status/elapsed/step/file — CTC-02 only adds the logo + description uniformly"
    - "Provider differences stay pure-presentation over the already-resolved message.provider / tc — NO provider branch enters any backend or SSE path (red line D-14); the unmapped-provider Bot fallback preserves zero regression"
  artifacts:
    - path: "frontend/src/components/chat/RunCard.tsx"
      provides: "CTC-01 avatar swap: providerLogo(message.provider) on the gradient-primary backing, brandPulse preserved, Bot fallback"
      contains: "providerLogo"
    - path: "frontend/src/components/chat/ToolCallPanel.tsx"
      provides: "TDP-02: preparingDescription(tc) appended to the preparing-branch copy + routed into the ToolArgsLivePanel title"
      contains: "preparingDescription"
    - path: "frontend/src/__tests__/components/RunCard.logo.test.tsx"
      provides: "CTC-01/02 coverage: provider mark renders for a given message.provider; Bot for unknown; brandPulse class present while streaming"
  key_links:
    - from: "frontend/src/components/chat/RunCard.tsx"
      to: "frontend/src/lib/providerLogo.tsx"
      via: "import providerLogo, render <Mark size={18}/> ?? <Bot/>"
      pattern: "providerLogo"
    - from: "frontend/src/components/chat/ToolCallPanel.tsx"
      to: "frontend/src/lib/providerLogo.tsx"
      via: "import preparingDescription, surface during preparing"
      pattern: "preparingDescription"
---

<objective>
Render the unified cross-provider tool card (CTC-02) by wiring the Plan 03 shared
helper into its two consumers:
1. **CTC-01** — swap the brand-pulse `Bot` dot in the RunCard tool-card header
   (`RunCard.tsx:283-287`) for the real per-provider `@lobehub/icons` mark via
   `providerLogo(message.provider)`, keeping the `gradient-primary` backing + the
   `brandPulse` ring (D-01).
2. **TDP-02** — surface a tool's `description` DURING the preparing window in
   `ToolCallPanel.tsx` via `preparingDescription(tc)` (the partial-JSON parse), with the
   honest `Preparing {tool}…` fallback when absent (D-01 / D-06).

Together these are the "unified card" — CTC-02 (the rest of the card, RunStatusStrip +
step/file counts, is already provider-agnostic). This plan closes the **TDP-02
description-window slice** of `setting-up-agent-hides-model-activity` (D-04); it does
NOT fold the BUG-260610-01 timer-reseed (research: that reseed is in the harness/workflow
strip = Phase 127's surface, not the Deep `RunStatusStrip` this phase governs — left open).

Purpose: distinguish providers in the header AND show "what it's about to do" during the
prep gap — the #1 honesty target — without any shared-path fork (red line D-14).
Output: RunCard avatar swap + ToolCallPanel description surface + a RunCard logo vitest.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The shared helper this plan consumes (from Plan 03)
@frontend/src/lib/providerLogo.tsx

# Sketch acceptance bar (G-2 — the visual contract)
@.planning/sketches/048-cross-provider-tool-card/
@.planning/sketches/MANIFEST.md
# (Running Design Decision 38 — the unified card; real @lobehub marks, no gradient-ID collisions)

# Pattern map — the EXACT in-place edit points + the preserve-list + the test scaffold
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md
# (§ "RunCard.tsx (MOD) — CTC-01 avatar swap", § "ToolCallPanel.tsx (MOD) — TDP-02 description surface",
#  § "Component (render) test scaffold" → the brandPulse assertion + jsdom-no-layout note)

# Research — Pattern 1/2 callers, the honest fallback, Pitfall 1/2 (preparing-window + Google atomic), the XSS rule
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md

# Reported bug whose TDP-02 slice this closes (D-04) — read the frontmatter re_open_trigger
@.planning/reported-bugs/setting-up-agent-hides-model-activity.md
# (BUG-260607-02 — folded: the description-window sub-cause ONLY. Causes b/c + dup-avatar stay open.)

<interfaces>
<!-- Key facts the executor needs — extracted from the codebase. No exploration required. -->

CTC-01 TARGET — RunCard.tsx:283-287 (swap the <Bot> glyph; keep EVERYTHING else):
```tsx
<div className={cn(
  "flex-shrink-0 w-8 h-8 rounded-full gradient-primary flex items-center justify-center shadow-sm shadow-primary/20",
  isStreamingNow && "animate-brandPulse",          // D-01: brandPulse ring STAYS while streaming
)}>
  <Bot className="w-4 h-4 text-white" />            // ← swap to: const Mark = providerLogo(message.provider); Mark ? <Mark size={18}/> : <Bot className="w-4 h-4 text-white"/>
</div>
```
- `message.provider` is ALREADY threaded (it feeds runSub at :246 — `${message.provider} · ${message.model} · turn N`). NO new prop, NO new wire.
- `cn` + `Bot` already imported at RunCard.tsx:2,7. ADD `import { providerLogo } from "@/lib/providerLogo"`.
- DO NOT touch the SECOND <Bot> at RunCard.tsx:352 (a different, non-avatar glyph) — CTC-01 is the :283-287 tool-card header avatar ONLY.
- RunCard has `data-testid="run-card"` at :252. The MessageItem assistant-bubble avatar (`assistant-bot-icon`) is a DIFFERENT surface — out of scope.

TDP-02 TARGET A — ToolCallPanel.tsx:819-833 (the preparing-branch quiet copy; APPEND description when present, keep the quiet copy when null):
```tsx
{tc.status === "preparing" ? (
  <span className="font-semibold text-foreground/50 italic">
    Preparing {toolLabel(tc.name)}…
    {/* TDP-02 ADD here: {preparingDescription(tc) && <span className="..."> — {preparingDescription(tc)}</span>} */}
    {tc.argsBytesStreamed != null && tc.argsBytesStreamed > 0 && ( /* the (X.X KB) badge — keep */ )}
  </span>
) : ( /* running/done branch — unchanged */ )}
```
TDP-02 TARGET B — ToolCallPanel.tsx:940-945 (the ToolArgsLivePanel title; route through the shared helper instead of reading tc.args.description directly — which is EMPTY during early preparing):
```tsx
<ToolArgsLivePanel
  title={ tc.args?.description
    ? `Generating ${toolLabel(tc.name)}: ${tc.args.description}`   // ← change to: const d = preparingDescription(tc); d ? `Generating ${toolLabel(tc.name)}: ${d}` : `Generating ${toolLabel(tc.name)}…`
    : `Generating ${toolLabel(tc.name)}…` }
  …
/>
```
- `toolLabel` is ALREADY imported in ToolCallPanel.tsx:14. ADD `import { preparingDescription } from "@/lib/providerLogo"`.
- DO NOT remove the Google-atomic honest block at ToolCallPanel.tsx:900-905 ("Waiting for model…") — same never-fabricate family; Google ships args atomically (~0 prep window — acceptable per D-06).

XSS rule (T-095.1-03-01, RunCard.tsx:300-304): rendered text = React text children only, never innerHTML. A @lobehub/icons SVG is a component (safe). The parsed description renders as a text child (safe).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CTC-01 — RunCard tool-card header logo swap + logo test</name>
  <files>frontend/src/components/chat/RunCard.tsx, frontend/src/__tests__/components/RunCard.logo.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunCard.tsx:1-10 (imports — `Bot`, `cn` present; ADD providerLogo), :246 (runSub proving message.provider is threaded), :280-288 (the avatar = the in-place swap target), :252 (`data-testid="run-card"`), :352 (the OTHER Bot — do NOT touch)
    - frontend/src/lib/providerLogo.tsx (the helper from Plan 03 — `providerLogo(provider) → component|null`)
    - frontend/src/__tests__/components/MessageItem.test.tsx (the `renderWithTooltip` + brandPulse-class assertion `expect(el.className).toContain("animate-brandPulse")` to copy)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md § "RunCard.tsx (MOD)" (the preserve-list) + § "Component (render) test scaffold"
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md Open-Question 2 (size≈18px inside the 32px backing — confirm vs sketch 048-A)
  </read_first>
  <behavior>
    - Test 1 (mapped provider → mark renders): render RunCard with a message whose `provider` is a mapped value (e.g. `"google"` or `"zhipu"`) → the provider mark renders inside the avatar (assert the avatar wrapper exists and the `<Bot>` fallback glyph is NOT the rendered child — query within `data-testid="run-card"`; add a stable testid on the avatar div if needed for a clean query).
    - Test 2 (unknown/absent provider → Bot fallback): render with `provider: "lmstudio"` (or undefined) → the `<Bot>` glyph renders (the D-08 fallback).
    - Test 3 (brandPulse preserved while streaming): render with a streaming run state + a mapped provider → the avatar wrapper className still contains `animate-brandPulse` (D-01 "brandPulse stays while streaming"). Render with a non-streaming/terminal state → no `animate-brandPulse`.
  </behavior>
  <action>
    In `RunCard.tsx` add `import { providerLogo } from "@/lib/providerLogo"`. At the tool-card header avatar (`:283-287`), compute `const Mark = providerLogo(message.provider)` and render `Mark ? <Mark size={18} /> : <Bot className="w-4 h-4 text-white" />` inside the existing `gradient-primary` rounded backing. KEEP the `isStreamingNow && "animate-brandPulse"` class (D-01) and the `gradient-primary` backing exactly as today — only the inner glyph changes. Set the mark size to ~18px to sit inside the 32px (`w-8 h-8`) backing; confirm visually against sketch 048-A at build (G-2). Do NOT change `runSub` (:246), the RunStatusStrip wiring, `unifiedStepCount`, `fileCount`, or anything else — the rest of the card is already the uniform CTC-02 surface. Do NOT touch the second `<Bot>` at :352 (non-avatar) or the MessageItem assistant-bubble avatar (out of scope). If a stable test query is needed, add a `data-testid` (e.g. `run-card-avatar`) to the avatar div — additive, no behavior change. Write `RunCard.logo.test.tsx` (`.tsx`) per the three behaviors using `renderWithTooltip` + the brandPulse-className assertion from `MessageItem.test.tsx`; note jsdom renders SVG components fine (this is structure, not layout). The provider mark is a React component (safe); preserve the React-text-children XSS rule for any text.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/RunCard.logo.test.tsx</automated>
  </verify>
  <done>The RunCard tool-card header renders the correct provider mark for a mapped `message.provider`, the `<Bot>` fallback for `lmstudio`/unknown, and keeps `animate-brandPulse` while streaming; the second Bot (:352) and the assistant-bubble avatar are untouched; the logo vitest passes.</done>
  <acceptance_criteria>
    `npx vitest run src/__tests__/components/RunCard.logo.test.tsx` exits 0.
    The test proves: mapped provider → mark (not Bot); unknown/undefined → Bot; `animate-brandPulse` present while streaming, absent on terminal.
    `RunCard.tsx` imports `providerLogo`; the avatar swap preserves the `gradient-primary` backing + the `animate-brandPulse` class.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TDP-02 — surface the preparing-window description in ToolCallPanel (closes the D-04 description slice)</name>
  <files>frontend/src/components/chat/ToolCallPanel.tsx</files>
  <read_first>
    - frontend/src/components/chat/ToolCallPanel.tsx:14 (`toolLabel` import — ADD preparingDescription), :819-833 (the preparing-branch quiet copy = TARGET A), :900-905 (the Google-atomic honest block — do NOT remove), :940-945 (the ToolArgsLivePanel title reading tc.args?.description directly = TARGET B)
    - frontend/src/lib/providerLogo.tsx (the `preparingDescription(tc)` helper from Plan 03)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md § "ToolCallPanel.tsx (MOD)" (the two reads to unify; the never-fabricate fallback)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md § Pattern 2 caller + the D-06 honest fallback + Pitfall 1 (tc.args empty during preparing) + Pitfall 2 (Google atomic ~0 window is correct, not a bug)
    - .planning/reported-bugs/setting-up-agent-hides-model-activity.md (the re_open_trigger — this task closes ONLY the TDP-02 description sub-cause; D-04)
  </read_first>
  <action>
    In `ToolCallPanel.tsx` add `import { preparingDescription } from "@/lib/providerLogo"`. TARGET A (the preparing-branch at `:819-833`): inside the `tc.status === "preparing"` `<span>`, after `Preparing {toolLabel(tc.name)}…` and alongside the existing `(X.X KB)` badge, append the description WHEN present — `{preparingDescription(tc) && <span className="ml-1 font-normal text-foreground/60 not-italic"> — {preparingDescription(tc)}</span>}` (call the helper once into a `const` if you prefer to avoid the double call). When `preparingDescription(tc)` is null, render NOTHING extra — the existing quiet `Preparing {toolLabel(tc.name)}…` copy stays (D-06 honest fallback; never fabricate). TARGET B (the `ToolArgsLivePanel` title at `:940-945`): replace the direct `tc.args?.description` read with the shared helper — `const d = preparingDescription(tc); title = d ? \`Generating ${toolLabel(tc.name)}: ${d}\` : \`Generating ${toolLabel(tc.name)}…\`` — so the title uses the partial-JSON description during preparing (today it reads the EMPTY `tc.args` and shows nothing until tool_start — Pitfall 1). Keep `contentText={tc.argsCodeText!}` and the rest of the ToolArgsLivePanel props unchanged. Do NOT remove the Google-atomic honest block at `:900-905` (same never-fabricate family). Do NOT mutate `tc.args` anywhere (StreamsProvider owns it — research Anti-Pattern). The card ALWAYS renders (logo + status); the description is additive. The description renders as a React text child (auto-escaped) — never innerHTML (V5).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/ToolCallPanel.test.tsx && grep -q "preparingDescription" src/components/chat/ToolCallPanel.tsx && echo "preparingDescription wired"</automated>
  </verify>
  <done>The preparing branch shows the parsed `description` during the prep window when present (and the quiet `Preparing {tool}…` when absent); the ToolArgsLivePanel title routes through `preparingDescription(tc)`; `tc.args` is never mutated; the Google-atomic block remains; the existing ToolCallPanel test suite still passes and `preparingDescription` is wired.</done>
  <acceptance_criteria>
    `npx vitest run src/__tests__/components/ToolCallPanel.test.tsx` exits 0 (no regression on the existing suite).
    `grep -c "preparingDescription" src/components/chat/ToolCallPanel.tsx` is ≥ 2 (both TARGET A and TARGET B wired).
    `grep "tc.args.description =" src/components/chat/ToolCallPanel.tsx` finds NOTHING (no mutation of tc.args).
    The Google-atomic honest block (`:900-905`) is still present.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output (tool-call args / description) → rendered card | The parsed/partial `description` is model-emitted text rendered into the card. |
| already-resolved `message.provider` → presentation map | A trusted, server-resolved string keys the pure-presentation logo map (no input crosses a backend boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-04-01 | Tampering / Info-disclosure (XSS) | the rendered `description` (preparing branch + ToolArgsLivePanel title) | mitigate | Rendered as React text children (auto-escaped); never `dangerouslySetInnerHTML` (RunCard.tsx:300-304 rule). The `@lobehub/icons` mark is a component, not interpolated markup. |
| T-128-04-02 | DoS (render crash) | `preparingDescription(tc)` invoked in the render path | mitigate | The helper (Plan 03) is internally try/catch → null on any parse failure (T-128-03-01); the caller never re-parses raw JSON itself. |
| T-128-04-03 | Spoofing (wrong-provider logo) | `providerLogo(message.provider)` | accept | The map is keyed by the verbatim server-resolved `runs.provider`; an unmapped string degrades to the honest Bot fallback (never a wrong brand). OpenRouter is shown as OpenRouter (not unwrapped) — the honest resolved provider (D-08). |
</threat_model>

<verification>
- CTC-01: the tool-card header shows the per-provider mark (Bot fallback for lmstudio/unknown); brandPulse preserved while streaming.
- TDP-02: the description appears during preparing when present (parsed from partial args); the quiet `Preparing {tool}…` fallback when absent; tc.args never mutated.
- CTC-02: RunCard + ToolCallPanel both import the ONE providerLogo.tsx; the rest of the card is already uniform.
- D-04: closes the TDP-02 description slice of BUG-260607-02; the timer-reseed + banner-honesty + title-gen + dup-avatar causes stay open.
- `RunCard.logo.test.tsx` passes; the ToolCallPanel suite passes; both helpers wired.
- The LIVE cross-provider proof (per-provider description presence + uniform card) is Plan 05 (D-06) — this plan's automated tests are structural; the live WIN per provider is measured there.
</verification>

<success_criteria>
The unified cross-provider tool card (CTC-02) is implemented: real per-provider logo (CTC-01) + preparing-window description (TDP-02), de-duplicated through the shared helper, with the honest Bot/`Preparing {tool}…` fallbacks and zero shared-path fork.
</success_criteria>

<output>
Create `.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-04-SUMMARY.md` when done
</output>
