---
phase: 128-chat-tool-card-unification-chat-area-reclaim
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/components/chat/MessageItem.tsx, frontend/src/__tests__/components/MessageItem.clamp.test.tsx]
autonomous: true
requirements: [CTC-04]

must_haves:
  truths:
    - "D-03: a long USER prompt collapses to a -webkit-line-clamp:7 preview + a fade matched to the violet end of the bubble's 135° gradient (hsl(258 90% 66%)) + an inline Read more / Show less chip"
    - "SHORT user prompts render byte-identically to today — no fade, no Read-more button (the clamp + fade are gated on overflow)"
    - "USER prompts ONLY — the assistant branch (RunCard / MarkdownRenderer) is untouched; right-alignment, rounded-br-md tail, max-w-[70%], whitespace-pre-wrap+break-words, the User avatar all preserved"
    - "message.content renders as React text children (auto-escaped), never dangerouslySetInnerHTML (V5 output-encoding)"
  artifacts:
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "User-bubble clamp + fade + Read-more (CTC-04), factored as a UserBubble subcomponent to keep MessageItem hook order pristine"
      contains: "webkit-line-clamp"
    - path: "frontend/src/__tests__/components/MessageItem.clamp.test.tsx"
      provides: "Structural coverage: long content → Read-more button + clamp container + fade class; short content → no button"
      contains: "from-[hsl(258_90%_66%)]"
  key_links:
    - from: "frontend/src/components/chat/MessageItem.tsx"
      to: "message.content"
      via: "clamped <p> with overflow-detect via useLayoutEffect + ref"
      pattern: "scrollHeight"
---

<objective>
Long USER prompts in the chat collapse to a clamped preview with a "Read more"
expander instead of rendering full-height — reclaiming chat-area vertical space
(CTC-04 / sketch 050-A / D-03). This plan is INDEPENDENT of the provider-logo chain
(it touches only `MessageItem.tsx`, imports nothing from `@lobehub/icons`), so it runs
in Wave 1 in parallel with the package install.

Purpose: a pasted ≥5KB user prompt today renders at full height and pushes the live
run off-screen; CTC-04 keeps the chat area honest.
Output: the user-bubble branch of `MessageItem.tsx` gains an overflow-detect clamp +
a gradient-matched fade + a Read-more/Show-less toggle, with a structural vitest.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Sketch acceptance bar (G-2 — the visual contract; there is NO UI-SPEC.md)
@.planning/sketches/050-long-prompt-readmore/
@.planning/sketches/MANIFEST.md
# (Running Design Decision 40 — the fade-into-the-bubble-not-a-panel-cut direction)

# Pattern map — the exact CTC-04 overflow-detect shape + the local-subcomponent factoring
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md
# (§ "MessageItem.tsx (MOD) — CTC-04 user-prompt clamp" + § "Local subcomponent factoring"
#  + § "Component (render) test scaffold" — note the jsdom-no-layout caveat)

# Research — Pattern 3 (cheapest-honest overflow-detect) + the fade color + Pitfall 5
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md
# (§ "Pattern 3: CTC-04 overflow-detect clamp", Pitfall 5, V5 output-encoding row)

<interfaces>
<!-- Key facts the executor needs — extracted from the codebase. No exploration required. -->

frontend/src/index.css:199 — the bubble's 135° gradient (the fade MUST dissolve to its violet END, NOT the page bg):
```css
.gradient-primary { background: linear-gradient(135deg, hsl(239 84% 67%), hsl(258 90% 66%)); }
```
So the CTC-04 fade color = hsl(258 90% 66%)  →  Tailwind arbitrary class `from-[hsl(258_90%_66%)]`.

frontend/src/components/chat/MessageItem.tsx:1 — current React import (ADD useLayoutEffect):
```tsx
import { memo, useRef, useState } from "react"
```
`cn` is NOT yet imported in MessageItem (RunCard imports it from "@/lib/utils" — same pattern available). The user branch is `if (isUser)` at :205; the user bubble `<p>` is at :209-210:
```tsx
<div className="gradient-primary text-white rounded-2xl rounded-br-md px-4 py-2.5 text-sm leading-relaxed shadow-sm">
  <p className="whitespace-pre-wrap break-words">{message.content}</p>
</div>
```
Hook-order note: MessageItem is memo-wrapped and has hooks (useWorkflowLockForThread, useState) BEFORE the `if (isUser)` early return at :205. Factor a local `<UserBubble>` subcomponent (mirror FinalOutputsPanel at :73) so its useRef/useLayoutEffect/useState do NOT perturb the parent hook order.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UserBubble subcomponent — overflow-detect clamp + gradient fade + Read-more</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/__tests__/components/MessageItem.clamp.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageItem.tsx:1 (imports), :197 (`isUser`), :205-217 (the user-bubble branch = the in-place target), :73 (`FinalOutputsPanel` local-subcomponent factoring to mirror)
    - frontend/src/__tests__/components/MessageItem.test.tsx (the `makeMessage` factory + `renderWithTooltip` + the `container.querySelector` / `getByRole` idioms to copy; the brandPulse-class assertion shape)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md § "Component (render) test scaffold" (the jsdom-no-layout rule: assert STRUCTURE + the fade CLASS, defer live overflow to D-06 manual UAT)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md § Pattern 3 + Pitfall 5
  </read_first>
  <behavior>
    - Test 1 (long content → clamp + Read-more present): a user `message.content` of many lines renders a Read-more button — `screen.getByRole("button", { name: /read more/i })` is found. (jsdom has no layout, so force the overflow branch: either render with the expanded/overflow state exposed for test, OR assert the clamp container + fade structure unconditionally present in the DOM and that the button label toggles. Mirror PATTERNS — assert structure + class, NOT runtime scrollHeight.)
    - Test 2 (short content → unchanged): a one-line user `message.content` renders NO Read-more button — `screen.queryByRole("button", { name: /read more/i })` is null; the `<p>` carries `whitespace-pre-wrap break-words` exactly as today.
    - Test 3 (fade color is the bubble violet, not page bg): the fade overlay element carries the class `from-[hsl(258_90%_66%)]` (assert via `container.querySelector('[class*="from-[hsl(258_90%_66%)]"]')` or className match) — proves the fade dissolves into the bubble.
    - Test 4 (assistant branch untouched): an `role: "assistant"` message does NOT render the user-bubble clamp container (CTC-04 is USER prompts only).
  </behavior>
  <action>
    Factor a local `UserBubble({ content }: { content: string })` subcomponent inside `MessageItem.tsx` (mirror `FinalOutputsPanel` at :73) and render it from the `if (isUser)` branch in place of the bare `<p className="whitespace-pre-wrap break-words">{message.content}</p>` at :210. Inside `UserBubble`: declare `const pRef = useRef<HTMLParagraphElement>(null)`, `const [overflowing, setOverflowing] = useState(false)`, `const [expanded, setExpanded] = useState(false)`; add `useLayoutEffect(() => { const el = pRef.current; if (el) setOverflowing(el.scrollHeight > el.clientHeight + 1) }, [content])` — use `useLayoutEffect` (NOT `useEffect`) so the measure runs pre-paint (Pitfall 5: no one-frame full-height flash). Add `useLayoutEffect` to the react import at :1. Wrap the `<p>` in a `<div className="relative">`; keep `whitespace-pre-wrap break-words` on the `<p>` always, and apply the clamp classes ONLY when `!expanded`: `[display:-webkit-box] [-webkit-line-clamp:7] [-webkit-box-orient:vertical] overflow-hidden` (gate them via `cn(...)` — add `import { cn } from "@/lib/utils"`). Render the fade overlay as an `aria-hidden pointer-events-none absolute inset-x-0 bottom-0 h-8` sibling with `bg-gradient-to-t from-[hsl(258_90%_66%)] to-transparent` ONLY when `overflowing && !expanded` — the `from-[hsl(258_90%_66%)]` is the violet end of the bubble's 135° gradient (index.css:199), NOT the page bg (D-03). Render a `<button onClick={() => setExpanded(v => !v)}>` with text `Read more` / `Show less` ONLY when `overflowing` (a small `mt-1 text-xs text-white/80 underline` chip). Do NOT touch the bubble shell, the `rounded-br-md` tail, `max-w-[70%]`, the right-alignment, or the `User` avatar — they stay exactly as :209-219. Do NOT clamp or otherwise alter the assistant branch (the `:218+` RunCard / MarkdownRenderer path). `content` renders as React text children — never `dangerouslySetInnerHTML` (V5).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/MessageItem.clamp.test.tsx</automated>
  </verify>
  <done>The user-bubble branch renders a clamped `<p>` (when not expanded) with a violet `from-[hsl(258_90%_66%)]` fade and a Read-more/Show-less toggle that appears only on overflow; short prompts are visually unchanged; the assistant branch is untouched; the new vitest passes.</done>
  <acceptance_criteria>
    `npx vitest run src/__tests__/components/MessageItem.clamp.test.tsx` exits 0.
    The test file asserts: long content → a `/read more/i` button is present; short content → that button is absent; the fade element's className contains `from-[hsl(258_90%_66%)]`; an assistant message does not render the user-bubble clamp container.
    `MessageItem.tsx` imports `useLayoutEffect` and `cn`; the clamp lives in a `UserBubble` local subcomponent (parent hook order unchanged).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → rendered chat bubble | `message.content` is user-authored text rendered into the DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-02-01 | Tampering / Info-disclosure (XSS) | `message.content` render in `UserBubble` | mitigate | Render as React text children (auto-escaped); never `dangerouslySetInnerHTML`. Preserves the existing user-bubble rule (the `<p>` already renders content as a text child today). |
| T-128-02-02 | DoS (render crash) | the `useLayoutEffect` overflow measure | accept | Pure DOM measurement (`scrollHeight`/`clientHeight`) with a ref guard; cannot throw on user content. jsdom returns 0/0 (no layout) → effect no-ops in tests, which is why tests assert structure + class (PATTERNS). |
</threat_model>

<verification>
- Long user prompt → clamp + fade + Read-more; short → unchanged; assistant branch untouched.
- The fade uses `from-[hsl(258_90%_66%)]` (bubble violet), not the page bg.
- `npx vitest run src/__tests__/components/MessageItem.clamp.test.tsx` passes.
- NOTE: live overflow behavior (the actual `scrollHeight > clientHeight` reveal) is covered by the D-06 manual UAT (Plan 05, long-message axis) — jsdom has no layout.
</verification>

<success_criteria>
CTC-04 satisfied: long user prompts collapse to a gradient-faded clamped preview + Read more; short prompts render exactly as before; the assistant branch is untouched.
</success_criteria>

<output>
Create `.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-02-SUMMARY.md` when done
</output>
