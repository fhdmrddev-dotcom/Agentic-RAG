---
phase: 128-chat-tool-card-unification-chat-area-reclaim
plan: 06
type: execute
wave: 5
depends_on: ['128-05']
files_modified: [frontend/src/components/chat/ChatArea.tsx]
autonomous: false
requirements: [CTC-03]

must_haves:
  truths:
    - "D-07: this is the LAST plan in the phase, explicitly GATED on the Plan 05 D-06 live cross-provider scoreboard PASSING — CTC-03 must not delete the StickyTimerBar until the unified card is proven to carry the run info uniformly on every provider"
    - "D-02: DELETE the StickyTimerBar component def (ChatArea.tsx:533-585, the 076.1 D-03 bar) + its mount (ChatArea.tsx:517-524) — pure subtraction, NO net-new wire; the existing showJumpToLive floating chip is kept untouched"
    - "D-02: after deletion the header RunStatusStrip (in-view, status home #1) + the MessageList floating '↓ Jump to live' chip (scroll-away, status home #2) remain the ONLY two status homes — both already exist and stay wired"
    - "The now-orphaned toolLabel import (ChatArea.tsx:22) is ALSO deleted — it was used ONLY inside StickyTimerBar (at :554); leaving it triggers a no-unused-vars build failure (research Pitfall 4)"
    - "Net-subtractive G-5 discipline: one of three redundant elapsed surfaces is removed, reclaiming chat-area vertical space; threads.py untouched; no shared-path fork"
  artifacts:
    - path: "frontend/src/components/chat/ChatArea.tsx"
      provides: "CTC-03: StickyTimerBar def + mount + the orphaned toolLabel import removed"
      contains: "showJumpToLive"
  key_links:
    - from: "frontend/src/components/chat/ChatArea.tsx"
      to: "the two surviving status homes (RunStatusStrip + showJumpToLive chip)"
      via: "deletion leaves both intact; {inputBar} renders immediately after the removed block"
      pattern: "showJumpToLive"
---

<objective>
Remove the redundant sticky elapsed timer above the composer — the `StickyTimerBar`
(`ChatArea.tsx`, the Phase 076.1 D-03 bar) — reclaiming chat-area vertical space
(CTC-03 / sketch 049-A / D-02). It is one of three redundant elapsed surfaces; the
tool card (proven uniform in Plan 05) + the floating chip make it superfluous.

**This is the LAST plan in Phase 128 (D-07), explicitly gated on the Plan 05 D-06 live
cross-provider scoreboard PASSING.** Build + prove CTC-01/CTC-02/TDP-02 first (Plans
01-04), prove the card carries the run info uniformly on every provider (Plan 05), THEN
delete. Do not run this plan if Plan 05 recorded a FAIL.

Purpose: pure subtraction (no net-new wire) that kills the "shows-for-some-providers,
vanishes-for-others" inconsistency by removing the redundant surface entirely, once the
canonical surface (the tool card) is proven complete.
Output: `ChatArea.tsx` loses the StickyTimerBar def, its mount, and the orphaned
`toolLabel` import; the build stays green; the two surviving status homes are intact.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The gate this plan depends on — Plan 05 must record PASS before this runs (D-07)
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-VALIDATION.md
# (the filled SC#10 scoreboard verdict — PASS gates this deletion)

# Sketch acceptance bar (G-2 — the clean removal direction)
@.planning/sketches/049-chat-area-reclaim/
@.planning/sketches/MANIFEST.md
# (Running Design Decision 39 — clean StickyTimerBar removal; sketch B rejected as imperceptible)

# Pattern map — the EXACT delete targets + the preserve-list + the orphaned-import gotcha
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md
# (§ "ChatArea.tsx (MOD) — CTC-03 StickyTimerBar DELETE" + § "Known Gotchas" → the toolLabel-orphan row)

# Research — Pitfall 4 (the toolLabel import orphan → build failure) + the two surviving status homes
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md
# (Pitfall 4, § State of the Art → three-elapsed-surfaces → two)

<interfaces>
<!-- Key facts the executor needs — extracted from the codebase. No exploration required. -->

DELETE TARGET 1 — the mount (ChatArea.tsx:517-524, the whole IIFE block):
```tsx
{/* Phase 076.1 D-03: Sticky elapsed timer above input box during active runs. */}
{isStreaming && (() => {
  const activeMsg = messages.findLast(m => m.role === "assistant")
  return activeMsg ? <StickyTimerBar message={activeMsg} /> : null
})()}
```
DELETE TARGET 2 — the component def (ChatArea.tsx:533-585, the entire `function StickyTimerBar({ message }: { message: Message }) { … }`). It is the ONLY caller of `toolLabel` in this file (at :554).

DELETE TARGET 3 — the now-orphaned import (ChatArea.tsx:22):
```tsx
import { toolLabel } from "@/lib/toolMeta"   // ← used ONLY inside StickyTimerBar → DELETE this whole line (Pitfall 4: no-unused-vars build failure otherwise)
```

PRESERVE (the two surviving status homes — D-02; both already wired):
  - The MessageList mount (~ChatArea.tsx:509-516) owns `showJumpToLive = !isPinned && isStreaming` (the floating "↓ Jump to live" chip — status home #2). KEEP.
  - The header RunStatusStrip (inside RunCard) — status home #1. KEEP.
  - `{inputBar}` (~ChatArea.tsx:525) renders immediately after the deleted mount block. KEEP.
  - Other imports on the lucide-react line (Folder as FolderIcon, Loader2, Menu, Sparkles) and `Message` from @/types remain used elsewhere — ONLY the `toolLabel` import line goes. Verify each other import is still referenced before removing anything beyond toolLabel.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: D-07 gate — confirm the Plan 05 scoreboard PASSED before deleting</name>
  <read_first>
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-VALIDATION.md (the recorded SC#10 scoreboard verdict from Plan 05)
  </read_first>
  <action>Operator-driven D-07 gate: confirm the Plan 05 D-06 scoreboard recorded PASS in 128-VALIDATION.md before the deletion proceeds. No code is written in this task — it is the blocking sequencing gate; if the verdict is FAIL, halt and route to gap closure.</action>
  <what-built>
    Nothing built — this is the D-07 sequencing gate. CTC-03 deletes the StickyTimerBar
    (a status surface). Per D-07 it may only proceed once the Plan 05 D-06 LIVE
    cross-provider scoreboard PASSED (the tool card proven to carry status/elapsed/step/file
    + description uniformly on all 8 providers). This checkpoint confirms that precondition.
  </what-built>
  <how-to-verify>
    1. Open `128-VALIDATION.md` and confirm the SC#10 scoreboard verdict is PASS (all 8 providers' cards carried the run info uniformly; the 4 axes passed).
    2. If the verdict is FAIL or incomplete, STOP — CTC-03 must not proceed; route the failure to gap closure first (D-07).
  </how-to-verify>
  <resume-signal>Type "approved — scoreboard passed" to proceed with the deletion, or "halt — scoreboard not passed" to stop.</resume-signal>
  <acceptance_criteria>
    The operator has confirmed the Plan 05 scoreboard recorded a PASS verdict in `128-VALIDATION.md`.
    If not passed, the deletion does not proceed (D-07 gate honored).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete StickyTimerBar (def + mount + orphaned toolLabel import)</name>
  <files>frontend/src/components/chat/ChatArea.tsx</files>
  <read_first>
    - frontend/src/components/chat/ChatArea.tsx:22 (the `toolLabel` import = DELETE TARGET 3), :509-525 (the MessageList mount + showJumpToLive to PRESERVE, the StickyTimerBar mount IIFE :517-524 to DELETE, `{inputBar}` :525 to PRESERVE), :533-585 (the StickyTimerBar def = DELETE TARGET 2; note `toolLabel` used at :554)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md § "ChatArea.tsx (MOD)" (the exact excerpts + the preserve-list)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md Pitfall 4 (delete the toolLabel import too)
  </read_first>
  <action>
    In `ChatArea.tsx` remove THREE things, nothing more: (1) the StickyTimerBar mount block — the `isStreaming` IIFE that renders `<StickyTimerBar message={activeMsg} />` at `:517-524` (DELETE TARGET 1); (2) the entire `function StickyTimerBar({ message }: { message: Message }) { … }` definition at `:533-585` (DELETE TARGET 2); (3) the now-orphaned `import { toolLabel } from "@/lib/toolMeta"` line at `:22` (DELETE TARGET 3 — `toolLabel` is used ONLY inside StickyTimerBar at `:554`; leaving it fails `tsc -b` on `no-unused-vars`, research Pitfall 4). This is pure subtraction — add NO new wire (D-02). PRESERVE the MessageList mount + its `showJumpToLive` floating chip (status home #2), the header RunStatusStrip (status home #1), and `{inputBar}` at `:525` (renders right after the removed mount). Before removing the import line, confirm `toolLabel` has no other reference in the file (it does not). Do NOT remove any other import — `Folder as FolderIcon`, `Loader2`, `Menu`, `Sparkles`, and `Message` from `@/types` remain used elsewhere; verify each is still referenced after the deletion. Do NOT touch `threads.py` or any other file (G-5; CTC-03 is frontend-subtractive).
  </action>
  <verify>
    <automated>cd frontend && test "$(grep -c StickyTimerBar src/components/chat/ChatArea.tsx)" = "0" && test "$(grep -c toolLabel src/components/chat/ChatArea.tsx)" = "0" && npx tsc -b --noEmit && echo "CTC-03 clean: StickyTimerBar+toolLabel removed, tsc green"</automated>
  </verify>
  <done>`StickyTimerBar` and the `toolLabel` import are gone from `ChatArea.tsx`; `tsc -b` is green (no unused-import error); the `showJumpToLive` floating chip + the header RunStatusStrip + `{inputBar}` are intact; no other file changed.</done>
  <acceptance_criteria>
    `grep -c StickyTimerBar src/components/chat/ChatArea.tsx` returns 0.
    `grep -c toolLabel src/components/chat/ChatArea.tsx` returns 0.
    `npx tsc -b --noEmit` exits 0 (no `no-unused-vars` failure).
    `grep -c showJumpToLive src/components/chat/ChatArea.tsx` is ≥ 1 (the floating chip survives).
    No file other than `ChatArea.tsx` is modified.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Regression check — the two surviving status homes still render</name>
  <files>frontend/src/components/chat/ChatArea.tsx</files>
  <read_first>
    - frontend/src/__tests__/components/ (the existing ChatArea / MessageList component tests, if any — run the chat-surface suite to confirm no regression)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-VALIDATION.md § "Per-Task Verification Map" CTC-03 row (header strip + floating chip still render after deletion)
  </read_first>
  <action>
    Run the frontend chat-surface vitest suite to confirm the deletion introduced no regression and the two surviving status homes (`RunStatusStrip` + the `showJumpToLive` floating chip) still render. Per `project_frontend_vitest_rot`, ~14-17 frontend tests fail at baseline AND HEAD — prove net-new green via the delta, NOT an absolute pass count: any test that was green before the deletion must still be green. No new StickyTimerBar test is required (none existed; the deletion is low-risk and covered structurally by the grep gates in Task 2 + the build gate). Do NOT add code; this task is verification only.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/ 2>&1 | tail -15</automated>
  </verify>
  <done>The chat-surface component suite shows no NEW failures versus baseline (rot-adjusted); the RunStatusStrip + floating chip render paths are unbroken.</done>
  <acceptance_criteria>
    `npx vitest run src/__tests__/components/` shows no failure that was not already failing at baseline (rot-adjusted delta).
    No source file changed in this task (verification only).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CTC-03 is a pure frontend deletion — it removes a redundant render surface; no trust boundary is crossed or added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-06-01 | Denial of Service (lost run-status visibility) | removing a status surface prematurely | mitigate | D-07 gate (Task 1): the deletion is blocked until the Plan 05 D-06 scoreboard PROVES the tool card carries the run info uniformly on all 8 providers; the two surviving status homes (RunStatusStrip + floating chip) are verified intact (Task 2/3). Sketch 049-B (promoted floating chip) is the documented fallback if users report lost always-visible status post-ship. |
| T-128-06-02 | Tampering (build break from orphaned import) | the `toolLabel` import after StickyTimerBar deletion | mitigate | Task 2 deletes the import line and gates on `tsc -b` green (Pitfall 4); the grep gate asserts `toolLabel` count is 0. |
</threat_model>

<verification>
- `StickyTimerBar` def + mount + the `toolLabel` import are removed from `ChatArea.tsx`; `tsc -b` green.
- The two surviving status homes (RunStatusStrip + `showJumpToLive` floating chip) + `{inputBar}` are intact.
- No file other than `ChatArea.tsx` changed (G-5: threads.py untouched).
- The deletion ran ONLY after the Plan 05 D-06 scoreboard PASSED (D-07 gate).
- The chat-surface vitest suite shows no new failures (rot-adjusted).
</verification>

<success_criteria>
CTC-03 satisfied: the redundant sticky composer timer is removed (gated on the proven-uniform card), chat-area space reclaimed, build green, two honest status homes preserved — and the phase's D-07 sequencing is honored.
</success_criteria>

<output>
Create `.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-06-SUMMARY.md` when done
</output>
