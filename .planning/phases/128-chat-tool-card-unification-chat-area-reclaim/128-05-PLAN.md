---
phase: 128-chat-tool-card-unification-chat-area-reclaim
plan: 05
type: execute
wave: 4
depends_on: ['128-02', '128-04']
files_modified: [.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-VALIDATION.md]
autonomous: false
requirements: [CTC-01, CTC-02, TDP-02, CTC-04]

must_haves:
  truths:
    - "D-06: the 'CTC-02 holds' acceptance gate is a LIVE SC#10 run across the FULL native-7 + OpenRouter (openai · anthropic · google · deepseek · moonshot · zhipu · minimax · openrouter) — NOT static screenshots, NOT big-4 only (mocks/static false-greened this surface before — Phase 122 caught 2 live bugs)"
    - "D-06: per provider, confirm the card carries {logo · status · elapsed · step/file counts · description-or-honest-fallback}; cross-check the run:{run_id} Redis stream for whether tool_args_progress.code_so_far contains 'description' during preparing (the same method the bug report used)"
    - "D-06 honest fallback verified: description shows only when present; Google delivers args atomically (~0 prep window → quiet 'Preparing {tool}…' is correct, not a bug); the card always renders logo+status regardless"
    - "SC#10 4-axis bandwidth all exercised LIVE: cross-provider (8) · multi-tool (2+ tools one prompt) · parallel-thread (A streaming while B accepts) · long-message (≥5KB user prompt → CTC-04 clamp engages AND ≥50-prior-message thread renders honestly)"
    - "This scoreboard MUST pass BEFORE the CTC-03 StickyTimerBar deletion (D-07) — it is the explicit gate Plan 06 depends on"
  artifacts:
    - path: ".planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-VALIDATION.md"
      provides: "The filled-in LIVE native-7+OR scoreboard results (per-provider verdict rows + the 4-axis sign-off)"
      contains: "scoreboard"
  key_links:
    - from: "the live chat UI (RunCard / ToolCallPanel)"
      to: "run:{run_id} Redis stream"
      via: "per-provider tool-using prompt + Chrome-MCP capture + Redis-stream + psycopg2 cross-check"
      pattern: "tool_args_progress"
---

<objective>
Run the **D-06 LIVE cross-provider scoreboard** — the acceptance gate that proves CTC-02
holds across the FULL native-7 + OpenRouter (NOT static, NOT big-4). This is the
mandatory SC#10 4-axis verification authored in `128-VALIDATION.md`; this plan EXECUTES
it (operator-driven) and records the per-provider verdicts back into VALIDATION.md.

It is the explicit **gate for D-07**: the CTC-03 `StickyTimerBar` deletion (Plan 06)
may NOT proceed until this scoreboard passes — the sticky timer can only be removed once
the tool card demonstrably carries the run info uniformly on every provider.

Purpose: mocks/static screenshots have false-greened this exact surface before (Phase 122
caught 2 live bugs that mocks masked). The honest proof that "the unified card is the
single complete surface on every provider" can only come from a live run.
Output: `128-VALIDATION.md` updated with the filled scoreboard + a pass/fail verdict.

> This plan does NOT rewrite VALIDATION.md — its scoreboard rows + 4-axis matrix are
> already authored. This plan EXECUTES those rows and records results.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The authored validation contract this plan EXECUTES (do NOT rewrite — fill in results)
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-VALIDATION.md
# (§ "Manual-Only Verifications" → the SC#10 4-axis scoreboard + the per-provider TDP-02 expectation)

# Research — the per-provider description-presence expectation + the Redis-stream method + Pitfall 2 (Google atomic)
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md
# (§ "Open Questions / verify-at-execution" #1, § "SC#10 4-Axis Coverage", Pitfall 2)

# Project UAT mandate
@CLAUDE.md
# (§ "UAT scoreboard recipe (MANDATORY)" — the 4-axis bandwidth; § "Cross-provider full native roster")

<interfaces>
<!-- Evidence-gathering facts the operator needs. -->

The full native-7 + OpenRouter provider set (D-06 — pick ONE representative model per provider):
  openai · anthropic · google · deepseek · moonshot · zhipu · minimax · openrouter

Per-provider TDP-02 expectation (research — verify live, do NOT assume):
  - Anthropic: streams args token-by-token (input_json_delta) → real prep window → description SHOWS.
  - OpenAI-compat path (openai/deepseek/moonshot/zhipu/minimax/openrouter): streams in deltas → real window → description SHOWS (per-model: whether `description` is emitted EARLY enough is the live unknown).
  - Google: ships function_call args ATOMICALLY in one chunk → ~0 prep window → the quiet `Preparing {tool}…` fallback is CORRECT (not a bug). Card still renders logo+status.

Evidence method (the same the bug report used — reference_evidence_tools_inventory + reference_local_dev_app):
  - Dev app: http://localhost:5173/  · login fhdmrd@gmail.com / 123456 · backend started by the OPERATOR (do not run uvicorn in background; ask + wait).
  - Per provider: send ONE tool-using prompt (e.g. that triggers execute_code or search_documents); WATCH the card live; capture a Chrome-MCP screenshot.
  - Cross-check the wire: inspect the `run:{run_id}` Redis stream — confirm whether `tool_args_progress.code_so_far` contains `"description"` DURING preparing (Redis local :6379 via docker; or psycopg2 :54322 for the messages/runs cross-check). Chrome MCP can HANG — fallback is operator-clicks + a one-line observation (do not keep pulling browser telemetry — feedback_ask_dont_pull_browser_telemetry).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator runs the LIVE native-7 + OpenRouter scoreboard (cross-provider axis)</name>
  <read_first>
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-VALIDATION.md § "Manual-Only Verifications" (the cross-provider scoreboard row + the per-provider TDP-02 expectation table — the authoritative rows to fill)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md Open-Question 1 (the Redis-stream cross-check method) + Pitfall 2 (Google atomic)
  </read_first>
  <action>Operator-driven: run a live tool-using prompt on each of the 8 providers (native-7 + OpenRouter) plus the 3 remaining SC#10 axes, capturing per-provider card completeness + the run:{run_id} Redis-stream `description`-during-preparing evidence. No code is written in this task — it executes the VALIDATION.md scoreboard live; Task 2 records the results.</action>
  <what-built>
    Plans 03 + 04 shipped: the shared `providerLogo.tsx` helper, the CTC-01 RunCard logo
    swap, and the TDP-02 ToolCallPanel preparing-window description. Plan 02 shipped the
    CTC-04 clamp. This checkpoint verifies — LIVE, on real streaming across all 8 providers —
    that the tool card is the single, complete, byte-identical run-info surface (CTC-02),
    so the redundant `StickyTimerBar` can be safely removed next (Plan 06 / D-07).
  </what-built>
  <how-to-verify>
    For EACH of the 8 providers (openai · anthropic · google · deepseek · moonshot · zhipu · minimax · openrouter), pick one representative model and:
    1. Send a tool-using prompt (one that triggers `execute_code` or `search_documents`). Watch the tool card during the prep gap and the run.
    2. Confirm the card carries: the CORRECT provider logo (or the honest Bot fallback for an unmapped local provider) · status · elapsed · step/file counts · the description-or-honest-`Preparing {tool}…`-fallback. The card must read byte-identically across providers — only the logo differs.
    3. Cross-check the wire: inspect the `run:{run_id}` Redis stream — note whether `tool_args_progress.code_so_far` contained `"description"` during preparing for that provider (records the WIN magnitude; Google is expected atomic → quiet fallback = correct).
    4. Record each provider's verdict (logo ✓/Bot-fallback · description shown / honest-fallback · card uniform) as a row.
    Then the THREE remaining SC#10 axes (live):
    5. Multi-tool: one prompt invoking 2+ tools (e.g. `search_documents` + `execute_code`) → the card stays uniform across the tool switch; the preparing-description updates per tool.
    6. Parallel-thread: Thread A streaming (card live, description showing) while Thread B accepts a new prompt → no cross-thread logo/description bleed.
    7. Long-message: paste a ≥5KB user prompt → CTC-04 clamp + Read-more engages (the fade dissolves into the violet bubble, not a panel cut); AND open a ≥50-prior-message thread → the card/strip still render honestly (no perf regression).
  </how-to-verify>
  <resume-signal>Type "approved" with the per-provider verdicts + the 4-axis results (or "fail: {provider/axis} — {what broke}" to halt and trigger gap closure before CTC-03).</resume-signal>
  <acceptance_criteria>
    The operator has run a LIVE tool-using prompt on ALL 8 providers (not a subset, not mocks/static) and recorded a per-provider verdict.
    All 4 SC#10 axes exercised live (cross-provider · multi-tool · parallel-thread · long-message).
    Each provider's card carried {logo-or-honest-fallback · status · elapsed · step/file · description-or-honest-fallback}; any provider where the card was incomplete or showed a WRONG logo / fabricated description is recorded as a FAIL (blocks CTC-03).
    Google showing the quiet `Preparing {tool}…` (atomic args, ~0 window) is recorded as PASS, not a failure.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record the scoreboard results into 128-VALIDATION.md</name>
  <read_first>
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-VALIDATION.md (the existing § "Manual-Only Verifications" + § "Validation Sign-Off" — fill these, do NOT rewrite the contract)
  </read_first>
  <action>
    Edit `128-VALIDATION.md`: under § "Manual-Only Verifications", append a filled per-provider scoreboard results table (one row per provider: logo verdict · description-shown-or-honest-fallback · card-uniform · the Redis `"description"`-during-preparing note) + the 4-axis results (multi-tool / parallel-thread / long-message: pass + one-line evidence). Tick the § "Validation Sign-Off" boxes that are now satisfied ("SC#10 4-axis scoreboard authored + operator-run (LIVE native-7 + OpenRouter)"). Set the overall scoreboard verdict (PASS gates D-07 → Plan 06 may proceed; FAIL → gap closure before CTC-03). Do NOT alter the Per-Task Verification Map or the Test Infrastructure sections (already authoritative). Do NOT touch `threads.py` or any source file — this plan is documentation of a live run only.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -iE "scoreboard|PASS|FAIL" .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-VALIDATION.md | head -5 && echo "results recorded"</automated>
  </verify>
  <done>`128-VALIDATION.md` carries the filled per-provider scoreboard + the 4-axis results + an overall PASS/FAIL verdict; the sign-off boxes for the SC#10 scoreboard are ticked.</done>
  <acceptance_criteria>
    `128-VALIDATION.md` contains a per-provider results table covering all 8 providers and an explicit overall verdict.
    The "SC#10 4-axis scoreboard authored + operator-run" sign-off box is checked.
    No source file changed (documentation-only plan).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live LLM providers → the chat UI | Real model streams cross the SSE/Redis boundary into the rendered card — the exact surface mocks have false-greened before. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-05-01 | Repudiation (false-green) | mocked/static cross-provider "proof" | mitigate | D-06 mandates a LIVE run across all 8 providers with a `run:{run_id}` Redis-stream cross-check (the bug-report method); static screenshots are explicitly insufficient (Phase 122 precedent). Recorded per-provider verdicts make the proof auditable. |
| T-128-05-02 | Info-disclosure (env/secrets in evidence) | provider API keys during the live run | accept | The operator starts the backend (keys in `backend/.env`, never pasted); evidence capture is name-only / screenshot of the UI + Redis stream — no secret material recorded (feedback_env_secrets_handling). |
</threat_model>

<verification>
- LIVE run across all 8 providers (not a subset, not mocks); per-provider card completeness recorded.
- All 4 SC#10 axes exercised live.
- `128-VALIDATION.md` carries the filled scoreboard + an overall PASS/FAIL verdict.
- PASS is the explicit precondition for Plan 06 (CTC-03 deletion, D-07).
</verification>

<success_criteria>
The D-06 live cross-provider scoreboard is run and recorded; CTC-02 is proven to hold across the full native-7 + OpenRouter (or failures are recorded and routed to gap closure before any deletion).
</success_criteria>

<output>
Create `.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-05-SUMMARY.md` when done
</output>
