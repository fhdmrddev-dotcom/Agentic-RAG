---
status: partial
phase: 063.1-frontend-stream-decoupling-gap-closure
source: [063.1-VERIFICATION.md, 063.1-01-SUMMARY.md, 063.1-02-SUMMARY.md, 063.1-03-SUMMARY.md, 063.1-04-SUMMARY.md]
started: 2026-05-04T22:00:00Z
updated: 2026-05-04T22:00:00Z
---

## Current Test

[live UAT pending — to be executed via Chrome DevTools MCP + local Supabase + Redis stack against the v2.5-dev branch with Plans 063.1-01..04 shipped]

Backend at http://localhost:8000 (Python venv, uvicorn single worker per CLAUDE.md), frontend at http://localhost:5173 (Vite dev server). Test login per project memory: `fhdmrd@gmail.com / 123456`.

## Tests

### 1. SC#7 Resume button — `ENABLE_TEST_FIXTURES=1` fixture-injected failed run

expected: With backend started with `ENABLE_TEST_FIXTURES=1`, the route `POST /test-fixtures/inject-failed-run/{tid}` is mounted. Hitting it creates a `runs` row with `status='failed'` and `message_id` pointing at an assistant message. After page reload, the assistant message renders with a Resume button (`aria-label="Resume failed run"`, lucide-react `RotateCcw` icon, label "Resume" — per Phase 063 Plan 04 `MessageItem.tsx:101-112`). Clicking it fires a fresh `POST /threads/{tid}/messages` with the same content as the user message immediately above the failed assistant message, returns 201, opens a fresh `GET /runs/{new_rid}/stream`, and new assistant content streams in.

result: [pending]

evidence:
- network reqids:
- screenshots:
- DB query (`SELECT run_id, status, message_id FROM runs WHERE thread_id = '...'`):

repro:
1. Stop backend if running. Set `ENABLE_TEST_FIXTURES=1` in env (export for shell, or in `.env` if backend reads it on startup). Restart backend.
2. Verify the test fixture endpoint exists:
   ```bash
   curl -X POST http://localhost:8000/test-fixtures/inject-failed-run/<any-thread-id> \
     -H "Authorization: Bearer $TOKEN"
   ```
   Expected: 200/201 (NOT 404). 404 means the env-gate didn't mount the route — diagnose before proceeding.
3. Pick an existing thread with at least one user→assistant exchange (or send one fresh).
4. POST to `/test-fixtures/inject-failed-run/{tid}` with the assistant `message_id` (or whatever shape the fixture endpoint accepts — see Phase 063 Plan 05 implementation).
5. F5 the page; navigate to that thread.
6. ASSERT: Resume button visible on the failed assistant message bubble (`aria-label="Resume failed run"` + `RotateCcw` icon + "Resume" text per Phase 063 Plan 04).
7. Click Resume.
8. ASSERT in DevTools Network:
   - A fresh `POST /threads/{tid}/messages` fires.
   - Response is 201 with new `run_id`.
   - A new `GET /runs/{new_rid}/stream?since=0` opens (SSE 200).
   - New assistant content streams into a new bubble.
9. ASSERT in DB: a new `runs` row exists with the new `run_id` and `status='streaming'` (or `completed` if the test-prompt finished quickly).

### 2. SC#5 cross-tab Stop — DELETE in tab A closes SSE in tab B

expected: Open the same thread in two tabs (call them A and B). Both tabs subscribe to the same in-flight run via SSE. Click Stop in tab A. Tab A's stream closes with "Response stopped" badge; tab B's stream ALSO closes within ~1s of the click (the Redis Stream `cancelled` terminal event broadcasts to all consumers per Phase 062 D-062-09 + D-062-13). Tab B's bubble shows the same "Response stopped" badge.

result: [pending]

evidence:
- tab A network reqids (POST /messages, DELETE /runs/{rid}, SSE close):
- tab B network reqids (initial GET /runs/{rid}/stream, SSE close):
- screenshots: tab A "Response stopped" badge; tab B same badge
- timing observation: tab B's SSE closed within ___ ms of tab A's DELETE click

repro:
1. In tab A, send a long-running prompt (e.g. `LONG_STREAM_PROMPT` from the e2e specs — "List 10 documents from the knowledge base, then for each one give a 30-word summary based on its content.").
2. While streaming, copy tab A's URL.
3. Open tab B in a new browser tab; paste the URL.
4. Verify both tabs show streaming bubbles. In DevTools Network on each tab, confirm `GET /runs/{rid}/stream` is open (status pending / time accumulating).
5. In tab A, click the Stop button.
6. ASSERT in tab A:
   - DevTools Network: `DELETE /runs/{rid}` → 204.
   - SSE GET stream shows finished/closed (no longer pending).
   - "Response stopped" badge rendered at the end of the assistant bubble.
   - Send button returns to non-streaming state.
7. ASSERT in tab B (within ~1s of step 6):
   - DevTools Network: SSE GET stream shows finished/closed.
   - Bubble renders the SAME "Response stopped" badge.
   - Final content matches tab A's content (cut off at the same token).

### 3. Re-runs of original 063 SC#1 through SC#6 against fixed code

Mirror `.planning/phases/063-frontend-stream-decoupling/063-HUMAN-UAT.md`'s scoreboard but with 063.1 fixes applied. Confirm all six original 063 success criteria still PASS post-fix. Each row carries its own `result: [pending]` slot below.

| SC | Description | Expected | Result | Evidence |
|----|-------------|----------|--------|----------|
| SC#1 | POST /threads/{id}/messages returns 201 JSON | 201 application/json with `{message_id, run_id}` (NOT text/event-stream) | [pending] | network reqid: |
| SC#2 | Reconcile triggers fire on visibilitychange / focus / pageshow(persisted=true); NO reconcile on pageshow(persisted=false) | 4 cases — one active-runs GET on each of the first three; zero on the last | [pending] | reqids: |
| SC#3 | Refresh mid-stream replay (now WITHOUT Gap-001 duplicate bubble) | F5 mid-stream → reconcile → SSE reattaches → ONE bubble at all times during replay (063.1-refresh-no-duplicate-bubble.spec.ts is the automated guard) | [pending] | reqids + bubble count screenshots: |
| SC#4 | Multi-tab fanout (now with offset cursor — verify since>0 on second tab subscribe) | Open same thread in two tabs; tab 2's GET /runs/{rid}/stream uses `since={ms-id}` not `since=0` once it has consumed any deltas (D-063.1-01..02) | [pending] | tab 2 stream URL query string: |
| SC#5 | Single-tab Stop (no regression) | DELETE /runs/{rid} → 204; SSE closes; "Response stopped" badge | [pending] | reqid: |
| SC#6 | No tool JSON in chat content (no regression) | Used-tool card renders structured params; assistant prose does NOT contain raw `{"name":"..."}` JSON | [pending] | screenshot: |

### 4. SECURITY — `ENABLE_TEST_FIXTURES` NOT mounted in production env

expected: When backend is started WITHOUT `ENABLE_TEST_FIXTURES=1`, `/test-fixtures/*` endpoints return 404 (route not registered at app startup time, NOT a runtime header check). This is the T-063.1-15 mitigation — Phase 063 Plan 05 added the gate; this plan VERIFIES it stays correct.

result: [pending]

evidence:
- backend startup log line confirming `ENABLE_TEST_FIXTURES` evaluation:
- curl response when var unset:
- curl response when `ENABLE_TEST_FIXTURES=1`:

repro:
1. Stop backend.
2. Unset `ENABLE_TEST_FIXTURES` env var (or set it to anything other than `"1"` — e.g. `unset ENABLE_TEST_FIXTURES` in bash, `Remove-Item Env:ENABLE_TEST_FIXTURES` in PowerShell).
3. Restart backend.
4. Run:
   ```bash
   curl -X POST http://localhost:8000/test-fixtures/inject-failed-run/00000000-0000-0000-0000-000000000000 \
     -H "Authorization: Bearer $TOKEN" -i
   ```
5. ASSERT: response is `404 Not Found` — the route does NOT exist in this env. Body should NOT contain test fixture details (no information disclosure).
6. Stop backend. Set `ENABLE_TEST_FIXTURES=1`. Restart.
7. Re-run the same curl.
8. ASSERT: response is now 200 / 201 / 422 (route IS registered; whatever validation error or success the fixture body produces is fine — the gate is the 404→non-404 transition).

## Summary

total: 4
passed: <fill after run>
issues: <fill — file as Gap-NNN entries below mirroring Phase 063's Gap-001..005 format>
pending: <fill>

## Gaps

[Any new gaps surfaced during this UAT run — file as Gap-006+ following Phase 063 numbering. Each entry: severity, discovered, repro, root cause, suggested fix.]

---

*Phase: 063.1-frontend-stream-decoupling-gap-closure*
*Scaffold created by 063.1-05 executor; evidence collection deferred to live UAT pass.*
