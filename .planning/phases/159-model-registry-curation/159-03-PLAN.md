---
phase: 159-model-registry-curation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/103_model_discovery_filter.sql
  - backend/app/models/user_settings.py
  - backend/app/api/settings.py
  - backend/app/main.py
  - supabase/full-schema.sql
autonomous: false
requirements: [MODEL-03]

must_haves:
  truths:
    - "D-159-04: the discovery filter is a persisted operator-governed app_settings knob (model_discovery_filter_enabled, default ON) that survives sessions + workers via the existing TTL settings cache"
    - "D-159-04: GET /settings surfaces model_discovery_filter_enabled so the Control Room shell can read the persisted default"
    - "SC#1: the filter default is served from app_settings; before the migration is applied the readback is fail-soft (absent column → default true, no crash)"
    - "migration 103 is applied via the Supabase SQL editor (NEVER db push/reset), then full-schema.sql is regenerated with no reset"
  artifacts:
    - path: "supabase/migrations/103_model_discovery_filter.sql"
      provides: "idempotent self-seeding ADD COLUMN app_settings.model_discovery_filter_enabled boolean NOT NULL DEFAULT true"
      contains: "model_discovery_filter_enabled"
    - path: "backend/app/models/user_settings.py"
      provides: "Settings field + fail-soft _val_bool readback (default true)"
      contains: "model_discovery_filter_enabled"
    - path: "backend/app/api/settings.py"
      provides: "GET /settings response field + serialization"
      contains: "model_discovery_filter_enabled"
    - path: "backend/app/main.py"
      provides: "_DIRECT_COLUMNS entry — documentation-completeness for the legacy settings_override.json migration path (Phase-147 precedent), NOT write-path SQLi safety"
      contains: "model_discovery_filter_enabled"
  key_links:
    - from: "backend/app/api/settings.py get_settings"
      to: "UserEffectiveSettings.model_discovery_filter_enabled"
      via: "_build_settings_from_row readback"
      pattern: "model_discovery_filter_enabled"
    - from: "save_app_settings"
      to: "app_settings.model_discovery_filter_enabled"
      via: "_FLAG_KEYS + _VALID_COLUMN_NAME parameterized write (the SQLi-safe path); _DIRECT_COLUMNS is the legacy-migration allowlist, not this write route"
      pattern: "_DIRECT_COLUMNS"
---

<objective>
Persist the discovery-filter default as an operator-governed `app_settings` knob
(`model_discovery_filter_enabled`, default ON) and wire the full readback chain so the
Control Room shell can read it and `save_app_settings` (via the Plan-02 flag key) can write it
SQLi-safely (D-159-04 persistence half). Authors migration 103 (NOT applied by Claude) and
gates its application on the operator (Supabase SQL editor — never `db push`/`reset`).

Purpose: give the filter a durable, multi-worker home (rides the existing 30s TTL settings
cache) so the operator's "filter on by default" preference survives sessions + WORKER_COUNT=2.
Output: migration 103 + the Settings field + settings.py response + `_DIRECT_COLUMNS` entry +
the operator-applied schema regen.

Note: this plan is `autonomous: false` — Task 2 is an operator-gated migration apply. All code
is fail-soft: a settings load with the column ABSENT returns the safe default `true` and never
raises, so the code is safe to land before the operator applies the migration.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-model-registry-curation/159-CONTEXT.md
@.planning/phases/159-model-registry-curation/159-PATTERNS.md
@supabase/migrations/099_model_registry_deprecated.sql
@backend/app/models/user_settings.py
@backend/app/api/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 103 + wire the app_settings readback chain (fail-soft)</name>
  <files>supabase/migrations/103_model_discovery_filter.sql, backend/app/models/user_settings.py, backend/app/api/settings.py, backend/app/main.py, backend/tests/test_159_filter_setting.py</files>
  <read_first>
    - supabase/migrations/099_model_registry_deprecated.sql — the ADD COLUMN app_settings template + the verbatim apply/cloud-parity header comments to copy.
    - backend/app/models/user_settings.py — the file being modified. Read L144-162 (the `setup_complete: bool = False` app_settings-only field — the exact analog, but default TRUE here), L716-735 (`_val_bool(row, ..., None, <default>)` readback analog), L247-330 (`_SETTINGS_CACHE_TTL`, `_load_settings_from_db`, `save_app_settings`, `invalidate_settings_cache` — the cache the toggle rides; note `_VALID_COLUMN_NAME` at L313, the regex that actually keeps the write SQLi-safe).
    - backend/app/api/settings.py — the file being modified. Read L60-75 (the response-model flag fields `self_improve_enabled`/`maintenance_mode`) and L225-234 (their serialization `= s.<field>`) — the exact two-line pattern to mirror.
    - backend/app/main.py — the file being modified. Read L97-116 (`_DIRECT_COLUMNS` — the code-constant column allowlist consumed ONLY by `_migrate_settings_override`, L133/L171, the one-shot legacy settings_override.json→DB migration; note the mig 097/099 flag-column additions were the same documentation-completeness move).
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §7 + §8, and CLAUDE.md migration rules (SQL editor apply, regenerate-full-schema.sh, cloud parity).
  </read_first>
  <action>
    (a) Create `supabase/migrations/103_model_discovery_filter.sql` — copy 099's header comment block
    (APPLY via LOCAL Supabase SQL editor, idempotent, NEVER `db push`/`reset`; THEN `bash scripts/regenerate-full-schema.sh`
    no `--reset`; CLOUD PARITY deferred to the standing push checklist). Body:
    `ALTER TABLE public.app_settings ADD COLUMN IF NOT EXISTS model_discovery_filter_enabled boolean NOT NULL DEFAULT true;`
    A `DEFAULT true` column is self-seeding (default-ON per D-159-04) — no seed row. DO NOT apply it (Task 2).
    (b) `user_settings.py`: add `model_discovery_filter_enabled: bool = True` to `UserEffectiveSettings`
    as an app_settings-only field (env_attr=None readback, mirroring `setup_complete`); add the readback
    line in `_build_settings_from_row`: `model_discovery_filter_enabled=_val_bool(row, "model_discovery_filter_enabled", None, True)`
    (fail-soft: absent/None column → True).
    (c) `settings.py`: add `model_discovery_filter_enabled: bool` to the GET /settings response model
    (beside the FLAG-01 fields) and `model_discovery_filter_enabled=s.model_discovery_filter_enabled` to
    its serialization.
    (d) `main.py`: add `"model_discovery_filter_enabled"` to `_DIRECT_COLUMNS` for documentation-completeness
    of the legacy `settings_override.json`→DB migration path (`_migrate_settings_override`, main.py L171 — the
    ONLY consumer of `_DIRECT_COLUMNS`; the Phase-147 precedent added the other flag columns here for exactly
    this reason). This is NOT what makes the write SQLi-safe: write-path safety is independently provided by
    `_FLAG_KEYS` + `save_app_settings`'s `_VALID_COLUMN_NAME` regex (user_settings.py L313). The addition is
    harmless and consistent with the shipped flags — keep it, but for the correct reason.
    Create `backend/tests/test_159_filter_setting.py` asserting the fail-soft default + the _DIRECT_COLUMNS membership.
  </action>
  <acceptance_criteria>
    - `supabase/migrations/103_model_discovery_filter.sql` exists, is idempotent (`ADD COLUMN IF NOT EXISTS`), `DEFAULT true`, and carries the 099-style APPLY/CLOUD-PARITY header.
    - `UserEffectiveSettings` has `model_discovery_filter_enabled: bool = True`; `_build_settings_from_row` reads it via `_val_bool(row, "model_discovery_filter_enabled", None, True)`.
    - the GET /settings response model + serialization include `model_discovery_filter_enabled`.
    - `"model_discovery_filter_enabled"` is in `main._DIRECT_COLUMNS` (for the legacy-migration allowlist; the write path's SQLi safety is `_FLAG_KEYS` + `_VALID_COLUMN_NAME`, not this).
    - with the column ABSENT (row dict lacks the key), building settings returns `model_discovery_filter_enabled=True` and raises no exception (fail-soft).
    - `backend/venv/Scripts/python.exe -c "import app.main, app.api.settings, app.models.user_settings"` imports clean.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_159_filter_setting.py -x && venv/Scripts/python.exe -c "import app.main, app.api.settings, app.models.user_settings"</automated>
  </verify>
  <done>Migration authored (not applied); Settings field → settings.py response wired; the `_DIRECT_COLUMNS` legacy-migration entry added (NOT the write route — the write is SQLi-safe via `_FLAG_KEYS`/`save_app_settings`); fail-soft default-true before apply.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [OPERATOR] Apply migration 103 + regenerate full-schema.sql</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - supabase/migrations/103_model_discovery_filter.sql (authored in Task 1) — the SQL the operator pastes.
    - CLAUDE.md — the migration discipline (SQL editor apply; `bash scripts/regenerate-full-schema.sh` no `--reset`; commit both; cloud parity at promotion).
  </read_first>
  <action>
    Migration 103 was authored in Task 1 but NOT applied. The column must now exist in the live local DB
    for the filter toggle to persist. This is an operator-gated pause — do NOT auto-apply the migration.
    1. OPERATOR: open the LOCAL Supabase SQL editor, paste the FULL contents of
       `supabase/migrations/103_model_discovery_filter.sql`, and run it (idempotent; NEVER `supabase db push` / `db reset`).
    2. CLAUDE (after the operator confirms): run `bash scripts/regenerate-full-schema.sh` (no `--reset` — a
       live-DB dump that preserves data) to rebuild `supabase/full-schema.sql`; commit the migration +
       regenerated schema together.
    CLOUD PARITY (deferred to the standing production-push checklist): paste the same SQL into the CLOUD
    Supabase SQL editor at the next promotion — migs 099/100/101/102 are already pending.
  </action>
  <verify>
    <automated>grep -c model_discovery_filter_enabled supabase/full-schema.sql</automated>
    Post-apply manual check: `GET /settings` returns `model_discovery_filter_enabled: true`.
  </verify>
  <acceptance_criteria>
    - the local `app_settings` table has the `model_discovery_filter_enabled` column (operator-confirmed).
    - `supabase/full-schema.sql` contains `model_discovery_filter_enabled boolean` on app_settings after regen.
    - `GET /settings` returns `model_discovery_filter_enabled: true` post-apply.
  </acceptance_criteria>
  <resume-signal>Operator types "applied" (or reports the SQL editor result).</resume-signal>
  <done>Migration applied locally via the SQL editor; full-schema.sql regenerated + committed; cloud-parity noted for the standing checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → local/cloud Postgres | static DDL applied by the operator via the SQL editor |
| app_settings row → settings readback | a possibly-absent column is read on every settings load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-06 | Tampering | migration 103 DDL | mitigate | static `ADD COLUMN IF NOT EXISTS` — no client input, idempotent, self-seeding `DEFAULT true`. No new RLS needed: mig 053 already ships read-all on the model tables and `app_settings` writes are service-role only. Acceptance: migration re-runnable without error. |
| T-159-07 | DoS/Availability | settings readback pre-migration | mitigate | `_val_bool(row, ..., None, True)` returns the safe default when the column is absent (mig not yet applied) — the code is safe to ship ahead of the apply and never raises (the mig-102 `setup_complete` fail-soft precedent). Acceptance: absent-column settings load returns true, no exception. |
| T-159-SC | Tampering (supply chain) | package installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/test_159_filter_setting.py -x` — fail-soft readback + _DIRECT_COLUMNS membership pass.
- Import smoke: `venv/Scripts/python.exe -c "import app.main, app.api.settings, app.models.user_settings"` clean.
- Post-apply: `GET /settings` returns `model_discovery_filter_enabled: true`; `grep -c model_discovery_filter_enabled supabase/full-schema.sql` ≥ 1.
</verification>

<success_criteria>
- The discovery filter is a persisted, operator-governed, multi-worker app_settings knob defaulting ON (D-159-04, SC#1).
- Fail-soft: the code ships safely ahead of the operator-applied migration.
- Migration applied via the SQL editor + full-schema regenerated (never db push/reset).
</success_criteria>

<output>
Create `.planning/phases/159-model-registry-curation/159-03-SUMMARY.md` when done.
</output>
</content>
