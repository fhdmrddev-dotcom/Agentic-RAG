---
phase: 159-model-registry-curation
plan: 05
type: execute
wave: 3
depends_on: [159-04]
files_modified:
  - frontend/src/components/admin/ModelRegistryTab.tsx
  - frontend/src/components/admin/ControlRoomPage.tsx
autonomous: true
requirements: [MODEL-03]

must_haves:
  truths:
    - "D-159-02: ModelRegistryTab has a '+ Add model by ID' form (070-A idiom) taking model_id + provider (8-cloud roster) + the 3 capability knobs; submit calls onAddModel → addModelById → the row appears disabled after the shell re-fetches"
    - "D-159-03: the 3 capability inputs pre-fill from familyDefaults with three visually-distinct sources (default — confirm / operator-typed / blank), following the SourceTag visual idiom"
    - "D-159-02: the form NEVER sends enabled (server forces false); the copy states the model lands disabled and is enabled from the table"
    - "SC#2: an operator can add a single model by ID with its capabilities set + editable per-family defaults pre-filled"
  artifacts:
    - path: "frontend/src/components/admin/ModelRegistryTab.tsx"
      provides: "onAddModel prop + the '+ Add model by ID' form (direct-submit + ✎ receipt + in-form refusal)"
      contains: "onAddModel"
    - path: "frontend/src/components/admin/ControlRoomPage.tsx"
      provides: "handleAddModel (addModelById → pulseRecording → fetchRegistry) wired to ModelRegistryTab"
      contains: "handleAddModel"
  key_links:
    - from: "ModelRegistryTab '+ Add model by ID' form"
      to: "ControlRoomPage.handleAddModel"
      via: "onAddModel prop"
      pattern: "onAddModel"
    - from: "ControlRoomPage.handleAddModel"
      to: "addModelById"
      via: "server-is-source-of-truth re-fetch"
      pattern: "addModelById"
    - from: "the form capability inputs"
      to: "familyDefaults"
      via: "source-labeled pre-fill"
      pattern: "familyDefaults"
---

<objective>
Ship the operator-facing add-by-ID vertical: a "+ Add model by ID" form in `ModelRegistryTab`
(the 070-A instrument-table idiom) that pre-fills the 3 capability knobs from `familyDefaults`
with source-honest labels (D-159-02 + D-159-03), plus the `ControlRoomPage` `handleAddModel`
wiring that calls `addModelById` and re-fetches the registry so the new (disabled) row appears.
This delivers SC#2 end-to-end.

Purpose: generalize the SEED-088 GPT-5.6 hand-add into a first-class UI path ("Kimi K3 dropped —
add it with the right context/output/tools").
Output: the form + prop + the shell handler.

Security invariants (client caller; the server write surface + its threat model are Plan 02):
- The client adds NO authority — `POST /admin/models` is 404-gated server-side.
- `enabled` is NEVER sent — the server forces `enabled=false` (149 opt-in / SC#3).
- Graded-guard (sketch 066): add-by-ID is reversible-no-victim → direct submit + ✎ receipt (no arm-to-confirm).
- The form surfaces the server's 409/422 refusal in-form — never a silent failure.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-model-registry-curation/159-CONTEXT.md
@.planning/phases/159-model-registry-curation/159-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/admin/ModelRegistryTab.tsx
@frontend/src/components/admin/ControlRoomPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: "+ Add model by ID" form in ModelRegistryTab (070-A idiom, source-labeled pre-fill)</name>
  <files>frontend/src/components/admin/ModelRegistryTab.tsx, frontend/src/components/admin/ModelRegistryTab.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/ModelRegistryTab.tsx — the file being modified. Read L1-51 (the pure-leaf doctrine + `ModelRegistryTabProps`), L88-203 (the section/header render where the "+ Add" affordance goes), L240-275 (`ModelRow.write` — the busy → await → ✎ receipt 3500ms → catch ApiError → in-row refusal chokepoint to mirror), L392-505 (`NumericCell` inline number input + `SourceTag` OVR/DEF badge to extend to three sources), L678-736 (`RowToggle` for native_tools), L130/L145 (`providerLogo(provider)` usage).
    - frontend/src/lib/api.ts — `AddModelBody`, `addModelById`, `ApiError` (Plan 04).
    - frontend/src/lib/model-defaults.ts — `familyDefaults` (Plan 04).
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md L296-336 — the 070-A instrument-table idiom + the graded-guard (066) rule (reversible-no-victim → direct flip) + the ✎ ledger-receipt vocabulary.
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §9.
  </read_first>
  <action>
    Add `onAddModel: (body: AddModelBody) => Promise<void>` to `ModelRegistryTabProps` (import
    `AddModelBody` from `@/lib/api`, `familyDefaults` from `@/lib/model-defaults`). Add a header
    "+ Add model by ID" button above the provider-grouped `<section>` that toggles an inline form
    panel (button-revealed; direct-submit per the graded-guard rule — no arm-to-confirm). Fields:
    `model_id` (`<input type="text">` mono), `provider` (`<select>` over the 8-cloud roster
    ["openai","anthropic","google","deepseek","moonshot","zhipu","minimax","openrouter"], each option
    labeled with `providerLogo(provider)`), `context_window_tokens` + `max_output_tokens` (number inputs),
    `native_tools` (RowToggle or select), and an optional deprecated note. On `provider` or `model_id`
    change, pre-fill the 3 capability inputs from `familyDefaults(model_id || provider)`. Render each
    capability with a THREE-way source label following the SourceTag visual idiom (mono badge, tone-colored):
    amber "default — confirm" when the value is the untouched family suggestion, primary "you set it" once
    the operator edits it (track a touched-set or compare to the default), and a blank input (no badge / a
    plain "set…" placeholder) when `familyDefaults` returned null. Submit via a write chokepoint mirroring
    `ModelRow.write`: busy → `await onAddModel({model_id, provider, context_window_tokens, max_output_tokens, native_tools, ...})`
    → `✎ recorded` flash 3500ms → clear+collapse the form; on `ApiError` show the server `detail` in-form
    (e.g. the 409 "already in the registry" refusal) — never crash, never silent. NEVER include `enabled`
    in the body. Form copy states: "Added disabled — enable it from the table." Keep the existing table
    byte-identical (loading placeholder, empty-state, `groupByProvider`, `ModelRow`, `SourceTag`, `NumericCell`,
    `RowToggle`, `LockControl` all unchanged). Extend `ModelRegistryTab.test.tsx`.
  </action>
  <acceptance_criteria>
    - opening "+ Add model by ID" shows `model_id` + a `provider` `<select>` with 8 roster options (each with a logo) + the 3 capability inputs.
    - selecting provider "moonshot" (or typing "kimi-k3") pre-fills context/maxOutput/tools from `familyDefaults`, each labeled amber "default — confirm"; editing a field flips its label to the operator-typed style; a family with a null default shows a blank input (no false value).
    - submitting calls `onAddModel` exactly once with `{model_id, provider, context_window_tokens, max_output_tokens, native_tools}` and NO `enabled` key.
    - a rejected submit (`ApiError`) renders the server `detail` in-form (assertable text); no crash, no silent success.
    - the form copy states the model is added disabled and enabled from the table.
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/admin/ModelRegistryTab.tsx` = 0; the existing ModelRegistryTab.test.tsx cases still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/ModelRegistryTab.test.tsx src/components/admin/ModelRegistryTab.a11y.test.tsx</automated>
  </verify>
  <done>The add-by-ID form exists in the 070-A idiom with source-labeled family pre-fills, direct-submit + ✎ receipt, honest in-form refusal, lands-disabled copy; the table is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: ControlRoomPage handleAddModel wiring (server-is-source-of-truth re-fetch)</name>
  <files>frontend/src/components/admin/ControlRoomPage.tsx</files>
  <read_first>
    - frontend/src/components/admin/ControlRoomPage.tsx — the file being modified. Read L36-86 (imports — where to add `addModelById`), L280-291 (`fetchRegistry` — alive-guard + honest-degrade), L500-535 (`handleSetCapability`/`handleConfirmDiscovery` — the write→pulseRecording→re-fetch, errors-RE-THROW pattern to mirror), L744-761 (the leaf wiring where `<ModelRegistryTab .../>` gets its props — leave the `<ModelDiscoveryPanel>` props for Plan 06).
    - frontend/src/lib/api.ts — `addModelById`, `AddModelBody` (Plan 04).
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §11.
  </read_first>
  <action>
    Import `addModelById` (and `AddModelBody` if needed) from `@/lib/api`. Add
    `const handleAddModel = useCallback(async (body: AddModelBody) => { await addModelById(body); pulseRecording(); if (alive.current) void fetchRegistry() }, [fetchRegistry, pulseRecording])`
    — mirror `handleSetCapability`: server is the source of truth (re-fetch, no optimistic add); do NOT
    swallow errors (let them propagate so the form surfaces the refusal). Pass `onAddModel={handleAddModel}`
    to `<ModelRegistryTab ... />`. Leave the existing `onSetCapability`/`onLock`/`showTechnical` props and
    the `<ModelDiscoveryPanel>` wiring untouched (Plan 06 extends the latter).
  </action>
  <acceptance_criteria>
    - ControlRoomPage imports `addModelById` and defines `handleAddModel` calling `addModelById` → `pulseRecording()` → `fetchRegistry()`.
    - `handleAddModel` does NOT catch/swallow the error (it re-throws so the form shows the refusal).
    - `<ModelRegistryTab>` receives `onAddModel={handleAddModel}`; its existing props are intact.
    - `npx tsc -b` reports 0 net-new errors; `ControlRoomPage.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | tail -5 && npx vitest run src/components/admin/ControlRoomPage.test.tsx</automated>
  </verify>
  <done>The shell wires add-by-ID end-to-end: form → addModelById → registry re-fetch → the new disabled row appears (no optimistic flip).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser (add-by-ID form) → /admin API | the form collects model_id/provider/caps and submits to the Plan-02 write surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-08 | Elevation | the add form + handleAddModel | mitigate | client adds NO authority — the real wall is the Plan-02 server (`require_operator` byte-identical 404 + roster/type/duplicate validation + parameterized upsert). The form only collects + submits. Acceptance: no client-side auth/gate assumed; errors surface from the server. |
| T-159-09 | Elevation (never-auto-enable) | the add form | mitigate | the body NEVER carries `enabled`; the server forces `enabled=false`. The lands-disabled copy makes the opt-in-enable rule visible. Acceptance: submitted body has no `enabled` key. |
| T-159-10 | Repudiation / honesty | the write chokepoint | mitigate | success → ✎ recorded flash (the ledger receipt lands via the shell's `pulseRecording` + audit); an `ApiError` renders the server detail in-form (never a silent fail). Acceptance: rejected submit shows the detail. |
| T-159-SC | Tampering (supply chain) | package installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/admin/ModelRegistryTab.test.tsx src/components/admin/ModelRegistryTab.a11y.test.tsx src/components/admin/ControlRoomPage.test.tsx` — form + a11y + shell tests pass.
- `npx tsc -b` — 0 net-new errors; `npm run build` exit 0.
- Live UAT (phase verification, Chrome MCP): open Control Room → Model Registry → "+ Add model by ID" → add `kimi-k3`/moonshot with pre-filled defaults → confirm it appears as a DISABLED row → enable it from the table.
</verification>

<success_criteria>
- An operator can add a single model by ID with its capabilities + editable per-family defaults pre-filled, source-honest, landing disabled (D-159-02, D-159-03, SC#2).
- The write never auto-enables and surfaces refusals honestly (SC#3).
</success_criteria>

<output>
Create `.planning/phases/159-model-registry-curation/159-05-SUMMARY.md` when done.
</output>
