---
phase: 159-model-registry-curation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/admin.py
autonomous: true
requirements: [MODEL-03]

must_haves:
  truths:
    - "D-159-02: POST /admin/models adds a single model by explicit id + provider; the row lands enabled=false and is immediately served by get_model_capability_async (no restart)"
    - "D-159-02: the write reuses the SQLi-safe parameterized column-allowlist upsert from set_model_capability — no client identifier/value reaches a SET clause"
    - "D-159-04: the persisted filter toggle rides the existing set_flag path via ONE new _FLAG_HUMAN_NAMES key (zero new endpoint code)"
    - "SC#3: add-by-ID never auto-enables — enabled is forced false server-side, never taken from the body"
  artifacts:
    - path: "backend/app/api/admin.py"
      provides: "POST /admin/models add-by-ID route (operator-gated, roster+type+duplicate validated, ✎ model.added receipt) + the model_discovery_filter_enabled flag key"
      contains: "async def add_model_by_id"
    - path: "backend/tests/test_159_add_model.py"
      provides: "happy-path + bogus-provider + duplicate + enabled-forced-false + non-operator-404 coverage"
  key_links:
    - from: "backend/app/api/admin.py:add_model_by_id"
      to: "model_capabilities_overrides"
      via: "parameterized asyncpg upsert (deps._pg_pool)"
      pattern: "INSERT INTO model_capabilities_overrides"
    - from: "backend/app/api/admin.py:add_model_by_id"
      to: "invalidate_model_overrides_cache"
      via: "SC#1 next-request visibility"
      pattern: "invalidate_model_overrides_cache"
    - from: "backend/app/api/admin.py:_FLAG_HUMAN_NAMES"
      to: "save_app_settings"
      via: "PUT /admin/flags model_discovery_filter_enabled"
      pattern: "model_discovery_filter_enabled"
---

<objective>
Add the two new operator-gated backend WRITE surfaces this phase needs: (1) a dedicated
`POST /admin/models` add-by-ID endpoint that writes a DB-only `model_capabilities_overrides`
row landing `enabled=false` (D-159-02), reusing the exact SQLi-safe upsert already proven in
`set_model_capability`; and (2) the one-line `_FLAG_HUMAN_NAMES` key that lets the persisted
discovery-filter toggle ride the existing `PUT /admin/flags` path with zero new endpoint code
(D-159-04 write half).

Purpose: give the frontend (Plans 04-06) a real backend contract to call — the add-by-ID form
and the filter toggle both land here.
Output: `add_model_by_id` route + `AddModelRequest` model + the flag key.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-model-registry-curation/159-CONTEXT.md
@.planning/phases/159-model-registry-curation/159-PATTERNS.md
@backend/app/api/admin.py

<interfaces>
<!-- The exact upsert + allowlist discipline to REUSE (from admin.py set_model_capability, verified). -->
<!-- Column names come ONLY from a code-constant allowlist; values are $N binds. Provider must be EXPLICIT for add-by-ID. -->

set_model_capability upsert (admin.py ~L1202-1210):
  insert_cols = ["model_id", "provider", *present_cols]
  placeholders = ", ".join(f"${i+1}" for i in range(len(insert_cols)))
  set_clause   = ", ".join(f"{c} = EXCLUDED.{c}" for c in present_cols)
  sql = INSERT INTO model_capabilities_overrides (...) VALUES (...) ON CONFLICT (model_id) DO UPDATE SET ..., updated_at = now()
  values = [model_id, provider, *[body[c] for c in present_cols]]  ; pool = deps._pg_pool

Writable capability columns allowlist (admin.py L104-125):
  _MODEL_CAP_COLUMNS = {llm_call_timeout_seconds, context_window_tokens, max_output_tokens, native_tools, enabled, deprecated, deprecated_reason}
  _MODEL_CAP_INT_COLUMNS = {llm_call_timeout_seconds, context_window_tokens, max_output_tokens}
  _MODEL_CAP_BOOL_COLUMNS = {native_tools, enabled, deprecated}

Provider roster (model_discovery_service.PROVIDER_ENDPOINTS keys = native-7 + openrouter):
  openai, anthropic, google, deepseek, moonshot, zhipu, minimax, openrouter

Flag allowlist (admin.py L67-74): _FLAG_HUMAN_NAMES keyed by human name; _FLAG_KEYS = set(_FLAG_HUMAN_NAMES)
Audit receipt: request.state.audit_action / audit_label are FREE-TEXT (no registration needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /admin/models add-by-ID endpoint (DB-only row, lands disabled)</name>
  <files>backend/app/api/admin.py, backend/tests/test_159_add_model.py</files>
  <read_first>
    - backend/app/api/admin.py — the file being modified. Read L99-132 (`_MODEL_CAP_COLUMNS`/`_MODEL_CAP_INT_COLUMNS`/`_MODEL_CAP_BOOL_COLUMNS` + the router with `require_operator`), L1081-1235 (`set_model_capability` — the EXACT upsert, type guards, honest-failure/500, `invalidate_model_overrides_cache`, `✎` receipt to mirror), L1041-1078 (`get_model_registry` — how the union is built, for the duplicate check), L1352-1432 (`run_model_discovery` — the `set(PROVIDER_ENDPOINTS)` roster-allowlist-before-touch pattern).
    - backend/app/services/model_discovery_service.py L49-84 — `PROVIDER_ENDPOINTS` (the roster to validate `provider` against).
    - backend/app/config.py L515-537 + L686-718 — `get_model_capability` / `get_model_capability_async` (the read path the new row rides — proves ZERO read-path edits needed).
    - backend/app/models/user_settings.py L440-468 — `load_all_model_overrides` + `invalidate_model_overrides_cache` (duplicate check source + the cache to invalidate).
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §4 (the KEY NUANCE: add-by-ID is a NEW POST route because the PATCH infers provider and its allowlist excludes provider).
  </read_first>
  <action>
    Add `class AddModelRequest(BaseModel)` with: `model_id: str`, `provider: str`,
    `context_window_tokens: int | None = None`, `max_output_tokens: int | None = None`,
    `native_tools: bool | None = None`, `deprecated: bool = False`, `deprecated_reason: str | None = None`.
    (No `enabled` field — it is forced false server-side.)
    Add `@router.post("/models")` `async def add_model_by_id(request: Request, body: AddModelRequest, _floor: None = Depends(operator_audit_floor))`.
    Validate BEFORE any DB touch (mirror the allowlist-before-touch discipline):
      (1) `model_id` non-empty after `.strip("/ \t\r\n")` else 422 "model_id must be non-empty.";
      (2) function-local import `from app.services.model_discovery_service import PROVIDER_ENDPOINTS`; `body.provider` must be in `set(PROVIDER_ENDPOINTS)` else 422 "Unknown provider: {provider}";
      (3) integer caps: each of context_window_tokens/max_output_tokens, when not None, `isinstance(v, int) and not isinstance(v, bool)` else 422; native_tools when not None must be bool else 422 (reuse the `_MODEL_CAP_INT_COLUMNS`/`_MODEL_CAP_BOOL_COLUMNS` type discipline);
      (4) DUPLICATE guard: if `model_id in MODEL_CAPABILITIES` or `model_id in (await load_all_model_overrides())` → 409 "That model is already in the registry — edit it in the table instead." (never clobber an existing row).
    Then build the upsert EXACTLY like set_model_capability: `present_cols` = the non-None capability columns among {context_window_tokens, max_output_tokens, native_tools, deprecated, deprecated_reason} (code allowlist) PLUS always `enabled` set to the literal `False`. `insert_cols = ["model_id", "provider", *present_cols]`; `$N` placeholders; `set_clause` from present_cols; `values = [body.model_id, body.provider, *[<the forced/body value per col>]]` (enabled's value is the literal False, never `body.*`). Column names ONLY from the code allowlist; values ONLY as `$N` binds. Execute on `deps._pg_pool` (live attribute) in a try/except; on failure stamp `request.state.audit_action = "model.add_failed"` + `audit_label = f"Adding {body.model_id} failed to persist"` and raise 500 "Could not persist the new model — it was not added." (never a false 2xx). On success: `invalidate_model_overrides_cache()`, `request.state.audit_action = "model.added"`, `request.state.audit_label = f"Added {body.model_id} ({body.provider})"`, return `{"ok": True, "model_id": body.model_id, "provider": body.provider, "enabled": False}`. The router-level `require_operator` gives non-operators a byte-identical 404 for free — no per-route auth.
    Create `backend/tests/test_159_add_model.py` mirroring `test_149_model_write.py` fixtures/gate helpers.
  </action>
  <acceptance_criteria>
    - `POST /admin/models` with `{model_id:"kimi-k3", provider:"moonshot", context_window_tokens:262144, max_output_tokens:65536, native_tools:true}` → 200, body `enabled:false`, and a `model_capabilities_overrides` row persisted with `enabled=false`; `get_model_capability_async("kimi-k3")` then returns `capability_source="db_override"` with `native_tools=true` (no restart).
    - `{provider:"bogus"}` → 422 BEFORE any DB touch.
    - `model_id` already built-in (e.g. `"gpt-4o"`) OR already an override → 409.
    - a body containing `"enabled": true` is IGNORED — the persisted row is still `enabled=false` (the model forces False; the field isn't on AddModelRequest).
    - a non-operator caller → 404 (router gate).
    - on success `request.state.audit_action == "model.added"`; on a simulated persist failure the endpoint raises 500 with action `model.add_failed` (never a 2xx).
    - the INSERT SQL uses `$` placeholders + `EXCLUDED` only — no f-string/`%`/`.format` interpolates model_id/provider/values into the statement (source assertion).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_159_add_model.py tests/test_149_model_write.py -x</automated>
  </verify>
  <done>POST /admin/models exists, operator-gated, roster+type+duplicate validated, SQLi-safe upsert, lands enabled=false, ✎ model.added receipt, honest 500, served immediately via the existing read path.</done>
</task>

<task type="auto">
  <name>Task 2: Filter-toggle write key rides set_flag (D-159-04 write half)</name>
  <files>backend/app/api/admin.py</files>
  <read_first>
    - backend/app/api/admin.py — the file being modified. Read L62-74 (`_FLAG_HUMAN_NAMES` / `_FLAG_KEYS` — the code-constant allowlist) and L507-557 (`set_flag` — the honest-failure PUT that already validates against `_FLAG_KEYS`, writes via `save_app_settings`, and stamps `flag.{key}.{on|off}`).
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §5 (the RECOMMENDED ride-along: add the key to `_FLAG_HUMAN_NAMES` → `PUT /admin/flags` accepts it with zero new endpoint code).
  </read_first>
  <action>
    Add one entry to `_FLAG_HUMAN_NAMES`: `"model_discovery_filter_enabled": "model discovery filter"`.
    That auto-includes it in `_FLAG_KEYS`, so `PUT /admin/flags` accepts the key and writes it via
    `save_app_settings` (parameterized, T-147-01) with the existing honest-500-on-failure path and the
    generic `flag.model_discovery_filter_enabled.{on|off}` / "Turned ON/OFF model discovery filter for
    everyone" audit label. Make NO other change to `set_flag`. The `app_settings` column this writes to
    is created by Plan 03's migration 103 (the readback is fail-soft until then — see Plan 03).
  </action>
  <acceptance_criteria>
    - `"model_discovery_filter_enabled"` appears in `_FLAG_HUMAN_NAMES` (`grep -c '"model_discovery_filter_enabled"' backend/app/api/admin.py` ≥ 1).
    - `PUT /admin/flags {key:"model_discovery_filter_enabled", value:false}` passes the allowlist (does NOT return 422 on the key).
    - `PUT /admin/flags {key:"bogus", value:true}` still returns 422.
    - a non-operator caller → 404.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_147_flag_refuse.py -x && grep -c '"model_discovery_filter_enabled"' backend/app/api/admin.py</automated>
  </verify>
  <done>The persisted filter toggle rides the existing set_flag path via one allowlist key; no new endpoint; SQLi-safe; honest-failure preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → /admin API | untrusted body (model_id, provider, numeric caps, flag key/value) crosses here |
| API → Postgres (asyncpg pool) | column identifiers + values reach a parameterized upsert / UPDATE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-01 | Elevation/Spoofing | POST /admin/models · PUT /admin/flags | mitigate | router-level `dependencies=[Depends(require_operator)]` → non-operator gets byte-identical 404 (no RLS backstop, SC#4). Acceptance: non-operator → 404 on both routes. |
| T-159-02 | Tampering (SQLi) | add_model_by_id upsert | mitigate | column names come ONLY from the code allowlist (`insert_cols`); `provider` validated against `set(PROVIDER_ENDPOINTS)`; ALL values are `$N` asyncpg binds. No client identifier/value reaches a SET clause. Acceptance: bogus provider → 422 before DB; SQL uses `$`+`EXCLUDED` only. |
| T-159-03 | Elevation (never-auto-enable) | add_model_by_id | mitigate | `enabled` is forced to the literal `False` in the write (not a body field). Acceptance: `enabled:true` in body → persisted row still `enabled=false`. Preserves the 149 opt-in-enable rule / SC#3. |
| T-159-04 | Repudiation | both writes | mitigate | `operator_audit_floor` dependency + `✎ model.added` / `flag.*` receipt on success; `*.add_failed` / `flag.write_failed` + real 500 on persist failure (never a false 2xx). Acceptance: success stamps `model.added`; failure raises 500 with `model.add_failed`. |
| T-159-05 | Tampering (flag key injection) | set_flag | mitigate | `body.key not in _FLAG_KEYS` → 422 before any write (T-147-01, reused). Acceptance: bogus flag key → 422; the new key accepted. |
| T-159-SC | Tampering (supply chain) | package installs | accept | No new npm/pip/cargo packages this phase (lift + wire only). No install task → no `[ASSUMED]`/`[SUS]` checkpoint required. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/test_159_add_model.py tests/test_149_model_write.py tests/test_149_model_gate.py tests/test_147_flag_refuse.py -x` — new + shipped model/flag tests pass (no regression to the 149 gate/write behavior).
- Source assertion: the add-by-ID INSERT interpolates NO client value/identifier (only `$N` binds + `EXCLUDED`).
</verification>

<success_criteria>
- An operator can add a single model by explicit id + provider; it lands enabled=false and is served immediately (D-159-02, SC#2 backend half).
- The write is SQLi-safe and never auto-enables (SC#3).
- The persisted filter toggle has a working write path via set_flag (D-159-04 write half).
</success_criteria>

<output>
Create `.planning/phases/159-model-registry-curation/159-02-SUMMARY.md` when done.
</output>
