---
phase: 159-model-registry-curation
plan: 06
type: execute
wave: 4
depends_on: [159-04, 159-05]
files_modified:
  - frontend/src/components/admin/ModelDiscoveryPanel.tsx
  - frontend/src/components/admin/ControlRoomPage.tsx
autonomous: true
requirements: [MODEL-03]

must_haves:
  truths:
    - "D-159-04: the discovery panel filters 'new' models to chat/tool-capable by default (utility rows hidden) with an honest 'N utility models hidden' count + a non-destructive 'Show all'; the toggle persists via onSetFilter → setFlag"
    - "D-159-03: un-returned capabilities in the discovery hand-fill pre-fill from familyDefaults, styled 'default — confirm', visually distinct from provider-confirmed (green) and blank 'unknown — you set it'"
    - "D-159-04: the filter lives ONLY in ModelDiscoveryPanel; the registry table (070-A) is unchanged (no registry-table search)"
    - "SC#3: pre-fill NEVER auto-enables — enableNow stays default-off, the model lands enabled=false unless the operator explicitly ticks Enable now; the confirmable diff is display-filtered only, never mutated"
  artifacts:
    - path: "frontend/src/components/admin/ModelDiscoveryPanel.tsx"
      provides: "filterEnabled/onSetFilter props + utility hiding + hidden-count + show-all + familyDefaults hand-fill pre-fill"
      contains: "filterEnabled"
    - path: "frontend/src/components/admin/ControlRoomPage.tsx"
      provides: "handleSetDiscoveryFilter (setFlag → fetchSettings) + filterEnabled/onSetFilter passed to ModelDiscoveryPanel"
      contains: "handleSetDiscoveryFilter"
  key_links:
    - from: "ModelDiscoveryPanel filter toggle"
      to: "ControlRoomPage.handleSetDiscoveryFilter"
      via: "onSetFilter prop"
      pattern: "onSetFilter"
    - from: "ControlRoomPage.handleSetDiscoveryFilter"
      to: "setFlag(model_discovery_filter_enabled)"
      via: "persisted app_settings write + settings re-fetch"
      pattern: "model_discovery_filter_enabled"
    - from: "NewModelRow un-returned inputs"
      to: "familyDefaults"
      via: "default — confirm pre-fill (never auto-enable)"
      pattern: "familyDefaults"
---

<objective>
Ship the discovery-curation vertical: the default-on suitability filter in `ModelDiscoveryPanel`
(hides utility "new" models with an honest hidden-count + non-destructive "Show all", persisted
via the Plan-02/03 flag) and the source-labeled family-default pre-fill on the discovery hand-fill
(D-159-04 + D-159-03), plus the `ControlRoomPage` `handleSetDiscoveryFilter` wiring. This delivers
SC#1 end-to-end while preserving the 149 propose-not-auto-enable red line (SC#3).

Purpose: the operator sees ~a dozen relevant proposals instead of the ~401 raw entries — and the
capability hand-fill is pre-filled but never authoritative and never auto-enabled.
Output: the filter toggle + hidden-count + show-all + hand-fill pre-fill + the shell handler.

Security invariants (client caller; the flag write surface + threat model are Plan 02/03):
- The filter is DISPLAY-only — it never enables, deletes, or mutates the confirmable diff (149 red line).
- Pre-filled defaults are operator-REVIEWED; `enableNow` stays default-off (never auto-enable — SC#3).
- The registry table (070-A) is untouched — no registry-table search this phase (D-159-04).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-model-registry-curation/159-CONTEXT.md
@.planning/phases/159-model-registry-curation/159-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/admin/ModelDiscoveryPanel.tsx
@frontend/src/components/admin/ControlRoomPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter toggle + show-all + honest hidden-count in ModelDiscoveryPanel</name>
  <files>frontend/src/components/admin/ModelDiscoveryPanel.tsx, frontend/src/components/admin/ModelDiscoveryPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/ModelDiscoveryPanel.tsx — the file being modified. Read L1-42 (the pure-leaf doctrine + `ModelDiscoveryPanelProps`), L65-100 (state + `run()`), L195-330 (the render: propose-only banner, provider run cards, the `✚ New models` `DiffGroup` at L262-285, the sticky confirm-bar summary at L322-326 to model the hidden-count copy on), L412-439 (`DiffGroup`), L443-568 (`NewModelRow`).
    - frontend/src/lib/api.ts — `DiscoveredNewModel.utility` (Plan 04).
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §10.
  </read_first>
  <action>
    Add props `filterEnabled: boolean` + `onSetFilter: (enabled: boolean) => Promise<void>` to
    `ModelDiscoveryPanelProps`. Add a local `showAllThisView` boolean state (ephemeral, default false).
    In the `✚ New models` group, compute `const utilityNew = result.new.filter(m => m.utility === true)`
    and, when `filterEnabled && !showAllThisView`, render only the non-utility `new` entries; otherwise
    render all. Add a small toggle row near the New-models group header: a "Filter to chat/tool models"
    switch bound to `filterEnabled` that calls `void onSetFilter(!filterEnabled)` (persisted — the shell
    writes + re-fetches). When `filterEnabled && !showAllThisView && utilityNew.length > 0`, render an honest
    line "{utilityNew.length} utility models hidden" + a "Show all" control that sets `showAllThisView=true`
    (ephemeral reveal — does NOT call onSetFilter, so the persisted default is unchanged). Model the count
    copy on the existing sticky-bar summary. CRITICAL: the filter is display-only — do NOT change `accepted`,
    `enableNow`, `drafts`, or `buildChanges`; a hidden model is simply not rendered. Leave the `± Changed`
    and `⊘ No longer offered` groups + the registry table entirely unchanged. Extend `ModelDiscoveryPanel.test.tsx`.
  </action>
  <acceptance_criteria>
    - with `filterEnabled=true` and a result whose `new` contains entries with `utility:true`, those rows are NOT rendered and the panel shows "{N} utility models hidden".
    - clicking "Show all" reveals the hidden rows WITHOUT calling `onSetFilter` (persisted default unchanged).
    - toggling "Filter to chat/tool models" calls `onSetFilter(false)` once.
    - with `filterEnabled=false`, every `new` row renders and no hidden-count shows.
    - `buildChanges()` output is identical whether or not rows are display-hidden (a display-only concern — assert the confirm payload is unaffected by the filter).
    - the registry-table component + the changed/vanished groups are byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/ModelDiscoveryPanel.test.tsx src/components/admin/ModelDiscoveryPanel.a11y.test.tsx</automated>
  </verify>
  <done>The default-on filter hides utility `new` models with an honest hidden-count + non-destructive Show-all; the confirmable diff is untouched (SC#3).</done>
</task>

<task type="auto">
  <name>Task 2: Family-default pre-fill on the discovery hand-fill (three-way source honesty)</name>
  <files>frontend/src/components/admin/ModelDiscoveryPanel.tsx</files>
  <read_first>
    - frontend/src/components/admin/ModelDiscoveryPanel.tsx — the file being modified. Read L110-165 (`setDraft`, `isComplete`, `coerce`, `buildChanges` — where drafts + the `patch.enabled = enableNow.has(id) && isComplete(m)` gating live), L443-568 (`NewModelRow` — the green provider-confirmed value vs the amber "unknown — you set it" input at L496-539, and the `enableNow`/`isComplete` gating + the "it will NOT be auto-enabled" warning at L542-565).
    - frontend/src/lib/model-defaults.ts — `familyDefaults` (Plan 04).
    - .planning/phases/159-model-registry-curation/159-CONTEXT.md D-159-03; .planning/phases/159-model-registry-curation/159-PATTERNS.md §10.
  </read_first>
  <action>
    For each un-returned (UNKNOWN) capability in `NewModelRow`, pre-fill the amber input's draft from
    `familyDefaults(model.model_id)[field]` when the operator hasn't typed a value (seed the draft or use it
    as the input's default value). For `native_tools` specifically — which renders via the tri-state `<select>` (`""`/`"native"`/`"none"`),
    not a numeric `<input>` — map the `tools: boolean | null` family default to the select vocabulary BEFORE
    seeding: `true` → `"native"`, `null` → leave unseeded (`""`), never `"none"` (`familyDefaults().tools` only
    ever yields `true` or `null`, never `false`, so SC#3 holds by construction).
    Style a pre-filled-from-default field distinctly as amber "default — confirm"
    (vs the existing blank amber "unknown — you set it" when `familyDefaults` returns null, vs the green
    provider-confirmed value) — three VISUALLY DISTINCT sources. CRITICAL SC#3 invariant: pre-filling MUST NOT
    auto-enable. Keep `enableNow` default-OFF; keep the `patch.enabled = enableNow.has(m.model_id) && isComplete(m)`
    gating unchanged (a pre-filled default makes `isComplete` true so the operator CAN opt in, but they must
    explicitly tick "Enable now"). When defaults are pre-filled, change the warning copy from "set the unknown
    fields" to "review the suggested defaults — it will NOT be auto-enabled". `buildChanges` continues to send
    the (possibly pre-filled) draft value ONLY for accepted models. Do not alter the provider-returned (green)
    rendering.
  </action>
  <acceptance_criteria>
    - a `new` model with an UNKNOWN capability and a matching family default shows the amber input pre-filled with the family value, labeled "default — confirm" — distinct from a blank "unknown — you set it" (null default) and from a green provider-returned value.
    - `enableNow` defaults OFF even when all fields are pre-filled (isComplete true); the model is NOT auto-enabled.
    - `buildChanges()` for an accepted-but-un-ticked pre-filled model yields `enabled: false`.
    - the warning copy reflects "review the suggested defaults — it will NOT be auto-enabled" when defaults are pre-filled.
    - the provider-confirmed (green) rendering is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/ModelDiscoveryPanel.test.tsx</automated>
  </verify>
  <done>Discovery hand-fill pre-fills from family defaults, three-way source-honest, and never auto-enables (SC#3 holds).</done>
</task>

<task type="auto">
  <name>Task 3: ControlRoomPage filter wiring (handleSetDiscoveryFilter → setFlag)</name>
  <files>frontend/src/components/admin/ControlRoomPage.tsx</files>
  <read_first>
    - frontend/src/components/admin/ControlRoomPage.tsx — the file being modified (Plan 05 already added `handleAddModel` here; build on that version). Read L36-86 (imports — `setFlag` is already imported), L253-256 (`fetchSettings`), L420-438 (the existing `handleFlag`/maintenance `setFlag → fetchSettings` pattern to mirror), L539-547 (`capabilityFlags` reading `settings?.<key>`), L744-761 (the leaf wiring where `<ModelDiscoveryPanel .../>` gets its props).
    - frontend/src/lib/api.ts — `setFlag` + `FullAppSettings.model_discovery_filter_enabled` (Plan 04).
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §11.
  </read_first>
  <action>
    Add `const handleSetDiscoveryFilter = useCallback(async (enabled: boolean) => { await setFlag("model_discovery_filter_enabled", enabled); if (alive.current) void fetchSettings() }, [fetchSettings])`
    (mirror the existing flag handlers — write then re-fetch settings; the ✎ receipt lands via the shared
    flag path). Pass to `<ModelDiscoveryPanel ... filterEnabled={settings?.model_discovery_filter_enabled ?? true}
    onSetFilter={handleSetDiscoveryFilter} />` (default-ON when settings are still loading). Keep the existing
    `onRunDiscovery`/`onConfirm` props and the Plan-05 `onAddModel` wiring intact.
  </action>
  <acceptance_criteria>
    - ControlRoomPage defines `handleSetDiscoveryFilter` calling `setFlag("model_discovery_filter_enabled", enabled)` → `fetchSettings()`.
    - `<ModelDiscoveryPanel>` receives `filterEnabled={settings?.model_discovery_filter_enabled ?? true}` and `onSetFilter={handleSetDiscoveryFilter}`.
    - the existing `onRunDiscovery`/`onConfirm` props and the Plan-05 `onAddModel` (on ModelRegistryTab) are preserved.
    - `npx tsc -b` reports 0 net-new errors; `ControlRoomPage.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | tail -5 && npx vitest run src/components/admin/ControlRoomPage.test.tsx</automated>
  </verify>
  <done>The persisted discovery filter is wired end-to-end: toggle → setFlag → app_settings → settings re-fetch → the panel reflects the persisted default (default-on).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser (discovery panel) → /admin API | the filter toggle writes a persisted app_settings flag; the hand-fill submits confirmed changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-11 | Tampering (flag write) | handleSetDiscoveryFilter → setFlag | mitigate | rides the Plan-02 `_FLAG_KEYS`-validated `PUT /admin/flags` (T-147-01) + Plan-03 `_DIRECT_COLUMNS` parameterized write; the client passes only the fixed key + a bool. Acceptance: the write goes through setFlag with the fixed key. |
| T-159-12 | Elevation (never-auto-enable) | NewModelRow pre-fill | mitigate | pre-fill seeds a REVIEWED draft only; `enableNow` stays default-off and the `enableNow && isComplete` gating is unchanged — no pre-fill path auto-enables. Acceptance: buildChanges yields `enabled:false` for an un-ticked pre-filled model. |
| T-159-13 | Tampering (diff integrity) | the display filter | mitigate | the filter hides rows for display ONLY — `accepted`/`enableNow`/`drafts`/`buildChanges` are untouched, so a hidden model can never be silently confirmed or dropped from the payload. Acceptance: buildChanges output is filter-independent. |
| T-159-SC | Tampering (supply chain) | package installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/admin/ModelDiscoveryPanel.test.tsx src/components/admin/ModelDiscoveryPanel.a11y.test.tsx src/components/admin/ControlRoomPage.test.tsx` — filter + hand-fill + a11y + shell tests pass.
- `npx tsc -b` 0 net-new; `npm run build` exit 0.
- Live UAT (phase verification, Chrome MCP): Control Room → Model Registry → Run discovery → confirm utility models are hidden by default with "N utility models hidden" → "Show all" reveals them → toggle the filter off (persists across reload) → confirm an un-returned capability pre-fills "default — confirm" and the model still lands disabled unless "Enable now" is ticked.
</verification>

<success_criteria>
- Live discovery filters to chat/tool models by default with a "show all" opt-in + honest hidden-count (D-159-04, SC#1).
- Un-returned capabilities pre-fill from source-honest family defaults, never authoritative, never auto-enabled (D-159-03, SC#3).
- The registry table is unchanged (no registry-table search).
</success_criteria>

<output>
Create `.planning/phases/159-model-registry-curation/159-06-SUMMARY.md` when done.
</output>
