---
phase: 118-auto-classification
plan: 03
type: execute
wave: 2
depends_on: ["118-01"]
files_modified:
  - backend/app/api/documents.py
autonomous: true
requirements: [CLASS-02, CLASS-03]
must_haves:
  truths:
    - "On upload, a rule-eval pass runs between metadata-build and the single persist write; it writes a suggestion into metadata_dict['_classification'] and NEVER writes folder_id (no silent move)"
    - "Rule reads in the BackgroundTask are user-scoped in app code: .or_(user_id.eq.{uploader},is_global.eq.true), enabled-only, owner-before-global then oldest created_at first (D-118-4/8)"
    - "First-match-wins → exactly ONE _classification object (not an array); the loop stops on the first matching enabled rule (D-118-3)"
    - "A second user's PRIVATE rule never matches the uploader's doc; a global rule is evaluated against the uploader's OWN metadata only (D-118-8)"
    - "A _classification key does not break existing flat metadata @> filters (regression)"
    - "Accept records prior folder_id, re-validates the target folder is readable, moves, marks status=accepted, and writes a classification.apply audit ONLY after the move succeeds; Dismiss clears _classification with no move and no audit; Undo reuses PATCH /documents/{id}/move"
  artifacts:
    - path: "backend/app/api/documents.py"
      provides: "ingest rule-eval pass + accept/dismiss endpoints"
      contains: "_classification"
  key_links:
    - from: "ingest_document rule-eval pass"
      to: "classification_matcher.match_metadata"
      via: "per-rule match against metadata_dict"
      pattern: "classification_matcher\\.match_metadata"
    - from: "ingest_document rule-eval pass"
      to: "classification_rules table"
      via: "user-scoped read in the BG task"
      pattern: "user_id\\.eq\\.\\{user_id\\},is_global\\.eq\\.true"
    - from: "accept_classification"
      to: "audit_log"
      via: "write_audit_entry classification.apply after move"
      pattern: "classification\\.apply"
---

<objective>
Ship CLASS-02 + CLASS-03 by editing `documents.py` only: (1) splice the rule-eval pass into
`ingest_document` between the final metadata_dict and the single persist write — it reads the
uploader's own + global enabled rules (user-scoped in app code, NO auth.uid() in the BG task),
evaluates each via the Plan-01 matcher first-match-wins, and writes ONE `_classification`
suggestion object — NEVER a folder move; and (2) add the reversible accept/dismiss endpoint
pair cloning `move_document` + the audited metadata-PATCH. Accept records the prior folder
(for Undo), re-validates the target folder, moves, marks accepted, and audits ONLY after the
move; Dismiss clears the suggestion; Undo reuses the existing move endpoint.

Purpose: The never-silent-move suggestion (CLASS-02) + the reversible accept/dismiss (CLASS-03)
— the milestone anti-feature ("silent autonomous auto-filing") is structurally impossible here.
Output: the edited `documents.py` (ingest splice + two endpoints).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-auto-classification/118-RESEARCH.md
@.planning/phases/118-auto-classification/118-PATTERNS.md

<interfaces>
<!-- The splice point + clone-targets, VERIFIED this plan-cycle against documents.py. -->

Splice point in ingest_document (VERIFIED): metadata_dict is FINAL after the Phase-112 re-extract
merge guard ends at documents.py:1624 (only READ — not mutated — through chunking/embedding). The
SINGLE persist UPDATE writes "metadata": metadata_dict at :1733-1748 (metadata key at :1744). Insert the
rule-eval pass immediately BEFORE the :1733 write (cleanest: metadata_dict is final, the existing write carries it).

From Plan 01 (backend/app/services/classification_matcher.py):
  - match_metadata(match_expr: dict, metadata: dict, whitelist: set[str]) -> bool   (PURE)
  - build_suggestion(rule: dict, supabase, user_id) -> dict   (the D-118-5 object; resolves folder name fresh)

Existing helpers in scope:
  - _METADATA_BUILTINS already exists at documents.py:1355 (= set(DocumentMetadata.model_fields)).
  - read_enabled_field_defs(supabase, user_id) is SYNC (embedding_service.py:270) — used raw in the sync ingest body, no aexec.
  - ingest_document is a SYNC def inside a BackgroundTask → sync .execute(); D-v2.5-01 does NOT fire here.

Clone-targets for accept/dismiss (async def endpoints):
  - move_document (documents.py:1304-1347) — owner SELECT (404 on miss) + target-folder own+global readability check (:1325-1335).
  - update_document_metadata (documents.py:1358-1452) — the audited metadata-PATCH + run_in_threadpool wrapping every .execute() (D-v2.5-01); the audit-after-write shape (:1446).
  - write_audit_entry(action_type="classification.apply", ...) — enum LIVE at audit_service.py:23.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rule-eval pass spliced into ingest_document (CLASS-02)</name>
  <read_first>
    - .planning/phases/118-auto-classification/118-PATTERNS.md §"rule-eval splice in ingest_document" (the exact pass, the splice invariant, Pitfalls 3/4)
    - .planning/phases/118-auto-classification/118-RESEARCH.md §"Splice point" Code Example + §"Pitfall 3 (leak)" + §"Pitfall 4 (no silent move)"
    - backend/app/api/documents.py:1602-1748 (the merge-guard end → chunking/embedding → the single persist write at :1733-1748; confirm metadata_dict is final + unmutated to the write)
    - backend/app/api/documents.py:1355 (_METADATA_BUILTINS) + backend/app/services/embedding_service.py:270 (read_enabled_field_defs SYNC)
    - backend/tests/integration/test_118_ingest_suggest.py + test_118_rule_leak.py + test_118_flat_filter_compat.py (the Plan-01 scaffolds this task flips GREEN)
  </read_first>
  <files>backend/app/api/documents.py</files>
  <action>
    Insert the rule-eval pass into `ingest_document` immediately BEFORE the single persist UPDATE at documents.py:1733 (after
    metadata_dict is final — i.e. after the :1624 merge guard). Guard on `if metadata_dict:` (no metadata → nothing to match;
    never blocks ingest). Read rules with the sync supabase client:
    `supabase.table("classification_rules").select("*").or_(f"user_id.eq.{user_id},is_global.eq.true").eq("enabled", True).order("is_global").order("created_at").execute()`
    — this `.or_()` predicate is NON-NEGOTIABLE (Pitfall 3: an unscoped select returns ALL users' rules; service-role bypasses
    RLS). `.order("is_global").order("created_at")` encodes owner-private(false) before global(true), oldest first (D-118-4;
    Postgres sorts false<true — assert this in a test, A1). Build the whitelist `_METADATA_BUILTINS | {d["field_key"] for d in
    read_enabled_field_defs(supabase, user_id)}` (sync reader). Loop first-match-wins (D-118-3): on the first
    `classification_matcher.match_metadata(rule["match_expr"], metadata_dict, whitelist)` that returns True, set
    `metadata_dict["_classification"] = classification_matcher.build_suggestion(rule, supabase, user_id)` and `break`. Wrap the
    whole pass in `try/except Exception` logging a warning and continuing (classification NEVER blocks ingestion — mirror the
    metadata degrade at :1569). Pitfall 4: write ONLY `metadata_dict["_classification"]` — NO folder_id write anywhere in the
    pass. The existing :1744 write carries the suggestion. Flip `test_118_ingest_suggest.py`, `test_118_rule_leak.py`, and
    `test_118_flat_filter_compat.py` GREEN.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_118_ingest_suggest.py tests/integration/test_118_rule_leak.py tests/integration/test_118_flat_filter_compat.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "user_id.eq.{user_id},is_global.eq.true" documents.py` matches inside the ingest pass (leak-safe read, D-118-8).
    - The ingest rule-eval block contains NO `folder_id` write (Pitfall 4: grep the inserted block — only `metadata_dict["_classification"]` is set).
    - `test_118_ingest_suggest.py` GREEN: an upload matching one rule writes exactly ONE `_classification` object (not array), status="suggested", folder_id UNCHANGED; a disabled rule never matches; owner-rule wins over a global rule (order proof, A1).
    - `test_118_rule_leak.py` GREEN live (two-user): a second user's PRIVATE rule never matches the uploader; a global rule evaluates against the uploader's OWN metadata only.
    - `test_118_flat_filter_compat.py` GREEN: a doc carrying `_classification` still matches a flat `metadata @>` filter (mirror test_111_flat_filter_compat).
  </acceptance_criteria>
  <done>The ingest pass reads user-scoped rules, first-match-wins writes ONE suggestion, never moves the folder, and the suggest/leak/flat-filter integration tests are GREEN live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Accept / Dismiss endpoints (CLASS-03, reversible)</name>
  <read_first>
    - .planning/phases/118-auto-classification/118-PATTERNS.md §"accept / dismiss endpoints" (the full accept clone + dismiss + Undo note + threadpool note)
    - .planning/phases/118-auto-classification/118-RESEARCH.md §"Pattern 3" + §"Pitfall 5 (stale/deleted folder)"
    - backend/app/api/documents.py:1304-1347 (move_document owner-gate + folder readability) + :1358-1452 (update_document_metadata audited PATCH + run_in_threadpool + audit-after-write at :1446)
    - backend/tests/integration/test_118_accept.py + test_118_dismiss_undo.py (the Plan-01 scaffolds this task flips GREEN)
  </read_first>
  <files>backend/app/api/documents.py</files>
  <action>
    Add two async PATCH endpoints to the documents router. ACCEPT `PATCH /{document_id}/classification/accept`: owner-scoped
    SELECT of the doc's folder_id+metadata (404 on miss, NEVER 403); read `metadata._classification`; if no active "suggested"
    suggestion or no `suggested_folder_id` → 404 "No active suggestion". Re-validate the target folder is readable (own+global,
    clone move_document:1325-1335) → 404 if gone (Pitfall 5: the FK is ON DELETE SET NULL — the folder can vanish). Record the
    PRIOR folder_id into the suggestion object (for Undo, D-118-6), set `status="accepted"` + `prior_folder_id`, then ONE UPDATE
    writing `folder_id` + the marked `metadata` (own-scoped). AFTER the move succeeds, `write_audit_entry(action_type=
    "classification.apply", metadata={document_id, rule_id, from_folder, to_folder})` — audit ONLY after the move (112/116
    honesty: never optimistic). DISMISS `PATCH /{document_id}/classification/dismiss`: own-scoped SELECT metadata → `meta.pop(
    "_classification", None)` → own-scoped UPDATE metadata → return; NO move, NO audit. Undo needs NO new endpoint — the frontend
    re-uses `PATCH /documents/{id}/move` with the recorded prior_folder_id. Wrap every `.execute()` in `run_in_threadpool`
    (D-v2.5-01 — these are async handlers; follow the update_document_metadata template, not move_document's raw .execute()).
    Flip `test_118_accept.py` + `test_118_dismiss_undo.py` GREEN.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_118_accept.py tests/integration/test_118_dismiss_undo.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "classification.apply" documents.py` matches inside the accept handler, positioned AFTER the move UPDATE (audit-after-write).
    - `grep -c "403" documents.py` is unchanged by this plan vs base (no new 403 — accept/dismiss are uniform-404 on miss); base-diff confirms.
    - `grep -n "run_in_threadpool" documents.py` present in BOTH new async handlers (D-v2.5-01).
    - `test_118_accept.py` GREEN live: accept moves to the suggested folder, records prior_folder_id, marks status=accepted, and a `classification.apply` audit row lands; a deleted/unreadable target → 404; a doc with no active suggestion → 404.
    - `test_118_dismiss_undo.py` GREEN live: dismiss pops `_classification` (no move, no audit); Undo via the existing move endpoint restores the prior folder (reversible).
  </acceptance_criteria>
  <done>Accept moves+audits-after+records prior folder; dismiss clears with no move/audit; Undo reuses move; both are run_in_threadpool-wrapped and uniform-404; the accept/dismiss tests are GREEN live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upload BackgroundTask (service-role, NO auth.uid) → classification_rules | The rule read has no JWT; app-code user-scoping is the SOLE gate |
| client → accept/dismiss PATCH endpoints | Authenticated caller mutates their own doc's folder + suggestion |
| rule-eval pass → documents.folder_id | The pass must NEVER write folder_id (suggestion-only invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-03-01 | Information Disclosure | ingest rule read (BG task, RLS-bypassed) | mitigate | App-code `.or_(user_id.eq.{uploader},is_global.eq.true)` predicate; evaluate each rule against the uploader's OWN metadata_dict only (D-118-8). Verified by the live two-user `test_118_rule_leak.py` (secure-phase re-runs non-vacuous — NOT the RLS label). |
| T-118-03-02 | Tampering / honesty | rule-eval pass | mitigate | The pass writes ONLY `metadata_dict["_classification"]` (status=suggested); NO `folder_id` write — the move happens ONLY on explicit Accept. The milestone anti-feature (silent auto-filing) is structurally impossible. Verified by grep-for-no-folder_id-in-block + the no-move assertion in test_118_ingest_suggest.py. |
| T-118-03-03 | Access Control | accept_classification target folder | mitigate | Accept re-validates the target folder is readable (own+global) BEFORE the move (clone move_document:1325) → 404 if gone/unreadable; a doc cannot be moved into another user's folder. |
| T-118-03-04 | Information Disclosure | accept/dismiss miss | mitigate | Owner-scoped SELECT/UPDATE; cross-user/absent → uniform 404, never 403 (clone the 113/116/117 contract). |
| T-118-03-05 | Repudiation | classification.apply audit | mitigate | Audit written ONLY after the move succeeds (never optimistic); live DB round-trip is the verification. |
| T-118-03-06 | Denial of Service | classification never blocks ingest | mitigate | The whole pass is wrapped in try/except → a matcher/read failure logs a warning and continues; ingestion completes regardless (mirror the metadata degrade at :1569). |
| T-118-03-SC | Tampering | npm/pip installs | accept | No new package (RESEARCH audit N/A); slopcheck moot. |
</threat_model>

<verification>
- `pytest tests/integration/test_118_ingest_suggest.py test_118_rule_leak.py test_118_flat_filter_compat.py test_118_accept.py test_118_dismiss_undo.py -x` GREEN live on :54322.
- `pytest -k "118 or 117 or 116 or 112 or 111"` regression spot GREEN (ingest_document is a shared edit — the merge guard + persist write must stay intact).
- The ingest rule-eval block has NO folder_id write (grep the inserted block); `grep -c 403 documents.py` unchanged vs base.
- Net-new failures = 0 via base-checkout of documents.py.
- threads.py byte-untouched (G-5).
</verification>

<success_criteria>
The ingest pass writes a user-scoped, first-match-wins, never-moved suggestion; accept is a
reversible move+audit-after; dismiss clears; Undo reuses move; all five 118 integration tests
this plan owns are GREEN live, and prior-phase regression (incl. the shared ingest_document
write site) is intact.
</success_criteria>

<output>
Create `.planning/phases/118-auto-classification/118-03-SUMMARY.md` when done.
</output>
