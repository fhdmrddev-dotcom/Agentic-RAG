---
phase: 200.1-the-run-says-what-it-produced-insert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/thread.py
  - backend/app/db/workflows.py
  - backend/tests/unit/test_200_1_phase_output_shape.py
autonomous: false
requirements: [RUN-04]

must_haves:
  truths:
    - "A phase row whose `output` is a jsonb STRING SCALAR yields its declared `_measure` through `declared_phase_measure`, instead of degrading to `(None, None)`."
    - "A per-step count declared by an executor renders on the run surface, on a REAL row, in a browser, beside a DB reading of the same row."
    - "A malformed `output` — not JSON, or JSON that is not an object — still yields `(None, None)` and NEVER raises."
    - "The three terminal `workflow_phases` writers stop double-encoding, so new rows land as jsonb objects."
    - "The (a)/(b) repair decision and the `inputs` / `workflow_definitions.definition` deferral are recorded IN THE SOURCE, with their reasons."
  artifacts:
    - path: "backend/app/models/thread.py"
      provides: "`phase_output_object` — the ONE read-side unwrap, beside `declared_phase_measure`"
      contains: "def phase_output_object"
    - path: "backend/app/db/workflows.py"
      provides: "the three terminal phase writers, no longer pre-encoding"
      contains: "complete_phase"
    - path: "backend/tests/unit/test_200_1_phase_output_shape.py"
      provides: "the unwrap contract + the driven counterfactual + the writer fence"
      min_lines: 120
  key_links:
    - from: "backend/app/models/thread.py::declared_phase_measure"
      to: "backend/app/models/thread.py::phase_output_object"
      via: "direct call at the top of the function"
      pattern: "phase_output_object\\("
    - from: "backend/app/db/workflows.py::complete_phase"
      to: "asyncpg pool jsonb codec (`dependencies._init_pg_connection`)"
      via: "a plain dict handed to the $2::jsonb parameter, encoded ONCE by the codec"
      pattern: "SET status='completed', output=\\$2::jsonb"
---

<objective>
Repair the read defect that makes Phase 200's SC#3 structurally unreachable: `workflow_phases.output`
is a jsonb STRING SCALAR on **484 of 484 `completed` rows**, so `declared_phase_measure` opens with
`if not isinstance(raw, dict): return None, None` and degrades **silently**. The declared per-step
count is built, gated, green — and reaches nobody.

Purpose: this is plan 1 rather than a footnote because the TEXT arm (`200.1-02`) reads `output`
through the SAME door. Repairing the door first is what makes the next plan a widening rather than a
second workaround.

Output: one shared unwrap beside `declared_phase_measure`; the three terminal writers stopped from
minting more string scalars; the decision recorded in the source with its reason; and the count seen
on screen on a real run.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<measurements>
<!-- Re-derived by the planner against the live local DB (127.0.0.1:54322) on 2026-08-20.
     Every figure below matched the ROADMAP's table EXACTLY. Re-derive; do not quote. -->

  workflow_phases rows with non-null output ............................ 588
  ... jsonb_typeof(output) = 'string' .................................. 527
  ... of the `completed` rows specifically ............................. 484 of 484
  output._measure reachable THROUGH the unwrap ......................... 13
  output._measure reachable WITHOUT the unwrap ......................... 0
  output.text non-empty THROUGH the unwrap ............................. 479

  A run that proves SC#3 in the browser, with FOUR declared measures on it:
      f4c8d111-e2fa-4f00-9936-7d249d396993
        gather-usage      (phase_index 0)  {"count": 15, "noun": "sources"}
        gather-support    (phase_index 1)  {"count": 10, "noun": "sources"}
        gather-commercial (phase_index 2)  {"count": 10, "noun": "sources"}
        emit-qbr          (phase_index 4)  {"count":  2, "noun": "fields"}
</measurements>

<interfaces>
<!-- The exact contracts this plan touches. Extracted from the codebase — no exploration owed. -->

From backend/app/models/thread.py:
  def declared_phase_measure(raw: object) -> tuple[int | None, str | None]
    - opens `if not isinstance(raw, dict): return None, None`  ← THE DEFECT
    - `0` and `None` are DIFFERENT answers and must not collapse
    - `bool` excluded deliberately (subclass of int)
    - TWO consumers, both wire models for the same rows:
        WorkflowPhaseState      (models/thread.py — the CHAT panel, GET /threads/{id}/workflow)
        WorkflowRunPhaseRead    (api/workflow_runs.py — the RUN PAGE, GET /workflow-runs/{id})

From backend/app/api/workflow_runs.py:
  def _coerce_definition(raw: object) -> dict[str, Any] | None
    - THE SHIPPED PRECEDENT for the (a) shape: dict passes through, `str` is json.loads-ed,
      anything unparseable degrades to None. Mirror its tolerance and its silence.

From backend/app/dependencies.py:
  async def _init_pg_connection(conn) -> None
    - registers a `jsonb` codec with `encoder=json.dumps` / `decoder=json.loads` on EVERY
      pooled connection (D-073-06). Its own docblock: "lets call sites pass plain Python
      dicts/lists". A call site that hands over an ALREADY-DUMPED STRING gets it dumped twice.

From backend/app/db/workflows.py — the three bind sites, all `json.dumps(...)` into `$2::jsonb`:
  complete_phase(pool, phase_id, output: dict) -> datetime | None        (~:1606)
  fail_phase(pool, phase_id, reason: str, output: dict | None) -> None   (~:1630)
  record_phase_not_sent(pool, phase_id, output: dict) -> None            (~:1683)
  — and the SHIPPED FIX of the same defect one column over, in this same file:
  create_workflow_run(...)  hands `definition.model_dump(mode="json")` — a PLAIN DICT — into
  `$7::jsonb`, and its docstring carries the full root-cause narrative. That is the pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The shared unwrap, and the (a)/(b) decision recorded in the source</name>

  <read_first>
    - `backend/app/models/thread.py` — ALL of `declared_phase_measure` and its docblock (the "ONE HOME
      FOR THE READ SIDE" claim, the `0` vs `None` rule, the `bool` exclusion). You are extending the
      one home, not adding a second.
    - `backend/app/api/workflow_runs.py` lines 179-200 — `_coerce_definition`. This is the shape to
      mirror, including its degrade-to-`None` silence.
    - `supabase/migrations/123_repair_definition_snapshot_string_scalars.sql` — the full header. It is
      the sibling case, it names the sibling deferral you must honour or overturn, and its own
      argument for why `definition_snapshot` was repairable alone is the argument you will reuse.
    - `backend/app/dependencies.py` lines 63-82 — `_init_pg_connection`, the codec that causes this.
    - `docs/HOT-FILE-LEDGER.md` — search for `models/thread.py`. **It is not there.** That absence is
      part of this task's output (see the ledger step below).
  </read_first>

  <files>backend/app/models/thread.py, backend/tests/unit/test_200_1_phase_output_shape.py, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>

  <action>
Add a module-level `phase_output_object(raw: object) -> dict | None` in `backend/app/models/thread.py`,
sited IMMEDIATELY ABOVE `declared_phase_measure` so the read side keeps exactly one home. Contract:
a `dict` passes through unchanged; a `str` is `json.loads`-ed and returned ONLY if the result is a
`dict`; `None`, a non-`str` non-`dict`, an unparseable string, and a string that parses to a list /
number / bool / null all return `None`. It must NEVER raise — catch `ValueError` and `TypeError`, the
same two `_coerce_definition` catches. Do not write `raw["_measure"]` anywhere.

Rewrite `declared_phase_measure`'s first line to call `phase_output_object(raw)` and return
`(None, None)` when it yields `None`. Everything below that line — the `_measure` dict guard, the
`isinstance(count, int) and not isinstance(count, bool)` test, the non-empty `noun` test, the
`(0, "sources")` vs `(None, None)` distinction — stays BYTE-IDENTICAL. Assert that byte-identity in
the test file rather than claiming it.

RECORD THE DECISION IN THE DOCBLOCK, in this module, in the file's existing voice. It is
`D-200.1-01` and it is TAKEN, not offered:

  **BOTH (a) and (b) are taken. NO migration.**

  · **(a) repairs the READ and is the load-bearing half.** It reaches all 588 historical rows,
    including the 484 `completed` ones and the 479 carrying `output.text` that `200.1-02` needs.
    Nothing else reaches history.
  · **(b) repairs the WRITER** (Task 2), because leaving it wrong mints roughly three new bad rows
    per run, and because the reader now tolerates both shapes so the flip cannot break a consumer.
  · **The sibling deferral is HONOURED, and the reason it does not transfer is stated rather than
    assumed.** STATE.md and migration 123's header leave `workflow_runs.inputs` (230 of 230 string)
    and `workflow_definitions.definition` (261 of 291 string) in the old shape because *"flipping a
    writer alone gives a table with two shapes in it."* `output` ALREADY has two shapes — 527 string
    against 61 object, and the object rows are `pending`/`cancelled`/`skipped`, which is what makes
    the split diagnostic rather than coincidental. And migration 123's own stated condition for
    repairing a column alone is *"one reader that already accepts BOTH shapes"* — which is precisely
    what (a) creates here. **Neither `inputs` nor `workflow_definitions.definition` is touched by this
    phase**, and their re-open trigger is carried forward verbatim: the next phase that touches either
    on the WRITE path.
  · **No migration, and the reason rather than the assurance.** The read repair reaches every row, so
    a data migration buys nothing the read does not already buy, while running a blind `#>> '{}'`-then-
    `::jsonb` cast over 527 rows of MODEL-INFLUENCED content is strictly more risk than the two-row
    `definition_snapshot` case migration 123 was written for. **Re-open trigger, named so it is not a
    silence: the first phase that needs `output` queryable IN SQL** — a `->> 'text'` predicate, an
    index, or a feed's `WHERE`/`ORDER BY` — at which point read-side tolerance is not enough, because
    SQL sees the raw column and not the helper.

Then discharge the hot-file ledger obligation for THIS FILE, in this task's commit. Re-derive with the
`CLAUDE.md` recipe (`git log --oneline` / the phase-bucket `sed` pipeline / `wc -l`) — the planner
measured `12 / 8 / 217` at `e0c57ef4`, which **fires G-5 at 8 phases against a threshold of 3, and this
file has NO ROW IN CLAUDE.md AND NO SECTION IN docs/HOT-FILE-LEDGER.md.** It has been invisible to its
own guardrail for its entire life — the `config.py` / `ChatLayout.tsx` finding again. Add BOTH in the
SAME COMMIT (the same-commit sync rule): a row in CLAUDE.md's hot-file table and a section in
`docs/HOT-FILE-LEDGER.md`. The section must carry the invariants a future editor needs: this module is
the ONE read-side home and a second copy is how the run page and the chat panel come to disagree about
the same row; `0` and `None` are different answers; `bool` is excluded because it subclasses `int`;
and the unwrap must degrade rather than raise because it reads model-influenced jsonb. Verdict:
*honoured by construction* — one read-side concern gained one helper — and say so rather than leaving
it implied. Finish by running the CLAUDE.md size gate.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit/test_200_1_phase_output_shape.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - SOURCE: `grep -c "def phase_output_object" backend/app/models/thread.py` returns `1`.
    - SOURCE: `declared_phase_measure`'s body contains `phase_output_object(` and no longer contains
      the literal `if not isinstance(raw, dict)`.
    - BEHAVIOR (the repair): `declared_phase_measure(json.dumps({"_measure": {"count": 15, "noun": "sources"}}))`
      returns `(15, "sources")`.
    - BEHAVIOR (the DRIVEN COUNTERFACTUAL — required, not optional): a test asserts that the SHIPPED
      pre-change predicate — `raw if isinstance(raw, dict) else None` — returns `(None, None)` on that
      SAME string input **and does not raise**. The counterfactual is expressed as a local re-statement
      of the old guard, so it keeps reproducing after the source is fixed. A fence that was never
      driven RED is a fence that could not fire.
    - BEHAVIOR (degrade, never raise), all four asserted individually: `phase_output_object("not json")`,
      `phase_output_object(json.dumps([1, 2]))`, `phase_output_object(json.dumps("a string"))` and
      `phase_output_object(12)` each return `None`, and `pytest.raises` is NOT needed anywhere.
    - BEHAVIOR (the honest zero survives): `declared_phase_measure(json.dumps({"_measure": {"count": 0, "noun": "sources"}}))`
      returns `(0, "sources")` — not `(None, None)`.
    - BEHAVIOR (bool still excluded): `declared_phase_measure(json.dumps({"_measure": {"count": True, "noun": "sources"}}))`
      returns `(None, None)`.
    - BEHAVIOR (the dict path is unregressed): the object-shaped input still returns its pair, so the
      61 `pending`/`cancelled`/`skipped` rows are unaffected.
    - DB: a script run against `127.0.0.1:54322` prints, for run `f4c8d111-e2fa-4f00-9936-7d249d396993`,
      the four `(slug, count, noun)` triples obtained by feeding each row's RAW `output` value through
      `declared_phase_measure`, and all four are non-null. Capture the output verbatim into the SUMMARY.
    - CLI: `git show --stat HEAD` for this task's commit lists BOTH `CLAUDE.md` and
      `docs/HOT-FILE-LEDGER.md`, and `grep -c "models/thread.py" CLAUDE.md` returns at least `1`
      (it returned `0` before this task).
    - CLI: `node scripts/check-claude-md-size.cjs` exits `0`.
  </acceptance_criteria>

  <done>
`phase_output_object` exists in one place, `declared_phase_measure` reads through it, the string-scalar
counterfactual is driven and reproduces, the decision is written in the source with its reasons and its
re-open trigger, and `backend/app/models/thread.py` has a ledger row and a ledger section for the first
time in its life.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stop the three terminal writers double-encoding — and record what else the defect was silently breaking</name>

  <read_first>
    - `backend/app/db/workflows.py` — `create_workflow_run`'s FULL docstring (≈:296-395). It carries the
      root-cause narrative, the measured evidence, the "⚠ THE FIX IS TO STOP PRE-ENCODING, NOT TO ADD A
      CAST" rule, and the scope paragraph that names the sibling columns. You are applying the fix it
      already documents, to three more sites in the same file.
    - `backend/app/db/workflows.py` — `complete_phase` (~:1550-1610) in full, including the L-01
      `IS DISTINCT FROM 'cancelled'` fence and the `RETURNING completed_at` contract; `fail_phase`
      (~:1612-1632); `record_phase_not_sent` (~:1655-1685). Note `skip_phase` writes NO output and is
      not in scope.
    - `backend/app/db/workflows.py` — `load_run_phases` (~:1311-1327): it SELECTs `output`.
    - `backend/app/services/harness_engine.py` ~:1550-1570 — the F7 resume re-fold, which does
      `r.get("output") or {}` and assigns the result into `accumulated_outputs: dict[str, dict]`.
    - `docs/HOT-FILE-LEDGER.md` → the `backend/app/db/workflows.py` section, in full. G-5 FIRES at
      20 phases. Read the named seam and the security-bearing invariants before editing.
  </read_first>

  <files>backend/app/db/workflows.py, backend/tests/unit/test_200_1_phase_output_shape.py, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>

  <action>
Change the THIRD positional argument at each of the three bind sites — `complete_phase`,
`fail_phase`, `record_phase_not_sent` — from `json.dumps(output)` / `json.dumps(payload)` to the plain
dict, so the pool's jsonb codec performs the ONE encode. The `$2::jsonb` cast STAYS: `create_workflow_run`
proves a codec-encoded dict passes a `::jsonb`-cast parameter, and the cast is not what was wrong. The
SQL literals must be otherwise BYTE-IDENTICAL — the `IS DISTINCT FROM 'cancelled'` fence, the
`RETURNING completed_at`, the `updated_at=now()` and the `completed_at = now()` all stay exactly as
shipped. `fail_phase` keeps composing `payload` with `_failure_reason` winning on a key collision.

Do NOT touch `json.dumps(inputs)` on the `create_workflow_run` INSERT, and do NOT touch the four
`json.dumps(definition.model_dump(mode="json"))` sites. Those are the honoured sibling deferral from
Task 1, and this plan must be able to prove it left them alone.

RECORD, in `complete_phase`'s docstring, a finding this plan measured and is NOT fixing — because a
finding that lives nowhere is a finding that was deleted. `load_run_phases` SELECTs `output`, and
`harness_engine.py`'s F7 resume re-fold reads `r.get("output") or {}` into a `dict[str, dict]`. On a
string-scalar row the pool codec decodes to a **Python `str`**, so the resumed run's grounding re-fold
has been folding STRINGS — the same silent degradation as `declared_phase_measure`, in a third
consumer, and the reason `isinstance` guards downstream (`_is_llm_human_input`,
`_active_tool_call_id`) have been quietly falling through. State plainly: **(b) repairs this for NEW
rows and does NOT repair it for the 527 historical ones**, it is outside RUN-04's scope, and the
re-open trigger is *the next phase that touches the resume path or the startup sweep*.

Then discharge the ledger obligation for `backend/app/db/workflows.py` in this task's commit: re-derive
the triple with the CLAUDE.md recipe (planner measured `42 / 20 / 2154` at `e0c57ef4`; the row currently
reads the same, so confirm rather than assume), update the CLAUDE.md row AND the
`docs/HOT-FILE-LEDGER.md` section in the SAME COMMIT, and state the G-5 verdict explicitly: **honoured
by construction — this file already owns every `workflow_phases` writer in the tree, and correcting the
PARAMETER of three writers it already owns is not a second concern.** Name the seam carried forward
verbatim from the existing section (the three list feeds vs the single-definition read vs the
publish-flip vs the run CRUD vs the seven phase-status writers) rather than inventing a new one. Run
the CLAUDE.md size gate.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit/test_200_1_phase_output_shape.py tests/unit/test_db_runs.py -q</automated>
  </verify>

  <acceptance_criteria>
    - SOURCE FENCE (the fix landed): over `inspect.getsource` of each of `complete_phase`, `fail_phase`
      and `record_phase_not_sent` with comments stripped, `json.dumps` appears **0** times. ⚠ Strip
      comments first — these docstrings legitimately discuss `json.dumps`, and a naive count is the
      187-24 trap this repo names repeatedly. Include a POSITIVE CONTROL proving the stripper still
      finds a real call.
    - SOURCE FENCE (the cast survived): each of the three SQL literals still contains `output=$2::jsonb`
      and still contains `status IS DISTINCT FROM 'cancelled'`.
    - SOURCE FENCE (byte-identity of the SQL): the three SQL string literals are asserted equal to the
      literals at the plan's base commit, extracted with `git show <base>:backend/app/db/workflows.py`.
      Only the PARAMETER moved.
    - SOURCE FENCE (the sibling deferral held): `inspect.getsource(create_workflow_run)` still contains
      `json.dumps(inputs)`, and `grep -c "json.dumps(definition.model_dump" backend/app/db/workflows.py`
      returns the same figure as at the base commit.
    - BEHAVIOR (driven, not reasoned): a test acquires a real asyncpg connection with
      `dependencies._init_pg_connection` applied, writes a dict through the SHIPPED `complete_phase`
      against a throwaway `workflow_phases` row, and asserts `jsonb_typeof(output) = 'object'` and
      that `output -> '_measure' ->> 'noun'` returns the noun. ⚠ Drive the COUNTERFACTUAL beside it:
      the same write with a pre-`json.dumps`-ed argument stores `jsonb_typeof = 'string'` and
      `output -> '_measure'` returns SQL NULL **without erroring** — which is the whole reason this
      defect was invisible. If no live DB is reachable in the executor's environment, mark this
      criterion BLOCKED in the SUMMARY with the reason and drive it at the Task 3 checkpoint instead;
      do not silently substitute a mock.
    - CLI: `backend/tests/unit` suite is at or better than its `62 failed / 2350 passed` baseline. Read
      the pytest summary line verbatim into the SUMMARY; a bigger passing figure is the suite growing.
    - CLI: `git diff --stat <base> HEAD -- supabase/migrations` is EMPTY and
      `ls supabase/migrations | wc -l` is unchanged — this plan chose no migration and proves it.
    - CLI: `node scripts/check-claude-md-size.cjs` exits `0`.
  </acceptance_criteria>

  <done>
The three terminal writers hand the codec a dict, every SQL literal is byte-identical, the sibling
columns are provably untouched, the resume-path finding is recorded with a trigger instead of being
fixed silently or dropped, and `db/workflows.py`'s ledger row and section are current in one commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC#3 discharged against data — a browser open and a DB reading side by side</name>

  <read_first>
    - `frontend/src/components/workflows/RunSpine.tsx` — `declaredCount` and where the count renders,
      so you know exactly which atom on screen to look for.
    - `.planning/ROADMAP.md` § Phase 200.1 success criterion 3.
  </read_first>

  <action>
Pause and hand the run surface to the operator. Run NOTHING further in this plan until the checkpoint
resolves. Before pausing, print the DB reading described below into the transcript so the operator has
one half of the comparison already in front of them: for run `f4c8d111-e2fa-4f00-9936-7d249d396993`,
each phase row's `slug`, `phase_index` and the `_measure` object obtained by feeding that row's RAW
`output` through `declared_phase_measure`. Do not summarise it — print the four triples.
  </action>

  <what-built>
Plan tasks 1 and 2: `phase_output_object` beside `declared_phase_measure`, `declared_phase_measure`
routed through it, and the three terminal `workflow_phases` writers stopped from double-encoding. The
backend now yields a real `step_count` / `step_noun` pair on rows that carry one. Every automated fence
above is green.

⚠ What is NOT yet proved is the only thing SC#3 asks for: that the count **reaches a person**. Phase 200
shipped this feature with a green count gate, a green `tsc`, two correct wire models and a correct
absent-arm render — and it reached nobody for four months. A green suite is exactly the evidence that
was already available when the defect shipped.
  </what-built>

  <how-to-verify>
1. Restart the backend so the changed serializer path is live (the operator starts the backend —
   `powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1` first if Supabase is down;
   if `54322` refuses a connection, read `CLAUDE.md` § the Windows port-reservation trap before
   diagnosing anything else).
2. In a terminal, print the DB reading for run `f4c8d111-e2fa-4f00-9936-7d249d396993` — for each phase
   row, `slug`, `phase_index`, and the `_measure` object read through the unwrap. Expect exactly four
   non-null: `gather-usage` 15 sources · `gather-support` 10 sources · `gather-commercial` 10 sources ·
   `emit-qbr` 2 fields.
3. In the browser at `http://localhost:5173`, open that same run's run surface.
4. Read the right-hand run panel (`RunSpine`). Each of those four steps must show its count as the
   spine's sub-line — `15 sources`, `10 sources`, `10 sources`, `2 fields` — matching the terminal
   exactly, step for step.
5. Confirm the steps that declare NO measure still show NOTHING — no `0`, no dash, no placeholder.
   D-07's absent arm is correct and must survive this repair.
6. Report the two readings TOGETHER. A screenshot of the browser alone is not the evidence SC#3 asks
   for; the point is that the two agree.

⚠ If `take_screenshot` times out (a known failure mode on this box), read the DOM geometry and text via
`evaluate_script` instead and paste the extracted strings.
  </how-to-verify>

  <done>The operator has seen the declared count on screen on a real run, and it agrees with the DB reading of the same rows, step for step. Phase 200's SC#3 is discharged against DATA rather than against a wire model.</done>
  <resume-signal>Type "approved" with the two readings pasted, or describe what disagreed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor output → `workflow_phases.output` jsonb | Model-influenced content is persisted verbatim (`_persist_output` stores each executor's dict FULL AND INLINE). |
| `workflow_phases.output` → `phase_output_object` | This plan puts a PARSER on that boundary where previously there was only an `isinstance` test. That is the new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.1-01 | Tampering | `models/thread.py::phase_output_object` | mitigate | The parser is exception-guarded on `ValueError` + `TypeError` and returns `None` for anything that is not a dict-shaped parse. Driven, not asserted: four malformed inputs (non-JSON, JSON array, JSON string, non-str non-dict) each return `None` and none raises. A parser that raises here 500s the run page for the owner of a run whose model wrote something odd. |
| T-200.1-02 | Denial of Service | same | accept | `json.loads` is unbounded, but no NEW byte crosses a boundary: `GET /workflow-runs/{id}` already SELECTs this exact column whole (`api/workflow_runs.py:636`) and `load_run_phases` already reads it on every resume. Largest single `output.text` measured locally is 38,935 chars. Re-open trigger: the first surface that parses `output` for a LIST of runs rather than one. |
| T-200.1-03 | Elevation of Privilege | the three writers in `db/workflows.py` | mitigate | The `WHERE id = $1 AND status IS DISTINCT FROM 'cancelled'` predicates are asserted BYTE-IDENTICAL against the base commit. On a service-role pool that bypasses RLS the predicate IS the access boundary, and this change moves a parameter, never a clause. |
| T-200.1-04 | Information Disclosure | this plan's scope | accept | Nothing new reaches the wire in this plan. `WorkflowRunPhaseRead` gains no field here; `step_count` / `step_noun` are already declared and already shipped. The exposure question belongs entirely to `200.1-02` and is modelled there. |
| T-200.1-SC | Tampering | npm/pip/cargo installs | mitigate | **This plan installs no package.** No `pip install`, no `npm install`, no dependency file is edited — asserted by `git diff --stat` over `backend/requirements*.txt` and `frontend/package.json` being empty. The Package Legitimacy Gate therefore does not apply and no `[ASSUMED]`/`[SUS]` checkpoint is owed. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/unit -q` — read the summary line VERBATIM.
  Baseline at `e0c57ef4` is `62 failed / 2350 passed`; the contract is **no new failure**, never a
  fixed total.
- `git diff --stat <base> HEAD -- supabase/migrations` is empty — this plan chose no migration.
- `node scripts/check-claude-md-size.cjs` exits `0`.
- The Task 3 checkpoint is approved with BOTH readings pasted.
- No frontend file is touched by this plan, so the count gate is not this plan's to move; if it is run,
  it must read unchanged.
</verification>

<success_criteria>
- `declared_phase_measure` returns the declared pair on a jsonb-string-scalar row, proved on real data.
- The string-scalar counterfactual is driven and reproduces after the fix.
- Malformed `output` degrades to `(None, None)` and never raises — four cases, individually asserted.
- New rows land as jsonb objects; the SQL is byte-identical; `inputs` and `definition` are provably
  untouched.
- D-200.1-01 is written in the source with its reason, and the sibling deferral is honoured in writing
  with a named re-open trigger.
- `backend/app/models/thread.py` has a CLAUDE.md row and a `docs/HOT-FILE-LEDGER.md` section for the
  first time; `backend/app/db/workflows.py`'s are re-derived and current.
- The count is seen on screen on run `f4c8d111-e2fa-4f00-9936-7d249d396993`, beside the DB reading.
</success_criteria>

<output>
Create `.planning/phases/200.1-the-run-says-what-it-produced-insert/200.1-01-SUMMARY.md` when done.
Record VERBATIM: the pytest summary line, the DB reading from Task 3, the re-derived ledger triples,
and — if the Task 2 live-DB criterion was blocked — which criterion was blocked and why.
</output>
</content>
</invoke>
