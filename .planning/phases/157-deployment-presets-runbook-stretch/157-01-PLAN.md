---
phase: 157-deployment-presets-runbook-stretch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/onebox.env.example
autonomous: true
requirements: [DEPLOY-01]

must_haves:
  truths:
    - "deploy/onebox.env.example is a curated one-box preset layered on backend/.env.example with the D-06 baseline: WORKER_COUNT=2, SANDBOX_ENABLED=true, RERANK_ENABLED=false, POSTGRES_POOL_MAX=10, bundled Redis 256MB, Supabase cloud"
    - "The preset productizes the local-cloud-onprem env-var-only switch and documents WORKER_COUNT as a scale dial turned inside a home, not a separate product (D-04)"
    - "Every key in the preset traces to backend/.env.example OR is one of the documented gaps FRONTEND_URL / ENVIRONMENT / VITE_*"
  artifacts:
    - path: deploy/onebox.env.example
      provides: "Curated one-box env preset (the filled-in .env template)"
      contains: "RERANK_ENABLED=false"
  key_links:
    - from: deploy/onebox.env.example
      to: docker-compose.prod.yml
      via: "operator copies it to root ./.env which feeds BOTH compose build-arg interpolation and the backend env_file"
      pattern: "VITE_SUPABASE_URL"
---

<objective>
Author `deploy/onebox.env.example` — the curated home-B (one-box self-host) env preset that layers on `backend/.env.example` with the D-06 baseline values. This is the DEPENDENCY ROOT of the phase: the operator copies it to a root-level `./.env` that feeds BOTH the compose `build.args` interpolation (build-time `VITE_*`) AND the backend `env_file:` (runtime). Author it first so every downstream artifact (compose, OPERATOR.md, the D-09 smoke) reads a settled env surface.

Purpose: Give a self-hoster a single filled-in template — set the DB URL/keys + one AI key + the encryption key, and `docker compose up`. Productizes the "env-var-only switch between homes" architectural fact.
Output: `deploy/onebox.env.example`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-CONTEXT.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-VALIDATION.md

<interfaces>
<!-- The env surface the preset curates. Names are VERIFIED from source — do not invent. -->
Base surface: backend/.env.example (the canonical grouped variable list).
Two vars the app READS but backend/.env.example OMITS (the preset MUST add them):
  - FRONTEND_URL   (default http://localhost:5173, backend/app/config.py:1168 — comma-split multi-origin CORS)
  - ENVIRONMENT    (default "", config.py:933 — deploy marker only; NO LONGER gates /admin as of Phase 146)
Frontend BUILD-TIME vars (build args, NOT runtime — VERIFIED frontend/src/lib/):
  - VITE_SUPABASE_URL        (supabase.ts:3)
  - VITE_SUPABASE_ANON_KEY   (supabase.ts:4)
  - VITE_API_BASE_URL        (api.ts:13 — recommend the relative value /api for same-origin through nginx)
SECRETS_ENCRYPTION_KEY: Fernet key (Phase 150). Blank = plaintext + loud warning; malformed = refuse boot.
  Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
OPERATOR_EMAILS: the operator's own email — bootstraps them as operator on first boot (the real /admin access control, not ENVIRONMENT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the one-box variable surface with the D-06 baseline</name>
  <files>deploy/onebox.env.example</files>
  <read_first>
    - deploy/onebox.env.example (the net-new file to create)
    - backend/.env.example (SOURCE OF TRUTH — the canonical grouped env surface the preset curates; copy its group ordering + comments, then set one-box values)
    - .planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md § "Env-Var Surface" (the grouped Secrets / Infra / Settings / VITE build-time lists + the two documented gaps)
  </read_first>
  <action>
    Create `deploy/onebox.env.example` as a filled-in one-box template mirroring backend/.env.example's group order. Four grouped sections:
    (a) SECRETS (placeholders ONLY, never real values): SUPABASE_SERVICE_ROLE_KEY, SUPABASE_SECRET_KEY, SUPABASE_ANON_KEY, SUPABASE_PUBLISHABLE_KEY, POSTGRES_DSN (embeds DB password), one provider key OPENAI_API_KEY (note others are optional), and SECRETS_ENCRYPTION_KEY (leave blank with the generate-command comment).
    (b) INFRA / TOPOLOGY with the D-06 one-box baseline: WORKER_COUNT=2, SANDBOX_ENABLED=true, SANDBOX_IMAGE=agentic-rag-sandbox:101.1, POSTGRES_POOL_MIN=2, POSTGRES_POOL_MAX=10, REDIS_URL=redis://redis:6379 (the bundled compose service name), SUPABASE_URL (cloud https placeholder), plus the two GAP vars FRONTEND_URL and ENVIRONMENT=production, plus OPERATOR_EMAILS.
    (c) SETTINGS: LLM_PROVIDER=openai, EMBEDDING_MODEL=text-embedding-3-small, EMBEDDING_DIMENSIONS=1536, and RERANK_ENABLED=false (no torch on a ~4GB box — the cloud image already drops sentence-transformers). Carry the other retrieval defaults as-is from backend/.env.example.
    (d) FRONTEND BUILD-TIME (VITE_*) — a clearly-labelled block stating these are build args, NOT runtime: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_BASE_URL=/api.
    Do NOT paste real secrets. Reference backend/.env.example for exact key spellings and default values.
  </action>
  <acceptance_criteria>
    - `deploy/onebox.env.example` exists.
    - Contains literal `RERANK_ENABLED=false`, `WORKER_COUNT=2`, `SANDBOX_ENABLED=true`, `POSTGRES_POOL_MAX=10`, `REDIS_URL=redis://redis:6379`.
    - Contains the two gap vars `FRONTEND_URL` and `ENVIRONMENT`.
    - Contains a labelled VITE build-time block with all three: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_API_BASE_URL=/api`.
    - Key-trace: every `KEY=` in the file either appears in `backend/.env.example` OR is one of the documented gaps (`FRONTEND_URL`, `ENVIRONMENT`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_API_BASE_URL`). No orphan keys.
    - No real secret values (all secret slots are placeholders / blank).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the operator usage-header, local-smoke URL split, and the scale-dial note</name>
  <files>deploy/onebox.env.example</files>
  <read_first>
    - deploy/onebox.env.example (the Task 1 output — append the header comment block + inline notes)
    - .planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md § "Pitfall 3" (browser vs backend see Supabase at DIFFERENT local URLs) and § "Pattern 2" (one root ./.env serves both mechanisms)
  </read_first>
  <action>
    Prepend a comment header to `deploy/onebox.env.example` documenting the operator flow: `cp deploy/onebox.env.example ./.env` at the repo root (gitignored), fill the DB URL/keys + one AI key + SECRETS_ENCRYPTION_KEY, then `docker compose -f docker-compose.prod.yml up -d --build`. Include the SECRETS_ENCRYPTION_KEY generation one-liner (the Fernet command) as a comment. Add a LOCAL-smoke-only note near the Supabase/VITE vars capturing the Pitfall-3 split: for the D-09 local-Supabase smoke the browser build arg is `VITE_SUPABASE_URL=http://127.0.0.1:54321` while the backend runtime uses `SUPABASE_URL=http://host.docker.internal:54321` and `POSTGRES_DSN=...@host.docker.internal:54322/postgres` (for a real cloud deploy both are the same https URL). Add a D-04 tuning-dial note next to WORKER_COUNT: "a dial inside a home — drop to 1 on a 1-vCPU box, raise on bigger hardware; not a separate product." Then run the key-trace static check.
  </action>
  <acceptance_criteria>
    - Header comment documents the `cp deploy/onebox.env.example ./.env` -> fill -> `docker compose ... up` flow.
    - Contains the Fernet `SECRETS_ENCRYPTION_KEY` generate one-liner as a comment.
    - Contains the Pitfall-3 local-smoke split note (both `127.0.0.1` for the VITE build arg and `host.docker.internal` for the backend runtime).
    - Contains the WORKER_COUNT scale-dial note referencing tuning inside a home (D-04).
    - Static check green: `grep -v '^#' deploy/onebox.env.example | grep -c '='` > 0 and every non-comment `KEY=` traces to `backend/.env.example` or a documented gap.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preset file in git → operator's real ./.env | The committed preset is a template; real secrets only ever live in the gitignored root `./.env` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-SEC | Information disclosure | deploy/onebox.env.example | mitigate | Placeholders only — no real secret values committed; header instructs `cp` to the gitignored root `./.env` (.gitignore line 2 `.env`); SECRETS_ENCRYPTION_KEY left blank with a generate-command comment |
| T-157-KEY | Information disclosure | provider keys at rest | mitigate | Preset documents SECRETS_ENCRYPTION_KEY (Fernet, Phase 150) so keys are encrypted at rest once set; blank = plaintext + loud warning is called out |
| T-157-SC | Tampering | package/image installs | accept | This plan adds NO npm/pip/cargo packages and NO Docker images — it authors a text `.env` template only; slopcheck N/A |
</threat_model>

<verification>
- `deploy/onebox.env.example` exists and is a valid `.env` (comment lines `#`, `KEY=value` lines).
- Key-trace passes: every non-comment key traces to `backend/.env.example` or a documented gap.
- Baseline values present exactly (`RERANK_ENABLED=false`, `WORKER_COUNT=2`, `POSTGRES_POOL_MAX=10`).
- No real secrets committed.
</verification>

<success_criteria>
The one-box preset is authored, curated on `backend/.env.example`, carries the D-06 baseline + the two gap vars + the VITE build-time block, documents the copy-to-root-`.env` flow and the local-smoke URL split, and every key traces to source. Downstream plans (compose, OPERATOR.md, smoke) can now read a settled env surface.
</success_criteria>

<output>
Create `.planning/phases/157-deployment-presets-runbook-stretch/157-01-SUMMARY.md` when done.
</output>
