---
phase: 094-workflow-legibility-mode-clarity
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - frontend/src/components/panel/PhaseTimeline.tsx
  - frontend/src/components/panel/PhaseCard.tsx
  - frontend/src/components/panel/WorkspacePanel.tsx
  - frontend/src/components/panel/panelOpenSignal.ts
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx
  - frontend/src/components/panel/__tests__/FailReason.test.tsx
  - frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx
autonomous: true
requirements: [PANEL-08, A11Y-03]
must_haves:
  truths:
    - "Entering Harness Mode auto-opens the panel to a phase timeline reconciled via GET /threads/{id}/workflow on mount"
    - "The full N-phase skeleton renders from total_phases alone (the spinner-killer); live events advance forward only, never backward"
    - "A failed/gate-failed run renders failed-with-reason (closed taxonomy + reason_unknown sentinel), never an empty done"
    - "The timeline meets WCAG 2.1 AA: APG accordion, non-color-only status, one role=status announcer, separate role=alert for failures, zero axe violations in every state"
    - "Per-phase tool/search/source count chips are SUPPRESSED, never faked"
  artifacts:
    - path: "frontend/src/components/panel/PhaseTimeline.tsx"
      provides: "section ol/li timeline + reconcile-then-live + announcer + suppress-counts"
      min_lines: 60
    - path: "frontend/src/components/panel/PhaseCard.tsx"
      provides: "APG accordion phase card with status atom (glyph+text+color) + failure reason + retrying purple"
      min_lines: 50
    - path: "frontend/src/components/panel/WorkspacePanel.tsx"
      provides: "PhaseTimeline section mount + hasActivity extension"
      contains: "PhaseTimeline"
  key_links:
    - from: "frontend/src/components/panel/PhaseTimeline.tsx"
      to: "frontend/src/providers/StreamsProvider.tsx"
      via: "usePhases(threadId) + useTasks(threadId)"
      pattern: "usePhases"
    - from: "frontend/src/components/chat/ChatArea.tsx"
      to: "frontend/src/components/panel/panelOpenSignal.ts"
      via: "requestOpenPanel() at harness kickoff"
      pattern: "requestOpenPanel"
---

<objective>
Render the live phase timeline in the workspace panel: `PhaseTimeline` (section/ol/li + reconcile-then-live + the a11y announcer + suppressed counts) and `PhaseCard` (APG accordion with non-color-only status atoms + failure-with-reason + the retrying purple gated on Plan 01's token), mount it as the 5th WorkspacePanel section, and fire `requestOpenPanel()` at harness kickoff for PANEL-08 auto-open. Flips the PhaseTimeline/FailReason/PhaseReconcile Wave 0 tests GREEN (INV-2/3b/4).

Purpose: this is the operator's #1 acceptance bar made real ("show the workflow's REAL steps, not a spinner"). The skeleton-from-total_phases is the spinner-killer; the closed failure taxonomy + reason_unknown sentinel is the RC-4 trust fix on the render side; A11Y-03 is the WCAG gate.
Output: 2 new components + WorkspacePanel mount + the panelOpenSignal emit + the ChatArea kickoff call + 3 tests GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-CONTEXT.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-RESEARCH.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-UI-SPEC.md
@.planning/sketches/094-grounding/DATA-CONTRACT.md

<interfaces>
From frontend/src/providers/StreamsProvider.tsx (Plan 02 outputs, consumed here):
- usePhases(threadId): { data: Phase[]; isLoading; error; reconcile } (fetcher getThreadWorkflow).
- useTasks(threadId): { data: TaskRunIndexItem[]; ... } (sub-agent rows + summaries, batch agent tally).
- useViewingThread(): string | null.

From frontend/src/components/panel/WorkspacePanel.tsx:
- hasActivity gate at :95-96 (todos.length>0 || files.length>0 || pendingAsks.length>0). Extend to include phases.length>0.
- Section mounts at :104-138; the Versions PanelSection at :126-134 is the shape to copy. Add usePhases read next to :85-87.

From frontend/src/components/panel/panelOpenSignal.ts:
- requestOpenPanel(): void at :25 (the emitter; ChatLayout already subscribes its expand via subscribeOpenPanel at ChatLayout.tsx:106). No caller exists today, so harness kickoff must call it.

From frontend/src/components/chat/ChatArea.tsx:
- The harness kickoff send path uses workflowMode === "harness" && selectedWorkflowId at :307 to stage the workflow id. requestOpenPanel() fires there.

GET /threads/{id}/workflow returns ThreadWorkflowState: {mode, locked, active_workflow_run_id, run_status, definition_name, current_phase_slug, current_phase_index, total_phases, latest_producer_run_id, ...}. current_phase_index + total_phases seed the honest "Phase i/N" + the N-row skeleton.

DATA-CONTRACT registries (section 5): PHASE_TYPE_LABEL (5 literals -> label+glyph), STATUS_GLYPH (status -> glyph+text+color token), COUNT_FORMULAS (agentsSpawned = tally of sub_agent_start; per-phase tool/source counts SUPPRESSED).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PhaseCard (APG accordion, non-color-only status, failure-with-reason, A11Y-03)</name>
  <read_first>
    - frontend/src/components/panel/PendingAskCard.tsx (the card chrome + a11y + plain-text-children precedent at :162-181; role=group, aria-labelledby, status word in real text + aria-hidden glyph, {prompt} rendered as plain React children, NEVER dangerouslySetInnerHTML)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-UI-SPEC.md (the Copywriting Contract :160-174 = the verbatim failure strings; the A11Y section :261-283 = APG accordion + role=status + aria-busy + icon-only aria-label)
    - .planning/sketches/094-grounding/DATA-CONTRACT.md section 5.3 STATUS_GLYPH + section 6 (EMPTY/LOADING/FAILED/UNKNOWN contract; gate_passed is audit-only, infer pass from advance; reason_unknown sentinel)
    - frontend/src/components/panel/__tests__/FailReason.test.tsx (Plan 01 scaffold; flip INV-3b GREEN)
    - frontend/src/test-fixtures/harness094.ts (the fixtures)
  </read_first>
  <behavior>
    - A done phase renders glyph + the text "Complete"; running renders "Running" + an indeterminate role=progressbar; failed renders "Failed" + the reason; retrying renders "Attempt N" in --accent-violet.
    - FailReason.test.tsx: a run_failed phase renders FAILED (not done) with the reason; run_failed{reason:""} renders the verbatim "Failure reason not captured by the backend" sentinel, never an empty red card.
    - The phase card is an APG accordion: a button with aria-expanded + aria-controls; the panel role=region aria-labelledby; the forced-open active phase carries aria-disabled="true".
  </behavior>
  <action>
    Create `frontend/src/components/panel/PhaseCard.tsx` rendering ONE phase as an APG accordion (mirror PendingAskCard chrome + a11y):
    - Header: `<h3>` wrapping one `<button aria-expanded={open} aria-controls={panelId}>`; the body panel `role="region" aria-labelledby` hidden via the `hidden` attribute when collapsed. The forced-open active (running) phase carries `aria-disabled="true"` (do not remove the button). Keyboard Enter/Space toggles a terminal phase.
    - Status atom = an `aria-hidden` glyph + REAL text label + color token (non-color-only, DATA-CONTRACT 5.3): pending "Locked" (dim --panel-muted-foreground), running "Running" (amber --panel-status-active) + a `role="progressbar"` indeterminate (omit aria-valuenow; NOT a live region), done "Complete" (green --panel-status-done), failed "Failed" (red --destructive lightened text), retrying "Attempt N" (--accent-violet, the Plan 01 token; gated on Plan 01 landing first per Pitfall 3), skipped "Skipped" (dim grey). NEVER --muted-foreground-dim for meaningful text (3.59:1 fail, Pitfall 4).
    - Phase-type label + glyph from PHASE_TYPE_LABEL (the 5 literals; UNKNOWN -> "Step", neutral glyph, never crash).
    - Failure reason block (the RC-4 render half): when status==="failed", auto-expand and render the closed-taxonomy copy VERBATIM from the UI-SPEC Copywriting Contract:
      - max_steps: `Sub-agent "{name}" reached its {max}-step cap without a final answer.` (render ONLY real where-line components: phase.slug + gate_failed.phase; omit model/sub-agent-index/step-ratio which are sub-stream).
      - gate_failed: `Validation gate failed after {attempt} attempt(s); run halted.` where-line `phase: {slug} -> gate · attempt {attempt}/{max}` (attempt/max real; omit the human gate name, not on the wire).
      - wall_clock_timeout: `Phase exceeded its wall-clock budget before completing.` (surface the N from "wall_clock_timeout after Ns" if present).
      - reason_unknown: `Failure reason not captured by the backend — surfaced explicitly so the run is never shown as an empty success.` (MANDATORY fallback when reason/error empty; never an empty red card).
      The taxonomy is a UI-derived classification over gate_failed.error + which event fired (no typed failure_kind field). Failures render in a SEPARATE `role="alert"` (assertive), distinct from the polite announcer.
    - Sub-agent child rows from the phase.subAgents (description + status + summary on done). SUPPRESS per-phase tool/search/source count chips (Pitfall 5 / D-03; they fire on the invisible sub stream). The ONLY counts allowed: "{n} agents" = client tally of sub_agent_start (phase.subAgents.length), run-level end-of-run sources.length (passed in), per-subtopic summary.
    - All agent-supplied text (slug, description, summary, reason, error) renders as plain React text children, NEVER dangerouslySetInnerHTML (T-087-11 XSS guard).

    Flip FailReason.test.tsx INV-3b GREEN (run_failed renders FAILED + reason; reason_unknown sentinel) and run the suite.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/FailReason.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/components/panel/__tests__/FailReason.test.tsx exits 0 (FAILED-not-done + reason_unknown sentinel GREEN)
    - grep "Failure reason not captured by the backend" frontend/src/components/panel/PhaseCard.tsx matches (verbatim sentinel)
    - grep "aria-expanded\|aria-controls\|role=\"region\"\|aria-disabled" frontend/src/components/panel/PhaseCard.tsx returns the APG accordion attributes
    - grep "role=\"alert\"" frontend/src/components/panel/PhaseCard.tsx matches (assertive failure region, separate from the announcer)
    - grep "dangerouslySetInnerHTML" frontend/src/components/panel/PhaseCard.tsx returns 0 (XSS guard)
    - grep "accent-violet" frontend/src/components/panel/PhaseCard.tsx matches (retrying purple, gated on Plan 01)
    - No per-phase tool/search count chip is rendered from a phase event (grep shows no "searches\|tool calls" hardcoded count chip in PhaseCard)
  </acceptance_criteria>
  <done>PhaseCard is an APG accordion with non-color-only status atoms, the verbatim closed-taxonomy failure copy + reason_unknown sentinel in a role=alert, the retrying purple from --accent-violet, suppressed per-phase counts, and plain-text children; FailReason.test.tsx GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build PhaseTimeline (reconcile-then-live skeleton + announcer + axe gate)</name>
  <read_first>
    - frontend/src/components/panel/WorkspacePanel.tsx (how a section reads its hook at :84-87; null-safe data never undefined)
    - .planning/sketches/094-grounding/DATA-CONTRACT.md section 3(c) (RECONCILE truth + the anti-drift rule: reconcile is the floor, live advances forward, NEVER backward) + section 6 (LOADING = full N-row skeleton from total_phases, the spinner-killer)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-UI-SPEC.md (A11Y :274-283: section aria-label, ol/li phases, one role=status aria-live=polite announcer present at load + written ONLY on transition edges + throttled never per-token)
    - frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx + PhaseTimeline.test.tsx (Plan 01 scaffolds; flip INV-4 + INV-2 GREEN)
    - frontend/src/test-fixtures/harness094.ts (fixtures)
  </read_first>
  <behavior>
    - PhaseReconcile.test.tsx INV-4: seeding reconcile {current_phase_index:1,total_phases:3} renders a 3-row skeleton (pending rows beyond the active) with "Phase 2 / 3"; replaying a regressing live event never moves the counter backward.
    - PhaseTimeline.test.tsx INV-2: axe(container) has zero violations for fxRunRunning/fxRunFailed/fxRunDone/fxRunAskuserPaused/fxRunGatefailRetry, with 1-phase and N-phase; the role=status announcer is present at load and fires exactly once per transition; aria-busy flips true->false on stream end.
  </behavior>
  <action>
    Create `frontend/src/components/panel/PhaseTimeline.tsx`:
    - Read `const { data: phases } = usePhases(threadId)` + `const { data: tasks } = useTasks(threadId)` + the reconcile state (definition_name, current_phase_index, total_phases, run_status, mode).
    - Structure: `<section aria-label="Workflow run timeline">` containing a header (definition_name + the honest "Phase {current_phase_index+1} / {total_phases}" chip) + an `<ol aria-label="Phases">` of `<li>` PhaseCard rows (native order + position; no manual aria-setsize).
    - Reconcile-then-live (DATA-CONTRACT 3c, the spinner-killer): on mount the reconcile seeds total_phases + current_phase_index; render the FULL N-row skeleton (N = total_phases) with rows beyond the active in `pending`, the active one `running` — derived from reconcile, NOT a spinner. Live phase events advance the rows FORWARD only; the "Phase i/N" counter NEVER moves backward (reconcile is the floor). Pending rows are total_phases minus phases-seen-live.
    - One `role="status" aria-live="polite"` visually-hidden announcer, PRESENT at page load, written ONLY on transition edges (plain-language milestones: "Phase 2 of 5, Review, started"), throttled — never per-token. Failures use the SEPARATE role=alert in PhaseCard. `aria-busy="true"` on the populating list, flips false on stream end (run terminal).
    - Doing-now narration line composed client-side from the active phase slug + phase_type + (batch) running-vs-total sub-agent counts; "Setting up…" briefly, run_failed.reason copy on failure, "Workflow complete" on run_completed.
    - SUPPRESS the invented count chips (Pitfall 5): render ONLY phases.length, "Phase i/N", "{n} agents" tally, run-level sources.length. No per-phase tool/search count.
    - UNKNOWN phase_type, stale lock, cap_paused, ask_user-paused, draft-missing handled per DATA-CONTRACT section 6 (never crash, never empty/misleading card).

    Flip PhaseReconcile.test.tsx (INV-4) + PhaseTimeline.test.tsx (INV-2) GREEN. Run the suite.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/PhaseTimeline.test.tsx src/components/panel/__tests__/PhaseReconcile.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run PhaseTimeline.test.tsx PhaseReconcile.test.tsx exits 0 (INV-2 axe-clean all states + INV-4 no-backward-counter + skeleton-from-total_phases GREEN)
    - grep "role=\"status\"" frontend/src/components/panel/PhaseTimeline.tsx matches (one polite announcer present at load)
    - grep "aria-live=\"polite\"\|aria-busy" frontend/src/components/panel/PhaseTimeline.tsx matches
    - grep "aria-label=\"Workflow run timeline\"\|<ol" frontend/src/components/panel/PhaseTimeline.tsx matches (section + ol/li)
    - grep "usePhases\|useTasks" frontend/src/components/panel/PhaseTimeline.tsx returns both hooks
    - grep "total_phases" frontend/src/components/panel/PhaseTimeline.tsx matches (skeleton-from-total_phases)
  </acceptance_criteria>
  <done>PhaseTimeline renders the section/ol/li timeline with the reconcile-seeded N-row skeleton, the forward-only counter, the single polite announcer, aria-busy flip, and suppressed counts; PhaseTimeline + PhaseReconcile tests GREEN (axe-clean all states).</done>
</task>

<task type="auto">
  <name>Task 3: Mount the timeline section in WorkspacePanel + PANEL-08 auto-open at harness kickoff</name>
  <read_first>
    - frontend/src/components/panel/WorkspacePanel.tsx (the modified file; hasActivity :95-96 + the section mounts :104-138 + the Versions PanelSection :126-134 shape + the usePhases read site next to :85-87)
    - frontend/src/components/panel/panelOpenSignal.ts (requestOpenPanel :25 emitter; ChatLayout subscribes expand via subscribeOpenPanel at ChatLayout.tsx:106)
    - frontend/src/components/chat/ChatArea.tsx (the harness kickoff send path; workflowMode === "harness" && selectedWorkflowId at :307; this is where requestOpenPanel() fires)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md (the WorkspacePanel analog section; hasActivity extension + section mount)
    - .planning/sketches/094-grounding/DATA-CONTRACT.md section 6 (the timeline section mounts when mode==="harness" OR phases exist; PanelEmpty fallback for deep/no-run)
  </read_first>
  <action>
    In `frontend/src/components/panel/WorkspacePanel.tsx`:
    - Add `const { data: phases } = usePhases(threadId)` next to the existing hook reads (:85-87).
    - Extend `hasActivity` (:95-96) to include `phases.length > 0`: `todos.length > 0 || files.length > 0 || pendingAsks.length > 0 || phases.length > 0` (so the panel does not short-circuit to PanelEmpty during a harness run that has phases but no todos/files/asks).
    - Add a `<PanelSection title="Workflow">` mounting `<PhaseTimeline threadId={threadId} />` AFTER the Versions section (:126-134), conditioned to render when the reconcile mode==="harness" OR phases.length > 0 (DATA-CONTRACT section 6).

    In `frontend/src/components/chat/ChatArea.tsx`:
    - At the harness kickoff send path (where workflowMode === "harness" && selectedWorkflowId stages the workflow id, ~:303-307), call `requestOpenPanel()` (imported from @/components/panel/panelOpenSignal) so the panel auto-opens to the timeline on entering Harness Mode (PANEL-08 auto-open; the ChatLayout expand seam is already subscribed). Fire it ONLY on the harness branch, never on a Deep send.

    Do NOT touch the chat-bucket tool path, ToolCallPanel.tsx, or MessageItem.tsx (G-5 satisfied; D-01 keeps Deep cards in chat).
  </action>
  <verify>
    <automated>cd frontend && grep -c "PhaseTimeline" src/components/panel/WorkspacePanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "PhaseTimeline" frontend/src/components/panel/WorkspacePanel.tsx returns >=1 (section mounted)
    - grep "phases.length" frontend/src/components/panel/WorkspacePanel.tsx matches (hasActivity extended)
    - grep "requestOpenPanel" frontend/src/components/chat/ChatArea.tsx matches (PANEL-08 auto-open at harness kickoff)
    - grep "requestOpenPanel" frontend/src/components/chat/ChatArea.tsx shows the call is inside the harness branch (not on a Deep send)
    - git diff confirms NO change to ToolCallPanel.tsx, MessageItem.tsx, or the StreamsProvider chat-bucket path (these files are not in files_modified)
    - cd frontend and npm run build exits 0
  </acceptance_criteria>
  <done>The PhaseTimeline section mounts as the 5th WorkspacePanel section gated on mode==="harness" OR phases exist; hasActivity includes phases; requestOpenPanel fires at harness kickoff only; no chat hot-file touched; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-rendered text to DOM | phase slug, description, summary, draft, failure reason, error are agent/LLM-influenced strings rendered in the panel |
| panel-only state to chat | the timeline must not re-render the chat message list (PANEL-06/09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094-03-01 | Tampering / XSS | PhaseCard/PhaseTimeline agent text | mitigate | All agent-supplied text renders as plain React text children; NEVER dangerouslySetInnerHTML (grep acceptance proves 0); mirrors the established PendingAskCard T-087-11 guard. |
| T-094-03-02 | Repudiation / trust | failure render | mitigate | RC-4: a failed run renders failed-with-reason (closed taxonomy + reason_unknown sentinel), never a silent empty done — the run is never misrepresented as a success. |
| T-094-03-03 | Information disclosure | suppressed counts | mitigate | Per-phase tool/search/source counts (sub-stream only) are SUPPRESSED, never faked (D-03); no invented number is shown that could misrepresent run activity. |
| T-094-03-04 | Denial of service | chat re-render via panel mount | accept | The timeline reads usePhases/useTasks (panel-only slices via Plan 02); it never reads bucketsBySurface; PANEL-09 isolation is structurally preserved (Plan 02 INV-1 proves it). |
</threat_model>

<verification>
- `npx vitest run src/components/panel/__tests__/PhaseTimeline.test.tsx src/components/panel/__tests__/FailReason.test.tsx src/components/panel/__tests__/PhaseReconcile.test.tsx` GREEN (INV-2 + INV-3b + INV-4)
- `cd frontend && npm run test && npm run build` clean
- grep confirms APG accordion + role=status + role=alert + no dangerouslySetInnerHTML + suppressed counts + requestOpenPanel at harness kickoff
- (manual, VALIDATION.md) Chrome MCP both-themes real contrast: status/title >=4.5:1, --accent-violet graphic >=3:1
</verification>

<success_criteria>
- PhaseCard (APG accordion, non-color-only status, verbatim failure taxonomy + reason_unknown, retrying purple) + PhaseTimeline (reconcile skeleton, forward-only counter, single announcer, suppressed counts) render
- PhaseTimeline/FailReason/PhaseReconcile tests GREEN (axe-clean all states + RC-4 render + no-backward-counter)
- Timeline mounted in WorkspacePanel; PANEL-08 auto-open at harness kickoff; no chat hot-file touched
</success_criteria>

<output>
After completion, create `.planning/phases/094-workflow-legibility-mode-clarity/094-03-SUMMARY.md`
</output>
