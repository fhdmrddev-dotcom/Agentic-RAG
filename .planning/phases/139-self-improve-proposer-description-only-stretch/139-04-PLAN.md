---
phase: 139-self-improve-proposer-description-only-stretch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [SI-02]

must_haves:
  truths:
    - "The SkillProposal type carries kind, proposed_description, base_description, scoreboard_snapshot (a TunerScoreboard-shaped blob), and source_tuner_run_id so the description card can render the diff + per-provider scoreboard (D-09, D-10)"
    - "proposeDescription(skillId, runId) POSTs { run_id } to /skills/{id}/description-proposals and returns a SkillProposal; approveDescriptionProposal / rejectDescriptionProposal hit the /description-proposals/{id}/approve|reject routes and return a plain SkillProposal (no re_eval_run_id — D-07)"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "Extended SkillProposal (description fields) reusing the existing TunerScoreboard shape for scoreboard_snapshot"
      contains: "proposed_description"
    - path: "frontend/src/lib/api.ts"
      provides: "proposeDescription + approveDescriptionProposal + rejectDescriptionProposal wire functions"
      contains: "description-proposals"
  key_links:
    - from: "frontend/src/lib/api.ts proposeDescription"
      to: "POST /skills/{id}/description-proposals"
      via: "fetch with { run_id } body + getAuthHeaders()"
      pattern: "description-proposals"
---

<objective>
Define the frontend contracts SI-02's UI consumes: extend the `SkillProposal` type with the
description fields and add the `proposeDescription` / `approveDescriptionProposal` /
`rejectDescriptionProposal` wire functions. Interface-first — the Wave 2 card + page plan (139-05)
builds against these.

Purpose: the card renders `lineDiff(base_description, proposed_description)` + a `ProviderScoreboard`
from `scoreboard_snapshot`, so the type must carry those fields; the wires mirror the existing
`proposeImprovement`/`approveProposal`/`rejectProposal` shape (D-13 additive).
Output: extended `SkillProposal` + three description-proposal wire functions.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-self-improve-proposer-description-only-stretch/139-PATTERNS.md

<interfaces>
<!-- Existing shapes to extend/mirror. Keep the status union unchanged (description rows use a subset). -->

types/index.ts:
  SkillProposal (:764): id, skill_id, base_skill_version_id, new_skill_version_id|null,
    re_eval_run_id|null, source_eval_run_id|null, proposed_instructions, base_instructions,
    rationale, evidence_summary, status (7-value union), override_forced, gate?, created_at, updated_at.

api.ts:
  TunerScoreboard (:3170): { skill_id, candidates: TunerCandidate[], winner_index|null, winner_description|null }
  TunerCandidate (:3161): { index, description, cells: TunerCell[], held_out_score, is_baseline }
  TunerCell (:3151): { provider, model, axes:{fires,no_false}, score, measured?, error_count? }
  proposeImprovement(skillId, sourceEvalRunId) -> SkillProposal  (POST /skills/{id}/proposals, body {source_eval_run_id})
  approveProposal(skillId, proposalId) -> ProposalApproveResult   (POST .../approve)
  rejectProposal(skillId, proposalId) -> SkillProposal            (POST .../reject)
  proposalError(res, fallback) helper (:~1860) for error hydration
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the SkillProposal type with the description fields</name>
  <files>frontend/src/types/index.ts</files>
  <read_first>
    - frontend/src/types/index.ts (SkillProposal :764-788, PromotionGate :749-758, nullable idiom + doc-comment discipline :760-763)
    - frontend/src/lib/api.ts (TunerScoreboard / TunerCandidate / TunerCell shapes :3151-3175 — for scoreboard_snapshot typing)
    - .planning/phases/139-self-improve-proposer-description-only-stretch/139-PATTERNS.md (types section, lines 107-114)
  </read_first>
  <action>
    In `frontend/src/types/index.ts`, extend the existing `SkillProposal` interface (do NOT fork a new
    type): add `kind: "instruction" | "description"`; `proposed_description: string | null`;
    `base_description: string | null`; `source_tuner_run_id: string | null`; and
    `scoreboard_snapshot: TunerScoreboard | null` (reuse the wire shape from `api.ts` —
    import/reference the `TunerScoreboard` type, or a structurally-equivalent local alias, so the card
    can feed its winner/baseline cells straight into `ProviderScoreboard`). Keep the existing status
    union unchanged (description rows only ever use `proposed`/`rejected`/`approved`/`promoted`).
    Mirror the existing nullable idiom and doc-comment discipline; note in a comment that
    `proposed_instructions`/`base_instructions` may be empty strings for a `kind='description'` row.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | grep -iE "error" | head -5 || echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `SkillProposal` includes `kind`, `proposed_description`, `base_description`,
      `scoreboard_snapshot` (TunerScoreboard-shaped), and `source_tuner_run_id`.
    - No TypeScript errors introduced (the tsc check prints `TSC_OK` or shows no new errors in
      `types/index.ts`).
    - The status union is unchanged.
  </acceptance_criteria>
  <done>The SkillProposal type carries every description field the card renders — diff base, proposed text, and the per-provider scoreboard snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Add proposeDescription / approveDescriptionProposal / rejectDescriptionProposal wires</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (proposeImprovement :1876-1888, approveProposal :1915-1926, rejectProposal :1945-1956, proposalError helper)
    - .planning/phases/139-self-improve-proposer-description-only-stretch/139-PATTERNS.md (api.ts section, lines 117-124)
  </read_first>
  <action>
    In `frontend/src/lib/api.ts`, add three wire functions mirroring the existing proposal wires but
    hitting the `/description-proposals` routes:
    `proposeDescription(skillId: string, runId: string): Promise<SkillProposal>` — POST
    `${API_BASE}/skills/${skillId}/description-proposals` with `getAuthHeaders()` and body
    `JSON.stringify({ run_id: runId })`; on `!res.ok` throw `await proposalError(res, "Failed to
    propose description")`; return `res.json()`.
    `approveDescriptionProposal(skillId, proposalId): Promise<SkillProposal>` — POST
    `.../description-proposals/${proposalId}/approve`; returns a PLAIN `SkillProposal` (NO
    `ProposalApproveResult` / `re_eval_run_id` — there is no re-eval, D-07).
    `rejectDescriptionProposal(skillId, proposalId): Promise<SkillProposal>` — POST
    `.../description-proposals/${proposalId}/reject`; returns `SkillProposal`.
    Optionally add `getLatestDescriptionProposal(skillId): Promise<SkillProposal | null>` — GET
    `.../description-proposals` (newest-first), returning the first row or null, for rehydration-on-open.
    Reuse `getAuthHeaders()` and `proposalError` — do not hand-roll auth or error handling.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && grep -q "description-proposals" src/lib/api.ts && grep -q "proposeDescription" src/lib/api.ts && grep -q "approveDescriptionProposal" src/lib/api.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v node_modules | grep -iE "src/lib/api.ts.*error" | head -3 || echo API_OK</automated>
  </verify>
  <acceptance_criteria>
    - `api.ts` exports `proposeDescription`, `approveDescriptionProposal`, `rejectDescriptionProposal`
      (and optionally `getLatestDescriptionProposal`), all targeting `/skills/{id}/description-proposals`.
    - `proposeDescription` POSTs `{ run_id }`; `approveDescriptionProposal` returns a plain
      `SkillProposal` (not `ProposalApproveResult`).
    - All use `getAuthHeaders()` + `proposalError`; no new TypeScript errors in `api.ts`.
  </acceptance_criteria>
  <done>The three description-proposal wire functions exist, typed to the extended SkillProposal, ready for the card + page in 139-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | The wire functions attach `getAuthHeaders()`; the token (not any body field) carries identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-12 | Spoofing | forged owner/skill identity from the client | mitigate | The wires send only `run_id` + path ids; identity is the auth token via `getAuthHeaders()` — the server derives `user_id` from the token (T-135-02), never a body field |
| T-139-13 | Information Disclosure | leaking another user's proposal on a wire error | mitigate | Errors flow through `proposalError` on `!res.ok`; the server returns 404-not-403 (Plan 139-02), so the client never receives a cross-user existence signal |
| T-139-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses the first-party `lineDiff`/scoreboard shapes and existing fetch helpers; nothing to slopcheck |
</threat_model>

<verification>
- `grep description-proposals src/lib/api.ts` matches; `proposeDescription`/`approveDescriptionProposal`/
  `rejectDescriptionProposal` exported.
- `npx tsc --noEmit` introduces no new errors in `types/index.ts` or `api.ts`.
</verification>

<success_criteria>
- `SkillProposal` extended with the description fields (scoreboard_snapshot TunerScoreboard-shaped).
- Three (optionally four) description-proposal wire functions added, additively, no shared-path change.
</success_criteria>

<output>
Create `.planning/phases/139-self-improve-proposer-description-only-stretch/139-04-SUMMARY.md` when done.
</output>
