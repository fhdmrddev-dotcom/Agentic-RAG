---
phase: 139-self-improve-proposer-description-only-stretch
plan: 03
type: execute
wave: 2
depends_on: ["139-01"]
files_modified:
  - backend/tests/test_139_migration_090.py
  - supabase/full-schema.sql
autonomous: false
requirements: [SI-02]

must_haves:
  truths:
    - "Migration 090 is applied to the LIVE local DB via the Supabase SQL editor or psycopg2 :54322 — NEVER supabase db push / db reset (preserves dev data) (D-14)"
    - "After apply, a kind='description' skill_proposals INSERT with NULL proposed_description is rejected by the skill_proposals_kind_fields CHECK, and an instruction row with NULL proposed_instructions now inserts (proposed_instructions relaxed) — proving the live schema matches the migration (D-11)"
    - "supabase/full-schema.sql is regenerated from the live DB with no --reset and committed together with migration 090 (D-14)"
  artifacts:
    - path: "backend/tests/test_139_migration_090.py"
      provides: "DB-CHECK test: kind-gated partial CHECK rejects a description row with NULL proposed_description; proposed_instructions nullable"
      contains: "test_kind_check_constraint"
    - path: "supabase/full-schema.sql"
      provides: "Regenerated single-file deploy artifact reflecting the applied migration 090"
      contains: "scoreboard_snapshot"
  key_links:
    - from: "supabase/migrations/090_skill_proposals_description_kind.sql"
      to: "live local DB (:54322) + supabase/full-schema.sql"
      via: "SQL editor / psycopg2 apply then regenerate-full-schema.sh (no --reset)"
      pattern: "skill_proposals_kind_fields"
---

<objective>
Apply migration 090 to the live local DB and regenerate the deploy artifact — the [BLOCKING] gate the
route behavior (against the real DB) and live UAT depend on. Also author the DB-CHECK test that can
only pass once the constraint is live.

Purpose: build/type checks pass WITHOUT the live migration (types come from config, not the DB),
creating a false-positive verification state. This plan makes the schema real so
`test_139_migration_090.py` and the phase's live UAT are honest (RESEARCH § Critical Finding).
Output: migration 090 live on :54322, `full-schema.sql` regenerated + committed with the migration,
and the kind-CHECK test green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/139-self-improve-proposer-description-only-stretch/139-RESEARCH.md
@supabase/migrations/090_skill_proposals_description_kind.sql
@supabase/SETUP.md

<interfaces>
<!-- Migration discipline (CLAUDE.md + D-14) — the apply is human-gated; NEVER db push / db reset. -->
Apply path: paste 090 into the Supabase SQL editor OR run it via psycopg2 against 127.0.0.1:54322.
Regen: bash scripts/regenerate-full-schema.sh   (default = live-DB dump, NO --reset — preserves dev data)
Commit: the migration file (from Plan 139-01) + the regenerated supabase/full-schema.sql together.
DB-CHECK test: reuse the psycopg2 :54322 connection pattern from existing migration tests
(evidence tool: psycopg2 on 127.0.0.1:54322, port per supabase status).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the migration-090 DB-CHECK test (kind-gated CHECK + nullable proposed_instructions)</name>
  <files>backend/tests/test_139_migration_090.py</files>
  <read_first>
    - backend/tests/test_skill_proposals.py (nearest schema-level sibling — mirror its DB-constraint assertion style if it uses psycopg2 :54322; otherwise use the psycopg2 :54322 connection pattern from another live-DB test)
    - supabase/migrations/090_skill_proposals_description_kind.sql (the constraint being asserted)
    - .planning/phases/139-self-improve-proposer-description-only-stretch/139-VALIDATION.md (V6 row)
  </read_first>
  <action>
    Create `backend/tests/test_139_migration_090.py`. Connect to the live local DB (psycopg2,
    127.0.0.1:54322). Write `test_kind_check_constraint`: attempt to INSERT a `skill_proposals` row
    with `kind='description'` and `proposed_description=NULL` (with valid FKs / a seeded skill+version)
    → assert the DB raises a check_violation (the `skill_proposals_kind_fields` CHECK). Also assert a
    `kind='description'` row WITH `proposed_description` set and `proposed_instructions=NULL` inserts
    successfully (proving `proposed_instructions` was relaxed to nullable). Roll back / clean up any
    seeded rows so the test is idempotent. Mark/skip gracefully if the DB is unreachable so the suite
    does not hard-fail in a no-DB CI lane, but the intent is that it PASSES against the live dev DB
    after Task 2 applies 090.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python -m pytest tests/test_139_migration_090.py -q --no-header 2>&1 | tail -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_139_migration_090.py` exists with `test_kind_check_constraint`.
    - The test asserts BOTH branches: NULL `proposed_description` on a description row is rejected, and
      a description row with `proposed_instructions=NULL` is accepted.
    - Before Task 2 applies 090, this test is RED (constraint absent) or skips on no-DB — that is
      expected; it goes green only after the live apply.
  </acceptance_criteria>
  <done>The kind-gated CHECK is pinned by a live-DB test, ready to prove the migration applied correctly.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 090 to the live local DB, regenerate full-schema.sql, commit together</name>
  <files>supabase/migrations/090_skill_proposals_description_kind.sql, supabase/full-schema.sql</files>
  <read_first>
    - supabase/migrations/090_skill_proposals_description_kind.sql
    - CLAUDE.md (migration discipline — apply via SQL editor / psycopg2, never db push / db reset)
    - scripts/regenerate-full-schema.sh (the no-reset live-DB dump)
  </read_first>
  <action>
    [BLOCKING — autonomous:false, human-gated.] Apply migration 090 to the LIVE local DB, then
    regenerate the deploy artifact and commit both files. This is a manual, operator-gated step because
    the migration write to the live DB is a project-mandated human action (D-14) — the executor pauses
    here and presents the steps below, then resumes on the operator's confirmation. Do NOT run
    `supabase db push` / `db reset`.
  </action>
  <what-built>
    Migration 090 (authored in Plan 139-01) is ready to apply. This checkpoint applies it to the live
    local DB WITHOUT any destructive reset, then regenerates the deploy artifact and commits both
    files together, and finally proves the constraint is live via the Task-1 test.
  </what-built>
  <how-to-verify>
    1. Apply `supabase/migrations/090_skill_proposals_description_kind.sql` to the LIVE local DB —
       paste it into the Supabase SQL editor OR run it via psycopg2 against 127.0.0.1:54322. Do NOT run
       `supabase db push` or `supabase db reset` (they wipe dev data — D-14).
    2. Regenerate the deploy artifact: `bash scripts/regenerate-full-schema.sh` (NO `--reset`).
    3. Confirm `supabase/full-schema.sql` now contains the new columns (grep for `scoreboard_snapshot`
       and `skill_proposals_kind_fields`).
    4. Run the DB-CHECK test:
       `cd backend && ./venv/Scripts/python -m pytest tests/test_139_migration_090.py -x -q` → GREEN.
    5. Commit migration 090 + the regenerated `supabase/full-schema.sql` together in one commit.
  </how-to-verify>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "scoreboard_snapshot" supabase/full-schema.sql && grep -q "skill_proposals_kind_fields" supabase/full-schema.sql && echo REGEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/full-schema.sql` contains `scoreboard_snapshot` and `skill_proposals_kind_fields` (the
      regen ran against a DB that has 090 applied).
    - `pytest tests/test_139_migration_090.py -x -q` passes (the live CHECK rejects the bad row).
    - The migration + regenerated full-schema.sql are committed together (no `supabase db push`/`reset`
      was used).
  </acceptance_criteria>
  <resume-signal>Type "applied" once 090 is live on :54322, full-schema.sql is regenerated + committed, and the DB-CHECK test is green — or describe any apply error.</resume-signal>
  <done>Migration 090 is live (dev data preserved), full-schema.sql regenerated + committed with it, and the kind-gated CHECK proven by a passing DB test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live local DB | The migration is applied by a human via the SQL editor / psycopg2 (reviewed DDL) |
| regenerated full-schema.sql → cloud deploy | The single-file artifact is the greenfield deploy source; it must match the live schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-10 | Tampering | destructive apply path wiping dev data | mitigate | Apply via SQL editor / psycopg2 only; `regenerate-full-schema.sh` runs with NO `--reset` (live-DB dump preserves data) — never `supabase db push`/`db reset` (D-14) |
| T-139-11 | Repudiation | schema drift between migration and full-schema.sql | mitigate | Regenerate full-schema.sql from the live DB and commit it in the SAME commit as migration 090 (deploy-parity) |
| T-139-SC | Tampering | npm/pip/cargo installs | accept | No package installs — SQL apply + full-schema regen only; nothing to slopcheck |
</threat_model>

<verification>
- `grep scoreboard_snapshot supabase/full-schema.sql` and `grep skill_proposals_kind_fields
  supabase/full-schema.sql` both match.
- `pytest tests/test_139_migration_090.py -x -q` green.
- The commit contains both migration 090 and the regenerated full-schema.sql.
</verification>

<success_criteria>
- Migration 090 is live on the local DB (dev data preserved), full-schema.sql regenerated + committed
  with it, and the kind-gated CHECK proven by a passing DB test.
- This unblocks the description routes against the real DB and the phase's live UAT.
</success_criteria>

<output>
Create `.planning/phases/139-self-improve-proposer-description-only-stretch/139-03-SUMMARY.md` when done.
</output>
