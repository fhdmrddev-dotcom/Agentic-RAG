---
phase: 101-template-fill-integrity-validation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/unit/test_template_render.py
  - backend/tests/unit/test_template_integrity.py
  - backend/tests/fixtures/templates/risk-register.docx
  - backend/tests/fixtures/templates/arbitrary-split-token.docx
  - backend/tests/fixtures/templates/table-deck.pptx
  - backend/tests/fixtures/templates/chart-book.xlsx
  - backend/tests/fixtures/templates/make_fixtures.py
  - backend/Dockerfile.sandbox
autonomous: true
requirements: [TMPL-02, TMPL-03]
user_setup:
  - service: docker
    why: "Rebuild the sandbox image with docxtpl so production render (Phase 101 tool) has the engine; the venv install is for the TEST tier only."
    dashboard_config:
      - task: "After the Dockerfile.sandbox edit lands, rebuild + bump the SANDBOX_IMAGE env var (Claude provides the exact commands in the task action). UAT must use FRESH chats — cached sandbox sessions keep the old image until idle eviction."
        location: "Local shell + backend/.env"

must_haves:
  truths:
    - "The single cross-plan TDD test files exist and the full backend suite stays exit-0 (RED-by-design stubs are xfail until their implementing plan lands)"
    - "docxtpl is installed in the backend venv for the TEST tier so the deterministic helpers are unit-testable"
    - "docxtpl==0.20.2 is added to Dockerfile.sandbox so production render has the engine after a rebuild"
    - "Real OOXML fixtures exist: a {%tr %} docx, a docx with a run-fragmented {{token}}, a pptx with a table, a chart-bearing xlsx"
  artifacts:
    - path: "backend/tests/unit/test_template_render.py"
      provides: "TMPL-02 deterministic-core cross-plan TDD contract (field-map, coverage, run-merge, engine selection, render-grows-rows)"
      contains: "xfail"
    - path: "backend/tests/unit/test_template_integrity.py"
      provides: "TMPL-03 cross-plan TDD contract (corrupt-rejected, autoescape, SSTI routing)"
      contains: "xfail"
    - path: "backend/tests/fixtures/templates/make_fixtures.py"
      provides: "Deterministic regeneration of all 4 OOXML fixtures"
    - path: "backend/Dockerfile.sandbox"
      provides: "docxtpl==0.20.2 in the pinned pip block"
      contains: "docxtpl==0.20.2"
  key_links:
    - from: "backend/tests/unit/test_template_render.py"
      to: "backend/tests/fixtures/templates/"
      via: "fixture path constants resolved relative to the test file"
      pattern: "fixtures/templates"
---

<objective>
Wave 0 scaffold for Phase 101. Create the two cross-plan TDD test files (the single files every downstream plan's `<verify>` runs a `-k` slice of), the four real OOXML fixtures the deterministic-core tests need, install `docxtpl==0.20.2` into the backend venv for the TEST tier, and add `docxtpl==0.20.2` to `Dockerfile.sandbox` (the one net-new production dependency) so the operator can rebuild before live UAT.

Purpose: Nyquist requires every downstream task to have an `<automated>` verify or a Wave 0 stub. This plan creates those stubs (RED-by-design, `xfail(strict=False)` until the implementing plan lands — the 098/099/100 convention) plus the fixtures and the build artifact prerequisite. No production `backend/app/**` code in this plan.
Output: `test_template_render.py`, `test_template_integrity.py`, 4 OOXML fixtures + `make_fixtures.py`, the `Dockerfile.sandbox` docxtpl add.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-template-fill-integrity-validation/101-VALIDATION.md
@.planning/phases/101-template-fill-integrity-validation/101-PATTERNS.md

<interfaces>
<!-- The Phase 100 cross-plan TDD contract is the analog. Mirror its structure exactly. -->
From backend/tests/test_workspace_template.py (the analog — DO NOT modify it; mirror its shape):
- A module docstring listing which symbols flip GREEN in which downstream plan.
- RED-by-design tests use `pytest.mark.xfail(strict=False)`.
- Imports of not-yet-created symbols live INSIDE the test body so an ImportError surfaces as xfail, NOT a collection error.
- Offline-friendly: no live DB / Storage; the cross-provider half stays MANUAL in VALIDATION.md.

The production module these stubs target (created in Plan 02) is `backend/app/services/template_render_service.py`. Expected importable symbols (the contract the stubs assert against):
  - `Cited` (Pydantic BaseModel: value, source_chunk_id, source_doc, source_page — all nullable)
  - `build_generic_field_map_model(placeholder_keys) -> type[BaseModel]`  OR a fixed envelope `GenericFieldMap` (scalars: dict[str, Cited], collections: dict[str, list[dict[str, Cited]]]) — Plan 02 pins the shape; the stub imports whatever Plan 02 exports and is xfail until then
  - `check_coverage(fm_dict, retrieved_ids, placeholder_keys) -> dict`  (port of derive_fields.check_coverage)
  - `is_truncated(meta) -> bool`  (the stop_reason==max_tokens / finish_reason==length guard)
  - `build_context(field_map_dict, *, numeric_hook=None) -> dict`  (port of render_docx.build_context + the D-11 worded→numeric hook)
  - `render_docx_template(template_path, context, out_path) -> dict`  (port of render_docx.render — DocxTemplate + SandboxedEnvironment(autoescape=True))
  - `run_replace_docx(template_path, field_map_scalars, out_path) -> dict`  (the arbitrary-path run-coalescing scalar replace — Plan 02 net-new)
  - `assert_integrity(out_path, fmt) -> dict`  (re-open with the format's library + residual_tags scan; fmt in {docx,pptx,xlsx})
  - `residual_tags_in(out_path, fmt) -> list[str]`
  - `select_engine(provenance) -> str`  ("docxtpl" for AssetRef/library, "run_replace" for kind='template_input')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the four OOXML fixtures + make_fixtures.py</name>
  <files>backend/tests/fixtures/templates/make_fixtures.py, backend/tests/fixtures/templates/risk-register.docx, backend/tests/fixtures/templates/arbitrary-split-token.docx, backend/tests/fixtures/templates/table-deck.pptx, backend/tests/fixtures/templates/chart-book.xlsx</files>
  <read_first>
    - scripts/spike-097/make_template.py — the {%tr %} risk-register.docx generator (the docx fixture analog; lift its `{%tr %}` table + `{{ }}` scalar tag construction)
    - scripts/spike-097/render_docx.py:144 (residual_tags) — confirms the token markers the fixtures must carry: `{{`, `}}`, `{%`, `%}`
    - .planning/phases/101-template-fill-integrity-validation/101-RESEARCH.md → "## Common Pitfalls" Pitfall 1 (the run-fragmented {{token}} fixture spec) + "## State of the Art" (pptx table / xlsx chart limits the fixtures must exhibit)
    - backend/Dockerfile.sandbox:37-49 (the pinned library versions: python-docx==1.1.2, python-pptx==1.0.2, openpyxl==3.1.5 — the fixtures must round-trip on these)
  </read_first>
  <action>
Create `backend/tests/fixtures/templates/make_fixtures.py` — a deterministic, runnable generator (so the binaries can be regenerated and reviewed). It writes 4 files into the same directory:

1. `risk-register.docx` — PORT scripts/spike-097/make_template.py: a docx with a 1-row table carrying a `{%tr for r in rows %}` ... `{%tr endfor %}` repeat row whose cells contain `{{ r.risk_id.value }}`, `{{ r.cause.value }}`, `{{ r.event.value }}`, `{{ r.effect.value }}`, `{{ r.probability.value }}`, `{{ r.impact.value }}`, `{{ r.score }}`, `{{ r.response_strategy.value }}`, `{{ r.owner.value }}`, `{{ r.status.value }}`, plus body scalar tags `{{ project_name.value }}` and `{{ report_date.value }}`. Build with `from docx import Document` (NOT docxtpl — the raw markup is what docxtpl renders later). `DocxTemplate(path).get_undeclared_template_variables()` on this file must return a set containing `project_name`, `report_date`, `rows`.

2. `arbitrary-split-token.docx` — a docx where a literal token `{{client_name}}` is intentionally FRAGMENTED across multiple `<w:r>` runs (the Pitfall-1 reproduction). Build it by adding a paragraph then appending three runs to that paragraph's `_p` element directly: write `{{cli` to run 1, `ent_` to run 2, `name}}` to run 3 (use `paragraph.add_run("...")` three times — each add_run is a separate `<w:r>`). Also add a second clean paragraph with a whole-in-one-run token `{{report_title}}` (the un-fragmented control). NO Jinja `{%...%}` blocks — this is the arbitrary scalar-only path.

3. `table-deck.pptx` — `from pptx import Presentation`; one slide with a 2-row × 2-col table whose cells carry scalar tokens `{{metric}}` / `{{value}}` and a title placeholder `{{deck_title}}`. The table exists to exercise the python-pptx "cannot grow tables" documented limit (Plan 03 verdict note); scalar fill must round-trip.

4. `chart-book.xlsx` — `from openpyxl import Workbook` + `from openpyxl.chart import BarChart, Reference`; one worksheet with scalar token cells (e.g. A1=`{{title}}`, B2 numeric data) AND a real `BarChart` added to the sheet (so the openpyxl chart-strip-on-save limit is reproducible — Plan 03 documents it). Include a merged-cell range (`ws.merge_cells("A4:B4")` with the anchor `A4=`{{merged_note}}``) for the merged-cell-mis-write failure mode (SC#4 #6).

Run the generator and commit the 4 produced binaries. These are small (<30 KB each). Keep `make_fixtures.py` so they are reproducible/reviewable (do not hand-edit binaries).
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe backend/tests/fixtures/templates/make_fixtures.py && backend/venv/Scripts/python.exe -c "from docxtpl import DocxTemplate; v=DocxTemplate('backend/tests/fixtures/templates/risk-register.docx').get_undeclared_template_variables(); assert {'project_name','report_date','rows'} <= v, v; print('OK', sorted(v))"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist under `backend/tests/fixtures/templates/`
    - `DocxTemplate('.../risk-register.docx').get_undeclared_template_variables()` is a superset of `{project_name, report_date, rows}`
    - `arbitrary-split-token.docx`: the first paragraph's `runs` list has length >= 3 and the concatenated paragraph text contains `{{client_name}}` (verifiable: `Document(path).paragraphs[0].text` contains the whole token while no single `run.text` does)
    - `table-deck.pptx` opens via `Presentation(path)` and has a table on slide 1
    - `chart-book.xlsx` opens via `load_workbook(path)` and the source workbook had a chart added (the strip-on-save is what Plan 03 documents)
    - `make_fixtures.py` is runnable and idempotent (re-running overwrites cleanly)
  </acceptance_criteria>
  <done>The 4 OOXML fixtures exist + regenerate from make_fixtures.py; risk-register.docx exposes the 3 top-level placeholder keys; the split-token docx reproduces the multi-run fragmentation.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two cross-plan TDD test files (RED-by-design stubs) + install docxtpl in the venv</name>
  <files>backend/tests/unit/test_template_render.py, backend/tests/unit/test_template_integrity.py</files>
  <read_first>
    - backend/tests/test_workspace_template.py:1-60 — the cross-plan TDD contract structure to mirror EXACTLY (docstring listing per-plan GREEN symbols; xfail(strict=False); inside-body imports)
    - backend/tests/test_099_skill_composition.py:14-21 — the xfail(strict=False) convention for not-yet-created symbols
    - scripts/spike-097/derive_fields.py:112-170 — the check_coverage shape the test asserts against (covered_keys, uncited_value_count, invented_citation_count, citation_coverage_pct)
    - scripts/spike-097/render_docx.py:105-174 — render() + assert_integrity() + residual_tags() the integrity tests assert against
    - .planning/phases/101-template-fill-integrity-validation/101-VALIDATION.md → "## Per-Task Verification Map" (the exact test NAMES required) + "### SC#4 Named Failure Modes"
  </read_first>
  <action>
First install the TEST-tier engine: run `backend/venv/Scripts/pip install docxtpl==0.20.2` (test-tier render only — production render is the sandbox; the venv install lets the deterministic helpers be unit-tested per RESEARCH §Validation Architecture).

Create `backend/tests/unit/test_template_render.py` (TMPL-02 deterministic core). Mirror `test_workspace_template.py`'s structure: module docstring mapping each test to its implementing plan; `pytest.mark.xfail(strict=False, reason="Plan 101-02: <symbol> not yet implemented")` on every RED stub; all imports of `app.services.template_render_service` symbols INSIDE the test body. Define these tests with the EXACT names from 101-VALIDATION.md:
  - `test_field_map_covers_template_keys` — import the Plan-02 field-map shape; assert it covers `risk-register.docx`'s `get_undeclared_template_variables()` keys; assert `Cited` leaves are all nullable (`Cited().value is None`).
  - `test_check_coverage_flags_uncited_and_invented` — feed a hand-built fm_dict with (a) a value + a valid `source_chunk_id`, (b) a value + a `source_chunk_id` NOT in retrieved_ids (invented), (c) a value + null `source_chunk_id` (uncited); assert `check_coverage` returns `invented_citation_count==1` and `uncited_value_count==1`.
  - `test_truncated_emission_rejected` — assert `is_truncated({"stop_reason":"max_tokens"})` is True and `is_truncated({"stop_reason":"tool_use"})` is False; assert the finish_reason=="length" alias too.
  - `test_trusted_render_grows_rows` — parametrize `[1, 5, 20]`; build a context with N rows via `build_context`, call `render_docx_template(risk-register.docx, ctx, out)`, re-open via `assert_integrity(out, "docx")`, assert table rows == header(1) + N (the Pitfall-5 row-growth probe — the spike's load-bearing surprise).
  - `test_run_merge_replaces_split_token` — call `run_replace_docx(arbitrary-split-token.docx, {"client_name":"Acme Co"}, out)`; re-open; assert the produced text contains `Acme Co` and `residual_tags_in(out,"docx") == []` (the split token was reassembled; SC#4 #1).
  - `test_engine_selection_by_provenance` — assert `select_engine("library"/AssetRef) == "docxtpl"` and `select_engine("template_input") == "run_replace"`.

Create `backend/tests/unit/test_template_integrity.py` (TMPL-03). Same conventions. Tests:
  - `test_corrupt_file_never_delivered` — write truncated/garbage bytes to a `.docx` path, assert `assert_integrity(path, "docx")` RAISES (or returns opened=False) so the caller never delivers it (SC#4 #3).
  - `test_autoescape_contains_xml_special_chars` — build a context whose scalar value is `Acme & <Corp>`, render via the docxtpl path, re-open, assert the literal text survives (no repair) and the special chars round-trip (SC#4 #2 / Pitfall 2).
  - `test_template_input_routes_to_non_jinja_engine` — assert the provenance for an ephemeral upload (`kind='template_input'`) resolves to the `run_replace` engine and NEVER to docxtpl/Jinja (the SSTI-structurally-impossible guard; a static/structural assertion is acceptable — e.g. `select_engine("template_input") != "docxtpl"`).

Run the suite to confirm exit-0 (all stubs xfail, no collection errors).
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_template_render.py backend/tests/unit/test_template_integrity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/unit/test_template_render.py` defines all 6 named render tests; `backend/tests/unit/test_template_integrity.py` defines all 3 named integrity tests
    - `grep -c "xfail" backend/tests/unit/test_template_render.py` >= 6 and the integrity file >= 3
    - The pytest run exits 0 (every RED stub is xfailed, no ImportError collection failure)
    - `backend/venv/Scripts/python.exe -c "import docxtpl; print(docxtpl.__version__)"` prints `0.20.2`
    - Every `app.services.template_render_service` import lives INSIDE a test body (grep: no top-level `from app.services.template_render_service import` at module scope)
  </acceptance_criteria>
  <done>Both TDD files exist with the exact VALIDATION.md test names, all RED stubs xfail, the suite is exit-0, and docxtpl is in the venv for the TEST tier.</done>
</task>

<task type="auto">
  <name>Task 3: Add docxtpl==0.20.2 to Dockerfile.sandbox + document the rebuild/bump step</name>
  <files>backend/Dockerfile.sandbox</files>
  <read_first>
    - backend/Dockerfile.sandbox:37-49 — the existing pinned `RUN pip install` block (add after `reportlab==4.2.5`); :32-36 the reportlab-add comment that documents the "rebuild + bump SANDBOX_IMAGE; new chats only" convention to mirror
    - .planning/phases/101-template-fill-integrity-validation/101-RESEARCH.md → "## Runtime State Inventory" (the SANDBOX_IMAGE bump + cached-session gotcha) + "Installation (the ONLY stack change)"
    - CLAUDE.md "Local dev infrastructure" → Sandbox image section (the `docker build -f backend/Dockerfile.sandbox -t agentic-rag-sandbox:<tag>` + `SANDBOX_IMAGE` convention; tag bump only affects NEW chats)
  </read_first>
  <action>
Edit `backend/Dockerfile.sandbox`: add `docxtpl==0.20.2 \` to the existing `RUN pip install --no-cache-dir` block at line ~37-49, placed after `reportlab==4.2.5 \` (and before/among the existing trailing entries — preserve the line-continuation backslashes so the block stays one RUN layer). Add a short comment line above it mirroring the 2026-05-31 reportlab comment: `# 2026-06-10 (Phase 101 / TMPL-02): docxtpl — the trusted-path Jinja render engine (the LLM produces the cited field-map, docxtpl produces the OOXML bytes). The ONLY net-new sandbox dep; python-docx/pptx/openpyxl already present.`

Do NOT run the build in this task (it is the operator's rebuild step, surfaced via `user_setup`). The exact operator commands are:
  `docker build -f backend/Dockerfile.sandbox -t agentic-rag-sandbox:101.1 backend/`
  then set `SANDBOX_IMAGE=agentic-rag-sandbox:101.1` in `backend/.env`.
The tag bump only affects NEW chats (cached sessions keep their old image until idle eviction) — live UAT (Plan 03+) MUST use fresh chats. Record these commands in the plan SUMMARY so the operator runs them before live UAT.

Do NOT add docxtpl to `backend/requirements.txt` — production backend code never imports docxtpl (the render runs in the sandbox; importing it in `backend/app/**` would be Pitfall 4). Only the Dockerfile (sandbox image) and the venv (test tier) get it.
  </action>
  <verify>
    <automated>grep -c "docxtpl==0.20.2" backend/Dockerfile.sandbox</automated>
  </verify>
  <acceptance_criteria>
    - `grep "docxtpl==0.20.2" backend/Dockerfile.sandbox` matches inside the `RUN pip install` block (after reportlab)
    - The pip block still ends with a clean final entry (no dangling backslash that breaks the build) — verify the block parses: the line after the last package has no trailing `\`
    - `grep "docxtpl" backend/requirements.txt` returns NO match (docxtpl must NOT be a backend runtime dep)
    - A comment naming Phase 101 / TMPL-02 sits directly above the docxtpl line
  </acceptance_criteria>
  <done>docxtpl==0.20.2 is in Dockerfile.sandbox's pinned pip block with a Phase-101 comment; not in requirements.txt; the operator rebuild/bump commands are captured in user_setup + the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo (fixtures) | Committed binary fixtures are test data, not runtime input; no untrusted bytes cross into production |
| build → sandbox image | The Dockerfile pip install pulls docxtpl from the default PyPI index (no arbitrary registry) — supply-chain surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01-01 | Tampering | `Dockerfile.sandbox` pip install | mitigate | Pin `docxtpl==0.20.2` exactly (no range); pull from default PyPI only (no `--index-url`) — matches the existing pinned-version supply-chain rule (Dockerfile.sandbox:13) |
| T-101-01-02 | Tampering | committed OOXML fixtures | accept | Fixtures are deterministic, regenerable from `make_fixtures.py`, and only ever loaded in the offline TEST tier — never executed, never reach production input paths |
| T-101-01-03 | Information disclosure | test fixtures | accept | Fixtures carry only synthetic placeholder tokens (`{{client_name}}` etc.) — no PII, no secrets |
</threat_model>

<verification>
- `backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_template_render.py backend/tests/unit/test_template_integrity.py -q` exits 0 (all stubs xfail)
- The 4 fixtures regenerate from `make_fixtures.py`
- `grep "docxtpl==0.20.2" backend/Dockerfile.sandbox` matches; `grep docxtpl backend/requirements.txt` does not
- Full backend suite stays net-new-failure-free: `backend/venv/Scripts/python.exe -m pytest backend/tests -q` (account for ~14-17 pre-existing rot per SEED-056)
</verification>

<success_criteria>
- Both cross-plan TDD test files exist with the exact VALIDATION.md test names and all RED stubs xfailed (suite exit-0)
- docxtpl is installed in the venv (test tier) AND added to Dockerfile.sandbox (production sandbox tier), but NOT to requirements.txt
- All 4 OOXML fixtures exist and regenerate deterministically
- The operator rebuild/bump commands are captured for use before live UAT
</success_criteria>

<output>
After completion, create `.planning/phases/101-template-fill-integrity-validation/101-01-SUMMARY.md`
</output>
