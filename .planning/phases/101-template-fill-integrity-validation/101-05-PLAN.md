---
phase: 101-template-fill-integrity-validation
plan: 05
type: execute
wave: 3
depends_on: [101-01, 101-04]
files_modified:
  - backend/app/services/harness/phase_types.py
  - backend/tests/unit/test_template_render.py
autonomous: true
requirements: [TMPL-02]

must_haves:
  truths:
    - "A workflow fill phase that declares render_template in its available_tools has it admitted on BOTH layers (the schemas the model sees + the phase_whitelist dispatch backstop) via the 099 pattern"
    - "render_template is NOT auto-injected into non-fill phases — a phase only gets it when it declares it (no Deep widening; the gated-no-op invariant holds)"
    - "Deep mode stays byte-identical — phase_whitelist is None in Deep so the dispatch guard is a no-op; the field-map emission rides the UNMODIFIED shared gateway (no per-provider branch)"
    - "The fill phase emits the cited field-map as render_template's typed argument via forced structured output through the existing gateway — all 8 providers inherit NATIVE/STRUCTURED resolution + the truncation/registry traps at the service boundary"
  artifacts:
    - path: "backend/app/services/harness/phase_types.py"
      provides: "render_template admitted to a fill phase via _effective_tools / _build_phase_tool_context (the 099 whitelist pattern); the field-map emission framing"
      contains: "render_template"
  key_links:
    - from: "phase_types._effective_tools"
      to: "phase_whitelist + apply_tool_budget"
      via: "the effective tool list feeds both layer-1 (model sees) and layer-2 (dispatch backstop)"
      pattern: "phase_whitelist=frozenset"
    - from: "the fill phase field-map emission"
      to: "the shared gateway (_stream_one_iteration / resolve_calling_mode)"
      via: "forced structured output, never a per-provider branch"
      pattern: "render_template"
---

<objective>
Wire `render_template` into the harness fill phase via the 099 per-phase tool whitelist pattern (`_effective_tools` / `_build_phase_tool_context`) so a workflow fill phase that declares `render_template` gets it admitted on both layers — without widening Deep and without branching the shared gateway per provider. This is the last seam: it connects the registered tool (Plan 04) to the harness so a fill phase can actually emit the cited field-map as the tool's argument across the full native provider roster (D-14).

Purpose: D-04/D-05 discretion — `_effective_tools` already returns `available_tools` unchanged when no skill snapshot, so a phase listing `render_template` is admitted with no new code. This plan makes that explicit + tested, and ensures the field-map emission rides the UNMODIFIED gateway (the cross-provider RED LINE — provider quirks live at the service boundary, the fill path never branches).
Output: the `phase_types.py` whitelist wiring + a test proving render_template is admitted to a declaring phase and NOT to a non-declaring phase, with Deep byte-identical.

This is a SMALL seam (one helper extension + the dispatch-context confirmation + tests). Single-concern plan.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-template-fill-integrity-validation/101-PATTERNS.md
@.planning/phases/101-template-fill-integrity-validation/101-RESEARCH.md

<interfaces>
From backend/app/services/harness/phase_types.py:177-190 — the 099 auto-whitelist helper (extend in place, SAME shape):
```python
def _effective_tools(phase) -> list[str]:
    base = list(phase.config.available_tools)
    if getattr(phase.config, "skill_snapshot", None) is not None and "read_skill_file" not in base:
        base.append("read_skill_file")
    return base
```
From backend/app/services/harness/phase_types.py:243-263 — _build_phase_tool_context returns a ToolContext carrying:
```python
return ToolContext(
    ...
    available_tools=_tools,            # layer 1 — what the model sees (via apply_tool_budget :355)
    phase_whitelist=frozenset(_tools), # layer 2 — the dispatch backstop (dispatch_tool:1677)
    ...
)
```
From backend/app/services/tool_dispatcher.py:1677 — the gated-no-op invariant: phase_whitelist is None in Deep Mode → the dispatch guard is skipped → byte-identical Deep.
From backend/app/services/task_service.py:178 — _stream_one_iteration (the shared gateway; the field-map emission rides this UNMODIFIED — all 8 providers' NATIVE/STRUCTURED resolution + truncation traps already live here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admit render_template to a fill phase via the 099 whitelist pattern (no Deep widening, no gateway branch)</name>
  <files>backend/app/services/harness/phase_types.py, backend/tests/unit/test_template_render.py</files>
  <read_first>
    - backend/app/services/harness/phase_types.py:177-190 — _effective_tools (the 099 auto-whitelist; extend with the SAME never-drop shape)
    - backend/app/services/harness/phase_types.py:198-268 — _build_phase_tool_context (how _effective_tools feeds layer-1 available_tools + layer-2 phase_whitelist; the 098 folder_scope narrowing + 099 snapshot attach already here — DO NOT touch those)
    - backend/app/services/harness/phase_types.py:335-360 — _exec_llm_agent (the executor that runs the fill phase; apply_tool_budget at ~:355 — confirm render_template survives the per-provider max_tools cap because it's whitelisted)
    - backend/app/services/tool_dispatcher.py:1670-1689 — the dispatch_tool whitelist guard (the gated-no-op invariant; render_template is admitted only when the phase declares it)
    - .planning/phases/101-template-fill-integrity-validation/101-PATTERNS.md → "### backend/app/services/harness/phase_types.py" (the 099 pattern verbatim — "a phase that lists render_template gets it admitted without any new code; if auto-injection is wanted, extend _effective_tools with the SAME shape")
    - .planning/phases/101-template-fill-integrity-validation/101-RESEARCH.md → "### Pattern 2" + "## Shared Patterns" (route the field-map emission through the UNMODIFIED gateway; NEVER branch the fill path per provider — D-14 / Cond 7)
  </read_first>
  <action>
The 099 `_effective_tools` already returns `available_tools` unchanged when no skill snapshot is present — so a fill phase that lists `render_template` in its `available_tools` is ALREADY admitted on both layers (layer-1 `apply_tool_budget` + layer-2 `phase_whitelist`) with zero code change. The work here is to make that path explicit, defended, and tested, NOT to add new branching.

Make this minimal, additive change to `phase_types.py`:

1. Add a short comment block above `_effective_tools` (~:177) documenting that `render_template` is a Phase-101 fill tool admitted via the SAME never-drop pattern: a fill phase declares `render_template` in `available_tools` and it flows through unchanged; the helper NEVER drops a declared tool (so the whitelisted `render_template` survives `apply_tool_budget`'s per-provider max_tools cap on layer-1, exactly like `read_skill_file`).

2. Do NOT add a provenance-based auto-injection of `render_template` (a phase should EXPLICITLY declare it — auto-injecting it into every phase would widen the tool surface and is not D-04's intent). Keep the helper's existing skill-snapshot append; only extend the docstring/comment to name render_template as a declared-tool case. If a future fill `phase_type` wants auto-injection, the extension shape is the same `if <condition> and "render_template" not in base: base.append("render_template")` — but DO NOT add that condition now (no phase-type flag exists; Plugin Contract phase_type is STRETCH 108).

3. Confirm (no code change needed, but verify by reading) that `_build_phase_tool_context` already threads `_effective_tools(phase)` into BOTH `available_tools=_tools` and `phase_whitelist=frozenset(_tools)` (:261-263) — so a declared `render_template` lands in the dispatch backstop. The 098 folder_scope narrowing (:237-242) is untouched: the fill phase's field-map retrieval runs under the bound folder_scope, not a prompt hint (the citation `retrieved_ids` the tool checks come from THAT bound retrieval).

4. RED LINE: do NOT modify `_stream_one_iteration`, `resolve_calling_mode`, or any provider path. The field-map emission is a forced structured output the model produces as `render_template`'s argument, riding the UNMODIFIED gateway so all 8 providers inherit NATIVE/STRUCTURED resolution + the GLM/MiniMax registry-miss recovery + the DeepSeek/Moonshot truncation handling at the service boundary (D-14 / Cond 7). The fill path NEVER branches per provider.

Add tests in `backend/tests/unit/test_template_render.py` (offline, no live LLM):
  - `test_fill_phase_admits_render_template` — build a minimal `llm_agent` phase config whose `available_tools` includes `render_template` (no skill snapshot), call `_effective_tools(phase)` and assert `"render_template" in result`; build the phase tool context (or assert the frozenset directly) and assert `render_template` is in the `phase_whitelist` (layer 2). Use the existing harness test fixtures/builders (mirror test_099_skill_composition.py's phase-config construction).
  - `test_non_fill_phase_excludes_render_template` — a phase whose `available_tools` does NOT list `render_template`; assert `_effective_tools` does NOT contain `render_template` (no auto-injection; no Deep widening).
  - `test_deep_mode_whitelist_none_noop` — assert that in Deep mode (`phase_whitelist=None` on a plain ToolContext) the dispatch guard is a no-op (a static assertion that `_effective_tools` is only called in the harness path, OR import dispatch_tool's guard and assert `phase_whitelist is None` short-circuits — mirror the 099 `test_deep_noop` shape).
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_template_render.py -k "phase or render_template or deep" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "render_template" backend/app/services/harness/phase_types.py` matches (the documenting comment naming render_template as a declared fill tool)
    - `_effective_tools` still NEVER drops a declared tool and adds NO unconditional auto-injection of render_template (grep: no `base.append("render_template")` with an always-true condition)
    - `test_fill_phase_admits_render_template` passes — render_template is in both `_effective_tools(phase)` AND the `phase_whitelist` frozenset for a declaring phase
    - `test_non_fill_phase_excludes_render_template` passes — a non-declaring phase does NOT get render_template
    - `test_deep_mode_whitelist_none_noop` passes — Deep (phase_whitelist=None) is a dispatch no-op
    - `_stream_one_iteration` / `resolve_calling_mode` / provider paths are byte-unchanged (`git diff` shows no edit to task_service.py / openai_service.py / any *_service provider file)
    - The 098 folder_scope narrowing block + the 099 skill-snapshot append in _build_phase_tool_context are byte-unchanged
  </acceptance_criteria>
  <done>render_template is admitted to a declaring fill phase on both whitelist layers via the 099 pattern, not auto-injected, Deep byte-identical, and the field-map emission rides the unmodified gateway; the three offline tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow phase config → tool whitelist | The phase's declared available_tools decides which tools the model can call — the per-phase tool-budget boundary |
| Deep mode vs workflow phase | Deep (phase_whitelist=None) must stay byte-identical; the whitelist guard is a no-op there |
| field-map emission → provider gateway | The forced structured output crosses into the shared gateway; provider quirks must stay at the boundary, not in the fill path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-05-01 | EoP | per-phase tool whitelist | mitigate | render_template is admitted only when the phase DECLARES it; the layer-2 phase_whitelist frozenset is the dispatch backstop (dispatch_tool:1677); a hallucinated tool name is refused |
| T-101-05-02 | Tampering | Deep-mode byte-identity | mitigate | phase_whitelist is None in Deep → the dispatch guard is skipped → byte-identical; no auto-injection of render_template into non-declaring phases (the gated-no-op invariant) |
| T-101-05-03 (D-14/Cond 7) | Tampering | shared gateway | mitigate | The field-map emission rides the UNMODIFIED _stream_one_iteration; provider quirks (GLM/MiniMax registry-miss, DeepSeek/Moonshot truncation) handled at the service boundary; the fill path never branches per provider |
| T-101-05-04 (T-097-06) | EoP | field-map retrieval scope | mitigate | The fill phase retrieves under the 098 bound folder_scope (server-side, not a prompt hint); the citation retrieved_ids the tool checks come from that bound retrieval — scope can't be widened |
</threat_model>

<verification>
- `backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_template_render.py -q` → the three phase-whitelist tests GREEN
- `git diff --stat` shows edits ONLY to `phase_types.py` + the test file — no provider/gateway file touched (the cross-provider RED LINE)
- Harness regression suite green: `backend/venv/Scripts/python.exe -m pytest backend/tests -q -k "harness or 099 or 098"` (account for the known pre-existing bounded_retry KeyError + FK-violation rot, proven via stash-at-base if phase_types.py touches trigger them)
- Full backend suite net-new-failure-free
- Cross-provider field-map emission across the full native roster is LIVE UAT (VALIDATION.md Manual-Only) — NOT a task here
</verification>

<success_criteria>
- render_template is admitted to a declaring fill phase on both whitelist layers (099 pattern), not auto-injected, Deep byte-identical
- The field-map emission rides the unmodified gateway — no per-provider branch in the fill path (D-14 / Cond 7)
- The fill phase retrieval runs under the 098 bound folder_scope
</success_criteria>

<output>
After completion, create `.planning/phases/101-template-fill-integrity-validation/101-05-SUMMARY.md`
</output>
