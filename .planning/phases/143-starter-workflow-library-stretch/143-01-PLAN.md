---
phase: 143-starter-workflow-library-stretch
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/app/models/harness.py
  - backend/tests/unit/test_starter_workflows.py
  - backend/tests/integration/test_workflows_routes.py
  - frontend/src/pages/WorkflowsPage.test.tsx
autonomous: true
requirements: [WF-01]

must_haves:
  truths:
    - "WorkflowDefinition accepts an additive-optional `category` field so a starter definition carrying `category:'starter'` round-trips through create_draft without a 422 — the single highest-risk detail (D-143-2, Shared Pattern 1)"
    - "Adding `category` does NOT relax `extra='forbid'` — every OTHER unknown key still 422s, and old JSONB rows model_validate() to category=None (098 additive-optional lock precedent)"
    - "Wave 0 backstops exist and fail RED until the shelf query + fork land: SC-a (starters query excludes the 5 mig-061 scaffolds), SC-b (owned_only narrows / default keeps globals per D-143-2b), SC-c (fresh-copy fork mints new slug + v1 per D-143-1)"
  artifacts:
    - path: "backend/app/models/harness.py"
      provides: "category: str | None = None on WorkflowDefinition"
      contains: "category"
    - path: "backend/tests/unit/test_starter_workflows.py"
      provides: "RED backstops for list_starter_workflows + owned_only symmetry"
      contains: "test_starters_query_excludes_scaffolds"
    - path: "backend/tests/integration/test_workflows_routes.py"
      provides: "RED backstops for GET /workflows/starters, ?scope=mine, fork 409"
      contains: "starters"
    - path: "frontend/src/pages/WorkflowsPage.test.tsx"
      provides: "RED backstops for Starters shelf render, onUseStarter fork, section order"
      contains: "use-starter"
  key_links:
    - from: "onUseStarter fork (Plan 04)"
      to: "WorkflowDefinition.category"
      via: "create_draft body parse"
      pattern: "category"
---

<objective>
Land the ONE enabling schema contract and the three Wave 0 RED test backstops so every
downstream implementation wave has a failing target to turn green.

The `category` field is load-bearing (Shared Pattern 1, highest risk in the phase): D-143-2
stores the curation marker INSIDE the `definition` JSONB, but `WorkflowDefinition` is
`extra="forbid"` and has no `category` field — so both the seed's `model_validate` and the
fork round-trip (`onUseStarter` → `POST /workflows` → `create_draft` parses the body as
`WorkflowDefinition`) would 422 on the `category` key. The fix mirrors the shipped "098
additive-optional schema lock": add one optional field, zero migration.

Purpose: unblock Plans 02/03/04 and encode the acceptance behaviors (SC-a/SC-b/SC-c) as tests first.
Output: `category` field on WorkflowDefinition; 3 test files (2 backend RED, 1 frontend extended RED).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-starter-workflow-library-stretch/143-CONTEXT.md
@.planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md
@.planning/phases/143-starter-workflow-library-stretch/143-VALIDATION.md

<interfaces>
<!-- The exact schema-lock precedent the category field must mirror. Extracted from source; no exploration needed. -->
From backend/app/models/harness.py:
- `_StrictBase.model_config = ConfigDict(extra="forbid")` (line 30) — rejects unknown keys.
- WorkflowDefinition (line 221) fields: slug, version, name, status, phases, plus the 098
  additive-optional lock block (lines 228-235): project_folder_id, output_target_folder,
  reingest_output, version_policy, provenance, inputs, assets — all `| None = <default>`.
  business_requirement: str | None = None (line 240).
- Two @model_validator(mode="after") guards: `_folder_scope_requires_project` (242-254) and
  `_skill_snapshot_requires_ref` (256-270). The new field must not disturb either.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the additive-optional `category` field to WorkflowDefinition</name>
  <files>backend/app/models/harness.py</files>
  <read_first>
    - backend/app/models/harness.py lines 27-30 (`_StrictBase` extra="forbid") and 221-254
      (WorkflowDefinition, the 098 additive-optional lock block, the folder-scope validator) —
      the exact precedent to mirror.
    - .planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md § "Shared Patterns → 1"
      (the load-bearing category-vs-extra=forbid finding).
  </read_first>
  <action>
    Add one field to `WorkflowDefinition`, alongside the 098 additive-optional lock block
    (after `assets` / near `business_requirement`): `category: str | None = None` with a
    comment tagging it `143 WF-01 — Starters-shelf curation marker (D-143-2)`. It MUST be
    optional with a `None` default so old JSONB rows model_validate() unchanged (the 098
    zero-migration guarantee). Do NOT touch `_folder_scope_requires_project` or
    `_skill_snapshot_requires_ref`. Do NOT add `category` to any inner phase-config model —
    it belongs only on the top-level definition (that is where D-143-2's `definition->>'category'`
    predicate reads it).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -c "$(cat <<'PY'
from app.models.harness import WorkflowDefinition as W
import pydantic
assert 'category' in W.model_fields and W.model_fields['category'].default is None
d = W.model_validate({'slug': 'x', 'version': 1, 'name': 'X', 'phases': [], 'category': 'starter'})
assert d.category == 'starter'
try:
    W.model_validate({'slug': 'x', 'version': 1, 'name': 'X', 'phases': [], 'bogus': 1})
    raise SystemExit('extra=forbid was relaxed')
except pydantic.ValidationError:
    print('OK: category accepted + round-trips, bogus still rejected')
PY
)"</automated>
  </verify>
  <acceptance_criteria>
    - `WorkflowDefinition.model_fields['category']` exists with default `None`.
    - A def with `category:'starter'` validates and `d.category == 'starter'` round-trips; a def with any OTHER unknown key still raises `ValidationError` (extra="forbid" intact).
    - The command runs from `backend/` (via the `cd backend` prefix) so `app.models.harness` imports; adjust cwd, do not edit sys.path.
  </acceptance_criteria>
  <done>WorkflowDefinition carries `category: str | None = None`; category accepted + round-trips, all other unknown keys still 422.</done>
</task>

<task type="auto">
  <name>Task 2: Backend Wave 0 RED backstops — starters query + scope symmetry + route round-trips</name>
  <files>backend/tests/unit/test_starter_workflows.py, backend/tests/integration/test_workflows_routes.py</files>
  <read_first>
    - backend/tests/unit/test_103_draft_crud.py (the WHOLE file) — `_pg_reachable` skip-guard (22-45),
      the per-test `create_pool(min_size=1,max_size=1)` → try/finally DELETE-by-slug → `pool.close()`
      envelope (77-107), and the patched-route round-trip `test_route_round_trip_...` (210-272) with
      `patch("app.api.workflows.get_pg_pool", AsyncMock(return_value=pool))`.
    - backend/tests/unit/test_103_tweak_fork.py — the publish-a-fork → `UNIQUE(slug,version)` 409 shape (64-114).
    - .planning/phases/143-starter-workflow-library-stretch/143-VALIDATION.md § Per-Task Verification Map (SC-a/SC-b/endpoint/fork rows).
    - .planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md § test_starter_workflows.py + § test_workflows_routes.py.
  </read_first>
  <action>
    Create `backend/tests/unit/test_starter_workflows.py` (db-layer) mirroring `test_103_draft_crud.py`'s
    skip-guard + asyncpg-pool + seed-then-cleanup scaffolding. Do all `app.db.workflows` imports INSIDE
    the test bodies (Phase 102 posture) so collection succeeds and the RED surfaces at runtime.
    Two tests:
      • `test_starters_query_excludes_scaffolds` (SC-a): seed one `is_global=true`, `status='published'`,
        `definition->>'category'='starter'` row with a fixed throwaway uuid/slug; assert `list_starter_workflows(pool)`
        returns it AND does NOT return any of the 5 mig-061 scaffold slugs (`research_summarize`,
        `plan_execute_verify`, `literature_review`, `doc_qa_human`, `eval_coverage` — they lack the marker).
        Clean up the seeded row in `finally`.
      • `test_published_owned_only_and_default` (SC-b, Pitfall 3 guard): seed one global row + one
        `created_by=<test uid>` row; assert `list_published_workflows(pool, user_id=<uid>, owned_only=True)`
        returns ONLY the owned row (no global double-render), while `owned_only=False` (the DEFAULT the
        composer picker / WorkspacePanel / threads.py rely on) STILL returns the global row.
    Create `backend/tests/integration/test_workflows_routes.py` mirroring the patched-route pattern.
    Three tests: `GET /workflows/starters` (`get_starter_workflows`) returns the seeded starter rows;
    `GET /workflows/published?scope=mine` (`get_published_workflows(scope="mine")`) narrows to owned;
    a fork whose `(slug,version)` collides maps to a 409 (adapt `test_103_tweak_fork.py`). All three call
    the route fns directly with a `current_user` dict + `patch("app.api.workflows.get_pg_pool", ...)`.
    These are RED: `list_starter_workflows`, the `owned_only` param, `get_starter_workflows`, and the
    `scope` param do not exist until Plan 02 — the tests must fail on AttributeError/ImportError, NOT on a test bug.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_starter_workflows.py backend/tests/integration/test_workflows_routes.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and `--collect-only` succeeds (imports are inside test bodies, so collection does not error).
    - Running them WITHOUT `--collect-only` fails RED with AttributeError/ImportError referencing the not-yet-built
      `list_starter_workflows` / `owned_only` / `get_starter_workflows` / `scope` — not a syntax or fixture error.
    - `test_published_owned_only_and_default` explicitly asserts the DEFAULT path still returns global rows (the D-143-2b symmetry guard).
    - If Postgres :54322 is unreachable the db test self-skips via the copied `_pg_reachable` guard (never hard-fails CI).
  </acceptance_criteria>
  <done>Two backend test files land; collect clean; fail RED only because Plan 02's query/route/params are absent.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend Wave 0 RED backstops — Starters shelf render, fork, section order</name>
  <files>frontend/src/pages/WorkflowsPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowsPage.test.tsx — the existing "Tweak forks a v(N+1) draft (INSERT, never UPDATE)"
      describe block (334-372), the "drafts-above-published shelves" section-order block (175-208), and the
      `vi.mock("@/lib/api", …)` seam (44-53, 124-132).
    - .planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md § WorkflowsPage.test.tsx
      (the fork-assertion + section-order + mock-seam snippets to clone).
  </read_first>
  <action>
    Extend `WorkflowsPage.test.tsx`. Add `listStarterWorkflows: mockListStarters` to the `vi.mock("@/lib/api", …)`
    block and a `beforeEach` default `mockListStarters.mockResolvedValue([...])` returning one starter with
    `slug:"risk-register"`, `name:"Risk Register"`, and a minimal two-phase `definition` carrying `category:"starter"`.
    Add three RED describe/it blocks:
      • Starters shelf renders: `findByTestId("starters-shelf")` exists and shows the mocked starter card
        plus its `Starter`/`Official` chip and a `use-starter` control.
      • `onUseStarter` fork (SC-c / D-143-1): clicking `use-starter` calls `mockCreateDraft` once with
        `forked.slug` matching `/^risk-register-[a-z0-9]{6}$/` (NEW suffixed slug, driven by the seeded slug
        `risk-register`), `forked.version === 1` (NOT N+1), `forked.status === "draft"`, and `mockUpdate` NOT
        called (INSERT, never UPDATE the frozen starter).
      • Section order (SC-e / D-143-5 / BUG-260628-01 fold): assert `starters-shelf` precedes `published-shelf`
        precedes the drafts shelf via `compareDocumentPosition & Node.DOCUMENT_POSITION_FOLLOWING`.
    These are RED until Plan 04 (no Starters shelf, no `onUseStarter`, current order is drafts-above-published).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The three new blocks exist and reference `starters-shelf`, `use-starter`, the `/^risk-register-[a-z0-9]{6}$/` slug shape, and `version === 1`.
    - The new blocks fail RED (no Starters shelf / no onUseStarter / wrong section order) while the pre-existing
      WorkflowsPage tests still pass (do not break the "Tweak" or existing shelf tests).
    - Pre-existing SEED-056 rot tests elsewhere are out of scope — only this file is run.
  </acceptance_criteria>
  <done>WorkflowsPage.test.tsx carries the 3 Starters/fork/order backstops, RED until Plan 04, existing tests intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (fork body) | `onUseStarter` sends a `definition` (with `category`) that `create_draft` parses as `WorkflowDefinition` — untrusted key set crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-02 | Tampering | `WorkflowDefinition` schema | mitigate | Add ONLY `category: str | None = None`; `extra="forbid"` stays — every other unknown key still 422s (Task 1 verify asserts both halves). |
| T-143-04 | Information disclosure | scope-symmetry backstop | mitigate | `test_published_owned_only_and_default` encodes the narrows-not-widens contract before Plan 02 implements it. |
| T-143-SC | Tampering (supply chain) | package installs | accept | No npm/pip/cargo installs in this plan or phase (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `category` field present and additive-optional; extra="forbid" intact.
- Both backend test files collect clean and fail RED only for absent Plan 02 symbols.
- Frontend WorkflowsPage.test.tsx: 3 new backstops RED, existing tests green.
</verification>

<success_criteria>
- Downstream waves have a green schema contract (`category`) and RED targets for SC-a/SC-b/SC-c/SC-e.
</success_criteria>

<output>
Create `.planning/phases/143-starter-workflow-library-stretch/143-01-SUMMARY.md` when done.
</output>
</output>
