---
phase: 143-starter-workflow-library-stretch
plan: 05
type: execute
wave: 3
depends_on: [143-03, 143-04]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [WF-01]

must_haves:
  truths:
    - "Migration 094 + the 3 re-homed templates are applied to the live local DB via the project-sanctioned path: run `seed-starters.py --upload`, THEN paste `094_starter_workflows.sql` into the Supabase SQL editor (never db push/reset), THEN regenerate `full-schema.sql` (no --reset), THEN commit migration + full-schema together (D-143-4)"
    - "Each of the 3 seeded starters forks via 'Use this starter' → binds a KB with matching content → publishes through its OWN gauntlet → runs end-to-end to a real `.docx` with citations present; the published starter row is unchanged after the fork (SC-d / D-143-4a — seeds bypass the gauntlet by construction, so a real fork+run is the honest trust check)"
    - "A1 is verified: at least one fork+run is executed from a NON-operator account, proving the engine's service-role Storage read resolves the re-homed seed-user template across users (T-143-06)"
    - "Empty-KB honesty holds: a fork run over an empty/mismatched KB FAILS with a strict citation gap (D-143-7), not a silent or fabricated document — the honest strict-gate behavior (Pitfall 2)"
    - "The competitor-gallery synthesis (D-143-6 → D-143-8) is validated live: the 'Starter'/'Official' chip + 'Use this starter' CTA + Starters-on-top layout read as intended"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated bootstrap artifact including the 094 starter rows"
      contains: "starter"
  key_links:
    - from: "engine ctx.supabase (service-role)"
      to: "00000000-0000-0000-0000-000000000001/_library/<slug>.docx"
      via: "cross-user Storage read at render_template"
      pattern: "_library"
---

<objective>
Apply the seed to the live local DB and prove the 3 starters actually work end-to-end — the honest
trust check that replaces the (by-construction-bypassed) publish gauntlet for seed content (D-143-4a).

A SQL migration cannot place Storage bytes and the migration must be applied by hand (project rule:
never `db push`/`db reset` — it wipes dev data). So this plan is operator-gated: upload the templates,
paste the migration into the Supabase SQL editor, regenerate the bootstrap artifact, commit both, then
run each starter through a real fork → bind KB → publish → run to verify a cited `.docx` is produced.
The phase cannot pass verification without this apply (the shelf returns nothing and forks can't run
otherwise).

Purpose: land the content on the live DB + verify SC-d, A1 (cross-user service-role read), and empty-KB honesty.
Output: applied migration 094, regenerated + committed `full-schema.sql`, UAT results.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-starter-workflow-library-stretch/143-VALIDATION.md
@.planning/phases/143-starter-workflow-library-stretch/143-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration 094 + upload templates + regenerate full-schema</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - CLAUDE.md § "Schema changes ship as numbered SQL migrations" (apply-via-SQL-editor rule; regenerate-full-schema, no --reset).
    - supabase/migrations/094_starter_workflows.sql (Plan 03) + scripts/seed-starters.py (Plan 03).
    - .planning/phases/143-starter-workflow-library-stretch/143-RESEARCH.md § Runtime State Inventory + Pitfall 7.
  </read_first>
  <what-built>
    Migration 094 (3 curated starter rows, transformed + distinct slugs) and `seed-starters.py` (uploads the 3 re-homed
    `.docx` to the seed-user `_library` prefix). Both authored in Plan 03; NOT yet applied to the live DB / Storage.
  </what-built>
  <action>
    Operator-gated apply (Claude cannot paste into the Supabase SQL editor). The operator, in order: (1) runs
    `seed-starters.py --upload` to place the 3 template bytes BEFORE the rows go live; (2) pastes
    `094_starter_workflows.sql` into the Supabase SQL editor and runs it (NEVER `db push`/`db reset`); (3) regenerates
    `supabase/full-schema.sql` via `bash scripts/regenerate-full-schema.sh` (no `--reset`); (4) commits migration +
    regenerated full-schema together. Claude may run the regenerate + commit once the operator confirms the paste applied.
  </action>
  <how-to-verify>
    Ensure local Supabase is up (`supabase start`) and `backend/.env` points at local. Then, in order:
      1. Upload the templates (bytes MUST exist before a run resolves them):
         `backend/venv/Scripts/python.exe scripts/seed-starters.py --upload`
         — expect 3 byte-round-trip OKs to `00000000-0000-0000-0000-000000000001/_library/{risk-register,weekly-status-report,compliance-gap-report}.docx`.
      2. Apply the row seed: open the Supabase SQL editor (local Studio) and PASTE the full contents of
         `supabase/migrations/094_starter_workflows.sql`; run it. Expect 3 rows inserted (or 0 on a clean re-run via
         ON CONFLICT). Do NOT run `supabase db push` or `db reset` (wipes dev data).
         — Sanity: `SELECT slug, is_global, definition->>'category' FROM workflow_definitions WHERE definition->>'category'='starter';` returns the 3 starters.
      3. Regenerate the bootstrap artifact (no reset): `bash scripts/regenerate-full-schema.sh`.
      4. Commit the migration AND the regenerated `supabase/full-schema.sql` together
         (`git add supabase/migrations/094_starter_workflows.sql supabase/full-schema.sql && git commit`).
    If the paste raises `23505` on `UNIQUE(slug,version)`, a `pm-*`-slug collision slipped in — STOP and re-check Plan 03
    Task 2 used the distinct slugs (`risk-register`, not `pm-risk-register`).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms 3 `category='starter'` rows live + 3 templates under the seed `_library` prefix + full-schema regenerated and committed.</human-check>
  </verify>
  <acceptance_criteria>
    - `SELECT count(*) ... WHERE definition->>'category'='starter'` = 3 on the live local DB.
    - The 3 templates exist under `00000000-…-01/_library/` (byte round-trip asserted by the upload step).
    - `supabase/full-schema.sql` regenerated (no --reset) and committed alongside the migration.
  </acceptance_criteria>
  <done>Migration 094 applied, 3 templates uploaded, full-schema regenerated + committed.</done>
  <resume-signal>Type "applied" once the 3 rows + 3 templates are live and full-schema is committed, or describe any error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [UAT] Fork + run each of the 3 starters to a cited .docx (incl. a non-operator fork — A1)</name>
  <files>none</files>
  <read_first>
    - .planning/phases/143-starter-workflow-library-stretch/143-VALIDATION.md § Manual-Only Verifications (the SC-d instructions).
    - .planning/phases/143-starter-workflow-library-stretch/143-RESEARCH.md § Assumptions Log A1 (service-role cross-user Storage read).
  </read_first>
  <what-built>
    The full Starters experience: a curated shelf on top of the Workflows page, "Use this starter" fresh-copy fork, and 3
    runnable KB→document starters (Risk Register, Weekly Status Report, Compliance Gap Report) with strict citations.
  </what-built>
  <action>
    Operator drives the live UAT (Claude cannot run the browser flow). For each starter: fork via "Use this starter",
    bind a KB with matching content, publish the fork, run, and confirm a cited `.docx`; then run at least one fork from a
    NON-operator account to exercise the A1 service-role cross-user Storage read. Claude records the results.
  </action>
  <how-to-verify>
    For EACH of the 3 starters:
      1. On the Workflows page confirm the starter appears on the TOP shelf with the "Starter"/"Official" chip.
      2. Click "Use this starter" → confirm a NEW owned draft opens in the Builder with a suffixed slug (e.g.
         `risk-register-ab12c3`) and version 1 — NOT a `v2` of the starter.
      3. Bind a KB folder that HAS matching content (the PM demo corpus for Risk Register / Weekly Status; author a small
         compliance corpus for Compliance Gap Report), Publish the fork through its own gauntlet, then Run.
      4. Confirm a `.docx` output file is produced, opens clean, and shows citations.
      5. Confirm the published STARTER row is unchanged (the fork did not mutate it).
    Then run AT LEAST ONE starter fork+run from a NON-OPERATOR account (login a second user) to prove A1 — the engine's
    service-role read resolves the re-homed seed-user template across users (this is the one claim not executed end-to-end
    in research; T-143-06). Live app: http://localhost:5173/ (backend started by the operator).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all 3 starters fork to a suffixed-slug v1 draft and run to a cited .docx, with at least one non-operator fork+run passing (A1).</human-check>
  </verify>
  <acceptance_criteria>
    - All 3 starters fork to a suffixed-slug v1 owned draft, publish, and run to a cited `.docx` that opens clean.
    - At least one fork+run succeeds from a non-operator account (A1 confirmed — no cross-user Storage 404).
    - No published starter row is mutated by any fork.
  </acceptance_criteria>
  <done>3 starters proven runnable end-to-end (incl. a non-operator run); no starter row mutated.</done>
  <resume-signal>Type "UAT pass" with the 3 output filenames + which starter you ran as a non-operator, or describe failures.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [UAT] Empty-KB strict-citation honesty + chip/CTA read</name>
  <files>none</files>
  <read_first>
    - .planning/phases/143-starter-workflow-library-stretch/143-VALIDATION.md § Manual-Only Verifications (empty-KB honesty row).
    - .planning/phases/143-starter-workflow-library-stretch/143-RESEARCH.md § Pitfall 2 + § Competitor Gallery Findings (D-143-6/8).
  </read_first>
  <what-built>Strict citation policy on the starters (D-143-7) + the curated-vs-mine visual distinction (D-143-8).</what-built>
  <action>
    Operator confirms two things live: (1) a fork run over an empty/mismatched KB FAILS honestly with a strict citation
    gap (not a fabricated `.docx`); (2) the curated-vs-mine visual reads correctly (chip + CTA + Starters-on-top + Published
    shows only the user's own workflows). Claude records the outcome.
  </action>
  <how-to-verify>
    1. Fork any starter, bind an EMPTY or mismatched KB folder, and Run. Confirm the run FAILS with a "no supporting
       evidence" / citation gap (strict `citations_required(fail_run)`) — NOT a silent success or a fabricated document.
       This is the honest strict-gate behavior, not a bug.
    2. Eyeball the layout: Starters shelf on top, the "Starter"/"Official" chip distinguishes curated from user-made, the
       "Use this starter" CTA reads clearly, and the Published (mine) shelf no longer shows the 5 dev scaffolds or the
       starters (no double-render). Confirm the Build-card is still discoverable.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the empty-KB fork run fails with a visible citation gap + the chip/CTA/layout read as intended (no scaffold/starter double-render on the Published shelf).</human-check>
  </verify>
  <acceptance_criteria>
    - An empty-KB fork run fails with a visible citation gap (honest failure), not a fabricated `.docx`.
    - The chip + CTA + Starters-on-top layout read as intended; Published shelf shows only the user's own published workflows (no scaffolds/starters).
  </acceptance_criteria>
  <done>Empty-KB honesty confirmed; curated-vs-mine visual + de-dupe confirmed live.</done>
  <resume-signal>Type "honesty pass" (or describe what rendered), confirming the empty-KB failure + the curated-vs-mine visual.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DB/Storage | SQL-editor apply + service-role template upload (the sanctioned is_global seed path). |
| engine (service-role) → Storage | cross-user read of the re-homed seed-user template during a forker's run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Elevation of privilege | is_global apply | mitigate | is_global is set ONLY by the SQL-editor superuser applying migration 094 — no app path; mig 056 RLS unchanged. |
| T-143-05 | Information disclosure / fragility | seed template path | mitigate | Templates live under the seed-user `_library` prefix (Plan 03) — no real account's private Storage is referenced; verified live in Task 1/2. |
| T-143-06 | Information disclosure | engine service-role Storage read (A1) | mitigate (verify) | Task 2 executes a NON-operator fork+run — if the service-role read did NOT resolve cross-user, the emit would 404; a passing run confirms the re-home is safe and functional. |
| T-143-02 | Tampering | published starter row | mitigate | Task 2 confirms no fork mutates the frozen starter (DB immutability trigger + fresh-INSERT fork). |
| T-143-SC | Tampering (supply chain) | package installs | accept | No package installs (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Live DB: 3 `category='starter'` rows; 3 templates under the seed `_library` prefix; full-schema.sql committed.
- SC-d: 3 starters fork+run to cited `.docx` (incl. a non-operator run for A1).
- Empty-KB run fails honestly (strict citations); chip/CTA/layout read as intended.
</verification>

<success_criteria>
- The curated starters are live, forkable, and provably runnable end-to-end; the shared-scope exception is verified safe (A1); strict-gate honesty confirmed.
</success_criteria>

<output>
Create `.planning/phases/143-starter-workflow-library-stretch/143-05-SUMMARY.md` when done.
</output>
