---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/vitest-count-gate.cjs
  - frontend/src/components/workflows/soulData.ts
  - frontend/src/components/workflows/soulData.test.ts
  - frontend/src/lib/phaseGlyph.tsx
autonomous: false
requirements: [CANVAS-02]

must_haves:
  truths:
    - "D-184-08: a per-file test-COUNT differential gate exists as a committed script BEFORE any Wave-0 work lands, so a deleted test is visible (the Phase-177 lesson a failures-only differential cannot catch)"
    - "D-184-07: the two PHASE_GLYPHS swaps (llm_agent robot→compass, llm_batch_agents busts-in-silhouette→handshake) ship as ONE atomic commit at the HEAD of Wave 0, revertable without touching any canvas code"
    - "D-184-07: both maps swap together — soulData.PHASE_GLYPHS AND phaseGlyph.PHASE_GLYPH_MARKS — so phaseGlyph() can never return the old 3D component while the string fallback changed"
    - "D-184-08: this commit is the ONE carved-out exception to the zero-assertion-edit gate; the single edited assertion is soulData.test.ts's PHASE_GLYPHS map check and it is enumerated in the SUMMARY"
    - "The five shipped surfaces that render PHASE_GLYPHS still render a visible 3D mark after the swap"
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "D-184-08 per-file count differential — Wave-0 baseline infrastructure, consumed by every later Wave-0 plan"
      contains: "--reporter=json"
    - path: "frontend/src/components/workflows/soulData.ts"
      provides: "PHASE_GLYPHS slug map with compass + handshake"
      contains: "llm_agent: \"compass\""
    - path: "frontend/src/lib/phaseGlyph.tsx"
      provides: "PHASE_GLYPH_MARKS bundled 3D component map with Compass + Handshake"
      contains: "~icons/fluent-emoji/compass"
  key_links:
    - from: "frontend/src/lib/phaseGlyph.tsx"
      to: "@iconify-json/fluent-emoji"
      via: "deep-subpath ~icons import (a missing slug FAILS THE BUILD)"
      pattern: "~icons/fluent-emoji/(compass|handshake)"
---

<objective>
Lay the Wave-0 measuring stick, then ship the two cross-cutting `PHASE_GLYPHS` swaps as their own
atomic commit at the head of Wave 0, before any extraction or feature code.

Purpose: two things, in this order, in **two separate commits**.

1. **The D-184-08 count gate is baseline infrastructure.** It must exist before ANY Wave-0 work —
   including the icon swap, whose one deliberate assertion edit it has to carve out. Every later
   Wave-0 plan (184-02, 184-03) calls `node scripts/vitest-count-gate.cjs` in its verify steps, so
   the script ships here, in the one plan both of them already depend on.
2. **The icon swap.** Sketch 137-B makes the 3D icon the **sole carrier of step type** (there is no
   per-type colour on the card at all), which promotes both swaps from optional to REQUIRED before
   Phase 184 builds. `busts-in-silhouette` measures luminance 34.5 on Deep Midnight — ~4× dimmer
   than the other five marks — and visually disappears; `robot` was rejected by the operator as
   generic. D-183-14's independent-revert concern is about the **commit** boundary, so the swap
   ships alone: a `git revert` of that one commit undoes the icon decision without touching the
   canvas — and without touching the gate script, which lives in its own earlier commit.

**D-184-07 atomicity is preserved, not diluted:** the gate script is a separate TASK and therefore a
separate COMMIT. The icon commit still touches exactly three files and is still independently
revertable on its own.

Output: (a) `scripts/vitest-count-gate.cjs`, committed and falsified; then (b) `llm_agent → compass`,
`llm_batch_agents → handshake`, swapped in BOTH maps plus both deep-subpath imports plus the
"verified slugs" docblock plus the one pinned assertion — one commit, five places, measured by the
gate from (a) and verified live on the five shipped surfaces.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
@.planning/sketches/MANIFEST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the D-184-08 per-file test-count differential gate</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - 184-VALIDATION.md § "Wave 0 Count Pin (D-184-08 — the mechanical half)" — the 16-file / 424-test table and the reproduce-mechanically snippet
    - scripts/check-181-scope-freeze.sh (the shipped shape for a committed gate script: explicit exit codes, named failure reasons, no watch flags)
  </read_first>
  <action>
Create `scripts/vitest-count-gate.cjs` — a Node CommonJS script that runs the Wave-0 blast-radius
vitest suites with `--reporter=json` and asserts the D-184-08 mechanical gate.

This ships FIRST, in its own commit, because it is **baseline infrastructure**: it is the measuring
stick every later Wave-0 commit is measured against, including Task 2's icon swap in this same plan,
and it is invoked by name in plans 184-02 and 184-03. It must exist before any Wave-0 source change.

Embed the baseline table from 184-VALIDATION.md as a module-level `const BASELINE` object keyed by
bare filename with the pinned counts: `canvasModel.fixtures.test.ts` 100 · `canvasModel.purity.test.ts`
69 · `phaseVocabulary.test.ts` 42 · `WorkflowCanvas.test.tsx` 31 · `canvasModel.test.ts` 26 ·
`PublishGauntlet.test.tsx` 24 · `WorkflowBuilderPage.canvas.test.tsx` 22 · `PhaseFormPanel.test.tsx` 19 ·
`WorkflowBuilderPage.test.tsx` 15 · `PhaseSpineGraph.test.tsx` 14 · `soulData.test.ts` 14 ·
`WorkflowDoorSwitch.test.tsx` 13 · `PhaseSpine.test.tsx` 11 · `deriveTier.test.ts` 9 ·
`WorkflowSoul.test.tsx` 8 · `revertByteIdentical.test.tsx` 7 — total 424.

Behaviour:
- Spawn `npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=json --outputFile=<tmp>` with `cwd` = `frontend`. Write the JSON to a path under the OS temp dir, never into `frontend/` or `backend/` (the "no scratch in a watched tree" rule — uvicorn/vite reload watchers wedge on Windows).
- Use `--reporter=json`. **Never `--reporter=basic`** (removed in vitest 4) and never a watch flag.
- Parse `numTotalTests`, `numFailedTests`, and per-suite `assertionResults.length` keyed by the bare filename of `testResults[].name`.
- FAIL (exit 1) with a named reason when: `numFailedTests > 0`; `numTotalTests < 424`; any baseline file is MISSING from the run; or any baseline file's count DECREASED. A count that INCREASED is allowed and printed as `+N` (feature waves add tests).
- PASS (exit 0) prints a per-file table with `pinned / actual / delta` and the total.
- Accept an optional `--json <path>` flag to reuse an already-produced report instead of re-running, so a caller that just ran the suite does not pay for it twice.

The script is committed tooling. It must not import anything from `frontend/src` and must not require
a database, a backend, or network access.

This task changes NO source file — only `scripts/vitest-count-gate.cjs`. Commit it on its own so
Task 2's icon commit stays a three-file, independently revertable D-184-07 commit.

Commit message: `chore(184-01): add D-184-08 per-file vitest count gate`.
  </action>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>node --check scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/vitest-count-gate.cjs` exits 0 on the current tree and prints a table containing all 16 baseline filenames and the line `total 424` (or a higher total with explicit `+N` deltas)
    - The script source contains the literal `--reporter=json` and contains neither `--reporter=basic` nor `--watch`
    - Falsification control: temporarily deleting one `it(...)` block from `frontend/src/components/workflows/deriveTier.test.ts` makes the script exit 1 with a message naming `deriveTier.test.ts` and the decrease; restore the file afterwards and re-run to confirm exit 0. Record both observations in the SUMMARY
    - The script writes its JSON report outside `frontend/` and outside `backend/` (assert the resolved output path in the SUMMARY)
    - `git status --porcelain frontend/` is clean after a run (the script leaves no artifact in the app tree)
    - `git show --stat HEAD` for this commit lists exactly one file: `scripts/vitest-count-gate.cjs` — no source file rides along, so the next commit is a clean D-184-07 revert unit
  </acceptance_criteria>
  <done>`node scripts/vitest-count-gate.cjs` is a committed, falsified, exit-code-honest gate for D-184-08's per-file count pin, landed in its own single-file commit before any Wave-0 source change.</done>
</task>

<task type="auto">
  <name>Task 2: Swap both glyph maps to compass + handshake in one commit</name>
  <files>frontend/src/components/workflows/soulData.ts, frontend/src/components/workflows/soulData.test.ts, frontend/src/lib/phaseGlyph.tsx</files>
  <read_first>
    - frontend/src/components/workflows/soulData.ts (the whole file — `PHASE_GLYPHS` is at `:35-42`)
    - frontend/src/lib/phaseGlyph.tsx (the whole file — the "verified slugs" docblock line at `:19-21`, the `~icons/fluent-emoji/*` imports at `:28-33`, `PHASE_GLYPH_MARKS` at `:44-51`)
    - frontend/src/components/workflows/soulData.test.ts (specifically `:128-136`, the `toMatchObject` assertion that pins `"robot"` / `"busts-in-silhouette"` verbatim)
    - 184-PATTERNS.md § W0a "The D-184-07 icon swap touches FIVE places, verified this session"
    - .planning/sketches/MANIFEST.md — the "Icon decisions (2026-07-25)" paragraph and the 137-B consequence list
  </read_first>
  <action>
Apply the swap in exactly five places, in ONE commit, per D-184-07 and PATTERNS.md §W0a.

**This commit is the D-184-07 revert unit.** It touches exactly the three files listed above and
nothing else. The count gate created in Task 1 is already committed separately, so `git revert` of
this commit undoes the icon decision alone and leaves the gate standing.

1. `soulData.ts:35-42` — in `PHASE_GLYPHS`, change `llm_agent: "robot"` to `llm_agent: "compass"` and
   `llm_batch_agents: "busts-in-silhouette"` to `llm_batch_agents: "handshake"`. Leave the other four
   entries (`programmatic: "gear"`, `llm_single: "memo"`, `llm_human_input: "raised-hand"`,
   `llm_emit: "package"`) untouched.
2. `phaseGlyph.tsx:28-33` — replace the deep-subpath imports `~icons/fluent-emoji/robot` and
   `~icons/fluent-emoji/busts-in-silhouette` with `~icons/fluent-emoji/compass` and
   `~icons/fluent-emoji/handshake`. Rename the bound identifiers `Robot` → `Compass` and
   `BustsInSilhouette` → `Handshake`. Both slugs were verified PRESENT in the installed
   `@iconify-json/fluent-emoji@1.2.7` icon set; a missing slug fails the Vite build, so a green
   `npm run build` is itself the presence proof.
3. `phaseGlyph.tsx:44-51` — in `PHASE_GLYPH_MARKS`, repoint `llm_agent` to `Compass` and
   `llm_batch_agents` to `Handshake`. Both maps MUST move together: swapping only
   `soulData.PHASE_GLYPHS` leaves `phaseGlyph()` returning the old 3D component while the string
   fallback changed — a silent split-brain (RESEARCH Runtime State Inventory).
4. `phaseGlyph.tsx:19-21` — update the "Only verified fluent-emoji slugs are used (API-checked …)"
   docblock line so it enumerates `gear, memo, compass, handshake, raised-hand, package`. Leaving the
   old list is the D-ITEM-183-02 "a comment that lies" trap, which fired five times in Phase 183.
   Append the new verification date and cite the installed package as the source.
5. `soulData.test.ts:128-136` — update the `toMatchObject` expectation to `llm_agent: "compass"` and
   `llm_batch_agents: "handshake"`. **This is the ONE assertion edit carved out of D-184-08's
   zero-assertion-edit gate** (Pitfall 3). It must be named explicitly in the SUMMARY as the carve-out,
   with the file, line and old/new values, so a verifier does not read it as a gate violation. It is
   also the SOLE permitted assertion change in this commit — the Task-1 gate is run here specifically
   to prove that nothing else moved.

Do NOT change the test COUNT in `soulData.test.ts` — it stays at 14 tests, which is exactly what the
Task-1 gate's `soulData.test.ts: 14` pin asserts. Do NOT touch any other file. Do NOT fold any
extraction work — or the gate script — into this commit; its whole purpose is an independently
revertable icon decision.

Commit message: `feat(184-01): swap llm_agent→compass and llm_batch_agents→handshake glyphs`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/soulData.test.ts src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/WorkflowSoul.test.tsx --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npm run build</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - No MAP ENTRY or IMPORT anywhere still binds the old slugs: `grep -nE '(llm_agent|llm_batch_agents)\s*:\s*("robot"|"busts-in-silhouette"|Robot|BustsInSilhouette)' frontend/src/components/workflows/soulData.ts frontend/src/lib/phaseGlyph.tsx frontend/src/components/workflows/soulData.test.ts` returns no matches, and `grep -nE '~icons/fluent-emoji/(robot|busts-in-silhouette)' frontend/src/lib/phaseGlyph.tsx` returns no matches. **Deliberately scoped to map entries and imports, not a blanket file grep** — a docblock or SUMMARY note that names the OLD slugs as historical context must stay legal (D-ITEM-183-02: a guard that only passes by making a comment lie is a broken guard)
    - `frontend/src/components/workflows/soulData.ts` contains the exact strings `llm_agent: "compass"` and `llm_batch_agents: "handshake"`
    - `frontend/src/lib/phaseGlyph.tsx` contains `import Compass from "~icons/fluent-emoji/compass"` and `import Handshake from "~icons/fluent-emoji/handshake"`
    - `frontend/src/lib/phaseGlyph.tsx`'s `PHASE_GLYPH_MARKS` maps `llm_agent` to `Compass` and `llm_batch_agents` to `Handshake`
    - `frontend/src/lib/phaseGlyph.tsx`'s verified-slugs docblock line lists `compass` and `handshake` and does NOT list `robot` or `busts-in-silhouette`
    - `npm run build` exits 0 (proves both `~icons` subpaths resolve in the installed icon set)
    - `node scripts/vitest-count-gate.cjs` exits 0: total >= 424, failed = 0, and **no per-file count decrease** — `soulData.test.ts` still reports exactly its pinned 14
    - The ONE carved-out assertion edit is the only assertion change in the commit: `git show HEAD -- 'frontend/src/**/*.test.ts' 'frontend/src/**/*.test.tsx'` shows changes in `soulData.test.ts` ONLY, confined to the `toMatchObject` glyph-map block at `:128-136`, and the SUMMARY enumerates it by file, line and old/new values
    - `npx tsc -b 2>&1 | grep -c "error TS"` returns a number <= 33 (D-ITEM-183-01 differential baseline on `develop`)
    - `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0 (PHASE_GLYPHS is not part of `PhaseNodeData`, so the canvas snapshot must not move — RESEARCH Open Question 5; if it DOES move, stop and report, do not bless the snapshot)
    - `git show --stat HEAD` lists exactly the three files above — `scripts/vitest-count-gate.cjs` is NOT among them (it is Task 1's separate commit), so this commit remains the standalone D-184-07 revert unit
  </acceptance_criteria>
  <done>Both glyph maps, both imports, the docblock and the one pinned assertion carry `compass` + `handshake` in a single revertable three-file commit; build green; the count gate green at >= 424 with no per-file decrease; canvas snapshot byte-unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Five-surface before/after check of the new marks</name>
  <files>none — visual verification only</files>
  <read_first>
    - .planning/sketches/MANIFEST.md — the "Measured icon finding (2026-07-25)" luminance table and the 137-B consequence list
    - .planning/sketches/themes/phase-icons-3d.js — the verified 3D mark set (never text glyphs)
  </read_first>
  <action>
Pause and hand the five-surface visual check to the operator. Do not modify any file in this task.
Start the frontend dev server if it is not already running (`cd frontend && npm run dev`,
http://localhost:5173/), confirm it compiles, and present the `<how-to-verify>` steps below. Collect the
operator's verdict per surface and record it in the SUMMARY. If any surface renders a blank box or a text
glyph instead of the 3D mark, name the component and stop — that is an unresolved `~icons` binding, not a
cosmetic nit.
  </action>
  <what-built>
    `PHASE_GLYPHS` and `PHASE_GLYPH_MARKS` now resolve `llm_agent` to the 🧭 compass mark (luminance
    157.4) and `llm_batch_agents` to the 🤝 handshake mark (luminance 182.6, up from
    busts-in-silhouette's 34.5). `PHASE_GLYPHS` is deliberately shared, so ONE map swap changes five
    already-shipped surfaces. D-184-07 requires this check because sketch 137-B makes the icon the
    sole carrier of step type — an invisible or missing mark now means the step type is unreadable.
  </what-built>
  <how-to-verify>
    Start the frontend dev server (`cd frontend && npm run dev`, http://localhost:5173/) and sign in
    with the local test account. Then look at each of the five shipped surfaces that render
    `PHASE_GLYPHS` / `phaseGlyph()`, and confirm a visible, legible 3D mark (never a text glyph, never
    a blank box) for BOTH `llm_agent` and `llm_batch_agents`:

    1. **Workflows page card** — the workflow library list (a card whose definition contains an
       `llm_agent` step; `research_summarize` or `eval_coverage` are good candidates).
    2. **Run soul header** — open a completed workflow run.
    3. **Publish soul header** — open a draft and start the publish gauntlet (do NOT complete it).
    4. **Gauntlet stage rows** — the stage list inside that same gauntlet view.
    5. **Live step cards** — the harness phase timeline in the workspace panel for any run.

    For each: (a) is the mark visible against Deep Midnight, (b) is it the compass / handshake and not
    the old robot / silhouettes, (c) does the `llm_batch_agents` mark now read at roughly the same
    brightness as `gear` / `memo` / `package` rather than fading into the background?
  </how-to-verify>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/soulData.test.ts --reporter=default</automated>
    <human-check>All five surfaces render a visible compass for `llm_agent` and a visible handshake for `llm_batch_agents`; neither renders a text glyph, an empty box, or a mark that disappears into the background.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator confirms all five surfaces render both new marks visibly
    - Any surface that renders a blank / text-glyph fallback is reported with its component name before the plan is marked done
    - No console error mentioning `~icons` or an unresolved icon module appears on any of the five surfaces
  </acceptance_criteria>
  <done>The operator has confirmed a visible compass on every `llm_agent` surface and a visible handshake on every `llm_batch_agents` surface across all five shipped renderers, with no console icon-resolution error.</done>
  <resume-signal>Type "approved" if all five surfaces render both new 3D marks visibly, or name the surface(s) that regressed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → bundle | The `~icons/fluent-emoji/*` deep-subpath imports are resolved at build time from an installed package; nothing user-supplied crosses here |
| repo tooling → local shell | `scripts/vitest-count-gate.cjs` spawns a local `npx vitest` run and writes a JSON report to the OS temp dir; no network, no database, no `frontend/src` import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-01-01 | Tampering | `phaseGlyph.tsx` icon subpath imports | mitigate | Slug presence is enforced by the build itself — an unresolvable `~icons/fluent-emoji/<slug>` fails `npm run build`. Verified in the acceptance criteria rather than assumed |
| T-184-01-02 | Information disclosure | rendered marks | accept | Icon slugs are static asset names; no user, org or tenant data is involved. No PII, no authz decision |
| T-184-01-03 | Tampering | `scripts/vitest-count-gate.cjs` report path | mitigate | The JSON report is written under the OS temp dir, never into `frontend/` or `backend/` — a scratch file in a watched tree wedges the vite/uvicorn reload watchers on Windows. The resolved path is asserted in the SUMMARY and `git status --porcelain frontend/` must be clean after a run |
| T-184-01-SC | Tampering | npm installs | accept | This plan installs nothing. `zundo` is the phase's only net-new dependency and is installed in plan 184-04, where its `## Package Legitimacy Audit` verdict (`[OK]`, MIT, 336k/wk, no `postinstall`) is carried |
</threat_model>

<verification>
- `node scripts/vitest-count-gate.cjs` — exit 0, total >= 424, failed = 0, no per-file count decrease, all 16 baseline files present
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0
- `cd frontend && npm run build` exits 0
- Exactly two commits: one single-file gate-script commit, then one three-file icon commit that is independently revertable
- `git show HEAD -- 'frontend/src/**/*.test.ts' 'frontend/src/**/*.test.tsx'` shows exactly one assertion change — `soulData.test.ts:128-136` — the sole D-184-08 carve-out
</verification>

<success_criteria>
- `scripts/vitest-count-gate.cjs` is committed, falsified and green BEFORE any Wave-0 source change, so plans 184-02 and 184-03 can call it unconditionally
- `compass` and `handshake` are live in BOTH `PHASE_GLYPHS` and `PHASE_GLYPH_MARKS`, with matching imports and an honest docblock
- The single edited assertion (`soulData.test.ts` glyph map) is enumerated in the SUMMARY as D-184-08's explicit carve-out, with file, line and old/new values, and the gate proves it was the ONLY one
- The icon commit is standalone and revertable with `git revert` without touching any canvas, store, ops or tooling code
- The operator has confirmed all five shipped surfaces render both new marks visibly
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-01-SUMMARY.md` when done.
The SUMMARY MUST contain (a) the count-gate falsification observation and its resolved temp report path,
and (b) a section titled `## D-184-08 carve-out — the one edited assertion` naming
`frontend/src/components/workflows/soulData.test.ts`, the assertion, and its old/new values.
</output>
