---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 10
type: execute
wave: 6
depends_on: ["184-05", "184-07", "184-08"]
files_modified:
  - frontend/src/components/workflows/builderStore.ts
  - frontend/src/components/workflows/builderStore.test.ts
  - frontend/src/components/workflows/WorkflowCanvas.tsx
  - frontend/src/components/workflows/WorkflowCanvas.test.tsx
  - frontend/src/components/workflows/PhaseNode.tsx
  - frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx
autonomous: true
requirements: [CANVAS-02]

must_haves:
  truths:
    - "D-184-09: reorder has a pointer path and a keyboard path with ONE op behind both — ⌥← / ⌥→ move a selected node one position with a live-region announcement, and a drag resolves to the same reorder op"
    - "D-184-10: one free drag, the AXES split the two meanings — the x-component resolves to a lane slot (a definition edit: undoable, validates, marks dirty) and the y-component is kept verbatim as the browser-local dy; a purely vertical drag can never enter the definition"
    - "R2: fromCanvas is the serializer behind the canvas-originated order commit — commitCanvasNodes is renumber(fromCanvas(nodes, phases)) and delegates to the one reorder op"
    - "R3: a dy nudge issues ZERO network requests and adds ZERO history entries"
    - "VALID-03: verdict marks reach the node face as DATA threaded down by the shell, exactly as the ⌥ reveal already is, so PhaseNode stays a context-free leaf"
    - "The shipped read-only opt-outs survive: nodesConnectable, edgesReconnectable, connectOnClick, edgesFocusable and deleteKeyCode={null} all stay — only per-node draggable flips, and only on phase nodes"
  artifacts:
    - path: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      provides: "The editing-capable canvas shell — drag, nudge, keyboard reorder, verdict marks"
      contains: "onNodeDragStop"
    - path: "frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx"
      provides: "The editing-surface proof suite (fireEvent only inside the plane)"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      to: "frontend/src/components/workflows/definitionOps.ts"
      via: "resolveDrop decides the axis; the reorder op is the store's"
      pattern: "resolveDrop"
    - from: "frontend/src/components/workflows/builderStore.ts"
      to: "frontend/src/components/workflows/canvasModel.ts"
      via: "commitCanvasNodes = renumber(fromCanvas(nodes, phases))"
      pattern: "fromCanvas"
---

<objective>
Make the canvas moveable: one free drag whose axes carry two different meanings, a keyboard reorder
behind the same op, and server verdict marks threaded onto the node face.

Purpose: 183 shipped `nodesDraggable={false}` and a read-only plane. D-184-10 splits the drag by AXIS
rather than by modifier — the x-component resolves to a lane slot and is a real definition edit; the
y-component is kept verbatim as the browser-local `dy` and is not. Because the op that runs is decided
by which component changed, a purely vertical drag can never enter the definition: separation by
construction, matching 138 C-local as drawn. D-184-09 adds the keyboard path because 183 made every node
keyboard-activatable and shipped a real screen-reader pass — a drag-only reorder would regress that, and
at 5 steps maximum the keyboard path is genuinely competitive.

Output: the store's canvas-order commit, the canvas's drag/nudge/keyboard plumbing, and the verdict
marks on the node face. **The ＋ and ✕ affordances land in 184-12** — this plan moves what exists.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-RESEARCH.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
@.planning/sketches/138-growing-the-flow/README.md
@.planning/sketches/themes/canvas-184.css
</context>

<interfaces>
<!-- Verified this session. -->

`frontend/src/components/workflows/WorkflowCanvas.tsx`:
- `ARIA_LABELS` `:118-121` — the two React Flow node-description overrides, currently promising only
  select-on-Enter. **Pinned by `WorkflowCanvas.test.tsx:395-412`.**
- `WorkflowCanvasProps` `:149-173` — and at `:168` the `onSelectNode` docblock literally says
  *"Selection only — NEVER reorders, NEVER moves a node."* **184 makes that false.**
- the view-copy `:187-201` — where selection and the ⌥ boolean are merged into a node copy; this is
  where `dy`, `draggable: true` and the verdict mark are merged too, and it is why no layout key can leak
- the keyboard-activation block `:223-251`, especially the auto-repeat guard `:234` (`if (event.repeat) return`)
- the read-only opt-out block `:302-310`, verbatim:
```
nodesDraggable={false}      // <- 184 flips THIS ONE, per-node, phase nodes only
nodesConnectable={false}    // <- KEEP
edgesReconnectable={false}  // <- KEEP
connectOnClick={false}      // <- KEEP
edgesFocusable={false}      // <- KEEP
deleteKeyCode={null}        // <- KEEP: the ✕ is the delete affordance, not Backspace
zoomOnDoubleClick={false}
```
- `<Controls showInteractive={false} />` at `:334` — pinned by `WorkflowCanvas.test.tsx:417`
- `onPaneClick={onClearSelection}` at `:326`
- module-scope constants rule `:99-104` — anything new (node types, aria labels, edge styles) goes at
  module scope or React Flow warns and re-renders every node

`WorkflowCanvas.test.tsx:415-429` — the shipped scope fence: the source must NOT match
`/workflows\/validate/` or `/grounding_mode/`, must contain `showInteractive={false}`, and must ship no
`MiniMap` / `hideAttribution`. **`verdicts` arrive as a PROP; the canvas fetches nothing.**

`WorkflowCanvas.test.tsx:231-238` — no focusable control may exist INSIDE any node. The drag handle is
the card itself (React Flow's own drag), so this plan adds none.

`@xyflow/react` v12, verified in `node_modules`: with the CONTROLLED `nodes` prop, `hasDefaultNodes` is
false, so **without `onNodesChange` the computed changes are discarded and a drag does not move the node
at all**. Apply only `type === "position"` changes; `dimensions` changes are measured into the library's
internal `nodeLookup` and need not be applied (183 proved this empirically by painting edges correctly
with no `onNodesChange`). `onNodeDragStop(event, node, nodes)` gives the dropped `node.position`;
`onNodeDragStart` is the natural place to capture the origin.

jsdom rule (`WorkflowCanvas.test.tsx:120-137`): drive interactions INSIDE the plane with `fireEvent`,
never `user-event` — a `user-event` click also dispatches `mousedown`, reaches d3-zoom, and d3-drag
dereferences a null `event.view`; every assertion passes, three unhandled `TypeError`s fire outside the
test body, and vitest still exits 1. **d3-drag is unusable in jsdom, so the pointer half of reorder is a
live-only G-4 row (U-2) — test `resolveDrop` as a pure function and invoke `onNodeDragStop` directly on
the component's prop where a component-level assertion is genuinely needed.**
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: builderStore gains commitCanvasNodes — fromCanvas behind one reorder op</name>
  <files>frontend/src/components/workflows/builderStore.ts, frontend/src/components/workflows/builderStore.test.ts</files>
  <read_first>
    - frontend/src/components/workflows/builderStore.ts (as built in 184-04)
    - frontend/src/components/workflows/canvasModel.ts — `fromCanvas` and `CANVAS_NODE_TYPES` (184-05)
    - frontend/src/components/workflows/definitionOps.ts — `movePhase`, `renumber` (184-02)
    - frontend/src/components/workflows/builderStore.test.ts (as built in 184-04 — APPEND, do not edit its assertions)
    - 184-RESEARCH.md § "Pattern 1" — why `fromCanvas` must not renumber and where renumbering belongs
  </read_first>
  <action>
Add ONE action to `builderStore.ts`:
`commitCanvasNodes(nodes: readonly CanvasNode[]): void`.

Implementation and the reasoning that must appear in its docblock, because a reviewer will otherwise
read the two modules as redundant:

- **`fromCanvas` is the sole canvas-to-definition serializer; `definitionOps` is the sole
  definition-to-definition mutation home.** `commitCanvasNodes` is the seam where a commit ORIGINATES in
  canvas node space, so it reads the new order through `fromCanvas(nodes, get().phases)`, derives the
  moved slug and its target render index from that order, and then **delegates to the existing
  `reorderPhase(slug, toIndex)` action** — which is `renumber(movePhase(...))`. That keeps D-184-09's
  "one op behind both" literally true: the pointer path and the `⌥←`/`⌥→` path both end in
  `reorderPhase`.
- If `fromCanvas` returns the source untouched (its duplicate-slug fail-safe) or the derived order is
  identical to the current order, `commitCanvasNodes` is a **no-op**: no history entry, no dirty flag.
- `lastEditKind` is `"structural"` for a real reorder, so the history entry pushes immediately (D-184-02).
- The action performs **no network call** — D-184-03.

APPEND to `builderStore.test.ts` (do not edit the 184-04 assertions):
- a reordered node array produces the expected `phases` order with contiguous `phase_index` `[0..n-1]`
- the same call raises `pastStates.length` by exactly 1, synchronously
- a node array in the CURRENT order is a no-op: `pastStates.length` unchanged and `dirty` unchanged
- a source with duplicate slugs is a no-op (the fail-safe propagates)
- non-`phase` nodes (the end cap, the unresolved-skip stub) in the array are ignored and do not become
  phases
- the `fetch` spy still reports 0 calls
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/builderStore.test.ts --reporter=default</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `builderStore.ts` exports a `commitCanvasNodes` action that calls `fromCanvas` and delegates to `reorderPhase`
    - `grep -c 'renumber' frontend/src/components/workflows/canvasModel.ts` shows `fromCanvas` still does not renumber (the renumber happens in the store, via `definitionOps`)
    - The identical-order call leaves `pastStates.length` and `dirty` unchanged
    - The duplicate-slug call is a no-op
    - End-cap / unresolved-skip nodes in the array produce no phases
    - The `fetch` spy reports 0 calls
    - `git diff frontend/src/components/workflows/builderStore.test.ts` shows only ADDED lines
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>The canvas-originated order commit runs through the one serializer and ends in the one reorder op, is a no-op when nothing moved, and never touches the network.</done>
</task>

<task type="auto">
  <name>Task 2: WorkflowCanvas gains the drag, the nudge and the verdict marks</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/components/workflows/PhaseNode.tsx</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowCanvas.tsx (the WHOLE file — the props docblock `:149-173`, the view-copy `:187-201`, the keyboard block `:223-251`, the read-only opt-outs `:302-310`, `onPaneClick` `:326`, `Controls` `:334`, module-scope rule `:99-104`)
    - frontend/src/components/workflows/WorkflowCanvas.test.tsx (all 31 assertions — especially `:231-238`, `:395-412`, `:415-429`)
    - frontend/src/components/workflows/PhaseNode.tsx (the adapter, as left by 184-03)
    - frontend/src/components/workflows/canvasNudge.ts (184-07) and definitionOps.resolveDrop (184-02)
    - frontend/src/components/workflows/verdictModel.ts (184-08) — `markFor`
    - 184-RESEARCH.md § "Pattern 6: @xyflow/react v12 editing — the controlled-flow contract" and "The D-184-10 drag shape, concretely" (steps 1-4)
  </read_first>
  <action>
Extend `WorkflowCanvasProps` (`:149-173`) with editing props, each carrying a docblock in the same
justify-its-existence style the shipped props use:
- `editable: boolean` — false renders exactly today's read-only canvas. Every editing affordance is
  behind this, and the page passes `canvasEnabled`.
- `marks?: (slug: string) => "error" | "incomplete" | "unknown" | undefined` — the server-derived verdict
  mark lookup, supplied by the page from `verdictModel.markFor`. **The canvas fetches nothing** — its
  own suite forbids the string `workflows/validate` in this file, which is what keeps the boundary right.
- `nudges?: Record<string, number>` and `onNudge?: (slug: string, dy: number) => void` — the cosmetic
  `dy`, read and written by the page through `canvasNudge.ts`. **`localStorage` must not appear in this
  file either** (keep the storage in its module).
- `onCommitNodes?: (nodes: readonly CanvasNode[]) => void` — the canvas-originated order commit.

**Fix the two comment-vs-behaviour traps in the SAME commit that falsifies them** (the D-ITEM-183-02
trap, which fired five times in Phase 183):
1. `:168`'s `onSelectNode` docblock says *"Selection only — NEVER reorders, NEVER moves a node."* Rewrite
   it to state what is now true: selection is still selection-only, and reordering travels on
   `onCommitNodes`, never on `onSelectNode`.
2. `ARIA_LABELS` (`:118-121`) promises only select-on-Enter. It must remain TRUE once `⌥←`/`⌥→` exist
   (Task 3). If the string has to change, edit the string AND its pinning assertion at
   `WorkflowCanvas.test.tsx:395-412` in the same commit, and **enumerate the change in the SUMMARY as a
   deliberate decision** — do not leave a promise the surface no longer keeps, and do not silently edit
   a shipped assertion.

Editing plumbing:
- **Per-node `draggable`, never the shell prop.** In the view-copy at `:187-201`, set `draggable: true`
  on `phase` nodes only when `editable` is true. The shell's `nodesDraggable={false}` line and **every
  other line of the `:302-310` opt-out block stays** — `nodesConnectable`, `edgesReconnectable`,
  `connectOnClick`, `edgesFocusable` and `deleteKeyCode={null}` are what make "no free wiring" true
  rather than assumed. `<Controls showInteractive={false} />` stays; its absence is two clicks from
  re-enabling drag and connect.
- **`onNodesChange`** — required, because with the controlled `nodes` prop the library computes changes
  and then discards them, so a drag would not move the node at all. Apply **only** `type === "position"`
  changes, into a local `dragOverlay: Record<string, XYPosition>` view state. Ignore `dimensions`,
  `select`, `remove`, `add` and `replace`: selection is owned by the page and removal by `definitionOps`.
- **`onNodeDragStart`** captures the origin position; **`onNodeDragStop`** calls
  `definitionOps.resolveDrop(origin, node.position, lanes)` and branches on the AXES:
  - `reorderTo !== null` -> build the reordered `CanvasNode[]` and call `onCommitNodes` (a definition
    edit: undoable, validates, marks dirty). The card then snaps to its computed lane x, because
    positions re-derive from the projection.
  - always -> call `onNudge(slug, dy)` with the y-component verbatim. **No history, no network, never
    serialized.**
  - clear `dragOverlay[node.id]` afterwards.
- **Merge `nudges[slug]` into the node's y position in the view-copy**, exactly like selection and the ⌥
  boolean are merged today — a COPY. The model output and the definition behind it are never touched,
  which is why no layout key can leak into the payload.
- **Thread the verdict mark down as DATA**: merge `marks?.(node.id)` into `node.data.verdict` in the same
  view-copy, and have `PhaseNode` pass it to `PhaseNodeCard`'s `verdict` slot. This mirrors the D-183-08
  pattern for the ⌥ reveal — the leaf stays context-free and a second source of truth is structurally
  impossible.
- **Motion keys off run state, never selection.** 184 has no run state; do not attach motion to the
  dragged or selected node.

`editable === false` must produce the pre-plan render: no `draggable`, no `onNodesChange` effect on the
DOM, no verdict marks unless `marks` is supplied.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>cd frontend && git diff --exit-code -- 'src/**/__snapshots__/*'</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All 31 `WorkflowCanvas.test.tsx` assertions pass; if `ARIA_LABELS` changed, exactly the pinning assertion at `:395-412` changed with it and both are enumerated in the SUMMARY — otherwise `git diff` on that file is empty
    - `grep -cE 'workflows/validate|grounding_mode|localStorage|MiniMap|hideAttribution' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0
    - `grep -c 'showInteractive={false}' frontend/src/components/workflows/WorkflowCanvas.tsx` returns >= 1
    - All five kept opt-outs are still present: `grep -cE 'nodesConnectable=\{false\}|edgesReconnectable=\{false\}|connectOnClick=\{false\}|edgesFocusable=\{false\}|deleteKeyCode=\{null\}' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 5
    - `onNodesChange` applies only `position` changes — `grep -n 'type === "position"' frontend/src/components/workflows/WorkflowCanvas.tsx` returns >= 1 and no other change type is applied
    - The `:168` `onSelectNode` docblock no longer contains the string `NEVER reorders`
    - `container.querySelectorAll("button, a, [tabindex]")` inside any `.react-flow__node` is still length 0 (the shipped `:231-238` assertion)
    - `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0 when `editable` is false in the snapshot suite
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>Phase nodes drag, the axes split the two meanings, the cosmetic dy stays in its own module, verdict marks arrive as threaded data, every read-only opt-out but one survives, and both lying comments were fixed in the commit that falsified them.</done>
</task>

<task type="auto">
  <name>Task 3: ⌥← / ⌥→ keyboard reorder, the live-region announcement, and the editing proof suite</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowCanvas.tsx (as extended in Task 2 — especially the keyboard-activation block `:223-251` and its auto-repeat guard `:234`)
    - frontend/src/components/workflows/WorkflowCanvas.test.tsx `:120-137` (the fireEvent rule and its full rationale) and `:26-28` (the FILE-LOCAL `mockReactFlow` import)
    - frontend/src/test-utils/mockReactFlow.ts (the jsdom recipe — file-local by rule, never `setupTests.ts`)
    - frontend/src/components/panel/PhaseTimeline.tsx (the polite live-region announcer shape)
    - 184-CONTEXT.md § D-184-09 — the Alt-modified binding rationale and the announcement wording
  </read_first>
  <action>
**Keyboard reorder.** With a phase node selected, `⌥←` moves it one position earlier and `⌥→` one
position later. Alt-modified so it cannot collide with `@xyflow`'s own arrow-key node navigation or with
panel field navigation. Both paths build the reordered `CanvasNode[]` and call **the same
`onCommitNodes`** as the drag — one op behind both (D-184-09).

Mount the handler in the shipped gated-listener shape: no listener exists unless the canvas is
`editable` and a phase node is selected, so it costs nothing at rest and cannot accumulate across
renders. Reuse the auto-repeat guard already at `:234` (`if (event.repeat) return`) — one press is one
move. Bail when `event.target` is an `input`, `textarea` or `contenteditable` element so native
field-level editing is untouched.

Announce every move through a **polite live region** in the `PhaseTimeline.tsx` shape, with the locked
wording: *"Step moved to position 2 of 5."* A move at the first or last position is a no-op and
announces nothing new.

Then create `frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx` — a NET-NEW suite that
does not absorb `WorkflowCanvas.test.tsx`. Call the FILE-LOCAL `mockReactFlow()` before the first
render, exactly as `WorkflowCanvas.test.tsx:26-28` does; never install it globally.

**Drive everything inside the plane with `fireEvent`, never `user-event`.** Quote the reason in the file
docblock so no future author re-discovers it: a `user-event` click inside the plane dispatches a real
`mousedown`, reaches d3-zoom's pan handler, and d3-drag dereferences `event.view.document` on a jsdom
synthetic `MouseEvent` with a null `view` — every assertion passes, three unhandled `TypeError`s fire
outside the test body, and vitest still exits 1.

Assertions:
1. `editable: false` renders no `draggable` phase node — assert the node's React Flow draggable class or
   the absence of the drag affordance, and assert the render matches the read-only baseline.
2. `editable: true` marks phase nodes draggable and leaves the end cap and unresolved-skip stub
   non-draggable.
3. **⌥→ on a selected middle node** calls `onCommitNodes` exactly once with the expected node ORDER, and
   the live region contains *"Step moved to position 3 of 5."* (adjust the numbers to the fixture).
4. **⌥← at position 0** does not call `onCommitNodes` and announces nothing new.
5. **Alt is required**: a bare `ArrowRight` does NOT call `onCommitNodes`.
6. **Auto-repeat**: a `keydown` with `repeat: true` does not move the node.
7. **Text-field yield**: an `⌥→` whose `event.target` is an `input` does not move the node.
8. **The drag half, at the component level**: invoke the component's `onNodeDragStop` prop directly with
   a purely VERTICAL drop and assert `onNudge` was called with the y-delta while `onCommitNodes` was NOT
   called. Then invoke it with a HORIZONTAL drop past half a pitch and assert `onCommitNodes` was called.
   State in the docblock that d3-drag itself is unusable in jsdom, so the real pointer gesture is the
   live-only G-4 row U-2, and this assertion covers the resolver wiring rather than the gesture.
9. **R3 — zero network on a nudge**: a `fetch` spy records 0 calls across a nudge-only drag.
10. **Verdict marks are threaded**: with a `marks` prop returning `"incomplete"` for one slug, that
    node renders the dashed grey mark and a container scan finds **zero** occurrences of the destructive
    token literal exported from `nodePresentation.ts`; a `marks` prop returning `"error"` makes it appear.
11. **The nudge moves the card without touching the model**: with a `nudges` map, the node's rendered
    position reflects the `dy` while the projection passed in is unchanged — assert the input `phases`
    array is `toStrictEqual` its pre-render clone.

Check the process EXIT CODE, not just the pass count — a green summary with exit 1 is the documented
jsdom failure mode on this surface.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx` passes with 0 failures AND exit code 0
    - `grep -c 'user-event' frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx` returns 0 for interactions inside the plane
    - `grep -c 'mockReactFlow' frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx` returns >= 1 and `frontend/src/setupTests.ts` is unchanged
    - The ⌥→ test asserts BOTH the `onCommitNodes` call and the exact live-region sentence
    - The bare-`ArrowRight`, auto-repeat and text-field-yield tests all assert `onCommitNodes` was not called
    - The vertical-drop test asserts `onNudge` called and `onCommitNodes` NOT called; the horizontal-drop test asserts the inverse
    - The `fetch` spy reports 0 calls on a nudge-only drag
    - The destructive-token scan returns 0 for an `incomplete`-only render and >= 1 for an `error` render
    - The input `phases` array is unmutated after a nudged render
    - `node scripts/vitest-count-gate.cjs` exits 0 with `WorkflowCanvas.test.tsx` still at exactly 31
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>Reorder works from the keyboard with a spoken position, both gesture paths end in the same op, the vertical axis provably cannot reach the definition, and the suite drives the plane the only way jsdom permits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer/keyboard gesture -> definition mutation | A drag is the one input that can silently change what runs |
| view-layer node copy -> outgoing payload | Layout values live in the view copy and must never reach the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-10-01 | Tampering | a cosmetic gesture silently changing what runs | mitigate | The axis split is structural: `resolveDrop` returns `reorderTo: null` for a purely vertical drop, so the definition branch is never entered. Asserted both as a pure-function test (184-02) and at the component level here |
| T-184-10-02 | Tampering | layout keys leaking into the definition | mitigate | `dy` is merged into a node COPY in the view layer only; `canvasNudge.ts` owns the storage and `WorkflowCanvas.tsx` contains no `localStorage` token. The payload-key assertion lands in 184-11 |
| T-184-10-03 | Elevation of privilege | re-enabling free wiring by accident | mitigate | Only `nodesDraggable` flips, and only per-node on phase nodes; the other five read-only opt-outs and `showInteractive={false}` are grep-asserted present, so a drawn branch the engine will never run stays unrepresentable |
| T-184-10-04 | Repudiation | a lying docblock or aria promise | mitigate | Both comment-vs-behaviour traps (`onSelectNode:168`, `ARIA_LABELS:118-121`) are fixed in the same commit that falsifies them, and any assertion edit is enumerated in the SUMMARY as a deliberate decision |
| T-184-10-05 | Information disclosure | verdict data reaching the node | accept | Marks arrive as an already-server-derived value threaded through node data, exactly as the ⌥ reveal already is; no new data class and no new fetch |
| T-184-10-SC | Tampering | npm installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx` — 0 failures, exit code 0
- `node scripts/vitest-count-gate.cjs` — exit 0, `WorkflowCanvas.test.tsx` still at 31
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `git diff --name-only -- backend/ | wc -l` returns 0
- `git diff --name-only -- supabase/migrations | wc -l` returns 0
</verification>

<success_criteria>
- Phase nodes drag; a purely vertical drag provably cannot enter the definition
- `⌥←` / `⌥→` reorder with a spoken position, behind the same op as the drag
- The canvas-originated commit runs through `fromCanvas` and ends in `reorderPhase`
- Verdict marks reach the node face as threaded data, with the `incomplete` state carrying no destructive token
- Five of the six read-only opt-outs and `showInteractive={false}` survive verbatim
- Both lying docblocks are corrected in the commit that falsified them
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-10-SUMMARY.md` when done.
It MUST state whether `ARIA_LABELS` changed and, if so, enumerate the string and the assertion edited
alongside it, with the reason.
</output>
