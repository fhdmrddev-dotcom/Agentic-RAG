---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 04
type: execute
wave: 3
depends_on: ["184-02", "184-03"]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/workflows/builderStore.ts
  - frontend/src/components/workflows/BuilderStoreProvider.tsx
  - frontend/src/components/workflows/builderStore.test.ts
  - frontend/src/pages/WorkflowBuilderPage.tsx
autonomous: true
requirements: [CANVAS-02, CANVAS-03]

must_haves:
  truths:
    - "D-184-01: the working definition lives in ONE page-wide zustand store wrapped in zundo's temporal middleware, created per-Builder-mount; WorkflowBuilderPage's useState<BuilderState> is no longer the home of the definition"
    - "D-184-02: a structural edit pushes a history entry immediately; two config edits inside 500 ms coalesce into ONE entry via zundo's handleSet"
    - "D-184-02: the cosmetic dy never enters the tracked store at all, so a nudge adds no history entry BY CONSTRUCTION rather than by a filter"
    - "D-184-03: undo crosses the save boundary and re-marks the draft dirty; undo never writes to the server"
    - "D-184-08: the state-home refactor is behaviour-preserving — zero shipped assertions edited, every per-file test count held, canvas snapshot byte-unchanged, revertByteIdentical green"
    - "R4: partialize narrows the undo stack to the definition slice only, so an arriving /validate response can never push a history entry"
    - "D-184-05: persistence STAYS on the page — draftIdRef, creatingRef, onPersist and onSaveDraft are unchanged, because Phase 186 rewrites exactly that seam"
  artifacts:
    - path: "frontend/src/components/workflows/builderStore.ts"
      provides: "createBuilderStore() factory + TrackedSlice + the selective handleSet"
      exports: ["createBuilderStore", "TrackedSlice", "BuilderStoreState", "BuilderStore", "HISTORY_LIMIT", "CONFIG_COALESCE_MS", "selectDefinition"]
    - path: "frontend/src/components/workflows/BuilderStoreProvider.tsx"
      provides: "Per-mount store context with an OPTIONAL accessor so leaves still render provider-less"
      exports: ["BuilderStoreProvider", "useBuilderStoreOptional", "useBuilderStore", "useBuilderTemporal"]
    - path: "frontend/src/components/workflows/builderStore.test.ts"
      provides: "R4 store-unit proof + research assumption A3's falsification"
  key_links:
    - from: "frontend/src/components/workflows/builderStore.ts"
      to: "frontend/src/components/workflows/definitionOps.ts"
      via: "every structural action delegates to a pure op"
      pattern: "from \"@/components/workflows/definitionOps\""
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/components/workflows/builderStore.ts"
      via: "useState(() => createBuilderStore(...)) — per-mount, never a module singleton"
      pattern: "createBuilderStore"
---

<objective>
Move the working definition into one per-Builder-mount zustand store wrapped in `zundo`'s `temporal`
middleware, and prove the history behaves before anything is built on top of it.

Purpose: D-184-01 puts the definition in ONE place because `PhaseFormPanel` is shared by BOTH views —
a canvas-scoped store would leave Spine-originated config edits outside the history R4 says must
contain them. The undo/redo AFFORDANCES stay canvas-only and flag-gated (184-13); the HISTORY is
complete. This is the last Wave-0 commit: after it, D-184-08's gate has run its course and feature
code may land.

Output: `zundo` installed, `builderStore.ts` + `BuilderStoreProvider.tsx` + `builderStore.test.ts`, and
`WorkflowBuilderPage.tsx` repointed onto the store with **persistence untouched**.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-RESEARCH.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
</context>

<interfaces>
<!-- Verified this session. Use these lines, not CONTEXT/RESEARCH's (both drifted). -->

`frontend/src/pages/WorkflowBuilderPage.tsx`:
- `BuilderState` union at `:107-111` — `{phase:"empty"} | {phase:"composing"} | {phase:"drafted"; definition} | {phase:"error"; message; detail?}`
- `const [state, setState] = useState<BuilderState>(...)` at **`:151`** (NOT `:100`)
- `handleSelectNode` **`:205`** · `clearSelection` **`:213`** · the Escape `useEffect` **`:224-231`**
- `onPhaseChange` `:334-345` · `onPersist` `:347-372` · `onSaveDraft` `:374-394` · transient-timer cleanup `:396-401`
- the flag gate `:190-201` — regex-pinned literal `visual_workflow_canvas === true`
- the lazy import `:86` — regex-pinned exact text, DO NOT reformat
- the panel mount `:640-649` · the publish seam `:630` (`renderPublish`)

`frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx:414-438` — the shipped source guards this file
must keep satisfying: no `localStorage`, no `sessionStorage`, no `aria-pressed`, no `reactflow`,
`useEffectiveFeaturesOptional()` present, `useEffectiveFeaturesContext(` absent,
`visual_workflow_canvas === true` present, and the lazy-import regex.

`zundo@2.3.0` facts verified upstream (`charkour/zundo` `src/index.ts`, `src/temporal.ts`):
- `store.temporal = createStore(...)` is assigned INSIDE the config initializer ⇒ a per-mount factory gets its own independent temporal store
- the curried `handleSet` receives FOUR arguments: `(pastState, replace, currentState, deltaState)` — `currentState` is the PARTIALIZED current state, which is the discriminator D-184-02 needs
- `limit` evicts exactly one entry per push at a `>=` boundary; every push sets `futureStates: []`
- `undo(n)`/`redo(n)` splice `pastStates` IN PLACE then `set({...})` — a `.length` selector still fires; a selector returning the ARRAY may not
- `ZundoOptions.handleSet`'s ARGUMENT is typed as `setState` while its RETURN is typed with the real 4-arg signature — one narrow cast at this seam is unavoidable

`zundo` React-19 landmine (upstream issue #207, closed as working-as-intended): `store.temporal.getState()`
inside a component is a snapshot, not a subscription — the toolbar's enabled state MUST read through a
selector or it never re-renders.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install zundo and build the per-mount builder store</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/components/workflows/builderStore.ts, frontend/src/components/workflows/BuilderStoreProvider.tsx</files>
  <read_first>
    - frontend/src/stores/streamsStore.ts (the whole file — the app's ONLY zustand store: the zustand-v5 curried `create<T>()(middleware(...))` form at `:36-37, 295`, the per-field docblock convention at `:1-35`, and the immutable copy-then-replace action shape)
    - frontend/src/providers/TechnicalNamesProvider.tsx (the OPTIONAL-accessor idiom; its consumption at `WorkflowCanvas.tsx:181-183` and rationale at `:45-53`)
    - frontend/src/hooks/usePanelReconcile.ts `:58-60` (the selector form) vs `:77` (the deliberate side-effect `getState()` form)
    - frontend/src/components/workflows/definitionOps.ts (as built in 184-02)
    - 184-RESEARCH.md § "Pattern 2: `zundo` `temporal` on a per-mount store" — the full code example, the `handleSet` cast note, and the verified-internals table
    - 184-RESEARCH.md § "Package Legitimacy Audit" — `zundo` verdict `[OK]`, MIT, 336k/wk, 5y old, no `postinstall`, listed in zustand's own official docs
  </read_first>
  <action>
**Install.** Run `cd frontend && npm install zundo` (exact version `2.3.0`). The
`## Package Legitimacy Audit` in 184-RESEARCH.md records the full verification chain with verdict
`[OK]` — approved, no `[ASSUMED]`/`[SUS]` checkpoint required. After install, confirm `npm ls zundo`
shows `2.3.0` with no peer warning against the installed `zustand@5.0.13`, and that
`node -e "console.log(require('./node_modules/zundo/package.json').scripts)"` shows no `postinstall`.
`zundo` is this phase's ONLY net-new dependency — do not add a debounce library, do not add a
property-test library.

**`builderStore.ts`.** Export a `createBuilderStore(initial)` FACTORY, not a module singleton. Document
the divergence from `streamsStore.ts` (the app's only other zustand store, deliberately a singleton) in
the module docblock, with the reason: history must not leak between two Builder sessions.

State shape, split explicitly into tracked and untracked halves with per-field docblocks in the
`streamsStore.ts:1-35` style:
- **TRACKED (`TrackedSlice`)** — `phases: PhaseSpecJSON[]`, `lastEditKind: "structural" | "config"`,
  `editSeq: number` (monotonic, so two identical-looking config edits stay distinguishable).
- **UNTRACKED** — `meta` (the definition minus `phases`), `builderPhase: "empty" | "composing" | "drafted" | "error"`,
  `errorMessage`, `errorDetail`, `verdicts`, `checking`, `degraded`, `dirty`, `saveState`.
  **The cosmetic `dy` is NOT a field of this store at all** (D-184-02) — it lives only in
  `canvasNudge.ts` (184-07). Say so in the docblock: R4's "a nudge adds no history entry" is true by
  construction, not by a filter that could be misconfigured.

`temporal` options:
- `partialize: (s) => ({ phases: s.phases, lastEditKind: s.lastEditKind, editSeq: s.editSeq })`.
  **REQUIRED** — every `set()` on this store fires the temporal hook, so without it an arriving
  `/validate` response landing in `verdicts` would push an undo entry and `⌘Z` would undo a server
  verdict (Pitfall 8). Say that in the option's comment.
- `limit: HISTORY_LIMIT` with `export const HISTORY_LIMIT = 50`.
- `equality: (past, next) => past.phases === next.phases` — a cheap identity early-out.
- `handleSet` — the D-184-02 selective flush, implemented per RESEARCH Pattern 2: keep a closure timer
  and a `pending` args tuple; when the curried function's THIRD argument (`currentState`, the
  partialized current state) has `lastEditKind === "structural"`, flush any coalescing run first and
  then push immediately; otherwise store the args as `pending`, clear and restart a
  `CONFIG_COALESCE_MS` timer (`export const CONFIG_COALESCE_MS = 500`) whose expiry flushes.
  Export a way to flush on demand so a field blur can commit early. Contain the one unavoidable
  `handleSet` type cast in this file, comment it against `zundo/src/index.ts`'s `curriedHandleSet` call
  site, and never let it leak past the module boundary.

Actions (all immutable, all delegating to `definitionOps` — the store owns no mutation logic of its own):
`setDrafted(definition)`, `setComposing()`, `setErrorState(message, detail?)`,
`addPhaseOfType(type)` (uses `slugForType` + `minimalPhaseFor` + `addPhase`),
`insertPhaseOfTypeAt(index, type)`, `reorderPhase(slug, toIndex)` (uses `movePhase`),
`removePhaseBySlug(slug)`, `patchConfig(slug, patch)` (uses `patchPhaseConfig`),
`setVerdicts(v)`, `setChecking(b)`, `setDegraded(cause)`, `setSaveState(s)`, `markSaved()`.
Every structural action sets `lastEditKind: "structural"` and bumps `editSeq`; `patchConfig` sets
`lastEditKind: "config"` and bumps `editSeq`.

**D-184-03 dirty tracking.** Inside the factory, register a `store.subscribe` that flips `dirty` to
`true` whenever the `phases` reference changes and `dirty` is currently `false`. This is what makes an
undo across the save boundary honestly re-read `dirty` without any action knowing about undo.
`markSaved()` is the only thing that clears it. **No action in this store may call the API** — undo
never writes to the server (D-184-03).

Export `selectDefinition(state)` returning `{ ...state.meta, phases: state.phases }` — the ONE place
the two halves recombine for persistence. Document that the outgoing JSON key ORDER may differ from the
loaded row's and that this is deliberate and not observable (Pydantic is order-insensitive; the R3
guard asserts on the key SET, never on order).

**Do NOT add a `commitCanvasNodes` action here** — it depends on `fromCanvas`, which lands in 184-05,
and it is added in 184-10 where the drag path consumes it.

**`BuilderStoreProvider.tsx`.** A context carrying a `BuilderStore` instance, plus:
- `useBuilderStoreOptional(): BuilderStore | null` — null outside a provider, following the
  `useTechnicalNamesOptional` idiom so a leaf still renders in a provider-less unit test. **Do not make
  the store a required context for leaves.**
- `useBuilderStore()` — throws a named error, for consumers that genuinely require it.
- `useBuilderTemporal(selector)` — wraps `useStore(store.temporal, selector)`. Its docblock must state
  the React-19 rule: `store.temporal.getState()` in a rendered value is non-reactive (upstream issue
  #207); side-effect calls (`undo()`, `redo()`) may use `getState()` because they are not rendered
  values.
  </action>
  <verify>
    <automated>cd frontend && npm ls zundo</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls zundo` reports `zundo@2.3.0` with no unmet-peer warning
    - `node -e "console.log(JSON.stringify(require('./node_modules/zundo/package.json').scripts))"` (run in `frontend/`) shows no `postinstall` key
    - `frontend/package.json` gains exactly one dependency; `git diff --stat frontend/package.json` shows a single added line
    - `builderStore.ts` exports `createBuilderStore`, `TrackedSlice`, `HISTORY_LIMIT` (= 50), `CONFIG_COALESCE_MS` (= 500), `selectDefinition`
    - `grep -n 'partialize' frontend/src/components/workflows/builderStore.ts` shows a `partialize` returning exactly `phases`, `lastEditKind`, `editSeq`
    - `grep -nE 'fetch\(|from "@/lib/api"' frontend/src/components/workflows/builderStore.ts` returns no matches (D-184-03: undo never writes to the server)
    - `grep -n 'commitCanvasNodes' frontend/src/components/workflows/builderStore.ts` returns no matches (it lands in 184-10)
    - `BuilderStoreProvider.tsx` exports `useBuilderStoreOptional` returning `null` outside a provider
    - `useBuilderTemporal`'s implementation uses `useStore(store.temporal, selector)` and its docblock names upstream issue #207
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
  </acceptance_criteria>
  <done>`zundo@2.3.0` is installed under its `[OK]` legitimacy verdict; a per-mount store factory exists with a verdict-safe `partialize`, a 50-entry cap, the D-184-02 selective flush, and a React-19-safe temporal accessor.</done>
</task>

<task type="auto">
  <name>Task 2: builderStore.test.ts — falsify research assumption A3 before anything is built on it</name>
  <files>frontend/src/components/workflows/builderStore.test.ts</files>
  <read_first>
    - frontend/src/components/workflows/builderStore.ts (as written in Task 1)
    - frontend/src/__tests__/integration/streamsStore_per_thread.test.ts (the shipped store-layer test shape: `beforeEach` reset, direct `setState`/`getState` drive)
    - 184-RESEARCH.md § Assumptions Log A3 — "the debounce-with-flush composition is NOT copied from a shipped example. Prove it with a small unit test on the store BEFORE building the toolbar on it"
    - 184-VALIDATION.md § Per-Requirement Verification Map — the four R4 rows
  </read_first>
  <action>
Create `frontend/src/components/workflows/builderStore.test.ts`. No DOM, no React render — drive the
store directly. A per-mount factory means each test constructs its own store, so no `beforeEach` global
reset is needed; say so in the file docblock (it is the reason the factory shape is better).

Assertions, one per VALIDATION.md R4 row:
1. **Structural edits push immediately.** `addPhaseOfType("llm_single")` raises
   `store.temporal.getState().pastStates.length` by exactly 1 **synchronously**, with no timer advance.
   Repeat for `insertPhaseOfTypeAt`, `reorderPhase` and `removePhaseBySlug`.
2. **Config edits coalesce.** With `vi.useFakeTimers()`, two `patchConfig` calls inside 500 ms raise
   `pastStates.length` by exactly 1 **after** `await vi.advanceTimersByTimeAsync(500)`, and by 0 before
   the advance. A third `patchConfig` after the flush produces a second entry.
3. **A structural edit interleaved into a coalescing run flushes it first**, so the ordering is
   `[config-run, structural]` and no config edit is swallowed.
4. **`partialize` holds the definition slice only.** `setVerdicts([...])`, `setChecking(true)` and
   `setDegraded({kind:"network"})` each leave `pastStates.length` **unchanged**. This is R4's
   "a `verdicts` set pushes nothing" and Pitfall 8's guard.
5. **A nudge adds no history entry.** Assert the store has **no** `dy` / nudge field at all — a
   `Object.keys(store.getState())` check that none matches `/dy|nudge|offset|position/i`. D-184-02's
   claim is structural, so the test proves the structure, not a filter.
6. **Undo/redo semantics.** After three structural edits, `undo()` restores the previous `phases` and
   `futureStates.length` becomes 1; a NEW edit after an undo clears `futureStates` to 0 (zundo sets
   `futureStates: []` on every push).
7. **D-184-03.** `markSaved()` sets `dirty` false; a subsequent `undo()` flips `dirty` back to `true`;
   and no `fetch` call occurs anywhere in the suite (assert with a `vi.spyOn(globalThis,"fetch")` at 0
   calls) — undo never writes to the server.
8. **`limit` eviction.** After `HISTORY_LIMIT + 5` structural edits, `pastStates.length` equals exactly
   `HISTORY_LIMIT`.
9. **`selectDefinition`** returns an object whose key SET equals the input definition's key set, and
   whose `phases` is the store's current array. Assert on the key set, never on key order.

Add a **falsification record**: temporarily change `handleSet` so it always pushes immediately, confirm
assertion 2 fails, restore, and record both observations in the SUMMARY. That is what discharges
research assumption A3 rather than assuming it.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/builderStore.test.ts --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/builderStore.test.ts` passes with 0 failures
    - Assertion 2 uses `await vi.advanceTimersByTimeAsync(500)` (the async form — the sync form does not flush the awaited microtasks)
    - Assertion 4 is present and passes: three untracked setters leave `pastStates.length` unchanged
    - Assertion 5 is present: no key of the store state matches `/dy|nudge|offset|position/i`
    - The `fetch` spy reports exactly 0 calls across the suite
    - The A3 falsification is recorded in the SUMMARY with both the failing and the restored observation
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
  </acceptance_criteria>
  <done>Research assumption A3 is discharged by a falsified test: structural pushes are synchronous, config edits coalesce at 500 ms, verdicts never enter the stack, and the nudge is structurally absent.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint WorkflowBuilderPage onto the store — persistence untouched</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.tsx (the WHOLE file — this is the delicate edit of the phase)
    - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx `:414-438` (the five source guards this edit must keep satisfying, quoted in `<interfaces>`)
    - frontend/src/pages/WorkflowBuilderPage.test.tsx (all 15 assertions — none may be edited)
    - frontend/src/components/admin/revertByteIdentical.test.tsx (the 181 acceptance gate; it must stay green)
    - frontend/src/components/workflows/builderStore.ts and BuilderStoreProvider.tsx (as built in Tasks 1-2)
    - 184-PATTERNS.md § "WorkflowBuilderPage.tsx" — the persistence block quoted verbatim at `:347-372` and the warning that `onPersist`'s `[state]` dependency is "the ONE genuinely delicate line of the extraction"
  </read_first>
  <action>
Replace `useState<BuilderState>` (`:151`) as the home of the definition with the per-mount store, while
leaving persistence, composition, the flag gate and the describe/compose flow exactly where they are.

1. **Create the store per mount**: `const store = useState(() => createBuilderStore(initial))[0]` (or the
   equivalent `useRef` form) at the top of the component. **Never a module singleton** — a singleton
   would leak history between two Builder sessions.
2. **Read through selectors**, never `getState()` in a rendered value:
   `useStore(store, s => s.builderPhase)`, `useStore(store, s => s.phases)`, etc. Follow the shipped
   selector discipline at `usePanelReconcile.ts:58-60`.
3. **`onPhaseChange` (`:334-345`)** becomes a call to `store.getState().patchConfig(selectedSlug, patch)`.
   The immutable merge itself now lives in `definitionOps.patchPhaseConfig` — delete the inline copy so
   there is exactly ONE mutation home (D-184-05).
4. **`onPersist` (`:347-372`) keeps its body byte-for-byte** apart from where it reads the definition:
   read it inside the async body via `selectDefinition(store.getState())` — a side-effect read, per the
   selector rule — so the callback stays referentially stable and its `draftIdRef` / `creatingRef`
   create-once guard is untouched. `onSaveDraft` (`:374-394`) is unchanged in this plan; its 409 message
   lands in 184-11.
5. **Selection stays where it is.** `handleSelectNode` (`:205`), `clearSelection` (`:213`) and the
   Escape `useEffect` (`:224-231`) keep using component state. Moving selection is not part of D-184-01
   and would churn the 22 shipped canvas assertions for nothing.
6. **Wrap the returned JSX in `<BuilderStoreProvider store={store}>`** so canvas leaves can reach the
   store via the optional accessor. The provider renders children only and adds no DOM element — the
   flag-off branch must still render `graphChild` as the grid's FIRST child with no wrapper element
   (`:520-575`'s D-183-03 comment).
7. **Do not reformat** the lazy import at `:86` or the flag gate's literal `visual_workflow_canvas === true`
   at `:190-201`: both are regex-pinned by `WorkflowBuilderPage.canvas.test.tsx`. Do not introduce
   `localStorage`, `sessionStorage`, `aria-pressed` or the string `reactflow` anywhere in this file —
   all four are forbidden by the same shipped guards, and the nudge's storage lives in its own module
   (184-07) precisely because of this.
8. **Update any docblock this edit falsifies in the SAME commit.** If a comment still says the page owns
   the definition state, rewrite it — a comment that lies is the D-ITEM-183-02 trap that fired five
   times in Phase 183.

This is a state-home refactor, not a behaviour change. If any shipped assertion has to move, the
extraction is wrong: stop and report rather than editing the assertion (D-184-08's whole rationale).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=default</automated>
    <automated>cd frontend && npm test</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>cd frontend && git diff --exit-code -- 'src/**/__snapshots__/*'</automated>
  </verify>
  <acceptance_criteria>
    - All 15 `WorkflowBuilderPage.test.tsx`, all 22 `WorkflowBuilderPage.canvas.test.tsx` and all 7 `revertByteIdentical.test.tsx` assertions pass **unmodified**
    - `git diff` shows ZERO changes inside any pre-existing `*.test.ts`/`*.test.tsx` file
    - `grep -cE 'localStorage|sessionStorage|aria-pressed|reactflow' frontend/src/pages/WorkflowBuilderPage.tsx` returns 0
    - `grep -c 'visual_workflow_canvas === true' frontend/src/pages/WorkflowBuilderPage.tsx` returns >= 1
    - The lazy-import line at `:86` is byte-identical to its pre-edit text (`git diff` shows it untouched)
    - `grep -n 'createBuilderStore' frontend/src/pages/WorkflowBuilderPage.tsx` shows creation inside the component body, not at module scope
    - `grep -n 'config: { ...p.config' frontend/src/pages/WorkflowBuilderPage.tsx` returns no matches (the inline merge is gone; `definitionOps.patchPhaseConfig` is the one home)
    - `onPersist`'s `draftIdRef` / `creatingRef` create-once block is unchanged apart from the definition read — confirm with `git diff` and quote the diff hunk in the SUMMARY
    - `npm test` (full frontend suite) reports no NEW failures versus the pre-plan baseline; any pre-existing rot (SEED-049 Playwright, SEED-056 vitest rot) is named in the SUMMARY rather than fixed
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
  </acceptance_criteria>
  <done>The definition lives in the per-mount temporal store; persistence, composition, selection and the flag gate are untouched; every shipped assertion passes unmodified. Wave 0 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build tree | One net-new dependency (`zundo`) enters `package.json` |
| in-memory store → outgoing draft payload | `selectDefinition` is the single seam where store state becomes a request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-04-SC | Tampering | `npm install zundo` | mitigate | 184-RESEARCH.md's `## Package Legitimacy Audit` records the full chain: registry age 5y4m, 336,208 downloads/wk, MIT, listed in zustand's OWN official third-party docs, `slopcheck` verdict `[OK]`, and `npm view zundo scripts` shows **no `postinstall`**. Re-verified in this plan's acceptance criteria. Disposition Approved — no `[ASSUMED]`/`[SUS]` human gate required |
| T-184-04-01 | Tampering | outgoing draft payload | mitigate | `selectDefinition` is the ONE recombination point and adds no key; the store holds no positional field at all, so a layout key cannot reach the payload by construction (R3). The payload key-SET assertion lands in 184-11 |
| T-184-04-02 | Repudiation | undo crossing the save boundary | mitigate | D-184-03: the store contains no API call (asserted by a `?raw`/grep guard and a 0-call `fetch` spy), so an undo can never silently PATCH the server. `dirty` re-arms honestly instead |
| T-184-04-03 | Elevation of privilege | flag-off surface drift | mitigate | The state-home refactor is gated on unmodified shipped assertions, a green `revertByteIdentical.test.tsx`, a byte-unchanged canvas snapshot and a no-wrapper-element flag-off branch |
| T-184-04-04 | Information disclosure | per-mount store lifetime | mitigate | The store is created per Builder mount, so one workflow's edit history cannot appear inside another workflow's session on a shared browser |
</threat_model>

<verification>
- `cd frontend && npm test` — no new failures versus the pre-plan baseline
- `node scripts/vitest-count-gate.cjs` — exit 0, all 16 baseline files at their pinned counts
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `cd frontend && git diff --exit-code -- 'src/**/__snapshots__/*'` exits 0
- `git diff` contains ZERO edits inside any pre-existing `*.test.ts`/`*.test.tsx` file
- `git diff --name-only -- supabase/migrations | wc -l` returns 0
- `git diff --name-only -- backend/ | wc -l` returns 0 (frontend-only phase)
</verification>

<success_criteria>
- `zundo@2.3.0` is the phase's only net-new dependency, installed under a re-verified `[OK]` legitimacy verdict
- One per-mount temporal store owns the definition; `partialize` keeps verdicts, checking and selection out of the undo stack
- Structural edits push synchronously; config edits coalesce at 500 ms — proven by a falsified test that discharges assumption A3
- The cosmetic `dy` is structurally absent from the store
- `WorkflowBuilderPage.tsx` keeps persistence, composition, selection and the flag gate; every shipped assertion passes unmodified
- **Wave 0 is complete**: D-184-08's gate has passed on all three extraction commits, with the icon commit (184-01) as the one enumerated carve-out
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-04-SUMMARY.md` when done.
It MUST include: (a) the A3 falsification observations, (b) the `onPersist` diff hunk, (c) the complete
enumerated list of import-path-only changes across ALL of Wave 0 (184-01 through 184-04), and (d) an
explicit Wave-0 gate report: per-file counts, `tsc` count, snapshot diff status, and `revertByteIdentical`
result.
</output>
