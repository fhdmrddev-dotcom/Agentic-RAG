---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 03
type: execute
wave: 2
depends_on: ["184-01"]
files_modified:
  - frontend/src/components/workflows/nodePresentation.ts
  - frontend/src/components/workflows/PhaseNodeCard.tsx
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
  - frontend/src/components/workflows/PhaseNode.tsx
autonomous: true
requirements: [CANVAS-02, VALID-03]

must_haves:
  truths:
    - "D-184-06: PhaseNode.tsx splits into a purely presentational PhaseNodeCard with ZERO @xyflow import and a thin NodeProps→slots adapter; the card renders outside a ReactFlowProvider in a plain test"
    - "D-184-06: nodePresentation.ts owns ICON_TINT, DEFAULT_TINT, GROUNDING_TONE and renderPhaseMark, moved verbatim with their docblocks intact"
    - "D-184-06: the badge slot is typed as a max-2 tuple, so Phase 185 physically CANNOT add a third badge — the 137-D two-badge budget is enforced by the type system"
    - "D-184-08: the split is behaviour-preserving — the canvas snapshot is byte-unchanged, every per-file test count holds, and zero shipped assertions are edited"
    - "VALID-03: the card carries an OPTIONAL verdict slot whose absence renders nothing, so Wave 0 adds the seam without adding behaviour"
  artifacts:
    - path: "frontend/src/components/workflows/nodePresentation.ts"
      provides: "The canvas node presentation const-tables and mark resolver"
      exports: ["ICON_TINT", "DEFAULT_TINT", "GROUNDING_TONE", "renderPhaseMark"]
    - path: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      provides: "The presentational card + its slot contract"
      exports: ["PhaseNodeCard", "PhaseNodeCardProps", "BadgeSlot", "BadgeSlots", "NodeVerdictMark"]
    - path: "frontend/src/components/workflows/PhaseNodeCard.test.tsx"
      provides: "Provider-less render proof + the zero-@xyflow source guard"
  key_links:
    - from: "frontend/src/components/workflows/PhaseNode.tsx"
      to: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      via: "the NodeProps → slots adapter (the adapter is the ONLY file of the pair that imports @xyflow/react)"
      pattern: "import \\{ PhaseNodeCard \\}"
---

<objective>
Land the presentational half of the Wave-0 G-5 extraction: split `PhaseNode.tsx` into a pure card, a
const-table module, and a thin adapter — before any editing feature code touches it.

Purpose: `PhaseNode.tsx` is on its 2nd touch here and its 3rd in Phase 185. D-184-06 makes the card
purely presentational with **zero `@xyflow` import**, so it renders in a plain test and Phase 188 can
reuse it outside a `ReactFlowProvider`; and it types the badge slot as a max-2 tuple so 185 physically
cannot spend the 137-D two-badge budget. This is sketch extensibility seam #1: 185 / 188 / 189 add
**data, not layout**.

Output: `nodePresentation.ts` + `PhaseNodeCard.tsx` + `PhaseNodeCard.test.tsx`, with `PhaseNode.tsx`
collapsed to an adapter. **Behaviour-preserving by construction** — the canvas snapshot must not move
and no shipped assertion may be edited (D-184-08).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
@.planning/sketches/137-agentic-canvas-look/README.md
@.planning/sketches/themes/canvas-184.css
</context>

<interfaces>
<!-- The identifiers that ACTUALLY exist. CONTEXT.md and RESEARCH.md drifted on both names and
     lines; PATTERNS.md re-derived them from the files this session. Use these. -->

`frontend/src/components/workflows/PhaseNode.tsx` — the real declarations:
- `ICON_TINT` at `:113` (NOT `PHASE_TINTS` — no such identifier exists anywhere in the repo), `DEFAULT_TINT` at `:122`
- `GROUNDING_TONE` at `:137` — **singular** (NOT `GROUNDING_TONES`)
- `renderPhaseMark` at `:159` (module scope, returns a `ReactNode`)
- `PhaseNode` at `:171`, `EdgeAnchors` at `:81-106`, `UnresolvedSkipNode` at `:278`, `EndCapNode` at `:311`
- the `@xyflow/react` import at `:66`: `import { Handle, Position, type NodeProps } from "@xyflow/react"`
- the `PHASE_GLYPHS` import from `soulData` at `:69` (imported, never redeclared — verified)

The DOM contract the shipped canvas suite selects on (`PhaseNode.tsx:182-189`, `:215`, `:225`) —
changing ANY of these breaks a shipped assertion, which is a D-184-08 failure:
```
data-testid={`canvas-node-${data.slug}`}
data-slug={data.slug}
data-phase-type={data.phaseType}
data-selected={selected ? "true" : "false"}
className="relative"
style={{ width: CANVAS_LAYOUT.NODE_WIDTH, minHeight: CANVAS_LAYOUT.NODE_MIN_HEIGHT }}
```
plus `data-grounding={data.grounding.mode}` and `data-waits-for-you="true"` on the badge wrappers,
whose `StatusChip` testIds are `"canvas-grounding"` and `"canvas-waits-for-you"`.

The machine-checked invariant that constrains where editing affordances may live
(`WorkflowCanvas.test.tsx:231-238`): **no focusable control may exist inside any node** —
`node.querySelectorAll("button, a, [tabindex]")` must have length 0 for every `.react-flow__node`.
The ✕-delete and `＋`-insert affordances therefore live on the plane/lane, never in the card.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: nodePresentation.ts — move the const-tables and the mark resolver verbatim</name>
  <files>frontend/src/components/workflows/nodePresentation.ts, frontend/src/components/workflows/PhaseNode.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PhaseNode.tsx (the whole file — the header docblock at `:16-55`, `ICON_TINT` `:108-122`, `GROUNDING_TONE` `:124-141`, `renderPhaseMark` `:143-162`)
    - frontend/src/components/org/StatusChip.tsx (the whole file — the Phase-177 shared-primitive docblock is the scope-discipline template for splitting a shared COMPONENT from a domain TONE MAP)
    - frontend/src/components/workflows/phaseVocabulary.ts `:17-29` — the W0c extraction-honesty docblock form: an extraction claim must name the `?raw` guard that keeps it honest
    - 184-PATTERNS.md § "nodePresentation.ts" — the verbatim blocks to move, with corrected identifiers
  </read_first>
  <action>
Create `frontend/src/components/workflows/nodePresentation.ts` and move four declarations out of
`PhaseNode.tsx` **verbatim, docblocks intact**:

- `ICON_TINT` (the per-step-type tint that sits BEHIND the floating mark — the whole of this surface's
  type-colour budget under 137-D) and `DEFAULT_TINT`. Keep the `Record<string, string>` shape and every
  HSL literal byte-for-byte.
- `GROUNDING_TONE` (singular) — the canvas-domain tone mapping for the shared `StatusChip`. **Its
  docblock already cites StatusChip's scope rule; preserve it verbatim on the move** — it is the record
  of why this is documented reuse rather than a fork.
- `renderPhaseMark(phaseType: string): ReactNode` — move it and `export` it. Keep it at MODULE scope in
  the new file too: its docblock explains that `phaseGlyph()` returns a COMPONENT and binding a
  component to a local during render is what `react-hooks/static-components` correctly flags. That
  docblock is load-bearing for a lint rule; move it intact.

Add a module docblock in the `phaseVocabulary.ts:17-29` form: state literally what was cut out of
`PhaseNode.tsx`, that no re-export shim was left behind, and **name the `?raw` guard added in Task 3
that keeps the claim honest**. Per CONTEXT anti-drift note 3, an extraction claim with no guard behind
it is exactly the hazard `soulData.ts` demonstrated — no guard, no claim.

Then edit `PhaseNode.tsx` to import all four from `nodePresentation` and delete the local copies. This
is an **import-path-only change** in D-184-08 terms: no rendered output changes, no assertion changes.
Enumerate every import-path edit in the SUMMARY.

Leave `EdgeAnchors`, `UnresolvedSkipNode` and `EndCapNode` in `PhaseNode.tsx` untouched in this task.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/canvasModel.fixtures.test.ts --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && git diff --exit-code -- 'src/**/__snapshots__/*'</automated>
  </verify>
  <acceptance_criteria>
    - `nodePresentation.ts` exports `ICON_TINT`, `DEFAULT_TINT`, `GROUNDING_TONE`, `renderPhaseMark`
    - `grep -nE 'const (ICON_TINT|DEFAULT_TINT|GROUNDING_TONE)|function renderPhaseMark' frontend/src/components/workflows/PhaseNode.tsx` returns no matches (a hard cut, no re-export shim)
    - The HSL literals in `nodePresentation.ICON_TINT` are byte-identical to the pre-split values — verify with `git show HEAD~1:frontend/src/components/workflows/PhaseNode.tsx | grep -A8 'ICON_TINT'` compared against the new file
    - `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0
    - `node scripts/vitest-count-gate.cjs` exits 0 with every one of the 16 baseline files at its pinned count and 0 failures
    - `git diff` shows ZERO changes inside any `*.test.ts`/`*.test.tsx` file
  </acceptance_criteria>
  <done>The three const-tables and the mark resolver live in one module with their docblocks intact; `PhaseNode.tsx` imports them; the canvas snapshot and every test count are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: PhaseNodeCard.tsx — the zero-@xyflow card and its slot contract; PhaseNode becomes an adapter</name>
  <files>frontend/src/components/workflows/PhaseNodeCard.tsx, frontend/src/components/workflows/PhaseNode.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PhaseNode.tsx (as left by Task 1 — especially the render body `:171-261` being lifted, the `ONE TAB STOP PER NODE` invariant at `:16-23`, and the HANDLES ARE MANDATORY note at `:26-34`)
    - frontend/src/components/panel/PhaseCard.tsx (the structural analog: props-driven card, a locked type-meta table, an UNKNOWN forward-compat fallback, and the non-colour-alone rule at `:17-20`)
    - frontend/src/components/org/StatusChip.tsx (the shared chip the badges render through)
    - frontend/src/components/workflows/canvasModel.ts `:108-153` — `PhaseNodeData`, the data shape the adapter maps into slots
    - .planning/sketches/themes/canvas-184.css — `body.card-b` block: the locked 137-B card geometry (248px card, floating icon owning the TOP edge, per-node actions on the BOTTOM edge, verdict mark on the RIGHT edge, step number INSIDE the card)
  </read_first>
  <action>
Create `frontend/src/components/workflows/PhaseNodeCard.tsx` by lifting the entire current `PhaseNode`
render body (`PhaseNode.tsx:171-261`) **minus `<EdgeAnchors />`**, and shrink `PhaseNode` to a thin
`NodeProps` → slots adapter.

`PhaseNodeCard` must import NOTHING from `@xyflow/react`. That is the whole point of D-184-06 and it is
machine-checked in Task 3. The adapter keeps the `@xyflow/react` import at `PhaseNode.tsx:66` and keeps
rendering `<EdgeAnchors />` — a custom node that renders no `Handle` paints ZERO edges, silently, with
no warning (`PhaseNode.tsx:26-34`).

Slot contract, exported from `PhaseNodeCard.tsx`:
- `icon: ReactNode` · `title: string` · `subtitle?: string` · `technicalLine?: string`
- `tint: string` (resolved by the adapter from `ICON_TINT`, with `DEFAULT_TINT` as the fallback)
- `badges?: BadgeSlots` where `type BadgeSlot = { testId: string; tone: ChipTone; label: string; dataAttr?: Record<string,string> }` and
  `type BadgeSlots = readonly [] | readonly [BadgeSlot] | readonly [BadgeSlot, BadgeSlot]`.
  **The max-2 tuple is the enforcement mechanism** — a three-element literal must be a typecheck error,
  not a review comment. State the 137-D budget in the prop's docblock.
- `status?: NodeRunStatus` — declared and rendered as nothing in 184; the slot exists so Phase 188 adds
  data, not layout. Do not invent run states here.
- `verdict?: NodeVerdictMark` where `type NodeVerdictMark = "error" | "incomplete" | "unknown"`.
  **In this plan the slot renders NOTHING when absent and is never passed by the adapter**, so the
  extraction stays behaviour-preserving. The actual marks land in 184-08.
- `stepNumber?: number` · `selected?: boolean` · `slug: string` · `phaseType: string`

The DOM the card emits MUST match the pre-split output byte-for-byte for the props the adapter passes
today. Preserve every attribute quoted in `<interfaces>`: `data-testid`, `data-slug`, `data-phase-type`,
`data-selected`, `className="relative"`, the `CANVAS_LAYOUT` width/min-height style, and both badge
wrapper data-attributes with their `canvas-grounding` / `canvas-waits-for-you` testIds.

**The card contains NO focusable control of any kind** — no inner pressable element, no anchor, no
tabindex, no click handler. Carry the `ONE TAB STOP PER NODE` docblock (`PhaseNode.tsx:16-23`) onto the
card so the rule travels with the code it constrains. This is what forces the ✕/`＋` affordances onto
the lane in 184-12.

The adapter (`PhaseNode`) keeps its exact exported signature `({ data, selected }: NodeProps<PhaseCanvasNode>)`
and does nothing but resolve `data` into slots (including the `data.technical === true` title swap that
keeps the ⌥ reveal a single app-wide state) and render `<PhaseNodeCard {...slots} />` beside
`<EdgeAnchors />`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/canvasModel.fixtures.test.ts src/pages/WorkflowBuilderPage.canvas.test.tsx --reporter=default</automated>
    <automated>cd frontend && git diff --exit-code -- 'src/**/__snapshots__/*'</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@xyflow' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0
    - `grep -c '@xyflow/react' frontend/src/components/workflows/PhaseNode.tsx` returns >= 1 (the adapter is the only file of the pair that imports it)
    - `PhaseNode` still exports the signature `({ data, selected }: NodeProps<PhaseCanvasNode>)` and still renders `<EdgeAnchors />`
    - `PhaseNodeCard.tsx` exports `BadgeSlots` typed as `readonly [] | readonly [BadgeSlot] | readonly [BadgeSlot, BadgeSlot]`; a temporary three-element badge literal produces a `tsc` error — record the observed error code in the SUMMARY, then remove the probe
    - `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0 (the split is DOM-identical)
    - All 31 `WorkflowCanvas.test.tsx` and all 22 `WorkflowBuilderPage.canvas.test.tsx` assertions pass **unmodified**
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `git diff` shows ZERO changes inside any `*.test.ts`/`*.test.tsx` file
  </acceptance_criteria>
  <done>The card is presentational, provider-free and budget-typed; the adapter is thin; the rendered DOM and the canvas snapshot are byte-identical to before the split.</done>
</task>

<task type="auto">
  <name>Task 3: PhaseNodeCard.test.tsx — the provider-less render proof and the zero-@xyflow source guard</name>
  <files>frontend/src/components/workflows/PhaseNodeCard.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PhaseNodeCard.tsx (as written in Task 2)
    - frontend/src/components/workflows/PhaseSpine.test.tsx (the provider-less leaf-render analog — this suite needs NO `mockReactFlow`, which is the whole point of the zero-@xyflow card)
    - frontend/src/components/workflows/canvasModel.purity.test.ts `:14-17` — the `?raw` import idiom for a machine-checkable scope fence
    - frontend/src/components/workflows/WorkflowCanvas.test.tsx `:30-32` and `:120-137` — the `?raw` precedent docblock and the `fireEvent`-not-`user-event` rule inside the plane (this suite renders OUTSIDE the plane, so `user-event` is legal here)
  </read_first>
  <action>
Create `frontend/src/components/workflows/PhaseNodeCard.test.tsx`.

It renders `PhaseNodeCard` with plain `render()` from `@testing-library/react` — **no
`ReactFlowProvider`, no `mockReactFlow()` call, no `@xyflow` import anywhere in the file**. If this
suite needs either, the extraction is wrong and the task is not done.

Assertions:
1. A minimal card renders with the `canvas-node-<slug>` testId, `data-slug`, `data-phase-type` and
   `data-selected="false"`, outside any provider, without throwing.
2. `data-selected` flips to `"true"` when `selected` is passed.
3. The ⌥ reveal: passing `technicalLine` renders it; omitting it renders nothing extra.
4. **No focusable control exists inside the card**: `container.querySelectorAll("button, a, [tabindex]")`
   has length 0. This is the leaf-level mirror of the shipped canvas-level assertion at
   `WorkflowCanvas.test.tsx:231-238`, so the invariant is now guarded at BOTH levels.
5. Badge budget: zero, one and two badges each render their `StatusChip` testIds; the `dataAttr` pairs
   land on the wrapper.
6. Absent optional slots render nothing: with `verdict` and `status` omitted, no element carries a
   verdict or status testId. **This is the assertion that proves Wave 0 added the seam without adding
   behaviour.**
7. A `?raw` source guard, in the `canvasModel.purity.test.ts:14-17` idiom, appended as its own
   `describe` block: `expect(phaseNodeCardSource).not.toMatch(/@xyflow/)` and
   `expect(phaseNodeCardSource).not.toMatch(/from "@\/lib\/api"/)`. Add a **positive control** in the
   house style proving the guard can fail — assert the same regex DOES match a planted literal string,
   so a broken guard is visible.
8. Forward-compat: an unrecognised `phaseType` renders the card with `DEFAULT_TINT` and does not throw
   (mirroring `PhaseCard.tsx`'s `UNKNOWN_PHASE_META` discipline).

This is a NET-NEW file. It must not replace or absorb any existing suite — the Phase-177 lesson is that
a "net-new" file swallowing an existing one is invisible to a failures-only differential, which is
exactly why `scripts/vitest-count-gate.cjs` gates per-file counts.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` passes with 0 failures
    - `grep -cE '@xyflow|mockReactFlow|ReactFlowProvider' frontend/src/components/workflows/PhaseNodeCard.test.tsx` returns 0
    - The `?raw` guard's positive control is present and passes (the regex demonstrably matches a planted literal)
    - The "absent optional slots render nothing" assertion is present and passes
    - `node scripts/vitest-count-gate.cjs` exits 0 with every one of the 16 baseline files still at its pinned count — no file swallowed, only the total raised
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0
  </acceptance_criteria>
  <done>The card provably renders outside a `ReactFlowProvider`, provably imports no `@xyflow`, and provably renders nothing for the slots Phase 185/188 will fill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-supplied phase `name` / `slug` → rendered node face | Author-controlled strings render as node titles and data attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-03-01 | Tampering | `PhaseNodeCard` title / subtitle / technicalLine | mitigate | Rendered as plain React text children and `title=` attributes only — never `dangerouslySetInnerHTML`. This is the shipped T-124-01 rule in `PhaseNode.tsx`'s docblock, carried onto the card with the code it constrains |
| T-184-03-02 | Denial of service | unknown `phase_type` | mitigate | `ICON_TINT` lookup falls back to `DEFAULT_TINT`; the card never throws on an unrecognised discriminator (asserted in Task 3) |
| T-184-03-03 | Elevation of privilege | flag-off surface drift | mitigate | The split is DOM-identical by construction and gated on a byte-unchanged canvas snapshot plus unmodified shipped assertions — the extraction cannot change what a flag-off audience sees |
| T-184-03-SC | Tampering | npm installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx` — 0 failures
- `node scripts/vitest-count-gate.cjs` — exit 0, all 16 baseline files at their pinned counts
- `cd frontend && git diff --exit-code -- 'src/**/__snapshots__/*'` exits 0
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `git diff` contains ZERO edits inside any pre-existing `*.test.ts`/`*.test.tsx` file (D-184-08)
- `git diff --name-only -- supabase/migrations | wc -l` returns 0
</verification>

<success_criteria>
- `PhaseNodeCard` renders in a plain test with no `ReactFlowProvider` and no `@xyflow` import, proven by a `?raw` guard with a positive control
- The badge slot is a max-2 tuple whose violation is a typecheck error
- `nodePresentation.ts` owns the tint table, the tone map and the mark resolver, with an extraction docblock that names its guard
- The canvas snapshot is byte-unchanged and every per-file test count holds — the split is provably behaviour-preserving
- Every import-path-only edit is enumerated in the SUMMARY (D-184-08)
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-03-SUMMARY.md` when done.
It MUST include: (a) the enumerated list of every import-path-only change, (b) the observed `tsc` error
from the three-badge probe, and (c) an explicit statement that **zero** assertions in pre-existing test
files were edited.
</output>
