---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 02
type: execute
wave: 2
depends_on: ["184-01"]
files_modified:
  - scripts/vitest-count-gate.cjs
  - frontend/src/components/workflows/definitionOps.ts
  - frontend/src/components/workflows/definitionOps.test.ts
autonomous: true
requirements: [CANVAS-02]

must_haves:
  truths:
    - "D-184-05: definitionOps.ts is the ONE mutation home shared by both views — addPhase / insertPhaseAt / movePhase / removePhase / renumber / patchPhaseConfig, all (phases, args) => phases, zero React, zero store import, zero @/lib/api import"
    - "D-184-08: a per-file test-COUNT differential gate exists as a committed script, so a deleted test is visible (the Phase-177 lesson a failures-only differential cannot catch)"
    - "D-184-10: the drag axis split is a pure resolveDrop(origin, dropped, lanes) function — a purely vertical drag can never produce a definition edit"
    - "D-184-11: a new step's slug is generated from the closed 6-member phase-type set plus a numeric suffix, never from user text, and is not editable in this phase"
    - "After any add / insert / reorder / delete, phase_index is exactly [0..n-1] in render order with no duplicates and no holes"
    - "The two cheap shape refusals (orphaning delete, stranding add) are pure predicates over phases[] and consult /workflows/validate zero times"
  artifacts:
    - path: "frontend/src/components/workflows/definitionOps.ts"
      provides: "The one pure definition-mutation module"
      exports: ["addPhase", "insertPhaseAt", "movePhase", "removePhase", "renumber", "patchPhaseConfig", "canRemovePhase", "allowedTypesAt", "slugForType", "minimalPhaseFor", "resolveDrop"]
      min_lines: 180
    - path: "frontend/src/components/workflows/definitionOps.test.ts"
      provides: "R1 contiguity proof, R10a/R10b refusal proofs, D-184-10 axis-split proof"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "D-184-08 per-file count differential"
  key_links:
    - from: "frontend/src/components/workflows/definitionOps.ts"
      to: "frontend/src/components/workflows/phaseVocabulary.ts"
      via: "import of the shipped parseSkipTarget (never a second copy)"
      pattern: "from \"@/components/workflows/phaseVocabulary\""
---

<objective>
Land the pure half of the Wave-0 G-5 extraction: one module that owns every mutation of
`WorkflowDefinition.phases`, plus the mechanical gate that proves Wave 0 was behaviour-preserving.

Purpose: D-184-05 pulls definition mutation out of `WorkflowBuilderPage.tsx` (its 3rd authoring door,
a G-5-firing hot file) into a module with zero React and zero store dependency, so R1's
contiguous-`phase_index` proof and R10's two shape refusals become plain unit tests with no canvas
and no store. D-184-08's count gate ships here because every later Wave-0 commit is measured against it.

Output: `definitionOps.ts` + its test suite + `scripts/vitest-count-gate.cjs`. **No production caller
is wired in this plan** — `WorkflowBuilderPage.tsx` is repointed in 184-04, so this plan cannot
regress a shipped surface.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-RESEARCH.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
</context>

<interfaces>
<!-- Contracts the executor needs. Do not re-derive these from the codebase. -->

From `frontend/src/components/workflows/phaseVocabulary.ts` (import, never re-declare — a second copy
of `parseSkipTarget` is grep-forbidden by `canvasModel.purity.test.ts:117-118` and the same discipline
applies here):
- `PhaseSpecJSON` — `{ slug: string; phase_index: number; name?: string; config: { phase_type: string; ... }; validators?: Array<{ kind: string; on_failure?: string; ... }> }`
- `parseSkipTarget(onFailure: string | null | undefined): string | null` — prefix-LENGTH slice (the D-183-02-A C-1 correction)
- `PHASE_TYPE_SENTENCES` — the D-183-06 plain-language sentences per phase type
- `nodeTitle(phase)` / `technicalTitle(phase)`

From `frontend/src/components/workflows/canvasModel.ts` (do NOT import it here — `definitionOps` must
stay free of canvas concepts; this is quoted only so `renumber`'s comparator MATCHES it verbatim):
```
  const ordered = [...phases].sort(
    (a, b) => a.phase_index - b.phase_index || (a.slug < b.slug ? -1 : a.slug > b.slug ? 1 : 0),
  )
```

The 6-member closed `phase_type` set (backend `harness.py` discriminated union):
`programmatic` · `llm_single` · `llm_agent` · `llm_batch_agents` · `llm_human_input` · `llm_emit`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Ship the D-184-08 per-file test-count differential gate</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - 184-VALIDATION.md § "Wave 0 Count Pin (D-184-08 — the mechanical half)" — the 16-file / 424-test table and the reproduce-mechanically snippet
    - scripts/check-181-scope-freeze.sh (the shipped shape for a committed gate script: explicit exit codes, named failure reasons, no watch flags)
  </read_first>
  <action>
Create `scripts/vitest-count-gate.cjs` — a Node CommonJS script that runs the Wave-0 blast-radius
vitest suites with `--reporter=json` and asserts the D-184-08 mechanical gate.

Embed the baseline table from 184-VALIDATION.md as a module-level `const BASELINE` object keyed by
bare filename with the pinned counts: `canvasModel.fixtures.test.ts` 100 · `canvasModel.purity.test.ts`
69 · `phaseVocabulary.test.ts` 42 · `WorkflowCanvas.test.tsx` 31 · `canvasModel.test.ts` 26 ·
`PublishGauntlet.test.tsx` 24 · `WorkflowBuilderPage.canvas.test.tsx` 22 · `PhaseFormPanel.test.tsx` 19 ·
`WorkflowBuilderPage.test.tsx` 15 · `PhaseSpineGraph.test.tsx` 14 · `soulData.test.ts` 14 ·
`WorkflowDoorSwitch.test.tsx` 13 · `PhaseSpine.test.tsx` 11 · `deriveTier.test.ts` 9 ·
`WorkflowSoul.test.tsx` 8 · `revertByteIdentical.test.tsx` 7 — total 424.

Behaviour:
- Spawn `npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=json --outputFile=<tmp>` with `cwd` = `frontend`. Write the JSON to a path under the OS temp dir, never into `frontend/` or `backend/` (the "no scratch in a watched tree" rule — uvicorn/vite reload watchers wedge on Windows).
- Use `--reporter=json`. **Never `--reporter=basic`** (removed in vitest 4) and never a watch flag.
- Parse `numTotalTests`, `numFailedTests`, and per-suite `assertionResults.length` keyed by the bare filename of `testResults[].name`.
- FAIL (exit 1) with a named reason when: `numFailedTests > 0`; `numTotalTests < 424`; any baseline file is MISSING from the run; or any baseline file's count DECREASED. A count that INCREASED is allowed and printed as `+N` (feature waves add tests).
- PASS (exit 0) prints a per-file table with `pinned / actual / delta` and the total.
- Accept an optional `--json <path>` flag to reuse an already-produced report instead of re-running, so a caller that just ran the suite does not pay for it twice.

The script is committed tooling. It must not import anything from `frontend/src` and must not require
a database, a backend, or network access.
  </action>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>node --check scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/vitest-count-gate.cjs` exits 0 on the current tree and prints a table containing all 16 baseline filenames and the line `total 424` (or a higher total with explicit `+N` deltas)
    - The script source contains the literal `--reporter=json` and contains neither `--reporter=basic` nor `--watch`
    - Falsification control: temporarily deleting one `it(...)` block from `frontend/src/components/workflows/deriveTier.test.ts` makes the script exit 1 with a message naming `deriveTier.test.ts` and the decrease; restore the file afterwards and re-run to confirm exit 0. Record both observations in the SUMMARY
    - The script writes its JSON report outside `frontend/` and outside `backend/` (assert the resolved output path in the SUMMARY)
    - `git status --porcelain frontend/` is clean after a run (the script leaves no artifact in the app tree)
  </acceptance_criteria>
  <done>`node scripts/vitest-count-gate.cjs` is a committed, falsified, exit-code-honest gate for D-184-08's per-file count pin.</done>
</task>

<task type="auto">
  <name>Task 2: definitionOps.ts — the six pure structural ops + R1 contiguity proof</name>
  <files>frontend/src/components/workflows/definitionOps.ts, frontend/src/components/workflows/definitionOps.test.ts</files>
  <read_first>
    - frontend/src/components/workflows/deriveTier.ts (the whole file — the house purity-module template: docblock shape, `as const satisfies Record<K,V>` const-table idiom, the runtime-safe exhaustiveness guard at `:102-129`)
    - frontend/src/components/workflows/phaseVocabulary.ts (its TOTALITY contract docblock at `:31-40`; `parseSkipTarget` at `:94-98`; `PHASE_TYPE_SENTENCES` at `:108`)
    - frontend/src/pages/WorkflowBuilderPage.tsx `:334-345` — `onPhaseChange`, the immutable config-merge being lifted into `patchPhaseConfig`
    - frontend/src/components/workflows/canvasModel.ts `:203-223` — the purity docblock and the total `(phase_index, slug)` sort comparator `renumber` must match verbatim
    - frontend/src/components/workflows/canvasModel.fixtures.test.ts `:18-63` and `:91-95` — the `describe.each` sweep and the throwing named-fixture accessor
    - frontend/src/components/workflows/__fixtures__/canvasFixtures.ts (read-only: `eval_coverage`, `indexGap` at `:260-269`, `ALL_FIXTURES`) — **do not modify this file**
    - frontend/src/components/workflows/deriveTier.test.ts (the plain-unit test shape)
  </read_first>
  <action>
Create `frontend/src/components/workflows/definitionOps.ts` — pure, zero React, zero store import,
zero `@/lib/api` import, zero `@/components/workflows/canvasModel` import.

Module docblock, following the `deriveTier.ts:1-22` / `phaseVocabulary.ts:31-40` template, must state
(a) the single-source claim naming D-184-05, (b) the purity contract ("imports NOTHING from the API
client"), (c) the TOTALITY contract (an unknown `phase_type`, a missing `validators` array and a
malformed `on_failure` all resolve honestly and NEVER throw — the definition JSONB is author-supplied),
and (d) the honest extraction statement in the `phaseVocabulary.ts:17-29` form: name what was lifted
out of `WorkflowBuilderPage.tsx` and name the guard that keeps the claim honest. **Do not claim an
extraction this plan does not perform** — `WorkflowBuilderPage.tsx` is repointed in 184-04, so the
docblock must say the page repoint lands there, not that it has already happened.

Export these six ops, all `(phases: readonly PhaseSpecJSON[], …args) => PhaseSpecJSON[]`, all
non-mutating (`[...phases]`, `.map(p => ({...p}))` — never in-place):

- `renumber(phases)` — sorts with the EXACT comparator quoted in `<interfaces>` (index, then slug as
  tiebreak) and rewrites `phase_index` to its 0-based position. This is the ONLY function in the phase
  that renumbers; `fromCanvas` (184-05) deliberately does not.
- `addPhase(phases, spec)` — appends `spec` and renumbers.
- `insertPhaseAt(phases, index, spec)` — inserts at render position `index` (clamped to `[0, n]`) and
  renumbers.
- `movePhase(phases, slug, toIndex)` — moves the phase with `slug` to render position `toIndex`
  (clamped) and renumbers. Unknown slug returns the input unchanged (TOTALITY).
- `removePhase(phases, slug)` — drops the phase with `slug` and renumbers. Unknown slug returns the
  input unchanged.
- `patchPhaseConfig(phases, slug, patch)` — the immutable merge lifted verbatim from
  `WorkflowBuilderPage.tsx:334-345`: `p.slug === slug ? { ...p, config: { ...p.config, ...patch } } : p`.
  It does NOT renumber (a config edit cannot change order).

Then create `definitionOps.test.ts`. Use the `describe.each` sweep shape from
`canvasModel.fixtures.test.ts:52-63` over `ALL_FIXTURES`, plus the throwing named-fixture accessor from
`:91-95`. Cover:
- For every fixture and every op: the resulting `phase_index` array equals `[0..n-1]` exactly — no
  duplicate, no hole. Assert on the sorted-by-position render order, not on the input order.
- A **planted-gap positive control** in the `canvasModel.purity.test.ts:69-72` style: build a phases
  array with indices `[0, 1, 3]` (mirroring the shipped `indexGap` shape) and assert the contiguity
  helper FINDS the hole before `renumber` and does not after — proving the assertion can fail.
- R1's named case: `insertPhaseAt(fixture("eval_coverage"), 2, minimal)` increments **every** downstream
  `phase_index` by exactly 1, asserted index by index, and the array length grows by exactly 1.
- Input immutability: after every op, the ORIGINAL array and its member objects are `toStrictEqual`
  their pre-call clones (no in-place mutation).
- `patchPhaseConfig` preserves every non-patched config key and every non-config phase key, and leaves
  `phase_index` untouched.
- TOTALITY: an unknown slug, an empty `phases` array, and a phase with no `validators` key each resolve
  without throwing.

Do NOT add fixtures to `__fixtures__/canvasFixtures.ts` — its header carries an acceptance guard and
D-184-17 puts new corpus data in a separate artifact (184-05). Hand-author any extra shapes inline in
the test file.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/definitionOps.test.ts --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/definitionOps.test.ts` passes with 0 failures
    - `definitionOps.ts` exports exactly `renumber`, `addPhase`, `insertPhaseAt`, `movePhase`, `removePhase`, `patchPhaseConfig` after this task (the remaining exports land in Task 3)
    - `grep -nE 'from "(react|zustand|@/lib/api|@/components/workflows/canvasModel)"' frontend/src/components/workflows/definitionOps.ts` returns no matches
    - `grep -n 'function parseSkipTarget' frontend/src/components/workflows/definitionOps.ts` returns no matches (import it, never re-declare it)
    - The `renumber` comparator in `definitionOps.ts` is character-for-character the one quoted in `<interfaces>` from `canvasModel.ts:221-223`
    - The planted-gap positive control is present and, when its planted indices are changed to `[0,1,2]`, that control test FAILS — record the falsification in the SUMMARY
    - `node scripts/vitest-count-gate.cjs` exits 0 with **no per-file count decrease** and every one of the 16 baseline files still at its pinned count (definitionOps.test.ts is a net-new file, so the TOTAL rises)
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0
  </acceptance_criteria>
  <done>Six pure ops exist with R1's contiguity property proven over the full fixture corpus, a falsified positive control, and zero shipped assertions edited.</done>
</task>

<task type="auto">
  <name>Task 3: definitionOps — the two shape refusals, slug generation and the drag axis split</name>
  <files>frontend/src/components/workflows/definitionOps.ts, frontend/src/components/workflows/definitionOps.test.ts</files>
  <read_first>
    - frontend/src/components/workflows/definitionOps.ts (as written in Task 2)
    - frontend/src/components/workflows/phaseVocabulary.ts `:94-98` (`parseSkipTarget`) and `:108` (`PHASE_TYPE_SENTENCES`)
    - frontend/src/lib/api.ts `:3286-3293` — the `PublishOutcome` discriminated-outcome idiom the refusal return type follows
    - 184-RESEARCH.md § "Pattern 7: the two cheap refusals are pure predicates" (and assumption A4 — the real orphaning case is a dangling `skip_to_phase` target, not index arithmetic)
    - 184-RESEARCH.md § "Pattern 6 → The D-184-10 drag shape, concretely" (steps 1-4)
    - frontend/src/components/workflows/canvasModel.ts `:53-77` — `CANVAS_LAYOUT` (`NODE_WIDTH`, pitch constants `resolveDrop` reads)
  </read_first>
  <action>
Extend `definitionOps.ts` with the refusal predicates, the slug generator and the pure drag resolver.
Extend `definitionOps.test.ts` with their proofs.

**R10a — `canRemovePhase(phases, slug): RemovalOutcome`** where
`type RemovalOutcome = { ok: true } | { ok: false; reason: string }`, in the `PublishOutcome`
discriminated-outcome idiom (`api.ts:3286-3293`). Per RESEARCH assumption A4, ground the predicate on
`parseSkipTarget` (imported from `phaseVocabulary`), NOT on index arithmetic: after a delete the spine
renumbers, so index contiguity self-heals and the genuine orphan is a surviving
`validators[].on_failure` whose `skip_to_phase` target is the slug being deleted. Refuse with a
plain-language `reason` naming both steps by their plain title (`nodeTitle`), e.g.
`"\"Write a section\" sends failures to this step. Remove that fallback first."` — R10 says no refusal
is silent. Every other delete returns `{ ok: true }`.

**R10b — `allowedTypesAt(phases, index): Array<{ type: PhaseTypeId; disabledReason?: string }>`.**
Returns all six types in the fixed order `programmatic`, `llm_single`, `llm_agent`,
`llm_batch_agents`, `llm_human_input`, `llm_emit`. A choice is offered DISABLED with a
`disabledReason` when inserting it at `index` would strand the deliverable — i.e. when the definition
already carries an `llm_emit` phase and `index` is at or after that phase's render position, so the
deliverable would stop being terminal. The reason names the consequence in plain language, e.g.
`"This would come after the deliverable, so the workflow would no longer end with it."` Never omit a
type from the list — an option that vanishes teaches nothing (139-C's finding).

**Neither refusal may consult the server.** Both are decidable from `phases[]` alone. Do not import
`@/lib/api` and do not reference `workflows/validate` anywhere in this module — that is a shape rule,
not a verdict, and keeping them separate is what keeps D-182-06 intact.

**D-184-11 — `slugForType(phases, type): string` + `minimalPhaseFor(type, slug, index): PhaseSpecJSON`.**
`slugForType` derives a base token from the CLOSED 6-member type set only (never from user text — ASVS
V5: the auto-generated slug is this phase's one client-side input), and appends `-2`, `-3`, … until the
slug is unique against `phases`. `minimalPhaseFor` returns the smallest phase object that satisfies the
backend discriminated union for that type — `{ slug, phase_index: index, config: { phase_type: type } }`
plus only the fields the union REQUIRES for that member. Emit no key the union does not require:
`WorkflowDefinition` is `extra="forbid"` and a re-materialised default is exactly the class of drift
R2's round-trip property exists to catch.

**D-184-10 — `resolveDrop(origin, dropped, lanes): { reorderTo: number | null; dy: number }`.**
Pure, no DOM, no clock. `origin` and `dropped` are `{ x: number; y: number }`; `lanes` is the ordered
array of lane centre x-coordinates derived from `CANVAS_LAYOUT`. Compute
`dx = dropped.x - lanes[currentIndex]`; when `Math.abs(dx) >= pitch / 2` resolve the nearest lane index
and return it as `reorderTo`, otherwise return `reorderTo: null`. Always return
`dy = dropped.y - origin.y` verbatim. The two components are read independently, so a purely vertical
drag returns `reorderTo: null` **by construction** — that is D-184-10's "separation by construction",
and it must be asserted, not merely commented.

Tests to add:
- `canRemovePhase`: a delete that leaves a dangling `skip_to_phase` target is refused and the `reason`
  is a non-empty sentence naming the referring step; a delete on the 5-phase `eval_coverage` shape that
  breaks nothing returns `{ ok: true }`; deleting a slug that nothing references returns `{ ok: true }`.
- `allowedTypesAt`: returns exactly 6 entries in the fixed order for every index; on a definition with
  an `llm_emit` at position 2, indices `0` and `1` offer `llm_emit`… (recording whatever the rule
  yields) while indices `>= 2` mark the stranding choices disabled with a non-empty `disabledReason`;
  on a definition with no `llm_emit`, no entry is disabled.
- Neither refusal issues any network call: a `vi.spyOn(globalThis, "fetch")` records **0** calls across
  the whole suite, and a `?raw` source assertion (the `canvasModel.purity.test.ts:14-17` idiom) shows
  `definitionOps.ts` contains no `fetch(`, no `workflows/validate` and no `from "@/lib/api"`.
- `slugForType`: six distinct base slugs; a collision yields `-2`; a second collision yields `-3`; the
  result matches `/^[a-z0-9-]+$/`.
- `resolveDrop`: a purely vertical drag (`dropped.x === origin.x`) returns `reorderTo: null` with a
  non-zero `dy`; a horizontal drag past half a pitch returns the target lane index with `dy` 0; a
  horizontal drag under half a pitch returns `reorderTo: null`; a diagonal drag returns BOTH.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/definitionOps.test.ts --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `definitionOps.ts` additionally exports `canRemovePhase`, `allowedTypesAt`, `slugForType`, `minimalPhaseFor`, `resolveDrop` and the `RemovalOutcome` type
    - `grep -nE 'fetch\(|workflows/validate|from "@/lib/api"' frontend/src/components/workflows/definitionOps.ts` returns no matches
    - The suite's `fetch` spy reports exactly 0 calls
    - `allowedTypesAt` returns 6 entries for every tested index — no type is ever omitted from the array
    - A refused delete's `reason` is a non-empty string; the test asserts `reason.length > 0` and that it contains the referring step's plain title
    - `resolveDrop({x:100,y:0},{x:100,y:64},lanes).reorderTo === null` and `.dy === 64`
    - `slugForType` output matches `/^[a-z0-9-]+$/` for all six types and for two collision rounds
    - `node scripts/vitest-count-gate.cjs` exits 0 with no per-file count decrease
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0
  </acceptance_criteria>
  <done>Both cheap refusals, slug generation and the drag axis split are pure, network-free, and proven — R10a, R10b, D-184-10 and D-184-11 all land as plain unit tests with no canvas and no store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-supplied definition JSONB → client module | `phases[]` is author-supplied content read by a pure projection; it may carry unknown `phase_type` values, malformed `on_failure` strings and duplicate slugs |
| client-generated slug → definition JSONB | `slugForType` is this phase's ONLY client-side input into the definition (ASVS V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-02-01 | Denial of service | `definitionOps` resolvers | mitigate | TOTALITY contract: every exported resolver is total — an unknown `phase_type`, a missing `validators` array and a malformed `on_failure` resolve honestly and never throw. Asserted in the test suite, not just documented |
| T-184-02-02 | Tampering | `slugForType` → definition JSONB | mitigate | The slug is derived from the CLOSED 6-member phase-type set plus a numeric suffix, never from user text; output is asserted against `/^[a-z0-9-]+$/`. `minimalPhaseFor` emits only union-required keys, so `extra="forbid"` cannot be tripped by a re-materialised default |
| T-184-02-03 | Repudiation | the two refusals | mitigate | Both are pure shape predicates that never consult `/workflows/validate` (asserted by a 0-call `fetch` spy and a `?raw` source guard), so a client refusal can never be mistaken for — or drift from — a server verdict (D-182-06) |
| T-184-02-SC | Tampering | npm installs | accept | This plan installs nothing. `zundo` is installed in 184-04 under its `[OK]` legitimacy verdict |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/definitionOps.test.ts` — 0 failures
- `node scripts/vitest-count-gate.cjs` — exit 0, no per-file decrease, all 16 baseline files present
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `git diff --exit-code -- 'frontend/src/**/__snapshots__/*'` exits 0
- `git diff --name-only -- supabase/migrations | wc -l` returns 0 (R3: this phase adds no migration; slot 114 stays RESERVED)
- **Zero assertion edits** in any pre-existing test file (D-184-08); `git diff --stat` for this plan must list only the three files in `files_modified`
</verification>

<success_criteria>
- One pure module owns every mutation of `phases[]`, with no React, no store, no API-client and no canvas import
- R1 is proven as a plain unit test over the full fixture corpus with a falsified planted-gap control
- R10a and R10b are pure predicates that provably issue zero network calls
- D-184-10's axis separation is asserted, not commented
- The D-184-08 count gate is committed, runnable and falsified
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-02-SUMMARY.md` when done.
It MUST include: (a) the count-gate falsification observation, (b) the planted-gap control falsification
observation, and (c) an explicit statement that **zero** assertions in pre-existing test files were edited.
</output>
