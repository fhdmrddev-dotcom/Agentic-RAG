---
status: testing
phase: 184-editable-canvas-live-structural-validation-round-trip
source: [184-01-SUMMARY.md, 184-02-SUMMARY.md, 184-03-SUMMARY.md, 184-04-SUMMARY.md, 184-05-SUMMARY.md, 184-06-SUMMARY.md, 184-07-SUMMARY.md, 184-08-SUMMARY.md, 184-09-SUMMARY.md, 184-10-SUMMARY.md, 184-11-SUMMARY.md, 184-12-SUMMARY.md, 184-13-SUMMARY.md, 184.1-01-SUMMARY.md]
started: 2026-07-28T14:05:04Z
updated: 2026-07-28T14:32:00Z
---

## Prior Coverage (not re-tested)

The 2026-07-28 operator run recorded in `184-UAT-RESULTS.md` already passed, live, with the
canvas flag ON: add-step menu open/dismiss, adding steps via `＋`, sideways drag reorder (no
flicker), vertical-only drag (cosmetic, undo stays greyed), delete-a-middle-step + Undo, the
narrowed-window header, and U-1's empty-draft invitation. Six defects found there are fixed and
confirmed. Those rows are NOT repeated below. Everything below is what that run did not reach.

## Current Test

number: 12
name: Flag Off = Yesterday's Builder — NORMAL-USER audience only (operator half done + fixed)
expected: |
  On a NON-OPERATOR account with visual_workflow_canvas OFF: no canvas, no
  Spine/Canvas toggle, no ＋/✕, no problems tray, no undo toolbar. Then flag ON:
  confirm whatever the audience map intends for a normal user.
  Needs a second, non-operator login (fhdmrd.automation@ or fhdmrd.dev@ exist in
  the roster). The operator half is verified and the underlying kill-switch bug
  is fixed (commit 1c94ffd4).
awaiting: nothing — Test 8 complete. Test 12's normal-user audience is the only open item.

## Tests

### 1. Undo and Redo
expected: Three structural edits step back one-at-a-time under Ctrl+Z and forward under Ctrl+Shift+Z / Ctrl+Y; toolbar Undo/Redo match and grey out at the stack ends; a vertical-only nudge creates no undo entry
result: pass

### 2. Keyboard Reorder
expected: Select a step card, then press Alt+Left / Alt+Right — the card moves one position each press, the other cards re-stitch and renumber, and a screen-reader-polite message announces the new position. Undo lights up (this IS a structural edit)
result: pass

### 3. Configure a Step in the Side Panel
expected: Clicking a card on the canvas opens the SAME side panel the phase-spine view uses (not a second, different form). The fields shown match the step's type. Edit a field, close the panel, reopen it — the edit is still there. Ctrl+Z after typing restores the whole typed sentence at once, not one character at a time
result: pass

### 4. Governance Rails in the Side Panel
expected: The panel's tool options come from the server, not a free-text box — you can pick from a list, and there is no place to type an arbitrary tool name. A tool the workflow names but the server does not recognise renders struck through. Locked validation gates show a 🔒 with no remove/X control anywhere on that row. If the server's tool list fails to load, the panel says it could not load them rather than showing an empty-but-normal picker
result: pass
verified_by: claude (live, Chrome MCP, local stack, canvas flag ON — operator delegated this row)
evidence: |
  - App issued GET /workflows/grounding-bundle → 200; server returned 28 tools and the
    panel rendered exactly those 28 chips. Zero input/textarea inside the tool group.
    Helper copy: "Pick from the tools this workspace allows — you cannot add one by typing."
  - Server answer changed ⇒ options changed: intercepted the bundle to return only 3 tools;
    the panel re-rendered with exactly those 3. Not a hardcoded list.
  - Unregistered tool: with search_documents removed from the registry, its chip rendered
    with computed text-decoration: line-through and title
    "search_documents — not in this workspace's tool registry".
  - Locked gate: <li data-testid="gate-row" data-locked="true"> → "🔒 Must cite its sources ·
    Cannot be removed", with 0 buttons inside the row (no removal control in the DOM).
  - Degraded: forced degraded:["tools"] → "We couldn't load the tools this step is allowed to
    use. Nothing is offered here rather than a list that would be wrong." plus an honest
    "This step currently names: Search documents." No empty-but-normal picker.
  - Incidental: an unauthenticated call to /workflows/grounding-bundle returned 404 (not 401),
    confirming the D-181-01 pre-auth 404.
  - No edits were saved; the fetch interception was reverted and the page reloaded.

### 5. Live Validation Marks and the Problems Tray
expected: As you edit, the canvas checks with the server (a brief "checking" beat). Steps with server-reported problems get a mark on the card's right edge; the problems tray at the bottom lists them with plain-language wording and a way to jump to the step. Problems that belong to no particular step still appear in the tray. Marks change only when the server's answer changes
result: pass
verified_by: claude (live, Chrome MCP, local stack, canvas flag ON — operator delegated this row)
evidence: |
  - Checking beat observed: tray footer "checked by the server" → "checking…" → "checked by
    the server" after an insert. One debounced POST /workflows/validate → 200.
  - R8 proven by holding local state CONSTANT: inserted a step then undid it (definition
    byte-identical to before), with /validate intercepted to return a synthetic verdict set.
    Marks and tray changed anyway — they are server-derived, not a client guess.
  - Node marks: data-testid="canvas-node-verdict" data-verdict="error", "✕ Has a problem",
    on exactly the phases the server named.
  - Two-count wording before expansion: "3 problems · 1 thing to finish".
  - Expanded rows are plain-language and jumpable: clicking "Produce the deliverable —
    Nothing leads to the deliverable step." selected Phase 3 and opened "Refine step: emit".
  - phase: null verdict rendered under a dedicated "THE WORKFLOW" heading — not dropped.
  - An unrecognised code (some_code_the_client_has_never_seen) rendered as an error rather
    than being silently discarded.
  - A verdict naming a slug with no matching node still rendered (raw slug shown) and still
    counted — nothing swallowed.
  - Bonus R10b: "Add a step at the end" offered all six types DISABLED with the stated reason
    "This would come after the deliverable, so the workflow would no longer end with…".
  - Nothing saved: after leaving, the draft reloaded with its original 2 phases, tray clean.

### 6. Mid-Build Never Reads as Failure (U-3)
expected: Reach a half-built state — several steps unfinished, no hard errors. Confirm: unfinished steps show a dashed grey ○ mark, there is NO red anywhere on the canvas or the tray, and the tray summary reads as two counts ("N problems · N things to finish") before you expand it. The screen should read as work-in-progress, not as a broken app
result: pass
verified_by: claude (live, Chrome MCP, local stack, canvas flag ON — operator delegated this row)
evidence: |
  Forced a 3-incomplete / 0-error verdict set through the /validate seam.
  - Marks: both nodes carry data-verdict="incomplete", glyph "○ Not finished yet",
    computed border-style: dashed, colour rgb(151,161,180) on rgb(20,25,36).
  - Colour scan (the mechanical half of R9): ZERO elements in the canvas region carry the
    `destructive` class token, and ZERO computed colour/background/border values across every
    element in the region are reddish. Not an eyeball — a scan.
  - Tray summary before expansion: "3 things to finish". NOTE the implementation ELIDES the
    zero half rather than printing "0 problems · 3 things to finish". This is better than the
    row's literal wording and is recorded as intended, not as a miss.
  - Aesthetic half (the operator-judgement part): screenshot shows quiet grey marks, a neutral
    "Not saved yet", no alarm colour anywhere. Reads as work-in-progress.

### 13. Canvas Notice Honesty After Keyboard / Toolbar Undo
expected: After undoing an add or a delete, the canvas must not keep claiming the act happened
result: issue
reported: "Delete a step → the canvas says 'Removed X · 1 step renumbered'. Press Ctrl+Z or the toolbar Undo → the step comes back, but the message stays on screen indefinitely, still saying it was removed. Same for 'Added …'."
severity: minor
found_by: claude (incidental, while verifying Test 6)
evidence: |
  Live, both paths: toolbar Undo after an insert, and Ctrl+Z after a delete. In both cases the
  definition was correctly restored while the notice persisted, still visible 7+ seconds later,
  with no timeout — it is only replaced by the next canvas act.
  Cause is visible in the source: WorkflowBuilderPage.tsx:887-893 calls setCanvasNotice(null)
  ONLY inside the notice's own inline Undo handler (onUndo). The keyboard binding and the
  toolbar Undo button both step the temporal store without clearing the notice.

### 7. Publish Blocked, With the Reason Named
expected: While the workflow is structurally invalid, the Publish control is disabled AND the reason is readable right there — the first problem's message appears on or next to the disabled button, not hidden in a tooltip-only or a console. Fix the problem and Publish becomes available again
result: pass
verified_by: claude (live, Chrome MCP, local stack, canvas flag ON — operator delegated this row)
evidence: |
  - Server returns 1 error + 1 incomplete → Publish button disabled: true, with
    aria-describedby resolving to "Nothing leads to the deliverable step." — the FIRST
    verdict's message (R12's literal claim).
  - The reason is REAL adjacent text, not tooltip-only: the header renders
    "◆ Publish…Nothing leads to the deliverable step." (title attribute is null).
  - Server returns ok:true with no verdicts → Publish disabled: false, aria-describedby gone,
    reason text gone, tray back to "Nothing to fix — the static checks pass".
  - Both transitions were driven by the server response alone, with the local definition
    returned to the same state (insert-then-undo round trip).

### 8. One Bottom Region at ~900px (R12 + the 184.1 header)
expected: Narrow the browser window to about 900px wide on the Builder. Confirm: exactly ONE region along the bottom edge (toolbar + problems tray composed together, not two stacked bars), no horizontal scrollbar anywhere on the page, and the header above the canvas is a single row — the canvas gets meaningfully more vertical space than it did before
result: pass
verified_by: claude (live, Chrome MCP, operator un-maximized the window so the viewport could actually be narrowed)
evidence: |
  Measured at an EXACT 900px viewport (window.innerWidth === 900), Builder → Canvas:
  - ONE bottom region: querySelectorAll('[aria-label="Canvas controls and problems"]')
    length 1 — 76px tall, 646px wide, top at y=560. Toolbar + tray composed, not stacked.
  - NO horizontal overflow: documentElement 0, body 0, main 0, AND a sweep of every element
    under <main> found ZERO whose right edge passes the viewport (overflowCount 0).
  - ONE header element (85px at this width — it wraps to two visual lines but remains a
    single band, which is what the 184.1 budget pins: no NET-NEW band).
  - Screenshot confirms the composition reads correctly at this width.

  THE 184.1 HEADER RECLAIM — now measured, not inferred (184-UAT-RESULTS.md recorded this
  as "Not measured": jsdom reports zero heights and Chrome MCP had wedged). The consolidation
  is flag-gated, so flag-off renders the OLD three-band header — a true A/B at one width:
    | at 900px                          | flag OFF (old) | flag ON (merged) |
    | header chrome, bottom edge        | y=146          | y=85             |
    | workflow content top, SPINE view  | y=255          | y=242            |
    | graph surface top, CANVAS view    | —              | y=176            |
  So: 61px less header chrome, and the canvas graph starts 79px higher than the flag-off
  content did — a real, meaningful gain.
  CORRECTION TO THE CLAIMED FIGURE: the "~90px reclaim" is NOT reproduced at 900px; the
  measured header reduction here is 61px. Note also that on the SPINE view the NET gain is
  only 13px, because the flag-on surface reinvests most of the saving in the new
  Spine/Canvas tablist. The original ~90px came from an operator observation at a 639px
  width, where the three bands wrap into more lines — both numbers can be true at their own
  widths. The direction and the "looks much better" verdict hold; the specific number is
  width-dependent and should not be quoted as a flat 90px.

### 9. Unsaved-Work Leave Guard
expected: Make an edit without saving, then try to leave — click the Workflows breadcrumb, and separately try a browser reload/close. Both warn that there is unsaved work rather than silently discarding it. Dismissing the warning and staying keeps every edit intact
result: pass
verified_by: claude (live, Chrome MCP, local stack, canvas flag ON — operator delegated this row)
evidence: |
  - beforeunload: a synthetic cancelable beforeunload event was defaultPrevented while the
    draft was dirty — the guard is armed (it is mounted only while dirty by design).
  - Breadcrumb: clicking "← Workflows" with unsaved work asked
    "This draft has unsaved changes. Leave without saving?".
  - The STAY path: declining the prompt kept us in the Builder with the phase list
    byte-identical before and after (3 phases, same order, same slugs) — nothing discarded.
  - window.confirm was stubbed for the duration so the native modal could not wedge the
    browser session; the stub recorded the exact prompt text and was restored afterwards.

### 10. Save, Hard Reload, Nothing Lost or Invented (U-4)
expected: Author at least 4 steps including some side-panel config edits, and drag one card vertically (a cosmetic nudge). Save. Then HARD reload the page (Ctrl+Shift+R). Every step, its order, and every config edit come back exactly as saved — nothing missing, nothing added that you did not author. The cosmetic vertical nudge is also restored (it lives in the browser, not the saved workflow)
result: pass
verified_by: claude (live, Chrome MCP, local stack, canvas flag ON — operator delegated this row)
evidence: |
  Ran on the throwaway duplicate draft compliance-gap-report-j74n9e
  (id f32f4384-42ba-4bb4-bd05-ee85ad927ddf), one of 54 identical test drafts.
  - Authored: inserted an llm_human_input step at index 1, typed a real config edit into its
    Instructions, and did a REAL pointer drag straight down on the card.
  - The vertical drag was cosmetic-only: step order unchanged, and it wrote
    localStorage["agentic-rag.canvas-nudge.v1.<userId>.<draftId>"] = {"ask":57.007…}
    (user-scoped AND draft-scoped, exactly as designed).
  - R6: saving issued EXACTLY ONE request — PATCH /workflows/{id} to the existing draft CRUD.
    No new endpoint, no second write.
  - R3: the PATCH body contained NO position / layout / x / y / dy key.
  - HARD reload (Ctrl+Shift+R), reopened the draft:
      * 3 phases with contiguous phase_index 0,1,2 — the insert renumbered everything
        downstream; nothing missing, nothing invented.
      * the typed config edit came back verbatim.
      * the nudge was RESTORED from localStorage — the ask card rendered at top 198 vs its
        lane peers at 141, i.e. the stored dy of 57.
  - Server-side confirmation via GET /workflows/drafts: the stored definition JSONB carries
    no position/layout/dy key anywhere.
  - CLEANUP: the draft was restored to its original 2-phase shape and saved, and the nudge
    localStorage key was removed. Verified persisted phases are back to [0 retrieve, 1 emit].

### 11. Step Icons Across All Five Surfaces
expected: The two swapped step glyphs — 🧭 compass for the AI-step type and 🤝 handshake for the parallel-agents type — render correctly and legibly everywhere they appear: the canvas cards, the phase-spine view, the add-step (`＋`) picker menu, the side panel, and the workflows list. No missing/blank glyph, no leftover robot or silhouette icon anywhere
result: pass
verified_by: claude (live, Chrome MCP, local stack — operator delegated this row; closes the D-184-07 five-surface sweep that 184-01 deferred to phase verification)
evidence: |
  Method: fingerprinted each of the six bundled fluent-emoji icons straight from the INSTALLED
  @iconify-json/fluent-emoji package (path count + first path `d` prefix — all six unique),
  then matched every rendered inline SVG in each surface against those fingerprints. Identity,
  not eyeballing.
    compass paths=26 · handshake paths=35 · memo 17 · gear 12 · raised-hand 30 · package 11
  Surfaces:
    - Canvas cards: llm_agent → compass (exact match), llm_emit → package (exact match).
    - Phase-spine view: compass + package, exact matches (rendered by both PhaseSpine and
      PhaseSpineGraph).
    - The ＋ StepTypePicker: ALL SIX rows correct — gear / memo / COMPASS / HANDSHAKE /
      raised-hand / package against their plain-language labels.
    - Workflows list: 150 phase glyphs across every card, ZERO unrecognised — 76 compass,
      50 package, 18 memo, 4 raised-hand, 1 handshake, 1 gear.
  No missing or blank glyph anywhere; the retired robot/silhouette slugs cannot render at all
  (their imports are gone, and unplugin-icons resolves at build time).
  CORRECTION to this row's premise: the side panel is NOT a glyph surface and never was — it
  names the type in words ("AI agent step"). The glyph consumers are PhaseNode/nodePresentation,
  PhaseSpine, PhaseSpineGraph, phaseVocabulary (the picker), soulData (the list card) and
  PhaseCard (the run panel, outside 184's authoring surface). Not a defect.

### 12. Flag Off = Yesterday's Builder (U-5, D-14)
expected: Flip `visual_workflow_canvas` OFF in /admin, then reload the Builder. On a NORMAL user account AND on an OPERATOR account: no canvas, no canvas/spine toggle, no `＋` or `✕` affordance, no problems tray, no undo toolbar — the Builder looks and behaves exactly as it did before this phase. Then flip it back on and confirm the canvas returns
result: issue — FIXED AND RE-VERIFIED (operator half); normal-user half still outstanding
reported: "The operator kill-switch does not reliably take effect. The Control Room writes audience=off (PUT /admin/visibility → 204, confirmed in the DB), the radio shows Off — but GET /features keeps returning visual_workflow_canvas: true for minutes afterwards, and after a HARD reload the Builder still renders the Spine/Canvas tablist. Flag-off is therefore NOT byte-identical to yesterday's Builder."
severity: major
fix: |
  Fixed in this session at the operator's request, test-first.
  - `refresh_settings_cache()` added (user_settings.py) — expires the timestamp, re-reads,
    then re-expires. Called by `set_feature_visibility` and `save_app_settings`.
  - STRICTLY ADDITIVE to D-07: the trailing re-expire keeps "after a write the cache is
    expired and the next read hits the DB" literally true, so
    test_147_flag_failure_semantics::test_write_invalidates_cache is untouched. (A first
    attempt that left the timestamp warm DID break that assertion — the pinned test caught
    it, and the fix was reshaped rather than the assertion edited.)
  - NOT fixed by `_settings_cache = None`: the sync reader would then fall back to
    env/Pydantic DEFAULTS for every column until re-warmed — a far worse transient.
  - Cross-process staleness under WORKER_COUNT=2 is explicitly NOT solved (documented in
    the new docstring); this makes the WRITING worker correct immediately.
  Tests: `backend/tests/test_184_settings_rewarm.py` — 4 tests, written FIRST and
  falsified (3 failed on the pre-fix code with exactly the live symptom: "everyone"
  instead of "off", "model-before" instead of "model-after"; the 4th guards against a
  fix that lets a re-warm failure fail the write).
  Full backend suite differential: baseline 200 failed / 3101 passed →
  200 failed / 3105 passed. +4 = exactly the new tests. Zero regressions.
  LIVE RE-VERIFICATION (the same scenario that produced the defect):
    - flip Off → GET /features false 5/5 immediately, with NOTHING in between;
      flip On → true 5/5. Before the fix this required an unrelated async request.
    - flag OFF + a single plain reload → operator Builder is byte-clean: no Spine/Canvas
      tablist, no canvas region, 0 react-flow nodes, no ＋/✕, no undo toolbar, no problems
      tray, no editing copy — and the spine still renders (pre-184 Builder intact).
    - flag ON + reload → the Spine/Canvas tabs return.
  Flag left at audience=everyone, confirmed in the DB.
verified_by: claude (live, Chrome MCP + direct psycopg2, operator account, operator-authorised flag flip)
evidence: |
  Sequence, all measured:
  1. Flipped "Visual workflow canvas" to Off in Control Room → Users & Access → Feature
     visibility. The write is a direct flip with no confirm sheet (consistent with the
     graded action-guards rule — a reversible visibility change, not a destructive act).
  2. Observed the request: PUT /admin/visibility {"feature":"visual_workflow_canvas",
     "audience":"off"} → 204.
  3. Direct DB read (psycopg2 :54322): app_settings.feature_visibility.visual_workflow_canvas
     = {"audience":"off"}, updated_at 15:09:19Z. Exactly one app_settings row ('global').
  4. GET /features (operator, cache:'no-store'): visual_workflow_canvas TRUE on 30+
     consecutive reads spanning >5 minutes, including after a 40s wait.
  5. Hard reload (Ctrl+Shift+R) of the Builder: the Spine/Canvas tablist is STILL rendered.
  6. Flipping back to On restored audience=everyone; the canvas surface behaves normally.
  ONE EARLIER OBSERVATION WENT THE OTHER WAY: immediately after the first flip to Off, a
  hard reload DID show a clean flag-off Builder (no tablist, no canvas region, no editing
  words, spine intact). So the off state IS reachable — it is not reliably reached.
  ROOT CAUSE NOT ESTABLISHED — do not treat the following as diagnosed:
    - backend/app/api/features.py deliberately makes the "off" guard beat the operator
      short-circuit (D-181-01), so the endpoint's LOGIC is right; the audience VALUE it
      reads is what is stale.
    - user_settings.py:1096 `_feature_record` → `load_app_settings()` (SYNC) returns
      `_settings_cache` with NO TTL check; the TTL lives only in the async
      `_load_settings_from_db()`. `set_feature_visibility` calls `invalidate_settings_cache()`
      in the writing worker only (admin.py:1263 already notes the per-worker-cache hazard
      under WORKER_COUNT=2).
    - BUT a cold/invalidated cache should fall back to _GOVERNED_FEATURES's hardcoded
      "off" for this feature, which would read FALSE, not TRUE. That contradiction is
      unexplained and needs a real debug session.
  SCOPE: this is the Phase 181 / 148 revert + visibility surface that 184 INHERITS
  (D-181-01 / D-14), not 184's own canvas code. It nonetheless breaks 184's stated revert
  guarantee, so it should not be waved through.
  OUTSTANDING: the NORMAL-USER audience half of this row was not run — the operator has no
  second account in this session. Two non-operator accounts exist in the roster
  (fhdmrd.automation@, fhdmrd.dev@) and the operator has said they will provide one.

## Summary

total: 13
passed: 11
issues: 2
pending: 1
skipped: 0
blocked: 0

<!-- Test 12 is counted under `issues` (its defect is FIXED and the operator half re-verified);
     its NORMAL-USER audience is the one outstanding item, tracked as `pending`. -->
updated_note: 2026-07-28 — tests 4,5,6,7,8,9,10,11 verified live by claude at the operator's
request; the Test-12 kill-switch defect was root-caused and fixed test-first (commit 1c94ffd4).

## Gaps

- truth: "After undoing an add or a delete, the canvas stops claiming the act happened"
  status: failed
  reason: "Claude reported: delete a step → 'Removed X · 1 step renumbered'; press Ctrl+Z or the toolbar Undo → the step returns but the message stays on screen indefinitely, still saying it was removed. Same for 'Added …'."
  severity: minor
  test: 13
  root_cause: "WorkflowBuilderPage.tsx:887-893 — setCanvasNotice(null) runs only inside the notice's own inline Undo handler (onUndo). The D-184-04 keyboard bindings and the CanvasToolbar Undo/Redo buttons step the temporal store without clearing canvasNotice, and the notice has no timeout."
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      issue: "canvasNotice is cleared on only one of the three undo paths"
  missing:
    - "Clear (or invalidate) canvasNotice whenever the temporal store steps, not only via the inline Undo handler"
  debug_session: ""

- truth: "Flipping visual_workflow_canvas OFF makes the Builder byte-identical to yesterday's, for every audience including operators (D-181-01 / D-14)"
  status: failed
  reason: "Claude reported: PUT /admin/visibility set audience=off (204, confirmed in the DB), the Control Room shows Off, but GET /features kept returning visual_workflow_canvas: true for 30+ reads over >5 minutes and the Builder still rendered the Spine/Canvas tablist after a hard reload."
  severity: major
  test: 12
  root_cause: "ESTABLISHED AND PROVEN LIVE. `invalidate_settings_cache()` (user_settings.py:301-304) zeroes ONLY `_settings_cache_time`; it never clears `_settings_cache`. The ASYNC reader `_load_settings_from_db()` (:272-298) checks that timestamp, so it correctly refetches. The SYNC reader `load_app_settings()` (:828-835) reads `_settings_cache` DIRECTLY with NO timestamp check, so it keeps serving the pre-flip row indefinitely. `GET /features` → `feature_audience()` → `_feature_record()` → `load_app_settings()` is the SYNC path, so invalidation is a no-op for it: the flip is written to the DB and then never observed."
  artifacts:
    - path: "backend/app/models/user_settings.py:301"
      issue: "invalidate_settings_cache() zeroes the timestamp but leaves _settings_cache populated"
    - path: "backend/app/models/user_settings.py:828"
      issue: "sync load_app_settings() reads _settings_cache with no TTL/staleness check — the invalidation contract does not reach it"
    - path: "backend/app/models/user_settings.py:1096"
      issue: "_feature_record (and thus feature_audience + resolve_feature_access) resolves through that sync path"
  missing:
    - "DONE — refresh_settings_cache() added (expire → re-read → re-expire) and wired into set_feature_visibility + save_app_settings; 4 falsified tests; live re-verification passed"
    - "DONE (avoided) — NOT fixed by setting _settings_cache = None; the sync reader would fall back to env/Pydantic DEFAULTS for every column until re-warmed"
    - "STILL OPEN — re-run the flag-off row on a NORMAL-USER account (needs a second, non-operator login)"
    - "STILL OPEN (separate concern, deliberately NOT fixed here) — cross-worker staleness under the WORKER_COUNT=2 production default: a sibling worker keeps its own cache until its own 30s TTL lapses"
  debug_session: "inline (this UAT session) — see the falsification below"
  falsification: |
    Proven live, no code change and no backend restart:
      1. Set audience=off (PUT 204; DB confirms off).
      2. GET /features → visual_workflow_canvas TRUE, 5/5.
      3. ONE GET /me/preferences — chosen ONLY because it awaits load_app_settings_async()
         (me_preferences.py:57), which re-warms _settings_cache.
      4. GET /features → FALSE, 5/5.
    A single unrelated request flipped the answer, which is exactly what a stale sync cache
    predicts and what a DB, browser-cache or multi-worker explanation does not.
  hypotheses_ruled_out: |
    - Multi-worker cache divergence: RULED OUT. The dev backend runs uvicorn --reload with a
      SINGLE app worker (PID 40288); process-tree inspected. (The hazard is real under the
      WORKER_COUNT=2 production default and admin.py:1263 already notes it — but it is not
      what was observed here.)
    - Browser/HTTP caching: RULED OUT. GET /features carries no Cache-Control, and every
      probe used cache:'no-store'.
    - The write not landing: RULED OUT. PUT /admin/visibility → 204 and a direct psycopg2
      read of app_settings.feature_visibility showed audience=off with a fresh updated_at.
    - The 30s TTL simply not having elapsed: RULED OUT. Stale reads persisted past 40s and
      across >5 minutes.
  blast_radius: |
    Wider than this one flag. The sync load_app_settings() is also read by tool_dispatcher.py,
    documents.py, extraction_service.py and admin.py, and save_app_settings() uses the same
    invalidate-only contract — so any setting written through the Settings/admin UI can be
    stale to a sync reader until an unrelated async read happens to re-warm the cache.
    Note also that `require_visible` (the API write-side gate) resolves through the SAME
    feature_audience seam, so the UI hide and the API refusal stay consistent with each
    other — but both stay consistently WRONG while the cache is stale.
