---
phase: 245-the-verification-debt-discharged-or-retired-in-writing
plan: 02
type: execute
wave: 2
depends_on: ["245-01"]
files_modified:
  - .planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-UAT-RESULTS.md
  - .planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md
autonomous: false
requirements: [DEBT-02, DEBT-01]

must_haves:
  truths:
    - "SC#2 — each of Phase 233's five G-4 operator rows has been driven in a live browser and carries its own written verdict, in the order row 2 demands: nothing is imported until the four-table unchanged-check is measured and written."
    - "Row 2 is proved against the DATABASE, not the screen — `documents`, `document_chunks`, `folders` and `ingestion_jobs` counted immediately before the preview and immediately after closing it without confirming, with the before/after pairs written into the verdict."
    - "SC#1's M-9 arm — a `path contains '/Finance/'` rule created through the real surface (RuleBuilderPanel), then confirmed firing through BOTH doors: the watch loop AND the manual import door, which is the arm Defect 3 lived in. ⛔ Its verdict is pass, or ⛔ BLOCKED with the failing door, a defect id and a named trigger — NEVER `HALF`, which is not one of SC#1's three admissible verdicts."
    - "The fixture is provably the app's own: the folder id the MCP returned EQUALS the folder id the app reports, both quoted in §Setup, before any row was driven — so an invisible or wrong-account folder can never be mistaken for a broken preview."
    - "D-14 — the verdict states exactly what the M-9 click covered of Phase 237's rule builder (a `path contains` rule through the real surface) and exactly what it did NOT (the scope selector, out-of-scope condition filtering on scope switch), with a named trigger for the rest."
    - "Every row states which requirement it discharges — DEBT-01 or DEBT-02 — because the filename no longer says."
  artifacts:
    - path: ".planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-UAT-RESULTS.md"
      provides: "the driven verdicts — 233's five rows + 238's M-9 + the 237 coverage record, in 243's frontmatter shape"
      contains: "rows_driven"
  key_links:
    - from: "the throwaway Drive fixture folder"
      to: "the app's own Google connection (service_id=google, status=active, org 22f9c615-0eec-440a-8804-ed4784d6f57f)"
      via: "browse through the product's connected-source picker BEFORE driving any row"
      pattern: "identity proof by ID EQUALITY — MCP-returned folder id == app-reported folder id, both quoted; a name match is not attribution"
    - from: ".planning/phases/245-.../245-UAT-RESULTS.md"
      to: ".planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md M-9"
      via: "the M-9 row's verdict is updated in place and points at the results file"
      pattern: "M-9"
---

<objective>
**DEBT-02 in full (SC#2) and DEBT-01's M-9 arm (SC#1).** Drive Phase 233's five G-4 operator rows in
a live browser, and drive Phase 238's M-9 end-to-end through both doors — the only rows in this phase
that need a person at a screen.

⭐ **This is the phase's ONLY substantial drive, and it is the phase's whole argument.** 233's own
verification says it: *"Every criterion above is proved at the unit grain and at the wire grain; none
of them has been watched happening."* And the record of what happens when someone does watch:
**one real Drive folder import on 2026-09-05 surfaced SIX defects, and 5,600 automated tests had
found none of them** (`233-UAT-FINDINGS.md`) — because the connector import door was not the door
`/upload` uses, and every difference between them was invisible on screen. 238's rows found four
more, **none caught by its 15-case unit suite.**

⛔⛔ **NO ROW IN THIS PLAN MAY BE SCORED BY A TEST RUN.** The ROADMAP's first named failure mode for
this phase is *"a row is marked done on the strength of a passing test suite rather than a driven
observation."* There is no `<automated>` command in this plan that scores a row; the automated
commands here only **capture measurements** that a human verdict is then written against.

Purpose: five rows owed since 233 shipped, plus the one 238 row whose end-to-end half was never
closed. Output: `245-UAT-RESULTS.md` with seven written verdicts, and 238's M-9 row moved off
`⚠ HALF PASS`.

⛔ **D-16 — THE DEFECT LINE, AND IT IS HARD.** Driving has a strong defect-finding record here, so
expect findings. A defect is fast-fixed **only** inside G-3's existing line: **≤ 1 file, ≤ 10 lines,
no schema, no API surface.** Anything larger is **filed as a reported-bug and deferred to a named
phase** — *filing is a complete response*, and this phase has no plan budget for fixes. ⛔ The "any
size if one re-run proves it" option was rejected for having no size bound.

⚠ **Five open reported-bugs sit on the surfaces these drives pass through** and were REVIEWED and
explicitly NOT folded: `BUG-260909-03` (a watch card reads its last run, not its connection),
`BUG-260909-04` (Sync now collapses the section), `BUG-260909-05` (`missing_since` never set),
`BUG-260909-06` (a fix button labelled as a write only navigates), `BUG-260909-07` ("last read
successfully" on a relative time). **If one is OBSERVED while driving, record the observation in
`245-UAT-RESULTS.md` against its existing report id** — that is evidence the report is still live. ⛔
**It is NOT a licence to fix it here.**

**Solo run (`OV-SOLO-01`):** Claude drives; Gemini is unavailable. 243 established that self-driving
is sound **when attribution is verifiable** — and here it is: ONE connection, not eight providers.
⚠ The counter-example is on the record: 243's cross-provider board was **abandoned** because a click
for `anthropic` selected `minimax`, and *a scoreboard with unverified attribution is worse than none*.
**Task 1's identity proof is the equivalent of that check for this plan.**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then `git rev-parse HEAD` — MUST
be a descendant of `245-01`'s merge on `develop`. ⚠ A fresh worktree starts on the DEFAULT branch
(`master`, stuck at the v3.9 ship commit). Assert the SHA first.
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

⛔⛔ **THIS PLAN RUNS ALONE IN ITS WAVE, AND THAT IS A HARD RULE, NOT A PREFERENCE.** Worktree rule 4:
*worktrees isolate files, not Postgres.* This plan **writes real rows** to the local Supabase —
documents, chunks, folders, ingestion_jobs and a `classification_rules` row. **No plan or suite that
mutates the local DB may run concurrently**, and no `files_modified` check can see the conflict.
⛔ **Do not run `backend/tests` at all in this plan** — not the ceiling check, not a single module.
Its own fixtures would corrupt the before/after pairs row 2 is scored on.

⛔ **THE SUPABASE MCP POINTS AT PRODUCTION.** Every DB read in this plan is against the **LOCAL**
Postgres on `127.0.0.1:54322`. ⛔ Do not use `mcp__supabase__*` for anything in this plan — a count
from the wrong database would score row 2 against a corpus this fixture never touched.

**Infra — measured live at planning time by the orchestrator, so do not re-provision blindly:**
all five ports already accept real TCP connections — `54322` (Postgres) · `6379` (Redis) · `54321`
(Supabase API) · `8000` (backend) · `5173` (vite). If any is dead:
- `powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1` — idempotent; it **verifies
  54322 / 6379 / 54321 accept a real TCP connection**, because *"container running" is not the property
  the backend needs*.
- ⚠ **The operator starts the backend.** If `:8000` is dead, that is a checkpoint, not a task.
- ⚠ A **stale uvicorn** can hold `:8000` with killed-supervisor workers still listening — check
  `Get-NetTCPConnection -LocalPort 8000` before trusting a live probe.
- ⚠ **vite binds IPv6 ONLY** — probe `localhost:5173`, **never** `127.0.0.1:5173`.
- ⚠ HMR can serve stale code after a provider/config change; hard-reload before drawing a conclusion.

**Browser:** Chrome MCP. ⚠ It can hang; ⚠ dropdowns can wedge (a `:hover` state needs a real
hit-test, not a class assertion). ⚠ **`scrollTop` is NOT the reader's position** — measure a leaf
element's bounding rect if scroll position ever matters here.

**⚠ G-4 is the governing guardrail for this plan** and this is exactly what it is for. **G-2 does not
fire** — no surface is being designed; shipped surfaces are being watched.
</execution_context>

<context>
@.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-CONTEXT.md
@.planning/milestones/v4.0-phases/233-the-preview-see-it-before-it-lands/233-VERIFICATION.md
@.planning/milestones/v4.0-phases/233-the-preview-see-it-before-it-lands/233-UAT-FINDINGS.md
@.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-UAT-RESULTS.md

⛔ Read **only** `238-VERIFICATION.md` lines **204-225** — the UAT table and its footer. That is the
M-9 row and its neighbours. ⛔ Do not read or re-judge the rest of that file; `245-01`'s objective
explains why (DEBT-03 must not become a re-review, and this plan is not exempt).

@frontend/src/components/sources/SourcePreviewPanel.tsx
@frontend/src/components/sources/previewVocabulary.ts
@frontend/src/components/classification/RuleBuilderPanel.tsx
@backend/app/api/classification_rules.py

<interfaces>
**The five rows, verbatim from `233-VERIFICATION.md:80-88`** — this is the acceptance text, copy it
into the results file rather than paraphrasing:

  1. Point at a real Drive folder -> four buckets, counts sum, the hatched segment reads as uncertainty.
  2. Close without confirming -> check `documents`, `document_chunks`, `folders`, `ingestion_jobs` are unchanged.
  3. Confirm -> the counts match, the bar dissolves, a refusal names its cause.
  4. Confirm the same folder twice -> nothing is imported again and nothing is embedded again.
  5. A native Google Doc that IS already in the Library -> previews as *can't tell*, resolves to *already here*.

  **Run row 2 first** — it is the one criterion whose failure is invisible from the screen.

**238's M-9 row, verbatim from `238-VERIFICATION.md:216`** — note precisely what is owed:

  | M-9 | A `path contains '/Finance/'` rule fires for a file that IS in that folder |
  ⚠ **HALF PASS, and stronger than before.** The fact is now real **in the database**, not only in the
  adapter: a watched document carries
  `metadata.source.path = /Attachments/Practical_Project_Management_Guide_Recreated.docx`.
  ⛔ The *rule* was still not created, so end-to-end matching remains owed — and it must be
  re-driven through the **manual import** door too, since that arm was broken until Defect 3 was fixed.

**Measured at HEAD by the orchestrator — use these, and RE-CAPTURE immediately before driving:**

| what | value at planning time |
|---|---|
| `documents` | **162** |
| `document_chunks` | **7988** |
| `folders` | **24** |
| `ingestion_jobs` | **72** |
| `classification_rules` | **1** — `name='Research'`, `rule_scope='classification'`, matches `author eq 'Fahed Mrad'`. ⭐ **No `path contains` rule exists, so M-9's rule is NEW and collides with nothing.** |
| `connector_watches` | **2** |
| the app's Google connection | `service_id=google · name='Google Workspace' · status=active · is_enabled=True · last_check_verdict=ok · auth_type=oauth_byo · org_id=22f9c615-0eec-440a-8804-ed4784d6f57f` |
| the Drive MCP available to Claude | authenticates as **`fhdmrd@gmail.com` — the operator's own account**; My Drive root id `0AA-HnFdZ5l8uUk9PVA`; `create_folder` / `create_file` / `upload_asset_from_url` available |

**⚠ REAL COLUMN NAMES — write SQL against these, not against the obvious guess:**
- `connector_watches` has **NO `folder_id`**. It is `source_folder_id` / `source_folder_name` /
  `source_drive_id` / `library_folder_id`.
- `connector_connections` has **NO `display_name`**. It is `name`.

**The local DB read recipe** (⛔ `psql` is NOT on PATH on this box; the venv python is):

  backend/venv/Scripts/python.exe -c "<script>"
  # asyncpg against postgresql://postgres:postgres@127.0.0.1:54322/postgres

Write the snapshot helper into the **scratchpad**, never into the watched tree
(`C:/Users/fhdmr/AppData/Local/Temp/claude/C--Vibe-Apps-Agentic-RAG/<session>/scratchpad/`).

**The results-file frontmatter shape to adopt (D-12), verbatim from `243-UAT-RESULTS.md:1-12`:**

  ---
  phase: 245
  kind: uat-results
  driven: <date>
  driver: claude (solo — OV-SOLO-01)
  environment: "local dev — vite :5173, backend :8000, Supabase :54322, Redis :6379"
  thread: "<the fixture folder identifier — Drive folder name + id, and the Library folder it imported into>"
  rows_driven: [...]
  rows_partial: [...]
  rows_owed: [...]
  ---
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The fixture, and the proof it is the app's own Drive — plus row 2's pre-drive baseline</name>
  <files>.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-UAT-RESULTS.md</files>
  <read_first>
    - `.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-UAT-RESULTS.md` lines 1-40 — the frontmatter and the "Setup — and why a thread had to be built" section; this task is the same move for a folder
    - `.planning/milestones/v4.0-phases/233-the-preview-see-it-before-it-lands/233-UAT-FINDINGS.md` — the doors and the failure shapes, before touching anything
  </read_first>
  <action>
**(a) Confirm infra rather than assuming it.** Probe `localhost:5173` (⚠ IPv6 only — not
`127.0.0.1`), `127.0.0.1:8000/health` (or the backend's real health route), `127.0.0.1:54322`,
`127.0.0.1:6379`. All five were live at planning time. If `:8000` is dead, check
`Get-NetTCPConnection -LocalPort 8000` for a stale uvicorn **before** concluding the backend is down,
then escalate to the operator — ⛔ **the operator starts the backend**, an executor does not.

**(b) Build the fixture — attempt MCP first, fall back to a named operator action (D-09, D-10).**

⭐ **Try Claude's own Drive connector first — and record this as `DEVIATION-245-02-A`, because it is
a deviation from D-10 and deviations are labelled here, never smuggled.** Write it into the SUMMARY
in the same shape as `DEVIATION-245-01-A`:

> **What D-10 said:** *"The operator does the Drive-side setup; Claude drives rows 1-4 in a real
> browser."* **What was done:** Claude created the fixture via the claude.ai Drive MCP. **Why:** the
> connector was measured authenticating as **`fhdmrd@gmail.com` — the operator's own account** (My
> Drive root `0AA-HnFdZ5l8uUk9PVA`), and the operator approved this route, so the credential boundary
> D-10 protects is not crossed. **The fallback stands unchanged:** any MCP failure, or any identity
> mismatch at (c), reverts to D-10's named operator action. ⛔ The gating identity proof is kept
> **precisely because** approval to write is not proof the accounts match.

It authenticates as `fhdmrd@gmail.com` with `create_folder` and `create_file` available. Create,
under My Drive (`0AA-HnFdZ5l8uUk9PVA`), a folder named:

  245-UAT-throwaway-DELETABLE

and put **five items** in it (D-09: a handful; exactly one native Doc per D-11):
1. **one native Google Doc** — ⛔ **required**, it is row 5's whole mechanism. Give it distinctive
   body text you can later search the Library for.
2. a PDF
3. a DOCX
4. a plain `.txt`
5. one file the previewer should bucket as unsupported / *can't tell* (e.g. a `.png` or `.zip`)

⚠ **If the MCP route fails for any reason** (a tool is unavailable, a write is refused, or the
identity proof in (c) fails) → **STOP and escalate to the operator as a named action**: *"create a
Drive folder `245-UAT-throwaway-DELETABLE` with five files including one native Google Doc, in the
account the app's Google Workspace connection uses."* ⛔ Do not improvise a substitute fixture, and
do not fall back to an existing real folder — D-09 rejected that explicitly for re-introducing a
live-corpus baseline, which is precisely what makes row 2 provable here.

**(c) ⛔⛔ PROVE THE FIXTURE IS THE APP'S OWN DRIVE, BEFORE DRIVING A SINGLE ROW.** This is a gating
step, not a formality. The claude.ai Drive connector and the app's OAuth connection could be two
different Google accounts — and **an invisible folder looks EXACTLY like a broken preview.** That
ambiguity has to be designed out, not discovered mid-drive.

In the browser, through the product: open the connected-source picker / folder browser for the Google
Workspace connection and confirm **`245-UAT-throwaway-DELETABLE` appears in the listing**, with its
five children. Screenshot it.

⛔⛔ **VISIBILITY IS NOT ATTRIBUTION — THE PROOF IS AN ID EQUALITY.** Two accounts can each hold a
folder of that name, and a same-named folder in the *wrong* Drive passes a name check while
guaranteeing every subsequent row measures the wrong thing. So:

  MCP-returned folder id  ==  app-reported folder id

Quote **both ids verbatim** in `245-UAT-RESULTS.md` §Setup, side by side, with the equality asserted
in words. ⚠ This is 243's exact lesson, cited two paragraphs above in this plan's own objective: **a
click for `anthropic` selected `minimax`, and a scoreboard with unverified attribution is worse than
none.** A name match is that scoreboard.

- **Ids equal** → the accounts match; proceed.
- **Folder absent, OR the ids differ** → ⛔ **STOP. Do NOT drive any row.** Escalate as the operator
  action in (b) and record the finding in the results file — *which Drive the app actually reads is
  itself a useful measurement*, not a failure of the plan.
- ⚠ If the app's browser does not surface a folder id at all, obtain it from the API response backing
  that listing. ⛔ Do not substitute the folder NAME as the identity proof; if no id is obtainable,
  that is a STOP and a recorded finding, not a downgrade to a name check.

**(d) Capture row 2's baseline — the measurement the whole criterion turns on.** Write the scratchpad
asyncpg helper and snapshot, in ONE transaction-free read:

  SELECT count(*) FROM documents;
  SELECT count(*) FROM document_chunks;
  SELECT count(*) FROM folders;
  SELECT count(*) FROM ingestion_jobs;
  SELECT count(*) FROM classification_rules;
  -- plus, so "unchanged" means "no new rows" and not merely "the same total":
  SELECT max(created_at) FROM documents;
  SELECT max(created_at) FROM ingestion_jobs;

Planning-time values to compare against (⛔ re-capture; do not reuse these as the baseline):
`documents 162 · document_chunks 7988 · folders 24 · ingestion_jobs 72 · classification_rules 1`.

⚠ **Capture counts AND the max timestamps.** A count is not a set — a delete plus an insert leaves the
count unchanged, and this project's own standing lesson is *capture the SET, never a tail*.

**(e) Create `245-UAT-RESULTS.md`** with the D-12 frontmatter from `<interfaces>`, a §Setup section
recording (a)-(d) — the fixture, the identity proof with its screenshot, and the baseline table —
and **seven empty row sections** (233 R1-R5, 238 M-9, and the D-14 237-coverage record). Each row
section states **which requirement it discharges** (R1-R5 → **DEBT-02**; M-9 → **DEBT-01**; the 237
record → neither, it is a coverage note). Verdict fields start as `pending`. ⛔ Never pre-filled.
  </action>
  <acceptance_criteria>
    - All five ports answered, with the method recorded (and, if `:8000` needed the operator, that is written down rather than silently worked around).
    - The Drive folder `245-UAT-throwaway-DELETABLE` exists with five items, exactly one of which is a **native Google Doc**, and the results file names how it was created (MCP or operator).
    - ⛔ **The identity proof is an ID EQUALITY, recorded with a screenshot**: the folder and its five children are visible in the app's own connected-source browser, AND the app-reported folder id is quoted beside the MCP-returned folder id, and they are equal. ⛔ A drive that proceeded on a NAME match, or with no id at all, is invalid and must be re-driven.
    - The baseline table in the results file carries FIVE counts **and** the two `max(created_at)` values, read from `127.0.0.1:54322` — ⛔ not from the Supabase MCP, which points at production.
    - `245-UAT-RESULTS.md` exists with D-12's frontmatter keys and seven row sections, every verdict reading `pending`, each row naming DEBT-01 or DEBT-02.
    - The scratchpad helper lives OUTSIDE the repo tree: `git status --short` shows no new untracked file other than the results file.
  </acceptance_criteria>
  <verify>
    <human-check>The fixture folder is visible in the app's own Drive browser with its five children, and the baseline snapshot is written down. ⛔ This is an observation, not a test: no suite can tell you whether the folder the app can see is the folder Claude created.</human-check>
  </verify>
  <done>A throwaway fixture exists, is PROVEN to be the Drive the app reads, and the four-table baseline is captured with timestamps as well as counts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — the operator confirms the fixture and authorises writes into the local corpus</name>
  <what-built>
A throwaway Drive folder `245-UAT-throwaway-DELETABLE` with five files including one native Google
Doc, proven visible through the app's own connected-source browser, and a four-table baseline
snapshot of the local database. Nothing has been imported yet and no row has been driven.
  </what-built>
  <how-to-verify>
1. Confirm the folder `245-UAT-throwaway-DELETABLE` in your Google Drive is yours and is safe to
   delete afterwards. ⚠ If Claude could not create it, this is where you create it — five files,
   **one of them a native Google Doc**.
2. Confirm the screenshot shows that folder inside the app's own Drive browser. If it is absent, the
   claude.ai connector is on a different Google account and the folder must be recreated in the
   account `Google Workspace` (org `22f9c615-…`) uses.
3. Authorise the drive to write into the LOCAL corpus. Rows 3, 4 and 5 import real documents; M-9
   creates a real `classification_rules` row. All of it is additive into a deletable fixture folder
   (D-09), and the pre-drive baseline is recorded so it can be told apart from your real data.
4. Confirm nothing else is running against the local database right now — no test suite, no second
   agent. Worktree rule 4: **worktrees isolate files, not Postgres**, and row 2 is scored on
   before/after counts that a concurrent suite would silently invalidate.
  </how-to-verify>
  <resume-signal>Type "approved" to authorise the drive, or name the fixture you created / the account mismatch you found.</resume-signal>
  <note>
⚠ **Two of these were pre-approved by the operator at planning time** — Claude creating the Drive
fixture via MCP, and writes into the local corpus. ⛔ **The checkpoint is kept anyway, and so is the
STOP arm**: approval to write is not proof the accounts match, and step 4 (nothing else touching the
local DB) is a fact only the operator can confirm at the moment of the drive. A pre-approval shortens
this pause; it does not remove it.
  </note>
  <action>
PAUSE. Present the fixture listing, the identity-proof screenshot and the baseline table from Task 1,
then wait for the operator. ⛔ Do not import, do not confirm a preview and do not create a
classification rule until the resume signal arrives. ⚠ If the operator reports an account mismatch,
the correct next move is to STOP and let them recreate the folder in the account the app's Google
Workspace connection uses — ⛔ not to substitute an existing real folder, which D-09 rejected for
re-introducing a live-corpus baseline.
  </action>
  <verify>
    <human-check>The operator has answered. Their answer is recorded verbatim in `245-UAT-RESULTS.md` §Setup — including a refusal or a correction, which are results too.</human-check>
  </verify>
  <done>The operator has authorised writes into the local corpus and confirmed the fixture is theirs, or has named what must change first.</done>
</task>

<task type="auto">
  <name>Task 3: Drive 233's five rows — row 2's verdict written before anything is imported</name>
  <files>.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-UAT-RESULTS.md</files>
  <read_first>
    - `.planning/milestones/v4.0-phases/233-the-preview-see-it-before-it-lands/233-VERIFICATION.md` lines 78-88 — the five rows verbatim
    - `frontend/src/components/sources/previewVocabulary.ts` — the four buckets and the exact *can't tell* wording rows 1 and 5 assert against. ⚠ Assert the **rendered words**, not a `data-testid`: this project measured a green presence fence coexisting with a shipped content defect, because *presence assertions cannot see content drift*.
    - `.planning/sketches/233-the-source-says-what-it-did/` — the bar for row 1's *"hatched segment reads as uncertainty"*
  </read_first>
  <action>
**Drive order, and how it honours "run row 2 first":** open the preview **once**, record row 1's
observations from that same preview, then **close without confirming and write row 2's verdict — and
write it BEFORE any confirm happens anywhere.** Nothing may be imported until row 2 is measured and
recorded. Then rows 3 → 4 → 5. That is the order the instruction exists to protect: *row 2 is the one
criterion whose failure is invisible from the screen*, and once a confirm has run it can never be
measured again on a virgin baseline.

**Row 1 — DEBT-02.** Point the preview at `245-UAT-throwaway-DELETABLE`. Record: the **four buckets**
by their rendered names, their counts, **whether the counts SUM to the file total** (arithmetic, done
explicitly and written out), and whether the hatched segment **reads as uncertainty** to a person
looking at it. ⚠ That last one is a judgement about the screen — write what you saw and why you
judged it either way, with the screenshot; do not convert it into a class-name assertion.

⚠ **Tie-breaker, so a judgement call cannot quietly become a pass:** if the hatched segment is
**borderline** — you can construct a reading either way — record **both readings** plus the
screenshot and score the row **`⚠ partial`**, not `pass`. ⛔ A judgement resolved in the drive's own
favour is the softest way this phase could fail its own subject.

**Row 2 — DEBT-02. ⛔ THE ROW THE WHOLE G-4 ARGUMENT RESTS ON.** Close the preview **without**
confirming. Re-run the exact baseline snapshot from Task 1 and write **before → after** pairs for all
four tables plus both `max(created_at)` values:

  documents · document_chunks · folders · ingestion_jobs   (+ max(created_at) on documents and ingestion_jobs)

Pass = every one identical. ⚠ **Report a delta as a DEFECT, not as noise** — and if you see one,
capture it and check whether anything else was writing (Task 2 step 4) before attributing it. ⛔ **No
screen observation can score this row**, which is exactly why it goes first and is read from the DB.

**Row 3 — DEBT-02.** Confirm the import. Record: **do the imported counts match what the preview
promised** (bucket counts vs rows actually created — compute it, do not eyeball it); **does the bar
dissolve**; and **if any file is refused, does the refusal NAME ITS CAUSE in words on screen**. ⚠ The
native Google Doc must come in here — row 5 depends on it. ⚠ Watch for the `233-UAT-FINDINGS` shapes
rather than assuming they are fixed: sub-folders ignored, connector path not queuing, provenance
(`metadata.source`) wiped on completion. Read one imported document's `metadata->'source'` from the DB
and write down whether tier-1 identity survived completion — **that is BUG-260905-03's exact grave,
and one query settles it.**

**Row 4 — DEBT-02.** Confirm the **same folder** again. Record: **nothing imported again**
(`documents` count delta = 0, and no new `ingestion_jobs` for those files) and **nothing embedded
again** (`document_chunks` delta = 0). Read both from the DB.

**Row 5 — DEBT-02 (D-11, runs after row 3 off the same fixture).** Re-preview the same folder now
that the native Google Doc IS in the Library. Assert the Doc **previews as *can't tell*** and
**resolves to *already here***, in the rendered vocabulary from `previewVocabulary.ts`.

⚠ **Rows 4 and 5 may share ONE observation pass** (both re-preview after an import) — but they are
**two distinct assertions and get two separate written verdicts** (D-11). ⛔ One verdict covering both
is a half-pass, which is the exact shape this phase is correcting elsewhere.

**Write each verdict into `245-UAT-RESULTS.md` the moment it is observed**, before moving to the next
row. ⛔ Do not batch them at the end: this project's standing lesson is *capture the set before
re-running anything*, and a verdict written from memory after four more rows is a recollection, not an
observation.

**D-16 stands on every row.** A defect ≤ 1 file / ≤ 10 lines / no schema / no API surface may be
fast-fixed **and the fix must then be re-driven on the affected row**. Anything larger → write a
report under `.planning/reported-bugs/` using `TEMPLATE.md` (`surface: Agentic-RAG`, `status: open`,
concrete `affected_areas`, a named `re_open_trigger`) and record it in the results file. ⛔ **Filing
is a complete response.** Do not begin a second fix.
  </action>
  <acceptance_criteria>
    - Five row sections in `245-UAT-RESULTS.md`, each with its own verdict (`pass` / `⚠ partial` + what is owed / `⛔ fail` + the defect id), each naming **DEBT-02**, each with the evidence inline.
    - **Row 2 carries eight numbers** — before and after for `documents`, `document_chunks`, `folders`, `ingestion_jobs` — plus both `max(created_at)` pairs, and its verdict was written BEFORE any confirm ran (assert the ordering in the section itself).
    - Row 1 states the four bucket names as RENDERED, their counts, and the explicit arithmetic that they sum.
    - Row 3 records the preview-promise vs rows-created comparison, the bar's behaviour, any refusal's worded cause, **and** one imported document's `metadata->'source'` read from the DB.
    - Row 4 records `documents` and `document_chunks` deltas of exactly `0`.
    - Row 5 asserts the rendered *can't tell* → *already here* transition for the native Google Doc, quoting the words on screen.
    - Rows 4 and 5 have **separate** verdicts even if observed in one pass.
    - Any defect found is either (a) ≤1 file/≤10 lines with the affected row **re-driven**, or (b) a filed report with an id, quoted in the results file. ⛔ No third option appears.
    - ⛔ `git diff --stat -- backend/ frontend/` is EMPTY unless a D-16 fast-fix landed — and if one did, it is ≤ 1 file and ≤ 10 lines, stated with its `--numstat`.
  </acceptance_criteria>
  <verify>
    <human-check>Five written verdicts exist, each traceable to something watched or measured. Row 2's eight numbers were read from the local database at `127.0.0.1:54322` before any confirm. ⛔ No suite run appears as evidence for any row — the ROADMAP's first named failure mode for this phase is a row marked done on a passing test.</human-check>
  </verify>
  <done>SC#2 is driven: 233's five owed rows each carry a written verdict, row 2 measured against the DB on a virgin baseline.</done>
</task>

<task type="auto">
  <name>Task 4: M-9 end-to-end through BOTH doors, and what it does and does not settle about Phase 237</name>
  <files>.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-UAT-RESULTS.md, .planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md</files>
  <read_first>
    - `frontend/src/components/classification/RuleBuilderPanel.tsx` — the real surface the rule must be created through; **the surface Phase 237 shipped and nobody has ever clicked**
    - `backend/app/api/classification_rules.py` — `rule_scope` validation and the `WATCH_ALLOWED_FIELDS` 422 refusal
    - `.planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md` lines **204-225 only** — the table and its footer
  </read_first>
  <action>
**(a) Create the rule THROUGH THE REAL SURFACE (D-05).** In the browser, via `RuleBuilderPanel`,
create a rule matching **`path contains '/Finance/'`**. ⛔ Not via the API, not via SQL — the surface
is half the point: 237 shipped it and *"never manually clicked"* ends here, or it does not end.
Record the rule's DB row afterwards (`classification_rules` count `1 → 2`, `rule_scope`, `match_expr`
as stored). ⭐ No `path contains` rule exists today, so nothing collides.

**(b) Drive the match through BOTH doors — and the manual one is not optional (D-05).**

- **Door 1, the watch loop.** A file that IS under a `/Finance/` path arrives via a watch and the rule
  fires. Record the document row, its `metadata.source.path`, and the classification the rule
  produced. ⚠ `connector_watches` uses `source_folder_id` / `source_folder_name` /
  `source_drive_id` / `library_folder_id` — **there is no `folder_id`**.
- **Door 2, MANUAL IMPORT. ⛔ This is the arm that must be driven, and the reason is on the record:**
  *"the manual-import arm is exactly where Defect 3 lived (`metadata.source` had two writers; only the
  watch one was fixed), so the arm with the worse track record is the one that must be driven."*
  Import a `/Finance/`-pathed file through the manual door and record the same three facts. ⛔ A pass
  on door 1 alone is a **half pass** — the exact verdict shape this phase is correcting.

⚠ Both doors need a `/Finance/` path. Simplest fixture: a `Finance` subfolder inside
`245-UAT-throwaway-DELETABLE` holding one file, reachable by both doors. Record how you arranged it.

**(c) Update M-9's row in `238-VERIFICATION.md` — in place, history preserved.** Flip the headline to
its measured verdict and keep the existing half-pass text as its history, in this project's house
style: **correct BESIDE, never over.** Point at `245-UAT-RESULTS.md` for the evidence. ⛔ Touch **only
the M-9 row**. Do not touch M-8 (that is `245-03`'s, per D-06), the footer line (also `245-03`'s), or
any other row. ⛔ Do not re-read or re-judge anything else in that file — DEBT-03's no-re-review rule
binds this plan too.

⛔⛔ **IF EITHER DOOR FAILS, M-9 IS RECORDED AS `⛔ BLOCKED` — NEVER AS `HALF`.** Name the failing
door, its defect id and a **named trigger** (e.g. *"door 2 (manual import) failed — `BUG-…` filed —
trigger: the next phase touching connector import"*).

⚠ **`HALF` is not one of SC#1's three admissible verdicts.** SC#1 admits **pass** · **⛔ blocked with
its reason and its blocking id** · **retired with a named trigger** — and this whole phase exists
partly because M-8's `HALF PASS` headline contradicted its own body. ⛔ Reproducing that shape on
M-9 would be the phase committing its own subject. ⛔ Equally: **do not round a partial UP.** A row
that reads PASS on one door is not a pass.

⚠ **And it must not be left in limbo either:** if the drive does not happen at all, M-9 is left
untouched here and `245-03` T1 converts it to `⛔ BLOCKED — <reason>, <blocking id>, trigger:
<named>`. **That path is written into `245-03` explicitly**, so no end state exists in which M-9
reads HALF and no plan owns the fix.

**(d) The 237 coverage record (D-14).** Write the seventh section of `245-UAT-RESULTS.md`:

- **What the click COVERED:** a `path contains` rule authored end-to-end through the real
  `RuleBuilderPanel` surface, persisted, and fired against real documents through both ingest doors.
  **"237's rule builder was never manually clicked" is now false**, and that is worth recording in
  plain words.
- **What it did NOT cover:** 237's **scope selector** (the segmented control choosing `rule_scope`)
  and **out-of-scope condition filtering on scope switch** — a single `path contains` rule exercises
  neither. Also unexercised: the `WATCH_ALLOWED_FIELDS` **422 refusal** for an arrival-watch rule.
- **Named trigger for the rest:** *the next classification-rules phase* (per the CONTEXT's deferred
  list).

⛔ **Claiming "clicked" on one of three behaviours is the same half-pass problem M-8 and M-9 are being
corrected for.** Say which third was clicked.
  </action>
  <acceptance_criteria>
    - `classification_rules` went `1 → 2`, and the new row's `match_expr` encodes `path contains '/Finance/'` — read from the local DB and quoted.
    - The results file records **HOW** the rule was created, and it is `RuleBuilderPanel` in a browser. ⛔ An API or SQL creation invalidates the row and must be re-driven through the surface.
    - **Two door sections**, watch loop and manual import, each with the document row, its `metadata.source.path`, and the resulting classification. ⛔ A single combined section is a half pass.
    - M-9's row in `238-VERIFICATION.md` reads either `✅ PASS` (both doors) or `⛔ BLOCKED` with the failing door, its defect id and a named trigger. ⛔ It does **not** read `HALF` in either case — `grep -c "HALF"` on the M-9 line region returns `0`. The prior half-pass text is preserved beside the new verdict as history, and `git diff` on that file touches the M-9 line region only.
    - The 237 coverage section names what was covered, the three things that were not (scope selector, out-of-scope filtering on switch, the 422 refusal), and one named trigger.
    - `git diff --numstat -- .planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md` shows a change confined to the M-9 row; ⛔ the M-8 row and the `9 rows driven …` footer are byte-unchanged (`grep -c "HALF PASS" ` still counts M-8's).
  </acceptance_criteria>
  <verify>
    <human-check>A rule was clicked into existence in a real browser, and it was watched firing through the watch loop AND through the manual import door, with the DB rows to show it. ⛔ Nothing here is scored by a test: 238's own durable lesson is *"the test doubles were mine, so they agreed with my implementation rather than with reality"* — four defects, none caught by its suite.</human-check>
  </verify>
  <done>M-9 is driven end-to-end through both doors with the rule created through the surface 237 shipped, and the verdict says exactly which third of that surface was exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **external Drive → the app's ingest path** | Untrusted file names, paths and content cross into `documents` / `document_chunks`. This drive deliberately sends real third-party content through it. |
| **browser → API** | Every row is driven through the product's own authenticated surface; nothing is issued directly against the DB except **reads**. |
| **local Postgres** | Written by this plan. ⛔ Production is NOT touched: `mcp__supabase__*` is forbidden here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-245-02-01 | Tampering | the local corpus (`documents`, `document_chunks`, `folders`, `ingestion_jobs`) | mitigate | Every write is additive into a folder that did not exist before (D-09), a four-table baseline **with timestamps** is captured first, and the fixture is named `…-DELETABLE` so teardown is unambiguous. ⛔ No existing row is edited. |
| T-245-02-02 | Tampering | **production** Supabase | mitigate | The Supabase MCP points at production and is **forbidden in this plan by name**. All reads go to `127.0.0.1:54322`. ⛔ A production write would also need per-action operator approval, which this plan never requests. |
| T-245-02-03 | Information Disclosure | the fixture's file contents | accept | The operator's own Drive, five files they chose, a local-only corpus. No third-party data and no secret enters the repo; screenshots are the only artifact and they show the operator's own fixture. |
| T-245-02-04 | Repudiation | `245-UAT-RESULTS.md` | mitigate | Verdicts are written **at the moment of observation**, never batched; row 2 carries eight measured numbers rather than a claim; each row names the requirement it discharges; a partial stays partial. ⛔ `rows_owed` is populated rather than left empty — silence is how these rows got here. |
| T-245-02-05 | Denial of Service | concurrent DB mutation | mitigate | The plan runs ALONE in its wave, `backend/tests` is forbidden inside it, and the checkpoint asks the operator to confirm nothing else is writing. Worktree rule 4 is stated as the reason. |
| T-245-02-06 | Spoofing | the fixture's account identity | mitigate | ⛔ The gating identity proof: the folder must be visible **through the app's own browser** before any row is driven. Two Google accounts would make an empty preview indistinguishable from a broken one — and 243's abandoned provider board is the precedent for what unverified attribution does to a scoreboard. |
| T-245-02-SC | Tampering | npm/pip/cargo installs | n/a | **No package is installed.** The scratchpad snapshot helper uses `asyncpg`, already a backend dependency, via `backend/venv/Scripts/python.exe`. `git diff -- backend/requirements.txt frontend/package.json` MUST be empty. |
</threat_model>

<verification>
⛔⛔ **EVERY ITEM HERE IS AN OBSERVATION OR A MEASUREMENT. THERE IS NO TEST RUN IN THIS PLAN'S
VERIFICATION, AND THAT IS DELIBERATE.** The ROADMAP's first named failure mode for Phase 245 is *"a
row is marked done on the strength of a passing test suite rather than a driven observation"*, and
`233-UAT-FINDINGS.md` is the measured proof: **5,600 automated tests found none of the six defects one
folder import found.**

**What must be true at the end:**
1. `245-UAT-RESULTS.md` holds **seven** sections — 233 R1-R5, 238 M-9, the 237 coverage record —
   each with a verdict and each naming DEBT-01 or DEBT-02.
2. The identity proof screenshot exists and the fixture is named in the frontmatter's `thread` field.
3. Row 2's eight numbers plus two timestamp pairs, read from `127.0.0.1:54322`, written **before** any
   confirm.
4. Rows 3/4 carry computed deltas, not impressions. Row 5 quotes the rendered words.
5. M-9 has two door sections. `238-VERIFICATION.md`'s M-9 row is updated; **M-8 and the footer are
   byte-unchanged** (they are `245-03`'s).
6. `rows_owed` in the frontmatter is **populated** — anything not driven is named with a reason, never
   dropped. A scoreboard that lists only what passed is not a scoreboard.
7. Any defect is either a ≤1-file/≤10-line fast-fix **with its row re-driven**, or a filed report with
   an id. ⛔ No third option.

**Negative fences:**
- ⛔ `backend/tests` was not run at all in this plan (it would corrupt row 2's baseline). Say so.
- ⛔ No `mcp__supabase__*` call appears in the transcript.
- `git diff --stat -- backend/ frontend/ supabase/` is EMPTY unless a D-16 fast-fix landed, stated
  with its `--numstat`.
- The fixture Drive folder still exists at plan close (leave it, per 243's precedent — a later-owed
  row may need it), and the results file says it is safe to delete.

**⛔ NOT verified here, and owed:**
- **SC#1 is NOT closed by this plan.** M-9 is one of eleven rows. M-8's flip and S-1/S-2's retirement
  are `245-03`'s, and SC#1 closes only when all of 238's rows read pass / blocked / retired.
- **SC#3 / SC#4** — `245-01`'s, already discharged; nothing here touches them.
</verification>

<success_criteria>
- **SC#2 closed by observation:** 233's five G-4 rows driven in a live browser with a written verdict
  each, row 2 first and measured against the database.
- **SC#1's M-9 arm closed:** the rule created through `RuleBuilderPanel` and fired through **both**
  doors, or honestly recorded as partial with a filed defect and a named deferral.
- The fixture was **proven** to be the Drive the app reads before any row was driven — by **id equality**, not by name — and `DEVIATION-245-02-A` is recorded for Claude having done D-10's operator half.
- D-14's 237 record says which third of that surface was clicked and names a trigger for the rest.
- `245-UAT-RESULTS.md` exists in 243's shape with `rows_owed` populated.
- The SUMMARY states plainly: **SC#2 driven; SC#1 partially advanced (M-9 only); no criterion in this
  plan was scored by a suite.**
</success_criteria>

<output>
Create `.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-02-SUMMARY.md` when done.
</output>
