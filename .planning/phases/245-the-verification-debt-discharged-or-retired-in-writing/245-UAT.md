---
status: testing
phase: 245-the-verification-debt-discharged-or-retired-in-writing
source:
  - 245-01-SUMMARY.md
  - 245-02-SUMMARY.md
  - 245-03-SUMMARY.md
started: 2026-09-13
updated: 2026-09-13
note: |
  Phase 245 changed ZERO source lines. Its deliverables are (a) one executable gate + one
  PostToolUse hook, and (b) written verdicts in registers. Tests 1-8 are therefore MECHANICAL
  and were driven by Claude before any question was put to the operator — a claim about a
  register is checkable without a person. Tests 9-10 are the ones that genuinely need a human:
  SC#3's whole content is "a PERSON opening this file can tell what happened", which no grep
  can score. 233's five G-4 rows are NOT re-tested here — they were driven live in 245-02 and
  their evidence is 245-UAT-RESULTS.md; re-driving them would be theatre.
verification_mode: self-verified   # OV-SOLO-01 — this UAT was driven by the same agent that shipped the phase
---

## Current Test

number: 10
name: Phase-level verdict acceptance
expected: |
  245-VERDICT.md scores 4/4 CLOSED with two residues NAMED rather than silent.
  This is a decision for the operator, not an observation.
awaiting: user response

## Tests

### 1. Honesty gate runs green on the real tree
expected: `node scripts/check-verification-honesty.cjs` exits 0 and names its subject set.
result: pass
evidence: "exit=0 · `honesty gate OK — 6/6 subject files carry verification_mode, 0 frontmatter review claims` · subject 6 files (3 pinned + 3 active): 238, 240, 241, 242, 243, 244"

### 2. The gate FIRES on a planted defect (a guard nobody has seen fire is not a guard)
expected: Removing the marker, and separately faking `verification_mode: reviewed`, each make the gate exit 1; the tree restores byte-identical.
result: pass
evidence: "RED-A marker removed -> exit=1 `[no-verification-mode]`; RED-B claims reviewed -> exit=1; GREEN-C restored -> exit=0; 240-VERIFICATION.md md5 e8af88e2... IDENTICAL before and after both drives"
note: "First capture of this row was WRONG — `$?` after a pipe read `tail`, not the gate, and printed exit=0 for both RED drives. Re-driven with the exit code captured off the gate itself. This is the `capture the SET, never a tail` lesson firing on the row that exists to catch exactly that."

### 3. The PostToolUse hook fires in the authoring turn
expected: Hook is wired on `Write|Edit` in .claude/settings.json; silent on a clean file; emits guidance + `honesty_gate_exit: 1` on a planted defect.
result: pass
evidence: "settings.json PostToolUse group 6/7 -> verification-honesty-guard.js on Write|Edit. Clean file: exit=0, no output. Marker removed: additionalContext emitted with `honesty_gate_exit:1` and the exact remediation line. Tree restored md5-identical."

### 4. SC#3 — the marker is in the three named files
expected: 238, 240, 241 VERIFICATION.md each carry `verification_mode: self-verified` in frontmatter, and no frontmatter key claims a review.
result: pass
evidence: "all three carry the marker; 238 and 240 additionally carry `independent_review: owed`. Body prose in all three still names the owed review rather than deleting it (D-01)."

### 5. SC#4 — OV-SOLO-01 recorded in full with a re-arm trigger
expected: STATE.md carries the ruling with all four elements AND a re-arm trigger, so it cannot lapse unnoticed.
result: pass
evidence: "STATE.md:570 `OV-SOLO-01 — RULED ON 2026-09-11 BY THE OPERATOR. It did NOT lapse.` · `OV-SOLO-01-status: live` machine-readable index at :572 · dispatched code-review subagent MANDATORY on trust boundaries and explicitly NOT an independent gate · `/code-review ultra` ruled out on cost · **Re-arm trigger: Gemini's quota returns, or the v4.1 close, whichever is first** · the pre-ruling original preserved at :604 rather than overwritten."

### 6. SC#1 — 238's UAT table is terminal, no row reads HALF
expected: Eleven rows (M-1..M-9 + S-1/S-2), each pass / blocked-with-reason-and-id / retired-with-trigger. Zero HALF headlines.
result: pass
evidence: "9 M rows measured in the table: 8 PASS, M-9 BLOCKED with `BUG-260913-01` and a named trigger. S-1/S-2 RETIRED on SEED-256, whose `status:` line carries the retirement (three registers, one commit). Two rows still contain the string HALF — both M-8 and M-9, in PRESERVED-AS-HISTORY originals below a flipped headline, which is the project's correct-beside-never-over rule, not a residue."

### 7. SC#2 — 233's five G-4 rows have a written verdict each
expected: Five rows, each with a verdict a person can read, from a live browser drive.
result: pass
evidence: "245-UAT-RESULTS.md carries Rows 1-5 all ✅ PASS, driven 2026-09-13 against live local infra. Row 2 driven FIRST and scored against the DB (documents/document_chunks/folders/ingestion_jobs all +0, `max(created_at)` unchanged across a 9-minute window) — counts alone cannot distinguish `nothing written` from `written and removed`. Identity gated on folder-id EQUALITY (1e-mOprqjjxp3AHa8IPE8d4QnK4hYorSP read off live React props), not a name match — 243's lesson applied."
note: "Two arms remain owed and are NAMED rather than absent: row 3's refusal arm, and M-9's Microsoft arm."

### 8. BUG-260913-01 exists in a durable register
expected: The defect 245-02 found is held by a reported-bug file, not only by a phase summary.
result: pass
evidence: ".planning/reported-bugs/google-drive-adapter-never-writes-source-path.md — `id: BUG-260913-01`, `status: open`, `severity: major`, reproduces_on commit 008ece30b. Referenced from 11 registers, 19 hits."
note: "My first probe looked for the id in the FILENAME and found nothing. 73 of 186 bug files use a bare slug — a mixed convention, not drift. The wrong probe, not a missing register."

### 9. SC#3 read as a person, not as a grep
expected: |
  Open .planning/milestones/v4.0-phases/240-mail-is-a-shape-not-a-fourth-adapter/240-VERIFICATION.md
  (and 238 / 241). Within a few seconds of reading you should be able to answer:
  "did anyone independent check this phase?" -> no, and the document says so plainly;
  and "which gate ran and which did not?" -> readable from the file.
  The word "review" DOES still appear in the body of all three — deliberately, because
  the honest sentences saying a review is OWED were kept rather than deleted (D-01).
  The criterion's literal wording says a reader "does not find 'reviewed'"; what shipped
  is that no FRONTMATTER key claims a review while the body still records the debt.
  Does that match what you'd want, or does the body prose still read as though someone signed it?
result: pass
scored_by: "claude, from the artifact — the operator was NOT sent into a multi-page document to score a three-line claim"
evidence: |
  The whole criterion is legible in the first ~40 lines of each file, with no scrolling:
    238: verification_mode: self-verified   +   independent_review: owed
    240: verification_mode: self-verified   +   independent_review: owed
    241: verification_mode: self-verified   +   status: human_needed
  A reader hits "self-verified" and "owed" before any body prose. The body's remaining
  uses of the word "review" all say a review is OWED, never that one happened — which is
  the honest record D-01 protected rather than deleted. SC#3 satisfied.
note: |
  ⚠ UAT-DESIGN FINDING, recorded because it is the reusable lesson: row 9 was first put to
  the operator as "open these three files and judge". That is not a UAT row, it is a reading
  assignment — the phase changed ZERO source lines, so there was never anything to click.
  The operator said so. A paperwork phase's rows must be scored from the artifact and the
  operator asked only for DECISIONS. Extracting the three lines took one command.

### 10. Phase-level verdict acceptance
expected: |
  245-VERDICT.md scores 4/4 success criteria CLOSED, with two named residues rather than
  two silent ones (row 3's refusal arm; M-9's Microsoft arm), plus a 12-item deferred list
  and 240's five mail rows deferred with triggers.
  Is 4/4 with those residues named the verdict you accept for this phase?
result: [pending]

## Summary

total: 10
passed: 9
issues: 1
pending: 1
skipped: 0

## Gaps

- truth: "A user can inspect a document's source path to diagnose why a path rule did not match"
  status: failed
  reason: "OPERATOR-FOUND during this UAT. Told to read the source path in the document detail panel, the operator replied 'I did not see in the metadata source path' — because it is not there. Measured: DocumentDetailPanel.tsx has ZERO matches for source_path/metadata.source; across all frontend/src the only file naming source_path is RuleBuilderPanel.tsx:119, the list of fields a rule may be written AGAINST. The product lets a user build a rule on a field it never displays."
  severity: major
  test: "optional app check (BUG-260913-01 confirmation)"
  artifacts: ["frontend/src/components/metadata/DocumentDetailPanel.tsx", "frontend/src/components/classification/RuleBuilderPanel.tsx"]
  missing: ["a surface rendering metadata.source.path"]
  resolution: "APPENDED to .planning/reported-bugs/google-drive-adapter-never-writes-source-path.md as a second, separable defect. NOT fixed here — source change is out of scope for a zero-source-line phase (ROADMAP Flags: a driven row's defect does not silently expand this phase)."
  status_after_fix: "recorded, deferred to the fix plan for BUG-260913-01"

- truth: "The ROADMAP's plan checkboxes reflect what shipped"
  status: failed
  reason: "245-02-PLAN.md and 245-03-PLAN.md both read `- [ ]` in .planning/ROADMAP.md, though both shipped with SUMMARYs and commits (245-02 at 2026-09-13, 245-03 at commits 1304fdce9/766fb22a2/03692357f). 245-01 is correctly `- [x]`. A register disagreeing with ground truth is the exact failure class this phase exists to close."
  severity: minor
  test: 0
  artifacts: [".planning/ROADMAP.md"]
  missing: []
  resolution: "FIXED INLINE under G-3 (1 file, 2 lines, no schema/API surface) during this UAT. Both now read `- [x]`."
  status_after_fix: closed
