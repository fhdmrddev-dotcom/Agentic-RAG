---
phase: 245-the-verification-debt-discharged-or-retired-in-writing
plan: 03
type: execute
wave: 3
depends_on: ["245-01", "245-02"]
files_modified:
  - .planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md
  - .planning/seeds/SEED-256-sharepoint-half-of-graph-deferred-no-work-tenant.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/milestones/v4.0-ROADMAP.md
  - CLAUDE.md
  - .planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md
  # ⚠ outside the repo, not committable, edited by hand and recorded as such:
  # ~/.claude/projects/C--Vibe-Apps-Agentic-RAG/memory/project_238_built_unreviewed.md
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03]

must_haves:
  truths:
    - "SC#1 — every one of Phase 238's rows reads pass, ⛔ blocked with its reason and blocking id, or retired with a named trigger. No row is silently absent, and NO ROW READS HALF — including M-9, which this plan converts to ⛔ BLOCKED-with-a-trigger if 245-02 could not drive it or drove it partially."
    - "The eleven-or-nine ambiguity is RESOLVED IN WRITING wherever the number appears: the table holds ELEVEN rows — M-1…M-9 plus S-1 and S-2 — and 'nine' means the M rows only."
    - "S-1 / S-2 are retired across all three registers IN ONE COMMIT — 245's verdict, REQUIREMENTS' DEBT-01 row, and SEED-256's frontmatter status — because a retirement held only by a seed is held by nothing (the seeds register is swept by nothing)."
    - "The false claim '238's nine rows are blocked on one Azure app registration, run M-1 first' is corrected BESIDE the original in EVERY live-register instance — DERIVED over two phrases rather than enumerated, at least 8 hits across at least 6 files — and every instance NOT corrected is named in the verdict with the reason it was excluded. ⛔ Zero uncorrected, unexplained instances remain."
    - "SEED-177's Flags item is recorded as REFUTED and CLAUDE.md's copy of the false sentence is corrected beside its original — while the seed itself, which is correct, is left untouched."
    - "240's five G-4 mail rows are deferred WITH A NAMED TRIGGER EACH, in 245's verdict — deferred, never silent, because silence is how they got here."
  artifacts:
    - path: ".planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md"
      provides: "the completed verdict — all four SC sections filled, S-1/S-2 retired, 240's five rows deferred with triggers, SEED-177 refuted"
      contains: "SEED-256"
    - path: ".planning/seeds/SEED-256-sharepoint-half-of-graph-deferred-no-work-tenant.md"
      provides: "the retirement's seed-side half — a status flip plus a pointer, not new prose"
      contains: "status:"
  key_links:
    - from: ".planning/REQUIREMENTS.md DEBT-01"
      to: ".planning/seeds/SEED-256-*.md"
      via: "the same-commit sync rule — a fact with multiple homes moves in one commit"
      pattern: "SEED-256"
    - from: "CLAUDE.md line ~41"
      to: ".planning/seeds/SEED-177-*.md frontmatter"
      via: "a correction beside the original, measured against the seed rather than repeated from the register"
      pattern: "SEED-177"
---

<objective>
**SC#1 closed, and the registers made true.**

Three of this phase's four criteria are bookkeeping that no drive can do: 238's M-8 row flipped off a
self-contradicting HALF PASS, S-1/S-2 retired in writing across three registers in one commit, four
register homes of a false claim corrected beside their originals, `SEED-177`'s Flags item refuted,
and 240's five mail rows deferred with a trigger each.

⭐ **THE FINDING THIS PLAN EXISTS TO MAKE VISIBLE.** *"238's nine rows are blocked on one Azure app
registration, run M-1 first"* is **false at HEAD and has been for six days.** All nine M rows were
driven on **2026-09-07** — the operator completed the registration *hours after* `238-SUMMARY.md` was
written — and `238-VERIFICATION.md:220` has read *"9 rows driven (7 full pass, 2 half) · 0 owed"*
ever since. Meanwhile the ROADMAP, REQUIREMENTS, STATE.md and a memory file all still say *blocked*.
**Each register only knows the one below it; the artifact is the bottom**
(`feedback_a_review_is_a_claim_about_code_not_the_code`, paid for on 2026-09-10 when two Phase 239
criticals were escalated as live after being fixed two days earlier).

⛔ **CORRECT BESIDE, NEVER OVER (D-08).** Overwriting erases the evidence that **six registers carried
one false claim for six days** — **which is this phase's most transferable output.** ⚠ The count is
`six`, not `three`: the derivation in `<interfaces>` found **32 phrase hits**, of which the live
registers span `ROADMAP.md` · `REQUIREMENTS.md` · `STATE.md` · `PROJECT.md` · `MILESTONES.md` ·
`v4.0-ROADMAP.md`, plus one out-of-repo memory file. House style, and
the project's most-repeated lesson: the CLAUDE.md count-gate figures, the `threads.py` superlative and
the `sourceComposition` red figure each keep their wrong original with a dated correction beside them.

⚠⚠ **THIS PLAN RUNS EVEN IF THE DRIVE DID NOT HAPPEN — AND HERE IS THE MECHANISM, NOT JUST THE
CLAIM.** The ROADMAP names *"the Azure registration does not arrive and the phase STALLS"* as a
failure mode, and answers it: **DEBT-01's retirement arm, DEBT-03 and the corrections need no
credential at all.**

⛔ **DEGRADED MODE — the executor follows this without asking, and records that it did:**
1. **`245-UAT-RESULTS.md` may not exist.** Its `@`-import in `<context>` and its mentions in
   `read_first` are **OPTIONAL**: if the file is absent, skip them and say so. ⛔ Do not block, do not
   wait, do not invent its contents.
2. **DEBT-02 is recorded as `still owed, trigger: <named>`** with the reason `245-02` halted (operator
   unavailable · fixture identity mismatch · a STOP arm fired). ⛔ Never quietly ticked.
3. **M-9 is converted by Task 1** to `⛔ BLOCKED — <reason>, <blocking id>, trigger: <named>`. See
   T1(a-bis). Without this, an undriven M-9 would sit at `HALF` — a verdict SC#1 does not admit —
   with no plan owning the fix.
4. ⚠ **If `245-02` DID run and left M-9 partial**, the same conversion applies: a partial is
   `⛔ BLOCKED` with the failing door named, never `HALF`.

⛔ What this plan may never do is leave any of these reading what they read today.

Purpose: *"the next milestone opens on ground whose measured extent is known."*
Output: a terminal 238 table, three synchronised registers, four corrected homes, a completed verdict.

**Solo run (`OV-SOLO-01`):** self-verification, never a review. Say so in the SUMMARY.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then `git rev-parse HEAD` — MUST be
a descendant of `245-02`'s merge on `develop`. ⚠ A fresh worktree starts on the DEFAULT branch
(`master`, stuck at the v3.9 ship commit). Assert the SHA first.
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

**⚠ CLAUDE.md IS EDITED BY THIS PLAN AND IS SUBJECT TO THE 150,000-CHAR GATE.**
`node scripts/check-claude-md-size.cjs` must exit `0` — it fails on size, on `[duplicate-row]`, on
`[malformed-row]` and on a ledger disposition cell over **200 chars**. ⚠ Measure with the gate, never
with `wc -c` (characters, not bytes: `wc` read 197,197 for a file the harness called 194.9k). The
limit has tripped **twice**, and a file over it silently stops applying to every agent everywhere.
⛔ The edit here is a **correction beside an existing sentence**, not a new section — keep it tight.

**⚠ STATE.md discipline:** hand-edit with Edit. ⛔ **NEVER a `state.*` SDK verb** — seven write false
records and corrupted that file five times in Phase 190 alone while reporting success.

⛔ **This plan touches NO application source.** `git diff --stat -- backend/ frontend/ supabase/` MUST
be empty. The backend ceiling (71) and the vitest count gate are therefore **not run**, and the SUMMARY
says that is deliberate with the empty diff as its justification.
⛔ No hot-file-ledger row is owed — no file under `backend/app/` or `frontend/src/` is touched.

**⚠ COMMIT POLICY — say it once so T1's "ONE commit" is not ambiguous.** This plan makes **three**
commits, one per task, in task order:
1. **T1 → ONE commit** carrying `238-VERIFICATION.md` + `SEED-256-*.md` + `REQUIREMENTS.md`
   (+ `245-VERDICT.md`). ⛔ D-07's same-commit sync rule binds **this** commit: the three registers of
   the S-1/S-2 retirement move together or the retirement has drifted at birth.
2. **T2 → one commit** for the correction sweep.
3. **T3 → one commit** for the close-out.
⚠ `REQUIREMENTS.md` is touched by all three; that is fine — the same-commit rule constrains the
**retirement's three registers**, not every later edit to one of those files.

**⚠ G-7 does not fire:** this is not a gap-closure round. **G-3 does not fire:** it is ten files
across six registers with a same-commit sync obligation, not a one-line fix. **G-2 does not fire:**
no UI.
</execution_context>

<context>
@.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-CONTEXT.md
@.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md
@.planning/seeds/SEED-256-sharepoint-half-of-graph-deferred-no-work-tenant.md

⚠ **OPTIONAL import — read it only if it exists** (see DEGRADED MODE in the objective):
`.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-UAT-RESULTS.md`.
⛔ Its absence is a legitimate state, not an error, and must not block this plan.

⛔ Read **only** these regions of the archived files — reading their verdicts is the re-review the
ROADMAP forbids, and it binds this plan as much as `245-01`:
- `.planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md` lines **204-228** — the eleven rows and the footer
- `.planning/milestones/v4.0-phases/240-mail-is-a-shape-not-a-fourth-adapter/240-VERIFICATION.md` lines **200-232** — the ⛔ OWED section: its five mail rows and its §2 carry-forward

<interfaces>
**⛔⛔ THE STALE CLAIM HAS AT LEAST EIGHT LIVE INSTANCES ACROSS SIX FILES, NOT FOUR — AND THE COUNT
IS DERIVED, NEVER ENUMERATED.** An earlier draft of this plan listed four locations; running the
derivation found **five more**, two of them the **highest-traffic homes in the set**. ⚠ **One phrase
is not the derivation either** — `REQUIREMENTS.md:208` matches only the second phrase, and
`ROADMAP.md:421` spells it `eight` where every other home spells it `8`.

**Run BOTH, from the repo root, and treat the union as the subject set:**

    grep -rn "unblocks the other" --include="*.md" .planning CLAUDE.md | grep -v "phases/245-"
    grep -rn "Azure app registration" --include="*.md" .planning CLAUDE.md | grep -v "phases/245-"

**Measured at planning time — the LIVE REGISTERS, all of which must be corrected:**

| file:line | what it is | why it must be corrected |
|---|---|---|
| `.planning/ROADMAP.md:110` | the milestone **credential-blocked routing table** | ⭐⭐ **highest traffic in the set** — this is what an orchestrator reads to decide whether 245 is blocked |
| `.planning/STATE.md:321` | *"Two phases can be blocked on something that is not engineering"* | ⭐⭐ **the other routing table**, same reason |
| `.planning/ROADMAP.md:121` | the phase summary row | read on every roadmap scan |
| `.planning/ROADMAP.md:131` | the milestone checklist line | ditto |
| `.planning/ROADMAP.md:421` | Phase 245 **SC#1** (spells it `eight`) | the criterion itself |
| `.planning/ROADMAP.md:435` | Phase 245 **Flags** | the planning wall |
| `.planning/ROADMAP.md:593` | the credential paragraph | |
| `.planning/REQUIREMENTS.md:161-162` | DEBT-01 | the requirement |
| `.planning/REQUIREMENTS.md:208` | the traceability row (⚠ **second phrase only**) | a corrected requirement with an uncorrected traceability row is a half-corrected register |
| `.planning/STATE.md:396` | item 4's credential-blocked table | |
| `.planning/PROJECT.md:118` and `:886` | the project's own standing statement of the gap | |
| `.planning/MILESTONES.md:28` | **the v4.0 close record** | ⭐ **the strongest case in the set, not the weakest** — the rows were driven **2026-09-07** and v4.0 closed **2026-09-10**, so **the close record was false on the day it was written.** Say that in the verdict. |
| `.planning/milestones/v4.0-ROADMAP.md:14` and `:828` | the archived milestone roadmap + its 238 progress row | the same close record, one register over |
| `~/.claude/projects/C--Vibe-Apps-Agentic-RAG/memory/project_238_built_unreviewed.md` | *"9 UAT rows owed on ONE Azure app registration"* | ⚠ outside the repo; cannot be diffed |

⭐⭐ **AND THE FINDING THAT OUTRANKS ALL OF THEM:** the bottom register **already carries the
correction**. `238-VERIFICATION.md:132` reads *"The operator completed the Azure app registration, so
the rows below stopped being owed"* — **the artifact was right the whole time and every layer above
it was wrong for six days.** That is `feedback_a_review_is_a_claim_about_code_not_the_code` in one
measurement, and it belongs in the verdict.

**DELIBERATE EXCLUSIONS — dated snapshots that were TRUE when written, and are not routing surfaces.**
⛔ They must be **named in the verdict with this reason**, never silently skipped: `238-SUMMARY.md`,
`238-04-SUMMARY.md`, `238-CONTEXT.md`, `240-CONTEXT.md`, `240-VERIFICATION.md:224`,
`v4.0-MILESTONE-AUDIT.md`, `v4.0-STATE-at-close.md`, `SEED-253`, `SEED-260`, and the OneDrive
reported-bug file. ⚠ Correcting a phase's own dated record would erase *when* something was believed,
which is the opposite of D-08's point. ⛔ `238-VERIFICATION.md:132` is excluded because **it is already
correct**.

**The second stale claim, independently measured.** ROADMAP Flags says *"`SEED-177` still reads
`status: planted`"* and **CLAUDE.md line ~41 ends with the same sentence**: *"⚠ `SEED-177` still reads
`status: planted`."* The seed actually reads:

  status: partially-answered  # trigger #2 ANSWERED by Phase 206 (2026-08-25); triggers #1 and #4 have NOT fired

⛔ **The seed is CORRECT and is left alone (D-15).** Only the two registers that describe it wrongly
are touched. ⛔ **No broader trigger sweep happens here** — SEED-177 is high-priority with Open
Platform attached, so ruling on its other triggers is a capability decision, not bookkeeping.

**SEED-256's frontmatter today** — the retirement's seed-side half is a **status flip plus a pointer**,
not new prose, because its four `trigger_when` arms are already written and concrete:

  status: planted
  trigger_when: > (a) a free M365 Developer tenant; (b) a customer/pilot org on M365 — ⭐ the real one,
  because it arrives WITH the admin-consent question attached; (c) any paid M365 Business/Enterprise
  subscription; (d) a phase proposes SharePoint as a source.

**The ground for the retirement, confirmed LIVE rather than assumed:** `check()` returned
`drive_type: personal`. A personal account has no `/sites/` to address, so S-1 and S-2 are blocked on
an **account boundary, not on effort**.

**240's five owed mail rows, verbatim from `240-VERIFICATION.md:206-214`** — copy them, do not
paraphrase:

  M-1  Watch a real Gmail label; a message arrives by itself and reads as a document   (run first — unblocks M-2/M-3)
  M-2  Open a watched message with an attachment; the attachment is its own document, findable from both ends
  M-3  Search the Library for a phrase from message 1 of a long thread; ONE result, not fourteen
  M-4  Disable the Google connection; the mail watch stops AND browse/preview refuse   ⚠ disables a live integration
  M-5  Delete a message at the source; the document is NOT deleted and the source state says what happened   ⚠ destructive at source

  §2: **No mail watch has ever run.** `M-4` and `M-5` settle SC#4.

**238's footer as it reads today (`:220`)** — the line the eleven-or-nine statement replaces:

  **9 rows driven (7 full pass, 2 half) · 0 owed · 2 blocked (S-1, S-2) · 0 claimed.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 238's table reaches a terminal state — M-8 flipped, S-1/S-2 retired across three registers in ONE commit</name>
  <files>.planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md, .planning/seeds/SEED-256-sharepoint-half-of-graph-deferred-no-work-tenant.md, .planning/REQUIREMENTS.md, .planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md</files>
  <read_first>
    - `.planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md` lines **204-228 only** — the eleven rows and the footer
    - `.planning/seeds/SEED-256-sharepoint-half-of-graph-deferred-no-work-tenant.md` — the whole file; it is short and its four triggers are the retirement's substance
    - `.planning/phases/245-.../245-UAT-RESULTS.md` — M-9's driven verdict from `245-02`, so this task records the measured state and not a hoped-for one. ⚠ **OPTIONAL: if the file does not exist, `245-02` did not drive — proceed under DEGRADED MODE and apply (a-bis) below.**
  </read_first>
  <action>
**(a) M-8's headline flips (D-06) — with its half-pass history preserved INLINE.**

M-8 reads `⚠ **HALF PASS — and the failing half is a DEFECT, BUG-260907-03.**` while **its own body
already ends** `✅ **FIXED THE SAME DAY** … driven both directions on the same row … BUG-260907-03
closed.` **The row contradicts itself, and a reader believes whichever half they reach first** — which
is DEBT-03's failure mode mirrored inside a table. SC#1 admits only **pass / blocked / retired**, and
"half" is none of those.

Change the **headline only** to `✅ **PASS.**`, and immediately after it keep the whole existing text
as history, introduced so the flip is auditable rather than silent — e.g.
*"⚠ Read as HALF PASS until 2026-09-13; the original assessment and its fix are preserved verbatim
below, because the row's self-contradiction is the finding."*

⛔ **DO NOT RE-DRIVE M-8.** That means disabling a live integration again for a result already in the
file — the rejected option under D-06. The fix landed at the choke point
(`SourceRegistry.get_adapter` raising `SourceConnectionDisabled`), was driven **both directions on
that row**, and `BUG-260907-03` is closed. Cite that; do not re-measure it.
⛔ **Do not touch M-1…M-7.** They read ✅ PASS and are already terminal.

**(a-bis) M-9 — the ONE case where this plan may touch it, and it exists so SC#1 cannot be left
unmeetable by nobody.** In the normal path `245-02` owns M-9 and this task leaves it alone.

⛔ **BUT if `245-UAT-RESULTS.md` is absent (no drive), OR M-9 still reads `HALF` / `⚠ partial` in
`238-VERIFICATION.md`, this task CONVERTS it** to:

    ⛔ **BLOCKED — <reason>, <blocking id or "no id: not driven">, trigger: <named>.**

with the prior text preserved beside it, exactly as M-8's flip preserves its history. Reason and
trigger come from `245-02`'s SUMMARY if it ran (which door failed, which defect id) or from the
halt cause if it did not (operator unavailable · fixture identity mismatch · a STOP arm fired) —
trigger: *the next phase touching connector import, or the v4.1 close sweep, whichever is first.*

⚠ **Why this clause exists:** `HALF` is **not one of SC#1's three admissible verdicts** (pass ·
blocked-with-reason-and-id · retired-with-trigger). Without it there is an end state where M-9 reads
`HALF`, SC#1 is unmet, and **no plan owns the fix** — which would be this phase failing at exactly
the thing it is correcting in M-8. ⛔ **This clause may never be used to convert a row `245-02`
actually PASSED**; a pass stays a pass.

**(b) S-1 / S-2 retired in writing (D-07), in ONE commit across THREE registers.** This is the
same-commit sync rule already used for `Dockerfile.sandbox` ↔ `docs/SANDBOX-PACKAGES.md` and for the
hot-file ledger. All three edits go in one commit — ⛔ a split commit is drift by construction.

1. **`238-VERIFICATION.md`** — S-1 and S-2 move from `⛔ BLOCKED — SEED-256` to
   **`⛔ RETIRED — SEED-256`**, each naming the ground (**no M365 work/school tenant; `check()`
   returned `drive_type: personal` — an account boundary, not effort**) and pointing at SEED-256's
   four re-open triggers rather than restating them.
2. **`.planning/REQUIREMENTS.md` DEBT-01** — the row records the retirement and points at SEED-256.
3. **`SEED-256`'s frontmatter** — `status: planted` → **`status: deferred`**, with a one-line pointer
   comment naming Phase 245 as where it was retired in writing. ⛔ **A status flip plus a pointer, and
   no new prose** — its four triggers are already written and concrete. ⚠ **`status:` frontmatter IS
   the index**: a body sentence saying "still open" is invisible to every scan, which is exactly why
   the flip is the deliverable.

⛔ **Seed-only was REJECTED and the reason is measured:** the seeds register is swept by **nothing**
(`grep -rln "SEED" .claude/commands/gsd/` returns only `capture.md`, the command that *writes* seeds),
so a retirement held only by a seed is held by nothing.

**(c) The footer, re-derived and disambiguated — resolve the eleven-or-nine ambiguity here, in the
place the number is read from.** `:220` currently reads *"9 rows driven (7 full pass, 2 half) · 0
owed · 2 blocked (S-1, S-2) · 0 claimed."* Replace it with a line that states the count **and what it
counts**, something of the shape:

  **ELEVEN rows: M-1…M-9 (the "nine") plus S-1 and S-2.** 9 driven — <N> pass, <N> partial as of
  2026-09-13 (M-8 flipped ✅ and M-9 driven by Phase 245) · 0 owed · 2 RETIRED (S-1, S-2 — SEED-256).

⚠ **Fill `<N>` from the table as it now stands, do not copy this shape's placeholders**, and ⛔ **do
not round M-9 up if `245-02` left it partial** — record what is there. **STATE.md item 4 reads as
though 9 includes S-1/S-2 and REQUIREMENTS reads as though it does not; both are corrected in Task 2,
and this footer is the anchor they point at.**

**(d) `245-VERDICT.md` §SC#1 — fill the section `245-01` left as an owned placeholder.** Record: the
eleven-or-nine statement; each row's terminal verdict in one table; M-8's flip with its reason; S-1/S-2's
retirement with its ground and its four re-open triggers cited by reference; M-9's driven outcome
pointing at `245-UAT-RESULTS.md`. ⭐ And record the register finding: **all nine M rows were driven on
2026-09-07 while four registers said they were blocked** — that is what SC#1 actually cost to
discover, and it is worth more than the verdicts themselves.
  </action>
  <acceptance_criteria>
    - `grep -c "HALF PASS" .planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md` decreased by exactly the M-8 headline (re-derive the pre-edit count and assert the delta; ⛔ do not hardcode a number — M-9's state depends on `245-02`).
    - M-8's row still contains `BUG-260907-03` and its original assessment text — the history is preserved, not deleted.
    - M-1…M-7 are **byte-unchanged**: `git diff` on that file touches only the M-8 row, the S-1/S-2 rows and the footer line.
    - S-1 and S-2 read `RETIRED` with `SEED-256` and the words `drive_type: personal` (or an equally explicit account-boundary statement) in each.
    - `SEED-256`'s frontmatter reads `status: deferred` and names Phase 245; its four `trigger_when` arms are **unchanged**.
    - REQUIREMENTS' DEBT-01 row names the retirement and points at SEED-256.
    - ⛔ **ONE commit** contains all three register edits: `git show --stat HEAD` lists `238-VERIFICATION.md`, `SEED-256-*.md` and `REQUIREMENTS.md` together.
    - The footer states **eleven** rows and says explicitly what "nine" means; no bare number survives on that line.
    - `245-VERDICT.md` §SC#1 is filled, and its per-row table matches the 238 table row for row (verify by reading both, not by assuming).
    - ⛔ **No row in 238's table reads `HALF` after this task**: `grep -c "HALF" ` on the table region returns `0` except inside preserved-history text explicitly labelled as history. M-9 specifically reads `PASS` (driven, both doors) or `⛔ BLOCKED` with a reason, an id-or-"not driven", and a named trigger.
    - If (a-bis) fired, the SUMMARY states **why** — no drive, or which door failed — and states that a passed row was NOT converted.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=".planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md"; grep -q "RETIRED" "$f" || { echo "S-1/S-2 not retired"; exit 1; }; grep -q "ELEVEN\|eleven" "$f" || { echo "footer does not disambiguate the count"; exit 1; }; grep -q "^status: deferred" .planning/seeds/SEED-256-sharepoint-half-of-graph-deferred-no-work-tenant.md || { echo "SEED-256 status not flipped"; exit 1; }; echo "238 table + SEED-256 OK"'</automated>
  </verify>
  <done>238's table is terminal — no row reads HALF, S-1/S-2 are retired with their ground and triggers, and the retirement moved through all three registers in one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Every live-register home of a false claim, corrected BESIDE — and SEED-177's Flags item refuted</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/PROJECT.md, .planning/MILESTONES.md, .planning/milestones/v4.0-ROADMAP.md, CLAUDE.md, .planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md</files>
  <read_first>
    - ⛔ **FIRST: run the two-phrase derivation in `<interfaces>` and print the union.** Do not read the files one at a time from a list — the list is what was wrong last time.
    - `.planning/ROADMAP.md` → the Phase 245 block (SC#1 and Flags) **and** lines 110, 121, 131, 593
    - `.planning/REQUIREMENTS.md` lines **155-170** and **204-212**
    - `.planning/STATE.md` **items 3 and 4**, and the routing table at ~`:321`
    - `.planning/PROJECT.md` around `:118` and `:886`; `.planning/MILESTONES.md` around `:28`; `.planning/milestones/v4.0-ROADMAP.md` around `:14` and `:828`
    - `CLAUDE.md` — the bullet containing `⚠ \`SEED-177\` still reads \`status: planted\`` (≈ line 41); ⛔ read that bullet, not the whole file
    - `.planning/seeds/SEED-177-mcp-connections-connect-and-be-connected.md` — **frontmatter only**, to quote what it actually says. ⛔ Do not edit this file.
  </read_first>
  <action>
**(a) The Azure claim, in EVERY live-register instance (D-08). ⛔ CORRECT BESIDE, NEVER OVER.** Keep
the original sentence — struck through or explicitly labelled — and put a dated correction next to it.
**The rot being visible IS the finding**, and overwriting erases the evidence that six registers
carried one false claim for six days.

⛔⛔ **DERIVE THE SET FIRST; DO NOT WORK FROM A LIST.** Run **both** greps from `<interfaces>`, write
the union to the scratchpad as `stale-claim-set.txt` (file:line + the matched line), and classify
**every hit** as `CORRECT` or `EXCLUDE:<reason>`. ⚠ **An earlier draft of this plan enumerated four
homes and the derivation found nine-plus, including the two routing tables an orchestrator actually
reads.** *"Guardrails miss what is absent"* — re-derive the whole list, never trust a list.

⛔ **C-5's fence: capture the originals VERBATIM before editing.** For each file in the CORRECT set,
copy its matched line(s) **exactly** into the scratchpad capture. After editing, assert each captured
line still exists in the file as a **contiguous substring**. ⚠ Grepping for *"a distinctive
fragment"* is not enough — a wholesale rewrite that happens to retain the fragment would pass, and
"beside, never over" is precisely the property a fragment-grep cannot see.

Use one consistent correction sentence in all four, of this shape (adjust wording per home, keep the
facts identical):

  ⚠ **CORRECTED 2026-09-13 (Phase 245) — the original above is preserved, not deleted.** All nine of
  238's M rows were **DRIVEN LIVE on 2026-09-07** (`238-VERIFICATION.md:204-220` — 7 full pass, 2
  half at the time; **four defects found by driving and none by the 15-case unit suite**). The
  operator completed the Azure registration *hours after* `238-SUMMARY.md` was written. **The
  blocker was discharged six days before three registers stopped saying so.**

Per-home notes, on top of the derived set:

1. **`ROADMAP.md`** — SC#1 (`:421`, spells it **`eight`**), Flags (`:435`), **the routing table
   (`:110`)**, the summary row (`:121`), the checklist line (`:131`) and the credential paragraph
   (`:593`). ⛔ Do not rewrite the criterion itself — SC#1 is still exactly right; only its blocking
   claim is false.
2. **`REQUIREMENTS.md`** — DEBT-01 (`:161-162`) **and** the traceability row (`:208`, which matches
   only the *second* phrase). ⚠ Both, in the same pass: a corrected requirement with an uncorrected
   traceability row is a half-corrected register, which is this phase's own subject.
3. **`STATE.md`** — **the routing table at `:321`** and item 4's table at `:396`. ⚠ Item 4's 238 row
   also **reads as though "9" includes S-1/S-2**, while REQUIREMENTS reads as though it does not —
   correct both facts in the same edit and point at 238's new footer as the anchor. ⛔ Hand-edit;
   **never a `state.*` SDK verb.** ⛔ Do not touch item 3 (the owed §6.3 reviews) beyond, if needed, a
   pointer to `245-VERDICT.md`; ⛔ do not touch `OV-SOLO-01` or its `OV-SOLO-01-status:` index line —
   both belong to `245-01`.
4. **`PROJECT.md`** (`:118`, `:886`) — the project's own standing statement of the gap.
5. **`MILESTONES.md:28`** — ⭐ **the v4.0 close record, and the strongest case in the set.** Driven
   2026-09-07, milestone closed 2026-09-10: **it was false on the day it was written.** The correction
   must say that, not merely update the fact.
6. **`.planning/milestones/v4.0-ROADMAP.md`** (`:14`, `:828`) — the same close record one register
   over. ⚠ Archived, but a **close record**, not a dated snapshot of a belief — which is exactly the
   line the exclusion list draws.
7. **`~/.claude/projects/C--Vibe-Apps-Agentic-RAG/memory/project_238_built_unreviewed.md`** — the
   *"9 UAT rows owed on ONE Azure app registration"* line. ⚠ **Outside the repository; it will not
   appear in any diff** — edit it by hand and quote the before/after line in the SUMMARY, because
   nothing else can evidence it.

⛔ **Write the EXCLUSION table into `245-VERDICT.md`** — every hit in the derived union that was not
corrected, with its reason (the list in `<interfaces>`), **each as a literal `file:line` token** so
the sweep below can find it. A hit that is neither corrected nor explained is the silent gap this task
exists to close.

**(a-bis) Write the sweep as a scratchpad script, because the criterion must be re-runnable.** Put
`sweep-stale-claim.cjs` in the scratchpad (⛔ **never in the watched tree**) — zero-dependency Node:
walk every `*.md` under `.planning/` plus `CLAUDE.md`, skip `phases/245-`, match
`/unblocks the other/i` **and** `/Azure app registration/i`, and for each hit require **either** a
line containing `CORRECTED 2026-09-13` within **±8 lines**, **or** the literal `file:line` present in
`245-VERDICT.md`. Print every failure as `UNCORRECTED AND UNEXPLAINED: <file>:<line>` and a tail line
`--- N hits · M uncorrected-and-unexplained`. **Exit 0 / 1 / 2.**

⚠ **Measured at planning time: this script reports `32 hits · 32 uncorrected` on the tree as it stands
today** — so the executor has a known-RED starting point and the sweep is itself driven RED before it
is believed, exactly as Task 1 of `245-01` treats its own guard.
⛔ **Do NOT write this as an inline bash one-liner.** The process-substitution form was measured
**hanging past 120 s under Git Bash on this box**; the Node form runs in about a second.
⚠ Export `SCRATCH` to the session scratchpad directory before running the verify command.

**(b) `SEED-177`'s Flags item — REFUTED (D-15).** ROADMAP Flags says *"`SEED-177` still reads
`status: planted` while its retire-the-egress-fence trigger already fired"*. Measured: the seed reads
**`status: partially-answered`** and its own frontmatter already records trigger #2 as *"ANSWERED by
Phase 206 (2026-08-25)"*. **Half the claim is false and half was already handled.**

- **`CLAUDE.md`** — the bullet ending *"⚠ `SEED-177` still reads `status: planted`."* Correct it
  **beside** its original (strike through, keep it), noting: the seed reads `partially-answered`;
  trigger #2 is recorded ANSWERED by Phase 206 in the seed's own frontmatter; **what rotted was the
  prose in TWO registers, not the seed.** ⭐ That is the same shape as the bullet's own existing
  correction two sentences earlier — *"the fence, the seed and this bullet are three registers and only
  one of them was updated"* — so the bullet now demonstrates its own lesson twice.
  ⛔ Keep it to a sentence or two. `node scripts/check-claude-md-size.cjs` must exit `0`.
- **`ROADMAP.md`** — the Flags item, corrected beside.
- **`245-VERDICT.md`** — a short §"SEED-177 — refuted" recording the measurement.
- ⛔ **`SEED-177` itself is NOT edited.** It is correct. ⛔ **No broader trigger sweep** — its
  remaining arms are a capability decision (trigger: `SEED-013` / Open Platform getting a phase
  number), recorded in the verdict's deferred list, not ruled on here.
  </action>
  <acceptance_criteria>
    - ⛔ **EXHAUSTIVE, not per-file.** Re-run **both** derivation greps after editing. For **every** hit outside `phases/245-`, exactly one of these is true and is demonstrated: (a) a line containing `CORRECTED 2026-09-13` appears within ±8 lines of it, or (b) its `file:line` appears in `245-VERDICT.md`'s exclusion table with a reason. ⛔ **Zero hits satisfy neither.** ⚠ A criterion of the form `grep -c "2026-09-13" <file> >= 1` is NOT sufficient — it passes on one correction in a file carrying six instances, which is the exact bug this criterion replaces.
    - The derived union has **≥ 8 hits across ≥ 6 files**, and the scratchpad `stale-claim-set.txt` classifying every hit is quoted (or attached) in the SUMMARY.
    - **C-5's survival fence:** every originally-matched line captured before editing still exists in its file as a **contiguous substring** (assert with a literal `grep -F` per captured line, not with a fragment).
    - For any home where the correction was appended rather than struck through, `git diff --numstat` on that file shows **0 deletions**.
    - ⭐ `MILESTONES.md:28`'s correction states that **the close record was false on the day it was written** (driven 2026-09-07, closed 2026-09-10) — not merely that the fact changed.
    - REQUIREMENTS' DEBT-01 **and** its `:208` traceability row are both corrected — ⛔ not one of the two. `ROADMAP.md`'s `:110` and `STATE.md`'s `:321` routing tables are both corrected — ⛔ these are the two the checker found missing, and they are the highest-traffic homes.
    - STATE.md item 4's 238 row states whether its "9" includes S-1/S-2, and points at 238's footer.
    - The memory file's line is corrected, and the SUMMARY quotes it before and after (it is outside the repo and cannot be diffed).
    - `CLAUDE.md` no longer asserts `SEED-177` reads `planted` as a live claim; the original sentence is preserved struck-through beside the correction; `grep -c "partially-answered" CLAUDE.md` ≥ 1.
    - ⛔ `git diff -- .planning/seeds/SEED-177-mcp-connections-connect-and-be-connected.md` is **EMPTY**.
    - `node scripts/check-claude-md-size.cjs` exits `0` — no size trip, no `[duplicate-row]`, no `[disposition-too-long]`, no `[malformed-row]`.
    - `node scripts/check-verification-honesty.cjs` exits `0` — ⚠ this task does not edit a `*-VERIFICATION.md`, but T1 did, so re-assert the gate is clear before closing the plan.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs && node "$SCRATCH/sweep-stale-claim.cjs" && git diff --quiet -- .planning/seeds/SEED-177-mcp-connections-connect-and-be-connected.md && grep -q "partially-answered" CLAUDE.md && echo "sweep EXHAUSTIVE · CLAUDE.md gate OK · SEED-177 untouched"</automated>
  </verify>
  <done>Four registers carry the false Azure claim beside its dated correction, SEED-177's Flags item is refuted in both of its homes, and the seed itself is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: 240's five mail rows deferred with triggers, and the phase's closing scoreboard</name>
  <files>.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/milestones/v4.0-phases/240-mail-is-a-shape-not-a-fourth-adapter/240-VERIFICATION.md` lines **200-232 only** — the five rows and §2
    - `.planning/phases/245-.../245-UAT-RESULTS.md` — the driven outcomes, so the scoreboard records measurement rather than intention
    - `.planning/ROADMAP.md` → Phase 245 → `## How we'd know this failed` — the five named failure modes this scoreboard must be checkable against
  </read_first>
  <action>
**(a) 240's five mail rows — DEFERRED WITH A TRIGGER EACH (D-13), in `245-VERDICT.md`.**

⛔ They are **out of 245's scope by the ROADMAP's own wording**, and folding five more live rows into a
2-3 plan phase is the consolidation failure mode — **M-4 disables a live integration and M-5 deletes
at the source.** ⛔ **But silence is how they got here.** Write all five verbatim (they are in
`<interfaces>`), each with:

- why it needs a person (from 240's own table),
- a **named trigger**: *the next phase touching mail ingestion, **or** the v4.1 close sweep, whichever
  is first* — ⚠ per-row where a row has a sharper one (M-1 unblocks M-2 and M-3, so its trigger is the
  gate for those two).

⚠ **Record 240's own §2 finding beside them, because it is the reason the deferral matters:**
**no mail watch has ever run in this product.** SC#4 of that phase is inherited by construction, and
`M-4` / `M-5` are what would settle it.

**(b-0) FILL §SC#2 — ⛔ no owned section may survive empty.** `245-01` created `245-VERDICT.md` with
§SC#1 and §SC#2 as owner-named placeholders. T1 filled §SC#1; **this step fills §SC#2**, and nothing
else in the plan set does. Either:
- **the drive ran** → summarise the five 233 rows' verdicts from `245-UAT-RESULTS.md` and point at it
  for the evidence (⛔ summarise, do not duplicate — two homes for one verdict is drift); or
- **the drive did not run** → replace the stub with one line: *"DEBT-02 not driven — see §deferred;
  trigger: <named>"*.

⚠ **An owned-but-empty section is the exact shape this phase is correcting elsewhere** — a heading
that promises a verdict and delivers silence. ⛔ Do not leave the stub.

**(b) The closing scoreboard in `245-VERDICT.md`.** One table, four rows — SC#1, SC#2, SC#3, SC#4 —
each with: its verdict, the plan that discharged it, and **the evidence type** (`driven observation` /
`artifact grep` / `citation` / `written retirement`). ⛔ **No row may cite a test suite as its
evidence.** That is the ROADMAP's first named failure mode for this phase, and a scoreboard that
violated it would be self-refuting.

Then answer the ROADMAP's `## How we'd know this failed` list **explicitly, one line each** — five
items, each stating whether it happened:
1. a row marked done on a passing suite → did it happen?
2. DEBT-03 became a re-review → did it?
3. a file gained "self-verified" while its verdict still reads as though a reviewer signed it → did it?
4. `OV-SOLO-01` recorded without a re-arm trigger → did it?
5. the phase stalled on the Azure registration → did it?

⭐ Item 5 deserves its own sentence: **the registration was never the blocker.** It had been complete
for six days when the phase was planned, and *the phase's real risk was believing its own register.*

**(c) The deferred-and-named list.** Everything this phase touched and did not settle, each with a
trigger, so nothing leaves silently: 237's scope selector + out-of-scope filtering (trigger: the next
classification-rules phase — cross-reference `245-UAT-RESULTS.md`'s D-14 section rather than
duplicating it); `SEED-177`'s remaining arms (trigger: `SEED-013` / Open Platform getting a phase
number); making `OV-SOLO-01`'s re-arm trigger executable (trigger: the v4.1 close); the five open
`BUG-260909-0x` reports reviewed-and-not-folded (plus any of them **observed** during `245-02`'s
drives, quoted from the results file — observation is evidence a report is live); `BUG-260910-02`
(240's, same trigger as its rows); the 13 `SECURITY DEFINER` functions still `anon`-executable
(unrelated, named so it is not lost — ⚠ a role-by-role revoke achieves nothing while the `PUBLIC`
grant stands); **`BUS-171`, the 23-item operator queue, 6 days old and addressed to Claude —
parked is not dropped.**

**(d) REQUIREMENTS + STATE.md record the MEASURED end state.**
- REQUIREMENTS: DEBT-01 / DEBT-02 / DEBT-03 checkboxes and their `:206-210` traceability rows updated
  to what actually happened. ⛔ If `245-02` did not drive, DEBT-02 stays `Pending` **with a named
  trigger and the reason**, and the SUMMARY says so plainly. A ticked box that outran the evidence is
  the failure this whole requirement family exists to prevent.
- STATE.md: the current-position block reflects 245's close and points at `245-VERDICT.md` and
  `245-UAT-RESULTS.md`. ⛔ Hand-edit; **never a `state.*` SDK verb.**

⚠ **`REQUIREMENTS.md` has been stale for four milestones running.** This is the pass that makes it
honest for one; say in the SUMMARY that it was updated **from measurement**, and name the rows.
  </action>
  <acceptance_criteria>
    - All five of 240's mail rows appear verbatim in `245-VERDICT.md`, each with a named trigger, and §2's *"no mail watch has ever run"* is recorded beside them.
    - ⛔ **`245-VERDICT.md` contains NO empty owned section.** §SC#1 and §SC#2 both carry content; neither still reads as the placeholder `245-01` created.
    - The scoreboard has four SC rows, each naming its discharging plan and its evidence type; ⛔ `grep -i "vitest\|pytest\|test suite" ` inside the scoreboard returns nothing as an evidence citation.
    - All five `## How we'd know this failed` items are answered explicitly, one line each, including item 5's finding.
    - The deferred list carries ≥ 8 named items each with a trigger, including `BUS-171`.
    - REQUIREMENTS' three DEBT checkboxes and their three traceability rows match `245-VERDICT.md`'s scoreboard exactly — read both and compare; a mismatch is the register drift this phase exists to end.
    - `git diff --numstat -- .planning/STATE.md` shows the current-position update and nothing that deletes an operator-authored ruling.
    - ⛔ No `state.*` SDK verb in the transcript.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; v=".planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md"; for t in "SC#1" "SC#2" "SC#3" "SC#4" "BUS-171" "no mail watch has ever run"; do grep -qi -- "$t" "$v" || { echo "verdict missing: $t"; exit 1; }; done; echo "verdict scoreboard OK"'</automated>
  </verify>
  <done>Nothing leaves this phase silently: 240's five rows carry triggers, four criteria carry verdicts with non-suite evidence, the five named failure modes are each answered, and REQUIREMENTS/STATE.md record the measured truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan edits planning documents, one seed, CLAUDE.md and one out-of-repo memory file. No source, no schema, no network, no auth decision, no dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-245-03-01 | Repudiation | every live-register home of the stale claim | mitigate | **Correct BESIDE, never over** — the original claim stays readable and the correction is dated, so the six days six registers carried a false claim cannot be quietly erased. ⛔ The set is **DERIVED over two phrases**, never enumerated (an enumeration missed 5 of 9 live instances including both routing tables), and survival is asserted by **literal `grep -F` on each originally-matched line captured before editing** — ⛔ not by a "distinctive fragment", which a wholesale rewrite retaining the fragment would pass. |
| T-245-03-02 | Tampering | `CLAUDE.md` | mitigate | `node scripts/check-claude-md-size.cjs` exits 0 as a hard acceptance criterion — the 150,000-char limit has tripped twice, and a file over it silently stops applying to every agent everywhere. The edit is a sentence beside an existing one, not a new section. |
| T-245-03-03 | Tampering | `.planning/STATE.md` | mitigate | Hand-edited with Edit; ⛔ no `state.*` SDK verb (seven write false records; the file was corrupted five times in Phase 190 alone while reporting success). Acceptance asserts no operator-authored ruling was deleted. |
| T-245-03-04 | Repudiation | `SEED-256` / `SEED-177` | mitigate | 256's `status:` is flipped because **frontmatter IS the index** and body prose is invisible to every scan; 177 is left byte-unchanged and that is asserted by `git diff --quiet`, so a "helpful" edit to a correct seed cannot slip in. |
| T-245-03-05 | Spoofing | `245-VERDICT.md`'s scoreboard | mitigate | Every SC row must name its **evidence type**, and a test suite is forbidden as evidence. A ticked REQUIREMENTS box must match the scoreboard row for row — the drift this phase exists to end cannot be re-created by its own closing commit. |
| T-245-03-SC | Tampering | npm/pip/cargo installs | n/a | **No package is installed.** `git diff -- package.json frontend/package.json backend/requirements.txt` MUST be empty; no legitimacy audit is owed. |
</threat_model>

<verification>
⛔ **Observations and register comparisons, not test runs. No criterion in this plan closes on a
suite.**

**What must be true at the end:**
1. `238-VERIFICATION.md` — no row reads `HALF`; S-1/S-2 read `RETIRED` with their ground; the footer
   states **eleven** rows and defines "nine"; M-1…M-7 byte-unchanged.
2. `SEED-256` reads `status: deferred` with its four triggers unchanged, and the three-register
   retirement landed in **ONE** commit (`git show --stat`).
3. **Every live-register instance** of the Azure claim carries original **and** dated correction —
   proved by the sweep script reporting `0 uncorrected-and-unexplained`, not by a per-file grep; every
   excluded hit is named with a reason in `245-VERDICT.md`. The memory file's before/after is quoted
   in the SUMMARY (it cannot be diffed).
4. `CLAUDE.md` corrected beside; `node scripts/check-claude-md-size.cjs` exit `0`;
   `git diff -- .planning/seeds/SEED-177-*.md` **EMPTY**.
5. `245-VERDICT.md` complete: SC#1-#4 filled, 240's five rows with triggers, the deferred list, and
   the five `How we'd know this failed` items each answered.
6. REQUIREMENTS' three DEBT rows and three traceability rows match the scoreboard exactly.

**Negative fences:**
- `git diff --stat -- backend/ frontend/ supabase/` is **EMPTY**. The backend ceiling (71) and the
  vitest count gate are **not run**, deliberately, with the empty diff as the justification.
- `node scripts/check-verification-honesty.cjs` still exits `0` — ⚠ this plan edits a
  `*-VERIFICATION.md`, so `245-01`'s hook will fire on that write. **If it reds, that is the guard
  working; read its finding.**
- No `state.*` SDK verb anywhere in the transcript.

**⛔ NOT verified here, and stated as owed:**
- The three phases still owe an **independent §6.3 review**. ⛔ This plan does not discharge that and
  must not read as though it does — DEBT-03 was always *"say so in the record"*, never *"do it"*.
- 240's five mail rows are **deferred, not driven**. Named with triggers; not closed.
</verification>

<success_criteria>
- **SC#1 closed:** every one of 238's eleven rows reads pass, blocked-with-reason-and-id, or
  retired-with-a-trigger. No row reads HALF. No row is silently absent.
- S-1/S-2's retirement moved through 245's verdict, REQUIREMENTS and SEED-256 in **one commit**.
- The false Azure claim is corrected **beside** its original in **every live-register instance**,
  derived over two phrases; every uncorrected hit is named in the verdict's exclusion table with its
  reason; the sweep script reports `0 uncorrected-and-unexplained`. The correction names the six-day
  gap, and `MILESTONES.md:28`'s says the close record was **false on the day it was written**.
- `SEED-177`'s Flags item is refuted in both registers that carried it; the seed is untouched;
  CLAUDE.md's size gate passes.
- 240's five mail rows are deferred with a trigger each; the deferred list names ≥ 8 items including
  `BUS-171`.
- The scoreboard answers all four SCs and all five named failure modes, and **cites no test suite as
  evidence for anything**.
- The SUMMARY states plainly which criteria closed, on what evidence, and — if `245-02` could not
  drive — that DEBT-02 remains owed with a named trigger rather than quietly ticked.
</success_criteria>

<output>
Create `.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-03-SUMMARY.md` when done.
</output>
