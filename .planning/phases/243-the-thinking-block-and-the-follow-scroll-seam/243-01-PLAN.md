---
phase: 243-the-thinking-block-and-the-follow-scroll-seam
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CHAT-01, CHAT-04]

must_haves:
  truths:
    - "D-243-16: the thinking block has a safety net BEFORE any code moves — today `grep -rn 'thinking-trigger|thinking-row'` across every suite returns 0 matches, so 'the extraction changed no pixel' is currently an assertion and not a measurement."
    - "D-243-11: where the words are the deliverable the net asserts RENDERED CONTENT — the exact label `Thinking...` (three ASCII dots), the reasoning prose itself, the planning row's copy — never that `data-testid=\"thinking-trigger\"` is present."
    - "D-243-02: the net pins the fold as CLOSED by default at rest AND while streaming, so a later plan cannot auto-expand it without turning this suite red."
    - "D-243-02: the net pins the ABSENCE of a count on the trigger, so inventing one is a build failure rather than a style choice."
    - "The net has been SEEN TO FAIL against two planted defects and the file was restored byte-identical — a fence nobody has watched fire is not a fence."
    - "CHAT-04's defect is pinned as a DECLARED-DEFECT case: today a reasoning-bearing reply with zero tool calls renders no reasoning anywhere. 243-02 inverts that case; it is not left implicit."
    - "D-243-10 / D-243-17: the new suite is registered in BOTH `TARGETS` and `BASELINE` in the SAME commit, and the pinned number is read from the gate's own `— N new` column, never hand-counted."
    - "D-243-18: `count gate OK` is NOT a reachable acceptance criterion at this base — the gate is RED with two inherited failures in `library/__tests__/sketchComposition.test.tsx`. Every gate criterion in this plan is written as a SET DIFF against `243-BASELINE.md`, never as a colour."
    - "D-243-12: solo running. Mechanical evidence — a driven fence, a byte-identical file, a measured count — is not weakened by it; judgement calls are. This plan produces the mechanical kind, and the SUMMARY says which is which."
  artifacts:
    - path: "frontend/src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx"
      provides: "The pre-extraction characterization net for RunCard's three thinking states"
      contains: "Thinking..."
    - path: "scripts/vitest-count-gate.cjs"
      provides: "Both gate knobs carrying the new suite"
      contains: "ThinkingBlock.characterization.test.tsx"
  key_links:
    - from: "frontend/src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx"
      to: "frontend/src/components/chat/RunCard.tsx"
      via: "direct render of the UNMOVED component"
      pattern: "from \"@/components/chat/RunCard\"|from \"\\.\\./RunCard\""
    - from: "scripts/vitest-count-gate.cjs TARGETS"
      to: "scripts/vitest-count-gate.cjs BASELINE"
      via: "the same suite named in both knobs — path-keyed in one, bare-filename-keyed in the other"
      pattern: "ThinkingBlock.characterization.test.tsx"
---

<objective>
Write the safety net for the thinking block **against the unmoved `RunCard`**, before a single line
of it is extracted.

Purpose: `243-PATTERNS.md` §C's headline finding is that **zero tests assert the thinking block**.
`RunCard.characterization.test.tsx`'s eight Phase-227 states do not include reasoning; the only
suite that even mentions `reasoningContent` passes it as an outer-banner input. So 243-02's
extraction would today be verified by the sentence *"it changed no pixel"* and by nothing
executable. Phase 227 already solved this problem on this exact file — write the cases against the
unmoved component, then move the code and require the cases to still pass — and D-243-16 makes that
template this plan's instruction.

Output: one new suite in the 227/228 convention home, registered in both gate knobs, and a record
of it failing against two planted defects and then passing again over a byte-identical file.

⚠ **This plan changes no production code.** `git diff --stat -- frontend/src/components`
must name only the new `__tests__` file at its close.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-CONTEXT.md
@.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-PATTERNS.md
@.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-BASELINE.md
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
<!-- Measured at HEAD on 2026-09-11. The executor should not re-discover these. -->

**The surface under characterization — `RunCard.tsx:478-522`, three states in one ternary chain.**

State 1 (`message.reasoningContent` truthy) — a Radix `Collapsible`, `open={thinkingOpen}`,
default `useState(false)` at `RunCard.tsx:91`. Its trigger is a bare `<button>` (⚠ **no
`type="button"`** — PATTERNS §F.5) carrying `data-testid="thinking-trigger"` and
`aria-expanded={thinkingOpen}`, wrapping the SHARED `FoldTrigger` with
`label={isStreamingNow ? "Thinking..." : "Thinking"}` and **no `count` prop**.
Its body div carries exactly:
`px-3 py-2 text-xs text-muted-foreground whitespace-pre-wrap font-mono leading-relaxed max-h-64 overflow-y-auto border-l-2 border-muted-foreground/20 ml-3`

State 2 (`isStreamingNow && message.isPlanning`, reasoning absent) — `data-testid="thinking-row"`,
`aria-label="Agent is planning the next step"`, an `aria-hidden` `💭`, the copy
`Thinking · planning next step`, and a trailing `{elapsedLabel}` span rendered only when
`hasElapsed`.

State 3 — `null`.

⚠ **`isStreamingNow` is `message.runStatus === "streaming"`** (`RunCard.tsx:72`), NOT the
`isStreaming` prop. A fixture that sets only the prop will characterize the settled arm.

⚠ **The streaming label ships as `"Thinking..."` — three ASCII dots, not `…`** (`RunCard.tsx:496`).
A case pinning `Thinking…` reds against correct code (PATTERNS §F.10).

⚠ **`hasElapsed` is honesty-gated** (`RunCard.tsx:143-198`): a terminal run with no `completedAt`
and no same-session frozen end shows NO duration. A state-2 fixture must set `startedAt` /
`created_at` deliberately if it wants the elapsed segment at all.

**The CHAT-04 defect, measured:** `MessageItem.tsx:359-361` mounts `<RunCard>` only when
`message.tool_calls && message.tool_calls.length > 0`, and `RunCard.tsx:478-502` is the ONLY
renderer of `reasoningContent` in the codebase. ⚠ **D-243-14: the mount is `:359-361`, NOT `:425`** —
`:425` is the `StreamingNarration` branch. Both gate on the same predicate, which is why the wrong
line survived three documents.

**The house test harness** (`RunCard.characterization.test.tsx:55-57`,
`MessageItem.clamp.test.tsx:40-42`, `MessageList.test.tsx:45-47`):
`function renderWithTooltip(ui) { return render(<TooltipProvider>{ui}</TooltipProvider>) }`
No router, no `StreamsProvider`, no store. Every suite carries its OWN local
`makeMessage(overrides: Partial<Message> = {}): Message` — there is no shared fixture module
(`RunCard.characterization.test.tsx:28-53` is the closest one to copy).

**Suite home** (PATTERNS §C): `frontend/src/components/chat/__tests__/` is the 227/228 convention.

**The gate knobs** (PATTERNS §D, measured):
- `BASELINE` — `scripts/vitest-count-gate.cjs:122`, keyed by **bare filename**, must be unique
  tree-wide. Neighbouring chat pins live around `:3163-3176`.
- `TARGETS` — `scripts/vitest-count-gate.cjs:3637`, keyed by **frontend-relative path**.
  Chat entries are FILE-LEVEL by deliberate decision (`:4084`, `:4135-4141`, `:4311`); there is no
  `src/components/chat` directory entry. Neighbours at `:4550-4562`.
- ⛔ A `BASELINE` key naming a file that does not yet exist makes the gate **ERROR (exit 2)**, not
  fail — so pin in the SAME commit that creates the suite, never ahead of it.
- ⛔ The pinned number is READ FROM THE GATE'S OWN `actual` COLUMN on the run that first executes
  the file (printed as `— N new`). Never hand-counted from `it(` literals, never taken from a
  planning document — including this one.

**The RAW chat sweep** (PATTERNS §Shared 5): `components/panel/__tests__/WorkspacePanel.test.tsx:1137-1141`
globs `../../chat/**/*.{ts,tsx}` with `query:"?raw"`, un-stripped **by design**. Forbidden
spellings anywhere in a `chat/` file INCLUDING ITS COMMENTS: `cancelRun(`, `stopThread(`,
`stopStream(`, and `/(?:workflowLock|lock)\s*\??\.\s*runId/`. Phase 194.1 tripped it twice on
docblocks that merely explained the rules; the recorded workaround is to write the name without
its parenthesis.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the characterization net against the UNMOVED RunCard</name>
  <files>frontend/src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx</files>
  <read_first>
    - `frontend/src/components/chat/RunCard.tsx:70-100` and `:143-198` and `:460-525` — the three states, the fold default, and the honesty-gated elapsed derivation.
    - `frontend/src/components/chat/__tests__/RunCard.characterization.test.tsx:1-60` — the Phase 227 template this suite copies, including its docblock, which cites `admin/revertByteIdentical.test.tsx` as ITS precedent.
    - `frontend/src/components/chat/FoldTrigger.tsx:21-48` — the one-component rule and the `count` prohibition in its source form.
    - `frontend/src/components/chat/MessageItem.tsx:319` and `:359-361` — `isMessageStreaming` and the tool-gated RunCard mount, for the CHAT-04 declared-defect case.
    - `.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-PATTERNS.md` §C and §F.
  </read_first>
  <behavior>
    The suite characterizes what ships TODAY. Its docblock states, in its first ten lines, that it
    is a **pre-extraction net written under D-243-16**, that 243-02 moves the code and these cases
    must still pass, and that 243-04 is the ONE plan permitted to change the class-token case and
    the label case — with the sketch named as the reason.

    Required cases, each named so a failure reads as a sentence. ⛔ **Every one asserts rendered
    CONTENT or a class-token set. `expect(getByTestId(...)).toBeInTheDocument()` as the whole
    assertion is forbidden here (D-243-11) — the testids already ship and asserting they exist
    proves nothing about this phase.**

    §1 — the settled reasoning fold
    - a message with `reasoningContent` and `runStatus` unset renders a trigger whose textContent is
      exactly `Thinking` (trim the chevron's own text if any) — a CONTENT equality, not a substring.
    - the fold is CLOSED: the reasoning prose is NOT in the document before any click.
    - after clicking the trigger, the reasoning prose IS in the document, verbatim, including its
      newlines.

    §2 — the streaming label, with its own negative control
    - `runStatus: "streaming"` + `isStreaming` renders the trigger text exactly `Thinking...` —
      three ASCII full stops.
    - a negative control in the same case asserts the rendered text does NOT contain `\u2026`.
      ⭐ This pair is what stops a later editor "tidying" the ellipsis and reds nothing.

    §3 — folded while streaming (D-243-02, operator 2026-09-11)
    - while `runStatus: "streaming"`, with reasoning already accumulating, the reasoning prose is
      NOT in the document until the trigger is clicked. **This case is the mechanical form of
      "auto-expanding during the stream would flip the default in effect — so it does not."**

    §4 — no count, ever (D-243-02)
    - the trigger's textContent matches `/^\s*Thinking(\.\.\.)?\s*$/` after stripping the chevron —
      so a `· 4 steps`, a `· 1,180 chars` or any other invented unit reds here.
    - positive control in the same case: a fixture whose `reasoningContent` itself contains digits
      still produces a digit-free trigger, proving the assertion binds to the trigger and not to the
      page.

    §5 — the body's class tokens, pinned as the thing 243-04 will change
    - the reasoning body element carries ALL of `text-xs`, `font-mono`, `whitespace-pre-wrap`,
      `max-h-64`, `overflow-y-auto`, `border-l-2`, `ml-3`.
    - the case's own comment names 243-04 / D-243-02 as the one authorised change and lists which
      four tokens leave and which two stay. ⚠ **This is the class-string form PATTERNS §C blesses:
      the classes ARE the deliverable of the V1 diff, so pinning them is not a presence assertion.**

    §6 — the planning placeholder (state 2) and its mutual exclusion with state 1
    - `runStatus: "streaming"`, `isPlanning: true`, NO `reasoningContent` renders the copy
      `Thinking · planning next step`.
    - the same fixture WITH `reasoningContent` does NOT render that copy — the two states are
      mutually exclusive by construction, and 243-02 must keep them so.
    - the elapsed segment: one case with an honest start+end shows a duration, one case with a
      terminal run carrying neither `completedAt` nor a same-session stream shows NONE.
      ⭐ That second case is `RunCard.tsx:181-186`'s honesty rule, and D-243-13 applies it to a
      second value in 243-04.

    §7 — state 3
    - no reasoning, not planning: neither `Thinking` as a trigger nor `Thinking · planning next step`
      is anywhere in the rendered output.

    §8 — ⚠ CHAT-04, THE DECLARED DEFECT (this case asserts what is WRONG today)
    - rendered through `MessageItem`, a `role: "assistant"` message with `reasoningContent` set and
      `tool_calls: []` renders **no reasoning anywhere** — no trigger, no prose.
    - the case name and an adjacent comment say IN WORDS that this pins the defect
      (105 of 340 reasoning-bearing rows, 31%, measured — D-243-03) and that **243-02 inverts it**.
      ⛔ It must never be read as desired behaviour by a later reader.
  </behavior>
  <action>
    Create the suite in `frontend/src/components/chat/__tests__/`. Copy the house harness verbatim —
    `renderWithTooltip` over `TooltipProvider`, a LOCAL
    `makeMessage(overrides: Partial<Message> = {}): Message`, no router, no provider, no store.
    Model the fixture factory on `RunCard.characterization.test.tsx:28-53`.

    §1-§7 render `<RunCard>` directly. §8 renders `<MessageItem>`; check that suite's neighbours
    (`MessageItem.clamp.test.tsx`, `MessageItem.harnessBanner.test.tsx`) for the mocks a MessageItem
    mount needs and copy the **spread-the-actual** module-mock form recorded as load-bearing at
    `MessageList.test.tsx:30-40` — a hand-picked factory breaks unrelated describes.

    ⚠ The reasoning fixtures must exercise BOTH ends of D-243-03's 170× spread: a ~198-character
    body (the median) and a body over 30,000 characters (near the measured max of 33,713). Build the
    long one programmatically; do not paste 33 KB into the file.

    ⛔ **Do not touch `RunCard.tsx`, `MessageItem.tsx` or any other production file in this task.**
    If a case cannot be written without a production change, that is a finding to record in the
    SUMMARY, not a change to make here.

    ⚠ This file lives under `chat/` and therefore enters `WorkspacePanel.test.tsx`'s RAW `?raw`
    sweep. Do not spell `cancelRun(`, `stopThread(`, `stopStream(` or `lock.runId` anywhere in it,
    **including its comments** — Phase 194.1 tripped that fence twice on prose alone. Write such a
    name without its parenthesis if it must be discussed at all.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx --maxWorkers=2</automated>
  </verify>
  <acceptance_criteria>
    - The suite exits 0 against UNMODIFIED production code, and `git diff --stat -- frontend/src/components/chat/RunCard.tsx frontend/src/components/chat/MessageItem.tsx` is EMPTY. Quote both in the SUMMARY.
    - Every §1-§8 case listed in `<behavior>` exists, and the SUMMARY lists their names verbatim.
    - `grep -c "toBeInTheDocument()" <suite>` is reported, and every occurrence is paired with a content or class-token assertion in the same case — the SUMMARY names any that is not and justifies it.
    - The trigger-label cases assert `"Thinking..."` with three ASCII dots and carry the `\u2026` negative control.
    - Both reasoning-length fixtures exist; the SUMMARY quotes their measured `.length` values.
    - §8's case name contains the word `defect` (or equivalent) so it cannot be misread, and the SUMMARY quotes the comment naming 243-02 as its inverter.
  </acceptance_criteria>
  <done>The thinking block has an executable description of what it does today, written before anything moved.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the net RED against two planted defects, then register both gate knobs</name>
  <files>frontend/src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs:118-200` — the `BASELINE` contract, the "read the number from the gate's own column" rule, and the exit-2 trap.
    - `scripts/vitest-count-gate.cjs:3140-3180` — the neighbouring chat pins, for placement and comment style.
    - `scripts/vitest-count-gate.cjs:3637-3671` and `:4550-4562` — the `TARGETS` shape and the file-level-not-directory reasoning recorded beside its neighbours.
    - `.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-BASELINE.md` — the two inherited failures, the failing SET, and why `count gate OK` is not reachable.
  </read_first>
  <action>
    **Part A — prove the net can fire.** A fence nobody has watched fail is not a fence.

    Plant 1 — the label. In `RunCard.tsx:496`, change the streaming label from the three-ASCII-dot
    form to the single ellipsis character. Run the suite. Record the failing case name and the
    diff between expected and received, verbatim. Restore.

    Plant 2 — the body. In `RunCard.tsx:501`, delete the `font-mono` token from the body's class
    string. Run the suite. Record the failing case name verbatim. Restore.

    ⛔ **Prove each restoration by HASH, not by a green re-run.** Record the file's md5 before the
    plant and assert `git diff --stat -- frontend/src/components/chat/RunCard.tsx` is EMPTY
    afterwards. A green re-run does not show the planted edit was fully backed out; this project has
    been bitten by exactly that.

    ⚠ If either plant does NOT turn the suite red, the case that should have caught it is vacuous.
    Fix the case, re-plant, and record both readings — the vacuous one beside the working one.

    **Part B — register the suite in BOTH knobs, in this commit.**

    Add the frontend-relative path to `TARGETS` beside its chat neighbours, with a comment naming
    Phase 243 / 243-01 and stating file-level-not-directory in the form its neighbours already use.

    Run the gate once. Read the suite's count from the gate's own `actual` column — it prints as
    `— N new` for a file with no pin. Add that number to `BASELINE` keyed by the BARE filename,
    with a comment stating **what is unguarded without it**: that the thinking block's three states
    have no other test in the tree, and that this pin is what stops 243-02's extraction from
    silently deleting them.

    ⛔ Do not write the number from this plan or from any planning document — read it from the run.
    ⛔ Do not add the `BASELINE` key in a commit where the file does not exist; that makes the gate
    exit 2 rather than fail.

    **Part C — the gate, as a SET DIFF.** Run
    `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the **repo root**. The gate
    is RED at this base and that is inherited — `243-BASELINE.md` names the exact two failures, both
    in `src/components/library/__tests__/sketchComposition.test.tsx`, a suite in `SEED-171` on its
    third recorded reproduction. Compare the failing SET, not a count. ⛔ If a THIRD failure appears,
    capture the filenames from the gate's own persisted JSON BEFORE re-running anything, check each
    against `git diff --numstat 149360176 HEAD`, and do NOT reach for the worker cap — the cap is
    measured not to fix failures of this shape.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx --maxWorkers=2</automated>
  </verify>
  <acceptance_criteria>
    - Both plants are recorded in the SUMMARY with the failing case NAME and the assertion's expected-vs-received text, and both restorations are proven by an EMPTY `git diff --stat` over `RunCard.tsx` plus the pre-plant md5 quoted beside the post-restore one.
    - `grep -c "ThinkingBlock.characterization.test.tsx" scripts/vitest-count-gate.cjs` is exactly 2 — one `TARGETS` entry, one `BASELINE` key — and the SUMMARY quotes both lines.
    - The SUMMARY states the pinned number AND quotes the gate line it was read from, including the `— N new` text.
    - The gate's failing set at this plan's close is **exactly** the two `sketchComposition.test.tsx` cases named in `243-BASELINE.md`, and no others. The SUMMARY names them.
    - No per-file DECREASE on any pinned file; the SUMMARY quotes the gate's verdict line verbatim rather than summarising it.
    - `npx tsc -p tsconfig.app.json --noEmit` in `frontend/` introduces no error that is not in the base 67 — report the SET DIFF, never a count. ⛔ `npx tsc --noEmit` checks ZERO files here and proves nothing.
    - `git diff --stat` at close names ONLY the new suite and `scripts/vitest-count-gate.cjs`.
  </acceptance_criteria>
  <done>The net fails when the surface changes, and the gate both runs it and guards it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → production source | A characterization net that is vacuous certifies a change it cannot see |
| plan → `scripts/vitest-count-gate.cjs` | The shared gate for the whole frontend; a wrong edit reds or silences every other agent's work |
| planted defect → the working tree | Production source is deliberately broken and must be restored byte-identically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-01-01 | Repudiation | The characterization net | mitigate | Two planted defects are driven RED and the failing case names are recorded. A suite that passes against a broken surface is worse than no suite, and this is the only way to know which it is. |
| T-243-01-02 | Tampering | `RunCard.tsx` after the plants | mitigate | md5 recorded before each plant; `git diff --stat` asserted EMPTY afterwards. A green re-run is explicitly NOT accepted as proof of restoration. |
| T-243-01-03 | Denial of service | `scripts/vitest-count-gate.cjs` | mitigate | The `BASELINE` key is added in the same commit as the file it names (an absent path makes the gate exit 2, halting every agent). The `TARGETS` entry is appended, never substituted — `:4722-4726` records that shortening `TARGETS` to fit a shell limit is forbidden. |
| T-243-01-04 | Information disclosure | Test fixtures | accept | Reasoning fixtures are authored or generated in the suite. ⛔ Do NOT paste real `messages.reasoning_content` rows from the operator's local Supabase into a tracked file; the sketch already carries the sanctioned sample. |
| T-243-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** `vitest`, `@testing-library/react` and `TooltipProvider` all ship. Any task that believes it needs a package STOPS and reports — no legitimacy checkpoint is authorised in this plan, and `[ASSUMED]`/`[SUS]` packages would require a blocking human checkpoint that this plan does not contain. |
</threat_model>

<verification>
- The suite passes against unmodified production code and both planted defects were seen to fail it.
- `RunCard.tsx` and `MessageItem.tsx` are byte-identical to the base commit at this plan's close.
- The suite appears exactly twice in `scripts/vitest-count-gate.cjs` — once per knob.
- The gate's failing set is exactly the two inherited `sketchComposition.test.tsx` cases.
- No per-file decrease on any pinned file.
- `tsc -p tsconfig.app.json --noEmit` introduces no new error against the base 67.
- No ledger obligation arises: `scripts/` and `__tests__/` are both EXEMPT from `check-hot-file-ledger.cjs`. Run `node scripts/check-hot-file-ledger.cjs 243` anyway and record the exit code — an unexpected non-zero means the plan's `files_modified` drifted from what was actually touched.
</verification>

<success_criteria>
243-02 can move the thinking block and PROVE it changed no pixel, because there is now something
that fails when it does.
</success_criteria>

<output>
Create `.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-01-SUMMARY.md` when done.
</output>
