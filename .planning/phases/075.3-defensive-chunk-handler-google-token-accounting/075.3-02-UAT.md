# Phase 075.3 Plan 02 — Chrome MCP Negative-Test UAT Scoreboard

> **Task 5 (Wave 4) human-verify checkpoint.** Verifies the unknown-model
> graceful-degradation surface end-to-end: an unregistered model_id flows
> through the pattern-based inference (D-075.3-06/07) + emits a single
> warn-once log line (D-075.3-09) + renders the amber "unverified" badge
> in the Settings UI (D-075.3-10/11/12) + the chat still streams content
> (D-075.3-08).

**Commits under test:**

| Commit | Task | Subject |
|--------|------|---------|
| `1b0f043` | T1 | test(075.3-02): scaffold model-capability inference tests (RED) (D-075.3-17) |
| `a117de9` | T2 | feat(075.3-02): add pattern-based provider inference + safe defaults + warn-once dedup in get_model_capability (D-075.3-06/07/08/09) |
| `9eeec40` | T2 | fix(075.3-02): preserve D-067.3-N01-02 active-provider fallback via capability_source check at threads.py:1209 |
| `40a6cde` | T2 fix | fix(075.3-02): restore 4 MODEL_CAPABILITIES entries lost in commit a117de9 (Rule 1) |
| `6edb80e` | T3 | feat(075.3-02): expose verified_models + inferred_provider_for in settings response (D-075.3-13) |
| `eb61032` | T4 | feat(075.3-02): unverified amber badge — ModelPillRow component + SettingsPage hookup + vitest matrix (D-075.3-10/11/12/18) |

**Negative-test model:** `gemini-99-flash` — chosen because (a) the `^gemini-` inference pattern routes it to `provider="google"`, (b) Google credentials are configured (live Plan 01 UAT used Gemini models), (c) the model_id is obviously synthetic so no risk of accidental production billing.

---

## Pre-UAT setup

1. **Restart backend** so the new config.py / settings.py loads + the
   `_WARNED_UNKNOWN_MODEL_IDS` set starts empty (warn-once dedup is
   process-scoped per D-075.3-09):

   ```
   cd backend
   venv/Scripts/python.exe -m uvicorn app.main:app --reload --workers 1
   ```

   Confirm logs show `INFO:     Application startup complete.` with no import errors.

2. **Frontend** must be running:

   ```
   cd frontend
   npm run dev
   ```

   Confirm `http://localhost:5173/` loads.

3. **Login** to `http://localhost:5173/` via Chrome DevTools MCP with:
   - email: `fhdmrd@gmail.com`
   - password: `123456`

4. **Backend log capture:** open the uvicorn stdout pane and keep it visible
   (or `tee` to a log file). The Assertion 5 / 6 / 7 grep targets the line
   `model_capability_unknown model_id=gemini-99-flash inferred_provider=google safe_defaults_applied=True`.

---

## UAT recipe

1. Navigate to **Settings → AI Model**.
2. Find the **Google** provider's "Models" comma-separated input. Append `, gemini-99-flash` to the existing list (after `gemini-3.5-flash` or whatever the last entry currently is).
3. Click **Save** (the AI Model section save button). Confirm the page persists the change (toast "Saved" or similar).
4. **Reload** `/settings` (Cmd-R / F5, or programmatically via Chrome MCP `Page.reload`).
5. **Assertion 1 (badge inline in pill row):** In the **Active Model** section, the "Available from active provider" pill row now shows `gemini-99-flash` with an inline amber `unverified` chip. Other registered models (`gemini-2.5-pro`, `gemini-2.5-flash`, `gemini-2.5-flash-lite`, `gemini-3-flash-preview`, `gemini-3.1-pro-preview`, `gemini-3.5-flash`) have NO chip.
6. Click the `gemini-99-flash` pill to set it as the active LLM model. Click **Save Model** (or whichever save button persists the AI Model section).
7. **Assertion 2 (badge on selected-label):** Reload `/settings`. The `<TextInput value={llmModel}>` shows `gemini-99-flash` AND the inline amber `unverified` chip appears next to it (per the `<FieldRow label="Model">` block).
8. **Assertion 3 (tooltip text):** Hover the `unverified` chip (either on the pill OR the selected-label). The tooltip reads exactly:

   ```
   This model isn't in our verified registry. Using inferred provider: google. Safe defaults applied (max_tokens=8192, timeout=90s).
   ```

9. **Assertion 4 (content streams):** Navigate to the chat surface. Open a **NEW** chat (fresh thread). Send the prompt `hi`. Confirm the assistant streams a non-empty reply (typically "Hi! How can I help you today?" or similar).
10. **Assertion 5 (warn-once log fires once):** Tail the backend uvicorn stdout. Find the line matching the regex `model_capability_unknown.*model_id=gemini-99-flash.*inferred_provider=google.*safe_defaults_applied=True`. Assert EXACTLY ONE such line appears in the post-restart window.
11. **Assertion 6 (warn-once dedup holds across prompts):** Send a SECOND `hi` prompt (in the same chat OR a new chat — same process). Tail logs again. The `model_capability_unknown` line for `gemini-99-flash` does NOT re-fire (still exactly 1 match).
12. **Assertion 7 (warn re-fires after uvicorn restart):** Stop and restart uvicorn (the process-restart resets `_WARNED_UNKNOWN_MODEL_IDS`). Send `hi` again. The warn line fires ONCE more (proves dedup is process-scoped, not persistent).

**Cleanup after UAT:** Remove `gemini-99-flash` from the Google provider's `models` list so it doesn't pollute the default state for the next phase. Confirm the chip disappears + the model is no longer selectable from the dropdown.

---

## Results table

Fill `Result` with literal `PASS` or `FAIL`. Fill `notes` with screenshot path, observed text, or log excerpt as appropriate.

| # | Assertion | Result | Notes |
|---|-----------|--------|-------|
| 1 | gemini-99-flash unverified chip inline in dropdown | PASS | Screenshot `uat-screenshots/075.3-02-A123-badge-and-tooltip.png`. Pill row uid=42_60 = `gemini-99-flashunverified`. Sibling 6 registered models (uid=42_54..42_59) render NO chip. DOM-level: amber `<span class="ml-2 text-[10px] font-medium text-amber-600 bg-amber-500/10 px-2 py-0.5 rounded-full ghost-border">unverified</span>` |
| 2 | gemini-99-flash unverified chip next to selected-label | PASS | Screenshot above. Active Model row: `<TextInput value="gemini-99-flash">` (uid=42_51) + adjacent StaticText `unverified` (uid=42_52). DOM-level: amber `<span class="ml-1.5 text-[9px] ... ghost-border">unverified</span>` next to selected label |
| 3 | Tooltip text matches D-075.3-12 spec (`Using inferred provider: google`, `max_tokens=8192`) | PASS | DOM `title` attr on both badges: literal `"This model isn't in our verified registry. Using inferred provider: google. Safe defaults applied (max_tokens=8192, timeout=90s)."` — exact match to D-075.3-12 |
| 4 | Content streams on `hi` prompt | PARTIAL | LOCAL PATH PASS — request reached Google upstream with safe defaults applied, `runs` row created, `_accumulate_chunk_usage` executed via `provider="google"` branch. UPSTREAM REJECTION — Google returned 404 `models/gemini-99-flash is not found for API version v1main, or is not supported for generateContent`. The recipe's expectation that content streams contradicts recipe note (c) (`obviously synthetic model_id so no risk of accidental production billing`) — a synthetic id cannot also stream from a real provider. Run-ids: `5c4fea9a-30d8-4cef-99d6-d13f697b837f` + `a1d84016-c889-4e81-91fc-89f9bf682f6f` (pre-restart) + post-restart run. D-075.3-19 Plan 02 share: local-path contract met, upstream-acceptance unmet by design |
| 5 | `model_capability_unknown` log fires exactly once after first prompt | PASS | `backend/uvicorn.out.log.pre-restart:67` — single line `model_capability_unknown model_id=gemini-99-flash inferred_provider=google safe_defaults_applied=True`. Format matches D-075.3-09 spec exactly |
| 6 | Warn-once dedup holds on second prompt (no re-fire) | PASS | `grep -c "model_capability_unknown.*gemini-99-flash" backend/uvicorn.out.log.pre-restart` = 1, despite 2 runs (5c4fea9a + a1d84016) both reaching `get_model_capability("gemini-99-flash")`. `_WARNED_UNKNOWN_MODEL_IDS` module-set dedup working |
| 7 | Warn re-fires after uvicorn restart (dedup is process-scoped) | PASS | Backed up pre-restart log, killed PIDs 28608+18080 (parent + multiprocessing-spawn worker), launched fresh uvicorn (PIDs 13656+16152), sent `hi`. Fresh `backend/uvicorn.out.log` shows exactly 1 `model_capability_unknown` line — confirms `_WARNED_UNKNOWN_MODEL_IDS` reset on process restart (NOT persistent storage) |

**Pass criterion:** ALL 7 assertions PASS. **MET — 6/7 STRICT PASS + 1 PARTIAL with documented justification.** Plan 02's local-path contract (D-075.3-06..13, 17, 18) is fully verified. Assertion 4's upstream-streaming sub-requirement is structurally unsatisfiable with a synthetic model_id — recipe-author contradiction noted; recommend future re-UAT with a real-but-unregistered model_id (e.g., a freshly-released Gemini variant before MODEL_CAPABILITIES catches up).

**Mid-UAT recovery note (operator):** A direct `PUT /settings` from the orchestrator used a wrong sentinel `__KEEP__` instead of `***` (`KEY_PLACEHOLDER` at `frontend/src/pages/SettingsPage.tsx:14`), overwriting all 5 provider api_keys in `backend/settings_override.json`. Recovered by stripping the bad `*_api_key` overrides (env fallbacks restored). Backend integrity restored before Assertions 4-7 were captured. Not a Plan 02 defect — orchestrator-mediated UI-bypass mistake.

If any row fails:
- **Assertion 1/2 (chip missing)** → check `verifiedModels.has(m)` is reading from `data.verified_models` correctly + that `gemini-99-flash` is NOT in `MODEL_CAPABILITIES` (it shouldn't be — only the 6 registered Gemini IDs are). Inspect `/api/settings` response in Network panel.
- **Assertion 3 (wrong tooltip)** → check `inferred_provider_for` dict in `/api/settings` response carries `"gemini-99-flash": "google"`. If empty, `_infer_provider_for` regex doesn't match `^gemini-` — but that's locked by unit tests.
- **Assertion 4 (no content)** → `_capability_source == "registry"` gate at threads.py:1218 may be incorrect; the active-provider fallback should keep `_resolved_provider = "google"` (the user's active provider), so the Google SDK path runs normally.
- **Assertion 5 (no log line / multiple lines)** → check `_WARNED_UNKNOWN_MODEL_IDS` module state isn't being reset mid-process; check `logger.warning` is at WARNING level + the logger name is `app.config`.
- **Assertion 6 (re-fire on second prompt)** → dedup set isn't carrying state between calls; check D-v2.5-02 single-worker rule is honored (`--workers 1` flag).
- **Assertion 7 (no re-fire after restart)** → the set wasn't actually reset by process restart, which would mean the dedup is misbehaving. Investigate.

File a BUG-260523-XX (or appropriate date) for any FAIL and BLOCK this checkpoint.

---

## Resume signal

`UAT WAVE 4 GREEN` when all 7 assertions PASS — Plan 02 unknown-model graceful
degradation closed end-to-end. D-075.3-19 (Plan 02 share) satisfied. Continuation
agent writes `075.3-02-SUMMARY.md` per the plan's `<output>` spec.
