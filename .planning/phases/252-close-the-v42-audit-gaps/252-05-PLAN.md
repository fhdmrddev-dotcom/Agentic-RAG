---
phase: 252-close-the-v42-audit-gaps
plan: 05
type: execute
wave: 2
depends_on: ["252-03", "252-04"]
files_modified:
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CRED-02]
user_setup: []

must_haves:
  truths:
    - "D-39: this plan is the SOLE WRITER of `scripts/vitest-count-gate.cjs` in phase 252. It exists because three wave-1 plans would otherwise contend on one file, and a wave boundary that looks parallel and is not is this project's recorded failure."
    - "D-03: `WatchRowCard.test.tsx` is adopted into BOTH knobs — TARGETS (what RUNS) and BASELINE (what is GUARDED). A suite in only one is invisible to exactly half the gate."
    - "W-7 / D-33: `ConnectionGrantsList.test.tsx` is pinned at 9, not 8 — closing one unit of permanent slack that has stood since Phase 221 and would let a deleted case keep the gate green."
    - "Every per-file count moved by plans 03 and 04 is re-pinned, each with a written attribution. An unexplained +n is the thing to worry about, never a bigger number."
    - "⛔ The failing SET is IDENTICAL to the base's two SEED-171 `sketchComposition` cases. A third failure is this phase's; those two are not. The cap is NOT touched."
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "both knobs updated — every suite this phase created or changed now RUNS and is GUARDED"
      contains: "WatchRowCard.test.tsx"
  key_links:
    - from: "scripts/vitest-count-gate.cjs"
      to: "frontend/src/components/sources/__tests__/WatchRowCard.test.tsx"
      via: "TARGETS entry + BASELINE pin"
      pattern: "WatchRowCard.test.tsx"
---

<objective>
Make every suite this phase created or changed both **run** and **be guarded**, and close a unit of
permanent slack that has stood since Phase 221.

Purpose: ⭐ **The reason this is a plan and not a task is the finding that produced it.**
`WatchRowCard.tsx` shipped B-4's three defects because it had **no test file** and
`src/components/sources` is **not a TARGETS directory entry** — so the gate was structurally unable
to say anything about it at any total. Plans 03 and 04 fix that for their own files; **this plan is
the one place that writes the gate**, so three wave-1 worktrees cannot race on it.

⛔ **This plan changes no product code.** Its whole diff is one file.

⚠ **Read the base reading before doing anything:** the count gate is **already RED** at this phase's
base — `total 8379 · failed 2 · pinned 7572` — and both failures are `sketchComposition.test.tsx`'s
§2 positive controls, **SEED-171's recorded pair on its FOURTH reproduction**, on a byte-unchanged
tree. **`count gate OK` is therefore not a reachable acceptance criterion here**, and a plan that
wrote one would have written a criterion nothing can satisfy.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/252-close-the-v42-audit-gaps/252-CONTEXT.md
@.planning/phases/252-close-the-v42-audit-gaps/252-RESEARCH.md
@.planning/phases/252-close-the-v42-audit-gaps/252-03-SUMMARY.md
@.planning/phases/252-close-the-v42-audit-gaps/252-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-derive the truth before editing the gate that asserts it</name>
  <files></files>
  <read_first>
    - `252-03-SUMMARY.md` and `252-04-SUMMARY.md` — the per-file counts those plans hand over.
      ⛔ **They are inputs to be VERIFIED, not accepted.** Run the gate and read its own `— N new` /
      `actual` columns; where a SUMMARY and the gate disagree, **the gate wins** and the SUMMARY is
      corrected in this plan's own SUMMARY.
    - `252-CONTEXT.md` D-44a — the base reading and the two known-failing cases, verbatim.
    - `CLAUDE.md` § *Parallel execution* — the count-gate trajectory and the cap-triage procedure.
  </read_first>
  <behavior>
    - The gate's own output — not a summary of it — is the authority for every number this plan writes.
  </behavior>
  <action>
    From the **repo root**, on the merged wave-1 tree:
    ```bash
    GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs 2>&1 | tee /tmp/252-gate-pre.txt
    ```
    Record **verbatim**: the verdict line, every row showing a delta or `new`, and the failing set.

    ⛔ **Triage any failure by PROCEDURE, never by touching the cap.** Pull the failing filenames
    from the gate's **own persisted JSON report** (the path it prints) *before* re-running anything,
    and check each against `git diff --numstat <base> HEAD` and `git status --short`:
      - **byte-unchanged and one of SEED-171's five** → record it as *"provably unmodified"*.
        ⛔ Never as *"fine"* — **one green sample of a flaky suite proves nothing**, and this exact
        pair has now reproduced four times.
      - **anything else** → it is this phase's. ⛔ **STOP and report**; do not pin around it.
    ⚠ `GSD_VITEST_MAX_WORKERS` stays at **2**. The cap is measured **not** to fix these failures —
    cap 1 and cap 2 each produced clean runs and red runs on byte-identical trees, and one of
    SEED-171's suites flakes in isolation with nothing else on the box.
    ⚠ Note also that **not every failure in that family is a timeout**: three of the five fail with a
    plain `AssertionError`, so `STACK_TRACE_ERROR` is **not** a reliable tell for *"not a real
    defect"*. Decide by the **diff**, never by the error shape.
  </action>
  <acceptance_criteria>
    - `/tmp/252-gate-pre.txt` exists and its verdict line is quoted verbatim in the SUMMARY.
    - The failing set is listed **in full** — file **and** case name for each, pulled from the JSON
      report, ⛔ not a count and ⛔ not a tail.
    - Each failure is dispositioned against `git diff --numstat`, with the verdict written out.
  </acceptance_criteria>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs 2>&1 | tail -20</automated>
  </verify>
  <done>The numbers this plan is about to pin came from the gate, and every red is attributed before
  a single line is edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Both knobs — adopt, re-pin, and close W-7's slack</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs:140-160` — the BASELINE pin block around `TodosSection` and
      `WorkspacePanel.derived`, and its comment conventions.
    - `:2880-2910` — the `ConnectionGrantsList` pins (**W-7's `8`** at `:2887`, and the
      `.grouping` pin at `:2902`).
    - `:4190-4215` — the TARGETS block for `src/components/panel/__tests__/`, including the
      `:4206-4212` comment recording **why Phase 250 adopted `WorkspacePanel.derived.test.tsx`**.
      That is the precedent this task follows.
    - `:5120-5185` — the TARGETS block for `src/components/sources/`. ⭐ **Ten individually-named
      files, and no directory entry** — the structural fact that made `WatchRowCard.tsx` invisible.
    - `CLAUDE.md` § *hot-file ledger* row for `scripts/vitest-count-gate.cjs` (STALE a 5th time) and
      its section in `docs/HOT-FILE-LEDGER.md`.
  </read_first>
  <behavior>
    - `WatchRowCard.test.tsx` appears in **both** the TARGETS list and the BASELINE pin map.
    - `ConnectionGrantsList.test.tsx` is pinned at **9**.
    - Every per-file count plans 03 and 04 moved is re-pinned to the gate's measured `actual`.
    - The gate's failing set after the edit is **identical** to Task 1's.
  </behavior>
  <action>
    **(a) W-7 first, and drive it RED so the slack is proven to have existed.**
    Before editing: `grep -c "^\s*it(\|^\s*test(" frontend/src/components/settings/__tests__/ConnectionGrantsList.test.tsx`
    → expect **9** against the pin of **8**. **Record both.** ⭐ Then demonstrate the slack: delete
    one case locally, re-run the gate for that file, observe it **still passes** at 8, restore the
    file, and prove it **md5-identical**. ⛔ Record the md5 before and after — a slack nobody has
    seen exploited is an assertion, and this plan is about not making those.
    Then change `:2887` from `8` to `9`, with a comment naming **W-7**, that the slack stood since
    Phase 221, and what the RED drive showed.

    **(b) Adopt `WatchRowCard.test.tsx` into BOTH knobs.**
      - TARGETS: add `"src/components/sources/__tests__/WatchRowCard.test.tsx"` to the
        `src/components/sources/` block, in the file's existing ordering convention.
      - BASELINE: add `"WatchRowCard.test.tsx": <N>` using the gate's measured count.
    Add a comment in the house voice recording **the measured reason**: this component carried three
    shipped defects (`WATCH-04`'s completion claim, the `(0 changes)` literal, and the
    refusal/success contradiction) and had **no test file at all**, while `src/components/sources` is
    a set of ten named files rather than a directory entry — so **TARGETS decides what RUNS,
    BASELINE decides what is GUARDED, and this file was on the wrong side of both.**
    ⛔ **Do NOT add a `src/components/sources` directory entry.** It would adopt ~10 unpinned suites
    in one edit and move the shared gate for reasons unrelated to this phase. It is recorded as a
    deferred idea in `252-CONTEXT.md` and gets a seed at close.

    **(c) Re-pin everything the wave moved.** For each file whose count changed — at minimum
    `sourceHealthVocabulary.test.ts`, `TodosSection.test.tsx`, `PhaseCard.test.tsx`,
    `WorkspacePanel.derived.test.tsx` — set the pin to the gate's `actual` and write **one line of
    attribution beside it**: which plan, which cases, why. ⛔ **A count changed without an
    attribution is the drift this gate exists to catch**; leaving one unexplained turns a working
    gate into a rubber stamp.
    ⚠ Where a SUMMARY's figure and the gate's `actual` disagree, **the gate wins** — and say so.

    **(d) Re-run and compare SETS.**
    ```bash
    GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs 2>&1 | tee /tmp/252-gate-post.txt
    diff <(grep -oE "^\s+[A-Za-z0-9_.-]+\.test\.tsx?" /tmp/252-gate-pre.txt | sort -u) \
         <(grep -oE "^\s+[A-Za-z0-9_.-]+\.test\.tsx?" /tmp/252-gate-post.txt | sort -u)
    ```
    The only acceptable residual failure set is Task 1's two `sketchComposition` cases.
  </action>
  <acceptance_criteria>
    - `grep -c "WatchRowCard.test.tsx" scripts/vitest-count-gate.cjs` is **2** — one per knob.
      ⛔ A `1` means it is in exactly one, which is the defect this plan exists to close.
    - `grep -n "ConnectionGrantsList.test.tsx" scripts/vitest-count-gate.cjs` shows the pin at **9**,
      and the W-7 RED drive (delete → still passes at 8 → restore → md5-identical) is recorded with
      both md5 values.
    - The post-edit verdict line is quoted verbatim, and `failed` is **2**.
    - The failing SET is **identical** to Task 1's — file and case name for each, compared as SETS.
      ⛔ Not as counts.
    - Every changed pin has a one-line attribution naming its plan and its cases.
    - `git diff --stat` names **exactly one file**: `scripts/vitest-count-gate.cjs`. ⛔ Any product
      file in this plan's diff is a scope breach.
    - `GSD_VITEST_MAX_WORKERS` was `2` on every invocation — state it.
  </acceptance_criteria>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs 2>&1 | tail -20; git diff --stat</automated>
  </verify>
  <done>Every suite this phase touched both runs and is guarded, W-7's slack is closed after being
  seen, and the only red is the inherited pair that was red before the phase began.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: The ledger row and the seeds re-sweep</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `CLAUDE.md` § *Hot-file ledger* — the same-commit sync rule and the re-derivation recipe.
    - `docs/HOT-FILE-LEDGER.md` § `scripts/vitest-count-gate.cjs`.
  </read_first>
  <behavior>
    - The ledger row for `scripts/vitest-count-gate.cjs` carries a re-derived triple and this
      phase's verdict, and its section in `docs/HOT-FILE-LEDGER.md` is updated **in the same
      commit**.
    - The REG-02 seeds sweep is re-run now that `files_modified` exists across five PLAN.md files.
  </behavior>
  <action>
    ⚠ **Re-derive the triple; do not copy it forward.** CLAUDE.md records this row as **STALE a 5th
    time**, and this ledger's own repeated finding is that a figure written at a phase's close goes
    stale on the next commit that touches the file.
    ```bash
    git log --oneline -- scripts/vitest-count-gate.cjs | wc -l
    git log --format=%s -- scripts/vitest-count-gate.cjs | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' \
      | sed -E 's/-.*//' | grep -E '^[0-9]+(\.[0-9]+)?$' | sort -u | wc -l
    wc -l scripts/vitest-count-gate.cjs
    ```
    ⚠ **Six-digit buckets (`260814`, `260809`, …) are DATED QUICK TASKS, not phases — subtract
    them.** Only the phase count feeds G-5.

    Update the CLAUDE.md row (⛔ **disposition cell ≤ 200 chars** — the gate fails above it) and
    write the reasons into its `docs/HOT-FILE-LEDGER.md` section **in the same commit**. Then:
    ```bash
    node scripts/check-claude-md-size.cjs
    node scripts/check-hot-file-ledger.cjs 252
    node scripts/check-seeds-register.cjs --phase 252
    ```
    ⭐ The seeds sweep now has a real blast radius to match against — at scoping it printed
    `0 seeds matched` **while also printing that the phase declared no surfaces**, which was an
    honest non-result rather than a clean sweep (D-38). **Record its output either way**, and if a
    trigger fires, write the routing back into the seed's frontmatter: *a seed is answered by
    editing the seed*, and `status:` frontmatter IS the index.
  </action>
  <acceptance_criteria>
    - `node scripts/check-claude-md-size.cjs` → exit 0, with no `[disposition-too-long]`,
      `[duplicate-row]` or `[malformed-row]`.
    - `node scripts/check-hot-file-ledger.cjs 252` → `ledger gate OK` over all five plans'
      `files_modified`.
    - `node scripts/check-seeds-register.cjs --phase 252` output recorded in full, with the matched
      path **and glob** for each hit, and each hit's routing written into the seed.
    - The re-derived triple and the subtracted date buckets are both shown in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs; node scripts/check-hot-file-ledger.cjs 252; node scripts/check-seeds-register.cjs --phase 252</automated>
  </verify>
  <done>The ledger row is re-derived rather than inherited, its narrative moved in the same commit,
  and the seeds register has been swept against a blast radius that actually exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan edits one build-time script and two planning documents; nothing it touches runs in
production or handles user data. ⛔ One integrity obligation:

| Concern | Obligation |
|---|---|
| a gate that cannot fail | Pinning **around** a red, or loosening a pin to absorb one, produces a green gate over a real defect — the failure mode `CLAUDE.md` names as *"pinning a red suite with an allowance makes a gate that cannot fail."* ⛔ Every pin here moves to a **measured** `actual`, and any failure outside the inherited pair **stops the plan**. |

## STRIDE Threat Register

| ID | Threat | Mitigation | Verified by |
|---|---|---|---|
| TM-252-21 | **Tampering (of the gate itself)** — a suite adopted into one knob only looks adopted and guards nothing. | Both knobs asserted by grep count of **2**. | Task 2 acceptance criterion 1. |
| TM-252-22 | **Repudiation** — a pin raised to absorb a genuine regression hides it permanently. | Every pin is set to the gate's measured `actual`; a red outside the inherited pair stops the plan. | Task 1 triage + Task 2 SET comparison. |
| TM-252-23 | **Vacuous slack** — a pin below the real case count lets a deleted test keep the gate green (W-7, standing since Phase 221). | RED-driven: delete a case, observe the pass, restore, prove md5-identical. | Task 2 acceptance criterion 2. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root → `failed 2`, the set identical to the base pair.
- `node scripts/check-hot-file-ledger.cjs 252` → `ledger gate OK`.
- `node scripts/check-claude-md-size.cjs` → exit 0.
- `node scripts/check-seeds-register.cjs --phase 252` → output recorded.
- `git diff --stat` for Task 2 → exactly one file.
</verification>

<success_criteria>
- Every suite this phase created or changed is in **both** knobs, with an attributed pin.
- W-7's slack is closed, after being **seen** to exist.
- The failing set is unchanged from the base — this phase added no red.
</success_criteria>

<output>
`252-05-SUMMARY.md` — carrying the verbatim pre and post verdict lines, the full failing SET both
times with each case dispositioned against the diff, the W-7 md5 drive, the per-pin attributions,
the re-derived ledger triple with date buckets subtracted, and the seeds sweep output.
</output>
