---
phase: 252-close-the-v42-audit-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/full-schema-supplement.sql
  - supabase/full-schema.sql
  - scripts/check-schema-acl-parity.cjs
  - backend/app/api/setup.py
  - backend/tests/unit/test_252_setup_refusal.py
  - .planning/reported-bugs/grant-override-marker-claims-a-person-changed-it.md
  - .planning/reported-bugs/BUG-260828-11-grant-override-marker-claims-a-person-changed-it.md
  - .planning/reported-bugs/BUG-260911-01-app-settings-writable-by-anon-in-production.md
  - .planning/reported-bugs/search-tab-cannot-save-because-a-stored-value-is-out-of-its-own-bound.md
  - .planning/reported-bugs/google-drive-adapter-never-writes-source-path.md
  - .planning/reported-bugs/BUG-260907-02-custom-client-id-accepts-a-secret-into-a-readable-column.md
  - .planning/reported-bugs/a-newly-added-model-appears-only-if-the-request-lands-on-the-worker-that-wrote-it.md
autonomous: true
requirements: [CRED-03, CRED-04, MODEL-08]
user_setup: []

must_haves:
  truths:
    - "SC#1 / D-05: `scripts/full-schema-supplement.sql` gains a section 6 carrying migration 181's ACL block for all 13 SECURITY DEFINER functions — 31 REVOKEs and 17 GRANTs, signatures byte-identical to the migration, PUBLIC revoked before anon."
    - "SC#1 / D-06: `supabase/full-schema.sql` is updated in the SAME COMMIT, and `diff <(tail -n $(wc -l < scripts/full-schema-supplement.sql) supabase/full-schema.sql) scripts/full-schema-supplement.sql` is EMPTY after the edit — the equivalence is PRINTED in the SUMMARY, not asserted."
    - "D-07: `scripts/check-schema-acl-parity.cjs` exits 0 clear / 1 violation / 2 harness error, derives its scan set from `readdirSync` over `supabase/migrations/`, and FAILS naming any function whose migration ACL is absent from the supplement."
    - "D-07 RED arm 1: with one mirrored function deleted from the supplement, the gate exits 1 and NAMES that function's full signature."
    - "D-07 RED arm 2 (the counterfactual): a function present in BOTH is ABSENT from the failure output — the gate does not print everything."
    - "D-07 count assertion: the gate prints `migrations scanned: N` derived at run time and exits 2 (never 0) over a collapsed or empty scan set — the Phase-242 vacuous-pass defence."
    - "W-4 / D-31: `POST /setup/provider-key` answers 400 with the refusal detail when `save_app_settings` raises `SettingsWriteRefused`; the pre-existing 500 arm for a `False` return is BYTE-UNCHANGED."
    - "D-36: `BUG-260828-02` names exactly ONE report after this plan. The CLOSED one (`…no-authoring-surface…`, `verified_closed_by: 214.1`) keeps the id; the open grant-override report becomes `BUG-260828-11` in its `id:` frontmatter AND its filename AND every in-repo citation."
    - "D-35: each of the six stale-open reports carries a status that matches reality — `closed` ONLY where a named artifact verifiably closes it, otherwise `folded_into` plus a written reason. A report is never flipped to `closed` without a verifier."
  artifacts:
    - path: "scripts/check-schema-acl-parity.cjs"
      provides: "the durable guard for the third recurrence of the 118/181 ACL-mirror class"
      contains: "MIN_MIGRATION_FILES"
    - path: "scripts/full-schema-supplement.sql"
      provides: "section 6 — migration 181's function EXECUTE ACLs, the only artifact that can carry a grant past --no-privileges"
      contains: "REVOKE EXECUTE ON FUNCTION public.current_user_org_ids() FROM PUBLIC;"
    - path: "backend/tests/unit/test_252_setup_refusal.py"
      provides: "the W-4 RED drive — a refused provider-key write answers 400, not an opaque 500"
  key_links:
    - from: "scripts/check-schema-acl-parity.cjs"
      to: "supabase/migrations/"
      via: "fs.readdirSync + per-file read"
      pattern: "readdirSync"
    - from: "scripts/check-schema-acl-parity.cjs"
      to: "scripts/full-schema-supplement.sql"
      via: "normalised signature set comparison"
      pattern: "full-schema-supplement"
    - from: "backend/app/api/setup.py"
      to: "backend/app/models/user_settings.py"
      via: "except SettingsWriteRefused -> HTTP 400"
      pattern: "SettingsWriteRefused"
---

<objective>
Make migration 181 reachable by a greenfield deploy, guard that reachability with something
executable, answer a refused provider-key write honestly, and sweep the reported-bugs register.

Purpose: **SC#1.** A project bootstrapped from `supabase/full-schema.sql` today ships all 13
SECURITY DEFINER functions with the default `PUBLIC` EXECUTE grant — `anon`-executable over
PostgREST — which is the exact advisor finding `CRED-03` exists to close, and would make `CRED-04`
read 13/13 against that deployment. **The migration is right and unreachable.**

⭐ This is the **third** recurrence of one class: `supabase/full-schema.sql:7449-7456` documents it
verbatim for migration 118, and the supplement's own header already carries a prose instruction to
mirror ACLs that nobody followed for 181. **So the mirror is half the plan and the gate is the other
half** — a fourth prose note would be this recurrence's only contribution to a fourth.

Output: supplement §6 + a regenerated-equivalent `full-schema.sql`, `check-schema-acl-parity.cjs`
driven RED on both arms, a 400 on `POST /setup/provider-key`, and a register with one
`BUG-260828-02`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/252-close-the-v42-audit-gaps/252-CONTEXT.md
@.planning/phases/252-close-the-v42-audit-gaps/252-RESEARCH.md
</context>

<interfaces>
<!-- The contract this plan publishes. Nothing else in the phase consumes it, but the NEXT
     migration that narrows a privilege does — so it is stated, not left implicit. -->

`scripts/check-schema-acl-parity.cjs`
  - `node scripts/check-schema-acl-parity.cjs`           → scan mode, exit 0 / 1 / 2
  - `node scripts/check-schema-acl-parity.cjs --self-test` → drives both RED arms against a temp
    fixture and exits 0 only if BOTH fire correctly

  Prints, before any verdict:
    `schema ACL parity — migrations scanned: <N> · function ACLs found: <M> · mirrored: <K>`

  Failure codes, one per line, each naming the full function signature:
    `[not-mirrored]  public.foo(uuid, text)  — migrations/181_…sql grants/revokes it; the supplement does not`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror migration 181 into the supplement, and PROVE full-schema.sql still equals its tail</name>
  <files>scripts/full-schema-supplement.sql, supabase/full-schema.sql</files>
  <read_first>
    - `252-RESEARCH.md` §1 — the four measurements, the 13 signatures **verbatim**, the
      PUBLIC-before-anon ordering fact, and §1.4's carry / do-not-carry list.
    - `supabase/migrations/181_revoke_public_secdef_functions.sql` lines 45-125 — the source block.
      ⛔ **Copy the signatures from here, do not retype them.** `REVOKE … ON FUNCTION f(uuid, text)`
      does not affect `f(uuid)`; an argument list is part of the identity.
    - `scripts/full-schema-supplement.sql` lines 190-259 (§5, the mig-118 precedent) — the header
      framing, the idempotency statement and the ordering note to mirror.
    - `scripts/full-schema-supplement.sql` lines 1-33 — the file header's MAINTENANCE block, which
      must gain 181 beside its existing `ACLs -> migration 118` line.
    - `supabase/full-schema.sql` lines 7449-7456 — **this exact failure class, written down for
      migration 118.** Read it; it is the argument for Task 3.
  </read_first>
  <behavior>
    - The supplement gains a `-- 6. Function EXECUTE privileges (migration 181 / Phase 248, CRED-03)`
      section after §5, carrying **31 REVOKE** and **17 GRANT** statements over **13** functions.
    - Every `REVOKE … FROM PUBLIC` for a function precedes that function's `REVOKE … FROM anon`.
    - The block is idempotent — REVOKE and GRANT both are — so re-pasting the supplement onto an
      already-provisioned DB is safe, exactly as every other section claims of itself.
    - `supabase/full-schema.sql` ends with the supplement, byte-identically, after the edit as
      before it.
  </behavior>
  <action>
    **Step 1 — record the BEFORE reading, so the AFTER means something.**
    ```bash
    SUP=$(wc -l < scripts/full-schema-supplement.sql)
    diff <(tail -n "$SUP" supabase/full-schema.sql) scripts/full-schema-supplement.sql && echo "BEFORE: tail == supplement"
    grep -c "REVOKE.*EXECUTE ON FUNCTION" supabase/full-schema.sql scripts/full-schema-supplement.sql
    ```
    Expect `BEFORE: tail == supplement` and `0` / `0`. **Put both readings in the SUMMARY.**
    ⛔ If the diff is non-empty, STOP and report — the D-06 licence does not hold and the edit must
    not be made.

    **Step 2 — append §6 to `scripts/full-schema-supplement.sql`.**

    Header, in §5's voice and stating this section's own measured reason:

    ```sql
    -- ============================================================
    -- 6. Function EXECUTE privileges (migration 181 / Phase 248, CRED-03)
    -- ============================================================
    -- ⚠ WHY THIS LIVES HERE RATHER THAN IN THE DUMP: regenerate-full-schema.sh runs
    --    `pg_dump --no-privileges` (scripts/regenerate-full-schema.sh:104), so a dump
    --    can NEVER carry a function grant. Migration 181 is therefore INVISIBLE to the
    --    generated schema: its 13 functions survive, its 31 REVOKEs do not. A greenfield
    --    project bootstrapped from full-schema.sql alone ships every one of them with the
    --    default PUBLIC EXECUTE grant — anon-executable over PostgREST — which is exactly
    --    the advisor finding 181 exists to close.
    --
    -- ⭐ THIRD RECURRENCE OF ONE CLASS. full-schema.sql:7449-7456 documents this failure
    --    verbatim for migration 118; §5 above is that fix; and the MAINTENANCE note at the
    --    top of this file already told maintainers to mirror ACLs. 181 reproduced it anyway.
    --    ⛔ So this section is guarded by `scripts/check-schema-acl-parity.cjs`, which FAILS
    --    when a migration grants or revokes EXECUTE on a function this file does not mirror.
    --    A prose instruction is what did not work twice; do not add a fourth.
    --
    -- ⚠ ORDER IS LOAD-BEARING: `anon` INHERITS from PUBLIC, so `REVOKE … FROM anon` changes
    --    nothing while the PUBLIC grant stands (measured in migration 177). PUBLIC is
    --    revoked first, every time.
    --
    -- ⚠ TRIGGER FUNCTIONS ARE SAFE TO REVOKE. PostgreSQL checks EXECUTE at CREATE TRIGGER
    --    time, not at trigger-execution time (empirically verified against local Postgres,
    --    BUS-235 / TM-248-03). Revoking does NOT break INSERT/UPDATE or signup; it blocks
    --    direct invocation via /rest/v1/rpc/<trigger_fn>.
    --
    -- Idempotent, like everything else in this file: REVOKE and GRANT are.
    -- Runs at the END of full-schema.sql, so every function exists and Supabase's stock
    -- default privileges have already been applied.
    ```

    Then the three groups, **in migration 181's own order and with its own group comments**, copied
    from `181_revoke_public_secdef_functions.sql:45-125`:
      - Group A trigger functions (4): `capture_skill_version()`, `handle_new_user()`,
        `stale_skill_embedding()`, `stale_skill_embedding_from_case()` — revoke PUBLIC, anon,
        authenticated; grant service_role.
      - Group B trigger functions (2): `autofill_org_id_by_owner()`, `autofill_org_id_from_parent()`
        — revoke anon, authenticated; grant service_role.
      - Group C RLS helpers + app RPCs (7): `current_user_org_ids()`,
        `connection_doc_is_visible(uuid, text)`, `current_user_has_permission(uuid, text)`,
        `folder_is_org_shared(uuid)`,
        `keyword_search_chunks(text, uuid, integer, jsonb, uuid[])`,
        `match_document_chunks(vector, uuid, integer, double precision, jsonb, uuid[], text)`,
        `match_skills(vector, uuid, text)` — revoke PUBLIC and anon; grant authenticated and
        service_role.

    ⛔ **No `BEGIN;`/`COMMIT;`.** The migration wraps itself; the supplement is pasted inside
    whatever transaction the operator is running and no other section opens one.

    **Step 3 — update the file header's MAINTENANCE block.** Its `ACLs -> migration 118 (…)` line
    gains 181: *"181 (function EXECUTE on 13 SECURITY DEFINER functions, CRED-03 — guarded by
    scripts/check-schema-acl-parity.cjs)"*. Same edit, same commit.

    **Step 4 — apply the IDENTICAL text to `supabase/full-schema.sql`.**
    ⛔ `regenerate-full-schema.sh` cannot run here — **Docker is denied in this environment** — and
    migration 181 changes **no schema object**, only ACLs, so the `pg_dump` half of the artifact is
    unchanged by construction. The supplement is appended **verbatim** as the file's tail (proven in
    Step 1), so appending the same text to `full-schema.sql`'s tail reproduces exactly what the
    script would emit. **This is the one sanctioned exception to *never hand-edit `full-schema.sql`*,
    and it is sanctioned only because the result is mechanically checkable.**

    The safe mechanical way — do it this way rather than by editing the file by hand:
    ```bash
    OLD=$(git show HEAD:scripts/full-schema-supplement.sql | wc -l)
    head -n "-$OLD" supabase/full-schema.sql > /tmp/fs-head.sql
    cat /tmp/fs-head.sql scripts/full-schema-supplement.sql > supabase/full-schema.sql
    ```
    (`$OLD` is the supplement's line count **before** this task's edit, taken from git so a partial
    edit cannot corrupt it.)

    **Step 5 — print the proof.**
    ```bash
    SUP=$(wc -l < scripts/full-schema-supplement.sql)
    diff <(tail -n "$SUP" supabase/full-schema.sql) scripts/full-schema-supplement.sql && echo "AFTER: tail == supplement"
    grep -c "REVOKE EXECUTE ON FUNCTION" supabase/full-schema.sql scripts/full-schema-supplement.sql
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "REVOKE EXECUTE ON FUNCTION" scripts/full-schema-supplement.sql` is **31**, and the
      same grep over `supabase/full-schema.sql` is also **31**.
    - `grep -c "GRANT EXECUTE ON FUNCTION" scripts/full-schema-supplement.sql` is **17**.
    - `grep -o "ON FUNCTION public\.[a-z_]*" scripts/full-schema-supplement.sql | sort -u | wc -l`
      is **13**.
    - ⭐ **Signature parity, machine-checked rather than eyeballed:** the sorted set of
      `ON FUNCTION …` signatures in the supplement is **identical** to the sorted set in
      `supabase/migrations/181_revoke_public_secdef_functions.sql` — `diff` of the two sorted
      `grep -o` outputs is empty. Record the command and its empty output in the SUMMARY.
    - For every one of the 13, the line number of its `FROM PUBLIC` revoke (where one exists) is
      **less than** the line number of its `FROM anon` revoke. Verify with a one-off `awk`/`node`
      expression and record it.
    - `diff <(tail -n $(wc -l < scripts/full-schema-supplement.sql) supabase/full-schema.sql) scripts/full-schema-supplement.sql`
      is **empty**, printed verbatim in the SUMMARY. ⛔ Not paraphrased as "verified".
    - `grep -c "BEGIN;" scripts/full-schema-supplement.sql` is **0**.
    - `git diff --stat supabase/full-schema.sql` shows **only additions** at the file's tail — no
      line above the old supplement boundary is touched.
  </acceptance_criteria>
  <verify>
    <automated>SUP=$(wc -l < scripts/full-schema-supplement.sql); diff <(tail -n "$SUP" supabase/full-schema.sql) scripts/full-schema-supplement.sql && echo "PARITY OK"; grep -c "REVOKE EXECUTE ON FUNCTION" scripts/full-schema-supplement.sql supabase/full-schema.sql</automated>
  </verify>
  <done>Migration 181's ACLs are in both bootstrap artifacts, the signature set matches the
  migration exactly, and `full-schema.sql` is still byte-identical to the supplement at its tail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The parity gate — driven RED on both arms before it is believed</name>
  <files>scripts/check-schema-acl-parity.cjs</files>
  <read_first>
    - `scripts/check-hot-file-ledger.cjs` lines 1-70 and 130-195 — the house header block
      (`WHY THIS EXISTS` / `USAGE` / `EXIT 0·1·2`), `fail()`, root resolution, the derivation print
      style, and the `try { process.exit(main()) } catch { fail() }` entry point. **This is the
      primary analog.**
    - `scripts/check-seeds-register.cjs` — the most recent sibling; copy its
      `MIN_*`-floor / count-assertion shape and its two-mode (`scan` vs targeted) structure.
    - `252-RESEARCH.md` §1.2 (the 13 signatures) and §8 (the Phase-242 vacuous-pass precedent).
  </read_first>
  <behavior>
    - `node scripts/check-schema-acl-parity.cjs` today, against Task 1's output, exits **0** and
      prints `migrations scanned: <N> · function ACLs found: 13 · mirrored: 13`.
    - With any single function's mirror removed from the supplement, it exits **1** and prints
      `[not-mirrored]` naming that function's **full signature** and the migration file that
      grants or revokes it.
    - A function present in both is **absent** from the failure output.
    - Pointed at an empty or collapsed migrations directory it exits **2**, never 0.
  </behavior>
  <action>
    Create `scripts/check-schema-acl-parity.cjs`.

    **Header.** House block. `WHY THIS EXISTS` names the measured failure in one paragraph:
    *migration 118 shipped this class, `full-schema.sql:7449-7456` wrote it down, the supplement's
    MAINTENANCE note told maintainers to mirror ACLs, and migration 181 reproduced it anyway —
    invisible to every gate because `pg_dump --no-privileges` means a dump can never carry a
    function grant.* Then `USAGE`, then `EXIT 0 = clear · 1 = violation · 2 = harness error`.

    **Zero dependencies.** `fs`, `path`, `os` only — all Node built-ins. State it, as the siblings do.

    **`fail(msg)`** — `FATAL: ${msg}` to stderr, `process.exit(2)`. Colour constants RED/YEL/GRN/RST.
    `root = path.resolve(__dirname, '..')`.

    **`normaliseSignature(raw)`** — the piece that decides correctness. Given the text after
    `ON FUNCTION`, return a canonical key: lowercase, schema-qualified, whitespace inside the
    parens collapsed to a single space after each comma, trailing `FROM …`/`TO …` stripped.
    ⛔ **It must NOT drop the argument list** — `f(uuid, text)` and `f(uuid)` are different
    functions, and a normaliser that collapsed them would make the gate pass over a real gap.
    ⚠ Unit-cover this function directly: `public.f( uuid ,text )` and `PUBLIC.F(uuid, text)` both
    normalise to `public.f(uuid, text)`, and `public.f(uuid)` does **not**.

    **`scanMigrations(dir)`** — `fs.readdirSync(dir)` filtered on `/^\d+_.*\.sql$/`. That filtered
    length is `migrationCount` and is the ONLY authority for how many files were read.
    For each, match `/^\s*(REVOKE|GRANT)\s+EXECUTE\s+ON\s+FUNCTION\s+([^;]+?)\s+(?:FROM|TO)\s/gim`
    and collect `{ signature, file }`. ⛔ Skip lines inside `--` comments — 181's VERIFY block at
    its tail is entirely commented and quotes function names; counting those would inflate the
    expected set and make the gate fail against a correct supplement.

    **`scanSupplement(path)`** — the same regex over `scripts/full-schema-supplement.sql`,
    comment-stripped the same way, into a `Set` of normalised signatures.

    **The comparison.** For every signature in the migration set not present in the supplement set,
    emit one `[not-mirrored]` line naming the signature and the migration file. Exit 1 if any.

    **Count assertion (the Phase-242 defence).** `const MIN_MIGRATION_FILES = 150` — a floor, not an
    exact figure, so the gate survives new migrations but not a collapsed scan. If
    `migrationCount < MIN_MIGRATION_FILES`, `fail()` with a message naming both numbers and the
    sentence *"a gate that passes over nothing is worse than absent"*. ⛔ Derive `migrationCount`
    from `readdirSync`; a hardcoded expected total anywhere outside a comment is a defect.

    **`--self-test`.** Build a temp fixture with `fs.mkdtempSync(path.join(os.tmpdir(), …))`:
    a migrations dir padded to the floor plus one real ACL migration, and two supplement variants —
    one complete, one with a single function removed. ⛔ **`assertOutsideWatchedTree`** the temp dir
    the way `vitest-count-gate.cjs` does; this repo has a no-scratch-in-the-watched-tree rule.
    Assert: complete → exit 0; missing-one → exit 1 **and the output contains that signature**;
    and the counterfactual — **a signature present in both does NOT appear in the failure output**.
    ⛔ The counterfactual is not optional. Without it the gate could pass its RED arm by printing
    every signature it knows.

    **Entry point.** `if (require.main === module) { try { process.exit(main()) } catch (e) { fail(...) } }`
    so an uncaught throw becomes exit 2, never Node's default 1.
  </action>
  <acceptance_criteria>
    - `node scripts/check-schema-acl-parity.cjs; echo "exit=$?"` prints `exit=0` and a derivation
      line whose `function ACLs found:` and `mirrored:` figures are **both 13**.
    - **RED arm 1, driven for real on the working tree and restored:** delete the four
      `current_user_org_ids()` lines from the supplement → the gate exits **1** and the output
      contains `public.current_user_org_ids()`. Restore, and prove the file is **md5-identical** to
      before (`md5sum` before and after, both recorded in the SUMMARY).
    - **RED arm 2 (counterfactual), same drive:** in that same red run, the output does **not**
      contain `public.match_skills(vector, uuid, text)` — a signature present in both.
    - `node scripts/check-schema-acl-parity.cjs --self-test; echo "exit=$?"` prints `exit=0` and
      reports all three assertions passing.
    - Pointed at an empty fixture directory the gate exits **2** and the FATAL names the floor.
    - `grep -c "require(" scripts/check-schema-acl-parity.cjs` matches only `fs`, `path`, `os`.
    - `node -e "…normaliseSignature…"` (or the `--self-test` output) shows
      `public.f( uuid ,text )` → `public.f(uuid, text)` and `public.f(uuid)` → `public.f(uuid)`,
      **not equal to each other**.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-schema-acl-parity.cjs; echo "scan exit=$?"; node scripts/check-schema-acl-parity.cjs --self-test; echo "selftest exit=$?"</automated>
  </verify>
  <done>The guard exists, has been SEEN to fire on a planted omission, has been seen NOT to fire on
  a mirrored function, cannot pass over a collapsed scan set, and the supplement is md5-identical to
  before the drive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: W-4 — a refused provider-key write answers 400, not an opaque 500</name>
  <files>backend/app/api/setup.py, backend/tests/unit/test_252_setup_refusal.py</files>
  <read_first>
    - `backend/app/api/setup.py` lines 380-392 — `provider_key`, and its existing 500 arm.
    - `backend/app/api/admin.py` lines 1780-1806 — **the shape to copy**, two `except
      SettingsWriteRefused` arms with their 400 detail.
    - `backend/app/api/settings.py` line 951 — the third sibling.
    - `backend/app/models/user_settings.py` lines 45-60 and 700-712 — `SettingsWriteRefused`'s
      constructor signature and the site that raises it.
    - `backend/app/services/setup_service.py` lines 335-346 — the docstring that states the
      exception **propagates by design** and must not be caught *there*.
    - `backend/app/api/setup.py` lines 465-482 — the Phase-249 comment recording that a previous
      claim about this exception was **factually wrong and corrected rather than deleted**. Match
      that discipline.
  </read_first>
  <behavior>
    - `POST /setup/provider-key` where `save_app_settings` raises `SettingsWriteRefused` answers
      **400**, with a detail naming the refused keys and the constraint — the same shape the four
      sibling seams already answer.
    - `POST /setup/provider-key` where `save_provider_key` returns `False` still answers **500**
      with its existing message, byte-unchanged.
    - No other route in `setup.py` changes.
  </behavior>
  <action>
    **RED first.** Write `backend/tests/unit/test_252_setup_refusal.py` with two cases, and run it
    before touching `setup.py`:
      1. `save_provider_key` patched to raise `SettingsWriteRefused([...], "<constraint>")` →
         assert the response status is **400** and the detail names the refused key.
         ⛔ This must FAIL first, and the SUMMARY must record **how** — the pre-fix behaviour is an
         uncaught exception, so note whether the test client re-raises or returns 500, and quote it.
      2. `save_provider_key` patched to return `False` → assert **500** and the exact existing
         message `"Could not persist the provider key — it was not saved."`. ⛔ This must PASS
         before and after — it is the **control** proving the fix did not widen.

    ⚠ Construct `SettingsWriteRefused` with the **real** constructor read from
    `user_settings.py:51` and the real raise site at `:707` — ⛔ do not invent an argument shape.
    Use the app's existing FastAPI test-client fixture; `provider_key` sits behind
    `Depends(require_setup_token)`, so the fixture must satisfy that dependency the way the shipped
    setup tests already do. **Find and reuse that pattern; do not write a new override.**

    **GREEN.** Add to `provider_key`, around the existing call:
    ```python
    try:
        persisted = await save_provider_key(body.provider, body.api_key, body.embedding_key)
    except SettingsWriteRefused as refused:
        raise HTTPException(status_code=400, detail=<the sibling's detail shape>) from refused
    if not persisted:
        raise HTTPException(status_code=500, detail="Could not persist the provider key — it was not saved.")
    ```
    ⛔ The 500 arm's status, message and condition are **byte-unchanged**. They mean a different
    thing from a refusal — an honest write-through failure — and the docstring at `:387-389` says so.
    Import `SettingsWriteRefused` from `app.models.user_settings`, matching `settings.py:22`.

    Add a short comment naming **W-4** and the measured reason: this is the one `save_app_settings`
    seam of six that was uncaught, and the refusal it would meet is the *knob shipped without its
    migration* signature the exception exists to name.
  </action>
  <acceptance_criteria>
    - `pytest backend/tests/unit/test_252_setup_refusal.py -q` → **2 passed**.
    - The RED reading is recorded: case 1's pre-fix failure output is **quoted** in the SUMMARY.
    - `grep -c "status_code=500" backend/app/api/setup.py` is unchanged from `HEAD` — record both
      figures.
    - `git diff backend/app/api/setup.py` touches **only** the `provider_key` function and the
      import line. No other route appears in the diff.
    - `pytest backend/tests/unit -q --continue-on-collection-errors` → **failed ≤ 71**, and every
      failing node id is present in `252-BASELINE-backend-failures.txt`. ⛔ Any id not in that file
      is this plan's and blocks the task. ⭐ **Zero connector/setup/oauth/mcp suites fail at
      baseline**, so there is no inherited noise to attribute a red to here.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_252_setup_refusal.py -q</automated>
  </verify>
  <done>A refused provider-key write is named rather than swallowed, and the control proves the 500
  path is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: The reported-bugs register sweep — one BUG-260828-02, six honest statuses</name>
  <files>.planning/reported-bugs/</files>
  <read_first>
    - `252-RESEARCH.md` §7 — the eight-row table, the free id, and which report keeps `-02`.
    - `.planning/reported-bugs/TEMPLATE.md` — the frontmatter contract.
    - `CLAUDE.md` § *Reported bugs cross-check* — the `open → folded → closed` lifecycle and the
      rule that only `surface: Agentic-RAG` reports are routing candidates.
  </read_first>
  <behavior>
    - `grep -l "^id: BUG-260828-02" .planning/reported-bugs/*.md` returns exactly **one** file.
    - No report reads `status: closed` without a `verified_closed_by` naming a real artifact.
    - Every one of the six is either resolved to a named closer, or carries `folded_into` plus a
      written reason. **Neither is guessed.**
  </behavior>
  <action>
    **Step 1 — the duplicate (D-36).** `git mv` the OPEN report
    `grant-override-marker-claims-a-person-changed-it.md` →
    `BUG-260828-11-grant-override-marker-claims-a-person-changed-it.md`, and set its frontmatter
    `id: BUG-260828-11`. Add a one-line note in its body recording that it previously carried
    `BUG-260828-02`, that the collision was found by `248-CONTEXT.md:376` and left unfixed, and that
    the closed report keeps the original id **because `verified_closed_by: 214.1` is cited in
    ROADMAP and verification artifacts**.
    ⛔ Then `grep -rn "BUG-260828-02" .planning/ docs/ backend/ frontend/ scripts/` and re-point
    every citation that means the **grant-override** bug. ⚠ Citations that mean the workflow-input
    bug stay. **Record the full grep output in the SUMMARY, both before and after** — a rename that
    leaves a dangling citation is W-8 in another register.

    **Step 2 — the six statuses (D-35).** For each of `BUG-260911-01`, `BUG-260910-03`,
    `BUG-260913-01`, `BUG-260907-02`, `BUG-260828-11` and `BUG-260902-06`, find the v4.2 phase that
    cites it and **check whether that phase's artifact actually closes it**:
      - Closed by a named artifact → `status: closed` + `verified_closed_by: <phase>` + one line
        saying what closes it.
      - Cited but not closed → `status: folded` + `folded_into: <phase>` + one line saying what
        remains.
      - Neither → **leave `open`** and add a line saying which phase was checked and why it does
        not close it.
    ⛔ **A report is never flipped to `closed` without a verifier.** *A register that says `closed`
    without one is the defect this sweep exists to fix* — and a wrong `closed` is worse than a
    stale `open`, because it stops the next reader looking.
    ⚠ `BUG-260907-02` is **this phase's own B-2/B-3 territory** — it is NOT closed by Plan 01. Mark
    it `folded_into: 252` and leave the close to the phase's verification.
    ⚠ `BUG-260915-01` is **not** in this task — Plan 04 Task 1 owns its correction.

    **Step 3 — the ROADMAP checklist row (D-37).** Verify the Phase 248 checklist row reads `[x]`.
    The milestone audit already corrected it; **confirm, do not re-do**, and say which you found.
  </action>
  <acceptance_criteria>
    - `grep -c "^id: BUG-260828-02" .planning/reported-bugs/*.md | grep -v ":0" | wc -l` is **1**.
    - `grep -rn "BUG-260828-02" .planning/ docs/ backend/ frontend/ scripts/` returns **only**
      citations of the workflow-input bug — every hit is listed in the SUMMARY with which bug it
      means.
    - Each of the six reports shows a frontmatter diff, and **none** has `status: closed` with
      `verified_closed_by: null`.
    - `ls .planning/reported-bugs/ | wc -l` is **190** — a rename, not an addition or a deletion.
    - The ROADMAP Phase 248 checklist row reads `[x]`, and the SUMMARY says whether it already did.
  </acceptance_criteria>
  <verify>
    <automated>grep -l "^id: BUG-260828-02" .planning/reported-bugs/*.md | wc -l; ls .planning/reported-bugs/ | wc -l; grep -L "verified_closed_by" .planning/reported-bugs/*.md | head -0; echo ok</automated>
  </verify>
  <done>One report carries `BUG-260828-02`, every citation points at the bug it means, and no status
  claims a close nothing verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Crossing | This plan's effect |
|---|---|---|
| PostgREST `anon` → Postgres function | `/rest/v1/rpc/<fn>` | **Closes it for 13 functions on a greenfield deploy.** Today they are `anon`-executable there and only there — the migration already closed it on every DB that ran 181. |
| Setup token holder → `app_settings` | `POST /setup/provider-key` | Unchanged in authority; the refusal becomes **nameable** instead of an opaque 500. ⛔ The detail must name **keys and constraint**, never the key VALUE. |
| Repo → future migration author | `check-schema-acl-parity.cjs` | New guard. Read-only; it never writes and never touches a database. |

## STRIDE Threat Register

| ID | Threat | Mitigation | Verified by |
|---|---|---|---|
| TM-252-01 | **Information disclosure** — a greenfield deploy exposes 13 SECURITY DEFINER functions to `anon`, including `match_document_chunks` and `keyword_search_chunks` (corpus read) and `current_user_org_ids` (tenancy). | Supplement §6 mirrors 181's full ACL block into the only artifact a bootstrap reads. | Task 1 acceptance: 31 REVOKEs in both artifacts, signature set `diff`-identical to the migration. |
| TM-252-02 | **Tampering** — a *partial* mirror is worse than none: it looks closed and is not. | Signature-set parity is machine-checked against the migration, and PUBLIC-before-anon ordering is asserted per function. | Task 1 acceptance criteria 4 and 5. |
| TM-252-03 | **Repudiation / recurrence** — a fourth migration narrows a privilege and nobody mirrors it. | `check-schema-acl-parity.cjs`, driven RED on a planted omission **and** on the counterfactual. | Task 2 acceptance: both arms driven, supplement md5-identical after. |
| TM-252-04 | **Vacuous guard** — the gate exits 0 over a collapsed scan set (Phase 242 measured exactly this twice). | `MIN_MIGRATION_FILES` floor + `readdirSync`-derived count + exit **2** on breach. | Task 2 acceptance: empty fixture → exit 2. |
| TM-252-05 | **Information disclosure** — the new 400 detail echoes a provider API key. | The detail is built from the exception's **key names and constraint**, copied from the sibling seams; ⛔ the request body is never interpolated. | Task 3: assert the response body contains the key NAME and **not** the patched key value. |
</threat_model>

<verification>
- `node scripts/check-schema-acl-parity.cjs` → exit 0, `13 / 13`.
- `node scripts/check-schema-acl-parity.cjs --self-test` → exit 0, three assertions.
- `diff <(tail -n $(wc -l < scripts/full-schema-supplement.sql) supabase/full-schema.sql) scripts/full-schema-supplement.sql` → empty, **printed**.
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q --continue-on-collection-errors` → `failed ≤ 71`, every id in `252-BASELINE-backend-failures.txt`.
- `node scripts/check-hot-file-ledger.cjs 252` → `ledger gate OK`.
- `grep -l "^id: BUG-260828-02" .planning/reported-bugs/*.md | wc -l` → `1`.
</verification>

<success_criteria>
- SC#1 is structurally true: a greenfield bootstrap from `supabase/full-schema.sql` alone revokes
  `PUBLIC` EXECUTE on all 13 of migration 181's SECURITY DEFINER functions.
- The third recurrence of the ACL-mirror class is guarded by something that has been **seen to
  fire**, and seen **not** to fire on a mirrored function.
- A refused provider-key write is named (400), and the unrelated 500 path is proven untouched.
- The reported-bugs register has one `BUG-260828-02` and no unverified `closed`.
</success_criteria>

<output>
`252-01-SUMMARY.md` — carrying the before/after parity diffs verbatim, the RED drive output for both
gate arms with the md5 restore proof, the quoted pre-fix failure for W-4, and the full
`BUG-260828-02` citation grep before and after.
</output>
