---
phase: 252-close-the-v42-audit-gaps
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/sources/WatchRowCard.tsx
  - frontend/src/components/sources/WatchedFoldersSection.tsx
  - frontend/src/components/sources/sourceHealthVocabulary.ts
  - frontend/src/components/sources/__tests__/WatchRowCard.test.tsx
autonomous: true
requirements: [WATCH-03, WATCH-04, WATCH-06, WATCH-07]
user_setup: []

must_haves:
  truths:
    - "SC#4 / D-15: `onSyncNow` returns the real outcome — the card renders what `triggerWatchSync` actually said and never a literal."
    - "SC#4 / D-16: a refusal and a success are STRUCTURALLY unable to render together, because the card has ONE outcome slot derived from ONE value — not two slots with a rule."
    - "SC#4 / D-17: the string `(0 changes)` is DELETED and nothing replaces it. `WatchSyncResponse` carries no change count, so any number the card printed would be invented."
    - "SC#4 / D-18: a rejected sync renders a failure reading rather than leaving the previous one standing."
    - "W-2 / D-28: a Drive 503, a DNS failure and a socket timeout no longer render `Run failed (429)`. The tautological `isRateLimited` is deleted."
    - "W-1 / D-29: `token_revoked` and `app_credentials_invalid` no longer render `● Connected`. The connection pill reads the CAUSE, sourced from `sourceHealthVocabulary`, never a local ternary."
    - "W-3 / D-30: `token_revoked`'s control label names the door like its two siblings, and the three-named-actions pin stays green BY CONSTRUCTION — no fourth `action` is added."
    - "D-03 / D-19: `WatchRowCard.test.tsx` EXISTS for the first time, and every assertion is over RENDERED CONTENT — not `data-testid` presence. Presence assertions cannot see content drift, and that is exactly how B-4 shipped green."
  artifacts:
    - path: "frontend/src/components/sources/__tests__/WatchRowCard.test.tsx"
      provides: "the first test coverage this component has ever had"
      contains: "Synced just now"
  key_links:
    - from: "frontend/src/components/sources/WatchedFoldersSection.tsx"
      to: "frontend/src/components/sources/WatchRowCard.tsx"
      via: "onSyncNow returns WatchSyncOutcome"
      pattern: "onSyncNow"
    - from: "frontend/src/components/sources/WatchRowCard.tsx"
      to: "frontend/src/components/sources/sourceHealthVocabulary.ts"
      via: "connection-pill reading and the sync outcome sentence"
      pattern: "sourceHealthVocabulary"
---

<objective>
Make the watched-source card say what happened.

Purpose: **SC#4**, plus the three source-health warnings that ride this phase. Six lines at
`WatchRowCard.tsx:234-244` carry three defects at once: the card claims **completion** where the
parent only **queues**; `(0 changes)` is a literal that renders identically for 0 changes and for
500; and on a refusal the card renders the refusal notice at `:611-613` **while** `syncOutcome` at
`:380-385` reads `✓ Synced just now` — **two contradictory statements from one click, on one card.**

⭐ **The two findings that shape the work, and neither is in the audit:**

1. **The card already receives the parent's truth.** `refusal` and `pendingSays` are **props**
   (`:136-137`), set by `handleSyncNow` immediately after its await. The local `syncOutcome` is a
   **third, invented** source of truth sitting beside two correct ones. So D-16's "one slot" is a
   deletion, not an addition.
2. ⛔ **`WatchRowCard.tsx` has no test file at all**, and `src/components/sources` is not a TARGETS
   **directory** entry. SC#3's evidence was a *"does NOT unmount or collapse the card"* assertion —
   but there was no suite for even that to live in. **TARGETS decides what RUNS; BASELINE decides
   what is GUARDED, and this file is on the wrong side of both.**

Output: an honest outcome line, three honest readings, and the component's first suite.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/252-close-the-v42-audit-gaps/252-CONTEXT.md
@.planning/phases/252-close-the-v42-audit-gaps/252-RESEARCH.md
</context>

<interfaces>
<!-- Published so Plan 05 knows what to pin, and so a future caller cannot re-widen it. -->

```ts
/** What one "Sync now" click actually produced. ⛔ There is no `changes` field, because
 *  `WatchSyncResponse` carries no change count — see `lib/api/sources.ts:117-126`. */
export type WatchSyncOutcome =
  | { kind: "queued";  says: string }   // the parent's COPY.asked(withinPhrase(...)) sentence
  | { kind: "refused"; says: string }   // the server's own res.message
  | { kind: "failed";  says: string }   // the caught error's message

onSyncNow: (watch: ConnectorWatch) => Promise<WatchSyncOutcome>
```
⛔ The card **never** calls `triggerWatchSync` itself. The parent owns the refusal/pending state, and
a second caller would be a second writer of one slice — this repo's recorded drift mechanism.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The component's first suite — RED against all three shipped defects</name>
  <files>frontend/src/components/sources/__tests__/WatchRowCard.test.tsx</files>
  <read_first>
    - `252-RESEARCH.md` §4 — the six lines verbatim, §4.2's proof that the parent already
      try/catches and never rejects, §4.3's proof that no change count exists, and §4.7.
    - `frontend/src/components/sources/__tests__/ConnectedSourceSection.test.tsx` — the **mount
      harness to copy**: how a sources component is rendered, what is mocked, and the fixture shape
      for a `ConnectorWatch`. ⛔ Reuse it; do not invent a second harness.
    - `frontend/src/components/sources/WatchRowCard.tsx` lines 130-180 (the full prop list) and
      `WatchedFoldersSection.tsx:440-455` (the real call site — every prop's real value).
    - `CLAUDE.md` § *presence assertions cannot see content drift*.
  </read_first>
  <behavior>
    - The suite mounts the REAL `WatchRowCard` with a realistic `ConnectorWatch` and the real prop
      set, and asserts what the card **says**.
  </behavior>
  <action>
    Create `frontend/src/components/sources/__tests__/WatchRowCard.test.tsx`.

    ⛔ **Every assertion is over rendered text.** Use `getByText` / `queryByText` /
    `toHaveTextContent`. ⛔ **A `getByTestId(...)` that only asserts the node exists is forbidden in
    this suite** — a `data-testid` may be used to *scope* a query, never to *be* the assertion.
    This rule is the whole reason the suite exists: B-4's three defects shipped under a green fence
    that asserted the card *"does NOT unmount or collapse"*.

    Write these cases, and **run them before any source change**:

    1. **the literal** — click Sync now with `onSyncNow` resolving; assert the card does **not**
       render the text `(0 changes)`. ⛔ RED today.
    2. **the contradiction** — render with `refusal="Reading is switched off for this source."` and
       click Sync now; assert the card does **not** simultaneously contain both that refusal text
       and a `Synced` / `✓` success reading. ⛔ RED today — this is B-4's headline.
    3. **the failure** — `onSyncNow` rejecting; assert the card ends up showing a failure reading and
       **not** a success one. ⛔ RED today.
    4. **W-2 control** — a watch whose `last_error` is a Drive **503** / DNS / timeout (i.e. cause
       resolves to `unreachable` with no rate-limit evidence); assert the run pill does **not** read
       `Run failed (429)`. ⛔ RED today.
    5. **W-2 positive control** — a watch whose error genuinely names rate limiting; assert the pill
       **does** say so. ⚠ Write this from whatever `classifySourceFailure` actually produces for
       that input — **read the classifier, do not assume a cause name exists.** If no rate-limit
       cause exists in the taxonomy, this case asserts the honest fallback instead and the SUMMARY
       says which, and why.
    6. **W-1** — a watch whose cause is `token_revoked`; assert the connection pill does **not**
       read `Connected`. ⛔ RED today. Repeat for `app_credentials_invalid`.
    7. **W-1 control** — a healthy watch still reads `Connected`.
    8. **W-3** — with cause `token_revoked`, assert the fix control's label names Settings, the way
       `connection_disabled`'s and `app_credentials_invalid`'s already do. ⛔ RED today.

    ⛔ **Record each case's RED output in the SUMMARY** — the actual assertion failure text, not
    "it failed". A RED drive whose failure nobody read cannot prove the case binds the defect.
    ⚠ Run with `GSD_VITEST_MAX_WORKERS=2`.
  </action>
  <acceptance_criteria>
    - The file exists at `frontend/src/components/sources/__tests__/WatchRowCard.test.tsx`.
    - `grep -c "getByTestId\|findByTestId" frontend/src/components/sources/__tests__/WatchRowCard.test.tsx`
      — every hit, if any, is inside a `within(...)` scoping call. **List them in the SUMMARY with
      the assertion each one scopes.** ⛔ A bare existence assertion on a testid fails this task.
    - Cases 1, 2, 3, 4, 6 and 8 all **FAIL** before any source edit, and each failure message is
      quoted in the SUMMARY.
    - Cases 5 and 7 **PASS** before and after — they are the controls.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/sources/__tests__/WatchRowCard.test.tsx</automated>
  </verify>
  <done>Six failing cases pin six real defects, two passing controls pin what must not change, and
  nothing is asserted by presence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: B-4 — the outcome comes from the parent, and there is exactly one slot</name>
  <files>frontend/src/components/sources/WatchedFoldersSection.tsx, frontend/src/components/sources/WatchRowCard.tsx</files>
  <read_first>
    - `252-RESEARCH.md` §4.1-4.3.
    - `WatchedFoldersSection.tsx` lines 262-293 — `handleSyncNow`. ⭐ **It is already correct**: it
      branches on `res.status === "refused"`, sets `refusals` or `pendingAsks`, and try/catches into
      `setError`. It simply never tells the card.
    - `WatchedFoldersSection.tsx` lines 440-455 — where `refusal` and `pendingSays` are already
      passed down.
    - `WatchRowCard.tsx` lines 234-243 (`handleRowSync`), 376-390 (the `syncOutcome` slot) and
      608-615 (the `refusal` slot). **Two independent slots is the defect.**
    - `sourceHealthVocabulary.ts:396-403` — `COPY.asked` / `checkedNoChange` / `stopped`, and the
      comment *"the pending state. It says what was ASKED, never that work is happening."*
  </read_first>
  <behavior>
    - Clicking Sync now on a source the reader has switched off renders the server's refusal and
      **no** success sentence.
    - Clicking Sync now successfully renders the parent's existing queued sentence — *asked, next
      check within N* — and **no** count and **no** claim of completion.
    - A rejected call renders a failure reading.
    - ⛔ `WatchedFoldersSection`'s own `refusals` / `pendingAsks` / `setError` behaviour is
      unchanged; it additionally **returns** what it already computed.
  </behavior>
  <action>
    **(a) The parent returns what it already knows.** Declare `WatchSyncOutcome` (see
    `<interfaces>`) and have `handleSyncNow` return it:
      - refused branch → `{ kind: "refused", says: res.message }`
      - queued branch → `{ kind: "queued", says: COPY.asked(withinPhrase(res.next_check_within_seconds)) }`
        — ⛔ **reuse the existing expression**, do not re-write the sentence in the card.
      - `catch` → still `setError(...)`, and **also** return `{ kind: "failed", says: <that message> }`.
    ⛔ Every existing state write stays. This is an addition to the return type, not a rewrite.
    ⚠ The `finally { setActionInProgress(null) }` must still run on every path.

    **(b) The card renders ONE value.** Change the prop type to
    `onSyncNow: (watch: ConnectorWatch) => Promise<WatchSyncOutcome>`.
    `handleRowSync` becomes:
    ```ts
    async function handleRowSync() {
      setIsSyncing(true)
      setSyncOutcome(null)
      try {
        setSyncOutcome(await onSyncNow(watch))
      } catch (err: any) {
        setSyncOutcome({ kind: "failed", says: err?.message || <the vocabulary's failure sentence> })
      } finally {
        setIsSyncing(false)
      }
    }
    ```
    ⚠ The `catch` is added **even though** §4.2 proves the parent currently never rejects. The
    reason is written as a comment: the parent's swallow is what made the card's optimism invisible,
    and a future caller that does reject must not leave a stale reading standing. ⛔ Do not claim in
    that comment that the catch fixes today's contradiction — it does not; **(a)** does.

    **(c) ONE slot, structurally.** Derive:
    ```ts
    const syncLine: WatchSyncOutcome | null =
      syncOutcome ?? (refusal ? { kind: "refused", says: refusal } : null)
    ```
    Render **exactly one** element from `syncLine`, styled by `kind` (the existing emerald treatment
    for `queued`, the existing muted treatment for `refused`/`failed`). ⛔ **DELETE the separate
    `{refusal && …}` block at `:612-616`.** Two slots with a rule is how the pair drifted apart; one
    slot cannot. Keep `data-testid="sources-refusal"` on the rendered element when
    `kind === "refused"` so existing parent-level suites that scope by it keep working — ⚠ and
    **check whether any do**, with `grep -rn "sources-refusal" frontend/src`, recording the result.

    **(d) Delete the literal.** `"✓ Synced just now (0 changes)"` is gone from the file.
    ⛔ Nothing replaces it: `WatchSyncResponse` (`lib/api/sources.ts:117-126`) carries `status`,
    `message`, `next_run_at?`, `next_check_within_seconds?`, `reader_running?` — **no change count**.
    Add a one-line comment saying so, so the next author does not reinvent it.
  </action>
  <acceptance_criteria>
    - `grep -c "0 changes" frontend/src/components/sources/WatchRowCard.tsx` is **0**.
    - `grep -c "Synced just now" frontend/src/components/sources/WatchRowCard.tsx` is **0**.
    - `grep -c "{refusal &&" frontend/src/components/sources/WatchRowCard.tsx` is **0** — the second
      slot is gone, not merely guarded.
    - `grep -c "triggerWatchSync" frontend/src/components/sources/WatchRowCard.tsx` is **0** — the
      card still does not call the API.
    - Task 1 cases 1, 2 and 3 now **pass**; cases 5 and 7 still pass.
    - `grep -rn "onSyncNow" frontend/src` shows the signature change reaching **only**
      `WatchRowCard.tsx` and `WatchedFoldersSection.tsx`. ⛔ It must not reach `IngestionTab` or
      `LibraryPage`.
    - `npx tsc -p tsconfig.app.json --noEmit` — error count **≤ 67** and **no new error names a
      `src/components/sources/` file**. ⛔ Compare the SET against the base 67, not the count.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/sources/__tests__/WatchRowCard.test.tsx src/components/sources/WatchedFoldersSection.test.tsx</automated>
  </verify>
  <done>The card reports the outcome the server produced, in one slot, with no invented count and no
  way to contradict itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: W-1, W-2, W-3 — three honest readings</name>
  <files>frontend/src/components/sources/WatchRowCard.tsx, frontend/src/components/sources/sourceHealthVocabulary.ts</files>
  <read_first>
    - `252-RESEARCH.md` §4.4 (the tautology in full), §4.5 (the binary), §4.6 (why `token_revoked`
      alone is wrong, and why its VERB stays).
    - `sourceHealthVocabulary.ts` line 49 — **the rule this task obeys**: *a new cause adds a ROW,
      never an `if`*, and *"Always Reconnect" was rejected*.
    - `sourceHealthVocabulary.ts` lines 165-205 — `CONTROL_FOR_CAUSE`, and the two comment blocks on
      `connection_disabled` and `app_credentials_invalid` recording that each reuses the `reconnect`
      **action** so the three-named-actions pin stays green **by construction**.
    - `WatchedFoldersSection.tsx` lines 352-368 — `runFix`, which for `reconnect` **only navigates**.
    - `frontend/src/components/sources/sourceHealthVocabulary.test.ts` — the existing 42-case suite,
      including whatever pins the three-actions invariant. ⛔ **Read it before editing the
      vocabulary**; adding a row must not break it, and if it does, that pin is the thing to check
      first, not to loosen.
  </read_first>
  <behavior>
    - **W-2:** a failure whose only evidence is `unreachable` renders the ordinary state label. A
      `429` is claimed only from evidence that actually names rate limiting.
    - **W-1:** the connection pill distinguishes the connection-level failure causes. `token_revoked`
      and `app_credentials_invalid` do not read `Connected`.
    - **W-3:** `token_revoked`'s label keeps the verb **Reconnect** and adds where it happens.
  </behavior>
  <action>
    **W-2 (D-28) — delete the pseudo-discriminator.** `WatchRowCard.tsx:247-248` reads
    `cause === "unreachable" && classifySourceFailure(watch.last_error) === "unreachable"`, and
    `cause` at `:184` **already defaults to that same expression** — so when `stopped` is absent the
    two conjuncts are identical and the whole thing is `cause === "unreachable"`. Since `unreachable`
    is the **catch-all**, a Drive 503, a DNS failure and a socket timeout all render
    `Run failed (429)` at `:276`.
    Delete `isRateLimited` and render `presentation.label`.
    ⚠ **If — and only if — the taxonomy carries a cause that genuinely means rate-limited**, add the
    429 reading as a **ROW** in `sourceHealthVocabulary`, keyed on that cause. ⛔ **Check first**:
    read `backend/app/services/sources/failure_cause.py`'s `Cause` union and
    `classifySourceFailure`. If no such cause exists, the honest outcome is that **the 429 reading
    disappears entirely**, and the SUMMARY says so — *a label nothing can justify is worse than no
    label*, and this is the same honesty class as `BUG-260909-07`, in the code that fixed it.
    ⛔ **Do NOT invent a cause** to keep the string alive.
    ⚠ `failure_cause.py`'s `Cause` union must stay ONE plain-text line — a frontend suite binds it by
    `?raw` and a computed union is invisible to that fence. **Read that file's ledger section before
    touching it, and prefer not to touch it at all.**

    **W-1 (D-29) — the pill reads the cause.** Replace the `isConnectionDisabled` binary at
    `:246-265` with a reading sourced from `sourceHealthVocabulary`. Add a `CONNECTION_PILL_FOR_CAUSE`
    row-map (or extend the nearest existing map — **prefer extending**) giving each cause its pill
    label and tone. ⛔ **No local ternary in the component** — a one-cause binary in a call site is
    exactly how this shipped. `token_revoked` and `app_credentials_invalid` are connection-level and
    must not read `Connected`; `folder_gone` and `unreachable` are **not** connection-level and
    should still read `Connected`, because the connection genuinely is.
    ⚠ Every new row needs a case in `sourceHealthVocabulary.test.ts`; its count moves, and Plan 05
    re-pins it.

    **W-3 (D-30) — name the door.** `CONTROL_FOR_CAUSE.token_revoked.label` becomes
    `` `Reconnect ${named(connectionName)} in Settings ↗` `` (match the exact punctuation and arrow
    its two siblings use — read them, do not guess). ⛔ **`action` stays `"reconnect"`.** No fourth
    action; the three-named-actions pin stays green by construction, exactly as the two prior rows
    recorded.
    ⭐ Add a comment in the row recording **why the verb survives here and did not for its
    siblings**: for a revoked token, reconnecting genuinely **is** the fix — what was missing was
    only *where*. That distinction is the finding; without it a later reader will "consistency-fix"
    the verb away.
  </action>
  <acceptance_criteria>
    - `grep -c "isRateLimited" frontend/src/components/sources/WatchRowCard.tsx` is **0**.
    - `grep -c "429" frontend/src/components/sources/WatchRowCard.tsx` is **0** — or, if a genuine
      rate-limit cause exists, the only hit is inside a vocabulary **row**, and the SUMMARY names the
      cause and quotes the classifier line that produces it.
    - `grep -c "isConnectionDisabled" frontend/src/components/sources/WatchRowCard.tsx` is **0**.
    - `grep -c "? (" frontend/src/components/sources/WatchRowCard.tsx` for the pill region shows **no
      inline cause ternary** — the pill reads a map. State the region and the reading in the SUMMARY.
    - `grep -n "action:" frontend/src/components/sources/sourceHealthVocabulary.ts | sort -u` yields
      exactly the three shipped actions — **no fourth**.
    - Task 1 cases 4, 6 and 8 now **pass**; cases 5 and 7 still pass.
    - `npx vitest run src/components/sources/sourceHealthVocabulary.test.ts` passes. ⛔ If its count
      moved, record **old → new with the per-case attribution**; Plan 05 re-pins it, and an
      unexplained `+n` is the thing to worry about, never a bigger number.
    - `npx tsc -p tsconfig.app.json --noEmit` — SET diff against the base 67; no new error names a
      `src/components/sources/` file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/sources/</automated>
  </verify>
  <done>Three readings that were false are true, no cause was invented to keep a string alive, and
  the vocabulary gained rows rather than the call site gaining branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is crossed — this plan changes what an already-authorised surface **says**,
never what it may do. ⛔ One negative obligation, and it is the security-relevant part:

| Boundary | Obligation |
|---|---|
| server response → rendered text | `res.message` is **server-controlled text**. It is rendered as a React child (auto-escaped) and ⛔ **never** via `dangerouslySetInnerHTML`. Unchanged from today; stated so the widening cannot quietly introduce it. |

## STRIDE Threat Register

| ID | Threat | Mitigation | Verified by |
|---|---|---|---|
| TM-252-11 | **Repudiation** — the UI asserts a sync completed when nothing ran, so an operator believes their corpus is current when it is not. Flow A is broken at exactly this reporting step. | The card renders the parent's real outcome; the completion claim is deleted. | Task 1 cases 1-3, passing after Task 2. |
| TM-252-12 | **Repudiation** — two contradictory statements from one click leave no readable record of what happened. | ONE derived slot; the second renderer is **deleted**, not guarded. | `grep -c "{refusal &&"` is 0, plus Task 1 case 2. |
| TM-252-13 | **Misattribution of a security-relevant failure** — a revoked token renders `● Connected`, so a person does not act on an authorisation that is gone. | The pill reads the cause from the vocabulary. | Task 1 case 6 + the case-7 control. |
| TM-252-14 | **Misdiagnosis** — a 503/DNS/timeout renders `429`, sending the operator to rate-limit remediation for an outage. | The tautology is deleted; a 429 is claimed only from evidence. | Task 1 case 4 + `grep -c "429"` is 0. |
| TM-252-15 | **XSS via server-controlled message** — `res.message` reaches a new render site. | Rendered as a React child; no `dangerouslySetInnerHTML` anywhere in the file. | `grep -c "dangerouslySetInnerHTML" frontend/src/components/sources/WatchRowCard.tsx` is **0**. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/sources/` → all green.
- `npx tsc -p tsconfig.app.json --noEmit` → SET diff against the base 67; no new `src/components/sources/` error.
- `node scripts/check-hot-file-ledger.cjs 252` → `ledger gate OK`.
- ⛔ **This plan does NOT edit `scripts/vitest-count-gate.cjs`.** Plan 05 is its sole writer. Hand
  Plan 05 the new/changed per-file counts in this SUMMARY: `WatchRowCard.test.tsx` (new, N cases)
  and `sourceHealthVocabulary.test.ts` (42 → M, with per-case attribution).
</verification>

<success_criteria>
- SC#4: the card reports the real `triggerWatchSync` result, never a literal; a refusal and a
  success cannot render simultaneously; a failure is caught.
- W-1, W-2 and W-3 are closed, with no invented cause and no fourth action.
- The component has a suite for the first time, and it asserts content rather than presence.
</success_criteria>

<output>
`252-03-SUMMARY.md` — carrying each RED case's quoted failure text, the `grep -rn "sources-refusal"`
result, the W-2 classifier finding (does a genuine rate-limit cause exist — yes or no, with the line
that decides it), the `sourceHealthVocabulary.test.ts` count change fully attributed, and the
per-file counts Plan 05 must pin.
</output>
