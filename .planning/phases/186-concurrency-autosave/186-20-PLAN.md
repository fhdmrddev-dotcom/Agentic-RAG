---
phase: 186-concurrency-autosave
plan: 20
type: execute
wave: 2
gap_closure: true
closes: [WR-14, WR-15, WR-16, WR-17, WR-18]
depends_on: ["186-18", "186-19"]
# WAVE RATIONALE: two reasons this is wave 2 and not wave 1. (1) FILE OVERLAP —
# `186-VALIDATION.md` is owned by 186-18 in wave 1 (it appends the CR-03 row); this plan
# re-reads and appends to the same file, so it must follow. (2) CONTENT DEPENDENCY — the
# deferral entries must describe the tree AS SHIPPED after both fixes, and one of them
# (WR-14) is measurably WIDENED by 186-18's reorder. Writing them first would record a
# premise that stops being true an hour later.
files_modified:
  - .planning/phases/186-concurrency-autosave/deferred-items.md
  - .planning/phases/186-concurrency-autosave/186-VALIDATION.md
autonomous: true
requirements: [CONCUR-01, CONCUR-02]

must_haves:
  truths:
    - "EVERY REMAINING WARNING IS RECORDED WITH A CONCRETE RE-OPEN TRIGGER, NOT SILENTLY DROPPED. WR-14, WR-15, WR-16, WR-17 and WR-18 each get an entry in `deferred-items.md` naming what was not built, why it is a product/design decision rather than a bug fix this round can absorb, and the specific observable event that re-opens it. The project rule is that every deferral gets a re-open trigger, so a deferred idea is a scheduled decision rather than a lost one."
    - "THE ONE DEFERRAL THIS ROUND MADE WORSE IS SAID OUT LOUD. 186-18's CR-03 reorder extends WR-14's flicker and free-click cost to the FLAG-OFF Builder, because `SAVING_PUBLISH_WAIT` now outranks the verdict branches on that surface too. The WR-14 entry records that widening as a measured consequence of closing a blocker, so the next reader inherits the trade rather than discovering it."
    - "CR-03 / WR-12 / WR-13 ARE CLOSED AGAINST A MEASURED GATE, NOT AN ASSERTION. The phase-186 frontend consumer set, the phase-186 backend files and `tsc --noEmit` are re-run after both wave-1 plans land, with test COUNTS recorded before and after and required to be non-decreasing (the Phase 177 lesson). The measured numbers are written into `186-VALIDATION.md` so the next `/gsd:verify-work 186` reads evidence rather than a claim."
    - "SC#4 / D-186-13 / G-4 STAYS OPEN AND HONEST. The eight Manual-Only rows remain `to run` (row 4 stays ⛔) and `**Approval:** pending` is untouched — no plan task can close an operator-driven lived-experience row, and CONCUR-02 stays Pending in `REQUIREMENTS.md` until the operator runs them."
  artifacts:
    - path: ".planning/phases/186-concurrency-autosave/deferred-items.md"
      provides: "five new deferral entries, each with a concrete re-open trigger"
      contains: "Re-open trigger"
    - path: ".planning/phases/186-concurrency-autosave/186-VALIDATION.md"
      provides: "the measured post-gap-closure gate numbers, and an untouched operator approval line"
  key_links:
    - from: "the verification's Anti-Patterns table (WR-14..WR-18)"
      to: "deferred-items.md"
      via: "one entry per warning, each naming its re-open trigger"
      pattern: "WR-1[4-8]"
    - from: "the wave-1 fixes"
      to: "186-VALIDATION.md's recorded counts"
      via: "a re-run of the phase suites with before/after numbers"
      pattern: "vitest run"
---

<objective>
Finish the round the way this phase has finished every round: nothing dropped, nothing
claimed without a measurement.

The 2026-08-01 re-verification listed eight anti-patterns. Three are closed by plans 186-18
and 186-19 (CR-03, WR-12, WR-13). The other five are real, independently re-derived, and each
one is a PRODUCT DECISION rather than a race fix — changing any of them alters what an author
can do, contradicts a shipped and tested decision, or re-opens a seam whose most recent
repair is this phase's most recently closed blocker. Closing them opportunistically inside a
gap-closure round is exactly how a small honest fix becomes a large unreviewed one.

So they get recorded, with triggers sharp enough that a future reader can tell whether the
moment to act has arrived — and the round closes against a measured gate rather than a
summary paragraph.

Purpose: leave the phase auditable.
Output: five deferral entries, one measured gate, zero false completion records.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/186-concurrency-autosave/186-VERIFICATION.md
@.planning/phases/186-concurrency-autosave/deferred-items.md
@.planning/phases/186-concurrency-autosave/186-VALIDATION.md
@.planning/phases/186-concurrency-autosave/186-18-SUMMARY.md
@.planning/phases/186-concurrency-autosave/186-19-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: record WR-14 through WR-18 as deferrals, each with a re-open trigger</name>
  <files>.planning/phases/186-concurrency-autosave/deferred-items.md</files>
  <read_first>
    - .planning/phases/186-concurrency-autosave/deferred-items.md (the whole file — five entries already exist and set the house shape: what is deferred / why / re-open trigger, with §"The publish-time `flushPendingWrites()` seam — REJECTED" and §"Publish while the write loop is in `conflict` or `error`" as the nearest neighbours in subject matter; do NOT re-litigate or edit them)
    - .planning/phases/186-concurrency-autosave/186-VERIFICATION.md (the "Anti-Patterns Found" table — the five warnings and their stated impacts are the source text)
    - frontend/src/hooks/useDraftPersistence.ts (the sites WR-15/16/17/18 name: the `{kind:"saving"}` set inside `performWrite` and its `finally`, `reload()`'s try scope and its catch, and `overwrite()`'s `tokenRef.current = conflictTokenRef.current` — re-derive each before writing about it)
    - frontend/src/pages/WorkflowBuilderPage.tsx and frontend/src/components/workflows/PublishGauntlet.tsx (WR-14's two halves: the reason's RANK in `blockedReason`, and the outer trigger's `disabled={blocked}` — both AS SHIPPED after 186-18's reorder)
    - frontend/src/lib/api.ts (the draft PATCH transport — WR-15's "no client-side timeout" claim; confirm there is no abort/timeout belt before repeating it)
  </read_first>
  <action>
    Append five entries to `deferred-items.md` in the file's established shape (a `##` heading
    naming the thing, then **What is deferred**, **Why**, **Re-open trigger**), followed by
    the existing dated-attribution line pattern (`*Recorded: 2026-08-01 · Phase
    186-concurrency-autosave, plan 20*`). Edit no existing entry.

    RE-DERIVE EVERY LINE NUMBER AND EVERY BEHAVIOURAL CLAIM FROM SOURCE BEFORE WRITING IT.
    Four of four executors in this phase found an inherited claim false; the verification's
    references were re-derived on 2026-08-01, but plans 186-18 and 186-19 have since edited
    two of the three files those references point into.

    **WR-14 — the wait reason outranks the fixable ones, and it disables a free click.**
    Deferred: re-ranking `blockedReason`, and/or narrowing the disable to the inner Publish
    button. Why: the rank is a SHIPPED, TESTED decision — `WorkflowBuilderPage.canvas.test.tsx`
    pins "the saving reason OUTRANKS the empty-draft invitation" with an explicit rationale
    (fixing a verdict will not make Publish go until the PATCH lands), and reversing it is a
    product call rather than a race fix. Splitting the two gates — letting the outer trigger,
    which spends nothing, open the modal while only the inner Publish click refuses — is
    defensible but changes the modal's contract and the R12 placement of its reason. RECORD
    THE WIDENING THIS ROUND CAUSED: 186-18's CR-03 reorder makes the wait reason outrank the
    verdict branches on the FLAG-OFF Builder too, so the flicker and the intermittently
    unclickable trigger now exist on both surfaces. That is the price of closing a blocker,
    and it is recorded rather than discovered. Re-open trigger: the first operator observation
    that the Publish trigger felt unclickable while typing, OR the first phase that gives
    `blockedReason` a rank/kind model able to distinguish "wait a moment" from "fix this" — at
    which point the outer trigger should stop being gated by a wait.

    **WR-15 — the `saving` block has no ceiling.** Deferred: a client-side timeout / abort
    belt on the draft write. Why: this file's §"Publish while the write loop is in `conflict`
    or `error`" entry argues that `saving` is safe to block on precisely because it is
    BOUNDED; WR-15 observes that the bound rests on `performWrite`'s `finally` plus every
    terminal branch setting a non-`saving` state, with one branch that would not (unreachable
    today) and no network timeout at all. Adding an abort belt is a transport-wide decision —
    the sibling test "cancels no write — the abort belt `useLiveValidation` uses is
    deliberately absent" pins its absence as a CHOICE — and a half-applied timeout could abort
    a write the server has already committed, which is strictly worse than waiting. Re-open
    trigger: the first live observation of a `Saving…` reading that never resolves, OR the
    first phase that gives the workflow-draft transport an abort/timeout policy — that plan
    owns deleting the absence-pinning assertion in the same commit.

    **WR-16 — `reload()`'s catch spans more than the request.** Deferred: narrowing the try
    scope so only the network call is caught. Why: it is a real honesty defect (a failure
    AFTER a successful read is reported as "we couldn't reach the server", inviting a retry
    that cannot work), but the catch is the very one GAP-4/CR-02 was just repaired in, and
    that repair is the phase's most recently closed blocker. Re-scoping it means re-proving
    the whole F21 family. Re-open trigger: the first report of the reload note appearing
    against a server that answered, OR any change that makes the reload path parse more of the
    response body.

    **WR-17 — a second Reload shows the first failure's note.** Deferred: clearing
    `state.note` when a new resolution starts. Why: same mechanism and same edit as WR-16 —
    they are one change, and splitting them across rounds would touch the CR-02 repair twice.
    Re-open trigger: identical to WR-16's; these two re-open together.

    **WR-18 — `overwrite()` degrades to an unguarded write on a `null` token.** Deferred:
    refusing, or re-reading, when the refusal body carried no `detail.token`. Why: this is the
    sharpest of the five — an Overwrite with a null token sends no `If-Match` and can clobber
    a third writer with no refusal at all. It is deferred only because every available answer
    CHANGES WHAT AN AUTHOR CAN DO: refusing removes an exit D-186-08 guarantees is always
    offered, and re-reading turns the one-request exit into two. That is a product decision
    for the phase that also gives the conflict banner a publish-aware reading (the existing
    §"Publish while the write loop is in `conflict` or `error`" entry names the same seam).
    State the containment honestly: the server still applies its owner scope and its
    published-row guard, so the exposure is a lost OPTIMISTIC guard, not a lost authorisation.
    Re-open trigger: the first 409 observed whose body lacks `detail.token`, OR the phase that
    gives the conflict banner and the publish gate one voice.

    Close the appended block with one short note stating that CR-03, WR-12 and WR-13 were
    CLOSED this round (naming plans 186-18 and 186-19), so a reader of this file alone can
    tell the deferred five from the closed three.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -c "Re-open trigger" .planning/phases/186-concurrency-autosave/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - The count above increased by exactly 5 (record the before and after numbers).
    - `grep -o "WR-1[4-8]" .planning/phases/186-concurrency-autosave/deferred-items.md | sort -u`
      returns all five ids.
    - The WR-14 entry explicitly states that 186-18's reorder widened the effect to the
      flag-off surface (a `grep -n "flag-off"` match falls inside that entry).
    - `git diff` on the file shows INSERTIONS ONLY — zero deletions, zero edits to the five
      pre-existing entries.
    - Every line number or behavioural claim written into the file was re-derived in this
      task; the SUMMARY lists each claim with the file + symbol it was read from, and flags
      any that disagreed with the verification report's version.
    - No entry proposes a fix as "v1", "simplified", "basic", "for now" or "placeholder" —
      each is a full deferral with a trigger, never a scope reduction.
  </acceptance_criteria>
  <done>The five open warnings are scheduled decisions with observable triggers, the trade this round made on WR-14 is on the record, and a reader of the deferrals file alone can separate what was closed from what was carried.</done>
</task>

<task type="auto">
  <name>Task 2: the measured gate — re-run the phase suites and write the numbers into VALIDATION.md</name>
  <files>.planning/phases/186-concurrency-autosave/186-VALIDATION.md</files>
  <read_first>
    - .planning/phases/186-concurrency-autosave/186-VALIDATION.md (the frontmatter, the "Test Infrastructure" baselines, the "Validation Sign-Off" checklist, and the Manual-Only table including the CR-03 row 186-18 appended)
    - .planning/phases/186-concurrency-autosave/186-18-SUMMARY.md and 186-19-SUMMARY.md (the counts those plans measured, so this task states deltas rather than fresh absolutes)
    - .planning/phases/186-concurrency-autosave/deferred-items.md §"The frontend full-suite rot band is wider — and less STABLE — than SEED-056 records" (the recorded reason the FULL frontend suite is not a usable pass/fail signal: measure per file, never as one whole-suite number)
  </read_first>
  <action>
    Run and record, editing no source file.

    Command 1 — the phase-186 frontend consumer set, in one run:
    `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/hooks/useDraftPersistence.test.tsx src/lib/api.workflows.test.ts src/components/workflows/PublishGauntlet.test.tsx src/components/workflows/builderStore.test.ts src/components/workflows/BuilderSaveRegion.test.tsx src/components/workflows/WorkflowCanvas.editing.test.tsx src/components/workflows/canvasNudge.test.ts`
    Record the runner's passed/total summary line verbatim.

    Command 2 — the phase-186 backend files:
    `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_186_concurrent_patch.py tests/unit/test_186_publish_race.py tests/unit/test_103_published_409.py -q`
    Record passed/skipped/total. Skips without a live Postgres are the SHIPPED design (WR-06's
    per-test `skipif`), not a failure — say which it was.

    Command 3 — `cd frontend && npx tsc --noEmit -p tsconfig.app.json`. Record the error count
    and confirm zero errors in any phase-186 file.

    Command 4 — `cd "C:/Vibe Apps/Agentic RAG" && git status --porcelain supabase/migrations`
    (expect empty: zero migrations this round) and `git diff --stat frontend/package.json`
    (expect empty: zero package changes).

    Then edit ONLY `186-VALIDATION.md`:

    - Append a dated subsection titled "Post-gap-closure gate (2026-08-01 · plans 186-18 /
      186-19 / 186-20)" beneath the "Validation Sign-Off" block, listing every command above
      with its measured result and the delta against the counts 186-18 and 186-19 recorded.
      A DECREASE in any test count is a FAILURE of this task — stop and report rather than
      recording it as green (the Phase 177 lesson: a "net-new" file can replace a suite
      invisibly).
    - Tick ONLY the sign-off checkboxes this round can PROVE, and leave the rest unticked. In
      particular, tick nothing that depends on the operator's manual rows.
    - `wave_0_complete` in the frontmatter reads `false` while all six Wave-0 files appear to
      exist. Verify each by `ls` — `backend/tests/unit/test_186_concurrent_patch.py`,
      `backend/tests/unit/test_186_publish_race.py`,
      `frontend/src/hooks/useDraftPersistence.test.tsx`,
      `frontend/src/components/workflows/PublishGauntlet.test.tsx`,
      `backend/tests/unit/test_103_published_409.py`,
      `frontend/src/components/workflows/builderStore.test.ts` — and flip it to `true` ONLY if
      all six are present, pasting the `ls` output into the SUMMARY as the evidence. If any is
      missing, leave the flag alone and report which.
    - Leave `**Approval:** pending` exactly as it is. Leave every Manual-Only row at `to run`
      (row 4 stays ⛔). Do not touch `.planning/REQUIREMENTS.md` — CONCUR-01/CONCUR-02 stay
      Pending until the operator's rows are run, and no plan task may flip them.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/hooks/useDraftPersistence.test.tsx src/components/workflows/PublishGauntlet.test.tsx src/components/workflows/BuilderSaveRegion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All four commands were run and their raw outputs are pasted into the SUMMARY — no
      result is described without its command output.
    - Command 1 is green and its total is greater than or equal to the pre-round total, with
      the expected delta being exactly the three rows added by 186-18 (+1) and 186-19 (+2).
    - Command 2 is green; the skip count is reported with its reason (live Postgres present or
      absent).
    - Command 3 reports zero errors.
    - Command 4 shows zero migration changes and zero package changes.
    - `grep -n "Post-gap-closure gate" .planning/phases/186-concurrency-autosave/186-VALIDATION.md`
      matches, and the subsection contains all four commands with their numbers.
    - `grep -n "^\*\*Approval:\*\* pending" .planning/phases/186-concurrency-autosave/186-VALIDATION.md`
      still matches, and `grep -c "to run" ` is unchanged from before this task.
    - `git status --porcelain .planning/REQUIREMENTS.md` is empty — this plan writes no
      requirement status.
  </acceptance_criteria>
  <done>The round's claims are backed by pasted command output in the phase's own validation file, the stale Wave-0 flag agrees with the filesystem, and everything that only an operator can close is still visibly open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning records → future implementers | a deferral with a weak trigger becomes a silently dropped defect; the record IS the control |
| this round's claims → the next `/gsd:verify-work 186` | a green claim without pasted output is an unverifiable assertion the next gate will inherit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-20-01 | Repudiation | the five deferred warnings | mitigate | Each entry names an observable re-open trigger, so "we knew and chose" is distinguishable from "we forgot". WR-18's entry additionally states its residual exposure (a lost optimistic guard, not a lost authorisation) so a future reader can size it without re-deriving. |
| T-186-20-02 | Tampering (with the record, not the code) | `186-VALIDATION.md` sign-off | mitigate | Only checkboxes this round can prove are ticked; `**Approval:** pending` and all `to run` statuses are left untouched, and `.planning/REQUIREMENTS.md` is explicitly out of `files_modified` — the SDK verbs that write false completion records are not used anywhere in this plan. |
| T-186-20-03 | Information disclosure | WR-18's deferral text | accept | The entry describes an unguarded-write path in a planning document inside the repo. It names no credential, no token value and no bypass technique that is not already visible in the source it cites; the repo's own threat model treats `.planning/` as trusted-internal. |
| T-186-20-04 | Denial of service (of the next verification) | inheriting counts instead of measuring them | mitigate | Every number in the gate subsection is required to come with its command output pasted in the SUMMARY, and a decrease is a hard stop rather than a note. |
| T-186-20-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs nothing and touches no source file. Acceptance: `git diff --stat` shows changes to two `.planning/` markdown files only. |
</threat_model>

<verification>
1. `deferred-items.md` gained exactly five entries, insertions only, each with a re-open trigger, and WR-14's records the flag-off widening.
2. `186-VALIDATION.md` carries the dated post-gap-closure gate subsection with four commands and their measured results.
3. Test counts non-decreasing across the whole round (+3 expected: 186-18's one canvas row, 186-19's F20i and F23).
4. `**Approval:** pending` untouched; every Manual-Only row still `to run` (row 4 ⛔); `.planning/REQUIREMENTS.md` unmodified.
5. Zero migrations, zero package changes, zero source-file edits by this plan.
</verification>

<success_criteria>
- The five carried warnings are deferrals with triggers, not omissions — and the one this round made worse is on the record.
- The round's green claims are reproducible from pasted command output.
- Nothing an operator alone can close was marked closed.
</success_criteria>

<output>
Create `.planning/phases/186-concurrency-autosave/186-20-SUMMARY.md` when done. Record: the
before/after `Re-open trigger` counts, every re-derived claim with the symbol it was read from
(flagging any that disagreed with the verification report), the four gate commands with raw
output, and the `ls` evidence behind any `wave_0_complete` change.
</output>
