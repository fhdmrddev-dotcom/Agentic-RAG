---
phase: 186-concurrency-autosave
plan: 19
type: execute
wave: 1
gap_closure: true
closes: [WR-12, WR-13]
depends_on: []
# WAVE RATIONALE: this plan owns `useDraftPersistence.ts` + `useDraftPersistence.test.tsx` and
# NOTHING else. 186-18 owns `WorkflowBuilderPage.tsx`, its canvas test and `186-VALIDATION.md`.
# Zero file overlap ⇒ genuinely parallel in wave 1. No component file is touched: the new
# reading rides `BuilderSaveRegion`'s existing `held` → quiet-line path, which 186-13 (WR-04)
# deliberately left UNGATED by `autosaveEnabled` — confirmed by reading that component.
files_modified:
  - frontend/src/hooks/useDraftPersistence.ts
  - frontend/src/hooks/useDraftPersistence.test.tsx
autonomous: true
requirements: [CONCUR-01, CONCUR-02]

must_haves:
  truths:
    - "WR-12 — THE FLAG-OFF HOLD RELEASE SAYS WHAT HAPPENED INSTEAD OF GOING SILENT. When the hold ends on the `!enabled` path and the store still holds unsent work, the reading resolves to a `{kind:\"held\"}` sentence that names the situation and the way out, rather than to a bare `{kind:\"idle\"}`. The instruction the author was given while the gauntlet ran (`HOLD_PUBLISHING_MANUAL` — \"press Save draft again when it finishes\") is therefore REPLACED at the moment it becomes actionable, not erased."
    - "WR-12 / D-181-01 — NOTHING IS WRITTEN TO BUY THAT HONESTY. The `!enabled` branch still returns before `performWrite()`, and `heldPendingRef.current = store.getState().dirty` is unchanged. F20a, F20a-create and F20b still assert ZERO network calls on the flag-off drive; the only thing that changed on that path is what the surface SAYS."
    - "WR-12 — THE NEW SENTENCE IS NOT A RECEIPT AND NOT A REFUSAL. It is carried by `{kind:\"held\"}`, which `BuilderSaveRegion` renders on the quiet line on EVERY surface (186-13 / WR-04, ungated by `autosaveEnabled`), so no component change is needed; `markSaved` is not called, `dirty` stays true, and the constant is asserted DISTINCT from `SAVE_FAILED_SENTENCE`, `HOLD_PUBLISHING` and `HOLD_PUBLISHING_MANUAL` (the F20e/F8 inequality idiom) so two constants holding the same string cannot pass by accident."
    - "WR-13 — A CHOSEN EXIT LEAVES NO STALE ARMING BEHIND, SO A LATER CLEAN HOLD CYCLE WRITES NOTHING. `overwrite()` clears `heldPendingRef` where it clears `haltedRef`, exactly as `reload()`'s success path already does — so after a conflict is resolved, a subsequent publish hold that ends against a CLEAN store issues no PATCH. RED (measured before the fix): one extra no-op PATCH that mints a fresh token, manufacturing precisely the conflict this phase exists to prevent (CONCUR-01's \"a write nobody asked for\" and CONCUR-02's token churn)."
    - "WR-13 — THE HALTED EARLY RETURN IS ANSWERED WITH A DERIVATION, NOT A SHRUG. `haltedRef` is cleared in exactly two places — `reload()`'s success path and `overwrite()` — and both now clear `heldPendingRef` too, so the ref cannot survive a halt into a running loop. The hold-release effect's `if (haltedRef.current) return` therefore needs no ref write of its own, and the effect's docblock records that reasoning by name so the next reader does not re-open it as an oversight."
  artifacts:
    - path: "frontend/src/hooks/useDraftPersistence.ts"
      provides: "a hold release that ends with a true sentence on both branches of the flag, and exits that leave no stale arming"
      exports: ["useDraftPersistence", "HOLD_PUBLISHING", "HOLD_PUBLISHING_MANUAL", "HOLD_ENDED_UNSAVED"]
    - path: "frontend/src/hooks/useDraftPersistence.test.tsx"
      provides: "F20i (the flag-off release speaks) and F23 (an exit clears the arming), plus the retargeted F20b"
  key_links:
    - from: "the non-null → null hold transition on the !enabled path"
      to: "BuilderSaveRegion's quiet line"
      via: "a {kind:\"held\"} reading carrying HOLD_ENDED_UNSAVED, rendered ungated by autosaveEnabled"
      pattern: "HOLD_ENDED_UNSAVED"
    - from: "overwrite()"
      to: "the hold-release effect's nothing-pending gate"
      via: "heldPendingRef cleared beside haltedRef, mirroring reload()'s success path"
      pattern: "heldPendingRef.current = false"
---

<objective>
Close the two residual warnings the 2026-08-01 re-verification re-derived directly from
`useDraftPersistence.ts:825-846` — both on the same mechanism 186-17 half-fixed, and both on
the paths this phase has found residue in every single round: the failure and flag-off paths
of mechanisms that are honest on their success path.

**WR-12 (`:834-839`).** 186-17 made the hold's reading resolve unconditionally on the
non-null → null transition, which killed the stale *"Publishing — not saved; press Save draft
again when it finishes"* sentence. But on the `!enabled` branch it resolves to a bare
`{kind:"idle"}` — so the instruction is erased at the exact instant it becomes actionable.
The author is left holding unsent work with ZERO on-screen indication until the leave guard
fires on navigate-away. The write suppression is right; the silence is the defect.

**WR-13 (`:833`).** The `haltedRef` early return leaves `heldPendingRef` untouched. Chased to
its end, that memory can outlive the halt and flush a PATCH against a draft nobody edited —
the same class of harm 186-17 fixed on the flag-off path (F20f), through a different door.

Purpose: finish the mechanism on the branches nobody watches.
Output: one new locked constant, one branch that speaks, one exit that cleans up after
itself, two new falsifiable guards, and one retargeted assertion — with the assertion count
going UP, never down.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/186-concurrency-autosave/186-VERIFICATION.md
@.planning/phases/186-concurrency-autosave/186-17-SUMMARY.md
@.planning/phases/186-concurrency-autosave/deferred-items.md

<interfaces>
<!-- Extracted from source 2026-08-01. Line numbers WILL move as you edit — re-locate by
     symbol, never by number, and re-derive any number you put in the SUMMARY. -->

frontend/src/hooks/useDraftPersistence.ts
  :162  export const HOLD_PUBLISHING        = "Publishing — changes will save when it finishes"
  :183  export const HOLD_PUBLISHING_MANUAL = "Publishing — not saved; press Save draft again when it finishes"
  :194  export const HOLD_UNREADABLE        = "Not saved — we can't read this shape yet"
  :202  export const SAVE_FAILED_SENTENCE   = "Not saved — we couldn't complete the save"
  :219  export const DRAFT_GONE_SENTENCE    = …
  :261  export const RELOAD_FAILED_NOTE     = …
  :289  PersistState = idle | saving | saved{at} | held{sentence} | conflict{currentToken,note?} | error{sentence}
  :502/:521/:522  haltedRef, holdRef, heldPendingRef
  :825-846  the hold-release effect:
            :826-827 mirror `holdRef`; :828 transition test;
            :832 setState((s) => (s.kind === "held" ? { kind: "idle" } : s))   ← the F20h functional resolution
            :833 if (haltedRef.current) return                                 ← WR-13's named site
            :834-839 if (!enabled) { heldPendingRef.current = store.getState().dirty; return }  ← WR-12
            :841-845 the nothing-pending gate, then heldPendingRef = false, then performWrite()
  :852-863  saveNow — arms heldPendingRef and sets {kind:"held"} when holdRef is non-null
  :910-954  reload() — SUCCESS path at :930-936 already does: setDrafted, tokenRef, conflictTokenRef=null,
            haltedRef=false, heldPendingRef=false, pendingRef=false, setState idle
  :975-987  overwrite() — tokenRef = conflictTokenRef; haltedRef = false; await performWrite()
            (heldPendingRef is NOT cleared here — that asymmetry is WR-13's real closure site)

frontend/src/components/workflows/BuilderSaveRegion.tsx
  the quiet line: `state.kind === "held" ? state.sentence : !autosaveEnabled ? null : …`
  ⇒ a `held` sentence renders on the FLAG-OFF surface by design (186-13 / WR-04). No change needed here.

frontend/src/hooks/useDraftPersistence.test.tsx
  harness(opts) → { store, markSaved, created, view, set, edit }; stateOf(view); reachableStrings(v);
  advance(ms), flush(), write(token), sentDefinition(i); mockedUpdate / mockedCreate / mockedList
  the F20 describe: F20a, F20a-create, F20b, F20f, F20g, F20h, F20c, F20e, F20d
  F20b currently asserts, after a flag-off release:
      expect(after.kind).not.toBe("held")
      expect(reachableStrings(after)).not.toContain(HOLD_PUBLISHING_MANUAL)
      expect(reachableStrings(after)).not.toContain(HOLD_PUBLISHING)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-12 — the flag-off release ends with a sentence, not with silence</name>
  <files>frontend/src/hooks/useDraftPersistence.ts, frontend/src/hooks/useDraftPersistence.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useDraftPersistence.ts (the four hold/refusal constants and their docblocks — the house voice the new constant must join; the hold-release effect and its full docblock, which is the specification this task amends)
    - frontend/src/hooks/useDraftPersistence.test.tsx (the whole F20 describe — F20b is retargeted, F20e's inequality idiom is copied, F20f/F20g/F20h must stay green untouched)
    - frontend/src/components/workflows/BuilderSaveRegion.tsx (the `quietLine` derivation and its docblock — the proof that a `held` sentence renders on the flag-off surface without any component change)
  </read_first>
  <behavior>
    - Flag OFF, publish in flight, the author edits and/or presses Save draft, the gauntlet ends, the store is still dirty ⇒ the reading is `{kind:"held"}` carrying the NEW sentence; it is neither `HOLD_PUBLISHING` nor `HOLD_PUBLISHING_MANUAL` (no publish is running any more), and it is not `SAVE_FAILED_SENTENCE` (nothing was refused).
    - Flag OFF, the same drive but the store is CLEAN at release ⇒ the reading resolves to `{kind:"idle"}` and says nothing. Silence is correct exactly when there is nothing unsent.
    - Neither drive issues any network call: `updateWorkflowDraft` and `createWorkflowDraft` are both untouched (F20a's claim, restated).
    - A `conflict`, `error` or `saved` reading that arrived during the hold is untouched by the release (F20h's claim, restated) — the new reading is applied functionally and only to a `held` state.
    - `markSaved` is never called on this path and `dirty` stays true — the sentence is not a receipt.
  </behavior>
  <action>
    SOURCE. Add one exported constant next to the other hold sentences in
    `useDraftPersistence.ts`, named `HOLD_ENDED_UNSAVED`, with the exact text
    `"Not saved — press Save draft to save your changes"`. Give it a docblock in the file's
    established voice that records: it is WR-12's sentence; it is reachable only on the
    flag-off path where D-181-01 forbids the automatic flush, so unlike `HOLD_PUBLISHING` it
    promises nothing and instructs instead (the F20e rule — a sentence may promise a flush
    only where one will happen); it is deliberately NOT `SAVE_FAILED_SENTENCE`, because
    nothing was refused and the cause-neutral line would invite a retry of something that
    never failed; and it has ONE home, like its neighbours.

    In the hold-release effect's `!enabled` branch, keep `heldPendingRef.current =
    store.getState().dirty` and keep the `return` before `performWrite()` — neither moves.
    Read the pending truth into a local const first (a single `store.getState().dirty` read,
    used for both the ref and the reading), then, when it is true, apply a SECOND functional
    `setState` that upgrades the reading to `{ kind: "held", sentence: HOLD_ENDED_UNSAVED }`
    and is a NO-OP for any state that is not the `idle` the line above just produced. The
    functional form is not stylistic: a bare object would erase a `conflict` — the only
    reading that carries Reload and Overwrite — which is GAP-4 through a second door, and
    F20h exists to catch exactly that.

    Amend the effect's docblock in the same edit. It currently states the fifth fact ("none of
    [the gates] is a reason to go on claiming that a publish is running") — extend it with the
    sixth: none of them is a reason to say NOTHING either, when the person is holding unsent
    work that this surface will never send on its own. Name WR-12.

    TEST. Retarget F20b rather than weakening it, and record why in a comment. Its three
    current assertions are: the kind is not `held`, and neither publishing sentence is
    reachable. The first is PATCH-shaped (the Phase 185 lesson: verify the PROPERTY, not the
    patch) and is now false by design. DELETE NOTHING ELSE: keep both `not.toContain`
    assertions verbatim — they are the real property — and replace the kind assertion with a
    POSITIVE one naming the new reading (`stateOf` is `{kind:"held", sentence:
    HOLD_ENDED_UNSAVED}`), so the row's assertion count goes UP by nothing less than zero and
    the property it proves gets stronger.

    Add one new row, `F20i`, in the same describe, driving BOTH directions with the harness's
    own helpers: (a) flag off, publish in flight, `h.edit()`, then release ⇒ the new sentence
    is the reading, `mockedUpdate`/`mockedCreate` are uncalled, `h.markSaved` is uncalled, and
    `h.store.getState().dirty` is true; (b) flag off, publish in flight, NO edit (a clean
    store), then release ⇒ the reading is `{kind:"idle"}` and `reachableStrings` contains
    none of the three hold sentences. Add the inequality control in the F20e idiom:
    `HOLD_ENDED_UNSAVED` differs from `HOLD_PUBLISHING`, from `HOLD_PUBLISHING_MANUAL` and
    from `SAVE_FAILED_SENTENCE` — two constants that happened to hold the same string would
    pass the equalities and prove nothing.

    OBSERVE RED FIRST. Write the test edits, run them against the UNMODIFIED source, and
    record the failure verbatim (F20i(a) should fail with a received `{kind:"idle"}`). Only
    then make the source change.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useDraftPersistence.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RED recorded verbatim in the SUMMARY: F20i(a) and the retargeted F20b assertion both
      fail against unmodified source, with the received values pasted in.
    - GREEN: the whole hook suite passes after the source change, and the suite's test COUNT
      is recorded before and after — it must be exactly one higher (F20i), never lower.
    - F20a, F20a-create, F20f, F20g, F20h, F20c, F20d and F20e pass UNCHANGED (`git diff` on
      the test file shows no edit inside those rows).
    - Source assertion: `grep -n "HOLD_ENDED_UNSAVED" frontend/src/hooks/useDraftPersistence.ts`
      matches exactly three regions — the constant's declaration, the `!enabled` branch, and
      nowhere else in a component file
      (`grep -rn "HOLD_ENDED_UNSAVED" frontend/src --include=*.tsx --include=*.ts | grep -v "useDraftPersistence"`
      returns only the test file).
    - Source assertion: the `!enabled` branch still contains `heldPendingRef.current =
      store.getState().dirty` and still `return`s above any `performWrite()` call.
    - Source assertion: the new `setState` in that branch is FUNCTIONAL — the line matching
      `HOLD_ENDED_UNSAVED` inside the effect is inside a `setState((s) =>` form, not a
      `setState({` form.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports zero errors.
    - `git diff --stat frontend/src/components/workflows/BuilderSaveRegion.tsx` is EMPTY — the
      new reading rides the shipped `held` path, and if it does not, the design is wrong.
  </acceptance_criteria>
  <done>A flag-off author whose publish just ended sees a true sentence about their unsent work instead of a blank header, nothing is written to achieve it, and every other reading the release can meet is provably untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-13 — a chosen exit leaves no stale arming for a later hold cycle to flush</name>
  <files>frontend/src/hooks/useDraftPersistence.ts, frontend/src/hooks/useDraftPersistence.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useDraftPersistence.ts (the hold-release effect's `haltedRef` early return; `saveNow`'s arming of `heldPendingRef`; `reload()`'s SUCCESS path, which already clears `heldPendingRef` — the symmetry this task restores; `overwrite()`, which does not)
    - frontend/src/hooks/useDraftPersistence.test.tsx (the F19 describe — the double-click / exit rows, for the idiom of driving `overwrite` and `reload` through `act`; the F10 describe, for the `WorkflowStaleTokenError` refusal drive)
    - frontend/src/hooks/useDraftPersistence.ts `performWrite`'s catch and its superseded branch (the two places `heldPendingRef` is armed inside the writer)
  </read_first>
  <behavior>
    - Drive the chain: a write is outstanding (deferred), a publish hold begins, the author presses Save draft (which arms `heldPendingRef` and reports the hold), the outstanding write is then refused with a stale token (the loop halts into `conflict`), the publish ends (the release hits the halted early return), the author presses Overwrite (the halt clears, exactly one PATCH goes out), and finally a LATER publish hold begins and ends against a now-clean store.
    - GREEN: the total `updateWorkflowDraft` call count over that whole drive is exactly TWO — the original refused write and the Overwrite. The final hold cycle writes nothing.
    - RED (must be observed first): THREE calls — the third is a no-op PATCH carrying a definition nobody changed, which mints a fresh token and invalidates every other open tab's optimistic guard.
    - The store is clean (`dirty === false`) at the moment the final hold cycle releases — the positive control proving the third PATCH really was a no-op rather than a legitimate flush.
    - `reload()`'s behaviour is unchanged: F21a-f and F19c stay green.
  </behavior>
  <action>
    OBSERVE THE RED FIRST, AND DO NOT INHERIT THIS PLAN'S DERIVATION. Write the new test row
    (name it `F23 — a chosen exit clears the pending arming, so a later clean hold writes
    nothing (WR-13)`) and run it against unmodified source. Drive it with the existing
    harness: use `mockedUpdate.mockImplementationOnce` returning a promise you reject manually
    with `WorkflowStaleTokenError` so the first write is outstanding while the hold begins,
    `h.set({ publishInFlight: true })` for the hold, `await act(async () => { await
    h.view.result.current.saveNow() })` for the arming press, then reject, then
    `h.set({ publishInFlight: false })`, then `await act(async () => { await
    h.view.result.current.overwrite() })` with `mockedUpdate` resolving via `write(...)`, then
    a second `publishInFlight` true→false cycle with `flush()` and an `advance` past the
    debounce. Assert the call count and the store's `dirty` at the points named in
    `<behavior>`.

    If the RED does not reproduce — i.e. the drive already yields two calls — STOP and report
    it. The planner's derivation is that `reload()`'s success path already clears
    `heldPendingRef`, so `overwrite()` is the only surviving route by which an arming outlives
    a halt; if the measurement disagrees, the mechanism must be re-derived before any source
    line is touched. Record whichever answer you measure.

    SOURCE (after RED). In `overwrite()`, clear `heldPendingRef.current = false` where
    `haltedRef.current = false` is set — above `await performWrite()`, in the same
    "nothing is mutated until the exit is known to be takeable" block that already assigns
    `tokenRef.current`. Nothing is lost by clearing it: `performWrite` is called
    unconditionally on the very next line and writes the CURRENT definition, bypassing the
    dirty gate, so any work the flag was remembering is written by that call.

    Document the halted early return rather than editing it. Add to the hold-release effect's
    docblock (or a comment at that line) the derivation the verifier's WR-13 text invites:
    `haltedRef` is cleared in exactly two places — `reload()`'s success path and `overwrite()`
    — and after this change BOTH clear `heldPendingRef` too, so no arming can survive a halt
    into a running loop; a halted loop is also always dirty (it halts on a REFUSED write, and
    a refused write leaves `dirty` true), so writing the ref on that branch would be
    unobservable by construction. State that plainly, so the next reader does not re-open a
    closed question as an oversight. If your measurement contradicts the "always dirty"
    claim, write what you measured instead — this docblock must not carry an inherited claim.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useDraftPersistence.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RED recorded verbatim: the F23 row fails against unmodified source with `updateWorkflowDraft`
      called 3 times where 2 are expected (or, if it does not, the plan STOPPED and reported).
    - GREEN: the whole hook suite passes; the suite COUNT is exactly one higher than after
      Task 1 (F23), never lower.
    - Source assertion: `grep -n "heldPendingRef.current = false" frontend/src/hooks/useDraftPersistence.ts`
      now matches inside BOTH `reload()` and `overwrite()` as well as the release's flush path
      — record the three matches with their enclosing function names.
    - Source assertion: in `overwrite()`, the `heldPendingRef` clear precedes `await performWrite()`.
    - The F19 exit rows (F19a table, F19b, F19c, F19d) and the whole F21 describe pass
      unchanged (`git diff` shows no edit inside them).
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports zero errors.
    - The hold-release effect's `if (haltedRef.current) return` line is UNCHANGED in behaviour
      (`git diff` shows only comment/docblock additions around it).
  </acceptance_criteria>
  <done>An arming made before a conflict can no longer flush a PATCH after the conflict is resolved, the closure is proven by a call count that was measured wrong first, and the site the verifier named carries the derivation for why it needs no code of its own.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → `PATCH /workflows/{id}` | every write this hook issues; the optimistic token crosses here and each successful write MINTS A NEW ONE |
| the write loop's state → rendered DOM | client-authored sentences render on the Builder header on both branches of the canvas flag |
| `visual_workflow_canvas` OFF ⇒ no automatic write | D-181-01, this milestone's HARD gate #1 — the revert switch is a behavioural boundary, not just a UI one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-19-01 | Spoofing (a false receipt — the T-185-04-01 class) | the new `HOLD_ENDED_UNSAVED` reading | mitigate | It is carried by `{kind:"held"}`, which by construction has no `at` field and no `ok` field, and `BuilderSaveRegion` maps `held` to the quiet line — never to `SAVED_STILL_A_DRAFT` and never to the `builder-save-error` alert span. F20i asserts `markSaved` is uncalled and `dirty` stays true on that path, so the surface cannot claim a save that did not happen. |
| T-186-19-02 | Information disclosure | the new constant's text | mitigate | Fixed client-authored string with nothing interpolated — no token, no draft id, no server body (the `RELOAD_FAILED_NOTE` rule). Asserted DISTINCT from the three neighbouring sentences so it cannot be produced by a mis-selected branch either. |
| T-186-19-03 | Tampering / integrity | `heldPendingRef` cleared in `overwrite()` | mitigate | Clearing it cannot lose work: `performWrite()` is invoked on the next line and writes the CURRENT store definition, bypassing the dirty gate. F23 asserts the Overwrite's own PATCH still happens (call 2 of 2) and F20f's both-directions drive still proves the flag-off arming survives into a later enabled session when there IS unsent work. |
| T-186-19-04 | Denial of service / economics | the no-op PATCH WR-13 describes | mitigate | Every PATCH mints a fresh `updated_at` token, so an unrequested write invalidates the optimistic guard every other open tab holds — the phase's own failure mode, manufactured by its own repair. Closed by T-186-19-03's clear and measured by F23's call count, not argued. |
| T-186-19-05 | Elevation of privilege | D-181-01's no-automatic-write-past-the-revert-switch guarantee | mitigate | The `!enabled` branch keeps its `return` above `performWrite()`; only a `setState` is added below it. F20a, F20a-create and F20b continue to assert ZERO `updateWorkflowDraft` AND ZERO `createWorkflowDraft` calls on the flag-off drive, and those rows are not edited except for F20b's retargeted assertion. |
| T-186-19-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs nothing. Acceptance: `git diff --stat` shows no change to `frontend/package.json` or any lockfile. No `## Package Legitimacy Audit` is required — there are no package-manager install tasks. |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/hooks/useDraftPersistence.test.tsx` — green; count = baseline + 2 (F20i, F23), never lower.
2. `cd frontend && npx vitest run src/components/workflows/BuilderSaveRegion.test.tsx src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx` — the hook's consumers, green and unedited.
3. `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — zero errors.
4. `git diff --stat frontend/src/components/workflows/BuilderSaveRegion.tsx` is empty.
5. Both REDs recorded verbatim before their fixes; zero migrations; zero package changes.
</verification>

<success_criteria>
- WR-12 closed: the flag-off hold release ends with a true sentence when work is unsent and with silence when it is not, and writes nothing either way.
- WR-13 closed: a conflict resolved by Overwrite leaves no arming behind, proven by a call count observed wrong first.
- Every pre-existing F20 / F19 / F21 row is still green, and the only assertion that moved is F20b's PATCH-shaped one, retargeted to a stronger positive claim.
</success_criteria>

<output>
Create `.planning/phases/186-concurrency-autosave/186-19-SUMMARY.md` when done. Record: both RED
outputs verbatim, the hook-suite count before/after, the three `heldPendingRef.current = false`
sites with their enclosing functions, and an explicit yes/no on whether the "a halted loop is
always dirty" derivation held up under measurement.
</output>
