---
phase: 186-concurrency-autosave
plan: 08
type: execute
wave: 5
depends_on: ["186-07", "186-04"]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/pages/WorkflowBuilderPage.header.test.tsx
  - .planning/reported-bugs/BUG-260731-03-no-ui-to-rebind-workflow-knowledge-base.md
  - .planning/phases/186-concurrency-autosave/deferred-items.md
autonomous: true
requirements: [CONCUR-01]

must_haves:
  truths:
    - "D-186-14 — 186 folds the MINIMUM fix (the author-time binding CONTROL); the deterministic build-time /validate verdict stays in Phase 187 and is NOT built here"
    - "D-186-15 — the control is the EXISTING display-only header chip, promoted into the picker that already exists; an explicit UNBOUND state renders rather than nothing"
    - "D-186-16 — the unbound chip is an INVITATION, never a verdict: it states the consequence ('No knowledge base · searches everything') with no severity, no code, no problems-tray row and nothing in the node marks; D-182-06 stays intact"
    - "D-186-17 — the folder_scope ordering trap is RECORDED and carried forward, not built; and its UNBIND direction (which D-186-17 did not anticipate) is proven unreachable today"
    - "The binding persists through the very save path this phase rebuilt, and arms the leave guard (F14 / F16)"
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "the promoted KB chip picker + UNBOUND_KB_INVITATION"
      contains: "UNBOUND_KB_INVITATION"
    - path: ".planning/phases/186-concurrency-autosave/deferred-items.md"
      provides: "the D-186-17 carry-forward with its concrete re-open trigger"
  key_links:
    - from: "the header chip picker"
      to: "builderStore setProjectFolder"
      via: "onChange, alongside setHasEdited(true)"
      pattern: "setProjectFolder"
    - from: "the unbound chip"
      to: "nothing"
      via: "it feeds no verdicts array, no groupVerdicts row, no problems tray, and NOT blockedReason"
      pattern: "UNBOUND_KB_INVITATION"
---

<objective>
Close the repair path for `BUG-260731-03` — the blocking bug where a workflow's knowledge base
could only be chosen on the pre-draft describe screen.

Purpose: the operator reproduced this bug on themselves within ten minutes, and the publish
judge then **passed a worse deliverable than it had failed an hour earlier** (11 files across
5+ folders vs 3 files across 3 folders). Three creation paths can produce an unbound workflow
and two of them never offer the choice at all. The only in-product fix was regeneration, which
discards the authored canvas.

Output: the shipped display-only `📁 <folder>` header chip becomes a picker — the same
`project-folder-picker` component that already exists on the describe screen, at a second
mount point — plus an explicit unbound state that states the consequence in plain language.
The binding persists through the autosave path this phase just rebuilt.

**What this plan does NOT build.** The deterministic build-time `/validate` `incomplete`
verdict for an unbound retrieval workflow is Phase 187's (D-186-14, SEED-132 envelope). The
bug's `re_open_trigger` says explicitly it must **not** flip to `closed` when 186 ships. Do not
add a verdict, a severity, a code, a lint finding, or a `blockedReason` entry here.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/186-concurrency-autosave/186-CONTEXT.md
@.planning/phases/186-concurrency-autosave/186-RESEARCH.md
@.planning/phases/186-concurrency-autosave/186-PATTERNS.md
@.planning/phases/186-concurrency-autosave/186-VALIDATION.md
@.planning/reported-bugs/BUG-260731-03-no-ui-to-rebind-workflow-knowledge-base.md
@.planning/phases/186-concurrency-autosave/186-07-SUMMARY.md
@.planning/phases/186-concurrency-autosave/186-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the chip into the picker, with an honest unbound state</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1345-1367` — the `<select
      data-testid="project-folder-picker">` on the describe screen, whose options come from
      `folderOptions` (fetched **unconditionally on mount**, `:1002-1028`, so no new fetch is
      needed in the drafted view)
    - `:1533-1551` — `identityGroup` and the `boundFolderName &&` gate that currently hides the
      unbound state entirely
    - `:244-263` — the exported named-constant block, and specifically `EMPTY_DRAFT_INVITATION`
      (`:251-258`) whose docblock is the D-186-16 precedent word for word
    - `:985-998` — `blockedReason`, the publish-block seam the KB invitation must **NOT** join
    - `frontend/src/components/workflows/builderStore.ts` — `setProjectFolder` (added in 186-04)
    - `frontend/src/pages/WorkflowBuilderPage.tsx:700-720` — the `hasEdited` subscription and
      its explicit exclusion of `meta` changes
    - `186-RESEARCH.md` §RECOMMENDATION 5 and §"The KB-chip trap"
    - `186-PATTERNS.md` §Shared Patterns 4 — the four properties of an invitation
    - `.claude/skills/sketch-findings-agentic-rag/SKILL.md` — the builder header conventions
  </read_first>
  <action>
Replace the display-only chip in `identityGroup` with the picker. Reuse the existing
`project-folder-picker` select rather than authoring a new control — one control, two mount
points (D-186-15 explicitly rejects a net-new workflow-settings panel; that would fire G-2 and
blow this phase's UI budget of a status line plus a conflict banner). Style it to sit in the
header without changing the header's layout; a bound workflow still reads `📁 <folder name>`,
never a UUID.

**Remove the `boundFolderName &&` gate.** An unbound workflow must render an explicit state,
because the operator's evidence is that the failure is invisible precisely when nothing is
shown.

Add `UNBOUND_KB_INVITATION` to the exported named-constant block: *"No knowledge base ·
searches everything"* — **the consequence, not the state**. "Unbound" sounds harmless; "searches
everything" is what actually happened. Give it a docblock in `EMPTY_DRAFT_INVITATION`'s exact
style: it is an INVITATION, not a claimed verdict; the client is not validating anything; no
severity, no code and no lint rule is being computed; D-182-06 stays intact because the server
owns every verdict.

Wire the picker's `onChange` to `store.getState().setProjectFolder(id || null)` **and**
`setHasEdited(true)`.

**The `hasEdited` decision, taken here and stated (RESEARCH open risk #4):** option (a) — flip
it at the call site. D-184-15's actual rule is *"nothing is claimed before the author's first
EDIT"*, and a deliberate re-bind IS an author edit. Option (b) — leaving `hasEdited` alone —
would mean the very edit this bug is about produces no live check, which is the wrong answer
for a bug whose whole story is a failure that surfaced too late. The `:713-717` subscription's
`meta` exclusion is NOT widened: that exclusion exists so generate/open (document transitions,
which always replace `meta`) do not start the loop, and widening it would restart the loop on
every document load. A call-site flip is precise; a subscription change is a blunt instrument.

**Where the invitation is allowed to travel — and where it is not.** It renders on the chip and
stops there. It must NOT join `blockedReason` (that would make an unbound workflow
unpublishable, which is 187's call, not 186's), must not become a `Verdict`, must not enter
`verdicts` or `groupVerdicts`, and must not add a problems-tray row or any node mark.

**The unbind direction (RESEARCH open risk #6 / Pitfall 8), decided:** unbinding is allowed and
is **proven unreachable-to-harm by test**, not blocked by a client-side rule. A phase declaring
`folder_scope` on an unbound workflow raises a raw 422 (`_folder_scope_requires_project`), which
under D-186-04's hold rule would produce a permanently-unsaveable draft — but `folder_scope` is
**read-only** in `PhaseFormPanel` today, so no reachable draft can carry it. A client-side
"can't unbind" refusal would be the client computing a validation rule, which is exactly what
D-182-06 forbids. Add a source comment on the unbind path naming the trap and pointing at the
carry-forward record; Task 2 proves the premise.
  </action>
  <acceptance_criteria>
    - `grep -c "boundFolderName &&" frontend/src/pages/WorkflowBuilderPage.tsx` returns **0**
    - `grep -c "UNBOUND_KB_INVITATION" frontend/src/pages/WorkflowBuilderPage.tsx` returns **>= 3** (constant, docblock, render site); the string "No knowledge base · searches everything" appears exactly **once** in the file
    - `grep -c "project-folder-picker" frontend/src/pages/WorkflowBuilderPage.tsx` returns **2** — one control, two mount points (a third would mean a new control was authored)
    - `grep -n "UNBOUND_KB_INVITATION" frontend/src/pages/WorkflowBuilderPage.tsx` shows **no** hit inside the `blockedReason` memo
    - `grep -n "state.meta !== prev.meta" frontend/src/pages/WorkflowBuilderPage.tsx` still returns its shipped hit — the `hasEdited` subscription's `meta` exclusion was NOT widened
    - `git diff --name-only -- frontend/src/components/workflows/` -> **0 files** (no new component; `PhaseFormPanel.tsx`, `WorkflowCanvas.tsx` and `canvasNudge.ts` all untouched)
    - `cd frontend && npx tsc -b` error count == baseline (33), 0 naming a touched file
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx</automated>
  </verify>
  <done>An author can bind or re-bind the knowledge base from the canvas on every entry path, and an unbound workflow says what that means.</done>
</task>

<task type="auto">
  <name>Task 2: F16 — the chip states a fact and claims nothing; and the trap is recorded</name>
  <files>frontend/src/pages/WorkflowBuilderPage.header.test.tsx, .planning/reported-bugs/BUG-260731-03-no-ui-to-rebind-workflow-knowledge-base.md, .planning/phases/186-concurrency-autosave/deferred-items.md</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx` (whole) — its mount helper, its
      `?raw` imports at `:115-120`, and its existing header assertions
    - `frontend/src/components/workflows/PhaseFormPanel.tsx:340-390` — `folder_scope` rendered
      as a BOUND DISPLAY with no writer (the premise Task 1's unbind decision rests on)
    - `.planning/reported-bugs/BUG-260731-03-...md` frontmatter — `status: folded`,
      `folded_into: "186 (control) / 187 (verdict)"`, and the `re_open_trigger` forbidding a
      flip to `closed` when 186 ships
    - `.planning/phases/185-graded-governance/deferred-items.md` — the house shape for a
      carry-forward record
    - `186-VALIDATION.md` — F16
  </read_first>
  <action>
**F16, RED first.** In `WorkflowBuilderPage.header.test.tsx`:
- an UNBOUND drafted workflow renders the invitation sentence (RED today: nothing renders);
- the same render produces **zero** problems-tray rows and leaves the `verdicts` array byte-
  identical to a control render — assert the tray COUNT and the verdicts value, not just the
  absence of a specific string;
- the invitation carries **no** severity token and **no** code: assert the rendered chip's
  `textContent` and `className` contain none of the severity vocabulary the surface uses
  elsewhere (read the actual tokens from the shipped source, do not retype them);
- changing the picker calls `setProjectFolder` and leaves `dirty === true` (the F14 half at the
  page level);
- the bound state still renders the folder NAME, never a UUID.

Add a **source-fence** case using the file's shipped `?raw` idiom: assert
`WorkflowBuilderPage.tsx`'s source does not place `UNBOUND_KB_INVITATION` inside the
`blockedReason` memo or any `verdicts`/`groupVerdicts` expression. Word it narrowly and add
BOTH controls — a positive control that finds a planted violation and a negative control
proving the regex leaves the constant's own docblock alone (the `useGroundingBundle.test.ts`
over-broad-fence warning).

**Prove the unbind premise.** Add one assertion that no reachable authoring control writes
`folder_scope`: a `?raw` source assertion over `PhaseFormPanel.tsx` showing `folder_scope`
appears only in read positions (no `onChange`/setter writing it). If that assertion cannot be
made cleanly, say so in the SUMMARY and escalate rather than weakening it — the whole unbind
decision rests on this premise.

**Record, do not build (D-186-17).** Create
`.planning/phases/186-concurrency-autosave/deferred-items.md` in the house shape with two
entries:
1. **D-186-17 carry-forward** — whichever phase makes `folder_scope` editable per-phase must
   land the workflow-level binding first and gate the per-phase control on it, because an
   editable `folder_scope` on an unbound workflow raises a raw 422 which, under D-186-04's
   hold-the-write rule, produces a permanently-unsaveable draft. Re-open trigger: the first
   plan that proposes an editable `folder_scope` control. Note that the UNBIND direction (which
   D-186-17 did not anticipate) is currently unreachable and is pinned by the test above.
2. **`BUG-260731-03`'s verdict half** — Phase 187, unchanged.

Append a dated note to the bug report recording that the **control half shipped in 186** and
that the report stays `status: folded` — it closes only when BOTH halves are verified. **Do not
edit the frontmatter.**
  </action>
  <acceptance_criteria>
    - F16 observed RED before Task 1 and GREEN after; RED output recorded
    - The tray-count and `verdicts` assertions compare against a control render, not against a hardcoded expectation
    - The `?raw` fence has BOTH a positive and a negative control, and the negative control asserts the constant's own docblock does not trip it
    - `.planning/phases/186-concurrency-autosave/deferred-items.md` exists and contains the literal token `D-186-17` and a concrete re-open trigger
    - The bug report's frontmatter is byte-unchanged: `git diff .planning/reported-bugs/BUG-260731-03-*.md` shows additions **below** the frontmatter only, and `status: folded` still holds
    - `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx` passes; the file's count is strictly greater than before (record both)
    - `cd frontend && npx vitest run` full frontend suite: the 209-test clean subset is non-decreasing and the total failure count is within the recorded pre-existing rot band (SEED-056) — no NEW failing file
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.test.tsx src/components/workflows/builderStore.test.ts</automated>
  </verify>
  <done>The chip's neutrality is machine-enforced, the unbind premise is pinned by a test, and the ordering trap is written down where the next phase will find it.</done>
</task>

</tasks>

<threat_model>
Non-regression invariants only.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-authored copy -> the verdict surface | the line D-182-06 draws: the client may state a fact about configuration, never compute a verdict |
| the picker -> the definition -> the golden run's retrieval scope | binding changes which corpus a published workflow reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-08-01 | Spoofing (of a verdict) | `UNBOUND_KB_INVITATION` | mitigate | It renders on the chip and nowhere else. F16 asserts the tray count and the `verdicts` array are unchanged against a control render, and a `?raw` fence asserts the constant never appears inside `blockedReason` / `verdicts` / `groupVerdicts`. |
| T-186-08-02 | Denial of Service (self-inflicted) | unbinding while a phase carries `folder_scope` | mitigate | Pinned by a source assertion that no authoring control writes `folder_scope` today, plus a recorded carry-forward for the phase that makes it editable. A client-side refusal was rejected because it would be the client computing a validation rule. |
| T-186-08-03 | Elevation of Privilege | the folder id written into the definition | accept | `project_folder_id` is an existing declared field on `WorkflowDefinition`; retrieval scope is resolved server-side by `scope.py` against the caller's own access. Binding a folder the caller cannot read grants nothing. Unchanged by this phase. |
| T-186-08-04 | Repudiation | a binding silently lost on navigate-away | mitigate | `setProjectFolder` arms `dirty` (186-04, F14) and the call site flips `hasEdited`, so the leave guard and the live check both see the edit. |
| T-186-08-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed in this phase. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx` -> passes, count strictly increased
- `cd frontend && npx vitest run` -> the 209-test clean subset non-decreasing; no new failing file beyond the recorded SEED-056 rot band
- `cd frontend && npx tsc -b` -> error count == 33 baseline, 0 naming a touched file; `npx vite build` exits 0
- `git diff --name-only -- backend/ supabase/migrations frontend/src/components/workflows/` -> **0 files**
- `BUG-260731-03` frontmatter unchanged; `status` still `folded`
</verification>

<success_criteria>
- The knowledge base is bindable and re-bindable from the built canvas on all four Builder entry paths
- An unbound workflow states its consequence in plain language, with no severity, code, tray row or node mark
- The binding persists through autosave and survives a reload
- F16 observed RED then GREEN, with both fence controls present
- D-186-17's carry-forward and the unbind-direction addendum are recorded, not built
- `BUG-260731-03` remains `folded`, with a dated note recording that only the control half shipped
</success_criteria>

<output>
Create `.planning/phases/186-concurrency-autosave/186-08-SUMMARY.md` when done. Record the F16
RED output, the header-test before/after counts, the final invitation string, and an explicit
statement that the `/validate` verdict half was NOT built and remains Phase 187's.
</output>
