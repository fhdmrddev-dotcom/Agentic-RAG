---
phase: 186-concurrency-autosave
plan: 10
type: execute
wave: 6
gap_closure: true
closes: [WR-02]
depends_on: ["186-08"]  # the shipped seam (waves 1-5); no gap-closure plan precedes this one
files_modified:
  - frontend/src/components/workflows/PublishGauntlet.tsx
  - frontend/src/components/workflows/PublishGauntlet.test.tsx
autonomous: true
requirements: [CONCUR-02]

must_haves:
  truths:
    - "Every `blocked_stage` literal `backend/app/services/harness/publish_service.py` can emit places a block on EXACTLY ONE spine node, or is named in an explicit `NOT_ON_THE_SPINE` set with the api.ts line that proves it never reaches the spine. `PublishGauntlet.test.tsx` proves it by extracting the literals from the Python source at test time and driving one render per stage — not by a hand-typed list that can rot."
    - "`grounding_fidelity` (publish_service.py:239) has a row in `STAGES` sitting between `Pause` (interactive_phase) and `Golden run`, which is its real position in the pipeline. Re-derivable by reading `PublishGauntlet.tsx` — the `STAGES` array has 10 entries and a `codes: [\"grounding_fidelity\"]` row at index 5."
    - "The running-node highlight follows the Golden run ROW, not a hard-coded index. Re-derivable by reading `PublishGauntlet.tsx` — the two `i === 5` literals are replaced by a comparison against an index derived from `STAGES`, so inserting a stage cannot silently move the pulse."
    - "F7 stays green: a genuinely unknown `blocked_stage` still paints zero passed nodes, zero reached connectors and zero ✓ badges."
  artifacts:
    - path: "frontend/src/components/workflows/PublishGauntlet.tsx"
      provides: "a spine that names every stage the server can emit, with a derived running index"
      contains: "grounding_fidelity"
    - path: "frontend/src/components/workflows/PublishGauntlet.test.tsx"
      provides: "the server-sourced superset property test + the retargeted running-highlight test"
      contains: "NOT_ON_THE_SPINE"
  key_links:
    - from: "publish_service.py `stage=\"...\"` literals"
      to: "STAGES[].codes in PublishGauntlet.tsx"
      via: "a test-time read of the Python source, one render per stage"
      pattern: "grounding_fidelity"
---

<objective>
Close WR-02: `publish_service` has emitted `grounding_fidelity` since Phase 182 and the client
spine has no node for it, so the single most likely real publish refusal renders as a fully
grey, unplaceable nine-node spine under the generic fallback sentence.

Purpose: the F7 fail-open repair is real and holds — this is NOT a false-green regression. But
the spine's own docblock claims it mirrors the server's stage list and it does not, and the
"one forgotten stage" failure mode will recur every time the backend adds a check. Fix the
row, and replace the claim with a test that reads the server's own source so the claim cannot
go stale again.

Output: a tenth `Grounding` stage at its real pipeline position, a running-highlight index that
is derived rather than hard-coded, and a property test sourced from `publish_service.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/186-concurrency-autosave/186-VERIFICATION.md
@.planning/phases/186-concurrency-autosave/186-REVIEW.md
@CLAUDE.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- Extracted from the codebase this session. -->

`backend/app/services/harness/publish_service.py` emits these `stage="..."` literals, in
source order (line numbers from this session):
  :130 already_published · :151 definition_invalid · :165 business_requirement · :180 lint ·
  :203 interactive_phase · :239 grounding_fidelity · :279 golden_run_timeout ·
  :293 golden_run_error · :316 structural_gate · :362 judge · :385 already_published ·
  :405 draft_changed
`grounding_fidelity` is emitted between the `interactive_phase` gate (:203) and the golden run
(:255), which is its real pipeline position.

`frontend/src/lib/api.ts:3753-3754`:
  `if (res.status === 404) return { kind: "not_found" }`
  `if (res.status === 409) return { kind: "already_published" }`
Neither carries a `verdict`, so `already_published` can never arrive as a `blocked_stage` on a
`kind: "verdict"` outcome and therefore never reaches `GauntletSpine`. (`not_found` is in the
same position but already has an `Owner` row, which stays — it documents the stage.)

`frontend/src/components/workflows/PublishGauntlet.tsx`:
- `const STAGES: { label: string; what: string; codes: string[]; Icon: StageIcon }[]` at :141,
  nine entries, module-LOCAL and deliberately unexported (`react-refresh/only-export-components`).
- `GauntletSpine` at :372: `blockedIndex = STAGES.findIndex(s => s.codes.includes(blockedStage))`,
  `unknownBlock = blockedStage != null && blockedIndex === -1`, then per node
  `isRunning = running && i === 5` (:391) and the comet `running && i === 5` (:417).
- Icons are `~icons/fluent-emoji/*` 3D components typed as `StageIcon`.

`frontend/src/components/workflows/PublishGauntlet.test.tsx`:
- helpers `doPublish()`, `nodeBoxFor(label)` (the node box is the label's `previousElementSibling`),
  `mockedPublish`, and the constants `PASSED_NODE_TONE = "bg-success/10"` /
  `REACHED_CONNECTOR_TONE = "bg-success/50"`.
- :684 `it("keeps the running highlight on the Golden run row — appending the ninth stage moved
  nothing")` asserts `nodeBoxFor("Golden run").className` contains `border-amber-500`. This test
  WILL go red when the Grounding row is inserted, and that is the intended falsification.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the server-sourced stage-coverage property test and observe it RED</name>
  <files>frontend/src/components/workflows/PublishGauntlet.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PublishGauntlet.test.tsx (the whole file — especially the F7 describe at :517-604, the draft_changed describe at :606+, and `doPublish` / `nodeBoxFor` / `mockedPublish`)
    - frontend/src/components/workflows/PublishGauntlet.tsx (:126-165 the STAGES table and `httpStatusForKind`, :372-443 `GauntletSpine`)
    - backend/app/services/harness/publish_service.py (grep `stage="` to see all twelve emission sites in context)
    - frontend/src/lib/api.ts (:3720-3760 — the publish outcome mapping that proves `already_published` never arrives as a verdict blocked_stage)
  </read_first>
  <behavior>
    - F18a "the extraction is not vacuous": the regex applied to `publish_service.py` yields at
      least 11 distinct stage literals, AND a positive control proves the regex captures a
      planted literal.
    - F18b "every emitted stage places a block on exactly one node": for each extracted stage
      that is not in `NOT_ON_THE_SPINE`, rendering the gauntlet with that `blocked_stage`
      produces exactly one node carrying the blocked tone `border-destructive/60` and at least
      one node NOT carrying the passed tone. RED: `grounding_fidelity` produces ZERO blocked
      nodes.
    - F18c "the exclusion set is justified, not convenient": `NOT_ON_THE_SPINE` contains only
      `already_published`, and the test asserts its size is exactly 1 so a future stage cannot
      be silently parked there.
  </behavior>
  <action>
    Add ONE new describe to `frontend/src/components/workflows/PublishGauntlet.test.tsx`, placed
    after the existing F7 describe, headed
    `PublishGauntlet — F18: the spine names every stage the server can emit (WR-02)`.

    Read the server source AT TEST TIME rather than transcribing a list. Use `readFileSync` from
    `node:fs` and `fileURLToPath` from `node:url`, resolving
    `new URL("../../../../backend/app/services/harness/publish_service.py", import.meta.url)` —
    from `frontend/src/components/workflows/` that is four levels up to the repo root. Extract
    with the call-form regex `/stage="([a-z_]+)"/g` and collect into a `Set`. Do the read once at
    module scope so `it.each` can enumerate it.

    Declare `const NOT_ON_THE_SPINE = new Set(["already_published"])` with a docblock naming the
    exact evidence: `api.ts:3753-3754` maps 404 and 409 to `{kind:"not_found"}` and
    `{kind:"already_published"}`, neither of which carries a `verdict`, so `already_published`
    cannot reach `GauntletSpine` as a `blocked_stage`. State plainly that this set is an
    ALLOW-LIST WITH REASONS and that adding to it requires the same kind of evidence line —
    otherwise it becomes the drift hatch this test exists to close.

    Write the two controls first (F18a and F18c), then the `it.each` over
    `[...emitted].filter(s => !NOT_ON_THE_SPINE.has(s))`. Each case: reset `mockedPublish` to
    resolve a `kind: "verdict"` outcome with `published: false`, `version: null`,
    `golden_run_id: null`, the stage under test, and `named_failures: ["publish was refused"]`;
    `render(<PublishGauntlet definitionId="def-1" />)`; `await doPublish()`; wait for
    `publish-block`; then query the spine for elements whose class contains
    `border-destructive/60` and assert exactly one. Because `it.each` renders repeatedly, call
    `cleanup()` from `@testing-library/react` in an `afterEach` scoped to this describe (or rely
    on the file's existing global cleanup — check first and do not add a second one).

    Do NOT weaken F7. Its bogus stage stays genuinely unknown and must keep asserting zero
    passed nodes, zero reached connectors, zero ✓ badges.

    OBSERVE THE RED. Run the file with `PublishGauntlet.tsx` UNMODIFIED and record which stage
    fails and with what numbers. The expected RED is exactly one case — `grounding_fidelity` —
    reporting 0 blocked nodes where 1 is expected. If any OTHER stage also fails, stop and
    report it in the SUMMARY before changing source; that would mean a second forgotten stage
    the review did not name.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The command FAILS, and the only failing case in the new F18 describe is `grounding_fidelity`.
    - The F18a control passes: the extracted set size is >= 11.
    - `git diff --stat frontend/src/components/workflows/PublishGauntlet.tsx` is EMPTY.
    - The file's total test count is strictly greater than the pre-task count; record both.
  </acceptance_criteria>
  <done>F18 exists, is red on exactly `grounding_fidelity`, and the component is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Grounding row at its real position and derive the running-node index</name>
  <files>frontend/src/components/workflows/PublishGauntlet.tsx, frontend/src/components/workflows/PublishGauntlet.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PublishGauntlet.tsx (:60-75 the icon imports, :126-151 the STAGES docblock + table, :372-443 GauntletSpine)
    - frontend/src/components/workflows/PublishGauntlet.test.tsx (:606-700 — the draft_changed describe and the running-highlight test at :684 that this task retargets)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (the Phase 127 energized pip/energy-spine + worded verdict vocabulary — the new row's copy must match it, not invent a register)
    - backend/app/services/harness/publish_service.py (:210-243 — the grounding stage's placement and what it actually checks)
  </read_first>
  <action>
    Insert a tenth `STAGES` entry at index 5, between the `Pause` row (`interactive_phase`) and
    the `Golden run` row, because that is where `_grounding_fidelity_failures` runs in
    `publish_service.py` (after the interactive-phase gate at :203, before `_drive_golden_run`
    at :255). Its fields: `label: "Grounding"`, `codes: ["grounding_fidelity"]`, and a `what`
    sentence in the same voice as the nine existing rows — an em-dashed plain-language gloss of
    what the check does, e.g. naming that the folders, tools and skills the workflow points at
    must resolve. Keep the register of the existing rows (Phase 127 worded-verdict vocabulary);
    do not introduce severity language, a code, or a second sentence.

    Add ONE icon import alongside the existing `~icons/fluent-emoji/*` imports. Use
    `~icons/fluent-emoji/books`; if that slug does not resolve at build time, fall back to
    `~icons/fluent-emoji/open-book`. Resolution is proven by `npx vite build` exiting 0, which
    is an acceptance criterion below — do not assume, run it. Do NOT reuse
    `MagnifyingGlassTiltedLeft` (it is the Structure row's mark; the icon convention is one
    mark per meaning).

    Now de-magic the running index, because inserting a row moves `Golden run` from index 5 to
    index 6 and the two hard-coded `i === 5` reads at :391 and :417 would silently pulse the
    wrong node. Derive it once at module scope beside `STAGES` — an exported-nowhere const
    computed as the index of the row whose `codes` include `"golden_run_timeout"` — and replace
    both `i === 5` comparisons with a comparison against that const. Add a comment stating that
    the running node is located by the stage it belongs to, so a stage inserted anywhere cannot
    move the pulse again.

    Correct the `STAGES` docblock (:126-140). Its current closing argument — that the Commit row
    was APPENDED rather than inserted specifically so the golden-run row keeps the index the
    running highlight points at — is no longer the reason the code is safe, and leaving it would
    tell the next reader something false. Replace that clause with the derived-index rule, and
    add a sentence recording that the table is now pinned to the server's emission list by
    F18 in the co-located suite.

    Retarget (do NOT delete) the test at `PublishGauntlet.test.tsx:684`. Its name and its
    premise both refer to appending the ninth stage; the property it should now defend is that
    the running highlight sits on the Golden run row wherever that row is. Rename it accordingly,
    keep the `nodeBoxFor("Golden run")` amber assertion, and ADD an assertion that no OTHER node
    box carries `border-amber-500` — which is what makes the derived index falsifiable rather
    than merely re-passing.

    Then confirm F18 is green for every stage, F7 is still green, and the whole file passes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/PublishGauntlet.test.tsx` exits 0 with zero failures.
    - `grep -c "codes:" frontend/src/components/workflows/PublishGauntlet.tsx` counts 10.
    - `grep -n "grounding_fidelity" frontend/src/components/workflows/PublishGauntlet.tsx` returns a line inside the STAGES table.
    - `grep -n "i === 5" frontend/src/components/workflows/PublishGauntlet.tsx` returns NOTHING.
    - The retargeted running-highlight test asserts exactly one amber node box.
    - `cd frontend && npx vite build` exits 0 (proves the new icon slug resolves).
    - `cd frontend && npx vitest run` — full suite; failures within the SEED-056 rot band and
      total count non-decreasing. Record both counts.
    - `git diff --stat frontend/package.json` is empty.
  </acceptance_criteria>
  <done>The spine places `grounding_fidelity`, the pulse is derived, and F7 + F18 are both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `publish_service` verdict → the rendered spine | server-owned verdict vocabulary crossing into a client that must not re-derive it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-10-01 | Spoofing (false status) | `GauntletSpine` node tones | mitigate | The F7 `unknownBlock` property is preserved unchanged and re-asserted; adding a row must not turn the fail-closed read into a per-code special case. F18 drives every server stage AND F7 keeps driving a genuinely unknown one. |
| T-186-10-02 | Information disclosure | the new `what` gloss | mitigate | The row's copy is plain-language and names no internal identifier; the server's raw `blocked_stage` continues to appear only inside the raw-verdict disclosure, which the existing F7 second test pins. |
| T-186-10-03 | Repudiation | the running-node pulse | mitigate | The pulse is derived from the Golden run row rather than an index literal, so a future insertion cannot make the surface claim a different stage is running than the one that is. The retargeted test asserts exactly one amber node. |
| T-186-10-SC | Tampering | npm installs | accept | No dependency is added — the icon comes from the already-installed `unplugin-icons` / `~icons/fluent-emoji` set. Verified by an empty `git diff --stat frontend/package.json`. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx` — 0 failures.
- `cd frontend && npx vite build` — exit 0.
- `cd frontend && npx vitest run` — full suite, counts non-decreasing.
- Newly observable manually (already a row in `186-VALIDATION.md`, do not create a task):
  row 3 ("Publish race") — the spine now shows a placed block for a grounding refusal instead
  of an all-grey spine.
</verification>

<success_criteria>
- Every stage `publish_service.py` can emit either places a block on exactly one spine node or
  is in a one-element, evidence-justified exclusion set.
- `grounding_fidelity` renders as a red `Grounding` node between `Pause` and `Golden run`.
- No hard-coded stage index survives in `GauntletSpine`.
- F7 unchanged and green.
</success_criteria>

<output>
Create `.planning/phases/186-concurrency-autosave/186-10-SUMMARY.md` when done. It MUST record:
the F18 RED output, the full extracted stage list the test read out of `publish_service.py`,
which icon slug resolved, and the frontend test counts before and after.
</output>
