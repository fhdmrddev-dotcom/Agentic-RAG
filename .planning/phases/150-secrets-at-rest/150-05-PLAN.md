---
phase: 150-secrets-at-rest
plan: 05
type: execute
wave: 2
depends_on: ["150-01"]
files_modified:
  - backend/app/api/admin.py
  - backend/tests/test_150_backpressure_secrets.py
  - frontend/src/lib/api.ts
  - frontend/src/components/admin/HealthSignals.tsx
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "GET /admin/backpressure returns an additive secrets_encryption block with state encrypted/plaintext/error (+ columns_unreadable / columns_plaintext on error), derived from encryption_status over the RAW (ciphertext) app_settings row (D-150-02)"
    - "A focused pytest case asserts get_backpressure()'s payload carries secrets_encryption with a valid three-state shape (Nyquist: the new field is directly exercised, not only the pre-existing backpressure regression)"
    - "The Control Plane health board renders a secrets_encryption tile: encrypted=green, plaintext(no key)=NEUTRAL grey (not red), error=red (N secrets unreadable and/or not-encrypted) (D-150-02; Pitfall 6)"
    - "The existing backpressure fields and dependency tiles stay byte-identical; the new field is optional/back-compat on the frontend (D-078-08 additive-only)"
    - "The raw technical field name is revealed under the Technical-names toggle (two-audience reveal)"
  artifacts:
    - path: backend/app/api/admin.py
      provides: "secrets_encryption appended to the get_backpressure payload"
      contains: "secrets_encryption"
    - path: backend/tests/test_150_backpressure_secrets.py
      provides: "focused pytest proving get_backpressure's payload carries the additive secrets_encryption three-state field"
      contains: "secrets_encryption"
    - path: frontend/src/components/admin/HealthSignals.tsx
      provides: "secrets_encryption tile with three-state dot/label + raw name reveal"
      contains: "secrets_encryption"
    - path: frontend/src/lib/api.ts
      provides: "optional secrets_encryption field on BackpressureSignals"
      contains: "secrets_encryption"
  key_links:
    - from: backend/app/api/admin.py
      to: backend/app/security/secret_cipher.py
      via: "encryption_status(raw_row) over the un-decrypted app_settings row"
      pattern: "encryption_status"
    - from: frontend/src/components/admin/HealthSignals.tsx
      to: "GET /admin/backpressure secrets_encryption"
      via: "signals.secrets_encryption tile render"
      pattern: "secrets_encryption"
---

<objective>
Wire the D-150-02 operator-visible encryption-state signal onto the EXISTING Phase-147 Control Plane dependency-health board — the phase's only frontend touch (one tile on an existing board, no new surface). encryption_status() (built in Plan 01) rides the same additive path as the Phase-147 dependencies block: appended to GET /admin/backpressure, typed optional on BackpressureSignals, rendered as a tile alongside Redis/Supabase/sandbox.

Purpose: D-150-02 second half (the first half — startup logs — lands in Plan 04). Operators see encrypted / plaintext (no key set) / N secrets unreadable-or-not-encrypted without any new page. NEUTRAL-not-red for the deliberate no-key config (Pitfall 6) — only genuine decrypt failures (or a swallowed sweep leaving plaintext under an active key) are red.
Output: admin.py payload append + a focused backpressure test, api.ts type extension, HealthSignals.tsx tile.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-secrets-at-rest/150-CONTEXT.md
@.planning/phases/150-secrets-at-rest/150-RESEARCH.md
@.planning/phases/150-secrets-at-rest/150-PATTERNS.md
@backend/app/api/admin.py
@frontend/src/components/admin/HealthSignals.tsx
@frontend/src/lib/api.ts

<interfaces>
<!-- From Plan 01 (built). Pure — pass the RAW app_settings row (ciphertext, pre-decrypt). -->
from app.security.secret_cipher import encryption_status
  encryption_status(row) -> {"state": "encrypted" | "plaintext" | "error", "columns_unreadable"?: int, "columns_plaintext"?: int}
  # error carries columns_unreadable (undecryptable ciphertext) and/or columns_plaintext (a non-enc:v1: value while a cipher is active = a swallowed D-150-03 sweep failure)

<!-- The RAW-row loader (returns ciphertext; do NOT use load_app_settings() which decrypts). -->
from app.models.user_settings import _load_settings_from_db   # 30s-cached RAW row (ciphertext post-Plan-03)

<!-- Existing additive precedents to copy exactly. -->
admin.py get_backpressure :185-198 — dependencies = await probe_dependencies() then appended to the return dict
api.ts BackpressureSignals :3604-3618 — the optional dependencies?: {...} additive block
HealthSignals.tsx :100-213 — DEP_ORDER tiles, DEP_DOT (:117-123), DEP_STATUS_LABEL (:127-133), the showTechnical reveal (:201-209); sandbox off = bg-muted-foreground/40 NEVER bg-destructive (Pitfall 6)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append secrets_encryption to the /admin/backpressure payload (+ focused field test)</name>
  <files>backend/app/api/admin.py, backend/tests/test_150_backpressure_secrets.py</files>
  <read_first>
    - backend/app/api/admin.py:170-199 (get_backpressure — the additive dependencies append shape to mirror)
    - backend/app/security/secret_cipher.py (encryption_status — the pure status function)
    - backend/app/models/user_settings.py:242-257 (_load_settings_from_db — the RAW-row cache loader returning ciphertext; confirm it is the raw loader, NOT load_app_settings which decrypts)
    - backend/tests/ — the EXISTING `-k backpressure` test (its invocation harness for get_backpressure: how it stubs probe_dependencies and any auth dep) — mirror it for the new focused test
    - .planning/phases/150-secrets-at-rest/150-RESEARCH.md (Control Plane signal) and 150-PATTERNS.md (admin.py section)
  </read_first>
  <action>
    In get_backpressure (admin.py), after the dependencies = await probe_dependencies() line, load the RAW global app_settings row via the settings substrate raw-row loader (_load_settings_from_db() — the 30s-cached row holding CIPHERTEXT). Do NOT call load_app_settings() / _build_settings_from_row, which decrypt and would make encryption_status misread every value as plaintext. Compute secrets_encryption = encryption_status(raw_row) and add "secrets_encryption": secrets_encryption as a NEW key in the returned dict — additive, leaving every existing key (anyio_threadpool_depth, redis_active_runs, postgres_pool_in_use, per_worker_run_count, dependencies) byte-identical. Import both symbols lazily inside the function (matches the existing lazy from app.services.health_probe import probe_dependencies at :187). Best-effort: if the raw-row load raises, degrade to a plaintext state dict — never raise out of the health endpoint (same posture as the dependency probes).
    Add a focused pytest case `backend/tests/test_150_backpressure_secrets.py::test_backpressure_carries_secrets_encryption` that invokes get_backpressure using the SAME harness the existing `-k backpressure` test uses (stub probe_dependencies + monkeypatch the raw-row loader `_load_settings_from_db` to a known dict), and asserts the returned payload contains a `secrets_encryption` dict whose `state` is one of `{"encrypted","plaintext","error"}` — directly proving the new additive field (Nyquist: the verify must exercise the new field, not only the pre-existing regression). Add a second assertion that the four existing keys + dependencies are still present so the additive contract is locked.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/ -k backpressure -q && venv/Scripts/python.exe -m pytest tests/test_150_backpressure_secrets.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - admin.py contains secrets_encryption as a returned key of get_backpressure and calls encryption_status(...) over the raw row (grep both present).
    - The four existing backpressure keys plus dependencies remain unchanged (source review — only an additive key is inserted).
    - encryption_status is fed the RAW row from _load_settings_from_db (NOT the decrypted load_app_settings) — grep shows the raw loader used for this call.
    - `tests/test_150_backpressure_secrets.py` passes and asserts payload["secrets_encryption"]["state"] ∈ {encrypted, plaintext, error} — the new field is directly proven, not inferred.
    - Importing admin.py succeeds and get_backpressure does not raise when the raw-row load is stubbed to fail (degrades to plaintext state).
  </acceptance_criteria>
  <done>The backpressure payload carries an additive secrets_encryption state block derived from the raw ciphertext row, with no change to existing fields, no way to crash the endpoint, and a focused test that asserts the new three-state field directly.</done>
</task>

<task type="auto">
  <name>Task 2: Type + render the secrets_encryption tile on the Control Plane board</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/admin/HealthSignals.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts:3600-3619 (BackpressureSignals — the optional dependencies?: {...} additive block to mirror; add the new optional field right after it)
    - frontend/src/components/admin/HealthSignals.tsx:95-216 (DEP_ORDER, depStatus, DEP_DOT, DEP_STATUS_LABEL, the two tile grids, the showTechnical raw-name reveal; the sandbox off NEUTRAL-not-red pattern at :104-133)
    - .planning/phases/150-secrets-at-rest/150-PATTERNS.md (HealthSignals section — the three-state mapping) and 150-RESEARCH.md (Control Plane signal)
  </read_first>
  <action>
    In api.ts, extend BackpressureSignals additively (right after the dependencies?: block, ~:3618) with an OPTIONAL field: secrets_encryption?: an object with state of "encrypted" | "plaintext" | "error", an optional columns_unreadable number, AND an optional columns_plaintext number. Comment it Phase 150 (SEC-01, D-150-02, additive/back-compat) so a backend that has not shipped this stays byte-compatible.
    In HealthSignals.tsx, render ONE secrets_encryption tile mirroring the DEP_ORDER tile markup (dot + plain label + sub + showTechnical raw-name reveal). Map the three states to the EXISTING vocabulary (Pitfall 6 — a deliberate no-key config is NEUTRAL, never red): encrypted maps to dot bg-success and label "Encrypted" (sub e.g. "Secret keys encrypted at rest"); plaintext (no key) maps to dot bg-muted-foreground/40 (NEUTRAL — the same class the sandbox off state uses) and label "Plaintext (no key set)"; error maps to dot bg-destructive with an HONEST label derived from whichever counter is present — "N secrets unreadable" from columns_unreadable and/or "M secrets not encrypted" from columns_plaintext (the lingering-plaintext / swallowed-sweep case); when both counts are present, combine them (e.g. "N unreadable, M not encrypted"). When signals.secrets_encryption is absent (loading or an older backend), render a NEUTRAL unknown placeholder — never a crash, never red. Under showTechnical, reveal the raw field name secrets_encryption.state (matching the dependencies.{key} reveal shape at :201-209). Place the tile in a small row after the dependency grid (or extend the dependency grid) — do not disturb the existing four-signal grid or the three dependency tiles.
  </action>
  <verify>
    <automated>cd frontend && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - api.ts BackpressureSignals has an OPTIONAL secrets_encryption field with the three-state union plus optional columns_unreadable AND columns_plaintext (grep present; the field is optional so existing callers still typecheck).
    - HealthSignals.tsx renders a tile keyed on signals.secrets_encryption with the three state-to-dot/label mappings; the error label reflects columns_unreadable and/or columns_plaintext; the plaintext state uses bg-muted-foreground/40 and NEVER bg-destructive (grep the tile block).
    - npx vite build exits 0 (no new type errors introduced by the additive field).
    - An absent secrets_encryption renders a neutral placeholder (no crash, no red) — source review of the fallback branch.
    - The existing four-signal grid and three dependency tiles are untouched (source review — only an additive tile/row).
  </acceptance_criteria>
  <done>The Control Plane board shows a three-state secrets_encryption tile (green/neutral/red per D-150-02 and Pitfall 6, with the red label honestly naming unreadable and/or not-encrypted counts) with the raw field name under the technical toggle, additively and back-compatibly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status read → operator UI | The encryption state is surfaced to operators; it must report honestly and never leak key material or a secret value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-02b | Information Disclosure | backpressure payload | mitigate | encryption_status returns only a state string + integer counts (columns_unreadable / columns_plaintext) — NO column values, NO tokens, NO key material. Task 1 feeds it the raw row but returns only counts. |
| T-150-11 | Repudiation / honesty | plaintext state rendered as red alarm | mitigate | Pitfall 6 — the deliberate no-key config is NEUTRAL grey, not red; only genuine decrypt failures OR lingering plaintext under an active key (error) are red. Task 2 acceptance greps the class. |
| T-150-03b | Information Disclosure | status read decrypts row | mitigate | encryption_status classifies by enc:v1: prefix + decrypt attempt but returns only pass/fail counts; it is fed the RAW row and never emits plaintext into the payload. |
| T-150-V4 | Elevation of Privilege (access) | /admin/backpressure exposure | accept (inherited) | Endpoint is already operator/auth-gated at the router (Phase 146/147); this phase adds no new endpoint and no new auth surface. |
</threat_model>

<verification>
- Backend: pytest -k backpressure green AND tests/test_150_backpressure_secrets.py green (asserts the additive three-state field); admin.py imports clean; get_backpressure degrades (no raise) when the raw load fails.
- Frontend: npx vite build exits 0; grep confirms the additive optional field (state + columns_unreadable + columns_plaintext) + the NEUTRAL plaintext class.
- Source review: existing backpressure keys, the four-signal grid, and the three dependency tiles are byte-unchanged.
</verification>

<success_criteria>
Operators see an honest three-state secrets-encryption tile on the existing Control Plane board (encrypted/plaintext/error), fed by the raw ciphertext row, additive and back-compatible, with the deliberate no-key config rendered NEUTRAL not red and a swallowed-sweep lingering-plaintext state surfaced red; a focused test proves the new backpressure field directly.
</success_criteria>

<output>
Create `.planning/phases/150-secrets-at-rest/150-05-SUMMARY.md` when done.
</output>
