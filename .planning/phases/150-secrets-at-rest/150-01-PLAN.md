---
phase: 150-secrets-at-rest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/security/secret_cipher.py
  - backend/app/config.py
  - backend/requirements.txt
  - backend/.env.example
  - backend/tests/test_150_cipher.py
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "get_cipher() returns a MultiFernet when SECRETS_ENCRYPTION_KEY is set, and None when it is unset (D-150-01 fail-open)"
    - "A malformed SECRETS_ENCRYPTION_KEY makes get_cipher() raise ValueError (D-150-04 fail-hard signal)"
    - "encrypt_secret() then decrypt_secret() round-trips a value; ciphertext carries the enc:v1: envelope"
    - "sweep_row() is idempotent — a second call over an all-primary-key row returns no changes (D-150-03)"
    - "MultiFernet decrypts an old-key token and sweep_row() rotates it under the primary key (D-150-06)"
    - "encryption_status() reports plaintext with no key, encrypted when all values decrypt, error with columns_unreadable when a value fails (D-150-02)"
  artifacts:
    - path: backend/app/security/secret_cipher.py
      provides: "MultiFernet cipher wrapper — the single source of key material and the encrypt/decrypt/detect/sweep/status helpers every seam imports"
      exports: ["SECRET_COLUMNS", "get_cipher", "is_encrypted", "encrypt_secret", "decrypt_secret", "sweep_row", "encryption_status"]
      min_lines: 70
    - path: backend/tests/test_150_cipher.py
      provides: "cipher unit tests: roundtrip, malformed-key ValueError, rotation, no-key passthrough, sweep idempotence, status states"
    - path: backend/app/config.py
      provides: "secrets_encryption_key env-bound Settings field"
      contains: "secrets_encryption_key"
  key_links:
    - from: backend/app/config.py
      to: "SECRETS_ENCRYPTION_KEY env var"
      via: "pydantic BaseSettings field secrets_encryption_key: str = ''"
      pattern: "secrets_encryption_key"
    - from: backend/app/security/secret_cipher.py
      to: backend/app/config.py
      via: "lazy `from app.config import settings` inside _load_keys()"
      pattern: "from app.config import settings"
---

<objective>
Build the cipher foundation for at-rest secret encryption: a single `secret_cipher` module wrapping `MultiFernet` (comma-split `SECRETS_ENCRYPTION_KEY`), an explicit `enc:v1:` envelope, and the pure helpers every downstream seam imports (encrypt/decrypt/detect/sweep/status). This is the interface-first contract plan — Plans 03/04/05 all import from this module; it must land first.

Purpose: Turn the phase from "implement a crypto scheme" into "wire two seams + a boot hook around a library that already solved encryption, rotation, and key validation" (RESEARCH §Don't Hand-Roll). All key material and every `Fernet(...)` construction live here and nowhere else.
Output: `backend/app/security/secret_cipher.py` (new module + new package dir), the `secrets_encryption_key` Settings field, the declared `cryptography>=44.0.0` dep, `.env.example` operator guidance, and the cipher unit-test suite.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-secrets-at-rest/150-CONTEXT.md
@.planning/phases/150-secrets-at-rest/150-RESEARCH.md
@.planning/phases/150-secrets-at-rest/150-PATTERNS.md

<interfaces>
<!-- The public contract this module MUST expose (consumed by Plans 03/04/05). Do NOT explore the codebase to design these — they are fixed here. -->
- SECRET_COLUMNS: frozenset[str] — the 12 secret columns, verbatim from backend/app/main.py:125-130:
  openai_api_key, anthropic_api_key, google_api_key, openrouter_api_key, ollama_api_key,
  deepseek_api_key, moonshot_api_key, minimax_api_key, zhipu_api_key,
  embedding_api_key, rerank_api_key, tavily_api_key
- _ENVELOPE_PREFIX = "enc:v1:"
- get_cipher() -> MultiFernet | None            # None when no key (D-150-01); raises ValueError on malformed key (D-150-04)
- is_encrypted(value: str) -> bool              # value.startswith("enc:v1:")
- encrypt_secret(plaintext: str, cipher: MultiFernet) -> str    # returns "enc:v1:<token>"
- decrypt_secret(value: str, cipher: MultiFernet) -> str        # strips prefix, cipher.decrypt (NO ttl); may raise InvalidToken
- sweep_row(row: dict) -> dict[str, str]        # {col: new_enc_value} for SECRET_COLUMNS needing (re)encryption; {} when converged (idempotent)
- encryption_status(row: dict) -> dict          # {"state": "encrypted"|"plaintext"|"error", "columns_unreadable"?: int}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the master-key env var, dependency, and operator guidance</name>
  <files>backend/app/config.py, backend/requirements.txt, backend/.env.example</files>
  <read_first>
    - backend/app/config.py (find the `class Settings(BaseSettings)` tail; the analog fields are `operator_emails: str = ""` at :921 and `langsmith_api_key: str = ""` at :1149 — copy that env-only-string shape)
    - backend/requirements.txt (the commented-pin convention block, e.g. the Camelot/google-genai entries around :43-55)
    - backend/.env.example (the top secrets block where provider keys live)
    - .planning/phases/150-secrets-at-rest/150-RESEARCH.md (§Config env var; §Code Examples — operator key-gen one-liner)
  </read_first>
  <action>
    Add a new field `secrets_encryption_key: str = ""` to the `Settings(BaseSettings)` class in `config.py` (pydantic-settings auto-binds the env var `SECRETS_ENCRYPTION_KEY`; `settings = Settings()` is already instantiated at config.py:1156). Comment it: Phase 150 (SEC-01) — comma-separated MultiFernet key list, FIRST key encrypts / rest decrypt-only; secret/infra → env only per CLAUDE.md; empty => D-150-01 fail-open plaintext + loud boot warning.
    Add to `requirements.txt` a declared direct dependency line `cryptography>=44.0.0` with the commented-pin form used by prior phases: `# Phase 150 (SEC-01) — Fernet/MultiFernet at-rest secret encryption`. Note: `cryptography` 46.0.7 is already transitively present in the venv, so this only promotes it to a declared dep (no build toolchain needed — wheels available).
    Add a `SECRETS_ENCRYPTION_KEY=` entry to `.env.example` near the secrets block, commented with the operator key-generation one-liner `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"` and a note that rotation = prepend the new key (`NEW,OLD`) then reboot then drop OLD. Do NOT put a real key value — placeholder/blank only.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -c "from cryptography.fernet import Fernet, MultiFernet; import app.config as c; assert hasattr(c.settings, 'secrets_encryption_key'); print('ok', c.settings.secrets_encryption_key == '')"</automated>
  </verify>
  <acceptance_criteria>
    - `app.config.settings.secrets_encryption_key` exists and defaults to `""` (empty string) when `SECRETS_ENCRYPTION_KEY` is unset.
    - `requirements.txt` contains a line matching `cryptography>=44.0.0`.
    - `.env.example` contains `SECRETS_ENCRYPTION_KEY` and the `Fernet.generate_key()` guidance; no real key material is committed.
    - `python -c "from cryptography.fernet import MultiFernet"` succeeds in the backend venv.
  </acceptance_criteria>
  <done>The env var binds to a Settings field defaulting to empty, `cryptography` is a declared dep, and `.env.example` documents the operator key-gen + rotation steps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the secret_cipher module (MultiFernet wrapper + sweep/status helpers) with unit tests</name>
  <files>backend/app/security/secret_cipher.py, backend/app/security/__init__.py, backend/tests/test_150_cipher.py</files>
  <read_first>
    - .planning/phases/150-secrets-at-rest/150-RESEARCH.md (§Pattern 1 — the full module body: _load_keys/get_cipher/is_encrypted/encrypt_secret/decrypt_secret; §Pattern 2 — the enc:v1: envelope rationale; §Pattern 4 — rotation-detection via Fernet(primary).decrypt success/InvalidToken; §Common Pitfalls 1/3/4)
    - .planning/phases/150-secrets-at-rest/150-PATTERNS.md (secret_cipher.py section — copy the health_probe.py module-header/`__all__` convention, the lazy `from app.config import settings` idiom, and the T-081.1-04 secret-logging discipline)
    - backend/app/services/health_probe.py:1-46 (module docstring + `logger = logging.getLogger(__name__)` + `__all__` shape to mirror)
    - backend/app/main.py:123-130 (the exact 12-column `_API_KEY_COLUMNS` literal — copy it verbatim into SECRET_COLUMNS)
    - backend/tests/test_147_health_probe.py:27-50 (monkeypatch-at-boundary unit-test shape — monkeypatch `app.config.settings.secrets_encryption_key`)
  </read_first>
  <behavior>
    - test_roundtrip: with a key set, encrypt_secret("sk-abc123", cipher) starts with "enc:v1:" and is_encrypted() is True; decrypt_secret(that, cipher) == "sk-abc123".
    - test_malformed_key_raises: settings.secrets_encryption_key = "not-a-valid-fernet-key" → get_cipher() raises ValueError (raised at Fernet construction — the D-150-04 fail-hard signal).
    - test_no_key_passthrough: settings.secrets_encryption_key = "" → get_cipher() returns None; encryption_status({"embedding_api_key": "sk-x"}) == {"state": "plaintext"}.
    - test_rotation: keys = "<NEW>,<OLD>" (two generated Fernet keys, new first); a token encrypted under OLD decrypts via the MultiFernet; sweep_row({"embedding_api_key": "enc:v1:<old-token>"}) returns a changed value whose token IS decryptable by Fernet(NEW) alone; a second sweep_row over the rotated row returns {} (converged/idempotent).
    - test_sweep_encrypts_plaintext: sweep_row({"embedding_api_key": "plainsecret", "rerank_api_key": ""}) returns {"embedding_api_key": "enc:v1:..."} (empty string skipped); second call returns {} (idempotent); the returned value decrypts back to "plainsecret".
    - test_encryption_status_error: a value encrypted under a key NOT in the current env → encryption_status(row) == {"state": "error", "columns_unreadable": 1}.
  </behavior>
  <action>
    Create the new package dir `backend/app/security/` with an empty `__init__.py`, and the module `secret_cipher.py`. Implement exactly the public contract in the plan's `<interfaces>` block:
    - `_ENVELOPE_PREFIX = "enc:v1:"` and `SECRET_COLUMNS` = the 12-column frozenset copied verbatim from main.py:123-130. This module becomes the SINGLE source of the secret-column allowlist; Plan 04 re-points main.py to import it.
    - `_load_keys()` reads `app.config.settings.secrets_encryption_key` LAZILY (import inside the function so tests can monkeypatch), `.strip()`, comma-splits, drops blanks; FIRST element encrypts, all decrypt.
    - `get_cipher()` returns `None` on empty list, else `MultiFernet([Fernet(k) for k in keys])` — the `Fernet(k)` call is what raises `ValueError` on a malformed key; do NOT catch it here (the boot caller in Plan 04 re-raises to refuse start).
    - `encrypt_secret`/`decrypt_secret` per §Pattern 1 — decrypt strips the prefix and calls `cipher.decrypt(token)` with NO `ttl` (Pitfall 4: a ttl would make aging secrets undecryptable).
    - `sweep_row(row)` iterates `SECRET_COLUMNS`; for each present non-empty string value: if not is_encrypted → encrypt under primary; elif is_encrypted but `Fernet(primary).decrypt(token)` raises InvalidToken → `MultiFernet.rotate(token)` re-wrap under primary (Pitfall 3: NEVER blind-rotate every value every boot); else skip. Return only changed `{col: enc_value}`.
    - `encryption_status(row)` calls get_cipher(); None → `{"state": "plaintext"}`; else count `SECRET_COLUMNS` entries that are is_encrypted yet raise InvalidToken on decrypt → `{"state": "error", "columns_unreadable": N}` when N>0 else `{"state": "encrypted"}`.
    Secret-logging discipline (T-081.1-04): any log line emits column NAMES + counts only — NEVER a plaintext value or a token. Close the file with `__all__`.
    Write `tests/test_150_cipher.py` covering every case in `<behavior>`, generating real keys via `Fernet.generate_key().decode()` and monkeypatching `app.config.settings.secrets_encryption_key`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_150_cipher.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_150_cipher.py` passes all six behavior cases.
    - `secret_cipher.py` exports SECRET_COLUMNS, get_cipher, is_encrypted, encrypt_secret, decrypt_secret, sweep_row, encryption_status (assertable via `from app.security.secret_cipher import ...`).
    - `SECRET_COLUMNS == frozenset(...)` matches the 12 names in main.py:125-130 exactly (grep both, set-equal).
    - No `Fernet(` construction exists anywhere except inside `secret_cipher.py` (grep `Fernet(` across `backend/app/` returns only secret_cipher.py).
    - No log statement in secret_cipher.py interpolates a value/token — only column names + counts (source review + grep for `%s` args are column vars, not value vars).
    - `decrypt` is called WITHOUT a `ttl=` argument anywhere (grep `ttl=` in secret_cipher.py returns nothing).
  </acceptance_criteria>
  <done>The cipher contract is implemented and unit-proven: round-trip, malformed-key ValueError, MultiFernet rotation, no-key passthrough, idempotent sweep, and the three encryption_status states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | The master key crosses from infra (env var) into the process; it must never leave (no logs, no DB, no audit). |
| process → logs | Crypto helpers emit diagnostics; secret values/tokens must never cross into log sinks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-02 | Information Disclosure | secret_cipher.py logging | mitigate | Every crypto/sweep log emits column NAMES + counts only (T-081.1-04); Task 2 acceptance greps that no value/token is interpolated. |
| T-150-05 | Tampering / Repudiation | get_cipher() key construction | mitigate | Malformed key raises ValueError at `Fernet()` construction — NOT caught here; Plan 04 boot re-raises (fail-hard). Task 2 test_malformed_key_raises locks it. |
| T-150-06 | Denial of Service | key rotation | mitigate | MultiFernet decrypts under any listed key; sweep_row rotates to primary only when needed (Pitfall 3). test_rotation locks converge-to-idempotent. |
| T-150-P5 | Information Disclosure | master-key storage | mitigate | Key read ONLY from `SECRETS_ENCRYPTION_KEY` env (Pitfall 5); never stored in DB / never auto-generated. `.env.example` holds a placeholder, not a key. |
| T-150-SC | Tampering | `cryptography` install (supply chain) | mitigate | RESEARCH §Package Legitimacy Audit = `[VERIFIED: PyPI + slopcheck OK]`, PyCA reference lib, ~13 yrs / ~300M dl/mo — APPROVED, no `[ASSUMED]`/`[SUS]` gate needed; no postinstall network risk (Python, not npm). |
</threat_model>

<verification>
- `pytest tests/test_150_cipher.py -x` green.
- `python -c "import app.config"` and `python -c "from app.security.secret_cipher import get_cipher, sweep_row, encryption_status, SECRET_COLUMNS"` both import clean.
- Grep confirms `Fernet(` appears only in secret_cipher.py across `backend/app/`.
</verification>

<success_criteria>
The cipher module is the single source of key material and exposes the full encrypt/decrypt/detect/sweep/status contract, unit-proven across all six behaviors; the env var + dependency + operator docs are declared. Downstream plans can import without exploring.
</success_criteria>

<output>
Create `.planning/phases/150-secrets-at-rest/150-01-SUMMARY.md` when done.
</output>
