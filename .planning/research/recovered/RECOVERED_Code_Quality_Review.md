# Agentic RAG ÔÇö Comprehensive Code Quality Review**Review Date:** 2026-04-25  **Scope:** Backend API, context window, SSE streaming, frontend state management, type safety, test coverage  **Status:** ­ƒö┤ **Issues Found** ÔÇö multiple reliability and correctness concerns identified---## 1. Architecture Concerns### A. Global Supabase Client Singleton Without Lifecycle Management**File:** `backend/app/dependencies.py`  **Lines:** 9ÔÇô16The `get_supabase()` function creates a module-level singleton `_supabase` client with no shutdown hook. Supabase-py uses `httpx` under the hood, which maintains persistent HTTP connection pools. In production (uvicorn with multiple workers), each worker process holds orphaned connections that are never explicitly closed during graceful shutdown or worker reload.**Impact:** Connection pool exhaustion under load; memory leaks during hot reloads.  **Fix:** Register a `@app.on_event("shutdown")` (or `lifespan` context manager) that calls `await _supabase.aclose()` or explicitly disposes the client.---### B. Context-Window Trimming Fails When Protected Messages Alone Exceed Budget**File:** `backend/app/services/context_window.py`  **Lines:** 211ÔÇô217The `trim_messages_to_fit()` function guarantees that the last `reserve_recent` messages are **always** preserved. When those protected messages alone exceed `max_tokens`, the function still returns them (plus the system prompt and a trim marker). The result violates the stated contract that messages must fit within `max_tokens`.```python# Lines 211-217: detects the overrun but does not resolve itprotected_only_tokens = (    estimate_messages_tokens(_build_candidate(system_msg, [], protected, False))    if not trimmed_any and trimmable == []    else 0)if trimmed_any or (trimmable == [] and protected_only_tokens > max_tokens):    trimmed_any = True```**Impact:** LLM API calls with context exceeding model limits, causing runtime `context_length_exceeded` errors that degrade user experience.  **Fix:** When `protected_only_tokens > max_tokens`, either raise an explicit error ("Conversation too long ÔÇö start a new thread") or progressively drop the oldest protected messages until the budget is satisfied.---### C. Token Estimation Heuristic Is Overly Aggressive for Tool Calls**File:** `backend/app/services/context_window.py`  **Lines:** 136ÔÇô139```pythontool_calls = msg.get("tool_calls")if tool_calls:    total += max(1, len(json.dumps(tool_calls)) // 3)```JSON-heavy tool calls are estimated at ~3 chars/token. For `cl100k_base`, typical JSON tokenizes closer to 3.5ÔÇô4 chars/token. Using `// 3` systematically **overestimates** tool-call tokens by 15ÔÇô30%. Combined with the already-conservative `// 4` fallback for non-OpenAI providers, this causes premature trimming of valid conversation history.**Impact:** Users lose context earlier than necessary; multi-turn tool workflows degrade.  **Fix:** Calibrate against real tiktoken counts for representative JSON payloads, or use `// 4` consistently with the fallback heuristic.---## 2. Reliability Issues### A. `except Exception: pass` Swallows Critical Failures During Cleanup**File:** `backend/app/api/threads.py`  **Lines:** 245ÔÇô247```pythontry:    # ... cleanup sandbox files ...except Exception:    pass  # Best-effort cleanup ÔÇö don't block thread deletion```While the intent is to avoid blocking deletion, this pattern silently swallows **all** exceptions including database connection failures, network timeouts, and permission errors. A failing Supabase storage call here could indicate a systemic outage that operators need to know about.**Fix:** Log at `WARNING` level with the exception details:```pythonexcept Exception as exc:    logger.warning("Sandbox cleanup failed for thread %s: %s", thread_id, exc)```---### B. Storage Upload Failure Silently Accepted**File:** `backend/app/api/documents.py`  **Lines:** 257ÔÇô264```pythontry:    supabase.storage.from_("documents").upload(        path=storage_path, file=raw, file_options={"content-type": mime_type}    )except Exception:    pass  # Storage upload failure doesn't block ingestion```If storage upload fails, the `documents` row is still created and the background ingestion task is scheduled. However, `reingest_document()` (line 448) depends on being able to download the raw file from storage. A failed initial upload means reingest will permanently fail with a 502 error, yet the document appears healthy in the UI (status: `pending` ÔåÆ `completed`).**Fix:** Mark the document status as `failed` or at least log the error and surface a warning to the user.---### C. Dedup Race Condition Allows Duplicate Processing**File:** `backend/app/api/documents.py`  **Lines:** 186ÔÇô204The duplicate check queries for `status="completed" AND is_latest=True`. If two identical files are uploaded concurrently:1. Request A checks dedup ÔåÆ no match (no completed row yet).2. Request B checks dedup ÔåÆ no match.3. Both insert `pending` rows and schedule `ingest_document` background tasks.4. Both tasks insert duplicate chunks into `document_chunks`.There is no database-level unique constraint on `content_hash`, and no advisory lock or atomic compare-and-swap on the `pending` ÔåÆ `processing` transition.**Fix:** Add a partial unique index on `(user_id, content_hash, folder_id) WHERE status != 'failed'`, or use a `processing` flag with an atomic `UPDATE ... WHERE status = 'pending'` at the start of `ingest_document()`.---### D. `ingest_document` Not Re-entrant ÔÇö Concurrent Reingest Risk**File:** `backend/app/api/documents.py`  **Lines:** 623ÔÇô712`ingest_document()` does not check whether another instance is already processing the same `document_id`. A rapid double-click on "Reingest" (or a reingest called while the initial background task is still running) causes two concurrent chunk-insertion streams into the same `document_id`. Because `document_chunks` has no unique constraint on `(document_id, chunk_index)`, duplicate chunks are created.**Fix:** At the top of `ingest_document`, atomically update `status = 'processing'` with a check that `status != 'processing'`, and bail early if no rows were updated.---### E. SSE Cleanup Misses `asyncio.CancelledError`**File:** `backend/app/responses.py`  **Lines:** 115ÔÇô136`SSEStreamingResponse.__call__` catches `OSError`, `AssertionError`, and `RuntimeError`, but **not** `asyncio.CancelledError`. When uvicorn cancels the ASGI task (e.g., timeout or server shutdown), `CancelledError` bypasses the `stop_event.set()` calls. The underlying generator may continue consuming the LLM stream because the iterator's `__anext__` only checks `stop_event.is_set()`.**Fix:** Add an explicit `except asyncio.CancelledError:` block that sets `stop_event` and re-raises.---### F. Frontend `loadMessages` Called After Unmount**File:** `frontend/src/hooks/useMessages.ts`  **Lines:** 295ÔÇô302```typescriptsetMessages((prev) => {  // ...  if (!wasStoppedByUser) {    setTimeout(() => {      stoppedByUserRef.current = false    }, 0)  } else {    setTimeout(() => {      stoppedByUserRef.current = false      loadMessages(threadId).catch(console.error)    }, 800)  }  return updated})````loadMessages` is scheduled inside `setTimeout` within a React state updater. If the component unmounts before 800 ms elapse, `loadMessages` will still fire and attempt `setMessages` on an unmounted component. React will emit a memory-leak warning.**Fix:** Track a `mountedRef` and guard the `setTimeout` callback, or use an `AbortController`/`cleanup` pattern.---### G. `useDocuments` Realtime Channel Setup Race**File:** `frontend/src/hooks/useDocuments.ts`  **Lines:** 25ÔÇô74The `useEffect` calls `loadDocuments()` immediately, then asynchronously calls `supabase.auth.getSession()` to set up the Realtime channel. If the user signs out and back in rapidly, the old channel may not be removed before the new one is created because `channelRef.current` is only overwritten after `subscribe()` resolves.**Fix:** Move channel cleanup into the `return` cleanup function and ensure `removeChannel` is awaited or at least invoked synchronously before re-subscribing.---## 3. Performance Bottlenecks*(Out of scope for v1 critical review, but noted for roadmap)*### A. Context Trimming Is O(n┬▓) in Message Count**File:** `backend/app/services/context_window.py`  **Lines:** 196ÔÇô206Each iteration of the while loop calls `estimate_messages_tokens()`, which itself iterates over all messages. For a 100-message history being trimmed down to 50 messages, this is ~2,500 token-estimation operations. With tiktoken encoding, each operation requires a full pass over message content.**Recommendation:** Cache per-message token counts and incrementally subtract removed messages rather than re-estimating the full list on every loop iteration.---## 4. Maintainability Problems### A. Type Safety ÔÇö Frontend Relies on Unchecked `as` Assertions**File:** `frontend/src/lib/api.ts`  **Lines:** 157ÔÇô206The SSE parser makes 20+ unchecked type assertions:```typescriptconst parsed = JSON.parse(raw) as Record<string, unknown>// ...onDelta(parsed.content as string)onToolStart(parsed.name as string, parsed.args as Record<string, string>)```If the backend ever sends `content: null` (e.g., for a tool-call-only delta), `parsed.content as string` becomes `null`, which will crash string concatenation in `useMessages.ts`:```typescript// useMessages.ts line 103content: m.content + delta   // if delta is null, this throws```**Fix:** Add runtime type guards (e.g., `typeof parsed.content === 'string'`) before invoking callbacks.---### B. `ToolCall.args` Type Is Too Narrow**File:** `frontend/src/types/index.ts`  **Lines:** 28ÔÇô42```typescriptexport interface ToolCall {  args: Record<string, string>  // ...}```Tool arguments from the backend can contain numbers (`top_n: 5`), booleans (`force: true`), and nested objects (`metadata_filter: {author: "John"}`). Typing them as `Record<string, string>` forces incorrect string-casting throughout the frontend and will break any tool that expects non-string args.**Fix:** Use `Record<string, unknown>` or a discriminated union per tool type.---### C. `_persist_assistant_message` Is Double-Defined (Closure + Global Logic)**File:** `backend/app/api/threads.py`  **Lines:** 572ÔÇô605The `_persist_assistant_message` closure is called both at the normal path (line 1408) and in the `finally` block (line 1449). While the `_message_persisted` guard prevents double-insert in most cases, the closure mutates external lists (`_confidence_slot`, `unique_citations`) by reference. If an exception occurs **after** the normal-path persist but **before** `finally`, the `finally` block re-executes with potentially mutated state.**Fix:** Make `_message_persisted` an immutable sentinel or move persistence into a standalone idempotent helper function that does not depend on closure state mutation.---### D. `generate_thread_title` Swallows All Errors Without Logging**File:** `backend/app/api/threads.py`  **Lines:** 259ÔÇô292```pythondef generate_thread_title(...) -> str:    try:        # ... LLM call ...    except Exception:        return first_user_message[:40].strip() or "New Chat"```Title generation failures (including auth errors, rate limits, or provider outages) are completely silent. There is no log entry, making production debugging impossible.**Fix:** Add `logger.warning("Title generation failed, falling back: %s", exc)` before returning the fallback.---### E. `except Exception` in Auth Dependency Masks Real Bugs**File:** `backend/app/dependencies.py`  **Lines:** 24ÔÇô30```pythontry:    response = supabase.auth.get_user(token)    # ...except Exception:    raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid or expired token")```A `ConnectionError` from the Supabase server, a `TimeoutError`, or a JSON parsing bug in the Supabase SDK are all presented to the client as "Invalid or expired token". This misleads users and makes operational monitoring impossible.**Fix:** Catch `SupabaseAuthException` or similar specific exceptions for auth failure, and let infrastructure errors propagate (or log them distinctly).---## 5. Test Coverage Gaps### A. Missing Race-Condition Tests for Document Ingestion**Files:** `backend/tests/integration/test_documents.py`- No test for concurrent uploads of the same file.- No test for `reingest` while a prior ingestion is still running.- No test verifying that `document_chunks` does not accumulate duplicates across reingests.### B. Missing Context-Window Edge Cases**Files:** `backend/tests/unit/test_context_window.py`- No test for `protected_only_tokens > max_tokens` (the overrun bug above).- No test for `reserve_recent` larger than the total message count.- No test for non-OpenAI models using the chars/4 fallback during trimming.### C. Missing SSE Disconnect / Error Tests**Files:** `backend/tests/integration/test_threads.py`- Tests mock `create_streaming_chat` with a simple iterator. There is no test for:  - Client disconnect mid-stream (`GeneratorExit` handling).  - Transient provider error retry logic (`_is_transient_provider_error`).  - `finish_reason == "length"` with partial tool calls.  - Empty response retry (`_empty_retries`).### D. Frontend Hook Tests Are Shallow**Files:** `frontend/src/__tests__/hooks/useDocuments.test.ts`- No test for Realtime UPDATE/INSERT/DELETE events.- No test for `deleteDoc` with `scope="all"`.- No test for upload failure (API throws).- `useMessages` has **no test file at all**.### E. `test_api_error_guards.py` Tests Isolated Logic, Not Integration**Files:** `backend/tests/unit/test_api_error_guards.py`The file tests a local `_is_context_error()` helper, but the actual keyword-matching logic in `threads.py` (lines 1344ÔÇô1363) is slightly different and includes additional keywords (e.g., `"overloaded"`). There is no guarantee the unit-tested helper matches the production code.**Fix:** Refactor the production error-mapping logic into a standalone tested function and call it from the endpoint.---## 6. Security & Data Integrity Notes### A. Raw Bytes Read Into Memory Without Streaming**File:** `backend/app/api/documents.py`  **Lines:** 154```pythonraw = await file.read()```The entire uploaded file is read into memory before any size check. While a 50 MB cap is enforced immediately after, a malicious client could upload a multi-gigabyte file and exhaust server memory before the check runs.**Fix:** Read a bounded preview first, or use `file.read(MAX_FILE_SIZE + 1)` and reject if the result exceeds the limit.### B. `_strip_nul` Recursively Mutates Without Type Validation**File:** `backend/app/api/threads.py`  **Lines:** 607ÔÇô615```pythondef _strip_nul(obj):    if isinstance(obj, str):        return obj.replace('\x00', '')    if isinstance(obj, dict):        return {k: _strip_nul(v) for k, v in obj.items()}    if isinstance(obj, list):        return [_strip_nul(item) for item in obj]    return obj```If `obj` is a custom class instance, it falls through to `return obj` unchanged. If that object contains null bytes internally (e.g., a bytes object), PostgreSQL will still reject the insert. A `bytes` branch should be added.---## Summary of Recommendations| Priority | Issue | File | Action ||----------|-------|------|--------|| ­ƒö┤ **Critical** | Context budget overrun when protected messages exceed limit | `context_window.py:211` | Trim protected messages or raise explicit error || ­ƒö┤ **Critical** | Concurrent ingestion race ÔåÆ duplicate chunks | `documents.py:186` | Add DB unique constraint or atomic status transition || ­ƒƒí **High** | Storage upload failure silently accepted | `documents.py:263` | Log error and mark doc failed || ­ƒƒí **High** | `CancelledError` bypasses SSE stop signal | `responses.py:115` | Add `except asyncio.CancelledError` || ­ƒƒí **High** | Frontend `loadMessages` after unmount | `useMessages.ts:295` | Guard with `mountedRef` || ­ƒƒí **High** | `except Exception` masks infra errors as 401 | `dependencies.py:29` | Catch specific auth exceptions only || ­ƒƒí **High** | Type assertions crash on null SSE fields | `api.ts:157` | Add runtime type guards || ­ƒƒó **Medium** | Supabase client never closed on shutdown | `dependencies.py:9` | Add lifespan/shutdown disposal || ­ƒƒó **Medium** | Title generation errors are silent | `threads.py:291` | Add logging before fallback || ­ƒƒó **Medium** | `useDocuments` has no Realtime event tests | `useDocuments.test.ts` | Add INSERT/UPDATE/DELETE event tests || ­ƒƒó **Medium** | Missing `useMessages` hook tests | ÔÇö | Create `useMessages.test.ts` |---*Reviewed: 2026-04-25*  *Reviewer: gsd-code-reviewer*  *Depth: Deep (cross-file analysis with import tracing)*