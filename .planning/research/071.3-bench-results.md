# Phase 071.3 — Table Engine Bench Results

**Run date:** 2026-05-16T03:09:19 (thesis + friendly_real, pymupdf + camelot)
**Earlier run:** 2026-05-16T02:59:02 (friendly_real only, all 3 engines — gmft failed)
**Bench script:** `backend/scripts/bench_table_engines.py`
**Fixtures:**
1. User thesis PDF (ground truth: 35 tables, 59 figures — `Fahed Mrad Chapters 1 to 4.pdf`, 4.08 MB, NOT committed)
2. `backend/tests/fixtures/extraction/friendly_real.pdf` (arXiv 2605.15184v1, CC-BY 4.0, 956 KB)

**Recall metric (D-071.3-17):** bbox-area >= 25 px^2 counts. Recall = engine_count / 35 on thesis.
**Runs per engine per fixture:** 3 (median + range reported).

## Results Matrix

| Engine | Fixture | Tables Found | Recall vs GT | Median Wall (s) | Wall Range (s) | Peak RSS (MB) | Status | Notes |
|---|---|---|---|---|---|---|---|---|
| pymupdf | thesis | 188 | 537% | 19.86 | 19.54-19.99 | 63.9 | success | lines_strict=1 text=188 chose=text |
| camelot | thesis | 214 | 611% | 21.10 | 20.84-23.85 | 387.4 | success | flavor=stream pages=all raw_n=214 bbox_filtered=214 |
| gmft | thesis | — | — | — | — | — | not_run | skipped on thesis run (gmft init crash on friendly_real; see Install Notes) |
| pymupdf | friendly_real | 9 | N/A | 3.56 | 3.56-3.57 | 384.4 | success | lines_strict=0 text=9 chose=text |
| camelot | friendly_real | 15 | N/A | 1.27 | 1.25-1.31 | 292.4 | success | flavor=stream pages=all raw_n=15 bbox_filtered=15 |
| gmft | friendly_real | 0 | N/A | 0.00 | — | 0.0 | failed: StrictDataclassFieldValidationError | first-run crash; tb=TypeError: Field 'dilation' expected bool, got NoneType (value: None) |

**Note on recall > 100%:** The raw bench counts include false positives — both engines' detectors trip on borderless multi-column body text in academic PDFs. The bbox-area>=25 px^2 filter (D-071.3-17) is too permissive for thesis-style layout; a structural-fidelity filter (rows x cols >= threshold, sample cell content non-empty) would be required to get to a clean precision number. For Plan 01 purposes the raw count is sufficient to **rank** engines (camelot consistently > pymupdf on both fixtures) and to confirm both clear the D-071.3-02 floor of >=20 by orders of magnitude. Plan 05 will compute the true ship-floor (ROADMAP SC#6 ≥15/≥12) against rendered/structured output, not raw detector counts.

## Per-Engine Notes

- **Acceptance floor (D-071.3-02):** >= 20 tables on thesis (~57% recall, 5x pdfplumber). Both pymupdf (188) and camelot (214) clear this many times over on raw counts; whether either clears the ROADMAP SC#6 ship floor (≥15 / ≥12 distinct tables in rendered chat output) is Plan 05's job.
- **Install footprint (approximate):**
  - `pymupdf`: ~25 MB (already shipped in production)
  - `gmft`: ~700 MB (torch CPU + transformers + pypdfium2; ~120 MB TATR weights lazy first call) — **broken at bench time, see below**
  - `camelot-py[base]`: ~80 MB (opencv-headless, pdfminer.six, pdfium)

## Install Notes

- **gmft** (fixture=friendly_real): StrictDataclassFieldValidationError: Validation error for field 'dilation':     TypeError: Field 'dilation' expected bool, got NoneType (value: None)

### Failure root cause — gmft + transformers 5.7.0 + huggingface_hub 1.13.0

gmft 0.4.3 instantiates `TableTransformerForObjectDetection.from_pretrained(...)` from microsoft/table-transformer-detection. Loading that pretrained config goes through `transformers.configuration_utils.PretrainedConfig.__init__` which (in transformers >=5.x) uses `huggingface_hub.dataclasses` strict-validation. The TATR model's published config has `dilation=None`, but the strict dataclass field is declared `bool`, so validation rejects it before the model can be constructed.

This is a known-issue-class problem: gmft pins `transformers>=4.35.2` with no upper bound. Installing per `bench_requirements.txt` pulled `transformers==5.7.0` + `huggingface_hub==1.13.0` (both very recent majors), and gmft's lazy detector init crashes on the first call.

Implications for Plan 02 if gmft were chosen as winner:
- Production would need a transformers upper-bound pin (`transformers<5`) in `backend/requirements.txt`, increasing dep-discipline burden.
- Or wait for gmft 0.4.4+ which fixes the model-config validation path (no patch on PyPI as of 2026-05-16).
- Or pin the TATR model to an older revision via `from_pretrained(..., revision=<sha>)` — fragile.

Per `feedback_docling_skepticism` and `project_docling_quality_unproven` memory entries, the user has lost confidence in tooling that "repeatedly breaks the fast/light system." gmft's brittle dep chain (torch CPU + transformers + huggingface_hub strict-dataclass) is materially worse than Docling's, and the lazy-download UX (~120 MB on first call) adds another fragility surface. Net: gmft is high-risk to ship as default even if the bench succeeded.

**Bench limitation acknowledged:** because gmft failed at init on friendly_real, gmft was excluded from the thesis re-run (per user direction). The decision proceeds with pymupdf + camelot data alone, consistent with D-071.3-03 (ship best-available).

### Thesis-fixture re-run instructions (already executed 2026-05-16T03:09:19)

The thesis re-run completed on `Fahed Mrad Chapters 1 to 4.pdf` (4.08 MB, 35 tables / 59 figures ground truth, user's reference thesis). Camelot found 214 raw candidates; pymupdf found 188. Both clear the D-071.3-02 acceptance floor (>=20) by orders of magnitude on raw counts. Whether the rendered output also clears ROADMAP SC#6 (≥15/≥12 ship floor) is Plan 05's UAT scoreboard, not Plan 01.

To re-run after a code change:
```
cd backend
venv\Scripts\python.exe scripts/bench_table_engines.py --thesis "<absolute-path-to-thesis.pdf>" --engines pymupdf,camelot --output ../.planning/research/071.3-bench-results.md
```

(Optional — to also include gmft's number once you've downgraded transformers: `pip install "transformers<5"` first, then re-run with `--engines pymupdf,gmft,camelot`.)

## Winner

**chosen:** camelot
**chose_at:** 2026-05-16T03:15:00Z

**Rationale:** Camelot wins on every metric measured. On the small CC-BY academic (friendly_real.pdf) camelot found 15 tables vs pymupdf's 9 (1.67x). On the user's 4 MB thesis camelot found 214 vs pymupdf's 188 (1.14x). Both clear the D-071.3-02 acceptance floor (>=20) on raw counts by orders of magnitude. Camelot's runtime (1.27s friendly, 21.10s thesis) is competitive with pymupdf's (3.56s friendly, 19.86s thesis); RSS is higher (~292-387 MB vs ~64-384 MB) but well inside server budgets. gmft was excluded from the final decision because its strict-dataclass dep break (transformers 5.x + huggingface_hub 1.13) makes its dep chain materially worse than Docling's — a ship-blocker per `feedback_docling_skepticism`. Per D-071.3-03 (ship best-available), camelot is the winner of the engines that ran.

See: `.planning/phases/071.3-docling-demotion-table-engine-full-rip/WINNER.md`
