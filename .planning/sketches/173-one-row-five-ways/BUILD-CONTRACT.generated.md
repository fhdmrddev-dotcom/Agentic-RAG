# Sketch 173 — BUILD CONTRACT (generated)

Generated by `build.cjs`. Do not hand-edit.

- assertions run: **25**, failing: **0**
- proposed (`NEW`) nodes in one card: **27**
- ⚠ this sketch is a **DRAWING** — the decisions card exists in no component. Unlike 172, its
  central surface is not the build.

## Read from the real DOM (sketch 172's dump), never re-typed

| constant | value |
|---|---|
| `UNBOUND_KB_INVITATION` | No knowledge base · searches everything |
| `REQUIREMENT_INVITATION` | What must this workflow deliver? · required to publish |
| `REQUIREMENT_AI_MARK_LABEL` | AI-proposed |
| `REQUIREMENT_AI_MARK_EXPLANATION` | The generator proposed this line when this workflow was created. Edit it and this mark clears. |
| `SEED_RECEIPT_NOTHING_COMMITTED` | Everything else is yours to change. Nothing is saved or published yet. |
| the receipt's heading | Here's what I built — 5 steps |
| the requirement's live value | Produce a cited vendor-risk brief for each vendor under review this quarter. |

Row 3 renders the **shipped** `AI-proposed` mark markup lifted whole from the dump — the one
node on the page that is not a proposal.

## The five rows are not alike

| row | backed by | empty state |
|---|---|---|
| `kb` | stored field | **SHIPPED sentence** |
| `template` | stored, control elsewhere | proposed |
| `requirement` | stored field | **SHIPPED sentence** |
| `name` | stored, no control | proposed |
| `deliverable` | **no field** | proposed |
