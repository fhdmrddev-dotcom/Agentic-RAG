# Sketch 172 — BUILD CONTRACT (generated)

Generated by `build.cjs`. Do not hand-edit.

- assertions run: **41**, failing: **0**
- proposed (`NEW`) nodes on variant A: **33**
- D-07 rows: **5**, of which **3** already have a shipped control

| D-07 row | ask (proposed) | control today | where |
|---|---|---|---|
| `kb` | Which knowledge base should this search? | **SHIPPED** | `header · project-folder-picker` |
| `template` | Is there a template it has to fill? | **SHIPPED** | `step panel · TemplateAttachSection` |
| `requirement` | What must this workflow deliver? | **SHIPPED** | `header · business-requirement-input` |
| `name` | What should this be called? | **none** | `nowhere — the header shows the slug` |
| `deliverable` | What does it produce? | **none** | `derived — no field to write` |

## Copy read from the real DOM, never re-typed

| constant | value |
|---|---|
| `UNBOUND_KB_INVITATION` | No knowledge base · searches everything |
| `REQUIREMENT_INVITATION` | What must this workflow deliver? · required to publish |
| `REQUIREMENT_AI_MARK_LABEL` | AI-proposed |
| `REQUIREMENT_AI_MARK_EXPLANATION` | The generator proposed this line when this workflow was created. Edit it and this mark clears. |
| the requirement's live value | Produce a cited vendor-risk brief for each vendor under review this quarter. (76 chars) |
| the bound folder's name | Vendor contracts |

## Shipped regions (rendered, never redrawn)

- the drafted `<header>` of `WorkflowBuilderPage` with `visual_workflow_canvas` ON
- `SeedReceipt` at 3 sealed steps / 2 detected / 1 carried
- the graph column's `grid-rows-[auto_auto_minmax(0,1fr)]` template and its `last-child` row pin
