{
  "sketch_id": "223-the-step-inside-the-run",
  "phase": 224,
  "title": "The Step Inside The Run — where a panel-owned card belongs",
  "question": "A write_todos card is panel-owned, so it renders as a sibling beneath the run that counts it. Does it belong inside the run frame, and if so as what?",
  "views": [
    {
      "id": "today",
      "description": "The measured shipping state: header says 3 steps, rail shows 2, WRITE_TODOS below the frame, Square glyph reading as an unticked box"
    },
    {
      "id": "a",
      "description": "The card becomes step 3 on RunCard's rail. One grammar, count true by construction. Requires the RunCard extraction."
    },
    {
      "id": "b",
      "description": "Inside the frame but in its own panel-owned band. Keeps panel=now / chat=happened. Two grammars in one card."
    },
    {
      "id": "c",
      "description": "RunCard untouched; a continuation bracket groups run and siblings. No G-5 obligation; count still disagrees."
    },
    {
      "id": "d",
      "description": "DELETE the write_todos seam card. Measured redundant: panel and card both read persisted tool_calls. workspace_write also redundant (panel fetches from server). ask_user must survive."
    },
    {
      "id": "src",
      "description": "BUG-260902-07 — the References footer opens by default and its trigger is styled as prose. Today vs proposed, side by side."
    }
  ],
  "real_strings": {
    "WRITE_TODOS": "SeamCard.tsx:43 headLabel returns raw kind, :51 applies uppercase",
    "count-only payload": "SeamCard.tsx:83 receives counts only; raw payload forbidden by T-087-13",
    "Agent reached time limit": "MessageItem.tsx:799, lucide Square at 12px",
    "sibling placement": "MessageItem.tsx:841 — ADDITIVE ONLY, never a touch of RunCard internals (G-5)",
    "Track its to-dos": "toolNames.ts:114 TOOL_PHRASES, unimported by chat",
    "References open-by-default": "MessageItem.tsx:619 defaultOpen=hasInRangeMarker(...)",
    "References trigger styling": "CitationList.tsx:32-44 text-xs text-muted-foreground, 12px chevron",
    "panel derives from persisted tool_calls": "StreamsProvider.tsx:106 + workspacePanel.ts:204",
    "panel files fetch from server": "FilesSection.tsx:158 useWorkspaceFiles -> getThreadWorkspaceFiles"
  },
  "design_tokens": "frontend/src/index.css",
  "stitch": {
    "project": "projects/10591382119939539231",
    "title": "Aether Chat Column - Phase 224 - quiet run, folded sources",
    "screen": "projects/10591382119939539231/screens/a4599de5d737426da390912cfe18a062",
    "screenshot": "stitch-take.png",
    "notes": [
      "Stitch put the STOPPED status line INSIDE the run card; the sketch put it beneath. Nobody drew that variant.",
      "Stitch right-aligned each step result into its own column (6 results / chart.png) instead of an inline arrow.",
      "Palette drifted: checks and run dot came out olive-green, indigo accent barely present.",
      "The References pill reads very quiet at this scale - close to the affordance problem BUG-260902-07 is fixing.",
      "Two generate calls timed out at the tool boundary; the second still completed server-side after ~2 min."
    ]
  },
  "winner": "d",
  "winner_decided": "2026-09-02 (operator)"
}
