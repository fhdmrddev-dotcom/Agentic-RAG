---
id: SEED-038
title: Generated-Files / Artifacts Model — Unify workspace_files vs sandbox-outputs vs KB documents
status: planted
planted: 2026-05-29
planted_by: orchestrator (087 scenario-matrix round-3 testing)
trigger_when: Users are confused about "where did my file go" — OR an agent produces an artifact that lands in the chat (sandbox output link) when the user expected it in the panel (or vice-versa) — OR demand emerges for a single coherent "artifacts" view — OR a WORKFLOW produces a final artifact with no panel/download home (093 re-UAT finding #4) — OR Phase 094/095 planning (confirm artifact + download coverage; the sub-agent-file-threading backend gap)
priority: medium
tags: [frontend/panel, artifacts, sandbox, execute_code, workspace, ux, design-question, phase-087]
---

# SEED-038: Generated-Files / Artifacts Model Unification

## Context

The app currently has **three distinct file surfaces with different lifecycles, locations, and UI homes** — confirmed during 087 round-3 testing (2026-05-29):

| Surface | Created by | Stored in | Shown in | Lifecycle |
|---------|-----------|-----------|----------|-----------|
| **Workspace files** | `workspace_write` (text only) | `workspace_files` table (inline ≤256 KB, else `workspace-files` bucket), versioned | **Panel → FILES** + quiet chat pointer | Persistent, per-thread, RLS, version history |
| **Sandbox outputs** | `execute_code` (e.g. matplotlib PNG) | sandbox container → served at `/sandbox-outputs/...` | **CHAT run-card → "Output files" links** | Sandbox-ephemeral (evicted with the session) |
| **KB documents** | user upload / ingestion | `documents` table + storage | Documents page; read by `search_documents` | Persistent KB, not in the panel |

The inconsistency is real and user-visible: a chart generated by `execute_code` shows up as a **chat link** (sandbox-outputs), NOT in the panel FILES; a file written by `workspace_write` shows up in the **panel**, not chat. There's no unified "artifacts" concept, and the same conceptual thing (a file the agent made) lives in two different places depending on which tool made it.

## Why deferred

Each surface shipped independently and correctly for its own purpose: Phase 084 built `workspace_files`, the 075.x line built sandbox-output links, KB documents predate v2. Unifying them is a **design decision** (what is an "artifact"? where do generated files belong? should `execute_code` be able to persist to the workspace?), not a bug. Phase 087's scope was the panel for `workspace_files` specifically.

## Re-open trigger

Promote to a phase / design spike when **any** of:
1. Users repeatedly ask "where did the file the agent made go?" or can't find a generated chart/file.
2. An agent produces an artifact in one surface that users clearly expected in the other (e.g. a generated report they want versioned in the panel, but it's a sandbox link that expires).
3. We want `execute_code` outputs to be persistable/versioned (promote a chosen sandbox output into `workspace_files`).
4. A milestone targets a coherent "Artifacts" or "Files" hub.

## Likely shape if promoted

Directions, picked by evidence/UX research:
1. **Unified artifacts index** — one panel section (or page) listing all agent-produced artifacts regardless of origin, with provenance + lifecycle badges (persistent vs ephemeral).
2. **Promote-to-workspace** — let `execute_code` (or a user action) persist a sandbox output into `workspace_files` so it gains versioning + a panel home + download.
3. **Consistent surfacing** — at minimum, give sandbox outputs and workspace files the same card vocabulary and the same "open in panel" / download affordances so they feel like one concept.

Note: this directly intersects the tool-panel-consistency concern and SEED-037 (download + viewing).

## Phase 094 dimension — workflow artifacts + builder assets (operator, 2026-06-04, /gsd:sketch 094)

The original seed framed the three surfaces around **Deep/chat** tools. The v2.8 harness + the
v2.9 builder add **two more file moments** the unified model must cover:

5. **Workflow FINAL artifacts** — a harness phase (`llm_agent` with `execute_code`, e.g.
   `plan_execute_verify` / a template-fill phase) produces the deliverable (`.docx`/`.xlsx`/`.pdf`).
   **Where does it render / download?** Per **D-094-UNIFY** the answer is the **panel FILES section**
   (the single execution+artifact home) with download + preview, and a **link from the harness
   provenance answer card** (sketch 010) + a quiet chat pointer (sketch 009) — NOT a separate place.
   **BLOCKER (093 re-UAT finding #4, confirmed backend gap):** workflow/sub-agent `execute_code`
   artifacts fire on the **sub-agent's** stream and are **never threaded up** to the harness producer
   stream, so they don't reach the panel today (`final_output_files`/`code_execution_complete` on the
   sub stream). Surfacing them is **backend plumbing** (thread sub-agent file events up), not just UI.
6. **Workflow ASSETS (builder upload)** — a `WorkflowDefinition` owns uploaded templates/forms
   (SEED-051): the builder (sketch 013) uploads them via an **Assets** strip; they're Storage-backed,
   attached at authoring, used at run time to be filled. A *4th* file lifecycle (authoring-owned,
   not per-run, not scratch, not KB) the unified model must place.

**Locked design answer for where files live (sketch 094 direction):** ONE FILES home = the workspace
panel FILES section (sketch 004/005) for **all** produced files — Deep `execute_code` outputs +
workflow artifacts + `workspace_write` files — each with **download + preview**; chat keeps a quiet
pointer; the harness answer card links its deliverables. KB `documents` stay their own store (read by
`search_documents`), surfaced read-only. This makes SEED-037's download wire-up + this seed's
unification a **094/095 confirm-at-planning item**, with the sub-agent-file-threading gap as the
backend prerequisite.

## Related

- `.planning/phases/087-panel-ui/087-SCENARIO-MATRIX.md` — round-3 "Gaps/design questions #2" + "execute_code (success)" verified row (chart → chat link, NOT panel)
- SEED-037 — file viewing + download (the "get artifacts out / view them" half)
- `project_087_panel_followups` memory — artifacts/consistency design question
- Architecture facts in the scenario matrix: the three file concepts and their stores.
