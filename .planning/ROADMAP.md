# Roadmap: Agentic RAG

## Milestones

- ✅ **v1.0 Knowledge Base Explorer** — Phases 1–8 (shipped 2026-03-29)
- ✅ **v2.0 Agent Skills & Code Execution** — Phases 9–17 (shipped 2026-04-04)
- ✅ **v2.1 Stability & RAG Correctness** — Phases 18–25 (shipped 2026-04-11)
- ✅ **v2.2 Trust & Compliance** — Phases 26–32 (shipped 2026-04-16)
- ✅ **v2.3 Memory, Multimodal & Experience** — Phases 33–43 (shipped 2026-04-19)
- ✅ **v2.4 Stability, Polish & UX Fixes** — Phases 44–57 (shipped 2026-04-30)
- 🔄 **v2.5 SSE Concurrency & Reconnect Stability** — Phases 058–063 (in progress, started 2026-05-01)

## Phases

<details>
<summary>✅ v1.0 Knowledge Base Explorer (Phases 1–8) — SHIPPED 2026-03-29</summary>

- [X] Phase 1: Folder Schema & Core APIs (2/2 plans) — completed 2026-03-21
- [X] Phase 2: Document-Folder Integration (2/2 plans) — completed 2026-03-21
- [X] Phase 3: Ingestion UI (3/3 plans) — completed 2026-03-21
- [X] Phase 4: Navigation Tools (2/2 plans) — completed 2026-03-22
- [X] Phase 5: Search Tools (2/2 plans) — completed 2026-03-21
- [X] Phase 6: Read Tool (2/2 plans) — completed 2026-03-22
- [X] Phase 7: Explorer Sub-Agent (2/2 plans) — completed 2026-03-22
- [X] Phase 8: Folder System Enhancements (3/3 plans) — completed 2026-03-28

Full details: `.planning/milestones/v1.0-ROADMAP.md`

</details>

<details>
<summary>✅ v2.0 Agent Skills & Code Execution (Phases 9–17) — SHIPPED 2026-04-04</summary>

- [X] Phase 9: Persistent Tool Memory (1/1 plans) — completed 2026-03-29
- [X] Phase 10: Agent Skills Core (3/3 plans) — completed 2026-03-31
- [X] Phase 11: Skills LLM Integration (3/3 plans) — completed 2026-04-01
- [X] Phase 12: Skills UI (3/3 plans) — completed 2026-04-02
- [X] Phase 13: Skills Open Standard (2/2 plans) — completed 2026-04-02
- [X] Phase 14: Code Execution Sandbox (5/5 plans) — completed 2026-04-03
- [X] Phase 15: Code Output UI (2/2 plans) — completed 2026-04-03
- [X] Phase 16: Skill File Management UI (2/2 plans) — completed 2026-04-04
- [X] Phase 17: Tech Debt Cleanup (1/1 plans) — completed 2026-04-04

Full details: `.planning/milestones/v2.0-ROADMAP.md`

</details>

<details>
<summary>✅ v2.1 Stability & RAG Correctness (Phases 18–25) — SHIPPED 2026-04-11</summary>

- [X] Phase 18: Context Window Hardening (1/1 plans) — completed 2026-04-09
- [X] Phase 19: Sub-Agent Guards & API Error Visibility (1/1 plans) — completed 2026-04-10
- [X] Phase 20: Blank Response Guards (1/1 plans) — completed 2026-04-10
- [X] Phase 21: Keyword Search Folder Scope (1/1 plans) — completed 2026-04-10
- [X] Phase 22: RAG Correctness Fixes (1/1 plans) — completed 2026-04-10
- [X] Phase 23: System Prompt Quality (1/1 plans) — completed 2026-04-10
- [X] Phase 24: Infrastructure Hardening (1/1 plans) — completed 2026-04-10
- [X] Phase 25: Sub-Agent Intelligence & Model-Aware Context (1/1 plans) — completed 2026-04-10

Full details: `.planning/milestones/v2.1-ROADMAP.md`

</details>

<details>
<summary>✅ v2.2 Trust & Compliance (Phases 26–32) — SHIPPED 2026-04-16</summary>

- [X] Phase 26: Citations & Confidence — Backend (2/2 plans) — completed 2026-04-12
- [X] Phase 27: Citations & Confidence — Frontend (1/1 plans) — completed 2026-04-12
- [X] Phase 28: Document Versioning — Schema & Ingestion (2/2 plans) — completed 2026-04-12
- [X] Phase 29: Document Versioning — UI (2/2 plans) — completed 2026-04-13
- [X] Phase 30: Audit Log — Backend (2/2 plans) — completed 2026-04-14
- [X] Phase 31: Audit Log — Settings UI (2/2 plans) — completed 2026-04-14
- [X] Phase 32: Suggested Follow-Up Questions (2/2 plans) — completed 2026-04-16

Full details: `.planning/milestones/v2.2-ROADMAP.md`

</details>

<details>
<summary>✅ v2.3 Memory, Multimodal & Experience (Phases 33–43) — SHIPPED 2026-04-19</summary>

- [X] Phase 33: Cross-Thread Memory — Backend (2/2 plans) — completed 2026-04-17
- [X] Phase 34: Cross-Thread Memory — Settings UI (1/1 plans) — completed 2026-04-17
- [X] Phase 35: Multi-Modal Ingestion (4/4 plans) — completed 2026-04-18
- [X] Phase 36: Multi-Modal Query & Library UI (3/3 plans) — completed 2026-04-18
- [X] Phase 37: Knowledge Health Dashboard — Backend (2/2 plans) — completed 2026-04-18
- [X] Phase 38: Knowledge Health Dashboard — Frontend (2/2 plans) — completed 2026-04-18
- [X] Phase 39: User Feedback Loop — Backend (2/2 plans) — completed 2026-04-18
- [X] Phase 40: User Feedback Loop — Frontend (2/2 plans) — completed 2026-04-19
- [X] Phase 41: UI Redesign — Tool Call Visualizer & Citations (2/2 plans) — completed 2026-04-19
- [X] Phase 42: UI Redesign — Layout Shell & Skills (3/3 plans) — completed 2026-04-19
- [X] Phase 43: UI Redesign — Mobile & Responsive (3/3 plans) — completed 2026-04-19

Full details: `.planning/milestones/v2.3-ROADMAP.md`

</details>

<details>
<summary>✅ v2.4 Stability, Polish & UX Fixes (Phases 44–57) — SHIPPED 2026-04-30</summary>

- [X] Phase 44: SSE & Stop Reliability (1/1 plans) — completed 2026-04-23
- [X] Phase 45: Chat UX Fixes (2/2 plans) — completed 2026-04-23
- [X] Phase 46: Document Version Deletion (2/2 plans) — completed 2026-04-25
- [X] Phase 47: Document List & Upload Polish (2/2 plans) — completed 2026-04-25
- [X] Phase 48: Settings & Navigation Polish (3/3 plans) — completed 2026-04-25
- [X] Phase 49: Library Health at Scale (2/2 plans) — completed 2026-04-25
- [X] Phase 51: Context Window Management (7/7 plans) — completed 2026-04-24
- [X] Phase 52: Multi-Provider Model Routing (3/3 plans) — completed 2026-04-25
- [X] Phase 53: Cross-Provider Tool Calling Reliability (4/4 plans) — completed 2026-04-26
- [X] Phase 54: Reliable Agentic Generation (5/5 plans) — completed 2026-04-26
- [~] Phase 55: Streaming Reliability & Connection Resilience (4/5 plans) — DEFERRED (Realtime INSERT race)
- [X] Phase 56: Agent Real-Time Feedback (3/3 plans) — completed 2026-04-29
- [X] Phase 56.1: Agent Feedback Gaps (3/3 plans) — completed 2026-04-29
- [~] Phase 57: SSE Realtime Reconnect Fix (2/2 plans executed, tests unreliable) — DEFERRED → v2.5

Full details: `.planning/milestones/v2.4-ROADMAP.md`

</details>

<details open>
<summary>🔄 v2.5 SSE Concurrency & Reconnect Stability (Phases 058–063) — IN PROGRESS, started 2026-05-01</summary>

- [X] **Phase 058: Backend SSE Concurrency Fix** — Wrap blocking supabase `.execute()` calls so cross-tab requests aren't queued behind streaming agents (completed 2026-05-01)
- [ ] **Phase 059: SSE Architecture Refactor** — `asyncio.Queue` + background task + `sse-starlette` so handler lifetime decouples from agent loop lifetime
- [ ] **Phase 060: Frontend Race Fixes** — `setViewingThread` separation, `AbortController` cancellation, drop the `finally`-block reload that leaked tool-result JSON
- [X] **Phase 061: Run-Backed Streaming (Backend)** — Per-run durable stream buffer in Redis Streams; agent producer task writes tokens keyed by `run_id`; SSE handler is a *consumer* with offset cursor, lifecycle decoupled from any single HTTP request (completed 2026-05-02)
- [ ] **Phase 061.1: Run-Backed Streaming Cleanup** — Diagnose `ERR_INCOMPLETE_CHUNKED_ENCODING` consumer drop surfaced during 061 manual UAT; fix the 5 integration test-pattern races (mocks inspected before background `_shielded_finalize` completes); close out code-review WR-01 (consumer cursor `$` race), WR-04/06/07 + IN-01..04. Can run in parallel with 062.
- [ ] **Phase 062: Replay & Tail API** — `GET /threads/{id}/active-runs` returns active `run_id` + current offset; `GET /runs/{id}/stream?since={offset}` replays from offset + live-tails new tokens + emits termination event on completion
- [ ] **Phase 063: Frontend Stream Decoupling** — POST returns `run_id` immediately; frontend opens separate replay-and-tail subscription; on every (re)connect (page load, focus, visibilitychange, pageshow) query active-runs and reattach if found; multi-tab sync falls out for free
- [~] **Phase 064: Validation Harness — DEFERRED** — Originally scoped chrome MCP scripts for scenarios E/F/G/H + multi-tab sync. Deferred at v2.5 close 2026-05-09: scenarios E/F/H validated organically by Phases 067.3/067.4/067.5 user-driven UAT (5/5 GREEN cycles for empty-thread-until-refresh + cross-thread switch). G + multi-tab sync remain partially deferred to a future side-phase if ever needed. See ROADMAP entry below for full coverage table.
- [x] **Phase 065: Skills Test Infrastructure Repair** — Fix 13+ broken patches in `test_threads_skills.py` and 3 broken export tests in `test_skills_import_export.py` so the next milestone (Skill Studio) starts on a green test foundation. *(Closed 2026-05-09 — VERIFICATION 4/4 must-haves verified, re-verified after Plan 065-03 gap closure; combined skills test run reports 26 passed / 0 failed / 0 skipped / 0 xfailed; 058 + 059 binding gates green; 065-01 renamed `create_streaming_chat` → `create_adaptive_streaming_chat` patch targets + tuple-wrapped fakes; 065-02 fixed 3 export-side assertion drifts; 065-03 migrated 11 tests to canonical Phase 063 POST→GET-stream pattern using `_build_mock_supabase()` + per-test `thread_id` fixture; pre-existing `test_059_disconnect.py::test_normal_stream_unchanged` failure deferred to a future test-infra phase per `deferred-items.md` D-065-01-DEFER-2 — NOT a Phase 065 regression. No production code touched.)*
- [x] **Phase 066: Adaptive Run Timeouts & Lifecycle States** — Fix Gap-006 (LLM agent stops mid-iteration on complex tool-call prompts; surfaces as `GeneratorExit` in LangSmith). Replace the total-deadline `RUN_HARD_TIMEOUT_SECONDS=120` budget with a per-LLM-call timeout that resets on tool-call boundaries (matches ChatGPT/Claude's never-cap-the-agent-loop UX). Split overloaded `cancelled` terminal status into `cancelled` (user-Stop) vs `timed_out` (system limit). Capture stop-reason in `runs.error` so the UI can surface "Agent reached time limit" instead of silent stop. Restores parity with v2.4 long-tool-call behavior under the new run-backed architecture. *(Closed 2026-05-06 — VERIFICATION 7/7 must-haves verified; HUMAN-UAT `green` with project-level approval `approved` 2026-05-07; per-call timeout machinery + cancelled/timed_out lifecycle split shipped across 5 plans; live UAT 9m02s multi-step run verified Gap-006 closed at runtime. SC#6 promoted partial→green by Phase 067.1's Track A drain-into-queue fix.)*
- [x] **Phase 067: Frontend Streaming-UX Fix** — Restore real-time first-paint of streaming agent events (no manual refresh required) and close the five carry-forward UX issues (UX-067-01..05) surfaced by Phase 066's live UAT: empty-paint, "Saving response…" thrash, refresh-required first-paint, redis-consumer log noise on tab cycle, and tool-call iteration boundary surfacing. Re-runs Phase 066 Plan 05 Task 2 protocol to close out SC#6 live verification. *(Closed 2026-05-07 — UAT `partial` / project-level `approved`; 5/5 plans landed; UX-067-01..05 all green via Chrome MCP; SC#6 4-of-5 sub-criteria green, LangSmith trace hygiene red → Gap-007 escalated to **Phase 067.1**.)*
- [x] **Phase 067.1: Agent Streaming & Behavior Polish** — Close out Gap-007 (D-066-11 `stream.close()` invariant violated under synthetic-timeout: `GeneratorExit` at `run_helpers.py:1680` instead of clean `TimeoutError`) plus 3 deep-UAT findings: (a) replace generic "Thinking…" placeholder with context-aware in-flight copy ("Searching knowledge base…", "Setting up agent…", "Spinning up sandbox…") driven by `iteration_start` + `tool_preparing` event payloads; (b) system-prompt redesign so the agent executes clear multi-step pipelines without intermediate confirmation prompts (Claude.ai-like agentic behavior, customized for RAG); (c) skill-load tool card copy clarity ("Loading skill X for next operation: Y"). Closes the SC#6 deferral cleanly and tightens the user-perceived streaming feel. *(Closed 2026-05-07 — Track A drain-into-queue shipped; Phase 066 SC#6 promoted partial→green; SC#3 green-with-note (3/5 strict, 5/5 substantive); 6 lived-experience gaps reported post-closure → escalated to **Phase 067.2**.)*
- [x] **Phase 067.2: Streaming Render & Storage Fixes** — Close 6 lived-experience gaps reported after Phase 067.1: (1) empty-render-until-cycle-finishes on multi-step new-thread runs (`guardedSetMessages` race in `useMessages.ts:623` + late `setViewingThread` in `ChatArea.tsx:79`); (2) F5 / navigation mid-stream empties UI (same race on reconcile path `useMessages.ts:866`); (3) Storage signed-URL `InvalidJWT` after 1h (`sandbox_service.py:138` hardcoded `create_signed_url(path, 3600)`); (4) confidence + suggestions not rendered (likely render-side gap in `MessageItem.tsx`); (5) chat title stays "New Chat" on run failure (`threads.py:2398-2408` runs only on success path); (6) multi-provider live-coverage gap (Anthropic + OpenRouter Kimi/MiniMax untested for Track A + multi-step pipeline). *(Closed 2026-05-09 via cross-phase chain — Initial closing UAT 7G/3R/3D 2026-05-08 escalated to 067.3. Downstream phases closed all gate-blocking rows: 067.3 closed R-1 (cross-thread switch via per-thread `messagesByThread` Map), R-2 (sandbox-output JS blob fetch+download), R-2-IDOR (path-segment fence), N-01 (model→provider router); 067.4 closed R-3 (suggestion pills emit GREEN at Row 1, run aa92f90e); 067.5 closed Row 11 empty-thread-until-refresh regression (5/5 cycles GREEN, Branch D-3-CLEAR-WIPES fix). 067.2-11/12 OpenRouter synthetic-timeout rows remain user-deferred — non-blocking, carried forward in seeds.)*
- [x] **Phase 067.3: Streaming Render & Storage Fixes — Round 2** — Close the 3 RED rows + 1 HIGH new finding from Phase 067.2's user-driven UAT: **R-1** Row 3 D-067.2-02b cross-thread switch loses streaming render on the thread you return to (per-thread streaming cache — explicitly out-of-scope for 067.2, now in scope); **R-2** Row 4 D-067.2-03a `/sandbox-outputs/{path}` returns 401 on browser `<a href>` click because the endpoint reads `Authorization: Bearer` header but anchor clicks send only cookies (fix: token-in-URL signed JWT in query param OR JS blob fetch+download); **R-3** Row 6 D-067.2-04 confidence renders but suggestion pills don't (post-`done` block at `threads.py:2477` — investigate whether emit silently errors, returns empty, or frontend gate filters render); **N-01** (HIGH) model→provider router at `threads.py:949` uses `user_settings.active_provider` ignoring `MODEL_CAPABILITIES[model]["provider"]`, so Anthropic models routed through OpenAI SDK return 404 (~5-line fix). *(Closed 2026-05-09 via cross-phase chain — Initial closing UAT 9G/2R/2D 2026-05-09 escalated to 067.4. R-1, R-2, R-2-IDOR, N-01 all GREEN at 067.3 closure. R-3 mirror + 067.2-6 mirror RED rows closed in 067.4 (Row 1 GREEN, run aa92f90e — DOM 3 question buttons + SSE 1× suggestions event with non-empty questions ordered before `done`). 067.2-11/12 OpenRouter synthetic-timeout rows remain user-deferred. UAT-discovered R-4 + R-5 escalated to 067.4 and shipped GREEN there.)*
- [x] **Phase 067.4: Streaming Suggestions, Tool-Stage Render & Code-Execution Stream** — Close the 2 RED rows + 2 carry-forward deferred rows from Phase 067.3's user-driven UAT, plus the 2 UAT-discovered findings: **R-3** (gate-blocking, mirrored 067.2 Row 6) suggestion pills not emitted to SSE; SSE replay shows `[title, iteration_start, ..., confidence, done]` but no `suggestions` event for Fahed-Mrad search-only prompts. **R-4** (UAT-discovered) Active-thread UI freezes at tool-execution stage; refetch on thread-switch repairs (likely Plan 03 R-1 regression: SSE `tool_end`/`done` handlers in `useMessages.ts` may have missed the per-thread bucket update). **R-5** (UAT-discovered, additive UX) Code-execution shows only spinner with no token-by-token feedback; add `code_output_line` SSE event end-to-end, strictly additive. **NR-067.2-11/12** (carry-forward) OpenRouter Kimi 2.5 + MiniMax 2.7 synthetic-timeout regression. *(Closed 2026-05-09 — Plans 01/02/03 deliverable rows all GREEN at orchestrator-driven Chrome MCP UAT: Row 1 R-3 suggestion pills GREEN (run `aa92f90e`, 3 question buttons in DOM + SSE `suggestions` event before `done`); Row 2 R-4 active-thread tool-stage GREEN (sandbox sub-run on active thread updates without thread-switch refetch); Row 3 R-5 code-execution heartbeat GREEN (5× heartbeats + lifecycle bookends); Row 10 N-01 model→provider router GREEN (claude-sonnet-4-6 streamed via Anthropic). Row 11 (067.3-NR no-regression on 4 streaming threads) hit RED on 1/4 — empty-thread-until-refresh regression escalated to **Phase 067.5**, where Branch D-3 `clearMessages` guard fix at commit `3d040c7` closed it with 5/5 lived-experience cycles GREEN. Rows 4–9, 12 (OpenRouter synthetic-timeout protocol + remaining regression spot-checks) explicitly user-retained ownership at UAT start; orchestrator delivered all UI-driveable rows. Per cross-phase closure rule, Phase 067.4's gate is functionally met: all gate-blocking rows GREEN downstream, no contradicting evidence.)*
- [x] **Phase 067.5: Frontend Reconcile Fix (Empty-Thread-Until-Refresh)** — Close 067.4's gate-blocking Row 11 RED: one of 4 streaming threads renders empty until F5 page refresh; data IS persisted to Postgres but React in-memory state is stale because `clearMessages()` (called from `ChatArea.tsx:89-126` on every `thread.id` change) wipes the per-thread bucket while the thread is still streaming, leaving subsequent SSE callbacks no-op'd against an empty state. *(Closed 2026-05-09 — Branch D-3-CLEAR-WIPES-STREAMING-BUCKET fix in `frontend/src/hooks/useMessages.ts:572-590` makes `clearMessages` refuse to wipe a bucket whose thread is `streamingThreadIdRef.current`; total diff 14 insertions / 1 deletion at commit `3d040c7`. Closing UAT (Plan 02 Phase A) ran 5 consecutive lived-experience cycles user-driven 2026-05-09: 4/4 streaming threads rendered without F5 across all 5 cycles, 0 reproductions of the empty-thread bug, no cross-thread token leakage. R-1 cross-thread dual-pass (Phase B) + 067.4 Plans 01/02/03 GREEN regression spot-check (Phase C) skipped at user request — vitest in `frontend/src/__tests__/hooks/useMessages.test.ts` (6/6 pass post-fix) covers R-1 / R-4 / R-5 protection and the Branch D-3 fix is structurally additive to clearMessages without touching the SSE event dispatcher or per-thread bucket write contract. STREAM-04-correctness-round3 residual gap closed; v2.5 milestone gate eligible to advance. Carry-forward: cycle 5 surfaced `claude-haiku-4-5-20251001 max_tokens=65536 > 64000` cap mismatch (user clicked Resume → next run succeeded; D-063-04 working as designed); worthy of separate seed, NOT a 067.5 regression.)*

Full details below in **Phase Details**.

</details>

## v2.5 Deployment Strategy (D-v2.5-11)

**Phases 061 + 062 + 063 ship as a single feature branch merged to main as one merge commit.** No feature flags, no dual code paths, no incremental cutover.

Rationale: 061 (backend writes to Redis), 062 (replay-and-tail API), and 063 (frontend cuts over to run_id flow) form an atomic architectural change. Shipping 061 alone would leave the frontend POSTing-and-streaming the legacy way while the backend silently fills a Redis buffer no one reads — a partial state that's a bug-magnet for half a sprint. Feature flags would add toggle logic + dual code paths that must both work indefinitely. For a dev-stage, single-developer change, a long-lived `v2.5-stream` feature branch with all three phases merged together is cleaner: no half-state on main, no flag-flip incidents, the legacy POST-streams path gets deleted in 063 with no compatibility shim left behind.

**Phase 064 (Validation Harness) and Phase 065 (Skills Test Infra)** ship independently after 063 lands.

## Phase Details

### Phase 058: Backend SSE Concurrency Fix

**Goal**: While Thread A is mid-SSE-stream, concurrent authenticated requests on the same uvicorn worker complete without being queued behind the stream.
**Depends on**: Nothing (first phase of v2.5; parallel-able with 062)
**Requirements**: CONCUR-01
**Success Criteria** (what must be TRUE):
  1. With Thread A actively streaming, an authenticated `GET /threads/B/messages` request returns within 1 second (cross-tab GET <1s benchmark — currently hangs ~30s in `master`).
  2. The fix wraps every `supabase-py` `.execute()` call inside the SSE path with `starlette.concurrency.run_in_threadpool` (or equivalent `asyncio.to_thread`) so the asyncio event loop is no longer blocked by the sync DB client.
  3. The AnyIO default thread limiter is explicitly raised from 40 to 200 tokens at app startup, so the threadpool ceiling does not become the next blocker.
  4. No regression in single-thread happy path: send a normal chat, see streaming tokens with the same latency profile as v2.4 (no added per-token delay from threadpool dispatch).
**Plans**: TBD
**Risks / pitfalls** (from research §A2 and 057-DEFERRAL.md):
  - 40-thread AnyIO ceiling — wrapping every call without raising the limiter just moves the bottleneck.
  - `--workers N` is **not** an acceptable substitute (research §A4; PROJECT.md D-v2.5-02). Single worker stays in dev so the bug remains observable.
  - Sync calls inside tools (not just inside `event_stream`) can still block — audit any `.execute()` in tool dispatch paths.

### Phase 059: SSE Architecture Refactor

**Goal**: The SSE handler exits cleanly on client disconnect within 1 second and the agent loop is cancelled, eliminating wasted LLM tokens after tab close, F5, or network drop.
**Depends on**: Phase 058
**Requirements**: CONCUR-02
**Success Criteria** (what must be TRUE):
  1. The streaming endpoint uses an `asyncio.Queue` producer/consumer pattern: agent loop runs as a background task, the SSE handler only consumes the queue and yields events.
  2. The custom `SSEStreamingResponse` subclass is replaced by `sse-starlette`'s `EventSourceResponse` with `request.is_disconnected()` polling for active disconnect detection.
  3. When a client disconnects mid-stream (close tab, F5, drop network), backend logs show the agent task receives `CancelledError` within 1 second of the disconnect, no further LLM API calls fire afterward, and `CancelledError` is re-raised after cleanup (not swallowed).
  4. Stop button (existing v2.4 behavior) and partial-response persistence via `asyncio.shield` continue to work — STREAM-01/STREAM-03 do not regress.
**Plans**: 3 plans
  - [x] 059-01-PLAN.md — Wave 0: Pin sse-starlette==2.4.1 + pytest-timeout in requirements.txt; create test_059_disconnect.py with helper scaffolding (slow-mock-LLM, LLMCallCounter, _read_then_disconnect) and failing placeholders.
  - [x] 059-02-PLAN.md — Wave 1: Lift event_stream body into agent_runner producer task; wire asyncio.Queue + EventSourceResponse(ping=15) consumer; change CancelledError pass→raise; delete backend/app/responses.py.
  - [x] 059-03-PLAN.md — Wave 2: Implement test_agent_task_cancels_on_disconnect (Invariants I1-I4) + test_normal_stream_unchanged smoke; create 059-VERIFICATION.md mirroring 058 format.
**Risks / pitfalls** (from research §A3, §A5, 057-DEFERRAL.md):
  - KI-001 (in-flight LLM calls only stop at `yield` points) is structurally harder than `GeneratorExit` — confirm cancellation actually interrupts the agent loop, not just the queue consumer.
  - `asyncio.shield` end-of-stream persistence must still run; cancellation needs to flow only into the agent loop, not into the persistence write.
  - Don't re-add the OSError-catching `SSEStreamingResponse` shim once `sse-starlette` is in place — it becomes redundant and can hide real bugs.

### Phase 060: Frontend Race Fixes

**Goal**: Switching from a streaming Thread A to Thread B always renders Thread B's correct messages, with no cross-thread data leak and no raw tool-result JSON regression.
**Depends on**: Phase 059
**Requirements**: STREAM-02a
**Success Criteria** (what must be TRUE):
  1. `activeThreadIdRef` is written by exactly one function — a new `setViewingThread(threadId)` callback called from `ChatArea`'s `useEffect([thread?.id])` BEFORE `abortStream`/`clearMessages`/`loadMessages`. `loadMessages` only reads the ref post-await.
  2. `loadMessages` accepts an `AbortSignal`; a `loadAbortRef` cancels the previous in-flight `getMessages` fetch when a new `loadMessages` runs, so Thread A's pending fetch does not overwrite Thread B's data.
  3. The `loadMessages` call in `useMessages` `sendMessage`'s `finally` block is removed — streaming-format messages built up by SSE deltas remain authoritative and the Bug 3 raw-JSON regression (tool-result JSON leaking into chat content) does not recur.
  4. Browser MCP test: start streaming Thread A → click Thread B before stream ends → Thread B view shows only Thread B messages, no leak from A; Network tab shows Thread A's `getMessages` aborted.
**Plans**: 3 plans
  - [x] 060-01-PLAN.md — Wave 1: useMessages.ts surgery — setViewingThread + loadAbortRef + loadMessages rewrite + delete Phase-057 Realtime band-aids + clearMessages no longer aborts + drop finally-block reload (D-060-01..03, 05, 06, 07, 10, 11)
  - [x] 060-02-PLAN.md — Wave 2: api.ts getMessages signal parameter + ChatArea useEffect rewrite (setViewingThread→abortStream→clearMessages→loadMessages, dep array reduced, 8s timer + visibilitychange listener deleted) (D-060-04, 07b, 07c, 08, 09)
  - [x] 060-03-PLAN.md — Wave 3: Playwright e2e test at e2e/tests/060-thread-race.spec.ts + 060-VERIFICATION.md scaffold with manual two-tab DevTools backstop (D-060-12, D-060-13)
**Risks / pitfalls** (from 057-DEFERRAL.md §1, §3):
  - Don't accept the "loadMessages writes the ref then reads it" pattern again — the v2.5-dev attempt proved concurrent calls overwrite each other's guards.
  - Don't put any reload logic inside a `setMessages` updater — Strict Mode double-invokes updaters and React can bail out, making side effects non-deterministic.
  - Keep an eye on `clearMessages` — it should NOT call `abortControllerRef.abort()`; ChatArea aborts explicitly via `abortStream()` first.

### Phase 061: Run-Backed Streaming (Backend)

**Goal**: Generation lifetime is decoupled from any single HTTP request — agent task writes tokens to a durable per-run buffer; the SSE handler becomes a *consumer* of that buffer rather than the producer. Foundation for STREAM-04.
**Depends on**: Phase 059 (asyncio.Queue producer task already exists; this phase swaps the in-memory queue for a Redis Stream backing).
**Requirements**: STREAM-04 (foundation layer)
**Success Criteria** (what must be TRUE):
  1. A `runs` concept exists with a unique `run_id` per generation; the agent producer task writes every SSE event (token, tool, error, done) into a Redis Stream keyed by `run_id` with a monotonic offset.
  2. The SSE response handler reads from the Redis Stream with `XREAD STREAMS run:{id} {offset}` semantics — the handler is a thin consumer; killing it does NOT kill the producer.
  3. If the original POST connection drops (client navigates, refreshes, network blips), the agent task continues to completion and the buffer is filled. Verified via instrumentation: producer writes the full event sequence even when no consumer is attached.
  4. Run TTL: a completed run's buffer auto-expires after a configurable retention window (default 10 min); aborted/failed runs expire faster (default 60s) to bound storage.
  5. **Run history persistence (per D-v2.5-11):** a new Postgres table `public.runs` records run lifecycle metadata (`run_id`, `thread_id`, `user_id`, `message_id`, `status`, `model`, `provider`, `started_at`, `completed_at`, `input_tokens`, `output_tokens`, `error`) with full RLS. Producer task writes a row at run start and updates it at completion/failure/cancellation. This persists *after* the Redis buffer expires so audit, debugging, and future billing/usage UI have ground truth. Lands as migration `035_runs_table.sql` and is included in the regenerated `full-schema.sql`.
  6. Backend dependency: `redis>=5` added to `backend/requirements.txt`; `/health` endpoint extended with a Redis ping check that returns `{"redis": "ok" | "unreachable"}`.
  7. No regression in 058/059 binding tests: `test_058_concurrency.py::test_cross_tab_unblocked_during_sse` and `test_059_disconnect.py` still pass — single-thread happy path latency profile unchanged.
  8. Existing tests in 060's e2e harness (`060-thread-race.spec.ts`) still pass — STREAM-02a guarantees preserved.
**Plans**: 5 plans
  - [x] 061-01-PLAN.md — Foundation: redis>=5.2 dep + Settings (REDIS_URL, RUN_HARD_TIMEOUT_SECONDS) + get_redis() singleton + lifespan PING + /health Redis status
  - [x] 061-02-PLAN.md — Migration 035_runs_table.sql + apply via Supabase SQL editor + regenerate full-schema.sql
  - [x] 061-03-PLAN.md — Producer XADD rewrite + asyncio.timeout(120) + RUN_TASKS registry + two-mode XREAD consumer + finally-ordering (sentinel→UPDATE→EXPIRE→ZREM→pop) — completed 2026-05-02
  - [x] 061-04-PLAN.md — Test infrastructure: redis_client function-scoped fixture + session-end FLUSHDB + CI workflow with docker-compose Redis preamble — completed 2026-05-02
  - [x] 061-05-PLAN.md — Binding tests (D-061-15), TTL/runs/hard-timeout integration tests, D-061-16 contract inversion of test_059, 061-VERIFICATION.md — completed 2026-05-02
**Risks / pitfalls**:
  - Redis Stream is new infra. Add `REDIS_URL` env var; document Upstash free tier setup in DEPLOYMENT.md or equivalent. Confirm Redis client library choice (`redis-py` async API) early — `redis.asyncio.Redis` is the modern path.
  - Don't conflate `run_id` with `message_id`. A single run produces a single assistant message but emits many events. `run_id` is the buffer key.
  - Auto-cleanup is non-trivial: per-key TTL via `EXPIRE` works but doesn't remove individual stream entries. Use `XTRIM MAXLEN` if buffers grow large, plus `EXPIRE` for the whole key on completion.
  - LLM token cost shifts: with run-backed streaming, navigating away no longer cancels the LLM call. Mitigations (Stop button, server-side hard timeout, abandoned-run TTL) are scoped to D-v2.5-09 (to be locked in /gsd:discuss-phase 061).

### Phase 062: Replay & Tail API

**Goal**: Surface the durable run buffer from Phase 061 as a clean HTTP API so any client can reattach to a stream at the right offset and tail to completion.
**Depends on**: Phase 061
**Requirements**: STREAM-04 (API layer)
**Success Criteria** (what must be TRUE):
  1. `GET /threads/{thread_id}/active-runs` returns either an empty list or `[{run_id, started_at, current_offset, status: "streaming" | "completed" | "failed"}]` for the requesting user's thread. Backed by the `public.runs` table (Phase 061 deliverable) — query by `thread_id` + status filter — and falls back to Redis active-set check if Postgres write hasn't landed yet. RLS enforced via Supabase auth.
  2. `GET /runs/{run_id}/stream?since={offset}` opens an SSE response that: (a) replays events from `offset` (default 0) up to the current head of the stream, (b) live-tails new events as the producer writes them, (c) emits a terminal `done` (or `error`) event and closes when the producer finishes, (d) supports the same auth headers + `request.is_disconnected()` polling pattern as the existing endpoint.
  3. **`DELETE /runs/{run_id}` cancel verb**: authenticated user can cancel an in-flight run they own. Endpoint cancels the producer task (asyncio cancellation), sets the `runs.status` row to `cancelled` in Postgres, writes a terminal `cancelled` event to the Redis Stream so all attached consumers close cleanly, and returns 204. Idempotent: cancelling an already-completed/failed/cancelled run returns 204 without side effects.
  4. Two concurrent consumers of the same `run_id` each receive the full event sequence independently — multi-tab fan-out works at the API layer.
  5. Auth + RLS: a user can only query active-runs, replay streams, or cancel runs they own (verified by integration test against Supabase Auth + RLS policy on `public.runs`).
  6. The original `POST /threads/{thread_id}/messages` endpoint either returns `{message_id, run_id}` immediately (no streaming over POST) OR keeps backward-compatible streaming for a deprecation window — decision deferred to /gsd:discuss-phase 062.
**Plans**: 4 plans
  - [X] 062-01-PLAN.md — Wave 1: ActiveRunResponse Pydantic model + GET /threads/{tid}/active-runs route in threads.py + Wave 0 stubs for SC#1 + SC#5(active-runs) (D-062-02/03/04/12, T-062-01) — completed 2026-05-03
  - [X] 062-02-PLAN.md — Wave 1: New backend/app/api/runs.py module with replay_tail_consumer + GET /runs/{rid}/stream + register router in main.py + Wave 0 stubs for SC#2 (3 sub-paths) + SC#5(stream) (D-062-05/06/07/12/13/14, T-062-01/03/04) — completed 2026-05-03
  - [x] 062-03-PLAN.md — Wave 2: DELETE /runs/{rid} appended to runs.py with zombie heal + setup_zombie_state helper + Wave 0 stubs for SC#3 (3 sub-paths) + SC#5(delete) (D-062-08/09/10/11/12/13, T-062-01/02/03)
  - [x] 062-04-PLAN.md — Wave 2: SC#4 multi-consumer fan-out test + D-062-13 Redis-down tests + 062-VERIFICATION.md scaffold (SC#4, T-062-03/04)
**DEF-061.1-02 disposition**: Carried forward to a future 061.2 phase — the producer exception classifier suspected fix lives at threads.py:2057-2076 (agent_runner), a region D-062-14 physically partitions OUT of 062's scope. The misclassification only affects audit metadata (filtered out by D-062-02 active-runs SELECT WHERE status='streaming'), so 062's user-visible contract is unaffected. 062's full-suite verify inherits 061.1's canonical -k exclusion clause.
**Risks / pitfalls**:
  - SSE consumer with offset cursor: `XREAD COUNT N STREAMS run:{id} {offset}` returns immediately if events exist; need to fall through to `XREAD BLOCK ms STREAMS run:{id} $` for live tail. Two-mode loop (replay-then-tail) is the canonical pattern. *Resolved by 062-02 mirroring event_consumer (threads.py:336-423) verbatim with last_id=since parameterization.*
  - Don't leak Redis errors as 500s — wrap in domain errors and return 503 with retry hint if Redis is unreachable. *Resolved per D-062-13 differentiated degradation: stream → 503 + Retry-After: 10; DELETE → 204 even if all Redis ops fail.*
  - Active-runs query must be cheap — keep an index. *Resolved via partial index idx_runs_active (migration 035) per D-062-02; sorted-set stale entries invisible at API layer.*
  - For STREAM-04 verification, this phase is the API contract — Phase 063 wires the frontend; Phase 064 validates end-to-end.

### Phase 063: Frontend Stream Decoupling

**Goal**: Rewire the frontend so the assistant-message stream lives independently of the POST request that started it. On every (re)connect, the frontend reconciles state via `active-runs` and reattaches to the replay-and-tail endpoint. Multi-tab sync, refresh-mid-stream, and navigate-away-and-back all work as a side-effect.
**Depends on**: Phase 062
**Requirements**: STREAM-04 (frontend layer); also delivers STREAM-02b (recovery is automatic; Resume button optional fallback).
**Success Criteria** (what must be TRUE):
  1. Sending a message: `POST /threads/{id}/messages` returns `{message_id, run_id}` synchronously; the frontend then opens `GET /runs/{run_id}/stream?since=0` for the actual tokens. The POST request closes immediately.
  2. On every page load, focus, `visibilitychange`, and `pageshow`, the frontend queries `active-runs` for the current thread; if a run is found and the local state is behind, it opens `GET /runs/{run_id}/stream?since={local_offset}` and resumes rendering with no manual user action.
  3. Refresh mid-stream: F5 during streaming → page reloads → `active-runs` reports streaming run → frontend reattaches → assistant message continues animating from the offset where the local buffer left off. No "Resume" button required for the happy path.
  4. Multi-tab sync: open same thread in two tabs while streaming → both render the same tokens with no leak between threads (regression guard for STREAM-02a).
  5. Symptom G regression guard: clicking Stop sends `DELETE /runs/{run_id}` (or equivalent cancel verb) — server cancels producer, terminal event fires, all consumers close cleanly.
  6. Bug 3 regression guard: no tool-result JSON leaks into chat content; the streaming-format messages built up by SSE deltas remain authoritative (Phase 060's invariants preserved).
  7. Resume button: if `active-runs` returns a `failed` run (producer errored), surface a Resume button rather than silently retrying — preserves D-v2.5-05's principle of explicit user intent for paid LLM retries.
**Plans**: 5 plans
  - [x] 063-01-PLAN.md — Wave 0: 5 stub test files (3 backend + 2 e2e) RED-failing for contract reasons + legacy POST-SSE test audit document
  - [x] 063-02-PLAN.md — Wave 1: Backend POST contract rewrite — JSONResponse {message_id, run_id} HTTP 201 + delete event_consumer (D-063-01 hard cutover)
  - [x] 063-03-PLAN.md — Wave 2: Frontend API layer — split streamMessage into postMessage + subscribeToRun + getActiveRuns + cancelRun; extend Message type with runId/runStatus
  - [x] 063-04-PLAN.md — Wave 3: Frontend hook + components — useMessages reconcile + Stop=DELETE + resumeFromFailed; ChatArea reconcile triggers (mount/focus/visibilitychange/pageshow w/ bfcache); MessageItem Resume button
  - [x] 063-05-PLAN.md — Wave 4: Legacy-test rewrites/deletions per audit + Wave 0 timing assertion fill-in + e2e fault-injection fixture + final regression sweep
**Risks / pitfalls**:
  - Don't keep two streaming code paths (legacy POST-streams + new run-stream) longer than one phase — choose one, deprecate the other, delete dead code in this phase.
  - `active-runs` must be queried on the client *before* `loadMessages` settles, otherwise the local message list will appear "missing" the in-flight assistant message until the next reconcile tick. Wire ordering carefully.
  - Stop button semantics change: today it aborts the in-flight HTTP request; with run-backed streaming, the request is detached, so Stop must call a server endpoint to cancel the producer. Test cross-tab Stop (clicking Stop in tab B while tab A initiated the stream).
  - `pageshow` fires on bfcache restore — the local buffer may be hours stale. Treat bfcache restore as "always reconcile via active-runs," even if local state looks complete.

**Phase 063 status**: UNBLOCKED — Phase 063.1 shipped 2026-05-04 closing all 5 surfaced gaps (Gap-001..005). Live-bundle verification (Chrome MCP) confirmed Wave 2/3/4 fixes are served at `localhost:5173`. Phase 063 + 063.1 marked Complete in the Progress table; carry-forward UAT items belong to next manual UAT pass per Phase 063 precedent.

### Phase 063.1: Frontend Stream Decoupling — Gap Closure

**Goal**: Resolve the 5 gaps surfaced by Phase 063's live UAT so the navigate-away-during-stream, refresh-mid-stream, AND multi-tab-activation flows are visually clean and Redis-efficient. Confirms the Resume-button data path against a live failed run via the `ENABLE_TEST_FIXTURES=1` harness.
**Depends on**: Phase 063 (all 5 plans landed)
**Requirements**: STREAM-04 polish (no new requirement — finishes the user-visible side of STREAM-04 that Phase 063 left rough); also confirms STREAM-02b Resume-button data flow is live-correct.
**Success Criteria** (what must be TRUE):
  1. Switching threads mid-stream no longer shows a blank window on switch-back. The persisted/in-flight assistant content is visible within ≤ 200ms of switching back.
  2. There is at most ONE active SSE consumer per `run_id` per tab at any time. Verifiable in DevTools Network: navigating away and back does NOT open a second `GET /runs/{rid}/stream`. (Multi-tab fanout — one SSE per tab — is correct and stays.)
  3. SSE reattach uses `since={highest-seen-ms-id}` (or equivalent offset cursor), not hard-coded `since="0"`. The Redis Stream is NOT re-replayed from the beginning on every reconnect.
  4. Refresh-mid-stream no longer shows the duplicate-bubble flicker (Gap-001). After F5 + thread-click, exactly ONE assistant bubble exists at all times.
  5. Resume button confirmed working live: with `ENABLE_TEST_FIXTURES=1`, injecting a failed run + reload makes the Resume button render on the failed assistant message; clicking it fires a fresh POST. If the data flow is broken (verifier-flagged Gap-002), this phase wires it correctly (e.g. join `runs.status` into `loadMessages`, or surface failed runs via a new `getRecentlyFailedRuns(threadId)` endpoint).
  6. **(NEW — Gap-005)** Concurrent reconcile calls (e.g. `visibilitychange` + `focus` firing in the same tick) do NOT wipe the temp `${run_id}` placeholder. The placeholder bubble renders within ≤ 200ms of any reconcile that finds an active run, regardless of how many reconciles fire concurrently.
**Plans**: 5 plans (planned 2026-05-04 via /gsd:plan-phase 063.1; wave-serialized to avoid useMessages.ts merge conflicts)
  - [x] 063.1-01-PLAN.md — Wave 1: Backend JOIN — extend GET /threads/{tid}/messages with run_id + run_status from public.runs via two-query Python merge; extend MessageResponse Pydantic; integration tests for happy path + pre-run-backed null + cross-user RLS guard (D-063.1-13/14/15)
  - [x] 063.1-02-PLAN.md — Wave 2: Frontend offset cursor + reconcile dedup + narrowed short-circuit — add lastSeenOffsetRef Map; thread `since` cursor through reconcile call site; runId-match dedup against messagesRef.current; api.ts getMessages mapper renames snake → camel for runId/runStatus; SSE parser captures `id:` lines via onCursor callback (D-063.1-01..05, D-063.1-09) — completed 2026-05-04
  - [x] 063.1-03-PLAN.md — Wave 3: Thread-switch SSE persistence — remove abortStream() from ChatArea thread-change useEffect; wrap sendMessage's makeStreamCallbacks setMessages with guardedSetMessages gating on streamingThreadIdRef === activeThreadIdRef; audit hook unmount semantics (D-063.1-06..08, D-063.1-10) — completed 2026-05-04
  - [x] 063.1-04-PLAN.md — Wave 4: Concurrent-reconcile guard + loadMessages MERGE — add reconcileInFlightRef bool guard at top of reconcile(); change loadMessages line 375 from setMessages(data) REPLACE to MERGE preserving live temp- placeholders whose runId not yet in DB result (D-063.1-11..12) — completed 2026-05-04
  - [x] 063.1-05-PLAN.md — Wave 5: E2E + manual UAT — three Playwright specs (063.1-thread-switch-mid-stream, 063.1-concurrent-reconcile, 063.1-refresh-no-duplicate-bubble) + 063.1-HUMAN-UAT.md filled with live-bundle Chrome MCP verification + Gap-006 escalation to new phase — completed 2026-05-04 (project-level approved; UAT `partial` with carry-forward items per Phase 063 precedent)
**Risks / pitfalls**:
  - Keeping the SSE alive across thread switches means the response body keeps streaming in the background. If the user opens many threads with active streams, the browser holds many open EventSource connections. Mitigate: cap concurrent subscriptions (LRU?) or accept the cost — long-stream count is naturally low.
  - The offset-cursor change must not skip events. Test the boundary: switch away after event N, switch back, verify event N+1 onward arrives (no off-by-one).
  - Ensure `ENABLE_TEST_FIXTURES` test endpoint is NOT mounted in production env. Plan 05 added the env-gate; verify it during 063.1 UAT.
  - The Gap-005 fix must not regress SC#2: `visibilitychange` and `focus` listeners must STILL fire reconcile — just not duplicate it. The fix is about coalescing, not skipping.

## Progress

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|---------------|--------|-----------|
| 44. SSE & Stop Reliability | v2.4 | 1/1 | Complete | 2026-04-23 |
| 45. Chat UX Fixes | v2.4 | 2/2 | Complete | 2026-04-23 |
| 46. Document Version Deletion | v2.4 | 2/2 | Complete | 2026-04-25 |
| 47. Document List & Upload Polish | v2.4 | 2/2 | Complete | 2026-04-25 |
| 48. Settings & Navigation Polish | v2.4 | 3/3 | Complete | 2026-04-25 |
| 49. Library Health at Scale | v2.4 | 2/2 | Complete | 2026-04-25 |
| 51. Context Window Management | v2.4 | 7/7 | Complete | 2026-04-24 |
| 52. Multi-Provider Model Routing | v2.4 | 3/3 | Complete | 2026-04-25 |
| 53. Cross-Provider Tool Calling Reliability | v2.4 | 4/4 | Complete | 2026-04-26 |
| 54. Reliable Agentic Generation | v2.4 | 5/5 | Complete | 2026-04-26 |
| 55. Streaming Reliability & Connection Resilience | v2.4 | 4/5 | Deferred | — |
| 56. Agent Real-Time Feedback | v2.4 | 3/3 | Complete | 2026-04-29 |
| 56.1. Agent Feedback Gaps | v2.4 | 3/3 | Complete | 2026-04-29 |
| 57. SSE Realtime Reconnect Fix | v2.4 | 2/2 | Deferred | — |
| 058. Backend SSE Concurrency Fix | v2.5 | 3/3 | Complete | 2026-05-01 |
| 059. SSE Architecture Refactor | v2.5 | 3/3 | Complete    | 2026-05-02 |
| 060. Frontend Race Fixes | v2.5 | 3/3 | Complete    | 2026-05-02 |
| 061. Run-Backed Streaming (Backend) | v2.5 | 5/5 | Complete    | 2026-05-02 |
| 061.1. Run-Backed Streaming Cleanup | v2.5 | 2/2 | Complete    | 2026-05-03 |
| 062. Replay & Tail API | v2.5 | 4/4 | Complete    | 2026-05-03 |
| 063. Frontend Stream Decoupling | v2.5 | 5/5 | Complete (gap-closed by 063.1) | 2026-05-03 |
| 063.1. Frontend Stream Decoupling — Gap Closure | v2.5 | 5/5 | Complete    | 2026-05-04 |
| 064. Validation Harness | v2.5 | 0/0 | Not started | — |
| 065. Skills Test Infrastructure Repair | v2.5 | 3/3 | Complete    | 2026-05-09 |
| 066. Adaptive Run Timeouts & Lifecycle States | v2.5 | 5/5 | Complete    | 2026-05-06 |

### Phase 064: Validation Harness — DEFERRED (covered organically by 067.x UAT scoreboards)

**Status:** **DEFERRED at v2.5 milestone close (2026-05-09)** — Phase 064's intended scenarios were validated organically through the user-driven UAT scoreboards in Phases 067.x (and earlier, 063.1). The original goal was a reusable Chrome MCP regression harness; in practice the v2.5 stream of work pivoted to live UAT scoreboards driven by the user, which produced lived-experience evidence superior to a synthetic harness for the streaming-render edge cases that surfaced.

**Coverage achieved without Phase 064:**

| Original SC | Where it was actually validated in v2.5 |
|---|---|
| Symptom E (tab-switch mid-stream) | Phase 067.3 R-1 row + Phase 067.5 5-cycle UAT (cross-thread mid-stream nav, 5/5 GREEN) |
| Symptom F (F5 mid-stream) | Phase 067.5 Row 11 protocol explicitly tests F5 repair behavior; vitest in `useMessages.test.ts` covers reconcile-on-switch-back |
| Symptom G (Stop button) | Phase 063.1 UAT SC#5 — partial; cross-tab Stop end-to-end deferred (already documented as carry-forward in STATE.md) |
| Symptom H (thread navigation during stream) | Phase 067.4 R-4 row + Phase 067.5 Branch D-3 fix (clearMessages streaming-bucket guard) |
| Refresh-mid-stream parity | Phase 063.1 UAT SC#6 + Phase 067.5 Cycle protocol (4/4 threads render without F5 across 5 cycles) |
| Multi-tab sync | Phase 063.1 UAT SC#5 — partially deferred (cross-tab Stop live test) |

**Carry-forward items (already in STATE.md Carry-forward):**
- SC#5 cross-tab Stop end-to-end (live multi-tab test)
- SC#7 full Phase 063 SC#1–#6 manual regression

**Original goal (re-open trigger):** if the project ever needs a reusable, scriptable Chrome MCP regression harness — e.g., to onboard a new contributor to the streaming codebase, or to gate a CI pipeline against the streaming scenarios — re-open Phase 064 as a side-phase. Until then, the 067.x UAT runbooks (`067.5-CLOSING-UAT.md`, `067.4-CLOSING-UAT-RUNBOOK.md`, `063.1-HUMAN-UAT.md`) serve as the canonical validation reference.

**Decision rationale:** during the v2.5 streaming-fix escalation chain (067 → 067.1 → 067.2 → 067.3 → 067.4 → 067.5), the user-driven UAT scoreboards proved more effective at catching edge cases than a synthetic harness would have — they surfaced Row 11's empty-thread-until-refresh bug, which would have been hard to encode as a deterministic Chrome MCP script. The defer is a recognition that the actual validation pattern that emerged was different from (and arguably better than) the originally-planned harness.

**Original Goal (preserved for reference):** Each scenario that the run-backed streaming architecture must satisfy is reproducible in browser MCP without writing new code per run, so regressions are caught before merge — including the new ChatGPT/Claude-class behaviors (refresh-mid-stream, multi-tab sync) introduced by 061–063.

### Phase 065: Skills Test Infrastructure Repair

**Goal**: Restore a green test foundation for the skills test suite so the next milestone (Skill Studio, see `PRD_Skill_Studio.md` and `.planning/seeds/SEED-002-skill-studio-milestone-prep.md`) can extend `tests/integration/test_threads_skills.py` and `tests/integration/test_skills_import_export.py` patterns without inheriting broken patches.
**Depends on**: Nothing (parallel-able with 061–064 since it touches an isolated test surface)
**Requirements**: TBD (no new REQUIREMENTS.md ID — pure maintenance closing TEST-DEBT discovered in 059)
**Success Criteria** (what must be TRUE):
  1. All tests in `backend/tests/integration/test_threads_skills.py` either PASS or are explicitly marked as `@pytest.mark.skip(reason=...)` with a documented out-of-scope justification — no `AttributeError: module 'app.api.threads' does not have the attribute 'create_streaming_chat'` failures remain.
  2. All tests in `backend/tests/integration/test_skills_import_export.py` either PASS or are explicitly skipped with documented reason — the 3 currently-failing export tests are fixed or formally deferred.
  3. The combined skills test run (`pytest tests/integration/test_threads_skills.py tests/integration/test_skills_import_export.py -q`) reports 0 errors and 0 unexpected failures.
  4. No regression in 058 / 059 binding gates: `test_058_concurrency.py::test_cross_tab_unblocked_during_sse` and the `test_059_disconnect.py` suite still pass.
**Plans:** 3/3 plans complete
Plans:
- [x] 065-01-PLAN.md — Repair test_threads_skills.py: rename 13 patch targets to create_adaptive_streaming_chat + tuple-wrap fake returns + add CallingMode import
- [x] 065-02-PLAN.md — Triage 3 failing tests in test_skills_import_export.py: fix-or-skip-with-reason per SEED-002 scope guard
- [x] 065-03-PLAN.md — Gap-closure plan for SC-3: migrate 11 tests in test_threads_skills.py to canonical Phase 063 POST→GET-stream pattern using `_build_mock_supabase()` + per-test `thread_id` fixture; closes BL-01 (INSERT-id mock deficit) + WR-01 (SSE-on-POST staleness) + WR-02 (THREAD_ID singleton) + IN-01 (assertion message strings)
**Closure** *(2026-05-09 — VERIFICATION 4/4 must-haves verified after Plan 065-03 gap closure)*:
  - SC-1 ✓ — 0 occurrences of `app.api.threads.create_streaming_chat`; 11 occurrences of `app.api.threads.create_adaptive_streaming_chat`
  - SC-2 ✓ — 15/15 PASS in test_skills_import_export.py
  - SC-3 ✓ — Combined skills test run reports 26 passed / 0 failed / 0 skipped / 0 xfailed
  - SC-4 ✓ — 058 binding gate (`test_cross_tab_unblocked_during_sse`) PASS; 059 suite preserves pre-Plan-01 baseline (one pre-existing `test_normal_stream_unchanged: Event loop is closed` failure verified pre-existing on `fa1e327` base via git-stash round-trip — NOT a Phase 065 regression; deferred to a future test-infra phase per `deferred-items.md` D-065-01-DEFER-2)
  - No production code touched. Code review (post-execution, depth=quick) clean (0 blockers / 0 warnings / 0 info).
**Risks / pitfalls:**
  - The `create_streaming_chat` patch target was likely removed in a refactor (current name is `create_adaptive_streaming_chat`). Some tests may have additional drift beyond just the name — they may also be testing call signatures, return shapes, or event emission patterns that have evolved. A pure mechanical rename is a starting point, not necessarily the finish line.
  - The 3 export-test failures may share a root cause with the known MIME fidelity gap (skill files stored as `application/octet-stream` on import) — fixing the test may require fixing the export to preserve original MIME, which is a real behavioral change. Decide upfront: scope this phase to test-only fixes (skip-with-reason if the underlying behavior is wrong), or expand to fix the export path.
  - These tests intersect heavily with the upcoming Skill Studio milestone — be careful not to absorb scope that belongs there. SEED-002 captures what should be deferred.

### Phase 066: Adaptive Run Timeouts & Lifecycle States

**Goal**: Restore parity with v2.4's long-tool-call behavior under the new run-backed architecture. Complex tool-calling agents (search → research → charts → docx export, etc.) must not be silently cut off by a 120s total-deadline timeout. The agent loop has no hard total cap (matches ChatGPT/Claude UX); only individual LLM calls have per-call budgets that reset on tool-call boundaries. When a budget IS exceeded, the run terminates as `timed_out` (distinct from user-initiated `cancelled`), captures a clear `runs.error = "timed_out after Ns at iteration N"` row, and the UI surfaces a "Agent reached time limit" banner with a Resume button — not a silent freeze.
**Depends on**: Phase 063.1 (run-backed streaming foundation must be stable; this phase tunes the lifecycle on top of it)
**Requirements**: STREAM-04 polish — addresses Gap-006 from `.planning/phases/063.1-frontend-stream-decoupling-gap-closure/063.1-HUMAN-UAT.md`. Triggered by user report 2026-05-04: "this is a RAG system that needs time with complex tasks and we should give it time especially if we are using different models, it was working before perfectly … but we have to restore this according to new updated architecture."
**Success Criteria** (what must be TRUE):
  1. Complex tool-calling prompt (e.g. "search for X → write report → make charts → save as docx", expected ≥4 tool-call iterations) completes successfully end-to-end. The agent does not stop mid-iteration. Final assistant content + final tool result both render in chat.
  2. `RUN_HARD_TIMEOUT_SECONDS` is no longer the dominant terminating signal for legitimate long agent loops. Either it is replaced by a per-LLM-call budget (e.g. `LLM_CALL_TIMEOUT_SECONDS` defaulting to 60–120s) that resets on tool-call boundaries, OR the total budget is raised high enough (≥600s) that real long agents complete within it. Document the chosen approach in PROJECT.md key decisions (D-v2.5-NN).
  3. The `public.runs.status` column has a 5th value `timed_out` (distinct from `cancelled`). DB CHECK constraint extended in a new migration; backend writes `timed_out` when the budget fires; backend writes `cancelled` only on user-initiated DELETE. Pydantic `MessageResponse.run_status` Literal extended to match (5 values).
  4. `public.runs.error` is non-NULL on every `failed`, `timed_out`, and (optionally) `cancelled` terminal state. Format: `<reason>: <detail>`. Examples: `timed_out: 600s deadline exceeded at iteration 8`, `cancelled: user clicked Stop`, `failed: <exception class>: <message>`. The frontend reads this field and renders a clear banner.
  5. SSE consumers see a `timed_out` terminal event (or `error` event with reason `"timed_out"`) — distinct from `cancelled` so the frontend can choose between "Response stopped" (user) and "Agent reached time limit. [Resume]" (system).
  6. New backend integration test asserts: a synthetic agent loop that exceeds the budget terminates as `timed_out`, writes a non-NULL `runs.error`, and does NOT propagate `GeneratorExit` to the LangSmith trace as an unexpected error (the cancellation must be a CLEAN, OBSERVED termination, not an exception leak).
  7. Live regression: re-run the user's failing prompt from the Gap-006 report — it must complete successfully.
**Plans**: 5 plans (planned 2026-05-06 via /gsd:plan-phase 066; wave-structured per file ownership)
  - [ ] 066-01-PLAN.md — Wave 1: Backend lifecycle split — migration 038_runs_timed_out_status.sql (DROP+ADD CHECK, 5 values); MANUAL Supabase SQL editor apply (BLOCKING task) + regenerate full-schema.sql; Pydantic MessageResponse.run_status Literal 4→5 values; rewrite terminal classification at threads.py:2140-2158 (TimeoutError → timed_out static placeholder; CancelledError UNCHANGED; Exception → "failed: <Class>: <truncated≤200>"); extend TERMINAL_TYPES + _RUN_STATUS_TO_TERMINAL_TYPE map (D-066-04, 05, 06, 07, 08).
  - [ ] 066-02-PLAN.md — Wave 2 (depends 01): Backend timeout machinery — extend ModelCapability TypedDict with llm_call_timeout_seconds + populate per-model values across all 30 entries (60s/90s/180s/240s/600s matrix); add LLM_CALL_TIMEOUT_OVERRIDES env parser bounded to [1, 3600] (T-066-05); DELETE outer asyncio.timeout(settings.run_hard_timeout_seconds) wrapper at threads.py:855; wrap each LLM stream block in async with asyncio.timeout(per_call_budget) (Anthropic 1149-1186 + OpenAI 1190-1244); add try/except TimeoutError that calls _ant_gen.close() / stream.close() before re-raising for clean LangSmith trace (D-066-11); refine TimeoutError error string with iteration + model + budget closure vars; replace settings.run_hard_timeout_seconds with new settings.consumer_timeout_seconds (610s) at runs.py:70/85/180; remove Settings.run_hard_timeout_seconds field entirely (env stays parsed-and-ignored via Pydantic extra="ignore") (D-066-01, 02, 03, 11; T-066-01 partition guard preserved).
  - [ ] 066-03-PLAN.md — Wave 2 (depends 01, parallel to 02): Frontend lifecycle UI — extend Message.runStatus type union 4→5 values (types/index.ts:98); extend Pydantic mirror in api.ts getMessages mapper (line 73); extend StreamCallbacks.onTerminal kind union to 4 values (api.ts:168); subscribeToRun parser dispatches `t === "timed_out"` to onTerminal("timed_out") BEFORE cancelled branch; add 5th onTerminal branches in BOTH useMessages.ts callsites (sendMessage line ~570 + reconcile line ~798) — sendMessage sets stopped:true, reconcile does not; extend Resume button gating from runStatus==="failed" to (failed || timed_out); switch banner copy on runStatus — "Agent reached time limit" for timed_out, "Response stopped" for cancelled (D-066-04, 06, 09, 10). Vitest deferred per Phase 063.1 precedent — gate is tsc --noEmit green.
  - [ ] 066-04-PLAN.md — Wave 3 (depends 01, 02): Backend integration test suite — 5 new files + 1 rewrite. test_066_status_enum.py (SC#3 — Pydantic Literal + TERMINAL_TYPES + namespace map); test_066_per_call_timer.py (SC#2 — timer fires within ε; quick call completes); test_066_terminal_classification.py (SC#4 + T-066-01 partition guard — TimeoutError→timed_out, generic Exception→failed:200char-cap, DELETE writes cancelled NEVER timed_out); test_066_sse_terminal.py (SC#5 — consumer sees distinct {type:'timed_out'} sentinel; no error/cancelled partition violations); test_066_langsmith_clean.py (SC#7 + D-066-11 — caplog asserts no GeneratorExit substring on TimeoutError path); test_061_hard_timeout.py REWRITTEN as 4-test deletion guard (legacy wrapper gone; Settings field gone; per-call timer present ≥2 times; SDK close methods present). Full integration suite green.
  - [ ] 066-05-PLAN.md — Wave 4 (depends 02, 03, 04): Live UAT + Gap-006 regression — primary run: re-fire user's verbatim Gap-006 prompt ("search for research authored by Fahed Mrad → professional short report → charts/diagrams → docx") in dev app at localhost:5173/, observe end-to-end completion + capture runs.status='completed' + LangSmith trace URL. Secondary run: set LLM_CALL_TIMEOUT_OVERRIDES=<active-model>=10 in backend/.env + restart + submit deliberately-slow prompt, observe "Agent reached time limit" banner + Resume button + runs.status='timed_out' + LangSmith trace shows clean TimeoutError (NO GeneratorExit at run_helpers.py:1680). Fill 066-HUMAN-UAT.md scoreboard with concrete evidence (timestamps, run_ids, trace URLs); commit closes phase.
**Risks / pitfalls**:
  - Splitting `cancelled` vs `timed_out` in the DB CHECK constraint is a non-trivial migration. Existing rows must be classified retroactively (decision in plan-phase: assume all historical `cancelled` are user-initiated unless `runs.error` indicates otherwise — usually NULL on legacy rows so default to `cancelled`).
  - Per-LLM-call budget must not allow infinite agent loops via tool-call boundary resets. Cap iteration count separately (e.g. `MAX_AGENT_ITERATIONS=24`) so a misbehaving agent that loops on tool calls still terminates.
  - LangSmith's `GeneratorExit` traceback at `run_helpers.py:1680` is the visible symptom; the underlying issue is `asyncio.timeout()` cancelling a task that's mid-LLM-stream. Even after lifecycle split, ensure the cancellation path is graceful — close the Anthropic/OpenAI stream cleanly via the SDK's `stream.close()` method before raising the timeout, so LangSmith records a clean termination, not an unexpected GeneratorExit.
  - Multi-model awareness: different providers have different speed profiles. Slow reasoning models (o1, Claude Opus extended) need longer per-call budgets than fast ones (Haiku, Mini). Consider per-model timeout overrides in `MODEL_CAPABILITIES` registry. Decide in plan-phase whether to ship per-model defaults in 066 or defer to a follow-up.
  - **Stopgap for users blocked NOW** (apply before this phase ships): set `RUN_HARD_TIMEOUT_SECONDS=600` in `backend/.env` and restart the backend. Buys headroom on most tool-call agents while the proper fix is planned.

### Phase 067: Frontend Streaming-UX Fix

**Goal**: Restore real-time first-paint of streaming agent events so the chat surface renders progressively while the agent runs — without requiring a manual page refresh — and close the five carry-forward UX issues (UX-067-01..05) surfaced incidentally by Phase 066's live UAT. After this phase lands, re-run Phase 066 Plan 05 Task 2 protocol (synthetic per-call timeout) to close out SC#6 live verification (banner + Resume + LangSmith clean).
**Depends on**: Phase 066 (carry-forward dossier in `.planning/phases/066-adaptive-run-timeouts-lifecycle-states/066-HUMAN-UAT.md` "Carry-forward to Phase 067" section provides scope; backend lifecycle architecture from 061–066 is the substrate this phase polishes)
**Requirements**: STREAM-04-polish (continuation of v2.5 STREAM-04)
**Success Criteria** (what must be TRUE):
  1. **First-paint works without refresh (UX-067-01).** Submitting a prompt that triggers a long agent run renders backend events in real-time on the chat surface — no extended empty period before the first paint, no requirement to manually refresh the page to see in-flight progress.
  2. **`runStatus` state machine is in sync with backend (UX-067-02).** The "Saving response…" indicator appears only when the run is genuinely between SSE delivery and persistence; never mid-stream while events are still flowing.
  3. **Refresh-recovery path remains healthy (UX-067-03).** F5/tab-restore continues to work as designed (Phase 061+ replay-tail) — but is no longer *required* for first-time delivery. Refresh becomes a recovery-only path, not a workaround.
  4. **Redis consumer cancellation is clean (UX-067-04).** SSE client disconnects (tab refresh / close) log at INFO/DEBUG level — no `redis.exceptions.TimeoutError` stack traces from `runs.py` xread paths when a browser tab cycles.
  5. **Tool-call iteration boundaries are surfaced (UX-067-05).** Multi-iteration agent runs (e.g. `execute_code → PNGs → execute_code → PNGs+DOCX`) communicate the iteration boundary clearly enough that the user recognizes intended multi-step work rather than perceiving a runaway loop.
  6. **Phase 066 SC#6 closed.** Re-run Plan 05 Task 2 protocol from Phase 066: force a synthetic per-call timeout (`per_call_budget=1` against `slow_llm_response_seconds=2`) and confirm the "Agent reached time limit" banner renders, the Resume button is visible, Resume click re-POSTs the original prompt, and the LangSmith trace shows clean closure (no `GeneratorExit` exception column). Update `066-HUMAN-UAT.md` SC#6 row from `deferred` → `green` after verification.
**Plans**: 5 plans (planned 2026-05-07 via /gsd:plan-phase 067; wave-structured by file ownership and dependency)
  - [x] 067-01-state-machine-cleanup-PLAN.md — Wave 1: useMessages.ts terminal-flip guards (existence-check on assistantId/targetId in BOTH sendMessage and reconcile onTerminal blocks) + delete "Saving response…" fallback at MessageItem.tsx:140 (D-067-01, D-067-02)
  - [x] 067-02-step-n-divider-PLAN.md — Wave 2 (depends 01): add `iteration?: number` field on ToolCall + stamp from currentIteration counter in onToolPreparing/onToolStart + render gradient "Step N" divider with data-testid="iteration-divider" between iteration boundaries in ToolCallPanel.tsx (D-067-03)
  - [x] 067-03-runs-xread-cancellation-cleanup-PLAN.md — Wave 1 (parallel to 01): runs.py module-top `from redis.exceptions import RedisError, TimeoutError as RedisTimeoutError` + differentiated handlers at three xread sites (lines 104, 163, 184): asyncio.CancelledError re-raise, RedisTimeoutError INFO-only, RedisError keep traceback (D-067-04)
  - [x] 067-04-stopgap-env-removal-PLAN.md — Wave 1 (parallel to 01, 03): one-line delete of RUN_HARD_TIMEOUT_SECONDS from backend/.env + cosmetic clean of backend/.env.example, supabase/SETUP.md, REDIS-SETUP.md (D-067-06; preserve config.py:366 dead-code comment + test_061_hard_timeout.py:44 test docstring)
  - [~] 067-05-live-uat-and-sc6-closure-PLAN.md — Wave 3 (depends 01, 02, 03, 04): live UAT with Chrome MCP + Supabase MCP + LangSmith MCP for UX-067-01..05; re-run Phase 066 Plan 05 Task 2 protocol (LLM_CALL_TIMEOUT_OVERRIDES=<model>=10) for SC#6 closure; flip 066-HUMAN-UAT.md SC#6 row from deferred → green; create 067-HUMAN-UAT.md and finalize 067-VALIDATION.md as nyquist_compliant (D-067-05, D-067-07; autonomous: false — checkpoints for human MCP verification) **— PARTIAL (2026-05-07): UX-067-01..05 all green; SC#6 4-of-5 sub-criteria green, LangSmith trace hygiene red → Gap-007 escalation**
**Risks / pitfalls**:
  - First-paint repair must not break the refresh-recovery path. Both paths share replay-tail wiring; regressing one to fix the other is the failure mode.
  - The iteration-boundary UI affordance (UX-067-05) is design-sensitive — a heavy-handed treatment (loud banners, modal dividers) would harm the conversational feel. Aim for a subtle visual cue and validate against ChatGPT/Claude's tool-call surfacing.
  - `redis-py`'s `async_timeout` wrapper converting `CancelledError` → `TimeoutError` is library behavior; the fix is at our consumer layer (catch and reclassify), not in redis-py.
  - The synthetic timeout fixture path (`ENABLE_TEST_FIXTURES=1` with `slow_llm_response_seconds`) must still exist when this phase reaches its UAT — confirm before scoping Plan 05's re-run.

### Phase 067.1: Agent Streaming & Behavior Polish

**Goal**: Close out Phase 067's deferred Gap-007 (D-066-11 `stream.close()` invariant violated under synthetic-timeout — `GeneratorExit` recorded at `langsmith/run_helpers.py:1680` instead of clean `TimeoutError`) and bundle three deep-UAT findings from the 2026-05-07 Fahed Mrad multi-tool live run into a single tightening pass: (a) replace generic "Thinking…"/"Working" placeholder copy with context-aware in-flight affordance driven by `iteration_start` and `tool_preparing` SSE event payloads ("Searching knowledge base…", "Setting up agent…", "Spinning up sandbox…", "Loading skill {name}…"); (b) redesign the agent system prompt so multi-step user intents (e.g. "search → report → charts → docx") execute the full pipeline without intermediate "if you want, I can also…" confirmation requests, customized for the RAG-tool surface; (c) clarify the skill-load tool-card copy so users understand WHY a skill is being loaded ("Loading skill 'docx' for next operation: docx generation"). Promotes Phase 066 SC#6 from `partial` → `green` and tightens the user-perceived streaming feel established in Phase 067.
**Depends on**: Phase 067 (carry-forward state-machine cleanup, iteration divider, runs.py cancellation handlers, env stopgap removal — see `.planning/phases/067-frontend-streaming-ux-fix/067-VERIFICATION.md`); Phase 066 D-066-11 architectural context for stream-close-on-timeout invariant
**Requirements**: STREAM-04-polish (continuation of v2.5 STREAM-04, sub-phase of Phase 067)
**Success Criteria** (what must be TRUE):
  1. **D-066-11 stream-close ordering honored (Gap-007 closure).** Re-running the synthetic-timeout protocol (`LLM_CALL_TIMEOUT_OVERRIDES=<model>=10` + slow long-form prompt) produces a LangSmith ChatOpenAI sub-trace whose exception column shows a clean exception type (e.g. `TimeoutError`, `CancelledError`) — NOT `GeneratorExit` at `run_helpers.py:1680`. The Supabase `runs` row continues to record `status='timed_out'` with the existing per-call deadline error message.
  2. **Context-aware in-flight copy at all latency phases.** Pre-streaming silence (1-3s pre-first-delta), inter-iteration silence (between "Step N — Thinking…" divider and the next tool card), and tool-preparing latency each surface a labeled affordance reflecting the actual in-flight operation (agent setup, knowledge-base search, document analyze, sandbox spin-up, code execution, skill load). No generic "Thinking…"/"Working" copy remains as a fallback in the streaming UI.
  3. **Multi-step pipelines execute without intermediate confirmation.** Five consecutive runs of the verbatim Fahed Mrad prompt ("search for research authored by Fahed Mrad → professional short report → charts/diagrams → docx") produce all four deliverables (themes report + chart PNGs + docx file) end-to-end, with zero "If you want, I can also…" / "Shall I proceed to…" confirmation prompts blocking pipeline progress. Verified via Chrome MCP run captures.
  4. **Skill-load card explains the upcoming tool context.** A docx-generating prompt produces a `data-testid="skill-load-card"` (or equivalent) DOM element whose copy includes the upcoming tool/operation hint ("Loading skill 'docx' (needed for: docx generation in next step)"), so the skill-load → tool-fire ordering reads as intentional rather than out-of-order. SSE `skill_loaded` payload (or frontend inference from the next `tool_preparing`) carries the upcoming-operation hint.
  5. **Phase 066 SC#6 promoted to green.** With Plan 01's fix landed, update `.planning/phases/066-adaptive-run-timeouts-lifecycle-states/066-HUMAN-UAT.md` SC#6 row from `partial` → `green` with concrete Chrome MCP + LangSmith trace evidence captured during Plan 05's UAT.
  6. **Phase 067 UX-067-01..05 still green after polish.** Re-running the deep UAT confirms Phase 067's first-paint, runStatus state machine, refresh-recovery, redis-consumer cancellation, and iteration-boundary affordances remain green — none of the 067.1 changes regress those success criteria.
**Plans**: 5 plans (planned 2026-05-07 via /gsd:plan-phase 067.1; Wave 1 backend-only Plans 01+03 parallel; Wave 2 frontend Plans 02+04 parallel; Wave 3 closing UAT Plan 05 sequential)
  - [x] 067.1-01-stream-close-on-timeout-PLAN.md — Wave 1: Plan 01 Track A (drain-into-queue helper bypassing _TracedStream.__iter__ implicit cleanup) with 2h time-box → Track B fallback (formal D-066-11 retirement with xfail marker on test_066_langsmith_clean.py); symmetric across OpenAI (threads.py:1297-1340) + Anthropic (threads.py:1240-1256) timeout branches; explicit checkpoint:decision after Task 1 to gate Track A→B pivot
  - [x] 067.1-02-context-aware-in-flight-copy-PLAN.md — Wave 2: new outerBannerLabel sibling export in toolMeta.ts + 2 literal replacements in MessageItem.tsx (line 121 Thinking literal; line 135 Working/Synthesizing chained ternary); preserves D-067-02 :null at line 140 + animate-fadeSlideUp + terminal banners; tsc gate (vitest unavailable per 063.1 carry-forward)
  - [x] 067.1-03-agent-system-prompt-overhaul-PLAN.md — Wave 1 (parallel to 01): insert ## Multi-step intent section between ## Rules (line 237) and ## Confidence & hedging (line 239) in SYSTEM_PROMPT; positive framing per Anthropic guidance; explicit EXCEPTIONS sub-section preserves clarification for genuinely-ambiguous + missing-information + irreversible/destructive actions; module-import sanity gate
  - [x] 067.1-04-skill-load-ux-clarity-PLAN.md — Wave 2 (parallel to 02): backend skill_loaded follow-up SSE emit at threads.py ~line 1633 (description-only payload, V7 mitigation; truthy guard for Pitfall 4); api.ts onSkillLoaded callback + dispatch; types/index.ts SkillActivation.description?; useMessages onSkillLoaded handler (merge into matching activation, not append); ToolCallPanel SkillRow extension (data-testid=skill-load-card + Loading-skill copy + conditional description span)
  - [x] 067.1-05-closing-uat-PLAN.md — Wave 3 (depends 01, 02, 03, 04): autonomous: false; Chrome MCP + Supabase MCP + LangSmith MCP UAT scoreboard with 10 sequential tasks (pre-flight env hygiene → SC#1+#5 synthetic-timeout protocol → SC#3 5x Fahed-Mrad multi-step + 1x ambiguous regression → SC#2 per-phase in-flight copy capture → SC#4 skill-load DOM assertion → SC#6 UX-067-01..05 regression → 067.1-HUMAN-UAT.md author → 066-HUMAN-UAT.md SC#6 promotion partial→green → 067.1-VALIDATION.md frontmatter promotion → human review checkpoint); Pitfall 5 LLM_CALL_TIMEOUT_OVERRIDES env hygiene guarded at 3 independent points
**Risks / pitfalls**:
  - **langsmith-py @traceable wrapper behavior.** Plan 01 may discover that `langsmith-py`'s `@traceable` async-generator wrapper records `GeneratorExit` regardless of the agent_runner.py close-before-cancel ordering — meaning D-066-11 cannot be honored as currently specified. Mitigation: Plan 01 begins as audit-and-fix; if the audit confirms the wrapper records GeneratorExit unconditionally, the plan pivots to formally retiring the D-066-11 invariant from SC#6 (with rationale recorded in 067.1-CONTEXT.md and 066-HUMAN-UAT.md) rather than chasing an unfixable upstream behavior.
  - **System-prompt tuning risk for non-multi-step intents.** Plan 03's "execute full pipeline without confirmation" instruction must NOT regress the legitimate confirmation-needed paths (genuinely ambiguous user intents, destructive operations, skill-not-yet-installed cases). Tune with explicit guardrails: "Only ask for clarification if the intent is genuinely ambiguous OR if executing would cause irreversible/destructive action."
  - **Skill-load UX hint dependency.** Plan 04 requires either a backend SSE payload change (`skill_loaded` event carries upcoming-tool hint) OR frontend inference from the next `tool_preparing` event. Choose backend-payload approach unless the inference path is materially simpler — backend hint is more explicit and easier to test.
  - **Out of scope (preserve carry-forward boundaries):** sandbox spin-up time reduction (Docker container creation is operationally bounded — out of scope for 067.1), Vitest test infrastructure (machine-level vitest broken — deferred per Phase 063.1 carry-forward), multi-tenant org-level scope (separate v2.6+ milestone).

### Phase 067.2: Streaming Render & Storage Fixes

**Goal**: Close 6 lived-experience gaps reported after Phase 067.1 closure (the Phase 067.1 UAT was DOM-observer-driven and missed several user-visible failure modes). Six issues, all line-anchored to their root cause: (1) empty-render-until-cycle-finishes on multi-step new-thread runs (`useMessages.ts:623-628` `guardedSetMessages` race + late `setViewingThread` write at `ChatArea.tsx:79`); (2) F5 / mid-stream navigation empties the UI (same root cause on the reconcile path at `useMessages.ts:866-871`); (3) Storage signed-URL `InvalidJWT` 1h after generation (`sandbox_service.py:138` hardcoded `create_signed_url(path, 3600)` — replace with on-demand re-sign endpoint); (4) confidence + suggestions not visible in chat UI (likely render-side gap in `MessageItem.tsx`; investigate post-fix-#1 since #1 may mask state population); (5) chat title stays "New Chat" when run fails / is cancelled / times out (`threads.py:2398-2408` block runs only AFTER `_persist_assistant_message` in success path — hoist to fire after first user message persisted); (6) multi-provider live coverage gap (Anthropic `claude-opus-4-6` + `claude-sonnet-4-6` and OpenRouter Kimi 2.5 + MiniMax 2.7 untested for both Plan 01 Track A drain-into-queue AND Plan 03 `## Multi-step intent` SYSTEM_PROMPT). The phase closure gate is **user-driven UAT** (12 user-actionable checkboxes) — no partial-with-approval shortcuts; this is the reliability lesson from Phase 067.1's DOM-observer-driven false-green.
**Depends on**: Phase 067.1 (Track A helper at `threads.py:155-249` is the substrate for D-067.2-06 multi-provider verification; SYSTEM_PROMPT `## Multi-step intent` section at `threads.py:226-258` is the substrate for cross-provider full-pipeline verification); Phase 063 frontend reconcile-on-(re)connect contract (`ChatArea.tsx:163-188` mount + visibilitychange + focus + pageshow triggers) — D-067.2-02 fix sits within this contract without changing it.
**Requirements**: STREAM-04-correctness (continuation of v2.5 STREAM-04-polish; covers the multi-step new-thread render correctness + F5-recovery contracts that STREAM-04-polish targeted but did not fully verify under user-driven conditions).
**Success Criteria** (what must be TRUE — every criterion is user-observable behavior, NOT a test/grep proxy):
  1. **Multi-step new-thread runs render progressively (D-067.2-01).** Submitting a Fahed-Mrad multi-step prompt on a fresh thread (no pre-existing `thread.id`) shows assistant content streaming delta-by-delta in the UI — NOT empty until end-of-run, NOT lump-replaced when `loadMessages` runs.
  2. **Mid-stream F5 + thread-navigation recover content (D-067.2-02).** During an in-flight multi-step run: (a) pressing F5 reloads the page and continues streaming with NO blank-state period; (b) navigating Thread A → Thread B → back to Thread A shows the streaming content present, no missing deltas.
  3. **Output document downloads work after 1+ hour (D-067.2-03).** Clicking a docx / chart download link in chat history >1 hour after generation downloads the file — no `InvalidJWT` / `"exp" claim timestamp check failed` error. Cross-user IDOR check: a different authenticated user clicking someone else's download URL gets a 403, not the file.
  4. **Confidence + suggestions render (D-067.2-04).** A Fahed-Mrad search-only prompt produces an assistant message that visibly includes a confidence indicator (low/medium/high or numeric) AND a follow-up suggestions block with 2-4 clickable example questions.
  5. **Chat title generates regardless of run outcome (D-067.2-05).** (a) Submit any prompt → click Stop mid-stream → sidebar shows a generated title (NOT "New Chat"). (b) Submit a prompt with synthetic timeout → run fails → sidebar still shows generated title.
  6. **Multi-provider Track A + multi-step works for 4 models (D-067.2-06).** For each of `claude-opus-4-6`, `claude-sonnet-4-6`, OpenRouter Kimi 2.5, OpenRouter MiniMax 2.7: (a) synthetic-timeout protocol produces a clean LangSmith trace exception (NOT `GeneratorExit at run_helpers.py:1680`); (b) Fahed-Mrad multi-step prompt executes the full pipeline (search → analyze → load_skill → execute_code) without mid-pipeline confirmation.
**Plans**: 6 plans expected (Plan 01 thread-ref-sync — addresses D-067.2-01 + D-067.2-02 from a single root cause; Plan 02 title-on-first-message; Plan 03 storage-signed-url-resign; Plan 04 confidence-suggestions-render-audit — investigation-first; Plan 05 multiprovider-track-a-uat — verification-only; Plan 06 user-driven-closing-uat — `autonomous: false` with 12 user-actionable checkboxes that gate phase closure).
**Risks / pitfalls**:
  - **Race-fix correctness.** D-067.2-01/02's fix is a `useLayoutEffect` placement change; subtle React scheduling. Test new-thread creation, existing-thread send, F5 mid-stream, thread-switch mid-stream, AND visibilitychange reconcile to ensure no regression. The deeper per-thread message cache refactor (mentioned at `ChatArea.tsx:115-120`) is explicitly OUT OF SCOPE.
  - **Storage URL migration.** D-067.2-03's re-sign endpoint changes the URL shape stored in assistant messages. Legacy stored URLs (1h-expiry baked-in) will continue to decay after 1h post-generation; only NEW outputs use the re-sign endpoint. Don't attempt to migrate existing rows — the storage objects ARE accessible by re-signing, but rewriting historical message content is out of scope.
  - **Render-side investigation depth (Plan 04).** D-067.2-04 may be a side effect of D-067.2-01 (events arrive during the no-op window and never land in state). Run Plan 04's investigation AFTER Plan 01 lands; if state is now populated but UI doesn't render, then add render code. Avoid blind code change.
  - **Multi-provider model availability.** D-067.2-06 requires the user to have valid API keys / OpenRouter credentials configured for all 4 models. If a provider is unreachable, mark its checkbox as `n/a — provider unconfigured` and proceed; do NOT block phase closure on missing credentials.
  - **Closure-gate strictness.** This phase explicitly rejects partial-with-approval. Either all 12 user-UAT checkboxes are GREEN or the phase remains open. Phase 067.1's lesson was that "partial-with-approval" let lived-experience failures escape verification. Plan 06 is the reliability gate.

### Phase 067.3: Streaming Render & Storage Fixes — Round 2

**Goal**: Close the 3 RED rows + 1 HIGH new finding from Phase 067.2's user-driven UAT (Plan 06 SUMMARY at `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md`), then re-run the deferred rows to clear Phase 067.2's UAT gate. Four scoped issues, all line-anchored to their root cause: **R-1** cross-thread streaming render loss (mid-stream thread-switch loses the streamed-into thread's render until both runs complete; the deeper per-thread message cache that CONTEXT.md `### D-067.2-04` flagged out-of-scope for 067.2 is now the in-scope fix); **R-2** sandbox-outputs auth fails for browser `<a href>` clicks (`/sandbox-outputs/{path}` uses `Depends(get_current_user)` reading `Authorization: Bearer` headers; anchor clicks send only cookies, returning 401 — Plan 03's TestClient-based unit tests passed because TestClient injects Bearer headers automatically, missing the lived-experience failure mode); **R-3** confidence + suggestions render asymmetry (confidence badge renders ✓; suggestion pills don't — emit at `threads.py:2477` lives inside a post-`done` try/except that silently swallows OR returns empty `questions`, OR the frontend gate `!isStreaming && message.suggestions && message.suggestions.length > 0 && onSendMessage` filters render); **N-01** (HIGH) model→provider router at `backend/app/api/threads.py:949` reads `_resolved_provider = _user_settings.active_provider`, ignoring `MODEL_CAPABILITIES[model]["provider"]` — with `active_provider="openai"` and `model="claude-sonnet-4-6"` (no explicit `body.provider`), the request goes through the OpenAI SDK and returns 404 (repro: run `6eab949f-78da-4ea4-ac01-f04b16c9be7d`). After fixes land, re-run Phase 067.2 UAT rows 3, 4, 6 + complete deferred rows 5 (D-067.2-03b 90-min wait, blocked by R-2), 11, 12 (kimi/minimax live timeout — Plan 05 smoke ✓; symmetric Track A evidence via Row 9 sufficient, but live re-test wraps the gate cleanly).
**Depends on**: Phase 067.2 (Plan 03 sandbox-outputs endpoint logic at `backend/app/api/threads.py:1819-1898` is the substrate for R-2's auth integration; CONTEXT.md `### D-067.2-04` flagged R-1's per-thread cache as future scope — that future is now); Phase 063 frontend reconcile-on-(re)connect contract (`ChatArea.tsx:163-188`); Phase 052 multi-provider model routing (`MODEL_CAPABILITIES` registry — N-01's intended source of truth).
**Requirements**: STREAM-04-correctness-round2 (continuation of STREAM-04-correctness; covers R-1 cross-thread render correctness, R-2 lived-experience download auth, R-3 suggestions render parity, and N-01 HIGH multi-provider routing correctness).
**Success Criteria** (what must be TRUE — every criterion is user-observable behavior, NOT a test/grep proxy):
  1. **Cross-thread switch preserves streaming render (R-1).** With two concurrent in-flight runs on Thread A and Thread B: switching A → B → back to A shows Thread A's streaming content present and continuing — NOT empty until both runs finish. Tested via Chrome MCP per the verbatim Phase 067.2 Row 3 protocol; passes lived-experience UAT, not just DOM-observer.
  2. **Sandbox output downloads work via browser `<a href>` click (R-2).** A docx / png link in a chat history entry, clicked via real browser anchor (not `fetch()` with manual Bearer), downloads the file. No 401 `{"detail":"Not authenticated"}`. Cross-user IDOR check: a different authenticated user clicking someone else's download URL gets a 403, not the file. New regression test: browser-link e2e (Chrome MCP) added to lock the lived-experience failure mode.
  3. **Suggestion pills render alongside confidence (R-3).** A Fahed-Mrad search-only prompt produces an assistant message that visibly renders BOTH a confidence indicator AND a follow-up suggestions block with 2-4 clickable example questions. If root cause is silent backend swallow → emit a server-side info log when `questions=[]` and surface the gate criteria in dev mode. If root cause is empty model output → upstream prompt fix. If root cause is frontend gate → render the block per spec.
  4. **Model→provider router uses capability registry (N-01).** Submitting a request with `model="claude-sonnet-4-6"` and `user_settings.active_provider="openai"` (no explicit `body.provider`) routes through the Anthropic SDK and succeeds — NOT 404 from OpenAI SDK. Resolution order: explicit `body.provider` > `MODEL_CAPABILITIES[model]["provider"]` > `user_settings.active_provider` fallback. Unit test asserts model→provider auto-resolution for at least one Anthropic and one OpenRouter model when `active_provider="openai"`.
  5. **Phase 067.2 UAT closes (deferred rows clear).** After R-1, R-2, R-3, N-01 land: re-running Phase 067.2's `067.2-HUMAN-UAT.md` rows 3, 4, 6 turns them GREEN; deferred row 5 (D-067.2-03b 90-min wait, previously blocked by Row 4) turns GREEN; deferred rows 11, 12 (kimi/minimax live timeout) turn GREEN or are explicitly accepted as `n/a — provider unconfigured` per the multi-provider availability rule. Phase 067.2's strict gate (12/12 user-ticked GREEN) is met and 067.2 closes alongside 067.3.
  6. **Phase 067.2 GREEN rows still green after Round 2 (no regression).** Re-running rows 1, 2, 7, 8, 9, 10 + NR-1, NR-2 confirms no regression: streaming render race fix (Plan 01), F5 mid-stream replay (Plan 01), Stop/timeout title (Plan 02), Anthropic-native + opus multi-provider (Plan 05), gpt-5.4 + skill-load card (Phase 067.1).
**Plans**: 5 plans expected, ordered by risk and Phase 067.2 UAT priority — N-01 first (HIGH severity, ~5-line fix, unblocks ALL Anthropic-via-OpenAI-default request paths); R-2 second (lived-experience download UX, isolated endpoint surface); R-3 third (investigation-first since root cause is uncertain — backend silent error vs empty emit vs frontend gate); R-1 fourth (per-thread streaming cache — biggest scope, highest design surface, benefits from R-2/R-3 stability before refactor); Plan 05 closing UAT (`autonomous: false`, scoreboard re-tick of Phase 067.2 rows 3/4/5/6/11/12 + new R-1/R-2/R-3/N-01 evidence rows).
**Risks / pitfalls**:
  - **R-1 design fork — per-thread streaming cache shape.** Plan 04 needs to decide cache key (`thread_id` only? `thread_id + run_id`?), eviction policy (LRU? on-stream-complete?), and storage shape (Map<string, deltas[]>? Map<string, useState<string>>?). CONTEXT.md `### D-067.2-04` ruled this out-of-scope for 067.2; the fork should be resolved in `/gsd:discuss-phase 067.3`, not invented inside Plan 04. Mind the existing `streamingThreadIdRef` + `activeThreadIdRef` pair — the new cache must compose with (not replace) the single-stream race fix from Phase 067.2 Plan 01.
  - **R-2 design fork — token-in-URL signed JWT vs JS blob download.** Token-in-URL: backend issues short-lived signed token (~60s), appended as `?token=<jwt>` to each link; endpoint validates from query param. Pros: native browser anchor, copy-paste-friendly URL, no JS hooks. Cons: another secret rotation surface; need to verify Supabase Storage doesn't strip query params. JS blob: frontend intercepts click, calls `fetch()` with Bearer header, downloads via `Blob` URL + `<a download>`. Pros: reuses existing auth pipe, no new endpoint. Cons: brittle to "Open in new tab" / right-click "Save link as" / non-JS clients. Resolve in `/gsd:discuss-phase 067.3`. Whichever path: add a browser-link Chrome MCP e2e test to lock the regression.
  - **R-3 investigation-first, fix-second.** Plan 03 must investigate before patching: backend log + browser DevTools to determine whether `message.suggestions` is populated on the SSE stream and on the persisted `messages.confidence_level=null` row. Avoid blind frontend gate changes — could mask real backend silence. Phase 067.2 Plan 04 INVESTIGATION-NOTES already lists candidate root causes; start there.
  - **N-01 unit-test trap.** N-01's ~5-line fix is mechanically simple but the test must assert auto-resolution under the WRONG `active_provider` setting (openai default + claude model). A unit test that always hand-sets `body.provider` would pass without exercising the bug. Tag the test with the repro `run_id` for traceability.
  - **Phase 067.2 deferred row symmetric-evidence trap.** Rows 11/12 (kimi/minimax live timeout) were deferred via "Plan 05 smoke ✓ + Row 9 symmetric Track A evidence." Plan 05 (closing UAT) should re-test live where credentials permit; if `.env` has been reset and creds are unavailable, accept `n/a — provider unconfigured` per the Phase 067.2 multi-provider availability rule rather than block phase closure. Don't re-litigate symmetric-evidence; honor it.
  - **Out of scope (preserve carry-forward boundaries):** N-02 multi-step self-QA iteration cap (defer to a UX/cost-tuning phase per Phase 067.2 Plan 06 SUMMARY); D-066-11 langsmith trace gap for Anthropic-native streams (resolved as `runs.error` is canonical-clean-cancellation per Phase 067.2 D-067.2-06-01); historical message URL migration (legacy 1h-expiry URLs continue decaying — re-sign endpoint is for new outputs only, per Phase 067.2 D-067.2-03 boundary).

### Phase 067.4: Streaming Suggestions, Tool-Stage Render & Code-Execution Stream

**Goal**: Close the 2 RED rows + 2 carry-forward deferred rows from Phase 067.3's user-driven UAT (Plan 05 SUMMARY at `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-05-SUMMARY.md`), plus the 2 UAT-discovered findings, then re-run all three phases' UAT scoreboards to clear Phase 067.2 + Phase 067.3 + Phase 067.4 closure gates together. Five scoped issues, all line-anchored or instrumentation-anchored to their root cause: **R-3** (gate-blocking, mirrors 067.2 Row 6) suggestion pills not emitted to SSE for Fahed-Mrad search-only prompts; SSE replay (`/runs/{id}/stream?since=0`) `uniqueKinds` shows `[title, iteration_start, tool_preparing, tool_start, tool_end, planning, delta, sources, citations, confidence, done]` but NO `suggestions` event and NO `fallback_model` event across two distinct run_ids (`36b509e2-…` initial + `00dcf270-…` retest after `max_tokens` 200→800 disconfirmed hypothesis). Plan 04's instrumentation at `backend/app/api/threads.py:2487-2526` (`logger.warning("suggestion generation failed", exc_info=True)` for non-`NotFoundError` exceptions + `logger.info("suggestions empty for run X")` for `questions=[]`) is live; first action is uvicorn-stdout capture to disambiguate exception-swallow vs empty-LLM-output vs reasoning-token-starvation. **R-4** (UAT-discovered, NEW) Active-thread UI freezes at tool-execution stage; refetch on thread-switch repairs the state. Symptom: pgvector docx prompt produced "Executing code" spinner for ~9 min from operator POV but the run actually completed in 2.8s — visible only after `getMessages()` refetch on thread navigation. Sonnet text path had no such issue, ruling out general streaming regression. Likely a Plan 03 R-1 refactor regression: when `useMessages.ts` was refactored to `messagesByThread: Map<string, Message[]>`, one or more SSE handlers (`tool_end`, `tool_preparing`, post-`done` finalizer) likely missed the update to write to the per-thread bucket on the active thread. **R-5** (UAT-discovered, additive UX) Code-execution progress shown only as a spinner; no token-by-token feedback. Best practice = stream sandbox stdout line-by-line as `code_output_line` SSE events, parallel to how `delta` flows for text. Strictly additive — no conflict with shipped R-1/R-2/R-3/N-01 surfaces. **NR-067.2-11/12** (carry-forward) OpenRouter Kimi 2.5 + MiniMax 2.7 synthetic-timeout regression. `OPENROUTER_API_KEY` IS present in `backend/.env`; just exercise the synthetic-timeout protocol. After fixes land, re-run all three phases' UAT to close 067.2 + 067.3 + 067.4 together.
**Depends on**: Phase 067.3 (Plan 04 instrumentation at `threads.py:2487-2526` is the substrate for R-3 disambiguation; Plan 03 `messagesByThread` Map at `useMessages.ts` is what R-4 investigates; Plan 02 `OutputFileCard` JS-blob path is what R-5's `code_output_line` event flows through); Phase 067.2 (Rows 11/12 deferred, carried forward); Phase 052 multi-provider model routing (`MODEL_CAPABILITIES` registry — N-01 confirmed working in 067.3 unit tests, used as substrate for OpenRouter rows).
**Requirements**: STREAM-04-correctness-round3 (continuation of STREAM-04-correctness-round2; covers R-3 suggestions emit, R-4 active-thread tool-stage render correctness, R-5 code-execution stdout streaming UX, NR-067.2-11/12 OpenRouter timeout regression).
**Success Criteria** (what must be TRUE — every criterion is user-observable behavior, NOT a test/grep proxy):
  1. **Suggestion pills render after Fahed-Mrad search-only prompts (R-3, gate-blocking).** A search-only prompt produces an assistant message that visibly renders BOTH a confidence indicator AND a follow-up suggestions block with 2-4 clickable pills below the message. SSE replay for the run includes a `suggestions` event with `questions=[…]`. Disambiguation evidence captured in 067.4 SUMMARY: which Plan 04 log line fired (`suggestion generation failed` exception path vs `suggestions empty for run X` empty-output path) and how it was resolved. Re-ticks Phase 067.2 Row 6 + Phase 067.3 Row R-3 GREEN.
  2. **Active-thread UI updates on tool-execution boundaries without thread navigation (R-4).** Submitting a sandbox-invoking prompt (e.g., "create a one-page docx summary using python-docx") shows the spinner-to-result transition fire WITHOUT the user navigating to another thread and back. The Output Files card with the download link renders as soon as the run completes server-side. Plan 03's per-thread store (`messagesByThread` Map) is preserved; the fix updates SSE handlers to write to the active thread's bucket for tool-stage events. No regression to R-1's cross-thread switch behavior.
  3. **Code-execution UI shows live progress during execution (R-5, additive UX, AMENDED 2026-05-09).** During sandbox code execution, the tool card's spinner shows a live elapsed counter (e.g., "Executing… 2.4s") driven by a `code_executing` heartbeat SSE event emitted ~once per second from the existing `sandbox_queue` drain loop in `backend/app/api/threads.py`. On completion, stdout/stderr lines render in the Output panel via the existing `code_stdout`/`code_stderr` events. No full-duration silent spinner. Frontend handler in `useMessages.ts` updates `tool_calls[N].elapsed_seconds` on the active thread's per-thread bucket; `ExecuteCodeBlock.tsx` renders the elapsed counter next to the spinner. NO changes to `OutputFileCard` (R-2), `messagesByThread` shape (R-1, only one new key under `tool_calls[N]`), `MODEL_CAPABILITIES` router (N-01), or `suggestion_service` (R-3) — strictly additive. Original "line-by-line every \\n" plan was infeasible: `InteractiveSandboxSession` (required for Python state persistence across multi-iteration code runs) does not fire `on_stdout`/`on_stderr` callbacks during execution. Captured as deferred re-open trigger.
  4. **OpenRouter Kimi/MiniMax synthetic-timeout shows clean cancellation format (NR-067.2-11/12).** With `LLM_CALL_TIMEOUT_OVERRIDES=moonshotai/kimi-k2.5=10` (and separately `=minimax/minimax-m2.7=10`), submitting a long-form prompt with the matching model produces `runs.status='timed_out'` + `runs.error='timed_out: 10s per-call deadline exceeded at iteration N (model=<id>)'`. Clean cancellation format (NOT `GeneratorExit`). Mirrors Phase 067.2 Rows 11/12 closure protocol.
  5. **All three phases' UAT scoreboards close together (cross-phase closure rule).** After R-3, R-4, R-5, NR-067.2-11/12 land: re-running Phase 067.2's `067.2-HUMAN-UAT.md` Rows 6, 11, 12 turns them GREEN (or n/a for 11/12 if creds reset); Phase 067.3's `067.3-HUMAN-UAT.md` Row R-3 + Row 067.2-6 mirror turn GREEN; Phase 067.4's new `067.4-HUMAN-UAT.md` shows R-3, R-4, R-5, NR rows + closing summary all GREEN. All three phases' frontmatter `status` flips to `closed`. Phase 067.4 closes Phase 067.3 closes Phase 067.2 in one closing-UAT pass.
  6. **No regression to Phase 067.3 GREEN baseline (R-1, R-2, R-2-IDOR, N-01, 067.2-3/4/5, 067.3-NR).** Re-ticking the GREEN rows from Phase 067.3 confirms they still pass after 067.4's R-4 fix (which touches `useMessages.ts` SSE handlers — adjacent to R-1's per-thread store; high regression risk surface).
**Plans**: 5 plans expected, ordered by dependency and risk — **Plan 01 R-3 disambiguation+fix** (gate-blocking; uvicorn stdout capture → identify which Plan 04 log line fires → fix per branch: exception-class fix OR token budget OR model swap OR JSON parser fix); **Plan 02 R-4 SSE handler audit** (diff `useMessages.ts` SSE handlers vs pre-Plan-03 baseline → fix any tool-stage handlers that missed the per-thread bucket update; high regression risk to R-1, requires careful preservation of `messagesByThread` semantics); **Plan 03 R-5 code-execution streaming** (additive — backend `code_output_line` emit + frontend handler + UI rendering; runs in parallel with Plan 02 since strictly additive); **Plan 04 NR-067.2-11/12 OpenRouter timeout regression** (synthetic-timeout protocol for kimi-k2.5 + minimax-m2.7); **Plan 05 closing UAT** (`autonomous: false`, scoreboard re-tick of Phase 067.2 Rows 6/11/12 + Phase 067.3 Row R-3 + Row 067.2-6 mirror + new R-3/R-4/R-5/NR rows + 067.3-NR no-regression).
**Risks / pitfalls**:
  - **R-3 hypothesis disconfirmation already logged.** The `max_tokens` 200→800 budget bump in `backend/app/services/suggestion_service.py:75/92` is DISCONFIRMED — retest run_id `00dcf270-…` SSE still missing `suggestions` event. Plan 01 must NOT re-explore this dead-end. Start with uvicorn-stdout capture from Plan 04's instrumentation (`logger.warning` for exception path, `logger.info` for empty-output path), then branch by which fired.
  - **R-4 high regression risk to R-1.** R-4's fix touches the same file (`frontend/src/hooks/useMessages.ts`) and event-handler code paths that Plan 03 R-1 refactored. Plan 02 must isolate the missing tool-stage handler updates without disturbing the per-thread store semantics. Closing UAT must spot-check R-1 (cross-thread switch protocol) alongside R-4 (active-thread tool-stage protocol) — both pass, OR neither phase closes.
  - **R-5 strictly additive — no conflict surface.** R-5 introduces a new SSE event (`code_output_line`) and consumer; do NOT modify any existing event handlers (`delta`, `tool_end`, `done`, `stream_end`, `confidence`, `sources`, `suggestions`). The existing `messagesByThread` Map shape is preserved — R-5 just adds a new event type that writes to the same buckets.
  - **R-3 emit-path investigation depth.** If Plan 01 captures `logger.warning("suggestion generation failed", exc_info=True)` with a stack trace, the fix may be deeper than a one-line patch — could be `client.chat.completions.create(...)` raising a non-`NotFoundError` exception (e.g., `BadRequestError`, `RateLimitError`, malformed response). The broad `except Exception:` at `threads.py:2509` swallows this. Plan 01 may need to narrow the exception class OR re-raise in a more controlled way. If `logger.info("suggestions empty for run X")` fires, the fix is upstream (prompt or model) — capture the actual `response.choices[0].message.content` value to confirm.
  - **OpenRouter creds rotation trap.** `OPENROUTER_API_KEY` is currently present in `backend/.env`. If the user rotates `.env` between 067.4 plan and 067.4 execute, Plan 04 must accept `n/a — provider unconfigured` per the multi-provider availability rule rather than block phase closure. Don't re-litigate symmetric-evidence; honor it.
  - **Cross-phase closure UAT — three scoreboards.** Plan 05 must mirror verdicts back into BOTH 067.2-HUMAN-UAT.md AND 067.3-HUMAN-UAT.md AND 067.4's own 067.4-HUMAN-UAT.md. Closes ALL THREE phases together when the strict 12/12 GREEN gate is met. Frontmatter `status` flips to `closed` for all three; STATE.md `Recent Completed Phases` adds entries for 067.2, 067.3, 067.4 in one commit.
  - **Out of scope (preserve carry-forward boundaries):** N-02 multi-step self-QA iteration cap (defer to a UX/cost-tuning phase per Phase 067.2 Plan 06 SUMMARY); D-066-11 langsmith trace gap (resolved as `runs.error` is canonical per Phase 067.2 D-067.2-06-01); historical message URL migration (legacy decay accepted per Phase 067.2 D-067.2-03 boundary).

### Phase 067.5: Frontend Reconcile Fix (Empty-Thread-Until-Refresh)

**Goal**: Close Phase 067.4 Row 11 RED — runtime-reproduced reconcile bug where one of four streaming threads renders EMPTY in the UI after streaming completes (auto-title generated, no cross-thread leak, but message body absent), and a manual page refresh repairs the rendering. Data IS persisted to Postgres; React in-memory state is stale. Phase 067.4 Plan 02 (R-4 active-thread tool-stage handler audit) shipped a static-only fix — all 21 in-factory `setMessages` handlers were verified bucket-targeted post-R-1 — but the static audit explicitly flagged 6 outside-factory write sites + 4 ref-mirror surfaces in `frontend/src/hooks/useMessages.ts` as risk surface that the in-factory refactor did NOT cover (per `.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md` §2.2 "Where R-4 likely IS"). User-driven UAT on 2026-05-09 (4 streaming threads: Bread / Physicists / Photosynthesis / pgvector across mixed providers) reproduced the freeze on 1 of 4 threads. This phase fixes the OUTSIDE-factory + ref-mirror lag surface and closes the carry-forward dual-pass requirement (R-1 cross-thread + R-4 active-thread both pass without F5).
**Depends on**: Phase 067.4 (Plan 02 R-4 static handler audit established the in-factory bucket-targeting baseline; this phase fixes what static analysis could not exercise — the runtime reconcile-on-(re)visit window where `messagesByThreadRef.current.get(threadId)` may hold a stale snapshot from the moment-of-switch); Phase 067.3 (Plan 03 R-1 `messagesByThread: Map<string, Message[]>` per-thread store is the substrate this phase preserves; D-067.3-R1 invariants must hold — no regression to the cross-thread switch behavior); Phase 062 replay-tail HTTP API contract (`GET /runs/{id}/stream?since={offset}`) which the reconcile-on-(re)connect path consumes
**Requirements**: STREAM-04-correctness-round3 (residual gap closure — round3 parent requirement remains pending until Row 11 RED + Row 6 R-1 dual-pass both close per D-067.4-QUALITY-01)
**Success Criteria** (what must be TRUE — every criterion is user-observable behavior, NOT a test/grep proxy):
  1. **Empty-thread-until-refresh repro is GONE.** Re-running the Phase 067.4 Row 11 protocol (4 streaming threads with diverse prompts: Bread / Physicists / Photosynthesis / pgvector across mixed providers — OpenAI, Anthropic, OpenRouter), all 4 threads render their assistant message body in the UI WITHOUT requiring a page refresh. Each thread's auto-title generates, no cross-thread content leak, and each thread's message body is visible upon navigation to that thread (no F5 needed). Run 5 consecutive cycles; 5/5 cycles must pass for the phase to close (per D-067.4-QUALITY-01 lived-experience requirement).
  2. **R-1 cross-thread switch dual-pass holds (Phase 067.4 Row 6).** Concurrent multi-step streaming on 2+ threads with thread-switch mid-stream — both threads render their full content, no token loss, no cross-talk, no content leak (mirrors Phase 067.3 R-1 GREEN baseline run_ids `8cb9c8cd-…` + `7742ed37-…`). The fix in Plan 01 must NOT regress R-1 — `messagesByThread` per-thread store semantics preserved.
  3. **Reconcile path uses fresh state, not ref-mirror snapshot.** The fix targets one or more of the 4 ref-mirror surfaces RESEARCH.md flagged: `messagesByThreadRef` (`useMessages.ts:384-387` useEffect); `messagesRef` (`:445-450` useEffect); `reconcile`'s `messagesByThreadRef.current.get(threadId)` read at `:952`; or the loadMessages MERGE logic at `:639-651` that may keep an empty placeholder while the DB-persisted row sits unfetched. Acceptance: the runtime repro (Criterion 1) no longer reproduces AND a vitest regression test simulates the (i) Thread A streaming → (ii) thread switch → (iii) thread switch back → (iv) assert `messagesByThread.get(threadA)` has the post-`done` final state sequence and passes deterministically.
  4. **No regression to the Phase 067.4 Plan 01/02/03 GREEN baseline.** Re-running the 3 GREEN rows from 067.4-HUMAN-UAT.md (Row 1 R-3 suggestion pills, Row 2 R-4 active-thread tool-stage spinner-to-result transition without thread nav, Row 3 R-5 code-execution heartbeat + live elapsed counter) all still pass after Plan 01's fix lands. SSE replay evidence captured for at least one re-test run per row.
  5. **Phase 067.4 closing-UAT scoreboard advances Row 11 from RED → GREEN.** `067.4-HUMAN-UAT.md` Row 11 verdict updates from `❌ RED — empty-thread-until-refresh on 1/4` to GREEN with the 5/5 cycle evidence committed inline (run_ids + per-thread title + per-thread body presence assertion). The remaining 9 user-pending rows are NOT in 067.5's scope — only Row 11 (the only RED) is escalated.
**Plans**: 2 plans expected, narrowly scoped:
  - **Plan 01 — Frontend reconcile fix.** (a) `git diff 19bedcd..4a70efa -- frontend/src/hooks/useMessages.ts` to baseline the 6 outside-factory write sites + 4 ref-mirror surfaces (RESEARCH.md §2.2 lines 257-269); (b) Chrome MCP runtime repro of the Row 11 protocol — submit Bread / Physicists / Photosynthesis / pgvector across 4 threads with mixed providers, navigate away mid-stream on at least 2, return after completion; capture which thread renders empty + the SSE consumer state via React DevTools/network panel; (c) implement the targeted fix on the surface the repro implicates (most-likely candidates per RESEARCH.md "Most-likely R-4 root cause" §2.2: stale `messagesRef.current` / `messagesByThreadRef.current` during reconcile-on-(re)visit window, OR SSE consumer aborted by `loadAbortRef.current?.abort()` cascade at `:568`, OR loadMessages MERGE at `:639-651` keeping empty placeholder); (d) vitest regression test extending the existing `frontend/src/__tests__/hooks/useMessages.test.ts` mock pattern (vi.hoisted + vi.mock for api module) — simulate Thread A stream → switch → switch back → assert post-`done` state present in `messagesByThread.get(threadA)`; (e) preserve `messagesByThread` Map shape and per-thread store semantics from Phase 067.3 Plan 03 R-1 (no API surface changes, no new fields beyond what reconcile needs).
  - **Plan 02 — Closing UAT for Row 11 + R-1 dual-pass.** (`autonomous: false` — requires Chrome MCP authenticated session + 5/5 lived-experience cycles per D-067.4-QUALITY-01.) Re-run Row 11 protocol 5 consecutive cycles with explicit cycle log. Re-run Row 6 (R-1 cross-thread switch) symmetric protocol — 2+ concurrent streams with mid-stream switch. Re-run Phase 067.4 Plan 01/02/03 regression rows (Row 1 R-3, Row 2 R-4, Row 3 R-5) for no-regression evidence. Update `067.4-HUMAN-UAT.md` Row 11 verdict from RED → GREEN with run_ids + per-thread evidence. Single commit covering 067.4 UAT mirror + 067.5 SUMMARY.
**Risks / pitfalls**:
  - **R-1 regression risk is HIGH.** Plan 01 touches the same file (`frontend/src/hooks/useMessages.ts`) and adjacent code paths (ref-mirror useEffects, reconcile, loadMessages MERGE) that Phase 067.3 Plan 03 R-1 refactored. The fix MUST preserve `messagesByThread: Map<string, Message[]>` shape AND the per-thread bucket semantics. Plan 02's closing UAT must spot-check R-1 cross-thread switch protocol alongside Row 11 — both pass, OR phase does NOT close (D-067.4-QUALITY-01 dual-pass).
  - **Runtime repro may be intermittent.** The Row 11 RED was 1-of-4 frequency under user-driven 4-thread protocol. Plan 01 must build repro confidence (e.g., 3+ reproductions across separate browser sessions) BEFORE shipping a fix; otherwise the "fix" may merely shift timing. If Chrome MCP cannot reproduce on a single session, Plan 01 includes a pivot decision: instrument the suspected surface (add console.warn on `messagesByThreadRef.current.get(threadId)` returning empty bucket while `messagesByThread` state has data) and ship instrumentation-as-detection on a feature flag for user re-test. Don't ship a speculative fix without runtime confirmation.
  - **Static audit blind spot.** Phase 067.4 Plan 02 RESEARCH explicitly noted "21/21 in-factory handlers ARE bucket-targeted post-R-1" — the static audit was correct, the bug is genuinely OUTSIDE the factory or in the ref-mirror lag. Don't re-litigate the in-factory handlers (Plan 02 already shipped that audit + the regression vitest at `frontend/src/__tests__/hooks/useMessages.test.ts`). Focus on the 6 outside-factory + 4 ref-mirror surfaces RESEARCH.md §2.2 lines 257-269 enumerated.
  - **SSE consumer abort cascade is a leading hypothesis.** RESEARCH.md §2.2 line 283 flags `loadAbortRef.current?.abort()` at `useMessages.ts:568` as a cascade-abort risk: if it cancels the SSE consumer for the streaming thread when `loadMessages` runs on a different thread, the `tool_end` / `done` events for the streaming thread never land. Plan 01's repro must include a network-panel check on the `/runs/{id}/stream?since=N` connection state during cross-thread navigation — if the consumer aborted, the fix is in `loadMessages` lines 565-633 NOT in the ref-mirror useEffects.
  - **Out of scope (preserve carry-forward boundaries):** Rows 4-10 + Row 12 from `067.4-HUMAN-UAT.md` (the user-pending regression spot-checks: OpenRouter synthetic-timeout protocol, R-2/R-2-IDOR/067.2-5 sandbox download regression, N-01 router regression, multi-step pipeline cross-provider) — these are NOT in 067.5 scope. Phase 067.4's `partial` closure already covered them via automated regression (`pytest tests/unit/` 12/12 GREEN, vitest 4/4 GREEN, TypeScript clean). Driving them via Chrome MCP is high-value-but-not-strictly-required for closing 067.5; the user retains optional ownership. The R-5 sandbox stdout streaming polish (SEED-008) is also OUT OF SCOPE — line-by-line streaming is upstream-blocked on llm-sandbox `InteractiveSandboxSession` and was deferred with re-open trigger per Phase 067.4 D-067.4-R5-01-amended.
