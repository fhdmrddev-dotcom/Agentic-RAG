---
phase: 26-citations-confidence-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/supabase/migrations/016_citations_chunk_index.sql
  - backend/app/services/retrieval_service.py
  - backend/tests/unit/test_retrieval_service.py
autonomous: true
requirements: [CITE-01, CITE-02, CONF-01, CONF-02]
must_haves:
  truths:
    - "search_documents returns a tuple of (list[dict], float) where float is the average vector cosine similarity"
    - "Each enriched result dict contains a chunk_index key sourced from the match_document_chunks RPC"
    - "The SQL RPC match_document_chunks returns chunk_index in its result set"
    - "All existing retrieval tests pass after mock fixes"
  artifacts:
    - path: "backend/supabase/migrations/016_citations_chunk_index.sql"
      provides: "Updated match_document_chunks RPC with chunk_index in RETURNS TABLE"
      contains: "chunk_index integer"
    - path: "backend/app/services/retrieval_service.py"
      provides: "Modified search_documents return type + _avg_cosine helper + chunk_index enrichment"
      exports: ["search_documents"]
    - path: "backend/tests/unit/test_retrieval_service.py"
      provides: "Fixed and extended retrieval tests"
      min_lines: 150
  key_links:
    - from: "backend/app/services/retrieval_service.py"
      to: "match_document_chunks RPC"
      via: "supabase.rpc call"
      pattern: "supabase\\.rpc\\(\"match_document_chunks\""
    - from: "backend/app/services/retrieval_service.py"
      to: "_enrich_with_filenames"
      via: "chunk_index passthrough from RPC row"
      pattern: "chunk_index.*row\\.get"
---

<objective>
Modify the retrieval service to return chunk_index in enriched results and average vector similarity as a second return value, enabling citation and confidence features in Plan 02.

Purpose: Establishes the data pipeline that Plan 02 consumes to emit citations and confidence SSE events.
Output: Updated SQL RPC, modified retrieval_service.py, passing test suite.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-citations-confidence-backend/26-CONTEXT.md
@.planning/phases/26-citations-confidence-backend/26-RESEARCH.md

<interfaces>
<!-- Current retrieval_service.py signatures (to be modified) -->
From backend/app/services/retrieval_service.py:
```python
def _enrich_with_filenames(rows: list[dict], supabase: Client) -> list[dict]:
    # Currently builds: {"content", "document_id", "filename", "similarity", "metadata"?}
    # Must add: "chunk_index": row.get("chunk_index")

def search_documents(
    query: str,
    user_id: str,
    supabase: Client,
    metadata_filter: dict | None = None,
    user_settings: UserEffectiveSettings | None = None,
    folder_ids: list[str] | None = None,
) -> list[dict]:
    # Current return: list[dict]
    # New return: tuple[list[dict], float]  — (enriched_results, avg_vector_similarity)
```

From backend/supabase/migrations/002_module2_byo_retrieval.sql:
```sql
-- Current match_document_chunks RETURNS TABLE:
-- (id uuid, document_id uuid, content text, similarity float)
-- Must add: chunk_index integer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL migration + retrieval_service changes (chunk_index + return type)</name>
  <files>
    backend/supabase/migrations/016_citations_chunk_index.sql,
    backend/app/services/retrieval_service.py
  </files>
  <read_first>
    backend/app/services/retrieval_service.py,
    backend/supabase/migrations/002_module2_byo_retrieval.sql
  </read_first>
  <action>
1. Create `backend/supabase/migrations/016_citations_chunk_index.sql`:
   - `CREATE OR REPLACE FUNCTION match_document_chunks(...)` preserving all existing parameters: `query_embedding vector(1536)`, `match_user_id uuid`, `match_count integer DEFAULT 5`, `match_threshold float DEFAULT 0.3`, `metadata_filter jsonb DEFAULT NULL`, `p_folder_ids uuid[] DEFAULT NULL`
   - RETURNS TABLE adds `chunk_index integer` after `similarity float`: `RETURNS TABLE (id uuid, document_id uuid, content text, similarity float, chunk_index integer)`
   - In the SELECT body, add `dc.chunk_index` after the similarity expression
   - Keep LANGUAGE plpgsql SECURITY DEFINER
   - Add comment at top: `-- Migration 016: Add chunk_index to match_document_chunks RPC for Phase 26 citations`

2. Modify `backend/app/services/retrieval_service.py`:

   a. Add `_avg_cosine` helper function after `_enrich_with_filenames` (before the Public API section):
   ```python
   def _avg_cosine(rows: list[dict]) -> float:
       """Average cosine similarity from vector search rows. Returns 0.0 if no rows."""
       sims = [row["similarity"] for row in rows if row.get("similarity") and row["similarity"] > 0]
       return sum(sims) / len(sims) if sims else 0.0
   ```

   b. In `_enrich_with_filenames`, add `chunk_index` to the entry dict (per D-01, D-02):
   Change the entry dict construction to include:
   ```python
   "chunk_index": row.get("chunk_index"),
   ```
   Insert this line after the `"filename"` line and before the `"similarity"` line.

   c. Change `search_documents` return type annotation from `-> list[dict]` to `-> tuple[list[dict], float]` (per D-08).

   d. In the vector-only path (the `if not hybrid_enabled:` block):
   - After `rows = _vector_search(...)`, add: `avg_sim = _avg_cosine(rows)`
   - Change `return _enrich_with_filenames(rows, supabase)` to `return _enrich_with_filenames(rows, supabase), avg_sim`

   e. In the hybrid path:
   - After `if not vector_rows and not keyword_rows:`, change `return []` to `return [], 0.0`
   - After `keyword_rows = _keyword_search(...)` and before `fused = _rrf_fuse(...)`, add: `avg_sim = _avg_cosine(vector_rows)`
   - Change the final `return _enrich_with_filenames(candidates, supabase)` to `return _enrich_with_filenames(candidates, supabase), avg_sim`

   Per D-09: In hybrid mode, `_avg_cosine(vector_rows)` averages only vector search results (which have real cosine similarity). Keyword-only rows have no cosine similarity. If `vector_rows` is empty (keyword-only path), `_avg_cosine` returns 0.0.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && source venv/Scripts/activate && python -c "from app.services.retrieval_service import search_documents; import inspect; sig = inspect.signature(search_documents); print('Return annotation:', sig.return_annotation); assert 'tuple' in str(sig.return_annotation).lower() or sig.return_annotation == tuple[list[dict], float], 'Return type not tuple'"</automated>
  </verify>
  <acceptance_criteria>
    - backend/supabase/migrations/016_citations_chunk_index.sql exists and contains `chunk_index integer` in RETURNS TABLE
    - backend/supabase/migrations/016_citations_chunk_index.sql contains `dc.chunk_index` in SELECT
    - retrieval_service.py contains `def _avg_cosine(rows: list[dict]) -> float:`
    - retrieval_service.py `_enrich_with_filenames` entry dict contains `"chunk_index": row.get("chunk_index")`
    - retrieval_service.py `search_documents` return annotation contains `tuple[list[dict], float]`
    - retrieval_service.py vector-only path returns `_enrich_with_filenames(rows, supabase), avg_sim`
    - retrieval_service.py hybrid path empty-result returns `[], 0.0`
    - retrieval_service.py hybrid path final return includes `, avg_sim`
  </acceptance_criteria>
  <done>search_documents returns (list[dict], float) with chunk_index in each enriched dict; SQL migration ready to apply</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix pre-existing test failures and add new retrieval tests</name>
  <files>backend/tests/unit/test_retrieval_service.py</files>
  <read_first>
    backend/tests/unit/test_retrieval_service.py,
    backend/app/services/retrieval_service.py
  </read_first>
  <behavior>
    - Existing tests: All 7 existing tests pass after fixing mock data (add "id" field to rpc_data entries) and unpacking tuple returns
    - test_returns_chunk_index_in_enriched_results: rpc_data with chunk_index=3 -> result[0]["chunk_index"] == 3
    - test_returns_avg_similarity_as_second_value: rpc_data with similarity 0.9 and 0.7 -> avg_sim == 0.8
    - test_returns_zero_avg_sim_when_no_results: empty rpc_data -> avg_sim == 0.0
    - test_hybrid_empty_returns_tuple: hybrid enabled, no results -> ([], 0.0)
    - test_chunk_index_none_when_missing: rpc_data without chunk_index field -> result[0]["chunk_index"] is None
  </behavior>
  <action>
1. Fix ALL existing test mock data: Every `rpc_data` entry must include `"id": "chunk-uuid-N"` (unique per entry). The pre-existing 6 failures are caused by `_rrf_fuse` doing `row["id"]` on rows that lack `"id"`. The default settings mock enables hybrid search, so all tests hit the hybrid path which calls `_rrf_fuse`.

2. Fix ALL existing tests that check `search_documents` return value: The function now returns `(list, float)` not `list`. Update assertions:
   - `test_returns_empty_list_when_no_chunks_match`: `result, avg_sim = search_documents(...)` then `assert result == []` and `assert avg_sim == 0.0`
   - `test_returns_empty_list_when_rpc_data_is_none`: same tuple unpacking
   - `test_returns_formatted_results`: `result, avg_sim = search_documents(...)` then check `result[0]` fields
   - `test_uses_unknown_filename_when_doc_not_found`: same tuple unpacking
   - `test_returns_multiple_results`: same tuple unpacking
   - `test_joins_results_with_documents_table`: same tuple unpacking (just add `_` for avg_sim)
   - `test_calls_embed_texts_with_query`: no return check needed but still unpacking if used
   - `test_calls_supabase_rpc_match_document_chunks`: no return check needed

3. Add `_make_supabase` second rpc call support: The hybrid path calls `supabase.rpc` twice (once for vector, once for keyword). Use `side_effect` list on `sb.rpc` to return different builders for each call, OR mock `settings` to disable hybrid search for vector-only tests. Simplest approach: patch `app.services.retrieval_service.settings` with `hybrid_search_enabled=False` in tests that don't test hybrid behavior, so they go through the vector-only path.

4. Add new test class `TestSearchDocumentsPhase26`:
   - `test_returns_chunk_index_in_enriched_results`: Create rpc_data with `"chunk_index": 3`. Patch settings with `hybrid_search_enabled=False`. Call search_documents, unpack `result, avg_sim`. Assert `result[0]["chunk_index"] == 3`.
   - `test_returns_avg_similarity_as_second_value`: Create rpc_data with two rows, similarity 0.9 and 0.7. Assert `avg_sim == pytest.approx(0.8)`.
   - `test_returns_zero_avg_sim_when_no_results`: Empty rpc_data. Assert `avg_sim == 0.0`.
   - `test_hybrid_empty_returns_tuple`: Patch settings with `hybrid_search_enabled=True`. Mock both rpc calls to return empty. Assert `result == []` and `avg_sim == 0.0`.
   - `test_chunk_index_none_when_missing`: rpc_data without chunk_index field. Assert `result[0]["chunk_index"] is None`.

5. For patching settings, use: `@patch("app.services.retrieval_service.settings", MagicMock(hybrid_search_enabled=False, retrieval_top_k=5, retrieval_match_threshold=0.3))` or create a helper mock settings object.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && source venv/Scripts/activate && python -m pytest tests/unit/test_retrieval_service.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_retrieval_service.py -v` exits with code 0
    - test_retrieval_service.py contains `"id":` in all rpc_data entries
    - test_retrieval_service.py contains `test_returns_chunk_index_in_enriched_results`
    - test_retrieval_service.py contains `test_returns_avg_similarity_as_second_value`
    - test_retrieval_service.py contains `test_returns_zero_avg_sim_when_no_results`
    - test_retrieval_service.py contains `test_chunk_index_none_when_missing`
    - All assertions use tuple unpacking: `result, avg_sim = search_documents(...)`
  </acceptance_criteria>
  <done>All existing tests pass with fixed mocks; 5 new tests verify chunk_index passthrough and avg_similarity return value</done>
</task>

</tasks>

<verification>
cd "C:/Vibe Apps/Agentic RAG/backend" && source venv/Scripts/activate && python -m pytest tests/unit/test_retrieval_service.py -v
</verification>

<success_criteria>
- search_documents returns tuple[list[dict], float]
- Enriched results include chunk_index field
- _avg_cosine helper computes mean of cosine similarity values
- SQL migration ready with chunk_index in RETURNS TABLE
- All unit tests green (0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/26-citations-confidence-backend/26-01-SUMMARY.md`
</output>
