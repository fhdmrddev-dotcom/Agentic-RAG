---
phase: 100-ephemeral-template-upload
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - backend/app/db/workspace.py
  - backend/app/services/workspace_service.py
autonomous: true
requirements: [TMPL-01]

must_haves:
  truths:
    - "The two asyncpg read seams (get_file_by_path, list_files_in_thread) filter out expired rows, gated so NULL-expiry agent files are byte-identical (D-06/D-11)"
    - "An expired-but-present read can be distinguished from truly-absent so the D-10 'template expired' error is possible"
    - "write_file can persist kind + expires_at through to the row (D-12 upload sets them)"
    - "An OOXML workspace_read returns the clean binary stub, not a garbage UTF-8 decode (A1 hygiene)"
  artifacts:
    - path: "backend/app/db/workspace.py"
      provides: "kind/expires_at in INSERT/SELECT; gated WHERE on the 2 asyncpg read seams; is_expired flag"
      contains: "expires_at IS NULL OR expires_at > now()"
    - path: "backend/app/services/workspace_service.py"
      provides: "write_file threads kind/expires_at; OOXML MIME prefix in _BINARY_MIME_PREFIXES; expired→FileNotFoundError_ with 'template expired'"
      contains: "application/vnd.openxmlformats-officedocument"
  key_links:
    - from: "backend/app/db/workspace.py get_file_by_path"
      to: "the gated WHERE clause"
      via: "asyncpg SQL"
      pattern: "expires_at IS NULL OR expires_at > now"
    - from: "backend/app/services/workspace_service.py write_file"
      to: "upsert_workspace_file"
      via: "kind/expires_at kwargs"
      pattern: "kind=|expires_at="
---

<objective>
Build the asyncpg data layer for ephemeral templates — the SECOND of the two disjoint data-access layers (the supabase-py REST layer is Plan 100-04). This is the consumer SC#3 matters MOST for: the agent/workflow run reads templates through THIS path. Add the kind/expires_at columns to the INSERT/SELECT, gate the two asyncpg read seams (Pitfall 1 — the #1 pitfall: a filter on the REST layer is invisible here), expose an `is_expired` flag for the D-10 error, thread kind/expires_at through `write_file`, and add the OOXML MIME prefix so a template read returns a clean binary stub (A1 hygiene).

Purpose: This is the foundation Plans 04 (upload + REST gates) and 05 (tools + sweep + pin) build on. Interface-first: it defines the `write_file` signature and the `is_expired` contract.
Output: modified `db/workspace.py` + `workspace_service.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-ephemeral-template-upload/100-CONTEXT.md
@.planning/phases/100-ephemeral-template-upload/100-RESEARCH.md
@.planning/phases/100-ephemeral-template-upload/100-PATTERNS.md

<interfaces>
<!-- Current signatures the executor extends. Extracted from codebase — no exploration needed. -->

From backend/app/db/workspace.py:
- async def upsert_workspace_file(pool, *, thread_id, path, size_bytes, mime_type, content_inline, content_storage_path, created_by) -> tuple[UUID, bool]
  INSERT column list: (thread_id, path, size_bytes, mime_type, content_inline, content_storage_path, created_by)
- async def get_file_by_path(pool, thread_id, path) -> dict | None  (SELECT ... WHERE thread_id=$1 AND path=$2)
- async def list_files_in_thread(pool, thread_id, prefix=None) -> list[dict]  (SELECT id, path, size_bytes, mime_type, created_at, updated_at WHERE thread_id=$1 [AND path LIKE $2])
- async def get_storage_paths_for_file(pool, workspace_file_id) -> list[str]  (UNIONs file + ALL version storage paths — reuse verbatim in the sweep, Plan 05)

From backend/app/services/workspace_service.py:
- MAX_FILE_SIZE = 10 * 1024 * 1024 ; DEFAULT_INLINE_THRESHOLD = 256 * 1024
- _BINARY_MIME_PREFIXES = ("image/", "audio/", "video/", "application/pdf", "application/zip", "application/gzip", "application/octet-stream")
- async def write_file(pool, supabase, *, thread_id, user_id, path, content, inline_threshold=DEFAULT_INLINE_THRESHOLD) -> dict
- async def read_file(...) -> dict  (binary-stub branch at line 318: `if any(mime.startswith(p) for p in _BINARY_MIME_PREFIXES)`)
- class FileNotFoundError_(WorkspaceError)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate the 2 asyncpg read seams + add kind/expires_at to INSERT/SELECT + is_expired flag</name>
  <files>backend/app/db/workspace.py</files>
  <read_first>
    - backend/app/db/workspace.py (FULL — upsert_workspace_file:8, get_file_by_path:103, list_files_in_thread:133, get_storage_paths_for_file:218)
    - backend/tests/test_workspace_template.py (the xfail stubs this task flips: test_expired_tool_read_errors, test_agent_files_unchanged)
    - .planning/phases/100-ephemeral-template-upload/100-PATTERNS.md (the "db/workspace.py" section: gated SELECT + D-10 is_expired nuance)
  </read_first>
  <behavior>
    - list_files_in_thread excludes rows where expires_at <= now(); NULL-expiry rows always returned (gated no-op)
    - get_file_by_path returns the row (NOT filtered out) but adds a computed `is_expired` boolean so the caller can raise the D-10 "template expired" error vs generic not-found
    - The SELECT column lists now include kind, expires_at
    - upsert_workspace_file accepts kind + expires_at kwargs (both default None) and writes them
  </behavior>
  <action>
In `backend/app/db/workspace.py`:

1. **`upsert_workspace_file`** — add two keyword params `kind: str | None = None`, `expires_at = None` (datetime). Extend the INSERT column list to `(thread_id, path, size_bytes, mime_type, content_inline, content_storage_path, created_by, kind, expires_at)` with `$8, $9`, and the `ON CONFLICT DO UPDATE SET` to also set `kind = EXCLUDED.kind, expires_at = EXCLUDED.expires_at`. Pass `kind, expires_at` as the two new positional args. (Adding nullable columns the OLD callers omit is safe — they pass None → NULL.)

2. **`get_file_by_path`** — do NOT add a filtering WHERE (D-10 needs to distinguish expired-present from absent). Instead add `kind, expires_at` to the SELECT and a computed column: `(expires_at IS NOT NULL AND expires_at <= now()) AS is_expired`. The returned dict gains `kind`, `expires_at`, `is_expired`. So the SELECT becomes:
```sql
SELECT id, thread_id, path, size_bytes, mime_type,
       content_inline, content_storage_path,
       created_by, created_at, updated_at,
       kind, expires_at,
       (expires_at IS NOT NULL AND expires_at <= now()) AS is_expired
FROM workspace_files
WHERE thread_id = $1 AND path = $2
```

3. **`list_files_in_thread`** — ADD the gated expiry filter to BOTH branches' WHERE (the listing must hide expired templates; NULL-expiry agent files always pass):
```sql
WHERE thread_id = $1 [AND path LIKE $2] AND (expires_at IS NULL OR expires_at > now())
```
Also add `kind, expires_at` to the SELECT column list in both branches so the panel/tool can surface them.

4. **`get_file_by_id`** — add `kind, expires_at` + the same `is_expired` computed column to its SELECT (used by the REST content route in Plan 04; keep it unfiltered for the same D-10 distinction).

Do NOT change `get_storage_paths_for_file` — Plan 05's sweep reuses it verbatim.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_workspace_template.py -q -k "agent_files_unchanged or expired" </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "expires_at IS NULL OR expires_at > now()" backend/app/db/workspace.py` matches in list_files_in_thread (both prefix + no-prefix branches)
    - `grep -n "is_expired" backend/app/db/workspace.py` matches (get_file_by_path AND get_file_by_id)
    - `grep -n "kind, expires_at" backend/app/db/workspace.py` matches in the INSERT column list of upsert_workspace_file
    - `grep -n "EXCLUDED.kind" backend/app/db/workspace.py` matches (ON CONFLICT updates kind/expires_at)
    - The pytest command shows test_agent_files_unchanged passing (NULL-expiry rows unfiltered) — xfail flips to pass OR the stub asserts the SQL shape
    - `cd backend && venv/Scripts/python -c "import app.db.workspace"` imports clean (no syntax error)
  </acceptance_criteria>
  <done>Both asyncpg read seams gated (list filters; get_*_by_* exposes is_expired); INSERT/SELECT carry kind/expires_at; NULL-expiry rows byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread kind/expires_at through write_file + OOXML binary-MIME hygiene + D-10 read error</name>
  <files>backend/app/services/workspace_service.py</files>
  <read_first>
    - backend/app/services/workspace_service.py (FULL: _BINARY_MIME_PREFIXES:42, write_file:209, read_file:297 with the binary-stub branch at 318, FileNotFoundError_:65)
    - backend/app/db/workspace.py (the upsert_workspace_file signature just extended in Task 1)
    - backend/tests/test_workspace_template.py (xfail stubs: test_upload_sets_kind_and_ttl reads write_file's persisted kind; test_expired_tool_read_errors expects "template expired")
  </read_first>
  <behavior>
    - write_file(..., kind="template_input", expires_at=dt) persists both to the row
    - write_file with no kind/expires_at (agent path) writes NULL/NULL → byte-identical (D-11)
    - read_file of an expired template raises FileNotFoundError_("template expired") (D-10) instead of generic not-found
    - read_file of an OOXML mime returns the binary stub (is_binary=True), not a garbage UTF-8 decode
  </behavior>
  <action>
In `backend/app/services/workspace_service.py`:

1. **`_BINARY_MIME_PREFIXES`** — append the OOXML prefix so a `workspace_read` of a docx/pptx/xlsx returns the clean binary stub (A1 hygiene — without this the UTF-8 decode at line 332 produces garbage):
```python
_BINARY_MIME_PREFIXES = (
    "image/", "audio/", "video/", "application/pdf", "application/zip",
    "application/gzip", "application/octet-stream",
    "application/vnd.openxmlformats-officedocument",  # 100: docx/pptx/xlsx → binary stub
)
```

2. **`write_file`** — add two keyword params `kind: str | None = None`, `expires_at=None` (datetime). Pass them through to the `upsert_workspace_file(...)` call (the call at lines 234-243 gains `kind=kind, expires_at=expires_at`). Include `kind`/`expires_at` in the returned dict so the upload handler (Plan 04) can echo them. Existing agent callers pass neither → both default None → NULL → byte-identical.

3. **`read_file`** — implement the D-10 distinction. `get_file_by_path` now returns `is_expired` (Task 1). After the `file_row = await get_file_by_path(...)` call, BEFORE the binary check, add:
```python
if not file_row:
    raise FileNotFoundError_(f"File not found: {path}")
if file_row.get("is_expired"):
    # D-10: name expiry so the model can relay honestly (run-honesty), NOT a generic not-found.
    raise FileNotFoundError_("template expired")
```
(The existing `if not file_row:` stays — just add the is_expired branch after it.)
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_workspace_template.py -q -k "kind_and_ttl or expired_tool_read" && cd backend && venv/Scripts/python -c "import app.services.workspace_service as w; assert any('openxmlformats' in p for p in w._BINARY_MIME_PREFIXES); print('ooxml prefix ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "application/vnd.openxmlformats-officedocument" backend/app/services/workspace_service.py` matches inside _BINARY_MIME_PREFIXES
    - `grep -n "kind=kind" backend/app/services/workspace_service.py` and `grep -n "expires_at=expires_at" backend/app/services/workspace_service.py` match in the write_file → upsert_workspace_file call
    - `grep -n "template expired" backend/app/services/workspace_service.py` matches (the D-10 message in read_file)
    - `grep -n "is_expired" backend/app/services/workspace_service.py` matches (read_file consumes the flag)
    - The verify command prints `ooxml prefix ok` and the two -k tests pass (or flip from xfail)
  </acceptance_criteria>
  <done>write_file persists kind/expires_at; OOXML reads return the binary stub; expired reads raise "template expired" (D-10); agent path byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent tool (asyncpg, no RLS) → workspace_files | Ownership comes from thread scoping (thread_id in every query); no RLS on this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03-01 | Information Disclosure | expired template readable via agent tool | mitigate | list_files_in_thread WHERE adds `(expires_at IS NULL OR expires_at > now())`; read_file raises on is_expired. The agent-tool path is the SC#3-critical consumer (Pitfall 1). |
| T-100-03-02 | Tampering | agent-written files acquiring expiry | mitigate | write_file kind/expires_at default None → NULL; only the upload caller (Plan 04) passes them. Gated filter treats NULL as never-expires (D-11). |
| T-100-03-03 | Spoofing | confabulation on expired read | mitigate | D-10: is_expired raises "template expired" (not generic not-found) so the model relays honestly instead of inventing about a file the user knows they uploaded. |
| T-100-03-04 | Tampering | garbage decode of OOXML bytes | mitigate | OOXML MIME prefix → binary stub; bytes never UTF-8-decoded into the agent context (also avoids surfacing raw ZIP bytes). |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_workspace_template.py -q` — the agent_files_unchanged + expired-read tests pass
- Both modules import clean
- list_files_in_thread gated; get_file_by_path/id expose is_expired; write_file threads kind/expires_at; OOXML → binary stub; expired read → "template expired"
</verification>

<success_criteria>
- The asyncpg read path (the SC#3-critical consumer) filters expired templates, gated so agent files are byte-identical (D-11)
- write_file can persist the kind/expires_at that the upload endpoint sets (D-12)
- An expired read raises the D-10 "template expired" error
- OOXML reads return the clean binary stub (A1 hygiene)
</success_criteria>

<output>
After completion, create `.planning/phases/100-ephemeral-template-upload/100-03-SUMMARY.md`
</output>
