---
phase: 097-spike-risk-register-template-fill-authoring-feel
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/spike-097/README.md
  - scripts/spike-097/make_template.py
  - scripts/spike-097/templates/risk-register.docx
  - scripts/spike-097/find_risk_folder.py
  - scripts/spike-097/out/.gitkeep
autonomous: false
requirements: []    # SPIKE — closes NO REQ-ID (ROADMAP: "Requirements: None closed"). Acceptance bar = ROADMAP SC#1–4 + VALIDATION evidence map. Do NOT invent REQ coverage.

must_haves:
  truths:
    - "A throwaway home exists at scripts/spike-097/ (repo root, OUTSIDE backend/'s --reload watch tree) with an out/ artifacts dir"
    - "docxtpl==0.20.2 is importable in the backend venv (spike-only — NOT added to Dockerfile.sandbox here)"
    - "A {%tr %} variable-row risk-register.docx template exists and parses via docxtpl"
    - "A real risk-content KB folder id for the test user is confirmed and recorded for the experiments to ground against"
  artifacts:
    - path: "scripts/spike-097/templates/risk-register.docx"
      provides: "The {{jinja}} / {%tr %} template under test (covers unknowns a/b)"
      contains: "{%tr"
    - path: "scripts/spike-097/find_risk_folder.py"
      provides: "Discovery helper listing candidate KB folders + doc counts for the test user"
    - path: "scripts/spike-097/out/spike-config.json"
      provides: "The operator-confirmed folder_id + resolved user_id the experiments consume"
      contains: "folder_id"
  key_links:
    - from: "scripts/spike-097/find_risk_folder.py"
      to: "backend/app/dependencies.py get_supabase()"
      via: "service-role Supabase client construction mirrored read-only"
      pattern: "create_client"
---

<objective>
Stand up the throwaway spike workspace and satisfy the three Wave 0 prerequisites the experiments depend on: (1) a `scripts/spike-097/` home at the repo root with `docxtpl==0.20.2` installed into the backend venv, (2) a `{%tr %}` variable-row `risk-register.docx` template, and (3) a confirmed real risk-content KB folder for the dev test user (`fhdmrd@gmail.com`).

Purpose: every downstream experiment (unknowns a/b/c/d) needs a template to fill, a folder to ground against, and `docxtpl` importable. Without these there is nothing to spike.
Output: the scaffold, the template, a folder-discovery helper, and `out/spike-config.json` (the confirmed `folder_id` + `user_id`).

This is a THROWAWAY SPIKE (SEED-051), not production code. NO SQL migration, NO edit to `backend/app/**`, NO production schema change. All code lives at the repo root under `scripts/spike-097/` — outside `backend/`'s uvicorn `--reload` watch tree (a stray scratch `.py` has wedged the Windows backend before; see CLAUDE.md).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-RESEARCH.md
@.planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Seams the helper mirrors read-only. Do NOT import/modify backend/app/** beyond reading. -->

From backend/app/dependencies.py (service-role Supabase client — mirror this construction in the throwaway helper):
```python
from supabase import create_client, Client
def get_supabase() -> Client:
    _supabase = create_client(settings.supabase_url, settings.supabase_service_role_key)
```
Local dev DB: Supabase at SUPABASE_URL in backend/.env (local Postgres on :54322). Test user: fhdmrd@gmail.com.

Folder subtree (BFS, node + descendants) — mirror the pattern, do not import:
- backend/app/api/kb.py:186  ("Collect all folder IDs in the subtree (BFS) including the node itself")
- backend/app/api/threads.py:1210  (`_wf_get_subtree(root_id, folders)`)

Sandbox shipped libs (python-docx==1.1.2 etc.; docxtpl ABSENT): backend/Dockerfile.sandbox lines 37-49.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold scripts/spike-097/, install docxtpl, generate the {%tr %} template</name>
  <read_first>
    - .planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-RESEARCH.md  (## Architecture Patterns → "Recommended throwaway layout"; "## Code Examples")
    - CLAUDE.md  (the "Never write scratch/temp .py into backend/" rule; venv rule)
    - backend/Dockerfile.sandbox  (lines 37-49 — the shipped docx lib set; docxtpl is NOT there — confirms docxtpl is the only new dep)
  </read_first>
  <files>scripts/spike-097/README.md, scripts/spike-097/make_template.py, scripts/spike-097/templates/risk-register.docx, scripts/spike-097/out/.gitkeep</files>
  <action>
    Create the repo-root throwaway home `scripts/spike-097/` with subdirs `templates/` and `out/` (add `out/.gitkeep`). Write `README.md` stating: this is the throwaway Phase 097 spike (SEED-051); all artifacts land in `out/`; docxtpl is installed into the backend venv only (Phase 101 adds it to `Dockerfile.sandbox`); run from repo root.

    Install docxtpl into the BACKEND VENV ONLY (not a production dep yet). On Windows PowerShell: `backend\venv\Scripts\python.exe -m pip install docxtpl==0.20.2` (verify the venv path — research cites `backend/venv`). This pulls `jinja2` + `lxml` transitively. Do NOT touch `backend/Dockerfile.sandbox`, `backend/requirements*.txt`, or any `backend/app/**` file.

    Write `make_template.py` that uses `python-docx` to GENERATE `templates/risk-register.docx` programmatically (no manual Word authoring). The template must contain:
      - A title paragraph with scalar tags: `Project: {{ project_name.value }}` and `Date: {{ report_date.value }}`.
      - A table with a header row (Risk ID | Cause | Event | Effect | P | I | Score | Response | Owner | Status) and ONE repeating body row built with docxtpl's `{%tr %}` row-loop: place `{%tr for r in rows %}` in the first cell of the body row and `{%tr endfor %}` so the body row repeats once per `r` (consult docxtpl docs for the exact `{%tr %}` placement convention — the loop tag goes inside table-row cells, NOT in a free paragraph, or it raises TemplateSyntaxError, Pitfall 4). Each body cell holds the matching tag: `{{ r.risk_id.value }}`, `{{ r.cause.value }}`, `{{ r.event.value }}`, `{{ r.effect.value }}`, `{{ r.probability.value }}`, `{{ r.impact.value }}`, `{{ r.score }}` (score is render-time COMPUTE, no `.value`), `{{ r.response_strategy.value }}`, `{{ r.owner.value }}`, `{{ r.status.value }}`.
    Run `make_template.py` to emit `templates/risk-register.docx`.
  </action>
  <acceptance_criteria>
    - `scripts/spike-097/out/.gitkeep` exists; `scripts/spike-097/README.md` exists and contains the string "throwaway" and "SEED-051".
    - `backend\venv\Scripts\python.exe -c "import docxtpl, jinja2.sandbox; print(docxtpl.__version__)"` prints `0.20.2`.
    - `scripts/spike-097/templates/risk-register.docx` exists AND `backend\venv\Scripts\python.exe -c "from docxtpl import DocxTemplate; print(sorted(DocxTemplate(r'scripts/spike-097/templates/risk-register.docx').get_undeclared_template_variables()))"` lists at least `project_name`, `report_date`, and `rows`.
    - NO file under `backend/app/`, `backend/Dockerfile.sandbox`, or `backend/requirements*.txt` was modified (git status shows changes only under `scripts/spike-097/`).
  </acceptance_criteria>
  <verify>
    <automated>backend\venv\Scripts\python.exe -c "from docxtpl import DocxTemplate; import docxtpl; assert docxtpl.__version__=='0.20.2'; v=set(DocxTemplate(r'scripts/spike-097/templates/risk-register.docx').get_undeclared_template_variables()); assert {'project_name','report_date','rows'} <= v, v; print('OK', sorted(v))"</automated>
  </verify>
  <done>docxtpl 0.20.2 importable in the venv; the {%tr %} template exists and exposes the expected placeholder set; no backend/ source touched.</done>
</task>

<task type="auto">
  <name>Task 2: Write find_risk_folder.py — list candidate risk-content KB folders for the test user</name>
  <read_first>
    - backend/app/dependencies.py  (get_supabase / create_client construction to mirror)
    - backend/app/api/kb.py  (around line 186 — the BFS subtree-collect helper to mirror)
    - .planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-RESEARCH.md  (## Environment Availability — assumption A2: a real risk-content KB folder is THE prerequisite)
  </read_first>
  <files>scripts/spike-097/find_risk_folder.py</files>
  <action>
    Write `find_risk_folder.py` (throwaway). It must:
      - Add `backend/` to `sys.path` (e.g. `sys.path.insert(0, str(Path(__file__).parents[2] / "backend"))`) and load `backend/.env` (via `dotenv.load_dotenv`) so `settings` resolves; build a service-role Supabase client by mirroring `dependencies.get_supabase()` (`create_client(settings.supabase_url, settings.supabase_service_role_key)`).
      - Resolve the test user's `user_id` (UUID) for `fhdmrd@gmail.com` — via `supabase.auth.admin.list_users()` (filter by email) OR a direct query against the local Postgres `auth.users` (:54322). Print the resolved `user_id`.
      - List the user's folders (the `folders` table: `id`, `name`, `parent_id`, `user_id`) and, for each folder, count documents in its subtree (mirror the BFS subtree-collect from `kb.py:186`, then count `documents` rows whose `folder_id` is in the subtree).
      - Rank folders by likely risk content: name match on `risk|register|project|charter|status` (case-insensitive) first, then by subtree doc count.
      - Write the ranked candidates (each: `folder_id`, `name`, `subtree_folder_ids`, `doc_count`) to `out/kb-folders.json` and print the top 5 to stdout for the operator.
    Do NOT pick the folder automatically — that is the operator's call in Task 3.
  </action>
  <acceptance_criteria>
    - `scripts/spike-097/out/kb-folders.json` exists and is a JSON array; each element has keys `folder_id`, `name`, `subtree_folder_ids` (a list), and `doc_count` (an int).
    - At least one candidate has `doc_count >= 1` (proving the test user has ingested documents to ground against — research A2). If NONE do, the task surfaces this clearly so the operator can ingest a small risk corpus before proceeding (the experiments cannot ground on an empty folder).
    - stdout printed the resolved test-user `user_id` (a UUID).
  </acceptance_criteria>
  <verify>
    <automated>backend\venv\Scripts\python.exe -c "import json; d=json.load(open(r'scripts/spike-097/out/kb-folders.json')); assert isinstance(d,list) and d, 'empty'; assert all({'folder_id','name','subtree_folder_ids','doc_count'} <= set(x) for x in d); print('candidates:', len(d), 'max_docs:', max(x['doc_count'] for x in d))"</automated>
  </verify>
  <done>out/kb-folders.json lists ranked candidate folders with subtree ids + doc counts; at least one folder has documents to ground against.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator confirms the risk-content folder → out/spike-config.json</name>
  <read_first>
    - scripts/spike-097/out/kb-folders.json  (the ranked candidates from Task 2)
  </read_first>
  <what-built>find_risk_folder.py produced `out/kb-folders.json` — a ranked list of the test user's KB folders with subtree folder-id lists and document counts. The experiments need ONE confirmed folder to ground the risk-register fill against. Picking which folder actually holds risk-relevant content is a human judgment (semantic, not mechanical) — hence this checkpoint.</what-built>
  <how-to-verify>
    1. Open `scripts/spike-097/out/kb-folders.json` and review the top candidates.
    2. Choose the folder whose documents genuinely contain risk/project content suitable for a risk register (causes, events, owners, mitigations). If none fit, ingest a small risk corpus for `fhdmrd@gmail.com` first (manual upload), then re-run `find_risk_folder.py`.
    3. Confirm the chosen `folder_id`, its `subtree_folder_ids`, and the printed `user_id`.
  </how-to-verify>
  <action>
    After the operator confirms, write `out/spike-config.json` with: `user_id`, `folder_id`, `subtree_folder_ids` (the chosen folder's subtree id list — this is the BOUND retrieval scope the experiments pass to `search_documents(folder_ids=...)`, NOT a prompt hint), and `template_path` = `scripts/spike-097/templates/risk-register.docx`. This file is the single source of truth every experiment plan reads.
  </action>
  <acceptance_criteria>
    - `scripts/spike-097/out/spike-config.json` exists and contains keys `user_id`, `folder_id`, `subtree_folder_ids` (non-empty list), `template_path`.
    - `folder_id` matches one of the candidates in `out/kb-folders.json`.
  </acceptance_criteria>
  <verify>
    <automated>backend\venv\Scripts\python.exe -c "import json; c=json.load(open(r'scripts/spike-097/out/spike-config.json')); cand={x['folder_id'] for x in json.load(open(r'scripts/spike-097/out/kb-folders.json'))}; assert {'user_id','folder_id','subtree_folder_ids','template_path'} <= set(c); assert c['folder_id'] in cand and c['subtree_folder_ids']; print('confirmed folder', c['folder_id'])"</automated>
  </verify>
  <resume-signal>Type "approved" once out/spike-config.json holds the confirmed folder, or describe the issue (e.g., "no risk content — ingesting first").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike script → local Supabase (service role) | The helper uses the service-role key, which BYPASSES RLS. Throwaway, dev-only, repo-root — never shipped. It must read only the test user's own folders/docs (filter by `user_id`). |
| operator → spike-config.json | The confirmed folder scope becomes the bound retrieval scope downstream; a wrong folder grounds the fill on the wrong corpus (a quality miss, not a security breach). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-097-01 | Information Disclosure | service-role Supabase client in find_risk_folder.py | mitigate | Filter every query by the test user's `user_id`; helper is throwaway/dev-only and never imported by backend; key sourced from backend/.env (name-only, never echoed to stdout or out/). |
| T-097-02 | Tampering | docxtpl install into backend venv | accept | Spike-only venv install; NOT added to Dockerfile.sandbox/requirements here (Phase 101 does the production add). No production surface changes. |
| T-097-03 | Elevation of Privilege | stray .py inside backend/ --reload tree | mitigate | ALL spike code lives at repo-root `scripts/spike-097/`; CLAUDE.md hard rule honored — zero files written under `backend/`. |
</threat_model>

<verification>
- `git status` shows new/modified files ONLY under `scripts/spike-097/` (nothing under `backend/`).
- docxtpl 0.20.2 importable in the backend venv; the {%tr %} template parses and exposes `project_name`/`report_date`/`rows`.
- `out/kb-folders.json` lists candidates with non-empty doc counts; `out/spike-config.json` records the operator-confirmed folder + subtree scope + user_id.
</verification>

<success_criteria>
The spike workspace is ready: docxtpl installed (venv-only), a {%tr %} template exists, and a real risk-content KB folder is confirmed and recorded in `out/spike-config.json` for the experiment plans to consume. (Prerequisite gate for ROADMAP SC#1/#2.)
</success_criteria>

<output>
After completion, create `.planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-01-SUMMARY.md`.
</output>
