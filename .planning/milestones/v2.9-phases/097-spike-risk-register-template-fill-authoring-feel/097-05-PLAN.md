---
phase: 097-spike-risk-register-template-fill-authoring-feel
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - scripts/spike-097/CONCLUSION.md
autonomous: false
requirements: []    # SPIKE — closes NO REQ-ID. This plan IS the spike's output (the go/no-go + the recommended schema shape). Acceptance = ROADMAP SC#3 (+ rolls up SC#1/#2/#4).

must_haves:
  truths:
    - "All four unknowns (a–d) have a written answer backed by a named artifact under scripts/spike-097/out/"
    - "A written GO / NO-GO on the docxtpl fill path exists, backed by the captured evidence"
    - "A recommended additive-optional inputs / assets / folder_scope schema shape is written (the spike's output that BECOMES the Phase 098/100/101 lock candidate — nothing committed in the spike)"
    - "The six named failure modes are consolidated as the pre-named Phase 101 UAT rows"
  artifacts:
    - path: "scripts/spike-097/CONCLUSION.md"
      provides: "The spike's deliverable: the 4-unknown roll-up + GO/NO-GO + the recommended inputs/assets/folder_scope schema shape + the Phase 101 UAT seed table"
      contains: "GO"
  key_links:
    - from: "scripts/spike-097/CONCLUSION.md"
      to: "out/field-map.json + out/risk-register-filled.docx + out/corruption.log + out/transcript.md + unknown-a/b/c/d.md"
      via: "synthesis — every claim cites the artifact that backs it"
      pattern: "out/"
---

<objective>
Produce the spike's actual deliverable (SC#3): a written go/no-go on the docxtpl fill path PLUS the recommended additive-optional `inputs` / `assets` / `folder_scope` schema shape — the output that BECOMES the Phase 098/100/101 lock candidate (nothing is committed to `harness.py` in the spike). Synthesize the four unknowns' answers, consolidate the six named failure modes into the pre-named Phase 101 UAT rows, and record the decision.

Purpose: this is why the spike exists — convert the captured evidence (field-map, filled docx, corruption log, transcript) into a decision and a schema recommendation before any production schema is locked. It rolls up SC#1 (end-to-end artifact), SC#2 (4 unknowns answered), and SC#4 (failure-mode UAT seed) into the SC#3 go/no-go.
Output: `scripts/spike-097/CONCLUSION.md`.

THROWAWAY SPIKE. The schema shape is a RECOMMENDATION written to disk — NOT an edit to `backend/app/models/harness.py`, NO SQL migration. The validation evidence is the artifacts already on disk; this plan reads them and decides.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-RESEARCH.md
@.planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-VALIDATION.md
@scripts/spike-097/out/unknown-a.md
@scripts/spike-097/out/unknown-b.md
@scripts/spike-097/out/unknown-c.md
@scripts/spike-097/out/unknown-d.md
@scripts/spike-097/out/corruption.log
@scripts/spike-097/out/field-map.json

<interfaces>
<!-- The schema hypothesis to confirm/revise — verbatim in 097-RESEARCH.md "## Recommended inputs/assets/folder_scope Schema Hypothesis". -->
Current shape (harness.py, do NOT edit): WorkflowDefinition{ slug, version, name, status, phases }.
Hypothesis to validate (all OPTIONAL → old published rows still model_validate cleanly):
  - project_folder_id: UUID | None   (PROJ-01: project = folder + subtree)
  - inputs: list[InputFieldSpec] | None   (AI-derived launch form)
  - assets: list[AssetRef] | None
  - InputFieldSpec{ key, label, type, required, source, enum_options, folder_scope }
  - AssetRef{ asset_id, filename, kind, mime }
  - folder_scope: str | None on LlmAgent/LlmSingle  (PROJ-02: per-phase retrieval narrowing)
Open questions the evidence settles: does InputFieldSpec need a `template_derived` provenance flag? does Cited provenance live in inputs or only in run output? is folder_scope a string path or a resolved id list?
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Draft CONCLUSION.md — 4-unknown roll-up + go/no-go + recommended schema shape + Phase 101 UAT seed</name>
  <read_first>
    - scripts/spike-097/out/unknown-a.md, unknown-b.md, unknown-c.md, unknown-d.md  (the four written answers)
    - scripts/spike-097/out/corruption.log  (the Pitfall 1–6 rows)
    - scripts/spike-097/out/field-map.json  (the cited field-map evidence)
    - .planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-RESEARCH.md  (## Recommended inputs/assets/folder_scope Schema Hypothesis — the shape to confirm/revise; ## Open Questions)
    - .planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-VALIDATION.md  (Validation Sign-Off — the spike's acceptance checklist)
    - .planning/ROADMAP.md  (Phase 097 Success Criteria SC#1–4; Phase 098/100/101 — what consumes this output)
  </read_first>
  <files>scripts/spike-097/CONCLUSION.md</files>
  <action>
    Write `scripts/spike-097/CONCLUSION.md` (the spike deliverable). Sections:
      1. **Four unknowns — answers (SC#2):** one paragraph each for (a)/(b)/(c)/(d), each citing its backing artifact (`out/field-map.json`, `out/risk-register-filled.docx` + `out/corruption.log`, `out/transcript.md` + `out/unknown-c.md`, `out/unknown-d.md`). Summarize: did fields derive cleanly? did docx fill clean (incl. 1/5/20 growth)? what grounding the generator needs? does the loop feel good?
      2. **GO / NO-GO (SC#3):** a single explicit line `DECISION: GO` or `DECISION: NO-GO` on the docxtpl trusted-template fill path, with the evidence rationale and any conditions (e.g. "GO with the OpenAI-strict pivot if Anthropic forced-tool drift recurs"; "GO — render in the sealed sandbox in prod per Phase 101"). Note the A1 (provider) and A5 (row-growth) findings explicitly — those most affect the decision.
      3. **Recommended additive-optional schema shape (SC#3):** the confirmed-or-revised `inputs` / `assets` / `folder_scope` shape (from the research hypothesis), updated by the evidence — settle the three open questions: (i) does `InputFieldSpec` need a `template_derived` flag? (ii) does `Cited` provenance belong in `inputs` or only in run output? (iii) is `folder_scope` a string path or a resolved id list? State clearly that this is a RECOMMENDATION for Phase 098/100/101 to lock — NOT committed here; and that it is additive-optional so old published `WorkflowDefinition` rows still validate (zero-migration).
      4. **Phase 101 UAT seed (SC#4):** a table of the six named failure modes (Pitfall 1–6, + Pitfall 7 freshness note) with the spike's clean/corrupt/not-exercised result for each, framed as the pre-named "How we'd know this failed" UAT rows Phase 101 inherits (G-6). Mark pptx/xlsx variable-row modes as deferred-to-101.
      5. **What the spike does NOT do:** no schema committed, no migration, no Dockerfile.sandbox change, no production code — list the throwaway artifacts and the Phase 101 production add (docxtpl → Dockerfile.sandbox + SANDBOX_IMAGE bump).
    Every claim must reference the artifact under `out/` that backs it.
  </action>
  <acceptance_criteria>
    - `scripts/spike-097/CONCLUSION.md` exists and contains all five sections: the four-unknown roll-up, a `DECISION: GO`/`DECISION: NO-GO` line, a recommended `inputs:` / `assets:` / `folder_scope:` schema shape, the Phase 101 UAT-seed failure-mode table, and the "does NOT do" list.
    - It explicitly settles the three schema open questions (template_derived flag / Cited placement / folder_scope string-vs-ids).
    - It references each of `out/field-map.json`, `out/risk-register-filled.docx`, `out/corruption.log`, `out/transcript.md`.
    - NO edit to `backend/app/models/harness.py`; no SQL migration file created; no `backend/` file modified.
  </acceptance_criteria>
  <verify>
    <automated>backend\venv\Scripts\python.exe -c "s=open('scripts/spike-097/CONCLUSION.md',encoding='utf-8').read(); import re; assert re.search(r'DECISION:\s*(GO|NO-GO)', s), 'missing decision line'; assert 'inputs' in s and 'assets' in s and 'folder_scope' in s; assert all(a in s for a in ['field-map.json','risk-register-filled.docx','corruption.log','transcript.md']); print('CONCLUSION sections OK')"</automated>
  </verify>
  <done>CONCLUSION.md drafts the 4-unknown roll-up, a go/no-go line, the recommended additive-optional schema shape, and the Phase 101 UAT seed — every claim backed by an out/ artifact.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator confirms the GO / NO-GO on the docxtpl fill path</name>
  <read_first>
    - scripts/spike-097/CONCLUSION.md  (the drafted decision + evidence rationale)
  </read_first>
  <decision>Is the docxtpl trusted-template fill path a GO for Phase 101, and is the recommended inputs/assets/folder_scope schema shape the right lock candidate for Phase 098/100/101?</decision>
  <context>The spike captured the field-map, the filled+re-opened .docx (incl. 1/5/20-row growth), the SSTI-contained render, the authoring transcript, and the six failure-mode rows. This decision converts that evidence into the milestone go/no-go — it gates whether Phase 098 locks the schema shape and Phase 101 builds on docxtpl. A1 (Anthropic forced-tool fidelity) and A5 (row growth) are the two findings that most affect it.</context>
  <options>
    <option id="go">
      <name>GO — proceed on docxtpl + the recommended schema</name>
      <pros>Evidence shows clean openable fill + correct row growth + contained SSTI; schema shape confirmed additive-optional (zero-migration). Phase 098 can lock the shape; Phase 101 builds on docxtpl.</pros>
      <cons>Carries the documented conditions (e.g. OpenAI-strict pivot if Anthropic drift recurs; production render must move to the sealed sandbox).</cons>
    </option>
    <option id="go-conditional">
      <name>GO with named conditions</name>
      <pros>Proceed but record specific guardrails the evidence surfaced (provider pivot, freshness gate, a revised schema field).</pros>
      <cons>Phase 098/101 must honor the conditions; CONCLUSION.md must list them explicitly.</cons>
    </option>
    <option id="no-go">
      <name>NO-GO — docxtpl path or schema shape failed the spike</name>
      <pros>Avoids locking a schema / building on a path the evidence rejected (e.g. row growth corrupt, fields uncited, generation unusable).</pros>
      <cons>Requires re-scoping the milestone's template-fill approach before Phase 098.</cons>
    </option>
  </options>
  <action>Record the confirmed decision (and any conditions) in `CONCLUSION.md`'s `DECISION:` line + a `Conditions:` list; the operator's call is final.</action>
  <acceptance_criteria>
    - `CONCLUSION.md` `DECISION:` line reflects the operator's confirmed choice; if conditional, a `Conditions:` list is present.
  </acceptance_criteria>
  <resume-signal>Select: go, go-conditional, or no-go (with any conditions to record).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike evidence → milestone decision | A misread artifact could send a wrong go/no-go into Phase 098/101 — mitigated by requiring every CONCLUSION claim to cite its backing out/ artifact. |
| recommended schema → production lock | The schema is a recommendation only; the actual lock + RLS + validation happen in Phase 098/100/101 (out of spike scope). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-097-15 | Tampering (decision integrity) | CONCLUSION.md claims | mitigate | Each claim must reference the out/ artifact that backs it; the operator confirms the go/no-go (checkpoint:decision). |
| T-097-16 | Elevation of Privilege | accidental production schema commit | mitigate | The schema is written to CONCLUSION.md only; acceptance asserts NO edit to `harness.py`, NO SQL migration, NO backend/ change. |
</threat_model>

<verification>
- `CONCLUSION.md` contains the 4-unknown roll-up, a confirmed `DECISION:` line, the recommended additive-optional `inputs`/`assets`/`folder_scope` shape, and the Phase 101 UAT-seed table — each claim citing its out/ artifact.
- All four unknowns have a written answer backed by a named artifact (Validation Sign-Off).
- No production schema edit, no migration, no `backend/` change.
</verification>

<success_criteria>
The spike's deliverable exists: a written, operator-confirmed go/no-go on the docxtpl fill path plus the recommended additive-optional `inputs`/`assets`/`folder_scope` schema shape (the Phase 098/100/101 lock candidate), with the six failure modes consolidated as the Phase 101 UAT seed. (ROADMAP SC#3; rolls up SC#1/#2/#4; VALIDATION Sign-Off.)
</success_criteria>

<output>
After completion, create `.planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-05-SUMMARY.md`.
</output>
