---
phase: 097-spike-risk-register-template-fill-authoring-feel
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - scripts/spike-097/authoring_feel.py
autonomous: false
requirements: []    # SPIKE — closes NO REQ-ID. Informs (does not close) WFAUTH-02. Acceptance = ROADMAP SC#2(c) + SC#2(d) + VALIDATION rows 097-c / 097-d.

must_haves:
  truths:
    - "A one-shot structured generation over the strict WorkflowDefinition schema produces a VALID draft from a plain-English description"
    - "The draft is grounded at design time in the folder tree + tool registry + skill registry + the template-derived placeholder set"
    - "The loop captures describe → at least one refine → re-draft (the authoring conversation, not wiring)"
    - "Unknown (c) (what grounding the generator needs) and unknown (d) (does describe→refine→publish FEEL good) each have a written answer"
  artifacts:
    - path: "scripts/spike-097/authoring_feel.py"
      provides: "The throwaway one-shot WorkflowDefinition generator grounded at design time + the refine loop + transcript capture"
      contains: "WorkflowDefinition"
    - path: "scripts/spike-097/out/transcript.md"
      provides: "describe → refine → re-draft transcript + the generated WorkflowDefinition JSON (the unknown c/d evidence)"
      contains: "describe"
    - path: "scripts/spike-097/out/unknown-c.md"
      provides: "Written answer to (c): the authoring-time grounding inventory the generator actually needed"
    - path: "scripts/spike-097/out/unknown-d.md"
      provides: "Written subjective verdict on (d): does describe→refine→publish feel like talking, not wiring?"
  key_links:
    - from: "scripts/spike-097/authoring_feel.py"
      to: "backend/app/models/harness.py WorkflowDefinition.model_json_schema()"
      via: "the strict (extra='forbid') schema as the generation response schema — read-only, mirror, do not edit"
      pattern: "WorkflowDefinition"
---

<objective>
Answer unknowns (c) and (d): what does authoring-time grounding NEED, and does describe → refine → publish FEEL good? Run a throwaway one-shot structured generation over the existing strict `WorkflowDefinition` schema (`extra="forbid"` rejects hallucinated keys), grounded at DESIGN time in the KB folder tree + the tool registry + the skill registry + the template's derived placeholder set. Capture the conversation: a plain-English description → a drafted definition → at least one refine turn → the re-draft. Judge the feel.

Purpose: this de-risks the Phase 103 NL-authoring generator prompt before any of it is built. The output (the grounding inventory the draft actually used + the subjective feel verdict) directly shapes WFAUTH-02. Throwaway is correct — a Markdown transcript + the generated JSON is sufficient evidence; NO UI (the production Workflows page is Phase 103, sketch-gated; `sketch-findings-agentic-rag` already names those surfaces — do NOT build them here).
Output: `authoring_feel.py`, `out/transcript.md`, `out/unknown-c.md`, `out/unknown-d.md`.

THROWAWAY SPIKE. Single provider (Anthropic native). No SQL, no `backend/app/**` edits, no UI. Mirror the SDK call shape; do NOT import `anthropic_service` (red line / G-5).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-RESEARCH.md
@.planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-VALIDATION.md
@scripts/spike-097/out/spike-config.json
@CLAUDE.md

<interfaces>
<!-- The strict schema the generation targets + the grounding sources. Read-only; mirror, do not edit. -->

From backend/app/models/harness.py (the response schema — extra='forbid' rejects hallucinated keys):
```python
class WorkflowDefinition(_StrictBase):   # _StrictBase = ConfigDict(extra="forbid")
    slug: str
    version: int
    name: str
    status: Literal["draft", "published"] = "draft"
    phases: list[PhaseSpec]              # each PhaseSpec.config is the 5-type discriminated union
# 5 phase types: programmatic | llm_single | llm_agent | llm_batch_agents | llm_human_input
# llm_agent / llm_batch_agents carry available_tools: list[str] (the per-phase whitelist)
```

Authoring-time grounding sources (research "## Authoring-Feel Artifact" + "what unknown (c) needs"):
- folder tree → the `folders` table (mirror the read from Plan 01's find_risk_folder.py / kb.py)
- tool registry → the whitelist-eligible tool names (backend/app/services/openai_service.py `get_tools`) — names only
- skill registry → the `skills` table / catalog
- template-derived field set → DocxTemplate(template_path).get_undeclared_template_variables()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: authoring_feel.py — grounded one-shot WorkflowDefinition generation + refine loop + transcript</name>
  <read_first>
    - .planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-RESEARCH.md  (## Authoring-Feel Artifact — the minimal experiment + the grounding inventory for unknown (c); ## Recommended inputs/assets/folder_scope Schema Hypothesis)
    - backend/app/models/harness.py  (READ ONLY — WorkflowDefinition + the 5 phase configs; the response schema + extra='forbid')
    - backend/app/services/openai_service.py  (READ ONLY — get_tools, to enumerate the whitelist-eligible tool NAMES for grounding)
    - scripts/spike-097/out/spike-config.json  (the confirmed folder_id + user_id for the folder-tree grounding)
    - scripts/spike-097/find_risk_folder.py  (Plan 01 — reuse the folders read + the service-client construction)
  </read_first>
  <files>scripts/spike-097/authoring_feel.py</files>
  <action>
    Write `authoring_feel.py` (throwaway), invokable as `python scripts/spike-097/authoring_feel.py`. It must:
      1. ASSEMBLE DESIGN-TIME GROUNDING: (a) the KB folder tree for the test user (reuse Plan 01's folders read; render as an indented name/id tree), (b) the tool registry — the whitelist-eligible tool NAMES from `openai_service.get_tools(...)` (names + one-line purpose; no schemas), (c) the skill registry — names from the `skills` table, (d) the template-derived placeholder set via `DocxTemplate(spike-config.template_path).get_undeclared_template_variables()`.
      2. ONE-SHOT GENERATION: a single Anthropic native call (mirror `anthropic_service` shape; do NOT import it) that takes a plain-English description — use exactly: "Each week, fill our risk register from the Acme project folder, grounded in our KB, and pause for me to confirm before finalizing." — plus the grounding bundle, and emits a `WorkflowDefinition` via forced tool-use with `input_schema = WorkflowDefinition.model_json_schema()`. Validate with `WorkflowDefinition.model_validate(...)`; on `ValidationError`, RE-PROMPT once with the validation error fed back (the spike does this loop by hand) and record both attempts.
      3. ONE REFINE TURN: send a follow-up refinement — exactly: "Make the research phase pull only from the /Risks subfolder, and add a human-confirm step before the file is produced." — regenerate, validate.
      4. CAPTURE: write `out/transcript.md` containing, in order: the grounding bundle summary, the description, the first drafted `WorkflowDefinition` JSON, the refine instruction, and the re-drafted `WorkflowDefinition` JSON. Write `out/unknown-c.md`: which grounding the draft actually USED to propose correct fields/tools/scope (the inventory that feeds the Phase 103 generator prompt) — note specifically whether the model needed the folder tree, the tool names, the skill names, and the template placeholders, and whether `folder_scope` came through as a string path or needed resolved ids (research Open Question — `folder_scope` resolution seam).
    Do NOT write the subjective feel verdict — that is the operator's call in Task 2.
  </action>
  <acceptance_criteria>
    - `scripts/spike-097/out/transcript.md` exists and contains: a grounding-bundle section, the literal description, a first `WorkflowDefinition` JSON block, a refine instruction, and a re-drafted `WorkflowDefinition` JSON block.
    - Both generated definitions are valid against the strict schema: `WorkflowDefinition.model_validate(...)` succeeds for each (the transcript records this; a re-prompt-on-error attempt is recorded if the first draft failed).
    - The re-drafted definition reflects the refine instruction (e.g. a narrowed retrieval scope referencing /Risks AND an added `llm_human_input` phase) — described in unknown-c.md.
    - `scripts/spike-097/out/unknown-c.md` exists and lists the authoring-time grounding the draft used (folder tree / tool names / skill names / template placeholders) + the `folder_scope` shape finding.
    - `authoring_feel.py` does NOT contain `import anthropic_service` / `from app.services.anthropic_service`; no `backend/` file modified.
  </acceptance_criteria>
  <verify>
    <automated>backend\venv\Scripts\python.exe -c "import os; t=open('scripts/spike-097/out/transcript.md').read(); assert 'describe' in t.lower() and 'refine' in t.lower() and t.count('\"phases\"')>=2, 'need 2 drafted definitions'; assert os.path.getsize('scripts/spike-097/out/unknown-c.md')>200; print('transcript + unknown-c OK')" ; findstr /C:"anthropic_service" scripts\spike-097\authoring_feel.py && echo FAIL_IMPORT || echo OK_NO_BACKEND_IMPORT</automated>
  </verify>
  <done>A grounded one-shot generation produced two valid WorkflowDefinition drafts (describe + refine), captured in transcript.md; unknown (c) grounding inventory written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator records the describe→refine→publish "feel" verdict → out/unknown-d.md</name>
  <read_first>
    - scripts/spike-097/out/transcript.md  (the describe → refine → re-draft conversation)
    - scripts/spike-097/out/unknown-c.md  (the grounding inventory)
  </read_first>
  <what-built>authoring_feel.py drove a grounded one-shot generation over the strict WorkflowDefinition schema: a plain-English description produced a valid draft, and one refine turn produced a re-draft — both captured in `out/transcript.md`. Whether this LOOP feels like talking rather than wiring is inherently subjective — the spike's job (unknown d) is to produce a written human verdict, not a metric (VALIDATION manual-only).</what-built>
  <how-to-verify>
    1. Read `out/transcript.md` end to end.
    2. Judge: did the FIRST draft propose the correct fields / tools / scope from the description alone? Did the refine turn feel like a conversation ("pull from /Risks", "add a confirm step") rather than editing JSON by hand? Where did it feel like wiring (friction points)? Would a domain expert (non-coder) be able to author this way?
  </how-to-verify>
  <action>
    Write `out/unknown-d.md` with the written subjective verdict: does describe→refine→publish feel good? The friction points. What the Phase 103 authoring loop must get right. A one-line GOOD / MIXED / POOR feel rating to carry into the Plan 05 conclusion.
  </action>
  <acceptance_criteria>
    - `scripts/spike-097/out/unknown-d.md` exists, contains a written verdict on the describe→refine→publish feel, and includes a GOOD / MIXED / POOR rating line.
  </acceptance_criteria>
  <verify>
    <automated>backend\venv\Scripts\python.exe -c "import os,re; s=open('scripts/spike-097/out/unknown-d.md').read(); assert os.path.getsize('scripts/spike-097/out/unknown-d.md')>200; assert re.search(r'GOOD|MIXED|POOR', s), 'need a feel rating'; print('unknown-d verdict OK')"</automated>
  </verify>
  <resume-signal>Type "approved" once out/unknown-d.md holds the feel verdict + rating, or describe the friction to record.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plain-English description → generated WorkflowDefinition | Untrusted NL could try to make the generator emit out-of-schema or unsafe phase configs. |
| KB folder/tool/skill names → generation prompt | Design-time grounding metadata (names only) flows into the prompt; low blast radius (no content, no exfil tool). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-097-12 | Tampering (schema injection) | generated WorkflowDefinition | mitigate | Generation targets the strict `extra="forbid"` schema and is `model_validate()`-checked; hallucinated/extra keys are rejected and re-prompted (V5 input validation). |
| T-097-13 | Information Disclosure | grounding bundle | accept | Only folder/tool/skill NAMES + template placeholders are sent (no document content, no secrets); throwaway dev-only generation. |
| T-097-14 | Elevation of Privilege | generated phase tool whitelists | accept (spike) | The draft is evidence only — never executed in the spike; Phase 102 lint + the publish-time gate are the production guard. Recorded as a Phase 103 requirement. |
</threat_model>

<verification>
- `out/transcript.md` captures describe → ≥1 refine → re-draft with two schema-valid `WorkflowDefinition` JSON blocks.
- `out/unknown-c.md` lists the authoring-time grounding inventory the draft used; `out/unknown-d.md` holds the subjective feel verdict + rating.
- No `backend/app/**` edit; no UI built; Anthropic call mirrors (never imports) anthropic_service.
</verification>

<success_criteria>
Unknowns (c) and (d) are answered with evidence: a grounded one-shot generation produces valid WorkflowDefinition drafts from plain English, the grounding inventory the generator needs is written down (feeds WFAUTH-02), and the describe→refine→publish feel has a written human verdict. (ROADMAP SC#2(c)/(d); VALIDATION rows 097-c / 097-d.)
</success_criteria>

<output>
After completion, create `.planning/phases/097-spike-risk-register-template-fill-authoring-feel/097-04-SUMMARY.md`.
</output>
