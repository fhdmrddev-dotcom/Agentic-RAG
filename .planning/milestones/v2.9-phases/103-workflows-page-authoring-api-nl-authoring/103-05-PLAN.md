---
phase: 103-workflows-page-authoring-api-nl-authoring
plan: 05
type: execute
wave: 3
depends_on: [103-03]
files_modified:
  - frontend/src/components/workflows/PublishGauntlet.tsx
  - frontend/src/components/workflows/PublishGauntlet.test.tsx
autonomous: true
requirements: [WFAUTH-01]
must_haves:
  truths:
    - "The publish surface is one golden_input textarea + a Publish button disabled while empty/whitespace; the body is {golden_input}"
    - "The 5 PublishVerdict fields render VERBATIM from the server (no client re-derivation of published/blocked_stage)"
    - "A 200-with-blocked_stage='judge' renders as a BLOCK (not success); a 200-with-published=true renders as success"
    - "The 4 HTTP outcomes are distinct (200-with-block / 400 business_requirement / 404 not_found / 409 already_published) — no binary 200=ok handler"
    - "named_failures render by KEY-DETECTION (criterion / code / phase+message / summary / bare-string); any string/unrecognized shape renders as a BLOCK, never a pass"
    - "No 'publish anyway'/override affordance exists anywhere (its absence rendered struck-through); the only forward affordance is 'Fix & re-publish'"
    - "The 'view the golden run' link renders only when golden_run_id != null; a pre-stage-3 block shows an explicit no-run note"
    - "lint findings render the LOWERCASE LintError.code literals"
  artifacts:
    - path: "frontend/src/components/workflows/PublishGauntlet.tsx"
      provides: "the publish-gauntlet UI client rendering PublishVerdict verbatim + 8 stages + judge hard wall"
      min_lines: 90
  key_links:
    - from: "frontend/src/components/workflows/PublishGauntlet.tsx"
      to: "publishWorkflow (api.ts)"
      via: "POST /workflows/{id}/publish -> 4 distinct HTTP outcomes"
      pattern: "publishWorkflow"
    - from: "PublishGauntlet renderFailure"
      to: "named_failures entries"
      via: "key-detection (criterion/code/phase+message/summary/bare-string)"
      pattern: "criterion|in e|typeof entry"
---

<objective>
Build the publish-gauntlet UI client (REQ-6 / WFAUTH-01): the single `golden_input` publish field, the 8 server-fixed stages, the 5 `PublishVerdict` fields rendered VERBATIM, the 4 distinguished HTTP outcomes, the polymorphic `named_failures` rendered by KEY-DETECTION, and the judge block as a HARD WALL with NO override.

Implements REQ-6 (gauntlet UI). Advances WFAUTH-01. Wave 3 — depends on Plan 03 (publishWorkflow + PublishVerdict/LintError types). No file overlap with Plans 04/06.

Purpose: the gauntlet RUNS server-side (Phase 102); this client must NEVER re-derive the verdict. The verbatim-render + key-detection + no-override + bare-string->block rules are the G-6 silent-pass guards.
Output: `PublishGauntlet.tsx` + its test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-SPEC.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-PATTERNS.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-RESEARCH.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-VALIDATION.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-03-SUMMARY.md

<interfaces>
<!-- The sketch-findings-agentic-rag skill (sketch 020-B) is the design acceptance bar — auto-loads. -->

From Plan 03 (frontend/src/lib/api.ts):
- publishWorkflow(id, golden_input, signal?) -> PublishOutcome:
    { kind: "verdict"; verdict: PublishVerdict } | { kind: "business_requirement"; verdict: PublishVerdict } | { kind: "not_found" } | { kind: "already_published" }
- PublishVerdict { published: boolean; version: number|null; golden_run_id: string|null; blocked_stage: string|null; named_failures: unknown[] }
- LintError { code: string; phase: string; message: string }

The 8 stages (server-fixed, sketch 020-B): owner -> definition-valid -> business_requirement -> lint -> interactive-phase -> golden run (on the project KB) -> structural gate -> judge -> flip. Render as a horizontal progress-spine (the long synchronous golden-run wait is the canvas hero).

The 10 real blocked_stage codes ONLY: not_found, already_published, definition_invalid, business_requirement, lint, interactive_phase, golden_run_timeout, golden_run_error, structural_gate, judge.

The polymorphic named_failures shapes (D-103-CONF-3 — render by KEY-DETECTION, NOT by blocked_stage; entries can MIX shapes in one list):
- lint            -> { code, phase, message }   (codes = the 5 LOWERCASE LintError.code literals)
- judge per-crit  -> { criterion, score, evidence }  + a { summary } row
- judge-shot fail -> a bare STRING ("the judge produced no verdict (...) — honest failure, not a silent pass")
- interactive     -> { phase, message }
- structural_gate / early stages -> a bare STRING

Render rule (locked):
  if typeof entry === "string"            -> BlockMessage (bare string -> block)
  if "criterion" in entry                 -> CriterionRow {criterion, score, evidence}
  if "code" in entry                      -> LintRow {code (lowercase), phase, message}
  if "phase" in entry && "message" in entry -> PhaseRow
  if "summary" in entry                   -> SummaryLine
  else                                    -> BlockMessage "the judge could not produce a verdict / treated as a block"
ANY string / missing-criterion / unrecognized shape => BLOCK, NEVER published=true (REQ-6 g).

Verbatim render: published/version/golden_run_id/blocked_stage/named_failures rendered exactly as returned (booleans green/red, nulls italic `null`). NEVER re-derive published/blocked_stage. "View the golden run" link gates strictly on golden_run_id != null; otherwise an explicit "blocked before stage 3, no run to open" note. NO "publish anyway"/override (its absence rendered struck-through); only "Fix & re-publish."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PublishGauntlet — golden_input field + 8 stages + verbatim PublishVerdict + 4 HTTP outcomes + judge hard wall</name>
  <read_first>
    - 103-PATTERNS.md "PublishGauntlet.tsx" section (the verdict contract + the polymorphic named_failures shapes + the _judge_named_failures excerpt + the render rule)
    - 103-RESEARCH.md Pattern 5 (the renderFailure key-detection function) + the Anti-Patterns ("switching named_failures on blocked_stage" forbidden)
    - sketch-findings-agentic-rag SKILL.md (sketch 020-B: the 8-stage horizontal progress-spine, judge HARD WALL no override / struck-through "publish anyway", per-criterion rows + server summary, golden-run link gating, un-producible verdict fails closed)
    - 103-SPEC.md REQ-6 acceptance (a)-(g) + "How we'd know this failed" (the optimistic-verdict / 200=ok / override / silent-pass failure modes)
    - frontend/src/lib/api.ts (publishWorkflow + PublishVerdict/LintError from Plan 03)
    - frontend/src/components/workflows/PublishGauntlet.test.tsx (create in this task)
  </read_first>
  <behavior>
    - One golden_input textarea + a Publish button disabled on empty/whitespace; body {golden_input}.
    - The 8 stages render (horizontal progress-spine); the long golden-run wait is the visible hero.
    - The 5 verdict fields render verbatim; published/blocked_stage NEVER re-derived.
    - 200-with-block (e.g. blocked_stage='judge') => BLOCK display; 200-with-published=true => success.
    - 400 business_requirement / 404 not_found / 409 already_published => each distinct display (no binary 200/else).
    - named_failures render by key-detection; any string/unrecognized => block.
    - No override anywhere (struck-through "publish anyway"); only "Fix & re-publish".
    - "View the golden run" only when golden_run_id != null; else the no-run note.
  </behavior>
  <action>
    Create `frontend/src/components/workflows/PublishGauntlet.tsx`. Props: `{ definitionId: string; onPublished?: (version: number) => void }` (onPublished lets Plan 06 auto-return to the Workflows page on PASS).
    State: a single `golden_input` string + an async `outcome: PublishOutcome | null` + a loading flag.

    1. The publish form: ONE `<textarea>` for golden_input + a Publish button `disabled` while `golden_input.trim()` is empty. On submit -> set loading -> `await publishWorkflow(definitionId, golden_input)` -> set outcome.

    2. The 8-stage progress-spine: render the 8 named stages (owner / definition-valid / business_requirement / lint / interactive-phase / golden run / structural gate / judge / flip) as a horizontal spine. While loading, show the golden-run stage as the active hero (the long synchronous wait). After the outcome resolves, mark stages up to `blocked_stage` as passed and the blocked stage as failed (DERIVED ONLY for the visual progress-spine highlight — the PASS/BLOCK truth comes from the server `published`/`blocked_stage`, never re-computed).

    3. Render the verdict VERBATIM when `outcome.kind` is "verdict" or "business_requirement":
       - published (boolean green true / red false), version (or italic `null`), golden_run_id (or italic `null`), blocked_stage (one of the 10 codes, or italic `null`).
       - The display is a SUCCESS only when `verdict.published === true`; a 200 with `published === false` (any blocked_stage incl. 'judge') is a BLOCK. Do NOT re-derive.

    4. HTTP outcomes: switch on `outcome.kind` — "verdict" (read published/blocked_stage), "business_requirement" (400 — show the business_requirement block + the verdict in detail), "not_found" (404 — "workflow not found"), "already_published" (409 — "already published"). Each distinct. (publishWorkflow already maps the statuses in Plan 03 — this component switches on `kind`.)

    5. `renderFailure(entry: unknown)` — the key-detection function (RESEARCH Pattern 5), used to render every `named_failures` entry. Render LintRow codes as the LOWERCASE literals. ANY string / missing-criterion / unrecognized shape => a Block message, NEVER a pass.

    6. Judge hard wall: when `blocked_stage === "judge"`, render the per-criterion `{criterion, score, evidence}` rows + the server summary. Render NO "publish anyway"/override control — render the absent override struck-through (e.g. a `<s>Publish anyway</s>` or a disabled/struck affordance) + a "Fix & re-publish" button (the only forward affordance). An un-producible verdict (bare-string judge failure) renders "the judge could not produce a verdict / treated as a block."

    7. "View the golden run" link renders ONLY when `verdict.golden_run_id != null`; otherwise the explicit "blocked before stage 3 — no run to open" note.

    Create `frontend/src/components/workflows/PublishGauntlet.test.tsx` (mock publishWorkflow):
    - Publish button disabled on empty/whitespace; enabled on non-empty; sends {golden_input}.
    - a 200 verdict {published:false, blocked_stage:"judge", named_failures:[{criterion,score,evidence},{summary}]} => renders as a BLOCK (not success) with per-criterion rows + summary; NO override control (assert the "publish anyway" is struck-through / absent as an enabled control); a "Fix & re-publish" exists.
    - a 200 verdict {published:true, version:2, golden_run_id:"..."} => renders success + the "view the golden run" link.
    - a verdict with golden_run_id:null and a pre-stage-3 block => NO run link, the explicit no-run note instead.
    - the 4 HTTP outcomes: kind "verdict"/"business_requirement"/"not_found"/"already_published" each render distinctly (assert distinct text).
    - named_failures key-detection: a list MIXING a {code,phase,message} lint row + a bare string => the lint row renders with the lowercase code AND the bare string renders as a block; assert the verdict is shown as a block, never a pass.
    - the 5 verdict fields render verbatim (a 200-with-block is NOT relabeled as success — proves no re-derivation).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "publishWorkflow" frontend/src/components/workflows/PublishGauntlet.tsx` confirms it calls the Plan-03 client (server verdict, not re-derived).
    - `grep -nE "criterion|\"code\" in|typeof entry === \"string\"" frontend/src/components/workflows/PublishGauntlet.tsx` confirms renderFailure uses KEY-DETECTION (not a switch on blocked_stage).
    - `grep -niE "publish anyway|override" frontend/src/components/workflows/PublishGauntlet.tsx` shows the override is ONLY rendered struck-through / disabled (no enabled override control).
    - `grep -n "golden_run_id" frontend/src/components/workflows/PublishGauntlet.tsx` shows the run link gated on `!= null`.
    - The component never recomputes `published`/`blocked_stage` from named_failures (no client truth re-derivation) — the test asserts a 200-with-block stays a block.
    - PublishGauntlet.test.tsx asserts: empty-disabled, 200-judge-block (no override, Fix&re-publish present), 200-success-with-run-link, no-run-note, the 4 distinct HTTP outcomes, and mixed-named_failures key-detection -> block.
    - PublishGauntlet.test.tsx exits 0 GREEN; `cd frontend && npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>PublishGauntlet renders the golden_input field, 8 stages, the 5 verdict fields verbatim, the 4 distinct HTTP outcomes, named_failures by key-detection (any string/unrecognized -> block), and the judge as a hard wall with no enabled override; test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server PublishVerdict → client render | the client must render the verdict verbatim and never re-derive pass/block |
| polymorphic named_failures → render | an unrecognized/bare-string entry must fail CLOSED (block), never default to a pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-05-01 | Tampering | client re-derives an optimistic verdict (recomputes published/blocked_stage) | mitigate | The 5 fields render verbatim from the server; a 200-with-block stays a block; no client recomputation (test-asserted) — the G-6 optimistic-verdict guard |
| T-103-05-02 | Tampering | a binary 200=ok/else=error handler mislabels a 200-with-block as success | mitigate | publishWorkflow (Plan 03) maps the 4 statuses; this component switches on `kind` AND reads `published` for 200 — a 200-with-block is surfaced as a block (test-asserted) |
| T-103-05-03 | Elevation of Privilege | a judge block exposes a "publish anyway"/override | mitigate | NO enabled override anywhere; the absent override is rendered struck-through; only "Fix & re-publish" forward — the judge is a hard wall (REQ-6 e) |
| T-103-05-04 | Tampering | an un-producible/bare-string judge verdict defaults to published=true | mitigate | renderFailure key-detection: any string / missing-criterion / unrecognized => a Block, never a pass (REQ-6 g, the G-6 silent-pass guard) |
| T-103-05-05 | Information Disclosure | a cross-user/not-found publish leaks existence | accept | publishWorkflow collapses cross-user + not-found to a uniform 404 (server-side, the get_definition precedent); the client shows the same "not found" for both |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx` — GREEN.
- `cd frontend && npx tsc -b` — exits 0.
- Manual/lived-experience (VALIDATION.md G-4): force a judge block live -> confirm no "publish anyway", per-criterion rows + summary, only "Fix & re-publish".
- Frontend rot caveat: net-new green via base-checkout (SEED-056).
</verification>

<success_criteria>
- REQ-6: golden_input field (disabled on empty); 8 stages; 5 verdict fields verbatim (no re-derivation); 200-with-block != success; 400/404/409 distinct; named_failures by key-detection; judge hard wall with no override; bare-string/unrecognized -> block; run link gated on golden_run_id != null.
- tsc -b clean.
</success_criteria>

<output>
After completion, create `.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-05-SUMMARY.md`
</output>
