---
phase: 103-workflows-page-authoring-api-nl-authoring
plan: 04
type: execute
wave: 3
depends_on: [103-03]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/PhaseSpineGraph.tsx
  - frontend/src/components/workflows/PhaseFormPanel.tsx
  - frontend/src/pages/WorkflowBuilderPage.test.tsx
  - frontend/src/components/workflows/PhaseSpineGraph.test.tsx
  - frontend/src/components/workflows/PhaseFormPanel.test.tsx
autonomous: true
requirements: [WFAUTH-01, WFAUTH-02, WFAUTH-03]
must_haves:
  truths:
    - "The empty Builder DOM contains EXACTLY one describe textarea + one hint line + one disabled submit button — no grounding chips, no dial, no folder picker, no phase node, no left rail"
    - "The draft button is disabled on empty/whitespace input"
    - "After 'Composing...' clears, the whole draft renders in a SINGLE state transition (one DOM batch) — no per-node animation/stagger/incremental append"
    - "Graph nodes appear in strict phase_index order on a vertical spine; one dashed skip_to_phase edge lands on its target node"
    - "The graph DOM has NO drag handler / draggable=true / connection handle / add-node control (static check)"
    - "The graph carries a 'View only' badge + legend; renders phase.name with a non-empty fallback when absent; phase-type glyphs present"
    - "Selecting a node opens the 400px form panel by PUSH (graph column shrinks; not position:absolute/fixed; no h-scroll >=1100px; bottom-sheet <768px)"
    - "Each of the 6 phase types renders only its real fields; integrity_policy is greyed/read-only only on llm_emit"
    - "The graph neither imports nor modifies PhaseTimeline/PhaseCard"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseSpineGraph.tsx"
      provides: "read-only vertical phase-spine graph (plain SVG/CSS, not PhaseTimeline)"
      min_lines: 60
    - path: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      provides: "400px push panel + 6 phase_type-conditioned forms"
      min_lines: 80
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "describe-first Builder + single-state-transition reveal + graph + form panel"
      min_lines: 80
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "generateWorkflow (api.ts)"
      via: "describe submit -> POST /workflows/generate -> single state transition"
      pattern: "generateWorkflow"
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "PhaseSpineGraph + PhaseFormPanel"
      via: "node click opens the push panel (gridTemplateColumns reflow)"
      pattern: "gridTemplateColumns"
    - from: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      to: "updateWorkflowDraft (api.ts)"
      via: "form edit -> PATCH /workflows/{id}"
      pattern: "updateWorkflowDraft"
---

<objective>
Build the describe-first Builder (REQ-5), the read-only vertical phase-spine graph (REQ-4 / WFAUTH-03), and the 400px push/split form panel with the six phase_type-conditioned forms (REQ-5). Authoring is form-led on a read-only spine — no drag-canvas, no router (the locked sketch-018/019 contract).

Implements REQ-4 (read-only graph), REQ-5 (describe-first Builder + 400px form panel). Advances WFAUTH-01 (form-refine layer) + WFAUTH-02 (NL-describe headline) + WFAUTH-03 (read-only graph). Wave 3 — depends on Plan 03 (api.ts authoring fns + types + deriveTier).

Purpose: the headline authoring surface. The describe-box-first single-state-transition reveal + the drag-FREE read-only graph + the push-not-overlay panel are the falsifiable acceptance bars (and the G-6 failure modes to avoid).
Output: `WorkflowBuilderPage.tsx`, `PhaseSpineGraph.tsx`, `PhaseFormPanel.tsx`, and their 3 test files.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-SPEC.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-PATTERNS.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-RESEARCH.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-VALIDATION.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-03-SUMMARY.md

<interfaces>
<!-- The sketch-findings-agentic-rag skill (sketch 018/019) is the design acceptance bar — auto-loads. -->

From Plan 03 (frontend/src/lib/api.ts): generateWorkflow(body) -> GenerateResult; updateWorkflowDraft(id, def); createWorkflowDraft(def); PublishVerdict; WorkflowDraftRow; CitationPolicy.
From Plan 03 (frontend/src/components/workflows/deriveTier.ts): deriveTier, TIERS, ValidatorKind.

The CSS-grid push pattern to mirror (frontend/src/components/layout/ChatLayout.tsx:230-235 — the canonical shipped 2-state track):
    <div className="grid min-w-0 flex-1 overflow-hidden motion-safe:transition-[grid-template-columns] motion-safe:duration-300"
         style={{ gridTemplateColumns: "minmax(0,1fr) " + (panelOpen ? "400px" : "44px") }}>
      <PhaseSpineGraph .../>   {/* shrinks when the panel opens */}
      <PhaseFormPanel .../>    {/* the 400px column; <768px = bottom-sheet via a media query */}
    </div>
  Acceptance: graph column width DECREASES on open; panel NOT position:absolute/fixed; no h-scroll >=1100px; <768px bottom-sheet.

The 6 PhaseConfig forms (backend/app/models/harness.py:52-167 — each renders ONLY its real fields):
    programmatic       -> fn, input_keys
    llm_single         -> prompt, model?, temperature?, folder_scope?, skill_ref?
    llm_agent          -> + available_tools, max_steps (default 12), wall_clock_seconds?
    llm_batch_agents   -> + max_parallel_agents (default 5), merge_strategy ("concat"|"concat_numbered")
    llm_human_input    -> prompt, options[], timeout_seconds (default 300)
    llm_emit           -> prompt, emitter, model?, folder_scope?, skill_ref?,
                          citation_policy ("strict"|"flag"|"partial"|"draft"),
                          integrity_policy ("strict"|"documented_limit" — GREYED/read-only in 103, shown-but-inert)
  folder_scope/project_folder_id render the folder NAME + bound UUID, never a path. integrity_policy greyed/read-only ONLY on llm_emit.

Read-only graph contract (REQ-4):
- one node per PhaseSpec ordered by phase_index; solid i->i+1 edges; ONE dashed edge for an on-fail skip_to_phase:<slug> validator (resolve the target slug client-side mirroring parse_skip_target — split on the last ":" and match the slug).
- "View only" badge + legend ("READ-ONLY GRAPH · ordered by phase_index · run order (i->i+1) · on-fail branch (skip_to_phase) · no depends_on · no parallel lanes · inspect, don't drag").
- node titles render phase.name (REQ-3) with a fallback (phase-type label / slug) when absent; phase-type glyphs: ⚙ programmatic / ✎ llm_single / 🤖 llm_agent / ⛓ llm_batch_agents / ☺ llm_human_input / ◆ llm_emit.
- STATIC CHECK (the bar): NO onDragStart / drag onMouseDown / onPointerDown drag handler, NO draggable=true, NO connection-handle element, NO add-node control in the DOM.
- G-5: MUST NOT import or modify frontend/src/components/panel/PhaseTimeline.tsx or PhaseCard.tsx. Plain SVG/CSS only (no graph lib).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PhaseSpineGraph — read-only vertical SVG/CSS spine (drag-free, View-only, glyphs, dashed skip edge)</name>
  <read_first>
    - frontend/src/components/panel/PhaseTimeline.tsx (READ to understand what NOT to reuse — it reads live usePhases; the authoring graph reads a DRAFT JSON; do NOT import it)
    - 103-PATTERNS.md "No Analog Found" row for PhaseSpineGraph (the full build spec)
    - sketch-findings-agentic-rag SKILL.md (sketch 019-D: read-only vertical phase-spine + the legend; Aether Deep Midnight tokens)
    - 103-SPEC.md REQ-4 acceptance (a)-(e) + "How we'd know this failed" (the drag failure mode)
    - frontend/src/components/workflows/PhaseSpineGraph.test.tsx (create in this task)
  </read_first>
  <behavior>
    - Renders one node per PhaseSpec ordered by phase_index on a vertical spine; solid i->i+1 edges; one dashed edge for a skip_to_phase:<slug> validator landing on the target node.
    - Carries a "View only" badge + the legend; node titles use phase.name with a non-empty fallback; phase-type glyphs present.
    - NO drag/draggable/connection-handle/add-node anywhere in the DOM.
    - Calls an onSelectNode(phaseSlug) callback on node click (selection only — never reorder).
  </behavior>
  <action>
    Create `frontend/src/components/workflows/PhaseSpineGraph.tsx` — plain SVG + CSS (no graph lib). Props: `{ phases: PhaseSpecJSON[]; selectedSlug: string | null; onSelectNode: (slug: string) => void }`.
    - Sort phases by phase_index. Render a vertical SVG line (the spine) + one node row per phase (CSS-positioned div over/beside the SVG, OR pure SVG nodes — discretionary, but NO graph lib).
    - Solid edge i->i+1 between consecutive nodes. For each phase, scan its validators for a `skip_to_phase:<slug>` (the on-fail target — parse by splitting on the last ":" and matching the resulting slug against a node); render exactly ONE dashed edge from that phase to the matched target node. Resolve the target the same way parse_skip_target does (`skip_to_phase:<slug>` -> `<slug>`).
    - Node title = `phase.name` if present else a fallback (a phase-type label, e.g. the glyph + type, or the slug). Glyphs: ⚙ programmatic / ✎ llm_single / 🤖 llm_agent / ⛓ llm_batch_agents / ☺ llm_human_input / ◆ llm_emit (map by config.phase_type).
    - A "View only" badge + the legend string (verbatim from the contract above).
    - Node click -> onSelectNode(phase.slug). The node is a `<button>` (keyboard-accessible select), NOT a draggable element. NO onDragStart/onPointerDown drag handler, NO draggable attr, NO connection handle, NO add-node button.
    - DO NOT import PhaseTimeline/PhaseCard or any graph lib.

    Create `frontend/src/components/workflows/PhaseSpineGraph.test.tsx`:
    - render a 3-phase draft -> nodes appear in phase_index order (assert DOM order).
    - a draft with one phase carrying a `skip_to_phase:<slug>` validator -> exactly ONE dashed edge to that target (assert the dashed-edge element + its target).
    - STATIC drag-free check: query the rendered DOM and assert ZERO `[draggable="true"]`, zero elements with an onDragStart/onPointerDown drag handler (assert via the absence of drag-related attrs/data hooks), zero connection-handle element, zero add-node control.
    - the "View only" badge + legend render; glyphs render; phase.name fallback when absent.
    - assert the module does NOT import PhaseTimeline/PhaseCard (a source-grep test or an import assertion).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PhaseSpineGraph.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PhaseTimeline\|PhaseCard" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns NOTHING (G-5).
    - `grep -nE "react-flow|d3|dagre" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns NOTHING (no graph lib).
    - `grep -nE "draggable=\{?true|onDragStart|onPointerDown" frontend/src/components/workflows/PhaseSpineGraph.tsx` returns NOTHING (drag-free; static-check bar).
    - The component renders a "View only" badge + the legend text + phase-type glyphs.
    - PhaseSpineGraph.test.tsx asserts phase_index order, exactly one dashed skip edge, and the drag-free static DOM (no draggable/handle/add-node).
    - PhaseSpineGraph.test.tsx exits 0 GREEN.
  </acceptance_criteria>
  <done>PhaseSpineGraph is a read-only vertical SVG/CSS spine (phase_index order, one dashed skip edge, glyphs, View-only badge/legend), drag-free by static check, and never imports PhaseTimeline/PhaseCard; test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PhaseFormPanel — 400px push panel + 6 phase_type-conditioned forms (integrity_policy greyed only on llm_emit)</name>
  <read_first>
    - frontend/src/components/layout/ChatLayout.tsx (the gridTemplateColumns 2-state push pattern :230-235 — mirror exactly; confirm motion-safe transition + minmax(0,1fr))
    - backend/app/models/harness.py (the 6 PhaseConfig members :52-167 — the EXACT field sets + defaults)
    - 103-PATTERNS.md "PhaseFormPanel" section (the 6-form field map) + "ChatLayout push pattern" section
    - sketch-findings-agentic-rag SKILL.md (sketch 019-D: fixed-width 400px right-side push/split panel, 44px rail, mobile bottom-sheet)
    - frontend/src/components/workflows/PhaseFormPanel.test.tsx (create in this task)
    - frontend/src/lib/api.ts (updateWorkflowDraft from Plan 03) + deriveTier.ts (CitationPolicy)
  </read_first>
  <behavior>
    - Opens as a 400px right-side column that PUSHES the graph (gridTemplateColumns reflow), never an overlay; 44px resting rail; <768px bottom-sheet.
    - Renders ONLY the selected phase_type's real fields (the 6-form map); programmatic/llm_human_input show no model/tools/scope; llm_emit is the only type with citation_policy + a greyed read-only integrity_policy.
    - folder_scope/project_folder_id render the folder NAME + bound UUID, never a path.
    - A form edit calls updateWorkflowDraft (PATCH) to persist.
  </behavior>
  <action>
    Create `frontend/src/components/workflows/PhaseFormPanel.tsx`. Props: `{ phase: PhaseSpecJSON | null; open: boolean; folderName?: string; onChange: (patch) => void; onPersist: () => void; isMobile?: boolean }`.
    - The panel is the SECOND grid column (the parent in WorkflowBuilderPage owns the grid; this component renders the panel content + its own width contract). The panel element must NOT be `position:absolute`/`fixed` — it is a grid track. At `<768px` render as a bottom-sheet (a media-query / isMobile branch).
    - Switch on `phase.config.phase_type` and render ONLY that type's fields per the 6-form map:
      - programmatic: fn (text), input_keys (string list).
      - llm_single: prompt (textarea), model? (select/text), temperature? (number), folder_scope? (folder name + UUID display), skill_ref? (select).
      - llm_agent: the llm_single fields + available_tools (multi-select), max_steps (number, default 12), wall_clock_seconds? (number).
      - llm_batch_agents: the llm_agent fields + max_parallel_agents (number, default 5), merge_strategy ("concat"|"concat_numbered").
      - llm_human_input: prompt (textarea), options[] (string list), timeout_seconds (number, default 300).
      - llm_emit: prompt, emitter, model?, folder_scope?, skill_ref?, citation_policy ("strict"|"flag"|"partial"|"draft" select), integrity_policy ("strict"|"documented_limit") rendered GREYED + read-only (disabled, shown-but-inert).
    - folder_scope/project_folder_id render `{folderName} · {uuid}` (name + bound UUID, never a path).
    - On a field change call onChange(patch); on blur/save call onPersist (which the page wires to updateWorkflowDraft).

    Create `frontend/src/components/workflows/PhaseFormPanel.test.tsx`:
    - render each of the 6 phase types -> assert EXACTLY its real fields appear and the others are ABSENT (programmatic/llm_human_input: no model/tools/scope; only llm_emit has citation_policy).
    - llm_emit: integrity_policy is present but `disabled`/read-only (greyed); citation_policy is editable.
    - the panel element is NOT position:absolute/fixed (assert the computed style / className contract — it is a grid track).
    - a folder_scope field renders the folder name + UUID, not a path.
    (The push-reflow geometry across viewport widths is a manual/lived-experience row in VALIDATION.md; the unit test asserts the non-overlay class contract + the per-type field sets.)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PhaseFormPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "phase_type" frontend/src/components/workflows/PhaseFormPanel.tsx` shows a switch over all 6 types.
    - `grep -n "integrity_policy" frontend/src/components/workflows/PhaseFormPanel.tsx` shows it rendered `disabled`/read-only and ONLY inside the llm_emit branch.
    - `grep -nE "position:\s*(absolute|fixed)|fixed inset|absolute inset" frontend/src/components/workflows/PhaseFormPanel.tsx` returns NOTHING (push, not overlay).
    - `grep -n "updateWorkflowDraft" frontend/src/components/workflows/PhaseFormPanel.tsx` (or the page wiring) confirms a form edit persists via PATCH.
    - PhaseFormPanel.test.tsx asserts each of the 6 types renders only its real fields and integrity_policy greyed only on llm_emit.
    - PhaseFormPanel.test.tsx exits 0 GREEN.
  </acceptance_criteria>
  <done>PhaseFormPanel renders the 6 phase_type-conditioned forms (only real fields each), integrity_policy greyed/read-only only on llm_emit, folder name+UUID never a path, non-overlay grid track; test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WorkflowBuilderPage — describe-first empty Builder + single-state-transition reveal + the push grid wiring</name>
  <read_first>
    - frontend/src/components/layout/ChatLayout.tsx (the push grid :230-235 — the parent grid this page owns)
    - frontend/src/components/workflows/PhaseSpineGraph.tsx (Task 1) + PhaseFormPanel.tsx (Task 2)
    - frontend/src/lib/api.ts (generateWorkflow, createWorkflowDraft, updateWorkflowDraft from Plan 03)
    - sketch-findings-agentic-rag SKILL.md (sketch 018-A: first screen is JUST the describe box, 3-second read; AI drafts one-shot; grounding/confirm appear POST-DRAFT)
    - 103-SPEC.md REQ-5 acceptance (a)-(e) + "How we'd know this failed" + Claude's Discretion (Composing... copy)
    - frontend/src/pages/WorkflowBuilderPage.test.tsx (create in this task)
  </read_first>
  <behavior>
    - The empty Builder DOM = EXACTLY one describe textarea (max-width ~640px) + one hint line + one disabled "Draft the workflow" button — and nothing else (no grounding chips, dial, folder picker, phase list, left rail).
    - The draft button is disabled on empty/whitespace.
    - On submit -> a "Composing..." loading state -> generateWorkflow -> the WHOLE draft renders in a SINGLE state transition (one DOM batch) — no per-node animation/stagger/incremental append/token narration.
    - After the draft renders: the read-only graph + the push form panel; selecting a node opens the 400px panel by PUSH (graph column shrinks).
    - An ok:false generate response surfaces an honest "could not generate," never a renderable broken draft.
  </behavior>
  <action>
    Create `frontend/src/pages/WorkflowBuilderPage.tsx`. State machine (a single union, NOT incremental): `"empty" | "composing" | "drafted" | "error"`.
    - "empty": render ONLY the describe `<textarea>` (max-w ~640px) + one hint line + one disabled submit button. The button is `disabled` while the trimmed describe is empty. NOTHING else at rest (no chips/dial/folder picker/phase list/left rail).
    - On submit: set state "composing" (the "Composing..." loading copy — discretionary visual). Call `generateWorkflow({ describe, project_folder_id?, ... })`.
    - On `ok:true`: set the draft definition into state AND set state "drafted" in ONE update (single commit -> one DOM batch). Render the graph + panel from the complete definition. NO per-node CSS enter transition, NO stagger delay, NO index-keyed setTimeout, NO incremental array push (build the node array fully, render once).
    - On `ok:false` (or a thrown error): set state "error" with the honest "could not generate" surface (discretionary copy) — NEVER render a partial/broken draft.
    - "drafted": the push grid (mirror ChatLayout :230-235): `gridTemplateColumns: "minmax(0,1fr) " + (panelOpen ? "400px" : "44px")` with `motion-safe:transition-[grid-template-columns]`. Column 1 = `<PhaseSpineGraph onSelectNode={...}/>`; column 2 = `<PhaseFormPanel open={panelOpen} .../>`. Selecting a node sets the selected phase + panelOpen=true (the graph shrinks). Wire PhaseFormPanel.onPersist -> updateWorkflowDraft (after a createWorkflowDraft on first save — persistence is REQ-1's explicit create; a fresh generated draft is persisted on the first edit/save, then PATCHed).
    - A "Publish" affordance hands off to the gauntlet UI (Plan 05's PublishGauntlet) — render a Publish button that opens it; the gauntlet component is imported from Plan 05 (if Plan 05 lands in the same wave, import it; otherwise leave a typed prop seam the page composes). Keep the Publish button's disabled-on-empty-golden_input rule inside the gauntlet (Plan 05), not here.

    Create `frontend/src/pages/WorkflowBuilderPage.test.tsx`:
    - empty-Builder DOM: assert EXACTLY one `<textarea>`, one hint line, one disabled submit button; assert the ABSENCE of any grounding chip, strictness dial, folder picker, phase node, and left rail (query for them and assert null). Any extra interactive element = FAIL.
    - the draft button is disabled on empty/whitespace; enabled on non-empty.
    - single state transition: mock generateWorkflow to return a 3-phase def; after the "composing" state clears, assert ALL phase nodes are present in one DOM snapshot; assert NO per-node CSS enter-transition class, NO setTimeout-driven stagger (assert the nodes appear synchronously after the resolve, not over time).
    - ok:false -> the error surface renders and NO phase nodes render (no broken draft).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - WorkflowBuilderPage.test.tsx asserts the empty DOM = exactly 1 textarea + 1 hint + 1 disabled button, and the ABSENCE of chips/dial/picker/node/rail.
    - The draft button has `disabled` on empty/whitespace (asserted).
    - `grep -nE "setTimeout|stagger|enter-transition|animationDelay|transitionDelay" frontend/src/pages/WorkflowBuilderPage.tsx` returns NOTHING tied to per-node reveal (single state transition).
    - `grep -n "gridTemplateColumns" frontend/src/pages/WorkflowBuilderPage.tsx` shows the push grid; selecting a node sets panelOpen=true (graph column shrinks).
    - `grep -n "generateWorkflow" frontend/src/pages/WorkflowBuilderPage.tsx` confirms the describe submit calls the NL-gen client.
    - An ok:false generate path renders the honest error and renders NO phase nodes (asserted).
    - WorkflowBuilderPage.test.tsx exits 0 GREEN; `cd frontend && npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>The empty Builder is exactly the describe box + hint + disabled button; the draft renders in a single state transition (no stagger/animation); node click opens the 400px push panel; ok:false surfaces an honest failure not a draft; test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| describe textarea → generateWorkflow | untrusted NL crosses to the backend (mitigated server-side in Plan 02) |
| generated draft → graph render | a shape-valid but broken/ungrounded draft must not render as a finished workflow |
| form edits → updateWorkflowDraft (PATCH) | a published-row edit must surface the 409, never a silent overwrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-04-01 | Tampering | a broken/ungrounded generate response renders as a "draft" | mitigate | The page renders a draft ONLY on `ok:true`; an `ok:false` (or thrown) response renders the honest "could not generate" surface and NO phase nodes (test-asserted) — the G-6 silent-invalid-draft guard at the UI seam |
| T-103-04-02 | Tampering | the read-only graph offers a drag/edit affordance | mitigate | Static-check: no drag handler/draggable/connection-handle/add-node in the graph DOM (Task 1); refinement is form-only |
| T-103-04-03 | Tampering | a form edit silently overwrites a published row | mitigate | updateWorkflowDraft throws WorkflowConflictError on 409 (Plan 03); the panel surfaces it (drafts are the only editable target; published refinement is Tweak->fork in Plan 06) |
| T-103-04-04 | Information Disclosure | folder_scope renders a filesystem path | accept->mitigate | folder_scope/project_folder_id render the folder NAME + bound UUID, never a path (the no-path contract) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseFormPanel.test.tsx src/pages/WorkflowBuilderPage.test.tsx` — GREEN.
- `cd frontend && npx tsc -b` — exits 0.
- `grep -n "PhaseTimeline\|PhaseCard" frontend/src/components/workflows/PhaseSpineGraph.tsx` — empty (G-5).
- Manual/lived-experience rows (VALIDATION.md): single-state-transition reveal, push-not-overlay reflow across widths, drag-free pointer-drag backstop — exercised at /gsd:verify-work (G-4 Chrome MCP / operator-driven).
- Frontend rot caveat: net-new green via base-checkout (SEED-056).
</verification>

<success_criteria>
- REQ-4: read-only vertical spine in phase_index order, one dashed skip edge, drag-free static check, View-only badge/legend, glyphs, phase.name fallback; never imports PhaseTimeline/PhaseCard.
- REQ-5: empty Builder = exactly describe + hint + disabled button; single state transition (no stagger/animation); 400px push panel (not overlay); 6 phase_type forms render only real fields; integrity_policy greyed only on llm_emit.
- ok:false generate -> honest failure, never a broken draft.
- tsc -b clean.
</success_criteria>

<output>
After completion, create `.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-04-SUMMARY.md`
</output>
