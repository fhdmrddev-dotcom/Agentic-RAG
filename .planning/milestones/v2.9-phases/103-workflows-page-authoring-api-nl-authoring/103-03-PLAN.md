---
phase: 103-workflows-page-authoring-api-nl-authoring
plan: 03
type: execute
wave: 2
depends_on: [103-01]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/nav-items.ts
  - frontend/src/App.tsx
  - frontend/src/components/layout/NavPanel.tsx
  - frontend/src/components/layout/AppDock.tsx
  - frontend/src/components/workflows/deriveTier.ts
  - frontend/src/components/workflows/deriveTier.test.ts
  - frontend/src/lib/api.workflows.test.ts
autonomous: true
requirements: [WFAUTH-04]
must_haves:
  truths:
    - "ActiveView includes 'workflows'"
    - "The Workflows nav entry appears from a single shared NAV_ITEMS with a distinct non-gear icon"
    - "AppDock.tsx is deleted and no import breaks (tsc -b + vite build clean)"
    - "deriveTier() is computed client-side from (citation_policy + validator-kind set); toggling citation_policy strict->draft changes the tier with NO server round-trip"
    - "deriveTier marks the judge gate (llm_judge_rubric) present even on LOOSE"
    - "publishWorkflow distinguishes the 4 HTTP outcomes (200-with-block != success; 400/404/409 distinct)"
    - "No react-router/useNavigate/URL is introduced"
  artifacts:
    - path: "frontend/src/lib/nav-items.ts"
      provides: "the single shared NAV_ITEMS const incl. the Workflows entry"
      contains: "workflows"
    - path: "frontend/src/components/workflows/deriveTier.ts"
      provides: "deriveTier()/TIERS single source of truth"
      exports: ["deriveTier", "TIERS"]
    - path: "frontend/src/lib/api.ts"
      provides: "authoring client fns + PublishVerdict/LintError/draft types"
      exports: ["createWorkflowDraft", "updateWorkflowDraft", "deleteWorkflowDraft", "listDraftWorkflows", "generateWorkflow", "publishWorkflow"]
  key_links:
    - from: "frontend/src/lib/api.ts publishWorkflow"
      to: "POST /workflows/{id}/publish"
      via: "4 distinct HTTP outcomes (200 body read / 400 / 404 / 409)"
      pattern: "status === 409|status === 404|status === 400"
    - from: "frontend/src/components/layout/NavPanel.tsx"
      to: "frontend/src/lib/nav-items.ts"
      via: "import the shared NAV_ITEMS"
      pattern: "from \"@/lib/nav-items\""
---

<objective>
Build the frontend foundation the Builder, gauntlet UI, and Workflows page all consume (REQ-7 wiring): the `api.ts` authoring client fns + `PublishVerdict`/`LintError`/draft TS types, the net-new client-side `deriveTier()`/`TIERS`, the shared `NAV_ITEMS` const (killing the triplication), the `"workflows"` `ActiveView` member, the NavPanel nav-entry wiring, and the deletion of the dead `AppDock.tsx`.

Implements the REQ-7 plumbing (ActiveView + shared NAV_ITEMS + AppDock deletion + authoring client fns/types + deriveTier). Wave 2 — it gates the Builder (Plan 04), the gauntlet UI (Plan 05), and the Workflows page (Plan 06).

Purpose: One shared source of truth for nav, one client layer for all authoring calls, one tier-derivation function — so the three Wave-3 UI plans compose against contracts, not exploration. No router (the locked no-router contract).
Output: extended `api.ts`, new `nav-items.ts` + `deriveTier.ts`, the `"workflows"` ActiveView, NavPanel consuming the shared const, AppDock deleted, and 2 frontend test files.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-SPEC.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-PATTERNS.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-RESEARCH.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-VALIDATION.md
@.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-01-SUMMARY.md

<interfaces>
<!-- Extracted from the live codebase. Use directly. -->

frontend/src/lib/api.ts — existing patterns to mirror:
- listPublishedWorkflows (lines 1140-1147): getAuthHeaders() -> fetch(`${API_BASE}/workflows/published`,{headers,signal}); if(!res.ok) throw; return json.
- PublishedWorkflow {id, slug, name} (lines 1116-1120).
- createThread (api.ts:49-60), sendMessage with workflowDefinitionId (api.ts:447-459) — reused by Plan 06's doRun (DO NOT modify here).
- API_BASE + getAuthHeaders are the existing helpers.

frontend/src/App.tsx (line 9) — the ActiveView union (5 members today; add "workflows"):
    export type ActiveView = "chat" | "documents" | "skills" | "settings" | "library-health"

frontend/src/components/layout/NavPanel.tsx (lines 44-50) — the NAV_ITEMS array to EXTRACT into the shared const:
    const NAV_ITEMS = [
      { view: "chat", icon: MessageSquare, label: "Chat" },
      { view: "documents", icon: FileText, label: "Documents" },
      { view: "library-health", icon: Activity, label: "Library Health" },
      { view: "skills", icon: Zap, label: "Skills" },
      { view: "settings", icon: Settings, label: "Settings" },
    ] as const
  Also: the `// From AppDock` comment at line 28 (delete it).

frontend/src/components/layout/AppDock.tsx — DEAD (verified zero importers); DELETE the whole file.
frontend/src/components/layout/ChatLayout.tsx (line 18) — NAV_ITEMS_MOBILE (consumed by the mobile drawer). NOTE: ChatLayout's NAV_ITEMS_MOBILE consumption + the "workflows" render branch are owned by Plan 06 (to avoid an import-before-exists break on the WorkflowsPage). This plan does NOT edit ChatLayout — it only provides nav-items.ts for Plan 06 to consume.

Backend route contracts (from Plan 01/02 — the client mirrors these):
- POST /workflows (create draft) -> {id, version}
- GET /workflows/drafts -> [{id, slug, version, name?}]
- PATCH /workflows/{id} (body: WorkflowDefinition) -> updated row; 409 on published; 404 on not-found
- DELETE /workflows/{id} -> 204; 409 on published; 404 on not-found
- POST /workflows/generate {describe, project_folder_id?, template_asset_id?, template_placeholders?} -> {ok, definition} | {ok:false, error, detail}
- POST /workflows/{id}/publish {golden_input} -> PublishVerdict (200-with-block / 200-success); 400 business_requirement (verdict in detail); 404 not_found; 409 already_published.

PublishVerdict (backend api/workflows.py:84-93): {published: bool, version: int|null, golden_run_id: string|null, blocked_stage: string|null, named_failures: unknown[]}.
LintError codes (LOWERCASE): bad_index, unsatisfiable_skip, orphan_phase, no_terminal, input_unsatisfied.
citation_policy enum: "strict"|"flag"|"partial"|"draft". ValidatorSpec.kind incl. llm_judge_rubric, citations_required, output_file_valid, freshness, structure_check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActiveView + shared nav-items.ts + NavPanel consuming it + AppDock deletion (no router)</name>
  <read_first>
    - frontend/src/App.tsx (the ActiveView union :9 + every consumer of ActiveView so the new member typechecks)
    - frontend/src/components/layout/NavPanel.tsx (the NAV_ITEMS array :44-50, the `// From AppDock` comment :28, the icon imports :15-20)
    - frontend/src/components/layout/AppDock.tsx (confirm it is dead before deleting)
    - frontend/src/components/layout/ChatLayout.tsx (line 18 NAV_ITEMS_MOBILE — DO NOT edit; just confirm it exists for Plan 06)
    - sketch-findings-agentic-rag SKILL.md (the three-homes nav contract; "distinct non-gear icon"; D-103 discretion: Workflow vs GitBranch)
  </read_first>
  <action>
    1. `frontend/src/App.tsx`: extend the union to `export type ActiveView = "chat" | "documents" | "skills" | "settings" | "library-health" | "workflows"`. (No router; useState<ActiveView> stays the whole nav model.)

    2. Create `frontend/src/lib/nav-items.ts` — the SINGLE shared const (extract from NavPanel, add the Workflows entry with a distinct non-gear lucide icon — use `Workflow` from lucide-react per discretion):
       ```ts
       import { MessageSquare, FileText, Activity, Zap, Settings, Workflow } from "lucide-react"
       import type { ActiveView } from "@/App"
       export interface NavItem { view: ActiveView; icon: typeof MessageSquare; label: string }
       export const NAV_ITEMS: readonly NavItem[] = [
         { view: "chat", icon: MessageSquare, label: "Chat" },
         { view: "workflows", icon: Workflow, label: "Workflows" },
         { view: "documents", icon: FileText, label: "Documents" },
         { view: "library-health", icon: Activity, label: "Library Health" },
         { view: "skills", icon: Zap, label: "Skills" },
         { view: "settings", icon: Settings, label: "Settings" },
       ] as const
       ```
       (Order/placement of the Workflows entry is discretionary within the three-homes contract; keep it a top-level home.)

    3. `frontend/src/components/layout/NavPanel.tsx`: delete the local `NAV_ITEMS` array (:44-50) and the `// From AppDock` comment (:28); `import { NAV_ITEMS } from "@/lib/nav-items"`; drop now-unused icon imports that were ONLY used by the local array (keep any still referenced elsewhere in the file). The render loop over NAV_ITEMS is unchanged.

    4. Delete `frontend/src/components/layout/AppDock.tsx` entirely (verified zero importers). Leave the cosmetic `index.css` AppDock comments (per RESEARCH Runtime State Inventory).

    Do NOT add react-router/useNavigate/URL anywhere.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && grep -rc "react-router" src | grep -v ":0" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"workflows"' frontend/src/App.tsx` shows the new ActiveView member.
    - `frontend/src/lib/nav-items.ts` exists and contains a `workflows` entry with a non-gear icon (`Workflow`/`GitBranch`, NOT `Settings`).
    - `grep -n "from \"@/lib/nav-items\"" frontend/src/components/layout/NavPanel.tsx` confirms NavPanel consumes the shared const; NavPanel no longer declares a local `NAV_ITEMS` array.
    - `frontend/src/components/layout/AppDock.tsx` does NOT exist (`test ! -f`).
    - `grep -rn "AppDock" frontend/src --include=*.tsx --include=*.ts` returns no import/JSX usage (cosmetic index.css comment OK).
    - `cd frontend && npx tsc -b` exits 0 (no broken import from the deletion).
    - `grep -rn "react-router\|useNavigate" frontend/src` returns NOTHING (no-router contract, REQ-7 j).
  </acceptance_criteria>
  <done>ActiveView has "workflows"; the shared NAV_ITEMS const exists and NavPanel + (later Plan 06) the mobile drawer consume it; AppDock deleted with no broken import; tsc -b clean; no react-router.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveTier()/TIERS client-side single source of truth (REQ-7 h)</name>
  <read_first>
    - 103-RESEARCH.md (the deriveTier() excerpt :477-485; the Strictness-real-enums-only constraint)
    - 103-SPEC.md REQ-7 acceptance (h) + Constraints "Strictness = REAL enums only" + "How we'd know this failed" (the tier-badge failure mode)
    - sketch-findings-agentic-rag SKILL.md (the Workflows Page D10: STRICT 🔒 / MIDDLE ◐ / LOOSE ○ -> strict/flag/draft + actual gates; judge always-on even on LOOSE; deriveTier = TIERS single source of truth, badge can't drift)
    - frontend/src/components/workflows/deriveTier.test.ts (create in this task)
  </read_first>
  <behavior>
    - deriveTier(citationPolicy, validatorKinds: Set<ValidatorKind>) returns one of STRICT/MIDDLE/LOOSE derived ONLY from the real enums.
    - Mapping rule (fixed; thresholds discretionary): citation_policy 'strict' (+ stricter gate signature) -> STRICT; 'flag'/'partial' -> MIDDLE; 'draft' -> LOOSE. The validator-kind set refines within (e.g. presence of output_file_valid/structure_check/freshness raises the floor).
    - llm_judge_rubric is present even on LOOSE (the judge is always-on) — TIERS reflects this.
    - No stored/free-text label; toggling citation_policy strict->draft changes the result with NO server call.
  </behavior>
  <action>
    Create `frontend/src/components/workflows/deriveTier.ts`:
    - Export `type CitationPolicy = "strict" | "flag" | "partial" | "draft"` and `type ValidatorKind = "citations_required" | "output_file_valid" | "freshness" | "structure_check" | "llm_judge_rubric"` (mirror the real enums).
    - Export `const TIERS = { STRICT: { id: "STRICT", glyph: "🔒", label: "Strict", ... }, MIDDLE: { id: "MIDDLE", glyph: "◐", label: "Middle", ... }, LOOSE: { id: "LOOSE", glyph: "○", label: "Loose", ... } } as const` — the SINGLE source of truth for the badge (presentation discretionary; glyphs per the sketch).
    - Export `type Tier = (typeof TIERS)[keyof typeof TIERS]`.
    - `export function deriveTier(citationPolicy: CitationPolicy, validatorKinds: Set<ValidatorKind>): Tier` — pure, derives on every call from (citationPolicy + validatorKinds), NEVER reads a stored label. Document the mapping rule inline.
    - Add a brief note that `llm_judge_rubric` is expected present even on LOOSE (the judge always-on contract).

    Create `frontend/src/components/workflows/deriveTier.test.ts`:
    - strict + full gate set -> STRICT.
    - draft + minimal gates -> LOOSE; assert the SAME validatorKinds with citation_policy flipped strict->draft returns a DIFFERENT tier (proves client-derivation, no round-trip).
    - flag/partial -> MIDDLE.
    - judge-present-even-on-LOOSE: a LOOSE result still treats llm_judge_rubric as present (assert via TIERS / the documented contract).
    - deriveTier is pure (same inputs -> same output; no fetch/import of api).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/deriveTier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "export function deriveTier|export const TIERS" frontend/src/components/workflows/deriveTier.ts` returns both.
    - deriveTier.ts imports NOTHING from `@/lib/api` (no server round-trip — purely client-derived).
    - The test asserts flipping citation_policy strict->draft (same validatorKinds) changes the returned tier with no fetch.
    - `grep -nE "level 1|level 2|level 3|compliance mode" frontend/src/components/workflows/deriveTier.ts` returns NOTHING (no invented labels — real enums only).
    - deriveTier.test.ts exits 0 GREEN.
  </acceptance_criteria>
  <done>deriveTier()/TIERS derive the tier client-side from the real (citation_policy + validator-kind) enums; no stored label; toggle changes the badge with no round-trip; judge always-on; test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: api.ts authoring client fns + PublishVerdict/LintError/draft types (4 distinct HTTP outcomes on publish)</name>
  <read_first>
    - frontend/src/lib/api.ts (listPublishedWorkflows :1140-1147 — the fn shape to mirror; PublishedWorkflow :1116-1120; API_BASE + getAuthHeaders; createThread :49-60; sendMessage :447-459 — DO NOT modify, just confirm the launch fns exist for Plan 06)
    - 103-PATTERNS.md (the api.ts analog excerpt :231-264; the "must distinguish 4 HTTP outcomes" note)
    - 103-SPEC.md REQ-6 (the 4 HTTP outcomes + verbatim PublishVerdict) + REQ-1 (CRUD)
    - frontend/src/lib/api.workflows.test.ts (create in this task)
  </read_first>
  <behavior>
    - createWorkflowDraft(def) -> {id, version}; listDraftWorkflows() -> draft rows; updateWorkflowDraft(id, def) -> updated row (throws a typed PublishedConflict-style error on 409, NotFound on 404); deleteWorkflowDraft(id) (409/404 distinguished); generateWorkflow(body) -> {ok, definition} | {ok:false,...}.
    - publishWorkflow(id, golden_input) distinguishes: 200 (read body.published/blocked_stage — a 200-with-block is NOT success), 400 (business_requirement, verdict in detail), 404 (not_found), 409 (already_published). A binary 200=ok/else=error handler is FORBIDDEN.
  </behavior>
  <action>
    Extend `frontend/src/lib/api.ts` (mirror the listPublishedWorkflows fn shape; reuse API_BASE/getAuthHeaders). Add types:
    - `export type CitationPolicy = "strict"|"flag"|"partial"|"draft"` (or re-export from deriveTier — keep one source).
    - `export interface PublishVerdict { published: boolean; version: number | null; golden_run_id: string | null; blocked_stage: string | null; named_failures: unknown[] }` (named_failures is polymorphic — `unknown[]`, rendered by key-detection in Plan 05).
    - `export interface LintError { code: string; phase: string; message: string }` (codes are the 5 LOWERCASE literals).
    - `export interface WorkflowDraftRow { id: string; slug: string; version: number; name: string | null }`.
    - `export type GenerateResult = { ok: true; definition: WorkflowDefinitionJSON } | { ok: false; error: string; detail?: string }` (use a permissive `WorkflowDefinitionJSON` type alias the Builder refines).
    - `export type PublishOutcome = { kind: "verdict"; verdict: PublishVerdict } | { kind: "business_requirement"; verdict: PublishVerdict } | { kind: "not_found" } | { kind: "already_published" }` (the 4 distinguished outcomes).

    Add fns:
    - `createWorkflowDraft(def, signal?)` -> POST `/workflows`, returns `{id, version}`.
    - `listDraftWorkflows(signal?)` -> GET `/workflows/drafts`, returns `WorkflowDraftRow[]`.
    - `updateWorkflowDraft(id, def, signal?)` -> PATCH `/workflows/{id}`; on 409 throw a typed `WorkflowConflictError`; on 404 throw `WorkflowNotFoundError`.
    - `deleteWorkflowDraft(id, signal?)` -> DELETE `/workflows/{id}`; same 409/404 distinction.
    - `generateWorkflow(body, signal?)` -> POST `/workflows/generate`, returns `GenerateResult` (the route returns 200 even on ok:false — read the body, never throw on ok:false).
    - `publishWorkflow(id, golden_input, signal?)` -> POST `/workflows/{id}/publish` with `{golden_input}`; switch on res.status: 200 -> `{kind:"verdict", verdict: body}` (read body to tell pass from block — DO NOT re-derive); 400 -> `{kind:"business_requirement", verdict: body.detail}`; 404 -> `{kind:"not_found"}`; 409 -> `{kind:"already_published"}`; anything else -> throw.

    Create `frontend/src/lib/api.workflows.test.ts` (mock `fetch`):
    - publishWorkflow: a 200 with `{published:false, blocked_stage:"judge"}` returns kind:"verdict" with published:false (NOT a success); a 200 with published:true returns kind:"verdict" success; 400/404/409 each map to their distinct kind. Assert a binary 200=ok handler is NOT used (the 200-with-block case must surface published:false).
    - generateWorkflow: a 200 ok:false returns the structured error WITHOUT throwing.
    - updateWorkflowDraft: a 409 throws WorkflowConflictError.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/api.workflows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "export async function (createWorkflowDraft|updateWorkflowDraft|deleteWorkflowDraft|listDraftWorkflows|generateWorkflow|publishWorkflow)" frontend/src/lib/api.ts` returns all 6.
    - `grep -nE "status === 409|status === 404|status === 400" frontend/src/lib/api.ts` confirms publishWorkflow handles the distinct HTTP codes (not a binary 200/else).
    - `grep -n "export interface PublishVerdict" frontend/src/lib/api.ts` shows the 5-field shape with `named_failures: unknown[]`.
    - api.workflows.test.ts asserts a 200-with-`blocked_stage` is surfaced as a block (published:false), proving no re-derivation and no binary handler.
    - `cd frontend && npx tsc -b` exits 0.
    - api.workflows.test.ts exits 0 GREEN.
  </acceptance_criteria>
  <done>api.ts has all 6 authoring fns + PublishVerdict/LintError/draft types; publishWorkflow distinguishes the 4 HTTP outcomes (200-with-block != success); generateWorkflow reads ok:false without throwing; tests GREEN; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client api.ts → backend /workflows routes | the client must not re-derive server verdicts (publish) or hide a block as a success |
| deriveTier (client) → badge | a tier label that drifts from the real gate signature would mislead about strictness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-03-01 | Tampering | publishWorkflow client re-derives pass/block | mitigate | publishWorkflow reads the SERVER verdict verbatim; a 200-with-block surfaces published:false; the 4 HTTP outcomes are distinct (a binary 200=ok handler is a FAIL); test-asserted |
| T-103-03-02 | Tampering | tier badge reads a stored/free-text strictness label | mitigate | deriveTier is pure + client-derived from the real (citation_policy + validator-kind) enums; no stored label; no invented "level 1/2/3"; toggle-changes-no-round-trip asserted |
| T-103-03-03 | Information Disclosure | the client widens owner scope via the project filter | accept | The filter is owner-scoped server-side (AND-appended, can only narrow — T-098-09); the client only passes the param. No client-side scope authority. |
| T-103-03-04 | Tampering | a 409/404 from a published/cross-user mutation is swallowed as success | mitigate | updateWorkflowDraft/deleteWorkflowDraft throw typed errors on 409/404; the UI (Plans 04/06) surfaces them — never a silent overwrite |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/deriveTier.test.ts src/lib/api.workflows.test.ts` — GREEN.
- `cd frontend && npx tsc -b` — exits 0 (AppDock deletion + ActiveView + nav-items + api types all typecheck).
- `grep -rn "react-router\|useNavigate" frontend/src` — empty.
- `grep -rn "AppDock" frontend/src --include=*.ts --include=*.tsx` — no import/JSX (cosmetic index.css OK).
- Frontend rot caveat: prove net-new green via base-checkout, not raw count (SEED-056 / project_frontend_vitest_rot).
</verification>

<success_criteria>
- REQ-7: ActiveView has "workflows"; the Workflows nav entry comes from a single shared NAV_ITEMS (distinct non-gear icon); AppDock deleted clean; no react-router.
- REQ-7 h: deriveTier()/TIERS derive the tier client-side; toggle changes the badge with no round-trip; judge always-on even on LOOSE.
- REQ-6/REQ-1 client: publishWorkflow distinguishes the 4 HTTP outcomes; CRUD fns + generate fn exist with typed conflict handling.
- tsc -b clean; the foundation is ready for Plans 04/05/06.
</success_criteria>

<output>
After completion, create `.planning/phases/103-workflows-page-authoring-api-nl-authoring/103-03-SUMMARY.md`
</output>
