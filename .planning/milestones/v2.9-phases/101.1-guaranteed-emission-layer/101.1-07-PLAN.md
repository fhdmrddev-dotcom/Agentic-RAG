---
phase: 101.1-guaranteed-emission-layer
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [TMPL-02, TMPL-03]
files_modified:
  - backend/app/services/openai_service.py
  - backend/app/services/forced_emit.py
  - backend/app/services/harness/phase_types.py
  - backend/app/services/harness_engine.py
  - backend/tests/unit/test_forced_emit.py
  - backend/tests/unit/test_llm_emit_executor.py
  - backend/tests/unit/test_gateway_forcing.py

must_haves:
  truths:
    - "A DeepSeek v4 forced emit never 400s on 'Thinking mode does not support this tool_choice' — the thinking block is disabled when a tool is forced"
    - "Any exception raised inside forced_emit OR _exec_llm_emit (provider 400, render/persist raise) is caught, writes an emit_failed receipt, surfaces ONE honest message, flips the active phase to failed, and returns an honest output — never a silent run-crash that leaves a phase stuck 'active'"
    - "An honest emit failure persists EXACTLY ONE assistant message (not two)"
  artifacts:
    - path: "backend/app/services/openai_service.py"
      provides: "DeepSeek thinking disabled on forced emit (FORCE-NOTHINK rider)"
      contains: "force_tool_name"
    - path: "backend/app/services/forced_emit.py"
      provides: "try/except around open_stream/_drain → honest _failure('provider_error')"
      contains: "provider_error"
    - path: "backend/app/services/harness/phase_types.py"
      provides: "_exec_llm_emit catch-all wrapping the attempt loop + render dispatch"
      contains: "_emit_unexpected_failure"
  key_links:
    - from: "backend/app/services/harness/phase_types.py"
      to: "harness_engine completion persist"
      via: "flagged failure output (emit_failed flag) the engine skips re-persisting"
      pattern: "emit_failed|_surfaced"
    - from: "backend/app/services/openai_service.py"
      to: "DeepSeek extra_body.thinking"
      via: "skip the thinking enable when force_tool_name is set"
      pattern: "force_tool_name is None"
---

<objective>
Close gap 1 (the DeepSeek silent run-crash: FORCE-NOTHINK provider fix + the missing D-08 layer-6 exception backstop) and gap 2 (duplicate honest-failure messages). After this plan, a forced emit on DeepSeek v4 no longer 400s, and ANY raised exception anywhere in the emit ladder (provider error, render error, persist error) is caught by the engine/executor and converted to an honest receipt + ONE surfaced message + a flipped phase row — never the silent run-crash UAT Test 2 found (runs 575e7345 / a7f415ad) where the run is marked failed but no receipt is written, no message surfaces, and the workflow_phases row is stuck 'active' forever.

Purpose: the no-fail ladder (D-08) is only honest if its layer-6 backstop actually wraps the code that can raise. Today forced_emit's open_stream/_drain and _exec_llm_emit's attempt loop + render dispatch are UNWRAPPED, so a provider 400 escapes the whole ladder to threads.py agent_runner (G-5 frozen — must NOT be the fix site). The engine/executor own the fix.

Output: a DeepSeek-forceable emit; a catch-all in forced_emit and _exec_llm_emit; a single-owner failure persist (no duplicate message).

RED LINE (non-negotiable): Deep Mode byte-identical; the shared SSE/chunk path NEVER branched per provider; threads.py is NOT touched (G-5); no docxtpl import in backend/app; all provider-specific handling stays at the service boundary. The DeepSeek fix is provider-scoped inside the EXISTING forced branch — it does not touch the auto path.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101.1-guaranteed-emission-layer/101.1-CONTEXT.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-UAT.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-06-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase — the executor should use these directly, no exploration needed. -->

The DeepSeek thinking-enable block — openai_service.py:1356-1361 (inside create_adaptive_streaming_chat):
```python
if provider == "deepseek" or effective_model.startswith("deepseek-"):
    kwargs.setdefault("extra_body", {})
    kwargs["extra_body"]["thinking"] = {
        "type": "enabled",
        "reasoning_effort": "high",
    }
```
The forced branch follows at :1369 — `if force_tool_name is not None:` sends the named tool_choice. The two are independent today: a DeepSeek forced emit enters BOTH blocks → thinking ON + named tool_choice → DeepSeek 400 "Thinking mode does not support this tool_choice". `force_tool_name` is already a parameter of create_adaptive_streaming_chat (Plan 02). DeepSeek shares Anthropic's no-force-under-thinking constraint; the D-13 two-step already moves reasoning to the gather phase, so the emit call needs no thinking.

forced_emit.py — the UNWRAPPED call sites (:213-215):
```python
stream, calling_mode = await open_stream(provider, req)
content, tool_calls, finish_reason = await run_in_threadpool(_drain, stream)
```
`_failure(tier, provider, *, forced, truncated=False)` already exists and returns the honest-failure dict shape `{emitted: None, tier, provider, forced, recovered_from_narration: False, truncated, failure: <str>}`. A provider 400 from open_stream/_drain currently propagates as a raised exception.

_exec_llm_emit (phase_types.py:990-1295) — the body has full inline honest-failure handling for states a-e, BUT no try/except wraps the attempt loop (:1100) or the render dispatch (:1245 `await entry.post_processor(...)`). _emit_failure_output(failure, message, *, field_map=None) → `{"text": message, "failure": failure}` already exists (:979). _surface_failure_message(ctx, run_id, reason, pool) (module-level :152) delegates to harness_engine._surface_failure_message (the harness-only persist — NEVER the shared Deep path). _emit_audit(ctx, event_type, metadata) and _emit_phase_substep(ctx, phase, *, status/failure) are best-effort (no redis = no crash).

The double-persist (gap 2): _exec_llm_emit calls _surface_failure_message DIRECTLY at its 5 failure returns AND returns _emit_failure_output(...) whose `text` becomes `last_output` → `ctx.final_output` (harness_engine.py:1019) → _surface_final_answer (harness_engine.py:1057) persists it AGAIN as the final answer (the run "completes" with the failure text). Two writers, one message.

The engine completion path — harness_engine.py:1015-1058:
```python
ctx.final_output = last_output                       # :1019
...
await finish_run(pool, run_id, "completed")          # :1045
await _surface_final_answer(ctx, run_id, stream_run_id, redis, pool)  # :1057 — persists ctx.final_output.text
```
_surface_final_answer reads `final_text = (ctx.final_output or {}).get("text", "")` (:360) and persists it when non-empty (:413).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeepSeek FORCE-NOTHINK — disable thinking on a forced emit (gap 1a)</name>
  <files>backend/app/services/openai_service.py, backend/tests/unit/test_gateway_forcing.py</files>
  <behavior>
    - Test 1: a DeepSeek forced call (provider="deepseek" or model startswith "deepseek-", force_tool_name set) builds kwargs WITHOUT `extra_body.thinking` (thinking disabled) AND with the named `tool_choice`. Assert `kwargs.get("extra_body", {}).get("thinking") is None` and `kwargs["tool_choice"]["function"]["name"] == force_tool_name`.
    - Test 2: a DeepSeek call with force_tool_name=None (the Deep / non-forced path) STILL enables thinking (byte-identical) — assert `kwargs["extra_body"]["thinking"]["type"] == "enabled"`.
    - Test 3: a non-DeepSeek forced call is unaffected (no extra_body.thinking key appears just because forcing is on).
  </behavior>
  <action>
    In create_adaptive_streaming_chat (openai_service.py), gate the DeepSeek thinking-enable block (:1356-1361) so it does NOT enable thinking when a tool is being forced. Change the condition from `if provider == "deepseek" or effective_model.startswith("deepseek-"):` to additionally require `force_tool_name is None` — i.e. `if (provider == "deepseek" or effective_model.startswith("deepseek-")) and force_tool_name is None:`. Add a comment: "Phase 101.1-07 (gap 1a / D-15 / TIER-FORCE-NOTHINK): DeepSeek shares Anthropic's no-force-under-thinking constraint — a named tool_choice WITH thinking ON returns 'Thinking mode does not support this tool_choice' (UAT runs 575e7345/a7f415ad). The D-13 two-step already moves reasoning to the gather phase, so the forced emit call runs thinking-OFF. Provider-scoped, inside the forced branch's precondition — the auto path (force_tool_name None) is byte-identical."
    Do NOT touch the forced branch (:1369) itself, the auto path, or any shared chunk/SSE handler. This is the symmetric sibling of the anthropic thinking-OFF seam Plan 02 shipped (D-14 / TIER-FORCE-NOTHINK now covers DeepSeek too).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_gateway_forcing.py -x -q</automated>
  </verify>
  <done>A DeepSeek forced emit sends a named tool_choice with thinking OFF; the non-forced DeepSeek path keeps thinking ON (byte-identical); non-DeepSeek forcing is unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: forced_emit provider-error backstop (gap 1b, layer 6 — substrate half)</name>
  <files>backend/app/services/forced_emit.py, backend/tests/unit/test_forced_emit.py</files>
  <behavior>
    - Test 1: when open_stream raises (simulate a provider 400 — patch open_stream to raise), forced_emit returns an honest failure dict `{"emitted": None, "failure": "provider_error", ...}` instead of propagating the exception. Assert no exception escapes and `result["failure"] == "provider_error"`.
    - Test 2: when _drain raises (patch run_in_threadpool/_drain to raise), forced_emit likewise returns `failure == "provider_error"` — never raises.
    - Test 3: the happy path (a clean forced shot) is UNCHANGED — still returns `{"emitted": <EmitFieldMap>, "failure": None, ...}`.
  </behavior>
  <action>
    Wrap the open_stream + _drain call sites in forced_emit (:213-215) in a try/except. On any Exception, log at warning (identifier-only — never log message content per T-073-04: `logger.warning("forced_emit: provider call raised tier=%s provider=%s", tier, provider)`), then `return _failure(tier, provider, forced=forced)` BUT extend the failure to carry a distinct `failure="provider_error"` value (add a `failure_override` kwarg to `_failure` defaulting to None, or build the dict inline mirroring `_failure`'s shape with `"failure": "provider_error"`). Keep the existing `_failure(...)` happy-failure values intact (model_failed_to_emit / truncated) — only the RAISED-exception path uses `provider_error`. Add a comment naming gap 1b and D-08 layer 6 (the substrate half: a provider raise is an honest failure, never a silent escape to threads.py agent_runner).
    The truncation guard, the recovery path, and the validate loop stay AFTER the try (they only run when open_stream/_drain succeeded). Ensure `is_truncated` / recovery logic remain reachable on the success path only.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_forced_emit.py -x -q</automated>
  </verify>
  <done>A provider exception from open_stream or _drain returns an honest `provider_error` failure dict; the happy path and existing failure values are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: _exec_llm_emit catch-all + single-owner failure persist (gap 1b executor half + gap 2)</name>
  <files>backend/app/services/harness/phase_types.py, backend/app/services/harness_engine.py, backend/tests/unit/test_llm_emit_executor.py</files>
  <behavior>
    - Test 1 (catch-all): when forced_emit returns failure="provider_error" (Task 2's path), OR when the render dispatch (entry.post_processor) RAISES, _exec_llm_emit does NOT propagate — it writes an emit_failed audit receipt, emits a phase_substep failure, calls _surface_failure_message once, and returns an honest _emit_failure_output (text non-empty, failure set). Assert the returned dict has a `failure` key + non-empty `text` and that no exception escaped.
    - Test 2 (provider_error → state a): a forced_emit `failure="provider_error"` result is treated as an honest model/provider failure (state a family) — receipt written, message surfaced, honest output returned (not a crash).
    - Test 3 (single-owner persist / gap 2): a failure output returned by _exec_llm_emit carries a flag (e.g. `"_surfaced": True` or `"emit_failed": True`) AND _surface_final_answer in the engine SKIPS the second persist when that flag is present on ctx.final_output. Assert _surface_final_answer (or its guard) does not insert a second assistant message for a flagged emit failure. (Use the engine helper or a thin guard — see action.)
  </behavior>
  <action>
    (A) CATCH-ALL — wrap the body of _exec_llm_emit (the GAP-B resolve through the render-dispatch return, i.e. everything after the `definition/emitter/model/run_id/pool` extraction at :1028) in a try/except. On any unexpected Exception (the ones NOT already handled by the inline state a-e returns — e.g. the render dispatch raising, or any new raise): write an `emit_failed` audit receipt (reuse `_emit_audit` + `_emit_audit_metadata` with `render_verdict={"failure": "render_failed", "reason": "unexpected"}`), emit `_emit_phase_substep(ctx, phase, failure="render_failed")`, call `_surface_failure_message(ctx, run_id, msg, pool)` ONCE with an honest message ("The deliverable could not be produced due to an unexpected error in the emit pipeline; no Markdown stand-in is delivered."), and `return _emit_failure_output("render_failed", msg)`. Factor the unexpected-failure handling into a small helper `_emit_unexpected_failure(ctx, phase, definition, emitter, run_id, pool, exc)` so the except body is one call. Log identifier-only (`logger.exception("llm_emit: unexpected failure run=%s", run_id)`). The catch-all must also handle the forced_emit `provider_error` result inline (in the existing `if result.get("failure"):` branch at :1130 — it already returns state (a); confirm a `provider_error` value flows through that branch and surfaces honestly. If the message should distinguish provider errors, extend that branch's `msg` minimally).
    (B) SINGLE-OWNER PERSIST (gap 2) — make _emit_failure_output flag its output so the engine persists it ONCE. Add `out["_surfaced"] = True` inside _emit_failure_output (:979-987) — the executor already surfaced the message via _surface_failure_message. Then in harness_engine._surface_final_answer (:359-414), add a guard near the top: if `(getattr(ctx, "final_output", None) or {}).get("_surfaced")` is truthy, SKIP the durable persist block (the assistant message was already written by _surface_failure_message) but STILL emit the `delta` so the failure text shows live without a reload — OR (simpler, recommended) skip BOTH the persist and the delta when `_surfaced` is set, because _surface_failure_message already persisted the row and the next reconcile renders it. Pick the variant that yields exactly ONE assistant message AND a visible failure on the live screen; document the choice in a comment. Mark this guard as harness-only (Deep never returns a `_surfaced` final_output — Deep does not run _exec_llm_emit).
    (C) PHASE-ROW FLIP — the engine's onRunFailed/finish path is the row-flip owner, but a flagged emit failure currently returns as a NORMAL output so the run "completes" and the active phase is never flipped to failed. Ensure that when _exec_llm_emit returns a `failure`-bearing output, the engine treats the phase as failed: in run_workflow, after a phase executor returns, if the output dict has a `failure` key, set that phase's status to failed via the existing per-phase status write (do NOT add a new schema — reuse the existing workflow_phases status update the engine already performs on gate failure). Keep this additive and harness-only. This closes the "phase stuck active" half of gap 1 for the GRACEFUL (non-raising) failure path; the RAISING path is covered by the catch-all in (A) which returns a `failure` output that this same check flips.
    Add comments referencing gap 1b (layer-6 backstop) and gap 2 (single-owner). Do NOT touch threads.py (G-5). Do NOT branch the shared Deep+harness terminal path.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_llm_emit_executor.py -x -q</automated>
  </verify>
  <done>An exception anywhere in the emit ladder returns an honest flagged failure output (receipt + one surfaced message + phase flipped failed); _surface_final_answer persists a flagged emit failure exactly once; no exception escapes to the engine's generic run-failed catch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → forced emit args | the model emits the field-map; a provider error or malformed tool-JSON crosses here |
| provider adapter → gateway | DeepSeek's thinking+tool_choice constraint is a provider-shape boundary handled at the service boundary only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101.1-07-01 | Denial of Service | _exec_llm_emit / forced_emit unwrapped raise | mitigate | catch-all backstop converts any raise to an honest receipt + surfaced message + flipped phase (Task 2/3) — a provider 400 can no longer wedge a run in 'active' forever |
| T-101.1-07-02 | Tampering | the shared chunk/SSE path | mitigate | the DeepSeek fix lives inside the EXISTING forced branch precondition (force_tool_name is None) — the auto/Deep path is byte-identical; a guard test asserts no provider branch leaks into the shared path (existing D-14 guard in test_gateway_forcing.py stays green) |
| T-101.1-07-03 | Repudiation | the audit trail | mitigate | the catch-all writes an emit_failed receipt (D-12) so even an unexpected pipeline error leaves an auditable transition keyed to run_id + definition@version |
| T-101.1-07-04 | Information disclosure | failure logs | accept | logs are identifier-only (run_id/tier/provider) — never message/args content (T-073-04 pattern) |
</threat_model>

<verification>
- DeepSeek forced emit no longer 400s on thinking+tool_choice (Task 1 unit; live-confirmed in Plan 10).
- A simulated provider raise and a simulated render raise both yield honest flagged failures, not propagated exceptions (Tasks 2/3).
- Exactly one assistant message persists per honest failure (Task 3 gap-2 guard).
- SEED-056 net-new-failure proof: target suites (test_forced_emit + test_gateway_forcing + test_llm_emit_executor + the harness slice) pass; prove any wider-suite failures are pre-existing rot via base-checkout (the 099/101 convention).
- RED LINE: `git diff --stat` shows no threads.py change; Deep get_tools unchanged; the auto-path branch byte-identical; no docxtpl import added to backend/app.
</verification>

<success_criteria>
- DeepSeek v4 forced emit builds thinking-OFF + named tool_choice (gap 1a).
- forced_emit and _exec_llm_emit catch every raise → honest receipt + ONE surfaced message + flipped phase row (gap 1b).
- Honest emit failures persist exactly one assistant message (gap 2).
- Net-new test failures = 0 vs the SEED-056 rot baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/101.1-guaranteed-emission-layer/101.1-07-SUMMARY.md`.
</output>
