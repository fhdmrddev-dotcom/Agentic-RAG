---
phase: 101.1
slug: guaranteed-emission-layer
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-11
---

# Phase 101.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source of truth: `101.1-RESEARCH.md` §8 (Validation Architecture). Per-task IDs reconcile once PLAN.md files exist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (backend) — `backend/tests/` ; vitest (frontend) for any GAP-C UI |
| **Config file** | `backend/pytest.ini` (existing) ; `frontend/vitest.config.*` |
| **Quick run command** | `cd backend && pytest tests/unit/test_template_render.py tests/unit/test_template_integrity.py tests/unit/test_emit_field_map.py -x` |
| **Full suite command** | `cd backend && pytest -q` |
| **Estimated runtime** | ~30s quick · full suite minutes (baseline rot 115 failed / SEED-056 — prove net-new=0 via baseline checkout, NEVER raw count) |

---

## Sampling Rate

- **After every task commit:** Run the quick command (< 30s)
- **After every plan wave:** `pytest tests/unit/ tests/ -k "emit or render or template or gateway or harness" -q`
- **Before `/gsd:verify-work`:** Full suite green at the SEED-056 baseline (net-new failures = 0, proven by baseline checkout)
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

> Requirement-level map from RESEARCH §8. Task IDs (`101.1-NN-NN`) are filled by the planner; the Nyquist gate at execution reconciles each task against this map.

| Requirement / Decision | Behavior | Test Type | Automated Command | File Exists |
|---|---|---|---|---|
| TMPL-02 | Forced emit produces a cited flat `EmitFieldMap` that validates strict | unit | `pytest tests/unit/test_emit_field_map.py::test_flat_model_strict_schema -x` | ❌ W0 |
| TMPL-02 | Flat `EmitFieldMap` maps to the SAME docxtpl context as the equivalent nested map | unit | `pytest tests/unit/test_template_render.py::test_flat_map_renders_identical -x` | ❌ W0 |
| TMPL-02 | Old 097/101 nested artifacts STILL `model_validate()` (back-compat) | unit | `pytest tests/unit/test_emit_field_map.py::test_legacy_nested_still_validates -x` | ❌ W0 |
| TMPL-02 (D-01) | `_exec_llm_emit` uses a FORCED single call, never the open agent loop (GAP-A) | unit | `pytest tests/unit/test_llm_emit_executor.py::test_emit_never_uses_agent_loop -x` | ❌ W0 |
| TMPL-02 (D-06) | NATIVE narrated-JSON recovery parses a fenced object or fails honestly | unit | `pytest tests/unit/test_forced_emit.py::test_native_recovery_or_honest_fail -x` | ❌ W0 |
| TMPL-02 (D-10) | Bound AssetRef injected at phase-build (GAP-B) — model never selects template | unit | `pytest tests/unit/test_llm_emit_executor.py::test_bound_assetref_injected -x` | ❌ W0 |
| TMPL-02 (D-05) | Tiered forcing translation per provider (mock each adapter) | unit | `pytest tests/unit/test_gateway_forcing.py -x` | ❌ W0 |
| D-14 | Guard: no provider branch leaks into the shared chunk/SSE path; Deep byte-identical | unit | `pytest tests/unit/test_gateway_forcing.py::test_no_provider_branch_in_shared_path -x` | ❌ W0 |
| TMPL-03 | Integrity gate (re-open) unchanged; non-opening file never persisted | unit | `pytest tests/unit/test_template_integrity.py -x` (extend) | ✅ exists |
| D-12 | `write_audit` accepts emit kinds (after migration 069); receipt metadata shape | unit | `pytest tests/unit/test_harness_audit_emit.py -x` | ❌ W0 |
| D-08 | Truncation guard rejects a half-object emission | unit | `pytest tests/unit/test_template_render.py::test_is_truncated_rejects -x` | ✅ exists (extend) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## SC#10 — 4-Axis Cross-Provider Scoreboard (MANDATORY)

> This phase touches streaming + agent-loop + provider-routing → the full 4-axis mandate applies. Authored HERE, not in PLAN.md tasks. Phase verification passes only when all 4 axes are exercised. Re-runs the 8 rows of `101-HUMAN-UAT.md`.

| Axis | Required coverage for 101.1 | Result |
|---|---|---|
| **Cross-provider** | FULL native-7 + OpenRouter — each fills the seeded library template → an openable `.docx` AND a cited flat field-map JSON in the run log. Tiers: OpenAI / Google / DeepSeek-v4 / GLM / MiniMax / OpenRouter(+`require_parameters`) = forced; Anthropic = forced-non-thinking; Kimi/Moonshot = TIER-COERCE (pass OR documented-honest-fail). | ⬜ |
| **Multi-tool** | A workflow with a prior `llm_agent` retrieval phase (`search_documents`) feeding the `llm_emit` phase (the D-13 two-step) — ≥ 1 row exercising retrieval+emit in one run. | ⬜ |
| **Parallel-thread** | Thread A streaming a fill while Thread B accepts a new prompt — the emit's atomic forced call must not block / cross-contaminate. | ⬜ |
| **Long-message** | A large register (≥ 50 rows) forcing the chunking path (multiple forced emits) OR a ≥ 5 KB prior planning-phase output feeding the emit. | ⬜ |

---

## Named G-6 Failure Modes (falsifiable — each must be proven NOT to occur)

| Failure mode | Falsifiable row | Result |
|---|---|---|
| Silent 0-docx (GAP-A) | Every provider either produces a `.docx` OR a visible honest failure naming the gate — NEVER a completed run with 0 files and no error. | ⬜ |
| Narrated-not-emitted (GAP-D) | A reasoning-native that narrates is RECOVERED (D-06) or fails honestly — never a text dump masquerading as the artifact. | ⬜ |
| Uncited-not-rejected | A field-map with an invented citation is rejected BEFORE render (`check_coverage` `invented_citation_count>0` → state b). | ⬜ |
| Won't-open delivered | A forced integrity failure → state d honest error + preserved field-map fallback; the file is NOT delivered. | ⬜ |
| No-template-bound | A definition with no `assets[]` template AND no ephemeral upload → state e honest error, never a silent fall-through. | ⬜ |

---

## Wave 0 Requirements

- [ ] `tests/unit/test_emit_field_map.py` — flat model strict-schema + back-compat (TMPL-02)
- [ ] `tests/unit/test_forced_emit.py` — forced call + D-06 NATIVE recovery + truncation (TMPL-02 / D-06 / D-08)
- [ ] `tests/unit/test_gateway_forcing.py` — per-provider translation + the D-14 shared-path guard
- [ ] `tests/unit/test_llm_emit_executor.py` — executor wiring (no agent loop, AssetRef inject, gates)
- [ ] `tests/unit/test_harness_audit_emit.py` — `write_audit` emit kinds + receipt shape (after migration 069 + `_AUDIT_EVENT_TYPES` extension)
- [ ] Extend `tests/unit/test_template_render.py` + `test_template_integrity.py` (flat-map render parity, truncation)
- [ ] Re-seed the live UAT `WorkflowDefinition` with an `llm_emit` fill phase + prior planning phase (D-13)
- Framework install: none (pytest/vitest exist)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live cross-provider forced emit (native-7 + OpenRouter) | TMPL-02 / SC#10 cross-provider | Real provider behavior + token-level forcing can't be faked (SC#10 rule) | Run the seeded fill workflow on each provider; confirm openable `.docx` + cited flat map in the run log, OR a documented honest TIER-COERCE failure |
| docx run-split via ephemeral upload path (101-UAT #3) | TMPL-03 | Arbitrary user-upload template | Upload an ad-hoc template; confirm `run_replace` + residual scan |
| pptx variable-row limit (101-UAT #5) | TMPL-03 | OOXML pptx fixture; documented limit ("pptx cannot grow tables") | pptx fixture run; confirm documented-limit surfaced honestly |
| xlsx chart strip (101-UAT #6) | TMPL-03 | openpyxl drops charts on save; xlsx fixture | xlsx fixture run; confirm documented limit |
| xlsx merged-cell (101-UAT #7) | TMPL-03 | OOXML merged-cell anchor edge | merged-cell fixture; integrity re-open passes |
| Sandbox isolation proof (101-UAT #8) | TMPL-02 | Requires bumped sandbox image + cached-session eviction timing | New chat (bumped image) renders; old cached session raises `ModuleNotFoundError` |
| Long-message ≥50-row chunking, live per provider | SC#10 long-message | Token-level chunking can't be mocked across providers | Large-register fixture per provider; confirm multiple forced emits compose one openable file |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
