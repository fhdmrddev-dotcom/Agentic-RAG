---
phase: 101.1-guaranteed-emission-layer
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/services/provider_gateway/dispatcher.py
  - backend/app/services/provider_gateway/anthropic.py
  - backend/app/services/provider_gateway/google.py
  - backend/app/services/provider_gateway/openai_compat.py
  - backend/app/services/anthropic_service.py
  - backend/app/services/forced_emit.py
  - backend/tests/unit/test_gateway_forcing.py
  - backend/tests/unit/test_forced_emit.py
autonomous: true
requirements: [TMPL-02, TMPL-03]
user_setup: []

must_haves:
  truths:
    - "A GatewayRequest can carry a forced-tool decision (force_tool_name + strict_schema) that each native adapter translates to its provider shape"
    - "Anthropic forcing runs with extended thinking OFF (TIER-FORCE-NOTHINK); the emit call sets tool_choice to a named tool"
    - "Gemini forcing injects function_calling_config(mode=ANY, allowed_function_names) into config_kwargs"
    - "OpenAI-compat forcing sets a named tool_choice (+ strict response_format when strict_json_schema)"
    - "A tier resolves registry-first, default-SAFE: a forced_emission miss => TIER-COERCE, never wrongly forces"
    - "A still-narrated emission on the NATIVE path is parsed back (fenced object) or fails honestly — never silently dropped (D-06)"
    - "No provider branch for forcing leaks into the shared chunk/SSE path; Deep with tool_choice=auto is byte-identical"
  artifacts:
    - path: "backend/app/services/provider_gateway/dispatcher.py"
      provides: "GatewayRequest gains force_tool_name + strict_schema (additive, defaults preserve auto)"
      contains: "force_tool_name"
    - path: "backend/app/services/forced_emit.py"
      provides: "forced single-shot emit substrate + D-06 NATIVE narrated-JSON recovery + truncation guard"
      contains: "def forced_emit"
    - path: "backend/app/services/anthropic_service.py"
      provides: "stream_anthropic gains exactly one tool_choice param (default preserves :177)"
      contains: "tool_choice"
    - path: "backend/tests/unit/test_gateway_forcing.py"
      provides: "per-provider translation + D-14 shared-path guard test (GREEN)"
      contains: "test_no_provider_branch_in_shared_path"
  key_links:
    - from: "backend/app/services/forced_emit.py"
      to: "backend/app/services/provider_gateway/dispatcher.py"
      via: "builds a GatewayRequest with force_tool_name set, calls open_stream"
      pattern: "open_stream"
    - from: "backend/app/services/provider_gateway/anthropic.py"
      to: "backend/app/services/anthropic_service.py"
      via: "passes a named tool_choice dict to stream_anthropic, thinking OFF"
      pattern: "tool_choice"
    - from: "backend/app/services/forced_emit.py"
      to: "backend/app/services/template_render_service.py"
      via: "is_truncated rejects half-objects; parse_structured_tool_calls recovers narration"
      pattern: "is_truncated"
---

<objective>
Build the **cross-provider core**: the capability-tiered forcing seam at the gateway boundary (D-05) and the `forced_emit` substrate that runs a single sealed forced shot with NATIVE-path narrated-JSON recovery (D-06) and a truncation guard (D-08 layers 1-4). This is the seam that turns the GAP-A/GAP-D 0-docx failure into either a forced emission or an honest failure. The D-14 guard test ships here, proving no provider branch leaks into the shared chunk/SSE path and Deep stays byte-identical.

Purpose: closes the live root cause — reasoning-natives narrating a field-map as prose in an open auto-tool-choice loop. After this plan, the emit step can be FORCED on 6/8 providers, runs thinking-off on Anthropic, and hard-validates/recovers on the rest. Implements D-05, D-06, D-08 (layers 1-4), D-14, D-15 (consumption).
Output: `GatewayRequest` forcing fields + per-adapter translation in 3 adapters; `anthropic_service.stream_anthropic` gains one `tool_choice` param; NEW `forced_emit.py` substrate; the D-14 guard test GREEN.

## D-14 / G-5 ROUTING DECISION (stated explicitly per the phase mandate)
The forcing seam lands as a **tightly-scoped additive gateway seam, NOT an `anthropic_service.py` adapter extraction.** Evidence (RESEARCH §6, whole-file inspection): `anthropic_service.py` is 344 lines, single public function, already wrapped by the Phase 092.5 `provider_gateway/anthropic.py` adapter; all 3 native paths already route through one `open_stream(GatewayRequest)` build site; `GatewayRequest` already carries `tool_choice`. An extraction would be churn-for-churn's-sake and risks the byte-identical-Deep RED LINE. The G-5 "adapter pattern audit due" debt is satisfied **in spirit** by confirming the adapter boundary holds the new forcing logic (it does) + the D-14 guard test. `anthropic_service.py` grows by exactly one parameter. The shared-path-untouched + guard-test invariant is non-negotiable.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-CONTEXT.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-PATTERNS.md

<interfaces>
<!-- Contracts extracted from the codebase. Use directly. -->

From backend/app/services/provider_gateway/dispatcher.py (:41-72 — the envelope to extend; :75 open_stream):
```python
@dataclass
class GatewayRequest:
    messages: list[dict]
    model: str
    active_provider_name: str
    tools: list[dict] | None = None
    system_prompt: str = ""
    force_no_tools: bool = False
    max_tokens: int | None = None
    api_key: str = ""
    user_settings: "UserEffectiveSettings | None" = None
    tool_choice: str = "auto"          # <-- forcing rides BESIDE this; ADD force_tool_name + strict_schema
async def open_stream(provider, request) -> tuple[AsyncIterator[GatewayEvent], CallingMode]:
    if provider == "anthropic": return open_anthropic_stream(request), CallingMode.NATIVE
    elif provider == "google":  return open_google_stream(request), CallingMode.NATIVE
    else:                        return open_openai_compat_stream(request)   # surfaces its own calling_mode
```

From backend/app/services/anthropic_service.py (:177 — the only forcing-relevant line; NO thinking param anywhere, grep=0 → thinking OFF):
```python
tool_choice = {"type": "none"} if force_no_tools else {"type": "auto"}   # :177
```

From backend/app/services/openai_service.py (:1352 — the auto branch a forced branch slots beside):
```python
if tool_choice == "auto":
    if calling_mode == CallingMode.NATIVE:
        kwargs["tools"] = tools_override if tools_override is not None else get_tools(user_settings)
        kwargs["tool_choice"] = "auto"
```

From backend/app/services/google_service.py (:406 — config_kwargs to inject tool_config into):
```python
config_kwargs: dict[str, Any] = {
    "max_output_tokens": max_tokens,
    "automatic_function_calling": types.AutomaticFunctionCallingConfig(disable=True),
}
if google_tools: config_kwargs["tools"] = google_tools
```

From backend/app/services/agent_loop.py (:1781-1814 — the STRUCTURED-only recovery to MIRROR onto NATIVE; do NOT edit agent_loop.py):
```python
if calling_mode == CallingMode.STRUCTURED:
    structured_calls = parse_structured_tool_calls(full_content)   # reuse THIS helper in forced_emit
    if structured_calls: ...
```

From backend/app/services/template_render_service.py:
```python
def is_truncated(stop_reason=None, finish_reason=None) -> bool: ...   # :253 — D-08 layer 4

From backend/app/config.py:410 — get_model_capability(model_id) -> ModelCapability  (Plan 01 added forced_emission/strict_json_schema)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GatewayRequest forcing fields + per-adapter tiered translation (D-05) + the D-14 guard test</name>
  <files>backend/app/services/provider_gateway/dispatcher.py, backend/app/services/provider_gateway/anthropic.py, backend/app/services/provider_gateway/google.py, backend/app/services/provider_gateway/openai_compat.py, backend/app/services/anthropic_service.py, backend/tests/unit/test_gateway_forcing.py</files>
  <read_first>
    - backend/app/services/provider_gateway/dispatcher.py:41-72 (GatewayRequest) — PATTERNS.md "tiered-forcing seam"
    - backend/app/services/provider_gateway/anthropic.py:48-73 (open_anthropic_stream) + anthropic_service.py:152-188 (stream_anthropic, the :177 seam, 0 thinking matches)
    - backend/app/services/provider_gateway/google.py + google_service.py:383-419 (config_kwargs)
    - backend/app/services/provider_gateway/openai_compat.py + openai_service.py:1352-1356 (the auto branch)
    - .planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md §2 (per-provider force mechanism table — copy the param shapes verbatim) + §6 (the D-14 guard test spec)
    - backend/tests/unit/test_provider_gateway_seam.py — the synthetic-chunk + open_stream contract test pattern
  </read_first>
  <behavior>
    - GatewayRequest carries force_tool_name (str|None=None) + strict_schema (bool=False); defaults preserve the current "auto" behavior byte-for-byte
    - anthropic adapter: force_tool_name set => stream_anthropic gets tool_choice={"type":"tool","name":<force_tool_name>}, thinking stays OFF; unset => :177 unchanged
    - google adapter: force_tool_name set => config_kwargs gains tool_config=ToolConfig(function_calling_config=FunctionCallingConfig(mode='ANY', allowed_function_names=[<force_tool_name>]))
    - openai_compat adapter: force_tool_name set => tool_choice={"type":"function","function":{"name":<force_tool_name>}} (or "required") + response_format json_schema strict when strict_schema; unset => the :1352 auto branch unchanged
    - The tier resolves via get_model_capability(model).get("forced_emission", False); a miss => no forcing (TIER-COERCE / SAFE)
    - GUARD (D-14): the shared chunk handler / SSE emitter contains NO `if provider ==` branch for forcing; a Deep tool_choice="auto" GatewayRequest is byte-identical pre/post; forcing provider X does not change provider Y's request
  </behavior>
  <action>
    (a) **dispatcher.py**: add `force_tool_name: str | None = None` and `strict_schema: bool = False` to `GatewayRequest` (additive — defaults preserve `tool_choice="auto"` behavior; the consumer that does not set them is byte-identical). Do NOT change `open_stream` routing.

    (b) **provider_gateway/anthropic.py**: in `open_anthropic_stream`, when `request.force_tool_name`, pass `tool_choice={"type":"tool","name":request.force_tool_name}` to `stream_anthropic`; otherwise pass `None` (preserve :177). Do NOT enable thinking anywhere (it is OFF today; TIER-FORCE-NOTHINK is the resting state). Add a one-line comment: "emit forces thinking OFF — forcing errors under extended thinking (D-05)".

    (c) **anthropic_service.py**: add exactly ONE param to `stream_anthropic`: `tool_choice: dict | None = None`. At :177, change to `tool_choice = tool_choice if tool_choice is not None else ({"type":"none"} if force_no_tools else {"type":"auto"})`. This is the ONLY change to this file (G-5 — additive, not an extraction).

    (d) **provider_gateway/google.py**: when `request.force_tool_name`, inject `tool_config=types.ToolConfig(function_calling_config=types.FunctionCallingConfig(mode='ANY', allowed_function_names=[request.force_tool_name]))` into the `config_kwargs` passed to `stream_google` (keep `automatic_function_calling` disabled — RESEARCH §2). Import `types` from the same module google_service.py uses.

    (e) **provider_gateway/openai_compat.py**: when `request.force_tool_name`, set the OpenAI named `tool_choice={"type":"function","function":{"name":request.force_tool_name}}` and, when `request.strict_schema`, inject `response_format={"type":"json_schema","json_schema":{...,"strict":true}}` (additionalProperties:false + all keys required — the EmitFieldMap schema). When `force_tool_name` is None, the existing auto path is byte-identical. Route this through the existing `create_adaptive_streaming_chat` kwargs (the `:1352` branch shape) without touching the shared chunk handler.

    (f) **test_gateway_forcing.py** (un-mark the Task-1 stubs to GREEN): write `test_no_provider_branch_in_shared_path` (the D-14 guard — grep agent_loop.py's shared `_on_chunk`/chunk handler for `if provider ==` and assert the only matches are the EXISTING usage/`<think>` ones, NOT forcing); `test_deep_request_byte_identical` (a GatewayRequest with force_tool_name=None + tool_choice="auto" produces the same downstream request as before — assert defaults); one per-adapter translation test (mock each adapter's translate step, assert the §2 param shape for a given force_tool_name); `test_force_default_safe_on_registry_miss` (a model id absent from MODEL_CAPABILITIES resolves forced_emission=False). Reuse `from app.services.provider_gateway import CallingMode, GatewayRequest, open_stream` + the synthetic-chunk factory from test_provider_gateway_seam.py.
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_gateway_forcing.py -x -q && python -c "from app.services.provider_gateway.dispatcher import GatewayRequest; r=GatewayRequest(messages=[],model='m',active_provider_name='openai'); print(r.force_tool_name is None and r.strict_schema is False and r.tool_choice=='auto')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_gateway_forcing.py -x -q` passes (all stubs un-marked GREEN)
    - `pytest tests/unit/test_gateway_forcing.py::test_no_provider_branch_in_shared_path -x` exits 0
    - `grep -c "force_tool_name" backend/app/services/provider_gateway/dispatcher.py` >= 1
    - `git diff --stat backend/app/services/anthropic_service.py` shows ONLY the one `tool_choice` param + the :177 line (no extraction); `grep -c "thinking\|budget_tokens\|extended_thinking" backend/app/services/anthropic_service.py` == 0
    - `grep -c "function_calling_config\|FunctionCallingConfig" backend/app/services/provider_gateway/google.py` >= 1
    - The shared chunk handler in agent_loop.py is NOT in this plan's `files_modified` (untouched); Deep byte-identical test GREEN
  </acceptance_criteria>
  <done>GatewayRequest carries forcing fields; the 3 adapters translate per §2; anthropic_service.py grows by one param; the D-14 guard + Deep-byte-identical tests are GREEN; shared path untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: forced_emit substrate — sealed single forced shot + D-06 NATIVE narrated-JSON recovery + truncation guard (D-08 layers 1-4)</name>
  <files>backend/app/services/forced_emit.py, backend/tests/unit/test_forced_emit.py</files>
  <read_first>
    - backend/app/services/task_service.py:178-308 (_stream_one_iteration — the single gateway-driven call to MODEL after, NOT the open loop) — PATTERNS.md "forced_emit.py"
    - backend/app/services/agent_loop.py:1781-1814 (parse_structured_tool_calls usage — the recovery to mirror onto NATIVE) — do NOT edit agent_loop.py
    - backend/app/services/template_render_service.py:253 (is_truncated) + the EmitFieldMap model from Plan 01
    - .planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md §1 (the forced_emit recovery note) + §3 (chunking) + Pitfall 3 + Pitfall 5
  </read_first>
  <behavior>
    - forced_emit(...) builds a GatewayRequest with force_tool_name=<emitter> (NOT tool_choice="auto") for a TIER-FORCE model; for a TIER-COERCE model it sets tool_choice="auto" + an injected "you MUST call <emitter> NOW" directive
    - It drives open_stream ONCE (a single sealed shot) — it MUST NOT call run_task_sub_agent or run_agent_loop (Pitfall 5 / D-01)
    - On the NATIVE path, when tool_calls is empty but the model narrated a fenced JSON object: parse it back via parse_structured_tool_calls and validate against EmitFieldMap; if it validates, treat it as the emission (recovered_from_narration=True); else fail honestly (return a structured failure, never a silent drop) — D-06
    - is_truncated(stop_reason/finish_reason) rejects a max_tokens/length half-object BEFORE acceptance (D-08 layer 4)
    - For a TIER-COERCE model, hard-validate finish_reason/tool_calls; narration that does not recover => honest failure state (a) model_failed_to_emit (never a code fallback) — D-05 TIER-COERCE / Pitfall 6
    - Returns a structured result: {emitted: EmitFieldMap|None, tier, provider, forced, recovered_from_narration, truncated, failure: <None|model_failed_to_emit>}
  </behavior>
  <action>
    Create `backend/app/services/forced_emit.py` composing the two analogs (PATTERNS.md flags this as net-new composition):
    - Resolve the tier: `cap = get_model_capability(model)`; `forced = cap.get("forced_emission", False)` (default-SAFE — a miss is coerce); `strict = cap.get("strict_json_schema", False)`.
    - Build a `GatewayRequest` mirroring `_stream_one_iteration`'s build but with `force_tool_name=<emitter>` + `strict_schema=strict` when `forced`; else `tool_choice="auto"` and prepend the explicit directive to the system prompt (TIER-COERCE). NEVER `tool_choice="auto"` on a forced provider.
    - Drive `open_stream(provider, req)` ONCE; drain the stream (model `_stream_one_iteration`'s drain — reuse its drain shape, do NOT call the bounded loop). Capture `(content, tool_calls, finish_reason/stop_reason, calling_mode)`.
    - D-08 layer 4: if `is_truncated(...)` → reject (truncated=True, failure="model_failed_to_emit").
    - D-06 NATIVE recovery: if `not tool_calls` and `calling_mode == CallingMode.NATIVE` and `content.strip()`: `recovered = parse_structured_tool_calls(content)` (the SAME helper agent_loop.py:1782 uses); if it yields the emitter's args → validate with `EmitFieldMap.model_validate(...)`; set `recovered_from_narration=True`. Else `failure="model_failed_to_emit"` (honest — NEVER a silent drop, NEVER accept the prose as the artifact).
    - Validate the accepted emission against `EmitFieldMap` (D-08 layer 4). Implement the §3 chunking hook: a helper `_chunk_emit(...)` that splits a large `rows` collection into N forced calls when the row count × per-row token estimate exceeds the model's `max_output_tokens` (config registry) and concatenates `rows` — leave it called-but-trivial for the v1 single-shot path (full chunking exercised live; the unit test asserts the split boundary math).
    - This module owns D-08 layers 1-4; layers 5-6 (retry / honest-fail surface) are the executor's (Plan 03) via `_retry_suffix`/`_surface_failure_message`.
    Un-mark `test_native_recovery_or_honest_fail` + `test_truncation_rejected` in test_forced_emit.py to GREEN (drive synthetic streams: one with a tool_call, one with only narrated fenced JSON on NATIVE → recovered, one with un-parseable narration → honest fail, one truncated → rejected).
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_forced_emit.py -x -q && python -c "import ast,inspect; src=open('app/services/forced_emit.py').read(); assert 'run_task_sub_agent' not in src and 'run_agent_loop' not in src, 'emit must not call the open loop'; print('no open-loop call OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_forced_emit.py -x -q` passes
    - `grep -c "def forced_emit" backend/app/services/forced_emit.py` >= 1
    - `grep -c "run_task_sub_agent\|run_agent_loop" backend/app/services/forced_emit.py` == 0 (Pitfall 5 — never the open loop)
    - `grep -c "parse_structured_tool_calls" backend/app/services/forced_emit.py` >= 1 (D-06 reuses the existing recovery helper)
    - `grep -c "is_truncated" backend/app/services/forced_emit.py` >= 1 (D-08 layer 4)
    - `grep -c 'get("forced_emission", False)' backend/app/services/forced_emit.py` >= 1 (default-SAFE)
    - `git diff --stat backend/app/services/agent_loop.py` is EMPTY (the Deep loop is untouched — D-06 lands in the new substrate, not here)
  </acceptance_criteria>
  <done>forced_emit runs a sealed single forced shot, recovers narrated NATIVE emissions or fails honestly, rejects truncation, never touches the open loop; tests GREEN; agent_loop.py byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway adapter → provider API | a forcing param could be mistranslated and silently downgrade forcing |
| narrated model output → recovered emission | untrusted model text parsed back as a structured object |
| forced_emit → shared chunk/SSE path | forcing logic must NOT leak into the path Deep shares |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101.1-02-01 | Tampering | provider-branch leakage into the shared chunk/SSE path | mitigate | the D-14 guard test asserts no `if provider ==` forcing branch in the shared handler; forcing lives ONLY in the 3 adapters; Deep byte-identical test |
| T-101.1-02-02 | Spoofing | narrated prose masquerading as a valid emission (GAP-D) | mitigate | D-06 recovery validates the recovered object against `EmitFieldMap.model_validate` before acceptance; an un-parseable/invalid narration → honest failure, never accepted as the artifact |
| T-101.1-02-03 | Elevation of Privilege | a registry/case miss wrongly forcing an unforceable provider | mitigate | `get_model_capability(id).get("forced_emission", False)` default-SAFE (Pitfall 7) — a miss is TIER-COERCE; Anthropic thinking stays OFF (forcing errors under thinking) |
| T-101.1-02-04 | Denial of Service | a truncated half-object accepted as a valid (empty) emission | mitigate | `is_truncated` rejects `stop_reason=max_tokens`/`finish_reason=length` before acceptance (D-08 layer 4); §3 chunking sizes the budget |
| T-101.1-02-05 | Tampering | TIER-COERCE false-confidence (Kimi/Moonshot narration accepted) | mitigate | hard-validate `finish_reason`/`tool_calls`; narration that does not recover → honest failure; NEVER a model-written-code fallback (D-03/D-05) |
</threat_model>

<verification>
- `pytest tests/unit/test_gateway_forcing.py tests/unit/test_forced_emit.py -x -q` passes
- D-14 guard GREEN: no forcing provider-branch in the shared path; Deep `tool_choice="auto"` byte-identical
- `forced_emit` never references `run_task_sub_agent`/`run_agent_loop` (grep == 0)
- `agent_loop.py` is byte-identical (`git diff --stat` empty)
- `anthropic_service.py` grew by exactly one param (G-5 additive seam, not an extraction)
- Net-new failures = 0 against the SEED-056 baseline (baseline checkout, not raw count)
</verification>

<success_criteria>
- Capability-tiered forcing translates per provider at the gateway boundary only (D-05); default-SAFE on a miss (D-15)
- Anthropic forcing runs thinking-OFF (TIER-FORCE-NOTHINK); Gemini uses mode=ANY; OpenAI-compat uses named tool_choice + optional strict response_format
- NATIVE narrated-JSON recovery parses-back-or-fails-honestly (D-06); truncation rejected (D-08 layer 4)
- The emit shot is a sealed single call, never the open agent loop (D-01 / Pitfall 5)
- D-14 guard + Deep byte-identical tests GREEN; shared path untouched; G-5 satisfied by the additive seam
</success_criteria>

<output>
After completion, create `.planning/phases/101.1-guaranteed-emission-layer/101.1-02-SUMMARY.md`
</output>
