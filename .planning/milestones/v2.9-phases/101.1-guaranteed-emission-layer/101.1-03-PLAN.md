---
phase: 101.1-guaranteed-emission-layer
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - backend/app/services/harness/phase_types.py
  - backend/app/services/harness_engine.py
  - backend/app/services/harness/emitters.py
  - backend/tests/unit/test_llm_emit_executor.py
autonomous: true
requirements: [TMPL-02, TMPL-03]
user_setup: []

must_haves:
  truths:
    - "A workflow phase of phase_type=llm_emit dispatches to _exec_llm_emit (the 6th entry in PHASE_TYPE_REGISTRY)"
    - "_exec_llm_emit runs a sealed forced shot via forced_emit — it never calls run_task_sub_agent / the open agent loop"
    - "The bound library template AssetRef is resolved server-side and injected into the emit args at phase-build time (model never selects the template) — GAP-B"
    - "All 6 no-fail layers are owned by the engine: isolation, tiered forcing, NATIVE recovery, schema validation, bounded retry, honest failure"
    - "render_template is the EMITTER_REGISTRY post_processor: the validated field-map is rendered via the hardened _handle_render_template (one render code path)"
    - "Every emit transition writes an INSERT-only harness_audit receipt keyed to run_id + definition@version"
    - "Deep stays byte-identical: render_template is reachable ONLY from a declaring llm_emit phase, never from get_tools()"
  artifacts:
    - path: "backend/app/services/harness/phase_types.py"
      provides: "_exec_llm_emit executor + PHASE_TYPE_REGISTRY_ENTRIES 6th line + GAP-B AssetRef inject"
      contains: "_exec_llm_emit"
    - path: "backend/app/services/harness/emitters.py"
      provides: "render_template post_processor wired to the hardened render dispatch"
      contains: "post_processor"
  key_links:
    - from: "backend/app/services/harness/phase_types.py"
      to: "backend/app/services/forced_emit.py"
      via: "_exec_llm_emit calls forced_emit (NOT run_task_sub_agent)"
      pattern: "forced_emit"
    - from: "backend/app/services/harness/phase_types.py"
      to: "backend/app/services/template_asset_service.py"
      via: "resolve_template_source(asset_ref=definition.assets[0]) injects the bound template at arg-build"
      pattern: "resolve_template_source"
    - from: "backend/app/services/harness/phase_types.py"
      to: "backend/app/db/workflows.py"
      via: "write_audit per emit transition (emit_forced/emit_validated/emit_failed/...)"
      pattern: "write_audit"
    - from: "backend/app/services/harness/emitters.py"
      to: "backend/app/services/tool_dispatcher.py"
      via: "render_template post_processor re-dispatches the hardened _handle_render_template"
      pattern: "_handle_render_template"
---

<objective>
Build `_exec_llm_emit` — the 6th harness executor that composes the Plan 01/02 substrate into a working forced-emit phase: it resolves the bound template (GAP-B), runs the sealed forced shot (Plan 02 `forced_emit`), validates via the citation gate, renders via the hardened `_handle_render_template` (the EMITTER_REGISTRY post_processor), enforces the integrity gate, runs bounded retry on failure, surfaces honest failures, and writes an INSERT-only audit receipt at every transition. This is where the layer becomes a usable phase type. Deep stays byte-identical — `render_template` remains reachable ONLY from a declaring `llm_emit` phase.

Purpose: turns the gateway forcing + flat field-map + emitter registry into the actual `llm_emit` phase that closes GAP-A (sealed forced shot, not the open loop) and GAP-B (server-side AssetRef inject). Owns the D-08 6-layer no-fail ladder and the D-12 audit receipt. Implements D-01, D-04, D-08 (all 6 layers), D-10, D-12.
Output: `_exec_llm_emit` + the 6th `PHASE_TYPE_REGISTRY_ENTRIES` line; the `render_template` post_processor wired to the hardened render handler; per-transition audit receipts; executor tests GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-CONTEXT.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-PATTERNS.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-01-SUMMARY.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-02-SUMMARY.md

<interfaces>
<!-- Contracts extracted from the codebase + Plans 01/02. Use directly. -->

From backend/app/services/harness/phase_types.py (the executor SHAPE + the tool-schema injection to reuse):
```python
def _phase_tools_override(whitelist, model, user_settings) -> list[dict]:   # :228 — reuse VERBATIM
    cand = get_tools(user_settings)
    if "render_template" in (whitelist or frozenset()):
        cand = cand + [RENDER_TEMPLATE_TOOL]
    return apply_tool_budget(cand, model, whitelist)

async def _exec_llm_agent(phase, accumulated_outputs: dict, ctx) -> dict:    # :390 — the analog
    whitelist = frozenset(_effective_tools(phase))
    model = _effective_model(phase, ctx)
    tools_override = _phase_tools_override(whitelist, model, ...)
    system_prompt = phase.config.prompt + _skill_block(phase, ctx) + _retry_suffix(ctx)
    result = await run_task_sub_agent(...)        # THE DIVERGENCE: emit MUST NOT call this
    return {"text": ..., "source_refs": ..., "citations": ...}

PHASE_TYPE_REGISTRY_ENTRIES: dict = {              # :715 — add the 6th line
    "programmatic": _exec_programmatic, "llm_single": _exec_llm_single,
    "llm_agent": _exec_llm_agent, "llm_batch_agents": _exec_llm_batch_agents,
    "llm_human_input": _exec_llm_human_input,
}
# ctx carries: ctx.run_id (workflow_run_id, threaded :322), ctx.current_user (run owner),
#              ctx.retry_feedback (D-08 layer 5 consumer), ctx.pool, ctx.supabase
```

From backend/app/services/template_asset_service.py (D-10 byte resolution — returns dict, never raises):
```python
async def resolve_template_source(*, pool, supabase, thread_id, user_id, asset_ref=None) -> dict: ...  # :81
# returns {bytes, filename, provenance, mime, error}
```

From backend/app/services/tool_dispatcher.py (the hardened render handler — reuse VERBATIM; CR-01/WR-02/WR-03/WR-04 already fixed):
```python
async def _handle_render_template(args: dict, ctx: ToolContext) -> dict: ...   # :1515
# carries _safe_out_filename allow-list (:1499), truncation guard, citation gate, integrity gate, sealed-sandbox plumbing
```

From backend/app/db/workflows.py: write_audit(pool, run_id, *, user_id, event_type, metadata)   # :478 (Plan 01 extended _AUDIT_EVENT_TYPES to 16)
From backend/app/services/forced_emit.py (Plan 02): forced_emit(...) -> dict {emitted, tier, provider, forced, recovered_from_narration, truncated, failure}
From backend/app/services/template_render_service.py: check_coverage(...) :182, emit_field_map_to_legacy(...) (Plan 01), EmitFieldMap (Plan 01)
From backend/app/services/harness_engine.py: _surface_failure_message :454 (RC-4 honest fail — harness-only branch, never the shared terminal path)
From backend/app/services/harness/emitters.py (Plan 01): EMITTER_REGISTRY[name] -> {schema_builder, post_processor}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _exec_llm_emit executor — sealed forced shot + GAP-B inject + D-08 6-layer ladder, registered as the 6th phase type</name>
  <files>backend/app/services/harness/phase_types.py, backend/app/services/harness_engine.py, backend/tests/unit/test_llm_emit_executor.py</files>
  <read_first>
    - backend/app/services/harness/phase_types.py:228-245 (_phase_tools_override) + :390-464 (_exec_llm_agent) + :715 (PHASE_TYPE_REGISTRY_ENTRIES) — PATTERNS.md "_exec_llm_emit" (CRITICAL note: MUST NOT call run_task_sub_agent)
    - backend/app/services/template_asset_service.py:81-134 (resolve_template_source library branch) — PATTERNS.md "GAP-B server-side AssetRef injection"
    - backend/app/services/harness_engine.py:454 (_surface_failure_message) + :579 (_run_phase_with_gates retry-feedback) + :130 (_emit) — PATTERNS.md "Honest failure" + "Bounded retry"
    - backend/app/models/harness.py — WorkflowDefinition.assets / AssetRef (the GAP-B source; pick the asset of kind=="template")
    - .planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md §4 (audit receipt metadata shape) + Pitfall 5 (the open-loop guard)
  </read_first>
  <behavior>
    - phase_type=llm_emit dispatches to _exec_llm_emit (6th PHASE_TYPE_REGISTRY entry)
    - GAP-B: at arg-build, resolve the bound template via resolve_template_source(asset_ref=<the definition's assets[] entry of kind=="template">) and inject the resolved asset into the forced tool args — the model NEVER selects the template; a definition with no template AND no ephemeral upload => failure state (e) no_template_bound
    - Layer 1 isolation: the emission is produced by forced_emit (sealed single shot) — _exec_llm_emit MUST NOT call run_task_sub_agent / run_agent_loop
    - Layer 2 forcing + Layer 3 NATIVE recovery: inherited from forced_emit (Plan 02)
    - Layer 4 schema validation + citation gate: the emitted EmitFieldMap is normalized (emit_field_map_to_legacy) then run through check_coverage BEFORE render; uncited/invented => failure state (b) citation_gate_rejected (rejected before touching the sandbox)
    - render: EMITTER_REGISTRY[emitter].post_processor renders the validated map via the hardened _handle_render_template; render error => state (c) render_failed; integrity fail => state (d) integrity_failed (non-opening file never persisted; cited map preserved as fallback)
    - Layer 5 bounded retry <=3: a recoverable failure re-prompts via _retry_suffix(ctx) with the gate verdict (reuse _run_phase_with_gates, no new loop)
    - Layer 6 honest failure: every terminal failure persists a real reason via _surface_failure_message (harness-only branch — never the shared Deep+harness terminal path); never an empty "done"
    - Audit (D-12): write_audit once per transition (emit_forced -> emit_validated/emit_rejected/emit_failed/emit_integrity_failed) keyed to ctx.run_id + the definition@version, owner-scoped ctx.current_user
  </behavior>
  <action>
    Add `async def _exec_llm_emit(phase, accumulated_outputs: dict, ctx) -> dict` to phase_types.py, modeled on `_exec_llm_agent` (PATTERNS.md adaptation note). Reuse from the analog: `_phase_tools_override` (so the model SEES `render_template` on the declaring phase — layer-1 schema visibility), `_effective_model`, `_skill_block`, `_retry_suffix`, and the `{"text", ..., "source_refs", "citations"}` return shape. THE DIVERGENCE (D-01 / Pitfall 5): call `forced_emit(...)` (Plan 02), NEVER `run_task_sub_agent`.

    Sequence inside the executor:
    1. GAP-B inject (D-10): find the `definition.assets` entry with `kind=="template"`; call `resolve_template_source(pool=ctx.pool, supabase=ctx.supabase, thread_id=..., user_id=ctx.current_user, asset_ref=<that AssetRef>)`. If no template asset AND no ephemeral upload resolves => write `emit_failed` receipt + `_surface_failure_message` with `failure="no_template_bound"` (state e), return failed. Inject the resolved template into the forced emit args so the model never selects it.
    2. emit_forced receipt (write_audit) => call `forced_emit(emitter=phase.config.emitter, model=_effective_model(...), system_prompt=phase.config.prompt + _skill_block(...) + _retry_suffix(ctx), schema=EMITTER_REGISTRY[emitter].schema_builder(...), evidence=<retrieved chunks from accumulated_outputs>, ...)`. If `result["recovered_from_narration"]` => also write `emit_recovered`. If `result["failure"]` (model_failed_to_emit, after Plan 02 recovery+truncation) => `emit_failed` receipt + honest fail (state a).
    3. citation gate (layer 4): normalize `result["emitted"]` via `emit_field_map_to_legacy`, run `check_coverage`. Uncited/invented => `emit_rejected` receipt + honest fail (state b), BEFORE render. Else `emit_validated` receipt.
    4. render (post_processor): `EMITTER_REGISTRY["render_template"].post_processor(validated_map, resolved_template, ctx)` => re-dispatches `_handle_render_template` (Task 2 wires it). Render error => `emit_failed` (state c). Integrity fail => `emit_integrity_failed` receipt + honest fail (state d), preserve the cited map as fallback, never persist a non-opening file. Success => `emit_rendered` receipt.
    5. retry (layer 5): a recoverable failure (b/c/d, model_failed_to_emit) re-prompts via the EXISTING `_run_phase_with_gates`/`_retry_suffix` <=3 — no new loop. Exhausted => honest fail.

    Build the audit `metadata` dict per RESEARCH §4 (definition_version, definition_id, phase_slug, emitter, tier, provider, model, forced, thinking=False, recovered_from_narration, retrieved_ids, field_map, gate_verdict, integrity_verdict, output_file{path, sha256 via stdlib hashlib, bytes}). Add the 6th line `"llm_emit": _exec_llm_emit` to `PHASE_TYPE_REGISTRY_ENTRIES` (:715). `register_all()` (:724) auto-updates the engine's `PHASE_TYPE_REGISTRY` (harness_engine.py:111) — no edit to harness_engine.py beyond confirming registration (the dict line lives in phase_types.py).

    Un-mark the 3 executor stubs in test_llm_emit_executor.py to GREEN: `test_emit_never_uses_agent_loop` (monkeypatch run_task_sub_agent to raise; assert _exec_llm_emit never triggers it while forced_emit IS called), `test_bound_assetref_injected` (resolve_template_source called with asset_ref=<the template AssetRef>, NOT None), `test_emit_writes_audit_receipt` (write_audit invoked with an emit_* event_type + the §4 metadata keys). Use the project's existing harness test fakes for ctx.
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_llm_emit_executor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_llm_emit_executor.py -x -q` passes
    - `python -c "from app.services.harness.phase_types import PHASE_TYPE_REGISTRY_ENTRIES; print('llm_emit' in PHASE_TYPE_REGISTRY_ENTRIES)"` prints True
    - `grep -c "def _exec_llm_emit" backend/app/services/harness/phase_types.py` == 1
    - The `_exec_llm_emit` body (the ~4000 chars after its def) contains NO `run_task_sub_agent` and NO `run_agent_loop` (Pitfall 5 / D-01) — grep the function body
    - `grep -c "resolve_template_source" backend/app/services/harness/phase_types.py` >= 1 (GAP-B inject)
    - `grep -c "write_audit" backend/app/services/harness/phase_types.py` >= 1 (D-12 receipt)
    - `grep -c "no_template_bound" backend/app/services/harness/phase_types.py` >= 1 (state e)
    - `git diff --stat backend/app/services/harness_engine.py` is empty OR shows only registration confirmation (the registry line lives in phase_types.py)
  </acceptance_criteria>
  <done>_exec_llm_emit dispatches forced shots (never the open loop), injects the bound template server-side, runs the 6-layer ladder, writes per-transition audit receipts; registered as the 6th phase type; tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the render_template EMITTER_REGISTRY post_processor to the hardened _handle_render_template</name>
  <files>backend/app/services/harness/emitters.py, backend/tests/unit/test_llm_emit_executor.py</files>
  <read_first>
    - backend/app/services/harness/emitters.py (Plan 01 — the render_template entry whose post_processor is currently None)
    - backend/app/services/tool_dispatcher.py:1515 (_handle_render_template) + :1499 (_safe_out_filename) + :1186 (_RENDER_DRIVER_SRC) — PATTERNS.md "Reuse the hardened render handler (do not re-implement)"
    - .planning/phases/101.1-guaranteed-emission-layer/101.1-RESEARCH.md Don't-Hand-Roll ("Render + integrity + persist => re-dispatch the hardened handler") + Pitfall 1/2 (SSTI + out_filename)
  </read_first>
  <behavior>
    - EMITTER_REGISTRY["render_template"].post_processor is a callable (no longer None)
    - It re-dispatches the EXISTING hardened _handle_render_template with the validated field-map + the server-resolved template (A2 — one render code path; CR-01/WR-02/WR-03/WR-04 hardening inherited)
    - It does NOT re-implement the render command, the citation gate, the integrity gate, or the sandbox plumbing (no second render path)
    - No docxtpl import at module top (Pitfall 4 — render is sandbox-only)
  </behavior>
  <action>
    In `harness/emitters.py`, replace the `render_template` entry's `post_processor=None` with `post_processor=_render_template_post` where `_render_template_post(validated_map, resolved_template, ctx)` builds the `_handle_render_template` args (the field-map as the legacy-normalized dict, the provenance-routed engine via the resolved template's `provenance`, the safe out_filename — let `_handle_render_template`'s own `_safe_out_filename` coerce it) and awaits `_handle_render_template(args, ctx)`, returning its `{status, output_file, field_map, ...}`. Import `_handle_render_template` lazily inside the function (avoid an import cycle: emitters -> tool_dispatcher -> ...). Add NO `import docxtpl` at module top. Do NOT re-implement any gate — the handler already carries the truncation guard, citation gate, integrity gate, and the sealed-sandbox plumbing.

    Un-mark `test_render_post_reuses_handler` in test_llm_emit_executor.py to GREEN: monkeypatch `_handle_render_template` to a recorder, assert the post_processor calls it exactly once with the validated field-map + the resolved-template engine, and returns its verdict. Add `test_emitter_post_no_top_level_docxtpl` asserting `grep` finds no `import docxtpl` at emitters.py module top (Pitfall 4).
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_llm_emit_executor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_llm_emit_executor.py -x -q` passes
    - `python -c "from app.services.harness.emitters import EMITTER_REGISTRY; print(EMITTER_REGISTRY['render_template'].post_processor is not None)"` prints True
    - `grep -c "_handle_render_template" backend/app/services/harness/emitters.py` >= 1 (re-dispatch, not re-implement)
    - `grep -c "^import docxtpl\|^from docxtpl" backend/app/services/harness/emitters.py` == 0 (Pitfall 4)
    - emitters.py does NOT contain a second `SandboxedEnvironment` / `DocxTemplate.render` call (no second render path)
  </acceptance_criteria>
  <done>The render_template emitter post_processor re-dispatches the hardened handler (one render code path); no second render path; no module-top docxtpl; tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow definition assets[] → render | a published definition's bound template AssetRef crosses into render |
| validated field-map → sealed sandbox | the cited DATA crosses into the render command |
| emit transition → audit row | every transition writes an owner-scoped immutable receipt |
| declaring llm_emit phase → render_template tool | the tool must be unreachable outside a declaring phase (Deep byte-identical) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101.1-03-01 | Tampering | SSTI via uploaded template (model-written code on the deliverable) | mitigate | emit produces DATA only (D-02 — no model-written code touches the deliverable on any path); the post_processor re-dispatches the hardened handler whose `select_engine` hard-asserts `engine!="docxtpl"` for untrusted uploads; NO docxtpl import in backend/app/** (Pitfall 1/4); the model never selects the template (D-10) |
| T-101.1-03-02 | Tampering | `out_filename` shell injection | mitigate | reuse `_handle_render_template`'s `_safe_out_filename` allow-list + `shlex.quote` (101-06 CR-01) — never re-implement the render command (one hardened path) |
| T-101.1-03-03 | Repudiation | emit transition not auditable | mitigate | write_audit once per transition (INSERT-only RLS, owner-scoped ctx.current_user); the D-12 receipt names which gate refused |
| T-101.1-03-04 | Information Disclosure | cross-user template/audit access | mitigate | resolve_template_source is user-scoped on created_by (404-not-403); write_audit is owner-scoped + RLS backstop |
| T-101.1-03-05 | Elevation of Privilege | render_template reachable outside a declaring phase (Deep regression) | mitigate | `_phase_tools_override` injects RENDER_TEMPLATE_TOOL ONLY when whitelisted on the declaring phase; `get_tools()` still omits it (Deep byte-identical); the dispatch backstop refuses a hallucinated name |
| T-101.1-03-06 | Spoofing | a non-opening file delivered as "done" | mitigate | integrity gate (state d) never persists a non-opening file; the cited field-map is preserved as fallback; honest failure receipt (Pitfall 4 / TMPL-03) |
</threat_model>

<verification>
- `pytest tests/unit/test_llm_emit_executor.py -x -q` passes
- `llm_emit` is the 6th PHASE_TYPE_REGISTRY entry; `_exec_llm_emit` never calls the open loop (grep == 0 in the function body)
- GAP-B proven: resolve_template_source called with the bound AssetRef (not None)
- All 5 failure states (a-e) write an honest receipt + surface a real reason (never empty "done")
- The render post_processor re-dispatches the hardened handler (one render path; no second SandboxedEnvironment)
- Deep byte-identical: `get_tools()` still omits render_template; the only injection is the whitelisted-phase override
- Net-new failures = 0 against the SEED-056 baseline (baseline checkout, not raw count)
</verification>

<success_criteria>
- `_exec_llm_emit` is the 6th harness executor; dispatches a sealed forced shot, never the open agent loop (D-01)
- The bound library template AssetRef is resolved server-side + injected at phase-build (GAP-B / D-10); no-template => honest state (e)
- The 6 no-fail layers are engine-owned (D-08); 5 distinguishable failure states each write a receipt
- render rides the hardened `_handle_render_template` via the EMITTER_REGISTRY post_processor (D-04; one render path)
- Every transition writes an INSERT-only audit receipt keyed to run_id + definition@version (D-12)
- Deep stays byte-identical (red line)
</success_criteria>

<output>
After completion, create `.planning/phases/101.1-guaranteed-emission-layer/101.1-03-SUMMARY.md`
</output>
