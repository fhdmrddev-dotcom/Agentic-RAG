---
phase: 101.1-guaranteed-emission-layer
plan: 09
subsystem: ui
tags: [workspace-panel, sse-demux, blob-download, react, fastapi, zustand, harness-run-honesty]

# Dependency graph
requires:
  - phase: 101.1-04
    provides: "PhaseCard renders emitSubStep/emitFailure (SUBSTEP_META + EmitFailure taxonomy) + the phase_substep backend wire event via _emit_phase_substep — this plan populates those fields from the wire"
  - phase: 101.1-08
    provides: "snapshot 'no such key' degrades (200) not 503 — the gap-4 backend half that keeps the terminal refetch from 503ing on a GC'd buffer"
  - phase: 100
    provides: "ephemeral template upload + the workspace FilesSection panel + the OutputFileCard the Download control fronts"
provides:
  - "downloadWorkspaceFile(threadId, fileId, filename) — the 067.3 Bearer-blob pattern for an inline binary workspace deliverable"
  - "GET /threads/{id}/workspace/files/{file_id}/raw — exact-bytes attachment route (the /content route str-decode corrupts a binary docx)"
  - "the phase_substep SSE demux — onPhaseSubstep callback + setPhaseEmitSubstepForThread store action (panel-only, writes phasesByThread, GAP-C / D-11)"
  - "a self-healing FILES panel — a successful harness terminal refetches workspace files so a just-persisted deliverable appears without F5 (gap 4 frontend)"
affects: [101.1-verify-work, run-legibility, workspace-panel, StreamsProvider, FilePreview]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "PANEL-09 demux discipline: a new SSE event populates phasesByThread ONLY (never bucketsBySurface) → the chat selector is untouched → Deep byte-identical, zero chat re-render"
    - "Closure-threadId isolation (Pitfall 6): each demux callback closes over the factory threadId so a background run never corrupts the viewed thread's rail/files"
    - "raw-bytes download route: bytes come from the pg pool (get_file_by_id → asyncpg raw bytes), NOT the supabase-py hex-string / the /content str-decode, so the inline round-trip is byte-exact"

key-files:
  created: []
  modified:
    - "backend/app/api/workspace.py — GET /files/{id}/raw exact-bytes route + _safe_download_filename header-injection guard"
    - "frontend/src/lib/api.ts — downloadWorkspaceFile helper + onPhaseSubstep callback + phase_substep demux branch"
    - "frontend/src/components/panel/FilePreview.tsx — onDownload wired at all three Fallback call sites"
    - "frontend/src/stores/streamsStore.ts — setPhaseEmitSubstepForThread action"
    - "frontend/src/providers/StreamsProvider.tsx — onPhaseSubstep demux + terminal workspace refetch"

key-decisions:
  - "GAP-C / gap-6 scope RATIFIED at the Task-4 checkpoint (operator: 'ship demux', 2026-06-11): KEEP the panel-only phase_substep demux in 101.1 — honor the CONTEXT D-11 promise rather than defer GAP-C's visible half to the run-legibility phase"
  - "raw-bytes download route reads bytes from the pg pool (asyncpg byte-exact), not the /content route (which base64-DECODES inline content to TEXT and corrupts a binary docx)"
  - "the demux writes phasesByThread EXCLUSIVELY (PANEL-09) — the chat surface (bucketsBySurface) is byte-untouched, so Deep stays byte-identical"

patterns-established:
  - "Pattern 1: additive panel-only SSE demux — add a StreamCallbacks field + an else-if branch (no return, cursor-advance fires) + a phasesByThread-only store action; mirror the 094 phase-lifecycle demux"
  - "Pattern 2: terminal self-heal — on a successful harness run_completed, best-effort refetch workspace files (a failed refetch never throws into the SSE consumer); harness-only event so Deep is untouched"

requirements-completed: [TMPL-02, TMPL-03]

# Metrics
duration: ~70min (across the prior executor's Tasks 1-3 + this continuation finalize)
completed: 2026-06-11
---

# Phase 101.1 Plan 09: Download + Emit Sub-Step Demux + Panel Self-Heal Summary

**A produced workspace deliverable is now downloadable (Bearer-authed exact bytes via a new /raw route), the emit sub-step rail is populated live from the phase_substep wire (GAP-C demux — panel-only, Deep byte-identical), and the FILES panel self-heals on a harness terminal without an F5.**

## Performance

- **Duration:** ~70 min (prior executor Tasks 1-3 + this continuation's checkpoint resolution + finalize)
- **Started:** 2026-06-11 (prior executor, Tasks 1-3 + Task-4 checkpoint)
- **Completed:** 2026-06-11T18:40Z (continuation finalize)
- **Tasks:** 4 (3 auto + 1 human-verify checkpoint, ratified)
- **Files modified:** 5 source + 3 test

## Accomplishments
- **gap 3 (Download dead text — UAT Test 2) CLOSED:** a produced docx/pptx/xlsx deliverable is reachable from the panel — the Download control fetches the real bytes (Bearer-authed) and saves the file. A new `GET /files/{id}/raw` route returns the EXACT bytes because the existing `/content` route str-decodes an inline binary and corrupts it.
- **gap 6 (GAP-C demux unwired — UAT Test 13) CLOSED + RATIFIED:** the backend already emits all 12 `phase_substep` events (Plan 04 `_emit_phase_substep`) but nothing populated `Phase.emitSubStep`/`emitFailure`. This plan wires the ADDITIVE, PANEL-ONLY demux (`phase_substep` → `setPhaseEmitSubstepForThread`) so the PhaseCard rail shows the discrete emit sub-steps (forcing → emitting → [recovering] → validating → rendering → validated) and failed-as-failed states live. The operator ratified keeping it in 101.1 ("ship demux").
- **gap 4 frontend half (panel staleness) CLOSED:** on a successful harness `run_completed`, the provider refetches workspace files for the owning thread so a just-persisted deliverable's file + phase status self-heal WITHOUT an F5 (the UAT log showed FILES fetched ~2 min before the emit's row existed and never refetched).
- **RED LINE held:** the demux writes `phasesByThread` only (never `bucketsBySurface`) → the chat selector is untouched, Deep byte-identical; closure threadId isolates background runs (Pitfall 6); one shared `phase_substep` event for every provider (no provider branch — D-14).

## Task Commits

Each task was committed atomically (TDD: RED → GREEN):

1. **Task 1: downloadWorkspaceFile helper + raw-bytes route + Download button (gap 3)** — `0ef88874` (test, RED) · `8b47e417` (feat, GREEN)
2. **Task 2: phase_substep demux → Phase.emitSubStep/emitFailure (gap 6, panel-only additive)** — `fdbb0502` (test, RED) · `991df25e` (feat: api.ts demux branch + store action) · `ad4828e2` (feat: StreamsProvider provider wiring)
3. **Task 3: Panel refetch on workflow terminal (gap 4 frontend half)** — folded into `ad4828e2` (the `onRunCompleted` "completed" refetch sits beside the gap-6 provider wiring in the same StreamsProvider commit; the RED test for it landed in `fdbb0502`)
4. **Task 4: Ratify the gap-6 scope (checkpoint:human-verify)** — operator resolved "ship demux" (2026-06-11); no revert, no source change.

**Plan metadata:** _this commit_ (docs: complete plan — SUMMARY + STATE)

_Note: Tasks 2 + 3 share the GREEN commit `ad4828e2` because the gap-6 demux callback and the gap-4 terminal refetch both live in the StreamsProvider callback factory and were committed together._

## Files Created/Modified
- `backend/app/api/workspace.py` — `GET /files/{id}/raw` returns the EXACT bytes of an inline binary workspace file as an attachment (bytes from the pg pool via `get_file_by_id` → asyncpg raw bytes, NOT the `/content` str-decode); `_safe_download_filename` neutralizes Content-Disposition header injection; SAME guards as `/content` (404 non-owner + expired + cross-thread, all collapsed — no existence leak).
- `frontend/src/lib/api.ts` — `downloadWorkspaceFile(threadId, fileId, filename)` (the 067.3 Bearer-blob pattern, reuses `DownloadError`); `onPhaseSubstep` added to `StreamCallbacks`; a `phase_substep` else-if demux branch (flat payload, NO return → cursor-advance still fires).
- `frontend/src/components/panel/FilePreview.tsx` — `onDownload` wired at all three Fallback call sites (inline-default / bucket-non-image / error) with a graceful inline error; the bucket-image preview unchanged.
- `frontend/src/stores/streamsStore.ts` — `setPhaseEmitSubstepForThread(threadId, slug, phaseIndex, patch)` patches the matching `phasesByThread` row's `emitSubStep`/`emitFailure` (match by slug, fallback phaseIndex) and writes `phasesByThread` exclusively.
- `frontend/src/providers/StreamsProvider.tsx` — the `onPhaseSubstep` demux callback (closure threadId, `phasesByThread` only) + the `onRunCompleted` "completed" workspace-files refetch (best-effort, harness-only so Deep is untouched).

## Decisions Made
- **GAP-C / gap-6 scope RATIFIED (Task-4 checkpoint, operator: "ship demux", 2026-06-11):** keep the panel-only `phase_substep` demux shipping in 101.1 — this honors the phase CONTEXT D-11 promise that emit sub-steps are visible in-scope. The alternative offered at the checkpoint (re-scope GAP-C's visible half to the dedicated run-legibility phase + revert Task 2) was DECLINED. No revert was performed; the wiring stands as implemented. The decision was safe to ratify because the demux is additive and panel-only (writes `phasesByThread`, never `bucketsBySurface`), so it does NOT pile onto the chat-surface G-5 hot files.
- raw-bytes download route reads from the pg pool (asyncpg returns `content_inline` as raw `bytes`, byte-exact) rather than the supabase-py client (hex string) or the `/content` route (str-decode corrupts a binary docx).

## Deviations from Plan

None - plan executed exactly as written. (The prior executor's task notes record no deviations; Tasks 2 and 3 sharing the `ad4828e2` GREEN commit is the natural consequence of both living in the same StreamsProvider callback factory, anticipated by the plan's Task-3 note "This may fold into the gap-6 demux wiring.")

## Issues Encountered
None. The gap-6 scope discrepancy flagged in the UAT (Plan 04 deferred the demux as a G-5 concern; the CONTEXT promised it in-scope) was the explicit subject of the Task-4 checkpoint and was resolved by operator ratification.

## User Setup Required
None - no external service configuration required. (The live download / sub-step rail / self-heal are exercised in the Plan-10 live re-verify; the sandbox-image rebuild from the prior plans still applies for new chats.)

## Next Phase Readiness
- Plan 09 closes gap 3 (download), gap 6 (GAP-C demux), and the gap-4 frontend half — all three of the remaining UI-reachability gaps from the 101.1 UAT.
- **TMPL-02 / TMPL-03 stay OPEN** until phase verification — the download / live sub-step rail / self-heal are wired but their end-to-end behavior is verified in the Plan-10 live re-verify (cross-provider UAT) + `/gsd:verify-work 101.1`.
- **Next:** the remaining gap plan (101.1-10 live re-verify: download a produced cited .docx from the panel; watch the emit sub-step rail populate live; confirm the FILES panel self-heals on completion without F5) then `/gsd:verify-work 101.1` + `/gsd:secure-phase 101.1`.

## SEED-056 Net-New-Failure Proof
- **Plan's exact touched test files (the 3 in `files_modified`):** `src/providers/StreamsProvider.test.tsx` + `src/lib/api.test.ts` + `src/components/panel/FilePreview.test.tsx` = **15 passed / 0 failed** (run in isolation).
- **Backend:** `tests/test_workspace_template.py` = **17 passed / 0 failed** (the workspace raw-route + template tests; `test_workspace_api.py` does not exist in this tree).
- **The 10 frontend failures seen on a broad substring `npm test -- StreamsProvider.test` come ONLY from the UNRELATED `src/__tests__/providers/streamsProvider.test.tsx`** (lowercase, in `__tests__/`), which plan 09 never touched (empty `git diff 16f5f837 HEAD` for that file). Proven PRE-EXISTING rot by **base-checkout**: with the 3 plan source files (`api.ts`, `StreamsProvider.tsx`, `streamsStore.ts`) reverted to the pre-plan base `16f5f837`, that file fails **10/41 IDENTICALLY** (same 10 failed / 31 passed at base AND HEAD), then restored clean to HEAD. **Net-new failures = 0.**

## Self-Check: PASSED
- Key files exist: `backend/app/api/workspace.py`, `frontend/src/lib/api.ts`, `frontend/src/components/panel/FilePreview.tsx`, `frontend/src/stores/streamsStore.ts`, `frontend/src/providers/StreamsProvider.tsx` (+ the 3 test files) — ALL FOUND.
- Commits exist in history: `0ef88874`, `8b47e417`, `fdbb0502`, `991df25e`, `ad4828e2` — ALL FOUND (`git log --oneline`).
- Targeted tests pass (15 frontend + 17 backend); net-new failures = 0.

---
*Phase: 101.1-guaranteed-emission-layer*
*Completed: 2026-06-11*
