---
phase: 101.1-guaranteed-emission-layer
plan: 10
type: execute
wave: 2
depends_on: [07, 08, 09]
gap_closure: true
autonomous: false
requirements: [TMPL-02, TMPL-03]
files_modified:
  - scripts/repair_dirty_workflow_phases.py
  - scripts/smoke_101_1_06.py

must_haves:
  truths:
    - "The 3 dirty workflow_phases rows (575e7345 / a7f415ad / 4ea9bc56) — runs whose workflow_runs.status='failed' but whose fill workflow_phases.status='active' forever — are repaired to 'failed'"
    - "After the Plan 07-09 fixes ship, DeepSeek v4-pro produces an openable cited .docx OR a VISIBLE honest failure naming the gate (never a silent 0-docx, never a phase stuck 'active')"
    - "The ephemeral run-split fill (UAT Test 4) persists its deliverable AND it is downloadable; blocked Tests 6-8 (pptx/xlsx fixtures) re-run"
  artifacts:
    - path: "scripts/repair_dirty_workflow_phases.py"
      provides: "one-off idempotent psycopg2 repair of the 3 dirty rows (active → failed)"
      contains: "workflow_phases"
  key_links:
    - from: "scripts/repair_dirty_workflow_phases.py"
      to: "live local DB (:54322)"
      via: "scoped UPDATE of the 3 named run_ids' active fill rows where the parent run is failed"
      pattern: "status = 'failed'"
---

<objective>
Repair the 3 dirty workflow_phases rows left by the silent run-crashes (a data fix the engine can't retroactively do), then re-verify the gap fixes LIVE using the smoke harness pattern. This is the close-the-loop plan: prove that after Plans 07-09, DeepSeek v4-pro no longer silently crashes (gap 1), the ephemeral fill persists + downloads (gaps 3+5), the emit sub-steps show on the rail (gap 6), and the blocked pptx/xlsx tests (6-8) run.

Purpose: the UAT diagnosed all 6 gaps with traceback-confirmed root causes; only a LIVE gates-all-green run proves the last mile (the 099/101.1-06 lesson: offline mocks drifted from live shapes on EVERY layer — a gated pipeline hides N-1 downstream bugs). The smoke harness scripts/smoke_101_1_06.py generalizes per provider/model (login → kickoff → receipt-poll → byte verification).

Output: a clean workflow_phases table (no stuck 'active' rows) + a live re-verification verdict feeding /gsd:verify-work 101.1.

OPERATOR-ROUTED ITEMS (noted here, NOT planned as tasks — they route to other phases): the template-requirements docs/UI guidance enhancement → Phase 103/104; the workflow-builder model-selection design gap → Phase 103/104; D-09 chunking unimplemented → deferred note with re-open trigger (Phase 104 flagship large registers, or the first real >50-row corpus). The weak-model cross-provider re-run is part of this plan's live re-verification task.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101.1-guaranteed-emission-layer/101.1-UAT.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-06-SUMMARY.md
@.planning/phases/101.1-guaranteed-emission-layer/101.1-CONTEXT.md

<interfaces>
<!-- Extracted from the codebase / UAT — use directly. -->

The 3 dirty rows (UAT Gaps section): runs 575e7345 (deepseek-v4-flash), a7f415ad (deepseek-v4-pro), 4ea9bc56 (gpt-5.4-mini ephemeral run-split). DB proof: each run's workflow_runs.status='failed' but its fill workflow_phases row status='active' permanently. The repair flips those active fill rows to 'failed'.

The smoke harness — scripts/smoke_101_1_06.py (101.1-06): orchestrator-driven (login → kickoff → receipt-poll → byte verification via python-docx re-open). It generalizes by changing the model/provider. The live success reference: run 4d035a65 (gpt-5.4-mini) produced /risk-register.docx (37,636 B, 36/36 cited 100%, table grown 5 rows, zero residual).

Live DB access: psycopg2 to local Supabase :54322 (the evidence-tools inventory). The seeded D-13 two-step UAT WorkflowDefinition is 00000000-…-0000000101a0 (gather llm_agent search_documents over folder 75755ec9 "Project Meridian — Risks" → fill llm_emit render_template, model=null). The library template AssetRef is bound.

Operator setup prerequisites (from the UAT Test 1 + STATE): migration 069 applied live; sandbox image agentic-rag-sandbox:101.1 set in backend/.env; the D-13 two-step fixture seeded; backend up (the user starts uvicorn themselves in a visible terminal — never run_in_background). UAT fixtures: uat-files/status-memo-runsplit-101uat.docx (the {{top_risk}} run-split fixture). pptx/xlsx fixtures for Tests 6-8 to be generated at re-run (table-deck.pptx / chart-book.xlsx exist from 101-01; regenerate if needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-off repair of the 3 dirty workflow_phases rows (data fix)</name>
  <files>scripts/repair_dirty_workflow_phases.py</files>
  <action>
    Write an idempotent psycopg2 repair script (clone the dotenv/psycopg2 shape of scripts/smoke_101_1_06.py / the seed fixtures — connect to local Supabase :54322 via the same env vars). The script:
    1. SELECTs the workflow_phases rows for the 3 named run_ids (575e7345, a7f415ad, 4ea9bc56 — match on the full run_id; the script should resolve the full UUIDs by prefix from workflow_runs if only the short prefixes are known) whose status='active' AND whose parent workflow_runs.status='failed'.
    2. Prints the rows it WILL change (dry-run preview), then — gated behind an explicit `--apply` flag (refuse without it, clean SystemExit + remedy, the seed-fixture safety convention) — UPDATEs those specific rows' status to 'failed'. SCOPED to exactly those 3 run_ids + the active-and-parent-failed predicate (never a blanket update). Idempotent: a second run finds 0 rows to change (already 'failed') and exits 0.
    3. Reads back the changed rows and asserts no 'active' fill row remains for those runs. NEVER touches any other run's rows; never deletes; never alters workflow_runs or harness_audit (the receipts are the audit trail and must be preserved).
    Do NOT use db push / db reset (apply via the script's direct connection only — the migration rule). Log identifier-only (run_id/phase_slug), never content.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && python scripts/repair_dirty_workflow_phases.py</automated>
  </verify>
  <done>The dry-run lists exactly the ≤3 stuck-active fill rows for the 3 failed runs; --apply flips them to 'failed' idempotently; a re-run finds 0 changes; no other table/row is touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live re-verification across the 6 gaps + the cross-provider render column</name>
  <what-built>The full gap-closure code (Plans 07-09): DeepSeek FORCE-NOTHINK + the honest-failure catch-all (gap 1), single-owner persist (gap 2), download + raw-bytes route (gap 3), terminal refetch + snapshot degrade (gap 4), binary-delta skip + distinct ephemeral output path (gap 5), the emit sub-step demux (gap 6) — plus the data repair (Task 1). Backend + frontend; Deep byte-identical; threads.py not grown.</what-built>
  <how-to-verify>
    Prerequisites (operator): backend up with SANDBOX_IMAGE=agentic-rag-sandbox:101.1; migration 069 applied; the D-13 two-step fixture seeded; a FRESH chat per run (cached sandbox sessions keep the old image until eviction). Run the smoke harness (scripts/smoke_101_1_06.py — change model/provider per run) OR drive the live UI; cross-check the DB via psycopg2 (:54322).

    1. **Gap 1 — DeepSeek v4-pro (the silent-crash pair):** kick off the seeded workflow on deepseek-v4-pro in a fresh chat. EXPECT: either an openable cited .docx (emit_forced → emit_validated → emit_rendered → workspace_files row; download + python-docx re-open clean) OR a VISIBLE honest failure naming the gate (emit_failed receipt + a surfaced assistant message + the phase flipped to failed). MUST NOT: silent run-crash, empty avatar, no message, phase stuck 'active'. Confirm via DB: no workflow_phases row left 'active' for a failed run; the harness_audit trail has the receipts.
    2. **Gap 2 — duplicate message:** on any honest-failure run, confirm EXACTLY ONE assistant failure message persists (messages table — one row, not two).
    3. **Gaps 3+5 — ephemeral run-split fill (UAT Test 4):** upload uat-files/status-memo-runsplit-101uat.docx ({{top_risk}} fractured across runs) to a fresh thread, run the ephemeral fill. EXPECT: it PERSISTS (no binary-delta crash, written to a DISTINCT path — the uploaded template is NOT overwritten), the file appears in FILES WITHOUT F5 (gap 4 refetch), and Download saves the real bytes (gap 3). Verify the saved .docx re-opens clean with residual [] (the fractured token coalesced+filled).
    4. **Gap 6 — emit sub-steps on the rail:** watch a live fill — the PhaseCard rail shows the discrete forcing → emitting → [recovering] → validating → rendering → validated sub-steps (not the static 'Running' pill); a failure renders failed-as-failed.
    5. **Blocked Tests 6-8 (pptx/xlsx):** re-run the pptx variable-row table limit (documented_limit "pptx cannot grow tables"), the xlsx chart strip (documented_limit "openpyxl drops charts"), and the xlsx merged-cell rows — each now reaches its verdict/file instead of crashing at persist.
    6. **Weak-model cross-provider re-run:** re-run the providers that didn't render an openable docx pre-fix (deepseek post-fix, gemini-3.5-pro / claude-sonnet-4-6 / full MiniMax) to finish the openable-docx column of the cross-provider axis (forcing already proven on all TIER-FORCE; this fills the render column).

    Record the verdict per row. Any NEW silent crash / stuck-active row / undeliverable file is a regression to fix before /gsd:verify-work 101.1.
  </how-to-verify>
  <resume-signal>Type "verified" with the per-row results, or describe any run that still crashes silently / leaves a phase 'active' / produces an undeliverable file.</resume-signal>
</task>

</tasks>

<verification>
- The 3 dirty rows are repaired (Task 1; DB read-back).
- DeepSeek v4-pro produces an openable cited .docx or a visible honest failure (checkpoint #1).
- Exactly one failure message persists (checkpoint #2).
- The ephemeral fill persists + downloads + self-heals the panel (checkpoint #3).
- The emit sub-steps render live (checkpoint #4).
- Tests 6-8 reach their verdicts (checkpoint #5).
- The cross-provider openable-docx column is filled (checkpoint #6).
</verification>

<success_criteria>
- No workflow_phases row is left 'active' for a failed run (table clean).
- Every provider attempt produces an openable cited .docx OR a visible honest failure naming the gate — never a silent 0-docx, never a stuck 'active' phase (the G-6 acceptance bar).
- The ephemeral fill's deliverable persists and is downloadable.
- The blocked UAT rows (Tests 6-8) run; the cross-provider scoreboard's render column is filled.
- Operator records the live verdict feeding /gsd:verify-work 101.1.
</success_criteria>

<output>
After completion, create `.planning/phases/101.1-guaranteed-emission-layer/101.1-10-SUMMARY.md`. Next: `/gsd:verify-work 101.1` (the 8 UAT rows + the SC#10 4-axis full roster) + `/gsd:secure-phase 101.1`.
</output>
