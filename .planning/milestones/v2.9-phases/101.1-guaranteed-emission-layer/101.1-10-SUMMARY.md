---
phase: 101.1-guaranteed-emission-layer
plan: 10
subsystem: testing
tags: [workflow, harness, llm_emit, forced-emission, template-fill, cross-provider, uat, data-repair, psycopg2]

# Dependency graph
requires:
  - phase: 101.1-07
    provides: "DeepSeek FORCE-NOTHINK + the D-08 layer-6 honest-failure catch-all + single-owner persist + active-phase flip on terminal failure"
  - phase: 101.1-08
    provides: "binary version-delta skip + distinct ephemeral output path + snapshot no-such-key degrade"
  - phase: 101.1-09
    provides: "downloadWorkspaceFile + raw-bytes route (gap 3); phase_substep demux RATIFIED (gap 6); FILES panel self-heal on harness terminal (gap 4 frontend)"
provides:
  - "The 3 dirty workflow_phases rows (575e7345 / a7f415ad / 4ea9bc56) repaired active -> failed via an idempotent scoped psycopg2 script"
  - "Live re-verification verdict across all 6 diagnosed gaps + the blocked pptx/xlsx Tests 6-8 + the weak-model cross-provider render column — the close-the-loop evidence feeding /gsd:verify-work 101.1"
  - "scripts/repair_dirty_workflow_phases.py (reusable scoped active-under-failed repair pattern)"
affects: [101.1-verify-work, 101.1-secure-phase, Phase-103, Phase-104]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Scoped idempotent data-repair script: dry-run preview, --apply gate, read-back assertion, SCOPED to named run_ids (never a blanket UPDATE)"
    - "Orchestrator-driven live re-verification: psycopg2 (:54322) receipts + Chrome MCP DOM sampling + downloaded-file byte validation (python-docx re-open + residual scan)"

key-files:
  created:
    - scripts/repair_dirty_workflow_phases.py
  modified:
    - .planning/phases/101.1-guaranteed-emission-layer/101.1-UAT.md
    - .planning/STATE.md

key-decisions:
  - "Repair scoped to exactly the 3 emission-layer run_ids; the 2 unrelated Phase-092 confirm rows left untouched (DI-101.1-10-A)"
  - "Live re-verification is the acceptance bar (the 099/101.1-06 lesson: offline mocks drift from live shapes on every layer)"

patterns-established:
  - "active-under-failed repair: SELECT the active fill rows for named failed runs, dry-run preview, --apply UPDATE to failed, read-back assert no active remains"

requirements-completed: [TMPL-02, TMPL-03]

# Metrics
duration: ~2h (incl. operator-driven live runs)
completed: 2026-06-11
---

# Phase 101.1 Plan 10: Live Re-Verification + Dirty-Row Repair Summary

**All 6 diagnosed emission-layer gaps PROVEN CLOSED live: DeepSeek no longer silently crashes (FORCE-NOTHINK), the ephemeral run-split fill persists + downloads a clean cited .docx, the emit sub-steps tick on the rail, blocked pptx/xlsx Tests 6-8 reach honest verdicts — plus the 3 dirty workflow_phases rows repaired and zero new dirty rows across 8 cross-provider runs.**

## Performance

- **Duration:** ~2h (Task 1 script + operator/orchestrator-driven live runs + docs)
- **Started:** 2026-06-11T22:45:00Z (Task 1 script authored)
- **Completed:** 2026-06-11T23:10:00Z (live verdict recorded)
- **Tasks:** 2 (Task 1 auto repair; Task 2 human-verify live re-verification)
- **Files modified:** 3 (1 created script + UAT scoreboard + STATE)

## Accomplishments

- **Data repair (Task 1):** the 3 dirty `workflow_phases` rows left by the silent run-crashes (575e7345 / a7f415ad / 4ea9bc56 — parent run `failed`, fill phase stuck `active` forever) flipped to `failed` via an idempotent, scoped psycopg2 script. Commit `cb573b72`.
- **Gap 1 CLOSED live (DeepSeek silent-crash):** deepseek-v4-pro run `fbec07cd` — no 400, no silent crash; the `emit_forced` receipt carries `thinking:false` (FORCE-NOTHINK proven live); honest no-commit failure ladder (`emit_forced -> emit_failed`, fill phase flipped `failed`).
- **Gap 2 CLOSED live (duplicate message):** every honest-failure run persisted EXACTLY ONE assistant message (run `fbec07cd` and all 8 runs).
- **Gaps 3+5 CLOSED live (ephemeral fill persist + download):** gpt-5.5 run `a5705707` — full success ladder `emit_forced -> emit_validated -> emit_rendered`; `/deliverable-filled.docx` (37,859 B) persisted to a DISTINCT path (template untouched, binary-delta skip held); `/raw` route 200 attachment; downloaded bytes open via python-docx, residual scan `[]`, the fractured `{{top_risk}}` token filled.
- **Gap 4 CLOSED live (panel self-heal):** the deliverable appeared in FILES WITHOUT an F5 (workspace files + snapshot refetch observed in the network log on the terminal event; snapshot 200 on the finished run).
- **Gap 6 PROVEN live (emit sub-steps on the rail):** DOM sampler captured the discrete sub-steps ticking on the PhaseCard rail (run `9b0e8f5f`: "Emitting the field-map" 19:21:49 -> "Rendering the deliverable" 19:21:57 -> named failure state 19:22:22).
- **Blocked Tests 6-8 (pptx/xlsx) now reach honest verdicts:** runs `cc7a4098` / `9b0e8f5f` / `23d5eb22` each reach `emit_forced -> emit_validated -> emit_integrity_failed` with the failure named on the rail and 1 message + phase `failed` (pre-fix: crashed at persist).
- **Weak-model cross-provider render column filled:** gemini-3.5-flash (`a7878fd6`), claude-sonnet-4-6 (`60bee474`), MiniMax-M3 (`dd8e0314`), deepseek-v4-pro (`fbec07cd`) all end in VISIBLE honest failures naming the gate (no docx) — the G-6 acceptance bar ("openable docx OR visible honest failure naming the gate") is MET for all.
- **Global invariant after all 8 runs:** zero NEW dirty rows; every fill phase flipped correctly; Task 1's 3 repaired rows stayed repaired. The only remaining `active`-under-`failed` rows are the 2 known out-of-scope Phase-092 `confirm` rows (DI-101.1-10-A).

## Task Commits

1. **Task 1: One-off repair of the 3 dirty workflow_phases rows (data fix)** — `cb573b72` (fix)
2. **Task 2: Live re-verification across the 6 gaps + the cross-provider render column** — `checkpoint:human-verify`, no source commit (live re-verification; results recorded in this SUMMARY + the UAT scoreboard)

**Plan metadata:** (this docs commit)

## Files Created/Modified

- `scripts/repair_dirty_workflow_phases.py` — idempotent, scoped psycopg2 repair: SELECTs the active fill rows for the 3 named failed runs, dry-run preview, `--apply`-gated UPDATE to `failed`, read-back assertion that no active row remains; never touches another run, never deletes, never alters `workflow_runs`/`harness_audit`.
- `.planning/phases/101.1-guaranteed-emission-layer/101.1-UAT.md` — gap rows flipped to live-proven status with run ids + evidence; the 3 blocked-on-fixes Tests 6-8 flipped to pass (honest verdict); findings 1-3 recorded as minor observations.
- `.planning/STATE.md` — Plan 10 marked complete; position advanced.

## Live Re-Verification Results

All runs on the live local stack (backend :8000 serving Plans 07-09 code, frontend :5173 hard-refreshed, local Supabase). 7 orchestrator-driven runs + 1 operator-driven DeepSeek run. Evidence = `workflow_runs` / `workflow_phases` / `harness_audit` / `messages` rows via psycopg2 (:54322) + Chrome MCP DOM sampling + downloaded-file byte validation.

| Run | Model | Scenario | Outcome |
|-----|-------|----------|---------|
| `fbec07cd` | deepseek-v4-pro (operator-driven) | library Risk Register Fill | **GAP 1 CLOSED:** no 400, no silent crash. `emit_forced` receipt has `thinking:false` (FORCE-NOTHINK live). Honest no-commit failure: `emit_forced -> emit_failed`, fill phase flipped `failed`, EXACTLY ONE message (**GAP 2 CLOSED**). Live pill mislabel found (Finding 1). |
| `a5705707` | gpt-5.5 | ephemeral run-split docx fill (UAT Test 4) | **GAPS 3+5 CLOSED:** full success ladder `emit_forced -> emit_validated -> emit_rendered`. `/deliverable-filled.docx` (37,859 B) persisted — binary delta skip held, DISTINCT path (template untouched). File appeared in FILES WITHOUT refresh (**GAP 4 frontend CLOSED**; snapshot 200 on finished run = **GAP 4 backend CLOSED**). FilePreview opens; REAL Download button; `/raw` route 200 content-disposition attachment; downloaded bytes open via python-docx, residual `[]`, fractured `{{top_risk}}` filled ("Risk M-02 (Integration dependency)"). |
| `cc7a4098` | gpt-5.5 | ephemeral pptx table-deck (blocked Test 6) | Reaches honest verdict (pre-fix: crashed at persist): `emit_forced -> emit_validated -> emit_integrity_failed`, "integrity re-open failed" named on rail, 1 message, phase `failed`. |
| `9b0e8f5f` | gpt-5.5 | ephemeral xlsx chart-book (blocked Test 7) | Same honest integrity-failure ladder. **GAP 6 PROVEN LIVE** during this run: DOM sampler captured "Emitting the field-map" (19:21:49) -> "Rendering the deliverable" (19:21:57) -> named failure state (19:22:22) ticking on the PhaseCard rail. |
| `23d5eb22` | gpt-5.5 | ephemeral xlsx merged-cell (blocked Test 8) | Same honest integrity-failure ladder. 1 message, phase `failed`. |
| `a7878fd6` | gemini-3.5-flash (google serves no 3.5-pro) | library fill | Honest no-commit failure: `emit_forced -> emit_failed`, named "model never committed the forced field-map", 1 message. |
| `60bee474` | claude-sonnet-4-6 | library fill | Honest CITATION rejection with bounded retry: `emit_forced -> emit_rejected x3`, named "citation gate rejected (uncited / invented)", 1 message. |
| `dd8e0314` | MiniMax-M3 | library fill | Honest citation rejection x3 retries, same named state, 1 message. "Emitting the field-map" observed live mid-run on the rail. |

**Global invariant after all runs:** `workflow_phases` stuck-`active` count = 2, BOTH the known out-of-scope Phase-092 `confirm` rows (`5aa42b6b` / `e0d1f740`, logged in `deferred-items.md` DI-101.1-10-A). ZERO new dirty rows. Every fill phase flipped correctly. Task 1's 3 repaired rows stayed repaired.

## Decisions Made

- **Repair scope = exactly the 3 named emission-layer runs.** The 2 unrelated Phase-092 `confirm` rows discovered during the global cleanliness check are left untouched and routed forward (DI-101.1-10-A) — a blanket "all active-under-failed" sweep would violate the plan SCOPE and risk touching unrelated terminal states.
- **Live re-verification is the acceptance bar.** Offline mocks drifted from live shapes on every layer through 101.1-06; only a live gates-all-green (or honest-fail) run proves the last mile.

## Deviations from Plan

None — plan executed exactly as written. Task 1 ran per the action spec (dry-run preview, `--apply`-gated, idempotent, read-back assertion); Task 2 (the human-verify checkpoint) was executed via orchestrator-driven live runs + one operator-driven DeepSeek run, all results recorded here and in the UAT scoreboard.

## Findings (run-honesty polish — minor, none block)

1. **LIVE phase pill mislabel:** a fill phase that fails honestly shows green "Complete" on the live card (the failure box renders correctly below it); after reload the pill correctly shows red "Failed". The live terminal event maps to Complete regardless of phase failure. Run-honesty polish, not a security/correctness gate failure — the honest failure IS visible (the failure box + the named state on the rail + the single failure message). Route to the run-legibility phase.
2. **RELOAD failure-reason degrade:** after reload the rich failure reason is replaced by "Failure reason not captured by the backend — error field was empty" — the reason lives in `harness_audit` + the chat message but NOT in the `workflow_phases` row's `error` field, so the DB reconcile can't render it. Persist the failure reason to the phase `error` field to close. Route to the run-legibility phase.
3. **Integrity-gate calibration on pptx/xlsx:** all three office-format ephemeral fills (Tests 6-8) validated citations then failed integrity re-open instead of delivering with `documented_limit` semantics (the pptx table-growth / xlsx chart-strip allowances from the Phase-101 sandbox path are not implemented in the 101.1 emitter integrity gate). HONEST, not silent — but the documented-limit "pass OR documented" delivery path never delivers a file for these formats. Route to a follow-up (the documented-limit semantics already exist in `template_render_service.assert_integrity`; the 101.1 emitter integrity gate must honor them).
4. **Weak-model openable-docx column remains unfilled by design:** gemini-3.5-flash / claude-sonnet-4-6 / MiniMax-M3 / deepseek-v4-pro all end in honest failures (no docx). The acceptance bar "openable docx OR visible honest failure naming the gate" is MET for all. Feeds the Phase 103/104 model-guidance design gap already captured (the workflow builder needs per-phase model selection filtered/badged by `forced_emission`).

## Issues Encountered

None during planned work. The live runs surfaced 4 run-honesty/calibration findings (above) — all minor, none blocking, all routed forward. The integrity-gate calibration on office formats (Finding 3) means pptx/xlsx ephemeral fills never deliver a file today; honest, observable, and out of this plan's gap-closure scope (the persist crash that blocked Tests 6-8 IS fixed — they now reach a verdict instead of silently dying).

## Next Phase Readiness

- **TMPL-02 / TMPL-03 are now closeable at phase verification.** The cross-provider render column is filled; the ephemeral fill persists + downloads; the emit sub-steps render live; the blocked tests reach verdicts. Every provider attempt produces an openable cited .docx OR a visible honest failure naming the gate — the G-6 acceptance bar.
- **Next:** `/gsd:verify-work 101.1` (the 8 UAT rows + the SC#10 4-axis full roster) then `/gsd:secure-phase 101.1`.
- **Routed forward:** Findings 1-2 (run-honesty polish: live-pill mislabel + reload failure-reason degrade) to the run-legibility phase; Finding 3 (office-format integrity-gate calibration) to a follow-up; Finding 4 (weak-model render guidance) already captured for Phase 103/104.

## Self-Check: PASSED

- FOUND: `.planning/phases/101.1-guaranteed-emission-layer/101.1-10-SUMMARY.md`
- FOUND: `scripts/repair_dirty_workflow_phases.py`
- FOUND commit: `cb573b72` (Task 1 — repair the 3 dirty workflow_phases rows)

---
*Phase: 101.1-guaranteed-emission-layer*
*Completed: 2026-06-11*
