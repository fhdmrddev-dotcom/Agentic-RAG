---
phase: 175-cross-provider-streaming-fidelity
plan: 03
type: execute
wave: 2
depends_on: [175-01]
files_modified:
  - backend/app/services/openai_service.py
  - backend/app/services/provider_gateway/errors.py
  - backend/tests/unit/test_reasoning_first_routing.py
  - backend/app/services/provider_gateway/test_errors.py
autonomous: true
requirements: [XPROV-01]

must_haves:
  truths:
    - "D-01: resolve_calling_mode returns STRUCTURED when the model's reasoning_first capability is set — so Deep chat sends NO tools param AND NO reasoning_effort param → no gpt-5.6 400, reasoning stays on"
    - "D-01: the reasoning_first gate sits ABOVE the effective_native/db_native resolution so an operator native_tools=True does NOT re-trigger the 400 (hard OpenAI API constraint wins — RESEARCH Open Q2)"
    - "D-04: the gpt-5.6 reasoning-tools-unsupported 400 gets an honest, actionable fixed-copy hint (dedicated ErrorKind) instead of the generic bad_request 'model parameter error' — NO raw-detail interpolation"
    - "D-14: a model with no reasoning_first key is byte-identical to today (default-inert); the error copy change is classification/copy only"
  artifacts:
    - path: backend/app/services/openai_service.py
      provides: "reasoning_first STRUCTURED gate in resolve_calling_mode, above effective_native"
      contains: "reasoning_first"
    - path: backend/app/services/provider_gateway/errors.py
      provides: "reasoning_tools_unsupported ErrorKind + fixed honest copy + narrow structured-signature classify branch"
      contains: "reasoning_tools_unsupported"
    - path: backend/tests/unit/test_reasoning_first_routing.py
      provides: "STRUCTURED gate + reasoning_first-wins-over-db_native ordering + non-5.6 byte-identical"
  key_links:
    - from: backend/app/config.py
      to: backend/app/services/openai_service.py
      via: "reasoning_first marker (Plan 01) read via get_model_capability at the resolve_calling_mode seam"
      pattern: "reasoning_first"
---

<objective>
Make gpt-5.6-class reasoning-first OpenAI models usable out of the box: route them STRUCTURED (XML tool injection) so no `tools`/`reasoning_effort` param is sent → no 400, reasoning stays on (XPROV-01 / D-01). Replace the generic 400 copy with an honest, actionable hint for the specific reasoning-tools-unsupported case (D-04).

Purpose: closes BUG-260714-01 (default OpenAI reasoning model dead with tools) at the adapter seam, capability-keyed (never an id-list), with the API constraint winning over an operator override.
Output: a one-line gate in `resolve_calling_mode`, a dedicated `reasoning_tools_unsupported` ErrorKind + fixed copy, and two unit proofs.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/175-cross-provider-streaming-fidelity/175-CONTEXT.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-RESEARCH.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-PATTERNS.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-VALIDATION.md

<interfaces>
<!-- Verified against live source 2026-07-22. Depends on Plan 01's reasoning_first marker. -->
openai_service.py:
  resolve_calling_mode(model_id, user_settings=None) -> CallingMode  (:1660)
    cap = get_model_capability(model_id)  :1662
    db_native = _resolve_db_native_tools(model_id)  :1679 ; `if db_native is False: return STRUCTURED`  :1680-1681
    effective_native = db_native if db_native is not None else cap["native_tools"]  :1698
  STRUCTURED auto-path → tool-attach branch hits `else: pass` → no tools param  (:1909-1912) ; client.chat.completions.create :1914
  NOTE (confirmed): there is NO top-level reasoning_effort anywhere — only inside the DeepSeek extra_body["thinking"] block :1812-1817.
errors.py:
  ErrorKind Literal :64-72 · classify_provider_error :117-170 (_OpenAIBadRequestError branch :151) · _MESSAGES :175-199 (bad_request :187-190) · message_for_kind :202-212 (specific kinds never interpolate raw detail)
  structured-signal reading pattern: _has_insufficient_quota reads exc.body["error"]["code"] (:83-99) — mirror this for the 400 signature.
Analog tests: backend/tests/test_149_native_tools_routing.py (CallingMode routing, `_warm_cache`) · backend/app/services/provider_gateway/test_errors.py (MagicMock(spec=[]) + explicit attrs).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reasoning_first STRUCTURED gate in resolve_calling_mode (XPROV-01 D-01)</name>
  <files>backend/app/services/openai_service.py, backend/tests/unit/test_reasoning_first_routing.py</files>
  <read_first>
    - backend/app/services/openai_service.py :1660-1701 (`resolve_calling_mode` — `cap` at :1662, `db_native` short-circuit :1679-1681, `effective_native` :1698) + :1865-1914 (the STRUCTURED `else: pass` no-tools branch).
    - 175-PATTERNS.md § "openai_service.py" (exact gate placement ABOVE effective_native) + 175-RESEARCH.md § "XPROV-01 Finding 2" + Open Q2 (reasoning_first must WIN over an operator db_native=True).
    - Analog test: backend/tests/test_149_native_tools_routing.py (`_warm_cache` monkeypatch + `resolve_calling_mode(...) is CallingMode.STRUCTURED`).
  </read_first>
  <behavior>
    - A model whose capability carries reasoning_first → resolve_calling_mode returns CallingMode.STRUCTURED (tools go via XML injection; no tools param, no reasoning_effort → no 400; reasoning stays on).
    - reasoning_first WINS over an operator db_native=True (the gate is above the effective_native/db_native resolution) — a forced native override cannot re-trigger the API 400.
    - A model with NO reasoning_first key → identical routing to today (NATIVE when native_tools true; existing db_native/openrouter branches untouched).
  </behavior>
  <action>
    In `resolve_calling_mode`, immediately after `cap = get_model_capability(model_id)` (:1662) and BEFORE the `db_native = _resolve_db_native_tools(model_id)` line (:1679), add: `if cap.get("reasoning_first"): return CallingMode.STRUCTURED`. Placing it here makes the hard OpenAI API constraint win over the operator native_tools override (Open Q2) and short-circuits before the openrouter/effective_native branches. Do NOT add any `reasoning_effort` param and do NOT flip `native_tools` on the rows — STRUCTURED routing alone removes the `tools` param at the `else: pass` branch (:1909-1912), which is what avoids the 400.
    Create `backend/tests/unit/test_reasoning_first_routing.py` mirroring the `_warm_cache` analog: a `reasoning_first` row → STRUCTURED; the same row with an operator db_native=True override → STILL STRUCTURED (ordering proof); a plain `native_tools:True` non-5.6 row → NATIVE (byte-identical). Also assert directly that the real `MODEL_CAPABILITIES["gpt-5.6-sol"]` carries `reasoning_first` (ties the gate to Plan 01's data).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_reasoning_first_routing.py backend/tests/test_149_native_tools_routing.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `resolve_calling_mode` returns STRUCTURED when `reasoning_first` is set (source: gate above `db_native`).
    - reasoning_first-wins-over-db_native=True proven (Open Q2 ordering).
    - A non-reasoning_first model is byte-identical (D-14); `test_149_native_tools_routing.py` stays green.
    - No `tools` and no `reasoning_effort` param is introduced anywhere by this change.
  </acceptance_criteria>
  <done>gpt-5.6-class models route STRUCTURED with the API constraint winning; routing test green; existing routing test unregressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honest reasoning-tools-unsupported error copy (XPROV-01 D-04)</name>
  <files>backend/app/services/provider_gateway/errors.py, backend/app/services/provider_gateway/test_errors.py</files>
  <read_first>
    - backend/app/services/provider_gateway/errors.py :64-72 (ErrorKind Literal) + :83-99 (`_has_insufficient_quota` structured-body reading pattern) + :117-170 (`classify_provider_error`, _OpenAIBadRequestError branch :151) + :175-199 (`_MESSAGES`, bad_request :187-190) + :202-212 (`message_for_kind` — specific kinds never interpolate raw detail).
    - 175-PATTERNS.md § "errors.py (D-04)" (recommended dedicated-kind option a) + 175-RESEARCH.md § "XPROV-01 Finding 3".
    - Analog test: backend/app/services/provider_gateway/test_errors.py (MagicMock(spec=[]) with explicit attrs; the Google-429 structured-signal precedent).
    - NOTE: the errors test is colocated at `app/services/provider_gateway/test_errors.py` (it DOES exist — extend it, do not create tests/unit/test_errors.py).
  </read_first>
  <behavior>
    - An OpenAI 400 carrying the structured signature ("Function tools with reasoning_effort are not supported" / "use /v1/responses or set reasoning_effort to 'none'") classifies as a new ErrorKind `reasoning_tools_unsupported` (not the generic `bad_request`).
    - message_for_kind("reasoning_tools_unsupported") returns a fixed, actionable copy with NO raw-detail interpolation.
    - Any OTHER 400 still classifies as `bad_request` with the existing generic copy (byte-identical).
  </behavior>
  <action>
    Add `"reasoning_tools_unsupported"` to the `ErrorKind` Literal (:64-72). Add a `_MESSAGES["reasoning_tools_unsupported"]` fixed-string entry with an honest, actionable hint (e.g. that the reasoning-first model can't use tools on this endpoint yet, it's been switched to prompt-based tools automatically, and to pick a non-reasoning OpenAI model if it persists) — fixed copy, NO interpolation of `raw_detail` (preserves the info-disclosure control at :210-212; specific kinds fall through the `message_for_kind` non-`unknown` branch automatically).
    In `classify_provider_error`, add a NARROW structured-signature branch for the OpenAI-compatible family: when the error represents a 400 AND its structured body/message carries the reasoning-tools signature, return `"reasoning_tools_unsupported"`. Read the signal from the exception's structured body the same way `_has_insufficient_quota` reads `exc.body["error"]` (:83-99) — anchor on `body["error"]["message"]` containing the signature substrings (optionally `param == "reasoning_effort"`), NOT a loose keyword scan. This branch must run before/within the existing bad_request classification and must NOT reclassify any 400 lacking the signature.
    Extend `backend/app/services/provider_gateway/test_errors.py`: a simulated OpenAI 400 (MagicMock with `status_code=400` and `body={"error":{"message":"...Function tools with reasoning_effort are not supported...","param":"reasoning_effort"}}`) classifies as `reasoning_tools_unsupported`; `message_for_kind("reasoning_tools_unsupported")` returns the fixed copy and contains no substring of the raw body; a generic 400 without the signature still classifies as `bad_request`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest app/services/provider_gateway/test_errors.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The specific reasoning-tools 400 signature → `reasoning_tools_unsupported`; message is fixed copy with no raw-detail interpolation (D-04).
    - A generic 400 still → `bad_request` (byte-identical, D-14).
    - Detection is anchored on the structured body signature (mirrors `_has_insufficient_quota`), not a keyword-soup scan.
  </acceptance_criteria>
  <done>The gpt-5.6 reasoning-tools 400 surfaces an honest actionable hint; generic 400s are unchanged; errors test extended and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| provider 400 body → user-facing copy | The provider error body is untrusted text that must not be echoed into user copy verbatim. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-03-01 | Information Disclosure | reasoning_tools_unsupported error copy | mitigate | Fixed-string copy via `_MESSAGES`; `message_for_kind` never interpolates `raw_detail` for specific kinds (:210-212) — no key/trace/body leak. |
| T-175-03-02 | Tampering | classify_provider_error signature branch | mitigate | Detection anchors on the structured body signature (mirrors `_has_insufficient_quota`), not a loose keyword scan, so a crafted message string cannot force a misclassification of unrelated 400s. |
| T-175-03-03 | — | resolve_calling_mode routing | accept | The gate changes request SHAPE (STRUCTURED) only; no new input surface, no authz change. |
| T-175-03-SC | Tampering | package installs | accept | No installs (RESEARCH Package Legitimacy Gate N/A); `git diff` shows zero requirements.txt / Dockerfile.sandbox changes. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` + `pytest app/services/provider_gateway/test_errors.py -q` green.
- Known boundary (note, out of scope): the `force_tool_name` path (:1825-1864) always attaches tools and does NOT consult calling_mode — a workflow forcing a tool on a reasoning-first model could still 400. Deep chat uses tool_choice="auto"/force_tool_name=None → fixed here. Forced-emission on reasoning-first = SEED-114 territory (do not plan).
- SC#10 cross-provider + multi-tool axes (gpt-5.6-class + tools → no 400, reasoning on) are authored in 175-VALIDATION.md (manual live-UAT — real OpenAI endpoint constraint). Do not duplicate.
</verification>

<success_criteria>
- gpt-5.6-class routes STRUCTURED (no 400), reasoning_first wins over db_native.
- The specific 400 gets honest fixed copy; generic 400s unchanged.
- Both unit proofs green; full unit suite green; Deep byte-identical for non-reasoning_first models.
</success_criteria>

<output>
Create `.planning/phases/175-cross-provider-streaming-fidelity/175-03-SUMMARY.md` when done.
</output>
