---
phase: 175-cross-provider-streaming-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/services/sub_agent_models.py
  - backend/tests/unit/test_reasoning_capability_markers.py
  - backend/tests/unit/test_utility_model_guard.py
autonomous: true
requirements: [XPROV-01, XPROV-03, XPROV-04]

must_haves:
  truths:
    - "D-01: the three gpt-5.6-class rows (gpt-5.6-sol/terra/luna) carry a capability-keyed reasoning_first marker — NEVER a hardcoded id-list in code (D-122-04)"
    - "D-05: EVERY docs-confirmed-SAFE registry row carries reasoning_off, marked per-MODEL across the whole SAFE set (NOT per-provider-default — thread_title.py:116-124 resolves the title model from the user's ACTIVE chat_model, so any SAFE model the user chats with must carry it) — thinking_disabled on deepseek-v4-flash/-pro, kimi-k2.5/k2.6, glm-4.6/4.7/5/5-turbo/5.1/5.2, MiniMax-M3; effort_none on gemini-2.5-flash/-flash-lite; the UNSAFE negatives (all MiniMax-M2.x, gemini-2.5-pro, all gemini-3.x, glm-4.5/4.5-air, moonshot-v1-8k) stay UNMARKED → keep derived fallback, no regression"
    - "D-03: provider_safe_utility_model drops a cross-provider override before any provider call, and the inferred-provider gate fires even when available_models is empty (the blind spot list-membership misses)"
    - "D-14: absent markers, same-provider overrides, and flexible providers (openrouter/ollama) are byte-identical to today — default-inert"
  artifacts:
    - path: backend/app/config.py
      provides: "reasoning_first + reasoning_off capability markers on the correct MODEL_CAPABILITIES rows (full docs-confirmed SAFE set)"
      contains: "reasoning_first"
    - path: backend/app/services/sub_agent_models.py
      provides: "provider_safe_utility_model helper + inferred-provider gate folded into resolve_sub_agent_model_safely"
      contains: "def provider_safe_utility_model"
    - path: backend/tests/unit/test_reasoning_capability_markers.py
      provides: "registry SAFE/UNSAFE marker-matrix regression guard (full SAFE set marked, UNSAFE negatives unmarked)"
    - path: backend/tests/unit/test_utility_model_guard.py
      provides: "inferred-provider guard unit proof (drop cross-provider, keep same-provider, flexible passthrough, empty-available_models)"
  key_links:
    - from: backend/app/config.py
      to: backend/app/services/thread_title.py
      via: "reasoning_off marker consumed by the title-call injection (Plan 04)"
      pattern: "reasoning_off"
    - from: backend/app/services/sub_agent_models.py
      to: backend/app/services/task_service.py
      via: "folded inferred-provider gate inherited by resolve_sub_agent_model_safely's existing caller"
      pattern: "resolve_sub_agent_model_safely"
---

<objective>
Land the shared substrate every downstream Plan reads: the capability-keyed registry markers (XPROV-01 `reasoning_first`, XPROV-04 `reasoning_off`) and the shared cross-provider utility-model guard (XPROV-03). All additive data + one pure helper — no request path forks yet.

Purpose: config markers and the `sub_agent_models.py` guard are the foundational reads for Plans 03 (routing gate) and 04 (title-call injection + guard application). Landing them first lets those plans parallelize.
Output: `reasoning_first` on the 3 gpt-5.6 rows + `reasoning_off` on the FULL docs-confirmed SAFE set, `provider_safe_utility_model` + a folded inferred-provider gate in `resolve_sub_agent_model_safely`, and two Wave-0 unit proofs.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/175-cross-provider-streaming-fidelity/175-CONTEXT.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-RESEARCH.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-PATTERNS.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-VALIDATION.md

<interfaces>
<!-- Verified against live source 2026-07-22 (config.py rows re-read this revision). -->

config.py — the target MODEL_CAPABILITIES rows (dict literal starts :239):
  XPROV-01 reasoning_first (mark): gpt-5.6-sol :265 · gpt-5.6-terra :266 · gpt-5.6-luna :267   (all native_tools:True today)
  XPROV-04 reasoning_off="thinking_disabled" (SAFE — mark, extra_body.thinking family):
    deepseek-v4-flash :325 · deepseek-v4-pro :326 · kimi-k2.6 :333 · kimi-k2.5 :334
    glm-4.6 :357 · glm-4.7 :358 · glm-5 :359 · glm-5-turbo :360 · glm-5.1 :361 · glm-5.2 :362 · MiniMax-M3 :349
  XPROV-04 reasoning_off="effort_none" (SAFE — mark, Google reasoning_effort=none):
    gemini-2.5-flash :303 · gemini-2.5-flash-lite :304
  UNSAFE — do NOT mark (accept-but-ignore / cannot-disable / pre-4.6 / non-reasoning):
    gemini-2.5-pro :302 (2.5-Pro cannot disable — RESEARCH verdict) · gemini-3-flash-preview :308 · gemini-3.1-pro-preview :309 · gemini-3.5-flash :313 · gemini-3.1-flash-lite :316
    MiniMax-M2 :342 · M2.1 :343 · M2.1-highspeed :344 · M2.5 :345 · M2.5-highspeed :346 · M2.7 :347 · M2.7-highspeed :348 (M2.x accepts {type:disabled} but keeps thinking ON)
    glm-4.5 :355 · glm-4.5-air :356 (pre-4.6, not docs-confirmed) · moonshot-v1-8k :335 (non-reasoning) · all OpenRouter rows (experimental, out of scope)
config.py — inference + fall-through (DRIFT-corrected):
  _infer_provider_for :466 (regex table :412-424) · _SUB_AGENT_MODEL_DEFAULTS :729 · get_model_capability :515
sub_agent_models.py — resolve_sub_agent_model_safely(user_settings, override_model=None, fallback_model=None) -> str  (:54-134);
  imports `from app.config import settings, _SUB_AGENT_MODEL_DEFAULTS`; list-membership gate is a no-op when available_models is empty (:134 passthrough — the leak).
thread_title.py root-cause (why per-MODEL, not per-default): `_SINGLE_MODEL_PROVIDERS` :47 {deepseek,moonshot,minimax,zhipu,ollama}; the model-resolution branch :116-124 tries the user's ACTIVE `chat_model` FIRST, THEN `_SUB_AGENT_MODEL_DEFAULTS[provider]` — so a user chatting with a non-default SAFE model (e.g. deepseek-v4-pro, kimi-k2.5, glm-5.2) hits THAT model for title-gen, not the default. Marking only the 3 defaults would leave those users with the degenerate title (the XPROV-04 symptom).
Analog test: backend/tests/unit/test_sub_agent_routing.py — SimpleNamespace user-settings factory + _SUB_AGENT_MODEL_DEFAULTS assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capability markers on the correct MODEL_CAPABILITIES rows (XPROV-01 D-01 + XPROV-04 D-05)</name>
  <files>backend/app/config.py, backend/tests/unit/test_reasoning_capability_markers.py</files>
  <read_first>
    - backend/app/config.py :239-388 (MODEL_CAPABILITIES row shape — every row is a flat dict; `native_tools` + `provider` always declared) and :729 (`_SUB_AGENT_MODEL_DEFAULTS`).
    - backend/app/services/thread_title.py :47 + :116-124 (the root-cause branch — title model comes from the ACTIVE `chat_model` FIRST for single-model providers; this is why XPROV-04 must be per-MODEL across the whole SAFE set, per BUG-260527-01/D-08 documented design).
    - 175-PATTERNS.md § "backend/app/config.py" (the exact rows + "by capability, not by name" D-122-04 rule).
    - 175-RESEARCH.md § "XPROV-04 — the per-provider verdict table" (Finding 7 — the docs-confirmed SAFE mechanisms + model-generation caveats) and § "Pattern 1" (the recommended shape — its example explicitly marks `"MiniMax-M3": {…,"reasoning_off":"thinking_disabled"}` and `"gemini-2.5-flash": {…,"reasoning_off":"effort_none"}`).
  </read_first>
  <action>
    Add `"reasoning_first": True` as a NEW key to the three gpt-5.6-class rows only — `gpt-5.6-sol` (:265), `gpt-5.6-terra` (:266), `gpt-5.6-luna` (:267). Do NOT touch their existing `native_tools: True` — the Plan-03 gate short-circuits STRUCTURED above the native-tools read, so the marker alone routes them correctly (per D-01).
    Add `"reasoning_off": "thinking_disabled"` as a NEW key to EVERY docs-confirmed-SAFE `extra_body.thinking`-family row — `deepseek-v4-flash` (:325), `deepseek-v4-pro` (:326), `kimi-k2.6` (:333), `kimi-k2.5` (:334), `glm-4.6` (:357), `glm-4.7` (:358), `glm-5` (:359), `glm-5-turbo` (:360), `glm-5.1` (:361), `glm-5.2` (:362), and `MiniMax-M3` (:349, **M3 ONLY** — RESEARCH Pattern-1 lists this exact row). These converge on the `extra_body={"thinking":{"type":"disabled"}}` shape (RESEARCH verdict table).
    Add `"reasoning_off": "effort_none"` as a NEW key to the two Google rows whose docs confirm disable — `gemini-2.5-flash` (:303) and `gemini-2.5-flash-lite` (:304) — via the top-level `reasoning_effort="none"` mechanism.
    This is a per-MODEL verdict across the WHOLE SAFE set, NEVER a per-provider-default: `thread_title.py`'s single-model branch (:116-124) resolves the title model from the user's ACTIVE `chat_model` first, so a user chatting with ANY SAFE model (not just the provider default) must carry the marker or the degenerate first-few-words title returns — the exact XPROV-04 symptom. The widening is registry DATA; there is NEVER an id-list branch in code.
    Do NOT add any `reasoning_off` key to the UNSAFE rows — the entire `MiniMax-M2.x` family (`MiniMax-M2` :342, `M2.1` :343, `M2.1-highspeed` :344, `M2.5` :345, `M2.5-highspeed` :346, `M2.7` :347, `M2.7-highspeed` :348 — M2.x accepts `{type:disabled}` but keeps thinking ON), `gemini-2.5-pro` (:302, 2.5-Pro **cannot** disable per RESEARCH verdict table — same reason as 3.x, NOT an oversight) and the whole `gemini-3.x` family (`gemini-3-flash-preview` :308, `gemini-3.1-pro-preview` :309, `gemini-3.5-flash` :313, `gemini-3.1-flash-lite` :316 — 3.x cannot disable), plus `glm-4.5`/`glm-4.5-air` (:355-356, pre-4.6 — not docs-confirmed) and the non-reasoning `moonshot-v1-8k` (:335). OpenRouter rows stay UNMARKED (experimental external surface, out of scope). Every UNMARKED row stays byte-identical so title-gen keeps today's derived fallback (no regression). This is a per-MODEL verdict, never a per-provider blanket.
    Create `backend/tests/unit/test_reasoning_capability_markers.py` asserting the exact SAFE/UNSAFE matrix directly against the real registry: gpt-5.6-sol/terra/luna each have `reasoning_first is True`; the FULL thinking_disabled SAFE set (deepseek-v4-flash, deepseek-v4-pro, kimi-k2.6, kimi-k2.5, glm-4.6, glm-4.7, glm-5, glm-5-turbo, glm-5.1, glm-5.2, MiniMax-M3) each have `reasoning_off == "thinking_disabled"`; gemini-2.5-flash and gemini-2.5-flash-lite each have `reasoning_off == "effort_none"`; and `"reasoning_off"` is NOT a key on the UNSAFE negatives (every MiniMax-M2.x row, gemini-2.5-pro, every gemini-3.x row, glm-4.5, glm-4.5-air, moonshot-v1-8k). Import `MODEL_CAPABILITIES` from `app.config`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_reasoning_capability_markers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `MODEL_CAPABILITIES["gpt-5.6-sol"]["reasoning_first"] is True` (and terra/luna) — capability-keyed, no id-list appears anywhere in code (D-01/D-122-04).
    - Every SAFE row carries reasoning_off: the 11 thinking_disabled rows (deepseek-v4-flash, deepseek-v4-pro, kimi-k2.6, kimi-k2.5, glm-4.6, glm-4.7, glm-5, glm-5-turbo, glm-5.1, glm-5.2, MiniMax-M3) == "thinking_disabled"; gemini-2.5-flash + gemini-2.5-flash-lite == "effort_none" (D-05 SAFE — per-MODEL across the whole set, not just the 3 defaults).
    - `"reasoning_off"` absent on EVERY UNSAFE negative: all seven MiniMax-M2.x rows, gemini-2.5-pro, all four gemini-3.x rows, glm-4.5, glm-4.5-air, moonshot-v1-8k (D-05 UNSAFE — accept-but-ignore + cannot-disable + pre-4.6 + non-reasoning keep the derived fallback).
    - D-14 default-inert: no existing key altered; every UNMARKED row is byte-identical; `git diff` touches ONLY the marked rows (3 `reasoning_first` + 13 `reasoning_off` = 16 rows) + the new test file — the earlier "only 3 named rows" boundary is superseded by this widened SAFE set.
    - No package installs — `git diff` shows zero changes to requirements.txt / Dockerfile.sandbox.
  </acceptance_criteria>
  <done>The registry carries reasoning_first on the 3 gpt-5.6 rows + reasoning_off on the full docs-confirmed SAFE set, with every UNSAFE negative unmarked; the marker-matrix test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared inferred-provider utility-model guard (XPROV-03 D-03)</name>
  <files>backend/app/services/sub_agent_models.py, backend/tests/unit/test_utility_model_guard.py</files>
  <read_first>
    - backend/app/services/sub_agent_models.py :54-134 (`resolve_sub_agent_model_safely` — the established shared home; note the empty-`available_models` passthrough at :134 that leaks a cross-provider id).
    - backend/app/config.py :466 (`_infer_provider_for`) + :729 (`_SUB_AGENT_MODEL_DEFAULTS`).
    - 175-PATTERNS.md § "backend/app/services/sub_agent_models.py" (recommended `provider_safe_utility_model` shape) + § "No Analog / Special Handling" (the D-03-vs-D-085-16 tension resolution).
    - 175-RESEARCH.md § "XPROV-03 Finding 6" (why inferred-provider beats list-membership; the 4-site table naming `sub_agent_service.py` byte-frozen) + Open Q3 (RESOLVED).
    - Analog test: backend/tests/unit/test_sub_agent_routing.py (SimpleNamespace factory).
  </read_first>
  <behavior>
    - provider_safe_utility_model(user_settings, override_candidate): cross-provider candidate (inferred provider != active_provider) → returns None (caller falls through to default).
    - same-provider candidate (inferred == active) → returns the candidate unchanged.
    - flexible providers (active_provider in {"openrouter","ollama"}) → never blocked, returns candidate.
    - None/empty candidate → returns None.
    - resolve_sub_agent_model_safely with an EMPTY available_models list AND a cross-provider candidate → now falls through to _SUB_AGENT_MODEL_DEFAULTS[active] via the folded inferred-provider gate (the blind spot list-membership missed).
    - resolve_sub_agent_model_safely with a same-provider candidate → byte-identical to today (D-14).
  </behavior>
  <action>
    Add a new pure helper `provider_safe_utility_model(user_settings, override_candidate: str | None) -> str | None` to `sub_agent_models.py`: read `active = (user_settings.active_provider if user_settings else "") or ""`; return None on falsy `override_candidate`; compute `inferred = _infer_provider_for(override_candidate)`; return `override_candidate` when `inferred == active or active in ("openrouter","ollama")`, else return None. Import `_infer_provider_for` from `app.config`.
    Fold the SAME inferred-provider check INTO `resolve_sub_agent_model_safely` as an additional gate BEFORE the existing list-membership branch — this is the single most-leveraged edit (RESEARCH): once the final `candidate` is chosen, if it is a genuine cross-provider mismatch (inferred provider != active, and active is not a flexible provider) and `_SUB_AGENT_MODEL_DEFAULTS[active]` is non-empty, return that default; otherwise keep today's behavior exactly. This closes the empty-`available_models` blind spot for `task_service.py` (its existing caller) WITHOUT touching the byte-frozen `sub_agent_service.py`.
    Do NOT edit `sub_agent_service.py` (D-085-16 byte-frozen). Document the D-03 coverage in the SUMMARY PRECISELY — do NOT claim "all 4 sites covered". State it as 3/4 sites fully closed via the shared inferred-provider mechanism (thread_title + suggestion via explicit `provider_safe_utility_model` calls in Plan 04; task_service via the folded gate landed here, which also closes its empty-`available_models` blind spot), while the 4th site — `sub_agent_service.py`, byte-frozen per D-085-16 — is NOT reached by the inferred-provider gate and RETAINS its existing populated-list-only inline guard (its empty-`llm_models` blind spot remains, mitigated in practice by its provider-correct default).
    Create `backend/tests/unit/test_utility_model_guard.py` covering every behavior above (mirror the `SimpleNamespace` user-settings factory from the analog).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_utility_model_guard.py tests/unit/test_sub_agent_routing.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `provider_safe_utility_model` returns None for a cross-provider candidate, the candidate for a same-provider or flexible-provider case, None for empty (D-03).
    - `resolve_sub_agent_model_safely` with empty `available_models` + a cross-provider candidate returns `_SUB_AGENT_MODEL_DEFAULTS[active]` (blind spot closed); same-provider candidate is byte-identical to pre-change (D-14 — `test_sub_agent_routing.py` stays green).
    - `sub_agent_service.py` is absent from `git diff` (D-085-16).
    - SUMMARY states D-03 coverage PRECISELY as 3/4 sites fully closed + the byte-frozen `sub_agent_service.py` retaining its populated-list-only guard — no blanket "all 4 covered" claim.
    - No new SSE event, no request-path fork, no package install.
  </acceptance_criteria>
  <done>The shared guard exists and is unit-proven; the folded gate closes the empty-list leak for task_service; the frozen file is untouched; the SUMMARY names the precise 3/4 coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator config → registry | MODEL_CAPABILITIES is code/operator-controlled, not user input — no new untrusted-input boundary crosses here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-01-01 | Tampering | MODEL_CAPABILITIES markers | accept | Registry is operator/code-controlled; markers are additive data with no user-input path. The marker-matrix test is the tamper regression guard. |
| T-175-01-02 | Elevation | provider_safe_utility_model routing | accept | Guard changes only WHICH utility model id is sent; it never changes the caller's RLS/auth context (request-scoped per Phase-163 D-03). No authz widening. |
| T-175-01-SC | Tampering | package installs | accept | No installs this plan (RESEARCH Package Legitimacy Gate = N/A); `git diff` shows zero requirements.txt / Dockerfile.sandbox changes. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` green after both tasks.
- Cross-plan reachability: the `reasoning_off` markers (full SAFE set) are consumed by Plan 04's title-call injection; `provider_safe_utility_model` by Plan 04's guard application; the folded gate by task_service's existing call.
- SC#10 live-UAT for this substrate is exercised end-to-end via Plans 03/04 UAT rows in 175-VALIDATION.md (do not duplicate here).
</verification>

<success_criteria>
- reasoning_first on the 3 gpt-5.6 rows; reasoning_off on the FULL docs-confirmed SAFE set (11 thinking_disabled + 2 effort_none); every UNSAFE negative unmarked.
- provider_safe_utility_model + folded gate land in sub_agent_models.py; sub_agent_service.py untouched; SUMMARY states precise 3/4 D-03 coverage.
- Both Wave-0 tests green; full unit suite green; Deep byte-identical (default-inert).
</success_criteria>

<output>
Create `.planning/phases/175-cross-provider-streaming-fidelity/175-01-SUMMARY.md` when done.
</output>
