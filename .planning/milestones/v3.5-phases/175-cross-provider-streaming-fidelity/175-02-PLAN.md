---
phase: 175-cross-provider-streaming-fidelity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/provider_gateway/openai_compat.py
  - backend/app/services/agent_loop.py
  - backend/tests/unit/test_openai_compat_dsml_strip.py
  - backend/tests/unit/test_dsml_leak_signal.py
autonomous: true
requirements: [XPROV-02]

must_haves:
  truths:
    - "D-02(a): _normalize flushes a trailing non-leaking _dsml_pending fragment as a final delta before the usage yield — no silent content-loss at stream end"
    - "D-02(a): the shipped _strip_deepseek_tool_markup guarantees no dirty DSML render across chunk boundaries and on long turns (the SC#2 floor)"
    - "D-02(b): a detected DSML leak surfaces exactly one honest signal via the EXISTING error SSE event through an Option-B post-drain hook (stream.dsml_leaked) — no new event type, no per-chunk edit"
    - "D-14: the shared _normalize per-chunk path and the shared _on_chunk consumer stay byte-identical for every non-deepseek provider and every clean deepseek turn (default-inert)"
  artifacts:
    - path: backend/app/services/provider_gateway/openai_compat.py
      provides: "stream-end flush + self.dsml_leaked leak flag (deepseek-gated)"
      contains: "dsml_leaked"
    - path: backend/app/services/agent_loop.py
      provides: "post-drain hook emitting the existing error event when stream.dsml_leaked is set"
      contains: "dsml_leaked"
    - path: backend/tests/unit/test_dsml_leak_signal.py
      provides: "leak → single error event; clean/non-deepseek → no emit"
  key_links:
    - from: backend/app/services/provider_gateway/openai_compat.py
      to: backend/app/services/agent_loop.py
      via: "stream.dsml_leaked read in the post-drain block → _emit('error', ...)"
      pattern: "dsml_leaked"
---

<objective>
Harden the shipped DeepSeek DSML sanitizer so it never loses trailing content at stream end, and turn a detected leak into an honest, reuse-the-existing-`error`-event signal — all deepseek-gated and default-inert (XPROV-02 / D-02).

Purpose: guarantee the no-dirty-render floor (SC#2) holds on long tool-chain turns and that a leaked-tool-as-text turn ends honestly ("it didn't run — retry") instead of silently incomplete, without forking the shared chunk/consumer path (D-14).
Output: a stream-end flush + a `self.dsml_leaked` flag in `_normalize`, an Option-B post-drain hook in `agent_loop`, and two unit proofs.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/175-cross-provider-streaming-fidelity/175-CONTEXT.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-RESEARCH.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-PATTERNS.md
@.planning/phases/175-cross-provider-streaming-fidelity/175-VALIDATION.md

<interfaces>
<!-- Verified against live source 2026-07-22. -->
openai_compat.py:
  _DSML_OPENER = "<｜｜DSML｜｜"  (:138 — U+FF5C fullwidth pipe, NOT ASCII '|')
  _strip_deepseek_tool_markup(text, pending, leaking) -> (visible_out, new_pending, new_leaking)  (:141-174, pure)
  class _ClosableEventStream (:177) — _normalize is a generator method; per-stream locals `_dsml_leaking`/`_dsml_pending` init :229-230
  loop `for chunk in self._raw:` :251 · deepseek-gated strip call :314-317 · finish dict :422-426 · post-loop usage yield :428-433
agent_loop.py:
  _drain_stream_with_close_on_cancel(stream, ...) :2151-2156 · post-drain STRUCTURED block :2159
  existing honest-fail vocabulary: `await _emit(redis, run_id, 'error', message=<str>)` :2291 (also :2777, :2789)
Analog test: backend/tests/unit/test_openai_compat_dsml_strip.py — pure `_feed(chunks)` harness (:16-22) + opener-split cases.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stream-end flush for the DSML sanitizer (XPROV-02a)</name>
  <files>backend/app/services/provider_gateway/openai_compat.py, backend/tests/unit/test_openai_compat_dsml_strip.py</files>
  <read_first>
    - backend/app/services/provider_gateway/openai_compat.py :141-174 (`_strip_deepseek_tool_markup` — do NOT rebuild) + :229-230 (state) + :251 (loop) + :314-317 (deepseek-gated call) + :428-433 (post-loop usage yield).
    - 175-PATTERNS.md § "openai_compat.py" (the flush gap: `_dsml_pending` is never flushed after the loop) + 175-RESEARCH.md § "XPROV-02 Finding 4 gap 1" (low-severity content-loss, not a dirty render).
    - Analog test: backend/tests/unit/test_openai_compat_dsml_strip.py (`_feed` harness + `test_opener_split_across_chunks_is_caught`).
  </read_first>
  <behavior>
    - A deepseek turn ending in a partial-opener prefix (e.g. final visible chunk "<｜", non-leaking) → the held `_dsml_pending` fragment is emitted as a final `delta` at stream end (content preserved).
    - A deepseek turn that IS leaking at stream end (`_dsml_leaking` True) → nothing is flushed (the markup stays suppressed — the SC#2 floor holds).
    - A long deepseek turn with the opener mid-stream → prose before the opener is preserved, markup + everything after suppressed (unchanged).
    - Non-deepseek providers never enter the strip branch → byte-identical.
  </behavior>
  <action>
    In `_normalize`, immediately AFTER the `for chunk in self._raw:` loop ends and BEFORE/BESIDE the existing post-loop usage yield (~:428), add a guarded flush: if `_dsml_pending and not _dsml_leaking:` yield `{"type": "delta", "content": _dsml_pending}`. Leave `_strip_deepseek_tool_markup` itself unchanged. The flush is reachable only on the deepseek path (only deepseek populates `_dsml_pending`), so every other provider is byte-identical.
    Extend `backend/tests/unit/test_openai_compat_dsml_strip.py`: add a stream-end-flush test that drives the deepseek `_normalize` path (a minimal fake `_raw` iterator of chunks ending in a non-leaking partial-opener prefix) and asserts the trailing fragment is emitted as a final `delta`; add a leaking-at-end test asserting NO flush; add one long-input case (opener after many chunks) asserting the floor still holds. Keep the existing pure `_feed` cases green.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_openai_compat_dsml_strip.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Trailing non-leaking `_dsml_pending` is emitted as a final `delta` (source: the flush guard runs after the loop, before the usage yield).
    - Leaking-at-end flushes nothing; the no-dirty-render floor holds across chunk boundaries and long turns (SC#2).
    - D-14 default-inert: the flush is deepseek-gated; a non-deepseek stream and a clean deepseek stream are byte-identical; all pre-existing `_feed` cases stay green.
  </acceptance_criteria>
  <done>The sanitizer no longer swallows a trailing partial-opener fragment; the floor is intact; the strip test is extended and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honest-incomplete leak signal via Option-B post-drain hook (XPROV-02b)</name>
  <files>backend/app/services/provider_gateway/openai_compat.py, backend/app/services/agent_loop.py, backend/tests/unit/test_dsml_leak_signal.py</files>
  <read_first>
    - backend/app/services/provider_gateway/openai_compat.py :177 (`_ClosableEventStream.__init__`) + :314-317 (deepseek strip branch where `_dsml_leaking` transitions True).
    - backend/app/services/agent_loop.py :2151-2156 (`_drain_stream_with_close_on_cancel`) + :2159 (post-drain STRUCTURED block) + :2291 (existing `_emit(...,'error',...)` vocabulary to reuse).
    - 175-PATTERNS.md § "agent_loop.py (Option B)" (the exact post-drain hook) + 175-RESEARCH.md § "XPROV-02 Finding 5" (reuse `error`, do NOT invent a new SSE type; Option B keeps the per-chunk handler byte-identical).
    - Analog test: backend/tests/unit/test_openai_compat_dsml_strip.py.
  </read_first>
  <behavior>
    - A deepseek stream whose visible content contains the DSML opener → `stream.dsml_leaked` flips to True; the agent_loop post-drain hook emits exactly ONE `error` SSE event with a fixed honest message.
    - A clean deepseek turn (no opener) → `stream.dsml_leaked` stays False; NO error emit.
    - A non-deepseek stream → `dsml_leaked` never set truthy; NO error emit (byte-identical).
    - The error message is a FIXED string — it never interpolates the model's raw (attacker-controllable) output.
  </behavior>
  <action>
    In `_ClosableEventStream.__init__`, initialize `self.dsml_leaked = False`. Inside `_normalize`'s deepseek-gated strip branch (:314-317), when `_strip_deepseek_tool_markup` reports leaking has begun (the returned leaking flag is True), set `self.dsml_leaked = True`. Do NOT edit the per-chunk emit dicts or the shared `finish` dict (Option B — strictly safer under D-14).
    In `agent_loop.py`, in the post-drain block right after `_drain_stream_with_close_on_cancel(...)` (:2151-2156) and alongside the existing STRUCTURED post-processing (:2159), add: `if getattr(stream, "dsml_leaked", False): await _emit(redis, run_id, 'error', message=<fixed honest copy>)`. Reuse the existing `error` event vocabulary (:2291) — do NOT add a new SSE event type. Fixed copy, e.g. "The model tried to call a tool but wrote it as text, so it didn't run. Please retry." (exact wording Claude's discretion, D-02b). The `getattr` default keeps every non-deepseek / non-leak path byte-identical.
    Create `backend/tests/unit/test_dsml_leak_signal.py`: assert `_normalize` sets `stream.dsml_leaked` True on a deepseek leaking stream and leaves it False on a clean deepseek stream and (flag absent/False) on a non-deepseek stream; assert the post-drain guard emits exactly ONE `error` event when the flag is set and ZERO otherwise (a minimal harness around the `if getattr(stream, "dsml_leaked", ...)` guard with a mock `_emit` is sufficient).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_dsml_leak_signal.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Leak → `stream.dsml_leaked` True → exactly one `error` SSE event (existing vocabulary, not a new type).
    - Clean deepseek turn and non-deepseek stream → no emit; `dsml_leaked` stays False (D-14 default-inert; the shared per-chunk `_on_chunk` handler is unedited).
    - The honest message is a fixed string with NO raw model-output interpolation (render-injection / info-disclosure safe).
  </acceptance_criteria>
  <done>A detected DSML leak ends the turn honestly via one existing `error` event; clean and non-deepseek turns are byte-identical; the signal test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| provider model output → chat render | DeepSeek can emit tool-call markup as visible text — untrusted model output that must never reach the browser raw. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-02-01 | Tampering / info-integrity | DSML markup rendered as UI content | mitigate | The shipped `_strip_deepseek_tool_markup` (floor) + this plan's stream-end flush keep raw markup out of the delta stream; the browser never receives the opener token. |
| T-175-02-02 | Information Disclosure | honest-incomplete `error` copy | mitigate | The `error` message is a FIXED string; it never interpolates the model's (attacker-controllable) leaked output, so it cannot break the SSE contract or echo injected content. |
| T-175-02-03 | Tampering | new SSE event surface | accept | No new event type or endpoint is added — the signal reuses the already-rendered `error` event (D-02b), so no new frontend attack surface. |
| T-175-02-SC | Tampering | package installs | accept | No installs (RESEARCH Package Legitimacy Gate N/A); `git diff` shows zero requirements.txt / Dockerfile.sandbox changes. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` green.
- Assumption A1 (alternate DeepSeek opener variants beyond `<｜｜DSML｜｜`) is a live-repro test-coverage NOTE, not a blocker — keep the single opener + flush; broaden only if the long-turn DeepSeek UAT surfaces a different delimiter.
- SC#10 long-message axis (DeepSeek ~26+ tool-call turn → no dirty render + honest signal) is authored in 175-VALIDATION.md (manual — the leak is timing/length-dependent). Do not duplicate as a task.
</verification>

<success_criteria>
- Stream-end flush lands (deepseek-gated); the no-dirty-render floor holds.
- A detected leak emits exactly one existing `error` event via the Option-B post-drain hook; clean/non-deepseek paths byte-identical.
- Both unit proofs green; full unit suite green.
</success_criteria>

<output>
Create `.planning/phases/175-cross-provider-streaming-fidelity/175-02-SUMMARY.md` when done.
</output>
