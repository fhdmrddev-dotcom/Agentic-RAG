---
phase: 174-run-state-lifecycle-honesty
plan: 03
type: execute
wave: 2
depends_on: [174-01]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/components/chat/__tests__/MessageItem.blockedNotice.test.tsx
  - frontend/src/__tests__/providers/streamsProvider_state01b_403.test.tsx
autonomous: true
requirements: [STATE-01]
nyquist_compliant: true

must_haves:
  truths:
    - "STATE-01b: a workflows kill-switch 403 renders an honest in-chat AMBER bubble carrying the server's ApiError.message (e.g. 'Workflows are currently disabled by the administrator') instead of a workflow title with a blank body (D-04)"
    - "STATE-01b: the 403 branch replaces the empty assistant placeholder (keeps the user bubble) and clears the harness workflow-lock so the composer is never left locked (D-04)"
    - "STATE-01b: the existing Deep 400 (disabled-skill) + 409 (workflow-lock) rollback-banner paths stay byte-identical — the new branch is keyed narrowly to err.status===403 inserted before the generic ApiError branch (D-05)"
    - "STATE-01b: the amber bubble reuses the model-fallback-notice primitive (border-amber-400/30 bg-amber-400/10 text-amber-400/90) — no bespoke amber CSS (D-06)"
    - "Deep Mode byte-identical: no shared-path fork; the 403 fires before any run/message INSERT so the branch is a pure frontend placeholder-swap + lock-clear, no runs query (D-14)"
    - "Design bar 129-C: amber = administrative-block tier, louder than dim, quieter than red (D-15)"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "blockedNotice?: { message: string } render-only optional Message field (sibling of modelFallbackNotice)"
      contains: "blockedNotice"
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "amber blocked-notice render block (data-testid=blocked-notice) reusing the model-fallback-notice classes, server string as React text children only"
      contains: "blocked-notice"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "new err.status===403 catch branch between the 409 branch and the generic ApiError branch — placeholder-swap + clearWorkflowLockForThread"
      contains: "err.status === 403"
    - path: "frontend/src/__tests__/providers/streamsProvider_state01b_403.test.tsx"
      provides: "catch test — 403 sets blockedNotice + clears lock + user bubble survives; 400/409 unchanged"
      contains: "403"
  key_links:
    - from: "StreamsProvider.sendMessage catch"
      to: "assistant placeholder blockedNotice"
      via: "err.status===403 branch setMessagesForBucket map"
      pattern: "err.status === 403"
    - from: "StreamsProvider 403 branch"
      to: "clearWorkflowLockForThread"
      via: "owning threadId closure"
      pattern: "clearWorkflowLockForThread\\(threadId\\)"
    - from: "message.blockedNotice"
      to: "amber notice render"
      via: "MessageItem blocked-notice block"
      pattern: "blocked-notice"
---

<objective>
Build STATE-01b: when a workflow launch is refused by the workflows kill-switch (a `403` raised in `workflow_kickoff.py:200` before any run/message is inserted), `StreamsProvider.sendMessage`'s catch currently leaves a workflow title with a blank body. Add a narrow `err.status === 403` branch that replaces the empty assistant placeholder with an honest in-chat AMBER bubble carrying the server's `ApiError.message`, and defensively clears the harness workflow-lock so the composer is never left locked (D-04). Reuse the existing `model-fallback-notice` amber primitive (D-06). The existing Deep 400/409 rollback-banner paths stay byte-identical (D-05).

Purpose: A killed-workflow send currently reads as a silent blank card. The 129-C amber tier makes the administrative block honest ("disabled by the administrator") without escalating to the red failure tier or the red rollback-banner path.
Output: A render-only `blockedNotice` Message field, the amber render block in MessageItem, the 403 catch branch in StreamsProvider, and two new tests (component render + provider catch).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-run-state-lifecycle-honesty/174-SPEC.md
@.planning/phases/174-run-state-lifecycle-honesty/174-CONTEXT.md
@.planning/phases/174-run-state-lifecycle-honesty/174-RESEARCH.md
@.planning/phases/174-run-state-lifecycle-honesty/174-PATTERNS.md
@.planning/phases/174-run-state-lifecycle-honesty/174-UI-SPEC.md

<interfaces>
<!-- Extracted from live source this session — executor uses these directly. -->

The catch ladder to extend (StreamsProvider.tsx:2047-2093), INSERT the new branch between :2068 and :2069:
```
} catch (err) {
  if (err instanceof Error && err.name === "AbortError") { /* … */ }
  else if (err instanceof ApiError && err.status === 409) { /* rollback + reconcileErrors banner — KEEP */ }
  // ← INSERT: } else if (err instanceof ApiError && err.status === 403) { … STATE-01b … }
  else if (err instanceof ApiError) { /* 400 disabled-skill: rollback + reconcileErrors + failedSendDrafts — KEEP */ }
  else { /* network → runStatus:"failed" — KEEP */ }
}
```
Closure vars in scope: `surfaceId`, `threadId`, `assistantId`, `userMsg.id`, `content`, `err`.

The amber primitive to reuse verbatim (MessageItem.tsx:467-473 — D-06 / SPEC A7):
```
<div data-testid="model-fallback-notice"
  className="mt-2 rounded-md border border-amber-400/30 bg-amber-400/10 px-3 py-2 text-xs text-amber-400/90">
  {message.modelFallbackNotice.message}
</div>
```

Existing store action (already called at :1731 and :2023 — do NOT add a new mutator):
```
useStreamsStore.getState().actions.clearWorkflowLockForThread(threadId)   // impl streamsStore.ts:2511
```

The render-only field precedent (types/index.ts:~205):
```
modelFallbackNotice?: { disabledModel: string; fallbackModel: string; message: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the blockedNotice type + the amber render block + its component test</name>
  <files>frontend/src/types/index.ts, frontend/src/components/chat/MessageItem.tsx, frontend/src/components/chat/__tests__/MessageItem.blockedNotice.test.tsx</files>
  <read_first>
    - frontend/src/types/index.ts:~199-205 — the `modelFallbackNotice` optional render-only field to mirror (174-PATTERNS.md types analog)
    - frontend/src/components/chat/MessageItem.tsx:467-473 — the `model-fallback-notice` amber primitive to reuse verbatim (D-06 / A7); and :627-680 for where terminal-state blocks attach inside the assistant column (A1)
    - frontend/src/components/chat/__tests__/MessageItem.fallbackNotice.test.tsx — the render+testid test harness this new sibling file mirrors (174-PATTERNS.md test analog)
    - 174-UI-SPEC.md A1/A2/A7/A10/A23 — amber tier attaches inside the assistant column, reuses the exact classes, glyph+word, React-text-only (no dangerouslySetInnerHTML)
  </read_first>
  <action>
    In `types/index.ts`, add a render-only optional field `blockedNotice?: { message: string }` as a sibling of `modelFallbackNotice` (same optional, no-persistence, no-migration family). In `MessageItem.tsx`, add a NEW sibling render block (assistant-only, gated on `message.blockedNotice`) that reuses the `model-fallback-notice` amber classes verbatim (`mt-2 rounded-md border border-amber-400/30 bg-amber-400/10 px-3 py-2 text-xs text-amber-400/90`), gives it `data-testid="blocked-notice"`, pairs a glyph + the server message (color-never-alone, A10), and renders `message.blockedNotice.message` as React text children ONLY — never `dangerouslySetInnerHTML` (A23/XSS, matching the T-099-08-01 pattern). Attach it inside the assistant message column near the existing terminal-state renders (A1); do not add a new chat row and do not put it on the user bubble. Create `MessageItem.blockedNotice.test.tsx` (mirroring `MessageItem.fallbackNotice.test.tsx`) asserting a message with `blockedNotice:{message:"Workflows are currently disabled by the administrator"}` renders `getByTestId("blocked-notice")` with that verbatim server text, and that a message without `blockedNotice` renders no such element.
  </action>
  <verify>
    <automated>cd frontend && npm test -- MessageItem.blockedNotice.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `types/index.ts` declares `blockedNotice?: { message: string }`.
    - Source: `MessageItem.tsx` has a block with `data-testid="blocked-notice"` using the exact `border-amber-400/30 bg-amber-400/10 text-amber-400/90` classes and rendering `message.blockedNotice.message` as text children (no `dangerouslySetInnerHTML`).
    - Behavior: the block renders only for assistant rows with `blockedNotice` set; absent otherwise.
    - Test-command: `cd frontend && npm test -- MessageItem.blockedNotice.test.tsx` green; the verbatim server string is asserted.
  </acceptance_criteria>
  <done>blockedNotice field added, amber render block reuses the primitive + testid, XSS-safe React-text render, component test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the err.status===403 catch branch in StreamsProvider (placeholder-swap + lock-clear)</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx:2047-2093 — the catch ladder; insert the new branch BETWEEN the 409 branch (:2050-2068) and the generic ApiError branch (:2069-2084), keying it narrowly on `err.status === 403` (174-PATTERNS.md STATE-01b catch section)
    - frontend/src/providers/StreamsProvider.tsx:1731 + :2023 — existing defensive `clearWorkflowLockForThread(threadId)` call sites (reuse this action, owning threadId closure — A25)
    - 174-RESEARCH.md Pattern 2 + Pitfall 5 + Open Q5 — the 403 discriminator; the 403 fires BEFORE any run/message INSERT so there is no run_id / DB row to touch
    - 174-UI-SPEC.md A17/A25 — replace placeholder + clear lock; per-thread owning closure; 400/409 stay byte-identical
  </read_first>
  <behavior>
    - err instanceof ApiError && err.status===403 → assistant placeholder (id===assistantId) gets `blockedNotice: { message: err.message }`; the user bubble (userMsg.id) is KEPT (not rolled back)
    - the same 403 branch calls `clearWorkflowLockForThread(threadId)` (defensive, owning threadId closure)
    - err.status===409 → unchanged (rollback both bubbles + reconcileErrors banner)
    - err.status===400 (generic ApiError) → unchanged (rollback both + reconcileErrors + failedSendDrafts)
    - network / non-ApiError → unchanged (assistant placeholder runStatus:"failed")
    - the branch does NOT query `runs` or reference a run_id (none exists at 403 time)
  </behavior>
  <action>
    Insert a new `else if (err instanceof ApiError && err.status === 403)` branch between the existing 409 branch and the generic `else if (err instanceof ApiError)` branch, so the narrowest status matches first and the 400/409 rollback paths stay byte-identical (D-05). In the new branch: DIVERGE from the rollback shape — keep the user bubble, and map the assistant placeholder (`m.id === assistantId`) to set `blockedNotice: { message: err.message }` via `setMessagesForBucket(surfaceId, threadId, prev => prev.map(...))` (the server string flows verbatim to the amber bubble from Task 1). Then call `useStreamsStore.getState().actions.clearWorkflowLockForThread(threadId)` defensively with the OWNING `threadId` closure (A25 — never a global flag). Do NOT query `runs`, do NOT read a run_id, do NOT mark a run cancelled (Pitfall 5 — the 403 fires before any INSERT). Touch nothing in the 409 / 400 / network branches (D-05, byte-identical).
  </action>
  <verify>
    <automated>cd frontend && npm test -- StreamsProvider.anthropic-ordering streamsProvider_bug_260707</automated>
  </verify>
  <acceptance_criteria>
    - Source: StreamsProvider.tsx catch contains `else if (err instanceof ApiError && err.status === 403)` positioned before the generic `else if (err instanceof ApiError)` branch.
    - Source: that branch sets `blockedNotice: { message: err.message }` on the `assistantId` row and calls `clearWorkflowLockForThread(threadId)`; the user bubble is not filtered out; no `runs`/run_id reference inside it.
    - Behavior: `git diff` shows the 409, generic-400, and network branches unchanged (byte-identical, D-05).
    - Test-command: `cd frontend && npm test -- StreamsProvider.anthropic-ordering streamsProvider_bug_260707` differentially green (Deep byte-identical guard + send/stream lifecycle intact).
  </acceptance_criteria>
  <done>403 branch inserted with placeholder-swap + defensive lock-clear; 400/409/network byte-identical; no runs query; Deep-ordering + lifecycle tests differentially green.</done>
</task>

<task type="auto">
  <name>Task 3: New StreamsProvider catch test — 403 sets blockedNotice + clears lock; 400/409 unchanged</name>
  <files>frontend/src/__tests__/providers/streamsProvider_state01b_403.test.tsx</files>
  <read_first>
    - frontend/src/__tests__/providers/streamsProvider_bug_260707_01_streaming_flag.test.tsx — the `vi.hoisted` API-mock harness (mockPostMessage, mockSubscribeToRun, supabase auth mock) this test mirrors (174-PATTERNS.md "NEW StreamsProvider catch test")
    - frontend/src/__tests__/providers/streamsProvider_075_7_reconcile_race.test.tsx — sibling harness reference for driving the send path deterministically
    - 174-VALIDATION.md Wave-0 requirement for the StreamsProvider catch test
  </read_first>
  <action>
    Create a new provider test using the `vi.hoisted` mock harness (mirror `streamsProvider_bug_260707_01`): mock `@/lib/api` and `@/lib/supabase`. Drive a send whose `postMessage` rejects with `new ApiError("Workflows are currently disabled by the administrator", 403)`; assert the assistant bucket row for that send has `blockedNotice.message === "Workflows are currently disabled by the administrator"`, the user bubble survives in the bucket, and `clearWorkflowLockForThread` was invoked for the owning threadId. Add two regression rows proving the DIVERGENCE is narrow: a `409` rejection still rolls back BOTH bubbles + sets a `reconcileErrors` entry (no `blockedNotice`); a `400` ApiError rejection still rolls back BOTH + sets `reconcileErrors` + `failedSendDrafts` (no `blockedNotice`). These lock D-05 (400/409 byte-identical) alongside the new 403 behavior.
  </action>
  <verify>
    <automated>cd frontend && npm test -- streamsProvider_state01b_403.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `streamsProvider_state01b_403.test.tsx` uses the `vi.hoisted` mock harness and rejects `postMessage` with `new ApiError(..., 403)`.
    - Behavior: the 403 case asserts `blockedNotice` set + user bubble kept + `clearWorkflowLockForThread` called; the 409 case asserts both-bubble rollback + `reconcileErrors` and NO `blockedNotice`; the 400 case asserts rollback + `reconcileErrors` + `failedSendDrafts` and NO `blockedNotice`.
    - Test-command: `cd frontend && npm test -- streamsProvider_state01b_403.test.tsx` green.
  </acceptance_criteria>
  <done>New catch test green: 403 amber+lock-clear proven, 400/409 regression rows prove byte-identical divergence (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client error string | The workflow kill-switch `403` and its `ApiError.message` cross from the backend into the amber bubble render. The authorization decision itself is already enforced server-side (`workflow_kickoff.py`); 174 only RENDERS the refusal message. No new authz, no new endpoint, no new persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-03-01 | Tampering / Information disclosure | The amber blocked-notice rendering `ApiError.message` | mitigate | Render the server string as React TEXT children only — never `dangerouslySetInnerHTML` — matching the established T-099-08-01 pattern (the `:2069` branch's reconcileErrors render + RunStatusStrip). XSS-safe by construction (A23/V5). |
| T-174-03-02 | Information disclosure | Cross-thread lock/state leak in parallel threads | mitigate | `clearWorkflowLockForThread(threadId)` and `setMessagesForBucket(surfaceId, threadId, …)` use the OWNING `threadId` closure (per-thread keyed, `workflowLockByThread`), never a global flag — so Thread A's block never unlocks Thread B's composer (A25 / BUG-260523-01). |
| T-174-03-03 | Elevation of privilege | The 403 authz decision | accept | The kill-switch authorization is enforced server-side before any INSERT (`workflow_kickoff.py:200`); the client only renders the outcome. No client-side authz introduced. |
| T-174-03-SC | Tampering | npm installs | accept | No package installed this phase — nothing to vet. |

**One thing to guard:** the amber bubble renders the server's `ApiError.message` as React text (never HTML). No new trust boundary is created — the 403 is already authorized and refused server-side; this plan only makes the refusal honest in-chat.
</threat_model>

<verification>
- `cd frontend && npm test -- MessageItem.blockedNotice.test.tsx streamsProvider_state01b_403.test.tsx` green.
- `cd frontend && npm test -- StreamsProvider.anthropic-ordering streamsProvider_bug_260707` differentially green (Deep byte-identical + send/stream lifecycle).
- `git diff` on StreamsProvider.tsx shows the 409/400/network branches byte-identical (D-05).
- Manual (live UAT, 174-VALIDATION.md — run at /gsd:verify-work): Control Plane → Workflows kill-switch OFF; launch a workflow from chat → amber "disabled by the administrator" bubble, composer usable (not locked). Parallel-thread axis: Thread A blocked while Thread B (Deep) keeps its own composer state.
</verification>

<success_criteria>
- A workflows kill-switch 403 renders the honest amber "disabled by the administrator" bubble (server string verbatim, React text only), never a blank workflow card; the composer is not left locked (D-04).
- The amber tier reuses the model-fallback-notice primitive; no bespoke CSS (D-06); the amber tier reads louder than dim, quieter than red (129-C / D-15).
- The Deep 400/409 rollback-banner paths are byte-identical; no shared-path fork; no runs query in the 403 branch (D-05/D-14).
</success_criteria>

<output>
Create `.planning/phases/174-run-state-lifecycle-honesty/174-03-SUMMARY.md` when done.
</output>
