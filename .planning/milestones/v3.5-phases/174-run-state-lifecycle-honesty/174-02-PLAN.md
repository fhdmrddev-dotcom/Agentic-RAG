---
phase: 174-run-state-lifecycle-honesty
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/__tests__/components/MessageItem.test.tsx
autonomous: true
requirements: [STATE-01, STATE-02]
nyquist_compliant: true

must_haves:
  truths:
    - "STATE-01a: an empty-content assistant row with runStatus='cancelled' renders the 'cancelled — no output yet' affordance (data-testid=cancelled-no-output), never an empty avatar-only bubble — VERIFY not rebuild (D-01)"
    - "STATE-02: a stopped/cancelled-with-content run renders 'Response stopped' derived from message.runStatus, so it survives nav-away-and-back AND a full cold reload (D-01)"
    - "STATE-01a/02 are VERIFY-first: the render conditions (MessageItem.tsx:627/:670) and the cold-reload derive (threads.py:346-353 zip → api.ts _mapRow) already exist and must NOT be re-implemented (D-01/D-02)"
    - "Reserve surgical fix fires ONLY if live UAT contradicts the traced reload-derive chain; then patch the threads.py join / api.ts map, never new persistence (D-03)"
    - "Design bar 129-C: dim terminal tier (Stopped / Cancelled) keyed off persisted runs.status (D-15)"
  artifacts:
    - path: "frontend/src/__tests__/components/MessageItem.test.tsx"
      provides: "extended component assertions locking the empty-content cancelled render (STATE-01a) + the stop-indicator-from-runStatus render (STATE-02)"
      contains: "cancelled-no-output"
  key_links:
    - from: "message.runStatus"
      to: "cancelled-no-output affordance"
      via: "MessageItem.tsx:627 empty-content cancelled render"
      pattern: "cancelled-no-output"
    - from: "message.runStatus"
      to: "Response stopped indicator"
      via: "MessageItem.tsx:670 reload-derived render"
      pattern: "Response stopped"
---

<objective>
Verify-and-close STATE-01a (Deep cancelled-no-output) and STATE-02 (stop indicator survives nav + full cold reload). The live scout confirmed BOTH the render conditions (`MessageItem.tsx:627` cancelled-no-output; `:670` "Response stopped") and the cold-reload derive (`threads.py:346-353` zips `runs.status` onto each message by `message_id`; `api.ts:_mapRow` maps `run_status → runStatus`) already exist and are correct end-to-end. This plan LOCKS those invariants with component tests and holds a surgical fix in reserve — it does NOT rebuild the existing render/derive (D-01).

Purpose: STATE-01/02 are the highest-regression-risk requirements (Phase 147/D-03 shipped them). Locking them under a test guards against an accidental regression from the STATE-01b/03/04 edits elsewhere in this phase, and the live UAT (VALIDATION.md) proves the reload-derive on real provider streams.
Output: Extended `MessageItem.test.tsx` assertions + a documented reserve-fix contingency (no code change expected).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-run-state-lifecycle-honesty/174-SPEC.md
@.planning/phases/174-run-state-lifecycle-honesty/174-CONTEXT.md
@.planning/phases/174-run-state-lifecycle-honesty/174-RESEARCH.md
@.planning/phases/174-run-state-lifecycle-honesty/174-PATTERNS.md
@.planning/phases/174-run-state-lifecycle-honesty/174-VALIDATION.md

<interfaces>
<!-- The two render conditions to VERIFY (do NOT rebuild — 174-PATTERNS.md "VERIFY ONLY"). -->

MessageItem.tsx:627 (STATE-01a — empty-content cancelled, exists):
```
) : message.runStatus === "cancelled" ? (
  <div className="..." data-testid="cancelled-no-output">
    <Square .../><span className="italic">cancelled — no output yet</span>
  </div>
```
Reached ONLY in the falsy-content branch (a cancelled run WITH content renders content + the "Response stopped" indicator below).

MessageItem.tsx:670 (STATE-02 — persistent stop indicator, exists, reload-derived):
```
{(message.stopped || message.runStatus === "timed_out" ||
  (message.runStatus === "cancelled" && !!message.content)) && !isStreaming && (
  <div ...><Square .../><span className="italic">
    {message.runStatus === "timed_out" ? "Agent reached time limit" : "Response stopped"}
  </span></div>
)}
```

Reload-derive chain (VERIFIED end-to-end in 174-RESEARCH.md — no change expected):
- backend/app/api/threads.py:346-353 — `m["run_status"] = run["status"] if run else None` (keyed by message_id; populates 'cancelled' even for the empty content_len=0 early-cancel row)
- frontend/src/lib/api.ts:_mapRow (~:147-198) — `run_status → runStatus` on every getMessages/getSnapshot
- frontend/src/hooks/useMessages.ts — carries ZERO runStatus logic (StreamsProvider + api mapper own it)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MessageItem.test.tsx to lock the STATE-01a + STATE-02 render invariants</name>
  <files>frontend/src/__tests__/components/MessageItem.test.tsx</files>
  <read_first>
    - frontend/src/__tests__/components/MessageItem.fallbackNotice.test.tsx — the `renderWithTooltip` + `makeMessage(overrides)` + `getByTestId` harness this extension mirrors (174-PATTERNS.md "EXTEND MessageItem.test.tsx")
    - frontend/src/components/chat/MessageItem.tsx:627-644 (STATE-01a cancelled-no-output) + :670-680 (STATE-02 "Response stopped") — the render conditions under test (VERIFY, do not edit)
    - 174-VALIDATION.md Per-Task Verification Map — the STATE-01a/02 component rows
  </read_first>
  <action>
    Extend the existing MessageItem test (or add cases using its `makeMessage`/`renderWithTooltip` harness) to assert two persisted-derive invariants. STATE-01a: a message with `content:""` and `runStatus:"cancelled"` (and not streaming) renders the `data-testid="cancelled-no-output"` element with the verbatim text "cancelled — no output yet". STATE-02: a message with non-empty `content` and `runStatus:"cancelled"` (not streaming) renders the "Response stopped" indicator; and a message with `runStatus:"timed_out"` renders "Agent reached time limit". Add a guard row proving the empty-content cancelled case does NOT ALSO render the "Response stopped" indicator (the two are mutually exclusive by the `!!message.content` gate). These assert the render derives purely from `message.runStatus` (the field the reload-derive populates) — the component-level proof of STATE-02's reload persistence. Do NOT modify MessageItem.tsx (VERIFY only, D-01).
  </action>
  <verify>
    <automated>cd frontend && npm test -- MessageItem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: MessageItem.test.tsx contains a case with `runStatus:"cancelled"` + empty content asserting `getByTestId("cancelled-no-output")`.
    - Source: a case with `runStatus:"cancelled"` + non-empty content asserting the "Response stopped" text, and a `runStatus:"timed_out"` case asserting "Agent reached time limit".
    - Behavior: the empty-content cancelled case does not render "Response stopped" (mutual-exclusion guard holds).
    - Test-command: `cd frontend && npm test -- MessageItem.test.tsx` green (differential vs SEED-056 baseline); MessageItem.tsx is unchanged (`git diff --stat` shows only the test file).
  </acceptance_criteria>
  <done>The STATE-01a empty-content testid + STATE-02 stop-indicator-from-runStatus invariants are locked under test; no production file touched.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the reload-derive chain intact + document the reserve surgical fix (contingent, no code expected)</name>
  <files>frontend/src/__tests__/components/MessageItem.test.tsx</files>
  <read_first>
    - 174-RESEARCH.md "Verify STATE-01a reload-derive end-to-end" + Open Questions 1 & 2 — the traced persist→finalize→zip→map→render chain and the CONFIRMED-CLOSED conclusion
    - backend/app/api/threads.py:346-353 — the runs.status→run_status zip keyed on message_id (VERIFY unchanged)
    - frontend/src/lib/api.ts:~147-204 — the `_mapRow` run_status→runStatus mapping (VERIFY unchanged)
    - 174-VALIDATION.md "Manual-Only Verifications" — the STATE-01a DeepSeek early-cancel + cold-reload UAT and STATE-02 mid-stream-Stop + reload UAT (run at /gsd:verify-work)
  </read_first>
  <action>
    Confirm — by inspection, not edit — that the reload-derive chain is intact and matches the research trace: `threads.py:346-353` keys the zip on the message's own run by `message_id` (so an empty content_len=0 early-cancel row still gets `run_status='cancelled'`), and `api.ts:_mapRow` maps `run_status → runStatus` on every messages/snapshot hydrate. Record this confirmation in the plan SUMMARY. The RESERVE SURGICAL FIX is explicitly contingent: it fires ONLY IF the SC#10 live UAT (authored in 174-VALIDATION.md, run at /gsd:verify-work) surfaces a real reload-derive gap — in which case the fix is a narrow correction at the `threads.py` join or the `api.ts` map (never new persistence, no migration — D-03/D-14). Research expects NO fix. If the UAT contradicts, capture the exact failing row (provider, run_id, message_id, content_len) in the SUMMARY before touching either file. As a lightweight regression guard, add/keep a test assertion that the reload-derive contract holds at the render layer (already covered by Task 1's runStatus-driven cases). No production code change in this task.
  </action>
  <verify>
    <automated>cd frontend && npm test -- MessageItem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: the SUMMARY records the inspected reload-derive chain (threads.py:346-353 keyed on message_id; api.ts _mapRow run_status→runStatus) as intact/unchanged.
    - Behavior: no edit to threads.py or api.ts (reserve fix did not fire, per research expectation) — or, if it did, the SUMMARY documents the exact live-UAT failing row that justified it.
    - Test-command: `cd frontend && npm test -- MessageItem.test.tsx` green; `git diff --name-only` for this plan lists only the test file (no backend/api edit) unless the reserve fix fired.
  </acceptance_criteria>
  <done>Reload-derive chain confirmed intact and recorded; reserve fix documented as contingent and NOT fired (or fired-with-evidence); no persistence/migration added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verify-only over the existing persisted `runs.status` reload-derive + existing render conditions. No new endpoint, input, auth, or persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-02-01 | Tampering | The runs.status→runStatus reload-derive (threads.py zip / api.ts map) | accept | Read-only verification; server-owned `runs.status` is authoritative (FND-01/145). Any reserve fix corrects a derive, never introduces new state — no new tamper surface. |
| T-174-02-SC | Tampering | npm installs | accept | No package installed this phase — nothing to vet. |

**No new trust boundary, no new attack surface** — VERIFY-first; the render markers derive from the already-authoritative persisted `runs.status`.
</threat_model>

<verification>
- `cd frontend && npm test -- MessageItem.test.tsx` green (STATE-01a empty-content testid + STATE-02 stop-indicator-from-runStatus locked; differential vs SEED-056).
- Reload-derive chain confirmed by inspection (threads.py:346-353 + api.ts _mapRow) — recorded in SUMMARY.
- Manual (live UAT, 174-VALIDATION.md — run at /gsd:verify-work): DeepSeek early-cancel → full cold reload keeps "cancelled — no output yet"; a mid-stream Stop keeps "Response stopped" after (a) nav-away-and-back and (b) a full cold browser reload, on ≥1 provider.
</verification>

<success_criteria>
- The STATE-01a empty-content cancelled render and the STATE-02 stop-indicator-from-runStatus render are locked under component tests; neither production render is rebuilt (D-01).
- The cold-reload derive is confirmed intact; no new persistence/migration added (D-03/D-14).
- The reserve surgical fix is documented as contingent on the live UAT and (per research) does not fire.
</success_criteria>

<output>
Create `.planning/phases/174-run-state-lifecycle-honesty/174-02-SUMMARY.md` when done.
</output>
