---
phase: 177-v3-4-org-surface-polish
plan: 03
type: execute
wave: 2
depends_on: [177-01]
files_modified:
  - frontend/src/components/org/InvitationsTab.tsx
  - frontend/src/components/org/InvitationsTab.test.tsx
  - frontend/src/components/org/SsoTab.tsx
autonomous: true
requirements: [ORGUX-02]
must_haves:
  truths:
    - "D-08: InvitationsTab + SsoTab render status via the ONE shared StatusChip — 0 remaining inline CHIP_TONE_CLASS maps in either file"
    - "D-08/D-09: SsoTab's documented UPPERCASE off-grid status fork is RETIRED — the chip drops `uppercase tracking-wide` + `py-1` and snaps to the shared py-0.5 grid baseline"
    - "D-09: SsoTab's row container snaps from `px-4` to the shared row-anatomy grid baseline used by InvitationsTab"
    - "D-10: link-first delivery (resend surfaces a copyable fresh link) + the SSO victim-naming remove confirm are preserved verbatim"
  artifacts:
    - path: "frontend/src/components/org/InvitationsTab.tsx"
      provides: "Invitations list rendering StatusChip + RoleBadge; link-first preserved"
      contains: "StatusChip"
    - path: "frontend/src/components/org/SsoTab.tsx"
      provides: "SSO tab rendering StatusChip on-grid; victim-naming remove preserved"
      contains: "StatusChip"
  key_links:
    - from: "InvitationsTab.tsx / SsoTab.tsx"
      to: "StatusChip"
      via: "import + <StatusChip tone={...}>"
      pattern: "StatusChip"
---

<objective>
Make the two in-app management surfaces one management language (ORGUX-02, sketch 133-B): retire the
byte-identical `CHIP_TONE_CLASS` maps in InvitationsTab + SsoTab for the shared `StatusChip` (177-01),
kill SsoTab's documented UPPERCASE off-grid status fork, and snap the SsoTab row onto the shared 4px
row anatomy. Every honest behavior — link-first resend, the victim-naming SSO remove confirm,
honest-absent affordances — is preserved verbatim (D-10). This is visual/cohesion only (D-01).

Purpose: 133-B's thesis is "one chip, everywhere" — the SsoTab UPPERCASE fork + the duplicated chip
maps are the exact cohesion target. Output: both tabs on the shared chip + grid, honest behaviors intact.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/177-v3-4-org-surface-polish/177-CONTEXT.md
@.planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md
@.planning/sketches/133-org-management-surfaces/README.md
@.planning/phases/177-v3-4-org-surface-polish/177-01-SUMMARY.md

<interfaces>
<!-- Consumed from 177-01 -->
StatusChip({ tone: ChipTone, children, testId? })   // org/StatusChip.tsx — grid-correct pill
statusChipMeta(status): { label, tone }              // shared union mapper (invitation + sso states)
RoleBadge({ role })                                  // org/OrgIdentity.tsx
OrgAvatar({ initial, pending? })                     // org/OrgIdentity.tsx

<!-- Grid-correct row baseline to standardize on (PATTERNS §Row-anatomy, InvitationsTab.tsx:231-306) -->
row container: "flex flex-col gap-2 px-3.5 py-3"
inner:         "flex flex-wrap items-center gap-x-3 gap-y-2"
identity:      "flex min-w-0 flex-[2] items-center gap-3"   // (SSO uses flex-1 + a domain string, no avatar — fine)
chip/action slots: "flex-none"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InvitationsTab → StatusChip + RoleBadge + OrgAvatar (D-08/D-04/D-10)</name>
  <files>frontend/src/components/org/InvitationsTab.tsx, frontend/src/components/org/InvitationsTab.test.tsx</files>
  <read_first>
    - frontend/src/components/org/InvitationsTab.tsx (whole file — remove :43-49 roleLabel, :51-75 ChipTone+statusChip+CHIP_TONE_CLASS, :236-241 avatar, :254-279 badge+chip render)
    - frontend/src/components/org/InvitationsTab.test.tsx (the green contract to keep: getByText "Pending"/"Accepted", resend/revoke, honest-absent, `invitation-fresh-link`)
    - frontend/src/components/org/StatusChip.tsx + OrgIdentity.tsx (the 177-01 APIs)
    - .planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md §"StatusChip" / §"OrgIdentity"
  </read_first>
  <action>
    Delete the local `CHIP_TONE_CLASS` (:71-75), the `ChipTone` type (:51), and the `statusChip`
    mapper (:56-69) — replace with the shared `statusChipMeta` from `@/components/org/StatusChip`.
    Delete the local `roleLabel` (:43-49) — replace with the shared `RoleBadge`. In `InvitationRow`,
    render the role slot as `<RoleBadge role={inv.role} />` (drop the inline ◆/muted spans :254-266);
    render the status slot as `<StatusChip tone={status.tone} testId="invitation-status">{status.label}
    </StatusChip>` (status = statusChipMeta(inv.status)); replace the inline avatar (:236-241) with
    `<OrgAvatar initial={initial} />`. Keep the row anatomy verbatim (it IS the grid-correct baseline).
    PRESERVE verbatim: the `canInvite &&` honest-absent Send affordance, the resend/revoke buttons
    (only on pending, only for an inviter), the link-first fresh-link surface (:308-339, incl.
    `data-testid="invitation-fresh-link"`), the empty/loading states, and the InviteMemberDialog mount.
    Keep `data-testid="invitation-status"` (pass it as StatusChip's testId). Extend the existing test
    only if a selector moved; the four existing describe-blocks must stay green unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "CHIP_TONE_CLASS" frontend/src/components/org/InvitationsTab.tsx` → 0
    - `grep -c "◆" frontend/src/components/org/InvitationsTab.tsx` → 0 (badge now via RoleBadge)
    - file imports `StatusChip`, `statusChipMeta`, `RoleBadge`, `OrgAvatar`
    - `data-testid="invitation-fresh-link"` still present (link-first preserved); the resend/revoke honest-absent gate (`isPending && canInvite`) intact
    - `npx vitest run src/components/org/InvitationsTab.test.tsx` passes 100% (Pending/Accepted text, resend→fresh-link, honest-absent, role-picker)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/org/InvitationsTab.test.tsx --reporter=dot</automated>
  </verify>
  <done>InvitationsTab renders shared StatusChip + RoleBadge + OrgAvatar; local chip/role maps gone; link-first + honest-absent preserved; the existing test suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: SsoTab → StatusChip; RETIRE the UPPERCASE off-grid fork; snap row to grid (D-08/D-09/D-10)</name>
  <files>frontend/src/components/org/SsoTab.tsx</files>
  <read_first>
    - frontend/src/components/org/SsoTab.tsx (whole file — :25-29 the misleading header comment, :52-75 ChipTone+statusChip+CHIP_TONE_CLASS, :284-325 the row + the UPPERCASE chip :302-310, :340-360 the victim-naming confirm)
    - frontend/src/components/org/StatusChip.tsx (the 177-01 API)
    - .planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md §"StatusChip" (the RETIRE-this-fork block + the NOTE overriding SsoTab's "do not fix back" comment) and §"Row-anatomy + 4px-grid"
  </read_first>
  <action>
    Delete the local `CHIP_TONE_CLASS` (:71-75) and the `ChipTone` type (:52). KEEP the SSO-specific
    `statusChip(status)` mapper (:57-67) — its labels ("Pending approval" / "Active" / "Disabled") are
    SSO-domain copy — but render through the shared component: replace the inline UPPERCASE status
    `<span>` (:302-310) with `<StatusChip tone={chip.tone} testId="sso-status">{chip.label}</StatusChip>`.
    This RETIRES the off-grid fork: the shared StatusChip base has NO `uppercase tracking-wide` and uses
    `py-0.5` (not `py-1`). Snap the row container (:285) from `px-4 py-3` to the grid baseline
    `px-3.5 py-3` (match InvitationsTab). Snap the remove button + any half-step padding to the shared
    grid. UPDATE the header comment (:25-29) that documented the intentional off-grid UPPERCASE fork as
    "gate-required — do NOT fix" — remove/rewrite it to say the deviation was retired for phase-177
    cohesion (D-08/D-09), so the next reader isn't misled. PRESERVE verbatim: the create form, the
    `canManageSso &&` honest-absent CTA, the `pending_approval` waiting note, the `role="status"`
    Updating flash, the SP-metadata well, and — critically — the victim-naming remove confirm
    (:340-360: "Remove SSO for {domain}?" + "members who sign in with {domain} will fall back to email
    and password" + the two-step Confirm). Keep `data-testid="sso-status"` (as StatusChip's testId).
  </action>
  <acceptance_criteria>
    - `grep -c "CHIP_TONE_CLASS" frontend/src/components/org/SsoTab.tsx` → 0
    - `grep -c "uppercase tracking-wide" frontend/src/components/org/SsoTab.tsx` → the STATUS chip no longer uses it (the remaining matches, if any, are the SP-metadata micro-labels — the STATUS chip's `uppercase tracking-wide` at old :304 is GONE; verify the status-chip render is `<StatusChip ...>`)
    - `grep -c "px-4 py-3" frontend/src/components/org/SsoTab.tsx` on the connection ROW → 0 (snapped to `px-3.5 py-3`)
    - file imports `StatusChip`; the header comment (:25-29) no longer instructs "do NOT fix them back"
    - victim-naming preserved: `grep -q "fall back to email and password" frontend/src/components/org/SsoTab.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/org --reporter=dot</automated>
  </verify>
  <done>SsoTab renders the shared StatusChip on the 4px grid; the UPPERCASE off-grid fork + its misleading comment are retired; victim-naming remove + create form + SP-metadata preserved.</done>
</task>

</tasks>

<verification>
Security posture: NO threat model (D-01/D-03) — visual cohesion over the already-secured 167/168
surfaces; `canInvite`/`canManageSso` render-gates and all server contracts (`/org/*`, `/org/sso/*`)
untouched. RED LINES as guards:

- RED LINE (D-02): `git diff --name-only` shows `frontend/src/providers/StreamsProvider.tsx` UNTOUCHED.
- RED LINE (authz byte-identical): the `canInvite &&` / `canManageSso &&` honest-absent gates + polarity unchanged (`git diff` shows no gate condition edited).
- RED LINE (D-10): resend link-first + the victim-naming remove confirm bodies unchanged (grep the copy strings above).
- `cd frontend && npx tsc -b` passes.
- Differential (SEED-056): `npx vitest run src/components/org --reporter=dot` — InvitationsTab + OrgAdminShell suites green + 0 net-new failures vs baseline `6d145860`.
</verification>

<success_criteria>
InvitationsTab + SsoTab share one StatusChip on one 4px grid; the SsoTab UPPERCASE off-grid fork is
gone; link-first + victim-naming + honest-absent are byte-identical; no authz / StreamsProvider /
server contract touched.
</success_criteria>

<output>
Create `.planning/phases/177-v3-4-org-surface-polish/177-03-SUMMARY.md` when done.
</output>
