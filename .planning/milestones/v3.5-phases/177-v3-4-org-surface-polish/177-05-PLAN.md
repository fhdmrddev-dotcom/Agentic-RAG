---
phase: 177-v3-4-org-surface-polish
plan: 05
type: execute
wave: 2
depends_on: [177-01]
files_modified:
  - frontend/src/components/auth/AuthCardShell.tsx
  - frontend/src/pages/AuthPage.tsx
  - frontend/src/pages/AcceptInvitePage.tsx
  - frontend/src/components/auth/SignInForm.tsx
autonomous: true
requirements: [ORGUX-02]
must_haves:
  truths:
    - "D-14: AuthPage + AcceptInvitePage share ONE branded card shell (AuthCardShell) — the duplicated orb/card/sparkle markup is a single source"
    - "D-11: AcceptInvitePage's 6 states + SignInForm's error line route through the ONE shared HonestNotice keyed by severity"
    - "D-12: recoverable invite dead-ends (missing / invalid-404 / expired-or-revoked-409) render severity=calm (Info, muted) — never the alarming AlertTriangle; genuine system errors keep weight"
    - "D-13: SignInForm fail-open is preserved + made legible — a route outage reveals the password field with a calm reassuring HonestNotice ('nothing's wrong with your account'), never a lockout (T-168-07 / SC#3)"
    - "The auth/route logic is byte-frozen — handleContinue / handleSubmit / handleSsoEscape / the accept flow are unchanged; only copy + notice container + card shell change"
  artifacts:
    - path: "frontend/src/components/auth/AuthCardShell.tsx"
      provides: "Shared branded auth card shell (orbs + Card + sparkle header)"
      exports: ["AuthCardShell"]
    - path: "frontend/src/pages/AcceptInvitePage.tsx"
      provides: "6-state invite landing routed through HonestNotice; recoverable → calm"
      contains: "HonestNotice"
    - path: "frontend/src/components/auth/SignInForm.tsx"
      provides: "identifier-first sign-in with a legible fail-open calm note"
      contains: "HonestNotice"
  key_links:
    - from: "AcceptInvitePage.tsx / SignInForm.tsx"
      to: "HonestNotice"
      via: "severity-keyed callout"
      pattern: "HonestNotice"
    - from: "AuthPage.tsx / AcceptInvitePage.tsx"
      to: "AuthCardShell"
      via: "shared brand shell"
      pattern: "AuthCardShell"
---

<objective>
Make "coming into an org" read as one calm, error-honest product surface (ORGUX-02 pre-auth, sketch
132-B): consolidate the duplicated brand-card shell into ONE `AuthCardShell` (D-14), route the invite
landing's six states + the sign-in error line through the shared `HonestNotice` (177-01, D-11), recolor
recoverable invite dead-ends to CALM instead of alarming red (D-12), and make the sign-in fail-open
legible with a reassuring calm note (D-13). ALL auth/route/accept logic is byte-frozen — this is copy +
notice + shell polish only (D-01).

Purpose: 132-B's key insight is that most invite "errors" are recoverable dead-ends the user can fix,
so they must read calm; and fail-open is a safety property that must stay a reassuring degrade, never a
lockout. Output: one card shell, one notice vocabulary, calm recoverable states, a legible fail-open.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/177-v3-4-org-surface-polish/177-CONTEXT.md
@.planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md
@.planning/sketches/132-org-entry-failure-honesty/README.md
@.planning/phases/177-v3-4-org-surface-polish/177-01-SUMMARY.md

<interfaces>
<!-- Consumed from 177-01 -->
HonestNotice({ severity: "calm"|"progress"|"success"|"error", children })  // auth/HonestNotice.tsx
// calm = recoverable dead-end (Info, muted, NO red) · progress = in-progress (Loader2) ·
// success = joined/already (CheckCircle2 green) · error = genuine system failure (AlertTriangle, EARNED)

<!-- The verbatim-cloned brand shell to extract (AuthCardShell) — identical in both pages -->
// orbs: "bg-primary/10 rounded-full blur-3xl" + "bg-violet-500/10 ... blur-3xl"
// card: "w-full max-w-md ghost-border bg-card/80 backdrop-blur-sm shadow-xl"
// header: "gradient-primary" sparkle tile + CardTitle + CardDescription(subhead)
// Sources: AuthPage.tsx:15-44 and AcceptInvitePage.tsx:206-227 (byte-identical clone)

<!-- SignInForm fail-open logic — BYTE-FROZEN (T-168-07 / SC#3), SignInForm.test.tsx locks 6 beats -->
// handleContinue: catch { setPhase("password"); return }   // the OUTAGE fail-open path (D-13 note anchors here)
// the non-SSO reveal (sso === false → setPhase("password")) is a NORMAL reveal, NOT a degrade — no note there
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract AuthCardShell (D-14) + consume in AuthPage + AcceptInvitePage</name>
  <files>frontend/src/components/auth/AuthCardShell.tsx, frontend/src/pages/AuthPage.tsx, frontend/src/pages/AcceptInvitePage.tsx</files>
  <read_first>
    - frontend/src/pages/AuthPage.tsx (whole file — :15-44 the shell)
    - frontend/src/pages/AcceptInvitePage.tsx:206-227 (the verbatim-cloned shell)
    - .planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md §"HonestNotice" D-14 block (the identical orb/card/sparkle markup)
  </read_first>
  <action>
    Create `frontend/src/components/auth/AuthCardShell.tsx` exporting `AuthCardShell({ title, subhead,
    children })` = the shared brand shell: the full-screen centered container with the two background
    orbs (`bg-primary/10` + `bg-violet-500/10 blur-3xl`), the `Card ... ghost-border bg-card/80
    backdrop-blur-sm` with the `gradient-primary` sparkle header tile, the `CardTitle` = title, the
    `CardDescription` = subhead, and `CardContent` = children. Lift the markup verbatim (same tokens,
    same Sparkles glyph — no visual change). Then rewrite `AuthPage` to render `<AuthCardShell
    title="Agentic RAG" subhead={mode === "signin" ? "Sign in to your account" : "Create a new
    account"}>` wrapping the existing SignInForm/SignUpForm mode switch (keep its `useState<mode>` +
    handlers verbatim). Rewrite `AcceptInvitePage`'s return (:206-227) to render `<AuthCardShell
    title="You've been invited" subhead={subhead}>{body}</AuthCardShell>` — keep the whole accept flow,
    the state machine, `captureToken`, the redirect, and the per-state `subhead`/`body` computation
    verbatim (Task 2 reskins the body). Remove the now-duplicated inline orb/card divs from both pages.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/auth/AuthCardShell.tsx` exists and exports `AuthCardShell`
    - both AuthPage + AcceptInvitePage import + render `AuthCardShell`
    - `grep -c "blur-3xl" frontend/src/pages/AuthPage.tsx frontend/src/pages/AcceptInvitePage.tsx` → 0 in both (orbs now single-sourced in AuthCardShell)
    - AuthPage still renders "Agentic RAG"; AcceptInvitePage still renders "You've been invited" (titles preserved via the shell)
    - `npx tsc -b` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <done>One AuthCardShell is the single source of the brand card; both pages consume it; titles + flows preserved; the duplicated orb/card markup is gone.</done>
</task>

<task type="auto">
  <name>Task 2: AcceptInvitePage 6 states → HonestNotice; recolor recoverable → calm (D-11/D-12)</name>
  <files>frontend/src/pages/AcceptInvitePage.tsx</files>
  <read_first>
    - frontend/src/pages/AcceptInvitePage.tsx (whole file — :113-127 the honest error copy, :137-204 the 6 state bodies, esp. :178-203 the missing/error AlertTriangle+muted)
    - frontend/src/components/auth/HonestNotice.tsx (the 177-01 API)
    - .planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md §"HonestNotice" D-12 recolor block ("preserve the honest copy verbatim, only reskin the container/glyph")
  </read_first>
  <action>
    Route each state's body through `HonestNotice`, PRESERVING the existing honest copy strings verbatim
    (:116-124 error messages, :179/:193 subheads, the per-state paragraph copy):
    - `accepting` → `<HonestNotice severity="progress">` (the Loader2 "Accepting your invitation…").
    - `joined` / `already` → `<HonestNotice severity="success">` (the CheckCircle2 confirm copy).
    - `missing` → `<HonestNotice severity="calm">` — D-12: was `AlertTriangle` + muted (:182); recolor
      to CALM (Info glyph). Copy unchanged ("This invite link is missing its token… ask for a fresh link").
    - `error` → `<HonestNotice severity="calm">` for the recoverable 404 (invalid) + 409 (expired/revoked)
      messages (they are "ask for a fresh link", the user's to fix — D-12). Keep the generic
      non-recoverable fallback ("We couldn't accept this invitation…") — you MAY leave the generic
      fallback at calm too (all three are recoverable "ask for a fresh link"); reserve `severity="error"`
      only if a genuinely non-recoverable system failure is later distinguished. Do NOT render the
      alarming red AlertTriangle for any of missing/invalid/expired/revoked.
    Keep the "Go to the app" button in the missing/error bodies, the `goToApp`, and the entire accept
    `useEffect` + redirect verbatim. `auth` state keeps rendering SignInForm/SignUpForm (no notice).
  </action>
  <acceptance_criteria>
    - file imports `HonestNotice`
    - `grep -c "AlertTriangle" frontend/src/pages/AcceptInvitePage.tsx` → 0 (the missing/error alarming glyph is gone; recoverable states are calm)
    - the honest copy is preserved: `grep -q "ask whoever invited you" frontend/src/pages/AcceptInvitePage.tsx` (the fresh-link copy) and the 404/409 messages unchanged
    - joined/already render `severity="success"`; accepting renders `severity="progress"` (grep the severities)
    - `npx tsc -b` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <done>All 6 invite states route through HonestNotice; recoverable dead-ends read calm (no AlertTriangle); the honest copy + accept flow are byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: SignInForm error line → HonestNotice; add the legible fail-open calm note (D-11/D-13)</name>
  <files>frontend/src/components/auth/SignInForm.tsx</files>
  <read_first>
    - frontend/src/components/auth/SignInForm.tsx (whole file — :31-61 handleContinue fail-open, :113-127 the password reveal + the bare text-destructive error line)
    - frontend/src/components/auth/SignInForm.test.tsx (the 6 green beats — esp. fail-open reveal + "couldn't start sso" copy)
    - frontend/src/components/auth/HonestNotice.tsx (the 177-01 API)
    - .planning/phases/177-v3-4-org-surface-polish/177-PATTERNS.md §"HonestNotice" D-13 block ("add, don't change behavior")
  </read_first>
  <action>
    BYTE-FREEZE the auth logic: `handleContinue`, `handleSubmit`, `handleSsoEscape`, `getSsoRoute` /
    `signInWithSSO` call sites, and the `phase` state machine are UNCHANGED (T-168-07 / SC#3). Two
    additive polish changes only:
    (1) Replace the bare error line (:127 `{error && <p className="text-sm text-destructive">{error}</p>}`)
    with `{error && <HonestNotice severity="error">{error}</HonestNotice>}` — the copy (incl. "We
    couldn't start SSO sign-in…") is unchanged; only the container gains the earned error weight.
    (2) D-13 fail-open legibility: add a NEW display-only boolean state `const [degraded, setDegraded] =
    useState(false)`, set `setDegraded(true)` ONLY inside the `catch` block of `handleContinue` (the
    route-outage fail-open path, :42-46) — NOT on the normal non-SSO reveal (that is not a degrade).
    When `phase === "password" && degraded`, render a `<HonestNotice severity="calm">` near the revealed
    password field with reassuring copy: "Nothing's wrong with your account — sign-in routing is
    temporarily unavailable, so you can sign in with your password." This makes the outage-degrade
    legible without ever locking the form. Do NOT gate or alter any existing reveal path; `degraded` is
    pure display state.
  </action>
  <acceptance_criteria>
    - file imports `HonestNotice`; the bare `text-destructive` error `<p>` is replaced by `<HonestNotice severity="error">`
    - a `degraded` state exists and is set true ONLY in the `handleContinue` catch block (grep: `setDegraded(true)` appears exactly once, inside the catch) — NOT on the non-SSO reveal
    - the calm fail-open note renders only when `phase === "password" && degraded`
    - the auth handlers are unchanged: `grep -q "getSsoRoute" && grep -q "signInWithSSO"` and the `catch { ... setPhase(\"password\") ... return }` fail-open body intact
    - `npx vitest run src/components/auth/SignInForm.test.tsx` passes 100% (all 6 beats: resting state, SSO redirect, non-SSO reveal, fail-open reveal, redirect-failed copy, escape hatch)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/auth/SignInForm.test.tsx --reporter=dot</automated>
  </verify>
  <done>The sign-in error uses HonestNotice; the fail-open degrade shows a calm reassuring note only on the outage path; all auth logic is byte-frozen and the 6-beat test suite is green.</done>
</task>

</tasks>

<verification>
Security posture: NO threat model (D-01/D-03) — copy/notice/shell polish over the already-secured
167/168 pre-auth surfaces; the fail-open safety property (T-168-07 / SC#3) is PRESERVED, not changed;
no server contract (`getSsoRoute`, `acceptInvitation`, `signInWithSSO`) touched. RED LINES as guards:

- RED LINE (D-02): `git diff --name-only` shows `frontend/src/providers/StreamsProvider.tsx` UNTOUCHED.
- RED LINE (D-13 fail-open frozen): SignInForm.test.tsx's fail-open + redirect-failed + non-SSO-reveal beats stay green (the logic is byte-identical; only additive display state).
- `cd frontend && npx tsc -b` passes.
- Differential (SEED-056): `npx vitest run src/components/auth --reporter=dot` — SignInForm + HonestNotice suites green + 0 net-new failures vs baseline `6d145860`.
</verification>

<success_criteria>
Sign-in + the /invite landing share one card shell and one honest-notice vocabulary; recoverable invite
dead-ends read calm (never alarming red); fail-open stays a legible reassuring degrade (never a
lockout); all auth/route/accept logic + StreamsProvider + server contracts are byte-identical.
</success_criteria>

<output>
Create `.planning/phases/177-v3-4-org-surface-polish/177-05-SUMMARY.md` when done.
</output>
