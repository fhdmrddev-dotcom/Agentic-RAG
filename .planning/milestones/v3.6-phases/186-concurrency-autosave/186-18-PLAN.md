---
phase: 186-concurrency-autosave
plan: 18
type: execute
wave: 1
gap_closure: true
closes: [CR-03]
depends_on: []
# WAVE RATIONALE: this plan owns `WorkflowBuilderPage.tsx`, its canvas test and
# `186-VALIDATION.md`. 186-19 owns `useDraftPersistence.ts` + its test. ZERO overlap, so the
# two run in parallel in wave 1. 186-20 re-edits `186-VALIDATION.md` and therefore waits.
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
  - .planning/phases/186-concurrency-autosave/186-VALIDATION.md
autonomous: true
requirements: [CONCUR-02]

must_haves:
  truths:
    - "CR-03 — PUBLISH REFUSES AN OUTSTANDING WRITE ON EVERY SURFACE THE WRITE IS REACHABLE FROM, NOT ONLY ON THE FLAG-ON ONE. `blockedReason`'s `persistState.kind === \"saving\"` branch is evaluated BEFORE the `!canvasEnabled || builderPhase !== \"drafted\"` short-circuit, so a `saving` reading produced by `saveNow` on the flag-off Builder reaches `PublishGauntlet` instead of being discarded. Re-derivable by reading the memo: the `saving` line is the FIRST statement in the body."
    - "CR-03 / D-186-03 — THE ASYMMETRY IS HONOURED IN BOTH DIRECTIONS. D-186-03 keeps `saveNow` ungated on the canvas flag (`useDraftPersistence.ts` — only `haltedRef` and `holdRef` gate it), so an explicit Save on the flag-off Builder really does issue a PATCH. The refusal that guards that PATCH must therefore be ungated on the same flag: a write THIS client has outstanding is a fact about the client, not a verdict about the workflow."
    - "CR-03 / D-181-01 — THE REVERT SWITCH IS UNTOUCHED, AND IT IS MEASURED RATHER THAN ASSUMED. The verdict branches (`EMPTY_DRAFT_INVITATION`, the degraded sentence, the first verdict's message) stay behind the flag/phase check exactly as they shipped, and the flag-off resting header still matches `FLAG_OFF_HEADER_MARKUP` byte for byte in `WorkflowBuilderPage.header.test.tsx` — because the new reading is reachable only while a write the person explicitly requested is outstanding, which is unreachable at rest."
    - "CR-03 — THE HOLE IS CLOSED BY A TEST THAT OBSERVED IT OPEN. `WorkflowBuilderPage.canvas.test.tsx` presses `builder-save-draft` on the flag-OFF surface against a deferred `mockUpdate` and asserts the captured `blockedReason` is `SAVING_PUBLISH_WAIT`. RED (recorded before the fix): `null`. The existing flag-off row — which drives an AUTOSAVE-path edit and asserts zero calls and a null reason — stays green unchanged, because the `enabled` gate still suppresses automatic writes."
    - "CR-03 — THE LIVE CHECK SHIPS WITH THE FIX. `186-VALIDATION.md` carries a Manual-Only row for the flag-off Save-then-Publish sequence, worded against the FIXED behaviour (both controls refuse, no golden run is spent), so the operator's UAT pass measures the product rather than the defect."
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "a `blockedReason` whose outstanding-write branch is reachable on both branches of the canvas flag gate"
      contains: "SAVING_PUBLISH_WAIT"
    - path: "frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx"
      provides: "the flag-off Save-draft row that pins the refusal"
      contains: "builder-save-draft"
    - path: ".planning/phases/186-concurrency-autosave/186-VALIDATION.md"
      provides: "the CR-03 manual row in the Manual-Only Verifications table"
  key_links:
    - from: "useDraftPersistence.saveNow (an explicit Save on the flag-off surface)"
      to: "WorkflowBuilderPage.blockedReason"
      via: "persistState.kind === \"saving\", evaluated above the flag/phase gate"
      pattern: "persistState.kind === \"saving\""
    - from: "blockedReason"
      to: "PublishGauntlet's blocked trigger and inner Publish click"
      via: "the shipped renderPublish third argument"
      pattern: "renderPublish\\(definition, draftId, blockedReason"
---

<objective>
Close **CR-03**, the blocker the 2026-08-01 re-verification opened: 186-16 shipped the
refusal WR-10 asked for and placed it one line BELOW a short-circuit that unconditionally
returns `null` whenever `visual_workflow_canvas` is off — so the guard is dead code on
exactly the surface where the write it guards against is still reachable by design.

Re-derived from source on 2026-08-01 (not inherited): `WorkflowBuilderPage.tsx:1075-1084` is
the `blockedReason` memo; `:1076` is `if (!canvasEnabled || builderPhase !== "drafted") return
null`; `:1077` is `if (persistState.kind === "saving") return SAVING_PUBLISH_WAIT`. Confirmed
reachable on the same surface: `saveNow` (`useDraftPersistence.ts:852-863`) gates on
`haltedRef` and `holdRef` only — never on `enabled` (D-186-03, stated in its own docblock at
`:820-823`); and `actionGroup` (`:1704-1727`), which mounts BOTH `BuilderSaveRegion`'s
`builder-save-draft` button and `renderPublish(definition, draftId, blockedReason, …)`, is
rendered by the `canvasEnabled` branch (`:1741-1746`) AND the plain-header branch
(`:1748-1751`). Confirmed the consumer is sound: `PublishGauntlet.tsx:846` derives `blocked`
from the string, `:921` disables the trigger, `:592`/`:607` gate the inner Publish click that
spends the golden run.

The harm is economics and honesty, not data safety: the server's stage-0/stage-5 token guard
(`publish_service.py`, verification truth 3) still refuses the drifted publish and preserves
the golden-run receipt. What an author on the flag-off Builder gets today is a full golden
run burned and a confusing after-the-fact `draft_changed` refusal for an edit they made
BEFORE clicking Publish — the exact pre-flight guard CONCUR-02's requirement text promises.

Purpose: make the refusal a property of the client's own write loop rather than of the
feature flag, and prove it with a row that observed the hole open first.
Output: a two-line reorder in one `useMemo`, one new canvas-test row, the header markup pin
re-measured, and the CR-03 live row added to `186-VALIDATION.md`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/186-concurrency-autosave/186-VERIFICATION.md
@.planning/phases/186-concurrency-autosave/deferred-items.md
@.planning/phases/186-concurrency-autosave/186-16-SUMMARY.md

<interfaces>
<!-- Extracted from source 2026-08-01. Use these directly; do not go exploring. -->

frontend/src/pages/WorkflowBuilderPage.tsx
  :298  export const SAVING_PUBLISH_WAIT = "Saving your last change — Publish will be ready in a moment"
  :495  renderPublish?: (…, blockedReason?: string | null, …) — the shipped 4-arg seam
  :606  const canvasEnabled = useCanvasGate()
  :826  the hook's `enabled` prop is `canvasEnabled && builderPhase === "drafted"`
  :1075-1084  the `blockedReason` useMemo (deps: canvasEnabled, builderPhase, persistState, phases, validation)
  :1704-1727  `actionGroup` — BuilderSaveRegion + renderPublish(definition, draftId, blockedReason, setPublishInFlight)
  :1740-1752  the header gate — BOTH branches render `actionGroup`

frontend/src/hooks/useDraftPersistence.ts
  PersistState = idle | saving | saved{at} | held{sentence} | conflict{currentToken,note?} | error{sentence}
  :852-863  saveNow — gates on haltedRef and holdRef ONLY (D-186-03)

frontend/src/components/workflows/PublishGauntlet.tsx
  :846  const blocked = typeof blockedReason === "string" && blockedReason.trim().length > 0
  :921  outer trigger  disabled={blocked}
  :932-938  the inline `publish-blocked-reason` span, rendered only when `blocked`
  :592/:607  canPublish = goldenInput.trim().length > 0 && !loading && !blocked; runGauntlet returns early
  :694-706  the inner `publish-inner-blocked-reason` + disabled Publish button

frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
  :1766  describe("WorkflowBuilderPage 186-16 — an outstanding write blocks publish (WR-10)")
  :1767  FLAG_ON_186_16 = { features: { visual_workflow_canvas: true }, loading: false }
  :1768  FLAG_OFF_186_16 = { features: {}, loading: false }
  :1781-1800  renderCapturing(features, def?) → { seen, latest() } — captures every blockedReason
  :1802-1815  deferUpdate() → returns a release fn; the PATCH never answers until called
  :1817-1821  editOnce(text) — clicks `spine-node-research`, types into /instructions/i
  :1849-1862  the existing flag-off row ("with the flag OFF nothing writes and nothing blocks")

frontend/src/pages/WorkflowBuilderPage.header.test.tsx
  :~300  FLAG_OFF_HEADER_MARKUP — the three flag-off bands as one normalised literal
  :309/:316  the two byte-for-byte pins that consume it
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — press Save draft on the flag-off Builder and record what the gauntlet is told</name>
  <files>frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx (the whole `186-16` describe, from its opening line to end of file — the helpers `renderCapturing`, `deferUpdate`, `editOnce` are reused verbatim, and the existing flag-off row must be left byte-identical)
    - frontend/src/pages/WorkflowBuilderPage.tsx (the `blockedReason` useMemo and the `actionGroup` / header-gate region — the code under test)
    - frontend/src/hooks/useDraftPersistence.ts (`saveNow` and its docblock — the D-186-03 asymmetry that makes the write reachable flag-off)
    - frontend/src/components/workflows/BuilderSaveRegion.tsx (the `builder-save-draft` button, its `disabled={saving}` and `onSaveNow` wiring)
  </read_first>
  <behavior>
    - The flag-OFF Builder mounts `builder-save-draft`; pressing it issues exactly ONE `updateWorkflowDraft` call even though `visual_workflow_canvas` is off (the D-186-03 premise, asserted rather than assumed).
    - While that PATCH is unanswered, the `blockedReason` handed to the shipped `renderPublish` seam is `SAVING_PUBLISH_WAIT`.
    - When the PATCH answers, the reading returns to whatever it was before the press (measured as an equality against a captured `before`, never a hard-coded `null`).
    - The pre-existing flag-off row is unchanged and still green: an AUTOSAVE-path edit on the flag-off surface writes nothing and blocks nothing.
  </behavior>
  <action>
    Append ONE new `it(...)` row to the existing `describe("WorkflowBuilderPage 186-16 — an
    outstanding write blocks publish (WR-10)", …)` block in
    `frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx`. Do not create a new describe, do
    not touch the three rows already in it, and do not touch the `FLAG_OFF_186_16` fixture.

    Name the row for the property it proves — e.g. "a CHOSEN save on the flag-OFF surface
    blocks publish too (CR-03) — D-186-03's asymmetry cuts both ways".

    Drive it with the block's own helpers: call `deferUpdate()` to get the release function,
    mount with `renderCapturing(FLAG_OFF_186_16)`, wait for `builder-grid` (the flag-off
    surface's landmark — `builder-view-toggle` exists only with the flag on), capture
    `latest()` into a `before` const, run `editOnce(...)` once so the store is genuinely
    dirty, then `fireEvent.click(screen.getByTestId("builder-save-draft"))`.

    Assert, in this order: `mockUpdate` has been called exactly once (the write really is
    reachable flag-off — this is the premise `deferred-items.md` §"flushPendingWrites —
    REJECTED" states and the shipped fix failed to carry through); then
    `await waitFor(() => expect(latest()).toBe(SAVING_PUBLISH_WAIT))`; then call the release
    function and `await waitFor(() => expect(latest()).toBe(before))`.

    `SAVING_PUBLISH_WAIT` is already imported at the top of this file from
    `./WorkflowBuilderPage` — reuse that import, do not re-declare the literal (its single
    literal pin lives in the flag-ON row and must stay the only one).

    RUN THE ROW AND RECORD THE RED OUTPUT VERBATIM in the plan SUMMARY — the expected failure
    is at the `SAVING_PUBLISH_WAIT` assertion with a received value of `null`. If the row
    passes as written, STOP and report: that would mean the defect is not reproducible as
    described and the whole fix needs re-deriving before any source change.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx -t "flag-OFF"</automated>
  </verify>
  <acceptance_criteria>
    - The command above runs both flag-off rows; the NEW row FAILS and the pre-existing
      "with the flag OFF nothing writes and nothing blocks (D-181-01)" row PASSES.
    - The new row's failure is at the `latest()` / `SAVING_PUBLISH_WAIT` assertion with
      received `null` — NOT at the `mockUpdate` call-count assertion. A failure at the
      call-count assertion means the premise is wrong and the plan must stop.
    - The recorded RED output is pasted into the SUMMARY verbatim (command + failing
      expectation + received value).
    - `git diff --stat frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx` shows
      insertions only — zero deletions in this task.
    - `grep -c "it(" src/pages/WorkflowBuilderPage.canvas.test.tsx` is exactly one higher
      than before the task (record both numbers).
  </acceptance_criteria>
  <done>The hole is recorded as a failing test with its measured RED output, and the premise (a chosen save writes on the flag-off surface) is proven by call count rather than argued.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — lift the outstanding-write branch above the flag/phase gate, and re-measure the D-181-01 pin</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.tsx (the `blockedReason` useMemo and its full docblock — the docblock currently states the flag gate is load-bearing for ALL five branches, which is what has to change)
    - frontend/src/pages/WorkflowBuilderPage.header.test.tsx (the `FLAG_OFF_HEADER_MARKUP` literal and the two `byte for byte` pins that consume it — the D-181-01 promise being re-measured)
    - frontend/src/components/workflows/PublishGauntlet.tsx (the `blocked` derivation and both gated controls — the consumer that becomes live on the flag-off surface)
    - .planning/phases/186-concurrency-autosave/deferred-items.md (§"The publish-time `flushPendingWrites()` seam — REJECTED" — its premise is the reason this fix is a reorder and not a flush)
  </read_first>
  <action>
    In `blockedReason`'s `useMemo` body, move the outstanding-write branch
    (`if (persistState.kind === "saving") return SAVING_PUBLISH_WAIT`) so it is the FIRST
    statement, ABOVE `if (!canvasEnabled || builderPhase !== "drafted") return null`. Leave
    the flag/phase short-circuit exactly as it is written, and leave the four verdict branches
    below it (`phases.length === 0` → `EMPTY_DRAFT_INVITATION`, `validation.kind ===
    "degraded"`, the `ok`/non-verdict early return, and the first-verdict `message`)
    byte-identical and still behind that gate. The dependency array
    `[canvasEnabled, builderPhase, persistState, phases, validation]` is already correct and
    must not change.

    Amend the memo's docblock in the same edit — it currently says the memo is "GATED ON THE
    FLAG, and that is load-bearing (D-14 / D-181-01)" as a statement about the whole memo, and
    after this change that is only true of the VERDICT branches. State the new rule in the
    docblock's own voice and name the finding: the flag gate hides VERDICTS (a claim about the
    workflow, which D-182-06 says the server owns and D-181-01 says the reverted surface must
    not show); an outstanding write is a fact about THIS client's own write loop, carries no
    severity, no code and no tray row, never enters `verdicts`, and is reachable on the
    flag-off surface because D-186-03 deliberately leaves `saveNow` ungated while
    `actionGroup` mounts both the Save button and `renderPublish` on both header branches.
    Record CR-03 by id, and record that the resting flag-off header is unchanged because the
    reading requires a write the person explicitly requested to be outstanding.

    Change nothing else in this file. The measured budget for this task is a single-digit
    insertion count in the render/logic body — everything beyond that is docblock prose.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx src/components/workflows/PublishGauntlet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Task 1's new row now PASSES; the RED → GREEN transition is recorded with both measured
      outputs in the SUMMARY.
    - All five files above are green, and the aggregate test COUNT is recorded before and
      after — it must be non-decreasing (the Phase 177 lesson; the only permitted delta is
      Task 1's `+1`).
    - `WorkflowBuilderPage.header.test.tsx`'s two byte-for-byte pins ("matches the captured
      flag-off header byte for byte" and "an OPERATOR-LIKE map produces the IDENTICAL markup")
      pass WITHOUT any edit to `FLAG_OFF_HEADER_MARKUP`. If either pin reds, STOP and report —
      do not update the literal; a changed resting header means the fix reached further than
      the finding and the design needs revisiting.
    - Source assertion: in `WorkflowBuilderPage.tsx` the line matching
      `persistState.kind === "saving"` has a SMALLER line number than the line matching
      `!canvasEnabled || builderPhase !== "drafted"`, and both are inside the `blockedReason`
      memo.
    - Source assertion: `EMPTY_DRAFT_INVITATION`, `DEGRADED_SENTENCE` and the
      `validation.verdicts.find` branch all still have LARGER line numbers than the
      `!canvasEnabled` line — the verdicts stayed behind the flag gate.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports zero errors in any
      phase-186 file.
    - `git diff --stat frontend/src/pages/WorkflowBuilderPage.tsx` shows the logic change is a
      move, not a rewrite: no branch deleted, no dependency-array change.
  </acceptance_criteria>
  <done>A chosen save on the flag-off Builder refuses publish with the same wait reason the flag-on surface gives, the verdict branches remain hidden behind the revert switch, and the flag-off resting header is still byte-identical to the pinned literal.</done>
</task>

<task type="auto">
  <name>Task 3: ship the live check with the fix — the CR-03 row in 186-VALIDATION.md</name>
  <files>.planning/phases/186-concurrency-autosave/186-VALIDATION.md</files>
  <read_first>
    - .planning/phases/186-concurrency-autosave/186-VALIDATION.md (the "Manual-Only Verifications" table — its column shape, the ⛔ row-4 precedent for a blocked row, and the existing row 3b which is the nearest neighbour in subject matter)
    - .planning/phases/186-concurrency-autosave/186-VERIFICATION.md (the `human_verification` frontmatter — its LAST item is the CR-03 row's source text)
    - frontend/src/components/workflows/PublishGauntlet.tsx (the two testids a live observer can name: `publish-blocked-reason` beside the trigger, `publish-inner-blocked-reason` inside the modal)
  </read_first>
  <action>
    Append ONE row to the "Manual-Only Verifications" table in `186-VALIDATION.md`, numbered
    `8`, without altering any existing row (row 4's ⛔ BLOCKED entry in particular must stay
    exactly as it is — the scoreboard rule is that a blocked row is recorded with its reason,
    never dropped).

    Scenario column: the flag-OFF Builder — with `visual_workflow_canvas` off, open a draft,
    edit it, press **Save draft**, and within that same round trip open the Publish door and
    attempt the inner Publish click.

    Requirement column: CONCUR-02 / CR-03.

    Expected column: worded against the FIXED behaviour, not the defect. While the PATCH is
    outstanding the outer `◆ Publish…` trigger is disabled and carries the reason
    (`publish-blocked-reason`) reading *"Saving your last change — Publish will be ready in a
    moment"*; if the modal is already open, the inner Publish button is disabled and shows the
    same sentence (`publish-inner-blocked-reason`). No golden run is spent. The instant the
    save lands, both controls become clickable again and the reason disappears. State
    explicitly what a FAILURE looks like, so the operator can recognise it: a gauntlet that
    runs to a `draft_changed` refusal after burning a full golden run is the pre-fix
    behaviour.

    Status column: `to run`.

    Add one sentence beneath the table (or extend the existing note block) recording that row
    8 is the live confirmation of CR-03, that it is the flag-OFF twin of the flag-ON path
    already covered by unit tests, and that the server-side stage-0/stage-5 guard remains the
    safety backstop — this row measures the pre-flight refusal (economics + honesty), not data
    safety. Do NOT flip the "Approval:" line, and do NOT change any other row's status.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -c "^| " .planning/phases/186-concurrency-autosave/186-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The Manual-Only Verifications table has exactly one more row than before (record the
      before/after row counts of that table specifically, not of the whole file).
    - `grep -n "publish-blocked-reason" .planning/phases/186-concurrency-autosave/186-VALIDATION.md` matches the new row.
    - `grep -n "⛔" .planning/phases/186-concurrency-autosave/186-VALIDATION.md` still matches
      row 4, with its text unchanged (`git diff` shows no deletion on that line).
    - `grep -n "^\*\*Approval:\*\* pending" .planning/phases/186-concurrency-autosave/186-VALIDATION.md` still matches — this plan does not sign anything off.
    - The new row's Status cell reads `to run`.
  </acceptance_criteria>
  <done>The operator's next UAT session has a row for the exact sequence CR-03 names, worded so a pass measures the fixed product and a failure is recognisable on sight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → `PATCH /workflows/{id}` | the autosave/chosen write; the optimistic token (`If-Match`) crosses here |
| browser client → `POST /workflows/{id}/publish` | the gauntlet; stage-0 reads the draft token, stage-5 re-checks it before the flip |
| `blockedReason` (client-derived string) → rendered DOM | a client-authored constant is rendered next to the publish trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-18-01 | Tampering | the client publish pre-flight refusal (`blockedReason` → `PublishGauntlet.blocked`) | accept | The client guard is ECONOMICS AND UX, never the security boundary. A user who bypasses it (devtools, a stale bundle, a scripted POST) still meets the server's stage-0/stage-5 token guard in `publish_service.py`, which refuses the drifted publish with `draft_changed` and preserves the golden-run receipt (verification truth 3, VERIFIED and untouched by this plan). Nothing in this plan moves a decision from server to client. |
| T-186-18-02 | Information disclosure | `SAVING_PUBLISH_WAIT` rendered on the flag-off header via `publish-blocked-reason` | mitigate | The string is a fixed client-authored constant with nothing interpolated — no token, no draft id, no server body (the `RELOAD_FAILED_NOTE` / `WorkflowDraftUnreadableError` precedent). Task 2 changes only WHERE the branch is evaluated, never what it renders; the literal keeps its single pin in the flag-ON test row. |
| T-186-18-03 | Denial of service | the publish trigger being disabled while `persistState.kind === "saving"` | mitigate | The block is BOUNDED by construction: `performWrite`'s `finally` always clears `inFlightRef` and every terminal branch sets a non-`saving` state, so the disable always ends by itself. Task 1's row measures this directly — it releases the deferred PATCH and asserts the reading returns to its captured `before` value. The one unbounded branch WR-15 names is unreachable today and is recorded as a deferral by plan 186-20, not introduced here. |
| T-186-18-04 | Elevation of privilege | the D-181-01 revert switch | mitigate | The verdict branches stay behind `!canvasEnabled`, so the reverted surface still shows no server verdict. The only new reading on that surface describes the client's own outstanding request. Proven by re-running the two byte-for-byte header pins WITHOUT editing `FLAG_OFF_HEADER_MARKUP`; a red pin stops the plan. |
| T-186-18-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs nothing. Acceptance: `git diff --stat` shows no change to `frontend/package.json` or any lockfile. No `## Package Legitimacy Audit` is required because there are no package-manager install tasks. |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx src/components/workflows/PublishGauntlet.test.tsx` — all green, count non-decreasing (+1 from Task 1).
2. `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — zero errors in phase-186 files.
3. Source order assertion inside the `blockedReason` memo: the `saving` line precedes the `!canvasEnabled` line; every verdict branch follows it.
4. `FLAG_OFF_HEADER_MARKUP` is unedited (`git diff` on `WorkflowBuilderPage.header.test.tsx` is empty).
5. Zero migrations, zero package changes.
</verification>

<success_criteria>
- CR-03 is closed: a chosen Save on the flag-off Builder produces `SAVING_PUBLISH_WAIT` at the `renderPublish` seam, and both publish controls refuse while it is outstanding.
- The refusal was observed FAILING before it was made to pass, with both measurements recorded.
- D-181-01 holds: the flag-off resting header is byte-identical to the shipped pin, and no verdict reaches the reverted surface.
- `186-VALIDATION.md` carries the CR-03 live row, worded against the fixed behaviour, status `to run`.
</success_criteria>

<output>
Create `.planning/phases/186-concurrency-autosave/186-18-SUMMARY.md` when done. Record: the RED
output verbatim, the before/after test counts for all five suites, the measured `git diff --stat`
for `WorkflowBuilderPage.tsx`, and an explicit statement of whether the header pins moved.
</output>
