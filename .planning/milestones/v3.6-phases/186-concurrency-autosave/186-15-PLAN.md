---
phase: 186-concurrency-autosave
plan: 15
type: execute
wave: 9
gap_closure: true
closes: [WR-11, WR-06]
depends_on: ["186-13"]  # the shipped waves 1-8; no wave-9 plan precedes this one
# WAVE RATIONALE (hard constraint 8): this plan owns NO source file and no file any other
# 186-1x plan touches — one frontend test file and one backend test file — so it runs in
# parallel with 186-14 and 186-16 in wave 9. It must NOT be given `useDraftPersistence.ts`.
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.session.test.tsx
  - backend/tests/unit/test_186_concurrent_patch.py
autonomous: true
requirements: [CONCUR-01, CONCUR-02]

must_haves:
  truths:
    - "WR-11 — THE PANE-CLICK ROW MEASURES THE DISMISSAL, NOT THE CLOCK. `WorkflowBuilderPage.session.test.tsx` asserts that the dismissal ITSELF adds zero `updateWorkflowDraft` calls — a DELTA captured immediately before the pane click and read immediately after it settles — so a legitimate autosave that matured during the multi-second lazy-canvas wait can no longer fail a row that is not about autosave. The claim under test is unchanged and still falsifiable; only the way it is measured is. RED evidence to record: the shipped absolute assertion fails 3/3 runs including full isolation."
    - "WR-11 — THE DELTA IS A REAL MEASUREMENT, PROVED IN THE SAME TEST. After asserting the dismissal delta is 0, the same helper is wrapped around an explicit `builder-save-draft` click and asserts a delta of at least 1. A helper that could not see a PATCH would make the zero meaningless; this control is deterministic (the explicit save bypasses the debounce) and needs no clock."
    - "WR-11 — THE 186-12 / 186-13 SUMMARY CLAIM THAT THIS ROW 'PASSES IN ISOLATION' IS FALSE AND IS RECORDED AS FALSE. The SUMMARY for this plan re-measures `npx vitest run src/pages/WorkflowBuilderPage.session.test.tsx --testTimeout=30000` before and after and reports both, so no future reader inherits the claim on trust."
    - "WR-06 residue — THE `stale_token` 409 WIRE SHAPE IS COVERED WITHOUT A DATABASE. `backend/tests/unit/test_186_concurrent_patch.py` carries per-test `skipif` decorators instead of a module-level `pytestmark` (the 186-11 precedent from `test_186_publish_race.py`), and adds DB-free tests that assert `update_draft`'s three refusal mappings byte for byte: `stale_token` → 409 with `detail['code'] == 'stale_token'` and `detail['token']`; `already_published` → 409 with a coded detail; `not_found` AND any unrecognised cause → 404 with a STRING detail carrying no code. Re-derivable by running the file with `POSTGRES_DSN` pointed at an unreachable port and observing passes rather than skips."
    - "WR-06 residue — BOTH HALVES OF THE CONTRACT ARE NOW PINNED, SO THEY CANNOT DRIFT UNDER A GREEN SUITE (D-186-09). The client branches its entire conflict UX on `detail.code === 'stale_token'` + `detail.token` (`api.ts:3459-3460`) against a mock; these tests pin the server that must produce that shape, including the owner-scoped disambiguation in `update_workflow_definition` driven through a mocked `pool.fetchrow`."
    - "WR-06 residue — NO FIXTURE ENCODES A TOKEN FORMAT. Every token value in the new DB-free tests is an opaque sentinel (e.g. `T-NOW`), never a timestamp-shaped literal, so nothing in the suite can be read as a promise that the token is a parseable datetime (D-186-07 — the client echoes bytes and never parses them)."
    - "The test COUNT is guarded in both stacks (the Phase 177 lesson): the frontend session file and the backend collection are measured before and after and a DECREASE is a failure. Backend baseline 3465 collected; the session file baseline 23 (22 passed + 1 failed)."
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.session.test.tsx"
      provides: "a dismissal assertion that is not time-dependent, with its own positive control"
    - path: "backend/tests/unit/test_186_concurrent_patch.py"
      provides: "per-test live-DB skips plus DB-free coverage of the three refusal causes at both the route and the db tier"
      contains: "stale_token"
  key_links:
    - from: "update_workflow_definition's refusal dict"
      to: "the 409 detail the client branches on"
      via: "update_draft's cause mapping, asserted without a database"
      pattern: "stale_token"
---

<objective>
Close the two findings that are about EVIDENCE rather than product behaviour, and that both take
the same form: a suite that is green while saying nothing true.

WR-11 — `WorkflowBuilderPage.session.test.tsx:591`'s `expect(mockUpdate).toHaveBeenCalledTimes(0)`
fails DETERMINISTICALLY (3/3 runs, including full isolation). The cause is re-derived and is not a
product defect: the row types (arming a real 1000 ms debounce against a seeded `draft-1`), then
waits up to 10 s for the lazily-imported canvas pane; whenever that wait exceeds the debounce, a
perfectly legitimate autosave PATCH fires. The two sibling rows assert the same thing, skip the
lazy import, and pass. The claim is worth keeping — a pane click must not itself mint a write —
but it is encoded as an absolute count over an interval that now routinely exceeds the debounce.

WR-06 residue — `grep -rn "stale_token" backend/tests/` matches exactly one file, and that file is
entirely module-`pytestmark`-skipped. The client branches its WHOLE conflict UX on
`detail.code === "stale_token"` + `detail.token`, and that half is covered only against a mock.
The two halves can drift — `cause` renamed, `detail` flattened to a string, the `token` key
dropped — with every suite staying green.

Purpose: the phase's re-verification already falsified one measurement claim inherited from a
SUMMARY. Both fixes here make a claim checkable rather than believable.

Output: one honest frontend assertion with its own positive control; one backend file whose
refusal contract survives a CI with no Postgres.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/186-concurrency-autosave/186-VERIFICATION.md
@.planning/phases/186-concurrency-autosave/186-REVIEW.md
@.planning/phases/186-concurrency-autosave/186-VALIDATION.md
@.planning/phases/186-concurrency-autosave/186-CONTEXT.md
@CLAUDE.md

<decisions_this_plan_locks>
**1. WR-11 is fixed as a DELTA, not under fake timers.** The review offered both. The delta is
chosen because the session suite drives a real page with a lazily-imported canvas and
`@testing-library`'s `waitFor`, and swapping that file onto fake timers would change the timing
model of 22 other rows for the sake of one — a far larger blast radius than the finding. The
delta keeps the row's own claim exactly as written ("the dismissal issues no PATCH") and removes
only the part that was never the claim (what happened during the lazy-import wait).

**DELETING OR LOOSENING THE ASSERTION IS A FAILURE OF THIS PLAN.** `toHaveBeenCalledTimes(0)`
must not become "any number", the row must not be skipped, and the two sibling rows (✕, Escape)
keep their assertions. The delta must come with the explicit-save positive control described
below, because a delta helper that cannot observe a PATCH would turn the row into decoration.

**2. `mockCreate` keeps its ABSOLUTE zero.** The create path is a different branch — the session
harness seeds `draftId: "draft-1"`, so a create can never be legitimate here. Only the PATCH
assertion becomes a delta.

**3. WR-06's residue is closed IN THE EXISTING FILE, not in a new one.** `test_186_publish_race.py`
already shows the shape (per-test `skipif` + a shared `_LIVE_DB_REASON` constant + a docblock
paragraph naming WR-06). Copying that shape into `test_186_concurrent_patch.py` keeps one family
of tests in one file and keeps the diff readable. Rejected: a new `test_186_stale_token_wire.py`,
which would split one contract across two files and leave the original still fully skipped.
</decisions_this_plan_locks>

<interfaces>
<!-- Extracted from the codebase this session. RE-READ before editing. -->

`frontend/src/pages/WorkflowBuilderPage.session.test.tsx`:
- the describe at `:537` — *"every dismissal commits and none of them PATCHes"* — with
  `typeWithoutBlurring(text)` (`:539`) and `promptOf(slug)` (`:547`).
- three rows: ✕ (`:551`), Escape (`:567`), pane click (`:579`). The pane row switches to the
  canvas view, `await waitFor(() => container.querySelector(".react-flow__pane") !== null, LAZY)`
  where `LAZY` is the file's own long-timeout options object, then clicks the pane. `:591` is the
  failing line.
- `mockUpdate` / `mockCreate` are the file's module-level API mocks; `renderBuilder()` is its
  mount helper.

`backend/tests/unit/test_186_concurrent_patch.py`:
- `_DSN`, `_pg_reachable()`, `PG_AVAILABLE` (`:32-50`), then a module-level
  `pytestmark = pytest.mark.skipif(...)` (`:52-55`) — the thing to move.
- four live tests: F1 (`:97`), F2 (`:172`), F3 (`:242`), F13 (`:317`). All four need a real pool.
- the shipped idiom for a direct route call: patch `app.api.workflows.get_pg_pool` with an
  `AsyncMock`, then `await wf_api.update_draft(definition_id=…, body=…, current_user={"id": …},
  if_match=…)` and read `HTTPException.status_code` / `.detail`.

`backend/tests/unit/test_186_publish_race.py:37-49, 78-81, 108` — THE PRECEDENT to copy: the
docblock paragraph headed *"WHICH TESTS SKIP WITHOUT POSTGRES — WR-06"*, the one-home
`_LIVE_DB_REASON` constant, and the per-test decorator form.

`backend/app/api/workflows.py:999-1038` — `update_draft`'s mapping: `CheckViolationError` → 409
`_ALREADY_PUBLISHED_DETAIL`; `cause == "already_published"` → 409 `_ALREADY_PUBLISHED_DETAIL`;
`cause == "stale_token"` → 409 `{"code", "message", "token"}`; everything else (including
`not_found` and any unrecognised cause) → 404 with the STRING detail `"draft not found"`.

`backend/app/db/workflows.py:557-605` — `update_workflow_definition`: two `pool.fetchrow` calls
(the guarded UPDATE, then the owner-scoped probe), returning `{"ok": True, ...}` or one of
`{"ok": False, "cause": "not_found"}` / `"already_published"` / `{"cause": "stale_token",
"token": probe["token"]}`. `CONCURRENCY_TOKEN_SQL` is the only rendering of the token.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-11 — make the pane-click row measure the dismissal, with its own positive control</name>
  <files>frontend/src/pages/WorkflowBuilderPage.session.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.session.test.tsx (whole file — the module mocks and `renderBuilder()` at the top, the `LAZY` options object, and the entire `:535-618` describe; note how `mockUpdate` is given a resolved value in the file's setup, because Task 1's positive control depends on it)
    - .planning/phases/186-concurrency-autosave/186-VERIFICATION.md (the WR-11 Anti-Patterns row and the three Behavioral Spot-Check rows that measured it failing in three configurations)
    - .planning/phases/186-concurrency-autosave/186-VALIDATION.md ("Guard the COUNT, not just the failures" — the Phase 177 lesson this task must honour)
    - frontend/src/hooks/useDraftPersistence.ts (`AUTOSAVE_DEBOUNCE_MS` and its docblock — the 1000 ms the lazy wait outruns; read it so the docblock you write states the real reason rather than "flake")
  </read_first>
  <action>
    FIRST, MEASURE AND RECORD THE RED. Run
    `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.session.test.tsx --testTimeout=30000`
    three times and record the pass/fail counts and which row failed each time. The verifier
    measured 22 passed / 1 failed, 3/3. If your machine disagrees, record that too — the point of
    this step is that the next reader gets a number instead of a claim.

    Add a small helper beside `promptOf` in the same describe:
    `async function patchDelta(run: () => Promise<void> | void): Promise<number>` — it records
    `mockUpdate.mock.calls.length`, awaits `run()`, awaits the settle the row already performs
    (`waitFor` on the phase-form rail, exactly as the rows do today), and returns the difference.
    Docblock it with the WHOLE reason, because a bare delta reads like a weakened assertion:
      • the claim is about the DISMISSAL, not about the interval before it;
      • the pane row alone crosses a real `AUTOSAVE_DEBOUNCE_MS` (1000 ms) because it waits up to
        10 s for a lazily-imported canvas with a seeded `draftId` and a dirty store, so an
        autosave firing in that window is the product working, not a defect;
      • an absolute count therefore encodes a claim about wall-clock timing that the test never
        meant to make, and it failed 3/3 including in full isolation.
    Name WR-11 and state explicitly that the 186-12 and 186-13 SUMMARY claim that this row "passes
    in isolation" was FALSIFIED by `/gsd:verify-work` on 2026-08-01.

    Rewrite ONLY the pane-click row (`:579-593`) to use it:
      - keep `expect(promptOf("research")).toBe(text)` unchanged;
      - replace the absolute `expect(mockUpdate).toHaveBeenCalledTimes(0)` with
        `expect(await patchDelta(() => { fireEvent.click(pane) })).toBe(0)` — the click and its
        settle move inside the helper, so the measurement brackets exactly the dismissal;
      - keep `expect(mockCreate).toHaveBeenCalledTimes(0)` ABSOLUTE (locked decision 2);
      - rename the row so its title carries the sharpened claim, e.g.
        `"pane click — the pending value is in the definition and the DISMISSAL ITSELF issues no PATCH (WR-11)"`.

    Add the POSITIVE CONTROL as a second assertion inside the same row (not a separate row, so it
    cannot drift away from the thing it controls): after the delta-zero assertion, wrap an
    explicit `fireEvent.click(screen.getByTestId("builder-save-draft"))` in the same helper and
    assert the delta is at least 1. Comment it as the falsification: the explicit save bypasses
    the debounce and is deterministic, so if the helper could not observe a PATCH here the zero
    above would be measuring nothing.

    Leave the ✕ and Escape rows exactly as they are — they do not cross the debounce, and keeping
    their absolute assertions preserves the contrast that identified the cause.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.session.test.tsx --testTimeout=30000</automated>
  </verify>
  <acceptance_criteria>
    - The command exits 0 with ZERO failures, run THREE times consecutively; record all three
      results. (Baseline: 22 passed / 1 failed.)
    - The file's test count is >= 23 — the row was rewritten, never removed. Record before/after.
    - `grep -c "toHaveBeenCalledTimes(0)" frontend/src/pages/WorkflowBuilderPage.session.test.tsx`
      is at least 4 (the two sibling rows' `mockUpdate` + all three rows' `mockCreate`), proving
      no sibling assertion was loosened while fixing this one.
    - `grep -n "patchDelta" frontend/src/pages/WorkflowBuilderPage.session.test.tsx` shows one
      declaration and exactly two call sites, both inside the pane-click row.
    - `grep -n "toBeGreaterThanOrEqual(1)" frontend/src/pages/WorkflowBuilderPage.session.test.tsx`
      matches the positive control.
    - `grep -rn "it.skip\|describe.skip\|xit(" frontend/src/pages/WorkflowBuilderPage.session.test.tsx`
      returns nothing — the row was not fixed by disabling it.
    - `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx --testTimeout=30000` — 50 passed / 0 failed (the verifier measured 49 passed + 1 failed for this pair).
  </acceptance_criteria>
  <done>The row asserts what it always claimed, and can prove it would notice if the claim broke.</done>
</task>

<task type="auto">
  <name>Task 2: WR-06 residue (route tier) — the three refusal mappings, asserted without a database</name>
  <files>backend/tests/unit/test_186_concurrent_patch.py</files>
  <read_first>
    - backend/tests/unit/test_186_concurrent_patch.py (whole file — the module docblock, `_pg_reachable` / `PG_AVAILABLE` / `pytestmark` at :32-55, and all four live tests so their `skipif` decorators land in the right place)
    - backend/tests/unit/test_186_publish_race.py (:1-110 — the WR-06 precedent: the docblock paragraph, `_LIVE_DB_REASON`, and the per-test decorator form. Copy this shape; do not invent a second one)
    - backend/app/api/workflows.py (:944-1038 — `update_draft` in full, including `_ALREADY_PUBLISHED_DETAIL` and the fail-closed 404 comment)
    - .planning/phases/186-concurrency-autosave/186-REVIEW.md (WR-06's re-report, including the suggested test body)
    - .planning/phases/186-concurrency-autosave/186-CONTEXT.md (D-186-07's opaque-token constraint and D-186-09's two rules)
  </read_first>
  <action>
    Move the skip, exactly as 186-11 did for the publish-race file: delete the module-level
    `pytestmark`, add a one-home `_LIVE_DB_REASON` constant whose text is BYTE-IDENTICAL to the
    reason the `pytestmark` carried (so the skip report reads as it did), and decorate F1, F2, F3
    and F13 with `@pytest.mark.skipif(not PG_AVAILABLE, reason=_LIVE_DB_REASON)` above their
    existing `@pytest.mark.asyncio`. Add a docblock paragraph headed
    *"WHICH TESTS SKIP WITHOUT POSTGRES — WR-06 residue"* that states the finding in the verifier's
    own terms: the client branches its ENTIRE conflict UX on `detail.code == "stale_token"` and
    `detail.token`, that half is covered only against a mock, and until now the server half was
    absent from any CI without a local database — so the two halves could drift while every suite
    stayed green.

    Then add DB-free route-tier tests. Each patches `app.api.workflows.get_pg_pool` with an
    `AsyncMock` and `app.api.workflows.update_workflow_definition` with an `AsyncMock` returning
    the refusal dict under test, then calls `wf_api.update_draft(...)` directly with a
    `current_user` dict and a valid `WorkflowDefinition` body (the shipped direct-route-call
    idiom). No pool, no network, no database:

      • `stale_token` → `HTTPException` 409 whose `detail` is a dict with
        `detail["code"] == "stale_token"` and `detail["token"]` equal to the token the db layer
        returned. Assert the dict also carries a `message`, and assert the message is NOT what the
        client branches on by asserting `code` is present and exact — the D-186-09 rule.
      • `already_published` → 409 whose `detail` carries `code == "already_published"`, asserted
        against `wf_api._ALREADY_PUBLISHED_DETAIL` rather than a retyped literal.
      • `not_found` → 404 whose `detail` is a `str`, with NO `code` anywhere in it.
      • an UNRECOGNISED cause (e.g. `{"ok": False, "cause": "cause_minted_after_this_test_shipped"}`)
        → 404 with a detail BYTE-IDENTICAL to the `not_found` one. This is the fail-closed
        property the route's own comment claims and nothing currently checks.
      • the 404-collapse as an equality assertion: the `not_found` detail and the unrecognised
        detail compare equal, and neither is a dict — a coded 404 would be an existence oracle
        (T-186-01-03).
      • `CheckViolationError` raised by the mocked db function → 409 with the same
        `_ALREADY_PUBLISHED_DETAIL` (the published-row race arm, which also has no DB-free cover).

    EVERY token value in these tests is an opaque sentinel — use `"T-NOW"` and `"T-OLD"`. Comment
    that choice: D-186-07 makes the token an opaque string the client echoes and never parses, and
    a fixture written as a timestamp literal would quietly document a format that nothing is
    entitled to rely on.

    Do not modify any of the four live tests' bodies.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_186_concurrent_patch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_186_concurrent_patch.py -q` with the local stack UP: all tests pass, and the count is strictly greater than 4. Record it.
    - `cd backend && POSTGRES_DSN=postgresql://postgres:postgres@127.0.0.1:59999/postgres ./venv/Scripts/python.exe -m pytest tests/unit/test_186_concurrent_patch.py -q` exits 0 and reports the NEW tests as PASSED with exactly 4 skipped. Record the verbatim line (the pre-plan value is 0 passed / 4 skipped).
    - `grep -c "^pytestmark" backend/tests/unit/test_186_concurrent_patch.py` returns 0.
    - `grep -c "skipif(not PG_AVAILABLE, reason=_LIVE_DB_REASON)" backend/tests/unit/test_186_concurrent_patch.py` returns 4.
    - `grep -rn "stale_token" backend/tests/ | wc -l` is strictly greater than before AND matches at least two distinct files OR at least two DB-free tests in this file — record which.
    - `grep -n "2026-\|T00:00:00\|datetime" backend/tests/unit/test_186_concurrent_patch.py` shows no NEW timestamp-shaped token literal in any test added by this task (the shipped F3 `some_token` literal may stay).
    - `ls supabase/migrations | tail -1` is still `114_harness_audit_action_risk_pending.sql`.
  </acceptance_criteria>
  <done>The refusal contract the client depends on is asserted on a machine with no database.</done>
</task>

<task type="auto">
  <name>Task 3: WR-06 residue (db tier) — the three causes come out of a mocked pool, and the token is never bound as a datetime</name>
  <files>backend/tests/unit/test_186_concurrent_patch.py</files>
  <read_first>
    - backend/tests/unit/test_186_concurrent_patch.py (as edited by Task 2)
    - backend/app/db/workflows.py (:500-606 — `update_workflow_definition` in full: both `pool.fetchrow` branches, the owner-scoped probe and its T-186-01-02 comment, and the four return shapes; plus `CONCURRENCY_TOKEN_SQL`'s declaration and the section comment above it)
    - backend/tests/unit/test_186_publish_race.py (F6 — the mocked-pool, every-boundary-patched idiom that runs everywhere)
    - .planning/phases/186-concurrency-autosave/186-VERIFICATION.md (truth 2 and the Key Link row for `CONCURRENCY_TOKEN_SQL` — the properties this task pins without a database)
  </read_first>
  <action>
    Add DB-free tests for `update_workflow_definition` itself, driving a mocked pool whose
    `fetchrow` is an `AsyncMock` with a `side_effect` list — first the guarded UPDATE's result,
    then the probe's. Four rows, one per return shape:

      • UPDATE returns a row ⇒ `{"ok": True, ...}` carrying `id`, `version` and `token`, and the
        probe is NEVER called (assert `fetchrow` was awaited exactly once — the extra query is a
        failure-path cost only, which is what D-186-09 promised).
      • UPDATE returns None, probe returns None ⇒ `{"ok": False, "cause": "not_found"}`.
      • UPDATE returns None, probe returns `status="published"` ⇒ `"already_published"`.
      • UPDATE returns None, probe returns `status="draft"` with a token ⇒ `"stale_token"` and
        the probe's token is carried through verbatim.
      • the `token=None` defensive collapse: UPDATE returns None, probe returns a draft row, and
        no token was supplied ⇒ `"not_found"`, never `stale_token`. The route would map a
        `stale_token` here to a 409 for a request that carried no precondition at all.

    Then assert the two SQL properties that the verification report established by reading, so
    they survive without a reader:
      • the token-guarded call's SQL string contains `CONCURRENCY_TOKEN_SQL` (imported from
        `app.db.workflows`, never retyped) in BOTH the WHERE and the RETURNING, and the probe's
        SQL contains it too and also contains `created_by = $2` — the owner scope is a conjunct
        the token was added ALONGSIDE, never in place of (T-186-01-01);
      • no positional argument passed to `fetchrow` in any of these calls is a `datetime` —
        every token bind is a `str`. Comment this with the live-probed reason: asyncpg refuses a
        `str` bind against a `timestamptz` and a parsed-and-re-rendered token loses Postgres
        microseconds, so text-space comparison through the one constant is the whole contract
        (D-186-07). This is the server-side sibling of the client's F15 source fence.

    Use opaque sentinels for every token here as well.

    Finish by running the full backend suite and comparing the collected count against the 3465
    baseline. A DECREASE is a failure of this plan (the Phase 177 lesson).
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_186_concurrent_patch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_186_concurrent_patch.py -q` exits 0 with the local stack up.
    - The same command with `POSTGRES_DSN=…:59999` exits 0, reports 4 skipped, and reports strictly more PASSED tests than at the end of Task 2. Record the line.
    - `cd backend && ./venv/Scripts/python.exe -m pytest --collect-only -q | tail -3` reports a collected count strictly greater than **3465**. Record it.
    - `cd backend && ./venv/Scripts/python.exe -m pytest -q` — the full suite's failure count is not greater than the pre-plan run; record both runs.
    - `grep -n "from app.db.workflows import" backend/tests/unit/test_186_concurrent_patch.py` shows `CONCURRENCY_TOKEN_SQL` imported rather than the SQL fragment being retyped.
    - `grep -n "isinstance(.*datetime" backend/tests/unit/test_186_concurrent_patch.py` matches the bind-type assertion.
    - `git diff --stat supabase/migrations/` is empty.
  </acceptance_criteria>
  <done>The refusal disambiguation and the token's text-space contract are both proved without Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the test suite → the reader's belief | a green suite that does not exercise a contract is an assertion about the world that nobody made; this plan's whole subject is that boundary |
| `PATCH /workflows/{id}` → the client's conflict UX | the 409 body is a contract with two independently-changeable halves; the client's half is covered against a mock |
| a test fixture → a format promise | a token literal written as a timestamp documents a shape nothing is entitled to parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-15-01 | Repudiation (a guard that hides the invariant it defends) | `test_186_concurrent_patch.py`'s module `pytestmark` | mitigate | The skip becomes a property of the tests that need a database, so the refusal contract is asserted in any CI. Measured by running the file with `POSTGRES_DSN` pointed at an unreachable port and observing passes rather than a clean skip. |
| T-186-15-02 | Information disclosure | the 404-collapse under a NEW refusal cause | mitigate | A DB-free test asserts that an unrecognised `cause` fails closed to the same STRING 404 as `not_found`, and that the two details compare equal. Without it, a future cause added to the db layer could reach the wire as a coded 404 — an existence oracle (T-186-01-03) — and no test would notice. |
| T-186-15-03 | Information disclosure | the `stale_token` 409 carrying a token | accept | Disclosing the current token is deliberate and already accepted (T-186-01-04): the disambiguating probe is owner-scoped, so the token describes a row the caller already owns. The new tests assert the token is CARRIED, and deliberately assert nothing about its format. |
| T-186-15-04 | Tampering (a proxy or a future client reading a format) | the test fixtures' token literals | mitigate | Opaque sentinels only (`T-NOW` / `T-OLD`). Nothing in the added coverage can be cited as evidence that the token is a parseable datetime, which keeps D-186-07's "echo the bytes, never parse them" rule the only reading available. |
| T-186-15-05 | Repudiation (a false measurement claim inherited) | `WorkflowBuilderPage.session.test.tsx:591` | mitigate | The row is re-measured three times before and after and both numbers are recorded in the SUMMARY, so the "passes in isolation" claim cannot be inherited a third time. The delta carries its own positive control, so the assertion cannot become decoration. |
| T-186-15-SC | Tampering | npm / pip installs | accept | No dependency change in either stack; verified by an empty `git diff --stat frontend/package.json frontend/package-lock.json backend/requirements.txt`. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.session.test.tsx --testTimeout=30000` — 0 failures, three consecutive runs, count >= 23.
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_186_concurrent_patch.py tests/unit/test_186_publish_race.py tests/unit/test_103_published_409.py -q` — 0 failures with the live DB (baseline 12 passed).
- The same three files with `POSTGRES_DSN=…:59999` — exit 0, and strictly more than **3 passed** (the measured pre-plan DB-free figure). Record the passed/skipped split.
- `cd backend && ./venv/Scripts/python.exe -m pytest --collect-only -q | tail -3` — collected count > 3465.
- No source file outside `backend/tests/` and `frontend/src/pages/WorkflowBuilderPage.session.test.tsx` appears in `git status` for this plan.
</verification>

<success_criteria>
- The pane-click row passes deterministically and still fails if a dismissal ever mints a PATCH.
- The delta helper is proved able to observe a PATCH in the same test that relies on it.
- `test_186_concurrent_patch.py` reports passes, not a clean skip, on a machine with no Postgres.
- All three refusal causes plus an unrecognised one are asserted at the route tier, and all four db-tier return shapes at the db tier.
- Test counts are non-decreasing in both stacks, recorded before and after.
</success_criteria>

<output>
Create `.planning/phases/186-concurrency-autosave/186-15-SUMMARY.md` when done. It MUST record:
the three pre-fix and three post-fix session-suite results verbatim; the frontend session file's
before/after test counts; the backend file's passed/skipped split BOTH with the live DB and with
`POSTGRES_DSN` unreachable, before and after; the backend collected count before and after; and an
explicit statement that the 186-12 / 186-13 "passes in isolation" claim was false, so no later
reader inherits it.
</output>
