---
phase: 186-concurrency-autosave
plan: 11
type: execute
wave: 6
gap_closure: true
closes: [WR-06]
depends_on: ["186-08"]  # the shipped seam (waves 1-5); no gap-closure plan precedes this one
files_modified:
  - backend/tests/unit/test_186_publish_race.py
  - backend/tests/unit/test_186_concurrent_patch.py
  - backend/tests/unit/test_103_published_409.py
autonomous: true
requirements: [CONCUR-02]

must_haves:
  truths:
    - "No test that touches no database is gated by a live-Postgres skip in any of the three backend files this phase touched. Re-derivable by running the three files with POSTGRES_DSN pointed at an unreachable port: `test_the_golden_run_receipt_survives_a_draft_changed_refusal` and the two `*_maps_check_violation_to_409` tests are SELECTED and PASS, not skipped."
    - "The phase's headline backend invariant — a `-2` sentinel becomes a `draft_changed` block rather than `{published: True, version: -2}` plus a false `publish_succeeded` receipt — has CI coverage without local Postgres. Proven by the unreachable-DSN run above reporting at least 1 passed in `test_186_publish_race.py`."
    - "The seven live-DB tests remain skipped (never failed) when Postgres is unreachable, so the suite still never fails for want of a database. Proven by the same run exiting 0."
    - "No test was deleted or renamed: the collected count of the three files is identical before and after when Postgres IS reachable."
  artifacts:
    - path: "backend/tests/unit/test_186_publish_race.py"
      provides: "F6 running DB-free; the three live tests carrying their own skipif"
      contains: "pytest.mark.skipif"
    - path: "backend/tests/unit/test_103_published_409.py"
      provides: "the two mock-only 409-mapping tests running DB-free"
      contains: "pytest.mark.skipif"
  key_links:
    - from: "PG_AVAILABLE"
      to: "the seven live-DB test functions"
      via: "a per-test decorator instead of a module-level pytestmark"
      pattern: "@pytest.mark.skipif"
---

<objective>
Close WR-06: `backend/tests/unit/test_186_publish_race.py` carries a module-level
`pytestmark = pytest.mark.skipif(not PG_AVAILABLE, ...)` that also gates
`test_the_golden_run_receipt_survives_a_draft_changed_refusal` — a test that drives
`publish_service.publish` with `pool=AsyncMock()` and patches every boundary, touching no
database at all. `test_103_published_409.py` has the same shape over two mock-only route tests.

Purpose: the phase's headline backend invariant is the ONLY automated proof that the `-2`
sentinel becomes a `draft_changed` block instead of a false `publish_succeeded` receipt. Gated
behind a live-Postgres probe, it is absent in any environment without local Postgres — so the
invariant can regress under a green suite. That is the same "green because it was scoped to the
wrong thing" shape this phase exists to close.

Output: the skip becomes a property of the tests that need a database, not of the file.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/186-concurrency-autosave/186-VERIFICATION.md
@.planning/phases/186-concurrency-autosave/186-REVIEW.md
@.planning/phases/186-concurrency-autosave/186-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the codebase this session. -->

`backend/tests/unit/test_186_publish_race.py`:
- `_DSN = os.environ.get("POSTGRES_DSN", "postgresql://postgres:postgres@127.0.0.1:54322/postgres")`
- `def _pg_reachable(dsn=_DSN) -> bool` — a guarded `psycopg2.connect(dsn, connect_timeout=2)`
- `PG_AVAILABLE = _pg_reachable()` (:65)
- `pytestmark = pytest.mark.skipif(not PG_AVAILABLE, reason=...)` (:67-70) ← the whole problem
- four tests, each already carrying `@pytest.mark.asyncio`:
    :98  `test_a_draft_that_moved_is_refused_at_the_flip`            — LIVE (asyncpg pool)
    :154 `test_the_two_sentinels_are_distinguishable`                 — LIVE
    :208 `test_an_absent_token_keeps_todays_unguarded_flip`           — LIVE
    :254 `test_the_golden_run_receipt_survives_a_draft_changed_refusal` — DB-FREE
         (`pool=AsyncMock()`, `redis=AsyncMock()`, every boundary patched)

`backend/tests/unit/test_103_published_409.py`: same `PG_AVAILABLE` / `pytestmark` shape at
:49-52, four tests, of which :192 `test_patch_route_maps_check_violation_to_409` and
:232 `test_delete_route_maps_check_violation_to_409` are mock-only
(`patch("app.api.workflows.get_pg_pool", AsyncMock(...))`).

`backend/tests/unit/test_186_concurrent_patch.py`: same shape at :50-55, four tests
(:97, :172, :242, :317) — all four appear to be live-DB. VERIFY EACH BODY before touching it.

Backend runner: `cd backend && ./venv/Scripts/python.exe -m pytest ...` (venv is mandatory).
`asyncio_mode = auto` is set in `backend/pytest.ini`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the live-Postgres skip off the module and onto the tests that need a database</name>
  <files>backend/tests/unit/test_186_publish_race.py, backend/tests/unit/test_103_published_409.py, backend/tests/unit/test_186_concurrent_patch.py</files>
  <read_first>
    - backend/tests/unit/test_186_publish_race.py (the whole file — the module docblock, `_pg_reachable`, the `pytestmark`, and each of the four test bodies so you can classify them by what they actually touch)
    - backend/tests/unit/test_103_published_409.py (:1-60 the guard block, and the bodies at :89, :152, :192, :232)
    - backend/tests/unit/test_186_concurrent_patch.py (:1-60 the guard block, and the four test bodies at :97, :172, :242, :317)
    - backend/pytest.ini
  </read_first>
  <action>
    Classify every test in the three files by ONE criterion: does the body open a real database
    connection (an asyncpg pool, a psycopg2 connect, or a fixture that does) — or does it drive
    the code under test with `AsyncMock()` / `patch(...)` boundaries only? Record the
    classification for all twelve tests in the SUMMARY; that table is the evidence for the
    property, and it must be produced by reading the bodies, not by trusting the names.

    Then, in each file that contains at least one DB-free test, delete the module-level
    `pytestmark = pytest.mark.skipif(not PG_AVAILABLE, ...)` and put an equivalent
    `@pytest.mark.skipif(not PG_AVAILABLE, reason=...)` decorator on each LIVE test function
    instead — stacked with the `@pytest.mark.asyncio` decorator each already carries. Keep the
    reason string byte-identical to the module-level one so the skip report reads the same.

    Bind the reason to a module-level constant (a single `_LIVE_DB_REASON` string used by every
    decorator) so there is exactly one home for it and the seven decorators cannot drift apart.

    Leave `test_186_concurrent_patch.py`'s module mark IN PLACE if and only if every one of its
    four tests is genuinely live-DB — a file with no DB-free test has nothing to rescue, and
    churning it would be a change with no property behind it. Say so explicitly in the SUMMARY
    if that is the outcome. If any of its four tests turns out to be mock-only, apply the same
    treatment there.

    Update each touched module's docblock: the sentence describing the guard currently says the
    FILE skips cleanly when the local stack is down. Replace it with the honest rule — the tests
    that need a database skip when it is down; the tests that do not, run everywhere, because a
    guard that hides the invariant it defends is not a guard. Reference WR-06.

    Delete nothing and rename nothing. The collected count with Postgres reachable must be
    identical before and after.
  </action>
  <verify>
    <automated>cd backend && POSTGRES_DSN="postgresql://postgres:postgres@127.0.0.1:1/postgres" ./venv/Scripts/python.exe -m pytest tests/unit/test_186_publish_race.py tests/unit/test_186_concurrent_patch.py tests/unit/test_103_published_409.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0 and reports at least 3 PASSED (the F6 golden-run-receipt test
      plus the two `*_maps_check_violation_to_409` tests) with the remaining tests SKIPPED —
      never failed, never errored.
    - Running the SAME command against the pre-task tree reports 0 passed and 12 skipped;
      record both outputs in the SUMMARY as the RED/GREEN pair.
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_186_publish_race.py tests/unit/test_186_concurrent_patch.py tests/unit/test_103_published_409.py --collect-only -q | tail -1` reports the SAME collected count as before the change.
    - `grep -c "^pytestmark" backend/tests/unit/test_186_publish_race.py` returns 0.
    - `grep -c "^pytestmark" backend/tests/unit/test_103_published_409.py` returns 0.
    - Every `skipif` reason in the touched files references the one shared constant, not a
      re-typed string: `grep -c 'reason=_LIVE_DB_REASON' <file>` equals the number of live tests
      in that file.
  </acceptance_criteria>
  <done>The DB-free guards run without Postgres; the live ones still skip; nothing was deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the property both ways and guard the full-suite count</name>
  <files>.planning/phases/186-concurrency-autosave/186-VALIDATION.md</files>
  <read_first>
    - backend/tests/unit/test_186_publish_race.py (as edited in Task 1)
    - backend/tests/unit/test_103_published_409.py (as edited in Task 1)
    - .planning/phases/186-concurrency-autosave/186-VALIDATION.md (the counts contract: backend baseline 3457 collected; "guard the COUNT, not just the failures")
  </read_first>
  <action>
    Run the three files twice and record both outcomes in the SUMMARY as a two-row table.

    Run A — database UNREACHABLE, by exporting `POSTGRES_DSN` at port 1 for the command only.
    Expect: the DB-free tests selected and passing, the live ones skipped, exit 0.

    Run B — database REACHABLE, using the default DSN (local Supabase on 54322 must be up; if
    Docker is not running, say so in the SUMMARY and mark Run B as owed rather than inventing a
    result). Expect: every test selected, all passing, exit 0, and the collected count identical
    to the pre-change collected count.

    Then run the whole backend suite once and record the collected total against the 3457
    baseline in `186-VALIDATION.md`. A decrease is a failure of this plan even if every listed
    file is green — the Phase 177 lesson is that a change can silently remove coverage while the
    files you were looking at stay green.

    Finally, update the F-guard table in `186-VALIDATION.md`: F6's "Where" cell currently reads
    `backend/tests/unit/test_186_publish_race.py` with no note about its reachability. Add a
    parenthetical stating F6 is DB-free and runs without local Postgres, so the next reader can
    tell at a glance which guards survive a CI without a database. Change no other row.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest --collect-only -q | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - The collected total is >= 3457 (the `186-VALIDATION.md` baseline); the exact number is
      recorded in the SUMMARY.
    - The SUMMARY contains the two-row Run A / Run B table with real pytest output lines, not
      paraphrases.
    - The SUMMARY contains the twelve-test classification table from Task 1.
    - `186-VALIDATION.md`'s F6 row names the DB-free property; no other row of that table changed
      (`git diff .planning/phases/186-concurrency-autosave/186-VALIDATION.md` shows one row).
  </acceptance_criteria>
  <done>Both runs recorded, the backend collected count is non-decreasing, and F6's row is honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI environment → the test suite's verdict | a suite that reports green while silently skipping its headline invariant is a false attestation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-11-01 | Repudiation | the module-level `pytestmark` in the two 186 backend suites | mitigate | The skip becomes per-test, so an environment without Postgres still executes the DB-free proof that a `-2` sentinel cannot become `{published: True}` plus a false `publish_succeeded` audit row. Proven by an unreachable-DSN run reporting passes, not skips. |
| T-186-11-02 | Tampering | the seven live-DB tests | accept | They still skip rather than fail when the database is down — the shipped Phase 102 posture is deliberate and unchanged. The risk that a live-only regression escapes CI is the same as before this plan; it is not widened. |
| T-186-11-03 | Repudiation | test coverage accounting | mitigate | The collected count is asserted identical (three files) and non-decreasing (whole suite) before and after, so this plan cannot silently remove a test while appearing to add reachability. |
| T-186-11-SC | Tampering | pip installs | accept | No package is installed; `psycopg2` and `pytest` are already in the venv. Verified by an empty `git diff --stat backend/requirements.txt`. |
</threat_model>

<verification>
- `cd backend && POSTGRES_DSN="postgresql://postgres:postgres@127.0.0.1:1/postgres" ./venv/Scripts/python.exe -m pytest tests/unit/test_186_publish_race.py tests/unit/test_186_concurrent_patch.py tests/unit/test_103_published_409.py -q` — exit 0, >= 3 passed.
- `cd backend && ./venv/Scripts/python.exe -m pytest -q` — full suite green, collected >= 3457.
- No manual UAT row becomes newly observable from this plan; it is a coverage-reachability fix.
</verification>

<success_criteria>
- Every DB-free test in the three 186-touched backend files runs in an environment with no
  Postgres, and is observed passing there.
- The seven live-DB tests still skip cleanly rather than failing.
- The collected count is unchanged for the three files and non-decreasing for the suite.
</success_criteria>

<output>
Create `.planning/phases/186-concurrency-autosave/186-11-SUMMARY.md` when done. It MUST record:
the twelve-test DB/DB-free classification table, the Run A and Run B pytest output lines, the
before/after collected counts, and whether `test_186_concurrent_patch.py` was touched (with the
reason either way).
</output>
