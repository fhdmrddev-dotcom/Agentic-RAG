---
phase: 189-governed-external-action-node-model
plan: 06
type: execute
wave: 2
depends_on: ["189-01"]
files_modified:
  - supabase/migrations/115_workflow_phases_recorded_not_sent.sql
  - supabase/full-schema.sql
autonomous: false
requirements: [CONN-01]

must_haves:
  truths:
    - "D-08: migration 115 exists as supabase/migrations/115_workflow_phases_recorded_not_sent.sql and widens workflow_phases_status_check from five literals to six — no table, no column, no index, no grant, no RLS change."
    - "D-17: the CHECK gains the SLUG recorded_not_sent, a lowercase snake_case literal in the shape of the five that exist. The display sentence 'Not sent — recorded' is NOT in the constraint and is REJECTED by it."
    - "The operator applied the migration by pasting it into the Supabase SQL editor. Claude never ran supabase db push or supabase db reset — CLAUDE.md forbids both because they wipe local dev data."
    - "supabase/full-schema.sql was REGENERATED by scripts/regenerate-full-schema.sh with no --reset, never hand-edited, and committed together with the migration."
    - "V13 goes from green-skip to PASS, with BOTH controls: the slug is admitted and the sentence is rejected with SQLSTATE 23514 naming workflow_phases_status_check."
    - "There is NO Python literal set to pin for this column — unlike harness_audit, workflow_phases.status has no Python enum — so the 115 header must not claim a pin that does not exist."
  artifacts:
    - path: "supabase/migrations/115_workflow_phases_recorded_not_sent.sql"
      provides: "the sixth workflow_phases.status literal"
      contains: "recorded_not_sent"
    - path: "supabase/full-schema.sql"
      provides: "the regenerated single-file deploy artifact carrying the widened constraint"
      contains: "recorded_not_sent"
  key_links:
    - from: "supabase/migrations/115_workflow_phases_recorded_not_sent.sql"
      to: "public.workflow_phases workflow_phases_status_check"
      via: "DROP CONSTRAINT then ADD CONSTRAINT with six literals"
      pattern: "workflow_phases_status_check"
    - from: "backend/tests/test_migration_115.py"
      to: "the applied constraint on the live local DB"
      via: "a rollback-only positive and negative control"
      pattern: "recorded_not_sent"
---

<objective>
Author migration 115, have the OPERATOR apply it, and regenerate the bootstrap artifact.

Purpose: D-08 amends this phase's original zero-migration flag. Measured and re-verified at plan time,
`workflow_phases_status_check` caps `status` at exactly five values, and the live migration head is
114, so 115 is free. The precedent is near-exact — Phase 185 amended its own zero-migration promise for
migration 114, and the ROADMAP records why: the zero-migration promise was a scoping convenience; the
honest word is a correctness property, and the alternative knowingly ships the defect the phase existed
to fix.

⚠ **This plan contains a BLOCKING human checkpoint.** CLAUDE.md forbids `supabase db push` and
`supabase db reset` outright — they would wipe the operator's local dev data. The operator applies the
SQL by pasting it into the Supabase SQL editor. Claude cannot and must not apply it.

Output: one migration file, one regenerated `full-schema.sql`, committed together, and V13 turning
from green-skip to PASS.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md
@CLAUDE.md

RE-DERIVE BEFORE WRITING: run `ls supabase/migrations/` and confirm the head is still 114 and that no
115 exists. Measured at plan time: head 114, slot 115 free. If a 115 has appeared, STOP and report —
do not pick 115b, because a letter suffix is silently skipped by the Supabase CLI.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 115_workflow_phases_recorded_not_sent.sql</name>
  <files>supabase/migrations/115_workflow_phases_recorded_not_sent.sql</files>
  <read_first>
    - `supabase/migrations/114_harness_audit_action_risk_pending.sql` — read the WHOLE file. Its header
      carries eight load-bearing elements and 115 owes all of them: the WHY with measured evidence; the
      ALTER-not-CREATE note; the "adds exactly ONE literal and changes nothing else — no table, no
      column, no index, no grant" line; the does-NOT-touch-RLS line; the paste-into-the-SQL-editor
      instruction; the regenerate-full-schema follow-through; the "this plan ONLY AUTHORS the file — it
      is NOT applied here" line; and the ACCESS EXCLUSIVE lock note for the cloud parity window.
    - `supabase/full-schema.sql` — search `workflow_phases_status_check` and copy the LIVE form of the
      constraint verbatim, including its `= ANY (ARRAY[...])` shape and the `::text` casts.
    - `.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md` — decisions D-08 and
      **D-17**.
    - `backend/tests/test_migration_115.py` — authored in plan 189-01. Read the two controls so the SQL
      you write is the SQL they gate.
  </read_first>
  <action>
Create `supabase/migrations/115_workflow_phases_recorded_not_sent.sql`.

The filename must match the `<digits>_name.sql` pattern exactly. A letter suffix such as `115b` is
SILENTLY SKIPPED by the Supabase CLI — that is a real, recorded failure mode in this project.

The SQL is a `DROP CONSTRAINT` followed by an `ADD CONSTRAINT` on `public.workflow_phases`, restoring
the five shipped literals `pending`, `active`, `completed`, `failed`, `skipped` and adding exactly ONE
more: **`recorded_not_sent`**.

⚠ **D-17 IS THE TRAP IN THIS FILE.** The constraint takes the SLUG — lowercase, snake_case, in the
shape of the five that already exist. The rendered sentence "Not sent — recorded" is what the client's
vocabulary layer produces for that slug, and it lives 400 lines and one language away. Putting display
prose inside a database constraint would make the wording un-editable without a second migration, would
put an em-dash in a CHECK, and would break the shape of the column. A plan that adds the sentence has
misread the decision, and `test_migration_115.py`'s negative control exists to catch exactly that.

TWO DELIBERATE DEPARTURES FROM THE 114 ANALOG, both stated in the header:

1. Use `= ANY (ARRAY[...])` with `::text` casts, NOT `IN (...)`. 114 used `IN`; the SHIPPED
   `workflow_phases` constraint uses `= ANY (ARRAY[...])`. Matching the live shape keeps the
   regeneration diff to one line instead of a reformat.
2. **There is NO Python literal set to pin.** 114's header points at `test_audit_event_registration.py`
   because `harness_audit`'s event kinds are mirrored by a Python allow-list.
   `workflow_phases.status` has no Python enum — the statuses are string literals inside the four
   `UPDATE`s in `backend/app/db/workflows.py`. **The 115 header must NOT claim a pin that does not
   exist.** Name the real counterpart instead: the new `record_phase_not_sent` write function landing
   in plan 189-11, and `backend/tests/test_migration_115.py` as the live-DB gate.

One further fact worth recording in the header, because it changes how the file should be reviewed:
`workflow_phases_status_check` appears in NO existing migration file — it predates the numbered-migration
era and exists only in the live DB and the dump. So 115 is the FIRST migration ever to touch it, and
there is no earlier ALTER to copy verbatim. `DROP CONSTRAINT` is still correct because the constraint
exists by that name in the live DB, which is what the dump reflects.

**This task ONLY AUTHORS the file. It does not apply it.**
  </action>
  <verify>
    <automated>grep -c "recorded_not_sent" supabase/migrations/115_workflow_phases_recorded_not_sent.sql</automated>
  </verify>
  <acceptance_criteria>
    - The file exists at exactly `supabase/migrations/115_workflow_phases_recorded_not_sent.sql` and the
      filename matches `<digits>_name.sql` with NO letter suffix.
    - `grep -c "recorded_not_sent"` returns at least 1.
    - The file contains NO occurrence of the display sentence — verify `grep -c "Not sent"` returns 0.
    - The constraint form is `= ANY (ARRAY[` and NOT `IN (` — verify by grep.
    - All five shipped literals `pending`, `active`, `completed`, `failed`, `skipped` are present in the
      re-added constraint. A migration that drops one of them is a data-corrupting change.
    - The header contains the paste-into-the-SQL-editor instruction, the phrase "NEVER" alongside
      `db push` / `db reset`, the ALTER-not-CREATE note, the does-NOT-touch-RLS note, the ACCESS
      EXCLUSIVE lock note, and an explicit statement that NO Python literal set mirrors this column.
    - `grep -c "test_audit_event_registration" supabase/migrations/115_workflow_phases_recorded_not_sent.sql`
      returns 0 — that pin belongs to 114 and claiming it here would be a false claim.
    - `cd backend; venv/Scripts/python.exe -m pytest tests/test_migration_115.py -q --no-header -rs`
      still SKIPS (the file is authored, not applied).
  </acceptance_criteria>
  <done>The migration is authored, carries 114's eight header elements plus its two departures, stores the slug, and is not yet applied.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING — OPERATOR] Apply migration 115 via the Supabase SQL editor</name>
  <files>supabase/migrations/115_workflow_phases_recorded_not_sent.sql (applied by the operator — Claude writes nothing here)</files>
  <action>
PAUSE. This task is a BLOCKING OPERATOR CHECKPOINT and Claude performs NO write in it.

CLAUDE.md forbids `supabase db push` and `supabase db reset` outright — both would wipe the operator's
local dev data — so the migration is applied by the operator pasting it into the Supabase SQL editor.
Present the SQL body of `supabase/migrations/115_workflow_phases_recorded_not_sent.sql`, state plainly
that this widens `workflow_phases_status_check` by exactly ONE literal and changes nothing else, and give
the verification query below. Then WAIT. Do not proceed to Task 3 until the operator reports the
constraint definition.
  </action>
  <what-built>
    `supabase/migrations/115_workflow_phases_recorded_not_sent.sql` is authored and committed. It widens
    `public.workflow_phases`'s `workflow_phases_status_check` from five status literals to six by adding
    the slug `recorded_not_sent`. It changes no table, no column, no index, no grant and no RLS policy.
    `backend/tests/test_migration_115.py` is already in the tree and currently GREEN-SKIPS with a reason
    naming this task.
  </what-built>
  <how-to-verify>
    ⚠ **Claude must NOT run this step.** `CLAUDE.md` forbids `supabase db push` and `supabase db reset`
    outright — both would wipe your local dev data. This is the project's standing migration protocol.

    1. Open the local Supabase SQL editor (Supabase Studio for the local stack on `:54322`).
    2. Open `supabase/migrations/115_workflow_phases_recorded_not_sent.sql`, copy its SQL body (the two
       ALTER statements — the header comments are optional), and paste it into the SQL editor.
    3. Run it. Expected: success, no rows returned. The DROP+ADD takes a brief ACCESS EXCLUSIVE lock on
       `workflow_phases`; on local dev this is immaterial.
    4. Confirm it applied. In the same editor run:
       `select pg_get_constraintdef(oid) from pg_constraint where conname = 'workflow_phases_status_check';`
       The returned definition must list SIX literals and include `recorded_not_sent`.
    5. Reply here with the returned constraint definition, or with the error text if it failed.

    ⚠ **Do NOT apply this to the cloud database.** Migration 115 joins the standing owed cloud-parity
    queue (migs 099 onward) and is applied to cloud only in the operator-driven parity window, in order,
    at the next production push.
  </how-to-verify>
  <resume-signal>Paste the `pg_get_constraintdef` output (or the error) and type "applied" to continue.</resume-signal>
  <verify>
    <human-check>The operator has pasted the returned `pg_get_constraintdef` output, and it lists SIX status literals including `recorded_not_sent`.</human-check>
    <automated>cd backend; venv/Scripts/python.exe -m pytest tests/test_migration_115.py -q --no-header -rs</automated>
  </verify>
  <acceptance_criteria>
    - The operator has reported a constraint definition containing SIX literals including
      `recorded_not_sent`.
    - No `supabase db push` and no `supabase db reset` was executed by anyone — confirm in the SUMMARY.
  </acceptance_criteria>
  <done>The live local database accepts the new status slug; the operator has the constraint definition in hand.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate full-schema.sql and take V13 from green-skip to PASS</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - `scripts/regenerate-full-schema.sh` — read its options. The DEFAULT dumps the live DB schema with
      NO reset. `--reset` is destructive (it wipes the local DB) and is only for CI / release
      verification. This task uses the DEFAULT.
    - `supabase/SETUP.md` — the full setup story for how `full-schema.sql` is used (the single-file
      deploy artifact for greenfield environments).
    - `backend/tests/test_migration_115.py` — the two controls that must now run rather than skip.
  </read_first>
  <action>
Run `bash scripts/regenerate-full-schema.sh` with NO `--reset`. This dumps the live local DB schema and
rebuilds `supabase/full-schema.sql`.

**Never hand-edit `supabase/full-schema.sql`.** If the regenerated diff looks wrong, the fix is to
correct the migration and have the operator re-apply — not to touch the artifact.

Inspect the diff. It should be small and centred on `workflow_phases_status_check`. If the regeneration
reformats large unrelated regions, STOP and report it in the SUMMARY rather than committing a diff
nobody can review — the `= ANY (ARRAY[...])` choice in Task 1 exists precisely to keep this diff to
roughly one line.

Then run `test_migration_115.py`. It must now RUN rather than skip, and both controls must pass:
- POSITIVE: a `workflow_phases` row set to `recorded_not_sent` is ADMITTED (inside a rolled-back
  transaction).
- NEGATIVE: setting it to the display sentence `Not sent — recorded` is REJECTED with SQLSTATE 23514
  naming `workflow_phases_status_check`. **This is D-17 expressed as a test.**
- And the five shipped statuses are all still admitted.

⚠ **WHY THIS IS A SEPARATE TASK FROM THE AUTHORING, AND WHAT "SAME COMMIT" ACTUALLY MEANS HERE.**
CLAUDE.md's rule is *apply, then regenerate, then commit both* — but this plan CANNOT put the authoring
and the regeneration in one task, because **the operator's apply (Task 2) is a blocking checkpoint that
must happen BETWEEN them**, and Claude may not apply a migration. That is the project's own protocol, not
a planning convenience, so a merge is genuinely impossible here — unlike plan 189-07, where the pair was
merged for exactly this reason. The shipped 114 precedent works the same way: its header says verbatim
*"this plan ONLY AUTHORS the file — it is NOT applied here"*, so 114's authoring commit landed before its
apply-and-regenerate commit.

**The invariant that must actually hold is directional, and both commits satisfy it:** no commit may ever
contain a regenerated `full-schema.sql` WITHOUT migration 115 present in the tree. After Task 1 the tree
has the migration and an unchanged `full-schema.sql` — coherent, and `test_migration_115.py` green-skips,
so nothing claims the constraint is widened. After this task the tree has both. There is no intermediate
state in which the artifact claims something the migrations do not.

So: commit `supabase/full-schema.sql` here, and record BOTH commit SHAs in the SUMMARY so the pair is
auditable as a pair. The failure the rule prevents — `full-schema.sql` disagreeing with the live DB so a
greenfield deploy rejects the new status — is prevented by the directional invariant, not by the two
files sharing a single SHA.

Finally, run `scripts/check-deploy-drift.sh` if it is present. 189 adds no env var and no seed-bearing
migration, so this is an EXPECTED NO-OP — but the drift check runs in CI regardless and it is cheaper to
learn that here.
  </action>
  <verify>
    <automated>cd backend; venv/Scripts/python.exe -m pytest tests/test_migration_115.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `test_migration_115.py` reports PASSED (not skipped) for both controls plus the five-shipped-statuses
      assertion.
    - `grep -c "recorded_not_sent" supabase/full-schema.sql` returns at least 1.
    - `git diff --stat supabase/full-schema.sql` shows a SMALL diff. If it exceeds roughly 10 changed
      lines, the SUMMARY explains every extra hunk.
    - `supabase/full-schema.sql` was produced by the script, not by hand — the SUMMARY records the exact
      command run and confirms `--reset` was NOT passed.
    - **The directional invariant holds** (this replaces an earlier, unsatisfiable "both files in ONE
      commit" criterion — see the action's note on why the operator checkpoint makes a merge impossible):
      `git log --oneline -- supabase/full-schema.sql` shows this task's commit, and
      `git show --stat` for it confirms `supabase/migrations/115_workflow_phases_recorded_not_sent.sql`
      was ALREADY in the tree at that commit. No commit anywhere in this phase carries a regenerated
      `full-schema.sql` without the migration file present.
    - Both SHAs — the authoring commit from Task 1 and this regeneration commit — are recorded in the
      SUMMARY so the pair is auditable as a pair.
    - `scripts/check-deploy-drift.sh` (if present) passes with no new drift.
  </acceptance_criteria>
  <done>The live DB, the migration file and the bootstrap artifact all agree, and V13's two controls both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the application to `public.workflow_phases.status` | the CHECK constraint is the closed vocabulary that keeps an unknown state out of the column |
| Claude to the operator's local database | Claude may AUTHOR schema; only the operator APPLIES it |
| local schema to `supabase/full-schema.sql` | the single-file deploy artifact for greenfield environments; drift here breaks a fresh deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-17 | Tampering | the operator's local dev data | mitigate | The apply is a BLOCKING human checkpoint. `supabase db push` and `supabase db reset` are forbidden by CLAUDE.md and are explicitly named as forbidden in both the migration header and the checkpoint text. `test_migration_115.py` writes only inside rolled-back transactions. |
| T-189-18 | Tampering | `workflow_phases_status_check` losing a shipped literal in the DROP+ADD | mitigate | An acceptance criterion requires all five shipped literals to be present in the re-added constraint, and the test asserts all five are still admitted. A migration that silently drops one would orphan every row using it. |
| T-189-19 | Spoofing | the display sentence entering the constraint (**the D-17 trap**) | mitigate | `test_migration_115.py`'s NEGATIVE control asserts the sentence is rejected with SQLSTATE 23514, and an acceptance criterion requires `grep -c "Not sent"` to return 0 in the migration file. |
| T-189-20 | Denial of Service | the ACCESS EXCLUSIVE lock taken by DROP+ADD CONSTRAINT | accept | Immaterial on local dev. On cloud it belongs in the standing migration-parity window (migs 099 onward land together), not mid-traffic. The header records this, and the checkpoint text forbids applying to cloud in this phase. |
| T-189-21 | Tampering | a hand-edited `full-schema.sql` | mitigate | The artifact is only ever produced by `scripts/regenerate-full-schema.sh` with no `--reset`; the SUMMARY records the exact command; the diff size is an acceptance criterion. |
| — | Information Disclosure / Elevation of Privilege | RLS on `workflow_phases` | accept | **No RLS change.** 115 touches a CHECK constraint only — no table, no column, no grant, no policy. The 114 precedent is explicit about stating this rather than leaving it inferred. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs nothing. No install task exists in this phase, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `ls supabase/migrations/` shows 115 as the new head and no 115 variant with a letter suffix.
- `cd backend; venv/Scripts/python.exe -m pytest tests/test_migration_115.py -q --no-header` — both
  controls PASS.
- `grep -n "workflow_phases_status_check" supabase/full-schema.sql` shows SIX literals.
- `git log --oneline -- supabase/migrations/115_workflow_phases_recorded_not_sent.sql supabase/full-schema.sql`
  shows the authoring commit BEFORE the regeneration commit, and no commit carries the regenerated
  artifact without the migration already in the tree (the directional invariant — a single shared SHA
  is impossible because the operator's apply is a blocking checkpoint between the two).
</verification>

<success_criteria>
- The sixth status literal is live, in the migration file, and in the regenerated artifact — all three agreeing.
- The display sentence is provably rejected by the constraint.
- No `db push`, no `db reset`, no hand-edit of `full-schema.sql`.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-06-SUMMARY.md` when done.
Record: the operator's `pg_get_constraintdef` output verbatim, the exact regeneration command, the
`full-schema.sql` diff size, and the V13 skip-to-pass transition.
</output>
