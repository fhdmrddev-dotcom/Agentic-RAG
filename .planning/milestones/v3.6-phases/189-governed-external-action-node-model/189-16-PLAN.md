---
phase: 189-governed-external-action-node-model
plan: 16
type: execute
wave: 9
depends_on: ["189-11", "189-15"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/189-governed-external-action-node-model/189-VALIDATION.md
autonomous: false
requirements: [CONN-01]

must_haves:
  truths:
    - "The ROADMAP's two stale migration-head bullets are corrected — both said live head 113 and next free slot 114, and 114 shipped with Phase 185 while 115 shipped with this phase. A file that contradicts itself is how the next executor authors a colliding migration."
    - "D-14 holds: nothing in this phase touched the two files whose WR-04 sinks were fixed by a separate fast run. BUG-260807-01 stays out of 189's ledger and remains open only for its driven browser row."
    - "D-25 is a RECORDED DECISION with a UAT observable, not a latent defect: the run band reads its RUN-level verdict while a node reads its PHASE-level one, and the pairing must be confirmed to read as intended on screen."
    - "The four driven rows jsdom cannot reach — glyph visibility, badge occlusion, greyscale ring distinguishability and card height — are DRIVEN via Chrome MCP with a falsification control observed swinging BOTH ways, not asserted from a green unit suite."
    - "The THREE OWED MANUAL ROWS ride ONE live run, not three staged runs: this phase's own driven rows, the owed 188.2 arc-distinguishability row, and the owed 188.2 visual glance."
    - "189-VALIDATION.md records which plan and task satisfies each of V01 through V23 and each of U1 through U7, so a reader can tell coverage from claim."
  artifacts:
    - path: ".planning/phases/189-governed-external-action-node-model/189-VALIDATION.md"
      provides: "the plan/task coverage map for every V and U row, plus the driven-row results"
      contains: "V20"
    - path: ".planning/ROADMAP.md"
      provides: "the corrected migration-head bullets"
      contains: "115"
  key_links:
    - from: ".planning/ROADMAP.md migration bullets"
      to: "supabase/migrations/"
      via: "a re-derived head rather than an inherited one"
      pattern: "live head"
    - from: "189-VALIDATION.md"
      to: "each 189-NN-PLAN task"
      via: "a per-row plan and task reference"
      pattern: "189-"
---

<objective>
Close the phase: correct the prose the tree has been contradicting itself about, map every validation row
to the plan that satisfies it, and DRIVE the rows a green unit suite cannot prove.

Purpose: three things are true of this repository and all three bite here. Stale pointers propagate — the
ROADMAP has said "live head = 113" in two places since before Phase 185 shipped 114, and an executor
trusting it would author a colliding migration. jsdom is blind — it applies no CSS, computes no stacking
contexts, and a synthetic click bypasses hit-testing entirely, which is how two occlusion bugs survived
into shipped code. And a scoreboard that lists only what passed is not a scoreboard.

⚠ **THREE OWED MANUAL ROWS ALL WANT THE SAME LIVE RUN.** This phase launches live runs, so author ONE
driven session that satisfies this phase's rows AND the two rows Phase 188.2 left owed, rather than
staging a run three times.

Output: two corrected ROADMAP bullets, a filled coverage map, and a driven session with recorded evidence.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-VALIDATION.md
@.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md
@CLAUDE.md

⚠ **THE OPERATOR STARTS THE BACKEND.** Never launch it in the background; ask and wait for confirmation.
The local app is at the dev server the project standardly uses; credentials are the project's local dev
account.
⚠ **The app has NO URL ROUTER** — the active view is React state, so visiting a path renders chat and the
URL is inert. **Every driven row navigates by CLICKING, never by deep link.**
⚠ **Screenshot capture times out repeatedly in this estate.** Prefer script-evaluated DOM and geometry
reads — they are machine-checkable and they do not wedge the session.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the self-contradicting migration-head prose, and confirm D-14 held</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — `grep -n "live head"` and read EVERY hit. Two of them (the milestone
      numbering paragraph and the migrations guardrail bullet) say the live head is 113 and the next free
      slot is 114. A third location — this phase's progress row — already states it correctly, so the
      file currently contradicts itself.
    - `ls supabase/migrations/` — re-derive the ACTUAL head. Measured at plan time: 114 was the head and
      115 was free; after plan 189-06, 115 is the head.
    - `.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md` — decision **D-14**, and
      the reported-bug routing it records.
    - `.planning/reported-bugs/` — list the reports and check whether any now carries a folded-into value
      naming this phase. Measured at plan time: NONE does.
  </read_first>
  <action>
A — THE TWO STALE BULLETS. Correct both ROADMAP locations to state the head as re-derived from
`ls supabase/migrations/`, and record WHY the line moved: 114 shipped with Phase 185 (the armed-pause
audit kind) and 115 shipped with Phase 189 (the recorded-not-sent status). Both were ROADMAP amendments
to a zero-migration promise, and both were amended for the same recorded reason — an honest word is a
correctness property, not a scoping convenience.

⚠ **Correct, do not rewrite.** These bullets carry other still-true content about the layout side table
and the connector-credentials table. Change the head figures and add the two-sentence reason; leave the
rest.

B — CONFIRM D-14 HELD. Decision D-14 routed a separate bug OUT of this phase, and its code fix shipped
standalone under the small-work guardrail. Verify mechanically that no 189 plan touched its two files:
run `git diff --stat` for the whole phase against the phase's base commit and confirm neither the
edit-affordance module nor the provider-logo module appears.
State the result plainly either way. That bug remains OPEN for its driven browser row only — and its row
is one of the three that ride this plan's live run (Task 3).

C — THE REPORTED-BUGS CROSS-CHECK. The project rule requires verifying at plan time that every report
folded into this phase is actually addressed. Re-run the check and record the result: measured at plan
time, NO report carries a folded-into value naming 189, so there is nothing to address and that is the
finding, not an omission.
  </action>
  <verify>
    <automated>grep -n "live head" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "live head" .planning/ROADMAP.md` shows NO remaining occurrence claiming the head is 113 or
      that the next free slot is 114.
    - The corrected figures match `ls supabase/migrations/ | tail -1` exactly — quote both in the SUMMARY.
    - Each corrected bullet retains its other content; `git diff` shows a small, targeted edit, not a
      rewrite.
    - The phase-wide `git diff --stat` is quoted in the SUMMARY and demonstrably contains neither the
      edit-affordance module nor the provider-logo module.
    - The reported-bugs check is re-run and its result recorded — including if the result is "none".
  </acceptance_criteria>
  <done>The ROADMAP no longer contradicts itself about the migration head, and D-14's boundary is proven by a diff rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the validation coverage map — every V and U row mapped to the plan that satisfies it</name>
  <files>.planning/phases/189-governed-external-action-node-model/189-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/189-governed-external-action-node-model/189-VALIDATION.md` — the twenty-three
      numbered V rows, the seven U rows, the Wave-0 checklist and the sign-off checklist. **EXTEND this
      file; do not replace it.**
    - Every `189-NN-SUMMARY.md` written so far — the RED-to-GREEN pairs, the observed controls, and the
      baselines each one recorded.
  </read_first>
  <action>
Extend `189-VALIDATION.md` with a coverage section mapping EVERY row to the plan and task that satisfies
it, plus its current status and the command that proves it.

Cover all twenty-three V rows and all seven U rows. For each, record: the plan and task id, the status,
and — where a plan SUMMARY captured one — the RED that was observed before the GREEN. A green whose RED
was never seen is recorded as such rather than as a pass.

Also update the file's frontmatter: set the Wave-0 completion flag once the three Wave-0 suites plus the
migration gate are all in place, and set the Nyquist-compliance flag only when every V row has an
automated command and every U row has a driven result or an explicit owed note.

⚠ **Owed rows are recorded as OWED, never dropped.** A scoreboard that lists only what passed is not a
scoreboard. If a driven row cannot be run — the local stack is down, a dependency is unavailable — record
it with the reason and the blocking condition, and name which row to run first when it clears.

⚠ **No cross-provider scoreboard is owed for this phase** — the ROADMAP exempts it as design and
vocabulary work with no live stream. Do not manufacture one.

Finally, tick the sign-off checklist honestly: each box either has evidence behind it or stays unticked
with a note. An unticked box with a reason is information; a ticked box without evidence is not.
  </action>
  <verify>
    <automated>grep -c "189-" .planning/phases/189-governed-external-action-node-model/189-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every row V01 through V23 appears in the coverage map with a plan id, a task, a status and a command.
    - Every row U1 through U7 appears with a plan id and either a driven result or an explicit OWED note
      carrying its blocking condition.
    - The original content of `189-VALIDATION.md` is intact — the file was EXTENDED, not replaced; verify
      with `git diff` showing additions dominate.
    - The frontmatter flags reflect reality, and the SUMMARY states the evidence for each flag set.
    - No cross-provider scoreboard was added.
    - Every sign-off box is either ticked with evidence or unticked with a reason.
  </acceptance_criteria>
  <done>Coverage is legible: a reader can tell which plan proves which row, and which rows are owed and why.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Drive ONE live session covering this phase's four visual rows and the two owed 188.2 rows</name>
  <files>.planning/phases/189-governed-external-action-node-model/189-VALIDATION.md</files>
  <action>
PAUSE. This task is a BLOCKING HUMAN-VERIFY CHECKPOINT. Claude has automated everything it can; what
remains cannot be reached by jsdom, which applies no CSS, computes no stacking contexts, and lets a
synthetic click bypass hit-testing entirely.

Confirm with the operator that the backend, the local database and Redis are running — the operator
starts the backend, never Claude in the background. Then drive ONE live session covering all six rows
below plus the two extras, recording each verdict and its numbers into `189-VALIDATION.md`. Navigate by
CLICKING; prefer script-evaluated DOM and geometry reads over screenshot capture, which times out
repeatedly in this estate. A row that cannot be run is recorded as BLOCKED with its reason and the row to
run first when it clears — never silently dropped.
  </action>
  <what-built>
    The seventh step type ships end to end: it appears in the picker with a 3D mark on its own tint, its
    card face is derived from the chosen capability, it carries the "Not connected" badge in the last free
    badge slot, its arming switch renders on and refuses to move, and at run time it reaches a sixth
    terminal — "Not sent — recorded" — carried by a new four-arc ring shape and a phase output that reads
    unmistakably as not sent. A workflow containing it publishes and runs. Nothing leaves the system.

    Every claim above is covered by unit tests EXCEPT the visual ones, and jsdom cannot reach those: it
    applies no CSS, computes no stacking contexts, and a synthetic click bypasses hit-testing entirely.
    That is how two occlusion bugs in this project survived into shipped code.
  </what-built>
  <how-to-verify>
    ⚠ **The operator starts the backend.** Confirm the dev server, the local database and Redis are up
    before starting. Do not launch the backend in the background.
    ⚠ **Navigate by CLICKING.** The app has no URL router; a path visit renders chat and the URL is inert.
    ⚠ **Prefer script-evaluated DOM and geometry reads over screenshots** — screenshot capture times out
    repeatedly in this estate, and a geometry read is machine-checkable anyway.

    Build ONE workflow containing an external-action step and ONE other step, publish it, and run it —
    this single session covers all six rows below.

    **U1 — is the seventh mark actually VISIBLE?** On one canvas showing all seven step types, read the
    computed fill and luminance of each rendered mark and compare the seventh against the other six. A
    previously shipped slug in this project measured roughly four times dimmer than its siblings and
    simply disappeared. Report the seven numbers.

    **U2 — does the "Not connected" badge occlude, or get occluded by, anything?** Read the element at the
    badge's own centre point, and at the centre of each neighbour — the governance seal position, the
    verdict mark, and the lane affordances. ⚠ **A falsification control must be observed swinging BOTH
    ways**: deliberately move or resize something so the check FAILS, confirm it fails, then restore and
    confirm it passes. That is how the earlier occlusion bug was finally closed, and an unfalsified pass
    here proves nothing.

    **U3 — is the eighth ring distinguishable by SHAPE ALONE in greyscale?** Read the dash-array and
    dash-offset presentation attributes for all eight readings and confirm the new one is the only reading
    drawn in four arcs. Then view the canvas with colour removed and confirm the eight remain
    distinguishable. Report the eight attribute strings.

    **U4 — does the badge push the card past its budget or move anything?** Measure the card's bounding
    rectangle with and without the badge, and measure an adjacent node's position both times. The
    acceptance is that the spine's edge baseline does not move and no neighbour reflows — NOT that all
    cards are the same height. This card is legitimately taller than its neighbours.

    **U5 — the OWED Phase 188.2 row.** On this same live run: confirm the seven shipped readings stay
    distinguishable by shape, confirm the running arc actually spins, and confirm a card with no reading
    is still. This row has been owed since Phase 188.2 because it needs a live run, and this is that run.

    **U6 — the OWED Phase 188.2 visual glance.** One look at a builder card while you are there. Zero cost.

    **PLUS — D-25's accepted divergence.** After the run completes, confirm the run band reads its
    completion verdict ABOVE a node reading "Not sent — recorded", and confirm the pairing reads as
    intended rather than as a contradiction. This was ACCEPTED as a decision: the band is a RUN-level
    verdict and the node is a PHASE-level one, and the run genuinely did complete. It is listed here so
    it reads as a recorded decision rather than as a defect found in testing.

    **PLUS — the separately-tracked bug's driven row.** While a live workflow is on screen, run the
    reachability check that bug is still open for: create a phase whose slug is an inherited property
    name and confirm its affordances remain reachable at their own coordinates.

    Report for each row: PASS, FAIL, or BLOCKED with the reason. **A row that could not be run is recorded
    as blocked with its reason — never silently dropped.**
  </how-to-verify>
  <resume-signal>Reply with the six row results plus the two extras, and type "approved" or describe what failed.</resume-signal>
  <verify>
    <human-check>All six driven rows (U1 through U6) plus the D-25 pairing and the separately-tracked bug's reachability row have an explicit PASS, FAIL or BLOCKED verdict recorded in `189-VALIDATION.md`, and U2's falsification control was observed swinging both ways.</human-check>
    <automated>grep -c "U1\|U2\|U3\|U4\|U5\|U6" .planning/phases/189-governed-external-action-node-model/189-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All six rows (U1 through U6) have an explicit PASS, FAIL or BLOCKED verdict recorded in
      `189-VALIDATION.md` with its evidence.
    - U2's falsification control was observed swinging BOTH ways, and both observations are recorded.
    - U1 reports seven luminance figures; U3 reports eight attribute strings; U4 reports four measurements.
    - The D-25 pairing is confirmed on screen and recorded as a satisfied decision.
    - The separately-tracked bug's driven row has a verdict, and its report's status is updated
      accordingly.
    - Every row was driven by CLICKING; no deep link was used.
    - Any blocked row names its blocking condition and which row to run first when it clears.
  </acceptance_criteria>
  <done>The claims jsdom cannot reach have been driven on a live run, with a control observed failing, and every owed row has a verdict rather than a silence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a live run against the local database | the driven session creates real workflow and run rows in the operator's dev environment |
| planning prose to the next executor | a stale figure in the ROADMAP is read as fact and acted on — that is the defect this plan is correcting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-48 | Tampering | a future executor authoring a COLLIDING migration from a stale head figure | mitigate | Both stale bullets are corrected against a re-derived `ls supabase/migrations/`, with the reason recorded. A letter-suffixed migration is silently skipped by the CLI, so a collision is not merely untidy. |
| T-189-49 | Spoofing | a visual claim asserted from a green unit suite | mitigate | Four rows are DRIVEN, and the occlusion row requires a falsification control observed swinging BOTH ways. jsdom applies no CSS and a synthetic click bypasses hit-testing; that combination is how two occlusion bugs survived into shipped code here. |
| T-189-50 | Repudiation | an owed row silently dropped so the board reads all-green | mitigate | Every row gets PASS, FAIL or BLOCKED with a reason. A scoreboard that lists only what passed is not a scoreboard, and closing with owed rows is legitimate only when stated as a decision. |
| T-189-51 | Tampering | the driven session mutating the operator's environment | accept | The session creates workflow and run rows in the LOCAL dev database. All workflow definition rows in this project are treated as test fixtures and are free to delete or reseed. No global setting is changed and no cloud environment is touched. |
| — | Information Disclosure | the live run | accept | **SC#4 holds and this session is where it is observed rather than assumed:** the external-action step records intent and sends nothing. Confirm during the run that the phase output reads as not-sent and that no outbound request was made. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs nothing. No install task exists in this phase, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `grep -n "live head" .planning/ROADMAP.md` — no occurrence claims 113 or that 114 is free.
- `ls supabase/migrations/ | tail -1` matches the corrected figure.
- `189-VALIDATION.md` maps every V and U row to a plan and a verdict.
- FULL SUITE GATES, re-run at phase close and reconciled against their measured baselines:
  `cd frontend; npx tsc --noEmit -p tsconfig.app.json` (baseline 33) ·
  `node scripts/vitest-count-gate.cjs` (baseline was total 2508 / 45 files before this phase; the new
  total and the new file count are both stated and every increment attributed to a plan) ·
  the nine-file backend command from `189-RESEARCH.md` section D15 (baseline 166 passed / 2 named
  pre-existing failures).
  ⚠ Do NOT read the count gate's `failed` column as a regression backstop. The COUNT columns are.
</verification>

<success_criteria>
- The ROADMAP states one migration head, and it is the true one.
- Every validation row has an owner and a verdict; owed rows are named, not hidden.
- The four visual claims are driven, with a control observed failing, and the two Phase 188.2 debts are
  paid on the same run.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-16-SUMMARY.md` when done.
Record: the corrected head figures beside the `ls` output, the phase-wide `git diff --stat` proving D-14
held, the six driven row verdicts with their numbers, both observations of the U2 control, and the three
full-suite gate readings with every increment attributed.
</output>
