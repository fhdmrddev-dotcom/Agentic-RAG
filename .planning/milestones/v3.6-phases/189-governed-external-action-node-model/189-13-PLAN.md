---
phase: 189-governed-external-action-node-model
plan: 13
type: execute
wave: 6
depends_on: ["189-12"]
files_modified:
  - frontend/src/components/workflows/soulData.ts
  - frontend/src/lib/phaseGlyph.tsx
  - frontend/src/components/workflows/nodePresentation.ts
  - frontend/src/components/workflows/phaseVocabulary.ts
  - frontend/src/components/workflows/canvasModel.ts
  - frontend/src/components/workflows/soulData.test.ts
  - frontend/src/components/workflows/phaseVocabulary.test.ts
  - frontend/src/components/workflows/canvasModel.test.ts
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "The 7th glyph and its resolver map are swapped in ONE COMMIT. Swapping one alone leaves the resolver returning the old component while the string fallback changed — the shipped docblock calls that a silent split-brain, and a missing icon slug FAILS THE BUILD."
    - "The split-brain guard is stated as a PROPERTY, not as six comparisons: the two glyph maps are asserted to have IDENTICAL KEY SETS, so a ninth type inherits the guard."
    - "D-13: the vocabulary ladder's middle tier is CONFIG-DERIVED from the chosen capability, computed at render and stored nowhere, falling back to the generic type sentence when no capability is chosen."
    - "The new ladder tier is a NUMBERED tier gated on a NAMED module-scope constant, sitting with the other context-free tier rather than as a decimal — a decimal tier defeats the ordering test the numbering exists to force."
    - "GROUNDING_DIAL_TYPES is READ and NEVER edited — external_action must not join it. Its capabilities are disjoint from the KB tools, so this step is free to think by construction and carries no governance seal, and that is correct rather than a bug."
    - "The capability lookup is own-property guarded (WR-04) using the inline form, because this file has ZERO imports today and adding one would make it the file's first ever."
    - "notConnectedOf exists as a named total predicate beside its siblings, written as a TYPE test and a STATE test on SEPARATE LINES so Phase 190 edits one line and the adapter is untouched."
  artifacts:
    - path: "frontend/src/components/workflows/soulData.ts"
      provides: "the 7th PHASE_GLYPHS entry"
      contains: "external_action"
    - path: "frontend/src/lib/phaseGlyph.tsx"
      provides: "the paired build-time icon import and resolver entry — same commit"
      contains: "external_action"
    - path: "frontend/src/components/workflows/phaseVocabulary.ts"
      provides: "the three per-type map entries, the capability-derived ladder tier, and notConnectedOf"
      contains: "notConnectedOf"
  key_links:
    - from: "frontend/src/components/workflows/soulData.ts PHASE_GLYPHS"
      to: "frontend/src/lib/phaseGlyph.tsx PHASE_GLYPH_MARKS"
      via: "identical key sets, asserted as a property"
      pattern: "PHASE_GLYPH"
    - from: "phaseVocabulary.ts derivedFace"
      to: "the config's capability"
      via: "an own-guarded lookup into a closed sentence map, computed at render"
      pattern: "capability"
---

<objective>
Give the seventh type its face: one 3D mark, one tint, three per-type words, one capability-derived
sentence, and the predicate the badge will read.

Purpose: D-13 says the vocabulary ladder's middle tier is config-derived from the chosen capability, and
this is the tier that decides what users actually SEE — the shipped docblock records that on the live
corpus effectively no phase carries a real author name, so the derived and generic tiers are what
renders essentially always.

⚠ **Two same-commit constraints bind here**, both stated in the source rather than invented: the glyph
string map and its component resolver must be swapped together (a missing icon slug FAILS THE BUILD, and
swapping one alone is a silent split-brain), and any test-count change moves with its gate pin.

Output: two glyph edits in one commit, one tint, three word-map entries, one numbered ladder tier, one
new predicate — and a split-brain guard stated as a property.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md
@.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@CLAUDE.md

**LOAD THE PROJECT DESIGN SKILL** — `Skill("sketch-findings-agentic-rag")` — and read its
`references/icon-convention.md` section 4, the canvas glyph vocabulary, BEFORE drawing any mark.
⚠ That file's line pointers have been measured stale repeatedly; treat any `:NNN` in it as needing
re-derivation. Read the skill, do not paraphrase a manifest.

RE-DERIVE EVERY ANCHOR by searching for the identifier.
BASELINES: `npx tsc --noEmit -p tsconfig.app.json` = **33** · count gate **total 2508, 45/45**.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 7th glyph and its tint — both maps in ONE commit, with a key-set property guard</name>
  <files>frontend/src/components/workflows/soulData.ts, frontend/src/lib/phaseGlyph.tsx, frontend/src/components/workflows/nodePresentation.ts, frontend/src/components/workflows/soulData.test.ts, frontend/src/components/workflows/PhaseNodeCard.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/soulData.ts` — search `PHASE_GLYPHS` (six entries: gear, memo,
      compass, handshake, raised-hand, package). ⚠ **This is the glyph map's true home. It is NOT in
      `phaseVocabulary.ts`** — that pointer was wrong in the phase's own refs and was corrected. Also
      read the comment above it recording the Phase 184 swap: a previously shipped slug measured
      luminance 34.5 on Deep Midnight, roughly four times dimmer than the other five, and DISAPPEARED.
      Presence is necessary, not sufficient.
    - `frontend/src/lib/phaseGlyph.tsx` — search `PHASE_GLYPH_MARKS` and the build-time icon imports.
      Read the docblock stating the SAME-COMMIT rule verbatim: swapping one map alone leaves the
      resolver returning the old component while the string fallback changed — a silent split-brain.
    - `frontend/src/components/workflows/nodePresentation.ts` — search `ICON_TINT` and the default tint.
      Read its docblock: the tint sits BEHIND the floating mark and is the whole of this surface's
      type-colour budget. Never a card wash, never a border, never text.
    - `frontend/src/components/workflows/soulData.test.ts` — the six glyph keys are currently asserted
      INDIVIDUALLY. That is the guard this task upgrades.
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — search for the six-count assertion on
      the icon-tint table. **This pin moves HERE**, in the same commit as the tint entry.
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` section 5 in full —
      the chosen slug, why the capability-specific alternatives were rejected, the measured palette
      luminances, and the tint's hue reasoning.
  </read_first>
  <action>
A — THE GLYPH, BOTH MAPS, ONE COMMIT.

Add the seventh `PHASE_GLYPHS` entry in `soulData.ts` using the slug `outbox-tray`, and in the SAME
COMMIT add the matching build-time icon import plus the `PHASE_GLYPH_MARKS` entry in
`frontend/src/lib/phaseGlyph.tsx`.

Why that slug, recorded so it is not re-litigated: it reads as "leaves here / goes outside", which is the
TYPE's nature. ⚠ The mark is keyed by phase type, so it is ONE mark for the type, not three per
capability — an envelope would say *email* on a node whose capability might be a ticket. ⚠ The slug
`outbox` (without the suffix) is ABSENT from the installed icon set and would fail the build; that is
recorded here so it is not tried.

**VERIFY THE SLUG EXISTS IN THE INSTALLED SET BEFORE IMPORTING IT** — read the installed package's
icon data directly rather than trusting this plan. A missing slug fails the build, which is the
verify-or-bundle discipline stated as a mechanism.

B — THE TINT. Add the seventh `ICON_TINT` entry with the hue specified in the UI-SPEC. Its reasoning,
for the comment: it is the midpoint of the largest unused hue gap; the other large gap's midpoint sits
adjacent to the success family and would read as a success cue behind an icon; and the alpha is inside
the shipped range. The tint goes behind the ICON ONLY. Move the icon-tint six-count pin in this commit.

C — THE SPLIT-BRAIN GUARD AS A PROPERTY. Rewrite `soulData.test.ts`'s six individual key assertions
into ONE property: the key SETS of `PHASE_GLYPHS` and `PHASE_GLYPH_MARKS` are IDENTICAL. That is the
split-brain rule stated as something a ninth type inherits, rather than as six comparisons that would
silently stop covering.
⚠ **Observe it RED** by temporarily adding the key to only one map, and record that observation. A guard
that has never failed is a guard nobody has tested.
Also assert every `PHASE_GLYPHS` key has an `ICON_TINT` entry, or explicitly assert the default-tint
fallback covers it — decide which and record the decision.

⚠ **THE REWRITE MUST NOT REDUCE THIS FILE'S TEST COUNT.** If the six key assertions live in six separate
cases, collapsing them into one property would LOWER the count — and the count gate refuses a per-file
decrease, so the run would block. It is also how coverage silently disappears. Measure the file's case
count BEFORE and AFTER: if the property rewrite would reduce it, KEEP the count at or above its baseline
by retaining the per-key cases alongside the new property, or by adding the cases the property makes
worth having (the seventh key, the tint coverage, the RED-observed control). **Never lower a pin to
match a shrunken suite.**

D — jsdom CANNOT prove the mark is VISIBLE. It applies no CSS and paints nothing. The luminance check
against the other six on a real canvas is U1, a driven Chrome MCP row in plan 189-16. Say that in the
test's comment so this green is not mistaken for the visual proof.

Update `scripts/vitest-count-gate.cjs` `BASELINE` for every changed suite in the SAME COMMIT.
  </action>
  <verify>
    <automated>cd frontend; npx vitest run src/components/workflows/soulData.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `PHASE_GLYPHS` and `PHASE_GLYPH_MARKS` both have SEVEN entries, added in ONE commit — verify with
      `git show --stat` on that commit listing both files.
    - The chosen icon slug was verified present in the INSTALLED icon package before import; the SUMMARY
      records how it was verified.
    - `cd frontend; npx vite build` (or the project's build command) SUCCEEDS — a missing icon slug fails
      the build, so this is the real check that the import resolves.
    - The key-set identity property test PASSES, and the SUMMARY records the observed RED from adding the
      key to only one map.
    - `ICON_TINT` has seven entries and its six-count pin moved in THIS commit.
    - `soulData.test.ts`'s case count is at or ABOVE its measured baseline — state the before and after
      numbers in the SUMMARY. `node scripts/vitest-count-gate.cjs` reports no per-file decrease.
    - `git diff` shows the tint value used behind the icon only — no card background, no border, no text
      colour anywhere in this diff.
    - The count gate reports OK with pins moved in-commit.
  </acceptance_criteria>
  <done>The seventh mark resolves and builds, its two maps cannot silently diverge, and the tint spends the type-colour budget where the budget lives.</done>
</task>

<task type="auto">
  <name>Task 2: The D-13 ladder tier, the three per-type words, and notConnectedOf</name>
  <files>frontend/src/components/workflows/phaseVocabulary.ts, frontend/src/components/workflows/canvasModel.ts, frontend/src/components/workflows/phaseVocabulary.test.ts, frontend/src/components/workflows/canvasModel.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/phaseVocabulary.ts` — read it broadly (measured 641 lines).
      Search the three per-type maps: the plain-language type SENTENCES (verb-first business voice), the
      SUBTITLES (third-person mechanism voice) and the technical LABELS (terse technical nouns). ⚠ Match
      the register PER MAP; one phrase reused three times is wrong.
    - `frontend/src/components/workflows/phaseVocabulary.ts` — search `derivedFace` and read the WHOLE
      numbered ladder. The numbering IS the decision record, most-specific-first. Note that each gate is
      a NAMED module-scope constant, never an inline literal, and note the rule that it must NEVER
      FABRICATE A CAPABILITY — rendering a claim about a step whose executor cannot do it states
      something false.
    - `frontend/src/components/workflows/phaseVocabulary.ts` — search `GROUNDING_DIAL_TYPES` and the
      comment declaring it READ and NEVER edited. ⚠ **`external_action` must NOT be added to it.**
    - `frontend/src/components/workflows/phaseVocabulary.ts` — search `derivedFaceOf` (the phase-shaped
      adapter that declares NO branch of its own) and `actionRiskArmed` / the waits-for-you predicate,
      which are the siblings `notConnectedOf` joins.
    - ⚠ `frontend/src/components/workflows/phaseVocabulary.ts` has **ZERO import statements today**.
      Search the file to confirm. Adding one would make it the file's first ever.
    - `frontend/src/components/workflows/runVocabulary.ts` — search its module-private own-property
      guard, the inline form this task copies.
    - `frontend/src/components/workflows/canvasModel.ts` — search `buildPhaseData` and the delegating
      predicates it calls. ⚠ Also search for the read of the SUBTITLES map with a nullish fallback: it is
      an UNGUARDED WR-04 sink and it is the only read of that map outside its declaration. **Fix it while
      adding the 7th key.**
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` sections 2b, 6a and 6b —
      the predicate's exact shape, the tier's number and gate, and the three words with their reasoning.
  </read_first>
  <action>
A — THE THREE PER-TYPE WORDS. Add one entry to each of the three maps, in that map's own register:
- the type SENTENCE (the tier-3 floor, and what users actually see when no capability is chosen) —
  the UI-SPEC's `Reach outside`: it says the step's nature without claiming a specific act.
- the SUBTITLE (the one supporting line) — `Stops for your approval before it acts outside`. ⚠ This is
  worded to SURVIVE Phase 190: it says nothing about not-sending, so it will not become a lie when the
  node starts sending. The not-sent fact is carried by the badge at design time and the run word at run
  time, which is where the decisions put it.
- the technical LABEL — `External action`, which the reveal renders alongside the slug.

B — THE LADDER TIER (D-13). Insert a NUMBERED tier that maps the chosen capability to a business
sentence: `Sends an email`, `Creates a ticket`, `Posts a message`.

Place it as the tier immediately ABOVE the human-input tier, renumbering the tiers below it. The
reasoning, for the comment: the tiers above it all read a NAME out of injected context and can therefore
fabricate; this tier and the human-input tier read the config alone and cannot. The capability tier is a
sibling of human-input, not of the name-reading tiers. ⚠ **Do NOT use a decimal tier number** — a
decimal defeats the ordering test the numbering exists to force, and it invites another.

Declare the gate as a NAMED module-scope constant following the shipped human-input gate's form, never
an inline literal.

⚠ **THE LOOKUP IS A WR-04 SINK.** The capability reaches this map from author-supplied JSONB. Guard it —
but use the INLINE own-property form, following the shipped precedent in the run-vocabulary module,
**not** an import of the shared helper. `phaseVocabulary.ts` has zero imports today and this is not the
change that should give it its first.

⚠ **The three sentences are ONE constant read TWICE** — here for the node face, and by the capability
picker in plan 189-14. Never a second copy. Export it so the picker can import it.

⚠ **Do NOT add `external_action` to `GROUNDING_DIAL_TYPES`.** It is READ and NEVER edited, and the gate
is still correct: this type's capabilities are disjoint from the KB tools, so it is free to think by
construction and carries no governance seal. That is correct, not a bug.

Extend `derivedFaceOf` to read the capability from the config behind a string-type guard, declaring no
branch of its own — that is what stops the panel and the canvas drifting.

C — `notConnectedOf`. Add a named TOTAL predicate beside its siblings. ⚠ Write the TYPE test and the
STATE test on SEPARATE LINES with a comment saying why: Phase 190 binds a destination and edits ONE
line, and the adapter that consumes it is untouched. That separation is the whole reason the badge was
made state-conditional rather than type-conditional.

D — `canvasModel.ts`. Add `notConnected` to the node data shape and ONE line to `buildPhaseData`
calling `notConnectedOf`, following the delegating-predicate shape its own docblock argues for (every
boolean on the node face has a NAME to argue with rather than an inline ternary). Document the new field
in the shape's field-doc convention. ⚠ **AND fix the unguarded subtitles-map read in the same file while
you are adding the 7th key** — a bare table lookup with a nullish fallback does not fire its fallback
for inherited names, and the measured value for `constructor` was a function object that hard-crashed
the node render.

E — TESTS. Extend `phaseVocabulary.test.ts`:
- All THREE capabilities produce their distinct sentence.
- No capability chosen falls through to the type sentence.
- An UNRECOGNISED stored capability value falls through to the type sentence and NEVER fabricates a face.
- `"constructor"` as a capability value falls through — the own-guard probe.
- `notConnectedOf` returns true for the new type and FALSE for all six shipped types. ⚠ Drive the
  negative branch over all six; that is the falsifiable half available today, since nothing in the app
  can be connected to anything until Phase 190. Say so in the test's comment rather than leaving it to be
  discovered.
- The tier ORDER is asserted: a phase that would match both the capability tier and a lower tier gets
  the capability sentence.
- `GROUNDING_DIAL_TYPES` is UNCHANGED — pin its membership.

Update the gate pins in the SAME COMMIT.
  </action>
  <verify>
    <automated>cd frontend; npx vitest run src/components/workflows/phaseVocabulary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three per-type maps have seven entries, each in its own register — quote the three new strings
      in the SUMMARY beside a shipped sibling from the same map.
    - The new ladder tier has an INTEGER number, a NAMED module-scope gate constant, and sits directly
      above the human-input tier with the lower tiers renumbered.
    - `grep -c "import " frontend/src/components/workflows/phaseVocabulary.ts` returns **0** — the file
      still has zero imports, and the WR-04 guard is the inline form.
    - `GROUNDING_DIAL_TYPES` is byte-identical to HEAD and pinned by a test.
    - The capability sentence map is EXPORTED (plan 189-14's picker imports it) and appears exactly once
      in the tree.
    - `notConnectedOf` has its type test and state test on SEPARATE LINES with the Phase-190 comment;
      it returns false for all six shipped types, asserted individually.
    - The unguarded subtitles-map read in `canvasModel.ts` is now own-guarded, and a `"constructor"`
      probe test covers it.
    - `cd frontend; npx tsc --noEmit -p tsconfig.app.json` reads exactly 33.
    - The count gate reports OK with pins moved in-commit.
  </acceptance_criteria>
  <done>The node's face is capability-derived at render, cannot fabricate, cannot be prototype-polluted, and the badge's predicate is ready with a one-line retirement path for Phase 190.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-supplied `config.capability` (JSONB) to a client lookup table | an arbitrary string from stored definition data keys a map here |
| author-supplied `phase_type` to the subtitles / glyph / tint tables | the same shape, and one of these reads is already unguarded at HEAD |
| the build-time icon import to the bundle | a missing slug is a build failure, which is the verify-or-bundle discipline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-24 | Tampering | prototype pollution through the capability-sentence and subtitles lookups (**WR-04**) | mitigate | Both lookups are own-property guarded — the capability map with the INLINE form (this file has zero imports and must keep them), the subtitles read in the projection module fixed while its 7th key is added. `"constructor"` probes cover both. Measured in this codebase: a bare lookup returned a function object and hard-crashed the node render. ⚠ Do NOT merge the two coexisting guard spellings; that boundary is recorded as untouched. |
| T-189-39 | Spoofing | a fabricated face claiming a capability the step does not have | mitigate | An unrecognised stored capability falls THROUGH to the type sentence and never fabricates — the shipped ladder's own rule, asserted by a test. Rendering a claim a step cannot perform states something false to the author. |
| T-189-40 | Elevation of Privilege | `external_action` joining `GROUNDING_DIAL_TYPES` | mitigate | Explicitly forbidden; the constant is pinned byte-identical by a test. It is a governance-semantics red line, and the type does not belong there — its capabilities are disjoint from the KB tools. |
| T-189-41 | Denial of Service | a missing icon slug or a split-brain between the two glyph maps | mitigate | The slug is verified against the INSTALLED package before import, the build is run as an acceptance criterion, and the two maps' key sets are asserted identical — a property observed RED against a one-sided plant. |
| — | Information Disclosure | — | n/a | **No new attack surface.** This plan adds presentation data and one pure predicate. No route, no credential, no request. Stated with its reason. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs NOTHING — the icon is a slug inside an ALREADY-INSTALLED package, confirmed by reading the installed icon data directly rather than by a network lookup. No `npm install` runs, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `cd frontend; npx vitest run src/components/workflows/soulData.test.ts src/components/workflows/phaseVocabulary.test.ts src/components/workflows/canvasModel.test.ts`
- `cd frontend; npx tsc --noEmit -p tsconfig.app.json` reads exactly 33.
- The project build succeeds — the real check that the icon slug resolves.
- `node scripts/vitest-count-gate.cjs` reports OK, 45/45, pins moved in-commit. ⚠ COUNT columns, not the
  `failed` column.
- `git diff --name-only` confirms NO file inside the fenced card subtree was touched by this plan.
</verification>

<success_criteria>
- One mark, one tint, three words, one capability sentence per capability — all computed at render.
- The split-brain guard is a property, observed RED, and a ninth type inherits it.
- Two WR-04 sinks are guarded where there was one unguarded before.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-13-SUMMARY.md` when done.
Record: how the icon slug's presence was verified, the observed RED of the key-set property, the three
new words beside a shipped sibling from each map, and the before/after of the fixed WR-04 sink.
</output>
