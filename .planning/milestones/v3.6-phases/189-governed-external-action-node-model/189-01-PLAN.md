---
phase: 189-governed-external-action-node-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_189_external_action_model.py
  - backend/tests/unit/test_189_no_egress.py
  - backend/tests/test_migration_115.py
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "The three Wave-0 backend suites exist and every assertion in them was OBSERVED RED (or green-skip for the live-DB one) before any 189 source change landed — a green that was never seen red is not evidence."
    - "D-04's falsification exists as a test BEFORE the pin is written: a definition carrying action_risk_armed:false on an external_action phase round-trips through WorkflowDefinition.model_validate and the resulting phase reads True."
    - "D-02's closed-set discipline is asserted: a capability name outside the closed set RAISES in the executor and is never resolved dynamically and never eval'd."
    - "D-15's exact three capability names — send_email, create_ticket, post_message — are asserted as a SET, and asserted DISJOINT from KB_TOOLS."
    - "SC#4 has a mechanical proof that does not depend on reading the code: a source fence over backend/app asserting zero 'mcp' identifiers, with a POSITIVE CONTROL so the fence cannot pass vacuously."
    - "D-08 / D-17: the migration-115 CHECK test carries BOTH a positive control (the slug recorded_not_sent is admitted) AND a negative control (the display sentence 'Not sent — recorded' is REJECTED), and it green-skips until the operator applies."
  artifacts:
    - path: "backend/tests/unit/test_189_external_action_model.py"
      provides: "V06 (D-04 stored-false coercion) · V09 (emptied available_tools refusal) · D-02's closed-set raise · D-15's set + KB_TOOLS disjointness"
    - path: "backend/tests/unit/test_189_no_egress.py"
      provides: "V10 (patched-transport falsification) · V11 (the mcp source fence with a positive control)"
    - path: "backend/tests/test_migration_115.py"
      provides: "V13 — the positive AND negative CHECK-constraint controls, rollback-only, green-skip until applied"
  key_links:
    - from: "backend/tests/unit/test_189_external_action_model.py"
      to: "backend/app/models/harness.py"
      via: "WorkflowDefinition.model_validate on a definition carrying an external_action phase"
      pattern: "model_validate"
    - from: "backend/tests/test_migration_115.py"
      to: "postgresql://…:54322 workflow_phases_status_check"
      via: "a rollback-only INSERT/UPDATE inside a transaction"
      pattern: "workflow_phases_status_check"
---

<objective>
Author the three NEW backend test files this phase's contract names as Wave-0 gaps (`189-VALIDATION.md`
rows V06, V09, V10, V11, V13), and OBSERVE EACH ONE RED before a single line of 189 source exists.

Purpose: the standing project lesson from 185 / 186 / 188.1 / 188.2 is that *a green that was never
observed RED proves nothing*. These three files are the falsifications for the phase's two hardest
claims — **the arming cannot be turned off** (D-04) and **nothing is sent** (SC#4) — plus the
migration's own constraint gate (D-08 / D-17). Writing them first means the later plans cannot
declare victory by construction.

Output: three new pytest files. **No source file is modified by this plan.**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-VALIDATION.md
@.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md
@CLAUDE.md

**⚠ STANDING RULE FOR THIS PHASE — RE-DERIVE, DO NOT INHERIT.** Every `file:NNN` you are handed
below was measured on 2026-08-07 and three of the four upstream documents corrected each other's
pointers. Locate every anchor by **searching for the named identifier**, never by seeking to a line
number. Phase 188.1 measured nine inherited claims FALSE across five plans.

**The Python interpreter is `backend/venv/Scripts/python.exe`.** A bare `python -m pytest` fails with
`ModuleNotFoundError: No module named 'pydantic_settings'`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author test_189_external_action_model.py — the D-04 and D-02 falsifications, observed RED</name>
  <files>backend/tests/unit/test_189_external_action_model.py</files>
  <read_first>
    - `backend/app/models/harness.py` — read the module docblock (the additive-growth contract), the
      six `*PhaseConfig` classes, the `PhaseConfig` union (search `Field(discriminator=`), `PhaseSpec`
      (search `action_risk_armed`), and the two shipped `@model_validator(mode="after")` methods on
      `WorkflowDefinition` (search `_folder_scope_requires_project`).
    - `backend/tests/unit/test_harness_models.py` — the fixture idiom for building a `WorkflowDefinition`
      dict and the assertion style. Copy its conventions; do not invent a new harness.
    - `.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md` § 1 — the
      `LlmEmitPhaseConfig` analog excerpt and the ⚠ note that both shipped validators RAISE while 189's
      must COERCE.
    - `backend/app/services/harness/grounding.py` — search `KB_TOOLS` for the frozenset's exact members.
  </read_first>
  <action>
Create `backend/tests/unit/test_189_external_action_model.py`. It is the falsification suite for the
7th phase type's two structural guarantees. It MUST fail RED today, because `external_action` is not
yet a member of the `PhaseConfig` union — capture that RED verbatim in the plan SUMMARY, naming the
exact exception text, so the later GREEN is proven and not merely coincident.

Author these cases, each with a docstring naming the decision it falsifies:

1. `test_external_action_is_a_seventh_union_member` (V01 precursor) — a `WorkflowDefinition` dict whose
   single phase carries `config.phase_type == "external_action"` and `config.capability == "send_email"`
   round-trips through `WorkflowDefinition.model_validate` and the parsed phase's config exposes
   `capability`.
2. `test_an_unknown_key_on_the_external_action_config_still_422s` — `_StrictBase` / `extra="forbid"` is
   untouched: adding a bogus key raises `ValidationError`.
3. `test_a_pre_189_row_still_validates` — a definition using only the six shipped `phase_type` values
   parses unchanged. This is the additive-growth guarantee `harness.py`'s docblock states.
4. **`test_action_risk_armed_cannot_be_stored_false_on_this_type` (V06 — D-04)** — build a definition
   whose `external_action` phase carries `action_risk_armed: false` at the `PhaseSpec` level, run it
   through `WorkflowDefinition.model_validate`, and assert the parsed phase reads
   `action_risk_armed is True`. Then assert the same for a definition where the key is ABSENT.
   ⚠ This must exercise the **stored** value through the model, not an engine-time resolution — the
   whole point of D-04 is that a hand-edited JSONB row cannot lie about it.
5. `test_a_shipped_type_may_still_be_unarmed` — the negative control: an `llm_single` phase with
   `action_risk_armed: false` parses as `False`. Without this control, case 4 could pass by arming
   everything.
6. **`test_emptied_available_tools_is_a_validation_error` (V09 — SC#2 / D-03)** — an `external_action`
   phase whose `available_tools` is `[]` or omits its own `capability` does not silently validate.
   Assert the resulting parsed phase has `available_tools == [<capability>]` (if the plan-07 shape
   coerces) OR raises — write the assertion as "the parsed phase's `available_tools` contains the
   capability", which is true under either disposition and is the property SC#2 actually needs.
7. **`test_the_capability_set_is_exactly_three_and_disjoint_from_kb_tools` (D-15 · D-03)** — assert the
   set of admissible capability values is exactly `{"send_email", "create_ticket", "post_message"}`
   read from the model's `Literal` via `typing.get_args`, and assert
   `capabilities & KB_TOOLS == set()` importing `KB_TOOLS` from `app.services.harness.grounding`.
   **Assert the SET, never one name** — a per-name assertion would still pass if a fourth capability
   were added.
8. **`test_a_capability_outside_the_closed_set_is_refused` (D-02)** — a config carrying
   `capability: "wire_transfer"` raises `ValidationError`. The closed-registry rule is *a name not
   present raises*; it is never resolved dynamically and never eval'd.

Mark the file's module docblock with: the phase, `CONN-01`, the V-row ids it satisfies, and the
sentence **"Authored in Wave 0 and OBSERVED RED before any 189 source existed."**
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit/test_189_external_action_model.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - The file exists at `backend/tests/unit/test_189_external_action_model.py` and pytest COLLECTS it
      (collection errors are not an acceptable RED — the RED must come from assertions/validation, not
      from an import error).
    - Running the command above reports **failures, not errors**, and the SUMMARY records the verbatim
      failure text for at least cases 1, 4, 6, 7 and 8.
    - Case 5 (the negative control, `llm_single` unarmed) **PASSES today** — it is the proof that the
      suite is not simply red for every input.
    - Case 3 (`test_a_pre_189_row_still_validates`) **PASSES today**.
    - `grep -c "def test_" backend/tests/unit/test_189_external_action_model.py` returns at least `8`.
    - The file contains the literal token `D-04`, `D-02`, `D-15`, `V06` and `V09`.
  </acceptance_criteria>
  <done>V06 and V09 exist as executable falsifications; their RED is captured verbatim; the two controls that must pass today do pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author test_189_no_egress.py — SC#4's only mechanical proof</name>
  <files>backend/tests/unit/test_189_no_egress.py</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — search `CARD_SUBTREE_PATHS` and
      `cardSubtreeSource` for the `?raw` source-fence idiom being adapted to Python here.
      ⚠ `189-PATTERNS.md` § E measured this at `:100-113`; `189-RESEARCH.md` § C13 said `:260-267`
      and is STALE. Search for the identifier.
    - `backend/app/services/harness/phase_types.py` — search `async def _exec_llm_single` and
      `async def _exec_programmatic` for the executor signature the fence will call.
    - `.planning/phases/189-governed-external-action-node-model/189-RESEARCH.md` § "Security Domain" —
      the V13/SSRF row that records egress as **Phase 190's**, not this phase's.
  </read_first>
  <action>
Create `backend/tests/unit/test_189_no_egress.py`. It is SC#4's proof — *"No live outbound egress ships
in this phase"* — expressed as two mechanically checkable properties rather than as a claim.

**Case A — the source fence (V11).** Walk `backend/app` for `*.py`, read each file as text, and assert
that the case-insensitive word-boundary token `mcp` appears **zero** times. Measured at plan time:
`grep -rni "\bmcp\b" backend/app --include=*.py | wc -l` returns `0`.
⚠ **A fence with no positive control is a fence that can pass vacuously.** Add a control in the same
test: build the same matcher, run it over a small in-test string that DOES contain the token, and
assert the matcher fires. Without that, an accidental typo in the regex turns the whole gate green
forever — the exact failure class `PhaseNodeCard.test.tsx` guards against with its own haystack checks.

**Case B — the patched-transport falsification (V10).** This is the honest half. Assert the
`external_action` executor performs **no network I/O**: patch the HTTP transport(s) the backend can
reach (at minimum `httpx.AsyncClient.send` and `httpx.Client.send`; add `aiohttp` only if it is a
declared dependency — check `backend/requirements*.txt` before naming it) so that any call RAISES a
sentinel exception, then invoke `_exec_external_action(phase, {}, ctx)` and assert it returns normally.
Today this fails RED with an `ImportError`/`AttributeError` because the executor does not exist — that
is the correct Wave-0 RED. Guard the import with a module-level import inside the test body so the file
still COLLECTS.
⚠ Add the falsifying control here too: a second test that patches the same transport and then *does*
make a trivial request, asserting the sentinel raises. If the patch is inert, Case B is vacuous.

**Case C — no `_TOOL_REGISTRY` entry and no `get_tools()` schema (D-22 / SC#4).** Import
`_TOOL_REGISTRY` from `app.services.tool_dispatcher` and `get_tools` from
`app.services.openai_service`, and assert none of the three capability names appears in either. This
is D-22 stated as a guard: **the capability is a step the executor performs, not a tool the LLM may
call**, and putting a schema in `get_tools()` would ship most of the plumbing for live egress.
This case PASSES today and must keep passing — it is a regression fence, not a Wave-0 RED.

Module docblock: phase, `CONN-01`, `SC#4`, `V10`, `V11`, `D-22`, and the sentence
**"Zero MCP code exists in backend/app and this file is what keeps it that way."**
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit/test_189_no_egress.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - The file exists, pytest COLLECTS it, and Case A (the `mcp` fence) **PASSES today** together with
      its positive control.
    - Case C (`_TOOL_REGISTRY` / `get_tools` exclusion) **PASSES today**.
    - Case B fails RED with a message naming the missing `_exec_external_action`, and that text is
      recorded verbatim in the SUMMARY.
    - The transport-patch control test **PASSES today** (proving the patch actually intercepts).
    - `grep -c "hasattr\|getattr\|import" backend/tests/unit/test_189_no_egress.py` is non-zero and the
      file contains the literal tokens `SC#4`, `V10`, `V11` and `D-22`.
  </acceptance_criteria>
  <done>SC#4 has a mechanical proof with a working positive control; D-22 has a standing guard; Case B's RED is captured.</done>
</task>

<task type="auto">
  <name>Task 3: Author test_migration_115.py — the CHECK constraint's positive AND negative controls</name>
  <files>backend/tests/test_migration_115.py</files>
  <read_first>
    - `backend/tests/test_139_migration_090.py` — **the exact model.** Read it whole: the
      rollback-only transaction, the nested savepoint for the rejection branch, the two named
      clean-skip conditions, and the "green-skip until applied, MUST PASS after" contract in its
      docstring.
    - `supabase/migrations/114_harness_audit_action_risk_pending.sql` — the migration shape 115 copies.
    - `.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md` — decision **D-17**.
    - `supabase/full-schema.sql` — search `workflow_phases_status_check` for the live five-value form.
  </read_first>
  <action>
Create `backend/tests/test_migration_115.py`, modelled on `test_139_migration_090.py`.

It gates migration 115 against the **REAL local Postgres at `postgresql://postgres:postgres@127.0.0.1:54322/postgres`**
(override via the `POSTGRES_DSN` env var, exactly as the 139 analog does). A mock store cannot
reproduce a CHECK firing, so this is a live-DB gate.

**Two controls, and both are load-bearing:**

- **Positive (V13a):** inside a transaction that ROLLS BACK, insert or update a `workflow_phases` row
  to `status = 'recorded_not_sent'` and assert it is ADMITTED.
- **Negative (V13b):** in a nested savepoint, attempt `status = 'Not sent — recorded'` (the DISPLAY
  SENTENCE, em-dash included) and assert Postgres raises a check-violation (SQLSTATE `23514`) naming
  `workflow_phases_status_check`.
  ⚠ **This negative control IS decision D-17 expressed as a test.** The CHECK takes the lowercase
  snake_case SLUG; the sentence lives 400 lines and one language away in the client's vocabulary
  layer. A plan that put the sentence in the constraint would make this test green for the wrong
  reason — so also assert that the five shipped statuses (`pending`, `active`, `completed`, `failed`,
  `skipped`) are all still admitted, which is the "adds exactly ONE literal and changes nothing else"
  property.

**Two named clean-skip conditions, copied from the analog:**
1. `:54322` unreachable → `pytest.skip` with a message saying there is no live DB to gate.
2. Migration 115 unapplied → detect by reading the constraint definition from `pg_constraint`
   (`conname = 'workflow_phases_status_check'`) and skipping when `recorded_not_sent` is absent from
   `pg_get_constraintdef`. The skip message MUST say **"expected until plan 189-06 Task 2 applies it"**.

⚠ **ALL writes roll back.** This suite must not mutate dev data — the operator's local database is the
working environment, and CLAUDE.md forbids `supabase db reset`.

Module docblock: phase, `CONN-01`, `D-08`, `D-17`, `V13`, and the green-skip-until-applied contract.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_migration_115.py -q --no-header -rs</automated>
  </verify>
  <acceptance_criteria>
    - The command reports **SKIPPED** (not failed, not errored) with a reason string containing the
      substring `189-06` — because migration 115 does not exist yet. Capture the skip reason verbatim.
    - The file contains the literal em-dash string `Not sent — recorded` inside the NEGATIVE control
      only, and the literal `recorded_not_sent` inside the positive control.
    - `grep -c "rollback\|ROLLBACK\|savepoint\|SAVEPOINT" backend/tests/test_migration_115.py` is non-zero.
    - The file contains the literal tokens `D-08`, `D-17` and `V13`.
    - Running the whole backend 189-scope suite is unchanged apart from this file: it adds SKIPs, never
      failures.
  </acceptance_criteria>
  <done>V13 exists with both controls, rolls everything back, and green-skips with a message that names the plan which will unblock it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-supplied definition JSONB → `WorkflowDefinition.model_validate` | untrusted `capability` / `action_risk_armed` / `available_tools` values cross here |
| test process → local Postgres `:54322` | this plan's only DB contact; every write is inside a rolled-back transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-01 | Elevation of Privilege | `PhaseSpec.action_risk_armed` on an `external_action` phase (**T2 — the D-04 disarm paths**) | mitigate | This plan authors the falsification (Task 1 case 4): a definition round-tripping `armed: false` through `model_validate` must read `True`. It covers ALL SIX write paths at once because every one goes through `WorkflowDefinition.model_validate`. The pin itself lands in plan 189-07. |
| T-189-02 | Tampering | dynamic resolution of an author-supplied `capability` string | mitigate | Task 1 case 8 asserts a name outside the closed set RAISES. Closed-registry discipline (`_TOOL_REGISTRY`, `PROGRAMMATIC_PHASE_REGISTRY`, `EMITTER_REGISTRY`): never resolved dynamically, never `eval`'d. |
| T-189-03 | Information Disclosure | outbound egress from an `external_action` step | mitigate | Task 2 cases A + B are SC#4's only mechanical proof — a source fence with a positive control, plus a patched-transport falsification with its own inertness control. |
| T-189-04 | Tampering | this test suite mutating the operator's local dev data | mitigate | Task 3 copies `test_139_migration_090.py`'s rollback-only transaction + nested savepoint. No `supabase db push`, no `db reset` — CLAUDE.md forbids both. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | **189 installs nothing.** `189-RESEARCH.md` § "Package Legitimacy Audit" records this explicitly: no new dependency anywhere in the phase; the only registry-adjacent item is an icon slug inside the already-installed `@iconify-json/fluent-emoji@1.2.7`. No legitimacy checkpoint is owed because no install task exists. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_189_external_action_model.py tests/unit/test_189_no_egress.py tests/test_migration_115.py -q --no-header -rs`
- The 189-scope backend baseline is unmoved apart from this plan's own new files: re-run the nine-file
  command from `189-RESEARCH.md` § D15 and confirm it still reads **166 passed / 2 failed**, the two
  failures being `tests/test_182_grounding_bundle.py::test_grounding_bundle_returns_server_sourced_palette`
  and `::test_grounding_bundle_fields_come_from_the_bundle` (both `asyncpg … pool is closing`,
  pre-existing and unrelated).
- **No source file under `backend/app`, `frontend/src` or `supabase/` is modified by this plan.**
  Prove it: `git diff --stat -- backend/app frontend/src supabase` is EMPTY.
</verification>

<success_criteria>
- Three new pytest files exist and all three COLLECT.
- Every RED is captured verbatim in the SUMMARY; every control that must pass today does pass.
- `git diff --stat` shows only the three new test files.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-01-SUMMARY.md` when done.
Record verbatim: each observed RED message, each control that passed, and the migration-115 skip reason.
</output>
