---
phase: 189-governed-external-action-node-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_publish_gate.py
  - backend/tests/unit/test_103_grounding_fidelity.py
  - backend/tests/test_182_grounding_bundle.py
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "Both publish-gauntlet conflicts are proven to exist AT HEAD by a driven test, not asserted from a reading of the source — the class of defect (an armed checkpoint killing a run) is BUG-260731-02, which is why migration 114 exists."
    - "CONFLICT 1 is captured WITHOUT the 7th phase type: an armed phase of a SHIPPED type on a golden run subscribes to the ask channel with timeout_seconds=None, which is the 7200 s publish death. It is therefore a pre-existing defect, not one 189 introduces."
    - "CONFLICT 2 is captured WITHOUT the 7th phase type: a shipped phase declaring send_email in available_tools produces an unregistered_tool finding at stage 2.6."
    - "The D-20 leak has a standing guard authored BEFORE the fix: the three capability names are absent from GroundingBundle.tools, so no llm_agent step can whitelist send_email."
    - "Every RED observed in this plan is recorded verbatim in the SUMMARY — a green that was never seen red proves nothing."
  artifacts:
    - path: "backend/tests/test_publish_gate.py"
      provides: "the D-19 golden-run falsification (V20 precursor) — the armed checkpoint must not subscribe on a golden run"
    - path: "backend/tests/unit/test_103_grounding_fidelity.py"
      provides: "the D-20 fidelity falsification (V21) — no unregistered_tool finding for the three capabilities"
    - path: "backend/tests/test_182_grounding_bundle.py"
      provides: "the D-20 leak guard (V22) — the capabilities are NOT in GroundingBundle.tools"
  key_links:
    - from: "backend/tests/test_publish_gate.py"
      to: "backend/app/services/ask_user_service.py subscribe_for_response"
      via: "a monkeypatched subscribe that records its timeout_seconds argument and never resolves"
      pattern: "subscribe_for_response"
    - from: "backend/tests/unit/test_103_grounding_fidelity.py"
      to: "backend/app/services/harness/grounding.py _unregistered_tools"
      via: "stage 2.6 rule 2 over a phase whose available_tools names a capability"
      pattern: "unregistered_tool"
---

<objective>
Capture the two publish-gauntlet conflicts RED, **before either is fixed and before the 7th phase type
exists**, and author the D-20 leak guard.

Purpose: `189-RESEARCH.md` measured that **D-06 ("a workflow containing the node PUBLISHES and RUNS")
is FALSE at HEAD — twice.** That is this phase's headline gate. Both conflicts are provable with
SHIPPED phase types, which matters enormously: it decouples the proof from the 7th type, it shows the
golden-run hang is a **pre-existing** defect rather than one 189 introduces, and it means plans 189-04
and 189-05 can be verified the moment they land instead of waiting four waves for the type.

Output: three extended pytest files, all RED on the new cases (except the leak guard, which is a
regression fence and passes today).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-VALIDATION.md
@CLAUDE.md

**⚠ RE-DERIVE EVERY ANCHOR.** Search for the named identifier; never seek to a line number.

**Interpreter:** `backend/venv/Scripts/python.exe`.

**⚠ `backend/tests/test_182_grounding_bundle.py` carries TWO PRE-EXISTING FAILURES** —
`test_grounding_bundle_returns_server_sourced_palette` and `test_grounding_bundle_fields_come_from_the_bundle`,
both `asyncpg.exceptions._base.InterfaceError: pool is closing`. They are live-DB integration failures,
unrelated to 189, and they must still be exactly two at the end of this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONFLICT 1 RED — an armed phase kills a golden-run publish (D-19)</name>
  <files>backend/tests/test_publish_gate.py</files>
  <read_first>
    - `backend/app/services/harness/publish_service.py` — the 8-stage docblock at the top; search
      `_interactive_phase_failures` (it blocks a publish pre-run for EXACTLY two shapes:
      `phase_type == "llm_human_input"` and a validator with `on_failure == "ask_user"`); search
      `is_golden_run` (four occurrences — note the `SimpleNamespace(` ctx built for the golden run and
      that it does NOT carry `is_golden_run`).
    - `backend/app/services/harness_engine.py` — search `action_risk_armed` (the run-time pause; its
      own comment records that the armed checkpoint is **not a member of `phase.validators` at all**,
      hoisted out by D-187-01), and search `timeout_seconds = None if is_action_risk`.
    - `backend/app/services/ask_user_service.py` — search `async def subscribe_for_response`; its
      docstring states `timeout_seconds=None` means **wait indefinitely**.
    - `backend/app/config.py` — search `harness_publish_max_seconds` (`DEFAULT_LLM_CALL_TIMEOUT_SECONDS * 24` = 7200).
    - `backend/tests/test_publish_gate.py` — read it whole. **Use its existing publish-driving fixture
      and its judge mock. Do not build a second harness.**
  </read_first>
  <action>
Extend `backend/tests/test_publish_gate.py` with the CONFLICT-1 falsification. Use a **shipped** phase
type — an `llm_single` phase carrying `action_risk_armed: true` at the `PhaseSpec` level — so the test
runs today and proves the defect is pre-existing.

Two cases:

1. **`test_an_armed_phase_does_not_subscribe_to_the_ask_channel_on_a_golden_run` (the D-19 property).**
   Monkeypatch `ask_user_service.subscribe_for_response` with a recorder that appends its
   `timeout_seconds` argument to a list and then raises a sentinel (so the test cannot hang — **never
   await the real 7200 s path**). Drive a publish of a definition containing the armed phase, with the
   judge mocked exactly as the existing tests mock it. Assert the recorder was **NEVER CALLED**.
   ⚠ Today this fails RED: the recorder IS called, and its recorded argument is `None`. **Assert both
   halves and record both verbatim** — the call count AND the `None` timeout — because the `None` is
   what `subscribe_for_response`'s own docstring calls "wait indefinitely", and it is the mechanism
   that turns into the 7200 s `golden_run_timeout`.

2. **`test_a_live_non_golden_run_still_pauses_on_an_armed_phase` (the negative control).** Drive the
   same armed phase on a NON-golden run and assert `subscribe_for_response` IS called with
   `timeout_seconds=None`. **This control must pass today and must STILL pass after 189-05 lands** —
   it is what stops the D-19 fix from quietly disarming the checkpoint on real runs, which is precisely
   the wire-around SC#2 forbids.

Add a third, cheap, source-level case:

3. **`test_the_armed_checkpoint_is_not_a_validator` (why stage 2.5 misses it).** Assert that
   `_interactive_phase_failures` returns NO failure for the armed-`llm_single` definition. This is the
   measured reason the phase reaches stage 3 at all, and it must remain true — extending
   `_interactive_phase_failures` to name armed phases is CONFLICT-1 **Option B**, which contradicts
   D-06 outright and is REJECTED. Put that sentence in the test's docstring so the next reader cannot
   mistake this assertion for an oversight.

Each new test's docstring names `D-19`, `D-06`, and `BUG-260731-02` (the same defect class — an armed
checkpoint killing a run — which is why migration 114 exists, and why this must be DRIVEN, never
asserted).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_publish_gate.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - Case 1 fails RED, and the SUMMARY records verbatim: the subscribe call count (expected 0, actual ≥ 1)
      and the recorded `timeout_seconds` value (`None`).
    - Cases 2 and 3 **PASS today**.
    - The test suite completes in under 60 seconds — **no test may await a real 7200 s timeout**; prove
      it by the wall-clock of the pytest run reported in the SUMMARY.
    - `grep -c "D-19" backend/tests/test_publish_gate.py` returns at least `1`.
    - No file outside `backend/tests/` is modified.
  </acceptance_criteria>
  <done>CONFLICT 1 is a driven, fast, RED test with a passing negative control; the rejected Option B is recorded as an assertion, not as prose.</done>
</task>

<task type="auto">
  <name>Task 2: CONFLICT 2 RED — a capability in available_tools blocks stage 2.6 (D-20)</name>
  <files>backend/tests/unit/test_103_grounding_fidelity.py</files>
  <read_first>
    - `backend/app/services/harness/grounding.py` — search `_unregistered_tools` (stage 2.6 rule 2);
      search `tool_names = ` (it is built from `get_tools(None)` — the **LLM-facing schema list**, not
      `_TOOL_REGISTRY`); search `KB_TOOLS`.
    - `backend/app/services/openai_service.py` — search `def get_tools`; read the comment recording the
      asymmetry: `render_template` is *registered in `_TOOL_REGISTRY` but NOT advertised here*.
    - `backend/tests/unit/test_103_grounding_fidelity.py` — read it whole; reuse its fixture shape.
    - `.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md` § 3 — the `KB_TOOLS`
      analog excerpt and the *"a second copy is a safety hole"* argument.
  </read_first>
  <action>
Extend `backend/tests/unit/test_103_grounding_fidelity.py` with the CONFLICT-2 falsification, using a
**shipped** type (`llm_agent`, which already carries `available_tools`) so it runs today.

1. **`test_an_external_capability_in_available_tools_produces_no_unregistered_tool_finding` (V21).**
   Build a phase whose `available_tools` contains `send_email` and run the stage-2.6 fidelity check.
   Assert **no** finding with `code == "unregistered_tool"` names `send_email`.
   ⚠ Today this fails RED — that finding IS produced, because `tool_names` is built from `get_tools(None)`
   and the capability has no schema there. Record the finding dict verbatim.
   Parameterise over **all three** capability names (`send_email`, `create_ticket`, `post_message`), and
   assert on the SET — a per-name test would still pass if only one were registered.

2. **`test_a_genuinely_unknown_tool_still_produces_the_finding` (the negative control).** The same phase
   with `available_tools = ["definitely_not_a_tool"]` DOES produce an `unregistered_tool` finding.
   **This control must pass today and must STILL pass after 189-04 lands.** Without it, plan 189-04
   could make case 1 green by disabling rule 2 altogether — which would retire a shipped governance
   rule rather than widen it by three reviewed names.

Docstrings name `D-20`, `D-06` and `V21`, and state the shape the fix must take: a new closed
`EXTERNAL_ACTION_CAPABILITIES` frozenset **unioned into `tool_names` for the fidelity check ONLY**,
never a rule-2 exemption keyed on `phase_type` (a type special-case inside a shared governance rule).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit/test_103_grounding_fidelity.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - Case 1 fails RED for **all three** capability names, and the SUMMARY records the verbatim finding
      dict for at least `send_email`.
    - Case 2 (the negative control) **PASSES today**.
    - `grep -c "D-20" backend/tests/unit/test_103_grounding_fidelity.py` returns at least `1`.
    - The three capability names appear as a single closed collection in the test file, not as three
      separate string literals scattered across cases.
  </acceptance_criteria>
  <done>CONFLICT 2 is a driven RED across the whole capability set, with a control that forbids fixing it by weakening rule 2.</done>
</task>

<task type="auto">
  <name>Task 3: The D-20 leak guard — the capabilities must never reach GroundingBundle.tools</name>
  <files>backend/tests/test_182_grounding_bundle.py</files>
  <read_first>
    - `backend/app/services/harness/grounding.py` — search `class GroundingBundle` and
      `return GroundingBundle(`; note `tools=sorted(tool_names)` and `tool_names=tool_names` are two
      fields that are equal today **by construction**, and that a docblock nearby asserts that identity
      in prose.
    - `frontend/src/pages/WorkflowBuilderPage.tsx` — search `toolOptions` to see where the bundle's
      `tools` is bound.
    - `frontend/src/components/workflows/PhaseFormPanel.tsx` — search `toolOptions` and the docblock
      containing *"THE OPTION SET IS THE SERVER'S"* — this is the author-facing whitelist rail the leak
      would widen.
    - `backend/tests/test_182_grounding_bundle.py` — read it whole; note its two pre-existing failures.
  </read_first>
  <action>
Extend `backend/tests/test_182_grounding_bundle.py` with the **single most important security property
189 owes** (`189-RESEARCH.md` § Security Domain names it NET-NEW and introduced by this phase):

**`test_external_action_capabilities_are_absent_from_the_author_facing_tool_options` (V22).**
Assert that none of `send_email`, `create_ticket`, `post_message` appears in `GroundingBundle.tools`.

State the threat in the docstring, because the assertion alone does not carry it:
`GroundingBundle.tools` is served on `GET /workflows/grounding-bundle` and bound straight into
`PhaseFormPanel`'s author-facing whitelist rail for `llm_agent` / `llm_batch_agents`. If a capability
reached that list, an author could whitelist `send_email` on an **ordinary, UNARMED `llm_agent` step**,
bypassing the `external_action` type's structural arming entirely — which is exactly the wire-around
D-04 and SC#2 forbid. **This is why the D-20 fix widens `tool_names` and NOT `tools`.**

Assert the SET is disjoint, and add the non-vacuity half: assert `GroundingBundle.tools` is
**non-empty** and contains at least one known shipped tool. A disjointness assertion against an empty
list is vacuous, and a vacuous security guard is worse than none.

⚠ **This test PASSES today** — it is a standing regression fence authored before the change that could
break it, not a Wave-0 RED. Say so in its docstring. To prove it is not vacuous, ALSO write a
temporary local assertion during development that plants one capability into the collection under test
and observes the guard go RED; record that observation in the SUMMARY and remove the plant before
committing.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_182_grounding_bundle.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - The new test **PASSES**, and the file still reports **exactly 2 failures**, both named:
      `test_grounding_bundle_returns_server_sourced_palette` and
      `test_grounding_bundle_fields_come_from_the_bundle` (`asyncpg … pool is closing`, pre-existing).
    - The SUMMARY records the observed RED from the temporary plant, and `git diff` shows the plant is
      NOT in the committed file.
    - The test asserts non-emptiness of `GroundingBundle.tools` as well as disjointness.
    - `grep -c "D-20\|V22" backend/tests/test_182_grounding_bundle.py` returns at least `1`.
  </acceptance_criteria>
  <done>The governance-hole guard exists, is proven non-vacuous by an observed plant, and the file's pre-existing failure count is unchanged at two.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /workflows/grounding-bundle` → the author-facing whitelist rail | the server decides which tool names an author may put on ANY step; widening it widens what an unarmed step may do |
| the publish golden run → the ask_user channel | a synchronous HTTP publish request blocks on a human-facing channel that may never answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-05 | Elevation of Privilege | `GroundingBundle.tools` → `PhaseFormPanel` `toolOptions` (**T1 — the D-20 governance hole**) | mitigate | Task 3 authors V22 as a standing fence: the three capability names are asserted absent from `GroundingBundle.tools`, with a non-emptiness check so the disjointness cannot pass vacuously, and an observed plant proving it fires. The fix in 189-04 widens `tool_names` only. |
| T-189-06 | Denial of Service | the publish request thread on an armed phase | mitigate | Task 1 proves the 7200 s hang exists and pins the property that fixes it (no ask-channel subscribe on a golden run). ⚠ The TEST itself must never await the real timeout — the monkeypatched recorder raises a sentinel. |
| T-189-07 | Repudiation | a golden run auto-approving an armed checkpoint | mitigate | Task 1 case 2 pins that a NON-golden run still pauses. D-19 REJECTED Option C (publishing a synthetic approval onto the ask channel) because it writes a `validator_ask_user_approved` receipt claiming a human approved when none did — the Control-Room `consequence ≠ receipt` rule. Plan 189-05 must satisfy this test without writing any approval receipt. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs nothing (`189-RESEARCH.md` § Package Legitimacy Audit). No install task exists in this phase, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/test_publish_gate.py tests/unit/test_103_grounding_fidelity.py tests/test_182_grounding_bundle.py -q --no-header`
- The 189-scope nine-file baseline from `189-RESEARCH.md` § D15 is re-run and the DELTA is accounted
  for exactly: the only new failures are the three intentional REDs from Tasks 1 and 2; the two
  `test_182_grounding_bundle.py` `pool is closing` failures are unchanged.
- `git diff --stat -- backend/app frontend/src supabase` is EMPTY — this plan modifies no source.
</verification>

<success_criteria>
- Both publish-gauntlet conflicts are proven RED at HEAD, by driven tests, in under 60 seconds total.
- Every RED is recorded verbatim; every control that must pass today passes.
- The D-20 leak guard exists and was observed firing against a plant.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-02-SUMMARY.md` when done.
Record: the verbatim CONFLICT-1 RED (call count + `timeout_seconds`), the verbatim CONFLICT-2 finding
dict, the observed plant-RED for V22, and the pytest wall-clock.
</output>
