---
phase: 189-governed-external-action-node-model
plan: 12
type: execute
wave: 5
depends_on: ["189-07", "189-10"]
files_modified:
  - frontend/src/components/workflows/definitionOps.ts
  - frontend/src/components/workflows/definitionOps.test.ts
  - frontend/src/components/workflows/StepTypePicker.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx
  - frontend/src/components/workflows/phaseVocabulary.corpus.test.ts
  - frontend/src/components/workflows/PhaseFormPanel.test.tsx
  - frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "SC#1: a user can PLACE the node — the step-type picker offers SEVEN choices, external_action is LAST so no shipped position moves, and slugForType plus minimalPhaseFor produce a valid phase for it."
    - "The client mirrors the backend union, it never defines it: PhaseTypeId is a mirror of the PhaseConfig discriminated union, and the docblock pointing at that union is CORRECTED because its line reference is already stale in the shipped source."
    - "The two typecheck-FORCED sites are filled (the slug-base table and the required-config switch with its never-exhaustiveness guard), and the two SILENT ones are filled deliberately (the type union and the fixed presentation order) — a subset satisfies the order tuple, so omitting the 7th compiles cleanly and the picker simply never offers the type."
    - "The roughly sixteen hard-coded six-count assertions across five suites are moved in the SAME COMMIT as the 7th type. The count gate pins test COUNTS, not assertions, so nothing warns — a green tsc and a red vitest is the signature."
    - "The shipped prose claiming the picker offers six choices and never fewer than six is corrected, because it becomes wrong the moment this lands."
    - "S2 binds: every test-count change lands with its vitest-count-gate BASELINE pin in the same commit."
  artifacts:
    - path: "frontend/src/components/workflows/definitionOps.ts"
      provides: "PhaseTypeId 7th member, the presentation order, the slug base, and the required-config switch arm"
      contains: "external_action"
  key_links:
    - from: "frontend/src/components/workflows/definitionOps.ts PhaseTypeId"
      to: "backend/app/models/harness.py PhaseConfig union"
      via: "a client mirror of a backend Literal, with a corrected source pointer"
      pattern: "PhaseTypeId"
    - from: "PHASE_TYPE_ORDER"
      to: "the step-type picker's rendered rows"
      via: "a fixed presentation tuple the picker iterates"
      pattern: "PHASE_TYPE_ORDER"
---

<objective>
Make the seventh type PLACEABLE on the canvas, and pay the bookkeeping debt that comes with it in the
same commit.

Purpose: SC#1 is "a user can place a governed external-action node on the canvas". That reduces to four
edits in one file — but `189-RESEARCH.md` measured that a 7th `phase_type` is **27 enumeration sites, of
which only 5 are typecheck-forced and roughly 16 are hard-coded six-count test assertions across five
suites.** The asymmetry between "the compiler will tell me" and "nothing will tell me" is named as the
single biggest execution risk in this phase, and the count gate cannot see it coming because it pins
test COUNTS, not assertions.

⚠ **The warning sign is a green `tsc` and a red vitest.** Treat the pin list below as a checklist and
move every one in this commit.

Output: four `definitionOps.ts` edits, roughly sixteen assertion updates, two prose corrections, and the
gate pins.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-governed-external-action-node-model/189-CONTEXT.md
@.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md
@.planning/phases/189-governed-external-action-node-model/189-PATTERNS.md
@CLAUDE.md

RE-DERIVE EVERY ANCHOR by searching for the identifier — and in this plan, RE-DERIVE THE PIN LIST TOO.
The sixteen assertions were enumerated on 2026-08-07; find them yourself with a grep for six-count
assertions across `frontend/src/components/workflows/*.test.*` and `frontend/src/pages/*.test.*` before
trusting any list.

BASELINES: `npx tsc --noEmit -p tsconfig.app.json` = **33** · `node scripts/vitest-count-gate.cjs` =
**total 2508, 45/45**. ⚠ The gate's `failed` column is rot on this machine; the COUNT columns are the
backstop.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The four definitionOps edits — two forced, two silent, one stale pointer</name>
  <files>frontend/src/components/workflows/definitionOps.ts, frontend/src/components/workflows/definitionOps.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/definitionOps.ts` — search `PhaseTypeId` and read its docblock.
      ⚠ **That docblock's backend pointer is ALREADY STALE in the shipped source** — it cites a line
      range for the `PhaseConfig` union that has moved. Re-derive the true location in
      `backend/app/models/harness.py` and correct it here. This is the same drift class 188.2 found four
      times in the icon-convention reference.
    - `frontend/src/components/workflows/definitionOps.ts` — search `PHASE_TYPE_ORDER` and read its
      docblock: it is a READONLY TUPLE, not a set, because "the picker's third option moved" is a UX
      regression a test should catch. ⚠ It uses `satisfies`, so a SUBSET satisfies it — omitting the 7th
      compiles cleanly and the picker simply never offers the type. Silent.
    - `frontend/src/components/workflows/definitionOps.ts` — search `SLUG_BASE`. It is
      `satisfies Record<PhaseTypeId, string>` and IS a typecheck error without a 7th entry. Note the
      register of the six shipped base tokens: single plain-language verbs or nouns.
    - `frontend/src/components/workflows/definitionOps.ts` — search `requiredConfigFor` and its
      exhaustiveness guard (a `never`-typed assignment in the default arm). It IS forced. Read its
      docblock rule for what belongs: REQUIRED keys only, never a field that has a backend default.
    - `frontend/src/components/workflows/definitionOps.ts` — search for the prose claiming the picker
      offers six choices and "never fewer than six". It becomes wrong when this lands.
    - `backend/app/models/harness.py` — search `ExternalActionPhaseConfig` for the exact field names and
      which of them have defaults.
    - `.planning/phases/189-governed-external-action-node-model/189-UI-SPEC.md` section 6d — why the 7th
      must be APPENDED LAST.
  </read_first>
  <action>
Four edits in `frontend/src/components/workflows/definitionOps.ts`:

1. `PhaseTypeId` — add `"external_action"` as the seventh member, LAST. Correct the docblock's stale
   backend pointer while you are in it, and update its count from six to seven. The client MIRRORS the
   backend union and never defines it; say so as the shipped docblock already does.
2. `PHASE_TYPE_ORDER` — append `external_action` LAST, after the sixth. ⚠ **Append, never insert.** The
   tuple is a fixed presentation order and moving a shipped option is a UX regression the tuple exists to
   catch.
3. `SLUG_BASE` — add the seventh entry (typecheck-FORCED). The value is a single plain-language token
   matching the register of the six shipped ones — a short verb or noun such as `send` or `act`.
   ⚠ `external_action` itself does NOT fit that register; do not use the type id as the slug base.
4. `requiredConfigFor` — add the seventh switch arm (typecheck-FORCED by the exhaustiveness guard).
   Return only the REQUIRED keys per its own docblock rule — a field with a backend default does not
   belong here. Decide from `ExternalActionPhaseConfig`'s actual field defaults, not from memory.

Then correct the shipped prose that claims the picker offers six and never fewer than six.

Extend `definitionOps.test.ts` with:
- `slugForType` produces a valid slug for the new type and `minimalPhaseFor` produces a phase that
  round-trips.
- `PHASE_TYPE_ORDER` has SEVEN entries and its first six are UNCHANGED and in their shipped order —
  assert the shipped prefix explicitly, so an accidental insert is caught rather than merely the length.
- `SLUG_BASE` and `requiredConfigFor` both answer for the new type.

⚠ **THOSE NEW CASES CHANGE `definitionOps.test.ts`'s TEST COUNT, so move its `BASELINE` pin in
`scripts/vitest-count-gate.cjs` IN THIS TASK'S COMMIT.** S2 binds per commit, not per plan: a test change
without its pin leaves HEAD red, and a pin edit without the test leaves the gate blind. Task 2 moves the
pins for the OTHER suites it touches; this one belongs here, with the tests that moved it.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `PhaseTypeId` has SEVEN members with `external_action` LAST, and its docblock's backend pointer is
      re-derived and correct — quote the old and new pointer in the SUMMARY.
    - `PHASE_TYPE_ORDER` has SEVEN entries; the first six are byte-identical to HEAD in the same order,
      asserted by a test on the prefix, not just the length.
    - `SLUG_BASE`'s new value is a single short token in the register of the six shipped ones and is NOT
      the string `external_action`.
    - `requiredConfigFor`'s new arm returns REQUIRED keys only; the SUMMARY names which
      `ExternalActionPhaseConfig` fields have backend defaults and were therefore excluded.
    - The "never fewer than six" prose is corrected.
    - `npx tsc --noEmit -p tsconfig.app.json` reads exactly 33 at the END of this task. Record the count
      BEFORE the two forced edits as proof the compiler forced them.
    - `definitionOps.test.ts`'s `BASELINE` pin moved in THIS task's commit — `git show --stat` for it lists
      BOTH `definitionOps.test.ts` and `scripts/vitest-count-gate.cjs`.
    - `node scripts/vitest-count-gate.cjs` reports OK at the end of this task, with no per-file DECREASE.
  </acceptance_criteria>
  <done>The type is placeable, the order is appended not inserted, and the docblock no longer points at a moved line.</done>
</task>

<task type="auto">
  <name>Task 2: Pay the silent six-count debt across five suites, in this commit</name>
  <files>frontend/src/components/workflows/StepTypePicker.test.tsx, frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx, frontend/src/components/workflows/phaseVocabulary.corpus.test.ts, frontend/src/components/workflows/PhaseFormPanel.test.tsx, frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - Re-derive the pin list yourself. Grep `frontend/src/components/workflows/*.test.*` and
      `frontend/src/pages/*.test.*` for hard-coded length-six assertions and for describe/it titles
      containing "six". Measured on 2026-08-07 the affected suites were `definitionOps.test.ts`,
      `StepTypePicker.test.tsx`, `WorkflowCanvas.editing.test.tsx`, `phaseVocabulary.corpus.test.ts`,
      `PhaseFormPanel.test.tsx` and `PhaseFormPanel.rails.test.tsx` — **verify that list rather than
      trusting it.**
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — search for the six-count assertion on
      the card-subtree path list and the one on the icon-tint table. ⚠ **The subtree path list must STAY
      at six** (this phase adds no module there); the ICON TINT pin moves in plan 189-13, NOT here. Read
      both so you do not move the wrong one.
    - `frontend/src/components/workflows/StepTypePicker.tsx` — search `PHASE_TYPE_ORDER` and the row
      render, so you can see that the picker's seventh row comes for free from the order tuple plus the
      shared vocabulary maps, with zero picker-specific copy.
    - `scripts/vitest-count-gate.cjs` — search `TARGETS` and `BASELINE`. This plan adds NO new suite, so
      only `BASELINE` moves.
  </read_first>
  <action>
Update every re-derived six-count assertion to seven, and rename any describe or it title that says
"six", **in the SAME COMMIT as Task 1**. The count gate pins test COUNTS, not assertions, so nothing
warns you — this is bookkeeping debt, not a broken feature, and treating it as a surprise is how a
phase loses a day.

Rules while doing it:

- ⚠ **Do not blanket-replace six with seven.** At least two six-count assertions must NOT move: the
  card-subtree path list stays at six (189 adds no module there), and the contract-export count stays at
  six (189 adds a FIELD, not a type). Check each one's SUBJECT before changing it.
- The icon-tint six-count pin belongs to plan 189-13 and must NOT be moved here — that plan adds the
  tint entry, and S2 requires the pin to move with the change that causes it.
- Where a suite loops over "the non-emit types" or a similar named subset, decide explicitly whether the
  new type belongs in that subset and record the decision in the test's comment. A silent inclusion is
  how a loop starts asserting something nobody intended.
- Where the picker's row COUNT is asserted, also assert the seventh row is LAST — the position matters
  and a bare count would pass on an insert.

Update `scripts/vitest-count-gate.cjs` `BASELINE` for every suite whose test COUNT changed. ⚠ A test
whose ASSERTION changed but whose COUNT did not needs no pin move — moving it anyway would make the gate
disagree with reality.

Then run the FULL gate and reconcile it line by line in the SUMMARY: every moved pin named, every
unchanged pin confirmed unchanged.
  </action>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/vitest-count-gate.cjs` reports OK with 45/45 pinned files present and NO per-file
      decrease. ⚠ The `failed` column is NOT read as evidence — the COUNT columns are.
    - `cd frontend; npx vitest run src/components/workflows src/pages` shows no NEW failures relative to
      the HEAD baseline. Any pre-existing failure is named in the SUMMARY.
    - The card-subtree path-list six-count assertion is UNMOVED, and the contract-export six-count
      assertion is UNMOVED — verify both with `git diff`.
    - The icon-tint six-count assertion is UNMOVED in this plan.
    - Every suite whose count changed has its `BASELINE` pin moved in THIS commit; the SUMMARY lists each
      file with its old and new count.
    - No describe or it title in the changed suites still says "six" where seven is meant.
    - The picker suite asserts the seventh row is LAST, not merely that there are seven.
  </acceptance_criteria>
  <done>The silent debt is paid in the commit that created it, the two counts that must not move did not move, and the gate is reconciled line by line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the backend `PhaseConfig` union to the client `PhaseTypeId` mirror | the client mirrors a server-owned closed set; a drift here means the picker offers or omits a type the server does not agree about |
| the picker to a newly created phase | `minimalPhaseFor` produces a definition fragment that is later re-validated server-side by `WorkflowDefinition.model_validate` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-36 | Tampering | the client type mirror drifting from the backend union | mitigate | The mirror is explicitly documented as a MIRROR, its stale backend pointer is re-derived and corrected, and the capability half of the same mirror gets a cross-language agreement fence in plan 189-14. A client-defined type set would be a second home for a server-owned fact. |
| T-189-37 | Spoofing | a client-assembled config bypassing server validation | accept-and-inherit | `minimalPhaseFor` produces a fragment, not an authority. Every write goes through `WorkflowDefinition.model_validate`, where `_StrictBase`, the capability `Literal` and the D-04 arming pin all apply. `requiredConfigFor` returning required keys only keeps the client from inventing values the backend defaults. |
| T-189-38 | Repudiation | shipped prose claiming the picker never offers fewer than six | mitigate | Corrected in the same commit as the change that falsifies it. |
| T-189-30 | Tampering | moving a six-count pin that must NOT move | mitigate | Acceptance criteria name the two that must stay at six (the fenced card-subtree path list and the contract-export count) and the one that belongs to another plan (the icon tint), and require `git diff` verification of each. A blanket six-to-seven replace would silently reduce fence coverage. |
| — | Information Disclosure / Denial of Service / Elevation of Privilege | — | n/a | **No new attack surface.** This plan widens a client-side union and updates test assertions. It opens no route, reads no credential and makes no request. Stated with its reason rather than omitted. |
| T-189-SC | Tampering | npm/pip/cargo installs | accept | 189 installs nothing. No install task exists in this phase, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `cd frontend; npx tsc --noEmit -p tsconfig.app.json` reads exactly 33.
- `node scripts/vitest-count-gate.cjs` reports OK, 45/45, no per-file decrease.
- `cd frontend; npx vitest run src/components/workflows/definitionOps.test.ts src/components/workflows/StepTypePicker.test.tsx`
- `git diff --stat` lists exactly the eight declared files. No backend file and no fenced card-subtree
  module is touched.
</verification>

<success_criteria>
- The picker offers seven choices with the seventh LAST and the shipped six in their original order.
- Both typecheck-forced sites and both silent sites are filled, and the stale backend pointer is fixed.
- Every six-count assertion that should move has moved, the ones that must not have not, and the gate is
  reconciled.
</success_criteria>

<output>
Create `.planning/phases/189-governed-external-action-node-model/189-12-SUMMARY.md` when done.
Record: the re-derived pin list with old and new counts per file, the corrected backend pointer
(old and new), the pre-edit `tsc` count proving the two forced sites forced, and explicit confirmation
that the subtree-path, contract-export and icon-tint counts were NOT moved.
</output>
