---
phase: 188-non-technical-run-observability
plan: 06
type: execute
wave: 5
depends_on: ["188-05"]
files_modified:
  - frontend/src/components/workflows/runVocabulary.ts
  - frontend/src/components/workflows/PhaseNodeCard.tsx
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
  - frontend/src/index.css
autonomous: true
requirements: [RUNVIZ-01]

must_haves:
  truths:
    - "Each of the seven readings paints a distinct RING SHAPE — arc geometry is the state and colour only ever reinforces (sketch 153-A, greyscale is a BUILD criterion)"
    - "Phase 188 introduces ZERO net-new glyphs anywhere; the pause chip is two 3x9px rectangles, never the ‖ character (D-188-06)"
    - "Arc gaps are placed with stroke-dashoffset, NEVER with a rotation — the composed-transform bug shipped twice in the sketch's own drafts (D-188-07)"
    - "Every ring number is COMPUTED from offset = (D + G/2) − p in code; the decimals in the UI-SPEC table are expected results for falsification, not literals to paste"
    - "The ⛨ governance seal renders byte-identically at all SEVEN readings — the shipped four-value guard is widened to seven (D-188-04, SPEC Req 1 second assertion)"
    - "The canvas business words live in runVocabulary.ts, NOT in phaseState.ts — one derivation, two vocabularies (D-188-02)"
    - "The run-time waiting words are 'Paused for your answer' — not equal to the shipped 'Waits for you' badge and not a tense variant of it (D-188-05, SPEC Req 5)"
    - "The card body is ONE budget: title → subtitle → run line → badges. technicalLine is DECLINED by this phase and stepNumber stays unrendered (D-188-17)"
    - "No overflow-hidden anywhere in the node subtree, and no focusable control inside the card — the run line is a <p> (D-188-07)"
    - "The run line renders in run mode for EVERY reading including Not started, so a card never changes height mid-run"
  artifacts:
    - path: "frontend/src/components/workflows/runVocabulary.ts"
      provides: "The canvas business-word table: reading → word + optional clause + ring geometry inputs + border token"
      contains: "Paused for your answer"
    - path: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      provides: "NodeRunStatus narrowed to CanvasReading; the status ring; the run line; the run-state border branch"
    - path: "frontend/src/index.css"
      provides: "The single ringspin keyframe and .canvas-ring-spin, reduced-motion guarded"
  key_links:
    - from: "frontend/src/components/workflows/runVocabulary.ts"
      to: "frontend/src/lib/phaseState.ts"
      via: "imports the CanvasReading type only — the derivation stays in lib, the words stay here"
      pattern: "CanvasReading"
    - from: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      to: "frontend/src/components/workflows/runVocabulary.ts"
      via: "reading → word/clause/geometry lookup"
      pattern: "runVocabulary"
---

<objective>
Turn the 62px icon well into the status dial, and give every node its own sentence.

Purpose: sketch 153-A won because state is carried as **arc geometry**, which spends no badge slot, no
`stepNumber`, no `technicalLine` and no glyph — and survives colour being switched off, which is that
sketch's whole acceptance test and a build criterion rather than a preference. Sketch 154-A adds that
each terminal step states its own truth inside its own card, so the run band can shrink to one line.

Output: the canvas word table, the ring, the run line, and a seal-invariance guard widened from four
run states to seven.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/188-non-technical-run-observability/188-SPEC.md
@.planning/phases/188-non-technical-run-observability/188-CONTEXT.md
@.planning/phases/188-non-technical-run-observability/188-UI-SPEC.md
@.planning/phases/188-non-technical-run-observability/188-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
</context>

<interfaces>
- `frontend/src/lib/phaseState.ts` — `type CanvasReading = "not-started" | "running" | "done" | "failed" | "skipped" | "waiting-for-you" | "unknown"`.
- `frontend/src/types/index.ts:1003-1008` — `EmitFailure = model_failed_to_emit | citation_gate_rejected | render_failed | integrity_failed | no_template_bound`. The failure clause set is derived ONLY from this typed enum.
- `frontend/src/components/workflows/PhaseNodeCard.tsx` at HEAD:
  - `:173-179` — `export type NodeRunStatus = string`, deliberately opaque, with a docblock saying Phase 188 replaces it with its own union and cannot break a 184 caller because 184 has none.
  - `:203-204` — `status?: NodeRunStatus`, "Declared, rendered as nothing in 184".
  - `:304-315` — the card `<div>`: `mx-auto block w-[248px] rounded-[22px] border pb-5 pt-[42px] px-5 text-center`, `bg-card/30 backdrop-blur-sm`, the border ternary `selected > grounded > default`, and `style={{ minHeight: CANVAS_LAYOUT.NODE_MIN_HEIGHT }}`.
  - `:317-331` — title `<p>`, subtitle `<p className="mt-1 text-[11px] leading-snug text-muted-foreground">`, then the `technicalLine` `<p>` (which stays unpassed), then the badge row.
  - `:403-443` — the ⛨ seal: `pointer-events-none absolute right-[17px] top-[11px] z-[6] grid h-[21px] w-[21px]`, reading `grounded` and nothing else.
  - `:445-475` — the 3D mark well: `pointer-events-none absolute left-1/2 top-[-26px] grid h-[62px] w-[62px] -translate-x-1/2 place-items-center`, overflowing the node box upward by 26px.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:1017-1040` — the shipped seal-identity render over
  `["idle", "running", "needs-you", "failed"]`, a FOUR-value loop over the opaque 184 alias.
- `frontend/src/components/workflows/nodePresentation.ts:174-190` — `VERDICT_MARK`; `unknown`'s glyph `?` at `:185`.
- `frontend/src/components/workflows/PhaseNode.tsx:174-180` — the shipped design-time badge literal `Waits for you`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The canvas business-word table</name>
  <files>frontend/src/components/workflows/runVocabulary.ts</files>
  <read_first>
    - `.planning/phases/188-non-technical-run-observability/188-UI-SPEC.md` § "The seven node readings — LOCKED words", § "The failure clause is a CLOSED set of three", § "The Status Ring — buildable values", § "The card border — precedence".
    - `.planning/phases/188-non-technical-run-observability/188-CONTEXT.md` D-188-04, D-188-05, D-188-06.
    - `frontend/src/components/workflows/phaseVocabulary.ts` — the right SHAPE for a vocabulary module in this directory (and its `?raw`-fenced purity), which this file mirrors without merging into.
    - `frontend/src/lib/phaseState.ts` — the type this file imports and the derivation it must NOT duplicate.
    - `frontend/src/types/index.ts:1003-1008` — `EmitFailure`.
  </read_first>
  <action>
Create `frontend/src/components/workflows/runVocabulary.ts`. It imports the `CanvasReading` **type** from
`@/lib/phaseState` and the `EmitFailure` type from `@/types`, and nothing else. It contains **no
derivation** — no status mapping, no `retrying` collapse, no DB literals. D-188-02: one derivation, two
vocabularies; siting the words here rather than in `phaseState.ts` is what keeps Req 8's fence able to
tell vocabulary from derivation.

Export:

1. `RUN_READING_WORD: Record<CanvasReading, string>` with the LOCKED words, which may not be reworded:
   `not-started` → `Not started`; `running` → `Running`; `done` → `Complete`; `failed` → `Failed`;
   `skipped` → `Skipped`; `waiting-for-you` → `Paused for your answer`; `unknown` → `State unknown`.
2. `runReadingClause(reading: CanvasReading, emitFailure?: EmitFailure | null): string | null` — the
   clause rendered after the word:
   - `skipped` → `— the run took a different path`
   - `waiting-for-you` → `— it needs your reply before it can continue`
   - `unknown` → `— we can't tell what happened to this step`
   - `failed` → exactly three closed outcomes keyed off the typed enum:
     `model_failed_to_emit | render_failed | no_template_bound` → `— it could not produce the document`;
     `citation_gate_rejected | integrity_failed` → `— its answer did not pass the required checks`;
     `null`/absent → `— this step did not finish`
   - every other reading → `null`.
   Write the `failed` branch as an exhaustive `switch` over `EmitFailure` with a `default` arm returning
   the third clause, so adding a member to the enum later is a visible decision.
3. `runReadingLabel(reading, emitFailure?)` — word plus clause joined with a space, used as the node's
   accessible-name suffix and as the run-line text. One function, so the visible line and the accessible
   name cannot disagree.
4. `RUN_READING_BORDER: Partial<Record<CanvasReading, string>>` — only three entries:
   `running` → the primary border utility, `waiting-for-you` → the warning border utility,
   `failed` → the destructive border utility. `done`, `not-started`, `skipped` and `unknown` are ABSENT
   on purpose; a comment must say why *Complete* does not recolour the border (every finished step turning
   green floods the canvas exactly as the run ends and spends the 10% accent budget on the least urgent news).
5. `RING_GEOMETRY: Record<CanvasReading, RingSpec>` where `RingSpec` describes the arc as **fractions of
   the circumference plus a gap-centre path position**, never as pre-computed decimals. Give the module a
   `ringDash(spec, circumference)` helper that computes `stroke-dasharray` and `stroke-dashoffset` from
   `offset = (D + G/2) − p`. The specs, from the UI-SPEC table:
   - `not-started`: no arc at all (track only)
   - `running`: dash `0.26C`, gap `0.74C`, offset irrelevant (it spins), spinning true
   - `done`: no dash pattern — an unbroken circle
   - `waiting-for-you`: one gap of `0.26C` centred at `p = 0.75C` (12 o'clock)
   - `failed`: two arcs of `0.42C` with two gaps of `0.08C`, first gap centred at `p = 0.375C`
   - `skipped`: dasharray `5 7`, offset 0
   - `unknown`: dasharray `1.5 6`, offset 0
   ⚠ The decimals in the UI-SPEC table (`55.543 158.085`, `25.635`, `89.724 17.090`, `18.158`) are the
   EXPECTED RESULTS of that formula at `r = 34` ⇒ `C = 2π·34 = 213.628`. They are for falsification in the
   test, not literals to paste into the source. Compute them.

**Zero net-new glyphs.** This module exports no glyph strings at all — the reading channel is geometry
plus word. Record that in the header docblock, and record that `⤳` may NOT be reused for *skipped* here
because it is already the on-fail `skip_to_phase` branch edge at `PhaseNode.tsx:257`.
  </action>
  <acceptance_criteria>
    - `grep -c 'Paused for your answer' frontend/src/components/workflows/runVocabulary.ts` returns 1
    - `grep -c 'Waits for you' frontend/src/components/workflows/runVocabulary.ts` returns 0
    - `grep -Ec '⤳|‖|⋯' frontend/src/components/workflows/runVocabulary.ts` returns 0
    - `grep -Ec 'pending|active|completed|skipped["\x27]\s*:|phase_index' frontend/src/components/workflows/runVocabulary.ts` shows no raw DB status literal and no `phase_index`
    - `grep -Ec '55\.543|158\.085|25\.635|89\.724|18\.158' frontend/src/components/workflows/runVocabulary.ts` returns 0 — the geometry is computed, not pasted
    - The file's imports are exactly two: `@/lib/phaseState` (type-only) and `@/types` (type-only)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>The canvas words, clauses, border tokens and ring specs exist as a pure vocabulary module with zero derivation and zero glyphs.</done>
</task>

<task type="auto">
  <name>Task 2: The status ring, the run line, and the run-state border on PhaseNodeCard</name>
  <files>frontend/src/components/workflows/PhaseNodeCard.tsx, frontend/src/index.css</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseNodeCard.tsx` — the whole file. Specifically `:173-179` (the opaque alias), `:203-204` (the prop), `:304-331` (the card div, border ternary and body stack), `:403-443` (the seal), `:445-475` (the 3D mark well and its overflow warning).
    - `.planning/phases/188-non-technical-run-observability/188-UI-SPEC.md` § "The Status Ring" (placement, geometry, motion, the greyscale clause), § "The Card Body Budget" (stacking order and the three rules), § "Two Facts on One Node", § "Accessibility Contract → The ring's role".
    - `.claude/skills/sketch-findings-agentic-rag/references/canvas-frame-and-node-anatomy.md` — the 137-B frozen geometry.
    - `.claude/skills/sketch-findings-agentic-rag/references/graded-governance.md` — the seal is load-bearing and may never be conditional on run state.
    - `.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md` §4 — the canvas glyph vocabulary. Read before drawing any mark.
    - `frontend/src/index.css` — the `@layer utilities` block where the one keyframe lands.
  </read_first>
  <action>
Four edits to `PhaseNodeCard.tsx`, plus one to `index.css`.

**(a) Narrow the alias.** Replace `export type NodeRunStatus = string` with
`export type NodeRunStatus = CanvasReading`, importing the type from `@/lib/phaseState`. Rewrite the
docblock: 184 declared it opaque precisely so 188 could land its own union without breaking a caller;
184 has no run-state caller, so this cannot break one. Also extend the props docblock for `status?:` —
it is no longer "rendered as nothing".

**(b) The ring.** Render it as a **sibling of the 3D-mark span, immediately BEFORE it**, so the mark
always paints above the arc. Wrapper: `pointer-events-none absolute left-1/2 top-[-31px] h-[72px] w-[72px] -translate-x-1/2 z-[5]`
with `aria-hidden="true"`, containing `<svg viewBox="0 0 72 72" class="block h-full w-full overflow-visible">`.
Inside: a track `<circle r="34" cx="36" cy="36" fill="none" stroke-width="2.5">` at the muted-foreground
token at 0.35 alpha, and — for every reading except `not-started` — an arc `<circle>` with
`fill="none" stroke-width="3.5" stroke-linecap="round"` whose `strokeDasharray` / `strokeDashoffset` come
from `ringDash()`.

  - `top-[-31px]` is `-26 − (72 − 62)/2`, i.e. concentric with the shipped 62px well. Do not round it.
  - `z-[5]` sits below the seal (`z-[6]`) and the verdict mark (`z-[8]`). None of the three overlap
    geometrically; `z` is hygiene, not arbitration.
  - ⚠ **Never place a gap with a rotation.** Setting the SVG `transform` attribute *and* CSS
    `transform-box`/`transform-origin` composes them and pivots about a doubled offset — that bug shipped
    in the sketch's first two drafts and put the amber gap in the wrong quadrant. The ONLY transform in
    the ring subtree is the `.canvas-ring-spin` class. Say so in a comment.
  - ⚠ **No `overflow-hidden`** on the ring, the svg, the card or the node box. The mark already overflows
    upward by 26px and the ring by 31px. `overflow-visible` on the `<svg>` is belt-and-braces.
  - For `waiting-for-you` only, render the **pause chip** inside the ring's 12-o'clock gap:
    `absolute left-1/2 top-[-37px] -translate-x-1/2 z-[9] flex gap-[3px] rounded px-[5px] py-[3px]` with
    a background of `--background` and a `--warning` border, containing **two `<span>` elements of
    `3px × 9px`** filled `--warning`. They are rectangles, NOT the `‖` character — this is exactly how
    D-188-06's zero-net-new-glyph rule survives while the waiting state still gets a mark.

**(c) The run line.** Insert one `<p data-testid="canvas-node-run-line">` immediately AFTER the subtitle
`<p>` and BEFORE the `technicalLine` block, styled `mt-1 text-[11px] leading-snug line-clamp-2` with a
`--foreground`-family text colour (never `--muted-foreground-dim`, never a `--panel-*` token — this
surface sits on `--background`). Its content is `runReadingLabel(status, emitFailure)`.
Render it **whenever `status` is supplied**, including for `Not started` — if it appeared only for
terminal readings, every card would change height as the run progressed, which is reflow on a live canvas
on the exact surface a non-technical person is watching. For the same reason, when `status` is supplied
the card's `minHeight` becomes a named local constant `RUN_MODE_NODE_MIN_HEIGHT = 120` instead of
`CANVAS_LAYOUT.NODE_MIN_HEIGHT`; keep `CANVAS_LAYOUT` itself untouched (`EDGE_ANCHOR_Y` is measured from
the node TOP, so edges are unaffected either way — that is what D-183-12 exists for).
The run line is a `<p>`: **no tooltip trigger, no button, no `tabIndex`, no click handler.** One tab stop
per node is a canvas-level invariant.
Add the phase's `emitFailure` as a new optional prop (`emitFailure?: EmitFailure | null`), threaded by
`PhaseNode` in Plan 07. **`Phase.error` is NEVER rendered here** — it is free-form harness text that may
carry slugs, model ids or gate identifiers, which is precisely what Req 2's grep forbids on this surface.
Record that in a comment.

**(d) The border.** Prepend ONE branch to the shipped ternary so precedence reads
`run state (when supplied) > selected > grounded > default`, taking the class from `RUN_READING_BORDER`.
The three tokened readings are `running`, `waiting-for-you`, `failed`; `done`, `not-started`, `skipped`
and `unknown` fall through unchanged. Note in a comment that the new branch can never actually contend:
the run surface renders with `selectedSlug={null}` and no `onSelectNode`, and the Builder never supplies
`status`, so the two states are mutually exclusive **by surface**.
**Do not touch the ⛨ seal block.** It reads `grounded` and nothing else, it owns top-right, and it may
never become conditional on run state.

**(e) `index.css`.** Add, once, under the existing `@layer utilities`: a `@keyframes ringspin` rotating to
360deg; `.canvas-ring-spin { transform-box: view-box; transform-origin: 36px 36px; }`; and the animation
itself only inside `@media (prefers-reduced-motion: no-preference)`. With motion suppressed, `running`
still renders a static 26%-covered arc and `waiting-for-you` a 74%-covered arc with the chip, so the two
remain unmistakable — reduced motion is safe by construction, not by a fallback.
  </action>
  <acceptance_criteria>
    - `grep -c 'export type NodeRunStatus = string' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0
    - `grep -c 'overflow-hidden' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0
    - `grep -Ec 'tabIndex|onClick|<button' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0 for the run-line and ring blocks (verify by reading those blocks)
    - `grep -Ec 'rotate\(|transform=' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0 — the only transform is the `.canvas-ring-spin` class name
    - `grep -c 'phase.error\|{error}' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0
    - `grep -c 'canvas-node-seal' frontend/src/components/workflows/PhaseNodeCard.tsx` still returns 1 and `git diff` shows the seal block byte-unchanged
    - `grep -c 'ringspin' frontend/src/index.css` returns at least 1, inside a `prefers-reduced-motion: no-preference` media query for the animation
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
    - `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` — the shipped 68 cases still pass (the seal widening is Task 3)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>Seven readings paint seven distinct ring shapes plus a sentence; the seal, the badge slots, `stepNumber` and `technicalLine` are all untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Widen the seal-invariance guard to seven, and fence the ring geometry</name>
  <files>frontend/src/components/workflows/PhaseNodeCard.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx:915-1040` — the `?raw` props fence proving the seal's markup never reads run state, the POSITIVE CONTROL at `:1004`, and the FOUR-value byte-identical render at `:1017-1040`.
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx:590-810` — the mark-occupancy zone check, whose own comment (`:749`) already says Phase 188 adds a `{ name: "run state", rendered: true, box: ... }` row.
    - `.planning/phases/188-non-technical-run-observability/188-UI-SPEC.md` § "✅ The greyscale acceptance clause" — the per-reading unique shape property; assert THAT, never "they look different".
    - `.planning/phases/188-non-technical-run-observability/188-PATTERNS.md` § "Positive controls on every absence assertion".
  </read_first>
  <action>
Extend the shipped suite. Do not create a second file — these guards belong beside the ones they widen.

1. **Seal at SEVEN.** Change the four-value loop at `:1017-1040` to iterate all seven `CanvasReading`
   values. D-188-04 is explicit that the SPEC's "all six readings" means every normal reading and that
   `unknown` must be covered, because `unknown` is the one a fail-open would hide behind. Keep the
   byte-identical class-list-and-text comparison; keep the props fence and its positive control unchanged.
2. **Ring shape uniqueness (the greyscale clause).** For all seven readings, render and capture the arc
   element's `stroke-dasharray` / `stroke-dashoffset` / presence. Assert the seven signatures are
   pairwise distinct, and assert the specific unique property per reading rather than only distinctness:
   `not-started` is the only reading with NO arc element; `done` is the only one with no dash pattern
   (an unbroken circle); `running` is the only one carrying the spin class; `waiting-for-you` is the only
   one that also renders the pause chip; `failed` is the only one whose dasharray describes two arcs;
   `skipped` and `unknown` are both fully dashed but at different dash/gap ratios.
3. **Geometry falsification.** Assert the computed values equal the UI-SPEC's expected decimals to three
   places at `r = 34` (`C = 213.628`): `running` ⇒ dasharray `55.543 158.085`; `waiting-for-you` ⇒
   dasharray `158.085 55.543`, offset `25.635`; `failed` ⇒ dasharray `89.724 17.090 89.724 17.090`,
   offset `18.158`. These decimals appear ONLY in the test — that is what makes them a falsification of
   the formula rather than a copy of it.
4. **No rotation, no overflow-hidden.** A `?raw` fence over `PhaseNodeCard.tsx` asserting zero
   `overflow-hidden`, zero SVG `transform=` attribute and zero `rotate(` — each with a positive control
   proving the needle matches a sample string built in the test.
5. **The card budget.** Assert `technicalLine` renders nothing when not passed (it is DECLINED by this
   phase), that the badge row is unchanged when `status` is supplied (188 spends ZERO badge slots), and
   that the run line renders for `not-started` as well as for terminal readings.
6. **Occupancy.** Add the `{ name: "run state", rendered: true, box: ... }` row the existing zone check's
   own comment predicts, and confirm the expected-set assertion grows accordingly. The ring spans x 94…166
   at the node scale; the seal 222…243; the verdict −8…14 — assert zero overlaps.

Assemble every fence needle from parts so this file's own source cannot satisfy a grep run over it.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with a case count strictly greater than the 68 pinned in Plan 01
    - The seal loop iterates 7 readings — verify the array literal in the test has 7 members and includes `unknown`
    - The suite contains the literal strings `55.543`, `158.085`, `25.635`, `89.724`, `17.090` and `18.158`
    - Every `not.toMatch` in the `?raw` fence block has a positive control in the same `it(`
    - `node scripts/vitest-count-gate.cjs` exits 0, `failed` 0, and `PhaseNodeCard.test.tsx` shows an increase (never a decrease) against its Plan-01 pin
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx</automated>
  </verify>
  <done>Seven readings are proved distinguishable by shape alone, the geometry is falsified against the formula, and the seal is proved invariant across all seven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-authored phase titles / subtitles → the card | Authored strings render inside a component that now also renders a derived run sentence. |
| governance seal ↔ run state | The seal is a security-relevant claim (this step is grounded). Making it conditional on run state would let a run state suppress a governance fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-06-01 | Tampering | the ⛨ governance seal | mitigate | The seal block is byte-unchanged and reads `grounded` and nothing else. The shipped `?raw` props fence stays, and the byte-identical render guard is widened from four run states to seven so `unknown` — the reading a fail-open would hide behind — is covered too. |
| T-188-06-02 | Information disclosure | the run line | mitigate | The run line renders `runReadingLabel(...)`, a closed set of fixed strings. `Phase.error` is explicitly NEVER rendered on the canvas: it is free-form harness text that can carry slugs, model ids and gate identifiers. The detailed reason stays in the developer `PhaseTimeline`, reachable through "Open the chat thread". |
| T-188-06-03 | Tampering / XSS | authored title and subtitle | accept | Both already render as plain React text children through the shipped card; this plan adds no `dangerouslySetInnerHTML` and no `innerHTML`. The house grep guard at `PhaseNode.tsx:65-71` continues to apply (T-124-01). |
| T-188-06-04 | Denial of service (usability) | the ring animation | mitigate | One keyframe, guarded by `prefers-reduced-motion: no-preference`. With motion suppressed the two moving-adjacent readings stay distinguishable by static arc coverage plus the pause chip, so suppression is safe by construction rather than by a degraded fallback. |
| T-188-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; no shadcn registry component is added (`188-UI-SPEC.md § Registry Safety`). |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/` exits 0
- `node scripts/vitest-count-gate.cjs` exits 0, `failed` 0, no per-file decrease
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 33
- `grep -rn "overflow-hidden" frontend/src/components/workflows/PhaseNodeCard.tsx frontend/src/components/workflows/PhaseNode.tsx` returns nothing
- `git diff --name-only HEAD -- supabase/migrations/` is empty
</verification>

<success_criteria>
- Seven readings, seven distinct ring shapes, proved distinguishable with colour off (sketch 153-A's build criterion)
- Zero net-new glyphs and zero badge slots spent (D-188-06); `technicalLine` and `stepNumber` remain unspent
- The run-time waiting words differ from the shipped design-time badge in verb, object and aspect (D-188-05)
- The ⛨ seal renders byte-identically at all seven readings (D-188-04)
- Ring geometry computed from `offset = (D + G/2) − p`, falsified against the UI-SPEC decimals (D-188-07)
</success_criteria>

<output>
Create `.planning/phases/188-non-technical-run-observability/188-06-SUMMARY.md` when done.

⚠ Do NOT call `requirements.mark-complete`, `state.advance-plan` or `roadmap.update-plan-progress` —
those SDK verbs write false completion records in this project.
</output>
