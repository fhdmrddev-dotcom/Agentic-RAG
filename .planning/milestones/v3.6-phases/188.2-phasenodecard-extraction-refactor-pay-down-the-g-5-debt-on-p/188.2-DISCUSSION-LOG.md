# Phase 188.2: PhaseNodeCard Extraction Refactor - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-06
**Phase:** 188.2-phasenodecard-extraction-refactor
**Areas discussed:** What "paid down" means · Which seams to cut · Load-bearing prose rule · BUG-260806-01's home

**Framing note.** Two measurements taken before the first question reshaped the phase and are worth
preserving here because they were *not* what the phase was scoped on:

1. `PhaseNodeCard.tsx` is **797 L but only 249 lines of code** — 62.5 % of it is prose. G-5 fired on
   a line count that is two-thirds comment. (`WorkflowCanvas.tsx` at 188.1 was 1593 L of mostly code.)
2. `PhaseNodeCard.test.tsx:1745` asserts the clipping utility appears in the card's source **exactly
   once** — pinning *prose*, not code — and that one mention sits in the 3D-mark block, which the
   extraction moves. A naive cut takes that fence to 0 and RED.

---

## What "paid down" means

| Option | Description | Selected |
|--------|-------------|----------|
| Cut by code seams | Extract genuinely self-contained sub-components; prose travels WITH its code; re-scope fences first (188.1's Wave-0 shape) | ✓ |
| Re-measure G-5 on code lines | Record that 797 is 62.5 % prose and only 249 lines are code, so the row doesn't fire on the measure that matters; phase becomes a measurement correction + the bug fix | |
| Relocate the prose | Move institutional memory to a companion doc, leave a lean component (~797 → ~300) | |

**User's choice:** Cut by code seams.
**Notes:** The rejected middle option was genuinely defensible on the measurement — it was declined
because it would overturn a call the operator made deliberately at the 188.1-05 checkpoint the day
before. The third fails on mechanics: the fences grep the SOURCE, so relocating prose breaks every
pinned docblock, and co-located prose is prose that actually gets read.

| Option | Description | Selected |
|--------|-------------|----------|
| Body must shrink | The four absolutely-positioned mark blocks (~150 L) are in scope alongside the peripherals, because 189 writes in the body | ✓ |
| Peripherals only | Slot contract + ring constants (231 L, 797 → 566); lowest risk, every fenced JSX block stays put | |
| Body only | Cut just the mark blocks; skip the type shuffling | |

**User's choice:** Body must shrink.
**Notes:** The decisive fact, surfaced by the option's own preview: the two obvious candidates sit
*outside* the body, so cutting both leaves the 427-line body — the part 189 writes into — untouched.
That would satisfy the ledger while missing the ROADMAP goal, which names 189's diff.

| Option | Description | Selected |
|--------|-------------|----------|
| Body-line target + both totals | Pin total lines, CODE lines, and the component body, before/after; a named target the plan must hit | ✓ |
| Property, not a number | "Every block that is not the 137-B card lives in its own module" | |
| Ledger flip only | 188.1's bar: measurably smaller, record before/after, flip the row | |

**User's choice:** Body-line target + both totals.
**Notes:** The third option is the sibling phase's own bar — declined because it is the bar that let
a 62.5 %-prose file read as G-5 firing in the first place.

---

## Which seams to cut

| Option | Description | Selected |
|--------|-------------|----------|
| Four modules by concern | `phaseNodeCardContract.ts` (147) · `NodeRunOverlay.tsx` (104) · `NodeCornerMarks.tsx` (90) · `NodeIconWell.tsx` (34); card lands ~300 L, body ~170 L | ✓ |
| Two modules | One omnibus `NodeMarks.tsx` for every overlay + the contract module | |
| Three modules | Verdict + seal stay inline under their geometry fences; body lands ~260 L | |

**User's choice:** Four modules by concern.
**Notes:** Grouping is the decision; the names are indicative. The rejected omnibus module would
have had five reasons to change and would mix 184/185/188 ownership in one file.

| Option | Description | Selected |
|--------|-------------|----------|
| Shared module, widened | One home for `own<T>()` that both new modules import; `runVocabulary` keeps its own private copy and its fences | ✓ |
| One private copy per module | Honour the recorded decision literally — three near-identical copies in one directory | |
| Keep both lookups in the card | Resolve mark and stroke in the body, pass results down as props (the existing `tint` pattern) | |

**User's choice:** Shared module, widened.
**Notes:** `own()`'s own docblock records a prior decision preferring two private copies — but that
decision weighed two copies against widening `runVocabulary`'s module-private, `CanvasReading`-typed
`own()`, whose `?raw` fences constrain that signature. A small dedicated module is a third option the
original decision never had to consider. The WR-04 fix class is exactly the kind that must not drift
between copies.

| Option | Description | Selected |
|--------|-------------|----------|
| Update the importers | Point `PhaseNode.tsx:92` and `PhaseNodeCard.test.tsx:45` at the contract module; card exports only the component | ✓ |
| Re-export for compatibility | Card re-exports the types so no import line anywhere changes | |

**User's choice:** Update the importers.
**Notes:** Measured blast radius is two lines. A re-export kept purely to avoid changing them would
leave two valid import paths for the same type — free to be the one 189 imports by accident.

---

## Load-bearing prose rule

| Option | Description | Selected |
|--------|-------------|----------|
| 188.1's subtree list, verbatim | `CARD_SUBTREE_PATHS` naming all five files before four exist, with all three non-vacuity controls; fences re-scoped and RED before any code moves | ✓ |
| Directory sweep, no path list | Glob the whole workflows directory — future modules covered by construction | |
| Per-module fences | Each new module carries its own copy of the negative assertions | |

**User's choice:** 188.1's subtree list, verbatim.
**Notes:** Read at source before recommending, not from memory — `WorkflowCanvas.test.tsx:51-80`
records why it names files that do not exist yet (Phase 188's F7 lesson: an invariance fence says
nothing about what it no longer covers), and `:591-615` carries a substitution guard that was
measured RED. The sweep was declined because it would fence 60+ unrelated files; per-module copies
because a module added without them is silently unfenced.

| Option | Description | Selected |
|--------|-------------|----------|
| Count over the subtree | Re-anchor `toBe(1)` to the subtree source so it holds wherever the rule lives | ✓ |
| Fence moves to the new module | Assert against `NodeIconWell?raw` | |
| Don't move the 3D mark | Leave the icon well inline, take the ~34 L hit | |

**User's choice:** Count over the subtree.
**Notes:** A per-file count can be satisfied by shuffling prose between files — the exact failure
mode a refactor invites. Moving the fence to the new module would leave the card, the card div and
the node box unpinned, though the ban covers the whole subtree.

| Option | Description | Selected |
|--------|-------------|----------|
| Pin it with a falsification control | Wave-0 `@ts-expect-error` type test, observed RED against a widened union first | ✓ |
| Out of charter | 188.2 moves code; it does not add coverage the file never had | |
| Pin it, and the focus invariant too | Same control plus non-vacuous no-focusable-control coverage across the new modules | |

**User's choice:** Pin it with a falsification control.
**Notes:** Measured during discussion: **"a third badge is a typecheck error" is asserted nowhere.**
The two-badge block tests 0/1/2 badges rendering; there is no `@ts-expect-error`, no
`BadgeSlot2Tuple` reference, no type-level assertion. Widening `BadgeSlots` during the move would
leave every test green. The third option's extra half turned out to be unnecessary — the `onClick=`
fence gets re-scoped anyway as a consequence of the subtree-source decision above.

---

## BUG-260806-01's home

| Option | Description | Selected |
|--------|-------------|----------|
| Here, own plan, named exception | Two stated contracts: card + new modules prove UNCHANGED; `PlaneEditingLayer` stacking proves FIXED (the 188.1-04 shape) | ✓ |
| Split to `/gsd:fast` now | G-3 sizes it as ≤1 file / ≤10 lines / no schema or API surface | |
| Defer to 189 | 189 touches the canvas anyway | |

**User's choice:** Here, own plan, named exception.
**Notes:** G-3 genuinely applies on size, and was surfaced rather than hidden. It lost on evidence
economics: 188.2 will already be driving the canvas with `elementFromPoint` to prove the card
unchanged, so the reachability evidence is nearly free at that moment, whereas a `/gsd:fast` split
needs its own driven verification regardless — no unit test can see occlusion. Deferring was declined
because it re-opens routing settled the previous day and puts a major-severity user-facing defect
behind a feature phase.

| Option | Description | Selected |
|--------|-------------|----------|
| Driven row + a stacking assertion | One `elementFromPoint` row with the card SELECTED, plus a unit assertion that the affordance z-index exceeds xyflow's 1000 | ✓ |
| Driven row only | Record it as UAT evidence, 188.1's board shape | |
| Leave the guard to research | Capture that it must be guarded; let research choose the mechanism | |

**User's choice:** Driven row + a stacking assertion.
**Notes:** jsdom has no layout, so occlusion is invisible to unit tests (`.click()` bypasses
hit-testing). A driven row alone is a one-time photograph — which is how this class survived from
184-12 until it was found while driving 188.1's board.

---

## Claude's Discretion

- Module file names (the grouping in D-04 is locked; names are indicative).
- Whether `own<T>()`'s shared home is a new small module or an existing utility file.
- Wave/plan decomposition, subject to D-07's ordering constraint (fences re-scoped and observed RED
  before any code moves) and 188.1's *create additively, then cut* split.

## Deferred Ideas

Three gray areas surfaced at the close and consciously not discussed — recorded in CONTEXT.md
`<deferred>` as planner discretion rather than dropped:

1. UAT shape for a phase with two contracts (prove-unchanged half + prove-fixed half), and keeping
   one half's evidence from standing in for the other's.
2. Whether the 110-line file docblock is rewritten after four blocks leave it, or left as-is.
3. Count-gate `TARGETS` / `BASELINE` registration for four new test files.

No scope creep arose — every thread stayed inside the extraction, its fences, or the folded bug.
