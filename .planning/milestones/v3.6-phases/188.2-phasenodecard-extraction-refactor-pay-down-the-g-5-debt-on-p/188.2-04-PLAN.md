---
phase: 188.2
plan: 04
type: execute
wave: 3
depends_on: ["188.2-01", "188.2-03"]
files_modified:
  - frontend/src/components/workflows/phaseNodeCardContract.ts
  - frontend/src/components/workflows/ownProperty.ts
autonomous: true
requirements: []
# EMPTY BY DESIGN — refactor phase, no REQ- ids (ROADMAP §188.2).
success_criteria: ["SC#4"]
gap_closure: false

must_haves:
  truths:
    - "D-04: the slot contract lands in its own camelCase leaf — `BadgeSlot`, `BadgeSlot2Tuple`, `BadgeSlots`, `NodeVerdictMark`, `NodeRunStatus` and `PhaseNodeCardProps` move verbatim out of `PhaseNodeCard.tsx:139-285`, so 189 adds a slot to a 147-line contract rather than to a 797-line component"
    - "D-05: `own<T>()` gets ONE shared home the new modules import, because its two sinks split across two destinations — a third option the helper's own docblock never had to consider, and it beats three private copies. `runVocabulary.ts` keeps its own private copy with its different `CanvasReading` signature and its fences stay untouched"
    - "D-01: the prose travels WITH its code — every docblock, every inline comment and every `⚠` paragraph moves with the declaration it describes, unedited, including anything that is wrong; a correction folded into the move commit destroys the property a reviewer relies on"
    - "This plan is ADDITIVE ONLY: two files are created and nothing is deleted. `PhaseNodeCard.tsx` still declares its own copies, both trees typecheck, and the card renders exactly as it did — 188.1's proven split of an extraction into create-additively then cut"
    - "The house leaf rules hold by construction: both files are camelCase `.ts` (11 of 11 measured, and enforced by a TEST at `WorkflowCanvas.test.tsx:661-665`, not by convention), `ownProperty.ts` has ZERO imports, and neither names a `PhaseNodeCard` specifier in any import form"
  artifacts:
    - path: "frontend/src/components/workflows/phaseNodeCardContract.ts"
      provides: "the six slot-contract types, moved verbatim with their docblocks"
      contains: "export interface PhaseNodeCardProps"
      min_lines: 150
    - path: "frontend/src/components/workflows/ownProperty.ts"
      provides: "the own-property table read, as a zero-import leaf"
      contains: "export function own<T>("
      min_lines: 25
  key_links:
    - from: "frontend/src/components/workflows/phaseNodeCardContract.ts"
      to: "frontend/src/components/workflows/PhaseNodeCard.test.tsx's CARD_SUBTREE_PATHS"
      via: "the Plan 01 subtree fence, whose path list already named this file before it existed"
      pattern: "phaseNodeCardContract"
---

<objective>
Create the two camelCase leaves — the slot contract and the own-property helper — by moving
their source verbatim out of `PhaseNodeCard.tsx`. Delete nothing.

Purpose: 188.1's measured result was that an extraction split into *create additively*, then
*cut*, reads as a MOVE under `git diff --numstat` rather than as a rewrite a reviewer has to
re-derive. This plan is the first half. Both files are pure leaves: `phaseNodeCardContract.ts`
emits no runtime code at all (six types), and `ownProperty.ts` has zero imports, which makes it
un-cyclable by construction.

These two are cut first because everything else depends on them. `NodeCornerMarks` and
`NodeRunOverlay` (Plan 05) each import `own`, and every module in the subtree types against
`PhaseNodeCardProps`. Interface-first ordering means Plan 05's executor receives the contracts
rather than going looking for them.

D-05 is worth restating because it overturns a recorded decision. `own<T>()`'s own docblock says
*"one concept, two small private copies, is cheaper here than one shared function with a blast
radius"* — but that decision weighed TWO copies against widening `runVocabulary`'s
module-private, `CanvasReading`-typed `own()`, whose `?raw` fences constrain that signature. A
small dedicated leaf is a third option it never had to consider, and it beats three copies. That
paragraph is therefore the ONE piece of moved prose this plan rewrites, and the rewrite is part
of the move rather than a tidy-up.

Output: two new files, both typechecking, with `PhaseNodeCard.tsx` untouched.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-CONTEXT.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-RESEARCH.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-PATTERNS.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-VALIDATION.md

<interfaces>
<!-- RE-MEASURED at HEAD `42b42cb7` by the planner, 2026-08-06, by printing each boundary line.
     Re-derive before cutting — a line number is the one thing in this repo that rots fastest. -->

THE TWO L1 SOURCE RANGES (exact, and the region map they come from):

    phaseNodeCardContract.ts   ←  PhaseNodeCard.tsx:139-285      147 L (114 comment / 28 code / 5 blank)
      :139        the section separator `// ── The slot contract ──…`
      :141-163    BadgeSlot docblock + interface
      :165-175    BadgeSlot2Tuple + BadgeSlots (the max-2 tuple union)
      :177-196    NodeVerdictMark (alias of nodePresentation.VerdictMarkKind)
      :198-213    NodeRunStatus (alias of lib/phaseState.CanvasReading)
      :215-285    PhaseNodeCardProps — 14 slots, each with its own docblock
                  (`:215` is `export interface PhaseNodeCardProps {`; `:285` is its `}`)

    ownProperty.ts             ←  PhaseNodeCard.tsx:289-310      22 L
      :289-305    the helper's docblock, including the WR-04 / 188.1-04 paragraph
      :306-310    `function own<T>(table: Record<string, T>, key: string): T | undefined`

    ⚠ `:287` IS NOT PART OF EITHER. It is the separator `// ── The status ring's constants
    (188-06 · sketch 153-A) ──` and it names the RING, so it travels to `NodeRunOverlay.tsx` in
    Plan 05. RESEARCH's cut table folds it into the `own()` range (`287-311`); that is the one
    place its table is imprecise, and this plan corrects it rather than inheriting it.

THE IMPORTS `phaseNodeCardContract.ts` NEEDS — all type-only, because `verbatimModuleSyntax:
true` is on in `tsconfig.app.json`:

    import type { ReactNode } from "react"
    import type { ChipTone } from "@/components/org/StatusChip"
    import type { VerdictMarkKind } from "@/components/workflows/nodePresentation"
    import type { CanvasReading } from "@/lib/phaseState"
    import type { EmitFailure } from "@/types"

    ⚠ The card's line `:113` is `import { StatusChip, type ChipTone }` — a VALUE import, because
    the card renders `<StatusChip>`. The contract leaf needs only the type. Splitting them is not
    tidying: it is what the leaf rule requires, and the card keeps its value import in Plan 06.

`ownProperty.ts` NEEDS **ZERO IMPORTS**. State that as its contract in its docblock — it is what
makes the module un-cyclable by construction, and it is why it satisfies the leaf fence
(`WorkflowCanvas.test.tsx:661-678`, the `COMPONENT_SIBLING` / `REACT_IMPORT` pair) without
needing the third `toMatch` non-vacuity line `editAffordance` needs. It has no allowed import to
point at — say so rather than inventing one.

THE HOUSE LEAF RULE, enforced as a TEST rather than as a convention
(`WorkflowCanvas.test.tsx:661-665`): *a PascalCase sibling is a component module, a camelCase one
is a plain module.* Re-measured this session: **11 of 11** `.ts` modules in this directory are
camelCase — `deriveTier`, `soulData`, `canvasNudge`, `builderStore`, `canvasModel`,
`phaseVocabulary`, `definitionOps`, `verdictModel`, `runVocabulary`, `editAffordance`,
`nodePresentation`. No exception exists to point at.

THE L2 CONTAINER ANALOGS — copy their shape, not their content:

    deriveTier.ts:24-50      the house ordering for a camelCase leaf that leads with types:
                             exported type aliases first, then the exported interface, one
                             docblock per member.
    deriveTier.ts:118-129    the `void _never` idiom for a deliberately-unused local.
    editAffordance.ts:1-6    the leaf header shape 188.1 shipped, which states what the module
                             IS and what it deliberately does NOT do ("renders nothing, reads no
                             DOM, opens no request and holds no store reference").
    PlaneEditingLayer.tsx:9-22  the STATE OF THE EXTRACTION paragraph, and the rule that follows
                             it: the moved `:NNN` references point at the PRE-MOVE file and are
                             KEPT STALE ON PURPOSE, with a `⚠` saying so.
    editAffordance.ts:15-18  the rule that a misplaced docblock MOVED MISPLACED, "because a
                             first diff that reads as a pure MOVE is the property a reviewer of
                             this phase relies on, and a correction folded into the same commit
                             destroys it."

    ⇒ DO NOT TIDY ON THE WAY OUT. Anything wrong stays wrong in the move commit. The single
    exception is named in Task 2 and is a decision (D-05), not a tidy.

THE ONE PARAGRAPH THAT BECOMES FALSE AND MUST BE REWRITTEN (D-05) — `PhaseNodeCard.tsx:300-305`,
the "WRITTEN LOCALLY RATHER THAN IMPORTED" paragraph. Replace it with the recorded third option:
two private copies became three sinks across two destinations, so a dedicated leaf beats three
copies, while `runVocabulary.ts:84-88`'s copy — a DIFFERENT signature (`reading: CanvasReading`,
not `key: string`), module-private, called at four sites — stays exactly where it is and its
fences stay untouched.

NO EXACT ANALOG EXISTS for a types-ONLY module in this directory — measured: all 11 camelCase
leaves export at least one runtime value. A 147-line module exporting only types is new in shape
though not in kind. Say so in its docblock so a reviewer does not go looking for a precedent that
is not there.

WHAT THIS PLAN MUST NOT DO:
  · not delete one line from `PhaseNodeCard.tsx` — that is Plan 06;
  · not update `PhaseNode.tsx:88-92` or `PhaseNodeCard.test.tsx:45` — that is Plan 06 (D-06);
  · not create a re-export shim anywhere;
  · not edit `scripts/vitest-count-gate.cjs`.

MEASURED GATES (re-run; do not inherit): `tsc --noEmit -p tsconfig.app.json` → 33.
`eslint src/components/workflows/` → 5 errors, 0 warnings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phaseNodeCardContract.ts by moving PhaseNodeCard.tsx:139-285 verbatim</name>

  <files>
frontend/src/components/workflows/phaseNodeCardContract.ts
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.tsx:111-285` — the import block and the whole
  slot contract. Read every docblock in the range: they are the payload, not decoration, and
  several are under live `?raw` fences.
- `frontend/src/components/workflows/deriveTier.ts:1-60` — the house ordering and docblock
  density for a camelCase leaf that leads with types.
- `frontend/src/components/workflows/editAffordance.ts:1-31` — 188.1's leaf header, including the
  "STATE OF THE EXTRACTION" paragraph and the `⚠` about stale `:NNN` references.
- `frontend/src/components/workflows/nodePresentation.ts:140-150` — the `VerdictMarkKind`
  docblock whose sentence *"`PhaseNodeCard.NodeVerdictMark` is an alias of this type"* goes false
  on this move. Do NOT edit it here; Plan 06 owns it, and this task must record it in the SUMMARY
  as owed so it cannot be forgotten between waves.
- `frontend/tsconfig.app.json` — confirm `verbatimModuleSyntax: true`, which is why every import
  in this new file is `import type`.
  </read_first>

  <action>
Create `frontend/src/components/workflows/phaseNodeCardContract.ts` holding
`PhaseNodeCard.tsx:139-285` moved VERBATIM — every docblock, every inline comment, every `⚠`
paragraph, in the shipped order, nothing re-typed, nothing tidied, nothing re-ordered. Keep the
`:139` section separator as the file's own in-body separator or fold it into the header, but do
not delete the words.

Add the five type-only imports listed in `<interfaces>`. Add nothing else — this module emits no
runtime code and must export exactly six type names and no value.

Write the file header in the 188.1 leaf shape, carrying four things:
  · what the module IS: the phase card's slot contract, the surface 185 / 188 / 189 add data to;
  · the STATE OF THE EXTRACTION paragraph — these six types were CUT out of `PhaseNodeCard.tsx`
    (declared there at `:139-285`, in a file measured at **797 L** at HEAD `42b42cb7` before the
    188.2-06 cut), it is a HARD CUT with NO re-export shim, and the card will import them back
    from here;
  · the `⚠` that every `:NNN` reference inside the moved docblocks points at the PRE-MOVE
    `PhaseNodeCard.tsx` and is kept stale on purpose, exactly as 188.1 kept its own;
  · the fences paragraph in the `PlaneEditingLayer.tsx:38-51` shape: `PhaseNodeCard.test.tsx`
    reads this file's source through the 188.2-01 subtree fence — an `import.meta.glob` `?raw`
    sweep whose path list already named `./phaseNodeCardContract.ts` before this file existed —
    so the 17 negative scope fences that guarded these types inside the card now read them here
    rather than quietly covering 147 fewer lines; and a second fence forbids this module from
    naming a `PhaseNodeCard` specifier in ANY import form.

Add one further sentence the 188.1 headers did not need: **this is the directory's first
types-only module.** All 11 camelCase leaves export at least one runtime value, so a reviewer
looking for a precedent for this shape will not find one, and that is a measurement rather than
an oversight.

Do NOT delete anything from `PhaseNodeCard.tsx`. Both declarations coexist after this task and
both typecheck — that is the additive half of the split, and it is deliberate.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json 2&gt;&amp;1 | grep -c "error TS"</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` returns exactly **33**, and `... | grep -c "phaseNodeCardContract"` returns **0**.
- `wc -l frontend/src/components/workflows/phaseNodeCardContract.ts` is **≥ 150** (147 moved plus the header and imports). Record the exact figure — Plan 06's before/after table reads it.
- **THE MOVE IS VERBATIM, PROVEN MECHANICALLY**: `git show HEAD:frontend/src/components/workflows/PhaseNodeCard.tsx | sed -n '139,285p' > /tmp-scratch/contract-src.txt` and `diff` that against the corresponding span of the new file reports **only** the changes this plan authorised (the header, the imports, and the separator's placement). Paste the `diff` output into the SUMMARY. Use the session scratchpad for the temp file — never write scratch files into the watched tree.
- `grep -c "^export type\|^export interface" frontend/src/components/workflows/phaseNodeCardContract.ts` returns **6**, and `grep -c "^export const\|^export function\|^export let" ...` returns **0**. A runtime export from this leaf would be a different module than the one D-04 specifies.
- `grep -c "import type" frontend/src/components/workflows/phaseNodeCardContract.ts` returns **5** and `grep -Ec "^import \{" ...` returns **0** — every import is type-only, as `verbatimModuleSyntax` requires.
- `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors, 0 warnings** — unmoved.
- `git diff --stat -- frontend/src/components/workflows/PhaseNodeCard.tsx frontend/src/components/workflows/PhaseNode.tsx` reports **0 files changed**. This plan is additive.
- The `nodePresentation.ts:145-146` staleness is recorded in the SUMMARY as OWED TO PLAN 06, with the sentence quoted.
  </acceptance_criteria>

  <done>`phaseNodeCardContract.ts` exists holding the six slot-contract types moved verbatim with their prose, exports no runtime value, typechecks at an unmoved 33, and its header records the extraction state, the deliberately-stale line references, its fences, and the fact that it is the directory's first types-only module.</done>
</task>

<task type="auto">
  <name>Task 2: Create ownProperty.ts as a zero-import leaf, and rewrite the one paragraph D-05 makes false</name>

  <files>
frontend/src/components/workflows/ownProperty.ts
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.tsx:287-311` — the separator, the helper's
  full docblock and the function. Note that `:287` names the RING and does NOT travel here.
- `frontend/src/components/workflows/runVocabulary.ts:80-100` — the sibling `own()` that STAYS.
  Read its signature: `own<T>(table: Record<string, T>, reading: CanvasReading)` — a DIFFERENT
  second parameter type, module-private, called at four sites. Do not touch it, do not unify it,
  and do not import it.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:2000-2063` — the two shipped WR-04
  falsifications (sites 4 and 5), observed RED at 188.1-04. They drive the card, so they will
  keep reaching this helper through the card after Plan 06's cut; this task must not change the
  function's behaviour by one character.
- `frontend/src/components/workflows/editAffordance.ts:1-50` — the leaf header shape, and the
  recorded rule that a misplaced docblock moved MISPLACED.
- `frontend/src/components/workflows/deriveTier.ts:118-129` — the `void` idiom, for reference.
  </read_first>

  <action>
Create `frontend/src/components/workflows/ownProperty.ts` holding `PhaseNodeCard.tsx:289-310`
moved verbatim, with the function EXPORTED rather than module-private. Its body — the
`Object.prototype.hasOwnProperty.call(table, key)` guard returning the indexed value or
`undefined` — must not change by one character: it is the WR-04 mitigation and two shipped
falsifications reach it.

The module takes **ZERO imports**. State that as its explicit contract in the header, together
with what follows from it: it is un-cyclable by construction, and it satisfies the leaf fence
without needing the "allowed import" non-vacuity line `editAffordance.ts` carries, because it has
no allowed import to point at.

Move the helper's docblock verbatim EXCEPT for one paragraph, and make that exception visible.
The "WRITTEN LOCALLY RATHER THAN IMPORTED" paragraph (`PhaseNodeCard.tsx:300-305`) becomes false
the moment this file exists, so it is rewritten HERE, in the `⚠` register, recording the actual
decision rather than deleting the old one: that paragraph weighed TWO private copies against
widening `runVocabulary`'s module-private `CanvasReading`-typed `own()`, whose `?raw` fences
constrain that signature; D-05 introduces a third option it never had to consider — a dedicated
leaf — because the card's two sinks now split across two destination modules, which would have
made three copies. Name the constraint that still holds: `runVocabulary.ts:84-88` keeps its own
private copy with its different signature, and its fences stay untouched. This single rewrite is
a DECISION, not a tidy; everything else in the docblock moves unedited, including anything that
is wrong.

Add the extraction-state and fences paragraphs in the same shape as Task 1: cut out of
`PhaseNodeCard.tsx` (declared at `:289-310`, pre-move file measured 797 L at HEAD `42b42cb7`),
hard cut, no shim, `:NNN` references deliberately stale, read by `PhaseNodeCard.test.tsx`'s
188.2-01 subtree fence, and forbidden from naming a `PhaseNodeCard` specifier in any import form.

Do NOT delete anything from `PhaseNodeCard.tsx`. Do NOT touch `runVocabulary.ts`. Do NOT attempt
to consolidate the other four inline `hasOwnProperty` guards in the tree (`lib/phaseState.ts:77`,
`lib/phaseGlyph.tsx:92`, `PhaseNode.tsx:165`, `nodePresentation.ts:128`) — that is a
behaviour-shaped change owed its own phase, and `editAffordance.ts:42-44` states exactly that
rule for a different duplication.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json 2&gt;&amp;1 | grep -c "error TS"</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` returns exactly **33**.
- `grep -Ec "^import" frontend/src/components/workflows/ownProperty.ts` returns **0**. The zero-import contract is the module's whole safety argument; one import falsifies it.
- `grep -c "Object.prototype.hasOwnProperty.call" frontend/src/components/workflows/ownProperty.ts` returns **1**, and the function body is byte-identical to `PhaseNodeCard.tsx:306-310`. Prove it with a `diff` of the two spans and paste the output (empty) into the SUMMARY. This is the tree's ONLY shipped own-guard spelling — `Object.hasOwn` has zero occurrences in `frontend/src` and this phase does not introduce a second spelling.
- `grep -c "own" frontend/src/components/workflows/runVocabulary.ts` is unchanged from its pre-plan value, and `git diff --stat -- frontend/src/components/workflows/runVocabulary.ts` reports **0 files changed**.
- The rewritten D-05 paragraph is present and names all three of: the two-copies decision it supersedes, the third option and why it exists (two sinks, two destinations, three copies avoided), and the fact that `runVocabulary`'s copy and its different signature stay put. Quote it in the SUMMARY.
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with **0 failed** and an UNCHANGED test count — this plan adds no test and changes no behaviour.
- `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors, 0 warnings**.
- `git diff --name-only` shows only NEW files (`git status --porcelain` reports both new files as `??`/`A` and no `M` on any existing source).
  </acceptance_criteria>

  <done>`ownProperty.ts` exists as a zero-import leaf whose function body is byte-identical to the card's, its docblock moved verbatim except for the one paragraph D-05 makes false, `runVocabulary.ts` untouched, and both gates unmoved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a moved security mitigation → its falsifications | `own<T>()` IS the WR-04 prototype-pollution mitigation. Moving it can silently sever the two shipped falsifications from the code they falsify, and both would go green. |
| a leaf module → the module graph | A leaf that acquires an import can acquire a cycle. `ownProperty.ts`'s zero-import contract is what makes that impossible rather than merely unlikely. |
| verbatim move → helpful correction | A correction folded into a move commit destroys the reviewer's ability to read the diff as a move, which is the only cheap check that a behaviour-preserving change preserved behaviour. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188.2-14 | Tampering | prototype-pollution lookup on a plain object table (WR-04) — `own<T>()` | mitigate | The function body moves byte-identically, proven by a `diff` of the two spans; `Object.prototype.hasOwnProperty.call` remains the tree's only own-guard spelling; both shipped falsifications (`PhaseNodeCard.test.tsx:2007-2050`, sites 4 and 5, observed RED at 188.1-04) drive through the CARD and are re-verified as still reaching the moved code in Plan 06. ASVS V5 |
| T-188.2-15 | Denial of Service | an ESM cycle reaching `ownProperty.ts` or `phaseNodeCardContract.ts` | mitigate | `ownProperty.ts` declares and asserts ZERO imports; `phaseNodeCardContract.ts` imports only types from modules that do not import it back; both are covered by the Plan 01 cycle fence, whose destination loop begins resolving the moment these files exist |
| T-188.2-16 | Repudiation | the move reading as a rewrite | mitigate | A span-level `diff` against `git show HEAD:` output is an acceptance criterion for both files, and the only authorised prose edit (the D-05 paragraph) is named in the plan text and quoted in the SUMMARY |
| T-188.2-17 | Information Disclosure | `PhaseNodeCardProps.emitFailure`'s docblock (`:249-253`) — the harness's free-form failure text can carry slugs, model ids and gate identifiers | accept | Preserved by the verbatim move: only the typed enum reaches the card, and the constraint travels with the type. No new surface. ASVS V7 |
| T-188.2-SC | Tampering | npm / pip / cargo installs | mitigate | Zero packages installed. Hard gate: `git diff --stat -- frontend/package.json frontend/package-lock.json` reports 0 files changed |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → exactly 33
- `cd frontend && npx eslint src/components/workflows/` → exactly 5 errors, 0 warnings
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` → 0 failed, count unchanged
- `node scripts/vitest-count-gate.cjs` → `count gate OK`, failed 0, no `[count-decrease]`
- `git status --porcelain` → two NEW files only; no modification to any existing source
- `git diff --stat -- frontend/package.json frontend/package-lock.json backend/ supabase/migrations/ scripts/` → 0 files changed
</verification>

<success_criteria>
1. `phaseNodeCardContract.ts` holds `PhaseNodeCard.tsx:139-285` verbatim, exports exactly six types and zero runtime values, and imports only types (D-04).
2. `ownProperty.ts` holds `:289-310` verbatim with a byte-identical function body, exports `own<T>()`, and has zero imports (D-05).
3. The one paragraph D-05 makes false is rewritten in place, recording the decision it supersedes; everything else moved unedited (D-01).
4. `runVocabulary.ts` and its differently-signed private `own()` are untouched.
5. Nothing was deleted from `PhaseNodeCard.tsx` — the additive half of 188.1's proven split.
6. Type gate 33, lint gate 5, suite count unchanged, count gate green.
</success_criteria>

<output>
Create `.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-04-SUMMARY.md` when done. Record verbatim: the two span `diff` outputs proving the moves; the measured `wc -l` of both new files; the rewritten D-05 paragraph; the `tsc`, `eslint` and vitest numbers; and the OWED note that `nodePresentation.ts:145-146` still claims `PhaseNodeCard.NodeVerdictMark` is an alias of its type and must be corrected in Plan 06.
</output>
