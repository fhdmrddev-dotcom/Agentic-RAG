---
phase: 188.2
plan: 06
type: execute
wave: 5
depends_on: ["188.2-05"]
files_modified:
  - frontend/src/components/workflows/PhaseNodeCard.tsx
  - frontend/src/components/workflows/PhaseNode.tsx
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
  - frontend/src/components/workflows/nodePresentation.ts
  - frontend/src/components/workflows/NodeCornerMarks.tsx
  - frontend/src/components/workflows/NodeRunOverlay.tsx
  - frontend/src/components/workflows/NodeIconWell.tsx
autonomous: true
requirements: []
# EMPTY BY DESIGN — refactor phase, no REQ- ids (ROADMAP §188.2).
success_criteria: ["SC#1", "SC#2", "SC#4"]
gap_closure: false

must_haves:
  truths:
    - "D-02: the 427-line component BODY shrinks — peripheral cuts alone were never enough, because 189 writes in the body (badge slot 1, the capability-derived subtitle) and cutting only the contract and the ring constants would have satisfied the ledger while missing the goal"
    - "D-03: THREE measured numbers are pinned before and after with the SAME NAMED CLASSIFIER — total lines, CODE lines, and the body measured as the exact range from the `// ── The card ──` separator to EOF. A named target the plan must hit, not 188.1's 'measurably smaller'. The classifier is named in the plan because a 62.5-%-prose file compared under two different classifiers is not a comparison"
    - "D-02's arithmetic gap is CLOSED BY DECISION, not rounded away: the extraction alone lands the body at ~182, missing ~170 by 12. Door (b) is taken — terse call sites PLUS relocating the `:379-392` preamble prose into the destination docblocks, which D-01's 'the prose travels WITH its code' already licenses"
    - "D-06: the two importers update and the card does NOT re-export the contract types — a re-export that exists so nobody has to change two lines is a second name for the same type, free to be the one 189 imports by accident. Enforced mechanically by the cycle fence's one-direction half, not by prose"
    - "D-01: the file docblock is REWRITTEN rather than left narrating blocks that now live elsewhere — a docblock that names a slot the component no longer holds is the same defect as a false one, in the docblock's own words at `:33-35`. The five paragraphs under live fences move NOT ONE CHARACTER"
    - "The rendered DOM is byte-identical: `CARD_HTML_BASELINE` and the reading/verdict matrices, captured in Wave 2 on the UNMOVED tree, still hold. The moved SOURCE is not byte-identical and this plan does not claim it is"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      provides: "the card after the hard cut — the 137-B card div, the border ternary, the run line, and three JSX call sites"
      contains: "import { NodeRunOverlay }"
    - path: "frontend/src/components/workflows/PhaseNode.tsx"
      provides: "the adapter importing the contract types from their new home"
      contains: "phaseNodeCardContract"
    - path: "frontend/src/components/workflows/nodePresentation.ts"
      provides: "the VerdictMarkKind docblock corrected in the same commit that makes it false"
      contains: "VerdictMarkKind"
  key_links:
    - from: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      to: "the three new component modules"
      via: "module-scope named imports rendered at byte-identical JSX sites"
      pattern: "from \"@/components/workflows/Node"
    - from: "frontend/src/components/workflows/PhaseNode.tsx"
      to: "frontend/src/components/workflows/phaseNodeCardContract.ts"
      via: "the re-pointed type import — one of exactly two in the tree (D-06)"
      pattern: "phaseNodeCardContract"
---

<objective>
Cut. Delete the seven moved ranges from `PhaseNodeCard.tsx`, wire the three call sites, update
the two importers, settle the prose, and measure the three numbers this phase is judged on.

Purpose: this is the phase. The ledger row fires on `PhaseNodeCard.tsx` and Phase 189 must add
its 7th `phase_type` — a badge in slot 1 and a capability-derived subtitle — to a file that has
been paid down. **The test of whether a cut was worth making is whether 189's diff gets smaller,
not whether the file did**, which is exactly why D-02 put the body in scope rather than settling
for the cheap peripheral cuts.

Two things this plan decides rather than discovers, because discovering them at execution is how
a target gets quietly rounded:

**The D-02 arithmetic gap.** The body is exactly lines 371–797 = 427 L. The seven blocks that
leave remove 254 L, landing it at 173 before anything is added back; three JSX call sites cost
9–15 L terse, or +24 L in this file's house comment style. So the extraction ALONE lands at
~182 and misses ~170 by 12. **Door (b) is taken:** terse call sites, with the explanation living
in each destination module's own docblock — which is where D-01 puts it anyway — PLUS relocating
the `:379-392` preamble comment, most of which now narrates blocks that live elsewhere. That
lands the body at ~162–167. If the measured result misses the target anyway, it is recorded as a
measured near-miss with the number, never rounded — 188.1's whole lesson is that an unmeasured
claim is worse than a missed target.

**The classifier.** `code = 249` is the only figure that is method-independent; CONTEXT's
comment/blank split (499/50) and RESEARCH's (518/30) disagree because they classify blank lines
INSIDE block comments differently, and CONTEXT's three numbers sum to 798 against a 797-line
file. This plan names its classifier so before and after are the same measurement.

Output: a card at ~350 lines with a ~165-line body, two updated importers, a rewritten docblock,
and a before/after table produced by one named script run twice.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-CONTEXT.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-RESEARCH.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-PATTERNS.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-VALIDATION.md

<interfaces>
<!-- RE-MEASURED at HEAD `42b42cb7` by the planner, 2026-08-06. Plans 04 and 05 were additive, so
     `PhaseNodeCard.tsx` is still 797 L on entry — VERIFY that with `wc -l` before cutting rather
     than assuming it, and if it has moved, re-derive every range below from the separators. -->

THE SEVEN RANGES TO DELETE, and where each already lives:

    :139-285   the slot contract          → phaseNodeCardContract.ts   (Plan 04)
    :289-310   own<T>()                   → ownProperty.ts             (Plan 04)
    :287       the ring separator         → NodeRunOverlay.tsx         (Plan 05)
    :312-355   the five RING_* constants  → NodeRunOverlay.tsx
    :410-434   the verdict derivation     → NodeCornerMarks.tsx
    :440-443   the arc derivation         → NodeRunOverlay.tsx
    :567-599   verdict-mark JSX           → NodeCornerMarks.tsx
    :601-656   ⛨ governance-seal JSX      → NodeCornerMarks.tsx
    :658-739   status-ring JSX            → NodeRunOverlay.tsx
    :741-762   pause-chip JSX             → NodeRunOverlay.tsx
    :764-794   3D icon-well JSX           → NodeIconWell.tsx

WHAT STAYS — do not touch:
    :357-369   RUN_MODE_NODE_MIN_HEIGHT   feeds the card's own minHeight (:444, :453, :498)
    :436-439   `reading` and `runBorder`  runBorder drives the border ternary at :490-496
    :444       `minHeight`
    :446-565   the node box, `{anchors}` and the 137-B card div

THE THREE CALL SITES, terse per door (a): `<NodeCornerMarks verdict={verdict}
grounded={grounded} />` replacing the two mark blocks as ONE element, `<NodeRunOverlay
reading={reading} />` replacing the ring and chip as ONE element, `<NodeIconWell icon={icon}
tint={tint} />` replacing the well. Each rendered at the byte-identical position in the JSX so
DOM ORDER is unchanged — the marks are absolutely positioned and their paint order is their
document order.

    ⚠ VERIFY DOM ORDER, DO NOT ASSUME IT. `CARD_HTML_BASELINE` compares whole `innerHTML`, so a
    call site placed one sibling out of position fails it. That failure is the instrument working;
    fix the placement, never the baseline.

D-06 — THE ENTIRE NON-TEST BLAST RADIUS IS TWO LINES, both measured this session:
    PhaseNode.tsx:88-92        import { PhaseNodeCard, type BadgeSlot, type BadgeSlots }
                                 from "@/components/workflows/PhaseNodeCard"
    PhaseNodeCard.test.tsx:45  import { PhaseNodeCard, type BadgeSlot, type BadgeSlots }
                                 from "./PhaseNodeCard"
    Each splits into two statements: the component from `./PhaseNodeCard`, the types from
    `./phaseNodeCardContract`. THE CARD MUST NOT RE-EXPORT THE TYPES.

    THE IMPORT-STATEMENT CONVENTION — `FlowEdge.test.tsx:80-85` is 188.1 applying it to exactly
    this situation: the new import lands as a SEPARATE statement with its own comment, rather
    than by widening the existing line, so the diff reads as added lines and "no shipped
    assertion was touched" is auditable by `git diff` alone.

THE DOOR-(b) PREAMBLE. `PhaseNodeCard.tsx:379-392` is a 14-line comment inside the function.
Read it: `:379-385` narrates which slots are filled by which phase (the seal, the ring, the mode),
most of which now describes blocks in other files; `:387-392` records that `technicalLine` is
DECLINED, which is about a slot the card STILL owns and therefore STAYS. Move the sentences about
relocated blocks into the destination docblocks; keep the `technicalLine` and `stepNumber`
sentences here. Target: ~15-20 L removed from the body.

THE FILE DOCBLOCK (`:1-110`) — deferred gray area (b), DECIDED: **rewrite it.** These paragraphs
become false and each moves to the destination whose code it describes:
    :27-32   185-09's corner seal            → NodeCornerMarks
    :34-41   188-06's ring / chip / mode     → NodeRunOverlay (the run line and the border
                                                branch STAY, so this one is HALF-true, which is
                                                the worst state for a docblock — split it)
    :43-52   184-08's verdict corner mark    → NodeCornerMarks
    :54-57   the BadgeSlots max-2 tuple      → phaseNodeCardContract
    :72-87   the 137-B geometry paragraph    → the 3D-mark half to NodeIconWell; the padding and
                                                min-height half STAYS (mixed — split it)
    :89-95   the arc and the spin            → NodeRunOverlay

    ⚠ FIVE PARAGRAPHS MUST STAY AND MUST NOT BE REWORDED — each is under a live fence, and a
    reworded sentence can turn a green fence red or, worse, vacuous:
    :5-9     purely presentational / renders with no provider   fenced :135-147, :390-435
    :12-16   the `?raw` source-guard note                        fenced :390-435
    :59-70   ONE TAB STOP PER NODE          fenced :200-216 AND WorkflowCanvas.test.tsx:231-238
    :97-101  TOTALITY                                            fenced :334-351
    :103-109 XSS                                                 fenced :426 (fence N10)

    Leave a one-line pointer per destination. Expected result: 110 L → roughly 55-70 L.

THE SAME-COMMIT DOC TRUTH owed here, measured:
    nodePresentation.ts:145-146 says "`PhaseNodeCard.NodeVerdictMark` is an alias of this type,
    kept so every existing caller's name still resolves." TRUE today, FALSE after this cut.
    Correct it in THIS commit — the type now lives in `phaseNodeCardContract.ts`.

THE CLASSIFIER — NAMED, so before and after are the same measurement (D-03). Write it to the
session scratchpad (NEVER into the watched tree — `feedback_no_scratch_in_watched_tree`) and run
it against both the pre-cut and post-cut file:
    blank    — `trim() === ""` AND not inside a block comment
    comment  — entirely inside a `/* … */` (including blank lines within it and the `{`/`}` of a
               JSX `{/* … */}` wrapper), or a whole-line `//`
    code     — everything else. A line with code AND a trailing comment counts as CODE
               (conservative: it can never inflate the prose figure)
    Sanity check that reproduces RESEARCH's measurement on the PRE-cut file:
        total=797  comment=518  code=249  blank=30
        and the region sums verify: 110 + 28 + 147 + 1 + 84 + 427 = 797

THE THREE PINS (D-03) — the named targets:
    total lines            797  →  ≤ 380
    CODE lines             249  →  ≤ 120
    BODY (separator→EOF)   427  →  ≤ 175      ← the figure that answers the goal
    The BODY is measured as the exact RANGE from the `// ── The card ──` separator through EOF,
    not as a remembered number. Pin the range, not the number, or the comparison drifts.

TREE-TOTAL HONESTY, to be stated so nobody reads it as a regression: the five new files carry
their own docblocks, imports and wrappers, so the SUBTREE total rises to roughly 940 L against
797 today — about +18 %. That is normal and comparable to 188.1 (1593 → 1292 + 268 + 164 = 1724,
+8 %). A phase whose headline is "the file got smaller" must say out loud that the tree got
bigger.

MEASURED GATES: `tsc --noEmit -p tsconfig.app.json` → 33. `eslint src/components/workflows/` → 5.
Do NOT edit `scripts/vitest-count-gate.cjs` — the single upward pin move is Plan 07's.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the seven ranges, wire three terse call sites, and re-point the two importers</name>

  <files>
frontend/src/components/workflows/PhaseNodeCard.tsx
frontend/src/components/workflows/PhaseNode.tsx
frontend/src/components/workflows/PhaseNodeCard.test.tsx
frontend/src/components/workflows/nodePresentation.ts
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.tsx` in full (797 L) — every range being
  deleted and every range staying. Re-derive the boundaries from the section separators rather
  than from this plan's line numbers.
- `frontend/src/components/workflows/NodeCornerMarks.tsx`, `NodeRunOverlay.tsx`,
  `NodeIconWell.tsx` — the three props interfaces Plan 05 exported, so the call sites are written
  from the real signatures.
- `frontend/src/components/workflows/PhaseNode.tsx:80-100` — the adapter's import block.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:36-60` — the test file's import
  block and the `FlowEdge.test.tsx:80-85` separate-statement convention recorded near it.
- `frontend/src/components/workflows/nodePresentation.ts:140-150` — the `VerdictMarkKind`
  docblock that goes false on this cut.
- `frontend/src/components/workflows/WorkflowCanvas.test.tsx:680-690` — 188.1's one-direction
  fence, the shape Task 3 extends.
  </read_first>

  <action>
Delete the seven ranges named in `<interfaces>` from `PhaseNodeCard.tsx`. Delete only those
ranges; `RUN_MODE_NODE_MIN_HEIGHT`, `reading`, `runBorder`, `minHeight`, the node box and the
137-B card div stay.

Add module-scope imports for the three components and for `PhaseNodeCardProps` (and any other
contract type the card's own signature names) from `./phaseNodeCardContract`. The card no longer
needs `VERDICT_MARK`, `ringDash`, `ringSpecFor` or `own`; remove exactly those imports and no
others — `runReadingBorder` and `runReadingLabel` are still used by the run line and the border,
and `StatusChip` is still rendered.

Render the three components at byte-identical JSX positions: `NodeCornerMarks` where the verdict
and seal blocks were, `NodeRunOverlay` where the ring and chip were, `NodeIconWell` where the well
was. Keep the surrounding siblings and their order untouched — these elements are absolutely
positioned and their paint order is their document order, so a call site one sibling out of place
changes the rendering. **Terse call sites, per door (a): one to four lines each, no pointer
prose.** The explanation lives in each destination module's docblock, which is where D-01 puts it.

**THE CARD MUST NOT RE-EXPORT THE CONTRACT TYPES.** No `export type { BadgeSlots } from ...`, no
`export * from ...`. A re-export that exists so nobody has to change two lines is a second name
for the same type, free to be the one 189 imports by accident (D-06).

Update the two importers, each as a SEPARATE new statement with its own `188.2-06` comment rather
than by widening the existing line, per the `FlowEdge.test.tsx:80-85` convention:
`PhaseNode.tsx:88-92` and `PhaseNodeCard.test.tsx:45`. Leave `PhaseNodeCard` itself imported from
`./PhaseNodeCard`.

Correct `nodePresentation.ts:145-146` in this same commit: the sentence claiming
`PhaseNodeCard.NodeVerdictMark` is an alias of `VerdictMarkKind` is false the moment this cut
lands, and a docblock naming a home a type no longer has is the defect `PhaseNodeCard.tsx:33-35`
names. Point it at `phaseNodeCardContract.ts`. Change nothing else in that file — its fences
(`PhaseNodeCard.test.tsx:437-471`) are anchored on `ICON_TINT`, `DEFAULT_TINT`, `renderPhaseMark`
and `GROUNDING_TONE` identifiers, so the docblock edit breaks none of them, but a wider edit could.

Do not touch the file docblock yet — that is Task 2. Do not edit `scripts/vitest-count-gate.cjs`.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/PhaseNodeCard.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with **0 failed** — including `CARD_HTML_BASELINE`'s three whole-`innerHTML` comparisons and both geometry matrices, all captured in Wave 2 on the unmoved tree. **This is the phase's central claim and it is now machine-checked.** If a baseline fails, the call-site placement or the move is wrong; fix the code, never the baseline.
- Fence **N15 returns to exactly 1** — the clip mention now exists only in `NodeIconWell.tsx`. It was RED at 2 at the end of Plan 05; record both readings so the fence is visibly live rather than assumed.
- `cd frontend && npx vitest run src/components/workflows/PhaseNode.test.tsx src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.editing.test.tsx` exits 0 with **0 failed** — the adapter and the canvas render the card.
- **THE WR-04 FALSIFICATIONS STILL REACH THE MOVED CODE (V3)**: the two shipped tests near `PhaseNodeCard.test.tsx:2007-2050` (sites 4 and 5) pass, AND they were proven to still bite — temporarily remove the `own(...)` guard from `NodeCornerMarks.tsx` and confirm site 4 fails; revert; temporarily remove it from `NodeRunOverlay.tsx` and confirm site 5 fails; revert. Both sinks now live in different files and a falsification that stopped reaching either would be green and meaningless. Record both failure texts.
- `grep -Ec "^export (type|interface) \{?(BadgeSlot|BadgeSlots|BadgeSlot2Tuple|NodeVerdictMark|NodeRunStatus|PhaseNodeCardProps)" frontend/src/components/workflows/PhaseNodeCard.tsx` returns **0**, and `grep -c "export \*\|export type {.*} from" frontend/src/components/workflows/PhaseNodeCard.tsx` returns **0**. No re-export shim (D-06).
- `grep -rn "from \"@/components/workflows/PhaseNodeCard\"\|from \"./PhaseNodeCard\"" frontend/src` returns **exactly 2 lines** (the component import in `PhaseNode.tsx` and in the test), and `grep -rn "phaseNodeCardContract" frontend/src` returns **≥ 2** — the two re-pointed type imports.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` returns exactly **33**. `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors, 0 warnings**.
- `git diff --numstat -- frontend/src/components/workflows/PhaseNodeCard.tsx` shows deletions **≥ 440** and insertions **≤ 40**. Record both. This is what makes the diff read as a MOVE. **Do not claim the moved source is byte-identical** — it is not, and saying so would itself be an inherited-false claim.
  </acceptance_criteria>

  <done>The seven ranges are gone from the card, three terse call sites render the new modules at byte-identical positions, both importers point at the contract leaf with no shim, `nodePresentation`'s docblock is true again, and every Wave-2 baseline still holds.</done>
</task>

<task type="auto">
  <name>Task 2: Settle the prose — rewrite the file docblock and relocate the preamble (door b)</name>

  <files>
frontend/src/components/workflows/PhaseNodeCard.tsx
frontend/src/components/workflows/NodeCornerMarks.tsx
frontend/src/components/workflows/NodeRunOverlay.tsx
frontend/src/components/workflows/NodeIconWell.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.tsx:1-110` — the whole file docblock, read
  paragraph by paragraph against the two lists in `<interfaces>`: the six that become false and
  the five that must not move one character.
- `frontend/src/components/workflows/PhaseNodeCard.tsx:379-392` (as they now sit after Task 1) —
  the preamble comment. Distinguish the sentences about relocated blocks from the `technicalLine`
  / `stepNumber` sentences, which are about slots the card still owns.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx:135-147`, `:200-216`, `:334-351`,
  `:390-435` — the four fence regions that read the docblock's surviving paragraphs. Read them
  before touching any prose; several assert on words.
- `frontend/src/components/workflows/PlaneEditingLayer.tsx:1-52` and
  `frontend/src/components/workflows/editAffordance.ts:1-31` — how 188.1 left the PARENT's header
  describing what remains while the moved prose travelled with its code.
  </read_first>

  <action>
Rewrite `PhaseNodeCard.tsx`'s file docblock so it describes the component that now exists.

Move each of the six false-after-move paragraphs into the destination module whose code it
describes, appending it to that module's own docblock (D-01 — the prose travels WITH its code).
Two of the six are MIXED and must be SPLIT rather than moved whole: `:34-41` (the ring and chip
leave, but the run line and the border branch stay) and `:72-87` (the 3D mark leaves, but
`pt-[42px]` and `NODE_MIN_HEIGHT: 104` stay). Half-true is the worst state for a docblock; split
each sentence to the file that owns what it describes.

Leave a ONE-LINE pointer per destination in the card's header, so a reader arriving at the card
can find the seal, the ring, the mark and the contract without grepping.

**Do not touch the five fenced paragraphs** — `:5-9`, `:12-16`, `:59-70`, `:97-101`, `:103-109`.
Not one character. Each is asserted by a live fence, and the XSS clause in particular
(`:103-109`) is quoted verbatim by convention precisely so its grep guard can be anchored on the
JSX prop form rather than on the bare identifier.

Then apply door (b) to the body: relocate the sentences in the preamble comment that narrate
relocated blocks into the destination docblocks, and keep the `technicalLine`-declined and
`stepNumber`-unread sentences where they are — those describe slots the card still owns and 189
will read them. Target ~15-20 lines removed from the body.

Add one short paragraph to the card's header that this phase owes its reader and that 188.1's did
not need: **the tree got bigger while the file got smaller.** State the measured subtree total
against the pre-cut 797 and the percentage, so nobody later reads the sum as a regression against
a phase whose headline is that the file shrank.

Do not add any behaviour. Do not spend badge slot 1 — it stays EMPTY and reserved for 189 (D-12).
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/PhaseNodeCard.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with **0 failed**. A docblock edit that turns a fence red is a fence doing its job; a docblock edit that turns one VACUOUS is invisible, which is why the next criterion exists.
- **THE FIVE FENCED PARAGRAPHS ARE PROVABLY UNTOUCHED**: `git diff -U0 -- frontend/src/components/workflows/PhaseNodeCard.tsx` contains **no** `-` line originating in `:5-9`, `:12-16`, `:59-70`, `:97-101` or `:103-109` of the pre-Task-2 file. Verify by extracting those five spans from `git show` and grepping the post-edit file for each verbatim; paste the five confirmations into the SUMMARY.
- The XSS paragraph is byte-identical: `git show HEAD:frontend/src/components/workflows/PhaseNodeCard.tsx | sed -n '103,109p'` matches the corresponding text in the working file exactly.
- `wc -l` of the docblock region (line 1 through the line before the first `import`) is **≤ 75**, down from 110. Record the exact figure and the command.
- Each of the six relocated paragraphs is present in exactly one destination file: `grep -l` for a distinctive phrase from each returns exactly one path, and it is the expected one. List all six in the SUMMARY.
- `grep -c "badge slot 1\|slot 1" frontend/src/components/workflows/PhaseNodeCard.tsx` still finds the reserved-for-189 statement, and no badge is rendered in slot 1 (D-12).
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` returns exactly **33**; `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors**.
  </acceptance_criteria>

  <done>The card's docblock describes the card that exists, with each relocated paragraph living beside the code it explains and the five fenced paragraphs byte-identical; the preamble's relocated sentences are gone from the body; and the tree-grew-while-the-file-shrank fact is stated rather than left to be discovered.</done>
</task>

<task type="auto">
  <name>Task 3: Measure D-03's three numbers with the named classifier, and close the one-direction fence</name>

  <files>
frontend/src/components/workflows/PhaseNodeCard.test.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/PhaseNodeCard.tsx` as it now stands — for the exact position
  of the `// ── The card ──` separator, which is the body range's opening boundary.
- `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — the ESM-cycle `describe(` block's
  third `it(`, which Plan 01 authored with only the `not.toContain("function Node…(")` half.
- `frontend/src/components/workflows/WorkflowCanvas.test.tsx:680-690` — the shipped one-direction
  fence, including the no-re-export-shim assertion, which is the shape to complete here.
- `.planning/phases/188.2-.../188.2-RESEARCH.md` § The Cut List — the classifier's three rules and
  the pre-cut region map that the classifier must reproduce.
  </read_first>

  <action>
Write the named classifier to the session scratchpad directory — **never into the watched tree**
(`feedback_no_scratch_in_watched_tree`) — implementing exactly the three rules in `<interfaces>`:
blank is an empty trim OUTSIDE a block comment; comment is anything wholly inside a `/* … */`
(including blank lines within it and the braces of a JSX `{/* … */}` wrapper) or a whole-line
`//`; everything else is code, and a line carrying code plus a trailing comment counts as code.

**Validate the classifier before trusting its output**: run it against
`git show HEAD:frontend/src/components/workflows/PhaseNodeCard.tsx` (the PRE-cut file) and confirm
it reproduces `total=797 comment=518 code=249 blank=30`. If it does not, the classifier is wrong
and every after-number it produces is uncomparable — fix it and re-validate before proceeding.

Then produce the D-03 before/after table with that one script:

    total lines            797  →  measured, target ≤ 380
    CODE lines             249  →  measured, target ≤ 120
    BODY (separator→EOF)   427  →  measured, target ≤ 175

Measure the body as the RANGE from the `// ── The card ──` separator through EOF — locate the
separator by grep and compute the range, never by a remembered line number. **If any of the three
misses its target, record the measured number and the shortfall plainly and do not adjust the
target.** Door (b) was chosen precisely to close the ~12-line gap the extraction alone leaves;
if it did not, that is a measured near-miss and it is reported as one.

Also record the subtree total: the card plus all five new files, against 797, with the percentage.

Then complete the one-direction fence in `PhaseNodeCard.test.tsx`. Plan 01 authored the
`not.toContain("function NodeRunOverlay(")` half, which was true by vacuity. Add the other half in
the `WorkflowCanvas.test.tsx:680-690` shape: assert the card really IMPORTS each of the three
components and the contract types by matching the import statements, and assert it leaves NO
re-export shim by asserting the absence of a re-export form for any contract type name. Without
the import half, the three negatives are also satisfied by three modules nobody uses.

Then remove any remaining `if (path in CARD_MODULES)` guard from the cycle fence's destination
loop — all five files now exist, and a guard over an existing file is a fence that reads an empty
string in silence. Confirm each of the five resolved sources is non-empty.

Do not edit `scripts/vitest-count-gate.cjs`.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/PhaseNodeCard.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- The classifier reproduced `total=797 comment=518 code=249 blank=30` on the PRE-cut file before it was used for any after-number. Paste that validation output into the SUMMARY. A classifier that does not reproduce the before-figures cannot produce a comparison.
- The D-03 table is recorded with all three measured after-numbers, the command that produced each, and an explicit PASS or measured-near-miss verdict per line. `PhaseNodeCard.tsx` total is **≤ 380**, CODE is **≤ 120**, BODY is **≤ 175** — or the shortfall is stated in lines with no target adjusted.
- The body range is reported as `separator line N through EOF at line M`, with the grep that located the separator — the range, not the number (`188.2-RESEARCH.md`: "pin the range, not the number, or the before/after comparison drifts").
- The subtree total (card + 5 new files) is recorded against 797 with the percentage, and the SUMMARY states that the tree growing is expected and comparable to 188.1's +8 %.
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with **0 failed**, and the one-direction fence's import half is proven non-vacuous: temporarily rename one of the three imports in `PhaseNodeCard.tsx`, confirm the fence fails, revert. Record the failure text.
- `grep -c "if (path in CARD_MODULES)" frontend/src/components/workflows/PhaseNodeCard.test.tsx` returns **0**, and the resolved-source-non-empty assertion covers all five destinations unconditionally.
- No scratch file was written inside the repository: `git status --porcelain` shows no untracked `.cjs`/`.js`/`.py` anywhere under the working tree.
- `node scripts/vitest-count-gate.cjs` prints `count gate OK`, **failed 0**, no `[count-decrease]`, no `[missing-file]`; `git diff --stat -- scripts/vitest-count-gate.cjs` reports **0 files changed**.
  </acceptance_criteria>

  <done>All three D-03 numbers are measured with a classifier validated against the pre-cut file's known figures and reported against their named targets; the subtree total is stated honestly; the cut is proven to have one direction with no shim, and no cycle-fence guard remains over a file that exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a cut → the fences that covered what left | Fifteen negative fences were re-scoped in Plan 01 precisely so this cut cannot narrow them. This is the commit where that either holds or was wasted. |
| a security mitigation → its falsification, across a file boundary | WR-04's two sinks now live in two different modules while both falsifications drive through the card. A falsification that stopped reaching its sink is green and meaningless. |
| a docblock under a fence → an editorial rewrite | Five paragraphs are asserted by live fences. Rewording one can turn a fence red — visible — or vacuous — invisible. |
| a measured target → a rounded one | A body figure that misses by 12 and is reported as "~170" is the exact failure this phase's D-03 exists to prevent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188.2-23 | Tampering | WR-04 prototype-pollution sinks 4 and 5, split across `NodeCornerMarks` and `NodeRunOverlay` (V3) | mitigate | Both falsifications must PASS and must be proven to still BITE — each guard is temporarily removed from its new home and the corresponding falsification observed RED, then reverted. Passing is not evidence of reaching. ASVS V5 |
| T-188.2-24 | Tampering | the XSS clause and its fence (N10, `:426`) | mitigate | The `:103-109` paragraph is asserted byte-identical against `git show` output; N10 now reads the six-file subtree, so all three string-rendering modules are inside it. ASVS V5 |
| T-188.2-25 | Repudiation | the fifteen re-scoped fences at the moment the code actually leaves | mitigate | The full card suite must be green after the cut with N15 measurably moving 2 → 1, which proves the clip fence is live rather than assumed; C-2's plants in Plan 05 already proved the sweep reaches the destinations |
| T-188.2-26 | Spoofing | a second name for the same type (D-06) | mitigate | No re-export shim, asserted by grep AND by the one-direction fence's no-shim half; the two importers are re-pointed as separate statements so the diff is auditable |
| T-188.2-27 | Elevation of Privilege | red line D-14 — the card acquiring run-state derivation during the cut | mitigate | Only `arc` moved out; `reading`, `runBorder` and `minHeight` stay as the card's own already-derived values, and no new derivation is added. `@/lib/phaseState` remains a type-only import everywhere in the subtree |
| T-188.2-28 | Repudiation | a rounded D-03 figure | mitigate | The classifier is named, written to the scratchpad, and validated against the pre-cut file's known 797/518/249/30 before any after-number is trusted; a missed target is reported as a measured shortfall with no target adjusted |
| T-188.2-SC | Tampering | npm / pip / cargo installs | mitigate | Zero packages installed. Hard gate: `git diff --stat -- frontend/package.json frontend/package-lock.json` reports 0 files changed |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` → 0 failed, all Wave-2 baselines hold
- `cd frontend && npx vitest run src/components/workflows/PhaseNode.test.tsx src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.editing.test.tsx` → 0 failed
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → exactly 33
- `cd frontend && npx eslint src/components/workflows/` → exactly 5 errors, 0 warnings
- `node scripts/vitest-count-gate.cjs` → `count gate OK`, failed 0, no `[count-decrease]`, no `[missing-file]`
- `git diff --numstat -- frontend/src/components/workflows/PhaseNodeCard.tsx` → deletions ≥ 440, insertions ≤ 40
- `git diff --stat -- frontend/package.json frontend/package-lock.json backend/ supabase/migrations/ scripts/` → 0 files changed
</verification>

<success_criteria>
1. The seven ranges are deleted and three terse call sites render the new modules at byte-identical JSX positions; every Wave-2 baseline still holds (D-02).
2. D-03's three numbers are measured with a NAMED classifier validated against the pre-cut file, and reported against named targets — total ≤ 380, code ≤ 120, body ≤ 175 — with any shortfall stated in lines rather than rounded.
3. Door (b) was applied: the preamble's relocated sentences moved into destination docblocks, and the file docblock was rewritten to 75 lines or fewer.
4. The five fenced paragraphs are byte-identical, proven against `git show` output.
5. Both importers re-point with no re-export shim, enforced by the one-direction fence's import half and proven non-vacuous by a renamed import (D-06).
6. Both WR-04 falsifications pass AND were proven to still bite their now-separated sinks (V3).
7. `nodePresentation.ts`'s alias sentence is corrected in the same commit that makes it false.
8. The subtree total is stated honestly against 797, so the growth cannot be misread as a regression.
</success_criteria>

<output>
Create `.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-06-SUMMARY.md` when done. Record verbatim: the classifier's validation output on the pre-cut file; the D-03 before/after table with the command per number and a PASS or shortfall verdict per line; the body range as `separator line N → EOF line M`; the subtree total and percentage; `git diff --numstat` for the card; the two WR-04 bite-proof failure texts; the five byte-identical paragraph confirmations; the N15 readings 2 → 1; and the renamed-import failure text.
</output>
