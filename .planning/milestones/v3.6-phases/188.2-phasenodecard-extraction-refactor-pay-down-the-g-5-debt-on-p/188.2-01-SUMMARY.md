---
phase: 188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p
plan: 01
subsystem: testing
tags: [vitest, import.meta.glob, raw-source-fence, esm-cycle, typescript, ts-expect-error, react]

# Dependency graph
requires:
  - phase: 188.1
    provides: "the `CANVAS_SUBTREE_PATHS` subtree-fence idiom, its three non-vacuity controls (measured RED under a SUBSTITUTION a length pin missed), and the `?raw` ESM-cycle fence with inline positive controls"
  - phase: 188
    provides: "the F7 lesson — an invariance fence says nothing about what it no longer covers, and neither the count gate nor `tsc` can see the loss"
provides:
  - "`CARD_SUBTREE_PATHS` / `CARD_MODULES` / `cardSubtreeSource` in `PhaseNodeCard.test.tsx` — a six-path subtree source naming all five destination modules BEFORE any of them exists"
  - "17 negative `?raw` fences and 4 haystacks re-pointed onto that subtree source, green against a completely unchanged production tree"
  - "both moving-needle landmines re-anchored: D-08's clip-utility count and P4's `canvas-ring-spin` positive (which CONTEXT does not name)"
  - "the D-09 third-badge typecheck invariant, asserted for the FIRST time in this repository"
  - "the PhaseNodeCard ESM-cycle fence, with inline POSITIVE and NEGATIVE controls, authored before the modules it guards"
affects: [188.2-02, 188.2-03, 188.2-04, 188.2-05, 188.2-06, 188.2-07, 189]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "subtree `?raw` fence: `import.meta.glob` + an explicit path list that names destination modules before they exist; `?? \"\"` makes an absent path contribute the empty string rather than throw"
    - "three non-vacuity controls per subtree fence: a length pin, EACH destination named individually (a length pin catches TRUNCATION but never SUBSTITUTION), and resolved-module-non-empty once the file exists"
    - "`@ts-expect-error` + the `void x` house idiom (deriveTier.ts:124-126) as a TYPE-GATE control that reads 33 narrow / 34 widened"

key-files:
  created: []
  modified:
    - frontend/src/components/workflows/PhaseNodeCard.test.tsx

key-decisions:
  - "The five destination paths are declared TWICE — once in `CARD_SUBTREE_PATHS`, once in a named `CARD_DESTINATION_PATHS` const reused by both the non-vacuity loop and the ESM-cycle loop. A single inline array would have satisfied the acceptance grep but forced a second, drift-prone copy in Task 3."
  - "P2's `/ 4` bound was NOT loosened. The haystack widened to `cardSubtreeSource` and the divisor stayed at 4 — measured, the carved seal block is 569 chars against a 45783-char subtree, i.e. 1.24 %, so there was never any pressure on the bound."
  - "P5 and P6 stay on `phaseNodeCardSource` by design, each with an inline note. Task 3's one-direction fence deliberately also reads `phaseNodeCardSource`, because it asserts what the CARD no longer declares — widening it to the subtree would make it assert the opposite of its purpose."

patterns-established:
  - "Author the fence BEFORE the cut, against the unchanged tree: every re-scoped fence must stay green with ZERO production edits, which is what makes the re-scope provably behaviour-neutral."
  - "A guarded loop that skips every path today is legitimate — but the skip must be SAID in an inline note naming the plan that will observe it RED."

requirements-completed: []  # EMPTY BY DESIGN — ROADMAP §Phase 188.2: "Requirements: none — refactor phase."

# Metrics
duration: 32min
completed: 2026-08-07
---

# Phase 188.2 Plan 01: Re-scope the card's source fences before the cut Summary

**Seventeen negative `?raw` fences (fifteen of which were about to go silently green-vacuous, including the zero-graph-library property and the `dangerouslySetInnerHTML` XSS ban) now read a six-path subtree source that names five modules that do not exist yet — plus the third-badge typecheck invariant and the ESM-cycle fence, both asserted here for the first time.**

## Performance

- **Duration:** ~32 min
- **Started:** 2026-08-07T01:17:00Z
- **Completed:** 2026-08-07T01:49:00Z
- **Tasks:** 3
- **Files modified:** 1 (`frontend/src/components/workflows/PhaseNodeCard.test.tsx`, 2063 → 2288 lines, +248/−23)

## Accomplishments

- `CARD_SUBTREE_PATHS` / `CARD_MODULES` / `cardSubtreeSource` shipped, naming `./phaseNodeCardContract.ts`, `./ownProperty.ts`, `./NodeCornerMarks.tsx`, `./NodeRunOverlay.tsx` and `./NodeIconWell.tsx` — **none of which exists at this commit**, which is the property being shipped.
- All **17** negative fences and all **4** haystacks re-pointed. The suite is green against a **completely unchanged production tree**, which is the proof the re-scope is behaviour-neutral.
- Both landmines re-anchored with their reasons recorded inline: D-08's clip-utility count and P4's `canvas-ring-spin` positive.
- **D-09 asserted for the first time.** Re-measured before writing it: `BadgeSlot2Tuple` has exactly two references in the whole tree (both its own declaration lines, `PhaseNodeCard.tsx:174-175` — zero callers), and `@ts-expect-error` occurred **0 times** in the entire workflows directory.
- The ESM-cycle fence authored before the modules it guards, with inline positive controls for all four forbidden import forms **and** the two negative controls that stop it false-positiving on `./PhaseNodeCard?raw` and `./phaseNodeCardContract`.

## Task Commits

1. **Task 1: the subtree source + three non-vacuity controls** — `471104d4` (test)
2. **Task 2: re-point 17 negatives, 4 haystacks, both landmines** — `28f7ee64` (test)
3. **Task 3: D-09 third-badge invariant + ESM-cycle fence** — `8cb82a6b` (test)

## Recorded measurements (verbatim, as `<output>` requires)

### Vitest test count after each task

| Point | Command | Result |
|---|---|---|
| Baseline (HEAD `d9039d36`) | `npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` | `Tests  107 passed (107)` · `Test Files  1 passed (1)` |
| After Task 1 | same | `Tests  108 passed (108)` |
| After Task 2 | same | `Tests  108 passed (108)` |
| After Task 3 | same | `Tests  112 passed (112)` |

112 = 107 baseline + 1 (subtree non-vacuity control) + 1 (D-09) + 3 (cycle fence) — the plan predicted exactly 112.

### Grep counts

| Grep | Plan expected | Measured |
|---|---|---|
| `grep -c "CARD_SUBTREE_PATHS" PhaseNodeCard.test.tsx` | ≥ 4 | **6** |
| `grep -c "cardSubtreeSource" PhaseNodeCard.test.tsx` (after Task 2) | ≥ 24 | **28** |
| the five destination paths, `grep -c` | 10 lines | **10** |
| `git diff -U0 … \| grep -c "^+.*length / 2"` | 0 | **0** |
| `ls frontend/src/components/workflows/phaseNodeCardContract.ts` | no such file | `No such file or directory` |

`grep -n "phaseNodeCardSource"` — the plan claimed "exactly 4 lines" after Task 2. **Measured 3 CODE lines** at that point (`:37` the `?raw` import, P5, and P6 — whose *two* operands sit on **one** line, which is why the plan's arithmetic reads 4). After Task 3 there are **6** code lines, the three extra being the one-direction fence at `:624-626`, which the plan itself instructs to assert over `phaseNodeCardSource`. No fence was missed; every remaining occurrence is deliberate and annotated.

### Type gate

```
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"
  baseline .................. 33   (grep -c "src/components/workflows" → 0)
  after Task 3 .............. 33   (grep -c "src/components/workflows" → 0)
  with BadgeSlots widened ... 34
  after git checkout -- ..... 33
```

### Other gates

- `npx eslint src/components/workflows/` → `✖ 5 problems (5 errors, 0 warnings)`; occurrences in `PhaseNodeCard.test.tsx`: **0**. No eslint-disable was added.
- `node scripts/vitest-count-gate.cjs` → **`count gate OK` — 45/45 pinned files present, no per-file decrease, 0 failing.** Exit 0. `PhaseNodeCard.test.tsx  107  112  +5`; `total 2482  2487  +5`. An increase over a pin is legal and printed as `+N`; the upward pin move stays with Plan 07 per RESEARCH.
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json scripts/` → **0 files changed** (T-188.2-SC hard gate: zero packages installed).

## RED observations (full failure text)

### C-1(a) — TRUNCATION

Removed `"./NodeIconWell.tsx"` from `CARD_SUBTREE_PATHS`:

```
FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard — the scope fences
     (source guard) > the subtree fence covers the code 188.2 moves, wherever that code lives
AssertionError: expected [ './PhaseNodeCard.tsx', …(4) ] to have a length of 6 but got 5

- Expected
+ Received

- 6
+ 5

 ❯ src/components/workflows/PhaseNodeCard.test.tsx:511:32
Tests  1 failed | 107 passed (108)
```

### C-1(b) — SUBSTITUTION (the asymmetry that justifies the loop)

Reverted, then swapped `"./NodeRunOverlay.tsx"` → `"./nodePresentation.ts"` (a real file) in `CARD_SUBTREE_PATHS` only:

```
FAIL src/components/workflows/PhaseNodeCard.test.tsx
AssertionError: expected [ './PhaseNodeCard.tsx', …(5) ] to include './NodeRunOverlay.tsx'
Tests  1 failed | 107 passed (108)
```

**`toHaveLength(6)` still passed and all six `toContain` literals still passed** — the length pin and the move-invariant literals are both blind to a substitution while the code still sits in the card. That asymmetry is the entire reason the individual-name loop exists, and it is now observed rather than argued.

### C-4 — the clip-utility count (D-08's landmine)

Planted a second prose mention of the clipping utility in a comment in `PhaseNodeCard.tsx`:

```
FAIL src/components/workflows/PhaseNodeCard.test.tsx > 188-06 — no clipping anywhere in the
     node subtree (the mark overhangs) > the card's PROSE names it exactly once — the rule,
     and only the rule
AssertionError: expected 2 to be 1 // Object.is equality
Tests  1 failed | 107 passed (108)
```

Reverted with `git checkout -- frontend/src/components/workflows/PhaseNodeCard.tsx`.

### N5 — the zero-graph-library property (D-184-06), the fence that matters most

Planted the graph package scope as a literal inside a comment in `PhaseNodeCard.tsx`:

```
FAIL src/components/workflows/PhaseNodeCard.test.tsx > PhaseNodeCard — the scope fences
     (source guard) > the card names the canvas graph package NOWHERE (D-184-06)
AssertionError: expected '/**\n * Phase 184-03 Task 2 (D-184-06…' not to contain '@xyflow'
Tests  1 failed | 107 passed (108)
```

Reverted.

### C-3 — the third-badge typecheck invariant, BOTH directions

Widened `PhaseNodeCard.tsx:175` to `export type BadgeSlots = readonly BadgeSlot[]`:

```
$ npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"
34
$ npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep "src/components/workflows"
src/components/workflows/PhaseNodeCard.test.tsx(324,5): error TS2578: Unused '@ts-expect-error' directive.
```

Then `git checkout -- frontend/src/components/workflows/PhaseNodeCard.tsx`:

```
$ npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"
33
$ npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "src/components/workflows"
0
```

A control that only ever reads 33 is not known to be a control. This one was observed at 34 and back at 33.

### C-7 — the seal extractor after the re-scope

`PLANTED_SEAL_SOURCE` still passes (`sealJsxBlock(PLANTED_SEAL_SOURCE)` matches `statusIdentifier()`), and the carve against the widened haystack is comfortably inside the bound:

| Quantity | Value |
|---|---|
| carved seal block length | **569** chars |
| `cardSubtreeSource.length` | **45783** chars |
| bound (`length / 4`) | **11445.75** |
| block as a fraction of the haystack | **1.24 %** |

No pressure whatsoever on the `/ 4` bound — the plan's warning against loosening it to `/ 2` never came into play.

### Cycle-fence negative controls (asserted, both green)

`'import type { X } from "./PhaseNodeCard?raw"'` and `'import { own } from "./phaseNodeCardContract"'` each fail to match **both** `IMPORT_FROM_CARD` and `DYNAMIC_IMPORT_CARD`. Asserted as a live loop in the first `it(` of the cycle-fence describe, not trusted from prose — an over-broad regex here would have made Plan 05's fence unfixable in the one file that has to carry it.

## Files Created/Modified

- `frontend/src/components/workflows/PhaseNodeCard.test.tsx` — the only file touched. Adds the subtree source block + `CARD_DESTINATION_PATHS`, re-points 17 negatives and 4 haystacks, annotates P5/P6/N15/P4, re-scopes `stripComments`' docblock, adds the D-09 control and the three-`it(` ESM-cycle fence.

## Decisions Made

1. **`CARD_DESTINATION_PATHS` is a named const, not an inline array.** The plan's Task 1 text implies an inline `for (const path of [...] as const)`. Task 3's cycle fence needs the identical five paths, and a second inline copy is exactly the drift the rest of this file's conventions exist to prevent. The acceptance grep still measures 10 (each destination named twice), which is the property the criterion is actually about.
2. **The three cycle-fence `it(` blocks live in their own `describe(` placed immediately after the scope-fences block**, mirroring `WorkflowCanvas.test.tsx:637`. No existing `it(` or `describe(` was renamed, deleted or re-described anywhere in this plan.
3. **Task 3's one-direction fence asserts over `phaseNodeCardSource`, not the subtree** — deliberately, and per the plan's own wording. It asserts what the CARD no longer declares; over the subtree it would assert the opposite of its purpose once the modules exist.
4. **A non-vacuity floor was added to the otherwise fully-skipped cycle loop** (`expect(CARD_DESTINATION_PATHS).toHaveLength(5)`), so the `it(` carries at least one live assertion at this commit rather than passing with zero.

## Deviations from Plan

None requiring a deviation rule. No production file was changed; no package was installed; no architectural decision arose.

**Total deviations:** 0
**Impact on plan:** none — this plan is behaviour-neutral on the shipped tree by construction, and that is now a measured fact rather than an intention.

## Inherited claims re-measured (per the standing "don't inherit unmeasured claims" rule)

Fourteen claims from the plan were re-derived. **Twelve were exactly right.** Two are corrected:

| # | Claim | Verdict |
|---|---|---|
| 1 | `const RING_RADIUS` at `PhaseNodeCard.tsx:313` | **FALSE — it is at `:315`.** Off by two. The literal itself is correct and occurs exactly once. |
| 2 | `grep -n "phaseNodeCardSource"` returns "exactly 4 lines" after Task 2 | **FALSE as a line count — measured 3 code lines.** P6's *two* operands share **one** line. The plan counted operands, not lines. No fence was missed. |
| 3 | 17 negative fences over `phaseNodeCardSource` | TRUE — enumerated and re-pointed, all 17. |
| 4 | Baseline `107 passed / 107` | TRUE. |
| 5 | Baseline `tsc -p tsconfig.app.json` = 33, zero in `src/components/workflows/` | TRUE. |
| 6 | Bare `npx tsc --noEmit` exits 0 having checked nothing | TRUE — never used. |
| 7 | eslint baseline = 5 errors, 0 warnings | TRUE. |
| 8 | All six move-invariant literals occur **exactly once** in `PhaseNodeCard.tsx` | TRUE — all six measured at 1. |
| 9 | `PhaseNodeCard.tsx:772` is the tree's only clipping-utility mention | TRUE, and it is at `:772` exactly. |
| 10 | `canvas-ring-spin` at `PhaseNodeCard.tsx:734` | TRUE, exactly. |
| 11 | `@ts-expect-error` occurs 0 times in the workflows directory | TRUE. |
| 12 | `BadgeSlot2Tuple` has zero callers | TRUE — two references, both its own declaration lines. |
| 13 | The workflows directory holds 66 `.ts`/`.tsx` files, so `> 5` is a safe floor | TRUE — measured 66. |
| 14 | `TARGETS[0]` is the bare directory `src/components/workflows`, so nothing needs registering | TRUE. |

## Issues Encountered

**A flaky `failed 1` in the count gate, not reproducible and not ours.** The first `node scripts/vitest-count-gate.cjs` run after Task 3 reported `total 2487 · failed 1` and `RESULT: COUNT GATE VIOLATED [failing-tests]`, without naming the failing test. Investigated rather than re-run-and-hoped:

- All 12 `TARGETS` entries were executed in three separate vitest invocations — `39 passed (39) / 2277 passed (2277)`, `5 passed (5) / 169 passed (169)`, `1 passed (1) / 41 passed (41)`. Sum **2487**, **zero** failures.
- The gate's exact single-invocation shape was then reproduced by hand with `--reporter=json` over all 12 targets: `numTotal 2487 failed 0`.
- The gate itself was re-run: **`count gate OK` — 45/45 pinned files present, no per-file decrease, 0 failing**, exit 0.

Conclusion: a flake under the gate's full-parallel run, in a suite outside this plan's single file (which is 112/112 green in every run, isolated and combined). Logged here rather than fixed — out of scope per the executor scope boundary. Worth watching in Plans 02-07: if it recurs, it will need naming.

## User Setup Required

None.

## Next Phase Readiness

- **The fences are in place before the cut, which is the whole point of the wave ordering.** Plans 02-06 can now move the ~471 lines out of `PhaseNodeCard.tsx` knowing that no negative fence can silently narrow.
- **Owed to later plans, by design, and each already annotated inline in the test file:**
  - **C-5 (Plan 05):** the cycle fence's second `it(` skips every path today (`if (path in CARD_MODULES)` is false five times). Plan 05 must observe it RED against a deliberately planted back-import.
  - **C-2 / T-188.2-02 (Plan 05):** N10's `dangerouslySetInnerHTML=` ban must be observed RED against a plant at the seal's authored-string render site in `NodeCornerMarks.tsx`.
  - **Plan 06:** the one-direction fence is green by vacuity; it becomes meaningful once the matching `import` assertions are added.
  - **Plan 07:** the count-gate pin move (`PhaseNodeCard.test.tsx` 107 → 112, total 2482 → 2487). Deliberately NOT taken here — the gate prints `+5` and passes, and RESEARCH's recommended shape is one upward pin move at the end of the phase.
- **No blockers.**

## Self-Check: PASSED

- `FOUND: .planning/phases/188.2-…/188.2-01-SUMMARY.md`
- `FOUND: frontend/src/components/workflows/PhaseNodeCard.test.tsx`
- `FOUND: 471104d4` · `FOUND: 28f7ee64` · `FOUND: 8cb82a6b`

---
*Phase: 188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p*
*Completed: 2026-08-07*
