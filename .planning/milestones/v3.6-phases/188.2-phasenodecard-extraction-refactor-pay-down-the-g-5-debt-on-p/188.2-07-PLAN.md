---
phase: 188.2
plan: 07
type: execute
wave: 6
depends_on: ["188.2-02", "188.2-06"]
files_modified:
  - CLAUDE.md
  - scripts/vitest-count-gate.cjs
  - .claude/skills/sketch-findings-agentic-rag/references/icon-convention.md
  - .claude/skills/sketch-findings-agentic-rag/SKILL.md
  - .planning/reported-bugs/BUG-260806-01-selected-card-occludes-its-own-remove-control.md
  - .planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-UAT.md
  - .planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-DEFERRED.md
autonomous: false
requirements: []
# EMPTY BY DESIGN — refactor phase, no REQ- ids (ROADMAP §188.2).
success_criteria: ["SC#5", "SC#6", "SC#7"]
gap_closure: false

must_haves:
  truths:
    - "The CLAUDE.md hot-file ledger row for `PhaseNodeCard.tsx` reads *satisfied (188.2)* rather than *G-5 fires — extraction due*, and carries the MEASURED before/after figures with the commands that produced them, in the house style of the `WorkflowCanvas.tsx` row — so Phase 189 opens against an honest ledger and its discuss-phase no longer owes a refactor recommendation on this file"
    - "Three measured stale documentation pointers are corrected in the phase that moves what they name: `icon-convention.md:66` (the ⛨, already stale at `:440` when its true home was `:653`, and moving again now), `icon-convention.md:69` (the ＋/✕, already stale at `WorkflowCanvas.tsx` when 188.1 moved them to `PlaneEditingLayer.tsx` — the exact elements Plan 02 edited), and the sketch skill's *always read `PhaseNodeCard.tsx`* geometry row, four of whose four facts left the file"
    - "BOTH UAT boards are driven and carry DISJOINT evidence — Board A (prove UNCHANGED, 3 lean rows) may never cite Board B's evidence and vice versa. Phase 188 shipped a board whose header read COMPLETE while six rows read `[pending]`; a row without its own driven evidence is not a driven row"
    - "D-11's driven half is discharged: `elementFromPoint` at the ✕'s own centre returns the ✕ WITH THE CARD SELECTED, with the node's z-index reported as 1000 so the row proves the failing state was actually reached. Deselected already passed before the fix — a row that does not select proves nothing"
    - "`BUG-260806-01`'s frontmatter moves `folded` → `closed` with `verified_closed_by` naming this phase, only after the driven row observed it fixed — never on the strength of the analysis alone"
    - "One upward `BASELINE` pin move, read from the gate's own `actual` column across two agreeing runs. A pin is LOWERED only alongside a plan-authorised deletion; nothing here is a deletion"
  artifacts:
    - path: "CLAUDE.md"
      provides: "the flipped G-5 ledger row with measured figures and the commands that produced them"
      contains: "satisfied (188.2"
    - path: ".planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-UAT.md"
      provides: "Board A and Board B with per-row driven evidence in disjoint columns"
      contains: "elementFromPoint"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "the single upward pin move for PhaseNodeCard.test.tsx"
      contains: "PhaseNodeCard.test.tsx"
  key_links:
    - from: "CLAUDE.md hot-file ledger"
      to: "Phase 189's discuss-phase G-5 audit"
      via: "the row a future orchestrator scans a plan's files_modified against"
      pattern: "PhaseNodeCard.tsx"
---

<objective>
Close the phase: flip the ledger row this phase exists to flip, correct the three documentation
pointers it invalidates, move the count-gate pin once upward, and drive both UAT boards.

Purpose: the ledger row is not bookkeeping — it is what changes what Phase 189 is allowed to do.
`PhaseNodeCard.tsx`'s row currently reads *G-5 fires — extraction due*, which is why 189's
discuss-phase owes a refactor recommendation as its first option. This plan is what retires that
obligation, and it retires it with measured numbers rather than with an assertion.

The two boards are the other half, and they cannot be automated. jsdom applies no CSS, computes
no stacking contexts, and `.click()` bypasses hit-testing entirely, so the single most important
claim of Plan 02 — that a **selected** card no longer occludes its own `✕` — is structurally
invisible to every test in this repository. `WorkflowCanvas.tsx:390-410` records that this exact
blindness once shipped an unreachable `＋` while every unit test passed, and this very bug
survived from 184-12 until an operator found it by hand. One driven row is not ceremony here; it
is the only instrument that can see the defect class.

Board A is deliberately LEAN — three rows, per the efficiency calibration's
lean-UAT-for-refactors rule. Do **not** re-drive Phase 188's 16-row board. SC#10's cross-provider
roster does **not** apply: this phase touches no streaming, no agent loop, no provider routing and
no UI state machine, and that absence is stated as a decision rather than left as an omission.

Output: a flipped ledger, three corrected pointers, one pin move, a closed bug report, a deferred
register and a driven `188.2-UAT.md`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-CONTEXT.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-RESEARCH.md
@.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-VALIDATION.md
@.planning/reported-bugs/BUG-260806-01-selected-card-occludes-its-own-remove-control.md

<interfaces>
<!-- RE-MEASURED at HEAD `42b42cb7` by the planner, 2026-08-06. -->

THE LEDGER ROW TO FLIP — `CLAUDE.md` § Workflow guardrails, hot-file ledger. Its current text:

    | `frontend/src/components/workflows/PhaseNodeCard.tsx` | 184 (184-03, 184-08) / 185 (185-01
    …, 185-08 …, 185-09 …) / 188 (188-06 …) / 188.1 (188.1-04 …) — **7 plans across 4 phases,
    797 lines**. … | **G-5 fires — extraction due.** … Per G-5, a dedicated refactor phase on
    this file is due BEFORE the next feature phase that touches it — Phase 189's discuss-phase
    must produce that refactor recommendation as its first option, not the planned feature. …
    Badge slot 1 is empty and reserved for 188/189. |

    THE TEMPLATE is the `WorkflowCanvas.tsx` row two rows above it: it names the measured
    before AND after figures, the commands that produced them, what was extracted and to where,
    what was PROVED rather than asserted, and what remains deferred with its own trigger.

    ⚠ MEASURED TOUCH HISTORY, re-derived this session by `git log --oneline -- <file>`: exactly
    8 commits — `b7603244`(184-03) `296350b4`(184-08) `c31c3811`(185-01) `30cb77f9`(185-08)
    `6bdc658a`(185-09) `c635e39f`(188-06) `60cddc78`(188-06) `b018b20b`(188.1-04) — across
    4 distinct phases. The row says "7 plans"; the commit count is 8 because 188-06 landed twice.
    Both figures are defensible; say which you are quoting and how it was derived, in the house
    style of the `WorkflowCanvas.tsx` row, which corrects its own stale numbers on measurement.

THE TWO INVARIANTS THE ROW MUST KEEP CARRYING, both now type/test-enforced rather than prose:
    · a THIRD BADGE IS A TYPECHECK ERROR — as of 188.2-01 this is asserted by an
      `@ts-expect-error` control that was observed RED at exactly 34 type errors. Until this
      phase it was asserted NOWHERE. Say so; the row currently implies it was already enforced.
    · NO FOCUSABLE CONTROL LIVES INSIDE THE CARD — the ✕ and ＋ live on the lane.
    · BADGE SLOT 1 STAYS EMPTY, reserved for 189 (D-12).

THE THREE STALE DOCUMENTATION POINTERS, each measured:

    .claude/skills/sketch-findings-agentic-rag/references/icon-convention.md:66
      says  `⛨` → `PhaseNodeCard.tsx:440`
      ALREADY STALE at HEAD — the seal's glyph is at `PhaseNodeCard.tsx:653`
      AFTER 188.2 — it lives in `NodeCornerMarks.tsx`

    .claude/skills/sketch-findings-agentic-rag/references/icon-convention.md:69
      says  `＋` / `✕` → `WorkflowCanvas.tsx:637, :680`
      ALREADY STALE — 188.1 moved them to `PlaneEditingLayer.tsx:181, :224`
      AND Plan 02 edited exactly those elements, so this phase has no excuse to leave it

    .claude/skills/sketch-findings-agentic-rag/SKILL.md — the *Canvas Frame & Node Anatomy* row
      says  "always read `PhaseNodeCard.tsx`" for: mark 62×62 at `top:-26px`, seal `top 11
            right 17`, verdict `-left-2 top-1.5`, nothing may take `overflow:hidden`
      AFTER 188.2 — ALL FOUR of those facts left the file

    ⚠ DECIDED, AND THE ANSWER IS YES: fixing these belongs in THIS phase. The same-commit
    doc-truth rule this project applies to `Dockerfile.sandbox` ↔ `docs/SANDBOX-PACKAGES.md` and
    to the Phase-157 deploy artifacts is the same rule; a phase that moves what a pointer names
    and leaves the pointer stale ships the defect it just proved it can see
    (`feedback_load_findings_skill_not_manifest` — four icon drifts in one session came from
    exactly this). No new mark ships here; only the pointers move.

THE COUNT GATE — one upward pin move, and only one:
    scripts/vitest-count-gate.cjs:294   "PhaseNodeCard.test.tsx": 107
    scripts/vitest-count-gate.cjs:411   "WorkflowCanvas.editing.test.tsx": 58
    scripts/vitest-count-gate.cjs:470   const BASELINE_TOTAL = … // ⚠ 2482 (184.1 gap-closure)
    scripts/vitest-count-gate.cjs:473-474  TARGETS[0] === "src/components/workflows"  — a BARE
                                        DIRECTORY. Do NOT edit TARGETS (M6: new test files there
                                        run automatically; CONTEXT's "four new registrations" is
                                        wrong for this directory and right for `lib/`, `pages/`
                                        and `panel/__tests__/`).
    Read both pins from the gate's own printed `actual` column across TWO agreeing runs. Raise
    each to that actual. **A pin is LOWERED only alongside a deliberate plan-authorised DELETION**
    (`:82-84`), and nothing in this phase is a deletion — Plans 01, 03, 05 and 06 only added
    tests, and no `it()` was moved out of `PhaseNodeCard.test.tsx`. Update the `BASELINE_TOTAL`
    trailing note if the total moves.

THE TWO BOARDS, from `188.2-VALIDATION.md` § Manual-Only Verifications. Their evidence columns
are DISJOINT by construction and the header note in `188.2-UAT.md` must say so.

  BOARD A — prove UNCHANGED (lean; do not re-drive the 188 board)
    A1  the Builder card is visually identical at rest
    A2  a live run's seven readings paint the same seven shapes
    A3  a grounded + selected + verdict-carrying card is unchanged

  BOARD B — prove FIXED (BUG-260806-01) — its own evidence, never A's
    B1  the SELECTED card's ✕ is reachable — `elementFromPoint(✕ centre) === the ✕`, with
        `nodeZ` reported as "1000" so the row proves the failing state was actually reached
    B2  the DESELECTED control is still reachable — shows the variable that changed

DRIVING NOTES, measured and non-obvious:
    · `take_screenshot` TIMES OUT REPEATEDLY in this environment (`Page.captureScreenshot`).
      Read DOM geometry and attributes via `evaluate_script` / `elementFromPoint` instead —
      machine-checkable, and it does not wedge.
    · Move the PHYSICAL pointer (`hover` / `left_click_drag`) to drive a real `:hover`; synthetic
      `computer` clicks can deliver zero events
      (`reference_chrome_mcp_drive_css_hover_and_hit_test`).
    · `REVEAL_ON_HOVER` (`editAffordance.ts:123`): at ≥ 1024 px the affordances are
      `lg:opacity-0 group-hover/canvas:opacity-100`, so the pointer must be over the canvas for
      the ✕ to be revealed at all. Plan for the two-step drive — click to select, then hover to
      reveal (RESEARCH A5).
    · HARD RELOAD before the first row. Vite HMR keeps the pre-move module graph
      (`reference_hmr_provider_change_stale_code`); a row driven against a stale bundle measures
      the code this phase replaced. A driven row that passes before its fix commit was written is
      the warning sign.
    · The operator starts the dev server; never `run_in_background` it. Backend, Supabase and
      Redis are NOT needed — the Builder canvas renders from `builderStore` and needs only the
      frontend plus a saved draft.
    · App: http://localhost:5173/ · login fhdmrd@gmail.com / 123456.

SC#10 DOES NOT APPLY — state it as a decision in `188.2-UAT.md`, not as an omission. The
cross-provider roster binds phases touching streaming, the agent loop, provider routing or UI
state. This phase touched a purely presentational leaf component and a CSS stacking order.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the ledger row, correct the three stale pointers, move the pin once, and register the deferred items</name>

  <files>
CLAUDE.md
scripts/vitest-count-gate.cjs
.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md
.claude/skills/sketch-findings-agentic-rag/SKILL.md
.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-DEFERRED.md
  </files>

  <read_first>
- `CLAUDE.md` § Workflow guardrails — the whole hot-file ledger table. Read the
  `WorkflowCanvas.tsx` row first: it is the house style for a flipped row, including how it
  corrects its own stale figures ON MEASUREMENT and names the commands.
- `.planning/phases/188.2-.../188.2-06-SUMMARY.md` — the D-03 before/after table, the body range,
  the subtree total and the classifier validation. Every number in the flipped row comes from
  there, re-run rather than copied if any commit has landed since.
- `scripts/vitest-count-gate.cjs:70-130` — the LOWERED/EXTENDED doctrine in the header, and the
  `BASELINE` object literal with its per-entry inline reasons at `:286-296`.
- `.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md:62-72` — §4's canvas
  glyph table, rows `:66` and `:69`.
- `.claude/skills/sketch-findings-agentic-rag/SKILL.md` — the *Canvas Frame & Node Anatomy* row,
  found by grepping for "always read `PhaseNodeCard.tsx`".
- `.planning/phases/188.1-.../188.1-DEFERRED.md` — the house shape for a deferred register: every
  item carries a concrete re-open TRIGGER, never a vague intent.
  </read_first>

  <action>
Flip the `PhaseNodeCard.tsx` hot-file ledger row in `CLAUDE.md` from *G-5 fires — extraction due*
to *satisfied (188.2 — 2026-08-xx)*, written in the `WorkflowCanvas.tsx` row's house style. It
must carry: the measured before and after for all THREE of D-03's numbers (total, CODE, body) and
the classifier that produced them; the five modules the code went to; the commands
(`wc -l`, `git log --oneline -- <file>`, the named classifier) so a future reader can re-derive
rather than inherit; and what was PROVED rather than asserted — the rendered DOM held against a
baseline captured on the unmoved tree, the seventeen fences were re-scoped BEFORE the move, and
the third-badge invariant went from asserted-nowhere to asserted-and-observed-RED.

Correct the touch history in the same edit if the measurement disagrees with the row's "7 plans":
`git log --oneline` reports 8 commits across 4 phases because 188-06 landed twice. Say which
figure is quoted and how it was derived, exactly as the `WorkflowCanvas.tsx` row does when it
corrects itself on measurement.

Keep the row's three invariants and sharpen one of them: the third-badge typecheck error is now
mechanically asserted (188.2-01, `@ts-expect-error`, observed RED at 34) where the row previously
implied an enforcement that measurement showed did not exist. Badge slot 1 is still EMPTY and
still reserved for 189 (D-12).

Correct the three stale documentation pointers:
  · `icon-convention.md:66` — re-point `⛨` at `NodeCornerMarks.tsx` (grep for the glyph to get
    the line), and note in the row that the previous pointer was already stale by 213 lines
    before this phase moved it, so the correction is not merely a move-update.
  · `icon-convention.md:69` — re-point `＋`/`✕` at `PlaneEditingLayer.tsx` (grep for the two
    `data-testid` sites). 188.1 moved them; Plan 02 edited them; leaving the pointer stale after
    both would be indefensible.
  · `SKILL.md`'s *Canvas Frame & Node Anatomy* row — the sentence "always read
    `PhaseNodeCard.tsx`" is now false for all four geometry facts it introduces. Re-word it to
    name the card AND its sibling modules, with a per-fact pointer: the mark to `NodeIconWell`,
    the seal and the verdict to `NodeCornerMarks`, and the clipping ban to the whole subtree.
    Do not restate the geometry values from memory — grep each one and quote what the source says.

Move the count-gate pin ONCE, upward. Run `node scripts/vitest-count-gate.cjs` TWICE, read
`PhaseNodeCard.test.tsx` and `WorkflowCanvas.editing.test.tsx` from the script's own printed
`actual` column, confirm the two runs agree, and raise each pin to that measured actual with an
inline reason above the entry in the file's own annotation style. **EXTENSIONS ONLY.** Never lower
a pin; never edit `TARGETS`. Correct the `BASELINE_TOTAL` trailing note if the total moved.

Create `188.2-DEFERRED.md` in the house shape, every item carrying a concrete re-open trigger.
At minimum: the four remaining inline `hasOwnProperty` guards that were deliberately NOT
consolidated (`lib/phaseState.ts:77`, `lib/phaseGlyph.tsx:92`, `PhaseNode.tsx:165`,
`nodePresentation.ts:128`) with the trigger being the next phase that touches two of them; and
`188.1-DEFERRED.md`'s D5 restated as still deferred and still belonging to a different file.
  </action>

  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>

  <acceptance_criteria>
- `node scripts/vitest-count-gate.cjs` prints `count gate OK`, **failed 0**, no `[count-decrease]`, no `[missing-file]`, and the pinned and actual columns are EQUAL for both edited entries. Paste both runs' rows into the SUMMARY and confirm they agreed BEFORE the pins were written.
- `git diff scripts/vitest-count-gate.cjs` shows **no** edit to `TARGETS` and **no** pin lowered anywhere. `grep -n "\"PhaseNodeCard.test.tsx\":" scripts/vitest-count-gate.cjs` shows a value **greater than 107**.
- `grep -n "satisfied (188.2" CLAUDE.md` returns the flipped row, and `grep -c "G-5 fires — extraction due" CLAUDE.md` no longer counts the `PhaseNodeCard.tsx` row among its matches (`backend/app/api/threads.py` and `anthropic_service.py` still legitimately fire — confirm those are the only remaining ones and say so).
- The flipped row contains all three D-03 numbers as `before → after`, names the classifier, names the five destination modules, and names the commands. A reviewer must be able to re-derive every figure from the row alone.
- All three documentation pointers now resolve: for each, run the grep the row implies and confirm the named file and line actually contain the named glyph or fact. Paste the three greps and their output.
- `.planning/phases/188.2-.../188.2-DEFERRED.md` exists and every item carries a re-open trigger phrased as an observable condition, not an intent.
- `git diff --stat -- frontend/src backend/ supabase/migrations/ frontend/package.json` reports **0 files changed** — this task edits documentation and one build-config pin only.
  </acceptance_criteria>

  <done>The ledger row reads satisfied with measured, re-derivable figures; the three stale pointers resolve to the files that now hold what they name; one upward pin move is recorded from two agreeing gate runs with `TARGETS` untouched; and the deferred register carries concrete triggers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive Board A — prove the card UNCHANGED on the surfaces jsdom cannot see</name>

  <files>
.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-UAT.md
  </files>

  <read_first>
- `.planning/phases/188.2-.../188.2-VALIDATION.md` § Manual-Only Verifications, Board A — the
  three rows and their evidence column.
- `.planning/phases/188.2-.../188.2-03-SUMMARY.md` — the captured baselines, so the driven rows
  confirm the same surfaces the automated instruments pin rather than a different set.
  </read_first>


  <action>
Create `.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-UAT.md` with a header note naming the disjoint-evidence rule and recording the DECISION
that SC#10's cross-provider roster does not apply to this phase, with its reason. Then present
Board A's three rows below to the operator, wait for them to be DRIVEN in a real browser after a
hard reload, and transcribe each result — one row per line with `result:` and its OWN evidence,
never a reference to another row and never to Board B. Do not auto-approve, do not infer a PASS
from a green automated gate, and do not drive the browser on the operator's behalf. A row that
cannot be driven is recorded ⛔ with its reason and its blocking id, never omitted.
  </action>

  <what-built>
`PhaseNodeCard.tsx` was cut from 797 lines to its measured post-cut size, with the slot contract,
the own-property helper, the verdict mark, the ⛨ governance seal, the status ring, the pause chip
and the 3D icon well relocated into five sibling modules. Every JSX block moved verbatim; three
terse call sites render them at byte-identical positions. Three whole-`innerHTML` baselines and
two geometry matrices — all captured BEFORE the move, on the unmoved tree — hold green, and all
seventeen source fences were re-scoped to the six-file subtree before one line moved.

What automation cannot see: jsdom applies no CSS. Font rendering, real layout, the frosted-glass
treatment, the 3D mark's contact shadow, the ring's spin animation and the greyscale
shape-distinguishability of the seven readings are all invisible to it.
  </what-built>

  <how-to-verify>
This board is deliberately LEAN — three rows. Do not re-drive Phase 188's 16-row board; the phase
is behaviour-preserving by construction and the automated baselines carry the structural claim.

**Before row 1: HARD RELOAD** `http://localhost:5173/` (Ctrl+Shift+R). Vite HMR keeps the
pre-move module graph, and a row driven against a stale bundle measures the code this phase
replaced. `take_screenshot` times out repeatedly here — read DOM geometry and attributes with
`evaluate_script` instead.

**A1 — the Builder card is visually identical at rest.** Open an editable workflow draft. Look at
one phase card: the 3D mark floating above the top edge over its own contact shadow, the 248px
frosted card, the title and supporting line centred beneath it. Nothing is clipped, nothing has
shifted, no mark is missing. Evidence to record: your own confirmation, plus the
`evaluate_script` readout of the card div's computed `width`, `border-radius` and `padding`.

**A2 — a live run's seven readings paint the same seven shapes.** Run a workflow and watch the
ring through the run, or drive a run surface that exercises the readings. Confirm the readings
remain distinguishable BY SHAPE ALONE — the acceptance clause is greyscale, colour reinforces and
never carries. Confirm the running arc still spins and that a card with no reading is completely
still. Evidence: which readings you observed and what each looked like.

**A3 — a grounded, selected, verdict-carrying card is unchanged.** Find or construct a card that
is grounded (⛨ seal at top-right), selected, and carrying a verdict mark on its left edge. All
three marks are present simultaneously, the seal survives the selected border treatment, and the
verdict mark does not overlap the icon well. Evidence: your confirmation plus an
`evaluate_script` readout of the three marks' bounding boxes.

**This board's evidence may NEVER be cited for Board B, and Board B's may never be cited here.**
They are different claims about different code (`feedback_uat_rows_need_their_own_evidence` —
Phase 188 shipped a board whose header read COMPLETE while six rows read `[pending]`).

Any row that cannot be driven is recorded ⛔ with its reason and blocking id — never omitted.
  </how-to-verify>

  <resume-signal>Type "approved" once all three rows are recorded with their own evidence, or describe what looks different.</resume-signal>

  <acceptance_criteria>
- `188.2-UAT.md` exists with Board A's three rows, each carrying its OWN evidence in its own row — no row's `result` reads `[pending]` while the board's header claims completion.
- The hard reload is recorded as performed BEFORE row A1, with the reason (HMR stale-code guard).
- Each row's evidence includes at least one machine-readable `evaluate_script` readout, not only a subjective confirmation. `take_screenshot` was not relied upon.
- The board carries the header note naming the disjoint-evidence rule and stating that SC#10's cross-provider roster does not apply to this phase, with the reason.
- Any ⛔ row names its reason and its blocking id.
  </acceptance_criteria>

  <done>Board A's three rows are driven and recorded with their own evidence, the reload guard is documented, and the board states its own scope decision rather than leaving the absent cross-provider roster to read as an omission.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Drive Board B — prove BUG-260806-01 FIXED in the state that was broken</name>

  <files>
.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-UAT.md
  </files>

  <read_first>
- `.planning/reported-bugs/BUG-260806-01-selected-card-occludes-its-own-remove-control.md` — the
  measured hit-test table and the fact that the distinguishing variable is SELECTION, not position.
- `.planning/phases/188.2-.../188.2-02-SUMMARY.md` — the unit half already discharged, and the
  measured `style.zIndex` of a selected node wrapper.
- `.planning/phases/188.2-.../188.2-VALIDATION.md` § Board B — the two rows and their evidence
  column.
  </read_first>


  <action>
Append Board B's two rows to `.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-UAT.md` under their own heading, with an evidence column that is
DISJOINT from Board A's. Present them to the operator, wait for them to be DRIVEN in a real
browser after a hard reload, and transcribe each result with its own `elementFromPoint` return
value and both z-index readings. Then, ONLY IF both rows passed, update
`.planning/reported-bugs/BUG-260806-01-selected-card-occludes-its-own-remove-control.md`
frontmatter to `status: closed` with `verified_closed_by: 188.2` and append a short
"how it was verified" note naming the driven row and the unit guard. If either row is ⛔, leave
the status at `folded` and say so plainly in the SUMMARY. Do not auto-approve, do not close the
report on the strength of the stacking analysis, and do not drive the browser on the operator's
behalf.
  </action>

  <what-built>
All three editing-affordance groups in `PlaneEditingLayer.tsx` — the `＋` buttons, the `✕`
buttons and the insert picker — now carry `zIndex: AFFORDANCE_Z` (1002), which is above
`@xyflow`'s `SELECTED_NODE_Z` (1000). The portal container carries no `z-index`, so a positioned
child escapes into the viewport's stacking context and wins outright. A three-part unit assertion
guards the relation, the library's own constant and the rendered wiring on every build, and
`AFFORDANCE_SHAPE_BASELINE` was re-captured as the phase's one authorised exception.

What automation cannot see: **occlusion.** jsdom has no layout and computes no stacking contexts,
and `.click()` dispatches directly to an element, bypassing hit-testing entirely. That is exactly
why this defect survived from 184-12 until an operator found it by hand during 188.1's UAT — and
why the unit assertion, however good, cannot close it alone.
  </what-built>

  <how-to-verify>
**Before row B1: HARD RELOAD** `http://localhost:5173/` (Ctrl+Shift+R). A row driven against the
pre-fix bundle proves nothing.

Open an editable workflow draft with at least two steps.

**B1 — the SELECTED card's `✕` is reachable. THIS IS THE WHOLE ROW.**
  1. **Click step 2's card so it is SELECTED.** Without this the row proves nothing — deselected
     already passed BEFORE the fix, and selection is the distinguishing variable in the report's
     own measured table.
  2. Move the **physical** pointer over the canvas. `REVEAL_ON_HOVER` (`editAffordance.ts:123`)
     hides the affordances at ≥ 1024 px until the canvas is hovered, and a synthetic click can
     deliver zero events. Plan for the two-step drive: click to select, then `hover` /
     `left_click_drag` to reveal.
  3. With `evaluate_script`: find step 2's `[data-testid^="canvas-remove-"]` element, take its
     bounding rect, and call `document.elementFromPoint` at the rect's centre.
  4. **PASS ⇔ the returned element IS the `✕` button**, AND the selected node's computed
     `zIndex` reads `"1000"` — that second reading is what proves the card really was in the
     failing state rather than in a state that always worked.
  5. Record the returned element, its class list, the node's `zIndex` and the `✕`'s `zIndex`.

**B2 — the DESELECTED control is still reachable.** Deselect, repeat the identical hit test, and
record that it passes with the node's `zIndex` no longer 1000. This row exists to show the
variable that changed, which is what makes B1 a comparison rather than a single observation.

**Board B's evidence may NEVER be cited for Board A, and Board A's may never be cited here.**

If either row cannot be driven, record it ⛔ with its reason and blocking id, and do NOT flip the
bug report to `closed` — the analysis alone is explicitly not sufficient (RESEARCH rates the
fix's SUFFICIENCY at MEDIUM until this row is driven).
  </how-to-verify>

  <resume-signal>Type "approved" once both rows are driven and recorded with their own `elementFromPoint` and z-index readings, or paste what `elementFromPoint` actually returned.</resume-signal>

  <acceptance_criteria>
- `188.2-UAT.md` carries Board B's two rows with per-row evidence: the `elementFromPoint` return value, the selected node's computed `zIndex` and the `✕`'s computed `zIndex` for EACH row. No row's `result` reads `[pending]`.
- B1's evidence shows the node's `zIndex` as `"1000"` at the moment of the passing hit test — without that reading the row does not prove the failing state was reached.
- The hard reload is recorded as performed before B1, and the two-step select-then-hover drive is described as it was actually performed.
- ONLY IF both rows passed: `.planning/reported-bugs/BUG-260806-01-...md` frontmatter is updated to `status: closed` with `verified_closed_by: 188.2`, and a short "how it was verified" note is appended naming the driven row and the unit guard. If either row is ⛔, the status stays `folded` and the SUMMARY says so plainly.
- `git diff --stat -- frontend/src` reports **0 files changed** by this task — driving a board changes no code.
  </acceptance_criteria>

  <done>The selected-state hit test was driven and returned the `✕` with the node measurably at z-index 1000, the deselected control was driven as its own row, and the bug report is closed only on that evidence — never on the analysis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a shipped fix → the evidence it is fixed | The one thing that can see this defect class is a real browser hit test. Everything else in the estate is structurally blind to it, and a green suite is not evidence. |
| the ledger → the next phase's scope | Phase 189's discuss-phase reads this row to decide whether it owes a refactor recommendation. A row flipped without measured numbers hands 189 a claim rather than a fact. |
| documentation pointer → the source it names | A pointer that names a file which no longer holds what it names teaches the wrong vocabulary to whoever builds from it — the failure mode `icon-convention.md` §4 was created to stop. |
| a board's header → its rows | Phase 188 shipped a board whose header read COMPLETE while six rows read `[pending]`. A per-row evidence column is the structural fix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188.2-29 | Repudiation | `BUG-260806-01`'s closure | mitigate | The report flips to `closed` ONLY on B1's driven evidence with the node measured at z-index 1000. RESEARCH rates the fix's sufficiency MEDIUM until that row is driven; the stacking analysis alone does not close it |
| T-188.2-30 | Repudiation | the two UAT boards' evidence | mitigate | Disjoint evidence columns, a header note naming the rule, and a per-row `result` that cannot read `[pending]` under a completed header. ⛔ rows are recorded with a reason, never omitted |
| T-188.2-31 | Spoofing | a row driven against a stale bundle | mitigate | A hard reload is required and recorded before the first row of each board; a row that passes before its fix commit exists is named as the warning sign |
| T-188.2-32 | Tampering | the count-gate pin | mitigate | EXTENSIONS ONLY, read from the gate's own `actual` column across two agreeing runs, with `TARGETS` untouched. A pin is lowered only alongside a plan-authorised deletion, and this phase deleted no test |
| T-188.2-33 | Repudiation | the flipped ledger row | mitigate | Carries all three D-03 numbers as before → after, the classifier that produced them, and the commands, so a future reader re-derives rather than inherits — the `WorkflowCanvas.tsx` row's own house style |
| T-188.2-SC | Tampering | npm / pip / cargo installs | mitigate | Zero packages installed across the whole phase. Hard gate: `git diff --stat -- frontend/package.json frontend/package-lock.json` reports 0 files changed |
</threat_model>

<verification>
- `node scripts/vitest-count-gate.cjs` → `count gate OK`, failed 0, pinned == actual for both edited entries, no `[count-decrease]`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → exactly 33
- `cd frontend && npx eslint src/components/workflows/` → exactly 5 errors, 0 warnings
- `grep -n "satisfied (188.2" CLAUDE.md` → the flipped row
- `188.2-UAT.md` → 5 rows across two boards, every one with its own evidence, none `[pending]` under a completed header
- `git diff --stat -- frontend/src backend/ supabase/migrations/ frontend/package.json` → 0 files changed
</verification>

<success_criteria>
1. The `PhaseNodeCard.tsx` ledger row reads *satisfied (188.2)* and carries all three D-03 numbers as before → after with the classifier and the commands that produced them.
2. All three stale documentation pointers resolve to the files that now hold what they name, each verified by a grep pasted into the SUMMARY.
3. One upward pin move, read from two agreeing gate runs, with `TARGETS` untouched and no pin lowered.
4. Board A's three rows and Board B's two rows are each driven with their OWN evidence in disjoint columns; no row reads `[pending]`.
5. B1 was driven with the card SELECTED and the node measured at z-index 1000 — the failing state, not one that always worked.
6. `BUG-260806-01` is `closed` with `verified_closed_by: 188.2` only if both Board B rows passed; otherwise it stays `folded` and the SUMMARY says so.
7. `188.2-DEFERRED.md` exists with a concrete re-open trigger per item, and SC#10's non-application is stated as a decision.
</success_criteria>

<output>
Create `.planning/phases/188.2-phasenodecard-extraction-refactor-pay-down-the-g-5-debt-on-p/188.2-07-SUMMARY.md` when done. Record verbatim: both count-gate runs' rows and the two pin values written; the flipped ledger row's text; the three pointer-verification greps and their output; every Board A and Board B row with its own evidence including B1's `elementFromPoint` return value and both z-index readings; and the final state of `BUG-260806-01`'s frontmatter with the reason.
</output>
