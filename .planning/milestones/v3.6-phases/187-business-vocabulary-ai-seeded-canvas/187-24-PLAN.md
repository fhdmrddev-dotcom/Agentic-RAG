---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 24
type: execute
wave: 11
depends_on: ["187-23"]
gap_closure: true
files_modified:
  - frontend/src/components/workflows/phaseVocabulary.ts
  - frontend/src/components/workflows/phaseVocabulary.test.ts
  - frontend/src/components/workflows/SeedReceipt.tsx
  - frontend/src/components/workflows/SeedReceipt.test.tsx
  - frontend/src/components/workflows/definitionOps.test.ts
autonomous: true
requirements: [VOCAB-02, VOCAB-01]

# WAVE 11, behind 187-23. Both plans edit `SeedReceipt.test.tsx` and `definitionOps.test.ts`; two
# executors cannot own those files in one wave. VOCAB-01 is carried here because Task 1 genuinely
# edits `phaseVocabulary.ts` — VOCAB-01's one-home vocabulary module, guarded by `?raw` source
# checks in two sibling suites — and those guards must be re-run rather than assumed.

must_haves:
  truths:
    - "D-187-08 — the receipt's docblock claim that there is no second derivation to drift becomes TRUE by construction: the `available_tools ∩ kbTools` membership rule has exactly one home, and the receipt imports it rather than re-implementing it"
    - "The tool named inside a `detected` reason can no longer disagree with the classifier that decided the cause, because both read one predicate"
    - "The WR-09 fence stops asserting the absence of strings nothing says: it carries a positive control over its own needles and checks the PROPERTY (no causal attribution) rather than one historical wording"
    - "The testid-coverage guard measures COVERAGE rather than a mention: it sees every JSX spelling of the attribute, is not satisfied by a commented-out query, sweeps the sibling `data-*` state attributes `data-carried-count` belongs to, and has its own planted-literal control"
    - "A deny-list is never extended to make it fail-closed — the Phase-185 lesson is applied by adding controls and widening to the property, not by adding needles"
    - "VOCAB-01 is not disturbed: `nodeTitle`'s three tiers and the one-copy `?raw` guards in the spine suites are re-run and green"
    - "No false completion record is written: `requirements.mark-complete`, `state.advance-plan` and `roadmap.update-plan-progress` are not called by any task in this plan"
  artifacts:
    - path: "frontend/src/components/workflows/phaseVocabulary.ts"
      provides: "the exported first-intersecting-KB-tool resolution, sitting with the classifier that owns the membership rule"
      contains: "kbTools"
    - path: "frontend/src/components/workflows/SeedReceipt.tsx"
      provides: "a component that holds no derivation at all and a docblock whose claim the code now supports"
      contains: "groundingCauseOf"
    - path: "frontend/src/components/workflows/definitionOps.test.ts"
      provides: "the WR-09 fence with a positive control over its own needles and a property assertion"
      contains: "POSITIVE CONTROL"
    - path: "frontend/src/components/workflows/SeedReceipt.test.tsx"
      provides: "a testid/state-attribute coverage sweep that sees every spelling, ignores comments, and carries a planted-literal control"
      contains: "data-carried-count"
  key_links:
    - from: "frontend/src/components/workflows/SeedReceipt.tsx"
      to: "the one membership rule in `phaseVocabulary.ts`"
      via: "a direct import beside `groundingCauseOf`, with no local predicate left behind"
      pattern: "from \"@/components/workflows/phaseVocabulary\""
---

<objective>
Retire the second derivation, and make round 3's own two guards measurements instead of gestures.

Purpose: three separate warnings share one root. `SeedReceipt.tsx`'s docblock tells the next reader
that "there is no second derivation to drift" while the file re-implements the
`available_tools ∩ kbTools` membership predicate that `groundingCause` owns — they agree today and
would disagree the day the rule stops being exact string equality, at which point the reason names a
tool the classifier did not count. And both of round 3's new guards shipped without the controls
their own files mandate for every other fence: the WR-09 fence is a deny-list of the exact clause
that was deleted, so every needle in it now matches nothing anywhere in the product; the testid
sweep is satisfiable by a comment, blind to two other JSX spellings, and blind to the whole `data-*`
class the `data-carried-count` it shipped alongside belongs to.

The project has already recorded the lesson these three share, twice: a deny-list cannot be made
fail-closed by extension (Phase 185), and verify the property, not the patch (Phase 187).

Output: one predicate with one home, and two guards that have been observed biting.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-REVIEW.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VERIFICATION.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: One membership rule, one home — move the KB-tool resolution to the classifier that owns it (WR-14)</name>
  <files>frontend/src/components/workflows/phaseVocabulary.ts, frontend/src/components/workflows/SeedReceipt.tsx, frontend/src/components/workflows/phaseVocabulary.test.ts, frontend/src/components/workflows/SeedReceipt.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/SeedReceipt.tsx` — `intersectingKbTool` (around `:170-180`)
      and its docblock, the `rows` `useMemo` that calls it one line after `groundingCauseOf`
      (around `:193-206`), and the module docblock's "IT RENDERS A REASON, IT NEVER DECIDES ONE"
      section containing the sentence "there is no second derivation to drift" (around `:20-35`).
      That sentence is the claim this task makes true.
    - `frontend/src/components/workflows/phaseVocabulary.ts` — `GroundingCause` (`:268`),
      `GroundingInputs`, `groundingCause` (`:311-332`) and `groundingCauseOf` (`:343-356`).
      Note precisely how `groundingCauseOf` reads `available_tools`: it filters to strings and does
      NOT optional-chain `phase.config`. The moved function must behave identically on the same
      inputs, and the review notes the receipt's defensive `?.` is theatre because
      `groundingCauseOf` is called one line earlier without it — decide deliberately and say which
      guard survives.
    - `frontend/src/components/workflows/phaseVocabulary.test.ts` — the `phaseVocabularySource`
      `?raw` import and the shape of the existing `groundingCause` cases, so the new tests read like
      their neighbours.
    - `frontend/src/components/workflows/SeedReceipt.test.tsx` — the source-fence block near the end
      (the KB-tool-id fence that asserts no tool id is spelled anywhere in the component, and
      `it("those fences are real")`), because the new source fence joins that block and must carry a
      planted literal like every other pattern there.
    - `frontend/src/components/workflows/PhaseSpineGraph.test.tsx` and
      `frontend/src/components/workflows/PhaseSpine.test.tsx` — their `?raw` checks that exactly one
      copy of the vocabulary survives. Read them before editing `phaseVocabulary.ts` so you know
      what an added export may and may not look like.
  </read_first>
  <action>
    Move the intersection resolution out of `SeedReceipt.tsx` and into `phaseVocabulary.ts`, placed
    directly beneath `groundingCauseOf` so the rule and its consumer sit together. Export it under a
    name in that module's `-Of` convention for phase-shaped adapters. Its contract: given a phase and
    the server's KB tool list, return the FIRST tool the step reaches for by the same membership rule
    `groundingCause`'s detected branch uses, or `null`.

    "The same membership rule" is the deliverable, not a comment about it. Read `available_tools` the
    way `groundingCauseOf` does — the same defensive string filter — so the two cannot disagree about
    what counts as a tool, and the day the rule stops being exact string equality there is exactly
    one place to change. Write the docblock to say that in one sentence and to name D-187-08.

    Delete the local copy from `SeedReceipt.tsx` and import the new function beside `groundingCauseOf`
    and `nodeTitle`. The component must then declare no predicate of its own. Repair the module
    docblock: the "no second derivation to drift" sentence becomes true, so state WHY it is true now
    (the predicate moved, it did not merely agree) rather than leaving a claim that reads as
    aspiration.

    Add cases to `phaseVocabulary.test.ts` covering: the first-match rule when several tools
    intersect and the step's own ordering decides; a non-KB tool listed FIRST (the case that catches
    anything naming the head of the list instead of the intersection); a miss returning `null`; and
    the malformed shapes the loose definition JSONB admits — `available_tools` absent, not an array,
    and holding non-string members — none of which may throw. Assert the agreement property
    directly: over a table of phases, whenever `groundingCauseOf` returns `detected` the resolver
    returns a non-null tool, and whenever it does not return `detected` because of tools, the
    resolver returns `null`. One assertion of agreement is worth more than two parallel copies of
    the same expectations.

    Add a source fence to `SeedReceipt.test.tsx`'s existing fence block: the component's `?raw`
    source contains no membership predicate of its own. Assemble the needles from parts in that
    file's shipped idiom so the guard cannot satisfy itself, and add its planted literal to
    `it("those fences are real")` so the pattern is proved capable of firing.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts src/components/workflows/phaseVocabulary.corpus.test.ts src/components/workflows/SeedReceipt.test.tsx src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseSpine.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The run exits 0 with zero failures, and no pinned file's count decreases against numbers
      re-measured at HEAD immediately before this task.
    - `grep -nE "kbTools\.(includes|indexOf)|includes\(tool\)" frontend/src/components/workflows/SeedReceipt.tsx`
      returns nothing — the component holds no membership test.
    - The moved function is exported from `phaseVocabulary.ts` exactly once, and
      `grep -c "<its name>" frontend/src/components/workflows/SeedReceipt.tsx` shows it is imported
      and called, not redeclared.
    - Falsification, observed and recorded: change the moved function's membership test to be
      case-insensitive while leaving `groundingCause`'s alone, and re-run — the agreement case must
      fail. This proves the agreement assertion is real and not a restatement. Revert cleanly and
      prove it with `git diff --stat`.
    - Falsification, observed and recorded: re-declare a local membership predicate inside
      `SeedReceipt.tsx` and re-run — the new source fence must fail. Revert cleanly and prove it.
    - The receipt's rendered behaviour is unchanged: every shipped `SeedReceipt.test.tsx` case that
      asserts a `detected` reason naming a tool still passes without edit.
    - `npx tsc --noEmit` reports no new error against the recorded `D-ITEM-01` baseline of 33
      (record before and after).
    - `git diff --name-only -- backend/ supabase/migrations` is empty.
    - No SDK completion verb was called.
  </acceptance_criteria>
  <done>The membership rule has one home, the receipt holds no derivation, the docblock's claim is true by construction, and both halves are proved by an observed falsification.</done>
</task>

<task type="auto">
  <name>Task 2: Give the WR-09 fence a positive control and a property (WR-12)</name>
  <files>frontend/src/components/workflows/definitionOps.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/definitionOps.test.ts` — the WR-09 fence
      (`it("the carried sentence ATTRIBUTES NO CAUSE — it counts two of them (187-20/WR-09)")`,
      around `:838-855`) and, nineteen lines below it, its sibling
      `it("the carried sentence CLAIMS NO AUTHORSHIP — with a positive control")`, which ends with
      the control the first one lacks. That sibling is the shape to match; read both before editing
      either. Note that 187-23 has already edited this file — re-read at HEAD, do not work from the
      line numbers in this plan.
    - `frontend/src/components/workflows/definitionOps.ts` — `seedReceiptCarriedLead` and its
      property-2 docblock, so the property you assert is the one the module actually promises.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-REVIEW.md` — WR-12 in full,
      including its measurement that both deleted-clause needles now match nothing anywhere in
      `frontend/src` except comments.
  </read_first>
  <action>
    Re-derive WR-12's measurement first rather than inheriting it: run the grep for the deleted
    clause over `frontend/src` and record what it returns at HEAD. If the needles really do match
    nothing but prose, the fence as written is asserting the absence of strings the product cannot
    produce, and that is what the repair addresses. Record the raw output.

    Repair the fence in two ways, both required:

    Give it a POSITIVE CONTROL over its OWN needles, in the sibling's shape: construct a sentence
    that genuinely carries a cause claim and assert the same needles DO match it, so the absences
    below are measurements rather than tautologies. Keep the assemble-from-parts idiom so a grep of
    the guard cannot satisfy the guard.

    Then make the fence check the PROPERTY the module promises rather than one historical wording:
    the carried sentence attributes no cause at all, so no causal connective may appear in it. A
    deny-list of wordings is extended forever and is fail-open by construction (the recorded
    Phase-185 lesson); a word-class assertion over causal connectives is a property. Keep the two
    historical needles as well — they are cheap regression pins for the exact clause that shipped —
    but they are no longer the fence.

    Keep the existing assertions that the sentence borrows neither per-step reason. Those compare by
    identity against the exports and are correct; 187-23 changed one of those sentences, so re-run
    and confirm they still hold rather than assuming.

    Do not touch `seedReceiptCarriedLead`'s returned strings. This task hardens the guard over a
    closed fix; it does not reopen the fix.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/definitionOps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The run exits 0 with a count strictly greater than the number re-measured at HEAD immediately
      before this task.
    - The positive control is inside the same `it` as the absences it justifies, so a future deletion
      of the control cannot leave the absences standing alone.
    - Falsification, observed and recorded: re-append a DIFFERENTLY-worded cause clause to
      `seedReceiptCarriedLead` — one the two historical needles do NOT contain, e.g. a "because"
      form — and re-run. The new property assertion must fail while the two historical needles pass,
      which is precisely the gap WR-12 reports. Revert cleanly and prove it with `git diff --stat`.
    - The raw grep output for the historical clause over `frontend/src` at HEAD is recorded in the
      SUMMARY, with its hit count and whether each hit is prose or a rendered string.
    - `git diff --name-only -- backend/ supabase/migrations` is empty.
    - No SDK completion verb was called.
  </acceptance_criteria>
  <done>The WR-09 fence measures a property, carries a control over its own needles, and has been observed failing on a wording it could not previously see.</done>
</task>

<task type="auto">
  <name>Task 3: Make the coverage sweep measure coverage (WR-13)</name>
  <files>frontend/src/components/workflows/SeedReceipt.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/SeedReceipt.test.tsx` — the sweep
      (`it("every STATIC testid the component renders is queried by this suite (187-20)")`, around
      `:911-936`) and `it("those fences are real — each pattern matches its planted literal")`
      immediately after it, which plants a literal for every other pattern in the block including a
      deliberate non-firing control. The sweep is the only fence there with no entry. Re-read at
      HEAD: 187-22 and Task 1 of this plan have both already edited this file.
    - `frontend/src/components/workflows/SeedReceipt.tsx` — the rendered attributes, so you can
      enumerate what the sweep must see: the static `data-testid` strings, the template-literal row
      testid, and the sibling state attributes `data-grounded-count`, `data-detected-count`,
      `data-carried-count`, `data-slug`, `data-cause`.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-REVIEW.md` — WR-13's four
      measured probes and its proposed widening. The probes are the acceptance bar: reproduce them
      rather than trusting the table.
  </read_first>
  <action>
    First reproduce WR-13's four probes against the sweep AS IT STANDS, and record each result: a
    brand-new unqueried `data-testid` (should fail the sweep — the direction it was written for),
    the two brace spellings `data-testid={"..."}` and `data-testid={'...'}` (reported invisible), and
    an id whose only query site is inside a comment (reported passing). Only mutate the component
    source temporarily to run these, and revert cleanly. A repair aimed at a defect you have not
    reproduced is a patch aimed at a report.

    Then widen the sweep in three directions, each closing one probe:

    Extraction — match every spelling JSX admits for the attribute, not only the double-quoted form,
    so a brace-wrapped id cannot pass unseen.

    Comments — strip block and line comments from the suite's own `?raw` source before the substring
    check, so a commented-out or skipped query no longer satisfies the guard. State in a comment
    that the guard measures COVERAGE and no longer a mention, since the old docblock claimed more
    than the code did.

    The sibling class — sweep the `data-*` state attributes too. `data-carried-count` shipped in the
    same plan as this guard and is exactly the class of surface hook the guard exists for, yet it is
    invisible to a `data-testid`-only sweep. Assert each such attribute is queried somewhere in the
    suite. Exclude `data-testid` itself so the two halves do not double-count.

    Then add the missing entries to `it("those fences are real")`: a planted literal proving the
    widened extraction fires on an unqueried id, and one proving the comment-stripping actually
    removes a commented query. Follow the block's existing idiom, including its practice of planting
    a deliberate NON-firing control beside a firing one.

    Keep the two non-vacuity assertions the sweep already has (a non-empty extraction, and
    `seed-receipt-carried` named explicitly) — they are cheap and they stop the sweep passing over
    an empty set.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The run exits 0 with a count strictly greater than the number re-measured at HEAD immediately
      before this task.
    - All four original probes are reproduced against the OLD sweep and recorded with their raw
      results, then re-run against the NEW sweep and recorded again. The new sweep must FAIL on all
      four mutations; the old one is expected to fail on only the first. Every mutation is reverted
      cleanly and the revert proved with `git diff --stat`.
    - A fifth probe is run and recorded: add a new sibling state attribute to the component that no
      test queries (e.g. a `data-*` count) — the widened sweep must fail on it. Revert cleanly.
    - `it("those fences are real")` gains an entry for each new pattern, and each is exercised in
      the passing run.
    - The template-literal row testid remains explicitly acknowledged rather than silently
      unswept — the existing assertion covering it still passes.
    - `git diff --name-only -- backend/ supabase/migrations` is empty.
    - No SDK completion verb was called.
  </acceptance_criteria>
  <done>The sweep sees every spelling, ignores comments, covers the sibling attribute class, and has been observed failing on all five probes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server `kb_tools` (`workflows.py:745`) → the membership rule → the tool named in a reason | The safety-defining input is the server's. The rule that reads it is client-side and, today, written twice. Two copies of one server-owned rule is the boundary this plan collapses. |
| test guard → the property it claims to hold | A guard is itself a trust boundary: everything downstream treats a green suite as evidence. A guard that cannot fail transfers no trust, and both of round 3's new guards were shown to transfer less than they claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-R4-10 | Tampering | the duplicated `available_tools ∩ kbTools` predicate | mitigate | Two derivations of one server-owned rule can disagree the moment the rule stops being exact string equality, and the receipt would then name a tool the classifier did not count — a reason that contradicts its own cause, on the governance surface. Closed by moving the predicate to the classifier's module (Task 1) and pinned by an agreement assertion plus a source fence, both observed failing. |
| T-187-R4-11 | Repudiation | the WR-09 deny-list fence | mitigate | A fence whose every needle matches nothing in the product reports a property it does not measure, so a future differently-worded cause attribution ships green. Closed by a positive control over its own needles plus a word-class property assertion, observed failing on a wording the deny-list cannot see. |
| T-187-R4-12 | Repudiation | the testid-coverage sweep | mitigate | Satisfiable by a comment and blind to two JSX spellings and to the whole sibling `data-*` class — so the exact regression class it was written for (an unqueried surface hook) can recur beside it. Closed by widening extraction, stripping comments, sweeping the sibling class, and adding the planted-literal control every other fence in the block has. Five probes are the acceptance bar. |
| T-187-R4-13 | Denial of Service | `phaseVocabulary.ts` as VOCAB-01's one-home module | accept | Editing the vocabulary module risks VOCAB-01's shipped one-copy `?raw` guards in the two spine suites. Accepted with a control: those suites are inside Task 1's verify command and must stay green, and the change is purely additive — no existing export's behaviour is touched. |
| T-187-R4-SC | Tampering | npm / pip / cargo installs | accept | No package-manager install and no new dependency in this plan; the Package Legitimacy Gate is not engaged. Adding a dependency re-opens it and requires the audit table plus a blocking human checkpoint before install. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/SeedReceipt.test.tsx src/components/workflows/definitionOps.test.ts src/components/workflows/phaseVocabulary.test.ts src/components/workflows/phaseVocabulary.corpus.test.ts src/components/workflows/StepTypePicker.test.tsx` — the five-suite named set from `187-VALIDATION.md`; exit 0 and a total strictly greater than the figure re-measured at HEAD before this plan.
- `cd frontend && npx vitest run src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseSpine.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx` — exit 0; the one-copy vocabulary guards and 187-22's CR-04 cases stay green.
- `cd frontend && npx tsc --noEmit` — no new error against the recorded `D-ITEM-01` baseline of 33.
- Nine observed falsifications across the three tasks (two in Task 1, one in Task 2, five in Task 3), each with raw failure output and a clean revert.
- `git diff --name-only -- backend/` and `git status --porcelain supabase/migrations` — both empty.
- ⚠ Never gate on the full frontend suite. `vitest` is 4.1.0: no `--reporter=basic`.
</verification>

<success_criteria>
- The `available_tools ∩ kbTools` rule exists once, in the module that owns the classification, and the receipt's docblock claim is supported by the code rather than by hope.
- The WR-09 fence carries a control over its own needles and asserts a property, and has been observed failing on a wording its deny-list could not see.
- The coverage sweep fails on all five probes, including the two spellings and the commented query it previously passed, and has a planted literal like every other fence in its block.
- VOCAB-01's one-copy guards and the shipped receipt behaviour are unmoved, proved by re-running the owning suites.
- Zero migrations, zero backend files, zero SDK completion verbs.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-24-SUMMARY.md` when done.

It MUST carry: the re-derived WR-12 grep output at HEAD; all four WR-13 probes measured against the
OLD sweep and again against the NEW one, plus the fifth sibling-attribute probe; the Task-1
agreement and source-fence falsifications; per-file counts re-measured before and after each task;
the `tsc --noEmit` before/after; and every revert proved with `git diff --stat`. State plainly which
guard failed on which probe — a table of intentions is not a table of measurements.
</output>
</content>
