---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 12
type: execute
wave: 3
depends_on: ["187-04", "187-08"]
files_modified:
  - frontend/src/components/workflows/__fixtures__/canvasFixtures.ts
  - frontend/src/components/workflows/phaseVocabulary.corpus.test.ts
autonomous: true
requirements: [VOCAB-01]

must_haves:
  truths:
    - "D-187-16 — a 'face' is the string nodeTitle() returns; SC#5 is asserted as a PURE-FUNCTION sweep over the corpus, never a DOM scrape"
    - "SC#5 check 1 — across the corpus, zero reveal-OFF faces contain a slug or a raw phase_type token"
    - "D-187-15 — SC#5 check 2 is NARROWED to 'no two steps with MATERIALLY DIFFERENT config render identical faces'; plan_execute_verify's two bare llm_single steps are the documented exception, recorded with their measured shape"
    - "The corpus is the checked-in canvasFixtures.ts module — 3 curated starters + the 4 canonical seeds + the PM pack — extended, never transcribed a second time"
    - "Every new fixture field carries its own file:line JSDoc source line, so the module's own 'names a source' test stays green"
  artifacts:
    - path: "frontend/src/components/workflows/__fixtures__/canvasFixtures.ts"
      provides: "skill_ref / folder_scope / template-asset coverage on the existing corpus"
    - path: "frontend/src/components/workflows/phaseVocabulary.corpus.test.ts"
      provides: "the SC#5 check-1 and check-2 sweeps"
  key_links:
    - from: "frontend/src/components/workflows/phaseVocabulary.corpus.test.ts"
      to: "__fixtures__/canvasFixtures.ALL_FIXTURES"
      via: "describe.each over the shipped corpus"
      pattern: "ALL_FIXTURES"
---

<objective>
Make SC#5's two checks falsifiable over the named corpus, as pure-function assertions.

Purpose: the ROADMAP's acceptance bar is the PM pack + the Starter Library + all four canonical seed
shapes — *"no jargon leak, no over-simplification (not a toy demo)"*. Both halves are testable, but
only if "face" is defined precisely. Measured, `PhaseSpineGraph.tsx:184` prints the raw `phase_type` in
a mono chip unconditionally and `:164`'s `aria-label` carries it too, so a DOM-scrape reading of check 1
fails 100% on the spine today and could only pass by deleting shipped chrome. D-187-16 settles it: the
face is the string `nodeTitle()` returns.

Output: the extended corpus fixtures and the two sweeps.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-SPEC.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-PATTERNS.md
</context>

<interfaces>
```ts
// frontend/src/components/workflows/__fixtures__/canvasFixtures.ts:286-360 (shipped)
// ALL_FIXTURES, in order — THE CORPUS SC#5 NAMES ALREADY EXISTS:
//   research_summarize / plan_execute_verify / literature_review / doc_qa_human   (the 4 canonical seeds)
//   risk-register / weekly-status-report / compliance-gap-report                   (the 3 Starter Library rows)
//   pm-weekly-status-report / pm-risk-register                                     (the PM pack)
//   eval_coverage / empty draft / single phase / branching / unresolvable skip / non-contiguous phase_index
//
// Header rule (:12-19): TRANSCRIBED, NEVER READ LIVE. Every entry is copied from a CHECKED-IN
// artifact and carries a JSDoc line naming that artifact by `file:line`. This module performs no I/O.
// It transcribes only slug, phase_index, config.phase_type, config.citation_policy and validators[]
// (:28-32) — it deliberately OMITS skill_ref, folder_scope and template assets.
```

```ts
// frontend/src/components/workflows/phaseVocabulary.ts (from plan 187-04)
export function nodeTitle(phase: PhaseSpecJSON, ctx?: NameContext): string
export function technicalTitle(phase: PhaseSpecJSON): string
export interface NameContext { folderNames?; skillNames?; templateFilename? }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the corpus fixtures with the fields the derived tier reads</name>
  <files>frontend/src/components/workflows/__fixtures__/canvasFixtures.ts</files>
  <read_first>
    - `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts:1-60` — the header's TRANSCRIBED-NEVER-READ-LIVE rule, the "names a source" requirement, and the explicit statement at `:28-32` of which fields are transcribed and which are omitted.
    - `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts:53-66` — `plan_execute_verify`, the D-187-15 exception, whose `plan` and `verify` are both bare `llm_single`.
    - `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts:286-360` — `ALL_FIXTURES` and the per-entry `source` field.
    - `frontend/src/components/workflows/canvasModel.fixtures.test.ts:17-90` — the corpus self-check (`registers at least 14 fixtures`, `names a source for every fixture`) and the `describe.each` sweep idiom.
    - `backend/tests/conftest.py:867-890` — `four_seed_defs()`'s `plan_execute_verify`, verbatim, to cite as the exception's source.
    - `backend/app/models/harness.py:255-282` — `AssetRef` (`asset_id`, `filename`, `kind: Literal["template","reference"]`, `mime`) and `WorkflowDefinition.assets`.
  </read_first>
  <action>
The derived tier reads exactly the fields this fixture module deliberately omits. Extend it — do not
transcribe a second corpus.

For the three curated starters (`risk-register`, `weekly-status-report`, `compliance-gap-report`), add
the definition-level template asset each one carries. Measured live on `:54322`, each holds exactly one
`{kind: "template", filename: "<slug>.docx", …}`. Add it as a definition-level field on the fixture
entry (the fixtures are phase lists today; add a sibling optional field rather than smuggling it into a
phase), so the corpus test can build the `NameContext.templateFilename` from it.

Add at least two fixture entries — or extend two existing ones — covering a `config.skill_ref` and a
single-entry `config.folder_scope`, so the skill and folder tiers are exercised over the corpus rather
than only in unit tests. Prefer extending an existing PM-pack or starter entry over inventing a new
workflow; if a new entry is needed, mark its `source` explicitly as hand-authored, test-only.

**Every new field carries its own JSDoc `file:line` source line.** The module's own "names a source for
every fixture" test enforces `/:\d+|hand-authored, test-only/`, and the header rule exists because two
live database reads on the same day disagreed — a snapshot test whose input drifts is not a gate.

Update the header's "what is transcribed" paragraph at `:28-32` so it no longer says these fields are
omitted. A comment that still names an omission the module now includes is the same defect as a false
docblock.

Do NOT read the live database. Every value comes from a checked-in artifact — the migration files, the
`conftest.py` seed helper, or the recorded measurements in `187-RESEARCH.md` §"The template door" — and
is cited as such.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/canvasModel.fixtures.test.ts src/components/workflows/canvasModel.roundtrip.test.ts src/components/workflows/canvasModel.test.ts src/components/workflows/canvasModel.purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/canvasModel.fixtures.test.ts src/components/workflows/canvasModel.roundtrip.test.ts src/components/workflows/canvasModel.test.ts src/components/workflows/canvasModel.purity.test.ts` passes; the fixtures suite's "names a source for every fixture" test is green.
    - `git status --porcelain frontend/src/components/workflows/__snapshots__` is empty — adding config fields must not move the projection snapshot (the derived tier is inert without a context).
    - `grep -c "skill_ref" frontend/src/components/workflows/__fixtures__/canvasFixtures.ts` ≥ 1 and `grep -c "folder_scope" …` ≥ 1 and `grep -c '"template"' …` ≥ 3.
    - `grep -cE "read live|psycopg2|fetch\(" frontend/src/components/workflows/__fixtures__/canvasFixtures.ts` returns 0 apart from the header's prohibition sentence.
    - The header no longer claims `skill_ref` / `folder_scope` / template assets are omitted.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>The shipped corpus now carries the fields the derived tier reads, each with a cited source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the SC#5 check-1 and check-2 sweeps</name>
  <files>frontend/src/components/workflows/phaseVocabulary.corpus.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts` (as extended in Task 1)
    - `frontend/src/components/workflows/canvasModel.fixtures.test.ts:17-90` — the corpus self-check and `describe.each(ALL_FIXTURES)` sweep idiom to copy.
    - `frontend/src/components/workflows/phaseVocabulary.ts` — `nodeTitle`, `derivedFaceOf`, `PHASE_TYPE_SENTENCES` (the six type sentences check 1 must tolerate).
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md` §"Post-research amendments" — D-187-15 and D-187-16 in full.
  </read_first>
  <behavior>
    - **Check 1:** for every phase of every corpus fixture, the reveal-OFF face (`nodeTitle(phase, ctx)`) contains neither that phase's `slug` nor any of the six raw `phase_type` tokens.
    - **Check 2 (narrowed per D-187-15):** within one workflow, no two steps whose config differs materially render identical faces. "Materially different" is computed from the fields the derived tier reads plus `phase_type` — two steps identical on all of those are honestly the same kind of step.
    - `plan_execute_verify` is the documented exception: its `plan` and `verify` steps are both bare `llm_single` differing only in `prompt`, so they are NOT materially different and their identical face is an honest statement, not a defect.
    - Check 2 currently FAILS on HEAD's face resolution for at least one corpus member whose config IS materially different — record which, and confirm it passes after the derived tier.
    - The sweeps assert on the strings `nodeTitle` returns; nothing renders a component.
  </behavior>
  <action>
Create `frontend/src/components/workflows/phaseVocabulary.corpus.test.ts` as a **pure-function** sweep
over `ALL_FIXTURES`. `describe.each(ALL_FIXTURES)` + `nodeTitle(phase, ctx)` is the whole shape. Do not
render `PhaseNode` or `PhaseSpineGraph`; D-187-16 settles that a face is the string `nodeTitle()`
returns, and a DOM scrape would fail 100% on the spine's pre-existing raw chrome and could only pass by
deleting shipped UI.

Build each fixture's `NameContext` from the fixture data itself: the folder and skill id→name maps as
small literal maps declared beside the fixture ids, and `templateFilename` from the fixture's template
asset field. Declare the maps once, in the test, and cite where the names come from.

**Check 1** — assert the face contains neither the phase's own `slug` nor any of the six raw
`phase_type` tokens. Use exact token matching (word-boundary), not substring, so a legitimate word that
happens to contain a type token does not false-positive. Report failures with the fixture name, the
phase slug and the offending face, so a failure is diagnosable without a re-run.

**Check 2** — group each fixture's phases by a `materialConfigKey(phase)` helper defined in the test:
the tuple of `phase_type` plus the fields the derived tier reads (`skill_ref`, single-entry
`folder_scope`, and whether the template tier applies). Assert that no two phases with DIFFERENT
material keys share a face. Phases with the SAME material key are permitted to share a face — that is
D-187-15's narrowing, and it is what makes the check measure over-simplification rather than punishing
honesty.

Add an explicit, named test for the documented exception: `plan_execute_verify`'s `plan` and `verify`
render the same face, their material keys are equal, and that is recorded as intentional with a comment
citing `backend/tests/conftest.py:867-890` and D-187-15. Do not edit the seed fixture to fit the test —
the acceptance bar would stop measuring the real corpus.

Add a corpus self-check mirroring the shipped one: assert `ALL_FIXTURES` still registers at least the
number of entries it does today plus any added in Task 1, so a fixture silently disappearing fails
here too.

Record in the SUMMARY: which corpus members fail check 2 on HEAD's resolution and which pass after the
derived tier — the SPEC's *"this check FAILS on HEAD today"* must be re-stated precisely, as a count
over named members rather than as a slogan.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/phaseVocabulary.corpus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/phaseVocabulary.corpus.test.ts` passes, 0 failed.
    - `grep -cE "render\(|@testing-library" frontend/src/components/workflows/phaseVocabulary.corpus.test.ts` returns 0 — it is a pure-function sweep.
    - A test named with the substring `plan_execute_verify` documents the D-187-15 exception and cites `conftest.py:867-890`.
    - Check 1 covers every phase of every fixture (assert the swept phase count in a self-check, so a `describe.each` that silently skips a fixture fails).
    - `materialConfigKey` is defined in the test and includes `phase_type`, `skill_ref`, `folder_scope` and the template applicability.
    - `git status --porcelain backend/tests/conftest.py` is empty — the seed fixture was not edited to fit the test.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>SC#5's two checks are automated over the real corpus, with the one honest collision documented rather than engineered away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in fixture corpus → the acceptance bar | a corpus that drifts, or is edited to fit, stops being evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-12-01 | Tampering | the acceptance bar | mitigate | Every new fixture field carries a `file:line` source; the module's own "names a source" test enforces it; `git status` on `conftest.py` empty proves the seed was not edited to pass. |
| T-187-12-02 | Repudiation | an over-claimed SC#5 result | mitigate | The SUMMARY must state which corpus members fail on HEAD and which pass after, as a count over named members. The corpus is small (27 well-formed rows, max 5 phases) and the plan says so rather than over-claiming. |
| T-187-12-03 | Denial of Service | the fixture corpus silently shrinking | mitigate | A self-check asserts the registered fixture count and the swept phase count, so a `describe.each` that quietly covers nothing fails. |
| T-187-12-04 | Information Disclosure | live DB reads in a fixture | mitigate | The module performs no I/O; a grep for live-read APIs returning 0 is an acceptance criterion. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.corpus.test.ts src/components/workflows/canvasModel.fixtures.test.ts src/components/workflows/canvasModel.roundtrip.test.ts src/components/workflows/phaseVocabulary.test.ts` → 0 failed.
- The frontend 8-file vocabulary set is ≥ 863 passed with the corpus file's net-new on top.
- `cd frontend && npx tsc -b` exits 0.
</verification>

<success_criteria>
- SC#5 check 1 and the narrowed check 2 are automated over the shipped corpus as pure-function sweeps.
- The `plan_execute_verify` exception is documented with its measured shape rather than engineered away.
- No snapshot moved and the seed fixture was not edited.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-12-SUMMARY.md` when done.
</output>
