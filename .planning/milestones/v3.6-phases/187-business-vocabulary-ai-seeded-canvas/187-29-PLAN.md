---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 29
type: execute
wave: 16
depends_on: ["187-26", "187-27", "187-28"]
gap_closure: true
gap_closure_round: 5
gap_closure_base: 15339441
files_modified:
  - scripts/vitest-count-gate.cjs
  - .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
  - .planning/phases/187-business-vocabulary-ai-seeded-canvas/deferred-items.md
autonomous: true
requirements: [VOCAB-02, VOCAB-03]

must_haves:
  truths:
    - "Every guard round 5 added lives in a suite whose per-file count is PINNED, so it cannot be deleted with the gate green (the Phase-177 lesson the script's own header cites)"
    - "Each new pin's number came from the gate's own `actual` column, never from a hand count of `it(` literals"
    - "Each new pin was OBSERVED producing a [count-decrease] on a genuinely deleted `it(` block — a pin never seen biting is a gesture"
    - "The round-5 record carries every figure with the command that produced it, and every 'before' is DERIVED at this commit rather than transcribed"
    - "G-4 is honoured: the manual board GAINS rows for what round 5 built and no shipped row is ticked, softened, re-scoped or dropped"
    - "The seeded-rows caveat is on the record: an assertion made only against a legacy row is not evidence about what the CURRENT authoring path produces (operator, 2026-08-04)"
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "per-file pins for the three suites carrying round 5's honesty estate"
      contains: "DescribeKbPicker.test.tsx"
    - path: ".planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md"
      provides: "the round-5 section: per-task rows, every probe with its observed RED, every re-measured gate, and the amended manual board"
      contains: "187-26-T1"
  key_links:
    - from: "scripts/vitest-count-gate.cjs"
      to: "frontend/src/components/workflows/DescribeKbPicker.test.tsx"
      via: "the BASELINE pin map, read from the gate's own printed actual column"
      pattern: "DescribeKbPicker"
---

<objective>
Round 5's guards are only guards if they cannot be deleted quietly, and the round is only closed if
its figures are on the record with the commands that produced them.

Purpose: WR-16 — the round-4 finding — was exactly this: neither suite carrying the whole Req-5
governance estate was in the count gate's `BASELINE`, so thirteen tests could be deleted with the
gate green. Round 5 adds three suites in the same position: `DescribeKbPicker.test.tsx` (the
born-bound round trip), `ProblemsTray.test.tsx` (the fence that the all-clear cannot render before a
check has run) and `verdictModel.test.ts` (the sentence's word-class property).

Output: three new pins, each observed catching a deletion; the round-5 section of
`187-VALIDATION.md`; three new manual rows; nothing ticked.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-UAT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
@scripts/vitest-count-gate.cjs

<read_first>
- `scripts/vitest-count-gate.cjs` — the `BASELINE` map, the `BASELINE_TOTAL` derivation, the
  `pinnedNames` success line, and the header's `RE-PINNED` history block. Note that
  `BASELINE_TOTAL` is COMPUTED and the success line's count is DERIVED (187-25 made both so, because
  a literal there prints a false number on a GREEN gate — the same stale-claim class the pin exists
  to catch). Keep them derived.
- `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` — §"Per-Task
  Verification Map", the round-3 and round-4 gate sections (for the SHAPE a round section takes), and
  the manual board M1…M14. Read the round-4 section's §(j)/§(k) closely: the "before is derived, not
  inherited" method is the one you will reuse.
- The three round-5 SUMMARYs (`187-26`, `187-27`, `187-28`) — the source of the rows you transcribe.
  **A SUMMARY's figure is a claim to CHECK, not a number to copy.** Where your re-run disagrees with a
  SUMMARY, record BOTH and mark the re-run as the measurement (round 3's §(q) precedent).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pin the three suites, from the gate's own output, and watch each pin bite</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <action>
**Step 1 — sample the gate TWICE before touching it**, and transcribe BOTH tables in full.

```bash
node scripts/vitest-count-gate.cjs
```

⚠ Two files carry recorded parallel-execution flakes: `PublishGauntlet.test.tsx`
(`D-ITEM-187-20-01`) and `WorkflowBuilderPage.canvas.test.tsx` (`D-ITEM-187-25-01`). If a sample
reds, **do not lower a pin and do not proceed on the assumption that it is one of those two** —
round 4's plan predicted the wrong file. Extract the failing FULLNAME from the gate's own JSON
report, re-run that file in ISOLATION, record both results, and log a new `D-ITEM-187-29-xx` if it is
a third file. A red that is NOT green in isolation is a stop-and-report.

**Step 2 — read the three numbers from the `actual` column.** `DescribeKbPicker.test.tsx`,
`ProblemsTray.test.tsx` and `verdictModel.test.ts` will appear as `new`. **Never hand-count `it(`
literals**: `definitionOps.test.ts` declares 122 literals and runs 232 cases, so a hand count would
have pinned a fiction by ~110 — and a pin below the real count can never fire.

**Step 3 — add the three entries to `BASELINE`.** Leave every existing pin exactly as it is: this is
an EXTENSION with no deletion behind it, which is a different act from 185-08's lowering, and the
header's history block must say so in those terms — only a LOWERING needs an authorised deletion
behind it. `BASELINE_TOTAL` and the success line stay derived; confirm the printed `N/N` moves from
18/18 to 21/21 on its own.

**Step 4 — three deletion probes, each observed.** For each newly pinned file, delete ONE genuine
`it(` block, run the gate, and paste the raw `[count-decrease]` line; then restore and prove the
restore.

⚠ **`it.skip` is NOT a valid probe** — the gate counts `assertionResults.length`, which includes
skipped cases, so a skip leaves the count unmoved. The block must be genuinely deleted.
⚠ **Do not revert with `git checkout --`** — it restores to HEAD and will wipe this task's own
uncommitted edits (the 187-24 lesson). Use a sidecar copy that reverses exactly what it applied, and
check `git diff --numstat` against what it SHOULD be rather than against zero.

| Probe | File | Must print |
|---|---|---|
| P-23 | `DescribeKbPicker.test.tsx` | `[count-decrease] … pinned N, ran N-1` |
| P-24 | `ProblemsTray.test.tsx` | same |
| P-25 | `verdictModel.test.ts` | same |

Note in the SUMMARY whether each probe ran with `failed 0` — the count decrease being the ONLY
signal is the entire reason this gate exists.
  </action>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Gate exit 0 with 21/21 pinned; both pre-pin samples and the post-pin table transcribed raw;
  P-23/P-24/P-25 each observed producing a `[count-decrease]` and each restore proved; no existing
  pin lowered.</done>
</task>

<task type="auto">
  <name>Task 2: the round is on the record, and the board grows by three</name>
  <files>.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md, .planning/phases/187-business-vocabulary-ai-seeded-canvas/deferred-items.md</files>
  <action>
**A. Frontmatter.** Add `gap_closure_round_5: 2026-08-04`, `gap_closure_5_plans: [187-26, 187-27,
187-28, 187-29]`, `gap_closure_5_base: 15339441`, `gap_closure_5_gates_measured: <date>`. Update
`manual_rows` to the new total and leave `manual_rows_performed: 0` — **no manual row is performed by
this plan.**

**B. Per-Task Verification Map.** Add one row per round-5 task (`187-26-T1` … `187-29-T2`) in the
shipped column shape, each with its threat ref, its secure behaviour in one sentence, its automated
command and its measured status.

**C. A new §"Phase gates — gap-closure round 5" section**, appended AFTER round 4's last line as a
PURE INSERTION (prove it with the diff hunk list, as 187-25 did). Base is **`15339441`**. It carries:

1. **Every falsification probe** P-16…P-25 with its raw RED output and its proved revert. Ten probes;
   if a plan ran a different number, record the MEASURED set and say so (187-25 §5 corrected both a
   plan's and a SUMMARY's probe count — that correction is the pattern, not an exception).
2. **The D-187-14 mount cap, re-measured**:
   `git diff --numstat 15339441 HEAD -- frontend/src/pages/WorkflowBuilderPage.tsx`.
   Round budget **≤ 15 insertions / ≤ 3 deletions**, and the arithmetic shown: 187-26's 6/1 plus
   187-27's 9/2. ⚠ **The third deletion is one over the ≤ 2 the phase had held.** It was claimed IN
   ADVANCE and by name in 187-27 (the §(a2) "single named allowance" precedent) — record it as a
   named spend, never as a renegotiated budget. Also record the STRONGER property the decision rests
   on: **the render body is unchanged** — zero new JSX elements, zero new props on existing JSX
   elements, zero new declarations — with both greps' raw output. All three deletions are a hook
   argument, a `useState` initializer and a `useMemo` field.
3. **The zero-migration gate**: `git diff --stat 35261e96 HEAD -- supabase/migrations`,
   `git diff --stat 15339441 HEAD -- supabase/migrations`, `git status --porcelain supabase/migrations`
   — all three empty.
4. **The isolated named sets**, run isolated and never as the full frontend suite (measured 42–49
   failures, flaky at one commit). Guard the COUNT, not just failures. Every "before" DERIVED at this
   commit by counting declared case literals in the base blob and the HEAD blob
   (`git show <rev>:<path> | grep -cE …`), with the ⚠ note that literal counts ≠ run counts
   (`it.each` expands) so only the DELTAS are the instrument.
5. **`npx tsc --noEmit -p tsconfig.app.json`** — 33 vs the `D-ITEM-01` baseline, none in round 5's
   files. ⚠ Re-state `D-ITEM-187-23-02`: the bare `npx tsc --noEmit` from `frontend/` is VACUOUS
   (root tsconfig is a solution file with `files: []`) and exits 0 having checked nothing.
6. **`npx vite build`** — exit 0.
7. **The backend half**: the unit-tree figure with its re-derived pre-existing-rot baseline, and
   `git diff --name-only 15339441 HEAD -- backend/` showing exactly the one comment-only file plus
   round 5's test file.
8. **No false completion record**:
   `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md`.
   State plainly that any `.planning/STATE.md` / `ROADMAP.md` movement in the range is the
   ORCHESTRATOR's tracking write, whose job it is, and that no executor called
   `requirements.mark-complete`, `state.advance-plan` or `roadmap.update-plan-progress`.

**D. The manual board grows by three. Nothing is ticked.** Write M15, M16, M17 in the shipped row
shape (source · Req · expected · result: `[pending]`):

- **M15 — you can say what the workflow is about BEFORE the AI drafts it.** On the loose
  "Describe & run" door, pick a knowledge base, type a requirement, click Draft the workflow. The
  generated draft is BOUND to what you picked — check the header chip AND the stored
  `project_folder_id`, not just the chip. Then check the canvas: the retrieval steps carry no
  unbound verdict and Publish is not blocked for a reason you were never asked about.
- **M16 — a draft you just opened does not claim it passed a check nobody ran.** Open an existing
  draft into the canvas and read the bottom strip in the first second, before touching anything. It
  must NOT say "Nothing to fix — the static checks pass" and it must NOT say "checked by the server".
  Publish must be honestly unavailable until an answer lands, then behave according to that answer.
  Watch the FIRST SECOND, not the settled state — that is the test.
- **M17 — the fast path is still fast.** On the same door, ignore the knowledge-base picker
  entirely. Type and click once. It still drafts, with no extra question, no extra screen and no
  extra click. If the picker ever stands between you and a draft, this row FAILS regardless of
  anything else round 5 shipped.

**E. The standing caveat, recorded in the round-5 section as a rule for every row above.**
Verbatim from the operator, 2026-08-04: the 73 local drafts / 68 published rows are **test data of
unknown vintage** and some predate the v3.6 canvas work entirely. **Every round-5 row must ALSO be
performed against a FRESHLY GENERATED draft**; an observation made only against a seeded row is not
evidence about what the current authoring path produces, and a shape found in an old row is not a
regression until the row's write date is checked.

**F. Repair, never soften.** If any shipped row's expectation was made stale by round 5 — in
particular anything asserting the tray's resting wording or the publish trigger's availability —
**strike it through, name the shipped sentence, and give the reason**. A silently vanishing
expectation reads as one that was never true (187-25's recorded rule). If NO row is stale, say so
with the grep that establishes it — round 4's plan asserted two stale rows that did not exist, and
the correction is the pattern to follow.

**G. `deferred-items.md`** — append any out-of-scope discovery from the round with a concrete re-open
trigger. Log rather than fix, and log rather than forget.

⚠ **Do NOT edit `187-UAT.md`.** It is the operator's session record; the UAT workflow owns it.
⚠ **Do NOT tick, soften, re-scope or drop any existing row**, and do not mark any requirement
complete.
  </action>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs &amp;&amp; git status --porcelain .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <done>The round-5 section is a pure insertion after round 4's last line (proved by the hunk list);
  every figure carries its command and every "before" is derived at this commit; the board reads
  M1…M17 with `manual_rows_performed: 0`; `187-UAT.md` is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. This plan writes no code that runs in the product: a pin map, planning records, and a
deferred-items entry. `scripts/vitest-count-gate.cjs` imports nothing from `frontend/src`, needs no
database, no backend and no network, and still refuses to write its report inside a reload-watched
tree.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-R5-15 | Tampering | round 5's new suites | mitigate | all three pinned; each pin observed producing a `[count-decrease]` on a genuinely deleted `it(` block (P-23…P-25) — a failures-only differential cannot see a deleted test |
| T-187-R5-16 | Spoofing (of a number) | the new pins | mitigate | every number read from the gate's own `actual` column over two agreeing samples, raw tables transcribed; a hand count of `it(` literals is explicitly forbidden and its measured error is recorded |
| T-187-R5-17 | Repudiation | a red gate | mitigate | a red is diagnosed by extracting the failing FULLNAME from the gate's own report and re-running in isolation; both results recorded; no pin lowered; a third flaking file gets its own deferred item rather than being folded into the wrong one |
| T-187-R5-18 | Repudiation | the manual board | mitigate | board grows, nothing ticked or softened; stale expectations struck through with the shipped sentence named; the seeded-rows caveat recorded as a rule binding every new row |
| T-187-R5-19 | Repudiation | the record's figures | mitigate | every "before" derived at this commit from the base blob; a SUMMARY figure is checked, not copied, and a disagreement records both with the re-run as the measurement |
| T-187-R5-SC | Tampering | npm installs | accept | not engaged — zero package-manager installs, zero new dependencies |
</threat_model>

<verification>
- `node scripts/vitest-count-gate.cjs` exits 0 at 21/21 with all three new pins at delta 0.
- Each new pin has been SEEN failing on a deleted test.
- The round-5 section's every figure has a command beside it, and `187-UAT.md` is byte-untouched.
</verification>

<success_criteria>
- [ ] `DescribeKbPicker.test.tsx`, `ProblemsTray.test.tsx` and `verdictModel.test.ts` are pinned from
      the gate's own `actual` column; no existing pin lowered; `BASELINE_TOTAL` and the success count
      stay derived
- [ ] P-23, P-24, P-25 each observed producing `[count-decrease]`, each restore proved from a sidecar
- [ ] The round-5 section is a pure insertion; base `15339441` stated; every "before" derived here
- [ ] The mount cap re-measured at ≤ 15/3 with the third deletion recorded as a NAMED spend, and the
      render-body property (0 new JSX, 0 new props on existing JSX, 0 new declarations) proved
- [ ] Zero migrations proved three ways; `tsc` 33 with the vacuous-form warning restated; `vite build` 0
- [ ] Board reads M1…M17, `manual_rows_performed: 0`, nothing ticked or softened, the seeded-rows
      caveat recorded as binding on every new row
- [ ] `187-UAT.md` untouched; no SDK completion verb called by any task in this round
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-29-SUMMARY.md` when done.
</output>
