---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 07
type: execute
wave: 2
depends_on: ["187-02"]
files_modified:
  - backend/tests/integration/test_187_authoring_roster.py
  - .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
autonomous: true
requirements: [VOCAB-02]

must_haves:
  truths:
    - "D-187-13 — SC#10 is an automated per-provider generation roster: the FULL native roster plus OpenRouter, DERIVED from MODEL_CAPABILITIES by grouping on `provider`, never re-typed"
    - "Each roster row passes its own `settings` stub to generate_workflow_definition — settings is a PARAMETER, so there is zero global mutation and zero contamination between rows"
    - "Every row asserts a valid WorkflowDefinition with a non-empty `name` on EVERY phase"
    - "Blocked rows are recorded ⛔ with the reason and the blocking id — never silently omitted"
    - "The roster lives under backend/tests/integration/ behind a skipif guard, so `pytest tests/ -q` stays green and fast"
  artifacts:
    - path: "backend/tests/integration/test_187_authoring_roster.py"
      provides: "the 8-provider SC#10 generation roster, derived not re-typed"
      contains: "MODEL_CAPABILITIES"
    - path: ".planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md"
      provides: "the recorded SC#10 scoreboard including ⛔ rows"
  key_links:
    - from: "backend/tests/integration/test_187_authoring_roster.py"
      to: "app.config.MODEL_CAPABILITIES"
      via: "import + group by provider + one representative per group"
      pattern: "MODEL_CAPABILITIES"
    - from: "backend/tests/integration/test_187_authoring_roster.py"
      to: "app.services.workflow_authoring.generate_workflow_definition"
      via: "settings=SimpleNamespace(harness_authoring_model=<row id>) — a parameter, not a monkeypatch"
      pattern: "harness_authoring_model"
---

<objective>
Prove the per-step `name` instruction holds across the whole provider roster, without mutating a global.

Purpose: CLAUDE.md's UAT scoreboard recipe requires the FULL native roster plus OpenRouter, derived
from `MODEL_CAPABILITIES` and never re-typed, with blocked rows recorded ⛔ rather than dropped. The
SPEC assumed the roster must be driven by an app setting and therefore run serially with global
mutation; that is superseded — measured, `generate_workflow_definition` takes `settings` as a
**parameter** (`workflow_authoring.py:222`), so each row passes a stub and no global is touched.

The SPEC's `MODEL_CAPABILITIES` figures are REFUTED on four counts. Measured 2026-08-02: **61 models
across 8 provider groups**; **DeepSeek IS present** (`deepseek-v4-flash`, `deepseek-v4-pro`); 5 models
declare no `forced_emission` (all moonshot natives plus 2 OpenRouter moonshot rows, `emit_tier="coerce"`);
all 8 provider keys are configured locally, so **zero ⛔ rows are expected from missing keys**.
Re-derive at execution time regardless — the registry is edited often.

Output: the roster test and the recorded scoreboard.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-SPEC.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-PATTERNS.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md
</context>

<interfaces>
```python
# backend/app/services/workflow_authoring.py:92-114 (shipped)
def resolve_authoring_model(settings) -> str | None:
    model = getattr(settings, "harness_authoring_model", None)
    if model: return model                          # ⚠ branch 1 does NOT validate forced_emission
    from app.config import get_model_capability
    for candidate in ("claude-opus-4-8", "gpt-5.5"):
        if (get_model_capability(candidate) or {}).get("forced_emission"): return candidate
    return None

# :222 — `settings` is a PARAMETER
async def generate_workflow_definition(*, describe, supabase, user_id, settings, pool=None, ...)
# :250 — provider resolved from get_model_capability(authoring_model)["provider"]
# :267 — load_user_settings(user_id) → needs a live Supabase and a real user id
# :276 — _assemble_grounding → reads the folder tree + skill registry
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Derive the roster from MODEL_CAPABILITIES and build the parametrized integration test</name>
  <files>backend/tests/integration/test_187_authoring_roster.py</files>
  <read_first>
    - `backend/tests/integration/test_075_4_anthropic_langsmith_trace.py:1-80` — the shipped `pytest.mark.skipif` idiom for a test that needs a real key, and its module docblock stating what is skipped and why.
    - `backend/tests/integration/conftest.py:60-120` — the local-Postgres probe and the shipped `pytest.skip` messages.
    - `backend/tests/unit/test_103_nl_generate.py:40-130` — `_patch_grounding` and the `settings=object()` call at `:114` (the proof that `settings` is a parameter).
    - `backend/app/services/workflow_authoring.py:217-320` — the whole `generate_workflow_definition` body, including what it needs live (`load_user_settings`, `_assemble_grounding`).
    - `CLAUDE.md` §"UAT scoreboard recipe" — the roster rule, the derive-never-retype rule, and "blocked, never silently omitted".
  </read_first>
  <action>
Create `backend/tests/integration/test_187_authoring_roster.py`.

**Derive the roster; never re-type it.** `from app.config import MODEL_CAPABILITIES`, group the entries
on their `provider` value, and pick ONE representative per group. Prefer the newest registry-backed id
per provider — an id absent from `MODEL_CAPABILITIES` resolves `capability_source=inferred` and
silently loses `emit_tier`, so the row would measure a weaker configuration than the one that ships.
Write the selection as code (a documented ordering heuristic plus an explicit override map for any
group where the heuristic picks badly), not as a hand-typed list of eight strings. Assert as a
first-class test that the derived group count is **at least 8** and that the group keys include
`deepseek` and `moonshot` — the two the SPEC's crude parse got wrong.

**Each row passes its own settings stub.** `settings=SimpleNamespace(harness_authoring_model=<row id>)`.
No `monkeypatch` of a module-level singleton, no global mutation, no contamination between rows.
`@pytest.mark.parametrize` over the derived rows, one row per provider group.

**Each row asserts:** the call returns `ok`; the payload `WorkflowDefinition.model_validate()`s; and
**every** phase carries a `name` that trims non-empty. Assert the name property with an assertion
message naming the offending phase slug, so a partial failure is legible without a re-run.

**Skip guard.** Put a module-level `pytestmark = pytest.mark.skipif(...)` gated on an explicit opt-in
environment variable (name it and document it in the docblock) AND on local Supabase reachability —
the row needs a live Supabase and a real user id for `load_user_settings` and `_assemble_grounding`.
`pytest tests/ -q` must stay green and fast without the opt-in. Per-row, additionally skip-with-reason
when that provider's API key is absent from settings, and make the skip reason machine-readable so
Task 2 can transcribe it as a ⛔ row.

**Record, never omit.** Collect per-row outcomes into a module-level results dict written out at
session end (a `pytest` fixture with `scope="module"` and a finalizer), so a run always produces a
full 8-row table including skipped and failed rows with their reasons — a scoreboard that lists only
what passed is not a scoreboard.

**Expect the moonshot row to be the interesting one.** Its `emit_tier` is `coerce` and its
`forced_emission` is `None`, and `resolve_authoring_model`'s branch 1 does **not** validate
`forced_emission` for an explicitly-set model — so an explicit `harness_authoring_model="kimi-k2.6"`
hands an unforceable model to `forced_emit(schema_model=WorkflowDefinition)`. If that row fails it is a
**real finding about the knob**, not a broken test. Say so in the docblock and record the outcome
either way.

Do not mark any row `xfail`. A predicted failure recorded as a finding is worth more than a green
suite that hides it.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/integration/test_187_authoring_roster.py -q --no-header --collect-only</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/test_187_authoring_roster.py --collect-only -q` collects at least 8 parametrized rows plus the roster-shape test.
    - `grep -n "MODEL_CAPABILITIES" backend/tests/integration/test_187_authoring_roster.py` shows a live import and a grouping expression.
    - `grep -cE '"(gpt-5\.6-sol|claude-sonnet-5|gemini-3\.5-flash|deepseek-v4-pro|glm-5\.2|MiniMax-M3|kimi-k2\.6)"' backend/tests/integration/test_187_authoring_roster.py` returns 0 OR every such literal appears only inside a documented override map — the eight ids are never a hand-typed roster.
    - A test asserts the derived group count ≥ 8 and that `deepseek` and `moonshot` are among the keys.
    - `grep -c "monkeypatch" backend/tests/integration/test_187_authoring_roster.py` returns 0.
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q --collect-only 2>&1 | tail -1` reports a collection count ≥ 3474 + net-new, and a plain `pytest tests/ -q` run does not attempt a provider call (the rows skip).
  </acceptance_criteria>
  <done>The roster is derived, parametrized, opt-in, and records every row including the blocked ones.</done>
</task>

<task type="auto">
  <name>Task 2: Run the roster and record the scoreboard in 187-VALIDATION.md</name>
  <files>.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md, backend/tests/integration/test_187_authoring_roster.py</files>
  <read_first>
    - `backend/tests/integration/test_187_authoring_roster.py` (as written in Task 1)
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` — the existing structure; the scoreboard is appended, the measured baseline table is not edited.
    - `CLAUDE.md` §"UAT scoreboard recipe" — the four-axis bandwidth and the ⛔ rule.
  </read_first>
  <action>
Run the roster with the opt-in variable set and a live local Supabase. Ask the operator to confirm the
backend environment is up before running anything that makes a provider call; do not start services
in the background.

Append a `## SC#10 roster — recorded <date>` section to `187-VALIDATION.md` with one row per provider
group and these columns: provider · model id (as derived) · `native_tools` · `emit_tier` ·
`forced_emission` · key configured · verdict (✅ / ❌ / ⛔) · evidence (for ✅: the phase count and that
every phase carried a name; for ❌: the assertion and the error; for ⛔: the reason and the blocking id).

**No row may be omitted.** A provider with no key, or one blocked by a known defect, is recorded ⛔
with the reason — never dropped. Also record the derived group count and the two SPEC refutations
(DeepSeek present; five models declare no `forced_emission`) with the date they were re-measured, so
the next phase does not inherit the SPEC's numbers.

Add a short paragraph under the table recording what this roster does and does NOT prove: it proves
the per-step `name` instruction survives every provider's emission path; it does **not** prove the
`HARNESS_AUTHORING_MODEL` env path reaches `resolve_authoring_model`. That is manual row **M7** and
stays in the Manual-Only table.

If any row is ❌, do not weaken the assertion. Record the finding, and if it is the moonshot row state
plainly that `resolve_authoring_model` validates its fallback but not an explicitly-set model — a real
defect in the knob, adjacent to open bug `BUG-260731-01`.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -c "⛔\|✅\|❌" .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `187-VALIDATION.md` contains a `## SC#10 roster — recorded` heading with a table of **at least 8 data rows**, one per derived provider group.
    - Every row carries a verdict glyph (✅ / ❌ / ⛔); no row has an empty verdict cell.
    - The section records the derived group count and both SPEC refutations with a re-measurement date.
    - The paragraph stating that M7 (the live env-path row) is NOT covered by this automation is present.
    - The `## Manual-Only Verifications` table still contains row M7 unchanged.
    - `git diff -- .planning/phases/187-business-vocabulary-ai-seeded-canvas/187-VALIDATION.md` shows additions only in the new section (the measured-baseline table is untouched).
  </acceptance_criteria>
  <done>The 8-row scoreboard is recorded with a verdict on every row, including blocked ones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider API (8 external services) → `WorkflowDefinition` emit | untrusted model output crosses into a validated definition |
| test process → provider API keys | a roster row reads real keys from settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-07-01 | Information Disclosure | provider API keys | mitigate | Keys are read through the shipped settings object and are never logged, printed or written into the scoreboard; the scoreboard records key presence as a boolean only. |
| T-187-07-02 | Tampering | roster completeness | mitigate | The roster is derived by importing the registry and grouping on `provider`; a hand-typed list of ids is an acceptance-criteria failure. Blocked rows are recorded ⛔, never dropped. |
| T-187-07-03 | Denial of Service | the default test suite | mitigate | The file lives under `tests/integration/` behind a `skipif` opt-in, so `pytest tests/ -q` makes no provider call. Verified by an acceptance criterion. |
| T-187-07-04 | Tampering | global settings contamination | mitigate | Each row passes its own `settings` stub — `settings` is a parameter. `grep -c monkeypatch` returning 0 is an acceptance criterion. |
| T-187-07-05 | Input Validation (V5) | emitted definition | mitigate | Every row asserts `WorkflowDefinition.model_validate()` — the `extra="forbid"` union IS the safety mechanism (SC#3). |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/integration/test_187_authoring_roster.py --collect-only -q` collects ≥ 9 items.
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q --collect-only 2>&1 | tail -1` ≥ 3474 + net-new.
- `187-VALIDATION.md` carries an 8+ row SC#10 scoreboard with a verdict on every row.
</verification>

<success_criteria>
- The roster is derived from `MODEL_CAPABILITIES`, one representative per provider group, never re-typed.
- Every row asserts a valid definition with a non-empty `name` on every phase.
- The scoreboard is recorded with ⛔ rows preserved and their reasons named.
- The default backend suite is unaffected.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-07-SUMMARY.md` when done.
</output>
