---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/phaseVocabulary.ts
  - frontend/src/components/workflows/phaseVocabulary.test.ts
autonomous: true
requirements: [VOCAB-01]

must_haves:
  truths:
    - "D-187-04 — the derived tier resolves bound skill → template → folder scope → human input → null, first hit wins (most-specific-first, NOT the SPEC's folder-first drafting slip)"
    - "D-187-05 — the derivation takes an injected, OPTIONAL name-lookup context; absent or empty falls through to the type sentence, never a fabricated or id-shaped face"
    - "nodeTitle() resolves four tiers: stored name → derived face → type sentence → raw type; the derived tier is computed at render and written nowhere"
    - "The CANVAS-01 TOTALITY contract holds — an unknown phase_type, an absent config, a malformed available_tools, a non-array folder_scope and a non-string skill_ref all resolve without throwing"
    - "The refuted docblock figure (\"Only 10 of 119 live phases carry a real phase.name\") is corrected to the measured 0 of 57 across 27 well-formed rows in the same edit"
    - "The new function is NOT named deriveTier — frontend/src/components/workflows/deriveTier.ts already exists as the Phase-103 strictness-tier resolver"
  artifacts:
    - path: "frontend/src/components/workflows/phaseVocabulary.ts"
      provides: "NameContext, derivedFace/derivedFaceOf, the 4-tier nodeTitle"
      contains: "export function nodeTitle"
    - path: "frontend/src/components/workflows/phaseVocabulary.test.ts"
      provides: "tier precedence, totality and never-fabricate coverage"
  key_links:
    - from: "frontend/src/components/workflows/phaseVocabulary.ts"
      to: "nodeTitle callers"
      via: "an optional second parameter defaulting to a frozen module-scope empty context"
      pattern: "NO_NAME_CONTEXT"
---

<objective>
Insert a config-derived tier between the stored name and the type sentence, in the ONE vocabulary module.

Purpose: measured live, every node face on every workflow is one of exactly six type sentences —
`nodeTitle()` falls through its first tier 100% of the time. Two steps that do different work render
letter-for-letter identical faces. The derived tier makes a step say what **that** step does, from
config it already has, computed at render and stored nowhere, so re-binding a skill changes the face
without a write.

Output: `NameContext`, the flat `derivedFace` core with its phase-shaped `derivedFaceOf` adapter, a
four-tier `nodeTitle`, and the tests that pin precedence, totality and the never-fabricate floor.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-SPEC.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-PATTERNS.md
</context>

<interfaces>
```ts
// frontend/src/components/workflows/phaseVocabulary.ts (shipped, measured)
export interface PhaseConfigJSON { phase_type: string; [k: string]: unknown }   // LOOSE JSONB read shape
export interface PhaseSpecJSON {
  slug: string; phase_index: number; name?: string | null
  config: PhaseConfigJSON; validators?: ValidatorJSON[]
  grounding_escalated?: boolean; action_risk_armed?: boolean
}
export function nodeTitle(phase: PhaseSpecJSON): string          // :169 — the 3-tier resolver today
export function technicalTitle(phase: PhaseSpecJSON): string     // :180
export function groundingCause(inputs: GroundingInputs): GroundingCause          // :265 — the flat core
export function groundingCauseOf(phase, kbTools): GroundingCause                 // :297 — the phase adapter
```

```python
# backend/app/models/harness.py:88, :93 — what the derived tier actually reads
folder_scope: list[UUID] | None = None   # resolved id LIST, NOT a name
skill_ref: UUID | None = None            # resolved skill id, NOT a name
# and WorkflowDefinition.assets: list[AssetRef] | None — AssetRef = {asset_id, filename, kind, mime}
#   the TEMPLATE is DEFINITION-level, not phase-level
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NameContext and the derived-face core with the D-187-04 precedence</name>
  <files>frontend/src/components/workflows/phaseVocabulary.ts, frontend/src/components/workflows/phaseVocabulary.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/phaseVocabulary.ts:1-45` — the purity contract (*"imports NOTHING from the API client"*) and the CANVAS-01 TOTALITY contract.
    - `frontend/src/components/workflows/phaseVocabulary.ts:56-90` — `PhaseConfigJSON` (index signature: the LOOSE JSONB read shape) and `PhaseSpecJSON` with its additive-optional Phase-185 fields.
    - `frontend/src/components/workflows/phaseVocabulary.ts:118-200` — the `PHASE_TYPE_SENTENCES` docblock (with the refuted "10 of 119" figure at `:122-123`), the six sentences, `PHASE_TYPE_SUBTITLES`, `PHASE_TYPE_LABELS`, `nodeTitle` at `:169` and `technicalTitle` at `:180`.
    - `frontend/src/components/workflows/phaseVocabulary.ts:239-310` — `GroundingInputs`, the load-bearing numbered branch-order comments in `groundingCause`, and the defensive reads in `groundingCauseOf` (`Array.isArray(...)` / `typeof ... === "string"`). **This flat-core + phase-adapter pair is the exact shape to copy.**
    - `frontend/src/components/workflows/phaseVocabulary.test.ts:110-160` — the shipped `nodeTitle` tier tests and the totality assertions at `:150-151`.
    - `frontend/src/components/workflows/deriveTier.ts:1-30` — **the name collision.** This is the Phase-103 strictness-tier resolver. Do not reuse the name.
  </read_first>
  <behavior>
    - A phase with a `skill_ref` that resolves in `skillNames` renders `Run the <skill name>`, even when `folder_scope` and a template are also present (most-specific-first).
    - An `llm_emit` phase with no skill and a definition template filename renders `Fill <filename>`.
    - A NON-`llm_emit` phase with the same template filename does NOT render the template face — it falls to the next tier.
    - A phase with exactly one `folder_scope` id that resolves in `folderNames` renders `Search <folder name>`; a phase with two or more ids falls through.
    - An `llm_human_input` phase with nothing bound renders `Wait for your approval`.
    - A phase with nothing bound returns `null` (the honest floor).
    - A `skill_ref` present but NOT in `skillNames` returns `null` for that tier and falls through — never the raw UUID.
    - An empty `NameContext` (`{}`) makes every tier miss and returns `null` for every phase.
    - Totality: `config` absent, `phase_type` unknown, `folder_scope` a string instead of an array, `skill_ref` a number, `assets` malformed — none throws.
  </behavior>
  <action>
Add, in `phaseVocabulary.ts`, below the `groundingCause` section so the file's one-vocabulary shape is
preserved:

**`export interface NameContext`** with three optional members, each with a JSDoc line naming where
the value comes from and stating that absent or empty means the tier misses:
  - `folderNames?: Readonly<Record<string, string>>` — folder id → display name
  - `skillNames?: Readonly<Record<string, string>>` — skill id → display name
  - `templateFilename?: string` — the DEFINITION's `assets[]` entry where `kind === "template"`

**`const NO_NAME_CONTEXT: NameContext = Object.freeze({})`** at module scope. Frozen and module-scope,
copying `canvasModel.ts`'s `NO_KB_TOOLS` idiom verbatim in spirit: an omitted context must hand the
same reference on every call so the projection stays deterministic to the byte.

**`export interface DerivedFaceInputs`** — the flat inputs, mirroring `GroundingInputs`:
`phaseType: string`, `skillName?: string`, `templateFilename?: string`, `folderName?: string`.

**`export function derivedFace(inputs: DerivedFaceInputs): string | null`** — the pure core, with the
numbered branch-order comments the shipped `groundingCause` uses, because the ORDER is the decision:

  1. BOUND SKILL — `Run the <skillName>`. A bound skill states what *this* step does; it wins over
     everything below. Most-specific-first is the whole argument for the precedence (D-187-04).
  2. TEMPLATE — `Fill <templateFilename>`, **only when `phaseType === "llm_emit"`**. The template is
     DEFINITION-level, so an ungated tier would render the same filename on every phase that reaches
     it and make distinct steps identical — the exact failure SC#5 check 2 exists to catch.
     `llm_emit` is the only type whose executor resolves the bound template.
  3. FOLDER SCOPE — `Search <folderName>`. Deliberately last of the three config tiers: `folder_scope`
     is a subset of the single `project_folder_id`, so most steps in one workflow share it and
     folder-first would collapse distinct steps back into identical faces. **SPEC Req 1's
     "folder scope → bound skill → template" is a drafting slip and is overridden by D-187-04.**
  4. HUMAN INPUT — `Wait for your approval` when `phaseType === "llm_human_input"`.
  5. otherwise `null` — the honest floor. Never fabricate.

**`export function derivedFaceOf(phase: PhaseSpecJSON, ctx: NameContext = NO_NAME_CONTEXT): string | null`**
— the phase-shaped adapter. It declares no branch of its own; it only reads defensively off the LOOSE
JSONB shape and calls `derivedFace`:
  - `skillName`: read `phase.config.skill_ref`, accept it only when `typeof === "string"`, then look
    it up in `ctx.skillNames`. A miss yields `undefined` — never the raw id.
  - `folderName`: read `phase.config.folder_scope`, accept only `Array.isArray` with **exactly one**
    entry that is a string, then look it up in `ctx.folderNames`. Two or more scoped folders yield
    `undefined`: a face that names one of several would be wrong, and a count is not a name.
  - `templateFilename`: pass `ctx.templateFilename` straight through; the `llm_emit` gate lives in the
    core so both callers get it.

Name nothing `deriveTier` or `deriveTierOf` — `frontend/src/components/workflows/deriveTier.ts` is the
shipped Phase-103 strictness-tier resolver and the two must stay legible.

Do **not** import anything from the API client. Do **not** read `assets` here — the definition-level
lookup belongs to the caller that holds the definition (plan 187-08 / 187-15).

Extend `phaseVocabulary.test.ts` with every behaviour above, including all five totality shapes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/phaseVocabulary.test.ts` passes with a test count strictly greater than HEAD's.
    - `grep -c "deriveTier" frontend/src/components/workflows/phaseVocabulary.ts` returns 0.
    - `grep -n "NO_NAME_CONTEXT" frontend/src/components/workflows/phaseVocabulary.ts` shows `Object.freeze({})` at module scope.
    - `grep -cE "from ['\"]@/lib/api['\"]|fetch\(" frontend/src/components/workflows/phaseVocabulary.ts` returns 0 (the purity contract holds).
    - `derivedFace` contains numbered branch comments `(1)`..`(5)` naming the precedence and citing D-187-04.
    - A test asserts `derivedFaceOf(phaseWithUnresolvedSkillRef, ctx) === null` and that the returned string never contains the raw id.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>The derived tier exists as a total, pure, injected-context function with the most-specific-first precedence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make nodeTitle four-tier and correct the refuted docblock</name>
  <files>frontend/src/components/workflows/phaseVocabulary.ts, frontend/src/components/workflows/phaseVocabulary.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/phaseVocabulary.ts` (as edited in Task 1)
    - `frontend/src/components/workflows/phaseVocabulary.ts:118-135` — the `PHASE_TYPE_SENTENCES` docblock carrying the refuted figure.
    - `frontend/src/components/workflows/phaseVocabulary.ts:160-186` — `nodeTitle`'s docblock (*"The SLUG NEVER appears in this string"*) and `technicalTitle`.
    - `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-RESEARCH.md` §"Corpus — the SC#5 measurement, honestly split" — the measured replacement figures.
  </read_first>
  <behavior>
    - `nodeTitle(phase)` with the context omitted returns exactly what it returns on HEAD, for every phase shape the shipped suite covers (byte-identical when omitted).
    - `nodeTitle(phase, ctx)` returns a trimmed stored `name` when present, even when the derived tier would also resolve (tier 1 still wins).
    - `nodeTitle(phase, ctx)` returns the derived face when `name` is absent/blank and a tier resolves.
    - `nodeTitle(phase, ctx)` returns the type sentence when no tier resolves, and the raw `phase_type` for an unknown type.
    - `nodeTitle` never throws on a malformed phase (the shipped totality assertion still passes).
  </behavior>
  <action>
Change `nodeTitle`'s signature to `nodeTitle(phase: PhaseSpecJSON, ctx: NameContext = NO_NAME_CONTEXT): string`
and insert the derived tier between the stored name and the type sentence:

  tier 1 stored `phase.name` (trimmed non-empty) → tier 2 `derivedFaceOf(phase, ctx)` →
  tier 3 `PHASE_TYPE_SENTENCES[type]` → tier 4 the raw type string.

The default parameter is what makes every existing caller render byte-identically; do not make the
parameter required and do not change `technicalTitle`.

Update `nodeTitle`'s docblock to describe four tiers and to restate the two floors it now carries:
the slug still never appears in this string, and a name is never fabricated — an unresolved lookup
falls through rather than showing an id.

**Correct the refuted figure in the same edit** (CONTEXT requires it). The `PHASE_TYPE_SENTENCES`
docblock at `:122-123` claims *"Only 10 of 119 live phases carry a real `phase.name`"*. Measured live
on `:54322`, 2026-08-02: **0 of 57 phases across the 27 well-formed `workflow_definitions` rows**
(145 of the 172 rows store `definition` as a double-encoded JSON string that no app read path can
parse). State the corrected figure with its measurement date, and note that the conclusion the
docblock draws — that this fallback is the DOMINANT node face, not an edge case — is *strengthened*
by the correction. Also note that Phase 187 adds a derived tier above it, so the sentence is no longer
the last stop before the raw type.

Also add `name_seeded_by_ai?: boolean` to `PhaseSpecJSON`, additive-optional in the same register as
`grounding_escalated` / `action_risk_armed`, with a JSDoc line saying it records PROVENANCE (the NL
generator wrote the stored `name`) and is read by the demote rule in `definitionOps.ts` — it is never
read by any resolver in this module.

Extend `phaseVocabulary.test.ts` with the five behaviours above. Include an explicit
"omitted context is byte-identical" test that calls `nodeTitle(phase)` and `nodeTitle(phase, {})` and
asserts both equal the shipped tier-3 result.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseSpine.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/phaseVocabulary.test.ts src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseSpine.test.tsx` passes; the `?raw` source guards in the two spine files are green (exactly one copy of the vocabulary).
    - `grep -c "10 of 119" frontend/src/components/workflows/phaseVocabulary.ts` returns 0.
    - `grep -c "0 of 57" frontend/src/components/workflows/phaseVocabulary.ts` returns at least 1.
    - `grep -n "name_seeded_by_ai" frontend/src/components/workflows/phaseVocabulary.ts` shows exactly one declaration, inside `PhaseSpecJSON`, and it is not referenced by any function in the file.
    - The frontend 8-file vocabulary set passes with a count ≥ 863: `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts src/components/workflows/canvasModel.test.ts src/components/workflows/canvasModel.purity.test.ts src/components/workflows/canvasModel.roundtrip.test.ts src/components/workflows/canvasModel.fixtures.test.ts src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseSpine.test.tsx src/components/workflows/PhaseNodeCard.test.tsx`
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>`nodeTitle` resolves four tiers, omitting the context is byte-identical, and the file no longer carries a refuted measurement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| definition JSONB (author-supplied, hand-editable) → client projection | a malformed row must not crash the canvas |
| id→name maps (server data) → rendered node face | a missing name must not leak a raw id onto the face |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-04-01 | Denial of Service | `nodeTitle` / `derivedFaceOf` | mitigate | CANVAS-01 totality: every read off `PhaseConfigJSON` is guarded by `Array.isArray` / `typeof`, and five malformed shapes are asserted not to throw. |
| T-187-04-02 | Information Disclosure | the node face | mitigate | An unresolved `skill_ref` or `folder_scope` id yields `undefined`, never the raw UUID — asserted by a test that the returned string does not contain the id. |
| T-187-04-03 | Tampering | second copy of the vocabulary | mitigate | The derivation lives only in `phaseVocabulary.ts`; the `?raw` guards in `PhaseSpineGraph.test.tsx` / `PhaseSpine.test.tsx` are run as an acceptance criterion. |
| T-187-04-04 | Spoofing | client-derived face used as authorization | accept | The face is display-only. Every governance read (`groundingCauseOf`, the run-time gate) is unchanged and server-defined; a wrong face is a display bug by construction. |
| T-187-04-05 | Input Validation (V5) | rendered strings | accept | Skill/folder names render as plain React text children downstream; React escapes text children and the raw-HTML prop is never used on the canvas. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/phaseVocabulary.test.ts src/components/workflows/canvasModel.test.ts src/components/workflows/canvasModel.purity.test.ts src/components/workflows/canvasModel.roundtrip.test.ts src/components/workflows/canvasModel.fixtures.test.ts src/components/workflows/PhaseSpineGraph.test.tsx src/components/workflows/PhaseSpine.test.tsx src/components/workflows/PhaseNodeCard.test.tsx` → ≥ 863 passed, 0 failed.
- `cd frontend && npx tsc -b` exits 0.
- Do NOT run the full frontend suite as a gate (measured 42–49 failures, flaky at one commit).
</verification>

<success_criteria>
- `nodeTitle` resolves four tiers with the D-187-04 precedence, and omitting the context is byte-identical.
- The derivation is pure, total, injected-context, and lives in exactly one module.
- The refuted "10 of 119" figure is replaced by the measured 0 of 57.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-04-SUMMARY.md` when done.
</output>
