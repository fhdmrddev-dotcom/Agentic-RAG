---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.describe.test.tsx
autonomous: true
requirements: [VOCAB-03]

must_haves:
  truths:
    - "The Builder's flag-OFF describe screen has a byte-identity markup pin, captured against the UNMODIFIED page BEFORE the template door ships (D-181-01)"
    - "An OPERATOR-LIKE visibility map produces the IDENTICAL markup — asserted as an equality against the same literal, never a second copied literal"
    - "The pin covers the region the template door lands in: the CTA and the describe-hint line beneath it"
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.describe.test.tsx"
      provides: "the D-181-01 flag-off describe-screen markup pin"
      contains: "FLAG_OFF_DESCRIBE_MARKUP"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.describe.test.tsx"
      to: "frontend/src/pages/WorkflowBuilderPage.tsx"
      via: "rendering the describe screen with visual_workflow_canvas OFF and pinning its outerHTML"
      pattern: "describe-hint"
---

<objective>
Pin the flag-OFF describe screen before anything is added to it.

Purpose: measured — `describeScreen` (`WorkflowBuilderPage.tsx:1403-1495`) is built on **both** flag
branches; only the `BuilderHeaderBar` wrapper at `:1483-1493` is flag-dependent. So Req 6's "one quiet
line under the CTA" would ship with `visual_workflow_canvas` OFF unless it is `canvasEnabled`-gated —
and **no shipped test would catch it**. `WorkflowBuilderPage.header.test.tsx` pins only the header.
D-181-01's promise is byte-identity for everyone, operators included; a regression there would ship
green.

Written first, against the UNMODIFIED page, so its later failure means a real regression rather than
a new test finding its feet — the discipline the shipped header pin states in its own docblock.

Output: `WorkflowBuilderPage.describe.test.tsx` with a normalised `outerHTML` literal for the
flag-off describe screen, green on HEAD.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-SPEC.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up the flag-off describe-screen render harness</name>
  <files>frontend/src/pages/WorkflowBuilderPage.describe.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx:1-120` — the docblock stating why the pin is written first, and the `vi.hoisted` enumeration of the WHOLE api surface (`mockGenerate`, `mockCreate`, `mockUpdate`, `mockListFolders`, `mockListSkills`, `mockValidate`, `mockBundle`, `mockPublish`, `mockListPublished`, `mockListStarters`, `mockListDrafts`, `mockDeletePreview`, `mockDeleteCascade`). **Reuse this exact enumeration — a factory mock that omits one symbol hands back `undefined` and the failure surfaces far from its cause.**
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx:250-330` — `OFF_VARIANTS`, the `openDraftBuilder` helper, the normalisation helper, and `FLAG_OFF_HEADER_MARKUP` at `:296-322`.
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1403-1495` — `describeScreen`: the `<textarea aria-label="business requirement">` at `:1416-1424`, the project picker at `:1430-1448`, the CTA at `:1450-1458`, and the `data-testid="describe-hint"` line at `:1459-1464`.
  </read_first>
  <action>
Create `frontend/src/pages/WorkflowBuilderPage.describe.test.tsx`.

Copy the header pin's harness shape: the same `vi.hoisted` api-surface enumeration, the same
`vi.mock("@/lib/api", …)` factory, the same flag-visibility variant fixtures, and the same
normalisation helper for `outerHTML` (whitespace-collapsing so formatting churn does not fail the
pin).

Add a `renderDescribeScreen(visibilityMap)` helper that renders `WorkflowBuilderPage` with
`visual_workflow_canvas` resolved **OFF** and with no draft loaded, so the page settles on the
describe screen (`builderPhase === "empty"`). Await the `data-testid="describe-hint"` node so the
mount effects that fetch folders and skills have settled before anything is captured — an unsettled
render would bake a loading shape into the pin.

Add a `describeRegion(container)` helper returning the normalised `outerHTML` of the region the
template door lands in: the element that contains the CTA button and the `describe-hint` line
(`WorkflowBuilderPage.tsx:1450-1464`'s flex column). Select it by walking up from the
`describe-hint` node to its parent element rather than by class name — a class-based selector would
silently start matching a different node after a Tailwind edit.

Write the docblock in the header pin's register, stating: (a) that this pin exists because the
describe screen renders on BOTH flag branches and only the header wrapper is flag-dependent; (b) that
it was captured against the UNMODIFIED page in Phase 187 wave 1, before the template door; (c) that
its later failure means the door leaked flag-off.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.describe.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pages/WorkflowBuilderPage.describe.test.tsx` passes with at least one test.
    - The file's `vi.hoisted` block declares every mock name the header pin declares, including `mockListStarters`.
    - `describeRegion` resolves its target by walking up from `screen.getByTestId("describe-hint")`, not by a CSS class selector.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>A flag-off describe-screen render settles deterministically and its CTA region is addressable.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the byte-identity literal and assert the operator-like map matches it</name>
  <files>frontend/src/pages/WorkflowBuilderPage.describe.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.describe.test.tsx` (as written in Task 1)
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx:296-322` — `FLAG_OFF_HEADER_MARKUP` and the two properties to carry across verbatim: the literal is deliberately verbatim rather than a shape assertion, and the operator-like variant is asserted against the SAME literal.
  </read_first>
  <action>
Capture the normalised `outerHTML` of the CTA region from the UNMODIFIED page and paste it into a
module-level `const FLAG_OFF_DESCRIBE_MARKUP`. It must be a verbatim literal, not a shape matcher: the
promise D-181-01 makes is about the markup a shipped user receives, and a "looks about right" matcher
cannot break when a wrapper is introduced or a line is added.

Add two tests:

  1. `matches the captured flag-off describe-screen CTA region byte for byte` — renders with the
     absent-key visibility map and asserts equality against the literal.
  2. `an OPERATOR-LIKE map produces the IDENTICAL markup (D-181-01, everyone included)` — renders with
     the operator-like variant and asserts equality against **the same literal**. Do not paste a
     second copy; two literals can drift apart, and the point is the equality.

Add a third, cheaper guard that does not depend on formatting: assert the flag-off describe screen
contains **no** element whose accessible name or text mentions a starter/template door. Express it as
a negative query over the rendered region for the words this phase will introduce (the picker's
trigger wording is fixed in plan 187-10's copy constants; here, assert the region's text does not
match `/template|starter/i`). This survives a whitespace change that would only churn the literal.

Do NOT modify `WorkflowBuilderPage.tsx` in this plan. The pin must be green on HEAD.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.describe.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pages/WorkflowBuilderPage.describe.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx` passes, with the header and canvas counts unchanged from HEAD.
    - `grep -c "FLAG_OFF_DESCRIBE_MARKUP" frontend/src/pages/WorkflowBuilderPage.describe.test.tsx` is at least 3 (declaration + two assertions) and the literal appears exactly ONCE.
    - The operator-like test asserts against `FLAG_OFF_DESCRIBE_MARKUP`, not against a second inline literal.
    - A test asserts the flag-off region's text does not match `/template|starter/i`.
    - `git status --porcelain frontend/src/pages/WorkflowBuilderPage.tsx` is empty — the page is unmodified.
  </acceptance_criteria>
  <done>The flag-off describe screen is pinned byte-for-byte on HEAD, for every visibility map, before anything is added to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature-visibility map → rendered surface | an operator-audience map must not produce a different flag-off surface than anyone else's (D-181-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-05-01 | Tampering | flag-off describe screen | mitigate | A verbatim normalised `outerHTML` pin captured against the UNMODIFIED page; plan 187-15's template door must keep it green or the door leaked flag-off. |
| T-187-05-02 | Elevation of Privilege | operator-like visibility map | mitigate | The operator variant is asserted against the SAME literal, so an operator-only affordance appearing on the flag-off screen fails the pin. |
| T-187-05-03 | Repudiation | the pin's own credibility | mitigate | `git status --porcelain frontend/src/pages/WorkflowBuilderPage.tsx` empty is an acceptance criterion — the pin cannot have been written to fit a change. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.describe.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx src/pages/WorkflowBuilderPage.test.tsx` passes.
- `cd frontend && npx tsc -b` exits 0.
- `git status --porcelain frontend/src/pages/WorkflowBuilderPage.tsx` is empty.
</verification>

<success_criteria>
- A flag-off describe-screen markup pin exists and is green on unmodified HEAD.
- It asserts the operator-like map against the same literal.
- It carries a formatting-independent negative guard for the words the template door will introduce.
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-05-SUMMARY.md` when done.
</output>
