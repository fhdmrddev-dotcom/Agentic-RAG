---
phase: 187-business-vocabulary-ai-seeded-canvas
plan: 28
type: execute
wave: 15
depends_on: ["187-27"]
gap_closure: true
gap_closure_round: 5
gap_closure_base: 15339441
files_modified:
  - backend/app/api/workflows.py
  - backend/tests/unit/test_187_route_assigned_reach.py
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
autonomous: true
requirements: [VOCAB-02]

must_haves:
  truths:
    - "D-187-11's comment states what the code does: a route-assigned code stays OUT of the SERVER publish gate but DOES reach the client's blockedReason, so it gates the Publish control"
    - "The blocking BEHAVIOUR is unchanged — an unbound retrieval workflow still disables Publish, which is what BUG-260731-03 asked for. Only the claim moves"
    - "The half of the old comment that IS true is pinned by a behavioural fence: unbound_retrieval never reaches grounding.grounding_verdicts and never becomes a server publish blocker"
    - "The half that was FALSE is pinned by a behavioural fence: an ok:false whose sole verdict is an incomplete route-assigned code yields a non-null blockedReason, verbatim"
    - "The grep over the module source is a regression PIN beneath the property, never the fence itself (the Phase-185 lesson: a deny-list cannot be made fail-closed by extension)"
  artifacts:
    - path: "backend/app/api/workflows.py"
      provides: "the corrected comment above _ROUTE_ASSIGNED_CODES"
      contains: "_ROUTE_ASSIGNED_CODES"
    - path: "backend/tests/unit/test_187_route_assigned_reach.py"
      provides: "the server-side property (route-assigned codes stay out of the shared collector) + the source pin beneath it"
      contains: "grounding_verdicts"
    - path: "frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx"
      provides: "the client-side property the old comment denied — a route-assigned verdict gates the Publish control"
      contains: "unbound_retrieval"
  key_links:
    - from: "backend/app/api/workflows.py"
      to: "backend/tests/unit/test_187_route_assigned_reach.py"
      via: "the test imports the module and asserts over its real sets and its real source"
      pattern: "_ROUTE_ASSIGNED_CODES"
---

<objective>
Close **GAP C** (minor, doc-only — the operator has already decided the disposition).

`backend/app/api/workflows.py`'s comment above `_ROUTE_ASSIGNED_CODES` claims the three
route-assigned codes are *"canvas-only"* and that *"Phase 187 is not scoped to change what
publishes"*. Measured false end-to-end in the live Builder: a draft whose SOLE verdict is
`unbound_retrieval` produces `publish-trigger.disabled === true` with the verdict message verbatim
beside it.

**Operator decision, 2026-08-04: the BEHAVIOUR is correct** — blocking an unbound retrieval workflow
is exactly what `BUG-260731-03` asked for. **The COMMENT is stale and must be corrected.** Do NOT
change the blocking behaviour.

Purpose: this is the same defect class as WR-14, which plan 187-24 closed hours earlier — a docblock
asserting a property the file does not have. A correction alone would be another unmeasured claim,
so both halves of the corrected sentence get a behavioural fence.

Output: a comment that is true, and two properties that keep it true.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-UAT.md
@.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-CONTEXT.md
@.planning/reported-bugs/BUG-260731-03-no-ui-to-rebind-workflow-knowledge-base.md

@backend/app/api/workflows.py

<read_first>
- `backend/app/api/workflows.py` — the comment block above `_ROUTE_ASSIGNED_CODES` (grep for
  `WHY THE ROUTE AND NOT`), the `_ROUTE_ASSIGNED_CODES` / `_INCOMPLETE_CODES` sets, `_severity`, and
  the `/validate` route's own docblock. **Re-derive the line numbers**; the UAT's `:470-473` was
  measured on 2026-08-04 and every executor in rounds 3-4 found an inherited coordinate false.
- `backend/app/services/harness/grounding.py` — `grounding_verdicts` and
  `GROUNDING_VERDICT_CODES`. You need to know, by reading, what that collector can emit — this is
  the TRUE half of the old comment and the fence must measure it rather than restate it.
- `backend/tests/unit/test_182_severity_codes.py` — the shipped emit-site scanner. Read it before
  writing a new file: if the property you need already exists there, EXTEND it and say so rather
  than shipping a second copy. Record which way you went and why.
- `frontend/src/pages/WorkflowBuilderPage.tsx` — the `blockedReason` memo as plan 187-27 leaves it.
  The `ok:false` branch's `verdicts.find(v => v.severity !== "incomplete") ?? verdicts[0]` is the
  exact line that makes an incomplete-only response block; your client fence asserts over it.
</read_first>

<interfaces>
Re-derive; these are shapes.

```python
# backend/app/api/workflows.py
_ROUTE_ASSIGNED_CODES: frozenset[str] = frozenset({
    "business_requirement", "interactive_phase", "unbound_retrieval",
})
_INCOMPLETE_CODES: frozenset[str] = frozenset({... "unbound_retrieval" ...})
```

```ts
// frontend — the client reach, measured live 2026-08-04
// validation = { kind: "verdicts", ok: false,
//                verdicts: [{ code: "unbound_retrieval", severity: "incomplete", phase: "<slug>",
//                             message: "<server's own words>" }] }
// ⇒ blockedReason === that message, verbatim  ⇒ publish-trigger.disabled === true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the comment says what the code does</name>
  <files>backend/app/api/workflows.py</files>
  <action>
Rewrite ONLY the *"WHY THE ROUTE AND NOT `grounding.grounding_verdicts`"* paragraph, PLUS the
one-line stale-count fix named below. Nothing else in this file changes — no set membership, no
severity, no route logic. Confirm that with `git diff -U0 -- backend/app/api/workflows.py` showing
comment lines only, and paste it.

**FOLDED IN by the round-5 plan-checker (verified independently before folding, 2026-08-04).** The
inventory line that reads ``_ROUTE_ASSIGNED_CODES`` — the 2 codes THIS route mints itself` is stale
by the same defect class this plan exists to close: the set has held **three** members since
D-187-11 landed `unbound_retrieval`, and a comment ~19 lines below it already says *"The three codes
the ROUTE mints itself"*. So the file currently contradicts itself about its own set size, in the
same block. Correct `the 2 codes` → `the 3 codes` in this SAME commit.

**Re-derive its position — do NOT trust this number.** It was measured at `:441` on 2026-08-04 in the
inventory block that also lists `reachability.LINT_CODES` and `grounding.GROUNDING_VERDICT_CODES`;
grep for `codes THIS route mints itself` rather than seeking a line. If the count is already correct
when you arrive, say so in the SUMMARY and change nothing — do not invent an edit to satisfy this
paragraph.

**A count is not a fence.** Fixing the digit does not stop it drifting again. Check whether
`tests/unit/test_182_severity_codes.py` (which the surrounding comment says SCANS the owning modules'
emit sites) already pins `len(_ROUTE_ASSIGNED_CODES)` or its membership. If it does, name the test in
the SUMMARY as the existing guard. If it does not, add that assertion — one line, the set's members
against the literal three codes — and observe it RED by removing a member before accepting it green.

The corrected paragraph must state, in the file's own voice:

1. **What stays true, and why the route is still the right home.** `grounding.grounding_verdicts` is
   SHARED with publish (182-06 made the publish gate enforcing through it), so a rule added there
   becomes a hard SERVER publish blocker. These three codes are deliberately not there, and the
   server's publish gate is genuinely unchanged by them.
2. **What was false and is now corrected.** These codes are NOT "canvas-only" from the author's
   seat. `/validate`'s `ok:false` reaches the Builder's `blockedReason`, which returns the first
   verdict's message for ANY not-ok envelope — including an `incomplete`-only one — and that lands
   on the Publish control's `disabled`. **So a route-assigned code DOES gate the Publish button.**
3. **That this is intended, not a leak.** Blocking an unbound retrieval workflow before a golden run
   is spent is precisely what `BUG-260731-03` asked for (operator decision, 2026-08-04). The
   distinction that survives is between the SERVER publish gate (unchanged) and the CLIENT publish
   CONTROL (gated) — name both, so the next reader cannot collapse them again.
4. **Where the claim is measured.** Name the two test files that hold the two halves, so the comment
   points at its own evidence rather than asserting a property on its own authority. This is the
   WR-14 lesson applied in the same shape 187-24 used.

The strings *"canvas-only"* and *"not scoped to change what publishes"* must not survive anywhere in
this module as a claim about the author's experience. ⚠ **Do not quote the old sentence verbatim to
explain it** — that satisfies the grep pin in Task 2 and defeats it (the exact conflict 187-25 hit).
Paraphrase the former claim, say in-source that the verbatim wording is not reproduced and why, and
point at the commit where it lives. History preserved, needle absent.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py tests/unit/test_182_severity_codes.py -q --no-header</automated>
  </verify>
  <done>The paragraph is rewritten; `git diff -U0 -- backend/app/api/workflows.py` shows comment lines
  only; the two shipped validate suites are green and their counts unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: both halves of the corrected sentence become properties</name>
  <files>backend/tests/unit/test_187_route_assigned_reach.py, frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx</files>
  <behavior>
    **Backend — the TRUE half, measured rather than restated.**
    - Every member of `_ROUTE_ASSIGNED_CODES` is absent from `grounding.GROUNDING_VERDICT_CODES` and
      is not emitted by `grounding.grounding_verdicts` for any input the test can construct. Quantify
      over the SET, never over one hand-picked code — a property, not an example.
    - Every member of `_ROUTE_ASSIGNED_CODES` is registered in exactly one severity bucket, so
      `_severity` never falls through to the fail-loud `error` default (re-derive; 187-03 may already
      assert this — if so, extend rather than duplicate and say which).
    - **The regression PIN, explicitly ranked BENEATH the property:** the module's source no longer
      contains the two stale claim phrases. Assemble the needles from parts so this test file's own
      source cannot satisfy them, and add a POSITIVE control proving each needle can match. Label it
      in-source as a pin, not the fence — a deny-list cannot be made fail-closed by extension (the
      Phase-185 lesson, and the reason WR-12 existed).

    **Frontend — the FALSE half, now pinned as the intended behaviour.**
    - A `verdicts` state with `ok:false` whose SOLE verdict is
      `{code:"unbound_retrieval", severity:"incomplete"}` yields a NON-NULL `publish-blocked-reason`
      equal to that verdict's `message` **verbatim**, and `publish-trigger` disabled.
    - A control with a mixed list (`error` + `incomplete`) still leads with the `error` finding —
      the shipped ordering, asserted so this case cannot be "fixed" later by dropping incompletes.
    - A control with `ok:true` and zero verdicts leaves Publish ENABLED — so the case above is
      provably about `ok:false` and not about the tray being non-empty.
    - Both are observed RED by making the `ok:false` branch skip `incomplete`-only responses — the
      exact "fix" someone would reach for after reading the old comment. Record that RED signature;
      it is the whole reason this fence exists.
  </behavior>
  <action>
Backend: prefer extending `tests/unit/test_182_severity_codes.py` if the property genuinely belongs
beside its scanner; create `tests/unit/test_187_route_assigned_reach.py` only if it does not. Decide
by reading, record the decision and the reason. Either way the file's docblock states the two-part
claim the corrected comment makes and which half this file owns.

Frontend: extend `WorkflowBuilderPage.canvas.test.tsx` (already the home of the publish-trigger and
`blockedReason` cases). Do NOT create a second page suite. Its per-file count is pinned at 22 in
`scripts/vitest-count-gate.cjs` and must only rise.

**`BUG-260731-03` — the reported-bugs touchpoint (CLAUDE.md, MANDATORY).** The report carries
`folded_into: "186 (control) / 187 (verdict)"`. Verify in this task, by reading the report and the
code, that the verdict half is covered — and record the finding in the SUMMARY:
- the verdict half shipped in 187-03 and is pinned here;
- **plan 187-26 materially advances the CONTROL half too** — the loose "Describe & run" door was the
  third creation path with no binding control, which is exactly what the report's `re_open_trigger`
  requires before it may close.

**Do NOT flip the report's `status` or write `verified_closed_by`.** Its trigger requires BOTH halves
verified LIVE, and this round performs no manual row. Leave the frontmatter untouched and say so.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./venv/Scripts/python.exe -m pytest tests/unit/test_187_route_assigned_reach.py tests/unit/test_182_validate.py tests/unit/test_182_severity_codes.py -q --no-header</automated>
  </verify>
  <done>Both properties observed RED before their fixes/pins with raw signatures recorded; the backend
  suite and `WorkflowBuilderPage.canvas.test.tsx` are green; the grep pin is labelled beneath the
  property and carries positive controls; `BUG-260731-03` frontmatter is untouched and the finding is
  recorded.</done>
</task>

<task type="auto">
  <name>Task 3: measure the gates</name>
  <files>backend/tests/unit/test_187_route_assigned_reach.py</files>
  <action>
Paste RAW output for each:

1. `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_route_assigned_reach.py tests/unit/test_182_validate.py tests/unit/test_182_severity_codes.py tests/unit/test_187_armed_checkpoint_property.py tests/unit/test_187_authoring_step_names.py -q --no-header` — 0 failed.
2. `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q --no-header` — record collected/passed/failed. ⚠ **The unit tree carries a measured pre-existing rot baseline** (187-VALIDATION §(d): 62 failed / 1693 passed, unchanged to the test). **Re-derive the baseline at the plan's base commit before editing** — do not inherit that figure; the criterion is *no NEW failure attributable to this plan*, and collection must not fall.
3. `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx` — 0 failed, count ≥ its pre-task value.
4. `node scripts/vitest-count-gate.cjs` — exit 0, `WorkflowBuilderPage.canvas.test.tsx` at or above its pin. **Never lower a pin to quiet a red**: re-run the file in isolation, record both results, log it (`D-ITEM-187-25-01` pattern).
5. `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — ⚠ the bare form is VACUOUS (`D-ITEM-187-23-02`). Baseline **33** (`D-ITEM-01`), none in this plan's files.
6. `git diff --stat 15339441 HEAD -- supabase/migrations` and `git status --porcelain supabase/migrations` — both EMPTY. This phase ships ZERO migrations and this plan changes no schema, no route behaviour and no persisted field.
7. `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` — EMPTY. Do NOT call `requirements.mark-complete`, `state.advance-plan` or `roadmap.update-plan-progress`.
8. `git diff --numstat 15339441 HEAD -- frontend/src/pages/WorkflowBuilderPage.tsx` — must be UNCHANGED from plan 187-27's recorded value (this plan spends none of the mount cap; it touches the page's TEST file only).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./venv/Scripts/python.exe -m pytest tests/unit/test_187_route_assigned_reach.py tests/unit/test_182_validate.py tests/unit/test_182_severity_codes.py -q --no-header</automated>
  </verify>
  <done>All eight gates measured with raw output; the backend unit baseline re-derived at the base
  commit rather than inherited; zero migrations; the mount cap unspent by this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `POST /workflows/validate` → browser | the server's verdict vocabulary crosses here; the client renders and never classifies |
| the publish gauntlet's server gate | the enforcing seam `grounding_verdicts` feeds — a rule added there blocks a real publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-R5-11 | Repudiation | the `_ROUTE_ASSIGNED_CODES` comment | mitigate | the claim is rewritten to match measured behaviour and both halves gain a behavioural fence, so the comment cannot drift back into an unmeasured assertion (WR-14's shape) |
| T-187-R5-12 | Elevation of privilege | `grounding.grounding_verdicts` | mitigate | a property test quantified over the whole route-assigned SET asserts none of them reaches the shared collector — the seam where a canvas verdict would silently become a hard publish blocker |
| T-187-R5-13 | Spoofing (of a fence) | the source grep pin | mitigate | ranked in-source BENEATH the property, needles assembled from parts, each with a positive control — a deny-list is a regression pin, never the fence (Phase-185 lesson) |
| T-187-R5-14 | Tampering | `backend/app/api/workflows.py` | accept | comment-only change, proved by a comment-lines-only diff; zero set membership, severity or route-logic edits |
| T-187-R5-SC | Tampering | pip/npm installs | mitigate | **ZERO package-manager installs.** If one becomes necessary, STOP — Package Legitimacy Gate + a blocking human checkpoint first |
</threat_model>

<verification>
- The corrected comment names both the server publish gate (unchanged) and the client publish control
  (gated), and points at the two files that measure them.
- `unbound_retrieval` is provably absent from the shared collector.
- An `incomplete`-only `ok:false` provably disables Publish with the server's own message.
</verification>

<success_criteria>
- [ ] `git diff -U0 -- backend/app/api/workflows.py` shows comment lines ONLY
- [ ] The stale claim phrases are absent from the module as claims about the author's experience,
      and are not reproduced verbatim anywhere that would satisfy the pin
- [ ] The server-side property is quantified over `_ROUTE_ASSIGNED_CODES`, not over one code
- [ ] The client-side property asserts a non-null `blockedReason` verbatim + a disabled trigger, with
      two controls (mixed severities, `ok:true`) proving it is not vacuous
- [ ] Both properties observed RED before their pins, with raw signatures recorded
- [ ] The grep pin is labelled beneath the property and carries positive controls
- [ ] `BUG-260731-03` frontmatter UNCHANGED; the touchpoint finding recorded in the SUMMARY
- [ ] Backend unit baseline re-derived at the base commit; no new failure; collection did not fall
- [ ] Zero migrations; the page mount cap unspent by this plan; zero SDK completion verbs called
</success_criteria>

<output>
Create `.planning/phases/187-business-vocabulary-ai-seeded-canvas/187-28-SUMMARY.md` when done.
</output>
