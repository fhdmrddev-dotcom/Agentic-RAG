---
phase: 185-graded-governance-per-node-grounding-mode-action-risk-dial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/PhaseNodeCard.tsx
  - frontend/src/components/workflows/canvasModel.ts
  - frontend/src/components/workflows/canvasModel.test.ts
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.tsx
autonomous: true
requirements: [GOVERN-02]

must_haves:
  truths:
    - "D-185-17 — the 137-D to 137-B card-geometry rebuild ships as its own separately-committed Wave-1 plan, before any governance mark is placed"
    - "The card renders 137-B: a 248px centred card with the 3D mark floating above its top edge"
    - "The verdict mark renders at the card's LEFT and overlaps no other rendered mark"
    - "A zone check over {icon, verdict, stepNumber} reports 0 overlaps between rendered marks"
    - "No governance signal (seal, grounding, armed) is added by this plan — it is geometry only"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      provides: "The 137-B card shape: centred 248px card, icon on the top edge, verdict mark at left"
      contains: "-left-2"
    - path: "frontend/src/components/workflows/canvasModel.ts"
      provides: "CANVAS_LAYOUT.NODE_MIN_HEIGHT raised 96 to 104"
      contains: "NODE_MIN_HEIGHT: 104"
    - path: "frontend/src/components/workflows/PhaseNodeCard.test.tsx"
      provides: "The zone-overlap geometry assertion (SPEC acceptance criterion 23)"
  key_links:
    - from: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      to: "frontend/src/components/workflows/canvasModel.ts"
      via: "CANVAS_LAYOUT import"
      pattern: "CANVAS_LAYOUT\\.NODE_MIN_HEIGHT"
---

<objective>
Rebuild `PhaseNodeCard` from the shipped 137-D geometry (icon at `left-0 top-1/2`, card inset `ml-6`,
260 × 96) to the operator-locked 137-B geometry (3D mark floating above a 248px centred card,
`padding-top` 42, `NODE_MIN_HEIGHT` 104), and move the VALID-03 verdict mark from `-right-2` to
`-left-2 top-1.5` so top-right is free for the governance seal that plan 185-09 places.

Purpose: SPEC Req 6's acceptance criterion 23 ("a zone check over {icon, verdict, seal, stepNumber,
run-state slot} reports 0 overlaps between rendered marks") **cannot pass on 137-D**. Computed against
the shipped card: the icon well occupies x 0…56 / y 20…76, and a verdict at `-left-2 top-1.5` occupies
x −8…14 / y 6…28 — a 14×8 px overlap. Worse, `ml-6` puts the card's left border at x = 24, so `-left-2`
does not even straddle the card edge. The SPEC's own sentence *"185 places its marks against 137-B"* is
only true if 137-B exists first. D-185-17 sequences this rebuild as Wave 1 of this phase, as its own
plan — a separately-planned, separately-committed unit is the opposite of "smuggled into a governance
phase".

Output: a 137-B `PhaseNodeCard`, an updated `CANVAS_LAYOUT`, corrected docblocks in two files that
already reason from 137-B on a component that renders 137-D, and a falsifiable zone-overlap test.

**This plan adds NO governance signal.** No seal, no grounded state, no armed mark. Geometry only.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-SPEC.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-CONTEXT.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-PATTERNS.md
@.planning/sketches/147-the-armed-mark/README.md
@.planning/sketches/themes/canvas-184.css

Invoke `Skill("sketch-findings-agentic-rag")` before touching the card.

<interfaces>
The 137-B geometry constants, from `.planning/sketches/themes/canvas-184.css:182-208` (this file is a
SKETCH theme, NOT shipped CSS — no `card-b` class is applied anywhere in `frontend/src`):

  body.card-b .node          { display:block; text-align:center; width:248px;
                               padding:34px 20px 20px; margin-left:0; margin-top:30px;
                               border-radius:22px; }
  body.card-b .node .icowrap { left:50%; top:-26px; transform:translateX(-50%);
                               width:62px; height:62px; }
  body.card-b .stepn         { top:12px; left:12px; right:auto; }
  body.card-b .acts          { top:auto; bottom:-12px; left:50%; transform:translateX(-50%); }
  body.card-b .vmark         { left:auto; right:-8px; top:6px; }

TWO AMENDMENTS from D-185-17 that OVERRIDE the CSS above — take these from CONTEXT, not from the CSS:
  - `padding-top` 34 → **42** (so the visible 52px mark clears the title by 11px, not 3px)
  - `NODE_MIN_HEIGHT` 96 → **104**

ONE AMENDMENT from SPEC Req 6 (operator, 2026-07-29) that OVERRIDES the CSS `.vmark` line:
  - the verdict mark moves to the card's LEFT (`-left-2 top-1.5`); top-right is CLAIMED for governance.
    The sketch theme keeps the verdict on the right under 137-B; the occupancy decision supersedes it.

The shipped layout table (`canvasModel.ts:62-77`) — NODE_WIDTH stays 260 (that is the NODE BOX);
the 248px is the CARD INSIDE that box:

  export const CANVAS_LAYOUT = {
    NODE_WIDTH: 260, NODE_MIN_HEIGHT: 96, PITCH_X: 320, LANE_Y: 0,
    SKIP_LANE_Y: 200, EDGE_ANCHOR_Y: 28, END_CAP_SIZE: 40,
  } as const satisfies Record<string, number>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the card to 137-B and move the verdict mark left</name>
  <files>frontend/src/components/workflows/PhaseNodeCard.tsx, frontend/src/components/workflows/canvasModel.ts, frontend/src/components/workflows/WorkflowCanvas.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseNodeCard.tsx` (whole file, 335 lines — the card being rebuilt)
    - `frontend/src/components/workflows/canvasModel.ts:54-80` (the `CANVAS_LAYOUT` frozen table and its "one table, no stray literals" docblock)
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:495-560` (the `PlaneEditingLayer` docblock at :502-504 that already reasons from 137-B, and `heightAt`)
    - `.planning/phases/185-.../185-PATTERNS.md` §10a and §10b (the CSS block verbatim + the mark-rendering analog)
    - `.planning/sketches/147-the-armed-mark/README.md` §"RESOLVED 2026-07-29" (the three locked decisions)
  </read_first>
  <action>
Change `CANVAS_LAYOUT.NODE_MIN_HEIGHT` from `96` to `104` in `canvasModel.ts`, keeping the frozen-table
form (`as const satisfies Record<string, number>`) and its one-line `/** … */` comment; extend that
comment to name D-185-17 and state that 104 is the 137-B floor giving the floating 52px mark 11px
clearance over the title. Leave `NODE_WIDTH` at 260 — that is the node BOX; the 137-B card is 248px
INSIDE it.

In `PhaseNodeCard.tsx`, replace the frosted-card inner `<div>`'s layout classes
(`ml-6 flex flex-col justify-center rounded-2xl border py-3 pl-10 pr-3`) with the 137-B form: a
centre-aligned block card of width 248px, horizontally centred inside the 260px node box
(`mx-auto w-[248px]`), `text-center`, `rounded-[22px]`, and padding `42px 20px 20px` expressed as
Tailwind arbitrary values (`pt-[42px] px-5 pb-5`). Keep `bg-card/30 backdrop-blur-sm`, the existing
shadow, and the existing `selected` border treatment byte-unchanged. Keep the outer node box at
`width: CANVAS_LAYOUT.NODE_WIDTH` / `minHeight: CANVAS_LAYOUT.NODE_MIN_HEIGHT` and keep the inner card's
`minHeight: CANVAS_LAYOUT.NODE_MIN_HEIGHT`.

Move the 3D-mark/icon-well block (currently `pointer-events-none absolute left-0 top-1/2 grid h-14 w-14
-translate-y-1/2 place-items-center`) to the 137-B floating position: horizontally centred on the card
and straddling its top edge — `left-1/2 top-[-26px] -translate-x-1/2`, size `h-[62px] w-[62px]`. Keep
every inner layer (the radial tint, the `inset-1` blur disc, the contact shadow, the `text-[20px]` glyph
wrapper) unchanged in kind; adjust only what the 62px well requires. The mark stays `aria-hidden` and
`pointer-events-none`. It overflows above the node box by 26px, which is the same overflow the verdict
mark already relies on — do not add `overflow-hidden` anywhere.

Change the verdict mark's position class from `-right-2 top-1.5` to `-left-2 top-1.5`. Leave its size
(`h-[22px] w-[22px]`), `z-[8]`, `rounded-full`, `pointer-events-none`, the `aria-hidden` glyph and the
`sr-only` label untouched.

Rewrite the verdict mark's docblock at `PhaseNodeCard.tsx:286-292`. It currently justifies *right* by
reasoning from 137-B on a component that renders 137-D. The new reasoning is lifetime, not taste: the
corner seal is a PERMANENT property of the step and keeps top-right (CLAIMED for governance by SPEC
Req 6); a verdict only exists when the server has returned a problem, so the transient mark moves left.
Record the residual verbatim: the moved verdict grazes the `stepNumber` slot by 2×16px and that slot
renders nothing (D-183-07); if `phase_index` is ever brought to the face, move the step number to
`left:16` and the graze clears.

Update the module docblock's "THE LOOK IS SKETCH 137-D" paragraph (`:50-57`) to say 137-B and describe
the floating-top mark, and correct the "TWO-BADGE BUDGET (137-D / D-183-07)" reference at `:32` to name
137-B. Do NOT change the badge budget itself — `BadgeSlot2Tuple` stays a max-2 tuple.

In `WorkflowCanvas.tsx`, rewrite the `PlaneEditingLayer` docblock paragraph at `:502-504`. It says
"Under 137-B … its RIGHT edge carries the verdict mark (184-08)" — the right edge no longer carries it.
State the current truth: under 137-B the top edge belongs to the floating icon, the LEFT edge carries
the verdict mark and the top-right corner is claimed for governance, so the `✕` sits on the bottom edge.
Change no code in `WorkflowCanvas.tsx` — docblock only.
  </action>
  <acceptance_criteria>
    - `grep -n "NODE_MIN_HEIGHT: 104" frontend/src/components/workflows/canvasModel.ts` returns exactly 1 line.
    - `grep -c "NODE_MIN_HEIGHT: 96" frontend/src/components/workflows/canvasModel.ts` returns 0.
    - `grep -n '\-left-2 top-1.5' frontend/src/components/workflows/PhaseNodeCard.tsx` returns exactly 1 line, and `grep -c '\-right-2' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0.
    - `grep -c 'ml-6' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0; `grep -c 'w-\[248px\]' frontend/src/components/workflows/PhaseNodeCard.tsx` returns at least 1.
    - `grep -c 'left-0 top-1/2' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0.
    - `grep -c 'pt-\[42px\]' frontend/src/components/workflows/PhaseNodeCard.tsx` returns at least 1.
    - The card contains no focusable element: `grep -cE '<(button|a)\b|tabIndex|onClick' frontend/src/components/workflows/PhaseNodeCard.tsx` returns 0.
    - `git diff --stat -- supabase/migrations` reports 0 files.
    - `cd frontend && npx tsc -b` reports no NEW errors naming `PhaseNodeCard.tsx`, `canvasModel.ts` or `WorkflowCanvas.tsx` (the repo baseline is 33 pre-existing errors; the differential must be 0 for these three files).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx src/components/workflows/WorkflowCanvas.test.tsx</automated>
  </verify>
  <done>The card renders 137-B geometry, the verdict mark is at the card's left, and all three docblocks state the shipped truth rather than an unbuilt card.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the geometry — the zone-overlap test and the layout-table assertion</name>
  <files>frontend/src/components/workflows/PhaseNodeCard.test.tsx, frontend/src/components/workflows/canvasModel.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx` (whole file — the provider-less render posture and the existing `?raw` source fences with their positive controls)
    - `frontend/src/components/workflows/canvasModel.test.ts:100-125` (the `CANVAS_LAYOUT` literal assertion that pins `NODE_MIN_HEIGHT: 96`)
    - `.planning/phases/185-.../185-VALIDATION.md` (acceptance criterion 23)
    - `.planning/phases/185-.../185-PATTERNS.md` §S7 (the `?raw` source-guard fence, always with a positive control)
  </read_first>
  <action>
Update the `CANVAS_LAYOUT` literal assertion in `canvasModel.test.ts` (currently `NODE_MIN_HEIGHT: 96`
at :108) to `104`. Do not add or remove any `it()` block in this file — its count is pinned at 26 in
`scripts/vitest-count-gate.cjs` and this plan must not move that pin.

Add a `describe("PhaseNodeCard — 137-B mark occupancy (SPEC criterion 23)")` block to
`PhaseNodeCard.test.tsx` implementing a BOUNDING-BOX zone check, which is how sketch 147 computed the
3×17px figure it reported. Build a module-scope table of the rendered marks' boxes derived from the
component's own position classes (not re-typed literals where the component reads `CANVAS_LAYOUT`):

  - icon well:   x = (NODE_WIDTH − 62)/2 … +62,  y = −26 … +36
  - verdict:     x = −8 … +14,                    y = 6 … +28   (`-left-2 top-1.5`, 22×22)
  - stepNumber:  x = 12 … +34,                    y = 12 … +34  (137-B `.stepn`; declared, unrendered)

Assert that every pair of boxes among {icon, verdict, stepNumber} has zero intersection area. Because
the seal (185-09) and the run-state slot (Phase 188) are not rendered by this plan, the check covers the
three marks that exist today; write the helper so adding a fourth box is a one-line table edit, and name
in a comment that 185-09 adds the seal box at top-right (x 227…248-relative, y 11…32) to this same table.

Add a FALSIFICATION control in the same file, in the shipped house style: a commented assertion block
(or a `it.skip`-free inline note) recording that the executor ran the check with the verdict temporarily
restored to `-right-2` / with the icon restored to `left-0 top-1/2` and observed it turn RED, and what
the reported overlap was. The falsification must be performed and its observed output recorded in the
plan SUMMARY — a zone check that has never been red is not evidence.

Also assert the card renders the verdict at the LEFT: query `canvas-node-verdict` and assert its
`className` contains `-left-2` and does not contain `-right-2`.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/PhaseNodeCard.test.tsx` exits 0 with the new occupancy describe block green.
    - `cd frontend && npx vitest run src/components/workflows/canvasModel.test.ts` exits 0 and reports exactly 26 tests (the pinned count is unchanged).
    - `node scripts/vitest-count-gate.cjs` exits 0 with every one of the 16 pinned files at delta 0 (`canvasModel.test.ts` must report 26).
    - The SUMMARY records the observed RED output of the falsification (the pre-rebuild placement producing a non-zero overlap), quoted.
    - `git diff --stat -- supabase/migrations` reports 0 files.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows && cd .. && node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Acceptance criterion 23 has a green, falsified automated proof, and the layout table's pin agrees with the shipped constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan is a pure client-side presentational geometry change. No input crosses any trust boundary; no server data is newly read, derived or rendered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-01-01 | Information disclosure | `PhaseNodeCard` authored strings | accept | Every authored string is already rendered as a plain React text child; this plan adds no `dangerouslySetInnerHTML` and the shipped XSS clause and its grep guard are unchanged (T-124-01 / T-184-03-01). |
| T-185-01-02 | Elevation of privilege | node focus surface | mitigate | A focusable element inside the card would create a second tab stop per node. Acceptance criterion greps for `<button`/`<a`/`tabIndex`/`onClick` returning 0, and `WorkflowCanvas.test.tsx:231-238` walks every rendered node. |

No package installs. No migration. No new dependency.
</threat_model>

<verification>
- `cd frontend && npx tsc -b` — no NEW error naming any file in `files_modified`.
- `cd frontend && npx vitest run src/components/workflows` — green.
- `node scripts/vitest-count-gate.cjs` — exit 0, all 16 pinned files at delta 0.
- `cd frontend && npx vite build` — exit 0.
- `git diff --stat -- supabase/migrations` — 0 files.
- `git diff --stat -- backend/` — 0 files (this plan is frontend-only).
</verification>

<success_criteria>
The shipped card is 137-B. The verdict mark is at the card's left and overlaps nothing. Top-right of the
card is empty and available to plan 185-09's governance seal. No governance concept was introduced.
</success_criteria>

<output>
Create `.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-01-SUMMARY.md` when done.
</output>

