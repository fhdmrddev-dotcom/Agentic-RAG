---
phase: 185-graded-governance-per-node-grounding-mode-action-risk-dial
plan: 08
type: execute
wave: 4
depends_on: ["185-06", "185-07"]
files_modified:
  - frontend/src/components/workflows/phaseVocabulary.ts
  - frontend/src/components/workflows/canvasModel.ts
  - frontend/src/components/workflows/PhaseNode.tsx
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/phaseVocabulary.test.ts
  - frontend/src/components/workflows/canvasModel.test.ts
  - frontend/src/components/workflows/WorkflowCanvas.test.tsx
  - frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [GOVERN-01, GOVERN-02]

must_haves:
  truths:
    - "D-185-11 — the synthesized gate is shown at author time as a locked row and never blocks publish; the gauntlet gains no new stage"
    - "D-185-19 — the locked gate row is CLIENT-synthesized from available_tools intersected with kb_tools; nothing waits on /validate, which returns problems and has no channel for a gate"
    - "The three retired badge strings appear nowhere in the workflow component tree"
    - "The canvas node data carries grounded and armed; the grounding word-badge slot is freed and left empty for 188/189"
    - "The vitest count-gate baseline is re-measured, not guessed, and lands in the same commit as the deletion"
  artifacts:
    - path: "frontend/src/components/workflows/phaseVocabulary.ts"
      provides: "GROUNDINGS and groundingFor deleted; waitsForYou retained"
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "gatesFor synthesizing the locked row from the tool intersection, plus the onGovernanceChange write and kbTools on the rails"
      contains: "onGovernanceChange"
    - path: "frontend/src/components/workflows/canvasModel.ts"
      provides: "PhaseNodeData.grounded and PhaseNodeData.armed replacing PhaseNodeData.grounding"
      contains: "grounded"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "re-measured pins for phaseVocabulary.test.ts and WorkflowCanvas.test.tsx"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/stores/builderStore.ts"
      via: "the onGovernanceChange useCallback calling store.getState().setGovernance"
      pattern: "setGovernance\\("
---

<objective>
Delete the shipped 3-face grounding word-badge, replace the author-time gate row with a client-synthesized
`🔒` row, thread `grounded` / `armed` onto the canvas node data, and wire the page's governance write.

Purpose: SPEC Req 6 says the word-badge is REMOVED (governance spends no colour and no badge slot; both
card badge slots are committed to 188/189). Its own docblock already names this phase as the replacer, so
the deletion is pre-authorized in the source. `gatesFor()` currently derives its row from `groundingFor`,
so the deletion and the page rewrite are ONE atomic unit — splitting them would leave the tree failing
typecheck.

D-185-19 is the correction this plan must honour: `POST /workflows/validate` returns
`ValidateResponse(ok, verdicts)` where a verdict is `{code, phase, message, severity}` — a list of
PROBLEMS, with no channel for "here is a gate that will run", and a passing gate produces nothing. **No
task here may expect `/validate` to return the locked row.** The client already holds `available_tools`
and (per D-185-09) the server's `kb_tools`, so the panel's row is synthesized locally — the same
client-predicts / server-enforces split D-185-09 established.

Output: `groundingFor` gone from ten call sites, a client-synthesized locked row, node data carrying the
two governance booleans, the page's write chain closed, and a re-measured count-gate baseline.

This plan renders NO new canvas mark. The seal lands in plan 185-09; the detour in 185-10.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-SPEC.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-CONTEXT.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-PATTERNS.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-06-SUMMARY.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-07-SUMMARY.md
@.claude/skills/sketch-findings-agentic-rag/references/graded-governance.md

Invoke `Skill("sketch-findings-agentic-rag")`.

<interfaces>
THE COMPLETE DELETION INVENTORY (re-verified 2026-07-29). The deletion must be TOTAL:

| File | Line(s) | What |
|---|---|---|
| `components/workflows/phaseVocabulary.ts` | 191-195, 223-230 | `GROUNDINGS` const + `groundingFor` — the deletion target |
| `components/workflows/canvasModel.ts` | 41, 196 | the import + `grounding: groundingFor(phase)` in `buildPhaseData` |
| `components/workflows/canvasModel.ts` | 118-121 | `PhaseNodeData.grounding` (the "Badge slot 1" comment) |
| `components/workflows/PhaseNode.tsx` | 170-176 | `badges: BadgeSlots = data.waitsForYou ? [grounding, waitsForYou] : [grounding]` |
| `pages/WorkflowBuilderPage.tsx` | 142, 293, 310 | the import + `gatesFor()`'s docblock + its call |
| `components/workflows/phaseVocabulary.test.ts` | 13, 30, 165-233 | ~10 `it()` blocks over `groundingFor` — PINNED AT 42 |
| `components/workflows/canvasModel.test.ts` | 317, 326, 329-330 | asserts every node carries `grounding` + `waitsForYou` — PINNED AT 26 |
| `components/workflows/WorkflowCanvas.test.tsx` | 300-304 | `expect(chip.textContent).toContain("No sources needed")` — PINNED AT 31 |
| `components/workflows/PhaseFormPanel.rails.test.tsx` | 112, 228, 248 | fixture labels `"Must cite its sources"` (not pinned) |

`waitsForYou` is **NOT** deleted — it is badge slot 2 and is unrelated to grounding.

What is being deleted (`phaseVocabulary.ts:191-230`):

  const GROUNDINGS = {
    strict: { mode: "strict", words: "Must cite its sources", glyph: "🔒" },
    flag:   { mode: "flag",   words: "Flags uncited claims",  glyph: "◐" },
    open:   { mode: "open",   words: "No sources needed",     glyph: "○" },
  } as const satisfies Record<Grounding["mode"], Grounding>

  export function groundingFor(phase: PhaseSpecJSON): Grounding { … }

What replaces `gatesFor` (`WorkflowBuilderPage.tsx:292-312`) — same signature, same all-rows-locked
reasoning, D-185-19 synthesis:

  function gatesFor(phase: PhaseSpecJSON | null): PhaseGateRow[] {
    if (phase === null) return []
    const grounding = groundingFor(phase)
    return grounding.mode === "open" ? [] : [{ label: grounding.words, locked: true }]
  }

The write chain to close (`WorkflowBuilderPage.tsx:1039-1049` is the shape to mirror):

  const onPhaseChange = useCallback((patch: PhaseConfigPatch) => {
    if (selectedSlug === null) return
    store.getState().patchConfig(selectedSlug, patch)
  }, [selectedSlug, store])

The count-gate pins to re-measure (`scripts/vitest-count-gate.cjs:57-91`) — `phaseVocabulary.test.ts: 42`,
`WorkflowCanvas.test.tsx: 31`, `canvasModel.test.ts: 26`, `BASELINE_TOTAL` 424. A DECREASE fails the gate;
an increase is allowed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the word-badge and re-pin the gate, in ONE commit</name>
  <files>frontend/src/components/workflows/phaseVocabulary.ts, frontend/src/components/workflows/phaseVocabulary.test.ts, frontend/src/components/workflows/WorkflowCanvas.test.tsx, frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/phaseVocabulary.ts:180-240` (`GROUNDINGS`, `groundingFor` and its docblock naming Phase 185 as the replacer, `waitsForYou`)
    - `frontend/src/components/workflows/phaseVocabulary.test.ts:1-40, 160-240` (the ~10 `groundingFor` blocks to remove and the file's suite shape)
    - `frontend/src/components/workflows/WorkflowCanvas.test.tsx:290-310` (the `"No sources needed"` chip assertion)
    - `scripts/vitest-count-gate.cjs:1-95` (the `BASELINE` map keyed by bare filename, `BASELINE_TOTAL`, the `TARGETS` glob list, and the `WorkflowBuilderPage.header.test.tsx` note explaining why a new suite is NOT added to `BASELINE`)
    - `.planning/phases/185-.../185-PATTERNS.md` §9 and §T5
    - `.planning/phases/185-.../185-RESEARCH.md` §Landmines L-9
  </read_first>
  <action>
Delete `GROUNDINGS` and `groundingFor` from `phaseVocabulary.ts`. Keep `waitsForYou` and the `Grounding`
type only if something still uses them after this plan — if nothing does, delete the type too and say so
in the SUMMARY. **There is no prior clean deletion of a vocabulary helper in this tree**; every earlier
phase in this family ADDED to this file. Do not pretend there is a template — follow the inventory above
exactly and treat completeness as the deliverable.

Remove the ~10 `it()` blocks over `groundingFor` from `phaseVocabulary.test.ts`. Remove the
`"No sources needed"` chip assertion from `WorkflowCanvas.test.tsx:300-304` — remove the ASSERTION, and
only delete the enclosing `it()` if nothing else in it survives; prefer keeping the block and rewriting
what it asserts about the node's chips. Replace the `"Must cite its sources"` fixture labels in
`PhaseFormPanel.rails.test.tsx` with the new `GOVERNANCE_GATE_ROW_LABEL` constant imported from
`definitionOps`.

**Then re-measure the pins.** Run `node scripts/vitest-count-gate.cjs`, read the reported per-file counts
for `phaseVocabulary.test.ts` and `WorkflowCanvas.test.tsx` from its output, and write those MEASURED
numbers into `BASELINE`. Recompute `BASELINE_TOTAL`'s trailing comment. **Never guess a pin.** The gate
exits 1 on `[count-decrease]`, and this edit MUST land in the SAME COMMIT as the deletion — that is the
Phase-177 lesson working as designed, and a separate commit would leave HEAD red.

Do NOT add `GovernanceSection.test.tsx` or `FlowEdge.test.tsx` to `BASELINE`. They postdate the 424 pin,
report as `new`, and are already inside the `src/components/workflows` target glob, exactly as
`WorkflowBuilderPage.header.test.tsx` documents at `:85-90`. `TARGETS` needs no edit.

Quote the gate's before/after per-file output in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -rn "groundingFor\|GROUNDINGS" frontend/src` returns 0 lines.
    - `grep -rn "Must cite its sources\|Flags uncited claims\|No sources needed" frontend/src` returns 0 lines (SPEC criterion 14).
    - `grep -c "waitsForYou" frontend/src/components/workflows/phaseVocabulary.ts` returns at least 1 — badge slot 2 is untouched.
    - `git show --stat HEAD` for the deletion commit lists BOTH `phaseVocabulary.ts` AND `scripts/vitest-count-gate.cjs` — the pin edit is in the same commit (L-9).
    - `node scripts/vitest-count-gate.cjs` shows NO `[count-decrease]`, NO `[total-below-baseline]`, NO `[missing-file]`, and no NEW failing test in a file this plan touched — the `[failing-tests]` reason is a KNOWN pre-existing block (40 rotted tests, SEED-056); see `185-VALIDATION.md` §"Count-gate posture" with no `[count-decrease]` and no `[total-below-baseline]`.
    - The SUMMARY quotes the measured per-file counts for `phaseVocabulary.test.ts` and `WorkflowCanvas.test.tsx`, and states that they were read from the gate's output rather than computed by hand.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows && cd .. && node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The three retired badge strings are gone from the tree and the measuring stick agrees with what actually runs.</done>
</task>

<task type="auto">
  <name>Task 2: Thread grounded and armed onto the node data; free badge slot 1</name>
  <files>frontend/src/components/workflows/canvasModel.ts, frontend/src/components/workflows/PhaseNode.tsx, frontend/src/components/workflows/canvasModel.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/canvasModel.ts:54-160` (`CANVAS_LAYOUT`, `PhaseNodeData` incl. the `grounding` field and its "Badge slot 1" comment, `CanvasEdgeData`)
    - `frontend/src/components/workflows/canvasModel.ts:186-200` (`buildPhaseData` — the named total resolver every face value goes through)
    - `frontend/src/components/workflows/canvasModel.ts:355-421` (`fromCanvas`'s CARRY-THROUGH-BY-REFERENCE docblock — it needs ZERO changes)
    - `frontend/src/components/workflows/PhaseNode.tsx:160-190` (the `badges: BadgeSlots` assembly)
    - `frontend/src/components/workflows/canvasModel.purity.test.ts:70-90` (the "node data must never alias the phase object" pin)
    - `.planning/phases/185-.../185-PATTERNS.md` §11
  </read_first>
  <action>
Replace `PhaseNodeData.grounding: Grounding` with two flat booleans:

    /** GOVERN-02 — this step must prove it (any of the three causes). Resolved once here.
     *  The canvas RENDERS this as SHAPE (the corner seal, plan 185-09), never as colour and
     *  never as a word-badge: both badge slots are committed to 188/189. */
    grounded: boolean
    /** GOVERN-03 — an action-risk checkpoint is armed on this step. Read-only on the canvas;
     *  arming happens in the panel (sketch 147: the panel is where you SET, the canvas is
     *  where you SEE). */
    armed: boolean

Resolve both inside `buildPhaseData` through a NAMED TOTAL FUNCTION, matching the module's contract that
every face value is resolved once at projection time. `grounded` needs the KB intersection, so
`toCanvas` must receive `kbTools` — thread it as an OPTIONAL argument on `toCanvas`'s existing options
object, defaulting to an empty list. Document the default explicitly: an unread palette marks NOTHING
rather than un-marking something, and the server enforces regardless (D-185-09). The module stays PURE
(D-183-12) — no DOM read, no clock, no randomness, no fetch: `kbTools` is passed IN.

Derive `grounded` in the same total order the server uses: a KB-tool intersection, else
`citation_policy === "strict"`, else `grounding_escalated`. Derive `armed` from `action_risk_armed`.
Copy values — never spread `phase` into `data` (`canvasModel.purity.test.ts:74-83` pins that node data
must never alias the phase object).

In `PhaseNode.tsx`, remove the grounding entry from the `badges` tuple. The assembly becomes
`data.waitsForYou ? [waitsForYou] : []`. **Do NOT fill the freed slot.** `BadgeSlots` is a max-2 tuple
union and a third badge is a typecheck error; deleting the grounding badge frees slot 1 for 188/189, and
SPEC Req 6 says governance spends no word-badge. Add a comment saying the slot is deliberately empty and
who it belongs to. Pass `grounded` and `armed` through to `PhaseNodeCard` only if the card's props
already accept them — otherwise leave them on the data object; plan 185-09 adds the card prop and the
render.

In `canvasModel.test.ts`, REWRITE the `grounding` assertions at `:317-330` onto the new fields rather than
deleting them — the file is pinned at 26 and a rewrite keeps the count. Add cases: a phase with a KB tool
yields `grounded: true`; a phase with only non-KB tools yields `grounded: false`; an `llm_emit` at
`citation_policy: "strict"` yields `grounded: true`; `grounding_escalated: true` with no tool yields
`grounded: true`; `action_risk_armed: true` yields `armed: true`; and `toCanvas` called with no `kbTools`
yields `grounded: false` for a KB-tool phase (the marks-nothing default).
  </action>
  <acceptance_criteria>
    - `grep -c "grounding:" frontend/src/components/workflows/canvasModel.ts` returns 0.
    - `grep -c "grounded: boolean" frontend/src/components/workflows/canvasModel.ts` returns 1 and `grep -c "armed: boolean" frontend/src/components/workflows/canvasModel.ts` returns 1.
    - `cd frontend && npx vitest run src/components/workflows/canvasModel.test.ts` exits 0 and reports exactly 26 tests or more; `node scripts/vitest-count-gate.cjs` reports `canvasModel.test.ts` at delta ≥ 0.
    - `cd frontend && npx vitest run src/components/workflows/canvasModel.purity.test.ts` exits 0 — node data still never aliases the phase object.
    - `grep -c "BadgeSlot2Tuple" frontend/src/components/workflows/PhaseNodeCard.tsx` still returns at least 1 — the max-2 fence is intact and slot 1 is left EMPTY.
    - `git diff frontend/src/components/workflows/canvasModel.ts` shows ZERO changes inside `fromCanvas` (`:403-421`) — the round trip needs none.
    - `cd frontend && npx tsc -b` reports no NEW error naming `canvasModel.ts` or `PhaseNode.tsx`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/canvasModel.test.ts src/components/workflows/canvasModel.purity.test.ts src/components/workflows/canvasModel.roundtrip.test.ts</automated>
  </verify>
  <done>The canvas node data carries the governance state as two booleans, resolved once, and badge slot 1 is empty.</done>
</task>

<task type="auto">
  <name>Task 3: The page — synthesize the locked row and close the write chain</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.tsx:130-150` (the imports, incl. `groundingFor` at :142)
    - `frontend/src/pages/WorkflowBuilderPage.tsx:285-340` (`gatesFor` and its docblock's "Phase 185 replaces this derivation" sentence)
    - `frontend/src/pages/WorkflowBuilderPage.tsx:740-765` (the `bundle` read, `toolOptions`, and the `rails` `useMemo`)
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1035-1055` (`onPhaseChange` — the `useCallback` shape to mirror)
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1550-1580` (the `<PhaseFormPanel …>` mount and the `{...(canvasEnabled ? { rails } : {})}` D-14 spread-conditional)
    - `.planning/phases/185-.../185-PATTERNS.md` §12
    - `.planning/phases/185-.../185-CONTEXT.md` D-185-11 and D-185-19
  </read_first>
  <action>
Drop the `groundingFor` import. Rewrite `gatesFor(phase)` to synthesize the locked row CLIENT-SIDE from
the same intersection the dial uses. It now needs the KB list, so give it a second parameter:
`gatesFor(phase: PhaseSpecJSON | null, kbTools: readonly string[]): PhaseGateRow[]`. Return
`[{ label: GOVERNANCE_GATE_ROW_LABEL, locked: true }]` when the phase's cause is `detected` or
`escalated`, and `[]` otherwise. Every row is LOCKED, and the shipped docblock's reasoning for that stays:
offering a Remove button that could not remove anything would be the "a removable gate with no way to
remove it" lie the row union exists to make unrepresentable.

Rewrite the docblock's "Phase 185 replaces this derivation" sentence into the present tense, and add
D-185-19's correction VERBATIM as the reason this is synthesized here rather than read off the server:
`POST /workflows/validate` returns `ValidateResponse(ok, verdicts)` where a verdict is
`{code, phase, message, severity}` — a list of PROBLEMS. It has no channel for "here is a gate that will
run", a passing gate produces nothing, and whether a future run will retrieve anything is unknowable at
author time. The client already holds `available_tools` and the server's `kb_tools`, so it predicts here
while the server enforces at run time. Also record D-185-11's publish half, which IS true and does bite:
the gauntlet gains no new stage, but its golden run drives `run_workflow` and therefore the enforcement
seam, so a detected step whose golden run retrieves nothing now blocks publish.

Add `kbTools` to the `rails` `useMemo`, read off the palette hook on BOTH honest readings
(`bundle.kind === "ready" || bundle.kind === "unavailable" ? bundle.kbTools : []`) — deliberately NOT
gated the way `toolOptions` is, because a degraded palette must not un-mark a locked step. Pass the same
list into `toCanvas` so the canvas and the panel read ONE value.

Add the governance write callback in `onPhaseChange`'s exact shape:

    const onGovernanceChange = useCallback(
      (patch: { grounding_escalated?: boolean; action_risk_armed?: boolean }) => {
        if (selectedSlug === null) return
        store.getState().setGovernance(selectedSlug, patch)
      },
      [selectedSlug, store],
    )

and pass it to `<PhaseFormPanel>` **inside the existing `{...(canvasEnabled ? { rails } : {})}`
spread-conditional**, not as a separate always-on prop — that spread is the D-14 mechanism for the
flag-off panel and adding a second unconditional governance prop would leak the canvas contract into the
Spine view.

Add cases to `WorkflowBuilderPage.canvas.test.tsx` (pinned at 22 — an increase is allowed): a selected
`llm_agent` with `search_documents` produces exactly one locked gate row labelled
`GOVERNANCE_GATE_ROW_LABEL`; the same phase with only non-KB tools produces zero rows; toggling the
dial calls `setGovernance` with the expected patch and the definition's other phases stay `toBe`-identical;
and — the D-185-19 guard — the page does NOT call `/validate` to obtain the row (extend the file's
existing request-spy or scope fence, with a positive control).
  </action>
  <acceptance_criteria>
    - `grep -rn "groundingFor" frontend/src` returns 0 lines.
    - `grep -c "onGovernanceChange" frontend/src/pages/WorkflowBuilderPage.tsx` returns at least 2 (the callback and the prop pass).
    - `grep -c "setGovernance" frontend/src/pages/WorkflowBuilderPage.tsx` returns 1.
    - The `onGovernanceChange` prop is passed inside the `canvasEnabled` spread-conditional — `grep -c "onGovernanceChange={" frontend/src/pages/WorkflowBuilderPage.tsx` returns 0 outside that spread (verify by reading).
    - The D-185-19 guard test passes: no request to the validate route is issued while deriving the gate row, with a positive control proving the spy can fire.
    - `cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.test.tsx` exits 0.
    - `node scripts/vitest-count-gate.cjs` shows NO `[count-decrease]`, NO `[total-below-baseline]`, NO `[missing-file]`, and no NEW failing test in a file this plan touched — the `[failing-tests]` reason is a KNOWN pre-existing block (40 rotted tests, SEED-056); see `185-VALIDATION.md` §"Count-gate posture", all 16 pinned files at delta ≥ 0.
    - `cd frontend && npx tsc -b` reports no NEW error naming `WorkflowBuilderPage.tsx`, and `npx vite build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages src/components/workflows && cd .. && node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The author sees the consequence of their tool choice as a locked row before running, the dial writes through the four-layer chain, and nothing waits on a route that cannot answer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server palette → client display | `kb_tools` drives what the author is TOLD; it drives no enforcement. |
| author dial → `PATCH /workflows/{id}` | The escalation bit leaves the browser inside the definition JSONB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-08-01 | Spoofing | the client-synthesized locked row | accept by design | D-185-19: the row is a PREDICTION rendered to the author. It authorizes nothing and blocks nothing. The gate that actually runs is synthesized server-side at the run seam and cannot be reached from the page. A wrong prediction is a display bug. |
| T-185-08-02 | Tampering | the removed word-badge | mitigate | A partial deletion is the real hazard: a stale `groundingFor` call site would keep asserting the OLD three-face reading beside the new one. The acceptance criteria grep the whole of `frontend/src` for both the function and the three retired strings, expecting 0. |
| T-185-08-03 | Tampering | the flag-off Spine surface | mitigate | The governance prop rides the existing `{...(canvasEnabled ? { rails } : {})}` spread-conditional, so a flag-off render is unchanged by construction (D-181-01 / D-14) and `PhaseFormPanel.rails.test.tsx` guards it. |
| T-185-08-04 | Repudiation | the test measuring stick | mitigate | Deleting ~10 `it()` blocks without re-pinning would silently lower the coverage floor. The gate fails on a decrease, and the pin edit is required to land in the same commit — verified by inspecting the commit's `--stat`. |

No package installs. No migration. Frontend-only plan.
</threat_model>

<verification>
- `grep -rn "groundingFor\|GROUNDINGS\|Must cite its sources\|Flags uncited claims\|No sources needed" frontend/src` — 0 lines.
- `cd frontend && npx tsc -b` — no NEW error naming any file in `files_modified`; `npx vite build` exit 0.
- `cd frontend && npx vitest run src/components/workflows src/pages` — green.
- `node scripts/vitest-count-gate.cjs` shows NO `[count-decrease]`, NO `[total-below-baseline]`, NO `[missing-file]`, and no NEW failing test in a file this plan touched — the `[failing-tests]` reason is a KNOWN pre-existing block (40 rotted tests, SEED-056); see `185-VALIDATION.md` §"Count-gate posture" — , no decrease, pins re-measured.
- `git diff --stat -- backend/ supabase/migrations` — 0 files.
</verification>

<success_criteria>
The 3-face word-badge is gone from the tree, badge slot 1 is empty and reserved, the author-time locked
row is synthesized where the data already is, and the governance write reaches the definition through the
one mutation home.
</success_criteria>

<output>
Create `.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-08-SUMMARY.md` when done.
</output>
