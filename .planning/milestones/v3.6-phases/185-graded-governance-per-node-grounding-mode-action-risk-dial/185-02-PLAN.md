---
phase: 185-graded-governance-per-node-grounding-mode-action-risk-dial
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/harness.py
  - backend/app/services/harness/grounding.py
  - backend/app/api/workflows.py
  - backend/tests/unit/test_185_detection.py
  - backend/tests/unit/test_harness_models.py
autonomous: true
requirements: [GOVERN-01, GOVERN-03]

must_haves:
  truths:
    - "D-185-06 — both governance fields sit at PhaseSpec level, outside the config union, as siblings of validators and name"
    - "D-185-07 — only the author's escalation INTENT is stored; detected and already-set are derived at read time, so a detected step set back to free-to-think has no representable value"
    - "D-185-08 — two flat optional booleans, not one nested object"
    - "D-185-09 — the KB tool list is SERVER-supplied on GET /workflows/grounding-bundle; the client only does the intersection"
    - "A pre-185 workflow_definitions JSONB row with neither new key still model_validate()s"
    - "Each of the 5 KB_TOOLS names, in isolation, detects a step as must-prove-it with cause detected"
    - "folder_scope alone never triggers detection; an llm_single never auto-locks"
    - "git diff -- supabase/migrations is 0 lines"
  artifacts:
    - path: "backend/app/models/harness.py"
      provides: "PhaseSpec.grounding_escalated + PhaseSpec.action_risk_armed; ValidatorSpec.kind gains action_risk_approval"
      contains: "grounding_escalated"
    - path: "backend/app/services/harness/grounding.py"
      provides: "KB_TOOLS, KB_TOOLS_SORTED, grounding_cause() — the one home for every grounding rule"
      contains: "KB_TOOLS"
    - path: "backend/app/api/workflows.py"
      provides: "GroundingBundleResponse.kb_tools on the shipped palette route"
      contains: "kb_tools"
    - path: "backend/tests/unit/test_185_detection.py"
      provides: "Acceptance criteria 4, 5, 6, 7, 8"
  key_links:
    - from: "backend/app/api/workflows.py"
      to: "backend/app/services/harness/grounding.py"
      via: "GroundingBundle.kb_tools read off the shared bundle, never computed in the route"
      pattern: "kb_tools=bundle\\.kb_tools"
---

<objective>
Add the two additive-optional governance booleans to `PhaseSpec`, register the `action_risk_approval`
validator kind in the `ValidatorSpec.kind` Literal, put `KB_TOOLS` and the pure `grounding_cause()`
derivation in `harness/grounding.py` (the D-182-06 one-rule-one-home module), and ship the KB tool list
to the client on the existing `GET /workflows/grounding-bundle` payload.

Purpose: this is the foundation both dials stand on. D-185-07's derive-don't-store choice is what makes
SPEC Req 3 ("no code path anywhere sets a detected step back to *free to think*") true **by
construction** rather than by a code audit someone must keep defending — there is no representable value
that says it, so a stale or hand-edited JSONB row cannot lie.

Output: two `bool = False` fields, one Literal member, one frozen `KB_TOOLS` set, one pure
`grounding_cause()` function, one additive `kb_tools` wire field, and the phase's cheapest test file.

Zero migrations. Zero engine behaviour change — nothing reads these fields yet (plan 185-03 attaches).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-SPEC.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-CONTEXT.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-RESEARCH.md
@.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-PATTERNS.md
@CLAUDE.md

<interfaces>
The shipped `PhaseSpec` (`backend/app/models/harness.py:189-194`) — the additive-optional precedent to
copy LITERALLY, including its trailing-comment convention (`<REQ/D id>` — `additive` — `pre-<phase> rows
validate with it absent`):

  class PhaseSpec(_StrictBase):
      slug: str
      phase_index: int
      config: PhaseConfig  # parsed via discriminator
      validators: list[ValidatorSpec] = Field(default_factory=list)
      name: str | None = None  # REQ-3 — additive; serializes into the definition JSONB; pre-103 rows validate with it absent

The shipped `ValidatorSpec.kind` Literal (`:178-182`) — 9 members today:

      kind: Literal[
          "json_schema", "regex_match", "workspace_file_exists", "programmatic",
          "citations_required", "freshness", "structure_check",
          "output_file_valid", "llm_judge_rubric",
      ]

The shipped `GroundingBundle` dataclass (`backend/app/services/harness/grounding.py:100-134`) carries
BOTH a JSON-friendly list and a membership set for the same values (`tools` / `tool_names`) — mirror
that shape for `kb_tools`.

The shipped `GroundingBundleResponse` (`backend/app/api/workflows.py:373-390`) — four flat independent
lists plus `degraded`; `degraded` is the closest prior additive extension and is the form to copy:

  class GroundingBundleResponse(BaseModel):
      tools: list[str] = Field(default_factory=list)
      folders: list[PaletteFolder] = Field(default_factory=list)
      skills: list[PaletteSkill] = Field(default_factory=list)
      template_placeholders: list[str] = Field(default_factory=list)
      degraded: list[str] = Field(default_factory=list)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The two PhaseSpec booleans and the action_risk_approval kind</name>
  <files>backend/app/models/harness.py, backend/tests/unit/test_harness_models.py</files>
  <read_first>
    - `backend/app/models/harness.py` (lines 1-240 — the module docblock, `_StrictBase`, the 6-member `PhaseConfig` union, `ValidatorSpec`, `PhaseSpec`, `WorkflowDefinition`'s 098 additive block at :228-235)
    - `backend/tests/unit/test_harness_models.py:20-60` (the `VALID_SEED` strict-parse fixture)
    - `.planning/phases/185-.../185-PATTERNS.md` §1 (analogs A/B/C and the comment convention)
    - `.planning/phases/185-.../185-RESEARCH.md` §Landmines L-11 and L-12
  </read_first>
  <action>
Add two fields to `PhaseSpec`, after `name`, under a `# ── Phase 185 … ──` section header in the form
`WorkflowDefinition`'s 098 block uses:

    grounding_escalated: bool = False   # GOVERN-01 / D-185-08
    action_risk_armed: bool = False     # GOVERN-03 / D-185-08

Both are `bool = False`, NOT `X | None = None` — D-185-08 requires absence to be unambiguous, and that
is the one deliberate deviation from `name`'s spelling. The section comment must carry three clauses:
(a) additive / zero-migration, pre-185 rows `model_validate()` with both absent; (b) D-185-07's
INTENT-ONLY rule — `detected` and `already-set` are DERIVED at read time from `config`, never stored, so
"a detected step set back to free-to-think" is not a representable value; (c) a pointer to
`harness/grounding.py` as the one home of the derivation.

Do NOT add anything to any `PhaseConfig` union member (L-12). The union's `extra="forbid"` would force
a 6× copy-paste and D-185-06 rejected it.

Add `"action_risk_approval"` to the `ValidatorSpec.kind` Literal. This is additive — old rows still
validate — and it is required because `ValidatorSpec` is `_StrictBase` and construction validates.

Correct the module docblock at `:15-16`, which says *"the two Literal sets remain LOCKED (do NOT change
them)"*. That note is stale: Phase 102 already grew this Literal from 4 to 9 members. Rewrite it the way
Phase 102 amended `ValidatorSpec`'s own docstring at `:173-175` — state that the Literal GROWS additively
(4 → 9 at Phase 102, 9 → 10 at Phase 185) and that growth is safe because every prior value still
validates; what must not change is an existing member's spelling.

In `test_harness_models.py`, add one `it`-equivalent test function proving SPEC acceptance criterion 1:
a `PhaseSpec` dict carrying NEITHER `grounding_escalated` NOR `action_risk_armed` parses without error
and both attributes read `False`. Add a second asserting a dict carrying both explicitly at `True`
round-trips through `model_dump(mode="json")` with both keys present. State in the test docstring the
side effect worth recording: `model_dump(mode="json")` now writes `"grounding_escalated": false,
"action_risk_armed": false` into every saved draft JSONB — additive and harmless (identical to what
`name: null` already does), but the first save after 185 is not a zero-diff save.
  </action>
  <acceptance_criteria>
    - `grep -c "grounding_escalated: bool = False" backend/app/models/harness.py` returns 1 and `grep -c "action_risk_armed: bool = False" backend/app/models/harness.py` returns 1.
    - `grep -c "action_risk_approval" backend/app/models/harness.py` returns at least 1 (inside the `kind` Literal).
    - `grep -v '^\s*#' backend/app/models/harness.py | grep -c "the two Literal sets remain LOCKED"` returns 0 — i.e. the stale sentence is gone from live code AND from prose (verify by reading the rewritten docblock, not only by grep).
    - No `PhaseConfig` union member gained a field: `git diff backend/app/models/harness.py` shows zero added lines between the `class Llm*PhaseConfig` / `class ProgrammaticPhaseConfig` declarations.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_harness_models.py -q` exits 0 with the two new tests green.
    - `git diff --stat -- supabase/migrations` reports 0 files.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_harness_models.py -x -q</automated>
  </verify>
  <done>Both booleans exist at PhaseSpec level, the new kind is in the Literal, the stale docblock is corrected, and a pre-185 row still parses.</done>
</task>

<task type="auto">
  <name>Task 2: KB_TOOLS and the pure grounding_cause() derivation</name>
  <files>backend/app/services/harness/grounding.py, backend/tests/unit/test_185_detection.py</files>
  <read_first>
    - `backend/app/services/harness/grounding.py:1-140` (the RED LINE docblock at :13-15, the MODULE-SHAPE DISCIPLINE at :41-46, the `GroundingBundle` dataclass at :100-134)
    - `backend/app/services/harness/grounding.py:640-780` (`_unregistered_skill_ref` and `business_requirement_missing` — the per-phase pure-rule and one-home-even-for-a-trivial-check forms)
    - `backend/tests/unit/test_182_validate.py:21-80` (the convention header and the `_definition` / `_llm_single` / `_llm_agent` builders to copy verbatim)
    - `.planning/phases/185-.../185-PATTERNS.md` §5 and §T1
    - `.planning/phases/185-.../185-RESEARCH.md` §Landmines L-13 (branch order is load-bearing)
  </read_first>
  <action>
Add to `harness/grounding.py`, under a `# ── Phase 185 (GOVERN-01 / D-185-09) — the KB-reading rule ──`
section header in the shipped two-blank-lines-around form:

    KB_TOOLS: frozenset[str] = frozenset({
        "search_documents", "query_documents", "read_document",
        "analyze_document", "get_related_documents",
    })
    KB_TOOLS_SORTED: list[str] = sorted(KB_TOOLS)

`KB_TOOLS_SORTED` is the wire form, mirroring how `GroundingBundle` already carries `tools: list[str]`
(JSON-friendly) beside `tool_names: set[str]` (membership) for the same values.

Add a pure, zero-I/O `grounding_cause(phase) -> str | None` returning `"detected"` / `"already-set"` /
`"escalated"` / `None`, reading union-member-specific fields with the shipped totality idiom
`getattr(phase.config, "<field>", None)`:

  1. `detected`     — `set(getattr(phase.config, "available_tools", None) or ()) & KB_TOOLS` is non-empty
  2. `already-set`  — `getattr(phase.config, "citation_policy", None) == "strict"`
  3. `escalated`    — `getattr(phase, "grounding_escalated", False)`
  4. otherwise `None`

**The branch ORDER is load-bearing and must not be reordered (L-13).** Checking `detected` FIRST is what
makes SPEC Req 3's "detection wins and the undo disappears" true by construction: an escalated step that
later gains `search_documents` reports `detected` and the stored `grounding_escalated: True` simply
becomes inert; removing the tool restores `escalated` and the undo. Say exactly this in the docstring,
and say that the function is pure with no DB pool — unlike most of this module, which touches the DB.
`folder_scope` is deliberately NOT an input (it exists on all five LLM config members, so reading it
would auto-lock steps that read nothing).

Add `kb_tools: list[str] = field(default_factory=list)` to the `GroundingBundle` dataclass with a
block comment above it naming D-185-09, stating that this is the safety-DEFINING constant (not a
per-caller registry read) and that it is populated UNCONDITIONALLY — including on the `degraded` path —
because a degraded folder/skill read must not silently un-mark a locked step. Populate it in
`assemble_grounding_bundle`'s single `return GroundingBundle(...)` at `:418` as
`kb_tools=KB_TOOLS_SORTED`, so the bundle assembler remains the one computation.

Create `backend/tests/unit/test_185_detection.py`. Copy `test_182_validate.py`'s convention header
(imports INSIDE the test bodies; `from __future__ import annotations`) and its `_definition` /
`_llm_single` / `_llm_agent` builders verbatim, adding `_llm_emit` and `_llm_batch_agents` in the same
shape. No route call and no monkeypatch is needed — `grounding_cause` is pure. Cover:

  - `@pytest.mark.parametrize` over all 5 `KB_TOOLS` names: each name IN ISOLATION on an `llm_agent`
    returns `"detected"` (criterion 4)
  - an `llm_agent` carrying only non-KB tools (e.g. `["execute_code", "web_search"]`) returns `None`;
    an `llm_single` (no `available_tools` at all) returns `None`; an `llm_emit` at
    `citation_policy: "strict"` returns `"already-set"` (criterion 5)
  - a phase with `folder_scope` set and `available_tools: []` returns `None` (criterion 6)
  - an `llm_agent` with `grounding_escalated: True` and no KB tool returns `"escalated"`; the SAME phase
    with `search_documents` added returns `"detected"` — the cause flips and the stored escalation bit
    becomes inert; removing the tool again returns `"escalated"` (criterion 7)
  - criterion 8 as a source-guard in the `canvasModel.purity.test.ts` idiom: read
    `grounding.py`'s source and assert there is no assignment to a `grounding_mode` / `grounding_cause`
    / `cause` FIELD anywhere (only the function's `return` statements produce a cause). Assemble the
    searched token from parts rather than spelling it, so the guard cannot be satisfied by making a
    neighbouring docblock lie (the D-ITEM-183-02 trap), and include a POSITIVE CONTROL that proves the
    guard can go red.
  </action>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_185_detection.py -q` exits 0 with at least 5 parametrized detection cases green.
    - `grep -c "KB_TOOLS" backend/app/services/harness/grounding.py` returns at least 3 (the frozenset, the sorted wire form, the bundle population).
    - `grep -rn "KB_TOOLS\|search_documents" backend/app/services/ --include=*.py | grep -v "harness/grounding.py" | grep -v tool_dispatcher | grep -c "KB_TOOLS"` returns 0 — the safety-defining list has exactly ONE backend home (D-182-06 RED LINE).
    - The source-guard test's positive control was observed RED and its output is quoted in the SUMMARY.
    - `grep -c "folder_scope" backend/app/services/harness/grounding.py` shows `folder_scope` is not read by `grounding_cause` (verify by reading the function body).
    - `git diff --stat -- supabase/migrations` reports 0 files.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_185_detection.py -x -q</automated>
  </verify>
  <done>Detection is a named list with one server home and a pure derivation whose branch order makes Req 3 structural.</done>
</task>

<task type="auto">
  <name>Task 3: Serve kb_tools on the shipped grounding-bundle route</name>
  <files>backend/app/api/workflows.py</files>
  <read_first>
    - `backend/app/api/workflows.py:335-420` (`PaletteFolder`, `PaletteSkill`, `GroundingBundleResponse` and the block-comment-above-the-field form used for `degraded`)
    - `backend/app/api/workflows.py:651-720` (the `/grounding-bundle` route and the `return GroundingBundleResponse(...)` tail with the `degraded=sorted(bundle.degraded or ())` comment)
    - `.planning/phases/185-.../185-PATTERNS.md` §6
    - `.planning/phases/185-.../185-RESEARCH.md` §"Discretion Items" D-3
  </read_first>
  <action>
Add `kb_tools: list[str] = Field(default_factory=list)` to `GroundingBundleResponse`, immediately after
`tools`, with a block comment above it in the shipped `degraded` form. The comment must state: the
decision id (D-185-09 / GOVERN-01); that the client intersects this with a phase's `available_tools` so
the dial, the strike-through and the seal move on the SAME render as a tool chip (no network hop for a
refusal reason); that the client NEVER enforces — the run-time gate at `harness_engine.py` is
unconditional and server-side, so a wrong client read is a display bug, never a safety hole; and that
this list is NOT filtered by `tools`, because it is the safety-DEFINING list and a degraded palette must
not silently un-mark a locked step.

Serialize it in the route's single `return GroundingBundleResponse(...)` as `kb_tools=bundle.kb_tools`
— read off the shared `GroundingBundle`, NEVER recomputed in the route. That is the same discipline the
`degraded` comment already enforces.

Do NOT convert `tools` to a list of objects. `tools: list[str]` is typed `string[]` on BOTH members of
`useGroundingBundle`'s state union and on `PhaseFormRails.toolOptions` (`string[] | "degraded"`);
changing it is a breaking change to a shipped, cacheable, near-static route.

Add a route-level test beside the existing grounding-bundle tests (or in `test_185_detection.py` if no
route suite exists for it) asserting the response carries all 5 `KB_TOOLS` names sorted, and that it
still carries them when `bundle.degraded` is non-empty.
  </action>
  <acceptance_criteria>
    - `grep -n "kb_tools" backend/app/api/workflows.py` returns exactly 2 code lines: the model field and `kb_tools=bundle.kb_tools`.
    - `grep -c "kb_tools=grounding.KB_TOOLS\|kb_tools=sorted(\|kb_tools=\[" backend/app/api/workflows.py` returns 0 — the route computes nothing.
    - `grep -c "tools: list\[str\] = Field" backend/app/api/workflows.py` still returns at least 1 — `tools` keeps its shipped shape.
    - `cd backend && venv/Scripts/python -m pytest tests/unit -q -k "grounding or 185"` exits 0.
    - `git diff --stat -- supabase/migrations` reports 0 files.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit -q -k "grounding or 185 or workflows"</automated>
  </verify>
  <done>The client can read the KB tool list from the server; the list still has exactly one home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `PATCH /workflows/{id}` | The two new `PhaseSpec` booleans arrive in an author-supplied definition JSONB and are parsed by an `extra="forbid"` Pydantic model. |
| server → client (`GET /workflows/grounding-bundle`) | `kb_tools` leaves the server as a fixed 5-element constant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-02-01 | Tampering | `PhaseSpec` governance state | mitigate | A client can only ever set `grounding_escalated` (an ESCALATION — strictly safe-direction). `detected` and `already-set` are derived server-side from `config`, so a hand-edited JSONB claiming "not detected" is unrepresentable (D-185-07). This is the structural half of Req 3. |
| T-185-02-02 | Tampering | `ValidatorSpec.kind` Literal | accept | The Literal grows additively; every prior value still validates. An unrecognised kind fails CLOSED in `run_gates` (`validators.py:233-241`), which is the shipped behaviour. |
| T-185-02-03 | Information disclosure | `kb_tools` payload | accept | The value is a fixed constant of five public tool names already present in the shipped `tools` palette. It discloses nothing about any user, org or document. |
| T-185-02-04 | Spoofing | client-side detection | accept by design | The client's intersection is PREDICTION only. Enforcement is unconditional and server-side (plan 185-03). A wrong client read is a display bug by construction. |

No package installs. No migration (`git diff -- supabase/migrations` must be 0 lines). Live head stays 113.
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` — green.
- `git diff --stat -- supabase/migrations` — 0 files.
- `git diff --stat -- backend/app/services/agent_loop.py backend/app/services/tool_dispatcher.py backend/app/services/openai_service.py backend/app/services/anthropic_service.py` — 0 files (D-14 fence).
- `git diff --stat -- frontend/` — 0 files (this plan is backend-only).
</verification>

<success_criteria>
Two additive-optional booleans exist at `PhaseSpec` level and pre-185 rows still parse. The KB-reading
rule has exactly one backend home and is served to the client as data. Nothing enforces anything yet.
</success_criteria>

<output>
Create `.planning/phases/185-graded-governance-per-node-grounding-mode-action-risk-dial/185-02-SUMMARY.md` when done.
</output>
