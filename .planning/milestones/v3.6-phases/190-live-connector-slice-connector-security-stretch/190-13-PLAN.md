---
phase: 190-live-connector-slice-connector-security-stretch
plan: 13
type: execute
wave: 5
depends_on: ["190-03", "190-06", "190-07", "190-08", "190-09", "190-10", "190-11"]
files_modified:
  - backend/app/services/harness/phase_types.py
  - backend/tests/test_harness_engine.py
  - backend/tests/unit/test_190_egress_ordering.py
  - backend/tests/unit/test_189_no_egress.py
autonomous: true
requirements: [CONN-02, CONN-03]

must_haves:
  truths:
    - "D-16 — the SEND is gated on ctx.is_golden_run INSIDE the executor: the send is skipped, the record is NOT, so _external_action_body remains the single composer of that sentence. test_a_golden_run_of_an_external_action_performs_no_egress was OBSERVED RED on the send and driven green by the gate in the SAME commit (CONN-03)"
    - "D-06 — the egress guard runs BEFORE credential resolution, unconditionally; a send with no credential bound raises the EGRESS REFUSAL, not a missing-credential error, and the recorded call order is asserted (CONN-03)"
    - "D-14 — the executor resolves through connector_service.resolve_connection(connection_id, org_id) using the RUN's org, never the id alone (CONN-03)"
    - "D-17 — ZERO new statuses and ZERO workflow_phases migration: sent = completed, send FAILED = failed, no connection bound (or a disabled one) = recorded_not_sent, decline unchanged. failed and recorded_not_sent stay distinguishable on all three axes (CONN-02)"
    - "D-18 — at-most-once: a failed send fails the phase and halts the run; there is no retry, no backoff and no queue anywhere on this path (CONN-02)"
    - "D-19 — the send happens AFTER the armed checkpoint and the executor still owns no waiting; 190 adds network I/O to the executor, not a second approval and not a second wait (CONN-02)"
    - "D-26 — with live_connectors off, an external_action step behaves EXACTLY as it does today: it records, it does not send, and it reads Not sent — recorded (CONN-03)"
    - "The _exec_external_action docblock's two now-FALSE invariants (this executor performs NO network I/O; NOTHING IN THIS FUNCTION MAY NAME A TRANSPORT) are rewritten in the SAME commit as the send — a stale invariant docblock is how this project ships lies"
    - "The harness_audit send receipt is written with event_type external_action_sent, the literal migration 117 added (CONN-02)"
  artifacts:
    - path: "backend/app/services/harness/phase_types.py"
      provides: "the ordered executor: golden-run gate, kill-switch, unbound terminal, egress, resolve, adapter, receipt"
      contains: "is_golden_run"
  key_links:
    - from: "backend/app/services/harness/phase_types.py::_exec_external_action"
      to: "backend/app/security/egress.py"
      via: "validate BEFORE resolve (D-06)"
      pattern: "egress"
    - from: "backend/app/services/harness/phase_types.py::_exec_external_action"
      to: "backend/app/services/connector_service.py::resolve_connection"
      via: "org-scoped resolution using the run's org"
      pattern: "resolve_connection"
---

<objective>
Replace `_exec_external_action` with the ordered send path — and drive the two latent defects
this commit creates from RED to green **inside this commit**.

Purpose: three orderings are what the tests must assert, because no amount of library quality
makes an ordering correct: **the golden-run check before the send** (D-16), **the guard before
the credential** (D-06), and **the org before the id** (D-14). This plan lands the first two at
the executor and consumes the third.

⚠ `test_a_golden_run_of_an_external_action_performs_no_egress` **passes today because the step
is inert. It goes RED on the commit that adds the send.** That is D-16's re-open trigger,
expressed as a check rather than as prose. **Observe it RED, then land the gate and drive it
green — in the same commit. Do not route around it and do not pre-emptively gate.**

Output: one rewritten function, one rewritten docblock, the audit receipt, and three recorded
RED→green observations.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-RESEARCH.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md
@.planning/phases/189-governed-external-action-node-model/deferred-items.md
</context>

<interfaces>
<!-- ⚠ CORRECTION #4, re-confirmed at HEAD 2026-08-08. CONTEXT and <canonical_refs> both say
     phase_types.py:1667-1903. MEASURED: the function is :1810-1885; the neighbourhood CONTEXT
     describes spans :1700-1903. RE-GREP BEFORE EDITING. -->

```
1705: RECORDED_INTENT_KEY = "recorded_intent"
1729: def _external_action_inputs(accumulated_outputs, ctx) -> dict
1778: def _external_action_body(capability, resolved) -> str
1810: async def _exec_external_action(phase, accumulated_outputs: dict, ctx) -> dict
1902:     "external_action": _exec_external_action,
```

**Both return keys are load-bearing.** `text` is what `_latest_phase_text` scans for;
`recorded_intent` is what the engine branches on at `harness_engine.py:1764-1774`:

```python
if _emit_failure:      await fail_phase(pool, phase_id, str(_emit_failure), output=durable_output)
elif _recorded_intent: await record_phase_not_sent(pool, phase_id, durable_output)
else:                  await complete_phase(pool, phase_id, durable_output)
```

**D-16 is ONE LINE with NO signature change.** `ctx` is a plain `SimpleNamespace` set at exactly
one site (`publish_service.py:840-868`, `is_golden_run=True`), passed unchanged through
`run_workflow` → `_run_phase_with_gates` → the executor, and the engine already reads it the
same way at `harness_engine.py:837`:

```python
_armed = getattr(phase, "action_risk_armed", False)
if _armed and getattr(ctx, "is_golden_run", False):
```

So the executor's gate is `if getattr(ctx, "is_golden_run", False):` — **skip the send, keep the
record** (D-16 shape 1), so `_external_action_body` stays the single composer of that sentence.
The engine's own comment at `:832-835` names both shapes and hands 190 the choice.

**The armed trigger, in the engine's own words (`harness_engine.py:825-830`):**
> *"It passes today BECAUSE the step is inert. THE DAY A REAL SEND IS ADDED IT GOES RED, on the
> publish path specifically, naming this comment. That is the re-open trigger, expressed as a
> check rather than as prose."*

**⚠ The docblock at `:1814-1829` becomes FALSE on this commit.** It asserts *"this executor
performs NO network I/O"* and *"NOTHING IN THIS FUNCTION MAY NAME A TRANSPORT, NOT EVEN TO DENY
IT"*. D-01 builds no MCP client, so `test_189_no_egress.py`'s `mcp` fence (Case A) stays GREEN
and untouched; the HTTP-client-name half is what 190 re-scopes deliberately, by moving every
transport name behind `connectors/` and `egress.py`.

**⚠ One open hazard (research §M6 / assumption A4):** `harness_engine.py:2205`
(`_build_resume_context`) builds a SECOND `SimpleNamespace` ctx that does NOT set
`is_golden_run`, so a resumed run defaults to live. Golden runs do not pause (D-19
auto-continues), so this should be unreachable — **assert that unreachability with a test rather
than assume it, and do NOT thread the flag into the second builder** (threading it widens D-16's
surface for a path that should not exist).

**The Windows trap** (`test_harness_engine.py:1143-1148`): build the event loop BEFORE
`_block_all_http(monkeypatch)` arms.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite _exec_external_action with the ordered gates — and observe W0-1 RED then green in this commit</name>
  <files>backend/app/services/harness/phase_types.py</files>
  <read_first>
    - `backend/app/services/harness/phase_types.py:1700-1918` — re-grep the function's real span; read `RECORDED_INTENT_KEY`, `_external_action_inputs`, `_external_action_body`, the current `_exec_external_action` INCLUDING its docblock, the closed-capability raise, and `PHASE_TYPE_REGISTRY_ENTRIES`
    - `backend/app/services/harness_engine.py:765-845` (the golden-run branch and its two-shapes comment) and `:1731-1885` (the `recorded_intent` branch, `record_phase_not_sent`, the ledger write and the `phase_recorded_not_sent` SSE)
    - `backend/app/services/harness/publish_service.py:830-875` — the ONE ctx that sets `is_golden_run=True`
    - `backend/app/services/connectors/registry.py`, `backend/app/services/connector_service.py`, `backend/app/security/egress.py`
    - `.planning/phases/189-governed-external-action-node-model/deferred-items.md` D-189-DEF-04 (the whole entry)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-06, D-13, D-16, D-17, D-18, D-19, D-26
  </read_first>
  <action>
Rewrite `_exec_external_action`, keeping its signature and BOTH return keys. The order below IS
the security property and each step is separately asserted:

1. **D-16 — the golden-run gate, FIRST.** `if getattr(ctx, "is_golden_run", False):` skip the
   send and fall through to the record. One line, no signature change, no engine edit. The
   record is still composed by `_external_action_body`, so one composer owns that sentence.
2. **D-26 — the kill-switch.** If the `live_connectors` feature audience resolves to `"off"`,
   skip the send and record. With the switch off the step behaves exactly as it does today —
   a genuine, already-tested state, which is what makes this off-switch cheap and honest rather
   than a second code path.
3. **D-13/D-17 — no connection bound (or a disabled one).** Skip the send and record →
   `recorded_not_sent`, *"Not sent — recorded"*. This is not a failure and must not read as one.
4. **D-06 — the egress guard, BEFORE any credential resolution.** Call
   `egress.validate_destination(...)` and let `EgressRefused` propagate as a phase failure
   carrying the reason code. **This call must textually and causally precede step 5.**
5. **D-14 — resolve, scoped by the RUN's org.** `connector_service.resolve_connection(
   connection_id, org_id=<the run's org>)`. Never the id alone.
6. **Dispatch** through `connectors.registry.get_adapter(capability).send(...)` with the
   validated destination, the resolved credential and the composed inputs.
7. **D-17 — the outcome, with ZERO new statuses.** Adapter `ok` → return WITHOUT
   `recorded_intent` so the engine calls `complete_phase`. Adapter failure or `EgressRefused` →
   surface as the engine's failure path so the phase lands `failed`, carrying the provider's
   words verbatim. **D-18: never retry.** A failed send fails the phase and halts the run.

**Input resolution is unchanged (D-09).** Keep `_external_action_inputs` exactly as it is —
run inputs plus the latest upstream phase text, with `kickoff_prompt` excluded by name. **Add no
expression language and no per-field templating surface.** Where a field must be composed, use
the shipped `SandboxedEnvironment(autoescape=True)` path or nothing at all. SC#3's proof is a
fence over an existing property, and the plan must say so rather than claim to have built it.

**D-19 stays true:** the send happens after the armed checkpoint and the executor still owns no
waiting. Add no `await` on a human, no second approval, no second pause.

**THE ORDER OF OPERATIONS FOR THIS TASK, and it is not optional:**
(a) write the send WITHOUT the D-16 gate;
(b) run `pytest tests/test_harness_engine.py -k golden_run_of_an_external_action` and **OBSERVE
IT RED**, recording the verbatim `_EgressAttempted` failure text;
(c) add the one-line gate;
(d) re-run and observe green.
**Both observations go in the summary. The RED observation IS the evidence.**
  </action>
  <acceptance_criteria>
    - The summary records the W0-1 RED observation VERBATIM (the `_EgressAttempted` message and the test name) followed by the green re-run, both in this commit
    - `cd backend && python -m pytest tests/test_harness_engine.py -q` reports `0 failed` and a total at or above the recorded baseline (46 on 2026-08-08 — re-measure)
    - `cd backend && python -m pytest tests/unit/test_publish_service.py -q` reports `0 failed` (a golden run still reaches `recorded_not_sent` carrying the real capability and a `NOT SENT` body)
    - `grep -c "is_golden_run" backend/app/services/harness/phase_types.py` is `>= 1`
    - `grep -ci "retry|backoff|sleep(" backend/app/services/harness/phase_types.py` shows no NEW hit versus HEAD~ on the external-action path (D-18)
    - `cd backend && python -c "
import inspect, app.services.harness.phase_types as p
s = inspect.getsource(p._exec_external_action)
print(s.index('egress') < s.index('resolve_connection'))"` prints `True` (D-06's ordering, asserted on source as well as behaviour)
    - `git diff --numstat backend/app/services/harness_engine.py` prints nothing (D-16 needs no engine edit)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_harness_engine.py tests/unit/test_publish_service.py -q</automated>
  </verify>
  <done>The executor sends behind seven ordered gates, the golden-run fence went RED on the send and green on the gate inside one commit, the engine is untouched, and nothing retries.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the now-FALSE docblock in the SAME commit, and assert the resume-context unreachability</name>
  <files>backend/app/services/harness/phase_types.py, backend/tests/test_harness_engine.py</files>
  <read_first>
    - `backend/app/services/harness/phase_types.py:1810-1835` — the current docblock, in full, so the two false sentences are quoted exactly before being replaced
    - `backend/tests/unit/test_189_no_egress.py:206-262` — Case A, the `mcp` token fence over `backend/app/**`, which must stay GREEN and untouched
    - `backend/app/services/harness_engine.py:2190-2220` — `_build_resume_context`, the SECOND `SimpleNamespace` ctx that does not set `is_golden_run`
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-RESEARCH.md` §M6 and assumption A4
  </read_first>
  <action>
**A. Rewrite the docblock.** Two of its sentences are now false: *"this executor performs NO
network I/O"* and *"NOTHING IN THIS FUNCTION MAY NAME A TRANSPORT, NOT EVEN TO DENY IT"*. Replace
them with what is true after this commit, and **quote the superseded sentences inline as
superseded**, dated, with the phase and decision that moved them — the same discipline plan
190-04 applies to the ROADMAP. A stale invariant docblock is how this project ships lies, and
erasing the old invariant hides that a promise changed.

The new docblock must state:
- the seven ordered gates, in order, with their decision ids
- that D-06's ordering is the n8n CVE class inverted, and that the fence which proves it is
  `tests/unit/test_190_egress_ordering.py`
- that this function names NO transport: every socket comes from `app.security.egress`, and the
  D-05 source fence over `backend/app/services/connectors/**` is what keeps that true
- that the `mcp` fence (Case A) is still green and why (D-01 builds no MCP client)

**B. Assert the resume-context unreachability (A4).** Add a test to
`backend/tests/test_harness_engine.py` proving a resumed run can never be a golden run — for
example, that the publish path never pauses, so `_build_resume_context` is never reached with a
golden run id. If the test shows it IS reachable, **stop and report**: a resumed golden run
would SEND, and that is a blocker, not a follow-up. **Do not thread `is_golden_run` into the
second ctx builder** — that widens D-16's surface for a path that should not exist.

**C. Confirm the `mcp` fence is untouched and green.** Run it and record the result; its passing
is part of D-01's amendment evidence.
  </action>
  <acceptance_criteria>
    - `cd backend && python -c "
import inspect, app.services.harness.phase_types as p
d = inspect.getdoc(p._exec_external_action)
print('NO network I/O' in d, 'superseded' in d.lower())"` prints `False True`
    - `grep -c "egress" backend/app/services/harness/phase_types.py` is `>= 2` (the function now names its one door, deliberately)
    - `cd backend && python -m pytest tests/unit/test_189_no_egress.py -k mcp -q` reports `0 failed`, and `git diff --numstat backend/tests/unit/test_189_no_egress.py` shows no change to Case A
    - A new test asserting the resume-context unreachability exists and passes; its name contains `resume` and `golden`
    - `git diff --numstat backend/app/services/harness_engine.py` prints nothing
    - `cd backend && python -m pytest tests/test_harness_engine.py -q` reports `0 failed`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_harness_engine.py tests/unit/test_189_no_egress.py -q</automated>
  </verify>
  <done>The docblock states the true invariants with the superseded ones quoted and dated, the resume path is proved unreachable for a golden run without threading the flag, and the `mcp` fence is untouched and green.</done>
</task>

<task type="auto">
  <name>Task 3: The send receipt, W0-3 green, and the sentinel's bound-connection case green</name>
  <files>backend/app/services/harness/phase_types.py, backend/tests/unit/test_190_egress_ordering.py, backend/tests/unit/test_189_no_egress.py</files>
  <read_first>
    - `backend/app/db/workflows.py:100-140` and `:1155-1200` — `_AUDIT_EVENT_TYPES` (now 24) and `write_audit`'s derived-count error
    - `supabase/migrations/117_harness_audit_external_action_sent.sql` (from plan 190-03)
    - `backend/tests/unit/test_190_egress_ordering.py` (from plan 190-01 — including its `TO BE RE-OBSERVED AT PLAN 190-13` note)
    - `backend/tests/unit/test_189_no_egress.py` — the bound-connection positive case plan 190-01 left RED
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §8a and §8b (the three axes that keep `failed` and `recorded_not_sent` distinguishable)
  </read_first>
  <action>
**A. Write the send receipt.** On a successful send, `write_audit(... event_type=
"external_action_sent" ...)` carrying the capability, the connection id and the destination
HOST — **never the credential, never the request body** (D-08). Assert in a test that the
receipt row's payload contains no secret substring.

**B. Drive W0-3's meaningful RED, as plan 190-01's note requires.** Reorder the executor so the
resolver call precedes the guard call, run `tests/unit/test_190_egress_ordering.py`, and
**observe the wrong error identity** — record the verbatim missing-credential failure. Then
restore the correct order and observe green. This is the n8n CVE class: the ordering is
invisible to any test that binds a credential, so it must be driven directly.

**C. Drive the sentinel's bound-connection positive case green.** Plan 190-01 added
`test_a_bound_connection_under_the_armed_sentinel_MUST_attempt_egress` and left it RED because
the executor was inert. It must now PASS — proving the send is genuinely live rather than
accidentally still inert. `_block_all_http` stays byte-identical; re-verify its md5 against the
hash plan 190-01 recorded.

**D. Assert D-17's three axes.** Add a test that a FAILED send and a `recorded_not_sent` step
differ on all three: the status written, the body's first line, and the `recorded_intent` key's
presence. Neither may ever read "Complete", and neither may borrow the other's word. Both are
"nothing arrived" and conflating them is the real risk.

**E. Re-run the full backend suite** and compare against the recorded baselines rather than
against any number in a document.
  </action>
  <acceptance_criteria>
    - The summary records the W0-3 wrong-order RED observation VERBATIM, followed by the green re-run
    - `cd backend && python -m pytest tests/unit/test_190_egress_ordering.py -q` reports `0 failed`
    - `cd backend && python -m pytest tests/unit/test_189_no_egress.py -q` reports `0 failed` (the bound-connection case now PASSES), and `_block_all_http`'s md5 matches the hash recorded in plan 190-01
    - `grep -c "external_action_sent" backend/app/services/harness/phase_types.py` is `>= 1`
    - A test asserts the audit payload contains no secret substring, and it passes
    - A test asserts `failed` and `recorded_not_sent` differ on status, first body line and `recorded_intent` presence, and it passes
    - `cd backend && python -m pytest tests/ -q` reports no NEW failures versus the baseline recorded at the start of this plan (record both numbers)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_190_egress_ordering.py tests/unit/test_189_no_egress.py tests/test_harness_engine.py -q</automated>
  </verify>
  <done>The send receipt is written without a credential, W0-3's wrong order was observed RED and corrected, the sentinel's bound case passes, and `failed` versus `recorded_not_sent` is proved distinguishable on three axes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publish (golden run) → the wire | D-16: this commit is the one that makes publish-time egress possible |
| the run's org → a connection id in the definition | D-14: the definition is authored data; the org is not |
| destination validation → credential resolution | D-06: whichever runs first decides whether the guard is bypassable |
| adapter outcome → the word on the card | D-17/D-31: a wrong mapping paints "Complete" for a send that did not leave |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-13-T10 | Tampering | golden-run send | mitigate | `getattr(ctx, "is_golden_run", False)` gate INSIDE the executor, shape 1 (skip the send, keep the record). The already-armed fence was OBSERVED RED on the send and green on the gate in the same commit |
| T-190-13-A4 | Tampering | `_build_resume_context` | mitigate | A test asserts a resumed run can never be a golden run; the flag is deliberately NOT threaded into the second ctx builder. If the test shows it IS reachable, the plan stops and reports a blocker |
| T-190-13-D06 | Tampering | guard-vs-resolver ordering | mitigate | The wrong order was driven and the wrong error identity observed; the correct order is asserted both behaviourally and on `inspect.getsource` index order |
| T-190-13-T8 | Elevation of privilege | credential resolution | mitigate | `resolve_connection(connection_id, org_id=<run's org>)`; `org_id` has no default, proved in plan 190-06 |
| T-190-13-T9 | Tampering | composed fields | mitigate | Input resolution is UNCHANGED — no evaluator is added. Where composition is needed the shipped `SandboxedEnvironment(autoescape=True)` is used. The SC#3 fence is driven in plan 190-14 |
| T-190-13-T13 | Repudiation | outcome mapping | mitigate | Only an adapter's own `ok` verdict produces `completed`; `failed` and `recorded_not_sent` are proved distinguishable on status, body first line and `recorded_intent` presence |
| T-190-13-T5 | Information disclosure | the audit receipt | mitigate | The receipt carries capability, connection id and destination HOST only; a test asserts no secret substring appears in the payload (D-08) |
| T-190-13-D18 | Repudiation | failure handling | mitigate | No retry, no backoff, no queue on this path — a duplicate email is worse than a missing one. Asserted by grep against HEAD~ |
| T-190-13-D26 | Tampering | kill-switch | mitigate | With `live_connectors` off the step records and does not send — the shipped, already-tested state, exercised in both directions by plan 190-09's test and by the executor's own branch test |
| T-190-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs. Acceptance: `git diff backend/requirements.txt` prints nothing |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_harness_engine.py tests/unit/test_publish_service.py tests/unit/test_189_no_egress.py tests/unit/test_190_egress_ordering.py -q` — `0 failed`
- `cd backend && python -m pytest tests/ -q` — no new failures versus the recorded baseline
- `git diff --numstat backend/app/services/harness_engine.py` prints nothing
- `git diff --numstat backend/requirements.txt` prints nothing
</verification>

<success_criteria>
- The send lands behind seven ordered gates, with D-16 and D-06 each proved by a recorded RED→green
- The now-false docblock is rewritten in the same commit, with the superseded invariants quoted and dated
- The sentinel's bound-connection case passes and `_block_all_http` is byte-identical
- Zero new statuses, zero engine edits, zero retries, zero installs
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-13-SUMMARY.md` when done, recording the W0-1 and W0-3 RED→green observations verbatim, the `_block_all_http` md5 comparison, and the before/after full-suite numbers.
</output>
