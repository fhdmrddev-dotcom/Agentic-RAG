---
phase: 190-live-connector-slice-connector-security-stretch
plan: 06
type: execute
wave: 2
depends_on: ["190-01", "190-03"]
files_modified:
  - backend/app/models/connector.py
  - backend/app/models/harness.py
  - backend/app/services/connector_service.py
  - backend/tests/unit/test_190_credentials.py
  - backend/tests/unit/test_190_cross_org_credential.py
autonomous: true
requirements: [CONN-03]

must_haves:
  truths:
    - "D-14 — resolve_connection scopes EVERY lookup by the RUN's org; the id-only resolver was authored FIRST, the leak was OBSERVED, and only then was the org scope added (CONN-03)"
    - "D-11 — the cipher is fail-CLOSED at BOTH ends: get_cipher() returning None refuses the STORE, and a stored value that is not an enc:v1: envelope refuses the READ; SECRET_COLUMNS is NOT extended and sweep_row is NOT reused (CONN-03)"
    - "D-13 — ExternalActionPhaseConfig gains ONE additive-optional field, connection_id, and its standing no-optionals comment block is extended with the justification in the SAME commit; no secret, no host and no token may enter the definition JSONB (CONN-03)"
    - "D-15 — the plan states, PER CALL SITE, whether the application org filter or RLS is the real gate: the API CRUD path runs on the user-JWT connection, the harness resolver runs on the BYPASSRLS pool where the application filter IS the gate and RLS is the backstop"
    - "D-03 — the three credential shapes are static tokens only (Slack bot token, Jira account email + API token, SMTP username/password); no OAuth authorization-code flow exists anywhere in this plan"
    - "D-08 — every log line from this service emits column NAMES and counts only; a sentinel secret appears zero times in captured logs, proved WITH a positive control that the capture works"
  artifacts:
    - path: "backend/app/services/connector_service.py"
      provides: "org-scoped resolve + CRUD + the fail-CLOSED cipher inversion"
      contains: "org_id"
    - path: "backend/app/models/connector.py"
      provides: "Pydantic request/response models; the response model NEVER carries secret_ciphertext"
    - path: "backend/tests/unit/test_190_credentials.py"
      provides: "D-11 both ends, T5 log discipline, T7 response-shape"
  key_links:
    - from: "backend/app/services/connector_service.py"
      to: "backend/app/security/secret_cipher.py"
      via: "encrypt_secret / decrypt_secret / is_encrypted / get_cipher"
      pattern: "encrypt_secret|decrypt_secret"
    - from: "backend/app/services/connector_service.py::resolve_connection"
      to: "public.connector_connections"
      via: "SELECT scoped by id AND org_id"
      pattern: "org_id"
---

<objective>
Build the credential layer: the Pydantic models, the org-scoped resolver, and the two-ended
fail-CLOSED cipher inversion — then drive plan 190-01's W0-2 cross-org leak RED against an
id-only resolver before landing the scoped one.

Purpose: **D-14 is not discretionary.** An `id`-only `SELECT` passes every ordinary test and
leaks a tenant's credential into another tenant's workflow. This milestone has the precedent
twice — SEED-124 / migration 110 and SEED-125 / migration 112. The leak must be OBSERVED, not
reasoned about.

Output: `connector_service.py` with a resolver whose `org_id` is a required parameter, a
response model that structurally cannot carry ciphertext, and a recorded leak observation.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md
</context>

<interfaces>
<!-- ⚠ CORRECTION #5, re-confirmed at HEAD: CONTEXT D-11 names `encrypt_value` / `decrypt_value`.
     THOSE FUNCTIONS DO NOT EXIST. Measured `backend/app/security/secret_cipher.py:244-252`: -->

```python
__all__ = ["SECRET_COLUMNS","get_cipher","is_encrypted","encrypt_secret","decrypt_secret","sweep_row","encryption_status"]
```

| Symbol | Verdict for 190 |
|---|---|
| `get_cipher() -> MultiFernet \| None` (`:74-83`) | **REUSE** — and this is the EXACT site of D-11's inversion |
| `encrypt_secret(plaintext, cipher) -> str` (`:91-94`) | **REUSE verbatim** — takes the cipher as an argument, so it cannot fail open on its own |
| `decrypt_secret(value, cipher) -> str` (`:97-104`) | **REUSE verbatim** at call time |
| `is_encrypted(value) -> bool` (`:86-88`) | **REUSE** — classify by prefix, never try-decrypt |
| `_ENVELOPE_PREFIX = "enc:v1:"` (`:43`) | read through `is_encrypted`; **never re-type the literal** |
| `sweep_row(row)` (`:107-169`) | **DO NOT REUSE** — hard-keyed to `SECRET_COLUMNS`, iterates ONE `app_settings` row |
| `SECRET_COLUMNS` (`:50-59`) | **DO NOT EXTEND** — it drives `main.py`'s boot sweep and a per-org per-row table cannot be swept that way (Pitfall 5) |

The read-side inversion already exists in the tree — `sso_provider_service.get_management_token`,
`:82-94`. **Copy `:82-92`. Do NOT copy `:93-94`**, which are the fail-OPEN half:

```python
if is_encrypted(raw):
    cipher = get_cipher()
    if cipher is None:
        logger.error("… is encrypted but no SECRETS_ENCRYPTION_KEY is configured — cannot decrypt (fail closed)")
        raise SsoProviderError("management token is encrypted but SECRETS_ENCRYPTION_KEY is not configured")
    return decrypt_secret(raw, cipher)
# No envelope: the value is plaintext at rest (fail-open D-150-01 — no master key). Return as-is.
return raw          # ← :93-94, THE POLARITY 190 INVERTS
```

The user-JWT path for API CRUD (`retrieval_service._call_as_user:37-52`) — *"Fail-closed: on a
service-role/owner connection `auth.uid()` is NULL → empty org set → 0 rows."*

`ExternalActionPhaseConfig` (`backend/app/models/harness.py:172-275`) — its
additive-optional / ZERO-MIGRATION docblock at `:181-188`, its field block at `:243-256`, and
⚠ its **standing refusal to add optionals** at `:258-265`, which 190 is the first to break and
must therefore extend in the same commit.

The no-existence-leak rule to inherit (`api/classification_rules.py:11-19`): *"Every
cross-user/unseeable miss collapses to a generic 404, NEVER the forbidden status."*
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The models — connector.py, and ONE additive-optional field on ExternalActionPhaseConfig</name>
  <files>backend/app/models/connector.py, backend/app/models/harness.py</files>
  <read_first>
    - `backend/app/models/classification_rule.py` — the house Pydantic CRUD model shape
    - `backend/app/models/harness.py:172-275` — `ExternalActionPhaseConfig`, `_StrictBase`, the additive-optional docblock, the field block, the standing no-optionals comment, and the `_available_tools_is_the_capability` validator's coerce-don't-raise posture
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-03, D-10, D-13
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md` §3.9
    - `supabase/migrations/116_connector_connections.sql` (from plan 190-03 — the column list is the source of truth for the models)
  </read_first>
  <action>
**A. `backend/app/models/connector.py`** — Pydantic models on `_StrictBase` (`extra='forbid'`),
in `classification_rule.py`'s shape:

- `ConnectorCapability` — reuse the closed `Literal["send_email","create_ticket","post_message"]`
  spelling `models/harness.py` already uses. Add a module-scope `assert` that its members equal
  `EXTERNAL_ACTION_CAPABILITIES` (imported from `harness/grounding.py`) — the two-spellings rule
  the codebase already records: the agreement is MECHANICAL, never remembered.
- `ConnectorConnectionCreate` — `capability`, `name`, `config` (a per-capability model, see
  below) and `secret` (the write-only plaintext, `str`, never echoed).
- Per-capability config models, so the NON-secret facts are typed rather than a free `dict`:
  `SendEmailConfig` (host, port, from_address, tls mode), `CreateTicketConfig` (base_url,
  project_key, account_email), `PostMessageConfig` (channel only — Slack takes **no**
  user-supplied URL, D-02). `extra='forbid'` on each is what stops a `smtp_password` key ever
  reaching `config`.
- `ConnectorConnectionUpdate` — every field optional; `secret` optional (a Replace).
- `ConnectorConnectionResponse` — **id, org-safe fields, capability, name, config, is_enabled,
  last_checked_at, last_check_verdict, created_at, updated_at. It has NO `secret_ciphertext`
  field and NO `secret` field.** T7's real gate is this model, not the TS type.

**B. `backend/app/models/harness.py`** — add ONE optional field to
`ExternalActionPhaseConfig`: `connection_id: str | None = None`, with a comment naming D-13,
CONN-03 SC#4 and what it is NOT (no secret, no host, no token — the one fact the executor needs
and the only one the JSONB may carry).

⚠ **Extend the standing no-optionals comment block (`:258-265`) in the SAME commit.** It is a
recorded refusal to add optionals, and 190 is the first to add one. Say why this one is
different: a reference id is not a shape-symmetry optional.

**Do NOT add a validator that resolves the id.** Follow the sibling validator's coerce-don't-
raise posture: an unresolvable `connection_id` is a RUN-time `recorded_not_sent` (D-17), never a
save-time 422. A stale client must not be able to brick the definition it is saving.
  </action>
  <acceptance_criteria>
    - `cd backend && python -c "from app.models.connector import ConnectorConnectionResponse as R; print([f for f in R.model_fields if 'secret' in f or 'cipher' in f])"` prints `[]`
    - `cd backend && python -c "from app.models.harness import ExternalActionPhaseConfig as C; print('connection_id' in C.model_fields, C.model_fields['connection_id'].is_required())"` prints `True False`
    - `cd backend && python -c "
from app.models.connector import PostMessageConfig
try:
    PostMessageConfig(channel='C1', base_url='https://evil.com'); print('LEAK')
except Exception: print('forbidden-extra OK')"` prints `forbidden-extra OK`
    - `cd backend && python -m pytest tests/unit/test_189_external_action_model.py -q` reports `0 failed` (an old JSONB row that never names `connection_id` still validates)
    - `grep -c "D-13" backend/app/models/harness.py` is `>= 1` and the standing no-optionals comment block now names `connection_id`
    - `grep -c "EXTERNAL_ACTION_CAPABILITIES" backend/app/models/connector.py` is `>= 1` (the assert is derived, not a retyped list)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_189_external_action_model.py -q</automated>
  </verify>
  <done>The models exist with per-capability `extra='forbid'` configs, a response model that structurally cannot carry ciphertext, and one justified additive-optional field whose standing refusal comment was extended in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: connector_service — author the id-only resolver FIRST, OBSERVE the leak, then scope it</name>
  <files>backend/app/services/connector_service.py, backend/tests/unit/test_190_cross_org_credential.py</files>
  <read_first>
    - `backend/app/services/classification_rule_service.py:1-30` — the "Security invariants" header block whose SHAPE this service copies
    - `backend/app/services/sso_provider_service.py:60-100` — the read-side fail-closed inversion; copy `:82-92`, NOT `:93-94`
    - `backend/app/services/retrieval_service.py:37-74` — `_call_as_user` and the `run_in_threadpool` incident comment
    - `backend/app/security/secret_cipher.py` — the whole file
    - `backend/tests/unit/test_190_cross_org_credential.py` — plan 190-01's W0-2 drive; it is the specification
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-CONTEXT.md` D-11, D-12, D-14, D-15
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md` §3.6
  </read_first>
  <action>
**STEP 1 — author the DEFECT deliberately.** Write `resolve_connection` with an **id-only**
lookup (`WHERE id = $1`, ignoring `org_id` even though the parameter is present). Run
`tests/unit/test_190_cross_org_credential.py` and **OBSERVE THE LEAK**: the org-B connection
resolves for an org-A caller and the non-vacuity control passes too, so an ordinary suite would
have shipped it. Record the observation verbatim — the failing test name and the assertion
text — in the plan summary AND in a `RED OBSERVED` comment above the resolver.

**STEP 2 — scope it.** Change the lookup to `WHERE id = $1 AND org_id = $2`. `org_id` is a
REQUIRED keyword parameter with no default. A miss raises `ConnectorNotFound` whose message
names neither the id's true owner nor the word `forbidden` — a cross-org id reads as ABSENT
(the `classification_rules.py:11-19` no-existence-leak rule). Re-run and observe green.

**STEP 3 — the rest of the service**, with a `classification_rule_service`-shaped
"Security invariants" header block naming D-14, D-11 and D-15:

- `create_connection(...)` — **D-11's write inversion.** Call `get_cipher()`; if it returns
  `None`, raise `ConnectorCipherUnavailable` and store NOTHING. `app_settings` tolerates
  plaintext at rest (D-150-01); a TENANT credential does not. Otherwise
  `secret_ciphertext = encrypt_secret(plaintext, cipher)`. Hard-set `org_id` from the caller
  rather than trusting an argument — the migration-116 trigger is defence in depth, not the gate.
- `update_connection(...)` — validates ownership FIRST so an unowned or absent id uniformly
  404s regardless of body validity (no 422-vs-404 ordering oracle). A `secret` Replace also
  resets `last_check_verdict` to `not_checked` in the SAME UPDATE (research OQ#4).
- `list_connections(org_id, capability=None)` and `get_connection(...)` — org-scoped, and they
  return rows through `ConnectorConnectionResponse`, never raw rows.
- `resolve_connection(connection_id, org_id)` — **D-11's read inversion.** If the stored value
  is not `is_encrypted(...)`, REFUSE — a plaintext tenant credential at rest is a refusal, not a
  passthrough. If it is encrypted and `get_cipher()` is `None`, REFUSE. Otherwise
  `decrypt_secret(raw, cipher)`. Return a small frozen result carrying the decrypted secret and
  the NON-secret config; give it a `__repr__` that redacts the secret so an incidental log or a
  traceback cannot print it.
- **DO NOT** touch `SECRET_COLUMNS` and **DO NOT** call `sweep_row`.

**STEP 4 — state D-15 per call site, in the module header.** The API CRUD path runs on the
per-request user-JWT connection (`_call_as_user`'s shape), where RLS is a real runtime gate.
The harness resolver runs on the service-role/BYPASSRLS pool, where **the application org filter
IS the gate and RLS is the backstop**. Write both sentences; do not assert "RLS covers it".

**Blocking I/O:** any `supabase-py` call from an async path goes through `run_in_threadpool`
(D-v2.5-01). `asyncpg` paths are already async.
  </action>
  <acceptance_criteria>
    - The summary records the STEP 1 leak observation verbatim, naming the test that PASSED while the resolver was id-only and the one that then failed
    - `cd backend && python -m pytest tests/unit/test_190_cross_org_credential.py -q` reports `0 failed`
    - `cd backend && python -c "import inspect
from app.services.connector_service import resolve_connection as r
p=inspect.signature(r).parameters['org_id']; print(p.default is inspect._empty)"` prints `True`
    - `grep -c "sweep_row\|SECRET_COLUMNS" backend/app/services/connector_service.py` prints `0`
    - `grep -c "enc:v1:" backend/app/services/connector_service.py` prints `0` (the envelope literal is read through `is_encrypted`, never retyped)
    - `grep -c "run_in_threadpool" backend/app/services/connector_service.py` is `>= 1` OR the module header states that every DB call on this path is already async and names the driver
    - The module header contains both D-15 sentences — one naming the user-JWT CRUD path, one naming the BYPASSRLS resolver path — verified by `grep -c "BYPASSRLS"` `>= 1`
    - `grep -c "forbidden" backend/app/services/connector_service.py` prints `0`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_190_cross_org_credential.py -q</automated>
  </verify>
  <done>The leak was observed against a real id-only resolver, the scoped resolver is green, `org_id` is structurally required, the cipher is fail-CLOSED at both ends, and D-15 is stated per call site.</done>
</task>

<task type="auto">
  <name>Task 3: test_190_credentials.py — D-11 both ends, T5 log discipline, T7 response shape</name>
  <files>backend/tests/unit/test_190_credentials.py</files>
  <read_first>
    - `backend/app/services/connector_service.py` (just written)
    - `backend/app/security/secret_cipher.py:26-28` and `:157-161`, `:216-225` — the "column NAMES + counts only" logging discipline, enacted
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-VALIDATION.md` rows T5, T6, T7
    - `backend/tests/unit/test_189_no_egress.py:206-232` — the positive-control-first idiom
  </read_first>
  <action>
Create `backend/tests/unit/test_190_credentials.py` with these cases, each RED-first against a
named plant in `connector_service.py`:

1. `test_the_store_is_REFUSED_when_no_cipher_is_configured` (D-11 write) — patch `get_cipher`
   to return `None`; assert `create_connection` raises `ConnectorCipherUnavailable` and that
   NOTHING was written (the row store stays empty). **Plant:** make the write fall through to a
   plaintext store; observe RED.
2. `test_a_plaintext_stored_value_is_REFUSED_on_read` (D-11 read) — a row whose
   `secret_ciphertext` carries no envelope must raise, never return the value. **Plant:** copy
   `sso_provider_service.py:93-94`'s `return raw`; observe RED. Name the plant in the test's
   docstring so the polarity difference from `app_settings` is legible.
3. `test_an_encrypted_value_with_no_key_is_REFUSED_on_read` — the second half of the read
   inversion.
4. `test_a_round_trip_stores_an_enc_v1_envelope_and_returns_the_original` — the non-vacuity
   control. Assert the stored string satisfies `is_encrypted` and is NOT the plaintext.
5. `test_T5_no_secret_substring_appears_in_any_log_line` — drive a refusal AND a resolve
   failure with a sentinel secret in scope; assert the sentinel appears **zero** times across
   captured log records, **and include a positive control** that deliberately logs the sentinel
   through the same capture and asserts it IS found — without it, a broken capture makes this
   test green forever. **Plant:** log the resolved row instead of `row["id"]`; observe RED.
6. `test_T7_the_response_model_cannot_carry_ciphertext` — assert `secret_ciphertext` and
   `secret` are absent from `ConnectorConnectionResponse.model_fields`, and that constructing
   one with `secret_ciphertext=` raises (because `extra='forbid'`). **Plant:** add the field to
   the model; observe RED.
7. `test_the_repr_of_a_resolved_credential_redacts_the_secret` — a traceback is a log line
   nobody planned.

Restore every plant md5-identical and confirm `grep -c PLANT` → 0 in both files.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_190_credentials.py -q` reports `0 failed` and at least 7 passing tests
    - The summary records SIX plant→RED→restore observations (cases 1, 2, 5, 6 plus the two read-inversion halves), each with its verbatim failure text
    - `grep -c PLANT backend/app/services/connector_service.py backend/app/models/connector.py backend/tests/unit/test_190_credentials.py` prints `0` for all three
    - `grep -c "positive control" backend/tests/unit/test_190_credentials.py` is `>= 1` and case 5's positive control asserts the sentinel IS found
    - `cd backend && python -m pytest tests/unit/ -q -k "190 or 189"` reports `0 failed`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_190_credentials.py tests/unit/test_190_cross_org_credential.py -q</automated>
  </verify>
  <done>Seven credential cases green, each proved by a plant observed RED in production source and restored md5-identical, with a working-capture positive control on the log test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org A run → org B credential row | D-14; crossed by an id-only `SELECT` that passes every ordinary test |
| database at rest → application memory | D-11; the `enc:v1:` envelope is the only acceptable stored form for a tenant credential |
| service internals → log sink / traceback | D-08; a repr is a log line nobody planned |
| API caller → connection row | RLS on the user-JWT connection; the application org filter on the BYPASSRLS pool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-06-T8 | Elevation of privilege | `resolve_connection` | mitigate | The id-only resolver was authored first and the LEAK OBSERVED; the scoped version requires `org_id` with no default, asserted via `inspect.signature`. A cross-org miss 404s as absent, never forbidden |
| T-190-06-T5 | Information disclosure | service logging + `__repr__` | mitigate | Sentinel-secret log test with a positive control proving the capture works; a redacting `__repr__`; plant = log the row instead of `row["id"]`, observed RED |
| T-190-06-T7 | Information disclosure | `ConnectorConnectionResponse` | mitigate | The field is absent from the model and `extra='forbid'` rejects it; plant = add the field, observed RED. The Pydantic model is the real gate, not the TS type |
| T-190-06-T6 | Information disclosure | `config` jsonb / definition JSONB | mitigate | Per-capability config models with `extra='forbid'`; a `smtp_password` key cannot be constructed. `ExternalActionPhaseConfig` gains a REFERENCE only |
| T-190-06-D11 | Information disclosure | `get_cipher() is None` | mitigate | Fail-CLOSED at BOTH ends — refuse the store, refuse a non-envelope read. Both plants (fall-through store; `sso_provider_service.py:93-94`'s `return raw`) observed RED |
| T-190-06-ORACLE | Information disclosure | `update_connection` | mitigate | Ownership validated FIRST so an unowned id 404s regardless of body validity — no 422-vs-404 ordering oracle |
| T-190-06-BLOCK | Denial of service | async handlers | mitigate | Any blocking driver call wrapped in `run_in_threadpool` (D-v2.5-01, the measured SEED-065 incident) |
| T-190-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs — `cryptography` arrives via the shipped `secret_cipher`. Acceptance: `git diff backend/requirements.txt` prints nothing |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/unit/test_190_credentials.py tests/unit/test_190_cross_org_credential.py tests/unit/test_189_external_action_model.py -q` — `0 failed`
- `grep -c "sweep_row\|SECRET_COLUMNS" backend/app/services/connector_service.py` — `0`
- `grep -rc PLANT backend/app/services/connector_service.py backend/app/models/connector.py` — `0`
- `git diff --numstat backend/requirements.txt` prints nothing
</verification>

<success_criteria>
- The cross-org leak was OBSERVED against a real id-only resolver and is now closed, with `org_id` structurally required
- The cipher is fail-CLOSED at write and at read, each half proved by a plant observed RED
- The response model structurally cannot carry ciphertext; the config models structurally cannot carry a password
- D-15 is answered per call site in the module header, naming which of RLS and the app filter is the gate where
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-06-SUMMARY.md` when done, recording the STEP-1 leak observation verbatim and all six plant→RED→restore cycles.
</output>
