---
phase: 190-live-connector-slice-connector-security-stretch
plan: 18
type: execute
wave: 7
depends_on: ["190-15", "190-17"]
files_modified:
  - frontend/src/components/settings/ConnectionFormPanel.tsx
  - frontend/src/components/settings/connectionRefusalCopy.ts
  - frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-02, CONN-03]

must_haves:
  truths:
    - "D-08 — the egress refusal names the refused HOST and the reason and NEVER the credential, the request body or the resolved secret; the closed six-row reason table is authored once and rendered from the guard's own reason code, never improvised at a call site (CONN-03)"
    - "UI-SPEC §4b — the binding asymmetry holds: an egress refusal leaves Save ENABLED (a refusal you can fix), and the no-encryption-key refusal DISABLES Save with aria-describedby pointing at the reason (a refusal you cannot fix) (CONN-03)"
    - "D-06 told in user-facing words — refusal block 8's third paragraph states that the refusal happened BEFORE the password was read; it is the one sentence that makes the n8n ordering property visible to a human, and it is not optional prose (CONN-03)"
    - "UI-SPEC §4d — refused, unreachable and rejected keep three headings, three glyphs and three next steps; a refusal must NEVER render the word failed and an unreachable host must NEVER render the word refused (CONN-02)"
    - "UI-SPEC §5b / U-07a — the check-failure sentence says the PICKER will not offer it, never an absolute verb, because Gate 2 validates org and is_enabled only; a grep fence forbids cannot / no step will be allowed / never bound (CONN-02)"
    - "UI-SPEC §5c — the check success headline is verbatim and its closing negation is picked from the SAME closed table 189 §9d ships, never improvised (CONN-02)"
    - "UI-SPEC §4a — banned from user-facing copy: SSRF, RFC1918, CIDR, link-local, NAT64, SIIT, TOCTOU, metadata endpoint, allow-list. The audience is an org admin, not an engineer"
  artifacts:
    - path: "frontend/src/components/settings/connectionRefusalCopy.ts"
      provides: "the closed six-row reason map plus the three §4d headings, as exported identifiers"
      contains: "address_not_public"
  key_links:
    - from: "frontend/src/components/settings/ConnectionFormPanel.tsx"
      to: "connectionRefusalCopy.ts"
      via: "rendered from the guard's reason code, never improvised"
      pattern: "REFUSAL_"
---

<objective>
Land the refusal copy, the check moments and the destructive guards on the panel — the part of
this surface where a wrong word is a security defect rather than a style choice.

Purpose: sketch 156 drew ONE refusal (a private address, on SMTP). `egress.py` can refuse for
**six** reasons across three capabilities, and there are three further outcome shapes the panel
must keep apart. UI-SPEC §4c authored the closed table so no sentence is improvised at a call
site; UI-SPEC §4d separated *refused* from *unreachable* from *rejected* because collapsing them
trains users to retry a security decision.

Output: the closed copy module, the two asymmetric refusal blocks, the three check moments, and
the suite that asserts every string by character-identity.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
</context>

<interfaces>
<!-- The closed six-row reason table (UI-SPEC §4c). These strings are the contract. -->

| Reason code | Heading (glyph ⛔) | Body sentence | Save |
|---|---|---|---|
| `address_not_public` | `That address was refused before anything was sent` | `{host} resolves to {ip} — a private address inside the network this platform runs on. Connections may only reach the public internet.` | enabled |
| `scheme_not_tls` | same | `{host} was given without encryption. Connections must be encrypted end to end — a plain http:// or smtp:// address is refused, including inside this network.` | enabled |
| `host_not_allowed` | same | `{host} is not a {vendor} address. A {capability_word} connection may only send to {allowed}.` | enabled |
| `host_not_ascii` | same | `{host} contains characters that can be made to look like another address. Type the address using plain Latin letters.` | enabled |
| `unresolvable` | `That address could not be looked up` | `Nothing on the internet answers to {host}. Check the spelling — nothing was sent, and your password was never read.` | enabled |
| `redirected` | same as the first | `{host} answered by pointing somewhere else. Connections follow no redirects, so the address you type is the only address that is ever contacted.` | enabled |

**Every row closes with the same audit line**, unchanged:
`Recorded as a refusal: capability, host and reason — never the credential.`

`{capability_word}` ∈ `email` · `ticket` · `message` (never the wire id). `{allowed}` is rendered
from the guard's own allow-list, never re-typed in the client.

⚠ **`unresolvable` is deliberately worded as a lookup failure, not a security refusal** — it is
the one row where the person genuinely made a typo, and calling it a refusal would train the
distrust §4a-3 forbids.

**Refusal block 9 (no encryption key), verbatim** — the asymmetry's other half:
```
⛔  This platform cannot store a credential safely yet

    No encryption key is configured, so a token saved now would sit in the database
    as readable text — and it would be your organisation's token, not ours.

    What this costs you: no connection can be created until it is set, so every
    external_action step keeps reading “Not sent — recorded”.

    To lift it: an operator sets SECRETS_ENCRYPTION_KEY and restarts the backend.
    Nothing you have typed here is sent anywhere in the meantime.
```

**§4d — three headings, three glyphs, three next steps:**

| | Glyph | Heading | Next step offered |
|---|---|---|---|
| Refused | `⛔` | `That address was refused before anything was sent` | `Correct the host and try again.` |
| Unreachable | `✕` | `That host did not answer` | `The address is allowed — nothing answered on it. Check the host and port, then check again.` |
| Rejected | `✕` | `The host rejected this credential` | the verbatim vendor error |

**§5b's corrected sentence — binding, replacing sketch 156 moment 6's:**
> `The connection is saved. The picker will not offer it while it is failing, and a step already bound to it will fail on the next run rather than pretend.`

Each clause maps to exactly one shipped mechanism: clause 1 → Gate 1 (client); clause 2 → run
time (D-17). **Any future edit that restores an absolute verb — *cannot pick*, *no step will be
allowed*, *never bound* — is a defect (U-07a, §14).**
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: connectionRefusalCopy.ts — the closed six-row map and the three §4d headings</name>
  <files>frontend/src/components/settings/connectionRefusalCopy.ts</files>
  <read_first>
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §4a, §4b, §4c, §4d, §11d (the copy table) — these are the strings, verbatim
    - `frontend/src/components/workflows/GovernanceSection.tsx:10-17` — the "authors no sentence of its own" docblock and why refusal copy lives as imported identifiers asserted by character-identity
    - `backend/app/security/egress.py` — `REFUSAL_REASONS`, so the client map is keyed off the same six codes the guard emits
  </read_first>
  <action>
Create `frontend/src/components/settings/connectionRefusalCopy.ts` — a copy module with **no
JSX, no state and no fetch**. It exports:

- `REFUSAL_HEADINGS` — the three §4d headings with their glyphs (`⛔ refused`, `✕ unreachable`,
  `✕ rejected`) and their three next-step sentences.
- `REFUSAL_REASON_BODIES` — the closed six-row map keyed by reason code, each a template
  function taking `{host, ip, vendor, capability_word, allowed}` and returning the §4c sentence.
- `REFUSAL_AUDIT_LINE` — the one line every row closes with, unchanged.
- `CIPHER_UNAVAILABLE_BLOCK` — refusal block 9's four paragraphs, verbatim.
- `CHECK_SUCCESS_HEADLINE` and `CHECK_NEGATION_BY_CAPABILITY` — the §5c headline and the closed
  negation table (`No mail was delivered to anyone.` / `No ticket was created.` /
  `No message was posted.`), which is the SAME closed table 189 §9d already ships. **Import or
  re-derive it from the shipped source rather than re-typing it**, so the two cannot drift.
- `CHECK_FAILURE_SENTENCE` — §5b's corrected middle paragraph, exactly.

**Add a module-scope assertion** that `Object.keys(REFUSAL_REASON_BODIES)` equals the six codes
`egress.py` declares. If the guard grows a seventh code, the client must fail loudly rather than
render an empty string at a person who is trying to fix something.

**The banned-vocabulary rule is enforced in this file's own test** (Task 3): none of `SSRF`,
`RFC1918`, `CIDR`, `link-local`, `NAT64`, `SIIT`, `TOCTOU`, `metadata endpoint` or `allow-list`
may appear in any exported string. The audience is an org admin configuring a connection, not an
engineer reading a threat model.
  </action>
  <acceptance_criteria>
    - `grep -c "export const" frontend/src/components/settings/connectionRefusalCopy.ts` is `>= 6`
    - All six reason codes appear as keys: `grep -cE "address_not_public|scheme_not_tls|host_not_allowed|host_not_ascii|unresolvable|redirected" ` is `>= 6`
    - `grep -ciE "SSRF|RFC1918|CIDR|link-local|NAT64|SIIT|TOCTOU|metadata endpoint|allow-list" frontend/src/components/settings/connectionRefusalCopy.ts` prints `0`
    - `grep -ciE "\bcannot\b|no step will be allowed|never bound" frontend/src/components/settings/connectionRefusalCopy.ts` prints `0`
    - The file contains the literal audit line `Recorded as a refusal: capability, host and reason — never the credential.`
    - `grep -cE "<|useState|fetch\(" frontend/src/components/settings/connectionRefusalCopy.ts` prints `0` (no JSX, no state, no fetch)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` is at or below the re-measured baseline
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>The closed copy module exports all six reason bodies, the three §4d headings, block 9, the check headline with its shipped negation table and §5b's corrected sentence — with no banned vocabulary, no absolute verb and no JSX.</done>
</task>

<task type="auto">
  <name>Task 2: Render the two asymmetric refusals, the three check moments and the destructive guards</name>
  <files>frontend/src/components/settings/ConnectionFormPanel.tsx</files>
  <read_first>
    - `frontend/src/components/settings/ConnectionFormPanel.tsx` (from plan 190-17)
    - `frontend/src/components/settings/connectionRefusalCopy.ts` (just written)
    - `frontend/src/lib/api.ts` — `checkConnectorConnection` and its three-bucket result (from plan 190-15)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §4b, §4d, §5b, §5c, §9 (the panel's OFF notice), §12
    - `frontend/src/components/admin/UsersAndAccess.tsx:385-405` — the victim-naming sheet shape and the `runWrite(fn, "… · recorded")` receipt idiom
  </read_first>
  <action>
**A. THE TWO REFUSALS AND THEIR BINDING ASYMMETRY (§4b).**
> *A refusal you can fix leaves the door open; a refusal you cannot fix closes it.*

- **Moment 8 — egress refused.** Render the heading + the §4c body for the guard's reason code +
  `Correct the host and try again.` + the audit line. **Save stays ENABLED.** Moment 8's third
  paragraph — *"The refusal happened before your password was read, so it was never used and
  never left this form."* — is **not optional prose**: it is the n8n ordering property (D-06)
  told in user-facing words, and it is the only place a human can see it.
- **Moment 9 — no encryption key.** Render `CIPHER_UNAVAILABLE_BLOCK`. **Save goes DISABLED**
  with `aria-describedby` pointing at `Disabled because no encryption key is configured.`
  Nothing the person types helps, so the door closes.

**B. §4d — three states, never flattened.** Map the API's `bucket` to the right heading. **A
refusal must never render the word "failed", and an unreachable host must never render the word
"refused"** — those two swaps make a security decision read as a bug and a bug read as a policy.

**C. The three check moments (§5c).**
- in flight: `Checking the credential…` / `Asking the host who this token belongs to. No email,
  ticket or message is sent by this check.` Footer button `[⟳ Checking…]` disabled, beside
  `Saved. Nothing is sent by a check.`
- success: the §5c headline verbatim, with the closing negation picked from the closed table by
  capability, plus `Checked just now · re-checking is always safe.`
- failure: §5b's block — the heading, the reached-and-answered line, `CHECK_FAILURE_SENTENCE`
  and the verbatim vendor error under a `what the host said, verbatim` label, in `font-mono`,
  **never paraphrased and never truncated** (071-A).

**D. The panel's `live_connectors` OFF notice (§9)** — one `⛨` block, verbatim, with the footer
`will save · will not send` in `font-mono`/`--warning`. The person is about to create a
connection that will not send; not saying so would be the coy version.

**E. Destructive guards (§2g) reachable from the panel footer:** Delete → victim-naming sheet
always; Disable → sheet when `Used by > 0`, direct flip otherwise; Replace → direct, and the
panel says it invalidates the last check verdict, because the server does exactly that. Every
write lands as `✎ {verb} · recorded` — a receipt, never a toast.

**F. All reason text is real DOM text** wired by `aria-describedby` where it explains a disabled
control. **Never a `title` attribute** (142-B, the 184-07 lesson).
  </action>
  <acceptance_criteria>
    - `grep -c "title=" frontend/src/components/settings/ConnectionFormPanel.tsx` prints `0`
    - `grep -c "aria-describedby" frontend/src/components/settings/ConnectionFormPanel.tsx` is `>= 1`
    - `grep -c "REFUSAL_\|CHECK_\|CIPHER_UNAVAILABLE" frontend/src/components/settings/ConnectionFormPanel.tsx` is `>= 5` (rendered from identifiers, not inline literals)
    - `grep -ciE "\bcannot\b|no step will be allowed|never bound" frontend/src/components/settings/ConnectionFormPanel.tsx` prints `0`
    - `grep -ciE "SSRF|RFC1918|CIDR|TOCTOU|metadata endpoint" frontend/src/components/settings/ConnectionFormPanel.tsx` prints `0`
    - The file contains the literal fragment `before your password was read`
    - `grep -ci "toast" frontend/src/components/settings/ConnectionFormPanel.tsx` prints `0`
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` is at or below the re-measured baseline
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>Both refusals render with their binding asymmetry, the three §4d states stay distinct, the three check moments use the closed strings, the OFF notice appears in the panel, and destructive actions are graded with receipts.</done>
</task>

<task type="auto">
  <name>Task 3: Assert every string by character-identity, prove the asymmetry, and pin the count</name>
  <files>frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx` (from plan 190-17 — extend it)
    - `frontend/src/components/settings/connectionRefusalCopy.ts` and `ConnectionFormPanel.tsx` (just written)
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §14 — the failure conditions this suite must be able to catch
    - `scripts/vitest-count-gate.cjs`
  </read_first>
  <action>
Extend the suite with:

1. **Character-identity assertions** for every exported copy string that reaches the DOM —
   compare rendered `textContent` against the imported identifier, not against a re-typed
   literal. A test that re-types the sentence cannot detect drift.
2. **The asymmetry (§4b)**: on an egress refusal Save is **enabled**; on the cipher-unavailable
   refusal Save is **disabled** AND `aria-describedby` resolves to a node whose text is the
   reason. Both directions, in one test file, because the asymmetry is the point.
3. **`test_a_refusal_never_renders_the_word_failed_and_an_unreachable_host_never_renders_refused`**
   — §4d's two forbidden swaps, asserted on rendered output for all three buckets.
4. **The six reason codes each render their own sentence** — parametrised; an unknown code must
   surface loudly rather than render an empty block.
5. **`unresolvable` reads as a lookup failure, not a security refusal** — assert its heading is
   the lookup one and that its body contains no refusal vocabulary.
6. **§5b has no absolute verb** — assert the rendered check-failure paragraph matches
   `CHECK_FAILURE_SENTENCE` exactly and contains none of `cannot`, `no step will be allowed`,
   `never bound`. **This is §14's single most likely regression**, because the absolute reads
   stronger and nothing in the type system objects.
7. **The check success negation matches the capability** — three cases against the closed table,
   asserted against the SHIPPED 189 §9d source rather than a copy.
8. **No credential ever reaches the DOM** — drive a refusal and a failed check with a sentinel
   secret in scope, then assert the sentinel appears zero times in `innerHTML`, in any
   `data-` attribute, in any `aria-label` and in any `title`. Include a positive control proving
   the search would find it.
9. **The banned-vocabulary fence** over `connectionRefusalCopy.ts`'s exports.
10. **The OFF notice appears in the panel and its footer reads `will save · will not send`.**

Then re-run `node scripts/vitest-count-gate.cjs`, bump `ConnectionFormPanel.test.tsx`'s pin to
the measured count with a dated comment naming plan 190-18, and confirm the gate reports OK.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/settings/__tests__/ConnectionFormPanel.test.tsx` reports `0 failed` with at least 19 tests total (the 9 from plan 190-17 plus these 10)
    - Test 8 includes a positive control asserting the sentinel IS found when deliberately rendered
    - Test 6 asserts exact string equality against the imported `CHECK_FAILURE_SENTENCE`
    - `node scripts/vitest-count-gate.cjs` exits 0 printing `count gate OK`, and the `ConnectionFormPanel.test.tsx` pin equals the measured count
    - `cd frontend && npm test` reports `0 failed`
    - `git diff --numstat HEAD -- frontend/src/components/workflows/PhaseFormPanel.tsx frontend/src/components/workflows/PhaseNode.tsx frontend/src/components/workflows/PhaseNodeCard.tsx frontend/src/components/workflows/phaseNodeCardContract.ts frontend/src/components/workflows/ownProperty.ts frontend/src/components/workflows/NodeCornerMarks.tsx frontend/src/components/workflows/NodeRunOverlay.tsx frontend/src/components/workflows/NodeIconWell.tsx` prints NOTHING
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test && cd .. && node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Every rendered string is asserted by character-identity, both halves of the asymmetry are proved, the two forbidden §4d swaps are caught, §5b's absolute-verb regression is fenced, and no credential can reach the DOM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard reason code → rendered sentence | An improvised sentence at a call site is a sentence nobody can test for drift |
| refusal text → the credential | A refusal that names the secret defeats the guard it is reporting |
| copy → enforcement | A sentence claiming more than a gate delivers is the §5 defect; §5b is the exact place it would recur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-18-T5 | Information disclosure | rendered refusal and check-failure blocks | mitigate | A sentinel secret asserted absent from `innerHTML`, every `data-` attribute, every `aria-label` and every `title`, WITH a positive control proving the search works. The audit line states the discipline the backend enforces (D-08) |
| T-190-18-D06 | Repudiation | refusal block 8 | mitigate | The third paragraph states that the refusal happened BEFORE the password was read — the n8n ordering property in user-facing words, asserted by character-identity so it cannot be trimmed as filler |
| T-190-18-ASYM | Repudiation | Save enablement | mitigate | Egress refusal → Save enabled; cipher unavailable → Save disabled with `aria-describedby`. Both directions asserted in one file, because the asymmetry IS the contract |
| T-190-18-SWAP | Repudiation | §4d vocabulary | mitigate | A refusal never renders "failed" and an unreachable host never renders "refused", asserted for all three buckets. These two swaps make a security decision read as a bug and a bug read as a policy |
| T-190-18-U07a | Repudiation | §5b's middle paragraph | mitigate | Exact-equality assertion against the imported identifier plus a grep fence on absolute verbs. §14 names this as the single most likely regression in the section |
| T-190-18-JARGON | Denial of service (to comprehension) | user-facing copy | mitigate | Banned-vocabulary fence over the copy module's exports: no `SSRF`, `RFC1918`, `CIDR`, `link-local`, `NAT64`, `SIIT`, `TOCTOU`, `metadata endpoint`, `allow-list` |
| T-190-18-CODE | Tampering | the closed reason map | mitigate | A module-scope assertion that the client's key set equals `egress.py`'s six codes; a seventh code fails loudly rather than rendering an empty block at someone trying to fix something |
| T-190-SC | Tampering | npm installs | mitigate | Zero installs. Acceptance: `git diff frontend/package.json frontend/package-lock.json` prints nothing |
</threat_model>

<verification>
- `cd frontend && npm test` — `0 failed`
- `node scripts/vitest-count-gate.cjs` — `count gate OK`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` — at or below baseline
- `git diff --numstat HEAD -- <the eight fenced files>` — empty
</verification>

<success_criteria>
- The closed six-row reason map is rendered from the guard's own codes, with a key-set assertion against `egress.py`
- Both refusal blocks render with their binding Save asymmetry, and D-06's user-facing sentence is present and asserted
- Refused / unreachable / rejected stay three states with three next steps; neither forbidden swap can ship
- §5b carries no absolute verb, and no credential can reach any rendered attribute
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-18-SUMMARY.md` when done, recording the character-identity assertions added, the sentinel-secret positive control, and the gate's printed totals.
</output>
