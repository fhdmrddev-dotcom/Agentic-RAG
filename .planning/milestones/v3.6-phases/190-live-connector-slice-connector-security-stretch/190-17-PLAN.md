---
phase: 190-live-connector-slice-connector-security-stretch
plan: 17
type: execute
wave: 6
depends_on: ["190-16"]
files_modified:
  - frontend/src/components/settings/ConnectionFormPanel.tsx
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-02, CONN-03]

must_haves:
  truths:
    - "D-27 — sketch 156-A is a CONTRACT: a 400px right-side push/split panel, the list stays visible, mobile becomes a bottom sheet and it must survive 375px (CONN-02)"
    - "The panel inherits two obligations a Dialog would have given free — a FOCUS TRAP and FOCUS RESTORE — and both are NET-NEW a11y work in this phase, named as plan tasks rather than discovered in review (CONN-02)"
    - "D-03 — the three field sets are static-token only: SMTP host/port/from/app-password, Jira site/project-key/account-email/API-token, Slack channel/bot-token and NOTHING ELSE. No OAuth consent surface exists (CONN-02)"
    - "The always-on 🔒 destination footer updates as you type and is NEVER behind an Advanced disclosure — you can never bind a connection without seeing where it sends (CONN-03)"
    - "The write-only secret renders as a font-mono span on edit, NEVER an input carrying a fake value — a fake value is a value the DOM holds (CONN-03)"
    - "D-23 — frontend/src/components/workflows/PhaseFormPanel.tsx is READ ONLY: its two type rules are MIRRORED, never imported from, and its measured git diff --numstat stays 0 0"
  artifacts:
    - path: "frontend/src/components/settings/ConnectionFormPanel.tsx"
      provides: "the push/split shell, the per-capability fields, the 🔒 footer, the write-only secret and the org-shared line"
  key_links:
    - from: "frontend/src/components/settings/ConnectionsTab.tsx"
      to: "frontend/src/components/settings/ConnectionFormPanel.tsx"
      via: "row click and the Add button open the panel beside the list"
      pattern: "ConnectionFormPanel"
---

<objective>
Build the add/edit panel shell and its fields: the 400px push/split container sketch 156-A
locked, its per-capability field sets, the always-on 🔒 destination footer, the write-only
secret and the org-shared line.

Purpose: 156-A won on **placement**, not on form content — the form is byte-identical across the
three variants by construction. What A buys is that the list stays visible, which matters
because *"when a check fails the honest next action is to look at the list — which connection is
now unusable, is anything else in the same state. A dialog scrims that away."* What A **costs**
is a focus trap and a focus restore that `Dialog` would have provided free, and those are named
here as tasks so they are not review findings.

Output: the shell, the fields, the footer, the secret row. The refusals, the check moments and
the destructive guards are plan 190-18.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md
@.planning/phases/190-live-connector-slice-connector-security-stretch/190-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
</context>

<interfaces>
<!-- ⚠ PhaseFormPanel.tsx is the lineage AND a hard empty-diff fence. READ IT, NEVER EDIT IT,
     and do NOT import from it. Its two type rules are MIRRORED:
       panel title  → text-[13px] font-semibold   (PhaseFormPanel.tsx:742)
       field label  → text-[11px] font-medium     (PhaseFormPanel.tsx:228)
     Do not "unify" them with the Settings card title (text-base font-headline font-bold,
     SettingsPage.tsx:189-195) — those are two different shipped rules and 190 declares neither. -->

**The shell grid** (`WorkflowBuilderPage.tsx:1833-1836` is the shipped shape):
```tsx
className="grid min-h-0 min-w-0 flex-1 overflow-hidden motion-safe:transition-[grid-template-columns] motion-safe:duration-300"
style={{ gridTemplateColumns: "minmax(0,1fr) " + (panelOpen ? "400px" : "44px") }}
```

**The fields, by capability (156-A):**

| Capability | Fields |
|---|---|
| `send_email` | `Name` · `SMTP host and port` (a `1fr 92px` two-column row) · `Send from` · `App password` |
| `create_ticket` | `Name` · `Jira site` · `Project key` · `Account email` · `API token` |
| `post_message` | `Name` · `Channel` · `Bot token` — **and nothing else** |

**Slack says out loud that it has nothing to type**, verbatim:
> `The only thing you supply. The address this posts to is a constant in our source — it cannot be pointed anywhere else, by you or by a workflow.`

**SMTP's host help line**, verbatim:
> `Must be reachable on the public internet over TLS. Plain smtp:// is refused, including for addresses inside this network.`

**The 🔒 footer**, rendered ALWAYS, updating as you type, never behind an Advanced disclosure —
024-A's cure for BUG-260616-01 applied to a destination instead of a model:
```
🔒 sends to  smtp.fastmail.com:465   [implicit TLS]  · from ops@northwind.co
🔒 sends to  https://slack.com/api/chat.postMessage   [fixed in code] [TLS]
🔒 sends to  smtp.internal.northwind.co:587   [refused]        ← border --destructive/50
```
Copy `ProviderPicker.tsx:202-222`'s structure and its derived-`footerParts` idiom
(`:159-165` — build from parts during render, never from state) — **but NOT its `title=`
attribute**, which it uses as a truncation affordance and which 190 forbids.

**The write-only secret (§3d).** On create, a `password` input with
`Encrypted before it touches the database, and never rendered back to any browser once saved.`
On edit, **never an input**:
```
•••••••••••••••• ················· stored 3 Aug   [ Replace ]
The stored value is never sent back to this browser — not to you, not to an admin,
not to the workflow author who picks this connection. It can be replaced, never read.
```
The dots are a `font-mono` `<span>`, **never an `<input type=password>` carrying a fake value —
a fake value is a value the DOM holds.** `ProviderPicker.tsx:167`'s masked-key sentinel is the
concept to reuse; render text, not an input.

**The org-shared line (§3e)**, verbatim, stating D-12 as a property because the person creating
it is authorising their colleagues:
> `⚭ Shared with everyone in {Org} — a connection exists so colleagues' workflows can use it. It is never visible to another organisation.`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The push/split shell — 400px, 375px-safe, with a REAL focus trap and focus restore</name>
  <files>frontend/src/components/settings/ConnectionFormPanel.tsx, frontend/src/components/settings/ConnectionsTab.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/PhaseFormPanel.tsx` — **READ ONLY**: the panel header at `:742`, the field label at `:228`, and the overall shell composition. Do not edit it and do not import from it
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1830-1845` — the shipped push/split grid
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §3a, §11a (the justified spacing exceptions), §11b (typography and U-13), §12 (the a11y row naming the focus work as NET-NEW)
    - `frontend/src/components/settings/ConnectionsTab.tsx` (from plan 190-16) — the open/close seam
  </read_first>
  <action>
Create `frontend/src/components/settings/ConnectionFormPanel.tsx` as a **400px right-side
push/split panel**, using the shipped grid shape. The list stays visible and is never covered —
that is the whole reason A won.

**Mobile (<768px) → a bottom sheet**, per the shipped shell rule. **It must remain usable at
375px**; the notice blocks are the tallest content and are what will break first.

**The two net-new a11y obligations, both real code in this task:**
- **Focus trap** — Tab and Shift+Tab cycle within the panel while it is open. `Dialog` would
  have given this free; the push/split panel gives neither.
- **Focus restore** — Escape closes and focus returns to the row (or the Add button) that opened
  it. Store the opener element on open; restore on close, including when the close comes from a
  successful save.

Wire the open/close seam in `ConnectionsTab.tsx`: the Add button opens the panel in create mode,
a row click opens it in edit mode. For a non-admin a row click opens it **read-only** — fields
render as static text, the secret row renders its stored form with no `Replace`, and the footer
carries no actions (U-02).

**Spacing:** the shipped panel form uses `padding: 16px 18px` and `margin-bottom: 14px` between
fields. These are **justified, pre-existing exceptions inherited from `PhaseFormPanel` and
`StatusChip`** — 190 must NOT "fix" them onto the 4px grid, because doing so would change shipped
visual rhythm on five surfaces AND require editing `PhaseFormPanel.tsx`, whose required diff is
`0 0`. Record that in a comment so a later round does not correct it.

**Typography:** mirror the shipped rules at each site — panel title `text-[13px] font-semibold`,
field label `text-[11px] font-medium`. Do not invent a third title style and do not unify the
panel title with the Settings card title.
  </action>
  <acceptance_criteria>
    - `git diff --numstat frontend/src/components/workflows/PhaseFormPanel.tsx` prints nothing
    - `grep -c "PhaseFormPanel" frontend/src/components/settings/ConnectionFormPanel.tsx` prints `0` (no import from it)
    - `grep -cE "400px|gridTemplateColumns" frontend/src/components/settings/ConnectionFormPanel.tsx` is `>= 1`
    - `grep -cE "Escape|keydown|Tab" frontend/src/components/settings/ConnectionFormPanel.tsx` is `>= 2` (a real trap, not a comment about one)
    - `grep -c "focus()" frontend/src/components/settings/ConnectionFormPanel.tsx` is `>= 1` (restore is implemented, not intended)
    - `grep -cE "font-bold" frontend/src/components/settings/ConnectionFormPanel.tsx` prints `0`
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` is at or below the re-measured baseline
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS" && git diff --numstat frontend/src/components/workflows/PhaseFormPanel.tsx</automated>
  </verify>
  <done>The 400px push/split panel opens beside the list, traps focus while open, restores focus to its opener on close, degrades to a bottom sheet below 768px, and `PhaseFormPanel.tsx` is untouched and un-imported.</done>
</task>

<task type="auto">
  <name>Task 2: The per-capability fields, the always-on 🔒 footer and the org-shared line</name>
  <files>frontend/src/components/settings/ConnectionFormPanel.tsx</files>
  <read_first>
    - `frontend/src/components/settings/ProviderPicker.tsx:150-225` — the always-on footer structure and the derived-`footerParts` idiom. ⚠ Copy the structure, NOT the `title=` attribute
    - `frontend/src/components/workflows/ConnectionPicker.tsx` (from plan 190-12) — the 🔒 destination form, so all three surfaces use ONE shape
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §3b, §3c, §3e, §11d (the copy table, verbatim)
    - `frontend/src/lib/api.ts` — `createConnectorConnection` / `updateConnectorConnection` and their body types
  </read_first>
  <action>
**A. The capability is chosen first and the rest of the form appears** — *"every capability needs
different facts, and Slack needs almost none."* Render the three field sets exactly as §3b binds
and **nothing beyond them**. No CC field, no attachment field, no issue-type picker beyond what
is listed, no advanced section. Each extra field is a surface nobody threat-modelled, and D-32
fences the phase to the thin three.

**B. Slack says out loud that it has nothing to type** — render the §3b sentence verbatim as an
exported identifier. This is D-02 told to the person: one of the three destinations is
unforgeable by construction and the surface should say so.

**C. SMTP's host help line** — verbatim, as an exported identifier. It states the plaintext
refusal BEFORE the person can trip it.

**D. The always-on 🔒 destination footer.** Rendered ALWAYS, updating as you type, never behind
an Advanced disclosure. Build it from parts during render, never from state. Show the mode tag
(`[implicit TLS]` / `[STARTTLS]` / `[fixed in code]` / `[TLS]`) and, for a refused destination,
the `--destructive/50` border treatment. **You can never bind a connection without seeing where
it sends** — that is the whole point, and it is 024-A's cure for a silently mis-routed endpoint
applied to a destination instead of a model.

**E. The write-only secret.** On create, a real `password` input plus its help line. On edit,
**a `font-mono` `<span>` of dots plus `stored {date}` plus a `[ Replace ]` button** — never an
`<input type=password>` carrying a fake value. Render the §3d note verbatim, and say that
Replace invalidates the last check verdict, because the server does exactly that.

**F. The org-shared line**, verbatim, at the foot of the fields.

**G. Copy discipline:** every user-visible string in this file is an exported identifier,
asserted by character-identity in plan 190-18's suite. **No `title=` anywhere.** Reasons and
help text are real DOM text; where they explain a control they are wired by `aria-describedby`.

**H. Colour:** `--primary` only for the focused input border and the save button;
`--muted-foreground` for help lines and the org-shared line; `--destructive` only for the
refused-destination footer. Introduce no new colour value.
  </action>
  <acceptance_criteria>
    - `grep -c "title=" frontend/src/components/settings/ConnectionFormPanel.tsx` prints `0`
    - `grep -c "type=\"password\"" frontend/src/components/settings/ConnectionFormPanel.tsx` prints `1` (create only), and the edit branch renders a `<span>` — verified by printing the edit-mode secret block
    - The file contains, verbatim, the Slack sentence fragment `it cannot be pointed anywhere else, by you or by a workflow`, the SMTP fragment `Plain smtp:// is refused, including for addresses inside this network.`, and the org-shared fragment `It is never visible to another organisation.`
    - `grep -c "export const" frontend/src/components/settings/ConnectionFormPanel.tsx` is `>= 10` (copy strings are exported identifiers)
    - The footer render path is unconditional — `grep -c "Advanced" frontend/src/components/settings/ConnectionFormPanel.tsx` prints `0`
    - Field counts per capability match §3b exactly: 4 for `send_email`, 5 for `create_ticket`, 3 for `post_message` — asserted by a test in plan 190-18 and recorded here in the summary
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` is at or below the re-measured baseline
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>The three field sets render exactly as specified with no extras, the 🔒 footer is unconditional and derived during render, the secret is write-only with no fake value in the DOM, and every string is an exported identifier with no `title` anywhere.</done>
</task>

<task type="auto">
  <name>Task 3: The shell's own suite and the count-gate pin</name>
  <files>frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/settings/ConnectionFormPanel.tsx` (just written)
    - `frontend/src/components/admin/__tests__/UsersAndAccess.a11y.test.tsx` — the shipped a11y suite shape
    - `scripts/vitest-count-gate.cjs` — the pin block and the same-commit registration rule
    - `.planning/phases/190-live-connector-slice-connector-security-stretch/190-UI-SPEC.md` §12 and §14
  </read_first>
  <action>
Create the suite covering the shell and the fields (the refusals and check moments are plan
190-18's):

1. **Focus trap** — with the panel open, Tab from the last focusable control returns to the
   first, and Shift+Tab from the first goes to the last. Assert focus never lands on a node
   outside the panel. §14's failure condition is *"the panel traps focus nowhere (Tab escapes
   into the table behind it)"* — this is that condition, inverted.
2. **Focus restore** — Escape closes and focus returns to the element that opened the panel;
   assert the same for a close-after-save.
3. **The list stays visible** — assert the table is still in the DOM and not `aria-hidden` while
   the panel is open. A dialog would have scrimmed it; this container must not.
4. **375px** — render at a 375px viewport and assert the panel is usable: no horizontal overflow
   and the tallest notice block is fully reachable.
5. **Per-capability field counts** — 4 / 5 / 3, asserted, so an extra field fails loudly.
6. **The footer is unconditional** — present on first render before any typing, and updated as
   the host field changes.
7. **The secret is write-only on edit** — assert NO `input[type=password]` exists in edit mode
   and that the dots are a `<span>`; assert the DOM holds no value resembling a stored secret.
8. **No `title` attribute in the rendered output.**
9. **Read-only for a non-admin** — no `Replace`, no save action, fields as static text.

Then register the suite with the count gate **in this commit**: run
`node scripts/vitest-count-gate.cjs`, read the printed numbers, add
`"ConnectionFormPanel.test.tsx"` with its measured count and a dated comment naming plan 190-17,
and re-run until it reports OK.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/settings/__tests__/ConnectionFormPanel.test.tsx` reports `0 failed` with at least 9 tests
    - Tests 1 and 2 assert focus positions directly (via `document.activeElement`), not by asserting a handler exists
    - Test 7 asserts `queryAllByDisplayValue` finds nothing secret-shaped and that no `input[type=password]` is present in edit mode
    - `node scripts/vitest-count-gate.cjs` exits 0 printing `count gate OK`, and `grep -c "ConnectionFormPanel.test.tsx" scripts/vitest-count-gate.cjs` is `>= 1`
    - `cd frontend && npm test` reports `0 failed`
    - `git diff --numstat HEAD -- frontend/src/components/workflows/PhaseFormPanel.tsx` prints nothing
    - `git diff --numstat frontend/package.json frontend/package-lock.json` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test && cd .. && node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Nine cases green including a directly-asserted focus trap and restore, the list-stays-visible property, 375px usability, the per-capability field counts and the write-only secret; the suite is pinned in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed destination → the person's understanding | The 🔒 footer is the only thing standing between a person and binding a connection to a place they did not intend |
| stored secret → the DOM | A rendered fake value is a value the browser holds; the only safe render is text |
| keyboard user → the panel | Without a trap, Tab walks into the table behind an open form and the person loses their place silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-17-SECRET | Information disclosure | the edit-mode secret row | mitigate | A `font-mono` `<span>` of dots, never an `input[type=password]` with a fake value; asserted by a test that queries for both. The copy states the value is never sent back to any browser, which the response model (plan 190-09) makes true |
| T-190-17-DEST | Spoofing | the destination footer | mitigate | Rendered ALWAYS, derived during render, never behind an Advanced disclosure — 024-A's cure for BUG-260616-01. A refused destination is styled and worded distinctly |
| T-190-17-FOCUS | Denial of service (to a keyboard user) | the push/split shell | mitigate | A real focus trap and a real focus restore, asserted via `document.activeElement` rather than by the presence of a handler. Named as tasks because `Dialog` would have given both free and A does not |
| T-190-17-U02 | Elevation of privilege | the read-only branch | mitigate | A non-admin gets static text, no `Replace` and no actions; the API gate (plan 190-09) is the enforcement and this is the honest surface for it |
| T-190-17-D23 | Tampering | `PhaseFormPanel.tsx` | mitigate | Read-only lineage: its two type rules are MIRRORED, it is never imported, and its `git diff --numstat` is asserted empty as an automated verify |
| T-190-17-SCOPE | Tampering | field set | mitigate | Exactly 4 / 5 / 3 fields, asserted. No OAuth surface, no advanced section, no extra vendor field — D-03 and D-32 |
| T-190-SC | Tampering | npm installs | mitigate | Zero installs; only already-vendored shadcn primitives. Acceptance: `git diff frontend/package.json frontend/package-lock.json` prints nothing |
</threat_model>

<verification>
- `cd frontend && npm test` — `0 failed`
- `node scripts/vitest-count-gate.cjs` — `count gate OK`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` — at or below baseline
- `git diff --numstat frontend/src/components/workflows/PhaseFormPanel.tsx` prints nothing
</verification>

<success_criteria>
- The 400px push/split panel keeps the list visible, traps and restores focus, and survives 375px
- Exactly 4 / 5 / 3 fields per capability, with the Slack and SMTP sentences rendered verbatim
- The 🔒 footer is unconditional and derived during render; the secret is write-only text on edit
- `PhaseFormPanel.tsx` is untouched and un-imported; nothing was installed
</success_criteria>

<output>
Create `.planning/phases/190-live-connector-slice-connector-security-stretch/190-17-SUMMARY.md` when done, recording the focus-trap and focus-restore evidence, the per-capability field counts and the gate's printed totals.
</output>
