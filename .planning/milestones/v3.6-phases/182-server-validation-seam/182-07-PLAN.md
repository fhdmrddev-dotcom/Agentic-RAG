---
phase: 182-server-validation-seam
plan: 07
type: execute
wave: 3
depends_on: ["182-04", "182-06"]
files_modified:
  - backend/app/services/harness/reachability.py
  - backend/app/services/harness/grounding.py
  - backend/app/api/workflows.py
  - backend/tests/unit/test_182_severity_codes.py
autonomous: true
gap_closure: true
requirements: [VALID-01]

must_haves:
  truths:
    - "WR-05 closed: an UNRECOGNISED verdict code no longer silently classifies as the soft `incomplete` severity. `_severity` fails LOUD — an unknown code classifies as `error` and is logged — so a genuinely blocking future rule can never paint soft-yellow on the canvas and surprise the author at publish."
    - "The classifier's known-code set is DERIVED from the owning modules, not duplicated as literals: `reachability` publishes the canonical set of codes `lint_workflow` can emit, `grounding` publishes the canonical set `grounding_verdicts` can emit, and `workflows.py` composes them with the two codes the route itself mints."
    - "A drift-detector test scans the OWNING SOURCE for actual emit sites and fails if a module's published code set falls out of sync with the codes its functions can really emit — the same spirit as the existing NL-gen count guard in `test_182_extraction_parity.py`, because a failures-only differential cannot see a code that was added without being classified."
    - "The deliberate `no_terminal` dual-source split is PRESERVED: `phases_empty` maps to `incomplete`, otherwise to `error`. That split is D-182-03 intent, not a bug (Pitfall 2)."
    - "Every currently-emittable code/condition pair classifies exactly as it does today — the change is fail-loud on the UNKNOWN branch only, with zero reclassification of any known code."
    - "`reachability.py` stays PURE: no DB token is introduced into its source, so the `/validate` seam keeps its import-light property (Pitfall 1, guarded by `test_182_extraction_parity.py`)."
  artifacts:
    - path: "backend/app/services/harness/reachability.py"
      provides: "LINT_CODES — the canonical frozenset of codes lint_workflow can emit, owned by the module that emits them"
      contains: "LINT_CODES"
    - path: "backend/app/services/harness/grounding.py"
      provides: "GROUNDING_VERDICT_CODES — the canonical frozenset of codes grounding_verdicts can emit"
      contains: "GROUNDING_VERDICT_CODES"
    - path: "backend/app/api/workflows.py"
      provides: "A composed known-code set + a fail-loud _severity classifier that logs and returns error for an unrecognised code"
      contains: "_KNOWN_CODES"
    - path: "backend/tests/unit/test_182_severity_codes.py"
      provides: "The drift detector (source-scan vs published set) + the unknown-code fail-loud proof + the pinned code-to-severity table"
      min_lines: 100
  key_links:
    - from: "backend/app/api/workflows.py"
      to: "backend/app/services/harness/reachability.py"
      via: "LINT_CODES imported module-direct alongside lint_workflow"
      pattern: "LINT_CODES"
    - from: "backend/app/api/workflows.py"
      to: "backend/app/services/harness/grounding.py"
      via: "grounding.GROUNDING_VERDICT_CODES read off the already-imported module"
      pattern: "GROUNDING_VERDICT_CODES"
---

<objective>
Close WR-05 from `182-VERIFICATION.md`. `_ERROR_CODES` in `backend/app/api/workflows.py` is a hardcoded
`frozenset` of 6 string literals duplicated from `reachability` and `grounding`, and `_severity`
returns `"incomplete"` for anything not in it. So an unrecognised future code silently classifies as
a soft "still building" warning instead of a hard error.

This trap fires within two phases: Phase 184 and Phase 185 both add verdict codes (185 adds the GOVERN
grounding-mode verdict per ROADMAP SC#4). A genuinely blocking rule would paint soft-yellow on the
canvas and the author would hit a publish-block surprise.

Purpose: make the classifier fail loud instead of failing open, and make its known-code set derive
from the modules that own the codes so it cannot drift.
Output: canonical code sets on the two owning modules, a fail-loud classifier, and a drift-detector
test that catches a code added without a classification.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-server-validation-seam/182-CONTEXT.md
@.planning/phases/182-server-validation-seam/182-VERIFICATION.md
@.planning/phases/182-server-validation-seam/182-04-SUMMARY.md
@.planning/phases/182-server-validation-seam/182-06-SUMMARY.md

<interfaces>
<!-- Exact current contracts. Use these directly — no codebase exploration needed. -->

backend/app/api/workflows.py — the code being replaced (lines 341-368):
  _ERROR_CODES = frozenset({"bad_index", "orphan_phase", "unsatisfiable_skip",
                            "folder_scope", "unregistered_tool", "unregistered_skill"})
  def _severity(code: str, *, phases_empty: bool) -> str:
      if code == "no_terminal":
          return "incomplete" if phases_empty else "error"
      if code in _ERROR_CODES:
          return "error"
      return "incomplete"          # <-- WR-05: unknown codes fail OPEN (soft)

  Already imported at module top:
    line 14  import logging  — a module `logger` is available for the fail-loud warning
    line 53  from app.services.harness import grounding, publish_service     (MODULE import)
    line 58  from app.services.harness.reachability import lint_workflow     (module-direct, import-light)

  The route MINTS two codes itself (neither owning module emits them):
    line 440  "business_requirement"  — from grounding.business_requirement_missing being True
    line 451  "interactive_phase"     — from publish_service._interactive_phase_failures

backend/app/services/harness/reachability.py — the 5 codes lint_workflow can emit
  (confirmed by scanning every LintError( emit site: lines 52, 91, 99, 106, 123, 157, 166):
    bad_index, input_unsatisfied, no_terminal, orphan_phase, unsatisfiable_skip
  MODULE CONTRACT: documented "PURE — no I/O, no DB, no engine import".
  HARD GUARD: test_182_extraction_parity.py::test_grounding_is_the_one_source asserts the substrings
  "folder_utils", "supabase" and "_skill_registry" do NOT appear ANYWHERE in this file's source —
  comments and docstrings included. Do not write any of those three tokens into this file.

backend/app/services/harness/grounding.py — the 3 codes grounding_verdicts can emit
  (confirmed by scanning the "code": "..." emit sites inside grounding_verdicts):
    folder_scope, unregistered_skill, unregistered_tool

The CURRENT classification, which must be preserved exactly (D-182-03 / RESEARCH taxonomy table):
  error      : bad_index, orphan_phase, unsatisfiable_skip, folder_scope,
               unregistered_tool, unregistered_skill, and no_terminal when phases is NOT empty
  incomplete : input_unsatisfied, business_requirement, interactive_phase,
               and no_terminal when phases IS empty

Working drift-detector regexes (verified against the current source this session):
  lint codes      : re.findall(r'LintError\(\s*"([a-z_]+)"', reachability_src)
                    yields {bad_index, input_unsatisfied, no_terminal, orphan_phase, unsatisfiable_skip}
  grounding codes : re.findall(r'"code":\s*"([a-z_]+)"', grounding_src)
                    yields {folder_scope, unregistered_skill, unregistered_tool}
  Python's \s matches newlines, so the multi-line LintError( call sites are covered.

Test-count facts for the no-coverage-loss guard (verified this session):
  tests/unit/test_182_validate.py 12 | tests/test_182_extraction_parity.py 6
  tests/test_182_grounding_bundle.py 6 | tests/test_revert_byte_identical.py 5
  tests/test_181_flip_on.py 7 | tests/test_181_off_audience.py 9
  tests/unit/test_103_nl_generate.py 6 | tests/unit/test_103_grounding_fidelity.py 2
  tests/unit/test_103_lint_block.py 1
  Combined verified green baseline before this gap closure: 54 passed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish canonical code sets from the owning modules and make _severity fail loud</name>
  <files>backend/app/services/harness/reachability.py, backend/app/services/harness/grounding.py, backend/app/api/workflows.py</files>
  <read_first>
    - backend/app/services/harness/reachability.py (lines 1-40 — the module docstring's PURE contract and the `LintError` NamedTuple; lines 42-180 — every `LintError(` emit site, so the code set is enumerated from source rather than memory)
    - backend/app/services/harness/grounding.py (lines 338-435 — the three atomic rules and `grounding_verdicts` as they stand after plan 182-04, to enumerate the emitted codes from source)
    - backend/app/api/workflows.py (lines 253-280 — the `Verdict` model and its `severity` Literal; lines 341-368 — `_ERROR_CODES` and `_severity`; lines 415-468 — the handler showing which codes the ROUTE itself mints; line 14 for the logging import)
    - backend/tests/test_182_extraction_parity.py (lines 162-225 — `test_grounding_is_the_one_source`, whose forbidden-token assertions constrain what may be written into `reachability.py`)
  </read_first>
  <behavior>
    - `_severity("bad_index", phases_empty=False)` returns `"error"`; likewise `orphan_phase`, `unsatisfiable_skip`, `folder_scope`, `unregistered_tool`, `unregistered_skill`.
    - `_severity("input_unsatisfied", ...)`, `_severity("business_requirement", ...)` and `_severity("interactive_phase", ...)` return `"incomplete"`.
    - `_severity("no_terminal", phases_empty=True)` returns `"incomplete"`; with `phases_empty=False` it returns `"error"`.
    - `_severity("some_future_govern_code", phases_empty=False)` returns `"error"` — NOT `"incomplete"` — and emits a log warning naming the code.
    - `reachability.LINT_CODES` equals the set of codes `lint_workflow` actually emits; `grounding.GROUNDING_VERDICT_CODES` equals the set `grounding_verdicts` actually emits.
  </behavior>
  <action>
    In `backend/app/services/harness/reachability.py`, add a module-level `LINT_CODES: frozenset[str]` holding exactly the five codes `lint_workflow` emits — `bad_index`, `input_unsatisfied`, `no_terminal`, `orphan_phase`, `unsatisfiable_skip` — placed next to the `LintError` NamedTuple so the declaration sits beside the type that carries them. Docstring it as the CANONICAL set of codes this module can emit, published so downstream classifiers derive from it instead of re-declaring literals, and note that adding a new `LintError` code requires adding it here in the SAME commit because the drift test enforces the pairing. CRITICAL CONSTRAINT: do not write the substrings `supabase`, `folder_utils` or `_skill_registry` anywhere in this file — including in the new docstring — because `test_182_extraction_parity.py` asserts their absence to protect this module's documented PURE / import-light property (Pitfall 1).

    In `backend/app/services/harness/grounding.py`, add a module-level `GROUNDING_VERDICT_CODES: frozenset[str]` holding exactly `folder_scope`, `unregistered_skill`, `unregistered_tool`, placed just above the section banner introducing the three atomic grounding-fidelity rules. Docstring it as the canonical set of codes `grounding_verdicts` can emit — one per atomic rule — published for the same derive-don't-duplicate reason, with the same add-in-the-same-commit note.

    In `backend/app/api/workflows.py`, replace the hardcoded `_ERROR_CODES` frozenset and rewrite `_severity`:

    Extend the existing module-direct reachability import on line 58 to bring in `LINT_CODES` alongside `lint_workflow`. Read the grounding set as `grounding.GROUNDING_VERDICT_CODES` off the module already imported on line 53 — do not add a second import of that module.

    Declare `_ROUTE_ASSIGNED_CODES: frozenset[str]` holding the two codes the ROUTE itself mints and neither owning module emits: `business_requirement` and `interactive_phase`. Comment where each is minted (the `business_requirement_missing` branch and the `_interactive_phase_failures` loop inside `validate_workflow`).

    Declare `_INCOMPLETE_CODES: frozenset[str]` holding `input_unsatisfied`, `business_requirement`, `interactive_phase` — the not-yet-ready / still-wiring set from D-182-03.

    Declare `_KNOWN_CODES` as the union of `LINT_CODES`, `grounding.GROUNDING_VERDICT_CODES` and `_ROUTE_ASSIGNED_CODES` — the full set the route knows how to classify.

    Derive the error set rather than listing it: `_ERROR_CODES` becomes `_KNOWN_CODES` minus `_INCOMPLETE_CODES` minus the single-element set holding `no_terminal` (excluded because it is resolved by the dual-source split before any set lookup). Keep the name `_ERROR_CODES` so any existing reference or test lens still resolves.

    Rewrite `_severity(code, *, phases_empty)` with this exact branch order: (1) `no_terminal` returns `"incomplete"` when `phases_empty` else `"error"` — the D-182-03 / Pitfall-2 dual-source split, preserved verbatim; (2) `code in _INCOMPLETE_CODES` returns `"incomplete"`; (3) `code in _ERROR_CODES` returns `"error"`; (4) UNKNOWN — log a warning through the module `logger` naming the unrecognised code and instructing the reader to add it to the owning module's canonical set and, if it is a still-building condition, to `_INCOMPLETE_CODES` — then return `"error"`.

    Rewrite the `_severity` docstring to state the WR-05 rationale explicitly: an unrecognised code fails CLOSED (loud red) rather than open (soft grey), because a new blocking rule painted as "still building" would silently mislead the author and surprise them at publish; Phase 184 and Phase 185 both add codes (185 adds the GOVERN grounding-mode verdict), so the unknown branch is a live path, not a theoretical one. Keep the existing `no_terminal` explanation intact.

    Update the taxonomy comment above the constants so it describes the DERIVED composition (owning-module sets plus route-minted codes) instead of the old literal list.

    Do NOT change the `Verdict.severity` Literal — the taxonomy stays closed at `error` and `incomplete`. Do NOT change any check function, any verdict message, or the handler's aggregation order.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py tests/test_182_extraction_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py tests/test_182_extraction_parity.py -q` exits 0 — the existing 12 verdict tests still pin every known code's severity, and the PURE-module guard still passes.
    - `grep -c "LINT_CODES" backend/app/services/harness/reachability.py` returns >= 1 and `grep -c "LINT_CODES" backend/app/api/workflows.py` returns >= 1.
    - `grep -c "GROUNDING_VERDICT_CODES" backend/app/services/harness/grounding.py` returns >= 1 and `grep -c "GROUNDING_VERDICT_CODES" backend/app/api/workflows.py` returns >= 1.
    - The classifier no longer ends in a bare soft default: `grep -c "return \"incomplete\"" backend/app/api/workflows.py` returns exactly 2 (the `no_terminal` split arm and the `_INCOMPLETE_CODES` arm) — the old trailing unconditional `return "incomplete"` is gone.
    - The unknown branch logs: within the `_severity` body there is a `logger.warning` call, and the function's final statement returns `"error"`.
    - `grep -c "supabase" backend/app/services/harness/reachability.py` returns 0, `grep -c "folder_utils" backend/app/services/harness/reachability.py` returns 0, `grep -c "_skill_registry" backend/app/services/harness/reachability.py` returns 0 — the PURE-module tokens were not introduced.
    - `_ERROR_CODES` is derived, not literal: within `backend/app/api/workflows.py` the `_ERROR_CODES` assignment references `_KNOWN_CODES` and `_INCOMPLETE_CODES` and contains no verdict-code string literal.
    - `cd backend && venv/Scripts/python.exe -c "import app.main"` exits 0 — the composed module-level sets resolve at import time with no circular-import breakage.
  </acceptance_criteria>
  <done>The route's known-code set is composed from the modules that own the codes, and an unrecognised code fails loud as `error` instead of silently soft-classifying.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drift detector + fail-loud proof + pinned code-to-severity table</name>
  <files>backend/tests/unit/test_182_severity_codes.py</files>
  <read_first>
    - backend/tests/test_182_extraction_parity.py (lines 1-60 and 290-330 — the source-scan / count-guard idiom and the "guard has teeth" self-test posture this file mirrors)
    - backend/app/api/workflows.py (the constants and `_severity` as rewritten in Task 1)
    - backend/app/services/harness/reachability.py (the emit sites the scan targets)
    - backend/app/services/harness/grounding.py (the emit sites the scan targets)
    - backend/tests/unit/test_182_validate.py (lines 273-335 — the existing severity tests, so the new file complements rather than duplicates them)
  </read_first>
  <behavior>
    - Scanning `reachability.py` source for `LintError(` emit sites yields a set EQUAL to `reachability.LINT_CODES`.
    - Scanning `grounding.py` source for the verdict `"code"` emit sites yields a set EQUAL to `grounding.GROUNDING_VERDICT_CODES`.
    - `workflows._KNOWN_CODES` is a superset of both published sets plus the two route-minted codes.
    - Every code in `_KNOWN_CODES` classifies to exactly one of `"error"` / `"incomplete"`, matching a pinned literal table.
    - An unrecognised code classifies as `"error"`, and specifically NOT as `"incomplete"`.
    - The `no_terminal` dual-source split still holds in both directions.
  </behavior>
  <action>
    Create `backend/tests/unit/test_182_severity_codes.py`. Follow the repo conventions: a module docstring naming the phase, the requirement (VALID-01) and the WR-05 finding it closes; imports INSIDE test bodies (Phase 102 posture); plain sync tests (no async needed — `_severity` is pure). Mirror `test_182_extraction_parity.py` for the source-scan idiom: resolve the backend directory from `Path(__file__).resolve()`, read each module's source with `read_text(encoding="utf-8")`, and use the regexes given in the `<interfaces>` block.

    Write these tests:

    (1) The lint drift detector. Scan `reachability.py` for `LintError(` emit-site codes and assert the scanned set EQUALS `reachability.LINT_CODES`. Failure message must say a `lint_workflow` code was added or removed without updating `LINT_CODES`, so the `/validate` severity classifier would silently mis-classify it, and must name the symmetric difference.

    (2) The grounding drift detector. Scan `grounding.py` for the verdict `"code"` emit-site codes and assert the scanned set EQUALS `grounding.GROUNDING_VERDICT_CODES`, with an equivalent failure message.

    (3) The composition guard. Assert `workflows._KNOWN_CODES` is a superset of `reachability.LINT_CODES` union `grounding.GROUNDING_VERDICT_CODES`, and that `workflows._ROUTE_ASSIGNED_CODES` equals the two-element set of `business_requirement` and `interactive_phase`. Assert `_INCOMPLETE_CODES` and `_ERROR_CODES` are disjoint and that their union plus `no_terminal` equals `_KNOWN_CODES` — i.e. every known code has exactly one classification path and none is orphaned.

    (4) The pinned code-to-severity table. Write an explicit literal mapping of all 11 code/condition pairs to their expected severity (the table in the `<interfaces>` block, including both `no_terminal` conditions) and assert `_severity` reproduces it exactly. Write the table out as literals — do NOT re-derive it from the constants, or the test only asserts the code agrees with itself. Add a comment stating this table is the D-182-03 taxonomy and that changing an entry is a deliberate product decision requiring a matching CONTEXT/decision update.

    (5) The WR-05 fail-loud proof. Assert `_severity("a_code_no_module_emits", phases_empty=False)` is NOT `"incomplete"` and IS `"error"`. Add a second case using a plausible future name to make the intent concrete — a governance-mode style code Phase 185 might add — and assert the same. Use `caplog` at WARNING level to assert the unrecognised code NAME appears in the emitted log record, so the failure is loud in operations, not just in the return value.

    (6) The guard-has-teeth self-test (mirroring `test_182_extraction_parity.py::test_count_guard_has_teeth`). Feed the two scan regexes a small inline source SAMPLE containing a known emit site, a commented-out emit site, and a non-matching line, and assert the scanner extracts exactly the real code(s). This proves the drift detectors would actually catch a drift rather than silently matching nothing — a scanner that returns an empty set would make tests (1) and (2) pass vacuously if the published sets were also emptied.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_severity_codes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_severity_codes.py -q` exits 0.
    - `grep -c "^\s*\(async \)\?def test_" backend/tests/unit/test_182_severity_codes.py` returns >= 6 — all six behaviors above are covered.
    - A test asserts an unrecognised code is NOT classified `incomplete` — `grep -c "incomplete" backend/tests/unit/test_182_severity_codes.py` returns >= 1 and the fail-loud test contains an explicit not-equal assertion against `"incomplete"`.
    - A test asserts on captured log output for the unknown branch — `grep -c "caplog" backend/tests/unit/test_182_severity_codes.py` returns >= 1.
    - The pinned severity table is written as literals, not derived: the file contains the strings `bad_index`, `orphan_phase`, `unsatisfiable_skip`, `no_terminal`, `input_unsatisfied`, `folder_scope`, `unregistered_tool`, `unregistered_skill`, `business_requirement` and `interactive_phase`.
    - The teeth self-test exists and asserts a non-empty extracted set from an inline sample — proving the scanners cannot pass vacuously.
    - Falsification check recorded in the SUMMARY: temporarily add a fake code literal to one owning module's emit path WITHOUT adding it to that module's published set, confirm the matching drift test FAILS, then revert. Record the observed failure message.
    - Full gap-closure regression stays green with no count reduction: `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_severity_codes.py tests/unit/test_182_validate.py tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_publish_grounding_stage.py tests/unit/test_publish_service.py tests/test_182_extraction_parity.py tests/test_182_grounding_bundle.py tests/test_revert_byte_identical.py tests/test_181_flip_on.py tests/test_181_off_audience.py tests/unit/test_103_nl_generate.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_103_lint_block.py -q` reports 0 failed.
  </acceptance_criteria>
  <done>A drift detector catches any future code added without a classification, the unknown branch is proven to fail loud in both return value and logs, and the current taxonomy is pinned as an explicit literal table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdict producer → severity classifier | Codes minted by `reachability`, `grounding` and the route itself cross into the route's only interpretive step. An unclassified code is a silent mis-signal to the client. |
| classifier → canvas client | `severity` is the UI hint Phase 184 paints badges from. A soft classification on a hard rule is a false-safety signal to the author. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-20 | Spoofing (false-safety signal) | `_severity`'s unknown-code branch | mitigate | THIS IS THE GAP BEING CLOSED. An unrecognised code now classifies as `error` and logs a warning naming it, instead of silently returning `incomplete`. A future blocking rule — Phase 185's GOVERN grounding-mode verdict is the concrete near-term case — can no longer present as "still building" to an author who would then be blocked at publish. Pinned by an explicit not-equal-`incomplete` assertion plus a `caplog` assertion. |
| T-182-21 | Tampering (silent drift) | duplicated literal code sets across three modules | mitigate | The classifier derives its known set from the modules that OWN the codes, and a source-scanning drift detector fails when a published set diverges from the emit sites. A "guard has teeth" self-test prevents the detector from passing vacuously on an empty scan. |
| T-182-22 | Denial of Service | log volume from the unknown branch | accept | `_severity` runs once per verdict on a read-only advisory route. An unknown code implies a code-level regression that a developer must fix, so the warning is bounded by the number of verdicts in one response and is the desired operational signal — this is exactly the loud failure being introduced, not noise to suppress. |
| T-182-23 | Tampering | edits to `reachability.py` | mitigate | The module's documented PURE / import-light property is protected by `test_182_extraction_parity.py`'s forbidden-token assertions. The action explicitly forbids writing `supabase`, `folder_utils` or `_skill_registry` into the file (docstrings included), and an acceptance criterion greps each to 0. Only a constant declaration is added — no import, no I/O. |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan — no `requirements.txt` change. Nothing to audit. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_severity_codes.py tests/unit/test_182_validate.py tests/test_182_extraction_parity.py -q` green.
- Full gap-closure regression across all Phase-182 test files reports 0 failed and no reduction in test count versus the pre-gap-closure baseline of 54 for the original nine files.
- `cd backend && venv/Scripts/python.exe -c "import app.main"` exits 0 — module-level set composition is import-safe.
- Falsification recorded in the SUMMARY for the drift detector (add an unpublished code, observe the failure, revert).
- `git show --stat` on the plan's commits touches only the four files in `files_modified`.
</verification>

<success_criteria>
- An unrecognised verdict code classifies as `error` and is logged — never silently `incomplete` (WR-05 closed).
- `_ERROR_CODES` is derived from `LINT_CODES`, `GROUNDING_VERDICT_CODES` and the route-minted set — no duplicated string literals.
- A drift detector fails if a module gains an emittable code without publishing it, and the detector is proven to have teeth.
- The `no_terminal` dual-source split and every current code's classification are unchanged.
- `reachability.py` remains PURE and import-light; no migration, no new dependency, no frontend file.
</success_criteria>

<output>
Create `.planning/phases/182-server-validation-seam/182-07-SUMMARY.md` when done.
</output>
