---
phase: 182-server-validation-seam
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/seeds/SEED-130-workflow-template-placeholders-dead-path.md
  - .planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md
  - .planning/seeds/SEED-132-harness-model-validator-bypasses-verdict-envelope.md
  - .planning/phases/182-server-validation-seam/182-DECISION-NOTES.md
autonomous: true
gap_closure: true
requirements: [VALID-01]

must_haves:
  truths:
    - "Every WARNING-level finding in `182-VERIFICATION.md`'s Anti-Patterns table that this gap closure deliberately does NOT fix (WR-03 dead template_placeholders path, WR-04 unsealed always-200 invariant, WR-07 model_validator 422s bypassing the {ok, verdicts} envelope) is captured as a seed file with a CONCRETE `re_open_trigger` naming the phase or observable condition that revives it — nothing is silently dropped."
    - "WR-08 (the verifier's `require_visible` asymmetry finding) is recorded as REJECTED with its D-182-05 rationale in a file inside the phase directory, so a re-verification of this phase does not re-raise a locked decision as a defect."
    - "Each seed names which Phase-184 or Phase-185 consumer it blocks or degrades, so the next planner can sequence it without re-deriving the analysis."
    - "No source file, migration, or test is touched by this plan — it is pure `.planning/` bookkeeping, so it carries zero file conflict with the backend gap-closure plans running in the same wave."
  artifacts:
    - path: ".planning/seeds/SEED-130-workflow-template-placeholders-dead-path.md"
      provides: "WR-03 — template_asset_id UUID can never match a storage-path asset, so template_placeholders is permanently []; the test asserting [] passes for the wrong reason"
      contains: "re_open_triggers"
    - path: ".planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md"
      provides: "WR-04 — the ALWAYS-200 docstring promise is unsealed; an uncaught APIError/ValueError inside the grounding reads would 500 a route the canvas calls on every edit"
      contains: "re_open_triggers"
    - path: ".planning/seeds/SEED-132-harness-model-validator-bypasses-verdict-envelope.md"
      provides: "WR-07 — two model_validator rules raise raw Pydantic 422s before the handler, a second un-server-mapped error surface the canvas would have to interpret itself"
      contains: "re_open_triggers"
    - path: ".planning/phases/182-server-validation-seam/182-DECISION-NOTES.md"
      provides: "WR-08 recorded as a REJECTED verifier finding with the D-182-05 rationale and its supersede-only re-open trigger"
      contains: "D-182-05"
  key_links:
    - from: ".planning/phases/182-server-validation-seam/182-DECISION-NOTES.md"
      to: ".planning/phases/182-server-validation-seam/182-CONTEXT.md"
      via: "cites D-182-05 as the locked decision that makes WR-08 a non-defect"
      pattern: "D-182-05"
---

<objective>
Bookkeeping half of the Phase 182 gap closure. `182-VERIFICATION.md` surfaced eight WARNING-level
findings; three of them (the SC#4 `folder_scope` blocker, the publish-enforcement gap, and WR-05's
fail-open severity classifier) are being FIXED by plans 182-04 / 182-06 / 182-07. Three more (WR-03,
WR-04, WR-07) are real but deliberately deferred, and one (WR-08) is a REJECTED finding that a locked
decision already answers.

Capture all four so nothing is silently dropped and so the next verifier does not re-raise WR-08 as
a defect.

Purpose: honor the project's preserve-every-deferred-idea rule with concrete re-open triggers, and
make the D-182-05 rationale discoverable from the phase directory.
Output: three seed files + one phase-directory decision note. No source change.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-server-validation-seam/182-CONTEXT.md
@.planning/phases/182-server-validation-seam/182-VERIFICATION.md

<interfaces>
<!-- The seed file format to mirror + the exact source coordinates each artifact must cite. -->

Seed file format — mirror `.planning/seeds/SEED-129-residual-org-blind-service-role-skill-reads.md`:
  frontmatter keys in this order — seed_id, title, status, planted, phase_origin, folded_into,
  category, related_seeds, related_decisions (list of prose strings), confirmed (optional list),
  needs_confirmation (optional list), re_open_triggers (list), priority, suggested_phase
  ...then a body with `# SEED-NNN — <short title>`, a `## The gap` section (a table of sites is
  welcome), and a `## How we would know this is closed` section of concrete checkable conditions.

Numbering: SEED-129 is the highest existing seed in `.planning/seeds/`. This plan plants 130/131/132.

WR-03 source coordinates (182-VERIFICATION.md Anti-Patterns row 4):
  backend/app/api/workflows.py:481  — `template_asset_id: UUID | None = None` query param on GET /grounding-bundle
  backend/app/api/workflows.py:505  — `pool = await get_pg_pool() if template_asset_id is not None else None`
  backend/app/services/harness/grounding.py:189-237 — `_resolve_template_placeholders` builds
    AssetRef(asset_id=str(template_asset_id), filename=str(template_asset_id), kind="template", ...)
    and calls resolve_template_source Branch 1, which keys asset_id as the STORAGE PATH — a UUID can
    never match a real library asset, so the `except Exception` degrade always fires and it returns [].
  backend/tests/test_182_grounding_bundle.py:114-136 — asserts the `[]` result as the expected shape.

WR-04 source coordinates (Anti-Patterns row 5):
  backend/app/api/workflows.py:338-368, 405-456 — the validate_workflow docstring promises
    "ALWAYS HTTP 200 with the machine-renderable envelope", but grounding.assemble_grounding_bundle
    (Supabase reads) and grounding.grounding_verdicts (the DB-backed subtree walk) can raise an
    uncaught postgrest APIError / ValueError, which FastAPI turns into a 500. No test covers it.
  Already fails closed: grounding._skill_registry (returns [] on a read miss, CR-01).
  Does NOT fail closed: fetch_visible_folders, _resolve_caller_org_ids, resolve_project_subtree.

WR-07 source coordinates (Anti-Patterns row 7):
  backend/app/models/harness.py:252-280 — two @model_validator rules:
    (1) a phase declaring folder_scope on a workflow with no project_folder_id;
    (2) skill_snapshot present without skill_ref.
  Both raise a raw Pydantic ValidationError -> FastAPI 422 BEFORE validate_workflow runs.

WR-08 — the REJECTED finding (Anti-Patterns row 8). The verifier flagged that
  POST /workflows/validate and GET /workflows/grounding-bundle (workflows.py:374, 474) carry
  Depends(require_canvas()) ALONE while the 8 sibling authoring routes on the same router
  (:559, 623, 656, 685, 720, 790, 820, 960) also carry require_visible("workflow_authoring").
  This is decided, not overlooked:
    - 182-CONTEXT.md D-182-05 — both routes inherit 181's require_canvas 404 posture verbatim;
      the off-flag check resolves BEFORE any auth dependency can leak route existence.
    - backend/app/api/workflows.py:244-248 (the seam header comment) — "They must NEVER stack
      require_visible (which raises 403 and would leak that the route exists) nor couple canvas
      availability to another feature's audience."
    - 182-RESEARCH.md Pitfall 3 and 182-PATTERNS.md "The flag gate" say the same.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plant SEED-130 / SEED-131 / SEED-132 for the three deferred findings</name>
  <files>.planning/seeds/SEED-130-workflow-template-placeholders-dead-path.md, .planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md, .planning/seeds/SEED-132-harness-model-validator-bypasses-verdict-envelope.md</files>
  <read_first>
    - .planning/seeds/SEED-129-residual-org-blind-service-role-skill-reads.md (the frontmatter schema + body structure to mirror exactly)
    - .planning/phases/182-server-validation-seam/182-VERIFICATION.md (the Anti-Patterns table rows 4, 5, 7 — the verbatim findings these seeds capture)
    - backend/app/services/harness/grounding.py (lines 189-237 — `_resolve_template_placeholders`, the WR-03 dead path)
    - backend/app/api/workflows.py (lines 338-368 and 471-529 — the WR-04 always-200 docstring and the bundle route's template_asset_id plumbing)
    - backend/app/models/harness.py (lines 240-290 — the two `@model_validator` rules behind WR-07)
    - .planning/ROADMAP.md (Phase 184 and Phase 185 sections — so each re_open_trigger names the real downstream consumer)
  </read_first>
  <action>
    Create three seed files under `.planning/seeds/`, each mirroring the SEED-129 frontmatter schema and body structure. Common frontmatter values: `status: open`, `planted: 2026-07-25`, `folded_into: null`, and a `phase_origin` naming "Phase 182 verification (182-VERIFICATION.md Anti-Patterns, WR-NN) — surfaced at verification, deliberately deferred at gap closure".

    SEED-130 — file `SEED-130-workflow-template-placeholders-dead-path.md`. The gap: the `GET /workflows/grounding-bundle` `template_placeholders` path is dead code. `template_asset_id` is typed `UUID` but `resolve_template_source` Branch 1 keys `asset_id` as a STORAGE PATH, so no real library asset can ever match; the `except Exception` degrade in `_resolve_template_placeholders` always fires and the function returns `[]` for every real asset. Cite `workflows.py:481,505` and `grounding.py:189-237`. Add a `confirmed:` entry recording that `backend/tests/test_182_grounding_bundle.py:114-136` asserts the `[]` result as the EXPECTED shape — a test passing for the wrong reason, which is exactly why a failures-only differential can never surface this. Set `category: correctness / dead-path — a documented feature that cannot fire`, `priority: medium`, `related_seeds: [SEED-110]` only if that seed is genuinely about workflow template files (check it; otherwise use an empty list). `re_open_triggers` must include: (a) Phase 184 wires template selection into node config and the dropdown needs real placeholder fields; (b) any consumer requires a non-empty `template_placeholders` payload; (c) anyone edits `_resolve_template_placeholders` or the `template_asset_id` param type — fix the key contract in that same commit rather than planting a sibling seed. `suggested_phase`: fold into Phase 184's node-config work, the first real consumer.

    SEED-131 — file `SEED-131-validate-always-200-invariant-unsealed.md`. The gap: `POST /workflows/validate` documents an "ALWAYS HTTP 200" invariant that is not sealed — an uncaught postgrest `APIError` or `ValueError` from `assemble_grounding_bundle` / `grounding_verdicts` becomes a 500, and no test exercises the failure path. Cite `workflows.py:338-368,405-456`. In the body, split the reads into those that already fail closed (`grounding._skill_registry` returns `[]` on a read miss, per CR-01) and those that do not (`fetch_visible_folders`, `_resolve_caller_org_ids`, `resolve_project_subtree`), so a closing phase knows the fix is a handler-level seal plus an honest degraded verdict — NOT a blanket try/except that would silently paint a broken workflow green. Set `category: reliability / unsealed invariant`, `priority: high`, and justify the priority with the Phase 184 blast radius. `re_open_triggers` must include: (a) Phase 184 lands the live canvas, which calls `/validate` on EVERY edit, turning a single DB blip into a visible 500 storm mid-authoring; (b) any observed 5xx on `/workflows/validate` in backend logs or LangSmith; (c) the route gains a second I/O dependency. `suggested_phase`: a `/gsd:quick` immediately before or inside Phase 184, since the canvas is the amplifier.

    SEED-132 — file `SEED-132-harness-model-validator-bypasses-verdict-envelope.md`. The gap: two `@model_validator` rules in `backend/app/models/harness.py:252-280` — a phase declaring `folder_scope` on an unbound workflow, and a `skill_snapshot` without a `skill_ref` — raise raw Pydantic 422s BEFORE the `/validate` handler runs, so they are the only validation rules not expressible in the `{ok, verdicts}` envelope. State the tension honestly: this is not a D-182-06 violation (nothing is re-implemented client-side) but it does leave the client rendering a raw 422 with no verdict payload, and BOTH conditions are exactly what a partially-configured node looks like mid-authoring. Set `category: API surface / error-model consistency`, `priority: high`. `re_open_triggers` must include: (a) Phase 184 must render validation state for a partially-configured node and hits a 422 with no verdict payload; (b) any proposal to let `/validate` accept a lenient body — record that relaxing `extra="forbid"` is FORBIDDEN (182-RESEARCH Anti-Patterns; the 422 IS the shape tier), so the fix must be a 422-to-verdict mapping in the handler or a FastAPI exception handler, never a looser model; (c) Phase 185 adds the GOVERN grounding-mode verdict and needs one uniform error envelope. `suggested_phase`: Phase 184, alongside the per-node badge work (VALID-03).

    Every seed's "How we would know this is closed" section must list concrete, checkable conditions — a command, an observable behavior, or a specific test that must exist — not prose intentions.
  </action>
  <verify>
    <automated>ls .planning/seeds/SEED-130-workflow-template-placeholders-dead-path.md .planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md .planning/seeds/SEED-132-harness-model-validator-bypasses-verdict-envelope.md && grep -c "re_open_triggers" .planning/seeds/SEED-130-workflow-template-placeholders-dead-path.md .planning/seeds/SEED-131-validate-always-200-invariant-unsealed.md .planning/seeds/SEED-132-harness-model-validator-bypasses-verdict-envelope.md</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist at the exact paths listed in `<files>`; `ls` on all three exits 0.
    - Each file's `grep -c "re_open_triggers"` returns at least 1, and each `re_open_triggers` list holds at least 3 entries.
    - Each file's frontmatter carries `status: open`, `folded_into: null`, `planted: 2026-07-25`, a non-empty `priority`, and a non-empty `suggested_phase`.
    - `grep -c "182-VERIFICATION" .planning/seeds/SEED-13*.md` returns >= 1 per file — every seed traces back to its verification finding.
    - SEED-130 cites `grounding.py:189-237` and names `test_182_grounding_bundle.py` as the test passing for the wrong reason.
    - SEED-131 names BOTH the fail-closed read (`_skill_registry`) and at least two reads that do not fail closed, and explicitly rules out a blanket try/except as the fix.
    - SEED-132 explicitly records that relaxing `extra="forbid"` is forbidden as a fix path.
    - `git status --porcelain backend/` reports no changes — this task touches nothing under `backend/`.
  </acceptance_criteria>
  <done>Three seeds exist with concrete re-open triggers tying each deferred finding to the downstream phase that revives it.</done>
</task>

<task type="auto">
  <name>Task 2: Record WR-08 as a REJECTED verifier finding with its D-182-05 rationale</name>
  <files>.planning/phases/182-server-validation-seam/182-DECISION-NOTES.md</files>
  <read_first>
    - .planning/phases/182-server-validation-seam/182-VERIFICATION.md (the Anti-Patterns row for `workflows.py:374, 474` — the WR-08 wording to quote and answer)
    - .planning/phases/182-server-validation-seam/182-CONTEXT.md (the `D-182-05` entry, verbatim — the locked decision)
    - backend/app/api/workflows.py (lines 230-252 — the seam header comment that already states the rule; lines 371-380 and 471-480 — the two gate attachments)
    - backend/app/dependencies.py (the `require_canvas` definition and the `require_visible` 403 path — so the note states the 403-vs-404 difference from source, not from memory)
  </read_first>
  <action>
    Create `.planning/phases/182-server-validation-seam/182-DECISION-NOTES.md` — a short phase-directory record whose ONLY job is to stop a locked decision from being re-raised as a defect on re-verification.

    Structure it as: a one-paragraph purpose line naming this as the standing answer to verifier findings on Phase 182 that are decisions rather than defects; then one section per recorded finding. Ship exactly one section for now, headed with the finding id and the word REJECTED, covering:
      - The finding as the verifier stated it: `POST /workflows/validate` and `GET /workflows/grounding-bundle` (`workflows.py:374, 474`) carry `Depends(require_canvas())` alone, while the 8 sibling authoring routes on the same router also carry `require_visible("workflow_authoring")` — flagged as an authorization asymmetry that becomes a silent authz gap the moment an operator narrows authoring visibility.
      - The verdict: REJECTED — this asymmetry is the intended design, not an oversight.
      - The rationale, with citations: `D-182-05` in `182-CONTEXT.md` locks the `require_canvas`-alone posture; `require_visible` raises 403 on deny, which LEAKS that the route exists and breaks the byte-identical-404 property the whole canvas off-switch is built on (Phase 181 D-181-01/02 and the pre-auth code-review fix); stacking it would additionally couple canvas availability to a DIFFERENT feature's audience, so flipping `workflow_authoring` off would silently disable the canvas seam. Quote the existing in-code statement at `workflows.py:244-248` so a reader can see the rule is already asserted in source. Cross-reference `182-RESEARCH.md` Pitfall 3 and `182-PATTERNS.md` "The flag gate".
      - The residual risk, stated honestly: if an operator narrows `workflow_authoring` visibility while `visual_workflow_canvas` stays on, a caller outside the authoring audience can still reach these two READ-ONLY routes. Note precisely what that gets them — `/validate` is read-only advice that never persists or executes, and `/grounding-bundle` returns only the caller's OWN org-gated palette (CR-01/CR-02 projections apply) — so the exposure is the caller's own data plus the public tool registry, and no write path is reachable.
      - A re-open trigger stated as: this is revisited ONLY if the operator explicitly supersedes D-182-05, or if `/validate` or `/grounding-bundle` ever gains a write/persist/execute side effect (at which point the read-only argument above no longer holds and the gate stack must be re-derived).

    Do not restate the fixed findings (SC#4 keying, publish enforcement, WR-05) here — those are closed by plans 182-04 / 182-06 / 182-07 and belong in their SUMMARYs.
  </action>
  <verify>
    <automated>ls .planning/phases/182-server-validation-seam/182-DECISION-NOTES.md && grep -c "D-182-05" .planning/phases/182-server-validation-seam/182-DECISION-NOTES.md && grep -c "REJECTED" .planning/phases/182-server-validation-seam/182-DECISION-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/182-server-validation-seam/182-DECISION-NOTES.md` exists.
    - `grep -c "D-182-05"` on that file returns >= 1 and `grep -c "REJECTED"` returns >= 1.
    - The note cites all three of: `workflows.py:374`, `workflows.py:474`, and `workflows.py:244-248`.
    - The note states the 403-vs-404 distinction explicitly (that `require_visible` raises 403 and would leak route existence).
    - The note contains a residual-risk paragraph that names what a non-authoring caller could reach and why it is bounded (read-only, own-palette-only).
    - The note contains a re-open trigger limited to an explicit operator supersede of D-182-05 or the routes gaining a write/execute side effect.
    - `git status --porcelain backend/` reports no changes.
  </acceptance_criteria>
  <done>A future verifier of this phase directory finds WR-08 already answered, with the locked decision, the honest residual risk, and a narrow re-open trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan writes only Markdown under `.planning/`. No route, no handler, no query, no dependency, no runtime code path is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-12 | Information Disclosure | seed + note file contents | mitigate | The files describe DEFECT SHAPES and cite file:line coordinates in a private planning repo — they must not embed secrets, tokens, connection strings, real user ids, or real org ids. Acceptance criteria forbid any `backend/` change and the content is limited to source coordinates already present in `182-VERIFICATION.md`. |
| T-182-13 | Repudiation | the WR-08 rejection record | mitigate | The rejection is recorded WITH its rationale, its citations, its honest residual risk, and a narrow re-open trigger — an auditor can reconstruct why the finding was closed without re-running the analysis. A bare "won't fix" would be the repudiation risk; this is its mitigation. |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan — documentation only. Nothing to audit. |
</threat_model>

<verification>
- All four files exist at the exact paths in `files_modified`.
- `git status --porcelain backend/ frontend/ supabase/` is empty — the plan is `.planning/`-only, so it can share Wave 1 with 182-04 without any file conflict.
- Each of SEED-130/131/132 carries `status: open`, `folded_into: null`, and >= 3 `re_open_triggers` entries.
- `182-DECISION-NOTES.md` answers WR-08 with the D-182-05 citation, the 403-vs-404 rationale, a residual-risk paragraph, and a supersede-only re-open trigger.
</verification>

<success_criteria>
- Nothing surfaced by verification is silently dropped: WR-03, WR-04 and WR-07 each live in a seed with a concrete trigger naming the phase that revives them.
- WR-08 will not be re-raised as a defect at re-verification — the phase directory carries the decision, the rationale, the residual risk, and the narrow condition under which it reopens.
- Zero backend changes; zero migrations; zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/182-server-validation-seam/182-05-SUMMARY.md` when done.
</output>
