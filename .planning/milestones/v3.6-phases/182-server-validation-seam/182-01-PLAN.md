---
phase: 182-server-validation-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/harness/grounding.py
  - backend/app/services/workflow_authoring.py
  - backend/app/services/harness/publish_service.py
  - backend/tests/test_182_extraction_parity.py
autonomous: true
requirements: [VALID-01]

must_haves:
  truths:
    - "The grounding computation lives in ONE shared source `backend/app/services/harness/grounding.py`; `workflow_authoring` delegates to it — the anti-drift mechanism (D-182-02 reuse-verbatim, D-182-06 one-source red line)."
    - "`_assemble_grounding` returns the byte-identical `(grounded_prompt, tool_names, skill_ids)` tuple after extraction — NL-gen behavior unchanged (D-182-02)."
    - "`grounding.py` exposes a per-node `grounding_verdicts(...)` collector over the SAME three atomic fidelity rules AND keeps `_check_grounding_fidelity` as a short-circuit wrapper for NL-gen — one source, two presentations (D-182-02)."
    - "The extracted grounding reads stay OWNER-scoped by `user_id` — no cross-user folder/skill disclosure introduced by the move (D-182-06)."
    - "`business_requirement_missing(definition)` is a single shared predicate imported by BOTH publish stage-1 and (later) `/validate` — no copy-paste drift for the D-13 check (D-182-02)."
    - "The existing `test_103` NL-gen test COUNT is guarded so a silently-dropped test is caught post-extraction (D-182-02, the Phase-177 coverage-loss lesson)."
  artifacts:
    - path: "backend/app/services/harness/grounding.py"
      provides: "GroundingBundle dataclass + assemble_grounding_bundle + render_grounding_prompt + the 3 atomic fidelity checks + grounding_verdicts + _check_grounding_fidelity wrapper + business_requirement_missing"
      min_lines: 90
    - path: "backend/app/services/workflow_authoring.py"
      provides: "_assemble_grounding + _check_grounding_fidelity are thin delegates to grounding.py"
      contains: "from app.services.harness.grounding import"
    - path: "backend/tests/test_182_extraction_parity.py"
      provides: "byte-identical extraction assertion + one-source guard + NL-gen test COUNT guard"
      min_lines: 20
  key_links:
    - from: "backend/app/services/workflow_authoring.py"
      to: "backend/app/services/harness/grounding.py"
      via: "delegate import"
      pattern: "from app.services.harness.grounding import"
    - from: "backend/app/services/harness/publish_service.py"
      to: "backend/app/services/harness/grounding.py"
      via: "shared business_requirement_missing predicate"
      pattern: "business_requirement_missing"
---

<objective>
Extract the two grounding functions out of `workflow_authoring.py` into ONE shared source —
`backend/app/services/harness/grounding.py` — so NL-gen and the (Wave 2) `/validate` route call
the SAME copy. This is the phase crux: the anti-drift mechanism that lets the canvas never drift
from the publish gauntlet (VALID-01 / D-182-02). The old NL-gen call sites become thin delegates
that return the byte-identical `(prompt, tool_names, skill_ids)` tuple, and the existing `test_103`
NL-gen tests are the regression backstop (they must pass UNCHANGED, and their COUNT is guarded).

Purpose: create the single grounding source everything downstream (routes in 182-02, palette
bundle, per-node fidelity verdicts) reads from — with zero behavior change to NL generation.
Output: `grounding.py` (new shared source), thin delegates in `workflow_authoring.py`, a one-line
shared `business_requirement_missing` predicate, and an extraction-parity + count-guard test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-server-validation-seam/182-CONTEXT.md
@.planning/phases/182-server-validation-seam/182-RESEARCH.md
@.planning/phases/182-server-validation-seam/182-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase this session — use directly. -->

From backend/app/services/workflow_authoring.py (the code being MOVED):
  async def _assemble_grounding(*, supabase, pool, user_id, project_folder_id,
      template_asset_id=None, template_placeholders=None) -> tuple[str, set[str], set[str]]
      # returns (grounded_prompt, tool_names, skill_ids)  — lines 262-313
  def _grounding_failed(detail: str) -> dict   # {"ok": False, "error": "grounding_failed", "detail": detail} — :316
  async def _check_grounding_fidelity(wd, *, supabase, user_id, tool_names, skill_ids) -> dict | None  # :322-355 short-circuit
  # helpers used ONLY inside _assemble_grounding (safe to MOVE — verified no other call sites):
  def _skill_registry(supabase, user_id: str) -> list[dict]                 # :140 (blocking DB read)
  def _render_folder_tree(folders: list[dict]) -> str                       # :184
  async def _resolve_template_placeholders(*, supabase, pool, user_id, template_asset_id, template_placeholders) -> list[str]  # :211
  # the registry compute inside _assemble_grounding (:275-288):
  #   folders = await fetch_visible_folders(supabase, user_id)
  #   tool_names = {t["function"]["name"] for t in get_tools(None)}
  #   skills = await run_in_threadpool(_skill_registry, supabase, user_id)   # D-v2.5-01 — blocking read wrapped
  #   skill_ids = {str(s["id"]) for s in skills}

From backend/app/services/harness/scope.py:
  async def assert_folder_scopes_subset(definition, *, supabase, user_id) -> None
      # owner-scoped ⊆ check; raises ValueError naming the offending phase slug (:227-230); no-op if project_folder_id is None (:219)

From backend/app/services/harness/publish_service.py:
  # stage-1 D-13 predicate is INLINE at :124 — `not (definition.business_requirement or "").strip()` (no reusable fn today)

From backend/app/services/harness/reachability.py:
  # docstring contract: "PURE — no I/O, no DB, no engine import" (:1-22) — grounding must NOT be folded here (Pitfall 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared grounding source `harness/grounding.py`</name>
  <files>backend/app/services/harness/grounding.py</files>
  <read_first>
    - backend/app/services/workflow_authoring.py (lines 140-355 — `_skill_registry`, `_render_folder_tree`, `_resolve_template_placeholders`, `_assemble_grounding`, `_grounding_failed`, `_check_grounding_fidelity` — the code being moved)
    - backend/app/services/harness/scope.py (lines 197-231 — `assert_folder_scopes_subset`, the ValueError message shape)
    - backend/app/services/harness/reachability.py (lines 1-22 — the PURE module-shape discipline to respect: do NOT fold grounding here)
  </read_first>
  <action>
    Create `backend/app/services/harness/grounding.py` as the ONE shared grounding source (Discretion item 1 resolved: a NEW `harness/grounding.py`, NOT inside `reachability.py` — grounding TOUCHES the DB and would poison reachability's pure-import property, Pitfall 1). Define, MOVING the code verbatim from `workflow_authoring.py`:
    (a) a `GroundingBundle` dataclass with fields `tools: list[str]` (sorted tool names for JSON), `tool_names: set[str]` (for fidelity), `folders: list[dict]` (the `fetch_visible_folders` `{id, name, parent_id, ...}` rows), `skills: list[dict]` (`{id, name}` enabled owner+global), `skill_ids: set[str]` (for fidelity), `placeholders: list[str]`;
    (b) `async def assemble_grounding_bundle(*, supabase, pool, user_id, project_folder_id, template_asset_id=None, template_placeholders=None) -> GroundingBundle` — lift the registry compute from `_assemble_grounding` lines 275-288 verbatim (`fetch_visible_folders`, `get_tools(None)`, `run_in_threadpool(_skill_registry, ...)` — preserve the `run_in_threadpool` wrap per D-v2.5-01, `_resolve_template_placeholders`); set `tools = sorted(tool_names)`;
    (c) `def render_grounding_prompt(bundle, project_folder_id) -> str` — lift the string render from `_assemble_grounding` lines 290-312 verbatim so the NL prompt is byte-identical;
    (d) MOVE `_skill_registry`, `_render_folder_tree`, `_resolve_template_placeholders`, `_grounding_failed` into this module (they are used ONLY by the grounding code — verified no other call sites — so the move is one-directional, no circular import);
    (e) the three atomic fidelity rules + `async def grounding_verdicts(wd, *, supabase, user_id, tool_names, skill_ids) -> list[dict]` — a per-node collector that APPENDS instead of short-circuiting (RESEARCH Pattern 2): call `assert_folder_scopes_subset` verbatim in a try/except, on ValueError append `{"code": "folder_scope", "phase": None, "message": str(exc)}` (the slug is in the message); iterate `wd.phases` appending `{"code": "unregistered_tool", "phase": phase.slug, "message": ...}` for each `available_tools` entry not in `tool_names`, and `{"code": "unregistered_skill", "phase": phase.slug, "message": ...}` when `skill_ref` is not in `skill_ids`;
    (f) `async def _check_grounding_fidelity(wd, *, supabase, user_id, tool_names, skill_ids) -> dict | None` — keep the SHORT-CIRCUIT wrapper for NL-gen: run the same three rules, return `_grounding_failed(...)` on the first violation, else `None` (byte-identical to today's `workflow_authoring._check_grounding_fidelity`);
    (g) `def business_requirement_missing(definition) -> bool` returning `not (definition.business_requirement or "").strip()` — the single shared D-13 predicate (Pitfall 4).
    Keep all reads OWNER-scoped by `user_id` exactly as before (V4 — never widen the palette to another user's folders/skills).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "from app.services.harness.grounding import GroundingBundle, assemble_grounding_bundle, render_grounding_prompt, grounding_verdicts, _check_grounding_fidelity, business_requirement_missing; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grounding.py` defines all seven symbols: `GroundingBundle`, `assemble_grounding_bundle`, `render_grounding_prompt`, `grounding_verdicts`, `_check_grounding_fidelity`, `business_requirement_missing`, and holds the moved `_skill_registry`/`_render_folder_tree`/`_resolve_template_placeholders`/`_grounding_failed`.
    - `grep -c "import supabase\|from app.utils.folder_utils" backend/app/services/harness/reachability.py` == 0 (reachability stays DB-free — Pitfall 1).
    - `assemble_grounding_bundle` preserves the `await run_in_threadpool(_skill_registry, ...)` wrap (D-v2.5-01) — `grep -c "run_in_threadpool" backend/app/services/harness/grounding.py` >= 1.
    - `grounding_verdicts` uses `assert_folder_scopes_subset` verbatim (does NOT re-derive the ⊆ walk) — `grep -c "assert_folder_scopes_subset" backend/app/services/harness/grounding.py` >= 1.
  </acceptance_criteria>
  <done>The shared grounding source exists and imports cleanly; reachability's pure-import property is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire `workflow_authoring` + publish stage-1 to delegate (byte-identical)</name>
  <files>backend/app/services/workflow_authoring.py, backend/app/services/harness/publish_service.py</files>
  <read_first>
    - backend/app/services/workflow_authoring.py (lines 262-355 to replace; lines 358-480 for the `generate_workflow_definition` call sites at :417 and :472 that must keep working)
    - backend/app/services/harness/publish_service.py (lines 110-152 — stage-1 business_requirement inline check at :124)
    - backend/tests/unit/test_103_nl_generate.py + backend/tests/unit/test_103_grounding_fidelity.py (the regression backstop — the test monkeypatch seam `monkeypatch.setattr(wa, "_assemble_grounding", ...)` must survive)
  </read_first>
  <action>
    Rewrite `workflow_authoring._assemble_grounding` to a THIN delegate: import `assemble_grounding_bundle` + `render_grounding_prompt` from `app.services.harness.grounding`, call `bundle = await assemble_grounding_bundle(...)` then `grounded = render_grounding_prompt(bundle, project_folder_id)`, and `return grounded, bundle.tool_names, bundle.skill_ids` — the IDENTICAL `(str, set, set)` tuple contract (so the `test_103_grounding_fidelity.py` monkeypatch of `wa._assemble_grounding` keeps working). Rewrite `workflow_authoring._check_grounding_fidelity` to delegate to `grounding._check_grounding_fidelity`. Remove the now-moved `_skill_registry`, `_render_folder_tree`, `_resolve_template_placeholders`, `_grounding_failed` definitions from `workflow_authoring.py` (import from `grounding.py` if any residual reference remains). In `publish_service.py` stage-1 (:124), replace the inline `not (definition.business_requirement or "").strip()` with a call to `business_requirement_missing(definition)` imported from `grounding.py` — behavior identical, now one source (Pitfall 4). Do NOT change `generate_workflow_definition`'s behavior, the golden-run/judge stages, or any provider path.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_103_nl_generate.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_publish_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_103_nl_generate.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_publish_service.py` exits 0 (NL-gen + grounding-fidelity + publish tests pass UNCHANGED).
    - `workflow_authoring.py` contains `from app.services.harness.grounding import` and `_assemble_grounding` returns `grounded, bundle.tool_names, bundle.skill_ids`.
    - `publish_service.py` calls `business_requirement_missing(` (no remaining inline `(definition.business_requirement or "").strip()` in stage-1).
    - `grep -c "def _assemble_grounding\|def assemble_grounding_bundle" backend/app/services/**/*.py` shows the registry compute defined once in `grounding.py` (one source; the `workflow_authoring` symbol is a delegate wrapper, not a second implementation).
  </acceptance_criteria>
  <done>NL generation, grounding fidelity, and publish stage-1 behave byte-identically while sourcing all grounding logic from `grounding.py`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extraction-parity + one-source + NL-gen count-guard test</name>
  <files>backend/tests/test_182_extraction_parity.py</files>
  <read_first>
    - backend/tests/unit/test_103_grounding_fidelity.py (2 test defs — the count to guard)
    - backend/tests/unit/test_103_nl_generate.py (6 test defs — the count to guard)
    - backend/tests/conftest.py (lines 133-167 — central get_current_user/get_supabase overrides + reset_mocks autouse)
  </read_first>
  <behavior>
    - Test: `_assemble_grounding` (via a monkeypatched `assemble_grounding_bundle` returning a known fake `GroundingBundle`) returns the exact `(rendered_prompt, tool_names, skill_ids)` tuple — the tuple contract is preserved.
    - Test: `grounding.py` is the ONE source — assert `render_grounding_prompt` and `grounding_verdicts` are importable from `grounding.py`, and `grep`-style assert (via reading the two source files) that the registry compute is NOT duplicated in `workflow_authoring.py`.
    - Test (COUNT guard): the number of `test_` functions in `test_103_grounding_fidelity.py` (== 2) and `test_103_nl_generate.py` (== 6) is unchanged — a silently-dropped NL-gen test fails this guard (the Phase-177 coverage-loss lesson).
  </behavior>
  <action>
    Create `backend/tests/test_182_extraction_parity.py`. Assert the byte-identical extraction contract by monkeypatching `grounding.assemble_grounding_bundle` to return a fixed `GroundingBundle` and confirming `render_grounding_prompt(bundle, pfid)` produces the expected NL string and `_assemble_grounding` returns the `(prompt, tool_names, skill_ids)` tuple. Add the ONE-source assertion: `grounding_verdicts` + `render_grounding_prompt` import from `grounding.py`. Add the COUNT guard: count `def test_` occurrences in `tests/unit/test_103_grounding_fidelity.py` (expect 2) and `tests/unit/test_103_nl_generate.py` (expect 6) by reading the files with `grep -v '^#'` hygiene, asserting the exact counts so an extraction that drops a regression test is caught. Use imports inside test bodies + `pytest.mark.asyncio` for async (the Phase-102 posture).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_182_extraction_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_182_extraction_parity.py` exits 0.
    - The count-guard asserts `== 2` for `test_103_grounding_fidelity.py` and `== 6` for `test_103_nl_generate.py` (literal counts, not `>=`).
    - The parity test proves `_assemble_grounding` returns a 3-tuple `(str, set, set)`.
  </acceptance_criteria>
  <done>The anti-drift backstop is in place: extraction parity + one-source + NL-gen count are all guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → Supabase (owner-scoped read) | `assemble_grounding_bundle` reads the caller's folders/skills via service-role; scoped by hand on `user_id` (service-role bypasses RLS). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-03 | Information Disclosure | `assemble_grounding_bundle` / moved `_skill_registry` + `fetch_visible_folders` | mitigate | Preserve the exact OWNER-scoped `user_id` reads during the move — never widen the palette to another user's folders/skills (same posture as NL-gen, T-098-02 / T-103-02-03). |
| T-182-05 | Tampering | grounding rule source | mitigate | ONE shared source (`grounding.py`); `workflow_authoring` + publish delegate — no second copy of the rules can drift (D-182-06 red line). `assert_folder_scopes_subset` reused verbatim, never re-derived. |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase — backend reuse/extraction only (no new dependency; existing venv). |
</threat_model>

<verification>
- `pytest tests/unit/test_103_nl_generate.py tests/unit/test_103_grounding_fidelity.py tests/unit/test_publish_service.py tests/test_182_extraction_parity.py -q` all green.
- `grounding.py` imports cleanly and is the single source of the grounding compute; `reachability.py` gains no DB import.
</verification>

<success_criteria>
- One shared `harness/grounding.py` exists; `workflow_authoring._assemble_grounding`/`_check_grounding_fidelity` + publish stage-1 delegate to it with byte-identical behavior.
- The NL-gen + grounding-fidelity + publish regression tests pass unchanged; the extraction-parity + count-guard test passes.
- No new dependency, no migration, no route (routes land in 182-02).
</success_criteria>

<output>
Create `.planning/phases/182-server-validation-seam/182-01-SUMMARY.md` when done.
</output>
