---
phase: 182-server-validation-seam
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/middleware/canvas_gate.py
  - backend/app/main.py
  - backend/tests/test_182_canvas_gate.py
  - backend/tests/test_revert_byte_identical.py
  - backend/tests/unit/test_182_validate.py
autonomous: true
gap_closure: true
requirements: [VALID-01]

must_haves:
  truths:
    - "D-182-R2-01 half A closed: with `visual_workflow_canvas` off, a MALFORMED JSON body sent to `POST /workflows/validate` returns a byte-identical 404 — not the 422 it returns today. The flag decision happens in a pure-ASGI middleware, BEFORE Starlette routing and before FastAPI decodes the request body, so no `Depends` ordering question can reopen it."
    - "The off-state response on both canvas paths is byte-identical to a path that was never built — same status, same body bytes `{\"detail\":\"Not Found\"}`, same content-type — for EVERY method (a wrong-method probe returns 404 too, where today `GET /workflows/validate` returns 405 and thereby advertises that a POST handler exists at that exact path) and for the trailing-slash variant."
    - "`Depends(require_canvas())` REMAINS on both routes as defense in depth — the middleware only decides the master off-switch; role/operator/greenlist resolution stays in the dependency, and the two structural gate tests that assert `require_canvas` is present and `require_visible` is absent still pass."
    - "D-182-R2-02 closed: with the flag off, `GET /openapi.json` no longer advertises `/workflows/validate` or `/workflows/grounding-bundle`, and no longer publishes `ValidateResponse`, `Verdict`, `GroundingBundleResponse`, `PaletteFolder` or `PaletteSkill`."
    - "D-182-R2-02's cache trap is handled: the FULL schema is generated and cached exactly once, the flag-filtered document is produced per request from a deep copy that is NEVER written back to `app.openapi_schema` and NEVER mutates the cached original — proven by a single-process test that flips the flag OFF then ON and asserts the served schema changes in BOTH directions."
    - "The two tautological assertions are gone (IN-04). The probe that claims to prove the gate beat body validation now actually sends malformed bytes, and the severity-taxonomy assertion that was guaranteed by a Pydantic `Literal` is deleted rather than left as false coverage."
    - "The gate reads the flag through the in-memory TTL settings cache with NO per-request DB call (D-v2.5-01) and fails CLOSED — any read failure resolves to 404, mirroring `require_canvas`'s D-181-02 posture (and deliberately opposite to `MaintenanceMiddleware`'s fail-OPEN)."
  artifacts:
    - path: "backend/app/middleware/canvas_gate.py"
      provides: "The ONE source of canvas-gated paths + the pre-routing pure-ASGI 404 gate + the flag-aware OpenAPI filter"
      contains: "CANVAS_GATED_PATHS"
      min_lines: 90
    - path: "backend/app/main.py"
      provides: "Middleware registration (innermost, inside Setup + Maintenance) and the app.openapi hook wiring"
      contains: "CanvasGateMiddleware"
    - path: "backend/tests/test_182_canvas_gate.py"
      provides: "Falsifiable probes: malformed body, wrong method, trailing slash, unbuilt-path byte-identity, and the both-directions OpenAPI flip"
      min_lines: 120
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/middleware/canvas_gate.py"
      via: "app.add_middleware(CanvasGateMiddleware) registered before MaintenanceMiddleware so it is INNERMOST"
      pattern: "CanvasGateMiddleware"
    - from: "backend/app/main.py"
      to: "backend/app/middleware/canvas_gate.py"
      via: "app.openapi replaced by the canvas-aware hook built from the app's original bound method"
      pattern: "app.openapi"
    - from: "backend/app/middleware/canvas_gate.py"
      to: "backend/app/models/user_settings.py"
      via: "feature_audience('visual_workflow_canvas') — in-memory TTL cache read, no DB"
      pattern: "feature_audience"
---

<objective>
Close the SC#3 BLOCKER independently confirmed in `182-VERIFICATION.md` and raised as CR-01/CR-02 in
`182-REVIEW.md` (both were already CR-03/CR-04 in round 1 and were never triaged).

Two concrete, live leaks, both re-reproduced against the real app this planning session with the flag
cold-off and no credentials:

  POST /workflows/validate  valid json     -> 404 {"detail":"Not Found"}
  POST /workflows/validate  MALFORMED json -> 422 {"detail":[{"type":"json_invalid",...}]}
  POST /workflows/validate/ MALFORMED json -> 422   (trailing-slash variant)
  GET  /workflows/validate  (wrong method) -> 405 {"detail":"Method Not Allowed"}
  POST /workflows/__nope__/__nope__        -> 404 {"detail":"Not Found"}   (the honest baseline)
  GET  /openapi.json (anonymous)           -> 200, advertising both paths + all 5 schemas
  GET  /docs                               -> 200

FastAPI decodes the request body in `get_request_handler` BEFORE `solve_dependencies` runs, so
`Depends(require_canvas())` structurally cannot intervene on a malformed body — no rewrite of the
dependency fixes it. And OpenAPI generation happens entirely outside the dependency-injection layer.

Purpose: make the flag-off state genuinely indistinguishable from "this route was never built", which
is the REVERT-01 / REVERT-02 HARD gate #1 this whole milestone is built on and which phases 183-189
all inherit.
Output: a pure-ASGI canvas gate that decides the flag before routing/body-decode, a dynamic
`app.openapi()` hook that hides the canvas surface only while the flag is off, and probes that
FAIL against today's code.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-server-validation-seam/182-CONTEXT.md
@.planning/phases/182-server-validation-seam/182-VERIFICATION.md
@.planning/phases/182-server-validation-seam/182-REVIEW.md

<interfaces>
<!-- Exact current contracts, verified by reading the source this planning session. -->

backend/app/main.py
  line 61-62  from app.middleware.maintenance import MaintenanceMiddleware
              from app.middleware.setup import SetupMiddleware
  line 592    app = FastAPI(title="Agentic RAG API", version="1.0.0", lifespan=lifespan)
  line 604    app.add_middleware(MaintenanceMiddleware)
  line 614    app.add_middleware(SetupMiddleware)
  line 622    app.add_middleware(CORSMiddleware, ...)
  Starlette applies add_middleware in REVERSE registration order: the LAST registered is
  OUTERMOST. Current stack, outermost first: CORS -> Setup -> Maintenance -> router.

backend/app/middleware/maintenance.py — the analog to mirror for POSTURE (pure ASGI, not
  BaseHTTPMiddleware, so the SSE hot path is never buffered):
    class MaintenanceMiddleware:
        def __init__(self, app: ASGIApp) -> None: self.app = app
        async def __call__(self, scope: Scope, receive: Receive, send: Send) -> None:
            if scope.get("type") != "http": await self.app(scope, receive, send); return
            ...
            response = JSONResponse(status_code=503, content={...})
            await response(scope, receive, send)
  It fails OPEN on a flag-read error (D-Q4). The canvas gate must fail CLOSED (D-181-02) —
  the opposite polarity, deliberately.

backend/app/models/user_settings.py
  feature_audience(feature: str) -> str        # line 1116 — returns "everyone"|"operators"|"role"|"off"
  Reads the per-worker 30s TTL settings cache via _feature_record (line 1096); documented
  NEVER raises; a cold cache / DB blip / missing key resolves to the hardcoded default,
  which for visual_workflow_canvas is "off". No DB call per request (D-v2.5-01).

backend/app/dependencies.py
  _NOT_FOUND = HTTPException(status_code=404, detail="Not Found")     # line 302
  require_canvas()  # line 600 — resolves the "off" audience FIRST, pre-auth, then the
                    # caller, then operator/everyone/role. Stays attached to both routes.

backend/app/api/workflows.py — the two gated routes (do NOT edit this file in this plan):
  @router.post("/validate", response_model=ValidateResponse,
               dependencies=[Depends(require_canvas())])           # line 469-473
  @router.get("/grounding-bundle", response_model=GroundingBundleResponse,
              dependencies=[Depends(require_canvas())])            # line 569-573
  Router prefix is "/workflows", so the absolute paths are
  "/workflows/validate" and "/workflows/grounding-bundle".
  The five response models declared for these routes are: ValidateResponse, Verdict,
  GroundingBundleResponse, PaletteFolder, PaletteSkill.

fastapi 0.115.6 / starlette 0.41.3 / pydantic 2.12.5 (confirmed in the venv).
  fastapi/applications.py:966  def openapi(self) -> Dict[str, Any]:
                                   if not self.openapi_schema:
                                       self.openapi_schema = get_openapi(...)
                                   return self.openapi_schema
  fastapi/applications.py:1003 the /openapi.json route handler calls `self.openapi()` at
  REQUEST time, so assigning an instance attribute `app.openapi = <callable>` after
  construction is honored on every request.

Probe transcript captured at HEAD this session (flag cold-off, anonymous, TestClient):
  POST /workflows/validate           valid json -> 404  b'{"detail":"Not Found"}'
  POST /workflows/validate           b"{"       -> 422  b'{"detail":[{"type":"json_invalid",...'
  POST /workflows/validate/          b"{"       -> 422
  GET  /workflows/validate                      -> 405  b'{"detail":"Method Not Allowed"}'
  POST /workflows/__nope__           b"{"       -> 405  b'{"detail":"Method Not Allowed"}'
  POST /workflows/__nope__/__nope__  b"{"       -> 404  b'{"detail":"Not Found"}'
  GET  /workflows/grounding-bundle              -> 404  b'{"detail":"Not Found"}'
  GET  /workflows/grounding-bundle/             -> 404  b'{"detail":"Not Found"}'
  GET  /workflows/__nope__/__nope__             -> 404  b'{"detail":"Not Found"}'
  GET  /workflows/__nope__                      -> 405  b'{"detail":"Method Not Allowed"}'
  GET  /openapi.json -> 200, 115 schemas, both canvas paths advertised, all 5 schemas present
  GET  /docs         -> 200
  NOTE the single-segment unbuilt sibling is a 405, not a 404 — that asymmetry is round-2
  review WR-09 and is explicitly OUT OF SCOPE this round. The honest 404 baseline for a
  byte-identity comparison is the TWO-segment probe, exactly as the existing tests use.

backend/tests/test_revert_byte_identical.py — the two things to change:
  line 128-133 (in test_require_canvas_404s_when_off): validates _MINIMAL_VALID_DEFINITION
    against the real model, POSTs it, then asserts `resp_validate.status_code != 422`
    with the comment "the gate fired BEFORE body validation" — tautological (a valid body
    cannot be 422 regardless of ordering).
  line 231-236 (in test_require_canvas_404s_pre_auth_when_off): the parity probe against
    "/workflows/__nope__/__nope__" — KEEP this shape; it is the honest 404 baseline.

backend/tests/unit/test_182_validate.py — line 341:
  assert {v.severity for v in resp.verdicts} <= {"error", "incomplete"}
  Guaranteed by `Verdict.severity: Literal["error","incomplete"]` (construction would
  raise), so it can never fail. test_182_severity_codes.py now pins the taxonomy properly.

Test conventions in this repo: `client` fixture is `TestClient(app)` (tests/conftest.py:223);
the flag is driven by monkeypatching `app.models.user_settings.load_app_settings` to a
SimpleNamespace (the `_cold_off` / `_flipped_on` helpers in test_revert_byte_identical.py and
test_181_flip_on.py); imports live INSIDE test bodies (Phase 102 posture).

Known-good baseline, re-measured this session:
  venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py
    tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_publish_grounding_stage.py
    tests/unit/test_182_severity_codes.py tests/unit/test_publish_service.py
    tests/test_182_extraction_parity.py tests/test_182_grounding_bundle.py
    tests/test_revert_byte_identical.py tests/test_181_flip_on.py -q
  => 80 passed. Deleting the tautology at test_182_validate.py:341 removes an ASSERTION,
  not a test, so the count must stay 80 (plus whatever this plan adds).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-ASGI canvas gate that decides the flag before routing and body decode</name>
  <files>backend/app/middleware/canvas_gate.py, backend/app/main.py</files>
  <read_first>
    - backend/app/middleware/maintenance.py (the whole file — the pure-ASGI posture, the `scope.get("type") != "http"` passthrough, the `JSONResponse(...)(scope, receive, send)` short-circuit, and the module-docstring style this file must mirror)
    - backend/app/main.py lines 55-70 (the middleware import block) and lines 590-630 (app construction and the three add_middleware calls with their ordering comments)
    - backend/app/dependencies.py lines 296-310 (`_NOT_FOUND`) and lines 600-652 (`require_canvas` — the exact 404 posture and the D-181-01/02 ordering the middleware must not disturb)
    - backend/app/api/workflows.py lines 234-272 (the seam header comment) and lines 469-480, 569-580 (the two route declarations, to confirm the absolute paths)
    - backend/app/models/user_settings.py lines 1096-1133 (`_feature_record` + `feature_audience` — proof the flag read is in-memory and never raises)
    - .planning/phases/182-server-validation-seam/182-REVIEW.md section "CR-01" (the file:line evidence and the suggested middleware shape)
    - .planning/phases/182-server-validation-seam/182-VERIFICATION.md `gaps:` block, the first `missing:` entry (the proposed fix, verbatim)
  </read_first>
  <behavior>
    - With the flag off, `POST /workflows/validate` carrying the bytes `{` and header `content-type: application/json` returns 404 with body `{"detail":"Not Found"}` — today it returns 422.
    - With the flag off, `GET /workflows/validate` returns 404 — today it returns 405.
    - With the flag off, `POST /workflows/validate/` (trailing slash) returns 404 — today it returns 422.
    - With the flag off, `GET /workflows/grounding-bundle` still returns 404 with body `{"detail":"Not Found"}` (unchanged).
    - With the flag off, the status code, body bytes and content-type of every canvas-path probe equal those of `GET /workflows/__nope__/__nope__`.
    - With the flag flipped on, both canvas paths reach the router again and behave exactly as they do today (404 becomes the normal authenticated flow; the existing flip-on tests stay green).
    - A non-canvas path is untouched in both flag states — the middleware is a no-op for every other request.
  </behavior>
  <action>
    Create `backend/app/middleware/canvas_gate.py`. Write it as a pure-ASGI middleware in the exact shape of `MaintenanceMiddleware` — a plain class with `__init__(self, app: ASGIApp)` and `async def __call__(self, scope: Scope, receive: Receive, send: Send)`, importing `JSONResponse` from `starlette.responses` and `ASGIApp`/`Receive`/`Scope`/`Send` from `starlette.types`. It must NOT be a `BaseHTTPMiddleware` subclass — that wraps response bodies through an anyio memory stream and would sit in front of every SSE stream in the app.

    Declare the module-level public constant `CANVAS_GATED_PATHS: frozenset[str] = frozenset({"/workflows/validate", "/workflows/grounding-bundle"})`. This is the ONE source of canvas-gated absolute paths for the whole app: the middleware and the OpenAPI filter in Task 2 both read it, and Phase 183+ adds its routes here in the same commit that mounts them. Say that in the constant's comment.

    Add a module-level `_read_canvas_is_off() -> bool` helper mirroring `maintenance._read_maintenance`'s shape but with the OPPOSITE failure polarity. It lazy-imports `feature_audience` from `app.models.user_settings` inside the function (avoiding the user_settings-to-middleware import cycle, matching `require_canvas`'s lazy import) and returns `feature_audience("visual_workflow_canvas") == "off"`. Wrap it in `try/except Exception` returning `True` — FAIL CLOSED. Document why the polarity differs from the analog: `MaintenanceMiddleware` fails OPEN because a settings blip must never wedge the platform read-only, whereas D-181-02 requires the canvas to fail CLOSED because a settings blip must never reveal a gated route. The read is a pure in-memory TTL-cache lookup, so there is no per-request DB call (D-v2.5-01).

    Add a module-level `_is_canvas_path(path: str) -> bool` that normalizes a single trailing slash before the membership test — strip one trailing `/` when the path is longer than one character, then test membership in `CANVAS_GATED_PATHS`. This closes the trailing-slash escape: Starlette's `redirect_slashes` issues a 307 for `/workflows/validate/` only because the slash-stripped path DOES match a mounted route, so without normalization the redirect itself would advertise the route's existence.

    Implement `CanvasGateMiddleware.__call__`: pass straight through when `scope.get("type") != "http"` (websocket and lifespan scopes untouched); otherwise read `scope.get("path", "")` and, if `_is_canvas_path(path)` and `_read_canvas_is_off()`, short-circuit by awaiting `JSONResponse(status_code=404, content={"detail": "Not Found"})(scope, receive, send)` and returning. Everything else awaits `self.app(scope, receive, send)` unchanged.

    Gate on PATH ONLY — never on method. A path that was never built returns the same response for every method, so restricting the gate to POST/GET would leave the wrong-method probe (`GET /workflows/validate` returns 405 today) advertising that a POST handler is declared at exactly that path. Put that reasoning in a comment.

    The body content is exactly `{"detail": "Not Found"}` — the same payload `dependencies._NOT_FOUND` produces through FastAPI's `http_exception_handler` and the same payload Starlette's unmatched-route 404 produces. Do not invent a different message, do not add headers, do not add a `WWW-Authenticate`. Byte-identity with an unbuilt path is the contract.

    In `backend/app/main.py`: add `from app.middleware.canvas_gate import CanvasGateMiddleware` to the middleware import block next to the maintenance and setup imports (around line 61), and add `app.add_middleware(CanvasGateMiddleware)` IMMEDIATELY BEFORE the existing `app.add_middleware(MaintenanceMiddleware)` call at line 604. Registration order is load-bearing and must be explained in a comment above the call: Starlette applies `add_middleware` in reverse registration order, so registering FIRST makes this middleware INNERMOST — it runs after `SetupMiddleware` and `MaintenanceMiddleware` and immediately before the router. That is exactly right for byte-identity, because on an unfinalized box an unbuilt path returns the setup 503 and under maintenance a mutating request to an unbuilt path returns the maintenance 503; if the canvas gate ran outside those, a gated path would answer 404 where an unbuilt path answers 503, which is itself a distinguishing signal. CORS stays outermost, so the 404 still carries CORS headers exactly as the router's own 404 does.

    Do NOT touch `backend/app/api/workflows.py` in this plan. `Depends(require_canvas())` stays on both routes as defense in depth — the middleware owns only the master off-switch, while the dependency still owns caller resolution and the operator/everyone/role audience logic, and two shipped structural tests assert the dependency is present and that `require_visible` is not stacked.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_revert_byte_identical.py tests/test_181_flip_on.py tests/test_182_grounding_bundle.py tests/test_181_off_audience.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -c "import app.main"` exits 0 — no import cycle introduced by the new middleware module.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_revert_byte_identical.py tests/test_181_flip_on.py tests/test_182_grounding_bundle.py tests/test_181_off_audience.py -q` reports 0 failed — the shipped gate behaviour (404-when-off for operator and user, 200-after-flip-on, the pre-auth 404, the require_canvas-alone structural assertions) is unchanged.
    - `grep -c "BaseHTTPMiddleware" backend/app/middleware/canvas_gate.py` returns 0 — the module is pure ASGI, so it can never buffer an SSE response.
    - `grep -c "CANVAS_GATED_PATHS" backend/app/middleware/canvas_gate.py` returns >= 1 and the frozenset contains the literals `/workflows/validate` and `/workflows/grounding-bundle`.
    - `grep -n "add_middleware" backend/app/main.py` shows `CanvasGateMiddleware` registered BEFORE `MaintenanceMiddleware`, which is before `SetupMiddleware`, which is before `CORSMiddleware`.
    - The flag read is in-memory only: `grep -c "supabase\|get_pg_pool\|await load_app_settings" backend/app/middleware/canvas_gate.py` returns 0.
    - Fail-closed polarity is present: the `except Exception` branch of the flag helper returns `True` (gate ON), not `False`.
    - Behavioural proof (covered by Task 3's test file, and the executor must observe it once by hand): with the flag cold-off, `POST /workflows/validate` with body bytes `{` returns 404, and `GET /workflows/validate` returns 404.
  </acceptance_criteria>
  <done>The flag decision for both canvas paths happens before Starlette routing and before FastAPI decodes any body, and the off-state response is the byte-identical 404 for every method and for the trailing-slash variant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dynamic flag-aware app.openapi() hook that drops the canvas surface only while off</name>
  <files>backend/app/middleware/canvas_gate.py, backend/app/main.py</files>
  <read_first>
    - backend/app/middleware/canvas_gate.py (as written in Task 1 — `CANVAS_GATED_PATHS` and `_read_canvas_is_off` are reused verbatim by the filter; do not declare a second copy of either)
    - backend/venv/Lib/site-packages/fastapi/applications.py lines 960-1012 (`FastAPI.openapi` and the `/openapi.json` route handler — proof that `self.openapi()` is resolved at request time and that `self.openapi_schema` is the cache)
    - backend/app/main.py lines 590-630 (where the hook must be wired, after `app = FastAPI(...)`)
    - backend/app/api/workflows.py lines 272-360 (the five response models `Verdict`, `ValidateResponse`, `PaletteFolder`, `PaletteSkill`, `GroundingBundleResponse` — to understand what the reference graph under the two canvas operations actually contains)
    - .planning/phases/182-server-validation-seam/182-REVIEW.md section "CR-02" (the evidence and the two fix options; D-182-R2-02 selected the second one)
  </read_first>
  <behavior>
    - Flag off: `GET /openapi.json` returns 200 and its `paths` object contains neither `/workflows/validate` nor `/workflows/grounding-bundle`.
    - Flag off: `components.schemas` contains none of `ValidateResponse`, `Verdict`, `GroundingBundleResponse`, `PaletteFolder`, `PaletteSkill`.
    - Flag off: `components.schemas` still contains schemas used by other routes (for example `PublishVerdict` and `HTTPValidationError`), and `paths` still contains the other `/workflows/*` routes — nothing beyond the canvas surface is removed.
    - Flag on: all five schemas and both paths are present again.
    - Both directions hold in the SAME process, in either order — off then on, and on then off.
    - `GET /docs` still returns 200 in both states; it is a static Swagger shell that fetches `/openapi.json`, so filtering the schema is what removes the disclosure.
  </behavior>
  <action>
    Add the OpenAPI filter to `backend/app/middleware/canvas_gate.py`, co-located with `CANVAS_GATED_PATHS` so there is exactly ONE list of canvas-gated paths in the app. Explain in the module docstring that this file owns BOTH halves of the off-switch's non-discoverability contract: the request-path half (the middleware) and the schema half (this filter).

    Write a private helper that collects schema names referenced by an arbitrary JSON-ish document: walk dicts and lists recursively, and whenever a dict has a `$ref` key whose value is a string starting with `#/components/schemas/`, record the trailing name. Then write a second helper that expands a starting set of names to its transitive closure by repeatedly walking the definitions of already-collected names inside `components.schemas`.

    Write `canvas_filtered_openapi(schema: dict) -> dict` that returns a filtered DEEP COPY (use `copy.deepcopy`) and never mutates its argument:
    (a) pop every key in `CANVAS_GATED_PATHS` from the copy's `paths` mapping, keeping the popped path items;
    (b) compute `removed_refs` = the transitive closure of schema names referenced from those popped path items;
    (c) compute `kept_refs` = the transitive closure of schema names referenced from everything that REMAINS in the copy (the surviving `paths`, plus `webhooks` and any other top-level content present);
    (d) delete `components.schemas[name]` for every name in `removed_refs - kept_refs`.
    Deriving the removal set from the reference graph rather than hardcoding the five model names is deliberate: Phase 183+ adds routes to `CANVAS_GATED_PATHS` and their models must disappear with them without anyone remembering to update a literal list. Subtracting `kept_refs` guarantees a shared model referenced by a non-canvas route is never removed, and starting from `removed_refs` guarantees a pre-existing orphan schema unrelated to the canvas is never touched — so the filtered document equals what the app would publish if the two canvas routes had never been declared.

    Write `build_canvas_aware_openapi(app)` returning a zero-argument callable to assign to `app.openapi`. Capture the app's ORIGINAL bound `openapi` method ONCE, at build time, into a local. The returned callable must: call the original (which generates the FULL schema on first call and caches it on `app.openapi_schema`, so generation cost is paid exactly once); return that object UNCHANGED when `_read_canvas_is_off()` is False; otherwise return `canvas_filtered_openapi(full)`.

    THE CACHE TRAP — this is the load-bearing detail of D-182-R2-02 and must be handled explicitly, with a comment saying so. `FastAPI.openapi()` memoizes on `self.openapi_schema`, so a naive hook that computes the filtered document once and stores it there serves a STALE schema forever: it would keep hiding the routes after the flag is flipped on, or keep publishing them after the flag is flipped off. Therefore the filtered document is (1) computed per request, (2) built with `copy.deepcopy` so the cached full schema is never mutated in place, and (3) NEVER assigned back to `app.openapi_schema`. Only the FULL schema is ever cached. `/openapi.json` is not a hot path, so the per-request deep copy is an acceptable cost for a correct answer.

    In `backend/app/main.py`, immediately after `app = FastAPI(...)` at line 592 (before or after the middleware registrations — order does not matter for this hook), import `build_canvas_aware_openapi` from the same module and assign `app.openapi = build_canvas_aware_openapi(app)`. Comment it as the D-182-R2-02 schema half of the off-switch, and record the deliberate boundary: `include_in_schema=False` was rejected because it would hide both routes from `/docs` permanently, including while the canvas is ON, degrading the API docs for the whole remainder of the v3.6 milestone (phases 183-189 all build on this seam).

    Record one further boundary in the same comment so a future verifier does not re-raise it as an unfixed leak: `GET /docs` deliberately continues to return 200 in both flag states. It is a static Swagger UI shell that carries no route information itself and renders whatever `/openapi.json` serves; app-wide `docs_url` gating was never a convention in this codebase and is not introduced here.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_revert_byte_identical.py tests/test_181_flip_on.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -c "import app.main"` exits 0.
    - `grep -c "openapi_schema" backend/app/middleware/canvas_gate.py` returns 0 — the filtered document is never written to FastAPI's cache from the filter module (only the original bound method touches it).
    - `grep -c "deepcopy" backend/app/middleware/canvas_gate.py` returns >= 1 — the cached full schema is never mutated in place.
    - The five model names are NOT hardcoded in the filter: `grep -c "ValidateResponse\|PaletteFolder\|PaletteSkill\|GroundingBundleResponse" backend/app/middleware/canvas_gate.py` returns 0 (they may appear only in the TEST file, as expectations).
    - `grep -n "app.openapi" backend/app/main.py` shows a single assignment wiring `build_canvas_aware_openapi(app)`.
    - Behavioural proof (pinned by Task 3, and observed once by hand): in one process, with the flag off, `GET /openapi.json` omits both canvas paths and all five schemas; after flipping the flag on WITHOUT restarting, the same request includes them again.
    - Falsification recorded in the SUMMARY: temporarily make the hook assign its filtered result to `app.openapi_schema`, confirm the both-directions test FAILS on the second (flag-on) assertion, then revert. Record the observed failure message.
  </acceptance_criteria>
  <done>`/openapi.json` reflects the live flag in both directions with the full schema generated only once, and the canvas paths plus exactly their own schemas disappear while off.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Falsifiable probes — malformed body, wrong method, trailing slash, unbuilt-path byte-identity, both-directions OpenAPI flip; and retire both tautologies</name>
  <files>backend/tests/test_182_canvas_gate.py, backend/tests/test_revert_byte_identical.py, backend/tests/unit/test_182_validate.py</files>
  <read_first>
    - backend/tests/test_revert_byte_identical.py (the whole file — the `_cold_off` helper, `_MINIMAL_VALID_DEFINITION`, the tautology at line 132, and the two-segment unbuilt-path parity probe at lines 231-236 that this plan keeps)
    - backend/tests/test_181_flip_on.py lines 1-70 (the `_flipped_on` helper this file needs for the flag-on direction)
    - backend/tests/unit/test_182_validate.py lines 325-345 (the second tautology — the `severity` set-comparison guaranteed by the `Literal`)
    - backend/tests/unit/test_182_severity_codes.py lines 141-190 (`test_every_known_code_classifies_exactly_as_the_pinned_table` — proof the taxonomy is now pinned properly, so the tautology can be deleted rather than replaced)
    - backend/app/middleware/canvas_gate.py (as written in Tasks 1 and 2)
    - .planning/phases/182-server-validation-seam/182-REVIEW.md sections "CR-01", "CR-02" and "IN-04" (the exact assertions the reviewer proposed, and the second tautology's location)
  </read_first>
  <behavior>
    - A malformed-body POST to each canvas path, flag off, returns 404 and its status, body bytes and content-type all equal those of `GET /workflows/__nope__/__nope__`.
    - A wrong-method request to each canvas path, flag off, returns 404 (not 405).
    - A trailing-slash request to each canvas path, flag off, returns 404 (not 307 and not 422).
    - An anonymous caller and an authenticated-operator caller both get the same 404 for the malformed-body probe.
    - `GET /openapi.json` with the flag off omits both canvas paths and all five canvas schemas while still containing a control schema and a control path from a non-canvas route.
    - `GET /openapi.json` with the flag on contains both canvas paths and all five schemas, in the same process, after the off assertion has already run.
    - `test_revert_byte_identical.py` no longer contains an assertion that cannot fail; the malformed-body case is exercised there or in the new file, and the surviving assertions still pass.
  </behavior>
  <action>
    Create `backend/tests/test_182_canvas_gate.py`. Module docstring: name the phase, VALID-01, ROADMAP SC#3, and state that this file closes the CR-01/CR-02 blocker by probing the two ways the flag-off state was distinguishable from an unbuilt route — the request-body decode race and the published OpenAPI schema. State explicitly that every probe here FAILS against the pre-fix code, and name the observed pre-fix results (422 for the malformed body, 405 for the wrong method, 422 for the trailing slash, and both paths plus five schemas present in `/openapi.json`), so a future reader can re-falsify. Follow repo conventions: imports inside test bodies, `client` fixture, `_cold_off` / `_flipped_on` helpers copied from the sibling files (they are three-line local helpers in every one of these files — keep that convention rather than inventing a shared fixture).

    Write these tests.

    (1) Malformed body byte-identity. For each of `/workflows/validate` and `/workflows/grounding-bundle`, with the flag cold-off and no Authorization header, POST the raw bytes `{` with header `content-type: application/json` using the client's `content=` argument (NOT `json=`, which would serialize valid JSON). Assert status 404. Then issue the same malformed POST to `/workflows/__nope__/__nope__` and assert the two responses agree on `status_code`, on `.content` (raw bytes, not parsed JSON), and on the `content-type` header. Assert the parsed body equals `{"detail": "Not Found"}`. Add an explicit negative assertion that the status is not 422 with a failure message naming CR-01.

    (2) Wrong-method non-discoverability. With the flag off, `GET /workflows/validate` and `POST /workflows/grounding-bundle` must both return 404, with a failure message explaining that a 405 admits a handler is declared at exactly that path while the canvas is off.

    (3) Trailing-slash non-discoverability. With the flag off, `POST /workflows/validate/` (malformed body) and `GET /workflows/grounding-bundle/` must both return 404. Note in the docstring that Starlette's `redirect_slashes` only redirects when the slash-stripped path matches a mounted route, so a 307 here would itself be the existence signal.

    (4) The authenticated-operator case. Reuse the `_is_op_true` + `authenticate_canvas_request` injection posture from `test_revert_byte_identical.py::test_require_canvas_404s_for_an_authenticated_operator_when_off`, and assert the malformed-body probe is still 404 for an operator — the middleware precedes auth entirely, so this must hold by construction.

    (5) OpenAPI, flag off. Assert `GET /openapi.json` is 200; assert neither canvas path is in `paths`; assert none of `ValidateResponse`, `Verdict`, `GroundingBundleResponse`, `PaletteFolder`, `PaletteSkill` is in `components.schemas`. Assert a CONTROL is untouched: `PublishVerdict` is still in `components.schemas` and `/workflows/published` is still in `paths` (read the router to confirm the exact control path before pinning it). This control is what proves the filter removes the canvas surface and not more.

    (6) OpenAPI, both directions in one process — the D-182-R2-02 cache-trap guard. In a SINGLE test, flip the flag off, GET `/openapi.json`, assert absence; then flip the flag on via `_flipped_on`, GET `/openapi.json` again, and assert BOTH canvas paths and ALL FIVE schemas are present. Then flip back off and assert absence again. The failure message must name the cache trap: a hook that memoizes its filtered result on `app.openapi_schema`, or that mutates the cached full schema in place, passes the first assertion and fails the second. Add a comment stating this test is the falsifier for that specific implementation mistake.

    (7) A non-canvas path is unaffected. With the flag off, assert some ordinary route still behaves as before — for example `GET /health` returns 200 — proving the middleware is a no-op outside `CANVAS_GATED_PATHS`.

    Then fix the two tautologies.

    In `backend/tests/test_revert_byte_identical.py`, inside `test_require_canvas_404s_when_off`: replace the line asserting `resp_validate.status_code != 422` (and its misleading trailing comment claiming the gate fired before body validation) with a REAL malformed-body probe — POST the bytes `{` with the JSON content-type to `/workflows/validate` and assert 404, with a comment naming CR-01 and stating that this assertion fails against the pre-middleware code with a 422. Keep the surrounding valid-body 404 assertions, keep the `!= 403` and `!= 405` assertions, and keep the positive mounted-route control at the end of the test. Update the module docstring's Phase-182 paragraph, which currently explains that `POST /workflows/validate` is probed with a VALID body specifically so a 422 cannot race the 404: state that the flag is now decided in `CanvasGateMiddleware` ahead of routing and body decode, so the hostile-input case is probed directly rather than avoided.

    In `backend/tests/unit/test_182_validate.py`, delete the line `assert {v.severity for v in resp.verdicts} <= {"error", "incomplete"}` inside `test_every_severity_is_one_of_the_two_literals`. It is guaranteed by `Verdict.severity: Literal["error", "incomplete"]` — construction would raise before the assertion could ever run — and `test_182_severity_codes.py::test_every_known_code_classifies_exactly_as_the_pinned_table` now pins the taxonomy against the classifier for real. Keep the test's other assertions (`resp.verdicts` non-empty and `resp.ok is False`), rename the test to describe what it actually proves now (that a genuinely dirty draft produces verdicts and reports `ok False`), and update its docstring to say the closed-taxonomy claim moved to `test_182_severity_codes.py`. Do not delete the whole test — that would reduce coverage.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_182_canvas_gate.py tests/test_revert_byte_identical.py tests/unit/test_182_validate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_182_canvas_gate.py tests/test_revert_byte_identical.py tests/unit/test_182_validate.py -q` exits 0.
    - `grep -c "^\s*def test_" backend/tests/test_182_canvas_gate.py` returns >= 7 — all seven behaviours above are covered.
    - The malformed-body probe uses raw bytes, not a serialized object: `grep -c "content=b\"{\"" backend/tests/test_182_canvas_gate.py` returns >= 1.
    - Byte-identity is asserted on the response body and headers, not just the status: the file contains a comparison of `.content` between the gated path and `/workflows/__nope__/__nope__`, and a comparison of the `content-type` header.
    - `grep -c "status_code != 422" backend/tests/test_revert_byte_identical.py` returns 0 — the tautological assertion is gone.
    - `grep -c "<= {\"error\", \"incomplete\"}" backend/tests/unit/test_182_validate.py` returns 0 — the second tautology is gone.
    - No test count regression: `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_publish_grounding_stage.py tests/unit/test_182_severity_codes.py tests/unit/test_publish_service.py tests/test_182_extraction_parity.py tests/test_182_grounding_bundle.py tests/test_revert_byte_identical.py tests/test_181_flip_on.py -q` reports at least 80 passed and 0 failed.
    - Falsification recorded in the SUMMARY, three separate observations: (a) with the `app.add_middleware(CanvasGateMiddleware)` line commented out in `main.py`, the malformed-body test FAILS with 422 and the wrong-method test FAILS with 405; (b) with the `app.openapi` assignment commented out, tests (5) and (6) FAIL; (c) restore both and confirm green. Record the exact observed statuses.
  </acceptance_criteria>
  <done>Every probe that proves the off-switch is genuinely non-discoverable exercises hostile input, both tautological assertions are gone, and the OpenAPI hook is pinned in both flag directions within one process.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous internet -> ASGI request path | An unauthenticated caller can send arbitrary bytes to any path. Everything the app does BEFORE authentication — routing, body decode, error shaping — is observable and is a disclosure channel. |
| anonymous internet -> `GET /openapi.json` / `GET /docs` | Unauthenticated, unconditional reads on any finalized deploy (including the live `superrag.cloud` box). Whatever the schema contains is public. |
| operator feature flag -> reachable surface | `visual_workflow_canvas` is the operator's revert switch (HARD gate #1). Any surface that ignores it is a non-revertible remnant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-24 | Information Disclosure | `POST /workflows/validate` body-decode path | mitigate | THE BLOCKER. `CanvasGateMiddleware` decides `feature_audience("visual_workflow_canvas") == "off"` in pure ASGI, before Starlette routing and before FastAPI's `get_request_handler` decodes the body, and short-circuits to `JSONResponse(404, {"detail": "Not Found"})`. A malformed byte can no longer distinguish the gated path (422 today) from an unbuilt path. Pinned by a test that sends raw `b"{"` and compares status, body bytes and content-type against `/workflows/__nope__/__nope__`. |
| T-182-25 | Information Disclosure | wrong-method + trailing-slash probes on the canvas paths | mitigate | The gate matches on PATH ONLY (all methods) and normalizes one trailing slash, so `GET /workflows/validate` returns 404 instead of today's 405, and `POST /workflows/validate/` returns 404 instead of a 307 redirect into a 422. Both are pinned by dedicated probes. |
| T-182-26 | Information Disclosure | `GET /openapi.json` served to anonymous callers | mitigate | `app.openapi` is replaced by a flag-aware hook that, while off, returns a filtered deep copy with `CANVAS_GATED_PATHS` removed from `paths` and the schemas reachable ONLY from those operations removed from `components.schemas`. Removal is derived from the `$ref` graph, so Phase 183+ routes are covered automatically and a schema shared with a non-canvas route is provably never removed. Pinned by an absence test plus a control-schema presence test. |
| T-182-27 | Tampering (stale-cache correctness) | FastAPI's `app.openapi_schema` memo | mitigate | The full schema is cached once by the original bound method; the filtered variant is computed per request via `copy.deepcopy` and is NEVER written to `app.openapi_schema`, so neither flag direction can serve a stale document. A single-process off-then-on-then-off test is the falsifier, and the executor must observe it failing against a deliberately mis-cached implementation before trusting it. |
| T-182-28 | Denial of Service | per-request deep copy of the OpenAPI document while off | accept | `/openapi.json` is a documentation endpoint, not on any hot path, and the deep copy only runs while the canvas is off. The alternative (caching the filtered document) reintroduces T-182-27, which is a correctness defect on the off-switch — the trade is deliberate and one-directional. |
| T-182-29 | Elevation of Privilege | removal of the per-route dependency | accept | `Depends(require_canvas())` is deliberately NOT removed. The middleware owns only the master off-switch; caller resolution and the operator / everyone / role audience decision stay in the dependency (D-182-05), so a middleware bypass (for example a future mount under a different prefix) still meets the route-level gate. Two shipped structural tests assert the dependency's presence and `require_visible`'s absence. |
| T-182-30 | Spoofing (fail-open on a settings blip) | the middleware's flag read | mitigate | `_read_canvas_is_off` wraps the in-memory `feature_audience` read in `try/except` returning `True` — FAIL CLOSED, the opposite polarity to the `MaintenanceMiddleware` analog it otherwise mirrors, matching D-181-02. A cold cache or DB blip resolves to 404, never to a reveal. No DB call is made per request (D-v2.5-01), pinned by a grep acceptance criterion. |
| T-182-31 | Information Disclosure | `GET /docs` still returns 200 | accept | Recorded boundary, not an oversight. `/docs` is a static Swagger UI shell containing no route information; it renders whatever `/openapi.json` serves, which T-182-26 now filters. D-182-R2-02 explicitly chose the dynamic hook over `include_in_schema=False` so `/docs` stays useful once the canvas is on, and app-wide `docs_url` gating has never been a convention in this codebase. |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan — no `requirements.txt` change and no new dependency. `copy`, `starlette.responses` and `starlette.types` are stdlib / already-present. Nothing to audit. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/test_182_canvas_gate.py tests/test_revert_byte_identical.py tests/test_181_flip_on.py tests/test_181_off_audience.py tests/test_182_grounding_bundle.py tests/unit/test_182_validate.py -q` reports 0 failed.
- The full phase-182 surface stays at or above the measured baseline of 80 passed / 0 failed: `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_182_validate.py tests/unit/test_182_folder_scope_keying.py tests/unit/test_182_publish_grounding_stage.py tests/unit/test_182_severity_codes.py tests/unit/test_publish_service.py tests/test_182_extraction_parity.py tests/test_182_grounding_bundle.py tests/test_revert_byte_identical.py tests/test_181_flip_on.py -q`.
- `cd backend && venv/Scripts/python.exe -c "import app.main"` exits 0.
- Three falsification observations recorded in the SUMMARY (middleware disabled, openapi hook disabled, openapi hook mis-cached) with the exact statuses / messages seen.
- `git show --stat` on this plan's commits touches only the five files in `files_modified`. Stage by explicit path only — the working tree carries ~400 unrelated modifications under `.claude/` and 4 untracked files under `backend/`; never `git add -A` and never `git add .`.
- Do NOT create scratch `.py` files anywhere under `backend/` — uvicorn's `--reload` watcher wedges on Windows. Use the session scratchpad for any ad-hoc probe.
</verification>

<success_criteria>
- With the flag off, no request to either canvas path can be distinguished from a request to a path that was never built — not by status, not by body, not by content-type, not by method, not by trailing slash.
- With the flag off, `/openapi.json` publishes neither canvas path nor any of the five canvas-only schemas, while every non-canvas path and schema is untouched.
- Flipping the flag in either direction changes `/openapi.json` immediately within one process; the full schema is generated once and never mutated.
- `Depends(require_canvas())` is still attached to both routes, and both structural gate tests still pass.
- Both tautological assertions are replaced by probes that fail against the pre-fix code, with the failures observed and recorded.
- No migration, no new dependency, no frontend file, no change to `backend/app/api/workflows.py`.
</success_criteria>

<output>
Create `.planning/phases/182-server-validation-seam/182-08-SUMMARY.md` when done.
</output>
