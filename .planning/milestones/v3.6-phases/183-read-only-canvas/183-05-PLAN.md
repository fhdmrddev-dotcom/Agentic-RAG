---
phase: 183-read-only-canvas
plan: 05
type: execute
wave: 2
depends_on: ["183-01", "183-02"]
files_modified:
  - frontend/src/components/workflows/canvasModel.ts
  - frontend/src/components/workflows/canvasModel.test.ts
  - frontend/src/components/workflows/__fixtures__/canvasFixtures.ts
  - frontend/src/components/workflows/canvasModel.fixtures.test.ts
  - frontend/src/components/workflows/__snapshots__/canvasModel.fixtures.test.ts.snap
  - frontend/src/components/workflows/canvasModel.purity.test.ts
autonomous: true
requirements: [CANVAS-01]

must_haves:
  truths:
    - "D-183-12: `toCanvas` is a PURE function of the definition — same phases in, byte-identical nodes/edges out; no DOM read, no network, no clock, no randomness, no input mutation, no two-pass render"
    - "CANVAS-01 / SC#4: the sequential edge is derived by `phase_index + 1` LOOKUP, mirroring `reachability.py:164` exactly — a `phase_index` gap yields NO bridging edge, so the naive sorted-array walk's phantom edge cannot occur (correction C-2)"
    - "D-183-10: an unresolvable `skip_to_phase` target renders as a visibly broken reference — a stub node terminating the edge, never a silent drop and never an edge to a node that does not exist; it agrees with the backend's existing `UNSATISFIABLE_SKIP` verdict"
    - "D-183-09: the SC#4 corpus is TRANSCRIBED from checked-in migrations 061 / 066 / 094 and `scripts/seed-pm-pack.py`, each entry citing its source file:line — never read live — plus ONE synthetic `branching` entry that ships to no database and adds no starter workflow"
    - "SC#3: every phase node's `id` equals its `phase.slug`; `llm_batch_agents` renders as exactly ONE node, never N fan-out lanes"
  artifacts:
    - path: "frontend/src/components/workflows/canvasModel.ts"
      provides: "The pure definition→{nodes,edges} projection plus the frozen layout constants"
      exports: ["toCanvas", "CANVAS_LAYOUT", "PhaseNodeData", "UnresolvedSkipNodeData", "CanvasNode", "CanvasEdge"]
      min_lines: 100
    - path: "frontend/src/components/workflows/__fixtures__/canvasFixtures.ts"
      provides: "The SC#4 corpus: 4 canonical seeds + 3 Starter Library + 2 PM pack + eval_coverage + empty + 1-phase + branching + unresolvable + index-gap"
      min_lines: 200
    - path: "frontend/src/components/workflows/canvasModel.purity.test.ts"
      provides: "The D-183-12 / G-6 tripwire — determinism, non-mutation, and the no-DOM-read source grep"
  key_links:
    - from: "frontend/src/components/workflows/canvasModel.ts"
      to: "backend/app/services/harness/reachability.py:162-169"
      via: "byIndexValue.get(phase_index + 1) — index lookup, not array adjacency"
      pattern: "phase_index \\+ 1|phaseIndex \\+ 1"
    - from: "frontend/src/components/workflows/canvasModel.ts"
      to: "frontend/src/components/workflows/phaseVocabulary.ts"
      via: "parseSkipTarget / nodeTitle / groundingFor / waitsForYou"
      pattern: "from \"@/components/workflows/phaseVocabulary\""
---

<objective>
Build `canvasModel.toCanvas` — the pure projection that carries this phase's truth — and the
transcribed fixture corpus that proves it renders every real seed shape faithfully.

Purpose: SC#2 and SC#4 are provable with zero DOM if the projection is a pure function, which is
exactly what D-183-12 locks. The risk is concentrated in one place: the obvious edge implementation
(sort the array, draw i→i+1) produces a PHANTOM EDGE on a non-contiguous `phase_index`, which G-6
names as a failure condition and SC#4 forbids. `reachability.py:164` derives the successor by index
LOOKUP with an explicit comment that a gap cannot be bridged; the canvas edge set must agree with
that adjacency or the picture lies about what will run.
Output: the model, the corpus, a snapshot sweep, and the purity tripwire.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-read-only-canvas/183-CONTEXT.md
@.planning/phases/183-read-only-canvas/183-RESEARCH.md
@.planning/phases/183-read-only-canvas/183-PATTERNS.md
@.planning/phases/183-read-only-canvas/183-VALIDATION.md
@.planning/phases/183-read-only-canvas/183-02-SUMMARY.md
@.planning/research/PITFALLS.md
@frontend/src/components/workflows/phaseVocabulary.ts
@frontend/src/components/workflows/deriveTier.ts
@backend/app/services/harness/reachability.py
</context>

<interfaces>
<!-- Contracts extracted so the executor writes rather than explores. -->

The AUTHORITATIVE adjacency to mirror (`backend/app/services/harness/reachability.py:158-169`) — note
the comment: a gap CANNOT be bridged, and the phase after the gap is genuinely an orphan:
```python
adjacency: dict[str, set[str]] = {p.slug: set() for p in phases}
for p in phases:
    successor = by_index_value.get(p.phase_index + 1)
    if successor is not None:
        adjacency[p.slug].add(successor.slug)
    for target in _skip_targets(p):
        if target in by_slug:
            adjacency[p.slug].add(target)
```

The donor loop being ADAPTED, not copied (`PhaseSpineGraph.tsx:102-118`) — note the `slugSet.has(target)`
SILENT DROP that D-183-10 replaces, and the non-mutating `[...phases].sort(...)` idiom that is
load-bearing for the purity test:
```ts
const ordered = [...phases].sort((a, b) => a.phase_index - b.phase_index)
const slugSet = new Set(ordered.map((p) => p.slug))
for (const phase of ordered) {
  for (const v of phase.validators ?? []) {
    const target = parseSkipTarget(v.on_failure)
    if (target && slugSet.has(target)) { /* ← the silent drop */ }
  }
}
```

The shipped const-table idiom for `CANVAS_LAYOUT` (`deriveTier.ts:57-79`): one exported frozen table
with `as const satisfies Record<K, T>`, so a test asserts against the ONE source rather than scattered
literals.

The shipped discriminated-union idiom for "one of two honest outcomes" (`soulData.ts:129`):
`export type SoulDeliverable = { kind: "file"; label: string } | { kind: "chat" }` — a resolved-vs-broken
edge is exactly this shape.

Fixture source-of-truth locations (§Fixture Corpus, RESEARCH:865-903):
| Fixture group | Checked-in source |
|---|---|
| 4 canonical seeds | `supabase/migrations/061_harness_seed_templates.sql:48-236`; Python mirror `backend/tests/conftest.py:837-938` (`_four_seed_defs`, docstring declares itself the SINGLE SOURCE OF TRUTH) |
| Starter Library ×3 | `supabase/migrations/094_starter_workflows.sql:9-11` (slugs) and `:30-32` (the emit gate set) |
| PM pack ×2 | `scripts/seed-pm-pack.py:106,111,405,433`; phase shape at `:358,369` |
| 5-phase maximum | `supabase/migrations/066_eval_coverage_seed.sql:64-…` |
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: canvasModel.toCanvas — the pure projection with the C-2 index-lookup edge set</name>
  <files>frontend/src/components/workflows/canvasModel.ts, frontend/src/components/workflows/canvasModel.test.ts</files>
  <read_first>
    - `backend/app/services/harness/reachability.py` lines 85-180 — `parse_skip_target`, `_skip_targets`, the `UNSATISFIABLE_SKIP` emission at `:147-156`, and the adjacency build at `:158-169`. This is the behaviour the model must agree with
    - `frontend/src/components/workflows/PhaseSpineGraph.tsx` lines 100-120 — the donor sort-then-walk loop and the silent-drop filter being replaced
    - `frontend/src/components/workflows/phaseVocabulary.ts` — the exact export names from plan 183-02
    - `frontend/src/components/workflows/deriveTier.ts` lines 52-79 — the `as const satisfies Record<K,T>` const-table idiom for `CANVAS_LAYOUT`
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 1442-1498 (the `toCanvas` skeleton and the end-cap counting note) and lines 640-683 (correction C-2 in full)
    - `.planning/phases/183-read-only-canvas/183-CONTEXT.md` — D-183-10, D-183-11, D-183-12, and the `<specifics>` topology rules
    - `.planning/research/PITFALLS.md` — Pitfall 3 (layout NEVER in the definition JSONB)
  </read_first>
  <behavior>
    - `toCanvas([])` returns `{ nodes: [], edges: [] }` — no end cap, no ghost node (D-183-11)
    - a 3-phase contiguous definition yields 3 nodes of `type: "phase"` plus exactly one `endCap` node, and 3 edges (two sequential plus the end-cap edge)
    - every phase node's `id` equals its `phase.slug` (SC#3)
    - shuffled input order yields byte-identical output to sorted input order (the model sorts by `phase_index`)
    - `phase_index = [0, 1, 3]` yields EXACTLY ONE sequential edge (`0→1`); there is NO `1→3` edge (C-2 — the phantom edge)
    - a validator with `on_failure: "skip_to_phase:<existing-slug>"` yields exactly one skip edge whose `target` is that slug
    - a validator with `on_failure: "skip_to_phase:nonexistent"` yields an `unresolvedSkip` stub node AND an edge terminating on that stub — never a dropped edge, never an edge to a non-emitted id (D-183-10)
    - `on_failure` values `fail_run`, `ask_user`, `retry` and a missing `validators` array yield no skip edge and no stub
    - `llm_batch_agents` yields exactly ONE node (fan-out is runtime, not topology)
    - a 1-phase definition yields 1 phase node, 1 end-cap node, and 1 edge
    - every emitted edge's `source` and `target` resolve to an emitted node id (no phantom edge, structurally)
    - node `data.title` is the plain-language title and `data.technicalTitle` carries the label · slug form; both are present on every phase node
    - the returned objects contain no function values and survive `JSON.parse(JSON.stringify(...))` unchanged (serializable — the snapshot gate depends on it)
  </behavior>
  <action>
    Create `frontend/src/components/workflows/canvasModel.ts`.

    Signature: `toCanvas(phases: PhaseSpecJSON[]): { nodes: CanvasNode[]; edges: CanvasEdge[] }`. It
    takes the phases array — the only canvas-relevant content of a `WorkflowDefinition` — and matches
    the shipped `PhaseSpineGraphProps.phases` shape so the canvas is a drop-in peer. It takes NO
    selection argument and NO technical-names argument: selection is applied by the view in plan
    183-06, and both title forms are emitted on every node so the ⌥ reveal is a render-time choice.
    That keeps `toCanvas` a pure function of the definition ALONE, which is what makes D-183-12's
    "same definition in ⇒ byte-identical positions out" a one-line test.

    Import ONLY TYPES from the canvas library — `import type { Node, Edge } from "@xyflow/react"` — so
    the model stays library-agnostic at runtime. Arrow markers and edge styling belong to the view
    (plan 183-06 sets them via `defaultEdgeOptions`), not here.

    Export `CANVAS_LAYOUT` as one frozen const table (the `deriveTier.ts:57-79` idiom) so a test can
    assert against the single source rather than scattered literals. Values (Claude's discretion,
    chosen to make UAT row U-2 pass by construction — five phases at 320px pitch is 1,600px, matching
    sketch 136-B's measurement): `NODE_WIDTH: 260`, `NODE_MIN_HEIGHT: 96`, `PITCH_X: 320`,
    `LANE_Y: 0`, `SKIP_LANE_Y: 200`, `EDGE_ANCHOR_Y: 28`, `END_CAP_SIZE: 40`.

    Layout is a pure arithmetic function of ordinal position — NO DOM measurement, ever. A phase at
    sorted position `col` is placed at `x = col * PITCH_X`, `y = LANE_Y`. The end cap goes at
    `x = phases.length * PITCH_X`. An unresolved-skip stub goes at
    `x = sourceCol * PITCH_X + PITCH_X / 2`, `y = SKIP_LANE_Y`. Sketch 136 measured DOM heights to
    avoid clipping on gate-heavy nodes; solve that in CSS instead (plan 183-06): uniform node width,
    the card grows DOWNWARD, and handles anchor at a fixed `EDGE_ANCHOR_Y` offset from the node top
    rather than at 50%, so a taller card never moves the edge baseline.

    Node types (the `type` field, which the view maps through `nodeTypes`): `"phase"`,
    `"unresolvedSkip"`, `"endCap"`.

    `PhaseNodeData` carries: `slug`, `phaseIndex`, `phaseType`, `title` (from `nodeTitle`),
    `technicalTitle` (from `technicalTitle`), `subtitle` (from `PHASE_TYPE_SUBTITLES`, empty string for
    an unknown type), `grounding` (from `groundingFor`), `waitsForYou` (from `waitsForYou`). xyflow v12
    types node data as `Record<string, unknown>`, so the interface needs an index signature — declare
    it explicitly and comment why. `UnresolvedSkipNodeData` carries `declaredTarget` and `fromSlug`.
    The end-cap node carries no data beyond an empty object.

    Every phase node also sets `draggable: false`, `ariaRole: "button"` and `ariaLabel` — the v12
    node-object a11y fields. Use the shipped label sentence shape from `PhaseSpineGraph.tsx:188`:
    position, then the title, then the phase type in parentheses. The stub and end-cap nodes set
    `draggable: false`, `selectable: false` and `focusable: false` — they are not selection targets.

    Edge derivation, in this order:

    (1) SEQUENTIAL — for each phase, look up `byIndexValue.get(phase.phase_index + 1)` and emit an edge
    only when that lookup hits. This is correction C-2 and it is the single most important line in the
    plan: `reachability.py:164` does exactly this, with an explicit comment that a gap cannot be
    bridged. Do NOT walk `ordered[i]` → `ordered[i+1]`; for `phase_index [0,1,3]` the array walk draws
    `0→1→3`, a phantom edge that G-6 names as a failure and SC#4 forbids, while the lint yields `0→1`
    plus an orphan. Edge id `seq:<from>-><to>`, `data.kind: "flow"`.

    (2) SKIP — for each validator, `parseSkipTarget(v.on_failure)`. When the target resolves to an
    existing slug, emit `skip:<from>-><target>` with `data.kind: "skip"`. When it does NOT resolve
    (D-183-10), do NOT drop it silently the way the Spine does today — migration 065 dropped a real
    gate for exactly this reason. Emit an `unresolvedSkip` STUB node plus an edge terminating on that
    stub: the edge connects to no phase node (so it is not a phantom edge) and it does not lie about
    what the definition declares. Its wording must agree with the backend's `UNSATISFIABLE_SKIP`
    verdict (`reachability.py:147-156`).

    (3) END CAP — sketch 136 requires every flow to end in an explicit `○ end` cap, never a dangling
    edge stub. Emit EXACTLY ONE `endCap` node and one edge from the phase with the MAXIMUM
    `phase_index` (`data.kind: "end"`). On a `phase_index` gap this deliberately leaves the pre-gap
    phase with no outgoing edge — that is the honest picture of an orphan, and drawing a second cap
    would falsely imply that phase ends the flow. Emit no cap at all for an empty definition.

    Reserved node ids must never collide with a real slug (`PhaseSpec.slug` is an unconstrained `str`
    in `backend/app/models/harness.py:190`). Build them through a tiny deterministic helper that
    prefixes `__canvas__` and, while the candidate id is already in the slug set, prepends another `_`.
    Three lines, total, collision-proof, still pure.

    Hard prohibitions: no `getBoundingClientRect`, `offsetHeight`, `offsetWidth`, `document.`,
    `window.`, `Date.now`, `Math.random`, `fetch`, or any `@/lib/api` import. Do not mutate the input
    array or any phase object — use `[...phases].sort(...)` (the shipped `PhaseSpine.tsx:38-40` idiom).
    Do not write a `position`, `x`, `y` or `layout` key back onto any definition object (Pitfall 3 /
    G-6). Do not call `POST /workflows/validate` or any other endpoint — the canvas must draw with zero
    network calls (D-183-15; the server verdict arrives in Phase 184).

    Write `canvasModel.test.ts` covering every behaviour row above.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/canvasModel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/workflows/canvasModel.ts` exports `toCanvas`, `CANVAS_LAYOUT`, `PhaseNodeData`, `UnresolvedSkipNodeData`, `CanvasNode`, `CanvasEdge`
    - `grep -c "phase_index + 1" frontend/src/components/workflows/canvasModel.ts` returns ≥ 1, and `grep -cE "ordered\[i ?\+ ?1\]|ordered\[index ?\+ ?1\]" frontend/src/components/workflows/canvasModel.ts` returns 0
    - `grep -cE "getBoundingClientRect|offsetHeight|offsetWidth|document\.|window\.|Date\.now|Math\.random|fetch\(" frontend/src/components/workflows/canvasModel.ts` returns 0
    - `grep -c "@/lib/api" frontend/src/components/workflows/canvasModel.ts` returns 0
    - `grep -c "^import type" frontend/src/components/workflows/canvasModel.ts` returns ≥ 1 and `grep -cE "^import \{[^}]*\} from \"@xyflow/react\"" frontend/src/components/workflows/canvasModel.ts` returns 0 (types-only from the library)
    - `cd frontend && npx vitest run src/components/workflows/canvasModel.test.ts` green with at least one assertion per behaviour row
    - the non-contiguous test explicitly asserts the ABSENCE of an edge whose `source` is the index-1 phase and whose `target` is the index-3 phase
    - `cd frontend && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>The projection is pure, its edge set agrees with `reachability.py`'s adjacency, and the gap case provably produces no phantom edge.</done>
</task>

<task type="auto">
  <name>Task 2: The SC#4 fixture corpus, transcribed from checked-in artifacts</name>
  <files>frontend/src/components/workflows/__fixtures__/canvasFixtures.ts, frontend/src/components/workflows/canvasModel.fixtures.test.ts, frontend/src/components/workflows/__snapshots__/canvasModel.fixtures.test.ts.snap</files>
  <read_first>
    - `backend/tests/conftest.py` lines 836-940 — `_four_seed_defs`, which declares itself the SINGLE SOURCE OF TRUTH for migration 061's definition JSONB. Transcribe the four seeds from here
    - `supabase/migrations/061_harness_seed_templates.sql` lines 48-236 — cross-check the SQL against the Python mirror
    - `supabase/migrations/094_starter_workflows.sql` lines 1-60 — the three Starter Library slugs and the emit phase's `citation_policy` + `citations_required` + `output_file_valid` gate set
    - `supabase/migrations/066_eval_coverage_seed.sql` lines 55-120 — `eval_coverage`, the only 5-phase definition, covering all five non-emit phase types with zero names and zero validators
    - `scripts/seed-pm-pack.py` lines 100-120 and 350-440 — the two `pm-*` slugs and their phase shape
    - `frontend/src/components/workflows/soulData.test.ts` lines 27-96 — the JSDoc-per-fixture style ("A strict workflow: a terminal llm_emit with citation_policy 'strict' → STRICT.")
    - `.planning/phases/183-read-only-canvas/183-RESEARCH.md` lines 865-903 (§Fixture Corpus and §Transcription discipline) and 684-738 (corrections C-3 and C-4)
  </read_first>
  <action>
    Create `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts`. Note plainly in the file
    docblock that this is net-new structure: the frontend has no shared fixture module anywhere — every
    suite declares inline module-level consts — so this directory is created here rather than following
    an existing convention. The backend does have `backend/tests/fixtures/`, so the concept is
    house-approved, just not on the frontend.

    TRANSCRIBE, NEVER READ LIVE. Two live database reads on the same day disagreed (correction C-3:
    CONTEXT and sketches 134-136 quote 119 phases / 40 zero-phase / 10 named / two 5-phase definitions;
    the reproducible read this session found 51 / 71 / 0 / one), and a definition that was 5-phase at
    sketch time is 0-phase now (C-4). A snapshot test whose input drifts is not a gate. Every fixture
    carries a JSDoc line naming its source `file:line` so a future reader can re-verify without a
    database, following the `soulData.test.ts:27-96` style.

    Export each fixture as a typed `PhaseSpecJSON[]` (plus a named `WorkflowDefinition`-shaped wrapper
    where it reads better), and also export one `ALL_FIXTURES` array of
    `{ name, phases, source }` so the sweep test iterates one list and a future fixture cannot be
    silently omitted from the sweep.

    The corpus:
    the FOUR canonical seeds — `research_summarize` (2: `llm_agent`→`llm_single`),
    `plan_execute_verify` (3: `llm_single`→`llm_agent`→`llm_single`, the verify phase carrying a
    `regex_match` validator with `on_failure: "retry"`), `literature_review`
    (3: `programmatic`→`llm_batch_agents`→`llm_single`), `doc_qa_human`
    (3: `llm_agent`→`llm_human_input`→`llm_single`);
    the THREE Starter Library workflows — `risk-register`, `weekly-status-report`,
    `compliance-gap-report`, each 2-phase `llm_agent`→`llm_emit` with the emit carrying
    `citation_policy: "strict"` plus `citations_required` and `output_file_valid` validators
    (correction C-6: these are what CONTEXT calls "the 3 PM-pack starters");
    the TWO PM-pack originals — `pm-weekly-status-report` and `pm-risk-register`, topologically
    identical to the Starter Library rows;
    the 5-phase maximum — `eval_coverage`
    (`programmatic`→`llm_batch_agents`→`llm_agent`→`llm_human_input`→`llm_single`, zero names, zero
    validators), which is what UAT row U-2 exercises;
    the EMPTY draft — `{ phases: [] }`, the single most common canvas state (D-183-11);
    a ONE-phase definition — the live corpus has four, and the layout maths has its own degenerate
    case there (no preceding edge before the `○ end` cap);
    the SYNTHETIC branching entry (D-183-09) — `gather → assess ⇢(on fail) escalate`, skipping
    `draft`, sketch 136's proposed shape. `skip_to_phase` appears ZERO times across the whole live
    corpus, so SC#1's branch edge cannot be demonstrated from real data. This fixture is TEST-ONLY:
    no DB row is seeded and NO new starter workflow ships — shipping one is a Starter Library / Phase
    187 product decision, not a projection phase's to make;
    the UNRESOLVABLE-skip entry — a validator with `on_failure: "skip_to_phase:nonexistent"` (D-183-10);
    the NON-CONTIGUOUS entry — `phase_index` values `[0, 1, 3]` (C-2).

    Then write `canvasModel.fixtures.test.ts`, the SC#4 sweep. For EVERY entry in `ALL_FIXTURES`
    assert: the count of nodes with `type === "phase"` equals `phases.length` (no dropped phase — note
    this must filter by type, because the end cap and any stub are rendered nodes that do not
    correspond to a phase); every phase node's `id` equals its slug; every edge's `source` and `target`
    resolve to an emitted node id (no phantom edge); no two nodes share an id; and the output survives
    a JSON round-trip unchanged. Add the targeted assertions the general sweep cannot express: the
    `llm_batch_agents` fixtures each yield exactly one node for that phase (two witnesses); the
    branching fixture yields exactly one skip edge landing on `escalate`; the unresolvable fixture
    yields exactly one `unresolvedSkip` node and no edge to a non-existent id; the non-contiguous
    fixture yields exactly one sequential edge.

    Finally add a `toMatchSnapshot()` of `{ nodes, edges }` per fixture and COMMIT the generated
    `.snap` file. The structural invariants above are the real gate; the snapshot is the drift
    tripwire that catches an unintended change to titles, grounding, positions or ids.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/canvasModel.fixtures.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts` exports `ALL_FIXTURES` containing at least 14 entries covering all six phase types and node counts 0, 1, 2, 3 and 5
    - every fixture has a JSDoc comment naming a checked-in source `file:line` (or explicitly stating "hand-authored, test-only" for the synthetic three)
    - `grep -cE "psycopg2|postgres://|localhost:54322|supabase" frontend/src/components/workflows/__fixtures__/canvasFixtures.ts` returns 0 (transcribed, never read live)
    - `cd frontend && npx vitest run src/components/workflows/canvasModel.fixtures.test.ts` green
    - the sweep counts phase nodes with an explicit `type === "phase"` filter (`grep -c 'type === "phase"' frontend/src/components/workflows/canvasModel.fixtures.test.ts` returns ≥ 1)
    - `frontend/src/components/workflows/__snapshots__/canvasModel.fixtures.test.ts.snap` exists and is committed
    - re-running `npx vitest run src/components/workflows/canvasModel.fixtures.test.ts` reports 0 snapshots written and 0 obsolete
    - `cd frontend && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>Every canonical seed, the Starter Library, the PM pack, the 5-phase maximum and the three synthetic edge cases project with no dropped phase and no phantom edge.</done>
</task>

<task type="auto">
  <name>Task 3: The D-183-12 / G-6 purity tripwire</name>
  <files>frontend/src/components/workflows/canvasModel.purity.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/soulData.test.ts` lines 185-205 — the `?raw` purity / anti-duplication source-grep block, the template for this file
    - `frontend/src/components/workflows/canvasModel.ts` — the module under test
    - `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts` — `ALL_FIXTURES`
    - `.planning/research/PITFALLS.md` — Pitfall 3
    - `.planning/phases/183-read-only-canvas/183-CONTEXT.md` — the G-6 "How we'd know this failed" list
  </read_first>
  <action>
    Write `canvasModel.purity.test.ts` — the machine-checkable form of D-183-12 and the first four G-6
    failure conditions.

    Determinism: for every entry in `ALL_FIXTURES`, `JSON.stringify(toCanvas(phases))` equals itself
    across two separate calls — same definition in, byte-identical nodes/edges out.

    Order-independence: a shuffled copy of the input array produces byte-identical output to the
    original, proving the model sorts rather than depending on array order.

    Non-mutation and the Pitfall-3 tripwire: deep-clone each fixture's input BEFORE the call, run
    `toCanvas`, then assert the input still deep-equals the clone AND that a recursive walk of the
    input finds no `position`, `x`, `y` or `layout` key anywhere. A layout key reaching a definition
    object is the first named G-6 failure and would 422 against the backend's `extra="forbid"`.

    Source grep via the shipped `?raw` idiom: the `canvasModel` source must not match
    `/getBoundingClientRect|offsetHeight|offsetWidth|document\.|window\.|Date\.now|Math\.random/`, must
    not match `/from\s+["']@\/lib\/api["']/`, and must not match `/workflows\/validate/` — the canvas
    draws with zero network round trips (D-183-15; the `/validate` seam is Phase 184's).

    Tree-wide single-home guard (G-6 "a third copy of `parseSkipTarget` or the glyph map exists at
    phase end"): assert via `?raw` that the `canvasModel` source contains no `const PHASE_GLYPHS` and
    no `function parseSkipTarget` declaration — it imports both concerns from their one home.

    Layout-constant guard: assert the emitted x positions are exactly `col * CANVAS_LAYOUT.PITCH_X` for
    a known fixture, so the constants table remains the single source and a stray literal cannot creep
    into the placement maths.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/canvasModel.purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflows/canvasModel.purity.test.ts` green
    - the file contains a determinism assertion, an order-independence assertion, a non-mutation deep-equal assertion, a recursive no-`position`/`x`/`y`/`layout`-key walk over the INPUT, and a `?raw` source-grep block
    - deliberately adding a `Date.now()` call to `canvasModel.ts` makes this suite fail (confirm once, then revert)
    - deliberately mutating the input inside `toCanvas` makes this suite fail (confirm once, then revert)
    - `cd frontend && npx vitest run src/components/workflows` reports zero failures across the directory
  </acceptance_criteria>
  <done>Purity, determinism and the no-layout-in-the-definition rule are enforced by tests that provably fail when violated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow definition JSONB → client projection | Authored slugs, names, `on_failure` values and validator kinds drive node and edge construction |
| client projection → workflow definition | The direction that MUST stay closed: no layout may ever flow back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-04 | Tampering / Information Disclosure | canvas layout leaking into `workflow_definitions.definition` | mitigate | Persist nothing. `toCanvas` never mutates its input, and the purity test walks the input after every call asserting no `position` / `x` / `y` / `layout` key exists. This is Pitfall 3 and the first named G-6 failure; a leaked key would 422 against `extra="forbid"` or force relaxing it. |
| T-183-06 | Denial of Service | a malformed or adversarial definition crashing the projection | mitigate | Totality is the live control: an unknown `phase_type` falls through `phaseVocabulary`'s honest echo, an unknown `validator.kind` is ignored, a malformed `on_failure` returns null, a missing `validators` array defaults to `[]`, duplicate or non-contiguous `phase_index` values are handled by lookup rather than array arithmetic, and a slug colliding with a reserved node id is resolved by the deterministic disambiguation helper. Asserted across the 14-fixture sweep. |
| T-183-07 | Tampering | the canvas edge set disagreeing with the server's reachability adjacency | mitigate | The sequential edge is derived by `phase_index + 1` LOOKUP, mirroring `reachability.py:164` line-for-line, and the non-contiguous fixture asserts the absence of the bridging edge. The unresolvable-skip case agrees with `UNSATISFIABLE_SKIP` rather than silently dropping. |
| T-183-10 | Information Disclosure | a client-side call leaking definition content to an unintended endpoint | accept | Zero network calls: the source grep asserts no `@/lib/api` import and no `workflows/validate` reference. There is nothing to leak because nothing is sent. |

## ASVS conclusion (carried from RESEARCH §Security Domain, line 1759)

Only V5 Input Validation applies, and it applies as TOTALITY over data the backend already validated
with Pydantic `_StrictBase` / `extra="forbid"` (`harness.py:30`) before it could reach the client. V2,
V3, V4 and V6 do not apply: no auth surface, no session state, no authz decision, no cryptography. The
ROADMAP's "no threat model" flag for Phase 183 stands.

**Frontend-only, and this plan adds no route and no migration.** If any task grows a backend touch,
this conclusion is VOID.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/canvasModel.test.ts src/components/workflows/canvasModel.fixtures.test.ts src/components/workflows/canvasModel.purity.test.ts` all green
- `cd frontend && npx tsc -b` exits 0
- `cd frontend && npx vitest run src/components/workflows` reports zero failures across the directory
- `cd frontend && npm test` shows no NEW failing test names and a total test count higher than the recorded 1877 baseline (this plan only adds tests)
- `git status --porcelain backend supabase scripts` is empty — no backend source, no migration, no seed script touched
</verification>

<success_criteria>
- `toCanvas` is pure, deterministic, order-independent and non-mutating, proven by tests that fail when violated
- The sequential edge set matches `reachability.py`'s index-lookup adjacency; the `[0,1,3]` fixture has no phantom edge
- An unresolvable `skip_to_phase` renders an honest broken-reference stub rather than a silent drop
- All 14+ fixtures project with no dropped phase, no phantom edge, `node.id === phase.slug`, and `llm_batch_agents` as one node
- No layout key ever reaches a definition object
</success_criteria>

<output>
Create `.planning/phases/183-read-only-canvas/183-05-SUMMARY.md` when done.
Record the exact `CANVAS_LAYOUT` values and the node/edge `type` string vocabulary — plan 183-06's
`nodeTypes` map and CSS must agree with them exactly.
</output>
