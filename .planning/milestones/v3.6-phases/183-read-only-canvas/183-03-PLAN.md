---
phase: 183-read-only-canvas
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/providers/EffectiveFeaturesProvider.tsx
  - frontend/src/providers/EffectiveFeaturesProvider.test.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [CANVAS-01]

must_haves:
  truths:
    - "D-183-03: a page-level component can read `visual_workflow_canvas` without issuing a SECOND `GET /features` — the existing single per-session fetch is broadcast through one context"
    - "The Phase-148 fail-closed policy is preserved EXACTLY — `{}` before first resolve and on any error (T-148-FAILCLOSED), and a NULL context (no provider) is also read as fail-closed, never as 'unknown, show it'"
    - "The WR-01 `userId`-keyed re-probe and the D-04 `refetch` bounce from App's FEATURE_FORBIDDEN_EVENT handler still work unchanged"
    - "`visibleNavItems(effectiveFeatures)` still receives the identical map object; `revertByteIdentical.test.tsx` stays GREEN and UNMODIFIED by this plan"
    - "CANVAS-01: no `NAV_ITEMS` entry tagged `visual_workflow_canvas` is added — D-183-01 released that promise and the 181 scope-freeze assertion depends on its absence"
  artifacts:
    - path: "frontend/src/providers/EffectiveFeaturesProvider.tsx"
      provides: "One context broadcasting the existing effective-features map to any descendant"
      exports: ["EffectiveFeaturesProvider", "useEffectiveFeaturesContext", "useEffectiveFeaturesOptional"]
    - path: "frontend/src/providers/EffectiveFeaturesProvider.test.tsx"
      provides: "The null-context-is-fail-closed and single-source assertions"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/providers/EffectiveFeaturesProvider.tsx"
      via: "mount wrapping the ChatLayout subtree, value fed by the existing hook call"
      pattern: "<EffectiveFeaturesProvider"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/hooks/useEffectiveFeatures.ts"
      via: "the ONE call site, unchanged"
      pattern: "useEffectiveFeatures\\(user\\?\\.id \\?\\? null\\)"
---

<objective>
Promote the existing `useEffectiveFeatures` result to a context so the Builder can gate the Canvas
toggle on `visual_workflow_canvas` without a second network call — additive plumbing only.

Purpose: the pattern mapper found (F-2) that `useEffectiveFeatures` has exactly ONE call site
(`App.tsx:149`), no cache, and no consumer below the nav. A second `useEffectiveFeatures()` call in
`WorkflowBuilderPage` would therefore issue a second `GET /features` per session. Operator decision
OP-2: wrap it in a provider modelled on the shipped `TechnicalNamesProvider`, so two consumers can
never disagree and the fetch budget stays at one.
Output: the provider plus its accessors, its unit suite, and the App mount — with every Phase-148
security-adjacent invariant preserved byte-for-byte.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-read-only-canvas/183-CONTEXT.md
@.planning/phases/183-read-only-canvas/183-PATTERNS.md
@.planning/phases/181-revert-foundation/181-CONTEXT.md
@frontend/src/hooks/useEffectiveFeatures.ts
@frontend/src/providers/TechnicalNamesProvider.tsx
@frontend/src/lib/nav-items.ts
</context>

<interfaces>
<!-- The exact contract to broadcast, unchanged. -->

`frontend/src/hooks/useEffectiveFeatures.ts` — the shape this provider carries. Do NOT modify this file:
```ts
export interface UseEffectiveFeatures {
  /** Fails CLOSED to `{}` on any error AND before the first resolve (T-148-FAILCLOSED).
   *  RENDER-ONLY: the `require_visible` API gates (148-05) are the security wall. */
  features: EffectiveFeatures
  /** True until the one-shot per-session fetch resolves. */
  loading: boolean
  /** Re-fetch the map — the D-04 graceful-bounce re-sync. */
  refetch: () => void
}
export function useEffectiveFeatures(userId: string | null): UseEffectiveFeatures
```

The provider shape to mirror (`frontend/src/providers/TechnicalNamesProvider.tsx:58,90-106`) — a
`createContext<T | null>(null)`, a THROWING accessor for writers, a NON-THROWING optional accessor for
leaves that must render outside a provider in unit tests:
```ts
const TechnicalNamesContext = createContext<TechnicalNamesValue | null>(null)
export function useTechnicalNames(): TechnicalNamesValue { /* throws when ctx === null */ }
export function useTechnicalNamesOptional(): TechnicalNamesValue | null { return useContext(TechnicalNamesContext) }
```

The App call site being preserved (`App.tsx:149-150`):
```ts
const { features: effectiveFeatures, refetch: refetchFeatures } = useEffectiveFeatures(user?.id ?? null)
const navItems = visibleNavItems(effectiveFeatures)
```

The VANISH semantics that transfer (`lib/nav-items.ts:61-73`): strict `=== true`, never truthy — an
absent key must hide. Fail-closed by construction.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create EffectiveFeaturesProvider and its accessors</name>
  <files>frontend/src/providers/EffectiveFeaturesProvider.tsx, frontend/src/providers/EffectiveFeaturesProvider.test.tsx</files>
  <read_first>
    - `frontend/src/providers/TechnicalNamesProvider.tsx` — the file shape, docblock voice, and the throwing/optional accessor pair to mirror
    - `frontend/src/hooks/useEffectiveFeatures.ts` — the full file; the `UseEffectiveFeatures` interface, the fail-closed comments, the WR-01 keying rationale. This file must end the plan byte-unchanged
    - `frontend/src/lib/citationNav.tsx` — the other shipped `createContext<T|null>(null)` + throwing-accessor precedent named in `TechnicalNamesProvider.tsx:12-16`
    - `frontend/src/lib/nav-items.ts` lines 55-75 — the strict `=== true` VANISH semantics and the fail-closed docblock
    - `.planning/phases/183-read-only-canvas/183-PATTERNS.md` §Blocking Findings row **F-2**
  </read_first>
  <action>
    Create `frontend/src/providers/EffectiveFeaturesProvider.tsx`, modelled file-for-file on
    `TechnicalNamesProvider.tsx`: same docblock voice ("two consumers must never disagree"), same
    `createContext<T | null>(null)` shape, same throwing-writer / non-throwing-leaf accessor pair.

    Export exactly three names:
    `EffectiveFeaturesProvider({ value, children }: { value: UseEffectiveFeatures; children: ReactNode })`
    which provides `value` and nothing else;
    `useEffectiveFeaturesContext(): UseEffectiveFeatures` which throws a clear
    "must be used within an EffectiveFeaturesProvider" error when the context is null (the
    `useTechnicalNames` / `citationNav` writer idiom);
    and `useEffectiveFeaturesOptional(): UseEffectiveFeatures | null` returning `useContext(...)`
    unchanged (the `useTechnicalNamesOptional` leaf idiom).

    This is deliberately a VALUE-PASSING provider, and the docblock must say why: the hook needs
    `user?.id` from `useAuth()`, which is called inside `App` itself, and a React component cannot
    consume a context that it mounts. Keeping the single `useEffectiveFeatures(user?.id ?? null)` call
    at `App.tsx:149` and broadcasting that SAME object downward is additive plumbing that preserves
    every Phase-148 invariant untouched: the fail-closed `{}` before first resolve and on any error
    (T-148-FAILCLOSED), the `userId`-keyed re-probe (WR-01), and the reachability of `refetch` from
    App's `FEATURE_FORBIDDEN_EVENT` handler. It also guarantees `visibleNavItems` and any page consumer
    read the identical object rather than two independently-fetched maps.

    Hard prohibitions: do NOT modify `frontend/src/hooks/useEffectiveFeatures.ts`. Do NOT call
    `getEffectiveFeatures` or any api-client function from this provider. Do NOT add `useState`,
    `useEffect`, a cache, a TTL, a retry, or a default/fallback value — the provider holds no state of
    its own. Do NOT change the `GET /features` contract or the fail-closed policy.

    Record explicitly in the docblock: a NULL context (no provider mounted — unit tests, isolated
    renders) MUST be read by consumers as FAIL-CLOSED, i.e. every governed feature hidden. It must
    never be read as "unknown, so show it". Consumers use the optional accessor and treat null exactly
    like `{}`.

    Write `EffectiveFeaturesProvider.test.tsx` asserting: `useEffectiveFeaturesOptional()` returns null
    outside a provider; `useEffectiveFeaturesContext()` throws outside a provider; inside a provider
    both return the exact object passed as `value` (reference equality); and a source-grep block
    (`?raw`, the shipped idiom) proving the module does not import `@/lib/api`, does not reference
    `getEffectiveFeatures`, and declares no `useState`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/providers/EffectiveFeaturesProvider.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/providers/EffectiveFeaturesProvider.tsx` exports exactly `EffectiveFeaturesProvider`, `useEffectiveFeaturesContext`, `useEffectiveFeaturesOptional`
    - `grep -c "getEffectiveFeatures" frontend/src/providers/EffectiveFeaturesProvider.tsx` returns 0
    - `grep -c "useState" frontend/src/providers/EffectiveFeaturesProvider.tsx` returns 0
    - `grep -c "@/lib/api" frontend/src/providers/EffectiveFeaturesProvider.tsx` returns 0 for value imports (a `import type` of `UseEffectiveFeatures` from the hook module is required and permitted)
    - the docblock states the null-context-is-fail-closed rule in words
    - `git diff --stat frontend/src/hooks/useEffectiveFeatures.ts` shows no change
    - `cd frontend && npx vitest run src/providers/EffectiveFeaturesProvider.test.tsx` green with the four assertion groups above
    - `cd frontend && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>The provider exists, holds no state, fetches nothing, and its null-context fail-closed contract is asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the provider in App.tsx without perturbing the 148 invariants</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - `frontend/src/App.tsx` lines 130-270 — the hook call at `:149`, the `visibleNavItems` line at `:150`, the FEATURE_FORBIDDEN_EVENT effect at `:161-176`, and the provider stack at `:227-260`
    - `frontend/src/providers/TechnicalNamesProvider.tsx` — mounted at `App.tsx:252`; the new provider sits beside it
    - `frontend/src/components/admin/revertByteIdentical.test.tsx` — the 181 acceptance gate this plan must leave green AND untouched
    - `frontend/src/lib/nav-items.ts` — confirm no `visual_workflow_canvas` entry is added anywhere
  </read_first>
  <action>
    In `App.tsx`, import `EffectiveFeaturesProvider` from `@/providers/EffectiveFeaturesProvider` and
    mount it around the authed subtree so any page can read the map.

    Placement: immediately INSIDE `<TechnicalNamesProvider>` and OUTSIDE `<CitationNavProvider>` — i.e.
    it wraps the `CitationNavProvider` / `ChatLayout` subtree that starts at `App.tsx:253`. That mirrors
    where `TechnicalNamesProvider` already sits, so both app-wide UI contexts share one home and the
    workflows view (rendered by `ChatLayout`'s view switch) is inside both.

    Feed it from the EXISTING hook call. Today `App.tsx:149` destructures only `features` and `refetch`;
    add `loading` to that same destructure. Pass
    `value={{ features: effectiveFeatures, loading, refetch: refetchFeatures }}`. Memoize the object with
    `useMemo` keyed on those three values so descendants do not re-render on every App render — mirroring
    `TechnicalNamesProvider.tsx:75-78`.

    Leave UNCHANGED, byte-for-byte: the `useEffectiveFeatures(user?.id ?? null)` call itself, the
    `const navItems = visibleNavItems(effectiveFeatures)` line at `:150`, and the entire
    `FEATURE_FORBIDDEN_EVENT` effect at `:161-176` including its `detail?.status !== 403 ||
    detail?.message !== VISIBILITY_REFUSAL` guard and its `refetchFeatures()` call. `visibleNavItems`
    must still receive the identical map object, and the D-04 bounce must still be reachable.

    Add nothing else: no new fetch, no new state, no new window event, no new nav item. In particular do
    NOT add a `NAV_ITEMS` entry tagged `visual_workflow_canvas` — D-183-01 formally released Phase 181's
    deferred promise that such an entry would "land with the view in 183", and
    `revertByteIdentical.test.tsx:62-64`'s scope-freeze assertion depends on its continued absence.

    Do not modify `revertByteIdentical.test.tsx` in this plan; its stale prose comments are corrected in
    plan 183-07, which is the plan that actually lands the view.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/admin/revertByteIdentical.test.tsx src/providers/EffectiveFeaturesProvider.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useEffectiveFeatures(" frontend/src/App.tsx` returns exactly 1
    - `grep -c "visibleNavItems(effectiveFeatures)" frontend/src/App.tsx` returns exactly 1
    - `grep -c "EffectiveFeaturesProvider" frontend/src/App.tsx` returns at least 2 (import + mount)
    - `git diff frontend/src/App.tsx` touches only: the import block, the `:149` destructure, one `useMemo`, and the two JSX provider lines — the `FEATURE_FORBIDDEN_EVENT` effect body is unchanged
    - `git diff --name-only frontend/src/components/admin/revertByteIdentical.test.tsx` is empty for this plan
    - `cd frontend && npx vitest run src/components/admin/revertByteIdentical.test.tsx` green, including the `no NAV_ITEMS entry is tagged visual_workflow_canvas` scope-freeze test
    - `grep -c "visual_workflow_canvas" frontend/src/lib/nav-items.ts` returns 0
    - `cd frontend && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>The provider is mounted, the nav path and the 148 bounce are untouched, and the 181 gate is green without editing it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /features` response → client render decisions | The effective-features map decides what UI renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-02 | Elevation of Privilege | reading the client feature map as a security boundary | accept | Correct by construction and unchanged by this plan: the client hide is RENDER-ONLY and cosmetic; the backend `require_visible` gate (Phase 148-05) plus `require_canvas`'s pre-auth 404 (Phase 181, D-181-02) are the authority. This plan adds no route and no gated data path. Documented at `useEffectiveFeatures.ts:8` and `:33-35`. |
| T-183-03 | Information Disclosure | a governed feature flashing to a non-audience user pre-resolve or on a fetch blip | mitigate | The fail-closed policy is preserved verbatim — the hook still starts at `{}` and resolves to `{}` on any error (T-148-FAILCLOSED), and the provider adds no default value. Additionally the new NULL-context case is specified as fail-closed and asserted in Task 1's suite, closing the one new code path this plan creates. |
| T-183-04 | Spoofing | a stale prior user's map leaking across a same-tab user switch | accept (inherited) | Unchanged: WR-01 keying to `userId` re-probes on user change and clears the map on sign-out. This plan does not touch the hook, and the acceptance criteria assert `useEffectiveFeatures.ts` ends byte-unchanged. |

## ASVS conclusion (carried from RESEARCH §Security Domain, line 1759)

V4 Access Control applies **partially and inherited, not extended** — this plan re-plumbs an existing
render-only signal and adds no gated route, no new endpoint, and no authz decision. V2, V3, V5 and V6
do not apply. The ROADMAP's "no threat model" flag for Phase 183 stands.

**Frontend-only.** If any task here grows a backend touch, this conclusion is VOID.
</threat_model>

<verification>
- `cd frontend && npx tsc -b` exits 0
- `cd frontend && npx vitest run src/providers/EffectiveFeaturesProvider.test.tsx src/components/admin/revertByteIdentical.test.tsx` green
- `cd frontend && npm test` shows no NEW failing test names and no test-count drop versus the recorded baseline
- `git diff --stat frontend/src/hooks/useEffectiveFeatures.ts frontend/src/lib/nav-items.ts frontend/src/components/admin/revertByteIdentical.test.tsx` shows zero changes
</verification>

<success_criteria>
- Exactly one `GET /features` per session still (one hook call site in the whole tree)
- Any descendant of `ChatLayout` can read `features.visual_workflow_canvas` from context
- Null context is fail-closed and asserted
- The 181 scope-freeze gate passes unmodified; no canvas nav entry exists
</success_criteria>

<output>
Create `.planning/phases/183-read-only-canvas/183-03-SUMMARY.md` when done.
Record the exact accessor names and the null-context fail-closed rule — plan 183-07's Builder gate
depends on `useEffectiveFeaturesOptional()` and on treating null as `{}`.
</output>
