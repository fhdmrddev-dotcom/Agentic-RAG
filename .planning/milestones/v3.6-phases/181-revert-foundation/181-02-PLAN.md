---
phase: 181-revert-foundation
plan: 02
type: execute
wave: 2
depends_on: [181-01]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/admin/FeatureVisibility.tsx
  - frontend/src/components/admin/ControlRoomPage.tsx
  - frontend/src/components/admin/revertByteIdentical.test.tsx
autonomous: true
requirements: [REVERT-01, REVERT-02]

must_haves:
  truths:
    - "D-181-01: the frontend feature machinery threads the `\"off\"` audience -- `FeatureAudience` gains `\"off\"` and `GovernedFeature` gains `\"visual_workflow_canvas\"` (the SEED-115 enum-not-boolean contract), so the fail-closed effective map hides the canvas everywhere when off"
    - "D-181-03: the operator control for `visual_workflow_canvas` is a simple Off | On toggle (On writes audience `\"everyone\"`, Off writes `\"off\"`) reusing the `FeatureVisibility` card frame verbatim -- NOT the Everyone|Operators|By-role triad"
    - "REVERT-01: adding `visual_workflow_canvas:false` to the effective map leaves the `visibleNavItems` set byte-identical to today (no canvas nav entry exists in 181 -- it lands in 183), and no nav item is tagged `visual_workflow_canvas` yet"
    - "REVERT-02: the frontend byte-identical vitest gate `revertByteIdentical.test.tsx` rides the existing `frontend-tests.yml` (`npm test`) with no new CI job"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "GovernedFeature union += visual_workflow_canvas; FeatureAudience += off"
      contains: "visual_workflow_canvas"
    - path: "frontend/src/components/admin/FeatureVisibility.tsx"
      provides: "the 5th FeatureDef + the Off|On segmented-control variant for the canvas key"
      contains: "visual_workflow_canvas"
    - path: "frontend/src/components/admin/revertByteIdentical.test.tsx"
      provides: "the frontend half of the D-181-06 acceptance gate (nav-set parity + Off|On card)"
      contains: "visibleNavItems"
  key_links:
    - from: "frontend/src/components/admin/ControlRoomPage.tsx"
      to: "setFeatureVisibility"
      via: "handleSetVisibility non-role branch writes off/everyone for the canvas key"
      pattern: "visual_workflow_canvas"
    - from: "frontend/src/components/admin/FeatureVisibility.tsx"
      to: "onSetVisibility"
      via: "Off|On SegButtons call onFlip(\"off\") / onFlip(\"everyone\")"
      pattern: "onFlip\\(.everyone.\\)"
---

<objective>
Wire the `visual_workflow_canvas` governed feature through the frontend feature machinery
and give the operator a simple Off | On control in the Control Room's `FeatureVisibility`
card, then lock flag-off nav byte-identity with a frontend `revertByteIdentical` vitest.
Verbatim reuse of the shipped Phase 148 feature-visibility surface; the only net-new logic
is the `"off"` audience type + the Off|On two-position variant for this one key.

Purpose: An operator turns the entire canvas layer on/off from the Control Room; when off,
the effective map hides the canvas everywhere by construction (fail-closed), and the nav
set stays identical to today. This is the design contract from the
`sketch-findings-agentic-rag` skill (069-A feature-visibility card) — no new UI-SPEC.

Output: `GovernedFeature` + `FeatureAudience` extended; the 5th `FeatureDef` + Off|On
control; the `DEFAULT_VISIBILITY` seed; and `revertByteIdentical.test.tsx` (nav parity +
Off|On card). No backend edits (depends on 181-01 for the backend key + allowlist).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/181-revert-foundation/181-CONTEXT.md
@.planning/phases/181-revert-foundation/181-RESEARCH.md
@.planning/phases/181-revert-foundation/181-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- Contracts extracted from the live codebase this session. Use these directly. -->

frontend/src/lib/api.ts:65 — `export type GovernedFeature = "skill_studio" | "model_management" | "workflow_authoring" | "governance_health"` (extend with "visual_workflow_canvas").
frontend/src/lib/api.ts:73 — `export type EffectiveFeatures = Partial<Record<GovernedFeature, boolean>>` (picks up the new key automatically).
frontend/src/lib/api.ts:4049 — `export type FeatureAudience = "everyone" | "operators"` (extend with "off"). Used by setFeatureVisibility(feature, audience: FeatureAudience) at :4126, DEFAULT_VISIBILITY, and AudienceValue.
frontend/src/lib/api.ts:4126 — setFeatureVisibility(feature, audience) -> PUT /admin/visibility {feature, audience}. Reused for the Off|On flip ("off" | "everyone").
frontend/src/lib/nav-items.ts:30 — NAV_ITEMS (no canvas entry today); :71 — `visibleNavItems(features)` filters generically: an item renders iff it has no `feature` tag OR `features[item.feature] === true`.
frontend/src/components/admin/FeatureVisibility.tsx:47 — `type AudienceValue = FeatureAudience | "role"` (auto-includes "off" once FeatureAudience does); :80 FeatureDef interface; :101-142 the 4 existing FeatureDef entries; :393-436 AudienceSegments (3 fixed SegButtons: Everyone / Operators only / By role); :441+ SegButton primitive.
frontend/src/components/admin/ControlRoomPage.tsx:188 — `DEFAULT_VISIBILITY: Record<GovernedFeature, FeatureAudience>` (4 keys); :537 handleSetVisibility(feature, audience, roles=[]) — non-"role" audiences route through setFeatureVisibility(feature, audience); :801 <FeatureVisibility> mount (unchanged).
frontend/src/lib/nav-items.test.ts — the NAV_ITEMS byte-identity lock pattern to model the nav-parity assert after.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the feature types + the 5th FeatureDef with an Off|On control</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/admin/FeatureVisibility.tsx, frontend/src/components/admin/ControlRoomPage.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts (the `GovernedFeature` union :65-69, `EffectiveFeatures` :73, `FeatureAudience` :4049, `setFeatureVisibility` :4126)
    - frontend/src/components/admin/FeatureVisibility.tsx (the `AudienceValue` type :47, the `FeatureDef` interface :80-96, the 4 existing `FeatureDef` entries :101-142, `AudienceSegments` :393-436, `SegButton` :441+)
    - frontend/src/components/admin/ControlRoomPage.tsx (the `DEFAULT_VISIBILITY` seed :188-193, `handleSetVisibility` :537-551)
    - .planning/phases/181-revert-foundation/181-PATTERNS.md (the `api.ts`, `FeatureVisibility.tsx`, `ControlRoomPage.tsx` Pattern Assignments — the exact appends + the Off|On discretion)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (Operator Control Room 069-A — the feature-visibility card is the design contract; NO new UI-SPEC)
  </read_first>
  <action>
    In `api.ts`: add `"visual_workflow_canvas"` to the `GovernedFeature` union and `"off"` to the `FeatureAudience` union at :4049 (the SEED-115 enum-not-boolean contract, D-181-01). `EffectiveFeatures` and `AudienceValue` pick up the additions automatically. In `FeatureVisibility.tsx`: append a 5th `FeatureDef` for `visual_workflow_canvas` (name "Visual workflow canvas"; desc "The drag-and-drop visual authoring + live-run canvas. Off = today's product exactly."; livesOn "Workflows"; glyph `Workflow` — already imported; uiSurface + refusedApi + routePrefixes copy per PATTERNS.md). For THIS key ONLY, render an **Off | On** two-position control instead of the Everyone|Operators|By-role triad (D-181-03): branch `AudienceSegments` on `def.key === "visual_workflow_canvas"` to render two `SegButton`s reusing the primitive verbatim — On selected when `audience === "everyone"` calling `onFlip("everyone")`, Off selected when `audience === "off"` calling `onFlip("off")`. The amber/receipt/consequence frame is reused unchanged. In `ControlRoomPage.tsx`: add `visual_workflow_canvas: "off"` to `DEFAULT_VISIBILITY` (now type-checks because `FeatureAudience` includes `"off"`); `handleSetVisibility`'s non-"role" branch already routes `"off"`/`"everyone"` through `setFeatureVisibility` unchanged. Do NOT touch any door/run-surface file.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` (frontend) exits 0 — the `"off"` audience + new key type-check across api.ts, FeatureVisibility.tsx, ControlRoomPage.tsx.
    - `grep -n 'visual_workflow_canvas' frontend/src/lib/api.ts frontend/src/components/admin/FeatureVisibility.tsx frontend/src/components/admin/ControlRoomPage.tsx` shows the key in the union, a FeatureDef, and DEFAULT_VISIBILITY.
    - `grep -n '"off"' frontend/src/lib/api.ts` shows `"off"` in the `FeatureAudience` union.
    - The canvas row renders exactly two segments (Off, On); the other 4 rows keep the triad.
  </acceptance_criteria>
  <done>The frontend knows the `visual_workflow_canvas` key + `"off"` audience end-to-end, and the operator sees an Off|On control for it in the reused FeatureVisibility card — no door/run-surface file touched.</done>
</task>

<task type="auto">
  <name>Task 2: revertByteIdentical.test.tsx — nav-set parity + the Off|On card gate</name>
  <files>frontend/src/components/admin/revertByteIdentical.test.tsx</files>
  <read_first>
    - frontend/src/lib/nav-items.ts (`NAV_ITEMS` :30-59 + `visibleNavItems` :71-73 — the generic feature filter)
    - frontend/src/lib/nav-items.test.ts (the NAV_ITEMS byte-identity lock pattern to model after)
    - frontend/src/components/admin/FeatureVisibility.tsx (the rendered card + the Off|On control from Task 1)
    - .planning/phases/181-revert-foundation/181-VALIDATION.md (the frontend test row: `npm test -- revertByteIdentical`, nav parity; note the render-guard is deferred with the render branch to 182/183)
    - .planning/phases/181-revert-foundation/181-PATTERNS.md (the `revertByteIdentical.test.tsx` Pattern Assignment)
  </read_first>
  <action>
    Create `frontend/src/components/admin/revertByteIdentical.test.tsx` (vitest). Assert nav-set byte-identity (REVERT-01): `visibleNavItems({...allFeaturesTrue, visual_workflow_canvas: false})` deep-equals `visibleNavItems({...allFeaturesTrue})` — adding the canvas key (false) changes nothing because no `NAV_ITEMS` entry is tagged `visual_workflow_canvas` in 181 (that entry lands in 183); AND assert `NAV_ITEMS.some(i => i.feature === "visual_workflow_canvas") === false` (a scope-freeze-adjacent guard that no canvas nav entry slipped in early). Assert the operator control (D-181-03): render `<FeatureVisibility>` (or its `AudienceSegments` for the canvas key) and confirm the `visual_workflow_canvas` row exposes exactly an Off|On control (two radios, not three), that On maps to audience `"everyone"` and Off to `"off"`, and that flipping calls `onSetVisibility("visual_workflow_canvas", "everyone")` / `(..., "off")`. Add a top-of-file comment that the `ChatLayout` render-guard vitest (a stale canvas `activeView` returns the fallback when off) is DEFERRED to land WITH the first canvas `ActiveView` render branch in 182/183 (D-181 Claude's-discretion), so the render-guard assertion is intentionally not present in 181.
  </action>
  <verify>
    <automated>cd frontend && npm test -- revertByteIdentical</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- revertByteIdentical` exits 0 with the nav-parity + Off|On card assertions passing.
    - The nav-parity test proves `visibleNavItems` is unchanged by `visual_workflow_canvas: false` AND that no NAV_ITEMS entry is tagged with the canvas key.
    - The card test proves exactly two segments (Off, On) for the canvas key and the correct `"everyone"`/`"off"` writes.
    - The full frontend suite `npm test` is green (no collateral regression).
  </acceptance_criteria>
  <done>The frontend byte-identical acceptance gate exists, rides `frontend-tests.yml` (no new CI job), and locks flag-off nav parity + the Off|On operator control; the render-guard is explicitly deferred to 182/183.</done>
</task>

</tasks>

<verification>
- Quick (per task commit): `cd frontend && npm test -- revertByteIdentical` (< 30s) + `npx tsc --noEmit`.
- Wave merge: full `cd frontend && npm test` + `npm run lint:a11y` green (rides `frontend-tests.yml`).
- The FeatureVisibility card shows Off|On for `visual_workflow_canvas` and the triad for the other 4 features.
- No door/run-surface file appears in this plan's diff (enforced hard by Plan 03's scope-freeze).
</verification>

<success_criteria>
- `GovernedFeature` + `FeatureAudience` carry the new key + `"off"` (D-181-01); the whole frontend type-checks.
- The operator has a working Off|On toggle in the reused FeatureVisibility card (D-181-03).
- `revertByteIdentical.test.tsx` locks flag-off nav byte-identity and rides the existing CI (REVERT-01/02).
- Zero backend edits, zero door/run-surface edits.
</success_criteria>

<output>
Create `.planning/phases/181-revert-foundation/181-02-SUMMARY.md` when done.
</output>
