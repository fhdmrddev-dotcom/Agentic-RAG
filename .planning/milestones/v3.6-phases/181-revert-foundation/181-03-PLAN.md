---
phase: 181-revert-foundation
plan: 03
type: execute
wave: 3
depends_on: [181-01, 181-02]
files_modified:
  - scripts/check-181-scope-freeze.sh
autonomous: false
requirements: [REVERT-01, REVERT-02]

must_haves:
  truths:
    - "D-181-06: the byte-identical gate is a REAL CI-run acceptance gate -- full `pytest tests -q` (rides `backend-tests.yml`) + `npm test` (rides `frontend-tests.yml`) are green with the flag off, no new CI job, and re-runnable at live milestone-close"
    - "D-181-07: byte-identity is verified OBSERVABLY -- the nav-item set + reachable-route set + responses + `GET /features` map + the two authoring doors + the run surface match today; NOT a JS bundle hash"
    - "D-181-08: a `git diff`-based scope-freeze proves the phase diff MUST NOT touch `WorkflowDoorSwitch.tsx`, `WorkflowBuilderPage.tsx`, `WorkflowsPage.tsx`, `PhaseTimeline.tsx`, `PhaseCard.tsx`, or the harness engine (`harness_engine.py` / `services/harness/` / `models/harness.py`) -- the standing D-14 confirmed-absent-from-the-diff technique"
    - "REVERT-01: an operator confirms live that with the flag OFF there is no canvas nav entry and `GET /features` returns `visual_workflow_canvas: false`, and Off->On->Off flips the layer with the two existing doors + run surface unchanged"
  artifacts:
    - path: "scripts/check-181-scope-freeze.sh"
      provides: "reusable scope-freeze check (phase diff + milestone-close) asserting the FROZEN files are absent"
      contains: "WorkflowDoorSwitch.tsx"
  key_links:
    - from: "scripts/check-181-scope-freeze.sh"
      to: "git diff --name-only"
      via: "compares the phase-start ref to HEAD and greps for any FROZEN path"
      pattern: "git diff --name-only"
---

<objective>
Close operator HARD gate #1: prove flag-off is byte-identical OBSERVABLY (D-181-07), freeze
the two authoring doors + the run surface + the harness engine out of the phase diff
(D-181-08), and re-confirm the whole D-181-06 gate green — then an operator eyeballs the
live Off|On flip. This plan writes one reusable scope-freeze script and gates on the full
suites + a human live-close verification.

Purpose: The revert story is a TESTED acceptance gate, not prose. The scope-freeze is the
strongest cheap proof the doors + run surface are untouched; the operator UAT is the
lived-experience confirmation that today's product is exactly preserved.

Output: `scripts/check-181-scope-freeze.sh` (reusable at milestone-close), a green full
backend + frontend suite run, and an operator sign-off on the live flip.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/181-revert-foundation/181-CONTEXT.md
@.planning/phases/181-revert-foundation/181-RESEARCH.md
@.planning/phases/181-revert-foundation/181-VALIDATION.md

<interfaces>
<!-- The FROZEN contracts (D-181-08) — MUST be absent from the entire 181 phase diff. -->
frontend/src/components/workflows/WorkflowDoorSwitch.tsx   # the two authoring doors
frontend/src/pages/WorkflowBuilderPage.tsx                 # Author & govern door
frontend/src/pages/WorkflowsPage.tsx                       # Workflows library/launch
frontend/src/components/panel/PhaseTimeline.tsx            # developer run surface
frontend/src/components/panel/PhaseCard.tsx                # developer run surface
backend/app/services/harness_engine.py                     # the harness engine (ONLY executor)
backend/app/services/harness/                              # harness engine package
backend/app/models/harness.py                              # harness definition model

<!-- CI the gate rides (no new job): -->
.github/workflows/backend-tests.yml  -> `pytest tests -q`
.github/workflows/frontend-tests.yml -> `npm test` + `npm run lint:a11y`

<!-- Local dev: app http://localhost:5173/ ; operator login is the operator-granted account. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/check-181-scope-freeze.sh + full-suite green (D-181-06/07/08)</name>
  <files>scripts/check-181-scope-freeze.sh</files>
  <read_first>
    - .planning/phases/181-revert-foundation/181-CONTEXT.md (D-181-07 observable byte-identity + D-181-08 the FROZEN file list)
    - .planning/phases/181-revert-foundation/181-PATTERNS.md (the "FROZEN contracts" Shared Pattern — the exact paths + the `git show --stat` technique)
    - .planning/phases/181-revert-foundation/181-VALIDATION.md (the scope-freeze + full-suite sampling rows)
    - scripts/ (an existing repo script e.g. `scripts/check-deploy-drift.sh` for the house shell-script style + non-zero-on-violation convention)
  </read_first>
  <action>
    Create `scripts/check-181-scope-freeze.sh` (POSIX sh, `set -euo pipefail`) taking the phase-start ref as `$1` (default it to the commit immediately before the first 181 code commit — the executor passes the captured phase-start SHA; document that milestone-close re-runs it with the same ref). Compute `git diff --name-only "$BASE"..HEAD` and FAIL (exit 1, printing each offender) if ANY of these appears: `WorkflowDoorSwitch.tsx`, `WorkflowBuilderPage.tsx`, `WorkflowsPage.tsx`, `PhaseTimeline.tsx`, `PhaseCard.tsx`, `backend/app/services/harness_engine.py`, anything under `backend/app/services/harness/`, `backend/app/models/harness.py` (D-181-08 — the two doors + run surface + harness engine are frozen). Match by path against the changed-file list (do NOT count comments). On a clean diff print `scope-freeze OK` and exit 0. Then run the FULL suites and confirm green (D-181-06): `cd backend && source venv/bin/activate && pytest tests -q` AND `cd frontend && npm test`. Record the observable byte-identity checklist (D-181-07) in the plan SUMMARY: nav-item set, reachable-route set + responses, `GET /features` map, the two doors, the run surface — each unchanged vs today. Also confirm NO migration file and NO package delta landed in the phase (`git diff --name-only "$BASE"..HEAD` shows no `supabase/migrations/*`, no `package.json`/`requirements.txt` dependency change).
  </action>
  <verify>
    <automated>bash scripts/check-181-scope-freeze.sh "$(git rev-list -1 HEAD -- .planning/phases/181-revert-foundation/181-PATTERNS.md)" && cd backend && source venv/bin/activate && pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-181-scope-freeze.sh <phase-start-ref>` prints `scope-freeze OK` and exits 0 (none of the 8 FROZEN paths in the phase diff).
    - Introducing any FROZEN path into the diff makes the script exit non-zero and name the offender (self-test the negative path once, then revert).
    - `pytest tests -q` (backend) exits 0 and `npm test` (frontend) exits 0 — the full D-181-06 gate is green with the flag off.
    - `git diff --name-only <phase-start>..HEAD` contains no `supabase/migrations/*` and no dependency-manifest change (D-181-05 corroboration).
  </acceptance_criteria>
  <done>The reusable scope-freeze script proves the doors + run surface + harness engine are absent from the phase diff, the full backend + frontend suites are green with the flag off, and the observable byte-identity checklist is recorded — HARD gate #1's automated half is closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator live-close UAT — flag-off byte-identity + the Off|On flip</name>
  <action>
    All automated work (the backend gate, the operator card, the byte-identical test gate, and
    the scope-freeze script) is complete and green BEFORE this checkpoint. Nothing is built here —
    this is the human live-close verification gate for HARD gate #1. Present the app to the
    operator and pause for the sign-off described below; do NOT auto-advance (this checkpoint is
    never auto-approvable).
  </action>
  <what-built>
    The backend `require_canvas` 404 gate + `visual_workflow_canvas` flag (default off), the
    operator Off|On control in the Control Room FeatureVisibility card, and the byte-identical
    test gate + scope-freeze — all automated and green. This checkpoint is the operator
    live-close confirmation that today's product is exactly preserved (REVERT-01/02, HARD gate #1).
  </what-built>
  <how-to-verify>
    With the local app running (http://localhost:5173/, operator-granted account):
    1. Flag OFF (default): confirm the primary nav shows NO "Visual workflow canvas" entry, and
       both existing authoring doors ("Describe & run" / "Author & govern") + the run surface
       (PhaseTimeline/PhaseCard) look and behave exactly as today.
    2. Open DevTools → Network → the `GET /features` response: confirm `visual_workflow_canvas`
       is `false` (for your operator account too — not a phantom True).
    3. Control Room → Users & Access → the feature-visibility card: confirm the "Visual workflow
       canvas" row shows a simple Off | On control (two positions, not the Everyone|Operators|By-role
       triad). Flip it On, then Off again — confirm the flip records a receipt and does not alter
       the two doors or the run surface.
    4. Confirm a canvas-gated route 404s while off (e.g. `curl -s -o /dev/null -w "%{http_code}"
       http://localhost:8000/canvas/ping` with an auth header returns 404, never 403).
  </how-to-verify>
  <resume-signal>Type "approved" if all four hold, or describe the discrepancy (which surface changed / which response was wrong).</resume-signal>
</task>

</tasks>

<verification>
- `bash scripts/check-181-scope-freeze.sh <phase-start-ref>` exits 0 (`scope-freeze OK`); the negative self-test exits non-zero.
- Full `pytest tests -q` + `npm test` green with the flag off (D-181-06).
- Operator confirms live: no canvas nav when off, `GET /features` false, Off|On flip works, doors + run surface unchanged, `/canvas/ping` 404s.
</verification>

<success_criteria>
- The scope-freeze script is a committed, reusable artifact (phase diff + milestone-close) that fails on any FROZEN-file touch (D-181-08).
- Flag-off byte-identity is proven observably (D-181-07) and by the green full suites (D-181-06).
- The operator has confirmed the live off-state matches today and the Off|On flip works (REVERT-01/02) — HARD gate #1 closed.
</success_criteria>

<output>
Create `.planning/phases/181-revert-foundation/181-03-SUMMARY.md` when done, including the
D-181-07 observable byte-identity checklist and the operator sign-off.
</output>
