---
phase: 181-revert-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/user_settings.py
  - backend/app/api/admin.py
  - backend/app/dependencies.py
  - backend/app/api/features.py
  - backend/app/api/canvas_canary.py
  - backend/app/main.py
  - backend/tests/test_181_off_audience.py
  - backend/tests/test_181_flip_on.py
  - backend/tests/test_revert_byte_identical.py
autonomous: true
requirements: [REVERT-01, REVERT-02]

must_haves:
  truths:
    - "D-181-01: `feature_audience(\"visual_workflow_canvas\")` returns \"off\" on the cold default, and BOTH `require_canvas` AND the `GET /features` map resolve \"off\" -> hidden BEFORE the operator short-circuit (byte-identical for operators too, not just end users)"
    - "D-181-02: `require_canvas` returns a byte-identical 404 (reuses the module `_NOT_FOUND` at dependencies.py:302, mirrors `require_operator`), NEVER a 403; fail-closed to \"off\" -> 404 on any cold-cache/DB error"
    - "D-181-04: one temporary canary route `GET /canvas/ping` is mounted behind `Depends(require_canvas())` so `test_require_canvas_404s_when_off` proves the 404 gate end-to-end via TestClient in 181"
    - "D-181-05: NO migration -- the flag is the `_GOVERNED_FEATURES` key `visual_workflow_canvas: \"off\"` as the authoritative cold default; the existing `app_settings.feature_visibility` JSONB gains the key only on an operator flip via the atomic `||` merge in `set_feature_visibility`"
    - "The 4 shipped governed features (skill_studio, model_management, workflow_authoring, governance_health) resolve exactly as before -- no Phase-148 regression (REVERT-02)"
    - "A crafted `PUT /admin/visibility` cannot set an arbitrary audience -- `_VISIBILITY_FEATURES` + `_VISIBILITY_AUDIENCES` allowlists gate the write (T-181-03)"
  artifacts:
    - path: "backend/app/dependencies.py"
      provides: "require_canvas() 404 gate factory that resolves \"off\" before the operator no-op"
      contains: "def require_canvas"
    - path: "backend/app/api/canvas_canary.py"
      provides: "temporary GET /canvas/ping gated by require_canvas() (removed/repurposed in 182/183)"
      contains: "require_canvas"
    - path: "backend/app/models/user_settings.py"
      provides: "visual_workflow_canvas: off cold default + off-audience resolution"
      contains: "visual_workflow_canvas"
    - path: "backend/tests/test_revert_byte_identical.py"
      provides: "the backend half of the D-181-06 acceptance gate (map-hides + 404 + no-regression)"
      exports: ["test_features_map_hides_canvas_from_everyone_when_off", "test_require_canvas_404s_when_off", "test_existing_governed_features_unchanged"]
  key_links:
    - from: "backend/app/api/features.py"
      to: "feature_audience"
      via: "GET /features comprehension guard `feature_audience(f) != \"off\"` BEFORE `op or ...`"
      pattern: "feature_audience\\(f\\) != .off."
    - from: "backend/app/api/canvas_canary.py"
      to: "require_canvas"
      via: "dependencies=[Depends(require_canvas())]"
      pattern: "Depends\\(require_canvas\\(\\)\\)"
    - from: "backend/app/main.py"
      to: "canvas_canary.router"
      via: "app.include_router(canvas_canary.router)"
      pattern: "include_router\\(canvas_canary"
---

<objective>
Install the backend half of operator HARD gate #1: a governed `visual_workflow_canvas`
feature flag (default OFF for everyone incl. operators) plus a 404 `require_canvas` gate,
proven end-to-end NOW via a temporary canary route, and locked by the backend
`test_revert_byte_identical` acceptance gate. This is ~95% verbatim reuse of the shipped
v3.3 Phase 148 feature-visibility pattern; the only net-new logic is the `"off"` audience
enum member and the 404 refusal.

Purpose: Every later v3.6 canvas route inherits this tested off-switch. Flag-off must be
provably byte-identical to today, and indistinguishable from "the canvas was never built"
(a 404, never a 403 that leaks route existence).

Output: The `"off"` audience threaded through the model + admin write allowlist, the
`require_canvas` 404 gate, the `GET /features` operator-bypass for `"off"`, the temporary
canary route, and three backend test files (`test_181_off_audience.py`,
`test_181_flip_on.py`, `test_revert_byte_identical.py`). NO migration, NO new package.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/181-revert-foundation/181-CONTEXT.md
@.planning/phases/181-revert-foundation/181-RESEARCH.md
@.planning/phases/181-revert-foundation/181-PATTERNS.md
@.planning/phases/181-revert-foundation/181-VALIDATION.md

<interfaces>
<!-- Contracts extracted from the live codebase this session. Use these directly — no exploration needed. -->

backend/app/models/user_settings.py:1081 — the ONE cold-default polarity map (currently 4 keys):
  _GOVERNED_FEATURES: dict[str, str] = {"skill_studio":"operators","model_management":"operators","workflow_authoring":"everyone","governance_health":"everyone"}
backend/app/models/user_settings.py:1109 — feature_audience(feature)->str; the accepted-enum tuple at :1120 is `("everyone","operators","role")`; falls through to _GOVERNED_FEATURES.get(feature,"operators"). NEVER raises.
backend/app/models/user_settings.py:1125 — resolve_feature_access(feature, caller_role, caller_groups)->bool; explicit branches for "everyone"(True)/"operators"(False)/"role"; trailing `return False` (fail-closed).
backend/app/models/user_settings.py:1155 — set_feature_visibility(feature, audience, roles, groups) — atomic JSONB `||` merge (UNCHANGED; this is why no migration is needed).
backend/app/dependencies.py:302 — `_NOT_FOUND = HTTPException(status_code=404, detail="Not Found")` (reuse this constant; do NOT hand-roll a 404).
backend/app/dependencies.py:391 — require_operator: raises `_NOT_FOUND` on non-membership (the byte-identical-404 posture to copy).
backend/app/dependencies.py:462 — resolve_caller_role(request, current_user) -> (role|None, groups:set); fail-closed to "member".
backend/app/dependencies.py:517 — require_visible(feature): factory; runs `is_operator` no-op FIRST, then audience, raises 403 (the SKELETON to copy — but require_canvas resolves "off" FIRST and raises _NOT_FOUND 404 instead).
backend/app/api/features.py:58 — GET /features comprehension `f: (op or feature_audience(f)=="everyone" or (feature_audience(f)=="role" and resolve_feature_access(...)))` over `_GOVERNED_FEATURES`.
backend/app/api/admin.py:97 — `_VISIBILITY_FEATURES` set (4 keys); :107 — `_VISIBILITY_AUDIENCES = {"everyone","operators","role"}`; :999-1008 — set_visibility validators reject off-allowlist feature/audience with 400.
backend/app/main.py:657 — `from app.api import ... features, ...`; :683 — `app.include_router(features.router)` (the mount pattern for the new canary router).
backend/app/api/workflows.py:237 — route-attach convention: `dependencies=[Depends(require_visible("workflow_authoring"))]`.
backend/tests/test_148_visibility_cold_default.py — the monkeypatch test pattern: `monkeypatch.setattr(us, "load_app_settings", ...)` to drive cold-default vs stored-record.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread the "off" audience through the model + the admin write allowlist</name>
  <files>backend/app/models/user_settings.py, backend/app/api/admin.py, backend/tests/test_181_off_audience.py</files>
  <read_first>
    - backend/app/models/user_settings.py (the `_GOVERNED_FEATURES` map :1081, `feature_audience` :1109, `resolve_feature_access` :1125, `set_feature_visibility` :1155 — the ONE cold-default home)
    - backend/app/api/admin.py (the `_VISIBILITY_FEATURES` / `_VISIBILITY_AUDIENCES` allowlist sets :97-108 + the `set_visibility` validators :999-1008)
    - backend/tests/test_148_visibility_cold_default.py (the monkeypatch pattern to model the new unit tests after)
    - .planning/phases/181-revert-foundation/181-PATTERNS.md (the `user_settings.py` + `admin.py` Pattern Assignments — the exact in-place edits)
  </read_first>
  <behavior>
    - `feature_audience("visual_workflow_canvas")` returns "off" on a cold read (DB unreachable / missing key), driven by the new `_GOVERNED_FEATURES` default.
    - A stored `{"audience": "off"}` record for the key is HONORED (returns "off"), because "off" is now in the accepted-enum tuple — not silently ignored and re-defaulted.
    - `resolve_feature_access("visual_workflow_canvas", <any role>, <any groups>)` returns False when the audience is "off".
    - The 4 shipped keys (skill_studio/model_management/workflow_authoring/governance_health) resolve EXACTLY as `test_148_visibility_cold_default.py` asserts (no regression).
    - `PUT /admin/visibility` accepts `{feature:"visual_workflow_canvas", audience:"off"}` and `{...audience:"everyone"}`; a crafted `{...audience:"bogus"}` still 400s (unknown audience).
  </behavior>
  <action>
    Add the line `"visual_workflow_canvas": "off"` to `_GOVERNED_FEATURES` (D-181-01/D-181-05 — the authoritative cold default; an unseeded key falls through to it, which is why no migration is needed). Extend the accepted-enum tuple in `feature_audience` at :1120 to `("everyone","operators","role","off")` so a stored `"off"` record is honored rather than falling through. Add an explicit `if aud == "off": return False` branch to `resolve_feature_access` (belt-and-suspenders — the trailing `return False` already denies, but the explicit branch matches the `operators` short-circuit style and documents intent). Do NOT change `set_feature_visibility` — its atomic `||` merge already carries the Off flip (`"off"`) and On flip (`"everyone"`) unchanged. In `admin.py`, add `"visual_workflow_canvas"` to `_VISIBILITY_FEATURES` and `"off"` to `_VISIBILITY_AUDIENCES` (T-181-03 — the crafted-body allowlist); the `set_visibility` handler validators need NO change beyond set membership. Write `backend/tests/test_181_off_audience.py` modeled on `test_148_visibility_cold_default.py` (monkeypatch `us.load_app_settings`) covering the five behaviors above.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest tests/test_181_off_audience.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_181_off_audience.py -q` exits 0.
    - `grep -n 'visual_workflow_canvas' backend/app/models/user_settings.py` shows the key in `_GOVERNED_FEATURES` with value `"off"`.
    - `feature_audience` accepted-enum tuple literally contains `"off"`.
    - `admin.py` `_VISIBILITY_AUDIENCES` literally contains `"off"` and `_VISIBILITY_FEATURES` contains `"visual_workflow_canvas"`.
    - The 4 existing keys' resolution is unchanged (the test asserts each explicitly).
  </acceptance_criteria>
  <done>The "off" audience resolves and denies at the model layer, the admin write allowlist accepts the new key + off/everyone (and rejects garbage), and the off-audience unit tests are green — no migration, no writer change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: require_canvas 404 gate + GET /features off-bypass + temporary canary route</name>
  <files>backend/app/dependencies.py, backend/app/api/features.py, backend/app/api/canvas_canary.py, backend/app/main.py, backend/tests/test_181_flip_on.py</files>
  <read_first>
    - backend/app/dependencies.py (the `_NOT_FOUND` constant :302, `require_operator` 404 posture :391-408, `require_visible` factory skeleton :517-555, `resolve_caller_role` :462)
    - backend/app/api/features.py (the whole file — the `GET /features` comprehension :58-70 to guard)
    - backend/app/api/workflows.py:237 (the `dependencies=[Depends(require_visible(...))]` route-attach convention)
    - backend/app/main.py:657 + :683 (the router import + `include_router` mount pattern)
    - .planning/phases/181-revert-foundation/181-PATTERNS.md (the `dependencies.py`, `features.py`, `canvas_canary.py` Pattern Assignments — the reconciled require_canvas target shape)
  </read_first>
  <behavior>
    - `GET /canvas/ping` returns 404 when the flag is off, for an operator AND a non-operator caller — never 403, byte-identical to an unknown path.
    - After an operator flip on (`set_feature_visibility("visual_workflow_canvas","everyone")`), `GET /canvas/ping` returns 200 and the gate is a no-op.
    - `GET /features` returns `visual_workflow_canvas: false` for an operator AND an end user when off (the "off" guard wins over the `op or ...` short-circuit).
    - On a cold-cache/DB blip the gate fails closed to "off" -> 404 (never accidentally reveals the canvas).
  </behavior>
  <action>
    Add `require_canvas()` to `dependencies.py` as a dependency FACTORY that returns an async `_dep`: resolve `feature_audience("visual_workflow_canvas")` FIRST and `raise _NOT_FOUND` when it is `"off"` (D-181-01 — the master switch hides from operators too, resolved BEFORE the `is_operator` no-op); then operator -> return, `"everyone"` -> return, `"role"` -> resolve via `resolve_caller_role` + `resolve_feature_access`, else `raise _NOT_FOUND`. Reuse the module `_NOT_FOUND` constant (D-181-02) — NEVER raise a 403 and NEVER hand-roll a new 404. In `features.py`, add the guard `feature_audience(f) != "off"` as the leading conjunct of the map comprehension so `"off"` resolves False for everyone INCLUDING operators (Pattern 3 / D-181-01) — the operator short-circuit `op or ...` becomes the second conjunct. Create `backend/app/api/canvas_canary.py` — a tiny top-level `APIRouter` with one throwaway `GET /canvas/ping` handler attached via `dependencies=[Depends(require_canvas())]` (D-181-04; comment it TEMPORARY, removed/repurposed when the first real canvas route lands in 182/183). Mount it in `main.py` (add to the `from app.api import ...` line + one `app.include_router(canvas_canary.router)` next to `features.router`). Write `backend/tests/test_181_flip_on.py` (TestClient): 404-when-off on the canary for operator + user, then flip on -> 200 + gate no-op + `GET /features` True.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest tests/test_181_flip_on.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_181_flip_on.py -q` exits 0.
    - `grep -n 'def require_canvas' backend/app/dependencies.py` matches; the closure raises `_NOT_FOUND` (404) and contains NO `HTTP_403_FORBIDDEN`.
    - `grep -n 'feature_audience(f) != "off"' backend/app/api/features.py` matches and precedes the `op or` short-circuit.
    - `grep -n 'Depends(require_canvas())' backend/app/api/canvas_canary.py` matches; `grep -n 'canvas_canary' backend/app/main.py` shows both the import and the `include_router`.
    - A TestClient `GET /canvas/ping` returns 404 (never 403) while off, and 200 after `set_feature_visibility("visual_workflow_canvas","everyone")`.
  </acceptance_criteria>
  <done>Canvas routes 404 when off for ALL callers (incl. operators), `GET /features` hides the key from everyone when off, the canary proves the gate end-to-end, and the flip-on path is green.</done>
</task>

<task type="auto">
  <name>Task 3: test_revert_byte_identical.py — the backend acceptance gate (D-181-06)</name>
  <files>backend/tests/test_revert_byte_identical.py</files>
  <read_first>
    - backend/tests/test_148_visibility_cold_default.py (the monkeypatch technique + the existing 4-key polarity asserts to reuse for the no-regression test)
    - backend/app/api/features.py (the `get_effective_features` shape the map-hides test drives)
    - backend/app/api/canvas_canary.py (the canary route the 404 test hits — created in Task 2)
    - .planning/phases/181-revert-foundation/181-VALIDATION.md (the exact test names + automated commands)
    - .planning/phases/181-revert-foundation/181-PATTERNS.md (the `test_revert_byte_identical.py` Pattern Assignment — the three named asserts)
  </read_first>
  <action>
    Create `backend/tests/test_revert_byte_identical.py` — the CI-run acceptance gate (D-181-06), modeled on `test_148_visibility_cold_default.py` (monkeypatch `load_app_settings`). Author exactly these three named tests: `test_features_map_hides_canvas_from_everyone_when_off` (drive `is_operator` True and False + a cold `load_app_settings`; assert `get_effective_features(...)["features"]["visual_workflow_canvas"]` is False for BOTH an operator and an end user); `test_require_canvas_404s_when_off` (TestClient `GET /canvas/ping` returns 404, NEVER 403, while off — the D-181-02 posture, using the canary from Task 2); `test_existing_governed_features_unchanged` (the 4 shipped keys resolve exactly as the Phase-148 contract — no regression, REVERT-02). Add a module docstring defining "byte-identical" OBSERVABLY (nav set + reachable-route set + responses + `/features` map + the two doors + run surface), NOT a bundle hash (D-181-07), and noting each future canvas route adds its own "404 when off" assertion here. Use `grep -v '^#'`-safe assertions (no comment-counting gates).
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest tests/test_revert_byte_identical.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_revert_byte_identical.py -q` exits 0 with the three named tests present and passing.
    - `pytest backend/tests/test_revert_byte_identical.py::test_require_canvas_404s_when_off -x` exits 0 and the test asserts status 404 (never 403).
    - `pytest backend/tests/test_revert_byte_identical.py::test_existing_governed_features_unchanged -x` exits 0.
    - The full backend suite `pytest tests -q` is green (no collateral regression).
  </acceptance_criteria>
  <done>The backend byte-identical acceptance gate exists, rides the existing `backend-tests.yml` (no new CI job), and asserts the flag-off product is observably byte-identical + the canary 404s + the 4 shipped features are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (canvas routes) | Untrusted caller hits a `require_canvas`-gated route; the gate is the SOLE authority (backend runs on the service-role key, no RLS backstop for product features). |
| client → API (`GET /features`) | Authed non-operator reads its own effective map to learn which nav to hide; must never leak an off feature as visible. |
| client → API (`PUT /admin/visibility`) | Operator-only write of the audience record; body is untrusted input that reaches a JSONB codec. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-01 | Information Disclosure | `feature_audience` / `_feature_record` / `require_canvas` flag resolution | mitigate | Cold default `"off"` in `_GOVERNED_FEATURES`; the resolvers NEVER raise (try/except -> `{}` -> cold default). A cache/DB blip resolves to `"off"` -> 404 / hidden (fail-closed), never a fail-open reveal. Asserted by `test_features_map_hides_canvas_from_everyone_when_off` + the cold-read unit test. |
| T-181-02 | Elevation of Privilege / Information Disclosure | operator short-circuit in `require_canvas` (dependencies.py) + `GET /features` comprehension (features.py) | mitigate | Resolve `"off"` BEFORE the operator short-circuit in BOTH sites, so an operator gets a byte-identical 404 / `false` — not a phantom canvas nav — while off. Asserted for an operator caller in `test_require_canvas_404s_when_off` + the map-hides test. |
| T-181-03 | Tampering | `PUT /admin/visibility` request body (`feature` / `audience`) | mitigate | Extend the existing code allowlists (`_VISIBILITY_FEATURES` += `visual_workflow_canvas`, `_VISIBILITY_AUDIENCES` += `off`); a crafted body with an off-allowlist feature/audience 400s BEFORE any write (SQLi-safe — the validated record is the only thing serialized). Route is already `require_operator`-gated (404 to non-operators). |

**Supply-chain note (T-181-SC):** N/A this phase — zero package-manager installs (`@xyflow/react` lands in 183, `zundo` in 184). RESEARCH `## Package Legitimacy Audit` = Not Applicable; no `[ASSUMED]`/`[SUS]` packages, no install-gate checkpoint needed. The phase is a `no threat model` phase per ROADMAP; this register is the light STRIDE pass the security_enforcement flag requires, all threats mitigated-by-design (no HIGH open threats — the full connector threat model lands with Phase 190).
</threat_model>

<verification>
- Quick (per task commit, < 30s): `cd backend && source venv/bin/activate && pytest tests/test_revert_byte_identical.py tests/test_181_*.py -q`
- Wave merge: full `cd backend && pytest tests -q` green (rides `backend-tests.yml`, no new CI job).
- The `require_canvas` gate returns 404 (never 403) for the canary route while off, for operator AND end-user callers.
- `GET /features` returns `visual_workflow_canvas: false` for everyone when off; True after an operator flip on.
- No migration file created; no package installed (`git status` shows no `supabase/migrations/*` and no `package.json`/`requirements.txt` dependency delta).
</verification>

<success_criteria>
- `visual_workflow_canvas` resolves to `"off"` on the cold default; the audience is honored end-to-end (model + gate + map + admin allowlist).
- `require_canvas` 404s for ALL callers (incl. operators) when off, reusing `_NOT_FOUND`; never 403.
- The temporary canary route makes the 404 gate testable NOW (D-181-04).
- `test_revert_byte_identical.py` + `test_181_off_audience.py` + `test_181_flip_on.py` are green and ride the existing CI.
- Zero migration, zero new package (D-181-05).
</success_criteria>

<output>
Create `.planning/phases/181-revert-foundation/181-01-SUMMARY.md` when done.
</output>
