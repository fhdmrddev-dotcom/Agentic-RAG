---
phase: 188.1
plan: 03
type: execute
wave: 3
depends_on: ["188.1-01", "188.1-02"]
files_modified:
  - frontend/src/components/workflows/editAffordance.ts
  - frontend/src/components/workflows/PlaneEditingLayer.tsx
  - frontend/src/components/workflows/WorkflowCanvas.tsx
  - frontend/src/components/workflows/FlowEdge.tsx
  - frontend/src/components/workflows/FlowEdge.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: []
# EMPTY BY DESIGN — refactor phase, no REQ- ids (ROADMAP §188.1).
success_criteria: ["SC#1", "SC#2", "SC#3", "SC#4", "SC#5"]
gap_closure: false

must_haves:
  truths:
    - "Per D-01 the extraction lands in TWO modules: `editAffordance.ts` holds `EDIT_AFFORDANCE`, `REVEAL_ON_HOVER`, `insertPointX` and `verticalOffsetFor`; `PlaneEditingLayer.tsx` holds the component and its props type and NO runtime constant at all — the mechanical corollary of D-01, and the reason `eslint src/components/workflows/` falls from 6 errors to 5"
    - "`WorkflowCanvas.tsx` is measurably smaller — the before figure is the MEASURED 1593 L, not the ROADMAP's stale 1574 — and the shrink is a MOVE: deletions from that file are ≈311 and insertions under 15"
    - "Neither new module can reach `WorkflowCanvas` in any import form, and that is enforced by a source fence with a positive control rather than by a docblock — a cycle here fails at RUNTIME, not at typecheck"
    - "The Plan 01 subtree fence now really does cover the moved code: a forbidden token injected into `PlaneEditingLayer.tsx` was observed turning it RED"
    - "The canvas renders identically: the Plan 02 captured baseline still deep-equals, and the count gate's 2462 existing tests are green"
    - "No re-export shim was left behind — `FlowEdge.test.tsx` imports `EDIT_AFFORDANCE` from its new home, and it is the tree's ONLY importer"
  artifacts:
    - path: "frontend/src/components/workflows/editAffordance.ts"
      provides: "the geometry table and the three pure helpers, as a leaf module"
      contains: "EDIT_AFFORDANCE"
      min_lines: 80
    - path: "frontend/src/components/workflows/PlaneEditingLayer.tsx"
      provides: "the ＋ / ✕ / picker plane layer"
      contains: "function PlaneEditingLayer"
      min_lines: 180
    - path: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      provides: "the shell, ≈307 lines lighter, importing the layer rather than declaring it"
      contains: "import { PlaneEditingLayer }"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      to: "frontend/src/components/workflows/PlaneEditingLayer.tsx"
      via: "a module-scope named import, rendered at the unchanged JSX site inside <ReactFlow>"
      pattern: "from \"@/components/workflows/PlaneEditingLayer\""
    - from: "frontend/src/components/workflows/FlowEdge.test.tsx"
      to: "frontend/src/components/workflows/editAffordance.ts"
      via: "the re-pointed EDIT_AFFORDANCE import — the only importer in the tree"
      pattern: "from \"./editAffordance\""
---

<objective>
Perform the extraction: two new modules, a verbatim move, exactly three import removals,
and the guards that can only exist once the new files do.

Purpose: this is the phase. `WorkflowCanvas.tsx` is the largest file in the workflow tree
and the CLAUDE.md hot-file ledger has recorded this extraction as due since Phase 188.
Phase 189 must add its node type to a file that has been paid down. The whole risk is that
a *behaviour-preserving* move is not behaviour-preserving — so the move is verbatim, the
diff must read as a move (`git diff --numstat` proves it), and every guard the code was
under before must be observed still biting after.

Output: `editAffordance.ts`, `PlaneEditingLayer.tsx`, a ≈307-line-lighter shell, a
mechanical ESM-cycle fence, a strengthened focusable-token control, two `⚠` prose
corrections, and a green gate battery.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-CONTEXT.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-PATTERNS.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-VALIDATION.md

<interfaces>
<!-- MEASURED at HEAD `18267202`. Every number is live in this repo: re-derive, do not
     inherit (`feedback_dont_inherit_unmeasured_claims`). Line numbers WILL have shifted
     if Plans 01-02 touched the file — they did not touch WorkflowCanvas.tsx, but verify. -->

THE SEAM — two contiguous regions of `WorkflowCanvas.tsx`, 311 lines total:

  Region A  346–413  (68 L)  EDIT_AFFORDANCE docblock + const (346-378)
                             REVEAL_ON_HOVER docblock + const (380-413)
  Region B  486–728 (243 L)  the MIS-PLACED insertPointX docblock (486-487)
                             verticalOffsetFor docblock + fn (488-516)
                             insertPointX (518-524)
                             PlaneEditingLayerProps (526-542)
                             PlaneEditingLayer docblock + fn (544-728)

SYMBOL → DESTINATION:

  EDIT_AFFORDANCE       :365  →  editAffordance.ts     (exported)
  REVEAL_ON_HOVER       :412  →  editAffordance.ts     (exported — the layer needs it)
  verticalOffsetFor     :506  →  editAffordance.ts     (exported)
  insertPointX          :518  →  editAffordance.ts     (exported)
  PlaneEditingLayerProps :526 →  PlaneEditingLayer.tsx (a TYPE — safe to export)
  PlaneEditingLayer     :569  →  PlaneEditingLayer.tsx (exported)

WHAT STAYS AND MUST NOT BE TOUCHED: `nodeTypes` (231), `ARIA_LABELS` (244),
`ARIA_LABELS_EDITABLE` (264), `edgeTypes` (306), `DEFAULT_EDGE_OPTIONS` (326), `EDGE_STYLE`
(336), `CanvasNotice` (431), `CanvasSession` (455), `WorkflowCanvasProps` (745), and the
entire `WorkflowCanvas` body (873-1591). `nodeTypes`/`edgeTypes` are module-scope BY DESIGN
— an inline object makes React Flow warn and re-render every node.

EXACTLY THREE IMPORTS TO REMOVE from `WorkflowCanvas.tsx`, and no others
(`noUnusedLocals: true` turns a leftover into a NEW typecheck error, moving the count off 33):

  :190  ViewportPortal            used at 604, 726 — both move            REMOVE
  :191  useStore as useFlowStore  used at 584, 590 — both move            REMOVE
  :201  CANVAS_LAYOUT             used 371,373,522,523,598,635,636,673,
                                  680,710,712 — ALL inside the regions    REMOVE

IMPORTS THAT MUST SURVIVE (each has a use that STAYS):
  :195  XYPosition    908, 920      · :208  PhaseTypeId   848, 1026
  :208  resolveDrop   1161          · :212  PhaseSpecJSON 746
  :215  StepTypePicker 1454 — the EMPTY-STATE picker. Do not remove it.

MEASURED THIS SESSION (a correction to research's closure table, which listed
`PlaneEditingLayer`'s value imports as react / @xyflow/react / editAffordance /
StepTypePicker): the layer ALSO needs `CANVAS_LAYOUT` from `canvasModel` — it is referenced
at 635, 636, 673, 680 and 712, inside Region B. This does NOT weaken the cycle proof:
`canvasModel` was already in the verified closure and reaches only `phaseVocabulary`.
DERIVE the final import list of each new module from actual usage after the paste; do not
copy any list from a planning document.

ALSO MEASURED: every occurrence of `EDIT_AFFORDANCE`, `REVEAL_ON_HOVER`, `insertPointX` and
`verticalOffsetFor` in `WorkflowCanvas.tsx` is INSIDE the moved regions (the only others —
:34, :491, :1494 — are prose). Consequence: after the move `WorkflowCanvas.tsx` imports
`PlaneEditingLayer` and NOTHING from `editAffordance`. If you find yourself adding an
`editAffordance` import to the shell, a usage was left behind.

THE ONLY OTHER FILE REFERENCING A MOVED SYMBOL:
  FlowEdge.test.tsx:75  import { EDIT_AFFORDANCE, WorkflowCanvas } from "./WorkflowCanvas"
Split it into two ADDED import statements (`canvasModel.purity.test.ts:18-21` is the shipped
statement of the additive-diff rule). `FlowEdge.tsx` itself does NOT import EDIT_AFFORDANCE
— its four mentions at :98, :99, :104, :132 are PROSE.

TWO `⚠` PROSE CORRECTIONS OWED (house rule: `WorkflowCanvas.tsx:313`, `soulData.ts:21-23` —
a correction is marked and kept, never a silent rewrite; and `PhaseNodeCard.tsx:34-35`: "a
docblock that still names a slot the component now fills is the same defect as a false one"):

  1. `FlowEdge.tsx:97-105` states the ESM cycle as the REASON `DETOUR` re-derives GAP and
     INSERT_Y instead of importing `EDIT_AFFORDANCE`. That reason goes FALSE the moment
     `EDIT_AFFORDANCE` leaves `WorkflowCanvas.tsx` — the import would now be legal. The PIN
     at `FlowEdge.test.tsx:466-484` STAYS and the duplication STAYS (D2, deferred: deleting
     one side of a falsified-RED drift pin deletes the pin's meaning). Only the stated reason
     is restated.
  2. `WorkflowCanvas.tsx:33-37` is the G-5 flag block naming `PlaneEditingLayer` and
     `EDIT_AFFORDANCE` as "the natural extraction … Flagged for Phase 188". Mark it done.
     `WorkflowCanvas.tsx:1494` points a reader at `REVEAL_ON_HOVER` as though it were local.

⚠ FENCE HAZARD ON EVERY PROSE EDIT: `WorkflowCanvas.tsx`, `PlaneEditingLayer.tsx` and
`editAffordance.ts` are all inside the Plan 01 subtree fence. Do NOT write the tokens
`grounding_mode`, `workflows/validate`, `MiniMap`, `hideAttribution`, `severity`, or any of
the seven `READING_WORDS` into any of them. And `phase_index` is pinned at EXACTLY ONE
occurrence in `WorkflowCanvas.tsx` (at :845, the `onInsertAt` prop docblock, which stays) —
do not add a second.

HOUSE MODULE SHAPE — the shipped precedent is `nodePresentation.ts`, itself created by Phase
184-03 as a "Wave-0 G-5 extraction" for the SAME eslint reason. Read its head. Required:
  · camelCase `.ts` for a non-component module, PascalCase `.tsx` for a component
  · named exports only, declared inline; NO default export (the shell's `export default` is
    legacy back-compat, do not copy it)
  · `react` first, blank line, then `@/…` alias imports alphabetised by path
  · a mandatory "STATE OF THE EXTRACTION — read this literally" paragraph naming what was
    cut, from where, that NO re-export shim was left, and WHICH GUARD keeps the paragraph
    honest (`nodePresentation.ts:30-45` is the model)

ANTI-PATTERNS, each already ruled out on evidence:
  · `React.memo` on the layer — changes re-render timing = an unmeasured behaviour change
    under SC#2. `StepTypePicker` (the closer analog) is unmemoised; `FlowEdge.tsx:122` uses
    `memo` and is NOT the analog to copy.
  · a `satisfies` upgrade on `EDIT_AFFORDANCE` — it is a bare `as const` today; move verbatim.
  · re-exporting `EDIT_AFFORDANCE` from `WorkflowCanvas.tsx` "for compatibility" — that keeps
    the coupling this phase exists to remove AND keeps the eslint error.
  · tidying prose while moving, including the mis-placed docblock at :486-487 (D8: move it
    verbatim, in the wrong place; a verbatim first diff is what makes "pure move" auditable).

GATE BASELINE (re-derive; measured this session):
  wc -l WorkflowCanvas.tsx                     1593   → expect ≈1286, record the ACTUAL
  npx tsc --noEmit -p tsconfig.app.json          33   → must stay 33
  node scripts/vitest-count-gate.cjs           2462   → ≥ 2462 after Plans 01-02 raised pins
  npx eslint src/components/workflows/            6   → must become 5
  npx vite build                              exit 0, exactly ONE [INEFFECTIVE_DYNAMIC_IMPORT]
The five eslint errors that must REMAIN (pre-existing, out of scope):
  BuilderStoreProvider.tsx:45,:53,:78 · FlowEdge.tsx:135 · StepTypePicker.test.tsx:561
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create both new modules — additive only, nothing deleted yet</name>

  <files>
frontend/src/components/workflows/editAffordance.ts
frontend/src/components/workflows/PlaneEditingLayer.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/WorkflowCanvas.tsx` — read Region A (`346-413`) and
  Region B (`486-728`) in full, plus the import block (`185-225`) and the JSX site
  (`1558-1585`). Read them as the source of a byte-for-byte copy, not as a starting point.
- `frontend/src/components/workflows/nodePresentation.ts` — the whole file. It is the
  shipped G-5 extraction this one is the third instance of: copy its docblock structure, its
  "STATE OF THE EXTRACTION" paragraph and its import-block ordering.
- `frontend/src/components/workflows/StepTypePicker.tsx:60-115` — the props-interface and
  component-signature convention for the `.tsx` module (`export interface XProps` with JSDoc
  per field; destructured props; named `export function`; no `React.FC`, no `memo`).
- `frontend/src/components/workflows/FlowEdge.tsx:90-140` — the tree's own statement of the
  ESM-cycle constraint, and the `DETOUR` bare-`as const` table. Copy this VOICE for the new
  modules' cycle paragraph: state the mechanism and the failure mode, not just the rule.
- `frontend/src/components/workflows/canvasModel.ts` — confirm the exact export name and
  member spellings of `CANVAS_LAYOUT` before importing it in two places.
  </read_first>

  <action>
Create `frontend/src/components/workflows/editAffordance.ts` and paste, byte-for-byte,
`EDIT_AFFORDANCE` + its docblock, `REVEAL_ON_HOVER` + its docblock, `verticalOffsetFor` +
its docblock, and `insertPointX` — including the mis-placed docblock at `:486-487` in its
current wrong position (D8). Add `export` to each of the four (three are currently
file-local). Derive the import list from the pasted body: `CANVAS_LAYOUT` as a value from
`canvasModel`, and `XYPosition` as `import type` from `@xyflow/react` (`verbatimModuleSyntax`
is on — a value import of a type is an error). No React, no component import: this module is
a LEAF and its own fence will say so.

Create `frontend/src/components/workflows/PlaneEditingLayer.tsx` and paste, byte-for-byte,
`PlaneEditingLayerProps` and the `PlaneEditingLayer` function with its docblock. Export the
component; export the props interface as a TYPE (`StepTypePicker.tsx` is the house form and
exporting a type does not trip `react-refresh/only-export-components`). **Export no runtime
constant of any kind** — that is D-01's mechanical corollary and the entire reason there are
two files rather than one; `FlowEdge.tsx:135` errors today for exactly that mistake. Derive
its imports from the pasted body — at minimum `ViewportPortal` and `useStore as useFlowStore`
from `@xyflow/react`, `StepTypePicker`, `CANVAS_LAYOUT` from `canvasModel`, the four symbols
from `./editAffordance`, plus type-only `XYPosition`, `PhaseSpecJSON`, `PhaseTypeId`.

Give each module the house docblock: the `Phase 188.1-03 Task 1 (D-01) — <ModuleName>.`
opener, a SHOUTED one-sentence thesis, and a mandatory "STATE OF THE EXTRACTION — read this
literally" paragraph naming the origin (`WorkflowCanvas.tsx` Region A / Region B), stating
that it is a HARD CUT with NO re-export shim, that the values moved verbatim with docblocks
intact, and naming the guard that keeps the claim honest (the cycle fence added in Task 3
and the Plan 01 subtree fence). In `editAffordance.ts`, restate the ESM-cycle constraint in
`FlowEdge.tsx:97-105`'s register and mark with `⚠` that this module's existence is what makes
a `FlowEdge → EDIT_AFFORDANCE` import legal — while recording that the duplication and its
drift pin deliberately STAY (D2).

Do NOT modify `WorkflowCanvas.tsx` in this task. The tree at the end of this task has the
moved code in two places; that is intentional and makes Task 2's diff a pure deletion.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports exactly **33** errors and **0** lines naming `editAffordance` or `PlaneEditingLayer`.
- `cd frontend && npx eslint src/components/workflows/editAffordance.ts src/components/workflows/PlaneEditingLayer.tsx` reports **0 problems**. A `react-refresh/only-export-components` error on `PlaneEditingLayer.tsx` means a runtime constant rode along — D-01's win is not realised and it must be moved to `editAffordance.ts`.
- `grep -c "^export" frontend/src/components/workflows/editAffordance.ts` returns **4** (the table plus three helpers).
- `grep -n "export const\|export let\|export var" frontend/src/components/workflows/PlaneEditingLayer.tsx` returns **no match** — the component file exports no runtime constant.
- `grep -n "React.memo\|memo(" frontend/src/components/workflows/PlaneEditingLayer.tsx` returns **no match**; `grep -n "export default" frontend/src/components/workflows/PlaneEditingLayer.tsx frontend/src/components/workflows/editAffordance.ts` returns **no match**.
- `grep -n "satisfies" frontend/src/components/workflows/editAffordance.ts` returns **no match** — `EDIT_AFFORDANCE` is still a bare `as const`.
- Both files contain the literal `STATE OF THE EXTRACTION`.
- `grep -nE "grounding_mode|workflows/validate|MiniMap|hideAttribution|severity|phase_index" frontend/src/components/workflows/editAffordance.ts frontend/src/components/workflows/PlaneEditingLayer.tsx` returns **no match** — no fenced token entered the subtree.
- `node scripts/vitest-count-gate.cjs` still prints `count gate OK` with **failed 0** (the duplicate declarations break nothing).
  </acceptance_criteria>

  <done>Both modules exist, typecheck, lint clean, export exactly what D-01 permits, carry the house extraction docblock, and were created additively — `WorkflowCanvas.tsx` is untouched at this commit.</done>
</task>

<task type="auto">
  <name>Task 2: Cut both regions from the shell, drop exactly three imports, and re-point the one external importer</name>

  <files>
frontend/src/components/workflows/WorkflowCanvas.tsx
frontend/src/components/workflows/FlowEdge.tsx
frontend/src/components/workflows/FlowEdge.test.tsx
  </files>

  <read_first>
- `frontend/src/components/workflows/WorkflowCanvas.tsx:185-225` — the import block, to
  remove exactly three specifiers and add exactly one. Re-derive each import's usage with a
  grep BEFORE deleting it; the three-and-only-three rule is the single most likely way this
  phase moves `tsc` off 33.
- `frontend/src/components/workflows/WorkflowCanvas.tsx:28-40` and `:1488-1498` — the two
  prose sites that name the moved symbols and go stale at this commit.
- `frontend/src/components/workflows/FlowEdge.tsx:90-140` — the cycle-reason paragraph whose
  stated reason goes false here.
- `frontend/src/components/workflows/FlowEdge.test.tsx:60-80` — the import block holding the
  tree's only `EDIT_AFFORDANCE` import, and `:460-490` — the drift pin that STAYS.
- `frontend/src/components/workflows/canvasModel.purity.test.ts:18-21` — the shipped
  statement of the additive-import rule (split into a separate statement, do not widen).
  </read_first>

  <action>
Delete Region A and Region B from `WorkflowCanvas.tsx` in full. The bodies now live in the
Task-1 modules; nothing is rewritten here, only removed.

Remove exactly three import specifiers — `ViewportPortal`, `useStore as useFlowStore`,
`CANVAS_LAYOUT` — and no others. Verify by grep that `StepTypePicker`, `XYPosition`,
`PhaseTypeId`, `resolveDrop` and `PhaseSpecJSON` each still have a use in the file before
you leave them, and that none of the three removed ones does. Add ONE import,
`{ PlaneEditingLayer } from "@/components/workflows/PlaneEditingLayer"`, placed to keep the
alias block alphabetised by path. Do NOT add an `editAffordance` import — every use of those
four symbols moved, and needing one means a usage was left behind.

Leave the JSX site (`{editable ? <PlaneEditingLayer … /> : null}` inside `<ReactFlow>`) and
its `184-12` comment byte-identical. The layer must keep rendering as a child of
`<ReactFlow>` through `<ViewportPortal>`; that is required, not incidental.

Re-point `FlowEdge.test.tsx`'s import: keep `import { WorkflowCanvas } from "./WorkflowCanvas"`
and ADD a separate `import { EDIT_AFFORDANCE } from "./editAffordance"` statement rather than
widening a line, so the whole diff is added lines. Change no assertion; the `DETOUR` drift
pin stays exactly as shipped.

Apply the two `⚠` prose corrections. In `FlowEdge.tsx:97-105`, keep the paragraph and mark
that the stated cycle reason was superseded in 188.1 — `EDIT_AFFORDANCE` now lives in a leaf
module so an import would be legal, and the duplication plus its pin are retained
deliberately (D2), not by inertia. In `WorkflowCanvas.tsx:33-37`, mark the G-5 flag as
DISCHARGED, naming the two modules and the measured before/after line counts. Fix `:1494`'s
pointer so it names `editAffordance`'s `REVEAL_ON_HOVER` rather than implying a local
declaration. Prose only — no behaviour, and none of the fenced tokens.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/FlowEdge.test.tsx</automated>
  </verify>

  <acceptance_criteria>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports exactly **33** errors, with **0** `TS6133` / `TS6192` (unused local/import) and **0** lines naming `WorkflowCanvas`, `PlaneEditingLayer` or `editAffordance`. Any drift off 33 means the import-removal set was wrong.
- `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/FlowEdge.test.tsx` exits 0 with **0 failed** — including Plan 02's captured `AFFORDANCE_SHAPE` baseline, which is the SC#2 proof that the rendered geometry is unchanged.
- `wc -l frontend/src/components/workflows/WorkflowCanvas.tsx` is **< 1350** and is recorded verbatim in the SUMMARY beside the measured before figure of **1593** (not the ROADMAP's stale 1574).
- `git diff --numstat -- frontend/src/components/workflows/WorkflowCanvas.tsx` shows deletions **≥ 300** and insertions **< 25**. A large insertion count means prose was rewritten rather than moved, and the "pure move" property a reviewer relies on is gone.
- `grep -c "ViewportPortal\|useFlowStore\|CANVAS_LAYOUT" frontend/src/components/workflows/WorkflowCanvas.tsx` returns **0**; `grep -c "StepTypePicker" frontend/src/components/workflows/WorkflowCanvas.tsx` returns **≥ 2** (the surviving import plus the empty-state use).
- `grep -n "EDIT_AFFORDANCE" frontend/src/components/workflows/WorkflowCanvas.tsx` returns **no match** — no re-export shim was left behind.
- `grep -c "phase_index" frontend/src/components/workflows/WorkflowCanvas.tsx` (excluding nothing — the pin counts raw occurrences) returns exactly **1**.
- `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors**, and the five are `BuilderStoreProvider.tsx:45,:53,:78`, `FlowEdge.tsx:135`, `StepTypePicker.test.tsx:561`. This is D-01's measurable win: 6 → 5.
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json` reports **0 files changed**.
  </acceptance_criteria>

  <done>The shell no longer declares the moved symbols, imports the layer, drops exactly three imports and no more; the tree's one external importer reads the new home; `tsc` is unmoved at 33 and eslint has fallen 6 → 5.</done>
</task>

<task type="auto">
  <name>Task 3: Fence the cycle, prove the subtree fence bites, and run the full gate battery</name>

  <files>
frontend/src/components/workflows/WorkflowCanvas.test.tsx
frontend/src/components/workflows/FlowEdge.test.tsx
scripts/vitest-count-gate.cjs
  </files>

  <read_first>
- `frontend/src/components/workflows/WorkflowCanvas.test.tsx` — the Plan 01 subtree-fence
  block and the `:646-666` region, whose `POSITIVE CONTROLS — both regexes match the shapes
  they forbid` comment is the exact shape the cycle fence must copy.
- `frontend/src/components/workflows/FlowEdge.test.tsx:290-315` — the `FOCUSABLE_TOKENS`
  positive control. Measured: `onClick` in `WorkflowCanvas.tsx` goes 4 → 2 across this move
  (`:1352` notice-Undo and `:1441` add-first-step stay), so the control stays green while
  covering less. That is the same silent-weakening class as Pitfall 1.
- `frontend/src/components/workflows/canvasModel.purity.test.ts:14-21` — the `?raw` import
  idiom and its comment convention.
- `scripts/vitest-count-gate.cjs:40-120` — LOWERED vs EXTENDED. This plan performs only
  extensions, and RELOCATES NO TEST: every existing assertion reaches `PlaneEditingLayer`
  through `<WorkflowCanvas editable>` and renders at the same JSX site after the move, so
  every suite passes unchanged. A relocation would present as `[count-decrease]` plus a
  `new` file, which the script's doctrine does not recognise.
  </read_first>

  <action>
Add the ESM-cycle fence to `WorkflowCanvas.test.tsx` as an EXTENSION (a new `describe`, no
existing test touched). Import `./PlaneEditingLayer?raw` and `./editAffordance?raw` in the
house form — relative specifier, no extension, `<camelCaseModuleName>Source` identifier, with
a `//` comment naming the idiom and its precedent. Assert that NEITHER source matches a
static import from a `WorkflowCanvas` specifier NOR a dynamic `import("…WorkflowCanvas")`
call, and that `editAffordance` is a LEAF: it names no `react` import and no component
module. Include an inline POSITIVE CONTROL asserting each regex matches a literal string of
the shape it forbids — a fence with no control can pass vacuously. Forbid the type-only
import back as well: `verbatimModuleSyntax` makes the value/type distinction easy to get
wrong under a later edit, and the cheap rule is the safe one.

Strengthen the `FOCUSABLE_TOKENS` control in `FlowEdge.test.tsx`: point it at
`planeEditingLayerSource` IN ADDITION to `workflowCanvasSource`, since the `＋`/`✕` click
handlers — the very controls SC#4's "no focusable control lives inside the card" invariant is
about — now live in the new file. Keep the existing canvas half; this is an addition.

PROVE THE SUBTREE FENCE NOW BITES ON THE MOVED CODE. Temporarily insert the literal
`grounding_mode` into a comment in `PlaneEditingLayer.tsx`, run
`npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx`,
and confirm the fence FAILS. Revert with `git checkout`. This is the phase's headline
verification: before Plan 01 the fence read one file, and after this observation it is known
— not assumed — to read the moved 311 lines. Record the failure text verbatim.

Then extend the count-gate pins for any suite whose actual rose, reading the numbers from the
gate script's own printed `actual` column across TWO agreeing runs. Extensions only. No
`TARGETS` edit. Finally run the whole battery and record every number.
  </action>

  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>

  <acceptance_criteria>
- `node scripts/vitest-count-gate.cjs` prints `count gate OK`, **failed 0**, total **≥ 2462**, **no `[count-decrease]`**, **no `[missing-file]`**, and pinned == actual for every suite this phase touched. Both runs' actual columns are recorded and agree.
- RED OBSERVED AND RECORDED VERBATIM: the injected `grounding_mode` in `PlaneEditingLayer.tsx` made the subtree fence FAIL, and `git status` is clean of that injection afterwards. Without this observation SC#5's "no loss of coverage" is an assertion, not a finding.
- RED OBSERVED for the cycle fence: temporarily add `import { WorkflowCanvas } from "./WorkflowCanvas"` to `editAffordance.ts`, confirm the cycle fence FAILS, revert. Record the failure text.
- `cd frontend && npx vite build` exits **0**, emits **no new circular-dependency warning**, and emits exactly **one** `[INEFFECTIVE_DYNAMIC_IMPORT]` warning — the pre-existing `WorkflowRunPage` one that D-02 deliberately leaves alone. Record the count before and after.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports exactly **33**.
- `cd frontend && npx eslint src/components/workflows/` reports exactly **5 errors**.
- `wc -l frontend/src/components/workflows/WorkflowCanvas.tsx` recorded; `git diff --numstat` for that file shows deletions ≥ 300, insertions < 25.
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json` reports **0 files changed**.
- No test was relocated: `git diff scripts/vitest-count-gate.cjs` shows no pin lowered and no `TARGETS` edit.
  </acceptance_criteria>

  <done>The ESM-cycle constraint is machine-enforced with a positive control and was observed RED; the subtree fence was observed RED against a violation inside the moved code; the focusable-token control covers the new file; the full battery is green and every number is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module graph → runtime | An ESM cycle typechecks and lints clean and fails only at runtime, as a TDZ `ReferenceError` in whichever module a caller reaches first. `WorkflowCanvas` imports `FlowEdge`'s VALUE at module scope for `edgeTypes`, so this boundary is live. |
| shipped guard → moved code | A `?raw` fence's scope is a FILE, not a behaviour. Code that leaves the file leaves the fence, silently and greenly. |
| shell → extracted layer | Ten props and two React Flow context reads. Anything the layer starts deriving for itself, rather than receiving, crosses red line D-14. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188.1-05 | Denial of service | `PlaneEditingLayer.tsx` / `editAffordance.ts` import graph | mitigate | A `?raw` fence forbidding any static or dynamic import naming `WorkflowCanvas` from either new module, carrying an inline positive control, observed RED against a deliberate back-import; plus `vite build` exit 0 with no new circular-dependency warning |
| T-188.1-04 | Repudiation | the Plan 01 subtree fence, now that code has actually moved | mitigate | `grounding_mode` injected into `PlaneEditingLayer.tsx` must turn the fence RED. This is the single observation that converts "no loss of coverage" from a claim into a measurement |
| T-188.1-06 | Tampering (integrity of a shipped promise) | the rendered canvas | mitigate | Plan 02's captured `AFFORDANCE_SHAPE` baseline must still deep-equal after the move; the 2462-test count gate is the wider regression net; operator rows M1-M3 in Plan 05 cover what jsdom structurally cannot see |
| T-188.1-08 | Tampering | `WorkflowCanvas.tsx` prose edits | mitigate | Every fenced token is forbidden in the three subtree files, and `phase_index` is pinned at exactly one occurrence; grep-level acceptance criteria enforce both |
| T-188.1-09 | Elevation of privilege | red line D-14 — the canvas as a projection | accept | The move is verbatim and the layer's ten props are unchanged, so no run-state derivation can be acquired. Mechanically fenced by the re-scoped `@/lib/phaseState` and `runVocabulary` negatives from Plan 01, which now cover both new modules |
| T-188.1-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (`188.1-RESEARCH.md` § Package Legitimacy Audit: not applicable — nothing proposed). Gate: `git diff --stat -- frontend/package.json frontend/package-lock.json` = 0 files changed |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → exactly 33, zero TS6133/TS6192
- `node scripts/vitest-count-gate.cjs` → `count gate OK`, failed 0, total ≥ 2462, no `[count-decrease]`, no `[missing-file]`
- `cd frontend && npx eslint src/components/workflows/` → exactly 5 errors (the five named pre-existing ones)
- `cd frontend && npx vite build` → exit 0, one `[INEFFECTIVE_DYNAMIC_IMPORT]`, no circular-dependency warning
- `wc -l frontend/src/components/workflows/WorkflowCanvas.tsx` → < 1350 (before: 1593)
- `git diff --numstat -- frontend/src/components/workflows/WorkflowCanvas.tsx` → ≥300 deletions, <25 insertions
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json` → 0 files changed
</verification>

<success_criteria>
1. Two modules exist per D-01; `PlaneEditingLayer.tsx` exports no runtime constant; `eslint src/components/workflows/` falls 6 → 5.
2. `WorkflowCanvas.tsx` is measurably smaller and the diff reads as a move (numstat proves it).
3. The ESM-cycle constraint is a test with a positive control, observed RED.
4. The subtree fence was observed RED against a violation inside the moved code.
5. `tsc` unmoved at 33; count gate green; captured affordance baseline still equal; `vite build` clean.
6. No re-export shim; no relocated test; no package churn; no backend or migration change.
</success_criteria>

<output>
Create `.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-03-SUMMARY.md` when done. Record verbatim: `wc -l` before/after, the numstat line, the tsc count, the eslint error list, the two RED-observation failure texts, the `vite build` warning count before/after, and both gate runs' actual columns.
</output>
