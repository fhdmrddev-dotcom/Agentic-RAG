---
phase: 188.1
slug: workflowcanvas-extraction-refactor-lift-planeeditinglayer-an
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-05
audited: 2026-08-06
auditor: orchestrator (/gsd:validate-phase 188.1) — measured at HEAD 91c37b5c
gaps_found: 0
gaps_resolved: 0
gaps_escalated: 0
manual_only: 3
manual_only_driven: 3
---

# Phase 188.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `188.1-RESEARCH.md` § Validation Architecture. This is a
> **behaviour-preserving refactor** — validation proves *unchanged*, it does not
> re-drive the Phase 188 board.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (frontend), invoked via `npx vitest run` from `frontend/` |
| **Config file** | `frontend/vite.config.ts` (vitest section) + gate wrapper `scripts/vitest-count-gate.cjs` |
| **Quick run command** | `cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/FlowEdge.test.tsx` |
| **Full suite command** | `node scripts/vitest-count-gate.cjs` |
| **Typecheck command** | `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — **never** bare `--noEmit` (checks zero files), **never** `tsc -b` (stale tsbuildinfo) |
| **Build command** | `cd frontend && npx vite build` |
| **Lint command** | `cd frontend && npx eslint src/components/workflows/` |
| **Estimated runtime** | quick ~10 s · full count gate ~2–4 min |

**Measured baselines at phase start (re-derived at plan time — do not inherit):**

| Metric | At phase start | Stale claim corrected |
|---|---|---|
| `WorkflowCanvas.tsx` | **1593 L** | ROADMAP said 1574 |
| `PhaseNodeCard.tsx` | **755 L** | CLAUDE.md ledger said 478 |
| `tsc --noEmit -p tsconfig.app.json` | **33 errors** | (confirmed accurate) |
| count gate | **2462 / 2462, 0 failed, zero slack** | (confirmed accurate) |
| `eslint src/components/workflows/` | ~~**2 errors**~~ → **6** (see audit correction) | ⚠ **this row was itself wrong** |

### Re-measured at phase close — 2026-08-06, HEAD `91c37b5c`

Every figure below was run in the audit session, not carried from a SUMMARY
(`feedback_dont_inherit_unmeasured_claims`).

| Metric | Command | At close | Δ vs start | Verdict |
|---|---|---|---|---|
| `WorkflowCanvas.tsx` | `wc -l` | **1292 L** | −301 | ✅ shrank (plan predicted ≈1286; actual is **1292**, six lines above the estimate — the estimate, not the result, was approximate) |
| `PhaseNodeCard.tsx` | `wc -l` | **797 L** | +42 | ⚠ grew — this is why its ledger row now reads **G-5 fires** |
| `PlaneEditingLayer.tsx` | `wc -l` | **268 L** | new | ✅ destination module exists |
| `editAffordance.ts` | `wc -l` | **164 L** | new | ✅ destination module exists |
| count gate | `node scripts/vitest-count-gate.cjs` | **2477 / 2477 · 0 failed · 45/45 pinned · no `[count-decrease]`** | +15 | ✅ SC#5 met, zero slack |
| typecheck | `npx tsc --noEmit -p tsconfig.app.json` | **33 errors** | 0 | ✅ SC#4 met — unmoved |
| build | `npx vite build` | **exit 0**, **zero** circular-dependency warnings | — | ✅ SC#3 met |
| lint | `npx eslint src/components/workflows/` | **5 errors** | −1 | ✅ one removed (see below) |
| net-new test files | `git log --diff-filter=A` over the range | **0** | — | ✅ the "relocate no test" doctrine held — every W0 item **extended** an existing suite |

**⚠ The phase-start eslint baseline of `2` was FALSE; the real figure was `6`.** The audit
measured 5 at close, distributed as `BuilderStoreProvider.tsx` 3 · `FlowEdge.tsx` 1 ·
`StepTypePicker.test.tsx` 1. `git log` over the phase range shows `BuilderStoreProvider.tsx`
and `StepTypePicker.test.tsx` were **never touched**, so their 4 errors pre-existed; the one
error this phase actually removed is the `react-refresh/only-export-components` on
`WorkflowCanvas.tsx` that this very table recorded at `:365` — `npx eslint
src/components/workflows/WorkflowCanvas.tsx` now exits **0**, because lifting `EDIT_AFFORDANCE`
out left the file exporting only components. That makes the true movement **6 → 5**, which is
what `188.1-SECURITY.md` recorded independently, and it means the Sampling-Rate line below
(*"eslint errors 2 → 1"*) named the right **delta** against the wrong **base**. The delta was
met; the base was never measured. Corrected in place below.

---

## Sampling Rate

- **After every task commit:** quick run command (three canvas suites, seconds).
- **After every plan wave:** `node scripts/vitest-count-gate.cjs` **plus** `npx tsc --noEmit -p tsconfig.app.json` — the two knobs SC#5 names.
- **Before `/gsd:verify-work`:** full gate green — **≥ 2462 / 0 failed / no `[count-decrease]` / no `[missing-file]`** · `tsc` **= 33** · `vite build` exit 0 · `eslint src/components/workflows/` errors ~~2 → 1~~ **6 → 5** *(base corrected at audit; the delta of −1 was always right)* · `git diff --stat -- backend/ supabase/migrations/ frontend/package.json` = **0 files**.

**Measured at close:** 2477 / 0 failed / 45-of-45 pinned · tsc 33 · build exit 0 · eslint 5 · 0 files. **All met.**
- **Max feedback latency:** 15 s (quick) / 240 s (full gate).

---

## Per-Task Verification Map

Task IDs are assigned by the planner; this map is the **contract each task must satisfy**.
Every row's `Automated Command` is the proof for the named success criterion.

| SC | Secure / correct behavior | Test Type | Automated Command | Verified at (read in the audit session) | Status |
|----|---------------------------|-----------|-------------------|-------------|--------|
| SC#1 | Both symbols live outside `WorkflowCanvas.tsx`; file measurably shrank | measurement | `wc -l frontend/src/components/workflows/WorkflowCanvas.tsx` + `git diff --numstat` | **1593 → 1292** (−301); `PlaneEditingLayer.tsx` 268 L, `editAffordance.ts` 164 L both exist | ✅ green |
| SC#1 | `EDIT_AFFORDANCE` importable from new module; canvas does not re-export it | unit | `npx vitest run src/components/workflows/FlowEdge.test.tsx` | `FlowEdge.test.tsx` pinned **22/22**; leaf assertion `WorkflowCanvas.test.tsx:655`; positive control `:645` proves a re-export *would* match | ✅ green |
| SC#1 | CLAUDE.md hot-file ledger row flips `G-5 fires` → `satisfied` with measured numbers | doc | manual edit, measured before/after | `CLAUDE.md:195` satisfied w/ 1593→1292; `:196` PhaseNodeCard **G-5 fires**, 797 L. Independently verified as T-188.1-11 | ✅ green |
| SC#2 | Affordance geometry, classes and ARIA byte-identical | unit (captured baseline) | `npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx` | `AFFORDANCE_SHAPE_BASELINE` **captured** literal at `WorkflowCanvas.editing.test.tsx:1234`, deep-equal at `:1369`; survived the 311-line move untouched | ✅ green |
| SC#2 | Every shipped canvas state still renders | integration | `node scripts/vitest-count-gate.cjs` | **2477 / 2477 · 0 failed** (baseline row said 2462 — the net grew, never decreased) | ✅ green |
| SC#2 | CSS-dependent reachability (`pointer-events`, hover reveal, zoom counter-scale) | **manual** | — (jsdom applies no CSS) | **M1 + M2 driven** — `188.1-UAT.md`, own evidence each | ✅ driven |
| SC#2 | 185 seal / 187 node face / 188 run readings on the live run surface | **manual** | — | **M3 driven by the operator** on a real end-to-end run; clock sampled **twice**, delta asserted | ✅ driven |
| SC#3 | Neither new module imports `WorkflowCanvas`, in any form | unit (source fence + positive control) | `npx vitest run src/components/workflows/WorkflowCanvas.test.tsx` | `IMPORT_FROM_CANVAS` `:634` + `DYNAMIC_IMPORT_CANVAS` `:635`; **4 inline positive controls** `:641-647`; edge-exists-and-points-one-way `:681-689` | ✅ green |
| SC#3 | No cycle at build time | smoke | `npx vite build` exit 0, no new circular-dependency warning | **exit 0**, **zero** circular-dependency warnings (the two `[INEFFECTIVE_DYNAMIC_IMPORT]` are pre-existing chunking notes, not cycles) | ✅ green |
| SC#4 | A third badge is a typecheck error (max-2 `BadgeSlots` tuple untouched) | type | `npx tsc --noEmit -p tsconfig.app.json` stays at 33 | **33** — unmoved | ✅ green |
| SC#4 | No focusable control inside the card | unit | `npx vitest run src/components/workflows/WorkflowCanvas.test.tsx` — **must be re-verified to still measure the extracted layer** | **Re-verified, and it was vacuous before:** an *editable*-variant assertion now exists at `:339-370` with a `[data-testid^="canvas-insert-"]` non-vacuity control at `:352-357`. Also fenced from the other side by `FOCUSABLE_TOKENS` over `planeEditingLayerSource` (`FlowEdge.test.tsx:316`) | ✅ green |
| SC#5 | No test lost, relocated out of the gate, or unpinned | integration | `node scripts/vitest-count-gate.cjs` | **45/45 pinned files present · no per-file decrease · 0 failing**. `git log --diff-filter=A` = **0 net-new test files** — every W0 item extended a suite in place, so the RELOCATION hazard below was never triggered | ✅ green |
| SC#5 | Eight `?raw` negative fences still cover the moved 311 lines | unit | re-scoped fences in `WorkflowCanvas.test.tsx` + `.composition.test.tsx` | Both suites read `canvasSubtreeSource` built from `CANVAS_SUBTREE_PATHS` (`WorkflowCanvas.test.tsx:67/:77`, `.composition.test.tsx:59/:69`), naming **both** destination modules; non-empty guard at `:612`; move-invariant control `:598` asserts `EDIT_AFFORDANCE = {` is inside the fenced source | ✅ green |
| SC#6 | Five WR-04 prototype-key sites own-guarded | unit | `npx vitest run src/components/workflows/PhaseNodeCard.test.tsx src/components/panel/__tests__/` | All 5 guards read out of source: `PhaseNode.tsx:165` · `panel/PhaseCard.tsx:72` · `:137` · `PhaseNodeCard.tsx:307` (called `:434`/`:731`). Falsifications: `PhaseNode.test.tsx` 26 · `PhaseNodeCard.test.tsx` 107 · **`PhaseTimeline.test.tsx` 10** — sites 2/3 live there and drive `panel/PhaseCard` **directly**, because a timeline fixture is normalised by the already-guarded `phaseStatusFromDb` and would have been green against the unguarded tree | ✅ green |
| SC#6 | `getWorkflowRun` encodes its id (WR-07) | unit | assert the URL a mocked `fetch` receives for `runId = "a/b"` | `lib/api.ts:3768` `encodeURIComponent`; assertion `WorkflowRunPage.test.tsx:1724` expects `a%2Fb` | ✅ green |
| SC#6 | UF-1 `last_workflow_run_id` registered on `GET /threads/{id}/workflow` | doc | `<threat_model>` entry in plan + `188.1-SECURITY.md` | Registered as **T-188.1-03** (`accept`); verified `CANVAS_GATED_PATHS` still has exactly 3 members and no second query was added | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**16 / 16 rows resolve. 13 automated · 3 manual-only, all three driven. 0 gaps, 0 escalations.**

⚠ **Two path citations in this table's original draft did not resolve** and were corrected on
measurement: the canvas suites sit at `src/components/workflows/*.test.tsx` (alongside their
subjects), **not** under a `workflows/__tests__/` directory — that directory does not exist.
`src/components/panel/__tests__/` *does* exist and the SC#6 command is valid as written. The
same drift class was reported by `/gsd:secure-phase 188.1`; it is a documentation defect only,
never a coverage one — every named suite is pinned and ran.

---

## Wave 0 Requirements

- [x] `WorkflowCanvas.test.tsx` — **ESM cycle fence** over `PlaneEditingLayer?raw` + `editAffordance?raw`, with a positive control (SC#3) — **shipped `:634-690`**, observed RED against a deliberate back-import
- [x] `WorkflowCanvas.test.tsx` / `WorkflowCanvas.composition.test.tsx` — **re-scope the eight narrowed `?raw` negative fences** to the concatenated subtree source. *This is the phase's real coverage hazard: after the move all eight stay green while covering 311 fewer lines — Phase 188's F7 lesson in a new dress.* — **shipped, and wider than scoped: 13 negatives now read `canvasSubtreeSource`, not 8.** Proved by a RED observation (`grounding_mode` planted in the moved code turned **both** subtree fences red), not by the green
- [x] `WorkflowCanvas.test.tsx` — re-verify the SC#4 "no focusable control inside a node" assertion still measures the extracted layer (confirm it renders in `editable` mode, else it is vacuous) — **it WAS vacuous; an editable variant now exists at `:339-370`** with an inversion-proven control
- [x] `WorkflowCanvas.editing.test.tsx` — `AFFORDANCE_SHAPE` **captured-baseline** block (SC#2 Tier 2) — **shipped `:1234`**, 12 entries, sorted by `data-testid`, two agreeing runs before commit, deep-equal `:1369`
- [x] `FlowEdge.test.tsx` — re-point the `EDIT_AFFORDANCE` import and strengthen the `FOCUSABLE_TOKENS` positive control — **shipped**; `FOCUSABLE_TOKENS` now measures `workflowCanvasSource`, `planeEditingLayerSource` **and** `flowEdgeSource` (`:306`/`:316`/`:319`)
- [x] `PhaseNodeCard.test.tsx` / `src/components/panel/__tests__/` — **one falsification per WR-04 site**: a `constructor` key must not yield a function — **shipped across three suites** (107 · 26 · 10). All six observed RED first
- [x] `WorkflowRunPage.test.tsx` (or an `api` fetch-shape test) — assert the encoded URL for a `runId` containing `/` — **shipped `:1724`** (`a%2Fb`), asserting the URL the **mocked `fetch` actually received**
- [x] **Framework install:** none — vitest, tsc, eslint and vite all verified running this session. **Confirmed at close: zero packages installed across the whole phase** (`git diff --stat` on `package.json`/`package-lock.json` = 0 files)

**Wave 0: 8 / 8 complete.** Two items shipped *wider* than scoped (the fence count, and the
focusable-control assertion which turned out to be vacuous rather than merely unverified).

### Count-gate knob contract (SC#5)

| Knob | Edit required |
|---|---|
| `TARGETS` | **None.** `TARGETS[0]` is the bare directory `src/components/workflows` (`vitest-count-gate.cjs:403`) — both new modules and any new suite are already inside it. Do not edit. |
| `BASELINE` | **EXTENSIONS ONLY, never a lowering.** `WorkflowCanvas.test.tsx` 46 → measured actual · `WorkflowCanvas.editing.test.tsx` 57 → measured actual · `PhaseNodeCard.test.tsx` 105 → measured actual. |

**⚠ Doctrine gap the plan must not walk into.** The gate script's header (`:82-86`) recognises only
**LOWERED** and **EXTENDED** pin movements. A **RELOCATION** (same test, different file) is neither
and presents as `[count-decrease]` plus a `new` file. **Recommendation: relocate no test.** Every
existing assertion reaches `PlaneEditingLayer` *through* `<WorkflowCanvas editable>` (it is not
exported today), and after the move it renders at the same JSX site — so existing suites pass
unchanged. If the plan does relocate a test anyway, it must lower the source pin and raise the
destination pin **in the same commit** keeping `BASELINE_TOTAL` constant, **and** add a third named
case to the script header.

**Rule for every pin number:** read it from the script's own printed `actual` column across **two
agreeing runs** — never from a hand count of `it(` literals (unsound under `it.each`) and never from
a planning document.

---

## Manual-Only Verifications

Lean per the project efficiency calibration: a refactor proves *unchanged*, it does not re-drive
the Phase 188 UAT board. Three rows only.

| ID | Behavior | Why Manual | Test Instructions |
|----|----------|------------|-------------------|
| **M1** | Editing affordances remain reachable and correctly shaped | jsdom applies no CSS — `pointer-events`, hover reveal and zoom counter-scale are unprovable in unit tests | Open a workflow in the Builder in editable mode. Hover a lane: the ＋ insert affordance appears. Click it: a phase inserts at the expected index. Click ✕: the phase removes. Zoom in and out: affordances keep constant on-screen size and stay clickable at both extremes. |
| **M2** | The 185 governance corner seal and the 187 node-face ladder render unchanged | Visual identity against shipped commit `7e1bff34`; not a DOM-equality claim | On a workflow with a grounded/locked phase, confirm the corner seal sits top-right of the card and the node face still shows author name → config-derived → type sentence. Toggle ⌥ Technical-names: the **subtitle** swaps, not the title. |
| **M3** | The 188 run readings still animate on the live run surface | Phase 188's F7 lesson: reachability on a real surface is not provable by a component fence — a clock is correct at every sample and wrong only *across two* | Launch a workflow run. Watch the canvas for ≥ 2 samples: the elapsed clock **ticks** (assert the delta, not one reading), phase statuses advance, and an unrecognised wire status still renders "State unknown" — never "Complete". |

### Manual-only results — 3 / 3 DRIVEN, 0 pending, 0 blocked

| ID | Result | Driven by | Own evidence? |
|----|--------|-----------|---------------|
| **M1** | **PASS** (refactor-scoped) | assistant — Chrome MCP, DOM-geometry instrumentation | yes — Builder draft |
| **M2** | **PASS** | assistant + operator (seal sub-check on the operator's own run surface) | yes |
| **M3** | **PASS** | **operator**, real end-to-end run, 2026-08-06 | yes — clock sampled twice, delta asserted |

**No row inherited a PASS from another row** (`feedback_uat_rows_need_their_own_evidence`): M1
and M2's evidence 1-3 came from the Builder draft, M2's seal sub-check and all of M3 from the
operator's run surface.

**Four defects surfaced by driving these rows, all dispositioned rather than dropped:**

| # | Finding | Disposition |
|---|---|---|
| **F-1** | A selected card occludes its own `✕` | **Measured pre-existing**, not caused by the move. Filed as **BUG-260806-01**. Not security-relevant (`/gsd:secure-phase` concurred) |
| **F-2** | shipped-behaviour observation | recorded |
| **F-3** | The run surface rendered its status **twice** | **FIXED in-phase** (`WorkflowRunPage.tsx`; `sr-only` only, `aria-live` preserved, guarded at `WorkflowRunPage.test.tsx:729-742`) |
| **F-4** | shipped-behaviour observation | **deferred** with a trigger |

⚠ **Process note carried from `/gsd:secure-phase`:** the F-3 fix was a production change shipped
inside plan 05, and `188.1-05-SUMMARY.md` has **no `## Threat Flags` section** to declare it —
Phase 188's UF-5 recurring. The change was read and cleared retroactively; the gap is in the
declaration, not the code.

---

## Validation Sign-Off

- [x] All tasks have an `<automated>` verify or a declared Wave 0 dependency
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all ❌ MISSING references above — **8/8, zero left MISSING**
- [x] No watch-mode flags (`vitest run`, never bare `vitest`) — verified across every command in this file
- [x] Feedback latency < 240 s — count gate measured well inside the bound
- [x] M1–M3 driven and recorded with their own evidence — **a PASS inherited from another row is not a driven row** — **3/3, none inherited**
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ✅ **VALIDATED 2026-08-06** at HEAD `91c37b5c` — 16/16 rows resolve, 0 gaps, 0 escalations.

---

## Validation Audit 2026-08-06

| Metric | Count |
|--------|-------|
| Requirements audited | 16 |
| COVERED (automated) | 13 |
| Manual-only (all driven) | 3 |
| Gaps found | **0** |
| Resolved | 0 |
| Escalated | 0 |
| Tests generated this audit | **0** — none were needed |
| `gsd-nyquist-auditor` spawned | no (Step 3: no gaps → Step 6) |

**Why zero gaps, stated plainly:** this phase front-loaded its coverage. Wave 0 was authored
*before* the move (plans 01 and 02 wrote the fences and captured the baseline; plan 03 did the
cut), so by the time code relocated, every fence that could have silently narrowed had already
been re-scoped and observed RED. The audit's job was to confirm that, and it did.

**What the audit corrected rather than accepted** — three claims in this file's own draft were
measured false, in keeping with the standing project lesson that an inherited number is a claim,
not a fact:

1. **eslint phase-start `2` → the real base was `6`.** The named delta (−1) was right; the base
   was never measured. Now 5, and the one error removed is the `WorkflowCanvas.tsx:365`
   `react-refresh` violation this table itself recorded — the extraction fixed it as a
   side-effect, which is a genuine and previously unclaimed phase win.
2. **`workflows/__tests__/` does not exist.** Four suite paths cited through it; the suites sit
   alongside their subjects. Documentation defect only — all four are pinned and ran.
3. **SC#1 predicted ≈1286 L; the actual is 1292.** The estimate was approximate, the result is
   not. Recorded as measured.

**One thing this audit deliberately did NOT do:** re-drive the Phase 188 UAT board. Per the
project efficiency calibration, a behaviour-preserving refactor proves *unchanged* — the three
manual rows are the whole manual surface, and they are driven.
