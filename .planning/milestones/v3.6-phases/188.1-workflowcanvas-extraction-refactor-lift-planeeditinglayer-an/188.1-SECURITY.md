# 188.1-SECURITY.md — Phase 188.1 WorkflowCanvas extraction refactor

```yaml
phase: 188.1
phase_name: workflowcanvas-extraction-refactor-lift-planeeditinglayer-an
authored: 2026-08-06
author: gsd-executor (plan 188.1-05, Task 1)
verified: 2026-08-06
verifier: gsd-security-auditor (/gsd:secure-phase 188.1)
register_authored_at_plan_time: true
asvs_level: 1
block_on: high
threats_total: 18
threats_closed: 18
threats_open: 0
threats_accepted: 3
threats_transferred: 1
verdict: SECURED — 18/18 resolved at HEAD 91c37b5c; 0 open, 0 high findings. 4 non-blocking warnings recorded below.
phase_commit_range: 004a6486..HEAD
head_verified_at: 91c37b5c
```

## How to read this file

This register was **authored at plan time** (`register_authored_at_plan_time: true`), mirroring
the shape of `188-SECURITY.md`. `/gsd:secure-phase 188.1` **verified these entries against shipped
source**; it did not re-derive a fresh register from the diff.

The plan-time register spanned **five** `<threat_model>` blocks whose union is **18 distinct IDs**.
The version of this file written at plan close carried only **7 rows** — the other eleven had a
plan-time disposition and no register row, and were therefore OPEN by default. All eighteen are now
present, each with a `file:line` **read out of shipped source at HEAD `91c37b5c`** by the verifier.

A mitigation is CLOSED only when it was *verified*. A re-scan of the plan text is not evidence, and
neither is a summary sentence. Where a plan or summary sentence is the only support for a claim, the
entry says so explicitly.

**⚠ ID COLLISION, RESOLVED.** `T-188.1-01b` named **two different threats**: `188.1-04-PLAN.md`
defines it as *Information disclosure — the CSS `radial-gradient` sink*, while the plan-close version
of this file redefined it as *Denial of service — the render crash in `phaseGlyph` /
`renderPhaseMark`*. **The plan's definition wins** and keeps the id; the DoS finding is renumbered
**`T-188.1-01c`** below so one id never names two threats. Both concerns were verified independently
and both hold.

### Re-derived by the VERIFIER at HEAD `91c37b5c`, not inherited

Every figure in the plan-close version of this table was captured at HEAD `c3e4e5c2` and is a CLAIM.
Each was re-run. **One was stale** and is corrected.

| Measurement | Command | Result at `91c37b5c` |
|---|---|---|
| `WorkflowCanvas.tsx` line count | `wc -l` | **1292** — and the pre-move figure re-derived independently: `git show 004a6486:…\| wc -l` → **1593** ✅ |
| `WorkflowCanvas.tsx` move shape | `git diff --numstat 004a6486..HEAD` | **22 insertions / 323 deletions** ✅ |
| `PlaneEditingLayer.tsx` / `editAffordance.ts` | `wc -l` | **268** / **164** ✅ |
| `PhaseNodeCard.tsx` line count | `wc -l` | **797** ✅ (see T-188.1-11 — the plan's `755` was the phase-START figure; `755` at `004a6486` + `47/−5` = 797) |
| Frontend count gate | `node scripts/vitest-count-gate.cjs` | **`count gate OK` — total 2477 · pinned 2477 · failed 0 · 45/45.** ⚠ **The plan-close table read 2476 and is STALE** — the F-3 regression guard added in plan 05 moved it to 2477, as `188.1-UAT.md` § F-3 itself records. Corrected here. |
| Typecheck | `npx tsc --noEmit -p tsconfig.app.json` | **33 errors**, of which **0** name `WorkflowCanvas`, `PlaneEditingLayer` or `editAffordance` ✅ |
| Lint | `npx eslint src/components/workflows/` | **5 errors** — `BuilderStoreProvider.tsx:45/:53/:78`, `FlowEdge.tsx:147`, `StepTypePicker.test.tsx:561` ✅ (confirms D3's `:135` → `:147` correction) |
| Build | `npx vite build` | **exit 0**; `[INEFFECTIVE_DYNAMIC_IMPORT]` = **2** (confirms `188.1-DEFERRED.md` D-02's correction of the plans' "exactly one"); **circular-dependency warnings = 0** |
| Backend / migration / dependency churn | `git diff --stat 004a6486..HEAD -- backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json` | **0 files changed** (empty output, exit 0) ✅ |
| Deploy-artifact drift | `bash scripts/check-deploy-drift.sh` | **RESULT: PASS**, exit 0 (2 pre-existing non-blocking WARNs, both predating this phase) ✅ |
| House guard spelling | `grep -rn "Object.hasOwn" frontend/src` | **0** ✅ — no second spelling entered the tree |
| `phase_index` occurrences in `WorkflowCanvas.tsx` | `grep -c` | **1** (at `WorkflowCanvas.tsx:541`) ✅ |

### Citation drift found at HEAD `91c37b5c` (reported, not silently fixed)

The plan-close file cited line numbers captured at `c3e4e5c2`. Re-measured:

| Cited as | Actually at HEAD | Note |
|---|---|---|
| `PhaseNodeCard.tsx:738` (the `radial-gradient` sink, per `188.1-04-PLAN.md`) | **`:780`** | drifted +42 |
| `PhaseNodeCard.tsx:396` (the "Total by construction" docblock, per `188.1-04-PLAN.md`) | **`:410-424`** | drifted |
| `backend/app/api/threads.py:1043-1044` (ownership docstring) | **`:1045`** | drifted +1 |
| `WorkflowCanvas.test.tsx:612` (subtree non-empty guard) | **`:613`** | drifted +1 |
| `WorkflowCanvas.test.tsx:655` ("editAffordance imports no component sibling / no React") | **`:673-674`**; `:655-657` is the *back-import* loop | the two assertions were conflated |
| `WorkflowCanvas.test.tsx:676` (leaf non-vacuity control) | **`:677`** | drifted +1 |
| `lib/phaseGlyph.tsx` / `lib/nodePresentation.ts` / `panel/PhaseCard.tsx` | **`frontend/src/lib/phaseGlyph.tsx`**, **`frontend/src/components/workflows/nodePresentation.ts`**, **`frontend/src/components/panel/PhaseCard.tsx`** | the `lib/`-prefixed forms in the plan-close file and in the audit brief resolve to **no file**; `workflows/__tests__/` likewise does not exist — the suites sit **alongside** their subjects in `components/workflows/` |
| `WorkflowCanvas.tsx:845` (`onInsertAt` docblock, quoted **inside shipped source** at `WorkflowCanvas.test.tsx:869`) | **`:541`** | ⚠ a stale citation now living in the shipped tree — see the T-188.1-04b entry |

None of these drifts changes a disposition. All are recorded because this project's standing lesson
(`feedback_dont_inherit_unmeasured_claims`) is that an uncited-but-inherited number is how a false
claim survives a phase — nine were measured FALSE inside 188.1 itself.

---

## Threat register — all 18

| Threat ID | Category | Component | Disposition | Status | Evidence read at HEAD `91c37b5c` |
|---|---|---|---|---|---|
| T-188.1-01 | Tampering (WR-04) | the five prototype-key lookups the canvas subtree performs on an author-supplied discriminator | mitigate | **CLOSED** | All five guarded in the tree's only shipped spelling, `Object.prototype.hasOwnProperty.call(...)`, each read out of source: **site 1** `PhaseNode.tsx:165-167` (`ICON_TINT`, written as a **ternary** so `PhaseNodeCard.test.tsx:373`'s `/ICON_TINT\[/` positive control stays green — the ternary is present and the load-bearing rationale ships at `:160-164`); **site 2** `panel/PhaseCard.tsx:72`; **site 3** `panel/PhaseCard.tsx:137` (`STATUS_META`, which had **no fallback at all** and now lands on the table's own declared `unknown` row — the pre-fix expression `const status = STATUS_META[phase.status]` is visible in the diff); **sites 4 + 5** through one module-private `own()` at `PhaseNodeCard.tsx:307`, called at **`:434`** (`VERDICT_MARK`) and **`:731`** (`RING_STROKE`). Verifier confirmed via `grep -rn "hasOwnProperty.call" frontend/src` that these are the only production guards of this shape besides the pre-existing `runVocabulary.ts:85` and `lib/phaseState.ts:77`. RED-first observation is attested by `188.1-04-SUMMARY.md:127-163` (verbatim failure texts) — **that half rests on a summary, not on re-execution.** |
| T-188.1-01b | Information disclosure | the CSS `radial-gradient` sink fed by the resolved tint | mitigate | **CLOSED** | The sink is `PhaseNodeCard.tsx:780` — `` background: `radial-gradient(circle, ${tint}, transparent 68%)` `` (⚠ cited as `:738` in `188.1-04-PLAN.md`; drifted). Its input is the `tint` **prop** (`PhaseNodeCard.tsx:232-234`), resolved one component away at the guarded `PhaseNode.tsx:165-167`, so no unguarded path reaches the style context. The falsification states the **sink** rather than the lookup: `PhaseNode.test.tsx:698-700` walks *every* `[style]` element in the rendered tree under `phaseType = "constructor"` and asserts none contains the substring `function`, with an ordinary-miss comparison at `:705-714`. |
| T-188.1-01c | Denial of service (hard render crash) | `lib/phaseGlyph.tsx` + `nodePresentation.renderPhaseMark` — two **unregistered** lookups on the *same* author-supplied value | mitigate | **CLOSED — the phase's headline finding; ⚠ RENUMBERED from `01b` to end the id collision** | `data.phaseType` feeds **three** lookups, not one. `phaseGlyph("constructor")` returned the `Object` FUNCTION (truthy, so `?? null` never fired), the caller did `createElement(Object, …)`, and React threw *"Objects are not valid as a React child"* — a whole-node crash, strictly worse than the mis-paint the five registered sites produce. Guarded and read at **`frontend/src/lib/phaseGlyph.tsx:92`** and **`frontend/src/components/workflows/nodePresentation.ts:128-131`**. The pre-fix one-liner `return mark ? createElement(…) : (PHASE_GLYPHS[phaseType] ?? "•")` is visible in the diff. Lesson for the next register: **assert what the DOM does, not what the expression returns** — an expression-level falsification would have found neither. ⚠ See WARNING-1: `phaseGlyph.tsx:7` still carries the uncorrected false-totality sentence. |
| T-188.1-02 | Tampering (request-path reshaping, WR-07) | `api.getWorkflowRun` interpolated an unencoded run id into a fetch path | mitigate | **CLOSED** | `frontend/src/lib/api.ts:3768` — `` fetch(`${API_BASE}/workflow-runs/${encodeURIComponent(runId)}`, …) ``, with the rationale shipped at `:3764-3767`. The falsification asserts the URL the **mocked `fetch` actually received**: `pages/WorkflowRunPage.test.tsx:1718-1725` drives `runId = "a/b"` and asserts the URL contains `a%2Fb` **and** `not.toContain("workflow-runs/a/b")`. |
| T-188.1-02b | Denial of service | `getWorkflowRun`'s unused `signal` parameter | **transfer (deferred, D4)** | **CLOSED — the transfer is DURABLE** | The re-open trigger ships **inside the function's own docblock**, not only in a plan: `frontend/src/lib/api.ts:3753-3757` — *"The unused `signal` parameter is DELIBERATELY left alone (D4) … Re-open trigger: the next phase touching `WorkflowRunPage`'s fetch lifecycle, or a superseded read causing a visible defect."* Recorded as **D4** in `188.1-DEFERRED.md:99-108` with the same trigger. Verifier note, measured rather than assumed: the parameter **is** forwarded to `fetch` at `api.ts:3770`; what is missing is a caller that supplies one — both live call sites (`pages/WorkflowRunPage.tsx:350` and `:389`) pass no signal. So the deferral describes a real, still-open superseded-read window, and it is correctly scoped. |
| **T-188.1-03** | **Information disclosure (UF-1)** | **`last_workflow_run_id` returned by the un-gated `GET /threads/{id}/workflow`** | **accept + register** | **CLOSED — accepted, registered, NO code change; both stated verifier checks re-run and PASSED** | See the full entry below. |
| T-188.1-04 | Repudiation (a fence that narrows in silence) | the `?raw` negative assertions across `WorkflowCanvas.test.tsx` / `.composition.test.tsx` that would have kept passing while covering 311 fewer lines after the move | mitigate | **CLOSED — by measurement, not by argument** | Both suites read a **move-invariant subtree source**: `WorkflowCanvas.test.tsx:67-79` and `WorkflowCanvas.composition.test.tsx:59-69` build `canvasSubtreeSource` from a `CANVAS_SUBTREE_PATHS` list naming all three modules, over an `import.meta.glob("./*.{ts,tsx}", { query: "?raw", eager: true })` sweep. The move-invariant control at `WorkflowCanvas.test.tsx:591-615` asserts the fenced source contains `EDIT_AFFORDANCE = {` **and** `function PlaneEditingLayer(` — true before AND after the move — plus a length pin (`:603`), a glob-non-emptiness pin (`:604`), per-destination `toContain` checks that catch **substitution** where a length pin cannot (`:609-610`), and a resolved-and-non-empty guard at **`:613`**. Mirrored at `composition.test.tsx:412-423`. **The proof is a RED observation** — `grounding_mode` planted inside the moved code turned BOTH fences red (`188.1-03-SUMMARY.md`, RED #1); *that observation rests on the summary, the fence itself was read from source.* |
| T-188.1-04b | Repudiation | `WorkflowCanvas.test.tsx`'s `phase_index` count pin | **accept** | **CLOSED — all three stated checks PASS; ⚠ one stale citation now lives in shipped source** | The pin reads **`workflowCanvasSource`**, not the widened subtree: `WorkflowCanvas.test.tsx:873` — `expect(workflowCanvasSource.match(new RegExp(ORDINAL_FIELD, "g")) ?? []).toHaveLength(1)`. The explicit in-file note is present at **`:868-872`** (*"THE COUNT PIN IS DELIBERATELY NOT WIDENED (188.1-01, T-188.1-04b) … Pointing a count at three files changes what the number means"*), while the two negatives immediately above it (`:866-867`) read the whole subtree — exactly the split the disposition asks for. Verifier re-measured the occurrence itself: `grep -c phase_index frontend/src/components/workflows/WorkflowCanvas.tsx` → **1**, at **`WorkflowCanvas.tsx:541`**, and **0** occurrences in either extracted module — so the prose survived the move outside the moved regions, as claimed. ⚠ **WARNING-2:** the note at `:869` cites that docblock as `WorkflowCanvas.tsx:845`; the 323-line deletion moved it to `:541`. The pin is sound; its shipped citation is stale. |
| T-188.1-05 | Denial of service (a runtime-only ESM cycle) | `PlaneEditingLayer.tsx` / `editAffordance.ts` importing `WorkflowCanvas` back | mitigate | **CLOSED — and independently re-confirmed against the live dev server** | `WorkflowCanvas.test.tsx:634-690`. One regex `IMPORT_FROM_CANVAS` (`:634`) covers static, re-export and `import type` (all end in `from "<specifier>"`); `DYNAMIC_IMPORT_CANVAS` (`:635`) covers `import()`. **Four inline positive controls run first** (`:641-648`) plus non-emptiness of both `?raw` subjects (`:650-651`); the negative loop is `:654-659`. The leaf assertion — `editAffordance` imports no component sibling and no React — is at **`:673-674`** (⚠ cited as `:655`), with its own non-vacuity control at **`:677`** proving the module *does* import `canvasModel`. `:680-690` asserts the edge **exists and points one way** (`import { PlaneEditingLayer } from …`, no local `function PlaneEditingLayer(`, and no `EDIT_AFFORDANCE` re-export shim) — without which three green negatives are also satisfied by two modules nobody imports. Why a test and not a docblock is stated in-file at `:618-633`. **Verifier build:** `npx vite build` exits **0** with **0** circular-dependency warnings. Independently corroborated against the *served* bundle in `188.1-UAT.md:29-37` (`layer_imports_back_into_canvas false`, `affordance_imports_back_into_canvas false`). |
| T-188.1-06 | Tampering (integrity of a shipped promise) | the live canvas on the Builder and run surfaces | mitigate | **CLOSED — both halves. ⚠ THIS MOVED since the plan-close file, which read "PARTIALLY CLOSED — operator half PENDING"** | *Automated half:* `WorkflowCanvas.editing.test.tsx:1234-1343` holds `AFFORDANCE_SHAPE_BASELINE`, a **captured** DOM literal — verifier counted **12 entries** (`grep -c "testid:"` over the literal), matching the claim — asserted deep-equal at **`:1369`** against a render that opens the picker with a real `fireEvent.click` (`:1358`) and uses a non-zero one-card nudge (`:1352`) so `verticalOffsetFor` returns three different numbers. It survived the 323-deletion move **untouched** (`git diff` shows no edit to the literal in `004a6486..HEAD`). *Operator half:* **now driven.** `188.1-UAT.md` records **M1/M2/M3 = 3 PASS, 0 blocked, 0 pending**, each with its own evidence per `feedback_uat_rows_need_their_own_evidence`: M1 from a controlled hover A/B plus `elementFromPoint` hit-tests on the Builder draft; M2's evidence 1-3 from a real pointer drag on that draft and evidence 4 (the 185 seal) from the operator's own run surface, recorded as a separate observation on a separate surface; M3 entirely from the operator's live end-to-end run. **M3 does sample the elapsed clock across time and assert the delta** (`188.1-UAT.md:201-208`): the operator watched the figure *"visibly climb through intermediate values rather than jumping to its final reading"*, with the file naming why a terminal screenshot cannot make that claim — Phase 188's F6 hid at exactly that seam. |
| T-188.1-06b | Repudiation | the capture's own determinism (research assumption A6) | mitigate | **CLOSED** | The capture is sorted by `data-testid` inside the capture function itself, so DOM order cannot leak into the literal: `WorkflowCanvas.editing.test.tsx:1198` — `.sort((a, b) => (a.testid! < b.testid! ? -1 : a.testid! > b.testid! ? 1 : 0))`. The two-agreeing-runs requirement is attested by the shipped docblock at `:1209-1210`; **that half rests on prose, not on re-execution by the verifier.** |
| T-188.1-06c | Spoofing | a deep-equal against an EMPTY capture | mitigate | **CLOSED — the control is present and covers each affordance KIND** | `WorkflowCanvas.editing.test.tsx:1362-1367`, declared **before** the deep-equal at `:1369`: array non-empty (`:1364`), at least one numbered `＋` (`:1365`, regex `^canvas-insert-\d+$` so the picker cannot satisfy it), at least one `✕` (`:1366`), and **exactly one OPEN picker** (`:1367`, `toHaveLength(1)`). This is the control that stops `[] deep-equals []` passing forever, and its in-file rationale says so verbatim. |
| T-188.1-07 | Spoofing | SC#4's "no focusable control inside the card" walk | mitigate | **CLOSED — the editable variant exists and is non-vacuous** | `WorkflowCanvas.test.tsx:339-370`, *"no focusable control exists inside a node WHEN THE EDITING LAYER IS MOUNTED (188.1-01)"*. It renders `renderCanvas(evalCoverage, { editable: true })` at `:347` — the two shipped assertions at `:303-310` and `:325-337` render **read-only**, and `PlaneEditingLayer` mounts only under `editable`, so the invariant genuinely was never enforced against the moved code before. The `[data-testid^="canvas-insert-"]` non-vacuity control is at **`:352-354`** with a sibling `canvas-remove-` control at `:355-357`, both declared **before** the walk; the tab-stop count is pinned at one per node (`:361-363`); the walk itself is `:365-369`. Inversion (`toHaveLength(0)` → fails, *"expected 6 to be +0"*) is attested at `:351` and in `188.1-DEFERRED.md:134`; **the inversion observation rests on those records, the control was read from source.** |
| T-188.1-08 | Tampering | `WorkflowCanvas.tsx` prose edits leaving a fence behind | mitigate | **CLOSED — every negative in BOTH suites reads the three-file subtree** | Verifier enumerated every `not.toMatch` / `not.toContain` in both suites. `WorkflowCanvas.test.tsx`: `:582` `MiniMap`, `:583` `hideAttribution`, `:587` `workflows/validate`, `:588` `grounding_mode`, `:833` the seven `READING_WORDS`, `:843-844` the runVocabulary **value**-import shape, `:849` `@/lib/phaseState`, `:866-867` the ordinal in stripped CODE — **all eight on `canvasSubtreeSource`**. `WorkflowCanvas.composition.test.tsx`: `:397`, `:398`, `:402`, `:403` and the severity fence at `:431-438` — all on `canvasSubtreeSource`, the last over a comment-stripped copy with positive controls at `:442-444` proving the stripper did not silently empty the subject. The only two `workflowCanvasSource` readers left are **positive** assertions (`:578` / `:405`) and the per-file count pin (T-188.1-04b) — which is the declared, deliberate exception, not an oversight. |
| T-188.1-09 | Elevation of privilege | red line D-14 — the canvas as a pure projection | **accept** | **CLOSED — the mechanical fence behind the accept covers BOTH new modules** | The two negatives that stop the extracted layer acquiring run-state derivation read the **subtree**, not the shell: `WorkflowCanvas.test.tsx:843-845` forbids a **value** import of `@/components/workflows/runVocabulary` anywhere in the subtree, and **`:849`** forbids `@/lib/phaseState` in **any** form — with the in-file rationale at `:846-848` naming D-14 explicitly (*"an extracted module that grew this import would otherwise be invisible here"*). Both regexes carry positive controls at `:851-856`. The shell's own type-only import is separately pinned at `:839-841` on `workflowCanvasSource`, which is correct — a positive claim is about one file. Verifier confirmed neither `PlaneEditingLayer.tsx` nor `editAffordance.ts` imports either module. |
| T-188.1-10 | Repudiation | docblocks claiming a totality the expression does not provide | mitigate | **CLOSED against its declared scope — with one residual (WARNING-1)** | The **registered** subject — `PhaseNodeCard.tsx`'s *"Total by construction"* — is corrected at **`:410-424`** (⚠ cited as `:396`), and correctly **paraphrased rather than quoted**, per the D9 lesson that a quoted correction fails the acceptance grep that asks whether the claim still exists. Verifier counted the corrections actually shipped: **(1)** `PhaseNodeCard.tsx:410-424`; **(2)** `lib/phaseGlyph.tsx:78-90`, which corrects the inline comment and states that the header `@returns` was false for the same reason — the `@returns` at `:74-75` is now TRUE of the guarded function; **(3)** `nodePresentation.ts` header ⚠ paragraph plus the inline correction at `:116-127`; **(4)+(5)** `panel/PhaseCard.tsx` — **two** ⚠ qualifications, one per guarded lookup, each opening *"TOTAL: anything this table does not OWN reads as …"* and then naming what the old expression really did. So **five** correction sites ship, not four; the plan-close file's "four" undercounts. ⚠ **Residual:** `frontend/src/lib/phaseGlyph.tsx:7` still reads *"returning `PHASE_GLYPH_MARKS[phaseType] ?? null` (total over any key…)"* — a sentence that both repeats the false totality claim and describes an expression the file no longer contains. Documentation-only; see WARNING-1. |
| T-188.1-11 | Repudiation | the G-5 hot-file ledger in `CLAUDE.md` | mitigate | **CLOSED — and the 755-vs-797 conflict RESOLVED BY MEASUREMENT: 797 is right** | `CLAUDE.md:195` — the `WorkflowCanvas.tsx` row reads **satisfied (188.1 — 2026-08-06)** and carries **1593 → 1292**, the corrected touch history (**13 plans across 6 phases**, with Phase 187 restored), and the extracted modules' own sizes. `CLAUDE.md:196` — the `PhaseNodeCard.tsx` row reads **G-5 fires — extraction due** and **797 lines**, with the instruction that Phase 189's discuss-phase must produce that refactor recommendation FIRST. **Verifier re-measured all four numbers rather than inheriting either:** `wc -l WorkflowCanvas.tsx` → 1292; `git show 004a6486:… \| wc -l` → 1593; `wc -l PhaseNodeCard.tsx` → **797**; `git show 004a6486:PhaseNodeCard.tsx \| wc -l` → **755** with `git diff --numstat` → **47 / 5**, and 755 + 47 − 5 = **797**. **`188.1-05-PLAN.md:330`'s "755" is a phase-START figure that the plan's own execution then invalidated**; `CLAUDE.md` and this register are correct. `188.1-DEFERRED.md:257` reaches the same conclusion independently. |
| T-188.1-12 | Repudiation | a UAT board that records only what passed | mitigate | **CLOSED** | `188.1-UAT.md` frontmatter declares `rows_total: 3 / rows_passed: 3 / rows_blocked: 0`, and the scoreboard at `:280-289` carries **one row per declared row** with a result, a driver, and an explicit *"evidence is its own"* column — no row is silently omitted, and the file states in terms that *"a PASS observed while driving a different row is not a driven row"* (`:15-16`). Failures are recorded rather than dropped: **four** findings are dispositioned in the same file — F-1 (`:97-120`, measured **pre-existing**, filed as **BUG-260806-01** with a re-open trigger), F-2 (`:122-136`, shipped behaviour; the ROW'S OWN stated expectation is recorded as having been wrong), F-3 (`:221-237`, **found and FIXED in-phase**), F-4 (`:239-253`, deliberately out of scope under G-2). The one un-exercised branch (unrecognised wire status) is named at `:214-219` as a residual note with its reason, not omitted. The SC#10 8-provider roster is recorded at `:257-274` as a **DECISION scoped out on measured evidence** (0-file backend diff), not as an absent board. |
| T-188.1-SC | Tampering (supply chain) | npm / pip / cargo installs | mitigate | **CLOSED — RE-MEASURED BY THE VERIFIER** | `git diff --stat 004a6486..HEAD -- frontend/package.json frontend/package-lock.json backend/ supabase/migrations/` → **empty output, exit 0 — 0 files changed**; `git status --porcelain` on the same paths → clean; `bash scripts/check-deploy-drift.sh` → **RESULT: PASS**, exit 0. **Zero packages installed across the whole phase** — every symbol the extracted modules use was already imported by `WorkflowCanvas.tsx`. No new dependency was required, proposed, or permitted. |

**Disposition tally:** 14 `mitigate` · 3 `accept` (T-188.1-03, -04b, -09) · 1 `transfer` (T-188.1-02b) = **18**.

---

## T-188.1-03 (UF-1) — the accept-and-register entry, in full

**The finding, verbatim from `188-SECURITY.md` § UF-1 and confirmed still present at HEAD `91c37b5c`:**

`last_workflow_run_id: UUID | None` is declared on `ThreadWorkflowState` at
`backend/app/models/thread.py:115` and populated at `backend/app/api/threads.py:1267`, shipping on
`GET /threads/{thread_id}/workflow` (`backend/app/api/threads.py:1036`). That path is **not** a
member of `CANVAS_GATED_PATHS` (`backend/app/middleware/canvas_gate.py:85-93`, whose three members
are `/workflows/validate`, `/workflows/grounding-bundle` and `/workflow-runs/{workflow_run_id}`). So
with `visual_workflow_canvas` OFF, an owner can still learn that a `workflow_runs` row exists for
their own thread and obtain its id, even though `GET /workflow-runs/{id}` answers 404.

**Disposition: `accept` + register. Assessed LOW.**

1. The route is **ownership-gated first** — its own docstring at `backend/app/api/threads.py:1045`
   states *"Ownership-gated FIRST (T-092-04 — 404, never leak existence)"* (⚠ previously cited as
   `:1043-1044`; drifted).
2. The id disclosed is the **caller's own**.
3. The sibling fields `latest_producer_run_id` and `phases` on the *same response model*
   (`backend/app/models/thread.py:100`, `:116+`) already disclosed workflow-run existence through
   this endpoint **before** Phase 188. No *new class* of information crosses the boundary.
4. The field is additive, optional, and sourced from a value the handler had already computed.

**NO CODE CHANGE was made, and that is a phase gate, not a preference.** SC#6 asks for
REGISTRATION; re-gating is D7 (`188.1-DEFERRED.md:142-154`).

```
$ git diff --stat 004a6486..HEAD -- backend/ supabase/migrations/ frontend/package.json frontend/package-lock.json
(0 files changed — empty output, exit 0)      ← re-run by the verifier at 91c37b5c
```

**Why re-gating is deferred (D7), with its trigger.** Adding `/threads/{thread_id}/workflow` to
`CANVAS_GATED_PATHS` is a **backend behaviour change on an endpoint the client depends on** — the
frontend calls `getThreadWorkflow` on every (re)connect and thread switch to reconcile the
per-thread lock and Continue affordance. Gating it would 404 a read the pre-canvas product served.
**Re-open trigger:** the endpoint gains a field disclosing something the pre-canvas product did not
expose, **or** the visibility model changes.

### The two checks this entry told a verifier to run rather than take on trust — BOTH RE-RUN, BOTH PASS

| Check | Result at HEAD `91c37b5c` |
|---|---|
| *"the field is still populated from `phases_source_run_id` (an already-resolved value) and not from a second query"* | **PASS.** `backend/app/api/threads.py:1267-1271` reads `last_workflow_run_id=(UUID(phases_source_run_id) if isinstance(phases_source_run_id, str) else phases_source_run_id)` — the same local that sourced `phases`. The inline comment at `:1261-1266` states *"literally the value that sourced `phases` above; nothing new is queried and nothing is written"*, and the model docblock at `thread.py:101-104` says the same. No new `.execute()` / `select` appears between the resolution and the assignment. |
| *"`CANVAS_GATED_PATHS` still has exactly three members"* | **PASS — exactly three.** `backend/app/middleware/canvas_gate.py:85-93`: `/workflows/validate`, `/workflows/grounding-bundle`, `/workflow-runs/{workflow_run_id}`. No member added or removed in this phase (0-file backend diff). |

---

## Carried-forward notes from `188-SECURITY.md`

| Item | Status after 188.1, re-verified at `91c37b5c` |
|---|---|
| **UF-1** | **Registered here as T-188.1-03** (accept). Its instruction was *"Register it in the next phase touching this file"* — this is that phase's register, and the endpoint was not touched. |
| **UF-2 (WR-04)** | **Closed** — see T-188.1-01 and T-188.1-01c. Its instruction was *"Route all five through `own()` / `Object.hasOwn` in the next phase touching these files, and correct the two docblocks that currently claim a totality the expression does not provide."* Both halves done; the docblock half **exceeded** — the verifier counted **five** correction sites, not two or four (T-188.1-10). ⚠ One spelling correction to `188-SECURITY.md`'s instruction: the house form is `Object.prototype.hasOwnProperty.call(...)`, and `Object.hasOwn` still has **zero** occurrences in `frontend/src` (re-measured) — a second spelling was deliberately not introduced. |
| **UF-3 (WR-07)** | **Closed** — see T-188.1-02, with its unclosed half transferred as T-188.1-02b / D4. |
| **UF-4 (WR-03)** | **Untouched.** `ChatLayout.tsx` navigation after a failed thread lookup is outside this phase's declared blast radius; it is neither closed nor re-assessed here. |
| **UF-5** | ⚠ **RECURRED — the plan-close claim was FALSE.** That file stated *"all five 188.1 summaries carry a `## Threat Flags` section"*. Measured: `188.1-05-SUMMARY.md` contains **zero** occurrences of the string "Threat" and has no such section (its headings are `What this plan did` / `Every number re-measured…` / `Plan-inherited claims found FALSE` / `The operator board` / `Four findings…` / `Deviations` / `Self-Check`). Plans 01-04 do carry it. See WARNING-3. |
| **RD-1 / RD-2 / RD-3** | Rationale-drift findings in Phase 188's own files. Not re-audited here — this phase changed no backend line (0-file backend diff, re-measured), so none of them can have moved. |
| **An EIGHTH prototype-key site** | `frontend/src/lib/providerLogo.tsx:107-108` is byte-for-byte the `phaseGlyph` defect, false *"total over any key"* comment included — **verifier confirmed it is still present and unfixed at HEAD**. **Logged, not fixed** — D-188.1-DEF-02, trigger in `188.1-DEFERRED.md:214-239`. Correctly scoped out: `provider` comes from the model registry, not from author-supplied definition JSONB. |

---

## Threat flags raised by this phase

**None registered by the executor** — plans 01-04 each declare `## Threat Flags: None`, and the
verifier confirmed the substance of those declarations independently: this phase created no network
endpoint, no auth path, no file-access pattern and no schema change (`git diff --stat 004a6486..HEAD
-- backend/ supabase/migrations/` → 0 files changed). It relocated a presentational layer inside the
client, narrowed one client request path, and made eight client-side lookups total.

**One unregistered flag, informational (see WARNING-3):** plan 05 shipped a **production source
change** — `frontend/src/pages/WorkflowRunPage.tsx`, the F-3 duplicate-status fix — with no
`## Threat Flags` section in its summary to declare it. The verifier read the change: the run band
becomes `sr-only`, the `aria-live="polite"` announcement channel is **preserved** (deliberately not
deleted, with the reasoning shipped in the JSX comment), and the previously-visible `aria-hidden`
elapsed sibling is removed as unreachable markup. It is presentation-only, adds no data flow, no
input handling and no new surface. Guarded by a regression test asserting exactly one *visible*
status carrier at `frontend/src/pages/WorkflowRunPage.test.tsx:729-742`. **Not a blocker**; recorded
so the absent section is auditable rather than invisible.

---

## Non-blocking warnings

| # | Finding | Where | Why it is not a blocker | Suggested owner |
|---|---|---|---|---|
| **WARNING-1** | A false-totality docblock claim survives the phase that existed partly to correct them. `frontend/src/lib/phaseGlyph.tsx:7` still reads *"returning `PHASE_GLYPH_MARKS[phaseType] ?? null` (**total over any key**; caller renders a unicode fallback on null)"* — a sentence that repeats the exact claim `:78-90` corrects **and** describes an expression the file no longer contains (the shipped function own-guards at `:92`). | `frontend/src/lib/phaseGlyph.tsx:7` | Documentation only. The **behaviour** is correct and fenced — T-188.1-01c is closed on the guard at `:92`, not on the prose. No user-reachable consequence. | ≤ 1 file / ≤ 3 lines → **`/gsd:fast` under G-3**, or free-ride on the D-188.1-DEF-02 fix (which touches the sibling `providerLogo.tsx` carrying the identical sentence). |
| **WARNING-2** | A stale `file:line` citation now lives in shipped source: `WorkflowCanvas.test.tsx:869` cites the sole `phase_index` prose mention as `WorkflowCanvas.tsx:845`; the 323-line deletion moved it to **`:541`**. | `frontend/src/components/workflows/WorkflowCanvas.test.tsx:869` | The **pin** is correct and was re-measured green (count = 1). Only the pointer in its explanatory comment drifted. | Free-ride on the next edit to that suite. Never its own plan (G-3). |
| **WARNING-3** | `188.1-05-SUMMARY.md` carries **no `## Threat Flags` section at all** — the same process gap `188-SECURITY.md` logged as UF-5 against `188-01-SUMMARY.md`, recurring one phase later. The plan-close version of this register asserted the opposite. It matters more here than in plans 01-04 because plan 05 is the **only** one of the five that shipped an undeclared production source change (`WorkflowRunPage.tsx`). | `188.1-05-SUMMARY.md` | The verifier assessed the change directly and found no new attack surface (see § Threat flags). A missing declaration is a **repudiation/process** gap, not an exploitable one. | The executor template: make `## Threat Flags` a hard section gate in `execute-phase`, so an omission fails rather than reads as "None". |
| **WARNING-4** | Two independently-measured facts in the plan-close register were **already stale one commit later**: the count-gate total (**2476** → the true **2477** after the F-3 guard) and the collision-bearing use of `T-188.1-01b` for two different threats. | `188.1-SECURITY.md` (now corrected) | Corrected in this rewrite. Recorded because it is the phase's own headline lesson recurring inside the phase's own security file: **a measurement is only true of the commit that produced it.** | n/a — closed by this rewrite. |

**None of the four meets `block_on: high`.** All are documentation / process; none changes a
disposition, and none is user-reachable.

---

## What a future verifier should NOT take on trust from this file

Four sub-claims below are supported by a SUMMARY sentence rather than by re-execution, and are
labelled as such in their rows. They are the honest edges of this audit:

1. **The RED-first observations** for the six 188.1-04 falsifications (T-188.1-01) rest on
   `188.1-04-SUMMARY.md:127-163`, which records the failure texts verbatim. The verifier read the
   guards and the falsifications out of source but did not revert the tree to re-observe RED.
2. **The `grounding_mode` RED** that proved the subtree fence (T-188.1-04) rests on
   `188.1-03-SUMMARY.md` RED #1.
3. **The two-agreeing-runs capture discipline** behind `AFFORDANCE_SHAPE_BASELINE` (T-188.1-06b)
   rests on the shipped docblock at `WorkflowCanvas.editing.test.tsx:1209-1210`.
4. **The inversion** proving the SC#4 editable control non-vacuous (T-188.1-07) rests on
   `WorkflowCanvas.test.tsx:351` and `188.1-DEFERRED.md:134` (*"expected 6 to be +0"*).

Everything else in this register was read out of shipped source or re-executed at HEAD `91c37b5c`.

---

## Verdict

**SECURED — 18 of 18 resolved at HEAD `91c37b5c`: 14 mitigated-and-verified, 3 accepted-and-registered
(T-188.1-03, -04b, -09), 1 transferred with a durable in-source trigger (T-188.1-02b), 0 open.**

The plan-close register's one PENDING entry has **moved**: T-188.1-06's operator half is now closed —
`188.1-UAT.md` carries **3/3 driven rows, 0 pending, 0 blocked**, each with evidence of its own, and
M3 asserts the elapsed clock's delta ACROSS samples rather than at one.

Four non-blocking warnings are recorded above; none meets `block_on: high`, and none is
user-reachable. `/gsd:secure-phase 188.1` verified this file against shipped source; it did not
re-derive one.
