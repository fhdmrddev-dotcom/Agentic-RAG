---
phase: 188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an
plan: 05
subsystem: docs + frontend
tags: [g-5-ledger, uf-1, security-register, deferred, operator-uat, checkpoint, run-surface, a11y, fast-fix]

# Dependency graph
requires:
  - phase: 188.1
    plan: 03
    provides: "the extraction whose result the ledger row is flipped to describe — 1593 → 1292 L"
  - phase: 188.1
    plan: 04
    provides: "the settled tree the closing battery was measured against — gate 2476/2476, tsc 33, eslint 5"
provides:
  - "The `WorkflowCanvas.tsx` G-5 ledger row flipped to `satisfied (188.1 — 2026-08-06)` with re-measured before/after counts"
  - "TWO stale ledger figures corrected: 1574 → measured 1593 (before), 478 → measured 797"
  - "The `PhaseNodeCard.tsx` row hardened to **G-5 fires — extraction due** by explicit operator direction — binds Phase 189's discuss-phase"
  - "`188.1-SECURITY.md` — the pre-authored register incl. T-188.1-03 (UF-1, accept + register, zero backend change)"
  - "`188.1-DEFERRED.md` — every deferral with a concrete re-open trigger; D-02 pinned by the MEASURED `[INEFFECTIVE_DYNAMIC_IMPORT]` count"
  - "`188.1-UAT.md` — 3/3 operator rows DRIVEN, 0 blocked, 0 pending, each with its own evidence"
  - "F-3 fixed: the run surface no longer renders its status twice (4f9d26d5), regression guard observed RED first"
  - "BUG-260806-01 filed — a selected card occludes its own ✕ (measured PRE-EXISTING)"
  - "SEED-139 planted — workflow-run threads carry no type in the chat list"
affects: [189, secure-phase 188.1]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A CSS `:hover` reveal CAN be driven without a real click: move the physical pointer, then read computed opacity. The controlled off/on A/B is stronger evidence than a screenshot and does not wedge on `take_screenshot`"
    - "`document.elementFromPoint` at a control's own centre is the machine-checkable form of REACHABILITY — it catches the occlusion class that `.click()` and jsdom are both blind to by construction"
    - "Freshness after a module split is provable by fetching the modules from the dev server and asserting the moved symbols are absent from the source and present in the destination — better than asserting a hard reload happened"
    - "A test that pins a duplicate as a contract must MOVE with the fix, not be deleted: keep the assertion's intent (the clock is visible, never announced) and re-point it at the surviving home"
    - "A regression guard must be falsified against the reverted fix, not merely observed green — the sibling test here passed either way and guarded nothing"

key-files:
  created:
    - ".planning/phases/188.1-.../188.1-SECURITY.md"
    - ".planning/phases/188.1-.../188.1-DEFERRED.md"
    - ".planning/phases/188.1-.../188.1-UAT.md"
    - ".planning/reported-bugs/BUG-260806-01-selected-card-occludes-its-own-remove-control.md"
    - ".planning/seeds/SEED-139-workflow-run-threads-indistinguishable-in-chat.md"
  modified:
    - "CLAUDE.md"
    - "frontend/src/pages/WorkflowRunPage.tsx"
    - "frontend/src/pages/WorkflowRunPage.test.tsx"
    - "scripts/vitest-count-gate.cjs"
---

# 188.1-05 — Close the phase

## What this plan did

Task 1 flipped the ledger row this phase exists to flip, registered UF-1 without
touching the endpoint, and pinned the deferred debt. Task 2 was a blocking
human-verify checkpoint: three operator rows on surfaces jsdom structurally
cannot see.

## Every number re-measured, with the command that produced it

| Measurement | Command | Result |
|---|---|---|
| Canvas line count | `wc -l .../WorkflowCanvas.tsx` | **1292** (pre-move 1593) |
| Card line count | `wc -l .../PhaseNodeCard.tsx` | **797** |
| Extracted modules | `wc -l PlaneEditingLayer.tsx editAffordance.ts` | **268** / **164** |
| Count gate (close) | `node scripts/vitest-count-gate.cjs` | `count gate OK` — **2477 / 2477**, 45/45, failed **0** |
| Typecheck | `npx tsc --noEmit -p tsconfig.app.json` | **33** (baseline held all phase) |
| Lint | `npx eslint src/components/workflows/` | **5** (was 6 — D-01's win, landed 188.1-03) |
| Build | `npx vite build` | exit **0**, `[INEFFECTIVE_DYNAMIC_IMPORT]` **2** |
| UF-1 gate | `git diff --stat -- backend/ supabase/migrations/ frontend/package.json` | **0 files changed** |
| Deploy drift | `bash scripts/check-deploy-drift.sh` | **PASS**, exit 0 |

## Plan-inherited claims found FALSE (five in this plan; nine across the phase)

1. The ledger's touch list was missing **Phase 187**, not just 188/188.1 — measured
   **13 plans across 6 phases** (`187-08` and `187-27` were never recorded).
2. `PhaseNodeCard.tsx` is **797 L**, not the plan's 755 — 755 was accurate at phase
   *start*; `188.1-04` then added +47/−5. A phase-start figure is not a phase-close figure.
3. `[INEFFECTIVE_DYNAMIC_IMPORT]` is **2**, not "exactly one" — the fourth consecutive
   plan to carry that stale number. Pinned at the measured value.
4. The research's own claim that **"no `WorkflowCanvas-*.js` chunk exists at all"** is
   false: `dist/assets/WorkflowCanvas-B9QZZNad.js` exists at **151 bytes**, a re-export
   shim. The substantive point (the module body is not code-split) survives.
5. `FlowEdge.tsx`'s eslint error is at **`:147`**, not the `:135` CONTEXT names.

## The operator board — 3/3 driven

Freshness was proven **mechanically** rather than by asserting a keypress: the dev
server's `WorkflowCanvas.tsx` no longer defines `EDIT_AFFORDANCE` or
`PlaneEditingLayer`, both new modules do, and **neither imports back** — which
re-confirms Wave 3's ESM-cycle fence against the running app, not against a test.

- **M1 PASS** — hover reveal driven as a controlled A/B (off-canvas `0,0,0,0,0` →
  on-plane `1,1,1,1,1`, with `.group/canvas :hover` false → true); 5/5 reachable by
  `elementFromPoint`; insert landed at the promised index; remove restored; picker
  held **300 px / `transform: none`** at zoom 1.69 (the counter-scale, matching
  `188.1-02`'s `PICKER_WIDTH`); implied base size **26.0** matching `INSERT_SIZE`.
- **M2 PASS** — vertical drag moved the card **+76 y** and its `✕` **+76 y** (no
  stranded control — the defect the operator filed once before) while the neighbouring
  `＋` moved **+38 y**, exactly half: `verticalOffsetFor` computing the slanted-connector
  midpoint from its new module. ⌥ Technical names swapped the **subtitle**, not the
  title. The 185 seal confirmed **top-right** on the operator's run surface.
- **M3 PASS** — operator-driven end-to-end run. The elapsed figure was confirmed to
  **climb through intermediate values** rather than jump to its final reading; statuses
  advanced; `compliance-gap-report.docx` (38.1 KB) produced.

## Four findings, dispositioned differently on purpose

| | Finding | Disposition |
|---|---|---|
| F-1 | A **selected** card (xyflow `z-index: 1000`) occludes its own `✕`, which renders in `.react-flow__viewport-portal` at `z-index: auto`. Invisible to `.click()` and to jsdom. | **PRE-EXISTING** — pre-move and post-move both render through `<ViewportPortal>` with no `zIndex` in either, and the affordance baseline still deep-equals. Filed as **BUG-260806-01**, major. |
| F-2 | Affordances scale with zoom (43.9 px at 1.69× → 12.3 px at 0.472×) rather than holding constant size; only the picker counter-scales. | Shipped behaviour, not a regression. The row's stated expectation appears wrong about the affordances. Recorded. |
| F-3 | The run surface rendered its status **twice** — `band.sentence` at `:888` (header) and `:921` (polite region), both `shrink-0` so neither ever scrolled away. | **FIXED** under G-3 (`4f9d26d5`). Polite region → `sr-only`; a11y behaviour unchanged. Guard observed RED first. |
| F-4 | Dead space above the spine — `fitView` `padding: 0.1` / `maxZoom: 2` against a `flex-1` container, for a graph that is inherently wide and short. | **NOT fixed, deliberately.** A layout decision wanting a sketch under G-2, and out of a refactor phase's charter. |

## Deviations

- **Operator hardened the `PhaseNodeCard.tsx` ledger cell** from the measured
  `G-5 THRESHOLD CROSSED` to **`G-5 fires — extraction due`** (`fa039060`). This makes
  `grep -c "G-5 fires"` read **3**, where Task 1's acceptance criterion required it to
  end one *lower* (3 → 2). The criterion's intent — that the `WorkflowCanvas.tsx` row
  stop firing — holds (`satisfied (188.1 — 2026-08-06)`). A newly measured firing row is
  an operator decision superseding a mechanical count, and the reasoning is recorded in
  the commit rather than left to look like drift.
- **F-3 was fixed inside this plan**, which is not in the plan's task list. Justified as
  a G-3 defect fix (1 source file, ~3 render lines, no schema or API), reported by the
  operator against the very surface M3 was driving. It adds **no capability** — it removes
  a duplicate — so it does not breach the refactor charter. F-4, which *would* have been
  new surface, was explicitly left alone.
- **A test moved with the fix.** The one assertion pinning the now-removed aria-hidden
  sibling encoded the duplicate as a contract; it now asserts the clock is still visible
  (in the header, via `run-elapsed`) and still outside the polite region — intent kept,
  location updated. A **new** guard counts on-screen renderings, because the old one
  passed whether or not the band was visible and therefore guarded nothing.

## Self-Check: PASSED

- Ledger: `satisfied (188.1` ×1 · `1574 L|478 L` ×0 · `third badge` ×2 · `focusable control` ×1
- `188.1-SECURITY.md` contains `last_workflow_run_id`, an explicit `accept` for T-188.1-03,
  and `register_authored_at_plan_time: true`
- `188.1-DEFERRED.md` carries a `Re-open trigger` per item incl. the literal
  `INEFFECTIVE_DYNAMIC_IMPORT` count
- `188.1-UAT.md`: 3 rows, none `[pending]`, no row's evidence references another row;
  SC#10 non-applicability recorded as a DECISION with `git diff --stat -- backend/` = 0
- Gate 2477/2477 · tsc 33 · eslint 5 · build exit 0 · deploy-drift PASS · backend diff 0 files
