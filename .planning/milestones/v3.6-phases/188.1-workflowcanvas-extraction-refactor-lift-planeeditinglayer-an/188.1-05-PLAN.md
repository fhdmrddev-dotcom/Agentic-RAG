---
phase: 188.1
plan: 05
type: execute
wave: 5
depends_on: ["188.1-03", "188.1-04"]
files_modified:
  - CLAUDE.md
  - .planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-SECURITY.md
  - .planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-DEFERRED.md
  - .planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-UAT.md
autonomous: false
requirements: []
# EMPTY BY DESIGN — refactor phase, no REQ- ids (ROADMAP §188.1).
success_criteria: ["SC#1", "SC#2", "SC#6"]
gap_closure: false

must_haves:
  truths:
    - "The CLAUDE.md hot-file ledger row for `WorkflowCanvas.tsx` reads *satisfied* rather than *G-5 fires*, and carries the MEASURED before/after line counts — the before figure is 1593, correcting the ledger's own stale 1574 — so Phase 189 opens against an honest ledger"
    - "UF-1 is registered: `last_workflow_run_id` on the un-gated `GET /threads/{id}/workflow` has a durable threat-model entry with an explicit accept-and-register disposition, and NO endpoint was changed"
    - "Per D-02 the `WorkflowRunPage` static-import debt is deferred rather than fixed, and is pinned by a recorded `[INEFFECTIVE_DYNAMIC_IMPORT]` warning count with a concrete re-open trigger — so the debt stays visible and cannot get worse under cover of this phase"
    - "Every deferred item this phase touched carries a concrete re-open trigger in a durable file, not in a research document that expires"
    - "The three operator rows M1-M3 were each DRIVEN with their own evidence — a PASS inherited from another row is not a driven row — and the canvas was confirmed unchanged on the surfaces jsdom structurally cannot see"
  artifacts:
    - path: "CLAUDE.md"
      provides: "the flipped G-5 ledger row with measured numbers"
      contains: "satisfied (188.1"
    - path: ".planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-SECURITY.md"
      provides: "the pre-authored threat register including T-188.1-03 / UF-1"
      contains: "last_workflow_run_id"
    - path: ".planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-DEFERRED.md"
      provides: "D1-D9 plus the D-02 lazy-import pin, each with a re-open trigger"
      contains: "INEFFECTIVE_DYNAMIC_IMPORT"
    - path: ".planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-UAT.md"
      provides: "the three driven operator rows with per-row evidence"
      contains: "M1"
  key_links:
    - from: "CLAUDE.md hot-file ledger"
      to: "Phase 189's discuss-phase G-5 audit"
      via: "the row a future orchestrator scans files_modified against"
      pattern: "WorkflowCanvas.tsx"
---

<objective>
Close the phase: flip the ledger row that this phase exists to flip, register UF-1, pin the
deferred debt, and drive the three operator rows.

Purpose: SC#1's third clause is a documentation deliverable — *"the ledger row flips from
G-5 fires to satisfied"* — and it is the one that changes what Phase 189 is allowed to do.
SC#6's third fix is likewise documentation: UF-1 is registered, never re-gated. And SC#2's
hardest half cannot be automated at all: jsdom applies no CSS, so `pointer-events`, the
hover reveal and the zoom counter-scale are structurally invisible to every test in this
phase. `WorkflowCanvas.tsx:390-410` records that this exact blindness once shipped an
unreachable `＋` while every unit test passed. Three operator rows are not ceremony here;
they are the only instrument that can see that defect class.

Output: a flipped and corrected ledger, `188.1-SECURITY.md`, `188.1-DEFERRED.md`, and a
driven `188.1-UAT.md`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-CONTEXT.md
@.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-VALIDATION.md
@.planning/phases/188-non-technical-run-observability/188-SECURITY.md

<interfaces>
<!-- MEASURED at HEAD `18267202`. Re-derive the post-move numbers from the tree, never from
     a plan or summary — three of the five figures in the ledger today are already stale. -->

THE LEDGER ROWS TO EDIT (`CLAUDE.md` § Workflow guardrails, "Hot-file ledger"):

  `frontend/src/components/workflows/WorkflowCanvas.tsx` — currently reads
    "183 (×3 plans) / 184 (×3) / 185 (185-01, 185-08, 185-10) — 9 plans across 3 phases,
     **1574 L**, now the largest workflow file | **G-5 fires — extraction due in Phase 188.**"
  Two corrections are owed, not one: the row must flip to *satisfied*, AND its 1574 L is
  stale — the measured pre-move figure is **1593 L**. Also add Phase 188 and 188.1 to the
  touch list, which the row does not yet name.

  `frontend/src/components/workflows/PhaseNodeCard.tsx` — the row says **478 L**; measured
  **755 L**. Correct it while the file is open; a ledger with a 58%-wrong number is not a
  ledger a future G-5 audit can lean on.

  ⚠ Preserve everything else in both rows verbatim — in particular `PhaseNodeCard.tsx`'s two
  hard invariants (a third badge is a typecheck error; no focusable control may live inside
  the card) and the `WorkflowCanvas.tsx` row's ESM-cycle note. This phase PROVED the cycle
  note rather than retiring it: it is now enforced by a fence in `WorkflowCanvas.test.tsx`,
  and the row should say so.

UF-1, verbatim from `188-SECURITY.md` and the ROADMAP: `last_workflow_run_id` is returned by
the un-gated `GET /threads/{id}/workflow`. Assessed **low** — the route is ownership-gated,
and sibling fields already disclosed run existence before Phase 188. SC#6 asks for
REGISTRATION, not re-gating. Gating it behind `CANVAS_GATED_PATHS` is D7, deferred: a backend
behaviour change on an endpoint the client depends on. `git diff --stat -- backend/` must
report **0 files changed** at phase close, and that is a gate.

D-02, from `188.1-CONTEXT.md`: do NOT convert `WorkflowRunPage.tsx`'s static `WorkflowCanvas`
import to `lazy()`. `ChatLayout.tsx` uses no `lazy()` anywhere, so the conversion needs a new
`<Suspense>` fallback — a visible loading state, which violates SC#2. Pin the current
`[INEFFECTIVE_DYNAMIC_IMPORT]` warning count instead. Measured before this phase: `vite build`
exit 0, exactly one such warning, `index-*.js` **2,041.23 kB** (gzip 522.90), and **no**
`WorkflowCanvas-*.js` chunk exists — which also makes `185-10-SUMMARY.md:328`'s "canvas still
its own lazy chunk (195.49 kB)" false, and that correction belongs in the deferred record.

THE NINE DEFERRED ITEMS to carry forward with their triggers are enumerated in
`188.1-RESEARCH.md` § "Deferred / out of scope" (D1-D9). D8 (the mis-placed `insertPointX`
docblock) and D9 (the stale gate comments) may already have been discharged by Plans 02-03 —
record their actual disposition rather than copying the research row.

THE THREE OPERATOR ROWS (`188.1-VALIDATION.md` § Manual-Only Verifications). This is a
refactor: prove *unchanged*, do NOT re-drive the Phase 188 board, and the SC#10 8-provider
roster does NOT apply — no provider, streaming, agent-loop or routing code was touched, and
`git diff --stat -- backend/` = 0 files is the mechanical evidence. Record that determination
in the UAT file as a DECISION, so a later reader can tell "scoped out on evidence" from
"nobody ran it".

  ⚠ HARD RELOAD before every row. Splitting a module mid-session is exactly the case where
  Vite HMR serves stale code (`reference_hmr_provider_change_stale_code`).
  ⚠ The OPERATOR drives the browser (`feedback_ask_dont_pull_browser_telemetry`). If Chrome
  MCP is used, read DOM geometry via `evaluate_script` — `take_screenshot` times out
  repeatedly on this machine (`feedback_chrome_mcp_testing`).
  ⚠ A row with no key configured or blocked by a defect is recorded ⛔ WITH ITS REASON, never
  silently omitted. M1 and M2 are pure-frontend; M3 additionally needs the backend, which the
  operator starts (`feedback_user_starts_backend` — never `run_in_background`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip and correct the ledger, register UF-1, and pin the deferred debt</name>

  <files>
CLAUDE.md
.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-SECURITY.md
.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-DEFERRED.md
  </files>

  <read_first>
- `CLAUDE.md` § "Workflow guardrails (MANDATORY)" — the whole hot-file ledger table, plus
  G-5 and G-7, so the flipped row is written in the table's own register and no adjacent row
  is disturbed.
- `.planning/phases/188-non-technical-run-observability/188-SECURITY.md` — the shape of a
  phase security file in this project, its frontmatter, and its UF-1 / UF-2 / UF-3 entries.
  `188.1-SECURITY.md` mirrors that shape; it does not invent one.
- `.planning/phases/188.1-.../188.1-RESEARCH.md` § "Deferred / out of scope" — the nine items
  and their triggers, and § "State of the Art" for the two now-false upstream claims.
- The SUMMARY files of plans 01-04 in this phase — every number written into these documents
  must be a number an executor measured, cited to the plan that measured it.
  </read_first>

  <action>
Re-measure before writing anything: `wc -l frontend/src/components/workflows/WorkflowCanvas.tsx`
and `frontend/src/components/workflows/PhaseNodeCard.tsx`, `cd frontend && npx vite build`
(count the `[INEFFECTIVE_DYNAMIC_IMPORT]` warnings and record the `index-*.js` size),
`npx tsc --noEmit -p tsconfig.app.json`, `node scripts/vitest-count-gate.cjs`, and
`npx eslint src/components/workflows/`. Do not inherit a figure from a SUMMARY without
re-deriving it — four of the five numbers this phase started from were stale.

Edit the `WorkflowCanvas.tsx` ledger row: flip `G-5 fires — extraction due in Phase 188` to
`satisfied (188.1 — <date>)`, give the measured before → after line counts with the `⚠`
correction that the row's own 1574 was stale (measured 1593), add Phase 188 and 188.1 to the
touch list, and record that the ESM-cycle constraint the row names is now enforced by a fence
rather than by prose. Correct the `PhaseNodeCard.tsx` row's 478 L to its measured value.
Preserve both rows' invariant clauses verbatim.

Create `188.1-SECURITY.md` mirroring `188-SECURITY.md`'s structure, carrying the full
register — T-188.1-01 (WR-04, five sites, mitigated, each with the shipped `file:line` and
its falsification), T-188.1-02 (WR-07, mitigated), **T-188.1-03 (UF-1 — `last_workflow_run_id`
on the un-gated `GET /threads/{id}/workflow`, accept + register, assessed low, NO code
change, with D7 named as the re-gating deferral and its trigger)**, T-188.1-04 (the fence
re-scoping), T-188.1-05 (the ESM cycle), T-188.1-06 (rendered-output identity), and
T-188.1-SC (zero package installs). Every CLOSED verdict cites shipped source at `file:line`
and names the observation that proved it — the 188 standard was that mitigations are
VERIFIED, not re-scanned. Set `register_authored_at_plan_time: true` in the frontmatter so
`/gsd:secure-phase 188.1` verifies this register rather than re-deriving one.

Create `188.1-DEFERRED.md` carrying every deferred item with a CONCRETE re-open trigger
(`feedback_preserve_all_deferred_ideas`): D-02 / D1 (the `lazy()` conversion) with the
measured `[INEFFECTIVE_DYNAMIC_IMPORT]` warning count as its pin and the bundle figure as its
evidence; D2 (the `DETOUR` collapse — the drift pin is load-bearing); D3 (`FlowEdge.tsx:135`);
D4 (the `AbortController` half of WR-07); D5 (extracting more of the shell); D6 (a direct
`PlaneEditingLayer.test.tsx`); D7 (gating the endpoint); plus the actual disposition of D8 and
D9. Record the two now-false upstream claims — `185-10-SUMMARY.md:328`'s lazy-chunk claim and
the ROADMAP's 1574 L — as corrections, so the next reader is not misled by them.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vite build</automated>
  </verify>

  <acceptance_criteria>
- `grep -n "satisfied (188.1" CLAUDE.md` returns a match on the `WorkflowCanvas.tsx` ledger row, and `grep -c "G-5 fires" CLAUDE.md` is **one lower** than before this task (`backend/app/api/threads.py` and `anthropic_service.py` still fire and must not be touched).
- `grep -n "1574 L\|478 L" CLAUDE.md` returns **no match** — both stale figures corrected; the replacements are the numbers re-measured in this task and are recorded in the SUMMARY beside the commands that produced them.
- `CLAUDE.md`'s `PhaseNodeCard.tsx` row still contains the literals `third badge` and `focusable control` — the two hard invariants were preserved verbatim.
- `.planning/phases/188.1-.../188.1-SECURITY.md` exists, contains the literal `last_workflow_run_id`, an explicit `accept` disposition for T-188.1-03, and `register_authored_at_plan_time: true` in its frontmatter. Every `mitigate` entry cites at least one `file:line`.
- `.planning/phases/188.1-.../188.1-DEFERRED.md` exists and contains a `Re-open trigger` line for **every** item listed, including the literal `INEFFECTIVE_DYNAMIC_IMPORT` count for D-02.
- `cd frontend && npx vite build` exits **0** and emits exactly the same `[INEFFECTIVE_DYNAMIC_IMPORT]` warning count as the phase-start measurement — the D-02 debt did not get worse under this phase.
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json` reports **0 files changed** — UF-1 was registered, never re-gated.
  </acceptance_criteria>

  <done>The ledger reads *satisfied* with re-measured numbers and two stale figures corrected; UF-1 is registered in a durable, pre-authored security register; every deferral has a concrete re-open trigger and the lazy-import debt is pinned rather than hidden.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive the three operator rows — prove unchanged on the surfaces jsdom cannot see</name>

  <files>
.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-UAT.md
  </files>

  <read_first>
- `.planning/phases/188.1-.../188.1-VALIDATION.md` § Manual-Only Verifications — the three
  rows and their pass conditions, and the sign-off line *"a PASS inherited from another row
  is not a driven row"*.
- `frontend/src/components/workflows/WorkflowCanvas.tsx:390-410` — the recorded incident this
  gate exists for: `pointer-events: none` once shipped an unreachable `＋` while every unit
  test passed.
- `.claude/skills/sketch-findings-agentic-rag` — read only what confirms the SHIPPED
  contracts for the 185 governance corner seal and the 187 node-face ladder. This phase draws
  NO new mark; the skill is a reference for *unchanged*, never for authoring.
  </read_first>

  <action>
Present the three rows below to the operator, wait for them to be DRIVEN in a real browser
after a hard reload, then transcribe each result into a new
`.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-UAT.md`
— one row per line with `result:` and its OWN evidence, never a reference to another row —
plus the recorded DECISION that the SC#10 8-provider roster does not apply to this phase.
Do not auto-approve, do not infer a PASS from an automated gate, and do not drive the
browser on the operator's behalf.
  </action>

  <what-built>
`PlaneEditingLayer` and the `EDIT_AFFORDANCE` geometry table now live in their own modules
(`PlaneEditingLayer.tsx` and `editAffordance.ts`); `WorkflowCanvas.tsx` is ~307 lines lighter
and renders the layer through an import at the same JSX site. Five prototype-key lookups are
own-guarded and `getWorkflowRun` encodes its path segment. Automated proof already green:
count gate ≥ 2462 / 0 failed, `tsc` 33, `vite build` exit 0, eslint 6 → 5, the captured
affordance-geometry baseline still deep-equals, and both source fences were observed RED
against deliberate violations.

What none of that can see: jsdom applies **no CSS**. `pointer-events`, the
`group-hover/canvas` reveal and the picker's zoom counter-scale are invisible to every test
in this phase, and reachability on a live surface is not provable by a component fence —
Phase 188's F7 lesson.
  </what-built>

  <how-to-verify>
**HARD RELOAD before each row** (`Ctrl+Shift+R` at `http://localhost:5173/`). Vite HMR serves
stale code after a module split, and a stale bundle would make every row below meaningless.
Operator drives the browser; login `fhdmrd@gmail.com` / `123456`. If Chrome MCP is used,
read DOM geometry with `evaluate_script` — `take_screenshot` times out repeatedly here.

**M1 — the affordances are reachable and correctly shaped** (frontend only)
1. Open a workflow in the Builder, editable, in a window ≥ 1024 px wide, with a mouse.
2. Hover the plane: the `＋` insert affordance reveals on the connector.
3. Click it, pick a step type from the picker: the phase inserts at the expected index.
4. Click a card's `✕`: the phase is removed.
5. Zoom in and out: the affordances keep a constant on-screen size and stay clickable at
   both extremes; the picker's text stays legible at ~0.3×.
PASS = both controls reveal on plane hover, both are clickable, and the picker's rows respond.

**M2 — the moved geometry still tracks a dragged card, and the 185/187 marks are unchanged**
(frontend only)
1. Drag one card VERTICALLY ONLY. Its `✕` must follow it, and the neighbouring `＋` must sit
   on the slanted connector — no stranded control. (This is the stranded-`✕` screenshot the
   operator filed once already; `verticalOffsetFor` is the moved helper it exercises.)
2. On a workflow with a grounded/locked phase: the 185 governance corner seal sits at the
   TOP-RIGHT of the card, and the 187 node face still reads author name → config-derived →
   type sentence.
3. Toggle ⌥ Technical-names: the **subtitle** swaps, not the title.
PASS = no stranded control, seal top-right, face ladder and ⌥ reveal exactly as before.

**M3 — the 188 run readings still animate on the live run surface** (needs the backend,
which the operator starts)
1. Launch a workflow run and open it on `WorkflowRunPage` — the surface whose static
   `WorkflowCanvas` import D-02 deliberately leaves alone.
2. Sample the elapsed clock at least TWICE and assert the DELTA. A clock is correct at every
   single sample and wrong only across two — that is exactly how Phase 188's F6 hid.
3. Confirm phase statuses advance, and that an unrecognised wire status still renders
   "State unknown" — never "Complete".
PASS = the clock ticks between samples, statuses advance, and the unknown reading is honest.

Record each row in `188.1-UAT.md` with ITS OWN evidence. A row that passed because you saw
something while driving a different row is **not driven** — that is the audit finding that
re-opened Phase 188's board. A row that cannot run is recorded **⛔ with its reason**, never
dropped. Also record the explicit DECISION that the SC#10 8-provider roster does not apply
here, with `git diff --stat -- backend/` = 0 files as its evidence.
  </how-to-verify>

  <acceptance_criteria>
- `188.1-UAT.md` exists with exactly three rows, each carrying `result: PASS` / `FAIL` / `⛔ <reason>` and its own evidence line. **No row reads `[pending]` at sign-off**, and no row's evidence is a reference to another row.
- M3's evidence names **two** clock samples and the delta between them — a single reading is not evidence that a clock ticks.
- M2's evidence names the seal's corner and the ⌥ toggle's observed effect on the subtitle.
- The file records the SC#10 non-applicability determination as a DECISION with its mechanical evidence, so it reads as scoped-out rather than skipped.
- If any row is ⛔, its reason and the blocking issue id are recorded, and it is carried into `STATE.md` as owed manual debt with the row to run first named — closing a phase with owed rows is legitimate, but only as a stated decision, never as a claim that everything ran.
  </acceptance_criteria>

  <done>All three operator rows are recorded in `188.1-UAT.md` with their own evidence and a non-pending result; any ⛔ row is carried into `STATE.md` as owed debt with the row to run first named.</done>

  <resume-signal>Type "approved" with the three row results, or describe what you saw so the finding can be triaged (fast-fix under G-3 / defer with a trigger / accept).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unit-test surface → live rendered surface | jsdom applies no CSS. `pointer-events`, hover reveal and zoom counter-scale cross this boundary invisibly; `WorkflowCanvas.tsx:390-410` records that an unreachable `＋` once shipped with every unit test green. |
| documentation → future phase behaviour | The G-5 ledger is what a future orchestrator scans `files_modified` against at discuss-phase. A row that still reads *G-5 fires* after the debt is paid, or one carrying a 58%-wrong line count, misroutes Phase 189. |
| threat registered → threat handled | A registered `accept` is only honest if the endpoint genuinely did not change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188.1-03 | Information disclosure | `last_workflow_run_id` returned by the un-gated `GET /threads/{id}/workflow` (UF-1) | **accept + register** | Ownership-gated route; sibling fields already disclosed run existence before Phase 188, so the marginal disclosure is low. Registered in `188.1-SECURITY.md` with an explicit accept disposition. **No code change** — re-gating behind `CANVAS_GATED_PATHS` is D7, deferred with a trigger, because it is a backend behaviour change the client depends on. Gate: `git diff --stat -- backend/` = 0 files changed |
| T-188.1-06 | Tampering (integrity of a shipped promise) | the live canvas on the Builder and run surfaces | mitigate | Operator rows M1-M3 after a HARD RELOAD, each driven with its own evidence; M3 samples the clock twice and asserts the delta, because a clock is correct at every sample and wrong only across two |
| T-188.1-11 | Repudiation | the G-5 hot-file ledger | mitigate | The row is flipped AND its stale 1574 L corrected to the measured 1593; `PhaseNodeCard.tsx`'s 478 L corrected to 755; every number re-derived in this task rather than inherited from a SUMMARY |
| T-188.1-12 | Repudiation | a UAT board that records only what passed | mitigate | Every row records PASS / FAIL / ⛔-with-reason; a ⛔ row is carried into `STATE.md` as owed debt with the row to run first named. A scoreboard that lists only what passed is not a scoreboard |
| T-188.1-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed across the whole phase. Final gate: `git diff --stat -- frontend/package.json frontend/package-lock.json` = 0 files changed; `bash scripts/check-deploy-drift.sh` exit 0 |
</threat_model>

<verification>
- `cd frontend && npx vite build` → exit 0, `[INEFFECTIVE_DYNAMIC_IMPORT]` count unchanged from phase start
- `node scripts/vitest-count-gate.cjs` → `count gate OK`, failed 0, total ≥ 2462, no `[count-decrease]`, no `[missing-file]`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → exactly 33
- `cd frontend && npx eslint src/components/workflows/` → exactly 5 errors
- `git diff --stat -- backend/ supabase/migrations/ frontend/package.json` → 0 files changed
- `bash scripts/check-deploy-drift.sh` → exit 0
- `node scripts/check-gap-closure-rounds.cjs 188.1` → exit 0 before any `--gaps` routing is emitted (G-7)
</verification>

<success_criteria>
1. The `WorkflowCanvas.tsx` ledger row reads *satisfied (188.1)* with re-measured before/after counts; the two stale figures (1574, 478) are corrected; both rows' invariant clauses are preserved.
2. `188.1-SECURITY.md` registers UF-1 as accept-and-register with no backend change, and every mitigated threat cites shipped source.
3. `188.1-DEFERRED.md` carries a concrete re-open trigger for every deferral, including D-02's pinned `[INEFFECTIVE_DYNAMIC_IMPORT]` count.
4. Three operator rows driven, each with its own evidence; M3 asserts a clock delta across two samples.
5. The SC#10 non-applicability is recorded as a decision with mechanical evidence.
6. Zero backend, migration or package change across the whole phase.
</success_criteria>

<output>
Create `.planning/phases/188.1-workflowcanvas-extraction-refactor-lift-planeeditinglayer-an/188.1-05-SUMMARY.md` when done. Record verbatim: every re-measured number with the command that produced it, the ledger row before and after, and the three UAT row results with their evidence.
</output>
