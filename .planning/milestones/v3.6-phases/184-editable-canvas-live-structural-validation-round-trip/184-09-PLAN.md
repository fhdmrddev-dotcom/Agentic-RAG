---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 09
type: execute
wave: 5
depends_on: ["184-06"]
files_modified:
  - frontend/src/hooks/useGroundingBundle.ts
  - frontend/src/hooks/useGroundingBundle.test.ts
  - frontend/src/components/workflows/PhaseFormPanel.tsx
  - frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx
autonomous: true
requirements: [CANVAS-03, CANVAS-04]

must_haves:
  truths:
    - "CANVAS-03 / R5: the canvas is a third way IN to ONE form — PhaseFormPanel is extended in place, no second form component is added, onClose stays a REQUIRED prop, the 44px collapsed rail and the <768px bottom sheet both survive"
    - "D-14 / Pitfall 6: the rails ride an OPTIONAL prop whose ABSENCE renders today's panel byte-for-byte, so a flag-off user's surface cannot drift; the 19 shipped PhaseFormPanel.test.tsx assertions pass unmodified"
    - "CANVAS-04 / R11: the tool whitelist option SET comes from GET /workflows/grounding-bundle and from no frontend constant; a degraded bundle SAYS SO instead of rendering an empty-but-normal picker"
    - "R11: a named tool the registry lacks renders STRUCK THROUGH, never hidden — the server already says unregistered_tool and the rail's job is to make that legible where it can be fixed"
    - "R11: a locked gate row has NO removal control in the DOM; the gate rail is DERIVED exactly as the shipped groundingFor() does today"
    - "R11: Phase 184 does not invent grounding_mode — no authored grounding field appears in the definition, the panel, the store or the props"
  artifacts:
    - path: "frontend/src/hooks/useGroundingBundle.ts"
      provides: "The CANVAS-04 palette source — the only caller of GET /workflows/grounding-bundle"
      exports: ["useGroundingBundle", "GroundingBundleState"]
    - path: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      provides: "The ONE form, extended in place with an optional rails prop"
      contains: "rails?:"
  key_links:
    - from: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      to: "GET /workflows/grounding-bundle"
      via: "the rails.toolOptions prop, supplied by useGroundingBundle at the page — the panel fetches nothing"
      pattern: "rails\\?\\."
---

<objective>
Make governance legible as rails inside the shipped 400px inspector — without changing one pixel of what
a flag-off user sees.

Purpose: 140-A won because it is the only option that does not create a second form surface to keep in
step with the Builder's. But `PhaseFormPanel` is rendered ONCE, by the page, and serves BOTH views, so
any change to its rendered controls changes the flag-off surface — a D-14 / D-181-01 violation that
`revertByteIdentical.test.tsx` does not catch, because it never renders the panel. The only safe shape
is an optional prop whose absence is today, byte-for-byte.

Output: `useGroundingBundle` (the palette's only caller) and an optional `rails` prop carrying the three
rails — order is locked, the tool whitelist is a bundle-sourced chip set with no free-text box, and gates
are 🔒 rows that cannot be detached beside `○` rows that can.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
@.planning/sketches/140-step-inspector-and-rails/README.md
</context>

<interfaces>
<!-- Verified in the files this session. -->

`frontend/src/components/workflows/PhaseFormPanel.tsx`:
- the props interface `:53-74`. Every existing optional prop carries a `?` plus a docblock naming its
  originating phase (`:58-65`). `onClose` at `:70-73` is **REQUIRED** with a docblock explaining that
  "a panel the user cannot close" was made un-representable in the type system rather than merely tested
  for. **Do not weaken it.**
- conditioning anchor `:430-433` — `const pt = phase.config.phase_type`; there is no control anywhere in
  the file that writes `phase_type` (which is what forces D-184-11's choose-the-type-at-add-time)
- `ToolsField` `:336-384` — the free-text comma box plus friendly chips; call sites `:568-572`
  (`llm_agent`) and `:619-623` (`llm_batch_agents`)
- `friendlyToolName` `:386-396` — a **display-LABEL map** (`search_documents` -> "Search documents") that
  falls back to the raw id. **It is not an options source and it must survive.** R11's "no frontend
  constant" guard must therefore be worded as *"the option SET equals the mocked bundle response"* — a
  blanket source-grep for tool-name literals would false-positive here and force a docblock to lie
  (the D-ITEM-183-02 trap, fired five times in Phase 183)
- `onBlur={onPersist}` on every field (e.g. `:378`) — the persist seam
- `onClose` call site `:464`

`GET /workflows/grounding-bundle` (184-06's `getGroundingBundle`) returns
`{ tools: string[], folders, skills, template_placeholders, degraded: string[] }`. **`degraded: []` is
the ONLY value meaning "this palette is complete"** — a registry blip renders `{folders: [], skills: []}`
at HTTP 200, byte-indistinguishable from an author who owns nothing.

The gate rail is DERIVED, exactly as shipped: `groundingFor()` in
`frontend/src/components/workflows/phaseVocabulary.ts` reads `citation_policy` plus the presence of a
`citations_required` validator; `deriveTier.ts` owns `CitationPolicy` / `ValidatorKind`. Phase 185
replaces that derivation with an authored per-node grounding mode. **184 must not invent that field.**

The locked rail wording (140-A): order reads *"Runs as step 1 of 3 — steps run in order, one after
another."*
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: useGroundingBundle — the palette's only caller, honest about degraded</name>
  <files>frontend/src/hooks/useGroundingBundle.ts, frontend/src/hooks/useGroundingBundle.test.ts</files>
  <read_first>
    - frontend/src/lib/api.ts — `getGroundingBundle` and the `GroundingBundle` type added in 184-06
    - frontend/src/hooks/usePanelReconcile.ts (the effect + abort-on-cleanup shape `:114-130`, the post-await guard `:70-75`, the AbortError check `:84-92`)
    - frontend/src/hooks/useOperatorProbe.test.ts (the co-located hook test with a module-mocked api client)
    - .planning/sketches/140-step-inspector-and-rails/README.md — the whitelist rail's fourth honesty beat (struck-through `web_scrape`) and the degraded rule
  </read_first>
  <action>
Create `frontend/src/hooks/useGroundingBundle.ts` — the ONLY caller of
`GET /workflows/grounding-bundle` in the app.

Signature: `useGroundingBundle(enabled: boolean): GroundingBundleState`, where `GroundingBundleState` is
a discriminated union: `{ kind: "idle" }` | `{ kind: "loading" }` |
`{ kind: "ready"; tools: string[]; folders; skills; degraded: string[] }` |
`{ kind: "unavailable"; reason: "degraded" | "unreachable" }`.

Behaviour:
- Fetch once when `enabled` flips true; do not poll. Use the `usePanelReconcile.ts:114-130`
  effect + `AbortController`-cleanup shape and its double-shaped AbortError check.
- **`degraded` is the honesty field.** A 200 whose `degraded` array is NON-EMPTY must NOT be presented as
  a complete palette. Surface it as `kind: "unavailable", reason: "degraded"` **while still carrying the
  partial `tools` array**, so the caller can say "we could not load these" rather than render an
  empty-but-normal picker. Say in the docblock that `degraded: []` is the only value meaning complete.
- A network failure, a 404 (the flag was turned off mid-session) or a 401 maps to
  `kind: "unavailable", reason: "unreachable"`.
- **No frontend tool constant anywhere in this file.** The hook returns the server's `tools` array and
  invents nothing.

Create `useGroundingBundle.test.ts` in the `useOperatorProbe.test.ts` shape with a WHOLE-module
`vi.mock("@/lib/api", …)` enumerating every symbol the hook's path reaches (the
`feedback_mock_completeness` lesson):
- `enabled: false` issues zero calls
- a 200 with `degraded: []` yields `kind: "ready"` and the exact `tools` array from the mock
- **changing the mock changes the tools** — two renders with different mocked `tools` yield different
  results, which is R11's "the options come from the bundle response" proof at the source
- a 200 with a non-empty `degraded` yields `kind: "unavailable"` with `reason: "degraded"` and still
  carries whatever `tools` came back
- a rejected call yields `kind: "unavailable"` with `reason: "unreachable"`
- an abort caused by unmount produces no state update and no error
- a `?raw` source guard asserts the file contains no hardcoded tool-name array; word it as *"no array
  literal containing two or more known tool ids"* rather than a blanket token grep, and give it a
  positive control
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useGroundingBundle.test.ts --reporter=default</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `useGroundingBundle.ts` exports `useGroundingBundle` and `GroundingBundleState` as a `kind`-discriminated union with all four members
    - A non-empty `degraded` array yields `kind: "unavailable"` with `reason: "degraded"` — never `kind: "ready"`
    - The change-the-mock-changes-the-tools test passes
    - `enabled: false` yields exactly 0 calls
    - The `?raw` guard's positive control passes and `friendlyToolName`-style label maps are NOT flagged (this file contains none)
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
  </acceptance_criteria>
  <done>One hook owns the palette read, returns the server's tool set and nothing of its own, and refuses to present a degraded registry as a complete list.</done>
</task>

<task type="auto">
  <name>Task 2: PhaseFormPanel gains an optional rails prop — order and gates, absent = today</name>
  <files>frontend/src/components/workflows/PhaseFormPanel.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PhaseFormPanel.tsx (the WHOLE file — especially the props interface `:53-74`, the required `onClose` docblock `:70-73`, the conditioning anchor `:430-433`, the 44px collapsed rail and the <768px bottom-sheet branches)
    - frontend/src/components/workflows/PhaseFormPanel.test.tsx (all 19 assertions — none may be edited; all render WITHOUT rails)
    - frontend/src/components/workflows/phaseVocabulary.ts — `groundingFor()` and the grounding faces
    - frontend/src/components/workflows/deriveTier.ts — `CitationPolicy`, `ValidatorKind`, the derived tier
    - .planning/sketches/140-step-inspector-and-rails/README.md — the three-rail table and the forward seam to Phase 185
  </read_first>
  <action>
Extend `PhaseFormPanel`'s props interface with ONE new optional prop, following the shipped
optional-prop idiom at `:58-65` (a `?` plus a docblock naming its originating phase):

`rails?: { order: { index: number; total: number }; toolOptions: string[] | "degraded"; gates: Array<{ label: string; locked: boolean }> }`

**ABSENT ⇒ the panel renders exactly as it ships today, byte-for-byte.** That is the D-14 mechanism and
it must be stated in the prop's docblock. Do not weaken `onClose` from required. Do not add a second
form component. Do not touch the 44px collapsed rail or the <768px bottom-sheet branches — a 400px panel
beside 248px cards leaves room for roughly two steps at 900px, which is exactly why both already exist.

In this task, render two of the three rails when `rails` is present:

**Order rail** — reads *"Runs as step {index} of {total} — steps run in order, one after another."*
It is informational and carries no control: `phase_index` IS the order, there is no `depends_on`, and
branching is not representable. Moving is allowed; rewiring is not.

**Gates rail** — one row per `rails.gates` entry. A `locked: true` row renders the 🔒 affordance and
**has no removal control anywhere in its DOM subtree**; a `locked: false` row renders `○` and its
removal control, and says it can be removed. A locked gate came WITH a choice above it — remove what
made it apply and it goes; there is no switch.

**The gates array is DERIVED by the caller, exactly as `groundingFor()` does today.** This panel does
not compute it and — critically — **184 invents no `grounding_mode`**. The string `grounding_mode` must
not appear in this file, in the props, in the store, or anywhere else this phase touches. Phase 185
replaces the derivation with an authored per-node mode and plugs into `rails.gates`; designing the
container for it now is the point, inventing the field is not.

Update any docblock this edit falsifies in the SAME commit.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PhaseFormPanel.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All 19 `PhaseFormPanel.test.tsx` assertions pass **unmodified** — `git diff frontend/src/components/workflows/PhaseFormPanel.test.tsx` is empty
    - `onClose` is still declared without a `?` — removing it from a call site produces a `tsc` error; record the observed error code in the SUMMARY after a temporary probe, then remove the probe
    - `grep -c 'grounding_mode' frontend/src/components/workflows/PhaseFormPanel.tsx` returns 0
    - `grep -c 'rails?:' frontend/src/components/workflows/PhaseFormPanel.tsx` returns >= 1 and the prop's docblock contains the words "absent" and "today"
    - The 44px collapsed-rail branch and the <768px bottom-sheet branch are untouched — confirm with `git diff` and state it in the SUMMARY
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
  </acceptance_criteria>
  <done>The panel carries an optional rails prop whose absence is today's render; the order rail and the lock-honest gates rail exist; no `grounding_mode` was invented and `onClose` is still required.</done>
</task>

<task type="auto">
  <name>Task 3: The tool-whitelist rail from the bundle, plus the D-14 and R11 proof suite</name>
  <files>frontend/src/components/workflows/PhaseFormPanel.tsx, frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PhaseFormPanel.tsx (as extended in Task 2 — plus `ToolsField` `:336-384`, its call sites `:568-572` and `:619-623`, and `friendlyToolName` `:386-396`)
    - frontend/src/components/workflows/PhaseFormPanel.test.tsx (the 19 shipped assertions — the regression net; DO NOT edit)
    - 184-PATTERNS.md §S2 and §S6 — the `?raw` guard-wording rule and the optional-prop additivity rule
    - .planning/sketches/140-step-inspector-and-rails/README.md — the struck-through `web_scrape` beat and the "an empty dropdown would be a lie" rule
  </read_first>
  <action>
**Tool rail.** When `rails` is present AND the selected step's type carries `available_tools`
(`llm_agent`, `llm_batch_agents`), replace the free-text comma box with a **chip set the user chooses
FROM**, sourced entirely from `rails.toolOptions`:
- **No free-text box** in the rails variant — R11 is explicit. The shipped `ToolsField` free-text path
  must remain the render when `rails` is ABSENT, unchanged, so the flag-off surface and the 19 shipped
  assertions are untouched.
- A tool the definition NAMES that `rails.toolOptions` does not contain renders **struck through, not
  hidden**. The server already says `unregistered_tool`; the rail's job is to make that legible where it
  can be fixed.
- `rails.toolOptions === "degraded"` renders a plain sentence saying the tools could not be loaded, and
  **never an empty-but-normal picker** — an empty dropdown is byte-indistinguishable from an author who
  owns nothing, which is precisely the lie R11 exists to prevent.
- `friendlyToolName` (`:386-396`) stays and keeps labelling the chips. It is a display-LABEL map, not an
  options source.

Then create `frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx` — a NET-NEW suite that
does not absorb or replace `PhaseFormPanel.test.tsx` (the Phase-177 coverage-loss lesson; the count gate
enforces it).

Assertions:
1. **D-14, the load-bearing one (Pitfall 6).** Render the panel WITHOUT `rails` and assert its DOM is
   unchanged from the shipped render: snapshot the container's `outerHTML` for a representative
   `llm_agent` phase and assert the free-text `available_tools` input IS present, the chip picker is
   NOT, and no rail heading renders. `revertByteIdentical.test.tsx` never renders this panel, so this is
   the only place the flag-off panel surface is guarded.
2. **R11 — the options come from the bundle.** Render with `rails.toolOptions = ["search_documents","execute_code"]`
   and assert the rendered option SET equals exactly that array. Re-render with a DIFFERENT array and
   assert the options change. **Word the guard as an option-set equality, never as a blanket source-grep
   for tool-name literals** — that would false-positive on `friendlyToolName` and force its docblock to
   omit the real identifiers.
3. **No free-text box in the rails variant**: with `rails` present, there is no `input[type=text]` bound
   to `available_tools`.
4. **Struck-through unregistered tool**: a phase naming `web_scrape` while `toolOptions` omits it renders
   `web_scrape` with a line-through style/class and does NOT hide it.
5. **Degraded**: `toolOptions: "degraded"` renders the could-not-load sentence and renders **zero** tool
   options; assert both.
6. **A locked gate has no removal control in the DOM**: for a `locked: true` row,
   `row.querySelectorAll('button, [role="button"], input')` has length 0. An unlocked row has exactly one.
7. **Order rail** renders the locked sentence with the right numbers for `{index: 1, total: 3}`.
8. **No `grounding_mode` anywhere**: a `?raw` guard on `PhaseFormPanel.tsx` asserts the token is absent,
   with a positive control.
9. **Per-type conditioning is unchanged**: with `rails` present, `programmatic` and `llm_human_input`
   still carry no model / tools / folder-scope fields, and `citation_policy` still appears only on the
   deliverable. This is the shipped behaviour, not a simplification, and adding rails must not perturb it.
10. **No second form component**: a source scan asserts exactly one component in
    `frontend/src/components/workflows/` renders the phase config form — assert that no file other than
    `PhaseFormPanel.tsx` exports a component whose name matches `/PhaseForm|PhaseConfigForm|StepForm/`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - All 19 `PhaseFormPanel.test.tsx` assertions pass unmodified; `git diff` on that file is empty
    - `PhaseFormPanel.rails.test.tsx` exists as a NET-NEW suite and `node scripts/vitest-count-gate.cjs` shows `PhaseFormPanel.test.tsx` still at exactly 19
    - The rails-absent test asserts the free-text `available_tools` input is present and the chip picker is absent
    - The option-set equality test passes for two different mocked arrays
    - The struck-through test asserts the unregistered tool is present in the DOM with a line-through treatment
    - The degraded test asserts the could-not-load sentence AND zero rendered options
    - A `locked: true` gate row has zero `button`/`[role="button"]`/`input` descendants
    - `grep -c 'grounding_mode' frontend/src/components/workflows/PhaseFormPanel.tsx` returns 0 and the `?raw` positive control passes
    - The "no second form component" scan passes
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>The tool rail is bundle-sourced with no free-text box and no frontend constant, an unregistered tool is visible and struck through, a degraded read says so, a locked gate cannot be detached, and the rails-absent panel is provably today's panel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /workflows/grounding-bundle` -> rendered option set | The server-owned registry is the only legitimate source for a tool whitelist |
| flag-on rails vs flag-off panel | One component instance serves both audiences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-09-01 | Elevation of privilege | tool whitelist option source | mitigate | The option SET is `bundle.tools` and nothing else — asserted by an option-set equality test over two different mocked responses. A frontend constant would let KB content whitelist itself (research Pitfall 1); `friendlyToolName` stays a display-label map only |
| T-184-09-02 | Spoofing | a degraded registry rendering as a complete palette | mitigate | `useGroundingBundle` maps a non-empty `degraded` array to `kind: "unavailable"`, and the panel renders a could-not-load sentence with zero options. A 200 carrying empty arrays is byte-indistinguishable from "you own nothing", which is why the honesty field is load-bearing |
| T-184-09-03 | Elevation of privilege | flag-off surface drift | mitigate | The rails ride an optional prop whose absence renders today's DOM; the 19 shipped panel assertions pass unmodified and a dedicated rails-absent assertion guards the surface `revertByteIdentical.test.tsx` cannot see (Pitfall 6) |
| T-184-09-04 | Tampering | governance the author can wire around | mitigate | A `locked: true` gate row has NO removal control in its DOM subtree, asserted structurally rather than by disabling a control that still exists |
| T-184-09-05 | Tampering | scope creep into Phase 185's field | mitigate | `grounding_mode` is grep-asserted absent from the panel; the gate rail is DERIVED exactly as the shipped `groundingFor()` does |
| T-184-09-SC | Tampering | npm installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx` — 0 failures, exit code 0
- `node scripts/vitest-count-gate.cjs` — exit 0, `PhaseFormPanel.test.tsx` still at exactly 19
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `git diff frontend/src/components/workflows/PhaseFormPanel.test.tsx` is empty
- `git diff --name-only -- backend/ | wc -l` returns 0
- `git diff --name-only -- supabase/migrations | wc -l` returns 0
</verification>

<success_criteria>
- One form, extended in place; no second form component exists and `onClose` is still required
- The rails-absent panel is provably byte-identical to today, guarding the one D-14 surface `revertByteIdentical.test.tsx` cannot see
- The tool option set comes from the bundle response, with no frontend constant and no free-text box in the rails variant
- An unregistered named tool is visible and struck through; a degraded read says so rather than showing an empty picker
- A locked gate has no removal control in the DOM, and no `grounding_mode` was invented
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-09-SUMMARY.md` when done.
It MUST record the observed `tsc` error from the `onClose`-required probe and confirm that the 44px rail
and <768px bottom-sheet branches were untouched.
</output>
