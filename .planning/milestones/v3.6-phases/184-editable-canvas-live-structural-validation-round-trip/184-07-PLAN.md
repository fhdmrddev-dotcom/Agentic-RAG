---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 07
type: execute
wave: 4
depends_on: ["184-04"]
files_modified:
  - frontend/src/components/workflows/canvasNudge.ts
  - frontend/src/components/workflows/canvasNudge.test.ts
  - frontend/src/components/workflows/StepTypePicker.tsx
  - frontend/src/components/workflows/StepTypePicker.test.tsx
autonomous: true
requirements: [CANVAS-02, VALID-02]

must_haves:
  truths:
    - "R3 / 138 C-local: the cosmetic dy is a per-user BROWSER preference — never sent to the server, never written to the definition, and this phase adds ZERO migration files; slot 114 stays RESERVED"
    - "D-184-02: canvasNudge is a module of its own with no store import, so the dy never enters the tracked store and a nudge adds no history entry by construction"
    - "The nudge module owns ALL storage access — WorkflowBuilderPage.tsx must never contain the token localStorage (its shipped source guard forbids it)"
    - "D-184-11: the picker lists the 6 step types in plain-language sentences with their 3D marks; a choice that would strand the deliverable is offered DISABLED with its reason inline, never hidden"
    - "D-184-11: the slug is auto-generated from the closed type set and is NOT user-editable in this phase"
    - "R10: neither refusal consults /workflows/validate — a refusal is a shape rule decided locally, a verdict is a server judgement, and the two never mix"
  artifacts:
    - path: "frontend/src/components/workflows/canvasNudge.ts"
      provides: "The browser-local, user+draft-scoped dy map"
      exports: ["CANVAS_NUDGE_KEY_PREFIX", "canvasNudgeKey", "readNudges", "writeNudge", "clearNudges"]
    - path: "frontend/src/components/workflows/StepTypePicker.tsx"
      provides: "The plain-language step-type picker with inline disabled reasons"
      exports: ["StepTypePicker"]
  key_links:
    - from: "frontend/src/components/workflows/StepTypePicker.tsx"
      to: "frontend/src/components/workflows/definitionOps.ts"
      via: "allowedTypesAt supplies the disabled reasons — the picker computes none of its own"
      pattern: "allowedTypesAt"
---

<objective>
Build the two leaf modules the canvas editing surface consumes: the browser-local cosmetic nudge store,
and the plain-language step-type picker with its inline refusal.

Purpose: 138 C-local resolved OPEN-05 to **zero migration** by keeping the interaction and moving the
storage — the `dy` is a per-user view preference, so a stale layout row against a deleted step is not
representable and Phase 186's "a cosmetic drag never mints a version" is true by construction rather
than being a live code path. The picker is 139-C's cheap prevention folded into 139-A: prevention can
decide anything about the SHAPE of the flow, and nothing else — and it must never become the thing that
stops someone building.

Output: `canvasNudge.ts` + `StepTypePicker.tsx` and their suites. Neither module is mounted yet —
184-12 wires them into the canvas — so this plan cannot regress a shipped surface.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
@.planning/sketches/138-growing-the-flow/README.md
@.planning/sketches/139-validation-while-building/README.md
@.planning/sketches/themes/canvas-184.css
</context>

<interfaces>
<!-- Verified this session. -->

`frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx:414-438` — the shipped guard that DECIDES this
module boundary: `expect(builderSource).not.toMatch(/localStorage/)` and `.not.toMatch(/sessionStorage/)`.
Therefore `canvasNudge.ts` owns ALL storage access and the page passes `draftId` in.

`frontend/src/lib/streamsCache.ts` — the shipped user-scoped-localStorage module and the exact
structural analog:
- key-namespace pattern `:29-44` — `STREAMS_CACHE_KEY_PREFIX = "agentic-rag.streams.v1"`, resolved key
  shape `agentic-rag.streams.v1.<user_id>`, with the docblock stating WHY it is user-scoped (a shared
  origin must not leak one user's content into another's first paint)
- `streamsCacheKey(userId)` `:60` · `getCurrentUserIdSync()` `:60-79` (reads the `sb-*-auth-token`
  localStorage entry defensively; **reuse this exported helper, do not re-derive it**)
- quota discipline `:19-22` / `writeSnapshotToLocalStorage` `:264` — wrap `setItem`, catch
  `QuotaExceededError`, `console.warn` and fall through silently; never crash the surface

`frontend/src/hooks/useResizablePanel.ts:67-76` — the defensive-read pattern (try/catch around
`getItem`, finite check, clamp, fall back to default when localStorage is unavailable).

`frontend/src/components/workflows/definitionOps.ts` (184-02) — `allowedTypesAt(phases, index)` returns
all six types in fixed order, each optionally carrying a `disabledReason`; `slugForType(phases, type)`;
`minimalPhaseFor(type, slug, index)`.

`frontend/src/components/workflows/phaseVocabulary.ts:108` — `PHASE_TYPE_SENTENCES`, the D-183-06
plain-language sentences ("Search the knowledge base", "Wait for you", …).

`frontend/src/components/workflows/nodePresentation.ts` (184-03) — `renderPhaseMark(phaseType)` returns
the 3D `fluent-emoji` mark as a `ReactNode`. **Never a text glyph** (the locked sketch rule).

`frontend/src/components/workflows/PhaseFormPanel.tsx:201` — the shipped `SelectField` shape the picker
borrows its label/helper-line vocabulary from.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: canvasNudge.ts — the browser-local, user+draft-scoped dy map</name>
  <files>frontend/src/components/workflows/canvasNudge.ts, frontend/src/components/workflows/canvasNudge.test.ts</files>
  <read_first>
    - frontend/src/lib/streamsCache.ts (the whole file — the key-namespace docblock `:29-44`, `getCurrentUserIdSync` `:60-79`, the quota discipline `:19-22` and `writeSnapshotToLocalStorage` `:264`)
    - frontend/src/hooks/useResizablePanel.ts `:60-90` (the defensive-read pattern) and useResizablePanel.test.ts (the storage-test analog)
    - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx `:414-438` (the guard that forces this module to exist)
    - .planning/sketches/138-growing-the-flow/README.md — the C-local decision table and the written promotion trigger
  </read_first>
  <action>
Create `frontend/src/components/workflows/canvasNudge.ts`. It owns **all** storage access for the
cosmetic vertical nudge. It imports nothing from the builder store, nothing from `@/lib/api`, and
nothing from `canvasModel` — the `dy` must not be able to reach the definition even by accident.

Key namespace, in the `streamsCache.ts:29-44` shape:
`export const CANVAS_NUDGE_KEY_PREFIX = "agentic-rag.canvas-nudge.v1"` and
`canvasNudgeKey(userId: string, draftId: string): string` resolving to
`agentic-rag.canvas-nudge.v1.<user_id>.<draft_id>`. Reuse `getCurrentUserIdSync()` from
`@/lib/streamsCache` rather than re-deriving the auth-token read. Document WHY the key is user-scoped:
a shared origin (a dev machine with two test logins, a kiosk, a family device) must not leak one user's
layout into another's canvas. It is cosmetic data, but keying it per user is free.

API:
- `readNudges(draftId: string | null): Record<string, number>` — the slug→dy map. Defensive read in the
  `useResizablePanel.ts:67-76` shape: try/catch, JSON parse guard, drop any non-finite value, return
  `{}` on any failure (private mode, SSR, corrupt entry).
- `writeNudge(draftId: string | null, slug: string, dy: number): void` — merge one entry. Wrap `setItem`
  and follow the `streamsCache.ts:19-22` quota discipline: on `QuotaExceededError`, `console.warn` and
  fall through silently. A cosmetic nudge must never crash the authoring surface.
- `clearNudges(draftId: string | null): void` — "Tidy up". Clears **the current workflow's key only** —
  never the whole prefix, never another draft's key.

**The unsaved-draft rule** (Claude's discretion, recorded in CONTEXT): a draft that has never been saved
has no id. When `draftId` is null, keep the map in a module-level in-memory bucket for the session only
and persist nothing — so there is no orphan-key bucket to garbage-collect. Document that the bucket is
cleared when the module's owner unmounts and that nothing about it is durable.

Write the module docblock with the C-local decision recorded verbatim: the `dy` is a per-user view
preference, **never sent to the server, never written to `definition`, zero migration — slot 114 stays
RESERVED**, plus the written promotion trigger (promote into `workflow_layouts` when a nudge must
survive across devices, OR a layout is deliberately shared between editors — neither is true today).

Then create `canvasNudge.test.ts`:
- round trip: `writeNudge` then `readNudges` returns the entry; two slugs coexist; a second write to the
  same slug overwrites
- user scoping: two different user ids resolve to different keys and do not see each other's entries
- draft scoping: two draft ids do not see each other's entries; `clearNudges(a)` leaves `b` intact
- unsaved draft (`draftId: null`): `writeNudge` then `readNudges` works in-memory, and
  `localStorage.length` is unchanged — assert **zero** keys were created
- defensive read: a corrupt JSON entry, a non-numeric `dy`, and a `getItem` that throws each yield `{}`
  without throwing
- quota: a `setItem` stubbed to throw `QuotaExceededError` produces a `console.warn` and does not throw
- **zero network**: a `vi.spyOn(globalThis, "fetch")` records 0 calls across the whole suite (R3's
  "nudging a card produces zero network requests", proven at the module level)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/canvasNudge.test.ts --reporter=default</automated>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `canvasNudge.ts` exports `CANVAS_NUDGE_KEY_PREFIX` (= `"agentic-rag.canvas-nudge.v1"`), `canvasNudgeKey`, `readNudges`, `writeNudge`, `clearNudges`
    - `grep -nE 'from "@/(lib/api|components/workflows/(builderStore|canvasModel))"' frontend/src/components/workflows/canvasNudge.ts` returns no matches
    - `grep -c 'localStorage' frontend/src/pages/WorkflowBuilderPage.tsx` returns 0 and all 22 `WorkflowBuilderPage.canvas.test.tsx` assertions pass unmodified
    - `canvasNudgeKey("u1","d1")` returns exactly `agentic-rag.canvas-nudge.v1.u1.d1`
    - The unsaved-draft test asserts `localStorage.length` is unchanged after a `writeNudge(null, ...)`
    - The `fetch` spy reports exactly 0 calls
    - The quota test asserts no throw and one `console.warn`
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
  </acceptance_criteria>
  <done>The cosmetic `dy` lives in one browser-local, user+draft-scoped module that issues zero network calls, degrades silently on quota failure, persists nothing for an unsaved draft, and cannot reach the definition.</done>
</task>

<task type="auto">
  <name>Task 2: StepTypePicker.tsx — the plain-language picker with inline disabled reasons</name>
  <files>frontend/src/components/workflows/StepTypePicker.tsx</files>
  <read_first>
    - frontend/src/components/workflows/phaseVocabulary.ts `:100-130` — `PHASE_TYPE_SENTENCES` and the plain-language title resolvers
    - frontend/src/components/workflows/nodePresentation.ts (184-03) — `renderPhaseMark`
    - frontend/src/components/workflows/definitionOps.ts (184-02) — `allowedTypesAt`, `slugForType`, `minimalPhaseFor`
    - frontend/src/components/workflows/PhaseFormPanel.tsx `:180-240` — the shipped `SelectField` / label + always-visible-helper-line vocabulary
    - .planning/sketches/139-validation-while-building/README.md — variant C's "disabled WITH a reason" finding and the build rule it becomes
    - .planning/sketches/themes/canvas-184.css — the locked insert-affordance and picker chrome
  </read_first>
  <action>
Create `frontend/src/components/workflows/StepTypePicker.tsx`.

Props: `{ phases: readonly PhaseSpecJSON[]; index: number; onChoose: (type: PhaseTypeId) => void;
onDismiss: () => void; open: boolean }`. The picker is a leaf: it takes `phases` and an insertion
`index`, renders, and calls back. It creates no phase itself and holds no store reference, so it renders
in a plain unit test with no provider.

Render one row per step type, in the fixed order `allowedTypesAt(phases, index)` returns — **all six,
always**. Each row carries:
- the 3D mark from `renderPhaseMark(type)`. **Never a text glyph** — that single miss is most of why an
  earlier sketch batch read as "very basic … does not look very agentic".
- the D-183-06 plain-language sentence from `PHASE_TYPE_SENTENCES` as the row's primary label
- when the entry carries a `disabledReason`, render the row **disabled with the reason inline and
  visible** (not a tooltip, not hidden) and set `aria-disabled="true"` plus `disabled`. An option that
  vanishes teaches nothing; an option that is greyed with no reason is worse.

**The picker computes no refusal of its own.** Every `disabledReason` comes from
`definitionOps.allowedTypesAt`. This module must not import `@/lib/api`, must not contain the string
`workflows/validate`, and must not fetch: R10 says both refusals are decidable from the local shape, and
keeping the shape rule visibly separate from the server verdict is what keeps D-182-06 intact.

The technical `phase_type` identifier renders only behind the ⌥ Technical-names reveal, read via
`useTechnicalNamesOptional()` in the shipped fail-closed shape (`WorkflowCanvas.tsx:181-183`): null
outside a provider means plain language, no control, no crash.

**D-184-11 — no slug field.** There is no slug input anywhere in this component. Choosing a type calls
`onChoose(type)`; the caller derives the slug with `slugForType` and the phase with `minimalPhaseFor`.
State in the component docblock why: the slug is node identity (`node.id === phase.slug`) and a
`skip_to_phase` target, so renaming needs a cascade nobody asked for; the plain-language title is what
the user names.

Keyboard and dismissal: Escape and a click outside both call `onDismiss`; Enter/Space on an enabled row
chooses it; a disabled row is not focusable-as-activatable and announces its reason to assistive
technology via `aria-describedby` pointing at the visible reason text.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `StepTypePicker.tsx` exports `StepTypePicker` and renders exactly six rows for any `(phases, index)`
    - `grep -nE 'fetch\(|workflows/validate|from "@/lib/api"' frontend/src/components/workflows/StepTypePicker.tsx` returns no matches
    - `grep -n 'renderPhaseMark' frontend/src/components/workflows/StepTypePicker.tsx` returns >= 1 and there is no hardcoded unicode glyph fallback in the component
    - There is no `input`, `textarea` or `contenteditable` for a slug anywhere in the component — `grep -ci 'slug' frontend/src/components/workflows/StepTypePicker.tsx` shows no form field bound to a slug
    - Every `disabledReason` rendered comes from `allowedTypesAt`; `grep -cE 'would (strand|come after)' frontend/src/components/workflows/StepTypePicker.tsx` returns 0 (no reason string is authored here)
    - `useTechnicalNamesOptional` is used, not `useTechnicalNamesContext`
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>The picker offers all six types in plain language with 3D marks, renders stranding choices disabled with a visible reason it did not author, has no slug field, and touches no network.</done>
</task>

<task type="auto">
  <name>Task 3: StepTypePicker.test.tsx — R10b's component half and the zero-network proof</name>
  <files>frontend/src/components/workflows/StepTypePicker.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/StepTypePicker.tsx (as written in Task 2)
    - frontend/src/components/workflows/PhaseSpine.test.tsx (the provider-less leaf-render analog — this component sits OUTSIDE the React Flow plane, so `user-event` is the right driver here)
    - frontend/src/components/workflows/WorkflowCanvas.test.tsx `:120-137` — the driver rule and WHY it does not apply outside the plane
    - 184-VALIDATION.md — the R10b and R10 rows
  </read_first>
  <action>
Create `frontend/src/components/workflows/StepTypePicker.test.tsx`. Render with plain `render()` — no
`ReactFlowProvider`, no `mockReactFlow()`. Drive with `@testing-library/user-event`, which is correct
here because the picker sits OUTSIDE the React Flow plane (inside the plane a `user-event` click
dispatches a real `mousedown`, reaches d3-zoom, and d3-drag dereferences a null `event.view` — a green
summary with a non-zero exit code).

Assertions:
1. Six rows render for a 2-step definition, in the fixed order, each with its plain-language sentence.
2. **R10b**: on a definition whose `llm_emit` deliverable sits at position 2, inserting at index 2 or
   later renders the stranding choice(s) **disabled with a visible reason**. Assert the row has
   `aria-disabled="true"` AND that the reason text is present in the DOM — not in a `title` attribute
   only. Assert clicking it does NOT call `onChoose`.
3. On a definition with no `llm_emit`, no row is disabled and every row calls `onChoose` with its type.
4. **No option is ever hidden**: for every index tested, exactly six rows are in the DOM.
5. **R10 — zero network**: `vi.spyOn(globalThis, "fetch")` records 0 calls across the whole suite, and a
   `?raw` source guard (the `canvasModel.purity.test.ts:14-17` idiom) asserts the component source
   contains no `fetch(`, no `workflows/validate` and no `from "@/lib/api"`. Add a **positive control**
   proving the regex can match, in the house style.
6. **D-184-11**: no element in the rendered DOM is an editable slug field — assert
   `container.querySelectorAll('input, textarea, [contenteditable]')` has length 0.
7. Dismissal: Escape calls `onDismiss`; Enter on an enabled row calls `onChoose`.
8. Technical reveal: rendered without a `TechnicalNamesProvider`, the component shows plain language,
   renders no reveal control, and does not crash.
9. The 3D mark renders as an element, not as a text glyph — assert the row contains an `svg` (or the
   mark component's rendered element) rather than a bare unicode character.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/StepTypePicker.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/workflows/StepTypePicker.test.tsx` passes with 0 failures AND exit code 0
    - `grep -cE '@xyflow|mockReactFlow|ReactFlowProvider' frontend/src/components/workflows/StepTypePicker.test.tsx` returns 0
    - The R10b test asserts BOTH `aria-disabled="true"` and the visible reason text, and asserts `onChoose` was not called
    - Every tested index renders exactly 6 rows
    - The `fetch` spy reports exactly 0 calls, and the `?raw` guard's positive control passes
    - The no-editable-slug assertion is present and passes
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
  </acceptance_criteria>
  <done>R10b's component half is proven: stranding choices are disabled with a visible reason, nothing is hidden, no slug is editable, and the picker provably never consults the server.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared browser origin -> `localStorage` | Two users on one machine share an origin; the nudge map must not cross between them |
| author-supplied phase data -> picker rows | Phase titles and disabled reasons render as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-07-01 | Information disclosure | `canvasNudge` key namespace | mitigate | The key is scoped per user id AND per draft id (`agentic-rag.canvas-nudge.v1.<user_id>.<draft_id>`), reusing the shipped `getCurrentUserIdSync()`. A `dy` is cosmetic and low-value, but keying it per user is free and asserted by a two-user isolation test |
| T-184-07-02 | Tampering | `dy` reaching the definition | mitigate | `canvasNudge.ts` imports neither the builder store nor `canvasModel` nor `@/lib/api` (asserted by grep), so a layout value cannot reach the definition JSONB or a request body. No migration is added; slot 114 stays RESERVED |
| T-184-07-03 | Denial of service | `localStorage` quota / private mode | mitigate | Defensive read returns `{}` on any failure; the writer catches `QuotaExceededError`, warns and falls through. A storage failure degrades the cosmetic layer and never crashes the authoring surface |
| T-184-07-04 | Repudiation | a client refusal mistaken for a server verdict | mitigate | The picker authors no reason string of its own — every `disabledReason` comes from `definitionOps.allowedTypesAt`, and a `?raw` guard plus a 0-call `fetch` spy prove the picker never consults `/validate` (D-182-06) |
| T-184-07-05 | Tampering | rendered reason / title text | mitigate | Plain React text children only; no `dangerouslySetInnerHTML` anywhere in the component |
| T-184-07-SC | Tampering | npm installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx` — 0 failures, exit code 0
- `node scripts/vitest-count-gate.cjs` — exit 0, all 16 baseline counts held
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `git diff --name-only -- supabase/migrations | wc -l` returns 0 (R3 — slot 114 stays RESERVED)
- `git diff --name-only -- backend/ | wc -l` returns 0
- `grep -c 'localStorage' frontend/src/pages/WorkflowBuilderPage.tsx` returns 0
</verification>

<success_criteria>
- The cosmetic `dy` lives in one browser-local module, scoped per user and per draft, issuing zero network calls and adding zero migrations
- An unsaved draft persists nothing at all
- The picker offers all six step types in plain language with 3D marks, disabling stranding choices with a visible reason it did not author
- Neither module can reach `/workflows/validate`, proven by a `?raw` guard with a positive control and a 0-call `fetch` spy
- No slug is editable anywhere in the picker
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-07-SUMMARY.md` when done.
It MUST state the resolved nudge key shape and confirm that zero migration files were added.
</output>
