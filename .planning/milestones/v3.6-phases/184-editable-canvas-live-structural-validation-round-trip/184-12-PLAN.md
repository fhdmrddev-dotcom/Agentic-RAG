---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 12
type: execute
wave: 8
depends_on: ["184-07", "184-10", "184-11"]
files_modified:
  - frontend/src/components/workflows/WorkflowCanvas.tsx
  - frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
autonomous: true
requirements: [CANVAS-02, VALID-02]

must_haves:
  truths:
    - "R1: hovering the line between two steps reveals a ＋ at exactly the insertion point; ✕ removes a step and the line re-stitches; after every add / insert / delete phase_index is exactly [0..n-1]"
    - "D-184-11: the ＋ opens the plain-language step-type picker at the insertion point, a stranding choice is offered DISABLED with its reason inline, and choosing inserts a minimal valid phase there and opens PhaseFormPanel on it"
    - "D-184-12: delete is IMMEDIATE with Undo inline on the message — 'Removed <title> · N steps renumbered' — and there is NO confirm dialog; the R10a orphaning case is a REFUSAL with a stated reason, which is a different act and must read differently"
    - "R10: neither refusal consults /workflows/validate — both are decidable from the local shape, and no refusal is silent"
    - "The ＋ and ✕ live on the plane/lane, never inside the node DOM — the shipped no-focusable-control-inside-a-node assertion still holds"
    - "U-1: the empty draft gets a named 'Add your first step' invitation, not a bare ＋ and not a broken screen"
  artifacts:
    - path: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      provides: "The insert / delete affordances and the empty-draft invitation"
      contains: "onInsertAt"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      to: "frontend/src/components/workflows/StepTypePicker.tsx"
      via: "mounted at the insertion point; every disabledReason comes from definitionOps.allowedTypesAt"
      pattern: "StepTypePicker"
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/components/workflows/definitionOps.ts"
      via: "canRemovePhase gates the delete before the store action runs"
      pattern: "canRemovePhase"
---

<objective>
Make the canvas grow: a `＋` on the line where the step will land, a `✕` that removes one and re-stitches
the flow, and a first-run invitation on the empty draft that reads as an invitation rather than a broken
screen.

Purpose: the operator named the core action at intake — **grow a flow step-by-step** — so the add
affordance is the hero and the empty canvas is a first-class screen, not an edge case. Sketch 138-A's
finding is why the `＋` sits ON the line: the question "where does this go?" is answered before you pick
anything. And because the projection draws its sequential edge by a `phase_index + 1` LOOKUP, insert is
never cosmetic — it renumbers, and the surface says how many moved.

Output: both affordances, the picker mount, the two refusals in the UI, the delete message with inline
Undo, and the empty-draft invitation.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
@.planning/sketches/138-growing-the-flow/README.md
@.planning/sketches/139-validation-while-building/README.md
@.planning/sketches/themes/canvas-184.css
</context>

<interfaces>
<!-- Verified this session. -->

`WorkflowCanvas.test.tsx:231-238` — the machine-checked invariant that DECIDES where these affordances
live: for every `.react-flow__node`, `node.querySelectorAll("button, a, [tabindex]")` must have length 0.
**The ＋ and ✕ therefore live on the plane / the lane, never inside the card.** Under 137-B
(`themes/canvas-184.css` `body.card-b`) the card's TOP edge belongs to the floating icon, the RIGHT edge
carries the verdict mark, and per-node actions belong on the BOTTOM edge — rendered as plane-level
overlays positioned against the lane, not as children of the node.

`WorkflowCanvas.tsx:302-310` — `deleteKeyCode={null}` stays: **the ✕ is the delete affordance, not
Backspace.** All five kept read-only opt-outs and `<Controls showInteractive={false} />` (`:334`) survive.

`frontend/src/components/workflows/definitionOps.ts` (184-02):
- `canRemovePhase(phases, slug): { ok: true } | { ok: false; reason: string }` — R10a, grounded on
  `parseSkipTarget`, because with `renumber` running immediately after a delete index contiguity
  self-heals and the genuine orphan is a dangling `skip_to_phase` target
- `allowedTypesAt(phases, index)` — R10b; returns ALL six types, some carrying a `disabledReason`
- `slugForType`, `minimalPhaseFor`

`frontend/src/components/workflows/StepTypePicker.tsx` (184-07) — props
`{ phases, index, onChoose, onDismiss, open }`. It authors no reason of its own.

`frontend/src/components/workflows/builderStore.ts` — `insertPhaseOfTypeAt(index, type)`,
`removePhaseBySlug(slug)`, and the temporal `undo()` reached through `store.temporal.getState()` (a
side-effect call, so `getState()` is correct there — only RENDERED values need a selector).

Locked wording (CONTEXT `<specifics>`): the delete message reads *"Removed **Write a section** · 2 steps
renumbered"* with **Undo** inline; the empty draft's invitation is *"Add your first step"*.

jsdom rule: drive anything inside the React Flow plane with `fireEvent`, never `user-event`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The ＋ on the line, the picker at the insertion point, and R10b in the UI</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowCanvas.tsx (the whole file as left by 184-10 — the props interface, the view-copy, the module-scope constants rule `:99-104`, the read-only opt-outs `:302-310`)
    - frontend/src/components/workflows/WorkflowCanvas.test.tsx `:231-238` (the no-focusable-control-inside-a-node invariant)
    - frontend/src/components/workflows/StepTypePicker.tsx and definitionOps.allowedTypesAt / slugForType / minimalPhaseFor (184-07, 184-02)
    - frontend/src/components/workflows/canvasModel.ts `:62` — `CANVAS_LAYOUT`, for positioning the affordance against the lane
    - frontend/src/pages/WorkflowBuilderPage.tsx (as left by 184-11)
    - .planning/sketches/138-growing-the-flow/README.md — "Where the ＋ is" and why insert is never cosmetic
    - .planning/sketches/themes/canvas-184.css — the locked insert-affordance chrome
  </read_first>
  <action>
Add an `onInsertAt?: (index: number, type: PhaseTypeId) => void` prop to `WorkflowCanvasProps`, with a
docblock in the shipped justify-its-existence style. Render the insert affordance only when `editable`.

**Placement.** The `＋` renders as a plane-level overlay positioned against the connector between two
adjacent lane slots — computed from `CANVAS_LAYOUT`, at module scope per the `:99-104` rule — plus one
at the end of the flow. It is **not** a child of any node: the shipped `:231-238` assertion forbids a
focusable control inside a node, and that constraint is the reason this design is right rather than an
obstacle.

**Visibility** (Claude's discretion, recorded in CONTEXT): hover-revealed above 1024px, **always
visible under 1024px and on touch**. A hover-only affordance on a touch device is an affordance that
does not exist.

**Behaviour.** Activating a `＋` opens `StepTypePicker` at that insertion point with
`phases` and the resolved `index`. The picker renders all six types with their plain-language sentences
and 3D marks; entries carrying a `disabledReason` from `definitionOps.allowedTypesAt` render disabled
with the reason inline (R10b). **The canvas authors no reason of its own and consults `/validate` for
nothing** — a refusal is a shape rule decided locally, a verdict is a server judgement, and the two must
read differently and stay separately sourced.

Choosing a type calls `onInsertAt(index, type)`. On the page, that handler:
1. calls `store.insertPhaseOfTypeAt(index, type)`, which derives the slug via `slugForType`, builds the
   phase via `minimalPhaseFor`, inserts and renumbers — so `phase_index` is contiguous `[0..n-1]`
   immediately;
2. **selects the new phase and opens `PhaseFormPanel` on it** (D-184-11: the type is chosen at add time
   because the panel conditions on `phase.config.phase_type` and offers no control that writes it);
3. states how many steps moved, in the same surface vocabulary as the delete message.

Add the page wiring for `onInsertAt` inside the `canvasEnabled` branch only.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.editing.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `WorkflowCanvasProps` gains `onInsertAt` and the affordance renders only when `editable` is true
    - The `＋` is NOT a descendant of any `.react-flow__node`: the shipped `:231-238` assertion still passes and a new assertion confirms the affordance's closest `.react-flow__node` ancestor is null
    - `grep -cE 'workflows/validate|grounding_mode' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0
    - `grep -cE 'would (strand|come after)' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0 (no reason string authored here)
    - Choosing a type calls `onInsertAt` with the correct `index` and `type`
    - After the page handler runs, `phase_index` is exactly `[0..n-1]` and the new phase is selected with the panel open
    - All five kept read-only opt-outs and `showInteractive={false}` are still present
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>The `＋` sits on the line where the step will land, opens the plain-language picker there, offers stranding choices disabled with a reason it did not author, and inserts a contiguous, selected, panel-open phase.</done>
</task>

<task type="auto">
  <name>Task 2: The ✕ delete, the re-stitch message with inline Undo, and R10a's refusal</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowCanvas.tsx (as extended in Task 1)
    - frontend/src/components/workflows/definitionOps.ts — `canRemovePhase` and its `reason` (184-02)
    - frontend/src/components/workflows/builderStore.ts — `removePhaseBySlug`, and the temporal `undo()`
    - frontend/src/pages/WorkflowBuilderPage.tsx (as left by Task 1)
    - .planning/sketches/139-validation-while-building/README.md — the folded-in C finding: prevention covers what is decidable from SHAPE, and the gate must never become the thing that stops someone building
    - 184-CONTEXT.md § D-184-12 — delete is immediate with inline Undo, NO confirm dialog, and the orphaning case is a refusal, not a confirm
  </read_first>
  <action>
Add an `onRequestRemove?: (slug: string) => void` prop and render a `✕` per phase node, again as a
**plane-level overlay on the lane, never inside the node DOM**, positioned on the card's BOTTOM edge per
`canvas-184.css` `body.card-b`. Render it only when `editable`. `deleteKeyCode={null}` stays — the `✕`
is the delete affordance, not Backspace.

On the page, `onRequestRemove(slug)` first calls `definitionOps.canRemovePhase(phases, slug)`:

- **`{ok: false}` — the R10a REFUSAL.** Do NOT delete. Surface the returned `reason` as a stated,
  non-blocking message. **This is a refusal, not a confirm** — it offers no "delete anyway" and it must
  read differently from the delete message. Nothing about it consults `/validate`.
- **`{ok: true}` — delete IMMEDIATELY, with no confirm dialog.** Call `store.removePhaseBySlug(slug)`,
  which removes and renumbers so `phase_index` stays contiguous and the line re-stitches. Then surface
  the locked message — *"Removed **{plain title}** · {n} steps renumbered"* — carrying an **inline Undo**
  action that calls the temporal `undo()`. A modal on the phase's most-used destructive act contradicts
  the sketch build rule; undo is the safety net it was built to be.
  - `{n}` is the count of phases whose `phase_index` actually changed. Compute it by comparing before and
    after, never by assuming "everything downstream".
  - After a delete, **selection moves to the following step, or the preceding one if the deleted step was
    last** (Claude's discretion, recorded in CONTEXT). If no phases remain, clear the selection and show
    the empty-draft invitation (Task 3).

Both messages render in the same region and are announced through the polite live region added in
184-10, so neither is silent for a screen-reader user.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The `✕` is NOT a descendant of any `.react-flow__node`; the shipped `:231-238` assertion still passes
    - `grep -c 'deleteKeyCode={null}' frontend/src/components/workflows/WorkflowCanvas.tsx` returns >= 1
    - A non-orphaning delete removes the phase with NO confirm dialog rendered at any point — assert no `role="dialog"` appears
    - The delete message text matches the pattern `Removed <title> · <n> steps renumbered` with the correct `n` computed from an actual before/after index comparison
    - The message carries an activatable **Undo** control whose activation restores the previous `phases`
    - An orphaning delete (a surviving `skip_to_phase` target being removed) does NOT delete, renders the `reason` from `canRemovePhase`, and offers no delete-anyway control
    - The refusal and the delete message render as visibly different treatments — assert they use different testIds/roles
    - After a delete, selection is the following step, or the preceding one when the deleted step was last
    - `grep -cE 'workflows/validate' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>Delete is immediate with a re-stitch message and inline Undo, the orphaning case is refused with a stated reason that reads differently, and neither path touches the server.</done>
</task>

<task type="auto">
  <name>Task 3: The empty-draft invitation and the add/delete proof suite</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx, frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowCanvas.tsx (as extended in Tasks 1-2)
    - frontend/src/components/workflows/canvasModel.ts `:203-213` — the empty-projection contract (an empty definition returns empty arrays: no end cap, no ghost node, no chrome — D-183-11, the single most common canvas state)
    - frontend/src/components/workflows/WorkflowCanvas.editing.test.tsx (as built in 184-10 — APPEND, do not edit its assertions)
    - frontend/src/components/workflows/WorkflowCanvas.test.tsx `:120-137` — the fireEvent rule
    - .planning/sketches/141-the-authoring-session/README.md — "What to Look For" item 1: does the first move read as an invitation or as a broken screen?
    - 184-VALIDATION.md — the U-1 and U-2 G-4 rows these assertions partly cover
  </read_first>
  <action>
**The empty-draft invitation (U-1).** When `editable` is true and the projection is empty, render a named
invitation — *"Add your first step"* — as the plane's content, activating the same `StepTypePicker` at
index 0. **Not a bare `＋`**, and not the read-only empty state: 40-plus of the real definitions have zero
phases, so this is the single most common canvas state and it must read as an invitation, not as a broken
screen. When `editable` is false, the shipped read-only empty state renders exactly as today.

APPEND to `WorkflowCanvas.editing.test.tsx` (do not edit its 184-10 assertions). Use the file-local
`mockReactFlow()` and drive the plane with `fireEvent`, never `user-event`. Check the process EXIT CODE,
not just the pass count.

1. **R1 insert-at-middle.** On the 5-phase `eval_coverage` shape, activate the `＋` between positions 1
   and 2, choose a type, and assert the resulting `phase_index` array is exactly `[0,1,2,3,4,5]` and that
   **every downstream index incremented by exactly 1** — asserted index by index, not in aggregate.
2. **R1 delete re-stitch.** Delete a middle step and assert the resulting indices are `[0..n-1]` with no
   duplicate and no hole, and that the message names the correct moved count.
3. **R10a.** An orphaning delete is refused with a visible reason and the `phases` array is unchanged; a
   non-orphaning delete proceeds. Assert both in the same suite so the contrast is explicit.
4. **R10b.** The picker opened at an index that would strand the deliverable renders that entry disabled
   with a visible reason, and activating it does not insert.
5. **R10 — zero network on either refusal.** A `fetch` spy records 0 calls across a refused delete and a
   refused-choice interaction.
6. **No confirm dialog.** A successful delete renders no `role="dialog"` at any point.
7. **Undo restores.** Activating the inline Undo on the delete message restores the previous `phases`
   array (`toStrictEqual` its pre-delete clone) and the history depth decreases by one.
8. **The affordances are outside the node DOM.** For both the `＋` and the `✕`, assert
   `affordance.closest(".react-flow__node")` is null, and re-assert the shipped
   `WorkflowCanvas.test.tsx:231-238` invariant inside this suite too.
9. **Empty draft.** With zero phases and `editable: true`, the named invitation renders and activating it
   opens the picker at index 0. With `editable: false`, the shipped read-only empty state renders and no
   invitation appears.
10. **Touch / narrow viewport.** With a narrow container, the `＋` is present without a hover event —
    assert it is in the DOM and not hidden behind a hover-only class.

APPEND to `WorkflowBuilderPage.canvas.test.tsx` one page-level assertion: after an insert, the new
phase is selected and `PhaseFormPanel` is open on it (D-184-11's "choosing inserts a minimal valid phase
there and opens the panel on it").
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.editing.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - The suites pass with 0 failures AND exit code 0
    - The insert-at-middle test asserts each downstream index individually incremented by exactly 1
    - The delete test asserts contiguous `[0..n-1]` and the correct moved count in the message
    - The orphaning-delete test asserts the `phases` array is unchanged and a reason is visible
    - The `fetch` spy reports 0 calls across both refusal interactions
    - No `role="dialog"` renders during a successful delete
    - The Undo test asserts `toStrictEqual` against the pre-delete clone and a history-depth decrease
    - Both affordances have a null `.react-flow__node` ancestor
    - The empty-draft invitation renders only when `editable` is true
    - `git diff` on the 184-10 portion of `WorkflowCanvas.editing.test.tsx` shows only ADDED lines
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>R1's insert and delete are proven index by index, both cheap refusals are proven silent-free and network-free, undo restores exactly, both affordances live outside the node DOM, and the empty draft invites rather than looking broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture -> destructive definition change | Delete is the phase's most-used destructive act and has no confirm dialog by design |
| local shape predicate -> user-facing refusal | A client refusal must never be mistakable for a server verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-12-01 | Tampering | an accidental irreversible delete | mitigate | Delete is immediate but always recoverable: the message carries an inline Undo backed by the `zundo` history, and the test asserts the restore is `toStrictEqual` the pre-delete array. A modal would cost more than it protects and contradicts the sketch build rule |
| T-184-12-02 | Repudiation | a client refusal read as a server verdict | mitigate | Both refusals are pure shape predicates from `definitionOps`; the canvas authors no reason text and never contains the string `workflows/validate`. The refusal and the delete message use different treatments and testIds so they cannot be conflated |
| T-184-12-03 | Elevation of privilege | Backspace deleting a node via the library | mitigate | `deleteKeyCode={null}` stays, grep-asserted. The `✕` is the only delete path |
| T-184-12-04 | Tampering | an affordance breaking the node tab-stop contract | mitigate | Both affordances are plane-level overlays with a null `.react-flow__node` ancestor, asserted structurally, so the shipped one-tab-stop-per-node invariant is preserved |
| T-184-12-05 | Tampering | slug injection through the add path | mitigate | The slug is generated by `slugForType` from the CLOSED 6-member type set plus a numeric suffix; the picker has no slug field at all (asserted in 184-07) |
| T-184-12-SC | Tampering | npm installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx` — 0 failures, exit code 0
- `node scripts/vitest-count-gate.cjs` — exit 0, all 16 baseline counts held
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `git diff --name-only -- backend/ | wc -l` returns 0
- `git diff --name-only -- supabase/migrations | wc -l` returns 0
</verification>

<success_criteria>
- A `＋` on the line inserts a contiguous, selected, panel-open phase; a `✕` deletes immediately and re-stitches
- Every downstream index increments by exactly 1 on an insert-at-middle, proven index by index
- The orphaning delete is refused with a stated reason; the stranding add is disabled with a stated reason; neither touches the network
- Delete carries an inline Undo that restores exactly, with no confirm dialog anywhere
- Both affordances live outside the node DOM, preserving one tab stop per node
- The empty draft invites by name rather than presenting a bare control
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-12-SUMMARY.md` when done.
It MUST quote the exact delete-message and refusal strings as rendered, and confirm both affordances'
null `.react-flow__node` ancestor.
</output>
