---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 11
type: execute
wave: 7
depends_on: ["184-06", "184-07", "184-09", "184-10"]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.session.test.tsx
  - frontend/src/pages/WorkflowsPage.tsx
  - frontend/src/components/workflows/PublishGauntlet.tsx
autonomous: true
requirements: [CANVAS-02, CANVAS-03]

must_haves:
  truths:
    - "R6: a canvas editing session issues exactly ONE POST (the first save) and PATCH thereafter, through the UNCHANGED create-once-then-PATCH path; no new persistence endpoint, no autosave, and the saved-state wording contains no word implying published"
    - "R3: the serialized create/PATCH body contains no position, x, y or layout key — asserted on the actual payload with the shipped forbiddenKeysIn walk"
    - "D-184-15: no /validate call fires until the user's first edit; the empty draft's Publish reads as an invitation, not a claimed verdict"
    - "R12: Publish lives in the EXISTING page header — no net-new header band — and while ok:false it is disabled and NAMES the first blocking reason rather than only greying"
    - "D-184-16: all three session-edge debts are paid — the unsaved-work leave guard on the breadcrumb plus beforeunload, WR-09-01/WR-09-02 on ALL THREE dismissal paths, and the 409 WorkflowConflictError gets its own honest message"
    - "D-14: with visual_workflow_canvas off, the rails prop is genuinely ABSENT (not undefined) and no editing affordance is reachable anywhere"
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "The composed authoring session — live validation, rails, nudge, publish handoff, session edges"
    - path: "frontend/src/pages/WorkflowBuilderPage.session.test.tsx"
      provides: "R6 + R3 payload proof, the leave guard, the 409 message, and WR-09 on all three dismissal paths"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/hooks/useLiveValidation.ts"
      via: "enabled only after the user's first edit (D-184-15)"
      pattern: "useLiveValidation"
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "the Builder's dirty state"
      via: "a canLeave callback the Builder registers — there is no router, so no router blocker exists"
      pattern: "backToLibrary"
---

<objective>
Compose the authoring session: wire the live validation loop, the governance rails and the cosmetic
nudge into the page, hand publish its blocking reason in the header that already exists, and pay down
all three session-edge debts.

Purpose: 141-B put editing controls where editing happens and the way out where leaving happens — with
the operator's correction that publish belongs in the **existing** `← Workflows` header rather than a
new band. D-184-16 re-opens the 183 debts because a session can now be five structural edits deep with
no autosave until Phase 186: an Escape that drops a focused field's pending value was accepted debt when
the panel was secondary; it is not once the panel is the primary authoring surface.

Output: the page's composition, `PublishGauntlet`'s optional `blockedReason`, the leave guard spanning
`WorkflowsPage`, and the session proof suite. **Persistence itself is unchanged** — Phase 186 rewrites
that seam.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
@.planning/sketches/141-the-authoring-session/README.md
</context>

<interfaces>
<!-- Verified this session. -->

`frontend/src/pages/WorkflowBuilderPage.tsx`:
- the panel mount `:640-649` — add `rails` **spread-conditionally** so the prop is genuinely ABSENT on
  the flag-off path, never `undefined`: `{...(canvasEnabled ? { rails } : {})}`
- the publish seam `:630` — `{renderPublish && <div>{renderPublish(state.definition, draftId)}</div>}`
  with its typed prop at `:125`: `renderPublish?: (def, draftId) => React.ReactNode`
- `onSaveDraft` `:374-394` — the save-state machine (`saving` -> `saved` -> idle after 2500 ms) whose
  bare `setSaveState("error")` branch gains the 409 message
- the Escape listener `:224-231` and `clearSelection` `:213`
- the flag gate `:190-201` and the lazy import `:86` — both regex-pinned, DO NOT reformat
- the page source must still contain no `localStorage`, no `sessionStorage`, no `aria-pressed`, no
  `reactflow` (`WorkflowBuilderPage.canvas.test.tsx:414-438`)

`frontend/src/pages/WorkflowsPage.tsx`:
- `backToLibrary` at **`:290-294`** (NOT `:311-318` — that is the button JSX at `:314-318`)
- the `← Workflows` button at `:314-318`
- `renderPublish` is supplied from here at `:335`
- **there is no router** — navigation is a `useState<ActiveView>` switch, so no router blocker exists and
  the guard must hang off the breadcrumb handler plus `beforeunload`

`frontend/src/components/workflows/PublishGauntlet.tsx` — the `◆ Publish…` trigger at `:731-738`. It has
no notion of a verdict; it gains an optional `blockedReason?: string | null` (absent ⇒ today's
behaviour), threaded from the store through `renderPublish`.

`frontend/src/lib/api.ts:3295-3311` — `WorkflowConflictError` (409) is already thrown by
`updateWorkflowDraft`; D-184-16 only gives it a message.

`frontend/src/components/workflows/PhaseFormPanel.tsx` — the persist seam is `onBlur={onPersist}` on
every field (e.g. `:378`). The three dismissal paths are: ✕ -> `onClose` (`:464`); Escape ->
`WorkflowBuilderPage.tsx:224-231`; pane-click -> `WorkflowCanvas.tsx:326` (`onPaneClick={onClearSelection}`).

`canvasModel.purity.test.ts:35` — the shipped `forbiddenKeysIn` walk, reused here against the serialized
draft payload.

Locked wording: save reads *"Saved · still a draft"*; the empty draft's Publish reads
*"Add a step to get started"*; the 409 reads *"This version is published and can't be edited — use Tweak
to start a new draft"*.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire the live loop, the rails and the nudge into the page</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.tsx (the WHOLE file, as left by 184-04)
    - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx (all 22 assertions and the five source guards at `:414-438`)
    - frontend/src/hooks/useLiveValidation.ts (184-06) and frontend/src/hooks/useGroundingBundle.ts (184-09)
    - frontend/src/components/workflows/canvasNudge.ts (184-07) and verdictModel.ts (184-08)
    - frontend/src/components/workflows/WorkflowCanvas.tsx (the editing props added in 184-10)
    - frontend/src/components/workflows/canvasModel.purity.test.ts `:35` — the `forbiddenKeysIn` walk
  </read_first>
  <action>
Compose the page. Every net-new affordance lives inside the `canvasEnabled` branch (`:520-575`); the
else-branch stays literally `graphChild` with no wrapper element.

**Live validation (D-184-15).** Call `useLiveValidation(definition, enabled)` where `enabled` becomes
true only **after the user's first edit in this session** — track a `hasEditedRef`/store flag flipped by
the first structural or config action, not by mount. A zero-step draft therefore shows no tray and no
marks, and is never greeted with `ok:false` + `no_terminal`. Once a step exists the loop takes over and
the server owns every verdict from then on.

Feed the result into the store's untracked half (`setVerdicts` / `setChecking` / `setDegraded`) so a
single source drives both the marks and the tray — and remember `partialize` keeps all three out of the
undo stack, so an arriving response can never push a history entry.

**Marks to the canvas.** Build `marks` from `verdictModel.groupVerdicts(...).markFor` and pass it to
`WorkflowCanvas`. The canvas fetches nothing.

**Rails to the panel.** Call `useGroundingBundle(canvasEnabled)` and assemble
`rails = { order: {index, total}, toolOptions, gates }`, where `order` comes from the selected phase's
render position, `toolOptions` is the bundle's `tools` (or the literal `"degraded"` when the hook reports
`unavailable`), and `gates` is DERIVED exactly as the shipped `groundingFor()` does. Pass it to
`PhaseFormPanel` **spread-conditionally** — `{...(canvasEnabled ? { rails } : {})}` — so the prop is
genuinely ABSENT, not `undefined`, on the flag-off path. **Invent no `grounding_mode`.**

**Nudge.** Read `readNudges(draftId)` and pass it as `nudges`; pass `onNudge` writing through
`writeNudge(draftId, slug, dy)`. All storage stays inside `canvasNudge.ts` — the page must still contain
no `localStorage` token, which its own shipped guard enforces.

**Update any docblock this edit falsifies in the same commit.**

APPEND to `WorkflowBuilderPage.canvas.test.tsx` (do not edit its 22 shipped assertions):
- **R3 payload proof.** Drive an editing session with a mocked api client, capture the serialized body
  passed to `createWorkflowDraft` and `updateWorkflowDraft`, and run the shipped `forbiddenKeysIn` walk
  over it: **zero** `position` / `x` / `y` / `layout` keys at any depth. Add a **positive control** — a
  planted `position` key IS found — so the walk is proven to work here too.
- **D-184-15.** On mount with a zero-step draft, `validateWorkflow` is called **0 times** and no tray or
  mark renders. After the first `addPhaseOfType`, it is called (once, after the debounce).
- **D-14.** With the flag off, the `PhaseFormPanel` element receives NO `rails` key at all — assert on
  the props object, distinguishing absent from `undefined` — and no editing affordance is in the DOM.
- The five shipped source guards still pass unmodified.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.test.tsx --reporter=default</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx` shows only ADDED lines; all 22 shipped assertions pass unmodified
    - `grep -cE 'localStorage|sessionStorage|aria-pressed|reactflow' frontend/src/pages/WorkflowBuilderPage.tsx` returns 0
    - `grep -c 'visual_workflow_canvas === true' frontend/src/pages/WorkflowBuilderPage.tsx` returns >= 1 and the lazy-import line at `:86` is untouched in `git diff`
    - `grep -c 'grounding_mode' frontend/src/pages/WorkflowBuilderPage.tsx` returns 0
    - The rails prop is passed spread-conditionally: `grep -c 'canvasEnabled ? { rails }' frontend/src/pages/WorkflowBuilderPage.tsx` returns >= 1
    - The payload walk finds 0 forbidden keys and its positive control finds a planted `position`
    - The zero-step-draft test asserts `validateWorkflow` was called exactly 0 times on mount
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
  </acceptance_criteria>
  <done>The live loop, the rails and the nudge are composed on the page; no positional key can reach a draft payload; nothing validates before the first edit; and the flag-off panel receives no rails key at all.</done>
</task>

<task type="auto">
  <name>Task 2: The publish handoff — the existing header, disabled, naming the first reason</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/components/workflows/PublishGauntlet.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PublishGauntlet.tsx (the `◆ Publish…` trigger at `:731-738` and the props interface)
    - frontend/src/components/workflows/PublishGauntlet.test.tsx (all 24 assertions — none may be edited; they render without `blockedReason`)
    - frontend/src/pages/WorkflowBuilderPage.tsx `:125` and `:630` — the `renderPublish` prop and its mount
    - frontend/src/pages/WorkflowsPage.tsx `:335` — where `renderPublish` is supplied
    - .planning/sketches/141-the-authoring-session/README.md — the operator's correction (page header, not a bottom bar) and honesty beat 3 (publish is a handoff, not "run it and see")
  </read_first>
  <action>
Add `blockedReason?: string | null` to `PublishGauntlet`'s props, in the shipped optional-prop idiom (a
`?` plus a docblock naming its originating phase). **ABSENT ⇒ today's behaviour, byte-for-byte** — the
24 shipped `PublishGauntlet.test.tsx` assertions render without it and must pass unmodified.

When `blockedReason` is a non-empty string, the `◆ Publish…` trigger renders **disabled and names the
reason**: the reason text must be reachable through the control's accessible name OR as adjacent visible
text tied to it via `aria-describedby`. R12 is explicit that greying alone is not enough — a disabled
control with no stated reason is the exit hidden.

On the page, derive `blockedReason` and thread it through the existing `renderPublish` seam (`:630`)
rather than moving the mount. Do NOT add a header band: the Builder already has the `← Workflows`
breadcrumb header, and 141-B's operator correction is to reuse it. The workflow name and the `draft`
chip stay where they are.

The reason is chosen honestly:
- **Empty draft (D-184-15):** the reason is the plain INVITATION *"Add a step to get started"* — an
  invitation, not a claimed verdict, so this is not client-side validation and D-182-06 stays intact.
  Say that in the code comment; a reader must not mistake it for a locally-computed verdict.
- **Otherwise:** the FIRST verdict's `message`, taken verbatim from the server response, with `error`
  verdicts ordered before `incomplete` ones so the thing that needs attention now is named first. The
  message is never rewritten.
- **Degraded (D-184-14):** publish stays blocked and the reason is the degraded sentence for that cause.
  A check that did not run must never unblock publish.
- **`ok: true`:** no `blockedReason` at all, and publish behaves exactly as today.

The save-state wording in the same header region reads *"Saved · still a draft"* — no word implying
published, ever.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All 24 `PublishGauntlet.test.tsx` assertions pass unmodified; `git diff` on that file is empty
    - `blockedReason` is declared optional and its docblock states that absent means today's behaviour
    - With a `blockedReason`, the trigger is `disabled` AND the reason text is reachable via its accessible name or an `aria-describedby` target — asserted in the page suite
    - With an empty draft, the reason is exactly `Add a step to get started`
    - With verdicts present, the reason equals the FIRST verdict's `message` verbatim, with `error` ordered before `incomplete` — assert exact string equality against the mocked response
    - With a degraded state, publish is still disabled and the reason is the degraded sentence
    - `grep -ci 'publish' frontend/src/pages/WorkflowBuilderPage.tsx` shows no net-new header band element — confirm with `git diff` that the header structure gained no wrapper
    - The save-state string is exactly `Saved · still a draft`
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>Publish sits in the header that already exists, blocks while `ok:false` or degraded, and names a real first reason — an invitation on an empty draft and the server's own first message otherwise.</done>
</task>

<task type="auto">
  <name>Task 3: The three session-edge debts and the R6 save-session proof</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/pages/WorkflowsPage.tsx, frontend/src/pages/WorkflowBuilderPage.session.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowsPage.tsx `:280-340` — `backToLibrary` at `:290-294`, the `← Workflows` button at `:314-318`, `renderPublish` at `:335`
    - frontend/src/pages/WorkflowBuilderPage.tsx `:224-231` (Escape), `:347-394` (onPersist / onSaveDraft), `:396-401` (timer cleanup)
    - frontend/src/components/workflows/PhaseFormPanel.tsx — `onBlur={onPersist}` (e.g. `:378`) and the ✕ `onClose` call site `:464`
    - frontend/src/components/workflows/WorkflowCanvas.tsx `:326` — `onPaneClick={onClearSelection}`
    - frontend/src/lib/api.ts `:3295-3311` — the typed `WorkflowConflictError`
    - 184-CONTEXT.md § D-184-16 — all three debts, stated
  </read_first>
  <action>
**Debt 1 — the unsaved-work leave guard.** The `← Workflows` breadcrumb lives in `WorkflowsPage`
(`backToLibrary:290-294`), the dirty state lives in the Builder's store, and **there is no router**, so
no router blocker exists. Have the Builder register a `canLeave()` callback with `WorkflowsPage` via an
optional prop (e.g. `registerCanLeave?: (fn: () => boolean) => void`), which `backToLibrary` consults
before switching `pageView`. When the draft is dirty, it prompts and only leaves on confirmation. Add a
`beforeunload` listener inside the Builder for tab close, mounted only while `dirty` is true so it costs
nothing at rest — the shipped gated-listener shape (`WorkflowBuilderPage.tsx:224-231`).
Keep `WorkflowsPage`'s change minimal and additive: an absent `registerCanLeave` must leave
`backToLibrary` behaving exactly as today.

**Debt 2 — WR-09-01 / WR-09-02, on ALL THREE dismissal paths.** The panel persists on `onBlur`, so a
dismissal that unmounts a focused field drops its pending value. Commit the focused field's pending
value before unmount on **✕ (`onClose`), Escape, and pane-click** — all three, not just ✕. Follow the
D-183-09-01 precedent: this is a **defect fix on the shipped Spine surface**, so it applies in BOTH views
and flag-independently; do not gate it behind `canvasEnabled`. Then put the WR-09-02 assertion — *a
dismissal must not PATCH a version* — on all three paths, not just ✕.

**Debt 3 — the 409.** In `onSaveDraft` (`:374-394`), replace the bare `setSaveState("error")` branch with
an `err instanceof WorkflowConflictError` check that surfaces the honest message *"This version is
published and can't be edited — use Tweak to start a new draft"*. Every other failure keeps today's
generic error state — a 409/404/network failure is still never swallowed as success.

Then create `frontend/src/pages/WorkflowBuilderPage.session.test.tsx` — a NET-NEW suite that does not
absorb `WorkflowBuilderPage.test.tsx` or `.canvas.test.tsx`. Mock `@/lib/api` as a WHOLE module,
enumerating every symbol the page's render path reaches. Use `fireEvent` for anything inside the React
Flow plane and `user-event` only for controls outside it.

Assertions:
1. **R6 — exactly one POST, then PATCH.** Drive add-step, config-edit, save, edit again, save again.
   Assert the api call log is exactly `[createWorkflowDraft, updateWorkflowDraft]` — one create, then
   patches. Assert `createWorkflowDraft` was called exactly once even across two rapid saves (the shipped
   `creatingRef` guard against the UNIQUE(slug, version) collision).
2. **R6 — no autosave.** A structural edit alone issues **zero** persistence calls; only the explicit
   save button does.
3. **R6 — the wording.** After a successful save the surface shows `Saved · still a draft`; assert no
   rendered string in the save region matches `/publish|published|live|shipped/i`.
4. **D-184-03 — undo re-marks dirty and never writes.** After a save, an undo flips the surface back to
   dirty and issues **zero** api calls.
5. **Leave guard.** With a dirty draft, activating `← Workflows` does NOT switch away until confirmed;
   with a clean draft it leaves immediately. Assert `beforeunload` is registered while dirty and removed
   when clean.
6. **WR-09-01 on all three paths.** Type into a panel field without blurring, then dismiss via ✕, via
   Escape and via a pane click — in three separate cases. In each, assert the pending value was committed
   to the definition before unmount.
7. **WR-09-02 on all three paths.** In each of those three cases, assert **zero** `updateWorkflowDraft`
   calls were issued by the dismissal itself — a dismissal must not PATCH a version.
8. **409.** With `updateWorkflowDraft` rejecting with `WorkflowConflictError`, the surface shows the
   honest published-row message; with a generic rejection it shows the existing error state.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowBuilderPage.session.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=default</automated>
    <automated>cd frontend && npm test</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pages/WorkflowBuilderPage.session.test.tsx` passes with 0 failures AND exit code 0
    - The api call log assertion shows exactly one `createWorkflowDraft` across the whole session
    - A structural edit alone issues 0 persistence calls
    - No rendered string in the save region matches `/publish|published|live|shipped/i`
    - The undo-after-save case issues 0 api calls and shows dirty
    - All three dismissal paths commit the pending field value AND issue 0 `updateWorkflowDraft` calls — six assertions total
    - The 409 case renders the exact string `This version is published and can't be edited — use Tweak to start a new draft`
    - `WorkflowsPage.tsx`'s `backToLibrary` behaves exactly as today when `registerCanLeave` is absent — assert with a render that does not supply it
    - `npm test` reports no NEW failures versus the pre-plan baseline; pre-existing rot (SEED-049, SEED-056) is named, not fixed
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
  </acceptance_criteria>
  <done>All three D-184-16 debts are paid, the save path is provably unchanged create-once-then-PATCH with no autosave, and no dismissal can either lose an edit or mint a PATCH.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `POST /workflows` and `PATCH /workflows/{id}` | The serialized draft body crosses to the server on explicit save |
| Builder session -> navigation away | Unsaved structural work can be lost without a guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-11-01 | Tampering | draft payload | mitigate | The shipped `forbiddenKeysIn` walk runs over the ACTUAL serialized body and finds zero `position`/`x`/`y`/`layout` keys at any depth, with a positive control proving the walk works. `extra="forbid"` on the server is the second line, not the first |
| T-184-11-02 | Repudiation | a dismissal silently minting a PATCH | mitigate | WR-09-02's "a dismissal must not PATCH a version" assertion is applied to all THREE dismissal paths, not just ✕ — the asymmetry was the accepted 183 debt this plan closes |
| T-184-11-03 | Denial of service | duplicate draft creation | mitigate | The shipped `draftIdRef`/`creatingRef` create-once guard is unchanged and re-asserted: two rapid saves still issue exactly one `createWorkflowDraft`, avoiding the UNIQUE(slug, version) 500 |
| T-184-11-04 | Spoofing | a blocked publish that reads as available, or a degraded check that unblocks it | mitigate | `blockedReason` is derived from the server response only; a degraded state keeps publish blocked; the empty-draft case is an explicit INVITATION and is commented as such so it is not mistaken for a client-computed verdict |
| T-184-11-05 | Information disclosure | 409 / error surfacing | mitigate | The 409 renders a business-plain sentence; the generic branch keeps today's non-swallowing behaviour. No server internals reach the surface |
| T-184-11-06 | Elevation of privilege | flag-off surface drift | mitigate | `rails` is passed spread-conditionally so the prop is genuinely ABSENT with the flag off; asserted on the props object, distinguishing absent from `undefined` |
| T-184-11-SC | Tampering | npm installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `cd frontend && npm test` — no new failures versus the pre-plan baseline
- `node scripts/vitest-count-gate.cjs` — exit 0, all 16 baseline counts held
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `git diff frontend/src/components/workflows/PublishGauntlet.test.tsx` is empty
- `git diff --name-only -- backend/ | wc -l` returns 0
- `git diff --name-only -- supabase/migrations | wc -l` returns 0
</verification>

<success_criteria>
- One POST then PATCH, no autosave, and no saved-state wording that implies published
- No positional key reaches any draft payload, proven on the real body with a positive control
- Nothing validates before the first edit; publish blocks with a real named reason, including on an empty draft and a degraded check
- All three dismissal paths commit the pending value and none of them PATCHes
- The leave guard covers both the breadcrumb and tab close, and is inert when clean
- With the flag off, the panel receives no `rails` key at all
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-11-SUMMARY.md` when done.
It MUST include the captured api call log from the R6 session test and the exact `blockedReason` strings
observed for the empty-draft, verdict and degraded cases.
</output>
