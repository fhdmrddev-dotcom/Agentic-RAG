---
phase: 184-editable-canvas-live-structural-validation-round-trip
plan: 13
type: execute
wave: 9
depends_on: ["184-08", "184-11", "184-12"]
files_modified:
  - frontend/src/components/workflows/CanvasToolbar.tsx
  - frontend/src/components/workflows/CanvasToolbar.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.tsx
  - frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.tsx
autonomous: true
requirements: [VALID-02, VALID-03]

must_haves:
  truths:
    - "R12: ONE bottom-edge region, two rows maximum — the toolbar row and the problems-tray summary row; the tray expands UPWARD from its summary, and no net-new header band is added"
    - "D-184-04: one window keydown listener, mounted only while the flagged canvas view is active, that YIELDS to input / textarea / contenteditable so native field-level undo still fixes a typo"
    - "D-184-01: the undo/redo AFFORDANCES are canvas-only and flag-gated, while the HISTORY is complete — a config edit made from the Spine is in the same stack"
    - "Pitfall 7 / zundo #207: canUndo and canRedo are read through a SELECTOR on store.temporal, never store.temporal.getState() in a rendered value, or the buttons never re-enable under React 19"
    - "R9: the tray summary states the two severities in separate words before the tray is opened, and a 3-incomplete / 0-error draft renders with zero destructive-token elements"
    - "R6: the toolbar's save state reads dirty then saving then 'Saved · still a draft' — never a word implying published"
  artifacts:
    - path: "frontend/src/components/workflows/CanvasToolbar.tsx"
      provides: "The floating canvas toolbar — undo / redo / save state"
      exports: ["CanvasToolbar"]
    - path: "frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx"
      provides: "The R12 one-bottom-region / two-rows-max / no-overflow structural proof"
  key_links:
    - from: "frontend/src/components/workflows/CanvasToolbar.tsx"
      to: "store.temporal"
      via: "useStore(store.temporal, s => s.pastStates.length > 0) — a selector, never getState() in render"
      pattern: "useBuilderTemporal"
---

<objective>
Close the session: put undo/redo and the save state where editing happens, fold the problems-tray
summary into the same bottom edge, and prove the whole thing holds at 900px.

Purpose: 141-B put editing controls on the canvas and the way out in the page header — but 139-A's tray
also lives at the bottom, so a naive build lands **two bottom bands**. The recorded composition fix is to
fold the tray's summary line into the bottom edge and let the tray expand upward from it: one region, two
rows maximum. And the toolbar is where `zundo`'s React-19 landmine bites — the buttons look right on
first render and never update if the temporal state is read with `getState()`.

Output: `CanvasToolbar`, the D-184-04 keyboard bindings, the composed bottom region, and the R12
structural proof. This is the last plan of the phase.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-SPEC.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-CONTEXT.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-PATTERNS.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-RESEARCH.md
@.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-VALIDATION.md
@.planning/sketches/141-the-authoring-session/README.md
@.planning/sketches/139-validation-while-building/README.md
@.planning/sketches/themes/canvas-184.css
</context>

<interfaces>
<!-- Verified this session. -->

`zundo` React-19 landmine (upstream issue #207, closed as working-as-intended), quoted from the
maintainer: *"Because you're using `getState()` within a React component, changes to `futureStates` and
`pastStates` never would force a re-render. […] you'll need to access `futureStates` and `pastStates`
with a selector for the values to be reactive."*
So: `useBuilderTemporal(s => s.pastStates.length > 0)` for `canUndo` and
`useBuilderTemporal(s => s.futureStates.length > 0)` for `canRedo`. Side-effect calls (`undo()`,
`redo()`) MAY use `store.temporal.getState()` because they are not rendered values — that asymmetry is
already the shipped house rule (`usePanelReconcile.ts:58-60` selector vs `:77` side-effect).
Also verified upstream: `undo(n)`/`redo(n)` splice `pastStates` IN PLACE then `set({...})`, so a
`.length` selector still fires while a selector returning the ARRAY may not.

`frontend/src/pages/WorkflowBuilderPage.tsx:217-231` — the gated window-listener precedent D-184-04
follows, with its "gated on state, costs nothing at rest and cannot accumulate across renders"
rationale. `WorkflowCanvas.tsx:234` carries the auto-repeat guard (`if (event.repeat) return`).

`frontend/src/components/workflows/ProblemsTray.tsx` (184-08) — props
`{ groups, phases, degraded, checking, open, onToggle, onJumpToStep }`; `summaryLine(groups)` is the
locked two-count wording.

`frontend/src/pages/WorkflowBuilderPage.tsx:374-394` — the save-state machine
(`saving` -> `saved` -> idle after 2500 ms) the toolbar renders.

`WorkflowCanvas.test.tsx:415-429` — the scope fence: no `workflows/validate`, no `grounding_mode`, no
`MiniMap`, no `hideAttribution`, and `showInteractive={false}` present. Everything the toolbar needs
arrives as props.

Locked wording: `Saved · still a draft`; the tray summary is the two-count form
*"1 problem · 2 things to finish"*.

R12's composition rule (141-B + the recorded composition risk): **one bottom region, two rows maximum.**
The width budget is real — a 400px panel beside 248px cards leaves roughly two steps visible at 900px,
which is why the shipped panel's 44px collapsed rail and <768px bottom sheet must be kept, not rebuilt.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CanvasToolbar — undo / redo through a selector, plus the honest save state</name>
  <files>frontend/src/components/workflows/CanvasToolbar.tsx, frontend/src/components/workflows/CanvasToolbar.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/BuilderStoreProvider.tsx (184-04) — `useBuilderTemporal` and its React-19 docblock
    - frontend/src/components/workflows/builderStore.ts (184-04) — `HISTORY_LIMIT`, the save-state field
    - frontend/src/hooks/usePanelReconcile.ts `:58-60` vs `:77` — the shipped selector-vs-side-effect asymmetry
    - frontend/src/components/org/StatusChip.tsx — the shared chip primitive the save state renders through
    - .planning/sketches/141-the-authoring-session/README.md — honesty beat 1 ("Saved ≠ published") and the operator's decision
    - .planning/sketches/themes/canvas-184.css — the locked toolbar chrome
  </read_first>
  <action>
Create `frontend/src/components/workflows/CanvasToolbar.tsx`.

Props: `{ saveState: "idle" | "dirty" | "saving" | "saved" | "error"; errorMessage?: string | null;
onSave: () => void; onTidyUp: () => void }`. The toolbar reads undo/redo state from the store through
`useBuilderTemporal` and calls `undo()` / `redo()` through `store.temporal.getState()`.

**The Pitfall 7 rule, stated in the component docblock and enforced by the test:** `canUndo` and
`canRedo` MUST be selector reads —
`useBuilderTemporal(s => s.pastStates.length > 0)` and `useBuilderTemporal(s => s.futureStates.length > 0)`.
`store.temporal.getState()` inside a rendered value is a snapshot, not a subscription, and React 19's
compiler correctly declines to re-render: the buttons would look right on first render and never update.
Select on `.length`, not on the arrays themselves — `undo()`/`redo()` splice in place.

Render:
- an **Undo** control, disabled when `canUndo` is false, with an accessible name naming the binding
- a **Redo** control, disabled when `canRedo` is false
- a **Tidy up** control calling `onTidyUp` (clears the current workflow's nudge key only)
- the **save state**, through the shared `StatusChip` primitive: `dirty` -> unsaved wording;
  `saving` -> in-progress wording; `saved` -> exactly **`Saved · still a draft`**; `error` -> the
  supplied `errorMessage` (which carries the 409 sentence when relevant). **No word implying published,
  ever** — autosave updates a draft row in place, does not mint a version and does not re-arm the
  gauntlet, and a word that implies "shipped" is a lie shown a hundred times a session.
- an explicit **Save draft** trigger calling `onSave` (R6: explicit save only; no autosave in this phase).

The toolbar renders no verdict of its own and issues no fetch.

Then create `CanvasToolbar.test.tsx` — plain `render()` inside a `BuilderStoreProvider` over a real
`createBuilderStore`, driven with `user-event` (the toolbar sits OUTSIDE the React Flow plane):
1. **Pitfall 7's falsification, the load-bearing test.** With zero history, Undo is disabled. Dispatch a
   structural action on the store, and assert Undo becomes enabled **without re-mounting the component**.
   Then temporarily rewrite the component to read `store.temporal.getState().pastStates.length` and
   confirm this test FAILS. Restore and record both observations in the SUMMARY — that is what discharges
   the landmine rather than trusting a comment about it.
2. Redo is disabled until an undo has happened, and becomes disabled again after a NEW edit (zundo clears
   `futureStates` on every push).
3. `saveState: "saved"` renders exactly `Saved · still a draft`; assert no rendered string in the
   component matches `/publish|published|live|shipped/i`.
4. `saveState: "error"` with an `errorMessage` renders that message.
5. Activating Undo calls the store's temporal undo and the store's `phases` reverts.
6. Tidy up calls `onTidyUp` exactly once.
7. A `fetch` spy records 0 calls.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/CanvasToolbar.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'temporal.getState()' frontend/src/components/workflows/CanvasToolbar.tsx` shows getState used ONLY inside event handlers, never in a rendered value — confirm by reading each occurrence's context and stating it in the SUMMARY
    - `grep -c 'useBuilderTemporal' frontend/src/components/workflows/CanvasToolbar.tsx` returns >= 2
    - The reactivity test passes without a remount, and the `getState()` falsification makes it fail — both recorded in the SUMMARY
    - `saveState: "saved"` renders exactly `Saved · still a draft` and nothing in the component matches `/publish|published|live|shipped/i`
    - Redo is disabled after a new edit following an undo
    - The `fetch` spy reports 0 calls
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>The toolbar's enabled state is genuinely reactive under React 19, proven by a falsified test, and its save wording never implies published.</done>
</task>

<task type="auto">
  <name>Task 2: D-184-04 — one gated window keydown listener that yields to text fields</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/components/workflows/CanvasToolbar.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.tsx `:217-231` — the shipped Escape listener: the gated-on-state pattern and its full "costs nothing at rest" rationale
    - frontend/src/components/workflows/WorkflowCanvas.tsx `:234` — the auto-repeat guard
    - frontend/src/components/workflows/builderStore.ts / BuilderStoreProvider.tsx (184-04)
    - 184-CONTEXT.md § D-184-04 — the bindings and the yield rule
  </read_first>
  <action>
Add ONE window `keydown` listener in `WorkflowBuilderPage.tsx`, following the shipped Escape-listener
shape at `:217-231` exactly: **mounted only while `canvasEnabled && activeGraphView === "canvas"`**, so
no listener exists on the Spine or with the flag off, it costs nothing at rest, and it cannot accumulate
across renders.

Bindings: `⌘Z` / `Ctrl+Z` undo, `⇧⌘Z` / `Ctrl+Y` redo. Both call the temporal actions through
`store.temporal.getState()` — a side-effect call, which is the correct place for `getState()`.

**It YIELDS to text fields.** Bail immediately when `event.target` is an `input`, a `textarea`, or an
element with `isContentEditable` — so native field-level undo still fixes a typo in a description rather
than reverting a structural edit the user did not have in mind. Reuse the auto-repeat guard
(`if (event.repeat) return`): one press is one step.

**Undo never writes to the server** (D-184-03) — the listener calls only store actions. Stepping back
past a save point re-marks the draft dirty via the store's existing subscription, which is honest: the
in-memory definition now differs from what was PATCHed.

Toolbar buttons remain the discoverable path; the keys are the accelerator, not the only way in.

Update the docblock of anything this edit falsifies in the same commit.

APPEND to `CanvasToolbar.test.tsx` (or a co-located page test, whichever keeps the suite cohesive) the
keyboard assertions:
1. `Ctrl+Z` on the flagged canvas view undoes the last structural edit
2. `Ctrl+Y` and `Shift+Meta+Z` both redo
3. A `keydown` whose `target` is an `input` does NOT undo — assert the store's `phases` is unchanged
4. A `keydown` with `repeat: true` does not undo
5. With the flag OFF, or with `activeGraphView === "spine"`, **no listener is registered** — assert by
   spying on `window.addEventListener` and checking no `keydown` handler was added by this code path
6. An undo issues **zero** api calls
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/CanvasToolbar.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The listener effect's dependency/guard includes both `canvasEnabled` and the active-view check — confirm by reading the effect body and quoting it in the SUMMARY
    - `Ctrl+Z` undoes; `Ctrl+Y` and `Shift+Meta+Z` both redo
    - A `keydown` targeting an `input` leaves `phases` unchanged
    - A `repeat: true` `keydown` leaves `phases` unchanged
    - With the flag off, the `window.addEventListener` spy records no `keydown` registration from this path
    - An undo issues 0 api calls
    - All 22 `WorkflowBuilderPage.canvas.test.tsx` and all 7 `revertByteIdentical.test.tsx` assertions still pass unmodified
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
  </acceptance_criteria>
  <done>One gated listener provides the four bindings, yields to text fields, never fires with the flag off, and never writes to the server.</done>
</task>

<task type="auto">
  <name>Task 3: The one bottom region — toolbar row + tray summary row — and the R12 proof</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx, frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowCanvas.tsx (the whole file as left by 184-12)
    - frontend/src/components/workflows/ProblemsTray.tsx (184-08) and CanvasToolbar.tsx (Task 1)
    - .planning/sketches/141-the-authoring-session/README.md — the decision and the composition note with 139-A
    - .planning/sketches/MANIFEST.md — the three composition risks the five picks create together (two bottom bands, the width budget, four chrome regions)
    - .planning/sketches/themes/canvas-184.css — the locked bottom-region chrome
    - frontend/src/components/workflows/PhaseFormPanel.tsx — the 44px collapsed rail and the <768px bottom sheet, both of which must be KEPT, not rebuilt
  </read_first>
  <action>
Compose the bottom region inside `WorkflowCanvas`, rendered only when `editable`.

**ONE bottom-edge region, TWO rows maximum:**
- **Row 1** — `CanvasToolbar` (undo / redo / tidy up / save state / save draft), floating on the canvas
  where editing happens.
- **Row 2** — the `ProblemsTray` **summary line**, folded into the same bottom edge. The tray expands
  **UPWARD** from that summary; it does not add a third row at rest and it does not push the toolbar.

That is the recorded fix for the composition risk the five sketch winners create together: 139-A's tray
and 141-B's bar both wanted the bottom. Publish stays in the EXISTING page header (184-11) — no net-new
header band.

**Do not rebuild the panel's responsive behaviour.** The shipped `PhaseFormPanel` already collapses to a
44px rail and becomes a bottom sheet under 768px, which is exactly why the width budget survives: a 400px
panel beside 248px cards leaves roughly two steps visible at 900px. Keep both.

**The tray does not auto-open on a new `error`** (Claude's discretion, recorded in CONTEXT) — the summary
line updates and the user opens it.

Add the page wiring inside the `canvasEnabled` branch: pass `saveState`, `onSave`, `onTidyUp`
(`clearNudges(draftId)`), the grouped verdicts, `degraded`, `checking`, the tray open state and
`onJumpToStep` (which selects the phase and opens the panel).

Then create `frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx`. Use the file-local
`mockReactFlow()`; drive the plane with `fireEvent` and the bottom-region controls with `user-event`.
jsdom cannot measure real overflow, so assert STRUCTURE here and leave the visual half to the G-4 live
rows.

Assertions:
1. **Exactly one bottom-edge region.** Query the canvas container for elements carrying the bottom-region
   role/testId and assert there is exactly **1**. Add a **positive control**: planting a second such
   element in the test DOM makes the assertion fail.
2. **Two rows maximum at rest.** The bottom region has exactly 2 direct row children when the tray is
   closed, and still 2 when it is open (the tray expands upward inside its own row rather than adding a
   third).
3. **No net-new header band**: assert the page's header region has the same number of direct children as
   the pre-plan baseline, and that Publish is inside it.
4. **R9 at rest.** With 3 `incomplete` and 0 `error` verdicts, the bottom region's `outerHTML` contains
   **zero** occurrences of the destructive token literal exported from `nodePresentation.ts`, and the
   summary line carries the two-count wording. Positive control: one `error` verdict makes the token
   appear.
5. **The tray expands upward and its rows jump**: opening it lists the verdicts, and activating a row
   calls `onJumpToStep` with the right slug.
6. **No auto-open**: a re-render introducing a new `error` leaves the tray closed.
7. **900px structural check**: render inside a 900px-wide container and assert no element inside the
   canvas column declares a fixed width greater than the container's, and that the panel's collapsed-rail
   affordance is reachable. Note in the docblock that true overflow measurement is the G-4 live row, not
   this assertion.
8. **`editable: false`** renders no bottom region at all — the read-only canvas is unchanged.
9. **The scope fence still holds**: the `WorkflowCanvas.tsx` source still contains no `workflows/validate`,
   no `grounding_mode`, no `MiniMap`, no `hideAttribution`, and still contains `showInteractive={false}`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/WorkflowCanvas.composition.test.tsx --reporter=default</automated>
    <automated>cd frontend && npx vitest run src/components/workflows src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.session.test.tsx src/components/admin/revertByteIdentical.test.tsx --reporter=default</automated>
    <automated>cd frontend && npm test</automated>
    <automated>node scripts/vitest-count-gate.cjs</automated>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - The composition suite passes with 0 failures AND exit code 0
    - Exactly one bottom-edge region is found, and the planted-second-element positive control fails the assertion
    - The bottom region has exactly 2 direct row children both closed and open
    - The R9 destructive-token scan returns 0 for the `incomplete`-only state and >= 1 for the positive control
    - The tray does not auto-open on a new `error`
    - `editable: false` renders no bottom region
    - `grep -cE 'workflows/validate|grounding_mode|MiniMap|hideAttribution' frontend/src/components/workflows/WorkflowCanvas.tsx` returns 0 and `showInteractive={false}` is still present
    - `npm test` reports no NEW failures versus the pre-plan baseline; pre-existing rot (SEED-049, SEED-056) is named, not fixed
    - `node scripts/vitest-count-gate.cjs` exits 0 with all 16 baseline counts held
    - `npx tsc -b 2>&1 | grep -c "error TS"` <= 33
    - `git diff --name-only -- supabase/migrations | wc -l` returns 0 and `git diff --name-only -- backend/ | wc -l` returns 0 for the WHOLE phase — verify with a diff against the pre-phase base commit and state the result in the SUMMARY
  </acceptance_criteria>
  <done>One bottom region carries two rows, the tray expands upward from its own summary, a mid-build draft carries no destructive token anywhere in it, and the read-only canvas is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| window keydown -> destructive history navigation | A global listener can revert work the user did not mean to revert |
| server verdict text -> the bottom-region summary | Server-authored strings render on a business-user surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-13-01 | Tampering | the global undo listener | mitigate | Gated on `canvasEnabled && activeGraphView === "canvas"`, so no listener exists on the Spine or with the flag off (asserted with an `addEventListener` spy); it yields to `input`/`textarea`/`contenteditable` so a field-level typo fix cannot revert a structural edit; and the auto-repeat guard makes one press one step |
| T-184-13-02 | Repudiation | undo silently writing to the server | mitigate | The listener and the toolbar call only store actions; asserted by a 0-api-call check on undo. D-184-03: an undo that auto-PATCHes is exactly the surprise a no-autosave phase exists to prevent |
| T-184-13-03 | Spoofing | a disabled control that lies about history depth | mitigate | `canUndo`/`canRedo` are selector reads on `store.temporal`; the reactivity test is falsified against the `getState()` form, so the React-19 landmine is discharged by evidence rather than by a comment |
| T-184-13-04 | Tampering | verdict text in the summary | mitigate | Rendered as plain React text children only; no `dangerouslySetInnerHTML` |
| T-184-13-05 | Elevation of privilege | flag-off surface drift | mitigate | The whole bottom region renders only when `editable` is true, which the page passes as `canvasEnabled`; `revertByteIdentical.test.tsx` stays green and the `editable: false` assertion pins the read-only render |
| T-184-13-SC | Tampering | npm installs | accept | This plan installs nothing. The phase's only net-new dependency was `zundo`, installed in 184-04 under its `[OK]` legitimacy verdict |
</threat_model>

<verification>
- `cd frontend && npm test` — no new failures versus the pre-plan baseline
- `node scripts/vitest-count-gate.cjs` — exit 0, all 16 baseline counts held
- `cd frontend && npx tsc -b 2>&1 | grep -c "error TS"` <= 33
- `cd frontend && git diff --exit-code -- 'src/**/__snapshots__/*'` exits 0 for the read-only render
- **Phase-wide, against the pre-phase base commit:** `git diff --name-only <base>..HEAD -- supabase/migrations | wc -l` returns 0 and `git diff --name-only <base>..HEAD -- backend/ | wc -l` returns 0
- The 5 G-4 live rows (U-1 … U-5) are driven by the operator at `/gsd:verify-work`, not here
</verification>

<success_criteria>
- One bottom region, two rows, the tray expanding upward from its own summary, and no net-new header band
- Undo/redo enabled state is genuinely reactive under React 19, proven by falsifying the `getState()` form
- Four keyboard bindings behind one gated listener that yields to text fields and never registers with the flag off
- A 3-`incomplete` / 0-`error` draft renders with zero destructive tokens anywhere in the bottom region
- The read-only canvas is unchanged with `editable: false`
- The phase closes with zero backend files and zero migration files changed
</success_criteria>

<output>
Create `.planning/phases/184-editable-canvas-live-structural-validation-round-trip/184-13-SUMMARY.md` when done.
It MUST include: (a) the Pitfall-7 falsification observations, (b) the quoted keydown-effect guard, and
(c) the phase-wide confirmation that `supabase/migrations` and `backend/` are untouched.
</output>
