---
phase: 041-ui-redesign-tool-call-visualizer-citations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/ToolCallPanel.tsx
  - frontend/src/components/chat/CitationCard.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "ToolCallPanel done-state wrapper uses bg-card/80 backdrop-blur-sm instead of bg-muted/30"
    - "ToolArgsBlock parameter block uses bg-card/50 backdrop-blur-md instead of bg-muted/30"
    - "ToolResultBlock result block uses bg-card/50 backdrop-blur-md instead of bg-muted/15"
    - "CitationCard left border is replaced by a w-0.5 gradient strip colored per file type"
    - "CitationCard file icon is color-coded: red for PDF, blue for DOCX, purple for Markdown"
    - "No prop interfaces, SSE parsing, or state management logic is changed"
  artifacts:
    - path: "frontend/src/components/chat/ToolCallPanel.tsx"
      provides: "Glassmorphic ToolCallPanel with upgraded wrapper and parameter blocks"
      contains: "bg-card/80 backdrop-blur-sm"
    - path: "frontend/src/components/chat/CitationCard.tsx"
      provides: "CitationCard with gradient left-accent strip and file-type icons"
      contains: "bg-gradient-to-b"
  key_links:
    - from: "ToolCallPanel.tsx"
      to: "done-state container div (line ~516)"
      via: "cn() conditional class"
      pattern: "bg-card/80 backdrop-blur-sm"
    - from: "CitationCard.tsx"
      to: "gradient strip div (first child of flex row)"
      via: "getFileType() helper"
      pattern: "w-0.5 self-stretch"
---

<objective>
Apply glassmorphic depth to ToolCallPanel and gradient left-accent border + file-type icons to CitationCard. All changes are additive Tailwind class modifications — no logic, prop, or state changes.

Purpose: Upgrade the visual treatment of tool call results and citation sources to match the Deep Midnight aesthetic established in other panels (HealthPanel, SuggestionPills).
Output: Two modified .tsx files. No new files created.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Vibe Apps/Agentic RAG/.planning/PROJECT.md
@C:/Vibe Apps/Agentic RAG/.planning/ROADMAP.md

<interfaces>
<!-- Reference patterns for glass depth classes (from established components) -->

From frontend/src/components/health/HealthPanel.tsx (pattern reference — do not modify):
- Card uses: `bg-card/50 ghost-border shadow-sm`
- Outer panels use lighter opacity glass

From frontend/src/components/chat/SuggestionPills.tsx (pattern reference — do not modify):
- Pills use: `bg-card/60 backdrop-blur-sm`

From frontend/src/components/chat/ToolCallPanel.tsx — current target classes:
- Root container done-state (line ~516–521): `bg-muted/30 ghost-border`
- ToolArgsBlock parameter block (line ~96): `rounded-md bg-muted/30 px-2.5 py-1.5 font-mono text-[10px]...`
- ToolResultBlock result block (line ~434): `rounded-lg bg-muted/15 p-2.5 ghost-border`

From frontend/src/components/chat/CitationCard.tsx — current target classes:
- Outer div (line 15): `border-l-2 border-muted-foreground/30 pl-3 bg-muted/30 rounded-r-md py-2 text-xs`
- FileText icon (line 18): `w-3 h-3 flex-shrink-0` (no color class)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ToolCallPanel glassmorphic wrapper and parameter blocks</name>
  <files>frontend/src/components/chat/ToolCallPanel.tsx</files>

  <read_first>
    - `frontend/src/components/chat/ToolCallPanel.tsx` — read the full file to locate exact line positions before touching anything
    - `frontend/src/components/health/HealthPanel.tsx` — confirm bg-card/50 ghost-border shadow-sm pattern as precedent
  </read_first>

  <action>
Make three targeted class changes in ToolCallPanel.tsx. No other code changes.

**Change 1 — Root container done-state (locate the cn() call at the bottom of the `return` block, ~line 516–521):**

Current done-branch class string:
```
"bg-muted/30 ghost-border"
```

Replace with:
```
"bg-card/80 backdrop-blur-sm ghost-border"
```

The full cn() expression should become:
```tsx
<div className={cn(
  "mb-3 rounded-xl overflow-hidden max-w-full text-sm transition-all duration-300",
  isActivelyWorking
    ? "bg-primary/5 border border-primary/20 shadow-[0_0_20px_-4px_hsl(239_84%_67%/0.15)]"
    : "bg-card/80 backdrop-blur-sm ghost-border"
)}>
```

Active state (`bg-primary/5 border border-primary/20 shadow-[...]`) is NOT changed — D-11 and D-12.

**Change 2 — ToolArgsBlock parameter block (~line 96):**

Current:
```tsx
<div className="mt-1 rounded-md bg-muted/30 px-2.5 py-1.5 font-mono text-[10px] leading-relaxed text-foreground/60 space-y-0.5 overflow-x-auto">
```

Replace `bg-muted/30` with `bg-card/50 backdrop-blur-md`:
```tsx
<div className="mt-1 rounded-md bg-card/50 backdrop-blur-md px-2.5 py-1.5 font-mono text-[10px] leading-relaxed text-foreground/60 space-y-0.5 overflow-x-auto">
```

**Change 3 — ToolResultBlock result block (~line 434):**

Current:
```tsx
<div className="mt-1.5 ml-4.5 rounded-lg bg-muted/15 p-2.5 ghost-border">
```

Replace `bg-muted/15` with `bg-card/50 backdrop-blur-md`:
```tsx
<div className="mt-1.5 ml-4.5 rounded-lg bg-card/50 backdrop-blur-md p-2.5 ghost-border">
```

No other lines in ToolCallPanel.tsx are modified. Do not touch: SubAgentBlock, ExecuteCodeBlock, tool row headers (the `flex items-center gap-2.5` rows), icon colors, animation classes.
  </action>

  <verify>
    <automated>grep -n "bg-card/80 backdrop-blur-sm ghost-border" "frontend/src/components/chat/ToolCallPanel.tsx" && grep -n "bg-card/50 backdrop-blur-md" "frontend/src/components/chat/ToolCallPanel.tsx" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep "bg-card/80 backdrop-blur-sm ghost-border" frontend/src/components/chat/ToolCallPanel.tsx` returns exactly 1 match (the done-state branch)
    - `grep "bg-card/50 backdrop-blur-md" frontend/src/components/chat/ToolCallPanel.tsx` returns exactly 2 matches (ToolArgsBlock and ToolResultBlock)
    - `grep "bg-muted/30" frontend/src/components/chat/ToolCallPanel.tsx` returns 0 matches for the parameter block line (the class was replaced, not appended)
    - `grep "bg-muted/15" frontend/src/components/chat/ToolCallPanel.tsx` returns 0 matches (the class was replaced)
    - `grep "bg-primary/5 border border-primary/20" frontend/src/components/chat/ToolCallPanel.tsx` still returns 1 match (active state unchanged)
    - Running `cd frontend && npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>

  <done>ToolCallPanel done-state uses bg-card/80 backdrop-blur-sm; both parameter and result inner blocks use bg-card/50 backdrop-blur-md. Active state unchanged. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade CitationCard gradient left-accent strip and file-type icons</name>
  <files>frontend/src/components/chat/CitationCard.tsx</files>

  <read_first>
    - `frontend/src/components/chat/CitationCard.tsx` — read the full file before editing; it is small (64 lines) and must be understood completely
    - `frontend/src/index.css` — verify that bg-muted/30 is a plain Tailwind utility (not a CSS class that needs to stay) to confirm removal is safe
  </read_first>

  <action>
Rewrite CitationCard.tsx with these changes. The component logic (useState, expanded/collapsed passage, show more/less) is UNCHANGED.

**Step 1 — Update lucide-react import to add FileCode:**
```tsx
import { ChevronDown, ChevronRight, FileText, FileCode } from "lucide-react"
```

**Step 2 — Add getFileType helper function above the component:**
```tsx
function getFileType(filename: string): "pdf" | "docx" | "md" | "default" {
  const ext = filename.split(".").pop()?.toLowerCase() ?? ""
  if (ext === "pdf") return "pdf"
  if (ext === "docx" || ext === "doc") return "docx"
  if (ext === "md" || ext === "markdown") return "md"
  return "default"
}
```

**Step 3 — Add getAccentClasses helper function (returns gradient and icon color):**
```tsx
function getAccentClasses(fileType: "pdf" | "docx" | "md" | "default") {
  switch (fileType) {
    case "pdf":
      return { gradient: "from-red-400/60 to-transparent", iconColor: "text-red-400" }
    case "docx":
      return { gradient: "from-blue-400/60 to-transparent", iconColor: "text-blue-400" }
    case "md":
      return { gradient: "from-purple-400/60 to-transparent", iconColor: "text-purple-400" }
    default:
      return { gradient: "from-muted-foreground/30 to-transparent", iconColor: "text-muted-foreground" }
  }
}
```

**Step 4 — Rewrite the CitationCard component body:**

Derive file type and accent at the top of the component:
```tsx
export function CitationCard({ citation }: Props) {
  const [expanded, setExpanded] = useState(false)
  const hasPassage = !!citation.passage
  const fileType = getFileType(citation.filename)
  const { gradient, iconColor } = getAccentClasses(fileType)
```

**Step 5 — Replace the outer div and add gradient strip as first child:**

Remove the old outer div:
```tsx
// OLD — remove this:
<div className="border-l-2 border-muted-foreground/30 pl-3 bg-muted/30 rounded-r-md py-2 text-xs">
```

Replace with a flex row containing the gradient strip and a content wrapper:
```tsx
<div className="flex gap-0 bg-muted/30 rounded-r-md py-2 text-xs">
  {/* Gradient left-accent strip per D-01, D-02 */}
  <div className={cn("w-0.5 self-stretch rounded-l-sm flex-shrink-0 bg-gradient-to-b", gradient)} />
  {/* Card content */}
  <div className="pl-3 flex-1 min-w-0">
    {/* Header */}
    <div className="flex items-center gap-1.5 text-muted-foreground font-medium">
```

The closing structure must be maintained: the passage section and show more/less button remain inside the content `<div className="pl-3 flex-1 min-w-0">`, and the outer flex row closes after all content.

**Step 6 — Replace the FileText icon with a type-specific colored icon:**

Current:
```tsx
<FileText className="w-3 h-3 flex-shrink-0" />
```

Replace with a conditional render using the fileType and iconColor:
```tsx
{fileType === "md"
  ? <FileCode className={cn("w-3 h-3 flex-shrink-0", iconColor)} />
  : <FileText className={cn("w-3 h-3 flex-shrink-0", iconColor)} />
}
```

The final file structure after all changes:
```tsx
import { useState } from "react"
import { ChevronDown, ChevronRight, FileText, FileCode } from "lucide-react"
import { cn } from "@/lib/utils"
import type { Citation } from "@/types"

interface Props {
  citation: Citation
}

function getFileType(filename: string): "pdf" | "docx" | "md" | "default" {
  const ext = filename.split(".").pop()?.toLowerCase() ?? ""
  if (ext === "pdf") return "pdf"
  if (ext === "docx" || ext === "doc") return "docx"
  if (ext === "md" || ext === "markdown") return "md"
  return "default"
}

function getAccentClasses(fileType: "pdf" | "docx" | "md" | "default") {
  switch (fileType) {
    case "pdf":
      return { gradient: "from-red-400/60 to-transparent", iconColor: "text-red-400" }
    case "docx":
      return { gradient: "from-blue-400/60 to-transparent", iconColor: "text-blue-400" }
    case "md":
      return { gradient: "from-purple-400/60 to-transparent", iconColor: "text-purple-400" }
    default:
      return { gradient: "from-muted-foreground/30 to-transparent", iconColor: "text-muted-foreground" }
  }
}

export function CitationCard({ citation }: Props) {
  const [expanded, setExpanded] = useState(false)
  const hasPassage = !!citation.passage
  const fileType = getFileType(citation.filename)
  const { gradient, iconColor } = getAccentClasses(fileType)

  return (
    <div className="flex gap-0 bg-muted/30 rounded-r-md py-2 text-xs">
      {/* Gradient left-accent strip per D-01, D-02 */}
      <div className={cn("w-0.5 self-stretch rounded-l-sm flex-shrink-0 bg-gradient-to-b", gradient)} />
      {/* Card content */}
      <div className="pl-3 flex-1 min-w-0">
        {/* Header */}
        <div className="flex items-center gap-1.5 text-muted-foreground font-medium">
          {fileType === "md"
            ? <FileCode className={cn("w-3 h-3 flex-shrink-0", iconColor)} />
            : <FileText className={cn("w-3 h-3 flex-shrink-0", iconColor)} />
          }
          <span className="truncate">
            {citation.filename}
            {citation.version_number != null && citation.version_number > 1
              ? ` (v${citation.version_number})`
              : ""}
          </span>
          <span className="text-muted-foreground/50 shrink-0">
            {citation.is_full_doc
              ? "Full document"
              : citation.chunk_index != null
                ? `Chunk ${citation.chunk_index + 1}`
                : ""}
          </span>
        </div>
        {/* Passage */}
        {hasPassage && (
          <div className="mt-1.5">
            <p
              className={cn(
                "text-muted-foreground italic leading-relaxed",
                !expanded && "line-clamp-2"
              )}
            >
              {citation.passage}
            </p>
            <button
              onClick={() => setExpanded(!expanded)}
              aria-expanded={expanded}
              aria-label={expanded ? "Show less" : "Show more"}
              className="flex items-center gap-1 mt-1 text-muted-foreground/60 hover:text-muted-foreground transition-colors"
            >
              {expanded ? (
                <>
                  <ChevronDown className="w-3 h-3" /> Show less
                </>
              ) : (
                <>
                  <ChevronRight className="w-3 h-3" /> Show more
                </>
              )}
            </button>
          </div>
        )}
      </div>
    </div>
  )
}
```
  </action>

  <verify>
    <automated>grep -n "bg-gradient-to-b" "frontend/src/components/chat/CitationCard.tsx" && grep -n "from-red-400/60" "frontend/src/components/chat/CitationCard.tsx" && grep -n "FileCode" "frontend/src/components/chat/CitationCard.tsx" && cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "border-l-2 border-muted-foreground/30" frontend/src/components/chat/CitationCard.tsx` returns 0 matches (old border removed)
    - `grep "bg-gradient-to-b" frontend/src/components/chat/CitationCard.tsx` returns 1 match (gradient strip div)
    - `grep "w-0.5 self-stretch" frontend/src/components/chat/CitationCard.tsx` returns 1 match
    - `grep "from-red-400/60 to-transparent" frontend/src/components/chat/CitationCard.tsx` returns 1 match (inside getAccentClasses)
    - `grep "from-blue-400/60 to-transparent" frontend/src/components/chat/CitationCard.tsx` returns 1 match
    - `grep "from-purple-400/60 to-transparent" frontend/src/components/chat/CitationCard.tsx` returns 1 match
    - `grep "FileCode" frontend/src/components/chat/CitationCard.tsx` returns at least 2 matches (import + usage)
    - `grep "text-red-400" frontend/src/components/chat/CitationCard.tsx` returns 1 match
    - `grep "getFileType" frontend/src/components/chat/CitationCard.tsx` returns at least 2 matches (definition + call)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>CitationCard renders a w-0.5 gradient left-accent strip (red/blue/purple/muted per file type) instead of a solid left border. File icon is FileCode (purple) for Markdown, FileText (red/blue/muted) for other types. Passage expand/collapse behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → render | citation.filename is user-supplied data rendered in the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-041-01 | Information Disclosure | CitationCard getFileType | accept | Extension parsing is read-only; no user input reaches DOM as HTML — filename rendered as text node via JSX, not dangerouslySetInnerHTML |
</threat_model>

<verification>
After both tasks complete:
1. `cd frontend && npx tsc --noEmit` exits 0
2. `cd frontend && npm run build` exits 0 (no Vite/Rollup errors)
3. Visual check: in browser, a done ToolCallPanel shows frosted glass card (bg-card/80 backdrop-blur-sm); expanding parameters shows a darker nested frosted block (bg-card/50 backdrop-blur-md)
4. Visual check: citation cards show colored left strip (red for .pdf, blue for .docx, purple for .md) and matching colored icon
</verification>

<success_criteria>
- ToolCallPanel done-state wrapper: `bg-card/80 backdrop-blur-sm ghost-border` (not `bg-muted/30`)
- ToolArgsBlock and ToolResultBlock inner blocks: `bg-card/50 backdrop-blur-md` (not `bg-muted/30` / `bg-muted/15`)
- ToolCallPanel active state unchanged
- CitationCard: no `border-l-2` class; has `w-0.5 self-stretch bg-gradient-to-b` strip div as first child
- CitationCard: file icons color-coded (PDF=text-red-400, DOCX=text-blue-400, MD=text-purple-400/FileCode)
- TypeScript compiles clean, Vite build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/041-ui-redesign-tool-call-visualizer-citations/041-01-SUMMARY.md`
</output>
