---
phase: 128-chat-tool-card-unification-chat-area-reclaim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/package-lock.json]
autonomous: false
requirements: [CTC-01]
user_setup: []

must_haves:
  truths:
    - "D-08: @lobehub/icons@^5.10.0 is a frontend dependency, installed via npm, pinned with the caret range, with the lockfile committed"
    - "Only .Color/.Mono marks are reachable — the lockfile does NOT gain antd or @lobehub/ui (the .Avatar dependency tree)"
    - "The install is gated behind a blocking checkpoint:human-verify (house supply-chain process) — slopcheck [OK], official lobehub repo, MIT, operator-specified"
  artifacts:
    - path: "frontend/package.json"
      provides: "@lobehub/icons dependency entry pinned ^5.10.0"
      contains: "@lobehub/icons"
    - path: "frontend/package-lock.json"
      provides: "Locked resolution for @lobehub/icons (committed)"
      contains: "@lobehub/icons"
  key_links:
    - from: "frontend/package.json"
      to: "node_modules/@lobehub/icons"
      via: "npm install"
      pattern: "@lobehub/icons"
---

<objective>
Install the `@lobehub/icons` npm package (the D-08 locked logo dependency) into the
frontend, behind the house supply-chain checkpoint. This is the ONLY net-new
dependency in Phase 128; every downstream plan that renders a provider logo imports
its `.Color`/`.Mono` marks via the shared `providerLogo.tsx` helper (Plan 03).

Purpose: ship REAL official per-provider brand marks (CTC-01) without vendoring SVGs
(operator rejected vendored SVGs in D-08) and without dragging `antd` (the `.Avatar`
trap — research Pitfall 3).
Output: `frontend/package.json` + `frontend/package-lock.json` gain `@lobehub/icons@^5.10.0`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Research — the package legitimacy audit + the antd-avoidance gotcha (READ before installing)
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md
# (§ "Package Legitimacy Audit", § "Standard Stack" → Installation + Version verification,
#  § "Peer-dependency note", Pitfall 3 ".Avatar import bloats the bundle with antd")
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain gate — verify @lobehub/icons before install</name>
  <read_first>
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md § "Package Legitimacy Audit" (the slopcheck [OK] row, the 7-maintainer / official-repo / MIT evidence) + § "Peer-dependency note" (why the unmet @lobehub/ui / antd peers are benign for our import surface)
    - frontend/package.json (the current dependency baseline — confirm @lobehub/icons is NOT yet present)
  </read_first>
  <action>Operator-driven supply-chain gate: review the npmjs.com page for @lobehub/icons and approve (or reject) the install of ^5.10.0 before Task 2 runs `npm install`. No code is written in this task — it is the blocking legitimacy checkpoint (never auto-approvable).</action>
  <what-built>
    Nothing built yet — this is the blocking supply-chain checkpoint that MUST precede
    the `@lobehub/icons` install (house process; legitimacy checkpoints are never
    auto-approvable). Research cleared it: slopcheck v0.6.1 rated `@lobehub/icons` `[OK]`;
    npm registry shows v5.10.0, MIT, published 2026-06-16 by the lobehub org (7
    maintainers incl. `arvinxx`, `canisminor1990`); repo `github.com/lobehub/lobe-icons`;
    the package name was operator-specified in D-08 (not Claude-discovered) on the correct
    ecosystem (npm). Tagged `[VERIFIED: npm registry]`.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@lobehub/icons — confirm: latest is 5.10.x, license MIT, repository points to github.com/lobehub/lobe-icons, weekly downloads non-trivial, last publish recent (≈2026-06).
    2. Confirm the package name is exactly `@lobehub/icons` (scope `@lobehub`, name `icons`) — no typosquat variant.
    3. Confirm you are approving the install of `^5.10.0` (caret range — patch/minor within 5.x only).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or describe a concern (e.g. a registry red flag) to halt.</resume-signal>
  <acceptance_criteria>
    Operator has typed "approved" after reviewing https://www.npmjs.com/package/@lobehub/icons.
    The checkpoint is recorded as resolved. (Never auto-approve — `workflow.auto_advance` is ignored for legitimacy gates.)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install @lobehub/icons@^5.10.0 + verify the lockfile stays antd-free</name>
  <read_first>
    - frontend/package.json (the dependency block — the install adds one line)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md Pitfall 3 (the lockfile MUST NOT gain antd / @lobehub/ui — that only happens if `.Avatar` is imported, which the helper never does)
  </read_first>
  <action>
    Run `cd frontend && npm install @lobehub/icons@^5.10.0` (the verbatim research Installation command). This adds `"@lobehub/icons": "^5.10.0"` to `frontend/package.json` dependencies and writes the locked resolution into `frontend/package-lock.json`. Do NOT import or render anything yet — this plan ONLY adds the dependency; the marks are consumed in Plan 03 (`providerLogo.tsx`) and Plan 04 (RunCard / ToolCallPanel). Per D-08, the consuming code will import ONLY `.Color`/`.Mono` sub-components, never `.Avatar` — so antd never becomes reachable. Confirm the lockfile did NOT pull `antd` or `@lobehub/ui` as resolved (installed) packages; the unmet PEER warnings for `@lobehub/ui`/`antd` are benign (research § Peer-dependency note) and are NOT a failure. Commit both `package.json` and `package-lock.json`.
  </action>
  <verify>
    <automated>cd frontend && node -e "const p=require('./package.json'); if(!p.dependencies['@lobehub/icons']) {console.error('MISSING @lobehub/icons');process.exit(1)}; console.log('dep ok:', p.dependencies['@lobehub/icons'])" && grep -q '@lobehub/icons' package-lock.json && echo 'lockfile has @lobehub/icons' && (grep -E '"node_modules/antd"|"node_modules/@lobehub/ui"' package-lock.json && { echo 'FAIL: antd/@lobehub-ui present in lockfile'; exit 1; } || echo 'OK: no antd / @lobehub/ui installed')</automated>
  </verify>
  <done>`@lobehub/icons@^5.10.0` is in package.json dependencies; package-lock.json resolves it; the lockfile contains NO `node_modules/antd` or `node_modules/@lobehub/ui` install entry. Both files committed.</done>
  <acceptance_criteria>
    `node -e` prints `dep ok: ^5.10.0` (caret range, not a pinned exact or a `*`).
    `grep '@lobehub/icons' package-lock.json` exits 0.
    The antd/@lobehub-ui grep finds NO install entry (prints `OK: no antd / @lobehub/ui installed`).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine / build | A third-party package (and its dependency tree) is fetched and executed at build/runtime. This is the supply-chain boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-SC | Tampering | `@lobehub/icons` npm install | mitigate | slopcheck `[OK]` + official lobehub repo + MIT + operator-specified in D-08 + correct ecosystem (npm); blocking `checkpoint:human-verify` (Task 1) before install; pin `^5.10.0`; commit the lockfile (Task 2). Legitimacy checkpoint is never auto-approvable. |
| T-128-01 | Tampering / DoS (bundle bloat) | transitive `antd` via `.Avatar` | mitigate | Consuming code (Plans 03/04) imports ONLY `.Color`/`.Mono` (pure SVG, zero deps); Task 2 verifies the lockfile gains no `antd` / `@lobehub/ui` install entry. |
</threat_model>

<verification>
- `frontend/package.json` lists `@lobehub/icons@^5.10.0`.
- `frontend/package-lock.json` resolves it and contains no antd / @lobehub/ui install entry.
- The supply-chain checkpoint was operator-approved (Task 1).
</verification>

<success_criteria>
`@lobehub/icons@^5.10.0` installed + lockfile committed + antd-free; downstream plans can import its `.Color`/`.Mono` marks.
</success_criteria>

<output>
Create `.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-01-SUMMARY.md` when done
</output>
