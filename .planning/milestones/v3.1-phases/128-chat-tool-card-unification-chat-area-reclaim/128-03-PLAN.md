---
phase: 128-chat-tool-card-unification-chat-area-reclaim
plan: 03
type: execute
wave: 2
depends_on: ['128-01']
files_modified: [frontend/src/lib/providerLogo.tsx, frontend/src/__tests__/lib/providerLogo.test.tsx, frontend/src/__tests__/lib/preparingDescription.test.ts]
autonomous: true
requirements: [CTC-01, TDP-02, CTC-02]

must_haves:
  truths:
    - "D-05: ONE shared helper frontend/src/lib/providerLogo.tsx holds BOTH the message.provider→logo map AND the preparing-description logic, so RunCard + ToolCallPanel import it instead of each growing a duplicate copy (the G-5 micro-extraction)"
    - "The logo map is keyed by the EXACT runs.provider strings — GLM is keyed 'zhipu', Kimi is keyed 'moonshot'; openrouter maps to the OpenRouter mark and is NOT unwrapped to the routed model brand (D-08)"
    - "lmstudio / unknown / undefined are deliberately ABSENT from the map → providerLogo() returns null so the caller renders the existing Bot fallback (D-08)"
    - "Only .Color/.Mono marks are imported from @lobehub/icons, never .Avatar (which drags antd); named imports only, never import *"
    - "preparingDescription(tc) prefers parsed tc.args.description when a non-empty string, else extracts description from the partial-JSON tc.argsCodeText (NOT tc.args, which is {} during preparing), returns null on absent, and NEVER throws in a render path (try/catch → null, the seamCardPayloadFor rule)"
  artifacts:
    - path: "frontend/src/lib/providerLogo.tsx"
      provides: "providerLogo(provider) → @lobehub/icons mark or null; preparingDescription(tc) → string|null"
      min_lines: 30
      exports: ["providerLogo", "preparingDescription"]
    - path: "frontend/src/__tests__/lib/providerLogo.test.tsx"
      provides: "CTC-01 map coverage: zhipu→Zhipu mark, lmstudio/undefined→null, openrouter-not-unwrapped"
    - path: "frontend/src/__tests__/lib/preparingDescription.test.ts"
      provides: "TDP-02 coverage: partial-JSON extraction, honest-null, parsed-args preference, never-throws"
  key_links:
    - from: "frontend/src/lib/providerLogo.tsx"
      to: "@lobehub/icons"
      via: "named .Color/.Mono imports (never .Avatar)"
      pattern: "@lobehub/icons"
    - from: "frontend/src/lib/providerLogo.tsx"
      to: "tc.argsCodeText"
      via: "tolerant partial-JSON description parse in try/catch"
      pattern: "argsCodeText"
---

<objective>
Build the ONE shared helper `frontend/src/lib/providerLogo.tsx` (the D-05 G-5
micro-extraction) that both `RunCard.tsx` and `ToolCallPanel.tsx` will import in
Plan 04 — so neither grows a duplicate copy of the logo map or the
preparing-description parse. It exports:
1. `providerLogo(provider)` — the `message.provider → @lobehub/icons` mark map with the
   locked D-08 fallback (CTC-01).
2. `preparingDescription(tc)` — extract a tool's `description` from the partial-JSON
   `tc.argsCodeText` during the preparing window, with the honest-null fallback (TDP-02).

This plan also writes the Wave-0 unit tests for both helpers (the VALIDATION.md Wave 0
gaps for `providerLogo.test.tsx` + `preparingDescription.test.ts`).

Purpose: this is the load-bearing correction from research — `tc.args.description` is
EMPTY during preparing (CONTEXT's "already present" claim was PARTLY WRONG); the bytes
live in `tc.argsCodeText`. Centralizing both reads here makes CTC-01 + TDP-02 trivial,
de-duplicated, and consistent (CTC-02's "byte-identical across providers").
Output: `providerLogo.tsx` + its two unit-test files, all green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Pattern map — the EXACT analog shapes (fileIcon.tsx map + seamCardPayloadFor safe-parse) + the provider→mark table + the test scaffolds + the Known-Gotchas table
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md
# (§ "providerLogo.tsx (NEW) — the D-05 shared helper": Analog A fileIcon.tsx, Analog B seamCardPayloadFor;
#  § "Lib unit test scaffold"; § "Known Gotchas Carried From Research")

# Research — Pattern 1 (the provider→mark assignments), Pattern 2 (the ~10-line parse body), the antd/.Avatar gotcha, the exact provider strings
@.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md
# (§ "Pattern 1: D-05 shared providerLogo() helper", § "Pattern 2: TDP-02 preparing-description extraction",
#  § "Package Legitimacy Audit" Peer-dependency note, § "Code Examples" → canonical provider strings)

<interfaces>
<!-- Key facts the executor needs — extracted from the codebase + research. No exploration required. -->

THE EXACT runs.provider STRINGS (config.py MODEL_CAPABILITIES["provider"]) — the map keys:
  openai · anthropic · google · deepseek · moonshot · zhipu · minimax · openrouter · ollama · lmstudio  (+ possible "unknown")
  CRITICAL: GLM is keyed "zhipu" (NOT "glm"); Kimi is keyed "moonshot" (NOT "kimi").

THE @lobehub/icons MARK ASSIGNMENTS (research Pattern 1 — import the brand module; use .Color where it exists, else the brand default/.Mono; NEVER .Avatar):
  openai → OpenAI (mono brand default)      anthropic → Anthropic (mono brand default)
  google → Gemini.Color                     deepseek → DeepSeek.Color
  moonshot → Moonshot (mono brand default)  zhipu → Zhipu.Color
  minimax → Minimax.Color                   openrouter → OpenRouter (D-08: do NOT unwrap)
  ollama → Ollama (D-08: local keeps its own mark)
  ABSENT (→ null → caller's Bot fallback): lmstudio, unknown, undefined.

frontend/src/types/index.ts — the ToolCall shape:
  - tc.args is typed Record<string, string> (so tc.args.description is string|undefined at the type level)
    BUT is EMPTY {} at runtime during "preparing" (the StreamsProvider reducer keeps it {} until tool_start).
  - tc.argsCodeText?: string  (line 81) — the partial-JSON args string, POPULATED during preparing. READ THIS for the preparing window.
  - tc.argsBytesStreamed?: number (line 74).

ANALOG A — frontend/src/lib/fileIcon.tsx (copy the map+fallback SHAPE): a lib .tsx with a module-level `Record<string, …>` keyed off a resolved string, a `DEFAULT` fallback via `MAP[key] ?? DEFAULT`, named Lucide imports, and a JSDoc security header (model/derived input → text children only).

ANALOG B — frontend/src/components/chat/MessageItem.tsx:108-119 (the seamCardPayloadFor write_todos branch): JSON.parse a model-derived string inside try/catch, `catch { /* leave empty — never throw in a render-path mapper */ }`. This is THE rule the research V5/DoS row cites at :117.

The fallback Bot glyph: `import { Bot } from "lucide-react"` (already imported in RunCard.tsx:2). providerLogo returns null for unmapped keys; the CALLER (Plan 04) renders <Bot> when null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: providerLogo() map + Wave-0 unit test (CTC-01)</name>
  <files>frontend/src/lib/providerLogo.tsx, frontend/src/__tests__/lib/providerLogo.test.tsx</files>
  <read_first>
    - frontend/src/lib/fileIcon.tsx (whole file — the map + `?? DEFAULT` fallback + JSDoc security header SHAPE to copy verbatim, swapping ext→provider and Lucide-glyph→@lobehub/icons mark)
    - frontend/src/__tests__/lib/buildFolderTree.test.ts (the lib unit-test scaffold: `describe/it/expect`, a `makeX` factory)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md § Pattern 1 (the exact provider→mark table) + § Peer-dependency note (.Color/.Mono only, never .Avatar)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md § "Known Gotchas" (GLM=zhipu, Kimi=moonshot, OpenRouter-not-unwrapped, .Avatar-drags-antd)
  </read_first>
  <behavior>
    - Test 1 (mapped provider → mark): `providerLogo("zhipu")` returns a non-null React component (the Zhipu mark); same for `"google"`, `"openai"`, `"minimax"`.
    - Test 2 (D-08 fallback → null): `providerLogo("lmstudio")`, `providerLogo("unknown")`, and `providerLogo(undefined)` each return null (→ the caller renders Bot).
    - Test 3 (OpenRouter NOT unwrapped): `providerLogo("openrouter")` returns the OpenRouter mark (a non-null component) — it does NOT return a routed-model brand. (Assert it is the same component the map's `openrouter` entry holds, distinct from e.g. the `openai` entry.)
    - Test 4 (GLM/Kimi key correctness): there is NO `"glm"` or `"kimi"` key — `providerLogo("glm")` and `providerLogo("kimi")` return null; the real marks live under `"zhipu"` / `"moonshot"`.
  </behavior>
  <action>
    Create `frontend/src/lib/providerLogo.tsx` mirroring `fileIcon.tsx`'s shape: a JSDoc header naming Phase 128 / D-05 + the security posture (provider string is already-resolved; any text rendered downstream is React text children only). Named-import the ~9 marks from `@lobehub/icons` — `OpenAI, Anthropic, Gemini, DeepSeek, Moonshot, Zhipu, Minimax, OpenRouter, Ollama` — and `Bot` from `lucide-react` is NOT needed here (the caller owns the fallback; this helper returns null). Use ONLY `.Color`/`.Mono` sub-components per the research Pattern 1 table (`google: Gemini.Color`, `deepseek: DeepSeek.Color`, `zhipu: Zhipu.Color`, `minimax: Minimax.Color`; `openai`/`anthropic`/`moonshot`/`openrouter`/`ollama` use the brand default/`.Mono`) — NEVER `.Avatar` (research Pitfall 3 — it drags antd). Build a module-level `const MARKS: Record<string, React.ComponentType<{ size?: number }>>` keyed by the EXACT `runs.provider` strings above (GLM→`zhipu`, Kimi→`moonshot`; `openrouter`→OpenRouter, do NOT unwrap; `lmstudio`/`unknown` deliberately absent). Export `providerLogo(provider: string | undefined): React.ComponentType<{ size?: number }> | null` as `provider ? (MARKS[provider] ?? null) : null` — the `?? null` mirrors `fileIcon`'s `?? DEFAULT_SPEC`. Do NOT use `import *` (defeats tree-shaking). Write `providerLogo.test.tsx` (a `.tsx` — it asserts on returned components) per the four behaviors using the `buildFolderTree.test.ts` scaffold.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/lib/providerLogo.test.tsx</automated>
  </verify>
  <done>`providerLogo()` returns the correct `@lobehub/icons` mark for each of the 9 mapped providers (GLM under `zhipu`, Kimi under `moonshot`, OpenRouter not unwrapped) and null for `lmstudio`/`unknown`/`undefined`/`glm`/`kimi`; only `.Color`/`.Mono` are imported; the unit test passes.</done>
  <acceptance_criteria>
    `npx vitest run src/__tests__/lib/providerLogo.test.tsx` exits 0.
    `grep -E "\.Avatar" frontend/src/lib/providerLogo.tsx` finds NOTHING (no `.Avatar` import).
    `grep "import \*" frontend/src/lib/providerLogo.tsx` finds NOTHING (named imports only).
    The map has no `glm` or `kimi` key; `providerLogo("zhipu")` and `providerLogo("moonshot")` return non-null.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: preparingDescription() partial-JSON parse + Wave-0 unit test (TDP-02)</name>
  <files>frontend/src/lib/providerLogo.tsx, frontend/src/__tests__/lib/preparingDescription.test.ts</files>
  <read_first>
    - frontend/src/components/chat/MessageItem.tsx:108-119 (the `seamCardPayloadFor` write_todos branch — the verbatim never-throw try/catch idiom to copy)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-RESEARCH.md § Pattern 2 (the exact ~10-line body: prefer parsed `tc.args.description`, else full JSON.parse of `argsCodeText`, else the `"description":"…"` regex + JSON-unescape, all try/catch → null) + Pitfall 1 (why read `argsCodeText` not `args` during preparing)
    - .planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-PATTERNS.md § "Never-throw render-path parse" + the `preparingDescription` mirror notes
    - frontend/src/types/index.ts:34-92 (the `ToolCall` shape — `args`, `argsCodeText`)
  </read_first>
  <behavior>
    - Test 1 (parsed args preferred): `preparingDescription({ args: { description: "render a chart" }, status: "running" })` returns `"render a chart"` (trusts the parsed value once present).
    - Test 2 (partial-JSON extraction during preparing): a tool with `args: {}`, `status: "preparing"`, `argsCodeText: '{"description":"render a chart"'` (truncated, no closing brace) returns `"render a chart"` (extracted from the partial JSON via regex when full-parse fails).
    - Test 3 (full-parse path): `argsCodeText: '{"description":"hello","code":"x=1"}'` (complete JSON) returns `"hello"`.
    - Test 4 (honest null — no description key): `argsCodeText: '{"code":"x=1"'` returns null (→ caller shows the quiet `Preparing {tool}…` fallback).
    - Test 5 (honest null — nothing on the wire): `args: {}`, `argsCodeText: undefined` returns null.
    - Test 6 (never throws on garbage): `argsCodeText: '{"descr'` (malformed mid-key) returns null and does NOT throw.
  </behavior>
  <action>
    Add `export function preparingDescription(tc: ToolCall): string | null` to the SAME `providerLogo.tsx` (D-05: one shared helper module — do NOT create a second file). Import the `ToolCall` type from `@/types`. Body per research Pattern 2: (1) if `typeof tc.args?.description === "string" && tc.args.description` → return it (the running/done/atomic-provider case). (2) `const raw = tc.argsCodeText; if (!raw) return null`. (3) `try { const o = JSON.parse(raw); if (typeof o?.description === "string") return o.description } catch { /* partial — fall through */ }` — mirror the `seamCardPayloadFor` catch (never re-throw). (4) Targeted regex for the first complete key: `const m = raw.match(/"description"\s*:\s*"((?:[^"\\]|\\.)*)"/)`; `return m ? JSON.parse(\`"${m[1]}"\`) : null` (JSON-unescape the captured group — also inside the implicit safe path; if you prefer, wrap step 4's unescape in try/catch → null to be fully crash-proof). NEVER mutate `tc.args` (the StreamsProvider reducer owns it — research Anti-Pattern). NEVER throw in this render-path mapper (every `JSON.parse` guarded). Write `preparingDescription.test.ts` (a `.ts` — pure value assertions, no JSX) using the `buildFolderTree.test.ts` scaffold + a `makeToolCall(overrides)` factory (mirror `ToolCallPanel.test.tsx:40`'s `mkTool`).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/lib/preparingDescription.test.ts</automated>
  </verify>
  <done>`preparingDescription(tc)` prefers a parsed `tc.args.description`, else extracts `description` from the partial-JSON `tc.argsCodeText` (full-parse then regex), returns null on absent/garbage, and never throws; the unit test (6 behaviors) passes.</done>
  <acceptance_criteria>
    `npx vitest run src/__tests__/lib/preparingDescription.test.ts` exits 0.
    The test proves: parsed-args preference; truncated-partial-JSON extraction; complete-JSON extraction; null on no-description-key; null on undefined argsCodeText; no-throw on malformed input.
    `preparingDescription` lives in `providerLogo.tsx` (same module as `providerLogo`) — NOT a separate file (D-05 one-helper rule).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output (partial tool-call JSON) → render-path parser | `tc.argsCodeText` is raw, possibly-truncated, model-emitted JSON parsed in a React render path. |
| npm package (`@lobehub/icons`) → rendered SVG component | Third-party SVG marks rendered as React components. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-03-01 | DoS (render crash) | `preparingDescription` partial-JSON parse | mitigate | Every `JSON.parse` on `tc.argsCodeText` wrapped in try/catch returning null (the `seamCardPayloadFor` rule, MessageItem.tsx:117). Malformed/truncated input → null, never a thrown error in the render path. Test 6 asserts no-throw on garbage. |
| T-128-03-02 | Tampering / Info-disclosure (XSS) | the extracted `description` string | mitigate | The returned string is rendered downstream (Plan 04) as React text children (auto-escaped); the regex unescape uses `JSON.parse('"…"')` (string decode only, no eval). Never `dangerouslySetInnerHTML`. |
| T-128-03-SC | Tampering | `@lobehub/icons` `.Color`/`.Mono` imports | mitigate | Inherits the Plan 01 supply-chain gate (slopcheck `[OK]`, pinned, lockfile). `.Color`/`.Mono` are pure SVG (zero deps); no `.Avatar`/antd reachable (acceptance grep). |
</threat_model>

<verification>
- `providerLogo("zhipu")` → Zhipu mark; `providerLogo("lmstudio")`/`undefined` → null; OpenRouter not unwrapped.
- `preparingDescription(tc)` extracts from partial-JSON `argsCodeText`, prefers parsed `args.description`, returns null on absent, never throws.
- Both helpers live in ONE `providerLogo.tsx` (D-05).
- `npx vitest run src/__tests__/lib/providerLogo.test.tsx` and `…/preparingDescription.test.ts` both pass.
</verification>

<success_criteria>
The D-05 shared helper exists with both functions + green unit tests; Plan 04 can import `providerLogo` + `preparingDescription` into RunCard + ToolCallPanel with no duplication.
</success_criteria>

<output>
Create `.planning/phases/128-chat-tool-card-unification-chat-area-reclaim/128-03-SUMMARY.md` when done
</output>
