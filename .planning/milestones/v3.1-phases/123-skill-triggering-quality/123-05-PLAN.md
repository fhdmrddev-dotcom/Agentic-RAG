---
phase: 123-skill-triggering-quality
plan: 05
type: execute
wave: 4
depends_on: ["123-04"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/pages/SkillsPage.tsx
  - frontend/src/pages/SkillTunerPage.tsx
  - frontend/src/components/skills/tuner/CaseEditor.tsx
  - frontend/src/components/skills/tuner/ProviderScoreboard.tsx
  - frontend/src/components/skills/tuner/CandidateCard.tsx
  - frontend/src/components/skills/tuner/LiveRunCard.tsx
  - frontend/src/lib/api.ts
  - frontend/src/pages/SkillTunerPage.test.tsx
  - frontend/src/components/skills/tuner/ProviderScoreboard.test.tsx
autonomous: true
requirements: [TRIG-01]
must_haves:
  truths:
    - "A 'Tune triggers' action on a skill opens the Trigger Tuner as a focused full-surface (rail stays, list+detail hidden) reached via the ActiveView no-router switch; '‹ Skills' returns (041-A)."
    - "THE SAME PLAN owns the ActiveView union member + the ChatLayout mount branch + the entry action (the Phase-118 built-but-unreachable lesson) — the surface is reachable in-phase."
    - "The scoreboard is N-column = the org's configured targets; a provider the org doesn't run never renders; N=1 single-provider is the clean baseline; OpenRouter is distinct from native DeepSeek/GLM/Kimi/MiniMax (042-A band / Pattern 4)."
    - "Each candidate card shows its held-out score + a per-provider grid where EVERY cell shows BOTH fires (should-trigger recall) and no-false (should-NOT precision) — the false-fire rail is never a hidden aggregate (042-A)."
    - "The author picks the winner by HELD-OUT score, then an explicit diff-confirm strip writes the live description via PATCH /skills/{id} (re-lints) — never auto-applied (042-A / D-03)."
    - "The case editor shows two columns (should-fire / should-NOT) with auto-seed provenance tags + a 60/40 split bar; the should-NOT column reads as the false-fire rail (043-A)."
    - "The background run renders a live per-provider progress card with a never-vanishing elapsed timer (stable start-ts, the 095 lesson) and 'you can leave; it reconciles on return'; a queued provider never shows a fake percent (043-A)."
  artifacts:
    - path: "frontend/src/pages/SkillTunerPage.tsx"
      provides: "The focused full-surface; two-column body (description+benchmark+run-config left, scoreboard+candidates right)"
    - path: "frontend/src/components/skills/tuner/ProviderScoreboard.tsx"
      provides: "N-column per-provider grid, each cell fires + no-false (no analog — built from sketch 042-A)"
    - path: "frontend/src/components/skills/tuner/LiveRunCard.tsx"
      provides: "per-provider live progress + never-vanishing elapsed timer + reconcile-on-return"
  key_links:
    - from: "frontend/src/App.tsx (ActiveView union + tunerSkillId state)"
      to: "frontend/src/components/layout/ChatLayout.tsx (skill-tuner mount branch)"
      via: "activeView === 'skill-tuner' renders SkillTunerPage skillId={tunerSkillId}"
      pattern: "skill-tuner"
    - from: "frontend/src/pages/SkillsPage.tsx (Tune triggers entry action)"
      to: "App.tsx onNavigate + tunerSkillId setter"
      via: "onTuneSkill(skillId) → setTunerSkillId + onNavigate('skill-tuner')"
      pattern: "onTuneSkill"
    - from: "frontend/src/pages/SkillTunerPage.tsx (confirm strip)"
      to: "PATCH /skills/{id}"
      via: "updateSkill winner description → re-lint"
      pattern: "updateSkill"
    - from: "frontend/src/components/skills/tuner/LiveRunCard.tsx"
      to: "the run:{id}/stream SSE + tuner_* events"
      via: "EventSource on the tuner stream route"
      pattern: "tuner_progress"
---

<objective>
Build the Trigger Tuner React surface (sketches 041-A / 042-A / 043-A) and — critically — make it REACHABLE in-phase: this plan owns the `ActiveView` union member, the `ChatLayout` mount branch, AND the "Tune triggers" entry action on a skill (the Phase-118 built-but-unreachable lesson). The Tuner is a focused full-surface entered WITH a `skillId` (not a cold top-level nav). It renders the case editor (two-column should-fire/should-NOT with provenance + 60/40 split bar), the N-column per-provider scoreboard (every cell fires + no-false), the candidate cards (held-out score + per-provider grid), the live background-run card (never-vanishing timer + reconcile-on-return), and the author-confirm diff strip that writes the winner via PATCH.

Purpose: This is the TRIG-01 surface the author actually uses — score + suggest, pick by held-out, confirm-not-auto-apply, with the false-fire rail always visible.
Output: SkillTunerPage + four tuner components + the api client calls + the reachability wiring + frontend tests.
Depends on Plan 04 (the tuner routes). Binding visual/interaction contract = sketches 041–044 READMEs + MANIFEST decisions 32–35.

Task structure (3 tasks): Task 1 owns the reachability triad + shell + api. Task 2 is split into Task 2a (the load-bearing, no-analog ProviderScoreboard + CandidateCard + their tests — the N-column scoreboard with both fires/no-false sub-scores is the highest-stakes/most-novel render and MUST land + pass FIRST) and Task 2b (CaseEditor + LiveRunCard + compose into SkillTunerPage + the SkillTunerPage suite). The surface stays reachable in this one plan (the Phase-118 triad is in Task 1).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/123-skill-triggering-quality/123-CONTEXT.md
@.planning/phases/123-skill-triggering-quality/123-RESEARCH.md
@.planning/phases/123-skill-triggering-quality/123-PATTERNS.md
@.planning/sketches/041-tuner-surface-shell/README.md
@.planning/sketches/042-scoreboard-and-candidates/README.md
@.planning/sketches/043-case-editor-and-live-run/README.md
@.planning/sketches/MANIFEST.md

<interfaces>
<!-- Verified at source this session. -->

App.tsx:9 union: type ActiveView = "chat" | "documents" | "skills" | "settings" | "library-health" | "workflows" | "classification-rules" | "governance"  — ADD "skill-tuner". App holds activeView + setActiveView via useState; add a sibling tunerSkillId useState (mirrors existing per-view selection state) threaded into ChatLayout props.

ChatLayout.tsx:280-310 mount switch: activeView === "skills" ? <SkillsPage onTryInChat={handleTryInChat} /> : ... : <KnowledgeHealthPage />. Add a `) : activeView === "skill-tuner" ? ( <SkillTunerPage skillId={tunerSkillId} onBack={() => onNavigate("skills")} /> )` branch BEFORE the trailing else (mirror the governance branch :301-307). onNavigate is the existing setActiveView prop.

SkillsPage.tsx:12-19: Props { onTryInChat?: (skillName: string) => void }. selectedSkill state + SkillDetailPanel (from SkillFormDialog). Add an onTuneSkill?: (skillId: string) => void prop, wire it through ChatLayout to App's tunerSkillId setter + onNavigate('skill-tuner'); surface a "Tune triggers" action on the selected skill (panel header / card).

GovernancePage.tsx — the focused-surface self-fetch home analog (no props beyond what App threads; ChatLayout.tsx:301-307 mount).

Tuner routes (Plan 04): POST /skills/{id}/tuner/runs, GET /skills/{id}/tuner/runs/{run_id}/stream (SSE tuner_progress/tuner_provider_done/tuner_complete), GET /skills/{id}/tuner/runs/{run_id} (scoreboard). PATCH /skills/{id} (existing) is the author-confirm winner write; useSkills().updateSkill wraps it.

Real production model-ids per axis (from eval_cross_provider.py, for default labels): gpt-5.4-mini / claude-haiku-4-5 / gemini-3.5-flash / z-ai/glm-5.1.
Never-vanishes timer: derive elapsed from a stable start-ts (Decision 9 / sketch 015 / 043-A) — never reset on transient stream-ends.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reachability wiring + SkillTunerPage shell + api client calls</name>
  <files>frontend/src/App.tsx, frontend/src/components/layout/ChatLayout.tsx, frontend/src/pages/SkillsPage.tsx, frontend/src/pages/SkillTunerPage.tsx, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/App.tsx:1-60 (the ActiveView union + activeView state + how per-view selection state + props thread to ChatLayout)
    - frontend/src/components/layout/ChatLayout.tsx:280-314 (the mount switch + the governance branch to mirror + onNavigate)
    - frontend/src/pages/SkillsPage.tsx:1-60 (Props + selectedSkill + SkillDetailPanel host for the entry action)
    - frontend/src/pages/GovernancePage.tsx (the focused-surface self-fetch precedent)
    - frontend/src/lib/api.ts (the fetch-wrapper convention to add tuner-run calls)
    - .planning/phases/123-skill-triggering-quality/123-PATTERNS.md §"SkillTunerPage.tsx + App.tsx + ChatLayout.tsx" (the union extension + mount + the Phase-118 reachability lesson)
    - .planning/sketches/041-tuner-surface-shell/README.md (the focused full-surface form + two-column body)
  </read_first>
  <action>
    (1) App.tsx: add `"skill-tuner"` to the `ActiveView` union; add a sibling `const [tunerSkillId, setTunerSkillId] = useState<string | null>(null)` and thread both `tunerSkillId` and a `onTuneSkill = (id) => { setTunerSkillId(id); setActiveView("skill-tuner") }` down to ChatLayout. (2) ChatLayout.tsx: add the `activeView === "skill-tuner" ? <SkillTunerPage skillId={tunerSkillId} onBack={() => onNavigate("skills")} />` mount branch BEFORE the trailing `KnowledgeHealthPage` else (mirror governance); pass `onTuneSkill` down to `<SkillsPage>`. (3) SkillsPage.tsx: add `onTuneSkill?: (skillId: string) => void` to Props and render a "Tune triggers" action on the selected skill (panel header or card). (4) Create `SkillTunerPage.tsx` as the focused full-surface shell: a `‹ Skills` back button (onBack), the skill's current description (labeled "current · live · drives firing"), and the two-column body scaffold (left: description + benchmark + run-config; right: scoreboard + candidates) — components filled in Tasks 2a/2b. (5) api.ts: add `startTunerRun(skillId, body)`, `getTunerResults(skillId, runId)`, and an SSE helper for the stream route. Self-check the Phase-118 reachability triad: union + mount + entry action all present.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "skill-tuner" frontend/src/App.tsx frontend/src/components/layout/ChatLayout.tsx` shows ≥1 in EACH (union member + mount branch — reachability).
    - `grep -c "onTuneSkill" frontend/src/App.tsx frontend/src/components/layout/ChatLayout.tsx frontend/src/pages/SkillsPage.tsx` shows ≥1 in each (entry action threaded end-to-end).
    - `SkillTunerPage.tsx` exists, accepts `skillId` + `onBack`, renders a `‹ Skills` back control.
    - api.ts has `startTunerRun` + `getTunerResults` + an SSE stream helper.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The Tuner is reachable in-phase (union + mount + Tune-triggers entry action all owned here) and the focused-surface shell + api calls compile.</done>
</task>

<task type="auto">
  <name>Task 2a: The N-column ProviderScoreboard + CandidateCard (no-analog, load-bearing — TDD; MUST land + pass before Task 2b)</name>
  <files>frontend/src/components/skills/tuner/ProviderScoreboard.tsx, frontend/src/components/skills/tuner/CandidateCard.tsx, frontend/src/components/skills/tuner/ProviderScoreboard.test.tsx</files>
  <read_first>
    - .planning/sketches/042-scoreboard-and-candidates/README.md (THE binding contract — candidate cards + per-provider grid, both fires/no-false per cell, held-out pick, author-confirm diff, the provider-set adaptivity band: N-column, single-provider baseline, OpenRouter≠native)
    - .planning/sketches/MANIFEST.md decisions 32-35 (the build contract — esp. the 042 provider-set adaptivity band)
    - frontend/src/pages/GovernancePage.tsx (the stacked-card render idiom for the scoreboard, the closest analog per PATTERNS "No Analog Found")
    - frontend/src/hooks/useSkills.ts (updateSkill — the author-confirm winner write)
    - frontend/src/components/skills/SkillCard.tsx (existing skills chrome / Aether tokens to reuse)
    - .planning/phases/123-skill-triggering-quality/123-PATTERNS.md §"No Analog Found" (ProviderScoreboard — build from sketch 042-A + RESEARCH Pattern 4) + RESEARCH §"Pattern 4"
  </read_first>
  <behavior>
    - Test (ProviderScoreboard.test.tsx): given a 3-provider target set, renders exactly 3 columns; given a 1-provider set, renders ONE column with no "degraded"/"missing" affordance (single-provider is the clean baseline); a provider not in the target set never renders.
    - Test: each scoreboard cell renders BOTH a fires sub-score AND a no-false sub-score (neither hidden to hover) — assert both labels present per cell.
    - Test: OpenRouter and a native provider (e.g. zhipu/z-ai) render as DISTINCT columns when both are targets.
    - Test (in ProviderScoreboard.test.tsx, covering CandidateCard): a candidate card shows its held-out score; selecting "Use" reveals an explicit diff-confirm strip (old vs new description) and confirming calls updateSkill (or the injected onConfirm that wraps updateSkill) — nothing auto-applies.
  </behavior>
  <action>
    Build the two highest-stakes, no-analog components FIRST. `ProviderScoreboard.tsx` (042-A, no analog — build from sketch + RESEARCH Pattern 4): an N-column grid where N = the org's configured targets; each cell shows BOTH a fires (should-trigger recall) sub-score AND a no-false (should-NOT precision) sub-score — neither is a hidden aggregate or hover-only; a provider NOT in the target set never renders; N=1 is the CLEAN baseline (single column, held-out = that provider's score, NO "degraded"/"missing" affordance); OpenRouter is a DISTINCT column from native zhipu/moonshot/minimax/deepseek when both are targets. `CandidateCard.tsx` (042-A): a card per candidate carrying its held-out score + the per-provider grid (rendered via ProviderScoreboard); "Use → confirm & save" reveals an explicit diff-confirm strip (old vs new) that, on confirm, writes the live description via `useSkills().updateSkill` (re-lints) and states the skill begins firing immediately — NEVER auto-applied. Write `ProviderScoreboard.test.tsx` covering the N-column adaptivity (3-col / 1-col-clean-baseline / non-target-never-renders), both-sub-scores-per-cell, OpenRouter≠native-distinct, AND the CandidateCard author-confirm-not-auto-apply behaviors above. ORDERING GATE: these ProviderScoreboard N-column adaptivity + both-sub-scores assertions are the load-bearing contract and MUST be GREEN before Task 2b proceeds. TDD: write the tests RED, then implement to GREEN.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/components/skills/tuner/ProviderScoreboard.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - ProviderScoreboard renders N columns from the target set; N=1 has no degraded affordance; a non-target provider never renders (tests green).
    - Both fires AND no-false appear in every cell: the test asserts both labels per cell.
    - OpenRouter and native zhipu render as distinct columns (test green).
    - Author-confirm (CandidateCard): selecting a winner reveals a diff strip and calls updateSkill on confirm — no auto-apply (test green).
    - `npx tsc --noEmit` clean for the new files.
  </acceptance_criteria>
  <done>The N-column ProviderScoreboard (both sub-scores per cell, single-provider clean baseline, OpenRouter≠native) and CandidateCard (held-out score + per-provider grid + author-confirm diff, no auto-apply) match sketch 042-A / RESEARCH Pattern 4 and pass ProviderScoreboard.test.tsx — the load-bearing gate for Task 2b.</done>
</task>

<task type="auto">
  <name>Task 2b: CaseEditor + LiveRunCard + compose into SkillTunerPage (TDD on the live-run + page compose)</name>
  <files>frontend/src/components/skills/tuner/CaseEditor.tsx, frontend/src/components/skills/tuner/LiveRunCard.tsx, frontend/src/pages/SkillTunerPage.tsx, frontend/src/pages/SkillTunerPage.test.tsx</files>
  <read_first>
    - .planning/sketches/043-case-editor-and-live-run/README.md (two-column should-fire/should-NOT + provenance tags + 60/40 split bar + the background-run live card + never-vanishes timer + reconcile-on-return)
    - .planning/sketches/042-scoreboard-and-candidates/README.md (the candidate-card + scoreboard contract — Task 2a built these; compose them into the page body here)
    - .planning/sketches/MANIFEST.md decisions 9/10/15 (never-vanishes strip / hero / status placement idioms)
    - frontend/src/components/skills/tuner/ProviderScoreboard.tsx + CandidateCard.tsx (Task 2a outputs — compose into the page right column)
    - frontend/src/pages/SkillTunerPage.tsx (Task 1 shell — fill the two-column body here)
    - .planning/phases/123-skill-triggering-quality/123-PATTERNS.md §"CaseEditor + CandidateCard + LiveRunCard" (the never-vanishes timer / 095 lesson)
  </read_first>
  <behavior>
    - Test (SkillTunerPage.test.tsx): a candidate card (from Task 2a, composed into the page) shows its held-out score; selecting "Use" reveals an explicit diff-confirm strip (old vs new description) and confirming calls updateSkill — nothing auto-applies (the page-level wiring proof).
    - Test: the LiveRunCard shows a per-provider lane; a queued provider shows a queued state, NOT a fake 0%/running percent; the elapsed timer is present and derives from a stable start-ts (does not reset on a transient stream-end).
    - Test: the CaseEditor renders two columns (should-fire / should-NOT) with provenance tags and the 60/40 split bar; the should-NOT column reads as the false-fire rail.
  </behavior>
  <action>
    Build the remaining two components and compose the page. `CaseEditor.tsx` (043-A): two columns (should-fire / should-NOT) with per-row provenance tags (`seeded`/`sibling`/`held`/`you`), add/edit/remove, the 60/40 train/held-out split bar, and the run bar ("cases × N models × 3 repeats"). The should-NOT column reads visually as the false-fire rail. `LiveRunCard.tsx` (043-A): per-provider live progress lanes over the SSE stream (tuner_progress/tuner_provider_done/tuner_complete), a never-vanishing elapsed timer derived from a stable start-ts (the 095 lesson — never reset on transient stream-ends), an explicit "runs in the background — reconciles on return", and Cancel; a queued provider shows queued, never a fake percent. Compose CaseEditor + LiveRunCard + the Task 2a ProviderScoreboard/CandidateCard into `SkillTunerPage.tsx`'s two-column body (left: description + CaseEditor + run-config; right: scoreboard + candidate cards + LiveRunCard). Write `SkillTunerPage.test.tsx` covering the page-level author-confirm-not-auto-apply (via the composed CandidateCard), the LiveRunCard queued≠running + stable-timer, and the CaseEditor two-column/60-40 behaviors above. TDD on the live-run + page compose: tests RED then implement to GREEN. (Prereq: Task 2a's ProviderScoreboard suite is already green.)
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/pages/SkillTunerPage.test.tsx src/components/skills/tuner/ProviderScoreboard.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Author-confirm (page-level): selecting a winner reveals a diff strip and calls updateSkill on confirm — no auto-apply (test green).
    - LiveRunCard shows queued ≠ running and a stable elapsed timer (test green).
    - CaseEditor renders two columns + provenance tags + the 60/40 split bar; the should-NOT column reads as the false-fire rail (test green).
    - Both suites green together (`SkillTunerPage.test.tsx` + the Task 2a `ProviderScoreboard.test.tsx` — confirms the compose did not regress 2a).
    - `npx tsc --noEmit` clean for the new files.
  </acceptance_criteria>
  <done>The case editor (two-column should-fire/should-NOT + provenance + 60/40 split bar) and live-run card (never-vanishing timer, reconcile-on-return, queued≠running) match the 043-A sketch contract, are composed with the Task 2a scoreboard/candidates into SkillTunerPage, and pass the SkillTunerPage + ProviderScoreboard vitest suites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tuner UI → tuner routes | The surface starts/streams/reads runs scoped to the selected skill |
| confirm strip → PATCH /skills | The author-confirmed winner description is written to the live skill |
| scoreboard render → server scores | The UI renders only server-computed scores (no client-side fabrication) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-05-01 | Tampering / honesty | The scoreboard fabricates an aggregate that hides a regressed provider, or renders a provider the org doesn't run | mitigate | N-column = configured targets only (a provider not run never renders — a score you can't act on is a fabricated measurement); every cell shows both fires AND no-false (no hidden aggregate). Task 2a tests assert N-column adaptivity + both sub-scores (the load-bearing gate). |
| T-123-05-02 | Tampering | The winner is auto-applied without author confirmation | mitigate | The confirm-diff strip is an explicit gate; updateSkill is only called on confirm (D-03). Task 2a + 2b tests assert no auto-apply. |
| T-123-05-03 | Repudiation / honesty | The live run shows a fake percent for a queued provider, or the timer vanishes mid-run (the documented Kimi/Moonshot vanish bug) | mitigate | Queued ≠ running (no fake percent); the elapsed timer derives from a stable start-ts and never resets on transient stream-ends (Decision 9 / 095 lesson). Task 2b asserts queued≠running + stable timer. |
| T-123-05-04 | Information Disclosure | The Tuner reads/streams another user's skill via a crafted skillId | mitigate | Owner-scoping is enforced server-side (Plan 04 routes 404 cross-user); the UI carries no service-role capability. |
| T-123-05-SC | Tampering | npm/pip installs | accept | No packages installed this plan (zero new deps per RESEARCH). |
</threat_model>

<verification>
- Task 2a: `npx vitest run src/components/skills/tuner/ProviderScoreboard.test.tsx` green (the load-bearing N-column + both-sub-scores gate).
- Task 2b: `npx vitest run src/pages/SkillTunerPage.test.tsx src/components/skills/tuner/ProviderScoreboard.test.tsx` green together.
- `npx tsc --noEmit` clean.
- Reachability triad grep gates pass (union + mount + entry action — Task 1).
- Manual/SC#10 UAT (VALIDATION.md): the surface renders the N-column scoreboard, candidate cards, case editor, live-run card across the configured targets.
</verification>

<success_criteria>
- The Trigger Tuner is reachable in-phase and matches sketches 041-A/042-A/043-A.
- N-column provider-set adaptivity, both fires/no-false per cell, pick-by-held-out, author-confirm-not-auto-apply, never-vanishing live run.
- No DB schema change; no new external package.
</success_criteria>

<output>
Create `.planning/phases/123-skill-triggering-quality/123-05-SUMMARY.md` when done.
</output>
