---
phase: 123-skill-triggering-quality
plan: 03
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - backend/app/config.py
  - backend/app/models/user_settings.py
  - backend/app/services/skill_tuner_service.py
  - backend/tests/unit/test_skill_builder_model.py
  - backend/tests/unit/test_skill_tuner_scoring.py
  - backend/tests/unit/test_skill_tuner_service.py
autonomous: true
requirements: [TRIG-01]
must_haves:
  truths:
    - "The skill-builder model is a configurable app setting resolving as: explicit setting → strong forced_emission default → honest None; selectable across the FULL provider list incl. local, NO paid-provider SPOF (D-08)."
    - "Candidate descriptions (≤N, default 3) are generated via forced_emit on the builder model; per-case firing is classified via forced_emit on each TARGET model — both with FLAT single-typed Pydantic schemas (Gemini-safe) and a non-empty system_prompt (D-02 / Pitfall 4)."
    - "The benchmark uses a 60/40 train/held-out split with 3 repeats; the winning description is picked by HELD-OUT score, never train (Pitfall — pick-by-train forbidden)."
    - "Each provider scoring cell shows BOTH sub-scores: fires (should-trigger recall) AND no-false (should-NOT precision) — the false-fire rail is never hidden (042-A)."
    - "Configured targets = providers with a non-empty API key or a local base_url, one representative model each; a provider the org doesn't run NEVER appears; N=1 single-provider is the clean baseline; OpenRouter is a gateway distinct from native DeepSeek/GLM/Kimi/MiniMax (D-05 / Pattern 4 / 042-A band)."
    - "The per-case classifier prompt mirrors the post-D-01 LOAD_SKILL_POLICY wording (imported from skill_lint) so it measures the REAL production policy (Pitfall 1)."
    - "The sibling-skill auto-seed uses the owner-scoped .or_(user_id.eq.{id}, is_global.eq.true) query — never leaks another user's private skills (D-04 / V4)."
  artifacts:
    - path: "backend/app/services/skill_tuner_service.py"
      provides: "build_candidates(), classify_fires(), score_held_out() (60/40 split + 3 repeats + winner-by-held-out), configured_targets(), auto_seed_cases()"
      contains: "def score_held_out"
    - path: "backend/app/config.py"
      provides: "skill_builder_model setting + resolve_skill_builder_model()"
      contains: "skill_builder_model"
    - path: "backend/tests/unit/test_skill_tuner_scoring.py"
      provides: "pure scoring math: 60/40 split, 3-repeat aggregation, winner-by-held-out"
    - path: "backend/tests/unit/test_skill_builder_model.py"
      provides: "D-08 resolution order incl. local/no-SPOF"
  key_links:
    - from: "backend/app/services/skill_tuner_service.py:classify_fires"
      to: "backend/app/services/skill_lint.py:LOAD_SKILL_POLICY"
      via: "classifier prompt imports the shared policy constant (Pitfall 1 fidelity)"
      pattern: "LOAD_SKILL_POLICY"
    - from: "backend/app/services/skill_tuner_service.py:build_candidates / classify_fires"
      to: "backend/app/services/forced_emit.py:forced_emit"
      via: "schema_model= Pydantic forced emission (builder model for candidates, target model for classification)"
      pattern: "forced_emit"
    - from: "backend/app/services/skill_tuner_service.py:auto_seed_cases"
      to: "the owner-scoped skills catalog query"
      via: ".or_(user_id.eq.{id},is_global.eq.true).eq(is_enabled,True)"
      pattern: "is_global.eq.true"
---

<objective>
Deliver the TRIG-01 backend orchestration core: the D-08 configurable builder-model knob and `skill_tuner_service.py` — the benchmark runner built entirely on proven primitives (`forced_emit`, the owner-scoped catalog query, the eval scoreboard cell shape). This plan builds the PURE/in-process pieces (candidate generation, per-case classification, the 60/40 held-out scoring math, configured-target adaptivity, owner-scoped auto-seed) and their tests. Plan 04 wraps these in the background job + routes; Plan 05/06 render them.

Purpose: This is the genuinely net-new logic of TRIG-01 — held-out split/repeat math, the per-case "did it fire?" measurement, and the N-column provider-set adaptivity. Everything else is wiring existing parts.
Output: `skill_tuner_service.py`, the `skill_builder_model` knob + resolver, and three unit-test files.
Depends on Plan 01 for the shared `LOAD_SKILL_POLICY` constant (the classifier must mirror the REAL policy — Pitfall 1).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/123-skill-triggering-quality/123-CONTEXT.md
@.planning/phases/123-skill-triggering-quality/123-RESEARCH.md
@.planning/phases/123-skill-triggering-quality/123-PATTERNS.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-CONTEXT.md

<interfaces>
<!-- Verified at source this session. -->

forced_emit signature (forced_emit.py:318):
  async def forced_emit(*, messages: list[dict], model: str, provider: str, emitter: str, tools: list[dict], user_settings, system_prompt: str = "", max_tokens: int | None = None, schema_model: type[BaseModel] | None = None, strict: bool | None = None) -> dict
  returns {"emitted": BaseModel|None, "tier", "emit_rung", "provider", "forced", "recovered_from_narration", "truncated", "failure"}. result["emitted"] is honest-fail-floor None on failure (never silent).

resolve_authoring_model(settings) (workflow_authoring.py:83) — the EXACT pattern to mirror for D-08:
  model = getattr(settings, "harness_authoring_model", None); if model: return model; for candidate in ("claude-opus-4-8","gpt-5.5"): if get_model_capability(candidate).get("forced_emission"): return candidate; return None

get_model_capability(model) (config.py:495) → {"provider": ..., "forced_emission": bool, "emit_tier": ...}. Use it to derive the provider arg for forced_emit and to validate a configured target's representative model.

Per-provider API key fields on Settings (config.py:718-731): openai_api_key, anthropic_api_key, google_api_key, openrouter_api_key, deepseek_api_key, moonshot_api_key, minimax_api_key, zhipu_api_key. Local providers (ollama / lm-studio / openai-compat) have a base_url, not a key.

Owner-scoped catalog query (agent_loop.py:1090-1094 / tool_dispatcher.py:663 / skills.py:114):
  supabase.table("skills").select(...).or_(f"user_id.eq.{uid},is_global.eq.true").eq("is_enabled", True)

eval_cross_provider.py scoring cell shape: score_forced_emit_axes (:1412) + _build_forced_emit_cell (:1484-1514) — mirror the per-provider cell SHAPE (axes dict of PASS/FAIL), but the Tuner's axes are fires/no_false (not trigger/force/recovery/honest_fail).

Settings model-id knob precedent: user_settings.py:148-167 (extraction_model app_settings field).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the D-08 skill_builder_model knob + resolver (TDD)</name>
  <files>backend/app/config.py, backend/app/models/user_settings.py, backend/tests/unit/test_skill_builder_model.py</files>
  <read_first>
    - backend/app/services/workflow_authoring.py:83-105 (resolve_authoring_model — the EXACT resolution pattern to mirror)
    - backend/app/config.py:980-1041 (the harness_authoring_model / harness_judge_model knob block — add skill_builder_model beside it) and :495 (get_model_capability)
    - backend/app/models/user_settings.py:148-167 (the extraction_model app_settings model-id knob precedent)
    - .planning/phases/123-skill-triggering-quality/123-PATTERNS.md §"resolve_skill_builder_model()" + §"Settings-not-env model-id knob"
  </read_first>
  <behavior>
    - Test: settings.skill_builder_model set → returns it verbatim (incl. a local model id like "ollama/llama3.1").
    - Test: setting unset → returns the first strong forced_emission default in the candidate list.
    - Test: setting unset AND no candidate is forced-emission-capable → returns None (honest).
    - Test: a local/self-hosted model id is accepted as the setting (no paid-provider hardcoding / no SPOF — D-08).
  </behavior>
  <action>
    Add `skill_builder_model: str | None = None` to the config/settings beside `harness_authoring_model` (config.py:~1004) with a comment matching the PATTERNS §"Config knob" wording (settings-not-env, full provider list incl. local, decoupled from benchmark targets, no SPOF). Mirror it into the app_settings surface in `user_settings.py` next to `extraction_model` so the Settings UI (Plan 06) can read/write it. Implement `resolve_skill_builder_model(settings) -> str | None` (in `skill_tuner_service.py` or a small config helper — your call; PATTERNS suggests the service) by copying `resolve_authoring_model` verbatim and swapping the setting name + default candidates to `("claude-haiku-4-5-20251001", "gpt-5.4-mini")` (A7). Use a function-local `get_model_capability` import (Pitfall-4 discipline). Write `test_skill_builder_model.py` covering all four behaviors FIRST (RED), then implement.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_skill_builder_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "skill_builder_model" backend/app/config.py` ≥ 1 and `grep -c "skill_builder_model" backend/app/models/user_settings.py` ≥ 1.
    - `resolve_skill_builder_model` resolution order test green; the no-candidate case returns None (honest, no SPOF).
    - The resolver does NOT hardcode a single paid provider as the only path — a local model id set as the setting is returned verbatim (test asserts).
  </acceptance_criteria>
  <done>The configurable, provider-agnostic builder-model knob exists with the settings→strong-default→honest-None resolution and is surfaced in app_settings; its unit suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the skill_tuner_service core — candidates, classification, held-out scoring, target adaptivity, owner-scoped auto-seed (TDD)</name>
  <files>backend/app/services/skill_tuner_service.py, backend/tests/unit/test_skill_tuner_scoring.py, backend/tests/unit/test_skill_tuner_service.py</files>
  <read_first>
    - backend/app/services/forced_emit.py:99-330 (forced_emit signature + the honest-fail floor + the non-empty-system_prompt requirement)
    - backend/app/services/workflow_authoring.py:1-110 (the thin-orchestration-over-forced_emit pattern + the RED LINE banner :20-24 — never open the agent loop or a new SDK path)
    - scripts/eval_cross_provider.py:1400-1514 (score_forced_emit_axes + _build_forced_emit_cell — the per-provider cell shape to mirror; the non-empty-system_prompt fix at :1400-1409)
    - backend/app/services/skill_lint.py (the LOAD_SKILL_POLICY constant from Plan 01 — the classifier MUST import this)
    - backend/app/config.py:694-696,718-731 (SUB_AGENT defaults / per-provider api_key fields for configured_targets) and :495 (get_model_capability)
    - .planning/phases/123-skill-triggering-quality/123-RESEARCH.md §"Pattern 2" + §"Pattern 4" + Pitfall 1 + Pitfall 4 + §"Open Questions" Q1/Q2/Q5
    - .planning/phases/123-skill-triggering-quality/123-PATTERNS.md §"skill_tuner_service.py" (the forced_emit call pattern, scoring cell, owner-scoped auto-seed, anti-patterns)
    - .planning/sketches/042-scoreboard-and-candidates/README.md (the provider-set adaptivity band — N-column, single-provider baseline, OpenRouter≠native; fires/no-false per cell)
  </read_first>
  <behavior>
    - Test (pure scoring, test_skill_tuner_scoring.py): given labeled per-case results, split_held_out() partitions 60/40 deterministically and aggregate_repeats() averages 3 repeats; the winner is selected by HELD-OUT score; a candidate that wins train but loses held-out is NOT selected.
    - Test: each scoring cell carries both fires (should-trigger recall over should-fire cases) and no_false (should-NOT precision over should-NOT cases) — neither sub-score is dropped.
    - Test (test_skill_tuner_service.py, mocked forced_emit): build_candidates(builder_model, n) returns ≤N candidate strings from the emitted CandidateDescriptions; an honest-fail (emitted=None) yields zero candidates, never a crash.
    - Test: classify_fires(target_model, catalog, prompt) returns a TriggerDecision {would_load, skill_name}; the classifier prompt CONTAINS the LOAD_SKILL_POLICY text (assert substring) — it mirrors the real policy (Pitfall 1).
    - Test (test_n_column_configured_only): configured_targets(settings) returns ONE representative per configured provider; a provider with no key/base_url NEVER appears; OpenRouter and native deepseek/zhipu/moonshot/minimax are DISTINCT entries; N=1 is a valid clean result.
    - Test: auto_seed_cases uses the owner-scoped .or_(user_id.eq.{id},is_global.eq.true) query and never returns a sibling outside that scope (mock the supabase call; assert the query shape).
    - Test: the Pydantic schemas (CandidateDescriptions, TriggerDecision) are FLAT + single-typed — no Union/anyOf/oneOf (Gemini trap Pitfall 4); str|None nullable is allowed.
  </behavior>
  <action>
    Create `backend/app/services/skill_tuner_service.py` as a thin orchestration over `forced_emit` (mirror `workflow_authoring.py` — NEVER open the agent loop or a new SDK path; carry the RED LINE banner). Define FLAT single-typed Pydantic models `CandidateDescriptions {candidates: list[str]}` and `TriggerDecision {would_load: bool, skill_name: str | None}` (Pitfall 4 — no Union shapes). Implement: (1) `build_candidates(name, description, builder_model, user_settings, n=DEFAULT_CANDIDATE_COUNT)` — one forced_emit shot on the builder model returning ≤N rewrites; DEFAULT_CANDIDATE_COUNT=3 (A4, document as tunable); always pass a NON-EMPTY system_prompt (anthropic empty-block 400). (2) `classify_fires(target_model, catalog_lines, user_prompt, user_settings)` — assemble the classifier context from the SAME pieces as the live path (owner-scoped catalog + the imported `LOAD_SKILL_POLICY` from skill_lint) and force a `TriggerDecision`; the target model gets NO tools in this shot (V5 — no privilege boundary). (3) `auto_seed_cases(skill, sibling_skills)` — should-fire = paraphrases of the description, should-NOT = sibling catalog skills' paraphrased trigger prompts + a small fixed generic off-topic set (D-04, Open-Q2); siblings come from the owner-scoped query ONLY. (4) Pure scoring: `split_held_out(cases, ratio=0.6)`, `aggregate_repeats(results, repeats=3)`, `score_candidate(...)` → a per-provider cell `{provider, model, axes: {fires, no_false}}`, and `pick_winner(candidates)` BY HELD-OUT score. (5) `configured_targets(settings)` — the Pattern-4 adaptivity: providers with a non-empty api_key OR a local base_url, one representative model each, OpenRouter and native zhipu/moonshot/minimax/deepseek as distinct entries; a provider with neither key nor base_url never appears. Write the pure-scoring tests (test_skill_tuner_scoring.py) and the mocked-forced_emit tests (test_skill_tuner_service.py) FIRST (RED), then implement to GREEN. Mock forced_emit in the service tests — NO live LLM calls in unit tests.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_skill_tuner_scoring.py backend/tests/unit/test_skill_tuner_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Winner-by-held-out test green: a train-winner/held-out-loser candidate is NOT picked.
    - Both sub-scores present in every cell: `grep -c "fires\|no_false" backend/app/services/skill_tuner_service.py` ≥ 2.
    - The classifier imports the shared policy: `grep -c "LOAD_SKILL_POLICY" backend/app/services/skill_tuner_service.py` ≥ 1.
    - Owner-scoped auto-seed: `grep -c "is_global.eq.true" backend/app/services/skill_tuner_service.py` ≥ 1.
    - No Union/anyOf in the schemas: `grep -c "Union\[\|anyOf\|oneOf" backend/app/services/skill_tuner_service.py == 0`.
    - The service never opens the agent loop or a raw SDK client: `grep -v '^#' backend/app/services/skill_tuner_service.py | grep -c "run_agent_loop\|AsyncOpenAI\|anthropic.Anthropic\|genai.Client" == 0` (all emission goes through forced_emit).
    - configured_targets test proves N=1 is valid and a keyless provider never appears.
  </acceptance_criteria>
  <done>skill_tuner_service exposes candidate generation, policy-faithful per-case classification, 60/40 held-out scoring (winner by held-out, both fires/no-false sub-scores), owner-scoped auto-seed, and N-column configured-target adaptivity — all over forced_emit, with green unit suites and the leak-safe query enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author description / benchmark cases → forced_emit | User-authored text crosses into builder + target model prompts |
| tuner service → skills catalog | The auto-seed reads sibling skills (must stay owner-scoped) |
| configured-targets probe → Settings keys | The target set is derived from API-key presence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-03-01 | Information Disclosure | The sibling auto-seed leaks another user's private skill names/descriptions as should-NOT bait | mitigate | auto_seed_cases uses the EXACT owner-scoped `.or_(user_id.eq.{id}, is_global.eq.true)` catalog query — only the author's own + global skills are seeded. grep gate + a query-shape test enforce it. |
| T-123-03-02 | Information Disclosure | configured_targets reads provider API keys | mitigate | configured_targets reads key PRESENCE only (truthy check / base_url presence), never the value; never logs keys (eval_cross_provider presence-only discipline). |
| T-123-03-03 | Tampering | Prompt-injection in an author-edited should-NOT case prompt steers the classifier | mitigate | The classification shot gives the target model NO tools and NO write capability (V5) — worst case is a wrong PASS/FAIL on one cell, visible to the author who picks the winner. No privilege boundary crossed. |
| T-123-03-04 | Tampering / honesty | The classifier measures a DIFFERENT policy than production (low fidelity) → the author tunes against the wrong target | mitigate | classify_fires imports the shared LOAD_SKILL_POLICY constant from skill_lint (the same text agent_loop uses) — single source (Pitfall 1). A live full-loop spot-check is in the SC#10 UAT (VALIDATION.md). |
| T-123-03-05 | Elevation of capability | A new raw SDK path / agent-loop entry forks the shared provider boundary (D-14 RED LINE) | mitigate | All emission goes through forced_emit; grep gate forbids raw SDK clients / agent-loop calls in the service. |
| T-123-03-SC | Tampering | npm/pip installs | accept | No packages installed this plan (zero new deps per RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_skill_builder_model.py backend/tests/unit/test_skill_tuner_scoring.py backend/tests/unit/test_skill_tuner_service.py -x -q` green.
- grep gates in each task pass (owner-scoped seed; shared policy import; flat schemas; no raw SDK).
- Full backend suite green: `backend/venv/Scripts/python.exe -m pytest backend/tests/ -q`.
</verification>

<success_criteria>
- D-08 builder model is a configurable, provider-agnostic knob with no SPOF.
- The Tuner core measures firing per the REAL policy, picks by held-out, shows both fires/no-false, adapts to N configured targets, and seeds leak-safe.
- All orchestration is over forced_emit (no shared-path fork); no DB schema change.
</success_criteria>

<output>
Create `.planning/phases/123-skill-triggering-quality/123-03-SUMMARY.md` when done.
</output>
