---
phase: 123.1-skill-trigger-tuner-design-fidelity-and-ux-polish
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [BUG-260624-01]
files_modified:
  - backend/app/services/skill_tuner_service.py
  - backend/app/api/skill_tuner.py
  - backend/tests/integration/test_skill_tuner_routes.py
  - frontend/src/lib/api.ts
  - frontend/src/components/skills/tuner/CaseEditor.tsx
  - frontend/src/pages/SkillTunerPage.tsx

must_haves:
  truths:
    - "Opening the Trigger Tuner pre-run shows a focused, capped set of seeded cases (not the entire skill library) — the seeded should-NOT count never exceeds the named cap."
    - "When siblings exceed the cap, the editor shows an honest 'showing N of M — capped' banner with a 'show all N' affordance — capping is never silent."
    - "The seeded case columns are legible (text-sm, not 9-11px) and bounded (max-height + internal scroll) so a long seeded set scrolls instead of building a wall."
    - "The idle/pre-run layout is a full-width single-column stack (description -> CaseEditor -> run bar); the empty 1fr results column is gone pre-run; scoreboard + candidate cards render full-width AFTER a run."
    - "The git diff is confined to the tuner files + planning docs — no scoring core, agent_loop, threads, anthropic_service, CTX-03 trim-pin, StreamsProvider, or useMessages change."
    - "Owner-scoping is unchanged — the cap slices the already-leak-safe fetch_owner_scoped_siblings output; a second user still cannot read another user's seeded cases (404)."
  artifacts:
    - path: "backend/app/services/skill_tuner_service.py"
      provides: "Named seed-cap constant + capped auto_seed_cases / seed_cases_with_provenance"
      contains: "MAX_SEEDED_SHOULD_NOT"
    - path: "backend/app/api/skill_tuner.py"
      provides: "GET /tuner/cases/seeded returns capped should_not slice + a total count"
      contains: "total"
    - path: "frontend/src/components/skills/tuner/CaseEditor.tsx"
      provides: "Bounded, legible case columns with per-column count + cap banner + show-all"
      contains: "scrollbar-thin"
    - path: "frontend/src/pages/SkillTunerPage.tsx"
      provides: "Full-width single-column pre-run stack; full-width results after a run"
      min_lines: 400
    - path: "backend/tests/integration/test_skill_tuner_routes.py"
      provides: "Non-vacuous cap + total tests (siblings > cap)"
      contains: "MAX_SEEDED_SHOULD_NOT"
  key_links:
    - from: "backend/app/api/skill_tuner.py"
      to: "skill_tuner_service.seed_cases_with_provenance"
      via: "capped slice + total in the GET /tuner/cases/seeded payload"
      pattern: "seed_cases_with_provenance"
    - from: "frontend/src/pages/SkillTunerPage.tsx"
      to: "frontend/src/components/skills/tuner/CaseEditor.tsx"
      via: "CaseEditor rendered inside the full-width idle stack"
      pattern: "CaseEditor"
    - from: "frontend/src/components/skills/tuner/CaseEditor.tsx"
      to: "SeededCasesResponse.total"
      via: "cap banner reads total vs shown count"
      pattern: "total"
---

<objective>
Close the single live-UAT gap from Phase 123.1 (BUG-260624-01 HIGH #1, design fidelity at the org's real scale): opening the Trigger Tuner pre-run for a skill with a large library renders the seeded case editor as an illegible wall of tiny cramped text jammed in the 360px config rail while the wide results column sits empty.

Build to the operator-approved design — sketch 045 Variant B "Full-width stack" (G-2 satisfied). Three compounding root causes are fixed:
1. Backend caps the auto-seeded should-NOT set (named constant), and `GET /tuner/cases/seeded` returns the capped slice PLUS a `total` so the UI can show "showing N of M — capped" honestly.
2. `CaseEditor` columns get max-height + internal scroll + `text-sm` legibility + a per-column count + the cap banner with "show all N".
3. `SkillTunerPage` replaces the pre-run 360px rail with a full-width single-column stack (description -> CaseEditor -> run bar); scoreboard + candidate cards render full-width AFTER a run (reusing the existing 123.1 components untouched).

Purpose: Make the already-built Tuner pre-run editor read cleanly at the org's real scale.
Output: Capped, honest backend seed; legible bounded CaseEditor; full-width pre-run layout; extended non-vacuous tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-CONTEXT.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-HUMAN-UAT.md
@.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-VERIFICATION.md
@.planning/sketches/045-tuner-prerun-editor-at-scale/README.md
@.planning/sketches/045-tuner-prerun-editor-at-scale/index.html
@./CLAUDE.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly, no exploration. -->

backend/app/services/skill_tuner_service.py — the two seed functions to cap (siblings are
ALREADY owner-scoped; this is the SOLE place to cap, never re-widens scope):
  def auto_seed_cases(skill: dict, sibling_skills: list[dict]) -> dict[str, list[str]]
      # should_not = one sibling description per enabled owned/global sibling (UNCAPPED) + _GENERIC_OFF_TOPIC
      # ALSO drives the RUN path (the benchmark when the client sends no custom cases).
  def seed_cases_with_provenance(skill: dict, sibling_skills: list[dict]) -> dict[str, list[dict[str,str]]]
      # provenance-carrying variant the GET route returns; calls auto_seed_cases internally.
  PROVENANCE_SEEDED = "seeded"; PROVENANCE_SIBLING = "sibling"   # never "held"
  _GENERIC_OFF_TOPIC: tuple[str, ...]   # the fixed off-topic set appended to should_not

backend/app/api/skill_tuner.py:745 — the route to extend with `total`:
  @router.get("/{skill_id}/tuner/cases/seeded")
  async def get_seeded_cases(...) -> dict   # currently {should_fire:[...], should_not:[...]}
  # owner-verify via _fetch_owned_or_global_skill (404 cross-user), then
  # fetch_owner_scoped_siblings (the SOLE leak gate), then seed_cases_with_provenance.

frontend/src/lib/api.ts:2755 — the response type to extend:
  export interface SeededCase { prompt: string; provenance: string }
  export interface SeededCasesResponse { should_fire: SeededCase[]; should_not: SeededCase[] }
  export async function getSeededCases(skillId: string): Promise<SeededCasesResponse>

frontend/src/components/skills/tuner/CaseEditor.tsx — the wall-of-text component:
  export interface EditorCase { id: string; prompt: string; should_fire: boolean; provenance: "seeded"|"sibling"|"you" }
  export function CaseEditor({ cases, onChange, skill }: Props)
  # CaseColumn renders an unbounded <ul> (line 110), prompts at text-xs (117), provenance tag
  # text-[9px] (69), sub-label text-[10px] (102). Two columns in a grid-cols-2 (197).
  # The 60/40 split bar (heldOf per-class, WR-03) MUST be preserved verbatim.

frontend/src/pages/SkillTunerPage.tsx:423 — the layout to replace:
  <div className="grid gap-6 px-8 py-6 lg:grid-cols-[minmax(0,360px)_minmax(0,1fr)]">
  # LEFT rail: current-description section + <CaseEditor> + cost-preview/run-bar.
  # RIGHT col: baseline-scoreboard (D-04) + LiveRunCard + candidate cards / idle placeholder.
  # The mount effect (lines 135-213) hydrates cases via getSeededCases, result via
  # getTunerLatest, target count via getSettings — KEEP all three reconcile fetches.
  # `runPhase` ("idle" | "running" | "error") + `scoreboard` drive pre/post-run state.
</interfaces>

<sketch_contract>
sketch 045 Variant B "Full-width stack" (the binding design):
- Pre-run (idle): NO rail. One full-width single-column stack — current-description panel ->
  CaseEditor (add bar + two wide case columns + split bar) -> run bar. The empty-results void
  is gone by construction (no results to render yet).
- Post-run: results render full-width — the D-04 baseline-scoreboard + LiveRunCard + candidate
  cards (reuse the EXISTING ProviderScoreboard / CandidateCard untouched).
- Case columns: max-height ~340px + internal scroll (`scrollbar-thin`), prompts at `text-sm`,
  a per-column count, and a cap banner "showing N of M seeded · capped to keep the benchmark
  focused" + a "show all N" button. Columns sit in a full-width two-up grid (md:grid-cols-2),
  not ~170px each.
- Backend cap default 8 (the sketch's default cap control); honesty is load-bearing — the
  banner is shown whenever total > shown, never silent truncation.
</sketch_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cap the auto-seeded should-NOT set (named constant) + return total from the seeded GET</name>
  <files>backend/app/services/skill_tuner_service.py, backend/app/api/skill_tuner.py, backend/tests/integration/test_skill_tuner_routes.py</files>
  <read_first>
    - backend/app/services/skill_tuner_service.py (lines 233-321: auto_seed_cases, seed_cases_with_provenance, PROVENANCE_* constants, _GENERIC_OFF_TOPIC at line 61)
    - backend/app/api/skill_tuner.py (lines 745-773: get_seeded_cases route)
    - backend/tests/integration/test_skill_tuner_routes.py (lines 838-907: test_seeded_cases_returned — mirror its fixtures: _supabase_returning_skill, _FakeRedis, OWNER/OTHER_USER, the patch.object on fetch_owner_scoped_siblings)
    - .planning/sketches/045-tuner-prerun-editor-at-scale/README.md (## Decision — Variant B, build-contract item 1)
    - ./CLAUDE.md (venv, run_in_threadpool for blocking supabase-py, no LangChain)
  </read_first>
  <behavior>
    - Test: seed_cases_with_provenance with 20 distinct siblings (each a unique description) caps the sibling-sourced should_not entries to MAX_SEEDED_SHOULD_NOT; the generic off-topic set is NOT counted against the sibling cap.
    - Test: auto_seed_cases with the same 20 siblings respects the same cap (the run path stays consistent with the editor seed).
    - Test: GET /tuner/cases/seeded with siblings > cap returns at most the cap sibling entries in should_not AND a `total` equal to the full uncapped sibling-sourced count; should_fire is untouched.
    - Test: with siblings <= cap, total equals the shown sibling count and no truncation happens (banner-suppression path).
    - Test (preserved): cross-user GET still 404s; owner-scoping unchanged.
  </behavior>
  <action>
    Add a module-level named constant `MAX_SEEDED_SHOULD_NOT = 8` (the sketch 045 default cap) in skill_tuner_service.py with a comment that the cap keeps the pre-run benchmark focused and applies to BOTH the editor seed AND the run path (the editor shows exactly what runs — honest). Cap ONLY the sibling-sourced should_not entries (the uncapped growth source at lines 258-263); ALWAYS keep the full `_GENERIC_OFF_TOPIC` set so the false-fire baseline never disappears. Apply the cap inside `auto_seed_cases` by slicing the sibling-derived list to MAX_SEEDED_SHOULD_NOT before extending with _GENERIC_OFF_TOPIC. Do NOT touch fetch_owner_scoped_siblings, the scoring core, split_held_out, or the held-out math — the cap is a pure post-fetch slice of the already-leak-safe sibling list.

    In `seed_cases_with_provenance` (which calls auto_seed_cases), expose the full uncapped sibling-sourced count to the caller so the route can compute `total` — return it as an additional key (e.g. `should_not_total`) OR compute it in the route from `len(siblings)` filtered the same way auto_seed_cases filters (sib_desc present AND != this skill's description). Document the choice in a docstring line. The capped should_not slice still carries provenance ("sibling" for sibling entries, "seeded" for the generic off-topic set), never "held".

    In get_seeded_cases (skill_tuner.py:745), add a top-level `total` field to the returned dict = the full uncapped sibling-sourced should_not count (so the UI shows "showing N of M — capped"). Keep should_fire/should_not as the capped provenance lists. Preserve the owner-verify-first ordering (_fetch_owned_or_global_skill -> 404 cross-user) and the run_in_threadpool wrap on fetch_owner_scoped_siblings exactly as-is. `total` reflects sibling-sourced entries before the cap; the generic off-topic set is constant and not part of the cap accounting.

    Extend test_skill_tuner_routes.py mirroring test_seeded_cases_returned's style/fixtures. Import MAX_SEEDED_SHOULD_NOT from skill_tuner.skill_tuner_service. Add a service-level test that drives the real auto_seed_cases / seed_cases_with_provenance with (MAX_SEEDED_SHOULD_NOT + N) distinct siblings and asserts the sibling-sourced should_not count == MAX_SEEDED_SHOULD_NOT. Add a route test patching fetch_owner_scoped_siblings to return > cap distinct siblings and assert: len(sibling-provenance should_not) <= MAX_SEEDED_SHOULD_NOT AND body["total"] == the full uncapped sibling count. Tests MUST be non-vacuous (real functions, > cap siblings, assert both the cap and total).
  </action>
  <verify>
    <automated>cd backend && source venv/Scripts/activate && python -m pytest tests/integration/test_skill_tuner_routes.py -k "seed" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MAX_SEEDED_SHOULD_NOT" backend/app/services/skill_tuner_service.py` >= 2 (definition + use)
    - `grep -n "MAX_SEEDED_SHOULD_NOT = 8" backend/app/services/skill_tuner_service.py` matches (named constant, sketch default)
    - `grep -n '"total"' backend/app/api/skill_tuner.py` OR `grep -n "total" backend/app/api/skill_tuner.py` shows total added to the seeded GET return
    - `grep -c "MAX_SEEDED_SHOULD_NOT" backend/tests/integration/test_skill_tuner_routes.py` >= 1 (non-vacuous cap assertion)
    - `cd backend && source venv/Scripts/activate && python -m pytest tests/integration/test_skill_tuner_routes.py -k "seed" -x -q` exits 0
    - `git diff --name-only` shows NO change to agent_loop.py, threads.py, anthropic_service.py
    - `grep -n "fetch_owner_scoped_siblings" backend/app/api/skill_tuner.py` still present and unchanged (owner-scoping intact)
  </acceptance_criteria>
  <done>seed functions cap the sibling-sourced should_not at the named constant (run + editor consistent), the generic off-topic set is always kept, the GET returns the capped slice + an honest total, owner-scoping is unchanged, and non-vacuous tests prove the cap + total with > cap siblings.</done>
</task>

<task type="auto">
  <name>Task 2: Make CaseEditor legible + bounded with a per-column count and the cap banner (sketch 045-B)</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/skills/tuner/CaseEditor.tsx, frontend/src/pages/SkillTunerPage.tsx</files>
  <read_first>
    - frontend/src/components/skills/tuner/CaseEditor.tsx (the whole file — CaseColumn line 76-135, ProvenanceTag line 65, the grid-cols-2 at 197, the 60/40 split bar at 218-230 which MUST be preserved verbatim)
    - frontend/src/lib/api.ts (lines 2755-2789: SeededCase, SeededCasesResponse, getSeededCases)
    - frontend/src/pages/SkillTunerPage.tsx (lines 135-213: the mount effect that hydrates cases from getSeededCases; line 440 where CaseEditor is rendered)
    - frontend/src/components/layout/NavPanel.tsx (existing `scrollbar-thin` usage — reuse the same class)
    - .planning/sketches/045-tuner-prerun-editor-at-scale/index.html (lines 55-68: .case-list.capped max-height 340px + scrollbar-thin, .cap-banner markup + "show all N" button; lines 464-492: renderColumns cap-banner text "showing N of M seeded · capped to keep the benchmark focused")
    - .planning/sketches/045-tuner-prerun-editor-at-scale/README.md (## Decision — Variant B, build-contract item 3)
  </read_first>
  <action>
    Extend `SeededCasesResponse` in api.ts with a `total: number` field (the uncapped sibling-sourced should_not count from Task 1). Do NOT change `SeededCase` or the `getSeededCases` fetch logic beyond consuming the new field. Thread `total` from the mount effect in SkillTunerPage into CaseEditor via a new optional prop (e.g. `seededNotTotal?: number`) so the banner can read "showing {shown} of {total}". When the seeded-cases fetch fails (the existing catch that sets `[]`), leave the total undefined so no banner shows.

    In CaseEditor.tsx, fix the three legibility/bound defects without disturbing the preserved elements:
    - Case-column prompt text: bump from `text-xs` (line 117) to `text-sm`; bump the column sub-label from `text-[10px]` (line 102) to `text-xs`; bump the ProvenanceTag from `text-[9px]` (line 69) to `text-[10px]` (still small but legible) — the prompt is the thing that must read.
    - Bound the case `<ul>` (line 110): add `max-h-[340px] overflow-y-auto scrollbar-thin` so a long seeded set scrolls instead of stacking into a wall (mirrors the sketch's `.case-list.capped { max-height: 340px }` + `scrollbar-thin`).
    - Add a per-column count to the CaseColumn header (next to the "Should fire" / "Should NOT fire" title) showing the column's case length.
    - Add the cap banner ABOVE the should-NOT column's list (only when `seededNotTotal` is provided AND `seededNotTotal > shownNoFireCount`): an amber-toned banner reading "showing {shown} of {total} seeded · capped to keep the benchmark focused" with an inline "show all {total}" button. "show all" is a non-destructive affordance — since the editor only holds the capped set client-side, wire the button to a clear honest behavior (e.g. a disabled-until-future / link-out, OR simply reveal that the cap is a backend default the author can extend by adding cases). Document the chosen behavior in a code comment; it MUST NOT silently fabricate the uncapped cases the backend withheld.
    - Keep the columns in a full-width two-up grid: ensure the grid is `grid-cols-1 md:grid-cols-2 gap-3` so each column gets ~half the FULL page width once the page is full-width (Task 3), not ~170px.
    - PRESERVE verbatim: the provenance tags + PROVENANCE_LABEL, the 60/40 split bar (the heldOf per-class WR-03 math at lines 47-57 and 218-230), the add/remove/edit bar, and the should-NOT = false-fire-rail visual treatment (the `bg-[hsl(0_50%_50%/0.06)]` rail tint).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run src/components/skills/tuner/ --reporter=dot 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max-h" frontend/src/components/skills/tuner/CaseEditor.tsx` >= 1
    - `grep -c "scrollbar-thin" frontend/src/components/skills/tuner/CaseEditor.tsx` >= 1
    - `grep -c "text-sm" frontend/src/components/skills/tuner/CaseEditor.tsx` >= 1 (prompts bumped)
    - `grep -E -c "text-\[9px\]" frontend/src/components/skills/tuner/CaseEditor.tsx` shows the 9px prompt size removed (no prompt at 9px; provenance tag may be text-[10px])
    - `grep -i -c "showing" frontend/src/components/skills/tuner/CaseEditor.tsx` >= 1 (cap banner present)
    - `grep -i -c "show all" frontend/src/components/skills/tuner/CaseEditor.tsx` >= 1
    - `grep -c "total" frontend/src/lib/api.ts | head` shows `total` added to SeededCasesResponse (verify the interface block contains `total`)
    - `grep -c "split-bar" frontend/src/components/skills/tuner/CaseEditor.tsx` >= 1 (60/40 split bar preserved)
    - `grep -c "heldOf" frontend/src/components/skills/tuner/CaseEditor.tsx` >= 2 (WR-03 per-class math preserved)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The seeded case columns read at text-sm, are bounded with a max-height + scrollbar-thin internal scroll, show a per-column count, and (when siblings exceeded the cap) show an honest "showing N of M — capped" banner with a "show all N" affordance — while the provenance tags, the WR-03 60/40 per-class split bar, add/remove/edit, and the false-fire-rail tint are preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the pre-run 360px rail with a full-width single-column stack; results full-width after a run (sketch 045-B)</name>
  <files>frontend/src/pages/SkillTunerPage.tsx</files>
  <read_first>
    - frontend/src/pages/SkillTunerPage.tsx (lines 405-527: the two-column body, the line-423 grid, the LEFT rail at 424-456 with current-description + CaseEditor + cost-preview/run-bar, the RIGHT results column at 458-520 with baseline-scoreboard / LiveRunCard / candidate cards / idle placeholder)
    - frontend/src/pages/SkillTunerPage.tsx (lines 135-213: the three reconcile-on-open fetches — KEEP all of them)
    - .planning/sketches/045-tuner-prerun-editor-at-scale/index.html (lines 228-293: VARIANT B markup — mode-idle = vb-stack: description panel -> case panel -> run-bar panel; mode-result = full-width scoreboard)
    - .planning/sketches/045-tuner-prerun-editor-at-scale/README.md (## Decision — Variant B, build-contract item 2)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (established Tuner/run-surface visual language)
  </read_first>
  <action>
    Replace the line-423 `lg:grid-cols-[minmax(0,360px)_minmax(0,1fr)]` two-column grid with a full-width single-column stack for the idle/pre-run state (Variant B). Drive the layout off the existing `runPhase` + `scoreboard` state already in the component (no new mode state machine needed):

    - PRE-RUN / IDLE (runPhase === "idle" AND no completed scoreboard): render a single full-width column (`flex flex-col gap-5 px-8 py-6` or equivalent, NOT a grid) in this order — the current-description section (the "current · live · drives firing" panel, moved out of the old rail), then `<CaseEditor>` (now full-width so its two case columns each get ~half the page), then the cost-preview + "Run tuning" run bar. Pass the seeded `total` into CaseEditor (from Task 2). The empty-results placeholder (lines 511-519) is GONE in this state — there are no results pre-run, so nothing sits empty.
    - DURING-RUN / POST-RUN (runPhase === "running"/"error" OR a completed scoreboard exists): render results full-width — the D-04 standalone baseline-scoreboard block (with the "Built by {builder} · measured on N models" attribution), the LiveRunCard, and the candidate cards — REUSING the existing ProviderScoreboard / CandidateCard / LiveRunCard components UNTOUCHED. Keep a way back to editing if the existing UX had one; do not add new mode-toggle chrome beyond what the sketch implies.

    PRESERVE intact: the D-04 standalone baseline-scoreboard block, the cost-preview (cases × N models × 3 repeats), the run-attribution, the LiveRunCard, the candidate-card sort-by-held-out, and ALL THREE reconcile-on-open fetches (getSeededCases / getTunerLatest / getSettings) in the mount effect — do not remove or reorder them. Do NOT rewrite ProviderScoreboard or CandidateCard. The narrow-rail-vs-wide-results imbalance is the defect; the components themselves are correct.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "minmax(0,360px)" frontend/src/pages/SkillTunerPage.tsx` == 0 (the narrow rail grid is gone)
    - `grep -c "CaseEditor" frontend/src/pages/SkillTunerPage.tsx` >= 1 (still rendered, now full-width)
    - `grep -c "baseline-scoreboard" frontend/src/pages/SkillTunerPage.tsx` >= 1 (D-04 block preserved)
    - `grep -c "cost-preview" frontend/src/pages/SkillTunerPage.tsx` >= 1 (cost preview preserved)
    - `grep -c "getSeededCases" frontend/src/pages/SkillTunerPage.tsx` >= 1 AND `grep -c "getTunerLatest" frontend/src/pages/SkillTunerPage.tsx` >= 1 AND `grep -c "getSettings" frontend/src/pages/SkillTunerPage.tsx` >= 1 (all three reconcile fetches intact)
    - `grep -c "LiveRunCard" frontend/src/pages/SkillTunerPage.tsx` >= 1 (live run preserved)
    - `git diff --stat frontend/src/components/skills/tuner/ProviderScoreboard.tsx` shows NO change (component reused untouched)
    - `git diff --stat frontend/src/components/skills/tuner/CandidateCard.tsx` shows NO change (component reused untouched)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The pre-run/idle Tuner is a full-width single-column stack (description -> CaseEditor -> run bar) with no empty results column; results render full-width after a run via the untouched 123.1 components; the D-04 baseline block, cost-preview, attribution, LiveRunCard, and all three reconcile-on-open fetches are preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Red-line confinement gate — assert the diff touches ONLY the tuner files</name>
  <files>backend/app/services/skill_tuner_service.py, backend/app/api/skill_tuner.py, backend/tests/integration/test_skill_tuner_routes.py, frontend/src/lib/api.ts, frontend/src/components/skills/tuner/CaseEditor.tsx, frontend/src/pages/SkillTunerPage.tsx</files>
  <read_first>
    - .planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-VERIFICATION.md (the red-line list of untouched files)
    - 123.1-CONTEXT.md (the OUT-of-scope red line: scoring core, agent loop, shared chat path, CTX-03 trim-pin)
  </read_first>
  <action>
    This is a verification-only gate (no new production code). Run the full red-line diff check and the full tuner test suite. Confirm `git diff --name-only` (against the phase-start baseline) lists ONLY: backend/app/services/skill_tuner_service.py, backend/app/api/skill_tuner.py, backend/tests/integration/test_skill_tuner_routes.py, frontend/src/lib/api.ts, frontend/src/components/skills/tuner/CaseEditor.tsx, frontend/src/pages/SkillTunerPage.tsx, and planning docs under .planning/. If ANY other source file appears (especially agent_loop.py, threads.py, anthropic_service.py, the CTX-03 trim-pin, StreamsProvider.tsx, useMessages.ts, the scoring core, ProviderScoreboard.tsx, CandidateCard.tsx), STOP and revert that change — it is out of scope. Run the full backend tuner test file (not just -k seed) and the frontend type-check to confirm no regression.
  </action>
  <verify>
    <automated>cd backend && source venv/Scripts/activate && python -m pytest tests/integration/test_skill_tuner_routes.py -q && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` lists ONLY the 6 allowed source files + .planning/ docs — assert NO match for: `agent_loop`, `threads.py`, `anthropic_service`, `StreamsProvider`, `useMessages`, `ProviderScoreboard.tsx`, `CandidateCard.tsx`
    - `git diff --name-only | grep -E "agent_loop|threads\.py|anthropic_service|StreamsProvider|useMessages|ProviderScoreboard|CandidateCard"` returns no lines (exit 1)
    - `cd backend && source venv/Scripts/activate && python -m pytest tests/integration/test_skill_tuner_routes.py -q` exits 0 (full tuner suite green, no regression to the 5 prior seeded/latest tests)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The git diff is confined to the 6 allowed tuner files + planning docs; no scoring core / agent loop / shared chat path / CTX-03 trim-pin / reused component was touched; the full tuner backend suite and the frontend type-check pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /tuner/cases/seeded | A user requests another user's skill's seeded cases (cross-user read). |
| sibling catalog → seed | A sibling skill's description becomes false-fire bait; must stay owner+global scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123.1-05-01 | Information Disclosure | get_seeded_cases (skill_tuner.py:745) | mitigate | Owner-verify-first (_fetch_owned_or_global_skill → 404 cross-user) is UNCHANGED; the cap is a pure post-fetch slice of the already-leak-safe fetch_owner_scoped_siblings output and never re-reads the DB or widens scope. Preserved cross-user-404 test asserts this. |
| T-123.1-05-02 | Tampering | MAX_SEEDED_SHOULD_NOT cap | accept | The cap reduces the should_not set shown/run; it cannot expose data and the generic off-topic baseline is always kept. Honesty banner ("showing N of M") prevents silent truncation deception. Low risk, no PII. |
| T-123.1-05-SC | Tampering | npm/pip/cargo installs | accept | ZERO new packages (no install task; reuses existing scrollbar-thin class + lucide icons). No supply-chain surface introduced. |
</threat_model>

<verification>
- Backend: `cd backend && source venv/Scripts/activate && python -m pytest tests/integration/test_skill_tuner_routes.py -q` — full tuner suite green, including the new non-vacuous cap + total tests and the preserved 5 seeded/latest tests.
- Frontend: `cd frontend && npx tsc --noEmit` — clean type-check across api.ts, CaseEditor.tsx, SkillTunerPage.tsx.
- Red line: `git diff --name-only` confined to the 6 allowed tuner files + .planning/ docs; no agent_loop/threads/anthropic_service/StreamsProvider/useMessages/ProviderScoreboard/CandidateCard change.
- Honesty: the cap banner is shown whenever total > shown (never silent); the generic off-topic baseline is always present.
- Manual (HUMAN-UAT, carries to verify): open the Tuner pre-run for a skill with a large library — the seeded set reads cleanly (text-sm, scrolls within max-height), the cap banner shows "showing 8 of M — capped", and the layout is a full-width single-column stack (no empty results column).
</verification>

<success_criteria>
- The pre-run seeded should_not set is capped at the named MAX_SEEDED_SHOULD_NOT constant (run + editor consistent); the generic off-topic baseline is always kept.
- GET /tuner/cases/seeded returns the capped slice + an honest `total`; the editor shows "showing N of M — capped" + "show all N" when siblings exceed the cap.
- CaseEditor case columns are legible (text-sm) and bounded (max-height + scrollbar-thin) with a per-column count.
- The idle/pre-run page is a full-width single-column stack; results render full-width after a run via the untouched 123.1 components.
- Owner-scoping is unchanged (cross-user 404 preserved); non-vacuous tests prove the cap + total.
- The git diff is confined to the 6 tuner files + planning docs.
</success_criteria>

<output>
Create `.planning/phases/123.1-skill-trigger-tuner-design-fidelity-and-ux-polish/123.1-05-SUMMARY.md` when done.
</output>
