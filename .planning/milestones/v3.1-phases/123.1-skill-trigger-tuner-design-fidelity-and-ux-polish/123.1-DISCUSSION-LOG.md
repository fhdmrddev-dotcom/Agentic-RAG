# Phase 123.1 — Discussion Log

**Date:** 2026-06-24
**Mode:** discuss (default)
**Origin:** Live UAT of Phase 123's Trigger Tuner surfaced design-fidelity + UX gaps. A 5-agent sketch-vs-build audit + functionality/persistence audit confirmed them; filed as BUG-260624-01 and folded into this phase.

## Reported-bugs cross-check (mandatory)
- Listed 9 open `surface: Agentic-RAG` reports. Only **BUG-260624-01** overlaps the tuner/skills domain → **folded into 123.1** (frontmatter updated: `status: folded`, `folded_into: "123.1"`). The other 8 (panel, harness/workflow-ui, title-gen, chat, provider-routing, minimax, chat-banner) do not overlap → left open for their own phases.

## Carried forward (decided earlier in the session — not re-asked)
- Builder-model picker → driven from configured models, strong models selectable (became D-09/D-10).
- Result behavior → show last result on return + overwrite on re-run (became D-07).

## Areas discussed (4 decisions, batched)

| # | Area | Options presented | Operator selection |
|---|------|-------------------|--------------------|
| 1 | Scope | HIGH only / **HIGH + MED** / Everything (HIGH+MED+LOW) | **HIGH + MED** (LOW deferred) |
| 2 | Result persistence | **Durable DB table** / Redis rehydrate only | **Durable DB table** (numbered migration, RLS owner-scoped) → D-07/D-08 |
| 3 | Scoreboard layout @ 8 providers | **Vertical rows** / horizontal-scroll / responsive ≤4-grid-else-rows | **Vertical rows** + full-width results (restores sketch 041) → D-02/D-03/D-04 |
| 4 | Seeded cases | **Show + fully editable before run** / show read-only + add-own | **Show + fully editable before run** → D-05/D-06 |

## Notes
- **G-2:** vertical-rows scoreboard restores sketch 041's original design → no new sketch required.
- LOW cosmetics + tuning-run history + the NL→workflow authoring spike (todo match, unrelated) → deferred.
- Out of scope (must not change): shared chat path, Phase-123 scoring core, CTX-03 trim-pin.
