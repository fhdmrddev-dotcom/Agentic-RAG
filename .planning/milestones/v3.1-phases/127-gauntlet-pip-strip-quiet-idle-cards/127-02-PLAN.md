---
phase: 127-gauntlet-pip-strip-quiet-idle-cards
plan: 02
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - frontend/src/components/workflows/PublishGauntlet.tsx
  - frontend/src/components/workflows/PublishGauntlet.test.tsx
autonomous: true
requirements: [WUX-03]

user_setup: []

must_haves:
  truths:
    - "The 8 server-fixed gauntlet stages render as a compact horizontal energy-spine (one node + one 3D icon per stage), not eight wrapping boxes — passed stages read green, the golden-run node reads amber with an energy comet, a blocked stage reads red; blocked_stage server truth still drives the tone (SC#1)."
    - "A resolved gauntlet LEADS with a plain-worded verdict (Published / Blocked-by-grader / Blocked-early); the raw 5-field PublishVerdict grid is DEMOTED behind a <details> 'Show raw verdict' and the '▦ rendered verbatim' provenance cap stays INSIDE the disclosure (SC#1)."
    - "Every existing gauntlet honesty contract survives unchanged: verbatim verdict never re-derived; judge HARD WALL (no enabled override, the <s>publish anyway</s> strike stays); 4 DISTINCT HTTP outcomes (200-block/400/404/409); run-link gated on golden_run_id != null; judge per-criterion rows first-class on a block; named_failures KEY-DETECTION."
    - "The golden-run wait is the hero (glowing panel + live elapsed clock); engine chips render via providerLogo(), honestly-absent when the provider is unknown — never a fabricated provider."
  artifacts:
    - path: "frontend/src/components/workflows/PublishGauntlet.tsx"
      provides: "The energized gauntlet re-skin: energy-spine, worded verdict leading, raw grid behind <details>, golden-run hero — all honesty contracts intact"
      contains: "raw-verdict"
    - path: "frontend/src/components/workflows/PublishGauntlet.test.tsx"
      provides: "Extended assertions: worded verdict leads, raw grid inside <details data-testid=raw-verdict>, verbatim cap inside the disclosure — plus all existing honesty assertions stay green"
      contains: "raw-verdict"
  key_links:
    - from: "frontend/src/components/workflows/PublishGauntlet.tsx (GauntletSpine)"
      to: "the server blocked_stage (verdict.blocked_stage)"
      via: "the blockedIndex derivation (visual tone only — never recomputes pass/block)"
      pattern: "blockedIndex"
    - from: "frontend/src/components/workflows/PublishGauntlet.tsx (PublishingNotice / verdict block)"
      to: "frontend/src/lib/providerLogo.tsx"
      via: "engine-chip import + render-or-Bot-fallback"
      pattern: "providerLogo"
---

<objective>
Re-skin the publish gauntlet (`PublishGauntlet.tsx`, winner 051-A) for density + energy, riding UNDER the unchanged Phase-124 `<WorkflowSoul scale="pub">` block: turn the 8 wrapping-box stages into a compact horizontal energy-spine with a 3D icon per stage, lead the resolved state with a plain-worded verdict, and demote the raw 5-field `PublishVerdict` grid behind a `<details>` "Show raw verdict" — while preserving every honesty contract verbatim.

This is a PRESENTATION-ONLY re-skin of one already-shipped file. The publish logic, the `PublishOutcome` switch, the verbatim verdict render, the judge hard wall, the 4 HTTP outcomes, the run-link gating, and the modal shell DO NOT change behavior. The existing `PublishGauntlet.test.tsx` (honesty contract + source-grep guards) MUST stay green unchanged; new visual contracts are added as EXTENDED assertions in that same file.

Purpose: The publish moment reads in ~3 seconds for a business user (a plain-worded pass/block), while the verbatim 5-field verdict + the `▦ rendered verbatim` provenance cap remain one click away — honesty preserved, just not leading (the sketch's "What to Avoid": never lead with the raw grid).
Output: `PublishGauntlet.tsx` re-skinned (energy-spine + worded verdict + raw-on-demand + golden-run hero + engine chips); `PublishGauntlet.test.tsx` extended with the raw-on-demand + worded-verdict + spine-shape assertions.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-RESEARCH.md
@.planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-PATTERNS.md
@.planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/references/workflow-energized-reskin.md
@.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md
@.claude/skills/sketch-findings-agentic-rag/sources/051-gauntlet-pip-strip/README.md

<interfaces>
<!-- The seams this plan re-skins. Re-skin the MARKUP/CSS; preserve the named logic + every testid/role. -->

The 8 STAGES array (PublishGauntlet.tsx:69-78) — each `{label, what, codes}`; the labels are grep-asserted (PublishGauntlet.test.tsx:279-291). Verified per-stage fluent-emoji slug (RESEARCH §Pitfall 2, all PRESENT): Owner→`shield`, Definition valid→`check-mark-button`, business_requirement→`bullseye`, Structural lint→`magnifying-glass-tilted-left`, Interactive-phase→`raised-hand`, Golden run→`rocket`, Structural gate→`locked`, Independent judge→`balance-scale`. (Goal/golden-run uses `bullseye`/`rocket`, NEVER `direct-hit` — the empty trap.)

GauntletSpine (PublishGauntlet.tsx:274-301) — `const blockedIndex = blockedStage ? STAGES.findIndex(s => s.codes.includes(blockedStage)) : -1`; `isPassed = blockedIndex === -1 ? !running : i < blockedIndex`; the amber running node is `running && i === 5`. KEEP `data-testid="gauntlet-spine"` + the 8 `stage.label` strings. This derivation STAYS — only the per-stage `<div className="rounded border …">` box becomes an energy node.

VerdictFields (PublishGauntlet.tsx:204-220) — the verbatim 5-row grid; each `VerdictRow` keeps `data-testid={`verdict-${name}`}`; the `▦ rendered verbatim from the server — not re-derived` cap is the last child (212-217). Render UNCHANGED, just relocated INSIDE the new <details>.

The resolved verdict block (PublishGauntlet.tsx:487-532): `data-testid={isSuccess ? "publish-success" : "publish-block"}`; the worded headline currently reads "Published v{n}" / "Blocked at {stage}". CriterionRow/LintRow/PhaseRow/SummaryLine via renderFailure (named_failures map, 516-523) stay FIRST-CLASS (OUTSIDE the disclosure). RunLink (gated on golden_run_id) + HardWall (the <s>publish anyway</s> strike) unchanged.

PublishingNotice (PublishGauntlet.tsx:309-332) — keep `data-testid="publish-elapsed"` + the live-clock logic (310-312).

Engine chip pattern (mirror RunCard.tsx:256 + 310-311): `import { providerLogo } from "@/lib/providerLogo"`; `import { Bot } from "lucide-react"`; `const EngineMark = providerLogo(provider)`; render `{EngineMark ? <EngineMark size={18}/> : <Bot className="w-4 h-4"/>}`. The provider must be the resolved `runs.provider` string (keys are `zhipu`/`moonshot`, NOT `glm`/`kimi`).

The 3D phase-glyph resolver from Plan 01: `import { phaseGlyph } from "@/lib/phaseGlyph"` is for PHASE TYPES (the soul/spine). The gauntlet STAGES are a DIFFERENT decorative set (shield/rocket/balance-scale…) — import those bundled SVGs directly from `~icons/fluent-emoji/<slug>` (the Vite plugin Plan 01 registered), following the same build-time, verified-slug discipline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Energy-spine + golden-run hero + engine chips (presentation only)</name>
  <files>frontend/src/components/workflows/PublishGauntlet.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PublishGauntlet.tsx (the full file — focus: STAGES 69-78, GauntletSpine 274-301, PublishingNotice 309-332, and where the spine + notice mount in GauntletContent 443-446)
    - .claude/skills/sketch-findings-agentic-rag/references/workflow-energized-reskin.md (CSS Patterns: `.econn.flowing` + `@keyframes flow`, `.enode.run::before` + `@keyframes auraPulse`; HTML Structures: the 051 `.espine` stage spine; "What to Avoid")
    - .claude/skills/sketch-findings-agentic-rag/sources/051-gauntlet-pip-strip/README.md (the energy-spine + golden-run-hero intent; the icon-sourcing build note)
    - frontend/src/components/chat/RunCard.tsx:252-256, 304-314 (the canonical providerLogo() → Bot-fallback engine-chip consumer to mirror)
    - frontend/src/index.css (the real app tokens to use: --panel-status-active is the amber family, --panel-status-done the green, --accent-violet; do NOT invent the sketch-only --neon-cyan / --grad-energy — use the existing token language / Tailwind amber the file already uses at lines 290/314)
  </read_first>
  <action>
    Re-skin `GauntletSpine` (274-301): keep the `blockedIndex` derivation and the `isBlocked`/`isPassed`/`running && i===5` tone logic VERBATIM; keep `data-testid="gauntlet-spine"` on the container and render all 8 `stage.label` strings. Replace each per-stage `<div className="rounded border …">` box with a compact energy node: a node holding the stage's bundled 3D `<Icon/>` (import each from `~icons/fluent-emoji/<verified-slug>` per the Interfaces map), the stage label, a `✓` badge on passed nodes, the red tone on the blocked node, and an amber pulsing aura + a horizontal energy comet flowing into the running golden-run node (`i===5`). Lay the 8 nodes out as a single horizontal spine with energy connectors between them (the `.espine`/`.econn` structure adapted to Tailwind + the app's amber/green/violet tokens). Honor `prefers-reduced-motion`: gate the comet/aura animations behind it (no motion when set) — follow the existing panel pattern (PanelSection/PanelRail already use it).

    Re-skin `PublishingNotice` (309-332) into the golden-run hero: keep `data-testid="publish-elapsed"` and the live mm/ss clock logic unchanged; add the glowing-panel aura (amber, reduced-motion-gated) so the synchronous golden-run wait reads as the hero moment, not a spinner. Add engine chips: source the provider HONESTLY (the run-level provider if available to this surface; otherwise render NO chip — do not fabricate). Render each chip with the providerLogo() → Bot-fallback pattern (`const EngineMark = providerLogo(provider); EngineMark ? <EngineMark size={18}/> : <Bot/>`). If no honest provider value is available on the publish surface, omit the chip entirely (honestly-absent) rather than guessing — this still satisfies the hero (glow + live clock).

    Do NOT touch: the `publishWorkflow` call, the `PublishOutcome` switch, `runGauntlet`, the `isSuccess`/`isBlock` derivation, or the modal shell. This is markup + CSS + the additive engine chip only.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `gauntlet-spine` testid present; all 8 stage labels still render (`PublishGauntlet.test.tsx` "the 8 server-fixed stages render" stays green).
    - The `blockedIndex` / `isPassed` / `running && i===5` derivation is byte-unchanged (visual tone only); `verdict.blocked_stage` still drives the highlight.
    - Each gauntlet stage renders a bundled 3D icon imported from `~icons/fluent-emoji/<slug>` using only verified slugs (no `direct-hit`/`no-entry-sign`; no `iconify-icon`/CDN).
    - `publish-elapsed` testid + the live clock unchanged; the comet/aura animations are gated behind `prefers-reduced-motion`.
    - Engine chips render via `providerLogo()` with the `Bot` fallback; when no honest provider is available the chip is OMITTED (no fabricated provider).
    - `cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx` passes — every EXISTING assertion stays green (G-5 visual-only proof for this file).
  </acceptance_criteria>
  <done>The 8 stages read as an alive energy-spine and the golden-run wait reads as the hero; the spine's pass/block tone is still server-truth-driven; engine chips are honest (real provider or absent).</done>
</task>

<task type="auto">
  <name>Task 2: Worded verdict leads + raw 5-field grid behind a <details> (honesty preserved) + test extension</name>
  <files>frontend/src/components/workflows/PublishGauntlet.tsx, frontend/src/components/workflows/PublishGauntlet.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/PublishGauntlet.tsx (the resolved verdict block 487-532: the worded headline at 494-503, VerdictFields render at 513, named_failures map 516-523, RunLink 526-527, HardWall 530; VerdictFields itself 204-220 with the ▦ cap 212-217)
    - frontend/src/components/workflows/PublishGauntlet.test.tsx (the FULL file — the honesty contract tests 105-272 + the source-grep guards 330-349 that MUST stay green; mirror the openModal()/doPublish() helpers 48-63 for new assertions)
    - .claude/skills/sketch-findings-agentic-rag/references/workflow-energized-reskin.md (CSS Patterns: `.rawbox > summary::before` disclosure; HTML Structures: the `.wverdict` worded-verdict-first block + the per-criterion rows first-class + the raw grid behind `<details class="rawbox">`)
    - .planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-VALIDATION.md (Wave 0: assert worded verdict leads, raw grid inside <details raw-verdict>, ▦ cap inside)
  </read_first>
  <action>
    In the resolved verdict block (487-532): add a NEW plain-worded headline that LEADS the block, derived from server truth (`verdict.published` / `verdict.blocked_stage`) — e.g. "🎉 Published — v{version} is live" on success; "⚖️ Blocked by the grader — the run finished, but the independent grader would not pass the result" when `blocked_stage === "judge"`; "⛔ Blocked early — {stage}" otherwise. Lead-with-words: this headline replaces the bare "Published v{n}" / "Blocked at {stage}" line as the FIRST thing in the block (keep the success/block color + the ✓/✕ glyph). Use the worded copy as plain text children (XSS rule). The exact copy strings are Claude's discretion per RESEARCH §Discretion — lead with words, confirm against the sketch at UAT.

    Wrap `<VerdictFields verdict={verdict} />` (currently rendered at line 513) in `<details data-testid="raw-verdict"><summary>Show raw verdict — the 5 server fields, verbatim</summary> … </details>`. Render VerdictFields UNCHANGED inside — the `▦ rendered verbatim from the server — not re-derived` cap (lines 212-217) MUST stay inside the disclosure (it is part of VerdictFields, so keeping VerdictFields whole satisfies this — do not split the cap out). Style the disclosure with the `.rawbox > summary::before` rotating-caret pattern (Tailwind/CSS).

    Keep FIRST-CLASS (OUTSIDE the <details>): the `named_failures` map render (516-523 — CriterionRow/LintRow/PhaseRow/SummaryLine via renderFailure), RunLink (526-527), and HardWall (530). The judge per-criterion rows are the worded "why" and must NOT move into the disclosure. Do not touch renderFailure, VerdictRow, RunLink, or HardWall internals.

    EXTEND PublishGauntlet.test.tsx (do NOT modify existing assertions): add tests that (a) on a resolved block, a plain-worded headline leads the block (assert the worded copy is present and precedes the `raw-verdict` details in DOM order); (b) the raw 5-field grid is INSIDE `<details data-testid="raw-verdict">` (assert `screen.getByTestId("raw-verdict").tagName === "DETAILS"` and that `verdict-published` is a descendant of it); (c) the `▦ rendered verbatim` cap text is INSIDE the raw-verdict details; (d) on a judge block, the per-criterion row (e.g. `grounded_in_evidence`) is NOT inside the raw-verdict details (stays first-class). Reuse openModal()/doPublish() and the existing judge-block / success / pre-stage-3 mock shapes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - A resolved block renders a plain-worded headline FIRST; on a judge block it conveys "blocked by the grader / hard wall", on early block it names the stage, on success it conveys "Published v{n}".
    - `<details data-testid="raw-verdict">` wraps VerdictFields; `verdict-published`/`verdict-version`/`verdict-golden_run_id`/`verdict-blocked_stage`/`verdict-named_failures` are all descendants of it; the `▦ rendered verbatim` cap is inside it.
    - The judge per-criterion rows + the server summary + RunLink + HardWall remain OUTSIDE the disclosure (first-class).
    - ALL existing PublishGauntlet.test.tsx assertions stay green UNCHANGED: verbatim verdict (a 200-block stays a block), 4 HTTP outcomes distinct, judge hard-wall (no enabled override + the `<s>publish anyway</s>` strike), run-link gating, named_failures key-detection, the source-grep guards (330-349: `typeof entry === "string"`, `"criterion" in`, `"code" in`, `publishWorkflow`, the `<s>` strike, `golden_run_id != null`).
    - The 4 new assertions (worded-leads / grid-in-details / cap-in-details / criteria-first-class) pass.
    - `cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx` passes.
  </acceptance_criteria>
  <done>The resolved gauntlet leads with words; the raw verbatim verdict + its provenance cap are one click away inside a disclosure; the judge's "why" stays first-class; no honesty contract softened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server publish verdict → client render | The 5-field `PublishVerdict` + `named_failures` (agent/judge-authored strings) cross into the gauntlet UI. |
| bundled stage icons → DOM | The decorative 3D stage SVGs are rendered into the resolved/in-flight gauntlet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-03 | Repudiation | Worded verdict + the demoted raw grid | mitigate | The worded headline is DERIVED from server truth (`verdict.published`/`blocked_stage`), never re-derives pass/block; the verbatim `PublishVerdict` + the `▦ rendered verbatim` cap stay (inside the <details>), so the provenance is one click away, not removed. Grep-guarded by the existing `verdict != null` / verbatim tests. |
| T-127-04 | Tampering / Elevation | named_failures / judge evidence / verdict strings | mitigate | All rendered as plain React text children (auto-escaped) via the unchanged CriterionRow/LintRow/SummaryLine/VerdictRow — never `dangerouslySetInnerHTML`. The re-skin does not introduce any raw-HTML render path. |
| T-127-05 | Tampering | judge hard-wall softening | mitigate | The `<s>publish anyway</s>` strike + the no-enabled-override invariant stay (HardWall unchanged); the source-grep guard (`PublishGauntlet.test.tsx:340-345`) blocks a regression. |
| T-127-06 | Tampering | Provenance via fabricated engine chip | accept→mitigate | The engine chip renders ONLY from a real resolved provider value or is OMITTED (honestly-absent) — never a guessed provider; `providerLogo()` returns null for unmapped → no false brand. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/PublishGauntlet.test.tsx` — all green (existing honesty + source-grep + the 4 new visual assertions).
- DOM-order proof: the worded headline precedes `raw-verdict`; `verdict-*` rows + the `▦` cap are inside `raw-verdict`; the per-criterion rows are outside it.
- No `iconify-icon`/`code.iconify.design` reference in PublishGauntlet.tsx; stage icons import from `~icons/fluent-emoji/*` (verified slugs).
- Per-wave merge gate: `cd frontend && npm test` (full Vitest suite) green.
- MANUAL UAT (authored in 127-VALIDATION.md, NOT duplicated here): the resolved-states walkthrough + all-4-HTTP-outcomes-distinct + the cross-provider engine-chip logo check (SC#10).
</verification>

<success_criteria>
- SC#1 met: the gauntlet renders as an energy-spine (pip-strip) + a plain-worded verdict with the raw 5-field grid on demand behind a disclosure.
- Every existing gauntlet honesty contract (verbatim · judge hard-wall · 4 HTTP outcomes · run-link gating · criteria-first-on-block · key-detection) stays green unchanged — the visual-only proof for this file.
- The golden-run wait reads as the hero; engine chips are honest (real provider or absent).
</success_criteria>

<output>
Create `.planning/phases/127-gauntlet-pip-strip-quiet-idle-cards/127-02-SUMMARY.md` when done.
</output>
