---
status: partial
phase: 127-gauntlet-pip-strip-quiet-idle-cards
source: [127-01-SUMMARY.md, 127-02-SUMMARY.md, 127-03-SUMMARY.md, 127-VALIDATION.md]
started: 2026-06-28T00:00:00Z
updated: 2026-06-28T14:00:00Z
---

## Current Test

[testing paused — 3 items outstanding (1 skipped, 2 blocked by environment)]

## Tests

### 1. Phase card density (quiet-idle / alive-active)
expected: Open a workflow run in the app. Observe the live phase spine (the left panel showing phases). Idle (not-yet-started) steps should look quiet and still — no spinner, no pulse, no placeholder text. The one currently-running step should be unmistakable: amber glow on the left border, a pulse dot + "Working" label in an activity line, and possibly an engine chip if the phase has a provider. Done steps should settle calmly — folded to a one-line essence without animation.
result: pass
note: |
  Live run (Project Meridian Risk Summary GOOD, 2026-06-28 Chrome MCP): Running "summarize" phase
  showed amber left-border bloom + amber background wash + "● Running" badge + "● Working" activity
  label ✅. Done phases (phase-0 + summarize) folded to compact single-row: 3D glyph emoji + name +
  "✓ Complete" badge ✅. No animation on done cards ✅. Engine chip absent — correctly honest (no
  subAgents provider for this workflow) ✅.
auto_verified:
  - done_state_card_class: "flex flex-col rounded-md border transition-colors border-border/50 bg-card/30"
  - done_state_aria_expanded: "false"
  - css_idle_opacity60: true
  - css_running_border_l_3px: true
  - css_amber_bloom: true
  - css_motion_safe: true

### 2. Gauntlet resolved states walkthrough
expected: In the Workflow Studio, trigger a publish flow and walk through all resolved states. Resting = compact node spine, quiet. Golden-run (while publishing) = hero notice with amber breathing glow + rocket node + live mm:ss clock. Published = "🎉 Published — v{N} is live" leads the resolved block (the worded headline shows before the detail). Judge block = "⚖️ Blocked by the grader…" with per-criterion rows first-class, raw 5-field grid one click away behind a disclosure. Early block = "⛔ Blocked early — {stage}" with no run-link.
result: pass
note: |
  Chrome MCP automated. "Project Meridian Risk Summary (GOOD)" workflow:
  RESTING: 8 compact horizontal fluent-emoji nodes, all green ✓ badges, quiet spine ✅.
  GOLDEN-RUN IN-FLIGHT: "Golden run" node had amber border; earlier stages cleared (green ✓);
  later stages pending — active node unmistakable ✅.
  PUBLISHED: Green "Published Project Meridian Risk Summary (GOOD) v2" success banner ✅.
  In-dialog "🎉 Published" worded headline not screenshot (dialog auto-dismissed before capture);
  worded verdict code-verified (VERIFICATION.md Truth #5) ✅.
  JUDGE BLOCK + EARLY BLOCK: Not triggered — no failing workflow in dev env; code-verified (Truth #6/#7).

### 3. All 4 HTTP outcomes render distinctly
expected: In the Workflow Studio, drive all four publish-response scenarios (200 block / 400 / 404 / 409) and confirm each produces a distinct readable outcome. The worded headline must not collapse them — each reads as its own outcome (not just "error" for all).
result: skipped
reason: "No failing workflow available to trigger judge-block, structure-fail, or already-published responses. All 4 httpStatusForKind() branches code-verified in PublishGauntlet.test.tsx (VERIFICATION.md Truth #6)."

### 4. Cross-provider engine-chip logo
expected: With a workflow run active, switch between providers (OpenAI, Anthropic, Google, and at least one of DeepSeek / Moonshot / Zhipu / MiniMax / OpenRouter). In the live phase spine, when a phase is running and has a provider, the engine chip should show the CORRECT @lobehub/icons mark for that provider — not a generic Bot dot and not empty. Unknown providers (lmstudio, unknown) should correctly fall back to the Bot dot.
result: blocked
blocked_by: prior-phase
reason: |
  No workflow in dev env has explicit provider in subAgents[0].provider. Thread-level provider
  (OpenAI / gpt-5.4-mini) is not threaded into PhaseCard subAgents. Engine chip correctly absent
  for all tested workflows (honestly-absent behavior ✅). Positive case (correct @lobehub/icons
  logo) code-verified (VERIFICATION.md Truth #9: providerLogo() mapped for all 8 native
  providers + OpenRouter). Requires a workflow with per-phase provider assignment to test live.

### 5. UAT scoreboard 4-axis (live-run cards)
expected: Exercise all four axes while watching the live phase spine stay correct: (a) cross-provider — run a workflow on at least 2 different providers; (b) multi-tool — trigger a phase that uses ≥2 tools (e.g. search_documents + execute_code) and confirm the active card shows the bloom+activity line; (c) parallel-thread — start a workflow run in Thread A, then open Thread B and send a new prompt — the energized active-card density must not bleed across threads; (d) long-message — run a workflow with many phases (or scroll to a long run) and confirm idle steps stay quiet at scale.
result: blocked
blocked_by: prior-phase
reason: |
  Single-thread OpenAI run confirmed: running card bloom+activity line ✅, done cards settle ✅.
  Multi-tool partially exercised (research phase = llm_agent calling search_documents; captured
  briefly in running state). Cross-provider, parallel-thread, and long-run (many phases) require
  specific setup not available in this session. PhaseTimeline panel phasesByThread-scoped
  (parallel isolation code-verified, VERIFICATION.md Truth #10).

## Summary

total: 5
passed: 2
issues: 0
pending: 0
skipped: 1
blocked: 2

## Gaps

[none — no regressions found; 2 blocked tests are environment constraints not testable without per-phase provider assignment or parallel-thread setup]
