---
phase: 122-cross-provider-trust-honesty-parity
plan: 03
type: execute
wave: 3
depends_on: ["122-02"]
files_modified:
  - scripts/eval_cross_provider.py
  - backend/tests/unit/test_eval_forced_emit.py
  - .planning/eval/README.md
autonomous: true
requirements: [MP-03]
must_haves:
  truths:
    - "eval_cross_provider.py has a --forced-emit mode (parallel to --workflow) that drives the REAL forced_emit per provider (D-122-06, SC#3)"
    - "The matrix runs each native-7 provider on an EASY and a HARD schema (optional-heavy + additionalProperties confidence object — the live trip-wire) (Pitfall 1, SC#3)"
    - "Each provider × axis cell scores trigger / force / recovery / honest-fail as PASS / FAIL / DOCUMENTED (D-122-07, SC#3)"
    - "A DOCUMENTED cell clears the gate because the declared emit_tier already reflects that reality (D-122-07)"
    - "The driver writes a dated .planning/eval/forced-emit-scoreboard-<date>.{json,md} artifact mirroring the capability-table writer (D-122-06)"
    - "native-7 GATES; OpenRouter is best-effort and never gates; one representative model per provider (D-122-07)"
    - "assert_localhost_only() hard-gates before any DB/HTTP work; presence-only env reporting; no secret material in the artifact (Security: Tampering / Information Disclosure)"
    - "The tier-change gate is the OPERATOR grep ritual documented in .planning/eval/README.md — NOT an automated pytest gate (D-122-06)"
    - "A structure-only pytest (fake gateway, no live keys) proves the writer + axis-scoring + localhost gate (SC#3)"
  artifacts:
    - path: "scripts/eval_cross_provider.py"
      provides: "--forced-emit matrix + dated scoreboard artifact writer"
      contains: "forced-emit"
    - path: "backend/tests/unit/test_eval_forced_emit.py"
      provides: "structure-only test: axis scoring + artifact writer + localhost gate (fake gateway)"
    - path: ".planning/eval/README.md"
      provides: "the forced-emit scoreboard grep ritual (tier-change gate)"
      contains: "forced-emit"
  key_links:
    - from: "--forced-emit matrix"
      to: "forced_emit ladder (Plan 02)"
      via: "direct-call harness drives real forced_emit per provider"
      pattern: "forced_emit"
    - from: "scoreboard writer"
      to: ".planning/eval/forced-emit-scoreboard-<date>.{json,md}"
      via: "_eval_artifact_dir + date stamp + json/md twin"
      pattern: "forced-emit-scoreboard"
---

<objective>
MP-03: make the eval treat provider as a first-class axis. Extend the proven operator-run `scripts/eval_cross_provider.py` with a `--forced-emit` mode (parallel to the existing `--workflow` mode) that drives the REAL `forced_emit` ladder (Plan 02) per provider on an EASY and a HARD schema, scores the 4 axes (trigger / force / recovery / honest-fail), and writes a dated `.planning/eval/forced-emit-scoreboard-<date>.{json,md}` artifact with PASS / FAIL / DOCUMENTED cells.

The tier-change gate is the existing OPERATOR ritual (D-122-06): grep the latest scoreboard before flipping any `emit_tier`, attach to VALIDATION.md. NOT an automated pytest gate (live cross-provider keys in CI = secrets + cost + flakiness — rejected). A separate structure-only pytest (fake gateway, no live keys) proves the writer/scoring/localhost-gate; the LIVE proof is the operator run.

Purpose: tier changes become measured, not assumed — "no silent tier flip" (SC#3). The HARD schema (the optional-heavy + `additionalProperties` confidence object that 400s the OpenAI-schema family) makes the recovery axis non-vacuous.
Output: the `--forced-emit` matrix + dated artifact writer + structure-only test + the README ritual extension.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-CONTEXT.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-RESEARCH.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-PATTERNS.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-VALIDATION.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-02-SUMMARY.md
@.planning/eval/README.md
@.planning/eval/capability-table-2026-06-07.json

<interfaces>
<!-- The exact live eval-driver seams to reuse verbatim (verified 2026-06-23). -->

scripts/eval_cross_provider.py:
  :82-93   PROVIDERS roster (8 tuples: openai/anthropic/google/openrouter/deepseek/moonshot/zhipu/minimax)
  :140-142 NATIVE_7 = ("openai","anthropic","google","deepseek","moonshot","zhipu","minimax")  ← the gate set; openrouter excluded
  :111-114 _COUNT_QUERIES — the fixed-allowlist constant-query + parameterized %s pattern (SQL safety, T-088-02-04)
  :423-440 assert_localhost_only() — the hard-gate to call before any work (refuses non-localhost SUPABASE_URL)
  :1137-1139 _eval_artifact_dir()
  :1142-1231 emit_capability_table — the JSON + md_lines twin writer to MIRROR (date.today().isoformat() stamp, two print(... written) lines)
  :1284-1324 _parse_args — where --forced-emit action="store_true" is added (mirror the --workflow flag at :1313-1323)
  :881-885 a missing provider key marks that row MISSING and never blocks others

config.py:672-682 — _SUB_AGENT_MODEL_DEFAULTS (the effective roster; Pitfall 6 — direct-call avoids body.model-doesn't-steer)

The 4 axes (D-122-07):
  trigger     — did the model attempt the forced/coerced tool call at all (not answer from training)?
  force       — did the model commit on the declared top rung (its emit_tier rung)?
  recovery    — did a 400/no-emit recover via a lower rung (non-strict/coerce) rather than going dark?
  honest_fail — when nothing succeeds, a clean _failure() (never silent/fabricated)?
Cells: PASS / FAIL / DOCUMENTED (DOCUMENTED = an explicit known-limitation row inside the artifact; clears the gate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the --forced-emit matrix (direct-call harness, EASY+HARD schema, 4-axis scoring) + dated scoreboard writer</name>
  <read_first>
    - scripts/eval_cross_provider.py:82-93 (PROVIDERS roster), :140-142 (NATIVE_7 gate), :423-440 (assert_localhost_only), :1137-1231 (_eval_artifact_dir + emit_capability_table twin writer), :1284-1324 (_parse_args — the --workflow flag template)
    - scripts/eval_cross_provider.py:111-114 (_COUNT_QUERIES — the SQL allowlist pattern, if any DB read is needed)
    - backend/app/services/forced_emit.py:205 (the forced_emit signature the harness imports + calls directly) + Plan 02's rung result dict (emit_rung)
    - 122-PATTERNS.md §"scripts/eval_cross_provider.py" (reuse-verbatim list + the new direct-call harness logic)
    - 122-RESEARCH.md §"Pattern 3" (the 4 axes + schema difficulty), §"Pitfall 1" (schema-specific failure — the HARD schema), §"Pitfall 6" (body.model doesn't steer — use direct-call), §"Open Q2 → A3" (direct-call harness recommendation)
    - .planning/eval/capability-table-2026-06-07.json (the artifact shape to mirror)
  </read_first>
  <action>
    Add a `--forced-emit` mode to `eval_cross_provider.py` parallel to `--workflow` (mirror the `_parse_args` flag at :1313-1323 as `action="store_true"`). Reuse verbatim: the `PROVIDERS` roster (:82-93), the `NATIVE_7` gate tuple (:140-142 — native-7 GATES, OpenRouter best-effort never gates), `assert_localhost_only()` (:423-440, called before any work), presence-only env reporting, and the missing-key→MISSING-row behavior (:881-885).

    Implement a localhost-only DIRECT-CALL harness (RESEARCH A3): import `forced_emit` and drive it per `(provider, schema_difficulty)` for the native-7 representative model. Define TWO schemas: an EASY schema (clean required fields) and a HARD schema (optional-heavy + an `additionalProperties` confidence object — the live trip-wire that 400s OpenAI/DeepSeek/Z.ai, Pitfall 1). Direct-call avoids Pitfall 6 (the requested model steers the shot; harness phases would not). Score the 4 axes per cell (trigger/force/recovery/honest_fail) reading the ladder's result dict (`emit_rung`, `emitted`, `failure`): force=PASS if it won on the declared top rung, recovery=PASS if it won on a lower rung after a higher one failed, honest_fail=PASS if exhaustion yields a clean `_failure`. Cells are PASS / FAIL / DOCUMENTED; a DOCUMENTED cell carries a `documented` note + evidence link inside the artifact and clears the gate (D-122-07).

    Mirror `emit_capability_table` (:1142-1231) for the writer: `_eval_artifact_dir()` + `date.today().isoformat()` stamp + a `.json` writer + an `md_lines` twin (a `| provider | difficulty | tier | trigger | force | recovery | honest_fail | rung | gated |` table) + the two `print(... written: ...)` lines. Write `forced-emit-scoreboard-<stamp>.{json,md}`. The artifact contains model NAMES + verdicts ONLY — never key material. If any DB read is needed, use the fixed-allowlist constant-query + parameterized `%s` pattern (never interpolate a table/identifier, T-088-02-04).
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe scripts/eval_cross_provider.py --help 2>&1 | grep -- --forced-emit</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/eval_cross_provider.py --help` lists the `--forced-emit` flag
    - Source: the new mode calls `assert_localhost_only()` before any DB/HTTP work
    - Source: the matrix defines BOTH an EASY and a HARD schema; the HARD schema includes an `additionalProperties` confidence object
    - Source: the matrix iterates the `NATIVE_7` gate set; OpenRouter (if exercised) is reported but excluded from gating
    - Source: the writer produces `forced-emit-scoreboard-<date>.{json,md}` via `_eval_artifact_dir` + a date stamp, mirroring `emit_capability_table`
    - Source: the JSON cell shape carries `axes` (trigger/force/recovery/honest_fail), `winning_rung`, `declared_emit_tier`, `documented[]`, `gated` (mirror 122-PATTERNS.md §scoreboard shape)
  </acceptance_criteria>
  <done>--forced-emit mode drives the real forced_emit per native-7 provider on EASY+HARD schemas, scores 4 axes PASS/FAIL/DOCUMENTED, writes the dated scoreboard twin; localhost-gated; no key material in the artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Structure-only pytest (fake gateway, no live keys) + extend the README with the scoreboard grep ritual</name>
  <read_first>
    - backend/tests/unit/test_103_forced_emit_strict.py (the _patch_gateway fake-gateway fixture — no live keys; the structure-only test reuses this style)
    - scripts/eval_cross_provider.py (the new --forced-emit matrix function from Task 1 — the SUT to import)
    - scripts/eval_cross_provider.py:423-440 (assert_localhost_only — monkeypatch SUPABASE_URL to a cloud URL → expect SystemExit)
    - 122-PATTERNS.md §"backend/tests/unit/test_eval_forced_emit.py" (the structure-only test pattern) + §".planning/eval/...README.md" (the README extension mirroring the existing grep ritual)
    - .planning/eval/README.md:13-27 ("Emit trigger") + :71-85 (the existing grep ritual to mirror)
  </read_first>
  <behavior>
    - test_axis_scoring: given a fake forced_emit result (a known rung outcome), the matrix scores the 4 axes correctly (e.g. won-on-coerce-after-strict-failed → recovery PASS)
    - test_artifact_written: the writer produces a .json + .md twin with PASS/FAIL/DOCUMENTED cells (mirror the capability-table shape)
    - test_localhost_gate: monkeypatch SUPABASE_URL to a non-localhost cloud URL → the matrix raises SystemExit (assert_localhost_only fired)
    - test_documented_clears_gate: a cell marked DOCUMENTED with a note sets gated=true (clears the operator gate, D-122-07)
  </behavior>
  <action>
    Create NEW `backend/tests/unit/test_eval_forced_emit.py`: import the `--forced-emit` matrix/scoring/writer functions from `eval_cross_provider`, inject a FAKE gateway / fake `forced_emit` result (no live keys), and assert STRUCTURE only — the 4 axes are scored, the `.json`/`.md` artifact is written with PASS/FAIL/DOCUMENTED cells (mirror the `emit_capability_table` JSON shape), a DOCUMENTED cell clears the gate, and `assert_localhost_only` fires when `SUPABASE_URL` is cloud (monkeypatch → expect `SystemExit`). The LIVE proof is the operator run (D-122-06) — this pytest proves only that the writer/scoring/gate are correct.

    Extend `.planning/eval/README.md` with a "Forced-emit scoreboard" section mirroring the existing "Emit trigger" + "grep ritual" (:13-27, :71-85): the operator runs `scripts/eval_cross_provider.py --forced-emit`, greps the latest `forced-emit-scoreboard-<date>.md` before flipping any `emit_tier`, and attaches it to VALIDATION.md. State explicitly that DOCUMENTED clears the gate (declared tier == measured reality) and that native-7 gates while OpenRouter is best-effort.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_eval_forced_emit.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/unit/test_eval_forced_emit.py -x` passes (no live keys — fake gateway)
    - Behavior: the localhost-gate test raises `SystemExit` on a cloud `SUPABASE_URL`
    - Behavior: the axis-scoring test asserts recovery=PASS on a won-on-lower-rung result and force=PASS on a won-on-top-rung result
    - Behavior: a DOCUMENTED cell sets `gated=true`
    - Source: `.planning/eval/README.md` contains a "Forced-emit scoreboard" section with the grep-before-tier-flip ritual
  </acceptance_criteria>
  <done>Structure-only pytest green (fake gateway, localhost-gate asserted); README extended with the forced-emit scoreboard grep ritual (the tier-change gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator eval → local Supabase / providers | the localhost-gated script crosses into the local DB + live provider APIs; a misconfigured SUPABASE_URL could write against prod |
| eval output → git-diffable artifact | the scoreboard artifact is committed; it must contain verdicts + model names only, never secret material |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-03-01 | Tampering | eval pointed at cloud → live writes against prod data | mitigate | `assert_localhost_only()` (`:423-440`) hard-gates before any DB/HTTP work; the structure-only test asserts a cloud `SUPABASE_URL` raises `SystemExit`. |
| T-122-03-02 | Information Disclosure | secret value in eval output / scoreboard artifact | mitigate | Presence-only env reporting (existing); the scoreboard contains model NAMES + verdicts ONLY (never key material). Identifier-only logging (T-073-04). Source-asserted: no env-value print in the new mode. |
| T-122-03-03 | Tampering | SQL injection via the new matrix | mitigate | If the matrix reads the DB, it uses the fixed-allowlist constant-query + parameterized `%s` pattern (`_COUNT_QUERIES`) — never interpolate a table/identifier (T-088-02-04). The direct-call harness needs no new dynamic SQL. |
| T-122-03-04 | Authentication | eval bearer mint | accept | The eval mints a bearer for the documented LOCAL test user only via the existing password grant; never prints token material (`:595-633`). No new auth surface. |
| T-122-03-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed — the eval imports already-pinned backend venv deps (psycopg2/requests/python-dotenv). Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `scripts/eval_cross_provider.py --help` lists `--forced-emit`
- `pytest backend/tests/unit/test_eval_forced_emit.py -x -q` green (structure-only, fake gateway, localhost-gate asserted)
- `.planning/eval/README.md` carries the forced-emit scoreboard ritual
- (Operator, at validate-phase) the live `--forced-emit` run writes `forced-emit-scoreboard-<date>.{json,md}` and is attached to VALIDATION.md — the MP-03 gate (manual exception, D-122-06)
</verification>

<success_criteria>
- --forced-emit mode drives the real forced_emit per native-7 provider on EASY+HARD schemas; 4 axes scored PASS/FAIL/DOCUMENTED
- Dated scoreboard artifact written (json+md twin); native-7 gates, OpenRouter best-effort; no key material
- Structure-only pytest green (no live keys); localhost gate asserted
- README documents the operator grep-before-tier-flip ritual (the tier-change gate)
</success_criteria>

<output>
Create `.planning/phases/122-cross-provider-trust-honesty-parity/122-03-SUMMARY.md` when done
</output>
