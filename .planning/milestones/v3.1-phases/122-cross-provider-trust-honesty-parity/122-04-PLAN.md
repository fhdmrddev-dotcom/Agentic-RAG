---
phase: 122-cross-provider-trust-honesty-parity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/agent_loop.py
  - backend/tests/unit/test_system_prompt.py
  - frontend/src/lib/workspacePanel.test.ts
autonomous: true
requirements: [TDP-01]
must_haves:
  truths:
    - "The shared SYSTEM_PROMPT carries ONE ungated nudge instructing a concrete, specific execute_code description (never a generic 'Run code') (D-122-08, SC#4)"
    - "The nudge is provider-agnostic — no Anthropic-specific extraction is added (BUG-260528-03's stated cause is WRONG; tool_args_progress is already cross-provider) (D-122-08)"
    - "The agent_loop.py touch is minimal — one additive bullet, no logic (G-5 hot file)"
    - "A string-presence guard test pins the nudge so it can't be silently deleted (SC#4)"
    - "The existing inferLabel()/humanize() frontend label floor is VERIFIED (not rebuilt) with a bare-name assertion (D-122-08, SC#4)"
    - "humanize() precedence holds: execute_code → description > inferLabel(code) > 'Run code'; a non-mapped tool falls back to its bare name (the floor the SC#10 UAT watches) (Pitfall 4)"
  artifacts:
    - path: "backend/app/services/agent_loop.py"
      provides: "the ungated execute_code.description nudge in SYSTEM_PROMPT"
      contains: "description"
    - path: "backend/tests/unit/test_system_prompt.py"
      provides: "string-presence guard for the nudge"
    - path: "frontend/src/lib/workspacePanel.test.ts"
      provides: "humanize precedence + bare-name floor assertions (verify, not rebuild)"
  key_links:
    - from: "SYSTEM_PROMPT nudge"
      to: "execute_code.description tool field (already strong)"
      via: "the model fills description → tool_args_progress (cross-provider) → humanize()"
      pattern: "description"
---

<objective>
TDP-01: make task/step labels concrete on every provider (OpenAI-parity), not the bare tool name. The real gap is a SINGLE ungated system-prompt nudge to fill `execute_code.description` — the schema field is already strong (`openai_service.py:601-603`), `tool_args_progress` is already cross-provider (all 3 adapters), and the deterministic `inferLabel()` floor already exists. So this is a PROMPT problem, not a schema problem (D-122-08).

Scope is targeted: add the ungated nudge (shared prompt — helps every provider, can only improve well-behaved ones), pin it with a string-presence guard, and VERIFY (not rebuild) the existing frontend label floor with a bare-name vitest assertion. The deterministic floor is EXTENDED to other tools ONLY if the SC#10 UAT surfaces a bare name (do not build a comprehensive per-tool summarizer up front).

Purpose: closes BUG-260528-03 (non-Anthropic generic code task descriptions); proves via SC#10 UAT that NO provider shows a bare tool name. This plan is independent of MP-01/MP-02/MP-03 (no file overlap) — it runs in parallel.
Output: the SYSTEM_PROMPT nudge + a string-presence guard + a verified frontend label-floor test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-CONTEXT.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-RESEARCH.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-PATTERNS.md
@.planning/phases/122-cross-provider-trust-honesty-parity/122-VALIDATION.md

<interfaces>
<!-- The exact live seams (verified 2026-06-23). -->

agent_loop.py:461 — SYSTEM_PROMPT = (the shared agent system prompt — G-5 hot file)
agent_loop.py:497-501 — the existing execute_code guidance bullet (the nudge site):
  "- **execute_code** → create downloadable files ... Write output files to /sandbox/output/ and list them in `output_files`.\n"
agent_loop.py:523-544 — the "## Rules" section (alt placement for the nudge bullet)

openai_service.py:601-603 — the execute_code.description tool-schema field (ALREADY strong; "Short human-readable label … e.g. 'Generating PowerPoint presentation'"). DO NOT change the schema — this is a prompt problem.

frontend/src/lib/workspacePanel.ts:
  :109-129 inferLabel() — deterministic code-label floor (leading-comment → keyword rules → "Run code")
  :156-167 PRETTY_TOOL_NAMES (the curated allow-list)
  :178-186 humanize() precedence — execute_code → description > inferLabel(code) > "Run code";
           non-execute_code → PRETTY_TOOL_NAMES[name] ?? name  (the `?? name` = the bare-name floor, Pitfall 4)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the ungated execute_code.description nudge to SYSTEM_PROMPT + string-presence guard test</name>
  <read_first>
    - backend/app/services/agent_loop.py:461 (SYSTEM_PROMPT start), :490-501 (the tool guidance bullets — the nudge site), :523-544 (the ## Rules section — alt placement)
    - backend/app/services/openai_service.py:601-603 (the execute_code.description schema field — already strong; do NOT change)
    - 122-PATTERNS.md §"backend/app/services/agent_loop.py" (the nudge-site excerpt + the one-additive-bullet pattern + the anti-pattern: NO Anthropic-specific extraction)
    - 122-RESEARCH.md §"Pattern 4" + §"Code Examples → TDP-01 the ungated nudge" (the recommended sentence)
    - 122-PATTERNS.md §"backend/tests/unit/test_system_prompt.py" (the inspect/SYSTEM_PROMPT string-presence guard pattern)
  </read_first>
  <behavior>
    - test_system_prompt_has_execute_code_label_nudge: SYSTEM_PROMPT contains the nudge — a string-presence guard on the exact phrase the plan ships (e.g. "description" + "label" + a no-generic-phrase instruction) so it can't be silently deleted
  </behavior>
  <action>
    Append ONE ungated bullet to `SYSTEM_PROMPT` near the `execute_code` guidance (`agent_loop.py:497-501`) OR as a `## Rules` bullet (`:523-544`) — instructing the model to ALWAYS set `description` to a short, specific label of what the code produces (e.g. "Generating Q3 revenue chart", "Creating the risk-register .docx"), never a generic phrase like "Run code", and noting the user sees this label live in the workspace panel. It is provider-agnostic (shared prompt — lifts the providers that don't label well, cannot regress those that do). One additive string, NO logic (G-5 hot file — touch minimally).

    Do NOT add Anthropic-specific extraction (BUG-260528-03's stated cause is WRONG — `tool_args_progress` is already cross-provider in all 3 adapters). Do NOT change the `execute_code.description` schema field (`openai_service.py:601-603` — already strong).

    Create NEW `backend/tests/unit/test_system_prompt.py` with a string-presence guard asserting the nudge phrase is present in `SYSTEM_PROMPT` (the exact phrase the plan ships) — so the nudge cannot be silently removed.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_system_prompt.py -k execute_code_label -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/unit/test_system_prompt.py -k execute_code_label -x` passes
    - Source: `agent_loop.py` `SYSTEM_PROMPT` contains the nudge bullet (one additive string; diff shows no logic change)
    - Source: no Anthropic-specific extraction added (grep the diff — no provider-name branch, no `tool_args_progress` edit)
    - Source: the `execute_code.description` schema field (`openai_service.py:601-603`) is unchanged
    - Behavior: the guard test fails if the nudge phrase is removed (asserts the exact shipped phrase)
  </acceptance_criteria>
  <done>Ungated execute_code.description nudge added to SYSTEM_PROMPT (one bullet, no logic, provider-agnostic); string-presence guard test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify the frontend label floor — humanize precedence + bare-name assertion (vitest)</name>
  <read_first>
    - frontend/src/lib/workspacePanel.ts:109-129 (inferLabel — the deterministic code-label floor), :156-167 (PRETTY_TOOL_NAMES), :178-186 (humanize precedence)
    - frontend/src/lib/model-info.test.ts:1-15 (the vitest describe/it/expect + lib-import style to mirror)
    - 122-PATTERNS.md §"frontend/src/lib/workspacePanel.test.ts" (the behavior-to-assert excerpt + the export-or-public-derive note)
    - 122-RESEARCH.md §"Pitfall 4" (a non-execute_code tool not in PRETTY_TOOL_NAMES renders its bare name — the floor the UAT watches)
  </read_first>
  <behavior>
    - execute_code with a description → humanize returns the description (e.g. "Generating Q3 chart")
    - execute_code without a description but with code → humanize returns inferLabel(code) (the deterministic floor)
    - execute_code with neither → humanize returns "Run code"
    - a non-mapped non-execute_code tool (e.g. query_documents_by_view) → humanize returns its bare name (the `?? name` floor — the surface the SC#10 UAT watches; Pitfall 4)
    - a mapped tool → humanize returns its PRETTY_TOOL_NAMES label
  </behavior>
  <action>
    Create NEW `frontend/src/lib/workspacePanel.test.ts` (vitest) VERIFYING the existing `humanize()`/`inferLabel()` floor — do NOT rebuild it (D-122-08). Assert the precedence chain (execute_code: description > inferLabel(code) > "Run code") and the bare-name floor for a non-mapped non-execute_code tool. If `humanize` is module-private, export it (minimal) or test via the public derive path per 122-PATTERNS.md.

    The bare-name assertion is the load-bearing one: it documents the floor the SC#10 UAT watches (a non-mapped tool renders its raw snake_case name). Per D-122-08, extending `PRETTY_TOOL_NAMES` to additional tools happens ONLY if the SC#10 UAT actually surfaces a bare name — this plan does NOT pre-emptively build a comprehensive summarizer.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/workspacePanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/workspacePanel.test.ts` passes
    - Behavior: execute_code description-present → returns the description; description-absent+code → returns inferLabel(code); neither → "Run code"
    - Behavior: a non-mapped non-execute_code tool returns its bare name (the floor asserted, not "fixed" — Pitfall 4)
    - Source: `workspacePanel.ts` `humanize`/`inferLabel`/`PRETTY_TOOL_NAMES` are NOT rebuilt (the test verifies existing behavior; at most `humanize` is exported)
  </acceptance_criteria>
  <done>Frontend label-floor test green — humanize precedence + the bare-name floor verified (not rebuilt); the SC#10-UAT-watched surface documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → workspace panel label | the model-authored `description` flows through `tool_args_progress` to the user-visible label; a missing/garbage label degrades UX honesty, not security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-04-01 | Tampering | the SYSTEM_PROMPT nudge silently deleted (regressing labels) | mitigate | The string-presence guard test pins the exact nudge phrase — a silent deletion fails CI. |
| T-122-04-02 | Information Disclosure | a model-authored description leaking sensitive content into the panel | accept | The description is a short label the model chooses; it surfaces only what the model would already narrate. No new data path; the panel already renders model output. Low-value, no PII boundary crossed. |
| T-122-04-03 | Tampering | the nudge as a prompt-injection vector | accept | The nudge is a static, provider-agnostic instruction in the SHARED system prompt (trusted, author-controlled) — not user input. No new injection surface. |
| T-122-04-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed — one prompt string + two test files. Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `pytest backend/tests/unit/test_system_prompt.py -x -q` green (nudge string-presence guard)
- `cd frontend && npx vitest run src/lib/workspacePanel.test.ts` green (humanize precedence + bare-name floor)
- Full frontend label suite no net-new failures
- Diff confirms `agent_loop.py` change is one additive bullet (no logic), no Anthropic-specific extraction, schema field unchanged
</verification>

<success_criteria>
- One ungated execute_code.description nudge in SYSTEM_PROMPT (provider-agnostic, one bullet, G-5 minimal touch)
- String-presence guard pins the nudge; frontend label-floor verified (not rebuilt) with the bare-name assertion
- No Anthropic-specific extraction; schema field unchanged; floor extension deferred unless SC#10 UAT surfaces a bare name
</success_criteria>

<output>
Create `.planning/phases/122-cross-provider-trust-honesty-parity/122-04-SUMMARY.md` when done
</output>
