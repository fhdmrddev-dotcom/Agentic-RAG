---
phase: 23-system-prompt-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/threads.py
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02

must_haves:
  truths:
    - "SYSTEM_PROMPT contains instruction to hedge when search similarity is below 0.4"
    - "SYSTEM_PROMPT contains structured citation guidance with document name and section format"
    - "Existing tool descriptions and mode-specific instructions are not broken"
  key_links:
    - from: "backend/app/api/threads.py"
      to: "backend/app/api/threads.py"
      via: "SYSTEM_PROMPT contains similarity hedging"
      pattern: "0\\.4|similarity.*low|low.*confidence"
---

<objective>
Add similarity confidence hedging and structured citation guidance to SYSTEM_PROMPT.

Note: The system prompt was recently refactored (Phase 18 follow-up) to reduce unnecessary
tool loops. This plan adds two missing requirements on top of that refactor:
1. PROMPT-01: hedge when search_documents returns results with similarity < 0.4
2. PROMPT-02: structured citation format (document name + section)

Read the CURRENT SYSTEM_PROMPT before making any changes — do not assume its content.
</objective>

<context>
@backend/app/api/threads.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add similarity hedging and citation guidance to SYSTEM_PROMPT</name>
  <files>backend/app/api/threads.py</files>
  <read_first>
    - backend/app/api/threads.py lines 32-100 (full SYSTEM_PROMPT)
  </read_first>
  <action>
Read the current SYSTEM_PROMPT in full. Then append two new sections to it BEFORE the final
"Output file rule" paragraph:

**Section to add (similarity hedging — PROMPT-01):**
```
## Confidence & hedging
search_documents results include a `similarity` score (0–1). If ALL returned chunks have
similarity below 0.4, the answer is likely not in the documents — say so explicitly:
"I couldn't find reliable information about this in your documents. The closest match was
[document name] but the similarity was low." Do not fabricate an answer from weak matches.

## Citation format (PROMPT-02)
When citing document content, use this format:
**[Document Name]** — [section or chapter if identifiable, otherwise omit]
Example: **Fahed Mrad Chapters 1-4.docx** — Chapter 3.4
Never cite a document you did not retrieve in this response.
```

Insert this text into the SYSTEM_PROMPT string, immediately before the "Output file rule:" paragraph.
Preserve all existing content exactly — only add, do not remove or reorder.
  </action>
  <verify>
    <automated>grep -c "0\.4\|similarity" "C:/Vibe Apps/Agentic RAG/backend/app/api/threads.py"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "0.4" backend/app/api/threads.py` returns a match in SYSTEM_PROMPT
    - `grep -i "citation\|cite" backend/app/api/threads.py` returns a match in SYSTEM_PROMPT
    - The SYSTEM_PROMPT string is syntactically valid Python (no unclosed parens/quotes)
  </acceptance_criteria>
  <done>SYSTEM_PROMPT includes similarity hedging (< 0.4) and structured citation guidance.</done>
</task>

</tasks>

<verification>
1. `grep "0.4" backend/app/api/threads.py` — match exists
2. `python -c "from app.api.threads import SYSTEM_PROMPT; print('OK')"` — no syntax errors
   (run from backend/ with venv active)
</verification>
