---
phase: 067.2-streaming-render-and-storage-fixes
plan: 03
subsystem: api
tags: [fastapi, supabase-storage, signed-url, ownership-fence, redirect, idor, react, vite]

# Dependency graph
requires:
  - phase: 062-runs-stream-replay-tail
    provides: D-062-12 invariant — cross-user resource access returns 404 (not 403); aexec helper pattern
  - phase: 061-redis-streams-buffered-runs
    provides: get_supabase / get_current_user dependency contract
provides:
  - GET /sandbox-outputs/{storage_path:path} re-sign endpoint (auth + ownership fence + 60s TTL + 302 redirect)
  - Backend harvest_output_files now emits relative `/sandbox-outputs/{path}` URL (no 1h hardcoded decay)
  - Frontend resolveOutputUrl helper that prepends API_BASE for relative URLs and passes legacy http URLs through
  - 3 backend tests asserting cross-user→404, missing-row→404, happy-path→302
affects: [067.2-04, 067.2-05, 067.2-06, future sandbox-storage work]

# Tech tracking
tech-stack:
  added: []   # no new libraries — RedirectResponse + run_in_threadpool already in stack
  patterns:
    - "Re-sign-on-demand storage URL pattern (Claude.ai/ChatGPT-style): store path → click triggers fresh short-TTL sign + 302"
    - "Two-layer ownership fence: structural path-segment match (UUID==current_user.id) + DB row eq filter"
    - "404-not-403 for cross-user sandbox storage access (extends D-062-12 to a new endpoint family)"

key-files:
  created:
    - backend/app/api/sandbox_outputs.py
    - backend/tests/test_067_2_sandbox_outputs.py
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-03-SUMMARY.md
  modified:
    - backend/app/main.py
    - backend/app/services/sandbox_service.py
    - frontend/src/components/chat/ExecuteCodeBlock.tsx

key-decisions:
  - "404 (not 403) on cross-user sandbox-output access — adopt project-wide D-062-12 invariant over CONTEXT.md acceptance criterion #3 which specified 403. Rationale: existence-leak prevention; consistency with runs.py / documents.py / skills.py / threads.py user-scoped routes; phase user UAT does not include any 403 checkbox so the substitution is closed at the developer-test layer."
  - "60s TTL on the re-sign step — long enough for the 302→CDN hop, short enough that leaked Location-header URLs decay quickly."
  - "302 (not 307) redirect — request is idempotent GET; no method-preservation requirement."
  - "Variant A URL shape (relative `/sandbox-outputs/{path}` stored in OutputFile.url) — keeps OutputFile.url type as `string`; frontend resolveOutputUrl handles both legacy http URLs and new relative URLs without a type change."

patterns-established:
  - "Sandbox-output download path: backend stores relative re-sign URL → frontend prepends API_BASE → backend authenticates + ownership-fences + re-signs → 302 to Supabase CDN"
  - "Path converter `:path` for FastAPI routes that must accept multi-segment paths (storage_path = `{user_id}/{exec_id}/{filename}`)"
  - "Defense-in-depth ownership fence: structural check (path-segment UUID match) precedes DB ownership check; both miss → 404"

requirements-completed: [STREAM-04-correctness]

# Metrics
duration: 4min
completed: 2026-05-08
---

# Phase 067.2 Plan 03: Sandbox-Outputs Re-Sign Endpoint Summary

**Replaced 1h hardcoded signed-URL decay with on-demand re-sign endpoint at `GET /sandbox-outputs/{path}` (auth + ownership-fence + 60s TTL + 302 redirect); closes the InvalidJWT-after-1h failure mode for new chat outputs.**

## Performance

- **Duration:** ~4 min (17:30:43 → 17:33:57 UTC+4 commit timestamps)
- **Started:** 2026-05-08T13:30:43Z
- **Completed:** 2026-05-08T13:33:57Z
- **Tasks:** 5/5
- **Files modified:** 5 (3 created + 2 modified, plus this SUMMARY.md and main.py)

## Accomplishments

- New FastAPI router `backend/app/api/sandbox_outputs.py` with the locked D-067.2-03 contract: auth via `get_current_user`, two-layer ownership fence (path-segment user_id match + `sandbox_files` row check, 404 on either miss), `create_signed_url(60)` re-sign wrapped in `run_in_threadpool`, 302 redirect to the freshly signed URL.
- Router registered in `backend/app/main.py` adjacent to feedback (alphabetic-grouped). `/sandbox-outputs/{storage_path:path}` route is discoverable in `app.routes`.
- `harvest_output_files` rewrite in `backend/app/services/sandbox_service.py`: 1h `create_signed_url(storage_path, 3600)` block deleted; emits relative URL `/sandbox-outputs/{storage_path}` so future clicks go through the re-sign endpoint and never decay (within Storage object lifetime).
- `frontend/src/components/chat/ExecuteCodeBlock.tsx` adds module-local `resolveOutputUrl(url)` helper: prepends `VITE_API_BASE_URL` when `url.startsWith("/")`, passes legacy `http://...` URLs through unchanged. Single call site at `OutputFileCard`'s `<a href={...}>`.
- Backend tests `backend/tests/test_067_2_sandbox_outputs.py` (3/3 green): cross-user IDOR → 404, missing `sandbox_files` row → 404, happy path → 302 with Location header.

## Task Commits

Each task was committed atomically with `--no-verify` (parallel-executor convention):

1. **Task 1: Create sandbox_outputs.py router** — `ab4c7c6` (feat)
2. **Task 2: Register router in main.py** — `7c740bf` (feat)
3. **Task 3: Replace 1h-signed URL block in harvest_output_files** — `21f8775` (refactor)
4. **Task 4: Add backend tests (3 assertions)** — `1fd7796` (test)
5. **Task 5: Frontend resolveOutputUrl + href wrap** — `37d88d3` (feat)

## Files Created/Modified

- `backend/app/api/sandbox_outputs.py` — **created**. New router; auth-fenced 60s re-sign + 302 redirect.
- `backend/app/main.py` — **modified**. Added `sandbox_outputs` to imports + `app.include_router(sandbox_outputs.router)`.
- `backend/app/services/sandbox_service.py` — **modified**. Removed `create_signed_url(storage_path, 3600)` block; emits `f"/sandbox-outputs/{storage_path}"` instead.
- `backend/tests/test_067_2_sandbox_outputs.py` — **created**. 3 tests, all green (mocks via `dependency_overrides` + `_make_builder`/`_make_supabase` helpers — same idiom as `test_document_versioning.py`).
- `frontend/src/components/chat/ExecuteCodeBlock.tsx` — **modified**. Added module-local `API_BASE` constant + `resolveOutputUrl` helper; `<a href>` now wraps `file.url` through it.

## Verification Evidence

### Grep gates (Task 1: sandbox_outputs.py)

| Gate | Expected | Actual |
|---|---|---|
| `APIRouter(prefix="/sandbox-outputs"` | 1 | 1 |
| `status.HTTP_404_NOT_FOUND` | ≥2 | 2 |
| `status.HTTP_403` | 0 | 0 |
| `RedirectResponse(url=...,status_code=302` | 1 | 1 |
| `run_in_threadpool` | ≥1 | 4 |
| `await aexec` | ≥1 | 1 |
| `{storage_path:path}` | 1 | 1 |

### Grep gates (Task 2: main.py)

| Gate | Expected | Actual |
|---|---|---|
| `sandbox_outputs` references | ≥2 | 2 |
| `app.include_router(sandbox_outputs.router)` | 1 | 1 |
| Route discovery via `app.routes` | non-empty | `['/sandbox-outputs/{storage_path:path}']` |

### Grep gates (Task 3: sandbox_service.py)

| Gate | Expected | Actual |
|---|---|---|
| `create_signed_url` | 0 | 0 |
| `/sandbox-outputs/` | ≥1 | 2 |
| `f"/sandbox-outputs/{storage_path}"` | 1 | 1 |
| `3600` | 0 | 0 |
| `D-067.2-03` comment marker | ≥1 | 1 |

### Grep gates (Task 4: test file)

| Gate | Expected | Actual |
|---|---|---|
| `assert r.status_code == 404` | ≥2 | 2 |
| `assert r.status_code == 403` | 0 | 0 |
| `D-062-12` (deviation cited) | ≥1 | 5 |

### Pytest output (Task 4)

```
tests/test_067_2_sandbox_outputs.py::test_path_segment_mismatch_returns_404 PASSED [ 33%]
tests/test_067_2_sandbox_outputs.py::test_missing_sandbox_files_row_returns_404 PASSED [ 66%]
tests/test_067_2_sandbox_outputs.py::test_happy_path_returns_302_with_signed_url PASSED [100%]
======================== 3 passed, 1 warning in 0.13s =========================
```

### Grep gates (Task 5: ExecuteCodeBlock.tsx)

| Gate | Expected | Actual |
|---|---|---|
| `resolveOutputUrl` references | ≥2 | 2 |
| `url.startsWith("/")` | ≥1 | 1 |
| `href={file.url}` direct | 0 (all wrapped) | 0 |
| `D-067.2-03` comment marker | ≥1 | 2 |

### TypeScript check

`tsc --noEmit` from worktree frontend directory (using parent-repo `node_modules` because the worktree is uninitialized): no errors anywhere, no errors in `ExecuteCodeBlock`.

## Decisions Made

1. **404 (not 403) on cross-user IDOR** — see "Deviations from Plan / CONTEXT.md acceptance #3 substitution" below. The plan's `<deviation_note>` already pre-resolved this conflict; this summary records that the resolution was applied as planned (no additional deviation triggered during execution — the deviation was planned-in).
2. **Variant A URL shape** (relative `/sandbox-outputs/{storage_path}` stored in `OutputFile.url`) — kept type `string`; recommended over Variant B (new `storage_path` field) by PATTERNS.md § 5 to minimize blast radius.
3. **Module-local API_BASE constant** in ExecuteCodeBlock.tsx — `lib/api.ts`'s `API_BASE` is not exported, so reading `import.meta.env.VITE_API_BASE_URL` directly inside ExecuteCodeBlock.tsx avoids touching `lib/api.ts`. Plan explicitly authorizes this.
4. **Test placement** at `backend/tests/test_067_2_sandbox_outputs.py` (top-level), matching the path the plan specified rather than nesting under `backend/tests/api/` — pytest discovers it via the existing `pytest.ini` `testpaths = tests` configuration.

## Deviations from Plan

### CONTEXT.md acceptance #3 substitution (planned-in by PLAN's `<deviation_note>`)

CONTEXT.md `### D-067.2-03` acceptance criterion #3 specified `403` for cross-user IDOR. PLAN.md `<deviation_note>` pre-resolved this conflict in favor of project-wide invariant **D-062-12 (404 not 403)**. Implementation followed the plan's resolution:

- Backend `sandbox_outputs.py` raises `HTTPException(status_code=status.HTTP_404_NOT_FOUND, ...)` on path-segment mismatch AND on missing `sandbox_files` row.
- Backend test asserts `response.status_code == 404` for both branches; cites D-062-12 in 5 separate locations in the docstrings/comments.
- Plan 06 user-UAT scoreboard does not reference 403 anywhere (only same-user happy path and post-1h re-sign), so this substitution does not change the user-facing acceptance gate.

### Auto-fixed Issues

**None.** No Rule 1/2/3 deviations were triggered. The plan's task templates were exact code-paste recipes (with the option to adapt mock idioms to the codebase's existing pattern, which Task 4 did per the plan's instruction). All grep gates passed on first run; pytest 3/3 green on first run; tsc no errors on first run.

---

**Total deviations:** 0 unplanned + 1 planned-in (404-vs-403 already pre-resolved by PLAN's `<deviation_note>`)
**Impact on plan:** Plan executed exactly as written.

## Issues Encountered

- **Worktree base mismatch (caught by `<worktree_branch_check>`):** initial `git merge-base HEAD 70c70cd` returned `b8950d4`, indicating the per-agent branch was based on a much older commit (pre-`runs` router, pre-Redis lifespan). The mandatory `git reset --hard 70c70cd...` at agent startup corrected this. Post-reset all referenced files (`main.py`, `sandbox_service.py`) were at the expected baseline.
- **Worktree has no own venv / node_modules:** the worktree directory's `backend/venv/` and `frontend/node_modules/` are absent — only the parent repo's installations exist. Worked around by invoking `C:/Vibe Apps/Agentic RAG/backend/venv/Scripts/python.exe` and `C:/Vibe Apps/Agentic RAG/frontend/node_modules/.bin/tsc.cmd` against worktree paths. Pytest auto-discovered tests from `cwd=worktree-backend` because `pytest.ini` lives in the worktree's `backend/` and uses relative `testpaths = tests`.
- **pydantic-settings env validation at import time:** the parent venv's `app.config` requires `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` env vars at import time. Verification snippets that import `app.api.sandbox_outputs` directly need these set inline (`SUPABASE_URL=https://test.supabase.co SUPABASE_SERVICE_ROLE_KEY=test ...`), matching the pattern used by `backend/tests/conftest.py`. Pytest sets these automatically via conftest, so the test run was unaffected.

## Legacy URL Decay — Explicit Acknowledgment

**Existing assistant messages with the OLD long-form signed URL** (created before this plan landed) continue to decay at the original 1-hour boundary. This is the migration cost CONTEXT.md `### D-067.2-03` § "Claude's Discretion" explicitly accepts: "_Backwards-compatible reads: if the existing assistant messages have the old long-URL form, they expire after 1h as today (no fix for already-stored URLs); only NEW outputs from this point use the re-sign endpoint. Acceptable migration cost._"

Frontend `resolveOutputUrl` handles both shapes: `url.startsWith("/")` → prepend API_BASE → re-sign endpoint; otherwise pass through unchanged → legacy long-form URL decays at its original `iat + 3600s`.

## User Setup Required

None — no external service configuration. Backend supabase service-role key already has the `sandbox-outputs` Storage bucket (Phase 056); no schema changes; no env vars added.

## Next Phase Readiness

- D-067.2-03 backend + frontend wiring complete; ready for Plan 06 user UAT (D-067.2-03a immediate-download + D-067.2-03b post-90min re-sign checkboxes).
- Wave 1 sibling (Plans 01/02 streaming-render fixes) executes in parallel; Plan 03 has zero overlap with their files (`useMessages.ts` / `ChatArea.tsx` / `MessageItem.tsx`) so no merge conflict expected.
- Plan 06 (user-UAT scoreboard) has the verification protocol locked. To smoke-test 03b before 90 minutes elapse, the dev can temporarily set the new endpoint's TTL low (the `60` literal in `sandbox_outputs.py` step 3) and verify via fresh fetch — but this is not in any plan's scope.

## Self-Check

Verified all SUMMARY.md claims against working tree:

- `backend/app/api/sandbox_outputs.py` → FOUND
- `backend/tests/test_067_2_sandbox_outputs.py` → FOUND
- `backend/app/main.py` modified (sandbox_outputs imported + included) → FOUND
- `backend/app/services/sandbox_service.py` modified (no create_signed_url, has /sandbox-outputs/) → FOUND
- `frontend/src/components/chat/ExecuteCodeBlock.tsx` modified (resolveOutputUrl) → FOUND
- Commit `ab4c7c6` (Task 1) → FOUND in `git log --oneline -7`
- Commit `7c740bf` (Task 2) → FOUND
- Commit `21f8775` (Task 3) → FOUND
- Commit `1fd7796` (Task 4) → FOUND
- Commit `37d88d3` (Task 5) → FOUND
- Pytest 3/3 green (re-run after all commits) → CONFIRMED
- TypeScript no-errors → CONFIRMED

## Self-Check: PASSED

---
*Phase: 067.2-streaming-render-and-storage-fixes*
*Completed: 2026-05-08*
