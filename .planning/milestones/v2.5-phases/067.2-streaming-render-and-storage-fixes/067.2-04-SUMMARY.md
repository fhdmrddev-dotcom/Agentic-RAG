---
phase: 067.2-streaming-render-and-storage-fixes
plan: 04
subsystem: ui
tags: [react, sse, streaming, render-audit, investigation, no-op-closure]

# Dependency graph
requires:
  - phase: 067.2-streaming-render-and-storage-fixes
    plan: 01
    provides: "useLayoutEffect-promoted setViewingThread call site + synchronous handleSend ref-alignment that closes the guardedSetMessages no-op race; this plan's predicted-no-op verdict depends on Plan 01 having landed."
provides:
  - "Evidence-backed verdict that D-067.2-04 (confidence + suggestions not visible) was a side effect of D-067.2-01 (guardedSetMessages no-op race) — confirmed via static layer-by-layer audit of all five render-pipeline layers."
  - "INVESTIGATION-NOTES.md artifact with line-anchored evidence trail covering backend emits (threads.py:2437/:2477), SSE-parser dispatch (api.ts:365-377), useMessages callback wiring (useMessages.ts:302-317 / :634 / :879), type fields (types/index.ts:60-99), and render blocks (MessageItem.tsx:89, :93-98)."
  - "Pre-runtime verdict (a) no-op locked; runtime confirmation deferred to Plan 06 user-UAT D-067.2-04 checkbox (already in CONTEXT.md <verification_protocol>)."
affects: [06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Investigation-first plan execution: no source code change unless live runtime evidence confirms a render-side gap. Predicted verdict (a) closes the plan with INVESTIGATION-NOTES.md as the gating artifact (per plan body's must_haves and success_criteria)."
    - "Layer-by-layer audit format: a line-anchored table (Layer | File | Line | Present? | Notes) makes the evidence trail diff-friendly and lets a follow-on plan jump directly to the gap layer if Plan 06 UAT exposes one."

key-files:
  created:
    - ".planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md — static-audit evidence + locked verdict + Plan 06 UAT confirmation contract"
  modified: []

key-decisions:
  - "Locked predicted verdict (a) no-op based on five-layer static audit. Plan body's success criterion 'If verdict was (a): no source file is modified by this plan' is honored; MessageItem.tsx, useMessages.ts, and threads.py are all untouched by this plan."
  - "Deferred Task 2's runtime DevTools capture to Plan 06 user UAT (not blocked on this worktree's lack of Chrome MCP). The D-067.2-04 acceptance criterion ('confidence indicator visible + follow-up suggestions visible') is already enumerated at CONTEXT.md <verification_protocol> line 305 — no contract change needed."
  - "Skipped Task 3 (conditional on verdict (b)) per plan body's contract: 'Skip this task entirely if Task 2 verdict is (a) no-op or (c) state gap. Mark Task 3 as n/a in the SUMMARY in those cases.'"
  - "Did NOT pre-write speculative render code — explicitly forbidden by success_criteria 'The plan does NOT pre-write speculative render code that turned out to be unnecessary.'"

patterns-established:
  - "Pattern 1: Investigation-first plans whose hypothesis is 'this is a side effect of an upstream fix' should ship a static-audit artifact even when runtime tools are unavailable, then defer runtime confirmation to an existing UAT scoreboard rather than fabricating runtime evidence or pre-writing speculative patches."
  - "Pattern 2: When a plan's checkpoint expects browser-driver tooling and the executor lacks it, the plan body's 'otherwise this is a user-checkpoint' clause routes the verification cleanly to user UAT — no need to introduce a new escalation channel."

requirements-completed: [STREAM-04-correctness]

# Metrics
duration: 4min
completed: 2026-05-08
---

# Phase 067.2 Plan 04: Confidence + Suggestions Render Investigation Summary

**Five-layer static audit confirms the full render path for `message.confidence` + `message.suggestions` is intact end-to-end in the post-Plan-01 build; predicted verdict (a) no-op closure shipped with no source code changes, runtime confirmation forwarded to Plan 06 user UAT.**

## Performance

- **Duration:** ~4 min (static audit + verdict-lock commit + SUMMARY)
- **Started:** 2026-05-08T17:20:51Z
- **Completed:** 2026-05-08T17:24:49Z
- **Tasks:** 1 / 3 executed (Task 2 deferred to Plan 06 UAT — see Decisions; Task 3 n/a per verdict (a))
- **Files modified:** 0 source files; 1 planning artifact created

## Accomplishments

- Confirmed every layer of the live render pipeline for `message.confidence` and `message.suggestions` is intact in the post-Plan-01 build:
  - **Layer 1 (backend emit):** `backend/app/api/threads.py:2437` (confidence) and `:2477` (suggestions) — both `await _emit(redis, run_id, ...)` calls present in the success-path producer block.
  - **Layer 2 (SSE-parser dispatch):** `frontend/src/lib/api.ts:365-370` (confidence → onConfidence) and `:376-377` (suggestions → onSuggestions) — both event names route to the correct callbacks.
  - **Layer 3 (useMessages callbacks):** `frontend/src/hooks/useMessages.ts:302-311` (onConfidence) and `:312-317` (onSuggestions) inside `makeStreamCallbacks`. Both call sites (sendMessage at `:634` and reconcile at `:879`) inject `setMessages: guardedSetMessages` — confirming CONTEXT.md's hypothesis that these events DID flow through the no-op race window that Plan 01 closed.
  - **Layer 4 (type fields):** `frontend/src/types/index.ts:98-99` declares `confidence?: ConfidenceResult` and `suggestions?: string[]`; the `ConfidenceResult` shape at `:60-64` matches the SSE payload contract.
  - **Layer 5 (render blocks):** `frontend/src/components/chat/MessageItem.tsx:89` (`<ConfidenceBadge />`) and `:93-98` (`<SuggestionPills />`) — both present, both with the imports at `:6` / `:8` correctly resolving.
- Verified Plan 01's fix is reachable from the worktree base commit `8032820`: `useLayoutEffect` import at `ChatArea.tsx:1`, the synchronous `setViewingThread` call inside the `useLayoutEffect` at `:85-87`, and the synchronous `setViewingThread(activeThread.id)` call inside `handleSend`'s new-thread branch at `:199`.
- Identified one CONTEXT.md drift: line numbers for the backend emits had shifted (CONTEXT.md said `:2387` and `:2428`; live tree has `:2437` and `:2477`) due to the Plan 02 title-hoist commit (`902ddae`) inserting code earlier in the file. Recorded in INVESTIGATION-NOTES.md as a layer-table drift, not a regression.
- Identified the suggestions-emit gating subtlety per PATTERNS.md § 3 step 4: the suggestions emit (`threads.py:2477`) lives in the post-`done`-event success block, so failed/cancelled/timed_out runs will not get a suggestions event by design. Distinguished this from a render-path gap so Plan 06 UAT can interpret the result correctly.
- Locked predicted verdict (a) no-op; deferred runtime DevTools capture to the existing Plan 06 user-UAT D-067.2-04 checkbox (CONTEXT.md `<verification_protocol>` line 305).

## Task Commits

Each task was committed atomically:

1. **Task 1: Static layer audit + predicted no-op verdict** — `c2ad5c3` (docs).
2. **Task 1 follow-up: Verdict-lock + Plan 06 deferral** — `08368b1` (docs).
3. **Task 2: human-verify checkpoint** — Deferred to Plan 06 user UAT (no commit; this worktree lacks browser-driver tools).
4. **Task 3: Conditional render patch** — n/a per plan body's contract (verdict (a) → skip Task 3).

## Files Created/Modified

- **Created:** `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md` — full layer-by-layer audit, side observations on suggestions emit gating, locked verdict, Plan 06 UAT confirmation contract, references.
- **Modified:** none. Per success criterion "If verdict was (a): no source file is modified by this plan."

## Decisions Made

- **Locked predicted (a) no-op without runtime DevTools capture.** The plan body's Task 2 explicitly says "if executor has Chrome MCP, drive the browser; otherwise this is a user-checkpoint." This worktree's tool surface does not include `mcp__chrome-devtools__*` or equivalent browser-driver tools, AND the parallel_execution directive requires SUMMARY.md to be committed before return. The path forward consistent with both constraints is to commit the static audit + locked verdict and forward runtime confirmation to the existing Plan 06 user-UAT scoreboard's D-067.2-04 checkbox.
- **Did NOT touch source files.** Plan 04's success_criteria explicitly forbids speculative render code: "The plan does NOT pre-write speculative render code that turned out to be unnecessary." Layer 5 (the render blocks at MessageItem.tsx:89 + :93-98) is already correct; rewriting it would be a no-op edit at best and a regression risk at worst.
- **Skipped Task 3 entirely.** Plan body's Task 3 action says: "Skip this task entirely if Task 2 verdict is (a) no-op or (c) state gap. Mark Task 3 as n/a in the SUMMARY in those cases." Predicted verdict is (a), so Task 3 is n/a.
- **Recorded CONTEXT.md line-number drift instead of editing CONTEXT.md.** The CONTEXT.md / PATTERNS.md / PLAN.md line numbers for the backend emits (`threads.py:2387`, `:2428`) are 50 lines off the live tree (`:2437`, `:2477`) because Plan 02's title-hoist commit landed before this plan and shifted the file. INVESTIGATION-NOTES.md records the live line numbers; CONTEXT.md was not edited because it is locked and the drift is non-substantive (the emit shape and gating logic are unchanged).

## Deviations from Plan

**No code deviations.** Static evidence-gathering proceeded exactly as Task 1's `<action>` block described.

**One execution-flow adaptation noted:**

### [Rule 3 / boundary] Task 2 routed to Plan 06 user UAT instead of stopping at checkpoint

- **Found during:** start-of-Task-2 evaluation.
- **Issue:** Task 2 is a `checkpoint:human-verify` gate that requires browser DevTools / Chrome MCP. This worktree executor's tool surface does NOT expose `mcp__chrome-devtools__*` (verified via the available function-call list). The parallel_execution prompt states "REQUIRED: SUMMARY.md MUST be committed before you return," which conflicts with stopping at the checkpoint and returning a structured CHECKPOINT message.
- **Fix:** routed Task 2's runtime verification to the existing Plan 06 user-UAT scoreboard's D-067.2-04 checkbox (already enumerated at CONTEXT.md `<verification_protocol>` line 305). The plan body's Task 2 explicitly says "if executor has Chrome MCP, drive the browser; otherwise this is a user-checkpoint" — Plan 06 user UAT IS that user-checkpoint surface; no contract change needed. INVESTIGATION-NOTES.md documents the deferral with a "Plan 06 UAT confirmation contract" section so the user UAT pass has clear acceptance criteria.
- **Files modified:** none in source; INVESTIGATION-NOTES.md captures the deferral.
- **Commit:** `08368b1` (docs).

## Issues Encountered

- **Worktree base drift detected at startup.** Initial check showed `git merge-base HEAD <expected-base>` returning the worktree's HEAD (`b8950d4` from 2026-04-30), NOT the expected base `8032820b7fa6efa1054a101eae4408b1f3a6430e`. The pre-execution `<worktree_branch_check>` instruction's hard-reset path was followed verbatim: `git reset --hard 8032820...`. After reset, all live source files matched the post-Plan-01/-02 expectations. No data loss; the worktree branch had been spawned from a stale base and the reset corrected it cleanly.
- **Edit-target path drift on initial Write.** The first Write of INVESTIGATION-NOTES.md landed in the main-repo `.planning/` path rather than the worktree `.planning/` path (same one-time issue Plan 01's executor encountered per its own SUMMARY.md "Issues Encountered"). Detected immediately when `git status` in the worktree returned a clean working tree after the Write. Resolved by `cp` + `rm` to migrate the file into the worktree path; final commit (`c2ad5c3`) reflects the worktree path correctly. No code defect.

## Threat Flags

None — Plan 04 modified zero source files. The lone artifact (INVESTIGATION-NOTES.md) is a developer-only planning document under `.planning/`; it contains line-anchored grep evidence and does not introduce new endpoints, auth surface, persisted state, or schema changes. Per the plan's `<threat_model>`, T-067.2-04-01 disposition is `accept` (no patch applied → no LLM-string render path introduced); T-067.2-04-02 disposition is `accept` (developer-only artifact, no new disclosure surface).

## TDD Gate Compliance

Plan type is `execute` (not `tdd`). No RED/GREEN/REFACTOR gate sequence required. The plan body's `<verify>` blocks were satisfied:

- INVESTIGATION-NOTES.md exists and contains the "Layer-by-layer Audit" section + "Predicted verdict" line ✅
- `_emit(redis, run_id, '(confidence|suggestions)'` matches in threads.py: 2 (line 2437 + line 2477) — required ≥ 2 ✅
- `ConfidenceBadge` matches in MessageItem.tsx: 2 (import + render block) — required ≥ 1 ✅
- `SuggestionPills` matches in MessageItem.tsx: 2 (import + render block) — required ≥ 1 ✅

## User Setup Required

None. No environment variables, no service configuration, no migrations. Plan 06 user UAT is the only follow-on action and it is already enumerated in CONTEXT.md `<verification_protocol>`.

## Next Phase Readiness

- **Plan 06 (user UAT)** — D-067.2-04 acceptance criterion at line 305 of CONTEXT.md is unchanged ("Confidence indicator visible + follow-up suggestions visible on a Fahed-Mrad search-only prompt"). Plan 06 will lock the runtime verdict retrospectively. If both green → Plan 04 closes confirmed verdict (a). If either red → INVESTIGATION-NOTES.md provides the line-anchored audit pointing the follow-on work directly at the gap layer.
- **No regressions expected** to Phase 067.1 SC#1-#6 or Phase 066 SC#6 — Plan 04 modified zero source files.
- **No new technical debt.** The CONTEXT.md / PATTERNS.md line-number drift (50 lines off due to Plan 02's title-hoist commit) is recorded in INVESTIGATION-NOTES.md but does not warrant a CONTEXT.md edit (CONTEXT.md is locked; the drift is non-substantive).

## Self-Check

**File presence:**

- `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-04-INVESTIGATION-NOTES.md` — FOUND (in worktree path, post-migration from initial Write).

**Commit presence (verified via `git log --oneline -3` on worktree branch `worktree-agent-aa58ae25f96f91d44`):**

- `c2ad5c3 docs(067.2-04): static layer audit + predicted no-op verdict` — FOUND.
- `08368b1 docs(067.2-04): lock predicted verdict (a) no-op; defer runtime confirmation to Plan 06 UAT` — FOUND.

**Grep gates re-verified (post-commit):**

- `Layer-by-layer` matches in INVESTIGATION-NOTES.md: 1 (heading at line 26) — PASS.
- `Predicted verdict` matches in INVESTIGATION-NOTES.md: 1 (subsection heading) — PASS.
- `_emit(redis, run_id, '(confidence|suggestions)'` matches in threads.py: 2 — PASS (≥ 2 required).
- `ConfidenceBadge` matches in MessageItem.tsx: 2 — PASS (≥ 1 required).
- `SuggestionPills` matches in MessageItem.tsx: 2 — PASS (≥ 1 required).
- `useLayoutEffect` matches in ChatArea.tsx: 2 (Plan 01 fix verified live) — PASS.
- `setViewingThread(activeThread.id)` matches in ChatArea.tsx (handleSend new-thread branch): 1 — PASS (Plan 01 synchronous-align fix verified live).

**TypeScript check:** n/a — no source files modified.

**Self-Check: PASSED**

---
*Phase: 067.2-streaming-render-and-storage-fixes*
*Plan: 04 (Wave 2)*
*Completed: 2026-05-08*
