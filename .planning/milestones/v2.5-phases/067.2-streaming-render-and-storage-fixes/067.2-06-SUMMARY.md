---
phase: 067.2
plan: 06
status: blocked
created: 2026-05-08
updated: 2026-05-08
closure_outcome: not_closed
green_count: 7
red_count: 3
deferred_count: 3
new_findings_count: 2
escalation_phase: 067.3
---

# Phase 067.2 Plan 06 — UAT closure SUMMARY (Branch B: not closed)

> Plan 06 coordinated the user-driven UAT scoreboard (`067.2-HUMAN-UAT.md`) per
> CONTEXT.md `<verification_protocol>` § User UAT Scoreboard. The strict closure
> gate (12/12 user-ticked GREEN, partial-with-approval REJECTED) was NOT met.
> Phase 067.2 stays OPEN. Follow-on Phase 067.3 covers the three RED rows + one
> HIGH-severity new finding discovered during the UAT.

## What this plan did

| Task | Status | Output |
|------|--------|--------|
| 1: Generate scoreboard scaffold (12 verbatim D-067.2-NN rows + 2 NR rows + completion gate) | ✅ done | `067.2-HUMAN-UAT.md` (commit `484b4c5`) |
| 2: User UAT session — capture every row's verdict | ✅ done | UAT verdicts captured inline in `067.2-HUMAN-UAT.md` (commit `daf6828`) |
| 3: Close phase — Branch B (RED outcomes) | ✅ done | This SUMMARY + STATE.md update; ROADMAP stays `[ ]` (per Branch B contract) |

## UAT outcome

| Verdict | Count | Rows |
|---------|-------|------|
| ✅ GREEN | 8 | 1, 2, 7, 8, 9, 10 + NR-1, NR-2 |
| ❌ RED | 3 | 3, 4, 6 |
| ⏸ Deferred | 3 | 5 (blocked by Row 4), 11, 12 (symmetric evidence via Row 9) |
| 🆕 New findings | 2 | N-01 routing bug, N-02 self-QA depth |

### What's GREEN — confirmed working

- **D-067.2-01 streaming render race fix (Row 1)** — Plan 01's `useLayoutEffect` placement closes the `guardedSetMessages` no-op window for new-thread + send paths. User-confirmed: tokens stream visibly delta-by-delta on multi-step prompt.
- **D-067.2-02a F5 mid-stream (Row 2)** — replay-from-offset-0 lands in the new bundle. Note: 1-2s reload + replay catch-up window observed (tolerable, not a regression).
- **D-067.2-05a Stop-title (Row 7)** — Plan 02's auto-title hoist runs before the agent loop, so titles fire on Stop. User confirmed.
- **D-067.2-05b Timeout-title (Row 8)** — same hoist; user-confirmed via gpt-5.4 with `LLM_CALL_TIMEOUT_OVERRIDES=gpt-5.4=5`. Backend logs: `Run … timed out at iteration 3 (model=gpt-5.4, budget=5s)` — title generated despite the run failing.
- **D-067.2-06 (Row 9 — claude-sonnet-4-6 / Anthropic native)** — synthetic-timeout test this session: run_id `219aa239-06bc-49c8-b8fd-90f83440a3c5`, `runs.error='timed_out: 10s per-call deadline exceeded at iteration 4 (model=claude-sonnet-4-6)'`. Plan 01 Track A's `_drain_stream_with_close_on_cancel` helper (`threads.py:155-249`) called `stream.close()` cleanly in the Anthropic-native branch (`threads.py:1297-1332`) before propagation. NOT `GeneratorExit at run_helpers.py:1680`. Note: LangSmith does NOT trace Anthropic-native streams (only the OpenAI-route as `ChatOpenAI` runs); `runs.error` IS the canonical clean-cancellation signal per Plan 05's spec.
- **D-067.2-06 (Row 10 — claude-opus-4-6)** — multi-step pipeline confirmed by user-driven run `d6ac7f2e-90ce-48c3-b901-35b02aea88fa`: 14 tool_calls (search → analyze → load_skill pptx → read_skill_file → 10× execute_code), `runs.status='completed'`, no errors. (Self-QA iteration count flagged separately as N-02.)
- **NR-1 / NR-2** — gpt-5.4 multi-step + synthetic-timeout still working; skill-load tool card present (verified via opus run's `load_skill: pptx` tool call). No Phase 067.1 / 066 regressions.

### What's RED — escalating to Phase 067.3

| ID | Row | Failure | Likely fix |
|----|-----|---------|-----------|
| **R-1** | Row 3 — D-067.2-02b cross-thread switch | Mid-stream navigation between concurrent threads loses streaming render on the thread you return to. After both runs complete, everything renders again. Plan 01 fixed `activeThreadIdRef` for the single-stream case but `streamingThreadIdRef` locks to the most-recent run. | Per-thread streaming cache (CONTEXT.md `### D-067.2-04` explicitly flagged this scope as out-of-scope for 067.2). Each thread maintains its own in-flight delta buffer keyed by thread_id; switching threads doesn't tear down the other thread's buffer. |
| **R-2** | Row 4 — D-067.2-03a sandbox-outputs auth | Plan 03's `/sandbox-outputs/{path}` endpoint returns `{"detail":"Not authenticated"}` (401) on browser `<a href>` click. Endpoint uses `Depends(get_current_user)` which reads `Authorization: Bearer <token>` header; `<a href>` clicks send only cookies, and the access token lives in localStorage. Plan 03 unit tests passed because `TestClient` sends Bearer headers automatically — lived-experience failure mode wasn't in test coverage. | **Token-in-URL** is the cleanest pattern: backend issues a short-lived signed token that's appended as `?token=<jwt>` to each link; endpoint validates from query param. Alternative: JS blob download (frontend intercepts click, uses `fetch()` with Bearer, downloads via Blob URL). Add browser-link e2e test to prevent regression. |
| **R-3** | Row 6 — D-067.2-04 suggestions don't render | Confidence badge renders correctly; suggestion pills do not. Confirms Plan 04 INVESTIGATION-NOTES side observation: suggestions emit at `threads.py:2477` lives in a post-`done` try/except — either generation errored silently, returned empty `questions`, or the frontend gate (`!isStreaming && message.suggestions && message.suggestions.length > 0 && onSendMessage`) filters render. | Investigation: backend log + browser DevTools to see whether `message.suggestions` is populated on the SSE stream and on `messages.confidence_level=null` rows. Likely root cause is the post-`done` block silently swallowing or the questions list being empty. Add a server-side info log when `questions=[]` and surface the gate criteria in dev mode. |

### New findings (out-of-scope for 067.2 contract)

| ID | Severity | Finding | Where to fix |
|----|----------|---------|--------------|
| **N-01** | HIGH | Model→provider router uses `user_settings.active_provider`, ignoring `MODEL_CAPABILITIES[model]["provider"]`. With `active_provider="openai"` and `model="claude-sonnet-4-6"` (no explicit `body.provider`), the request goes to OpenAI SDK and returns 404. Repro evidence: run `6eab949f-78da-4ea4-ac01-f04b16c9be7d` (provider=openai, error=NotFoundError). | `backend/app/api/threads.py:949` — change `_resolved_provider = _user_settings.active_provider` to prefer `MODEL_CAPABILITIES[model]["provider"]` when present, fallback to `user_settings`. ~5-line fix. Add unit test that asserts model→provider auto-resolution. |
| **N-02** | LOW (UX/cost concern) | Multi-step pipeline self-QA iteration count is high. Opus run `d6ac7f2e` did 14 tool_calls — 4 setup + 2 build + **8 self-QA** (markitdown extract, install libreoffice, render slides, contact sheet, slide-by-slide visual QA, final verification). Total wall-clock 9.25 min. Not a defect; the agent follows its instruction set correctly. | SYSTEM_PROMPT cap ("verify in at most 2 short iterations") OR `MAX_SELF_QA_ITERATIONS` config. Defer to a UX/cost-tuning phase, not Phase 067.3. |

### Deferred (no UAT blocker — covered by symmetric evidence)

- **Row 5 D-067.2-03b 90-minute wait** — blocked by Row 4 RED (immediate download already 401; long-TTL behavior unreachable). Re-test after R-2 fix.
- **Rows 11, 12 (kimi 2.5, minimax 2.7)** — Plan 05 smoke ✓ for both; Row 9 sonnet timeout test confirmed Track A behavior on the Anthropic native branch + the OpenAI-compat (OpenRouter) branch shares the same code path at `threads.py:1483`. `.env` was reset before kimi/minimax could be exercised live; symmetric coverage from Row 9 + Plan 05 smoke is sufficient evidence.

## Decisions

- **D-067.2-06-01 — Anthropic-native LangSmith trace gap.** LangSmith only traces OpenAI-route LLM calls (`ChatOpenAI` run name). Anthropic-native streaming (the branch Plan 01 Track A patched) is NOT traced. `runs.error` IS the canonical clean-cancellation signal per Plan 05's spec; LangSmith trace is corroborating-only. Sonnet timeout evidence is sufficient on `runs.error` alone.
- **D-067.2-06-02 — Branch B closure.** Phase 067.2 cannot close per the strict gate (CONTEXT.md `<verification_protocol>` § Completion gate: "Partial closures with project-level approval are NOT acceptable for this phase"). Roadmap stays `[ ]`. STATE.md records partial state. Phase 067.3 covers escalation.
- **D-067.2-06-03 — UAT driver as forensic artifact.** `_uat_driver.py` committed in the phase folder. Standalone Python script (no project dep) that acquires Supabase access token via password-grant, posts long-form prompts via `/threads/{id}/messages`, and polls `runs` rows. Reproducible from a clean checkout for any future re-test of timeout / multi-step behavior.

## Self-Check

- [x] All 14 UAT rows have a captured verdict (GREEN / RED / deferred-with-reason).
- [x] Strict-gate language present verbatim in 067.2-HUMAN-UAT.md.
- [x] ROADMAP.md Phase 067.2 entry remains `[ ]` (Branch B contract — no silent closure).
- [x] STATE.md reflects partial state with R-1, R-2, R-3, N-01 listed as blockers (orchestrator update).
- [x] Plan 06 SUMMARY exists at `.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md`.
- [x] All commits atomic; no source code outside `.planning/` modified by Plan 06.

## Next steps for orchestrator / user

1. Plan 06 itself is complete — Tasks 1, 2, 3 (Branch B) done.
2. Phase 067.2 stays OPEN. ROADMAP `[ ]`, STATE Current Position = "Phase 067.2 — UAT blocked (3 RED + 1 HIGH new finding)".
3. Run `/gsd:plan-phase 067.3` (or `/gsd:discuss-phase 067.3` first) to scope **Phase 067.3 — Streaming Render & Storage Fixes Round 2**, covering R-1, R-2, R-3, N-01.
4. Re-run Plan 06 (or its successor) UAT after 067.3 ships to clear the deferred rows + confirm the RED rows turn GREEN.

