---
phase: 067.1
slug: agent-streaming-and-behavior-polish
status: green
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-07
closed: 2026-05-07
project_level_approval: pending
---

# Phase 067.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from `067.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Backend framework** | pytest + pytest-asyncio + httpx |
| **Backend config file** | `backend/pytest.ini` (existing, unchanged) |
| **Backend quick run** | `cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_066_langsmith_clean.py tests/api/test_runs_cancellation.py -x -q --tb=short` |
| **Backend full suite** | `cd backend && venv/Scripts/python.exe -m pytest --tb=short` |
| **Frontend framework** | TypeScript compile only (vitest deferred — Phase 063.1 carry-forward; npm optional-dep cascade) |
| **Frontend quick run** | `cd frontend && npx tsc --noEmit -p tsconfig.json` |
| **E2E framework** | Playwright (existing — no new specs in this phase) |
| **E2E quick run** | `cd e2e && npx playwright test --reporter=line` |
| **Manual UAT** | Plan 05 — autonomous: false; Chrome MCP + Supabase MCP + LangSmith MCP |
| **Estimated runtime** | ~30s backend quick, ~10s frontend tsc, ~3min full backend pytest |

---

## Sampling Rate

- **After every task commit:** Run backend quick (`pytest tests/integration/test_066_langsmith_clean.py tests/api/test_runs_cancellation.py -x -q`) AND frontend tsc (`tsc --noEmit -p tsconfig.json`)
- **After every plan wave:** Run full backend pytest + full frontend tsc + Playwright suite (no new specs but ensures no regression in the existing 9 specs)
- **Before `/gsd-verify-work`:** All sampling-rate gates green + Plan 05 manual UAT scoreboard at `green` (or `partial` with explicit project-level approval per Phase 063.1 / 066 / 067 precedent)
- **Max feedback latency:** ~40s (backend quick + frontend tsc)

---

## Per-Task Verification Map

> Each plan's tasks will populate this matrix during /gsd-plan-phase. Verification commands are mapped to Success Criteria from the ROADMAP detail section. Refer to RESEARCH.md § Validation Architecture → Phase Requirements → Test Map for the per-SC source mapping.

| Task ID | Plan | Wave | Requirement / SC | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|------------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 067.1-01-* | 01 | 1 | SC#1 (Gap-007) | T-067.1-01 (V7 logging) | Preserve `logger.debug(..., exc_info=True)` non-fatal pattern | integration | `pytest tests/integration/test_066_langsmith_clean.py -x -q` | ✅ exists (caplog proxy); Wave 0 extension needed for Track A positive assertion | ✅ green |
| 067.1-02-* | 02 | 2 | SC#2 (in-flight copy) | — | N/A | tsc + manual (Chrome MCP) | `tsc --noEmit -p tsconfig.json` + Plan 05 evaluate_script | ✅ existing files | ✅ green |
| 067.1-03-* | 03 | 1 | SC#3 (multi-step) | T-067.1-03 (V11 destructive autonomy) | "EXCEPTIONS" sub-section preserves confirmation for irreversible/destructive | sanity pytest + manual (Chrome MCP × 5 runs) | optional pytest assert SYSTEM_PROMPT contains "Multi-step intent" + Plan 05 evaluate_script | ⚠ Wave 0 optional sanity test | ✅ green |
| 067.1-04-* | 04 | 2 | SC#4 (skill-load card) | T-067.1-04 (V4/V8 RLS, V7 description-only) | RLS filter at threads.py:1625 unchanged; emit `description` only (NOT `instructions`) | tsc + manual (Chrome MCP DOM assertion) | `tsc --noEmit -p tsconfig.json` + Plan 05 evaluate_script | ✅ existing files | ✅ green |
| 067.1-05-* | 05 | 3 | SC#5 + SC#6 (UAT scoreboard) | — | N/A — UAT-only | manual (Chrome + Supabase + LangSmith MCP) | per-SC capture checklist in PLAN | n/a (autonomous: false) | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] **Plan 01 Track A regression test extension** — `backend/tests/integration/test_066_langsmith_clean.py` currently asserts via caplog proxy. Track A requires positive assertion on the LangSmith trace's actual exception type via a wrapper-injection fixture (mock `_TracedStream._end_trace` to capture `error` argument) asserting `error is None` or `isinstance(error, asyncio.TimeoutError)`. **Skip if Plan 01 ships Track B (retire D-066-11) — Track B is documentation-only.**
- [ ] **Plan 02 `outerBannerLabel` export** — `frontend/src/lib/toolMeta.ts` does not yet export `outerBannerLabel`. Wave 0 task: TypeScript compile-only gate verifies the new export resolves at the call site in `MessageItem.tsx`.
- [ ] **Plan 03 SYSTEM_PROMPT regression sanity (optional)** — no automated test for system-prompt content. Wave 0 task: optional pytest asserting `"Multi-step intent"` substring is present in `SYSTEM_PROMPT` (catches accidental deletion in future PRs). Skip-OK if Plan 05's manual UAT covers behavioral regression.
- [ ] **Plan 04 `onSkillLoaded` callback** — `frontend/src/lib/api.ts` does not yet emit `onSkillLoaded` for the new SSE event payload extension. Wave 0 task: TypeScript compile-only gate verifies new callback signature.

*All four Wave 0 items are gated by either TypeScript-compile or pytest. None require new test infrastructure.*

---

## Manual-Only Verifications

| Behavior | Requirement / SC | Why Manual | Test Instructions |
|----------|------------------|------------|-------------------|
| Context-aware in-flight copy at each latency phase | SC#2 | Renders in real time during a live agent run; needs visual + DOM inspection across pre-first-delta / inter-iteration / tool-preparing phases | Plan 05 Task 3 — Chrome MCP `evaluate_script("document.querySelector('[data-testid=\"chat-streaming-placeholder\"]').textContent")` per phase against a long-form Fahed-Mrad multi-tool prompt |
| Multi-step pipeline executes without intermediate confirmation | SC#3 | gpt-5.4 / model behavior is empirical; 5 consecutive runs needed to establish reliability | Plan 05 Task 2 — Chrome MCP × 5 runs of verbatim Fahed-Mrad prompt; per-run check `evaluate_script("Array.from(document.querySelectorAll('p')).filter(p=>p.textContent.includes('If you want')||p.textContent.includes('Shall I'))")` returns `[]` AND all 4 deliverables present |
| Skill-load card renders upcoming-tool / description hint | SC#4 | DOM render during live skill activation; needs DOM assertion in browser | Plan 05 Task 4 — Chrome MCP `evaluate_script("document.querySelector('[data-testid=\"skill-load-card\"]').textContent")` includes a non-empty description |
| Phase 066 SC#6 promotion to green | SC#5 | UAT scoreboard authoritative; LangSmith trace inspection is browser-side | Plan 05 Task 1 — Re-run synthetic-timeout protocol; LangSmith MCP `getTrace(<new run_id>)` exception column inspection; update `066-HUMAN-UAT.md` row from `partial` → `green` |
| Phase 067 UX-067-01..05 still green after polish | SC#6 | Regression check against existing markers under live agent run | Plan 05 Task 5 — Re-run the 4 protocol runs from `067-HUMAN-UAT.md` (UX-067-01..04 use existing markers; UX-067-05 uses `data-testid="iteration-divider"`) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (per-task fields populated by /gsd-plan-phase)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (Plan 05 manual gate is intentional and sequential)
- [x] Wave 0 covers all MISSING references (4 items above — Plan 01 Track A regression test extension shipped via `_FakeTracedStream` + `_StallingIterator` doubles in `test_066_langsmith_clean.py`; Plan 02 `outerBannerLabel` export shipped + tsc gate green; Plan 03 SYSTEM_PROMPT regression sanity covered by Plan 05 Task 2 5-run capture; Plan 04 `onSkillLoaded` callback shipped + tsc gate green)
- [x] No watch-mode flags (all commands run-and-exit)
- [x] Feedback latency < 60s (backend quick + frontend tsc combined ~30-40s observed)
- [x] `nyquist_compliant: true` set in frontmatter (this commit — at phase close)

**Approval:** green-with-note (SC#3 strict 3/5 hedge-free; substantive failure mode fixed; project-level approval pending user sign-off on `067.1-HUMAN-UAT.md`)
