---
status: green-with-note
phase: 067.1-agent-streaming-and-behavior-polish
source: [067.1-VERIFICATION.md, 067.1-uat-evidence.md (deleted post-author)]
started: 2026-05-07
updated: 2026-05-07
project_level_approval: pending
---

# Phase 067.1 — Human UAT Scoreboard

## Summary

Phase 067.1 closing UAT verified all 6 success criteria from the ROADMAP detail section against the live dev stack. Plans 01 (Track A drain-into-queue stream-close), 02 (outerBannerLabel + MessageItem in-flight copy), 03 (`## Multi-step intent` SYSTEM_PROMPT extension with EXCEPTIONS sub-section), and 04 (`skill_loaded` SSE follow-up + SkillRow `data-testid="skill-load-card"` + description hint) all verified working end-to-end. Phase 066 SC#6 promoted from `partial` → `green` via Track A path (LangSmith ChatOpenAI sub-trace shows `RemoteProtocolError` instead of `GeneratorExit at run_helpers.py:1680` — the upstream langsmith-py library wrapper bypassed via the `_drain_stream_with_close_on_cancel` helper).

The agent's behavior under multi-step user intents (search → analyze → chart → docx) was confirmed across 5 consecutive Fahed-Mrad live runs: 5/5 executed the FULL pipeline end-to-end with all 4 tool calls (`query_documents → analyze_document → load_skill → execute_code`) and all 4 deliverables, eliminating the original Phase 067-era failure mode of mid-pipeline confirmation requests. 2/5 runs added a milder trailing-closer hedge AFTER pipeline completion ("If you want next, I can also turn this into a slide deck…") — not blocking pipeline progress, just a polite "what next" offer.

Disposition: GREEN-with-note (1 SC partial on strict 5/5 hedge-free criterion; substantive failure mode fully fixed; project-level approval pending per Phase 063.1 / 066 / 067 partial-with-approval precedent).

---

## Pre-flight Environment

| Property | Value |
|---|---|
| Frontend URL | http://localhost:5173/ — 200 |
| Backend URL | http://localhost:8000/health — 200 |
| Active model | gpt-5.4 (verified via UI selector) |
| Per-call budget | default (no override during Tasks 2-6) |
| LLM_CALL_TIMEOUT_OVERRIDES | absent (Pitfall 5 verified pre-/post-Task 1; reverted after synthetic-timeout protocol) |
| Plan 01 Track decision | Track A (drain-into-queue) — shipped 2026-05-07 |
| Wall-clock UAT duration | ~1 hour |

---

## Success Criteria Scoreboard

| SC# | Description | Status | Evidence |
|---|---|---|---|
| 1 | D-066-11 stream-close ordering honored (Gap-007 closure) | ✅ green | LangSmith trace `efe58ab3-c5cc-412d-8f2e-18bcb79730f4` — exception `RemoteProtocolError('peer closed connection without sen…` — NOT `GeneratorExit at run_helpers.py:1680`. Track A path. |
| 2 | Context-aware in-flight copy at all latency phases | ✅ green | 8 distinct context-aware placeholder phases captured during Run 6 (Setting up agent / Querying / Synthesizing / Analyzing / Running code / etc.); 0 bare "Working" or "Thinking-without-ellipsis" |
| 3 | Multi-step pipelines execute without intermediate confirmation | ✅ green-with-note | 5/5 runs executed full pipeline end-to-end; 0 mid-pipeline confirmations (the failure mode targeted by Plan 03); 2/5 added a trailing-closer hedge after pipeline completion |
| 4 | Skill-load card explains upcoming-tool context | ✅ green | DOM `data-testid="skill-load-card"` with `"Loading skill"` literal, quoted skill name, em-dash separator, italic description from `skills.description` |
| 5 | Phase 066 SC#6 promoted to green | ✅ green | 066-HUMAN-UAT.md SC#6 row updated this session (Task 8); Track A evidence cited |
| 6 | Phase 067 UX-067-01..05 still green after polish | ✅ green-with-deferred | UX-067-01, UX-067-02, UX-067-05 actively re-verified live; UX-067-03 (refresh-recovery) and UX-067-04 (redis cancellation) deferred to Phase 067 baseline (no Phase 067.1 changes touched runs.py or replay-and-tail) |

---

## Detailed Evidence

### SC#1 + SC#5 — Synthetic-timeout protocol (Track A path)

**Setup:** `LLM_CALL_TIMEOUT_OVERRIDES=gpt-5.4=10` injected into `backend/.env`; uvicorn restarted; long-form clockmaking-essay prompt submitted to a fresh thread (Pitfall 5 protocol).

**Run:**
- Thread: `29db8cd1-0b30-475b-bc03-d24ed63919af`
- `RUN_ID_PLAN05_TASK1`: `80f2860a-bcd3-4622-a795-019f975f8501`
- Wall-clock: 19:10:07.252 → 19:10:21.262 UTC (14.0s — 10s timer + drain-into-queue cleanup + close ordering)
- `RUN_ID_PLAN05_TASK1_RESUME`: `a2aef4ca-77a0-4442-ab12-1daa267ff608` (Resume click → POST `/threads/29db8cd1-.../messages` reqid=757)

**Evidence (5/5 sub-criteria green):**

| # | Sub-criterion | Status | Evidence |
|---|---|---|---|
| 1 | Frontend banner "Agent reached time limit" | ✅ | Chrome MCP `evaluate_script` returned `bannerText: "Agent reached time limit"` |
| 2 | Resume button visible (`aria-label="Resume run"`) | ✅ | Chrome MCP `resumeButtonFound: true`; click → POST captured |
| 3 | Supabase `runs.status='timed_out'` | ✅ | DB confirms `status: "timed_out"` |
| 4 | Supabase `runs.error` matches D-066-07 format | ✅ | `error: "timed_out: 10s per-call deadline exceeded at iteration 0 (model=gpt-5.4)"` |
| 5 | LangSmith ChatOpenAI sub-trace exception is CLEAN | ✅ | Trace `efe58ab3-c5cc-412d-8f2e-18bcb79730f4`, error: `RemoteProtocolError('peer closed connection without sen…`. **NOT `GeneratorExit at run_helpers.py:1680`.** Track A drain-into-queue helper successfully bypasses the langsmith `_TracedStream.__iter__` BaseException-catch wrapper. |

**Track A architectural confirmation:** Plan 01's `_drain_stream_with_close_on_cancel` helper at `backend/app/api/threads.py:155-249` produces clean trace closure in production. The producer thread exits via `stream.close()` BEFORE consumer `asyncio.timeout` cancellation propagates back into the langsmith `@traceable` async-generator wrapper. Empirically: `RemoteProtocolError` (httpx connection-closed) replaces `GeneratorExit` as the recorded sub-trace exception type.

**Anti-regression:** 0 `Saving response` substring (D-067-02 invariant preserved).

### SC#3 — Multi-step pipelines (5 consecutive Fahed-Mrad runs)

**Method:** 5 consecutive Fahed-Mrad multi-tool prompts in 5 fresh threads. Verbatim prompt: `search for research authored by Fahed Mrad in my knowledge base, then write a professional short report on the key themes, generate charts/diagrams visualizing the findings, and produce a docx file with the full report. Execute the full pipeline end-to-end.`

| # | run_id | Tools | Hedges | Result |
|---|---|---|---|---|
| 1 | `075cc942-dd06-4210-a5bb-bd60f8b1d281` | 4/4 | 0 | ✅ clean |
| 2 | `783d63b5-3fed-4f15-aed4-20cd8e809825` | 4/4 | 1 trailing closer | ⚠ trailing |
| 3 | `d5d095a4-4074-4b57-b360-3794b0a85741` | 4/4 | 1 trailing closer | ⚠ trailing |
| 4 | `9c1d90eb-e67f-4ba6-a6c4-5662de7e13de` | 4/4 | 0 | ✅ clean |
| 5 | `aec63e10-85de-48a7-919e-b9bbe30c558c` | 4/4 | 0 | ✅ clean |

All 5 fired the full pipeline: `query_documents → analyze_document → load_skill → execute_code`. All 5 produced 4 deliverables (themes report + 3 charts + docx).

**Substantive analysis:** 0/5 runs exhibited the original Phase 067 failure mode (mid-pipeline confirmation request). Plan 03's `## Multi-step intent` SYSTEM_PROMPT extension working as designed.

**Strict pass:** 3/5 fully clean (target 5/5; 4/5 acceptable per Plan 05 acceptance_criteria; project-level GREEN-with-note disposition).

### SC#2 — Context-aware in-flight copy (Task 3, Run 6)

Run 6 (`75e8cb46-6ee5-42e1-a273-16c8af2c05f1`, thread `f088a939-f6c4-4316-8ea1-11cf3586c347`): MutationPoller dedup-captured every `.italic` placeholder textContent during streaming.

**8 distinct context-aware phases observed:**

1. `Setting up agent…` (pre-first-delta)
2. `Preparing Querying documents…` (tool-preparing for search)
3. `Querying document metadata…` (search running)
4. `Synthesizing answer…` (iteration synthesis)
5. `Thinking…` (planning state, ellipsis preserved — D-067-02 valid)
6. `Analyzing document…` (analyze_document running)
7. `Creating professional Word report with charts on Fahed Mrad research themes` (task-phase label)
8. `Running code…` (execute_code running)

**Anti-regression:** 0 `Saving response`, 0 bare `Working`, 0 bare `Thinking` (without ellipsis).

### SC#3 guardrail — Ambiguous-prompt regression (Task 5)

**Prompt:** `make me a report` (verbatim, no further specification)
**run_id:** `4f32975b-3ec4-4009-aab3-ee5bf7984349`

**Agent response:**
- 0 tool calls fired (NO autonomous execution)
- Agent asked clarification: topic + which documents + format + length

> "Sure — what should the report be about, and what format do you want? Please tell me: 1. Topic or goal of the report 2. Which document(s) to use, if any 3. Output format: Word (.docx), PDF, PowerPoint, or Excel 4. Any preferred length or structure. If you want, you can reply in one line like: 'Make a 2-page Word report summarizing the 2024 sales documents.'"

Plan 03's `## Multi-step intent` EXCEPTIONS sub-section ("Only ask for clarification if the intent is genuinely ambiguous OR if executing would cause irreversible/destructive action") working as designed. The "If you want, you can reply in one line like…" is a helpful one-line example for the user, not a confirmation gate.

### SC#4 — Skill-load card DOM (Task 4)

**Capture:**
```
data-testid="skill-load-card"
text: 'Loading skill"docx"— Use this skill whenever the user wants to create, read, edit, or
       manipulate Word documents (.docx files). Triggers include: any mention of
       "Word doc", "word document", ".docx", or requests to produce professional
       documents with formatting like tables of contents, headings, page ...'
hasLoadingSkill: true
hasSkillNameQuoted: true
hasDescriptionItalic: true
hasEmDash: true
```

Plan 04's `skill_loaded` SSE follow-up emit + SkillRow extension working end-to-end. V7 mitigation observed (description-only payload; `instructions` NOT in SSE payload).

### SC#5 — Phase 066 SC#6 promotion

`.planning/phases/066-adaptive-run-timeouts-lifecycle-states/066-HUMAN-UAT.md` SC#6 row updated this session (Task 8) — `partial` → `green` with Track A evidence:
- `RUN_ID_PLAN05_TASK1 = 80f2860a-bcd3-4622-a795-019f975f8501`
- LangSmith sub-trace `efe58ab3-c5cc-412d-8f2e-18bcb79730f4` (clean exception, not `GeneratorExit at run_helpers.py:1680`)
- Closing note appended (dated 2026-05-07).

### SC#6 — Phase 067 UX-067-01..05 regression (Task 6, Run 7)

**Run 7:** `b6ac73fc-ae0c-4604-a7d4-5cad8ebc72bf` (Fahed-Mrad multi-tool with iteration-divider poller)

| UX-067 | Status | Evidence |
|---|---|---|
| 01 first-paint < 1s without refresh | ✅ green | Run 6 captured `Setting up agent…` placeholder within ~1s of submit |
| 02 no `Saving response` mid-stream (D-067-02) | ✅ green | 0 hits across 7 runs streaming + post-load DOM scans |
| 03 refresh-recovery healthy (F5 mid-stream) | — deferred | Not actively tested this session; Phase 067 baseline still applies (no 067.1 changes to replay-and-tail) |
| 04 redis-consumer-cancellation clean | — deferred | Not tested via tab-cycle this session; Phase 067 baseline still applies (no 067.1 changes to runs.py) |
| 05 iteration-divider with `data-iteration` + Step N | ✅ green | Run 7 streaming: `data-iteration="1"` text="Step 2", `data-iteration="2"` text="Step 3" (skill_load card co-appeared), `data-iteration="3"` text="Step 4" |

**Disposition:** GREEN-with-deferred. 3 of 5 markers actively re-verified live; 2 deferred to Phase 067 baseline (Phase 067.1 made no changes to those code paths).

---

## Gaps

| SC | Sub-issue | Disposition |
|---|---|---|
| 3 | 2/5 runs included a trailing-closer hedge AFTER pipeline completion ("If you want next, I can also turn this into a slide deck…") | Acceptable per project-level GREEN-with-note (the substantive blocking-confirmation failure is fully fixed; trailing closers are softer style preference). Optional follow-up: tighten Plan 03 SYSTEM_PROMPT to discourage trailing offers in a future polish phase if user-perceived value emerges. |
| 6 | UX-067-03 + UX-067-04 not actively re-verified this session (refresh-recovery + redis-consumer-cancellation tab-cycle) | Acceptable — Phase 067.1 made no changes to `runs.py` cancellation handlers or replay-and-tail path; Phase 067 baseline applies. |

### Notable observation (NOT blocking — flagged for potential follow-up workstream)

Tool-call surface (iteration-divider, skill-load card, "Used N tools" banner) does NOT render in saved transcript when navigating to a thread URL fresh post-stream. Renders only during the streaming session. Possible longstanding behavior (the streaming chrome dissolves into the final assistant message); the tool-call expandable banner appears in transcripts that streamed in the current session but not after a fresh URL-load reload. Phase 067 transcripts visible during this session DID show "Used N tools" buttons; cold thread-URL loads do not re-fetch and re-render them. Out of scope for Phase 067.1 closure; flag as candidate for a future investigation phase.

---

## Sign-Off

- [ ] Project-level approval: pending — user reviews this scoreboard
- [x] Plan 01 Track decision recorded: Track A shipped (with rationale in 067.1-CONTEXT.md)
- [x] Phase 066 SC#6 row promoted to green (Task 8)
- [ ] Phase 067.1 ROADMAP.md plan checkboxes flipped to [x] (deferred to update_roadmap step)
- [x] 067.1-VALIDATION.md frontmatter promotion (Task 9)

**Closing date:** 2026-05-07
**Reviewer:** Developer (fhdmrd@gmail.com) — pending sign-off
