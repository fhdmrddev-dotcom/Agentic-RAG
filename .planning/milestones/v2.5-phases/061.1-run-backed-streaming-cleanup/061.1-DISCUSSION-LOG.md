# Phase 061.1: Run-Backed Streaming Cleanup - Discussion Log

**Date:** 2026-05-03
**Mode:** discuss (default), SPEC-loaded
**Areas selected:** all 4 (S2 test-race fix pattern, S1 ERR_INCOMPLETE_CHUNKED_ENCODING disposition, IN-01..04 inclusion, plan splitting)

---

## Pre-discussion context loaded

- `.planning/phases/061.1-run-backed-streaming-cleanup/061.1-SPEC.md` (locked requirements)
- `.planning/phases/061-run-backed-streaming-backend/061-CONTEXT.md` (parent phase D-061-01..17)
- `.planning/phases/061-run-backed-streaming-backend/061-REVIEW.md` (CR/WR/IN findings)
- `.planning/phases/061-run-backed-streaming-backend/061-HUMAN-UAT.md` (S1 evidence)
- `.planning/STATE.md` (current position; CR-01/02/03 + WR-03/05 already fixed inline)
- `.planning/PROJECT.md` (D-v2.5-08..11)

Confirmed inline-fixed in commits 27e2d85, e02ba99, 74b6833, 1c2b385, 5d8ff10:
CR-01 (terminal-status scope), CR-02 (CancelledError shield), CR-03 (CI container name), WR-03 (assert→ValueError), WR-05 (httpx ASGITransport).

---

## Area 1 — S2 test-race fix pattern

**Question asked:** "How should the 5 integration tests wait for the detached `_shielded_finalize` before asserting on mocks?"

**Options presented:**
- (A) Await producer task via `RUN_TASKS` lookup with `asyncio.wait_for(timeout=10)` — fully deterministic [Recommended]
- (B) Poll for terminal sentinel/UPDATE — no shared state, slightly slower
- (C) Fixed `asyncio.sleep(5.0)` — status quo from existing test_059 inversion test

**User selected:** (A) Await producer task via `RUN_TASKS`.

**Reasoning captured:** Deterministic, zero CI-load sensitivity, no new shared state beyond the already-public `RUN_TASKS` registry per D-061-11. Removes ~25s of fixed sleeps. Will be implemented as a shared helper `await_producer_finalized` in `_run_helpers.py`; the existing `test_059_disconnect.py:300` `asyncio.sleep(5.0)` migrates to the same helper (which is what WR-04 was asking for) — so D-061.1-14 absorbs WR-04 into D-061.1-01.

---

## Area 2 — S1 ERR_INCOMPLETE_CHUNKED_ENCODING disposition

**Question asked:** "How aggressively should we pursue S1 in 061.1?"

**Options presented:**
- (A) Diagnose and fix in-place
- (B) Diagnose + defer fix to 063 [Recommended] — SPEC explicitly allowed this
- (C) Diagnose only, no active reproduction

**User selected:** (A) Diagnose and fix in-place.

**Reasoning captured:** Producer architecture is sound; the consumer drop is the last brittleness before Phase 063's frontend cutover. A clean consumer raises confidence that 062's new HTTP endpoints land on a stable foundation. Cost is bounded — if root cause is in Vite proxy or sse-starlette internals, fix may be larger than expected; planner is empowered to scope the fix conditionally on diagnosis evidence (Plan 01 Task 2).

**Follow-up question asked:** "Do you have a prime suspect among the 4 hypotheses, or systematically investigate all?"

**Options presented:**
- (A) Systematically rule out all 4 — most rigorous
- (B) Vite proxy (H4) + sub_agent_delta volume (H3) first [Recommended] — evidence-driven
- (C) sse-starlette ping behavior (H2) first — cheapest test

**User selected:** (B) H4 + H3 first, fall back to H1/H2.

**Reasoning captured:** Symptom (`TypeError: network error` browser-side, only on sub-agent flows with ~1000 sub_agent_delta events) points strongly at proxy/volume causes. Bisection plan documented in D-061.1-05.

---

## Area 3 — IN-01..04 inclusion

**Question asked:** "Which IN items should land in 061.1?" (multiSelect)

**Options presented:**
- IN-01 — move helpers to `_run_helpers.py`
- IN-02 — filter run-id INSERT by status
- IN-03 — reduce double-nonlocal pattern
- IN-04 — extract `event_consumer` to module-level

**User selected:** All four.

**Reasoning captured:** SPEC said "address opportunistically if cheap; defer otherwise" — user opted for the more thorough sweep. IN-03 and IN-04 are architecturally sensitive (touch the active SSE path and shielded finalizer). Land each as separate atomic commits with explicit "Behavior unchanged" rationale in commit messages so future archaeology connects refactor to cleanup intent.

---

## Area 4 — Plan splitting

**Question asked:** "How should 061.1 be split into plans?"

**Options presented:**
- (A) Two plans (production code + test infrastructure) [Recommended]
- (B) Single plan, multiple waves
- (C) Single plan, one wave

**User selected:** (A) Two plans.

**Plan layout:**
- **Plan 01 — production code:** T1 S1 diagnose → T2 S1 fix (conditional on T1) → T3 WR-01 cursor + regression test → T4 WR-07 redis pin → T5 IN-03 nonlocal refactor → T6 IN-04 event_consumer extraction. 6 atomic commits.
- **Plan 02 — test infrastructure:** T1 build out `_run_helpers.py` (await_producer_finalized + IN-01 moves) → T2 IN-02 robustness → T3 apply pattern to 5 S2 tests → T4 WR-04 migration → T5 WR-06 conftest upsert. 5 atomic commits.

**Reasoning captured:** Plan 02 is independent of S1's outcome — can ship even if S1 needs follow-up. Cleaner reviewer experience: production-code plan reads as small fixes; test plan reads as a coherent refactor.

---

## Deferred ideas surfaced

- sub_agent_delta coalescing strategy (only acted on if H3 confirmed)
- Real-Supabase dual-user RLS test substrate (carried forward from 061)
- testcontainers Redis fallback (carried forward)
- Token-counter accounting (carried forward)
- /__health/sse observability (carried forward)
- Browser-MCP scenario harness (Phase 064)

---

## Claude's discretion items

- Mock-redis vs real-Redis for WR-01 regression test
- File location of `await_producer_finalized` helper
- IN-04 extracted function lives in threads.py vs new `_event_consumer.py`
- IN-03 return-type ergonomics (Optional[str] vs dataclass)
- H3 fix shape if H3 confirmed (event-count vs time-window vs byte budget)

---

*Compiled by gsd-discuss-phase, default mode, 2026-05-03*
