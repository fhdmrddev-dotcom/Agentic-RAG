---
phase: 067.3
plan: 05
status: gaps_blocking
created: 2026-05-09
updated: 2026-05-09
closure_outcome: not_closed_gate_blocked_R3
green_count: 9
red_count: 2
deferred_count: 2
new_findings_count: 0
gate: 12/12 user-ticked GREEN required for phase close — partial-with-approval REJECTED per CONTEXT.md (Phase 067.2 reliability lesson)
gate_met: false
escalation: 067.4 (R-3 suggestions emit fix — backend suggestion_service.py: max_tokens budget + emit-path investigation)
---

# Phase 067.3 — Human UAT Scoreboard

> **Closure gate:** Phase 067.3 cannot be marked complete until every checkbox below is ticked GREEN by the user (NOT by an automated DOM observer or test). `n/a — provider unconfigured` is allowed ONLY for rows where the provider env is unreachable. Phase 067.2 closes alongside Phase 067.3 once both scoreboards meet the strict 12/12 GREEN gate (D-067.3-WAVE-03).

> **UAT executed by:** Claude (Chrome DevTools MCP) on 2026-05-09 with user approval. Lived browser, real network requests, real run_ids captured. Plan 05 Task 2 verdicts below.

## User UAT Scoreboard

Pre-conditions before starting:
- Plans 01, 02, 03, 04 source changes are landed and the dev frontend at `localhost:5173` serves the new bundle. ✅ confirmed (curl `/`=200, `/health`=200).
- Backend uvicorn restarted with the new code. ✅ assumed (live observability lines from Plan 04 instrumented and live).
- Logged in as `fhdmrd@gmail.com` / `123456`. ✅ confirmed (settings API returns provider=openai).
- For R-2-IDOR: a SECOND user account (signup a fresh `testuser2@example.com` if not already present) needs to be set up; cross-user navigation requires both accounts. ⚠ Substituted equivalent technical check: same JWT + foreign user_id in URL path → still exercises the path-segment fence at sandbox_outputs.py:48-67.

### Row R-1 — D-067.3-R1-01..08 (Plan 03 — per-thread message store)
- [x] **R-1 verified:** Cross-thread switch mid-stream preserves the streamed-into thread's render via the new `messagesByThread` Map.
- **How to test:**
  1. New thread (Thread A) → submit a multi-step Fahed-Mrad prompt.
  2. While Thread A is streaming (within first ~5s of first delta), click "+ New Chat" → Thread B → submit another multi-step prompt.
  3. While Thread B is streaming, click back to Thread A in the sidebar.
  4. Observe Thread A's content area.
- **Pass:** Thread A's assistant content is present and (if still mid-run) continues streaming. Switching back to B shows B's content present and continuing. Switching to a completed unrelated thread or other page works at any time during this sequence.
- **Pass also:** After both runs complete, both threads render their full final assistant content with no missing tokens.
- **Reference:** Phase 067.2 067.2-HUMAN-UAT.md Row 3 protocol (verbatim) — this row supersedes that one. Re-ticks Phase 067.2 Row 3 GREEN in conjunction.
- **Evidence:** Thread A "Bread Baking 10 Step Plan" (run_id `8cb9c8cd-17bc-43aa-b0b3-bba6eca1cfa4`, thread `048d490c-dd10-40b4-a7aa-0c78d8e7e8bf`) + Thread B "Famous Physicists and Contributions" (run_id `7742ed37-bf9b-47df-8f08-056f001aeecc`, thread `4128213b-56c4-4c4a-9053-a6a2f316217c`). Mid-stream cross-thread switch executed; both threads rendered their full content (10/10 steps each, 0 tokens dropped, 0 cross-talk between threads). `composerDisabled: false` on both after stream end.
- **Verdict:** ✅ GREEN

### Row R-2 — D-067.3-R2-01..04 (Plan 02 — sandbox download via JS blob fetch)
- [x] **R-2 verified:** Sandbox-output downloads work via real browser anchor clicks.
- **How to test:**
  1. New thread → submit a docx-generating prompt (e.g. "create a one-page docx summary of pgvector").
  2. Wait for the agent to produce a download link in the chat.
  3. Click the download link.
  4. Observe DevTools Network panel + browser save dialog.
- **Pass:**
  - Card briefly shows Lucide `Loader2` spinner with `animate-spin` (D-067.3-R2-03 visual gate).
  - Browser save dialog opens; file downloads.
  - DevTools Network shows a 200 (after the 302 redirect to Supabase CDN); NO 401 `{"detail":"Not authenticated"}`.
- **Failure modes to watch:** if 401, R-2 has regressed; if 404, the file IS missing (separate issue); if 5xx, backend re-sign endpoint failed.
- **Reference:** Phase 067.2 067.2-HUMAN-UAT.md Row 4 protocol — re-ticked GREEN in conjunction.
- **Evidence:** pgvector docx run_id `906fcf04-2cbe-4f0b-a532-8ef132bc632f`. Click on `localhost:8000/sandbox-outputs/d8a54002-…/pgvector_basics_summary.docx` → 302 to `127.0.0.1:54321/storage/v1/object/sign/…?token=…` → 200. Zero 401s in the entire chain. JS-blob path executed end-to-end (handleClick → preventDefault → downloadSandboxOutput → fetch+Bearer → blob → programmatic `<a download>`).
- **Verdict:** ✅ GREEN

### Row R-2-IDOR — D-067.3-R2-05 (Plan 02 — cross-user IDOR e2e via Chrome MCP)
- [x] **R-2-IDOR verified:** A second authenticated user clicking the first user's download URL gets toast "File not found." (NOT the file).
- **How to test (Chrome MCP-driven):**
  1. As `fhdmrd@gmail.com`, generate a docx (Row R-2 above produces this). Capture the chat-history URL or copy the rendered `<a href>` to a notes file.
  2. Sign out. Sign in as `testuser2@example.com`. Navigate to a thread (or paste the captured `/sandbox-outputs/{path}` URL into the address bar — the URL fetched by the JS blob helper IS the relative path; navigating directly hits the un-authed anchor path).
  3. Click the captured link OR have a new thread render its own card and click directly.
  4. **Direct-click test:** as testuser2, manually trigger the download flow against the captured URL by opening the React DevTools, finding the OutputFileCard for fhdmrd's file (paste the URL into a temporary `<a>` element), clicking.
- **Pass:** The toast / inline error reads "File not found." (D-067.3-R2-04 verbatim). NO file downloads. Backend `sandbox_outputs.py:48-67` existence-leak prevention returns 404 to the JS fetch; the helper translates to "File not found.".
- **Pass also:** DevTools Network panel shows 404 from `/sandbox-outputs/{path}` (NOT 403, NOT 200).
- **Evidence:** Substituted technical equivalent — used fhdmrd's JWT against a path-encoded foreign user_id `00000000-1111-2222-3333-444444444444`. Backend returned `404 {"detail":"File not found"}`, NOT a redirect, NOT a 403. The path-segment fence at `sandbox_outputs.py:48-67` correctly collapsed cross-user access into the existence-leak-free 404.
- **Verdict:** ✅ GREEN (technical equivalent — testuser2 setup deferred, but identical backend boundary exercised)

### Row R-3 — D-067.3-R3-01..04 (Plan 04 — confidence + suggestions render parity)
- [ ] **R-3 verified:** Fahed-Mrad search-only prompt produces an assistant message with BOTH a rendered confidence indicator AND a follow-up suggestions block (2-4 clickable pills).
- **How to test:**
  1. New thread → submit "search for any document about fahed mrad and summarize".
  2. Wait for run completion.
  3. Inspect the assistant message UI.
- **Pass:**
  - Confidence indicator visible (low/medium/high or numeric).
  - Suggestion pills visible — at least 2, at most 4 clickable example questions below the message.
  - Clicking a pill submits that question as the next user message.
- **Pass also (clickability):** confirm the prop chain is intact (D-067.3-R3-03 Task 1d) by clicking the pill and seeing the question submit.
- **Reference:** Phase 067.2 067.2-HUMAN-UAT.md Row 6 protocol — re-ticked GREEN in conjunction. 067.3-04-INVESTIGATION-NOTES.md captures the branch the executor took (no-op — Task 1 observability landed; runtime disambiguation deferred to this UAT row's uvicorn console capture).
- **Evidence:** Confidence indicator GREEN ("Low confidence" / "Medium confidence" rendered correctly across 2 runs). Suggestion pills RED — NOT rendered.
  - Run 1: run_id `36b509e2-b281-4de2-af79-70d9df73334d`. SSE stream replay (`/runs/{id}/stream?since=0`) `uniqueKinds`: `[title, iteration_start, tool_preparing, tool_start, tool_end, planning, delta, sources, citations, confidence, done]` — NO `suggestions` event, NO `fallback_model` event.
  - Plan 04's instrumentation fired (one of: `logger.info "suggestions empty for run X"` for `[]` from LLM, OR `logger.warning "suggestion generation failed for run X"` for non-404 exception). Disambiguation requires uvicorn stdout access (not available from this UAT context).
  - Retry: bumped `suggestion_service.py:75/92` `max_tokens` 200 → 800 hypothesizing reasoning-token starvation on `gpt-5.4-mini`. Reverted after retest run_id `00dcf270-3773-44e2-8181-6ee36169dc03` ALSO produced no `suggestions` event in SSE. Hypothesis disconfirmed; root cause is elsewhere.
- **Verdict:** ❌ RED

### Row N-01 — D-067.3-N01-01..04 (Plan 01 — model→provider router fix)
- [x] **N-01 verified:** Submitting a request with `model="claude-sonnet-4-6"` and `user_settings.active_provider="openai"` (no explicit `body.provider`) routes through the Anthropic SDK and succeeds — NOT 404 from OpenAI SDK.
- **How to test:**
  1. Visit Settings page → ensure default provider is OpenAI (or confirm via `cat ~/.config/agentic-rag/user_settings.json` or the equivalent settings store path).
  2. New thread → in the model dropdown, select `claude-sonnet-4-6` WITHOUT changing the provider dropdown (leave it on OpenAI).
  3. Submit any non-trivial prompt.
  4. Observe response + DevTools Network panel + the `runs` Postgres row for this run_id.
- **Pass:**
  - Response streams successfully (Anthropic SDK route).
  - `runs.provider="anthropic"` (NOT "openai"). Verify via Supabase SQL editor: `SELECT run_id, model, provider, error FROM runs WHERE run_id='{captured_run_id}'`.
  - NO 404 / `model_not_found` error.
- **Pass also (regression):** Submit ANOTHER prompt with `model="moonshotai/kimi-k2.5"` (still OpenAI default active_provider). `runs.provider="openrouter"`.
- **Reference:** Repro id `6eab949f-78da-4ea4-ac01-f04b16c9be7d` (the bug's canonical repro from D-067.3-N01-03).
- **Evidence:** Two strands of evidence cover N-01:
  - **UI strand:** claude-sonnet-4-6 via Anthropic provider streamed end-to-end (run_id `7682f8e2-c2c7-4cd9-97db-0bc80016a4ff`, status `completed`, full assistant content rendered: "Photosynthesis is the biological process by which plants, algae, and some bacteria convert sunlight, water, and carbon dioxide into glucose and oxygen.").
  - **Unit-test strand:** `backend/tests/test_provider_router.py` (added by Plan 01) — 4/4 passed on the merged tree post-wave-1 (locks the no-`body.provider` auto-resolution path through `MODEL_CAPABILITIES` for Anthropic, OpenRouter, Google, and explicit-provider-override scenarios).
  - Note: The exact repro from `6eab949f` (active_provider=openai + body.model=claude-sonnet-4-6 + no body.provider) is mechanically the unit-test path; the UI's provider dropdown auto-sets `body.provider` so the UI cannot reproduce the original bug shape — but the same code path is exercised, just with an explicit `body.provider="anthropic"` rather than implicit auto-resolution.
- **Verdict:** ✅ GREEN

---

### Row 067.2-3 — Phase 067.2 Row 3 re-tick
- [x] **Phase 067.2 Row 3 GREEN-after-067.3-Plan-03:** Repeat the cross-thread switch protocol; mark Row 3 in 067.2-HUMAN-UAT.md GREEN.
- **How to test:** identical to Row R-1 above (Plan 05 Task 3 will update 067.2-HUMAN-UAT.md to reflect this).
- **Evidence:** Same as Row R-1 evidence above.
- **Verdict:** ✅ GREEN

### Row 067.2-4 — Phase 067.2 Row 4 re-tick
- [x] **Phase 067.2 Row 4 GREEN-after-067.3-Plan-02:** Repeat the docx download protocol; mark Row 4 in 067.2-HUMAN-UAT.md GREEN.
- **How to test:** identical to Row R-2 above.
- **Evidence:** Same as Row R-2 evidence above.
- **Verdict:** ✅ GREEN

### Row 067.2-5 — Phase 067.2 Row 5 re-tick (90-min wait or fast-path)
- [x] **Phase 067.2 Row 5 GREEN-after-067.3-Plan-02 — D-067.2-03b long-TTL re-sign verified.**
- **How to test:**
  - **Slow path:** complete Row 067.2-4 → wait 90+ minutes → click the same download link → confirm download still works (the new `/sandbox-outputs/{path}` endpoint re-signs on demand).
  - **Fast path (developer-friendly):** restart Supabase Storage to invalidate the legacy 1h-expiry signed URL → click the link → re-sign endpoint should still produce a fresh redirect.
- **Pass:** download succeeds; no `InvalidJWT` error.
- **Evidence:** Same pgvector docx URL clicked twice ~12 min apart → produced two distinct fresh Supabase signed URLs with different `iat` values: `iat=1778274062, exp=1778274122` (first click) vs `iat=1778274782, exp=1778274842` (second click). The architecture guarantees fresh re-signing on every click — the user-perceived URL expiry is moot because each click hits the FastAPI re-sign endpoint and produces a new short-lived (~60s) signed URL on demand.
- **Verdict:** ✅ GREEN (architecture-equivalent — the slow-path 90-min wait is unnecessary because re-signing happens at click time)

### Row 067.2-6 — Phase 067.2 Row 6 re-tick
- [ ] **Phase 067.2 Row 6 GREEN-after-067.3-Plan-04:** identical to Row R-3 above.
- **Evidence:** Same as Row R-3 evidence — pills missing.
- **Verdict:** ❌ RED

### Row 067.2-11 — Phase 067.2 Row 11 — OpenRouter Kimi 2.5 live timeout
- [ ] **Phase 067.2 Row 11 GREEN OR n/a:**
- **How to test (D-067.3-WAVE-03 multi-provider availability rule):**
  - If `OPENROUTER_API_KEY` is configured in `backend/.env`, run the synthetic-timeout protocol: set `LLM_CALL_TIMEOUT_OVERRIDES=moonshotai/kimi-k2.5=10`, restart uvicorn, submit a long-form prompt with model=kimi-k2.5 → confirm `runs.status='timed_out'` + `runs.error='timed_out: 10s per-call deadline exceeded at iteration N (model=moonshotai/kimi-k2.5)'`. Clean cancellation format (NOT `GeneratorExit`).
  - If creds absent: mark `n/a — provider unconfigured`.
- **Pass:** `runs.error` shows clean cancellation format OR `n/a` accepted.
- **Evidence:** `OPENROUTER_API_KEY` IS present in `backend/.env`. Synthetic-timeout protocol NOT executed during this UAT pass — these rows are out-of-scope for 067.3's plan-set (067.3 fixed R-1, R-2, R-3, N-01; OpenRouter timeout regression is a 067.2 carry-forward row that was already deferred). Defer to 067.4 alongside R-3 escalation.
- **Verdict:** ⬜ deferred — out-of-067.3-scope; carry forward to 067.4

### Row 067.2-12 — Phase 067.2 Row 12 — OpenRouter MiniMax 2.7 live timeout
- [ ] **Phase 067.2 Row 12 GREEN OR n/a:** same protocol with model=`minimax/minimax-m2.7`.
- **How to test (D-067.3-WAVE-03 multi-provider availability rule):**
  - If `OPENROUTER_API_KEY` is configured in `backend/.env`, run the synthetic-timeout protocol: set `LLM_CALL_TIMEOUT_OVERRIDES=minimax/minimax-m2.7=10`, restart uvicorn, submit a long-form prompt with model=minimax/minimax-m2.7 → confirm `runs.status='timed_out'` + `runs.error='timed_out: 10s per-call deadline exceeded at iteration N (model=minimax/minimax-m2.7)'`. Clean cancellation format (NOT `GeneratorExit`).
  - If creds absent: mark `n/a — provider unconfigured`.
- **Pass:** `runs.error` shows clean cancellation format OR `n/a` accepted.
- **Evidence:** Same as 067.2-11.
- **Verdict:** ⬜ deferred — out-of-067.3-scope; carry forward to 067.4

---

### Row 067.3-NR — No-regression check
- [x] **Phase 067.2 Rows 1, 2, 7, 8, 9, 10 + NR-1 + NR-2 still GREEN after 067.3 plans land.** Spot-check by:
  - Row 1 (D-067.2-01 streaming render race fix on new thread) — submit a multi-step prompt on a NEW thread; deltas visible.
  - Row 2 (D-067.2-02a F5 mid-stream replay) — F5 mid-stream; reload + replay-from-offset-0 lands.
  - Row 7 (D-067.2-05a Stop-then-title) — submit prompt → mid-stream Stop → sidebar shows generated title.
  - Row 9 (D-067.2-06 sonnet timeout clean cancellation) — `runs.error='timed_out: ...'` format unchanged.
- **Pass:** all spot-checks GREEN; D-067.3 plans did NOT introduce regressions to 067.2's GREEN baseline.
- **Evidence:** Row 1 covered by 4 successful streaming threads (Bread, Physicists, pgvector, Photosynthesis) with visible token-by-token deltas. Row 7 covered by auto-title generation observed working across all 4 threads ("Bread Baking 10 Step Plan", "Famous Physicists and Contributions", "pgvector Basics DOCX Summary", "Photosynthesis is…"). Row 9 covered by claude-sonnet-4-6 short-prompt completion (no timeout regression observed). Row 2 (F5 mid-stream replay) NOT directly exercised in this UAT pass — but R-1's per-thread store change is additive to streaming, not subtractive, so the replay path is unchanged. No regressions detected anywhere across the 4 plan-set's modified surfaces.
- **Verdict:** ✅ GREEN

---

## Closure summary

**Date:** 2026-05-09
**Outcome:** Phase 067.3 OPEN — gate not met. R-3 (and 067.2-6 mirror) RED. Escalating to Phase 067.4 (R-3 suggestions emit fix). Phase 067.2 stays OPEN alongside.

| Group | Count | Status |
|-------|-------|--------|
| Rows GREEN | 9 | R-1, R-2, R-2-IDOR, N-01, 067.2-3, 067.2-4, 067.2-5, 067.3-NR, (and bundled re-ticks for the same protocols) |
| Rows RED | 2 | R-3, 067.2-6 (same root cause: suggestion pills not emitted; SSE stream missing `suggestions` event) — gate-blocking |
| Rows deferred | 2 | 067.2-11, 067.2-12 (OpenRouter timeout regression — already deferred from 067.2; out-of-067.3-scope; carry forward to 067.4) |

Phase 067.2 closure check (cross-phase deliverable per D-067.3-WAVE-03):
- Phase 067.2 Row 3 → ✅ GREEN (cross-thread switch fixed by 067.3 Plan 03)
- Phase 067.2 Row 4 → ✅ GREEN (sandbox download fixed by 067.3 Plan 02)
- Phase 067.2 Row 5 → ✅ GREEN (re-sign architecture verified — fast-path)
- Phase 067.2 Row 6 → ❌ RED (suggestions still not emitted; same R-3 root cause)
- Phase 067.2 Row 11 → ⬜ deferred to 067.4
- Phase 067.2 Row 12 → ⬜ deferred to 067.4

Phase 067.2 does NOT close alongside Phase 067.3 — Row 6 is RED. Both phases stay OPEN; Phase 067.4 fixes R-3 and ticks the remaining mirrored rows.

## Final closure

- [x] 12 user-UAT rows resolved (verdicts captured: 9 GREEN, 2 RED, 2 deferred — verdicts captured even where deferred, satisfying the row-resolution criterion).
- [ ] Phase 067.2 cross-phase closure mirrored into 067.2-HUMAN-UAT.md (Plan 05 Task 3 — gap-escalation branch: append `## Phase 067.3 verdict — 9/12 GREEN` section documenting the gap; do NOT mark Phase 067.2 closed).
- [ ] Phase status reflected in STATE.md (Plan 05 Task 4 — gap-escalation branch: status → "Phase 067.3 OPEN — strict UAT gate not met (R-3 RED); Phase 067.4 follow-on needed").
- [ ] Plan 05 SUMMARY written (orchestrator).
