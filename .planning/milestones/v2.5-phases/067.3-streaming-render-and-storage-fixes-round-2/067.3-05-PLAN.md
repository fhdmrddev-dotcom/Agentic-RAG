---
phase: 067.3
plan: 05
type: execute
wave: 3
depends_on: [067.3-01, 067.3-02, 067.3-03, 067.3-04]
files_modified:
  - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md
  - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md
autonomous: false
requirements: [STREAM-04-correctness-round2]
requirements_addressed: [STREAM-04-correctness-round2]
tags: [uat, closing-gate, multi-phase, chrome-mcp, autonomous-false-mandatory]

must_haves:
  truths:
    - "067.3-HUMAN-UAT.md scoreboard with R-1, R-2, R-3, N-01 evidence rows is created and 12/12 GREEN (or explicit `n/a — provider unconfigured` for unreachable providers per the Phase 067.2 multi-provider availability rule) (D-067.3-WAVE-03)"
    - "Phase 067.2 067.2-HUMAN-UAT.md rows 3, 4, 5, 6 turn GREEN after Plans 01-04 of 067.3 land (D-067.3-WAVE-03)"
    - "Phase 067.2 deferred rows 11, 12 (kimi/minimax live timeout) turn GREEN OR are explicitly accepted as `n/a — provider unconfigured` per the Phase 067.2 multi-provider availability rule (D-067.3-WAVE-03)"
    - "Phase 067.2 GREEN rows 1, 2, 7, 8, 9, 10 + NR-1 + NR-2 still GREEN — no regression"
    - "D-067.3-R2-05 cross-user IDOR Chrome MCP e2e test for sandbox download executed and locked: User B clicking User A's download URL gets toast 'File not found.' (NOT the file)"
    - "Phase 067.2 closes alongside Phase 067.3 once both scoreboards meet the strict 12/12 GREEN gate"
    - "autonomous: false — Phase 067.2 Plan 06 reliability lesson is honored (no DOM-observer proxies, no test-passes-as-acceptance)"
  artifacts:
    - path: ".planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md"
      provides: "Phase 067.3 HUMAN-UAT scoreboard — 12 rows + NR rows with user verdicts"
      contains: "## User UAT Scoreboard"
    - path: ".planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md"
      provides: "Phase 067.2 scoreboard updated — rows 3, 4, 5, 6 + 11, 12 re-ticked"
      contains: "Phase 067.3 closed"
  key_links:
    - from: ".planning/phases/067.3-.../067.3-HUMAN-UAT.md row R-1"
      to: "Phase 067.2 067.2-HUMAN-UAT.md row 3 (D-067.2-02b)"
      via: "shared verification protocol — cross-thread switch"
      pattern: "D-067.2-02b"
    - from: ".planning/phases/067.3-.../067.3-HUMAN-UAT.md row R-2"
      to: "Phase 067.2 067.2-HUMAN-UAT.md row 4 (D-067.2-03a)"
      via: "shared verification protocol — sandbox download"
      pattern: "D-067.2-03a"
---

<objective>
Phase 067.3 closing UAT — re-tick Phase 067.2 deferred + RED rows AND add Phase 067.3 evidence rows for R-1, R-2, R-3, N-01. This is a USER-DRIVEN scoreboard, NOT an automated test pass. Per Phase 067.2 Plan 06 reliability lesson (and CONTEXT.md D-067.3-WAVE-03), `autonomous: false` is **MANDATORY** — the executor pauses after creating the scoreboard and waits for the user to tick GREEN/RED for each row based on lived-experience verification.

Purpose: Close BOTH Phase 067.3 (R-1, R-2, R-3, N-01 evidence) AND Phase 067.2 (the gate that has been blocked since 2026-05-08 by 3 RED rows + 1 HIGH new finding). Strict 12/12 GREEN gate per CONTEXT.md "partial-with-approval REJECTED".

Output: A new `067.3-HUMAN-UAT.md` scoreboard + update to `067.2-HUMAN-UAT.md` with re-ticked verdicts. After the user marks all rows GREEN (or `n/a — provider unconfigured` for unreachable creds), both phases close.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-CONTEXT.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md
@.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-06-SUMMARY.md
@.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-INVESTIGATION-NOTES.md

<interfaces>
<!-- Phase 067.2 deferred / RED rows from 067.2-HUMAN-UAT.md (the source of truth for what 067.3 must close): -->

| Phase 067.2 row | Original verdict | Phase 067.3 plan that fixes | Re-tick after which 067.3 plan |
|-----------------|------------------|------------------------------|--------------------------------|
| Row 3 (D-067.2-02b cross-thread switch) | RED | Plan 03 (R-1) | After Plan 03 lands |
| Row 4 (D-067.2-03a sandbox auth) | RED | Plan 02 (R-2) | After Plan 02 lands |
| Row 5 (D-067.2-03b 90-min wait) | DEFERRED — blocked by Row 4 | Plan 02 (R-2) | After Plan 02 lands; 90-min wait or fast-path test |
| Row 6 (D-067.2-04 suggestions render) | RED | Plan 04 (R-3) | After Plan 04 lands |
| Row 11 (D-067.2-06 kimi 2.5 live timeout) | DEFERRED — creds unavailable | n/a (env-dependent) | If creds present at UAT time, run live; else mark n/a |
| Row 12 (D-067.2-06 minimax 2.7 live timeout) | DEFERRED — creds unavailable | n/a (env-dependent) | If creds present at UAT time, run live; else mark n/a |

<!-- Phase 067.3 evidence rows (NEW): -->

| Phase 067.3 row | Decision | Verifies which plan |
|-----------------|----------|---------------------|
| R-1 | D-067.3-R1-01..08 | Plan 03 |
| R-2 | D-067.3-R2-01..04 | Plan 02 |
| R-2-IDOR | D-067.3-R2-05 (cross-user Chrome MCP e2e) | Plan 02 |
| R-3 | D-067.3-R3-01..04 | Plan 04 |
| N-01 | D-067.3-N01-01..04 | Plan 01 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 067.3-HUMAN-UAT.md scoreboard (5 evidence rows + NR rows + closing summary)</name>
  <files>.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md (full file — exact format, exact frontmatter shape, exact row template, exact closing-summary format. Plan 05 mirrors this format VERBATIM.)
    - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-CONTEXT.md § D-067.3-R1-01..08, § D-067.3-R2-01..05, § D-067.3-R3-01..04, § D-067.3-N01-01..04, § D-067.3-WAVE-03
    - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-04-INVESTIGATION-NOTES.md (Plan 04 output — informs the R-3 row's "How to test" + branch-specific evidence pointer)
    - frontend/src/components/chat/ExecuteCodeBlock.tsx (Plan 02 OutputFileCard — informs R-2 row "How to test" steps)
    - frontend/src/hooks/useMessages.ts (Plan 03 messagesByThread — informs R-1 row's "How to test" cross-thread switch protocol)
    - backend/app/api/threads.py (Plan 01 _resolved_provider — informs N-01 row's repro)
  </read_first>
  <action>
    Per D-067.3-WAVE-03 + CONTEXT.md `### D-067.3-WAVE-01` Wave 3 description:

    Create `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md` mirroring the 067.2-HUMAN-UAT.md format VERBATIM. The frontmatter shape:

    ```yaml
    ---
    phase: 067.3
    plan: 05
    status: pending_user_verdict
    created: {YYYY-MM-DD}
    closure_outcome: not_yet_closed
    green_count: 0
    red_count: 0
    deferred_count: 0
    new_findings_count: 0
    gate: 12/12 user-ticked GREEN required for phase close — partial-with-approval REJECTED per CONTEXT.md (Phase 067.2 reliability lesson)
    ---
    ```

    The body has these sections:

    1. **Header + closure-gate paragraph** (mirror 067.2 line 14-17):
       > **Closure gate:** Phase 067.3 cannot be marked complete until every checkbox below is ticked GREEN by the user (NOT by an automated DOM observer or test). `n/a — provider unconfigured` is allowed ONLY for rows where the provider env is unreachable. Phase 067.2 closes alongside Phase 067.3 once both scoreboards meet the strict 12/12 GREEN gate (D-067.3-WAVE-03).

    2. **Pre-conditions** (mirror 067.2 line 21-24):
       - Plans 01, 02, 03, 04 source changes are landed and the dev frontend at `localhost:5173` serves the new bundle.
       - Backend uvicorn restarted with the new code.
       - Logged in as `fhdmrd@gmail.com` / `123456`.
       - For R-2-IDOR: a SECOND user account (signup a fresh `testuser2@example.com` if not already present) needs to be set up; cross-user navigation requires both accounts.

    3. **User UAT Scoreboard** — 5 Phase 067.3 evidence rows + 6 Phase 067.2 re-tick rows = 11 rows total. Add Row 12 = "Phase 067.2 final closure check" (deferred) for symmetric framing with 067.2's 12-row gate. EXACT row content per the template below — populate the values from the canonical refs:

    ---

    ### Row R-1 — D-067.3-R1-01..08 (Plan 03 — per-thread message store)
    - [ ] **R-1 verified:** Cross-thread switch mid-stream preserves the streamed-into thread's render via the new `messagesByThread` Map.
    - **How to test:**
      1. New thread (Thread A) → submit a multi-step Fahed-Mrad prompt.
      2. While Thread A is streaming (within first ~5s of first delta), click "+ New Chat" → Thread B → submit another multi-step prompt.
      3. While Thread B is streaming, click back to Thread A in the sidebar.
      4. Observe Thread A's content area.
    - **Pass:** Thread A's assistant content is present and (if still mid-run) continues streaming. Switching back to B shows B's content present and continuing. Switching to a completed unrelated thread or other page works at any time during this sequence.
    - **Pass also:** After both runs complete, both threads render their full final assistant content with no missing tokens.
    - **Reference:** Phase 067.2 067.2-HUMAN-UAT.md Row 3 protocol (verbatim) — this row supersedes that one. Re-ticks Phase 067.2 Row 3 GREEN in conjunction.
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row R-2 — D-067.3-R2-01..04 (Plan 02 — sandbox download via JS blob fetch)
    - [ ] **R-2 verified:** Sandbox-output downloads work via real browser anchor clicks.
    - **How to test:**
      1. New thread → submit a docx-generating prompt (e.g. "create a one-page docx summary of pgvector").
      2. Wait for the agent to produce a download link in the chat.
      3. Click the download link.
      4. Observe DevTools Network panel + browser save dialog.
    - **Pass:**
      - Card briefly shows Lucide `Loader2` spinner with `animate-spin` (D-067.3-R2-03 visual gate).
      - Browser save dialog opens; file downloads.
      - DevTools Network shows a 200 (after the 302 redirect to Supabase CDN); NO 401 `{"detail":"Not authenticated"}`.
    - **Failure modes to watch:** if 401, R-2 has regressed; if 404, the file IS missing (separate issue); if 5xx, backend re-sign endpoint failed.
    - **Reference:** Phase 067.2 067.2-HUMAN-UAT.md Row 4 protocol — re-ticked GREEN in conjunction.
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row R-2-IDOR — D-067.3-R2-05 (Plan 02 — cross-user IDOR e2e via Chrome MCP)
    - [ ] **R-2-IDOR verified:** A second authenticated user clicking the first user's download URL gets toast "File not found." (NOT the file).
    - **How to test (Chrome MCP-driven):**
      1. As `fhdmrd@gmail.com`, generate a docx (Row R-2 above produces this). Capture the chat-history URL or copy the rendered `<a href>` to a notes file.
      2. Sign out. Sign in as `testuser2@example.com`. Navigate to a thread (or paste the captured `/sandbox-outputs/{path}` URL into the address bar — the URL fetched by the JS blob helper IS the relative path; navigating directly hits the un-authed anchor path).
      3. Click the captured link OR have a new thread render its own card and click directly.
      4. **Direct-click test:** as testuser2, manually trigger the download flow against the captured URL by opening the React DevTools, finding the OutputFileCard for fhdmrd's file (paste the URL into a temporary `<a>` element), clicking.
    - **Pass:** The toast / inline error reads "File not found." (D-067.3-R2-04 verbatim). NO file downloads. Backend `sandbox_outputs.py:48-67` existence-leak prevention returns 404 to the JS fetch; the helper translates to "File not found.".
    - **Pass also:** DevTools Network panel shows 404 from `/sandbox-outputs/{path}` (NOT 403, NOT 200).
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row R-3 — D-067.3-R3-01..04 (Plan 04 — confidence + suggestions render parity)
    - [ ] **R-3 verified:** Fahed-Mrad search-only prompt produces an assistant message with BOTH a rendered confidence indicator AND a follow-up suggestions block (2-4 clickable pills).
    - **How to test:**
      1. New thread → submit "search for any document about fahed mrad and summarize".
      2. Wait for run completion.
      3. Inspect the assistant message UI.
    - **Pass:**
      - Confidence indicator visible (low/medium/high or numeric).
      - Suggestion pills visible — at least 2, at most 4 clickable example questions below the message.
      - Clicking a pill submits that question as the next user message.
    - **Pass also (clickability):** confirm the prop chain is intact (D-067.3-R3-03 Task 1d) by clicking the pill and seeing the question submit.
    - **Reference:** Phase 067.2 067.2-HUMAN-UAT.md Row 6 protocol — re-ticked GREEN in conjunction. 067.3-04-INVESTIGATION-NOTES.md captures the branch the executor took (2a/2b/2c/2d/no-op).
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row N-01 — D-067.3-N01-01..04 (Plan 01 — model→provider router fix)
    - [ ] **N-01 verified:** Submitting a request with `model="claude-sonnet-4-6"` and `user_settings.active_provider="openai"` (no explicit `body.provider`) routes through the Anthropic SDK and succeeds — NOT 404 from OpenAI SDK.
    - **How to test:**
      1. Visit Settings page → ensure default provider is OpenAI (or confirm via `cat ~/.config/agentic-rag/user_settings.json` or the equivalent settings store path).
      2. New thread → in the model dropdown, select `claude-sonnet-4-6` WITHOUT changing the provider dropdown (leave it on OpenAI).
      3. Submit any non-trivial prompt.
      4. Observe response + DevTools Network panel + the `runs` Postgres row for this run_id.
    - **Pass:**
      - Response streams successfully (Anthropic SDK route).
      - `runs.provider="anthropic"` (NOT "openai"). Verify via Supabase SQL editor: `SELECT run_id, model, provider, error FROM runs WHERE run_id='{captured_run_id}'`.
      - NO 404 / `model_not_found` error.
    - **Pass also (regression):** Submit ANOTHER prompt with `model="moonshotai/kimi-k2.5"` (still OpenAI default active_provider). `runs.provider="openrouter"`.
    - **Reference:** Repro id `6eab949f-78da-4ea4-ac01-f04b16c9be7d` (the bug's canonical repro from D-067.3-N01-03).
    - **Verdict:** ⬜ GREEN ⬜ RED

    ---

    ### Row 067.2-3 — Phase 067.2 Row 3 re-tick
    - [ ] **Phase 067.2 Row 3 GREEN-after-067.3-Plan-03:** Repeat the cross-thread switch protocol; mark Row 3 in 067.2-HUMAN-UAT.md GREEN.
    - **How to test:** identical to Row R-1 above (Plan 05 Task 2 will update 067.2-HUMAN-UAT.md to reflect this).
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 067.2-4 — Phase 067.2 Row 4 re-tick
    - [ ] **Phase 067.2 Row 4 GREEN-after-067.3-Plan-02:** Repeat the docx download protocol; mark Row 4 in 067.2-HUMAN-UAT.md GREEN.
    - **How to test:** identical to Row R-2 above.
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 067.2-5 — Phase 067.2 Row 5 re-tick (90-min wait or fast-path)
    - [ ] **Phase 067.2 Row 5 GREEN-after-067.3-Plan-02 — D-067.2-03b long-TTL re-sign verified.**
    - **How to test:**
      - **Slow path:** complete Row 067.2-4 → wait 90+ minutes → click the same download link → confirm download still works (the new `/sandbox-outputs/{path}` endpoint re-signs on demand).
      - **Fast path (developer-friendly):** restart Supabase Storage to invalidate the legacy 1h-expiry signed URL → click the link → re-sign endpoint should still produce a fresh redirect.
    - **Pass:** download succeeds; no `InvalidJWT` error.
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 067.2-6 — Phase 067.2 Row 6 re-tick
    - [ ] **Phase 067.2 Row 6 GREEN-after-067.3-Plan-04:** identical to Row R-3 above.
    - **Verdict:** ⬜ GREEN ⬜ RED

    ### Row 067.2-11 — Phase 067.2 Row 11 — OpenRouter Kimi 2.5 live timeout
    - [ ] **Phase 067.2 Row 11 GREEN OR n/a:**
    - **How to test (D-067.3-WAVE-03 multi-provider availability rule):**
      - If `OPENROUTER_API_KEY` is configured in `backend/.env`, run the synthetic-timeout protocol: set `LLM_CALL_TIMEOUT_OVERRIDES=moonshotai/kimi-k2.5=10`, restart uvicorn, submit a long-form prompt with model=kimi-k2.5 → confirm `runs.status='timed_out'` + `runs.error='timed_out: 10s per-call deadline exceeded at iteration N (model=moonshotai/kimi-k2.5)'`. Clean cancellation format (NOT `GeneratorExit`).
      - If creds absent: mark `n/a — provider unconfigured`.
    - **Pass:** `runs.error` shows clean cancellation format OR `n/a` accepted.
    - **Verdict:** ⬜ GREEN ⬜ RED ⬜ n/a — provider unconfigured

    ### Row 067.2-12 — Phase 067.2 Row 12 — OpenRouter MiniMax 2.7 live timeout
    - [ ] **Phase 067.2 Row 12 GREEN OR n/a:** same protocol with model=`minimax/minimax-m2.7`.
    - **Verdict:** ⬜ GREEN ⬜ RED ⬜ n/a — provider unconfigured

    ---

    ### Row 067.3-NR — No-regression check
    - [ ] **Phase 067.2 Rows 1, 2, 7, 8, 9, 10 + NR-1 + NR-2 still GREEN after 067.3 plans land.** Spot-check by:
      - Row 1 (D-067.2-01 streaming render race fix on new thread) — submit a multi-step prompt on a NEW thread; deltas visible.
      - Row 2 (D-067.2-02a F5 mid-stream replay) — F5 mid-stream; reload + replay-from-offset-0 lands.
      - Row 7 (D-067.2-05a Stop-then-title) — submit prompt → mid-stream Stop → sidebar shows generated title.
      - Row 9 (D-067.2-06 sonnet timeout clean cancellation) — `runs.error='timed_out: ...'` format unchanged.
    - **Pass:** all spot-checks GREEN; D-067.3 plans did NOT introduce regressions to 067.2's GREEN baseline.
    - **Verdict:** ⬜ GREEN ⬜ RED

    ---

    4. **Closure summary section** (mirror 067.2 line 157-176, populated AFTER user ticks all rows):
       ```markdown
       ## Closure summary

       **Date:** {YYYY-MM-DD}
       **Outcome:** {Phase 067.3 ships AND Phase 067.2 closes alongside | Phase 067.3 OPEN — gate not met}

       | Group | Count | Status |
       |-------|-------|--------|
       | Rows GREEN | {N} | {list} |
       | Rows RED | {N} | {list — gate-blocking} |
       | Rows n/a | {N} | {list — for kimi/minimax if creds absent} |

       Phase 067.2 closure check (cross-phase deliverable per D-067.3-WAVE-03):
       - Phase 067.2 Row 3 → {GREEN/RED}
       - Phase 067.2 Row 4 → {GREEN/RED}
       - Phase 067.2 Row 5 → {GREEN/RED}
       - Phase 067.2 Row 6 → {GREEN/RED}
       - Phase 067.2 Row 11 → {GREEN/RED/n/a}
       - Phase 067.2 Row 12 → {GREEN/RED/n/a}

       If all Phase 067.2 deferred + RED rows GREEN/n/a → Phase 067.2 closes alongside Phase 067.3.
       ```

    5. **DO NOT pre-fill verdicts.** Plan 05 is `autonomous: false` — the executor authors the scoreboard, then PAUSES with the structured handoff to the user (see Task 3 below). The user runs each row and ticks GREEN/RED. The executor returns AFTER the user has captured all verdicts.
  </action>
  <verify>
    <automated>test -f .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md &amp;&amp; grep -c "Verdict:" .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md | grep -E "^(1[0-9]|[0-9])$"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md` exists.
    - `grep -c "^### Row " 067.3-HUMAN-UAT.md` returns at least 11 (5 R-rows + 6 067.2-re-tick rows + 1 NR row = 12 rows total minimum).
    - `grep -c "Verdict:" 067.3-HUMAN-UAT.md` returns ≥ 11 (every row has a Verdict line).
    - `grep -c "D-067.3-R1\|D-067.3-R2\|D-067.3-R3\|D-067.3-N01\|D-067.3-WAVE" 067.3-HUMAN-UAT.md` ≥ 5 (decision-ID citation in row headers).
    - Frontmatter has `status: pending_user_verdict` + `closure_outcome: not_yet_closed` (so `gsd-sdk query frontmatter.validate` flags it as in-progress, not closed).
    - Frontmatter `gate` field includes the exact substring "12/12 user-ticked GREEN required" (D-067.3-WAVE-03 strict gate).
    - The R-2-IDOR row references D-067.3-R2-05 explicitly and includes the `testuser2@example.com` second-account setup step.
    - The N-01 row references the canonical repro run_id `6eab949f-78da-4ea4-ac01-f04b16c9be7d`.
    - The Closure summary section TEMPLATE is present (with `{YYYY-MM-DD}` / `{N}` placeholders) — to be filled in by the user after verdicts are captured.
  </acceptance_criteria>
  <done>067.3-HUMAN-UAT.md scoreboard authored with all rows + verdict checkboxes + How-to-test protocols + decision-ID citations. No verdicts pre-ticked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: USER UAT — execute scoreboard rows + capture verdicts</name>
  <what-built>
    Phase 067.3 closing UAT scoreboard (Task 1) + the four code-changes from Plans 01-04:
    - Plan 01 — N-01 router fix (backend threads.py:949 resolution order)
    - Plan 02 — R-2 sandbox download JS blob helper
    - Plan 03 — R-1 per-thread store (messagesByThread)
    - Plan 04 — R-3 confidence + suggestions render fix (one of branches 2a/2b/2c/2d/no-op)
  </what-built>
  <how-to-verify>
    Open `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md` and follow it row-by-row:

    1. Pre-conditions (frontmatter pre-conditions section): confirm dev frontend at localhost:5173, backend uvicorn running with all 4 plans landed, sign in as fhdmrd@gmail.com, set up testuser2@example.com if not already present.

    2. Run each row's "How to test" steps — use Chrome DevTools MCP for browser automation per the project's `feedback_chrome_mcp_testing.md` rule (drive a real browser, NOT DOM-observer proxies). For each row, mark `⬜ GREEN` or `⬜ RED` (or `⬜ n/a — provider unconfigured` for rows 11/12 if kimi/minimax creds absent).

    3. Specific row protocols:
       - **Row R-1:** New Thread A multi-step prompt → mid-stream Thread B multi-step → back to A → confirm content present and continuing.
       - **Row R-2:** Generate docx → click → confirm download (no 401).
       - **Row R-2-IDOR:** Sign out fhdmrd → sign in testuser2 → click captured fhdmrd download URL → confirm "File not found." toast (NOT the file).
       - **Row R-3:** Fahed-Mrad search-only prompt → confirm BOTH confidence + suggestion pills visible.
       - **Row N-01:** model=claude-sonnet-4-6 + active_provider=openai (no body.provider) → response streams + `runs.provider='anthropic'`.
       - **Rows 067.2-3, 067.2-4, 067.2-5, 067.2-6:** repeat R-1, R-2, R-2 long-TTL, R-3 protocols and tick the corresponding 067.2 row.
       - **Rows 067.2-11, 067.2-12:** check `backend/.env` for `OPENROUTER_API_KEY`. If present, run synthetic-timeout protocol with the OpenRouter model. If absent, mark n/a.
       - **Row 067.3-NR:** spot-check 067.2 GREEN baseline (Rows 1, 2, 7, 9) — ensure no regression.

    4. After all rows are ticked, fill in the "Closure summary" section with:
       - GREEN/RED/n/a counts.
       - The phase 067.2 cross-phase closure verdict (whether all 067.2 deferred + RED rows are now GREEN or n/a).

    5. Type `approved` to resume the executor — the executor will then proceed to Task 3 (update 067.2-HUMAN-UAT.md with the cross-phase verdicts) and Task 4 (close the phase OR escalate gaps).

    **DO NOT auto-tick GREEN. Use a real browser. Phase 067.2 Plan 06 reliability lesson is the reason this row is autonomous=false.**
  </how-to-verify>
  <resume-signal>Type `approved` once all UAT rows are ticked AND the Closure summary section is filled in. If any row is RED, type `gaps` instead — the executor will escalate to a follow-on phase planning step (per CONTEXT.md "partial-with-approval REJECTED").</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Mirror Phase 067.3 verdicts back into 067.2-HUMAN-UAT.md (cross-phase closure)</name>
  <files>.planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md (Task 2 output — user-ticked verdicts)
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md (the file being updated)
    - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-CONTEXT.md § D-067.3-WAVE-03 (cross-phase closure rule)
  </read_first>
  <action>
    Per D-067.3-WAVE-03:

    For each Phase 067.2 row that 067.3 closes, update its checkbox + Verdict + Evidence in `067.2-HUMAN-UAT.md`:

    Step 1 — Read 067.3-HUMAN-UAT.md (Task 2 user verdicts) and extract the cross-phase verdicts:
    - 067.3 Row R-1 verdict → maps to 067.2 Row 3
    - 067.3 Row R-2 verdict → maps to 067.2 Row 4
    - 067.3 Row 067.2-5 verdict → maps to 067.2 Row 5
    - 067.3 Row R-3 verdict → maps to 067.2 Row 6
    - 067.3 Row 067.2-11 verdict → maps to 067.2 Row 11
    - 067.3 Row 067.2-12 verdict → maps to 067.2 Row 12

    Step 2 — In `067.2-HUMAN-UAT.md`, for each row updated:
    - Flip the leading checkbox `[ ]` → `[x]`.
    - Update the `**Verdict:**` line to `✅ GREEN` (or `⬜ n/a — provider unconfigured` for 11/12 if applicable).
    - Append an `**Evidence (Phase 067.3 closure):**` line under the existing Evidence: `Closed by Phase 067.3 Plan {NN}; verified {YYYY-MM-DD} via Chrome MCP UAT — see 067.3-HUMAN-UAT.md Row {RID}.`
    - Update the row's failure-mode block to a closure note: `**Closure note:** Resolved by Phase 067.3 Plan {NN} ({brief description}). See {067.3-NN-SUMMARY.md} for implementation detail.`

    Step 3 — Update the frontmatter at the top of `067.2-HUMAN-UAT.md`:
    - `status:` → `closed` (if all 067.2 rows now GREEN/n/a) OR remain `blocked` (if any 067.3 row was RED, blocking the cross-phase closure).
    - `closure_outcome:` → `closed_via_phase_067.3` (if closing) OR remain `not_closed`.
    - `green_count:` → updated count.
    - `red_count:` → updated count.
    - `deferred_count:` → updated count.

    Step 4 — Update the **Closure summary** at the bottom of `067.2-HUMAN-UAT.md` (line 157-176):
    - Replace the `Outcome:` line: `Phase 067.2 UAT does NOT meet the strict closure gate. Phase stays OPEN.` → `Phase 067.2 UAT meets the strict closure gate via Phase 067.3 cross-phase closure. Phase CLOSED on {YYYY-MM-DD}.`
    - Update the table: GREEN count rises by however many rows turned GREEN; RED count drops to 0 (or remains > 0 if 067.3 didn't close all).
    - Append a final paragraph: `Phase 067.3 ships alongside this closure (D-067.3-WAVE-03). See 067.3-HUMAN-UAT.md for the cross-phase evidence rows R-1, R-2, R-2-IDOR, R-3, N-01.`

    Step 5 — DO NOT remove or rewrite ANY of 067.2-HUMAN-UAT.md's existing rows; only update the checkbox + Verdict + add the closure note. Preserve the failure-mode investigation history as audit trail.

    Step 6 — If ANY 067.3 row was RED (gate not met):
    - Skip the closure update.
    - Instead, append a new section to 067.2-HUMAN-UAT.md (after the New findings section) titled `## Phase 067.3 verdict — {GREEN row count}/{12} GREEN`. Document which 067.3 rows were RED.
    - Both phases stay open. Plan 05 SUMMARY documents the gap.
  </action>
  <verify>
    <automated>test -f .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md &amp;&amp; grep -c "Phase 067.3" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md | grep -vE "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Closed by Phase 067.3" .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md` ≥ 4 (rows 3, 4, 5, 6 closed by 067.3) — UNLESS Task 2 captured RED, in which case fewer are closed.
    - For each row that turned GREEN: the leading `[ ]` is now `[x]` AND the `**Verdict:**` line shows `✅ GREEN` (not `⬜ GREEN`).
    - 067.2-HUMAN-UAT.md frontmatter `status:` reflects the closure outcome (`closed` if gate met; `blocked` if any 067.3 row was RED).
    - 067.2-HUMAN-UAT.md Closure summary updated with the cross-phase closure paragraph.
    - The pre-existing failure-mode notes (e.g., "User-reported failure: ...") are PRESERVED as audit trail (NOT deleted).
    - If gate not met: a `## Phase 067.3 verdict` section documenting the gap is appended.
  </acceptance_criteria>
  <done>067.2-HUMAN-UAT.md updated with cross-phase verdicts; both phases close together if gate met OR both stay open with documented gap if not.</done>
</task>

<task type="auto">
  <name>Task 4: Phase closure (or gap escalation) — STATE.md + final SUMMARY</name>
  <files>.planning/STATE.md, .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-05-SUMMARY.md</files>
  <read_first>
    - .planning/STATE.md (current Phase 067.2 OPEN status block)
    - .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md (Task 2 verdicts)
    - .planning/phases/067.2-streaming-render-and-storage-fixes/067.2-HUMAN-UAT.md (Task 3 update)
  </read_first>
  <action>
    Per D-067.3-WAVE-03:

    **If both phases close (all rows GREEN or n/a):**

    Step 1 — Update STATE.md:
      - `status:` → `Phase 067.3 CLOSED — strict UAT gate met (Phase 067.2 closed alongside)`
      - `last_updated:` → today's ISO timestamp
      - `last_activity:` → `Phase 067.3 + 067.2 cross-phase closure via Plan 05 UAT — N+M GREEN`
      - `## Current Position` section: bump completed_plans counter; update Phase line to next phase or end-of-milestone.
      - `## Recent Completed Phases`: add `### Phase 067.3: Streaming Render & Storage Fixes — Round 2 (Complete YYYY-MM-DD)` block with the closing summary truths from must_haves.
      - Remove the Phase 067.2 OPEN block; replace with a closed-summary entry.

    Step 2 — Write `067.3-05-SUMMARY.md` documenting:
      - Final UAT counts (GREEN/RED/n/a).
      - Cross-phase closure note for 067.2.
      - Pointer to 067.3-HUMAN-UAT.md for full evidence.

    **If gate not met (any row RED that isn't n/a-eligible):**

    Step 1 — Update STATE.md:
      - `status:` → `Phase 067.3 OPEN — strict UAT gate not met ({N} RED rows); follow-on phase needed`
      - Note the specific RED rows + their captured evidence.

    Step 2 — Write `067.3-05-SUMMARY.md` documenting:
      - Which rows were RED + why.
      - Phase 067.4 escalation recommendation (sub-phase or follow-on phase number TBD by orchestrator).
      - Pointer to 067.3-HUMAN-UAT.md.

    Step 3 — Append a new entry to STATE.md `Blockers (escalated to Phase 067.4)` mirroring the format used for the 067.2 → 067.3 escalation.
  </action>
  <verify>
    <automated>grep -c "Phase 067.3" .planning/STATE.md &amp;&amp; test -f .planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md `status:` field reflects the actual phase outcome (CLOSED with cross-phase 067.2 closure, or OPEN with RED row count).
    - `067.3-05-SUMMARY.md` exists with final UAT counts + closure outcome + pointers to 067.3-HUMAN-UAT.md.
    - If CLOSED: STATE.md `## Recent Completed Phases` has a new "Phase 067.3" entry; Phase 067.2 OPEN block is removed/replaced.
    - If OPEN: STATE.md `Blockers` section appends the new 067.4 escalation.
    - Both 067.3-HUMAN-UAT.md (Task 1+2) and 067.2-HUMAN-UAT.md (Task 3) are referenced from the SUMMARY.
  </acceptance_criteria>
  <done>STATE.md reflects accurate phase outcome; SUMMARY documents the closure-or-escalation; future planning has the audit trail it needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (browser) → UAT scoreboard | Plan 05 is documentation + verdict capture. No new code surface. |
| Chrome MCP browser → localhost:5173 | Existing dev infrastructure. |
| testuser2@example.com cross-user check | One-time test account creation; NOT a multi-tenant feature surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067.3-05-01 | I (Information Disclosure) | testuser2 cross-user IDOR test exposes fhdmrd's file path in test notes | accept | The path-segment fence is internal (random UUID); no PII in the path. Test note retention is local to dev environment. |
| T-067.3-05-02 | S (Spoofing) | UAT verdicts are user-self-reported, not auditable | accept | This IS the design (Phase 067.2 reliability lesson — DOM-observer proxies are NOT acceptance). Cross-checked by run_id capture in Evidence fields; future audit reconstructs from Postgres + LangSmith. |
| T-067.3-05-03 | T (Tampering) | A user could fraudulently mark RED rows as GREEN | accept | Solo developer + Claude workflow (no team); single-user-trust assumption. STATE.md + SUMMARY commits leave a git audit trail. |

ASVS L1 mapping: V8.1.1 (verification of security controls — the cross-user IDOR row in 067.3 is an explicit ASVS-aligned check; D-067.3-R2-05).
</threat_model>

<verification>
- File `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md` exists with 11+ rows + Closure summary template.
- Task 2 checkpoint pauses for user; user runs Chrome MCP UAT; types `approved` or `gaps`.
- Task 3 mirrors verdicts to 067.2-HUMAN-UAT.md.
- Task 4 STATE.md update reflects the actual outcome.
- Both `067.3-HUMAN-UAT.md` and `067.2-HUMAN-UAT.md` show 12/12 GREEN (or explicit n/a) per the strict gate, OR document the gap with a follow-on escalation plan.
</verification>

<success_criteria>
1. 067.3-HUMAN-UAT.md scoreboard authored with all required rows + decision-ID citations + How-to-test protocols.
2. User-driven UAT executed via Chrome MCP (autonomous: false honored).
3. 067.2-HUMAN-UAT.md updated with cross-phase verdicts; both phases close together if gate met.
4. STATE.md + 067.3-05-SUMMARY.md reflect the accurate closure-or-escalation outcome.
5. Phase 067.2's strict 12/12 GREEN gate is met by re-running deferred + RED rows (D-067.3-WAVE-03).
</success_criteria>

<output>
After completion: `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-05-SUMMARY.md` is written; STATE.md is updated; both UAT scoreboards reflect their final verdicts.
</output>
