---
id: 067.4-02
plan_id: 067.4-02
phase: 067.4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/__tests__/hooks/useMessages.test.ts
  - frontend/src/hooks/useMessages.ts
autonomous: true
requirements:
  - STREAM-04-correctness-round3
requirements_addressed:
  - STREAM-04-correctness-round3
must_haves:
  truths:
    - "On a sandbox-invoking prompt run on the active thread, the spinner-to-result transition fires WITHOUT thread navigation (R-4 fix)."
    - "Cross-thread switch mid-stream still preserves both threads' renders (R-1 protection — no regression)."
    - "If the runtime repro does NOT reproduce the freeze (static audit shows all 21 factory handlers ARE bucket-targeted post-R-1), the regression test (`R-4 active-thread tool-stage`) ships ALONE as cheap insurance — see orchestrator-resolved decision #4."
    - "Plan 02 commits BEFORE Plan 03 begins — sequential within Wave 1 to avoid `useMessages.ts` add/add merge conflicts (per D-067.4-WAVE-01)."
  artifacts:
    - path: "frontend/src/__tests__/hooks/useMessages.test.ts"
      provides: "vitest hook test with R-4 active-thread tool-stage + R-1 protection cross-thread switch tests"
      exports: ["describe('useMessages — R-4 active-thread tool-stage')"]
      contains: "subscribeToRun"
    - path: "frontend/src/hooks/useMessages.ts"
      provides: "Surgical patches IF runtime repro confirms a missed bucket write; otherwise UNCHANGED (regression test ships alone)"
      contains: "setMessagesForThread"
  key_links:
    - from: "frontend/src/hooks/useMessages.ts factory `makeStreamCallbacks`"
      to: "messagesByThread.set(threadId, ...) via setMessagesForThread(threadId, updater)"
      via: "ThreadBoundSetMessages parameter routing"
      pattern: "setMessagesForThread\\(threadId"
    - from: "frontend/src/__tests__/hooks/useMessages.test.ts"
      to: "@/hooks/useMessages and @/lib/api (mocked)"
      via: "vi.hoisted + vi.mock + renderHook + act + waitFor"
      pattern: "vi\\.mock\\(\"@/lib/api\""
---

<objective>
Close R-4 (UAT-discovered, NEW): active-thread UI freezes at tool-execution stage; refetch on thread-switch repairs.

Two-step approach per D-067.4-R4-01 (surgical handler-by-handler) AND orchestrator-resolved decision #4 (ship regression test alone if non-reproducible):

1. **Author Wave 0 vitest test (`frontend/src/__tests__/hooks/useMessages.test.ts`)** — covers BOTH:
   - R-4 active-thread tool-stage repro: simulates the canonical sandbox SSE sequence (`tool_preparing` → `tool_start` → `code_stdout` → `code_execution_complete` → `tool_end` → `done` → terminal) WITHOUT a `setViewingThread` switch. Asserts `tool_calls[0].status === "done"` on the active thread's bucket.
   - R-1 protection: cross-thread switch mid-stream — Thread A streams, switch to Thread B, callbacks fire on A, switch back to A, assert A's content preserved.

2. **Runtime repro + surgical patch (Chrome MCP).** Per RESEARCH.md § 2.2, the static audit found **all 21 in-factory handlers ARE bucket-targeted post-R-1**. So the bug (if reproducible) lives in OUTSIDE-factory write paths or ref-mirror lag. Three runtime steps:
   - Run `git diff 19bedcd..4a70efa -- frontend/src/hooks/useMessages.ts` and inspect the 21 handler ports + 6 outside-factory write sites + 4 ref-mirror sites.
   - Submit `"Use the code execution tool to print numbers 1 to 5"` on a fresh thread via Chrome MCP at http://localhost:5173/ (login `fhdmrd@gmail.com` / `123456`); wait for spinner WITHOUT thread navigation; observe whether spinner-to-result transition fires.
   - **If freeze repros:** locate the missed handler/ref-mirror site (likely `messagesByThreadRef` lag at lines 384-387 OR `messagesRef` lag at lines 445-450 OR `reconcile` mid-stream re-attach at lines 910-1106); apply per-thread-bucket-write pattern surgically (mirror `onDelta` shape from lines 75-81 OR `onConfidence` from lines 316-323).
   - **If freeze does NOT reproduce:** ship the regression test ALONE (Plan 02 commit shape becomes test-only). Capture screenshot of original 067.3 UAT symptom in plan summary; flag as "intermittent — under observation". Per orchestrator-resolved decision #4, Plan 02 must NOT block on root-cause confirmation.

3. **Spot-check R-1 cross-thread switch protocol** alongside R-4 to confirm no regression.

Plan 03 cannot start until Plan 02 commits — sequential within Wave 1 (D-067.4-WAVE-01).

Purpose: Close the R-4 freeze whether it reproduces deterministically or not; lock the regression cheaply via vitest test that future refactors must not break.

Output: vitest test file (NEW); IF runtime repro fires the freeze, also surgical `useMessages.ts` patch.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-CONTEXT.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-PATTERNS.md
@.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-VALIDATION.md
@frontend/src/hooks/useMessages.ts
@frontend/src/__tests__/hooks/useDocuments.test.ts

<interfaces>
<!-- Key contracts the executor needs verbatim — extracted from PATTERNS.md and RESEARCH.md. -->

From `frontend/src/hooks/useMessages.ts:75-81` (canonical per-thread-bucket-write pattern — `onDelta` shape):
```typescript
onDelta: (delta) => {
  setMessages((prev) =>
    prev.map((m) =>
      m.id === assistantId ? { ...m, isPlanning: false, content: m.content + delta } : m,
    ),
  )
},
```

From `frontend/src/hooks/useMessages.ts:760-764` and `:1013-1016` (factory binding shape — DO NOT change):
```typescript
const callbacks: StreamCallbacks = makeStreamCallbacks({
  assistantId,
  threadId,
  onTitleUpdate,
  setMessages: (updater) => setMessagesForThread(threadId, updater),
  setFallbackNotice,
})
```

From `frontend/src/__tests__/hooks/useDocuments.test.ts:1-51` (vi.hoisted + vi.mock pattern — verbatim copy for new test):
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"
import { renderHook, waitFor, act } from "@testing-library/react"

const { mockPostMessage, mockSubscribeToRun, mockGetMessages, mockCancelRun } = vi.hoisted(() => ({
  mockPostMessage: vi.fn(),
  mockSubscribeToRun: vi.fn(),
  mockGetMessages: vi.fn(),
  mockCancelRun: vi.fn(),
}))

vi.mock("@/lib/api", () => ({
  postMessage: mockPostMessage,
  subscribeToRun: mockSubscribeToRun,
  getMessages: mockGetMessages,
  cancelRun: mockCancelRun,
}))

vi.mock("@/lib/supabase", () => ({
  supabase: {
    auth: {
      getSession: vi.fn().mockResolvedValue({
        data: { session: { user: { id: "user-1" }, access_token: "token" } },
      }),
    },
    channel: vi.fn(),
    removeChannel: vi.fn(),
  },
}))

import { useMessages } from "@/hooks/useMessages"
```

From RESEARCH.md § 2.2: the 21 handlers in `makeStreamCallbacks` factory + their target line numbers:
- `onDelta` 75-81, `onDone` 82-87, `onTerminal` 88-90, `onToolPreparing` 94-114, `onToolStart` 116-158, `onToolEnd` 160-172, `onSubAgentStart` 174-182, `onSubAgentDelta` 184-191, `onSubAgentDone` 193-200, `onSkillActivated` 204-220, `onSkillLoaded` 228-253, `onCodeExecutionStart` 255, `onCodeStdout` 257-269, `onCodeStderr` 271-283, `onCodeExecutionComplete` 285-302, `onSources` 304-308, `onCitations` 310-314, `onConfidence` 316-323, `onSuggestions` 325-330, `onPlanning` 331-336, `onIterationStart` 337-346, `onFallbackModel` 348-351

Outside-factory write paths (RESEARCH.md § 2.2 — likely R-4 culprits if freeze repros):
- `sendMessage` finally block 834-894
- `sendMessage` catch block 822-833
- `stopStreaming` callback 452-470
- `reconcile` 910-1106
- `messagesByThreadRef` mirror 384-387
- `messagesRef` mirror 445-450
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Author vitest test for R-4 active-thread tool-stage + R-1 protection cross-thread switch</name>
  <files>frontend/src/__tests__/hooks/useMessages.test.ts</files>
  <read_first>
    frontend/src/__tests__/hooks/useDocuments.test.ts (analog — vi.hoisted + vi.mock pattern, lines 1-51 verbatim);
    frontend/src/hooks/useMessages.ts (full file — to understand callback signatures, factory shape at line ~58, sendMessage at line ~720, setViewingThread reference);
    frontend/src/lib/api.ts (StreamCallbacks interface lines 184-216 — to know exact callback signatures);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-PATTERNS.md (the verbatim "useMessages.test.ts (NEW)" code excerpts including `makeSseRecorder` helper);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-CONTEXT.md (D-067.4-R4-01 verbatim);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-VALIDATION.md (Per-Task Verification Map for Plan 02 rows)
  </read_first>
  <behavior>
    - **Test 1 — `it("clears spinner on tool_end WITHOUT thread navigation (R-4)")`:** Sets up SSE recorder via `mockSubscribeToRun.mockImplementation` capturing the `StreamCallbacks` argument. Renders `useMessages()`; sets viewing thread to `"thread-A"`; calls `sendMessage("run code")`. Awaits `subscribeToRun` to be invoked, retrieves captured callbacks, then synchronously fires the canonical sandbox SSE sequence in one `act()`:
      ```
      onToolPreparing("execute_code", 0)
      onToolStart("execute_code", { code: "print(1)" })
      onCodeStdout("1")
      onCodeExecutionComplete(0, 100, [])
      onToolEnd("execute_code")
      onDone()
      onTerminal("done")
      ```
      Asserts (without any `setViewingThread` switch): `result.current.messages.find(m => m.role === "assistant")?.tool_calls?.[0]?.status === "done"`.
    - **Test 2 — `it("preserves both threads' renders during cross-thread switch (R-1 protection)")`:** Starts stream on Thread A; switches to Thread B; fires `cbA.onDelta("partial response from A")` + `cbA.onToolPreparing` + `cbA.onToolEnd`; switches back to A; asserts `result.current.messages.find(m => m.role === "assistant")?.content` contains `"partial response from A"`.
    - **Helper `makeSseRecorder()`** captures `StreamCallbacks` via `mockSubscribeToRun.mockImplementation((runId, since, callbacks, signal) => { captured = callbacks; return new Promise(() => {}) })` and returns a getter for the captured callbacks.
  </behavior>
  <action>
    Create `frontend/src/__tests__/hooks/useMessages.test.ts` from scratch. Use the verbatim code excerpts from `067.4-PATTERNS.md` "useMessages.test.ts (NEW)" section as the starting body. Specifically:

    1. Top-of-file: paste the imports + `vi.hoisted` + `vi.mock` blocks verbatim from PATTERNS.md.
    2. Add additional `vi.hoisted` mock for any other `@/lib/api` exports referenced by `useMessages.ts` (cross-reference `frontend/src/lib/api.ts` exports the hook imports — minimum: `postMessage`, `subscribeToRun`, `getMessages`, `cancelRun`; add others as needed to make `useMessages.ts` import-resolvable in the test environment).
    3. Define `makeSseRecorder()` helper exactly as PATTERNS.md shows.
    4. Define `describe("useMessages — R-4 active-thread tool-stage", () => { ... })` block containing **Test 1** and **Test 2** (verbatim PATTERNS.md bodies).
    5. Mock `mockGetMessages.mockResolvedValue([])` and `mockPostMessage.mockResolvedValue({ run_id: "run-A", user_message_id: "user-msg-1" })` in `beforeEach`.
    6. Quote D-067.4-R4-01 in the test docstring of Test 1 AND Test 2.

    DO NOT add Test 3 (R-5 additive guard) here — that belongs to Plan 03 (extends this same file sequentially per D-067.4-WAVE-01).

    Commit message: `test(067.4-02): R-4 — Wave 0 vitest test for active-thread tool-stage + R-1 protection`
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/hooks/useMessages.test.ts 2>&1 | tee /tmp/p2-t1.txt; grep -E "Tests|passed|failed|skipped|FAIL|PASS" /tmp/p2-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f frontend/src/__tests__/hooks/useMessages.test.ts` exits 0
    - `grep -nF "useMessages — R-4 active-thread tool-stage" frontend/src/__tests__/hooks/useMessages.test.ts` exits 0
    - `grep -nF "clears spinner on tool_end WITHOUT thread navigation (R-4)" frontend/src/__tests__/hooks/useMessages.test.ts` exits 0
    - `grep -nF "preserves both threads' renders during cross-thread switch (R-1 protection)" frontend/src/__tests__/hooks/useMessages.test.ts` exits 0
    - `grep -nF "vi.mock(\"@/lib/api\"" frontend/src/__tests__/hooks/useMessages.test.ts` exits 0
    - `grep -nF "subscribeToRun" frontend/src/__tests__/hooks/useMessages.test.ts` exits 0
    - `grep -nF "D-067.4-R4-01" frontend/src/__tests__/hooks/useMessages.test.ts` exits 0
    - `grep -cE "^\s*it\(" frontend/src/__tests__/hooks/useMessages.test.ts` returns at least `2`
    - `cd frontend && npx vitest run src/__tests__/hooks/useMessages.test.ts` exits 0 (both tests PASS — note: Test 1 may pass today because static audit found all 21 handlers ARE bucket-targeted; that's fine, the test guards against future regression. If it fails, that's the runtime repro we're hunting in Task 2.)
    - No `--watch` flag in any verify command.
  </acceptance_criteria>
  <done>
    Two test functions in new vitest file; commit `test(067.4-02): R-4 — Wave 0 vitest test for active-thread tool-stage + R-1 protection`. Test results recorded for Task 2 routing decision.
  </done>
</task>

<task type="auto">
  <name>Task 2: Runtime repro + surgical patch (or ship-regression-test-alone branch per orchestrator-resolved decision #4)</name>
  <files>frontend/src/hooks/useMessages.ts</files>
  <read_first>
    frontend/src/hooks/useMessages.ts (full file);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-RESEARCH.md (§ 2.2 R-4 — the 21-handler static audit table, the 6 outside-factory write paths, the 4 ref-mirror lag candidates);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-PATTERNS.md (Pattern 3 per-thread bucket write; Pattern 4 nested tool_calls[N] update);
    .planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-CONTEXT.md (D-067.4-R4-01 surgical posture — NO broad refactor)
  </read_first>
  <action>
    **Step 1 — Pre-Plan-03 baseline diff (read-only):**
    ```bash
    git log --oneline -- frontend/src/hooks/useMessages.ts | head -20
    # Identify the commit immediately preceding R-1 refactor (commit 4a70efa per RESEARCH.md).
    # The pre-Plan-03 baseline commit is 19bedcd (per RESEARCH.md § 2.2).
    git diff 19bedcd..4a70efa -- frontend/src/hooks/useMessages.ts | tee /tmp/p2-r4-diff.patch
    wc -l /tmp/p2-r4-diff.patch
    ```
    Confirm RESEARCH.md's static-audit verdict: every in-factory handler routes through `setMessages(prev => ...)` which is bound to `(updater) => setMessagesForThread(threadId, updater)` at the call sites (lines 760-764 and 1013-1016).

    **Step 2 — Runtime repro (Chrome MCP):**
    Use the Chrome DevTools MCP to drive a real browser session at http://localhost:5173/. Login with `fhdmrd@gmail.com` / `123456` (per `~/.claude/projects/.../MEMORY.md` reference_local_dev_app.md).

    Repro recipe (per RESEARCH.md § 2.2 step 2):
    1. Open a fresh thread.
    2. Submit prompt: `"Use the code execution tool to print numbers 1 to 5"`.
    3. Observe spinner during execution.
    4. **Do NOT navigate threads.** Wait up to 30 seconds for spinner-to-result transition.
    5. Capture: (a) whether spinner clears OR stays stuck; (b) network tab — confirm the `/runs/{id}/stream?since=N` connection state; (c) DOM snapshot of message bubble.

    **Step 3 — Branch decision:**

    **BRANCH A: Freeze REPRODUCES** (spinner stays stuck without thread switch). Apply surgical patch per the most likely culprit list:
    - **Most likely site (per RESEARCH.md § 2.2 most-likely root cause):** the SSE consumer abruptly closes on cross-thread navigation, leaving the bucket without the `tool_end` write. Refetch via `getMessages()` (loadMessages) hydrates the post-completion state from DB.
    - Investigate: in `useMessages.ts`, find the `loadMessages` body at ~lines 565-633 and check whether `loadAbortRef.current?.abort()` at ~line 568 cascades into aborting the SSE consumer (`subscriptionsRef.current.get(run_id)?.controller`).
    - If it does, the surgical fix is to ensure `loadAbortRef.abort()` ONLY cancels the in-flight `getMessages` HTTP fetch and does NOT touch `subscriptionsRef`. Confirm by reading `subscribeToRun(run_id, since, callbacks, signal)` — the `signal` argument should be a separate `AbortController` from `loadAbortRef`.
    - Apply the minimal fix on the offending line(s). Mirror Pattern 3 (per-thread bucket write — `setMessagesForThread(threadId, prev => ...)`) if a write path needs to be added/repointed.
    - Re-run `npx vitest run src/__tests__/hooks/useMessages.test.ts` — Test 1 must turn GREEN if it was RED.

    **BRANCH B: Freeze does NOT reproduce** (spinner clears in 2-3 seconds without thread switch — matches RESEARCH.md static-audit verdict). Per orchestrator-resolved decision #4:
    - Do NOT modify `useMessages.ts`. The regression test from Task 1 ships ALONE as cheap insurance.
    - Capture screenshot of original 067.3 UAT symptom from `.planning/phases/067.3-streaming-render-and-storage-fixes-round-2/067.3-HUMAN-UAT.md` Row R-4 evidence + paste into Plan 02 SUMMARY.
    - Flag in SUMMARY: "R-4 — non-reproducible at fix time; likely transient state in 067.3 UAT or fully closed by R-1 R-1 refactor 4a70efa subsequent fixes. Regression test in place. Under observation in Plan 05 closing UAT."

    **Step 4 — R-1 spot-check (always run regardless of branch):**
    Re-execute the R-1 cross-thread switch protocol from 067.3 Plan 05 — Bread Thread A + Physicists Thread B mid-stream switch — confirm both threads preserve their respective deltas after switch. Capture run_ids in summary.

    **Commit shape:**
    - **Branch A:** `fix(067.4-02): R-4 — surgical patch in useMessages.ts <line/site>`
    - **Branch B:** No production code change; commit is empty OR Plan 02 has only the Task 1 commit. SUMMARY documents Branch B explicitly.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/hooks/useMessages.test.ts 2>&1 | tee /tmp/p2-t2.txt; grep -E "Tests|passed|failed" /tmp/p2-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/__tests__/hooks/useMessages.test.ts` exits 0 (both tests GREEN)
    - **Branch A acceptance (if freeze reproduced):**
      - `git diff HEAD~1 frontend/src/hooks/useMessages.ts | wc -l` returns < 50 lines (surgical, NOT broad refactor)
      - `git log -1 --pretty=%s` matches `fix(067.4-02): R-4` prefix
      - `grep -nF "setMessagesForThread" frontend/src/hooks/useMessages.ts | wc -l` returns >= 8 (Pattern 3 still pervasive — no regression to a generic `setMessages`)
    - **Branch B acceptance (if freeze did NOT reproduce):**
      - `git diff HEAD -- frontend/src/hooks/useMessages.ts` is empty (no code change)
      - Plan 02 SUMMARY documents non-reproduction explicitly with original 067.3 UAT screenshot reference
      - 067.3 R-1 protocol re-verified: both threads preserve renders post-switch (Chrome MCP evidence in SUMMARY)
    - **R-1 spot-check (always):** screenshot or text capture in SUMMARY proving Bread + Physicists threads both rendered correctly post-switch
    - No `--watch` flag in any verify command
    - Plan 02 must NOT block on root-cause confirmation (per orchestrator-resolved decision #4)
  </acceptance_criteria>
  <done>
    Branch determined and acted on: surgical patch landed (Branch A) OR test ships alone with documented non-reproduction (Branch B). R-1 spot-check passes both ways. Plan 03 may begin (Plan 02's `useMessages.ts` modifications, if any, are committed).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE event stream → React hook setState | Untrusted-by-design SSE event payloads from server flow into `setMessagesForThread(threadId, prev => ...)` and update React state observed by render |
| Thread switch (`setViewingThread`) → `messagesByThreadRef` mirror | `activeThreadIdRef` change races against in-flight SSE callbacks targeting a different thread bucket |
| `loadMessages` HTTP fetch → SSE consumer | `loadAbortRef.current?.abort()` could cascade-abort the SSE consumer if controllers are entangled (R-4 root cause hypothesis) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067.4-02-01 | T (Tampering) | Cross-thread state leak via `setMessagesForThread(threadA, ...)` writing into Thread B's bucket | mitigate | R-1 protection test (Test 2 in Wave 0 vitest) explicitly asserts that cross-thread switch preserves both threads' renders. Pattern 3 (`setMessagesForThread(threadId, prev => ...)`) bound at `:760-764` and `:1013-1016` parameterizes by `threadId` from closure, not from a mutable ref — verified by static audit. |
| T-067.4-02-02 | D (Denial of service) | Stale `messagesRef.current` causing UI freeze (R-4 symptom) | mitigate | Branch A: surgical fix at the offending site; Test 1 guards regression. Branch B: regression test ships alone as insurance per orchestrator-resolved decision #4. |
| T-067.4-02-03 | I (Information disclosure) | Mocked `getSession` in test exposes a fake user_id | accept | Test-only mock; not a production code path. `vi.mock("@/lib/supabase", ...)` confined to vitest environment. |
| T-067.4-02-04 | E (Elevation of privilege) | None — no auth surface change | accept | Plan 02 touches React state only; no auth/RLS/route-level changes. |
| T-067.4-02-05 | R (Repudiation) | None | accept | No audit-log surface in scope. |

**Disposition tally:** 2 mitigate, 3 accept. No `high`-severity threats. Block-on-high gate: PASS.
</threat_model>

<verification>
1. **Wave 0 vitest test exists and runs:** `cd frontend && npx vitest run src/__tests__/hooks/useMessages.test.ts` exits 0; both tests pass (in steady-state — Test 1 may RED if R-4 freeze actually reproduces, in which case Branch A applies).
2. **Branch decision recorded:** Plan 02 SUMMARY explicitly documents Branch A vs Branch B based on Chrome MCP runtime repro outcome.
3. **R-1 spot-check:** cross-thread switch protocol from 067.3 Plan 05 re-verified; both threads' renders preserved post-switch.
4. **Plan 03 unblocked:** any `useMessages.ts` changes committed to enable sequential Wave 1 dispatch.

Manual UAT (deferred to Plan 05): final R-4 verification in closing UAT scoreboard with explicit screenshot evidence.
</verification>

<success_criteria>
- ✅ NEW vitest file `frontend/src/__tests__/hooks/useMessages.test.ts` with 2 tests covering R-4 active-thread tool-stage + R-1 protection
- ✅ Both tests PASS in CI (`npx vitest run`)
- ✅ Runtime repro performed via Chrome MCP at http://localhost:5173/ (login `fhdmrd@gmail.com` / `123456`)
- ✅ Branch A or Branch B explicitly chosen and documented in SUMMARY
- ✅ If Branch A: surgical patch < 50 lines diff; commit message `fix(067.4-02): R-4 — surgical patch ...`
- ✅ If Branch B: no production code change; regression test ships alone (per orchestrator-resolved decision #4)
- ✅ R-1 spot-check passed (Bread Thread A + Physicists Thread B mid-stream switch — both rendered correctly)
- ✅ Plan 03 sequencing honored — Plan 02 commits BEFORE Plan 03 starts (D-067.4-WAVE-01)
- ✅ No `--watch` flags anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/067.4-streaming-suggestions-tool-stage-render-code-execution/067.4-02-SUMMARY.md` documenting:
- Branch chosen (A: patch landed; B: test ships alone)
- Chrome MCP runtime repro evidence (network tab snapshot, DOM snapshot, video clip if available)
- If Branch A: which site was patched + diff hunk + before/after vitest output
- If Branch B: explicit non-reproduction note + reference to original 067.3 UAT screenshot
- R-1 spot-check evidence (cross-thread switch run_ids; render-preservation confirmation)
- Statement: "Plan 03 cleared to begin — `useMessages.ts` is committed."
</output>
