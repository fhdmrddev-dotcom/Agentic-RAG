---
phase: 063.1-frontend-stream-decoupling-gap-closure
verified: 2026-05-04T23:10:00Z
status: human_needed
score: 21/21 must-haves verified at code level (3 carry-forward UAT items routed to human verification per Phase 063 precedent)
overrides_applied: 0
human_verification:
  - test: "SC#5 Cross-tab Stop end-to-end (carry-forward)"
    expected: "Stop in tab A closes both tab A's and tab B's SSE consumers within ~1s; both tabs render 'Response stopped' badge."
    why_human: "Single-context Chrome MCP cannot orchestrate two independent tabs/contexts subscribed to the same run; the timing reproducibility was already noted as PARTIAL in Phase 063 UAT. Backend-level mechanism (DELETE → 204 → cancelled-event broadcast via Redis Stream) is verified; visual cross-tab UI propagation requires manual two-tab observation."
  - test: "SC#5 (063.1) Resume button live test under ENABLE_TEST_FIXTURES=1"
    expected: "With backend started with ENABLE_TEST_FIXTURES=1, POST /test-fixtures/inject-failed-run/{tid} succeeds; reload + thread-click renders Resume button on the failed assistant message; click fires fresh POST /threads/{tid}/messages with new run_id; new SSE opens; new content streams."
    why_human: "Resume button data path is wired in code (Plan 01 backend JOIN + Plan 02 api.ts mapper supply runStatus to the Message type), but exercising it requires a backend restart with the ENABLE_TEST_FIXTURES=1 env-gate, fixture injection, and visual confirmation of the button's render + click handler. Live UAT was deferred per Phase 063 precedent and routed to Phase 064 fixture harness work."
  - test: "Phase 063 SC#1–#6 full manual regression"
    expected: "After Wave 2/3/4 fixes, the original Phase 063 SCs (POST returns 201 immediately, reconcile fires on visibilitychange/focus/pageshow, refresh-mid-stream replay completes, multi-tab fanout works, single-tab Stop fires DELETE 204, no tool-JSON leak in chat content) all still PASS without regression."
    why_human: "Live-bundle markers (lastSeenOffsetRef, m.runId === run.run_id, abortStream removed from ChatArea, reconcileInFlightRef + temp- MERGE filter) are confirmed served at localhost:5173 via Chrome MCP; the load-bearing fixes are demonstrably present. Full step-by-step regression of Phase 063's six SCs against the fixed bundle is the user's standing carry-forward (per parent 063 UAT precedent of closing `partial` with deferred manual regression)."
---

# Phase 063.1: Frontend Stream Decoupling — Gap Closure Verification Report

**Phase Goal:** Resolve the 5 gaps surfaced by Phase 063's live UAT (Gap-001..005) so the navigate-away-during-stream, refresh-mid-stream, AND multi-tab-activation flows are visually clean and Redis-efficient. Confirms the Resume-button data path against a live failed run via the `ENABLE_TEST_FIXTURES=1` harness.
**Verified:** 2026-05-04T23:10:00Z
**Status:** human_needed (all 21 code-level must-haves verified; 3 carry-forward UAT items require manual confirmation per Phase 063 precedent — explicitly approved by user as non-blocking)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (15 D-063.1 LOCKED decisions + 6 ROADMAP success criteria)

#### LOCKED IMPLEMENTATION DECISIONS (D-063.1-01..15)

| #  | Decision      | Status     | Evidence (file:line) |
| -- | ------------- | ---------- | -------------------- |
| 1  | D-063.1-01 — `lastSeenOffsetRef.current = Map<run_id, ms_id>` declared in useMessages | VERIFIED | `frontend/src/hooks/useMessages.ts:310` (`const lastSeenOffsetRef = useRef<Map<string, string>>(new Map())`) |
| 2  | D-063.1-02 — sendMessage passes `"0"` (fresh run); reconcile passes `lastSeenOffsetRef.current.get(run.run_id) ?? "0"`; resumeFromFailed re-POSTs (irrelevant) | VERIFIED | sendMessage `subscribeToRun(run_id, "0", ...)` at `useMessages.ts:598`; reconcile `subscribeToRun(run.run_id, lastSeenOffsetRef.current.get(run.run_id) ?? "0", ...)` at `useMessages.ts:829-834` |
| 3  | D-063.1-03 — Backend already accepts `since`; no backend change required for Gap-004 | VERIFIED | No backend diff for `since` param in this phase; `frontend/src/lib/api.ts:208` shows `onCursor` parser-side only; ROADMAP Phase 062 Success Criterion #2 already shipped `since={offset}` |
| 4  | D-063.1-04 — reconcile uses `m.runId === run.run_id` against current state and reuses that id as assistantId; falls back to `temp-${run.run_id}` | VERIFIED | `useMessages.ts:721` (`const existingByRunId = messagesRef.current.find((m) => m.runId === run.run_id)`); `useMessages.ts:722` (`const targetId = existingByRunId?.id ?? \`temp-${run.run_id}\``) |
| 5  | D-063.1-05 — Backend assistant rows carry runId after F5 via loadMessages JOIN | VERIFIED | `backend/app/api/threads.py:644-665` two-query merge; `backend/app/models/message.py:38-39` (`run_id: UUID | None`, `run_status: Literal[...] | None`); plan order honored — Wave 1 backend before Wave 2 dedup |
| 6  | D-063.1-06 — Single message buffer architecture preserved (no Context lift, no per-thread Map) | VERIFIED | `useMessages.ts` retains single `messages` state via `useState<Message[]>` and `messagesRef`; no per-thread Map introduced; ChatArea consumer remains keyed on App-level routing per `useMessages.ts:917-923` audit comment |
| 7  | D-063.1-07 — abortStream() removed from ChatArea thread-change useEffect | VERIFIED | `frontend/src/components/chat/ChatArea.tsx:94-106` carries the documented removal; bare-call `abortStream()` is gone (verified via Grep — `abortStream\(\)\s*$` returns 0 hits in file body); abortStream still exported & used by genuine timeout paths (`useMessages.ts:938`) |
| 8  | D-063.1-08 — sendMessage's makeStreamCallbacks setMessages routes through `guardedSetMessages` gating on `streamingThreadIdRef.current === activeThreadIdRef.current` | VERIFIED | `useMessages.ts:544-549` (declaration); `useMessages.ts:547` (`if (streamingThreadIdRef.current !== activeThreadIdRef.current) return`); `useMessages.ts:555` (`setMessages: guardedSetMessages` passed to `makeStreamCallbacks`) |
| 9  | D-063.1-09 — Narrowed short-circuit: placeholder/dedup ALWAYS run; only `subscribeToRun` skipped when `subscriptionsRef.has(run.run_id)` | VERIFIED | `useMessages.ts:756` — short-circuit `if (subscriptionsRef.current.has(run.run_id)) continue` is positioned AFTER the placeholder/dedup insert block at lines 727-745 |
| 10 | D-063.1-10 — Hook unmount effect only fires on logout/route change (audit-confirmed) | VERIFIED | `useMessages.ts:917-923` audit comment; `grep -rn "useMessages()" frontend/src` returns single match in ChatArea; ChatArea is not keyed on `thread.id`; effect body unchanged |
| 11 | D-063.1-11 — `reconcileInFlightRef` boolean guard at top of reconcile; set before Promise.all, reset in finally | VERIFIED | `useMessages.ts:323` (decl); `useMessages.ts:695-696` (`if (reconcileInFlightRef.current) return; reconcileInFlightRef.current = true`); `useMessages.ts:855-861` (`finally { ... reconcileInFlightRef.current = false }`) |
| 12 | D-063.1-12 — loadMessages MERGE preserves live temp- placeholders whose runId is not yet a DB row | VERIFIED | `useMessages.ts:426-439` MERGE updater with `liveTempPlaceholders` filter `m.id.startsWith("temp-") && m.runId && (!dbRunIds.has(m.runId) || subscriptionsRef.current.has(m.runId))` — CR-01 fix included |
| 13 | D-063.1-13 — `GET /threads/{tid}/messages` returns run_id + run_status via two-query Python merge keyed by message_id | VERIFIED | `backend/app/api/threads.py:644-665` runs SELECT + dict merge; runs SELECT executes AFTER threads ownership SELECT (lines 587-595); `.eq("user_id", current_user["id"])` defense-in-depth at line 648; WR-01 first-wins-by-`started_at`-DESC at lines 649-656 |
| 14 | D-063.1-14 — RLS gates both tables via same `user_id`; no new policy needed; cross-user test verifies no leak | VERIFIED | `backend/tests/integration/test_063_1_messages_runs_join.py:186` (`test_cross_user_messages_get_404_no_leak`); WR-03 added `runs_builder.eq.call_args_list` assertion confirming `("user_id", USER_ID)` filter applied |
| 15 | D-063.1-15 — Pydantic MessageResponse extends with `run_id: UUID | None` and `run_status: Literal["streaming","completed","failed","cancelled"] | None`; api.ts maps snake → camel | VERIFIED | `backend/app/models/message.py:38-39` (snake_case Pydantic); `frontend/src/lib/api.ts:72-73, 89-90` (`runId: run_id ?? undefined`, `runStatus: run_status ?? undefined` via WR-02 nullish-coalesce) |

#### ROADMAP SUCCESS CRITERIA (6 of 6 — all "what must be TRUE")

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 16 | SC#1 — Switching threads mid-stream — assistant content visible ≤200ms after switch-back, no blank window | VERIFIED (code) | D-063.1-07 (abortStream removed) + D-063.1-08 (guardedSetMessages no-op writes through) + D-063.1-09 (placeholder always renders on switch-back) — collectively close Gap-003. e2e regression: `e2e/tests/063.1-thread-switch-mid-stream.spec.ts:147` (`expect(page.locator('[data-testid="assistant-message"]').first()).toBeVisible({ timeout: 2_000 })`) |
| 17 | SC#2 — At most ONE active SSE consumer per run_id per tab; nav-away+back does NOT open second `GET /runs/{rid}/stream` | VERIFIED (code) | D-063.1-09 narrowed short-circuit (`useMessages.ts:756`) + WR-06 fix slot reservation BEFORE subscribe (`useMessages.ts:763-764`) + BL-03 onTerminal cleanup. e2e: `e2e/tests/063.1-thread-switch-mid-stream.spec.ts:170` (`expect(distinctRuns.size).toBe(1)`) |
| 18 | SC#3 — SSE reattach uses `since={highest-seen-ms-id}`, NOT hard-coded `since="0"` | VERIFIED (code) | D-063.1-01/02/03 cursor pipeline. `api.ts:309-310` parser captures `id:` lines; `useMessages.ts:821-823` and `:594-596` stash via onCursor; `useMessages.ts:831` reads cursor at reconcile call site. e2e: `063.1-thread-switch-mid-stream.spec.ts:182-184` asserts `sinceValues[sinceValues.length - 1]).not.toBe("0")` when reconcile reattaches |
| 19 | SC#4 — Refresh-mid-stream — exactly ONE assistant bubble after F5+thread-click | VERIFIED (code) | D-063.1-04 runId-match dedup against messagesRef (depends on D-063.1-13 backend JOIN delivering runId). e2e: `063.1-refresh-no-duplicate-bubble.spec.ts:109-110` asserts `Math.max(...samples) === 1` across 10 polls of 500ms each |
| 20 | SC#5 — Resume button working live with `ENABLE_TEST_FIXTURES=1` | NEEDS HUMAN | Code path complete: `backend/app/api/threads.py:644-665` JOIN + `frontend/src/lib/api.ts:89-90` mapper + frontend Message type already has runStatus from Phase 063. Live exercise requires `ENABLE_TEST_FIXTURES=1` backend restart + fixture injection. Routed to human_verification per Phase 063 precedent. |
| 21 | SC#6 — Concurrent reconcile (visibilitychange + focus same tick) does NOT wipe placeholder; placeholder visible ≤200ms | VERIFIED (code) | D-063.1-11 in-flight bool guard (`useMessages.ts:695-696, 855-861`) + D-063.1-12 MERGE updater (`useMessages.ts:426-439`). e2e: `063.1-concurrent-reconcile.spec.ts:118-119` dispatches both events same tick; `:153` asserts `distinctRuns.size === 1`; `:161` asserts `activeRunsCount >= 1` (anti-false-RED) |

**Score:** 21 / 21 truths verified at code level. SC#5 (Resume live) routed to human_verification.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `backend/app/models/message.py` | MessageResponse extended with run_id + run_status | ✓ VERIFIED | Lines 38-39 carry snake_case fields with correct Literal type and `\| None` defaults; existing 11 fields untouched |
| `backend/app/api/threads.py` | get_messages with two-query Python merge keyed by message_id; ownership-first | ✓ VERIFIED | Lines 580-667; ownership SELECT lines 587-595 BEFORE runs SELECT lines 644-650; explicit field selection (`run_id, message_id, status` only) prevents T-063.1-04 leak; WR-01 ORDER BY started_at DESC + first-wins loop |
| `backend/tests/integration/test_063_1_messages_runs_join.py` | 3 tests: happy path, pre-run-backed null, cross-user RLS guard | ✓ VERIFIED | All 3 test functions present at lines 50, 133, 186; `pytest -x --timeout=30 -q` returns `3 passed, 1 warning in 0.18s`; WR-03 user_id filter assertion + WR-09 unused import removed |
| `frontend/src/lib/api.ts` | getMessages snake → camel mapper for runId/runStatus + StreamCallbacks.onCursor + parser captures `id:` lines | ✓ VERIFIED | Lines 72-73 (response shape), 89-90 (`runId: run_id ?? undefined` per WR-02), 208 (onCursor in StreamCallbacks), 309-310 (`line.startsWith("id: ")` capture), 396-397 (onCursor invocation after dispatch), 407 (WR-05 `lastEventId = undefined` reset on parse failure) |
| `frontend/src/hooks/useMessages.ts` | lastSeenOffsetRef + reconcileInFlightRef + streamingThreadIdRef + sendMessage guardedSetMessages + reconcile dedup + narrowed short-circuit + loadMessages MERGE | ✓ VERIFIED | All 5 ref/state additions + 4 logic changes confirmed via grep + line-context reads (lines 296, 310, 323, 426-439, 458, 544-557, 594-598, 695-696, 721-722, 756, 821-834, 855-861) |
| `frontend/src/components/chat/ChatArea.tsx` | abortStream() bare-call removed from thread-change useEffect; comment block documents removal + WR-07/WR-08 invariants | ✓ VERIFIED | Lines 94-127 carry the documented removal + clearMessages WR-07 acknowledgement; lines 142-157 carry WR-08 reconcileRef invariant comment block; `grep abortStream\(\)\s*$` returns 0 hits |
| `e2e/tests/063.1-thread-switch-mid-stream.spec.ts` | Gap-003 + Gap-004 e2e regression guard | ✓ VERIFIED | Exists; asserts `distinctRuns.size === 1` (line 170), `sinceValues[last] !== "0"` (line 183), `[data-testid="assistant-message"]` visible (line 147 with `.first()` per CR-04); WR-04 env-var guard present |
| `e2e/tests/063.1-concurrent-reconcile.spec.ts` | Gap-005 e2e regression guard | ✓ VERIFIED | Exists; dispatches `visibilitychange` + `focus` same-tick (lines 118-119); asserts `distinctRuns.size === 1` (line 153) + `activeRunsCount >= 1` (line 161 anti-false-RED); CR-04 `.first()` + `toHaveCount(1)` |
| `e2e/tests/063.1-refresh-no-duplicate-bubble.spec.ts` | Gap-001 e2e regression guard | ✓ VERIFIED | Exists; F5 mid-stream then `Math.max(...samples) === 1` (line 109) across 10×500ms polls; WR-04 env-var guard |
| `.planning/phases/063.1-.../063.1-HUMAN-UAT.md` | Filled scoreboard with results + Gap-006 escalation | ✓ VERIFIED | Frontmatter `status: partial`, `updated: 2026-05-04`; 7 SC entries; Gap-006 entry with full root-cause + 4 fix paths + user direction; carry-forward items flagged non-blocking per Phase 063 precedent |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| useMessages.ts reconcile | subscribeToRun | `lastSeenOffsetRef.current.get(runId) ?? "0"` | ✓ WIRED | useMessages.ts:831 — backend Phase 062 already accepts `since` (D-063.1-03 no-backend-change) |
| useMessages.ts reconcile | messagesRef.current | `find(m => m.runId === run.run_id)` for dedup | ✓ WIRED | useMessages.ts:721 — depends on backend JOIN at threads.py:644-665 + api.ts mapping at api.ts:89-90 |
| api.ts subscribeToRun parser | hook lastSeenOffsetRef | `onCursor` callback after each successful `data:` dispatch | ✓ WIRED | api.ts:396-397 (parser); useMessages.ts:594-596, 821-823 (cursor stash); WR-05 reset on parse failure prevents stale-id advancement |
| frontend api.ts getMessages | backend get_messages | snake_case `run_id`/`run_status` JSON body | ✓ WIRED | api.ts:72-73 inline response shape; backend Pydantic at message.py:38-39; WR-02 nullish-coalesce coerces `null → undefined` for type-correctness |
| MessageResponse Pydantic | public.runs table | two-query Python merge keyed by `message_id` | ✓ WIRED | threads.py:644-665; runs SELECT via supabase client with ownership-first sequencing |
| reconcile() top-of-function | reconcileInFlightRef bool | `if (reconcileInFlightRef.current) return; reconcileInFlightRef.current = true` synchronous before Promise.all | ✓ WIRED | useMessages.ts:695-696; finally block at :855-861 |
| loadMessages MERGE branch | live temp- placeholders | filter `m.id.startsWith('temp-') && m.runId && (!dbRunIds.has(m.runId) || subscriptionsRef.current.has(m.runId))` | ✓ WIRED | useMessages.ts:426-439; CR-01 fix added the `subscriptionsRef.has(m.runId)` clause to preserve live consumer placeholders during persist→stream_end window |
| ChatArea thread-change useEffect | in-flight sendMessage SSE consumer | abortStream() call removed; controller stays alive | ✓ WIRED | ChatArea.tsx:94-127 — D-063.1-07 comment block; bare-call `abortStream()` gone |
| sendMessage per-event setMessages | guardedSetMessages (no-op when streaming thread != viewing thread) | `streamingThreadIdRef !== activeThreadIdRef` early-return | ✓ WIRED | useMessages.ts:544-549 declaration; :555 wired into `makeStreamCallbacks({setMessages: guardedSetMessages})`; terminal flip + runId stamp use plain setMessages (PATTERNS.md line 134 invariant) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `frontend/src/lib/api.ts` getMessages | `runId` / `runStatus` on Message | Backend `GET /threads/{tid}/messages` two-query merge | YES — runs.run_id / runs.status from public.runs (migration 035) when message_id FK exists; null otherwise (correct null signal for Resume button) | ✓ FLOWING |
| `frontend/src/hooks/useMessages.ts` lastSeenOffsetRef | `Map<runId, ms_id>` cursor | `api.ts` SSE parser `id:` line capture → onCursor → set | YES — Redis Stream emits monotonic ms-ids per event (Phase 061); WR-05 prevents stale-id advancement on parse failure | ✓ FLOWING |
| `frontend/src/hooks/useMessages.ts` reconcile() messages render | `messages` state from setMessages writes | sendMessage SSE deltas + reconcile placeholder + loadMessages MERGE | YES — three converging writers; runId-match dedup ensures one bubble per run; CR-01 ensures live consumer's placeholder isn't dropped during persist window | ✓ FLOWING |
| `backend/app/api/threads.py` get_messages | `messages[i].run_id`, `messages[i].run_status` | `runs_by_message[m["id"]]` dict lookup | YES — Python merge populated from `supabase.table("runs").select("run_id, message_id, status").eq("thread_id", ...).eq("user_id", ...).order("started_at", desc=True)`; idx_runs_history hits this exactly | ✓ FLOWING |
| `frontend/src/components/chat/ChatArea.tsx` reconcile listener | reconcile() invocation on visibilitychange/focus/pageshow | `reconcileRef.current` indirection (WR-08 documented invariant) | YES — listeners attached at ChatArea.tsx:178-180; reconcile body executes; reconcileInFlightRef coalesces concurrent fires | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| MessageResponse import + instantiation with run_id + run_status='streaming' | `cd backend && venv/Scripts/python -c "from app.models.message import MessageResponse; ... print('OK', m.run_id, m.run_status)"` | `OK aff23fec-463d-4713-b3c2-9dc7c20c1184 streaming` | ✓ PASS |
| Backend integration tests for JOIN happy path + pre-run-backed null + cross-user RLS guard | `cd backend && venv/Scripts/python -m pytest tests/integration/test_063_1_messages_runs_join.py -x --timeout=30 -q` | `3 passed, 1 warning in 0.18s` | ✓ PASS |
| Frontend TypeScript compiles cleanly | `cd frontend && node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` | (no output, exit 0) | ✓ PASS |
| `abortStream()` bare-call gone from ChatArea thread-change useEffect | Grep `abortStream\(\)\s*$` in ChatArea.tsx | 0 hits | ✓ PASS |
| All 4 lock decisions present in served bundle (live-bundle Chrome MCP verification per HUMAN-UAT.md) | `lastSeenOffsetRef`, `m.runId === run.run_id`, no `abortStream` import in ChatArea, `reconcileInFlightRef` + `startsWith('temp-')` MERGE filter | All 4 markers present at localhost:5173 | ✓ PASS (per HUMAN-UAT.md SC#1-4 evidence) |
| Phase 063.1 e2e specs exist + use correct data-testid + .first() + env-guard | `Glob e2e/tests/063.1-*.spec.ts` + grep | 3 specs present; assertions verified via grep | ✓ PASS (live execution deferred to Phase 064 fixture harness work) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| STREAM-04-polish | All 5 plans (063.1-01..05) | Finishes the user-visible side of STREAM-04 that Phase 063 left rough; no new top-level requirement | ✓ SATISFIED | All 5 Gap closures landed (Gap-001 dedup, Gap-002 JOIN, Gap-003 abortStream removal, Gap-004 offset cursor, Gap-005 in-flight guard + MERGE); ROADMAP marks Phase 063.1 5/5 Complete |
| STREAM-04 (parent) | Phases 061+062+063 | Streaming response survives navigation, refresh, multi-tab access — generation lifetime decoupled from any single HTTP request | ✓ SATISFIED (no regression introduced) | All gaps closed strengthen STREAM-04's user-facing surface; live-bundle markers confirmed; ROADMAP/REQUIREMENTS.md row STREAM-04 stays Complete |
| STREAM-02b (Resume button data flow live-correct) | Plan 01 (D-063.1-13/15) | Resume control re-issues the run when status='failed'; data flow via JOIN | ✓ SATISFIED at code level; ❓ NEEDS HUMAN at live-fixture exercise | Backend JOIN serves run_status; api.ts maps to camelCase; Message type already carries runStatus (Phase 063); live click-through deferred to Phase 064 ENABLE_TEST_FIXTURES harness work — routed to human_verification |

No orphaned requirements: REQUIREMENTS.md does not map any requirement IDs to Phase 063.1 beyond STREAM-04-polish (an intentional non-toplevel ID per the phase context — "STREAM-04 polish (no new requirement)") and the carry-forward STREAM-02b validation. Both accounted for.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| _(none surfaced in this scan)_ | — | — | — | — |

Notes:
- All 4 BLOCKERs (CR-01..CR-04) and 8/9 WARNINGs from `063.1-REVIEW.md` were applied per `063.1-REVIEW-FIX.md`.
- WR-06 (mid-stream loadMessages persists `runStatus: "streaming"` after SSE terminates) is intentionally deferred. Reviewer classified as "mostly a timing artifact — next reconcile will resolve it." CR-01's fix (gate the MERGE drop on `subscriptionsRef.has(m.runId)`) materially reduces the practical impact: while the consumer is still live, the temp placeholder is preserved, so onTerminal's `runStatus: "completed"` flip survives a stale-DB read. This deferral is acknowledged and traced to `063.1-REVIEW-FIX.md:107-111`. NOT a blocker — flagged here for traceability only.
- Backend test_061_ttl.py::test_failed_run_expires_60s is a pre-existing failure unrelated to 063.1 (verified via baseline checkout per phase context).

### Human Verification Required

Three carry-forward items — explicitly approved as non-blocking by the user (Phase 063 precedent: parent UAT closed `partial` with similar deferrals). Routing rationale: code-level wiring is verified; live exercise requires backend restart with `ENABLE_TEST_FIXTURES=1`, fixture injection, multi-tab orchestration, or full Phase 063 SC re-run — all deferred to Phase 064's fixture harness work.

#### 1. SC#5 — Cross-tab Stop end-to-end

**Test:** Open same thread in two tabs (A, B); start a long stream from A; in tab A click Stop; observe tab B.
**Expected:** Tab A's SSE closes with "Response stopped" badge; within ~1s tab B's SSE ALSO closes (Redis Stream cancelled-event broadcasts to all consumers per Phase 062 D-062-09); tab B's bubble shows the same "Response stopped" badge.
**Why human:** Single-context Chrome MCP cannot orchestrate two independent tabs/contexts subscribed to the same run id. Phase 063 UAT noted "NOT cleanly reproducible — Tab 2 didn't have an active subscription at the moment of Stop; mechanism sound at backend level" (CONTEXT.md line 204). Backend mechanism (DELETE → 204 → producer cancellation → cancelled event → all SSE consumers see terminal) is verified at code level; cross-tab visual UI propagation needs manual two-tab observation.

#### 2. SC#7 — Resume button live test under `ENABLE_TEST_FIXTURES=1`

**Test:**
1. Restart backend with `ENABLE_TEST_FIXTURES=1` env var set.
2. Verify `POST /test-fixtures/inject-failed-run/{tid}` exists (200/422, NOT 404).
3. Pick a thread with at least one user→assistant exchange. POST the fixture with the assistant message_id.
4. F5 the page; navigate to that thread.
5. Confirm Resume button (lucide-react RotateCcw + "Resume" text) renders on the failed assistant bubble.
6. Click Resume; confirm fresh `POST /threads/{tid}/messages` fires with new `run_id`; new SSE opens; new content streams.

**Expected:** Backend JOIN supplies `runStatus: "failed"` on the assistant row; api.ts maps it to camelCase; Message type carries it (already shipped Phase 063); button render condition (`message.runStatus === "failed"`) fires; click handler re-POSTs.

**Why human:** Code path is wired and verified by Plan 01 backend tests + Plan 02 frontend type wiring + live-bundle markers. End-to-end exercise requires backend env-gate restart + fixture injection — explicitly routed to Phase 064 ("Validation Harness") per Plan 05's checkpoint resolution. Phase 063 UAT marked SC#7 PENDING with same rationale.

#### 3. Phase 063 SC#1–#6 full manual regression

**Test:** Re-run the original Phase 063 manual UAT scoreboard end-to-end against the Wave 2/3/4 fixed bundle.
**Expected:** All six SCs still PASS without regression: SC#1 (POST returns 201 immediately + GET stream attaches), SC#2 (reconcile triggers fire on visibilitychange/focus/pageshow with correct gating), SC#3 (refresh-mid-stream replay completes), SC#4 (multi-tab fanout), SC#5 (single-tab Stop fires DELETE 204 + "Response stopped" badge), SC#6 (no tool-JSON leak in chat content).
**Why human:** Live-bundle markers (`lastSeenOffsetRef`, `m.runId === run.run_id`, no `abortStream` in ChatArea, `reconcileInFlightRef` + `temp-` filter) confirm the load-bearing fixes are present at localhost:5173. Full step-by-step regression of all 6 Phase 063 SCs against the fixed bundle is a manual-observation task. Routed per Phase 063 precedent of closing `partial` with deferred manual regression.

### Gaps Summary

No code-level gaps. All 21 must-haves (15 D-063.1 LOCKED decisions + 6 ROADMAP success criteria) are verified in the codebase. The phase is approved at project level by the user (HUMAN-UAT.md frontmatter + 063.1-05-SUMMARY.md). Three carry-forward UAT items remain — explicitly non-blocking per Phase 063 precedent and routed to Phase 064 fixture harness work for live exercise. Gap-006 (LLM agent stops mid-iteration on complex tool-calling — RUN_HARD_TIMEOUT_SECONDS=120s `GeneratorExit`) is OUT OF SCOPE for 063.1 (frontend stream decoupling) and escalated to a new phase ("Adaptive Run Timeouts & Lifecycle States") per HUMAN-UAT.md.

### Verification Status: PASSED at code level; HUMAN_NEEDED for live carry-forward UAT items

The phase goal "Resolve the 5 gaps surfaced by Phase 063's live UAT (Gap-001..005) so navigate-away-during-stream, refresh-mid-stream, AND multi-tab-activation flows are visually clean and Redis-efficient" is achieved in the codebase:

- **Gap-001 (refresh-flicker)** — D-063.1-04 runId-match dedup + D-063.1-13 backend JOIN supply runId post-F5
- **Gap-002 (Resume data flow)** — D-063.1-13/14/15 JOIN + api.ts mapper + Pydantic extension; live exercise routed to human verification
- **Gap-003 (thread-switch blank window + duplicate SSE)** — D-063.1-06 single buffer + D-063.1-07 abortStream removal + D-063.1-08 sendMessage guardedSetMessages + D-063.1-09 narrowed short-circuit
- **Gap-004 (hard-coded since="0")** — D-063.1-01/02/03 offset cursor pipeline (api.ts onCursor parser → useMessages lastSeenOffsetRef → reconcile read)
- **Gap-005 (concurrent reconcile wipes placeholder)** — D-063.1-11 in-flight bool guard + D-063.1-12 loadMessages MERGE (with CR-01 live-consumer preservation)

Phase 063.1 is technically complete per the closure-by-orchestrator approval; this verification report formally records the code-level audit and routes the three carry-forward UAT items to Phase 064's fixture harness work as the user has approved.

---

_Verified: 2026-05-04T23:10:00Z_
_Verifier: Claude (gsd-verifier)_
