---
phase: 237-one-rule-engine-not-two
plan: 04
type: execute
wave: 4
depends_on:
  - 237-01
  - 237-02
  - 237-03
files_modified:
  - backend/tests/integration/test_118_rule_leak.py
  - backend/tests/unit/test_ingest_enrich_shared.py
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements:
  - RULES-01
  - RULES-02
user_setup: []

must_haves:
  truths:
    - "VIS-06 fence is re-proven: moving private connection doc to org-shared folder via accept_classification 403s unless force=True (SC#4)"
    - "The widened rule engine produces suggestions only and never writes folder_id directly"
    - "test_ingest_enrich_shared.py confirms identical suggestion outcomes across upload and queue paths"
    - "All touched files crossing G-5 thresholds have updated ledger rows and narrative sections in docs/HOT-FILE-LEDGER.md in the same commit"
    - "node scripts/check-hot-file-ledger.cjs 237 passes cleanly"
    - "Backend test suite remains within the locked 71-failure ceiling (zero regressions against baseline set)"
    - "Frontend count gate runs and confirms zero regressions"
  artifacts:
    - docs/HOT-FILE-LEDGER.md
  key_links:
    - "Satisfies SC#4: rules still only ever suggest and the VIS-06 fence is re-proven"
    - "Discharges G-5 hot-file ledger requirements for touched files"
---

<objective>
Re-prove the VIS-06 security fence, verify ingest path agreement, and sync the hot-file ledger (SC#4, RULES-01, RULES-02):
1. In `test_118_rule_leak.py`, re-prove the VIS-06 fence under the widened rule engine:
   - A rule suggesting an org-shared folder evaluates on a document with `source_connection_id` and `ingest_visibility="private"`.
   - Calling `accept_classification` without `force=True` fails with HTTP 403 `classification_refusal`.
   - Calling with `force=True` successfully applies the move.
2. In `test_ingest_enrich_shared.py`, assert that the upload path and the queue path evaluate classification rules identically with the widened scope model.
3. Update `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` for all touched files crossing G-5 phase thresholds (`classification_matcher.py`, `classification_rule_service.py`, `document_views.py`, `classification_rules.py`, `RuleBuilderPanel.tsx`), ensuring disposition cells stay <= 200 chars.
4. Execute full verification across backend and frontend gates.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/237-one-rule-engine-not-two/BASELINE.md
@.planning/237-one-rule-engine-not-two/237-CONTEXT.md
</execution_context>

<tasks>
<task id="237-04-01" name="Re-prove VIS-06 Fence under Widened Engine (SC#4)" wave="4">
In `backend/tests/integration/test_118_rule_leak.py`:
- Add test case: `test_widened_rule_engine_retains_vis06_classification_refusal`.
- Setup a private-connection document and an org-shared folder.
- Execute classification suggestion generation via the widened engine.
- Assert `_classification` contains `suggested_folder_id`.
- Call `accept_classification` without `force=True` → assert 403 `classification_refusal` is raised.
- Call `accept_classification` with `force=True` → assert move succeeds and `prior_folder_id` undo stamp is set.
</task>

<task id="237-04-02" name="Ingest Path Agreement Verification" wave="4">
In `backend/tests/unit/test_ingest_enrich_shared.py`:
- Extend the shared agreement suite to verify that a document processed via the legacy upload path and via the queue path generates identical `_classification` suggestions under the new scope discriminator.
</task>

<task id="237-04-03" name="Hot-File Ledger Update and CLAUDE.md Size Check" wave="4">
1. Derive exact commit/phase/line triples for touched hot files:
   - `backend/app/api/document_views.py`
   - `backend/app/api/classification_rules.py`
   - `frontend/src/components/classification/RuleBuilderPanel.tsx`
   - `backend/app/services/classification_matcher.py`
   - `backend/app/services/classification_rule_service.py`
   - `backend/app/services/view_filter_compiler.py`
2. Add/update rows in `CLAUDE.md` hot-file ledger table (disposition strictly <= 200 characters).
3. Add corresponding narrative sections in `docs/HOT-FILE-LEDGER.md` in the same commit.
4. Run `node scripts/check-hot-file-ledger.cjs 237` and `node scripts/check-claude-md-size.cjs`.
</task>

<task id="237-04-04" name="Final Gates and Baseline Diff" wave="4">
1. Run backend unit suite: `pytest tests/unit -q --continue-on-collection-errors`. Compare failing test IDs against the 71 saved in BASELINE.md — assert zero new failures.
2. Run frontend count gate: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`.
3. Run classification frontend tests: `npx vitest run src/components/classification/`.
</task>
</tasks>

<verify>
pytest backend/tests/integration/test_118_rule_leak.py
pytest backend/tests/unit/test_ingest_enrich_shared.py
node scripts/check-hot-file-ledger.cjs 237
node scripts/check-claude-md-size.cjs
</verify>
