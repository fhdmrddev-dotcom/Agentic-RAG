---
phase: 237-one-rule-engine-not-two
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/173_classification_rules_scope.sql
  - backend/app/models/classification_rule.py
  - backend/app/api/classification_rules.py
  - backend/app/services/classification_rule_service.py
  - backend/app/services/sources/preview_service.py
  - backend/tests/unit/test_118_rule_validation.py
  - backend/tests/unit/test_118_matcher.py
  - backend/tests/unit/services/test_preview_rules_scope.py
autonomous: true
requirements:
  - RULES-01
user_setup: []

must_haves:
  truths:
    - "Migration 173 adds rule_scope text NOT NULL DEFAULT 'classification' with check constraint to classification_rules"
    - "RuleCreate, RuleUpdate, and RuleResponse models carry rule_scope: Literal['watch', 'classification']"
    - "Saving or updating a rule with rule_scope='watch' that references extracted or custom fields is refused at build time with 422 Unprocessable Entity and an explicit reason (SC#3)"
    - "Saving a rule with rule_scope='watch' referencing arrival fields (name, path, mime, size, source_system, source_connection_id) succeeds"
    - "preview_service._suggest_destination evaluates only rules where rule_scope='watch' and populates preview_metadata with full arrival facts"
    - "All unit tests pass and introduce zero new failures to the backend baseline"
  artifacts:
    - supabase/migrations/173_classification_rules_scope.sql
    - backend/tests/unit/services/test_preview_rules_scope.py
  key_links:
    - "Satisfies SC#3: watch rules referencing extracted metadata are refused at build time"
    - "Fixes the live failure mode in preview_service.py:475 where 7-field whitelist was passed against 2-field dict"
---

<objective>
Implement the backend foundation for Phase 237 (RULES-01, SC#3):
1. Create migration `173_classification_rules_scope.sql` adding `rule_scope` (`'watch'` | `'classification'`) to `classification_rules` table.
2. Update Pydantic models in `backend/app/models/classification_rule.py` with `rule_scope`.
3. In `backend/app/api/classification_rules.py` and `classification_rule_service.py`, enforce scope-differentiated field whitelists:
   - `WATCH_FIELDS = {"name", "path", "mime", "size", "source_system", "source_connection_id"}`.
   - Refuse any watch rule referencing extracted fields (`document_type`, `topics`, `summary`, `author`, `language`, or custom fields) with HTTP 422 Unprocessable Entity (SC#3).
4. Update `preview_service._suggest_destination` to only evaluate `rule_scope == 'watch'` rules and provide complete arrival facts (`name`, `path`, `mime`, `size`, `source_system`, `source_connection_id`) in `preview_metadata`.
5. Add comprehensive unit tests in `test_118_rule_validation.py` and `test_preview_rules_scope.py`.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/237-one-rule-engine-not-two/BASELINE.md
@.planning/237-one-rule-engine-not-two/237-CONTEXT.md
</execution_context>

<tasks>
<task id="237-01-01" name="Database Migration for rule_scope" wave="1">
Create `supabase/migrations/173_classification_rules_scope.sql`:
- Add column `rule_scope text NOT NULL DEFAULT 'classification'` to `public.classification_rules`.
- Add constraint `ALTER TABLE public.classification_rules ADD CONSTRAINT classification_rules_scope_check CHECK (rule_scope IN ('watch', 'classification'));`.
- Update indexes or comments as appropriate.
</task>

<task id="237-01-02" name="Classification Rule Models and Data Service" wave="1">
1. In `backend/app/models/classification_rule.py`:
   - Add `rule_scope: Literal["watch", "classification"] = "classification"` to `RuleCreate`, `RuleUpdate` (optional), and `RuleResponse`.
2. In `backend/app/services/classification_rule_service.py`:
   - Accept and insert `rule_scope` in `create_rule`.
   - Update `list_rules` to support optional `rule_scope: str | None = None` filter.
</task>

<task id="237-01-03" name="Build-Time Scope Whitelist Validation (SC#3)" wave="1">
In `backend/app/api/classification_rules.py`:
- Define `WATCH_ALLOWED_FIELDS = frozenset({"name", "path", "mime", "size", "source_system", "source_connection_id"})`.
- Update `_validate_match_expr(match_expr, rule_scope, current_user, supabase)`:
  - If `rule_scope == "watch"`: check all fields in `match_expr`. If any field is not in `WATCH_ALLOWED_FIELDS`, raise `HTTPException(422, detail=f"field '{field}' is not available for watch rules (watch rules evaluate at arrival before extraction)")`.
  - If `rule_scope == "classification"`: validate against extracted metadata built-ins + custom field defs + source fields.
- Update `create_rule` and `update_rule` to pass `rule_scope`.
</task>

<task id="237-01-04" name="Preview Service Destination Alignment" wave="1">
In `backend/app/services/sources/preview_service.py`:
- In `_suggest_destination`:
  - Filter `rules` to only evaluate rules where `rule.get("rule_scope") == "watch"`.
  - Build `preview_metadata` containing:
    `"name": name, "title": name, "path": folder_path or "", "mime": mime_type or "", "type": mime_type or "", "size": file_size or 0, "source_system": (conn.get("service_id") or "").strip().lower()`.
  - Evaluate with `classification_matcher.match_metadata` using `WATCH_ALLOWED_FIELDS`.
</task>

<task id="237-01-05" name="Unit Verification Tests" wave="1">
1. In `backend/tests/unit/test_118_rule_validation.py`:
   - Add tests verifying watch rule with `document_type` or custom field is refused with 422.
   - Add tests verifying watch rule with `name`, `mime`, `path`, `size` succeeds.
   - Add tests verifying classification rule with `document_type` succeeds.
2. In `backend/tests/unit/services/test_preview_rules_scope.py`:
   - Test `_suggest_destination` matches watch rules with name/path/type conditions.
   - Test `_suggest_destination` ignores classification-scope rules.
</task>
</tasks>

<verify>
pytest backend/tests/unit/test_118_rule_validation.py
pytest backend/tests/unit/test_118_matcher.py
pytest backend/tests/unit/services/test_preview_rules_scope.py
</verify>
