---
phase: 237-one-rule-engine-not-two
plan: 02
type: execute
wave: 2
depends_on:
  - 237-01
files_modified:
  - backend/app/services/view_filter_compiler.py
  - backend/app/services/document_view_resolver.py
  - backend/tests/unit/test_113_view_filter_compiler.py
  - backend/tests/unit/test_114_view_filter_compiler.py
  - backend/tests/unit/test_document_views_source_fields.py
autonomous: true
requirements:
  - RULES-02
user_setup: []

must_haves:
  truths:
    - "Source facts (source_system, source_connection_id, ingest_visibility, path) are first-class filterable fields in saved views and rules"
    - "document_view_resolver._build_field_meta includes recognized source fact fields in whitelist"
    - "document_view_resolver:197 validates all conditions against whitelist, closing the typed-leg whitelist bypass seam (SC#2)"
    - "view_filter_compiler compiles source fact conditions cleanly to typed or custom fragment legs"
    - "A saved view filtering on source_system or source_connection_id compiles and resolves correctly"
    - "All unit tests pass and introduce zero new failures to the backend baseline"
  artifacts:
    - backend/tests/unit/test_document_views_source_fields.py
  key_links:
    - "Satisfies SC#2: where a document came from is an ordinary field in rules and saved views"
    - "Closes the named baseline failure mode: typed columns bypassing whitelist check in document_view_resolver.py:197"
---

<objective>
Implement source facts as first-class filterable fields in the compiler and resolver (RULES-02, SC#2):
1. Extend `document_view_resolver._build_field_meta` to include source facts (`source_system`, `source_connection_id`, `path`, `ingest_visibility`) in the filterable whitelist.
2. In `view_filter_compiler.py`, map real column source fields into `PROMOTED_TYPED_COLUMNS` (`source_connection_id`, `ingest_visibility`, `source_state`) and map `source_system` and `path` to custom or typed legs.
3. Close the typed-leg whitelist bypass seam in `document_view_resolver.py:197`: validate every fragment / condition against the whitelist so no caller can bypass validation simply because a column is typed.
4. Add unit test coverage in `test_document_views_source_fields.py` and ensure compatibility with existing `test_113` and `test_114` compiler suites.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/237-one-rule-engine-not-two/BASELINE.md
@.planning/237-one-rule-engine-not-two/237-CONTEXT.md
</execution_context>

<tasks>
<task id="237-02-01" name="Source Facts in Field Metadata Whitelist" wave="2">
In `backend/app/services/document_view_resolver.py`:
- Define `_SOURCE_FACT_FIELDS = frozenset({"source_system", "source_connection_id", "path", "ingest_visibility", "source_state"})`.
- In `_build_field_meta(user_id, supabase)`: include `_SOURCE_FACT_FIELDS` in the returned whitelist alongside built-ins and custom fields.
</task>

<task id="237-02-02" name="Compiler Promotion and Normalization for Source Facts" wave="2">
In `backend/app/services/view_filter_compiler.py`:
- Extend `PROMOTED_TYPED_COLUMNS`:
  - `"source_connection_id": "source_connection_id"`
  - `"ingest_visibility": "ingest_visibility"`
  - `"source_state": "source_state"`
- In `_op_eq` and operator handlers:
  - Handle `source_connection_id`, `ingest_visibility`, and `source_state` via `leg="typed"`.
  - Handle `source_system` and `path` via `leg="custom"` (mapping to `metadata->'source'->>'system'` or `metadata->>'path'` / `file_path`).
  - Add `source_system` to `NORMALIZED_LOWER_FIELDS` so `eq` matches case-insensitively (e.g. `'google_drive'`).
</task>

<task id="237-02-03" name="Close Typed-Leg Whitelist Bypass Seam (SC#2)" wave="2">
In `backend/app/services/document_view_resolver.py`:
- Review line 197 (`if frag.leg == "custom" and frag.field not in whitelist:`).
- Update the defense-in-depth re-check so that ALL fragments (both `custom` and `typed`) verify their source field is whitelisted:
  - Map typed column names back to original condition fields or check against `whitelist | set(PROMOTED_TYPED_COLUMNS.values())`.
  - Raise `ResolveError(detail=f"filter field {field!r} is no longer available")` if any unvetted field is accessed.
</task>

<task id="237-02-04" name="Unit Verification for Source Fact Views and Rules" wave="2">
1. In `backend/tests/unit/test_document_views_source_fields.py`:
   - Test compiling and resolving a ViewFilter with `source_system eq "google_drive"`.
   - Test compiling and resolving a ViewFilter with `source_connection_id eq "<uuid>"`.
   - Test that unvetted typed or custom fields are rejected by the resolver.
2. Run `test_113_view_filter_compiler.py` and `test_114_view_filter_compiler.py` to ensure regression-free behavior across all 34 existing pinning tests.
</task>
</tasks>

<verify>
pytest backend/tests/unit/test_113_view_filter_compiler.py
pytest backend/tests/unit/test_114_view_filter_compiler.py
pytest backend/tests/unit/test_document_views_source_fields.py
</verify>
