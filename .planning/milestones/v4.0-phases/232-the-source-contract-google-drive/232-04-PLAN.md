---
phase: 232-the-source-contract-google-drive
plan: 04
type: execute
wave: 4
depends_on:
  - 232-03
files_modified:
  - backend/app/api/connectors.py
  - frontend/src/lib/api/connectors.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/sources/SourceFolderPicker.tsx
  - frontend/src/components/sources/SourceFolderPicker.test.tsx
  - frontend/src/components/chat/ConnectedFilePickerModal.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements:
  - SRC-01
  - SRC-02
user_setup: []

must_haves:
  truths:
    - "GET /connectors/connections/{connection_id}/browse exposes hierarchical folder browsing via SourceAdapter.browse"
    - "frontend/src/lib/api/connectors.ts implements browseSourceFolders and defines SourceNode types"
    - "frontend/src/components/sources/SourceFolderPicker.tsx renders a recursive tree view with top-level My Drive and Shared Drives"
    - "SourceFolderPicker supports expanding and collapsing folders, loading child pages on demand, and selecting a target folder"
    - "The fake Mock Source appears in development/test mode or when enabled via feature flag"
    - "frontend/src/components/sources/SourceFolderPicker.test.tsx passes 100% and is pinned in scripts/vitest-count-gate.cjs"
    - "HOT-FILE-LEDGER.md and CLAUDE.md record the new G-5 ledger row for frontend/src/lib/api/connectors.ts (10 / 5 / 552) in the same commit (G-2)"
    - "tsc -p frontend/tsconfig.app.json reports 66 errors (zero new errors vs baseline)"
  artifacts:
    - frontend/src/components/sources/SourceFolderPicker.tsx
    - frontend/src/components/sources/SourceFolderPicker.test.tsx
  key_links:
    - "SourceFolderPicker fulfills SRC-02 by enabling interactive Drive and Shared Drive folder picking"
    - "Hierarchical browsing connects frontend components to the backend SourceAdapter.browse contract"
    - "Hot-file ledger row added for connectors.ts maintains guardrail visibility on G-5 firing modules"
---

<objective>
Deliver the Source Folder Picker UI and hierarchical browse API (`SRC-01`, `SRC-02`, D-232-01, D-232-03):
1. Expose hierarchical browsing endpoint `GET /connectors/connections/{connection_id}/browse` in `backend/app/api/connectors.py`.
2. Add `browseSourceFolders` client method and types in `frontend/src/lib/api/connectors.ts` and barrel `frontend/src/lib/api.ts`.
3. Create `frontend/src/components/sources/SourceFolderPicker.tsx`:
   - Displays root nodes: `"My Drive"` and `"Shared Drives"`.
   - Supports lazy child folder loading, folder expand/collapse toggle, chevron icons, and folder selection.
   - Emits selected folder metadata `{ folder_id, folder_name, drive_id, drive_name }`.
4. Update `ConnectedFilePickerModal.tsx` to integrate with `SourceRegistry` sources and support mock source testing.
5. Author comprehensive unit tests in `SourceFolderPicker.test.tsx` and pin in `scripts/vitest-count-gate.cjs`.
6. Verify clean typecheck and test passes.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/232-the-source-contract-google-drive/232-CONTEXT.md
@frontend/src/lib/api/connectors.ts
@frontend/src/components/chat/ConnectedFilePickerModal.tsx
@scripts/vitest-count-gate.cjs
</execution_context>

<threat_model>
<trust_boundaries>
Frontend Client -> GET /connectors/connections/{id}/browse -> Supabase Auth -> SourceRegistry
</threat_model>
<threats>
- threat_id: TM-232-07
  category: Information Disclosure / Cross-Tenant Folder Access
  component: /connectors/connections/{connection_id}/browse
  disposition: mitigated
  mitigation_plan: Require active org check and valid connection ownership in connector_service.get_connection before resolving adapter.
</threats>
</threat_model>

<tasks>
<task>
<task_id>232-04-01</task_id>
<description>Expose GET /connectors/connections/{id}/browse endpoint</description>
<action>
In `backend/app/api/connectors.py`:
- Add route:
  ```python
  @router.get(
      "/connections/{connection_id}/browse",
      summary="Browse folder hierarchy in connected cloud source (SRC-02)",
  )
  async def browse_connection_hierarchy(
      connection_id: str,
      folder_id: str | None = None,
      page_token: str | None = None,
      active_org: str = Depends(get_active_org_id),
      user: dict = Depends(get_current_user),
      supabase: Client = Depends(get_user_supabase_client),
  ):
  ```
- Validate connection access for `active_org`.
- Call `adapter = SourceRegistry.get_adapter(conn)`. If not found, return 404 or empty browse page.
- Call `await adapter.browse(conn, folder_id=folder_id, page_token=page_token)`.
- Return `{ "items": [node.__dict__ for node in page.items], "next_page_token": page.next_page_token }`.
</action>
<verify>
python -c "from app.api.connectors import router; print('browse route registered')"
</verify>
</task>

<task>
<task_id>232-04-02</task_id>
<description>Add browseSourceFolders API client and types</description>
<action>
In `frontend/src/lib/api/connectors.ts`:
- Define `SourceNode`:
  ```typescript
  export interface SourceNode {
    id: string
    name: string
    kind: "folder" | "drive"
    drive_id?: string | null
    has_children?: boolean
    parent_id?: string | null
  }
  export interface SourceBrowseResponse {
    items: SourceNode[]
    next_page_token?: string | null
  }
  ```
- Implement `browseSourceFolders(connectionId: string, folderId?: string, pageToken?: string): Promise<SourceBrowseResponse>`.
- Re-export in `frontend/src/lib/api.ts`.
</action>
<verify>
npx tsc -p frontend/tsconfig.app.json --noEmit
</verify>
</task>

<task>
<task_id>232-04-03</task_id>
<description>Implement SourceFolderPicker.tsx</description>
<action>
Create `frontend/src/components/sources/SourceFolderPicker.tsx`:
- Accepts props:
  - `connectionId: string`
  - `selectedFolderId?: string | null`
  - `onSelectFolder: (folder: { folderId: string; folderName: string; driveId?: string | null; driveName?: string | null }) => void`
- Fetches root folders via `browseSourceFolders(connectionId)`.
- Displays folder tree with `Folder`, `HardDrive`, and `ChevronRight`/`ChevronDown` icons.
- Supports expanding folder nodes to load child subfolders lazily.
- Highlights currently selected folder with subtle brand styling (`bg-brand/10 border-brand`).
- Provides clean empty state and loading spinners during page fetches.
</action>
<verify>
npx tsc -p frontend/tsconfig.app.json --noEmit
</verify>
</task>

<task>
<task_id>232-04-04</task_id>
<description>Author unit tests in SourceFolderPicker.test.tsx and pin in vitest-count-gate.cjs</description>
<action>
1. Create `frontend/src/components/sources/SourceFolderPicker.test.tsx`:
   - Test initial render fetches root folders ("My Drive" and "Shared Drives").
   - Test expanding a folder node triggers fetch for child nodes.
   - Test clicking a folder triggers `onSelectFolder` callback with correct metadata.
   - Test error display when API call fails.
2. Run vitest on the new suite to verify all tests pass.
3. Update `scripts/vitest-count-gate.cjs` TARGETS and BASELINE to pin the test count.
</action>
<verify>
npm run test:unit frontend/src/components/sources/SourceFolderPicker.test.tsx
</verify>
</task>

<task>
<task_id>232-04-05</task_id>
<description>Add G-5 hot-file ledger row for frontend/src/lib/api/connectors.ts (G-2)</description>
<action>
Update `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md`:
- Add entry for `frontend/src/lib/api/connectors.ts`:
  - Measured at `10 / 5 / 552` + 1 commit/phase in 232 (`11 / 6 / ~570`).
  - Record disposition: "Source folder browse endpoints added in Phase 232 (SRC-02)".
</action>
<verify>
node scripts/check-claude-md-size.cjs
</verify>
</task>
</tasks>
