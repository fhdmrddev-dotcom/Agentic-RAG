---
phase: 234-the-watch-loop-the-library-reads-by-itself
plan: 04
type: execute
wave: 4
depends_on:
  - 234-03
files_modified:
  - backend/app/models/source.py
  - backend/app/api/sources.py
  - backend/app/main.py
  - frontend/src/lib/api/sources.ts
  - backend/tests/unit/api/test_sources_watches_api.py
autonomous: true
requirements:
  - LIB-08
  - SURF-01
  - VIS-05
user_setup: []

must_haves:
  truths:
    - "backend/app/api/sources.py exposes CRUD endpoints for watches: POST, GET, PATCH, DELETE /api/sources/watches"
    - "backend/app/models/source.py defines Pydantic request/response schemas for watches and items"
    - "POST /api/sources/watches/{id}/sync triggers an immediate on-demand check"
    - "POST /api/sources/watches/{id}/purge deletes frozen/disconnected documents"
    - "frontend/src/lib/api/sources.ts provides typed client wrappers for all watch API endpoints"
  artifacts:
    - backend/app/models/source.py
    - backend/app/api/sources.py
    - frontend/src/lib/api/sources.ts
    - backend/tests/unit/api/test_sources_watches_api.py
  key_links:
    - "api/sources.py connects the frontend watch UI to db/watches.py and WatchService"
---

<objective>
Expose the HTTP API and TypeScript client layer for watch operations:
1. Define Pydantic models in `backend/app/models/source.py`:
   - `WatchCreateRequest`, `WatchUpdateRequest`, `WatchResponse`, `WatchItemResponse`, `WatchSyncResponse`.
2. Implement FastAPI router in `backend/app/api/sources.py`:
   - `POST /api/sources/watches`: Create folder watch.
   - `GET /api/sources/watches`: List watches for user/org.
   - `GET /api/sources/watches/{id}`: Get watch details + mirror items.
   - `PATCH /api/sources/watches/{id}`: Update interval, pause/resume.
   - `DELETE /api/sources/watches/{id}`: Remove watch.
   - `POST /api/sources/watches/{id}/sync`: Trigger immediate check.
   - `POST /api/sources/watches/{id}/purge`: Purge disconnected documents (`VIS-05`).
3. Include router in `backend/app/main.py`.
4. Create `frontend/src/lib/api/sources.ts` exporting typed client calls.
5. Author unit tests in `backend/tests/unit/api/test_sources_watches_api.py`.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/234-the-watch-loop-the-library-reads-by-itself/234-CONTEXT.md
@.planning/phases/234-the-watch-loop-the-library-reads-by-itself/234-MEASUREMENTS.md
</execution_context>

<tasks>
<task>
<task_id>234-04-01</task_id>
<description>Define Pydantic wire models in backend/app/models/source.py</description>
<action>
Create `backend/app/models/source.py`:
- `WatchCreateRequest`: `connection_id: UUID`, `source_folder_id: str`, `source_folder_name: str`, `source_drive_id: str | None = None`, `library_folder_id: UUID | None = None`, `interval_minutes: int = 30`.
- `WatchUpdateRequest`: `interval_minutes: int | None = None`, `is_active: bool | None = None`, `library_folder_id: UUID | None = None`.
- `WatchResponse`: Full serialized watch record including `last_run_at`, `last_status`, `last_error`, `item_count: int`.
- `WatchItemResponse`: Serialized `connector_watch_items` row with `state`, `source_version`, `document_id`.
</action>
<verify>
Pydantic validation checks in unit tests.
</verify>
</task>

<task>
<task_id>234-04-02</task_id>
<description>Implement routes in backend/app/api/sources.py and register in main.py</description>
<action>
1. Create `backend/app/api/sources.py`:
   - Endpoints under prefix `/api/sources`:
     - `POST /watches`
     - `GET /watches`
     - `GET /watches/{watch_id}`
     - `PATCH /watches/{watch_id}`
     - `DELETE /watches/{watch_id}`
     - `POST /watches/{watch_id}/sync`
     - `POST /watches/{watch_id}/purge`
   - Enforce authentication (`get_current_user`) and org tenancy (`current_user_org_ids`).
2. In `backend/app/main.py`:
   - Include `sources.router` under tags `["sources"]`.
</action>
<verify>
Router imports cleanly and responds on test requests.
</verify>
</task>

<task>
<task_id>234-04-03</task_id>
<description>Create TypeScript client library frontend/src/lib/api/sources.ts</description>
<action>
Create `frontend/src/lib/api/sources.ts`:
- Types: `ConnectorWatch`, `ConnectorWatchItem`, `CreateWatchPayload`, `UpdateWatchPayload`.
- Functions:
  - `listWatches()`
  - `getWatch(id: string)`
  - `createWatch(payload: CreateWatchPayload)`
  - `updateWatch(id: string, payload: UpdateWatchPayload)`
  - `deleteWatch(id: string, purgeDocuments?: boolean)`
  - `triggerWatchSync(id: string)`
</action>
<verify>
TypeScript compile check via `tsc -p tsconfig.app.json`.
</verify>
</task>

<task>
<task_id>234-04-04</task_id>
<description>Author unit tests for sources API</description>
<action>
Create `backend/tests/unit/api/test_sources_watches_api.py`:
- Tests CRUD endpoints with authenticated test client.
- Tests tenant isolation (user A cannot modify user B's watch).
- Tests immediate sync trigger and purge responses.
</action>
<verify>
`pytest tests/unit/api/test_sources_watches_api.py` passes 100%.
</verify>
</task>
</tasks>
