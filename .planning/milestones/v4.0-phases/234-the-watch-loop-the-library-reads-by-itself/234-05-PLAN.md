---
phase: 234-the-watch-loop-the-library-reads-by-itself
plan: 05
type: execute
wave: 5
depends_on:
  - 234-04
files_modified:
  - frontend/src/components/sources/WatchedFoldersSection.tsx
  - frontend/src/components/sources/CreateWatchModal.tsx
  - frontend/src/pages/LibraryPage.tsx
  - frontend/src/components/sources/WatchedFoldersSection.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements:
  - LIB-08
  - SURF-01
  - VIS-05
user_setup: []

must_haves:
  truths:
    - "frontend/src/components/sources/WatchedFoldersSection.tsx displays watched folders list with exact copy 'checked every N minutes' (SURF-01)"
    - "CreateWatchModal allows picking a source folder via SourceFolderPicker, a destination Library folder, and a cadence preset (15m, 30m, 1h, 6h, 24h)"
    - "WatchedFoldersSection offers a 'Purge missing files' action allowing explicit removal of files in missing_at_source or source_disconnected states (VIS-05 / SC#3)"
    - "When a connection is disconnected, the watch card shows a warning banner with 'Reconnect {connection_name}' offered BY NAME (VIS-05 / SC#3)"
    - "The UI mounts inside Library Ingestion surface, leaving ConnectionFormPanel.tsx and ConnectionsTab.tsx completely untouched (G-1 pre-emption)"
    - "WatchedFoldersSection.test.tsx is pinned in both TARGETS and BASELINE in scripts/vitest-count-gate.cjs"
    - "Zero new frontend test failures, isolating pre-existing WorkflowBuilderPage.canvas.test.tsx failure"
  artifacts:
    - frontend/src/components/sources/WatchedFoldersSection.tsx
    - frontend/src/components/sources/CreateWatchModal.tsx
    - frontend/src/components/sources/WatchedFoldersSection.test.tsx
    - scripts/vitest-count-gate.cjs
  key_links:
    - "SURF-01 copy is strictly enforced without words 'instantly' or 'on change'"
    - "SC#3 lifecycle actions (Purge now and Reconnect by name) are directly reachable on the watch card"
    - "G-1 fence prevents chain violations on hot connection settings files"
---

<objective>
Implement the frontend Watched Folders surface inside the Library:
1. Create `frontend/src/components/sources/WatchedFoldersSection.tsx`:
   - Renders a clean card/section showing active folder watches.
   - For each watch: displays connection name, source folder path, destination Library folder, last checked time, status pill, and exact copy: *"checked every {N} minutes"* (`SURF-01`).
   - Actions: "Check now" button, pause/resume toggle, and delete watch.
   - **SC#3 Actions**:
     - "Purge missing files" action: triggers call to `/api/sources/watches/{id}/purge-missing` to purge documents in `missing_at_source` or `source_disconnected` state.
     - Warning banner when connection is disconnected/expired: *"Connection disconnected · Reconnect {connection_name}"* (by name) linking directly to connection reconnection flow.
2. Create `frontend/src/components/sources/CreateWatchModal.tsx`:
   - Integrates `SourceFolderPicker` (reusing the component from Phase 232/233).
   - Lets user pick the target Library destination folder.
   - Cadence picker: 15m, 30m (default), 1h, 6h, 24h.
   - Clean visibility summary indicator and plain-language retention note.
3. Mount within `LibraryPage.tsx` under the Ingestion tab (pre-empting G-1 by avoiding any edits to `ConnectionFormPanel.tsx` or `ConnectionsTab.tsx`).
4. Author unit test `WatchedFoldersSection.test.tsx` verifying render, cadence text, Reconnect banner by name, Purge action, and user actions.
5. Pin `WatchedFoldersSection.test.tsx` in `TARGETS` and `BASELINE` in `scripts/vitest-count-gate.cjs`.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/234-the-watch-loop-the-library-reads-by-itself/234-CONTEXT.md
@.planning/phases/234-the-watch-loop-the-library-reads-by-itself/234-MEASUREMENTS.md
@.planning/sketches/232-the-watch-loop-and-its-sentence/README.md
</execution_context>

<threat_model>
<trust_boundaries>
User Browser -> WatchedFoldersSection UI -> API (/api/sources/watches)
</trust_boundaries>
<threats>
- threat_id: TM-234-02
  category: Information Disclosure / Misleading Copy
  component: WatchedFoldersSection.tsx
  disposition: mitigated
  mitigation_plan: Enforce SURF-01 copy: 'checked every N minutes', strictly forbidding 'instantly' or 'on change'.
</threats>
</threat_model>

<tasks>
<task>
<task_id>234-05-01</task_id>
<description>Create CreateWatchModal.tsx in frontend/src/components/sources/</description>
<action>
Create `frontend/src/components/sources/CreateWatchModal.tsx`:
- Modal dialog with step-wise or unified form:
  1. Connection selection (filters to connected Google accounts).
  2. Source folder selection using `<SourceFolderPicker />`.
  3. Destination Library folder selector.
  4. Cadence preset selector: 15 minutes, 30 minutes (default), 1 hour, 6 hours, 24 hours.
  5. Helper note stating visibility scope and exact sentence: *"This folder will be checked every {N} minutes."*
  6. Retention note: *"Files deleted at source are retained in your Library until you purge them."*
- Submits to `createWatch` from `lib/api/sources.ts`.
</action>
<verify>
Component builds cleanly with `tsc -p tsconfig.app.json`.
</verify>
</task>

<task>
<task_id>234-05-02</task_id>
<description>Create WatchedFoldersSection.tsx in frontend/src/components/sources/</description>
<action>
Create `frontend/src/components/sources/WatchedFoldersSection.tsx`:
- Loads watches using `listWatches()`.
- Shows table or card list with:
  - Source account + folder name
  - Target Library folder
  - Cadence: *"checked every {interval_minutes} minutes"* (`SURF-01`)
  - Status badge: Active, Paused, Disconnected, Still running, Failed
  - Last synced timestamp
- When watch is in `disconnected` status or has connection error:
  - Render banner: `"Source connection disconnected: Connection '{connection_name}' token revoked or expired. Sync is frozen."`
  - Render button: `"Reconnect {connection_name}"` with handler to trigger connection reconnect (`VIS-05` / `SC#3`).
- Row actions:
  - "Sync now" button.
  - "Purge missing files" button (triggers `/api/sources/watches/{id}/purge-missing` with confirmation dialog) (`VIS-05` / `SC#3`).
  - Pause/Resume toggle.
  - Delete watch.
- Header button: "Add Watched Folder" opening `CreateWatchModal`.
</action>
<verify>
Component renders cleanly and types check.
</verify>
</task>

<task>
<task_id>234-05-03</task_id>
<description>Mount WatchedFoldersSection inside Library Ingestion surface</description>
<action>
In `frontend/src/pages/LibraryPage.tsx`:
- Under the Ingestion tab view, render `<WatchedFoldersSection />` alongside the manual/connected file ingestion section.
- **Strictly DO NOT edit** `ConnectionFormPanel.tsx` or `ConnectionsTab.tsx` (G-1 pre-emption).
</action>
<verify>
Verify `LibraryPage.tsx` imports and renders `WatchedFoldersSection`.
</verify>
</task>

<task>
<task_id>234-05-04</task_id>
<description>Author WatchedFoldersSection.test.tsx and pin in vitest-count-gate.cjs</description>
<action>
1. Create `frontend/src/components/sources/WatchedFoldersSection.test.tsx`:
   - Mocks `lib/api/sources.ts`.
   - Asserts list of watches renders with folder names and status badges.
   - Asserts exact string `"checked every 30 minutes"` appears (`SURF-01`).
   - Asserts forbidden strings `"instantly"` and `"on change"` are absent (`SURF-01`).
   - Asserts disconnected watch renders banner with `"Reconnect Google Drive (Procurement)"` naming the connection (`SC#3`).
   - Asserts "Purge missing files" action invokes purge API (`SC#3`).
   - Asserts "Sync now" and "Delete" triggers appropriate API calls.
2. In `scripts/vitest-count-gate.cjs`:
   - Add entry in `TARGETS` and `BASELINE` arrays for `src/components/sources/WatchedFoldersSection.test.tsx`.
</action>
<verify>
Run `npx vitest run src/components/sources/WatchedFoldersSection.test.tsx` -> passes 100%.
</verify>
</task>
</tasks>
