---
phase: 228-v3.9-closeout-the-debt-gets-a-number
plan: 04
type: execute
wave: 4
depends_on:
  - "228-01"
  - "228-02"
  - "228-03"
files_modified:
  - supabase/full-schema.sql
  - .planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-VERIFICATION.md
  - .planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-VALIDATION.md
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements:
  - DEBT-01
user_setup: []

must_haves:
  truths:
    - "supabase/full-schema.sql is regenerated against the live database without destructive reset (Phase 211)"
    - "Reachable UAT rows across Phase 210, 211, and 217 are actively driven locally and marked with concrete DOM/computed-style evidence"
    - "Phase 214 eight-row provider roster and eight G-4 operator drives are executed for local credentials and formally re-deferred with named triggers for unprovisioned keys"
    - "228-VERIFICATION.md records every single v3.9 owed row with an unambiguous verdict: pass, ⛔ blocked, or re-deferred; zero rows silently omitted (DEBT-01)"
    - "All gates pass cleanly: vitest-count-gate.cjs (0 failures), check-backend-unit-baseline.cjs (failed <= 71), tsc (<= 65 errors), check-claude-md-size.cjs (< 120,000 chars)"
  artifacts:
    - supabase/full-schema.sql
    - .planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-VERIFICATION.md
    - .planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-VALIDATION.md
  key_links:
    - "scripts/regenerate-full-schema.sh outputs updated supabase/full-schema.sql"
    - "228-VERIFICATION.md provides complete audit trail across all v3.9 owed verification items"
---

<objective>
Execute v3.9 owed verification drives, regenerate full schema artifact, and compile complete phase verification documentation (DEBT-01):
1. Run `scripts/regenerate-full-schema.sh` to update `supabase/full-schema.sql` against the live local database.
2. Actively drive all reachable verification rows for Phase 210, 211, and 217 in local test/browser environments.
3. Structure the Phase 214 provider roster and G-4 drives with explicit named triggers and operator checklists.
4. Author `228-VERIFICATION.md` and `228-VALIDATION.md` ensuring no owed row from v3.9 is silently absent.
5. Re-derive and verify all repository mechanical gates.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-CONTEXT.md
@supabase/full-schema.sql
@scripts/regenerate-full-schema.sh
@scripts/vitest-count-gate.cjs
@scripts/check-backend-unit-baseline.cjs
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate full schema and verify migrations (DEBT-01)</name>
  <files>
    supabase/full-schema.sql
  </files>
  <read_first>
    supabase/SETUP.md
    scripts/regenerate-full-schema.sh
  </read_first>
  <action>
    1. Run `bash scripts/regenerate-full-schema.sh` (without `--reset`, preserving dev database data).
    2. Inspect `git diff supabase/full-schema.sql` to verify clean schema dump reflecting all shipped migrations through 152.
  </action>
  <verify>
    Run `git status supabase/full-schema.sql` and ensure file is regenerated cleanly.
  </verify>
  <acceptance_criteria>
    - `supabase/full-schema.sql` is updated and matches live database schema.
  </acceptance_criteria>
  <done>Full schema artifact regenerated and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Drive reachable UAT rows and compile 228-VERIFICATION.md (DEBT-01)</name>
  <files>
    .planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-VERIFICATION.md
    .planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-CONTEXT.md
    .planning/ROADMAP.md
  </read_first>
  <action>
    1. Actively execute reachable tests for:
       - Phase 210: 4 SCs (Connection status indicators, tool discovery, grants).
       - Phase 211: Schema dump + secret column isolation checks.
       - Phase 214: Locally provisioned providers tested; unprovisioned keys formally re-deferred with trigger "Trigger: Multi-provider API keys provisioned for Phase 236 adversarial eval".
       - Phase 217: 16 UAT rows for Library views, filters, and virtual folders.
    2. Write `.planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-VERIFICATION.md`:
       - Include every single owed row from Phase 210, 211, 214, 217.
       - Each row must read `pass`, `⛔ blocked (reason, id)`, or `re-deferred (trigger)`.
       - Record concrete evidence (DOM snippets, computed styles, test command results).
    3. Write `.planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-VALIDATION.md`:
       - Matrix of all 5 DEBT requirements with verification results.
  </action>
  <verify>
    Inspect `.planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-VERIFICATION.md` to confirm no row is missing a verdict.
  </verify>
  <acceptance_criteria>
    - `228-VERIFICATION.md` and `228-VALIDATION.md` are authored with zero missing rows.
  </acceptance_criteria>
  <done>v3.9 owed verification driven and documented with complete verdicts.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive all mechanical gates and verify tree cleanliness</name>
  <files>
    scripts/vitest-count-gate.cjs
  </files>
  <read_first>
    scripts/vitest-count-gate.cjs
    scripts/check-backend-unit-baseline.cjs
    CLAUDE.md
  </read_first>
  <action>
    1. Run `node scripts/vitest-count-gate.cjs` and verify 0 failures.
    2. Run `node scripts/check-backend-unit-baseline.cjs` and verify backend baseline satisfies `failed <= 71`.
    3. Run `cd frontend && npx tsc -p tsconfig.app.json` and verify no new TypeScript errors (errors <= 65).
    4. Run `node scripts/check-claude-md-size.cjs` and confirm CLAUDE.md is under character budget.
    5. Re-derive count gate totals in `scripts/vitest-count-gate.cjs` and commit.
  </action>
  <verify>
    Run all 4 mechanical gates in sequence. All must exit 0.
  </verify>
  <acceptance_criteria>
    - All 4 mechanical gates exit 0.
  </acceptance_criteria>
  <done>All gates green and baseline clean across the tree.</done>
</task>

</tasks>
