---
phase: 228-v3.9-closeout-the-debt-gets-a-number
plan: 02
type: execute
wave: 2
depends_on:
  - "228-01"
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/ToolCallPanel.tsx
  - frontend/src/components/chat/StepRow.tsx
  - backend/app/api/threads.py
  - backend/app/api/runs.py
  - frontend/src/components/chat/__tests__/MessageItem.retry.test.tsx
  - frontend/src/components/chat/__tests__/MessageItem.capPaused.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements:
  - DEBT-02
user_setup: []

must_haves:
  truths:
    - "The 'Resume' button on failed turns is renamed to 'Retry turn' with honest retry semantics (BUG-260818-01)"
    - "Retry turn preserves the model and provider stamped on the failed message, preventing silent system default fallback (BUG-260818-02)"
    - "At the 15-iteration cap, the Continue card renders after full page reload/navigation by reading persisted cap_paused run status (BUG-260818-03)"
    - "Historical tool calls on thread mount render settled without staggered entrance blink wave (BUG-260823-02)"
    - "MessageItem.retry.test.tsx and MessageItem.capPaused.test.tsx are adopted into scripts/vitest-count-gate.cjs and pass green"
  artifacts:
    - frontend/src/components/chat/__tests__/MessageItem.retry.test.tsx
    - frontend/src/components/chat/__tests__/MessageItem.capPaused.test.tsx
  key_links:
    - "StreamsProvider.tsx passes model and provider from failedMsg into sendMessage"
    - "MessageItem.tsx checks message.runStatus === 'cap_paused' alongside workflowLock?.capPaused"
    - "backend runs record persists status='cap_paused' and continue_count"
---

<objective>
Triage the resume/continue bug cluster and align with Claude AI interaction standards (DEBT-02):
1. Resolve `BUG-260818-01`: Rename "Resume" to "Retry turn", honestly framing it as a fresh turn retry and cleanly handling previous failed turn presentation without prompt duplication.
2. Resolve `BUG-260818-02`: Forward `model` and `provider` from the failed assistant message into `sendMessage` during retry, eliminating silent default fallback.
3. Resolve `BUG-260818-03`: Persist and reconcile `cap_paused` run status so the Continue card survives page reloads and navigation.
4. Resolve `BUG-260823-02`: Suppress staggered entrance animation for settled historical tool calls to prevent blink waves on thread load.
5. Author dedicated Vitest suites and pin them into `scripts/vitest-count-gate.cjs`.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-CONTEXT.md
@frontend/src/providers/StreamsProvider.tsx
@frontend/src/components/chat/MessageItem.tsx
@backend/app/api/threads.py
@backend/app/api/runs.py
@scripts/vitest-count-gate.cjs
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 'Retry turn' semantics and model/provider persistence (BUG-260818-01, BUG-260818-02)</name>
  <files>
    frontend/src/providers/StreamsProvider.tsx
    frontend/src/components/chat/MessageItem.tsx
    frontend/src/components/chat/__tests__/MessageItem.retry.test.tsx
  </files>
  <read_first>
    frontend/src/providers/StreamsProvider.tsx
    frontend/src/components/chat/MessageItem.tsx
    .planning/reported-bugs/BUG-260818-01-resume-replays-the-original-prompt-instead-of-continuing.md
    .planning/reported-bugs/BUG-260818-02-resume-drops-the-threads-selected-model.md
  </read_first>
  <action>
    1. In `frontend/src/providers/StreamsProvider.tsx`:
       - Update `resumeFromFailed` (or add `retryTurn`):
         - Read `failedMsg = bucket[idx]`.
         - Extract `model = failedMsg.model` and `provider = failedMsg.provider` (and `agentMode = failedMsg.agentMode`).
         - Locate preceding user message `userMsg`.
         - Call `sendMessage(threadId, userMsg.content, { model, provider, agentMode, surfaceId })`.
         - Mark the failed message in the local store as retried or collapsed so UI renders it as a previous failed attempt.
    2. In `frontend/src/components/chat/MessageItem.tsx`:
       - Update button copy from `"Resume"` to `"Retry turn"` (and update `data-testid` to `retry-turn-button` while keeping backwards-compatible attribute if needed).
       - When rendering an assistant message marked with a retried status or prior failure, render with calm attempt styling.
    3. Author `frontend/src/components/chat/__tests__/MessageItem.retry.test.tsx`:
       - Test that the button reads `"Retry turn"` on failed/timed-out messages.
       - Test that clicking `"Retry turn"` triggers the retry callback passing the original message's `model` and `provider`.
  </action>
  <verify>
    Run `npx vitest run src/components/chat/__tests__/MessageItem.retry.test.tsx` in `frontend/`.
  </verify>
  <acceptance_criteria>
    - Button reads "Retry turn" and forwards original model and provider.
    - `MessageItem.retry.test.tsx` passes with 0 failures.
  </acceptance_criteria>
  <done>Retry turn semantics and model persistence implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Persist and reconcile cap_paused run status across page reloads (BUG-260818-03)</name>
  <files>
    backend/app/api/threads.py
    backend/app/api/runs.py
    frontend/src/components/chat/MessageItem.tsx
    frontend/src/components/chat/__tests__/MessageItem.capPaused.test.tsx
  </files>
  <read_first>
    backend/app/api/threads.py
    backend/app/api/runs.py
    frontend/src/components/chat/MessageItem.tsx
    .planning/reported-bugs/BUG-260818-03-iteration-cap-shows-stop-instead-of-continue.md
  </read_first>
  <action>
    1. In `backend/app/api/threads.py` / `agent_loop.py` / `runs.py`:
       - When the agent loop stops due to iteration cap exhaustion, ensure `runs.status` is saved as `'cap_paused'` (or metadata carries `cap_paused: true` and `continue_count`).
       - When `GET /threads/{id}/messages` serializes the latest message or run summary, include `run_status: run.status` (e.g. `'cap_paused'`) and `continue_count`.
    2. In `frontend/src/components/chat/MessageItem.tsx`:
       - Expand Continue card visibility condition:
         `message.role === "assistant" && isLastAssistant && (workflowLock?.capPaused || message.runStatus === "cap_paused" || message.metadata?.cap_paused)`
       - Ensure clicking Continue invokes `continueRun(runId)` regardless of whether `workflowLock` was delivered live or reconstructed from message data.
    3. Author `frontend/src/components/chat/__tests__/MessageItem.capPaused.test.tsx`:
       - Render an assistant message with `isLastAssistant=true`, `message.runStatus='cap_paused'`, and `workflowLock=null` (simulating full page reload).
       - Assert the Continue card is rendered (`data-testid="continue-run-card"`).
       - Click Continue and assert `continueRun` is called with the message's `runId`.
  </action>
  <verify>
    Run `npx vitest run src/components/chat/__tests__/MessageItem.capPaused.test.tsx` in `frontend/`.
  </verify>
  <acceptance_criteria>
    - Continue card renders when `runStatus === 'cap_paused'` without live workflowLock.
    - `MessageItem.capPaused.test.tsx` passes with 0 failures.
  </acceptance_criteria>
  <done>Durable cap_paused Continue affordance implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 3: Suppress blink wave on historical tool call replay and pin test suites (BUG-260823-02)</name>
  <files>
    frontend/src/components/chat/StepRow.tsx
    frontend/src/components/chat/ToolCallPanel.tsx
    scripts/vitest-count-gate.cjs
  </files>
  <read_first>
    frontend/src/components/chat/StepRow.tsx
    frontend/src/components/chat/ToolCallPanel.tsx
    scripts/vitest-count-gate.cjs
  </read_first>
  <action>
    1. In `frontend/src/components/chat/StepRow.tsx` and `ToolCallPanel.tsx`:
       - For historical messages (`!isStreaming` or settled runs), set animation duration to 0 or bypass staggered entrance delays so all steps render immediately without a sequential blink wave.
    2. In `scripts/vitest-count-gate.cjs`:
       - Add `src/components/chat/__tests__/MessageItem.retry.test.tsx` and `src/components/chat/__tests__/MessageItem.capPaused.test.tsx` to `TARGETS` and `BASELINE`.
       - Re-derive count gate baseline.
  </action>
  <verify>
    Run `node scripts/vitest-count-gate.cjs` from repo root and verify 0 failing tests and counts match baseline.
  </verify>
  <acceptance_criteria>
    - Tool call animations are suppressed for historical messages.
    - Both new suites are adopted into `scripts/vitest-count-gate.cjs`.
    - Gate passes with 0 failures.
  </acceptance_criteria>
  <done>Blink wave suppressed and new test suites pinned in count gate.</done>
</task>

</tasks>
