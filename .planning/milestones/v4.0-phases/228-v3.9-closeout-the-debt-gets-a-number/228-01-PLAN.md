---
phase: 228-v3.9-closeout-the-debt-gets-a-number
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-backend-unit-baseline.cjs
  - CLAUDE.md
  - backend/app/api/connectors.py
  - backend/tests/test_228_oauth_redis_resilience.py
autonomous: true
requirements:
  - DEBT-05
  - DEBT-03
user_setup: []

must_haves:
  truths:
    - "scripts/check-backend-unit-baseline.cjs runs pytest tests/unit -q --continue-on-collection-errors and enforces failed <= 71"
    - "CLAUDE.md records the canonical backend testing command and baseline (71 failed / 3497 passed)"
    - "connectors.py OAuth callback catches Redis connection errors and redirects cleanly to /app?connections=1&oauth_error=redis_unavailable instead of throwing an unhandled 500 (DEBT-03)"
    - "backend/tests/test_228_oauth_redis_resilience.py proves the Redis outage graceful redirect on both oauth_callback and mcp_oauth_callback"
  artifacts:
    - scripts/check-backend-unit-baseline.cjs
    - backend/tests/test_228_oauth_redis_resilience.py
  key_links:
    - "check-backend-unit-baseline.cjs parses pytest output and guards against any regression beyond the locked 71 failures"
    - "connectors.py oauth_callback uses primary_frontend_origin() and handles Redis outage before token consumption"
---

<objective>
Lock the backend unit test baseline gate (DEBT-05) and implement OAuth Redis outage hardening (DEBT-03 addition):
1. Author `scripts/check-backend-unit-baseline.cjs` to enforce the measured baseline of 71 failing tests in `tests/unit`.
2. Update `CLAUDE.md` to document the exact backend gating command, flags, and baseline numbers.
3. Harden `backend/app/api/connectors.py` OAuth callback handlers against Redis outages, converting raw 500 exceptions into clean redirects with error query parameters (advisory from Phase 225 review; note that `/code-review ultra` for DEBT-03 itself is blocked on operator credits and recorded as such in 228-VERIFICATION.md).
4. Author unit test `test_228_oauth_redis_resilience.py` verifying both standard OAuth and MCP OAuth callbacks handle Redis errors gracefully.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-CONTEXT.md
@CLAUDE.md
@backend/app/api/connectors.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Author backend baseline gate script and update CLAUDE.md (DEBT-05)</name>
  <files>
    scripts/check-backend-unit-baseline.cjs
    CLAUDE.md
  </files>
  <read_first>
    CLAUDE.md
    scripts/vitest-count-gate.cjs
  </read_first>
  <action>
    1. Create `scripts/check-backend-unit-baseline.cjs`:
       - Executes `pytest tests/unit -q --continue-on-collection-errors` using the backend venv (`backend/venv/bin/pytest` or Windows `backend/venv/Scripts/pytest.exe`).
       - Parses stdout for test summary line: `(\d+) failed, (\d+) passed`.
       - Enforces:
         - `failed <= 71` (the measured v4.0 baseline).
         - If `failed > 71`, exit code 1 with detailed failure output.
         - If unexpected collection errors occur, exit code 1.
       - Supports `--json` or `--quiet` flags.
    2. In `CLAUDE.md`:
       - Under the testing/guardrails section, document:
         - Canonical backend command: `pytest tests/unit -q --continue-on-collection-errors` (or `node scripts/check-backend-unit-baseline.cjs`).
         - Baseline: 71 failed, 3497 passed, 2 xfailed, 2 xpassed.
         - Rule: Any new failure above 71 breaks the gate.
  </action>
  <verify>
    Run `node scripts/check-backend-unit-baseline.cjs` from repo root and verify it exits 0 reporting baseline satisfied.
    Run `node scripts/check-claude-md-size.cjs` to confirm CLAUDE.md character limit remains under 120,000.
  </verify>
  <acceptance_criteria>
    - `scripts/check-backend-unit-baseline.cjs` exists, is executable, and enforces `failed <= 71`.
    - `CLAUDE.md` accurately documents the command and baseline.
  </acceptance_criteria>
  <done>Backend unit baseline gate script authored and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Harden OAuth callbacks against Redis outages (DEBT-03)</name>
  <files>
    backend/app/api/connectors.py
    backend/tests/test_228_oauth_redis_resilience.py
  </files>
  <read_first>
    backend/app/api/connectors.py
    backend/app/services/oauth_state.py
    backend/tests/test_225_oauth_state_security.py
  </read_first>
  <action>
    1. In `backend/app/api/connectors.py`:
       - In `oauth_callback` (lines ~1270-1310) and `mcp_oauth_callback` (lines ~1460-1500):
         - Wrap the `consume_oauth_state(state)` lookup in `try ... except` that catches not only `OAuthStateError` but also `(redis.exceptions.RedisError, ConnectionError, TimeoutError, OSError)`.
         - On Redis outage, log an error with context and return `RedirectResponse(url=f"{base_url}/app?connections=1&oauth_error=redis_unavailable", status_code=307)`.
    2. Author `backend/tests/test_228_oauth_redis_resilience.py`:
       - Test that mocking `consume_oauth_state` to raise `redis.exceptions.ConnectionError("Redis connection refused")` results in a 307 Redirect to `/app?connections=1&oauth_error=redis_unavailable` for both:
         - `/connectors/oauth/callback?code=fake&state=valid_handle`
         - `/connectors/mcp/oauth/callback?code=fake&state=valid_handle`
       - Assert response status code is 307 and `Location` header starts with `{primary_frontend_origin()}/app?connections=1&oauth_error=redis_unavailable`.
  </action>
  <verify>
    Run `backend/venv/Scripts/pytest.exe backend/tests/test_228_oauth_redis_resilience.py -v`. All tests must pass.
  </verify>
  <acceptance_criteria>
    - Redis errors during OAuth state consumption produce 307 redirects to `/app?connections=1&oauth_error=redis_unavailable` rather than HTTP 500.
    - `test_228_oauth_redis_resilience.py` passes 100%.
  </acceptance_criteria>
  <done>OAuth callback hardened against Redis outages and verified with unit tests.</done>
</task>

</tasks>
