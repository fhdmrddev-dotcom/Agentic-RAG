---
phase: 228-v3.9-closeout-the-debt-gets-a-number
plan: 03
type: execute
wave: 3
depends_on:
  - "228-01"
files_modified:
  - frontend/vercel.json
  - backend/app/config.py
  - deploy/onebox.env.example
  - docs/OPERATOR.md
  - docs/DEPLOYMENT-WORKFLOW.md
  - frontend/src/__tests__/routing/vercelRouting.test.ts
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements:
  - DEBT-04
user_setup: []

must_haves:
  truths:
    - "frontend/vercel.json specifies host-scoped rewrites routing app.<domain> to /app.html and 308-redirecting root /app, /setup, /invite to app.<domain>"
    - "backend/app/config.py and main.py accommodate the app subdomain in CORS origins and primary frontend resolution"
    - "deploy/onebox.env.example, docs/OPERATOR.md, and docs/DEPLOYMENT-WORKFLOW.md document the 7-step cutover instructions"
    - "scripts/check-deploy-drift.sh passes with 0 drift"
    - "frontend/src/__tests__/routing/vercelRouting.test.ts validates the vercel.json routing rules and is pinned in scripts/vitest-count-gate.cjs"
  artifacts:
    - frontend/vercel.json
    - frontend/src/__tests__/routing/vercelRouting.test.ts
  key_links:
    - "vercel.json has rewrite rules matching host pattern app.* pointing to /app.html"
    - "check-deploy-drift.sh runs clean against deploy/onebox.env.example"
---

<objective>
Prepare cloud subdomain routing and preview deployment gating for serving the product at `app.<domain>` (DEBT-04 / SEED-242):
1. Author host-scoped rewrites and 308 redirects in `frontend/vercel.json`.
2. Ensure backend CORS and primary origin configuration support the `app` subdomain.
3. Update `deploy/onebox.env.example`, `docs/OPERATOR.md`, and `docs/DEPLOYMENT-WORKFLOW.md`, ensuring `scripts/check-deploy-drift.sh` passes.
4. Author unit test `vercelRouting.test.ts` verifying routing configuration syntax and rules.
5. Provide the operator step-by-step checklist for dashboard execution during production promotion.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/228-v3.9-closeout-the-debt-gets-a-number/228-CONTEXT.md
@.planning/seeds/SEED-242-product-at-app-subdomain-on-production.md
@frontend/vercel.json
@backend/app/config.py
@deploy/onebox.env.example
@docs/OPERATOR.md
@docs/DEPLOYMENT-WORKFLOW.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Author host-scoped rewrites and redirects in vercel.json (DEBT-04)</name>
  <files>
    frontend/vercel.json
    frontend/src/__tests__/routing/vercelRouting.test.ts
  </files>
  <read_first>
    frontend/vercel.json
    .planning/seeds/SEED-242-product-at-app-subdomain-on-production.md
  </read_first>
  <action>
    1. Update `frontend/vercel.json`:
       - Add rewrites:
         - When `has: [{ "type": "host", "value": "app\\.(.*)" }]`: rewrite `/(.*)` to `/app.html`.
       - Add redirects:
         - When on the root domain (not starting with `app.`):
           - `/app` -> 308 redirect to `https://app.:host/app`
           - `/app/(.*)` -> 308 redirect to `https://app.:host/app/$1`
           - `/setup` -> 308 redirect to `https://app.:host/setup`
           - `/invite` -> 308 redirect to `https://app.:host/invite`
       - Ensure default filesystem serving of `/` (landing `index.html`) is preserved on the root domain.
    2. Author `frontend/src/__tests__/routing/vercelRouting.test.ts`:
       - Parse `frontend/vercel.json`.
       - Assert that rewrite rules contain the `app.*` host condition targeting `/app.html`.
       - Assert that redirects contain permanent/308 redirects for `/app`, `/setup`, `/invite`.
  </action>
  <verify>
    Run `npx vitest run src/__tests__/routing/vercelRouting.test.ts` in `frontend/`.
  </verify>
  <acceptance_criteria>
    - `vercel.json` contains valid host-scoped rules for `app.<domain>` and redirects.
    - `vercelRouting.test.ts` passes with 0 failures.
  </acceptance_criteria>
  <done>Host-scoped vercel.json configuration authored and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Update CORS configuration, deploy docs, and check drift (DEBT-04)</name>
  <files>
    backend/app/config.py
    deploy/onebox.env.example
    docs/OPERATOR.md
    docs/DEPLOYMENT-WORKFLOW.md
    scripts/vitest-count-gate.cjs
  </files>
  <read_first>
    backend/app/config.py
    deploy/onebox.env.example
    docs/OPERATOR.md
    docs/DEPLOYMENT-WORKFLOW.md
    scripts/check-deploy-drift.sh
  </read_first>
  <action>
    1. In `backend/app/config.py`:
       - Ensure `primary_frontend_origin()` and CORS origin parser handle `app.<domain>` seamlessly alongside root domains.
    2. In `deploy/onebox.env.example`, `docs/OPERATOR.md`, and `docs/DEPLOYMENT-WORKFLOW.md`:
       - Document `VITE_APP_URL=https://app.<domain>` for frontend build.
       - Document `FRONTEND_URL=https://app.<domain>` for backend.
       - Document Cloud Supabase Auth Site URL and redirect URL allowlist updates.
       - Document preview deploy verification steps before promotion.
    3. Adopt `frontend/src/__tests__/routing/vercelRouting.test.ts` into `scripts/vitest-count-gate.cjs` and re-derive count gate baseline.
    4. Run `bash scripts/check-deploy-drift.sh`.
  </action>
  <verify>
    Run `bash scripts/check-deploy-drift.sh` from repo root.
    Run `node scripts/vitest-count-gate.cjs` to confirm 0 test failures.
  </verify>
  <acceptance_criteria>
    - `check-deploy-drift.sh` passes with 0 drift.
    - `vitest-count-gate.cjs` includes `vercelRouting.test.ts` and passes with 0 failures.
  </acceptance_criteria>
  <done>Deployment documentation and CORS parity verified without drift.</done>
</task>

</tasks>
