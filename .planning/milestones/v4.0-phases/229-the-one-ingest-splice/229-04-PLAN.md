---
phase: 229-the-one-ingest-splice
plan: 04
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - backend/app/api/documents.py
autonomous: true
requirements:
  - TRUST-01
user_setup: []

must_haves:
  truths:
    - "G-5 hot file backend/app/api/documents.py is discharged via extraction of mint_document_row and splice_document into ingest_splice.py"
    - "CLAUDE.md hot-file ledger row and docs/HOT-FILE-LEDGER.md section for documents.py are updated in the SAME COMMIT"
    - "Cross-plan seam audit mechanically verifies all fields (file_path, content_hash, folder_id, version_number, is_latest, chunk_count, metadata) from producer to consumer"
    - "pytest tests/unit -q --continue-on-collection-errors enforces the backend unit baseline (failed <= 71)"
    - "All three new Phase 229 test suites pass 100%"
    - "TypeScript check matches 66 error baseline exactly"
    - "scripts/check-deploy-drift.sh passes with 0 drift"
    - "CLAUDE.md character count remains below 120,000"
  artifacts:
    - CLAUDE.md
    - docs/HOT-FILE-LEDGER.md
  key_links:
    - "CLAUDE.md hot-file ledger and docs/HOT-FILE-LEDGER.md updated atomically in the same commit"
---

<objective>
Discharge G-5 on `backend/app/api/documents.py`, perform cross-plan seam audit, and verify all mechanical project gates (TRUST-01, G-5, DEBT-05):
1. Measure the re-derived line count and commit metrics of `backend/app/api/documents.py` post-extraction.
2. Update the hot-file ledger table in `CLAUDE.md` and the detailed narrative in `docs/HOT-FILE-LEDGER.md` in the SAME COMMIT.
3. Conduct the mechanical cross-plan seam audit verifying data flow across all four plans.
4. Run all mechanical gates (pytest unit baseline, tsc, deploy drift, CLAUDE.md character budget).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/229-the-one-ingest-splice/229-CONTEXT.md
@.planning/phases/229-the-one-ingest-splice/229-01-PLAN.md
@.planning/phases/229-the-one-ingest-splice/229-02-PLAN.md
@.planning/phases/229-the-one-ingest-splice/229-03-PLAN.md
@.agent-bus/OPEN.md
@CLAUDE.md
@docs/HOT-FILE-LEDGER.md
@backend/app/api/documents.py
</execution_context>

<threat_model>
<trust_boundaries>
Repository Documentation & Gate Scripts -> Verification Engine
</trust_boundaries>
<threats>
- threat_id: TM-229-08
  category: Repudiation / Audit Drift
  component: CLAUDE.md & HOT-FILE-LEDGER.md
  disposition: mitigated
  mitigation_plan: Update both CLAUDE.md and docs/HOT-FILE-LEDGER.md in the exact same commit, preventing ledger drift.
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md and docs/HOT-FILE-LEDGER.md for documents.py discharge</name>
  <files>
    CLAUDE.md
    docs/HOT-FILE-LEDGER.md
    backend/app/api/documents.py
  </files>
  <read_first>
    CLAUDE.md
    docs/HOT-FILE-LEDGER.md
    backend/app/api/documents.py
  </read_first>
  <action>
    1. Re-derive exact metrics for `backend/app/api/documents.py`:
       - Run `git log --oneline -- backend/app/api/documents.py | wc -l` (commits).
       - Count line count of `backend/app/api/documents.py`.
    2. In `CLAUDE.md`:
       - Update the hot-file table row for `backend/app/api/documents.py`:
         - Update commit count, phase count, line count.
         - Update status to `Discharged (Phase 229: mint_document_row + splice_document extracted to services/ingest_splice.py)`.
    3. In `docs/HOT-FILE-LEDGER.md`:
       - Under the `backend/app/api/documents.py` section:
         - Document the Phase 229 discharge: extraction of `mint_document_row()` and `splice_document()` into `backend/app/services/ingest_splice.py`, reducing handler complexity, unifying deduplication and versioning across all producers, and resolving D-5 defects 1 and 2.
    4. Commit both files atomically in the same commit.
  </action>
  <verify>
    Run `node scripts/check-claude-md-size.cjs` to confirm CLAUDE.md is < 120k chars.
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` both record Phase 229 discharge for `documents.py`.
    - Both files committed in the same commit.
  </acceptance_criteria>
  <done>Hot file documents.py discharged with ledger and documentation updated.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanical cross-plan seam audit and full gate verification</name>
  <files>
    backend/tests/unit/test_ingest_splice.py
    backend/tests/integration/test_connector_import_splice.py
    backend/tests/integration/test_email_attachment_cascade_splice.py
  </files>
  <read_first>
    .planning/phases/229-the-one-ingest-splice/229-01-PLAN.md
    .planning/phases/229-the-one-ingest-splice/229-02-PLAN.md
    .planning/phases/229-the-one-ingest-splice/229-03-PLAN.md
  </read_first>
  <action>
    1. Conduct cross-plan seam audit:
       - Verify every field touched (`file_path`, `content_hash`, `folder_id`, `version_number`, `is_latest`, `status`, `chunk_count`, `metadata`) is wired from producer (`/upload`, `connectors.py`, email cascade) to consumer (`ingest_splice.py`, `document_chunks`, `library.py`).
       - Confirm all files on the path are accounted for.
    2. Run mechanical gates:
       - Run `pytest backend/tests/unit/test_ingest_splice.py -v` (100% pass).
       - Run `pytest backend/tests/integration/test_connector_import_splice.py -v` (100% pass).
       - Run `pytest backend/tests/integration/test_email_attachment_cascade_splice.py -v` (100% pass).
       - Run `node scripts/check-backend-unit-baseline.cjs` (enforces failed <= 71).
       - Run `npm run typecheck` (tsc 66 errors exact match).
       - Run `bash scripts/check-deploy-drift.sh` (0 drift).
  </action>
  <verify>
    Verify all commands exit 0 and report passing baselines.
  </verify>
  <acceptance_criteria>
    - All unit and integration test suites pass.
    - Mechanical gates all green.
  </acceptance_criteria>
  <done>Seam audit complete and mechanical gates fully verified.</done>
</task>

</tasks>
