---
phase: 230-the-durable-ingestion-queue
plan: 05
type: execute
wave: 4
depends_on:
  - 230-04
files_modified:
  - frontend/src/components/library/IngestionTab.tsx
  - frontend/src/components/ingestion/DocumentList.tsx
  - frontend/src/components/ingestion/IngestionPauseBanner.tsx
  - frontend/src/components/ingestion/IngestionBatchLane.tsx
  - backend/tests/eval/test_retrieval_recall_baseline.py
  - scripts/measure-recall.py
autonomous: true
requirements:
  - QUEUE-05
user_setup: []

must_haves:
  truths:
    - "IngestionPauseBanner and IngestionBatchLane (Sketch 227 Variant B) mount inside IngestionTab.tsx under the 'In progress' sub-tab above in-flight rows"
    - "Displays real countable files ('218 of 340 files') and strictly excludes time-based ETAs per D-217-19"
    - "Renders verbatim mono block with provider name and code (openai · 429 insufficient_quota) and a live countdown retry, never 'your documents returned nothing' (SC#3, closes BUG-260815-05)"
    - "Supports energized motion (laneRise, dotBounce, brandPulse, fadeSlideUp, comet) and calm toggle (--energy: 0) respecting prefers-reduced-motion"
    - "backend/tests/eval/test_retrieval_recall_baseline.py is collected by backend/pytest.ini (G-3) and establishes the Hit@K and MRR benchmark over 77 documents for Phase 241"
    - "Frontend builds cleanly with zero new TypeScript errors; Vitest and eval test pass 100%"
  artifacts:
    - frontend/src/components/library/IngestionTab.tsx
    - frontend/src/components/ingestion/IngestionPauseBanner.tsx
    - frontend/src/components/ingestion/IngestionBatchLane.tsx
    - backend/tests/eval/test_retrieval_recall_baseline.py
    - scripts/measure-recall.py
  key_links:
    - "Implements locked Sketch 227 Variant B (The Batch Lane) approved by operator on BUS-111"
    - "D-217-19 honored: Countable queue rows allow determinate file bars without violating the no-ETA rule"
    - "Pre-flight G-2, G-3 resolved: Real frontend directories targeted; recall test collected by pytest"
---

<objective>
Implement the approved Sketch 227 Variant B (The Batch Lane) in `IngestionTab.tsx` and `DocumentList.tsx`, and author the Phase 241 recall baseline evaluation harness under `backend/tests/eval/` (`QUEUE-05`, `BUG-260815-05`, SC#3, SC#5, D-10..D-13, G-2, G-3):
1. Build `IngestionBatchLane.tsx` and `IngestionPauseBanner.tsx` in `frontend/src/components/ingestion/` implementing Variant B from Sketch 227.
2. Integrate both components into `frontend/src/components/library/IngestionTab.tsx` inside the **In progress** sub-tab above in-flight document rows, respecting D-217-19 (no ETA, countable file bars).
3. Author `backend/tests/eval/test_retrieval_recall_baseline.py` and `scripts/measure-recall.py` computing Hit@K and MRR across the 77 baseline documents (G-3).
4. Verify frontend build, typecheck, and recall evaluation tests.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/230-the-durable-ingestion-queue/230-CONTEXT.md
@.planning/sketches/227-the-paused-queue-and-its-refusal/README.md
@.planning/sketches/227-the-paused-queue-and-its-refusal/index.html
@frontend/src/components/library/IngestionTab.tsx
@frontend/src/components/ingestion/DocumentList.tsx
@backend/pytest.ini
</execution_context>

<threat_model>
<trust_boundaries>
Backend Refusal Metadata -> API Response -> Frontend IngestionPauseBanner
</trust_boundaries>
<threats>
- threat_id: TM-230-10
  category: Information Dishonesty / Masquerading
  component: IngestionPauseBanner
  disposition: mitigated
  mitigation_plan: Explicitly identify the failing provider name (e.g. OpenAI) and status code (429) rather than generic error copy, preventing misdirection to operator.
- threat_id: TM-230-11
  category: XSS / Untrusted Content Injection
  component: IngestionPauseBanner error rendering
  disposition: mitigated
  mitigation_plan: Sanitize and escape provider refusal strings before rendering in React JSX.
- threat_id: TM-230-12
  category: UX Contract Regression / False Precision
  component: IngestionBatchLane
  disposition: mitigated
  mitigation_plan: Enforce D-217-19: no estimated times or percentages; display only real discrete file counts ("X of Y files").
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build IngestionPauseBanner and IngestionBatchLane (Sketch 227 Variant B)</name>
  <files>
    frontend/src/components/ingestion/IngestionPauseBanner.tsx
    frontend/src/components/ingestion/IngestionBatchLane.tsx
  </files>
  <read_first>
    .planning/sketches/227-the-paused-queue-and-its-refusal/index.html
    frontend/src/components/ingestion/DocumentList.tsx
  </read_first>
  <action>
    1. Create `frontend/src/components/ingestion/IngestionBatchLane.tsx`:
       - Implements the batch lane from Sketch 227 Variant B.
       - Props: `totalFiles: number`, `completedFiles: number`, `isPaused: boolean`, `energy?: number`.
       - Computes bar count (e.g. 34 bars, each representing ~`Math.ceil(totalFiles/34)` files).
       - Styles each bar: completed (`ok`, green gradient), current (`run`, violet glow with `laneRise` animation when active, or `hold`, amber when paused), queued (empty).
       - File count line: `{completedFiles} of {totalFiles} files` and legend `each bar ≈ {step} files`. (Strictly NO percentage and NO ETA per D-217-19).
       - Gated on `--energy` CSS variable (default 1) with calm toggle and `prefers-reduced-motion` compliance.
    2. Create `frontend/src/components/ingestion/IngestionPauseBanner.tsx`:
       - Props: `provider: string`, `statusCode: number`, `verbatimError: string`, `completedCount: number`, `totalCount: number`, `retrySeconds: number`.
       - Header: `<h4>⏸ {provider} is rate-limiting us — ingestion paused</h4>`.
       - Body: `<p><strong>{completedCount} of {totalCount} files are already added</strong> and stay added. The remaining {totalCount - completedCount} are queued, not lost.</p>`.
       - Verbatim mono block: `<div className="font-mono text-xs p-2.5 bg-background/80 border rounded text-muted-foreground">{provider} · {statusCode} — "{verbatimError}"</div>`.
       - Live countdown retry line: bouncing dots animation (`dotBounce`) + `Retrying automatically in {retrySeconds}s — nothing for you to do.`.
  </action>
  <verify>
    Verify files exist and compile:
    `npm --prefix frontend run build`
  </verify>
</task>

<task type="auto">
  <name>Task 2: Mount Batch Lane and Pause Banner in IngestionTab.tsx (In Progress sub-tab)</name>
  <files>
    frontend/src/components/library/IngestionTab.tsx
    frontend/src/components/ingestion/DocumentList.tsx
  </files>
  <read_first>
    frontend/src/components/library/IngestionTab.tsx
  </read_first>
  <action>
    In `frontend/src/components/library/IngestionTab.tsx`:
    1. Update `inFlight` filter (around line 117) to include paused documents:
       `const inFlight = documents.filter((d) => d.status === "pending" || d.status === "processing" || d.status === "paused")`
    2. Check for paused outage condition:
       Inspect `inFlight`: if any doc has `status === "paused"` or contains provider rate limit errors in `metadata`.
    3. In `<TabsContent value="in-progress">`:
       Mount `IngestionPauseBanner` (if paused) and `IngestionBatchLane` above the in-flight document rows.
       If active uploads or queue jobs are running, `IngestionBatchLane` renders live progress.
    4. In `frontend/src/components/ingestion/DocumentList.tsx`:
       Ensure rows with `status === "paused"` render the amber paused badge cleanly without breaking seven-column thead invariant.
  </action>
  <verify>
    Run frontend build and typecheck:
    `npm --prefix frontend run build`
  </verify>
</task>

<task type="auto">
  <name>Task 3: Author Phase 241 Recall Baseline Evaluation Harness under backend/tests/eval/ (G-3)</name>
  <files>
    backend/tests/eval/test_retrieval_recall_baseline.py
    scripts/measure-recall.py
  </files>
  <read_first>
    backend/pytest.ini
    backend/app/services/retrieval_service.py
  </read_first>
  <action>
    1. Create `scripts/measure-recall.py` (CLI runner):
       - Connects to database and runs a standard 10-query probe set against the 77 baseline documents.
       - Evaluates retrieval rank of planted ground truth target documents.
       - Outputs Hit@1, Hit@3, Hit@5, and MRR.
    2. Create `backend/tests/eval/test_retrieval_recall_baseline.py`:
       - Under `backend/tests/eval/` so it is collected by `pytest tests` per `backend/pytest.ini` (G-3).
       - Asserts Hit@5 >= 0.80 and MRR >= 0.70 across the 77 baseline documents.
       - Records corpus size (77 documents) alongside metrics so Phase 241 compares like with like.
  </action>
  <verify>
    Run the recall evaluation test:
    `backend/venv/Scripts/pytest tests/eval/test_retrieval_recall_baseline.py -v`
  </verify>
</task>

</tasks>

<acceptance_criteria>
- `IngestionPauseBanner` and `IngestionBatchLane` implement Sketch 227 Variant B inside `IngestionTab.tsx` under "In progress".
- Real file counts displayed; no estimated times or percentages (D-217-19).
- Refusal copy closes `BUG-260815-05` (names provider, verbatim mono error, live countdown).
- `backend/tests/eval/test_retrieval_recall_baseline.py` is collected by pytest and establishes the 77-doc baseline for Phase 241.
- All gates pass: `npm --prefix frontend run build`, `tsc`.
</acceptance_criteria>
