---
phase: 230-the-durable-ingestion-queue
plan: 04
type: execute
wave: 3
depends_on:
  - 230-03
files_modified:
  - backend/app/services/ingest_splice.py
  - backend/app/api/documents.py
  - backend/tests/integration/test_230_upload_queue_cutover.py
autonomous: true
requirements:
  - QUEUE-01
  - QUEUE-02
user_setup: []

must_haves:
  truths:
    - "POST /documents/upload inserts an ingestion_jobs record and returns HTTP 201 immediately (H-3: proven on /upload FIRST)"
    - "background_tasks.add_task(splice_document) is removed from documents.py, replacing ephemeral BackgroundTask with the durable queue"
    - "ingest_splice.py supports checkpointed resumption: resumes chunk embedding from progress.chunk_offset without re-embedding prior chunks (SC#4)"
    - "Existing upload UX, response schema, stage names, and counts are 100% unchanged (SC#5)"
    - "Simulated worker crash mid-ingest claims job, drops worker, runs stale sweeper, and completes all files with ZERO rows stuck in processing (SC#1 / G-1)"
    - "backend/tests/integration/test_230_upload_queue_cutover.py passes 100%"
  artifacts:
    - backend/app/services/ingest_splice.py
    - backend/app/api/documents.py
    - backend/tests/integration/test_230_upload_queue_cutover.py
  key_links:
    - "H-3: The queue is verified against /upload with 30 phases of existing test coverage before any connector touches it"
    - "Closes SEED-077's core gap: ingestion jobs survive worker process restarts"
---

<objective>
Cut over the `/documents/upload` route to enqueue durable `ingestion_jobs`, update `ingest_splice.py` with checkpointed resumption, and prove restart/lost-worker recovery (`QUEUE-01`, `QUEUE-02`, H-3, SC#1, SC#4, SC#5, G-1):
1. Enhance `backend/app/services/ingest_splice.py` to record stage checkpoints and batch chunk offsets in `ingestion_jobs.progress`, skipping already-embedded chunks upon resumption.
2. In `backend/app/api/documents.py`, cut over `upload_document` to insert an `ingestion_jobs` row and retire `background_tasks.add_task(splice_document)`. Preserve 100% of existing upload contracts (SC#5).
3. Author comprehensive integration test suite `backend/tests/integration/test_230_upload_queue_cutover.py` verifying upload queueing, lost-worker crash recovery (SC#1), burst throttling (SC#2), and checkpointed chunk resumption (SC#4).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/230-the-durable-ingestion-queue/230-CONTEXT.md
@backend/app/services/ingest_splice.py
@backend/app/api/documents.py
@backend/app/db/ingestion_jobs.py
</execution_context>

<threat_model>
<trust_boundaries>
Frontend Upload Form -> /documents/upload -> ingestion_jobs Table -> Worker Loop
</trust_boundaries>
<threats>
- threat_id: TM-230-08
  category: Denial of Service / Orphaned In-Flight Work
  component: splice_document & IngestionQueueService
  disposition: mitigated
  mitigation_plan: Record progress.chunk_offset after each chunk batch; on restart, the worker re-claims the job and continues from chunk_offset rather than starting over.
- threat_id: TM-230-09
  category: Regression / Broken Upload API Contract
  component: /documents/upload
  disposition: mitigated
  mitigation_plan: Return identical MintResult document row with HTTP 201, keeping status='pending' and all stages/counts unchanged (SC#5).
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Checkpointed Resumption to ingest_splice.py</name>
  <files>
    backend/app/services/ingest_splice.py
  </files>
  <read_first>
    backend/app/services/ingest_splice.py
    backend/app/db/ingestion_jobs.py
  </read_first>
  <action>
    In `backend/app/services/ingest_splice.py`:
    1. Update `splice_document` signature to accept optional `job_id: str | None = None` and `progress: dict | None = None`.
    2. Checkpoint recovery logic:
       - Read `chunk_offset = (progress or {}).get("chunk_offset", 0)`.
       - If `(progress or {}).get("stage") == "tables_embedded"`, skip table extraction and embedding.
       - When chunking text: slice `chunks[chunk_offset:]`.
       - After each batch of chunks is successfully embedded and inserted into `document_chunks`, if `job_id` is provided, update `progress.chunk_offset = current_offset` via `update_job_progress`.
    3. Finalize:
       - Execute authoritative recount of `document_chunks`.
       - Update document `status='completed'`.
       - If `job_id` provided, update job `stage='completed'`.
  </action>
  <verify>
    Verify module imports and signature:
    `backend/venv/Scripts/python -c "import app.services.ingest_splice as isp; import inspect; print(inspect.signature(isp.splice_document))"`
  </verify>
</task>

<task type="auto">
  <name>Task 2: Cut Over /documents/upload to Durable Queue (H-3)</name>
  <files>
    backend/app/api/documents.py
  </files>
  <read_first>
    backend/app/api/documents.py
    backend/app/db/ingestion_jobs.py
  </read_first>
  <action>
    In `backend/app/api/documents.py` at `upload_document` (around line 520-545):
    1. After `mint_result = await async_mint_document_row(...)`:
       If `mint_result.is_duplicate`:
         Return `mint_result.document` with HTTP 200 (preserving deduplication parity).
    2. Replace:
       `background_tasks.add_task(splice_document, ...)`
       With durable job enqueue:
       Insert row into `ingestion_jobs` (using asyncpg pool or supabase service client):
       - `document_id = doc["id"]`
       - `user_id = current_user["id"]`
       - `org_id = current_user.get("org_id")`
       - `status = 'pending'`
       - `next_run_at = now()`
    3. Keep `background_tasks.add_task(write_audit_entry, ...)` for the upload audit event.
    4. Set `response.status_code = status.HTTP_201_CREATED` and return `doc`.
  </action>
  <verify>
    Verify documents.py syntax:
    `backend/venv/Scripts/python -c "import app.api.documents; print('documents.py loaded')"`
  </verify>
</task>

<task type="auto">
  <name>Task 3: Author Integration Tests for Upload Queue Cutover & Worker Recovery</name>
  <files>
    backend/tests/integration/test_230_upload_queue_cutover.py
  </files>
  <read_first>
    backend/tests/integration/test_documents.py
  </read_first>
  <action>
    Author integration tests in `backend/tests/integration/test_230_upload_queue_cutover.py`:
    1. Test basic upload flow:
       - `POST /documents/upload` returns 201 and creates pending `ingestion_jobs` row.
       - Worker poll claims job and completes document to `status='completed'`.
    2. Test lost-worker restart recovery (SC#1 / G-1):
       - Upload file, claim job with `worker_id='dead-worker'`, simulate crash (process dies, claim not completed, claimed_at expires lease).
       - Run `reclaim_stale_ingestion_claims` or boot `IngestionQueueService`.
       - Job is reclaimed to `pending` and picked up by live worker, completing to `completed`.
       - Asserts ZERO stuck rows in `processing`.
    3. Test burst throttling (SC#2):
       - Upload 20 documents concurrently.
       - Verifies that concurrent active ingest tasks never exceed `INGEST_MAX_CONCURRENT_JOBS` (3).
       - All 20 documents complete successfully.
    4. Test checkpointed resumption (SC#4):
       - Document with 100 chunks fails at chunk 40.
       - On retry, chunks 0-39 are skipped and embedding starts at chunk 40.
    5. Test contract preservation (SC#5):
       - Asserts 201 response JSON matches existing schema, stage names ('extracting', 'completed') and counts.
  </action>
  <verify>
    Run integration tests:
    `backend/venv/Scripts/pytest tests/integration/test_230_upload_queue_cutover.py -v`
  </verify>
</task>

</tasks>

<acceptance_criteria>
- `POST /documents/upload` writes to `ingestion_jobs` and does not use ephemeral `BackgroundTask`.
- `ingest_splice.py` skips previously-embedded chunks when resumed from `chunk_offset`.
- `backend/tests/integration/test_230_upload_queue_cutover.py` passes 100%.
- SC#1, SC#2, SC#4, and SC#5 verified by tests including hard worker drop.
</acceptance_criteria>
