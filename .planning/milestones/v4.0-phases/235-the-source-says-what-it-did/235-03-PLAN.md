---
phase: 235-the-source-says-what-it-did
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/sketches/233-the-source-says-what-it-did/drive.cjs
  - frontend/src/components/sources/__generated__/sourceComposition.json
  - frontend/src/components/sources/sourceComposition.test.tsx
  - .planning/phases/235-the-source-says-what-it-did/235-BASELINE.md
autonomous: true
requirements: [SURF-02, LIB-10, SURF-03]

must_haves:
  truths:
    - "D-235-19: G-2 is honoured — the operator-approved sketch (winner variant B) is the acceptance bar, and all four surfaces are asserted together as COMPOSITION, not as vocabulary"
    - "The contract is IMPORTED from a generated in-package JSON, never transcribed — a hand-listed block array reintroduces exactly the staleness the emitter exists to prevent"
    - "The fence lands RED and the red run is the deliverable — a guard adopted green on a tree it never failed on has proved nothing about its own wiring"
    - "The fence is in NEITHER `TARGETS` nor `BASELINE` while red — the gate's contract is zero failing, forever, and Plan 12 adopts both knobs in one commit"
    - "The emitter is region-scoped — `drive.cjs`'s own `region()` helper exists at `:356-362` and `--emit` ignores it, which is why §2's four per-screen lists are byte-identical 104-entry artifacts"
  artifacts:
    - path: "frontend/src/components/sources/__generated__/sourceComposition.json"
      provides: "region-scoped block kinds, button labels and the 27-key COPY object, per screen"
      contains: "source-line"
    - path: "frontend/src/components/sources/sourceComposition.test.tsx"
      provides: "the six-section composition fence, deliberately red on landing"
      min_lines: 220
    - path: ".planning/phases/235-the-source-says-what-it-did/235-BASELINE.md"
      provides: "the verbatim RED run"
  key_links:
    - from: "frontend/src/components/sources/sourceComposition.test.tsx"
      to: "frontend/src/components/sources/__generated__/sourceComposition.json"
      via: "in-package import, never a ?raw reach into .planning/sketches/"
      pattern: "__generated__/sourceComposition.json"
---

<objective>
Make the operator-approved sketch machine-readable, then write the fence that can name a MISSING block — and
land it red.

Purpose: `sketchComposition.test.tsx:5-9` records this project's own measurement — Phase 217 shipped against a
contract that pinned WORDS, *"and every one of those assertions was green while four of the five tabs carried
none of the sketch's furniture. A contract that cannot name a MISSING BLOCK cannot tell 'built' from 'not built
yet'."* The operator's verdict on sketch 218 was *"nothing at all like what we designed"*. This plan is the
mechanism that stops that happening a third time.

Output: a `--emit-json` arm on the sketch's own driver, a region-scoped `sourceComposition.json` in-package, a
six-section fence suite, and `235-BASELINE.md` recording its red run verbatim.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/235-the-source-says-what-it-did/235-VALIDATION.md
@.planning/phases/235-the-source-says-what-it-did/235-RESEARCH.md
@.planning/phases/235-the-source-says-what-it-did/235-PATTERNS.md
@.planning/sketches/233-the-source-says-what-it-did/BUILD-CONTRACT.generated.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
The generated artifact's SHAPE is fixed by the shipped 218 precedent
(`frontend/src/components/library/__generated__/sketchComposition.json`, verified by
`sketchComposition.test.tsx:194-199`):

  {
    "<screen>": {
      "blocks":  [ { "kind": string, "heading": string | null, "atoms": string[] } ],
      "buttons": string[]
    },
    "copy": { ...the sketch's 27-key COPY object, functions serialised as template strings... }
  }

Screens for this phase: `"sources"` (the Ingestion-tab body), `"health"` (the Health-tab attention section),
`"rail"` (the badge + popover).

⛔ THE HOOK CONVENTION IS `data-testid="<screen>-<block-kind>"`, e.g. `sources-source-card`, `rail-badge`,
`health-attention-row`. The BUILD-CONTRACT §2 sentence says `data-block`; `sketchComposition.test.tsx:34-38`
forbids `data-block` in the build and records that `data-testid` is the house convention at ~1500 occurrences.
**The contract's block NAMES bind; its attribute spelling does not.** This deviation is recorded, not silent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a region-scoped `--emit-json` arm to the sketch driver</name>
  <files>.planning/sketches/233-the-source-says-what-it-did/drive.cjs, frontend/src/components/sources/__generated__/sourceComposition.json</files>
  <read_first>
    - `.planning/sketches/233-the-source-says-what-it-did/drive.cjs` — `region(h, blk)` at `:356-362` (the helper
      that already exists), the `--emit` arm at `:394-447` (which calls the UNSCOPED `blocks(h)` / `actions(h)`
      over the whole page), and the warning the same file carries at `:355-361`: *"an unscoped `blocks()`/
      `actions()` over a whole page counts the OTHER tab's controls and TWELVE cards' run rows. Three assertions
      passed against the wrong region before this helper existed; scoping is not tidiness, it is correctness."*
    - `.planning/sketches/233-the-source-says-what-it-did/index.html:309-390` — the live `COPY` object, 27 keys.
    - `frontend/src/components/library/__generated__/sketchComposition.json` (first ~900 bytes) — the output shape.
    - `235-RESEARCH.md` C-8 and C-10 — why §2's 104-entry lists must NOT be transcribed, and why no JSON companion
      exists yet.
  </read_first>
  <action>
    Add an `--emit-json` arm to `drive.cjs` beside `--emit`. It must call `region(h, blk)` for each screen rather
    than the unscoped `blocks(h)` / `actions(h)` — that single difference is the whole point of the arm.

    For each of the three screens (`sources`, `health`, `rail`), emit the DISTINCT block-kind set found in that
    region (deduplicated, in first-appearance order), the button labels found in that region, and — once, at the
    top level — the sketch's whole `COPY` object.

    Write the result to `frontend/src/components/sources/__generated__/sourceComposition.json` with a two-space
    indent and a trailing newline. ⛔ It is an in-package COPY, never a `?raw` reach across the repo boundary into
    `.planning/sketches/` — `/gsd:complete-milestone` archives that directory, and the suite must survive it.

    Run `node .planning/sketches/233-the-source-says-what-it-did/drive.cjs --emit-json` and commit the generated
    file. Do NOT hand-edit the JSON afterwards; if it is wrong, fix the emitter and regenerate.

    The variant-B distinct set the JSON must contain for `sources` + `health` + `rail` combined is 20 kinds:
    `rail-item · rail-item-library · badge · popover · pop-item · instance-statement · tab-ingestion · tab-health ·
    body-ingestion · source-line · source-card · outcome · stopped-sentence · history · run · quiet-fold ·
    fail-reason · body-health · attention-list · attention-row`. If the emitter produces a different set, the
    emitter is wrong (or the sketch changed) — reconcile before proceeding, and say which.
  </action>
  <verify>
    <automated>node -e "const c=require('./frontend/src/components/sources/__generated__/sourceComposition.json');const kinds=new Set(Object.values(c).flatMap(s=>(s&&s.blocks)?s.blocks.map(b=>b.kind):[]));if(kinds.size<15)throw new Error('non-vacuity floor: only '+kinds.size+' kinds');['source-line','source-card','quiet-fold','instance-statement','attention-row','badge'].forEach(k=>{if(!kinds.has(k))throw new Error('missing kind '+k)});if(Object.keys(c.copy||{}).length<27)throw new Error('COPY has '+Object.keys(c.copy||{}).length+' keys, expected >= 27');console.log('contract OK — '+kinds.size+' kinds, '+Object.keys(c.copy).length+' copy keys')"</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/sketches/233-the-source-says-what-it-did/drive.cjs --emit-json` exits 0 and rewrites the JSON
      byte-identically when run twice (idempotent).
    - The JSON's combined distinct block-kind count is ≥ 15 (VALIDATION's non-vacuity floor) and contains
      `source-line` — the marker that variant **B** was emitted, not variant A.
    - `grep -c "region(" .planning/sketches/233-the-source-says-what-it-did/drive.cjs` increases, and the
      `--emit-json` arm contains no call to a bare `blocks(h)` / `actions(h)`.
    - `frontend/src/components/sources/__generated__/sourceComposition.json` is committed and no test imports the
      sketch directory via `?raw`.
  </acceptance_criteria>
  <done>The operator-approved contract is a file the build's suite can import, scoped per region, with all 27 copy keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write `sourceComposition.test.tsx` — six sections, and it lands RED</name>
  <files>frontend/src/components/sources/sourceComposition.test.tsx</files>
  <read_first>
    - `frontend/src/components/library/__tests__/sketchComposition.test.tsx` in full (527 L) — copy it section by
      section: the docblock at `:5-21` (why it is red, why it is in neither knob), the in-package import rule at
      `:22-33`, the PARTIAL barrel mock at `:48-91` and its reason, the non-vacuity floor at `:229-252`, the
      positive controls at `:314-335`, the `hook()` helper and mount harness at `:205-227` / `:398-419`, the
      per-control loop at `:494-527`.
    - `frontend/src/components/sources/WatchedFoldersSection.test.tsx:12-46` — the TWO separate `vi.mock` calls.
      ⛔ `@/lib/api/sources` is NOT in the `@/lib/api` barrel (measured); it must be mocked separately.
    - `frontend/src/components/library/IngestionTab.tsx:157-160`, `:196`, `:228-230` — `WatchedFoldersSection`
      mounts inside the `add-files` sub-tab, so every source block maps to the `"Add files"` sub-tab in the
      mount harness.
    - `.planning/phases/235-the-source-says-what-it-did/235-VALIDATION.md` §"The composition fence" — the six
      sections and the exact counts.
    - `.planning/sketches/233-the-source-says-what-it-did/BUILD-CONTRACT.generated.md` §"Counts that must hold"
      and §4 (the four invariants).
  </read_first>
  <behavior>
    §1 non-vacuity floor — the imported JSON is an object, has ≥ 15 distinct block kinds, and
    `JSON.stringify(CONTRACT).length > 1200`. *A moved import resolves EMPTY rather than throwing, and a contract
    of zero blocks makes every case below pass over nothing.*

    §2 positive controls — 2-3 cases that are GREEN in the red baseline: `IngestionTab` mounts and
    `screen.getByTestId("ingestion-tab")` is present; `LibraryPage`'s `<h1>` reads "Library"; the rail renders a
    Library nav button. *A run in which EVERY case is red is indistinguishable from a broken mount harness.*

    §3 every block — one `it()` per kind from the distinct-20 set:
    `expect(screen.getByTestId(hook(screen, kind))).toBeInTheDocument()`.

    §4 every named control — one `it()` per label in the contract's `buttons`, via
    `getByRole("button", { name: label })`: `badge · open-health · fix · sync-now · toggle-history · toggle-quiet ·
    report-source · go-to-source`.

    §5 the counts that must hold — variant **B**: **9** `source-line` + **3** `source-card` (every source
    accounted for exactly once); exactly **2** `attention-row`; per open card, **3** `run` collapsed vs **17**
    expanded; and `instance-statement` appears **exactly once** (`getAllByTestId(...).length === 1`), never per row.

    §6 the §4 invariants as SOURCE fences, over `?raw` imports of the shipped files — the string `"scheduled"`
    appears in NEITHER `backend/app/api/sources.py` NOR
    `frontend/src/components/sources/WatchedFoldersSection.tsx`; `"instantly"` and `"on change"` appear in neither;
    `--color-danger` is never applied to a source state. ⚠ Each source fence carries its own non-vacuity assertion
    (the `?raw` import is non-empty and contains a known anchor) — a regex that matched nothing yields "" and
    would look green.
  </behavior>
  <action>
    Create `frontend/src/components/sources/sourceComposition.test.tsx`. Open with a docblock that states, in this
    order: (a) it is SUPPOSED to be red when it lands and its red run is the deliverable, recorded verbatim in
    `.planning/phases/235-the-source-says-what-it-did/235-BASELINE.md`; (b) ⛔ it is in NEITHER `TARGETS` nor
    `BASELINE` in `scripts/vitest-count-gate.cjs` while red, because the gate's contract is zero failing forever
    and Plan 12 adopts both knobs in one commit at the gate's own printed figure; (c) the hook convention is
    `data-testid="<screen>-<kind>"` and ⚠ `data-block` is the SKETCH's marker and must never appear in the build —
    naming the BUILD-CONTRACT §2 deviation explicitly; (d) the contract is IMPORTED, never transcribed.

    Import the contract as `import CONTRACT from "./__generated__/sourceComposition.json"`.

    Mocking: a PARTIAL mock of `@/lib/api` via `importOriginal` (a full replacement has to declare every symbol
    every child imports, and the one it forgets throws at MOUNT about a missing export rather than about the
    missing block — the failure mode Phase 196-08 recorded across nine suites, and the one thing that would make
    this suite's RED unreadable), PLUS a separate `vi.mock("@/lib/api/sources", ...)` factory declaring
    `listWatches`, `triggerWatchSync`, `updateWatch`, `deleteWatch`, `purgeWatchFiles`, `createWatch`,
    `listSyncRuns`, `getSourceHealth`.

    Fixture: 12 watches — 9 healthy, 2 stopped (one `token_revoked`, one `folder_gone`), 1 degraded — and one
    watch carrying 17 run rows of which 14 are quiet, matching the sketch's own fixture so §5's counts are the
    sketch's numbers rather than invented ones. Use only the MEASURED `last_status` set
    `{running, success, failed, paused}` — ⚠ the shipped `WatchedFoldersSection.test.tsx` fixture uses
    `"completed"` and `"disconnected"`, which production never writes.

    Mount harness: `userEvent.click` on the tab (Radix Tabs needs pointer events — `fireEvent` is measured not to
    work), then the `add-files` sub-tab for every `sources` block.

    ⛔ Do NOT touch `scripts/vitest-count-gate.cjs` in this plan.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/sources/sourceComposition.test.tsx --maxWorkers=2 --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - The suite RUNS (exit code non-zero is expected and correct) — it does not ERROR at import or mount.
    - §1's four non-vacuity cases and §2's positive controls are **GREEN** in that same run. If any §2 case is red,
      the harness is broken and the baseline proves nothing — fix the harness, not the assertion.
    - At least 20 §3 cases are RED, naming missing `data-testid`s by their exact hook string.
    - `grep -c "data-block" frontend/src/components/sources/sourceComposition.test.tsx` may be non-zero only inside
      the docblock's prohibition sentence; it must appear in no `getByTestId` call.
    - `git diff --name-only` for this task names no `scripts/vitest-count-gate.cjs`.
  </acceptance_criteria>
  <done>A fence exists that can name a missing block, its harness is proven by green positive controls, and it is red for the right reason.</done>
</task>

<task type="auto">
  <name>Task 3: Record the RED run verbatim in `235-BASELINE.md`</name>
  <files>.planning/phases/235-the-source-says-what-it-did/235-BASELINE.md</files>
  <read_first>
    - `.planning/phases/217.1-the-library-exactly-as-sketched/217.1-BASELINE.md` — the precedent for what a red
      baseline record contains and how it is worded.
    - `.planning/phases/235-the-source-says-what-it-did/235-VALIDATION.md` §"The composition fence" step 3.
  </read_first>
  <action>
    Create `235-BASELINE.md` with frontmatter (`phase: 235`, `kind: red-baseline`, `recorded: <date>`) and four
    sections:

    1. **The command and its verbatim output** — the full `npx vitest run
       src/components/sources/sourceComposition.test.tsx --maxWorkers=2` tail, exit code included, pasted rather
       than summarised.
    2. **What was GREEN and why that matters** — the §1 non-vacuity cases and the §2 positive controls, listed by
       name, with the sentence: *a run in which every case is red is indistinguishable from a broken mount harness.*
    3. **What was RED, grouped by the wave that will turn it green** — each failing block kind or control mapped to
       its owning plan (`source-line`/`source-card`/`outcome`/`stopped-sentence`/`history`/`run`/`quiet-fold`/
       `fail-reason` → Plan 10; `instance-statement` → Plan 10; `rail-item*`/`badge`/`popover`/`pop-item` → Plan 09;
       `body-health`/`attention-list`/`attention-row` → Plan 11; `tab-ingestion`/`tab-health` → Plan 08).
    4. **The adoption contract** — that neither knob is set while red, and that Plan 12 sets both in ONE commit at
       the gate's own printed `— N new` figure, never a guessed number.

    Also record the two MEASURED gate baselines this phase compares against, verbatim, so no later plan quotes
    CLAUDE.md's stale figures: vitest `total 7544 · failed 0 · pinned total 6814` / `count gate OK — 227/227
    pinned files present`, exit 0; backend `72 failed, 3785 passed, 2 xfailed, 2 xpassed`, 0 collection errors —
    with the note that 72 is PRE-EXISTING (234 measured 72 on its merge base) and the phase's rule is **no NEW
    failure above 72**.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/235-the-source-says-what-it-did/235-BASELINE.md','utf8');['sourceComposition.test.tsx','7544','6814','227/227','72 failed','no NEW failure above 72','TARGETS','BASELINE'].forEach(t=>{if(!s.includes(t))throw new Error('baseline record missing: '+t)});if(s.length<1500)throw new Error('baseline too thin to be a verbatim record');console.log('baseline record OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `235-BASELINE.md` contains the pasted vitest output including the failing case names, not a paraphrase.
    - Every red block kind is mapped to an owning plan number.
    - Both measured gate baselines appear verbatim with their measurement date.
  </acceptance_criteria>
  <done>The red run exists as a record, so a later green run means the fence was wired rather than never fired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sketch artifact → build test suite | a generated file crossing from `.planning/` into `frontend/src/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-235-09 | Tampering | a hand-edited `sourceComposition.json` drifting from the approved sketch | mitigate | it is generated by `drive.cjs --emit-json` and the verify step re-runs the emitter; the acceptance criterion requires idempotent regeneration |
| T-235-10 | Repudiation | a fence that passes over an empty contract | mitigate | §1's non-vacuity floor (≥ 15 kinds, > 1200 chars) asserted BEFORE any loop |
| T-235-11 | Denial of service | a deliberately red suite entering the shared gate and reddening every later plan | mitigate | ⛔ neither `TARGETS` nor `BASELINE` is touched here; adoption is Plan 12's, in the final wave |
| T-235-SC | Tampering | package installs | n/a | **No package is installed by this plan.** No legitimacy gate applies |
</threat_model>

<verification>
- `node .planning/sketches/233-the-source-says-what-it-did/drive.cjs --emit-json` — exit 0, idempotent.
- `cd frontend && npx vitest run src/components/sources/sourceComposition.test.tsx --maxWorkers=2` — RUNS, §1 and §2
  green, ≥ 20 §3 cases red.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — must still print
  `count gate OK` with **0 failing**, because the new suite is in neither knob. If it does not, the suite was
  adopted by accident: remove it from the knob and re-run.
</verification>

<success_criteria>
- The approved sketch is an importable, region-scoped, in-package JSON carrying all 27 copy keys.
- A six-section fence exists, its harness is proven green, and ≥ 20 composition cases are red with named hooks.
- The red run is recorded verbatim in `235-BASELINE.md` with each failure mapped to its owning plan.
- The shared count gate is unaffected — it still reads `count gate OK`, 0 failing.
</success_criteria>

<output>
Create `.planning/phases/235-the-source-says-what-it-did/235-03-SUMMARY.md` when done.
</output>
