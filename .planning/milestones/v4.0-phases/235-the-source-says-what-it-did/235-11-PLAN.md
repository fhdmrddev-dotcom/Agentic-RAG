---
phase: 235-the-source-says-what-it-did
plan: 11
type: execute
wave: 4
depends_on: ["235-04", "235-06", "235-08"]
files_modified:
  - frontend/src/components/library/SourcesAttentionSection.tsx
  - frontend/src/components/library/HealthTab.tsx
  - frontend/src/components/library/__tests__/SourcesAttentionSection.test.tsx
  - frontend/src/pages/LibraryPage.tsx
autonomous: true
requirements: [SURF-03, LIB-10]

must_haves:
  truths:
    - "D-235-01: the Health tab carries the DETAIL half of SURF-03 — option B (the shell signal) plus option A (this section). Option A alone was rejected, and this section does not pretend otherwise"
    - "D-235-17: Health lists ONLY what needs attention and routes back to the card — `Ingestion = every source and everything it did; Health = only what is wrong`. Rendering the full history here too would be two homes for one truth, even with a shared component"
    - "The row's action is `Go to source`, not a repair — the fix has ONE home, on the source card (the sketch's first fork was measured unfeelable and settled by rule)"
    - "An instance with nothing wrong says so — `Every source is reading.` — rather than rendering an empty list"
    - "The section fails quiet: a failed fetch leaves the rest of the Health tab intact, matching the shipped sibling idiom"
  artifacts:
    - path: "frontend/src/components/library/SourcesAttentionSection.tsx"
      provides: "body-health / attention-list / attention-row blocks and the go-to-source control"
    - path: "frontend/src/components/library/HealthTab.tsx"
      provides: "one sibling mount, zero branches"
      contains: "SourcesAttentionSection"
  key_links:
    - from: "frontend/src/components/library/SourcesAttentionSection.tsx"
      to: "useSourceAttention"
      via: "the same one reader every other surface uses"
      pattern: "useSourceAttention"
    - from: "frontend/src/components/library/HealthTab.tsx"
      to: "LibraryPage::handleGoToSource"
      via: "onGoToSource threaded from the page boundary"
      pattern: "onGoToSource"
---

<objective>
Give the Health tab the detail half of SURF-03: a list of only the sources that need attention, each one a hop
back to the card that can fix it.

Purpose: D-235-01 is explicit that the Health tab alone does NOT satisfy `SURF-03` — it is still a page a person
must open, and closing the requirement against it would close it against its own sentence. The rail badge (Plan
09) is what reaches a person; this section is where they land when they follow it. D-235-17 fixes the division
so it scales: **Ingestion = every source and everything it did; Health = only what is wrong.**

Output: a self-fetching attention section mounted as a sibling in `HealthTab`, and the page-boundary wiring that
lets its rows hop to the Ingestion card.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/235-the-source-says-what-it-did/235-CONTEXT.md
@.planning/phases/235-the-source-says-what-it-did/235-RESEARCH.md
@.planning/phases/235-the-source-says-what-it-did/235-PATTERNS.md
@.planning/sketches/233-the-source-says-what-it-did/BUILD-CONTRACT.generated.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@.planning/phases/235-the-source-says-what-it-did/235-08-SUMMARY.md

<interfaces>
Consumed:
  hooks/useSourceAttention.ts (Plan 04) — { stopped, readerRunning, pollIntervalSeconds, loading }
  sourceHealthVocabulary.ts (Plan 02) — COPY.attentionTitle, COPY.attentionEmpty, COPY.goToSource,
    COPY.roster(total, need), SENTENCE_FOR_CAUSE
  LibraryPage::handleGoToSource(watchId) (Plan 08) — dispatches SELECT_TAB "ingestion" then scrolls
    `#watch-card-${watchId}` into view

Declared here:
  export function SourcesAttentionSection({ onGoToSource }: { onGoToSource?: (watchId: string) => void })
  HealthTab gains: onGoToSource?: (watchId: string) => void      // declared AND forwarded, nothing else

Contract hooks emitted (screen prefix `health`): `health-body-health`, `health-attention-list`,
`health-attention-row`. Control: `go-to-source`.
⚠ The house convention is `data-testid`; `data-block` must never appear in the build. ⭐ `HealthTab.tsx:91`
already emits `data-testid="health-coverage-ring"` — literally `<screen>-<block-kind>`, which is what the
composition fence reads.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The attention section — only what is wrong, and a hop to the fix</name>
  <files>frontend/src/components/library/SourcesAttentionSection.tsx, frontend/src/components/library/__tests__/SourcesAttentionSection.test.tsx</files>
  <read_first>
    - `frontend/src/components/library/HealthTab.tsx:70-110` — the self-fetch + `.catch(() => setX(null))`
      fail-quiet idiom and the `animate-pulse` skeleton that pairs with it; `:91` for the block-hook convention
      already live in this file.
    - ⚠ `235-PATTERNS.md` §"No Analog Found" partial note — the closest structural twin is
      `frontend/src/components/health/CheckedQueriesSection.tsx` (a self-fetching section that lifts a count to
      its parent via `onTotalChange`, mounted at `HealthTab.tsx:182`). Read it before choosing this section's
      internal composition.
    - `.planning/sketches/233-the-source-says-what-it-did/BUILD-CONTRACT.generated.md` §1
      (`attentionTitle` = `"Sources needing attention"`, `attentionEmpty` = `"Every source is reading."`,
      `goToSource` = `"Go to source"`, `roster(total, need)`), §2 (`body-health` → `attention-list` →
      `attention-row`), §"Counts that must hold" (one `attention-row` per stopped source — **2** in the fixture),
      §3 (⛔ the Health row says **Go to source**; it carries no repair).
    - `.planning/seeds/SEED-248-a-surface-that-is-still-loading-must-say-so.md` — the loading state must SAY so.
  </read_first>
  <behavior>
    - With two stopped sources it renders `health-attention-list` once and exactly **2** `health-attention-row`s,
      each naming the folder and carrying `SENTENCE_FOR_CAUSE[cause](connectionName)`.
    - Each row offers exactly one control named `COPY.goToSource`, which calls `onGoToSource(watchId)` once.
      ⛔ No row offers Reconnect / Pick a different folder / Retry now — asserted negatively by name, because the
      fix has ONE home and it is the source card.
    - With zero stopped sources it renders `COPY.attentionEmpty` — `"Every source is reading."` — and NO
      `attention-list`. An empty list is not an answer.
    - While `loading` is true it says so (a named loading element), never an empty list that reads as "all clear"
      — the difference between "nothing is wrong" and "we have not looked yet" is the whole of `SEED-248`.
    - A rejected fetch does not throw and does not blank the section — it renders the same honest
      not-yet-known state, and the rest of `HealthTab` is unaffected (asserted by mounting `HealthTab` with a
      rejecting mock and finding the coverage ring still present).
    - When `onGoToSource` is absent the control is absent — never a dead click.
    - Non-vacuity FIRST: the suite asserts the fixture has ≥ 2 stopped sources and ≥ 2 distinct causes before any
      property rests on it.
  </behavior>
  <action>
    Create `SourcesAttentionSection.tsx` consuming `useSourceAttention()` — ⛔ the SAME hook the rail uses, never a
    second fetch and never a client-side re-derivation; D-235-05 puts the threshold on the server precisely so
    this section and the badge cannot disagree.

    Emit the three contract blocks under the `health-` screen prefix. Every string comes from
    `sourceHealthVocabulary`. Use `--color-warning` for the attention mark; ⛔ `--color-danger` is never applied to
    a source state. Per the design skill the state is a glyph + label + colour, never colour alone.

    Follow `HealthTab`'s shipped section idiom: static import (⚠ correction A-1 — `HealthTab` uses `lazy` for
    exactly ONE chart and its four section siblings are static), self-fetch through the hook, fail-quiet, and an
    `animate-pulse` skeleton for the loading arm.

    Create `SourcesAttentionSection.test.tsx` covering every bullet in `<behavior>`, mocking `@/lib/api/sources`
    separately from `@/lib/api` (it is not in the barrel) and mocking `@/lib/api` PARTIALLY via `importOriginal`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library/__tests__/SourcesAttentionSection.test.tsx --maxWorkers=2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "Reconnect|Pick a different folder|Retry now" frontend/src/components/library/SourcesAttentionSection.tsx`
      returns nothing.
    - `grep -n "color-danger" frontend/src/components/library/SourcesAttentionSection.tsx` returns nothing.
    - `grep -cE '"[A-Z][a-z]+ [a-z]' frontend/src/components/library/SourcesAttentionSection.tsx` finds no
      user-facing sentence literal — all copy resolves through the vocabulary leaf.
    - Named cases exist for the empty state, the loading state and the rejected-fetch state — three distinct
      cases, because they are three distinct truths.
    - `npx vitest run src/components/library/__tests__/SourcesAttentionSection.test.tsx --maxWorkers=2` — 0 failed,
      ≥ 8 passed.
  </acceptance_criteria>
  <done>Health lists only what is wrong, says so honestly when nothing is, and hands the person back to the one place that fixes it.</done>
</task>

<task type="auto">
  <name>Task 2: One sibling mount in HealthTab, and the page-boundary wiring</name>
  <files>frontend/src/components/library/HealthTab.tsx, frontend/src/pages/LibraryPage.tsx</files>
  <read_first>
    - `frontend/src/components/library/HealthTab.tsx:87-183` — the composition (ring + chart row → 5 stat tiles →
      `HealthSignalChips` → `HealthDocumentBars` → `CheckedQueriesSection`), and `:183` — the sibling slot the new
      section takes, after `CheckedQueriesSection` and before the closing `</div>`.
    - `frontend/src/pages/LibraryPage.tsx` — `handleGoToSource` (added by Plan 08) and the `<HealthTab />` mount
      site; `:705-735` for the surrounding dispatch idiom.
    - `.planning/phases/235-the-source-says-what-it-did/235-08-SUMMARY.md` — ⚠ it records WHICH option Plan 08 took
      for the not-yet-existing `onGoToSource` prop. Reconcile with it rather than assuming; if Plan 08 already
      declared the prop, this task only consumes it.
    - `frontend/src/components/library/IngestionTab.tsx:220-231` — the one-import-one-mount-zero-branches comment
      whose argument shape is reused here for G-5.
  </read_first>
  <action>
    In `HealthTab.tsx`: add `onGoToSource?: (watchId: string) => void` to its props (if Plan 08 did not already),
    ONE static import, and ONE mount of `<SourcesAttentionSection onGoToSource={onGoToSource} />` at `:183`.
    ⛔ Zero branches enter `HealthTab` — G-5 honoured by construction. Add the comment stating that, reusing the
    `ConnectedSourceSection` argument shape.

    In `LibraryPage.tsx`: pass `onGoToSource={handleGoToSource}` at the `<HealthTab />` mount site. That is the
    whole edit — the handler itself was added by Plan 08 at the page boundary, where `onOpenQueue` (`:708`) and
    `ReembedSearchPointer`'s `onViewProgress` (`:717-726`) already live.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library src/pages/__tests__ --maxWorkers=2</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat frontend/src/components/library/HealthTab.tsx` shows a small diff: one import, one prop,
      one mount — no new `useState`, no new `useEffect`, no new branch.
    - `grep -c "SourcesAttentionSection" frontend/src/components/library/HealthTab.tsx` returns 2 (import + mount).
    - `grep -n "onGoToSource={handleGoToSource}" frontend/src/pages/LibraryPage.tsx` matches once.
    - An end-to-end case (in `SourcesAttentionSection.test.tsx` or `LibraryPage.initialTab.test.tsx`) mounts
      `LibraryPage initialTab="health"`, clicks `Go to source`, and asserts the Ingestion tab becomes selected —
      the SURF-03 route's last hop, exercised rather than assumed.
    - `src/components/library` and `src/pages/__tests__` suites pass at their prior counts — record before/after.
  </acceptance_criteria>
  <done>The badge's destination exists, and following it lands on the card that can fix the source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server verdict → Health surface | the same polled response rendered a second way |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-235-37 | Spoofing | the Health row and the rail badge disagreeing about what is stopped | mitigate | both read `useSourceAttention` — one hook, one server verdict, zero client derivation (D-235-05) |
| T-235-38 | Information disclosure | a cause sentence exposing provider internals in the Health list | mitigate | rows render `SENTENCE_FOR_CAUSE`, never `last_error`; the vocabulary leaf's deny-by-default pass-through is the control |
| T-235-39 | Repudiation | an empty list reading as "all clear" while the fetch has not returned | mitigate | three distinct states — loading, empty, failed — each with its own named case (SEED-248) |
| T-235-SC | Tampering | package installs | n/a | **No package is installed by this plan.** No legitimacy gate applies |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/library src/pages/__tests__ --maxWorkers=2` — 0 failed.
- `npx tsc -p tsconfig.app.json --noEmit` — unchanged error count.
- `cd frontend && npx vitest run src/components/sources/sourceComposition.test.tsx --maxWorkers=2` — record which
  previously-red cases are now green. After Plans 09, 10 and 11 land, the fence is expected to be fully green;
  if any case is still red, name it and its owner in the summary rather than adopting the knobs around it.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — `count gate OK`.
  ⚠ `src/components/library` is NOT a TARGETS directory entry (measured — only `src/landing` and
  `src/components/workflows` are). The new suite is pinned by Plan 12, not here.
</verification>

<success_criteria>
- The Health tab lists only sources needing attention, one row each, with `Go to source` and no repair control.
- Nothing wrong renders `Every source is reading.`, and a pending fetch says it is pending.
- `HealthTab` gained one import, one prop and one mount, and no branch.
- The full SURF-03 route — badge → popover → Health → card — is exercised end to end by a test.
</success_criteria>

<output>
Create `.planning/phases/235-the-source-says-what-it-did/235-11-SUMMARY.md` when done. Record the composition
fence's remaining red cases, if any, with their owning plan.
</output>
