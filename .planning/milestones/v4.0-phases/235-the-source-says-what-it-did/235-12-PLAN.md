---
phase: 235-the-source-says-what-it-did
plan: 12
type: execute
wave: 5
depends_on: ["235-03", "235-05", "235-07", "235-09", "235-10", "235-11"]
files_modified:
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .planning/ROADMAP.md
  - .planning/seeds/SEED-231-nobody-is-told-an-approval-is-waiting.md
  - .planning/reported-bugs/watch-sync-button-reports-success-for-work-that-cannot-happen.md
  - .planning/phases/235-the-source-says-what-it-did/235-BASELINE.md
autonomous: true
requirements: [SURF-02, LIB-10, SURF-03]

must_haves:
  truths:
    - "The composition fence is adopted GREEN, in ONE commit, with BOTH knobs — `TARGETS` decides what RUNS and `BASELINE` decides what is GUARDED, and a BASELINE key naming a path that does not exist makes the gate ERROR (exit 2) rather than fail"
    - "Every pinned figure is the gate's OWN printed `— N new` number, never a guessed one and never one copied from a plan summary"
    - "D-235-02: option C (email on permanent failure) is DEFERRED with its two-part trigger recorded ON `SEED-231` — either a customer reports learning about a dead watch from a stale answer, OR the product gains an app-owned mailer. Not done, not dropped"
    - "D-235-18 is DISCHARGED, not implemented here — `BUG-260906-01` was closed by quick task `260906-5qd`; this plan verifies the discharge rather than assuming it, because a status field IS the index"
    - "D-235-21: this phase records the MEASURED value of every process-level flag it depends on — `WATCH_PROCESS_ENABLED` reads `true` in the operator's `backend/.env` and `false` in `deploy/onebox.env.example`, so a fresh deployment has the reader OFF and the instance statement is load-bearing rather than decorative"
    - "⚠ Six of this phase's files had NO hot-file ledger row at all — `NavPanel.tsx` at 10 phases and `App.tsx` at 22 — so G-5 could never have fired on either. Rows and their `docs/HOT-FILE-LEDGER.md` sections land in the SAME commit"
    - "Ledger triples are RE-DERIVED at this phase's final commit, never copied — a cell written mid-phase was falsified by the same phase's own later commit at 231, and again at 232"
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "both knobs for every new suite, in one commit"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "sections for the six absent files and re-derived triples for the stale ones"
    - path: ".planning/phases/235-the-source-says-what-it-did/235-BASELINE.md"
      provides: "the red run, now paired with the green adoption run"
  key_links:
    - from: "scripts/vitest-count-gate.cjs::TARGETS"
      to: "scripts/vitest-count-gate.cjs::BASELINE"
      via: "one commit, matching entries, checked against the array not assumed"
      pattern: "sourceComposition.test.tsx"
    - from: "CLAUDE.md hot-file ledger table"
      to: "docs/HOT-FILE-LEDGER.md"
      via: "the same-commit sync rule"
      pattern: "NavPanel.tsx"
---

<objective>
Close the phase's guardrails: adopt the fence green with both knobs, give six invisible files their first ledger
rows, and record every deferral and every measured flag value so nothing silently becomes a deletion.

Purpose: three of this project's own recurring failures converge here. (1) A guard adopted green on a tree it
never failed on has proved nothing — so this plan pairs the red baseline with the green adoption. (2) A
`BASELINE` key without its `TARGETS` line makes the gate ERROR rather than fail, and `src/components/sources`,
`src/components/library` AND `src/components/layout` are all file-level — measured, not assumed. (3) A ledger
cell written mid-phase was falsified by the same phase's later commit at 231, and the identical thing happened
at 232 — so the triples are re-derived HERE, at the final commit.

Output: a green gate at the gate's own numbers, six new ledger rows, an updated seed register, and the
deferral record SURF-03's email option needs to stay revivable.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/235-the-source-says-what-it-did/235-CONTEXT.md
@.planning/phases/235-the-source-says-what-it-did/235-RESEARCH.md
@.planning/phases/235-the-source-says-what-it-did/235-PATTERNS.md
@.planning/phases/235-the-source-says-what-it-did/235-VALIDATION.md
@.planning/phases/235-the-source-says-what-it-did/235-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt every new suite — both knobs, one commit, the gate's own numbers</name>
  <files>scripts/vitest-count-gate.cjs, .planning/phases/235-the-source-says-what-it-did/235-BASELINE.md</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs:3165-3198` — Phase 233's own comment block, the template for what a new pin
      must SAY, including its recorded lesson: *"this comment first read `src/components/library` IS a directory
      entry so the file already runs. **It is not.**"* — and `:3197` (`"WatchedFoldersSection.test.tsx": 6,`), the
      BASELINE insertion point; the object closes at `:3198`.
    - `scripts/vitest-count-gate.cjs:4470-4493` — the TARGETS insertion point (`:4492` is
      `"src/components/sources/WatchedFoldersSection.test.tsx",`; the array closes at `:4493`).
    - `scripts/vitest-count-gate.cjs:3166-3175` — the recorded reason BOTH knobs go in together: *"a `BASELINE`
      key naming a path that does not yet exist makes this gate ERROR (exit 2) rather than fail."*
    - `frontend/src/components/library/__tests__/sketchComposition.test.tsx:12-21` — the adoption rule the fence
      itself carries: the phase's FINAL wave adopts it, once each wave has turned its own blocks green.
    - `.planning/phases/235-the-source-says-what-it-did/235-BASELINE.md` — the red run this plan closes.
    - Each plan summary's recorded per-file counts (`235-08` for `librarySelection.test.ts`, `235-10` for
      `WatchedFoldersSection.test.tsx`). ⚠ Those are inputs to reconcile against, NOT the numbers to pin —
      the number to pin is the gate's own printed `— N new` figure at THIS commit.
  </read_first>
  <action>
    FIRST run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT on the merged tree,
    with the new suites still unpinned, and capture its printed `— N new` line for each new file. ⛔ Those printed
    figures are what gets pinned. Never a guessed number, never a number copied from a summary, and never a pin
    while the file is red.

    Then, in ONE commit, add to `BASELINE` (keyed by **BASENAME** only, never a path) and to `TARGETS` (the full
    **path**) every new suite:
    `sourceHealthVocabulary.test.ts` · `sourceComposition.test.tsx` · `runHistoryFold.test.ts` ·
    `RunHistoryList.test.tsx` · `WatchedFoldersSection.history.test.tsx` · `useSourceAttention.test.tsx` ·
    `NavPanel.badge.test.tsx` · `SourcesAttentionSection.test.tsx` · `LibraryPage.initialTab.test.tsx`
    (plus any sibling suite a plan reports having created instead of a listed one — reconcile against the eleven
    summaries rather than against this list alone).

    Re-pin the two files whose counts changed: `WatchedFoldersSection.test.tsx` (was 6) and
    `librarySelection.test.ts`.

    Write a comment block above the new entries in BOTH knobs, following Phase 233's template, stating: the phase
    and requirement ids; that BOTH knobs are set in the SAME commit and why; and — ⚠ **checked against the array
    rather than assumed** — that `src/components/sources`, `src/components/library` and `src/components/layout`
    are ALL file-level, because the whole TARGETS array contains only TWO directory entries (`src/landing` and
    `src/components/workflows`).

    Append the green adoption run to `235-BASELINE.md` beneath the red one: the verbatim verdict line, the
    per-file `— N new` figures that were pinned, and the sentence that the pair — red then green — is what makes
    this fence a guard rather than a decoration.
  </action>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The gate prints `count gate OK`, `failed 0`, and an `N/N pinned files present` line whose N increased by the
      number of suites adopted. Record the verbatim verdict line.
    - Exit code 0 — ⚠ exit **2** means a BASELINE key names a path TARGETS does not run; fix the pair, do not
      remove the assertion.
    - Every new suite appears in BOTH knobs — verify mechanically, e.g. by listing the new basenames and grepping
      each against both blocks, rather than by eye.
    - `235-BASELINE.md` now contains BOTH the red run and the green run, each labelled with its date and commit.
    - ⚠ If the gate reds: capture the failing filenames from its own persisted JSON BEFORE re-running anything,
      check each against `git diff --numstat`, and do NOT reach for the worker cap — it is measured not to fix
      these failures (SEED-171). Record any provably-unmodified flaky file as an observation, never as "fine".
  </acceptance_criteria>
  <done>The fence that landed red is green, adopted at the gate's own numbers, with both knobs set together.</done>
</task>

<task type="auto">
  <name>Task 2: Six absent ledger rows, two stale ones, re-derived at the final commit</name>
  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>
  <read_first>
    - `CLAUDE.md` §"Hot-file ledger" — the re-derivation recipe (`git log --oneline`, the phase-bucket `sed`
      pipeline, `wc -l`), the ⚠ note that six-digit buckets are DATED QUICK TASKS and must be subtracted, the
      200-character cap on a disposition cell enforced by `scripts/check-claude-md-size.cjs`, and the
      SAME-COMMIT SYNC RULE (a row here and its section there change together).
    - `235-RESEARCH.md` §14.4 — the re-derived triples measured 2026-09-06 and, more importantly, the six files
      with **NO ROW AT ALL**: `NavPanel.tsx` (19/10/237), `nav-items.ts` (8/6/95), `App.tsx` (30/22/326),
      `HealthTab.tsx` (8/1/184), `librarySelection.ts` (2/2/312), `lib/api/sources.ts` (2/0/163) — plus the two
      STALE rows: `IngestionTab.tsx` (row reads 13/4/456, measures 16/5/487) and `LibraryPage.tsx` (row reads
      40/12/825, measures 42/13/866). ⚠ `LibraryPage.tsx` must be re-derived with `git log --follow` or it reads 1.
    - `docs/HOT-FILE-LEDGER.md` — the per-file section format; ⚠ it carries TWO `## backend/app/api/connectors.py`
      sections already, so check for an existing section before adding one.
    - `.planning/STATE.md` §"Phase 231 ... three things carried out" and §"Phase 232 ... two reviewer corrections"
      — the twice-measured rule: **write the ledger note LAST, or re-derive at the phase's final commit.** A cell
      reading `✅ DISCHARGED` stops the next audit.
  </read_first>
  <action>
    RE-DERIVE the triple for every file this phase modified, at this phase's final commit, using CLAUDE.md's own
    recipe — never copy `235-RESEARCH.md`'s figures, which were measured before the phase wrote a line and which
    this project's own history says rot within days.

    Add rows to CLAUDE.md's ledger table for the six files that have none, and correct the two stale rows. Add a
    row for every net-new file this phase created that will plausibly be touched again (`failure_cause.py`,
    `health_verdict.py`, `sourceHealthVocabulary.ts`, `runHistoryFold.ts`, `RunHistoryList.tsx`,
    `AttentionPopover.tsx`, `attentionConditions.ts`, `SourcesAttentionSection.tsx`, `useSourceAttention.ts`).
    ⛔ Each disposition cell carries the VERDICT and nothing else, under 200 characters — the reasons, the
    invariants and the named seam go in that file's own section in `docs/HOT-FILE-LEDGER.md`, in the SAME commit.
    The disposition-cell cap is enforced by the gate and a long cell FAILS it.

    For each of `NavPanel.tsx` and `App.tsx`, the section must state the finding plainly: the file has been
    invisible to G-5 for its entire life at 10 and 22 phases respectively, so the guardrail could never have
    fired on either — and say whether this phase's change is honoured by construction (one optional prop, one
    decorative element, zero branches) or whether an extraction is now owed.

    Run `node scripts/check-claude-md-size.cjs` and record the character count and the headroom.
  </action>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-claude-md-size.cjs` exits 0, with no `[disposition-too-long]`, `[duplicate-row]` or
      `[malformed-row]` finding. Record the measured character count and the headroom against 150,000.
    - Every file in this phase's combined `files_modified` that is a source file has a row in CLAUDE.md's table
      AND a section in `docs/HOT-FILE-LEDGER.md` — verified mechanically by listing the paths and grepping both
      files, not by eye.
    - Every triple in a row this plan touched was produced by running the recipe at this commit — the summary
      records the commands and their raw output.
    - No row this plan writes reads `satisfied` without a re-derived triple beside it.
  </acceptance_criteria>
  <done>Six files that G-5 could never have fired on are now visible to it, with their reasons in the detail file and their verdicts in the table.</done>
</task>

<task type="auto">
  <name>Task 3: Record the deferrals, the discharges and the measured flag values</name>
  <files>.planning/ROADMAP.md, .planning/seeds/SEED-231-nobody-is-told-an-approval-is-waiting.md, .planning/reported-bugs/watch-sync-button-reports-success-for-work-that-cannot-happen.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 235: The Source Says What It Did" — its **Migrations** line reads
      `161 — 161_connector_sync_runs.sql` and is STALE (D-235-20); the Phase Table row already says 172.
    - `CLAUDE.md` §"Seeds register cross-check" — *"A seed is answered by editing the seed. Flip `status` and
      record where it went; a seed that shipped but still reads `planted` will be re-proposed forever, and one
      that was consciously rejected must say so. ⚠ `status:` frontmatter IS the index — prose inside the body
      saying `still open` is invisible to the scan."*
    - `CLAUDE.md` §"Reported bugs cross-check" — the status lifecycle `open → folded → closed`, and the
      `/gsd:plan-phase` obligation: *verify every report with `folded_into: NNN` is actually addressed by at least
      one plan task.*
    - `235-RESEARCH.md` C-5 — the measured flag table: `watch_process_enabled` default `False` (`config.py:1203`),
      `backend/.env.example` `false`, ⭐ the operator's live `backend/.env` **`true`**, and
      `deploy/onebox.env.example:267` still `false` — so any fresh deployment has the reader OFF.
    - `235-RESEARCH.md` §"Documents read" — `BUG-260906-01`'s frontmatter already reads `status: closed`,
      `folded_into: quick-260906-5qd`, and both `ingest_enrich.py:494-537` and
      `tests/unit/test_ingest_enrich_shared.py` were verified present, so **D-235-18 is DISCHARGED**.
  </read_first>
  <action>
    **ROADMAP.** Correct Phase 235's `**Migrations**` line from `161` to `172 — 172_connector_sync_runs.sql`,
    with a one-line note that the detail block was stale and the Phase Table row was correct (D-235-20). Update
    the phase's `**Plans**` line to the real count and list each plan file with its one-line objective, and tick
    the phase's checkbox state per the executor's actual result.

    **SEED-231.** Record D-235-02's deferral ON the seed, in its frontmatter as well as its body: option C (email
    on permanent failure) is DEFERRED with a TWO-part trigger — *either* a customer reports learning about a dead
    watch from a stale answer, *or* the product gains an app-owned mailer for any other reason. Two independent
    triggers so it cannot be orphaned by whichever arrives first. Also record that Phase 235 built the
    general-purpose shell surface with exactly ONE tenant (D-235-03) specifically so this seed's approval
    notifications can plug in WITHOUT a second surface, and name the file
    (`frontend/src/components/layout/attentionConditions.ts`) and its `ATTENTION_PRODUCERS` array as the seam.
    ⛔ Do not register a second producer.

    **BUG-260906-02.** Flip its status to `closed` if and only if all three parts verifiably shipped (the
    refusal, the outcome reply, the pending state) — and record which plan closed each part. If any part is
    unshipped, leave it `open` with a note naming what remains; a status that disagrees with the code is the
    failure this register exists to prevent.

    **D-235-21 — the measured flag record.** In the phase summary (and referenced from `235-BASELINE.md`), record
    the MEASURED value of every process-level flag this phase depends on, with where it was measured:
    `WATCH_PROCESS_ENABLED` = `true` in the operator's `backend/.env`; `false` in `config.py`'s default; `false`
    in `deploy/onebox.env.example`. State the consequence plainly: **a fresh deployment has the reader OFF**,
    which is exactly why D-235-12's instance-level statement is load-bearing rather than decorative — and why
    Phase 234 passed every gate while being incapable of running in the operator's process.

    **D-235-18 — verify the discharge, do not assume it.** Confirm by reading that
    `backend/app/services/ingest_enrich.py` carries the rule-evaluation step and that
    `backend/tests/unit/test_ingest_enrich_shared.py` exists and passes, and that
    `.planning/reported-bugs/classification-never-runs-on-the-queue-ingest-path.md` reads `status: closed`.
    Record the verification in the summary. ⛔ No plan in this phase implements D-235-18 — it was closed by quick
    task `260906-5qd` before planning, and this is the record of that check.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const seed=fs.readFileSync('.planning/seeds/SEED-231-nobody-is-told-an-approval-is-waiting.md','utf8');if(!/ATTENTION_PRODUCERS/.test(seed))throw new Error('SEED-231 does not name the seam');if(!/mailer/i.test(seed)||!/stale answer/i.test(seed))throw new Error('SEED-231 is missing one half of the two-part trigger');const rm=fs.readFileSync('.planning/ROADMAP.md','utf8');const blk=rm.slice(rm.indexOf('#### Phase 235:'),rm.indexOf('#### Phase 236:'));if(/161_connector_sync_runs/.test(blk))throw new Error('ROADMAP still names the stale migration 161');if(!/172_connector_sync_runs/.test(blk))throw new Error('ROADMAP does not name migration 172');console.log('register + roadmap records OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md`'s Phase 235 block names migration **172** and no longer names 161, and its
      `**Plans**` line matches the number of `235-NN-PLAN.md` files on disk.
    - `SEED-231`'s frontmatter carries the D-235-02 trigger and its body names
      `attentionConditions.ts` / `ATTENTION_PRODUCERS` as the seam. `status:` is updated — prose alone is
      invisible to the register scan.
    - `BUG-260906-02`'s status matches what actually shipped, with the closing plan named per part.
    - The summary records the three measured `WATCH_PROCESS_ENABLED` values and states the fresh-deployment
      consequence.
    - The summary records the D-235-18 discharge verification by file, not by assertion.
  </acceptance_criteria>
  <done>Every deferral has a live trigger, every discharge has a check, and every process flag this phase depends on has a measured value rather than a default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guardrail configuration | the gate, the ledger and the registers are the controls other phases rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-235-40 | Repudiation | a suite that runs but is not guarded, or is guarded but does not run | mitigate | both knobs in one commit, verified mechanically per basename; exit 2 is treated as a pairing bug, never worked around |
| T-235-41 | Repudiation | a ledger cell reading `satisfied` that stops the next audit | mitigate | every triple re-derived at the final commit with CLAUDE.md's own recipe; no `satisfied` without a fresh triple |
| T-235-42 | Repudiation | a deferral with no re-open trigger — a deletion that looks like a decision | mitigate | D-235-02's two-part trigger written into SEED-231's FRONTMATTER, which is what the register scan reads |
| T-235-43 | Tampering | CLAUDE.md crossing the 150,000-character gate and silently ceasing to apply to every agent | mitigate | `node scripts/check-claude-md-size.cjs` run and its headroom recorded; disposition cells capped at 200 chars with the prose in the detail file |
| T-235-SC | Tampering | package installs | n/a | **No package is installed by this plan.** No legitimacy gate applies |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — `count gate OK`, `failed 0`,
  exit 0. Record the verbatim verdict line. ⚠ Compare against the phase's MEASURED starting baseline
  (`total 7544 · failed 0 · pinned total 6814` · `227/227`, 2026-09-06) — a LARGER total is the gate working;
  its contract is no per-file DECREASE and zero failing, never a fixed total.
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q --continue-on-collection-errors` — record the
  verbatim tail. ⚠ The rule is **no NEW failure above 72**, established by `comm -13` over both normalised FAILED
  name-sets, not by comparing totals. ⛔ Do NOT re-pin CLAUDE.md's ceiling of 71 inside this phase — that needs
  explicit operator authorisation and a surface phase is the wrong place to move a project-wide gate. Raise it
  as an open question in the summary instead.
- `node scripts/check-claude-md-size.cjs` — exit 0, headroom recorded.
- `bash scripts/check-deploy-drift.sh` — exit 0.
- `npx tsc -p tsconfig.app.json --noEmit` — record the count against the measured pre-phase baseline.
</verification>

<success_criteria>
- The composition fence is green and adopted, both knobs, one commit, at the gate's own printed figures.
- Six previously invisible files have ledger rows and sections, and every triple was re-derived at this commit.
- The ROADMAP names migration 172; SEED-231 carries a live two-part trigger and names the seam;
  `BUG-260906-02`'s status matches the code.
- The measured value of `WATCH_PROCESS_ENABLED` is recorded in three places with its consequence stated.
- The backend gate reads ≤ 72 with zero NEW failures, and the ceiling question is raised rather than silently
  re-pinned.
</success_criteria>

<output>
Create `.planning/phases/235-the-source-says-what-it-did/235-12-SUMMARY.md` when done. It is the phase's closing
record: the two verbatim gate verdicts, the re-derived triples with their commands, the measured flag values, the
D-235-18 discharge check, and the open question about the 71 → 72 ceiling.
</output>
