# Phase 206.3 — PRE-FLIGHT

**Reviewed:** 2026-08-25 · **Reviewer:** Claude (pre-execution review; the plan is Gemini's and is NOT re-planned here)
**Plan reviewed:** `206.3-01-PLAN.md` @ `a500f29b`
**Verdict:** **PROCEED after G-1 … G-4.** The root-cause analysis is correct and I verified it
independently rather than accepting it. Four gaps must close before execution; three are mechanical.

---

## What I verified myself (not taken on trust)

| Claim | How checked | Result |
|---|---|---|
| `str(KeyError(None))` collapses to `"None"` | ran it in the backend venv | ✅ `'None'` — **this is the whole mechanism, and it is real** |
| Gate 1 calls `_record(...)` | read `phase_types.py:2322-2323` | ✅ verbatim as described |
| `_record` calls `_external_action_body(capability, …)` | read `:2303-2312` | ✅ |
| `_external_action_body` indexes `_EXTERNAL_ACTION_PHRASE[capability]` | read `:1924` | ✅ |
| `capability` is `None` on an MCP step | `_exec_external_action:2273`, and mig-116's closed set has no `"mcp"` | ✅ |
| The refuted-hypothesis story | `test_v20_an_external_action_workflow_publishes` passes | ✅ consistent with 206.2's verification |

**The diagnosis is sound.** It also explains the operator's exact string, which a weaker diagnosis
would not have.

### One measured NON-finding, recorded so nobody re-opens it
`recorded_intent`'s only consumer is `harness_engine.py:2240` / `:2286`, and both are
**truthiness tests** (`elif _recorded_intent:`) — nothing indexes `capability` out of it. So
widening the MCP arm to `{"tool_name":…, "capability":"mcp", "inputs":…}` is safe at the reader,
and `"mcp"` matches what 206.2's live send receipt already writes. **No reader change needed.**

---

## G-1 ⚠ BLOCKER — `_external_action_body` has TWO capability indexes, and only one is named

Every document in this phase names `_EXTERNAL_ACTION_PHRASE` as *the* crash site. Measured, there
are two, one line apart, both inside the same composer:

```
1924:  f"  {'Action'.ljust(width)}: {_EXTERNAL_ACTION_PHRASE[capability]}",
1929:  f"{_EXTERNAL_ACTION_NEGATION[capability]} Nothing left this workflow. "
```

**The fix as written (Task 3 — a separate `_external_action_mcp_body`, never calling
`_external_action_body` for MCP) correctly dodges both.** The risk is not the plan; it is the
*stated diagnosis*. An executor that "simplifies" Task 3 into *"just make `_external_action_body`
tolerate `None`"* fixes `:1924`, and `:1929` fires on the very next line — with the identical
`KeyError: None` signature, which will read as *"the fix didn't work"* rather than as a second site.

**Close it by:** stating in the plan (and in the source comment) that `_external_action_body` is
capability-shaped in **two** places and that the MCP path must not enter it at all. Add an
acceptance criterion: `grep -c "\[capability\]" ` over the MCP composer returns **0**.

⚠ For completeness, five further `[capability]` indexes exist at `:1947 :1950 :1984 :2070 :2111`.
I traced each: `:2070` is already guarded by an explicit `if capability is None`, and the rest sit
on the **live send / failure** paths, which 206.2 drove green for MCP. **None of them is on the
golden-run path** — recorded so this phase does not widen into them without cause.

---

## G-2 ⚠ BLOCKER — no browser drive, and this is the phase that least tolerates its absence

ROADMAP **SC#2** reads: *"reaches `published: true`, **driven in a real browser**."* Task 4 runs
pytest, `tsc` and vitest, and stops.

**This phase exists because a green suite hid a broken path.** 206.2's suites were green while
publish was broken; SEED-201 was found by *pressing the button*, not by reading code. Closing it
with unit evidence alone repeats the exact failure mode the phase is named after.

Two more criteria are only observable in the browser, because they are what a **person sees on the
verdict modal**:
- **SC#1** — the failure is NAMED (no longer the bare string `None`)
- **SC#4** — a blocked verdict carries `golden_run_id` and stops claiming *"there is no run to open"*

**Close it by:** adding a task that drives `◆ Publish` on an MCP-shaped draft in real Chromium and
records `published`, `version`, `blocked_stage` and `golden_run_id` off the modal.
⚠ **Chrome DevTools MCP is unavailable here** — 206.1 and 206.2 both used **Playwright + Chromium**
(`chromium-1223`). ⚠ The builder has **no URL router**: click the rail (`[aria-label="Workflows"]`).
⚠ Read audit rows as `(metadata #>> '{}')::jsonb ->> 'key'` — `metadata` is a **jsonb string scalar
on 2,906 of 2,906 rows**, so the obvious `->>` returns nothing against data that is there.

---

## G-3 — the count-gate command is wrong (mechanical)

Task 4.4 says `npm test -- run` from `frontend/`. That runs vitest; **it is not the count gate** and
performs no per-file baseline comparison, so a per-file DECREASE passes silently.

```
correct:  GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs     # from the REPO ROOT
```

Read the **verdict line verbatim**. A LARGER total is the gate WORKING — the contract is *no
per-file DECREASE* and *zero failing*, never a fixed total.

---

## G-4 — "full backend suite" is five files (mechanical)

Task 4.2 lists five test files and calls it the full suite. This phase edits `phase_types.py`, which
**seven executors** share. The baseline that means something is:

```
cd backend && venv/Scripts/python -m pytest tests/unit -q     # baseline: 68 failed, 2613 passed, 2 xfailed, 2 xpassed
```

⚠ The `68 failed` is the **known, pre-existing** baseline — it is not a regression, and it must be
**re-derived at this phase's own base** rather than quoted from here.

---

## Recommendations (not blockers)

**R-1 — SC#5 is not asserted, only implied.** The ROADMAP requires the D-04 approval checkpoint be
*"intact — asserted, not assumed."* `D-206.3-06` keeps two shipped tests green, which is close but
not the same claim. The fix edits the gate region directly, so add one positive assertion that a
**live (non-golden) MCP run still stops for approval**. Without it, the phase could silently do the
one thing the ROADMAP forbids.

**R-2 — the exception-transparency fix has no negative control.** Task 1 proves the *happy* path.
Nothing proves a *different* exception now surfaces honestly. Plant one inside the golden run and
assert the verdict names its type — otherwise *"no longer collapses to `None`"* ships untested, and
the next swallowed failure looks exactly like this one did.

**R-3 — the failure-path SEAM is untested.** Task 1's test is a genuine integration test that mocks
neither side (good — that is precisely what the 204 post-mortem asked for). But it covers only the
**success** path, while Task 2's `run_id` preservation lives on the **failure** path, where
`publish_service.py` and `phase_types.py` meet. ⚠ **The 204 pre-flight caught three gaps and missed
both defects that reached the operator — and both were seams with each side green.** Add a case that
makes the golden run raise *after* `create_workflow_run` and asserts the block carries the real
`golden_run_id`.

**R-4 — verify `result["version"] == 2` at execution.** Task 1 asserts it; I could not confirm the
fixture's starting version cheaply. If the mirror definition starts elsewhere the assertion is
wrong for a reason unrelated to this phase.

**R-5 — ledger.** `phase_types.py` and `publish_service.py` both carry ledger rows; this phase moves
both. Re-derive the triples at the close commit — do not increment by hand. ⚠ CLAUDE.md is at
**133,006 chars** against a 150,000 hard limit, so keep prose in `docs/HOT-FILE-LEDGER.md`; thinner
rows are not the remedy.

---

## What the plan gets right, and is worth keeping

- **TDD gate first, failing at HEAD, naming the exact exception.** That is the correct shape for a
  phase whose whole premise is that a cause was previously *inferred* rather than *observed*.
- **It refuses the tempting wrong fix.** Nothing here weakens the approval checkpoint; the governance
  question the ROADMAP flagged as operator-owned turns out to be **already answered by shipped
  D-16/D-19** — golden runs suppress the send and record intent. That is a real finding, not a dodge.
- **One plan, four tasks.** Correctly sized: this is a narrow defect, and ceremony scales with plan
  count, not with risk.
- Scope fences on `api.ts` (Phase 207) and `harness_engine.py` are stated and correct.
